# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Managers/GameStateManager.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`

### Client references checked

- `CLogin::Init` at `0x5d8010`
- `CLogin::Update` at `0x5dee90`
- `CLogin::OnPacket` at `0x5df940`
- `CUICharSelect::OnCreate` at `0x5e8550`
- `CUICharSelect::OnButtonClicked` at `0x5e0350`
- `CUIAvatar::OnCreate` at `0x5ebe10`
- `CUIAvatar::OnMouseButton` at `0x5ed130`
- `CUICharDetail::Draw` at `0x5e8860`
- `CConnectionNoticeDlg::Draw` at `0x5d6a30`
- `CLoginUtilDlg::Draw` at `0x5eee70`
- `CUIWorldSelect::OnCreate` at `0x60b660`
- `CUIWorldSelect::OnButtonClicked` at `0x6025e0`
- `CUIChannelSelect::OnCreate` at `0x606a10`
- `CUIChannelSelect::Draw` at `0x6056d0`
- `CUIChannelSelect::OnButtonClicked` at `0x6035f0`
- `CUIChannelShift::OnCreate` at `0x96c160`
- `CUIChannelShift::Draw` at `0x96ccb0`
- `CLogin::OnCheckDuplicatedIDResult` at `0x5d5790`
- `CLogin::OnCreateNewCharacterResult` at `0x5dab90`
- `CUINewCharNameSelect::OnCreate` at `0x5f3330`
- `CUINewCharNameSelect::OnButtonClicked` at `0x5f4fe0`
- `CUINewCharNameSelect::OnKey` at `0x5f5090`
- `CUINewCharAvatarSelect::OnCreate` at `0x5f3340`
- `CUINewCharAvatarSelect::OnButtonClicked` at `0x5f36b0`
- `CUINewCharAvatarSelect::Draw` at `0x5f51a0`
- `CUINewCharJobSelect::OnButtonClicked` at `0x5f35d0`
- `CUINewCharJobSelect::Draw` at `0x5f38e0`
- `CUINewCharJobSelect::OnCreate` at `0x5fa760`
- `CUINewCharRaceSelect::Draw` at `0x5f37d0`
- `CUINewCharRaceSelect::OnCreate` at `0x5f81c0`
- `CUINewCharRaceSelect::OnButtonClicked` at `0x5f9470`
- `CUIRecommendWorld::Draw` at `0x603980`
- `CUIRecommendWorld::OnCreate` at `0x6073a0`
- `CUIRecommendWorld::OnButtonClicked` at `0x607910`
- `CLogin::OnWorldInformation` at `0x5da7f0`
- `CLogin::OnRecommendWorldMessage` at `0x5d7280`
- `CLogin::OnLatestConnectedWorld` at `0x5d2200`
- `CLogin::OnCheckUserLimitResult` at `0x5d2250`
- `CLogin::OnViewAllCharResult` at `0x5de120`
- `CLogin::OnSelectCharacterResult` at `0x5dea80`
- `CLogin::OnDeleteCharacterResult` at `0x5d9e10`
- `CLogin::OnExtraCharInfoResult` at `0x5d25a0`
- `CLogin::OnSelectCharacterByVACResult` at `0x5de670`

Notes:
IDA confirms there is a coherent pre-field client surface that is not covered by the first ten backlog docs. The login bootstrap, world/channel selection owners, character-selection roster, avatar preview, character-detail panel, recommend-world helper, connection or utility dialogs, and the separate create-character race/job/avatar/name owners all have dedicated client classes and do not fit cleanly under in-map HUD work or the broader social-window bucket.

## Client Function Index By Backlog Area

The list below maps this backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Login and character-entry parity

- `CLogin::Init` at `0x5d8010`
- `CLogin::Update` at `0x5dee90`
- `CLogin::OnPacket` at `0x5df940`
- `CUICharSelect::OnCreate` at `0x5e8550`
- `CUICharSelect::OnButtonClicked` at `0x5e0350`
- `CUIAvatar::OnCreate` at `0x5ebe10`
- `CUIAvatar::OnMouseButton` at `0x5ed130`
- `CUICharDetail::Draw` at `0x5e8860`
- `CConnectionNoticeDlg::Draw` at `0x5d6a30`
- `CLoginUtilDlg::Draw` at `0x5eee70`
- `CUIWorldSelect::OnCreate` at `0x60b660`
- `CUIWorldSelect::OnButtonClicked` at `0x6025e0`
- `CUIChannelSelect::OnCreate` at `0x606a10`
- `CUIChannelSelect::Draw` at `0x6056d0`
- `CUIChannelSelect::OnButtonClicked` at `0x6035f0`
- `CUIChannelShift::OnCreate` at `0x96c160`
- `CUIChannelShift::Draw` at `0x96ccb0`
- `CUIRecommendWorld::Draw` at `0x603980`
- `CUIRecommendWorld::OnCreate` at `0x6073a0`
- `CUIRecommendWorld::OnButtonClicked` at `0x607910`

Notes:
`CLogin::Init`, `CLogin::Update`, and `CLogin::OnPacket` confirm a dedicated login-state machine and packet router rather than a generic splash-to-map handoff. `CUIWorldSelect`, `CUIChannelSelect`, and `CUIChannelShift` show that pre-field server/world selection is also its own owner family rather than a generic extension of utility-window browsing once the simulator is already in a field: `CUIWorldSelect::OnCreate` builds a dedicated world-info layer and repopulates world items, `CUIChannelSelect::OnCreate` resets world/channel population state and creates its own button/layout owner from StringPool/WZ assets, and `CUIChannelShift` keeps a separate transition surface alive while the channel change is in flight. `CUICharSelect`, `CUIAvatar`, and `CUICharDetail` confirm that roster browsing, avatar preview, and character metadata are separate entry-flow surfaces with their own create, draw, and click handling. `CConnectionNoticeDlg` and `CLoginUtilDlg` confirm that account-entry prompts such as connection notices and utility dialogs are distinct UI owners, while `CUIRecommendWorld` shows that server recommendation is not just data on the world list but a dedicated helper surface.

### 2. Additional client-owned create-character wrappers discovered by targeted IDA scan

- `CLogin::OnCheckDuplicatedIDResult` at `0x5d5790`
- `CLogin::OnCreateNewCharacterResult` at `0x5dab90`
- `CUINewCharNameSelect::OnCreate` at `0x5f3330`
- `CUINewCharNameSelect::OnButtonClicked` at `0x5f4fe0`
- `CUINewCharNameSelect::OnKey` at `0x5f5090`
- `CUINewCharAvatarSelect::OnCreate` at `0x5f3340`
- `CUINewCharAvatarSelect::OnButtonClicked` at `0x5f36b0`
- `CUINewCharAvatarSelect::Draw` at `0x5f51a0`
- `CUINewCharJobSelect::OnButtonClicked` at `0x5f35d0`
- `CUINewCharJobSelect::Draw` at `0x5f38e0`
- `CUINewCharJobSelect::OnCreate` at `0x5fa760`
- `CUINewCharRaceSelect::Draw` at `0x5f37d0`
- `CUINewCharRaceSelect::OnCreate` at `0x5f81c0`
- `CUINewCharRaceSelect::OnButtonClicked` at `0x5f9470`

Notes:
Targeted IDA lookup shows the client does not treat new-character creation as a small extension of `CUICharSelect`. It has a separate owner family for race selection, job selection, avatar selection, and typed name validation, with `CLogin` packet handlers feeding duplicate-name and create-result state back into that flow.

### 3. Additional pre-field selector packet handlers discovered by targeted `CLogin::OnPacket` scan

- `CLogin::OnWorldInformation` at `0x5da7f0`
- `CLogin::OnRecommendWorldMessage` at `0x5d7280`
- `CLogin::OnLatestConnectedWorld` at `0x5d2200`
- `CLogin::OnCheckUserLimitResult` at `0x5d2250`

Notes:
The current login doc names the pre-field selector windows, but this targeted IDA pass shows the packet-fed selector ownership is also a first-class login seam rather than only window-local state. `CLogin::OnWorldInformation` populates the world/channel list payloads that `CUIWorldSelect` and `CUIChannelSelect` render, `CLogin::OnRecommendWorldMessage` feeds the helper/recommendation surface, `CLogin::OnLatestConnectedWorld` restores the client-owned recent-world focus, and `CLogin::OnCheckUserLimitResult` gates the handoff from world selection into channel selection instead of acting like a generic error popup. Those handlers are already important enough in the simulator codebase to have their own `LoginRuntimeManager`, `LoginWorldInfoPacketCodec`, and `LoginSelectorPacketPayloadCodec` seams, so they should be tracked explicitly in the login backlog rather than being left implicit or only mentioned in the broader progression/utility doc.

### 4. Additional account-dialog and security-result handlers confirmed by targeted IDA lookup

- `CLogin::OnSetAccountResult` at `0x5d5e80`
- `CLogin::OnConfirmEULAResult` at `0x5d4d00`
- `CLogin::OnCheckPinCodeResult` at `0x5db000`
- `CLogin::OnSelectWorldResult` at `0x5dda00`
- `CLogin::OnUpdatePinCodeResult` at `0x5d2420`
- `CLogin::OnEnableSPWResult` at `0x5d2290`
- `CLogin::OnCheckSPWResult` at `0x5d23f0`

Notes:
The earlier login rows already described this modal/account surface in prose, but the latest IDA lookup pins the concrete handlers and keeps them out of the generic utility bucket. `CLogin::OnSetAccountResult`, `CLogin::OnConfirmEULAResult`, and `CLogin::OnCheckPinCodeResult` sit on the bootstrap chain that decides whether account setup can advance without reopening the wrong prompt; `CLogin::OnSelectWorldResult` owns the broader world-entry error and website-handoff family after selector choice; and `CLogin::OnUpdatePinCodeResult`, `CLogin::OnEnableSPWResult`, and `CLogin::OnCheckSPWResult` prove that PIC/SPW completion notices are first-class login-owned result handlers rather than anonymous canned dialogs. These handlers already map onto the simulator's `LoginRuntimeManager`, account-dialog codec path, and login utility windows, so keeping them indexed here makes future parity work start from the actual client owners instead of scattered row prose.

### 5. Additional roster-result handlers discovered by targeted `CLogin::OnPacket` scan

- `CLogin::OnViewAllCharResult` at `0x5de120`
- `CLogin::OnSelectCharacterResult` at `0x5dea80`
- `CLogin::OnDeleteCharacterResult` at `0x5d9e10`

Notes:
The targeted `CLogin::OnPacket` pass exposed another login-owned result family that was still only implied by the roster UI row. `OnViewAllCharResult` is not just more selector data; the decompile shows it owning the multi-server `ViewAllChar` aggregation path, the success-vs-failure notice branches, the `GotoWorldSelect` fallback when no characters arrive, and the separate VAC dialog handoff once the related-server count drains. `OnSelectCharacterResult` is likewise broader than a generic roster click result because it owns its own error table, title-return cases, website-handoff prompts, and the direct `CWvsContext::IssueConnect` success path for both normal and alternate result families. `OnDeleteCharacterResult` is another distinct owner rather than only a button callback, because it owns the roster mutation itself, the direct-notice StringPool text path (`0xFD4`), the mapped `CLoginUtilDlg::Error` table, and the follow-up avatar/detail refresh after a successful deletion. Those result handlers are now concrete enough to track as their own packet-owned seam inside the login backlog instead of leaving them spread across avatar or utility-window documents.

### 6. Additional extra-character entitlement handler discovered by targeted `CLogin::OnPacket` scan

- `CLogin::OnExtraCharInfoResult` at `0x5d25a0`

Notes:
This handler is still absent from the login backlog even though it owns a distinct roster-side entitlement seam. The decompile shows `CLogin::OnExtraCharInfoResult` clearing `m_bCanHaveExtraChar`, decoding an account id plus a one-byte result flag, and only re-enabling the extra-character entitlement when the packet account matches the active `CWvsContext` account and the result flag is `0`. That is not just another selector refresh; it is the client-owned gate for whether the roster should expose a `buyCharacter` slot at all, and the simulator already has matching seams in `LoginExtraCharInfoResultCodec`, `LoginRuntimeManager`, `_loginCanHaveExtraCharacter`, and the roster `BuyCharacterCount` path.

### 7. Additional alternate-entry packet handler discovered by targeted `CLogin::OnPacket` scan

- `CLogin::OnSelectCharacterByVACResult` at `0x5de670`

Notes:
`CLogin::OnPacket` routes packet type `9` into `CLogin::OnSelectCharacterByVACResult`, which is a distinct alternate character-entry path rather than a synonym for the normal `OnSelectCharacterResult` flow. IDA shows it owns its own result-code table, title-return failures, website-handoff prompts, and direct `CWvsContext::IssueConnect` success path, so it should be tracked explicitly instead of being assumed to fall under the ordinary roster-select result handler.

## Current State Summary

This area is still mostly absent from the simulator today.
The project currently includes:

- Cash-shop-map and wider game-state gating flags in `MapSimulator.cs` and `GameStateManager.cs`.
- Status-bar utility buttons that already expose some neighboring service entry points once the simulator is in-map.
- Local avatar loading and runtime state that now feed a simulator-side login roster window with selectable character builds and roster-driven field entry on login maps, even though avatar-preview and detail-specific surfaces are still separate gaps.
- A dedicated login bootstrap runtime in `HaCreator/MapSimulator/Managers/LoginRuntimeManager.cs` that now owns pre-field steps, delayed login-step changes, and login-packet dispatch on login maps.

The gap is not a small missing window shell.
The gap is that the simulator does not currently model the client's entire account-entry and character bootstrap pipeline before gameplay begins.

## Remaining Parity Backlog

### 1. Login and character-entry parity

This area is not represented by the existing ten documents even though IDA shows a large, dedicated client surface here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Login bootstrap and packet-state parity | The simulator now wires the dedicated login bootstrap runtime into a real login-title owner built from `Login.img/Title` (`backFrame`, `MSTitle`, `MapleDolls`, `signboard`, `ID`, `PW`, plus the title buttons), drives CheckPassword or GuestId bootstrap requests from that UI through the existing login-packet inbox instead of chat commands alone, surfaces live inbox status on the title scene, reuses the runtime's delayed step timing to show a visible connection-notice progress pass while Title advances into WorldSelect, and now keeps the packet-routed account follow-up chain alive inside the existing `Login.img/Notice` utility owner by accepting EULA, capturing typed PIC and SPW setup or verification, recording migration decisions, and feeding the accepted path back into the inbox until the flow can resume toward `CheckPasswordResult` instead of stopping at one-shot modal text. The row also now decodes and stores packet-authored account-dialog payloads for `AccountInfoResult`, `SetAccountResult`, `ConfirmEulaResult`, `CheckPinCodeResult`, `UpdatePinCodeResult`, `EnableSpwResult`, `CheckSpwResult`, `CreateNewCharacterResult`, and `DeleteCharacterResult`, so the shared utility owner can display packet-carried text, result codes, account ids, and character ids instead of only simulator-authored boilerplate. Client inspection of `CLogin::OnSetAccountResult`, `CLogin::OnConfirmEULAResult`, and `CLogin::OnCheckPinCodeResult` is now reflected in the active packet path as well: packet-authored success payloads for `SetAccountResult`, `ConfirmEulaResult`, and `CheckPinCodeResult` no longer reopen the generic migration, EULA, or PIC modal, but suppress that success dialog, enqueue the next bootstrap packet through the inbox, and keep the login chain advancing the way the client does; packet-authored `CheckPinCodeResult` prompt selection is now result-code aware so code `1` opens PIC creation, codes `2` and `4` open PIC verification, and code `7` forces the runtime back to Title while still surfacing the client-backed failure notice. WZ inspection of `Login.img/Title/BtHomePage` and `Login.img/Notice/BtNexon` is now also reflected in the active path: the title owner exposes the homepage button, and the security-site `SelectWorldResult` code `14` / `15` prompts can continue into a dedicated website-handoff step inside the existing notice seam instead of stopping at a passive yes/no placeholder. Remaining work is narrower: intake is still a simulator loopback packet feed rather than the client's authenticated socket protocol, the decoded follow-up payloads are still cached command or loopback data rather than live backend session state, and dedicated owners beyond the current title plus notice pair are still approximated through the shared utility or notice seam instead of implemented as separate packet-backed client classes | Without the login-state owner, all account-facing transitions are effectively skipped, which hides an entire category of client behavior before the character ever enters a map. The new runtime establishes that owner so later roster, avatar, and dialog work can attach to a real pre-field pipeline instead of jumping straight into a field | account-entry runtime, state machine, packet-routing layer (`CLogin::Init`, `CLogin::Update`, `CLogin::OnPacket`, `CLogin::OnSetAccountResult`, `CLogin::OnConfirmEULAResult`, `CLogin::OnCheckPinCodeResult`; simulator seam: `HaCreator/MapSimulator/Managers/LoginRuntimeManager.cs`) |
| Partial | World, channel, and channel-shift selector parity | The dedicated pre-field selector owner family is no longer absent from the simulator login runtime. The simulator already routes the login flow through dedicated `WorldSelect`, `ChannelSelect`, and timed `ChannelShift` owners, preserves the current world/channel choice in the login scene, simulates per-world channel occupancy plus busy/full gating, and resolves world/channel picks through delayed `CheckUserLimitResult` / `SelectWorldResult` style responses instead of committing immediately. WZ inspection of `Login.img/WorldSelect` is now also reflected more directly in the active owner shell: the selector loader sources the world frame from `scroll/0/{0,0_bak}`, world badges from `world/*`, world buttons from `BtWorld/*`, the world toggle buttons from `BtViewChoice` / `BtViewAll`, the channel shells from `chBackgrn` / `chBackgrn_Bak`, the channel enter button from `BtGoworld`, and the channel rows from `channel/*/{normal,disabled}` instead of keeping the main selector windows on placeholder world chrome and `UIWindow/Channel` art. The selector seam also now accepts packet-authored `WorldInformation`, `RecommendWorldMessage`, `LatestConnectedWorld`, and `CheckUserLimitResult` payloads through the login packet/runtime path, which matches the client-owned feeder handlers confirmed in this IDA pass (`CLogin::OnWorldInformation`, `CLogin::OnRecommendWorldMessage`, `CLogin::OnLatestConnectedWorld`, `CLogin::OnCheckUserLimitResult`) rather than leaving the selector windows to run on simulator-only metadata. The remaining gap is narrower: the simulator still does not source or decrypt the true authenticated upstream login feed on its own, still approximates the selector owner's final packet/session choreography through loopback/replay metadata, and still does not mirror every last world/channel micro-layout, selector animation state, or non-entry service side path exactly. | These selectors sit on the main pre-field path between account bootstrap and character entry. Keeping them explicit in the login backlog matters because the client treats world selection, channel selection, channel-shift presentation, and the packet handlers that feed them as part of one pre-field owner family rather than as generic utility windows. | pre-field server-selection owner family (`CUIWorldSelect::OnCreate`, `CUIWorldSelect::OnButtonClicked`, `CUIChannelSelect::OnCreate`, `CUIChannelSelect::Draw`, `CUIChannelSelect::OnButtonClicked`, `CUIChannelShift::OnCreate`, `CUIChannelShift::Draw`, `CLogin::OnWorldInformation`, `CLogin::OnRecommendWorldMessage`, `CLogin::OnLatestConnectedWorld`, `CLogin::OnCheckUserLimitResult`; simulator seams: `HaCreator/MapSimulator/UI/Windows/ChannelSelectionWindows.cs`, `HaCreator/MapSimulator/UI/Windows/RecommendWorldWindow.cs`, `HaCreator/MapSimulator/Managers/LoginRuntimeManager.cs`, `HaCreator/MapSimulator/Managers/LoginWorldInfoPacketCodec.cs`, `HaCreator/MapSimulator/Managers/LoginSelectorPacketPayloadCodec.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Partial | Packet-owned roster aggregation and selector-result parity | The login runtime now owns the broader selector-result family instead of leaving it implied by the roster UI alone. `ViewAllCharResult` keeps packet-authored roster chunks, delayed related-server aggregation, success-vs-failure notices, and the fallback `GotoWorldSelect` or VAC-dialog handoff inside the active path; `SelectCharacterResult` keeps its own client-backed error table, website-handoff prompts, title-return branches, and direct `IssueConnect` success payload instead of collapsing into a generic roster click result; and `DeleteCharacterResult` now stays on the same packet-owned mutation seam by applying the mapped `CLoginUtilDlg::Error` notices, the direct `StringPool` notice text for id `0xFD4`, the roster shrink, and the follow-up avatar/detail refresh through the live login runtime rather than only local UI state. This pass narrows the remaining `ViewAllChar` gap materially: the local no-session path no longer fakes expanded-roster entry with one generic result, but synthesizes the client-shaped header plus per-world chunk plus completion sequence from the authenticated account-backed world rosters (reusing the active `SelectWorldResult` world when present), and the active handler now waits for real aggregation completion before entering `ViewAllCharacters`, returns to WorldSelect immediately on zero-count header/completion branches, and forces the title/world fallback splits for the client VAC error-family result codes instead of treating every packet as a generic step advance. Remaining work is narrower and now mostly upstream-session owned: the simulator still does not drive that multi-server `ViewAllChar` choreography through a true authenticated backend session manager, and some final child-modal ownership nuances from `CLogin::OnViewAllCharResult`, `CLogin::OnSelectCharacterResult`, and `CLogin::OnDeleteCharacterResult` are still approximated through the shared simulator modal seam rather than dedicated packet-backed client dialog classes. | This keeps packet-owned roster/result behavior anchored to the actual `CLogin` handlers that mutate roster state and enter gameplay. Without an explicit row here, the login backlog overstates parity by counting only the selector windows while the actual result-routing, deletion ownership, and multi-server roster aggregation logic remain half-hidden inside other documents. | packet-owned login roster/result layer (`CLogin::OnViewAllCharResult`, `CLogin::OnSelectCharacterResult`, `CLogin::OnDeleteCharacterResult`; simulator seams: `HaCreator/MapSimulator/Managers/LoginViewAllCharResultCodec.cs`, `HaCreator/MapSimulator/Managers/LoginSelectCharacterResultCodec.cs`, `HaCreator/MapSimulator/Managers/LoginCreateCharacterResultCodecs.cs`, `HaCreator/MapSimulator/Managers/LoginRuntimeManager.cs`, `HaCreator/MapSimulator/Managers/LoginBackendSessionManager.cs`, `HaCreator/MapSimulator/Managers/LoginCharacterRosterManager.cs`, `HaCreator/MapSimulator/UI/Windows/CharacterSelectWindow.cs`, `HaCreator/MapSimulator/UI/Windows/LoginUtilityDialogWindow.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Partial | Extra-character slot entitlement parity | The simulator now keeps the extra-character entitlement on the same authenticated-account gate the client uses. Client inspection of `CLogin::OnExtraCharInfoResult` (`0x5d25a0`) shows the handler clearing `m_bCanHaveExtraChar`, decoding an account id plus one-byte result flag, and only re-enabling the entitlement when that packet account matches the active `CWvsContext` account and the result flag is `0`; the active simulator path now mirrors that by routing `ExtraCharInfoResult` through `LoginBackendSessionManager.ApplyExtraCharInfoResult` instead of trusting the decoded flag alone, reapplying that gate when the login bootstrap later resolves an authenticated account id, and immediately syncing the resulting `buyCharacter` state back into the live roster plus persisted account-backed roster store instead of leaving the carousel on stale local state. The account-backed fallback is also tighter than before: when a stored or packet-authored account id becomes available, `LoginCharacterAccountStore` now promotes the same roster snapshot and `BuyCharacterCount` from the legacy name key to the authenticated account-id key so the extra-slot entitlement survives the same account/world bootstrap seam as the rest of the roster state. Remaining work is now narrower and still backend-owned: the simulator still reaches this row through loopback, replayed, or persisted session state instead of a true authenticated login socket flow, and it still does not mirror the broader server-side account-billing lifecycle beyond the single account-id-plus-result gate that `CLogin::OnExtraCharInfoResult` applies. | If this seam stays implicit inside the broader roster row, the backlog overstates login parity by treating the extra `buyCharacter` slot as simple roster decoration rather than a packet-owned account entitlement. Naming it explicitly keeps future work anchored to the real client handler that decides whether the roster can expose an extra slot at all. | login roster-entitlement layer (`CLogin::OnExtraCharInfoResult`; simulator seams: `HaCreator/MapSimulator/Managers/LoginExtraCharInfoResultCodec.cs`, `HaCreator/MapSimulator/Managers/LoginRuntimeManager.cs`, `HaCreator/MapSimulator/Managers/LoginBackendSessionManager.cs`, `HaCreator/MapSimulator/Managers/LoginCharacterAccountStore.cs`, `HaCreator/MapSimulator/Managers/LoginCharacterRosterManager.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Partial | Character roster and selection parity | The simulator now exposes a dedicated login-map character-select window, keeps a slot-aware roster model that distinguishes occupied entries from empty and buy-character slots, pages the selection surface across the full visible slot count instead of only populated entries, renders the client `Login.img/CharSelect` empty-slot silhouette and animated `buyCharacter/*` card state on those extra pages, and continues to hand the selected roster build into field entry before map transition. WZ inspection of `Login.img/CharSelect` still keeps the row anchored to the existing `BtSelect`/`BtNew`/`BtDelete` roster seam, while client inspection of `CUICharSelect::OnButtonClicked`, `CLogin::OnCreateNewCharacterResult`, and `CLogin::OnDeleteCharacterResult` confirms the real client sends create/delete through dedicated packets and then mutates the in-memory avatar roster from those packet results rather than treating the buttons as local-only UI edits. Packet-authored roster data from `SelectWorldResult` and `ViewAllCharResult` is now part of the active path rather than the window always depending on simulator-seeded entries, and that packet-owned roster now caches into a persisted simulator account roster keyed by authenticated account id when the login bootstrap has surfaced one, with the existing login-account plus world key still acting as the fallback seam when packets are absent, so the no-packet path reopens the correct account-backed list instead of reseeding ad hoc simulator entries or colliding across reused account names. This pass moves the live roster ownership further into the intended backend seam: `LoginBackendSessionManager` now captures the authenticated account id from the login bootstrap, owns the active `SelectWorldResult` / assembled `ViewAllCharResult` roster profiles, feeds active-roster selection back into character-select, stays in the mutation path when `BtNew` or `BtDelete` rewrites the session roster from packet-authored create/delete results instead of leaving `MapSimulator` to reconcile duplicate snapshot fields on its own, now persists the cached multi-world `ViewAllCharResult` roster snapshots back into the authenticated account store so packet-owned refresh can survive later no-packet reopen, and now lets generated `ViewAllCharResult` discovery enumerate every persisted world owned by that authenticated account instead of only the currently registered selector worlds. Remaining work is now narrower and mostly upstream-session owned: the simulator still does not reconcile those session-owned rosters against a true authenticated backend beyond the existing official-session bridge and packet-configuration seam, so live server-authoritative refresh, conflict resolution against concurrent external roster changes, and a byte-for-byte remote session owner for the full multi-packet account lifecycle are still incomplete. | The client's path into gameplay is built around explicit character selection rather than spawning a hardcoded local avatar directly into a field, so even this partial shell materially changes how the simulator enters gameplay on login maps | roster UI layer, character-selection model (`CUICharSelect::OnCreate`, `CUICharSelect::OnButtonClicked`; simulator seams: `HaCreator/MapSimulator/Managers/LoginCharacterRosterManager.cs`, `HaCreator/MapSimulator/Managers/LoginCharacterAccountStore.cs`, `HaCreator/MapSimulator/Managers/LoginBackendSessionManager.cs`, `HaCreator/MapSimulator/UI/Windows/CharacterSelectWindow.cs`, `HaCreator/MapSimulator/UI/Windows/AvatarPreviewCarouselWindow.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Partial | Alternate VAC character-entry parity | WZ inspection confirms the client notice family this branch depends on is present in `Login.img/Notice/text` (`14`, `15`, `16`, `17`, `19`, `20`, `21`, `27`, `33`, `40`, `56`, `62`), and the simulator now names and routes the alternate packet type `9` owner explicitly instead of treating it like a reused normal select-character result. A dedicated `LoginSelectCharacterByVacResultCodec` / `LoginSelectCharacterByVacResultProfile` now decodes the VAC payload as its own `result + secondary` structure, preserves the direct connect payload for the VAC success branches (`result = 0`, `result = 23`, and `result = 12` with secondary `11` or `13`), feeds that branch through a dedicated `TryApplyPacketOwnedSelectCharacterByVacResult` owner, and keeps the runtime, login-packet chat configuration, success messaging, and backlog naming aligned with `CLogin::OnSelectCharacterByVACResult` rather than the normal `SelectCharacterResult` path. The VAC failure table is now also distinct in the active simulator path: result codes `14` and `15` reopen the existing website-handoff prompt through the same security-site notice seam, while the VAC-owned notice family (`16`, `17`, `19`, `20`, `21`, `27`, `33`, `40`, `56`, `62`) maps through dedicated VAC notice handling and can return the runtime to Title without being misfiled under the normal select-character response-code table. Remaining work is now narrower and client-confirmation-heavy: the simulator still approximates the exact per-code title-reset split recovered from IDA rather than replaying a live authenticated VAC session, and it still does not mirror the client’s final `IssueConnect` / entry-sound ordering byte-for-byte beyond the simulator-side field-entry handoff. | If this path stays invisible, the simulator can look complete for character entry while still missing a real client-owned alternate bootstrap route that bypasses part of the ordinary roster-select result handling. Tracking it separately keeps future work anchored to the actual packet router and prevents VAC-only failures or handoffs from being misfiled under the standard character-select row. | alternate login packet-entry layer (`CLogin::OnPacket`, `CLogin::OnSelectCharacterByVACResult`, `CWvsContext::IssueConnect`, `CLoginUtilDlg::Error`; simulator seams: `HaCreator/MapSimulator/Managers/LoginSelectCharacterByVacResultCodec.cs`, `HaCreator/MapSimulator/Managers/LoginRuntimeManager.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `HaCreator/MapSimulator/MapSimulator.ChatCommands.cs`) |
| Partial | Avatar preview carousel parity | The simulator now drives the login avatar-preview surface through a dedicated `AvatarPreviewCarouselWindow` owner separate from the wider roster shell, replaces the placeholder preview cards and page controls with client `Login.img/CharSelect` canvases (`charInfo`, `charInfo1`, `pageL`, `pageR`, plus the real `BtSelect`/`BtNew`/`BtDelete` button art on the roster shell), keeps the live assembled three-slot browsing, card focus, page-left/page-right cycling, and double-click field-entry handoff aligned with the three-slots-per-page interaction recovered from `CUIAvatar::OnMouseButton`, renders the per-card client nametag strip and job-family decoration path (`nameTag`, `adventure`, `knight`, `aran`, `evan`, `resistance`) using the client job-group selection logic recovered from `CUIAvatar::ResetCharacter`, and now replaces the wider simulator-owned owner shell with the client `Login.img/CharSelect` shell layers that are present in this data set (`scroll/0`, `effect/0`, `effect/1`, `character/0`, and the `BtSelect/keyFocused` flourish) so the roster owner animates a parchment status shell and client selection accent behind the carousel instead of only a plain frame plus footer text. This pass also closes the broader text fallback gap inside the same owner family: the carousel nametag width now follows the client rule recovered from `CUIAvatar::ResetCharacter` (`min 58px`, otherwise `CalcTextWidth + 18`), the nametag text no longer hardcodes `Tahoma`, and both the carousel nametag text and the owner shell's `FONT_BASIC_BLACK` copy now reuse the simulator's client-font probing path so the login carousel no longer depends only on generic installed-font guesses before falling back toward simulator text. Remaining work is now narrower and explicit: the exact client `IWzFont` assets behind the avatar-name and owner-copy fonts are still not identified or bundled byte-for-byte by default, and any still-unrecovered micro-placement or extra owner children around the `CharSelect` shell remain to be mirrored end-to-end | Character-entry parity needs more than a text roster; the client lets the player browse and inspect live avatar previews before entering the game, and the simulator now exposes that browse and inspect loop through a dedicated preview owner with client-backed card, paging, nametag, job-decoration, and outer-shell art instead of only a text list | avatar-preview UI layer, character-preview assembly path (`CUIAvatar::OnCreate`, `CUIAvatar::OnMouseButton`, `CUIAvatar::ResetCharacter`; simulator seams: `HaCreator/MapSimulator/UI/Windows/AvatarPreviewCarouselWindow.cs`, `HaCreator/MapSimulator/UI/Windows/CharacterSelectWindow.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`) |
| Partial | Character detail panel parity | The simulator now constrains the login-map character-detail owner to the real client `Login.img/CharSelect/charInfo` and `charInfo1` canvases instead of extending them with a simulator-authored lower metadata slab, resolves and draws the real `icon/{up,down,same,job/*}` rank assets, keeps the world and job rank lanes right-aligned off the recovered `CUICharDetail::Draw` composition, and renders the detail rows live in the window draw pass rather than rasterizing text into a replacement panel bitmap. Client inspection of `CUICharDetail::Draw` confirms the owner copies the background first and then renders the stat rows with `FONT_BASIC_BLACK`, so the simulator now follows that same background-plus-live-overlay structure and no longer depends on the shared MonoGame `SpriteFont` metrics for the normal detail rows: `CharacterDetailWindow` measures and caches those rows through a dedicated runtime bitmap-text path, no longer hard-gates detail-row drawing on a shared `SpriteFont`, now sizes the fallback raster font at the client-backed `12`-pixel `FONT_BASIC_BLACK` height recovered from `get_basic_font`, and trims/caches the rendered glyph bitmap before alignment so right-aligned rank numbers key off the actual rendered ink bounds instead of the looser pre-render `MeasureString` box. WZ inspection for `Login.img/CharSelect` still confirms `charInfo1` is only the taller second rank panel, not a hidden native owner for extra name, guild, EXP, or map rows, and client inspection of `get_basic_font` still points this branch at the same `DODOOMCHE` or basic-black face family used elsewhere. This pass narrows the remaining font-identity gap further: the detail owner no longer depends only on generic installed Windows fonts when a closer private face is available locally, because `CharacterDetailWindow` now accepts both the row-specific `MAPSIM_FONT_BASIC_BLACK_{PATH,FACE}` override and the simulator-wide `MAPSIM_CLIENT_TEXT_FONT_{PATH,FACE}` override, accepts either override as a direct font file or as a directory to probe, auto-scans nearby runtime `Fonts` and `Content` trees for Maple-like `Dotum` or `Gulim` font files, and also checks the local Windows `Fonts` directory for the same private candidates before falling back to installed families. Remaining work is now narrower and explicit: the exact client `IWzFont` asset behind `get_basic_font(..., FONT_BASIC_BLACK)` is still not bundled, extracted, or identified byte-for-byte by default, so glyph shape, kerning, and final alpha output can still differ on machines without a matching private font file even though the simulator now probes more of the likely local sources automatically | The client distinguishes high-level roster browsing from the focused detail view, so even a partial dedicated details owner materially improves the pre-entry feedback loop and gives later art-parity work a real surface to land on | character-detail UI layer, character metadata presentation (`CUICharDetail::Draw`, `get_basic_font`; simulator seams: `HaCreator/MapSimulator/UI/Windows/CharacterDetailWindow.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Partial | Dedicated create-character flow parity | The simulator now exposes dedicated login-map race, job, avatar, and name create owners instead of routing `BtNew` straight into the shared utility prompt, and that owner family is closer to the client than before: the create flow spans the full centered login scene, race selection renders as WZ-backed race cards drawn from the matching `Login.img/CustomizeChar/*` or `Login.img/NewChar*` `charAlert` art and placed in a client-shaped scene layout guided by `CUINewCharRaceSelect::OnCreate`, and avatar or name stages switch to the matching race-family `charSet`, `charName`, and `avatarSel` assets (`000`, `1000`, `2000`, `2001`, `3000`) instead of reusing one generic explorer theme for every family. The avatar owner also now respects the actual WZ row set more closely: it renders the `avatarSel/normal/*` labels instead of the disabled variant, and when a race family exposes a ninth `avatarSel` row the simulator uses that WZ-backed gender-row art instead of always falling back to plain simulator text. The existing pre-field flow state machine still mirrors the packet follow-up path from `CLogin::OnNewCharJobSel`, `CLogin::OnNewCharAvatarSelCanceled`, `CUINewCharNameSelect::OnButtonClicked`, `CUINewCharNameSelect::OnKey`, `CLogin::OnCheckDuplicatedIDResult`, and `CLogin::OnCreateNewCharacterResult`: avatar cancel backs out through the same flow instead of dropping to the roster shell, Enter on the name owner routes through the duplicate-id packet seam, duplicate-id success returns to avatar selection by recording the checked name instead of immediately mutating the roster, duplicate-id failure stays inside the create flow, and create-result success still mutates the authoritative roster only through `CreateNewCharacterResult`. This pass also closes the explicit race-confirm hop that was still missing before: WZ inspection of `Login.img/RaceSelect/confirm` and client inspection of `CUINewCharRaceSelect::OpenConfirmRaceDlg` plus `CConfirmRaceDlg::OnCreate` are now reflected in the active race owner, so picking a race or pressing the race-stage confirm button opens a dedicated `RaceSelect/confirm` modal, the modal uses the client `backgrnd`, `BtOK`, `BtCancel`, and per-race `confirm/race/*` art at the client-backed scene offset and button positions, cancel keeps the flow on race selection, and only accepting that confirm dialog advances into the job owner. The generated and live create paths are narrower than before as well: the simulator now materializes an explicit request profile from the selected race, beginner job id, subjob, gender, and WZ-backed avatar selections, encodes that state through the same opcode-22 packet layout `CLogin::SendNewCharPacket` emits, round-trips it back through a decoder, and then uses that packet-shaped request to drive live injection and generated `CreateNewCharacterResult` synthesis, so selected hair-color rows and the rest of the encoded avatar payload no longer diverge between the simulator's live and generated seams. Remaining work is now narrower and mostly split between pixel fidelity and the alternate send branch: exact client button rectangles for the avatar and name owners are still approximated rather than matched button-for-button from the hidden `OnCreate` constructors, the simulator does not yet mirror the rest of the client key-path behavior around the race confirm modal beyond the explicit click-to-confirm hop, and the char-sale-specific create request branch is still not mirrored through this dedicated simulator flow | Character creation is part of the visible pre-field pipeline, not just a roster mutation side effect. The simulator now has a dedicated pre-field create flow for that pipeline, and this pass moved the flow materially closer to client presentation by splitting the owner family, switching race choice to WZ-backed scene cards, honoring WZ-backed avatar row art including the gender row where present, carrying a packet-shaped request through both the live and generated create seams, and now restoring the client-owned race-confirm dialog hop before job selection | create-character owner family, login packet follow-up path (`CUINewCharRaceSelect`, `CUINewCharJobSelect`, `CUINewCharAvatarSelect`, `CUINewCharNameSelect`, `CConfirmRaceDlg::OnCreate`, `CUINewCharRaceSelect::OpenConfirmRaceDlg`, `CLogin::OnNewCharJobSel`, `CLogin::OnNewCharAvatarSelCanceled`, `CUINewCharNameSelect::OnButtonClicked`, `CUINewCharNameSelect::OnKey`, `CLogin::OnCheckDuplicatedIDResult`, `CLogin::OnCreateNewCharacterResult`, `CLogin::SendNewCharPacket`; simulator seams: `HaCreator/MapSimulator/UI/Windows/LoginCreateCharacterWindow.cs`, `HaCreator/MapSimulator/Managers/LoginCreateCharacterFlowState.cs`, `HaCreator/MapSimulator/Managers/LoginNewCharacterRequestCodec.cs`, `HaCreator/MapSimulator/Character/CharacterLoader.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Implemented | Recommend world helper parity | The simulator now builds the login recommend-world helper from the client `Login.img/WorldSelect/alert` frame and button art, draws the selected world's WZ-backed `Login.img/WorldSelect/world/<id>` badge instead of a placeholder label, wraps per-world helper text in the same 125px message lane the client uses, places the owner off the recovered client `CUIRecommendWorld` login-layout rect (`CreateDlg(302, 152, 200, 228)` against the centered 800x600 login scene), and accepts live loopback `RecommendWorldMessage` inbox traffic by routing socket-fed payload text through the existing packet-configuration seam for packet-authored recommend-world ordering plus per-world message strings | Server recommendation is part of the client's account-entry UX, not just an incidental line of text on the world list. Wiring a dedicated helper into the login world step means recommendation and guided selection now exist as a distinct pre-field surface instead of being implied only through world-list status text | world-recommendation UI layer, world-selection assist flow (`CUIRecommendWorld::OnCreate`, `CUIRecommendWorld::Draw`, `CUIRecommendWorld::OnButtonClicked`; simulator seams: `HaCreator/MapSimulator/UI/Windows/RecommendWorldWindow.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Partial | Connection-notice and login-utility dialog parity | The simulator now exposes dedicated login-map `ConnectionNotice` and `LoginUtilityDialog` modal windows wired to world or channel request progress, blocked character-entry validation, delete confirmation, client `Login.img/Notice` background variants (`backgrnd/0`, `backgrnd/1`, `backgrnd/2`, `Loading`, `LoadingSG`), the `Login.img/Notice/Loading/circle/*` 16-frame spinner animation on the loading notice owner, client canned notice canvases (`Notice/text/*`) rendered through both owners, packet-authored account prompt overrides on the existing `/loginpacket`, loopback inbox, and live official-session bridge seam for `AccountInfoResult`, `SetAccountResult`, `ConfirmEulaResult`, `CheckPinCodeResult`, `UpdatePinCodeResult`, `EnableSpwResult`, `CheckSpwResult`, `CreateNewCharacterResult`, and `DeleteCharacterResult`, plus the client-backed yes/no security prompts for `SelectWorldResult` server codes `14` and `15` via `Notice/text/27` and `Notice/text/26`. WZ inspection of `Login.img/Notice/Loading/BtCancel` and `LoadingSG/BtCancel`, together with IDA for `CConnectionNoticeDlg::OnCreate`, is now also reflected in the active path: the loading connection notice exposes a real cancel button at the client-backed slot instead of behaving like a passive splash only, and that cancel path can now abort pending selector waits or the title-to-world delayed bootstrap transition inside the existing runtime seam. Client inspection of `CLogin::OnSelectWorldResult`, `CLogin::OnUpdatePinCodeResult`, `CLogin::OnEnableSPWResult`, and `CLogin::OnCheckSPWResult` is now also reflected in the active path: packet-authored `SelectWorldResult` failures no longer stop at plain prose for only the website-handoff cases, but map through the same broader `CLoginUtilDlg::Error(...)` family (`Notice/text/3`, `14`, `15`, `16`, `17`, `19`, `20`, `21`, `27`, `33`, `40`) the client uses, while packet-authored PIC and SPW result payloads can collapse into passive canned notices instead of reopening the shared input flow when the client would already be on a result-only notice (`UpdatePinCodeResult` success/failure via `Notice/text/8` or `15`, `EnableSPWResult` result families via `Notice/text/18`, `39`, `40`, `91`, `92`, `93`, and `CheckSPWResult` via `Notice/text/93`). WZ inspection of `UIWindow2.img/UtilDlgEx/notice` and IDA for `CLoginUtilDlg::OnCreate` are now reflected a bit more directly in the shared utility seam as well: input-bearing prompts no longer behave like plain wrapped body text only, because the dialog now keeps a dedicated editable lane alive when canned notice art is present and uses the client-backed notice-side input slot for those prompts instead of dropping the input field whenever the canned notice has no extra prose. Packet-authored account-dialog payloads also preserve more transport context inside the active prompt path instead of behaving like anonymous cached blobs: the login packet configuration seam still routes those packet types through the account-dialog codec, both modal owners keep supplemental body text visible underneath canned notice canvases so decoded payload detail such as result codes, secondary codes, account ids, character ids, gender, grade, account flags, country, club id, purchase experience, chat-block metadata, register date, character count, and client key survive even when the client-backed notice art is shown, and the decoded detail block now also retains the packet source so live official-session traffic can stay distinguishable from command-authored or loopback-authored prompts. Remaining work is narrower: connection notices still approximate the client's richer modal family with the first two owners instead of adding the other pre-field dialog classes, true dedicated packet-backed owners for PIC dialogs, SPW setup and verification, account migration, and website handoff are still not present because those flows continue to run through the shared simulator modal seam, and the live backend path still reaches this row through the simulator's inbox or session bridge rather than a byte-for-byte clone of the client's original authenticated login owner stack | Many account-entry flows are mediated through modal notices and utility prompts, so adding explicit modal owners closes an important parity gap and lets login-shell actions surface through dedicated dialogs instead of footer text alone, even though more account-specific prompts still remain | account-entry dialog layer, modal utility flow (`CConnectionNoticeDlg::Draw`, `CConnectionNoticeDlg::OnCreate`, `CLoginUtilDlg::Draw`, `CLoginUtilDlg::OnCreate`, `CLogin::OnSelectWorldResult`, `CLogin::OnUpdatePinCodeResult`, `CLogin::OnEnableSPWResult`, `CLogin::OnCheckSPWResult`; simulator seams: `HaCreator/MapSimulator/UI/Windows/ConnectionNoticeWindow.cs`, `HaCreator/MapSimulator/UI/Windows/LoginUtilityDialogWindow.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `HaCreator/MapSimulator/Managers/LoginOfficialSessionBridgeManager.cs`) |

## Priority Order

If the goal is visible parity first, the next work in this area should be sequenced like this:

1. Bootstrap pass:
   Add a minimal login-state shell and step model so the simulator can represent pre-field transitions at all.
2. World/channel selection pass:
   Add dedicated world-select, channel-select, and channel-shift owners before treating recommend-world or connection notices as the whole pre-field selection surface.
3. Character-entry pass:
   Add roster, avatar-preview, and character-detail surfaces before attempting deeper account prompts.
4. Alternate entry-result pass:
   Add the packet-owned `OnSelectCharacterByVACResult` branch before treating the normal select-character result as the whole login handoff surface.
5. Create-character pass:
   Add the dedicated race, job, avatar, and name-selection owners before treating create-character parity as complete.
6. Utility-dialog pass:
   Add connection notices, recommend-world flows, and login utility dialogs once the main entry shell exists.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
