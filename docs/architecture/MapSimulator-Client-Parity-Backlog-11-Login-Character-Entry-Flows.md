# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Managers/GameStateManager.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`

### Client references checked

- `CLogin::Init` at `0x5d8010`
- `CLogin::Update` at `0x5dee90`
- `CLogin::OnPacket` at `0x5df940`
- `CLogin::OnCheckPasswordResult` at `0x5dc600`
- `CLogin::OnGuestIDLoginResult` at `0x5dd1a0`
- `CLogin::OnAccountInfoResult` at `0x5dd600`
- `CUICharSelect::OnCreate` at `0x5e8550`
- `CUICharSelect::OnButtonClicked` at `0x5e0350`
- `CUIAvatar::OnCreate` at `0x5ebe10`
- `CUIAvatar::OnMouseButton` at `0x5ed130`
- `CUICharDetail::Draw` at `0x5e8860`
- `CConnectionNoticeDlg::Draw` at `0x5d6a30`
- `CLoginUtilDlg::Draw` at `0x5eee70`
- `CUIWorldSelect::OnCreate` at `0x60b660`
- `CUIWorldSelect::OnButtonClicked` at `0x6025e0`
- `CUIChannelSelect::OnCreate` at `0x606a10`
- `CUIChannelSelect::Draw` at `0x6056d0`
- `CUIChannelSelect::OnButtonClicked` at `0x6035f0`
- `CUIChannelShift::OnCreate` at `0x96c160`
- `CUIChannelShift::Draw` at `0x96ccb0`
- `CLogin::OnCheckDuplicatedIDResult` at `0x5d5790`
- `CLogin::OnCreateNewCharacterResult` at `0x5dab90`
- `CUINewCharNameSelect::OnCreate` at `0x5f3330`
- `CUINewCharNameSelect::OnButtonClicked` at `0x5f4fe0`
- `CUINewCharNameSelect::OnKey` at `0x5f5090`
- `CUINewCharAvatarSelect::OnCreate` at `0x5f3340`
- `CUINewCharAvatarSelect::OnButtonClicked` at `0x5f36b0`
- `CUINewCharAvatarSelect::Draw` at `0x5f51a0`
- `CUINewCharJobSelect::OnButtonClicked` at `0x5f35d0`
- `CUINewCharJobSelect::Draw` at `0x5f38e0`
- `CUINewCharJobSelect::OnCreate` at `0x5fa760`
- `CUINewCharRaceSelect::Draw` at `0x5f37d0`
- `CUINewCharRaceSelect::OnCreate` at `0x5f81c0`
- `CUINewCharRaceSelect::OnButtonClicked` at `0x5f9470`
- `CUIRecommendWorld::Draw` at `0x603980`
- `CUIRecommendWorld::OnCreate` at `0x6073a0`
- `CUIRecommendWorld::OnButtonClicked` at `0x607910`
- `CLogin::GetBalloonInfo` at `0x607370`
- `CUIWorldSelect::MakeWSBalloon` at `0x60a0f0`
- `CUICharSelect::MakeBalloon` at `0x5e3c10`
- `CUIAvatar::MakeBalloon` at `0x5e58c0`
- `CLogin::OnWorldInformation` at `0x5da7f0`
- `CLogin::OnRecommendWorldMessage` at `0x5d7280`
- `CLogin::OnLatestConnectedWorld` at `0x5d2200`
- `CLogin::OnCheckUserLimitResult` at `0x5d2250`
- `CLogin::OnViewAllCharResult` at `0x5de120`
- `CLogin::OnSelectCharacterResult` at `0x5dea80`
- `CLogin::OnDeleteCharacterResult` at `0x5d9e10`
- `CLogin::OnExtraCharInfoResult` at `0x5d25a0`
- `CLogin::OnSelectCharacterByVACResult` at `0x5de670`
- `CField::OnCharacterSale` at `0x52a3e0`
- `CStage::OnPacket` at `0x71b0b0`
- `CStage::OnSetField` at `0x71b150`
- `CStage::OnSetITC` at `0x71c000`
- `CStage::OnSetCashShop` at `0x71bfa0`
- `CMapLoadable::OnPacket` at `0x61fd80`
- `CMapLoadable::OnSetBackEffect` at `0x620160`
- `CMapLoadable::OnSetMapObjectVisible` at `0x61fe40`
- `CMapLoadable::OnClearBackEffect` at `0x620150`

Notes:
IDA confirms there is a coherent pre-field client surface that is not covered by the first ten backlog docs. The login bootstrap, world/channel selection owners, character-selection roster, avatar preview, character-detail panel, recommend-world helper, connection or utility dialogs, and the separate create-character race/job/avatar/name owners all have dedicated client classes and do not fit cleanly under in-map HUD work or the broader social-window bucket.

## Cross-Document Scope Notes

- `CStage::OnSetCashShop` and `CStage::OnSetITC` stay indexed here only as pre-field stage-handoff references. The actual Cash Shop / ITC service-stage, child-window, and wishlist parity work belongs to backlog 8.
- Packet-owned stage/map-load dispatch ownership for `CField::OnPacket`, `CStage::OnPacket`, `CStage::OnSetField`, and `CMapLoadable::OnPacket` is consolidated in backlog 7. This document only keeps the login-shell selectors, roster, and entitlement consequences that consume those transitions before field play resumes.
- `CField::OnCharacterSale` remains relevant here because it is the stage-side bridge back into login-side character-slot entitlement flow, not the broader Cash Shop / ITC owner family itself.
- Login-scene balloons stay here, not in the social/chat balloon backlog. `CLogin::BALLOON`, `CUIWorldSelect::MakeWSBalloon`, `CUICharSelect::MakeBalloon`, and `CUIAvatar::MakeBalloon` are pre-field selector and avatar-entry presentation owners; field chat, MiniRoom, employee, ADBoard, and mob balloon presentation remains in backlog 10 or the relevant field/combat documents.

## Client Function Index By Backlog Area

The list below maps this backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Login and character-entry parity

- `CLogin::Init` at `0x5d8010`
- `CLogin::Update` at `0x5dee90`
- `CLogin::OnPacket` at `0x5df940`
- `CLogin::OnCheckPasswordResult` at `0x5dc600`
- `CLogin::OnGuestIDLoginResult` at `0x5dd1a0`
- `CLogin::OnAccountInfoResult` at `0x5dd600`
- `CUICharSelect::OnCreate` at `0x5e8550`
- `CUICharSelect::OnButtonClicked` at `0x5e0350`
- `CUIAvatar::OnCreate` at `0x5ebe10`
- `CUIAvatar::OnMouseButton` at `0x5ed130`
- `CUICharDetail::Draw` at `0x5e8860`
- `CConnectionNoticeDlg::Draw` at `0x5d6a30`
- `CLoginUtilDlg::Draw` at `0x5eee70`
- `CUIWorldSelect::OnCreate` at `0x60b660`
- `CUIWorldSelect::OnButtonClicked` at `0x6025e0`
- `CUIChannelSelect::OnCreate` at `0x606a10`
- `CUIChannelSelect::Draw` at `0x6056d0`
- `CUIChannelSelect::OnButtonClicked` at `0x6035f0`
- `CUIChannelShift::OnCreate` at `0x96c160`
- `CUIChannelShift::Draw` at `0x96ccb0`
- `CUIRecommendWorld::Draw` at `0x603980`
- `CUIRecommendWorld::OnCreate` at `0x6073a0`
- `CUIRecommendWorld::OnButtonClicked` at `0x607910`

Notes:
`CLogin::Init`, `CLogin::Update`, and `CLogin::OnPacket` confirm a dedicated login-state machine and packet router rather than a generic splash-to-map handoff. The same router now has explicit bootstrap result coverage in this seam index too: `CLogin::OnCheckPasswordResult`, `CLogin::OnGuestIDLoginResult`, and `CLogin::OnAccountInfoResult` sit on the pre-field auth/account chain before the world-selector family takes over, so they should stay absorbed into the main login row rather than being misfiled as generic utility prompts. `CUIWorldSelect`, `CUIChannelSelect`, and `CUIChannelShift` show that pre-field server/world selection is also its own owner family rather than a generic extension of utility-window browsing once the simulator is already in a field: `CUIWorldSelect::OnCreate` builds a dedicated world-info layer and repopulates world items, `CUIChannelSelect::OnCreate` resets world/channel population state and creates its own button/layout owner from StringPool/WZ assets, and `CUIChannelShift` keeps a separate transition surface alive while the channel change is in flight. `CUICharSelect`, `CUIAvatar`, and `CUICharDetail` confirm that roster browsing, avatar preview, and character metadata are separate entry-flow surfaces with their own create, draw, and click handling. `CConnectionNoticeDlg` and `CLoginUtilDlg` confirm that account-entry prompts such as connection notices and utility dialogs are distinct UI owners, while `CUIRecommendWorld` shows that server recommendation is not just data on the world list but a dedicated helper surface.

### 2. Additional client-owned create-character wrappers discovered by targeted IDA scan

- `CLogin::OnCheckDuplicatedIDResult` at `0x5d5790`
- `CLogin::OnCreateNewCharacterResult` at `0x5dab90`
- `CUINewCharNameSelect::OnCreate` at `0x5f3330`
- `CUINewCharNameSelect::OnButtonClicked` at `0x5f4fe0`
- `CUINewCharNameSelect::OnKey` at `0x5f5090`
- `CUINewCharAvatarSelect::OnCreate` at `0x5f3340`
- `CUINewCharAvatarSelect::OnButtonClicked` at `0x5f36b0`
- `CUINewCharAvatarSelect::Draw` at `0x5f51a0`
- `CUINewCharJobSelect::OnButtonClicked` at `0x5f35d0`
- `CUINewCharJobSelect::Draw` at `0x5f38e0`
- `CUINewCharJobSelect::OnCreate` at `0x5fa760`
- `CUINewCharRaceSelect::Draw` at `0x5f37d0`
- `CUINewCharRaceSelect::OnCreate` at `0x5f81c0`
- `CUINewCharRaceSelect::OnButtonClicked` at `0x5f9470`

Notes:
Targeted IDA lookup shows the client does not treat new-character creation as a small extension of `CUICharSelect`. It has a separate owner family for race selection, job selection, avatar selection, and typed name validation, with `CLogin` packet handlers feeding duplicate-name and create-result state back into that flow.

### 3. Additional pre-field selector packet handlers discovered by targeted `CLogin::OnPacket` scan

- `CLogin::OnWorldInformation` at `0x5da7f0`
- `CLogin::OnRecommendWorldMessage` at `0x5d7280`
- `CLogin::OnLatestConnectedWorld` at `0x5d2200`
- `CLogin::OnCheckUserLimitResult` at `0x5d2250`

Notes:
The current login doc names the pre-field selector windows, but this targeted IDA pass shows the packet-fed selector ownership is also a first-class login seam rather than only window-local state. `CLogin::OnWorldInformation` populates the world/channel list payloads that `CUIWorldSelect` and `CUIChannelSelect` render, `CLogin::OnRecommendWorldMessage` feeds the helper/recommendation surface, `CLogin::OnLatestConnectedWorld` restores the client-owned recent-world focus, and `CLogin::OnCheckUserLimitResult` gates the handoff from world selection into channel selection instead of acting like a generic error popup. Those handlers are already important enough in the simulator codebase to have their own `LoginRuntimeManager`, `LoginWorldInfoPacketCodec`, and `LoginSelectorPacketPayloadCodec` seams, so they should be tracked explicitly in the login backlog rather than being left implicit or only mentioned in the broader progression/utility doc.

### 4. Additional account-dialog and security-result handlers confirmed by targeted IDA lookup

- `CLogin::OnSetAccountResult` at `0x5d5e80`
- `CLogin::OnConfirmEULAResult` at `0x5d4d00`
- `CLogin::OnCheckPinCodeResult` at `0x5db000`
- `CLogin::OnSelectWorldResult` at `0x5dda00`
- `CLogin::OnUpdatePinCodeResult` at `0x5d2420`
- `CLogin::OnEnableSPWResult` at `0x5d2290`
- `CLogin::OnCheckSPWResult` at `0x5d23f0`

Notes:
The earlier login rows already described this modal/account surface in prose, but the latest IDA lookup pins the concrete handlers and keeps them out of the generic utility bucket. `CLogin::OnSetAccountResult`, `CLogin::OnConfirmEULAResult`, and `CLogin::OnCheckPinCodeResult` sit on the bootstrap chain that decides whether account setup can advance without reopening the wrong prompt; `CLogin::OnSelectWorldResult` owns the broader world-entry error and website-handoff family after selector choice; and `CLogin::OnUpdatePinCodeResult`, `CLogin::OnEnableSPWResult`, and `CLogin::OnCheckSPWResult` prove that PIC/SPW completion notices are first-class login-owned result handlers rather than anonymous canned dialogs. These handlers already map onto the simulator's `LoginRuntimeManager`, account-dialog codec path, and login utility windows, so keeping them indexed here makes future parity work start from the actual client owners instead of scattered row prose.

### 5. Additional roster-result handlers discovered by targeted `CLogin::OnPacket` scan

- `CLogin::OnViewAllCharResult` at `0x5de120`
- `CLogin::OnSelectCharacterResult` at `0x5dea80`
- `CLogin::OnDeleteCharacterResult` at `0x5d9e10`

Notes:
The targeted `CLogin::OnPacket` pass exposed another login-owned result family that was still only implied by the roster UI row. `OnViewAllCharResult` is not just more selector data; the decompile shows it owning the multi-server `ViewAllChar` aggregation path, the success-vs-failure notice branches, the `GotoWorldSelect` fallback when no characters arrive, and the separate VAC dialog handoff once the related-server count drains. `OnSelectCharacterResult` is likewise broader than a generic roster click result because it owns its own error table, title-return cases, website-handoff prompts, and the direct `CWvsContext::IssueConnect` success path for both normal and alternate result families. `OnDeleteCharacterResult` is another distinct owner rather than only a button callback, because it owns the roster mutation itself, the direct-notice StringPool text path (`0xFD4`), the mapped `CLoginUtilDlg::Error` table, and the follow-up avatar/detail refresh after a successful deletion. Those result handlers are now concrete enough to track as their own packet-owned seam inside the login backlog instead of leaving them spread across avatar or utility-window documents.

### 6. Additional extra-character entitlement handler discovered by targeted `CLogin::OnPacket` scan

- `CLogin::OnExtraCharInfoResult` at `0x5d25a0`

Notes:
This handler is still absent from the login backlog even though it owns a distinct roster-side entitlement seam. The decompile shows `CLogin::OnExtraCharInfoResult` clearing `m_bCanHaveExtraChar`, decoding an account id plus a one-byte result flag, and only re-enabling the extra-character entitlement when the packet account matches the active `CWvsContext` account and the result flag is `0`. That is not just another selector refresh; it is the client-owned gate for whether the roster should expose a `buyCharacter` slot at all, and the simulator already has matching seams in `LoginExtraCharInfoResultCodec`, `LoginRuntimeManager`, `_loginCanHaveExtraCharacter`, and the roster `BuyCharacterCount` path.

### 7. Additional field-side character-sale handler discovered by follow-up `CField::OnPacket` decompile

- `CField::OnCharacterSale` at `0x52a3e0`

Notes:
The latest `CField::OnPacket` re-check exposed another entitlement-adjacent owner that the backlog was only implying through the login-side extra-slot rows. Packet family `413` to `416` is forwarded from `CField::OnPacket` into `CField::OnCharacterSale`, and the handler itself is not a no-op shim back into login state: it checks a dedicated child-owner pointer at `this+0x244` and then dispatches through that owner's virtual slot `0x3C`. That means the client keeps a separate field/stage-owned character-sale bridge between the broader Cash Shop or ITC stage family and the login-side `CLogin::OnExtraCharInfoResult` entitlement gate, instead of letting `buyCharacter` exposure and purchase-result handling live only on the ordinary roster or selector packet path. Keeping this seam explicit in the login backlog avoids losing the stage-to-roster bridge under the already separate cash-service and extra-character rows.
The follow-up function scan resolves that child owner as `CUICharacterSaleDlg`: `CUICharacterSaleDlg::OnPacket` at `0x778c50` dispatches packet `413` to `OnCheckDuplicatedIDResult` and packet `414` to `OnCreateNewCharacterResult`, so this row should remain tied to the field/stage sale dialog rather than only the login-map new-character wrappers.

### 8. Additional alternate-entry packet handler discovered by targeted `CLogin::OnPacket` scan

- `CLogin::OnSelectCharacterByVACResult` at `0x5de670`

Notes:
`CLogin::OnPacket` routes packet type `9` into `CLogin::OnSelectCharacterByVACResult`, which is a distinct alternate character-entry path rather than a synonym for the normal `OnSelectCharacterResult` flow. IDA shows it owns its own result-code table, title-return failures, website-handoff prompts, and direct `CWvsContext::IssueConnect` success path, so it should be tracked explicitly instead of being assumed to fall under the ordinary roster-select result handler.

### 9. Login-scene balloon presentation owners discovered by broad IDA function scan

- `CLogin::GetBalloonInfo` at `0x607370`
- `CUIWorldSelect::MakeWSBalloon` at `0x60a0f0`
- `CUICharSelect::MakeBalloon` at `0x5e3c10`
- `CUIAvatar::MakeBalloon` at `0x5e58c0`

Notes:
The broad IDA function scan found a pre-field balloon owner family that was missing from this login document and should not be merged into the field/social balloon rows. `CLogin::GetBalloonInfo` exposes a login-owned `BALLOON` array, `CUIWorldSelect::MakeWSBalloon` constructs world-select message layers, and both `CUICharSelect::MakeBalloon` and `CUIAvatar::MakeBalloon` build sized balloon layers for the character-select and avatar-entry surfaces. That makes login balloon parity part of the account-entry presentation pipeline: selector notices, roster or avatar helper callouts, layer creation, arrow sizing, text wrapping, fade/timing, and login-scene placement should be tracked beside the login windows that consume them rather than under in-field chat or MiniRoom balloon behavior.

## Current State Summary

This area is still mostly absent from the simulator today.
The project currently includes:

- Cash-shop-map and wider game-state gating flags in `MapSimulator.cs` and `GameStateManager.cs`.
- Status-bar utility buttons that already expose some neighboring service entry points once the simulator is in-map.
- Local avatar loading and runtime state that now feed a simulator-side login roster window with selectable character builds and roster-driven field entry on login maps, even though avatar-preview and detail-specific surfaces are still separate gaps.
- A dedicated login bootstrap runtime in `HaCreator/MapSimulator/Managers/LoginRuntimeManager.cs` that now owns pre-field steps, delayed login-step changes, and login-packet dispatch on login maps.

The gap is not a small missing window shell.
The gap is that the simulator does not currently model the client's entire account-entry and character bootstrap pipeline before gameplay begins.

## Remaining Parity Backlog

### 1. Login and character-entry parity

This area is not represented by the existing ten documents even though IDA shows a large, dedicated client surface here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Login bootstrap and packet-state parity | The simulator now wires the dedicated login bootstrap runtime into a real login-title owner built from `Login.img/Title` (`backFrame`, `MSTitle`, `MapleDolls`, `signboard`, `ID`, `PW`, the animated `effect/*` overlay set, the `check/0` and `check/1` Save-ID indicator canvases, plus the title buttons), drives CheckPassword or GuestId bootstrap requests from that UI through the existing login-packet inbox instead of chat commands alone, surfaces live inbox status on the title scene, reuses the runtime's delayed step timing to show a visible connection-notice progress pass while Title advances into WorldSelect, and keeps the packet-routed account follow-up chain alive inside the existing `Login.img/Notice` utility owner by accepting EULA, capturing typed PIC and SPW setup or verification, recording migration decisions, and feeding the accepted path back into the inbox until the flow can resume toward `CheckPasswordResult` instead of stopping at one-shot modal text. The row also now decodes and stores packet-authored auth/account payloads for `CheckPasswordResult`, `GuestIDLoginResult`, `AccountInfoResult`, `SetAccountResult`, `ConfirmEulaResult`, `CheckPinCodeResult`, `UpdatePinCodeResult`, `EnableSpwResult`, `CheckSpwResult`, `CreateNewCharacterResult`, and `DeleteCharacterResult`, so the shared utility owner can display packet-carried text, result codes, account ids, character ids, and retained packet sources instead of only simulator-authored boilerplate. Client inspection of `CLogin::OnCheckPasswordResult`, `CLogin::OnGuestIDLoginResult`, `CLogin::OnAccountInfoResult`, `CLogin::OnSetAccountResult`, `CLogin::OnConfirmEULAResult`, `CLogin::OnCheckPinCodeResult`, `CLogin::OnUpdatePinCodeResult`, `CLogin::OnEnableSPWResult`, and `CLogin::OnCheckSPWResult` is now reflected in the active packet path as well: packet-authored success payloads for `SetAccountResult`, `ConfirmEulaResult`, and `CheckPinCodeResult` no longer reopen the generic migration, EULA, or PIC modal, but suppress that success dialog, enqueue the next bootstrap packet through the inbox, and keep the login chain advancing the way the client does; packet-authored `CheckPinCodeResult` prompt selection is now result-code aware so code `1` opens PIC creation, codes `2` and `4` open PIC verification, and code `7` forces the runtime back to Title while still surfacing the client-backed failure notice; packet-authored `UpdatePinCodeResult`, `EnableSpwResult`, and `CheckSpwResult` now keep the client-backed notice families (`Notice/text/8`, `15`, `18`, `39`, `40`, `91`, `92`, `93`) inside the same utility seam instead of falling back to generic prose alone. WZ inspection of `Login.img/Notice/backgrnd/0`, `BtYes1`, `BtNo1`, and `BtNexon` is now reflected more directly in the active notice path too: packet-backed login warnings and website-handoff prompts now stay on the login notice frame instead of falling back to the generic `UIWindow2.img/UtilDlgEx/notice` shell, the security-site `CheckPasswordResult`, `GuestIDLoginResult`, `AccountInfoResult`, and `SelectWorldResult` code `14` / `15` prompts keep the alternate question-button owner art, and the follow-up Nexon handoff step now stays inside that same notice-owner seam with the dedicated `BtNexon` button centered against the `Login.img/Notice` background rather than dropping back to the generic utility frame. The local no-socket bootstrap path is closer to those handlers now too: when the simulator reaches the no-PIC branch after EULA acceptance it no longer jumps straight into the old generic `UpdatePinCodeResult` prompt, but feeds a packet-authored `CheckPinCodeResult` code `1` through the inbox so PIC creation opens on the same result-code seam the client uses; after local PIC creation it now round-trips a packet-authored `UpdatePinCodeResult` success payload back through the inbox and keeps the client `Notice/text/8` notice visible until dismissal before advancing toward SPW; and both the `BtLater` and successful `SetSpw` branches now synthesize packet-authored `EnableSpwResult` success payloads so the shared notice seam shows the client `Notice/text/40` or `39` family before the bootstrap finally resumes toward `CheckPasswordResult` instead of short-circuiting those notices. Remaining work is narrower and still upstream-owner specific: intake is still a simulator loopback packet feed rather than the client's authenticated socket protocol, most decoded follow-up payloads still come from loopback, command, or locally synthesized packet-shaped data rather than a live authoritative backend session, and dedicated owners beyond the current title plus notice pair are still approximated through the shared utility or notice seam instead of implemented as separate packet-backed client classes | Without the login-state owner, all account-facing transitions are effectively skipped, which hides an entire category of client behavior before the character ever enters a map. The new runtime establishes that owner so later roster, avatar, and dialog work can attach to a real pre-field pipeline instead of jumping straight into a field | account-entry runtime, state machine, packet-routing layer (`CLogin::Init`, `CLogin::Update`, `CLogin::OnPacket`, `CLogin::OnCheckPasswordResult`, `CLogin::OnGuestIDLoginResult`, `CLogin::OnAccountInfoResult`, `CLogin::OnSetAccountResult`, `CLogin::OnConfirmEULAResult`, `CLogin::OnCheckPinCodeResult`, `CLogin::OnUpdatePinCodeResult`, `CLogin::OnEnableSPWResult`, `CLogin::OnCheckSPWResult`; simulator seam: `HaCreator/MapSimulator/Managers/LoginRuntimeManager.cs`) |
| Partial | World, channel, and channel-shift selector parity | The dedicated pre-field selector owner family is no longer absent from the simulator login runtime. The simulator already routes the login flow through dedicated `WorldSelect`, `ChannelSelect`, and timed `ChannelShift` owners, preserves the current world/channel choice in the login scene, simulates per-world channel occupancy plus busy/full gating, and resolves world/channel picks through delayed `CheckUserLimitResult` / `SelectWorldResult` style responses instead of committing immediately. WZ inspection of `Login.img/WorldSelect` is now also reflected more directly in the active owner shell: the selector loader sources the world frame from `scroll/0/{0,0_bak}`, world badges from `world/*`, world buttons from `BtWorld/*`, the client empty-slot filler from `BtWorld/e`, the world toggle buttons from `BtViewChoice` / `BtViewAll`, the channel shells from `chBackgrn` / `chBackgrn_Bak`, the channel enter button from `BtGoworld`, the channel rows from `channel/*/{normal,disabled}`, and the focused-world flourish layers from `BtWorld/*/mouseOver`, `BtWorld/*/keyFocused`, and `BtViewAll/{mouseOver,keyFocused}` instead of keeping the active owner on placeholder chrome or static-only selector focus. Client inspection of `CUIWorldSelect::OnCreate`, `CUIWorldSelect::DrawWorldItems`, `CUIChannelSelect::OnCreate`, `CUIChannelSelect::GetRect`, `CUIChannelSelect::Draw`, and `CUIChannelShift::GetRectFromIdx` is also reflected more tightly in the active layout now: the world selector owner keeps the client's `(-10,-10)` owner offset, the world grid now follows the client's row-major `96 x 26` slot placement instead of the earlier transposed ordering, the selector no longer collapses the remaining world rows away because it fills the client 36-slot grid with disabled `BtWorld/e` placeholders, the focused world and `BtViewAll` branch now render WZ-authored animated hover/key-focus flourishes instead of a static focus outline only, and the channel selector now places the selected-world badge back at the client draw slot (`34,10`) instead of leaving that badge on a looser simulator offset. The active owner seam now also owns a real keyboard navigation path instead of remaining mouse-only: the world selector consumes `Tab` / arrow / `Enter` navigation across the 36-slot world grid and the `BtViewAll` branch, and the channel selector now mirrors the client-owned row browsing more closely by accepting arrow-key movement across the channel grid plus `Enter` / `Escape` handling inside the selector owner rather than leaving those paths absent. The selector seam also now accepts packet-authored `WorldInformation`, `RecommendWorldMessage`, `LatestConnectedWorld`, and `CheckUserLimitResult` payloads through the login packet/runtime path, which matches the client-owned feeder handlers confirmed in this IDA pass (`CLogin::OnWorldInformation`, `CLogin::OnRecommendWorldMessage`, `CLogin::OnLatestConnectedWorld`, `CLogin::OnCheckUserLimitResult`) rather than leaving the selector windows to run on simulator-only metadata, and the packet-authored `WorldState`, `blocksCharacterCreation`, and authored world name now stay attached to selector metadata instead of being dropped after decode so the active world markers can distinguish busy or full worlds and creation-locked worlds from the real login feed rather than from occupancy heuristics alone. The remaining gap is narrower: the simulator still does not source or decrypt the true authenticated upstream login feed on its own, still approximates the selector owner's final packet/session choreography through loopback/replay metadata, and still does not yet mirror the client's dedicated animated world-state icon layer stack from `CUIWorldSelect::DrawWorldItems` or the non-entry service side branches exactly. | These selectors sit on the main pre-field path between account bootstrap and character entry. Keeping them explicit in the login backlog matters because the client treats world selection, channel selection, channel-shift presentation, and the packet handlers that feed them as part of one pre-field owner family rather than as generic utility windows. | pre-field server-selection owner family (`CUIWorldSelect::OnCreate`, `CUIWorldSelect::OnButtonClicked`, `CUIWorldSelect::DrawWorldItems`, `CUIChannelSelect::OnCreate`, `CUIChannelSelect::GetRect`, `CUIChannelSelect::Draw`, `CUIChannelSelect::OnButtonClicked`, `CUIChannelShift::OnCreate`, `CUIChannelShift::Draw`, `CUIChannelShift::GetRectFromIdx`, `CLogin::OnWorldInformation`, `CLogin::OnRecommendWorldMessage`, `CLogin::OnLatestConnectedWorld`, `CLogin::OnCheckUserLimitResult`; simulator seams: `HaCreator/MapSimulator/UI/Windows/ChannelSelectionWindows.cs`, `HaCreator/MapSimulator/UI/Windows/RecommendWorldWindow.cs`, `HaCreator/MapSimulator/Managers/LoginRuntimeManager.cs`, `HaCreator/MapSimulator/Managers/LoginWorldInfoPacketCodec.cs`, `HaCreator/MapSimulator/Managers/LoginSelectorPacketPayloadCodec.cs`, `HaCreator/MapSimulator/Interaction/LoginWorldSelectorMetadata.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Partial | Packet-owned roster aggregation and selector-result parity | WZ inspection confirms the shared notice assets this row depends on are present in this data set: `Login.img/Notice/text` contains the client-backed selector and deletion notice canvases used by these branches (`11`, `14`, `15`, `16`, `17`, `18`, `19`, `20`, `21`, `25`, `26`, `27`, `28`, `29`, `30`, `31`, `33`, `34`, `39`, `40`, `50`, `54`, `56`, `62`, `93`), and `Login.img/Notice/backgrnd/*` keeps the shared notice owner art that still hosts the packet-backed fallback prompts in the simulator seam. The login runtime now owns the broader selector-result family instead of leaving it implied by the roster UI alone. `ViewAllCharResult` keeps packet-authored roster chunks, delayed related-server aggregation, success-vs-failure notices, and the fallback `GotoWorldSelect` or VAC-dialog handoff inside the active path; the local no-session path no longer fakes expanded-roster entry with one generic result, but synthesizes the client-shaped header plus per-world chunk plus completion sequence from the authenticated account-backed world rosters (reusing the active `SelectWorldResult` world when present), and the active handler now waits for real aggregation completion before entering `ViewAllCharacters`, returns to WorldSelect immediately on zero-count header/completion branches, forces the title-vs-world fallback split for the client VAC error-family result codes instead of treating every packet as a generic step advance, and now feeds the client-backed `StringPool` notice family through the active modal seam more directly too: the no-character world-select fallback branch uses the client `0xFBD` notice text, result `2` uses the client `0xFBE` failure text, and results `3`, `6`, and `7` keep the packet-authored warning text when present before falling back to the client `0xFBF` warning text instead of showing simulator-authored prose only. The shared fallback-owner art is tighter in the same seam now too: `LoginUtilityDialogWindow` can swap between the generic `UIWindow2.img/UtilDlgEx/notice` frame and the client `Login.img/Notice/backgrnd/{0,1,2}` owner family, so `ViewAllCharResult` world-select fallback and error prompts plus `SelectCharacterResult` failure and website-handoff prompts now render through the login notice owner art instead of always falling back to the generic utility frame, while the `DeleteCharacterResult` child-modal error family (`6`, `9`, `20`, `22`, `24`, `29`, `35`, `36`) now routes through the alternate notice-owner variant instead of sharing the plain utility frame with unrelated bootstrap prompts. Client inspection of `CLogin::OnSelectCharacterResult` at `0x5dea80` is now reflected more exactly in the active selector path too: `SelectCharacterResult` keeps its own client-backed error table, title-return branches, result-`14` / `15` website-handoff prompts through the shared security yes-no dialog, and the direct `IssueConnect` success payload for result `0`, result `23`, and result `12` with response `11` or `13` instead of collapsing into a generic roster click result. Client inspection of `CLogin::OnDeleteCharacterResult` at `0x5d9e10` is now reflected more directly in the packet-owned mutation seam as well: `DeleteCharacterResult` applies the mapped `CLoginUtilDlg::Error` notice family (`11`, `15`, `18`, `19`, `34`, `39`, `50`, `54`, `93`), keeps the direct `StringPool` notice text branch for id `0xFD4`, removes the character through the shared authenticated backend-session cache before the live roster redraw, and then forces the follow-up character-select refresh so avatar-preview and detail selection stay on the packet-authored mutation path instead of only local UI state. The packet-owned account snapshot is now tighter in the same seam too: after roster persistence, `AccountInfoResult` keeps its stored `CharacterCount` synchronized to the authoritative account-backed roster or aggregated `ViewAllChar` packet state, so create/delete and cross-world aggregation no longer leave the cached account detail block advertising a stale count after packet-owned roster mutation. Remaining work is narrower and now mostly upstream-session plus dedicated-owner specific: the simulator still does not drive the multi-server `ViewAllChar` choreography through a true authenticated remote backend session, and the final packet-specific child-modal ownership stack from `CLogin::OnViewAllCharResult`, `CLogin::OnSelectCharacterResult`, and `CLogin::OnDeleteCharacterResult` is still approximated by shared simulator windows plus frame-routing rather than separate packet-backed client dialog classes with their own lifetime or parent/child stacks. | This keeps packet-owned roster/result behavior anchored to the actual `CLogin` handlers that mutate roster state and enter gameplay. Without an explicit row here, the login backlog overstates parity by counting only the selector windows while the actual result-routing, deletion ownership, and multi-server roster aggregation logic remain half-hidden inside other documents. | packet-owned login roster/result layer (`CLogin::OnViewAllCharResult`, `CLogin::OnSelectCharacterResult`, `CLogin::OnDeleteCharacterResult`; simulator seams: `HaCreator/MapSimulator/Managers/LoginViewAllCharResultCodec.cs`, `HaCreator/MapSimulator/Managers/LoginSelectCharacterResultCodec.cs`, `HaCreator/MapSimulator/Managers/LoginCreateCharacterResultCodecs.cs`, `HaCreator/MapSimulator/Managers/LoginRuntimeManager.cs`, `HaCreator/MapSimulator/Managers/LoginBackendSessionManager.cs`, `HaCreator/MapSimulator/Managers/LoginCharacterRosterManager.cs`, `HaCreator/MapSimulator/UI/Windows/CharacterSelectWindow.cs`, `HaCreator/MapSimulator/UI/Windows/LoginUtilityDialogWindow.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Partial | Extra-character slot entitlement parity | The simulator now keeps the extra-character entitlement on the same authenticated-account gate the client uses and pushes more of that ownership into the backend session seam instead of the local roster shell. Client inspection of `CLogin::OnExtraCharInfoResult` (`0x5d25a0`) shows the handler clearing `m_bCanHaveExtraChar`, decoding an account id plus one-byte result flag, and only re-enabling the entitlement when that packet account matches the active `CWvsContext` account and the result flag is `0`; the active simulator path now mirrors that by caching the decoded `ExtraCharInfoResult` in `LoginBackendSessionManager`, re-evaluating it when the login bootstrap later resolves an authenticated account id, and normalizing packet-authored `SelectWorldResult` / synchronized roster `BuyCharacterCount` values back through that same gate before the roster reaches character-select or the persisted account store. The row now also folds in the stage-side bridge that was still only indexed above it: the same entitlement seam now explicitly tracks `CField::OnCharacterSale` (`0x52a3e0`) as the packet-owned field/stage handoff between Cash Shop or ITC-side character-sale flow and the login-side extra-slot gate, instead of leaving purchase-result ownership implied outside the final backlog table. The simulator also now keeps spent-slot replay tighter than before: when local create-character flow consumes the extra slot, the backend session explicitly invalidates the cached entitlement and rewrites the cached extra-char result to a denied state so later account-id rebinding or packet replay does not resurrect a stale `buyCharacter` card, and when a stored or packet-authored account id becomes available the account-backed fallback now promotes the same roster snapshot and `BuyCharacterCount` across the persisted worlds owned by that account instead of only the currently active world key. Remaining work is still backend-owned and narrower: the simulator still reaches this row through loopback, replayed, or persisted session state instead of a true authenticated login socket flow, the broader server-side account-billing lifecycle behind buying, granting, or revoking extra slots is still approximated locally once the simulator spends the slot rather than refreshed from a live authoritative backend beyond the single account-id-plus-result gate that `CLogin::OnExtraCharInfoResult` applies, and the field-side `CField::OnCharacterSale` bridge is still documented ownership rather than a byte-for-byte end-to-end service-stage round trip. | If this seam stays implicit inside the broader roster row, the backlog overstates login parity by treating the extra `buyCharacter` slot as simple roster decoration rather than a packet-owned account entitlement. Folding the field-side `CField::OnCharacterSale` bridge into the same row keeps the entitlement path anchored to both of the client owners that actually decide when the roster can expose or consume an extra slot. | login roster-entitlement layer (`CLogin::OnExtraCharInfoResult`, `CField::OnCharacterSale`; simulator seams: `HaCreator/MapSimulator/Managers/LoginExtraCharInfoResultCodec.cs`, `HaCreator/MapSimulator/Managers/LoginRuntimeManager.cs`, `HaCreator/MapSimulator/Managers/LoginBackendSessionManager.cs`, `HaCreator/MapSimulator/Managers/LoginCharacterAccountStore.cs`, `HaCreator/MapSimulator/Managers/LoginCharacterRosterManager.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Partial | Character roster and selection parity | The simulator now exposes a dedicated login-map character-select window, keeps a slot-aware roster model that distinguishes occupied entries from empty and buy-character slots, pages the selection surface across the full visible slot count instead of only populated entries, renders the client `Login.img/CharSelect` empty-slot silhouette and animated `buyCharacter/*` card state on those extra pages, and continues to hand the selected roster build into field entry before map transition. WZ inspection of `Login.img/CharSelect` still keeps the row anchored to the existing `BtSelect`/`BtNew`/`BtDelete` roster seam and the existing `buyCharacter/*` animated card branch, while client inspection of `CUICharSelect::OnButtonClicked`, `CLogin::OnViewAllCharResult`, `CLogin::OnCreateNewCharacterResult`, and `CLogin::OnDeleteCharacterResult` confirms the real client keeps roster ownership in packet-fed login state, mutates the in-memory avatar roster from those packet results, and only completes the expanded roster once the related-server aggregation drains rather than treating the buttons as local-only UI edits. Packet-authored roster data from `SelectWorldResult` and `ViewAllCharResult` is now part of the active path rather than the window always depending on simulator-seeded entries, and that packet-owned roster now caches into a persisted simulator account roster keyed by authenticated account id when the login bootstrap has surfaced one, with the existing login-account plus world key still acting as the fallback seam when packets are absent, so the no-packet path reopens the correct account-backed list instead of reseeding ad hoc simulator entries or colliding across reused account names. This pass moves the live roster ownership further into the intended backend seam: `LoginBackendSessionManager` now captures the authenticated account id from the login bootstrap, owns the active `SelectWorldResult` / assembled `ViewAllCharResult` roster profiles, feeds active-roster selection back into character-select, stays in the mutation path when `BtNew` or `BtDelete` rewrites the session roster from packet-authored create/delete results instead of leaving `MapSimulator` to reconcile duplicate snapshot fields on its own, now persists packet-owned empty-world roster states instead of dropping zero-entry worlds on the floor, now prunes cached authenticated-account worlds that disappear from an authoritative `ViewAllCharResult` refresh so later no-packet reopen and generated `ViewAllCharResult` discovery do not resurrect stale deleted characters or stale world ownership, and still persists the surviving multi-world packet snapshot back into the account store for later no-packet reuse. Remaining work is now narrower and mostly upstream-session owned: the simulator still does not reconcile those session-owned rosters against a true authenticated backend beyond the existing official-session bridge and packet-configuration seam, so live server-authoritative refresh, conflict resolution against concurrent external roster changes, and a byte-for-byte remote session owner for the full multi-packet account lifecycle are still incomplete. | The client's path into gameplay is built around explicit character selection rather than spawning a hardcoded local avatar directly into a field, so even this partial shell materially changes how the simulator enters gameplay on login maps | roster UI layer, character-selection model (`CUICharSelect::OnCreate`, `CUICharSelect::OnButtonClicked`; simulator seams: `HaCreator/MapSimulator/Managers/LoginCharacterRosterManager.cs`, `HaCreator/MapSimulator/Managers/LoginCharacterAccountStore.cs`, `HaCreator/MapSimulator/Managers/LoginBackendSessionManager.cs`, `HaCreator/MapSimulator/UI/Windows/CharacterSelectWindow.cs`, `HaCreator/MapSimulator/UI/Windows/AvatarPreviewCarouselWindow.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Partial | Alternate VAC character-entry parity | WZ inspection confirms the VAC notice and handoff assets this branch depends on are present in this data set: `Login.img/Notice/text` includes the client-used VAC notice canvases (`14`, `15`, `16`, `17`, `19`, `20`, `21`, `26`, `27`, `29`, `30`, `31`, `33`, `40`, `56`, `62`) and `Sound/Game.img/GameIn` matches the entry sound the client plays after a successful VAC connect. The simulator now keeps packet type `9` as its own owner path instead of reusing the normal select-character result seam: `LoginSelectCharacterByVacResultCodec` / `LoginSelectCharacterByVacResultProfile` decode the VAC packet as the client-owned `result + secondary` shape, preserve the direct-connect payload for the success branches (`result = 0`, `result = 23`, and `result = 12` with secondary `11` or `13`), and route the active entry path through `TryApplyPacketOwnedSelectCharacterByVacResult` with VAC-specific runtime, packet-chat, and success messaging. Client inspection of `CLogin::OnSelectCharacterByVACResult` at `0x5de670` is now reflected more exactly in the active simulator seam and in test coverage: the VAC failure table keeps its own `CLoginUtilDlg::Error(...)` and website-handoff mapping instead of borrowing the normal select-character table, including the distinct result-`15` website prompt through `Notice/text/26`; the title-reset split now matches the IDA-recovered client branch (`result = 7`, plus `result = 12` with secondaries `1`, `2`, `3`, `19`, `25`, `27`, and `28` only); and the VAC success path preserves the client ordering at the simulator handoff point by issuing the direct connect before playing `Sound/Game.img/GameIn`. Remaining work is now narrower and mostly live-session-specific: this row still does not replay or verify a real authenticated VAC round-trip end-to-end against official traffic, so parity past the simulator-owned direct-connect and field-entry handoff still depends on the recovered client branch rather than a captured live VAC session. | If this path stays invisible, the simulator can look complete for character entry while still missing a real client-owned alternate bootstrap route that bypasses part of the ordinary roster-select result handling. Tracking it separately keeps future work anchored to the actual packet router and prevents VAC-only failures or handoffs from being misfiled under the standard character-select row. | alternate login packet-entry layer (`CLogin::OnPacket`, `CLogin::OnSelectCharacterByVACResult`, `CWvsContext::IssueConnect`, `CLoginUtilDlg::Error`; simulator seams: `HaCreator/MapSimulator/Managers/LoginSelectCharacterByVacResultCodec.cs`, `HaCreator/MapSimulator/Managers/LoginRuntimeManager.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `HaCreator/MapSimulator/MapSimulator.ChatCommands.cs`) |
| Partial | Avatar preview carousel parity | The simulator now drives the login avatar-preview surface through a dedicated `AvatarPreviewCarouselWindow` owner separate from the wider roster shell, replaces the placeholder preview cards and page controls with client `Login.img/CharSelect` canvases (`charInfo`, `charInfo1`, `pageL`, `pageR`, plus the real `BtSelect`/`BtNew`/`BtDelete` button art on the roster shell), keeps the live assembled three-slot browsing, card focus, page-left/page-right cycling, and double-click field-entry handoff aligned with the three-slots-per-page interaction recovered from `CUIAvatar::OnMouseButton`, renders the per-card client nametag strip and job-family decoration path (`nameTag`, `adventure`, `knight`, `aran`, `evan`, `resistance`) using the client job-group selection logic recovered from `CUIAvatar::ResetCharacter`, and now replaces the wider simulator-owned owner shell with the client `Login.img/CharSelect` shell layers that are present in this data set (`scroll/0`, `effect/0`, `effect/1`, the animated `character/0` strip, and the `BtSelect/keyFocused` flourish) so the roster owner animates the parchment status shell and client selection accent from the same WZ-backed owner family instead of falling back to a static placeholder accent. This pass also closes the broader text fallback gap inside the same owner family: the carousel nametag width now follows the client rule recovered from `CUIAvatar::ResetCharacter` (`min 58px`, otherwise `CalcTextWidth + 18`), the nametag text no longer hardcodes `Tahoma`, both the carousel nametag text and the owner shell's `FONT_BASIC_BLACK` copy now reuse the simulator's client-font probing path, and targeted StringPool review of `CUIAvatar::OnCreate` now identifies the exact client font request as `Canvas#Font` plus the `Arial` face string before the client recolors those `IWzFont` objects for the grey and white login-name states. Remaining work is now narrower and explicit: the simulator still does not instantiate or bundle the client's final `IWzFont` rasterization byte-for-byte by default, so glyph shape, kerning, and alpha can still drift from the login client on machines without matching local fonts, and any still-unrecovered micro-placement or other owner children around the `CharSelect` shell remain to be mirrored end-to-end | Character-entry parity needs more than a text roster; the client lets the player browse and inspect live avatar previews before entering the game, and the simulator now exposes that browse and inspect loop through a dedicated preview owner with client-backed card, paging, nametag, job-decoration, and outer-shell art instead of only a text list | avatar-preview UI layer, character-preview assembly path (`CUIAvatar::OnCreate`, `CUIAvatar::OnMouseButton`, `CUIAvatar::ResetCharacter`; simulator seams: `HaCreator/MapSimulator/UI/Windows/AvatarPreviewCarouselWindow.cs`, `HaCreator/MapSimulator/UI/Windows/CharacterSelectWindow.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`) |
| Partial | Character detail panel parity | The simulator now constrains the login-map character-detail owner to the real client `Login.img/CharSelect/charInfo` and `charInfo1` canvases instead of extending them with a simulator-authored lower metadata slab, resolves and draws the real `icon/{up,down,same,job/*}` rank assets, keeps the world and job rank lanes right-aligned off the recovered `CUICharDetail::Draw` composition, and renders the detail rows live in the window draw pass rather than rasterizing text into a replacement panel bitmap. WZ inspection for `Login.img/CharSelect` still confirms `charInfo1` is only the taller second rank panel, not a hidden native owner for extra name, guild, EXP, or map rows. Client inspection of `CUICharDetail::Draw` still confirms the owner copies the background first and then renders the stat rows with `FONT_BASIC_BLACK`, and a fresh read of `get_basic_font` narrows that branch further: this client resolves `FONT_BASIC_BLACK` through the `FONT_DODOOMCHE_11_BLACK` branch, with the same black face family and 11px creation size the client uses for that basic-black path. The simulator now follows that same background-plus-live-overlay structure and no longer depends on the shared MonoGame `SpriteFont` metrics for the normal detail rows: `CharacterDetailWindow` measures and caches those rows through a dedicated runtime bitmap-text path, no longer hard-gates detail-row drawing on a shared `SpriteFont`, sizes the fallback raster font at the client-backed `12`-pixel draw height recovered from `get_basic_font`, trims and caches the rendered glyph bitmap before alignment so right-aligned rank numbers key off the actual rendered ink bounds instead of the looser pre-render `MeasureString` box, and now pushes the shared client-text resolver through the row-specific `DODOOMCHE` candidate list while explicitly preferring Maple executable or DLL embedded font payloads ahead of generic loose `Dotum` or `Gulim` files. The shared resolver now also honors those row-specific preferred families during its default private-font search instead of dropping back to only the simulator-wide generic list, so a local Maple install can surface the closer embedded `DODOOMCHE` face automatically without requiring `MAPSIM_FONT_BASIC_BLACK_{PATH,FACE}` or `MAPSIM_CLIENT_TEXT_FONT_{PATH,FACE}` overrides. Remaining work is narrower and explicit: the exact client `IWzFont` source object or embedded resource behind `get_basic_font(..., FONT_BASIC_BLACK)` is still not bundled, extracted, or identified byte-for-byte by default, so glyph shape, kerning, and final alpha output can still differ on machines that do not have a matching private Maple font payload available locally even though the simulator now prefers the closer embedded Maple sources when they exist | The client distinguishes high-level roster browsing from the focused detail view, so even a partial dedicated details owner materially improves the pre-entry feedback loop and gives later art-parity work a real surface to land on | character-detail UI layer, character metadata presentation (`CUICharDetail::Draw`, `get_basic_font`; simulator seams: `HaCreator/MapSimulator/UI/Windows/CharacterDetailWindow.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Partial | Dedicated create-character flow parity | The simulator now exposes dedicated login-map race, job, avatar, and name create owners instead of routing `BtNew` straight into the shared utility prompt, and that owner family is closer to the client than before: the create flow spans the full centered login scene, race selection renders as WZ-backed race cards drawn from the matching `Login.img/CustomizeChar/*` or `Login.img/NewChar*` `charAlert` art and placed in a client-shaped scene layout guided by `CUINewCharRaceSelect::OnCreate`, and avatar or name stages switch to the matching race-family `charSet`, `charName`, and `avatarSel` assets (`000`, `1000`, `2000`, `2001`, `3000`) instead of reusing one generic explorer theme for every family. The avatar owner also now respects the actual WZ row set more closely: it renders the `avatarSel/normal/*` labels instead of the disabled variant, and when a race family exposes a ninth `avatarSel` row the simulator uses that WZ-backed gender-row art instead of always falling back to plain simulator text. WZ inspection of `Login.img/NewChar/BtCheck` is now reflected in the dedicated name owner too, so the name stage no longer shows an inert reused `BtYes`; it exposes the client `BtCheck` art as the actionable duplicate-name button while keeping `Enter` on the name owner routed through the duplicate-id packet seam. The existing pre-field flow state machine still mirrors the packet follow-up path from `CLogin::OnNewCharJobSel`, `CLogin::OnNewCharAvatarSelCanceled`, `CUINewCharNameSelect::OnButtonClicked`, `CUINewCharNameSelect::OnKey`, `CLogin::OnCheckDuplicatedIDResult`, and `CLogin::OnCreateNewCharacterResult`: avatar cancel backs out through the same flow instead of dropping to the roster shell, duplicate-id success returns to avatar selection by recording the checked name instead of immediately mutating the roster, duplicate-id failure stays inside the create flow, and create-result success still mutates the authoritative roster only through `CreateNewCharacterResult`. This pass also closes more of the alternate send branch that was still missing before. Client inspection of `CLogin::SendNewCharPacket` at `0x5d7bd0` shows the create flow switching from opcode `22` to opcode `23` when `m_bCharSale` is active and carrying the ninth avatar-selection value through the char-sale branch; the simulator now mirrors that decision inside the dedicated flow by starting the create owner in char-sale mode whenever the account-backed roster is full but a buy-character slot is still present, then preserving that mode through packet synthesis, round-trip decode, live injection, and generated `CreateNewCharacterResult` synthesis so the extra-character branch uses the same packet-shaped request path as the normal branch. The explicit race-confirm hop remains reflected from WZ inspection of `Login.img/RaceSelect/confirm` and client inspection of `CUINewCharRaceSelect::OpenConfirmRaceDlg` plus `CConfirmRaceDlg::OnCreate`: picking a race or pressing the race-stage confirm button opens a dedicated `RaceSelect/confirm` modal, the modal uses the client `backgrnd`, `BtOK`, `BtCancel`, and per-race `confirm/race/*` art at the client-backed scene offset and button positions, cancel keeps the flow on race selection, and only accepting that confirm dialog advances into the job owner. Remaining work is now narrower and mostly pixel or key-path fidelity: exact client button rectangles for the avatar and name owners are still approximated rather than matched button-for-button from the hidden `OnCreate` constructors, and the simulator still does not yet mirror the fuller client keyboard/focus behavior around the race-confirm modal beyond the explicit dialog open, accept, cancel, and focus toggle path already present | Character creation is part of the visible pre-field pipeline, not just a roster mutation side effect. The simulator now has a dedicated pre-field create flow for that pipeline, and this pass moved the flow materially closer to client presentation by splitting the owner family, switching race choice to WZ-backed scene cards, honoring WZ-backed avatar row art including the gender row where present, restoring the client-owned race-confirm dialog hop before job selection, fixing the name owner to use the client duplicate-check button, and now carrying both the normal and extra-character create branches through packet-shaped request synthesis | create-character owner family, login packet follow-up path (`CUINewCharRaceSelect`, `CUINewCharJobSelect`, `CUINewCharAvatarSelect`, `CUINewCharNameSelect`, `CConfirmRaceDlg::OnCreate`, `CUINewCharRaceSelect::OpenConfirmRaceDlg`, `CLogin::OnNewCharJobSel`, `CLogin::OnNewCharAvatarSelCanceled`, `CUINewCharNameSelect::OnButtonClicked`, `CUINewCharNameSelect::OnKey`, `CLogin::OnCheckDuplicatedIDResult`, `CLogin::OnCreateNewCharacterResult`, `CLogin::SendNewCharPacket`; simulator seams: `HaCreator/MapSimulator/UI/Windows/LoginCreateCharacterWindow.cs`, `HaCreator/MapSimulator/Managers/LoginCreateCharacterFlowState.cs`, `HaCreator/MapSimulator/Managers/LoginNewCharacterRequestCodec.cs`, `HaCreator/MapSimulator/Character/CharacterLoader.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Implemented | Recommend world helper parity | The simulator now builds the login recommend-world helper from the client `Login.img/WorldSelect/alert` frame and button art, draws the selected world's WZ-backed `Login.img/WorldSelect/world/<id>` badge instead of a placeholder label, wraps per-world helper text in the same 125px message lane the client uses, places the owner off the recovered client `CUIRecommendWorld` login-layout rect (`CreateDlg(302, 152, 200, 228)` against the centered 800x600 login scene), and accepts live loopback `RecommendWorldMessage` inbox traffic by routing socket-fed payload text through the existing packet-configuration seam for packet-authored recommend-world ordering plus per-world message strings | Server recommendation is part of the client's account-entry UX, not just an incidental line of text on the world list. Wiring a dedicated helper into the login world step means recommendation and guided selection now exist as a distinct pre-field surface instead of being implied only through world-list status text | world-recommendation UI layer, world-selection assist flow (`CUIRecommendWorld::OnCreate`, `CUIRecommendWorld::Draw`, `CUIRecommendWorld::OnButtonClicked`; simulator seams: `HaCreator/MapSimulator/UI/Windows/RecommendWorldWindow.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`, `HaCreator/MapSimulator/MapSimulator.cs`) |
| Partial | Connection-notice and login-utility dialog parity | The simulator now exposes dedicated login-map `ConnectionNotice` and `LoginUtilityDialog` modal windows wired to world or channel request progress, blocked character-entry validation, delete confirmation, client `Login.img/Notice` background variants (`backgrnd/0`, `backgrnd/1`, `backgrnd/2`, `Loading`, `LoadingSG`), the `Login.img/Notice/Loading/circle/*` 16-frame spinner animation on the loading notice owner, client canned notice canvases (`Notice/text/*`) rendered through both owners, packet-authored account prompt overrides on the existing `/loginpacket`, loopback inbox, and live official-session bridge seam for `AccountInfoResult`, `SetAccountResult`, `ConfirmEulaResult`, `CheckPinCodeResult`, `UpdatePinCodeResult`, `EnableSpwResult`, `CheckSpwResult`, `CreateNewCharacterResult`, and `DeleteCharacterResult`, plus the client-backed yes/no security prompts for `SelectWorldResult` server codes `14` and `15` via `Notice/text/27` and `Notice/text/26`. WZ inspection of `Login.img/Notice/Loading/BtCancel` and `LoadingSG/BtCancel`, together with IDA for `CConnectionNoticeDlg::OnCreate`, is now also reflected in the active path: the loading connection notice exposes a real cancel button at the client-backed slot instead of behaving like a passive splash only, and that cancel path can now abort pending selector waits or the title-to-world delayed bootstrap transition inside the existing runtime seam. Client inspection of `CLogin::OnSelectWorldResult`, `CLogin::OnUpdatePinCodeResult`, `CLogin::OnEnableSPWResult`, and `CLogin::OnCheckSPWResult` is now also reflected in the active path: packet-authored `SelectWorldResult` failures no longer stop at plain prose for only the website-handoff cases, but map through the same broader `CLoginUtilDlg::Error(...)` family (`Notice/text/3`, `14`, `15`, `16`, `17`, `19`, `20`, `21`, `27`, `33`, `40`) the client uses, while packet-authored PIC and SPW result payloads can collapse into passive canned notices instead of reopening the shared input flow when the client would already be on a result-only notice (`UpdatePinCodeResult` success/failure via `Notice/text/8` or `15`, `EnableSPWResult` result families via `Notice/text/18`, `39`, `40`, `91`, `92`, `93`, and `CheckSPWResult` via `Notice/text/93`). WZ inspection of `UIWindow2.img/UtilDlgEx/notice` and IDA for `CLoginUtilDlg::OnCreate` are now reflected a bit more directly in the shared utility seam as well: input-bearing prompts no longer behave like plain wrapped body text only, because the dialog now keeps a dedicated editable lane alive when canned notice art is present and uses the client-backed notice-side input slot for those prompts instead of dropping the input field whenever the canned notice has no extra prose, including the shared `CheckSpwResult` verification owner. The packet override seam now also preserves the base modal owner unless `/loginpacket ... mode=` explicitly changes it, so partial overrides no longer silently demote connection-notice prompts back into the utility dialog, and the same override parser now exposes the SPW-specific button family (`enabledisablespw`) instead of limiting packet-authored account prompts to the older generic layouts only. Packet-authored account-dialog payloads also preserve more transport context inside the active prompt path instead of behaving like anonymous cached blobs: the login packet configuration seam still routes those packet types through the account-dialog codec, both modal owners keep supplemental body text visible underneath canned notice canvases so decoded payload detail such as result codes, secondary codes, account ids, character ids, gender, grade, account flags, country, club id, purchase experience, chat-block metadata, register date, character count, and client key survive even when the client-backed notice art is shown, and the decoded detail block now also retains the packet source so live official-session traffic can stay distinguishable from command-authored or loopback-authored prompts. Remaining work is narrower: connection notices still approximate the client's richer modal family with the first two owners instead of adding the other pre-field dialog classes, true dedicated packet-backed owners for PIC dialogs, SPW setup and verification, account migration, and website handoff are still not present because those flows continue to run through the shared simulator modal seam, the `enabledisablespw` override layout is available only as a configurable shared-dialog approximation rather than a client-owned dedicated owner stack, and the live backend path still reaches this row through the simulator's inbox or session bridge rather than a byte-for-byte clone of the client's original authenticated login owner stack | Many account-entry flows are mediated through modal notices and utility prompts, so adding explicit modal owners closes an important parity gap and lets login-shell actions surface through dedicated dialogs instead of footer text alone, even though more account-specific prompts still remain | account-entry dialog layer, modal utility flow (`CConnectionNoticeDlg::Draw`, `CConnectionNoticeDlg::OnCreate`, `CLoginUtilDlg::Draw`, `CLoginUtilDlg::OnCreate`, `CLogin::OnSelectWorldResult`, `CLogin::OnUpdatePinCodeResult`, `CLogin::OnEnableSPWResult`, `CLogin::OnCheckSPWResult`; simulator seams: `HaCreator/MapSimulator/UI/Windows/ConnectionNoticeWindow.cs`, `HaCreator/MapSimulator/UI/Windows/LoginUtilityDialogWindow.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `HaCreator/MapSimulator/Managers/LoginOfficialSessionBridgeManager.cs`) |

### 2. Login-scene balloon presentation parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Login-scene selector, roster, and avatar balloons | The latest broad IDA function scan found a login-owned balloon family that was not named in this backlog set: `CLogin::GetBalloonInfo` returns the login `BALLOON` array, `CUIWorldSelect::MakeWSBalloon` builds world-select balloon layers, and `CUICharSelect::MakeBalloon` plus `CUIAvatar::MakeBalloon` build sized balloon layers for the character-select and avatar-entry owners. The simulator currently tracks field chat, MiniRoom, employee, ADBoard, and mob balloon work elsewhere, but those rows do not cover pre-field selector help, roster/avatar callouts, arrow sizing, text wrapping, lifetime, or placement inside the login scene. | Without this row, login presentation parity can silently miss client-owned callouts while the social balloon backlog appears to cover "balloons" generally. Keeping this here pins the missing work to the login owners that create and consume the layers. | login-scene presentation layer (`CLogin::GetBalloonInfo`, `CUIWorldSelect::MakeWSBalloon`, `CUICharSelect::MakeBalloon`, `CUIAvatar::MakeBalloon`; simulator seams: `LoginRuntimeManager`, `WorldSelectWindow`, `CharacterSelectWindow`, `AvatarPreviewCarouselWindow`, `LoginCreateCharacterWindow`) |

## Priority Order

If the goal is visible parity first, the next work in this area should be sequenced like this:

1. Bootstrap pass:
   Add a minimal login-state shell and step model so the simulator can represent pre-field transitions at all.
2. World/channel selection pass:
   Add dedicated world-select, channel-select, and channel-shift owners before treating recommend-world or connection notices as the whole pre-field selection surface.
3. Character-entry pass:
   Add roster, avatar-preview, and character-detail surfaces before attempting deeper account prompts.
4. Alternate entry-result pass:
   Add the packet-owned `OnSelectCharacterByVACResult` branch before treating the normal select-character result as the whole login handoff surface.
5. Create-character pass:
   Add the dedicated race, job, avatar, and name-selection owners before treating create-character parity as complete.
6. Utility-dialog pass:
   Add connection notices, recommend-world flows, and login utility dialogs once the main entry shell exists.
7. Login-scene balloon pass:
   Add the pre-field balloon owner family after the selector and avatar surfaces exist, and keep it separate from field chat or MiniRoom balloon work.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
