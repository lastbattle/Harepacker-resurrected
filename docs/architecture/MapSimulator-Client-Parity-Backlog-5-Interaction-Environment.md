# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/DropPool.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Field and portal parity

- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`

Notes:
`CUserLocal::TryPassiveTransferField` shows that client field transfer is not only explicit portal collision handling; the local-player update loop also retries an up-key style transfer after one-time actions finish when the character is allowed to move. A later IDA pass also shows that packet-authored field utility surfaces are split into their own remote pools rather than hanging off local collision handling: `CAffectedAreaPool::OnPacket` owns affected-area create/remove, `CTownPortalPool::OnPacket` owns town-portal create/remove, and `COpenGatePool::OnPacket` owns Open Gate create/remove.

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`

Notes:
The backlog already covered pickup notices and pet auto-loot side effects, but the broader client owner was still unnamed. IDA shows that drop parity is owned by a dedicated `CDropPool` runtime that handles enter/leave packet dispatch, per-frame drop update, player/pet/mob pickup arbitration, meso animation, and recent-pickup history instead of treating drops as generic field objects plus UI messages.

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive chairs, same-field teleports, and quest results from local intent and generic runtime state. It also has dedicated packet-response handlers that finalize those flows after the request is acknowledged.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Field and portal parity

The old backlog understated what is already present here, but there is still real work left.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Hidden portals | Reveal, hide, alpha fade, and collision gating are implemented | Remove from missing backlog | `PortalPool.cs` (`CPortalList::FindPortal_Hidden`) |
| Implemented | Portal property accessors | `PH`, `PSH`, and `PV` equivalents are implemented | Remove from missing backlog | `PortalPool.cs` |
| Implemented | Portal interaction parity | Standard and hidden portal triggering now share the same same-map delay seam as client-confirmed portal teleports, and Mechanic `Open Portal: GX-9` casts now place WZ-backed temporary linked gates that resolve on `Up` interaction with the client-like 120 ms same-map handoff; town portal / mystic door remains tracked separately below | Portal travel now covers the main visible, hidden, and same-map skill-portal branches instead of reducing open-gate travel to unsupported or generic transitions | `PortalPool.cs`, `MapSimulator.cs`, `TemporaryPortalField.cs` (`CUserLocal::CheckPortal_Collision`, `CUserLocal::TryRegisterTeleport`, `Skill/3510.img/skill/35101005`) |
| Partial | Passive transfer-field handoff parity | WZ re-check in the active export root still confirms portal-local transfer metadata in `map/Map/Map0/000050000`: map-level `info/fieldLimit = 4` coexists with ordinary transfer portal data on `portal/5` (`pn = east00`, `pt = 2`, `tm = 1000000`, `tn = west00`, `delay = 0`) and separate tutorial collision-script ownership on `portal/6` (`pn = tuto00`, `pt = 9`, `tm = 999999999`, `script = infoWorldmap`, `onlyOnce = 1`). Client re-check in IDA still confirms `CUserLocal::TryPassiveTransferField` (`0x91a2e0`) waits for `CAvatar::GetOneTimeAction() <= -1`, checks live interface gate `m_pvc.m_pInterface + 416`, gates replay by `!IsImmovable && !IsAttract`, and clears pending after gate admission, while `HandleUpKeyDown` (`0x919e50`) still stops skill macro first (`CMacroSysMan::Stop`), keeps one-time/immovable/foothold/portal ordering, keeps same-map non-changeable flow on `TryRegisterTeleport`, and routes cross-map or same-map changeable flow to `CField::SendTransferFieldRequest` with sound only when `!PORTAL::IsChangable`. The simulator keeps ownership in the same seams and makes queued-clear ownership explicit in `PassiveTransferFieldReadinessEvaluator`: fully admitted snapshots report replay ownership and only clear through `ShouldClearQueuedRetryAfterInterfaceGateAdmission`, transfer/map-load lifecycle consumption routes through `ShouldClearQueuedRetryFromTransferLifecycle`, packet-owned local-follow transfer detach arms retry through `ShouldArmQueuedRetryFromFollowCharacterTransferDetach`, chair get-up consumption clears through `ShouldClearQueuedRetryOnChairGetUp`, and recovered horizontal `CUserLocal::OnKey` release now uses one `EvaluateHorizontalOnKeyDown` decision so macro stop and queued retry clear stay coupled in the evaluator instead of separate caller-side branches. This pass also narrows `HandleUpKeyDown` ordering: fresh Up-key input now asks `ShouldStopSkillMacroForFreshUpKeyDown` and stops the skill macro before the attached packet-owned driver gate, while `CanHandleFreshUpKeyDown` remains the separate driver-admission gate. Regression coverage in `UnitTest_MapSimulator/PassiveTransferFieldHandoffParityTests.cs` now pins no-pending clear ownership, one-time-action holds, unresolved readiness owners, stale recovered-ready snapshots without live interface ownership, replay-plus-clear admission, transfer-lifecycle consumption, fresh Up-key macro-stop ownership before the driver gate, horizontal decision coupling, horizontal-key cancellation, horizontal `OnKey` macro-stop ownership, follow-transfer detach arming, chair get-up clear ownership, and immovable/attract replay gating. Remaining gap: simulator ownership of client interface gate `m_pvc.m_pInterface + 416` now keeps pending retry armed through unresolved readiness windows, replays only on fully admitted snapshots, centralizes the known transfer/map-load, chair, follow-detach, fresh Up-key macro-stop, and horizontal `OnKey` clear seams, and covers the recovered local-follow left/right release clearer, but it still does not reconstruct every native writer/clearer byte-for-byte, especially unrecovered external writers outside `TryPassiveTransferField`/`HandleUpKeyDown`/the recovered `OnKey` left-right branch and any undiscovered transfer/map-load lifecycle writers. | Without this row, passive transfer-field behavior can look like a minor portal follow-up even though the client treats it as a separate retry owner tied to movement state, one-time action completion, live interface admission, macro cancellation ordering, and portal-routing branches. Tracking it explicitly here keeps future parity work anchored to the handoff owner seam instead of unrelated teleport/collision polish. | `MapSimulator.cs`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `Character/PlayerCharacter.cs`, `Character/Skills/SkillManager.cs`, `Physics/CVecCtrl.cs`, `Fields/FieldRuleRuntime.cs`, `Fields/SpecialFieldRuntimeCoordinator.cs`, `Fields/PassiveTransferFieldReadinessEvaluator.cs`, `Interaction/LocalFollowCharacterRuntime.cs`, `UnitTest_MapSimulator/PassiveTransferFieldHandoffParityTests.cs`, portal/field transition layer (`CUserLocal::TryPassiveTransferField`, `CUserLocal::HandleUpKeyDown`, `CUserLocal::OnKey`, `CMacroSysMan::Stop`, `CAvatar::GetOneTimeAction`, `CWvsContext::SendGetUpFromChairRequest`, `CField::SendTransferFieldRequest`, WZ `Map/*/info/fieldLimit`, `Map/*/portal/*`) |
| Implemented | Transportation fields | Regular passenger ships now expose a dedicated transport-deck foothold that carries both the player and grounded mob passengers through the existing movement seam, while Balrog-only ship visuals no longer publish a false passenger deck | Transport maps now keep visible deck passengers riding the ship during departures and arrivals instead of leaving the transport system as a mostly visual-only layer | `TransportationField.cs`, `PassengerSyncController.cs`, `MapSimulator.cs` |
| Partial | Dynamic objects / quest layers | Field object drawables still retain WZ `quest` visibility metadata, keep authored `hide=1` quest layers suppressed until quest-state/tag publication reveals them, and keep tagged defaults from `publicTaggedObjectVisible` plus `pulbicTaggedObjectVisible` through the `MapLoader` copy seam. Entry-owned map scripts (`onUserEnter`, `onFirstUserEnter`, `fieldScript`), portal scripts/`pn` fallback, `directionInfo/*/EventQ`, quest `showLayerTag`, quest `startscript` / `endscript` / `npcAct`, NPC `info/script`, packet-owned NPC utility owners, reactor-owned publications, and item `spec/script` still publish through one dynamic-object seam with stage-family retirement, quest-id fallback (`q<questId>s` / `q<questId>e` -> `q<questId>` / numeric tag), path/extension normalization, numeric first-argument alias recovery, no-argument function alias-call recovery, and timer/callback delay parsing. This pass narrows parser and publication seams again in the same owners: script-container keys that represent metadata owners (`startscript`, `endscript`, `npcAct`, `onUserEnter`, `onFirstUserEnter`, `fieldScript`) still no longer self-publish as alias names when nested branches are recoverable; quest `npcAct` still carries parsed publication metadata (including wrapper-authored delay/alias publications) into the same published-script-publication seam used by start/end scripts, with first parsed alias fallback for action naming when raw string extraction is empty; `QuestRuntimeManager.ParseScriptNames(WzImageProperty)` now also skips scalar metadata leaves (`delay`/`wait`/`time`/`state`/`value`/`show`/`on`) unless they hold nested alias structure, so metadata values no longer leak as script aliases while nested wrapper traversal remains intact for alias recovery; and timer/callback publication parsing now normalizes callback-function text into canonical alias candidates before publication, so function-body callback wrappers (for example `setTimeout(function(){ q22000e(); }, 120)`) publish/schedule on recovered alias names instead of raw callback source text. This pass narrows that same callback seam again by recovering canonical member-leaf alias candidates from member-qualified callback references (for example `this.q22000e`, `owner.q22000e`, and bracket-member forms), including bracket-member function-body invocation and bracket-member callback-assignment recovery (`this["q22000e"]`, `owner['q22000e']`) in the same canonical alias seam, so timer publications and direct alias resolution no longer stay pinned to wrapper text when an authored leaf alias is recoverable. This pass narrows callback publication parsing again in the same seam by recovering authored aliases from function-wrapper return-member callbacks (for example `var cbFactory = function(){ return this.q22000e; }; setTimeout(cbFactory(), 70);`) and from recoverable object-state callback dispatch (`var callbacks = { on: this.q22000e }; var state = "on"; setTimeout(callbacks[state], 60);`). This pass narrows that callback seam once more by unwrapping direct callback-invoker wrappers (`.bind` / `.call` / `.apply`) to their callback-target expressions before canonical publication, so callback-argument wrappers such as `setTimeout(this.q22000e.bind(this), 80)`, `setTimeout(cb.bind(this), 50)` (`cb = this.q22000e`), and `setTimeout(owner['q22000e'].bind(owner), 70)` publish/schedule on authored alias `q22000e` instead of wrapper text. This pass narrows that same return-wrapper seam again by recovering authored aliases from function-wrapper return-variable callbacks (for example `var cb = this.q22000e; var cbFactory = function(){ return cb; }; setTimeout(cbFactory(), 58);`) and from function-wrapper return object-state dispatch (`var callbacks = { on: this.q22000e }; var state = "on"; var cbFactory = function(){ return callbacks[state]; }; setTimeout(cbFactory(), 62);`), so wrapper-return indirection no longer degrades publication to state-key or wrapper text when object/member metadata is recoverable in the same parser seam. This pass narrows the same callback-expression seam again by recovering all parser-visible member leaves from conditional callback expressions (for example `setTimeout(active ? this.q22000e : owner.q22000s, 64)`) and by treating array-literal callback tables as indexed member maps, so direct and function-returned array dispatch (`callbacks[1]`, `callbacks[0 + 1]`) publishes/schedules on the authored callback alias instead of the index expression. This pass narrows that branch-expression seam again by resolving parser-visible ternary and logical fallback callback branches after local alias-variable and object/table dispatch recovery (for example `setTimeout(active ? end : start, 112)`, `setTimeout(active ? callbacks.end : callbacks.start, 114)`, `setTimeout(primary || fallback, 116)`, and function-returned variants), so callback publication no longer degrades to branch wrapper text when the branch target metadata is recoverable. This pass narrows the same function-wrapper return seam again by treating bare authored alias returns as canonical callback candidates (for example `setTimeout(function(){ return q22000e; }, 120)`, `return active ? q22000e : q22000s`, and block-bodied arrow factory returns), so wrapper returns no longer require a local-variable or member/object wrapper before publication can recover the authored alias. This pass narrows the callback-expression seam again by canonicalizing optional member access before alias recovery (`this?.q22000e`, `owner?.['q22000s']`, and `callbacks?.[state]`) and by resolving parenthesized sequence/comma callback expressions to their executable tail target (`setTimeout((start, end), 142)` and function-returned `(callbacks.off, callbacks[state])`), so parser-visible authored aliases no longer stay pinned to optional-chain or sequence-wrapper text when the final callback leaf is recoverable. This pass narrows the same callback-expression seam again by unwrapping no-argument immediately-invoked function and arrow callback wrappers, plus parenthesized function targets passed through `.call` / `.apply` / `.bind`, before canonical publication (`setTimeout((function(){ return this.q22000e; })(), 146)`, `setTimeout((() => callbacks[state])(), 148)`, and `setTimeout((function(){ return this.q22000e; }).call(owner), 150)`), so direct wrapper invocation no longer publishes/schedules on wrapper source text when the invoked wrapper exposes a recoverable authored alias. Parser regression coverage in `UnitTest_MapSimulator/FieldObjectScriptPublicationParserParityTests.cs` now also includes metadata-leaf suppression, callback-function-body alias recovery, bracket-member callback invocation/assignment recovery, outer alias-variable callback invocation recovery, bracket-variable callback-assignment recovery, function-wrapper return-member alias recovery, function-wrapper return-bracket-member alias recovery, function-wrapper return-variable alias recovery, function-wrapper return object-state callback-dispatch alias recovery, direct callback-argument bind-wrapper alias recovery, conditional member callback-expression recovery, conditional alias-variable callback-expression recovery, conditional object-dispatch callback-expression recovery, logical fallback callback-expression recovery, return-conditional alias-variable recovery, bare function-wrapper return alias recovery, bare return-conditional alias recovery, block-bodied arrow return alias recovery, optional-chained callback member/object dispatch recovery, sequence-tail callback-expression recovery, function-returned sequence-tail object-dispatch recovery, no-argument immediately-invoked function and arrow callback recovery, parenthesized function callback-invoker target recovery, array-index callback-table dispatch recovery, parser-visible object/array mutation callback dispatch recovery, numeric-index variable dispatch recovery, push/unshift-built callback-table dispatch recovery, array/object spread-copied callback-table dispatch recovery, push-spread callback-table mutation recovery, slice-copied array callback dispatch recovery, and array/object destructured callback-alias recovery alongside mixed direct+wrapper and metadata-wrapper-only stacks with inherited delay on nested alias publications. Remaining gap: script-engine-owned sequencing is still absent when runtime control flow never surfaces parser-visible alias metadata (for example closure-only chains whose callable target is synthesized only inside nested runtime closures, argument-dependent immediate invocations whose returned callback depends on runtime arguments, runtime-computed property keys whose member/key text never resolves to a recoverable literal/alias, callback expressions whose branch targets are computed behind runtime-only state, and callback tables assembled behind runtime-only builders or computed mutation keys with no parser-visible literal/member metadata), so fully engine-internal callback chains can still remain unmodeled. | Quest/event maps keep publication ownership in one seam instead of per-feature toggles, and quest `npcAct` metadata no longer drops out of dynamic-object publication when authored under wrapper containers. This pass further closes parser-side drift by suppressing metadata-leaf alias leakage, by publishing callback wrappers on recovered alias names, by normalizing member-qualified and optional-chained callback aliases down to canonical leaf candidates in the same dynamic-object publication owner seam, by recovering bracket-member callback aliases when wrapper text still carries authored member leaves, by resolving outer/local alias-variable callback references (including `this[key]` assignment flows when key variables resolve to authored alias leaves), and by unwrapping direct callback-invoker wrappers to authored callback targets before publication, by recovering all parser-visible conditional member leaves, by resolving conditional/logical branch callback expressions through local alias-variable and object/table dispatch metadata, by resolving sequence callback wrappers to their tail callback target, by unwrapping no-argument immediately-invoked function/arrow callback wrappers and parenthesized function callback-invoker targets before publication, by resolving parser-visible array-index callback tables before publication, by carrying parser-visible callback table mutation (`callbacks.on = this.q22000e`, `callbacks[1] = owner.q22000e`, `callbacks.push(owner['q22000e'])`, `callbacks.unshift(owner.q22000e)`, `callbacks.push(...sourceCallbacks)`) into the same indexed member-map seam, by preserving parser-visible alias maps through array/object spread, slice copy, and destructuring seams, and by recovering bare authored alias returns from function and arrow callback wrappers. Remaining risk stays scoped to script-engine-only control flow where closure/runtime-computed state never surfaces recoverable parser-visible publication metadata, or where callback branches and tables are assembled behind runtime-only mutation/builders or computed keys with no recoverable literal/member metadata. | `MapLoader.cs`, `MapSimulator.cs`, `QuestRuntimeManager.cs`, `FieldObjectTagStateDefaultsLoader.cs`, `FieldObjectScriptTagAliasResolver.cs`, `FieldObjectScriptPublication.cs`, `FieldObjectDirectionEventTriggerLoader.cs`, `FieldObjectNpcScriptNameResolver.cs`, `ReactorScriptStatePublisher.cs`, `UI/Windows/InventoryItemMetadataResolver.cs`, `UnitTest_MapSimulator/FieldObjectScriptPublicationParserParityTests.cs`, parser-visible callback expressions such as `setTimeout(active ? this.q22000e : owner.q22000s, 64)`, `var start = this.q22000s; var end = owner.q22000e; setTimeout(active ? end : start, 112)`, `setTimeout(primary || fallback, 116)`, `setTimeout(active ? this?.q22000e : owner?.['q22000s'], 138)`, `setTimeout((start, end), 142)`, `return (callbacks.off, callbacks[state])`, `var callbacks = [this.q22000s, owner['q22000e']]; setTimeout(callbacks[1], 72)`, and `var callbacks = []; callbacks.push(this.q22000s, owner.q22000e); setTimeout(callbacks[1], 78)`, `callbacks.unshift(owner.q22000e); setTimeout(callbacks[0], 82)`, `var callbacks = [...baseCallbacks, owner['q22000e']]; setTimeout(callbacks[1], 126)`, `callbacks.push(...sourceCallbacks); setTimeout(callbacks[1], 128)`, `var callbacks = sourceCallbacks.slice(); setTimeout(callbacks[1], 130)`, `var [start, end] = callbacks; setTimeout(end, 134)`, and `var { end: selected } = callbacks; setTimeout(selected, 136)`, WZ `quest/Act.img/2165/1/npcAct = quest`, WZ `quest/Act.img/9539/1/npcAct = q9933e`, WZ `quest/Check.img/10008/0/startscript = q10008s`, WZ `quest/QuestInfo.img/22000/showLayerTag = 22000`, WZ `npc/0002003.img/info/script/0/script = begin5`, WZ `item/Consume/0243.img/02430073/spec/script = consume_2430073` |
| Implemented | Town portal / mystic door lifecycle | `Mystic Door` skill casts now create a WZ-backed cross-map door pair, place the return-side door on the target town map's `TownPortalPoint` / `tp` spawn, transfer into town on `Up`, and restore the player to the original cast position when the town-side door is used before expiry | The simulator now covers the visible create, enter, restore, and positioning loop for the client's town-door utility instead of leaving the skill family nonfunctional | `TemporaryPortalField.cs`, `MapSimulator.cs`, portal/field transition layer (`Skill/231.img/2311002/{cDoor,mDoor,common/time}`, `PortalType.TownPortalPoint`) |
| Partial | Remote portal-pool parity | Packet-authored remote portal pools remain in their own runtime seam instead of folding into local skill ownership, but the seam keeps client-facing `330/331` town-portal and `332/333` Open Gate lifecycle ownership aligned: Mystic Door owner/state phase handling (`state=0` fade snapshots, `Frame` overlay, nonzero re-create stable hold), Open Gate owner+slot phase transitions across `cDoor`/`sDoor`/`mDoor`/`eDoor`, immediate Mystic Door unlink during `state=0` fade, same-map-only Open Gate pairing/link gating, opening-partner link suppression, same-tick partner loss/create promotion-preserve behavior, same-slot create lifecycle preservation so `state=0` re-creates no longer demote a stable slot back to opening (including same-slot re-create while the previous stable slot is still in `Removing`), and stable-partner visual gating so linked visuals/links appear only when both sides are stable. Town-side source inference remains owner+town scoped and authority-ranked with carry-forward of metadata and existing inferred source identity when owner-observation groups are missing; pending owner-observation adoption keeps metadata/existing-source identity fallback when WZ town-map validation is unavailable; WZ fallback still validates preferred source against `returnMap`/`forcedReturn`, resolves preferred source immediately when position exists, and holds unresolved when preferred source validates but has no recoverable position (instead of jumping to unrelated unique candidates). Source-map return fallback remains in the same WZ seam for owner-observation misses: when a create arrives on a non-town-portal-point map, destination resolves from that map's configured `returnMap`/`forcedReturn` and the resolved town map's portal position (preferring `TownPortalPoint`/`tp`, then `StartPoint`, then first authored portal). Existing source-position fallback still prefers `StartPoint` and only falls back to first authored portal when no start portal exists. This pass keeps those WZ-authored portal-position orders in shared selector helpers used by runtime resolution and regression tests, so town-side destination fallback and source-side position fallback cannot drift into separate inline implementations. Configured source-town resolution now routes preferred-source validation, source-map return fallback, and unique WZ fallback through the same cached `MapInfo` / parsed WZ `info` lookup helper and the same `returnMap`/`forcedReturn` normalizer used by fallback tests, so invalid `returnMap` (`999999999`/nonpositive), valid `forcedReturn`, and cached-metadata misses cannot drift between preferred and unique runtime fallback paths. Source-side create packets that resolve town destination through the same `returnMap`/`forcedReturn` fallback persist packet-authored source coordinates back into owner+town `PacketCast` metadata (and adopt pending owner observations for that resolved town), so later town-side creates can reuse exact cast coordinates when those source packets were seen even if explicit town-side destination metadata was absent. Packet-cast metadata persistence is now also explicitly provenance-gated in that seam: shape-valid destinations only persist when resolution actually came from the source-map return fallback path, not just because the destination map shape matches. Dedicated regression coverage in `UnitTest_MapSimulator/RemotePortalPoolParityTests.cs` now includes preferred-source carry-forward, cross-town stale-state suppression, pending-owner adoption identity fallback, preferred/unique WZ fallback resolution (including ambiguity and validated-without-position unresolved hold), source-map return fallback resolve/suppression, configured `returnMap`/`forcedReturn` normalization and unique WZ fallback through `forcedReturn`, town-destination fallback order (`TownPortalPoint`/`tp` -> `StartPoint` -> first portal), source-position fallback order (`StartPoint` -> first portal), start-portal detection, owner-observation replacement authority, Mystic Door removing-phase non-link gating, Open Gate same-map/link/visual gating, partner create/loss phase transitions, same-slot `state=0` lifecycle preservation (including removing-phase re-create preserve-vs-opening split), and source-map-return-fallback packet-cast metadata persistence/provenance gating. Remaining hard gap from IDA `CTownPortalPool::OnTownPortalCreated` (`0x762c00`) still stands: the create packet only carries state, owner id, and current portal coordinates, so exact source-field cast-point recovery remains unrecoverable when no owner-backed source observation exists, no source-side create packet was observed to backfill owner+town packet-cast metadata, and no WZ source-position fallback resolves. Open Gate packet ownership remains aligned to `COpenGatePool::OnOpenGateCreated` (`0x68bfd0`) and `COpenGatePool::OnOpenGateRemoved` (`0x68b780`). | Visible parity no longer stops at self-authored portal utility: packet-owned remote Mystic Doors and Open Gates keep client-shaped create/remove lifecycle, pairing/link rules, and town-return inference inside one seam. This pass also keeps WZ-authored map portal fallback order and configured source-town lookup as shared runtime selector paths rather than duplicated inline choices. Remaining risk is now concentrated on packet-only Mystic Door source cast-point recovery when both owner evidence and WZ position fallback are absent. | field portal/runtime layer (`CTownPortalPool::OnPacket`, `CTownPortalPool::OnTownPortalCreated`, `COpenGatePool::OnPacket`, local reference seam: `CUserLocal::CheckPortal_Collision`, remote skill-use presentation seam), `HaCreator/MapSimulator/Fields/TemporaryPortalField.cs`, `UnitTest_MapSimulator/RemotePortalPoolParityTests.cs`, WZ `Skill/231.img/skill/2311002/{cDoor,mDoor,Frame}`, WZ `Skill/3510.img/skill/35101005/{cDoor,sDoor,mDoor,eDoor}`, WZ `Map/*/info/{returnMap,forcedReturn}`, WZ `Map/*/portal/*/{pt,pn,x,y}`, client pool owners `CTownPortalPool::OnPacket` (`0x7636b0`), `CTownPortalPool::OnTownPortalCreated` (`0x762c00`), `COpenGatePool::OnPacket` (`0x68c8b0`), `COpenGatePool::OnOpenGateCreated` (`0x68bfd0`), and `COpenGatePool::OnOpenGateRemoved` (`0x68b780`) |
| Partial | Remote affected-area / mist parity | The simulator keeps the packet-owned `328/329` affected-area runtime, start-delay activation, field-bind clearing, remote support projection, mixed friendly-plus-hostile Battle Mage aura handling, WZ-backed support ownership checks, WZ-backed `type=3` fog visuals and linked-duration resolution, and the existing mob-skill-mist reapplication path. This pass also snapshots owner runtime context at packet-owned area create time: live `ownerId -> name`, Battlefield owner-team assignment, and Monster Carnival owner-team-by-name are cached immediately when available so ownership checks can keep replaying after remote actor teardown without waiting for a later live lookup. The hostile player-area lane now preserves WZ-authored DOT metadata from `common/dot`, `dotInterval`, and `dotTime` instead of collapsing everything to generic `damage` / `time`, so affected-area wrappers such as `Skill/312.img/skill/3120010` and `Skill/3510.img/skill/35101009` keep their authored burn-style magnitude and timing when the packet-owned area path drives `SkillManager`'s inferred mob-status seam. The resolver also now treats linked hostile companions as part of disposition resolution instead of only the primary shell, and the linked-runtime collector now follows WZ `info/dummyOf` wrapper links in the same seam as `affectedSkill`, so sparse packet skills such as `Skill/3510.img/skill/35100008` can still reach hostile companion data on `35101009` / `35101010` before disposition and inferred mob-status replay run. This pass also closes a sparse-wrapper level-selection hole in the same support projection seam: when the visible wrapper has no direct level node, linked support and hostile companions now resolve from the packet-authored runtime skill level (and WZ `PVPcommon` level data in PvP contexts) instead of falling back to level `1`, so wrapper-driven areas keep authored high-level support magnitudes and timings. This pass narrows the same hostile linked-runtime seam again for sparse packet level payloads: when packet-owned hostile wrappers arrive with runtime skill level `0` but the visible primary level already resolved above `1`, linked hostile companions now reuse that resolved primary level before loading their own level data instead of collapsing back to level `1`. This pass also consumes that WZ DOT metadata in the packet-owned runtime instead of only carrying it through the inferred-status seam: hostile player-area gameplay now ticks on the fastest reachable positive `dotInterval`, and player-skill affected-area expiration now resolves the maximum reachable authored lifetime across the visible skill plus its `affectedSkill`, `dummyOf`, and reverse `affectedSkill` parent chain. In Battlefield / Monster Carnival contexts that same staging seam now prefers WZ `PVPcommon` level data before regular `common`, so packet-owned areas no longer expire early just because the gameplay lane already switched to PvP-authored `dotTime` or `time` while `AffectedAreaPool` was still building the visual/runtime shell from the non-PvP node. The same sparse-wrapper seam still feeds the visual owner in `AffectedAreaPool`, so wrappers such as `Skill/3510.img/skill/35100008` and reverse-parent auras surfaced through `Skill/3210.img/skill/32101002` no longer have to stay gameplay-only while their visible mist or aura tile lives on a linked companion. The team-only ownership hole in the friendly lane is also closed: the existing WZ-backed `SupportsTeamMembers` wording now reaches the runtime because remote support areas no longer hardcode `ownerIsSameTeamMember = false`, and the affected-area owner check now resolves shared-team membership from the battlefield team owner plus the Monster Carnival team seam before the local support, avatar-effect, and invincibility seams decide whether the local player is a valid recipient. Monster Carnival ownership is narrower now too: once the carnival member-state seam has already resolved a named member onto a team, hostile or friendly affected-area ownership can keep that `ownerId -> team` result in the existing remote-area runtime instead of depending on a currently live remote actor shell every tick, and the owner-name seam now keeps an `ownerId -> name` fallback cache so later team lookups can still replay against already-known Carnival member-state names after the live remote actor shell drops. In that same ownership seam, Monster Carnival owner-team resolution now prefers fresh live owner-name team resolution before falling back to cached owner-team snapshots, and cache refresh no longer bails out just because a stale owner-team entry already exists, so live team transitions can update the runtime ownership cache before later remote-actor teardown. Battlefield ownership now uses the same cached-runtime seam: when Battlefield has already resolved an owner team (or the live remote actor still carries `BattlefieldTeamId`), the affected-area runtime caches `ownerId -> team` and reuses it when later hostile/friendly owner checks run without a live actor shell. This pass narrows owner/team replay drift again in the same seam by introducing per-area owner runtime snapshots keyed by packet affected-area object id: each active area now refreshes and stores owner name, Battlefield owner team, and Monster Carnival owner team from live runtime when available, then replays those area-local snapshots for hostile/friendly ownership checks after remote actor teardown or stale owner-id cache contention, with lifecycle pruning on remove/clear/expire. Ownership replay in that same seam now prefers live runtime owners first, then area-object snapshots, then broad owner-id caches for owner name, Battlefield owner team, and Monster Carnival owner team so stale owner-id cache entries no longer override still-active area-local snapshots when runtime owners are temporarily unavailable. The `type=3` fog lane is narrower now too: when no reachable WZ `time` metadata, linked path duration, or duration-bearing item description exists, the area-buff-item resolver now falls back to authored fog timing instead of leaving every such packet-owned fog entirely removal-packet-owned, using the same WZ-authored frame delays already loaded for the visible fog surface; when that fallback reaches a single-frame fog, the resolver now still consumes the authored frame delay as a finite lifetime. Linked damage-sharing fields now recover reverse WZ `affectedSkill` parents such as `Skill/3211.img/skill/32110000 -> 32101002`, so packet streams that surface the linked aura skill can still project the authored `partyDamageSharing` owner-share metadata before the existing remote-owner damage recipient seam runs. WZ-authored `affectedSkillEffect=amplifyDamage` now also feeds the inferred hostile mob-status resolver and the earlier hostile gameplay/disposition admission gate instead of depending on `mes` metadata, damage fields, or broad hostile text alone, so affected-area wrappers such as `Skill/522.img/skill/5220011` can replay their authored marked-target bonus-damage status through the existing packet-owned hostile area seam even when the wrapper is otherwise a sparse self/party-shaped shell. That same WZ-authored mark now stays a pure hostile player debuff in the enemy-local lane instead of being forced through the simulator's periodic-status damage path just because the player mob-status controller also uses `PainMark` for mob-skill `134` ticks. PvP/team fields now also consume WZ `PVPcommon` area metadata in the same runtime seam: when Battlefield or Monster Carnival ownership is active, hostile tick metadata, fallback damage, support projection, and staged player-area lifetime all prefer authored `PVPcommon` values such as `Skill/312.img/skill/3120010/PVPcommon/{dot,dotInterval,dotTime}`, `Skill/3510.img/skill/35101009/PVPcommon/{dot,dotInterval,dotTime}`, `Skill/3510.img/skill/35100008/PVPcommon/{w,x,y}`, and `Skill/522.img/skill/5220011/PVPcommon/damage` before falling back to regular `common`, and hostile remote player zones no longer stay mob-only in those fields because enemy-owned packet areas can now apply recurring local-player damage plus WZ-inferred player status effects through the existing incoming-damage and player mob-status seams when the local avatar is standing inside the zone. Friendly support projection now consumes the same WZ alias-mapping seam as the rest of the support resolver instead of only raw positive stat merges, so aura-style packet areas such as `Skill/3210.img/skill/32101003` can carry authored `common/x = movement speed` plus `common/y = attack speed step` into the synthetic external-buff lane and the avatar-effect helper without dropping the negative booster-style `y` metadata on the floor before the local-player support runtime sees it. This pass also keeps generic WZ `u`, `v`, and `w` support metadata projectable in that same merge seam, and Body Boost-style aura aliases now consume their WZ-authored split instead of only carrying raw generic fields: `Skill/3211.img/skill/32111005/common/v` feeds Dark Aura outgoing damage, `w` feeds Yellow Aura speed, `x` remains the attack-speed step, and `y` feeds Yellow Aura avoidability before the synthetic external-buff lane and outgoing-damage resolver inspect the projected support data. Projected support buffs now also consume WZ-authored `criticaldamageMin` / `criticaldamageMax` in the local critical-hit damage resolver instead of only surfacing those values as carried tray metadata; the runtime keeps the strongest active projected critical-damage bonus and drops it when the area-owned support buff expires. This pass also keeps linked support metadata token unions in the same synthetic external-buff seam: `SkillManager` now deduplicates and merges `affectedSkillEffect` / `minionAbility` tokens across visible plus linked support skills instead of keeping only the first non-empty token, so linked wrappers no longer drop authored support metadata such as `partyDamageSharing` (`Skill/3211.img/skill/32110000/info/affectedSkillEffect`) or `amplifyDamage` (`Skill/522.img/skill/5220011/info/affectedSkillEffect`) when companion skills contribute additional runtime tags. The enemy-local debuff projection now keeps WZ-authored status chance ownership inside each inferred status instead of rolling the whole skill once before status resolution and then rolling the status again, so branches such as `affectedSkillEffect=bodyAttack&&stun` consume their authored `subProp` as the stun chance without an extra outer `prop` gate while DOT and `amplifyDamage` marks continue to use their primary `prop` status metadata. Focused coverage for this seam now includes `UnitTest_MapSimulator/LocalAttackOwnedAffectedAreaParityTests.cs`, `UnitTest_MapSimulator/RemoteAffectedAreaOwnershipCacheParityTests.cs`, and `UnitTest_MapSimulator/RemoteAffectedAreaSupportMetadataParityTests.cs`, including hostile companion level fallback coverage for sparse packet-owned runtime level payloads (`runtime level -> resolved primary level -> 1`) and generic `u` / `v` / `w` support metadata classification for party-owned remote support areas. Remaining gap: hostile remote player-zone ownership is still simulator-side approximation rather than a full client-authored reconstruction; this pass also carries a per-area resolved enemy-ownership snapshot in the same PvP owner seam (resolved team first, then area-local enemy snapshot, then hostile-metadata fallback), so areas that already resolved as friendly/enemy no longer flip local hostile targeting just because owner runtime reconstruction drops temporarily. Create-time owner snapshots, per-area owner runtime snapshots, and existing Battlefield/Monster Carnival owner caches still survive common remote-actor teardown paths, and the owner-team seam still refreshes live Monster Carnival owner-name -> team resolution then replays area-local snapshots before owner-id caches, but unrecovered client-only owner/team transition writers still remain outside the current seams, and hostile debuff targeting remains narrower but still not byte-accurate for the same reason. The enemy-local lane now gates self/party-targeted wrappers behind explicit hostile status metadata before local-player status projection, in addition to the existing hostile-runtime and fallback-hostile filters, but unresolved owner-to-team reconstruction and broader client-only debuff-owner state remain outside the current recovered seams. The enemy-local lane also now keeps its own per-area hostile tick gate, so local-player recurring damage/status replay no longer has to share the exact same `NextGameplayTickTime` lane as mob-side inferred-status replay for that area. This pass narrows owner-cache drift again in the same seam: per-area owner snapshots now stay sticky unless live runtime owners refresh them, so broad `ownerId` fallback caches no longer overwrite an already-active area's owner-name/team snapshot while runtime owners are unavailable. Active-area snapshot pruning in the same runtime now tracks all active packet-owned area object ids every update (instead of piggybacking on the local hostile tick dictionary), so active areas no longer lose area-local owner-name/team snapshots mid-lifetime just because no local-player hostile tick gate is armed for that object. Packet create/object-id reuse in that same lane now also starts a fresh per-area lifetime by clearing prior object-scoped owner snapshots and local hostile tick-gate state before re-caching the new create owner context. This pass closes the remaining branch hole in that same create seam: successful area-buff-item creates now run the same object-lifetime reset and area-local owner snapshot re-cache path as player and mob area creates, so recycled object ids no longer keep stale per-object owner/tick state when packet streams swap between mist/support families. Owner-cache pruning in this seam is also tighter now because active owner-id tracking is always rebuilt from live active areas every update, so owner-name/team caches refreshed during that update no longer get dropped only because the frame started from an empty owner-cache set. This pass narrows hostile debuff targeting again in the same seam: enemy-local status projection now requires explicit hostile status ownership when a wrapper is self- or party-targeted, so support-shaped self/party shells no longer leak inferred hostile statuses into the local-player lane from broad token/element matches alone, while explicit hostile wrappers such as WZ `info/affectedSkillEffect=bodyAttack&&stun`, `dot`, and `amplifyDamage` still project through the packet-owned hostile area runtime. This pass narrows unresolved-owner fallback drift in the same seam: when hostile metadata and explicit friendly metadata coexist on the same packet-owned area runtime, enemy inference no longer auto-promotes that unresolved owner to enemy solely from hostile metadata; resolved owner-team and area-local enemy snapshots still take precedence, and hostile-only metadata fallback still applies when no friendly ownership metadata is present. This pass also closes the generic support-metadata classification hole in the same friendly projection seam: WZ-authored raw `u`, `v`, and `w` support payloads now count as positive support metadata for party/team-owned remote support areas, so wrappers that carry only linked generic support numerics no longer stay neutral before the existing projected-buff merge can preserve those values. This pass also consumes projected WZ `ignoreMobpdpR` / `ignoreMobDamR` support metadata in the local mob-damage seam: active remote support buffs now pass their strongest ignore-defense rate into `MobAI.CalculateIncomingDamage`, where it reduces positive mob physical/magical guard reductions (`PDamage`, `PGuardUp`, `MDamage`, `MGuardUp`, `HardSkin`) without bypassing explicit mob immunity. This pass also aligns the friendly support classifier with the existing projected-buff merger for WZ enhanced/indie support fields: `epad`, `emad`, `epdd`, `emdd`, `emhp`, `emmp`, `indieMhp`, and `indieMmp` now count as positive remote support metadata instead of being preserved only after another support token already admitted the area; WZ evidence includes Mechanic equipment metadata such as `Skill/3500.img/skill/35001002/common/{epad,epdd,emdd,emhp,emmp}`. Focused coverage now also pins projected reward-rate metadata (`expR` / `dropR` / `mesoR`) through the existing mob reward helpers and enhanced support metadata classification in `UnitTest_MapSimulator/RemoteAffectedAreaSupportMetadataParityTests.cs`. Existing limits remain: the lane is still constrained to recurring damage plus WZ-inferred status/debuff projection in Battlefield / Monster Carnival contexts, still lacks byte-accurate client debuff-owner reconstruction, support branches that still need bespoke simulator mechanics beyond the current projected buff plus linked-token projection (and any remaining bespoke support behavior outside the existing cleanse, reward-rate, resistance, damage-reduction, damage-to-MP, outgoing-damage, mastery, boss-damage, ignore-defense, and enhanced/indie support consumers) still stop short of full gameplay, wrappers that expose no hostile or visual metadata across the visible node/`affectedSkill`/`dummyOf`/reverse-parent chain can still under-classify or stay packet-removal-owned for visuals, and unrecovered client-owned owner/team transition writers still keep hostile remote player-zone ownership and debuff targeting approximate instead of byte-accurate. | Packet-owned remote affected areas now preserve more of the client-authored hostile mist lane instead of degrading DOT-backed area wrappers into rough generic damage ticks. WZ-backed hostile areas with authored DOT payloads now carry their own per-skill magnitude, tick cadence, lifetime, linked visual surface, PvP/team-field `PVPcommon` overrides, and enemy-local status projection into the existing inferred mob-status runtime, player mob-status runtime, and affected-area staging seam, while the already-shipped support projection, mist resistance, linked support ownership, linked hostile-branch replay, mixed Battle Mage aura behavior, the battlefield / Monster Carnival team-target ownership checks, the cached Monster Carnival owner-id team carry once the carnival member-state seam has already named that owner, the `dummyOf` wrapper traversal, reverse `affectedSkill` parent recovery for linked damage-sharing auras, the enemy-local-player recurring-damage and status-projection lane for team fields, the WZ alias-mapped aura support seam for visible Battle Mage support areas such as `32101003`, the Body Boost `v` / `w` / `x` / `y` projected-support alias mapping for `32111005`, the projected support critical-damage resolver for WZ `criticaldamageMin` / `criticaldamageMax`, the projected ignore-defense consumer for WZ `ignoreMobpdpR` / `ignoreMobDamR` against mob guard reductions, the enhanced/indie support metadata classifier for WZ fields such as `Skill/3500.img/skill/35001002/common/{epad,epdd,emdd,emhp,emmp}`, the reward-rate consumer coverage for WZ `expR` / `dropR` / `mesoR`, the linked support metadata-token union fix in `SkillManager` (`affectedSkillEffect` / `minionAbility` dedupe and merge), the hostile `amplifyDamage` mark fix for local player projection, the per-status `prop` / `subProp` enemy-local debuff chance fix, the sticky per-area owner-snapshot refresh rule (live runtime first, no broad-cache overwrite of active area snapshots), the packet-create object-id lifecycle reset for area-local snapshots and hostile tick gating, and the fog-cycle lifetime fallback all stay in the same seams. This keeps remote areas such as Fire/Poison `3120010`, Mechanic `35101009`, wrapper skills such as `35100008`, reverse-parent auras such as `32101002`, WZ-authored team-targeted support zones, PvP-authored aura numerics such as `32110000/PVPcommon`, Yellow Aura `32101003`, marked-target wrappers such as `5220011`, enhanced Mechanic support metadata such as `35001002`, and sparse packet-owned `type=3` fogs closer to authored behavior without introducing a separate area-combat system. | field affected-area/runtime layer (`CAffectedAreaPool::OnPacket`, `CAffectedAreaPool::OnAffectedAreaCreated`, `CAffectedAreaPool::OnAffectedAreaRemoved`), `AffectedAreaPool.cs`, `AreaBuffItemMetadataResolver.cs`, `MapSimulator.RemoteAffectedAreaPackets.cs`, `RemoteAffectedAreaSupportResolver.cs`, `PlayerMobStatusController.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `MonsterCarnivalField.cs`, `UnitTest_MapSimulator/LocalAttackOwnedAffectedAreaParityTests.cs`, `UnitTest_MapSimulator/RemoteAffectedAreaOwnershipCacheParityTests.cs`, `UnitTest_MapSimulator/RemoteAffectedAreaSupportMetadataParityTests.cs`, WZ `Skill/312.img/skill/3120010/common/{dot,dotInterval,dotTime}`, WZ `Skill/312.img/skill/3120010/PVPcommon/{dot,dotInterval,dotTime}`, WZ `Skill/3510.img/skill/35101009/common/{dot,dotInterval,dotTime}`, WZ `Skill/3510.img/skill/35101009/PVPcommon/{dot,dotInterval,dotTime}`, WZ `Skill/3510.img/skill/35101010/common/damage`, WZ `Skill/3510.img/skill/35100008/info/dummyOf`, WZ `Skill/3510.img/skill/35100008/PVPcommon/{w,x,y}`, WZ `Skill/3211.img/skill/32110000/info/{affectedSkill,affectedSkillEffect}`, WZ `Skill/3210.img/skill/32101002`, WZ `Skill/3210.img/skill/32101003/common/{x,y}`, WZ `String/Skill.img/32101003/{desc,h,pdesc}`, WZ `Skill/522.img/skill/5220011/info/affectedSkillEffect`, WZ `Skill/522.img/skill/5220011/PVPcommon/damage`, WZ `Skill/3211.img/skill/32111010/common/{prop,subProp,time}`, WZ `Skill/3211.img/skill/32111005/common/{v,w,x,y,z}`, WZ `String/Skill.img/32111005/{desc,h}`, WZ `Item/Cash/0501/05010079/effect/darkFog/*/delay`, battlefield team ownership in `BattlefieldField`, Monster Carnival team ownership in `MonsterCarnivalField`, and client packet entry points `CAffectedAreaPool::OnPacket`, `CAffectedAreaPool::OnAffectedAreaCreated`, `CAffectedAreaPool::OnAffectedAreaRemoved` |
| Partial | Field-specific restrictions and effects | This row still covers the broad field-rule parity slice already landed in the simulator: entry notices for WZ field metadata and key `fieldLimit` bits, ambient rain/snow startup, `timeLimit`, `decHP`, `protectItem`, positive `recovery`, positive `moveLimit`, `allowedItem`, teleport-item bans, minimap auto-expand, movement/migration locks, map-transfer admission checks, mini-game/parcel/quest-alert/taming-mob/android/party-boss/drop/fall-damage restrictions, WZ-backed item-family bans, WZ-backed equipment takeoff locks, WZ-backed local damage-check suppression, WZ-backed upgrade-scroll disabling, WZ-backed monster-regeneration suppression, and the shared local item/meso drop evaluator plus WZ-backed numeric prompt shell. The shared `FieldInteractionRestrictionEvaluator` remains the owner for local and packet-owned window admission paths, quest timer/admission windows, social-room and cash-trading gates, map-transfer current-field and registration gates, shop/storage/chair metadata gates, follow-request gating, channel-change metadata gating, android and dragon companion gates, active skill-cancel metadata gating, WZ `blockTakeOffItem` metadata gating, WZ `damageCheckFree` falling-damage gating, WZ `scrollDisable` upgrade-scroll item-use gating, WZ `noRegenMap` mob-regeneration gating, and packet-aware field-entry/map-transfer admission context. This pass keeps the previous WZ `noLanding` split and narrows the same Orbis flying-map metadata family again: authored `Map/*/info/noCancelSkill` now routes through `FieldInteractionRestrictionEvaluator.GetActiveSkillCancelRestrictionMessage`, is included in entry-status notices, and remains the single source used by `FieldSkillRestrictionEvaluator` for client-cancel admission. WZ evidence from the active v95 export shows `noLanding = 1` and `noCancelSkill = 1` on the Orbis flying-map family such as `Map/Map2/200110000` through `200110071`, with `Map/Map2/200110000/info` also carrying `fly = 1`, `needSkillForFly = 0`, `fieldType = 6`, and `fieldLimit = 107390207`, so both are WZ-authored field metadata rules rather than ad hoc simulator state. The already-landed `noLanding` runtime behavior remains intact: map setup applies it to the local player physics state, spawn/respawn and teleport snapping stop forcing the player onto footholds under that flag, ladder-top exits do not land onto footholds, and regular falling landing checks short-circuit through the same `NoLandingMap` state. This pass narrows two more WZ-authored field metadata families: active v95 `Map/Map7/749040001.img/info` carries `protectItem = 1472081` and `blockTakeOffItem = 1472081` alongside `fieldLimit = 393310`, so `FieldInteractionRestrictionEvaluator.GetTakeOffItemRestrictionMessage` now resolves that exact item-id lock, entry-status notices publish the active equipment lock, and character equipment move/unequip requests route through the same restriction owner while inventory-to-character equip-in remains allowed; active v95 also carries `damageCheckFree = 1` on maps such as `Map/Map2/211042300.img`, `Map/Map7/749050500.img` through `749050502.img`, `Map/Map8/800020110.img`, and `Map/Map9/970020001.img` through `970020005.img`, so local falling-damage admission now uses the shared `FieldInteractionRestrictionEvaluator.CanTakeFallingDamage(fieldLimit, mapInfo)` overload and entry-status notices publish map-authored local damage-check suppression after fieldLimit precedence. This pass also narrows the existing WZ `scrollDisable` metadata family: active v95 carries `Map/Map2/221023300/info/scrollDisable = 1` alongside `fieldLimit = 73836`, while sibling matches such as `Map/Map2/211042300/info/scrollDisable = 0` and `Map/Map8/800020110/info/scrollDisable = 0` remain explicit non-blocking authored rows, so local inventory item-use requests and `FieldRuleRuntime` item admission now route upgrade-scroll bans through the same `FieldInteractionRestrictionEvaluator.GetScrollUseRestrictionMessage` owner instead of bypassing the map-info overload. This pass narrows the WZ `noRegenMap` metadata family: active v95 carries `Map/Map6/610030400.img/info/noRegenMap = 1`, while related authored rows such as `Map/Map2/211042300.img/info/noRegenMap = 0`, `Map/Map7/749050500.img/info/noRegenMap = 0`, and `Map/Map8/800020110.img/info/noRegenMap = 0` remain explicit non-blocking rows, so entry-status notices publish monster-regeneration suppression and `MobPool.ConfigureSpawnModel` now disables simulator respawn cadence through the same `FieldInteractionRestrictionEvaluator.CanRegenerateMobs` owner. Wedding invitations remain locked to explicit WZ-backed families only (`Item/Etc/0421.img`, `Item/Cash/0509.img/05090100`, `Item/Cash/0525.img/05251000..05251003`, `Item/Cash/0525.img/05251100`, and String `Etc.img/4031377`, `4031395`, `4031406`, `4031407`) with no phrase fallback; successful local meso drops still mirror `CWvsContext::SendDropMoneyRequest` (`0x9f6650`) through opcode `106` plus `Encode4(update_time)` and `Encode4(nAmount)` and shared utility-request stamping; the discard-result seam remains in local drop/utility bridge ownership with packet-owned `OnInventoryOperation(28)` finalize/rollback/fallback paths. This pass also narrows WZ stand-alone field metadata: active v95 `Map/Map2/270000200.img/info` carries `standAlone = 1` and `partyStandAlone = 0`, while sibling rows such as `Map/Map1/150000000.img/info/standAlone = 0` remain explicit non-blocking authored rows, so authored `standAlone` now routes through `FieldInteractionRestrictionEvaluator.GetStandAloneModeEntryMessage`, publishes entry-status notices, and applies the existing `GameStateManager.SetStandAloneMode(true)` owner during field setup. Authored `partyStandAlone` is now surfaced through the same entry-notice owner but does not yet synthesize local stand-alone control without party/session context. This pass also narrows the packet-owned numeric prompt shell in the existing `PacketScriptMessageRuntime` owner: AskNumber (`messageType = 4`) already decoded the WZ-backed `UtilDlgEx`-style 10-digit client input limit, and now the active prompt context carries decoded min/max bounds into response construction so out-of-range local submissions are rejected before opcode `65` script-answer encoding while in-range values still encode as accepted byte `1` plus `Encode4(value)`. Focused regression coverage in `UnitTest_MapSimulator/FieldInteractionRestrictionParityTests.cs` now also covers `noLanding`, `noCancelSkill`, `standAlone`, `partyStandAlone`, `blockTakeOffItem`, `damageCheckFree`, `scrollDisable`, and `noRegenMap` restriction/zero-or-nonmatching/precedence behavior alongside existing window-open, map-transfer, shop/storage, chair, follow, channel, dragon, social-room, and entry-admission precedence tests; `UnitTest_MapSimulator/PacketScriptMessageProcessingParityTests.cs` now covers AskNumber decoded range rejection and accepted response packet shape. Remaining gap: generic field scripts still stop at notice/tag publication, WZ `partyStandAlone` remains notice-only until party/session ownership is recovered, broader packet/session-owned field request gates outside the current shared window-open/social-room/map-transfer/shop/storage/chair/follow/channel/dragon-companion/no-landing/no-cancel-skill/stand-alone/block-takeoff-item/damage-check-free/scroll-disable/no-regen-map plus quest-alert timer admission-and-registration/source-request seams remain missing, fall-damage formula fidelity and death-EXP remain simulator-side approximations, numeric prompt flow now gates decoded AskNumber bounds before response encoding but still does not reproduce client `CUtilDlgEx` modal/input choreography byte-for-byte, and broader android/session-owned restrictions outside current equipment/runtime seams remain open. | Remaining risk is now concentrated in deeper packet/session ownership and formula/layout fidelity, not in missing shared window-admission, quest-timer window admission, source map-transfer request routing, follow-request gating, channel-selector metadata gating, no-dragon companion request gating, WZ `noCancelSkill` skill-cancel gating, WZ `standAlone` field-control activation, WZ `blockTakeOffItem` character equipment takeoff gating, WZ `damageCheckFree` falling-damage suppression, WZ `scrollDisable` local and runtime upgrade-scroll item-use gating, WZ `noRegenMap` mob-regeneration suppression, WZ `noLanding` foothold-landing suppression, or decoded AskNumber range admission before script-answer encoding for field-restricted local versus packet-owned owners. | `MapSimulator.cs`, `MapSimulator.EquipmentWindowParity.cs`, `MapSimulator.FieldRestrictionParity.cs`, `Managers/GameStateManager.cs`, `Pools/MobPool.cs`, `Character/PlayerManager.cs`, `Character/PlayerCharacter.cs`, `Physics/CVecCtrl.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedFieldFeedbackParity.cs`, `MapSimulator.PacketOwnedQuestResultParity.cs`, `MapSimulator.PacketOwnedQuestTimerOwnerParity.cs`, `FieldInteractionRestrictionEvaluator.cs`, `FieldRuleRuntime.cs`, `FieldSkillRestrictionEvaluator.cs`, `PacketScriptMessageRuntime.cs`, `MapSimulator.CashServiceStageParity.cs`, `UnitTest_MapSimulator/FieldInteractionRestrictionParityTests.cs`, `UnitTest_MapSimulator/PacketScriptMessageProcessingParityTests.cs`, `UI/Windows/MapTransferUI.cs`, `UI/Windows/InventoryItemMetadataResolver.cs`, WZ `Map/*/info/personalShop`, WZ `Map/*/info/entrustedShop`, WZ `Map/*/info/noMapCmd`, WZ `Map/*/info/moveLimit`, WZ `Map/*/info/fieldType`, WZ `Map/*/info/limitUseShop`, WZ `Map/*/info/limitUseTrunk`, WZ `Map/*/info/noChair`, WZ `Map/*/info/noLanding`, WZ `Map/*/info/noCancelSkill`, WZ `Map/*/info/standAlone`, WZ `Map/*/info/partyStandAlone`, WZ `Map/*/info/blockTakeOffItem`, WZ `Map/*/info/damageCheckFree`, WZ `Map/*/info/scrollDisable`, WZ `Map/*/info/noRegenMap`, WZ `Map/*/info/nofollowCharacter`, WZ `Map/*/info/shiftChannelForbidden`, WZ `Map/*/info/vanishDragon`, `FieldType.FIELDTYPE_NODRAGON`, WZ `Item/Etc/0421.img`, WZ `Item/Cash/0509.img`, WZ `Item/Cash/0525.img`, String WZ `Etc.img/4031377`, `4031395`, `4031406`, `4031407`, `UI/UIWindow.img/UtilDlgEx`, `UI/UIWindow2.img/UtilDlgEx`, and client seams `CWvsContext::SendDropMoneyRequest` (`0x9f6650`), `CWvsContext::SendChangeSlotPositionRequest` (`0x9d9c10`), `CUserLocal::OnInventoryOperation` (`0x28`), `CUserLocal::OnPartyResult`, `ExpeditionIntermediary::OnPacket` (`0x522a30`), `CScriptMan::OnScriptMessage` / AskNumber script-answer opcode `65` |

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned drop-pool parity | The simulator keeps an explicit packet-owned drop runtime (`RemoteDropPacketRuntime` + `RemoteDropPacketCodec`) over the recovered `CDropPool` packet surface (`322/324`) and applies packet enter/leave directly through `DropPool`, with packet-only field-clear behavior, packet expiry clock anchoring (`/droppacket clock` + `SetField(141)` FILETIME seed), fake-to-real same-id promotion, client-shaped create-delay/alpha timing, pickup-rectangle plus `30 ms` held-pickup cadence, pool-order first-eligible admission, `pickUpBlock` gating, packet meso layered animation timing/origin handling, packet elevate-layer draw pass, packet-owned absorb/removal/explode sequencing, and packet visual center-origin insertion for normal packet item/animated-frame drawing while preserving simulator/local top-left anchoring and the layered packet meso origin path. Owner-known packet pet pickups still register the bounded three-slot synthetic pet actor alias set instead of only the slot-0 owner anchor, packet pet leave now clamps decoded secondary pet slots into the same bounded slot range before local runtime lookup or remote owner aliasing, `UserDropPickup` observed pet target positions and packet leave/user pickup party-link actor sets capture those owner-slot aliases when owner identity is known, and the observed-link owner resolver still prefers the exact decoded pet actor slot before falling back to the lowest linked owner-slot alias. This pass narrows the remaining pet-motion seam by retaining bounded predicted owner-slot pet positions from the remote owner's last live state when a remote user leaves or is removed, after pruning observed party/link state, so later packet-owned pet pickup absorb/source recovery can still anchor to the last owner-shaped pet endpoint even when the owner actor is no longer live. Party pickup arbitration remains packet-party-id first, then tracked roster/helper fallback plus observed-link fallback with encoded-pet owner normalization, observed actor-to-owner persistence, owner-alias recovery through observed-link components, and stale observed-link pruning on remote-user leave/remove/clear. Focused source-side coverage now includes `RemoteDropPacketPartyOwnershipParityTests` for bounded owner-slot alias capture, packet secondary-slot clamping, exact decoded pet-slot preference, and retained predicted owner-slot leave/remove fallback, alongside `RemoteDropPacketVisualLayerParityTests` for packet visual center-origin drawing. Remaining gap: broader arbitration is still approximate when no packet-owned party id is known and neither direct endpoints, owner identity, owner-normalized observed components, nor bounded owner-slot aliases can be tied into a known party actor set after tracked/legacy/helper/observed-link normalization; packet visual ownership still does not reproduce byte-for-byte COM-owned `GR2D::LoadLayer`/`RemoveCanvas`/`InsertCanvas` lifetime and z/page/zMass choreography; and pet-owned pickup motion can still fall back when no packet-observed state, no retained last-live owner-slot prediction, no owner-normalized observed-link component alias, and no currently live owner/pet slot can be resolved for that packet actor id. | Without an explicit `CDropPool` row, drop parity is easy to overstate once pickup text, local loot, or pet auto-loot exists. The client keeps a dedicated pool owner for enter/leave/update/pickup behavior, so tracking it separately keeps future work anchored to the real field-entity runtime instead of scattering it across UI feedback, pet logic, or generic map-object handling. This pass keeps packet visual drift narrowed by applying the recovered GR2D center-origin layer insertion contract for packet-controlled item/animated drops without changing simulator-owned local drop rendering, and narrows pet pickup absorb/source drift by retaining bounded last-live owner-slot predictions across remote owner leave/remove while keeping packet leave pet-slot aliases inside the client three-slot range. | field drop/runtime layer (`DropPool.cs`, `PacketOwnedMesoAnimationPresentation.cs`, `RemoteDropPacketCodec.cs`, `RemoteDropPacketRuntime.cs`, `MapSimulator.RemoteDropPackets.cs`, `MapSimulator.RemoteDropPacketEffects.cs`, `MapSimulator.RemoteUsers.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.PacketOwnedStageTransitionParity.cs`, `PacketStageTransitionRuntime.cs`, `RenderingManager.cs`, `UnitTest_MapSimulator/RemoteDropPacketVisualLayerParityTests.cs`, `UnitTest_MapSimulator/RemoteDropPacketPetPickupPositionParityTests.cs`, `UnitTest_MapSimulator/RemoteDropPacketPartyOwnershipParityTests.cs`, `CDropPool::OnPacket`, `CDropPool::OnDropEnterField`, `CDropPool::OnDropLeaveField`, `CDropPool::Update`, `CDropPool::TryPickUpDrop`, `CDropPool::TryPickUpDropByPet`, `CDropPool::TryPickUpDropByMob`, `CDropPool::MakeMoneyAnimation`, `CDropPool::RecordRecentPickupItem`, `CAnimationDisplayer::RegisterAbsorbItemAnimation`, `CStage::OnSetField`, `Item/Special/0900.img/09000000..09000003/iconRaw/*`) |

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive these flows from local input and generic runtime state. It also has dedicated packet-response handlers for chair sit ack or correction, same-field teleport completion, and quest-result or quest-timer application.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned quest-result parity | The simulator keeps the dedicated local `OnQuestResult` packet seam on opcode `242`, including client-handled subtype coverage (`6` through `18`), subtype `10` follow-up decode and ordering, WZ-backed quest presentation (`Quest/QuestInfo.img`, `Quest/Say.img`, `Quest/Act.img`), client-shaped notice routing (`CUtilDlg::Notice` family split), and packet-owned fade-window plus availability-refresh tails. Follow-up `CWvsContext::StartQuest(..., bAutoStart = 0)` continuation still stays request-owned until matching packet response ownership returns, with pending-start ownership recorded only when live or deferred outbound transport accepts the request and shared exclusive-request ticks stamped only on accepted dispatch or queue. This pass closes additional `CheckCompleteDemand` ownership inside the same completion-alert registration seam: completion-demand gating now includes `Check.img` `questComplete` minimum-count demand (`.../1/questComplete`), `buff` / `exceptbuff` demand families (`.../1/buff`, `.../1/exceptbuff`) through the same signed synthetic buff owner the simulator uses for consumable buffs (`-itemId`, matching client-shaped `SecondaryStat::IsSetted` checks), and Monster Book minimum-card-type demand (`.../1/mbmin`) through live Monster Book ownership snapshots, completion popularity demand (`.../1/pop`), `partyQuest_S` rank-count demand (`.../1/partyQuest_S`) through quest-record-backed rank snapshots, completion `lvmin` floor demand (`.../1/lvmin`) with completion-owned `lvmin` preferred over start-owned `lvmin`, completion `level` floor demand (`.../1/level`) through the same build-context gate used by client-shaped level/fame/job checks, `userInteract` record demand (`.../1/userInteract`) through the same quest-record owner used by `infoNumber` / `fakeQuestID`, completion trait minimum/maximum demand families (`.../1/charismaMin`, `.../1/charismaMax`, `.../1/insightMin`, `.../1/insightMax`, `.../1/willMin`, `.../1/willMax`, `.../1/craftMin`, `.../1/craftMax`, `.../1/senseMin`, `.../1/senseMax`, `.../1/charmMin`, `.../1/charmMax`, plus completion `.../1/charm`) through live character trait snapshots with unresolved build-context gating when unavailable, and `QuestInfo.img` `dailyPlayTime` success demand through the same completion-alert branch, and the recovered time-keep field-set branch (`.../1/fieldset`) by requiring a quest-ex `kept` record for the current quest while now accepting both keyed `kept=<value>` and scalar kept-duration payloads from packet-owned quest-ex writers and writing a packet-owned `kept` quest-ex/compat quest-record value from subtype `9` time-keep timer removal using the timer's elapsed kept seconds, while preserving existing quest-state, quest-record (`infoNumber` / `info` / `infoex`), mob/item, consume-item, pet, skill, availability, meso, level-floor/cap, action-level, action-availability, action-repeat-interval, action-job, and morph-template demand checks plus unresolved build-context gating when required context owners are unavailable. WZ evidence remains `Quest/QuestInfo.img` (`autoComplete`, `autoPreComplete`, `dailyPlayTime`) and `Quest/Check.img` completion-demand branches, now explicitly including `buff`, `exceptbuff`, `mbmin`, `questComplete`, completion `pop`, `partyQuest_S`, completion `lvmin`, completion `level`, `userInteract`, trait min/max keys, and `fieldset` alongside `infoNumber`, `info`, `infoex`, and `morph`; the mounted WZ evidence includes `Quest/Check.img/23127/1/fieldset = Resi_23127`, `Quest/Check.img/4456/1/userInteract = 2`, `Quest/Check.img/10820/1/lvmin = 70`, `Quest/Check.img/2363/1/lvmin = 20` while `Quest/Check.img/2363/0/lvmin = 19`, `Quest/Check.img/2260/1/level = 30`, and `Quest/Check.img/29300/1/level = 200`. Client evidence remains `CQuest::StartQuest` at `0x6B40A0`, `CQuestMan::IsAutoAlertQuest` at `0x6B6890`, `CQuestMan::IsAutoCompletionAlertQuest` at `0x6B6870`, `CWvsContext::TryRegisterAutoCompletionAlertQuest` at `0x9E0800`, and `CQuestMan::CheckCompleteDemand` at `0x6BC3D0`, where demand evaluation also covers Monster Book/card, buff/except-buff, party-quest-rank, quest-complete-count, daily-play success, user-interaction record, trait min/max, time-keep quest-ex `kept`, completion-min-level, completion-level, and completion-popularity families before completion-alert registration is admitted. Coverage now materializes in `UnitTest_MapSimulator/PacketOwnedQuestStartRequestSharedResetParityTests.cs` with completion quest-complete-count demand helper assertions, completion fame-demand helper assertions, completion `lvmin` floor helper assertions, completion-level demand and unresolved build-context helper assertions, completion buff/except-buff demand helper assertions, Monster Book completion demand helper assertions, completion `partyQuest_S` rank-demand helper assertions, completion daily-play-demand helper assertions, completion user-interaction record helper assertions, completion trait min/max demand helper assertions, and completion time-keep field-set helper assertions including scalar quest-ex kept-value resolution, packet-owned timer elapsed-kept resolution, and packet-owned quest-ex kept-record writer coverage in addition to prior follow-up request/block/reset and completion-demand helper coverage. Remaining gap: subtype `10` continuation still runs on the simulator pumped modal host rather than native blocking `CUtilDlgEx` + `CDialog::DoModal`; follow-up request ownership still does not model every external writer/clearer of global `CWvsContext::SetExclRequestSent` byte-for-byte outside currently decoded inventory, transfer/portal response, collision-script transfer-lifecycle teardown, random-morph result/ack, item-upgrade result, and Vega result/request clearers; and auto-completion alert ownership is narrowed again but still does not fully reproduce byte-identical native `CQuestMan::CheckCompleteDemand` across unrecovered side-channel demand-state writers/mutations outside currently decoded completion-demand providers, including native time-keep quest-ex lifecycle branches beyond subtype `9` elapsed-kept removal plus the recovered keyed/scalar `kept` gate and other packet/client writers for currently unresolved demand owners. | Quest parity here depends on packet owner boundaries, not only quest window helpers: `CUserLocal::OnQuestResult` owns timer and result decode and continuation ordering, then drives both visible quest-result UI and the follow-up `StartQuest` request owner path before availability refresh. Keeping opcode `242` handling, subtype `10` continuation, and opcode `119` follow-up dispatch in one seam prevents regressions back into simulator-only quest-window flow. | packet-owned local quest-result layer (`CUserLocal::OnPacket`, `CUserLocal::OnQuestResult`, `CWvsContext::AddQuestTimer`, `CWvsContext::RemoveQuestTimer`, `CWvsContext::CheckNewQuestAvailable`, `CWvsContext::StartQuest`, `CWvsContext::DeleteFadeWnd`, `CQuest::StartQuest`, `CQuestMan::IsAutoAlertQuest`, `CUtilDlgEx`, `CDialog::DoModal`, `Quest/QuestInfo.img`, `Quest/Check.img`, `Quest/Say.img`, `Quest/Act.img`, `UI/UIWindow.img/UtilDlgEx`, `UI/UIWindow2.img/UtilDlgEx`, `MapSimulator.PacketOwnedQuestResultParity.cs`, `MapSimulator.cs`, `MapSimulator.UpdateLoop.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultClientSemantics.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultUtilDialogLayout.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultUtilDialogModalHostRuntime.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultFadeWindowRuntime.cs`, `HaCreator/MapSimulator/Interaction/PacketOwnedQuestStartRequest.cs`, `HaCreator/MapSimulator/Interaction/QuestRuntimeManager.cs`, `UnitTest_MapSimulator/PacketOwnedQuestResultClientSemanticsTests.cs`, `UnitTest_MapSimulator/PacketOwnedQuestStartRequestSharedResetParityTests.cs`) |
| Implemented | Packet-owned chair sit-result parity | The simulator now has a dedicated local `OnSitResult` packet seam on opcode `231` instead of treating chairs as local-only intent. The packet-owned handler stamps the shared local utility request tick, mirrors the client-owned `CWvsContext` chair bookkeeping in the packet-owned local-utility context by clearing the chair exclusive-request flag at `+0x20B8` and restamping the exclusive-request tick at `+0x20BC`, decodes the client-shaped success flag plus seat index payload, resolves the current map's WZ `seat/<index>` vector, validates the local player against the same client chair rectangle (`x +/- 10`, `y +/- 30`), and routes successful placement through a packet-owned local sit placement helper that also raises a dedicated secure sit confirmation flag on the local player. Successful packet-owned placement now clears any attached local passenger linkage through the existing follow-runtime and remote-user seams before the sit is finalized, matching the client path that zeroes `m_dwPassenserID` and the passenger's driver link. Invalid returned seats or rectangle mismatches now force the same stand-up correction path and send that correction through the shared local-utility context owner instead of a raw timestamp: the simulator still records a client-shaped `CWvsContext::SendGetUpFromChairRequest(0)` outpacket with opcode `45` and payload `FFFF`, re-arms the exclusive-request flag after emission, suppresses the send when the local HP gate or exclusive gate would block it, and injects that raw opcode `45` packet through a configured `LocalUtilityOfficialSessionBridgeManager` owner when a live Maple socket is attached. That bridge seam is broader now too: `/localutility session start ...` and `/localutility session startauto ...` no longer behave as one-shot manual helpers only. They persist their direct-proxy or discovery configuration onto the packet-owned local-utility owner, the simulator now keeps that bridge armed from the main update loop alongside the inbox, auto-refreshes discovery while no Maple client is attached, preserves an already attached live session instead of tearing it down during discovery refresh, reports outbound chair corrections in bridge status with pending and queued counts plus the last injected and last deferred raw packets, and now keeps a built-in deferred outbound queue for chair correction packets whenever the official-session bridge is enabled but no Maple session has finished attaching yet. Those deferred packets survive bridge re-arming and discovery refresh, then flush through the live Maple socket as soon as the proxied session completes the init handshake, so packet-owned chair corrections no longer depend on an external loopback consumer to reach the official-session transport path. The no-live-session fallback still remains the dedicated `LocalUtilityPacketTransportManager` loopback outbox on `/localutility outbox [status|start [port]|stop]`, which emits raw outbound corrections as `packetoutraw` lines containing the decrypted opcode-framed Maple packet, records the last emitted raw chair correction on that outbox owner, and lets packet-owned chair corrections leave the simulator whenever no live Maple bridge is attached but an external loopback consumer is listening. That outbox owner still keeps its own deferred packet-owned queue for chair correction packets when no outbox client is currently attached, flushes queued packets as soon as a loopback client connects (or before the next immediate send when a client is already attached), and reports both sent and pending correction counts in local-utility status so these packet-owned corrections no longer collapse into simulator-only state immediately when transport is temporarily unavailable. Packet rejection still clears the sit state without emitting that correction request, and the rejection teardown now also clears any stale external-owner couple-chair overlay when an active portable chair is unwound through the same local stand-correction path, matching the client-owned `Update(0)` cleanup branch more closely than the earlier partial teardown.g.cs`, `MultiBoard.g.cs`, and `HaCreator_Content.g.cs` missing under `HaCreator/obj/Debug/net10.0-windows`). | Chairs are not only a local toggle or render-state change. The client keeps a packet-owned success/failure and seat-validation branch that decides whether the user really sits, whether a passenger link is broken, and when to force a stand-up correction. Tracking this row against the dedicated opcode `231` seam keeps the remaining work anchored to the real local response owner instead of only local chair commands or remote chair packets. | packet-owned local chair-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnSitResult`, `CWvsContext::SendGetUpFromChairRequest`, WZ `Map/Map*/<map>.img/seat`, packet-owned local sit placement, correction path toward `PacketOwnedLocalUtilityContextState`, `LocalUtilityOfficialSessionBridgeManager`, `LocalUtilityPacketTransportManager`, `UnitTest_MapSimulator/LocalUtilityOfficialSessionBridgeManagerTests.cs`) |
| Implemented | Packet-owned teleport-result parity | Standard same-map portal travel still finishes through the packet-owned local `OnTeleport` seam: visible and hidden portals targeting the current field use the WZ-authored portal `delay` when present, otherwise the client-confirmed 120 ms handoff, stage a pending same-map teleport against the destination portal index, and let the dedicated apply helper own the final landing and `TryRegisterTeleport(..., bForced = 1)` side effects instead of writing position directly from the interact path. Temporary same-map completion stays in that seam too, carrying any resolved real source `pn`, forwarding same-map target `pn` metadata, and using relaxed current-field `CPortalList` coordinate matching so near-equal authored landings can still collapse to a real portal index. Cross-map portal travel remains packet-owned as well: pending teleport targets survive `UnloadMapContent`, reload the new field, try the rebuilt `PortalPool` first by the primary returned `pn`, then by preserved fallback candidate names, and only then fall back to exact-coordinate apply. This pass tightens that post-load collapse order in the existing seam so rebuilt-field completion no longer promotes an arbitrary returned portal index from whole-pool coordinate drift after the named and candidate-based collapse paths fail; ambiguous rebuilt candidate sets and destinations that no longer correspond to any rebuilt `CPortalList` entry now stay coordinate-owned through the packet apply seam instead of stealing a named handoff from whichever live portal happens to sit nearest the stored landing point, and the rebuilt-field seam no longer synthesizes a fresh named handoff from source-side reciprocal or map-only metadata once those preserved target-side collapse paths have already missed. The preserved primary returned portal name is still authoritative again once the rebuilt `PortalPool` can resolve it, even when the stored landing coordinates would have drifted toward another live portal and previously could have stolen the handoff. Preserved candidate destination names still no longer require the raw candidate strings to be unique up front, because after field load the simulator replays those names against the rebuilt `PortalPool`, promotes them into a true returned portal handoff whenever stale candidates drop out to one live portal index, and still replays the preserved landing coordinates against the still-live candidate subset so a rebuilt field can recover one real returned portal index even when multiple preserved destination names survive reload but only one of those live candidates still matches the stored landing point. Standard visible or hidden cross-map portals still preserve WZ-backed reciprocal return-link fallback (`target.tm == currentMap` and `target.tn == source.pn`) plus unique map-only return fallback from the synthetic opcode `113` request owner, and temporary cross-map travel still mirrors that metadata preservation while recovering real current-field source `pn` values through relaxed coordinate matching before target-side metadata recovery. Source-side collapse is tighter in that same packet-owned seam now too: synthetic opcode `113` request building and temporary-portal recovery still start from the full best-distance current-field source `pn` set derived from portal `x`/`y`/`pn`/`tn`/`tm` metadata, but the helper no longer treats each target-side clue as a separate all-or-nothing check. It now keeps that coordinate-authored source candidate set alive, narrows it cumulatively with the explicit target `tn` when present, then with the preserved target candidate names when those survive, and only then replays each surviving current-field source candidate back through the authored target-field portal metadata surface (`tn`, reciprocal `pn`, unique map-only return, and target-side coordinate candidates) before falling back to the older target-only inference or exact-coordinate completion. That closes another packet-owned source-side miss where current-field coordinates plus the preserved target candidate set still left multiple source `pn` values alive until the simulator asked which candidate's own target-field metadata actually matched the resolved landing side; temporary and synthetic recovery now keep the packet-owned named handoff in that case instead of dropping straight to coordinate-owned completion. The same request-building seam is tighter again now when the source side stays genuinely ambiguous even after that replay pass: temporary and synthetic owners preserve the surviving current-field source candidate set, replay those candidates back through authored target `tn` metadata, and carry any narrowed target `pn` or candidate subset forward instead of immediately dropping to exact-coordinate-only landing metadata. Exact-coordinate completion still avoids inventing a fallback target name when the packet-owned seam only knows ambiguous candidates, and outbound opcode `113` routing now keeps the request on the packet-owned official-session seam after the live direct-injection path misses by preferring deferred `LocalUtilityOfficialSessionBridgeManager` ownership before any generic `LocalUtilityPacketTransportManager` `packetoutraw` fallback while still preserving the raw payload and dispatch summary. WZ evidence for the active seam remains the portal metadata fields in `Map/Map0/001000000.img/portal/*/{pn,tn,tm,x,y,delay}` and the general teleport effect in `Effect/BasicEff.img/Teleport`, whose authored animation remains 4 frames at 90 ms each; `Map/Map0/001000000/portal/0` still exposes the same packet-relevant portal metadata surface (`pn = sp`, `tm = 999999999`, `x = 240`, `y = 190`) on the mounted export. Direct client evidence remains `CUserLocal::OnPacket` opcode `234` dispatch into `CUserLocal::OnTeleport`, where the client decodes the success byte, looks up the returned `CPortalList` entry by portal index, writes the landing point from that portal, and then re-enters `TryRegisterTeleport(..., bForced = 1)`. Focused source coverage now on disk in `UnitTest_MapSimulator/PacketOwnedTeleportParityTests.cs` includes the cumulative source-collapse regression where current-field source coordinates, an explicit target `tn`, and preserved target candidate names only become unique when the packet-owned seam intersects all of them in order, the replayed-target-field regressions where multiple coordinate-authored current-field source candidates collapse only after matching each candidate's authored target metadata or reciprocal return link against the resolved landing side, the surviving-source-candidate target recovery regressions where temporary or synthetic request building now replays those still-ambiguous source candidates back across authored target metadata before falling through to coordinate-only completion, the outbound-owner regressions that keep deferred opcode `113` delivery on the official-session seam before the generic outbox, alongside the earlier rebuilt-field primary-name precedence path, rebuilt-field candidate collapse when only one preserved destination still resolves in the live pool, rejection when multiple preserved candidates still resolve after reload, the rebuilt-field collapse path where multiple preserved destination names still resolve live but only one of those candidates still matches the preserved landing coordinates, the new rebuilt-field rejection where only source-side reciprocal metadata still resolves after the preserved target-side collapse paths fail, and the coordinate-only fallback regressions where rebuilt cross-map completion must not promote a named portal from whole-pool coordinates alone or from a non-candidate live portal. | Teleport parity is not only about entering a portal or moving to the right coordinates. The client also has a response-time authority point that applies the actual destination by portal index and records the named teleport handoff after the packet returns. Tracking that seam keeps the remaining parity gap anchored to the real response owner instead of hiding it behind already-implemented local portal interaction flow. | packet-owned local teleport-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnTeleport`, `CPortalList`, `CUserLocal::TryRegisterTeleport`, WZ `Map/Map0/001000000.img/portal/*/{pn,tn,tm,x,y,delay}`, WZ `Effect/BasicEff.img/Teleport`, `MapSimulator.cs`, `MapSimulator.PacketOwnedTeleportParity.cs`, `UnitTest_MapSimulator/PacketOwnedTeleportParityTests.cs`, `LocalUtilityOfficialSessionBridgeManager.cs`, `LocalUtilityPacketTransportManager.cs`) |

### 4. Portal and reactor collision ownership discovered by targeted `CUserLocal` decompile

The latest `CUserLocal` pass exposed one more field-runtime owner split that the backlog was still flattening into generic portal, teleport, and reactor rows.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Portal collision ownership parity | The simulator keeps portal touch ownership in one `CUserLocal::CheckPortal_Collision`-shaped seam (`TryHandlePortalInteractCore` plus `PortalPool.FindPortalCollision`) and continues to split type `9`, `12`, `13`, and default transfer handling in that owner. Type `9` (`CollisionScript`) still dispatches opcode `112` with field key plus portal name plus local x/y, consumes and releases exclusive-request ownership through decoded `CStage::OnSetField`, inventory-operation opcode `28` reset markers, and transfer lifecycle teardown, shares the recovered `CanSendExclRequest(500,0)` transfer gate, and preserves the stronger active delay window when multiple collision-script portals overlap. Type `12` (`CollisionVerticalJump`) and type `13` (`CollisionCustomImpact`) still use the shared vec-ctrl owner for move-path attributes `24` and `25`, queue `SetImpactNext`, gate on live field and vector-control ownership, and replay `Effect/BasicEff.img/VerticalJump`; this pass keeps portal-owned payload snapshots on the packet snapshot path that omits synthetic tail append, replays `CMovePath` random-count suffix bytes (`usRandCnt/usActualRandCnt`) behind decoded `CStage::OnSetField` client option `2` ownership, applies a `CMovePath::MakeMovePath`-style tail normalization pass before encode (attribute `0/12/14` coalescing on stable foothold/fall-start/action/facing plus `xOffset`/`yOffset` and velocity-direction continuity, plus zero-duration `attr=0` placeholder tail replacement), and now also gates snapshot span through `CVecCtrl::IsTimeForFlush`-style cadence ownership so below-threshold gathers keep only the latest portal-owned element while flush-admitted gathers preserve the normalized run, including the recovered dynamic-foothold short-threshold branch (`fieldType == FIELDTYPE_DYNAMICFOOTHOLD`) on the same seam; this pass further narrows cadence drift by adding seam-owned flush re-admission ownership (`ShouldAdmitPortalOwnedMovePathFlushCadence`) so repeated portal collisions only preserve full normalized runs when the recovered short-threshold window has elapsed since the previous admitted flush, with wraparound-safe tick math matching the existing client-style `Environment.TickCount` ownership model. The same seam now also carries one post-flush portal-owned element into the next below-threshold snapshot run (`ApplyPortalOwnedPostFlushCarryHint`) and consumes that carry when the encoded shape is already present, including multi-element retained live vec-ctrl runs where the carried shape survived the previous `CMovePath::Flush` branch, then applies `CMovePath::Flush`-style live vec-ctrl retention after flush-admitted portal-owned packet snapshots (`CVecCtrl.ApplyClientFlushRetentionAfterPacketSnapshot`): short-update, flying, and dynamic-foothold portal-owned runs clear the active gathered path after encode, while the long-update branch retains only post-ground tail elements and their gathered duration. This narrows client `CMovePath` flush-to-gather drift without changing owner split or packet option ownership. This pass also moves portal-owned move-path attribute stamping onto the collision owner tick instead of a second `Environment.TickCount` sample (`CVecCtrl.SetMovePathAttribute(..., timeStampMs)`), applies the same owned tick to packet-owned forced teleport registration before its packet snapshot, and routes vec-ctrl gather-duration/snapshot/continuous-path elapsed math through a wraparound-safe client tick helper so rollover and same-frame owner jitter no longer perturb the recovered flush threshold. Type `13` delayed session-value handling still queues opcode `191` requests, decodes direct and relayed release packets (`95`, `149`, and `163` relays), strips recursive owner-hint prefixes for key matching, and keeps live-map pending replacement semantics; this pass also narrows cancellation by pruning queued pending impacts when their owner family is no longer admitted by current field runtime ownership, so stale Party Raid, Hunting Ad Balloon, and Chaos Zakum pending entries no longer survive owner-runtime transitions inside the same map. WZ evidence remains `Effect/BasicEff.img/VerticalJump` plus authored collision-custom-impact portal metadata in `Map/Map0/003000000.img/portal/1` (`pt=13`, `horizontalImpact=1500`, `verticalImpact=200`), `Map/Map9/912060200.img/portal/3` (`pt=13`, `horizontalImpact=1700`, `verticalImpact=1700`, `sessionValueKey=fire`), and `Map/Map9/970050010.img/portal/6,7` (`pt=13`, `sessionValueKey=balloon_Team`, `sessionValue=redTeam/blueTeam`, reactor names `red_Jump`/`blue_Jump`); client evidence remains `CUserLocal::CheckPortal_Collision` (`0x919a10`), `CUserLocal::VerticalJump` (`0x90f510`), `CUserLocal::OnCollisionCustomImpact` (`0x90b750`), `CWvsContext::SetExclRequestSent` (`0x49c580`), `CWvsContext::SetImpactNextBySessionValue` (`0x9e52f0`), `CWvsContext::SendRequestSessionValue` (`0x9e1a90`), `CMovePath::IsTimeForFlush` (`0x666870`), `CMovePath::MakeMovePath` (`0x667c90`), `CMovePath::Flush` (`0x668160`), and `CMovePath::Encode` (`0x666e20`). Focused regression coverage in `UnitTest_MapSimulator/PortalOwnedMovePathClientParityTests.cs` now includes first-flush admission, threshold-window re-admission blocking, tick-wraparound re-admission for the portal-owned cadence gate, owner-tick move-path attribute stamping, wraparound owner-tick attribute stamping, post-flush carry prepend on below-threshold runs, carry-consume no-duplication behavior when the carried encoded shape is already present in single-element and retained multi-element snapshots, and live vec-ctrl flush-retention consumption for short and dynamic-foothold portal-owned runs. Remaining gap: portal-owned movement emission now follows recovered flush-threshold ownership for both default and dynamic-foothold branches, enforces flush re-admission windows between successive flush-admitted portal collisions, models one-step post-flush carry, consumes carry hints once the encoded shape is already resident in the next snapshot, consumes or retains the live vec-ctrl gathered path after admitted snapshots according to the recovered `CMovePath::Flush` branch shape, and removes the known extra tick-sampling drift at portal-owned attr-stamp/gather boundaries, but it still does not reproduce byte-for-byte `CMovePath` gather/flush retention for unrecovered native carry/flush writers outside the portal/packet-owned registration seams, and `CWvsContext` exclusive-request plus pending-session ownership remains modeled through simulator mirrors instead of full client memory-field replication across all unrecovered writers. | Portal parity is now owned by one collision seam where admission, request dispatch, delayed impact release, and transfer handoff are decided before later packet or map-transfer handlers run. Keeping type-specific logic and owner cleanup here reduces parity regressions when stage-transition, wrapper relay, and field-runtime ownership overlap in the same frame. | portal collision field-runtime layer (`CUserLocal::CheckPortal_Collision`, `PortalPool.FindPortalCollision`, `MapSimulator.TryHandlePortalInteractCore`, `MapSimulator.CanSendCollisionScriptPortalRequest`, `MapSimulator.TryDispatchCollisionScriptPortalRequest`, `MapSimulator.TryHandleVerticalJumpPortal`, `MapSimulator.TryHandleCustomImpactPortal`, `MapSimulator.PacketOwnedFieldStateParity` pending-impact queue and prune seam, `MapSimulator.PacketOwnedUtilityParity` inventory-operation reset decode, `MapSimulator.PacketOwnedStageTransitionParity`, `MapSimulator.PacketOwnedTeleportParity`, `CVecCtrl.SetMovePathAttribute`, `CVecCtrl.GetMovePathPacketSnapshot`, `CVecCtrl.IsTimeForFlush`, `CVecCtrl.ApplyClientFlushRetentionAfterPacketSnapshot`, `CMovePathClientPacketCodec`, `UnitTest_MapSimulator/PortalOwnedMovePathClientParityTests.cs`) |
| Partial | Reactor collision polling parity | The simulator keeps reactor touch ownership in one explicit once-per-second `CUserLocal::CheckReactor_Collision`-style poll in the main update loop: it tracks the last poll tick, refreshes a persistent local touched-reactor set through `ReactorPool`, and applies the recovered client contact rule of testing the live local-user vec position against each touch reactor's current bounds rather than a synthetic avatar rectangle. The packet transport split remains in the same seam: enter/leave diffs emit opcode `250` (`Encode4(dwID)` + `Encode1(touchFlag)`) through a live reactor official-session bridge when attached, otherwise defer through that bridge when armed without a connected Maple session, and otherwise send/queue the same raw opcode `250` through the dedicated reactor-touch `packetoutraw` outbox. Existing ownership behavior also remains: the main update loop keeps the reactor official-session bridge armed, refreshes discovery, and drains inbound packet-owned reactor-pool packets; packet `EnterField` can still adopt a real packet object id onto a unique still-live authored reactor (`template + x/y + flip`, with non-conflicting `name` when present) rather than always spawning a duplicate packet-owned reactor; packet leave/removal still clears touched ownership immediately and removes deferred opcode-`250` entries for removed `dwID`s; touch scans still skip `PacketLeavePending`; and late packet-id recovery for an already-touched authored reactor still re-keys and promotes touch-enter ownership in the same tick. This pass keeps collision-poll ownership ordering inside the same owner seam: local touch ownership diffs apply in live reactor iteration order only for reactors admitted by the recovered live-poll predicate, while `PacketLeavePending`, destroyed/respawning, and non-touch-capable reactors are omitted from the live poll result list and therefore clear through the stable stale-id cleanup path instead of being forced into live iteration order. Empty live bounds still preserve previous ownership rather than manufacturing a leave. Stale touched ids not seen in the poll are cleaned after the pass in stable stored live-index order (index then object id), keeping deterministic client-like leave sequencing when multiple stale ids clear together. Deferred touch transport still carries per-request source ticks and replays deferred opcode-`250` enter/leave ownership in recorded order with preserved inter-request cadence (including `Environment.TickCount` wraparound math), replay is still advanced from the main update loop through `FlushDeferredReactorTouchOwnership`, and attach/init-time `Environment.TickCount` auto-flush remains removed from both transport managers so both the official-session bridge and `packetoutraw` outbox stay on the same runtime clock owner. Overdue deferred flush still replays from the recorded due-tick cursor instead of resetting cadence to the current frame tick when flush is late, replay spacing still preserves full recorded tick deltas without the old 5000 ms clamp, and deferred queue removal still re-times the due-tick cursor when the currently scheduled head entry is pruned so remaining ownership replay cadence stays aligned with recorded source ticks. Live sends also remain behind deferred replay cadence in both transport managers: when deferred ownership replay is still pending after a flush attempt, new enter/leave requests are queued behind the pending backlog instead of being injected/sent ahead of not-yet-due deferred entries, preserving recorded ordering across reconnect windows in both the official-session bridge and the `packetoutraw` outbox. Re-arming the official-session bridge through `Start` now preserves deferred opcode-`250` touch ownership instead of clearing the pending queue, so reconnect-window replay remains owned by the main-loop `FlushDeferredReactorTouchOwnership` clock rather than by bridge lifecycle churn. This pass also keeps the previous unresolved packet-name hard-lock lift: when template/x/y/flip resolution still yields multiple authored candidates with no conflicting authored name, packet-enter adoption continues through the same touched/position/state client-signal filters and narrowed WZ-order fallback instead of bailing before discriminator evaluation. The recovered poll predicate remains narrowed to the decompiled `CReactorPool::FindTouchReactorAroundLocalUser` branch by admitting touch-capable live reactors without consulting local quest eligibility, while quest requirements remain on the separate activation/search paths. The outbound transport guard is now explicit and test-covered: local authored negative pool ids can keep simulator touch ownership, but only positive packet object ids with `UsesPacketObjectId` are eligible for opcode-`250` dispatch. The one-second poll gate remains explicit through `MapSimulator.ShouldPollReactorCollision`. Focused regression coverage now includes `UnitTest_MapSimulator/OfficialSessionBridgeConnectedSessionHarnessTests.cs` for the official-session bridge attach/init no-flush rule and re-arm queue preservation, alongside the reactor collision ownership coverage for the recovered `>= 1000 ms` threshold, tick-wraparound poll gating, client-style live-bounds point containment checks, packet-touch polling skip for `PacketLeavePending`, non-touch-candidate live-poll omission, explicit authored-local no-transport guarding, quest-independent touch-ownership poll admission for touch-capable reactors, client-ordered local touch ownership mixed enter/leave diff sequencing, orphan touched-id cleanup when poll results omit previously touched entries, deterministic stale-cleanup leave ordering by stored live index, due-tick replay resolution and delay-preservation helpers in both transport managers (including wraparound and out-of-order clamp behavior), deferred queue ordering/coalescing semantics in both managers, deferred-head removal cadence preservation in both managers when packet leave/removal prunes the currently scheduled head entry, connected-client and reconnect-window `packetoutraw` integration proof that live `TrySendTouchRequest` entries queue behind not-yet-due deferred replay and flush in recorded order, attached-session bridge integration proof for post-init and reconnect-window no-overtake ordering, and packet-enter authored-candidate selection for unresolved packet-name adoption across single-candidate client-signal, disjoint-signal narrowed WZ fallback, and fully-ambiguous WZ-order fallback paths. WZ evidence remains map `reactor/*` nodes (for example `Map/Map0/001000000.img/reactor/*`), which expose template id, x/y, reactorTime, flip, and name but no server object id; on mounted `v95`, `reactor/0` exposes `id=1002009`, `x=610`, `y=254`, `reactorTime=90`, `f=0`, and `name`, while `reactor/1` exposes `id=1002009`, `x=324`, `y=-78`, `reactorTime=100`, `f=0`, and `name`, with authored node order explicit in-map. Client evidence remains `CUserLocal::CheckReactor_Collision` at `0x903d20` (stores `m_tLastCheckReactorCollision`, gates on `update_time - last >= 1000`, then calls `CReactorPool::FindTouchReactorAroundLocalUser`), `CReactorPool::FindTouchReactorAroundLocalUser` at `0x6cded0` (iterates live reactors, computes bounds from live layer, tests local-user vec point, updates touched map, emits `COutPacket(250)` inline during iteration), and `CReactorPool::OnReactorEnterField` at `0x6cf490` (decodes `dwID/template/state/x/y/flip/name`, allocates live reactor, loads layer, inserts into live pool). Remaining gap: purely local authored reactors that never receive packet `EnterField` object ids still have no authoritative client request id to emit, and the simulator now keeps that as explicit non-transportable local ownership instead of inventing a client object id; deferred replay timing preserves ordering and spacing and keeps live sends behind pending deferred ownership, but byte-for-byte client socket scheduling jitter/subframe pacing across disconnect windows is still not reproduced; connected-client, reconnect-window no-overtake, and bridge re-arm preservation behavior is integration-covered for both `packetoutraw` and attached-session bridge transports where live `TrySendTouchRequest` ownership stays queued behind not-yet-due deferred entries and queued ownership survives bridge lifecycle re-arm; and packet-enter ambiguity is narrower but still present when otherwise-identical authored candidates remain signal-tied and fall back to authored order, because the client still exposes no stronger discriminator than exact-name (when present), current-position, current-touch, initial-state overlap, and authored order when server object-enter order diverges across otherwise identical live reactors. | Reactor parity here is an ownership/timing seam, not only authored reactor data or packet state decode. Keeping one poll owner plus packet-shaped touch transport in `MapSimulator` and `ReactorPool` prevents contact ownership from leaking into unrelated activation code paths. This pass tightens local poll ordering parity by matching client-style in-loop enter/leave emission order for admitted live-poll candidates, routing skipped candidates through deterministic stale cleanup, and preserving deferred replay ordering, cadence, and bridge re-arm queue ownership through reconnect windows in the existing seams. | reactor touch-poll layer (`CUserLocal::CheckReactor_Collision`, `CReactorPool::FindTouchReactorAroundLocalUser`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.cs`, `ReactorPool.cs`, `ReactorPoolOfficialSessionBridgeManager.cs`, `ReactorTouchPacketTransportManager.cs`, `UnitTest_MapSimulator/ReactorCollisionOwnershipParityTests.cs`, `UnitTest_MapSimulator/OfficialSessionBridgeConnectedSessionHarnessTests.cs`, WZ `Map/*/reactor/*`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
