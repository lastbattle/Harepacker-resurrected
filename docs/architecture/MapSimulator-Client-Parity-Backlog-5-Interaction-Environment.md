# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/DropPool.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Field and portal parity

- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`

Notes:
`CUserLocal::TryPassiveTransferField` shows that client field transfer is not only explicit portal collision handling; the local-player update loop also retries an up-key style transfer after one-time actions finish when the character is allowed to move. A later IDA pass also shows that packet-authored field utility surfaces are split into their own remote pools rather than hanging off local collision handling: `CAffectedAreaPool::OnPacket` owns affected-area create/remove, `CTownPortalPool::OnPacket` owns town-portal create/remove, and `COpenGatePool::OnPacket` owns Open Gate create/remove.

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`

Notes:
The backlog already covered pickup notices and pet auto-loot side effects, but the broader client owner was still unnamed. IDA shows that drop parity is owned by a dedicated `CDropPool` runtime that handles enter/leave packet dispatch, per-frame drop update, player/pet/mob pickup arbitration, meso animation, and recent-pickup history instead of treating drops as generic field objects plus UI messages.

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive chairs, same-field teleports, and quest results from local intent and generic runtime state. It also has dedicated packet-response handlers that finalize those flows after the request is acknowledged.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Field and portal parity

The old backlog understated what is already present here, but there is still real work left.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Hidden portals | Reveal, hide, alpha fade, and collision gating are implemented | Remove from missing backlog | `PortalPool.cs` (`CPortalList::FindPortal_Hidden`) |
| Implemented | Portal property accessors | `PH`, `PSH`, and `PV` equivalents are implemented | Remove from missing backlog | `PortalPool.cs` |
| Implemented | Portal interaction parity | Standard and hidden portal triggering now share the same same-map delay seam as client-confirmed portal teleports, and Mechanic `Open Portal: GX-9` casts now place WZ-backed temporary linked gates that resolve on `Up` interaction with the client-like 120 ms same-map handoff; town portal / mystic door remains tracked separately below | Portal travel now covers the main visible, hidden, and same-map skill-portal branches instead of reducing open-gate travel to unsupported or generic transitions | `PortalPool.cs`, `MapSimulator.cs`, `TemporaryPortalField.cs` (`CUserLocal::CheckPortal_Collision`, `CUserLocal::TryRegisterTeleport`, `Skill/3510.img/skill/35101005`) |
| Implemented | Passive transfer-field triggers | Portal `Up` requests now latch for a short client-style retry window while one-time actions or temporary movement locks are active, then auto-replay the transfer attempt as soon as the local player can move again | Passive transfer maps and scripted transitions no longer require perfectly timed repeat input after short lockout actions before the field handoff occurs | `MapSimulator.cs`, portal/field transition layer (`CUserLocal::TryPassiveTransferField`) |
| Implemented | Transportation fields | Regular passenger ships now expose a dedicated transport-deck foothold that carries both the player and grounded mob passengers through the existing movement seam, while Balrog-only ship visuals no longer publish a false passenger deck | Transport maps now keep visible deck passengers riding the ship during departures and arrivals instead of leaving the transport system as a mostly visual-only layer | `TransportationField.cs`, `PassengerSyncController.cs`, `MapSimulator.cs` |
| Partial | Dynamic objects / quest layers | Field object drawables now retain WZ `quest` visibility metadata, keep WZ `hide=1` object layers suppressed until a matching quest state or a published object-tag toggle explicitly reveals them, resolve WZ object `tags` through the dedicated published object-state seam instead of the obstacle bucket, ingest authored map-level tagged-object defaults from `publicTaggedObjectVisible` plus the client-data typo variant `pulbicTaggedObjectVisible`, including grouped multi-tag entries that inherit parent `visible/state` flags across nested tag containers even when a parent node also publishes its own `tag`/`tags`, auto-publish quest-owned layer tags from `QuestInfo.img/showLayerTag`, publish entry-owned map script aliases from `info/onUserEnter`, `info/onFirstUserEnter`, and `info/fieldScript` when their WZ script names resolve onto the current map's tagged-object set, load WZ `directionInfo/*/EventQ` trigger points and recurse nested script-name containers inside those `EventQ` nodes before publishing their resolved tags once the local player's hitbox reaches the authored `x`/`y` point, publish portal-owned script aliases when a portal is actually used, parse `Quest/Check.img` `startscript` / `endscript` names so successful NPC quest accept and complete actions publish their resolved tagged-object aliases through the same seam, now keep that same quest-script publication alive when the simulator accepts or completes a quest from the quest window instead of only the NPC-overlay lane, and publish non-quest NPC aliases from `Npc/<id>.img/info/script/*/script` when the player actually opens that NPC interaction, including packet-owned NPC dialog opens that have a speaker template id but no live field NPC instance to query. Staged `_01` / `_02` / `_03` style script aliases now retire sibling stage tags from the same camel-cased family when later entry, trigger, portal, quest, NPC, or reactor publications fire. Remaining gap: broader time-delayed or script-authored multi-step sequencing after map entry is still absent, including timer-driven follow-up publications that are not directly represented by immediate entry, trigger-point, portal-use, NPC-talk, quest-action, or reactor-action aliases. | Quest-driven and event-driven maps now respect quest state, quest-authored `showLayerTag` reveals, author-authored hidden-layer defaults, inherited grouped tagged-object default states including parent-tag containers that continue into nested inherited children, entry-script publication, WZ-authored `directionInfo/EventQ` trigger points including nested script containers, portal-script-owned tagged-layer publication, quest-owned NPC `startscript` / `endscript` publication from both NPC-overlay and quest-window acceptance/completion flows, non-quest NPC `info/script` publication on talk including packet-owned dialog opens that only expose an NPC template id, and WZ-authored staged tutorial/event script progression that swaps sibling stage tags through the same published-object seam instead of only ever accumulating visible stages; reactor-driven staged script publications now retire prior sibling stage tags through that same alias seam as well. Tagged event-map objects still fall back to authored map defaults when runtime overrides are cleared, generic object-state publication no longer has to masquerade as an obstacle toggle, and the remaining gap is narrowed to broader post-entry timer or script-driven sequencing beyond immediate entry, trigger, portal, NPC, quest, and reactor publications. | map/field load path, `MapSimulator.cs`, `QuestRuntimeManager.cs`, `FieldEffects.cs`, `FieldObjectTagStateDefaultsLoader.cs`, `FieldObjectScriptTagAliasResolver.cs`, `FieldObjectDirectionEventTriggerLoader.cs`, `FieldObjectNpcScriptNameResolver.cs`, `ReactorScriptStatePublisher.cs` |
| Implemented | Town portal / mystic door lifecycle | `Mystic Door` skill casts now create a WZ-backed cross-map door pair, place the return-side door on the target town map's `TownPortalPoint` / `tp` spawn, transfer into town on `Up`, and restore the player to the original cast position when the town-side door is used before expiry | The simulator now covers the visible create, enter, restore, and positioning loop for the client's town-door utility instead of leaving the skill family nonfunctional | `TemporaryPortalField.cs`, `MapSimulator.cs`, portal/field transition layer (`Skill/231.img/2311002/{cDoor,mDoor,common/time}`, `PortalType.TownPortalPoint`) |
| Partial | Remote portal-pool parity | Packet-authored remote portal pools now exist in the simulator runtime instead of being folded into local skill ownership: `330/331` decode and apply remote town-portal create/remove keyed by owner character id, `332/333` decode and apply remote Open Gate create/remove keyed by owner plus first/second slot, packet-owned remote portals clear on field reload, remote Open Gates link and teleport through the existing same-map interaction seam, remote town portals render on their own pool, preserve their packet-authored state byte, add the WZ `Frame` overlay for the nonzero remote-door phase, route into the field's resolved town return target when that destination is available, synthesize the inferred town-side return door back into the original field when that field-side return target is known, cache owner-level field/town Mystic Door metadata so later town-side packets can reuse an earlier observed source-field return, and a `/portalpacket` debug seam now drives the pool with either raw packet payloads or typed create/remove arguments. Remote Mystic Door timing is also closer to `CTownPortalPool`: state-`0` create packets now stay in the WZ-backed `cDoor` opening run for the asset-authored 1700 ms before promoting to the steady door/frame pair, stable remote door loops now retain their own phase start instead of sharing a global zero-tick cycle, state-`0` remove packets now snapshot the currently visible field/town door canvases plus the field-side `Frame` overlay into a 1000 ms fade instead of fading a freshly restarted animation, and the simulator now keeps per-owner field/town remote-door runtime instances alive across phase updates instead of tearing down and recreating the whole remote Mystic Door pool on each sync. Remote portal ownership is also closer to the client now: remote Mystic Doors keep persistent per-layer drawable slots instead of replacing whole portal drawable arrays, remote Open Gates now keep per-slot runtime instances that mutate between WZ-backed `cDoor`, solo `sDoor`, linked `mDoor`, and remove `eDoor` states without rebuilding the entire pool when a partner gate appears or disappears, town-side-first remote Mystic Doors can now reuse the owner's last observed non-town field position when that observed field's WZ-resolved town return matches the current town map even if no field-side door packet was seen, the destination resolver keeps an exact same-town packet-authored field-side cast position authoritative over later approximate movement observations for that town instead of letting those looser snapshots steal the inferred return point, town-side packet-only inference now promotes the chosen same-town observed source field into reusable owner-town metadata so later create/remove phase churn keeps returning to the same inferred field rather than jumping when newer approximate observations arrive from another matching field, the owner-observation cache now ranks remote-user field-entry and follow-transfer coordinates above generic move snapshots so town-side packet-only doors can keep stronger same-town source-field evidence when the simulator saw an exact server-authored entry or transfer point but never saw the field-side door packet itself, and repeated same-field fallback observations now refresh the stored approximation instead of freezing the first passive movement snapshot forever. Remaining gap: packet-only town-side streams still cannot recover the exact original cast position when the simulator never observed any usable same-town source field first, when it only ever saw generic movement snapshots rather than a field-entry, transfer, or cast point, or when no exact same-town field-side door packet was ever seen to replace those approximations; those cases now converge on a newer same-field approximation, but they still lack the exact authored cast coordinate the client would have learned from the missing stronger evidence. | Visible parity no longer stops at self-authored portal utility: other users' Open Gates and packet-owned Mystic Doors now have their own runtime pool, the field-side remote door can hand off into town through the packet seam, inferred town-side doors can return to the original cast field without needing a second authored packet stream, later town-side packets can now recover a previously observed source field via owner metadata caching or a last-observed owner field fallback, remote Mystic Door ownership now uses persistent per-layer runtime slots that mirror the client-side canvas choreography more closely than whole-portal drawable replacement, and remote Open Gate ownership now persists per slot so paired and unpaired visual transitions no longer re-register the whole pool. In this pass the Mystic Door source-field resolver was tightened one step further: packet-only town-side recovery no longer treats all owner-position evidence as equally weak, because remote-user enter-field and follow-transfer packets now outrank passive movement snapshots and can hold the inferred return field steady until an exact packet-authored cast supersedes them, while repeated same-field fallback observations now keep the approximate return point fresh instead of pinning it to the first passive snapshot seen in that map. The remaining work is now limited to exact-cast recovery for packet-only town-side cases where the simulator never witnessed a precise same-town source-field cast, field-entry point, or transfer point and therefore still has to rely on generic movement snapshots or no inferred source field at all. | field portal/runtime layer (`CTownPortalPool::OnPacket`, `COpenGatePool::OnPacket`, local reference seam: `CUserLocal::CheckPortal_Collision`) |
| Partial | Remote affected-area / mist parity | The simulator now has a real packet-owned `328/329` path for affected areas: a dedicated codec decodes create/remove payloads into the remote pool, `/affectedpacket` can drive the same raw-packet seam or build typed test payloads, packet create timing honors the client packet's short start-delay field before the area becomes active, and a dedicated packet-runtime binder clears remote affected areas on field bind instead of depending on unrelated simulator call sites. Hostile gameplay is no longer purely render-only either: remote mob-skill mist packets now reapply mapped mob-skill statuses to the local player while contained, now consult active projected abnormal-status and elemental resistance from friendly support affected areas before those resistible packet-owned mist statuses land, hostile remote player zones no longer stop at poison/burn hand-maps and now reuse the existing `SkillManager` inferred mob-debuff seam for broader freeze/stun/seal/blind/darkness/slow/weakness plus explicit debuff-token and stat-driven mob statuses while contained, and the player-zone classifier uses WZ-loaded support metadata before falling back to hostile handling so friendly remote auras or support fields with positive stat / heal metadata no longer spill into the hostile mob-debuff lane just because they are not one of the already-modeled invincible or recovery cases. Client-confirmed `type=3` affected areas still route through their own packet source kind and load item-backed `darkFog` visuals, and those fog lifetimes track `CAffectedAreaPool::OnAffectedAreaCreated` more closely by first reading item `info/spec/time`, then recursively following WZ item `info/path` links, and finally parsing the resolved linked-item description text before falling back to packet remove timing. Invincible and recovery-style remote support zones already used real remote owner resolution against the simulator's remote-user roster plus party list, and the broader friendly support lane now applies those same self-or-party ownership rules with WZ-backed party eligibility from the base zone skill plus any linked `info/affectedSkill` companions, so self-only remote fields no longer leak buffs onto party members while `massSpell` or linked-party aura fields such as Battle Mage support zones still project onto the local player when the owner is an actual party member. Friendly remote support areas with WZ-authored stat payloads continue to project their supported buff gameplay onto the local player while contained through the existing buff seam, including attack, defense, accuracy, avoidability, speed, jump, critical-rate, max-HP/max-MP, primary-stat style bonuses, and WZ-authored reduction/resistance and rate payloads such as `damR`, `asrR`, `terR`, `expR`, `dropR`, and `mesoR` when those fields are present. The simulator now also merges supportive linked-skill metadata from WZ `info/affectedSkill` branches before classifying or projecting remote support areas, so aura-style fields such as Battle Mage support zones can carry linked buff stats and party-targeting metadata into the packet-owned affected-area path instead of stopping at the base zone node, and projected `damR` from those remote support areas now changes real incoming damage through the existing combat seam rather than remaining HUD-only metadata. Remaining gap: the broader hostile remote player-zone semantics are still simulator-side approximations rather than a full reconstruction of the client's exact affected-area ownership and status rules, support branches whose client behavior needs mechanics beyond the simulator's current projected buff plus resistance plus damage-reduction plus party-eligibility surfaces still stop short of full gameplay reconstruction, and any `type=3` fogs whose lifetime is encoded outside reachable WZ time fields, linked item paths, or parsable descriptions still fall back to packet-driven removal timing. | This closes more of the row inside the existing packet and runtime seams: remote affected areas now self-expire from a stronger client-shaped area-buff metadata resolver instead of only a direct `info/time` read, including recursive linked-item and linked-description fallbacks; the player-area dispatcher classifies friendly support zones from WZ-loaded stat and summon metadata before deciding whether to run the hostile mob-debuff lane; and eligible remote support areas now refresh synthetic area-owned buffs through `SkillManager` instead of stopping at classification. In this pass the projected support-buff seam was extended further in three concrete WZ-backed ways: `info/affectedSkill` support companions are now folded into the remote support projection surface before the simulator decides whether a packet-owned player area is friendly, projected `damR` now feeds real incoming player damage through the combat path instead of exposing damage reduction only as temporary-stat UI, and linked support skills now also contribute party-targeting metadata so the remote support owner gate distinguishes self-only fields from `massSpell` or linked-party fields before projecting buffs or healing onto the local player. `asrR` / `terR` support metadata continues to flow through the same seam into the player mob-status controller, so real area skills such as `23111005` and `32110000` can now both resist remote mist ailments and reduce incoming damage while the player remains inside the friendly field. The remaining work is narrower and more data-sensitive: full client-backed ownership and targeting semantics for hostile player zones, support branches that require dedicated simulator mechanics beyond the current buff-plus-resistance-plus-damage-reduction projection surface, and residual `type=3` duration cases that still live outside the currently recoverable item metadata. | field affected-area/runtime layer (`CAffectedAreaPool::OnPacket`, `CAffectedAreaPool::OnAffectedAreaCreated`, `CAffectedAreaPool::OnAffectedAreaRemoved`, local reference seam: `SkillManager`, packet-owned seam in `AffectedAreaPool`, zone/effect draw path) |
| Partial | Field-specific restrictions and effects | Map entry now surfaces `timeLimit`, `decHP`, `protectItem`, `allowedItem`, ambient `rain` / `snow`, `Unable_To_Jump`, `Unable_To_Fall_Down`, `Unable_To_Use_Teleport_Item`, field-transfer, `Unable_To_Use_Rocket_Boost`, `Unable_To_Open_Mini_Game`, `Unable_To_Use_Pet`, `Auto_Expand_Minimap`, `Parcel_Open_Limit`, `No_Quest_Alert`, `No_Android`, `Unable_To_Change_Party_Boss`, `Drop_Limit`, `No_Damage_On_Falling`, Mystic Door field-limit metadata, positive WZ `moveLimit` counts, and WZ-authored `onUserEnter` / `onFirstUserEnter` / `fieldScript` names through simulator notices, matching `rain` / `snow` map-info flags automatically start the simulator's ambient weather layer on field entry, matching `Auto_Expand_Minimap` fields now force the minimap open on entry through the existing minimap seam, `onFirstUserEnter` notices and tagged-object publication now only fire on the first visit to that map in the current simulator session while `onUserEnter` / `fieldScript` continue to publish every entry, blocked skill casts raise explicit field-limit feedback for both Rocket Booster and Mystic Door map bans, jump-disabled maps now reject jump input with the same field-rule messaging seam, jump-down-disabled maps now reject down-jump input through that same field-rule path, cross-map portal travel respects `Unable_To_Migrate`, `decHP` maps deal periodic environmental damage with a mist cue unless the inventory currently holds a listed `protectItem`, WZ-authored positive `recovery` values now tick HP and MP recovery through the player's existing recover seam every 10 seconds, `timeLimit` maps warn before expiry then transfer to `returnMap` / `forcedReturn`, `allowedItem` maps now reject non-listed inventory-backed item consumption plus setup-chair activation through the same field-rule notice path, cash teleport-rock items now route into the simulator's map-transfer window unless `Unable_To_Use_Teleport_Item` blocks them, WZ-authored numeric `consumeItemCoolTime` values now enforce a shared consume-item cooldown through the same inventory guard seam, pending cross-map transfers now reject under-`lvLimit` characters plus maps marked `partyOnly` or `expeditionOnly` using the simulator's existing party-roster and expedition-search session state with explicit map-restriction notices, positive `moveLimit` values now cap WZ-classified movement-skill casts through the same field-skill rejection seam, additional WZ-backed `fieldLimit` item families now publish entry notices and reject inventory use through the same field-rule path for portal scrolls (`203xxxx`, including the separate specific-scroll bit), summon sacks (`210xxxx`), cash weather (`512xxxx`), lie-detector anti-macro items (`219xxxx`), wedding invitation items, NPC-summon items, pet-item families, and WZ-modeled stat-change consumables that apply temporary buffs, morphs, modeled recovery, or cure effects, and the wedding invitation plus NPC-summon classifiers now also use concrete WZ-backed invitation item families and `spec/script=summonEventNpc` metadata instead of relying only on localized text matches. `Unable_To_Open_Mini_Game` now blocks mini-room, personal-shop, and entrusted-shop window surfacing through the simulator's existing social-room launch seams, `Parcel_Open_Limit` now blocks memo-mailbox and quest-delivery window surfacing through the simulator's existing utility-window seams, `No_Quest_Alert` now blocks quest-alarm surfacing through the existing quest-alarm seam, `No_Android` now hides the Big Bang equip window's android pane while active, `Unable_To_Change_Party_Boss` now blocks party-leader reassignment through the social-list action seam, `Drop_Limit` now rejects local and pet drop looting through the existing pickup-availability path with explicit pickup feedback, `Unable_To_Use_Pet` now suppresses live pet runtime behavior by hiding pet updates/draw, blocking pet speech/slang/command and auto-HP interactions, and preventing pet-food planning while the field rule is active, and player landing now applies a simulator-owned distance-and-impact fall-damage model that is suppressed by `No_Damage_On_Falling` through the same field-limit authority. Actual field-script execution beyond these notice/publication effects, broader server-authored admission state, exact client-owned fall-damage thresholds or formulas, drop spawning or discard restrictions beyond looting, android restrictions outside the currently modeled equipment-pane/runtime seam, and any client-only checks that depend on packet/session context remain unmodeled. | Event and boss maps now expose a broader slice of client field-rule timing, movement locks, migration locks, hazard behavior, periodic recovery maps, ambient field weather, `protectItem` hazard suppression, `allowedItem` item-use gating, teleport-item map bans, several concrete `fieldLimit` item-family bans, auto-expanding minimap maps, Mechanic rocket-mobility bans, mini-room/shop opening bans, parcel and quest-alert utility-window bans, live pet-runtime suppression, numeric consume cooldowns, movement-skill count caps from positive `moveLimit` values, first-visit-only `onFirstUserEnter` behavior, basic level, party, and expedition entry gating, concrete runtime handling for android-tab suppression, party-leader change bans, and drop-loot bans through existing UI, social, and pickup seams, and now a landing-based fall-damage gameplay path that honors `No_Damage_On_Falling` instead of leaving that branch as notice-only metadata. This pass also hardens two of the broader item-family bans with WZ-backed evidence instead of pure text heuristics: wedding invitation families now recognize concrete invitation item IDs and `4211xxx` / `4212xxx` ETC families, and NPC-summon bans can resolve WZ `spec/script=summonEventNpc` in addition to the existing text fallback. Generic entry-owned field scripts are still not executed beyond notices or tag publication, deeper server or session admission checks are still absent, the fall-damage model is still an approximation rather than a byte-accurate reconstruction of the client rule, and residual android or drop restrictions still need dedicated simulator seams. | `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.PacketOwnedLocalOverlayParity.cs`, `MapSimulator.FieldRestrictionParity.cs`, `Companions/PetController.cs`, `Interaction/SocialListRuntime.cs`, `UI/Windows/EquipUIBigBang.cs`, `UI/PickupNoticeTextFormatter.cs`, `FieldRuleRuntime.cs`, `FieldRuleEffectApplier.cs`, `FieldEntryRestrictionEvaluator.cs`, `FieldEnvironmentEffectEvaluator.cs`, `FieldSkillRestrictionEvaluator.cs`, `FieldInteractionRestrictionEvaluator.cs`, `FieldFallingDamageEvaluator.cs`, `TeleportItemUsageEvaluator.cs`, `UI/Windows/InventoryItemMetadataResolver.cs`, `UnitTest_MapSimulator/FieldFallingDamageEvaluatorTests.cs`, `UnitTest_MapSimulator/FieldInteractionRestrictionEvaluatorTests.cs`, field systems (`CField::IsFearEffectOn`, `CField::OffFearEffect`) |

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned drop-pool parity | The simulator now has an explicit packet-owned drop runtime instead of leaving `CDropPool` implied behind local drop helpers. A dedicated `RemoteDropPacketRuntime` plus `RemoteDropPacketCodec` now model the client-owned `322` / `324` dispatch surface on the existing MapSimulator seams, `DropPool` can apply packet-authored enter and leave packets directly, field rebinding clears only packet-authored drops, and a focused `/droppacket` command can drive the same enter/leave path for manual verification. The packet-backed `DropPool` side now stores client-facing ownership and source metadata, resolves enter start positions through live mob source positions when available, handles packet-owned direct player, pet, and mob pickup removal through the existing pickup-notice and recent-history callbacks, and delays explode-style removal instead of collapsing every leave into an immediate generic delete. `DropItem` update was also tightened toward the scanned `CDropPool::Update` shape by honoring packet create delay before settle, retaining fake-vs-real drop state, and adding an idle hover branch for settled packet drops rather than treating every packet-authored drop as a plain local spawn. Remaining gap: exact party or broader owner-group arbitration is still approximated because the simulator does not yet carry the client party-id ownership surface into `DropPool`, the full `CDropPool::Update` parabola and settle math is still only partially mirrored, and `CDropPool::MakeMoneyAnimation` still stops at delayed removal plus existing pickup/history side effects instead of replaying the client's full layered meso icon animation stack and sound rules. | Without an explicit `CDropPool` row, drop parity is easy to overstate once pickup text, local loot, or pet auto-loot exists. The client keeps a dedicated pool owner for enter/leave/update/pickup behavior, so tracking it separately keeps future work anchored to the real field-entity runtime instead of scattering it across UI feedback, pet logic, or generic map-object handling. | field drop/runtime layer (`DropPool.cs`, `RemoteDropPacketCodec.cs`, `RemoteDropPacketRuntime.cs`, `MapSimulator.RemoteDropPackets.cs`, `MapSimulator.ChatCommands.cs`, `CDropPool::OnPacket`, `CDropPool::OnDropEnterField`, `CDropPool::OnDropLeaveField`, `CDropPool::Update`, `CDropPool::TryPickUpDrop`, `CDropPool::TryPickUpDropByPet`, `CDropPool::TryPickUpDropByMob`, `CDropPool::MakeMoneyAnimation`, `CDropPool::RecordRecentPickupItem`) |

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive these flows from local input and generic runtime state. It also has dedicated packet-response handlers for chair sit ack or correction, same-field teleport completion, and quest-result or quest-timer application.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned quest-result parity | The simulator now has a dedicated local `OnQuestResult` packet seam on opcode `242` instead of relying only on local quest helpers or the older field-owned timer path. The dispatcher decodes the client-confirmed quest-timer families (`case 6` through `9`, plus expiry/reset handling from `17` and `18`) into the existing `PacketFieldStateRuntime` timer owner, routes quest-result subtype `10` through a WZ-backed quest presentation builder that reads quest metadata already loaded from `Quest/QuestInfo.img`, `Quest/Say.img`, and `Quest/Act.img`, applies the existing job or gender reward filters when building follow-up text, shows the packet-owned notice through the chat/status seam, opens multi-page quest-result dialogue through the existing NPC interaction overlay using the closer util-dialog presentation style, and now defers the trailing follow-up quest id into an actual packet-owned `StartQuest` bridge by queueing the accept through the existing quest runtime once the quest-result modal closes. The packet decode is closer to the client now as well: subtype `10` no longer accepts simulator-only text-kind or view-mode bytes, requires the client-shaped trailing follow-up quest id after the quest and NPC ids, and chooses the start-vs-end WZ branch from the same quest-record-present check that `CUserLocal::OnQuestResult` uses instead of treating completed quests as a separate presentation family. Coverage is broader than before too: subtype `12` now uses that same quest-record-owned branch when building its WZ-backed action-summary notice from quest act data, fixed-notice subtypes `11`, `13`, `15`, and `16` no longer fail as unsupported packets and now track the client `StringPool` ids behind each direct notice branch, subtype `14` is modeled as the client no-op branch that only falls through to `CWvsContext::CheckNewQuestAvailable`, and quest availability still refreshes after every packet to mirror that trailing client call. Remaining gap: the exact client `StringPool` text and formatting flow for subtype `10`, subtype `12`, and the fixed notices is still approximated through simulator builders or `StringPool`-labeled placeholder notices rather than reconstructed byte-for-byte, the modal still reuses the simulator's overlay runtime instead of a dedicated `CUtilDlgEx`-specific surface, subtype `12` still summarizes act rewards through the simulator quest-action builder instead of the client's exact item-category string assembly, and any still-unrecovered `OnQuestResult` subtype semantics beyond the now-covered timer, presentation, action-summary, fixed-notice, no-op, expiry, and reset families remain unmodeled. | Quest parity is not only local accept/complete helpers, quest windows, or generic field timers. The client owns a packet-response dispatcher that adds and removes quest timers, shows quest-result notices and modal pages from WZ-backed quest data, can emit fixed or act-driven quest-result notices, can auto-start a follow-up quest after the modal returns, and then refreshes quest availability from that same packet seam. Tracking this row against the dedicated opcode `242` owner keeps the remaining work anchored to the real response path instead of scattering it across local quest commands or field-state utilities. | packet-owned local quest-result layer (`CUserLocal::OnPacket`, `CUserLocal::OnQuestResult`, `CWvsContext::AddQuestTimer`, `CWvsContext::RemoveQuestTimer`, `CWvsContext::CheckNewQuestAvailable`, `CWvsContext::StartQuest`, `CUtilDlgEx`, `Quest/QuestInfo.img`, `Quest/Say.img`, `Quest/Act.img`, `MapSimulator.PacketOwnedQuestResultParity.cs`) |
| Partial | Packet-owned chair sit-result parity | The simulator now has a dedicated local `OnSitResult` packet seam on opcode `231` instead of treating chairs as local-only intent. The packet-owned handler stamps the shared local utility request tick, mirrors the client-owned `CWvsContext` chair bookkeeping in the packet-owned local-utility context by clearing the chair exclusive-request flag at `+0x20B8` and restamping the exclusive-request tick at `+0x20BC`, decodes the client-shaped success flag plus seat index payload, resolves the current map's WZ `seat/<index>` vector, validates the local player against the same client chair rectangle (`x +/- 10`, `y +/- 30`), and routes successful placement through a packet-owned local sit placement helper that also raises a dedicated secure sit confirmation flag on the local player. Successful packet-owned placement now clears any attached local passenger linkage through the existing follow-runtime and remote-user seams before the sit is finalized, matching the client path that zeroes `m_dwPassenserID` and the passenger's driver link. Invalid returned seats or rectangle mismatches now force the same stand-up correction path and send that correction through the shared local-utility context owner instead of a raw timestamp: the simulator now records a client-shaped `CWvsContext::SendGetUpFromChairRequest(0)` outpacket with opcode `45` and payload `FFFF`, re-arms the exclusive-request flag after emission, and suppresses the send when the local HP gate or exclusive gate would block it. Packet rejection still clears the sit state without emitting that correction request, and the implementation still clears local couple-chair preview or external-owner pair overlays before applying packet-owned placement so the final seated state comes from the packet seam. Remaining gap: the chair `CWvsContext` bookkeeping and correction outpacket are now modeled under the simulator's local-utility context owner, but they still stop at simulator-owned recorded state rather than delivering through a live socket or broader outbound packet transport surface. | Chairs are not only a local toggle or render-state change. The client keeps a packet-owned success/failure and seat-validation branch that decides whether the user really sits, whether a passenger link is broken, and when to force a stand-up correction. Tracking this row against the dedicated opcode `231` seam keeps the remaining work anchored to the real local response owner instead of only local chair commands or remote chair packets. | packet-owned local chair-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnSitResult`, WZ `Map/Map*/<map>.img/seat`, packet-owned local sit placement, correction path toward `CWvsContext::SendGetUpFromChairRequest`, `PacketOwnedLocalUtilityContextState`) |
| Partial | Packet-owned teleport-result parity | Standard same-map portal travel no longer finalizes purely through local simulator movement. When a visible or hidden portal targets the current field and its destination `tn` resolves through the current `PortalPool`, the simulator still uses the WZ-backed portal `delay` (falling back to the client-confirmed 120 ms handoff), stages a pending same-map teleport against the destination portal index, and completes the move through a dedicated local `OnTeleport`-style apply seam instead of writing the final position directly from the interact handler. That packet-owned seam now also owns temporary same-map portal completion: linked portals from `TemporaryPortalField` stage the delayed move through the same apply helper, attempt to resolve any returned landing coordinates back through the current `PortalPool`, and only fall back to exact-coordinate placement when no current portal entry matches. Cross-map portal travel now also finishes through packet-owned teleport bookkeeping rather than stopping at field-load spawn setup alone: the simulator preserves a pending cross-map teleport target across `UnloadMapContent`, reloads the new field, resolves the destination portal name through the freshly built portal list when available, and then applies the post-load landing through the same local teleport-result seam, including request-flag cleanup, completion timestamping, exact portal-coordinate landing, and named `pn` / `tn` handoff capture from the resolved destination portal. Standard visible or hidden portal requests now also stamp simulator-owned request metadata before the ack arrives, and resolved portal-index completions replay two client-confirmed `TryRegisterTeleport(..., bForced = 1)` side effects through the same packet-owned seam: the post-ack 120 ms teleport cooldown now gates fresh portal use, and any attached local passenger link is cleared through the existing follow/runtime ownership when the teleport lands. Remaining gap: temporary portal destinations that do not line up with a real `CPortalList` entry still require coordinate fallback instead of a true returned portal index, and fallback or synthetic branches still stop short of the rest of the forced client registration path, including the general teleport effect, move-path attribute / set-item-background side effects, and a real outbound portal-request packet path instead of simulator bookkeeping for the last standard portal request. | Teleport parity is not only about entering a portal or moving to the right coordinates. The client also has a response-time authority point that applies the actual destination by portal index and records the named teleport handoff after the packet returns. Tracking that seam makes the remaining parity gap explicit instead of hiding it behind the already-implemented local portal interaction flow. | packet-owned local teleport-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnTeleport`, `CPortalList`, `CUserLocal::TryRegisterTeleport`, `MapSimulator.cs`, `MapSimulator.PacketOwnedTeleportParity.cs`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
