# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/DropPool.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Field and portal parity

- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`

Notes:
`CUserLocal::TryPassiveTransferField` shows that client field transfer is not only explicit portal collision handling; the local-player update loop also retries an up-key style transfer after one-time actions finish when the character is allowed to move. A later IDA pass also shows that packet-authored field utility surfaces are split into their own remote pools rather than hanging off local collision handling: `CAffectedAreaPool::OnPacket` owns affected-area create/remove, `CTownPortalPool::OnPacket` owns town-portal create/remove, and `COpenGatePool::OnPacket` owns Open Gate create/remove.

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`

Notes:
The backlog already covered pickup notices and pet auto-loot side effects, but the broader client owner was still unnamed. IDA shows that drop parity is owned by a dedicated `CDropPool` runtime that handles enter/leave packet dispatch, per-frame drop update, player/pet/mob pickup arbitration, meso animation, and recent-pickup history instead of treating drops as generic field objects plus UI messages.

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive chairs, same-field teleports, and quest results from local intent and generic runtime state. It also has dedicated packet-response handlers that finalize those flows after the request is acknowledged.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Field and portal parity

The old backlog understated what is already present here, but there is still real work left.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Hidden portals | Reveal, hide, alpha fade, and collision gating are implemented | Remove from missing backlog | `PortalPool.cs` (`CPortalList::FindPortal_Hidden`) |
| Implemented | Portal property accessors | `PH`, `PSH`, and `PV` equivalents are implemented | Remove from missing backlog | `PortalPool.cs` |
| Implemented | Portal interaction parity | Standard and hidden portal triggering now share the same same-map delay seam as client-confirmed portal teleports, and Mechanic `Open Portal: GX-9` casts now place WZ-backed temporary linked gates that resolve on `Up` interaction with the client-like 120 ms same-map handoff; town portal / mystic door remains tracked separately below | Portal travel now covers the main visible, hidden, and same-map skill-portal branches instead of reducing open-gate travel to unsupported or generic transitions | `PortalPool.cs`, `MapSimulator.cs`, `TemporaryPortalField.cs` (`CUserLocal::CheckPortal_Collision`, `CUserLocal::TryRegisterTeleport`, `Skill/3510.img/skill/35101005`) |
| Partial | Passive transfer-field handoff parity | WZ re-check in the active export root still shows the authored transfer restriction on the map, not the portal: `map/Map\Map0/000050000/info/fieldLimit = 4`, and the same map keeps ordinary transfer data on portal nodes such as `portal/5` with `pt = 2`, `tm = 1000000`, and `tn = west00` while its collision-script tutorial portal remains a separate `pt = 9` branch. Client re-check in IDA still shows `CUserLocal::TryPassiveTransferField` at `0x91a2e0` waiting for `CAvatar::GetOneTimeAction() <= -1`, reading the live field-interface readiness bit through `m_pvc.m_pInterface + 416`, and then replaying `CUserLocal::HandleUpKeyDown`; direct re-check of `HandleUpKeyDown` at `0x919e50` still shows `CMacroSysMan::Stop` before the one-time-action, immovable, foothold, and portal branches, same-map non-changeable portals using `TryRegisterTeleport`, and cross-map or same-map client-changeable portals using `CField::SendTransferFieldRequest` with portal sound only when `!PORTAL::IsChangable`; and a direct `CUserLocal::OnKey` re-check at `0x9369f0` shows fresh Up consuming the sitting branch first by sending `CWvsContext::SendGetUpFromChairRequest(500)` when the local user is seated and not attached to a follow driver instead of falling through to `HandleUpKeyDown`. This pass keeps the work inside the existing simulator seam and recovers that missing pre-branch alongside the earlier macro-stop work: passive queued replay still clears the queued skill-macro lane before the replay admission checks, fresh eligible Up still clears the same queue after attached-driver suppression and before the portal replay path, and fresh Up now mirrors the client-shaped chair pre-branch by stopping queued skill-macro playback first, consuming the chair get-up path, and clearing any queued passive retry before any portal replay or passive-transfer arming can run. Existing parity remains in place: `HandlePortalUpInteract` does not time out the queued passive-transfer retry or require Up to stay held after the original keydown; the queued retry is evaluated before the generic fresh-Up gate; once the one-time action finishes, the simulator re-queries the inferred live field-interface gate inline and replays the up-key path only when that gate reopens; replay still clears after the client-shaped immovable / attract / grounded admission contract; fresh left or right keydown cancels the queued retry; fresh Up is suppressed while `LocalFollowCharacterRuntime.AttachedDriverId` is set; the arm condition and queued replay both require current collision with an eligible transfer-field portal; same-map client-changeable portals still send a transfer-field request; `Changeable` and `ChangeableInvisible` transfer-field requests still suppress the immediate local portal sound; and the live-interface gate still drops for recovered simulator-owned transfer owners including field-rule migrate bans from `info/fieldLimit`, special-field pending transfers / scripted sequences, packet-owned teleport ownership (`_packetOwnedTeleportRequestActive` or `_pendingCrossMapTeleportTarget`), attached packet-owned follow drivers, pending same-map transfer ownership, and exclusive transfer request ownership. Focused regression coverage for the queue-arm contract, queued-retry decision, colliding-portal replay requirement, horizontal-key cancellation, attached-driver fresh-Up suppression, fresh-Up chair pre-branching, fresh-Up chair-consumed queued-retry clearing, interface gate, grounded replay contract, same-map client-changeable transfer-field request predicate, changeable-transfer no-local-sound predicate, and queued macro-skill clearing lives in `UnitTest_MapSimulator/PassiveTransferFieldHandoffParityTests.cs`. The remaining gap is narrower again but still not closed: the simulator still models `m_pvc.m_pInterface + 416` through inferred runtime ownership rather than a byte-for-byte reconstruction of every client writer, so unrecovered field-interface owners beyond the current colliding-transfer-portal, field-rule, special-field, packet-teleport, attached-follow-driver, horizontal-key cancellation, fresh-Up driver suppression, same-map client-changeable transfer-field request, changeable-transfer sound suppression, queued macro-stop, exclusive-request, same-map-transfer, scripted-sequence, and grounded replay seams may still be missing. | Without this row, passive transfer-field behavior can look like a minor portal follow-up even though the client treats it as a separate retry owner tied to movement state, one-time action completion, actual portal collision, vec-ctrl field readiness, transfer-request ownership, macro cancellation, fresh-Up pre-branches, and the client-changeable portal branch. Tracking it explicitly here keeps future parity work anchored to the actual handoff gate instead of folding it into unrelated teleport or portal-collision polish. | `MapSimulator.cs`, `Character/PlayerCharacter.cs`, `Character/Skills/SkillManager.cs`, `Physics/CVecCtrl.cs`, `Fields/FieldRuleRuntime.cs`, `Fields/SpecialFieldRuntimeCoordinator.cs`, `Fields/PassiveTransferFieldReadinessEvaluator.cs`, `Interaction/LocalFollowCharacterRuntime.cs`, `UnitTest_MapSimulator/PassiveTransferFieldHandoffParityTests.cs`, portal/field transition layer (`CUserLocal::TryPassiveTransferField`, `CUserLocal::HandleUpKeyDown`, `CUserLocal::OnKey`, `CMacroSysMan::Stop`, `CAvatar::GetOneTimeAction`, `CWvsContext::SendGetUpFromChairRequest`, `CField::SendTransferFieldRequest`, WZ `Map/*/info/fieldLimit`) |
| Implemented | Transportation fields | Regular passenger ships now expose a dedicated transport-deck foothold that carries both the player and grounded mob passengers through the existing movement seam, while Balrog-only ship visuals no longer publish a false passenger deck | Transport maps now keep visible deck passengers riding the ship during departures and arrivals instead of leaving the transport system as a mostly visual-only layer | `TransportationField.cs`, `PassengerSyncController.cs`, `MapSimulator.cs` |
| Partial | Dynamic objects / quest layers | Field object drawables retain WZ `quest` visibility metadata, keep authored `hide=1` quest layers suppressed until a matching quest state or published tag mutation reveals them, and still ingest authored tagged-object defaults from `publicTaggedObjectVisible` plus `pulbicTaggedObjectVisible`, including numeric boolean leaves and nested inherited tag/state containers preserved through the `MapLoader` copy seam. Entry-owned map scripts (`onUserEnter`, `onFirstUserEnter`, `fieldScript`), portal scripts and `pn` fallback, `directionInfo/*/EventQ`, quest `showLayerTag`, quest `startscript` / `endscript` / `npcAct`, NPC `info/script`, packet-owned NPC utility owners, reactor-owned publications, and item `spec/script` all continue to publish into the same dynamic-object seam, with stage-family retirement, quest-id fallback (`q<questId>s` / `q<questId>e` -> `q<questId>` / numeric tag), path/extension normalization, numeric first-argument alias recovery, and timer/callback/continuation delay parsing still applied there. This pass keeps that shared WZ script-property publication parser and now also routes the corresponding runtime consumers through publication objects instead of script-name strings when those publication objects are available: NPC `info/script` publication, quest-window and NPC-overlay quest action publication (`startscript` / `endscript` plus `npcAct`), and item-authored NPC publication now preserve inherited WZ `delay` / `wait` / `time` / `t` / `startDelay` metadata end-to-end into the existing dynamic-object scheduling seam, including parent alias containers that carry only delay/state metadata and nest later alias containers beneath them. Remaining gap: script-engine-owned sequencing is still absent when no WZ-visible alias timing, recoverable surfaced alias argument, surfaced timer/callback/continuation-shaped function text, quest `npcAct` metadata, or recoverable script-container alias metadata reaches an existing publication seam; callback chains that remain fully inside runtime script execution still stay unmodeled. | Quest-driven and event-driven maps now keep quest-authored `showLayerTag` reveals, authored hidden-layer defaults, numeric tagged-object defaults, nested inherited tag-state groups, entry-script publication, portal-script publication, `directionInfo/EventQ` trigger publication, quest-window and NPC-overlay quest script publication, packet-owned NPC utility publication, item-script publication, and staged tutorial/event tag retirement in the same published-object seam instead of scattering object visibility through unrelated toggles. Raw map-entry, quest, NPC, portal, item, and quest-action `npcAct` strings still share the normalized parser, so authored comma/semicolon-delimited, quoted, line-broken, path-qualified, extension-suffixed, and function-shaped transport strings continue to resolve onto the authored tag set without treating timer-delay literals as layer tags. Shared WZ script-property parsing now carries scheduled publications through the same path for EventQ, quest, NPC, and item owners: metadata-only alias containers publish their property-name alias, parent alias containers survive when they nest later alias containers beneath them, and inherited WZ delay metadata is added to timer/callback delay recovery before MapSimulator schedules the dynamic-object mutation. Runtime publication ownership now follows that same publication seam for NPC open, quest-window/NPC-overlay quest actions, and item-authored NPC handoff, so those flows no longer depend on string-only script-name publication when delay-aware publication objects are already available. The remaining gap is limited to callback/script-engine sequencing that never surfaces recoverable alias metadata, WZ property timing, or function text onto an existing publication path. | `MapLoader.cs`, `MapSimulator.cs`, `QuestRuntimeManager.cs`, `FieldObjectTagStateDefaultsLoader.cs`, `FieldObjectScriptTagAliasResolver.cs`, `FieldObjectScriptPublication.cs`, `FieldObjectDirectionEventTriggerLoader.cs`, `FieldObjectNpcScriptNameResolver.cs`, `ReactorScriptStatePublisher.cs`, `UI/Windows/InventoryItemMetadataResolver.cs`, `UnitTest_MapSimulator/DynamicObjectQuestLayerParityTests.cs`, `UnitTest_MapSimulator/FieldObjectScriptTagAliasResolverTests.cs`, WZ `Map/Map0/003000000/directionInfo/0/EventQ/0 = cannon_tuto_02`, WZ `QuestInfo.img/22000/showLayerTag = 22000`, WZ `Quest/Act.img/9539/1/npcAct = q9933e`, WZ `Quest/Check.img/9933/1/endscript = q9933e`, WZ `Npc/0002003.img/info/script/0/script = begin5`, WZ `Item/Consume/0243/02430073/spec/script = consume_2430073` |
| Implemented | Town portal / mystic door lifecycle | `Mystic Door` skill casts now create a WZ-backed cross-map door pair, place the return-side door on the target town map's `TownPortalPoint` / `tp` spawn, transfer into town on `Up`, and restore the player to the original cast position when the town-side door is used before expiry | The simulator now covers the visible create, enter, restore, and positioning loop for the client's town-door utility instead of leaving the skill family nonfunctional | `TemporaryPortalField.cs`, `MapSimulator.cs`, portal/field transition layer (`Skill/231.img/2311002/{cDoor,mDoor,common/time}`, `PortalType.TownPortalPoint`) |
| Partial | Remote portal-pool parity | Packet-authored remote portal pools still live in their own runtime seam instead of folding into local skill ownership, but that seam now covers the main client-facing behavior for this row: `330/331` decode remote Mystic Door create/remove by owner id, `332/333` decode remote Open Gate create/remove by owner plus slot, field reload clears both packet-owned remote portal visuals/runtimes and the owner/town inference buckets that drive packet-only town-side relinking, remote Open Gates link and teleport through the existing same-map interaction seam, and remote Mystic Doors preserve their packet state byte, WZ `Frame` overlay, WZ-backed `cDoor` opening timing, stable per-owner phase clocks, and snapshot-based state-`0` removal fade. Persistent per-owner and per-slot runtimes still stay in place across phase churn so remote Mystic Doors keep their field/town drawable slots and remote Open Gates mutate in place between WZ-backed `cDoor`, solo `sDoor`, linked `mDoor`, and remove `eDoor` states, including state-`0` Open Gates staying inactive/unlinked while their WZ `cDoor` opening phase is still running, nonzero-state partner creation immediately promoting an existing opening partner into linked `mDoor`, same-tick partner-loss relink, and a client-shaped solo-phase restart on the surviving partner. The town-side inference seam keeps owner observations bucketed by owner+town+source field plus authority, persists resolved destinations back into those same observation buckets, keeps an already active inferred source ahead of weaker unrelated later incoming destinations when that selected source has no current coordinate bucket, still lets stronger same-town evidence replace it, and keeps the deliberately lowest-authority WZ fallback for packet-only town-side streams: when the loaded map metadata has exactly one source map whose `returnMap` / `forcedReturn` points at the current town, unresolved remote Mystic Doors link back to that source map at its WZ `StartPoint` portal, falling back to the first authored portal only when no start portal is published. That fallback remains lower authority than movement, inferred, leave-field, enter-field, follow-transfer, skill-cast, and packet-cast observations, so later owner-backed coordinates replace it through the same observation seam, and this pass refreshes focused regression coverage on disk in `UnitTest_MapSimulator/RemotePortalPoolParityTests.cs` for inferred-source retention, stronger same-town replacement, the no-source-candidate unresolved branch, unique and ambiguous WZ fallback resolution, fallback authority replacement, start-point detection, field-reload inference clearing, Mystic Door packet-state churn, Open Gate inactive-opening link gating, partner-create promotion, and partner-loss relink / solo restart. IDA confirmation from `CTownPortalPool::OnTownPortalCreated` (`0x762c00`) still narrows the remaining town-side gap: the remote town-portal create packet only carries state, owner id, and the current portal coordinates, so exact source-field cast-point recovery still cannot come from the packet itself when the simulator never observed any matching owner-backed source candidate. The Open Gate client owner is now narrower too: `COpenGatePool::OnOpenGateCreated` (`0x68bfd0`) decodes state, owner id, current x/y, slot, and party id, keeps state `0` on inactive WZ `cDoor`, selects solo `sDoor` or linked `mDoor` for nonzero state, and immediately reloads an existing partner as linked `mDoor`; `COpenGatePool::OnOpenGateRemoved` (`0x68b780`) removes the packet slot, plays `eDoor` for state `0`, and immediately reloads a surviving partner as solo `sDoor`, which the simulator now mirrors inside the existing remote Open Gate runtime. | Visible parity no longer stops at self-authored portal utility: other users' Open Gates and packet-owned Mystic Doors keep their own runtime pool, the field-side remote door can hand off into town through the packet seam, inferred town-side doors can return to the original cast field without needing a second authored packet stream, packet-only town-side doors can use a WZ-authored unique return-map fallback instead of always staying unresolved, and field reload now drops stale owner/town inference state instead of letting a newly loaded field relink packet-only town-side doors through pre-reload observations. The remaining work is narrowly limited to exact cast-point recovery for packet-only town-side Mystic Door cases with no owner-backed source candidate, plus ambiguous or missing WZ fallback metadata. | field portal/runtime layer (`CTownPortalPool::OnPacket`, `CTownPortalPool::OnTownPortalCreated`, `COpenGatePool::OnPacket`, local reference seam: `CUserLocal::CheckPortal_Collision`, remote skill-use presentation seam), `UnitTest_MapSimulator/RemotePortalPoolParityTests.cs`, WZ `Skill/231.img/skill/2311002/{cDoor,mDoor,Frame}`, WZ `Skill/3510.img/skill/35101005/{cDoor,sDoor,mDoor,eDoor}`, WZ `Map/*/info/{returnMap,forcedReturn}`, WZ `Map/*/portal/*/{pt,pn,x,y}`, client pool owners `CTownPortalPool::OnPacket` (`0x7636b0`), `CTownPortalPool::OnTownPortalCreated` (`0x762c00`), `COpenGatePool::OnPacket` (`0x68c8b0`), `COpenGatePool::OnOpenGateCreated` (`0x68bfd0`), and `COpenGatePool::OnOpenGateRemoved` (`0x68b780`) |
| Partial | Remote affected-area / mist parity | The simulator keeps the packet-owned `328/329` affected-area runtime, start-delay activation, field-bind clearing, remote support projection, mixed friendly-plus-hostile Battle Mage aura handling, WZ-backed support ownership checks, WZ-backed `type=3` fog visuals and linked-duration resolution, and the existing mob-skill-mist reapplication path. The hostile player-area lane now preserves WZ-authored DOT metadata from `common/dot`, `dotInterval`, and `dotTime` instead of collapsing everything to generic `damage` / `time`, so affected-area wrappers such as `Skill/312.img/skill/3120010` and `Skill/3510.img/skill/35101009` keep their authored burn-style magnitude and timing when the packet-owned area path drives `SkillManager`'s inferred mob-status seam. The resolver also now treats linked hostile companions as part of disposition resolution instead of only the primary shell, and the linked-runtime collector now follows WZ `info/dummyOf` wrapper links in the same seam as `affectedSkill`, so sparse packet skills such as `Skill/3510.img/skill/35100008` can still reach hostile companion data on `35101009` / `35101010` before disposition and inferred mob-status replay run. This pass also consumes that WZ DOT metadata in the packet-owned runtime instead of only carrying it through the inferred-status seam: hostile player-area gameplay now ticks on the fastest reachable positive `dotInterval`, and player-skill affected-area expiration now resolves the maximum reachable authored lifetime across the visible skill plus its `affectedSkill`, `dummyOf`, and reverse `affectedSkill` parent chain. In Battlefield / Monster Carnival contexts that same staging seam now prefers WZ `PVPcommon` level data before regular `common`, so packet-owned areas no longer expire early just because the gameplay lane already switched to PvP-authored `dotTime` or `time` while `AffectedAreaPool` was still building the visual/runtime shell from the non-PvP node. The same sparse-wrapper seam still feeds the visual owner in `AffectedAreaPool`, so wrappers such as `Skill/3510.img/skill/35100008` and reverse-parent auras surfaced through `Skill/3210.img/skill/32101002` no longer have to stay gameplay-only while their visible mist or aura tile lives on a linked companion. The team-only ownership hole in the friendly lane is also closed: the existing WZ-backed `SupportsTeamMembers` wording now reaches the runtime because remote support areas no longer hardcode `ownerIsSameTeamMember = false`, and the affected-area owner check now resolves shared-team membership from the battlefield team owner plus the Monster Carnival team seam before the local support, avatar-effect, and invincibility seams decide whether the local player is a valid recipient. Monster Carnival ownership is narrower now too: once the carnival member-state seam has already resolved a named member onto a team, hostile or friendly affected-area ownership can keep that `ownerId -> team` result in the existing remote-area runtime instead of depending on a currently live remote actor shell every tick. The `type=3` fog lane is narrower now too: when no reachable WZ `time` metadata, linked path duration, or duration-bearing item description exists, the area-buff-item resolver now falls back to the authored fog animation cycle itself instead of leaving every such packet-owned fog entirely removal-packet-owned, using the same WZ-authored frame delays already loaded for the visible fog surface. Linked damage-sharing fields now recover reverse WZ `affectedSkill` parents such as `Skill/3211.img/skill/32110000 -> 32101002`, so packet streams that surface the linked aura skill can still project the authored `partyDamageSharing` owner-share metadata before the existing remote-owner damage recipient seam runs. WZ-authored `affectedSkillEffect=amplifyDamage` now also feeds the inferred hostile mob-status resolver instead of depending on `mes` metadata alone, so affected-area wrappers such as `Skill/522.img/skill/5220011` can replay their authored marked-target bonus-damage status through the existing packet-owned hostile area seam. That same WZ-authored mark now stays a pure hostile player debuff in the enemy-local lane instead of being forced through the simulator's periodic-status damage path just because the player mob-status controller also uses `PainMark` for mob-skill `134` ticks. PvP/team fields now also consume WZ `PVPcommon` area metadata in the same runtime seam: when Battlefield or Monster Carnival ownership is active, hostile tick metadata, fallback damage, support projection, and staged player-area lifetime all prefer authored `PVPcommon` values such as `Skill/312.img/skill/3120010/PVPcommon/{dot,dotInterval,dotTime}`, `Skill/3510.img/skill/35101009/PVPcommon/{dot,dotInterval,dotTime}`, `Skill/3510.img/skill/35100008/PVPcommon/{w,x,y}`, and `Skill/522.img/skill/5220011/PVPcommon/damage` before falling back to regular `common`, and hostile remote player zones no longer stay mob-only in those fields because enemy-owned packet areas can now apply recurring local-player damage plus WZ-inferred player status effects through the existing incoming-damage and player mob-status seams when the local avatar is standing inside the zone. Friendly support projection now consumes the same WZ alias-mapping seam as the rest of the support resolver instead of only raw positive stat merges, so aura-style packet areas such as `Skill/3210.img/skill/32101003` can carry authored `common/x = movement speed` plus `common/y = attack speed step` into the synthetic external-buff lane and the avatar-effect helper without dropping the negative booster-style `y` metadata on the floor before the local-player support runtime sees it. This pass also keeps generic WZ `u`, `v`, and `w` support metadata projectable in that same merge seam, and Body Boost-style aura aliases now consume their WZ-authored split instead of only carrying raw generic fields: `Skill/3211.img/skill/32111005/common/v` feeds Dark Aura outgoing damage, `w` feeds Yellow Aura speed, `x` remains the attack-speed step, and `y` feeds Yellow Aura avoidability before the synthetic external-buff lane and outgoing-damage resolver inspect the projected support data. The enemy-local debuff projection now keeps WZ-authored status chance ownership inside each inferred status instead of rolling the whole skill once before status resolution and then rolling the status again, so branches such as `affectedSkillEffect=bodyAttack&&stun` consume their authored `subProp` as the stun chance without an extra outer `prop` gate while DOT and `amplifyDamage` marks continue to use their primary `prop` status metadata. Focused coverage for the fog-duration seam, linked/reverse-parent sparse-wrapper resolution, enemy-local status projection, enemy-local player-targeted hostile-runtime filtering, enemy-local per-area hostile tick-lane gating, the PvP-level lifetime resolver, the Yellow Aura alias-mapping, Body Boost `v` / `w` / `x` / `y` alias mapping, generic `u` / `v` / `w` projection regressions, the hostile `amplifyDamage` mark regression, the per-status `prop` / `subProp` enemy-local debuff regression, and the Monster Carnival owner-team cache regression now lives in `UnitTest_MapSimulator/RemoteAffectedAreaParityTests.cs`. Remaining gap: hostile remote player-zone ownership and debuff targeting are still simulator-side approximations rather than a full client-authored reconstruction, but the enemy-local lane is narrower now: local-player projection no longer reuses the same broad hostile runtime set used for inferred mob-status replay, and now gates local hostile projection to runtimes that are explicitly hostile to players through WZ-inferred status/debuff metadata (`dot`, `bodyAttack`, `amplifyDamage`, status tokens, or other hostile status-resolver output) or truly hostile fallback-damage lanes, instead of also replaying friendly-only support wrappers that were retained only for mob-side inference. The enemy-local lane also now keeps its own per-area hostile tick gate, so local-player recurring damage/status replay no longer has to share the exact same `NextGameplayTickTime` lane as mob-side inferred-status replay for that area. Existing limits remain: the lane is still constrained to recurring damage plus WZ-inferred status/debuff projection in Battlefield / Monster Carnival contexts, still lacks byte-accurate client debuff-owner reconstruction, support branches that need bespoke simulator mechanics beyond the current projected buff, raw generic `u` / `v` / `w` metadata carry, Body Boost `v` / `w` / `x` / `y` alias mapping, alias-mapped movement/booster/avoidability support, resistance, cleanse, damage-reduction, damage-to-MP, outgoing-damage, reward-rate, crit-damage, mastery, boss-damage, and ignore-defense seams still stop short of full gameplay, wrappers that expose no hostile or visual metadata across the visible node/`affectedSkill`/`dummyOf`/reverse-parent chain can still under-classify or stay packet-removal-owned for visuals, and `type=3` fogs with neither explicit lifetime metadata nor a multi-frame authored fog cycle still rely on packet removal. | Packet-owned remote affected areas now preserve more of the client-authored hostile mist lane instead of degrading DOT-backed area wrappers into rough generic damage ticks. WZ-backed hostile areas with authored DOT payloads now carry their own per-skill magnitude, tick cadence, lifetime, linked visual surface, PvP/team-field `PVPcommon` overrides, and enemy-local status projection into the existing inferred mob-status runtime, player mob-status runtime, and affected-area staging seam, while the already-shipped support projection, mist resistance, linked support ownership, linked hostile-branch replay, mixed Battle Mage aura behavior, the battlefield / Monster Carnival team-target ownership checks, the cached Monster Carnival owner-id team carry once the carnival member-state seam has already named that owner, the `dummyOf` wrapper traversal, reverse `affectedSkill` parent recovery for linked damage-sharing auras, the enemy-local-player recurring-damage and status-projection lane for team fields, the WZ alias-mapped aura support seam for visible Battle Mage support areas such as `32101003`, the Body Boost `v` / `w` / `x` / `y` projected-support alias mapping for `32111005`, the hostile `amplifyDamage` mark fix for local player projection, the per-status `prop` / `subProp` enemy-local debuff chance fix, and the fog-cycle lifetime fallback all stay in the same seams. This keeps remote areas such as Fire/Poison `3120010`, Mechanic `35101009`, wrapper skills such as `35100008`, reverse-parent auras such as `32101002`, WZ-authored team-targeted support zones, PvP-authored aura numerics such as `32110000/PVPcommon`, Yellow Aura `32101003`, marked-target wrappers such as `5220011`, and sparse packet-owned `type=3` fogs closer to authored behavior without introducing a separate area-combat system. | field affected-area/runtime layer (`CAffectedAreaPool::OnPacket`, `CAffectedAreaPool::OnAffectedAreaCreated`, `CAffectedAreaPool::OnAffectedAreaRemoved`), `AffectedAreaPool.cs`, `AreaBuffItemMetadataResolver.cs`, `MapSimulator.RemoteAffectedAreaPackets.cs`, `RemoteAffectedAreaSupportResolver.cs`, `PlayerMobStatusController.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `MonsterCarnivalField.cs`, `UnitTest_MapSimulator/RemoteAffectedAreaParityTests.cs`, WZ `Skill/312.img/skill/3120010/common/{dot,dotInterval,dotTime}`, WZ `Skill/312.img/skill/3120010/PVPcommon/{dot,dotInterval,dotTime}`, WZ `Skill/3510.img/skill/35101009/common/{dot,dotInterval,dotTime}`, WZ `Skill/3510.img/skill/35101009/PVPcommon/{dot,dotInterval,dotTime}`, WZ `Skill/3510.img/skill/35101010/common/damage`, WZ `Skill/3510.img/skill/35100008/info/dummyOf`, WZ `Skill/3510.img/skill/35100008/PVPcommon/{w,x,y}`, WZ `Skill/3211.img/skill/32110000/info/{affectedSkill,affectedSkillEffect}`, WZ `Skill/3210.img/skill/32101002`, WZ `Skill/3210.img/skill/32101003/common/{x,y}`, WZ `String/Skill.img/32101003/{desc,h,pdesc}`, WZ `Skill/522.img/skill/5220011/info/affectedSkillEffect`, WZ `Skill/522.img/skill/5220011/PVPcommon/damage`, WZ `Skill/3211.img/skill/32111010/common/{prop,subProp,time}`, WZ `Skill/3211.img/skill/32111005/common/{v,w,x,y,z}`, WZ `String/Skill.img/32111005/{desc,h}`, WZ `Item/Cash/0501/05010079/effect/darkFog/*/delay`, battlefield team ownership in `BattlefieldField`, Monster Carnival team ownership in `MonsterCarnivalField`, and client packet entry points `CAffectedAreaPool::OnPacket`, `CAffectedAreaPool::OnAffectedAreaCreated`, `CAffectedAreaPool::OnAffectedAreaRemoved` |
| Partial | Field-specific restrictions and effects | The row still covers a wide slice of field-rule parity already landed in the simulator: entry notices for WZ field metadata and several `fieldLimit` bits, ambient rain/snow startup, `timeLimit`, `decHP`, `protectItem`, positive `recovery`, positive `moveLimit`, `allowedItem`, teleport-item bans, minimap auto-expand, movement/migration locks, map-transfer admission checks, mini-game/parcel/quest-alert/taming-mob/android/party-boss/drop/fall-damage restrictions, WZ-backed item-family bans, and the shared local item/meso drop evaluator/prompt seam. This pass tightens two remaining seams in that lane. First, wedding invitations stay locked to explicit WZ-backed families only (`Item/Etc/0421.img`, `Item/Cash/0509.img/05090100`, `Item/Cash/0525.img/05251000..05251003`, `Item/Cash/0525.img/05251100`, and String `Etc.img/4031377`, `4031395`, `4031406`, `4031407`) with no phrase fallback. Second, successful local meso drops now mirror the recovered client request shape from `CWvsContext::SendDropMoneyRequest` (`0x9f6650`) through the existing local-utility bridge/outbox seam: opcode `106`, payload `Encode4(update_time)` + `Encode4(nAmount)`, and shared utility-request stamping before live/deferred dispatch. Focused regression coverage now lives in `UnitTest_MapSimulator/FieldInteractionRestrictionParityTests.cs` for the positive wedding-family list, a non-family phrase false-positive guard, known-family restriction gating, and meso-drop request payload encoding. Remaining gap: generic field scripts still stop at notice/tag publication, deeper packet/session admission checks remain absent, the fall-damage and death-EXP rules are still simulator-side approximations, packet-authored item-discard echoes are still unmodeled (meso-drop echo is now mirrored), the numeric prompt shell still stops short of exact client layout, and broader android/session-owned restrictions outside the current equipment/runtime seams remain open. | The field-rule branch is already close enough that the remaining risk is mostly correctness drift in the shared restriction classifiers and simulator-owned authority seams. Keeping wedding-invitation detection purely WZ-backed removes heuristic false positives from a data-driven `fieldLimit` rule, and mirroring the client-shaped meso-drop request keeps local drop flow aligned with packet-owned utility transport without inventing a separate drop-dispatch owner. Remaining risk is now concentrated in item-discard packet parity plus deeper packet/session and formula/layout recovery. | `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.PacketOwnedLocalOverlayParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.FieldRestrictionParity.cs`, `MapSimulator.EquipmentWindowParity.cs`, `MapSimulator.Content.cs`, `MapSimulator.CashServiceStageParity.cs`, `Companions/PetController.cs`, `Interaction/SocialListRuntime.cs`, `UI/Windows/InventoryUI.cs`, `UI/Windows/UIWindowManager.cs`, `UI/Windows/EquipUIBigBang.cs`, `UI/PickupNoticeTextFormatter.cs`, `FieldRuleRuntime.cs`, `FieldRuleEffectApplier.cs`, `FieldEntryRestrictionEvaluator.cs`, `FieldEnvironmentEffectEvaluator.cs`, `FieldSkillRestrictionEvaluator.cs`, `FieldInteractionRestrictionEvaluator.cs`, `FieldDropRequestEvaluator.cs`, `FieldFallingDamageEvaluator.cs`, `FieldDeathPenaltyEvaluator.cs`, `TeleportItemUsageEvaluator.cs`, `UI/Windows/InventoryItemMetadataResolver.cs`, `UI/Windows/MapTransferUI.cs`, `UnitTest_MapSimulator/FieldInteractionRestrictionParityTests.cs`, `UnitTest_MapSimulator/FieldDeathPenaltyEvaluatorParityTests.cs`, WZ `Item/Etc/0421.img`, WZ `Item/Cash/0509.img`, WZ `Item/Cash/0525.img`, String WZ `Etc.img/4031377`, `4031395`, `4031406`, `4031407`, `UI/UIWindow.img/UtilDlgEx`, `UI/UIWindow2.img/UtilDlgEx`, WZ `Item/Install/0301/03010029/info/tamingMob`, field systems (`CField::IsFearEffectOn`, `CField::OffFearEffect`), client seam `CWvsContext::SendDropMoneyRequest` (`0x9f6650`) |

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned drop-pool parity | The simulator now has an explicit packet-owned drop runtime instead of leaving `CDropPool` implied behind local drop helpers. A dedicated `RemoteDropPacketRuntime` plus `RemoteDropPacketCodec` model the client-owned `322 / 324` dispatch surface on the existing MapSimulator seams, `DropPool` can apply packet-authored enter and leave packets directly, field rebinding clears only packet-authored drops, and a focused `/droppacket` command can drive the same enter/leave path for manual verification (now including optional packet `elevateLayer` and `expireRaw` when creating synthetic enter packets, a `/droppacket clock` UTC/FILETIME server-time anchor for packet item expiry checks, and client-shaped `petIndex` wording for synthetic pet-pickup leaves). The packet-backed `DropPool` side stores client-facing ownership/source metadata, now resolves enter start positions through live local-player, local-pet, remote-user, remote-pet slot-offset, and mob source positions when those runtimes are available, and now prefers that live source position even when the packet already carries `pt1`, matching the scanned `CDropPool::OnDropEnterField` mob-source override before falling back to authored packet start or the client-shaped `pt1 = pt2` target fallback when no packet start point is present. It also handles packet-owned direct player/pet/mob pickup removal through the existing pickup-notice and recent-history callbacks, and now treats `DropOwnershipType.Party` owner ids as the client packet party id first, using packet-owned `OnPartyResult` social-list state to compare against the local `m_nPartyID` equivalent before falling back to the older tracked-party roster inference, now also accepting packet-owned party-roster member ids even when no explicit packet party id has been observed, plus packet-authored remote-user party helper markers for simulator-only party actors. `DropItem` update remains aligned toward scanned `CDropPool::Update` timing: packet-owned drops honor packet create delay before settle and fake-vs-real state, follow client-shaped parabolic and settle branches derived from scanned `pt1 / pt2` math, now retire fake packet drops once they settle or once `tCur - tCreateTime > 3000`, hold packet-authored `324` reason `0` in a one-second fade, now freeze packet-owned non-meso animated item visuals on that fade so the current frame holds through removal instead of continuing to advance, and resolve pickup leaves (`2 / 3 / 5`) through local player/pet, remote-user, and mob position seams for absorb-style removal motion. This run also closes the remaining delayed-create presentation mismatch that was still visible in `CDropPool::Update`: packet enter type `3` now stays hidden while `tCur <= tCreateTime`, then starts falling with a client-shaped one-second alpha ramp instead of appearing fully opaque the moment the synthetic packet is applied. Local packet pet pickups now resolve that pet slot back through the live active-pet runtime so recent-history, pickup callbacks, absorb-target positioning, and the pre-absorb pet-loot sound branch all use the client-shaped local pet path instead of misreading the packet tail as a pet id or collapsing onto the generic pickup callback alone; remote packet pet pickups now encode owner plus slot through the existing remote-pet pickup naming seam so remote labels resolve against tracked remote pet slots instead of treating the packet tail as a remote pet runtime id, and packet-owned remote pet pickup motion now reuses the same remote-pet slot-offset seam already used by simulated remote pet looting so absorb-style removal aims at the tracked remote pet slot instead of always collapsing to the owner position when that owner/slot pair is known. Packet-owned absorb-style removal is also closer to the recovered `RegisterAbsorbItemAnimation` ownership model now: once a packet leave resolves a player, pet, or mob target, the in-flight pickup keeps a live target-position resolver instead of only snapshotting the actor position at packet-apply time, so local pet, remote pet, and player-owned absorb motion can continue aiming at the moving actor through the pickup window. Packet leave reason `4` (explode) is also closer to client cadence: it keeps delayed explode scheduling, raises a dedicated packet-explode callback on actual delayed removal, throttles explode callback cadence at the client-shaped `90 ms` pool level, and resolves its presentation through the same WZ/client-backed `Skill/421.img/skill/4211006/hit/*` and `Sound/Skill.img/4211006/Hit` seam the scan pointed at, including distance-scaled playback, instead of falling back to the older generic explosion effect path when those assets are available. Packet enter timing is also tighter against `CDropPool::Update` now: animated packet-owned drops no longer advance WZ frames while the client-style `tCreateTime` delay is still pending, delayed packet enter type `1` still raises the throttled drop-enter sound callback at the client-shaped `300 ms` cadence when creation promotes into falling state, packet-owned animated visuals restart their frame playback from frame `0` when the delayed enter actually activates so the first visible frame lines up with the decompiled `Animate(GA_REPEAT)` transition instead of burning animation time while still queued, and delayed type `3` drops now use the same promotion tick to begin their alpha reveal instead of rendering at full opacity during the queue window. WZ-backed `Item/Special/0900.img/09000000..09000003/iconRaw/*` money frames are pushed through the shared `DropPool` resource seam so packet-authored meso drops no longer fall back to static icons, and the packet-owned meso render pass now keeps client-shaped banded layer counts and timing closer to `CDropPool::MakeMoneyAnimation`: the layered draw helper still uses the scanned `80 ms / 120 ms` cadence split with `2 / 3 / 4` visible layers across the small, medium, large, and bag money bands, while respecting the WZ `iconRaw/*/origin` anchors during layered draws and driving those layers through per-band alpha / zoom / lift envelopes instead of a single generic pulse. The packet `elevateLayer` tail byte is retained and honored by a dedicated elevated draw pass, and packet-owned non-meso visuals are closer to the client `CAnimationDisplayer::LoadLayer` branch now too: when `ItemInfo` exposes `info/drop`, the simulator routes that property through the shared `MapSimulatorLoader.LoadFrames` seam instead of recursively flattening nested canvases by hand, so packet-authored item drops honor the same numeric-frame traversal, per-frame `delay` metadata, and WZ canvas `origin` handling already used across other animated simulator layers before falling back to static inventory icon art when no client drop art exists. The non-meso packet enter path is also tighter against the scanned `CDropPool::OnDropEnterField` tail now: `RemoteDropPacketCodec` keeps the client 8-byte item `m_dateExpire` payload on `322`, `DropPool` consumes that packet-authored FILETIME through a packet-expiry clock resolver instead of always forcing the simulator's generic two-minute lifetime; the MapSimulator drop-packet command can anchor that resolver to a server UTC/FILETIME reference for manual skew checks while defaulting to host UTC when no packet/server clock is supplied, packet-owned `CStage::OnSetField` handling now also auto-seeds that clock from the recovered `141` server FILETIME when one is present so live packet expiry no longer depends solely on a manual `/droppacket clock` step, field rebinding now preserves that `SetField`-seeded server clock only when the simulator actually lands in the matching target field and clears stale `SetField` anchors on unrelated field binds instead of letting an old map's server time leak into later packet-expiry resolution, and already-expired packet item drops now retire immediately through the existing fade/removal path instead of lingering as long-lived simulator drops. Authoritative same-`dropId` enter promotion is closer to `CDropPool::OnDropEnterField` now as well: when a fake packet-owned drop is later re-entered as a real drop, the existing runtime entry keeps its in-flight object identity but refreshes ownership, source, pet-loot permission, elevated-layer state, expiry, and meso/item presentation metadata from the authoritative packet instead of only flipping `IsReal`. Pickup admission is also tighter against the recovered `CDropPool::TryPickUpDrop`, `CDropPool::TryPickUpDropByPet`, and `CDropPool::TryPickUpDropByMob` seams now: local player, pet, remote-player, remote-pet, and mob pickup scans no longer stop at simulator radial range checks, but keep that distance gate only as a broad cull before requiring the client-shaped pickup rectangles recovered from IDA (`x +/- 25`, `y - 50 .. y + 10` for player/pet and `x +/- 20`, `y - 40 .. y + 10` for mob), while held local pickup repeats now use the recovered client `30 ms` cadence instead of the older pickup-animation-duration throttle. This pass closes the remaining remote-player branch of that pickup-rectangle contract, so simulated remote-player pickup now rejects in-range drops outside the client player rectangle before continuing in pool order. This run also closes the pickup candidate ordering mismatch: those local, pet, remote-player, remote-pet, and mob pickup scans now walk the simulator pool list in client order and accept the first eligible drop instead of sorting eligible candidates by nearest distance after the rectangle test. This pass narrows the pickup admission surface again by feeding WZ-authored `info/pickUpBlock` metadata through the shared inventory-item metadata resolver into `DropPool`, so local player, local pet, remote-player, remote-pet, and mob scans now skip client pickup-blocked item drops in the same pool-order pass before accepting an otherwise in-rectangle candidate, matching the `CItemInfo::IsPickUpBlockItem` gate recovered in `CDropPool::TryPickUpDrop`, `TryPickUpDropByPet`, and `TryPickUpDropByMob`. Focused source-side coverage now includes `RemoteDropPacketParityTests` on disk for the client party-id ownership resolver, the no-explicit-party-id tracked-roster fallback, party-owned `DropPool` packet pickup, packet FILETIME expiry resolution, fake-to-real promotion, local pet-pickup actor resolution, packet remove-fade animation freeze, delayed type-`3` create-delay plus alpha-ramp timing, live-source start override plus unresolved-source type-`2` `pt1 = pt2` start fallback, fake-drop retirement through settle or timeout, official `SetField(141)` server-FILETIME decode, field-bind clock-anchor retention versus stale-anchor clearing, party-helper marker fallback when no packet party id is known, and the client pickup-rectangle / `30 ms` held-pickup gating plus first-eligible pool-order pickup selection and `pickUpBlock` candidate skipping in `DropPoolPickupParityTests`, including explicit remote-player rejection of in-range drops outside the client player rectangle. Remaining gap: broader owner-group arbitration still remains approximate when no packet-owned party id has been observed and neither side can be mapped onto the packet-owned party roster, a tracked remote runtime, or a packet-authored party helper marker; exact COM-owned `GR2D::LoadLayer` / `RemoveCanvas` / `InsertCanvas` choreography is still approximated, especially for packet meso layers and the remaining non-meso removal-frame presentation details, even though the simulator now follows the same shared WZ frame loader and per-frame delay/origin path; remote pet-owned pickup motion still has to fall back to the remote-owner position whenever the packet/runtime surface cannot be mapped onto a tracked remote owner plus pet slot or when no independently tracked remote pet world state exists beyond that slot-offset approximation. | Without an explicit `CDropPool` row, drop parity is easy to overstate once pickup text, local loot, or pet auto-loot exists. The client keeps a dedicated pool owner for enter/leave/update/pickup behavior, so tracking it separately keeps future work anchored to the real field-entity runtime instead of scattering it across UI feedback, pet logic, or generic map-object handling. | field drop/runtime layer (`DropPool.cs`, `PacketOwnedMesoAnimationPresentation.cs`, `RemoteDropPacketCodec.cs`, `RemoteDropPacketRuntime.cs`, `MapSimulator.RemoteDropPackets.cs`, `MapSimulator.RemoteDropPacketEffects.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.PacketOwnedStageTransitionParity.cs`, `PacketStageTransitionRuntime.cs`, `UnitTest_MapSimulator/RemoteDropPacketParityTests.cs`, `UnitTest_MapSimulator/DropPoolPickupParityTests.cs`, `CDropPool::OnPacket`, `CDropPool::OnDropEnterField`, `CDropPool::OnDropLeaveField`, `CDropPool::Update`, `CDropPool::TryPickUpDrop`, `CDropPool::TryPickUpDropByPet`, `CDropPool::TryPickUpDropByMob`, `CDropPool::MakeMoneyAnimation`, `CDropPool::RecordRecentPickupItem`, `CAnimationDisplayer::RegisterAbsorbItemAnimation`, `CStage::OnSetField`, `Item/Special/0900.img/09000000..09000003/iconRaw/*`) |

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive these flows from local input and generic runtime state. It also has dedicated packet-response handlers for chair sit ack or correction, same-field teleport completion, and quest-result or quest-timer application.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned quest-result parity | The simulator now has a dedicated local `OnQuestResult` packet seam on opcode `242` instead of relying only on local quest helpers or the older field-owned timer path. The dispatcher decodes the client-confirmed quest-timer families (`case 6` through `9`, plus expiry/reset handling from `17` and `18`) into the existing `PacketFieldStateRuntime` timer owner, routes quest-result subtype `10` through a WZ-backed quest presentation builder that reads quest metadata already loaded from `Quest/QuestInfo.img`, `Quest/Say.img`, and `Quest/Act.img`, applies the existing job or gender reward filters when building follow-up text, keeps the client-observed subtype `10` ordering by deferring the visible quest-act notice until after any util-dialog pages close, and still bridges the trailing follow-up quest id into an actual packet-owned `StartQuest` call once that modal returns. The packet decode is now aligned with the direct client scan as well: subtype `10` no longer accepts simulator-only text-kind or view-mode bytes, requires the client-shaped trailing follow-up quest id after the quest and NPC ids, chooses the start-vs-end WZ branch from the same quest-record-present check that `CUserLocal::OnQuestResult` uses instead of treating completed quests as a separate presentation family, and the fresh `CUserLocal::OnQuestResult` re-check at `0x914080` confirms that this owner really only switches over `case 6` through `18` before tail-calling `CWvsContext::CheckNewQuestAvailable(0)`. The subtype `10` continuation order now stays closer to the recovered client flow too without leaving the seam: the local fade-window owner is now registered for every subtype `10` packet, not only modal presentations, so both non-modal and modal branches issue the client-shaped `DeleteFadeWnd(7, 0, questId, "", 0)` cleanup through the same packet-owned runtime before the trailing follow-up quest id is consumed; modal packets still preserve that trailing two-byte payload in raw form until the util dialog returns instead of consuming it eagerly during the initial packet pass. Coverage is broader than before too: subtype `10` and subtype `12` now both build their visible notice through a shared client-shaped quest-act summary path instead of the older full reward dump, the act summary collapses rewarded items into the same inventory-category family the client assembles from `Quest/Act.img` after applying the same job or gender filters, excludes the simulator-only cash-tab summary that the client branch never appends, and follows the recovered client `StringPool` format-id structure more closely by modeling the category labels (`10`, `6791`, `11`, `6712`), the shared separator (`3293`), the inventory-summary wrapper (`3292`), and the negative-meso wrap (`4552`) that `CUserLocal::OnQuestResult` uses around the `Quest/Act.img` reward-category flow. WZ inspection also keeps the authored side honest here: representative `Quest/Act.img` branches such as `1001/1` still publish mixed reward families (`exp` plus `item`), so the packet-owned notice builder has to stay tied to the client's item-category surface instead of dumping every authored action line. The shared builder now matches that client boundary inside the existing seam: subtype `10`/`12` act notices no longer fall back to simulator-owned EXP/fame/message/detail lines when no inventory-category summary exists, no longer append those supplemental lines after a valid inventory-category notice, and now return no visible notice at all when the filtered `Quest/Act.img` reward set does not resolve to any eligible inventory category. The packet owner now keeps that no-visible-output path client-shaped too: once subtype `10` decodes successfully, the seam still treats it as handled and still reaches the packet-owned availability refresh tail even when the filtered WZ presentation resolves to no modal pages, no visible notice, and no trailing follow-up quest id. Direct MapleStory.exe `StringPool::ms_aString` recovery closes the remaining text gap for the covered families too, and the simulator now pins those verified strings through `MapleStoryStringPool` overrides instead of trusting the generated table order: subtype `10`/`12` notices use the exact client wrapper text (`3292 = "%s item inventory is full."`, `3293 = " or"`, `4552 = "Either you don't have enough Mesos or %s"`), subtype `17` expiry chat formats the recovered client template from `0x1015`, and fixed-notice subtypes `11`, `13`, `15`, and `16` still resolve to the client strings behind `0xCDA`, `0x1A8B`, `0xD97`, and `0xD98`. The quest-result notice surface is narrower and more accurate now too without leaving the existing seam: packet-owned quest-result notice families that land on `CUtilDlg::Notice` reuse the WZ-backed `UI/UIWindow(.img|2.img)/UtilDlgEx/notice` window through the existing reward-notice host, but the auto-separated flag now stays split by the recovered client family instead of being flattened. Subtype `10` and fixed-notice subtypes `11`, `13`, `15`, and `16` stay on `bAutoSeparated = 0`, while the direct `CUserLocal::OnQuestResult` re-check still shows subtype `12` reaching `CUtilDlg::Notice(..., 1, 0)`, so the shared semantics keep that quest-act summary on the same notice surface with `bAutoSeparated = 1`. The visible modal surface is closer now too without leaving the existing seam: `PacketQuestResultUtilDialog` already loaded the recovered `UI/UIWindow(.img|2.img)/UtilDlgEx` top, center, bottom, separator, and navigation button canvases, and this pass extends that same WZ-backed surface by loading the `UtilDlgEx/bar` name-strip art, rendering the speaker name through that bar inside the portrait column instead of as bare text, making the previously cosmetic `keyFocused` button art actually track packet-owned keyboard focus, animating the speaker portrait from WZ-backed NPC frames instead of pinning the overlay to a single `stand/0` bitmap, and routing packet-owned button activation through an explicit util-dialog modal-result seam that mirrors the recovered `CDialog::DoModal` return split (`0x2000` = prev, `0x2001` = next / OK, anything else = close). The speaker lane now reads `Npc/<id>.img/say/speak/*` action aliases first, falls back to `stand` or `action` when those aliases are missing, loads the resolved numeric frame sequence through the existing WZ seam, advances it on the authored per-frame `delay` values (falling back to the client NPC default cadence), and anchors each frame through its WZ `origin` inside the portrait column so client-authored idle or talk loops no longer jitter or collapse back to a static sprite. Left-arrow now shifts focus to `BtPrev` when a previous page exists, right-arrow or `Tab` restores focus to `BtNext` or the final `BtOK`, `Shift+Tab` moves back onto `BtPrev`, `Enter` activates the currently focused packet-owned button through that shared modal-result seam, and `Esc` now leaves through the same packet-owned close-result path instead of bypassing the util-dialog owner logic. The shared util-dialog session runtime is closer to the client owner lifecycle as well: the overlay now keeps explicit per-page allocation and destruction counts for `Prev`, `Next/OK`, and close results so the simulator can distinguish page-to-page modal recreation from terminal close paths instead of treating the packet dialog as a single undifferentiated page stack. This pass also wires the existing packet quest-result modal-host runtime into that same subtype `10` path: opening WZ-backed modal pages now begins a tracked `CUtilDlgEx`-shaped host with quest id, speaker npc id, and page count, the main update loop pumps that host while the packet-owned dialog is visible to approximate the recovered `CWvsApp::Run(&m_bTerminate)` owner, page-to-page `Prev` and `Next` results close and recreate the tracked host with the recovered `CDialog::DoModal` return codes (`0x2000` / `0x2001`), and terminal OK or close results end the host before the packet continuation consumes the trailing follow-up quest id. The client tail-call is closer too: successful packet-owned quest-result application snapshots the available-quest set before decode, compares it against the rebuilt available-quest set after the packet-owned handler finishes, and flags newly surfaced quests through the existing quest alarm/update seam so the simulator mirrors `CWvsContext::CheckNewQuestAvailable(0)` more closely than the earlier detail-window-only refresh. The subtype `10` modal-close path now keeps that tail behavior on dismiss as well as complete: deferred post-dialog notice dispatch, `DeleteFadeWnd(7, 0, questId, "", 0)` cleanup, and availability refresh now still run after `Close`/`Esc`, while the trailing follow-up quest id continues to gate `StartQuest` to the client-shaped `0x2001` (`Next`/`OK`) modal result only. Focused source coverage for this pass lives in `UnitTest_MapSimulator/PacketOwnedQuestResultClientSemanticsTests.cs`, pinning the subtype `10` modal-dismiss availability-refresh disposition and the completed-versus-dismissed follow-up continuation split. Remaining gap: subtype `10` still relies on the simulator's tracked, pumped overlay host instead of constructing the native blocking `CUtilDlgEx` / `CDialog::DoModal` pair and running the real `CWvsApp::Run(&m_bTerminate)` loop, so any lifecycle behavior that depends on that native blocking owner rather than the shared packet seam may still differ. | Quest parity is not only local accept/complete helpers, quest windows, or generic field timers. The client owns a packet-response dispatcher that adds and removes quest timers, shows quest-result notices and modal pages from WZ-backed quest data, can emit fixed or act-driven quest-result notices, can auto-start a follow-up quest after the modal returns, and then refreshes quest availability from that same packet seam. Tracking this row against the dedicated opcode `242` owner keeps the remaining work anchored to the real response path instead of scattering it across local quest commands or field-state utilities. | packet-owned local quest-result layer (`CUserLocal::OnPacket`, `CUserLocal::OnQuestResult`, `CWvsContext::AddQuestTimer`, `CWvsContext::RemoveQuestTimer`, `CWvsContext::CheckNewQuestAvailable`, `CWvsContext::StartQuest`, `CWvsContext::DeleteFadeWnd`, `CUtilDlgEx`, `CDialog::DoModal`, `Quest/QuestInfo.img`, `Quest/Say.img`, `Quest/Act.img`, `UI/UIWindow.img/UtilDlgEx`, `UI/UIWindow2.img/UtilDlgEx`, `MapSimulator.PacketOwnedQuestResultParity.cs`, `MapSimulator.UpdateLoop.cs`, `HaCreator/MapSimulator/Interaction/NpcInteractionOverlay.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultClientSemantics.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultUtilDialogLayout.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultUtilDialogModalHostRuntime.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultFadeWindowRuntime.cs`, `UnitTest_MapSimulator/PacketOwnedQuestResultClientSemanticsTests.cs`) |
| Implemented | Packet-owned chair sit-result parity | The simulator now has a dedicated local `OnSitResult` packet seam on opcode `231` instead of treating chairs as local-only intent. The packet-owned handler stamps the shared local utility request tick, mirrors the client-owned `CWvsContext` chair bookkeeping in the packet-owned local-utility context by clearing the chair exclusive-request flag at `+0x20B8` and restamping the exclusive-request tick at `+0x20BC`, decodes the client-shaped success flag plus seat index payload, resolves the current map's WZ `seat/<index>` vector, validates the local player against the same client chair rectangle (`x +/- 10`, `y +/- 30`), and routes successful placement through a packet-owned local sit placement helper that also raises a dedicated secure sit confirmation flag on the local player. Successful packet-owned placement now clears any attached local passenger linkage through the existing follow-runtime and remote-user seams before the sit is finalized, matching the client path that zeroes `m_dwPassenserID` and the passenger's driver link. Invalid returned seats or rectangle mismatches now force the same stand-up correction path and send that correction through the shared local-utility context owner instead of a raw timestamp: the simulator still records a client-shaped `CWvsContext::SendGetUpFromChairRequest(0)` outpacket with opcode `45` and payload `FFFF`, re-arms the exclusive-request flag after emission, suppresses the send when the local HP gate or exclusive gate would block it, and injects that raw opcode `45` packet through a configured `LocalUtilityOfficialSessionBridgeManager` owner when a live Maple socket is attached. That bridge seam is broader now too: `/localutility session start ...` and `/localutility session startauto ...` no longer behave as one-shot manual helpers only. They persist their direct-proxy or discovery configuration onto the packet-owned local-utility owner, the simulator now keeps that bridge armed from the main update loop alongside the inbox, auto-refreshes discovery while no Maple client is attached, preserves an already attached live session instead of tearing it down during discovery refresh, reports outbound chair corrections in bridge status with pending and queued counts plus the last injected and last deferred raw packets, and now keeps a built-in deferred outbound queue for chair correction packets whenever the official-session bridge is enabled but no Maple session has finished attaching yet. Those deferred packets survive bridge re-arming and discovery refresh, then flush through the live Maple socket as soon as the proxied session completes the init handshake, so packet-owned chair corrections no longer depend on an external loopback consumer to reach the official-session transport path. The no-live-session fallback still remains the dedicated `LocalUtilityPacketTransportManager` loopback outbox on `/localutility outbox [status|start [port]|stop]`, which emits raw outbound corrections as `packetoutraw` lines containing the decrypted opcode-framed Maple packet, records the last emitted raw chair correction on that outbox owner, and lets packet-owned chair corrections leave the simulator whenever no live Maple bridge is attached but an external loopback consumer is listening. That outbox owner still keeps its own deferred packet-owned queue for chair correction packets when no outbox client is currently attached, flushes queued packets as soon as a loopback client connects (or before the next immediate send when a client is already attached), and reports both sent and pending correction counts in local-utility status so these packet-owned corrections no longer collapse into simulator-only state immediately when transport is temporarily unavailable. Packet rejection still clears the sit state without emitting that correction request, and the rejection teardown now also clears any stale external-owner couple-chair overlay when an active portable chair is unwound through the same local stand-correction path, matching the client-owned `Update(0)` cleanup branch more closely than the earlier partial teardown.g.cs`, `MultiBoard.g.cs`, and `HaCreator_Content.g.cs` missing under `HaCreator/obj/Debug/net10.0-windows`). | Chairs are not only a local toggle or render-state change. The client keeps a packet-owned success/failure and seat-validation branch that decides whether the user really sits, whether a passenger link is broken, and when to force a stand-up correction. Tracking this row against the dedicated opcode `231` seam keeps the remaining work anchored to the real local response owner instead of only local chair commands or remote chair packets. | packet-owned local chair-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnSitResult`, `CWvsContext::SendGetUpFromChairRequest`, WZ `Map/Map*/<map>.img/seat`, packet-owned local sit placement, correction path toward `PacketOwnedLocalUtilityContextState`, `LocalUtilityOfficialSessionBridgeManager`, `LocalUtilityPacketTransportManager`, `UnitTest_MapSimulator/LocalUtilityOfficialSessionBridgeManagerTests.cs`) |
| Implemented | Packet-owned teleport-result parity | Standard same-map portal travel still finishes through the packet-owned local `OnTeleport` seam: visible and hidden portals targeting the current field use the WZ-authored portal `delay` when present, otherwise the client-confirmed 120 ms handoff, stage a pending same-map teleport against the destination portal index, and let the dedicated apply helper own the final landing and `TryRegisterTeleport(..., bForced = 1)` side effects instead of writing position directly from the interact path. Temporary same-map completion stays in that seam too, carrying any resolved real source `pn`, forwarding same-map target `pn` metadata, and using relaxed current-field `CPortalList` coordinate matching so near-equal authored landings can still collapse to a real portal index. Cross-map portal travel remains packet-owned as well: pending teleport targets survive `UnloadMapContent`, reload the new field, try the rebuilt `PortalPool` first by the primary returned `pn`, then by preserved fallback candidate names, and only then fall back to exact-coordinate apply. This pass tightens that post-load collapse order in the existing seam so rebuilt-field completion no longer promotes an arbitrary returned portal index from whole-pool coordinate drift after the named and candidate-based collapse paths fail; ambiguous rebuilt candidate sets and destinations that no longer correspond to any rebuilt `CPortalList` entry now stay coordinate-owned through the packet apply seam instead of stealing a named handoff from whichever live portal happens to sit nearest the stored landing point, and the rebuilt-field seam no longer synthesizes a fresh named handoff from source-side reciprocal or map-only metadata once those preserved target-side collapse paths have already missed. The preserved primary returned portal name is still authoritative again once the rebuilt `PortalPool` can resolve it, even when the stored landing coordinates would have drifted toward another live portal and previously could have stolen the handoff. Preserved candidate destination names still no longer require the raw candidate strings to be unique up front, because after field load the simulator replays those names against the rebuilt `PortalPool`, promotes them into a true returned portal handoff whenever stale candidates drop out to one live portal index, and still replays the preserved landing coordinates against the still-live candidate subset so a rebuilt field can recover one real returned portal index even when multiple preserved destination names survive reload but only one of those live candidates still matches the stored landing point. Standard visible or hidden cross-map portals still preserve WZ-backed reciprocal return-link fallback (`target.tm == currentMap` and `target.tn == source.pn`) plus unique map-only return fallback from the synthetic opcode `113` request owner, and temporary cross-map travel still mirrors that metadata preservation while recovering real current-field source `pn` values through relaxed coordinate matching before target-side metadata recovery. Source-side collapse is tighter in that same packet-owned seam now too: synthetic opcode `113` request building and temporary-portal recovery still start from the full best-distance current-field source `pn` set derived from portal `x`/`y`/`pn`/`tn`/`tm` metadata, but the helper no longer treats each target-side clue as a separate all-or-nothing check. It now keeps that coordinate-authored source candidate set alive, narrows it cumulatively with the explicit target `tn` when present, then with the preserved target candidate names when those survive, and only then replays each surviving current-field source candidate back through the authored target-field portal metadata surface (`tn`, reciprocal `pn`, unique map-only return, and target-side coordinate candidates) before falling back to the older target-only inference or exact-coordinate completion. That closes another packet-owned source-side miss where current-field coordinates plus the preserved target candidate set still left multiple source `pn` values alive until the simulator asked which candidate's own target-field metadata actually matched the resolved landing side; temporary and synthetic recovery now keep the packet-owned named handoff in that case instead of dropping straight to coordinate-owned completion. The same request-building seam is tighter again now when the source side stays genuinely ambiguous even after that replay pass: temporary and synthetic owners preserve the surviving current-field source candidate set, replay those candidates back through authored target `tn` metadata, and carry any narrowed target `pn` or candidate subset forward instead of immediately dropping to exact-coordinate-only landing metadata. Exact-coordinate completion still avoids inventing a fallback target name when the packet-owned seam only knows ambiguous candidates, and outbound opcode `113` routing now keeps the request on the packet-owned official-session seam after the live direct-injection path misses by preferring deferred `LocalUtilityOfficialSessionBridgeManager` ownership before any generic `LocalUtilityPacketTransportManager` `packetoutraw` fallback while still preserving the raw payload and dispatch summary. WZ evidence for the active seam remains the portal metadata fields in `Map/Map0/001000000.img/portal/*/{pn,tn,tm,x,y,delay}` and the general teleport effect in `Effect/BasicEff.img/Teleport`, whose authored animation remains 4 frames at 90 ms each; `Map/Map0/001000000/portal/0` still exposes the same packet-relevant portal metadata surface (`pn = sp`, `tm = 999999999`, `x = 240`, `y = 190`) on the mounted export. Direct client evidence remains `CUserLocal::OnPacket` opcode `234` dispatch into `CUserLocal::OnTeleport`, where the client decodes the success byte, looks up the returned `CPortalList` entry by portal index, writes the landing point from that portal, and then re-enters `TryRegisterTeleport(..., bForced = 1)`. Focused source coverage now on disk in `UnitTest_MapSimulator/PacketOwnedTeleportParityTests.cs` includes the cumulative source-collapse regression where current-field source coordinates, an explicit target `tn`, and preserved target candidate names only become unique when the packet-owned seam intersects all of them in order, the replayed-target-field regressions where multiple coordinate-authored current-field source candidates collapse only after matching each candidate's authored target metadata or reciprocal return link against the resolved landing side, the surviving-source-candidate target recovery regressions where temporary or synthetic request building now replays those still-ambiguous source candidates back across authored target metadata before falling through to coordinate-only completion, the outbound-owner regressions that keep deferred opcode `113` delivery on the official-session seam before the generic outbox, alongside the earlier rebuilt-field primary-name precedence path, rebuilt-field candidate collapse when only one preserved destination still resolves in the live pool, rejection when multiple preserved candidates still resolve after reload, the rebuilt-field collapse path where multiple preserved destination names still resolve live but only one of those candidates still matches the preserved landing coordinates, the new rebuilt-field rejection where only source-side reciprocal metadata still resolves after the preserved target-side collapse paths fail, and the coordinate-only fallback regressions where rebuilt cross-map completion must not promote a named portal from whole-pool coordinates alone or from a non-candidate live portal. | Teleport parity is not only about entering a portal or moving to the right coordinates. The client also has a response-time authority point that applies the actual destination by portal index and records the named teleport handoff after the packet returns. Tracking that seam keeps the remaining parity gap anchored to the real response owner instead of hiding it behind already-implemented local portal interaction flow. | packet-owned local teleport-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnTeleport`, `CPortalList`, `CUserLocal::TryRegisterTeleport`, WZ `Map/Map0/001000000.img/portal/*/{pn,tn,tm,x,y,delay}`, WZ `Effect/BasicEff.img/Teleport`, `MapSimulator.cs`, `MapSimulator.PacketOwnedTeleportParity.cs`, `UnitTest_MapSimulator/PacketOwnedTeleportParityTests.cs`, `LocalUtilityOfficialSessionBridgeManager.cs`, `LocalUtilityPacketTransportManager.cs`) |

### 4. Portal and reactor collision ownership discovered by targeted `CUserLocal` decompile

The latest `CUserLocal` pass exposed one more field-runtime owner split that the backlog was still flattening into generic portal, teleport, and reactor rows.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Portal collision ownership parity | The simulator now owns portal touch through one explicit `CUserLocal::CheckPortal_Collision`-style seam instead of separate visible versus hidden lookup loops. `TryHandlePortalInteractCore` resolves the colliding portal from `PortalPool.FindPortalCollision` using the live local-user physics position, then branches by portal type in that owner: type `9` (`CollisionScript`) now uses a client-shaped collision-script request path instead of falling through the generic transfer helper, building opcode `112` as field key byte plus `EncodeStr(portalName)` plus the live local-user physics x/y shorts, dispatching it through the existing official-session bridge or packet outbox, recording an explicit `CWvsContext::SetExclRequestSent`-style in-flight flag/timestamp, applying the same recovered per-portal delay plus `onlyOnce` suppression keyed to the last collision-source portal, and now re-arming that exclusive sent state from the same collision seam when the local user leaves the original collision-script portal or when the portal-authored delay elapses while no later handoff has consumed the request. This pass narrows that type `9` owner again by feeding the request field-key byte from the existing packet-owned stage-transition seam when `CStage::OnSetField` has already disclosed an authoritative key for the live map, preserving that disclosed key across the stage-transition runtime clear/rebind that happens during map-content bind, and only falling back to the simulator-owned synthetic `0` when no authoritative map-load handoff has populated one yet; type `12` (`CollisionVerticalJump`) now routes through a dedicated vertical-jump branch that follows the recovered `CUserLocal::VerticalJump` helper more closely by queueing `SetImpactNext` with vertical velocity `-2300`, deriving horizontal velocity from live left/right input plus facing (`input * 100` plus `+/-200`), replaying the client-owned visual owner through `Effect/BasicEff.img/VerticalJump` as a one-shot animation attached to the live local-user physics position, feeding the recovered client move-path attribute `24` through the shared `CVecCtrl` move-path owner itself instead of leaving that attribute as simulator-only debug state, and now refusing the branch unless the same inferred live field-interface/vector-control/input ownership is available before impact is applied. The same shared vec-ctrl seam now also carries the default branch's packet-owned forced-teleport registration attribute `4`, and the new `CMovePathClientPacketCodec` serializes the recorded portal-owned movement snapshots with the client-shaped attribute body layouts for both the recovered `24` vertical-jump attribute and the default forced-teleport `4` attribute instead of leaving those attributes only as in-memory state; type `13` (`CollisionCustomImpact`) now mirrors more of the client-side admission, request, and delayed-impact shape instead of behaving like a generic spring everywhere. The type `13` owner is field-gated to the same recovered client family from `CUserLocal::CheckPortal_Collision` at `0x919a10`: `fieldType` `19`, `20`, `21`, `25`, and `32` (`Party Raid`, `Party Raid Boss`, `Chaos Zakum`, `Waiting Party Quest`, and `Hunting Ad Balloon`) plus the explicit `m_dwField == 180000002` fallback case, before the owner evaluates authored `reactorName` or `sessionValueKey`. WZ-backed `reactorName`, `sessionValueKey`, and `sessionValue` metadata still flow through `PortalInstance`, `MapLoader`, map save, and board serialization, and the session-valued branch still uses a client-shaped delayed seam: when a structured session owner is available for the portal-authored pair, the simulator queues a `PendingPortalSessionValueImpact`, emits the client-shaped `CWvsContext::SendRequestSessionValue` outbound request as opcode `191` with `EncodeStr(sessionValueKey)` plus reset byte `0`, and stores the pending impact until a matching client-owned session or field handoff arrives. This row now closes another part of that owner lifecycle inside the existing seam: pending type `13` impacts are no longer released only from the generic structured field-state handoff, because the simulator also decodes and matches the client-shaped packet forms that those admitted owners actually use. Party Raid release coverage inside the same seam still includes direct `OnSessionValue` ingress, packet `149`, and relayed wrapper opcode `163` when they carry the matching key/value pair; Hunting Ad Balloon release coverage now also accepts direct `OnFieldSetVariable` packet `95` and relayed `163 -> 95` in addition to packet `149`; and packet `149` matching now strips client owner-hint prefixes such as `field:` and `session:` before comparing against the authored portal key so wrapper-shaped field/session payloads wake the same pending impact lifecycle instead of falling back to later structured simulator handoff. Structured owner coverage inside that existing seam still includes active `PartyRaidField` owners, Hunting Ad Balloon's authored `balloon_Team = redTeam/blueTeam` pair, and the wrapper-side Chaos Zakum `fire` owner for `Map/Map9/912060200.img/portal/3`; this pass narrows that owner admission again by resolving the pending-impact family in client order, so Hunting Ad Balloon and Chaos Zakum no longer get collapsed into a generic active `PartyRaidField` session owner before the field-specific `balloon_Team` and `fire` seams can claim them. The type `13` lifecycle now keeps the pending store closer to the recovered `CWvsContext::SetImpactNextBySessionValue` shape by replacing any earlier pending portal session-value impact for the live map when a new admitted request is queued, while still pruning stale entries on map mismatch and requiring the decoded packet ingress shape to match the owning family before `SetImpactNext` is released: Party Raid remains session/packet-`149` owned, Hunting Ad Balloon is direct/relayed field-variable packet `95` or packet `149` owned, and Chaos Zakum accepts its wrapper-shaped session/field ingress plus packet `149`. This pass also narrows the type `13` impact owner itself: direct custom impacts and delayed session-value releases now share a recovered `CUserLocal::OnCollisionCustomImpact`-style helper that requires the inferred live field/vector-control owner, records move-path attribute `25` through `CVecCtrl`, queues `SetImpactNext(vx, vy)` instead of applying an immediate simulator-only impulse, and replays the client-resolved StringPool `0xAD7` visual path (`Effect/BasicEff.img/VerticalJump`) from the local-user physics position. The default branch still stays in this same owner too, deciding between same-map registered teleports and cross-map transfer staging before playing the handoff sound. WZ evidence for type `12` remains `Effect/BasicEff.img/VerticalJump`, which publishes a dedicated six-frame effect with authored per-frame origins such as frame `0/origin = (18,72)` and frame `1/origin = (20,77)`; WZ evidence for the type `13` branch remains authored portal nodes such as `Map/Map0/003000000.img/portal/1` and `Map/Map9/912060200.img/portal/3`, which expose `pt = 13` together with authored impact/session metadata, while `Map/Map9/970050010.img/portal/6` and `portal/7` pair `sessionValueKey = balloon_Team` with `sessionValue = redTeam` / `blueTeam` and the same map publishes `info/fieldType = 32` with `info/onUserEnter = Balloon_D_Enter`. Client evidence remains `CUserLocal::CheckPortal_Collision` at `0x919a10`, where type `9`, `12`, `13`, and the default transfer path are split in one function; the type `9` branch constructs `COutPacket(112)`, encodes `get_field()->m_bFieldKey`, the portal name, and the local-user x/y, sends it through `CClientSocket`, then calls `CWvsContext::SetExclRequestSent(1)` and stores the previous portal index, while `CWvsContext::SetExclRequestSent` at `0x49c580` writes the sent flag and current update time; the type `12` branch calls `CUserLocal::VerticalJump`, and the targeted `CUserLocal::VerticalJump` decompile at `0x90f510` shows the client checking the live vec-ctrl interface gate, setting move-path attribute `24`, queueing `SetImpactNext(vx, -2300)`, resolving the `VerticalJump` string-pool path, and spawning the effect before the caller runs the passenger check, while `CVecCtrl::SetMovePathAttribute` at `0x52b000` shows the client recording that attribute through the vec-ctrl move-path owner rather than as a loose side flag and the shared movement packet decode evidence keeps attribute `24` as a no-body element with the common move suffix and attribute `4` as position plus foothold plus the same suffix; `CUserLocal::OnCollisionCustomImpact` at `0x90b750` uses the same vec-ctrl interface gate, records move-path attribute `25`, queues `SetImpactNext(vx, vy)`, and resolves StringPool `0xAD7` to `Effect/BasicEff.img/VerticalJump`; the type `13` branch is admitted only for field types `19`, `20`, `21`, `25`, `32` or field id `180000002`, and session-valued impacts call `CWvsContext::SetImpactNextBySessionValue`; the follow-up decompile at `0x9e52f0` shows that helper calling `CWvsContext::SendRequestSessionValue` and storing the pending key/value plus velocities, while `SendRequestSessionValue` at `0x9e1a90` builds opcode `191` with the session key and reset byte `0`. Focused regression coverage for the recovered type `9` opcode `112` payload shape, the recovered type `9` authoritative-field-key handoff and post-bind persistence, the recovered type `9` exclusive-request release cases, the recovered type `12` horizontal-impact formula, the recovered type `12` live-interface gate, the shared vec-ctrl move-path attribute carry, the client-shaped move-path payload layouts for portal attributes `24`, `25`, and `4`, the type `13` field admission family, the type `13` live-vector-control gate, the `VerticalJump` effect path, the opcode `191` session request shape, and the type `13` release packet shapes now lives in `UnitTest_MapSimulator/PortalCollisionOwnershipParityTests.cs`, including owner-family resolution coverage that locks Hunting Ad Balloon and Chaos Zakum ahead of the generic Party Raid runtime, owner-specific pending-impact compatibility coverage for direct packet `95` Hunting Ad Balloon release, plus the client-shaped single live-map pending-impact replacement rule. Remaining gap: the type `9` owner now consumes `SetExclRequestSent` state from more of the existing handoff lifecycle by treating decoded `CStage::OnSetField` ingress and packet/map lifecycle transfer teardown as full-request consumers (`sent=0` plus reset of the simulator-side request portal/cooldown state), while collision-seam self-release still preserves the recovered per-portal delay gate when no handoff consumed the request. The owner still falls back to the simulator-owned synthetic key before any authoritative stage-transition handoff has populated the live map, and it still does not reconstruct every unrelated client writer that can mutate `CWvsContext` exclusive-request state byte-for-byte. The type `12` branch now records the recovered vec-ctrl move-path attribute through the shared physics path owner, gates the branch on inferred live interface ownership, and can serialize the recovered no-body attribute-`24` move-path element shape, but it still does not reconstruct every `CMovePath::MakeMovePath` flush/random-count/timing detail or the post-jump passenger release byte-for-byte; and the type `13` path now keeps the client-specific owner-family ordering, release packet shapes, single live-map pending-impact replacement, move-path attribute `25`, queued impact, live vec-ctrl gate, and StringPool `0xAD7` visual owner inside the collision seam, but it still models owner-family admission and cancellation with simulator-side state rather than byte-for-byte `CWvsContext` pending-session fields, every packet writer that can clear those fields, and per-field virtual-owner state transitions. | Portal parity is no longer only about later request or response handlers because the simulator now has an explicit collision owner that decides what a portal touch means before teleport handoff. Keeping the type split in this owner makes future parity work land in the same place the client uses for admission, one-shot suppression, custom impact, vertical jump, same-map teleport, and cross-map transfer decisions. This pass narrows the remaining type `13` gap by routing both direct custom impacts and delayed session-value releases through the recovered `CUserLocal::OnCollisionCustomImpact` shape: vec-ctrl attribute `25`, queued `SetImpactNext`, live vector-control gating, passenger cleanup through the existing seam, and the same `Effect/BasicEff.img/VerticalJump` visual owner that StringPool `0xAD7` resolves in the client, while the earlier single live-map pending replacement and owner-family ordering still keep Hunting Ad Balloon's field-owned `balloon_Team` path and Chaos Zakum's `fire` wrapper path from being reduced to a generic active Party Raid runtime. | portal collision field-runtime layer (`CUserLocal::CheckPortal_Collision`, `CPortalList::FindPortal_Collision`, `PortalPool.FindPortalCollision`, `MapSimulator.TryHandlePortalInteractCore`, `CWvsContext::SetExclRequestSent`, `CWvsContext::SetImpactNextBySessionValue`, `CWvsContext::SendRequestSessionValue`, `CUserLocal::VerticalJump`, `CVecCtrl::SetMovePathAttribute`, `CMovePath::MakeMovePath`, `CField::SendTransferFieldRequest`, `CUserLocal::TryRegisterTeleport`, `CUserLocal::OnCollisionCustomImpact`, `CStage::OnSetField`), `CMovePathClientPacketCodec.cs`, `UnitTest_MapSimulator/PortalCollisionOwnershipParityTests.cs` |
| Partial | Reactor collision polling parity | The simulator now owns reactor touch through one explicit once-per-second `CUserLocal::CheckReactor_Collision`-style poll in the main update loop rather than through duplicate simulator-side callsites or immediate local activation. That single tick owner still remembers the last poll time, refreshes a persistent local touched-reactor set through the existing `ReactorPool` seam, and keeps the recovered client contact rule of testing the live local-user vec position against each touch reactor's current bounds instead of intersecting a synthetic avatar rectangle. The transport split also remains packet-shaped in that same seam: enter/leave ownership diffs still emit client opcode `250` as `dwID + touchFlag` through a live reactor official-session bridge when one is attached, defer the same packet-shaped request on that bridge when the bridge is armed but no Maple session has finished attaching yet, and otherwise mirror or queue the same raw opcode `250` through the dedicated reactor touch `packetoutraw` outbox instead of collapsing immediately to simulator-local state. The broader ownership work from the previous pass remains in place too: the main update loop keeps the reactor official-session bridge armed, refreshes discovery, and drains inbound reactor-pool packets; packet-owned enter can still adopt a real packet object id onto a unique still-live WZ-authored local reactor (`template + x/y + flip`, with non-conflicting `name` when present) instead of always spawning a duplicate packet-owned reactor; packet-owned leave/removal still clears touched ownership immediately and drops any still-deferred opcode-`250` queue for the removed `dwID`; the pool-side touch scan still skips `PacketLeavePending` reactors; and late packet-id recovery for an already-touched authored reactor still re-keys the touched set immediately to the recovered packet object id and drains the promoted touch-enter back through the existing opcode-`250` transport path in the same tick rather than waiting for the next one-second poll. This pass keeps the same ownership seam but tightens it again in three places. First, the simulator now exposes the one-second poll rule as a dedicated helper and pins the recovered `>= 1000 ms` gate, including `Environment.TickCount` wraparound behavior, in `UnitTest_MapSimulator/ReactorCollisionOwnershipParityTests.cs`, so the client-owned timing rule is explicit instead of being incidental to duplicated call flow. Second, the authored adoption selector continues to apply the recovered client-backed filters cumulatively in priority order once packet `EnterField` metadata has reduced candidates to `template + x/y + flip (+ name when present)`: exact authored-name match, then packet-name match, then current local-user containment, then current touched-reactor ownership, then the returned initial state, with the stable WZ-authored reactor-order fallback constrained to the already-narrowed subset and the live-candidate gate still rejecting authored slots that are `Destroyed`, `Respawning`, packet-owned, or `PacketLeavePending`. Third, the packet-name signal is now only considered when packet `EnterField` actually carries a non-empty `name`, so unnamed packet enters no longer bias authored adoption toward unnamed WZ slots as a simulator-only discriminator. WZ evidence for the authored side remains map `reactor/*` nodes such as `Map/Map0/001000000.img/reactor/*`, which publish template id, x/y, reactorTime, flip, and name but not any server object id; on the mounted `v95` export, `reactor/0` still exposes `id=1002009`, `x=610`, `y=254`, `reactorTime=90`, `f=0`, and `name`, while `reactor/1` still exposes `id=1002009`, `x=324`, `y=-78`, `reactorTime=100`, `f=0`, and `name`, and the authored node order itself remains explicit in the map. Client evidence remains `CUserLocal::CheckReactor_Collision` at `0x903d20`, where the client stores `m_tLastCheckReactorCollision`, only re-runs the owner when `update_time - last >= 1000`, and then calls `CReactorPool::FindTouchReactorAroundLocalUser`; the pool decompile at `0x6cded0` still iterates the live reactor list, computes bounds from each live layer, tests the local-user vec position, inserts or removes `dwID` from the touched map, and emits `COutPacket(250)` with `Encode4(dwID)` plus `Encode1(touchFlag)`, while `CReactorPool::OnReactorEnterField` at `0x6cf490` still decodes `dwID/template/state/x/y/flip/name`, allocates a live `REACTOR`, loads its layer, and adds it to the live pool rather than binding the id to a non-live authored slot. Remaining gap: purely local map-authored reactors that never receive a packet `EnterField` object id still have no authoritative client request id to emit; when the simulator is running without a live session it still preserves only the latest deferred ownership state rather than the client's exact send timing; and the remaining packet-enter ambiguity class is now smaller again but not gone because the simulator still has no stronger client-backed discriminator than cumulative exact-name/packet-name-when-present/current-position/current-touch/initial-state filtering plus narrowed-subset WZ node order when server object-enter order diverges from authored map order for otherwise identical still-live reactors. | Reactor parity is not only about authored hit props or packet-fed state. The client also owns a local periodic touch-poll seam, so keeping this row tied to one explicit poll owner plus its local touch-set refresh prevents later reactor work from smuggling contact ownership into unrelated packet handlers, duplicate simulator update paths, or local activation shortcuts. The transport side matters too: touch ownership is now preserved across a missing live session through the dedicated reactor bridge queue or reactor touch outbox rather than disappearing into local-only activation as soon as the immediate official-session path is unavailable, the deferred queue now keeps only the latest ownership state per recovered `dwID` so a late bridge attach does not replay stale enter/leave history, packet-owned enter-field traffic can recover the live `dwID` equivalent for unique still-live authored reactors before the touch poll emits enter/leave ownership, that same enter-field seam can also promote an already-touched authored reactor to immediate packet-owned opcode `250` ownership as soon as its real object id is recovered, packet-owned leave-field teardown preserves server ownership by clearing stale local touch claims before the next once-per-second poll and by dropping any still-queued deferred touch ownership for the removed `dwID`, and the poll interval itself is now locked to the recovered client gate instead of being duplicated across two simulator-side update paths. | reactor touch-poll layer (`CUserLocal::CheckReactor_Collision`, `CReactorPool::FindTouchReactorAroundLocalUser`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.cs`, `ReactorPool.cs`, `ReactorPoolOfficialSessionBridgeManager.cs`, `ReactorTouchPacketTransportManager.cs`, `UnitTest_MapSimulator/ReactorCollisionOwnershipParityTests.cs`, WZ `Map/*/reactor/*`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
