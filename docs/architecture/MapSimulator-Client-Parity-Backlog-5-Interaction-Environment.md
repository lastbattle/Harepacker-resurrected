# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/DropPool.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Field and portal parity

- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`

Notes:
`CUserLocal::TryPassiveTransferField` shows that client field transfer is not only explicit portal collision handling; the local-player update loop also retries an up-key style transfer after one-time actions finish when the character is allowed to move. A later IDA pass also shows that packet-authored field utility surfaces are split into their own remote pools rather than hanging off local collision handling: `CAffectedAreaPool::OnPacket` owns affected-area create/remove, `CTownPortalPool::OnPacket` owns town-portal create/remove, and `COpenGatePool::OnPacket` owns Open Gate create/remove.

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`

Notes:
The backlog already covered pickup notices and pet auto-loot side effects, but the broader client owner was still unnamed. IDA shows that drop parity is owned by a dedicated `CDropPool` runtime that handles enter/leave packet dispatch, per-frame drop update, player/pet/mob pickup arbitration, meso animation, and recent-pickup history instead of treating drops as generic field objects plus UI messages.

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive chairs, same-field teleports, and quest results from local intent and generic runtime state. It also has dedicated packet-response handlers that finalize those flows after the request is acknowledged.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Field and portal parity

The old backlog understated what is already present here, but there is still real work left.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Hidden portals | Reveal, hide, alpha fade, and collision gating are implemented | Remove from missing backlog | `PortalPool.cs` (`CPortalList::FindPortal_Hidden`) |
| Implemented | Portal property accessors | `PH`, `PSH`, and `PV` equivalents are implemented | Remove from missing backlog | `PortalPool.cs` |
| Implemented | Portal interaction parity | Standard and hidden portal triggering now share the same same-map delay seam as client-confirmed portal teleports, and Mechanic `Open Portal: GX-9` casts now place WZ-backed temporary linked gates that resolve on `Up` interaction with the client-like 120 ms same-map handoff; town portal / mystic door remains tracked separately below | Portal travel now covers the main visible, hidden, and same-map skill-portal branches instead of reducing open-gate travel to unsupported or generic transitions | `PortalPool.cs`, `MapSimulator.cs`, `TemporaryPortalField.cs` (`CUserLocal::CheckPortal_Collision`, `CUserLocal::TryRegisterTeleport`, `Skill/3510.img/skill/35101005`) |
| Implemented | Passive transfer-field triggers | Portal `Up` requests now latch for a short client-style retry window while one-time actions or temporary movement locks are active, then auto-replay the transfer attempt as soon as the local player can move again | Passive transfer maps and scripted transitions no longer require perfectly timed repeat input after short lockout actions before the field handoff occurs | `MapSimulator.cs`, portal/field transition layer (`CUserLocal::TryPassiveTransferField`) |
| Implemented | Transportation fields | Regular passenger ships now expose a dedicated transport-deck foothold that carries both the player and grounded mob passengers through the existing movement seam, while Balrog-only ship visuals no longer publish a false passenger deck | Transport maps now keep visible deck passengers riding the ship during departures and arrivals instead of leaving the transport system as a mostly visual-only layer | `TransportationField.cs`, `PassengerSyncController.cs`, `MapSimulator.cs` |
| Partial | Dynamic objects / quest layers | Field object drawables now retain WZ `quest` visibility metadata, keep WZ `hide=1` object layers suppressed until a matching quest state or a published object-tag toggle explicitly reveals them, resolve WZ object `tags` through the dedicated published object-state seam instead of the obstacle bucket, ingest authored map-level tagged-object defaults from `publicTaggedObjectVisible` plus the client-data typo variant `pulbicTaggedObjectVisible`, including grouped multi-tag entries that inherit parent `visible/state` flags across nested tag containers even when a parent node also publishes its own `tag`/`tags`, auto-publish quest-owned layer tags from `QuestInfo.img/showLayerTag`, publish entry-owned map script aliases from `info/onUserEnter`, `info/onFirstUserEnter`, and `info/fieldScript` when their WZ script names resolve onto the current map's tagged-object set, load WZ `directionInfo/*/EventQ` trigger points and recurse nested script-name containers inside those `EventQ` nodes before publishing their resolved tags once the local player's hitbox reaches the authored `x`/`y` point, publish portal-owned script aliases when a portal is actually used, parse `Quest/Check.img` `startscript` / `endscript` names so successful NPC quest accept and complete actions publish their resolved tagged-object aliases through the same seam, keep that same quest-script publication alive for both quest-window acceptance and quest-window completion instead of only the NPC-overlay lane, and publish non-quest NPC aliases from `Npc/<id>.img/info/script/*/script` when the player actually opens that NPC interaction, including packet-owned script-message and quest-delivery NPC dialog opens that may only expose a speaker/template id instead of a live field NPC instance. Staged `_01` / `_02` / `_03` style script aliases now retire sibling stage tags from the same camel-cased family when later entry, trigger, portal, quest, NPC, or reactor publications fire, and when WZ only authors the base family tag for a later staged script alias such as `cannon_tuto_02`, the resolver now keeps that base tag published instead of incorrectly retiring it. Remaining gap: script-engine-owned multi-step sequencing is still absent when no WZ-visible alias timing exists; timer and callback chains that only live inside runtime script execution (without surfaced `EventQ`/entry/portal/NPC/quest/reactor alias metadata) remain unmodeled. | Quest-driven and event-driven maps now respect quest state, quest-authored `showLayerTag` reveals, author-authored hidden-layer defaults, inherited grouped tagged-object default states including parent-tag containers that continue into nested inherited children, entry-script publication, WZ-authored `directionInfo/EventQ` trigger points including nested script containers, portal-script-owned tagged-layer publication, quest-owned NPC `startscript` / `endscript` publication from both NPC-overlay and quest-window acceptance/completion flows, non-quest NPC `info/script` publication on talk including packet-owned script-message and quest-delivery dialog opens that only expose an NPC template id, and WZ-authored staged tutorial/event script progression that swaps sibling stage tags through the same published-object seam instead of only ever accumulating visible stages. In this pass the quest-window completion lane now mirrors the quest-window acceptance and NPC-overlay lanes, so both quest completion and reward-choice follow-through keep publishing `endscript` aliases through the same tagged-object seam. Tagged event-map objects still fall back to authored map defaults when runtime overrides are cleared, generic object-state publication no longer has to masquerade as an obstacle toggle, and `directionInfo/EventQ` alias containers now also honor authored delay keys (`delay`, `wait`, `time`, `t`, `startDelay`) by scheduling follow-up publications through the same seam after trigger contact. The remaining gap is now limited to script-engine timer chains that do not surface any WZ-side alias timing metadata. | map/field load path, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketScriptMessageParity.cs`, `QuestRuntimeManager.cs`, `FieldEffects.cs`, `FieldObjectTagStateDefaultsLoader.cs`, `FieldObjectScriptTagAliasResolver.cs`, `FieldObjectDirectionEventTriggerLoader.cs`, `FieldObjectNpcScriptNameResolver.cs`, `ReactorScriptStatePublisher.cs`, `FieldObjectScriptTagAliasResolverTests.cs` |
| Implemented | Town portal / mystic door lifecycle | `Mystic Door` skill casts now create a WZ-backed cross-map door pair, place the return-side door on the target town map's `TownPortalPoint` / `tp` spawn, transfer into town on `Up`, and restore the player to the original cast position when the town-side door is used before expiry | The simulator now covers the visible create, enter, restore, and positioning loop for the client's town-door utility instead of leaving the skill family nonfunctional | `TemporaryPortalField.cs`, `MapSimulator.cs`, portal/field transition layer (`Skill/231.img/2311002/{cDoor,mDoor,common/time}`, `PortalType.TownPortalPoint`) |
| Partial | Remote portal-pool parity | Packet-authored remote portal pools now exist in the simulator runtime instead of being folded into local skill ownership: `330/331` decode and apply remote town-portal create/remove keyed by owner character id, `332/333` decode and apply remote Open Gate create/remove keyed by owner plus first/second slot, packet-owned remote portals clear on field reload, remote Open Gates link and teleport through the existing same-map interaction seam, remote town portals render on their own pool, preserve their packet-authored state byte, add the WZ `Frame` overlay for the nonzero remote-door phase, route into the field's resolved town return target when that destination is available, synthesize the inferred town-side return door back into the original field when that field-side return target is known, cache owner-level field/town Mystic Door metadata so later town-side packets can reuse an earlier observed source-field return, and a `/portalpacket` debug seam now drives the pool with either raw packet payloads or typed create/remove arguments. Remote Mystic Door timing is also closer to `CTownPortalPool`: state-`0` create packets now stay in the WZ-backed `cDoor` opening run for the asset-authored 1700 ms before promoting to the steady door/frame pair for newly seen owners, while follow-up create packets for an already active owner now keep the door in the steady branch instead of replaying the opening run, stable remote door loops now retain their own phase start instead of sharing a global zero-tick cycle, state-`0` remove packets now snapshot the currently visible field/town door canvases plus the field-side `Frame` overlay into a 1000 ms fade instead of fading a freshly restarted animation, and the simulator now keeps per-owner field/town remote-door runtime instances alive across phase updates instead of tearing down and recreating the whole remote Mystic Door pool on each sync. Remote portal ownership is also closer to the client now: remote Mystic Doors keep persistent per-layer drawable slots instead of replacing whole portal drawable arrays, remote Open Gates now keep per-slot runtime instances that mutate between WZ-backed `cDoor`, solo `sDoor`, linked `mDoor`, and remove `eDoor` states without rebuilding the entire pool when a partner gate appears or disappears, town-side-first remote Mystic Doors can now reuse the owner's last observed non-town field position when that observed field's WZ-resolved town return matches the current town map even if no field-side door packet was seen, the destination resolver keeps an exact same-town packet-authored field-side cast position authoritative over later approximate movement observations for that town instead of letting those looser snapshots steal the inferred return point, town-side packet-only inference now promotes the chosen same-town observed source field into reusable owner-town metadata so later create/remove phase churn keeps returning to the same inferred field rather than jumping when newer approximate observations arrive from another matching field, the owner-observation cache now ranks remote-user field-entry and follow-transfer coordinates above generic move snapshots and keeps those higher-authority same-field coordinates authoritative over later weaker movement snapshots from that exact source field, and repeated same-field fallback observations now refresh the stored approximation instead of freezing the first passive movement snapshot forever. Active town-side packet-owned Mystic Doors also stop waiting for later phase churn before they improve: when stronger same-town source evidence arrives after the door is already open, the live remote portal runtime now retargets immediately from an older approximation to the later exact field-side packet cast or to a higher-authority enter-field / follow-transfer observation for that same source field, and that improved live destination is now persisted back into the reusable inferred owner-town metadata immediately instead of waiting for a later create/remove packet to make the stronger or fresher source choice stick. Remaining gap: packet-only town-side streams still cannot recover the exact original cast position when the simulator never witnesses any precise same-town source-field cast, enter-field point, or follow-transfer point for that owner at all; in those cases the runtime still has to fall back to the newest same-field approximation it has for the chosen source field, or remain unresolved when no matching source field was ever observed. | Visible parity no longer stops at self-authored portal utility: other users' Open Gates and packet-owned Mystic Doors now have their own runtime pool, the field-side remote door can hand off into town through the packet seam, inferred town-side doors can return to the original cast field without needing a second authored packet stream, later town-side packets can now recover a previously observed source field via owner metadata caching or a last-observed owner field fallback, remote Mystic Door ownership now uses persistent per-layer runtime slots that mirror the client-side canvas choreography more closely than whole-portal drawable replacement, and remote Open Gate ownership now persists per slot so paired and unpaired visual transitions no longer re-register the whole pool. In this pass the Mystic Door source-field resolver was tightened one step further: packet-only town-side recovery no longer treats all owner-position evidence as equally weak, because remote-user enter-field and follow-transfer packets now outrank passive movement snapshots, those exact same-field observations no longer get displaced by later weaker movement updates from the same field before an exact packet-authored cast supersedes them, repeated same-field fallback observations now keep the approximate return point fresh instead of pinning it to the first passive snapshot seen in that map, already-open town-side packet doors now adopt stronger same-town source evidence immediately instead of waiting for another create/remove packet to refresh their return target, and those live retargets now also refresh the reusable inferred owner-town metadata right away so later weaker cross-field observations cannot snap the door back to an older cached approximation before the next portal phase packet arrives. The remaining work is now limited to exact-cast recovery for packet-only town-side cases where the simulator never witnesses a precise same-town source-field cast, field-entry point, or transfer point and therefore still has to rely on generic movement snapshots or no inferred source field at all. | field portal/runtime layer (`CTownPortalPool::OnPacket`, `COpenGatePool::OnPacket`, local reference seam: `CUserLocal::CheckPortal_Collision`) |
| Partial | Remote affected-area / mist parity | The simulator now has a real packet-owned `328/329` path for affected areas: a dedicated codec decodes create/remove payloads into the remote pool, `/affectedpacket` can drive the same raw-packet seam or build typed test payloads, packet create timing honors the client packet's short start-delay field before the area becomes active, and a dedicated packet-runtime binder clears remote affected areas on field bind instead of depending on unrelated simulator call sites. Hostile gameplay is no longer purely render-only either: remote mob-skill mist packets now reapply mapped mob-skill statuses to the local player while contained, now consult active projected abnormal-status and elemental resistance from friendly support affected areas before those resistible packet-owned mist statuses land, hostile remote player zones no longer stop at poison/burn hand-maps and now reuse the existing `SkillManager` inferred mob-debuff seam for broader freeze/stun/seal/blind/darkness/slow/weakness plus explicit debuff-token and stat-driven mob statuses while contained, and the player-zone classifier uses WZ-loaded support metadata before falling back to hostile handling so friendly remote auras or support fields with positive stat / heal metadata no longer spill into the hostile mob-debuff lane just because they are not one of the already-modeled invincible or recovery cases. Client-confirmed `type=3` affected areas still route through their own packet source kind and load item-backed `darkFog` visuals, and those fog lifetimes now track `CAffectedAreaPool::OnAffectedAreaCreated` more closely by reading `info/time`, nested `info/spec/time`, then root `spec/time` before recursively following WZ item `info/path` links; linked-path extraction now tolerates deeper path forms, linked descriptions now parse multi-unit durations such as `1 min 30 sec`, and the same resolver now also consults non-item WZ targets behind authored `info/path` branches such as `Map/MapHelper.img/...` before falling back to packet remove timing. Invincible and recovery-style remote support zones already used real remote owner resolution against the simulator's remote-user roster plus party list, and the broader friendly support lane now applies those same self-or-party ownership rules with WZ-backed party eligibility from the base zone skill plus any linked `info/affectedSkill` companions, so self-only remote fields no longer leak buffs onto party members while `massSpell` or linked-party aura fields such as Battle Mage support zones still project onto the local player when the owner is an actual party member. Friendly remote support areas with WZ-authored stat payloads continue to project their supported buff gameplay onto the local player while contained through the existing buff seam, including attack, defense, accuracy, avoidability, speed, jump, critical-rate, max-HP/max-MP, primary-stat style bonuses, WZ-authored reduction/resistance and rate payloads such as `damR`, `asrR`, `terR`, `expR`, `dropR`, and `mesoR`, linked support-skill alias payloads that still live in `x` / `y` / `z`, Battle Mage Blue Aura-style linked `partyDamageSharing` protection that now converts the WZ-authored distributed-damage `x` payload into real incoming damage reduction for the local player through the existing buff/combat seam, and support buffs whose authored metadata redirects incoming damage to MP via `switchDamtoMP`, which now consume local MP and only spill the unredirected or unsupported remainder onto HP through the same shared incoming-damage seam instead of remaining HUD-only metadata. The simulator now also merges supportive linked-skill metadata from WZ `info/affectedSkill` branches before classifying or projecting remote support areas, follows those linked branches recursively instead of stopping at the first hop, and carries linked support descriptions and alias-backed buff values into the synthetic support-buff seam, so aura-style fields such as Battle Mage support zones can keep their linked party-targeting and `x` / `y` / `z` stat metadata on the packet-owned affected-area path instead of collapsing to only the base zone node. In this pass those linked/supportive heuristics were extended far enough to keep client-backed mixed Battle Mage aura fields on both gameplay lanes instead of forcing an either-or choice: WZ `String/Skill.img/32120001` and `32120000` now keep their friendly remote support projection while the same packet-owned field still runs the hostile mob-facing lane, so Advanced Yellow Aura can continue to buff the local player while slowing contained mobs and Advanced Dark Aura can continue to project its linked party aura while dealing hostile aura damage to nearby mobs. The projected support-buff resolver now also derives concrete Yellow Aura gameplay from the authored alias payloads instead of leaving them as HUD-only metadata, mapping the WZ-authored active `x` / `y` / `z` values onto movement speed, attack-speed/booster, and dodge/avoidability projection when the linked aura descriptions say those aliases are the client-facing buff stats. Projected `damR` from those remote support areas continues to change real incoming damage through the existing combat seam rather than remaining HUD-only metadata, and projected damage-to-MP support metadata now uses that same seam rather than stopping at temp-stat presentation. Remaining gap: the broader hostile remote player-zone semantics are still simulator-side approximations rather than a full reconstruction of the client's exact affected-area ownership and status rules, support branches whose client behavior needs dedicated mechanics beyond the simulator's current projected buff plus alias-stat plus resistance plus damage-reduction plus damage-to-MP plus party-eligibility surfaces still stop short of full gameplay reconstruction, the remote-owner side of linked damage-sharing fields is still not simulated because the packet-owned area path has no authoritative remote party-HP owner to receive the transferred share, and `type=3` fogs whose lifetime is authored outside reachable WZ time fields, resolvable linked paths, or duration-bearing descriptions still fall back to packet-driven removal timing. | This closes more of the row inside the existing packet and runtime seams: remote affected areas now self-expire from a stronger client-shaped area-buff metadata resolver instead of only a direct `info/time` read by also honoring nested `info/spec/time`, recursive linked-item resolution across deeper `info/path` forms, generic linked-property resolution for non-item WZ path targets, and multi-unit linked-description duration parsing before packet-timing fallback; the player-area dispatcher classifies friendly support zones from WZ-loaded stat and summon metadata before deciding whether to run the hostile mob-debuff lane; and eligible remote support areas now refresh synthetic area-owned buffs through `SkillManager` instead of stopping at classification. In this pass the projected support-buff seam was extended further in eight concrete WZ-backed ways: `info/affectedSkill` support companions are now folded into the remote support projection surface before the simulator decides whether a packet-owned player area is friendly, projected `damR` now feeds real incoming player damage through the combat path instead of exposing damage reduction only as temporary-stat UI, linked support skills now also contribute party-targeting metadata so the remote support owner gate distinguishes self-only fields from `massSpell` or linked-party fields before projecting buffs or healing onto the local player, the projected support-buff runtime now preserves linked `x` / `y` / `z` alias payloads plus merged support descriptions so Battle Mage-style aura fields do not lose their WZ-authored buff values when they are converted into synthetic area-owned buffs, Blue Aura-style linked `partyDamageSharing` metadata now contributes real local incoming-damage reduction by reusing the existing projected support-buff seam instead of stopping at HUD text, authored `switchDamtoMP` support metadata now redirects real incoming player damage into MP through that same seam instead of remaining temp-stat-only metadata, mixed support-plus-hostile remote player areas now keep their friendly support projection while still entering the hostile mob-status lane when the WZ area metadata also carries damage or debuff semantics, and Yellow Aura-style linked area aliases now derive real speed / booster / avoidability gameplay from the linked aura descriptions instead of stopping at generic `x` / `y` / `z` storage. `asrR` / `terR` support metadata continues to flow through the same seam into the player mob-status controller, so real area skills such as `23111005`, `32110000`, `32120000`, and `32120001` can now resist remote mist ailments, reduce incoming damage, buff the local player, and still apply their hostile aura side to contained mobs through the packet-owned area path. The remaining work is narrower and more data-sensitive: full client-backed ownership and targeting semantics for hostile player zones, support branches that require dedicated simulator mechanics beyond the current buff-plus-alias-stat-plus-resistance-plus-damage-reduction-plus-damage-to-MP projection surface, the unsimulated remote-owner share of Blue Aura-style transferred damage, and residual `type=3` duration cases that still live outside recoverable WZ time metadata, linked paths, or duration-bearing description text. | field affected-area/runtime layer (`CAffectedAreaPool::OnPacket`, `CAffectedAreaPool::OnAffectedAreaCreated`, `CAffectedAreaPool::OnAffectedAreaRemoved`, local reference seam: `SkillManager`, packet-owned seam in `AffectedAreaPool`, zone/effect draw path) |
| Partial | Field-specific restrictions and effects | Map entry now surfaces `timeLimit`, `decHP`, `protectItem`, `allowedItem`, ambient `rain` / `snow`, `Unable_To_Jump`, `Unable_To_Fall_Down`, `Unable_To_Use_Teleport_Item`, field-transfer, `Unable_To_Use_Rocket_Boost`, `Unable_To_Open_Mini_Game`, `Unable_To_Use_Pet`, `Auto_Expand_Minimap`, `Parcel_Open_Limit`, `No_Quest_Alert`, `No_Android`, `Unable_To_Change_Party_Boss`, `Drop_Limit`, `No_Damage_On_Falling`, Mystic Door field-limit metadata, positive WZ `moveLimit` counts, and WZ-authored `onUserEnter` / `onFirstUserEnter` / `fieldScript` names through simulator notices, matching `rain` / `snow` map-info flags automatically start the simulator's ambient weather layer on field entry, matching `Auto_Expand_Minimap` fields now force the minimap open on entry through the existing minimap seam, `onFirstUserEnter` notices and tagged-object publication now only fire on the first visit to that map in the current simulator session while `onUserEnter` / `fieldScript` continue to publish every entry, blocked skill casts raise explicit field-limit feedback for both Rocket Booster and Mystic Door map bans, jump-disabled maps now reject jump input with the same field-rule messaging seam, jump-down-disabled maps now reject down-jump input through that same field-rule path, cross-map portal travel respects `Unable_To_Migrate`, `decHP` maps deal periodic environmental damage with a mist cue unless the inventory currently holds a listed `protectItem`, WZ-authored positive `recovery` values now tick HP and MP recovery through the player's existing recover seam every 10 seconds, `timeLimit` maps warn before expiry then transfer to `returnMap` / `forcedReturn`, `allowedItem` maps now reject non-listed inventory-backed item consumption plus setup-chair activation through the same field-rule notice path, cash teleport-rock items now route into the simulator's map-transfer window unless `Unable_To_Use_Teleport_Item` blocks them, WZ-authored numeric `consumeItemCoolTime` values now enforce a shared consume-item cooldown through the same inventory guard seam, pending cross-map transfers now reject under-`lvLimit` characters plus maps marked `partyOnly` or `expeditionOnly` using the simulator's existing party-roster and expedition-search session state with explicit map-restriction notices, positive `moveLimit` values now cap WZ-classified movement-skill casts through the same field-skill rejection seam, additional WZ-backed `fieldLimit` item families now publish entry notices and reject inventory use through the same field-rule path for portal scrolls (`203xxxx`, including the separate specific-scroll bit), summon sacks (`210xxxx`), cash weather (`512xxxx`), lie-detector anti-macro items (`219xxxx`), wedding invitation items, NPC-summon items, pet-item families, and WZ-modeled stat-change consumables that apply temporary buffs, morphs, modeled recovery, or cure effects, and the wedding invitation plus NPC-summon classifiers now also use concrete WZ-backed invitation item families plus WZ `spec/script=summonEventNpc` and broader `spec/npc` metadata instead of relying only on localized text matches. `Unable_To_Open_Mini_Game` now blocks mini-room, personal-shop, and entrusted-shop window surfacing through the simulator's existing social-room launch seams, `Parcel_Open_Limit` now blocks memo-mailbox and quest-delivery window surfacing through the simulator's existing utility-window seams, `No_Quest_Alert` now blocks quest-alarm surfacing through the existing quest-alarm seam, `No_Android` now hides the Big Bang equip window's android pane while active and now also rejects android companion equip/unequip change submissions and delayed resolutions through the existing equipment-change authority seam, `Unable_To_Change_Party_Boss` now blocks party-leader reassignment through the social-list action seam, `Drop_Limit` now rejects local and pet drop looting through the existing pickup-availability path with explicit pickup feedback, `Unable_To_Use_Pet` now suppresses live pet runtime behavior by hiding pet updates/draw, blocking pet speech/slang/command and auto-HP interactions, and preventing pet-food planning while the field rule is active, and player landing now applies a simulator-owned distance-and-impact fall-damage model that is suppressed by `No_Damage_On_Falling` through the same field-limit authority. Actual field-script execution beyond these notice/publication effects, broader server-authored admission state, exact client-owned fall-damage thresholds or formulas, drop spawning or discard restrictions beyond looting, android restrictions outside the currently modeled equipment-pane/equipment-change/runtime seams, and any client-only checks that depend on packet/session context remain unmodeled. | Event and boss maps now expose a broader slice of client field-rule timing, movement locks, migration locks, hazard behavior, periodic recovery maps, ambient field weather, `protectItem` hazard suppression, `allowedItem` item-use gating, teleport-item map bans, several concrete `fieldLimit` item-family bans, auto-expanding minimap maps, Mechanic rocket-mobility bans, mini-room/shop opening bans, parcel and quest-alert utility-window bans, live pet-runtime suppression, numeric consume cooldowns, movement-skill count caps from positive `moveLimit` values, first-visit-only `onFirstUserEnter` behavior, basic level, party, and expedition entry gating, concrete runtime handling for android-tab suppression plus android companion equip/unequip rejection at equipment-change submission and resolution, party-leader change bans, and drop-loot bans through existing UI, social, and pickup seams, and now a landing-based fall-damage gameplay path that honors `No_Damage_On_Falling` instead of leaving that branch as notice-only metadata. This pass further hardens the WZ-backed item-family lane: wedding invitation bans still recognize the concrete invitation IDs and `4211xxx` / `4212xxx` ETC families, and NPC-summon bans now also treat positive WZ `spec/npc` metadata as authoritative instead of only the narrower `spec/script=summonEventNpc` path plus text fallback, which covers the broader `item/Consume/0243` summon catalog and matching cash-service style NPC items more closely. Generic entry-owned field scripts are still not executed beyond notices or tag publication, deeper server or session admission checks are still absent, the fall-damage model is still an approximation rather than a byte-accurate reconstruction of the client rule, and broader android runtime and packet-owned restrictions outside the currently modeled equip-pane/equipment-change seams plus drop spawning or discard restrictions still need dedicated simulator seams. | `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.PacketOwnedLocalOverlayParity.cs`, `MapSimulator.FieldRestrictionParity.cs`, `MapSimulator.EquipmentWindowParity.cs`, `Companions/PetController.cs`, `Interaction/SocialListRuntime.cs`, `UI/Windows/EquipUIBigBang.cs`, `UI/PickupNoticeTextFormatter.cs`, `FieldRuleRuntime.cs`, `FieldRuleEffectApplier.cs`, `FieldEntryRestrictionEvaluator.cs`, `FieldEnvironmentEffectEvaluator.cs`, `FieldSkillRestrictionEvaluator.cs`, `FieldInteractionRestrictionEvaluator.cs`, `FieldFallingDamageEvaluator.cs`, `TeleportItemUsageEvaluator.cs`, `UI/Windows/InventoryItemMetadataResolver.cs`, `UnitTest_MapSimulator/FieldFallingDamageEvaluatorTests.cs`, `UnitTest_MapSimulator/FieldInteractionRestrictionEvaluatorTests.cs`, field systems (`CField::IsFearEffectOn`, `CField::OffFearEffect`) |

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned drop-pool parity | The simulator now has an explicit packet-owned drop runtime instead of leaving CDropPool implied behind local drop helpers. A dedicated RemoteDropPacketRuntime plus RemoteDropPacketCodec model the client-owned 322 / 324 dispatch surface on the existing MapSimulator seams, DropPool can apply packet-authored enter and leave packets directly, field rebinding clears only packet-authored drops, and a focused /droppacket command can drive the same enter/leave path for manual verification (now including optional packet elevateLayer when creating synthetic enter packets). The packet-backed DropPool side stores client-facing ownership/source metadata, resolves enter start positions through live mob source positions when available, handles packet-owned direct player/pet/mob pickup removal through the existing pickup-notice and recent-history callbacks, and consults the existing remote-user/social-list seam so DropOwnershipType.Party honors tracked party members instead of treating every party-owned packet drop as owner-only. DropItem update remains aligned toward scanned CDropPool::Update timing: packet-owned drops honor packet create delay before settle and fake-vs-real state, follow client-shaped parabolic and settle branches derived from scanned pt1 / pt2 math, hold packet-authored 324 reason 0 in a one-second fade, and resolve pickup leaves (2 / 3 / 5) through local player/pet, remote-user, and mob position seams for absorb-style removal motion. Packet leave reason 4 (explode) is now closer to client cadence too: it keeps delayed explode scheduling, raises a dedicated packet-explode callback on actual delayed removal, throttles explode callback cadence at the client-shaped 90 ms pool level, and drives the existing simulator effect/sound seam on explode completion instead of silently disappearing. Packet enter type 1 now also raises a throttled drop-enter sound callback at the client-shaped 300 ms cadence when delayed creation promotes into falling state. WZ-backed item/Special/0900 money frames are pushed through the shared DropPool resource seam so packet-authored meso drops no longer fall back to static icons, packet-owned meso rendering approximates CDropPool::MakeMoneyAnimation with layered compositing plus alpha/zoom-like pulsing, the packet elevateLayer tail byte is retained and honored by a dedicated elevated draw pass, and remote pet-owned leave packets resolve pet labels through the existing remote-user pickup naming seam. Non-meso packet-owned rendering now prefers WZ item info/drop art (recursively loaded into a packet-item visual cache) before falling back to static inventory icon art. Focused lifecycle coverage now includes DropPoolPacketLifecycleParityTests for packet enter-sound cadence and explode callback cadence. Remaining gap: party ownership is still inferred from the simulator local tracked party roster rather than a real packet-authored party-id surface, so broader owner-group arbitration outside local party view is still approximate; packet meso animation and packet-owned non-meso/pickup visuals still approximate client COM-owned GR2D/LoadLayer choreography rather than reproducing exact canvas insertion/alpha/zoom/absorb ownership; and packet explode handling still uses simulator-owned effect/audio playback rather than the client's exact Skill/4211006 hit UOL selection and positional volume path. | Without an explicit CDropPool row, drop parity is easy to overstate once pickup text, local loot, or pet auto-loot exists. The client keeps a dedicated pool owner for enter/leave/update/pickup behavior, so tracking it separately keeps future work anchored to the real field-entity runtime instead of scattering it across UI feedback, pet logic, or generic map-object handling. | field drop/runtime layer (DropPool.cs, RemoteDropPacketCodec.cs, RemoteDropPacketRuntime.cs, MapSimulator.RemoteDropPackets.cs, MapSimulator.ChatCommands.cs, UnitTest_MapSimulator/DropPoolPacketLifecycleParityTests.cs, CDropPool::OnPacket, CDropPool::OnDropEnterField, CDropPool::OnDropLeaveField, CDropPool::Update, CDropPool::TryPickUpDrop, CDropPool::TryPickUpDropByPet, CDropPool::TryPickUpDropByMob, CDropPool::MakeMoneyAnimation, CDropPool::RecordRecentPickupItem) |

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive these flows from local input and generic runtime state. It also has dedicated packet-response handlers for chair sit ack or correction, same-field teleport completion, and quest-result or quest-timer application.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned quest-result parity | The simulator now has a dedicated local `OnQuestResult` packet seam on opcode `242` instead of relying only on local quest helpers or the older field-owned timer path. The dispatcher decodes the client-confirmed quest-timer families (`case 6` through `9`, plus expiry/reset handling from `17` and `18`) into the existing `PacketFieldStateRuntime` timer owner, routes quest-result subtype `10` through a WZ-backed quest presentation builder that reads quest metadata already loaded from `Quest/QuestInfo.img`, `Quest/Say.img`, and `Quest/Act.img`, applies the existing job or gender reward filters when building follow-up text, shows the packet-owned notice through the chat/status seam, opens multi-page quest-result dialogue through the existing NPC interaction overlay using the closer util-dialog presentation style, and now defers the trailing follow-up quest id into an actual packet-owned `StartQuest` bridge by queueing the accept through the existing quest runtime once the quest-result modal closes. The packet decode is closer to the client now as well: subtype `10` no longer accepts simulator-only text-kind or view-mode bytes, requires the client-shaped trailing follow-up quest id after the quest and NPC ids, chooses the start-vs-end WZ branch from the same quest-record-present check that `CUserLocal::OnQuestResult` uses instead of treating completed quests as a separate presentation family, and now mirrors the client modal-return behavior more closely by allowing that queued follow-up `StartQuest` bridge to fire after any real packet-owned util-dialog close rather than only the simulator's earlier "completed" close path. Coverage is broader than before too: subtype `10` and subtype `12` now both build their visible notice through a shared client-shaped quest-act summary path instead of the older full reward dump, the act summary collapses rewarded items into the same inventory-category family the client assembles from `Quest/Act.img` after applying the same job or gender filters, no longer leaks generic EXP/fame/message text into those packet-owned notices, excludes the simulator-only cash-tab summary that the client branch never appends, and now follows the recovered client `StringPool` format-id structure more closely by modeling the category labels (`10`, `6791`, `11`, `6712`), the shared separator (`3293`), the inventory-summary wrapper (`3292`), and the negative-meso wrap (`4552`) that `CUserLocal::OnQuestResult` uses around the `Quest/Act.img` reward-category flow. Direct MapleStory.exe `StringPool::ms_aString` recovery now closes the remaining text gap for the covered families too: subtype `10`/`12` notices use the exact client wrapper text (`3292 = "%s item inventory is full."`, `3293 = " or"`, `4552 = "Either you don't have enough Mesos or %s"`) instead of the earlier simulator-only reward-summary prose, the category labels now use the client-shortened `Eqp`/`Use`/`Setup`/`Etc` wording, subtype `17` expiry chat now formats the recovered client template from `0x1015`, and fixed-notice subtypes `11`, `13`, `15`, and `16` now resolve to the exact client strings behind `0xCDA`, `0x1A8B`, `0xD97`, and `0xD98` instead of approximate English placeholders. This pass also adds an explicit simulator owner for the client-side fade-window teardown that `CUserLocal::OnQuestResult` runs before decoding subtype `10`'s trailing follow-up quest id: the quest-result seam now applies `DeleteFadeWnd(7, 0, questId, "", 0)`-shaped cleanup before reading that id and tracks quest-owned fade-window registration through the packet-result modal open path. Focused `UnitTest_MapSimulator` coverage is now on disk for those recovered formatter and direct-notice strings, the close-behavior rule, and the new fade-window cleanup owner shape (`PacketQuestResultFadeWindowRuntimeTests`). Remaining gap: the modal still reuses the simulator's overlay runtime instead of a dedicated `CUtilDlgEx`-specific surface; and any still-unrecovered `OnQuestResult` semantics outside the now-covered `case 6` through `18` families remain unmodeled. | Quest parity is not only local accept/complete helpers, quest windows, or generic field timers. The client owns a packet-response dispatcher that adds and removes quest timers, shows quest-result notices and modal pages from WZ-backed quest data, can emit fixed or act-driven quest-result notices, can auto-start a follow-up quest after the modal returns, and then refreshes quest availability from that same packet seam. Tracking this row against the dedicated opcode `242` owner keeps the remaining work anchored to the real response path instead of scattering it across local quest commands or field-state utilities. | packet-owned local quest-result layer (`CUserLocal::OnPacket`, `CUserLocal::OnQuestResult`, `CWvsContext::AddQuestTimer`, `CWvsContext::RemoveQuestTimer`, `CWvsContext::CheckNewQuestAvailable`, `CWvsContext::StartQuest`, `CWvsContext::DeleteFadeWnd`, `CUtilDlgEx`, `Quest/QuestInfo.img`, `Quest/Say.img`, `Quest/Act.img`, `MapSimulator.PacketOwnedQuestResultParity.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultFadeWindowRuntime.cs`, `UnitTest_MapSimulator/QuestClientPacketResultNoticeTextTests.cs`, `UnitTest_MapSimulator/PacketQuestResultCloseBehaviorTests.cs`, `UnitTest_MapSimulator/PacketQuestResultFadeWindowRuntimeTests.cs`) |
| Implemented | Packet-owned chair sit-result parity | The simulator now has a dedicated local `OnSitResult` packet seam on opcode `231` instead of treating chairs as local-only intent. The packet-owned handler stamps the shared local utility request tick, mirrors the client-owned `CWvsContext` chair bookkeeping in the packet-owned local-utility context by clearing the chair exclusive-request flag at `+0x20B8` and restamping the exclusive-request tick at `+0x20BC`, decodes the client-shaped success flag plus seat index payload, resolves the current map's WZ `seat/<index>` vector, validates the local player against the same client chair rectangle (`x +/- 10`, `y +/- 30`), and routes successful placement through a packet-owned local sit placement helper that also raises a dedicated secure sit confirmation flag on the local player. Successful packet-owned placement now clears any attached local passenger linkage through the existing follow-runtime and remote-user seams before the sit is finalized, matching the client path that zeroes `m_dwPassenserID` and the passenger's driver link. Invalid returned seats or rectangle mismatches now force the same stand-up correction path and send that correction through the shared local-utility context owner instead of a raw timestamp: the simulator still records a client-shaped `CWvsContext::SendGetUpFromChairRequest(0)` outpacket with opcode `45` and payload `FFFF`, re-arms the exclusive-request flag after emission, suppresses the send when the local HP gate or exclusive gate would block it, and injects that raw opcode `45` packet through a configured `LocalUtilityOfficialSessionBridgeManager` owner when a live Maple socket is attached. That bridge seam is broader now too: `/localutility session start ...` and `/localutility session startauto ...` no longer behave as one-shot manual helpers only. They persist their direct-proxy or discovery configuration onto the packet-owned local-utility owner, the simulator now keeps that bridge armed from the main update loop alongside the inbox, auto-refreshes discovery while no Maple client is attached, preserves an already attached live session instead of tearing it down during discovery refresh, reports outbound chair corrections in bridge status with pending and queued counts plus the last injected and last deferred raw packets, and now keeps a built-in deferred outbound queue for chair correction packets whenever the official-session bridge is enabled but no Maple session has finished attaching yet. Those deferred packets survive bridge re-arming and discovery refresh, then flush through the live Maple socket as soon as the proxied session completes the init handshake, so packet-owned chair corrections no longer depend on an external loopback consumer to reach the official-session transport path. The no-live-session fallback still remains the dedicated `LocalUtilityPacketTransportManager` loopback outbox on `/localutility outbox [status|start [port]|stop]`, which emits raw outbound corrections as `packetoutraw` lines containing the decrypted opcode-framed Maple packet, records the last emitted raw chair correction on that outbox owner, and lets packet-owned chair corrections leave the simulator whenever no live Maple bridge is attached but an external loopback consumer is listening. That outbox owner still keeps its own deferred packet-owned queue for chair correction packets when no outbox client is currently attached, flushes queued packets as soon as a loopback client connects (or before the next immediate send when a client is already attached), and reports both sent and pending correction counts in local-utility status so these packet-owned corrections no longer collapse into simulator-only state immediately when transport is temporarily unavailable. Packet rejection still clears the sit state without emitting that correction request, and the rejection teardown now also clears any stale external-owner couple-chair overlay when an active portable chair is unwound through the same local stand-correction path, matching the client-owned `Update(0)` cleanup branch more closely than the earlier partial teardown.g.cs`, `MultiBoard.g.cs`, and `HaCreator_Content.g.cs` missing under `HaCreator/obj/Debug/net10.0-windows`). | Chairs are not only a local toggle or render-state change. The client keeps a packet-owned success/failure and seat-validation branch that decides whether the user really sits, whether a passenger link is broken, and when to force a stand-up correction. Tracking this row against the dedicated opcode `231` seam keeps the remaining work anchored to the real local response owner instead of only local chair commands or remote chair packets. | packet-owned local chair-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnSitResult`, `CWvsContext::SendGetUpFromChairRequest`, WZ `Map/Map*/<map>.img/seat`, packet-owned local sit placement, correction path toward `PacketOwnedLocalUtilityContextState`, `LocalUtilityOfficialSessionBridgeManager`, `LocalUtilityPacketTransportManager`, `UnitTest_MapSimulator/LocalUtilityOfficialSessionBridgeManagerTests.cs`) |
| Partial | Packet-owned teleport-result parity | Standard same-map portal travel no longer finalizes purely through local simulator movement. When a visible or hidden portal targets the current field and its destination `tn` resolves through the current `PortalPool`, the simulator still uses the WZ-backed portal `delay` (falling back to the client-confirmed 120 ms handoff), stages a pending same-map teleport against the destination portal index, and completes the move through a dedicated local `OnTeleport`-style apply seam instead of writing the final position directly from the interact handler. That packet-owned seam now also owns temporary same-map portal completion: linked portals from `TemporaryPortalField` stage the delayed move through the same apply helper, and the simulator now pre-resolves matching current-field landing coordinates back to a real `PortalPool` index before the delay when possible, then still falls back to coordinate-based resolution inside the apply seam when no immediate index exists. Cross-map portal travel now also finishes through packet-owned teleport bookkeeping rather than stopping at field-load spawn setup alone: the simulator preserves a pending cross-map teleport target across `UnloadMapContent`, reloads the new field, resolves the destination portal name through the freshly built portal list when available, and then applies the post-load landing through the same local teleport-result seam, including request-flag cleanup, completion timestamping, exact portal-coordinate landing, and named `pn` / `tn` handoff capture from the resolved destination portal. Temporary cross-map portal travel now pushes that further too: when a linked temporary portal lands on coordinates that match a real portal entry, the simulator resolves the destination portal name from target-map WZ metadata before unload when possible and, after load, re-checks the rebuilt `PortalPool` by landing coordinates so the packet-owned apply seam can finish through a true portal index instead of exact-coordinate placement. The forced `TryRegisterTeleport(..., bForced = 1)` side effects are broader now too. Resolved portal-index completions and exact-coordinate fallback branches both replay the client-owned 120 ms post-ack cooldown, clear any attached local passenger link through the existing follow/runtime owner, stamp packet-owned move-path attribute `4` plus set-item-background `(1, 1)` bookkeeping, and play the WZ-backed general teleport effect from `Effect/BasicEff.img/Teleport` (4 frames at 90 ms each) through the existing animation-effect seam. Standard visible or hidden portal requests now also stamp simulator-owned request metadata before the ack arrives and record a client-shaped opcode `113` portal-request payload through the teleport packet owner using the current source portal coordinates plus a synthetic field key `0` placeholder whenever the destination can be resolved from current-field or target-map WZ metadata. That outbound request owner no longer stops at simulator-only bookkeeping either: after building the payload, the teleport seam now first tries to inject opcode `113` through the existing `LocalUtilityOfficialSessionBridgeManager` when a live Maple session is attached, falls back to the existing `LocalUtilityPacketTransportManager` `packetoutraw` outbox when a loopback consumer is connected, and otherwise queues deferred bridge or outbox delivery while preserving the raw payload and dispatch summary on the teleport status seam. That destination resolution is broader now: when `tn` lookup fails, cross-map requests additionally try a WZ-backed reciprocal return link (`target.tm == currentMap` and `target.tn == source.pn`) and then a unique map-only return fallback before dropping to non-client-shaped bookkeeping. Temporary portal interaction is also closer now: linked temporary travel now carries the used temporary source coordinates into the teleport seam, pre-resolves any matching real `CPortalList` source portal name from the current field, and emits the same opcode `113`-shaped outbound request when that source name exists (while still using destination portal-name resolution by same-map index or target-map WZ coordinates when available) instead of always clearing the previous outbound request as stale. Remaining gap: synthetic/temporary branches that do not map back to a real source `CPortalList` portal still cannot produce a fully client-shaped request owner, ambiguous target-map fallbacks (multiple reciprocal/map-return candidates) still stay unresolved pre-ack, temporary destinations that do not correspond to any rebuilt `CPortalList` entry after field load still require exact-coordinate fallback instead of a true returned portal index, and when no live session or loopback consumer is available the simulator still relies on deferred generic bridge/outbox queues rather than a teleport-specific official socket owner. | Teleport parity is not only about entering a portal or moving to the right coordinates. The client also has a response-time authority point that applies the actual destination by portal index and records the named teleport handoff after the packet returns. Tracking that seam makes the remaining parity gap explicit instead of hiding it behind the already-implemented local portal interaction flow. | packet-owned local teleport-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnTeleport`, `CPortalList`, `CUserLocal::TryRegisterTeleport`, WZ `Effect/BasicEff.img/Teleport`, `MapSimulator.cs`, `MapSimulator.PacketOwnedTeleportParity.cs`, `LocalUtilityOfficialSessionBridgeManager.cs`, `LocalUtilityPacketTransportManager.cs`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
