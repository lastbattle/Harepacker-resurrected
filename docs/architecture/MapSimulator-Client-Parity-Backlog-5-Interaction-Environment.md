# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/DropPool.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Field and portal parity

- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`

Notes:
`CUserLocal::TryPassiveTransferField` shows that client field transfer is not only explicit portal collision handling; the local-player update loop also retries an up-key style transfer after one-time actions finish when the character is allowed to move. A later IDA pass also shows that packet-authored field utility surfaces are split into their own remote pools rather than hanging off local collision handling: `CAffectedAreaPool::OnPacket` owns affected-area create/remove, `CTownPortalPool::OnPacket` owns town-portal create/remove, and `COpenGatePool::OnPacket` owns Open Gate create/remove.

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`

Notes:
The backlog already covered pickup notices and pet auto-loot side effects, but the broader client owner was still unnamed. IDA shows that drop parity is owned by a dedicated `CDropPool` runtime that handles enter/leave packet dispatch, per-frame drop update, player/pet/mob pickup arbitration, meso animation, and recent-pickup history instead of treating drops as generic field objects plus UI messages.

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive chairs, same-field teleports, and quest results from local intent and generic runtime state. It also has dedicated packet-response handlers that finalize those flows after the request is acknowledged.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Field and portal parity

The old backlog understated what is already present here, but there is still real work left.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Hidden portals | Reveal, hide, alpha fade, and collision gating are implemented | Remove from missing backlog | `PortalPool.cs` (`CPortalList::FindPortal_Hidden`) |
| Implemented | Portal property accessors | `PH`, `PSH`, and `PV` equivalents are implemented | Remove from missing backlog | `PortalPool.cs` |
| Implemented | Portal interaction parity | Standard and hidden portal triggering now share the same same-map delay seam as client-confirmed portal teleports, and Mechanic `Open Portal: GX-9` casts now place WZ-backed temporary linked gates that resolve on `Up` interaction with the client-like 120 ms same-map handoff; town portal / mystic door remains tracked separately below | Portal travel now covers the main visible, hidden, and same-map skill-portal branches instead of reducing open-gate travel to unsupported or generic transitions | `PortalPool.cs`, `MapSimulator.cs`, `TemporaryPortalField.cs` (`CUserLocal::CheckPortal_Collision`, `CUserLocal::TryRegisterTeleport`, `Skill/3510.img/skill/35101005`) |
| Partial | Passive transfer-field handoff parity | WZ re-check still shows the authored transfer restriction for the map half of the seam lives on `info/fieldLimit` rather than any portal-local node; representative exports still publish migrate bans there, for example `Map/Map0/000050000.img/info/fieldLimit = 4`. Client re-check in IDA still shows `CUserLocal::TryPassiveTransferField` at `0x91a2e0` waiting for `CAvatar::GetOneTimeAction() <= -1`, reading the live field-interface readiness bit through `m_pvc.m_pInterface + 416`, and then replaying the full `CUserLocal::HandleUpKeyDown` path rather than a portal-only helper. The simulator now tracks that handoff more closely: `HandlePortalUpInteract` no longer times out the pending passive-transfer retry or require the Up key to stay held after the original keydown, the retry path no longer consults a dedicated vec-ctrl-owned `IsPassiveTransferFieldReady` cache, and the queued retry is now evaluated before the generic portal-interact gate instead of being blocked behind simulator-only checks such as the packet-teleport registration cooldown. Once the one-time action finishes, the simulator re-queries the live field-interface gate inline, replays the full up-key path when that gate reopens, and still clears the pending retry after the same immovable / attract / grounded admission contract the client applies at replay time. The live-interface gate also drops while packet-owned local-follow attachment owns the local player's movement, matching the existing packet-owned teleport and special-field ownership treatment instead of allowing a queued one-time-action retry to re-enter `HandleUpKeyDown` mid-follow. The gate therefore now drops while simulator-owned field transfer seams already own the handoff, including field-rule migrate bans from `info/fieldLimit`, special-field pending transfers / scripted sequences, packet-owned teleport ownership (`_packetOwnedTeleportRequestActive` or `_pendingCrossMapTeleportTarget`), and an attached packet-owned follow driver (`LocalFollowCharacterRuntime.AttachedDriverId`). Focused regression coverage for the queued-retry decision, the interface gate, and the grounded replay contract now lives in `UnitTest_MapSimulator/PassiveTransferFieldHandoffParityTests.cs`, including the attached-follow-driver owner seam. The remaining gap is narrower again: the simulator still models `m_pvc.m_pInterface + 416` through inferred runtime ownership rather than a direct reconstruction of every client writer, so unrecovered field-interface owners beyond the current field-rule, special-field, packet-teleport, attached-follow-driver, and grounded replay seams may still be missing. | Without this row, passive transfer-field behavior can look like a minor portal follow-up even though the client treats it as a separate retry owner tied to movement state, one-time action completion, and vec-ctrl field readiness. Tracking it explicitly here keeps future parity work anchored to the actual handoff gate instead of folding it into unrelated teleport or portal-collision polish. | `MapSimulator.cs`, `Physics/CVecCtrl.cs`, `Fields/FieldRuleRuntime.cs`, `Fields/SpecialFieldRuntimeCoordinator.cs`, `Fields/PassiveTransferFieldReadinessEvaluator.cs`, `Interaction/LocalFollowCharacterRuntime.cs`, `UnitTest_MapSimulator/PassiveTransferFieldHandoffParityTests.cs`, portal/field transition layer (`CUserLocal::TryPassiveTransferField`, `CUserLocal::HandleUpKeyDown`, `CAvatar::GetOneTimeAction`, WZ `Map/*/info/fieldLimit`) |
| Implemented | Transportation fields | Regular passenger ships now expose a dedicated transport-deck foothold that carries both the player and grounded mob passengers through the existing movement seam, while Balrog-only ship visuals no longer publish a false passenger deck | Transport maps now keep visible deck passengers riding the ship during departures and arrivals instead of leaving the transport system as a mostly visual-only layer | `TransportationField.cs`, `PassengerSyncController.cs`, `MapSimulator.cs` |
| Partial | Dynamic objects / quest layers | Field object drawables now retain WZ `quest` visibility metadata, keep WZ `hide=1` object layers suppressed until a matching quest state or a published object-tag toggle explicitly reveals them, and now also let hide-authored quest-only objects become visible on quest-state matches even when the WZ object publishes no `tags` node at all, resolve WZ object `tags` through the dedicated published object-state seam instead of the obstacle bucket, ingest authored map-level tagged-object defaults from `publicTaggedObjectVisible` plus the client-data typo variant `pulbicTaggedObjectVisible`, including numeric boolean leaf entries such as quest-layer ids and grouped multi-tag entries that inherit parent `visible/state` flags across nested tag containers even when a parent node also publishes its own `tag`/`tags`, and now preserve those authored tagged-object-default containers through the `MapLoader` copy seam so the normal `publicTaggedObjectVisible` branch reaches `MapInfo.additionalNonInfoProps` instead of being dropped before the simulator loader can read it. The row also auto-publishes quest-owned layer tags from `QuestInfo.img/showLayerTag`, now reading direct string tags plus numeric and nested tag containers through the same WZ property parser, publishes entry-owned map script aliases from `info/onUserEnter`, `info/onFirstUserEnter`, and `info/fieldScript` when their WZ script names resolve onto the current map's tagged-object set, now using the same shared raw-script parser as the quest and NPC loaders so authored comma/semicolon-delimited alias lists plus quoted or line-broken transport strings still publish every matching tag instead of collapsing into one unmatched literal, loads WZ `directionInfo/*/EventQ` trigger points and recurses nested script-name containers inside those `EventQ` nodes before publishing their resolved tags once the local player's hitbox reaches the authored `x`/`y` point, publishes portal-owned script aliases when a portal is actually used, now splits raw portal script strings through that same parser before publication, and now falls back to a script-type portal's WZ `pn` alias only after the authored `script` alias fails to publish against the current map's tagged-object set. Successful quest accept and complete state changes now publish WZ `Quest/Act.img/*/npcAct` aliases through that same shared parser in addition to `Quest/Check.img` `startscript` / `endscript`, so quest actions that surface their tagged-object alias only on the act branch no longer stop at NPC feedback UI, while the existing quest publication path still stays alive for both quest-window acceptance/completion and the NPC-overlay lane. The seam also continues to publish non-quest NPC aliases from `Npc/<id>.img/info/script/*/script` when the player actually opens that NPC interaction, including packet-owned script-message and quest-delivery NPC dialog opens that may only expose a speaker/template id instead of a live field NPC instance plus packet-owned NPC shop, admin-shop, store-bank, and repair-durability owners that surface only an NPC template id when their utility windows actually open. Quest-script alias resolution is broader now too: when WZ pairs `QuestInfo.img/showLayerTag` numeric tags with `Quest/Check.img` `q<questId>s` / `q<questId>e` script names, the publication seam now falls back from the full quest script name to both the base `q<questId>` alias and the numeric quest-id tag, so cases like `22000` no longer miss their tagged-object reveal because the authored script suffix stayed on the quest-script branch. Staged `_01` / `_02` / `_03` style script aliases now retire sibling stage tags from the same camel-cased family when later entry, trigger, portal, quest, NPC, or reactor publications fire, that staged-family retirement also covers separator-less camelCase or bare trailing-digit aliases such as `aranTutor3` / `cannonTuto2`, and the resolver now applies the same stage fallback and sibling-retirement rules to exact authored `_` / `-` tag families too, so later publications such as `Flag_R_2` can retire `Flag_R_1` while later stage aliases still keep an authored exact base tag like `back_stoner` published when no exact `back_stoner_2` node exists. The shared alias resolver now also strips path-qualified and extension-suffixed script identifiers before matching those same quest/stage families, so `folder/q22000s.js` and `map/cannon_tuto_02.lua` still resolve onto the authored tagged-object set when later runtime or packet seams surface transport-shaped script names instead of the bare WZ alias. Delayed `directionInfo/EventQ` publications now also validate that the authored script alias actually resolves onto the current map's tagged-object set before consuming the one-shot trigger, and the delay parser now accepts integer, floating-point, or numeric-string WZ delay literals on those nested `EventQ` containers before scheduling the follow-up publication, so unrelated or non-matching delayed aliases no longer arm and silently exhaust a trigger without changing any object state while authored numeric delay variants still land on the same publication seam. Remaining gap: script-engine-owned multi-step sequencing is still absent when no WZ-visible alias timing or quest `npcAct` metadata exists; timer and callback chains that only live inside runtime script execution (without surfaced `EventQ`/entry/portal/NPC/quest/reactor alias metadata) remain unmodeled. | Quest-driven and event-driven maps now respect quest state, quest-authored `showLayerTag` reveals including numeric and nested WZ property forms, author-authored hidden-layer defaults, numeric boolean tagged-object defaults such as quest-layer ids, inherited grouped tagged-object default states including parent-tag containers that continue into nested inherited children, and the underlying map-load copy seam now preserves both `publicTaggedObjectVisible` and `pulbicTaggedObjectVisible` containers so those authored defaults reliably survive into the simulator's dynamic-object default loader. Entry-script publication, WZ-authored `directionInfo/EventQ` trigger points including nested script containers, portal-script-owned tagged-layer publication, quest-owned `startscript` / `endscript` publication from both NPC-overlay and quest-window acceptance/completion flows, quest-owned `npcAct` publication from `Quest/Act.img` when successful accept/complete state changes fire, non-quest NPC `info/script` publication on talk including packet-owned script-message and quest-delivery dialog opens that only expose an NPC template id, packet-owned NPC utility owners that reopen the same tagged layers from shop/admin-shop/store-bank/repair template ids, and WZ-authored staged tutorial/event script progression that swaps sibling stage tags through the same published-object seam instead of only ever accumulating visible stages are all in place. This pass also keeps quest-only hide-authored objects on the same visibility seam as tagged objects instead of leaving them permanently suppressed when their authored WZ node omits `tags`, which keeps those layers governed by quest state alone when that is the only authored gate. Raw map-entry, quest, NPC, portal, and quest-action `npcAct` strings now share the same normalized parser, so authored `scriptA,scriptB`, `scriptA;scriptB`, or quoted and line-broken transport strings no longer miss later tagged-object reveals by staying collapsed into one unmatched literal. Script-type portals also now fall back to WZ `pn` publication only when the `script` field has no resolvable tagged-object mutation, which covers authored portal aliases stored as portal names without letting ordinary non-script transfer portal names publish dynamic object states. Path-qualified and extension-suffixed script identifiers now normalize through that same shared resolver before quest/stage matching, so runtime-facing aliases such as `folder/q22000s.js` or `map/cannon_tuto_02.lua` still land on the authored numeric quest-layer and staged tutorial tag families instead of missing them on transport-only naming noise. Quest-script publications still match the common WZ pattern where `QuestInfo.img/showLayerTag` authors a numeric map tag while `Quest/Check.img` uses `q<questId>s` / `q<questId>e`, and WZ `Quest/Act.img/*/npcAct` can mirror or replace that alias on the quest-action branch (for example `Act.img/9539/1/npcAct = q9933e` alongside `Check.img/9933/1/endscript = q9933e`), so quest-window and NPC-driven accept/complete flows now publish onto those tagged-object aliases even when the visible action name only surfaces on the act branch. Delayed `directionInfo/EventQ` alias containers still do not consume their one-shot trigger unless the authored alias resolves onto the map's available tag set, which keeps unmatched delayed script names from blocking later valid trigger contact on the same point. Tagged event-map objects still fall back to authored map defaults when runtime overrides are cleared, generic object-state publication no longer has to masquerade as an obstacle toggle, and `directionInfo/EventQ` alias containers now also honor authored delay keys (`delay`, `wait`, `time`, `t`, `startDelay`) plus numeric-string or float-style delay literals by scheduling follow-up publications through the same seam after trigger contact. The remaining gap is now limited to script-engine timer chains that do not surface any WZ-side alias timing metadata. | map/field load path, `MapLoader.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedNpcUtilityParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketScriptMessageParity.cs`, `QuestRuntimeManager.cs`, `FieldEffects.cs`, `FieldObjectTagStateDefaultsLoader.cs`, `FieldObjectQuestVisibilityEvaluator.cs`, `FieldObjectScriptTagAliasResolver.cs`, `FieldObjectDirectionEventTriggerLoader.cs`, `FieldObjectNpcScriptNameResolver.cs`, `ReactorScriptStatePublisher.cs`, `PacketOwnedNpcUtilityRuntimes.cs`, `UnitTest_MapSimulator/DynamicObjectQuestLayerParityTests.cs`, WZ `QuestInfo.img/showLayerTag`, WZ `Quest/Check.img/*/{startscript,endscript}`, WZ `Quest/Act.img/*/npcAct`, WZ `Map/*/directionInfo/*/EventQ` |
| Implemented | Town portal / mystic door lifecycle | `Mystic Door` skill casts now create a WZ-backed cross-map door pair, place the return-side door on the target town map's `TownPortalPoint` / `tp` spawn, transfer into town on `Up`, and restore the player to the original cast position when the town-side door is used before expiry | The simulator now covers the visible create, enter, restore, and positioning loop for the client's town-door utility instead of leaving the skill family nonfunctional | `TemporaryPortalField.cs`, `MapSimulator.cs`, portal/field transition layer (`Skill/231.img/2311002/{cDoor,mDoor,common/time}`, `PortalType.TownPortalPoint`) |
| Partial | Remote portal-pool parity | Packet-authored remote portal pools remain on their own simulator runtime path instead of folding into local skill ownership: `330/331` decode remote town-portal create/remove by owner character id, `332/333` decode remote Open Gate create/remove by owner plus first/second slot, packet-owned remote portals clear on field reload, remote Open Gates link and teleport through the existing same-map interaction seam, and remote town portals keep their packet-authored state byte, WZ `Frame` overlay, WZ-backed `cDoor` opening timing, stable per-owner phase clocks, and snapshot-based state-`0` removal fade. The pool also keeps persistent per-owner/per-slot runtime instances instead of rebuilding whole portal arrays, so remote Mystic Doors preserve their field/town drawable slots across phase churn and remote Open Gates mutate in place between WZ-backed `cDoor`, solo `sDoor`, linked `mDoor`, and remove `eDoor` states, including same-tick partner-loss resync when one gate is removed. Town-side packet-owned Mystic Doors still reuse field-authored return targets, inferred owner-town metadata, and same-town owner observations, and the town-side inference seam now keeps source choice and same-field coordinate reuse aligned even when cached inferred metadata points at one source field while newer unrelated same-town observations arrive from another: retained owner observations still stay bucketed per owner+town+source field plus authority bucket, the resolver now chooses the strongest same-town source field first, and it then reuses the best retained coordinates on that already chosen field instead of letting cached metadata from another field block a newer same-field approximation. The active town-side destination now also participates in that source-field selection as an inferred source observation, so weaker later movement snapshots from another source field no longer pull an already linked packet-owned Mystic Door away from its current return field while stronger same-town evidence can still replace it, and exact remote Mystic Door skill-use presentations on the source field now feed that same owner-observation seam as precise cast-point observations before the later town-side packet stream arrives. Focused regression coverage is now checked in under `UnitTest_MapSimulator/RemotePortalPoolParityTests.cs`, including skill-cast observation authority, packet-cast precedence over exact skill-use observations, metadata refresh onto exact skill-cast coordinates, metadata-selected source reuse, active-source preservation, stronger-observation replacement, and same-field approximation upgrades through the existing internal test helpers. Remaining gap: packet-only town-side streams still cannot recover the exact original cast point when the simulator never witnesses any precise same-town source-field packet cast, skill-cast presentation, enter-field point, or follow-transfer point for the ultimately chosen source field; those cases now fall back to the best retained same-field approximation when that source field was observed at all, but they still remain unresolved when no matching source field was ever observed. | Visible parity no longer stops at self-authored portal utility: other users' Open Gates and packet-owned Mystic Doors keep their own runtime pool, the field-side remote door can hand off into town through the packet seam, inferred town-side doors can return to the original cast field without needing a second authored packet stream, and remote portal ownership now uses persistent runtime slots that stay closer to the client canvas choreography than whole-pool drawable replacement. This pass closes another exact-source branch in the same-town inference seam too: when the simulator already sees the remote skill-use presentation for Mystic Door on the source field, that exact cast point now outranks later movement approximations for the same source field while packet-authored field-side door creates still remain authoritative over the skill-use observation. The remaining work is limited to packet-only town-side cases where the simulator never witnesses an exact packet-cast, skill-cast, entry, or transfer point for the chosen source field and therefore still cannot recover the true original cast coordinates, plus fully unobserved source fields that still leave the town-side destination unresolved. | field portal/runtime layer (`CTownPortalPool::OnPacket`, `CTownPortalPool::OnTownPortalCreated`, `COpenGatePool::OnPacket`, local reference seam: `CUserLocal::CheckPortal_Collision`, remote skill-use presentation seam), `UnitTest_MapSimulator/RemotePortalPoolParityTests.cs`, WZ `Skill/231.img/skill/2311002/{cDoor,mDoor,Frame}`, WZ `Skill/3510.img/skill/35101005/{cDoor,sDoor,mDoor,eDoor}` |
| Partial | Remote affected-area / mist parity | The simulator keeps the packet-owned `328/329` affected-area runtime, start-delay activation, field-bind clearing, remote support projection, mixed friendly-plus-hostile Battle Mage aura handling, WZ-backed support ownership checks, WZ-backed `type=3` fog visuals and linked-duration resolution, and the existing mob-skill-mist reapplication path. The hostile player-area lane now preserves WZ-authored DOT metadata from `common/dot`, `dotInterval`, and `dotTime` instead of collapsing everything to generic `damage` / `time`, so affected-area wrappers such as `Skill/312.img/skill/3120010` and `Skill/3510.img/skill/35101009` keep their authored burn-style magnitude and timing when the packet-owned area path drives `SkillManager`'s inferred mob-status seam. The resolver also now treats linked hostile companions as part of disposition resolution instead of only the primary shell, and the linked-runtime collector now follows WZ `info/dummyOf` wrapper links in the same seam as `affectedSkill`, so sparse packet skills such as `Skill/3510.img/skill/35100008` can still reach hostile companion data on `35101009` / `35101010` before disposition and inferred mob-status replay run. This pass also consumes that WZ DOT metadata in the packet-owned runtime instead of only carrying it through the inferred-status seam: hostile player-area gameplay now ticks on the fastest reachable positive `dotInterval`, and player-skill affected-area expiration now resolves the maximum reachable WZ `time` / `dotTime` across the visible skill plus its `affectedSkill`, `dummyOf`, and reverse `affectedSkill` parent chain, so packet wrappers with sparse primary nodes can still inherit companion or parent lifetimes such as `Skill/3510.img/skill/35101009/common/dotTime` or parent-authored wrapper durations. The same sparse-wrapper seam now also feeds the visual owner in `AffectedAreaPool`: when the packet-visible skill exposes no usable zone surface, the runtime can recover authored `zoneType` / tile animation from reachable linked or reverse-parent skills before the area is staged, so wrappers such as `Skill/3510.img/skill/35100008` and reverse-parent auras surfaced through `Skill/3210.img/skill/32101002` no longer have to stay gameplay-only while their visible mist or aura tile lives on a linked companion. The team-only ownership hole in the friendly lane is also closed: the existing WZ-backed `SupportsTeamMembers` wording now reaches the runtime because remote support areas no longer hardcode `ownerIsSameTeamMember = false`, and the affected-area owner check now resolves shared-team membership from the battlefield team owner plus the Monster Carnival name-to-team table before the local support, avatar-effect, and invincibility seams decide whether the local player is a valid recipient. The `type=3` fog lane is narrower now too: when no reachable WZ `time` metadata, linked path duration, or duration-bearing item description exists, the area-buff-item resolver now falls back to the authored fog animation cycle itself instead of leaving every such packet-owned fog entirely removal-packet-owned, using the same WZ-authored frame delays already loaded for the visible fog surface. Focused coverage for the fog-duration seam plus linked/reverse-parent sparse-wrapper resolution now lives in `UnitTest_MapSimulator/RemoteAffectedAreaParityTests.cs`. Linked damage-sharing fields now recover reverse WZ `affectedSkill` parents such as `Skill/3211.img/skill/32110000 -> 32101002`, so packet streams that surface the linked aura skill can still project the authored `partyDamageSharing` owner-share metadata before the existing remote-owner damage recipient seam runs. WZ-authored `affectedSkillEffect=amplifyDamage` now also feeds the inferred hostile mob-status resolver instead of depending on `mes` metadata alone, so affected-area wrappers such as `Skill/522.img/skill/5220011` can replay their authored marked-target bonus-damage status through the existing packet-owned hostile area seam. PvP/team fields now also consume WZ `PVPcommon` area metadata in the same runtime seam: when Battlefield or Monster Carnival ownership is active, remote support projection, hostile tick metadata, and fallback damage all prefer authored `PVPcommon` values such as `Skill/3211.img/skill/32110000/PVPcommon/{x,y,z}`, `Skill/3510.img/skill/35100008/PVPcommon/{w,x,y}`, and `Skill/522.img/skill/5220011/PVPcommon/damage` before falling back to regular `common`, and hostile remote player zones no longer stay mob-only in those fields because enemy-owned packet areas can now apply recurring local-player damage through the existing incoming-damage seam when the local avatar is standing inside the zone. Remaining gap: hostile remote player-zone ownership and debuff targeting are still simulator-side approximations rather than a full client-authored reconstruction, so the new enemy-local lane is currently limited to recurring damage in Battlefield / Monster Carnival contexts; support branches that need bespoke simulator mechanics beyond the current projected buff, resistance, cleanse, damage-reduction, damage-to-MP, outgoing-damage, reward-rate, crit-damage, mastery, boss-damage, and ignore-defense seams still stop short of full gameplay; wrappers that still expose no hostile or visual metadata across the visible node, `affectedSkill`, `dummyOf`, or reverse `affectedSkill` parent chain can still under-classify or stay packet-removal-owned for visuals; and `type=3` fogs that expose neither explicit lifetime metadata nor a multi-frame authored fog cycle still continue to rely on packet removal. | Packet-owned remote affected areas now preserve more of the client-authored hostile mist lane instead of degrading DOT-backed area wrappers into rough generic damage ticks. WZ-backed hostile areas with authored DOT payloads now carry their own per-skill magnitude, tick cadence, lifetime, linked visual surface, and PvP/team-field `PVPcommon` overrides into the existing inferred mob-status runtime and affected-area staging seam, while the already-shipped support projection, mist resistance, linked support ownership, linked hostile-branch replay, mixed Battle Mage aura behavior, the battlefield / Monster Carnival team-target ownership checks, the `dummyOf` wrapper traversal, reverse `affectedSkill` parent recovery for linked damage-sharing auras, the new enemy-local-player recurring-damage lane for team fields, and the fog-cycle lifetime fallback all stay in the same seams. This keeps remote areas such as Fire/Poison `3120010`, Mechanic `35101009`, wrapper skills such as `35100008`, reverse-parent auras such as `32101002`, WZ-authored team-targeted support zones, PvP-authored aura numerics such as `32110000/PVPcommon`, and sparse packet-owned `type=3` fogs closer to authored behavior without introducing a separate area-combat system. | field affected-area/runtime layer (`CAffectedAreaPool::OnPacket`, `CAffectedAreaPool::OnAffectedAreaCreated`, `CAffectedAreaPool::OnAffectedAreaRemoved`), `AffectedAreaPool.cs`, `AreaBuffItemMetadataResolver.cs`, `MapSimulator.RemoteAffectedAreaPackets.cs`, `RemoteAffectedAreaSupportResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `MonsterCarnivalField.cs`, `UnitTest_MapSimulator/RemoteAffectedAreaParityTests.cs`, WZ `Skill/312.img/skill/3120010/common/{dot,dotInterval,dotTime}`, WZ `Skill/3510.img/skill/35101009/common/{dot,dotInterval,dotTime}`, WZ `Skill/3510.img/skill/35101010/common/damage`, WZ `Skill/3510.img/skill/35100008/info/dummyOf`, WZ `Skill/3510.img/skill/35100008/PVPcommon/{w,x,y}`, WZ `Skill/3211.img/skill/32110000/info/{affectedSkill,affectedSkillEffect}`, WZ `Skill/3211.img/skill/32110000/PVPcommon/{x,y,z}`, WZ `Skill/3210.img/skill/32101002`, WZ `Skill/522.img/skill/5220011/info/affectedSkillEffect`, WZ `Skill/522.img/skill/5220011/PVPcommon/damage`, WZ `Item/Cash/0501/05010079/effect/darkFog/*/delay`, WZ team-target wording paths consumed by `RemoteAffectedAreaSupportResolver.SupportsTeamMembers(...)`, battlefield team ownership in `BattlefieldField`, and Monster Carnival team ownership in `MonsterCarnivalField` |
| Partial | Field-specific restrictions and effects | Map entry now surfaces `timeLimit`, `decHP`, `protectItem`, `allowedItem`, ambient `rain` / `snow`, `Unable_To_Jump`, `Unable_To_Fall_Down`, `Unable_To_Use_Teleport_Item`, field-transfer, `Unable_To_Use_Rocket_Boost`, `Unable_To_Open_Mini_Game`, `Unable_To_Use_Pet`, `Unable_To_Use_Taming_Mob`, `No_Monster_Capacity_Limit`, `No_EXP_Decrease`, `No_Item_Option_Limit`, `Auto_Expand_Minimap`, `Parcel_Open_Limit`, `No_Quest_Alert`, `No_Android`, `Unable_To_Change_Party_Boss`, `Drop_Limit`, `No_Damage_On_Falling`, Mystic Door field-limit metadata, positive WZ `moveLimit` counts, and WZ-authored `onUserEnter` / `onFirstUserEnter` / `fieldScript` names through simulator notices, matching `rain` / `snow` map-info flags automatically start the simulator's ambient weather layer on field entry, matching `Auto_Expand_Minimap` fields now force the minimap open on entry through the existing minimap seam, `onFirstUserEnter` notices and tagged-object publication now only fire on the first visit to that map in the current simulator session while `onUserEnter` / `fieldScript` continue to publish every entry, blocked skill casts raise explicit field-limit feedback for both Rocket Booster and Mystic Door map bans, jump-disabled maps now reject jump input with the same field-rule messaging seam, jump-down-disabled maps now reject down-jump input through that same field-rule path, cross-map portal travel respects `Unable_To_Migrate`, `decHP` maps deal periodic environmental damage with a mist cue unless the inventory currently holds a listed `protectItem`, WZ-authored positive `recovery` values now tick HP and MP recovery through the player's existing recover seam every 10 seconds, `timeLimit` maps warn before expiry then transfer to `returnMap` / `forcedReturn`, `allowedItem` maps now reject non-listed inventory-backed item consumption plus setup-chair activation through the same field-rule notice path, cash teleport-rock items now route into the simulator's map-transfer window unless `Unable_To_Use_Teleport_Item` blocks them, WZ-authored numeric `consumeItemCoolTime` values now enforce a shared consume-item cooldown through the same inventory guard seam, and tracked cash-side consumables now share that same cooldown instead of leaving the map rule effectively `USE`-only when the simulator routes a cash inventory click through the consumable or pet-food lanes. Pending cross-map transfers now reject under-`lvLimit` characters plus maps marked `partyOnly` or `expeditionOnly` using the simulator's existing party-roster and expedition-search session state with explicit map-restriction notices, teleport-book registration plus its status seam now reuse that same WZ-backed level / party / expedition admission context instead of still advertising or saving destinations the active session cannot legally enter, and the move-facing map-transfer lanes now consume that same context too: cached saved routes, return/forced destinations, portal-authored destinations, and world-map-selected manual targets now carry the resolved admission restriction into the existing destination model, blocked cached destinations disable the move action in the existing map-transfer UI, the selected-route detail line now surfaces the matching restriction text, and direct world-map or manual map-transfer move requests now reject through the existing field-rule message seam before the simulator stages a cross-map handoff. Cached WZ-backed entry gates now also short-circuit more non-UI cross-map seams before the simulator spends a fade/load cycle: portal double-click travel, `Up`-driven portal transfers, temporary-portal cross-map exits, field-timer / special-field return transfers, consumable move-item transfers, and packet-owned whisper-follow transfers now all reuse the same known `lvLimit` / `partyOnly` / `expeditionOnly` admission context when the destination map metadata is already available locally, while uncached destinations still fall back to the existing pending-transfer rejection seam before map content is swapped. Positive `moveLimit` values now cap WZ-classified movement-skill casts through the same field-skill rejection seam, additional WZ-backed `fieldLimit` item families now publish entry notices and reject inventory use through the same field-rule path for portal scrolls (`203xxxx`, including the separate specific-scroll bit), summon sacks (`210xxxx`), cash weather (`512xxxx`), lie-detector anti-macro items (`219xxxx`), wedding invitation items, NPC-summon items, pet-item families, and WZ-modeled stat-change consumables that apply temporary buffs, morphs, modeled recovery, or cure effects, and the wedding invitation plus NPC-summon classifiers now also use concrete WZ-backed invitation item families plus WZ `spec/script=summonEventNpc` and broader WZ NPC-reference metadata (`spec/npc` and `info/npc`) instead of relying only on localized text matches. The pet-item classifier is stricter now too: WZ-backed pet-food `spec/inc` items count as pet interactions even when localized text does not literally say `pet`, so `Unable_To_Use_Pet` no longer depends on name matching to block those feeds. Unsupported cash-inventory clicks for those WZ-backed families now also surface the same field-rule rejection path instead of failing silently when the simulator does not model any additional item runtime beyond the restriction itself, which closes the feedback gap for cash weather (`512xxxx`), pet cash items (`517xxxx` / `518xxxx` / `519xxxx`), and cash wedding invitation cards, and the cash-avatar-preview weather callback now routes through that same cash-weather field-rule message seam instead of staging a preview effect when `Unable_To_Use_Cash_Weather` is active. `Unable_To_Open_Mini_Game` now blocks mini-room, personal-shop, and entrusted-shop window surfacing through the simulator's existing social-room launch seams, including the cash-avatar-preview callbacks that previously opened shop owners directly, `Parcel_Open_Limit` now blocks memo-mailbox and quest-delivery window surfacing through the simulator's existing utility-window seams, including status-bar mailbox launch, memo-send return, packet-authored parcel dialogs, and packet-authored quest-delivery launches, `No_Quest_Alert` now blocks quest-alarm surfacing through the existing quest-alarm seam, `Unable_To_Use_Taming_Mob` now publishes an entry notice, hides the Big Bang mechanic equipment pane while active, disables the ride shortcut button through the existing skill-window shortcut seam, rejects taming-mob / saddle / mechanic companion equipment change submissions and delayed resolutions through the existing equipment-change authority seam, rejects packet-owned mechanic equipment mutations through the same field-rule message, and blocks setup riding-chair activation before it can apply a chair-owned taming-mob mount, `No_Monster_Capacity_Limit`, `No_EXP_Decrease`, and `No_Item_Option_Limit` now surface as entry notices while their deeper monster-capacity, death-EXP, and item-option gameplay remains unmodeled, `No_Android` now hides the Big Bang equip window's android pane while active and now also rejects android companion equip/unequip change submissions, base character `Android` / `AndroidHeart` equip-slot mutations, and delayed resolutions through the existing equipment-change authority seam, `Unable_To_Change_Party_Boss` now blocks party-leader reassignment through the social-list action seam, `Drop_Limit` now rejects local inventory drag-release drop requests plus local and pet drop looting through the existing drag, pickup, and field-rule feedback seams and now also suppresses simulator-owned mob-death reward drop spawning before the drop sound/spawn path while leaving EXP award and packet-owned remote drop entries untouched, the local player-drop lane now normalizes through a shared `FieldDropRequestEvaluator` contract before spawning the world drop so the `Drop_Limit` rejection path and positive stack-count normalization live in one seam, `Unable_To_Use_Pet` now suppresses live pet runtime behavior by hiding pet updates/draw, blocking pet speech/slang/command and auto-HP interactions, and preventing pet-food planning plus the remaining WZ-backed pet-feed feedback command path while the field rule is active, and player landing now applies a simulator-owned distance-and-impact fall-damage model that is suppressed by `No_Damage_On_Falling` through the same field-limit authority. Actual field-script execution beyond these notice/publication effects, broader server-authored admission state beyond the currently WZ-backed level / party / expedition gates, exact client-owned fall-damage thresholds or formulas, quantity-prompted discard flows, meso-drop requests beyond the shared evaluator seam, server-authored discard echoes, android restrictions outside the currently modeled equipment/runtime seams beyond the shared equipment-change authority path, and any client-only checks that depend on packet/session context remain unmodeled. | Event and boss maps now expose a broader slice of client field-rule timing, movement locks, migration locks, hazard behavior, periodic recovery maps, ambient field weather, `protectItem` hazard suppression, `allowedItem` item-use gating, teleport-item map bans, several concrete `fieldLimit` item-family bans, auto-expanding minimap maps, Mechanic rocket-mobility bans, taming-mob / mechanic equipment and riding-chair bans, passive notices for no-monster-capacity-limit, no-EXP-decrease, and no-item-option-limit maps, mini-room/shop opening bans, parcel and quest-alert utility-window bans, live pet-runtime suppression, numeric consume cooldowns, movement-skill count caps from positive `moveLimit` values, first-visit-only `onFirstUserEnter` behavior, basic level, party, and expedition entry gating, concrete runtime handling for android-tab suppression plus android companion equip/unequip rejection at equipment-change submission and resolution, party-leader change bans, and drop-loot bans through existing UI, social, and pickup seams. This pass closes the remaining simulator-owned `Drop_Limit` spawn gap in the same authority path the row already described: mob-death reward spawn now exits through `ShouldSpawnSimulatorOwnedMobDeathDrops(MapInfo)` before any meso or item drop is created, so EXP award and packet-owned remote drop entries remain untouched while simulator-owned reward drops obey the same field-limit bit the local drag-drop and pickup seams already used. The surrounding WZ-backed item-family, utility-window, pet-runtime, map-transfer, and equipment restrictions from earlier row work remain in place, and focused regression coverage for the drop admission branch now lives on disk in `UnitTest_MapSimulator/FieldDropRequestParityTests.cs` and `UnitTest_MapSimulator/FieldInteractionRestrictionParityTests.cs`, covering `Drop_Limit` rejection, invalid request rejection, positive stack-count normalization, meso-drop evaluator admission/rejection, and the simulator-owned mob-reward spawn gate instead of leaving those contracts implicit inside `MapSimulator.cs`. Generic entry-owned field scripts are still not executed beyond notices or tag publication, deeper server or session admission checks are still absent, monster-capacity, death-EXP, and item-option behavior beyond the passive notices remains unmodeled, the fall-damage model is still an approximation rather than a byte-accurate reconstruction of the client rule, `No_Android` still stops short of broader client-owned android session rules outside the current equip-pane/equipment/runtime seams, and the drop lane still lacks the client's quantity-prompted stack discard, meso discard UI, packet-authored discard-echo handling, and a surfaced local meso-drop UI flow on top of the shared evaluator seam. | `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.PacketOwnedLocalOverlayParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.FieldRestrictionParity.cs`, `MapSimulator.EquipmentWindowParity.cs`, `MapSimulator.Content.cs`, `MapSimulator.CashServiceStageParity.cs`, `Companions/PetController.cs`, `Interaction/SocialListRuntime.cs`, `UI/Windows/InventoryUI.cs`, `UI/Windows/UIWindowManager.cs`, `UI/Windows/EquipUIBigBang.cs`, `UI/PickupNoticeTextFormatter.cs`, `FieldRuleRuntime.cs`, `FieldRuleEffectApplier.cs`, `FieldEntryRestrictionEvaluator.cs`, `FieldEnvironmentEffectEvaluator.cs`, `FieldSkillRestrictionEvaluator.cs`, `FieldInteractionRestrictionEvaluator.cs`, `FieldDropRequestEvaluator.cs`, `FieldFallingDamageEvaluator.cs`, `TeleportItemUsageEvaluator.cs`, `UI/Windows/InventoryItemMetadataResolver.cs`, `UI/Windows/MapTransferUI.cs`, `UnitTest_MapSimulator/FieldDropRequestParityTests.cs`, `UnitTest_MapSimulator/FieldInteractionRestrictionParityTests.cs`, WZ `Item/Install/0301/03010029/info/tamingMob`, field systems (`CField::IsFearEffectOn`, `CField::OffFearEffect`) |

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned drop-pool parity | The simulator now has an explicit packet-owned drop runtime instead of leaving `CDropPool` implied behind local drop helpers. A dedicated `RemoteDropPacketRuntime` plus `RemoteDropPacketCodec` model the client-owned `322 / 324` dispatch surface on the existing MapSimulator seams, `DropPool` can apply packet-authored enter and leave packets directly, field rebinding clears only packet-authored drops, and a focused `/droppacket` command can drive the same enter/leave path for manual verification (now including optional packet `elevateLayer` and `expireRaw` when creating synthetic enter packets, a `/droppacket clock` UTC/FILETIME server-time anchor for packet item expiry checks, and client-shaped `petIndex` wording for synthetic pet-pickup leaves). The packet-backed `DropPool` side stores client-facing ownership/source metadata, now resolves enter start positions through live local-player, local-pet, remote-user, remote-pet slot-offset, and mob source positions when those runtimes are available, and now prefers that live source position even when the packet already carries `pt1`, matching the scanned `CDropPool::OnDropEnterField` mob-source override before falling back to authored packet start or the generic target-offset fallback. It also handles packet-owned direct player/pet/mob pickup removal through the existing pickup-notice and recent-history callbacks, and consults the existing remote-user/social-list seam so `DropOwnershipType.Party` honors tracked party members instead of treating every party-owned packet drop as owner-only. `DropItem` update remains aligned toward scanned `CDropPool::Update` timing: packet-owned drops honor packet create delay before settle and fake-vs-real state, follow client-shaped parabolic and settle branches derived from scanned `pt1 / pt2` math, now retire fake packet drops once they settle or once `tCur - tCreateTime > 3000`, hold packet-authored `324` reason `0` in a one-second fade, now freeze packet-owned non-meso animated item visuals on that fade so the current frame holds through removal instead of continuing to advance, and resolve pickup leaves (`2 / 3 / 5`) through local player/pet, remote-user, and mob position seams for absorb-style removal motion. This run also closes the remaining delayed-create presentation mismatch that was still visible in `CDropPool::Update`: packet enter type `3` now stays hidden while `tCur <= tCreateTime`, then starts falling with a client-shaped one-second alpha ramp instead of appearing fully opaque the moment the synthetic packet is applied. Local packet pet pickups now resolve that pet slot back through the live active-pet runtime so recent-history, pickup callbacks, absorb-target positioning, and the pre-absorb pet-loot sound branch all use the client-shaped local pet path instead of misreading the packet tail as a pet id or collapsing onto the generic pickup callback alone; remote packet pet pickups now encode owner plus slot through the existing remote-pet pickup naming seam so remote labels resolve against tracked remote pet slots instead of treating the packet tail as a remote pet runtime id, and packet-owned remote pet pickup motion now reuses the same remote-pet slot-offset seam already used by simulated remote pet looting so absorb-style removal aims at the tracked remote pet slot instead of always collapsing to the owner position when that owner/slot pair is known. Packet-owned absorb-style removal is also closer to the recovered `RegisterAbsorbItemAnimation` ownership model now: once a packet leave resolves a player, pet, or mob target, the in-flight pickup keeps a live target-position resolver instead of only snapshotting the actor position at packet-apply time, so local pet, remote pet, and player-owned absorb motion can continue aiming at the moving actor through the pickup window. Packet leave reason `4` (explode) is also closer to client cadence: it keeps delayed explode scheduling, raises a dedicated packet-explode callback on actual delayed removal, throttles explode callback cadence at the client-shaped `90 ms` pool level, and resolves its presentation through the same WZ/client-backed `Skill/421.img/skill/4211006/hit/*` and `Sound/Skill.img/4211006/Hit` seam the scan pointed at, including distance-scaled playback, instead of falling back to the older generic explosion effect path when those assets are available. Packet enter timing is also tighter against `CDropPool::Update` now: animated packet-owned drops no longer advance WZ frames while the client-style `tCreateTime` delay is still pending, delayed packet enter type `1` still raises the throttled drop-enter sound callback at the client-shaped `300 ms` cadence when creation promotes into falling state, packet-owned animated visuals restart their frame playback from frame `0` when the delayed enter actually activates so the first visible frame lines up with the decompiled `Animate(GA_REPEAT)` transition instead of burning animation time while still queued, and delayed type `3` drops now use the same promotion tick to begin their alpha reveal instead of rendering at full opacity during the queue window. WZ-backed `Item/Special/0900.img/09000000..09000003/iconRaw/*` money frames are pushed through the shared `DropPool` resource seam so packet-authored meso drops no longer fall back to static icons, and the packet-owned meso render pass now keeps client-shaped banded layer counts and timing closer to `CDropPool::MakeMoneyAnimation`: the layered draw helper still uses the scanned `80 ms / 120 ms` cadence split with `2 / 3 / 4` visible layers across the small, medium, large, and bag money bands, while respecting the WZ `iconRaw/*/origin` anchors during layered draws and driving those layers through per-band alpha / zoom / lift envelopes instead of a single generic pulse. The packet `elevateLayer` tail byte is retained and honored by a dedicated elevated draw pass, and packet-owned non-meso visuals are closer to the client `CAnimationDisplayer::LoadLayer` branch now too: when `ItemInfo` exposes `info/drop`, the simulator routes that property through the shared `MapSimulatorLoader.LoadFrames` seam instead of recursively flattening nested canvases by hand, so packet-authored item drops honor the same numeric-frame traversal, per-frame `delay` metadata, and WZ canvas `origin` handling already used across other animated simulator layers before falling back to static inventory icon art when no client drop art exists. The non-meso packet enter path is also tighter against the scanned `CDropPool::OnDropEnterField` tail now: `RemoteDropPacketCodec` keeps the client 8-byte item `m_dateExpire` payload on `322`, `DropPool` consumes that packet-authored FILETIME through a packet-expiry clock resolver instead of always forcing the simulator's generic two-minute lifetime; the MapSimulator drop-packet command can anchor that resolver to a server UTC/FILETIME reference for manual skew checks while defaulting to host UTC when no packet/server clock is supplied, packet-owned `CStage::OnSetField` handling now also auto-seeds that clock from the recovered `141` server FILETIME when one is present so live packet expiry no longer depends solely on a manual `/droppacket clock` step, and already-expired packet item drops now retire immediately through the existing fade/removal path instead of lingering as long-lived simulator drops. Authoritative same-`dropId` enter promotion is closer to `CDropPool::OnDropEnterField` now as well: when a fake packet-owned drop is later re-entered as a real drop, the existing runtime entry keeps its in-flight object identity but refreshes ownership, source, pet-loot permission, elevated-layer state, expiry, and meso/item presentation metadata from the authoritative packet instead of only flipping `IsReal`. Focused source-side coverage now includes `RemoteDropPacketParityTests` on disk for packet enter FILETIME round-trip, server-clock-referenced expiry calculation and expiry application, fake-to-real packet enter promotion metadata refresh for both item and meso drops, local packet pet-pickup pre-sound dispatch and pet-slot resolution, packet-authored reason `0` remove-fade animation freeze, delayed packet-owned type `3` hide-and-fade activation, delayed packet-owned animation activation through the public `DropPool` packet seam, and recovered `SetField` server-FILETIME drop-clock resolution, alongside the existing live-source enter start override and client-shaped fake-drop retirement branches. Remaining gap: party ownership is still inferred from the simulator local tracked party roster rather than a real packet-authored party-id surface, so broader owner-group arbitration outside local party view remains approximate; exact COM-owned `GR2D::LoadLayer` / `RemoveCanvas` / `InsertCanvas` choreography is still approximated, especially for packet meso layers and the remaining non-meso removal-frame presentation details, even though the simulator now follows the same shared WZ frame loader and per-frame delay/origin path; remote pet-owned pickup motion still has to fall back to the remote-owner position whenever the packet/runtime surface cannot be mapped onto a tracked remote owner plus pet slot or when no independently tracked remote pet world state exists beyond that slot-offset approximation; and packet enter starts still fall back to the generic target-position offset when the packet source id cannot be resolved onto any live local, local-pet, remote-user, remote-pet, or mob runtime. | Without an explicit `CDropPool` row, drop parity is easy to overstate once pickup text, local loot, or pet auto-loot exists. The client keeps a dedicated pool owner for enter/leave/update/pickup behavior, so tracking it separately keeps future work anchored to the real field-entity runtime instead of scattering it across UI feedback, pet logic, or generic map-object handling. | field drop/runtime layer (`DropPool.cs`, `PacketOwnedMesoAnimationPresentation.cs`, `RemoteDropPacketCodec.cs`, `RemoteDropPacketRuntime.cs`, `MapSimulator.RemoteDropPackets.cs`, `MapSimulator.RemoteDropPacketEffects.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.PacketOwnedStageTransitionParity.cs`, `PacketStageTransitionRuntime.cs`, `UnitTest_MapSimulator/RemoteDropPacketParityTests.cs`, `CDropPool::OnPacket`, `CDropPool::OnDropEnterField`, `CDropPool::OnDropLeaveField`, `CDropPool::Update`, `CDropPool::TryPickUpDrop`, `CDropPool::TryPickUpDropByPet`, `CDropPool::TryPickUpDropByMob`, `CDropPool::MakeMoneyAnimation`, `CDropPool::RecordRecentPickupItem`, `CAnimationDisplayer::RegisterAbsorbItemAnimation`, `CStage::OnSetField`, `Item/Special/0900.img/09000000..09000003/iconRaw/*`) |

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive these flows from local input and generic runtime state. It also has dedicated packet-response handlers for chair sit ack or correction, same-field teleport completion, and quest-result or quest-timer application.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned quest-result parity | The simulator now has a dedicated local `OnQuestResult` packet seam on opcode `242` instead of relying only on local quest helpers or the older field-owned timer path. The dispatcher decodes the client-confirmed quest-timer families (`case 6` through `9`, plus expiry/reset handling from `17` and `18`) into the existing `PacketFieldStateRuntime` timer owner, routes quest-result subtype `10` through a WZ-backed quest presentation builder that reads quest metadata already loaded from `Quest/QuestInfo.img`, `Quest/Say.img`, and `Quest/Act.img`, applies the existing job or gender reward filters when building follow-up text, keeps the client-observed subtype `10` ordering by deferring the visible quest-act notice until after any util-dialog pages close, and still bridges the trailing follow-up quest id into an actual packet-owned `StartQuest` call once that modal returns. The packet decode is now aligned with the direct client scan as well: subtype `10` no longer accepts simulator-only text-kind or view-mode bytes, requires the client-shaped trailing follow-up quest id after the quest and NPC ids, chooses the start-vs-end WZ branch from the same quest-record-present check that `CUserLocal::OnQuestResult` uses instead of treating completed quests as a separate presentation family, and the fresh `CUserLocal::OnQuestResult` re-check at `0x914080` confirms that this owner really only switches over `case 6` through `18` before tail-calling `CWvsContext::CheckNewQuestAvailable(0)`. The subtype `10` continuation order now stays closer to the recovered client flow too without leaving the seam: non-modal packets clear the quest-result fade owner before decoding the trailing quest id, modal packets preserve that trailing two-byte payload in raw form until the util dialog returns, and only then decode the client-shaped follow-up quest id after `DeleteFadeWnd(7, 0, questId, "", 0)` succeeds instead of consuming it eagerly during the initial packet pass. Coverage is broader than before too: subtype `10` and subtype `12` now both build their visible notice through a shared client-shaped quest-act summary path instead of the older full reward dump, the act summary collapses rewarded items into the same inventory-category family the client assembles from `Quest/Act.img` after applying the same job or gender filters, excludes the simulator-only cash-tab summary that the client branch never appends, and follows the recovered client `StringPool` format-id structure more closely by modeling the category labels (`10`, `6791`, `11`, `6712`), the shared separator (`3293`), the inventory-summary wrapper (`3292`), and the negative-meso wrap (`4552`) that `CUserLocal::OnQuestResult` uses around the `Quest/Act.img` reward-category flow. WZ inspection also keeps the authored side honest here: representative `Quest/Act.img` branches such as `1001/1` still publish mixed reward families (`exp` plus `item`), so the packet-owned notice builder has to stay tied to the client's item-category surface instead of dumping every authored action line. The shared builder now matches that client boundary more closely inside the existing seam: subtype `10`/`12` act notices no longer fall back to simulator-owned EXP/fame/message/detail lines when no inventory-category summary exists, no longer append those supplemental lines after a valid inventory-category notice, and now return no visible notice at all when the filtered `Quest/Act.img` reward set does not resolve to any eligible inventory category. Direct MapleStory.exe `StringPool::ms_aString` recovery closes the remaining text gap for the covered families too: subtype `10`/`12` notices use the exact client wrapper text (`3292 = "%s item inventory is full."`, `3293 = " or"`, `4552 = "Either you don't have enough Mesos or %s"`), subtype `17` expiry chat now formats the recovered client template from `0x1015`, and fixed-notice subtypes `11`, `13`, `15`, and `16` resolve to the exact client strings behind `0xCDA`, `0x1A8B`, `0xD97`, and `0xD98`. The quest-result notice surface is closer now too without leaving the existing seam: packet-owned quest-result notice families that land on `CUtilDlg::Notice` now reuse the WZ-backed `UI/UIWindow(.img|2.img)/UtilDlgEx/notice` window through the existing reward-notice host while explicitly disabling auto-separated wrapping (`bAutoSeparated = 0`), so subtype `10`, `12`, `11`, `13`, `15`, and `16` preserve the client-authored line-break shape instead of falling back to the simulator's generic wrapped notice layout. The visible modal surface is closer now too without leaving the existing seam: `PacketQuestResultUtilDialog` already loaded the recovered `UI/UIWindow(.img|2.img)/UtilDlgEx` top, center, bottom, separator, and navigation button canvases, and this pass extends that same WZ-backed surface by loading the `UtilDlgEx/bar` name-strip art, rendering the speaker name through that bar inside the portrait column instead of as bare text, making the previously cosmetic `keyFocused` button art actually track packet-owned keyboard focus, animating the speaker portrait from WZ-backed NPC frames instead of pinning the overlay to a single `stand/0` bitmap, and routing packet-owned button activation through an explicit util-dialog modal-result seam that mirrors the recovered `CDialog::DoModal` return split (`0x2000` = prev, `0x2001` = next / OK, anything else = close). The speaker lane now reads `Npc/<id>.img/say/speak/*` action aliases first, falls back to `stand` or `action` when those aliases are missing, loads the resolved numeric frame sequence through the existing WZ seam, advances it on the authored per-frame `delay` values (falling back to the client NPC default cadence), and anchors each frame through its WZ `origin` inside the portrait column so client-authored idle or talk loops no longer jitter or collapse back to a static sprite. Left-arrow now shifts focus to `BtPrev` when a previous page exists, right-arrow or `Tab` restores focus to `BtNext` or the final `BtOK`, `Shift+Tab` moves back onto `BtPrev`, `Enter` activates the currently focused packet-owned button through that shared modal-result seam, and `Esc` now leaves through the same packet-owned close-result path instead of bypassing the util-dialog owner logic. The shared util-dialog session runtime is closer to the client owner lifecycle as well: the overlay now keeps explicit per-page allocation and destruction counts for `Prev`, `Next/OK`, and close results so the simulator can distinguish page-to-page modal recreation from terminal close paths instead of treating the packet dialog as a single undifferentiated page stack. The client tail-call is closer too: successful packet-owned quest-result application snapshots the available-quest set before decode, compares it against the rebuilt available-quest set after the packet-owned handler finishes, and flags newly surfaced quests through the existing quest alarm/update seam so the simulator mirrors `CWvsContext::CheckNewQuestAvailable(0)` more closely than the earlier detail-window-only refresh. Focused source coverage now includes the subtype `10` trailing-follow-up payload contract, util-dialog session lifecycle snapshots, util-dialog notice auto-separation routing checks, the subtype `10` notice-order helper checks, the newly-available-quest diff helper, and the client-summary-only act-notice regression cases in `UnitTest_MapSimulator/PacketOwnedQuestResultParityTests.cs`, including the no-supplemental-text and empty-category-return branches. Remaining gap: the subtype `10` modal still runs through the simulator's reusable overlay event loop rather than a dedicated blocking `CUtilDlgEx` owner, so it still lacks the actual client dialog object itself, the owning `CWvsApp::Run(&m_bTerminate)` modal loop behind `CDialog::DoModal`, and the last owner-managed lifecycle details that depend on that blocking dialog host rather than the simulator's local approximation. | Quest parity is not only local accept/complete helpers, quest windows, or generic field timers. The client owns a packet-response dispatcher that adds and removes quest timers, shows quest-result notices and modal pages from WZ-backed quest data, can emit fixed or act-driven quest-result notices, can auto-start a follow-up quest after the modal returns, and then refreshes quest availability from that same packet seam. Tracking this row against the dedicated opcode `242` owner keeps the remaining work anchored to the real response path instead of scattering it across local quest commands or field-state utilities. | packet-owned local quest-result layer (`CUserLocal::OnPacket`, `CUserLocal::OnQuestResult`, `CWvsContext::AddQuestTimer`, `CWvsContext::RemoveQuestTimer`, `CWvsContext::CheckNewQuestAvailable`, `CWvsContext::StartQuest`, `CWvsContext::DeleteFadeWnd`, `CUtilDlgEx`, `Quest/QuestInfo.img`, `Quest/Say.img`, `Quest/Act.img`, `UI/UIWindow.img/UtilDlgEx`, `UI/UIWindow2.img/UtilDlgEx`, `MapSimulator.PacketOwnedQuestResultParity.cs`, `HaCreator/MapSimulator/Interaction/NpcInteractionOverlay.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultClientSemantics.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultUtilDialogLayout.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultFadeWindowRuntime.cs`, `UnitTest_MapSimulator/PacketOwnedQuestResultParityTests.cs`) |
| Implemented | Packet-owned chair sit-result parity | The simulator now has a dedicated local `OnSitResult` packet seam on opcode `231` instead of treating chairs as local-only intent. The packet-owned handler stamps the shared local utility request tick, mirrors the client-owned `CWvsContext` chair bookkeeping in the packet-owned local-utility context by clearing the chair exclusive-request flag at `+0x20B8` and restamping the exclusive-request tick at `+0x20BC`, decodes the client-shaped success flag plus seat index payload, resolves the current map's WZ `seat/<index>` vector, validates the local player against the same client chair rectangle (`x +/- 10`, `y +/- 30`), and routes successful placement through a packet-owned local sit placement helper that also raises a dedicated secure sit confirmation flag on the local player. Successful packet-owned placement now clears any attached local passenger linkage through the existing follow-runtime and remote-user seams before the sit is finalized, matching the client path that zeroes `m_dwPassenserID` and the passenger's driver link. Invalid returned seats or rectangle mismatches now force the same stand-up correction path and send that correction through the shared local-utility context owner instead of a raw timestamp: the simulator still records a client-shaped `CWvsContext::SendGetUpFromChairRequest(0)` outpacket with opcode `45` and payload `FFFF`, re-arms the exclusive-request flag after emission, suppresses the send when the local HP gate or exclusive gate would block it, and injects that raw opcode `45` packet through a configured `LocalUtilityOfficialSessionBridgeManager` owner when a live Maple socket is attached. That bridge seam is broader now too: `/localutility session start ...` and `/localutility session startauto ...` no longer behave as one-shot manual helpers only. They persist their direct-proxy or discovery configuration onto the packet-owned local-utility owner, the simulator now keeps that bridge armed from the main update loop alongside the inbox, auto-refreshes discovery while no Maple client is attached, preserves an already attached live session instead of tearing it down during discovery refresh, reports outbound chair corrections in bridge status with pending and queued counts plus the last injected and last deferred raw packets, and now keeps a built-in deferred outbound queue for chair correction packets whenever the official-session bridge is enabled but no Maple session has finished attaching yet. Those deferred packets survive bridge re-arming and discovery refresh, then flush through the live Maple socket as soon as the proxied session completes the init handshake, so packet-owned chair corrections no longer depend on an external loopback consumer to reach the official-session transport path. The no-live-session fallback still remains the dedicated `LocalUtilityPacketTransportManager` loopback outbox on `/localutility outbox [status|start [port]|stop]`, which emits raw outbound corrections as `packetoutraw` lines containing the decrypted opcode-framed Maple packet, records the last emitted raw chair correction on that outbox owner, and lets packet-owned chair corrections leave the simulator whenever no live Maple bridge is attached but an external loopback consumer is listening. That outbox owner still keeps its own deferred packet-owned queue for chair correction packets when no outbox client is currently attached, flushes queued packets as soon as a loopback client connects (or before the next immediate send when a client is already attached), and reports both sent and pending correction counts in local-utility status so these packet-owned corrections no longer collapse into simulator-only state immediately when transport is temporarily unavailable. Packet rejection still clears the sit state without emitting that correction request, and the rejection teardown now also clears any stale external-owner couple-chair overlay when an active portable chair is unwound through the same local stand-correction path, matching the client-owned `Update(0)` cleanup branch more closely than the earlier partial teardown.g.cs`, `MultiBoard.g.cs`, and `HaCreator_Content.g.cs` missing under `HaCreator/obj/Debug/net10.0-windows`). | Chairs are not only a local toggle or render-state change. The client keeps a packet-owned success/failure and seat-validation branch that decides whether the user really sits, whether a passenger link is broken, and when to force a stand-up correction. Tracking this row against the dedicated opcode `231` seam keeps the remaining work anchored to the real local response owner instead of only local chair commands or remote chair packets. | packet-owned local chair-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnSitResult`, `CWvsContext::SendGetUpFromChairRequest`, WZ `Map/Map*/<map>.img/seat`, packet-owned local sit placement, correction path toward `PacketOwnedLocalUtilityContextState`, `LocalUtilityOfficialSessionBridgeManager`, `LocalUtilityPacketTransportManager`, `UnitTest_MapSimulator/LocalUtilityOfficialSessionBridgeManagerTests.cs`) |
| Partial | Packet-owned teleport-result parity | Standard same-map portal travel still finishes through the packet-owned local `OnTeleport` seam: visible and hidden portals targeting the current field use the WZ-authored portal `delay` when present, otherwise the client-confirmed 120 ms handoff, stage a pending same-map teleport against the destination portal index, and let the dedicated apply helper own the final landing and `TryRegisterTeleport(..., bForced = 1)` side effects instead of writing position directly from the interact path. Temporary same-map completion stays in that seam too, carrying any resolved real source `pn`, forwarding same-map target `pn` metadata, and using relaxed current-field `CPortalList` coordinate matching so near-equal authored landings can still collapse to a real portal index. Cross-map portal travel remains packet-owned as well: pending teleport targets survive `UnloadMapContent`, reload the new field, try the rebuilt `PortalPool` first by the primary returned `pn`, then by preserved fallback candidate names, and only then fall back to exact-coordinate apply. This pass tightens that post-load collapse order in the existing seam so rebuilt-field completion no longer promotes an arbitrary returned portal index from whole-pool coordinate drift after the named and candidate-based collapse paths fail; ambiguous rebuilt candidate sets and destinations that no longer correspond to any rebuilt `CPortalList` entry now stay coordinate-owned through the packet apply seam instead of stealing a named handoff from whichever live portal happens to sit nearest the stored landing point, and the rebuilt-field seam no longer synthesizes a fresh named handoff from source-side reciprocal or map-only metadata once those preserved target-side collapse paths have already missed. The preserved primary returned portal name is still authoritative again once the rebuilt `PortalPool` can resolve it, even when the stored landing coordinates would have drifted toward another live portal and previously could have stolen the handoff. Preserved candidate destination names still no longer require the raw candidate strings to be unique up front, because after field load the simulator replays those names against the rebuilt `PortalPool`, promotes them into a true returned portal handoff whenever stale candidates drop out to one live portal index, and still replays the preserved landing coordinates against the still-live candidate subset so a rebuilt field can recover one real returned portal index even when multiple preserved destination names survive reload but only one of those live candidates still matches the stored landing point. Standard visible or hidden cross-map portals still preserve WZ-backed reciprocal return-link fallback (`target.tm == currentMap` and `target.tn == source.pn`) plus unique map-only return fallback from the synthetic opcode `113` request owner, and temporary cross-map travel still mirrors that metadata preservation while recovering real current-field source `pn` values through relaxed coordinate matching before target-side metadata recovery. Source-side collapse is tighter in that same packet-owned seam now too: synthetic opcode `113` request building and temporary-portal recovery still start from the full best-distance current-field source `pn` set derived from portal `x`/`y`/`pn`/`tn`/`tm` metadata, but the helper no longer treats each target-side clue as a separate all-or-nothing check. It now keeps that coordinate-authored source candidate set alive, narrows it cumulatively with the explicit target `tn` when present, then with the preserved target candidate names when those survive, and only then replays each surviving current-field source candidate back through the authored target-field portal metadata surface (`tn`, reciprocal `pn`, unique map-only return, and target-side coordinate candidates) before falling back to the older target-only inference or exact-coordinate completion. That closes another packet-owned source-side miss where current-field coordinates plus the preserved target candidate set still left multiple source `pn` values alive until the simulator asked which candidate's own target-field metadata actually matched the resolved landing side; temporary and synthetic recovery now keep the packet-owned named handoff in that case instead of dropping straight to coordinate-owned completion. Exact-coordinate completion still avoids inventing a fallback target name when the packet-owned seam only knows ambiguous candidates, and the outbound opcode `113` owner still first tries the live `LocalUtilityOfficialSessionBridgeManager`, then the generic `LocalUtilityPacketTransportManager` `packetoutraw` outbox, before queueing deferred delivery while preserving raw payload and dispatch summary. WZ evidence for the active seam remains the portal metadata fields in `Map/Map0/001000000.img/portal/*/{pn,tn,tm,x,y,delay}` and the general teleport effect in `Effect/BasicEff.img/Teleport`, whose authored animation remains 4 frames at 90 ms each; `Map/Map0/001000000/portal/0` still exposes the same packet-relevant portal metadata surface (`pn = sp`, `tm = 999999999`, `x = 240`, `y = 190`) on the mounted export. Direct client evidence remains `CUserLocal::OnPacket` opcode `234` dispatch into `CUserLocal::OnTeleport`, where the client decodes the success byte, looks up the returned `CPortalList` entry by portal index, writes the landing point from that portal, and then re-enters `TryRegisterTeleport(..., bForced = 1)`. Focused source coverage now on disk in `UnitTest_MapSimulator/PacketOwnedTeleportParityTests.cs` includes the cumulative source-collapse regression where current-field source coordinates, an explicit target `tn`, and preserved target candidate names only become unique when the packet-owned seam intersects all of them in order, the replayed-target-field regressions where multiple coordinate-authored current-field source candidates collapse only after matching each candidate's authored target metadata or reciprocal return link against the resolved landing side, alongside the earlier rebuilt-field primary-name precedence path, rebuilt-field candidate collapse when only one preserved destination still resolves in the live pool, rejection when multiple preserved candidates still resolve after reload, the rebuilt-field collapse path where multiple preserved destination names still resolve live but only one of those candidates still matches the preserved landing coordinates, the new rebuilt-field rejection where only source-side reciprocal metadata still resolves after the preserved target-side collapse paths fail, and the coordinate-only fallback regressions where rebuilt cross-map completion must not promote a named portal from whole-pool coordinates alone or from a non-candidate live portal. Remaining gap: synthetic or temporary request-building branches still fall back to exact coordinates when current-field source candidates remain genuinely ambiguous even after cumulative target-name narrowing and the per-candidate target-field metadata replay pass, and outbound transport still relies on the generic bridge/outbox owners rather than a teleport-specific official socket owner when no live direct injection path is available. | Teleport parity is not only about entering a portal or moving to the right coordinates. The client also has a response-time authority point that applies the actual destination by portal index and records the named teleport handoff after the packet returns. Tracking that seam keeps the remaining parity gap anchored to the real response owner instead of hiding it behind already-implemented local portal interaction flow. | packet-owned local teleport-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnTeleport`, `CPortalList`, `CUserLocal::TryRegisterTeleport`, WZ `Map/Map0/001000000.img/portal/*/{pn,tn,tm,x,y,delay}`, WZ `Effect/BasicEff.img/Teleport`, `MapSimulator.cs`, `MapSimulator.PacketOwnedTeleportParity.cs`, `UnitTest_MapSimulator/PacketOwnedTeleportParityTests.cs`, `LocalUtilityOfficialSessionBridgeManager.cs`, `LocalUtilityPacketTransportManager.cs`) |

### 4. Portal and reactor collision ownership discovered by targeted `CUserLocal` decompile

The latest `CUserLocal` pass exposed one more field-runtime owner split that the backlog was still flattening into generic portal, teleport, and reactor rows.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Portal collision ownership parity | The simulator now owns portal touch through one explicit `CUserLocal::CheckPortal_Collision`-style seam instead of separate visible versus hidden lookup loops. `TryHandlePortalInteractCore` resolves the colliding portal from `PortalPool.FindPortalCollision` using the live local-user physics position, then branches by portal type in that owner: type `9` (`CollisionScript`) still keeps a simulator-side exclusive-request gate around the existing packet-owned portal request path using the in-flight request flag, per-portal delay, and `onlyOnce` suppression keyed to the last collision-source portal; type `12` (`CollisionVerticalJump`) now routes through a dedicated vertical-jump branch that follows the recovered `CUserLocal::VerticalJump` helper more closely by queueing `SetImpactNext` with vertical velocity `-2300`, recording client move-path attribute `24`, deriving horizontal velocity from live left/right input plus facing (`input * 100` plus `+/-200`), and replaying the client-owned visual owner through `Effect/BasicEff.img/VerticalJump` as a one-shot animation attached to the live local-user physics position instead of leaving the spring purely physics-owned; type `13` (`CollisionCustomImpact`) now mirrors more of the client-side admission, request, and delayed-impact shape instead of behaving like a generic spring everywhere. The type `13` owner is field-gated to the same recovered client family from `CUserLocal::CheckPortal_Collision` at `0x919a10`: `fieldType` `19`, `20`, `21`, `25`, and `32` (`Party Raid`, `Party Raid Boss`, `Chaos Zakum`, `Waiting Party Quest`, and `Hunting Ad Balloon`) plus the explicit `m_dwField == 180000002` fallback case, before the owner evaluates authored `reactorName` or `sessionValueKey`. WZ-backed `reactorName`, `sessionValueKey`, and `sessionValue` metadata still flow through `PortalInstance`, `MapLoader`, map save, and board serialization, and the session-valued branch still uses a client-shaped delayed seam: when a structured session owner is available for the portal-authored pair, the simulator queues a `PendingPortalSessionValueImpact`, emits the client-shaped `CWvsContext::SendRequestSessionValue` outbound request as opcode `191` with `EncodeStr(sessionValueKey)` plus reset byte `0`, and stores the pending impact until a matching client-owned session or field handoff arrives. This row now closes another part of that owner lifecycle inside the existing seam: pending type `13` impacts are no longer released only from the generic structured field-state handoff, because the simulator also decodes and matches the client-shaped packet forms that those admitted owners actually use. Party Raid release coverage inside the same seam still includes direct `OnSessionValue` ingress, packet `149`, and relayed wrapper opcode `163` when they carry the matching key/value pair; Hunting Ad Balloon release coverage now also accepts direct `OnFieldSetVariable` packet `95` and relayed `163 -> 95` in addition to packet `149`; and packet `149` matching now strips client owner-hint prefixes such as `field:` and `session:` before comparing against the authored portal key so wrapper-shaped field/session payloads wake the same pending impact store instead of falling back to later structured simulator handoff. Structured owner coverage inside that existing seam still includes active `PartyRaidField` owners, Hunting Ad Balloon's authored `balloon_Team = redTeam/blueTeam` pair, and the wrapper-side Chaos Zakum `fire` owner for `Map/Map9/912060200.img/portal/3`. The default branch still stays in this same owner too, deciding between same-map registered teleports and cross-map transfer staging before playing the handoff sound. WZ evidence for type `12` remains `Effect/BasicEff.img/VerticalJump`, which publishes a dedicated six-frame effect with authored per-frame origins such as frame `0/origin = (18,72)` and frame `1/origin = (20,77)`; WZ evidence for the type `13` branch remains authored portal nodes such as `Map/Map0/003000000.img/portal/1` and `Map/Map9/912060200.img/portal/3`, which expose `pt = 13` together with authored impact/session metadata, while `Map/Map9/970050010.img/portal/6` and `portal/7` pair `sessionValueKey = balloon_Team` with `sessionValue = redTeam` / `blueTeam` and the same map publishes `info/fieldType = 32` with `info/onUserEnter = Balloon_D_Enter`. Client evidence remains `CUserLocal::CheckPortal_Collision` at `0x919a10`, where type `9`, `12`, `13`, and the default transfer path are split in one function; the type `12` branch calls `CUserLocal::VerticalJump`, and the targeted `CUserLocal::VerticalJump` decompile at `0x90f510` shows the client setting move-path attribute `24`, queueing `SetImpactNext(vx, -2300)`, resolving the `VerticalJump` string-pool path, and spawning the effect before the caller runs the passenger check; the type `13` branch is admitted only for field types `19`, `20`, `21`, `25`, `32` or field id `180000002`, and session-valued impacts call `CWvsContext::SetImpactNextBySessionValue`; the follow-up decompile at `0x9e52f0` shows that helper calling `CWvsContext::SendRequestSessionValue` and storing the pending key/value plus velocities, while `SendRequestSessionValue` at `0x9e1a90` builds opcode `191` with the session key and reset byte `0`. Focused regression coverage for the recovered type `12` horizontal-impact formula, the type `13` field admission family, the `VerticalJump` effect path, and the type `13` release packet shapes now lives in `UnitTest_MapSimulator/PortalCollisionOwnershipParityTests.cs`, including direct `95`, relayed `163 -> 95`, and owner-hint-prefixed `149` decode coverage. Remaining gap: the type `9` gate is still simulator-owned rather than a byte-for-byte replay of the client `CWvsContext::SetExclRequestSent` state; the type `12` branch still does not reconstruct the client vec-ctrl move-path attribute owner or the post-jump vec-ctrl interface/passenger gate byte-for-byte; and the type `13` session path still keeps one shared simulator-side pending-impact store plus request bookkeeping instead of a fuller per-owner `CWvsContext` request/response lifecycle, so the admitted non-Party-Raid owners are now reached through client-shaped ingress packets but not yet through a more exact owner-specific pending-session state machine. | Portal parity is no longer only about later request or response handlers because the simulator now has an explicit collision owner that decides what a portal touch means before teleport handoff. Keeping the type split in this owner makes future parity work land in the same place the client uses for admission, one-shot suppression, custom impact, vertical jump, same-map teleport, and cross-map transfer decisions. This pass narrows the remaining type `13` gap again by extending the existing pending-impact seam to the direct field-wrapper packet the client uses for Hunting Ad Balloon and by normalizing owner-hinted `149` keys before matching them, so wrapper-shaped ingress no longer has to wait for a later structured handoff to wake the stored portal impact. | portal collision field-runtime layer (`CUserLocal::CheckPortal_Collision`, `CPortalList::FindPortal_Collision`, `PortalPool.FindPortalCollision`, `MapSimulator.TryHandlePortalInteractCore`, `CWvsContext::SetImpactNextBySessionValue`, `CWvsContext::SendRequestSessionValue`, `CUserLocal::VerticalJump`, `CField::SendTransferFieldRequest`, `CUserLocal::TryRegisterTeleport`, `CUserLocal::OnCollisionCustomImpact`) |
| Partial | Reactor collision polling parity | The simulator now owns a dedicated once-per-second `CheckReactor_Collision`-style poll in the main update loop instead of treating touch as an immediate reactor activation side effect. That owner remembers the last poll tick, calls into the existing `ReactorPool` seam to refresh a persistent local touched-reactor set, and the pool-side refresh now follows the recovered client contact rule more closely by testing the local vec position against each touch reactor's current bounds instead of intersecting a synthetic avatar rectangle. The poll also now diffs enter/leave ownership and routes packet-authored touch ownership through transport seams instead of local activation: when a live reactor official-session bridge is attached and the touched reactor has a real packet object id it injects the client opcode `250` enter/leave touch request built as `objectId + touchFlag`; when no live Maple session is attached it can defer that same opcode on the reactor bridge until init completes; and when no reactor bridge is available it can mirror or queue the same raw opcode `250` packet through a dedicated reactor touch `packetoutraw` outbox instead of collapsing immediately to simulator-local state. This pass closes more of the remaining ownership gap in that same seam: the main update loop now explicitly keeps the reactor official-session bridge armed, refreshes discovery, and drains inbound reactor pool messages instead of leaving those bridge methods disconnected from the tick owner; packet-owned `CReactorPool::OnReactorEnterField` can now bind a real packet object id onto a unique matching WZ-authored local reactor (`template + x/y + flip`, with non-conflicting name when present) instead of always spawning a duplicate packet-owned reactor; packet-owned leave/removal now clears the touched-reactor owner immediately so the next poll does not emit a bogus opcode `250` leave for a reactor the server already removed; the pool-side touch scan now skips reactors already marked `PacketLeavePending`, which keeps server-authored leave-field teardown from being rediscovered as a fresh local contact while its exit animation finishes; and both deferred transport owners now coalesce queued touch state per live reactor object id so stale enter/leave history gathered before a bridge or outbox attaches collapses to the latest opcode-`250` ownership flag instead of replaying obsolete transitions later. This row is tighter again now that authored touch ownership no longer has to wait for the next one-second collision poll after a late packet-id recovery: when an already-touched local reactor is adopted onto a real packet object id, the old synthetic local key is dropped, the touched-set is re-keyed immediately to that recovered object id, and the packet-owned enter seam now drains a pending promoted touch-change back through the existing opcode-`250` transport path in the same tick instead of waiting for the next periodic poll. The authored-match seam now uses the recovered pool-side signals as intersecting disambiguation filters instead of isolated one-off tie-breakers: if packet `EnterField` metadata still leaves multiple authored reactors after `template + x/y + flip (+ name when present)`, the selector now replays exact-name matches, current local-user containment, current touched-reactor ownership, and the returned initial state in priority intersections so a single authored reactor that only becomes unique after combining those signals is adopted onto the recovered packet object id instead of letting an unrelated single-signal winner steal ownership or falling straight to duplicate packet-owned spawn; if those same pool-side signals still leave multiple authored candidates fully identical, the selector now falls back to stable WZ-authored reactor order (`reactor/0`, `reactor/1`, ...) and adopts the lowest remaining authored candidate instead of forcing an avoidable duplicate packet-owned spawn. WZ evidence for the authored side remains map `reactor/*` nodes such as `Map/Map0/001000000.img/reactor/*`, which publish template id, x/y, reactorTime, flip, and name but not a server object id; for example `Map/Map0/001000000.img/reactor/0` and `reactor/1` expose only `id=1002009`, `x`, `y`, `reactorTime`, `f`, and `name`, while the authored node order itself is explicit in the map. Client evidence remains `CUserLocal::CheckReactor_Collision` calling `CReactorPool::FindTouchReactorAroundLocalUser`, where the pool uses the live reactor `dwID` as the touched-set key and sends opcode `250` with `dwID + touchFlag`; the targeted decompile of `CReactorPool::FindTouchReactorAroundLocalUser` also still shows the client computing the reactor bounds from the live layer, testing the local user vec position, inserting or removing `dwID` from the touched map, and emitting `COutPacket(250)` with `Encode4(dwID)` plus `Encode1(touchFlag)`. Remaining gap: purely local map-authored reactors that never receive a packet `EnterField` object id still have no authoritative client request id to emit; when the simulator is running without a live session it still preserves only the latest deferred ownership state rather than the client's exact send timing; and the smaller fully-identical packet-enter class is narrower now but not gone because the simulator still lacks a stronger client-backed discriminator than exact-name/current-position/current-touch/initial-state plus WZ node order when server object-enter order diverges from authored map order for otherwise identical reactors. | Reactor parity is not only about authored hit props or packet-fed state. The client also owns a local periodic touch-poll seam, so keeping this row tied to the poll owner plus its local touch-set refresh prevents later reactor work from smuggling contact ownership into unrelated packet handlers or local activation shortcuts. The transport side matters too: touch ownership is now preserved across a missing live session through the dedicated reactor bridge queue or reactor touch outbox rather than disappearing into local-only activation as soon as the immediate official-session path is unavailable, the deferred queue now keeps only the latest ownership state per recovered `dwID` so a late bridge attach does not replay stale enter/leave history, packet-owned enter-field traffic can now recover the live `dwID` equivalent for unique authored reactors before the touch poll emits enter/leave ownership, that same enter-field seam can now also promote an already-touched authored reactor to immediate packet-owned opcode `250` ownership as soon as its real object id is recovered, packet-owned leave-field teardown now preserves server ownership by clearing stale local touch claims before the next once-per-second poll, and the authored-enter selector now keeps those same client-backed touch and live-bounds signals combined all the way through the adoption decision instead of treating them as unrelated fallbacks, only dropping to WZ-authored reactor order when those signals remain fully tied. | reactor touch-poll layer (`CUserLocal::CheckReactor_Collision`, `CReactorPool::FindTouchReactorAroundLocalUser`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.cs`, `ReactorPool.cs`, `ReactorPoolOfficialSessionBridgeManager.cs`, `ReactorTouchPacketTransportManager.cs`, WZ `Map/*/reactor/*`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
