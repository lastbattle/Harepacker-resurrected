# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/DropPool.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Field and portal parity

- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`

Notes:
`CUserLocal::TryPassiveTransferField` shows that client field transfer is not only explicit portal collision handling; the local-player update loop also retries an up-key style transfer after one-time actions finish when the character is allowed to move. A later IDA pass also shows that packet-authored field utility surfaces are split into their own remote pools rather than hanging off local collision handling: `CAffectedAreaPool::OnPacket` owns affected-area create/remove, `CTownPortalPool::OnPacket` owns town-portal create/remove, and `COpenGatePool::OnPacket` owns Open Gate create/remove.

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`

Notes:
The backlog already covered pickup notices and pet auto-loot side effects, but the broader client owner was still unnamed. IDA shows that drop parity is owned by a dedicated `CDropPool` runtime that handles enter/leave packet dispatch, per-frame drop update, player/pet/mob pickup arbitration, meso animation, and recent-pickup history instead of treating drops as generic field objects plus UI messages.

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive chairs, same-field teleports, and quest results from local intent and generic runtime state. It also has dedicated packet-response handlers that finalize those flows after the request is acknowledged.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Field and portal parity

The old backlog understated what is already present here, but there is still real work left.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Hidden portals | Reveal, hide, alpha fade, and collision gating are implemented | Remove from missing backlog | `PortalPool.cs` (`CPortalList::FindPortal_Hidden`) |
| Implemented | Portal property accessors | `PH`, `PSH`, and `PV` equivalents are implemented | Remove from missing backlog | `PortalPool.cs` |
| Implemented | Portal interaction parity | Standard and hidden portal triggering now share the same same-map delay seam as client-confirmed portal teleports, and Mechanic `Open Portal: GX-9` casts now place WZ-backed temporary linked gates that resolve on `Up` interaction with the client-like 120 ms same-map handoff; town portal / mystic door remains tracked separately below | Portal travel now covers the main visible, hidden, and same-map skill-portal branches instead of reducing open-gate travel to unsupported or generic transitions | `PortalPool.cs`, `MapSimulator.cs`, `TemporaryPortalField.cs` (`CUserLocal::CheckPortal_Collision`, `CUserLocal::TryRegisterTeleport`, `Skill/3510.img/skill/35101005`) |
| Partial | Passive transfer-field handoff parity | The simulator now keeps passive transfer-field retry as its own delayed owner in `HandlePortalUpInteract`: it waits for the local one-time action to finish, replays the up-key portal path only when the player is not immovable and not attract-locked, clears the pending flag after that retry window resolves, and in this pass no longer re-queries transfer readiness straight from the field-restriction helper inside the retry path. The client-side ownership split recovered in IDA is now mirrored more closely: `CUserLocal::TryPassiveTransferField` at `0x91a2e0` still waits for `CAvatar::GetOneTimeAction() <= -1`, then reads a vec-ctrl-backed readiness bit through the live field interface before replaying `CUserLocal::HandleUpKeyDown`, and the simulator now publishes that gate onto a dedicated vec-ctrl-owned `IsPassiveTransferFieldReady` flag that `HandlePortalUpInteract` reads instead of calling `FieldInteractionRestrictionEvaluator.CanTransferField(...)` inline. WZ re-check still shows the map-authored side of this seam is the `info/fieldLimit` surface on field data rather than a separate portal node; representative maps in the current export continue to publish transfer bans there, for example `Map/Map0/000050000.img/info/fieldLimit = 4`. The remaining gap is narrower: the simulator's dedicated vec-ctrl flag is still populated from the existing field-limit seam, while the client appears to source that live interface bit from deeper field-runtime state behind `m_pvc.m_pInterface` rather than from a direct map-info restriction query alone. | Without this row, passive transfer-field behavior can look like a minor portal follow-up even though the client treats it as a separate retry owner tied to movement state, one-time action completion, and vec-ctrl field readiness. Tracking it explicitly here keeps future parity work anchored to the actual handoff gate instead of folding it into unrelated teleport or portal-collision polish. | `MapSimulator.cs`, `Physics/CVecCtrl.cs`, portal/field transition layer (`CUserLocal::TryPassiveTransferField`, `CUserLocal::HandleUpKeyDown`, `CAvatar::GetOneTimeAction`, WZ `Map/*/info/fieldLimit`) |
| Implemented | Transportation fields | Regular passenger ships now expose a dedicated transport-deck foothold that carries both the player and grounded mob passengers through the existing movement seam, while Balrog-only ship visuals no longer publish a false passenger deck | Transport maps now keep visible deck passengers riding the ship during departures and arrivals instead of leaving the transport system as a mostly visual-only layer | `TransportationField.cs`, `PassengerSyncController.cs`, `MapSimulator.cs` |
| Partial | Dynamic objects / quest layers | Field object drawables now retain WZ `quest` visibility metadata, keep WZ `hide=1` object layers suppressed until a matching quest state or a published object-tag toggle explicitly reveals them, resolve WZ object `tags` through the dedicated published object-state seam instead of the obstacle bucket, ingest authored map-level tagged-object defaults from `publicTaggedObjectVisible` plus the client-data typo variant `pulbicTaggedObjectVisible`, including grouped multi-tag entries that inherit parent `visible/state` flags across nested tag containers even when a parent node also publishes its own `tag`/`tags`, and now preserve those authored tagged-object-default containers through the `MapLoader` copy seam so the normal `publicTaggedObjectVisible` branch reaches `MapInfo.additionalNonInfoProps` instead of being dropped before the simulator loader can read it. The row also auto-publishes quest-owned layer tags from `QuestInfo.img/showLayerTag`, publishes entry-owned map script aliases from `info/onUserEnter`, `info/onFirstUserEnter`, and `info/fieldScript` when their WZ script names resolve onto the current map's tagged-object set, now using the same comma/semicolon script-name parser as the quest and NPC loaders so authored raw multi-alias entry strings publish every matching tag instead of only the unsplit literal, loads WZ `directionInfo/*/EventQ` trigger points and recurses nested script-name containers inside those `EventQ` nodes before publishing their resolved tags once the local player's hitbox reaches the authored `x`/`y` point, publishes portal-owned script aliases when a portal is actually used, and now splits raw comma/semicolon portal script strings through that same parser before publication, parses `Quest/Check.img` `startscript` / `endscript` names so successful NPC quest accept and complete actions publish their resolved tagged-object aliases through the same seam, keeps that same quest-script publication alive for both quest-window acceptance and quest-window completion instead of only the NPC-overlay lane, and publishes non-quest NPC aliases from `Npc/<id>.img/info/script/*/script` when the player actually opens that NPC interaction, including packet-owned script-message and quest-delivery NPC dialog opens that may only expose a speaker/template id instead of a live field NPC instance plus packet-owned NPC shop, admin-shop, store-bank, and repair-durability owners that surface only an NPC template id when their utility windows actually open. Quest-script alias resolution is broader now too: when WZ pairs `QuestInfo.img/showLayerTag` numeric tags with `Quest/Check.img` `q<questId>s` / `q<questId>e` script names, the publication seam now falls back from the full quest script name to both the base `q<questId>` alias and the numeric quest-id tag, so cases like `22000` no longer miss their tagged-object reveal because the authored script suffix stayed on the quest-script branch. Staged `_01` / `_02` / `_03` style script aliases now retire sibling stage tags from the same camel-cased family when later entry, trigger, portal, quest, NPC, or reactor publications fire, that staged-family retirement also covers separator-less camelCase or bare trailing-digit aliases such as `aranTutor3` / `cannonTuto2`, and the resolver now applies the same stage fallback and sibling-retirement rules to exact authored `_` / `-` tag families too, so later publications such as `Flag_R_2` can retire `Flag_R_1` while later stage aliases still keep an authored exact base tag like `back_stoner` published when no exact `back_stoner_2` node exists. The shared alias resolver now also strips path-qualified and extension-suffixed script identifiers before matching those same quest/stage families, so `folder/q22000s.js` and `map/cannon_tuto_02.lua` still resolve onto the authored tagged-object set when later runtime or packet seams surface transport-shaped script names instead of the bare WZ alias. Delayed `directionInfo/EventQ` publications now also validate that the authored script alias actually resolves onto the current map's tagged-object set before consuming the one-shot trigger, and the delay parser now accepts integer, floating-point, or numeric-string WZ delay literals on those nested `EventQ` containers before scheduling the follow-up publication, so unrelated or non-matching delayed aliases no longer arm and silently exhaust a trigger without changing any object state while authored numeric delay variants still land on the same publication seam. Remaining gap: script-engine-owned multi-step sequencing is still absent when no WZ-visible alias timing exists; timer and callback chains that only live inside runtime script execution (without surfaced `EventQ`/entry/portal/NPC/quest/reactor alias metadata) remain unmodeled. | Quest-driven and event-driven maps now respect quest state, quest-authored `showLayerTag` reveals, author-authored hidden-layer defaults, inherited grouped tagged-object default states including parent-tag containers that continue into nested inherited children, and the underlying map-load copy seam now preserves both `publicTaggedObjectVisible` and `pulbicTaggedObjectVisible` containers so those authored defaults reliably survive into the simulator's dynamic-object default loader. Entry-script publication, WZ-authored `directionInfo/EventQ` trigger points including nested script containers, portal-script-owned tagged-layer publication, quest-owned NPC `startscript` / `endscript` publication from both NPC-overlay and quest-window acceptance/completion flows, non-quest NPC `info/script` publication on talk including packet-owned script-message and quest-delivery dialog opens that only expose an NPC template id, packet-owned NPC utility owners that reopen the same tagged layers from shop/admin-shop/store-bank/repair template ids, and WZ-authored staged tutorial/event script progression that swaps sibling stage tags through the same published-object seam instead of only ever accumulating visible stages are all in place. This pass also keeps the shared NPC publication seam preferring the live field NPC instance when one exists and falling back to the packet/template id otherwise, so NPC-backed object reveals no longer depend on overlay-only dialog owners. Raw map-entry and portal script strings still share the same delimited alias parsing already used by the quest and NPC WZ loaders, so authored `scriptA,scriptB` or `scriptA;scriptB` contracts no longer miss later tagged-object reveals by staying collapsed into one unmatched literal. Path-qualified and extension-suffixed script identifiers now normalize through that same shared resolver before quest/stage matching, so runtime-facing aliases such as `folder/q22000s.js` or `map/cannon_tuto_02.lua` still land on the authored numeric quest-layer and staged tutorial tag families instead of missing them on transport-only naming noise. Quest-script publications still match the common WZ pattern where `QuestInfo.img/showLayerTag` authors a numeric map tag while `Quest/Check.img` uses `q<questId>s` / `q<questId>e`, so quest-window and NPC-driven accept/complete flows now publish onto those numeric quest-layer tags instead of missing them on suffix-only mismatches. Delayed `directionInfo/EventQ` alias containers still do not consume their one-shot trigger unless the authored alias resolves onto the map's available tag set, which keeps unmatched delayed script names from blocking later valid trigger contact on the same point. Tagged event-map objects still fall back to authored map defaults when runtime overrides are cleared, generic object-state publication no longer has to masquerade as an obstacle toggle, and `directionInfo/EventQ` alias containers now also honor authored delay keys (`delay`, `wait`, `time`, `t`, `startDelay`) plus numeric-string or float-style delay literals by scheduling follow-up publications through the same seam after trigger contact. Focused source-side regression coverage now includes exact-family stage fallback and retirement, path-qualified quest alias normalization, nested NPC script container parsing, and recursive `directionInfo/EventQ` delay parsing in `UnitTest_MapSimulator/FieldObjectDynamicQuestLayerParityTests.cs`. The remaining gap is now limited to script-engine timer chains that do not surface any WZ-side alias timing metadata. | map/field load path, `MapLoader.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedNpcUtilityParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketScriptMessageParity.cs`, `QuestRuntimeManager.cs`, `FieldEffects.cs`, `FieldObjectTagStateDefaultsLoader.cs`, `FieldObjectScriptTagAliasResolver.cs`, `FieldObjectDirectionEventTriggerLoader.cs`, `FieldObjectNpcScriptNameResolver.cs`, `ReactorScriptStatePublisher.cs`, `PacketOwnedNpcUtilityRuntimes.cs`, `UnitTest_MapSimulator/FieldObjectDynamicQuestLayerParityTests.cs` |
| Implemented | Town portal / mystic door lifecycle | `Mystic Door` skill casts now create a WZ-backed cross-map door pair, place the return-side door on the target town map's `TownPortalPoint` / `tp` spawn, transfer into town on `Up`, and restore the player to the original cast position when the town-side door is used before expiry | The simulator now covers the visible create, enter, restore, and positioning loop for the client's town-door utility instead of leaving the skill family nonfunctional | `TemporaryPortalField.cs`, `MapSimulator.cs`, portal/field transition layer (`Skill/231.img/2311002/{cDoor,mDoor,common/time}`, `PortalType.TownPortalPoint`) |
| Partial | Remote portal-pool parity | Packet-authored remote portal pools now exist in the simulator runtime instead of being folded into local skill ownership: `330/331` decode and apply remote town-portal create/remove keyed by owner character id, `332/333` decode and apply remote Open Gate create/remove keyed by owner plus first/second slot, packet-owned remote portals clear on field reload, remote Open Gates link and teleport through the existing same-map interaction seam, remote town portals render on their own pool, preserve their packet-authored state byte, add the WZ `Frame` overlay for the nonzero remote-door phase, route into the field's resolved town return target when that destination is available, synthesize the inferred town-side return door back into the original field when that field-side return target is known, cache owner-level field/town Mystic Door metadata so later town-side packets can reuse an earlier observed source-field return, and a `/portalpacket` debug seam now drives the pool with either raw packet payloads or typed create/remove arguments. Remote Mystic Door timing is also closer to `CTownPortalPool`: newly seen state-`0` create packets stay in the WZ-backed `cDoor` opening run for the asset-authored 1700 ms before promoting to the steady door/frame pair, the first follow-up create against an owner still carrying stored state `0` now promotes that runtime into the steady nonzero overlay branch instead of leaving it on the packet byte forever, and later create packets for owners already carrying a nonzero stored state now replay the WZ-backed `cDoor` opening run and reset that owner's phase start regardless of the incoming packet byte, matching the direct `CTownPortalPool::OnTownPortalCreated` state-driven branch more closely than the older packet-byte-only create logic. Stable remote door loops now retain their own phase start instead of sharing a global zero-tick cycle, state-`0` remove packets now snapshot the currently visible field/town door canvases plus the field-side `Frame` overlay into a 1000 ms fade instead of fading a freshly restarted animation, and the simulator now keeps per-owner field/town remote-door runtime instances alive across phase updates instead of tearing down and recreating the whole remote Mystic Door pool on each sync. Remote portal ownership is also closer to the client now: remote Mystic Doors keep persistent per-layer drawable slots instead of replacing whole portal drawable arrays, remote Open Gates now keep per-slot runtime instances that mutate between WZ-backed `cDoor`, solo `sDoor`, linked `mDoor`, and remove `eDoor` states without rebuilding the entire pool when a partner gate appears or disappears, and Open Gate remove packets now resync those per-slot runtimes immediately so the surviving partner drops out of the linked `mDoor` branch or disappears on the same packet tick instead of lingering until the next simulator update. Town-side-first remote Mystic Doors can now reuse the owner's last observed non-town field position when that observed field's WZ-resolved town return matches the current town map even if no field-side door packet was seen, the destination resolver keeps an exact same-town packet-authored field-side cast position authoritative over later approximate movement observations for that town instead of letting those looser snapshots steal the inferred return point, town-side packet-only inference now promotes the chosen same-town observed source field into reusable owner-town metadata so later create/remove phase churn keeps returning to the same inferred field rather than jumping when newer approximate observations arrive from another matching field, the owner-observation cache now ranks remote-user field-entry and follow-transfer coordinates above generic move snapshots and keeps those higher-authority same-field coordinates authoritative over later weaker movement snapshots from that exact source field, repeated same-field fallback observations now refresh the stored approximation instead of freezing the first passive movement snapshot forever, remote-user move packets now feed the same-town owner observation cache from the actor pool's resolved live post-apply position instead of the raw passive snapshot whenever that stronger approximation is available, and remote-user leave-field teardown now also feeds the same-town owner observation cache from the last live actor position before removal so packet-only town-side doors can reuse that fresher same-field fallback without waiting for another move packet. Active town-side packet-owned Mystic Doors also stop waiting for later phase churn before they improve: when stronger same-town source evidence arrives after the door is already open, the live remote portal runtime now retargets immediately from an older approximation to the later exact field-side packet cast or to a higher-authority enter-field / follow-transfer observation for that same source field, and that improved live destination is now persisted back into the reusable inferred owner-town metadata immediately instead of waiting for a later create/remove packet to make the stronger or fresher source choice stick. Focused regression coverage is now checked in under `UnitTest_MapSimulator/RemotePortalPoolParityTests.cs`, covering the client-confirmed remote-town create-state transitions, unresolved-without-evidence behavior, and partner-aware Open Gate visual-mode selection through the existing internal test helpers. Remaining gap: packet-only town-side streams still cannot recover the exact original cast position when the simulator never witnesses any precise same-town source-field cast, enter-field point, or follow-transfer point for that owner at all; in those no-precision cases the runtime can now reuse the best same-field approximation it has for the chosen source field, including post-apply remote move positions and the owner's last live leave-field position when those were observed before teardown, but it still cannot do better than that approximation and still remains unresolved when no matching source field was ever observed. | Visible parity no longer stops at self-authored portal utility: other users' Open Gates and packet-owned Mystic Doors now have their own runtime pool, the field-side remote door can hand off into town through the packet seam, inferred town-side doors can return to the original cast field without needing a second authored packet stream, later town-side packets can now recover a previously observed source field via owner metadata caching or a last-observed owner field fallback, remote Mystic Door ownership now uses persistent per-layer runtime slots that mirror the client-side canvas choreography more closely than whole-portal drawable replacement, and remote Open Gate ownership now persists per slot so paired and unpaired visual transitions no longer re-register the whole pool. This pass also corrects a concrete client-state mismatch in the create path: `CTownPortalPool::OnTownPortalCreated` drives repeated remote-door create behavior from the runtime's stored door state rather than only the incoming packet byte, so the simulator now promotes the first follow-up create after an initial state-`0` spawn into the steady overlay branch and then replays `cDoor` on later creates once that owner is already in a nonzero state. The checked-in regression file now anchors those create-state transitions plus unresolved town-side fallback and partner-aware Open Gate mode selection in executable tests instead of leaving them only in backlog prose. The remaining work is still limited to exact-cast recovery for packet-only town-side cases where the simulator never witnesses a precise same-town source-field cast, field-entry point, or transfer point and therefore still has to rely on the best same-field approximation it observed or no inferred source field at all. | field portal/runtime layer (`CTownPortalPool::OnPacket`, `CTownPortalPool::OnTownPortalCreated`, `COpenGatePool::OnPacket`, local reference seam: `CUserLocal::CheckPortal_Collision`), `UnitTest_MapSimulator/RemotePortalPoolParityTests.cs`, WZ `Skill/231.img/skill/2311002/{cDoor,mDoor,Frame}`, WZ `Skill/3510.img/skill/35101005/{cDoor,sDoor,mDoor,eDoor}` |
| Partial | Remote affected-area / mist parity | The simulator keeps the packet-owned `328/329` affected-area runtime, start-delay activation, field-bind clearing, remote support projection, mixed friendly-plus-hostile Battle Mage aura handling, WZ-backed support ownership checks, WZ-backed `type=3` fog visuals and linked-duration resolution, and the existing mob-skill-mist reapplication path. In this pass the hostile player-area lane now preserves WZ-authored DOT metadata from `common/dot`, `dotInterval`, and `dotTime` instead of collapsing everything to generic `damage` / `time`, so affected-area wrappers such as `Skill/312.img/skill/3120010` and `Skill/3510.img/skill/35101009` keep their authored burn-style magnitude and timing when the packet-owned area path drives `SkillManager`'s inferred mob-status seam. The resolver also now treats linked hostile companions as part of disposition resolution instead of only the primary shell, and the linked-runtime collector now follows WZ `info/dummyOf` wrapper links in the same seam as `affectedSkill`, so sparse packet skills such as `Skill/3510.img/skill/35100008` can still reach hostile companion data on `35101009` / `35101010` before disposition and inferred mob-status replay run. This pass also closes the team-only ownership hole in the friendly lane: the existing WZ-backed `SupportsTeamMembers` wording now reaches the runtime because remote support areas no longer hardcode `ownerIsSameTeamMember = false`, and the affected-area owner check now resolves shared-team membership from the battlefield team owner plus the Monster Carnival name-to-team table before the local support, avatar-effect, and invincibility seams decide whether the local player is a valid recipient. Remaining gap: hostile remote player-zone ownership and targeting are still simulator-side approximations rather than a client-authored reconstruction; support branches that need bespoke simulator mechanics beyond the current projected buff, resistance, cleanse, damage-reduction, damage-to-MP, outgoing-damage, reward-rate, crit-damage, mastery, boss-damage, and ignore-defense seams still stop short of full gameplay; wrappers that still expose no hostile metadata across the visible node, `affectedSkill`, or `dummyOf` chain can still under-classify; the remote-owner recipient for linked damage-sharing fields is still not simulated; and some `type=3` fog lifetimes still fall back to packet removal when no reachable WZ time metadata, linked path, or duration-bearing description exists. | Packet-owned remote affected areas now preserve more of the client-authored hostile mist lane instead of degrading DOT-backed area wrappers into rough generic damage ticks. WZ-backed hostile areas with authored DOT payloads now carry their own per-skill magnitude, tick cadence, and duration into the existing inferred mob-status runtime, while the already-shipped support projection, mist resistance, linked support ownership, linked hostile-branch replay, mixed Battle Mage aura behavior, the newly wired battlefield / Monster Carnival team-target ownership checks, and the new `dummyOf` wrapper traversal remain in the same seams. This keeps remote areas such as Fire/Poison `3120010`, Mechanic `35101009`, wrapper skills such as `35100008`, and WZ-authored team-targeted support zones closer to their authored behavior without introducing a separate area-combat system. | field affected-area/runtime layer (`CAffectedAreaPool::OnPacket`, `CAffectedAreaPool::OnAffectedAreaCreated`, `CAffectedAreaPool::OnAffectedAreaRemoved`), `AffectedAreaPool.cs`, `MapSimulator.RemoteAffectedAreaPackets.cs`, `RemoteAffectedAreaSupportResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `MonsterCarnivalField.cs`, WZ `Skill/312.img/skill/3120010/common/{dot,dotInterval,dotTime}`, WZ `Skill/3510.img/skill/35101009/common/{dot,dotInterval,dotTime}`, WZ `Skill/3510.img/skill/35100008/info/dummyOf`, WZ team-target wording paths consumed by `RemoteAffectedAreaSupportResolver.SupportsTeamMembers(...)`, battlefield team ownership in `BattlefieldField`, and Monster Carnival team ownership in `MonsterCarnivalField` |
| Partial | Field-specific restrictions and effects | Map entry now surfaces `timeLimit`, `decHP`, `protectItem`, `allowedItem`, ambient `rain` / `snow`, `Unable_To_Jump`, `Unable_To_Fall_Down`, `Unable_To_Use_Teleport_Item`, field-transfer, `Unable_To_Use_Rocket_Boost`, `Unable_To_Open_Mini_Game`, `Unable_To_Use_Pet`, `Auto_Expand_Minimap`, `Parcel_Open_Limit`, `No_Quest_Alert`, `No_Android`, `Unable_To_Change_Party_Boss`, `Drop_Limit`, `No_Damage_On_Falling`, Mystic Door field-limit metadata, positive WZ `moveLimit` counts, and WZ-authored `onUserEnter` / `onFirstUserEnter` / `fieldScript` names through simulator notices, matching `rain` / `snow` map-info flags automatically start the simulator's ambient weather layer on field entry, matching `Auto_Expand_Minimap` fields now force the minimap open on entry through the existing minimap seam, `onFirstUserEnter` notices and tagged-object publication now only fire on the first visit to that map in the current simulator session while `onUserEnter` / `fieldScript` continue to publish every entry, blocked skill casts raise explicit field-limit feedback for both Rocket Booster and Mystic Door map bans, jump-disabled maps now reject jump input with the same field-rule messaging seam, jump-down-disabled maps now reject down-jump input through that same field-rule path, cross-map portal travel respects `Unable_To_Migrate`, `decHP` maps deal periodic environmental damage with a mist cue unless the inventory currently holds a listed `protectItem`, WZ-authored positive `recovery` values now tick HP and MP recovery through the player's existing recover seam every 10 seconds, `timeLimit` maps warn before expiry then transfer to `returnMap` / `forcedReturn`, `allowedItem` maps now reject non-listed inventory-backed item consumption plus setup-chair activation through the same field-rule notice path, cash teleport-rock items now route into the simulator's map-transfer window unless `Unable_To_Use_Teleport_Item` blocks them, WZ-authored numeric `consumeItemCoolTime` values now enforce a shared consume-item cooldown through the same inventory guard seam, and tracked cash-side consumables now share that same cooldown instead of leaving the map rule effectively `USE`-only when the simulator routes a cash inventory click through the consumable or pet-food lanes. Pending cross-map transfers now reject under-`lvLimit` characters plus maps marked `partyOnly` or `expeditionOnly` using the simulator's existing party-roster and expedition-search session state with explicit map-restriction notices, teleport-book registration plus its status seam now reuse that same WZ-backed level / party / expedition admission context instead of still advertising or saving destinations the active session cannot legally enter, and the move-facing map-transfer lanes now consume that same context too: cached saved routes, return/forced destinations, portal-authored destinations, and world-map-selected manual targets now carry the resolved admission restriction into the existing destination model, blocked cached destinations disable the move action in the existing map-transfer UI, the selected-route detail line now surfaces the matching restriction text, and direct world-map or manual map-transfer move requests now reject through the existing field-rule message seam before the simulator stages a cross-map handoff. Cached WZ-backed entry gates now also short-circuit more non-UI cross-map seams before the simulator spends a fade/load cycle: portal double-click travel, `Up`-driven portal transfers, temporary-portal cross-map exits, field-timer / special-field return transfers, consumable move-item transfers, and packet-owned whisper-follow transfers now all reuse the same known `lvLimit` / `partyOnly` / `expeditionOnly` admission context when the destination map metadata is already available locally, while uncached destinations still fall back to the existing pending-transfer rejection seam before map content is swapped. Positive `moveLimit` values now cap WZ-classified movement-skill casts through the same field-skill rejection seam, additional WZ-backed `fieldLimit` item families now publish entry notices and reject inventory use through the same field-rule path for portal scrolls (`203xxxx`, including the separate specific-scroll bit), summon sacks (`210xxxx`), cash weather (`512xxxx`), lie-detector anti-macro items (`219xxxx`), wedding invitation items, NPC-summon items, pet-item families, and WZ-modeled stat-change consumables that apply temporary buffs, morphs, modeled recovery, or cure effects, and the wedding invitation plus NPC-summon classifiers now also use concrete WZ-backed invitation item families plus WZ `spec/script=summonEventNpc` and broader WZ NPC-reference metadata (`spec/npc` and `info/npc`) instead of relying only on localized text matches. The pet-item classifier is stricter now too: WZ-backed pet-food `spec/inc` items count as pet interactions even when localized text does not literally say `pet`, so `Unable_To_Use_Pet` no longer depends on name matching to block those feeds. Unsupported cash-inventory clicks for those WZ-backed families now also surface the same field-rule rejection path instead of failing silently when the simulator does not model any additional item runtime beyond the restriction itself, which closes the feedback gap for cash weather (`512xxxx`), pet cash items (`517xxxx` / `518xxxx` / `519xxxx`), and cash wedding invitation cards, and the cash-avatar-preview weather callback now routes through that same cash-weather field-rule message seam instead of staging a preview effect when `Unable_To_Use_Cash_Weather` is active. `Unable_To_Open_Mini_Game` now blocks mini-room, personal-shop, and entrusted-shop window surfacing through the simulator's existing social-room launch seams, including the cash-avatar-preview callbacks that previously opened shop owners directly, `Parcel_Open_Limit` now blocks memo-mailbox and quest-delivery window surfacing through the simulator's existing utility-window seams, including status-bar mailbox launch, memo-send return, packet-authored parcel dialogs, and packet-authored quest-delivery launches, `No_Quest_Alert` now blocks quest-alarm surfacing through the existing quest-alarm seam, `No_Android` now hides the Big Bang equip window's android pane while active and now also rejects android companion equip/unequip change submissions and delayed resolutions through the existing equipment-change authority seam, `Unable_To_Change_Party_Boss` now blocks party-leader reassignment through the social-list action seam, `Drop_Limit` now rejects local and pet drop looting through the existing pickup-availability path with explicit pickup feedback, `Unable_To_Use_Pet` now suppresses live pet runtime behavior by hiding pet updates/draw, blocking pet speech/slang/command and auto-HP interactions, and preventing pet-food planning plus the remaining WZ-backed pet-feed feedback command path while the field rule is active, and player landing now applies a simulator-owned distance-and-impact fall-damage model that is suppressed by `No_Damage_On_Falling` through the same field-limit authority. Actual field-script execution beyond these notice/publication effects, broader server-authored admission state beyond the currently WZ-backed level / party / expedition gates, exact client-owned fall-damage thresholds or formulas, drop spawning or discard restrictions beyond looting, android restrictions outside the currently modeled equipment-pane/equipment-change/runtime seams, and any client-only checks that depend on packet/session context remain unmodeled. | Event and boss maps now expose a broader slice of client field-rule timing, movement locks, migration locks, hazard behavior, periodic recovery maps, ambient field weather, `protectItem` hazard suppression, `allowedItem` item-use gating, teleport-item map bans, several concrete `fieldLimit` item-family bans, auto-expanding minimap maps, Mechanic rocket-mobility bans, mini-room/shop opening bans, parcel and quest-alert utility-window bans, live pet-runtime suppression, numeric consume cooldowns, movement-skill count caps from positive `moveLimit` values, first-visit-only `onFirstUserEnter` behavior, basic level, party, and expedition entry gating, concrete runtime handling for android-tab suppression plus android companion equip/unequip rejection at equipment-change submission and resolution, party-leader change bans, and drop-loot bans through existing UI, social, and pickup seams, and now a landing-based fall-damage gameplay path that honors `No_Damage_On_Falling` instead of leaving that branch as notice-only metadata. This pass further hardens the WZ-backed item-family lane and the launch seams that consume those rules: wedding invitation bans still recognize the concrete invitation IDs and `4211xxx` / `4212xxx` ETC families, NPC-summon bans now also treat positive WZ NPC references from either `spec/npc` or `info/npc` as authoritative instead of only the narrower `spec/script=summonEventNpc` path plus text fallback, WZ-backed pet-food `spec/inc` items now classify as pet interactions even when localized text omits the word `pet`, tracked cash-side consumables now share the authored field `consumeItemCoolTime` cooldown through the same item-use guard seam instead of bypassing it, unsupported cash-family clicks now reuse the same field-rule feedback seam instead of silently no-oping when no extra simulator-side runtime exists for the blocked item, mini-room/shop plus parcel-owned utility restrictions now cover the remaining cash-preview, packet-owned, and mailbox-return launch paths instead of only the core UI buttons, cash-avatar-preview weather selections now reuse the same cash-weather restriction seam the inventory-backed `512xxxx` lane already used instead of bypassing the field rule through a preview-only callback, and the map-transfer movement lanes now match the existing registration/status gates more closely by carrying cached `lvLimit` / `partyOnly` / `expeditionOnly` restrictions into the route list itself, disabling blocked cached-route moves in the existing window, surfacing the restriction on the selected-route detail line, and rejecting direct world-map or manual move requests before a cross-map handoff is queued. The same admission context now also short-circuits known cached destination bans across portal-driven, consumable-driven, packet-owned whisper-follow, and field-owned return transfers instead of only surfacing after a later fade/load handoff is already under way. Focused source-side regression coverage now includes `UnitTest_MapSimulator/FieldTransferRestrictionParityTests.cs` for the shared move-facing admission helper inputs and cached destination gating assumptions. Generic entry-owned field scripts are still not executed beyond notices or tag publication, deeper server or session admission checks are still absent, the fall-damage model is still an approximation rather than a byte-accurate reconstruction of the client rule, and broader android runtime and packet-owned restrictions outside the currently modeled equip-pane/equipment-change seams plus drop spawning or discard restrictions still need dedicated simulator seams. | `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.PacketOwnedLocalOverlayParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.FieldRestrictionParity.cs`, `MapSimulator.EquipmentWindowParity.cs`, `MapSimulator.Content.cs`, `MapSimulator.CashServiceStageParity.cs`, `Companions/PetController.cs`, `Interaction/SocialListRuntime.cs`, `UI/Windows/EquipUIBigBang.cs`, `UI/PickupNoticeTextFormatter.cs`, `FieldRuleRuntime.cs`, `FieldRuleEffectApplier.cs`, `FieldEntryRestrictionEvaluator.cs`, `FieldEnvironmentEffectEvaluator.cs`, `FieldSkillRestrictionEvaluator.cs`, `FieldInteractionRestrictionEvaluator.cs`, `FieldFallingDamageEvaluator.cs`, `TeleportItemUsageEvaluator.cs`, `UI/Windows/InventoryItemMetadataResolver.cs`, `UI/Windows/MapTransferUI.cs`, `UnitTest_MapSimulator/FieldTransferRestrictionParityTests.cs`, field systems (`CField::IsFearEffectOn`, `CField::OffFearEffect`) |

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned drop-pool parity | The simulator now has an explicit packet-owned drop runtime instead of leaving `CDropPool` implied behind local drop helpers. A dedicated `RemoteDropPacketRuntime` plus `RemoteDropPacketCodec` model the client-owned `322 / 324` dispatch surface on the existing MapSimulator seams, `DropPool` can apply packet-authored enter and leave packets directly, field rebinding clears only packet-authored drops, and a focused `/droppacket` command can drive the same enter/leave path for manual verification (now including optional packet `elevateLayer` when creating synthetic enter packets, and client-shaped `petIndex` wording for synthetic pet-pickup leaves). The packet-backed `DropPool` side stores client-facing ownership/source metadata, now resolves enter start positions through live local-player, local-pet, remote-user, remote-pet slot-offset, and mob source positions when those runtimes are available, and now prefers that live source position even when the packet already carries `pt1`, matching the scanned `CDropPool::OnDropEnterField` mob-source override before falling back to authored packet start or the generic target-offset fallback. It also handles packet-owned direct player/pet/mob pickup removal through the existing pickup-notice and recent-history callbacks, and consults the existing remote-user/social-list seam so `DropOwnershipType.Party` honors tracked party members instead of treating every party-owned packet drop as owner-only. `DropItem` update remains aligned toward scanned `CDropPool::Update` timing: packet-owned drops honor packet create delay before settle and fake-vs-real state, follow client-shaped parabolic and settle branches derived from scanned `pt1 / pt2` math, now retire fake packet drops once they settle or once `tCur - tCreateTime > 3000`, hold packet-authored `324` reason `0` in a one-second fade, and resolve pickup leaves (`2 / 3 / 5`) through local player/pet, remote-user, and mob position seams for absorb-style removal motion. This run also closes the remaining delayed-create presentation mismatch that was still visible in `CDropPool::Update`: packet enter type `3` now stays hidden while `tCur <= tCreateTime`, then starts falling with a client-shaped one-second alpha ramp instead of appearing fully opaque the moment the synthetic packet is applied. Local packet pet pickups now resolve that pet slot back through the live active-pet runtime so recent-history, pickup callbacks, and absorb-target positioning use the correct local pet runtime id instead of misreading the packet tail as a pet id; remote packet pet pickups now encode owner plus slot through the existing remote-pet pickup naming seam so remote labels resolve against tracked remote pet slots instead of treating the packet tail as a remote pet runtime id, and packet-owned remote pet pickup motion now reuses the same remote-pet slot-offset seam already used by simulated remote pet looting so absorb-style removal aims at the tracked remote pet slot instead of always collapsing to the owner position when that owner/slot pair is known. Packet-owned absorb-style removal is also closer to the recovered `RegisterAbsorbItemAnimation` ownership model now: once a packet leave resolves a player, pet, or mob target, the in-flight pickup keeps a live target-position resolver instead of only snapshotting the actor position at packet-apply time, so local pet, remote pet, and player-owned absorb motion can continue aiming at the moving actor through the pickup window. Packet leave reason `4` (explode) is also closer to client cadence: it keeps delayed explode scheduling, raises a dedicated packet-explode callback on actual delayed removal, throttles explode callback cadence at the client-shaped `90 ms` pool level, and resolves its presentation through the same WZ/client-backed `Skill/421.img/skill/4211006/hit/*` and `Sound/Skill.img/4211006/Hit` seam the scan pointed at, including distance-scaled playback, instead of falling back to the older generic explosion effect path when those assets are available. Packet enter timing is also tighter against `CDropPool::Update` now: animated packet-owned drops no longer advance WZ frames while the client-style `tCreateTime` delay is still pending, delayed packet enter type `1` still raises the throttled drop-enter sound callback at the client-shaped `300 ms` cadence when creation promotes into falling state, packet-owned animated visuals restart their frame playback from frame `0` when the delayed enter actually activates so the first visible frame lines up with the decompiled `Animate(GA_REPEAT)` transition instead of burning animation time while still queued, and delayed type `3` drops now use the same promotion tick to begin their alpha reveal instead of rendering at full opacity during the queue window. WZ-backed `Item/Special/0900.img/09000000..09000003/iconRaw/*` money frames are pushed through the shared `DropPool` resource seam so packet-authored meso drops no longer fall back to static icons, and the packet-owned meso render pass now keeps client-shaped banded layer counts and timing closer to `CDropPool::MakeMoneyAnimation`: the layered draw helper still uses the scanned `80 ms / 120 ms` cadence split with `2 / 3 / 4` visible layers across the small, medium, large, and bag money bands, while respecting the WZ `iconRaw/*/origin` anchors during layered draws and driving those layers through per-band alpha / zoom / lift envelopes instead of a single generic pulse. The packet `elevateLayer` tail byte is retained and honored by a dedicated elevated draw pass, and packet-owned non-meso visuals are closer to the client `CAnimationDisplayer::LoadLayer` branch now too: when `ItemInfo` exposes `info/drop`, the simulator routes that property through the shared `MapSimulatorLoader.LoadFrames` seam instead of recursively flattening nested canvases by hand, so packet-authored item drops honor the same numeric-frame traversal, per-frame `delay` metadata, and WZ canvas `origin` handling already used across other animated simulator layers before falling back to static inventory icon art when no client drop art exists. Focused source-side coverage now includes `RemoteDropPacketParityTests` for delayed packet-owned type `3` hide-and-fade activation as well as delayed packet-owned animation activation through the public `DropPool` packet seam, alongside the existing live-source enter start override and client-shaped fake-drop retirement branches. Remaining gap: party ownership is still inferred from the simulator local tracked party roster rather than a real packet-authored party-id surface, so broader owner-group arbitration outside local party view remains approximate; exact COM-owned `GR2D::LoadLayer` canvas insertion choreography for some packet meso and non-meso visuals is still approximated even though the simulator now follows the same shared WZ frame loader and per-frame delay/origin path; remote pet-owned pickup motion still has to fall back to the remote-owner position whenever the packet/runtime surface cannot be mapped onto a tracked remote owner plus pet slot or when no independently tracked remote pet world state exists beyond that slot-offset approximation; broader packet-owned owner-group arbitration outside the currently tracked local party view still has no real packet surface to anchor against; and packet enter starts still fall back to the generic target-position offset when the packet source id cannot be resolved onto any live local, local-pet, remote-user, remote-pet, or mob runtime. | Without an explicit `CDropPool` row, drop parity is easy to overstate once pickup text, local loot, or pet auto-loot exists. The client keeps a dedicated pool owner for enter/leave/update/pickup behavior, so tracking it separately keeps future work anchored to the real field-entity runtime instead of scattering it across UI feedback, pet logic, or generic map-object handling. | field drop/runtime layer (`DropPool.cs`, `PacketOwnedMesoAnimationPresentation.cs`, `RemoteDropPacketCodec.cs`, `RemoteDropPacketRuntime.cs`, `MapSimulator.RemoteDropPackets.cs`, `MapSimulator.RemoteDropPacketEffects.cs`, `MapSimulator.ChatCommands.cs`, `UnitTest_MapSimulator/RemoteDropPacketParityTests.cs`, `CDropPool::OnPacket`, `CDropPool::OnDropEnterField`, `CDropPool::OnDropLeaveField`, `CDropPool::Update`, `CDropPool::TryPickUpDrop`, `CDropPool::TryPickUpDropByPet`, `CDropPool::TryPickUpDropByMob`, `CDropPool::MakeMoneyAnimation`, `CDropPool::RecordRecentPickupItem`, `CAnimationDisplayer::RegisterAbsorbItemAnimation`, `Item/Special/0900.img/09000000..09000003/iconRaw/*`) |

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive these flows from local input and generic runtime state. It also has dedicated packet-response handlers for chair sit ack or correction, same-field teleport completion, and quest-result or quest-timer application.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned quest-result parity | The simulator now has a dedicated local `OnQuestResult` packet seam on opcode `242` instead of relying only on local quest helpers or the older field-owned timer path. The dispatcher decodes the client-confirmed quest-timer families (`case 6` through `9`, plus expiry/reset handling from `17` and `18`) into the existing `PacketFieldStateRuntime` timer owner, routes quest-result subtype `10` through a WZ-backed quest presentation builder that reads quest metadata already loaded from `Quest/QuestInfo.img`, `Quest/Say.img`, and `Quest/Act.img`, applies the existing job or gender reward filters when building follow-up text, keeps the client-observed subtype `10` ordering by deferring the visible quest-act notice until after any util-dialog pages close, and still bridges the trailing follow-up quest id into an actual packet-owned `StartQuest` call once that modal returns. The packet decode is now aligned with the direct client scan as well: subtype `10` no longer accepts simulator-only text-kind or view-mode bytes, requires the client-shaped trailing follow-up quest id after the quest and NPC ids, chooses the start-vs-end WZ branch from the same quest-record-present check that `CUserLocal::OnQuestResult` uses instead of treating completed quests as a separate presentation family, applies the client-shaped `DeleteFadeWnd(7, 0, questId, "", 0)` cleanup before reading that follow-up id, and the fresh `CUserLocal::OnQuestResult` re-check at `0x914080` confirms that this owner really only switches over `case 6` through `18` before tail-calling `CWvsContext::CheckNewQuestAvailable(0)`. Coverage is broader than before too: subtype `10` and subtype `12` now both build their visible notice through a shared client-shaped quest-act summary path instead of the older full reward dump, the act summary collapses rewarded items into the same inventory-category family the client assembles from `Quest/Act.img` after applying the same job or gender filters, now stays limited to that client-shaped inventory-family summary instead of appending simulator-owned EXP/fame/message/detail lines, excludes the simulator-only cash-tab summary that the client branch never appends, and follows the recovered client `StringPool` format-id structure more closely by modeling the category labels (`10`, `6791`, `11`, `6712`), the shared separator (`3293`), the inventory-summary wrapper (`3292`), and the negative-meso wrap (`4552`) that `CUserLocal::OnQuestResult` uses around the `Quest/Act.img` reward-category flow. Direct MapleStory.exe `StringPool::ms_aString` recovery closes the remaining text gap for the covered families too: subtype `10`/`12` notices use the exact client wrapper text (`3292 = "%s item inventory is full."`, `3293 = " or"`, `4552 = "Either you don't have enough Mesos or %s"`), subtype `17` expiry chat now formats the recovered client template from `0x1015`, and fixed-notice subtypes `11`, `13`, `15`, and `16` resolve to the exact client strings behind `0xCDA`, `0x1A8B`, `0xD97`, and `0xD98`. The visible modal surface is closer now too without leaving the existing seam: `PacketQuestResultUtilDialog` already loaded the recovered `UI/UIWindow(.img|2.img)/UtilDlgEx` top, center, bottom, separator, and navigation button canvases, and this pass extends that same WZ-backed surface by loading the `UtilDlgEx/bar` name-strip art, rendering the speaker name through that bar inside the portrait column instead of as bare text, making the previously cosmetic `keyFocused` button art actually track packet-owned keyboard focus, animating the speaker portrait from WZ-backed NPC frames instead of pinning the overlay to a single `stand/0` bitmap, and routing packet-owned button activation through an explicit util-dialog modal-result seam that mirrors the recovered `CDialog::DoModal` return split (`0x2000` = prev, `0x2001` = next / OK, anything else = close). The speaker lane now reads `Npc/<id>.img/say/speak/*` action aliases first, falls back to `stand` or `action` when those aliases are missing, loads the resolved numeric frame sequence through the existing WZ seam, advances it on the authored per-frame `delay` values (falling back to the client NPC default cadence), and anchors each frame through its WZ `origin` inside the portrait column so client-authored idle or talk loops no longer jitter or collapse back to a static sprite. Left-arrow now shifts focus to `BtPrev` when a previous page exists, right-arrow or `Tab` restores focus to `BtNext` or the final `BtOK`, `Shift+Tab` moves back onto `BtPrev`, `Enter` activates the currently focused packet-owned button through that shared modal-result seam, and `Esc` now leaves through the same packet-owned close-result path instead of bypassing the util-dialog owner logic. The client tail-call is closer too: successful packet-owned quest-result application snapshots the available-quest set before decode, compares it against the rebuilt available-quest set after the packet-owned handler finishes, and flags newly surfaced quests through the existing quest alarm/update seam so the simulator mirrors `CWvsContext::CheckNewQuestAvailable(0)` more closely than the earlier detail-window-only refresh. Focused source coverage on disk now includes the subtype `10` notice-order helper checks, follow-up close-behavior checks, the newly-available-quest diff helper, and the quest-result act-summary-only notice checks in `UnitTest_MapSimulator/PacketQuestResultClientSemanticsTests.cs`. Remaining gap: the modal still runs through the simulator's reusable overlay event loop rather than a dedicated blocking `CUtilDlgEx` owner, so it still lacks the true client dialog object lifecycle, per-dialog allocation / destruction path, the exact `CUtilDlg::Notice` surface that follows subtype `10` dialog completion, and exact owner-managed `DoModal` state beyond the simulator's local modal-result approximation. | Quest parity is not only local accept/complete helpers, quest windows, or generic field timers. The client owns a packet-response dispatcher that adds and removes quest timers, shows quest-result notices and modal pages from WZ-backed quest data, can emit fixed or act-driven quest-result notices, can auto-start a follow-up quest after the modal returns, and then refreshes quest availability from that same packet seam. Tracking this row against the dedicated opcode `242` owner keeps the remaining work anchored to the real response path instead of scattering it across local quest commands or field-state utilities. | packet-owned local quest-result layer (`CUserLocal::OnPacket`, `CUserLocal::OnQuestResult`, `CWvsContext::AddQuestTimer`, `CWvsContext::RemoveQuestTimer`, `CWvsContext::CheckNewQuestAvailable`, `CWvsContext::StartQuest`, `CWvsContext::DeleteFadeWnd`, `CUtilDlgEx`, `Quest/QuestInfo.img`, `Quest/Say.img`, `Quest/Act.img`, `UI/UIWindow.img/UtilDlgEx`, `UI/UIWindow2.img/UtilDlgEx`, `MapSimulator.PacketOwnedQuestResultParity.cs`, `HaCreator/MapSimulator/Interaction/NpcInteractionOverlay.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultClientSemantics.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultUtilDialogLayout.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultFadeWindowRuntime.cs`, `UnitTest_MapSimulator/PacketQuestResultClientSemanticsTests.cs`) |
| Implemented | Packet-owned chair sit-result parity | The simulator now has a dedicated local `OnSitResult` packet seam on opcode `231` instead of treating chairs as local-only intent. The packet-owned handler stamps the shared local utility request tick, mirrors the client-owned `CWvsContext` chair bookkeeping in the packet-owned local-utility context by clearing the chair exclusive-request flag at `+0x20B8` and restamping the exclusive-request tick at `+0x20BC`, decodes the client-shaped success flag plus seat index payload, resolves the current map's WZ `seat/<index>` vector, validates the local player against the same client chair rectangle (`x +/- 10`, `y +/- 30`), and routes successful placement through a packet-owned local sit placement helper that also raises a dedicated secure sit confirmation flag on the local player. Successful packet-owned placement now clears any attached local passenger linkage through the existing follow-runtime and remote-user seams before the sit is finalized, matching the client path that zeroes `m_dwPassenserID` and the passenger's driver link. Invalid returned seats or rectangle mismatches now force the same stand-up correction path and send that correction through the shared local-utility context owner instead of a raw timestamp: the simulator still records a client-shaped `CWvsContext::SendGetUpFromChairRequest(0)` outpacket with opcode `45` and payload `FFFF`, re-arms the exclusive-request flag after emission, suppresses the send when the local HP gate or exclusive gate would block it, and injects that raw opcode `45` packet through a configured `LocalUtilityOfficialSessionBridgeManager` owner when a live Maple socket is attached. That bridge seam is broader now too: `/localutility session start ...` and `/localutility session startauto ...` no longer behave as one-shot manual helpers only. They persist their direct-proxy or discovery configuration onto the packet-owned local-utility owner, the simulator now keeps that bridge armed from the main update loop alongside the inbox, auto-refreshes discovery while no Maple client is attached, preserves an already attached live session instead of tearing it down during discovery refresh, reports outbound chair corrections in bridge status with pending and queued counts plus the last injected and last deferred raw packets, and now keeps a built-in deferred outbound queue for chair correction packets whenever the official-session bridge is enabled but no Maple session has finished attaching yet. Those deferred packets survive bridge re-arming and discovery refresh, then flush through the live Maple socket as soon as the proxied session completes the init handshake, so packet-owned chair corrections no longer depend on an external loopback consumer to reach the official-session transport path. The no-live-session fallback still remains the dedicated `LocalUtilityPacketTransportManager` loopback outbox on `/localutility outbox [status|start [port]|stop]`, which emits raw outbound corrections as `packetoutraw` lines containing the decrypted opcode-framed Maple packet, records the last emitted raw chair correction on that outbox owner, and lets packet-owned chair corrections leave the simulator whenever no live Maple bridge is attached but an external loopback consumer is listening. That outbox owner still keeps its own deferred packet-owned queue for chair correction packets when no outbox client is currently attached, flushes queued packets as soon as a loopback client connects (or before the next immediate send when a client is already attached), and reports both sent and pending correction counts in local-utility status so these packet-owned corrections no longer collapse into simulator-only state immediately when transport is temporarily unavailable. Packet rejection still clears the sit state without emitting that correction request, and the rejection teardown now also clears any stale external-owner couple-chair overlay when an active portable chair is unwound through the same local stand-correction path, matching the client-owned `Update(0)` cleanup branch more closely than the earlier partial teardown.g.cs`, `MultiBoard.g.cs`, and `HaCreator_Content.g.cs` missing under `HaCreator/obj/Debug/net10.0-windows`). | Chairs are not only a local toggle or render-state change. The client keeps a packet-owned success/failure and seat-validation branch that decides whether the user really sits, whether a passenger link is broken, and when to force a stand-up correction. Tracking this row against the dedicated opcode `231` seam keeps the remaining work anchored to the real local response owner instead of only local chair commands or remote chair packets. | packet-owned local chair-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnSitResult`, `CWvsContext::SendGetUpFromChairRequest`, WZ `Map/Map*/<map>.img/seat`, packet-owned local sit placement, correction path toward `PacketOwnedLocalUtilityContextState`, `LocalUtilityOfficialSessionBridgeManager`, `LocalUtilityPacketTransportManager`, `UnitTest_MapSimulator/LocalUtilityOfficialSessionBridgeManagerTests.cs`) |
| Partial | Packet-owned teleport-result parity | Standard same-map portal travel still finishes through the packet-owned local `OnTeleport` seam: visible and hidden portals targeting the current field use the WZ-authored portal `delay` when present, otherwise the client-confirmed 120 ms handoff, stage a pending same-map teleport against the destination portal index, and let the dedicated apply helper own the final landing and `TryRegisterTeleport(..., bForced = 1)` side effects instead of writing position directly from the interact path. Temporary same-map completion stays in that seam too, carrying any resolved real source `pn`, forwarding same-map target `pn` metadata, and using relaxed current-field `CPortalList` coordinate matching so near-equal authored landings can still collapse to a real portal index. Cross-map portal travel remains packet-owned as well: pending teleport targets survive `UnloadMapContent`, reload the new field, try the rebuilt `PortalPool` first by the primary returned `pn`, then by preserved fallback candidate names, and only then fall back to landing-coordinate resolution or exact-coordinate apply. This pass broadens that post-load collapse again in the existing seam: preserved candidate destination names no longer require the raw candidate strings to be unique up front, because after field load the simulator now replays those names against the rebuilt `PortalPool`, promotes them into a true returned portal handoff whenever stale candidates drop out to one live portal index, and also replays the preserved landing coordinates against the still-live candidate subset so a rebuilt field can recover one real returned portal index even when multiple preserved destination names survive reload but only one of those live candidates still matches the stored landing point. Standard visible or hidden cross-map portals still preserve WZ-backed reciprocal return-link fallback (`target.tm == currentMap` and `target.tn == source.pn`) plus unique map-only return fallback from the synthetic opcode `113` request owner, and temporary cross-map travel still mirrors that metadata preservation while recovering real current-field source `pn` values through relaxed coordinate matching before target-side metadata recovery. Source-side collapse also stays broader in the packet-owned seam: synthetic opcode `113` request building and temporary-portal recovery preserve the full best-distance source `pn` set from portal `x`/`y`/`pn`/`tn`/`tm` metadata, intersect that set with explicit target `tn`, preserved target candidate names, and unique map-only return metadata, and only then drop to the older target-only inference or exact-coordinate completion. Exact-coordinate completion still avoids inventing a fallback target name when the packet-owned seam only knows ambiguous candidates, and the outbound opcode `113` owner still first tries the live `LocalUtilityOfficialSessionBridgeManager`, then the generic `LocalUtilityPacketTransportManager` `packetoutraw` outbox, before queueing deferred delivery while preserving raw payload and dispatch summary. WZ evidence for the active seam remains the portal metadata fields in `Map/Map0/001000000.img/portal/*/{pn,tn,tm,x,y,delay}` and the general teleport effect in `Effect/BasicEff.img/Teleport`, whose authored animation remains 4 frames at 90 ms each; direct client evidence remains `CUserLocal::OnPacket` opcode `234` dispatch into `CUserLocal::OnTeleport`, where the client decodes the success byte, looks up the returned `CPortalList` entry by portal index, writes the landing point from that portal, and then re-enters `TryRegisterTeleport(..., bForced = 1)`. Focused source coverage now on disk in `UnitTest_MapSimulator/PacketOwnedTeleportParityTests.cs` covers rebuilt-field candidate collapse when only one preserved destination still resolves in the live pool, rejection when multiple preserved candidates still resolve after reload, and the new rebuilt-field collapse path where multiple preserved destination names still resolve live but only one of those candidates still matches the preserved landing coordinates. Remaining gap: target-map destination candidate sets that still resolve to more than one rebuilt `CPortalList` entry even after replaying the preserved landing coordinates against the live candidate subset intentionally stay unnamed and therefore still complete by coordinates instead of a named returned portal; destinations that do not correspond to any rebuilt `CPortalList` entry after field load still require coordinate-based completion instead of a true returned portal index; synthetic or temporary branches still fall back to exact coordinates when current-field source coordinates plus all target-side metadata still do not collapse to one real current-field `CPortalList` source portal name; and transport still relies on the generic bridge/outbox owners rather than a teleport-specific official socket owner when no live direct injection path is available. | Teleport parity is not only about entering a portal or moving to the right coordinates. The client also has a response-time authority point that applies the actual destination by portal index and records the named teleport handoff after the packet returns. Tracking that seam keeps the remaining parity gap anchored to the real response owner instead of hiding it behind already-implemented local portal interaction flow. | packet-owned local teleport-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnTeleport`, `CPortalList`, `CUserLocal::TryRegisterTeleport`, WZ `Map/Map0/001000000.img/portal/*/{pn,tn,tm,x,y,delay}`, WZ `Effect/BasicEff.img/Teleport`, `MapSimulator.cs`, `MapSimulator.PacketOwnedTeleportParity.cs`, `UnitTest_MapSimulator/PacketOwnedTeleportParityTests.cs`, `LocalUtilityOfficialSessionBridgeManager.cs`, `LocalUtilityPacketTransportManager.cs`) |

### 4. Portal and reactor collision ownership discovered by targeted `CUserLocal` decompile

The latest `CUserLocal` pass exposed one more field-runtime owner split that the backlog was still flattening into generic portal, teleport, and reactor rows.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Portal collision ownership parity | `CUserLocal::CheckPortal_Collision` is a much broader owner than a simple hidden-portal lookup. The decompile shows the client querying the current local-user position through vec-ctrl, resolving the colliding portal from `CPortalList`, then branching by portal type: type `9` enforces an exclusive-request gate, cooldown timing, and one-shot suppression before sending the portal request packet; type `12` triggers the vertical-jump portal path plus passenger checks; type `13` applies reactor-backed custom impacts or session-value impact setup in specific field families before rechecking passenger state; and the default branch decides between same-map registered teleports and full transfer-field requests before playing the handoff sound. The current field backlog already tracks portals, teleports, and passive transfer, but it still does not name this central collision owner or the fact that multiple portal families and impact-driven field utilities are split here instead of only in the later request/response paths. | If this seam stays implicit, portal parity can look like a thin wrapper around teleport requests even though the client keeps admission, timing, vertical-jump, custom-impact, same-map teleport, and transfer-field branching inside one collision owner. Tracking it explicitly keeps future work anchored to the client function that actually decides what a portal touch means. | portal collision field-runtime layer (`CUserLocal::CheckPortal_Collision`, `CPortalList::FindPortal_Collision`, `CField::SendTransferFieldRequest`, `CUserLocal::TryRegisterTeleport`, `CUserLocal::OnCollisionCustomImpact`) |
| Partial | Reactor collision polling parity | The simulator now owns a dedicated once-per-second `CheckReactor_Collision`-style poll in the main update loop instead of treating touch as an immediate reactor activation side effect. That owner remembers the last poll tick, calls into the existing `ReactorPool` seam to refresh a persistent local touched-reactor set, and the pool-side refresh now follows the recovered client contact rule more closely by testing the local vec position against each touch reactor's current bounds instead of intersecting a synthetic avatar rectangle. The poll also now diffs enter/leave ownership and routes packet-authored touch ownership through transport seams instead of local activation: when a live reactor official-session bridge is attached and the touched reactor has a real packet object id it injects the client opcode `250` enter/leave touch request built as `objectId + touchFlag`; when no live Maple session is attached it can now defer that same opcode on the reactor bridge until init completes; and when no reactor bridge is available it can now mirror or queue the same raw opcode `250` packet through a dedicated reactor touch `packetoutraw` outbox instead of collapsing immediately to simulator-local state. Remaining gap: map-authored local reactors that do not have a real server packet object id can still only update the simulator's local touched-reactor set, so those purely local reactors still have no authoritative client request to emit. | Reactor parity is not only about authored hit props or packet-fed state. The client also owns a local periodic touch-poll seam, so keeping this row tied to the poll owner plus its local touch-set refresh prevents later reactor work from smuggling contact ownership into unrelated packet handlers or local activation shortcuts. The transport side matters too: touch ownership is now preserved across a missing live session through the dedicated reactor bridge queue or reactor touch outbox rather than disappearing into local-only activation as soon as the immediate official-session path is unavailable. | reactor touch-poll layer (`CUserLocal::CheckReactor_Collision`, `CReactorPool::FindTouchReactorAroundLocalUser`, `MapSimulator.cs`, `ReactorPool.cs`, `ReactorPoolOfficialSessionBridgeManager.cs`, `ReactorTouchPacketTransportManager.cs`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".

