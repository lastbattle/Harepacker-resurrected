# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/DropPool.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Field and portal parity

- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CAffectedAreaPool::OnPacket` at `0x438330`
- `CTownPortalPool::OnPacket` at `0x7636b0`
- `COpenGatePool::OnPacket` at `0x68c8b0`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`

Notes:
`CUserLocal::TryPassiveTransferField` shows that client field transfer is not only explicit portal collision handling; the local-player update loop also retries an up-key style transfer after one-time actions finish when the character is allowed to move. A later IDA pass also shows that packet-authored field utility surfaces are split into their own remote pools rather than hanging off local collision handling: `CAffectedAreaPool::OnPacket` owns affected-area create/remove, `CTownPortalPool::OnPacket` owns town-portal create/remove, and `COpenGatePool::OnPacket` owns Open Gate create/remove.

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

- `CDropPool::Update` at `0x515890`
- `CDropPool::OnDropEnterField` at `0x516670`
- `CDropPool::OnDropLeaveField` at `0x511e20`
- `CDropPool::OnPacket` at `0x517410`
- `CDropPool::TryPickUpDrop` at `0x511860`
- `CDropPool::TryPickUpDropByPet` at `0x511ab0`
- `CDropPool::TryPickUpDropByMob` at `0x5112a0`
- `CDropPool::MakeMoneyAnimation` at `0x512df0`
- `CDropPool::RecordRecentPickupItem` at `0x515140`

Notes:
The backlog already covered pickup notices and pet auto-loot side effects, but the broader client owner was still unnamed. IDA shows that drop parity is owned by a dedicated `CDropPool` runtime that handles enter/leave packet dispatch, per-frame drop update, player/pet/mob pickup arbitration, meso animation, and recent-pickup history instead of treating drops as generic field objects plus UI messages.

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnSitResult` at `0x905e70`
- `CUserLocal::OnTeleport` at `0x913ff0`
- `CUserLocal::OnQuestResult` at `0x914080`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive chairs, same-field teleports, and quest results from local intent and generic runtime state. It also has dedicated packet-response handlers that finalize those flows after the request is acknowledged.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Field and portal parity

The old backlog understated what is already present here, but there is still real work left.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Hidden portals | Reveal, hide, alpha fade, and collision gating are implemented | Remove from missing backlog | `PortalPool.cs` (`CPortalList::FindPortal_Hidden`) |
| Implemented | Portal property accessors | `PH`, `PSH`, and `PV` equivalents are implemented | Remove from missing backlog | `PortalPool.cs` |
| Implemented | Portal interaction parity | Standard and hidden portal triggering now share the same same-map delay seam as client-confirmed portal teleports, and Mechanic `Open Portal: GX-9` casts now place WZ-backed temporary linked gates that resolve on `Up` interaction with the client-like 120 ms same-map handoff; town portal / mystic door remains tracked separately below | Portal travel now covers the main visible, hidden, and same-map skill-portal branches instead of reducing open-gate travel to unsupported or generic transitions | `PortalPool.cs`, `MapSimulator.cs`, `TemporaryPortalField.cs` (`CUserLocal::CheckPortal_Collision`, `CUserLocal::TryRegisterTeleport`, `Skill/3510.img/skill/35101005`) |
| Partial | Passive transfer-field handoff parity | WZ re-check in the active export root still keeps transfer ownership map-authored instead of portal-local: `map/Map/Map0/000050000/info/fieldLimit = 4`, while the same map keeps ordinary transfer metadata on `portal/5` (`pt = 2`, `tm = 1000000`, `tn = west00`) and a separate tutorial collision-script branch on `portal/6` (`pt = 9`, `script = infoWorldmap`). Client re-check in IDA still confirms `CUserLocal::TryPassiveTransferField` at `0x91a2e0` waits for `CAvatar::GetOneTimeAction() <= -1`, reads live field-interface gate `m_pvc.m_pInterface + 416`, only calls `HandleUpKeyDown` when `!IsImmovable && !IsAttract`, and then clears the pending passive flag once the interface gate is admitted; `HandleUpKeyDown` at `0x919e50` still clears skill macro first (`CMacroSysMan::Stop`), gates one-time action/immovable/foothold/portal flow, keeps same-map non-changeable travel on `TryRegisterTeleport`, and routes cross-map or same-map changeable travel to `CField::SendTransferFieldRequest` with portal sound only when `!PORTAL::IsChangable`. This pass narrows parity in the existing seam by decoupling queued interface readiness from transfer-candidate portal collision (`EvaluatePassiveTransferFieldQueuedRetryDecision` now resolves `HasReadyFieldInterface` with `ResolvePassiveTransferFieldReadyState(..., requirePortalCollision: false)`), so queued retries now clear once one-time action is complete and the inferred interface gate recovers even if collision has dropped, while replay admission still requires current transfer-candidate portal collision through `HasCollidingTransferPortal`. Existing parity remains in place: queued retry still runs before fresh-Up gating; fresh eligible Up still clears queued macro playback before portal handling; fresh Up still consumes chair get-up before portal replay/arming and clears queued retry when that chair branch is consumed; left/right keydown still cancels queued retry; fresh Up remains suppressed while `LocalFollowCharacterRuntime.AttachedDriverId` is set; passive-transfer field readiness still drops during packet-owned teleport registration cooldown (`_packetOwnedTeleportRequestCompletedAt` within `PACKET_OWNED_TELEPORT_REGISTRATION_COOLDOWN_MS`); passive queue/replay one-time-action admission still mirrors the client one-time attack owner gate by deriving ownership from attack playback state only (`MapSimulator.HasActivePassiveTransferFieldOneTimeAction` -> `PlayerCharacter.IsPlayingClientOwnedOneTimeAction`); transfer-candidate portal admission (`portal.tm > 0` and `portal.tm != 999999999`) stays split from transfer-request routing in `ShouldSendTransferFieldRequestForPortal`; queued replay macro-stop ownership still follows recovered `HandleUpKeyDown` ordering with pre-foothold admission split (`CanAttemptHandleUpKeyDownReplay` vs `CanReplayHandleUpKeyDown`); queued replay still clears after replay admission contracts complete; same-map client-changeable portals still send transfer-field requests; and `Changeable`/`ChangeableInvisible` transfer requests still suppress immediate local portal sound. Focused regression coverage for this seam now lives in `UnitTest_MapSimulator/PassiveTransferFieldHandoffParityTests.cs`, including queue decision helpers, one-time-complete hold-pending behavior while interface readiness is still unresolved, explicit clear-on-ready behavior when portal collision drops, transfer-candidate portal admission versus transfer-request split coverage, packet-owned teleport registration cooldown gating, attack-owned one-time-action admission helper coverage, queued replay macro-stop admission ownership, and pre-foothold HandleUp admission coverage. Remaining gap: simulator ownership for `m_pvc.m_pInterface + 416` is still inferred from recovered runtime seams rather than byte-for-byte reconstruction of every client writer/clearer, so additional unrecovered field-interface owners outside the current colliding-transfer-candidate-portal, field-rule, special-field, packet-transfer-active, packet-registration-cooldown, attached-driver, horizontal-key cancellation, queued macro-stop/replay-admission split, exclusive-request, same-map-transfer, scripted-sequence, and grounded replay seams may still be missing. | Without this row, passive transfer-field behavior can look like a minor portal follow-up even though the client treats it as a separate retry owner tied to movement state, one-time action completion, actual portal collision, vec-ctrl field readiness, transfer-request ownership, macro cancellation ordering, fresh-Up pre-branches, and the client-changeable portal branch. Tracking it explicitly here keeps future parity work anchored to the actual handoff gate instead of folding it into unrelated teleport or portal-collision polish. | `MapSimulator.cs`, `Character/PlayerCharacter.cs`, `Character/Skills/SkillManager.cs`, `Physics/CVecCtrl.cs`, `Fields/FieldRuleRuntime.cs`, `Fields/SpecialFieldRuntimeCoordinator.cs`, `Fields/PassiveTransferFieldReadinessEvaluator.cs`, `Interaction/LocalFollowCharacterRuntime.cs`, `UnitTest_MapSimulator/PassiveTransferFieldHandoffParityTests.cs`, portal/field transition layer (`CUserLocal::TryPassiveTransferField`, `CUserLocal::HandleUpKeyDown`, `CUserLocal::OnKey`, `CMacroSysMan::Stop`, `CAvatar::GetOneTimeAction`, `CWvsContext::SendGetUpFromChairRequest`, `CField::SendTransferFieldRequest`, WZ `Map/*/info/fieldLimit`) |
| Implemented | Transportation fields | Regular passenger ships now expose a dedicated transport-deck foothold that carries both the player and grounded mob passengers through the existing movement seam, while Balrog-only ship visuals no longer publish a false passenger deck | Transport maps now keep visible deck passengers riding the ship during departures and arrivals instead of leaving the transport system as a mostly visual-only layer | `TransportationField.cs`, `PassengerSyncController.cs`, `MapSimulator.cs` |
| Partial | Dynamic objects / quest layers | Field object drawables still retain WZ `quest` visibility metadata, keep authored `hide=1` quest layers suppressed until quest-state/tag publication reveals them, and keep tagged defaults from `publicTaggedObjectVisible` plus `pulbicTaggedObjectVisible` through the `MapLoader` copy seam. Entry-owned map scripts (`onUserEnter`, `onFirstUserEnter`, `fieldScript`), portal scripts/`pn` fallback, `directionInfo/*/EventQ`, quest `showLayerTag`, quest `startscript` / `endscript` / `npcAct`, NPC `info/script`, packet-owned NPC utility owners, reactor-owned publications, and item `spec/script` still publish through one dynamic-object seam with stage-family retirement, quest-id fallback (`q<questId>s` / `q<questId>e` -> `q<questId>` / numeric tag), path/extension normalization, numeric first-argument alias recovery, no-argument function alias-call recovery, and timer/callback delay parsing. This pass narrows the shared WZ publication parser again by keeping parent alias-container publication recoverable when nested alias leaves are authored as boolean-like state numerics behind wrapper branches: wrappers such as NPC `info/script/0/script` remain parseable, inherited parent delay metadata reaches both nested alias publications and parent alias publications before map-entry/NPC/quest/item string fallback runs, mixed recoverable branches (direct boolean alias leaves plus nested `0`/`1` + `script`/`scripts`/`info` wrapper stacks in the same alias container) keep parent alias publication instead of dropping it when wrapper and direct state leaves coexist, and metadata-wrapper-only stacks (`state`/`visible`/`value`/`show`/`on`) now stay recursively traversable so nested alias publications and parent alias publication both survive in the same pass. Remaining gap: script-engine-owned sequencing is still absent when no WZ-visible alias timing, recoverable surfaced alias argument (including function-style no-argument alias calls), surfaced timer/callback/continuation-shaped function text, quest `npcAct` metadata, or recoverable script-container alias metadata reaches an existing publication seam; callback chains that remain fully inside runtime script execution still stay unmodeled. | Quest/event maps keep publication ownership in one seam instead of per-feature toggles, and wrapper-authored alias state branches now preserve inherited scheduling on both parent and child alias publications rather than dropping the parent alias whenever nested leaves are numeric state values. This pass further closes parser-side parity drift by recursively retaining metadata-wrapper-only branches, including mixed direct + metadata wrapper alias containers, while unresolved work remains scoped to script-engine-only control flow that never surfaces into parser-visible/WZ publication metadata. | `MapLoader.cs`, `MapSimulator.cs`, `QuestRuntimeManager.cs`, `FieldObjectTagStateDefaultsLoader.cs`, `FieldObjectScriptTagAliasResolver.cs`, `FieldObjectScriptPublication.cs`, `FieldObjectDirectionEventTriggerLoader.cs`, `FieldObjectNpcScriptNameResolver.cs`, `ReactorScriptStatePublisher.cs`, `UI/Windows/InventoryItemMetadataResolver.cs`, `UnitTest_MapSimulator/FieldObjectScriptPublicationParserParityTests.cs`, WZ `map/Map/Map0/003000000.img/directionInfo/0/EventQ/0 = cannon_tuto_02`, WZ `quest/QuestInfo.img/22000/showLayerTag = 22000`, WZ `quest/Quest/Act.img/9539/1/npcAct = q9933e`, WZ `quest/Quest/Check.img/9933/1/endscript = q9933e`, WZ `npc/0002003.img/info/script/0/script = begin5`, WZ `item/Consume/0243.img/02430073/spec/script = consume_2430073` |
| Implemented | Town portal / mystic door lifecycle | `Mystic Door` skill casts now create a WZ-backed cross-map door pair, place the return-side door on the target town map's `TownPortalPoint` / `tp` spawn, transfer into town on `Up`, and restore the player to the original cast position when the town-side door is used before expiry | The simulator now covers the visible create, enter, restore, and positioning loop for the client's town-door utility instead of leaving the skill family nonfunctional | `TemporaryPortalField.cs`, `MapSimulator.cs`, portal/field transition layer (`Skill/231.img/2311002/{cDoor,mDoor,common/time}`, `PortalType.TownPortalPoint`) |
| Partial | Remote portal-pool parity | Packet-authored remote portal pools still live in their own runtime seam instead of folding into local skill ownership, but this seam keeps client-facing `330/331` town-portal and `332/333` Open Gate lifecycle ownership together: owner-keyed Mystic Door state/phase handling (including state-`0` fade snapshots and WZ `Frame` overlay, plus existing nonzero-state create updates staying in stable `mDoor` instead of replaying `cDoor` opening), owner+slot Open Gate transitions across `cDoor` opening, solo `sDoor`, linked `mDoor`, and remove `eDoor`, immediate Mystic Door unlink on state-`0` remove so fading pairs are non-interactive, same-map link gating while either Open Gate partner is opening, nonzero-state partner promotion, same-tick partner-loss relink, surviving-partner solo restart, and stable-partner visual gating so a gate stays `sDoor` until both sides are stable. Field reload still clears packet-owned runtime visuals plus owner/town inference buckets. Town-side source inference remains authority-ranked by owner+town+source map, keeps selected inferred sources ahead of weaker unrelated observations, still allows stronger same-town evidence replacement, and persists resolved coordinates back into the same buckets. This pass narrows preferred-source fallback ownership again: preferred source-map selection now still resolves when explicit owner-observation groups are missing by carrying forward existing inferred destination source identity and cached metadata identity, so preferred-source WZ validation still runs (and can still hold unresolved when validated-but-positionless) instead of incorrectly dropping to global-unique fallback only because source observations were absent in that tick. Pending owner-observation adoption now also keeps owner+town source identity recoverable when WZ source-to-town validation is unavailable in that tick by admitting pending source observations that match already-established metadata-source identity or existing inferred destination-source identity, instead of dropping those pending observations only because `returnMap`/`forcedReturn` resolution failed for that source map. WZ fallback remains narrowed to client intent for packet-only town streams: unresolved doors still use lowest-authority WZ fallback, and when source-map identity is already inferred the fallback path validates that preferred source against `returnMap` / `forcedReturn` first, resolves immediately when source position is available, and keeps destination unresolved (instead of jumping to an unrelated unique candidate) when the preferred source is validated but has no recoverable position; global unique-candidate fallback is still used when no preferred source exists or the preferred source cannot be validated. Source-position selection still prefers WZ `StartPoint` and falls back to first authored portal only when no start portal exists. Focused regression coverage lives in `UnitTest_MapSimulator/RemotePortalPoolParityTests.cs` for inference clearing, observed/pending source selection, preferred-source selection fallback when owner observations are missing (existing inferred source and metadata-source carry-forward), pending-owner observation adoption fallback when WZ town-map validation is unavailable (metadata-source and existing inferred-source identity carry-forward), existing-inferred-source holdover against weaker unrelated observations, stronger same-town evidence replacement, unique/ambiguous and preferred-source WZ fallback resolution (including preferred-source validation fallback and validated-without-position unresolved hold), start-point detection, Mystic Door non-link gating during state-`0` fade, Open Gate inactive-opening link gating, the stable-partner visual-mode gate, Open Gate partner create/loss phase promotion-preserve transitions, unique-candidate ambiguity unresolved hold, preferred-source validation failure fallback to unique candidate, and existing-nonzero-state Mystic Door create stable-phase hold. IDA confirmation from `CTownPortalPool::OnTownPortalCreated` (`0x762c00`) still defines the remaining hard gap: the create packet carries only state, owner id, and current portal coordinates, so exact source-field cast-point recovery still cannot be reconstructed when the simulator never observed matching owner-backed source evidence and no WZ-backed source-position fallback is available. Open Gate packet ownership remains aligned to `COpenGatePool::OnOpenGateCreated` (`0x68bfd0`) and `COpenGatePool::OnOpenGateRemoved` (`0x68b780`). | Visible parity no longer stops at self-authored portal utility: packet-owned remote Mystic Doors and Open Gates now keep client-shaped create/remove lifecycles, owner-scoped relinking, opening-time link suppression, stable-time visual/link promotion, and town-return inference inside one runtime seam. Remaining parity risk is concentrated on packet-only Mystic Door cases where neither owner-backed source observations nor WZ-backed source-position fallback can recover an exact source cast point. | field portal/runtime layer (`CTownPortalPool::OnPacket`, `CTownPortalPool::OnTownPortalCreated`, `COpenGatePool::OnPacket`, local reference seam: `CUserLocal::CheckPortal_Collision`, remote skill-use presentation seam), `HaCreator/MapSimulator/Fields/TemporaryPortalField.cs`, `UnitTest_MapSimulator/RemotePortalPoolParityTests.cs`, WZ `Skill/231.img/skill/2311002/{cDoor,mDoor,Frame}`, WZ `Skill/3510.img/skill/35101005/{cDoor,sDoor,mDoor,eDoor}`, WZ `Map/*/info/{returnMap,forcedReturn}`, WZ `Map/*/portal/*/{pt,pn,x,y}`, client pool owners `CTownPortalPool::OnPacket` (`0x7636b0`), `CTownPortalPool::OnTownPortalCreated` (`0x762c00`), `COpenGatePool::OnPacket` (`0x68c8b0`), `COpenGatePool::OnOpenGateCreated` (`0x68bfd0`), and `COpenGatePool::OnOpenGateRemoved` (`0x68b780`) |
| Partial | Remote affected-area / mist parity | The simulator keeps the packet-owned `328/329` affected-area runtime, start-delay activation, field-bind clearing, remote support projection, mixed friendly-plus-hostile Battle Mage aura handling, WZ-backed support ownership checks, WZ-backed `type=3` fog visuals and linked-duration resolution, and the existing mob-skill-mist reapplication path. This pass also snapshots owner runtime context at packet-owned area create time: live `ownerId -> name`, Battlefield owner-team assignment, and Monster Carnival owner-team-by-name are cached immediately when available so ownership checks can keep replaying after remote actor teardown without waiting for a later live lookup. The hostile player-area lane now preserves WZ-authored DOT metadata from `common/dot`, `dotInterval`, and `dotTime` instead of collapsing everything to generic `damage` / `time`, so affected-area wrappers such as `Skill/312.img/skill/3120010` and `Skill/3510.img/skill/35101009` keep their authored burn-style magnitude and timing when the packet-owned area path drives `SkillManager`'s inferred mob-status seam. The resolver also now treats linked hostile companions as part of disposition resolution instead of only the primary shell, and the linked-runtime collector now follows WZ `info/dummyOf` wrapper links in the same seam as `affectedSkill`, so sparse packet skills such as `Skill/3510.img/skill/35100008` can still reach hostile companion data on `35101009` / `35101010` before disposition and inferred mob-status replay run. This pass also consumes that WZ DOT metadata in the packet-owned runtime instead of only carrying it through the inferred-status seam: hostile player-area gameplay now ticks on the fastest reachable positive `dotInterval`, and player-skill affected-area expiration now resolves the maximum reachable authored lifetime across the visible skill plus its `affectedSkill`, `dummyOf`, and reverse `affectedSkill` parent chain. In Battlefield / Monster Carnival contexts that same staging seam now prefers WZ `PVPcommon` level data before regular `common`, so packet-owned areas no longer expire early just because the gameplay lane already switched to PvP-authored `dotTime` or `time` while `AffectedAreaPool` was still building the visual/runtime shell from the non-PvP node. The same sparse-wrapper seam still feeds the visual owner in `AffectedAreaPool`, so wrappers such as `Skill/3510.img/skill/35100008` and reverse-parent auras surfaced through `Skill/3210.img/skill/32101002` no longer have to stay gameplay-only while their visible mist or aura tile lives on a linked companion. The team-only ownership hole in the friendly lane is also closed: the existing WZ-backed `SupportsTeamMembers` wording now reaches the runtime because remote support areas no longer hardcode `ownerIsSameTeamMember = false`, and the affected-area owner check now resolves shared-team membership from the battlefield team owner plus the Monster Carnival team seam before the local support, avatar-effect, and invincibility seams decide whether the local player is a valid recipient. Monster Carnival ownership is narrower now too: once the carnival member-state seam has already resolved a named member onto a team, hostile or friendly affected-area ownership can keep that `ownerId -> team` result in the existing remote-area runtime instead of depending on a currently live remote actor shell every tick, and the owner-name seam now keeps an `ownerId -> name` fallback cache so later team lookups can still replay against already-known Carnival member-state names after the live remote actor shell drops. In that same ownership seam, Monster Carnival owner-team resolution now prefers fresh live owner-name team resolution before falling back to cached owner-team snapshots, and cache refresh no longer bails out just because a stale owner-team entry already exists, so live team transitions can update the runtime ownership cache before later remote-actor teardown. Battlefield ownership now uses the same cached-runtime seam: when Battlefield has already resolved an owner team (or the live remote actor still carries `BattlefieldTeamId`), the affected-area runtime caches `ownerId -> team` and reuses it when later hostile/friendly owner checks run without a live actor shell. This pass narrows owner/team replay drift again in the same seam by introducing per-area owner runtime snapshots keyed by packet affected-area object id: each active area now refreshes and stores owner name, Battlefield owner team, and Monster Carnival owner team from live runtime when available, then replays those area-local snapshots for hostile/friendly ownership checks after remote actor teardown or stale owner-id cache contention, with lifecycle pruning on remove/clear/expire. Ownership replay in that same seam now prefers live runtime owners first, then area-object snapshots, then broad owner-id caches for owner name, Battlefield owner team, and Monster Carnival owner team so stale owner-id cache entries no longer override still-active area-local snapshots when runtime owners are temporarily unavailable. The `type=3` fog lane is narrower now too: when no reachable WZ `time` metadata, linked path duration, or duration-bearing item description exists, the area-buff-item resolver now falls back to authored fog timing instead of leaving every such packet-owned fog entirely removal-packet-owned, using the same WZ-authored frame delays already loaded for the visible fog surface; when that fallback reaches a single-frame fog, the resolver now still consumes the authored frame delay as a finite lifetime. Linked damage-sharing fields now recover reverse WZ `affectedSkill` parents such as `Skill/3211.img/skill/32110000 -> 32101002`, so packet streams that surface the linked aura skill can still project the authored `partyDamageSharing` owner-share metadata before the existing remote-owner damage recipient seam runs. WZ-authored `affectedSkillEffect=amplifyDamage` now also feeds the inferred hostile mob-status resolver instead of depending on `mes` metadata alone, so affected-area wrappers such as `Skill/522.img/skill/5220011` can replay their authored marked-target bonus-damage status through the existing packet-owned hostile area seam. That same WZ-authored mark now stays a pure hostile player debuff in the enemy-local lane instead of being forced through the simulator's periodic-status damage path just because the player mob-status controller also uses `PainMark` for mob-skill `134` ticks. PvP/team fields now also consume WZ `PVPcommon` area metadata in the same runtime seam: when Battlefield or Monster Carnival ownership is active, hostile tick metadata, fallback damage, support projection, and staged player-area lifetime all prefer authored `PVPcommon` values such as `Skill/312.img/skill/3120010/PVPcommon/{dot,dotInterval,dotTime}`, `Skill/3510.img/skill/35101009/PVPcommon/{dot,dotInterval,dotTime}`, `Skill/3510.img/skill/35100008/PVPcommon/{w,x,y}`, and `Skill/522.img/skill/5220011/PVPcommon/damage` before falling back to regular `common`, and hostile remote player zones no longer stay mob-only in those fields because enemy-owned packet areas can now apply recurring local-player damage plus WZ-inferred player status effects through the existing incoming-damage and player mob-status seams when the local avatar is standing inside the zone. Friendly support projection now consumes the same WZ alias-mapping seam as the rest of the support resolver instead of only raw positive stat merges, so aura-style packet areas such as `Skill/3210.img/skill/32101003` can carry authored `common/x = movement speed` plus `common/y = attack speed step` into the synthetic external-buff lane and the avatar-effect helper without dropping the negative booster-style `y` metadata on the floor before the local-player support runtime sees it. This pass also keeps generic WZ `u`, `v`, and `w` support metadata projectable in that same merge seam, and Body Boost-style aura aliases now consume their WZ-authored split instead of only carrying raw generic fields: `Skill/3211.img/skill/32111005/common/v` feeds Dark Aura outgoing damage, `w` feeds Yellow Aura speed, `x` remains the attack-speed step, and `y` feeds Yellow Aura avoidability before the synthetic external-buff lane and outgoing-damage resolver inspect the projected support data. The enemy-local debuff projection now keeps WZ-authored status chance ownership inside each inferred status instead of rolling the whole skill once before status resolution and then rolling the status again, so branches such as `affectedSkillEffect=bodyAttack&&stun` consume their authored `subProp` as the stun chance without an extra outer `prop` gate while DOT and `amplifyDamage` marks continue to use their primary `prop` status metadata. Focused coverage for this seam now includes `UnitTest_MapSimulator/LocalAttackOwnedAffectedAreaParityTests.cs` and `UnitTest_MapSimulator/RemoteAffectedAreaOwnershipCacheParityTests.cs`. Remaining gap: hostile remote player-zone ownership is still simulator-side approximation rather than a full client-authored reconstruction; create-time owner snapshots, per-area owner runtime snapshots, and existing Battlefield/Monster Carnival owner caches now survive common remote-actor teardown paths, and the owner-team seam now refreshes live Monster Carnival owner-name -> team resolution then replays area-local snapshots before owner-id caches, but unrecovered client-only owner/team transition writers still remain outside the current seams, and hostile debuff targeting is narrower but still not byte-accurate for the same reason. The enemy-local lane now gates self/party-targeted wrappers behind explicit hostile status metadata before local-player status projection, in addition to the existing hostile-runtime and fallback-hostile filters, but unresolved owner-to-team reconstruction and broader client-only debuff-owner state remain outside the current recovered seams. The enemy-local lane also now keeps its own per-area hostile tick gate, so local-player recurring damage/status replay no longer has to share the exact same `NextGameplayTickTime` lane as mob-side inferred-status replay for that area. This pass narrows hostile debuff targeting again in the same seam: enemy-local status projection now requires explicit hostile status ownership when a wrapper is self- or party-targeted, so support-shaped self/party shells no longer leak inferred hostile statuses into the local-player lane from broad token/element matches alone, while explicit hostile wrappers such as WZ `info/affectedSkillEffect=bodyAttack&&stun`, `dot`, and `amplifyDamage` still project through the packet-owned hostile area runtime. Existing limits remain: the lane is still constrained to recurring damage plus WZ-inferred status/debuff projection in Battlefield / Monster Carnival contexts, still lacks byte-accurate client debuff-owner reconstruction, support branches that need bespoke simulator mechanics beyond the current projected buff, raw generic `u` / `v` / `w` metadata carry, Body Boost `v` / `w` / `x` / `y` alias mapping, alias-mapped movement/booster/avoidability support, resistance, cleanse, damage-reduction, damage-to-MP, outgoing-damage, reward-rate, crit-damage, mastery, boss-damage, and ignore-defense seams still stop short of full gameplay, wrappers that expose no hostile or visual metadata across the visible node/`affectedSkill`/`dummyOf`/reverse-parent chain can still under-classify or stay packet-removal-owned for visuals, and unrecovered client-owned owner/team transition writers still keep hostile remote player-zone ownership and debuff targeting approximate instead of byte-accurate. | Packet-owned remote affected areas now preserve more of the client-authored hostile mist lane instead of degrading DOT-backed area wrappers into rough generic damage ticks. WZ-backed hostile areas with authored DOT payloads now carry their own per-skill magnitude, tick cadence, lifetime, linked visual surface, PvP/team-field `PVPcommon` overrides, and enemy-local status projection into the existing inferred mob-status runtime, player mob-status runtime, and affected-area staging seam, while the already-shipped support projection, mist resistance, linked support ownership, linked hostile-branch replay, mixed Battle Mage aura behavior, the battlefield / Monster Carnival team-target ownership checks, the cached Monster Carnival owner-id team carry once the carnival member-state seam has already named that owner, the `dummyOf` wrapper traversal, reverse `affectedSkill` parent recovery for linked damage-sharing auras, the enemy-local-player recurring-damage and status-projection lane for team fields, the WZ alias-mapped aura support seam for visible Battle Mage support areas such as `32101003`, the Body Boost `v` / `w` / `x` / `y` projected-support alias mapping for `32111005`, the hostile `amplifyDamage` mark fix for local player projection, the per-status `prop` / `subProp` enemy-local debuff chance fix, and the fog-cycle lifetime fallback all stay in the same seams. This keeps remote areas such as Fire/Poison `3120010`, Mechanic `35101009`, wrapper skills such as `35100008`, reverse-parent auras such as `32101002`, WZ-authored team-targeted support zones, PvP-authored aura numerics such as `32110000/PVPcommon`, Yellow Aura `32101003`, marked-target wrappers such as `5220011`, and sparse packet-owned `type=3` fogs closer to authored behavior without introducing a separate area-combat system. | field affected-area/runtime layer (`CAffectedAreaPool::OnPacket`, `CAffectedAreaPool::OnAffectedAreaCreated`, `CAffectedAreaPool::OnAffectedAreaRemoved`), `AffectedAreaPool.cs`, `AreaBuffItemMetadataResolver.cs`, `MapSimulator.RemoteAffectedAreaPackets.cs`, `RemoteAffectedAreaSupportResolver.cs`, `PlayerMobStatusController.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `MonsterCarnivalField.cs`, `UnitTest_MapSimulator/LocalAttackOwnedAffectedAreaParityTests.cs`, `UnitTest_MapSimulator/RemoteAffectedAreaOwnershipCacheParityTests.cs`, WZ `Skill/312.img/skill/3120010/common/{dot,dotInterval,dotTime}`, WZ `Skill/312.img/skill/3120010/PVPcommon/{dot,dotInterval,dotTime}`, WZ `Skill/3510.img/skill/35101009/common/{dot,dotInterval,dotTime}`, WZ `Skill/3510.img/skill/35101009/PVPcommon/{dot,dotInterval,dotTime}`, WZ `Skill/3510.img/skill/35101010/common/damage`, WZ `Skill/3510.img/skill/35100008/info/dummyOf`, WZ `Skill/3510.img/skill/35100008/PVPcommon/{w,x,y}`, WZ `Skill/3211.img/skill/32110000/info/{affectedSkill,affectedSkillEffect}`, WZ `Skill/3210.img/skill/32101002`, WZ `Skill/3210.img/skill/32101003/common/{x,y}`, WZ `String/Skill.img/32101003/{desc,h,pdesc}`, WZ `Skill/522.img/skill/5220011/info/affectedSkillEffect`, WZ `Skill/522.img/skill/5220011/PVPcommon/damage`, WZ `Skill/3211.img/skill/32111010/common/{prop,subProp,time}`, WZ `Skill/3211.img/skill/32111005/common/{v,w,x,y,z}`, WZ `String/Skill.img/32111005/{desc,h}`, WZ `Item/Cash/0501/05010079/effect/darkFog/*/delay`, battlefield team ownership in `BattlefieldField`, Monster Carnival team ownership in `MonsterCarnivalField`, and client packet entry points `CAffectedAreaPool::OnPacket`, `CAffectedAreaPool::OnAffectedAreaCreated`, `CAffectedAreaPool::OnAffectedAreaRemoved` |
| Partial | Field-specific restrictions and effects | The row still covers a wide slice of field-rule parity already landed in the simulator: entry notices for WZ field metadata and several `fieldLimit` bits, ambient rain/snow startup, `timeLimit`, `decHP`, `protectItem`, positive `recovery`, positive `moveLimit`, `allowedItem`, teleport-item bans, minimap auto-expand, movement/migration locks, map-transfer admission checks, mini-game/parcel/quest-alert/taming-mob/android/party-boss/drop/fall-damage restrictions, WZ-backed item-family bans, and the shared local item/meso drop evaluator/prompt seam. Wedding invitations remain locked to explicit WZ-backed families only (`Item/Etc/0421.img`, `Item/Cash/0509.img/05090100`, `Item/Cash/0525.img/05251000..05251003`, `Item/Cash/0525.img/05251100`, and String `Etc.img/4031377`, `4031395`, `4031406`, `4031407`) with no phrase fallback; successful local meso drops still mirror `CWvsContext::SendDropMoneyRequest` (`0x9f6650`) through opcode `106` plus `Encode4(update_time)` + `Encode4(nAmount)` and shared utility-request stamping; and the numeric drop prompt still uses WZ-backed `UtilDlgEx` shell assets (`notice`, `line`, `bar`, `BtPrev`, `BtNext`, `BtOK`, `BtClose`) with centered entered amount/count text in the authored `bar` surface. The discard-result seam remains in the existing local drop/utility bridge ownership: local item drop requests stage through inventory pending-request ownership (`PendingRequestId`) instead of immediate consume/spawn, mirror opcode `77` (`CWvsContext::SendChangeSlotPositionRequest`) through the same stamped live/deferred dispatch bridge, and finalize only when packet-owned `OnInventoryOperation(28)` acknowledgements match the source slot via client-shaped operation decode (`mode 3` slot clear, `mode 1` quantity update, or `mode 2` swap-to-zero), with zero-operation rows clearing pending local discard ownership as a reject/rollback path while timed fallback commit preserves simulator continuity when no packet acknowledgement lands. Admission ownership remains packet-aware in the existing field-entry/map-transfer evaluator seam: `partyOnly` and `expeditionOnly` map admission prefers packet-owned social/session context when those owners are active (`OnPartyResult`-owned party id and expedition intermediary packet-owner roster state), while preserving simulator-owned roster fallback when packet ownership is not armed. This pass closes another field-owned request gate in the existing social-room restriction seam by wiring WZ `Map/*/info/{personalShop,entrustedShop}` map options (verified in the current export with both `0` and `1` authored values, e.g. `Map/554000000.img/info/personalShop = 1`, `Map/211042300.img/info/personalShop = 0`, `Map/910000007.img/info/entrustedShop = 1`, `Map/211042300.img/info/entrustedShop = 0`) and keeps the shared `fieldLimit` mini-game owner data-backed (`Unable_To_Open_Mini_Game` bit 7 set on `Map/910000000.img/info/fieldLimit = 4268268`, clear on `Map/211042300.img/info/fieldLimit = 8260`) into room admission: `PersonalShop` and `EntrustedShop` requests now reject from the same field restriction owner when those options are explicitly disabled, while `MiniRoom` and `TradingRoom` stay on the existing `fieldLimit` mini-game gate. Focused regression coverage now includes `UnitTest_MapSimulator/FieldInteractionRestrictionParityTests.cs` for map-option room gating precedence (`fieldLimit` first, then per-room map option blocks), explicit `TradingRoom` mini-game-bit admission, and room-allowed baselines, plus `UnitTest_MapSimulator/FieldEntryRestrictionAdmissionParityTests.cs` for packet-owned party/expedition required-map gating and fallback behavior. Remaining gap: generic field scripts still stop at notice/tag publication, broader packet/session-owned field request gates outside the current map-transfer and social-room admission seams remain missing, the fall-damage and death-EXP rules are still simulator-side approximations, the numeric prompt owner still does not reproduce client `CUtilDlgEx` modal/input choreography byte-for-byte, and broader android/session-owned restrictions outside the current equipment/runtime seams remain open. | The field-rule branch is already close enough that the remaining risk is mostly correctness drift in the shared restriction classifiers and simulator-owned authority seams. Keeping wedding-invitation detection purely WZ-backed removes heuristic false positives from a data-driven `fieldLimit` rule, mirroring the client-shaped meso-drop request keeps local drop flow aligned with packet-owned utility transport without inventing a separate drop-dispatch owner, and moving the prompt shell onto authored `UtilDlgEx` surfaces narrows visual drift in the same local prompt seam. This pass also narrows discard parity drift by routing local drop completion through pending-slot ownership plus packet-owned `OnInventoryOperation(28)` release/reject handling instead of immediate simulator consumption, and narrows room-admission drift by honoring authored `personalShop`/`entrustedShop` map options while keeping `TradingRoom` on the same fieldLimit-backed mini-game gate in the existing field restriction owner. Remaining risk is now concentrated in deeper packet/session and formula/layout recovery, plus broader session-owned restriction families. | `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.PacketOwnedLocalOverlayParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.FieldRestrictionParity.cs`, `MapSimulator.EquipmentWindowParity.cs`, `MapSimulator.Content.cs`, `MapSimulator.CashServiceStageParity.cs`, `MapSimulator.FieldDropPacketParity.cs`, `Loaders/UIWindowLoader.Inventory.cs`, `Companions/PetController.cs`, `Interaction/SocialListRuntime.cs`, `Interaction/SocialListRuntime.PacketOwned.cs`, `UI/Windows/InventoryUI.cs`, `UI/Windows/UIWindowManager.cs`, `UI/Windows/EquipUIBigBang.cs`, `UI/PickupNoticeTextFormatter.cs`, `FieldRuleRuntime.cs`, `FieldRuleEffectApplier.cs`, `FieldEntryRestrictionEvaluator.cs`, `FieldEnvironmentEffectEvaluator.cs`, `FieldSkillRestrictionEvaluator.cs`, `FieldInteractionRestrictionEvaluator.cs`, `FieldDropRequestEvaluator.cs`, `FieldFallingDamageEvaluator.cs`, `FieldDeathPenaltyEvaluator.cs`, `TeleportItemUsageEvaluator.cs`, `UI/Windows/InventoryItemMetadataResolver.cs`, `UI/Windows/MapTransferUI.cs`, `UnitTest_MapSimulator/FieldInteractionRestrictionParityTests.cs`, `UnitTest_MapSimulator/FieldEntryRestrictionAdmissionParityTests.cs`, `UnitTest_MapSimulator/FieldDeathPenaltyEvaluatorParityTests.cs`, WZ `Item/Etc/0421.img`, WZ `Item/Cash/0509.img`, WZ `Item/Cash/0525.img`, String WZ `Etc.img/4031377`, `4031395`, `4031406`, `4031407`, `UI/UIWindow.img/UtilDlgEx`, `UI/UIWindow2.img/UtilDlgEx`, WZ `Item/Install/0301/03010029/info/tamingMob`, WZ `Map/*/info/personalShop`, WZ `Map/*/info/entrustedShop`, field systems (`CField::IsFearEffectOn`, `CField::OffFearEffect`), client seams `CWvsContext::SendDropMoneyRequest` (`0x9f6650`), `CWvsContext::SendChangeSlotPositionRequest` (`0x9d9c10`), `CUserLocal::OnInventoryOperation` (`0x28`), `CUserLocal::OnPartyResult`, and `ExpeditionIntermediary::OnPacket` (`0x522a30`) |

### 2. Drop-pool and pickup lifecycle parity discovered by IDA function scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned drop-pool parity | The simulator keeps an explicit packet-owned drop runtime (`RemoteDropPacketRuntime` + `RemoteDropPacketCodec`) over the recovered `CDropPool` packet surface (`322/324`) and applies packet enter/leave directly through `DropPool`, with packet-only field-clear behavior, packet expiry clock anchoring (`/droppacket clock` + `SetField(141)` FILETIME seed), fake-to-real same-id promotion, client-shaped create-delay/alpha timing, pickup-rectangle plus `30 ms` held-pickup cadence, pool-order first-eligible admission, `pickUpBlock` gating, packet meso layered animation timing/origin handling, packet elevate-layer draw pass, and packet-owned absorb/removal/explode sequencing on existing seams. Party pickup arbitration remains packet-party-id first, then tracked roster/helper fallback plus observed-link fallback with encoded-pet owner normalization, and this pass tightens known-endpoint admission in that same seam again: observed links still refuse unknown-only endpoints, while the observed-link branch now also treats local party-id endpoints as known admission anchors through the shared `IsKnownDropPartyActor` path so packet-owner-tracked links no longer depend on helper/anchor fallback for that case. Remote pet pickup-motion ownership remains in the same seam and is tightened again: packet-observed pet pickup world positions are tracked by pet actor id (including `UserDropPickup` packet target positions), field bind clears that cache with other packet-owned drop state, packet leave/source position resolution still consults tracked pet actor positions before owner fallback, owner+slot decode still gates to tracked active remote pet slots (`RemotePetItemIds`), owner-slot observed-position lookup still falls back to the closest observed slot for the same owner, and actor-id-based owner/slot decode now reuses that same observed lookup before tracked-slot gating so packet-observed owner-scoped pet motion can survive temporary remote-slot desync before dropping to owner-position fallback. Focused source-side coverage for this pass is now in `RemoteDropPacketPetPickupPositionParityTests` plus `RemoteDropPacketPartyOwnershipParityTests`, including actor-id owner/slot observed fallback resolution, observed owner+slot override, tracked-slot offset resolution, closest-slot observed fallback, explicit unknown-only observed-link arbitration refusal, local-party-id anchor admission on observed links, and encoded-pet-owner normalization admission. Remaining gap: broader arbitration is still approximate when no packet-owned party id is known and neither side can be tied into any known party actor set even after tracked/legacy/helper/observed-link normalization; exact COM-owned `GR2D::LoadLayer`/`RemoveCanvas`/`InsertCanvas` choreography for packet drop visuals remains approximated; and pet-owned pickup motion still falls back when no packet-observed pet world state exists for that owner and owner+slot also cannot resolve to tracked remote pet state. | Without an explicit `CDropPool` row, drop parity is easy to overstate once pickup text, local loot, or pet auto-loot exists. The client keeps a dedicated pool owner for enter/leave/update/pickup behavior, so tracking it separately keeps future work anchored to the real field-entity runtime instead of scattering it across UI feedback, pet logic, or generic map-object handling. This pass narrows ownership arbitration overreach by keeping unknown-only observed links denied while still admitting local-party-id anchor links through the same known-party seam, and it narrows pet-motion fallback by reusing owner-scoped observed pet positions from encoded pet actor ids before tracked-slot fallback drops to owner position. | field drop/runtime layer (`DropPool.cs`, `PacketOwnedMesoAnimationPresentation.cs`, `RemoteDropPacketCodec.cs`, `RemoteDropPacketRuntime.cs`, `MapSimulator.RemoteDropPackets.cs`, `MapSimulator.RemoteDropPacketEffects.cs`, `MapSimulator.RemoteUsers.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.PacketOwnedStageTransitionParity.cs`, `PacketStageTransitionRuntime.cs`, `UnitTest_MapSimulator/RemoteDropPacketPetPickupPositionParityTests.cs`, `UnitTest_MapSimulator/RemoteDropPacketPartyOwnershipParityTests.cs`, `CDropPool::OnPacket`, `CDropPool::OnDropEnterField`, `CDropPool::OnDropLeaveField`, `CDropPool::Update`, `CDropPool::TryPickUpDrop`, `CDropPool::TryPickUpDropByPet`, `CDropPool::TryPickUpDropByMob`, `CDropPool::MakeMoneyAnimation`, `CDropPool::RecordRecentPickupItem`, `CAnimationDisplayer::RegisterAbsorbItemAnimation`, `CStage::OnSetField`, `Item/Special/0900.img/09000000..09000003/iconRaw/*`) |

### 3. Packet-authored local quest, chair, and teleport response parity discovered by direct `CUserLocal::OnPacket` scan

The direct `CUserLocal::OnPacket` re-check surfaced another ownership seam that is still absent from this plan: the client does not only drive these flows from local input and generic runtime state. It also has dedicated packet-response handlers for chair sit ack or correction, same-field teleport completion, and quest-result or quest-timer application.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned quest-result parity | The simulator keeps the dedicated local `OnQuestResult` packet seam on opcode `242`, including client-handled subtype coverage (`6` through `18`), subtype `10` follow-up decode and ordering, WZ-backed quest presentation (`Quest/QuestInfo.img`, `Quest/Say.img`, `Quest/Act.img`), client-shaped notice routing (`CUtilDlg::Notice` family split), and packet-owned fade-window plus availability-refresh tails. Follow-up `CWvsContext::StartQuest(..., bAutoStart = 0)` continuation remains request-owned until packet response ownership returns, with pending start ownership recorded only when live or deferred outbound transport accepts the packet, shared exclusive-request tick stamping only on accepted dispatch or queue, delivery-item slot resolution from packet-owned delivery owner state, and deferred quest-state mutation only when matching subtype `10` or `12` response ownership arrives in the same seam. This pass closes more of the conditional payload parity in that same continuation owner: subtype `10` follow-up opcode `119` subtype `1` now conditionally omits or includes user-position coordinates using the recovered client gate (`CQuestMan::IsAutoAlertQuest`), implemented through `PacketOwnedQuestStartRequest.ResolveIncludeUserPosition` plus quest-runtime auto-alert inference (`HasNormalAutoStart`, `fieldEnter`, `equipOn`, and packet-owned auto-start registration). Coverage remains focused in `UnitTest_MapSimulator/PacketOwnedQuestStartRequestTests.cs` (payload decode plus auto-alert coordinate gate helper assertions). WZ evidence remains the authored boundary in `Quest/Act.img` (for example `1001/1` mixed `exp` + `item` and `1003/1/nextQuest`) while client evidence remains `CQuest::StartQuest` at `0x6B40A0` and `CQuestMan::IsAutoAlertQuest` at `0x6B6890`, where opcode `119` start requests encode subtype `1`, quest id, NPC id, delivery-item position, and user position only when auto-alert is false. Remaining gap: subtype `10` continuation still runs on the simulator pumped modal host rather than native blocking `CUtilDlgEx` and `CDialog::DoModal`; the follow-up request owner still does not reproduce byte-for-byte `CWvsContext::SetExclRequestSent` write and clear lifecycle; and the auto-alert owner currently models auto-start-backed alert families but not the full client `IsAutoCompletionAlertQuest` table byte-for-byte. | Quest parity here depends on packet owner boundaries, not only quest window helpers: `CUserLocal::OnQuestResult` owns timer and result decode and continuation ordering, then drives both visible quest-result UI and the follow-up `StartQuest` request owner path before availability refresh. Keeping opcode `242` handling, subtype `10` continuation, and opcode `119` follow-up dispatch in one seam prevents regressions back into simulator-only quest-window flow. | packet-owned local quest-result layer (`CUserLocal::OnPacket`, `CUserLocal::OnQuestResult`, `CWvsContext::AddQuestTimer`, `CWvsContext::RemoveQuestTimer`, `CWvsContext::CheckNewQuestAvailable`, `CWvsContext::StartQuest`, `CWvsContext::DeleteFadeWnd`, `CQuest::StartQuest`, `CQuestMan::IsAutoAlertQuest`, `CUtilDlgEx`, `CDialog::DoModal`, `Quest/QuestInfo.img`, `Quest/Say.img`, `Quest/Act.img`, `UI/UIWindow.img/UtilDlgEx`, `UI/UIWindow2.img/UtilDlgEx`, `MapSimulator.PacketOwnedQuestResultParity.cs`, `MapSimulator.UpdateLoop.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultClientSemantics.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultUtilDialogLayout.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultUtilDialogModalHostRuntime.cs`, `HaCreator/MapSimulator/Interaction/PacketQuestResultFadeWindowRuntime.cs`, `HaCreator/MapSimulator/Interaction/PacketOwnedQuestStartRequest.cs`, `HaCreator/MapSimulator/Interaction/QuestRuntimeManager.cs`, `UnitTest_MapSimulator/PacketOwnedQuestResultClientSemanticsTests.cs`, `UnitTest_MapSimulator/PacketOwnedQuestStartRequestTests.cs`) |
| Implemented | Packet-owned chair sit-result parity | The simulator now has a dedicated local `OnSitResult` packet seam on opcode `231` instead of treating chairs as local-only intent. The packet-owned handler stamps the shared local utility request tick, mirrors the client-owned `CWvsContext` chair bookkeeping in the packet-owned local-utility context by clearing the chair exclusive-request flag at `+0x20B8` and restamping the exclusive-request tick at `+0x20BC`, decodes the client-shaped success flag plus seat index payload, resolves the current map's WZ `seat/<index>` vector, validates the local player against the same client chair rectangle (`x +/- 10`, `y +/- 30`), and routes successful placement through a packet-owned local sit placement helper that also raises a dedicated secure sit confirmation flag on the local player. Successful packet-owned placement now clears any attached local passenger linkage through the existing follow-runtime and remote-user seams before the sit is finalized, matching the client path that zeroes `m_dwPassenserID` and the passenger's driver link. Invalid returned seats or rectangle mismatches now force the same stand-up correction path and send that correction through the shared local-utility context owner instead of a raw timestamp: the simulator still records a client-shaped `CWvsContext::SendGetUpFromChairRequest(0)` outpacket with opcode `45` and payload `FFFF`, re-arms the exclusive-request flag after emission, suppresses the send when the local HP gate or exclusive gate would block it, and injects that raw opcode `45` packet through a configured `LocalUtilityOfficialSessionBridgeManager` owner when a live Maple socket is attached. That bridge seam is broader now too: `/localutility session start ...` and `/localutility session startauto ...` no longer behave as one-shot manual helpers only. They persist their direct-proxy or discovery configuration onto the packet-owned local-utility owner, the simulator now keeps that bridge armed from the main update loop alongside the inbox, auto-refreshes discovery while no Maple client is attached, preserves an already attached live session instead of tearing it down during discovery refresh, reports outbound chair corrections in bridge status with pending and queued counts plus the last injected and last deferred raw packets, and now keeps a built-in deferred outbound queue for chair correction packets whenever the official-session bridge is enabled but no Maple session has finished attaching yet. Those deferred packets survive bridge re-arming and discovery refresh, then flush through the live Maple socket as soon as the proxied session completes the init handshake, so packet-owned chair corrections no longer depend on an external loopback consumer to reach the official-session transport path. The no-live-session fallback still remains the dedicated `LocalUtilityPacketTransportManager` loopback outbox on `/localutility outbox [status|start [port]|stop]`, which emits raw outbound corrections as `packetoutraw` lines containing the decrypted opcode-framed Maple packet, records the last emitted raw chair correction on that outbox owner, and lets packet-owned chair corrections leave the simulator whenever no live Maple bridge is attached but an external loopback consumer is listening. That outbox owner still keeps its own deferred packet-owned queue for chair correction packets when no outbox client is currently attached, flushes queued packets as soon as a loopback client connects (or before the next immediate send when a client is already attached), and reports both sent and pending correction counts in local-utility status so these packet-owned corrections no longer collapse into simulator-only state immediately when transport is temporarily unavailable. Packet rejection still clears the sit state without emitting that correction request, and the rejection teardown now also clears any stale external-owner couple-chair overlay when an active portable chair is unwound through the same local stand-correction path, matching the client-owned `Update(0)` cleanup branch more closely than the earlier partial teardown.g.cs`, `MultiBoard.g.cs`, and `HaCreator_Content.g.cs` missing under `HaCreator/obj/Debug/net10.0-windows`). | Chairs are not only a local toggle or render-state change. The client keeps a packet-owned success/failure and seat-validation branch that decides whether the user really sits, whether a passenger link is broken, and when to force a stand-up correction. Tracking this row against the dedicated opcode `231` seam keeps the remaining work anchored to the real local response owner instead of only local chair commands or remote chair packets. | packet-owned local chair-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnSitResult`, `CWvsContext::SendGetUpFromChairRequest`, WZ `Map/Map*/<map>.img/seat`, packet-owned local sit placement, correction path toward `PacketOwnedLocalUtilityContextState`, `LocalUtilityOfficialSessionBridgeManager`, `LocalUtilityPacketTransportManager`, `UnitTest_MapSimulator/LocalUtilityOfficialSessionBridgeManagerTests.cs`) |
| Implemented | Packet-owned teleport-result parity | Standard same-map portal travel still finishes through the packet-owned local `OnTeleport` seam: visible and hidden portals targeting the current field use the WZ-authored portal `delay` when present, otherwise the client-confirmed 120 ms handoff, stage a pending same-map teleport against the destination portal index, and let the dedicated apply helper own the final landing and `TryRegisterTeleport(..., bForced = 1)` side effects instead of writing position directly from the interact path. Temporary same-map completion stays in that seam too, carrying any resolved real source `pn`, forwarding same-map target `pn` metadata, and using relaxed current-field `CPortalList` coordinate matching so near-equal authored landings can still collapse to a real portal index. Cross-map portal travel remains packet-owned as well: pending teleport targets survive `UnloadMapContent`, reload the new field, try the rebuilt `PortalPool` first by the primary returned `pn`, then by preserved fallback candidate names, and only then fall back to exact-coordinate apply. This pass tightens that post-load collapse order in the existing seam so rebuilt-field completion no longer promotes an arbitrary returned portal index from whole-pool coordinate drift after the named and candidate-based collapse paths fail; ambiguous rebuilt candidate sets and destinations that no longer correspond to any rebuilt `CPortalList` entry now stay coordinate-owned through the packet apply seam instead of stealing a named handoff from whichever live portal happens to sit nearest the stored landing point, and the rebuilt-field seam no longer synthesizes a fresh named handoff from source-side reciprocal or map-only metadata once those preserved target-side collapse paths have already missed. The preserved primary returned portal name is still authoritative again once the rebuilt `PortalPool` can resolve it, even when the stored landing coordinates would have drifted toward another live portal and previously could have stolen the handoff. Preserved candidate destination names still no longer require the raw candidate strings to be unique up front, because after field load the simulator replays those names against the rebuilt `PortalPool`, promotes them into a true returned portal handoff whenever stale candidates drop out to one live portal index, and still replays the preserved landing coordinates against the still-live candidate subset so a rebuilt field can recover one real returned portal index even when multiple preserved destination names survive reload but only one of those live candidates still matches the stored landing point. Standard visible or hidden cross-map portals still preserve WZ-backed reciprocal return-link fallback (`target.tm == currentMap` and `target.tn == source.pn`) plus unique map-only return fallback from the synthetic opcode `113` request owner, and temporary cross-map travel still mirrors that metadata preservation while recovering real current-field source `pn` values through relaxed coordinate matching before target-side metadata recovery. Source-side collapse is tighter in that same packet-owned seam now too: synthetic opcode `113` request building and temporary-portal recovery still start from the full best-distance current-field source `pn` set derived from portal `x`/`y`/`pn`/`tn`/`tm` metadata, but the helper no longer treats each target-side clue as a separate all-or-nothing check. It now keeps that coordinate-authored source candidate set alive, narrows it cumulatively with the explicit target `tn` when present, then with the preserved target candidate names when those survive, and only then replays each surviving current-field source candidate back through the authored target-field portal metadata surface (`tn`, reciprocal `pn`, unique map-only return, and target-side coordinate candidates) before falling back to the older target-only inference or exact-coordinate completion. That closes another packet-owned source-side miss where current-field coordinates plus the preserved target candidate set still left multiple source `pn` values alive until the simulator asked which candidate's own target-field metadata actually matched the resolved landing side; temporary and synthetic recovery now keep the packet-owned named handoff in that case instead of dropping straight to coordinate-owned completion. The same request-building seam is tighter again now when the source side stays genuinely ambiguous even after that replay pass: temporary and synthetic owners preserve the surviving current-field source candidate set, replay those candidates back through authored target `tn` metadata, and carry any narrowed target `pn` or candidate subset forward instead of immediately dropping to exact-coordinate-only landing metadata. Exact-coordinate completion still avoids inventing a fallback target name when the packet-owned seam only knows ambiguous candidates, and outbound opcode `113` routing now keeps the request on the packet-owned official-session seam after the live direct-injection path misses by preferring deferred `LocalUtilityOfficialSessionBridgeManager` ownership before any generic `LocalUtilityPacketTransportManager` `packetoutraw` fallback while still preserving the raw payload and dispatch summary. WZ evidence for the active seam remains the portal metadata fields in `Map/Map0/001000000.img/portal/*/{pn,tn,tm,x,y,delay}` and the general teleport effect in `Effect/BasicEff.img/Teleport`, whose authored animation remains 4 frames at 90 ms each; `Map/Map0/001000000/portal/0` still exposes the same packet-relevant portal metadata surface (`pn = sp`, `tm = 999999999`, `x = 240`, `y = 190`) on the mounted export. Direct client evidence remains `CUserLocal::OnPacket` opcode `234` dispatch into `CUserLocal::OnTeleport`, where the client decodes the success byte, looks up the returned `CPortalList` entry by portal index, writes the landing point from that portal, and then re-enters `TryRegisterTeleport(..., bForced = 1)`. Focused source coverage now on disk in `UnitTest_MapSimulator/PacketOwnedTeleportParityTests.cs` includes the cumulative source-collapse regression where current-field source coordinates, an explicit target `tn`, and preserved target candidate names only become unique when the packet-owned seam intersects all of them in order, the replayed-target-field regressions where multiple coordinate-authored current-field source candidates collapse only after matching each candidate's authored target metadata or reciprocal return link against the resolved landing side, the surviving-source-candidate target recovery regressions where temporary or synthetic request building now replays those still-ambiguous source candidates back across authored target metadata before falling through to coordinate-only completion, the outbound-owner regressions that keep deferred opcode `113` delivery on the official-session seam before the generic outbox, alongside the earlier rebuilt-field primary-name precedence path, rebuilt-field candidate collapse when only one preserved destination still resolves in the live pool, rejection when multiple preserved candidates still resolve after reload, the rebuilt-field collapse path where multiple preserved destination names still resolve live but only one of those candidates still matches the preserved landing coordinates, the new rebuilt-field rejection where only source-side reciprocal metadata still resolves after the preserved target-side collapse paths fail, and the coordinate-only fallback regressions where rebuilt cross-map completion must not promote a named portal from whole-pool coordinates alone or from a non-candidate live portal. | Teleport parity is not only about entering a portal or moving to the right coordinates. The client also has a response-time authority point that applies the actual destination by portal index and records the named teleport handoff after the packet returns. Tracking that seam keeps the remaining parity gap anchored to the real response owner instead of hiding it behind already-implemented local portal interaction flow. | packet-owned local teleport-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnTeleport`, `CPortalList`, `CUserLocal::TryRegisterTeleport`, WZ `Map/Map0/001000000.img/portal/*/{pn,tn,tm,x,y,delay}`, WZ `Effect/BasicEff.img/Teleport`, `MapSimulator.cs`, `MapSimulator.PacketOwnedTeleportParity.cs`, `UnitTest_MapSimulator/PacketOwnedTeleportParityTests.cs`, `LocalUtilityOfficialSessionBridgeManager.cs`, `LocalUtilityPacketTransportManager.cs`) |

### 4. Portal and reactor collision ownership discovered by targeted `CUserLocal` decompile

The latest `CUserLocal` pass exposed one more field-runtime owner split that the backlog was still flattening into generic portal, teleport, and reactor rows.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Portal collision ownership parity | The simulator now owns portal touch through one explicit `CUserLocal::CheckPortal_Collision`-style seam instead of separate visible versus hidden lookup loops. `TryHandlePortalInteractCore` resolves the colliding portal from `PortalPool.FindPortalCollision` using the live local-user physics position, then branches by portal type in that owner: type `9` (`CollisionScript`) now uses a client-shaped collision-script request path instead of falling through the generic transfer helper, building opcode `112` as field key byte plus `EncodeStr(portalName)` plus the live local-user physics x/y shorts, dispatching it through the existing official-session bridge or packet outbox, recording an explicit `CWvsContext::SetExclRequestSent`-style in-flight flag/timestamp, applying the same recovered per-portal delay plus `onlyOnce` suppression keyed to the last collision-source portal, and now re-arming that exclusive sent state from the same collision seam when the local user leaves the original collision-script portal or when the portal-authored delay elapses while no later handoff has consumed the request. This pass narrows that type `9` owner again by feeding the request field-key byte from the existing packet-owned stage-transition seam when `CStage::OnSetField` has already disclosed an authoritative key for the live map, preserving that disclosed key across the stage-transition runtime clear/rebind that happens during map-content bind, and only falling back to the simulator-owned synthetic `0` when no authoritative map-load handoff has populated one yet; type `12` (`CollisionVerticalJump`) now routes through a dedicated vertical-jump branch that follows the recovered `CUserLocal::VerticalJump` helper more closely by queueing `SetImpactNext` with vertical velocity `-2300`, deriving horizontal velocity from live left/right input plus facing (`input * 100` plus `+/-200`), replaying the client-owned visual owner through `Effect/BasicEff.img/VerticalJump` as a one-shot animation attached to the live local-user physics position, feeding the recovered client move-path attribute `24` through the shared `CVecCtrl` move-path owner itself instead of leaving that attribute as simulator-only debug state, and now refusing the branch unless the same inferred live field-interface/vector-control/input ownership is available before impact is applied. The same shared vec-ctrl seam now also carries the default branch's packet-owned forced-teleport registration attribute `4`, and the new `CMovePathClientPacketCodec` serializes the recorded portal-owned movement snapshots with the client-shaped attribute body layouts for both the recovered `24` vertical-jump attribute and the default forced-teleport `4` attribute instead of leaving those attributes only as in-memory state; type `13` (`CollisionCustomImpact`) now mirrors more of the client-side admission, request, and delayed-impact shape instead of behaving like a generic spring everywhere. The type `13` owner is field-gated to the same recovered client family from `CUserLocal::CheckPortal_Collision` at `0x919a10`: `fieldType` `19`, `20`, `21`, `25`, and `32` (`Party Raid`, `Party Raid Boss`, `Chaos Zakum`, `Waiting Party Quest`, and `Hunting Ad Balloon`) plus the explicit `m_dwField == 180000002` fallback case, before the owner evaluates authored `reactorName` or `sessionValueKey`. WZ-backed `reactorName`, `sessionValueKey`, and `sessionValue` metadata still flow through `PortalInstance`, `MapLoader`, map save, and board serialization, and the session-valued branch still uses a client-shaped delayed seam: when a structured session owner is available for the portal-authored pair, the simulator queues a `PendingPortalSessionValueImpact`, emits the client-shaped `CWvsContext::SendRequestSessionValue` outbound request as opcode `191` with `EncodeStr(sessionValueKey)` plus reset byte `0`, and stores the pending impact until a matching client-owned session or field handoff arrives. This row now closes another part of that owner lifecycle inside the existing seam: pending type `13` impacts are no longer released only from the generic structured field-state handoff, because the simulator also decodes and matches the client-shaped packet forms that those admitted owners actually use. Party Raid release coverage inside the same seam still includes direct `OnSessionValue` ingress, packet `149`, and relayed wrapper opcode `163` when they carry the matching key/value pair; Hunting Ad Balloon release coverage now also accepts direct `OnFieldSetVariable` packet `95` and relayed `163 -> 95` in addition to packet `149`; and packet `149` matching now strips client owner-hint prefixes such as `field:` and `session:` before comparing against the authored portal key so wrapper-shaped field/session payloads wake the same pending impact lifecycle instead of falling back to later structured simulator handoff. Structured owner coverage inside that existing seam still includes active `PartyRaidField` owners, Hunting Ad Balloon's authored `balloon_Team = redTeam/blueTeam` pair, and the wrapper-side Chaos Zakum `fire` owner for `Map/Map9/912060200.img/portal/3`; this pass narrows that owner admission again by resolving the pending-impact family in client order, so Hunting Ad Balloon and Chaos Zakum no longer get collapsed into a generic active `PartyRaidField` session owner before the field-specific `balloon_Team` and `fire` seams can claim them. The type `13` lifecycle now keeps the pending store closer to the recovered `CWvsContext::SetImpactNextBySessionValue` shape by replacing any earlier pending portal session-value impact for the live map when a new admitted request is queued, while still pruning stale entries on map mismatch and requiring the decoded packet ingress shape to match the owning family before `SetImpactNext` is released: Party Raid remains session/packet-`149` owned, Hunting Ad Balloon is direct/relayed field-variable packet `95` or packet `149` owned, and Chaos Zakum accepts its wrapper-shaped session/field ingress plus packet `149`. This pass also narrows the type `13` impact owner itself: direct custom impacts and delayed session-value releases now share a recovered `CUserLocal::OnCollisionCustomImpact`-style helper that requires the inferred live field/vector-control owner, records move-path attribute `25` through `CVecCtrl`, queues `SetImpactNext(vx, vy)` instead of applying an immediate simulator-only impulse, and replays the client-resolved StringPool `0xAD7` visual path (`Effect/BasicEff.img/VerticalJump`) from the local-user physics position. The default branch still stays in this same owner too, deciding between same-map registered teleports and cross-map transfer staging before playing the handoff sound. WZ evidence for type `12` remains `Effect/BasicEff.img/VerticalJump`, which publishes a dedicated six-frame effect with authored per-frame origins such as frame `0/origin = (18,72)` and frame `1/origin = (20,77)`; WZ evidence for the type `13` branch remains authored portal nodes such as `Map/Map0/003000000.img/portal/1` and `Map/Map9/912060200.img/portal/3`, which expose `pt = 13` together with authored impact/session metadata, while `Map/Map9/970050010.img/portal/6` and `portal/7` pair `sessionValueKey = balloon_Team` with `sessionValue = redTeam` / `blueTeam` and the same map publishes `info/fieldType = 32` with `info/onUserEnter = Balloon_D_Enter`. Client evidence remains `CUserLocal::CheckPortal_Collision` at `0x919a10`, where type `9`, `12`, `13`, and the default transfer path are split in one function; the type `9` branch constructs `COutPacket(112)`, encodes `get_field()->m_bFieldKey`, the portal name, and the local-user x/y, sends it through `CClientSocket`, then calls `CWvsContext::SetExclRequestSent(1)` and stores the previous portal index, while `CWvsContext::SetExclRequestSent` at `0x49c580` writes the sent flag and current update time; the type `12` branch calls `CUserLocal::VerticalJump`, and the targeted `CUserLocal::VerticalJump` decompile at `0x90f510` shows the client checking the live vec-ctrl interface gate, setting move-path attribute `24`, queueing `SetImpactNext(vx, -2300)`, resolving the `VerticalJump` string-pool path, and spawning the effect before the caller runs the passenger check, while `CVecCtrl::SetMovePathAttribute` at `0x52b000` shows the client recording that attribute through the vec-ctrl move-path owner rather than as a loose side flag and the shared movement packet decode evidence keeps attribute `24` as a no-body element with the common move suffix and attribute `4` as position plus foothold plus the same suffix; `CUserLocal::OnCollisionCustomImpact` at `0x90b750` uses the same vec-ctrl interface gate, records move-path attribute `25`, queues `SetImpactNext(vx, vy)`, and resolves StringPool `0xAD7` to `Effect/BasicEff.img/VerticalJump`; the type `13` branch is admitted only for field types `19`, `20`, `21`, `25`, `32` or field id `180000002`, and session-valued impacts call `CWvsContext::SetImpactNextBySessionValue`; the follow-up decompile at `0x9e52f0` shows that helper calling `CWvsContext::SendRequestSessionValue` and storing the pending key/value plus velocities, while `SendRequestSessionValue` at `0x9e1a90` builds opcode `191` with the session key and reset byte `0`. Focused regression coverage for the recovered type `9` opcode `112` payload shape, the recovered type `9` authoritative-field-key handoff and post-bind persistence, the recovered type `9` exclusive-request release cases, the recovered type `9` inventory-operation (`opcode 28`) `bExclRequestSent` reset-marker consumption seam, the recovered shared transfer-request exclusive gate for type `9` dispatch (`CanSendExclRequest(500, 0)`-style), the recovered type `12` horizontal-impact formula, the recovered type `12` live-interface gate, the shared vec-ctrl move-path attribute carry, the client-shaped move-path payload layouts for portal attributes `24`, `25`, and `4`, the type `13` field admission family, the type `13` live-vector-control gate, the `VerticalJump` effect path, the opcode `191` session request shape, and the type `13` release packet shapes now lives in `UnitTest_MapSimulator/PortalCollisionOwnershipParityTests.cs`, including owner-family resolution coverage that locks Hunting Ad Balloon and Chaos Zakum ahead of the generic Party Raid runtime, owner-specific pending-impact compatibility coverage for direct packet `95` Hunting Ad Balloon release, plus the client-shaped single live-map pending-impact replacement rule, transfer-lifecycle consumption coverage when cross-map handoff ownership is queued, and recursive owner-hint prefix stripping coverage (`field:`/`session:` chains) for packet-`149` key matching. Remaining gap: the type `9` owner now consumes `SetExclRequestSent` state from decoded `CStage::OnSetField` ingress, decoded inventory-operation (`opcode 28`) reset-marker ingress, and the existing transfer queue/teardown seam itself: all cross-map pending handoffs now enter through `QueueMapTransfer`, which clears the collision-script sent flag together with portal/cooldown metadata before setting pending map state (covering local portal collision staging, temporary portal staging, stage-transition queued transfers, and portal double-click travel), the collision-script send gate now shares the same recovered `CWvsContext::CanSendExclRequest(500, 0)` transfer-request window by refusing type `9` dispatch while transfer exclusive-request state is still in flight, and temporary cross-map portal collisions now enter that same transfer owner in client order (`CanSendTransferFieldRequest` + restriction/known-map admission + `SetTransferFieldExclusiveRequestSent`) before opcode `113` request staging/dispatch, and opcode-`28` reset-marker consumption now decodes the client-shaped inventory-operation header (`bExclRequestSent` byte plus operation-count byte), rejects malformed non-boolean reset-marker values, rejects operation-count headers whose payload body is shorter than the client minimum per-operation prefix (`operationMode` + `inventoryType` + `fromPosition`, 4 bytes each), and clears both the collision-script and transfer exclusive-request owners in the same seam before collision-seam self-release preserves the recovered per-portal delay gate when no handoff consumed the request. Collision-script cooldown admission now also preserves the stronger active cooldown owner by honoring the maximum of the prior sent portal delay and the currently colliding portal delay while the request tick is still live, so lower-delay collisions cannot short-circuit a still-owned request window. The owner still falls back to the simulator-owned synthetic key before any authoritative stage-transition handoff has populated the live map, and it still does not reconstruct every remaining unrelated client writer that can mutate `CWvsContext` exclusive-request state byte-for-byte. The type `12` branch now records the recovered vec-ctrl move-path attribute through the shared physics path owner, gates the branch on inferred live interface ownership, and can serialize the recovered no-body attribute-`24` move-path element shape, but it still does not reconstruct every `CMovePath::MakeMovePath` flush/random-count/timing detail or the post-jump passenger release byte-for-byte; and the type `13` path now keeps the client-specific owner-family ordering, release packet shapes, single live-map pending-impact replacement, transfer-lifecycle pending-impact consumption on queued cross-map handoff, recursive owner-hint prefix stripping for packet `149` session/field key matching, move-path attribute `25`, queued impact, live vec-ctrl gate, and StringPool `0xAD7` visual owner inside the collision seam, but it still models owner-family admission and cancellation with simulator-side state rather than byte-for-byte `CWvsContext` pending-session fields and per-field virtual-owner state transitions. | Portal parity is no longer only about later request or response handlers because the simulator now has an explicit collision owner that decides what a portal touch means before teleport handoff. Keeping the type split in this owner makes future parity work land in the same place the client uses for admission, one-shot suppression, custom impact, vertical jump, same-map teleport, and cross-map transfer decisions. This pass narrows the remaining type `13` gap by routing both direct custom impacts and delayed session-value releases through the recovered `CUserLocal::OnCollisionCustomImpact` shape: vec-ctrl attribute `25`, queued `SetImpactNext`, live vector-control gating, passenger cleanup through the existing seam, and the same `Effect/BasicEff.img/VerticalJump` visual owner that StringPool `0xAD7` resolves in the client, while the earlier single live-map pending replacement and owner-family ordering still keep Hunting Ad Balloon's field-owned `balloon_Team` path and Chaos Zakum's `fire` wrapper path from being reduced to a generic active Party Raid runtime. | portal collision field-runtime layer (`CUserLocal::CheckPortal_Collision`, `CPortalList::FindPortal_Collision`, `PortalPool.FindPortalCollision`, `MapSimulator.TryHandlePortalInteractCore`, `CWvsContext::SetExclRequestSent`, `CWvsContext::SetImpactNextBySessionValue`, `CWvsContext::SendRequestSessionValue`, `CUserLocal::VerticalJump`, `CVecCtrl::SetMovePathAttribute`, `CMovePath::MakeMovePath`, `CField::SendTransferFieldRequest`, `CUserLocal::TryRegisterTeleport`, `CUserLocal::OnCollisionCustomImpact`, `CStage::OnSetField`), `CMovePathClientPacketCodec.cs`, `UnitTest_MapSimulator/PortalCollisionOwnershipParityTests.cs` |
| Partial | Reactor collision polling parity | The simulator keeps reactor touch ownership in one explicit once-per-second `CUserLocal::CheckReactor_Collision`-style poll in the main update loop: it tracks the last poll tick, refreshes a persistent local touched-reactor set through `ReactorPool`, and applies the recovered client contact rule of testing the live local-user vec position against each touch reactor's current bounds rather than a synthetic avatar rectangle. The packet transport split remains in the same seam: enter/leave diffs emit opcode `250` (`Encode4(dwID)` + `Encode1(touchFlag)`) through a live reactor official-session bridge when attached, otherwise defer through that bridge when armed without a connected Maple session, and otherwise send/queue the same raw opcode `250` through the dedicated reactor-touch `packetoutraw` outbox. Existing ownership behavior also remains: the main update loop keeps the reactor official-session bridge armed, refreshes discovery, and drains inbound packet-owned reactor-pool packets; packet `EnterField` can still adopt a real packet object id onto a unique still-live authored reactor (`template + x/y + flip`, with non-conflicting `name` when present) rather than always spawning a duplicate packet-owned reactor; packet leave/removal still clears touched ownership immediately and removes deferred opcode-`250` entries for removed `dwID`s; touch scans still skip `PacketLeavePending`; and late packet-id recovery for an already-touched authored reactor still re-keys and promotes touch-enter ownership in the same tick. This pass narrows the timing seam again in the existing owner boundaries: deferred touch transport still carries per-request source ticks and replays deferred opcode-`250` enter/leave ownership in recorded order with preserved inter-request cadence (including `Environment.TickCount` wraparound math), replay is still advanced from the main update loop through `FlushDeferredReactorTouchOwnership` so both the official-session bridge and `packetoutraw` outbox progress on the same runtime clock, overdue deferred flush still replays from the recorded due-tick cursor instead of resetting cadence to the current frame tick when flush is late, and replay spacing still preserves full recorded tick deltas without the old 5000 ms clamp. In addition, live sends now stay behind deferred replay cadence in both transport managers: when deferred ownership replay is still pending after a flush attempt, new enter/leave requests are queued behind the pending backlog instead of being injected/sent ahead of not-yet-due deferred entries, preserving recorded ordering across reconnect windows in both the official-session bridge and the `packetoutraw` outbox. The one-second poll gate remains explicit through `MapSimulator.ShouldPollReactorCollision`. Focused regression coverage now lives in `UnitTest_MapSimulator/ReactorCollisionOwnershipParityTests.cs`, covering the recovered `>= 1000 ms` threshold, tick-wraparound poll gating, client-style live-bounds point containment checks, packet-touch polling skip for `PacketLeavePending`, due-tick replay resolution/delay preservation helpers in both transport managers, and outbox ordering when deferred replay is pending, plus packet-enter authored-candidate selection coverage for the unresolved packet-name single-candidate admit path versus the multi-candidate refusal path. WZ evidence remains map `reactor/*` nodes (for example `Map/Map0/001000000.img/reactor/*`), which expose template id, x/y, reactorTime, flip, and name but no server object id; on mounted `v95`, `reactor/0` exposes `id=1002009`, `x=610`, `y=254`, `reactorTime=90`, `f=0`, and `name`, while `reactor/1` exposes `id=1002009`, `x=324`, `y=-78`, `reactorTime=100`, `f=0`, and `name`, with authored node order explicit in-map. Client evidence remains `CUserLocal::CheckReactor_Collision` at `0x903d20` (stores `m_tLastCheckReactorCollision`, gates on `update_time - last >= 1000`, then calls `CReactorPool::FindTouchReactorAroundLocalUser`), `CReactorPool::FindTouchReactorAroundLocalUser` at `0x6cded0` (iterates live reactors, computes bounds from live layer, tests local-user vec point, updates touched map, emits `COutPacket(250)`), and `CReactorPool::OnReactorEnterField` at `0x6cf490` (decodes `dwID/template/state/x/y/flip/name`, allocates live reactor, loads layer, inserts into live pool). Remaining gap: purely local authored reactors that never receive packet `EnterField` object ids still have no authoritative client request id to emit; deferred replay timing now preserves ordering and spacing and no longer lets live sends overtake pending deferred entries, but it still does not reproduce byte-for-byte client socket scheduling jitter/subframe pacing across disconnect windows; and packet-enter ambiguity is narrower again but still present because unresolved packet-name adoption is now admitted only when template/x/y/flip resolution leaves exactly one authored candidate with no conflicting authored name, while multi-candidate unresolved packet-name cases still refuse fallback adoption; disjoint touch/position/state signal combinations still refuse fallback adoption, and the simulator still has no stronger client-backed discriminator than exact-name/packet-name-when-present/current-position/current-touch/initial-state overlap plus narrowed-subset WZ authored order when server object-enter order diverges across otherwise identical live reactors with no authoritative distinguishing signal. | Reactor parity here is an ownership/timing seam, not only authored reactor data or packet state decode. Keeping one poll owner plus packet-shaped touch transport in `MapSimulator` and `ReactorPool` prevents contact ownership from leaking into unrelated activation code paths. This pass tightens deferred replay parity further by preventing new live touch sends from overtaking pending deferred ownership events, so both transport owners preserve recorded order and cadence through reconnect windows while keeping server-authored packet ownership handoff and leave cleanup in the existing seams. | reactor touch-poll layer (`CUserLocal::CheckReactor_Collision`, `CReactorPool::FindTouchReactorAroundLocalUser`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.cs`, `ReactorPool.cs`, `ReactorPoolOfficialSessionBridgeManager.cs`, `ReactorTouchPacketTransportManager.cs`, `UnitTest_MapSimulator/ReactorCollisionOwnershipParityTests.cs`, WZ `Map/*/reactor/*`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".



