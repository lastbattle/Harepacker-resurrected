# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Animation/AnimationEffects.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CReactorPool::OnPacket` at `0x6cf9b0`
- `CReactorPool::OnReactorEnterField` at `0x6cf490`
- `CReactorPool::OnReactorLeaveField` at `0x6ccea0`
- `CReactorPool::OnReactorMove` at `0x6cd110`
- `CReactorPool::OnReactorChangeState` at `0x6ccd60`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUserLocal::OnSetDirectionMode` at `0x9054f0`
- `CUserLocal::OnSetStandAloneMode` at `0x905550`
- `CActionMan::GetNpcImgEntry` at `0x41a840`
- `CActionMan::LoadNpcAction` at `0x420ae0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CField::OnFieldSpecificData` at `0x52a7e0`
- `CField::OnSetQuestTime` at `0x52b790`
- `CField::OnSetQuestClear` at `0x52c870`
- `CField::OnDesc` at `0x5313d0`
- `CField::OnSetObjectState` at `0x539890`
- `CField::OnTransferFieldReqIgnored` at `0x52f3b0`
- `CField::OnTransferChannelReqIgnored` at `0x52f5f0`
- `CField::OnGroupMessage` at `0x535490`
- `CField::OnCoupleMessage` at `0x5357f0`
- `CField::OnFieldObstacleOnOff` at `0x535a80`
- `CField::OnFieldObstacleOnOffStatus` at `0x535b00`
- `CField::OnFieldEffect` at `0x53b790`
- `CField::OnBlowWeather` at `0x5468f0`
- `CField::OnAdminResult` at `0x53bc20`
- `CField::OnQuiz` at `0x537a90`
- `CField::OnWarnMessage` at `0x538160`
- `CField::OnPlayJukeBox` at `0x537940`
- `CField::OnWhisper` at `0x5448a0`
- `CField::OnSummonItemInavailable` at `0x52f7b0`
- `CField::OnStalkResult` at `0x539910`
- `CField::OnDestroyClock` at `0x52a7c0`
- `CQuickslotKeyMappedMan::OnInit` at `0x6c6130`
- `CField::OnFootHoldInfo` at `0x53a810`
- `CField::OnRequestFootHoldInfo` at `0x52ddd0`
- `CField::OnZakumTimer` at `0x530cc0`
- `CField::OnHontailTimer` at `0x530e70`
- `CField::OnChaosZakumTimer` at `0x531020`
- `CField::OnHontaleTimer` at `0x5311d0`
- `CField::OnPacket` at `0x546d50`
- `CStage::OnPacket` at `0x71b0b0`
- `CStage::OnSetField` at `0x71b150`
- `CStage::OnSetITC` at `0x71c000`
- `CStage::OnSetCashShop` at `0x71bfa0`
- `CMapLoadable::OnPacket` at `0x61fd80`
- `CMapLoadable::OnSetBackEffect` at `0x620160`
- `CMapLoadable::OnSetMapObjectVisible` at `0x61fe40`
- `CMapLoadable::OnClearBackEffect` at `0x620150`
- `CUIInitialQuiz::OnCreate` at `0x78e720`
- `CUIInitialQuiz::Draw` at `0x78e9a0`
- `CUIInitialQuiz::Update` at `0x79c390`
- `CScriptMan::OnPacket` at `0x6de360`
- `CWvsContext::OnInitialQuiz` at `0x9ffad0`
- `CUserLocal::OnFieldFadeOutForce` at `0x9057f0`
- `CAnimationDisplayer::RegisterUserStateAnimation` at `0x444d30`
- `CAnimationDisplayer::RegisterNewYearAnimation` at `0x4556f0`
- `CAnimationDisplayer::RegisterFireCrackerAnimation` at `0x45a6e0`
- `CAnimationDisplayer::RegisterFollowAnimation` at `0x45ac50`
- `CAnimationDisplayer::ResetLocalFadeLayer` at `0x43b6f0`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Interaction and NPC parity

- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CActionMan::LoadNpcAction` at `0x420ae0`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CReactorPool::OnPacket` at `0x6cf9b0`
- `CReactorPool::OnReactorEnterField` at `0x6cf490`
- `CReactorPool::OnReactorLeaveField` at `0x6ccea0`
- `CReactorPool::OnReactorMove` at `0x6cd110`
- `CReactorPool::OnReactorChangeState` at `0x6ccd60`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUser::PetAutoSpeaking` at `0x8deb10`

Notes:
`CUserLocal::TalkToNpc` is the first concrete client seam for future NPC bridge work. `CUserLocal::Update` also shows client-side quest-alert refresh calls, emotion reset / balloon lifetime upkeep, timer refresh, timed direction-mode release, and status-bar chat logging, while `CUserLocal::TryAutoRequestFollowCharacter` covers automatic escort/follow requests and `CUser::PetAutoSpeaking` covers one of the idle-feedback surfaces tied to those broader interaction loops. A follow-up IDA scan also showed that reactor ownership is split across both local collision and a packet-owned pool: `CReactorPool::OnPacket` dispatches reactor change-state (`334`), move (`335`), enter-field (`336`), and leave-field (`337`) traffic into dedicated handlers instead of leaving all reactor lifecycle behavior on `CUserLocal::CheckReactor_Collision`.

### 2. Reactor layer-build and hit-prop ownership discovered by targeted `CReactorPool` decompile

- `CReactorPool::LoadReactorLayer` at `0x6ce630`

Notes:
The latest targeted decompile exposes a distinct reactor owner that should not stay buried inside the broader packet-lifecycle row. `CReactorPool::LoadReactorLayer` lazy-resolves `CReactorTemplate`, looks up the current state branch by stringified state id, builds the visible state layer through `CAnimationDisplayer::LoadLayer`, switches between repeat and stop playback from `CReactorTemplate::STATEINFO::m_bRepeat`, preserves timed movement against `tStateEnd` for moving states, auto-selects hit reactions through `get_hittype_priority_level` plus `CReactorTemplate::GetHitProp` when no proper event index has been fixed yet, formats and plays the per-state reactor hit sound through the StringPool template at id `2121`, and then assigns the final layer depth from the closest foothold page or z-mass plus the reactor template layer mode. That makes reactor parity split the same way as the other action-loader families already called out elsewhere in the backlog set: `CReactorPool::OnPacket` owns lifecycle and timing, while `LoadReactorLayer` owns state-layer rebuild, hit-prop arbitration, sound, and final z placement.

### 3. NPC action-loader and client-action-set parity discovered by targeted `CActionMan` scan

- `CActionMan::LoadNpcAction` at `0x420ae0`

Notes:
The targeted `CActionMan` lookup exposed another NPC-side owner that was still only implied by broader talk or actor rows. `CActionMan::LoadNpcAction` caches by NPC template, action, and client action-set index; it also has a special `nClientActionIdx == -2` branch that calls `CNpcTemplate::CalcClientActionSetIdx` before choosing the action family, applies the client default frame delay of `180` when delay data is absent, and can append reversed frames when the authored data marks that path. That means NPC parity is still missing a dedicated action-loader seam for client action-set selection and frame-table construction instead of only needing more dialogue or field-placement polish.

### 4. NPC image-entry ownership discovered by targeted `CActionMan` scan

- `CActionMan::GetNpcImgEntry` at `0x41a840`

Notes:
The same `CActionMan` pass shows the client keeps another NPC-side owner below `LoadNpcAction`: template-image resolution itself. `GetNpcImgEntry` caches by NPC template id, resolves the formatted NPC resource path through the resource manager, checks the template `link` field, recursively reopens linked NPC templates, and copies any missing top-level branches from the linked image onto the cached property tree before client-action-set selection runs. That means NPC parity is not only talk flow plus action loading; it also depends on the earlier image-entry seam that decides which normalized NPC IMG entry the action-set loader sees.

### 5. Packet-authored direction and stand-alone mode parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnSetDirectionMode` at `0x9054f0`
- `CUserLocal::OnSetStandAloneMode` at `0x905550`

Notes:
The existing backlog tracks delayed `TryLeaveDirectionMode` release, but the direct `CUserLocal::OnPacket` re-check exposed the client-owned state-entry seam that actually drives those releases. `OnSetDirectionMode` decodes an enable byte plus a delay, clears `m_bAfterLeaveDirectionMode`, enters direction mode immediately when the packet enables it or when the delay is non-positive, and otherwise arms a delayed leave by storing `m_tAfterLeaveDirectionMode = delay + get_update_time()`. `OnSetStandAloneMode` is a separate packet-owned toggle that writes the decoded byte into the `CWvsContext` stand-alone flag rather than behaving like another window-local direction-mode alias. That owner pair is not named anywhere else in the current backlog set.

### 6. Field-state and script-message parity

- `CField::OnPacket` at `0x546d50`
- `CField::OnFieldSpecificData` at `0x52a7e0`
- `CField::OnDesc` at `0x5313d0`
- `CField::OnSetQuestClear` at `0x52c870`
- `CField::OnSetQuestTime` at `0x52b790`
- `CField::OnSetObjectState` at `0x539890`
- `CScriptMan::OnPacket` at `0x6de360`

Notes:
This packet cluster is a distinct client owner that the current backlog did not call out directly. `CField::OnPacket` dispatches field-specific data, help-message dialogs, quest-timer refresh and clear, and object-state flips from one place, while `CScriptMan::OnPacket` owns script-message delivery separately from NPC click handling or reactor collision. The dedicated quest-timer UI owner is tracked separately in the progression/utility backlog so this row stays focused on the packet source and field/script runtime.

### 7. Packet-authored field messaging and feedback parity

- `CField::OnTransferFieldReqIgnored` at `0x52f3b0`
- `CField::OnTransferChannelReqIgnored` at `0x52f5f0`
- `CField::OnGroupMessage` at `0x535490`
- `CField::OnWhisper` at `0x5448a0`
- `CField::OnCoupleMessage` at `0x5357f0`
- `CField::OnSummonItemInavailable` at `0x52f7b0`
- `CField::OnFieldEffect` at `0x53b790`
- `CField::OnFieldObstacleOnOff` at `0x535a80`
- `CField::OnFieldObstacleOnOffStatus` at `0x535b00`
- `CField::OnFieldObstacleAllReset` at `0x52c830`
- `CField::OnPlayJukeBox` at `0x537940`
- `CField::OnWarnMessage` at `0x538160`
- `CField::OnDestroyClock` at `0x52a7c0`
- `CField::OnZakumTimer` at `0x530cc0`
- `CField::OnHontailTimer` at `0x530e70`
- `CField::OnChaosZakumTimer` at `0x531020`
- `CField::OnHontaleTimer` at `0x5311d0`
- `CUserLocal::OnFieldFadeOutForce` at `0x9057f0`

Notes:
This is another packet-owned surface that the current backlog set does not describe explicitly. IDA shows `CField::OnPacket` and `CUserLocal::OnPacket` owning a broad feedback layer that sits between ordinary gameplay and the existing UI rows: group, whisper, and couple message routing feed status-bar chat and whisper-candidate state; field-effect packets drive screen effects, field sounds, BGM changes, boss HP tags, reward-roulette visuals, and object-state changes; obstacle, warning, and forced-fade packets update map presentation directly; and dedicated boss-timer plus transfer-ignored handlers show that several visible notice/timer flows are owned by packets rather than generic field-rule polling.

### 8. `CAnimationDisplayer` social/event animation ownership discovered by function scan

- `CAnimationDisplayer::RegisterUserStateAnimation` at `0x444d30`
- `CAnimationDisplayer::RegisterNewYearAnimation` at `0x4556f0`
- `CAnimationDisplayer::RegisterFireCrackerAnimation` at `0x45a6e0`
- `CAnimationDisplayer::RegisterFollowAnimation` at `0x45ac50`
- `CAnimationDisplayer::ResetLocalFadeLayer` at `0x43b6f0`

Notes:
The function scan surfaced another client-owned surface that the backlog was not naming anywhere else. These registrations show that the client keeps several visible social/event overlays under `CAnimationDisplayer` ownership rather than scattering them only across field-message handlers or one-shot item-effect helpers: follow effects, New Year card midpoint effects, firecracker bursts, avatar-adjacent user-state layers, and local fade cleanup all pass through one animation manager. A focused source pass in `HaCreator/MapSimulator` shows partial simulator coverage through `Animation/AnimationEffects.cs` for follow animations plus packet-owned fade handling, but not a single backlog row that treats this manager as its own parity seam.

### 9. Packet-authored field utility parity discovered by direct `CField::OnPacket` scan

- `CField::OnBlowWeather` at `0x5468f0`
- `CField::OnAdminResult` at `0x53bc20`
- `CField::OnQuiz` at `0x537a90`
- `CField::OnStalkResult` at `0x539910`
- `CQuickslotKeyMappedMan::OnInit` at `0x6c6130`
- `CField::OnFootHoldInfo` at `0x53a810`
- `CField::OnRequestFootHoldInfo` at `0x52ddd0`

Notes:
`HaCreator/MapSimulator` keeps this row on the dedicated `PacketFieldUtilityRuntime` plus `MapSimulator.PacketOwnedFieldUtilityParity.cs` owner seam for `CField::OnPacket` field utilities. `OnBlowWeather` (`158`) drives packet-authored weather and message overrides; `OnAdminResult` (`160`) stays on the client-owned chat/minimap path, including subtype `41` as the no-payload minimap hide branch confirmed by direct IDA (`CUIMiniMap::SetShowMiniMap(0)` then `ToggleMap()`); `OnQuiz` (`161`) resolves `Etc/OXQuiz.img` question and answer text, uses the `UI/StatusBar2.img/mainBar/notice` asset, keeps the client `CUIStatusBar::SetProblem` notice lifetime of `0x1388` (`5000`) ms, and leaves the `30` second event timer on the question-side owner only; `OnStalkResult` (`172`) updates stalk/follow markers; `CQuickslotKeyMappedMan::OnInit` (`175`) follows the client `Decode1` plus `DecodeBuffer(..., 0x20)` shape so the simulator hydrates eight DIK-style quickslot codes into both labels and live `PlayerInput` bindings; and `OnFootHoldInfo` (`176`) / `OnRequestFootHoldInfo` (`177`) keep packet decode/build helpers plus the client-shaped opcode-`270` outbound payload builder `(state, curX, curY, reverseV, reverseH)` with no count or name text in the reply body. The dynamic-foothold seam still prefers live `DynamicFootholdSystem` state over stale cached packet rows, and the packet-owned snapshot name helper keeps the WZ-authored `oS/l0/l1/l2` object contract while omitting the synthetic zero-piece segment and preserving nonzero `piece` values. Focused regression coverage is now actually on disk in `UnitTest_MapSimulator/PacketFieldUtilityParityTests.cs` for admin-result subtype `41`, quickslot init default/custom decode, foothold-info round-trip decode of the verified nonzero-piece authored name, opcode-`270` payload tuple encoding, direct-send/fallback-send/deferred-bridge dispatch shaping, and zero-piece/nonzero-piece snapshot naming. A targeted WZ re-check of the extracted tutorial-family dynamic contracts in `Map/Map0/003000000`, `003000100`, and `003000200` still does not surface authored `name`, `objName`, or singular `tag` overrides on those dynamic objects; the outbound foothold snapshot name stays on the WZ-backed `oS/l0/l1/l2[/piece]/x,y` form there, with plural `tags` remaining extra metadata only. Remaining gap: the outbound foothold-response injection is still not proven end-to-end against a captured official live session in this workspace, and the broader extracted dynamic-foothold map set outside the rechecked tutorial-family contracts still has not been exhaustively re-verified for rarer client-owned outbound name separators or authored override cases.

### 10. Stage-transition and map-load presentation parity discovered by direct `CField::OnPacket` decompile

- `CStage::OnPacket` at `0x71b0b0`
- `CStage::OnSetField` at `0x71b150`
- `CStage::OnSetITC` at `0x71c000`
- `CStage::OnSetCashShop` at `0x71bfa0`
- `CMapLoadable::OnPacket` at `0x61fd80`
- `CMapLoadable::OnSetBackEffect` at `0x620160`
- `CMapLoadable::OnSetMapObjectVisible` at `0x61fe40`
- `CMapLoadable::OnClearBackEffect` at `0x620150`

Notes:
The same `CField::OnPacket` decompile also hands packet families `141` to `146` to two owners that must stay explicit across backlog boundaries. `CStage::OnPacket` owns the stage-switch family for field, ITC, and Cash Shop transitions instead of leaving those jumps implied by login or UI code, while `CMapLoadable::OnPacket` owns packet-authored map back-effects, object-visibility toggles, and back-effect clears instead of treating them as generic field-effect fallout. That is a separate packet surface above the more specific field-feedback, field-state, and cash-stage rows; backlog 11 now links this same handoff from `CLogin::OnPacket` and keeps detailed implementation ownership merged here.

### 11. Passenger passive-move parity discovered by direct `CUserLocal::OnPacket` scan

- `CUser::OnPassiveMove` at `0x8dea10`

Notes:
The same `CUserLocal::OnPacket` re-check also exposed one missing follow-adjacent owner that should not stay buried under request and failure handling alone. `CUser::OnPassiveMove` only runs while `m_dwDriverID` is set, and then forwards the decoded movement straight into the passenger-side `CMovePath::OnMovePacket` seam on the attached vector controller. That makes it the client-owned movement handoff for attached passengers after the request and accept path has already succeeded, not just another follow-request dialog branch.

### 12. Context-owned stage-period transition parity discovered by targeted `CWvsContext::OnPacket` scan

- `CWvsContext::OnStageChange` at `0x9e5360`
- `CStageSystem::SetStagePeriod` at `0x71ec40`
- `CStage::FadeOut` at `0x7192b0`
- `CMapLoadable::ReloadBack` at `0x61f0c0`
- `CStage::FadeIn` at `0x718cd0`

Notes:
The targeted `CWvsContext::OnPacket` pass exposed one more stage-transition owner that should not stay implied by the `CField::OnPacket` / `CStage::OnSetField` rows alone. `CWvsContext::OnStageChange` decodes the target stage-period string plus mode byte, forwards them into `CStageSystem::SetStagePeriod`, and when the active stage is a field it immediately runs the client fade-out, `CMapLoadable::ReloadBack`, and fade-in chain on that live stage. That is a separate context-owned presentation handoff above the packet-owned field/stage dispatcher already tracked later in this document, not just another variant of `SetField`.

### 13. Initial-quiz utility owner parity discovered by targeted `CUIInitialQuiz` lookup

- `CUIInitialQuiz::OnCreate` at `0x78e720`
- `CUIInitialQuiz::Draw` at `0x78e9a0`
- `CUIInitialQuiz::Update` at `0x79c390`
- `CWvsContext::OnInitialQuiz` at `0x9ffad0`

Notes:
The broader script-message row already tracks packet decode, runtime sync, and the current WZ-backed owner preview, but a targeted IDA lookup shows there is still a separate client owner family underneath that packet bridge. `CWvsContext::OnInitialQuiz` is the context-owned entry point that opens or closes the quiz on client mode changes, while `CUIInitialQuiz::{OnCreate,Draw,Update}` prove the quiz is a dedicated utility owner with its own layout, timer/update cadence, and draw lifecycle rather than only an NPC-overlay sidecar. That owner is still absent from the backlog index today, so the current document overstates quiz parity if it stops at `CScriptMan::OnPacket` and the simulator preview seam alone.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Interaction and NPC parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | NPC talk flow | Nearby NPC clicks now open a real simulator dialogue overlay fed from loaded NPC string data including NPC `info/speak` idle lines, quest `Say.img` conversations preserve multi-page WZ ordering, root-level `yes`/`no` branches are now attached to the final numbered page, quest entries now open directly on the authored WZ conversation pages instead of always forcing a synthetic summary page first, common MapleStory script markup is stripped into readable text with additional `#q` including uppercase `#Q`, numeric `#s`, bare plural-suffix `#s`, `#m`, `#z`, `#v`, quest-item count `#c`, mob-name `#o`, quest-reference `#y`, quest-state `#u`, quest-record `#R`, and quest-selected `#M` or `#a` or `#x` token cleanup backed by loaded quest, item, mob, and stored quest-record data where possible, standalone client prompt markers such as `#E` and `#I` are now stripped from read-only simulator text, quest reward-summary markers such as `#Wbasic#`, `#Wselect#`, and `#Wprob#` are now stripped from simulator-visible text instead of leaking raw WZ tags into NPC quest pages, inline `#L` menu options now surface as clickable read-only choices that honor WZ `stop/<page>/answer` metadata using the client-authored 1-based choice position, continue along the numbered conversation flow only for the authored correct answer, and still fall back to authored choice-position branch keys when a `stop/<page>/<branch>` node does not reuse the raw `#L` id, quest dialogue entries now also reuse named WZ `stop/*` and `lost` branches such as `item`, `mob`, `monster`, `quest`, `default`, `info`, and lost-item fallback text instead of flattening those failure states into generic simulator prose, and now also checks authored `job` branch keys when present before falling back to generic locked-quest prose, started quests now also surface WZ `stop/npc` guidance when you revisit the starter NPC instead of falling back to generic progress text, nested authored branch conversations reached through WZ `stop/*`, `yes`, `no`, or other branch nodes now reuse the same multi-page, final-page root-choice, and inline-selection parsing rules as top-level `Say.img` pages instead of dropping those branch-root choices, authored empty `yes` or `no` handoff branches now remain visible as clickable placeholder choices instead of disappearing when WZ leaves the branch body empty for later script execution, authored `stop` metadata no longer leaks into the overlay as a fake visible `Stop` button, authored `answer` bookkeeping and known non-dialogue metadata such as `illustration` no longer surface as fake visible branch choices, item-gated quest conversations now fall back to authored `lost` text whenever the specific `stop/item` branch is absent instead of only when every required item count has dropped to zero, branch-only authored pages now stay in the overlay as actual clickable choice pages instead of being dropped when their visible prose strips to empty, packet-owned quest-result modals now keep those same authored choice-only pages instead of silently filtering them back out after formatting, inline menu labels now keep their raw WZ text until final page formatting so the same live quest-item-count token context used by the page body also resolves inside the choice labels, metadata-only `Say.img` string nodes such as direct stage `info` markers no longer surface as fake visible dialogue pages, recursive numeric conversation containers now also contribute authored `stop/*` and `lost` metadata instead of dropping nested branch guidance when WZ stores the conversation under deeper `0/0/...` wrappers, this pass now also re-selects authored `Say.img` conversation variants when a stage stores multiple numeric children gated by direct WZ `npc`, `job`, or `quest` metadata with the same runtime selector reused by nearby-NPC quest pages and packet-owned quest-result modals instead of always sticking to the first pre-parsed page set, keeps variant-root `yes`/`no` choices attached even when the selected runtime variant is a metadata-only handoff node that publishes its visible numbered pages as later siblings, and now also layers runtime-selected variant-root and generic container-root `stop` metadata together so authored shared `stop/<page>/answer` continuation rules, numbered wrong-answer branches, and generic `default`, `info`, or `quest` paths remain reachable when the selected variant only overrides part of the root stop tree, while still reusing variant-scoped `lost` metadata for issue-page resolution, and authored `#h0#` plus `#j` tokens in nearby-NPC quest pages and packet-owned quest-result modals now resolve from the active simulator build instead of falling back to generic prose, while nearby-NPC non-quest `Talk` pages now also reuse that live simulator formatting context so NPC `info` and `speak` lines can resolve authored player, job, current-map, and stored quest-record tokens instead of falling back to generic text. True script execution, broader automatic branch conditions beyond the current named WZ stop-branch mapping plus authored choice-position fallback and the direct `npc`/`job`/`quest` variant matcher, richer dynamic quest-script variable evaluation beyond the current WZ-backed token fallbacks, action or check-script conversation execution beyond these read-only WZ branches, additional variant-gating metadata beyond the currently parsed direct WZ `npc` or `job` or `quest` keys, and full client conversation layouts are still missing | Core interaction now covers a closer read-only version of client conversation flow, including authored page-first quest conversations, nested WZ branch conversations that now keep their own final-page root choices instead of flattening to plain text, choice-only authored menu pages that remain navigable after formatting strips their visible `#L` markup, packet-owned quest-result modals that now reuse that same choice-page visibility rule instead of re-dropping those authored menu pages, multi-step selection menus that respect WZ-authored answer metadata and its 1-based authored choice ordering instead of advancing on every missing branch or comparing against the raw `#L` id, additional WZ-authored wrong-answer branches whose `stop/<page>/<branch>` keys follow authored menu position instead of the raw `#L` id, the collision case where a correct authored choice now keeps the numbered continuation even when another wrong-answer branch reuses that same numeric key, more of the client-side WZ-backed failure and return-to-NPC text, runtime quest item-count, quest-state, and quest-record token rendering across both quest-detail text and authored `Say.img` conversation pages plus inline choice labels, build-backed player-name and job token rendering across nearby-NPC quest pages and packet-owned quest-result modals, nearby-NPC `Talk` pages that now run NPC `info/speak` text through that same live simulator formatting context including current-map fallback, cleaner MapleStory token rendering across lowercase and uppercase quest-name tokens, mob, pluralized item or mob mentions, selected-quest, quest-reference, stored quest-record placeholders, stray prompt markers, and reward-summary category tags, no longer exposes `stop` or `answer` bookkeeping as user-facing branch choices, no longer misstates branch-root pages by borrowing text from child `yes` or `no` paths, no longer leaks metadata-only stage strings such as `Say.img/3360/0/info = \"0.000000\"` into the overlay as bogus dialogue, now preserves authored empty `yes` or `no` handoff choices as explicit simulator placeholders instead of dropping them, now uses authored lost-item fallback dialogue more often when a quest item is missing, now also checks authored `job` branch keys for start-time job and branch locks before skipping straight to generic fallback prose, now keeps nested `stop/*` and `lost` metadata reachable even when WZ wraps the authored conversation under deeper numeric containers, now follows direct WZ `npc`/`job`/`quest` stage-variant metadata when authored `Say.img` roots gate alternative numeric conversation branches, and now keeps selected-variant `stop/<page>` answer metadata and wrong-answer branches layered over shared container-root stop data instead of letting a partial variant stop tree hide the generic continuation rules. The remaining gap is narrowed to true script execution, wider conditional branch selection beyond the current named WZ stop-branch mapping plus the direct `npc`/`job`/`quest` selector, dynamic script-variable resolution for tokens that still need runtime producers beyond the currently stored quest-record values, post-action or check-script execution, additional metadata-driven conversation variant keys beyond the currently parsed direct WZ selector set, and broader client conversation window parity | player/NPC/UI layer (`CUserLocal::TalkToNpc`) |
| Partial | NPC action-loader and client-action-set parity | NPC loading still routes through the dedicated `NpcClientActionSetLoader` seam from `LifeLoader.CreateNpcFromProperty`, keeps the client-shaped automatic `nClientActionIdx == -2` entry point, loads only the selected action-set actions, applies the client default frame delay of `180`, appends reversed interior frames when authored WZ actions publish `zigzag = 1`, and caches materialized frame lists per `(templateId, clientActionSetIndex, actionName)` so repeated NPC loads stop rebuilding the same selected frames each time. This pass keeps the selection owner aligned to direct `CActionMan::LoadNpcAction` / `CNpcTemplate::CalcClientActionSetIdx` evidence plus WZ-backed `condition*` data: the loader still keeps root actions separate from `condition*` client-action-set metadata instead of letting the synthetic root branch win the automatic pass before authored condition sets are considered, now treats direct string quest rows such as `condition*/<questId> = "1"` as quest-state requirements instead of misclassifying them as record-value checks, and still hardens the parser against the real WZ nested record-value shape surfaced in NPC data such as `condition*/<questId>/value` so exact quest-record matches no longer depend on loose child-type fallback. Authored state codes still follow the client `3 => started or completed` rule, quest-record strings still compare by exact value, ordinary map NPC creation plus NPC preview still thread live quest state and current quest-record values alongside local-player gender, and the social-room employee NPC actor path still receives that same gender plus quest-state plus quest-record context. The automatic selector still stays split the way `CNpcTemplate::CalcClientActionSetIdx` does: when quest context is present it walks authored non-root client-action sets in client order, but when quest context is absent it stays on the client-shaped no-quest-check pass and only considers conditionless sets before root fallback instead of accidentally satisfying authored quest rows like `state = 0` from simulator defaults. The loader still keeps the client-action-set index model fixed too: synthetic root fallback is `-1`, authored `condition*` sets start at `0` in WZ order, and explicit selection no longer aliases root fallback onto condition-set index `0`. `QuestRuntimeManager` still exposes the generic quest-record lookup seam and keeps the packet-owned mate-name record on that path so authored NPC conditions that depend on stored values can participate in action-set choice instead of falling back to row-presence heuristics. Focused source coverage is now on disk in `UnitTest_MapSimulator/NpcClientActionSetLoaderTests.cs` for the client-shaped root-versus-condition index numbering, explicit root and authored-condition selection, authored-order automatic selection, the no-quest-check conditionless pass, the client `state = 3` match rule, the WZ-backed nested `value` quest-record shape, and direct string quest-state rows. Remaining gap: the exact client numeric action-id table that `CActionMan::LoadNpcAction` keys before WZ lookup is still not reconstructed here, and broader `bQuestCheck = 1` parity still depends on recovering more of the client-owned quest-record producers beyond the quest-state plus quest-record seams already wired into the known NPC creation paths. | NPC parity now names and implements the loader owner that sits beneath talk flow and field rendering, and this pass keeps the selected action-family seam closer to the client by separating root fallback from `CLIENT_ACTION_SET` selection, replacing quest-row-presence checks with live quest-state and quest-record evaluation inside the existing seam, aligning the automatic set walk itself to the client's authored-order `CalcClientActionSetIdx` behavior instead of a simulator-invented two-pass priority, preventing simulator-only false positives when no quest runtime is available by honoring the client's separate no-quest-check branch, explicitly matching the client's root `-1` versus authored condition-set `0..n-1` index model instead of collapsing both onto simulator index `0`, and covering both the common nested WZ `value` quest-record row shape and direct string state-row shape in code and tests instead of relying on incidental child scanning. The remaining divergence is narrower and explicit: the simulator still needs the client's exact action-id lookup table and fuller client-shaped quest-record population before this seam can be called fully client-matched. | NPC action-loader layer (`CActionMan::LoadNpcAction`, `CNpcTemplate::CalcClientActionSetIdx`, `NpcClientActionSetLoader`, `QuestRuntimeManager`, `SocialRoomEmployeeActorRuntime`, `UnitTest_MapSimulator/NpcClientActionSetLoaderTests.cs`) |
| Partial | Quest lists and quest-only actions | Nearby NPC overlays now list WZ-backed available, locked, in-progress, and completable quests, keep accept/completion state plus quest mob counts, picked-up quest item counts, and quest action item grants/consumes for the simulator session, gate startable quests on common `Check.img` item, fame, trait-min, skill-acquire, ancestor-job, explorer `subJobFlags`, active-pet, pet-tameness, hidden-item, and `endmeso` requirements, and surface those same authored requirements through locked quest rows, quest detail lines, completion progress, and accept or complete actions. Common `Act.img` rewards now also apply skill/master-level and SP updates, trait EXP, buff-item effects, pet tameness/speed rewards, linked quest-state mutations, `nextQuest`, `npcAct`, `message`, `map`, signed meso/fame deltas, `resignRemove` give-up cleanup, root-job and class-branch reward filters, `jobEx` explorer subjob gating, reward-choice prompting for multi-option `prop = -1` groups, inventory-space validation before commit, transactional rollback on late live-inventory add failures, and negative-count consume rows in both gating and pre-action consumption. Mixed live-inventory plus tracked quest items for the same item id now also stay aligned inside that existing item seam: WZ-backed quest detail progress, locked requirement rows, completion checks, and negative-count `Act.img/item` consumes now resolve against the combined live plus tracked count instead of hiding tracked quest-state stock once a live stack exists, and partial live-inventory consumes now roll back cleanly if the tracked remainder is still insufficient. Using the already loaded action bundle from `Quest/Act.img`, the client-style packet-result action notice seam now stays aligned with nearby-NPC quest pages and quest detail reward text: authored `npcAct` values such as `quest/Act.img/2165/1/npcAct = "quest"`, `quest/Act.img/3340/1/npcAct = "act3340"`, and `quest/Act.img/10931/1/npcAct = "boom"` still surface through the shared action metadata path, common zero-sentinel `Act.img/info` strings such as `0`, `000`, or `000000` stay filtered, multi-option choice groups that still have more than one eligible `prop = -1` row after job/gender filtering no longer emit a bogus empty inventory-category notice, and single remaining eligible choice rows still auto-resolve into the category summary. This pass also keeps more of the authored action conversation on the same quest-action owner instead of dropping back to `Say.img` when `Act.img` already carries the result script: packet-result modal presentation now prefers parsed `Quest/Act.img` numbered and choice pages when present, action-owned numbered pages are now ordered by their numeric WZ keys instead of raw child enumeration so late or out-of-order WZ page inserts still render in authored `0 -> 1 -> 2 ...` sequence, and branches such as `quest/Act.img/10023/0` (`0`, `1`, `yes`, `no`, `stop/0/answer`) plus `quest/Act.img/1031/0` (`0`-`3`, `yes`, `no`) can travel through the shared packet-result modal seam instead of collapsing to the older say-page fallback. Focused regression coverage is now checked in at `UnitTest_MapSimulator/QuestActConversationParityTests.cs` for numeric `Act.img` page ordering, `Act.img` yes/no page parsing, and packet-result modal preference for action-owned conversation pages over the say-page fallback. Remaining gap: deeper full script branching beyond these direct `Act.img` numbered and choice pages, broader skill-book-style handling beyond direct skill/master-level updates, rarer nonstandard `Act.img` action families outside these common seams, and deeper per-slot inventory-backed quest parity such as exact slot-by-slot add/consume failure semantics beyond the current combined-count fix, accept-or-block precheck, and transactional late-add-failure guard are still incomplete. | Progression-facing NPC parity now has a broader WZ-backed simulator baseline for both start and completion requirements plus common reward mutations, so fame, trait, skill-gate, root-job start gating, explorer subjob start gating, beginner-inclusive job-array reward filtering, skill-level and master-level grants, SP, meso-cost, item, chained-quest state feedback, give-up item cleanup, bare-`id` skill acquire gates, root-job or class-branch reward filtering, the common `prop = -1` choice-reward surface, the common `buffItemID`, `message`, and `map` metadata path, WZ pet quest requirements, tameness rewards, speed rewards, authored NPC action metadata, hidden `Check.img/item/secret` requirements, linked quest mutations, next-quest pointers, build-filtered quest-detail reward text, reward lines, client-style packet-result action notices, and now direct `Act.img` result-script pages stay aligned across the NPC overlay, quest detail, and packet-result presentation surfaces without widening ownership away from the existing quest-action seam. The remaining gap is narrowed to deeper script-driven branches beyond these direct action-owned pages, broader skill-book style quest rewards beyond the current direct skill/master-level flow, remaining rarer nonstandard `Act.img` action families outside these common seams, and deeper exact slot semantics beyond the current combined-count fix plus existing accept-or-block and transactional late-add-failure guards. | NPC/UI layer |
| Partial | Auto-follow, escort, and passenger passive-move requests | Escort mobs still mirror the client-backed `TryAutoRequestFollowCharacter` auto-attach gate, and the simulator-owned local follow seam still covers more of the `CWvsContext::SendFollowCharacterRequest`, `CUserLocal::OnFollowCharacterFailed`, `CUser::OnFollowCharacter`, and `CUserLocal::OnKey` path. The latest IDA re-check kept the passenger owner narrow: `CUser::OnPassiveMove` at `0x8dea10` still only runs while `m_dwDriverID` is set and only forwards the inbound packet into the passenger `CMovePath::OnMovePacket` seam instead of treating passenger motion as a side effect of request or accept state alone. The existing simulator seams now stay aligned with that ownership split without widening elsewhere: local utility ingress still accepts named `passivemove` replay plus real client opcode `269`, the official-session bridge still mirrors inbound `269` beside `193/253/254/270`, attached local follow still prefers the decoded packet-owned move-path snapshot before falling back to the older driver-derived passive-follow resolver, the stored packet-owned passive move is still cleared on attach, detach, and map-content resets, blank or no-payload follow failures still fall back to the same client unknown-error notice text exposed through `FollowCharacterFailureCodec`, and attached passenger playback still carries the decoded or synthesized foothold id forward instead of dropping grounded transitions to `0`. Manual request initiation also stays in the existing social/profile seam but now continues to honor the recovered `CUserLocal::HandleRButtonClk` gate for live in-world target presence, foothold-backed range, traversability, passenger occupancy (`reason 3`), and occupied-driver formatting (`reason 2` with the live driver id) before it routes through the local follow runtime and official-session bridge. Focused regression coverage is now actually on disk in `UnitTest_MapSimulator/FollowCharacterPassiveMoveParityTests.cs` for the client-shaped blank failure fallback, occupied-target wording, and packet-owned passive-move foothold carry-through. Remaining gap: the incoming Yes/No follow prompt still opens through a synthetic simulator-owned packet owner rather than a confirmed client-backed prompt packet even though the body text comes from the client StringPool; manual request initiation still reuses the remote character-info seam instead of the exact `CUserLocal::HandleRButtonClk` context-menu owner and its full modal menu contract; the exact withdraw-packet body or caller contract beyond the opcode-owned lane is still unconfirmed; the `269` branch still reuses the shared simulator move-path decoder instead of a byte-for-byte `CMovePath::OnMovePacket` clone; and the shared remote riding mirror is still reconstructed from the available mount, temp-stat, and action owners rather than a directly decoded remote `m_nRidingVehicleID` field | Escort-style encounters already rejected the client-invalid midair, map-disabled, and local-user-invalid auto-attach cases, and this pass keeps the client follow-request option plus the explicit passenger move-path owner on that same parity story instead of leaving passenger motion implied by the request handshake alone. The simulator still preserves client-shaped request throttling, follow-failure clearing, attach acknowledgment, local-player snap-back versus transfer detach behavior, packet-authored attach or detach playback for the local passenger path, remote passenger ownership on the shared runtime, live-session outbound request or accept injection for opcode `134`, an explicit outbound withdraw lane for opcode `135`, a separate client-shaped attached-release request for `134(0,0,1)`, native arrow-key-triggered release on the same `CUserLocal::OnKey` seam the client uses, inbound official replay of `193`, `269`, and `270`, a closer active-mounted blocker than the earlier broad "mount equipped" approximation, a client-option-backed gate for both escort auto-follow and incoming follow prompts, direct client notice text for follow failures, the client StringPool body for the still-synthetic incoming prompt, the client unknown-error fallback for blank follow failures, the packet-authored passenger foothold state for attached passive-move playback, the client-authored manual follow button, and now the focused regression coverage that pins those follow-failure and passive-move foothold seams. The remaining gap is narrowed to the confirmed client-backed prompt owner, the exact `HandleRButtonClk` menu contract, the withdraw-packet body or caller contract beyond the modeled opcode lane, the last byte-exact `CMovePath::OnMovePacket` behavior details that the shared decoder still approximates, and a directly decoded remote riding-vehicle field instead of the current shared-owner reconstruction. | `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedStageTransitionParity.cs`, `EscortFollowController.cs`, `HaCreator/MapSimulator/UI/Windows/OptionMenuWindow.cs`, `HaCreator/MapSimulator/UI/Windows/UserInfoUI.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.SkillsAndQuests.cs`, `HaCreator/MapSimulator/Interaction/LocalFollowCharacterRuntime.cs`, `HaCreator/MapSimulator/Interaction/FollowCharacterFailureCodec.cs`, `HaCreator/MapSimulator/Interaction/PacketOwnedLocalFollowPassiveMoveResolver.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Pools/RemoteUserPacketCodec.cs`, `UnitTest_MapSimulator/FollowCharacterPassiveMoveParityTests.cs`, field interaction layer (`CUserLocal::TryAutoRequestFollowCharacter`, `CWvsContext::SendFollowCharacterRequest`, `CUserLocal::OnFollowCharacterFailed`, `CUserLocal::FollowCharacterFailedMsg`, `CUser::OnFollowCharacter`, `CUser::OnPassiveMove`, `CMovePath::OnMovePacket`, `CUserLocal::OnKey`, `CUserLocal::HandleRButtonClk`, `UIWindow.img/ContextMenu/BtFollow`, `UIWindow2.img/ContextMenu/BtFollow`) |
| Partial | Packet-owned reactor-pool parity discovered by IDA function scan | This row still stays on the existing packet-owned `ReactorPool` seam, but the simulator now closes two concrete owner gaps that were still only described in notes. WZ-first validation still anchors both spawn and hit timing: map reactor entries in `map/Map/Map0/*/reactor/*` expose packet-compatible fields such as `id`, `x`, `y`, `f`, `name`, and optional `reactorTime` (for example `map/Map/Map0/001000000/reactor/0` publishes `id=1002009`, `x=610`, `y=254`, `f=0`, empty `name`, and `reactorTime=90`), while reactor template `reactor/1002009/0/hit/*` publishes two `150ms` hit frames that back the simulator-owned packet hit window, and authored `info/reactorType` or `info/type` values now also hydrate freshly entered packet-owned reactors through the same metadata loader the local pool already uses. Client evidence from `CReactorPool::OnPacket` (`0x6cf9b0`) plus `OnReactorEnterField` (`0x6cf490`), `OnReactorChangeState` (`0x6ccd60`), `OnReactorMove` (`0x6cd110`), `OnReactorLeaveField` (`0x6ccea0`), `Update` (`0x6cefc0`), `LoadReactorLayer` (`0x6ce630`), and `get_hittype_priority_level` (`0x6cc000`) still drives the simulator-owned phase clocks in `Pools/ReactorPool.cs` alongside the packet decode seam in `Interaction/PacketReactorPoolRuntime.cs`. `/reactorpacket` still accepts opcode-framed `packetclientraw <hex>` input, `Managers/ReactorPoolPacketInboxManager.cs` still validates 2-byte opcode-framed client packets for `334` to `337`, the older shared-ingress replay gap stays closed through `Interaction/PacketFieldIngressRouter.cs` plus `TryApplyPacketOwnedFieldScopedPacket` in `MapSimulator.PacketOwnedFieldStateParity.cs`, and the live ingress bridge still stays on the dedicated reactor owner in `Managers/ReactorPoolOfficialSessionBridgeManager.cs` instead of widening the packet surface again. This pass tightens the same seam further instead of widening ownership again: packet-owned enter-field now hydrates the WZ-backed interaction metadata that the local pool already carries (reactor type, raw hit option, item or quest requirements, and script aliases) before applying the packet state, packet-owned auto-hit fallback now feeds the raw authored hit option into the simulator copy of `get_hittype_priority_level` instead of collapsing everything to coarse `ReactorType` guesses, packet-owned enter-field still preserves the client `OnReactorEnterField` fade-in instead of spawning fully opaque, packet-owned change-state and leave-field still no longer swap the visible reactor state on packet receipt, deferred state application still waits for the client-shaped `tHitStart` clock in `Update`, repeated change-state packets still keep the in-flight packet animation clock instead of re-arming it every time, and the change-state update gate now mirrors the client `tCur - tHitStart > 0` boundary instead of the earlier simulator `>=` approximation. The helper clocks still follow the client literals more closely by preserving `tHitStart = current + delay` with the client's only-when-zero `-> 1` guard, preserving leave-field's `current + 400` fallback with the same guard, always arming `tStateEnd = current + 100 * ticks` even when the packet delay byte is zero, and keeping the client-shaped signed `nProperEventIdx` byte instead of flattening `0xFF` or `0xFE` into unsigned sentinels. That means opcode-framed change-state replay and live session ingress can now preserve the same negative event-index sentinels that the client carries into `LoadReactorLayer`, while packet-owned hit fallback can also honor authored raw hit-option masks that do not map cleanly onto the small local `ReactorType` enum. Focused regression coverage is now actually on disk in `UnitTest_MapSimulator/ReactorPoolPacketParityTests.cs` for signed `properEventIndex` payload round-tripping, the client-shaped `tHitStart`, leave fallback, `tStateEnd`, enter-fade alpha, old-state hit-source helpers, and raw hit-option event selection. Remaining gap: exact COM-layer animation-completion polling, byte-exact `LoadReactorLayer` `GetHitProp` object selection and every `get_hittype_priority_level` caller-side state mutation, and the client's unresolved default `IWzVector2D::RelMove` behavior outside the recovered timed windows are still approximated through WZ frame-duration timing, pending-state bookkeeping, and the simulator's current fallback movement path rather than mirroring the client object state byte-for-byte. | Without this row, reactor parity looks more complete than it is because the current backlog only covers the local collision and activation half. The client also owns a separate packet lifecycle for reactor spawn, movement, state changes, and teardown, and that needs to be tracked explicitly so future work lands on the real owner instead of overloading the local `ReactorPool` touch path. | packet-owned reactor runtime (`CReactorPool::OnPacket`, `CReactorPool::OnReactorEnterField`, `CReactorPool::OnReactorChangeState`, `CReactorPool::OnReactorMove`, `CReactorPool::OnReactorLeaveField`, simulator seam search: `HaCreator/MapSimulator/Pools/ReactorPool.cs`) |
| Partial | Direction-mode release timing | NPC dialogue overlays now enter a simulator direction-mode lock that blocks local gameplay input without dropping into free-camera mode, closing those scripted overlays releases control on a delayed timer modeled after `CUserLocal::TryLeaveDirectionMode`, the overlay-dismissal click is consumed so it cannot leak into same-frame NPC or portal interactions before the delay expires, and special-field scripted dialogs are advanced before world interaction handlers so wedding ceremony field dialogs can claim that same blocking owner on the very frame they appear instead of leaking one frame of movement or click-through before the delayed-release path engages. The delayed-release path now uses an explicit scripted-owner registry instead of only a fixed visible-window list so tracked NPC or scripted launch points feed it directly for Cash Shop or MTS preview, trunk, item maker, MapleTV, memo send and package dialogs, social-room preview surfaces for miniroom, player shop, hired merchant, and trading-room, NPC-owned item-upgrade launches plus their Vega Spell child window when opened from the already locked upgrade flow, explicit special-field owner tokens for the wedding dialog, memory game blocker, tournament match-table dialog, and the bespoke `CRPSGameDlg` rock-paper-scissors owner whose open and destroy packets still toggle a direct `_isVisible` path outside `UIWindowManager`. Inherited utility or community windows opened while a scripted lock is already active such as memo inbox, guild BBS, party or guild search, guild skill, family chart or tree, messenger, and the guild-management child surfaces for guild rank, guild mark, and guild creation agreement continue to reuse that same delayed owner chain. The manager-driven owner path is tighter now too: `UIWindowManager` pre-show callbacks and the local `ShowWindow...` helper seam no longer register only a window name for these script-launched and packet-owned utility surfaces, but capture the concrete `UIWindowBase` instance as a live owner predicate when that instance is already known. This pass extends that same direct-owner family onto the shared modal hosts that were still mostly inherited-only: the simulator now lets `LoginUtilityDialog` declare itself as a direct direction-mode owner when the caller is itself a gameplay or packet-owned modal source, so trunk PIC or secondary-password verification and retry prompts, packet-owned AP/SP helper confirmations, packet-owned follow requests, packet-owned notice dialogs, and the packet-owned logout-gift completion follow-up all hold their own delayed-release owner even when no earlier scripted lock was active. The same owner opt-in now exists on the shared `ConnectionNotice` host too, so packet-owned field warnings can route through the existing timed notice seam without dropping back to the broad visibility fallback. The shared in-field `FadeYesNo` / `CUtilDlgEx`-shaped confirm host also continues to take that direct-owner path for the recovered game-menu quit prompt, Messenger invite confirm seam, and Vega white-scroll confirmation instead of depending on a pre-existing inherited lock before registering the host. Focused regression coverage now lives in `UnitTest_MapSimulator/DirectionModeWindowOwnerRegistryTests.cs` for the direct owner-predicate registry path and stale-owner cleanup behavior. Remaining gap: other bespoke non-manager scripted owners and shared-host call sites that still do not opt into a dedicated tracked owner token or live-owner callback, plus visibility paths that never reach either `UIWindowManager.ShowWindow(...)`, `UIWindowBase.BeforeShow`, or an explicit scripted-owner registration seam, still rely on the broader fallback path. | Scripted UI sequences now keep their dismiss or expiry transitions inside the scripted lifecycle instead of accidentally re-triggering world interactions while control is still supposed to be locked, and this pass moves the modal ownership closer to the client seam by making active scripted field runtimes, direct registered-window refreshes, shared `LoginUtilityDialog` prompts, shared `ConnectionNotice` warnings, the shared in-game `FadeYesNo` confirm host, and the in-flow packet, skill, utility-community, anti-macro, logout-gift, reward-result, follow-request, and rock-paper-scissors follow-up owners explicitly hold or inherit direction mode instead of relying only on a broad visibility sweep. The remaining gap is narrowed further to bespoke non-manager owners and shared-host launch paths that still need their own explicit registration seam rather than the legacy fallback path. | `GameStateManager.cs`, `DirectionModeWindowOwnerRegistry.cs`, `UIWindowManager.cs`, `UIWindowBase.cs`, `MapSimulator.cs`, `MapSimulator.ProgressionUtilityParity.cs`, `MapSimulator.VegaSpellParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedLogoutGiftParity.cs`, `MapSimulator.PacketOwnedFieldFeedbackParity.cs`, `MapSimulator.PacketOwnedAntiMacroParity.cs`, `MapSimulator.GuildManagementParity.cs`, `MapSimulator.TrunkAccountSecurityParity.cs`, `SpecialFieldRuntimeCoordinator.cs`, `SpecialEffectFields.cs`, `UnitTest_MapSimulator/DirectionModeWindowOwnerRegistryTests.cs`, `UIWindow(.img|2.img)/FadeYesNo`, `UIWindow2.img/UtilDlgEx` (`CUserLocal::TryLeaveDirectionMode`, `CRPSGameDlg`, `CWvsContext::OnLogoutGift`, `CUserLocal::OnAskAPSPEvent`, `CUtilDlgEx`) |
| Implemented | Packet-authored direction-mode and stand-alone control parity | The simulator now carries a dedicated packet-owned local-control layer instead of folding everything into the inferred window lock: `GameStateManager` separately tracks scripted-owner direction mode versus packet-owned direction mode, packet-authored `SetDirectionMode` writes now clear any pending packet release first, apply immediate enable or immediate disable when the delay is non-positive, and otherwise keep the packet-owned direction-mode bit alive until the stored delayed release expires in `UpdateDirectionMode`, while `SetStandAloneMode` now writes a separate runtime stand-alone flag that status surfaces and later owners can inspect without pretending it is another visible window owner. That stand-alone bit now also participates in the simulator's inherited direction-mode owner routing, so later modeless owners opened through the existing `ShowWindowWithInheritedDirectionModeOwner` and implicit-owner seams can re-enter the tracked direction-mode path after a packet-authored `SetStandAloneMode(254)` write even when no synthetic visible owner was created. This pass also removes the row's synthetic-id-only packet gap by teaching the local-utility seam to decode real opcode-framed `CUserLocal::OnPacket` traffic: `LocalUtilityPacketInboxManager` now accepts `packetclientraw` lines, validates the 2-byte opcode, and forwards live client opcodes such as `253` and `254` with their payloads instead of requiring synthetic `1013/1014` ids, while a new `LocalUtilityOfficialSessionBridgeManager` can proxy a live Maple session and mirror inbound `SetDirectionMode(253)` and `SetStandAloneMode(254)` packets into the same existing utility handler path. The local-utility command surface and help text now expose both `packetclientraw` and `/localutility session ...` control so this row can be exercised through opcode-framed local replay or a confirmed live inbound proxy rather than only through synthetic loopback aliases. Focused regression coverage now includes `UnitTest_MapSimulator/GameStateManagerTests.cs` for the inherited-owner predicate that keeps packet direction mode and stand-alone control wired into later owner inspection. | If this owner stays folded into the existing release-timing row, future work can overstate direction-mode parity while still missing the packet branch that actually turns the mode on, schedules a delayed off transition without a visible owner, or flips the separate stand-alone control flag in `CWvsContext`. Tracking it explicitly keeps future scripted-control and packet-driven interaction work anchored to the real `CUserLocal::OnPacket` owner instead of only the simulator's inferred window-lock path. | packet-owned local control-state layer (`CUserLocal::OnPacket`, `CUserLocal::OnSetDirectionMode`, `CUserLocal::OnSetStandAloneMode`, `CUserLocal::SetDirectionMode`, `CUserLocal::TryLeaveDirectionMode`, `CWvsContext`) |
| Partial | Reactor interaction parity | Local player movement now runs reactor touch checks through `ReactorPool`, throttles the local touch sweep to the client's once-per-second cadence from `CUserLocal::CheckReactor_Collision`, reactor visuals now load full WZ-defined direct frame sequences plus sparse numeric state branches instead of collapsing many reactors to `0/0` or hard-falling back to state `0`, runtime reactor activation now resolves `info/reactorType` back into touch, hit, skill, item, or animation-only handling instead of collapsing most non-quest reactors to touch, and now also falls back to WZ-authored state `event/*/type` metadata when `info/reactorType` is absent so common harvest, item-use, skill-use, touch-only, and sparse quest-only reactors stop defaulting to touch just because the info block is sparse. The non-touch branch stays wired into the simulator's actual player attack surfaces by forwarding world-space melee or magic hitboxes, projectile impacts and explosion areas, summon strike areas, and the basic no-skill attack fallback into bounded reactor hit checks with left-versus-right hit gating for directional reactor types; item-backed reactors now only activate from matching inventory use near the local player and can consume the matching item even when the simulator has no richer local effect for that item yet; quest-backed reactors now bind directly to the simulator quest runtime so they enter or reset their active state as quest requirements change; and sparse quest-backed reactors can now also infer their required quest id from authored `event/*/(quest|questID|questid|reqQuest|id)` metadata when the `info` block omits it while quest refresh forwards that live quest id into authored selector dispatch so same-type quest event branches can land on the matching WZ-authored target state instead of always defaulting to the first quest edge. Reactor runtime state progression now keeps the real WZ state ids and per-state frame durations alive during activation or follow-up hits instead of collapsing back to synthetic `1/2/3/4` states or destroying multi-state hit reactors after the first post-activation strike, authored WZ state-event targets are now consumed for single-target activation and timed reset paths so reactors that encode their next or reset state in `event/*/state` no longer rely only on numeric state ordering, authored item-, skill-, and quest-selector branches from `event/*/(item|itemID|itemid|skill|skillID|skillid|quest|questID|questid|reqQuest|id)` now also flow through the live activation seam so sparse reactors with multiple WZ-authored item, skill, or quest targets no longer always collapse onto the first matching event edge just because the simulator previously dropped the triggering selector id before transition selection, and the pool now preserves that triggering selector id across later same-type auto-chained reactor states as well so authored multi-step item, skill, or quest graphs continue down the matching WZ-authored branch instead of reverting to the first same-type candidate after the first hop. This pass also hardens two of the row's simulator-owned decision seams with direct regression coverage instead of leaving them documented-only: selectorless item branches that coexist with selector-specific authored fallbacks are now pinned by `UnitTest_MapSimulator/ReactorItemSelectorFallbackParityTests.cs`, and the standalone packet move fallback window plus eased progress math are now pinned by `UnitTest_MapSimulator/ReactorMoveParityTests.cs` through the same `ReactorPool` helpers that local packet-owned move playback uses at runtime. Packet-owned `CReactorPool::OnReactorChangeState` handling still seeds the authored-branch preference from the decoded `properEventIndex`, so server-selected same-type WZ `event/*` branches stay aligned across later packet-driven timed or auto-chained hops instead of falling back to authored order on the next transition. Authored mixed event-type graphs on the same WZ state now also stay live through the pool instead of being collapsed to one stored activation kind, so touch-plus-item, hit-plus-skill, and quest-plus-interaction reactors can all advertise and resolve the authored branch that matches the real incoming interaction, non-hit reactors now also continue walking authored numeric WZ state chains on their own timer instead of freezing forever on the first progressed state, reactor frame advancement now catches up across larger elapsed ticks instead of only stepping one frame per update, pool runtime state now keeps the live authored frame index for the active state, and local non-packet activation, reset, timed-transition, and auto-chain paths now also push each resolved authored visual state back into the live `ReactorItem` animation seam so render output, active-frame collision bounds, and later reset playback stay aligned instead of leaving local reactors visually stuck on the previous state while only the runtime bookkeeping advanced. Reactors with WZ `info/script` names now feed the simulator's existing dynamic object-tag publication seam through the same alias resolver used for field-entry scripts so script-backed tagged objects can turn on when those reactors activate and turn back off when the reactor resets, destroys, or respawns, and the simulator resolves the full authored `info/script` container through the same recursive or delimited WZ script-name parser already used for quest and NPC publication so reactors with multiple authored script aliases no longer collapse to one flat string before publication. The reactor script publisher now aggregates every authored alias onto the live tag-mutation seam at activation and reset time, so staged or multi-alias reactor publications can retire sibling stage tags and enable all matching authored targets together instead of only honoring one resolved script name, while packet-owned reactor enter-field and change-state application now also resynchronize that same script-publication seam from the live visual state so server-owned reactors that enter already progressed or later reset back to their authored initial state no longer leave tagged map objects stale until a local activation or destroy path happens to touch them. Live reactor collision or item-use checks now consume the active WZ frame bounds instead of a static `ReactorInstance` box so frame-authored origin shifts keep touch, hit, directional-hit, and local item-use interaction aligned to the visible reactor pose, destroyed non-respawning reactors now drop out of the renderable set immediately instead of lingering onscreen because no fade path ever ran for the common destroy case, packet-owned `CReactorPool::OnReactorMove` handling now reuses the same remaining packet state clock that already drives moving-state `tStateEnd` playback so move-enabled reactors can keep traversing toward the latest packet target instead of snapping there the moment opcode `335` arrives, and move-enabled standalone opcode-`335` packets now also keep a simulator-owned fallback move clock alive when the server does not pair the move with a live `tStateEnd` window. That fallback stays inside the existing `ReactorPool` packet seam, is only armed for WZ move-enabled reactors, stays anchored to the same client evidence that `CReactorPool::OnReactorMove` still routes through `IWzVector2D::RelMove` even though the packet itself only carries `(objectId, x, y)`, and now also uses a dedicated eased fallback interpolation instead of another immediate simulator snap. Focused regression coverage now includes `UnitTest_MapSimulator/ReactorItemSelectorFallbackParityTests.cs`, `UnitTest_MapSimulator/ReactorMoveParityTests.cs`, and `UnitTest_MapSimulator/ReactorPoolPacketParityTests.cs` so selector fallback, standalone packet move fallback timing/progress, and packet-authored branch sentinels all stay pinned to the current reactor seams. Remaining gap: the simulator now avoids the old standalone move snap and keeps a short eased fallback window alive, but the client's exact missing-variant `IWzVector2D::RelMove` timing and easing curve are still inferred rather than directly recovered, local or otherwise non-packet first-hop authored non-quest same-event-type branches can still fall back to WZ order when they expose neither a selector nor a selectorless authored fallback, and fuller named reactor-script or field-event execution beyond tagged-object publication remains incomplete | Environmental interaction now covers the client-like touch-reactor path, its collision polling cadence, more of the WZ reactor animation surface, WZ-backed touch versus hit versus skill versus item versus quest activation selection, local inventory-driven item reactor use, quest-state-driven reactor refresh, a first pass of script-backed object-state publishing for tagged map objects driven by reactor `info/script`, live WZ-frame collision bounds so authored frame offsets also affect touch, hit, directional-hit, and local item-reactor checks, closer WZ-authored state playback because the runtime now also honors single-target state-event metadata for missing activation typing and timed reset playback instead of relying only on `info/reactorType` and numeric next-state order, now preserves the triggering item, skill, or quest id across both initial selection and later same-type chain hops so selector-backed `event/*` graphs keep landing on their WZ-authored target state instead of falling back after the first transition, now prefers selectorless authored item-, skill-, and quest fallbacks when the simulator has no selector id to forward, now keeps multiple authored activation kinds live on the same reactor so the pool dispatches against the actual incoming touch, hit, skill, item, or quest request instead of whichever single activation enum happened to win during load, now keeps packet-authored same-type change-state follow-up hops aligned with the server-selected WZ `event/*` branch via `properEventIndex`, now keeps local authored state changes visually synchronized with the live `ReactorItem` animation and collision seam across activation, reset, timed playback, and same-type auto-chain follow-up states, now reuses the packet-owned move clock for opcode-`335` reactor repositioning whenever the server has already exposed a live state-end window, now keeps standalone move-enabled opcode-`335` packets on a fallback packet-owned move clock with eased interpolation instead of snapping them to the target, and now keeps reactor `info/script` publication aligned with the simulator's other field-script seams by parsing multi-name WZ script containers and publishing every authored alias instead of only one flat string while also resynchronizing packet-owned enter and change-state updates against the reactor's authored initial state. The remaining gap is narrowed to recovering the client's exact default `RelMove` duration or easing for standalone move packets, local or otherwise non-packet first-hop same-type authored branches that expose neither a selector nor a selectorless authored fallback beyond WZ order, plus richer named script or field-event execution beyond those existing dynamic-tag toggles | `ReactorPool.cs`, `ReactorItem.cs`, `MapSimulator.cs`, `ReactorScriptStatePublisher.cs`, `UnitTest_MapSimulator/ReactorItemSelectorFallbackParityTests.cs`, `UnitTest_MapSimulator/ReactorMoveParityTests.cs`, `UnitTest_MapSimulator/ReactorPoolPacketParityTests.cs` (`CUserLocal::CheckReactor_Collision`, packet `CReactorPool::OnReactorChangeState`, packet `CReactorPool::OnReactorMove`, `CReactorPool::LoadReactorLayer`) |
| Partial | Quest-alert, balloon, and idle social feedback loops | NPCs with available, in-progress, or completable quests now render the client-backed `QuestIcon` alert art above their heads based on the simulator quest runtime, quest accept/complete actions now queue timed in-world feedback balloons above the speaking NPC instead of collapsing to a single fixed message while also holding the NPC in `speak` until the active balloon expires, nearby NPCs with WZ `info/speak/*` idle lines now periodically surface those lines through the same in-world balloon path, and pets now mirror the client-backed `PetAutoSpeaking` surfaces for level-up, pre-level-up, idle `e_rest`, low-HP alert, and HP/MP potion failure speech with the same six WZ-backed `String/PetDialog.img` event keys that exist in this data set while also honoring the client-backed `0x100` pet-skill gate from `CPet::AutoSpeakingByEvent` instead of letting every pet auto-speak by default. The simulator specialist pet-dialog seam now also loads numbered `String/PetDialog.img` slang entries such as `s1`-`s4` instead of only flat `s` keys, and ordinary non-command player chat now feeds that same loaded specialist feedback path with a short cooldown so compatible active pets can surface non-`PetAutoSpeaking` chatter in-world instead of leaving those lines debug-only. The same specialist seam now also listens to live Messenger room chat, Messenger whispers, simulator-owned social-list chat and whisper actions, social-list guild and alliance notice or rank-title edits plus packet-owned guild/alliance notice or rank-title refresh text, guild-BBS post and reply submission text, skill-macro party-notify chat lines, packet-owned utility chat routes that resolve to the same social chat-log families, packet-owned social chat families such as incoming whispers plus friend, party, guild, alliance, couple, expedition, or channel-style client-chat type `19` field-message routes, MapleTV Megassenger chat mirrors from both local publishes and packet-owned set-message updates, MiniRoom and adjacent `SocialRoomRuntime` speaker plus system/game text, memo or parcel send plus quick-send body text from the memo mailbox owner, direct incoming memo or parcel delivery text from the same mailbox seam including packet-owned parcel arrivals and simulator-owned memo injections, and now the simulator-owned engagement proposal owner's request/custom note text across outgoing opens, incoming opens, inbox-driven opens, accept or withdraw actions, and dismiss/decline actions, so more of the simulator's social text owners can trigger the same WZ-backed in-world pet chatter instead of limiting it to the local chat input box alone. Packet-owned social specialist routing is now also centralized through `MapSimulatorChat.ClientChatMessageAdded`, so supported client chat-log families that reach the shared chat seam no longer need one-off pet-feedback hooks at each packet owner, and packet-owned social-list notice or rank-title refreshes now reuse that same specialist path instead of staying siloed inside the union-management state. Quest `Act.img/petskill` rewards now persist on a compatible active pet selected through the same WZ `Check.img/pet` plus the client-shaped active-pet limit aliases used by these pet-training quests, including `petRecallLimit` and the `petAutoSpeakingLimit` fallback found in this data set, and the quest reward text now resolves recognized client pet-skill bits such as `0x80` and `0x100` to named skills instead of falling back to opaque hex strings. Active pet runtime ownership now also persists the trained pet skill mask, command level, tameness, fullness, auto-loot toggle, and auto-HP configuration across unsummon or controller clears for the same pet item within the current simulator session, and WZ `Item/Pet/*/interact/*/inc` closeness gains now feed command success so trained pets accumulate tameness, step through the simulator's client-shaped command-level thresholds, and keep that command growth attached to the same pet item instead of treating command level as a detached manual knob. USE-item clicks or hotkeys now parse WZ `Item/Consume/*/spec` recovery fields including numeric-string `hpR`/`mpR` values seen in this data set, HP/MP potion failure speech is throttled to the client-shaped first-few repeats instead of firing forever, zero-count USE hotkey bindings now persist across config reloads instead of being dropped when the stack is empty, and live pet-food item uses now route through the loaded `f1`-`f4` dialog buckets by deriving food tiers only from the client-shaped `interact/c1`-`c4` ranges instead of folding unrelated higher `c*` commands back into those four buckets, while also falling back to the truly nearest actually loaded food bucket when a pet only ships a subset such as `f1`, selecting a compatible active pet from the food item's `spec/*` whitelist when present, and only consuming food when the targeted pet's new simulator fullness state can actually accept the WZ `spec/inc` increase while already-full pets surface the matching failure bucket instead. Packet-owned parcel receive-session hydration now also keeps a cached signature set for the last packet-owned mailbox snapshot, so receive-owner opens and later refreshes can surface stored or newly appeared parcel bodies through the existing specialist seam without duplicating rows that already announced through earlier packet `24` delivery events or older hydrated sessions. Remaining gap: additional automatic non-`PetAutoSpeaking` specialist chatter sources outside the currently wired local chat, skill-macro party-notify chat, Messenger, simulator-owned and packet-owned social-list text paths, guild-BBS text paths, MapleTV Megassenger mirrors, `SocialRoomRuntime` speaker/system/game text, memo or parcel compose plus direct incoming-delivery text, the engagement proposal note lifecycle, and the packet-owned client chat routes that now centralize the supported social families through `MapSimulatorChat.ClientChatMessageAdded` are still not modeled, pet ownership still only survives for the current simulator session rather than a fuller durable profile, and wider quest-driven pet-skill semantics beyond the currently consumed auto-speaking gate are still missing | The simulator now surfaces both static quest-state alerts and closer client-like NPC and pet idle feedback loops in-world instead of hiding all progression feedback inside the dialogue overlay alone, closes the consume recovery mismatch by honoring numeric-string recovery ratios in `Item/Consume/*/spec`, and now turns real pet-food item uses into WZ-backed in-world pet speech using the pet's existing command-tier ranges, compatibility list, and `spec/inc` fullness gain so failure speech no longer burns food on already-full pets. This pass also fixes the tier derivation seam to stay anchored to the client-shaped `interact/c1`-`c4` buckets, corrects the fallback selector so partial `f1`-`f4` coverage now chooses the closest loaded bucket rather than simply the next higher one, loads the numbered pet-dialog slang strings that this data set stores under `s1`-`s4` instead of a flat `s` property, and now gives those specialist lines live ordinary-chat, skill-macro party-notify chat, Messenger, simulator-owned social-list chat and whisper actions, social-list guild/alliance editor commits, packet-owned guild/alliance notice and rank-title refreshes, guild-BBS post and reply submission text, packet-owned utility and field-social chat call sites, MapleTV Megassenger mirrors, MiniRoom and adjacent social-room speaker plus system/game text, memo or parcel send text, direct incoming mailbox deliveries, and the engagement proposal owner's staged note/custom-text lifecycle instead of leaving them confined to manual simulator triggers. This pass also ties pet auto-speaking ownership back to WZ quest rewards and the client gate, teaches the quest runtime to honor the `petAutoSpeakingLimit` alias used by these training quests, surfaces named client pet-skill reward text for known bit flags, and keeps the trained pet's core runtime state attached to that pet item across unsummon or map-runtime clears within the active session instead of discarding it immediately. It now also consumes WZ `interact/*/inc` command gains into persisted pet tameness so command success can grow the pet's live command level instead of leaving command progression as a manual-only simulator override, brings channel-style client chat type `19` into the centralized `ClientChatMessageAdded` specialist filter, and keeps packet-owned parcel receive hydration aligned with the mailbox seam by replaying stored rows once while suppressing duplicates on later refreshes. The remaining gap is narrowed to additional chatter sources that live outside the client's six `PetAutoSpeaking` events plus the current local-chat, skill-macro party-notify, Messenger, simulator-owned and packet-owned social-list text seams, guild-BBS text seams, MapleTV Megassenger mirrors, `SocialRoomRuntime` speaker/system/game text, memo or parcel compose and direct-delivery text, the engagement proposal note lifecycle, and the currently resolved packet-owned social or utility specialist seams, plus fuller durable pet ownership beyond the current in-session item-keyed runtime and pet-skill semantics that still need consumers beyond the current auto-speaking path | `QuestRuntimeManager.cs`, `NpcFeedbackBalloonQueue.cs`, `NpcItem.cs`, `LifeLoader.cs`, `PetLoader.cs`, `PetController.cs`, `SocialListRuntime.cs`, `SocialListRuntime.Management.cs`, `GuildBbsRuntime.cs`, `MemoMailboxManager.cs`, `SocialRoomModels.cs`, `MapleTvRuntime.cs`, `EngagementProposalController.cs`, `MapSimulator.PacketOwnedFieldFeedbackParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.EngagementProposalParity.cs`, `MapSimulator.cs`, `MapSimulatorChat.cs`, `UnitTest_MapSimulator/PetSpecialistFeedbackParityTests.cs`, `UIWindow*.img/QuestIcon`, `String/PetDialog.img`, `UIWindow2.img/MateMessage`, `UIWindow2.img/Wedding/Invitation`, `Item/Pet/*/interact`, `Item/Consume/0212/*/spec`, `Quest/Check.img`, `Quest/Act.img` (`CUserLocal::Update`, `CUser::PetAutoSpeaking`, `CPet::AutoSpeakingByEvent`) |

### 2. NPC image-entry ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | NPC image-entry / linked-template parity | The simulator already creates NPCs from loaded template data, but the client resolves that template through a dedicated `CActionMan::GetNpcImgEntry` owner before `LoadNpcAction` chooses an action set. IDA shows `GetNpcImgEntry` caches by template id, opens the formatted NPC IMG path, follows the template `link` field recursively, and copies missing top-level branches from the linked NPC image onto the cached property tree before action-set selection runs. The backlog was not naming that earlier owner at all, which makes linked-template normalization easy to blur into either NPC talk flow or action-set logic. | Without this row, NPC parity can look covered once talk flow and `LoadNpcAction` are tracked, even though the client also decides which normalized NPC IMG property tree exists one layer earlier. Keeping that seam explicit isolates linked-template inheritance and per-template cache work from the later action-loader and dialogue rows. | NPC template/image-entry layer (`CActionMan::GetNpcImgEntry`, `LifeLoader.CreateNpcFromProperty`, `NpcClientActionSetLoader`) |

### 3. Reactor layer-build and hit-prop parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Reactor layer-build / hit-prop ownership | This pass keeps the row on the dedicated simulator-owned rebuild seam and closes more of the client-shaped work that still belonged there. `Pools/ReactorPool.cs` now not only replays packet-owned hit layers and recovered reactor-hit sounds, it also mirrors the client z/key math from `LoadReactorLayer` for layer `0/1/2`, reconstructs moving-state `RelMove` ownership more closely by queueing template-`move` packet state targets, starting motion when the packet reactor enters its active layer, interpolating toward the decoded position across `PacketStateEndTime`, and snapping the final position when the window closes, and now keeps the packet reactor on the pre-hit visual until the client-shaped hit layer finishes instead of switching to the pending active state first. This pass also moves one more part of that motion ownership off the authored reactor instance and onto the simulator’s layer seam: `Entities/ReactorItem.cs` now keeps a simulator-owned current layer position separate from the authored reactor coordinates, `Pools/ReactorPool.cs` now drives packet movement, bounds, and layer-sort placement from that transient position while `RelMove`-style playback is active, and only commits the authored instance position when the packet movement window closes or a non-animated move resolves immediately. `Loaders/EffectLoader.cs` and `Entities/ReactorItem.cs` now go a little deeper on the same `GetHitProp` seam instead of only materializing frame strips: the loader preserves the exact WZ state-layer, state-`hit`, indexed proper-event, and root-`hit` source properties that back each simulator reactor, and packet hit playback now resolves those raw branches with the same client order exposed by `CReactorTemplate::GetHitProp` before choosing any preloaded frame list. Negative proper-event indices still replay the authored state branch when present and otherwise fall back to root `hit`; valid proper-event indices still prefer the exact state event branch, then state `hit`, then root `hit`; and out-of-range authored event indices still fall straight back to root `hit`. Because `ReactorItem` now keeps the raw WZ branch identity alive alongside the preloaded frames, exact authored state-hit timing no longer drops back to `0` just because the simulator failed to preload a matching strip for that state branch, and the hit-prop seam can keep the client-authored branch choice pinned even when the draw path still has to fall back to the simulator’s own layer object. Focused source coverage is now on disk in `UnitTest_MapSimulator/ReactorHitPropParityTests.cs` for exact state-event selection, state-`hit` fallback, out-of-range root fallback, and the raw-property duration path for negative proper-event playback when no exact frame strip was preloaded. Remaining gap: the simulator still approximates `CAnimationDisplayer::LoadLayer` with `ReactorItem` frame substitution instead of the client’s COM layer object, flag, and origin plumbing; the hit-prop seam now preserves the exact raw WZ branch choice and authored duration ownership more faithfully, but it still does not hand that raw property object through a real client-style layer loader; and moving reactors still ease a simulator-owned transient layer position instead of invoking the client’s actual `IWzVector2D::RelMove` on the underlying WZ vector/layer objects. | Without a dedicated loader row, reactor parity can look more complete than it is because backlog 7 already tracks packet ingress, timing, and local collision. The client still splits reactor ownership one layer deeper: packet handlers decide when the reactor changes, but `LoadReactorLayer` decides which state branch, hit prop, playback mode, sound, and final z/page the runtime actually consumes. Naming that owner keeps future work anchored to the real layer-build seam instead of continuing to overload the packet-runtime row. | reactor layer-build/runtime seam (`CReactorPool::LoadReactorLayer`, `CReactorTemplate::GetTemplateProp`, `CReactorTemplate::GetStateInfo`, `CReactorTemplate::GetHitProp`, `get_hittype_priority_level`, `CAnimationDisplayer::LoadLayer`, `play_reactor_sound`, simulator seam: `HaCreator/MapSimulator/Pools/ReactorPool.cs`, `HaCreator/MapSimulator/Entities/ReactorItem.cs`, `HaCreator/MapSimulator/Loaders/EffectLoader.cs`, `HaCreator/MapSimulator/Managers/RenderingManager.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `UnitTest_MapSimulator/ReactorHitPropParityTests.cs`, packet contrast: `HaCreator/MapSimulator/Interaction/PacketReactorPoolRuntime.cs`) |

### 4. Field-state and script-message parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field-owned script/message and object-state packet parity | The simulator still has the dedicated packet-owned field-state runtime at the `CField::OnPacket` seam for `OnFieldSpecificData` (`149`), `OnDesc` (`162`), `OnSetQuestClear` (`166`), `OnSetQuestTime` (`167`), and `OnSetObjectState` (`169`), plus the packet-owned script-message bridge at the `CScriptMan::OnPacket` seam for `OnScriptMessage` (`363`). The bridge still decodes the shared `speakerType` / `speakerTemplate` / `msgType` / `bParam` header confirmed in prior IDA work, resolves speaker NPC names from map/runtime or WZ string data, and keeps packet-authored `OnSay`, `OnSayImage`, `OnAskYesNo`, `OnAskMenu`, `OnAskText`, `OnAskNumber`, `OnAskQuiz`, `OnAskSpeedQuiz`, `OnAskAvatar`, `OnAskMembershopAvatar`, `OnAskPet`, `OnAskPetAll`, `OnAskSlideMenu`, and `OnAskBoxText` inside the existing packet-script seams instead of scattering them across unrelated simulator UI. The newer prompt families remain anchored to WZ-confirmed client UI owners rather than ad hoc simulator surfaces: `UIWindow(.img|2.img)/InitialQuiz`, `SpeedQuiz`, `SlideMenu`, `UtilDlgEx_Pet`, and `UtilDlgEx_MultiPetEquip` still supply the packet-authored metadata and framing, while avatar and pet choice rows still label indexed entries with WZ-backed item names when available. This pass closes one more owner-lifecycle gap in that same seam: the dedicated `InitialQuizTimerRuntime` and `SpeedQuizOwnerRuntime` no longer drop their owner snapshot exactly when the countdown reaches zero, so the existing timeout path can now observe the expired owner state, send the client-shaped empty opcode-`65` reply on timeout, and then clear the owner instead of tearing the owner down one step too early. Using WZ evidence from `UIWindow(.img|2.img)/SpeedQuiz` plus IDA evidence from `CWvsContext::OnInitialSpeedQuiz`, `CUISpeedQuiz::OnCreate`, `CUISpeedQuiz::Update`, and `CUISpeedQuiz::SendResult`, `OnAskSpeedQuiz` mode `0` still suppresses NPC-overlay mutation entirely like the other dedicated client-owner packets, syncs the packet-owned speed-quiz runtime, and opens a centered `CUISpeedQuiz`-style owner instead of keeping the prompt on the generic NPC overlay with a side preview. The owner still uses the WZ-backed `backgrnd` / `backgrnd2` / `backgrnd3` layers, numeric strip, and `BtOK` / `BtNext` / `BtGiveup` controls, mirrors the client `OnCreate` control positions recovered in IDA for the edit box and buttons, captures keyboard and mouse while active, allows answer text entry with the client `nHorzMax = 50` limit, submits the current answer through Enter, OK, or Next, submits an empty answer for Give Up and timeout, and clears the owner when the client mode-`1` close packet arrives. The outbound speed-quiz reply contract is also still on the client-owned wire shape: opcode `65`, message type `7`, then a Maple string payload from `CUISpeedQuiz::SendResult`, rather than the earlier simulator menu-style `accepted + int` encoding. `OnAskQuiz` keeps the already recovered `InitialQuiz` owner branch, and `OnAskPet` / `OnAskPetAll` still resolve packet pet serials through the simulator's active-pet runtime before opening a prompt, drop zero or unresolved serials before building the choice list, label the surviving rows with live pet name, slot, serial, and item metadata, and mirror the recovered no-dialog fallback branches by auto-closing with opcode-`65` status-`2` replies when the client exposes only the fallback path and by auto-accepting the first resolved pet serial with opcode-`65` status-`1` replies when the packet omits prompt text and the client would skip `UtilDlgEx_Pet` or `UtilDlgEx_MultiPetEquip` entirely. The script-message seam still consumes the decoded client-owner sync instead of discarding it: packet-authored quiz packets update `InitialQuizTimerRuntime`, speed-quiz packets update the dedicated speed-quiz owner runtime, mode-`1` close packets clear those owners, `/scriptmsg status` still surfaces both owner countdown states, `/scriptmsg clear` still clears them alongside the overlay, and the initial-quiz channel-restriction path still stays in sync when `CScriptMan::OnPacket` opens or closes the same `InitialQuiz` owner family. Manual packet exercise still has client-shaped `/scriptmsg quizclient`, `/scriptmsg quizclose`, `/scriptmsg speedquizclient`, and `/scriptmsg speedquizclose` helpers alongside the existing compact `/scriptmsg quiz` / `/scriptmsg speedquiz`, pet, petall, slidemenu, say, sayimage, yes-no, menu, avatar, membershop-avatar, text, number, and box helpers plus raw payload injection. Packet-authored close packets still tear down the currently open owner branch, `/scriptmsg slidemenu` still emits the real slide-menu message type (`15`), `OnAskSlideMenu` still distinguishes client-owned type-`0` `CSlideMenuDlgEX` versus type-`1` `CSlideMenuDlg` owners and auto-cancels unsupported type values, and prompt submission still builds opcode `65` replies for `OnAskYesNo`, `OnAskMenu`, `OnAskText`, `OnAskNumber`, `OnAskBoxText`, `OnSayImage`, `OnAskAvatar`, `OnAskMembershopAvatar`, `OnAskQuiz`, `OnAskSpeedQuiz`, `OnAskPet`, `OnAskPetAll`, and `OnAskSlideMenu`, records dispatch status on the packet-script runtime surface, and still exposes the loopback packet-reply transport plus the live official-session bridge and `/scriptmsg transport <status|start [port]|stop>` / `/scriptmsg session ...` controls. On the field-state side, the runtime still binds map help text from map info, shows packet-authored field help overlays, tracks quest-timer start/end windows with WZ-backed quest names and `timerUI` metadata, clears packet-authored timers, appends active timer text into the quest UI surfaces, and routes named object-state flips through the existing tagged-object/runtime seam. The visible help and timer surfaces remain on WZ-backed `UtilDlgEx/notice` and `Quest/TimeQuest` assets including the quest timer alarm-clock icon families and time-gauge textures, and the `OnFieldSpecificData` seam still routes its structured Maple-string pair decode through the shared `PacketFieldSpecificDataCodec`, including prefix stripping for `field:`, `party:`, and `session:` owner hints before dispatching common packet-owned payloads into party-raid variables plus wrapper-owned `escortresult` and `huntingadballoon` state through the current special-field seams. Focused source-level regression coverage for this row is now checked into `UnitTest_MapSimulator/PacketFieldAndScriptParityTests.cs` for the timeout-boundary owner snapshots, the speed-quiz client-owner decode handoff, the opcode-`65` Maple-string speed-quiz reply shape, and the dedicated field-state `OnDesc` / `OnSetQuestTime` / `OnSetObjectState` packet seam. Remaining gap: field-specific-data decoding is still structured-string based rather than client-owner-specific or byte-exact across all field owners; `OnAskPet` / `OnAskPetAll` still use a simulator-owned active-pet serial mapping instead of the client's exact `CharacterData` cash-item inventory lookup and therefore still miss fuller missing-slot and serial-ownership edge cases outside the current session-owned pet runtime; the centered speed-quiz owner now keeps the timeout reply lifecycle aligned, but its body text, draw-time glyph placement, button-state transitions, and broader `CWndMan` focus/capture lifecycle are still simulator approximations rather than a byte-for-byte `CUISpeedQuiz::Draw` recreation; the slide-menu and pet prompt families still stop at WZ-backed metadata and NPC-overlay presentation rather than dedicated `CSlideMenuDlg(EX)` / `UtilDlgEx_*` owner recreation; and the help plus quest-timer packet presentation still does not reproduce the client's exact animation cadence or all `Quest/TimeQuest` layout nuances. The dedicated quest-timer modeless owner itself is tracked separately in the progression/utility backlog so timer UI work does not stay duplicated here. | Map scripts and event fields are not driven only by NPC clicks or reactor collisions. Without this packet-owned bridge, parity work keeps re-implementing isolated outcomes while missing the client seam that coordinates post-entry scripted help dialogs, timed quest deadlines, quest-clear resets, script prompts, and `CMapLoadable::SetObjectState` updates for object-tag and map-state changes. This row is narrower on the script side because the simulator now keeps both quiz-owner families on recovered client owners instead of leaving speed-quiz on the generic overlay, no longer drops the recovered quiz-owner runtime handoff after decode, now preserves the timeout-to-empty-reply owner lifecycle for both quiz owners, still uses the client string-based speed-quiz reply contract, and still filters packet pet prompts through the active pet runtime before surfacing `UtilDlgEx_Pet` / `UtilDlgEx_MultiPetEquip`, while the separate quest-timer UI row keeps modeless timer-owner work anchored to the actual utility-window family. | field/script runtime bridge (`CField::OnPacket`, `CField::OnFieldSpecificData`, `CField::OnDesc`, `CField::OnSetQuestClear`, `CField::OnSetQuestTime`, `CField::OnSetObjectState`, `CScriptMan::OnPacket`) |

### 5. Packet-authored field messaging and feedback parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field chat, effect, warning, and timer packet parity | The simulator keeps the dedicated packet-owned field-feedback runtime on the `CField::OnPacket` / `CUserLocal::OnPacket` seam for `OnGroupMessage` (`150`), `OnWhisper` (`151`), `OnCoupleMessage` (`152`), `OnFieldEffect` (`154`), the obstacle on/off and reset packets (`155`, `156`, `159`), `OnWarnMessage` (`157`), `OnPlayJukeBox` (`158`), transfer or summon-item failure notices (`160`, `161`, `164`), `OnDestroyClock` plus the Zakum/Horntail/Chaos Zakum/Hontale timer packets (`163`, `170`-`173`), and `CUserLocal::OnFieldFadeOutForce` (`174`). The row still carries blacklist suppression, blocked-friend whisper filtering, whisper-result routing, subtype-`9` whisper follow transfer queuing, object-state clears through the dynamic-tag seam, summon-screen-reward visual callbacks, summon sound ownership, the client-shaped chat-owner boss-timer text path, and the packet-owned screen-effect / reward-roulette visual split that now mirrors the recovered `LoadLayer` owner roots, offsets, one-time draw ordering, and fixed alpha setup from `CField::ShowScreenEffect` (`0x5376d0`) and `CAnimationDisplayer::Effect_RewardRullet` (`0x458820`). WZ inspection still shows `Etc/Swindle.img` carrying mixed Latin and Korean warning groups in this data set, while direct IDA on `CCurseProcess::SearchSubstring` (`0x7469e0`), `CCurseProcess::ConvertString` (`0x746c50`), and `CCurseProcess::GetSwindleWarning` (`0x747bc0`) confirms the client filters through recovered `s_FilterChars`, walks candidate starts on DBCS character boundaries, and scans highest-group-first before choosing a random warning from the matched group. `PacketFieldFeedbackRuntime` keeps that byte-path first when the filtered text round-trips through the active ANSI code page, but falls back to the same filtered substring check on Unicode text when extracted WZ keywords or chat text do not round-trip losslessly. That keeps the client-shaped ASCII filter and group ordering while preventing false misses and replacement-character collisions for locale groups such as the Korean `Etc/Swindle.img/6` entries on non-Korean desktop locales. Direct IDA on `CField::OnZakumTimer` (`0x530cc0`), `CField::OnHontailTimer` (`0x530e70`), `CField::OnChaosZakumTimer` (`0x531020`), and `CField::OnHontaleTimer` (`0x5311d0`) now also feeds the same runtime: Zakum/Horntail/Chaos Zakum chat lines resolve through the recovered StringPool ids (`0x107E`-`0x1083`), zero-time packets take the dedicated client expiry string path, `Hontale` follows the client’s distinct two-byte `(mode, value)` packet shape plus its own StringPool ids (`0x16EF`-`0x16F1`), and the runtime now keeps exact client fallback copy for those timer families instead of drifting back to simulator-owned generic prose if the generated StringPool data changes. This run also moves boss-timer ownership past a chat-only seam and into a dedicated packet-owned timerboard runtime: direct IDA on `CField::OnClock` (`0x531510`), `CClock::OnCreate` (`0x4b0bc0`), `CClock::SetTimer` (`0x4b0a80`), `CClock::Update` (`0x4b0930`), and `CField::OnDestroyClock` (`0x52a7c0`) shows the client keeping a top-center `CClock` owner alive until explicit teardown, while WZ re-checks on `Map/Obj/etc.img/timer/{backgrnd,backgrndhour}` and `Map/Obj/etc.img/clock/fontTime/{0-9,comma}` confirm the shared timerboard shell and digit glyphs. `MapSimulator.PacketOwnedFieldFeedbackParity.cs` now hydrates that same packet-owned state from the timer packets, clears it on destroy-clock or zero-time expiry packets, formats minute-only versus hour-capable countdown text through the recovered board split, and draws the WZ-backed timerboard above the field while preserving the existing chat/status notices. The manual command surface still emits the two-byte `Hontale` payload separately from the other boss timers, and the packet-owned UI registration still sorts the reward-roulette family into the client’s job/part/level draw order while preserving the recovered full-alpha one-time tint path. `UnitTest_MapSimulator/PacketFieldFeedbackParityTests.cs` is now on disk with focused coverage for the packet-owned boss-timer visual-state duration mapping and countdown shaping plus the minute-only versus hour-capable timer text formatting. Remaining gap: the swindle-warning path still lacks byte-for-byte proof for the remaining live locale-specific DBCS cases the retail client sees under matching regional ACP settings; whisper chase still queues the simulator's existing map-change seam instead of the client's fuller `SendTransferFieldRequest` lifecycle; screen-effect and reward-roulette playback still do not reconstruct the remaining `LoadLayer` flags, per-layer COM overlay or `z` wiring, or the full byte-for-byte `ShowScreenEffect` / `Effect_RewardRullet` registration lifecycle beyond the recovered owner roots, offsets, draw-order split, and fixed-alpha setup; obstacle presentation still reuses the existing tag-driven object seam instead of the client dynamic-object visuals; summon-effect fallback remains approximate for ids outside the recovered generic family when the extracted `Summon.img` path the client would try first is missing; and the new boss-timer timerboard still approximates the client `CClock` window lifecycle because the simulator now mirrors the recovered timerboard shell, glyph family, countdown ownership, and destroy-clock teardown without yet reproducing the remaining `CWnd::CreateWnd` parameter helper, exact per-mode `CClock::INITPARAM` colors and placement, or the non-boss real-clock/event-clock branches that the generic client owner also carries. | Visible packet-owned feedback is a distinct client-owned surface between gameplay, the status bar, map-state tags, local fade overlays, field music, and boss timers. Tracking it as its own seam keeps future parity work anchored to the dispatcher handlers that actually own chat feedback, warning dialogs, forced fades, obstacle toggles, jukebox swaps, transfer failures, and special-event timers instead of burying those effects inside unrelated rows. | packet-owned field feedback layer (`CField::OnPacket`, `CField::OnGroupMessage`, `CField::OnWhisper`, `CField::OnCoupleMessage`, `CField::OnFieldEffect`, `CField::OnFieldObstacleOnOff`, `CField::OnFieldObstacleOnOffStatus`, `CField::OnFieldObstacleAllReset`, `CField::OnWarnMessage`, `CField::OnPlayJukeBox`, `CField::OnTransferFieldReqIgnored`, `CField::OnTransferChannelReqIgnored`, `CField::OnSummonItemInavailable`, `CField::OnDestroyClock`, `CField::OnZakumTimer`, `CField::OnHontailTimer`, `CField::OnChaosZakumTimer`, `CField::OnHontaleTimer`, `CField::OnClock`, `CClock::OnCreate`, `CClock::SetTimer`, `CClock::Update`, `CUserLocal::OnFieldFadeOutForce`, `MapSimulator.PacketOwnedFieldFeedbackParity.cs`, `MapleStoryStringPool.cs`, `UnitTest_MapSimulator/PacketFieldFeedbackParityTests.cs`, `Map/Obj/etc.img/timer/{backgrnd,backgrndhour}`, `Map/Obj/etc.img/clock/fontTime`) |

### 6. `CAnimationDisplayer` social/event animation-owner parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shared social/event animation-displayer parity | The simulator still keeps this row on an explicit shared animation-owner seam instead of leaving the family implicit across unrelated systems. `Animation/AnimationEffects.cs` still owns the `CAnimationDisplayer`-shaped user-state, follow, and transient-area registrations, the event side still preserves the earlier timing recovery from `CMapLoadable::TransientLayer_NewYear`, `CMapLoadable::TransientLayer_FireCracker`, `CAnimationDisplayer::RegisterNewYearAnimation`, and `CAnimationDisplayer::RegisterFireCrackerAnimation`, and the user-state side still stays close to the client-owned quest-delivery owner recovered from `CUser::OnEffect` cases `30/31` plus `CAnimationDisplayer::Effect_QuestDeliveryItemUse`: `MapSimulator.AnimationDisplayerParity.cs` still resolves quest-delivery layers WZ-first from `Effect/OnUserEff.img/itemEffect/quest/<itemId>`, still falls back to the only shipped quest-delivery effect asset present in the loaded data set (`2430071`) when simulator-owned placeholder item ids have no authored art, and still preserves the client’s `arrive` / `wait` / `leave` path shape through the recovered StringPool templates used by `Effect_QuestDeliveryItemUse` (`0x1A05`, `0x1A06`, `0x1A07`). This pass keeps moving the follow side closer to direct client evidence from `CAnimationDisplayer::RegisterFollowAnimation` (`0x45ac50`), `CAnimationDisplayer::FOLLOWINFO::Update` (`0x44e4f0`), and the `FOLLOWINFO` construction path inside `CItemEffectManager::LoadItemEffect` (`0x58f950`) instead of leaving the simulator on only the earlier flat sprite-follow helper. WZ-first inspection still recovers the only authored follow-particle families visible in this data set under `Effect/OnUserEff.img/eventEffect/flame/0` and `Effect/OnUserEff.img/eventEffect/0`, and the shared follow seam still feeds those authored sequences into spawned follow particles instead of only cycling one attached marker. `Animation/AnimationEffects.cs` still preserves the client-owned random startup angle, generation-point churn, and `nTheta`-style angle stepping, now including the `FOLLOWINFO::Update` branch that picks a fresh random spawn angle whenever no authored generation-point list is present and `nTheta == 0` rather than reusing one startup angle forever. It also still consumes the recovered absolute-follow construction defaults from the client item-effect loader inside the same seam: the helper path understands the default `rtStart` box (`0,0,25,25`), the emission-only `+10` Y bias, and the default absolute follow travel fallback on `szOffset0/szOffset1.cy = -20` when no richer authored follow range has been supplied. That means the simulator’s emitted follow particles no longer keep the absolute-owner path on only the older captured-anchor lerp, and the non-generation-point relative path no longer stays stuck on a single startup heading either; the same shared follow runtime now distinguishes the client’s per-spawn random-angle branch, owner-relative generation-point branch, and emission-box start branch instead of only toggling between relative and absolute anchors. `MapSimulator.AnimationDisplayerParity.cs` feeds those richer defaults through `/socialanim follow <local|characterId> <on|off> [relative|absolute]`, so the manual exercise surface can still switch between the client’s owner-relative and absolute-emission modes while now also driving the recovered absolute emission-box defaults through the shared runtime. The existing packet-owned quest-delivery interaction owner in `MapSimulator.PacketOwnedUtilityParity.cs` still registers that local avatar-following start/repeat/end layer before opening the NPC overlay, the overlay-close and map-clear paths still clear it through the same shared seam, and the packet-owned blow-weather owner in `MapSimulator.PacketOwnedFieldUtilityParity.cs` still clears and re-registers the New Year and firecracker transient-layer family for item `4300000` and `5680024`, so `CField::BlowWeather -> CMapLoadable::TransientLayer_*` continues to feed the same shared seam instead of leaving those event owners as simulator-only exercises. The richer follow seam also no longer stays manual-only on the gameplay side: `MapSimulator.UpdateLoop.cs` now calls `SyncPacketOwnedAnimationDisplayerFollow()`, so the existing packet-owned local-follow owner re-registers and clears the shared follow animation automatically from `_localFollowRuntime.AttachedDriverId` instead of leaving that richer follow seam dormant outside `/socialanim follow`. Focused regression coverage is now on disk in `UnitTest_MapSimulator/AnimationDisplayerParityTests.cs` for the fresh-random follow spawn-angle branch, the generation-point and theta-driven angle helpers, and the recovered follow emission-box defaults. Remaining gap: the follow runtime is still not byte-for-byte with the client, because the exact authored `szOffset0`/`szOffset1` random ranges for the active follow families are still not being decoded from the client’s real item-effect info branch, the COM-layer `LoadLayer` flags and repeat-layer registration lifecycle still stay approximated through simulator-owned follow particles rather than the full client layer graph, and the alpha fade choreography still does not mirror the client’s `IWzGr2DLayer::Getalpha` / `RelMove` path. Generic temporary-stat user-state parity also still uses the single `OnUserEff.img/character` marker instead of stat-specific start/repeat/end assets recovered from the client. | If this manager stays implicit, parity work can overstate progress in field feedback, relationship-overlay, and local-overlay rows while still missing the shared animation owner that actually registers, updates, and resets several visible social/event effect families. Tracking it explicitly keeps future work anchored to the same `CAnimationDisplayer` seam instead of scattering follow, New Year, firecracker, quest-delivery repeat effects, and fade-cleanup behavior across unrelated backlog buckets. | shared animation-owner layer (`CAnimationDisplayer::RegisterUserStateAnimation`, `CAnimationDisplayer::RegisterNewYearAnimation`, `CAnimationDisplayer::RegisterFireCrackerAnimation`, `CAnimationDisplayer::RegisterFollowAnimation`, `CAnimationDisplayer::FOLLOWINFO::Update`, `CAnimationDisplayer::ResetLocalFadeLayer`, `CAnimationDisplayer::Effect_QuestDeliveryItemUse`, `CUser::OnEffect`, `CItemEffectManager::LoadItemEffect`, `HaCreator/MapSimulator/Animation/AnimationEffects.cs`, `HaCreator/MapSimulator/MapSimulator.PacketOwnedLocalOverlayParity.cs`, `HaCreator/MapSimulator/MapSimulator.PacketOwnedFieldUtilityParity.cs`, `HaCreator/MapSimulator/MapSimulator.PacketOwnedUtilityParity.cs`, `HaCreator/MapSimulator/MapSimulator.AnimationDisplayerParity.cs`, `HaCreator/MapSimulator/MapSimulator.UpdateLoop.cs`, `HaCreator/MapSimulator/Interaction/AnimationDisplayerQuestDeliveryStringPoolText.cs`, `UnitTest_MapSimulator/AnimationDisplayerParityTests.cs`) |

### 7. Packet-authored field utility parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field utility, quickslot-init, and foothold-info packet parity | `HaCreator/MapSimulator` keeps the dedicated `PacketFieldUtilityRuntime` plus `MapSimulator.PacketOwnedFieldUtilityParity.cs` owner seam for `CField::OnPacket` field utilities: `OnBlowWeather` (`158`) drives packet-authored weather/message overrides, `OnAdminResult` (`160`) stays on the client-owned chat/minimap path (including subtype-`41` minimap-hide persistence), `OnQuiz` (`161`) resolves `Etc/OXQuiz.img` question/answer text and renders through `UI/StatusBar2.img/mainBar/notice` with the client-shaped short notice lifetime and question-side 30-second timer ownership, `OnStalkResult` (`172`) updates stalk/follow markers, `CQuickslotKeyMappedMan::OnInit` (`175`) hydrates packet DIK-style key codes into both quickslot labels and live `PlayerInput` bindings, and now also mirrors the client’s redraw/save side effects back into the simulator-owned keybinding surfaces by refreshing `UIWindowManager`’s live window-key map and persisting the updated simulator binding snapshot through the existing func-key config owner seam, including routing the simulator helper reset path back through that same redraw/save surface. `OnFootHoldInfo` (`176`) / `OnRequestFootHoldInfo` (`177`) still keep packet decode/build helpers plus a client-shaped foothold response payload builder (`(state, curX, curY, reverseV, reverseH)`), and the broader packet-owner transport pattern now holds there all the way through deferred bridge delivery too: opcode-`270` foothold replies first try the live official-session bridge, then fall back to the generic local-utility outbox, then queue deferred bridge/outbox delivery when direct transport is unavailable, with deferred bridge queueing now gated by the same configured bridge-owner seam used elsewhere instead of requiring the bridge to already be live. This pass keeps the earlier direct client extraction for admin-result wording (`StringPool::ms_aString` ids `0xA4`, `0xA6`, `0xA9`, `0xAA`, `0xAC`, `0xAD`, `0xB0`, `0xB1`, `0xBDF`, and `0xBE0`) and the runtime-first foothold reply rule: once `DynamicFootholdSystem` has any current platforms, `BuildPacketOwnedFootholdSnapshot` emits live runtime entries plus WZ-authored default placeholders for remaining authored contracts, and only falls back to cached packet entries when the runtime has no platforms at all. That keeps `/fieldutility status` and live-session opcode-`270` injection aligned with active simulator foothold positions, states, and reverse flags instead of replaying stale packet movement or names. The authored-name path remains WZ-backed and slightly better evidenced here too. `DynamicFootholdField` still keeps the existing WZ-first canonical name resolution (`name` / `objName` / `tag`, then `oS/l0/l1/l2`) plus the broader inbound alias set for layer/object coordinate keys and `piece`-tagged coordinate variants, while packet-owned snapshot names continue to prefer the WZ object contract key itself for outbound foothold replies. In the extracted dynamic-foothold contracts rechecked here, `Map/Map0/003000000` object `0/obj/0` authors `oS=acc9`, `l0=cannonTuto`, `l1=ship`, `l2=8`, `x=2323`, `y=-126`, `piece=0`; `Map/Map0/003000100` object `4/obj/10` authors `oS=connect`, `l0=rope`, `l1=49`, `l2=0`, `x=708`, `y=-78`, `piece=10`; and `Map/Map0/003000200` object `2/obj/14` authors `oS=guide`, `l0=tutorial`, `l1=key`, `l2=0`, `x=-1114`, `y=122`, `piece=14`, with `tags=byeMonkey` present only as extra metadata and no authored `name`, `objName`, or singular `tag` override recovered on those dynamic objects. That keeps opcode-`270` snapshot names on the verified `oS/l0/l1/l2[/piece]/x,y` form, still omits the synthetic `/piece/0` segment for default-zero pieces, and still keeps nonzero `piece` tags in outbound names. `ApplyPacketOwnedFootholdInfo` still clears and rebuilds the serial-to-name cache on each packet update so subsequent official-session replies do not preserve stale authored names from older foothold packets when the runtime cannot supply a current platform snapshot. Focused coverage is now actually on disk in `UnitTest_MapSimulator/PacketFieldUtilityParityTests.cs` for admin-result subtype `41`, quickslot-init default/custom decode, zero-piece snapshot-name shaping, nonzero-piece snapshot-name shaping, foothold-info payload round-tripping of the verified nonzero-piece authored name, official-session foothold response tuple encoding, and the direct-send / fallback-send / deferred-bridge response dispatch shaping. Persistent minimap open/closed state is intentionally no longer treated as part of this row: the targeted `CWvsContext::OnPacket` re-check shows that `OnMiniMapOnOff` belongs to backlog 8's explicit minimap owner row because it writes `CWvsContext` state and calls `CUIMiniMap::ReloadMiniMap`, while this field-owner row keeps only the `OnAdminResult` subtype-`41` hide pulse. Remaining gap: outbound foothold response injection is wired through the same live/deferred transport surfaces as the other local-utility owners, but it is still not proven end-to-end against a captured official session in this workspace, and the broader extracted dynamic-foothold contract set outside the rechecked tutorial-family maps still has not been exhaustively re-verified for rarer client-owned outbound name separators or authored override cases. | If these handlers stay collapsed into a generic packet assumption, parity work will miss several visible client-owned flows that are neither ordinary UI windows nor general field notices: packet weather and admin-result prompts, quiz ownership, stalk-result feedback, quickslot-init sync, and foothold-data round trips all have their own dispatcher slots in the field owner and should not disappear behind unrelated rows. Splitting RPS out keeps this bucket focused on true field-owner utilities instead of mixing dialog-minigame ownership back into the generic packet section, separating persistent minimap owner reload state into backlog 8 avoids tracking the same minimap behavior under both field and utility owners, and separating `CQuickslotKeyMappedMan` from backlog 8's `CFuncKeyMappedMan` row keeps the two keymap owners from being mistaken for duplicate work. | packet-owned field utility layer (`CField::OnPacket`, `CField::OnBlowWeather`, `CField::OnAdminResult`, `CField::OnQuiz`, `CField::OnStalkResult`, `CQuickslotKeyMappedMan::OnInit`, `CField::OnFootHoldInfo`, `CField::OnRequestFootHoldInfo`, `UnitTest_MapSimulator/PacketFieldUtilityParityTests.cs`) |

### 8. Stage-transition and map-load presentation parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned stage-transition and map-load presentation parity | `PacketStageTransitionRuntime` remains the dedicated `/stagepacket` seam for packet types `141` to `146`, the login/session relay, packet-owned back-background fades, ITC/Cash Shop stage opens, named map-object visibility, and the official `CStage::OnSetField` non-character-data path. `CStage::OnSetField` character-data handling is deeper again inside that same seam instead of leaving the `CharacterData::Decode` graph opaque once the stat head, money, slot bytes, and inventories are decoded: the direct snapshot path now keeps the client-owned skill-record (`dbcharFlag & 0x100`), skill-expiration (`dbcharFlag & 0x200`), and skill-cooltime (`dbcharFlag & 0x4000`) sections distinct in client order; still preserves the unresolved `0x8000` span as opaque instead of reclassifying it as decoded cooldown data; recovers the later pre-map-transfer quest-string (`0x10000`) and short-filetime (`0x20000`) groups in that same client order; and then follows the fixed client tail order through mini-game (`0x400`), relationship (`0x800`), map-transfer (`0x1000`), quest-ex (`0x80000`), Wild Hunter (`0x200000` for job `33xx`), quest-complete (`0x400000`), and visitor-quest (`0x800000`) records instead of treating those post-map-transfer groups as count-only approximations. The two-int `0x100000` pre-inventory header is still semantically unresolved, but the stage-transition snapshot now preserves both raw values in the same pre-inventory position the client decodes. The tail boundary is tighter too: candidate selection now prefers the exact `CharacterData::Decode` endpoint or the client 12-byte logout-gift cache boundary when `CWvsContext::OnSetLogoutGiftConfig` follows in the same `SetField`, and `MapSimulator.MapTransferPacketParity.cs` now hydrates authoritative `adwMapTransfer` / `adwMapTransferEx` directly from the decoded snapshot when those arrays were recovered there instead of forcing every packet back through the trailing-tail bootstrap search. `MapTransferAuthoritativeBootstrapDecoder` still exists as the fallback seam, but its known-leading finder now follows the client-authored order through skill, skill-expiration, skill-cooltime, quest-string, short-filetime, mini-game, and relationship groups before the bootstrap books instead of guessing optional mini-game / relationship layouts. Focused source coverage is now on disk in `UnitTest_MapSimulator/PacketStageTransitionSetFieldCharacterDataParityTests.cs` for the richer `SetField` decode, including the separated skill / skill-expiration / skill-cooltime sections, the opaque `0x8000` span, the recovered `0x10000` quest-string and `0x20000` short-filetime branches ahead of map transfer, the later quest-ex / Wild Hunter / quest-complete / visitor-quest sections, the preserved `0x100000` header values, the logout-gift boundary, and the corrected bootstrap offset search through that same client-authored pre-map-transfer order. Remaining gap: the simulator still does not model the full byte-exact `CharacterData::Decode` section graph end to end, so the still-unmirrored `dbcharFlag & 0x8000` section, deeper skill-record semantics beyond the currently recovered raw level maps/counts, the exact identity of the `0x100000` two-int pre-inventory header, the full object semantics of `GW_WildHunterInfo` and `GW_NewYearCardRecord`, and any rarer trailing caches beyond the now-preserved logout-gift boundary still remain approximated rather than fully client-authored. | If these owners stay implicit, future parity work can keep overloading login flow, cash-shop stage work, or generic field-effect rows while still missing the actual packet dispatch seam that swaps stages and mutates map presentation during load/runtime. Tracking them explicitly keeps stage changes, packet-owned back effects, and map-object visibility toggles anchored to the client owners that `CField::OnPacket` actually calls. | packet-owned stage/map-load layer (`CField::OnPacket`, `CStage::OnPacket`, `CStage::OnSetField`, `CharacterData::Decode`, `GW_CharacterStat::DecodeMoney`, `CStage::OnSetITC`, `CStage::OnSetCashShop`, `CMapLoadable::OnPacket`, `CMapLoadable::OnSetBackEffect`, `CMapLoadable::OnSetMapObjectVisible`, `CMapLoadable::OnClearBackEffect`, `UnitTest_MapSimulator/PacketStageTransitionSetFieldCharacterDataParityTests.cs`) |

### 9. Context-owned stage-period transition parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Context-owned stage-period change parity | The simulator still keeps this owner on its dedicated `MapSimulator.ContextOwnedStagePeriodParity.cs` seam instead of burying it under `/stagepacket`, and this pass tightens the local cache again toward the client `CStageSystem::BuildCacheData` shape. `ContextOwnedStagePeriodRuntime` stays wired into the live update loop and `/stageperiod`, validation still builds a concrete stage-period catalog from `Etc/StageSystem.img`, and that catalog still captures each discovered period's `backColor`, `aStageBackImg`, keyword list, and enabled quest ids with affected-map augmentation from `Etc/StageAffectedMap.img` plus extra aliases from `Etc/StageKeyword.img` when those client files are present. The new parity work narrows three client-backed gaps: stage-keyword augmentation now honors per-period numeric nodes when `StageKeyword` authors them instead of spraying every extra keyword across every mode of the same theme, affected-map augmentation now only treats `StageAffectedMap` rows with a `questID` gate as active when the current period's enabled-quest cache actually contains that quest id, and the live back-color / `aStageBackImg` cache now mirrors `CStageSystem::BuildCacheData`'s `nStagePeriod > 0` rule so mode `0` or other non-positive periods can still refresh keyword and quest caches without incorrectly driving visual back-layer overrides. Successful `CWvsContext::OnStageChange` payloads still update simulator-owned keyword, quest, back-color, back-image, and affected-map caches before scheduling the field-owned `CStage::FadeOut` -> `CMapLoadable::ReloadBack` -> `CStage::FadeIn` handoff, `ReloadContextOwnedStagePeriodBackLayers` still prefers stage-system `aStageBackImg` descriptors over authored map back layers when the current map is inside the active affected-map set, and the world clear color still follows the active stage `backColor` instead of staying pinned to black during those period swaps. Focused regression coverage is now on disk in `UnitTest_MapSimulator/ContextOwnedStagePeriodParityTests.cs` for payload round-trip, trailing-byte rejection, mode-`0` visual suppression with keyword/quest retention, per-period `StageKeyword` augmentation, quest-gated affected-map augmentation, stage back image metadata extraction, and back-color decoding. Remaining gap: the simulator still builds this catalog from extracted WZ property names rather than replaying the client's original `CStageSystem` load/build object allocation path byte-for-byte, the `StageKeyword` / `StageAffectedMap` augmentation still does not reproduce every client-side cache mutation or `questState` / timer gate from the native owner, stage back image application still routes through `BackgroundInfo` / `MapSimulatorLoader` instead of the client's native `CMapLoadable::MakeBack` object creation sequence, and the fade path still approximates the underlying Gr2D red-tone / green-blue-tone choreography instead of owning the exact `CStage::FadeOut` / `CStage::FadeIn` object chain. | If this owner stays untracked, stage-transition parity can look complete once `CField::OnPacket` and `CStage::OnSetField` are covered even though the client still has a separate context-owned path for live stage-period swaps and background reloads. Naming it here keeps future work anchored to the real owner instead of hiding it inside generic set-field or field-effect notes. | context-owned stage-transition layer (`CWvsContext::OnStageChange`, `CStageSystem::SetStagePeriod`, `CStage::FadeOut`, `CMapLoadable::ReloadBack`, `CStage::FadeIn`, `HaCreator/MapSimulator/Interaction/ContextOwnedStagePeriodRuntime.cs`, `HaCreator/MapSimulator/Interaction/ContextOwnedStagePeriodCatalog.cs`, `HaCreator/MapSimulator/MapSimulator.ContextOwnedStagePeriodParity.cs`, `UnitTest_MapSimulator/ContextOwnedStagePeriodParityTests.cs`) |

### 10. Initial-quiz utility owner parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Context-owned Initial Quiz owner parity | The simulator now moves this owner family further past the old side-preview/runtime-only seam. Using WZ evidence from `UIWindow(.img|2.img)/InitialQuiz` plus the targeted IDA results for `CWvsContext::OnInitialQuiz` (`0x9ffad0`) and `CUIInitialQuiz::{OnCreate,Draw,Update}` (`0x78e720`, `0x78e9a0`, `0x79c390`), packet-authored mode-`0` quiz payloads no longer open the NPC overlay variant at all: `PacketScriptMessageRuntime` keeps the packet bridge and prompt context alive while suppressing overlay mutation for the client-owned quiz branch, outbound opcode-`65` replies for message type `6` follow the client owner path by encoding the entered Maple string instead of the older simulator numeric helper payload, and `MapSimulator.InitialQuizParity.cs` renders a centered `CUIInitialQuiz`-style owner that uses the WZ-backed layered background, button states, animated `ani` frames, and the correct `InitialQuiz`-specific digit strips from `UIWindow2.img`. This pass also tightens the owner lifetime against the `CWvsContext` seam instead of leaving it as a process-global timer bucket: `InitialQuizTimerRuntime` now binds the live quiz owner to the current local character id, carries that binding through both packet and packet-script client-owner opens, reports the bound/runtime character state in status output, and clears the owner plus edit state when the runtime character changes underneath it so stale quiz state does not leak across local-context swaps. Focused regression coverage is now on disk in `UnitTest_MapSimulator/InitialQuizOwnerParityTests.cs` for runtime-character binding, context-reset behavior, and the mode-`0`/mode-`1` client payload path. Remaining gap: the simulator still does not recreate the client `CWndMan` focus/capture plumbing or IME/text-edit control byte-for-byte, the centered owner still uses simulator-side text helpers instead of the client's exact `IWzCanvas::DrawTextA` calls and every recovered text or digit coordinate from `CUIInitialQuiz::Draw`, and the broader packet-script owner-preview seam still exists in parallel code even though this Initial Quiz branch now routes through the dedicated utility owner runtime. | If this owner stays folded into the general script-message row, quiz parity looks closer than it is because packet decode and a WZ-backed preview already exist. Naming the dedicated `CUIInitialQuiz` owner keeps future work anchored to the real client window lifecycle instead of stopping at packet sync, timer state, and side-preview rendering. | context-owned quiz utility owner (`CWvsContext::OnInitialQuiz`, `CUIInitialQuiz::OnCreate`, `CUIInitialQuiz::Draw`, `CUIInitialQuiz::Update`, `HaCreator/MapSimulator/Interaction/InitialQuizTimerRuntime.cs`, `HaCreator/MapSimulator/Interaction/PacketScriptMessageRuntime.cs`, `HaCreator/MapSimulator/MapSimulator.InitialQuizParity.cs`, `HaCreator/MapSimulator/MapSimulator.PacketScriptOwnerVisualParity.cs`, `UnitTest_MapSimulator/InitialQuizOwnerParityTests.cs`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.
6. Reactor loader pass:
   Split reactor state-layer rebuild and hit-prop ownership out from the existing packet-runtime row before more reactor work gets documented only as packet timing or collision polish.
7. Scripted field-state pass:
   Add the packet-owned bridge for field help messages, quest timers, quest-clear resets, and object-state flips before layering more one-off script behavior onto NPC or reactor seams.
8. Packet-feedback pass:
   Add the packet-owned field messaging, warn-dialog, forced-fade, obstacle, field-effect, and boss-timer surface before more status-bar or field-overlay polish is tracked as isolated UI work.
9. Field-utility packet pass:
   Add the packet-owned weather/admin-result/quiz/stalk-result, quickslot-init, and foothold-info surface before those handlers get misfiled under unrelated UI or field rows.
10. Stage/map-load packet pass:
   Add the packet-owned `CStage` and `CMapLoadable` bridge for field or Cash Shop or ITC stage switches plus back-effect/object-visibility mutations before those transitions get scattered across login, field-effect, or cash-stage rows.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
