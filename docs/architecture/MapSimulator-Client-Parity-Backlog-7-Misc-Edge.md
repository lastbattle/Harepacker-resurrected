# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Animation/AnimationEffects.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CReactorPool::OnPacket` at `0x6cf9b0`
- `CReactorPool::OnReactorEnterField` at `0x6cf490`
- `CReactorPool::OnReactorLeaveField` at `0x6ccea0`
- `CReactorPool::OnReactorMove` at `0x6cd110`
- `CReactorPool::OnReactorChangeState` at `0x6ccd60`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUserLocal::OnSetDirectionMode` at `0x9054f0`
- `CUserLocal::OnSetStandAloneMode` at `0x905550`
- `CActionMan::GetNpcImgEntry` at `0x41a840`
- `CActionMan::LoadNpcAction` at `0x420ae0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CField::OnFieldSpecificData` at `0x52a7e0`
- `CField::OnSetQuestTime` at `0x52b790`
- `CField::OnSetQuestClear` at `0x52c870`
- `CField::OnDesc` at `0x5313d0`
- `CField::OnSetObjectState` at `0x539890`
- `CField::OnTransferFieldReqIgnored` at `0x52f3b0`
- `CField::OnTransferChannelReqIgnored` at `0x52f5f0`
- `CField::OnGroupMessage` at `0x535490`
- `CField::OnCoupleMessage` at `0x5357f0`
- `CField::OnFieldObstacleOnOff` at `0x535a80`
- `CField::OnFieldObstacleOnOffStatus` at `0x535b00`
- `CField::OnFieldEffect` at `0x53b790`
- `CField::OnBlowWeather` at `0x5468f0`
- `CField::OnAdminResult` at `0x53bc20`
- `CField::OnQuiz` at `0x537a90`
- `CField::OnWarnMessage` at `0x538160`
- `CField::OnPlayJukeBox` at `0x537940`
- `CField::OnWhisper` at `0x5448a0`
- `CField::OnSummonItemInavailable` at `0x52f7b0`
- `CField::OnStalkResult` at `0x539910`
- `CField::OnDestroyClock` at `0x52a7c0`
- `CQuickslotKeyMappedMan::OnInit` at `0x6c6130`
- `CField::OnFootHoldInfo` at `0x53a810`
- `CField::OnRequestFootHoldInfo` at `0x52ddd0`
- `CField::OnZakumTimer` at `0x530cc0`
- `CField::OnHontailTimer` at `0x530e70`
- `CField::OnChaosZakumTimer` at `0x531020`
- `CField::OnHontaleTimer` at `0x5311d0`
- `CField::OnPacket` at `0x546d50`
- `CStage::OnPacket` at `0x71b0b0`
- `CStage::OnSetField` at `0x71b150`
- `CStage::OnSetITC` at `0x71c000`
- `CStage::OnSetCashShop` at `0x71bfa0`
- `CMapLoadable::OnPacket` at `0x61fd80`
- `CMapLoadable::OnSetBackEffect` at `0x620160`
- `CMapLoadable::OnSetMapObjectVisible` at `0x61fe40`
- `CMapLoadable::OnClearBackEffect` at `0x620150`
- `CUIInitialQuiz::OnCreate` at `0x78e720`
- `CUIInitialQuiz::Draw` at `0x78e9a0`
- `CUIInitialQuiz::Update` at `0x79c390`
- `CScriptMan::OnPacket` at `0x6de360`
- `CWvsContext::OnInitialQuiz` at `0x9ffad0`
- `CUserLocal::OnFieldFadeOutForce` at `0x9057f0`
- `CAnimationDisplayer::RegisterUserStateAnimation` at `0x444d30`
- `CAnimationDisplayer::RegisterNewYearAnimation` at `0x4556f0`
- `CAnimationDisplayer::RegisterFireCrackerAnimation` at `0x45a6e0`
- `CAnimationDisplayer::RegisterFollowAnimation` at `0x45ac50`
- `CAnimationDisplayer::ResetLocalFadeLayer` at `0x43b6f0`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Interaction and NPC parity

- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CActionMan::LoadNpcAction` at `0x420ae0`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CReactorPool::OnPacket` at `0x6cf9b0`
- `CReactorPool::OnReactorEnterField` at `0x6cf490`
- `CReactorPool::OnReactorLeaveField` at `0x6ccea0`
- `CReactorPool::OnReactorMove` at `0x6cd110`
- `CReactorPool::OnReactorChangeState` at `0x6ccd60`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUser::PetAutoSpeaking` at `0x8deb10`

Notes:
`CUserLocal::TalkToNpc` is the first concrete client seam for future NPC bridge work. `CUserLocal::Update` also shows client-side quest-alert refresh calls, emotion reset / balloon lifetime upkeep, timer refresh, timed direction-mode release, and status-bar chat logging, while `CUserLocal::TryAutoRequestFollowCharacter` covers automatic escort/follow requests and `CUser::PetAutoSpeaking` covers one of the idle-feedback surfaces tied to those broader interaction loops. A follow-up IDA scan also showed that reactor ownership is split across both local collision and a packet-owned pool: `CReactorPool::OnPacket` dispatches reactor change-state (`334`), move (`335`), enter-field (`336`), and leave-field (`337`) traffic into dedicated handlers instead of leaving all reactor lifecycle behavior on `CUserLocal::CheckReactor_Collision`.

### 2. Reactor layer-build and hit-prop ownership discovered by targeted `CReactorPool` decompile

- `CReactorPool::LoadReactorLayer` at `0x6ce630`

Notes:
The latest targeted decompile exposes a distinct reactor owner that should not stay buried inside the broader packet-lifecycle row. `CReactorPool::LoadReactorLayer` lazy-resolves `CReactorTemplate`, looks up the current state branch by stringified state id, builds the visible state layer through `CAnimationDisplayer::LoadLayer`, switches between repeat and stop playback from `CReactorTemplate::STATEINFO::m_bRepeat`, preserves timed movement against `tStateEnd` for moving states, auto-selects hit reactions through `get_hittype_priority_level` plus `CReactorTemplate::GetHitProp` when no proper event index has been fixed yet, formats and plays the per-state reactor hit sound through the StringPool template at id `2121`, and then assigns the final layer depth from the closest foothold page or z-mass plus the reactor template layer mode. That makes reactor parity split the same way as the other action-loader families already called out elsewhere in the backlog set: `CReactorPool::OnPacket` owns lifecycle and timing, while `LoadReactorLayer` owns state-layer rebuild, hit-prop arbitration, sound, and final z placement.

### 3. NPC action-loader and client-action-set parity discovered by targeted `CActionMan` scan

- `CActionMan::LoadNpcAction` at `0x420ae0`

Notes:
The targeted `CActionMan` lookup exposed another NPC-side owner that was still only implied by broader talk or actor rows. `CActionMan::LoadNpcAction` caches by NPC template, action, and client action-set index; it also has a special `nClientActionIdx == -2` branch that calls `CNpcTemplate::CalcClientActionSetIdx` before choosing the action family, applies the client default frame delay of `180` when delay data is absent, and can append reversed frames when the authored data marks that path. That means NPC parity is still missing a dedicated action-loader seam for client action-set selection and frame-table construction instead of only needing more dialogue or field-placement polish.

### 4. NPC image-entry ownership discovered by targeted `CActionMan` scan

- `CActionMan::GetNpcImgEntry` at `0x41a840`

Notes:
The same `CActionMan` pass shows the client keeps another NPC-side owner below `LoadNpcAction`: template-image resolution itself. `GetNpcImgEntry` caches by NPC template id, resolves the formatted NPC resource path through the resource manager, checks the template `link` field, recursively reopens linked NPC templates, and copies any missing top-level branches from the linked image onto the cached property tree before client-action-set selection runs. That means NPC parity is not only talk flow plus action loading; it also depends on the earlier image-entry seam that decides which normalized NPC IMG entry the action-set loader sees.

### 5. Packet-authored direction and stand-alone mode parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnSetDirectionMode` at `0x9054f0`
- `CUserLocal::OnSetStandAloneMode` at `0x905550`

Notes:
The existing backlog tracks delayed `TryLeaveDirectionMode` release, but the direct `CUserLocal::OnPacket` re-check exposed the client-owned state-entry seam that actually drives those releases. `OnSetDirectionMode` decodes an enable byte plus a delay, clears `m_bAfterLeaveDirectionMode`, enters direction mode immediately when the packet enables it or when the delay is non-positive, and otherwise arms a delayed leave by storing `m_tAfterLeaveDirectionMode = delay + get_update_time()`. `OnSetStandAloneMode` is a separate packet-owned toggle that writes the decoded byte into the `CWvsContext` stand-alone flag rather than behaving like another window-local direction-mode alias. That owner pair is not named anywhere else in the current backlog set.

### 6. Field-state and script-message parity

- `CField::OnPacket` at `0x546d50`
- `CField::OnFieldSpecificData` at `0x52a7e0`
- `CField::OnDesc` at `0x5313d0`
- `CField::OnSetQuestClear` at `0x52c870`
- `CField::OnSetQuestTime` at `0x52b790`
- `CField::OnSetObjectState` at `0x539890`
- `CScriptMan::OnPacket` at `0x6de360`

Notes:
This packet cluster is a distinct client owner that the current backlog did not call out directly. `CField::OnPacket` dispatches field-specific data, help-message dialogs, quest-timer refresh and clear, and object-state flips from one place, while `CScriptMan::OnPacket` owns script-message delivery separately from NPC click handling or reactor collision. The dedicated quest-timer UI owner is tracked separately in the progression/utility backlog so this row stays focused on the packet source and field/script runtime.

### 7. Packet-authored field messaging and feedback parity

- `CField::OnTransferFieldReqIgnored` at `0x52f3b0`
- `CField::OnTransferChannelReqIgnored` at `0x52f5f0`
- `CField::OnGroupMessage` at `0x535490`
- `CField::OnWhisper` at `0x5448a0`
- `CField::OnCoupleMessage` at `0x5357f0`
- `CField::OnSummonItemInavailable` at `0x52f7b0`
- `CField::OnFieldEffect` at `0x53b790`
- `CField::OnFieldObstacleOnOff` at `0x535a80`
- `CField::OnFieldObstacleOnOffStatus` at `0x535b00`
- `CField::OnFieldObstacleAllReset` at `0x52c830`
- `CField::OnPlayJukeBox` at `0x537940`
- `CField::OnWarnMessage` at `0x538160`
- `CField::OnDestroyClock` at `0x52a7c0`
- `CField::OnZakumTimer` at `0x530cc0`
- `CField::OnHontailTimer` at `0x530e70`
- `CField::OnChaosZakumTimer` at `0x531020`
- `CField::OnHontaleTimer` at `0x5311d0`
- `CUserLocal::OnFieldFadeOutForce` at `0x9057f0`

Notes:
This is another packet-owned surface that the current backlog set does not describe explicitly. IDA shows `CField::OnPacket` and `CUserLocal::OnPacket` owning a broad feedback layer that sits between ordinary gameplay and the existing UI rows: group, whisper, and couple message routing feed status-bar chat and whisper-candidate state; field-effect packets drive screen effects, field sounds, BGM changes, boss HP tags, reward-roulette visuals, and object-state changes; obstacle, warning, and forced-fade packets update map presentation directly; and dedicated boss-timer plus transfer-ignored handlers show that several visible notice/timer flows are owned by packets rather than generic field-rule polling.

### 8. `CAnimationDisplayer` social/event animation ownership discovered by function scan

- `CAnimationDisplayer::RegisterUserStateAnimation` at `0x444d30`
- `CAnimationDisplayer::RegisterNewYearAnimation` at `0x4556f0`
- `CAnimationDisplayer::RegisterFireCrackerAnimation` at `0x45a6e0`
- `CAnimationDisplayer::RegisterFollowAnimation` at `0x45ac50`
- `CAnimationDisplayer::ResetLocalFadeLayer` at `0x43b6f0`

Notes:
The function scan surfaced another client-owned surface that the backlog was not naming anywhere else. These registrations show that the client keeps several visible social/event overlays under `CAnimationDisplayer` ownership rather than scattering them only across field-message handlers or one-shot item-effect helpers: follow effects, New Year card midpoint effects, firecracker bursts, avatar-adjacent user-state layers, and local fade cleanup all pass through one animation manager. A focused source pass in `HaCreator/MapSimulator` shows partial simulator coverage through `Animation/AnimationEffects.cs` for follow animations plus packet-owned fade handling, but not a single backlog row that treats this manager as its own parity seam.

### 9. Packet-authored field utility parity discovered by direct `CField::OnPacket` scan

- `CField::OnBlowWeather` at `0x5468f0`
- `CField::OnAdminResult` at `0x53bc20`
- `CField::OnQuiz` at `0x537a90`
- `CField::OnStalkResult` at `0x539910`
- `CQuickslotKeyMappedMan::OnInit` at `0x6c6130`
- `CField::OnFootHoldInfo` at `0x53a810`
- `CField::OnRequestFootHoldInfo` at `0x52ddd0`

Notes:
`HaCreator/MapSimulator` keeps this row on the dedicated `PacketFieldUtilityRuntime` plus `MapSimulator.PacketOwnedFieldUtilityParity.cs` owner seam for `CField::OnPacket` field utilities. `OnBlowWeather` (`158`) drives packet-authored weather and message overrides; `OnAdminResult` (`160`) stays on the client-owned chat/minimap path, including subtype `41` as the no-payload minimap hide branch confirmed by direct IDA (`CUIMiniMap::SetShowMiniMap(0)` then `ToggleMap()`); `OnQuiz` (`161`) resolves `Etc/OXQuiz.img` question and answer text, uses the `UI/StatusBar2.img/mainBar/notice` asset, keeps the client `CUIStatusBar::SetProblem` notice lifetime of `0x1388` (`5000`) ms, and leaves the `30` second event timer on the question-side owner only; `OnStalkResult` (`172`) updates stalk/follow markers; `CQuickslotKeyMappedMan::OnInit` (`175`) follows the client `Decode1` plus `DecodeBuffer(..., 0x20)` shape so the simulator hydrates eight DIK-style quickslot codes into both labels and live `PlayerInput` bindings; and `OnFootHoldInfo` (`176`) / `OnRequestFootHoldInfo` (`177`) keep packet decode/build helpers plus the client-shaped opcode-`270` outbound payload builder `(state, curX, curY, reverseV, reverseH)` with no count or name text in the reply body. The dynamic-foothold seam still prefers live `DynamicFootholdSystem` state over stale cached packet rows, and the packet-owned snapshot name helper keeps the WZ-authored `oS/l0/l1/l2` object contract while omitting the synthetic zero-piece segment and preserving nonzero `piece` values. Focused regression coverage is now actually on disk in `UnitTest_MapSimulator/PacketFieldUtilityParityTests.cs` for admin-result subtype `41`, quickslot init default/custom decode, foothold-info round-trip decode of the verified nonzero-piece authored name, opcode-`270` payload tuple encoding, direct-send/fallback-send/deferred-bridge dispatch shaping, and zero-piece/nonzero-piece snapshot naming. A targeted WZ re-check of the extracted tutorial-family dynamic contracts in `Map/Map0/003000000`, `003000100`, and `003000200` still does not surface authored `name`, `objName`, or singular `tag` overrides on those dynamic objects; the outbound foothold snapshot name stays on the WZ-backed `oS/l0/l1/l2[/piece]/x,y` form there, with plural `tags` remaining extra metadata only. Remaining gap: the outbound foothold-response injection is still not proven end-to-end against a captured official live session in this workspace, and the broader extracted dynamic-foothold map set outside the rechecked tutorial-family contracts still has not been exhaustively re-verified for rarer client-owned outbound name separators or authored override cases.

### 10. Stage-transition and map-load presentation parity discovered by direct `CField::OnPacket` decompile

- `CStage::OnPacket` at `0x71b0b0`
- `CStage::OnSetField` at `0x71b150`
- `CStage::OnSetITC` at `0x71c000`
- `CStage::OnSetCashShop` at `0x71bfa0`
- `CMapLoadable::OnPacket` at `0x61fd80`
- `CMapLoadable::OnSetBackEffect` at `0x620160`
- `CMapLoadable::OnSetMapObjectVisible` at `0x61fe40`
- `CMapLoadable::OnClearBackEffect` at `0x620150`

Notes:
The same `CField::OnPacket` decompile also hands packet families `141` to `146` to two owners that must stay explicit across backlog boundaries. `CStage::OnPacket` owns the stage-switch family for field, ITC, and Cash Shop transitions instead of leaving those jumps implied by login or UI code, while `CMapLoadable::OnPacket` owns packet-authored map back-effects, object-visibility toggles, and back-effect clears instead of treating them as generic field-effect fallout. That is a separate packet surface above the more specific field-feedback, field-state, and cash-stage rows; backlog 11 now links this same handoff from `CLogin::OnPacket` and keeps detailed implementation ownership merged here.

### 11. Passenger passive-move parity discovered by direct `CUserLocal::OnPacket` scan

- `CUser::OnPassiveMove` at `0x8dea10`

Notes:
The same `CUserLocal::OnPacket` re-check also exposed one missing follow-adjacent owner that should not stay buried under request and failure handling alone. `CUser::OnPassiveMove` only runs while `m_dwDriverID` is set, and then forwards the decoded movement straight into the passenger-side `CMovePath::OnMovePacket` seam on the attached vector controller. That makes it the client-owned movement handoff for attached passengers after the request and accept path has already succeeded, not just another follow-request dialog branch.

### 12. Context-owned stage-period transition parity discovered by targeted `CWvsContext::OnPacket` scan

- `CWvsContext::OnStageChange` at `0x9e5360`
- `CStageSystem::SetStagePeriod` at `0x71ec40`
- `CStage::FadeOut` at `0x7192b0`
- `CMapLoadable::ReloadBack` at `0x61f0c0`
- `CStage::FadeIn` at `0x718cd0`

Notes:
The targeted `CWvsContext::OnPacket` pass exposed one more stage-transition owner that should not stay implied by the `CField::OnPacket` / `CStage::OnSetField` rows alone. `CWvsContext::OnStageChange` decodes the target stage-period string plus mode byte, forwards them into `CStageSystem::SetStagePeriod`, and when the active stage is a field it immediately runs the client fade-out, `CMapLoadable::ReloadBack`, and fade-in chain on that live stage. That is a separate context-owned presentation handoff above the packet-owned field/stage dispatcher already tracked later in this document, not just another variant of `SetField`.

### 13. Initial-quiz utility owner parity discovered by targeted `CUIInitialQuiz` lookup

- `CUIInitialQuiz::OnCreate` at `0x78e720`
- `CUIInitialQuiz::Draw` at `0x78e9a0`
- `CUIInitialQuiz::Update` at `0x79c390`
- `CWvsContext::OnInitialQuiz` at `0x9ffad0`

Notes:
The broader script-message row already tracks packet decode, runtime sync, and the current WZ-backed owner preview, but a targeted IDA lookup shows there is still a separate client owner family underneath that packet bridge. `CWvsContext::OnInitialQuiz` is the context-owned entry point that opens or closes the quiz on client mode changes, while `CUIInitialQuiz::{OnCreate,Draw,Update}` prove the quiz is a dedicated utility owner with its own layout, timer/update cadence, and draw lifecycle rather than only an NPC-overlay sidecar. That owner is still absent from the backlog index today, so the current document overstates quiz parity if it stops at `CScriptMan::OnPacket` and the simulator preview seam alone.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Interaction and NPC parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | NPC talk flow | Nearby NPC clicks now open a real simulator dialogue overlay fed from loaded NPC string data including NPC `info/speak` idle lines, quest `Say.img` conversations preserve multi-page WZ ordering, root-level `yes`/`no` branches are now attached to the final numbered page, quest entries now open directly on the authored WZ conversation pages instead of always forcing a synthetic summary page first, common MapleStory script markup is stripped into readable text with additional `#q` including uppercase `#Q`, numeric `#s`, bare plural-suffix `#s`, `#m`, `#z`, `#v`, quest-item count `#c`, mob-name `#o`, quest-reference `#y`, quest-state `#u`, quest-record `#R`, and quest-selected `#M` or `#a` or `#x` token cleanup backed by loaded quest, item, mob, and stored quest-record data where possible, with bare active-quest `#u#` and `#R#` forms now also resolving through the same live quest-state and quest-record context, quest-detail `#j...#` tokens now also resolve quest-scoped stored-record key/value pairs when the active quest has a loaded record, standalone client prompt markers such as `#E`, `#I`, `#E#`, and `#I#` are now stripped from read-only simulator text without leaving stray literal `#` characters behind, lowercase prompt markers such as the authored `#e#` emphasis in `Quest/Say.img/10000/0/1` and `10005/0/0` now also strip cleanly instead of leaving a literal trailing `#`, bare `#j` job tokens now resolve through the same live simulator build context as `#j#`, quest reward-summary markers such as `#Wbasic#`, `#Wselect#`, and `#Wprob#` are now stripped from simulator-visible text instead of leaking raw WZ tags into NPC quest pages, inline `#L` menu options now surface as clickable read-only choices that honor WZ `stop/<page>/answer` metadata using the client-authored 1-based choice position, continue along the numbered conversation flow only for the authored correct answer, and still fall back to authored choice-position branch keys when a `stop/<page>/<branch>` node does not reuse the raw `#L` id, quest dialogue entries now also reuse named WZ `stop/*` and `lost` branches such as `item`, `mob`, `monster`, `quest`, `default`, `info`, and lost-item fallback text instead of flattening those failure states into generic simulator prose, and now also checks authored `job` branch keys when present before falling back to generic locked-quest prose, started quests now also surface WZ `stop/npc` guidance when you revisit the starter NPC instead of falling back to generic progress text, nested authored branch conversations reached through WZ `stop/*`, `yes`, `no`, or other branch nodes now reuse the same multi-page, final-page root-choice, and inline-selection parsing rules as top-level `Say.img` pages instead of dropping those branch-root choices, authored empty `yes` or `no` handoff branches now remain visible as clickable placeholder choices instead of disappearing when WZ leaves the branch body empty for later script execution, authored `stop` metadata no longer leaks into the overlay as a fake visible `Stop` button, authored `answer` bookkeeping and known non-dialogue metadata such as `illustration` no longer surface as fake visible branch choices, item-gated quest conversations now fall back to authored `lost` text whenever the specific `stop/item` branch is absent instead of only when every required item count has dropped to zero, branch-only authored pages now stay in the overlay as actual clickable choice pages instead of being dropped when their visible prose strips to empty, packet-owned quest-result modals now keep those same authored choice-only pages instead of silently filtering them back out after formatting, inline menu labels now keep their raw WZ text until final page formatting so the same live quest-item-count token context used by the page body also resolves inside the choice labels, metadata-only `Say.img` string nodes such as direct stage `info` markers no longer surface as fake visible dialogue pages, recursive numeric conversation containers now also contribute authored `stop/*` and `lost` metadata instead of dropping nested branch guidance when WZ stores the conversation under deeper `0/0/...` wrappers, this pass now also re-selects authored `Say.img` conversation variants when a stage stores multiple numeric children gated by direct WZ `npc`, `job`, or `quest` metadata with the same runtime selector reused by nearby-NPC quest pages and packet-owned quest-result modals instead of always sticking to the first pre-parsed page set, keeps variant-root `yes`/`no` choices attached even when the selected runtime variant is a metadata-only handoff node that publishes its visible numbered pages as later siblings, now also layers generic container-root `yes` or `no` and other distinct root branch choices onto those selected-variant sibling-page flows instead of dropping shared authored root choices whenever the chosen variant only overrides part of the root choice tree, now also layers runtime-selected variant-root and generic container-root `stop` metadata together so authored shared `stop/<page>/answer` continuation rules, numbered wrong-answer branches, and generic `default`, `info`, or `quest` paths remain reachable when the selected variant only overrides part of the root stop tree, and now also keeps the selected numeric variant page itself in the authored page sequence when WZ stores later continuation pages as plain siblings under the same container instead of only reusing sibling pages for metadata-only handoff variants, while still reusing variant-scoped `lost` metadata for issue-page resolution, and authored `#h0#` plus `#j` tokens in nearby-NPC quest pages and packet-owned quest-result modals now resolve from the active simulator build instead of falling back to generic prose, while nearby-NPC non-quest `Talk` pages now also reuse that live simulator formatting context so NPC `info` and `speak` lines can resolve authored player, job, current-map, and stored quest-record tokens instead of falling back to generic text, and this pass now preserves WZ-authored `stop/<page>/flip` metadata through parsed and formatted NPC interaction pages, including correct-answer continuations and authored empty-branch or wrong-answer simulator placeholders, so packet-owned quest-result util dialogs can mirror the speaker portrait and body text placement for flipped pages backed by `Quest/Say.img/10587/0/stop/0/flip` and related `stop/*/flip` rows plus the client `CScriptMan::OnSay` speaker-side assignment (`m_bSpeakerOnRight`), and now keeps WZ-authored uppercase literal room-name text such as `Quest/Say.img/9532/0/0`'s `#Guest Room 3#k` readable as `Guest Room 3` instead of treating the uppercase `#G` as a lowercase `#g` color tag and rendering `uest Room 3`. True script execution, broader automatic branch conditions beyond the current named WZ stop-branch mapping plus authored choice-position fallback and the direct `npc`/`job`/`quest` variant matcher, richer dynamic quest-script variable evaluation beyond the current WZ-backed token fallbacks and current quest-record key/value decoding, action or check-script conversation execution beyond these read-only WZ branches, additional variant-gating metadata beyond the currently parsed direct WZ `npc` or `job` or `quest` keys, rarer malformed script-markup typos beyond the currently handled literal hash-word cleanup, and full client conversation layouts are still missing | Core interaction now covers a closer read-only version of client conversation flow, including authored page-first quest conversations, nested WZ branch conversations that now keep their own final-page root choices instead of flattening to plain text, choice-only authored menu pages that remain navigable after formatting strips their visible `#L` markup, packet-owned quest-result modals that now reuse that same choice-page visibility rule instead of re-dropping those authored menu pages, multi-step selection menus that respect WZ-authored answer metadata and its 1-based authored choice ordering instead of advancing on every missing branch or comparing against the raw `#L` id, additional WZ-authored wrong-answer branches whose `stop/<page>/<branch>` keys follow authored menu position instead of the raw `#L` id, the collision case where a correct authored choice now keeps the numbered continuation even when another wrong-answer branch reuses that same numeric key, more of the client-side WZ-backed failure and return-to-NPC text, runtime quest item-count, quest-state, quest-record, and quest-scoped `#jcmp#` or `#jmin#` or `#jsec#` or `#jdate#` or `#jhave*#` detail-token rendering across quest-detail text, authored `Say.img` conversation pages, and packet-owned quest-result modals when the loaded quest record publishes serialized key/value pairs, build-backed player-name and job token rendering across nearby-NPC quest pages and packet-owned quest-result modals, nearby-NPC `Talk` pages that now run NPC `info/speak` text through that same live simulator formatting context including current-map fallback, cleaner MapleStory token rendering across lowercase and uppercase quest-name tokens, bare or hash-terminated job tokens, mob, pluralized item or mob mentions, selected-quest, quest-reference, stored quest-record placeholders, fully stripped uppercase and lowercase prompt markers, and reward-summary category tags, no longer exposes `stop` or `answer` bookkeeping as user-facing branch choices, no longer misstates branch-root pages by borrowing text from child `yes` or `no` paths, no longer leaks metadata-only stage strings such as `Say.img/3360/0/info = \"0.000000\"` into the overlay as bogus dialogue, now preserves authored empty `yes` or `no` handoff choices as explicit simulator placeholders instead of dropping them, now uses authored lost-item fallback dialogue more often when a quest item is missing, now also checks authored `job` branch keys for start-time job and branch locks before skipping straight to generic fallback prose, now keeps nested `stop/*` and `lost` metadata reachable even when WZ wraps the authored conversation under deeper numeric containers, now follows direct WZ `npc`/`job`/`quest` stage-variant metadata when authored `Say.img` roots gate alternative numeric conversation branches, now keeps the selected numeric variant page in front of later plain-sibling continuation pages instead of dropping that authored opening page whenever the chosen variant both publishes local content and continues through shared siblings, now keeps both shared container-root choices and selected-variant `stop/<page>` answer metadata or wrong-answer branches layered over the chosen variant instead of letting a partial variant choice tree or stop tree hide the generic authored continuation rules, now also resolves bare active-quest `#u#` and `#R#` tokens through the same live quest formatting context already used by numeric quest-state and quest-record tags, and now carries WZ-authored `stop/<page>/flip` speaker-side metadata through parser and formatter state into packet-owned quest-result util dialogs and the remaining authored simulator placeholder pages instead of flattening every authored page onto the same speaker side. The remaining gap is narrowed to true script execution, wider conditional branch selection beyond the current named WZ stop-branch mapping plus the direct `npc`/`job`/`quest` selector, dynamic script-variable resolution for tokens that still need runtime producers or richer non-key/value quest-record decoding beyond the current numeric and bare active-quest WZ token fallbacks, post-action or check-script execution, additional metadata-driven conversation variant keys beyond the currently parsed direct WZ selector set, and broader client conversation window parity | player/NPC/UI layer (`CUserLocal::TalkToNpc`) |
| Partial | NPC action-loader and client-action-set parity | NPC loading still routes through the dedicated `NpcClientActionSetLoader` seam from `LifeLoader.CreateNpcFromProperty`, keeps the client-shaped automatic `nClientActionIdx == -2` entry point, loads only the selected action-set actions, applies the client default frame delay of `180`, appends reversed interior frames when authored WZ actions publish `zigzag = 1`, and caches materialized frame lists per `(templateId, clientActionSetIndex, actionName)` so repeated NPC loads stop rebuilding the same selected frames each time. This pass keeps the selection owner aligned to direct `CActionMan::LoadNpcAction` / `CNpcTemplate::CalcClientActionSetIdx` evidence plus WZ-backed `condition*` data: the loader still keeps root actions separate from `condition*` client-action-set metadata instead of letting the synthetic root branch win the automatic pass before authored condition sets are considered, now treats direct string quest rows such as `condition*/<questId> = "1"` as quest-state requirements instead of misclassifying them as record-value checks, and still hardens the parser against the real WZ nested record-value shape surfaced in NPC data such as `condition*/<questId>/value` so exact quest-record matches no longer depend on loose child-type fallback. Authored state codes still follow the client `3 => started or completed` rule, quest-record strings still compare by exact value, ordinary map NPC creation plus NPC preview still thread live quest state and current quest-record values alongside local-player gender, and the social-room employee NPC actor path still receives that same gender plus quest-state plus quest-record context. The automatic selector still stays split the way `CNpcTemplate::CalcClientActionSetIdx` does: when quest context is present it walks authored non-root client-action sets in client order, but when quest context is absent it stays on the client-shaped no-quest-check pass and only considers conditionless sets before root fallback instead of accidentally satisfying authored quest rows like `state = 0` from simulator defaults. The loader still keeps the client-action-set index model fixed too: synthetic root fallback is `-1`, authored `condition*` sets start at `0` in WZ order, and explicit selection no longer aliases root fallback onto condition-set index `0`. `QuestRuntimeManager` still exposes the generic quest-record lookup seam, keeps the packet-owned mate-name record on that path, ingests decoded `CharacterData::Decode` quest-string records from packet-owned SetField snapshots, merges the later character-data tail `questEx` string records onto that same lookup seam, still normalizes decoded visitor quest rows onto the same exact-string lookup path used by NPC condition-set selection, and now also promotes packet-owned SetField quest-state carriers onto the same NPC-selection path by marking `QuestRecordValues` rows as started-state ownership even when their string payload is empty and by marking `QuestCompleteRecords` rows as completed-state ownership before automatic client-action-set choice runs. That keeps authored `condition*/<questId> = "1"` / `"2"` / `"3"` rows participating in automatic NPC action-set choice on fresh packet-owned field loads even when the only recovered evidence for that quest is the presence of an active quest row or a completed-quest timestamp instead of a non-empty quest-record string. The same seam now also owns the simulator's current client-action-name lookup approximation for the known `CActionMan::LoadNpcAction` preview/shop-style ids already evidenced in this workspace: action `0` now prefers authored `default` before `stand`, action `1` stays on `stand`, action `2` still prefers `say` while falling back to authored actions that actually publish `speak` data when a template omits a literal `say` branch, and actions above `2` now follow the recovered `CNpcTemplate::aAct[nAction - 2].bsAction` model by resolving against the selected template's authored extra action order before the numeric WZ-key fallback. Focused source coverage is now on disk in `UnitTest_MapSimulator/PacketOwnedNpcQuestSelectionParityTests.cs` for packet-owned SetField active-state, completed-state, quest-string, `questEx`, and visitor-record merges into the quest-state plus quest-record seams used by NPC action-set selection. Remaining gap: broader `bQuestCheck = 1` parity still depends on recovering any further client-owned quest-record producers beyond simulator-session quest state, packet SetField active quest rows, packet SetField completed quest rows, packet SetField quest-string, packet SetField `questEx`, packet SetField visitor quest records, and packet mate-name seams now wired into the known NPC creation paths, and the simulator still approximates the native `CNpcTemplate::aAct` construction step by deriving that action order from the WZ action names already visible to the loader instead of mirroring the client template loader byte-for-byte. | NPC parity now names and implements the loader owner that sits beneath talk flow and field rendering, and this pass keeps the selected action-family seam closer to the client by separating root fallback from `CLIENT_ACTION_SET` selection, replacing quest-row-presence checks with live quest-state, packet SetField active/completed quest-state carriers, packet SetField quest-string, packet SetField `questEx`, packet SetField visitor quest records, and quest-record evaluation inside the existing seam, aligning the automatic set walk itself to the client's authored-order `CalcClientActionSetIdx` behavior instead of a simulator-invented two-pass priority, preventing simulator-only false positives when no quest runtime is available by honoring the client's separate no-quest-check branch, explicitly matching the client's root `-1` versus authored condition-set `0..n-1` index model instead of collapsing both onto simulator index `0`, covering both the common nested WZ `value` quest-record row shape and direct string state-row shape in code instead of relying on incidental child scanning, and moving the currently recovered action-id name lookup for the known preview/shop-style ids onto the same NPC action seam instead of leaving it as a UI-local fallback. The remaining divergence is narrower and explicit: the simulator now mirrors the fixed `0`/`1` entries plus the recovered authored-template ordinal lookup for `nAction >= 2`, but still needs any still-unrecovered client quest-record producers and byte-exact `CNpcTemplate::aAct` construction before this seam can be called fully client-matched. | NPC action-loader layer (`CActionMan::LoadNpcAction`, `CNpcTemplate::CalcClientActionSetIdx`, `NpcClientActionSetLoader`, `QuestRuntimeManager`, `SocialRoomEmployeeActorRuntime`, `MapSimulator.PacketOwnedStageTransitionParity.cs`, `UnitTest_MapSimulator/PacketOwnedNpcQuestSelectionParityTests.cs`) |
| Partial | Quest lists and quest-only actions | Nearby NPC overlays still keep the WZ-backed available, locked, in-progress, and completable quest lists plus simulator-session accept/completion state, quest mob counts, picked-up quest item counts, and common `Check.img` requirement gating across item, fame, trait-min, skill-acquire, ancestor-job, explorer `subJobFlags`, active-pet, pet-tameness, hidden-item, and `endmeso` checks. Common `Act.img` rewards still apply skill/master-level and SP updates, trait EXP, buff-item effects, pet tameness/speed rewards, linked quest-state mutations, `nextQuest`, `npcAct`, `message`, `map`, signed meso/fame deltas, `resignRemove` give-up cleanup, root-job and class-branch reward filters, `jobEx` explorer subjob gating, reward-choice prompting for multi-option `prop = -1` groups, inventory-space validation before commit, transactional rollback on late live-inventory add failures, and negative-count consume rows in both gating and pre-action consumption, with mixed live-inventory plus tracked quest-item seams still resolving against the combined count for requirement rows, detail progress, completion checks, and negative-count `Act.img/item` consumes. The packet-result action notice and modal seam also still prefers parsed `Quest/Act.img` conversation pages and metadata over `Say.img` fallback, keeps authored numbered-page ordering stable by numeric WZ key, and keeps common zero-sentinel `Act.img/info` strings filtered. This pass keeps that same client-shaped packet-result action notice fully on the parsed `Act.img` seam instead of silently dropping back to summary-only output and extends the repeatable-check seam from simple minute intervals to the authored daily and weekly reset families evidenced in `Quest/Check.img/10014/0/dayByDay = 1`, `Quest/Check.img/9880/0/dayByDay = 1`, and `Quest/Check.img/26000/0/{weeklyRepeat = 1, dayOfWeek/0 = "Monday"}`. `QuestRuntimeManager` still reuses the parsed visible-action-line seam behind packet-result subtype `12` notices, surfaces filtered non-item supplemental lines when a bundle has no inventory-category summary, appends those same filtered supplemental lines after the inventory-category notice when one does exist, keeps authored action-job metadata visible there even when the current build does not match the bundle, suppresses non-applicable EXP/fame/buff/message/next-quest style supplemental lines on mismatched action-job bundles instead of surfacing them like unconditional rewards, avoids duplicating the negative-meso action line when the notice is already wrapped through the client `4552` negative-meso string, and now also treats completed `dayByDay` or `weeklyRepeat` quests as locally restartable again once the next authored reset boundary has passed instead of leaving them permanently completed unless a minute-based interval row happened to exist too. The same seam now also carries the owning quest id through action `message` / `info` formatting, so tokenized `Act.img` action text resolves through the normal quest-record formatter in both the packet-result notice path and the live quest-action apply path instead of formatting those action lines as questless text. Focused regression coverage for the action notice seam still lives in `UnitTest_MapSimulator/QuestActPacketNoticeParityTests.cs`, and repeat-reset coverage now lives in `UnitTest_MapSimulator/QuestRepeatResetParityTests.cs` for next-midnight `dayByDay` unlocks plus weekly reset unlocks on the next authored start day. Remaining gap: deeper full script branching beyond these direct numbered or choice `Act.img` pages, byte-exact client scheduling for rarer repeatable action families beyond the current minute plus daily plus weekly reset seam, broader skill-book-style handling beyond direct skill/master-level updates, rarer nonstandard `Act.img` action families outside the parsed common and action-metadata seams, and deeper per-slot inventory-backed quest parity such as exact slot-by-slot add or consume failure semantics beyond the current combined-count fix, accept-or-block precheck, and transactional late-add-failure guard are still incomplete. | Progression-facing NPC parity now keeps both authored quest requirements and more of the authored quest-action runtime on the same WZ-backed seam: common reward and consume behavior, choice rewards, action-owned packet-result notices and modal pages, action-level level/period/interval metadata, action-level job metadata and conditional side-effect execution, parsed field-enter target maps, repeat gating, field-enter auto-start/action execution, metadata-only plus non-item packet-result action notices, and now the common WZ daily/weekly repeat-reset families all stay aligned across nearby-NPC overlays, quest detail, and packet-result presentation instead of splitting those authored behaviors across ad hoc simulator-only paths. The remaining gap is narrowed to deeper script-driven action branching, rarer action families, broader skill-book semantics, rarer repeat schedules beyond the current reset seam, and deeper slot-exact inventory behavior. | NPC/UI layer |
| Partial | Auto-follow, escort, and passenger passive-move requests | Escort mobs still mirror the client-backed `TryAutoRequestFollowCharacter` auto-attach gate, and the simulator-owned local follow seam still deliberately stays inside the existing `CWvsContext::SendFollowCharacterRequest`, `CUserLocal::OnFollowCharacterFailed`, `CUser::OnFollowCharacter`, and `CUserLocal::OnKey` owners instead of widening follow state inference. WZ-first evidence still anchors the inbound prompt owner on `UIWindow2.img/FadeYesNo`, whose authored `backgrnd7`, `BtOK`, and `BtCancel` family continues to match the simulator's in-field follow confirm surface before the older login utility fallback is used, and the same `UIWindow(.img|2.img)/ContextMenu/BtFollow` family still anchors the reused manual-request button art on the character-info seam. Client evidence was rechecked again in the same row-specific seams: `CUser::OnPassiveMove` at `0x8dea10` still only runs while `m_dwDriverID` is set and still just forwards the packet into the passenger `CMovePath::OnMovePacket(..., 1)` seam, `CUserPool::OnUserCommonPacket` still routes opcode `193` through `CUser::OnFollowCharacter`, `CUserLocal::OnFollowCharacterFailed` (`270`) still owns the local failure text path, and `CUserLocal::HandleRButtonClk` still passes a disabled-follow flag into the shared context-menu owner whenever the local user already has either `m_dwPassenserID` or `m_dwDriverID` set. The same `CUser::OnFollowCharacter` re-check also exposed one concrete detach detail the simulator was still missing: when a passenger detaches without transfer-field coordinates, the client snaps back to the driver's live position and carries the driver's current foothold forward before clearing `m_dwDriverID`, while transfer-field detach keeps the packet-authored override instead. The simulator now mirrors that narrower detach owner inside the existing local follow result seam by carrying the remote driver's foothold id through packet-authored detach and the local fallback attach-sync path, then resolving it back onto the local player physics foothold without re-opening broader movement ownership. The reused character-info `BtFollow` owner now also mirrors the confirmed `HandleRButtonClk` availability gate by disabling the shared follow button while the local simulator player already has an attached driver or passenger, instead of leaving the button clickable and only failing later in the request owner. The existing packet-owned split remains aligned elsewhere: local utility ingress still accepts named `passivemove` replay plus real client opcode `269`, the official-session bridge still mirrors inbound `269` beside `193/253/254/270`, attached local follow still prefers the decoded packet-owned move-path snapshot before falling back to the older driver-derived passive-follow resolver, stored packet-owned passive move is still cleared on attach, detach, and map-content resets, blank or no-payload follow failures still fall back to the same unknown-error notice text exposed through `FollowCharacterFailureCodec`, attached passenger playback still carries decoded or synthesized passive-move foothold ids instead of collapsing grounded transitions to `0`, manual request initiation still honors the recovered `CUserLocal::HandleRButtonClk` gate for live target presence, foothold-backed range, traversability, passenger occupancy (`reason 3`), and occupied-driver formatting (`reason 2` with the live driver id), and the incoming follow prompt still prefers the in-field `FadeYesNo` owner when that shared window exists. Focused regression coverage now actually lives in `UnitTest_MapSimulator/FollowCharacterPassiveMoveParityTests.cs` for the shared `BtFollow` disable gate plus the detach foothold-carry and transfer-field override cases. Remaining gap: the exact inbound follow-prompt packet body or owner setup beyond the recovered shared `FadeYesNo` art family is still not confirmed byte-for-byte; manual request initiation still reuses the remote character-info seam instead of the exact `CUserLocal::HandleRButtonClk` context-menu owner and its full modal menu contract even though the follow-entry disable flag now matches; the exact withdraw-packet body or caller contract beyond the opcode-owned lane is still unconfirmed; the `269` branch still reuses the shared simulator move-path decoder instead of a byte-for-byte `CMovePath::OnMovePacket` clone; and the shared remote riding mirror is still reconstructed from the available mount, temp-stat, and action owners rather than a directly decoded remote `m_nRidingVehicleID` field | Escort-style encounters already rejected the client-invalid midair, map-disabled, and local-user-invalid auto-attach cases, and this row now keeps both the explicit passenger move-path owner and the detach snap-back foothold owner on that same parity story instead of treating detachment as a position-only side effect. The simulator still preserves client-shaped request throttling, follow-failure clearing, attach acknowledgment, local-player snap-back versus transfer detach behavior, packet-authored attach or detach playback for the local passenger path, remote passenger ownership on the shared runtime, live-session outbound request or accept injection for opcode `134`, the existing explicit withdraw lane, a separate client-shaped attached-release request for `134(0,0,1)`, native arrow-key-triggered release on the same `CUserLocal::OnKey` seam the client uses, inbound official replay of `193`, `269`, and `270`, a closer active-mounted blocker than the older broad mount-equipped approximation, the in-field `FadeYesNo` prompt owner path, the client StringPool prompt and failure text seams, the packet-authored passenger foothold state for attached passive-move playback, the client-backed detach foothold carry that keeps grounded local follow snap-back closer to `CUser::OnFollowCharacter`, and now the confirmed context-menu follow disable gate on the reused `BtFollow` owner instead of leaving that availability rule entirely outside the simulator UI. The remaining gap is narrowed to the exact prompt payload and owner setup, the rest of the `HandleRButtonClk` modal menu contract beyond the now-mirrored follow disable flag, the withdraw-packet body or caller contract beyond the modeled opcode lane, the last byte-exact `CMovePath::OnMovePacket` behavior details that the shared decoder still approximates, and a directly decoded remote riding-vehicle field instead of the current shared-owner reconstruction. | `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedStageTransitionParity.cs`, `MapSimulator.ProgressionUtilityParity.cs`, `EscortFollowController.cs`, `HaCreator/MapSimulator/UI/Windows/OptionMenuWindow.cs`, `HaCreator/MapSimulator/UI/Windows/UserInfoUI.cs`, `HaCreator/MapSimulator/UI/Windows/InGameConfirmDialogWindow.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.SkillsAndQuests.cs`, `HaCreator/MapSimulator/Interaction/LocalFollowCharacterRuntime.cs`, `HaCreator/MapSimulator/Interaction/FollowCharacterFailureCodec.cs`, `HaCreator/MapSimulator/Interaction/PacketOwnedLocalFollowPassiveMoveResolver.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Pools/RemoteUserPacketCodec.cs`, `UnitTest_MapSimulator/FollowCharacterPassiveMoveParityTests.cs`, field interaction layer (`CUserLocal::TryAutoRequestFollowCharacter`, `CWvsContext::SendFollowCharacterRequest`, `CUserLocal::OnFollowCharacterFailed`, `CUserLocal::FollowCharacterFailedMsg`, `CUser::OnFollowCharacter`, `CUser::OnPassiveMove`, `CMovePath::OnMovePacket`, `CUserLocal::OnKey`, `CUserLocal::HandleRButtonClk`, `UIWindow.img/ContextMenu/BtFollow`, `UIWindow2.img/ContextMenu/BtFollow`, `UIWindow(.img|2.img)/FadeYesNo`) |
| Partial | Packet-owned reactor-pool parity discovered by IDA function scan | This row still stays on the existing packet-owned `ReactorPool` seam, and this pass tightens another concrete `LoadReactorLayer` owner gap instead of widening packet ownership again. WZ-first validation still anchors both spawn and hit timing: map reactor entries in `map/Map/Map0/*/reactor/*` expose packet-compatible fields such as `id`, `x`, `y`, `f`, `name`, and optional `reactorTime`, while reactor template `reactor/1002009/0/hit/*` still publishes two `150ms` hit frames that back the simulator-owned packet hit window. Client evidence from `CReactorPool::OnPacket` (`0x6cf9b0`) plus `OnReactorEnterField` (`0x6cf490`), `OnReactorChangeState` (`0x6ccd60`), `OnReactorMove` (`0x6cd110`), `OnReactorLeaveField` (`0x6ccea0`), `Update` (`0x6cefc0`), `LoadReactorLayer` (`0x6ce630`), `get_hittype_priority_level` (`0x6cc000`), and `CReactorTemplate::GetHitProp` (`0x6d0d80`) still drives the simulator-owned phase clocks in `Pools/ReactorPool.cs` alongside the packet decode seam in `Interaction/PacketReactorPoolRuntime.cs`. The earlier packet-owned work for WZ-backed interaction metadata hydration, enter-field fade-in, deferred state application on `tHitStart`, repeated change-state clock preservation, signed `nProperEventIdx`, leave-field `current + 400`, move fallback timing, raw signed sentinel preservation, `-2`-only authored-event derivation, old-state authored event-type scanning, unmatched `-2` refusal, and the leave-field no-hit-layer completion fallback all stay in place; this pass now also mirrors two more caller-side `LoadReactorLayer` details from `Update`: when packet change-state reaches `tHitStart` and a non-`-2` `LoadReactorLayer` attempt still finds no authored hit layer, the simulator now preserves whichever stopped-layer completion window is already live and otherwise falls back to the remaining WZ-timed stop window before applying the pending packet state instead of letting packet `tStateEnd` win early, and when client-owned `-2` auto-selection finds a concrete authored hit event the simulator now persists that resolved proper-event index onto the packet runtime before the hit-layer load result is known so later no-hit-layer fallback and post-hit completion follow the same non-`-2` owner path the client uses instead of keeping the raw sentinel alive too long. Focused regression coverage is now on disk in `UnitTest_MapSimulator/ReactorPoolPacketParityTests.cs` for the row's current packet-owned `LoadReactorLayer` seam: preserving `-1`, deriving only `-2`, leaving unmatched `-2` unresolved, rewriting matched `-2` auto-selection to the resolved authored index, pinning both leave-field and change-state no-hit-layer outcomes to the surviving stopped-layer completion window instead of an immediate teardown or an early `tStateEnd` handoff, and keeping the helper-level `LoadReactorLayer` proper-event selection aligned with the client-owned auto-derivation rules. Remaining gap: exact COM-layer animation-completion polling still remains approximated through WZ frame-duration timing, the unresolved object-state recovery needed to observe the client's live layer-complete signal directly is still missing, and the client's unresolved default `IWzVector2D::RelMove` behavior outside the recovered timed windows is still approximated through packet state-end bookkeeping plus the simulator's fallback movement path rather than mirrored byte-for-byte. | Without this row, reactor parity looks more complete than it is because the current backlog only covers the local collision and activation half. The client also owns a separate packet lifecycle for reactor spawn, movement, state changes, and teardown, and that needs to be tracked explicitly so future work lands on the real owner instead of overloading the local `ReactorPool` touch path. | packet-owned reactor runtime (`CReactorPool::OnPacket`, `CReactorPool::OnReactorEnterField`, `CReactorPool::OnReactorChangeState`, `CReactorPool::OnReactorMove`, `CReactorPool::OnReactorLeaveField`, simulator seam search: `HaCreator/MapSimulator/Pools/ReactorPool.cs`) |
| Partial | Direction-mode release timing | NPC dialogue overlays now enter a simulator direction-mode lock that blocks local gameplay input without dropping into free-camera mode, closing those scripted overlays releases control on a delayed timer modeled after `CUserLocal::TryLeaveDirectionMode`, the overlay-dismissal click is consumed so it cannot leak into same-frame NPC or portal interactions before the delay expires, and special-field scripted dialogs are advanced before world interaction handlers so wedding ceremony field dialogs can claim that same blocking owner on the very frame they appear instead of leaking one frame of movement or click-through before the delayed-release path engages. The delayed-release path now uses an explicit scripted-owner registry instead of only a fixed visible-window list so tracked NPC or scripted launch points feed it directly for Cash Shop or MTS preview, trunk, item maker, MapleTV, memo send and package dialogs, social-room preview surfaces for miniroom, player shop, hired merchant, and trading-room, NPC-owned item-upgrade launches plus their Vega Spell child window when opened from the already locked upgrade flow, explicit special-field owner tokens for the wedding dialog, Cake vs Pie item-info popup, memory game blocker, tournament match-table dialog, and the bespoke `CRPSGameDlg` rock-paper-scissors owner whose open and destroy packets still toggle a direct `_isVisible` path outside `UIWindowManager`. Inherited utility or community windows opened while a scripted lock is already active such as memo inbox, guild BBS, party or guild search, guild skill, family chart or tree, messenger, and the guild-management child surfaces for guild rank, guild mark, and guild creation agreement continue to reuse that same delayed owner chain. The manager-driven owner path is tighter now too: `UIWindowManager` pre-show callbacks and the local `ShowWindow...` helper seam no longer register only a window name for these script-launched and packet-owned utility surfaces, but capture the concrete `UIWindowBase` instance as a live owner predicate when that instance is already known. This pass also closes the remaining shared-prompt authoring hole on the `LoginUtilityDialog` side instead of only the runtime merge seam: `LoginPacketDialogPromptConfiguration.TrackDirectionModeOwner` now still survives prompt merges, is still forwarded into `ShowLoginUtilityDialog(...)`, and `/loginpacket ... trackowner=true|false` (plus the explicit `trackdirectionmodeowner=` alias) now also feeds that same flag into the shared prompt configuration, so packet/script prompt overrides routed through the shared login-utility host can keep or drop their own delayed-release owner intentionally instead of silently falling back to inherited-or-visible-window behavior. The same owner opt-in continues to exist on the shared `ConnectionNotice` host for packet-owned field warnings, and the shared in-field `FadeYesNo` / `CUtilDlgEx`-shaped confirm host continues to take that direct-owner path for the recovered game-menu quit prompt, Messenger invite confirm seam, and Vega white-scroll confirmation instead of depending on a pre-existing inherited lock before registering the host. This run also closes another bespoke non-manager gap that was still falling back to the broader scripted-sequence bit instead of claiming a concrete owner: `SpecialEffectFields` already marked `CUICakePieEventItemInfo` as blocking, but `GameStateManager.ShouldInheritDirectionModeOwner(...)` and the delayed-release registry did not yet register that popup directly, so its close button could release through the coarse fallback instead of the same explicit owner chain used by wedding, memory game, tournament, and RPS. The dedicated packet-script owner host now follows that same path too: WZ-backed pet selection (`UIWindow.img/UtilDlgEx_Pet` / `UtilDlgEx_MultiPetEquip`) and slide-menu (`UIWindow.img/SlideMenu`) prompts opened from `PacketScriptDedicatedOwnerRuntime` are registered as a live non-manager owner while visible, so closing the NPC overlay for those dedicated prompt surfaces no longer lets the inherited scripted direction-mode lock age out after the delayed timer while the prompt remains active. Focused regression coverage now lives in `UnitTest_MapSimulator/GameStateManagerDirectionModeParityTests.cs` for the inherited-owner predicates that keep this Cake/Pie popup and packet-script dedicated owners on the tracked direction-mode path, the delayed scripted release timer, and the shared `/loginpacket` track-owner parser seam. Remaining gap: other bespoke non-manager scripted owners and non-overridden shared-host launch paths that still do not opt into a dedicated tracked owner token or live-owner callback, plus visibility paths that never reach either `UIWindowManager.ShowWindow(...)`, `UIWindowBase.BeforeShow`, or an explicit scripted-owner registration seam, still rely on the broader fallback path. | Scripted UI sequences now keep their dismiss or expiry transitions inside the scripted lifecycle instead of accidentally re-triggering world interactions while control is still supposed to be locked, and this pass moves the modal ownership closer to the client seam by making active scripted field runtimes, direct registered-window refreshes, shared `LoginUtilityDialog` prompts, shared `ConnectionNotice` warnings, the shared in-game `FadeYesNo` confirm host, the Cake/Pie item-info popup, and the in-flow packet, packet-script dedicated prompt, skill, utility-community, anti-macro, logout-gift, reward-result, radio, Aran-skill-guide, follow-request, and rock-paper-scissors follow-up owners explicitly hold or inherit direction mode instead of relying only on a broad visibility sweep. The remaining gap is narrowed further to bespoke non-manager owners and host launch paths that still need their own explicit registration seam rather than the legacy fallback path, with the shared `/loginpacket` prompt-authoring hole now closed on top of the earlier merge/runtime propagation fix. | `GameStateManager.cs`, `DirectionModeWindowOwnerRegistry.cs`, `UIWindowManager.cs`, `UIWindowBase.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.ProgressionUtilityParity.cs`, `MapSimulator.VegaSpellParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedLogoutGiftParity.cs`, `MapSimulator.PacketOwnedFieldFeedbackParity.cs`, `MapSimulator.PacketOwnedAntiMacroParity.cs`, `MapSimulator.GuildManagementParity.cs`, `MapSimulator.TrunkAccountSecurityParity.cs`, `SpecialFieldRuntimeCoordinator.cs`, `SpecialEffectFields.cs`, `PacketScriptDedicatedOwnerRuntime.cs`, `UnitTest_MapSimulator/GameStateManagerDirectionModeParityTests.cs`, `UIWindow(.img|2.img)/FadeYesNo`, `UIWindow2.img/UtilDlgEx`, `UIWindow.img/UtilDlgEx_Pet`, `UIWindow.img/UtilDlgEx_MultiPetEquip`, `UIWindow.img/SlideMenu`, `UI/UIWindow.img/SkillGuide`, `UI/UIWindow.img/Radio`, `UI/UIWindow.img/5thevent/WorldUI` (`CUserLocal::TryLeaveDirectionMode`, `CRPSGameDlg`, `CWvsContext::OnLogoutGift`, `CUserLocal::OnAskAPSPEvent`, `CUtilDlgEx`, `CUICakePieEventItemInfo`, `CSlideMenuDlg`, `CSlideMenuDlgEX`) |
| Implemented | Packet-authored direction-mode and stand-alone control parity | The simulator now carries a dedicated packet-owned local-control layer instead of folding everything into the inferred window lock: `GameStateManager` separately tracks scripted-owner direction mode versus packet-owned direction mode, packet-authored `SetDirectionMode` writes now clear any pending packet release first, apply immediate enable or immediate disable when the delay is non-positive, and otherwise keep the packet-owned direction-mode bit alive until the stored delayed release expires in `UpdateDirectionMode`, while `SetStandAloneMode` now writes a separate runtime stand-alone flag that status surfaces and later owners can inspect without pretending it is another visible window owner. That stand-alone bit now also participates in the simulator's inherited direction-mode owner routing, so later modeless owners opened through the existing `ShowWindowWithInheritedDirectionModeOwner` and implicit-owner seams can re-enter the tracked direction-mode path after a packet-authored `SetStandAloneMode(254)` write even when no synthetic visible owner was created. This pass also removes the row's synthetic-id-only packet gap by teaching the local-utility seam to decode real opcode-framed `CUserLocal::OnPacket` traffic: `LocalUtilityPacketInboxManager` now accepts `packetclientraw` lines, validates the 2-byte opcode, and forwards live client opcodes such as `253` and `254` with their payloads instead of requiring synthetic `1013/1014` ids, while a new `LocalUtilityOfficialSessionBridgeManager` can proxy a live Maple session and mirror inbound `SetDirectionMode(253)` and `SetStandAloneMode(254)` packets into the same existing utility handler path. The local-utility command surface and help text now expose both `packetclientraw` and `/localutility session ...` control so this row can be exercised through opcode-framed local replay or a confirmed live inbound proxy rather than only through synthetic loopback aliases. Focused regression coverage now includes `UnitTest_MapSimulator/GameStateManagerTests.cs` for the inherited-owner predicate that keeps packet direction mode and stand-alone control wired into later owner inspection. | If this owner stays folded into the existing release-timing row, future work can overstate direction-mode parity while still missing the packet branch that actually turns the mode on, schedules a delayed off transition without a visible owner, or flips the separate stand-alone control flag in `CWvsContext`. Tracking it explicitly keeps future scripted-control and packet-driven interaction work anchored to the real `CUserLocal::OnPacket` owner instead of only the simulator's inferred window-lock path. | packet-owned local control-state layer (`CUserLocal::OnPacket`, `CUserLocal::OnSetDirectionMode`, `CUserLocal::OnSetStandAloneMode`, `CUserLocal::SetDirectionMode`, `CUserLocal::TryLeaveDirectionMode`, `CWvsContext`) |
| Partial | Reactor interaction parity | Local player movement still runs reactor touch checks through `ReactorPool` on the client-like once-per-second cadence from `CUserLocal::CheckReactor_Collision`, and the simulator now keeps the broader WZ-first interaction surface already landed for this row: full direct-frame reactor visuals plus sparse numeric branches, WZ-backed touch versus hit versus skill versus item versus quest activation selection when `info/reactorType` is sparse or absent, authored `event/*/type` fallback for touch, harvest, skill, item, and directional hit-only reactors, live player attack and summon hit surfaces on the non-touch path, local inventory-gated item use, quest-runtime-driven reactor refresh with selector-aware quest routing, preserved authored state ids and frame durations across activation or reset, selector-aware item/skill/quest branch chaining including selectorless lookahead and delimited-string selector parsing, mixed activation-type graphs on the same state, live authored frame-bounds for collision and item-use checks, script-name parsing plus multi-alias dynamic object-tag publication, and packet-owned branch preservation through `properEventIndex`. This pass closes another packet-move gap inside the same `ReactorPool` seam instead of widening ownership: direct client evidence from `CReactorPool::OnReactorMove` still shows the pool unconditionally forwarding opcode `335` onto `IWzVector2D::RelMove` for the resolved reactor object, so explicit packet move requests no longer lose their movement window just because the current reactor template omits `info/move`. When a live packet `tStateEnd` window is already active, explicit opcode-`335` repositioning now keeps animating on that packet-owned clock for non-template reactors too instead of snapping immediately; standalone opcode-`335` packets still keep the simulator fallback move clock and eased interpolation when no live state-end window exists. Focused source coverage for that packet-move allowance now lives in `UnitTest_MapSimulator/ReactorPacketMoveParityTests.cs`, which pins the client-shaped gate split between template move, fallback default-`RelMove`, and explicit packet-owned move requests. Remaining gap: the simulator still infers the missing-variant `IWzVector2D::RelMove` timing and easing for standalone move packets rather than recovering the client’s exact default window, local or otherwise non-packet first-hop non-quest same-type branches can still fall back to authored order when neither the current branch nor later same-type descendants expose a selector clue or client-backed hit-priority signal, and richer named reactor-script or field-event execution beyond the existing dynamic-tag publication seam is still incomplete | Environmental interaction now covers the client-shaped touch-reactor loop, more of the WZ-authored reactor animation and transition surface, actual hit-, item-, skill-, quest-, and mixed-graph activation routing, selector-aware authored branch preservation across later same-type hops, live frame-bounds collision, script-backed tagged-object publication, and packet-owned move or change-state follow-up handling. The new packet-move fix matters because `CReactorPool::OnReactorMove` is a pool-owned movement seam rather than a template-`move` hint, so explicit server repositioning now stays animated even for reactors whose WZ metadata is otherwise sparse. The remaining work is narrowed to recovering the client's exact standalone `RelMove` default, the last non-quest same-type local first-hop cases without selector or hit-priority clues, and fuller field-script execution beyond dynamic tag toggles | `HaCreator/MapSimulator/Pools/ReactorPool.cs`, `HaCreator/MapSimulator/Entities/ReactorItem.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `HaCreator/MapSimulator/Fields/ReactorScriptStatePublisher.cs`, `UnitTest_MapSimulator/ReactorPacketMoveParityTests.cs` (`CUserLocal::CheckReactor_Collision`, `CReactorPool::OnReactorChangeState`, `CReactorPool::OnReactorMove`, `CReactorPool::LoadReactorLayer`, WZ `reactor/*/event/*`) |
| Partial | Quest-alert, balloon, and idle social feedback loops | NPCs with available, in-progress, or completable quests now render the client-backed `QuestIcon` alert art above their heads based on the simulator quest runtime, quest accept/complete actions now queue timed in-world feedback balloons above the speaking NPC instead of collapsing to a single fixed message while also holding the NPC in `speak` until the active balloon expires, nearby NPCs with WZ `info/speak/*` idle lines now periodically surface those lines through the same in-world balloon path, and pets now mirror the client-backed `PetAutoSpeaking` surfaces for level-up, pre-level-up, idle `e_rest`, low-HP alert, and HP/MP potion failure speech with the same six WZ-backed `String/PetDialog.img` event keys that exist in this data set while also honoring the client-backed `0x100` pet-skill gate from `CPet::AutoSpeakingByEvent` instead of letting every pet auto-speak by default. The simulator specialist pet-dialog seam now also loads numbered `String/PetDialog.img` slang entries such as `s1`-`s4` instead of only flat `s` keys, and ordinary non-command player chat now feeds that same loaded specialist feedback path with a short cooldown so compatible active pets can surface non-`PetAutoSpeaking` chatter in-world instead of leaving those lines debug-only. The same specialist seam now also listens to live Messenger room chat, Messenger whispers, simulator-owned social-list chat and whisper actions, family-chart family-name, precept, entitlement, and roster-change text, social-list guild and alliance notice or rank-title edits plus packet-owned guild/alliance notice or rank-title refresh text, guild-BBS post and reply submission text, skill-macro party-notify chat lines and any incoming friend, party, guild, alliance, or expedition targeted chat routed through the shared `MapSimulatorChat.AddIncomingTargetedMessage` renderer, packet-owned utility chat routes that resolve to the same social chat-log families, packet-owned social chat families such as incoming whispers plus friend, party, guild, alliance, couple, expedition, or channel-style client-chat type `19` field-message routes, WZ-backed field message-box/chalkboard text from local creation and packet-owned enter-field packets, MapleTV Megassenger chat mirrors from both local publishes and packet-owned set-message updates, MiniRoom and adjacent `SocialRoomRuntime` speaker plus system/game text, memo or parcel send plus quick-send body text from the memo mailbox owner, direct incoming memo or parcel delivery text from the same mailbox seam including packet-owned parcel arrivals and simulator-owned memo injections, the simulator-owned engagement proposal owner's request/custom note text across outgoing opens, incoming opens, inbox-driven opens, accept or withdraw actions, and dismiss/decline actions, the adjacent wedding invitation owner's groom/bride participant-name surfaces across local opens, packet-owned marriage-result opens, accepts, and dismissals, and the adjacent wedding wish-list owner's committed gift-send item labels, committed gift-claim item labels, wish deletions, and final OK confirmation wish labels, so more of the simulator's social text owners can trigger the same WZ-backed in-world pet chatter instead of limiting it to the local chat input box alone. This pass also extends that same specialist routing into adjacent guild-management text that previously stayed utility-only: `SocialListRuntime` now publishes guild-create agreement request, approval, and rejection text plus local or packet-owned guild-mark request lifecycle text through its existing `SocialChatObserved` seam, while the separate guild-skill seam now pushes local renew / level-up feedback, packet-owned guild-skill result echoes, client `OnGuildResult` skill-record mirrors, and guild-skill-only `/sociallist packet resolve guild ...` outcomes through the same loaded specialist pet chatter path instead of leaving those status lines confined to utility chat. Packet-owned social specialist routing is now also centralized through `MapSimulatorChat.ClientChatMessageAdded`, so supported client chat-log families and incoming targeted chat rows that reach the shared chat seam no longer need one-off pet-feedback hooks at each packet owner or the skill-macro party-notify owner, and packet-owned social-list notice or rank-title refreshes now reuse that same specialist path instead of staying siloed inside the union-management state. Quest `Act.img/petskill` rewards now persist on a compatible active pet selected through the same WZ `Check.img/pet` plus the client-shaped active-pet limit aliases used by these pet-training quests, including `petRecallLimit` and the `petAutoSpeakingLimit` fallback found in this data set, and the quest reward text now resolves recognized client pet-skill bits such as `0x80` and `0x100` to named skills instead of falling back to opaque hex strings. Active pet runtime ownership now also persists the trained pet skill mask, command level, tameness, fullness, auto-loot toggle, and auto-HP configuration across unsummon or controller clears for the same pet item within the current simulator session, and WZ `Item/Pet/*/interact/*/inc` closeness gains now feed command success so trained pets accumulate tameness, step through the simulator's client-shaped command-level thresholds, and keep that command growth attached to the same pet item instead of treating command level as a detached manual knob. USE-item clicks or hotkeys now parse WZ `Item/Consume/*/spec` recovery fields including numeric-string `hpR`/`mpR` values seen in this data set, HP/MP potion failure speech is throttled to the client-shaped first-few repeats instead of firing forever, zero-count USE hotkey bindings now persist across config reloads instead of being dropped when the stack is empty, and live pet-food item uses now route through the loaded `f1`-`f4` dialog buckets by deriving food tiers only from the client-shaped `interact/c1`-`c4` ranges instead of folding unrelated higher `c*` commands back into those four buckets, while also falling back to the truly nearest actually loaded food bucket when a pet only ships a subset such as `f1`, selecting a compatible active pet from the food item's `spec/*` whitelist when present, and only consuming food when the targeted pet's new simulator fullness state can actually accept the WZ `spec/inc` increase while already-full pets surface the matching failure bucket instead. Packet-owned parcel receive-session hydration now also keeps a cached signature set for the last packet-owned mailbox snapshot, so receive-owner opens and later refreshes can surface stored or newly appeared parcel bodies through the existing specialist seam without duplicating rows that already announced through earlier packet `24` delivery events or older hydrated sessions. Remaining gap: additional automatic non-`PetAutoSpeaking` specialist chatter sources outside the currently wired local chat, shared incoming targeted chat renderer, Messenger, simulator-owned and packet-owned social-list text paths, guild-BBS text paths, field message-box/chalkboard local or packet-owned entry text, MapleTV Megassenger mirrors, `SocialRoomRuntime` speaker/system/game text, memo or parcel compose plus direct incoming-delivery text, the engagement proposal note lifecycle, the wedding invitation participant-name lifecycle, the wedding wish-list item-label lifecycle, adjacent guild-management status-only surfaces that still do not commit through `SocialListRuntime` or the guild-skill feedback seam such as guild-rank paging or other editor-only prompts, and other future text owners that still bypass both `MapSimulatorChat.ClientChatMessageAdded` and the explicit `SocialChatObserved` or `SocialMessagesObserved` delegates are still not modeled; pet ownership still only survives for the current simulator session rather than a fuller durable profile, and wider quest-driven pet-skill semantics beyond the currently consumed auto-speaking gate are still missing | The simulator now surfaces both static quest-state alerts and closer client-like NPC and pet idle feedback loops in-world instead of hiding all progression feedback inside the dialogue overlay alone, closes the consume recovery mismatch by honoring numeric-string recovery ratios in `Item/Consume/*/spec`, and now turns real pet-food item uses into WZ-backed in-world pet speech using the pet's existing command-tier ranges, compatibility list, and `spec/inc` fullness gain so failure speech no longer burns food on already-full pets. This pass also fixes the tier derivation seam to stay anchored to the client-shaped `interact/c1`-`c4` buckets, corrects the fallback selector so partial `f1`-`f4` coverage now chooses the closest loaded bucket rather than simply the next higher one, loads the numbered pet-dialog slang strings that this data set stores under `s1`-`s4` instead of a flat `s` property, and now gives those specialist lines live ordinary-chat, skill-macro party-notify chat via the shared incoming targeted chat renderer, Messenger, family-chart family-name/precept/entitlement/roster updates, simulator-owned social-list chat and whisper actions, social-list guild/alliance editor commits, packet-owned guild/alliance notice and rank-title refreshes, guild-BBS post and reply submission text, packet-owned utility and field-social chat call sites, WZ-backed field message-box/chalkboard local and packet-owned enter-field text, MapleTV Megassenger mirrors, MiniRoom and adjacent social-room speaker plus system/game text, memo or parcel send text, direct incoming mailbox deliveries, the engagement proposal owner's staged note/custom-text lifecycle, the wedding invitation owner's local and packet-fed participant-name surfaces, the wedding wish-list owner's committed send / claim / delete / confirm item-label transitions, and now the packet-authoritative guild create / guild mark request lifecycle plus the adjacent guild-skill result seam instead of leaving those newer guild-side surfaces confined to utility feedback. This pass also ties pet auto-speaking ownership back to WZ quest rewards and the client gate, teaches the quest runtime to honor the `petAutoSpeakingLimit` alias used by these training quests, surfaces named client pet-skill reward text for known bit flags, and keeps the trained pet's core runtime state attached to that pet item across unsummon or map-runtime clears within the active session instead of discarding it immediately. It now also consumes WZ `interact/*/inc` command gains into persisted pet tameness so command success can grow the pet's live command level instead of leaving command progression as a manual-only simulator override, brings channel-style client chat type `19` into the centralized `ClientChatMessageAdded` specialist filter, moves incoming friend, party, guild, alliance, and expedition targeted chat rows onto that same centralized event seam, and keeps packet-owned parcel receive hydration aligned with the mailbox seam by replaying stored rows once while suppressing duplicates on later refreshes. Focused coverage for the guild dialog specialist bridge now lives in `UnitTest_MapSimulator/SocialListGuildDialogPetSpecialistParityTests.cs`, alongside the existing wedding-invitation specialist coverage. The remaining gap is narrowed to additional chatter sources that live outside the client's six `PetAutoSpeaking` events plus the current local-chat, shared incoming targeted chat renderer, Messenger, simulator-owned and packet-owned social-list text seams, guild-BBS text seams, field message-box/chalkboard local or packet-owned entry text, MapleTV Megassenger mirrors, `SocialRoomRuntime` speaker/system/game text, memo or parcel compose and direct-delivery text, the engagement proposal note lifecycle, the wedding invitation participant-name lifecycle, the wedding wish-list item-label lifecycle, adjacent guild-management status surfaces that still do not resolve through those shared specialist seams, plus fuller durable pet ownership beyond the current in-session item-keyed runtime and pet-skill semantics that still need consumers beyond the current auto-speaking path | `QuestRuntimeManager.cs`, `NpcFeedbackBalloonQueue.cs`, `NpcItem.cs`, `LifeLoader.cs`, `PetLoader.cs`, `PetController.cs`, `FamilyChartRuntime.cs`, `SocialListRuntime.cs`, `SocialListRuntime.PacketModel.cs`, `SocialListRuntime.Management.cs`, `GuildBbsRuntime.cs`, `MemoMailboxManager.cs`, `SocialRoomModels.cs`, `MapleTvRuntime.cs`, `EngagementProposalController.cs`, `WeddingInvitationController.cs`, `WeddingInvitationRuntime.cs`, `WeddingWishListController.cs`, `WeddingWishListRuntime.cs`, `FieldMessageBoxRuntime.cs`, `MapSimulator.PacketOwnedFieldFeedbackParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.SocialListPacketParity.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.EngagementProposalParity.cs`, `MapSimulator.cs`, `MapSimulatorChat.cs`, `UnitTest_MapSimulator/SocialListGuildDialogPetSpecialistParityTests.cs`, `UnitTest_MapSimulator/WeddingInvitationPetSpecialistParityTests.cs`, `UIWindow*.img/QuestIcon`, `String/PetDialog.img`, `UIWindow2.img/MateMessage`, `UIWindow2.img/Wedding/Invitation`, `Item/Pet/*/interact`, `Item/Consume/0212/*/spec`, `Quest/Check.img`, `Quest/Act.img` (`CUserLocal::Update`, `CUser::PetAutoSpeaking`, `CPet::AutoSpeakingByEvent`) |

### 2. NPC image-entry ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | NPC image-entry / linked-template parity | Using WZ evidence from linked NPC templates such as `Npc/0002004.img -> info/link -> 0002003` and client evidence from `CActionMan::GetNpcImgEntry` (`0x41a840`), the simulator now keeps the dedicated image-entry owner explicit instead of letting link resolution blur into later action or talk seams. `HaCreator/MapSimulator/Loaders/NpcImgEntryResolver.cs` already matched the client-shaped cache and recursive top-level branch inheritance for action loading; this pass closes the remaining simulator bypasses by routing `HaCreator/MapEditor/Info/NpcInfo.cs` preview extraction and `HaCreator/MapSimulator/Interaction/NpcInteractionOverlay.cs` packet quest-result speaker lookup through that same normalized NPC IMG entry owner, so source-template overrides stay visible while missing top-level branches still inherit from linked templates before downstream action or speaker selection runs. Focused regression coverage is now on disk in `UnitTest_MapSimulator/NpcImgEntryResolverParityTests.cs` for recursive link-chain inheritance, source-branch precedence over linked branches, and cycle-safe recursion. | Without this row, NPC parity can look covered once talk flow and `LoadNpcAction` are tracked, even though the client also decides which normalized NPC IMG property tree exists one layer earlier. Keeping that seam explicit isolates linked-template inheritance and per-template cache work from the later action-loader and dialogue rows. | NPC template/image-entry layer (`CActionMan::GetNpcImgEntry`, `HaCreator/MapSimulator/Loaders/NpcImgEntryResolver.cs`, `HaCreator/MapEditor/Info/NpcInfo.cs`, `HaCreator/MapSimulator/Interaction/NpcInteractionOverlay.cs`, `LifeLoader.CreateNpcFromProperty`, `NpcClientActionSetLoader`, `UnitTest_MapSimulator/NpcImgEntryResolverParityTests.cs`) |

### 3. Reactor layer-build and hit-prop parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Reactor layer-build / hit-prop ownership | This pass keeps the row on the dedicated simulator-owned rebuild seam and closes another `CReactorTemplate::GetHitProp` / `CAnimationDisplayer::LoadLayer` approximation leak around packet-owned visual ownership. `Pools/ReactorPool.cs` still replays packet-owned hit layers and recovered reactor-hit sounds, mirrors the client z/key math from `LoadReactorLayer` for layer `0/1/2`, reconstructs moving-state `RelMove` ownership by queueing template-`move` packet state targets, starting motion when the packet reactor enters its active layer, interpolating toward the decoded position across `PacketStateEndTime`, snapping the final position when the window closes, and now keeps `MapSimulator.cs` on the source/pre-hit visual for the whole unresolved packet-owned handoff instead of only while a hit-layer timer is active. That means `PacketPendingVisualState` no longer bleeds through the render-loop state sync before the client-shaped handoff has actually committed, including no-hit-layer state changes that must wait until `PacketStateEndTime` and leave playback where there may be no pending target layer and the old visual should survive until removal. The same seam now also commits the pending visual exactly when the packet state-end window closes for activated packet reactors whose hit-layer path has already finished or never loaded, so the delayed target-state animation restart stays anchored to the simulator-owned rebuild seam rather than leaking through `VisualState` early. `Entities/ReactorItem.cs` still keeps a simulator-owned current layer position separate from the authored reactor coordinates, uses that transient position for view culling and draw-time frame culling, owns the transient layer alpha that `ReactorPool.cs` computes for packet enter/deactivation windows, and still mirrors the direct `GetHitProp` fallback split recovered in IDA instead of letting non-client proper-event cases bleed into `state/hit`. `MapSimulator.cs` still threads renderable reactor alpha from the pool onto the reactor item before visibility publication; `ReactorItem.Draw` still applies that alpha through the existing WZ-frame draw path while preserving the normal opaque `DrawObject` path when the layer is fully visible; and `Managers/RenderingManager.cs` still breaks equal reactor sort keys with `CurrentWorldY` and places reactor debug rectangles from `CurrentWorldX` / `CurrentWorldY`, so the simulator-owned layer position stays consistent across sorting, culling, draw, and debug presentation while `RelMove`-style playback is active. `Loaders/EffectLoader.cs` now keeps the exact raw WZ state branch and exact indexed proper-event branch that the client hands to `LoadLayer` instead of pre-normalizing those sources down to frame-shaped children before `ReactorItem` sees them, while still preserving the existing raw state-`hit` and root-`hit` branches. The same seam now approximates another client `LoadLayer` behavior more closely by letting lazy packet playback retry raw state/event sources through a nested `hit` branch when direct frames or nested default frames are absent, and `ReactorItem` now mirrors that fallback for duration recovery so an exact raw state/event source can still publish the authored nested-hit timing instead of collapsing to `0` during lazy packet-owned playback. Negative proper-event indices still replay the authored state branch when present and otherwise fall back to root `hit`; valid proper-event indices still prefer the exact state event branch, then state `hit`, then root `hit`; and out-of-range or non-state-backed proper-event indices still fall straight back to root `hit`, matching the native owner instead of replaying `state/hit`. Focused source coverage is now on disk in `UnitTest_MapSimulator/ReactorLayerBuildParityTests.cs` for the direct `GetHitProp` fallback split, nested-hit duration approximation, and the packet-owned pending-visual preservation plus state-end handoff now used by the simulator-owned loader seam. Remaining gap: the simulator still approximates `CAnimationDisplayer::LoadLayer` with `ReactorItem`-owned frame substitution instead of the client's COM layer object, it still approximates that loader with simulator-side frame selection rather than invoking a real client layer builder, it still does not drive `IWzVector2D::RelMove` directly, and moving reactors still ease a simulator-owned transient layer position instead of driving the underlying WZ vector/layer objects directly. | Without a dedicated loader row, reactor parity can look more complete than it is because backlog 7 already tracks packet ingress, timing, and local collision. The client still splits reactor ownership one layer deeper: packet handlers decide when the reactor changes, but `LoadReactorLayer` decides which state branch, hit prop, playback mode, sound, and final z/page the runtime actually consumes. Naming that owner keeps future work anchored to the real layer-build seam instead of continuing to overload the packet-runtime row. | reactor layer-build/runtime seam (`CReactorPool::LoadReactorLayer`, `CReactorTemplate::GetTemplateProp`, `CReactorTemplate::GetStateInfo`, `CReactorTemplate::GetHitProp`, `get_hittype_priority_level`, `CAnimationDisplayer::LoadLayer`, `play_reactor_sound`, simulator seam: `HaCreator/MapSimulator/Pools/ReactorPool.cs`, `HaCreator/MapSimulator/Entities/ReactorItem.cs`, `HaCreator/MapSimulator/Loaders/EffectLoader.cs`, `HaCreator/MapSimulator/Managers/RenderingManager.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `UnitTest_MapSimulator/ReactorLayerBuildParityTests.cs`, packet contrast: `HaCreator/MapSimulator/Interaction/PacketReactorPoolRuntime.cs`) |

### 4. Field-state and script-message parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field-owned script/message and object-state packet parity | The simulator still has the dedicated packet-owned field-state runtime at the `CField::OnPacket` seam for `OnFieldSpecificData` (`149`), `OnDesc` (`162`), `OnSetQuestClear` (`166`), `OnSetQuestTime` (`167`), and `OnSetObjectState` (`169`), plus the packet-owned script-message bridge at the `CScriptMan::OnPacket` seam for `OnScriptMessage` (`363`). The bridge still decodes the shared `speakerType` / `speakerTemplate` / `msgType` / `bParam` header confirmed in prior IDA work, resolves speaker NPC names from map/runtime or WZ string data, and keeps packet-authored `OnSay`, `OnSayImage`, `OnAskYesNo`, `OnAskMenu`, `OnAskText`, `OnAskNumber`, `OnAskQuiz`, `OnAskSpeedQuiz`, `OnAskAvatar`, `OnAskMembershopAvatar`, `OnAskPet`, `OnAskPetAll`, `OnAskSlideMenu`, and `OnAskBoxText` inside the existing packet-script seams instead of scattering them across unrelated simulator UI. The newer prompt families remain anchored to WZ-confirmed client UI owners rather than ad hoc simulator surfaces: `UIWindow(.img|2.img)/InitialQuiz`, `SpeedQuiz`, `SlideMenu`, `UtilDlgEx_Pet`, and `UtilDlgEx_MultiPetEquip` still supply the packet-authored metadata and framing, while avatar and pet choice rows still label indexed entries with WZ-backed item names when available. This pass keeps the earlier client-owned pet lookup fix in place and closes the owner-presentation gap without moving the seam: when `OnAskPet` / `OnAskPetAll` cannot resolve a packet cash serial through the live active-pet runtime, the bridge still falls back to the latest decoded `SetField` `CharacterData` cash inventory snapshot, keeps only pet-family cash items with real serial numbers, and surfaces those packet-owned candidates back through the prompt builder as `cash slot <n>` choices instead of dropping them as unresolved. The active-pet runtime branch still wins when a live summoned pet exists, so packet prompts keep their richer live pet name and slot metadata where the simulator can actually observe that state, while the authoritative `CharacterData` fallback still covers packet-authored serials that belong to the current character but are not presently represented by the local summoned-pet runtime. This pass keeps the dedicated `InitialQuizTimerRuntime` and `SpeedQuizOwnerRuntime` timeout-owner lifecycle fix in place as well: they no longer drop their owner snapshot exactly when the countdown reaches zero, so the existing timeout path can still observe the expired owner state, send the client-shaped empty opcode-`65` reply on timeout, and then clear the owner instead of tearing the owner down one step too early. Using WZ evidence from `UIWindow(.img|2.img)/SpeedQuiz` plus IDA evidence from `CWvsContext::OnInitialSpeedQuiz`, `CUISpeedQuiz::OnCreate`, `CUISpeedQuiz::Update`, and `CUISpeedQuiz::SendResult`, `OnAskSpeedQuiz` mode `0` still suppresses NPC-overlay mutation entirely like the other dedicated client-owner packets, syncs the packet-owned speed-quiz runtime, and opens a centered `CUISpeedQuiz`-style owner instead of keeping the prompt on the generic NPC overlay with a side preview. The owner still uses the WZ-backed `backgrnd` / `backgrnd2` / `backgrnd3` layers, numeric strip, and `BtOK` / `BtNext` / `BtGiveup` controls, mirrors the client `OnCreate` control positions recovered in IDA for the edit box and buttons, captures keyboard and mouse while active, allows answer text entry with the client `nHorzMax = 50` limit, submits the current answer through Enter, OK, or Next, submits an empty answer for Give Up and timeout, and clears the owner when the client mode-`1` close packet arrives. The outbound speed-quiz reply contract is also still on the client-owned wire shape: opcode `65`, message type `7`, then a Maple string payload from `CUISpeedQuiz::SendResult`, rather than the earlier simulator menu-style `accepted + int` encoding. `OnAskQuiz` keeps the already recovered `InitialQuiz` owner branch, and `OnAskPet` / `OnAskPetAll` still drop zero serials before building the choice list, keep the recovered no-dialog fallback branches by auto-closing with opcode-`65` status-`2` replies when the client exposes only the fallback path, and still auto-accept the first resolved pet serial with opcode-`65` status-`1` replies when the packet omits prompt text and the client would skip `UtilDlgEx_Pet` or `UtilDlgEx_MultiPetEquip` entirely. The script-message seam still consumes the decoded client-owner sync instead of discarding it: packet-authored quiz packets update `InitialQuizTimerRuntime`, speed-quiz packets update the dedicated speed-quiz owner runtime, packet-authored pet and slide-menu prompts now switch over to the existing dedicated packet-script owner runtime instead of reopening the generic NPC overlay, mode-`1` close packets still tear down the currently open owner branch, `/scriptmsg status` now surfaces the active dedicated owner alongside both quiz-owner countdown states, `/scriptmsg clear` clears all three owner families alongside the overlay, and the initial-quiz channel-restriction path still stays in sync when `CScriptMan::OnPacket` opens or closes the same `InitialQuiz` owner family. Manual packet exercise still has client-shaped `/scriptmsg quizclient`, `/scriptmsg quizclose`, `/scriptmsg speedquizclient`, `/scriptmsg speedquizclose`, and client-shaped `/scriptmsg slidemenuclient` helpers alongside the existing compact `/scriptmsg quiz` / `/scriptmsg speedquiz`, pet, petall, slidemenu, say, sayimage, yes-no, menu, avatar, membershop-avatar, text, number, and box helpers plus raw payload injection. `/scriptmsg slidemenu` still emits the real slide-menu message type (`15`), `OnAskSlideMenu` still distinguishes client-owned type-`0` `CSlideMenuDlgEX` versus type-`1` `CSlideMenuDlg` owners and auto-cancels unsupported type values, the client-shaped slide-menu helper still preserves the constructor-consumed `initialSelectionId` between the decoded owner type and button-info string instead of emitting the older malformed helper body, and prompt submission still builds opcode `65` replies for `OnAskYesNo`, `OnAskMenu`, `OnAskText`, `OnAskNumber`, `OnAskBoxText`, `OnSayImage`, `OnAskAvatar`, `OnAskMembershopAvatar`, `OnAskQuiz`, `OnAskSpeedQuiz`, `OnAskPet`, `OnAskPetAll`, and `OnAskSlideMenu`, records dispatch status on the packet-script runtime surface, and still exposes the loopback packet-reply transport plus the live official-session bridge and `/scriptmsg transport <status|start [port]|stop>` / `/scriptmsg session ...` controls. Using WZ evidence from `UIWindow2.img/UtilDlgEx_Pet`, `UIWindow.img/UtilDlgEx_MultiPetEquip`, and `UIWindow(.img|2.img)/SlideMenu/{0,1}`, the dedicated owner runtime now loads the authored pet/multi-pet background layers and slide-menu background animation branches, keeps selection ownership inside the packet-script seam, captures keyboard and mouse while active, supports left/right or up/down selection changes, confirms through Enter or click, cancels through Escape or click, routes the selected choice or cancel outcome back through the existing opcode-`65` reply encoder instead of reusing the generic NPC overlay submission path for those owner families, and now places the dedicated pet and slide-menu navigation / confirm / cancel controls from the WZ-authored button origins instead of generic centered rectangles. Type-`0` / type-`1` slide-menu cover bounds now also follow the authored WZ cover canvas origin, so the centered owner surface keeps the packet prompt inside the same owner-local control region the client assets define. On the field-state side, the runtime still binds map help text from map info, shows packet-authored field help overlays, tracks quest-timer start/end windows with WZ-backed quest names and `timerUI` metadata, clears packet-authored timers, appends active timer text into the quest UI surfaces, and routes named object-state flips through the existing tagged-object/runtime seam. The visible help and timer surfaces remain on WZ-backed `UtilDlgEx/notice` and `Quest/TimeQuest` assets including the quest timer alarm-clock icon families and time-gauge textures, and the `OnFieldSpecificData` seam still routes its structured Maple-string pair decode through the shared `PacketFieldSpecificDataCodec`, including prefix stripping for `field:`, `party:`, and `session:` owner hints before dispatching common packet-owned payloads into party-raid variables plus wrapper-owned `escortresult` and `huntingadballoon` state through the current special-field seams. Focused source-level regression coverage now includes `UnitTest_MapSimulator/PacketScriptDedicatedOwnerParityTests.cs` for the anchored WZ-origin bounds math that now drives packet-authored pet and slide-menu owner controls. Remaining gap: field-specific-data decoding is still structured-string based rather than client-owner-specific or byte-exact across all field owners; the pet prompt bridge now resolves the live active-pet runtime first, then live cash inventory slots, then the latest decoded `CharacterData` cash inventory fallback, but it still does not mirror the client's exact per-packet inventory ownership lookup when live cash slots lack authoritative serial metadata or when no authoritative `CharacterData` snapshot has been observed in the current session; the centered speed-quiz owner now keeps the timeout reply lifecycle aligned, but its body text, draw-time glyph placement, button-state transitions, and broader `CWndMan` focus/capture lifecycle are still simulator approximations rather than a byte-for-byte `CUISpeedQuiz::Draw` recreation; the slide-menu and pet prompt families now stay on dedicated WZ-backed owners instead of the NPC overlay, but their remaining row typography, recommend / choice choreography, animated background timing, and broader native `CSlideMenuDlg(EX)` / `UtilDlgEx_*` window lifecycle are still simulator approximations rather than byte-for-byte recreations; and the help plus quest-timer packet presentation still does not reproduce the client's exact animation cadence or all `Quest/TimeQuest` layout nuances. The dedicated quest-timer modeless owner itself is tracked separately in the progression/utility backlog so timer UI work does not stay duplicated here. | Map scripts and event fields are not driven only by NPC clicks or reactor collisions. Without this packet-owned bridge, parity work keeps re-implementing isolated outcomes while missing the client seam that coordinates post-entry scripted help dialogs, timed quest deadlines, quest-clear resets, script prompts, and `CMapLoadable::SetObjectState` updates for object-tag and map-state changes. This row is narrower on the script side because the simulator now keeps both quiz-owner families on recovered client owners instead of leaving speed-quiz on the generic overlay, no longer drops the recovered quiz-owner runtime handoff after decode, now preserves the timeout-to-empty-reply owner lifecycle for both quiz owners, still uses the client string-based speed-quiz reply contract, and now keeps packet pet plus slide-menu prompts on WZ-backed dedicated owner surfaces after resolving pet choices through the live active-pet runtime first with a `SetField` `CharacterData` cash-item fallback, while the separate quest-timer UI row keeps modeless timer-owner work anchored to the actual utility-window family. | field/script runtime bridge (`CField::OnPacket`, `CField::OnFieldSpecificData`, `CField::OnDesc`, `CField::OnSetQuestClear`, `CField::OnSetQuestTime`, `CField::OnSetObjectState`, `CScriptMan::OnPacket`) |

### 5. Packet-authored field messaging and feedback parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field chat, effect, warning, and timer packet parity | The simulator still keeps the dedicated packet-owned field-feedback runtime on the `CField::OnPacket` / `CUserLocal::OnPacket` seam for `OnGroupMessage` (`150`), `OnWhisper` (`151`), `OnCoupleMessage` (`152`), `OnFieldEffect` (`154`), the obstacle on/off and reset packets (`155`, `156`, `159`), `OnWarnMessage` (`157`), `OnPlayJukeBox` (`158`), transfer or summon-item failure notices (`160`, `161`, `164`), `OnDestroyClock` plus the Zakum/Horntail/Chaos Zakum/Hontale timer packets (`163`, `170`-`173`), `CField::OnClock`, and `CUserLocal::OnFieldFadeOutForce` (`174`). The seam still carries blacklist suppression, blocked-friend whisper filtering, whisper-result routing, subtype-`9` whisper follow transfer queuing, object-state clears through the dynamic-tag seam, summon/screen/reward visual callbacks, summon sound ownership, the client-shaped boss-timer chat path, the recovered `CCurseProcess` swindle-warning filter flow, and the recovered `CClock` realtime/countdown/event/Cake Pie owner behavior with the earlier event-board colors, window footprints, zero-second mode-`2` registration, event-clock replacement guard, and destroy-clock teardown. This run closes another slice of the recovered `CField::OnWhisper` owner inside the same seam instead of widening ownership: direct IDA re-check at `0x5448a0` confirms subtype `9` and subtype `72` do not share one flat chat path, because subtype `72` updates the user-list location owner without calling `CHATLOG_ADD`, while subtype `9` only attempts the chase handoff after the client rechecks `CField::GetFieldProp` for the target map before calling `SendTransferFieldRequest`. `MapSimulator.PacketOwnedFieldFeedbackParity.cs` and `PacketFieldFeedbackRuntime.cs` now keep the earlier reward-roulette suffix walk and also mirror more of that whisper-result routing: whisper find/reply text now formats through the client string-pool families instead of one generic sentence builder, hidden-field result `1` cases now fall back through the recovered hidden-field branch when the target map cannot be resolved through the simulator map cache seam, subtype `72` no longer fabricates a status-bar chat line, and subtype `9` only attempts the existing follow transfer queue when the target field resolves through that same packet-owned map lookup seam. Focused source coverage is now actually on disk in `UnitTest_MapSimulator/PacketFieldFeedbackParityTests.cs` for the shared reward-roulette suffix-order helper plus the new hidden-field whisper-result fallback and subtype-`72` no-chat routing. Remaining gap: the swindle-warning path still lacks byte-for-byte proof for the remaining live locale-specific DBCS cases the retail client sees under matching regional ACP settings; whisper chase is closer on result routing and field-prop admission, but it still ends at the simulator's existing map-change seam instead of the client's fuller `SendTransferFieldRequest` lifecycle and still does not reopen the native `CUIUserList` location surface for subtype `72`; screen-effect and reward-roulette playback still do not reconstruct the remaining `LoadLayer` flags, per-layer COM overlay handling, or the full byte-for-byte `ShowScreenEffect` / `Effect_RewardRullet` registration lifecycle beyond the recovered owner roots, offsets, repeated reward-family suffix walking, draw-order split, WZ-backed layered `a0`/`a1` alpha tweening, and layered `z` ordering; obstacle presentation still reuses the existing tag-driven object seam instead of the client dynamic-object visuals; summon-effect fallback remains approximate for ids outside the recovered generic family when the extracted `Summon.img` path the client would try first is missing; and the packet-owned `CClock` owner still approximates the client window lifecycle because the simulator mirrors recovered realtime normalization, countdown ownership, event-clock replacement guards, destroy-clock teardown, shared timerboard glyph usage, zero-second standard countdown registration, and the recovered event/Cake Pie window footprints without yet replaying the field-property `RestoreClock` reopen path, the full `CWnd::CreateWnd` helper or style matrix, or the native Cake Pie / event-board subclass visuals beyond the recovered bounds and event-board colors. | Visible packet-owned feedback is a distinct client-owned surface between gameplay, the status bar, map-state tags, local fade overlays, field music, and boss timers. Tracking it as its own seam keeps future parity work anchored to the dispatcher handlers that actually own chat feedback, warning dialogs, forced fades, obstacle toggles, jukebox swaps, transfer failures, and special-event timers instead of burying those effects inside unrelated rows. | packet-owned field feedback layer (`CField::OnPacket`, `CField::OnGroupMessage`, `CField::OnWhisper`, `CField::OnCoupleMessage`, `CField::OnFieldEffect`, `CField::OnFieldObstacleOnOff`, `CField::OnFieldObstacleOnOffStatus`, `CField::OnFieldObstacleAllReset`, `CField::OnWarnMessage`, `CField::OnPlayJukeBox`, `CField::OnTransferFieldReqIgnored`, `CField::OnTransferChannelReqIgnored`, `CField::OnSummonItemInavailable`, `CField::OnDestroyClock`, `CField::OnZakumTimer`, `CField::OnHontailTimer`, `CField::OnChaosZakumTimer`, `CField::OnHontaleTimer`, `CField::OnClock`, `CClock::OnCreate`, `CClock::SetClock`, `CClock::SetTimer`, `CClock::Update`, `CUserLocal::OnFieldFadeOutForce`, `MapSimulator.PacketOwnedFieldFeedbackParity.cs`, `MapleStoryStringPool.cs`, `UnitTest_MapSimulator/PacketFieldFeedbackParityTests.cs`, `Map/Obj/etc.img/timer/{backgrnd,backgrndhour}`, `Map/Obj/etc.img/clock/fontTime`) |

### 6. `CAnimationDisplayer` social/event animation-owner parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shared social/event animation-displayer parity | The simulator still keeps this row on an explicit shared animation-owner seam instead of leaving the family implicit across unrelated systems. Animation/AnimationEffects.cs still owns the CAnimationDisplayer-shaped user-state, follow, and transient-area registrations, the event side still preserves the earlier timing recovery from CMapLoadable::TransientLayer_NewYear, CMapLoadable::TransientLayer_FireCracker, CAnimationDisplayer::RegisterNewYearAnimation, and CAnimationDisplayer::RegisterFireCrackerAnimation, and the user-state side still stays close to the client-owned quest-delivery owner recovered from CUser::OnEffect cases 30/31 plus CAnimationDisplayer::Effect_QuestDeliveryItemUse: MapSimulator.AnimationDisplayerParity.cs still resolves quest-delivery layers WZ-first from Effect/OnUserEff.img/itemEffect/quest/<itemId>, still falls back to the only shipped quest-delivery effect asset present in the loaded data set (2430071) when simulator-owned placeholder item ids have no authored art, and still preserves the client arrive / wait / leave path shape through the recovered StringPool templates used by Effect_QuestDeliveryItemUse (0x1A05, 0x1A06, 0x1A07). This pass keeps the follow-side genOnMove behavior narrowed inside the same seam instead of widening ownership and now also locks the recovered helper behavior down with row-specific source coverage. WZ evidence from the extracted v95_2 Character data rechecked here still confirms the relevant authored item-effect metadata shapes include string-authored numeric fields on earring-family effect rows such as character/Accessory/01032053.img (left = "-10", genOnMove = 1), ordinary integer follow/emission/theta rows such as character/Ring/01112900.img (follow = 1, emission = 1, theta = 21), and non-ring genOnMove rows such as character/Cap/01002667.img and character/Longcoat/01051188.img. The WZ-backed CItemEffectManager::LoadItemEffect analogue therefore still parses info/effect follow metadata through the shared numeric reader that accepts both WZ ints and string-authored numbers, still resolves category 103xxxx earrings from the extracted character/Accessory/*.img family, still forwards current avatar facing into the runtime, still keeps authored genOnMove / bGenOnMove and bNoFlip / noFlip bits on the recovered follow definition, and now also keeps the client default `follow = 1` semantics when an equipped effect row omits that field entirely instead of flattening the missing case to absolute positioning. The same follow loader now also keeps the recovered offset fallback closer to `CItemEffectManager::LoadItemEffect`: authored follow-relative rows default their missing Y travel offset to `0`, while explicit non-follow rows still keep the recovered `-20` absolute fallback. Animation/AnimationEffects.cs keeps the earlier client-shaped random startup angle, generation-point churn, nTheta-style stepping, +10 owner-relative emission bias, full-vector relative follow-particle travel distance, and the recovered absolute fallback shape. The genOnMove path now also follows the client-owned CParticleEffect constructor plus CParticleEffect::OnChangeAction behavior recovered in IDA: genOnMove registrations can supply an owner action-state gate, MapSimulator.AnimationDisplayerParity.cs maps only client raw action ids 0, 1, 43, and 44 (walk1, walk2, ladder2, rope2) to the unpaused generation state, and AnimationEffects keeps its earlier position-delta fallback only for follow owners that cannot expose client action state. /socialanim follow <local|characterId> <on|off> [relative|absolute] still drives the same shared seam, the packet-owned follow sync in MapSimulator.UpdateLoop.cs still keeps a single shared follow registration alive across ordinary update ticks, the packet-owned quest-delivery interaction owner in MapSimulator.PacketOwnedUtilityParity.cs still clears and reuses the same user-state seam, and the packet-owned blow-weather owner in MapSimulator.PacketOwnedFieldUtilityParity.cs still clears and re-registers the New Year and firecracker transient-layer family so these owners stay centralized on CAnimationDisplayer rather than drifting back into unrelated packet rows. Generic temporary-stat user-state parity is unchanged from the earlier narrowing: remote users still prefer the decoded client-backed avatar-effect families from RemoteUserActorPool, and the local /socialanim userstate local on path still reuses SkillManager's active-buff family ordering before the generic OnUserEff.img/character fallback. Focused regression coverage is now actually on disk in `UnitTest_MapSimulator/AnimationDisplayerParityTests.cs` for the client default missing-follow behavior, the authored relative-position gate, and the corresponding implicit-offset fallback, alongside the earlier shared numeric-reader, follow helper, and travel-shape coverage. Remaining gap: the follow runtime is still not byte-for-byte with the client because the simulator still does not replay the full native item-effect loader and COM-layer registration lifecycle for every possible equipped effect family. Exact repeat-layer alpha / RelMove choreography, deeper per-layer offset handling beyond the now-recovered implicit-follow default plus current x/y fallback shape, broader live data-set cases that actually author bNoFlip/noFlip, and any future follow families that require more than the current WZ-backed category scan still remain approximated. On the user-state side, the stat-specific selection is still only as rich as the already decoded temporary-stat avatar-effect families: it still does not prove the client's exact cross-family priority for every coexistence case beyond the simulator's current status-bar-backed local ordering and existing remote candidate list, and it still does not recover distinct end-phase families for those temporary-stat registrations. | If this manager stays implicit, parity work can overstate progress in field feedback, relationship-overlay, and local-overlay rows while still missing the shared animation owner that actually registers, updates, and resets several visible social/event effect families. Tracking it explicitly keeps future work anchored to the same CAnimationDisplayer seam instead of scattering follow, New Year, firecracker, quest-delivery repeat effects, and fade-cleanup behavior across unrelated backlog buckets. | shared animation-owner layer (CAnimationDisplayer::RegisterUserStateAnimation, CAnimationDisplayer::RegisterNewYearAnimation, CAnimationDisplayer::RegisterFireCrackerAnimation, CAnimationDisplayer::RegisterFollowAnimation, CAnimationDisplayer::FOLLOWINFO::Update, CParticleEffect::CParticleEffect, CParticleEffect::OnChangeAction, CAnimationDisplayer::ResetLocalFadeLayer, CAnimationDisplayer::Effect_QuestDeliveryItemUse, CUser::OnEffect, CItemEffectManager::LoadItemEffect, HaCreator/MapSimulator/Animation/AnimationEffects.cs, HaCreator/MapSimulator/MapSimulator.PacketOwnedLocalOverlayParity.cs, HaCreator/MapSimulator/MapSimulator.PacketOwnedFieldUtilityParity.cs, HaCreator/MapSimulator/MapSimulator.PacketOwnedUtilityParity.cs, HaCreator/MapSimulator/MapSimulator.AnimationDisplayerParity.cs, HaCreator/MapSimulator/MapSimulator.UpdateLoop.cs, HaCreator/MapSimulator/Interaction/AnimationDisplayerQuestDeliveryStringPoolText.cs, UnitTest_MapSimulator/AnimationDisplayerParityTests.cs) |

### 7. Packet-authored field utility parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field utility, quickslot-init, and foothold-info packet parity | `HaCreator/MapSimulator` keeps the dedicated `PacketFieldUtilityRuntime` plus `MapSimulator.PacketOwnedFieldUtilityParity.cs` owner seam for `CField::OnPacket` field utilities: `OnBlowWeather` (`158`) drives packet-authored weather/message overrides, `OnAdminResult` (`160`) stays on the client-owned chat/minimap path (including subtype-`41` minimap-hide persistence), `OnQuiz` (`161`) resolves `Etc/OXQuiz.img` question/answer text and renders through `UI/StatusBar2.img/mainBar/notice` with the client-shaped short notice lifetime and question-side 30-second timer ownership, `OnStalkResult` (`172`) updates stalk/follow markers, `CQuickslotKeyMappedMan::OnInit` (`175`) hydrates packet DIK-style key codes into both quickslot labels and live `PlayerInput` bindings, and still mirrors the client redraw/save side effects back into the simulator-owned keybinding surfaces by refreshing `UIWindowManager`'s live window-key map and persisting the updated simulator binding snapshot through the existing func-key config owner seam, including routing the simulator helper reset path back through that same redraw/save surface. `OnFootHoldInfo` (`176`) / `OnRequestFootHoldInfo` (`177`) still keep packet decode/build helpers plus a client-shaped foothold response payload builder (`(state, curX, curY, reverseV, reverseH)`), and the broader packet-owner transport pattern now holds there all the way through deferred bridge delivery too: opcode-`270` foothold replies first try the live official-session bridge, then fall back to the generic local-utility outbox, then queue deferred bridge/outbox delivery when direct transport is unavailable, with the all-path failure summary now reporting both deferred queue statuses instead of dropping that tail state on the floor. This pass keeps the earlier direct client extraction for admin-result wording (`StringPool::ms_aString` ids `0xA4`, `0xA6`, `0xA9`, `0xAA`, `0xAC`, `0xAD`, `0xB0`, `0xB1`, `0xBDF`, and `0xBE0`) and the runtime-first foothold reply rule: once `DynamicFootholdSystem` has any current platforms, `BuildPacketOwnedFootholdSnapshot` emits live runtime entries plus WZ-authored default placeholders for remaining authored contracts, and only falls back to cached packet entries when the runtime has no platforms at all. That keeps `/fieldutility status` and live-session opcode-`270` injection aligned with active simulator foothold positions, states, and reverse flags instead of replaying stale packet movement or names. The authored-name path remains WZ-backed and is tightened further here through both extracted-contract review and row-specific tests. `DynamicFootholdField` still keeps the existing WZ-first canonical name resolution (`name` / `objName` / `tag`, then `oS/l0/l1/l2`) plus the broader inbound alias set for layer/object coordinate keys and `piece`-tagged coordinate variants, while packet-owned snapshot names continue to prefer the WZ object contract key itself for outbound foothold replies even when authored overrides exist. In the extracted tutorial-family dynamic-foothold contracts rechecked here, `Map/Map0/003000000` object `0/obj/0` authors `oS=acc9`, `l0=cannonTuto`, `l1=ship`, `l2=8`, `x=2323`, `y=-126`, `piece=0`; `Map/Map0/003000100` object `4/obj/10` authors `oS=connect`, `l0=rope`, `l1=49`, `l2=0`, `x=708`, `y=-78`, `piece=10`; `Map/Map0/003000200` object `2/obj/14` authors `oS=guide`, `l0=tutorial`, `l1=key`, `l2=0`, `x=-1114`, `y=122`, `piece=14`, `tags=byeMonkey`; `Map/Map0/003000300` object `7/obj/0` authors `oS=guide`, `l0=tutorial`, `l1=key`, `l2=4`, `x=-748`, `y=265`, `piece=0`, `tags=cannonTuto1`; `Map/Map0/003000400` object `5/obj/3` authors `oS=guide`, `l0=tutorial`, `l1=key`, `l2=1`, `x=970`, `y=185`, `piece=3`, `tags=cannonGet`; and `Map/Map0/003000500` object `5/obj/27` authors `oS=guide`, `l0=tutorial`, `l1=key`, `l2=5`, `x=-515`, `y=96`, `piece=27`, `tags=cannonGet`, with no authored `name`, `objName`, or singular `tag` override recovered on those dynamic objects. That keeps opcode-`270` snapshot names on the verified `oS/l0/l1/l2[/piece]/x,y` form, still omits the synthetic `/piece/0` segment for default-zero pieces, still keeps nonzero `piece` tags in outbound names, and still treats plural `tags` as inbound alias metadata rather than an outbound-name override. `ApplyPacketOwnedFootholdInfo` still clears and rebuilds the serial-to-name cache on each packet update so subsequent official-session replies do not preserve stale authored names from older foothold packets when the runtime cannot supply a current platform snapshot. Focused coverage is now actually on disk in `UnitTest_MapSimulator/PacketFieldUtilityParityTests.cs` for admin-result subtype `41`, quickslot-init default/custom decode, zero-piece and nonzero-piece snapshot-name shaping, tag-alias retention without outbound-name override, authored `name` / `objName` / `tag` precedence versus outbound object-contract naming, foothold-info payload round-tripping of the verified nonzero-piece authored name, official-session foothold response tuple encoding, and the direct-send / fallback-send / deferred-bridge / no-transport response dispatch shaping. Persistent minimap open/closed state is intentionally no longer treated as part of this row: the targeted `CWvsContext::OnPacket` re-check shows that `OnMiniMapOnOff` belongs to backlog 8's explicit minimap owner row because it writes `CWvsContext` state and calls `CUIMiniMap::ReloadMiniMap`, while this field-owner row keeps only the `OnAdminResult` subtype-`41` hide pulse. Remaining gap: outbound foothold response injection is wired through the same live/deferred transport surfaces as the other local-utility owners, but it is still not proven end-to-end against a captured official session in this workspace, and the broader extracted dynamic-foothold map set outside the rechecked tutorial-family contracts still has not been exhaustively re-verified for rarer real WZ-authored override cases beyond the now-tested owner precedence rules. | If these handlers stay collapsed into a generic packet assumption, parity work will miss several visible client-owned flows that are neither ordinary UI windows nor general field notices: packet weather and admin-result prompts, quiz ownership, stalk-result feedback, quickslot-init sync, and foothold-data round trips all have their own dispatcher slots in the field owner and should not disappear behind unrelated rows. Splitting RPS out keeps this bucket focused on true field-owner utilities instead of mixing dialog-minigame ownership back into the generic packet section, separating persistent minimap owner reload state into backlog 8 avoids tracking the same minimap behavior under both field and utility owners, and separating `CQuickslotKeyMappedMan` from backlog 8's `CFuncKeyMappedMan` row keeps the two keymap owners from being mistaken for duplicate work. | packet-owned field utility layer (`CField::OnPacket`, `CField::OnBlowWeather`, `CField::OnAdminResult`, `CField::OnQuiz`, `CField::OnStalkResult`, `CQuickslotKeyMappedMan::OnInit`, `CField::OnFootHoldInfo`, `CField::OnRequestFootHoldInfo`, `UnitTest_MapSimulator/PacketFieldUtilityParityTests.cs`) |

### 8. Stage-transition and map-load presentation parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned stage-transition and map-load presentation parity | `PacketStageTransitionRuntime` remains the dedicated `/stagepacket` seam for packet types `141` to `146`, the login/session relay, packet-owned back-background fades, ITC/Cash Shop stage opens, named map-object visibility, and the official `CStage::OnSetField` non-character-data path. This row intentionally owns the packet dispatch and cross-stage load handoff itself; backlog 11 only keeps the pre-field login-side selectors, roster, and entitlement consequences that consume this handoff after control has already returned to the login shell. `CStage::OnSetField` character-data handling is deeper again inside that same seam instead of leaving the `CharacterData::Decode` graph opaque once the stat head, money, slot bytes, and inventories are decoded: the direct snapshot path now keeps the client-owned skill-record (`dbcharFlag & 0x100`), skill-expiration (`dbcharFlag & 0x200`), and skill-cooltime (`dbcharFlag & 0x4000`) sections distinct in client order; still preserves the unresolved `0x8000` span as opaque instead of reclassifying it as decoded cooldown data; recovers the later pre-map-transfer quest-string (`0x10000`) and short-filetime (`0x20000`) groups in that same client order; and then follows the fixed client tail order through mini-game (`0x400`), relationship (`0x800`), map-transfer (`0x1000`), New Year card (`0x40000`), quest-ex (`0x80000`), Wild Hunter (`0x200000` for job `33xx`), quest-complete (`0x400000`), and visitor-quest (`0x800000`) records instead of treating those post-map-transfer groups as count-only approximations. The two-int `0x100000` pre-inventory header is still semantically unresolved, but the stage-transition snapshot now preserves both raw values in the same pre-inventory position the client decodes, and the packet-owned logout-gift owner strings now consistently describe the trailing `CWvsContext::OnSetLogoutGiftConfig` cache as the full client 16-byte block (`m_bPredictQuit` plus three commodity serial numbers) rather than the older 12-byte shorthand. This pass keeps the object-semantics improvements from `GW_NewYearCardRecord::Decode` (`0x4f8bf0`), `GW_WildHunterInfo::Decode` (`0x4f2bc0`), `CStage::OnSetField` (`0x71a0a0`), `CharacterData::Decode` (`0x4fcce0`), and `CWvsContext::OnSetLogoutGiftConfig` (`0x9cfa30`) and now locks them down with row-specific regression coverage: New Year card names and content still follow the client's fixed ANSI char-buffer truncation (`13` / `13` / `121` including terminators), the decoded card summary line still matches the client-owned `[sn:state] ...` formatting including the trailing newline, the Wild Hunter snapshot still exposes the packed mode-byte split as riding-type versus selected captured-mob index while preserving the five decoded mob ids, and the `SetField` tail still preserves the full client 16-byte logout-gift cache as `CWvsContext::m_bPredictQuit` plus the three commodity serial numbers. Focused source coverage is now actually on disk in `UnitTest_MapSimulator/PacketStageTransitionSetFieldCharacterDataParityTests.cs` plus `UnitTest_MapSimulator/MapTransferAuthoritativeBootstrapLeadInParityTests.cs` for the official `SetField` character-data path and the fallback authoritative bootstrap seam, including direct assertions for the `0x100000` pre-inventory raw header position, the preserved opaque `0x8000` bytes, the distinct skill / skill-expiration / skill-cooltime ordering, the post-map-transfer quest-complete and visitor-quest records, the client-shaped New Year card formatting and truncation, the Wild Hunter packed-mode split, the full logout-gift tail decode, and authoritative bootstrap recovery across both an exact `0x8000` lead-in shape and a preserved opaque `0x8000` span sitting between the client-known pre-map-transfer groups and the bootstrap books. `MapTransferAuthoritativeBootstrapDecoder` still exists as the fallback seam, but its known-leading finder and summary text now stay aligned with the broader client-authored lead-in order through the pre-inventory two-int header, skill, skill-expiration, skill-cooltime, preserved opaque `0x8000` span, quest-string, short-filetime, mini-game, and relationship groups before the bootstrap books, and its trailing logout-gift skip still matches the full `m_bPredictQuit` + `3 * commoditySN` cache size rather than only the commodity trio. Remaining gap: the simulator still does not model the full byte-exact `CharacterData::Decode` section graph end to end, so the still-unmirrored `dbcharFlag & 0x8000` section semantics, deeper skill-record semantics beyond the currently recovered raw level maps/counts, the exact identity of the `0x100000` two-int pre-inventory header, richer `GW_WildHunterInfo` behavior beyond the recovered mode-byte split and mob ids, and any rarer trailing caches beyond the now-decoded logout-gift block still remain approximated rather than fully client-authored. | If these owners stay implicit, future parity work can keep overloading login flow, cash-shop stage work, or generic field-effect rows while still missing the actual packet dispatch seam that swaps stages and mutates map presentation during load/runtime. Tracking them explicitly keeps stage changes, packet-owned back effects, and map-object visibility toggles anchored to the client owners that `CField::OnPacket` actually calls, while leaving backlog 11 responsible only for the pre-field UI/runtime that consumes those transitions once login regains control. | packet-owned stage/map-load layer (`CField::OnPacket`, `CStage::OnPacket`, `CStage::OnSetField`, `CharacterData::Decode`, `GW_CharacterStat::DecodeMoney`, `GW_NewYearCardRecord::Decode`, `GW_WildHunterInfo::Decode`, `CWvsContext::OnSetLogoutGiftConfig`, `CStage::OnSetITC`, `CStage::OnSetCashShop`, `CMapLoadable::OnPacket`, `CMapLoadable::OnSetBackEffect`, `CMapLoadable::OnSetMapObjectVisible`, `CMapLoadable::OnClearBackEffect`, `UnitTest_MapSimulator/PacketStageTransitionSetFieldCharacterDataParityTests.cs`, `UnitTest_MapSimulator/MapTransferAuthoritativeBootstrapLeadInParityTests.cs`) |

### 9. Context-owned stage-period transition parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Context-owned stage-period change parity | The simulator still keeps this owner on its dedicated `MapSimulator.ContextOwnedStagePeriodParity.cs` seam instead of burying it under `/stagepacket`, but this pass closes one more client-backed catalog/input gap. `ContextOwnedStagePeriodRuntime` stays wired into the live update loop and `/stageperiod`; validation still builds a concrete stage-period catalog from `Etc/StageSystem.img`; and that catalog still captures each discovered period's `backColor`, `aStageBackImg`, keyword list, and enabled quest ids with affected-map augmentation from `Etc/StageAffectedMap.img` plus extra aliases from `Etc/StageKeyword.img` when those files are present. The new parity work here corrects the stage-period resource lookups so the seam now resolves the native `CStageSystem::IterateStageSystemClient` string-pool ids for `Etc/StageSystem.img` and `Etc/StageKeyword.img` through `MapleStoryStringPool` overrides instead of drifting onto the generated-table `/cubecomplete` and off-by-one neighbors, while `Etc/StageAffectedMap.img` now also resolves through the matching affected-map slot. The previously covered cache/runtime behavior remains in place: `/stageperiod clear` flushes the simulator-owned keyword, quest, and theme-mode caches themselves; stage-keyword augmentation honors per-period numeric nodes; affected-map augmentation only activates `questID`-gated rows when the current enabled-quest cache contains that quest id; live affected-map resolution consults the simulator quest-state provider and re-evaluates those memberships during update so local quest-state flips can swap current-map stage back layers without waiting for another `CWvsContext::OnStageChange`; and the live back-color / `aStageBackImg` cache still mirrors `CStageSystem::BuildCacheData`'s `nStagePeriod > 0` rule so mode `0` retains keyword/quest cache updates without driving visual overrides. Successful `CWvsContext::OnStageChange` payloads still update simulator-owned keyword, quest, back-color, back-image, and affected-map caches before scheduling the field-owned `CStage::FadeOut` -> `CMapLoadable::ReloadBack` -> `CStage::FadeIn` handoff; `ReloadContextOwnedStagePeriodBackLayers` still prefers stage-system `aStageBackImg` descriptors over authored map back layers when the current map is inside the active affected-map set; and the world clear color still follows the active stage `backColor` during those swaps. Focused regression coverage is now on disk in `UnitTest_MapSimulator/ContextOwnedStagePeriodParityTests.cs` for the client-backed stage-system / stage-keyword / affected-map path resolution. Remaining gap: the simulator still builds this catalog from extracted WZ property names rather than replaying the client's original `CStageSystem` load/build object allocation path byte-for-byte, the `StageKeyword` / `StageAffectedMap` augmentation still does not reproduce every client-side cache mutation or `randTime` / timer-style gate from the native owner, stage back image application still routes through `BackgroundInfo` / `MapSimulatorLoader` instead of the client's native `CMapLoadable::MakeBack` object creation sequence, and the fade path still approximates the underlying Gr2D red-tone / green-blue-tone choreography instead of owning the exact `CStage::FadeOut` / `CStage::FadeIn` object chain. | If this owner stays untracked, stage-transition parity can look complete once `CField::OnPacket` and `CStage::OnSetField` are covered even though the client still has a separate context-owned path for live stage-period swaps and background reloads. Naming it here keeps future work anchored to the real owner instead of hiding it inside generic set-field or field-effect notes. | context-owned stage-transition layer (`CWvsContext::OnStageChange`, `CStageSystem::SetStagePeriod`, `CStage::FadeOut`, `CMapLoadable::ReloadBack`, `CStage::FadeIn`, `HaCreator/MapSimulator/Interaction/ContextOwnedStagePeriodRuntime.cs`, `HaCreator/MapSimulator/Interaction/ContextOwnedStagePeriodCatalog.cs`, `HaCreator/MapSimulator/MapSimulator.ContextOwnedStagePeriodParity.cs`, `UnitTest_MapSimulator/ContextOwnedStagePeriodParityTests.cs`) |

### 10. Initial-quiz utility owner parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Context-owned Initial Quiz owner parity | The simulator now moves this owner family further past the old side-preview/runtime-only seam. Using WZ evidence from `UIWindow(.img|2.img)/InitialQuiz` plus the targeted IDA results for `CWvsContext::OnInitialQuiz` (`0x9ffad0`) and `CUIInitialQuiz::{OnCreate,Draw,Update}` (`0x78e720`, `0x78e9a0`, `0x79c390`), packet-authored mode-`0` quiz payloads no longer open the NPC overlay variant at all: `PacketScriptMessageRuntime` keeps the packet bridge and prompt context alive while suppressing overlay mutation for the client-owned quiz branch, outbound opcode-`65` replies for message type `6` follow the client owner path by encoding the entered Maple string instead of the older simulator numeric helper payload, and `MapSimulator.InitialQuizParity.cs` renders a centered `CUIInitialQuiz`-style owner that uses the WZ-backed layered background, button states, animated `ani` frames, and the correct `InitialQuiz`-specific digit strips from `UIWindow2.img`. This pass tightens both the surface and the child-control behavior further around the confirmed `OnCreate` and `Draw` seams: the layered background canvases now draw at native WZ canvas size instead of being stretched into the owner bounds, the centered bounds use the loaded `UIWindow2.img/InitialQuiz/backgrnd` dimensions when available, the `UIWindow.img` fallback path now picks its older `num2` question-number strip when the `UIWindow2.img` `number` strip is unavailable, and the owner now mirrors the client-confirmed `edit=(109,157,150,13)` plus `BtOK=(241,199)` child layout while honoring the WZ-authored `BtOK/keyFocused` state instead of treating the button as hover-or-pressed only. The local owner focus model now starts on the answer field like `CUIInitialQuiz::OnCreate`, lets `Tab` or direct clicks move focus between the edit lane and `BtOK`, uses the WZ `keyFocused` canvas when the button owns keyboard focus, suppresses the text cursor while focus is on the button, dims the local edit lane when it no longer owns focus, preserves the stored `BtOK` pressed state through mouse release so a normal enabled press/release dispatches the client-owned text-submission path instead of dropping the click before response encoding, and now keeps the `3959` Answer label visible in the timeout state the same way `CUIInitialQuiz::Draw` still renders it after `Update` hides the edit control. The owner lifetime remains tied to the `CWvsContext` seam instead of a process-global timer bucket: `InitialQuizTimerRuntime` binds the live quiz owner to the current local character id, carries that binding through packet and packet-script client-owner opens, reports the bound/runtime character state in status output, clears the owner plus edit state when the runtime character changes underneath it so stale quiz state does not leak across local-context swaps, shares the same singleton reopen gate across both raw initial-quiz payload decode and packet-script client-owner sync so an already-live `CUIInitialQuiz` owner is no longer silently replaced by a second mode-`0` open, preserves the existing local edit text when that client-owned singleton gate rejects a reopen, and now arms one renderable timeout frame before the empty-string auto-submit so the disabled/timeout surface can actually mirror the `Update` plus `Draw` split instead of disappearing immediately. The duplicate Initial Quiz packet-preview surface is now removed from `MapSimulator.PacketScriptOwnerVisualParity.cs`, so this owner no longer keeps a second client-owned `InitialQuiz` art path alive in parallel after the dedicated centered utility owner took over. Focused regression coverage is now on disk in `UnitTest_MapSimulator/InitialQuizOwnerParityTests.cs` for the client-owner singleton reopen suppression, timeout-close staging, and the helper-level caret/timer behavior around the centered owner. Remaining gap: the simulator still does not recreate the native `CWndMan` capture/focus handoff or the actual `CCtrlEdit` / `CCtrlButton` / IME lifecycle byte-for-byte, the centered owner still uses simulator-side sprite-font text helpers rather than the client's exact `IWzCanvas::DrawTextA` font/render calls, and the deeper control tree is still approximated rather than routed through a real window/control stack even though the old side-preview branch is gone. | If this owner stays folded into the general script-message row, quiz parity looks closer than it is because packet decode and a WZ-backed preview already exist. Naming the dedicated `CUIInitialQuiz` owner keeps future work anchored to the real client window lifecycle instead of stopping at packet sync, timer state, and side-preview rendering. | context-owned quiz utility owner (`CWvsContext::OnInitialQuiz`, `CUIInitialQuiz::OnCreate`, `CUIInitialQuiz::Draw`, `CUIInitialQuiz::Update`, `HaCreator/MapSimulator/Interaction/InitialQuizTimerRuntime.cs`, `HaCreator/MapSimulator/Interaction/PacketScriptMessageRuntime.cs`, `HaCreator/MapSimulator/MapSimulator.InitialQuizParity.cs`, `HaCreator/MapSimulator/MapSimulator.PacketScriptOwnerVisualParity.cs`, `UnitTest_MapSimulator/InitialQuizOwnerParityTests.cs`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.
6. Reactor loader pass:
   Split reactor state-layer rebuild and hit-prop ownership out from the existing packet-runtime row before more reactor work gets documented only as packet timing or collision polish.
7. Scripted field-state pass:
   Add the packet-owned bridge for field help messages, quest timers, quest-clear resets, and object-state flips before layering more one-off script behavior onto NPC or reactor seams.
8. Packet-feedback pass:
   Add the packet-owned field messaging, warn-dialog, forced-fade, obstacle, field-effect, and boss-timer surface before more status-bar or field-overlay polish is tracked as isolated UI work.
9. Field-utility packet pass:
   Add the packet-owned weather/admin-result/quiz/stalk-result, quickslot-init, and foothold-info surface before those handlers get misfiled under unrelated UI or field rows.
10. Stage/map-load packet pass:
   Add the packet-owned `CStage` and `CMapLoadable` bridge for field or Cash Shop or ITC stage switches plus back-effect/object-visibility mutations before those transitions get scattered across login, field-effect, or cash-stage rows.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
