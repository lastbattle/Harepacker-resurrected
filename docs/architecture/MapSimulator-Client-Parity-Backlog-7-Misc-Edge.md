# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Animation/AnimationEffects.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CReactorPool::OnPacket` at `0x6cf9b0`
- `CReactorPool::OnReactorEnterField` at `0x6cf490`
- `CReactorPool::OnReactorLeaveField` at `0x6ccea0`
- `CReactorPool::OnReactorMove` at `0x6cd110`
- `CReactorPool::OnReactorChangeState` at `0x6ccd60`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUserLocal::OnSetDirectionMode` at `0x9054f0`
- `CUserLocal::OnSetStandAloneMode` at `0x905550`
- `CActionMan::LoadNpcAction` at `0x420ae0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CField::OnFieldSpecificData` at `0x52a7e0`
- `CField::OnSetQuestTime` at `0x52b790`
- `CField::OnSetQuestClear` at `0x52c870`
- `CField::OnDesc` at `0x5313d0`
- `CField::OnSetObjectState` at `0x539890`
- `CField::OnTransferFieldReqIgnored` at `0x52f3b0`
- `CField::OnTransferChannelReqIgnored` at `0x52f5f0`
- `CField::OnGroupMessage` at `0x535490`
- `CField::OnCoupleMessage` at `0x5357f0`
- `CField::OnFieldObstacleOnOff` at `0x535a80`
- `CField::OnFieldObstacleOnOffStatus` at `0x535b00`
- `CField::OnFieldEffect` at `0x53b790`
- `CField::OnBlowWeather` at `0x5468f0`
- `CField::OnAdminResult` at `0x53bc20`
- `CField::OnQuiz` at `0x537a90`
- `CField::OnWarnMessage` at `0x538160`
- `CField::OnPlayJukeBox` at `0x537940`
- `CField::OnWhisper` at `0x5448a0`
- `CField::OnSummonItemInavailable` at `0x52f7b0`
- `CField::OnStalkResult` at `0x539910`
- `CField::OnDestroyClock` at `0x52a7c0`
- `CQuickslotKeyMappedMan::OnInit` at `0x6c6130`
- `CField::OnFootHoldInfo` at `0x53a810`
- `CField::OnRequestFootHoldInfo` at `0x52ddd0`
- `CField::OnZakumTimer` at `0x530cc0`
- `CField::OnHontailTimer` at `0x530e70`
- `CField::OnChaosZakumTimer` at `0x531020`
- `CField::OnHontaleTimer` at `0x5311d0`
- `CField::OnPacket` at `0x546d50`
- `CStage::OnPacket` at `0x71b0b0`
- `CStage::OnSetField` at `0x71b150`
- `CStage::OnSetITC` at `0x71c000`
- `CStage::OnSetCashShop` at `0x71bfa0`
- `CMapLoadable::OnPacket` at `0x61fd80`
- `CMapLoadable::OnSetBackEffect` at `0x620160`
- `CMapLoadable::OnSetMapObjectVisible` at `0x61fe40`
- `CMapLoadable::OnClearBackEffect` at `0x620150`
- `CScriptMan::OnPacket` at `0x6de360`
- `CUserLocal::OnFieldFadeOutForce` at `0x9057f0`
- `CAnimationDisplayer::RegisterUserStateAnimation` at `0x444d30`
- `CAnimationDisplayer::RegisterNewYearAnimation` at `0x4556f0`
- `CAnimationDisplayer::RegisterFireCrackerAnimation` at `0x45a6e0`
- `CAnimationDisplayer::RegisterFollowAnimation` at `0x45ac50`
- `CAnimationDisplayer::ResetLocalFadeLayer` at `0x43b6f0`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Interaction and NPC parity

- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CActionMan::LoadNpcAction` at `0x420ae0`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CReactorPool::OnPacket` at `0x6cf9b0`
- `CReactorPool::OnReactorEnterField` at `0x6cf490`
- `CReactorPool::OnReactorLeaveField` at `0x6ccea0`
- `CReactorPool::OnReactorMove` at `0x6cd110`
- `CReactorPool::OnReactorChangeState` at `0x6ccd60`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUser::PetAutoSpeaking` at `0x8deb10`

Notes:
`CUserLocal::TalkToNpc` is the first concrete client seam for future NPC bridge work. `CUserLocal::Update` also shows client-side quest-alert refresh calls, emotion reset / balloon lifetime upkeep, timer refresh, timed direction-mode release, and status-bar chat logging, while `CUserLocal::TryAutoRequestFollowCharacter` covers automatic escort/follow requests and `CUser::PetAutoSpeaking` covers one of the idle-feedback surfaces tied to those broader interaction loops. A follow-up IDA scan also showed that reactor ownership is split across both local collision and a packet-owned pool: `CReactorPool::OnPacket` dispatches reactor change-state (`334`), move (`335`), enter-field (`336`), and leave-field (`337`) traffic into dedicated handlers instead of leaving all reactor lifecycle behavior on `CUserLocal::CheckReactor_Collision`.

### 2. Reactor layer-build and hit-prop ownership discovered by targeted `CReactorPool` decompile

- `CReactorPool::LoadReactorLayer` at `0x6ce630`

Notes:
The latest targeted decompile exposes a distinct reactor owner that should not stay buried inside the broader packet-lifecycle row. `CReactorPool::LoadReactorLayer` lazy-resolves `CReactorTemplate`, looks up the current state branch by stringified state id, builds the visible state layer through `CAnimationDisplayer::LoadLayer`, switches between repeat and stop playback from `CReactorTemplate::STATEINFO::m_bRepeat`, preserves timed movement against `tStateEnd` for moving states, auto-selects hit reactions through `get_hittype_priority_level` plus `CReactorTemplate::GetHitProp` when no proper event index has been fixed yet, formats and plays the per-state reactor hit sound through the StringPool template at id `2121`, and then assigns the final layer depth from the closest foothold page or z-mass plus the reactor template layer mode. That makes reactor parity split the same way as the other action-loader families already called out elsewhere in the backlog set: `CReactorPool::OnPacket` owns lifecycle and timing, while `LoadReactorLayer` owns state-layer rebuild, hit-prop arbitration, sound, and final z placement.

### 3. NPC action-loader and client-action-set parity discovered by targeted `CActionMan` scan

- `CActionMan::LoadNpcAction` at `0x420ae0`

Notes:
The targeted `CActionMan` lookup exposed another NPC-side owner that was still only implied by broader talk or actor rows. `CActionMan::LoadNpcAction` caches by NPC template, action, and client action-set index; it also has a special `nClientActionIdx == -2` branch that calls `CNpcTemplate::CalcClientActionSetIdx` before choosing the action family, applies the client default frame delay of `180` when delay data is absent, and can append reversed frames when the authored data marks that path. That means NPC parity is still missing a dedicated action-loader seam for client action-set selection and frame-table construction instead of only needing more dialogue or field-placement polish.

### 4. Packet-authored direction and stand-alone mode parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnSetDirectionMode` at `0x9054f0`
- `CUserLocal::OnSetStandAloneMode` at `0x905550`

Notes:
The existing backlog tracks delayed `TryLeaveDirectionMode` release, but the direct `CUserLocal::OnPacket` re-check exposed the client-owned state-entry seam that actually drives those releases. `OnSetDirectionMode` decodes an enable byte plus a delay, clears `m_bAfterLeaveDirectionMode`, enters direction mode immediately when the packet enables it or when the delay is non-positive, and otherwise arms a delayed leave by storing `m_tAfterLeaveDirectionMode = delay + get_update_time()`. `OnSetStandAloneMode` is a separate packet-owned toggle that writes the decoded byte into the `CWvsContext` stand-alone flag rather than behaving like another window-local direction-mode alias. That owner pair is not named anywhere else in the current backlog set.

### 5. Field-state and script-message parity

- `CField::OnPacket` at `0x546d50`
- `CField::OnFieldSpecificData` at `0x52a7e0`
- `CField::OnDesc` at `0x5313d0`
- `CField::OnSetQuestClear` at `0x52c870`
- `CField::OnSetQuestTime` at `0x52b790`
- `CField::OnSetObjectState` at `0x539890`
- `CScriptMan::OnPacket` at `0x6de360`

Notes:
This packet cluster is a distinct client owner that the current backlog did not call out directly. `CField::OnPacket` dispatches field-specific data, help-message dialogs, quest-timer refresh and clear, and object-state flips from one place, while `CScriptMan::OnPacket` owns script-message delivery separately from NPC click handling or reactor collision. The dedicated quest-timer UI owner is tracked separately in the progression/utility backlog so this row stays focused on the packet source and field/script runtime.

### 6. Packet-authored field messaging and feedback parity

- `CField::OnTransferFieldReqIgnored` at `0x52f3b0`
- `CField::OnTransferChannelReqIgnored` at `0x52f5f0`
- `CField::OnGroupMessage` at `0x535490`
- `CField::OnWhisper` at `0x5448a0`
- `CField::OnCoupleMessage` at `0x5357f0`
- `CField::OnSummonItemInavailable` at `0x52f7b0`
- `CField::OnFieldEffect` at `0x53b790`
- `CField::OnFieldObstacleOnOff` at `0x535a80`
- `CField::OnFieldObstacleOnOffStatus` at `0x535b00`
- `CField::OnFieldObstacleAllReset` at `0x52c830`
- `CField::OnPlayJukeBox` at `0x537940`
- `CField::OnWarnMessage` at `0x538160`
- `CField::OnDestroyClock` at `0x52a7c0`
- `CField::OnZakumTimer` at `0x530cc0`
- `CField::OnHontailTimer` at `0x530e70`
- `CField::OnChaosZakumTimer` at `0x531020`
- `CField::OnHontaleTimer` at `0x5311d0`
- `CUserLocal::OnFieldFadeOutForce` at `0x9057f0`

Notes:
This is another packet-owned surface that the current backlog set does not describe explicitly. IDA shows `CField::OnPacket` and `CUserLocal::OnPacket` owning a broad feedback layer that sits between ordinary gameplay and the existing UI rows: group, whisper, and couple message routing feed status-bar chat and whisper-candidate state; field-effect packets drive screen effects, field sounds, BGM changes, boss HP tags, reward-roulette visuals, and object-state changes; obstacle, warning, and forced-fade packets update map presentation directly; and dedicated boss-timer plus transfer-ignored handlers show that several visible notice/timer flows are owned by packets rather than generic field-rule polling.

### 7. `CAnimationDisplayer` social/event animation ownership discovered by function scan

- `CAnimationDisplayer::RegisterUserStateAnimation` at `0x444d30`
- `CAnimationDisplayer::RegisterNewYearAnimation` at `0x4556f0`
- `CAnimationDisplayer::RegisterFireCrackerAnimation` at `0x45a6e0`
- `CAnimationDisplayer::RegisterFollowAnimation` at `0x45ac50`
- `CAnimationDisplayer::ResetLocalFadeLayer` at `0x43b6f0`

Notes:
The function scan surfaced another client-owned surface that the backlog was not naming anywhere else. These registrations show that the client keeps several visible social/event overlays under `CAnimationDisplayer` ownership rather than scattering them only across field-message handlers or one-shot item-effect helpers: follow effects, New Year card midpoint effects, firecracker bursts, avatar-adjacent user-state layers, and local fade cleanup all pass through one animation manager. A focused source pass in `HaCreator/MapSimulator` shows partial simulator coverage through `Animation/AnimationEffects.cs` for follow animations plus packet-owned fade handling, but not a single backlog row that treats this manager as its own parity seam.

### 8. Packet-authored field utility parity discovered by direct `CField::OnPacket` scan

- `CField::OnBlowWeather` at `0x5468f0`
- `CField::OnAdminResult` at `0x53bc20`
- `CField::OnQuiz` at `0x537a90`
- `CField::OnStalkResult` at `0x539910`
- `CQuickslotKeyMappedMan::OnInit` at `0x6c6130`
- `CField::OnFootHoldInfo` at `0x53a810`
- `CField::OnRequestFootHoldInfo` at `0x52ddd0`

Notes:
The same `CField::OnPacket` decompile exposes another client-owned surface that is still absent from the backlog set. These handlers are not well-described by the existing scripted-field or feedback rows: they cover packet-authored weather/broadcast effects, GM or admin-result dialogs, OX-style quiz state, stalk/follow-result notices, a dedicated quickslot-keymap init path, and foothold-info request/reply exchange. This quickslot owner should not be conflated with the separate `CFuncKeyMappedMan` bootstrap tracked in backlog 8: the current IDA pass reconfirmed that `CQuickslotKeyMappedMan::OnInit` lives at `0x6c6130`, while `CFuncKeyMappedMan::OnInit` and the pet-consume init pair live in their own packet-owned keymap owner at `0x568c30`, `0x5688c0`, and `0x5688f0`. `CRPSGameDlg::OnPacket` was part of the same scan, but that unique-modeless microgame owner is now tracked in the dedicated minigame backlog (`MapSimulator-Client-Parity-Backlog-9-Special-Fields-Minigames.md`) instead of being double-counted here. That keeps this bucket focused on true field-owner utilities between generic field messaging and the broader progression/utility window backlog.

### 9. Stage-transition and map-load presentation parity discovered by direct `CField::OnPacket` decompile

- `CStage::OnPacket` at `0x71b0b0`
- `CStage::OnSetField` at `0x71b150`
- `CStage::OnSetITC` at `0x71c000`
- `CStage::OnSetCashShop` at `0x71bfa0`
- `CMapLoadable::OnPacket` at `0x61fd80`
- `CMapLoadable::OnSetBackEffect` at `0x620160`
- `CMapLoadable::OnSetMapObjectVisible` at `0x61fe40`
- `CMapLoadable::OnClearBackEffect` at `0x620150`

Notes:
The same `CField::OnPacket` decompile also hands packet families `141` to `146` to two owners that must stay explicit across backlog boundaries. `CStage::OnPacket` owns the stage-switch family for field, ITC, and Cash Shop transitions instead of leaving those jumps implied by login or UI code, while `CMapLoadable::OnPacket` owns packet-authored map back-effects, object-visibility toggles, and back-effect clears instead of treating them as generic field-effect fallout. That is a separate packet surface above the more specific field-feedback, field-state, and cash-stage rows; backlog 11 now links this same handoff from `CLogin::OnPacket` and keeps detailed implementation ownership merged here.

### 10. Passenger passive-move parity discovered by direct `CUserLocal::OnPacket` scan

- `CUser::OnPassiveMove` at `0x8dea10`

Notes:
The same `CUserLocal::OnPacket` re-check also exposed one missing follow-adjacent owner that should not stay buried under request and failure handling alone. `CUser::OnPassiveMove` only runs while `m_dwDriverID` is set, and then forwards the decoded movement straight into the passenger-side `CMovePath::OnMovePacket` seam on the attached vector controller. That makes it the client-owned movement handoff for attached passengers after the request and accept path has already succeeded, not just another follow-request dialog branch.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Interaction and NPC parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | NPC talk flow | Nearby NPC clicks now open a real simulator dialogue overlay fed from loaded NPC string data including NPC `info/speak` idle lines, quest `Say.img` conversations preserve multi-page WZ ordering, root-level `yes`/`no` branches are now attached to the final numbered page, quest entries now open directly on the authored WZ conversation pages instead of always forcing a synthetic summary page first, common MapleStory script markup is stripped into readable text with additional `#q` including uppercase `#Q`, numeric `#s`, bare plural-suffix `#s`, `#m`, `#z`, `#v`, quest-item count `#c`, mob-name `#o`, quest-reference `#y`, quest-state `#u`, and quest-selected `#M` or `#a` or `#x` token cleanup backed by loaded quest, item, and mob data where possible, standalone client prompt markers such as `#E` and `#I` are now stripped from read-only simulator text, quest reward-summary markers such as `#Wbasic#`, `#Wselect#`, and `#Wprob#` are now stripped from simulator-visible text instead of leaking raw WZ tags into NPC quest pages, inline `#L` menu options now surface as clickable read-only choices that honor WZ `stop/<page>/answer` metadata using the client-authored 1-based choice position, continue along the numbered conversation flow only for the authored correct answer, and still fall back to authored choice-position branch keys when a `stop/<page>/<branch>` node does not reuse the raw `#L` id, quest dialogue entries now also reuse named WZ `stop/*` and `lost` branches such as `item`, `mob`, `monster`, `quest`, `default`, `info`, and lost-item fallback text instead of flattening those failure states into generic simulator prose, and now also checks authored `job` branch keys when present before falling back to generic locked-quest prose, started quests now also surface WZ `stop/npc` guidance when you revisit the starter NPC instead of falling back to generic progress text, nested authored branch conversations reached through WZ `stop/*`, `yes`, `no`, or other branch nodes now reuse the same multi-page, final-page root-choice, and inline-selection parsing rules as top-level `Say.img` pages instead of dropping those branch-root choices, authored empty `yes` or `no` handoff branches now remain visible as clickable placeholder choices instead of disappearing when WZ leaves the branch body empty for later script execution, authored `stop` metadata no longer leaks into the overlay as a fake visible `Stop` button, authored `answer` bookkeeping and known non-dialogue metadata such as `illustration` no longer surface as fake visible branch choices, item-gated quest conversations now fall back to authored `lost` text whenever the specific `stop/item` branch is absent instead of only when every required item count has dropped to zero, branch-only authored pages now stay in the overlay as actual clickable choice pages instead of being dropped when their visible prose strips to empty, packet-owned quest-result modals now keep those same authored choice-only pages instead of silently filtering them back out after formatting, inline menu labels now keep their raw WZ text until final page formatting so the same live quest-item-count token context used by the page body also resolves inside the choice labels, metadata-only `Say.img` string nodes such as direct stage `info` markers no longer surface as fake visible dialogue pages, recursive numeric conversation containers now also contribute authored `stop/*` and `lost` metadata instead of dropping nested branch guidance when WZ stores the conversation under deeper `0/0/...` wrappers, and this pass now also re-selects authored `Say.img` conversation variants when a stage stores multiple numeric children gated by direct WZ `npc`, `job`, or `quest` metadata, with the same runtime selector reused by nearby-NPC quest pages and packet-owned quest-result modals instead of always sticking to the first pre-parsed page set. True script execution, broader automatic branch conditions beyond the current named WZ stop-branch mapping plus authored choice-position fallback and the direct `npc`/`job`/`quest` variant matcher, richer dynamic quest-script variable evaluation beyond the current WZ-backed token fallbacks, action or check-script conversation execution beyond these read-only WZ branches, and full client conversation layouts are still missing | Core interaction now covers a closer read-only version of client conversation flow, including authored page-first quest conversations, nested WZ branch conversations that now keep their own final-page root choices instead of flattening to plain text, choice-only authored menu pages that remain navigable after formatting strips their visible `#L` markup, packet-owned quest-result modals that now reuse that same choice-page visibility rule instead of re-dropping those authored menu pages, multi-step selection menus that respect WZ-authored answer metadata and its 1-based authored choice ordering instead of advancing on every missing branch or comparing against the raw `#L` id, additional WZ-authored wrong-answer branches whose `stop/<page>/<branch>` keys follow authored menu position instead of the raw `#L` id, the collision case where a correct authored choice now keeps the numbered continuation even when another wrong-answer branch reuses that same numeric key, more of the client-side WZ-backed failure and return-to-NPC text, runtime quest item-count and quest-state token rendering across both quest-detail text and authored `Say.img` conversation pages plus inline choice labels, cleaner MapleStory token rendering across lowercase and uppercase quest-name tokens, mob, pluralized item or mob mentions, selected-quest, quest-reference, stray prompt markers, and reward-summary category tags, no longer exposes `stop` or `answer` bookkeeping as user-facing branch choices, no longer misstates branch-root pages by borrowing text from child `yes` or `no` paths, no longer leaks metadata-only stage strings such as `Say.img/3360/0/info = \"0.000000\"` into the overlay as bogus dialogue, now preserves authored empty `yes` or `no` handoff choices as explicit simulator placeholders instead of dropping them, now uses authored lost-item fallback dialogue more often when a quest item is missing, now also checks authored `job` branch keys for start-time job and branch locks before skipping straight to generic fallback prose, now keeps nested `stop/*` and `lost` metadata reachable even when WZ wraps the authored conversation under deeper numeric containers, and now follows direct WZ `npc`/`job`/`quest` stage-variant metadata when authored `Say.img` roots gate alternative numeric conversation branches. The remaining gap is narrowed to true script execution, wider conditional branch selection beyond the current named WZ stop-branch mapping plus the newly added direct `npc`/`job`/`quest` selector, deeper dynamic script-variable resolution beyond the current WZ-backed token fallbacks, post-action or check-script execution, broader metadata-driven conversation variant selection beyond those currently parsed direct WZ keys, and broader client conversation window parity | player/NPC/UI layer (`CUserLocal::TalkToNpc`) |
| Partial | NPC action-loader and client-action-set parity | NPC loading now routes through a dedicated `NpcClientActionSetLoader` seam from `LifeLoader.CreateNpcFromProperty` instead of treating action-family construction as an implicit side effect of generic NPC setup. The loader now resolves WZ root versus `condition*` action-set containers, keeps the client-shaped automatic `nClientActionIdx == -2` entry point, chooses the first action set without quest-condition rows as the current `CNpcTemplate::CalcClientActionSetIdx` approximation, loads only the selected set's actions, applies the client default frame delay of `180`, appends reversed interior frames when the authored WZ action publishes `zigzag = 1`, and now caches the materialized frame lists per `(templateId, clientActionSetIndex, actionName)` so repeated NPC loads stop rebuilding the same selected action frames each time. Focused source coverage now exists in `UnitTest_MapSimulator/NpcClientActionSetLoaderTests.cs` for automatic action-set selection, root-versus-condition action-set partitioning, zigzag reverse-frame append shape, and cache reuse. The remaining gap is narrowed to the exact client numeric action-id table that `CActionMan::LoadNpcAction` keys before WZ lookup, and fuller `CNpcTemplate::CalcClientActionSetIdx` runtime predicates beyond the currently implemented WZ-visible quest-condition versus no-quest-condition set selection. | NPC parity now names and implements the loader owner that sits beneath talk flow and field rendering, so future passes no longer have to infer frame-family selection from broader interaction work alone. The remaining divergence is narrower and explicit: the simulator still needs the client's exact action-id lookup table and any additional non-WZ or non-quest action-set predicates before this seam can be called fully client-matched. | NPC action-loader layer (`CActionMan::LoadNpcAction`, `CNpcTemplate::CalcClientActionSetIdx`) |
| Partial | Quest lists and quest-only actions | Nearby NPC overlays now list WZ-backed available, locked, in-progress, and completable quests, keep accept/completion state plus quest mob counts, picked-up quest item counts, and quest action item grants/consumes for the simulator session, gate startable quests on `Check.img/0/item` requirements, and now also mirror additional common quest gates from WZ by surfacing `Check.img/0/pop`, trait-min requirements such as `charismaMin` or `craftMin`, `Check.img/0/skill` acquire gates including common bare-`id` entries with no explicit `acquire` flag, `Check.img/0/job` ancestor or branch matches such as a root magician gate accepting current magician advancements instead of only exact job ids, `Check.img/0/subJobFlags` explorer branch gates such as beginner-only Dual Blade or Cannoneer starts, `Check.img/*/pet` plus `petRecallLimit` or `petAutoSpeakingLimit` active-pet constraints, newly wired `Check.img/*/pettamenessmin` and `pettamenessmax` gates, `Check.img/*/item/*/secret` hidden-item requirements including common rows that omit `count` and therefore implicitly require one item such as `Quest/Check.img/10945/0/item/*` and `10945/1/item/*`, and `Check.img/1/endmeso` costs through locked quest rows, NPC quest pages, quest detail requirement lines, completion progress, and accept or complete actions, while common quest actions now also apply `Act.img/skill` skill-level grants plus direct `masterLevel` and `onlyMasterLevel` session updates, `Act.img/sp` per-session SP rewards for the current job tab, trait EXP rewards from `Act.img/*EXP`, `Act.img/buffItemID` consumable-style buff rewards through the existing item-effect seam, WZ pet-action rewards such as `Act.img/pettameness` and `petspeed` through the active pet runtime plus quest detail or NPC reward text surfacing, paired `Act.img/message` plus `Act.img/map` metadata through NPC quest pages, quest detail reward text, reward lines, and accept or completion feedback, WZ `Act.img/npcAct` metadata such as `Act.img/2165/1/npcAct = "quest"`, `Act.img/3340/1/npcAct = "act3340"`, or `Act.img/10931/1/npcAct = "boom"` through quest detail and reward text plus NPC accept or completion feedback animation selection, now filtering out common WZ zero-sentinel `Act.img/info` strings such as `0`, `000`, or `000000` instead of surfacing them as bogus authored text, plus EXP, fame, next-quest notices, linked quest-state mutations, WZ `Act.img/item/*/resignRemove` cleanup on give-up, and reward-item `jobEx` gating for common explorer subjob branches such as Dual Blade and Cannoneer, with quest reward, SP, and skill job filters now resolving root-job and class-branch matches instead of only exact job ids while preserving WZ `job = 0` beginner entries inside multi-value job arrays such as `Act.img/6000/1/skill/0/job`, quest accept and completion actions pre-resolving their WZ reward items and blocking on live inventory-space failures when an inventory runtime is present instead of silently shunting those rewards into quest-tracked counts, and WZ `Act.img/item` choice groups now no longer silently grant the first reward when multiple eligible `prop = -1` entries remain after job or gender filtering, instead surfacing those pending choices through a simulator-owned reward-choice prompt on both quest-window and NPC quest actions while still auto-resolving single remaining eligible choice rows, quest reward application now also treats late live-inventory add failures as hard blockers instead of silently downgrading those rewards back into quest-tracked counts after the action has already been validated and rolls back earlier live-inventory grants from that same quest action instead of leaving a partial reward state behind when a later add fails, and quest action consume rows with negative `Act.img/item/*/count` values now feed accept or completion gating, locked-quest issue text, quest detail requirement lines, progress ratios, progress totals, NPC quest pages, quest-window actions, quest alarms tied to tracked item changes, and pre-action consumption with rollback if a later reward add fails instead of being treated as best-effort side effects after the action already passed validation. This pass closes another concrete action-surface gap in the same seam: quest-detail reward summaries, reward lines, NPC action-detail pages, and packet-result action notices now all keep the parsed `Act.img` metadata for linked quest-state mutations, `nextQuest`, authored `npcAct`, pet tameness/speed rewards, and map-only metadata in sync instead of each dropping a different subset of the already loaded action data. Remaining gap: full script branching, broader skill-book style handling beyond direct skill/master-level updates, remaining rarer nonstandard `Act.img` action families outside these common seams, and wider inventory-backed quest parity such as richer per-slot inventory failure handling beyond simple accept-or-block checks are still incomplete | Progression-facing NPC parity now has a broader WZ-backed simulator baseline for both start and completion requirements plus common reward mutations, so fame, trait, skill-gate, root-job start gating, explorer subjob start gating, beginner-inclusive job-array reward filtering, skill-level and master-level grants, SP, meso-cost, item, chained-quest state feedback, give-up item cleanup, bare-`id` skill acquire gates, root-job or class-branch reward filtering, the common `prop = -1` choice-reward surface, the common `buffItemID`, `message`, and `map` metadata path, and now WZ pet quest requirements, tameness rewards, speed rewards, authored NPC action metadata, hidden `Check.img/item/secret` requirements, linked quest mutations, and next-quest pointers stay aligned across the NPC overlay, quest detail, and packet-result notice surfaces. This pass specifically tightens the presentation seam against authored `Act.img` data already present in the runtime, for example `quest/Act.img/2165/1/npcAct = "quest"`, `quest/Act.img/3340/1/npcAct = "act3340"`, `quest/Act.img/10931/1/npcAct = "boom"`, and `quest/Act.img/3706/1` carrying paired `buffItemID`, `message`, and `map` entries. The remaining gap is narrowed to script-driven branches, broader skill-book style quest rewards beyond the current direct skill/master-level flow, remaining rarer nonstandard `Act.img` action families outside these common seams, and deeper per-slot inventory semantics beyond the current accept-or-block plus transactional late-add-failure guard | NPC/UI layer |
| Partial | Auto-follow, escort, and passenger passive-move requests | Escort mobs still mirror the client-backed `TryAutoRequestFollowCharacter` auto-attach gate, and the simulator-owned local follow seam still covers more of the `CWvsContext::SendFollowCharacterRequest`, `CUserLocal::OnFollowCharacterFailed`, `CUser::OnFollowCharacter`, and `CUserLocal::OnKey` path. The latest IDA re-check also shows that the client keeps a separate passenger-movement owner after attach succeeds: `CUser::OnPassiveMove` only runs while `m_dwDriverID` is set, then forwards the packet into the attached passenger `CMovePath::OnMovePacket` seam instead of treating passenger motion as a side effect of request/accept state alone. This pass keeps the existing pending-driver, throttle, blocker, attach, detach, withdraw, failure, and passenger-slot parity, and now also wires the client `Follow Request` option (`1014`) into the same owner chain: authoritative `SetField` client-option payloads hydrate the committed option value onto `OptionMenuWindow`, escort auto-follow stops issuing requests when that option is off, and the shared follow-prompt helper refuses incoming `/localutility follow ask <requesterId|name>` or synthetic `/localutility packet followask` opens while disabled instead of bypassing the option owner entirely. The left/right attached-release seam also clears the simulator's pending passive-transfer request on the same `CUserLocal::OnKey` edge that already drives `SendFollowCharacterRequest(0,0,1)`, keeping the local portal handoff flag closer to the client branch that clears `m_bTryPassiveTransferField` during follow release. The local attached-passenger seam is broader now too: instead of hard-snapping the player onto the driver's current world position every update, the simulator now runs attached local playback through a dedicated passive-follow resolver that samples the remote driver's queued movement snapshot when available, reuses the shared passenger offset owner for ladder and ground spacing, and applies that sampled passive action, facing, and velocity onto the local player through a packet-owned passive-move helper. Source-side regression coverage now exists in `UnitTest_MapSimulator/PacketOwnedLocalFollowPassiveMoveResolverTests.cs` for movement-snapshot sampling plus fallback resolution. The remaining gap is narrowed to the still synthetic packet owner for opening the incoming Yes/No follow prompt, the still command-owned manual request owner instead of the client `HandleRButtonClk` context-menu path, the still unconfirmed byte-exact withdraw payload contract beyond the opcode-owned seam, the missing direct local packet decoder for `CUser::OnPassiveMove` itself, and the fact that the shared remote riding mirror is still reconstructed from the available mount, temp-stat, and action owners rather than a directly decoded remote `m_nRidingVehicleID` field | Escort-style encounters already rejected the client-invalid midair, map-disabled, and local-user-invalid auto-attach cases, and this pass now keeps the client follow-request option plus the newly explicit passenger move-path owner on that same parity story instead of leaving passenger motion implied by the request handshake alone. The simulator still models much of the interaction closely enough to preserve client-shaped request throttling, follow-failure clearing, attach acknowledgment, local-player snap-back versus transfer detach behavior, packet-authored attach or detach playback for the local passenger path, remote passenger ownership on the shared runtime, live-session outbound request or accept injection for opcode `134`, an explicit outbound withdraw lane for opcode `135`, a separate client-shaped attached-release request for `134(0,0,1)`, native arrow-key-triggered release on the same `CUserLocal::OnKey` seam the client uses, inbound official replay of `193` and `270`, a closer active-mounted blocker than the earlier broad "mount equipped" approximation, a client-option-backed gate for both escort auto-follow and incoming follow prompts, and now a dedicated local passive-follow playback owner instead of a pure attach-time snap. The remaining gap is narrower: a confirmed client-backed owner for opening the incoming Yes/No follow prompt itself, the exact withdraw-packet body or caller contract beyond the now-modeled opcode lane, the explicit right-click/context-menu request owner, the direct packet decoder and byte contract for `CUser::OnPassiveMove` rather than the new resolver-backed playback seam, and a directly decoded remote riding-vehicle field instead of the current shared-owner reconstruction still need future packet or session work | `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedStageTransitionParity.cs`, `EscortFollowController.cs`, `HaCreator/MapSimulator/UI/Windows/OptionMenuWindow.cs`, `HaCreator/MapSimulator/Interaction/LocalFollowCharacterRuntime.cs`, `HaCreator/MapSimulator/Interaction/PacketOwnedLocalFollowPassiveMoveResolver.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `UnitTest_MapSimulator/PacketOwnedLocalFollowPassiveMoveResolverTests.cs`, field interaction layer (`CUserLocal::TryAutoRequestFollowCharacter`, `CWvsContext::SendFollowCharacterRequest`, `CUserLocal::OnFollowCharacterFailed`, `CUser::OnFollowCharacter`, `CUser::OnPassiveMove`, `CMovePath::OnMovePacket`, `CUserLocal::OnKey`, `CUserLocal::HandleRButtonClk`) |
| Partial | Packet-owned reactor-pool parity discovered by IDA function scan | This row now carries opcode-framed replay, shared field-scoped ingress, live-session bridging, and a closer packet-owned animation clock on top of the existing packet-owned `ReactorPool` seam instead of staying loopback-only. WZ-first validation still anchors both spawn and hit timing: map reactor entries in `map/Map/Map0/*/reactor/*` expose packet-compatible fields such as `id`, `x`, `y`, `f`, `name`, and optional `reactorTime` (for example `map/Map/Map0/001000000/reactor/0` publishes `id=1002009`, `x=610`, `y=254`, `f=0`, empty `name`, and `reactorTime=90`), while reactor template `reactor/1002009/0/hit/*` publishes two `150ms` hit frames that now feed packet-owned teardown timing. Client evidence from `CReactorPool::OnPacket` (`0x6cf9b0`) plus `OnReactorEnterField` (`0x6cf490`), `OnReactorChangeState` (`0x6ccd60`), `OnReactorMove` (`0x6cd110`), `OnReactorLeaveField` (`0x6ccea0`), `Update` (`0x6cefc0`), and `LoadReactorLayer` (`0x6ce630`) now drives the simulator-owned phase clocks in `Pools/ReactorPool.cs` alongside the packet decode seam in `Interaction/PacketReactorPoolRuntime.cs`. `/reactorpacket` still accepts opcode-framed `packetclientraw <hex>` input, `Managers/ReactorPoolPacketInboxManager.cs` still validates 2-byte opcode-framed client packets for `334` to `337`, the older shared-ingress replay gap stays closed through `Interaction/PacketFieldIngressRouter.cs` plus `TryApplyPacketOwnedFieldScopedPacket` in `MapSimulator.PacketOwnedFieldStateParity.cs`, and `/fieldstate session [status|discover|start|startauto|stop]` plus `/reactorpacket session` still share `Managers/PacketFieldOfficialSessionBridgeManager.cs` for live ingress ownership instead of splitting reactor traffic onto a second owner. This pass closes most of the remaining visual teardown gap by adding WZ-backed one-cycle remaining-state timing plus authored hit-duration timing to packet-owned change-state and leave-field handling: packet reactors now keep the client next-tick fallback for zero `tHitStart`, defer change-state activation until the authored hit phase completes, stop leave-field removals on the current cycle instead of deleting on the same tick, and only remove after the deferred `+400` fallback or the authored hit frames complete. Focused regression coverage now includes `UnitTest_MapSimulator/Interaction/PacketOwnedReactorTimingTests.cs` for the client-shaped hit-start, leave fallback, and animation-end timing helpers. Remaining gap: exact COM-layer completion polling and byte-exact `GetHitProp` / hit-type priority selection inside `LoadReactorLayer` are still approximated by WZ frame-duration timing instead of mirroring the client object state byte-for-byte. | Without this row, reactor parity looks more complete than it is because the current backlog only covers the local collision and activation half. The client also owns a separate packet lifecycle for reactor spawn, movement, state changes, and teardown, and that needs to be tracked explicitly so future work lands on the real owner instead of overloading the local `ReactorPool` touch path. | packet-owned reactor runtime (`CReactorPool::OnPacket`, `CReactorPool::OnReactorEnterField`, `CReactorPool::OnReactorChangeState`, `CReactorPool::OnReactorMove`, `CReactorPool::OnReactorLeaveField`, simulator seam search: `HaCreator/MapSimulator/Pools/ReactorPool.cs`) |
| Partial | Direction-mode release timing | NPC dialogue overlays now enter a simulator direction-mode lock that blocks local gameplay input without dropping into free-camera mode, closing those scripted overlays releases control on a delayed timer modeled after `CUserLocal::TryLeaveDirectionMode`, the overlay-dismissal click is now consumed so it cannot leak into same-frame NPC or portal interactions before the delay expires, special-field scripted dialogs are advanced before world interaction handlers so wedding ceremony field dialogs can claim that same blocking owner on the very frame they appear instead of leaking one frame of movement or click-through before the delayed-release path engages, the delayed-release path now uses an explicit scripted-owner registry instead of only a fixed visible-window list so the tracked NPC or scripted launch points feed it directly for Cash Shop or MTS preview, trunk, item maker, MapleTV, memo send and package dialogs, the social-room preview surfaces for miniroom, player shop, hired merchant, and trading-room, NPC-owned item-upgrade launches and their Vega Spell child window when that child is opened from the already locked upgrade flow, explicit special-field owner tokens for the wedding dialog, memory game blocker, and tournament match-table dialog, inherited utility or community windows opened while a scripted lock is already active such as memo inbox, guild BBS, party or guild search, guild skill, family chart or tree, messenger, and the guild-management child surfaces for guild rank, guild mark, and guild creation agreement, packet-owned quest delivery and class-competition placeholder launches, plus the skill-window ride shortcut into Character Info, and known script-launched utility surfaces that still route through `UIWindowManager.ShowWindow` now also feed that same delayed-release owner path through a manager-level pre-show hook so status-bar community opens, status-bar character-info opens, skill-window guild-skill opens, skill-window ride opens, character-info party, family, miniroom, player-shop, hired-merchant, and trading-room launches, plus equivalent manager-routed reopen paths for Cash Shop, MTS, MapleTV, memo, guild, guild-rank, guild-mark, guild-create-agreement, family, quest-delivery, class-competition, social-room, radio, and packet-owned Aran skill-guide windows keep direction mode alive even after the original scripted owner has already started its release delay. This pass now also routes registered-window visibility transitions through that same pre-show seam instead of only explicit `Show()` callers, wires the soft keyboard's registered show path into the callback too, treats the Cash Shop wish-list child surface plus the wedding wish-list child surface as implicit delayed-release owners alongside the existing wedding invitation window, routes the direct registered skill-window Aran guide opener through the manager show seam as well, adds the packet-owned logout-gift owner plus the packet reward-result and random-meso-bag result notices to the same inherited-owner registry so `CWvsContext::OnLogoutGift`-style refreshes and the newer packet-owned result surfaces can continue holding direction mode when they replace an already tracked scripted parent, and now routes the packet-owned anti-macro challenge and notice owners through that same tracked `ShowWindow(...)` seam instead of raw `window.Show()` calls so inherited scripted locks survive those packet refreshes too. The remaining gap is narrowed to scripted owner surfaces that are not registered with `UIWindowManager` (or otherwise cannot publish a manager-owned visibility transition), plus launch paths that still bypass both the manager seam and the shared tracked `ShowWindow(...)` helper entirely | Scripted UI sequences now keep their dismiss or expiry transitions inside the scripted lifecycle instead of accidentally re-triggering world interactions while control is still supposed to be locked, and this pass moves the modal ownership closer to the client seam by making both the active scripted field runtimes and the in-flow packet, skill, utility-community, direct registered window launches, registered visibility-toggled launches, inherited guild-management child launches, inherited cash-shop or wedding child windows, the packet-owned anti-macro owners, and the packet-owned logout-gift or reward-result follow-up owners explicitly hold direction mode instead of relying only on a broad visibility sweep. The remaining gap is narrowed to unregistered or otherwise non-manager scripted owner surfaces, plus nonstandard visibility paths that bypass both the registered window visibility seam and the simulator's tracked `ShowWindow(...)` path | `GameStateManager.cs`, `DirectionModeWindowOwnerRegistry.cs`, `UIWindowManager.cs`, `UIWindowBase.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedLogoutGiftParity.cs`, `MapSimulator.PacketOwnedAntiMacroParity.cs`, `MapSimulator.GuildManagementParity.cs`, `SpecialFieldRuntimeCoordinator.cs`, `SpecialEffectFields.cs`, `UnitTest_MapSimulator/DirectionModeWindowOwnerRegistryTests.cs` (`CUserLocal::TryLeaveDirectionMode`, `CWvsContext::OnLogoutGift`) |
| Implemented | Packet-authored direction-mode and stand-alone control parity | The simulator now carries a dedicated packet-owned local-control layer instead of folding everything into the inferred window lock: `GameStateManager` separately tracks scripted-owner direction mode versus packet-owned direction mode, packet-authored `SetDirectionMode` writes now clear any pending packet release first, apply immediate enable or immediate disable when the delay is non-positive, and otherwise keep the packet-owned direction-mode bit alive until the stored delayed release expires in `UpdateDirectionMode`, while `SetStandAloneMode` now writes a separate runtime stand-alone flag that status surfaces and later owners can inspect without pretending it is another visible window owner. That stand-alone bit now also participates in the simulator's inherited direction-mode owner routing, so later modeless owners opened through the existing `ShowWindowWithInheritedDirectionModeOwner` and implicit-owner seams can re-enter the tracked direction-mode path after a packet-authored `SetStandAloneMode(254)` write even when no synthetic visible owner was created. This pass also removes the row's synthetic-id-only packet gap by teaching the local-utility seam to decode real opcode-framed `CUserLocal::OnPacket` traffic: `LocalUtilityPacketInboxManager` now accepts `packetclientraw` lines, validates the 2-byte opcode, and forwards live client opcodes such as `253` and `254` with their payloads instead of requiring synthetic `1013/1014` ids, while a new `LocalUtilityOfficialSessionBridgeManager` can proxy a live Maple session and mirror inbound `SetDirectionMode(253)` and `SetStandAloneMode(254)` packets into the same existing utility handler path. The local-utility command surface and help text now expose both `packetclientraw` and `/localutility session ...` control so this row can be exercised through opcode-framed local replay or a confirmed live inbound proxy rather than only through synthetic loopback aliases. Focused regression coverage now includes `UnitTest_MapSimulator/GameStateManagerTests.cs` for the inherited-owner predicate that keeps packet direction mode and stand-alone control wired into later owner inspection. | If this owner stays folded into the existing release-timing row, future work can overstate direction-mode parity while still missing the packet branch that actually turns the mode on, schedules a delayed off transition without a visible owner, or flips the separate stand-alone control flag in `CWvsContext`. Tracking it explicitly keeps future scripted-control and packet-driven interaction work anchored to the real `CUserLocal::OnPacket` owner instead of only the simulator's inferred window-lock path. | packet-owned local control-state layer (`CUserLocal::OnPacket`, `CUserLocal::OnSetDirectionMode`, `CUserLocal::OnSetStandAloneMode`, `CUserLocal::SetDirectionMode`, `CUserLocal::TryLeaveDirectionMode`, `CWvsContext`) |
| Partial | Reactor interaction parity | Local player movement now runs reactor touch checks through `ReactorPool`, throttles the local touch sweep to the client's once-per-second cadence from `CUserLocal::CheckReactor_Collision`, reactor visuals now load full WZ-defined direct frame sequences plus sparse numeric state branches instead of collapsing many reactors to `0/0` or hard-falling back to state `0`, runtime reactor activation now resolves `info/reactorType` back into touch, hit, skill, item, or animation-only handling instead of collapsing most non-quest reactors to touch, now also falls back to WZ-authored state `event/*/type` metadata when `info/reactorType` is absent so common harvest, item-use, skill-use, touch-only, and sparse quest-only reactors stop defaulting to touch just because the info block is sparse, the non-touch branch stays wired into the simulator's actual player attack surfaces by forwarding world-space melee or magic hitboxes, projectile impacts and explosion areas, summon strike areas, and the basic no-skill attack fallback into bounded reactor hit checks with left-versus-right hit gating for directional reactor types, item-backed reactors now only activate from matching inventory use near the local player and can consume the matching item even when the simulator has no richer local effect for that item yet, quest-backed reactors now bind directly to the simulator quest runtime so they enter or reset their active state as quest requirements change, sparse quest-backed reactors can now also infer their required quest id from authored `event/*/(quest|questID|questid|reqQuest|id)` metadata when the `info` block omits it, and quest refresh now forwards that live quest id into authored selector dispatch so same-type quest event branches can land on the matching WZ-authored target state instead of always defaulting to the first quest edge, reactor runtime state progression now keeps the real WZ state ids and per-state frame durations alive during activation or follow-up hits instead of collapsing back to synthetic `1/2/3/4` states or destroying multi-state hit reactors after the first post-activation strike, authored WZ state-event targets are now consumed for single-target activation and timed reset paths so reactors that encode their next or reset state in `event/*/state` no longer rely only on numeric state ordering, authored item-, skill-, and quest-selector branches from `event/*/(item|itemID|itemid|skill|skillID|skillid|quest|questID|questid|reqQuest|id)` now also flow through the live activation seam so sparse reactors with multiple WZ-authored item, skill, or quest targets no longer always collapse onto the first matching event edge just because the simulator previously dropped the triggering selector id before transition selection, and the pool now preserves that triggering selector id across later same-type auto-chained reactor states as well so authored multi-step item, skill, or quest graphs continue down the matching WZ-authored branch instead of reverting to the first same-type candidate after the first hop. Packet-owned `CReactorPool::OnReactorChangeState` handling now also seeds that same authored-branch preference from the decoded `properEventIndex`, so server-selected same-type WZ `event/*` branches stay aligned across later packet-driven timed or auto-chained hops instead of falling back to authored order on the next transition. Authored mixed event-type graphs on the same WZ state now also stay live through the pool instead of being collapsed to one stored activation kind, so touch-plus-item, hit-plus-skill, and quest-plus-interaction reactors can all advertise and resolve the authored branch that matches the real incoming interaction, non-hit reactors now also continue walking authored numeric WZ state chains on their own timer instead of freezing forever on the first progressed state, reactor frame advancement now catches up across larger elapsed ticks instead of only stepping one frame per update, pool runtime state now keeps the live authored frame index for the active state, reactors with WZ `info/script` names now feed the simulator's existing dynamic object-tag publication seam through the same alias resolver used for field-entry scripts so script-backed tagged objects can turn on when those reactors activate and turn back off when the reactor resets, destroys, or respawns, and this pass now resolves the full authored `info/script` container through the same recursive or delimited WZ script-name parser already used for quest and NPC publication so reactors with multiple authored script aliases no longer collapse to one flat string before publication. The reactor script publisher now aggregates every authored alias onto the live tag-mutation seam at activation and reset time, so staged or multi-alias reactor publications can retire sibling stage tags and enable all matching authored targets together instead of only honoring one resolved script name. Packet-owned reactor enter-field and change-state application now also resynchronize that same script-publication seam from the live visual state, so server-owned reactors that enter already progressed or later reset back to their authored initial state no longer leave tagged map objects stale until a local activation or destroy path happens to touch them. Live reactor collision or item-use checks now consume the active WZ frame bounds instead of a static `ReactorInstance` box so frame-authored origin shifts keep touch, hit, directional-hit, and local item-use interaction aligned to the visible reactor pose, and destroyed non-respawning reactors now drop out of the renderable set immediately instead of lingering onscreen because no fade path ever ran for the common destroy case. Focused regression coverage now includes `UnitTest_MapSimulator/ReactorPoolBranchSelectionTests.cs` for selector-less same-type authored branch preference, packet `properEventIndex` carry-through, and packet-owned script publication on enter or reset. Remaining gap: true reactor locomotion beyond those per-frame bounds shifts, local or otherwise non-packet first-hop authored non-quest same-event-type branches that still provide no selector or other discriminator beyond authored order, and fuller named reactor-script or field-event execution beyond tagged-object publication are still incomplete | Environmental interaction now covers the client-like touch-reactor path, its collision polling cadence, more of the WZ reactor animation surface, WZ-backed touch versus hit versus skill versus item versus quest activation selection, local inventory-driven item reactor use, quest-state-driven reactor refresh, a first pass of script-backed object-state publishing for tagged map objects driven by reactor `info/script`, live WZ-frame collision bounds so authored frame offsets also affect touch, hit, directional-hit, and local item-reactor checks, closer WZ-authored state playback because the runtime now also honors single-target state-event metadata for missing activation typing and timed reset playback instead of relying only on `info/reactorType` and numeric next-state order, now preserves the triggering item, skill, or quest id across both initial selection and later same-type chain hops so selector-backed `event/*` graphs keep landing on their WZ-authored target state instead of falling back after the first transition, now keeps multiple authored activation kinds live on the same reactor so the pool dispatches against the actual incoming touch, hit, skill, item, or quest request instead of whichever single activation enum happened to win during load, now keeps packet-authored same-type change-state follow-up hops aligned with the server-selected WZ `event/*` branch via `properEventIndex`, and now keeps reactor `info/script` publication aligned with the simulator's other field-script seams by parsing multi-name WZ script containers and publishing every authored alias instead of only one flat string while also resynchronizing packet-owned enter and change-state updates against the reactor's authored initial state. The remaining gap is narrowed to reactors that truly move independently of their current frame bounds, local or otherwise non-packet first-hop same-type authored branches that do not expose a selector or other runtime discriminator beyond WZ order, plus richer named script or field-event execution beyond those existing dynamic-tag toggles | `ReactorPool.cs`, `MapSimulator.cs`, `ReactorScriptStatePublisher.cs`, `UnitTest_MapSimulator/ReactorPoolBranchSelectionTests.cs` (`CUserLocal::CheckReactor_Collision`, packet `CReactorPool::OnReactorEnterField`, packet `CReactorPool::OnReactorChangeState`) |
| Partial | Quest-alert, balloon, and idle social feedback loops | NPCs with available, in-progress, or completable quests now render the client-backed `QuestIcon` alert art above their heads based on the simulator quest runtime, quest accept/complete actions now queue timed in-world feedback balloons above the speaking NPC instead of collapsing to a single fixed message while also holding the NPC in `speak` until the active balloon expires, nearby NPCs with WZ `info/speak/*` idle lines now periodically surface those lines through the same in-world balloon path, and pets now mirror the client-backed `PetAutoSpeaking` surfaces for level-up, pre-level-up, idle `e_rest`, low-HP alert, and HP/MP potion failure speech with the same six WZ-backed `String/PetDialog.img` event keys that exist in this data set while also honoring the client-backed `0x100` pet-skill gate from `CPet::AutoSpeakingByEvent` instead of letting every pet auto-speak by default. The simulator specialist pet-dialog seam now also loads numbered `String/PetDialog.img` slang entries such as `s1`-`s4` instead of only flat `s` keys, and ordinary non-command player chat now feeds that same loaded specialist feedback path with a short cooldown so compatible active pets can surface non-`PetAutoSpeaking` chatter in-world instead of leaving those lines debug-only. The same specialist seam now also listens to live Messenger room chat, Messenger whispers, simulator-owned social-list chat and whisper actions, guild-BBS post and reply submission text, skill-macro party-notify chat lines, packet-owned utility chat routes that resolve to the same social chat-log families, packet-owned social chat families such as incoming whispers plus friend, party, guild, alliance, couple, or expedition field-message packets, MapleTV Megassenger chat mirrors from both local publishes and packet-owned set-message updates, MiniRoom and adjacent `SocialRoomRuntime` speaker text, memo or parcel send plus quick-send body text from the memo mailbox owner, and now the simulator-owned engagement proposal owner's request/custom note text across outgoing opens, incoming opens, inbox-driven opens, accept or withdraw actions, and dismiss/decline actions, so more of the simulator's social text owners can trigger the same WZ-backed in-world pet chatter instead of limiting it to the local chat input box alone. Quest `Act.img/petskill` rewards now persist on a compatible active pet selected through the same WZ `Check.img/pet` plus the client-shaped active-pet limit aliases used by these pet-training quests, including `petRecallLimit` and the `petAutoSpeakingLimit` fallback found in this data set, and the quest reward text now resolves recognized client pet-skill bits such as `0x80` and `0x100` to named skills instead of falling back to opaque hex strings. Active pet runtime ownership now also persists the trained pet skill mask, command level, tameness, fullness, auto-loot toggle, and auto-HP configuration across unsummon or controller clears for the same pet item within the current simulator session, and WZ `Item/Pet/*/interact/*/inc` closeness gains now feed command success so trained pets accumulate tameness, step through the simulator's client-shaped command-level thresholds, and keep that command growth attached to the same pet item instead of treating command level as a detached manual knob. USE-item clicks or hotkeys now parse WZ `Item/Consume/*/spec` recovery fields including numeric-string `hpR`/`mpR` values seen in this data set, HP/MP potion failure speech is throttled to the client-shaped first-few repeats instead of firing forever, zero-count USE hotkey bindings now persist across config reloads instead of being dropped when the stack is empty, and live pet-food item uses now route through the loaded `f1`-`f4` dialog buckets by deriving food tiers only from the client-shaped `interact/c1`-`c4` ranges instead of folding unrelated higher `c*` commands back into those four buckets, while also falling back to the truly nearest actually loaded food bucket when a pet only ships a subset such as `f1`, selecting a compatible active pet from the food item's `spec/*` whitelist when present, and only consuming food when the targeted pet's new simulator fullness state can actually accept the WZ `spec/inc` increase while already-full pets surface the matching failure bucket instead. Remaining gap: additional automatic non-`PetAutoSpeaking` specialist chatter sources outside the currently wired local chat, skill-macro party-notify chat, Messenger, simulator-owned social-list and guild-BBS text paths, MapleTV Megassenger mirrors, `SocialRoomRuntime` speaker text, memo or parcel compose text, packet-owned social-chat families, packet-owned utility chat routes that already resolve into those social log families, and other still-unwired simulator-owned social text owners beyond the engagement proposal seam are still not modeled, pet ownership still only survives for the current simulator session rather than a fuller durable profile, and wider quest-driven pet-skill semantics beyond the currently consumed auto-speaking gate are still missing | The simulator now surfaces both static quest-state alerts and closer client-like NPC and pet idle feedback loops in-world instead of hiding all progression feedback inside the dialogue overlay alone, closes the consume recovery mismatch by honoring numeric-string recovery ratios in `Item/Consume/*/spec`, and now turns real pet-food item uses into WZ-backed in-world pet speech using the pet's existing command-tier ranges, compatibility list, and `spec/inc` fullness gain so failure speech no longer burns food on already-full pets. This pass also fixes the tier derivation seam to stay anchored to the client-shaped `interact/c1`-`c4` buckets, corrects the fallback selector so partial `f1`-`f4` coverage now chooses the closest loaded bucket rather than simply the next higher one, loads the numbered pet-dialog slang strings that this data set stores under `s1`-`s4` instead of a flat `s` property, and now gives those specialist lines live ordinary-chat, skill-macro party-notify chat, Messenger, simulator-owned social-list chat and whisper actions, guild-BBS post and reply submission text, packet-owned utility and field-social chat call sites, MapleTV Megassenger mirrors, MiniRoom and adjacent social-room speaker text, memo or parcel send text, and the engagement proposal owner’s staged note/custom-text lifecycle instead of leaving them confined to manual simulator triggers. This pass also ties pet auto-speaking ownership back to WZ quest rewards and the client gate, teaches the quest runtime to honor the `petAutoSpeakingLimit` alias used by these training quests, surfaces named client pet-skill reward text for known bit flags, and keeps the trained pet's core runtime state attached to that pet item across unsummon or map-runtime clears within the active session instead of discarding it immediately. It now also consumes WZ `interact/*/inc` command gains into persisted pet tameness so command success can grow the pet's live command level instead of leaving command progression as a manual-only simulator override. The remaining gap is narrowed to additional chatter sources that live outside the client's six `PetAutoSpeaking` events plus the current local-chat, skill-macro party-notify, Messenger, simulator-owned social-list and guild-BBS text seams, MapleTV Megassenger mirrors, `SocialRoomRuntime` speaker text, memo or parcel send text, the engagement proposal note lifecycle, and the currently resolved packet-owned social or utility specialist seams, fuller durable pet ownership beyond the current in-session item-keyed runtime, and pet-skill semantics that still need consumers beyond the current auto-speaking path | `QuestRuntimeManager.cs`, `NpcFeedbackBalloonQueue.cs`, `NpcItem.cs`, `LifeLoader.cs`, `PetLoader.cs`, `PetController.cs`, `SocialListRuntime.cs`, `GuildBbsRuntime.cs`, `MemoMailboxManager.cs`, `SocialRoomModels.cs`, `MapleTvRuntime.cs`, `EngagementProposalController.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.EngagementProposalParity.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/EngagementProposalControllerTests.cs`, `UIWindow*.img/QuestIcon`, `String/PetDialog.img`, `UIWindow2.img/MateMessage`, `UIWindow2.img/Wedding/Invitation`, `Item/Pet/*/interact`, `Item/Consume/0212/*/spec`, `Quest/Check.img`, `Quest/Act.img` (`CUserLocal::Update`, `CUser::PetAutoSpeaking`, `CPet::AutoSpeakingByEvent`) |

### 2. Reactor layer-build and hit-prop parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Reactor layer-build / hit-prop ownership | The current simulator already has the broader reactor runtime and packet seam in place, but the latest IDA pass shows that the client keeps a second owner beneath that packet shell. `CReactorPool::LoadReactorLayer` does more than refresh one canvas: it lazy-resolves `CReactorTemplate`, looks up the active state branch by stringified state id, builds the visible state layer through `CAnimationDisplayer::LoadLayer`, chooses repeat versus stop playback from `CReactorTemplate::STATEINFO::m_bRepeat`, preserves moving-state layer travel against `tStateEnd`, auto-selects hit branches through `get_hittype_priority_level` plus `CReactorTemplate::GetHitProp` when `nProperEventIdx` is still unset, formats the per-state hit sound through the StringPool template at id `2121`, and then assigns the final layer depth from the closest foothold page/z-mass plus the reactor template layer mode. The existing simulator rows still mention pieces of that behavior through `Pools/ReactorPool.cs` and `Interaction/PacketReactorPoolRuntime.cs`, but there is still no dedicated simulator seam that mirrors this client-owned state-layer rebuild, hit-prop arbitration, sound-owner, and z-placement path separately from packet decode or collision handling. | Without a dedicated loader row, reactor parity can look more complete than it is because backlog 7 already tracks packet ingress, timing, and local collision. The client still splits reactor ownership one layer deeper: packet handlers decide when the reactor changes, but `LoadReactorLayer` decides which state branch, hit prop, playback mode, sound, and final z/page the runtime actually consumes. Naming that owner keeps future work anchored to the real layer-build seam instead of continuing to overload the packet-runtime row. | reactor layer-build/runtime seam (`CReactorPool::LoadReactorLayer`, `CReactorTemplate::GetTemplateProp`, `CReactorTemplate::GetStateInfo`, `CReactorTemplate::GetHitProp`, `get_hittype_priority_level`, `CAnimationDisplayer::LoadLayer`, `play_reactor_sound`, existing simulator contrast: `HaCreator/MapSimulator/Pools/ReactorPool.cs`, `HaCreator/MapSimulator/Interaction/PacketReactorPoolRuntime.cs`) |

### 3. Field-state and script-message parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field-owned script/message and object-state packet parity | The simulator still has the dedicated packet-owned field-state runtime at the `CField::OnPacket` seam for `OnFieldSpecificData` (`149`), `OnDesc` (`162`), `OnSetQuestClear` (`166`), `OnSetQuestTime` (`167`), and `OnSetObjectState` (`169`), plus the packet-owned script-message bridge at the `CScriptMan::OnPacket` seam for `OnScriptMessage` (`363`). The bridge still decodes the shared `speakerType` / `speakerTemplate` / `msgType` / `bParam` header confirmed in prior IDA work, opens the existing NPC interaction overlay in direction mode, resolves speaker NPC names from map/runtime or WZ string data, and routes packet-authored `OnSay`, `OnSayImage`, `OnAskYesNo`, `OnAskMenu`, `OnAskText`, `OnAskNumber`, `OnAskQuiz`, `OnAskSpeedQuiz`, `OnAskAvatar`, `OnAskMembershopAvatar`, `OnAskPet`, `OnAskPetAll`, `OnAskSlideMenu`, and `OnAskBoxText` through the existing overlay/page-choice runtime already used by NPC and quest conversations. The newer prompt families remain anchored to WZ-confirmed client UI owners rather than ad hoc simulator surfaces: `UIWindow(.img|2.img)/InitialQuiz`, `SpeedQuiz`, `SlideMenu`, `UtilDlgEx_Pet`, and `UtilDlgEx_MultiPetEquip` still supply the packet-authored metadata and overlay framing, while avatar and pet choice rows still label indexed entries with WZ-backed item names when available. This pass kept the recovered quiz-owner behavior intact and tightened the pet-owner filtering path around the client `CharacterData` lookup seam. Using WZ evidence from `UIWindow.img` / `UIWindow2.img` plus IDA evidence from `CWvsContext::OnInitialQuiz`, `CWvsContext::OnInitialSpeedQuiz`, `CScriptMan::OnAskPet`, and `CScriptMan::OnAskPetAll`, `OnAskQuiz` and `OnAskSpeedQuiz` still explicitly treat mode `0` as the only open-dialog payload, treat mode `1` as a close-owner packet even when trailing bytes remain, and ignore other nonzero modes as client no-op updates instead of falling back into the simulator's compact helper decoder and mutating the NPC overlay incorrectly, while `OnAskPet` / `OnAskPetAll` now resolve packet pet serials through the simulator's active-pet runtime before opening a prompt, drop zero or unresolved serials before building the choice list, label the surviving rows with live pet name, slot, serial, and item metadata, and still mirror the recovered no-dialog fallback branches by auto-closing with opcode-`65` status-`2` replies when the client exposes only the fallback path and by auto-accepting the first resolved pet serial with opcode-`65` status-`1` replies when the packet omits prompt text and the client would skip `UtilDlgEx_Pet` or `UtilDlgEx_MultiPetEquip` entirely. Manual packet exercise still has client-shaped `/scriptmsg quizclient`, `/scriptmsg quizclose`, `/scriptmsg speedquizclient`, and `/scriptmsg speedquizclose` helpers alongside the existing compact `/scriptmsg quiz` / `/scriptmsg speedquiz`, pet, petall, slidemenu, say, sayimage, yes-no, menu, avatar, membershop-avatar, text, number, and box helpers plus raw payload injection. Packet-authored close packets still tear down the currently open NPC overlay, `/scriptmsg slidemenu` still emits the real slide-menu message type (`15`), `OnAskSlideMenu` still distinguishes client-owned type-`0` `CSlideMenuDlgEX` versus type-`1` `CSlideMenuDlg` owners and auto-cancels unsupported type values, and prompt submission still builds opcode `65` replies for `OnAskYesNo`, `OnAskMenu`, `OnAskText`, `OnAskNumber`, `OnAskBoxText`, `OnSayImage`, `OnAskAvatar`, `OnAskMembershopAvatar`, `OnAskQuiz`, `OnAskSpeedQuiz`, `OnAskPet`, `OnAskPetAll`, and `OnAskSlideMenu`, records dispatch status on the packet-script runtime surface, and still exposes the loopback packet-reply transport plus the live official-session bridge and `/scriptmsg transport <status|start [port]|stop>` / `/scriptmsg session ...` controls. On the field-state side, the runtime still binds map help text from map info, shows packet-authored field help overlays, tracks quest-timer start/end windows with WZ-backed quest names and `timerUI` metadata, clears packet-authored timers, appends active timer text into the quest UI surfaces, and routes named object-state flips through the existing tagged-object/runtime seam. The visible help and timer surfaces remain on WZ-backed `UtilDlgEx/notice` and `Quest/TimeQuest` assets including the quest timer alarm-clock icon families and time-gauge textures, and the `OnFieldSpecificData` seam still routes its structured Maple-string pair decode through the shared `PacketFieldSpecificDataCodec`, including prefix stripping for `field:`, `party:`, and `session:` owner hints before dispatching common packet-owned payloads into party-raid variables plus wrapper-owned `escortresult` and `huntingadballoon` state through the current special-field seams. `UnitTest_MapSimulator/PacketScriptMessageRuntimeTests.cs` now covers the client-shaped quiz and speed-quiz open packets, mode-`1` close packets with trailing bytes, nonzero no-op update modes, resolved-pet filtering for `OnAskPet`, the `OnAskPetAll` single-resolved-pet fallback branch, and the no-text first-resolved-pet implicit-accept branch, while the existing runtime coverage still handles the slide-menu type-owner split, unsupported-type auto-cancel behavior, slide-menu helper opcode/message-type contract, slide-menu opcode-`65` reply encoding, and prefixed field-specific key/value decode with owner-hint stripping. Remaining gap: field-specific-data decoding is still structured-string based rather than client-owner-specific or byte-exact across all field owners; `OnAskQuiz` and `OnAskSpeedQuiz` still do not reproduce the client owners' full local countdown/animation behavior beyond the recovered mode and field-order semantics; `OnAskPet` / `OnAskPetAll` still use a simulator-owned active-pet serial mapping instead of the client's exact `CharacterData` cash-item inventory lookup and therefore still miss fuller missing-slot and serial-ownership edge cases outside the current session-owned pet runtime; and the help plus quest-timer packet presentation still does not reproduce the client's exact animation cadence or all `Quest/TimeQuest` layout nuances. The dedicated quest-timer modeless owner itself is tracked separately in the progression/utility backlog so timer UI work does not stay duplicated here. | Map scripts and event fields are not driven only by NPC clicks or reactor collisions. Without this packet-owned bridge, parity work keeps re-implementing isolated outcomes while missing the client seam that coordinates post-entry scripted help dialogs, timed quest deadlines, quest-clear resets, script prompts, and `CMapLoadable::SetObjectState` updates for object-tag and map-state changes. This row is now narrower on the script side because the simulator no longer misroutes nonzero quiz / speed-quiz owner updates into the compact helper decoder and now filters packet pet prompts through the active pet runtime before surfacing `UtilDlgEx_Pet` / `UtilDlgEx_MultiPetEquip`, while the separate quest-timer UI row keeps modeless timer-owner work anchored to the actual utility-window family. | field/script runtime bridge (`CField::OnPacket`, `CField::OnFieldSpecificData`, `CField::OnDesc`, `CField::OnSetQuestClear`, `CField::OnSetQuestTime`, `CField::OnSetObjectState`, `CScriptMan::OnPacket`) |

### 4. Packet-authored field messaging and feedback parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field chat, effect, warning, and timer packet parity | The simulator now has a dedicated packet-owned field-feedback runtime wired to the `CField::OnPacket` / `CUserLocal::OnPacket` seam for `OnGroupMessage` (`150`), `OnWhisper` (`151`), `OnCoupleMessage` (`152`), `OnFieldEffect` (`154`), the obstacle on/off and reset packets (`155`, `156`, `159`), `OnWarnMessage` (`157`), `OnPlayJukeBox` (`158`), transfer or summon-item failure notices (`160`, `161`, `164`), `OnDestroyClock` plus the Zakum/Horntail/Chaos Zakum/Hontale timer packets (`163`, `170`-`173`), and `CUserLocal::OnFieldFadeOutForce` (`174`). This pass also tightened several client-owned behaviors inside that seam: group chat now respects the simulator blacklist for the same non-party families the client filters, incoming whispers from blacklisted senders are suppressed unless they are flagged as admin, blocked-friend whisper suppression now also consults the packet/social friend roster instead of only the simulator blacklist, the alternate whisper result branch (`138`) now routes through the same result handling as subtype `10`, subtype-`9` whisper find success can now consume the packet's extra transfer coordinates and queue a live map-change follow-through through the existing map-transfer seam, field-effect object-state pushes now clear through the existing dynamic-tag seam instead of only being summarized, and the `OnFieldEffect` summon, screen-effect, and reward-roulette branches now route into WZ-backed visual hooks instead of remaining summary-only. WZ evidence now drives those hooks through `Effect/Summon.img/<id>` with a narrower `MapEff.img/NpcSummon` fallback that only stays active for ids that still look like known generic summon shapes, a more client-shaped `MapEff.img/<descriptor>`-first screen-effect resolution order before broader effect-image fallback, a multi-layer `BasicEff.img/MainNotice/userReward/{Default,Notify,Appear}` composition for reward-roulette feedback instead of a single `Appear` strip, and `Sound/Summon.img/<id>` summon audio playback when the sound exists. This pass also pins the recovered client field-effect string-pool templates in the shared string-pool seam so packet-owned summon and screen-effect routing no longer depend on simulator fallback formats: string-pool id `0x663` now resolves the summon effect path through `Effect/Summon.img/%d`, while id `0x9ED` stays explicit for `Effect/MapEff.img/%s`. Client evidence from `CField::OnFieldEffect`, `CField::ShowScreenEffect`, `CAnimationDisplayer::Effect_RewardRullet`, `play_summon_sound`, `CCurseProcess::FilterChar`, `CCurseProcess::SearchSubstring`, and `CCurseProcess::GetSwindleWarning` also corrected the visual and warning ownership in this build: summon packets still go through a formatted summon-effect UOL plus summon-sound ownership, reward-roulette is treated as a layered animation surface rather than one flat sprite, repeated reward-roulette packets now clear the prior roulette layer set before re-registering a fresh layered surface instead of stacking stale packet-owned strips, the reward-roulette packet's three ints are now preserved as the client-owned index triplet consumed by `Effect_RewardRullet` rather than being relabeled as simulator `rewardId/step/total` state, the WZ-backed user-reward layers now sweep in the client's default-then-`0` order across the three layer families instead of interleaving each family's fallback immediately, the generic `NpcSummon` fallback gate is now narrowed to the recovered generic summon-id family (`0`, `2`, `5`, `13`, `14`, `24`, `25`, `26`) instead of any id that merely happened to expose a `Sound/Summon` asset, the Zakum/Horntail/Chaos Zakum/Hontale packets remain chat-log owned rather than a dedicated timerboard so the simulator mirrors them as packet-authored status-bar or chat feedback while still honoring `OnDestroyClock` teardown, and the swindle filter path now also uses the exact client `s_FilterChars` byte table recovered from the v95 executable (`0x20, 0x5F, 0x2D, 0x3A, 0x09, 0x5E, 0x2E, 0x2C, 0x2A, 0x2F, 0x3B, 0x21, 0x5C, 0x27, 0x22, 0x60, 0x2B`) instead of the older simulator-owned punctuation list. This pass also replaces the earlier scam-warning heuristic with a WZ-backed swindle-warning path: incoming whispers and party chat load warning groups, keywords, and randomized warning text from `Etc/Swindle`, preserve the client-shaped highest-group-first match order recovered from `CCurseProcess::GetSwindleWarning`, and still honor the same warning-strip ownership plus ten-second cooldown already modeled in this seam. Client evidence now also narrowed the warning matcher itself: the runtime stops pre-normalizing WZ keywords into simulator-owned lowercase tokens, filters incoming chat more like `CCurseProcess::FilterChar`, matches warnings through a case-insensitive filtered substring pass shaped after `CCurseProcess::SearchSubstring`, mirrors that helper's terminal-byte start guard instead of allowing simulator-owned final-byte matches the client would skip, now preserves DBCS pairs as whole characters while filtering and matching so trail bytes that happen to equal stripped punctuation no longer corrupt the keyword scan, and still uses exact client-shaped filter-table membership and ordering for stripped characters instead of the older simulator-owned case-folded filter check. `UnitTest_MapSimulator` now also has focused source-level coverage for the recovered summon and screen-effect string-pool templates, the reward-roulette layer source ordering, the recovered `s_FilterChars` bytes, the end-to-end filtered message result they produce, retained highest-group warning selection after filtered chat text, descending `Etc/Swindle` group extraction order, and raw-byte DBCS trail-byte or start-boundary cases in addition to the earlier reward-roulette layer list and raw index-triplet handling, the client-shaped `MapEff.img` screen-effect candidate ordering, the narrowed summon-fallback gate, WZ-backed swindle-warning selection and extraction, summon-sound callback dispatch, visual callback dispatch, corrected chat-only boss-timer status handling, blacklist suppression, blocked-friend whisper suppression, whisper chase transfer queuing, the extra whisper result branch, object-state clear cases, and the newer client-shaped swindle filter or case-match behavior. Remaining gap: the swindle-warning path now follows the client WZ tables, highest-group ordering, recovered `s_FilterChars` bytes, exact `FilterChar`-style byte membership checks, DBCS-safe character-boundary filtering, and a closer `SearchSubstring`-shaped matcher, but it still does not prove the remaining locale-specific DBCS behavior against a byte-for-byte live session capture; whisper chase follow-through is still limited to subtype-`9` success packets that actually carry transfer coordinates and currently queues the simulator's map-change seam rather than the client's fuller `SendTransferFieldRequest` lifecycle; screen-effect descriptor resolution and reward-roulette visuals now preserve the recovered summon or screen StringPool templates plus the closer client ordering and layer sweep, but still do not reconstruct the remaining load-layer flags or the full `ShowScreenEffect` / `Effect_RewardRullet` composition path byte-for-byte; obstacle presentation still reuses the existing tag-driven object seam rather than the client dynamic-object visuals; and summon-effect fallback still remains approximate for ids outside the recovered generic family when the extracted `Summon.img` set is missing the formatted summon-effect path the client would try first. | Visible packet-owned feedback is a distinct client-owned surface between gameplay, the status bar, map-state tags, local fade overlays, field music, and boss timers. Tracking it as its own seam keeps future parity work anchored to the dispatcher handlers that actually own chat feedback, warning dialogs, forced fades, obstacle toggles, jukebox swaps, transfer failures, and special-event timers instead of burying those effects inside unrelated rows. | packet-owned field feedback layer (`CField::OnPacket`, `CField::OnGroupMessage`, `CField::OnWhisper`, `CField::OnCoupleMessage`, `CField::OnFieldEffect`, `CField::OnFieldObstacleOnOff`, `CField::OnFieldObstacleOnOffStatus`, `CField::OnFieldObstacleAllReset`, `CField::OnWarnMessage`, `CField::OnPlayJukeBox`, `CField::OnTransferFieldReqIgnored`, `CField::OnTransferChannelReqIgnored`, `CField::OnSummonItemInavailable`, `CField::OnDestroyClock`, `CField::OnZakumTimer`, `CField::OnHontailTimer`, `CField::OnChaosZakumTimer`, `CField::OnHontaleTimer`, `CUserLocal::OnFieldFadeOutForce`) |

### 5. `CAnimationDisplayer` social/event animation-owner parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shared social/event animation-displayer parity | The simulator now has an explicit shared animation-owner seam instead of leaving this family implicit across unrelated systems. `Animation/AnimationEffects.cs` now owns two additional `CAnimationDisplayer`-shaped registrations: avatar-adjacent user-state sequences with start/repeat/end phases plus timed area-burst registrations that fan one-shot effects out across a rectangle. This pass keeps the earlier event-side timing recovery from `CMapLoadable::TransientLayer_NewYear`, `CMapLoadable::TransientLayer_FireCracker`, `CAnimationDisplayer::RegisterNewYearAnimation`, and `CAnimationDisplayer::RegisterFireCrackerAnimation`, and extends the user-state side with the client-owned quest-delivery owner shape recovered from `CUser::OnEffect` cases `30/31` and `CAnimationDisplayer::Effect_QuestDeliveryItemUse`: `MapSimulator.AnimationDisplayerParity.cs` now resolves quest-delivery user-state assets through a WZ-first candidate list under `Effect/OnUserEff.img/itemEffect/quest/<itemId>` and falls back to the only shipped quest-delivery effect asset currently present in the loaded data set (`2430071`) when simulator-owned placeholder item ids do not have their own art. The existing packet-owned quest-delivery interaction owner in `MapSimulator.PacketOwnedUtilityParity.cs` now registers that local avatar-following start/repeat/end layer before opening the NPC overlay, the overlay-close and map-clear paths now clear it through the same shared seam, and the manual `/socialanim` exercise surface now includes `questdelivery <itemId|clear>` in addition to the existing status, event, and generic user-state commands. The row still covers the packet-owned blow-weather owner in `MapSimulator.PacketOwnedFieldUtilityParity.cs`, which clears and re-registers the New Year and firecracker transient-layer family for item `4300000` and `5680024`, so `CField::BlowWeather -> CMapLoadable::TransientLayer_*` continues to feed the same shared seam instead of leaving those event owners as purely simulator-owned exercises. This pass also closes the earlier New Year sound-path gap inside that seam: the transient-layer helper now uses the recovered client string-pool format from `TransientLayer_NewYear` (`Sound/Field.img/%s`, string-pool id `0x125C`) to normalize client-shaped weather keys into exact `Field/<name>` sound descriptors before falling back to the older broad candidate sweep, so `Map/MapHelper.img/weather/<name>` and direct `Sound/Field.img/<name>` inputs resolve through the same sound-owner shape the client formats before registration. `UnitTest_MapSimulator/AnimationDisplayerParityTests.cs` now covers the quest-delivery UOL fallback ordering plus the WZ-backed arrive/wait/leave phase split for the shipped quest-delivery frames, and adds source-level coverage for the New Year sound descriptor normalization and candidate ordering. Remaining gap: follow-line parity is still limited to the older generic follow-animation seam and does not yet reproduce the client's `RegisterFollowAnimation` geometry or generation rules; generic temporary-stat user-state parity still uses a single `OnUserEff.img/character` marker rather than stat-specific start/repeat/end assets recovered from the client; and New Year parity still does not reproduce the exact client playback cadence once the transient-layer sound descriptor has been resolved. | If this manager stays implicit, parity work can overstate progress in field feedback, relationship-overlay, and local-overlay rows while still missing the shared animation owner that actually registers, updates, and resets several visible social/event effect families. Tracking it explicitly keeps future work anchored to the same `CAnimationDisplayer` seam instead of scattering follow, New Year, firecracker, quest-delivery repeat effects, and fade-cleanup behavior across unrelated backlog buckets. | shared animation-owner layer (`CAnimationDisplayer::RegisterUserStateAnimation`, `CAnimationDisplayer::RegisterNewYearAnimation`, `CAnimationDisplayer::RegisterFireCrackerAnimation`, `CAnimationDisplayer::RegisterFollowAnimation`, `CAnimationDisplayer::ResetLocalFadeLayer`, `CAnimationDisplayer::Effect_QuestDeliveryItemUse`, `CUser::OnEffect`, `HaCreator/MapSimulator/Animation/AnimationEffects.cs`, `HaCreator/MapSimulator/MapSimulator.PacketOwnedLocalOverlayParity.cs`, `HaCreator/MapSimulator/MapSimulator.PacketOwnedFieldUtilityParity.cs`, `HaCreator/MapSimulator/MapSimulator.PacketOwnedUtilityParity.cs`, `HaCreator/MapSimulator/MapSimulator.AnimationDisplayerParity.cs`) |

### 6. Packet-authored field utility parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field utility, quickslot-init, and foothold-info packet parity | `HaCreator/MapSimulator` keeps the dedicated `PacketFieldUtilityRuntime` plus `MapSimulator.PacketOwnedFieldUtilityParity.cs` owner seam for `CField::OnPacket` field utilities: `OnBlowWeather` (`158`) drives packet-authored weather/message overrides, `OnAdminResult` (`160`) stays on the client-owned chat/minimap path (including subtype-`41` minimap-hide persistence), `OnQuiz` (`161`) resolves `Etc/OXQuiz.img` question/answer text and renders through `UI/StatusBar2.img/mainBar/notice` with the client-shaped short notice lifetime and question-side 30-second timer ownership, `OnStalkResult` (`172`) updates stalk/follow markers, `CQuickslotKeyMappedMan::OnInit` (`175`) hydrates both quickslot labels and live `PlayerInput` bindings from packet DIK-style key codes, and `OnFootHoldInfo` (`176`) / `OnRequestFootHoldInfo` (`177`) keep packet decode/build helpers plus a client-shaped foothold response payload builder (`(state, curX, curY, reverseV, reverseH)`). This pass keeps the earlier direct client extraction for admin-result wording (`StringPool::ms_aString` ids `0xA4`, `0xA6`, `0xA9`, `0xAA`, `0xAC`, `0xAD`, `0xB0`, `0xB1`, `0xBDF`, and `0xBE0`) and tightens the foothold round-trip seam further with WZ-backed name evidence. Follow-up IDA on `CField::OnRequestFootHoldInfo` (`0x52ddd0`) still anchors outbound opcode `270`, but the simulator no longer snapshots request replies from stale cached packet rows when live dynamic-platform state is available: `BuildPacketOwnedFootholdSnapshot` now prefers the current `DynamicFootholdSystem` runtime and only falls back to cached packet entries when the runtime has no platforms, which keeps `/fieldutility status` and live-session opcode-`270` injection aligned with the active simulator foothold positions, states, and reverse flags instead of the last decoded packet payload. The authored-name path is tighter too. `DynamicFootholdField` still keeps the existing WZ-first canonical name resolution (`name` / `objName` / `tag`, then `oS/l0/l1/l2`) plus the broader inbound alias set for layer/object coordinate keys and `piece`-tagged coordinate variants, but it now also preserves a dedicated packet-owned snapshot name per dynamic object that prefers the WZ object contract key itself for outbound foothold replies. In the current extracted dynamic-foothold contracts that means opcode-`270` snapshot names now follow the verified `oS/l0/l1/l2` plus coordinate pattern and stop inventing a synthetic `/piece/0` segment when the authored object only carries the default zero piece, while still keeping nonzero `piece` tags in the outbound name. `BuildPacketOwnedFootholdSnapshot` now prefers that WZ-authored snapshot name over stale packet-cache text when live runtime platforms are present, so outbound foothold-info replies stay anchored to the active contract object key instead of replaying an older inbound alias. `ApplyPacketOwnedFootholdInfo` still clears and rebuilds the serial-to-name cache on each packet update so subsequent official-session replies do not preserve stale authored names from older foothold packets when the runtime cannot supply a current platform snapshot. Focused tests are now on disk in `UnitTest_MapSimulator/PacketFieldUtilityParityTests.cs` for the zero-piece outbound-name shaping and the live-runtime snapshot preference for WZ-authored names, alongside the earlier row-scoped parity coverage already described here. Remaining gap: the outbound foothold response injection is still not proven end-to-end against a captured official session in this workspace, and the exact byte-for-byte outbound name variant is still not proven across every dynamic-foothold contract map that might use nonzero `piece` values or other client-owned separators beyond the WZ-backed forms verified here. | If these handlers stay collapsed into a generic packet assumption, parity work will miss several visible client-owned flows that are neither ordinary UI windows nor general field notices: packet weather and admin-result prompts, quiz ownership, stalk-result feedback, quickslot-init sync, and foothold-data round trips all have their own dispatcher slots in the field owner and should not disappear behind unrelated rows. Splitting RPS out keeps this bucket focused on true field-owner utilities instead of mixing dialog-minigame ownership back into the generic packet section, and separating `CQuickslotKeyMappedMan` from backlog 8's `CFuncKeyMappedMan` row keeps the two keymap owners from being mistaken for duplicate work. | packet-owned field utility layer (`CField::OnPacket`, `CField::OnBlowWeather`, `CField::OnAdminResult`, `CField::OnQuiz`, `CField::OnStalkResult`, `CQuickslotKeyMappedMan::OnInit`, `CField::OnFootHoldInfo`, `CField::OnRequestFootHoldInfo`) |

### 7. Stage-transition and map-load presentation parity

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned stage-transition and map-load presentation parity | `PacketStageTransitionRuntime` is now wired into `MapSimulator` through a dedicated `/stagepacket` parity seam that drives packet types `141` to `146`, opens the ITC/MTS and Cash Shop owners for `CStage::OnSetITC` / `CStage::OnSetCashShop`, binds named map objects for `CMapLoadable::OnSetMapObjectVisible`, and applies/restores packet-owned back-background fades for `CMapLoadable::OnSetBackEffect` / `OnClearBackEffect` across map load and cleanup. This pass closes a major `CStage::OnSetField` hole at the existing seam instead of leaving packet `141` on a simulator-only helper path: the runtime now decodes the client-owned `CClientOptMan::DecodeOpt` header plus the official non-character-data branch (`channelId`, `oldDriverId`, `fieldKey`, notifier block, `dwPosMap`, `nPortal`, `HP`, chase coordinates, and `ftServer`), `/stagepacket field` now emits that official non-character-data payload instead of the older synthetic map-id/string helper, and the map-entry flow now preserves packet-owned portal indices across same-map teleports and cross-map loads so field entry no longer depends on ad hoc portal-name payloads. The row is also no longer command-only on the inbound side: `StageTransitionPacketInboxManager` now provides the same loopback packet-inbox surface used by other packet-owned seams, so `141` to `146` can be fed through `/stagepacket inbox` and drained in the normal update loop rather than only through direct parity commands. This pass also carries authoritative back-layer page ownership from map load through board/runtime persistence: `MapLoader`, `BackgroundInstance`, `MapSaver`, and `MapSimulatorLoader` now preserve WZ `back/*/page` values, and `CMapLoadable::OnSetBackEffect` now targets only the authored back-background page instead of approximating the fade across every back layer. The login-owned handoff is no longer telemetry-only either: live `LoginOfficialSessionBridgeManager` traffic and opcode-framed login inbox payloads for `141` to `146` now relay through the same stage-transition runtime seam that `/stagepacket` and the loopback inbox use, so `CLogin::OnPacket` forwarding can mutate simulator stage/map-load state instead of only incrementing login runtime counters. `CStage::OnSetField` character-data handling is now deeper at that same seam instead of stopping at a transfer-head probe: the runtime recovers the authoritative stat block already present at the front of both the flagged `CharacterData::Decode` path and the older fallback layout (`characterId`, `name`, gender, skin, face, hair, level, job, primary stats, HP/MP, AP/SP, EXP, fame, `dwPosMap`, `nPortal`, play time, and sub-job`), consumes the immediate client-owned stat trailer (`nFriendMax` plus the optional linked-character string), preserves the later character-data remainder for downstream packet-owned rows, and applies that packet-owned character snapshot back onto the active player or selected login-roster build before the queued field change so login entry, Cash Shop return, and revive-style field entry no longer keep stale simulator-owned core stats or appearance metadata. The flagged `CharacterData::Decode` branch now also recovers the first client-owned equipment section at the same seam instead of treating all post-stat bytes as opaque: the runtime mirrors the client `AvatarLook::Load` ownership rule over `aEquipped` / `aEquipped2`, decodes equipped versus cash-covered body-part entries from the flagged equip inventory block, promotes `aEquipped2[11]` into the packet-owned weapon-sticker owner, and synthesizes a `LoginAvatarLook` payload that `CharacterLoader.LoadFromAvatarLook` reapplies onto the active player or selected login-roster build. That means flagged `SetField` entry now refreshes visible cash overlays, concealed underlying equips, and weapon-sticker presentation from packet-owned character data instead of leaving the simulator’s older gear snapshot in place. Targeted unit coverage now exercises both flagged and fallback character-data `SetField` decode at the richer stat-snapshot-plus-trailer level, including flagged avatar-look equipment and weapon-sticker recovery. Remaining gap: the simulator still does not model the full byte-exact `CharacterData::Decode` section graph beyond the stat head, trailer, and first equip inventory block, so meso and non-equip inventory tabs, skill or cooldown sections, map-transfer caches beyond the already recovered transfer head, later quest or social record sections, and the exact boundary between those deeper sections and packet-owned trailing caches still remain approximated rather than fully client-authored end-to-end. | If these owners stay implicit, future parity work can keep overloading login flow, cash-shop stage work, or generic field-effect rows while still missing the actual packet dispatch seam that swaps stages and mutates map presentation during load/runtime. Tracking them explicitly keeps stage changes, packet-owned back effects, and map-object visibility toggles anchored to the client owners that `CField::OnPacket` actually calls. | packet-owned stage/map-load layer (`CField::OnPacket`, `CStage::OnPacket`, `CStage::OnSetField`, `CStage::OnSetITC`, `CStage::OnSetCashShop`, `CMapLoadable::OnPacket`, `CMapLoadable::OnSetBackEffect`, `CMapLoadable::OnSetMapObjectVisible`, `CMapLoadable::OnClearBackEffect`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.
6. Reactor loader pass:
   Split reactor state-layer rebuild and hit-prop ownership out from the existing packet-runtime row before more reactor work gets documented only as packet timing or collision polish.
7. Scripted field-state pass:
   Add the packet-owned bridge for field help messages, quest timers, quest-clear resets, and object-state flips before layering more one-off script behavior onto NPC or reactor seams.
8. Packet-feedback pass:
   Add the packet-owned field messaging, warn-dialog, forced-fade, obstacle, field-effect, and boss-timer surface before more status-bar or field-overlay polish is tracked as isolated UI work.
9. Field-utility packet pass:
   Add the packet-owned weather/admin-result/quiz/stalk-result, quickslot-init, and foothold-info surface before those handlers get misfiled under unrelated UI or field rows.
10. Stage/map-load packet pass:
   Add the packet-owned `CStage` and `CMapLoadable` bridge for field or Cash Shop or ITC stage switches plus back-effect/object-visibility mutations before those transitions get scattered across login, field-effect, or cash-stage rows.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
