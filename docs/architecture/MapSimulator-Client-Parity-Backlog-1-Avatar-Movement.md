# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Avatar and player parity

- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::SetActivePet` at `0x8e40e0`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::Update` at `0x937330`

Notes:
`CUser::LoadLayer` and `CUser::PrepareActionLayer` are the clearest resolved avatar-composition seams for layer loading and action-layer setup. `CUser::Update` and `CUser::UpdateMoreWildEffect` also show that the client maintains several additional avatar/effect layers outside the base assembler path, while `CUserLocal::Update` contains the local-player emotion reset path and action/state transitions that matter for expression and rare-action parity. A later IDA pass also confirms that remote avatar ownership is a first-class client surface rather than a loose extension of local-player logic: `CUserPool::OnPacket` dispatches enter, leave, common, remote, and local user packet families, `CUserPool::Update` advances local plus remote users and shared social-effect state, and `CUserRemote::Update` owns remote move-action, effect, portable-chair, and prepared-skill presentation.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Avatar and player parity

These are the biggest remaining gaps for visible client parity.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Default avatar selection | The simulator still keeps the deterministic, WZ-verified SuperGM/Wizet fallback selection of `1002140` (Wizet Invincible Hat), `1042003` (Wizet Plain Suit), `1062007` (Wizet Plain Suit Pants), `1072005` (Leather Sandals), `1092008` (Pan Lid), and `1322013` (Wizet Secret Agent Suitcase) with matching face, hair, level, and SuperGM job metadata, but login-side avatar ownership is now materially closer to the client: `AvatarLook` decode still validates gender, skin, face, hair, visible equip bodyparts, hidden equip lists, pet ids, and applies the packet-owned `weaponStickerID` visual through the normal weapon render seam; the login packet seam can ingest packet-authored `SelectWorldResult`, `ViewAllCharResult`, `SelectCharacterResult`, `ExtraCharInfoResult`, `CheckDuplicatedIdResult`, and `CreateNewCharacterResult` payloads supplied as hex or Base64 through `/loginpacket` or the inbox; `SelectWorldResult` and `ViewAllCharResult` preserve the client-owned rank block more faithfully by decoding `worldRank`, `worldRankMove`, `jobRank`, and `jobRankMove` from the `GW_CharacterStat -> AvatarLook -> family/rank -> slot metadata` sequence confirmed from `CLogin::OnSelectWorldResult` / `CLogin::OnViewAllCharResult`; `ExtraCharInfoResult` continues to decode the client-owned `accountId -> flag` payload confirmed from `CLogin::OnExtraCharInfoResult`; `CheckDuplicatedIdResult` now mirrors the client-owned `name -> resultCode` packet shape confirmed from `CLogin::OnCheckDuplicatedIDResult`, preserves the checked character name, and maps failure results through the same client notice ids (`5`, `10`, `18`) before the create-result path continues; `SelectCharacterResult` now mirrors the client-owned `resultCode -> responseCode -> endpoint/character/auth/premium` packet shape confirmed from `CLogin::OnSelectCharacterResult`, reselects the packet-owned roster entry by character id, routes successful packet replays through the same runtime-build and map-entry seam as normal character selection, and maps the title-return failure cases through the same client notice ids (`28`, `29`, `30`, `25`, `31`, `56`, `62`) instead of blindly advancing into field entry; and the local login-shell new-character confirm path now routes through that duplicate-id owner first and then through the same `CreateNewCharacterResult` owner that the client uses in `CLogin::OnCreateNewCharacterResult`, so simulator-authored creation now consumes the WZ-backed `Etc/MakeCharInfo.img` starter randomization pool end to end, re-enters character select through the packet/result seam, and consumes a bought extra slot when the roster was already full while still deferring to explicit packet-authored duplicate-id or create-result payloads when those are configured. In this pass the command and inbox seam also gained raw opcode-framed replay ownership tied to the client `CLogin::OnPacket` dispatch table: `/loginpacket stream` and inbox-fed script lines can now expand newline-, `|`-, or UTF-8 hex/Base64-authored transcripts, decode raw little-endian `opcode + payload` hex/Base64 frames, and accept explicit `opcode=<id>` plus binary payload lines into the same packet-owned login handlers instead of requiring every packet family to be named and injected manually one at a time. The remaining gap is narrower again: the simulator still does not consume live encrypted login traffic directly, still does not own a true authenticated backend request/response loop around live character creation or roster state, and still does not cover the rest of the broader login packet surface beyond the currently supported replayable packet families, especially packet-authored `CheckPasswordResult` / `GuestIdLoginResult` account-bootstrap ownership and other client login handlers that still need explicit decode plus runtime-side ownership instead of generic step transitions. | Default avatars and login previews are no longer owned only by direct preset construction or simulator-authored roster clones: the same `AvatarLook` seam now accepts decoded `SelectWorldResult`, `ViewAllCharResult`, and `CreateNewCharacterResult` ownership carrying `GW_CharacterStat` metadata, avatar bytes, packet-owned weapon-sticker visuals, starter randomization output, rank-gap deltas, and raw opcode-framed login capture replays, while selected-character field entry can now also be packet-owned through `SelectCharacterResult` instead of only through the local character-select click path. That is materially closer to the client's `OnSelectWorldResult` / `OnViewAllCharResult` / `OnSelectCharacterResult` / `OnCheckDuplicatedIDResult` / `OnCreateNewCharacterResult` -> `GW_CharacterStat::Decode` -> `AvatarLook::Decode` ownership for preview assembly, roster ownership, and selected-character field entry, and the packet seam can now replay those login-owned packet families as either named scripts or raw opcode-framed captures rather than only as isolated injections. | `CharacterLoader.LoadDefaultMale`, `CharacterLoader.LoadDefaultFemale`, `CharacterLoader.LoadFromAvatarLook`, `CharacterLoader.LoadRandom`, `CharacterAssembler`, `LoginAvatarLookCodec`, `LoginSelectWorldResultCodec`, `LoginViewAllCharResultCodec`, `LoginSelectCharacterResultCodec`, `LoginExtraCharInfoResultCodec`, `LoginCreateNewCharacterResultCodec`, `LoginAccountDialogPacketCodec`, `LoginPacketScriptCodec`, `LoginPacketInboxManager`, `MapSimulator.InitializeLoginCharacterRoster`, `MapSimulator.ApplyCheckDuplicatedIdResultProfile`, `MapSimulator.ApplyCreateNewCharacterResultProfile`, `MapSimulator.TryApplyPacketOwnedSelectCharacterResult`, `MapSimulator.ExecuteLoginCharacterCreateConfirmation`, `MapSimulator.TryConfigureLoginPacketPayload`, `MapSimulator.DispatchLoginRuntimePacket`, `LoginCharacterRosterEntry.CreateRuntimeBuild`, `CharacterConfigManager.CreateDefaultMalePreset`, `CharacterConfigManager.CreateDefaultFemalePreset` (`CUser::LoadLayer`, `CUIAvatar::CUIAvatar`, `CLogin::OnPacket`, `CLogin::OnSelectWorldResult`, `CLogin::OnViewAllCharResult`, `CLogin::OnSelectCharacterResult`, `CLogin::OnExtraCharInfoResult`, `CLogin::OnCheckDuplicatedIDResult`, `CLogin::OnCreateNewCharacterResult`, `GW_CharacterStat::Decode`, `AvatarLook::Decode`, `Etc/MakeCharInfo.img/000/{male,female}`) |
| Implemented | Action coverage | Character-part loading now preserves the existing core action order and appends additional WZ action nodes discovered on each part instead of stopping at the old fixed lists | Uncommon body/equipment poses can now load when the source IMG exposes them, which closes the loader-side empty-state gap for rare, scripted, and death-adjacent actions | `CharacterLoader.StandardActions`, `CharacterLoader.AttackActions` (`CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUserLocal::SetMoveAction`) |
| Implemented | Death / rare action frames | Runtime action selection now carries raw one-shot action names through to `CharacterAssembler` instead of forcing uncommon actions back through the limited enum-only render path | Loaded death and other rare WZ actions can now survive from selection to final frame assembly instead of degrading immediately to generic fallbacks | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler` |
| Implemented | Facial expression behavior | The local player runtime now owns blink cadence and short-lived hit expressions, and updates the assembler expression state when those transitions occur | The avatar now reacts over time instead of staying pinned to the static default face | `CharacterAssembler.GetFaceFrame`, `CharacterLoader.LoadFaceExpressions`, `PlayerCharacter` (`CUserLocal::Update` emotion reset path) |
| Implemented | Equip anchor fallback rules | Equipment alignment now resolves through explicit shared map-point precedence instead of using literal pixel nudges when preferred anchors are missing | Parts stay render-safe while remaining tied to WZ anchor data rather than ad hoc offsets | `CharacterAssembler.CalculateEquipOffset`, `CharacterAssembler.TryCalculateHeadEquipOffset` |
| Implemented | Client-verified fallback order | Action lookup now prefers exact action names, then raw-action family reductions, then swim/fly aliasing, before the final `stand1` escape hatch | Character assembly now follows a documented action-family precedence order instead of jumping straight from miss to generic standing frames | `CharacterAssembler`, `CharacterPart.FindAnimation` (`CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`) |
| Partial | Morph avatar pipeline parity | MapSimulator now resolves concrete morph template ids from both `common/morph` and `PVPcommon/morph` into `Morph/*.img`, loads those images as a dedicated avatar render owner, resolves UOL-linked morph frames, preserves direct per-frame anchors like `head`, and switches buff-driven transforms onto morph-specific action playback through the existing assembler/runtime timing path instead of leaving morphs as UI-only suppression metadata. The loader also backfills missing morph animation branches through same-family base IMG fallback (`id -> id0 -> id00 -> id000`), the observed paired `100x <-> 110x` morph families, and client-backed `info/link` inheritance so partial morph sets can inherit broader action coverage instead of hard failing on the exact IMG only; the IDA pass for `CActionMan::GetMorphImgEntry` also matches the WZ-backed `info/link` seam rather than exposing a broader built-in alias table. Hostile polymorph mob skills that publish a concrete morph template id in WZ now route through the same avatar-owner swap seam, and the mob-skill runtime loader now also inherits missing per-level morph ids from earlier authored levels instead of requiring every level to repeat `x`: `MobSkill.img/172/level/1/x = 2` resolves onto `Morph/0002.img`, `MobSkill.img/173/level/1/x = 1000` resolves onto `Morph/1000.img`, and the higher `MobSkill.img/173/level/2` entry now stays polymorphed by reusing that authored template even though the level only publishes the effect branches and omits `x`. The simulator now also handles the current flag-only skill outliers whose WZ only exposes `info/morph = 1` by deriving a concrete morph template id from the skill id suffix and validating it through the same morph candidate chain the loader already uses, so `Skill/001.img/0010109/info/morph = 1` routes onto `Morph/0109.img` and `Skill/2002.img/20020111/info/morph = 1` routes onto `Morph/0111.img`, and the transform runtime no longer collapses ladder and rope onto one shared morph branch or fly and swim onto another: ladder, rope, swim, and fly now resolve through distinct transform action lists, while morphs marked by WZ `info/superman = 1` prefer the authored `ladder2`, `rope2`, and `fly2` families in line with the dedicated `IsSuperMan` branch seen in `CAvatar::MoveAction2RawAction`. Remaining gap: the simulator still does not mirror the rest of the client-only morph alias surface beyond explicit WZ template ids, authored mob-skill level inheritance, `info/link`, and the current suffix-derived fallback, and superman morphs still do not distinguish the more specific airborne `fly2Move` / `fly2Skill` cases that would need clearer client-backed move-action evidence before being mapped. | Morph-heavy jobs, the confirmed hostile morph mob-skill pair including the higher `173` level that omits `x`, the currently known flag-only morph buffs, and now the stateful ladder/rope/fly/swim branches on authored morph assets all render from the same dedicated morph-owner path rather than ordinary body/equip assembly or status-only metadata. That closes the prior runtime regression where transforms with both rope and ladder branches, or both fly and swim branches, could resolve onto the wrong authored morph action, and superman morphs now favor their dedicated `ladder2` / `rope2` / `fly2` families instead of falling back immediately to the generic branches. Remaining work is limited to client-only alias cases outside the explicit-template paths plus the narrower superman-only airborne `fly2Move` / `fly2Skill` split that is still not evidenced strongly enough to wire in yet. | avatar transform/runtime assembly (`CActionMan::GetMorphImgEntry`, `CAvatar::MoveAction2RawAction`, `CActionMan::LoadMorphAction`, `MobSkill.img/172/level/1/x`, `MobSkill.img/173/level/{1,2}`, `Skill/001.img/0010109/info/morph`, `Skill/2002.img/20020111/info/morph`, `Morph/0109.img`, `Morph/0111.img`, `Morph/1000.img/info/superman`, `Morph/1000.img/{ladder2,rope2,fly2}`) |
| Partial | Mount / vehicle / mechanic mode rendering | `TamingMob` assembly now preserves exact ride frames for normal mounts, falls back to seated passenger variants when the asset only exposes `sit`, equips ride parts for skills that publish an `eventTamingMob` mount id, routes mechanic vehicle skills onto the shared ride layer while keeping that mount owned across both direct and timer-driven tank-to-siege return transitions, treats the shared Mechanic mount (`Character/TamingMob/01932016`) as the frame-timing and visibility owner for vehicle-only actions such as `tank_*`, `siege_*`, `flamethrower*`, `rbooster*`, `tank_laser`, and other mount-owned mechanic actions confirmed in WZ, auto-equips that shared Mechanic ride whenever a Mechanic skill `action`, `prepare`, `keydown`, or `keydownend` node maps onto an exact or `tank_`-prefixed mount animation, routes the client-owned Mechanic vehicle toggle buff itself through that same mount seam so `Skill/3500.img/35001002` (`Mech: Prototype`) equips `01932016` even though WZ only publishes vehicle-type buff metadata plus the mount/unmount description and no action node, mirrors the client's explicit vehicle enter/exit ownership surface more closely by auto-detecting non-skill `TamingMob` equip or unequip changes, playing WZ-backed `ride2` / `getoff2` transitions when the active mount exposes them, and preserving the departing mount as a transient render owner so `getoff2` can complete even after the equipment slot has already been cleared. This pass extends that same ownership seam across more of the current WZ-authored client-owned ride-buff surface: while a `type = 13` vehicle buff is active, during the immediate client vehicle-valid handoff after a known mount-toggle cancel request, or while a timed ride buff only advertises ownership through its name/description rather than `info/type`, `SkillManager` now republishes the ride owner through the existing player mount seam, falling back to the currently equipped `TamingMob` for client-owned toggle families whose mount id is not authored through `eventTamingMob`. That now covers the previously missed non-`type = 13` ride-buff outliers confirmed in WZ and strings, such as `Skill/000.img/0001016`, `Skill/1000.img/10001017`, and `Skill/8000.img/80001016`, and `PlayerCharacter` continues to treat that published client-owned state as a first-class `ride2` / `getoff2` transition source plus a state-driven render owner for ordinary mounted actions instead of only watching direct equip changes. The remaining gap is narrower again: the simulator still does not consume the rest of the client's broader `SetMoveAction` / `Update` vehicle-valid catalog beyond the confirmed WZ `type = 13` vehicle buffs, the known toggle-skill handoff list, these ride-description buff outliers, direct equip transitions, and the existing Mechanic ownership path, especially client-only vehicle state sources that do not surface an active buff, a short handoff window, or a renderable current mount through the existing seams. | Event ride buffs, mechanic transforms, direct mechanic vehicle-action skills, additional WZ-backed mechanic actions like `alert3` and tank-prefixed summon or rush variants, the basic `Mech: Prototype` mount-toggle buff, non-skill mount equip or teardown swaps that expose `ride2` / `getoff2`, the broader currently authored client-owned `type = 13` vehicle-buff family, and now the confirmed ride-description buff outliers whose WZ omits `info/type` but still publishes timed invisible ride-buff ownership now transition the avatar onto the real `TamingMob` asset instead of leaving the player visually unmounted, avoid dropping back to the previous ride too early when siege sustain expires on the same tick as buff cleanup, stop double-rendering the base avatar under full-body Mechanic vehicle sprites, keep tank-mode summon or rush casts on the mount-specific action families the shared mechanic ride actually exposes, preserve the outgoing mount through `getoff2` so explicit vehicle dismount animations do not vanish when the slot clears first, and now also let client-owned ride buffs keep the active ride surfaced through the same render seam even when ownership comes from an active buff whose WZ only exposes ride intent through text plus the currently equipped mount rather than `eventTamingMob` or `type = 13`. Remaining work is limited to broader client-managed vehicle state sources outside these detected ownership paths, especially client-only update or move-action cases that still need explicit evidence before adding more non-WZ mount ownership remaps or non-equipped ride grants. | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler`, `SkillLoader`, `SkillManager`, `PlayerSkillStateRestrictionEvaluator` (`CAvatar::MoveAction2RawAction`, `CUser::Update`, `Character/TamingMob/01932016`, `Character/TamingMob/01932016/{ride2,getoff2}`, `Skill/3300.img/33001001/info/type`, `Skill/3500.img/35001002/{common,info}`, `Skill/522.img/5221006/info/type`, `Skill/000.img/0001016`, `Skill/1000.img/10001017`, `Skill/8000.img/80001016`, `String/Skill.img/33001001`, `String/Skill.img/35001002`, `String/Skill.img/5221006`, `String/Skill.img/0001016`, `String/Skill.img/10001017`, `String/Skill.img/80001016`, `Skill/3500.img/35001004/action/0`, `Skill/3510.img/35101003/action/0`, `Skill/3510.img/35101005/action/0`, `Skill/3510.img/35101006/action/0`, `Skill/3511.img/35111015/action/0`, `Skill/3512.img/35121012/action/0`, `Skill/3511.img/35111013/action/0`, `Skill/3512.img/35121007/action/0`) |
| Partial | Skill-specific avatar transforms | Mechanic-mode skills now activate dedicated avatar action families (`tank_*`, `siege_*`, `tank_siege*`) from `action/0` nodes, prefer transform-specific jump / ladder / rope / fly / swim / hit branches when those movement variants exist on the avatar, play the corresponding `_after` exit actions when those transforms clear, the prepare-skill flamethrower family now stays on its `flamethrower` / `flamethrower2` avatar branches until release triggers `flamethrower_after` / `flamethrower_after2`, Rocket Booster now transitions from its one-shot cast into the dedicated airborne `rbooster` avatar branch while preserving the mechanic tank-specific `tank_rbooster_pre` / `tank_rbooster_after` avatar variants when the booster is fired out of an active tank transform, Cyclone now promotes its `cyclone_pre` cast into the persistent `cyclone` avatar branch until buff expiry triggers `cyclone_after`, `Monkey Wave` now promotes `noiseWave_pre` into the sustained `noiseWave_ing` avatar branch before release plays the body `noiseWave` action from the WZ `action/0` node, Wild Hunter `Swallow` now uses the client-confirmed body-only `swallow_pre` branch for its initial prepare windup before holding on `swallow_loop` and clearing into `swallow`, Wild Hunter `More Wild` now keeps the body on the dedicated `wildbeast` avatar branch for the full buff window exposed by `Skill/3312.img/33121006/action/0`, Evan dragon-breath charge skills now hold `icebreathe_prepare` / `breathe_prepare` until release resolves onto `dragonIceBreathe` / `dragonBreathe`, and the shared cast-start seam now routes non-keydown prepared skills through that same resolved prepare-avatar path so body-backed `prepare/action` branches no longer briefly start on `action/0` before the hold window begins. Dual Blade charge skills `Final Cut` and `Monster Bomb` now keep the avatar on `finalCutPrepare` / `monsterBombPrepare` for the hold window before release resolves onto `finalCut` / `monsterBombThrow`, Resistance `Dual Vulcan` now promotes `dualVulcanPrep` into the sustained `dualVulcanLoop` avatar branch until release triggers `dualVulcanEnd`, Night Walker `Poison Bomb` now replaces its WZ-only `dash` prepare and dummy keydown branch with the confirmed body-backed `darkTornado_pre` -> `darkTornado` -> `darkTornado_after` avatar family exposed on `Character/00002000.img`, the generic prepared-skill hold path now falls back from missing avatar-only `prepare/action` names onto renderable release-family branches when WZ and the loaded body disagree, and the matching keydown-release path now resolves missing avatar-only `keydownend/action` names onto renderable release-family branches before clearing. That closes the remaining prepared-skill exit regression on confirmed body-backed families such as the Demon blunt-smash charge line (`Skill/3100.img/31001000` / `Skill/3110.img/31101000` -> `Character/00002000.img/bluntSmash`) instead of letting `bluntSmashEnd` / `soulEater_end` fall back to a stand pose after the hold. This pass also rechecked the client seam in `CUserLocal::DoActiveSkill_Prepare`: `Swallow` remains the only currently confirmed client-side prepare override in this area (`Skill/3310.img/33101005/prepare/action = swallow_loop`, but the client forces the body-backed `swallow_pre` action code before hold). The remaining gap is now limited to candidate client-only prepared or transform families whose bodies expose plausible dedicated branches but whose remaps or exit branches are still not named in WZ and are not otherwise confirmed by the inspected client path, so they remain intentionally unmapped until more client evidence is found. | Mechanic avatar movement now stays on transform-family motion frames when the loaded body actually exposes them instead of collapsing every non-walk state back to the stand pose, while flamethrower, Rocket Booster, Cyclone, Monkey Wave, Wild Hunter Swallow, Wild Hunter More Wild, Evan dragon-breath charges, Dual Blade `Final Cut` / `Monster Bomb`, Battle Mage `Dual Vulcan`, Night Walker `Poison Bomb`, and the confirmed Demon prepared-skill charge families now keep both their visible hold windows, their non-keydown prepare starts, and any renderable release clear on body-backed avatar branches rather than dropping back to generic standing when WZ publishes a missing or non-renderable `prepare/action` or `keydownend/action`; broader transform parity remains incomplete only for the remaining client-shaped remaps whose final avatar family is not named directly in WZ or confirmed in the inspected client path | `SkillManager`, `PlayerCharacter.TriggerSkillAnimation`, `CharacterAssembler` (`14111006`, `22121000`, `22151001`, `23121000`, `31001000`, `31101000`, `31101002`, `32121003`, `33101005`, `33121006`, `4341002`, `4341003`, `5311002`, `35001001`, `35101009`, `35101004`, `35121005`, `35111004`, `35121013`, `CAvatar::MoveAction2RawAction`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::TryDoingSwallowBuff`, `Skill/1411.img/14111006/{prepare,keydown}`, `Skill/2212.img/22121000/{prepare,action}`, `Skill/2215.img/22151001/{prepare,action}`, `Skill/2312.img/23121000/{prepare,keydown,keydownend}`, `Skill/3100.img/31001000/{prepare,action,keydownend}`, `Skill/3110.img/31101000/{prepare,action,keydownend}`, `Skill/3110.img/31101002/{prepare,action}`, `Skill/3310.img/33101005/prepare/action`, `Skill/3312.img/33121006/action/0`, `Skill/434.img/4341002/{prepare,action}`, `Skill/434.img/4341003/{prepare,action}`, `Skill/531.img/5311002/{prepare,keydown,action}`, `Character/00002000.img/{darkTornado_pre,darkTornado,darkTornado_after,icebreathe_prepare,dragonIceBreathe,breathe_prepare,dragonBreathe,bluntSmash,demonTrace,dualVulcanPrep,dualVulcanLoop,dualVulcanEnd,finalCutPrepare,finalCut,monsterBombPrepare,monsterBombThrow,swallow_pre,swallow_loop,swallow,wildbeast}`) |
| Partial | Client-managed avatar effect layers | The simulator now loads persistent avatar-bound skill layer branches from WZ (`special`, `special0`, `finish`, `finish0`, ladder-aware `back` / `back_finish`, and `repeat` on `suddenDeath` skills), attaches them to the player render path as over-character / under-face layers, switches the `More Wild` family onto its ladder override, plays finish cleanup on buff expiry or cancellation, routes `doubleJump` action-family movement skills onto a client-style transient avatar-owned layer instead of the generic cast sprite, extends that same transient seam across the broader non-buff movement families the existing resolver already classifies (`Rush`, backstep, bound-jump / assaulter / flash, and flying-rush variants) so WZ-backed movement casts like `Skill/122.img/1221007/{effect,effect0}`, `Skill/433.img/4331005/{effect,effect0}`, and `Skill/3001.img/30010186/effect` suppress the detached cast sprite and keep authored planes bound to the avatar for the movement window, now also honors movement or buff shapes that only publish the secondary `effect0` branch by treating `effect` and `effect0` as equivalent client-owned sources for promotion instead of requiring the primary branch to exist, promotes fly/flying buff `effect` branches onto looping player-owned avatar layers so wing visuals stay attached to the avatar for the full buff window instead of rendering as detached cast sprites, promotes Dark Sight-family `effect` branches such as `Skill/400.img/4001003/effect` and `Skill/1400.img/14001003/effect` onto the same looping avatar-owned layer path instead of leaving them as detached cast sprites, promotes invisible swallow buff `effect` branches such as `Skill/3310.img/33101006/effect` onto a client-style under-face avatar layer that now hides while the player is on a ladder or rope, now preserves sibling `effect0` branches on transient client-owned movement layers as an extra avatar-bound plane instead of dropping them, also promotes root-`invisible` buff skills whose WZ publishes only timed `effect/effect0` planes without `affected` or `special` branches, such as `Skill/000.img/0001037/{effect,effect0}`, onto the same looping avatar-owned path instead of leaving them detached, also promotes WZ-backed damage-reflect buff visuals that publish timed `effect/effect0` without `affected` or `special` branches, such as `Skill/110.img/1101007`, `Skill/120.img/1201007`, and `Skill/3110.img/31101003`, onto looping avatar-owned layers while still suppressing those client-owned avatar layers whenever Oak Barrel-style morph buffs or full-body Mechanic ride actions own the sprite, now extends that same looping avatar-owned fallback to the broader timed-buff WZ shape where a self-buff only publishes `effect/effect0` companion planes without `affected` or `special*`, and no longer mirrors a lone promoted plane onto both avatar planes when WZ only authored one side, which keeps single-plane `effect`-only or `effect0`-only visuals on their authored z side instead of double-drawing them across under-face plus overlay. This pass also widens the smaller `CUser::Update`-owned affected-area seam that IDA confirmed through `CUser::UpdateAr01Effect`: while the player stands inside a qualifying friendly player-skill affected area that can apply to the local player and WZ publishes a looping `affected` branch, the simulator now tracks that area instance by object id and promotes its `affected` animation onto an under-face avatar-owned layer instead of leaving the local actor without the client-owned aura. That keeps the confirmed `Amplifier Robot AF-11` (`Skill/3512.img/35121010/affected`) surface, and now also covers the same friendly affected-area WZ shape for broader mass-spell or support-zone skills rather than hardcoding only the single `AR01` skill id. The remaining gap is narrower again: broader client-only `CUser::Update` families beyond `More Wild`, swallow, double-jump, the existing movement or buff WZ shapes, and this currently promoted friendly affected-area shape still need explicit client-backed ownership rules before they can be mirrored safely. | Visible parity is closer for `More Wild`, `Final Cut`-style sudden-death buffs, the broader WZ-backed movement cast surface, flying-wing buffs, Dark Sight-family stealth buffs, root-`invisible` effect-only buffs, Wild Hunter swallow buffs, the confirmed damage-reflect buff family, the secondary-only `effect0` surface, the broader timed self-buff `effect/effect0` pair surface, and now the client-owned friendly affected-area aura surface because the promoted avatar-owned layers now follow the client's ownership rules more closely, no longer require the primary `effect` branch to exist before promotion, no longer double-render single-plane authored visuals on both avatar planes, and now also surface under-face `affected` auras while the player stands inside qualifying local- or party-owned player-skill affected areas such as `35121010`, plus the same WZ-authored mass-spell or support-zone area shape. The remaining work is narrower: broader `CUser::Update`-owned effect families that do not publish these WZ-authored `special*`, timed `effect/effect0`, invisible/effect-only buff, movement-action, or the now-covered friendly affected-area `affected` shape still need explicit client-backed remaps before they can be mirrored safely. | `PlayerCharacter`, `PlayerManager`, `SkillLoader`, `SkillManager`, `CharacterAssembler` (`CUser::Update`, `CUser::UpdateMoreWildEffect`, `CUser::UpdateAr01Effect`, `CUserLocal::TryDoingSwallowBuff`, `Skill/3312.img/33121006/{special,special0,finish,finish0,back,back_finish}`, `Skill/434.img/4341002/repeat`, `Skill/400.img/4001003/effect`, `Skill/1400.img/14001003/effect`, `Skill/310.img/3101003/effect`, `Skill/3300.img/33001002/effect`, `Skill/122.img/1221007/{effect,effect0}`, `Skill/433.img/4331005/{effect,effect0}`, `Skill/3001.img/30010186/effect`, `Skill/3310.img/33101006/effect`, `Skill/000.img/0001037/{effect,effect0,invisible}`, `Skill/110.img/1101007/{effect,effect0}`, `Skill/120.img/1201007/{effect,effect0}`, `Skill/3110.img/31101003/{effect,effect0}`, `Skill/1411.img/14111000/effect0`, `Skill/510.img/5101007`, `Skill/3512.img/35121010/affected`, `Skill/3512.img/35121007/affected`, `Skill/3312.img/33121007/affected`, `Skill/3200.img/32001003/affected`, `Skill/3210.img/32101002/affected`, `Skill/3210.img/32101003/affected`) |
| Partial | Remote user / `CUserPool` actor parity | The simulator now has a first-class shared remote-user actor seam instead of only feature-local placeholders. `RemoteUserActorPool` owns remote AvatarLook decode to `CharacterBuild`, per-user insertion/removal, shared world draw, name lookup, chair and mount state, Battlefield team tagging, minimap helper marker registration, prepared-skill world overlays, and a decoded movement-snapshot path that samples packet-style `CMovePath` data into per-actor position, facing, foothold, and action updates. `MapSimulator` now also exposes a `CUserPool::OnPacket`-shaped `/remoteuser packet` / `/remoteuser packetraw` dispatcher that routes supported enter/leave plus common-vs-remote packet families (`179/180/181/182/183/184/210/211/212/213`, plus the simulator-owned melee seam `214`) through one owner instead of mutating remotes only through ad hoc feature commands, and the general enter-field owner now mirrors the existing Ariant handoff by applying packet-authored initial portable-chair state immediately after AvatarLook spawn rather than dropping the remote into a stand pose until a later chair packet arrives. Wedding's feature-local groom, bride, and audience actors remain migrated onto the shared pool: the wedding field exposes cloned actor snapshots for the existing actor state, `MapSimulator` rehydrates those snapshots through the shared pool after wedding command and inbox mutations, the wedding runtime clears its shared-pool source tag when the field deactivates, and the field-owned remote draw path is suppressed once the shared owner takes over so the same actors are not rendered twice. Messenger room occupants also continue to collapse onto that same owner instead of staying UI-only, and this pass closes the biggest remaining messenger build gap: `MessengerPacketCodec` member-info payloads can now optionally carry a length-prefixed `AvatarLook`, `MessengerRuntime` preserves that packet-authored look on the contact and live participant state, and `MapSimulator` rehydrates hidden messenger occupants through the shared pool from the decoded `AvatarLook` plus member metadata when present instead of always cloning a local-template build. This matches the client evidence that `CUserPool::OnPacket` owns user enter/leave and common-vs-remote dispatch, `CUserPool::Update` advances remote actors plus shared social state, and `CUserRemote::Update` owns remote movement, action, keydown-prepare, and portable-chair presentation before falling through to `CUser::Update`. | The remaining gap is now narrower: any other feature-local remote shells still need to collapse onto the shared pool; the packet router still only covers the implemented enter/leave, move/state, helper/team, chair/mount, prepared-skill, and simulator-owned melee families rather than the broader client packet surface; Messenger member parity still depends on the simulator's optional member-info `AvatarLook` trailer instead of a fuller official messenger packet decode and broader room-member lifecycle; and `CUserRemote::Update` parity is still incomplete for one-time-action/effect families such as remote afterimages, shared social overlays from `CUserPool::Update` (couple/friendship/new-year/marriage surfaces), packet-authored user effects, and higher-fidelity movement smoothing beyond the current sampled move-path seam. | shared field actor/runtime layer (`CUserPool::OnPacket`, `CUserPool::Update`, `CUserRemote::Update`, `CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUser::Update`, `CMovePath::{Decode,OnMovePacket}`) |


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Remote actor foundation pass:
   Add a real `CUserPool`-style remote-user actor layer before continuing to extend wedding, Ariant, Battlefield, minimap-helper, and follow-request overlays independently.
3. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
4. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
5. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
6. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
