# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CAnimationDisplayer::Effect_ByItem` at `0x44f330`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Avatar and player parity

- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::SetActivePet` at `0x8e40e0`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CAnimationDisplayer::Effect_ByItem` at `0x44f330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::Update` at `0x937330`

Notes:
`CUser::LoadLayer` and `CUser::PrepareActionLayer` are the clearest resolved avatar-composition seams for layer loading and action-layer setup. `CUser::Update` and `CUser::UpdateMoreWildEffect` also show that the client maintains several additional avatar/effect layers outside the base assembler path, while `CUserLocal::Update` contains the local-player emotion reset path and action/state transitions that matter for expression and rare-action parity. A later IDA pass also confirms that remote avatar ownership is a first-class client surface rather than a loose extension of local-player logic: `CUserPool::OnPacket` dispatches enter, leave, common, remote, and local user packet families, `CUserPool::Update` advances local plus remote users and shared social-effect state, `CUserRemote::Update` owns remote move-action, effect, portable-chair, and prepared-skill presentation, and `CAnimationDisplayer::Effect_ByItem` confirms that one-shot item effects are managed by a dedicated client animation-owner seam rather than only by feature-local overlay code.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Avatar and player parity

These are the biggest remaining gaps for visible client parity.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Default avatar selection | The simulator keeps the deterministic, WZ-verified SuperGM/Wizet fallback selection of `1002140` (Wizet Invincible Hat), `1042003` (Wizet Plain Suit), `1062007` (Wizet Plain Suit Pants), `1072005` (Leather Sandals), `1092008` (Pan Lid), and `1322013` (Wizet Secret Agent Suitcase) with matching face, hair, level, and SuperGM job metadata, and the no-packet avatar seam is now shared all the way through preset-backed defaults as well. WZ was rechecked again for this row in `Etc/MakeCharInfo.img/000/{male,female}` and still pins the non-sale starter request layout to the client-owned `0..7` face/hair/skin/hairColor/coat/pants/shoes/weapon families (`male/0 = {20000,20001,20002}`, `male/1 = {30030,30020,30000}`, `male/2 = {0,7,3,2}`, `male/3 = {0,1,2,3}`, `male/4 = {1040002,1040006,1040010}`, `male/5 = {1060002,1060006}`, `male/6 = {1072001,1072005,1072037,1072038}`, `male/7 = {1302000,1322005,1312004}`, `female/0 = {21000,21001,21002}`, `female/1 = {31000,31040,31050}`, `female/2 = {0,7,3,2}`, `female/3 = {0,1,2,3}`, `female/4 = {1041002,1041006,1041010,1041011}`, `female/5 = {1061002,1061008}`, `female/6 = {1072001,1072005,1072037,1072038}`, `female/7 = {1302000,1322005,1312004}`). `CharacterPreset` now owns a synthesized `LoginAvatarLook`, build-backed preset capture preserves hidden equip lists, pet ids, and packet-owned `weaponStickerID` visuals through that owner, `CharacterPreset.ToBuild` rebuilds through `CharacterLoader.LoadFromAvatarLook` whenever the synthesized owner is present, and `CharacterConfigManager.CreateDefaultMalePreset` / `CreateDefaultFemalePreset` now create the default preset from the same synthesized `LoginAvatarLook` source used by roster previews and packet emission instead of re-materializing only scalar selection metadata. Legacy preset files without a serialized `AvatarLook` still load through the existing scalar fallback for compatibility, but the default-avatar parity gap for new simulator-owned presets is closed. | Default avatars, synthesized login previews, packet-authored avatars, and preset-driven defaults now converge on the same render-safe avatar owner, so local default tooling no longer risks silently dropping hidden equips, pets, or weapon stickers just because no packet bytes existed. Focus for later avatar work can stay on other backlog rows instead of this ownership split. | `CharacterLoader.LoadDefaultMale`, `CharacterLoader.LoadDefaultFemale`, `CharacterLoader.LoadFromAvatarLook`, `LoginAvatarLookCodec`, `LoginCharacterRosterEntry.CreateRuntimeBuild`, `MapSimulator.ResolveLoginRosterAvatarLook`, `MapSimulator.CreateLoginPacketCharacterEntry`, `MapSimulator.CreateLoginCharacterAccountEntryState`, `CharacterPreset.FromBuild`, `CharacterPreset.ToBuild`, `CharacterConfigManager.CreateDefaultMalePreset`, `CharacterConfigManager.CreateDefaultFemalePreset`, `UnitTest_MapSimulator/DefaultAvatarSelectionPresetParityTests.cs` (`CUser::LoadLayer`, `CUIAvatar::CUIAvatar`, `GW_CharacterStat::Decode`, `AvatarLook::Decode`, `Etc/MakeCharInfo.img/000/{male,female}`) |
| Implemented | Action coverage | Character-part loading now preserves the existing core action order and appends additional WZ action nodes discovered on each part instead of stopping at the old fixed lists | Uncommon body/equipment poses can now load when the source IMG exposes them, which closes the loader-side empty-state gap for rare, scripted, and death-adjacent actions | `CharacterLoader.StandardActions`, `CharacterLoader.AttackActions` (`CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUserLocal::SetMoveAction`) |
| Implemented | Death / rare action frames | Runtime action selection now carries raw one-shot action names through to `CharacterAssembler` instead of forcing uncommon actions back through the limited enum-only render path | Loaded death and other rare WZ actions can now survive from selection to final frame assembly instead of degrading immediately to generic fallbacks | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler` |
| Implemented | Facial expression behavior | The local player runtime now owns blink cadence and short-lived hit expressions, and updates the assembler expression state when those transitions occur | The avatar now reacts over time instead of staying pinned to the static default face | `CharacterAssembler.GetFaceFrame`, `CharacterLoader.LoadFaceExpressions`, `PlayerCharacter` (`CUserLocal::Update` emotion reset path) |
| Implemented | Equip anchor fallback rules | Equipment alignment now resolves through explicit shared map-point precedence instead of using literal pixel nudges when preferred anchors are missing | Parts stay render-safe while remaining tied to WZ anchor data rather than ad hoc offsets | `CharacterAssembler.CalculateEquipOffset`, `CharacterAssembler.TryCalculateHeadEquipOffset` |
| Implemented | Client-verified fallback order | Action lookup now prefers exact action names, then raw-action family reductions, then swim/fly aliasing, before the final `stand1` escape hatch | Character assembly now follows a documented action-family precedence order instead of jumping straight from miss to generic standing frames | `CharacterAssembler`, `CharacterPart.FindAnimation` (`CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`) |
| Partial | Morph avatar pipeline parity | MapSimulator still resolves concrete morph template ids from both `common/morph` and `PVPcommon/morph` into `Morph/*.img`, loads those images as a dedicated avatar render owner, resolves UOL-linked morph frames, preserves direct per-frame anchors like `head`, backfills missing branches through same-family base IMG fallback (`id -> id0 -> id00 -> id000`), the observed paired `100x <-> 110x` families, and WZ-backed `info/link` inheritance, and routes hostile polymorph mob skills plus the current flag-only `info/morph = 1` skill outliers through that same avatar-owner seam. The transform runtime still keeps ladder, rope, swim, fly, superman `ladder2` / `rope2` / `fly2`, airborne `fly2Move` / `fly2Skill`, authored combat, hit, death, sit, and jump-special branches on morph assets instead of degrading immediately to locomotion. This run narrows the remaining resolver/runtime seam further without widening it beyond WZ evidence: `MorphClientActionResolver` now not only treats generic `shot*` inputs such as the client-shaped `shotC1` family as attack aliases alongside `attack*` / `stab*` / `swing*` / `shoot*`, but also treats raw ranged names containing `shot`, `spear`, `rain`, or `break` as archer-preferring requests before authored-family ordering is chosen, and now treats direct generic `doubleJump` requests as the same jump-special family already evidenced by authored WZ branches such as `archerDoubleJump`, `iceDoubleJump`, and `icemandoubleJump`. That keeps mixed or fallback-merged morph builds from checking pirate-authored branches ahead of the WZ-authored archer branches exposed by `Morph/1003.img` and `Morph/1103.img`, still falls through from `shotC1` onto the actual generic WZ `shoot*` branches after authored aliases like `{windshot,windspear,stormbreak,arrowRain}` and before the final raw-name fallback, and now also lets direct jump-special requests stay on authored morph jump branches instead of slipping back to plain `jump` as soon as the transform runtime rebuilds its per-state action lists. The morph attack-variant list in `PlayerCharacter` still seeds `shotC1` directly and classifies WZ-authored archer names containing `shot`, `spear`, `rain`, or `break` as first-class combat branches so those morph attacks stay ahead of generic walk or stand fallback once loaded, while the jump-state variant list now preserves a direct jump-special request ahead of plain `jump` only when the incoming transform action is itself jump-family. Focused resolver coverage is now checked in for this row in `UnitTest_MapSimulator/MorphClientActionResolverTests.cs`, including mixed-family `shotC1` ordering, pirate-first melee ordering, ice authored-combat promotion, plain-`jump` plus direct-`doubleJump` jump-special promotion, alert-family, and duplicate-suppression cases that pin the resolver seam directly. WZ was rechecked again for this row (`Morph/1000.img`, `1001.img`, `1003.img`, `1100.img`, `1101.img`, `1103.img`, `2001.img`, `2002.img`, `2003.img`) against the current extracted dataset, and an IDA re-check confirmed the client still routes this surface through `CActionMan::LoadMorphAction`, but the raw action-code-to-name table behind that function still is not recovered cleanly enough yet to justify broader non-WZ alias additions. Remaining gap: the simulator still does not mirror every client-only morph alias that may sit behind that raw `CActionMan::LoadMorphAction` table, especially action-code-to-name remaps not surfaced by explicit WZ branch names or the now-promoted authored combat, alert, jump-special, and `shotC1 -> shoot*` path; direct execution of the new resolver tests is also still blocked in this workspace by unrelated pre-existing `HaCreator` build failures before `UnitTest_MapSimulator` can finish compiling, with the current blocker in this workspace now being the duplicate `UIWindowLoader.LoadIndexedCanvasTextures` member in `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs` under the generated WPF temp project, so broader non-WZ alias expansion remains deferred until more client evidence is available. | Morph-heavy jobs, the confirmed hostile morph mob-skill pair including the higher `173` level that omits `x`, the currently known flag-only morph buffs, and the authored ladder/rope/fly/swim, combat, hit, death, sit, alert, and jump-special branches all stay on the dedicated morph-owner path rather than ordinary body/equip assembly or status-only metadata. This run closes the remaining WZ-backed jump/request seam inside that path more completely: generic raw ranged names like `shotC1` still keep authored archer morph combat branches ahead of pirate-authored branches even on mixed family builds and still fall through to the actual generic WZ `shoot1` / `shoot2` / `shootF` branches when those authored aliases are absent, while direct `doubleJump` requests now also promote onto authored morph jump-special branches such as `archerDoubleJump`, `iceDoubleJump`, and `icemandoubleJump` instead of immediately degrading to plain `jump`. The checked-in tests pin that family ordering, `shoot*` fallback, ice authored-combat promotion, plain-`jump` and direct-`doubleJump` jump-special promotion, alert-family, and duplicate-suppression behavior directly at the resolver seam; remaining work is limited to extra client-only morph alias-table cases if later IDA work recovers them. | avatar transform/runtime assembly (`CActionMan::GetMorphImgEntry`, `CAvatar::MoveAction2RawAction`, `CActionMan::LoadMorphAction`, `PlayerCharacter.CreateMorphTransform`, `MorphClientActionResolver`, `UnitTest_MapSimulator/MorphClientActionResolverTests.cs`, `MobSkill.img/172/level/1/x`, `MobSkill.img/173/level/{1,2}`, `Skill/001.img/0010109/info/morph`, `Skill/2002.img/20020111/info/morph`, `Morph/0006.img/sit`, `Morph/0109.img`, `Morph/0111.img`, `Morph/1000.img/{info/superman,alert2,alert3,alert4,alert5,fist,recovery,sit,timeleap,shockwave,eburster,edrain,dragonstrike,eorb,ladder2,rope2,fly2,fly2Move,fly2Skill,wave}`, `Morph/1001.img/{alert2,alert3,alert4,demolition,snatch}`, `Morph/1003.img/{alert2,alert4,windshot,windspear,stormbreak,arrowRain,archerDoubleJump,shoot1,shootF}`, `Morph/1100.img/{recovery,timeleap,shockwave,eburster,edrain,dragonstrike,eorb}`, `Morph/1101.img/{demolition,snatch}`, `Morph/1103.img/{windshot,windspear,archerDoubleJump,shoot1,shootF}`, `Morph/2001.img/{icemanAttack,icemandoubleJump}`, `Morph/2002.img/{iceAttack1,iceAttack2,iceDoubleJump,iceSmash,iceTempest,iceChop,icePanic,dead,pvpko}`, `Morph/2003.img/sit`) |
| Partial | Mount / vehicle / mechanic mode rendering | `TamingMob` assembly still preserves exact ride frames for normal mounts, falls back to seated passenger variants when the asset only exposes `sit`, equips ride parts for `eventTamingMob` skills, routes Mechanic vehicle skills onto the shared `01932016` ride layer, keeps that owner alive across direct and timer-driven tank-to-siege handoff, claims the client-owned Mechanic toggle buff `35001002`, mirrors non-skill equip and unequip `ride2` / `getoff2` transitions, recognizes the WZ-authored Battleship mounted and support family, and rebuilds or preserves client-owned mount ownership for distinct mounted actions such as `tank_*`, `msummon*`, `tank_laser`, Battleship `cannon` / `torpedo` / `fireburner` / `coolingeffect`, plus borrowed `alert2` / `alert3`, `ride2`, and `getoff2` actions. This pass narrows the remaining client-only edge further by extending that same current-action seam to the broader mounted move-action surface that is both authored on the real ride assets and explicitly accepted by the client riding path: the resolver now keeps a known active-skill or equipped Mechanic / Battleship owner through generic mounted movement or climb actions such as `walk1`, `walk2`, `stand1`, `stand2`, `jump`, `prone`, `fly`, `tired`, Mechanic `alert`, `alert2`, `proneStab`, `ladder`, `rope`, `ladder2`, and `rope2`, matching the WZ branches on `Character/TamingMob/01932016` and `Character/TamingMob/01932000` and the client-owned `IsAbleTamingMobAction` / `SetResistanceRidingMoveAction` surface instead of dropping back to an unmounted body as soon as the live action stops being a mount-only attack. Focused regression coverage is now checked in under `UnitTest_MapSimulator/ClientOwnedVehicleSkillTrackingTests.cs` for that owned-current-action seam, including the new generic mounted move-action cases and the existing distinct ownerless Mechanic fallback. The remaining gap is narrower again: the simulator still does not mirror the rest of the client's broader vehicle state sources beyond these WZ-authored ride buffs, known toggle-skill handoff ids, invisible ride-description buffs, direct equip transitions, the confirmed Battleship mounted and support family, fixed-state Mechanic mode skills, direct Mechanic mounted-action skills, and the now-covered still-owned mounted move-action surface, especially client-only update or move-action cases that do not expose an active buff, a recent handoff owner, an equipped owner, or one of the currently confirmed mounted action names. | Event ride buffs, mechanic transforms, direct mechanic vehicle-action skills, WZ-backed fixed-state Mechanic mode skills such as Siege Mode, Missile Tank, and Tank Siege Mode, additional WZ-backed mechanic actions like `alert3` and tank-prefixed summon or rush variants, the basic `Mech: Prototype` mount-toggle buff, non-skill mount equip or teardown swaps that expose `ride2` / `getoff2`, the broader currently authored client-owned `type = 13` vehicle-buff family, the wider invisible ride-description buff family whose WZ omits `info/type` but still publishes timed ride ownership through `invisible` plus ride text, the Battleship-only `cannon`, `torpedo`, `fireburner`, and `coolingeffect` action family, and now the broader still-owned mounted movement surface on the real ride assets all transition the avatar onto or keep it on the correct `TamingMob` owner instead of quietly falling back to an unmounted body during normal mounted walk, stand, jump, fly, prone, alert, or climb states. Remaining work is limited to broader client-managed vehicle state sources outside these detected ownership paths, especially any remaining client-only update or move-action cases and any non-WZ vehicle-valid support or state families that still need explicit client evidence before adding more mount ownership remaps or non-equipped ride grants. | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler`, `SkillLoader`, `SkillManager`, `PlayerSkillStateRestrictionEvaluator`, `FieldSkillRestrictionEvaluator`, `ClientOwnedVehicleSkillClassifier`, `UnitTest_MapSimulator/ClientOwnedVehicleSkillTrackingTests.cs` (`CAvatar::MoveAction2RawAction`, `CAvatar::SetResistanceRidingMoveAction`, `?IsAbleTamingMobAction@@YAHJJ@Z`, `CUser::Update`, `Character/TamingMob/01932016/{walk1,walk2,stand1,stand2,jump,prone,proneStab,ladder,rope,fly,tired,alert,alert2,alert3,ride2,getoff2,tank_laser,msummon2,ladder2,rope2}`, `Character/TamingMob/01932000/{walk1,walk2,stand1,stand2,jump,prone,fly,tired,alert2,alert3,cannon,torpedo,fireburner,coolingeffect}`, `Skill/3300.img/33001001/info/type`, `Skill/3500.img/35001002/{common,info}`, `Skill/3511.img/35111004/{action,info}`, `Skill/3512.img/35121005/{action,info,req}`, `Skill/3512.img/35121013/{action,info}`, `Skill/520.img/5201002`, `Skill/520.img/5201003/action/0`, `Skill/521.img/5211001`, `Skill/521.img/5211002`, `Skill/521.img/5211004/action/0`, `Skill/521.img/5211005/action/0`, `Skill/522.img/5221000/action/0`, `Skill/522.img/5221006/info/type`, `Skill/522.img/5221007/{action,req}`, `Skill/522.img/5221008/{action,req}`, `Skill/522.img/5221010`, `Skill/000.img/0001016`, `Skill/000.img/0001069`, `Skill/1000.img/10001017`, `Skill/8000.img/80001016`, `String/Skill.img/33001001`, `String/Skill.img/35001002`, `String/Skill.img/35111004`, `String/Skill.img/35121005`, `String/Skill.img/35121013`, `String/Skill.img/5201002`, `String/Skill.img/5201003`, `String/Skill.img/5211001`, `String/Skill.img/5211002`, `String/Skill.img/5211004`, `String/Skill.img/5211005`, `String/Skill.img/5221000`, `String/Skill.img/5221006`, `String/Skill.img/5221007`, `String/Skill.img/5221008`, `String/Skill.img/5221010`, `String/Skill.img/0001016`, `String/Skill.img/0001069`, `String/Skill.img/10001017`, `String/Skill.img/80001016`, `String/Skill.img/5221006/desc`, `Skill/3500.img/35001001/action/0`, `Skill/3510.img/35101003/action/0`, `Skill/3510.img/35101005/action/0`, `Skill/3510.img/35101006/action/0`, `Skill/3510.img/35101009/{action,prepare,keydown,keydownend}`, `Skill/3511.img/35111015/action/0`, `Skill/3512.img/35121012/action/0`, `Skill/3511.img/35111013/action/0`, `Skill/3512.img/35121007/action/0`) |
| Partial | Skill-specific avatar transforms | Mechanic-mode skills now activate dedicated avatar action families (`tank_*`, `siege_*`, `tank_siege*`) from `action/0` nodes, prefer transform-specific jump / ladder / rope / fly / swim / hit branches when those movement variants exist on the avatar, and play the corresponding `_after` exit actions when those transforms clear. The prepared-skill families on this row remain routed through the existing transform seam, and this pass closes the remaining WZ-backed cast-start remap holes that were still visible there: flamethrower still holds on `flamethrower` / `flamethrower2` until release triggers `flamethrower_after` / `flamethrower_after2`; Rocket Booster still keeps its WZ-backed ground startup on `rbooster_pre` while airborne motion promotes onto the dedicated `rbooster` body branch and preserves the tank-specific `tank_rbooster_pre` / `tank_rbooster_after` variants; Wild Hunter `Swallow` still uses the client-confirmed body-only `swallow_pre` branch for its prepare windup before holding on `swallow_loop` and clearing into `swallow`; Night Walker `Poison Bomb` still normalizes its WZ-owned `dash` prepare onto the body-backed `darkTornado_pre` -> `darkTornado` -> `darkTornado_after` family; and the shared prepare resolver now also explicitly remaps WZ-backed `cyclone_pre` onto body `cyclone`, `noiseWave_pre` onto body `noiseWave_ing`, and `dualVulcanPrep` onto body `dualVulcanLoop` so those families no longer briefly render the authored prepare pose when the row expects the dedicated hold branch. `More Wild` still stays on `wildbeast`; Evan breath charges still hold `icebreathe_prepare` / `breathe_prepare` before resolving onto `dragonIceBreathe` / `dragonBreathe`; and Dual Blade `Final Cut` / `Monster Bomb` still hold on `finalCutPrepare` / `monsterBombPrepare` before resolving onto `finalCut` / `monsterBombThrow`. The generic prepared-skill hold and keydown-release paths still fall back from missing avatar-only `prepare/action` or `keydownend/action` names onto renderable release-family branches such as Demon `bluntSmash` instead of dropping to a stand pose. Focused regression coverage is now checked in under `UnitTest_MapSimulator/PreparedSkillAvatarTransformParityTests.cs`, directly guarding the confirmed `darkTornado_pre`, `swallow_pre`, `cyclone`, `noiseWave_ing`, and `dualVulcanLoop` prepare remaps, the Demon charge-release fallback onto renderable `bluntSmash` when WZ requests either `bluntSmashEnd` or `soulEater_end`, the Rocket Booster transform jump/exit seam, and the tank-vs-normal Rocket Booster startup resolver. WZ was rechecked again for this row against the current extracted data source (`Skill/1411.img/skill/14111006/prepare/action = dash`, `Skill/3212.img/skill/32121003/prepare/action = cyclone_pre`, `Skill/3310.img/skill/33101005/prepare/action = swallow_loop`, `Skill/531.img/skill/5311002/{prepare/action = noiseWave_pre, keydown/action = noiseWave_ing, action/0 = noiseWave}`, `Skill/2312.img/skill/23121000/{prepare/action = dualVulcanPrep, keydown/action = dualVulcanLoop, keydownend/action = dualVulcanEnd}`, `Skill/3100.img/skill/31001000/keydownend/action = bluntSmashEnd`, `Skill/3110.img/skill/31101000/action/0 = bluntSmash`, `Skill/3110.img/skill/31101000/keydownend/action = soulEater_end`, `Skill/3510.img/skill/35101004/action/0 = rbooster_pre`, `Skill/3500.img/skill/35001001/action/0 = flamethrower`, `Skill/3512.img/skill/35121013/action/0 = tank_siegepre`, while `Character/00002000.img` still exposes `darkTornado_pre`, `swallow_pre`, `cyclone_pre`, `cyclone`, `cyclone_after`, `noiseWave_pre`, `noiseWave_ing`, `noiseWave`, `dualVulcanPrep`, `dualVulcanLoop`, `dualVulcanEnd`, `bluntSmash`, `rbooster_pre`, `rbooster`, `tank_rbooster_pre`, and `tank_rbooster_after` but not body-backed `bluntSmashEnd`, `soulEater_end`, or `soulEater`). `Swallow` still remains the only currently confirmed client-side prepare override in this area from the inspected `CUserLocal::DoActiveSkill_Prepare` seam, and no additional client-backed remaps surfaced beyond the WZ-safe families now implemented. The remaining gap is therefore limited to candidate client-only prepared or transform families whose bodies expose plausible dedicated branches but whose remaps or exit branches are still neither named in WZ nor otherwise confirmed by the inspected client path, so they remain intentionally unmapped until more client evidence is found; direct execution of this regression file is still blocked by unrelated pre-existing compile failures in the workspace, with full `dotnet test` currently failing first on syntax errors in `HaCreator/MapSimulator/MapSimulator.cs` under the generated WPF temp project and a no-project-reference rebuild still failing on unrelated `UnitTest_MapSimulator` errors in `ItemMakerParityTests.cs` and `MapSimulatorChatWhisperParityTests.cs`. | Mechanic avatar movement stays on transform-family motion frames when the loaded body actually exposes them instead of collapsing every non-walk state back to the stand pose, while flamethrower, Rocket Booster, Cyclone, `Monkey Wave`, Wild Hunter `Swallow`, Wild Hunter `More Wild`, Evan dragon-breath charges, Dual Blade `Final Cut` / `Monster Bomb`, Battle Mage `Dual Vulcan`, Night Walker `Poison Bomb`, and the confirmed Demon prepared-skill charge families now keep the intended body-backed hold window from cast start through release more consistently. The remaining parity gap on this row is no longer the known WZ-backed `cyclone`, `Monkey Wave`, or `Dual Vulcan` prepare families; it is limited to still-unconfirmed client-only remap or exit families whose final avatar action names are neither published in WZ nor yet recovered from the inspected client seam. Verification remains blocked only by unrelated pre-existing compile failures elsewhere in the workspace. | `SkillManager`, `PlayerCharacter`, `CharacterAssembler`, `UnitTest_MapSimulator/PreparedSkillAvatarTransformParityTests.cs` (`14111006`, `22121000`, `22151001`, `23121000`, `31001000`, `31101000`, `31101002`, `32121003`, `33101005`, `33121006`, `4341002`, `4341003`, `5311002`, `35001001`, `35101009`, `35101004`, `35121005`, `35111004`, `35121013`, `CAvatar::MoveAction2RawAction`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::TryDoingSwallowBuff`, `Skill/1411.img/skill/14111006/{prepare,keydown}`, `Skill/2212.img/skill/22121000/{prepare,action}`, `Skill/2215.img/skill/22151001/{prepare,action}`, `Skill/2312.img/skill/23121000/{prepare,keydown,keydownend}`, `Skill/3100.img/skill/31001000/{prepare,action,keydownend}`, `Skill/3110.img/skill/31101000/{prepare,action,keydownend}`, `Skill/3110.img/skill/31101002/{prepare,action}`, `Skill/3212.img/skill/32121003/prepare/action`, `Skill/3310.img/skill/33101005/prepare/action`, `Skill/3312.img/skill/33121006/action/0`, `Skill/3500.img/skill/35001001/action/0`, `Skill/3510.img/skill/35101004/action/0`, `Skill/3512.img/skill/35121013/action/0`, `Skill/434.img/skill/4341002/{prepare,action}`, `Skill/434.img/skill/4341003/{prepare,action}`, `Skill/531.img/skill/5311002/{prepare,keydown,action}`, `Character/00002000.img/{darkTornado_pre,darkTornado,darkTornado_after,icebreathe_prepare,dragonIceBreathe,breathe_prepare,dragonBreathe,bluntSmash,cyclone_pre,cyclone,cyclone_after,noiseWave_pre,noiseWave_ing,noiseWave,dualVulcanPrep,dualVulcanLoop,dualVulcanEnd,demonTrace,finalCutPrepare,finalCut,monsterBombPrepare,monsterBombThrow,rbooster_pre,rbooster,tank_rbooster_pre,tank_rbooster_after,swallow_pre,swallow_loop,swallow,wildbeast}`) |
| Partial | Client-managed avatar effect layers | The simulator now loads persistent avatar-bound skill layer branches from WZ (`special`, `special0`, `finish`, `finish0`, ladder-aware `back` / `back_finish`, and `repeat` on `suddenDeath` skills), attaches them to the player render path as over-character / under-face layers, switches the `More Wild` family onto its ladder override, plays finish cleanup on buff expiry or cancellation, routes `doubleJump` action-family movement skills onto a client-style transient avatar-owned layer instead of the generic cast sprite, extends that same transient seam across the broader non-buff movement families the existing resolver already classifies (`Rush`, backstep, bound-jump / assaulter / flash, and flying-rush variants) so WZ-backed movement casts like `Skill/122.img/1221007/{effect,effect0}`, `Skill/433.img/4331005/{effect,effect0}`, and `Skill/3001.img/30010186/effect` suppress the detached cast sprite and keep authored planes bound to the avatar for the movement window, now also honors movement or buff shapes that only publish the secondary `effect0` branch by treating `effect` and `effect0` as equivalent client-owned sources for promotion instead of requiring the primary branch to exist, promotes fly/flying buff `effect` branches onto looping player-owned avatar layers so wing visuals stay attached to the avatar for the full buff window instead of rendering as detached cast sprites, promotes Dark Sight-family `effect` branches such as `Skill/400.img/4001003/effect` and `Skill/1400.img/14001003/effect` onto the same looping avatar-owned layer path instead of leaving them as detached cast sprites, promotes invisible swallow buff `effect` branches such as `Skill/3310.img/33101006/effect` onto a client-style under-face avatar layer that now hides while the player is on a ladder or rope, now preserves sibling `effect0` branches on transient client-owned movement layers as an extra avatar-bound plane instead of dropping them, also promotes root-`invisible` buff skills whose WZ publishes only timed `effect/effect0` planes without `affected` or `special` branches, such as `Skill/000.img/0001037/{effect,effect0}`, onto the same looping avatar-owned path instead of leaving them detached, also promotes WZ-backed damage-reflect buff visuals that publish timed `effect/effect0` without `affected` or `special` branches, such as `Skill/110.img/1101007`, `Skill/120.img/1201007`, and `Skill/3110.img/31101003`, onto looping avatar-owned layers while still suppressing those client-owned avatar layers whenever Oak Barrel-style morph buffs or full-body Mechanic ride actions own the sprite, now extends that same looping avatar-owned fallback to the broader timed-buff WZ shape where a self-buff only publishes `effect/effect0` companion planes without `affected` or `special*`, no longer mirrors a lone promoted plane onto both avatar planes when WZ only authored one side, which keeps single-plane `effect`-only or `effect0`-only visuals on their authored z side instead of double-drawing them across under-face plus overlay, and now also treats `affected` and `affected0` as the same exclusion signal for those timed-buff fallbacks so a secondary-only active aura cannot be mistaken for a missing `affected` branch and incorrectly re-promoted through the client-owned `effect/effect0` path. This pass also widens the smaller `CUser::Update`-owned affected-area seam that IDA confirmed through `CUser::UpdateAr01Effect`: while the player stands inside a qualifying friendly player-skill affected area that can apply to the local player and WZ publishes a looping `affected` or `affected0` branch, the simulator now tracks that area instance by object id, keeps both planes when both siblings exist, and promotes those authored `affected` / `affected0` animations onto the avatar-owned under-face plus overlay seam instead of leaving the local actor without the client-owned aura or silently dropping the secondary plane. That keeps the confirmed `Amplifier Robot AF-11` (`Skill/3512.img/35121010/affected`) surface, preserves dual-plane authored auras such as `Skill/000.img/0000086/{affected,affected0}`, and now also covers the broader friendly support-summon area shape the resolver can identify from WZ, including party-owned Mechanic support robots such as `Healing Robot H-LX` (`Skill/3511.img/35111011/affected`), while now also letting those party-support robot auras stay eligible from WZ `info/type = 33` plus `info/minionAbility = heal` / `amplifyDamage` even when localized description text is unavailable, alongside the existing mass-spell or support-zone affected-area families instead of hardcoding only the single `AR01` skill id. The runtime also now keeps sibling `affected0` planes alive on ordinary looping buff visuals such as `Skill/532.img/5320007/{affected,affected0}` and `Skill/572.img/5720005/{affected,affected0}` instead of discarding the secondary authored layer, and it no longer incorrectly binds Energy Charge-family `special` layers on initial buff activation: WZ marks those skills with `info/energyCharge = 1` and `info/fullChargeEffect = bodyAttack`, while the client keeps a separate `m_tEnergyCharge`-owned runtime in `CUser::Update`, so the simulator now defers those full-charge avatar layers until a dedicated owner exists, tracks active Energy Charge buffs through that separate runtime seam, accumulates charge from successful local attack hits against the WZ-backed `common/x` threshold on `Skill/511.img/5110001` and `Skill/1510.img/15100004`, and only promotes the authored `special` layer once that inferred full-charge state is reached. The same IDA re-check of `CUser::Update` also shows the client suppressing several avatar-owned layers while `CAvatar::IsOnPlayingRotateAction` is active, so the simulator now marks the currently confirmed sudden-death / `Final Cut` repeat layer path and the flying-wing fallback path as rotate-sensitive and suppresses those avatar-owned effects during the rotate-style action family the current runtime can identify safely. The runtime now consults the player's current client raw-action code when it can resolve one, uses the exact client rotate raw-action set confirmed in IDA (`101`, `82`, `114`, `149`, `151`, `193`), and narrows the action-name fallback from a broad `spin` substring to the concrete WZ-authored base-body rotate families the simulator can actually name safely (`doubleJump`, `backspin`, `rollingSpin`, `darkSpin`, `screw`, `somersault`, and explicit `finalCut`) instead of continuing to overdraw those layers through the spin window; that suppression still also consults the player runtime's forced one-time skill action, so client-owned layers hide during skill-driven rotate windows even when the visible current action has already fallen back to a benign standing or generic attack pose. The remaining gap is narrower again: broader client-only `CUser::Update` families beyond `More Wild`, swallow, double-jump, the existing movement or buff WZ shapes, and these currently promoted friendly `affected`-area ownership families still need explicit client-backed ownership rules before they can be mirrored safely; the unresolved raw-action-to-name mapping behind `CAvatar::IsOnPlayingRotateAction` still needs deeper client confirmation before widening the explicit named fallback beyond these currently identified WZ-backed rotate families; and the Energy Charge runtime could still use deeper client confirmation if later IDA work proves a different accumulation cadence or extra non-visual full-charge behavior around `bodyAttack`. | Visible parity is closer for `More Wild`, `Final Cut`-style sudden-death buffs, the broader WZ-backed movement cast surface, flying-wing buffs, Dark Sight-family stealth buffs, root-`invisible` effect-only buffs, Wild Hunter swallow buffs, the confirmed damage-reflect buff family, the secondary-only `effect0` surface, the broader timed self-buff `effect/effect0` pair surface, the Energy Charge family, and now the client-owned friendly affected-area aura surface because the promoted avatar-owned layers now follow the client's ownership rules more closely, no longer require the primary `effect` branch to exist before promotion, no longer double-render single-plane authored visuals on both avatar planes, now also refuse to treat a WZ-authored `affected0` aura as 窶從o affected branch窶・when deciding whether timed buff `effect/effect0` planes should be rebound onto the avatar, now defer Energy Charge `special` layers until a separate full-charge owner exists instead of attaching them at cast start, now drive that Energy Charge owner forward on successful local hits toward the WZ-backed charge threshold, now let party-owned support robots keep their avatar-owned aura eligibility from WZ summon metadata alone instead of depending on localized description text, and now also preserve sibling `affected0` planes on looping buff / aura visuals while the player stands inside qualifying local- or party-owned player-skill affected areas such as `35121010`, `35111011`, and the same WZ-authored mass-spell or support-zone area shape. Rotate-sensitive avatar-owned layers also now stop depending on a broad visible-action `spin` substring, inspect the same forced one-time action seam the player runtime uses for skill-driven attacks, consult the same client rotate raw-action set (`101`, `82`, `114`, `149`, `151`, `193`) that `CAvatar::IsOnPlayingRotateAction` checks, and fall back only to the concrete WZ-authored rotate families currently identified on `Character/00002000.img` (`doubleJump`, `backspin`, `rollingSpin`, `darkSpin`, `screw`, `somersault`, `finalCut`). The remaining work is narrower: broader `CUser::Update`-owned effect families that do not publish these WZ-authored `special*`, timed `effect/effect0`, `affected/affected0`, invisible/effect-only buff, movement-action, or the now-covered friendly affected-area shape still need explicit client-backed remaps before they can be mirrored safely, the exact client raw-action set behind `IsOnPlayingRotateAction` no longer needs discovery but the remaining raw-action-to-action-name mapping still needs fuller client evidence before widening the explicit named fallback beyond the currently identified rotate families, and Energy Charge may still need deeper client-backed tuning if later evidence shows that its full-charge cadence or combat-side `bodyAttack` runtime differs from the current WZ-shaped accumulation seam. | `PlayerCharacter`, `PlayerManager`, `SkillLoader`, `SkillManager`, `CharacterAssembler`, `ClientOwnedAvatarEffectParity`, `UnitTest_MapSimulator/RemoteAffectedAreaSupportResolverTests.cs` (`CUser::Update`, `CUser::UpdateMoreWildEffect`, `CUser::UpdateAr01Effect`, `CUserLocal::TryDoingSwallowBuff`, `CAvatar::IsOnPlayingRotateAction`, `Character/00002000.img/{doubleJump,backspin,rollingSpin,darkSpin,screw,somersault,finalCut}`, `Skill/3312.img/33121006/{special,special0,finish,finish0,back,back_finish}`, `Skill/434.img/4341002/repeat`, `Skill/400.img/4001003/effect`, `Skill/1400.img/14001003/effect`, `Skill/310.img/3101003/effect`, `Skill/3300.img/33001002/effect`, `Skill/122.img/1221007/{effect,effect0}`, `Skill/433.img/4331005/{effect,effect0}`, `Skill/3001.img/30010186/effect`, `Skill/3310.img/33101006/effect`, `Skill/000.img/0001037/{effect,effect0,invisible}`, `Skill/110.img/1101007/{effect,effect0}`, `Skill/120.img/1201007/{effect,effect0}`, `Skill/3110.img/31101003/{effect,effect0}`, `Skill/1411.img/14111000/effect0`, `Skill/510.img/5101007`, `Skill/511.img/5110001/{special,affected,common/x,info/energyCharge,info/fullChargeEffect}`, `Skill/1510.img/15100004/{special,affected,common/x,info/energyCharge,info/fullChargeEffect}`, `Skill/000.img/0000086/{affected,affected0}`, `Skill/532.img/5320007/{affected,affected0}`, `Skill/572.img/5720005/{affected,affected0}`, `Skill/3511.img/35111011/{affected,info/type,info/minionAbility}`, `Skill/3512.img/35121010/{affected,info/type,info/minionAbility}`, `Skill/3512.img/35121007/affected`, `Skill/3312.img/33121007/affected`, `Skill/3200.img/32001003/affected`, `Skill/3210.img/32101002/affected`, `Skill/3210.img/32101003/affected`) |
| Partial | Remote user / `CUserPool` actor parity | The simulator now has a first-class shared remote-user actor seam instead of only feature-local placeholders. `RemoteUserActorPool` owns remote AvatarLook decode to `CharacterBuild`, per-user insertion/removal, shared world draw, name lookup, chair and mount state, Battlefield team tagging, minimap helper marker registration, prepared-skill world overlays, a decoded movement-snapshot path that samples packet-style `CMovePath` data into per-actor position, facing, foothold, and action updates, packet-owned one-shot social item effects loaded from `Effect/ItemEff.img` through the existing character loader seam, the `CUserRemote::OnAvatarModified`-shaped refresh path for packet `223`, and now also the adjacent temporary-stat packet family through shared `225/226` decode plus per-actor temp-stat snapshot storage. `MapSimulator` continues to expose a `CUserPool::OnPacket`-shaped `/remoteuser packet` / `/remoteuser packetraw` dispatcher that routes supported enter/leave plus common-vs-remote packet families (`179/180/181/182/183/184/210/211/212/213`, the simulator-owned melee seam `214`, the simulator-owned item-effect seam `215`, avatar-modified `223`, and temporary-stat set/reset `225/226`) through one owner instead of mutating remotes only through ad hoc feature commands, and the general enter-field owner still mirrors the existing Ariant handoff by applying packet-authored initial portable-chair state immediately after AvatarLook spawn rather than dropping the remote into a stand pose until a later chair packet arrives. The shared seam still covers the client-owned social/state slice exposed by `CUserRemote::OnAvatarModified`: packet `223` can reload remote `AvatarLook` through the shared loader seam, apply the packet-owned remote speed scalar onto the actor build, clear or refresh couple, friendship, and marriage relationship overlays through the existing `Effect/ItemEff.img` owner, and retain the packet-owned carry-item-effect byte plus completed-set-item id in shared remote state. This pass closes both of those avatar-modified rendering gaps with WZ plus client evidence instead of leaving one inspection-only: completed set ids now resolve through `Etc/SetItemInfo.img` `effectLink` ownership or direct `Effect/SetItemInfoEff.img/<setId>` fallback, load as a shared avatar-bound animation layer, and draw from the same remote actor seam instead of remaining dark when packet `223` reports a completed set; and the carry-item-effect byte now no longer stays as dead state either, because the shared loader resolves the same `Character/01112916.img -> Effect/CharacterEff.img/1112916` carry-effect family that `CUser::SetCarryItemEffect` owns, reuses the adjacent `1112924` and `1112926` authored visuals, and renders a client-shaped count split (`tens + ones`) as a packet-owned back/front orbit around the remote avatar rather than dropping the effect entirely. The already-landed social effect work remains in place: WZ-backed couple, friendship, and wedding ring effects in `Effect/ItemEff.img` can be loaded as non-looping remote item effects, the pool preserves packet-authored partner targeting when it exists, resolves same-item relationship pairing when the client-style avatar-modified records only expose serial-backed item ownership, renders the owner-side branch on the remote actor, and resolves the larger shared branch once per pair at the midpoint between linked actors or between the remote actor and the local player when the packet names the local character. In the same loader seam, `Effect/ItemEff.img` entries whose primary animation is authored as flat root-level numbered `Canvas` or `UOL` frames, such as the New Year-style `2270002` and `2270003` branches that point back into `2270000`, now resolve their main authored branch first, only drop to the item-local `fail` branch when the primary branch is not actually playable, and mirror the broader one-shot `CAnimationDisplayer::Effect_ByItem` owner instead of assuming ring-only `0`/`1` branches. Wedding's feature-local groom, bride, and audience actors remain migrated onto the shared pool, and this pass now also forwards packet-owned wedding participant temp-stat and avatar-modified state back into that shared owner when the external remote renderer is active instead of keeping those updates trapped only inside the wedding-local copy. Messenger room occupants also continue to collapse onto that same owner with packet-authored `AvatarLook` rehydration for hidden members when present. This now matches more of the client evidence that `CUserPool::OnPacket` owns user enter/leave and common-vs-remote dispatch, `CUserPool::Update` advances remote actors plus shared social state, `CUserRemote::Update` owns remote movement, action, keydown-prepare, portable-chair presentation, and adjacent one-shot effect surfaces, `CUserRemote::OnAvatarModified` owns avatar refresh plus relationship-record updates, `CUser::SetCarryItemEffect` owns the packet-authored carry-count orbit, and `CAnimationDisplayer::Effect_ByItem` at `0x44f330` is a dedicated one-shot item-effect owner rather than a feature-local overlay. | The remaining gap is narrower again: any other feature-local remote shells still need to collapse onto the shared pool; the packet router still only covers the implemented enter/leave, move/state, helper/team, chair/mount, prepared-skill, melee, item-effect, avatar-modified, and temp-stat families rather than the broader client packet surface; Messenger member parity still depends on the simulator's optional member-info `AvatarLook` trailer instead of a fuller official messenger packet decode and broader room-member lifecycle; and `CUserRemote::Update` / `CUserPool::Update` parity is still incomplete for the rest of the avatar-modified and one-time-effect surface, especially the exact client carry-item orbit choreography and asset choice beyond the now-visible shared `CharacterEff` reconstruction, any client-only relationship pairing rules that depend on serial tables instead of the simulator's same-item reconciliation, broader packet-authored user effects beyond `ItemEff`, remote afterimages beyond the existing melee seam, and higher-fidelity movement smoothing beyond the current sampled move-path path. | shared field actor/runtime layer (`CUserPool::OnPacket`, `CUserPool::Update`, `CUserRemote::Update`, `CUserRemote::OnAvatarModified`, `CUser::SetCarryItemEffect`, `CAnimationDisplayer::Effect_ByItem`, `CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUser::Update`, `CMovePath::{Decode,OnMovePacket}`, `Character/01112916.img/info/effect/path`, `Character/01112924.img/info/effect/path`, `Character/01112926.img/info/effect/path`, `Effect/CharacterEff.img/1112916`, `Effect/CharacterEff.img/1112924`, `Effect/CharacterEff.img/1112926`, `Effect/ItemEff.img/1112001`, `Effect/ItemEff.img/1112800`, `Effect/ItemEff.img/1112803`, `Effect/ItemEff.img/2270000`, `Effect/ItemEff.img/2270002`, `Effect/ItemEff.img/2270003`, `Etc/SetItemInfo.img`, `Effect/SetItemInfoEff.img`) |


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Remote actor foundation pass:
   Add a real `CUserPool`-style remote-user actor layer before continuing to extend wedding, Ariant, Battlefield, minimap-helper, and follow-request overlays independently.
3. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
4. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
5. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
6. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".


