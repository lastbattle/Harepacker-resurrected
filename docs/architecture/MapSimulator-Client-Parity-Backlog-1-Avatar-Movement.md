# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CAnimationDisplayer::Effect_ByItem` at `0x44f330`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Avatar and player parity

- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::SetActivePet` at `0x8e40e0`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CAnimationDisplayer::Effect_ByItem` at `0x44f330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::Update` at `0x937330`

Notes:
`CUser::LoadLayer` and `CUser::PrepareActionLayer` are the clearest resolved avatar-composition seams for layer loading and action-layer setup. `CUser::Update` and `CUser::UpdateMoreWildEffect` also show that the client maintains several additional avatar/effect layers outside the base assembler path, while `CUserLocal::Update` contains the local-player emotion reset path and action/state transitions that matter for expression and rare-action parity. A later IDA pass also confirms that remote avatar ownership is a first-class client surface rather than a loose extension of local-player logic: `CUserPool::OnPacket` dispatches enter, leave, common, remote, and local user packet families, `CUserPool::Update` advances local plus remote users and shared social-effect state, `CUserRemote::Update` owns remote move-action, effect, portable-chair, and prepared-skill presentation, and `CAnimationDisplayer::Effect_ByItem` confirms that one-shot item effects are managed by a dedicated client animation-owner seam rather than only by feature-local overlay code.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Avatar and player parity

These are the biggest remaining gaps for visible client parity.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Default avatar selection | The simulator keeps the deterministic, WZ-verified SuperGM/Wizet fallback selection of `1002140` (Wizet Invincible Hat), `1042003` (Wizet Plain Suit), `1062007` (Wizet Plain Suit Pants), `1072005` (Leather Sandals), `1092008` (Pan Lid), and `1322013` (Wizet Secret Agent Suitcase) with matching face, hair, level, and SuperGM job metadata, and the no-packet avatar seam is now shared all the way through preset-backed defaults as well. WZ was rechecked again for this row in `Etc/MakeCharInfo.img/000/{male,female}` and still pins the non-sale starter request layout to the client-owned `0..7` face/hair/skin/hairColor/coat/pants/shoes/weapon families (`male/0 = {20000,20001,20002}`, `male/1 = {30030,30020,30000}`, `male/2 = {0,7,3,2}`, `male/3 = {0,1,2,3}`, `male/4 = {1040002,1040006,1040010}`, `male/5 = {1060002,1060006}`, `male/6 = {1072001,1072005,1072037,1072038}`, `male/7 = {1302000,1322005,1312004}`, `female/0 = {21000,21001,21002}`, `female/1 = {31000,31040,31050}`, `female/2 = {0,7,3,2}`, `female/3 = {0,1,2,3}`, `female/4 = {1041002,1041006,1041010,1041011}`, `female/5 = {1061002,1061008}`, `female/6 = {1072001,1072005,1072037,1072038}`, `female/7 = {1302000,1322005,1312004}`). `CharacterPreset` now owns a synthesized `LoginAvatarLook`, build-backed preset capture preserves hidden equip lists, pet ids, and packet-owned `weaponStickerID` visuals through that owner, `CharacterPreset.ToBuild` rebuilds through `CharacterLoader.LoadFromAvatarLook` whenever the synthesized owner is present, and `CharacterConfigManager.CreateDefaultMalePreset` / `CreateDefaultFemalePreset` now create the default preset from the same synthesized `LoginAvatarLook` source used by roster previews and packet emission instead of re-materializing only scalar selection metadata. Legacy preset files without a serialized `AvatarLook` still load through the existing scalar fallback for compatibility, but the default-avatar parity gap for new simulator-owned presets is closed. | Default avatars, synthesized login previews, packet-authored avatars, and preset-driven defaults now converge on the same render-safe avatar owner, so local default tooling no longer risks silently dropping hidden equips, pets, or weapon stickers just because no packet bytes existed. Focus for later avatar work can stay on other backlog rows instead of this ownership split. | `CharacterLoader.LoadDefaultMale`, `CharacterLoader.LoadDefaultFemale`, `CharacterLoader.LoadFromAvatarLook`, `LoginAvatarLookCodec`, `LoginCharacterRosterEntry.CreateRuntimeBuild`, `MapSimulator.ResolveLoginRosterAvatarLook`, `MapSimulator.CreateLoginPacketCharacterEntry`, `MapSimulator.CreateLoginCharacterAccountEntryState`, `CharacterPreset.FromBuild`, `CharacterPreset.ToBuild`, `CharacterConfigManager.CreateDefaultMalePreset`, `CharacterConfigManager.CreateDefaultFemalePreset`, `UnitTest_MapSimulator/DefaultAvatarSelectionPresetParityTests.cs` (`CUser::LoadLayer`, `CUIAvatar::CUIAvatar`, `GW_CharacterStat::Decode`, `AvatarLook::Decode`, `Etc/MakeCharInfo.img/000/{male,female}`) |
| Implemented | Action coverage | Character-part loading now preserves the existing core action order and appends additional WZ action nodes discovered on each part instead of stopping at the old fixed lists | Uncommon body/equipment poses can now load when the source IMG exposes them, which closes the loader-side empty-state gap for rare, scripted, and death-adjacent actions | `CharacterLoader.StandardActions`, `CharacterLoader.AttackActions` (`CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUserLocal::SetMoveAction`) |
| Implemented | Death / rare action frames | Runtime action selection now carries raw one-shot action names through to `CharacterAssembler` instead of forcing uncommon actions back through the limited enum-only render path | Loaded death and other rare WZ actions can now survive from selection to final frame assembly instead of degrading immediately to generic fallbacks | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler` |
| Implemented | Facial expression behavior | The local player runtime now owns blink cadence and short-lived hit expressions, and updates the assembler expression state when those transitions occur | The avatar now reacts over time instead of staying pinned to the static default face | `CharacterAssembler.GetFaceFrame`, `CharacterLoader.LoadFaceExpressions`, `PlayerCharacter` (`CUserLocal::Update` emotion reset path) |
| Implemented | Equip anchor fallback rules | Equipment alignment now resolves through explicit shared map-point precedence instead of using literal pixel nudges when preferred anchors are missing | Parts stay render-safe while remaining tied to WZ anchor data rather than ad hoc offsets | `CharacterAssembler.CalculateEquipOffset`, `CharacterAssembler.TryCalculateHeadEquipOffset` |
| Implemented | Client-verified fallback order | Action lookup now prefers exact action names, then raw-action family reductions, then swim/fly aliasing, before the final `stand1` escape hatch | Character assembly now follows a documented action-family precedence order instead of jumping straight from miss to generic standing frames | `CharacterAssembler`, `CharacterPart.FindAnimation` (`CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`) |
| Partial | Morph avatar pipeline parity | MapSimulator still resolves concrete morph template ids from `common/morph` and `PVPcommon/morph` into `Morph/*.img`, loads those images as a dedicated avatar render owner, resolves UOL-linked morph frames, preserves per-frame anchors (for example `head`), backfills through same-family base IMG fallback (`id -> id0 -> id00 -> id000`), paired `100x <-> 110x` family fallback, and WZ-backed `info/link` inheritance, and routes hostile polymorph mob skills plus current flag-only `info/morph = 1` outliers through that same owner seam. Runtime transform coverage remains on morph assets for ladder, rope, swim, fly, superman `ladder2`/`rope2`/`fly2`, airborne `fly2Move`/`fly2Skill`, authored combat, hit, death, sit, and jump-special branches. This run narrows resolver parity further with WZ-first plus client-backed evidence: raw ranged requests containing `shoot`, `shot`, `spear`, `rain`, `break`, or `eruption` keep archer-family ordering first; exact published actions still stay first when explicitly requested (for example `stormbreak`, `windspear`, `shoot1`, `swingT1`, `archerDoubleJump`); mixed-family ranged requests still route to published generic `shoot*` before pirate fallback aliases when authored archer aliases are absent; generic melee requests such as `swingT2` still fall through to published generic melee (`swingT1`/`swingT3`) before pirate carry-over aliases; and jump-special promotion remains scoped to explicit `doubleJump` requests instead of generic `jump`. Legacy morph requests remain reduced through this same seam (`alert6`/`alert7` nearest indexed alert fallback before base `alert`, `shoot6` archer-first plus generic `shoot*`, `arrowEruption` and `rain` prioritizing published `arrowRain`, and `savage` reducing through published generic swing branches). WZ was rechecked for `Morph/1000.img`, `1001.img`, `1003.img`, `1100.img`, `1101.img`, `1103.img`, and `2003.img`: `1000`, `1001`, `1003`, `1100`, `1101`, and `1103` still publish `info/superman = 1`; `1003` and `1103` still expose authored archer aliases (`windshot`, `windspear`, `stormbreak`, `arrowRain`, `archerDoubleJump`), generic melee (`swingT1`, `swingT3`), and generic ranged backstop (`shoot1`, `shootF`); and no direct `arrowEruption`/`shoot6`/`alert6`/`alert7` branches are published there, so resolver reduction remains required. Client evidence was rechecked and extended in IDA: `CActionMan::LoadMorphAction` still routes through dedicated `s_sMorphAction` entries, `CActionMan::Init` still uses `get_action_name_from_code`, and this run decoded the concrete morph table names (`walk`, `stand`, `jump`, `alert*`, `proneStab`, `swingO*`, `swingT*`, `stabO*`, `stabTF`, `shoot1`, `shootF`, `windshot`, `windspear`, `stormbreak`, `timeleap`, `shockwave`, `demolition`, `snatch`, `fly2*`, and related published entries). Based on that client table plus WZ evidence that `Morph/1003.img` and `Morph/1103.img` omit stab-family branches, the resolver now also keeps client stab-family raw requests (`stabO1`, `stabO2`, `stabTF`) on published generic `swing*` surfaces before pirate fallback aliases. Focused regression coverage in `UnitTest_MapSimulator/MorphClientActionResolverTests.cs` includes mixed-family melee and ranged ordering, exact-branch preference, `doubleJump` promotion and generic `jump` non-promotion, `shotC1`, `alert6`, `alert7`, `shoot6`, `arrowEruption`, `rain`, `savage`, and now `stabO1`/`stabO2`/`stabTF` fallback ordering assertions. Building `UnitTest_MapSimulator` currently fails before executing updated tests due unrelated pre-existing `HaCreator` compile blockers in `MapSimulator/Managers/LocalUtilityPacketInboxManager.cs` (`PacketOwnedLocalRewardResultRuntime` unresolved). Remaining gap: the core `s_sMorphAction` table is now recovered for this seam, but full client-only morph alias parity still depends on confirming whether additional remaps exist outside that published table path (for example wider `MoveAction2RawAction` interactions) before adding broader non-WZ aliases. | Morph-heavy jobs, hostile polymorph skills, and current flag-only morph outliers remain on the dedicated morph-owner seam rather than generic body/equip assembly. With this pass, mixed-family builds keep WZ-authored generic melee and ranged surfaces ahead of unrelated fallback-family aliases, preserve exact published branch picks, keep `doubleJump` promotion explicit, preserve fallback-inherited `superman` handling for `ladder2`/`rope2`/`fly2`/`fly2Move`/`fly2Skill`, keep legacy `alert6`/`alert7`/`shoot6`/`arrowEruption`/`rain`/`savage` requests on client-shaped published surfaces, and now also keep client stab-family morph raw requests on published `swing*` branches when stab branches are not authored. The row is now narrowed to remaps not yet evidenced outside the recovered `LoadMorphAction` table, not WZ-backed owner assembly or currently evidenced resolver ordering. | avatar transform/runtime assembly (`CActionMan::GetMorphImgEntry`, `CAvatar::MoveAction2RawAction`, `CActionMan::LoadMorphAction`, `CActionMan::Init`, `get_action_name_from_code`, `CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUserLocal::SetMoveAction`, `HaCreator/MapSimulator/Character/MorphClientActionResolver.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `UnitTest_MapSimulator/MorphClientActionResolverTests.cs`, `Morph/1000.img`, `Morph/1001.img`, `Morph/1003.img`, `Morph/1100.img`, `Morph/1101.img`, `Morph/1103.img`, `Morph/2001.img`, `Morph/2002.img`, `Morph/2003.img`) |
| Partial | Mount / vehicle / mechanic mode rendering | `TamingMob` assembly still covers the implemented owner paths for this row: normal mount ride frames and `sit` fallback, `eventTamingMob` part equip, Mechanic owner routing to `Character/TamingMob/01932016`, Battleship owner routing to `Character/TamingMob/01932000`, direct `ride2`/`getoff2` transitions, fixed Mechanic state skills, distinct Mechanic/Battleship mounted actions, and known-owner authored action preservation (`swingT1`, `alert2`, `alert3`, `ride2`, `getoff2`, `tank_*`, `msummon*`, `tank_laser`, `cannon`, `torpedo`, `fireburner`, `coolingeffect`). This pass tightens the shared mounted-state resolver seam used by follow/packet/escort consumers: `FollowCharacterEligibilityResolver.ResolveMountedVehicleId` no longer infers a mount from ownerless shared move/support actions (`walk1`, `stand1`, `jump`, `alert2`), now requires an active or equipped mounted render owner for those borrowed surfaces, and only infers ownerless mount ids for distinct vehicle-only actions (`cannon`/`torpedo`/`fireburner`/`coolingeffect`, distinct mechanic `tank_*` actions, or explicit `mechanicMode`). WZ was rechecked in this workspace for the same row sources: `Character/00002000.img` publishes the shared body action roots, while `Character/TamingMob/01932016` and `Character/TamingMob/01932000` publish the ride-owner action families. IDA evidence still matches this seam: `?IsAbleTamingMobAction@@YAHJJ@Z` gates extra action coverage by vehicle id, and `?LoadTamingMobAction@CActionMan@@QAEXJJQAJHAAV?$ZArray@V?$ZRef@UTAMINGMOBACTIONFRAMEENTRY@CActionMan@@@@@@@Z` is the authored taming-mob action loader once ownership is established. Source-side regression coverage for this pass is in `UnitTest_MapSimulator/FollowCharacterEligibilityResolverTests.cs` (ownerless shared-action fallback, ownerless distinct-action inference, active-owner preservation, equipped-owner preservation). Direct execution is still blocked by unrelated workspace build failures before tests run: `dotnet build UnitTest_MapSimulator/UnitTest_MapSimulator.csproj --no-restore` currently stops first with `CS2001` on missing generated files `HaCreator/obj/Debug/net10.0-windows/GUI/Quest/QuestEditor.g.cs` and `HaCreator/obj/Debug/net10.0-windows/MapEditor/MultiBoard.g.cs`. Remaining gap: broader truly ownerless client-only vehicle update/move-state sources that still provide no active buff, handoff owner, equipped owner, explicit mechanic mode, or mounted render owner signal. | Mounted-state consumers now avoid false mounted detection from ownerless shared actions while preserving borrowed ride-only actions when a real owner exists. Remaining row work is limited to broader client-managed ownerless state sources that still need explicit client evidence. | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler`, `SkillManager`, `ClientOwnedVehicleSkillClassifier`, `FollowCharacterEligibilityResolver`, `EscortFollowController`, `MapSimulator.PacketOwnedUtilityParity`, `UnitTest_MapSimulator/FollowCharacterEligibilityResolverTests.cs` (`?LoadTamingMobAction@CActionMan@@QAEXJJQAJHAAV?$ZArray@V?$ZRef@UTAMINGMOBACTIONFRAMEENTRY@CActionMan@@@@@@@Z`, `?IsAbleTamingMobAction@@YAHJJ@Z`, `Character/00002000.img`, `Character/TamingMob/01932016`, `Character/TamingMob/01932000`) |
| Partial | Skill-specific avatar transforms | Mechanic-mode skills still activate the dedicated avatar action families published by WZ `action/0` nodes, and the prepared-skill families on this row remain routed through the existing transform seam. The remaining WZ-backed cast-start remap holes in that seam stay closed: flamethrower still holds on `flamethrower` / `flamethrower2` until release triggers `flamethrower_after` / `flamethrower_after2`; Rocket Booster still keeps its WZ-backed ground startup on `rbooster_pre` while airborne motion promotes onto `rbooster` and clears through `rbooster_after`, while the tank variant preserves `tank_rbooster_pre` / `tank_rbooster_after`; Wild Hunter `Swallow` still uses the client-confirmed body-only `swallow_pre` branch for its prepare windup before holding on `swallow_loop` and clearing into `swallow`; Night Walker `Poison Bomb` still normalizes WZ-owned `dash` onto `darkTornado_pre` -> `darkTornado` -> `darkTornado_after`; and the shared prepare resolver still explicitly remaps WZ-backed `cyclone_pre` onto body `cyclone`, `noiseWave_pre` onto body `noiseWave_ing`, and `dualVulcanPrep` onto body `dualVulcanLoop` so those families no longer briefly render the authored prepare pose when the row expects the dedicated hold branch. `More Wild` still stays on `wildbeast`; Evan breath charges still hold `icebreathe_prepare` / `breathe_prepare` before resolving onto `dragonIceBreathe` / `dragonBreathe`; Dual Blade `Final Cut` / `Monster Bomb` still hold on `finalCutPrepare` / `monsterBombPrepare` before resolving onto `finalCut` / `monsterBombThrow`; and the generic prepared-skill hold plus keydown-release paths still fall back from missing avatar-only `prepare/action` or `keydownend/action` names onto renderable release-family branches such as Demon `bluntSmash` instead of dropping to a stand pose. Focused source-side regression coverage is now present under `UnitTest_MapSimulator/PreparedSkillAvatarTransformParityTests.cs`, directly guarding the confirmed `darkTornado_pre`, `swallow_pre`, `cyclone`, `noiseWave_ing`, and `dualVulcanLoop` prepare remaps, the direct built-in transform remaps for WZ-owned `cyclone_pre`, `noiseWave_pre` / `noiseWave_ing`, and `dualVulcanPrep` / `dualVulcanLoop`, the Demon charge-release fallback onto renderable `bluntSmash` when WZ requests either `bluntSmashEnd` or `soulEater_end`, the flamethrower prepare/hold/exit families, the Mechanic state-family startup and `_after` exit resolver for `tank_pre`, `siege_pre`, and `tank_siegepre`, the tank-vs-normal Rocket Booster startup and exit resolver, and the remaining prepared families on this row that already route through `PlayerCharacter.TryCreateBuiltInSkillAvatarTransform`. WZ was rechecked again for this row against the current extracted data source in this workspace (`Skill/1411.img/skill/14111006/prepare/action = dash`, `Skill/3212.img/skill/32121003/action/0 = cyclone_pre`, `Skill/3310.img/skill/33101005/prepare/action = swallow_loop`, `Skill/531.img/skill/5311002/{prepare/action = noiseWave_pre, keydown/action = noiseWave_ing, action/0 = noiseWave}`, `Skill/2312.img/skill/23121000/{prepare/action = dualVulcanPrep, keydown/action = dualVulcanLoop, keydownend/action = dualVulcanEnd}`, `Skill/3100.img/skill/31001000/keydownend/action = bluntSmashEnd`, `Skill/3110.img/skill/31101000/action/0 = bluntSmash`, `Skill/3110.img/skill/31101000/keydownend/action = soulEater_end`, `Skill/3500.img/skill/35001001/{prepare/action = flamethrower_pre, keydown/action = flamethrower, keydownend/action = flamethrower_after, action/0 = flamethrower}`, `Skill/3510.img/skill/35101009/{prepare/action = flamethrower_pre2, keydown/action = flamethrower2, keydownend/action = flamethrower_after2}`, `Skill/3510.img/skill/35101004/action/0 = rbooster_pre`, `Skill/3511.img/skill/35111004/action/0 = siege_pre`, `Skill/3512.img/skill/35121005/action/0 = tank_pre`, `Skill/3512.img/skill/35121013/action/0 = tank_siegepre`). `Character/00002000.img` in the same data set still exposes `darkTornado_pre`, `swallow_pre`, `cyclone_pre`, `cyclone`, `cyclone_after`, `noiseWave_pre`, `noiseWave_ing`, `noiseWave`, `dualVulcanPrep`, `dualVulcanLoop`, `dualVulcanEnd`, `bluntSmash`, `rbooster_pre`, `rbooster`, `rbooster_after`, `tank_pre`, `tank_stand`, `tank_walk`, `tank_prone`, `tank_after`, `siege_pre`, `siege_stand`, `siege_after`, `tank_siegepre`, `tank_siegestand`, `tank_siegeattack`, `tank_siegeafter`, `tank_rbooster_pre`, and `tank_rbooster_after`, but still does not publish body-backed `bluntSmashEnd`, `soulEater_end`, `soulEater`, or dedicated `tank_jump` / `tank_ladder` / `tank_rope` / `tank_fly` / `tank_swim` / `tank_hit`, `siege_*`, or `tank_siege*` movement-variant branches in the current extracted data. `Swallow` still remains the only currently confirmed client-side prepare override in this area from the rechecked `CUserLocal::DoActiveSkill_Prepare` seam, where skill `33101005` still hardcodes prepare raw action `251`; no additional client-backed remaps surfaced in this pass beyond the WZ-safe families already implemented. The remaining gap is therefore limited to candidate client-only prepared or transform families whose final avatar remaps or exit branches are still neither named in WZ nor otherwise confirmed by the inspected client path, so they remain intentionally unmapped until more client evidence is found. Direct execution of this regression file is still blocked by unrelated pre-existing compile failures elsewhere in the workspace: the current `dotnet test UnitTest_MapSimulator/UnitTest_MapSimulator.csproj --no-restore --filter PreparedSkillAvatarTransformParityTests` run now stops in `HaCreator` first on unrelated missing-type and interface/accessibility errors in `MapSimulator/Interaction/SocialRoomModels.cs(468)`, `MapSimulator/UI/Windows/LoginCreateCharacterWindow.cs(17)`, `MapSimulator/UI/Windows/LoginTitleWindow.cs(29)`, `MapSimulator/UI/Windows/LoginUtilityDialogWindow.cs(14,131)`, and `MapSimulator/UI/Windows/MapTransferUI.cs(12)` before the regression file can execute; a narrower `dotnet build UnitTest_MapSimulator/UnitTest_MapSimulator.csproj --no-restore /p:BuildProjectReferences=false` check also remains blocked by unrelated pre-existing test-project compile failures in `MapSimulatorChatWhisperParityTests.cs`, `PacketQuestResultCloseBehaviorTests.cs`, and `WeddingFieldCeremonyParityTests.cs`. | Mechanic avatar state skills, flamethrower, Rocket Booster, Cyclone, `Monkey Wave`, Wild Hunter `Swallow`, Wild Hunter `More Wild`, Evan dragon-breath charges, Dual Blade `Final Cut` / `Monster Bomb`, Battle Mage `Dual Vulcan`, Night Walker `Poison Bomb`, and the confirmed Demon prepared-skill charge families now keep the intended body-backed hold or transform window from cast start through release more consistently, and the row now has source-side regression coverage for both the shared prepare-candidate seam and the direct built-in transform seam instead of only backlog prose describing those WZ-backed remaps. This pass also keeps the movement-family uncertainty narrowly documented on the row: the transform seam still prefers transform-specific jump / ladder / rope / fly / swim / hit aliases when those actions exist, but the current extracted `Character/00002000.img` data does not actually author those dedicated `tank_*`, `siege_*`, or `tank_siege*` movement variants beyond the state, attack, and `_after` families already wired here. The remaining parity gap is therefore limited to still-unconfirmed client-only remap or exit families whose final avatar action names are neither published in WZ nor yet recovered from the inspected client path. Verification remains blocked only by unrelated pre-existing compile failures elsewhere in the workspace. | `SkillManager`, `PlayerCharacter`, `CharacterAssembler`, `UnitTest_MapSimulator/PreparedSkillAvatarTransformParityTests.cs` (`14111006`, `22121000`, `22151001`, `23121000`, `31001000`, `31101000`, `31101002`, `32121003`, `33101005`, `33121006`, `4341002`, `4341003`, `5311002`, `35001001`, `35101009`, `35101004`, `35111004`, `35121005`, `35121013`, `CAvatar::MoveAction2RawAction`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::TryDoingSwallowBuff`, `Skill/1411.img/skill/14111006/{prepare,keydown}`, `Skill/2212.img/skill/22121000/{prepare,action}`, `Skill/2215.img/skill/22151001/{prepare,action}`, `Skill/2312.img/skill/23121000/{prepare,keydown,keydownend}`, `Skill/3100.img/skill/31001000/{prepare,action,keydownend}`, `Skill/3110.img/skill/31101000/{prepare,action,keydownend}`, `Skill/3110.img/skill/31101002/{prepare,action}`, `Skill/3212.img/skill/32121003/action/0`, `Skill/3310.img/skill/33101005/prepare/action`, `Skill/3312.img/skill/33121006/action/0`, `Skill/3500.img/skill/35001001/{prepare,keydown,keydownend,action}`, `Skill/3510.img/skill/35101009/{prepare,keydown,keydownend}`, `Skill/3510.img/skill/35101004/action/0`, `Skill/3511.img/skill/35111004/action/0`, `Skill/3512.img/skill/35121005/action/0`, `Skill/3512.img/skill/35121013/action/0`, `Skill/434.img/skill/4341002/{prepare,action}`, `Skill/434.img/skill/4341003/{prepare,action}`, `Skill/531.img/skill/5311002/{prepare,keydown,action}`, `Character/00002000.img/{darkTornado_pre,darkTornado,darkTornado_after,icebreathe_prepare,dragonIceBreathe,breathe_prepare,dragonDragonBreathe,bluntSmash,cyclone_pre,cyclone,cyclone_after,noiseWave_pre,noiseWave_ing,noiseWave,dualVulcanPrep,dualVulcanLoop,dualVulcanEnd,demonTrace,finalCutPrepare,finalCut,monsterBombPrepare,monsterBombThrow,rbooster_pre,rbooster,rbooster_after,tank_pre,tank_stand,tank_walk,tank_prone,tank_after,siege_pre,siege_stand,siege_after,tank_siegepre,tank_siegestand,tank_siegeattack,tank_siegeafter,tank_rbooster_pre,tank_rbooster_after}`) |
| Partial | Client-managed avatar effect layers | The simulator now loads persistent avatar-bound skill layer branches from WZ (`special`, `special0`, `finish`, `finish0`, ladder-aware `back` / `back_finish`, and `repeat` on `suddenDeath` skills), attaches them to the player render path as over-character / under-face layers, switches the `More Wild` family onto its ladder override, plays finish cleanup on buff expiry or cancellation, routes `doubleJump` action-family movement skills onto a client-style transient avatar-owned layer instead of the generic cast sprite, extends that same transient seam across the broader non-buff movement families the existing resolver already classifies (`Rush`, backstep, bound-jump / assaulter / flash, and flying-rush variants) so WZ-backed movement casts like `Skill/122.img/1221007/{effect,effect0}`, `Skill/433.img/4331005/{effect,effect0}`, and `Skill/3001.img/30010186/effect` suppress the detached cast sprite and keep authored planes bound to the avatar for the movement window, now also honors movement or buff shapes that only publish the secondary `effect0` branch by treating `effect` and `effect0` as equivalent client-owned sources for promotion instead of requiring the primary branch to exist, promotes fly/flying buff `effect` branches onto looping player-owned avatar layers so wing visuals stay attached to the avatar for the full buff window instead of rendering as detached cast sprites, promotes Dark Sight-family `effect` branches such as `Skill/400.img/4001003/effect` and `Skill/1400.img/14001003/effect` onto the same looping avatar-owned layer path instead of leaving them as detached cast sprites, promotes invisible swallow buff `effect` branches such as `Skill/3310.img/33101006/effect` onto a client-style under-face avatar layer that now hides while the player is on a ladder or rope, now preserves sibling `effect0` branches on transient client-owned movement layers as an extra avatar-bound plane instead of dropping them, also promotes root-`invisible` buff skills whose WZ publishes only timed `effect/effect0` planes without `affected` or `special` branches, such as `Skill/000.img/0001037/{effect,effect0}`, onto the same looping avatar-owned path instead of leaving them detached, also promotes WZ-backed damage-reflect buff visuals that publish timed `effect/effect0` without `affected` or `special` branches, such as `Skill/110.img/1101007`, `Skill/120.img/1201007`, and `Skill/3110.img/31101003`, onto looping avatar-owned layers while still suppressing those client-owned avatar layers whenever Oak Barrel-style morph buffs or full-body Mechanic ride actions own the sprite, now extends that same looping avatar-owned fallback to the broader timed-buff WZ shape where a self-buff only publishes `effect/effect0` companion planes without `affected` or `special*`, no longer mirrors a lone promoted plane onto both avatar planes when WZ only authored one side, which keeps single-plane `effect`-only or `effect0`-only visuals on their authored z side instead of double-drawing them across under-face plus overlay, and now also treats `affected` and `affected0` as the same exclusion signal for those timed-buff fallbacks so a secondary-only active aura cannot be mistaken for a missing `affected` branch and incorrectly re-promoted through the client-owned `effect/effect0` path. This pass also widens the smaller `CUser::Update`-owned affected-area seam that IDA confirmed through `CUser::UpdateAr01Effect`: while the player stands inside a qualifying friendly player-skill affected area that can apply to the local player and WZ publishes a looping `affected` or `affected0` branch, the simulator now tracks that area instance by object id, keeps both planes when both siblings exist, and promotes those authored `affected` / `affected0` animations onto the avatar-owned under-face plus overlay seam instead of leaving the local actor without the client-owned aura or silently dropping the secondary plane. That keeps the confirmed `Amplifier Robot AF-11` (`Skill/3512.img/35121010/affected`) surface, preserves dual-plane authored auras such as `Skill/000.img/0000086/{affected,affected0}`, and now also covers the broader friendly support-summon area shape the resolver can identify from WZ, including party-owned Mechanic support robots such as `Healing Robot H-LX` (`Skill/3511.img/35111011/affected`), while now also letting those party-support robot auras stay eligible from WZ `info/type = 33` plus `info/minionAbility = heal` / `amplifyDamage` even when localized description text is unavailable, alongside the existing mass-spell or support-zone affected-area families instead of hardcoding only the single `AR01` skill id. The synthetic active-buff owner used for those projected remote support areas now also converts whichever source or linked support skill actually publishes the authored `affected` / `affected0` branch into looping avatar-owned overlay / under-face planes and only applies that owner when the projected area is first created or upgraded from a non-avatar-owned state, so those remote support auras no longer restart every refresh tick and no longer stay dark when the summon metadata resolves but the aura branch lives on a linked support skill instead of the top-level area owner. The local-player affected-area avatar-effect path now mirrors that same linked-support-skill ownership rule instead of checking only the top-level area owner, so wrapper or summon skills whose WZ `info/affectedSkill` points at the authored aura source can still promote the real `affected` / `affected0` branch and still inherit linked party-facing support metadata when the wrapper skill itself only publishes the outer area or summon shape. WZ was rechecked again for that narrower seam in the current extracted data set: `Skill/3211.img/32110007/info/affectedSkill = 32001003`, while the authored aura still lives on `Skill/3200.img/32001003/affected`, so the simulator now keeps that avatar-owned aura path eligible instead of leaving the local actor dark just because the visible area owner is only the wrapper skill. The runtime also now keeps sibling `affected0` planes alive on ordinary looping buff visuals such as `Skill/532.img/5320007/{affected,affected0}` and `Skill/572.img/5720005/{affected,affected0}` instead of discarding the secondary authored layer, and it no longer incorrectly binds Energy Charge-family `special` layers on initial buff activation: WZ marks those skills with `info/energyCharge = 1` and `info/fullChargeEffect = bodyAttack`, while the client keeps a separate `m_tEnergyCharge`-owned runtime in `CUser::Update`, so the simulator now defers those full-charge avatar layers until a dedicated owner exists, tracks active Energy Charge buffs through that separate runtime seam, accumulates charge from successful local attack hits against the WZ-backed `common/x` threshold on `Skill/511.img/5110001` and `Skill/1510.img/15100004`, and only promotes the authored `special` layer once that inferred full-charge state is reached. The same IDA re-check of `CUser::Update` also shows the client suppressing several avatar-owned layers while `CAvatar::IsOnPlayingRotateAction` is active, so the simulator now marks the currently confirmed sudden-death / `Final Cut` repeat layer path and the flying-wing fallback path as rotate-sensitive and suppresses those avatar-owned effects during the rotate-style action family the current runtime can identify safely. The runtime now consults the player's current client raw-action code when it can resolve one, uses the exact client rotate raw-action set confirmed in IDA (`101`, `82`, `114`, `149`, `151`, `193`), and now also derives the exact client action roots for that raw-action set through the same `get_action_name_from_code`-shaped map the simulator already builds from `Character/00002000.img`, so the named fallback now safely covers `homing`, `triplefire`, `bamboo`, `lightingBolt`, `breathe_prepare`, and `chainPull` alongside the already-confirmed WZ-authored rotate families (`doubleJump`, `backspin`, `rollingSpin`, `darkSpin`, `screw`, `somersault`, and explicit `finalCut`) instead of continuing to overdraw those layers whenever only the action name is visible. That suppression still also consults the player runtime's forced one-time skill action, so client-owned layers hide during skill-driven rotate windows even when the visible current action has already fallen back to a benign standing or generic attack pose. The remaining gap is narrower again: broader client-only `CUser::Update` families beyond `More Wild`, swallow, double-jump, the existing movement or buff WZ shapes, and these currently promoted friendly `affected`-area ownership families still need explicit client-backed ownership rules before they can be mirrored safely; the rotate fallback now covers the exact client action roots for the currently confirmed `IsOnPlayingRotateAction` raw-action set, but any wider rotate family still needs new client evidence before more names can be added safely; and the Energy Charge runtime could still use deeper client confirmation if later IDA work proves a different accumulation cadence or extra non-visual full-charge behavior around `bodyAttack`. | Visible parity is closer for `More Wild`, `Final Cut`-style sudden-death buffs, the broader WZ-backed movement cast surface, flying-wing buffs, Dark Sight-family stealth buffs, root-`invisible` effect-only buffs, Wild Hunter swallow buffs, the confirmed damage-reflect buff family, the secondary-only `effect0` surface, the broader timed self-buff `effect/effect0` pair surface, the Energy Charge family, and now the client-owned friendly affected-area aura surface because the promoted avatar-owned layers now follow the client's ownership rules more closely, no longer require the primary `effect` branch to exist before promotion, no longer double-render single-plane authored visuals on both avatar planes, now also refuse to treat a WZ-authored `affected0` aura as Ågno affected branchÅh when deciding whether timed buff `effect/effect0` planes should be rebound onto the avatar, now defer Energy Charge `special` layers until a separate full-charge owner exists instead of attaching them at cast start, now drive that Energy Charge owner forward on successful local hits toward the WZ-backed charge threshold, now let party-owned support robots keep their avatar-owned aura eligibility from WZ summon metadata alone instead of depending on localized description text, now synthesize the projected remote support-area buff owner from whichever source or linked support skill actually publishes `affected` / `affected0`, now let the local affected-area avatar seam pull that same aura plane and party-support eligibility through linked support skills such as `32110007 -> 32001003` instead of only the wrapper owner, and now also preserve sibling `affected0` planes on looping buff / aura visuals while the player stands inside qualifying local- or party-owned player-skill affected areas such as `35121010`, `35111011`, `32001003`, and the same WZ-authored mass-spell or support-zone area shape without re-triggering those avatar-owned auras every refresh tick. Rotate-sensitive avatar-owned layers also now stop depending on a broad visible-action `spin` substring, inspect the same forced one-time action seam the player runtime uses for skill-driven attacks, consult the same client rotate raw-action set (`101`, `82`, `114`, `149`, `151`, `193`) that `CAvatar::IsOnPlayingRotateAction` checks, and now cover the exact client action roots for that set through the simulatorÅfs `Character/00002000.img` action-code map (`homing`, `triplefire`, `bamboo`, `lightingBolt`, `breathe_prepare`, `chainPull`) alongside the already-confirmed WZ-authored rotate families (`doubleJump`, `backspin`, `rollingSpin`, `darkSpin`, `screw`, `somersault`, `finalCut`). Focused source-side regression coverage is now present under `UnitTest_MapSimulator/ClientOwnedAvatarEffectParityTests.cs` for the confirmed raw-action set, the newly derived client action-name aliases, and the pre-existing named rotate fallback. The remaining work is narrower: broader `CUser::Update`-owned effect families that do not publish these WZ-authored `special*`, timed `effect/effect0`, `affected/affected0`, invisible/effect-only buff, movement-action, or the now-covered friendly affected-area shape still need explicit client-backed remaps before they can be mirrored safely, the currently confirmed rotate family no longer needs raw-action-to-name discovery but any wider rotate-family expansion still needs fuller client evidence, and Energy Charge may still need deeper client-backed tuning if later evidence shows that its full-charge cadence or combat-side `bodyAttack` runtime differs from the current WZ-shaped accumulation seam. | `PlayerCharacter`, `PlayerManager`, `SkillLoader`, `SkillManager`, `CharacterAssembler`, `CharacterData`, `ClientOwnedAvatarEffectParity`, `UnitTest_MapSimulator/ClientOwnedAvatarEffectParityTests.cs` (`CUser::Update`, `CUser::UpdateMoreWildEffect`, `CUser::UpdateAr01Effect`, `CUserLocal::TryDoingSwallowBuff`, `CAvatar::IsOnPlayingRotateAction`, `get_action_name_from_code`, `Character/00002000.img/{doubleJump,backspin,rollingSpin,darkSpin,screw,somersault,homing,triplefire,bamboo,lightingBolt,breathe_prepare,chainPull,finalCut}`, `Skill/3312.img/33121006/{special,special0,finish,finish0,back,back_finish}`, `Skill/434.img/4341002/repeat`, `Skill/400.img/4001003/effect`, `Skill/1400.img/14001003/effect`, `Skill/310.img/3101003/effect`, `Skill/3300.img/33001002/effect`, `Skill/122.img/1221007/{effect,effect0}`, `Skill/433.img/4331005/{effect,effect0}`, `Skill/3001.img/30010186/effect`, `Skill/3310.img/33101006/effect`, `Skill/000.img/0001037/{effect,effect0,invisible}`, `Skill/110.img/1101007/{effect,effect0}`, `Skill/120.img/1201007/{effect,effect0}`, `Skill/3110.img/31101003/{effect,effect0}`, `Skill/1411.img/14111000/effect0`, `Skill/510.img/5101007`, `Skill/511.img/5110001/{special,affected,common/x,info/energyCharge,info/fullChargeEffect}`, `Skill/1510.img/15100004/{special,affected,common/x,info/energyCharge,info/fullChargeEffect}`, `Skill/000.img/0000086/{affected,affected0}`, `Skill/532.img/5320007/{affected,affected0}`, `Skill/572.img/5720005/{affected,affected0}`, `Skill/3511.img/35111011/{affected,info/type,info/minionAbility}`, `Skill/3512.img/35121010/{affected,info/type,info/minionAbility}`, `Skill/3512.img/35121007/affected`, `Skill/3312.img/33121007/affected`, `Skill/3200.img/32001003/affected`, `Skill/3210.img/32101002/affected`, `Skill/3210.img/32101003/affected`, `Skill/3211.img/32110007/info/affectedSkill`) |
| Partial | Remote user / `CUserPool` actor parity | `RemoteUserActorPool` remains the shared remote-user owner for AvatarLook decode, enter/leave, movement snapshots, helper/team state, chair/mount state, prepared-skill world overlays, packet `223` avatar refresh, packet-owned `Effect/ItemEff.img` social effects, carry-item and completed-set rendering, and shared `225/226` temporary-stat storage instead of feature-local placeholders. This pass keeps that work in the same seam and closes another visible temp-stat presentation gap with WZ evidence first: the existing hidden-like ghost-family action split, morph override, Mechanic `01932016` mount owner, and Shadow Partner `special/*` rendering remain in place, and remote temp-stat presentation now also claims the known WZ-backed ammo and barrier avatar-effect families instead of leaving those decoded hints as dead state. When the shared temp-stat snapshot carries `SoulArrow`, the pool now resolves the known remote skill families in job-priority order across `Skill/412.img/4121006`, `Skill/3310.img/33101003`, `Skill/1310.img/13101003`, `Skill/320.img/3201004`, and `Skill/310.img/3101004`, reuses the existing buff-avatar-effect fallback rules to prefer authored `affected` branches and otherwise loop authored `effect` branches, preserves a stable per-actor animation start time across later avatar, chair, mount, or temp-stat refreshes, and draws those WZ-backed planes through the shared remote world renderer. The same shared owner now also claims the known Barrier-family presentation it can justify from WZ today through `Skill/2112.img/21120007`, again using the existing affected-vs-effect fallback plane rules instead of inventing a new owner. Focused source-side coverage for this row now includes `UnitTest_MapSimulator/RemoteUserTemporaryStatPresentationTests.cs`, pinning the new Soul Arrow job-priority ordering and the current Combo Barrier ownership ordering alongside the already-landed Mechanic and Shadow Partner temp-stat presentation checks. Executing `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter RemoteUserTemporaryStatPresentationTests` is still blocked in this workspace by unrelated pre-existing `HaCreator` compile failures before the test project can finish building; the current first surfaced blockers are `MapSimulator/Interaction/SocialRoomModels.cs(468)`, `MapSimulator/UI/Windows/LoginCreateCharacterWindow.cs(17)`, `MapSimulator/UI/Windows/MapTransferUI.cs(12)`, `MapSimulator/UI/Windows/LoginTitleWindow.cs(29)`, and `MapSimulator/UI/Windows/LoginUtilityDialogWindow.cs(131)`. This keeps the row aligned with the same client-owned seams already evidenced for `CUserPool::OnPacket`, `CUserPool::Update`, `CUserRemote::Update`, `CUserRemote::OnAvatarModified`, `SecondaryStat::DecodeForRemote`, `CUser::SetCarryItemEffect`, and `CAnimationDisplayer::Effect_ByItem`. | The remaining gap is narrower again: the packet router still only covers the implemented enter/leave, move/state, helper/team, chair/mount, prepared-skill, melee, item-effect, avatar-modified, and temp-stat families rather than the broader client packet surface; Messenger member parity still depends on the simulatorÅfs optional member-info `AvatarLook` trailer instead of fuller official messenger decode and room-member lifecycle; and remote temp-stat/update parity is still incomplete beyond the currently decoded hidden-like, morph, Mechanic, Shadow Partner, known Aura, Soul Arrow, and the currently justified Combo Barrier presentation. Remaining work on this row is concentrated on broader or less-certain Barrier ownership outside that known WZ branch, the exact client carry-item orbit choreography and asset choice, client-only relationship pairing rules that depend on serial tables instead of same-item reconciliation, broader packet-authored user effects beyond `ItemEff`, remote afterimages beyond the current melee seam, any other feature-local remote shells that still bypass the shared pool, and higher-fidelity movement smoothing beyond the current sampled move-path path. | shared field actor/runtime layer (`CUserPool::OnPacket`, `CUserPool::Update`, `CUserRemote::Update`, `CUserRemote::OnAvatarModified`, `SecondaryStat::DecodeForRemote`, `CUser::SetCarryItemEffect`, `CAnimationDisplayer::Effect_ByItem`, `CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUser::Update`, `CMovePath::{Decode,OnMovePacket}`, `Morph/*.img`, `Character/TamingMob/01932016.img`, `Character/01112916.img/info/effect/path`, `Character/01112924.img/info/effect/path`, `Character/01112926.img/info/effect/path`, `Character/00002000.img/{tank_stand,tank_walk,tank_prone,tank,siege_stand,siege,tank_siegestand,tank_siegeattack}`, `Skill/3511.img/35111004/action/0`, `Skill/3512.img/35121005/action/0`, `Skill/3512.img/35121013/action/0`, `Skill/411.img/skill/4111002/special`, `Skill/421.img/skill/4211008/special`, `Skill/1411.img/skill/14111000/special`, `Skill/412.img/4121006/effect`, `Skill/3310.img/33101003/effect`, `Skill/1310.img/13101003/effect`, `Skill/320.img/3201004/effect`, `Skill/310.img/3101004/effect`, `Skill/2112.img/21120007/{effect,affected}`, `Effect/CharacterEff.img/1112916`, `Effect/CharacterEff.img/1112924`, `Effect/CharacterEff.img/1112926`, `Effect/ItemEff.img/1112001`, `Effect/ItemEff.img/1112800`, `Effect/ItemEff.img/1112803`, `Effect/ItemEff.img/2270000`, `Effect/ItemEff.img/2270002`, `Effect/ItemEff.img/2270003`, `Etc/SetItemInfo.img`, `Effect/SetItemInfoEff.img`, `UnitTest_MapSimulator/RemoteUserTemporaryStatPresentationTests.cs`) |


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Remote actor foundation pass:
   Add a real `CUserPool`-style remote-user actor layer before continuing to extend wedding, Ariant, Battlefield, minimap-helper, and follow-request overlays independently.
3. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
4. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
5. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
6. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".



