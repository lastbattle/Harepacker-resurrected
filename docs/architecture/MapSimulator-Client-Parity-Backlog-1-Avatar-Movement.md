# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CAvatar::PrepareActionLayer` at `0x466cb0`
- `CAvatar::PrepareCharacterActionLayer` at `0x465100`
- `CAvatar::PrepareTamingMobActionLayer` at `0x465ce0`
- `CAvatar::FixCharacterPosition` at `0x4616b0`
- `CAvatar::CharacterFrameUpdate` at `0x46abe0`
- `CAvatar::TamingMobFrameUpdate` at `0x46b450`
- `CAvatar::SetMorphed` at `0x469c80`
- `CAvatar::SetRidingVehicle` at `0x46a790`
- `CAnimationDisplayer::Effect_ByItem` at `0x44f330`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Avatar and player parity

- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::SetActivePet` at `0x8e40e0`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CAnimationDisplayer::Effect_ByItem` at `0x44f330`
- `CActionMan::GetCharacterImgEntry` at `0x417fd0`
- `CActionMan::LoadTamingMobAction` at `0x427a10`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::Update` at `0x937330`

Notes:
`CUser::LoadLayer` and `CUser::PrepareActionLayer` are the clearest resolved avatar-composition seams for layer loading and action-layer setup. `CUser::Update` and `CUser::UpdateMoreWildEffect` also show that the client maintains several additional avatar/effect layers outside the base assembler path, while `CUserLocal::Update` contains the local-player emotion reset path and action/state transitions that matter for expression and rare-action parity. A later IDA pass also confirms that remote avatar ownership is a first-class client surface rather than a loose extension of local-player logic: `CUserPool::OnPacket` dispatches enter, leave, common, remote, and local user packet families, `CUserPool::Update` advances local plus remote users and shared social-effect state, `CUserRemote::Update` owns remote move-action, effect, portable-chair, and prepared-skill presentation, and `CAnimationDisplayer::Effect_ByItem` confirms that one-shot item effects are managed by a dedicated client animation-owner seam rather than only by feature-local overlay code.

### 2. Packet-owned avatar emotion parity discovered by direct `CUserLocal::OnPacket` scan

- `CUser::OnEmotion` at `0x8e0150`
- `CUser::OnRandomEmotion` at `0x8e34b0`

Notes:
The latest direct `CUserLocal::OnPacket` re-check exposed another local-avatar owner that the backlog was not naming anywhere else. `CUser::OnEmotion` decodes an emotion id, duration, and the `m_bEmotionByItemOption` flag before calling `CAvatar::SetEmotion`, while `CUser::OnRandomEmotion` decodes an area-buff item id, resolves `CItemInfo::GetAreaBuffItem`, chooses one weighted emotion slot from the authored prop table, and then routes back through the ordinary emotion-change path via `CWvsContext::SendEmotionChange`. That means client expression parity is not only the passive blink/hit reset surface already tracked in `CUserLocal::Update`; it also includes a packet-owned timed-emotion seam and an item-backed random-emotion owner that should stay explicit in the avatar backlog instead of being hidden under generic item-effect or chat feedback rows.

### 3. Taming-mob action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadTamingMobAction` at `0x427a10`

Notes:
The mount row already cited `LoadTamingMobAction` as evidence, but the owner itself was still buried inside a broader render/state row unlike the explicit loader sections already tracked for summons, mobs, pets, employees, and NPCs. IDA confirms this is a distinct client seam: the loader builds taming-mob action frames only after a vehicle id is already known, and the nearby `IsAbleTamingMobAction` / `IsAbleTamingMobOneTimeAction` gates keep the extra Mechanic and ride coverage keyed to vehicle ownership rather than raw action-name inference alone. That means mounted avatar parity is still split the same way as the other actor families in this backlog set: one row for runtime state/ownership and another for the loader that decides which taming-mob action data the runtime can actually consume.

### 4. Character image-entry ownership discovered by targeted `CActionMan` scan

- `CActionMan::GetCharacterImgEntry` at `0x417fd0`

Notes:
The broader avatar row already cited the runtime owners and later frame-build owners, but another client seam still sat unnamed underneath them. `CActionMan::GetCharacterImgEntry` is not just a raw IMG fetch. The decompile shows it caching by equip UOL key, resolving the formatted character data path, reading `info/islot`, `info/vslot`, `info/sfx`, the weekly-variant flag, and the weapon-side `afterImage`, `walk`, `stand`, `attack`, and `attackSpeed` metadata, then switching weekly-capable equips onto a day-specific property path while keeping the non-weekly fallback image entry alive. That means visible avatar parity is split one layer earlier than the current document said: runtime/state ownership in `CUser::*`, image-entry normalization and metadata ownership in `GetCharacterImgEntry`, frame-build ownership in `LoadCharacterAction`, and then feature-local consumers such as face-look or afterimage work that depend on the normalized character entry.

### 5. Character action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadCharacterAction` at `0x427d00`

Notes:
The broader avatar row already cites `CUser::LoadLayer` and `CUser::PrepareActionLayer`, but a targeted `CActionMan` pass shows there is still another distinct client owner underneath those runtime seams. `CActionMan::LoadCharacterAction` does not only fetch the current action frames. The decompile shows it first normalizing the avatar-hair-equip array for the requested action, zeroing body-part lanes the client refuses to merge, falling back to client default top or bottom items when the incoming equip combination would leave the body invalid, stripping known blocked accessory and weapon-sticker ids, suppressing shield and sticker lanes for ride or vehicle families, applying ghost-mode filtering, and only then calling the lower `load_character_action` plus `MergeCharacterSprite` path. That means visible avatar parity is still split three ways rather than two: runtime ownership in `CUser::*`, packet or state ownership in the local or remote actor seams, and a dedicated character frame-builder that decides which merged body-part set the runtime is even allowed to render for a given action.

### 6. Face-look loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadFaceLook` at `0x41cab0`

Notes:
The existing avatar backlog tracks blink, hit-face, and packet-owned emotion timing, but the loader that actually builds those face-expression canvases was still unnamed. IDA shows `CActionMan::LoadFaceLook` is a dedicated client seam: it caches by skin, face, emotion, and accessory state; resolves the face and accessory image entries; enumerates the authored emotion canvases; reads origin and brow metadata; composites face and accessory planes into the final brow canvas list; and records the authored duration on the cached face-look entry. That means facial-expression parity is not only a runtime question in `PlayerCharacter` or `CharacterAssembler`; it also depends on the client loader that decides which face and accessory canvases exist, how they are composited, and how long the resulting emotion entry stays valid.

### 7. Morph action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::GetMorphImgEntry` at `0x418b30`
- `CActionMan::LoadMorphAction` at `0x4193c0`

Notes:
The avatar backlog already tracks morph runtime and resolver parity, but the underlying morph loader was still folded into that broader row unlike the explicit loader sections already called out for character, face, taming-mob, summon, mob, NPC, pet, employee, and dragon paths. IDA shows the morph path is its own client owner: `CActionMan::GetMorphImgEntry` caches per-template morph IMG entries, resolves the formatted `Morph/<id>.img` resource, follows `info/link` indirection by reloading the linked template and merging missing action branches onto the current morph image entry, and stores the normalized property tree that later action requests consume. `CActionMan::LoadMorphAction` then caches by `(templateId, action)`, resolves the action through the client `s_sMorphAction` table rather than ad hoc string guesses, enumerates frame canvases, reads per-frame `delay`, `lt`, `rb`, and `head` metadata, preserves the authored frame-uol string used by the client morph path, and appends the interior-frame reverse pass when the morph action publishes the replay flag. That means morph parity is still split the same way as the other actor families in this backlog set: runtime transform ownership in the player/avatar seams, plus a dedicated morph image and action loader that decides which morph resources and frame tables the runtime is actually allowed to render.

### 8. Avatar action-layer coordinator ownership discovered by follow-up IDA function scan

- `CAvatar::PrepareActionLayer` at `0x466cb0`
- `CAvatar::PrepareCharacterActionLayer` at `0x465100`
- `CAvatar::PrepareTamingMobActionLayer` at `0x465ce0`
- `CAvatar::FixCharacterPosition` at `0x4616b0`
- `CAvatar::CharacterFrameUpdate` at `0x46abe0`
- `CAvatar::TamingMobFrameUpdate` at `0x46b450`
- `CAvatar::SetMorphed` at `0x469c80`
- `CAvatar::SetRidingVehicle` at `0x46a790`

Notes:
The latest IDA function scan did not find another missing `CActionMan` loader family; those are already distributed across the avatar, skill, combat, NPC, and companion backlog files. It did expose a lower avatar-runtime owner that the avatar document only implied through `CUser::*` and loader rows. `CAvatar::PrepareActionLayer` is the coordinator that resolves move or one-time action into raw action, routes morph templates to `LoadMorphAction`, routes normal and mounted avatars to `LoadCharacterAction` and `LoadTamingMobAction`, clamps frame delay through walk speed or action speed, applies held shoot-action infinite delays, inserts canvases into the under-face, under-character, over-character, and taming-mob layers, and calls `FixCharacterPosition` when riding composition needs the character and taming-mob origins reconciled. `FixCharacterPosition` then derives body, face, muzzle, taming-mob navel, head, and muzzle offsets from the current character and taming-mob frame tables while applying flip-specific signs. That means the remaining visible avatar gap is not only loader data selection or high-level `CUser` state: it also includes the lower runtime layer coordinator that binds those loader outputs into GR2 layer ownership, frame timing, mounted anchor relocation, and morph/taming-mob transition behavior.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Avatar and player parity

These are the biggest remaining gaps for visible client parity.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Default avatar selection | The simulator keeps the deterministic, WZ-verified SuperGM/Wizet fallback selection of `1002140` (Wizet Invincible Hat), `1042003` (Wizet Plain Suit), `1062007` (Wizet Plain Suit Pants), `1072005` (Leather Sandals), `1092008` (Pan Lid), and `1322013` (Wizet Secret Agent Suitcase) with matching face, hair, level, and SuperGM job metadata, and the no-packet avatar seam is now shared all the way through preset-backed defaults as well. WZ was rechecked again for this row in `Etc/MakeCharInfo.img/000/{male,female}` and still pins the non-sale starter request layout to the client-owned `0..7` face/hair/skin/hairColor/coat/pants/shoes/weapon families (`male/0 = {20000,20001,20002}`, `male/1 = {30030,30020,30000}`, `male/2 = {0,7,3,2}`, `male/3 = {0,1,2,3}`, `male/4 = {1040002,1040006,1040010}`, `male/5 = {1060002,1060006}`, `male/6 = {1072001,1072005,1072037,1072038}`, `male/7 = {1302000,1322005,1312004}`, `female/0 = {21000,21001,21002}`, `female/1 = {31000,31040,31050}`, `female/2 = {0,7,3,2}`, `female/3 = {0,1,2,3}`, `female/4 = {1041002,1041006,1041010,1041011}`, `female/5 = {1061002,1061008}`, `female/6 = {1072001,1072005,1072037,1072038}`, `female/7 = {1302000,1322005,1312004}`). `CharacterPreset` now owns a synthesized `LoginAvatarLook`, build-backed preset capture preserves hidden equip lists, pet ids, and packet-owned `weaponStickerID` visuals through that owner, `CharacterPreset.ToBuild` rebuilds through `CharacterLoader.LoadFromAvatarLook` whenever the synthesized owner is present, and `CharacterConfigManager.CreateDefaultMalePreset` / `CreateDefaultFemalePreset` now create the default preset from the same synthesized `LoginAvatarLook` source used by roster previews and packet emission instead of re-materializing only scalar selection metadata. Legacy preset files without a serialized `AvatarLook` still load through the existing scalar fallback for compatibility, but the default-avatar parity gap for new simulator-owned presets is closed. | Default avatars, synthesized login previews, packet-authored avatars, and preset-driven defaults now converge on the same render-safe avatar owner, so local default tooling no longer risks silently dropping hidden equips, pets, or weapon stickers just because no packet bytes existed. Focus for later avatar work can stay on other backlog rows instead of this ownership split. | `CharacterLoader.LoadDefaultMale`, `CharacterLoader.LoadDefaultFemale`, `CharacterLoader.LoadFromAvatarLook`, `LoginAvatarLookCodec`, `LoginCharacterRosterEntry.CreateRuntimeBuild`, `MapSimulator.ResolveLoginRosterAvatarLook`, `MapSimulator.CreateLoginPacketCharacterEntry`, `MapSimulator.CreateLoginCharacterAccountEntryState`, `CharacterPreset.FromBuild`, `CharacterPreset.ToBuild`, `CharacterConfigManager.CreateDefaultMalePreset`, `CharacterConfigManager.CreateDefaultFemalePreset`, `UnitTest_MapSimulator/DefaultAvatarSelectionPresetParityTests.cs` (`CUser::LoadLayer`, `CUIAvatar::CUIAvatar`, `GW_CharacterStat::Decode`, `AvatarLook::Decode`, `Etc/MakeCharInfo.img/000/{male,female}`) |
| Implemented | Action coverage | Character-part loading now preserves the existing core action order and appends additional WZ action nodes discovered on each part instead of stopping at the old fixed lists | Uncommon body/equipment poses can now load when the source IMG exposes them, which closes the loader-side empty-state gap for rare, scripted, and death-adjacent actions | `CharacterLoader.StandardActions`, `CharacterLoader.AttackActions` (`CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUserLocal::SetMoveAction`) |
| Implemented | Death / rare action frames | Runtime action selection now carries raw one-shot action names through to `CharacterAssembler` instead of forcing uncommon actions back through the limited enum-only render path | Loaded death and other rare WZ actions can now survive from selection to final frame assembly instead of degrading immediately to generic fallbacks | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler` |
| Implemented | Facial expression behavior | The local player runtime now owns blink cadence and short-lived hit expressions, and updates the assembler expression state when those transitions occur | The avatar now reacts over time instead of staying pinned to the static default face | `CharacterAssembler.GetFaceFrame`, `CharacterLoader.LoadFaceExpressions`, `PlayerCharacter` (`CUserLocal::Update` emotion reset path) |
| Implemented | Equip anchor fallback rules | Equipment alignment now resolves through explicit shared map-point precedence instead of using literal pixel nudges when preferred anchors are missing | Parts stay render-safe while remaining tied to WZ anchor data rather than ad hoc offsets | `CharacterAssembler.CalculateEquipOffset`, `CharacterAssembler.TryCalculateHeadEquipOffset` |
| Implemented | Client-verified fallback order | Action lookup now prefers exact action names, then raw-action family reductions, then swim/fly aliasing, before the final `stand1` escape hatch | Character assembly now follows a documented action-family precedence order instead of jumping straight from miss to generic standing frames | `CharacterAssembler`, `CharacterPart.FindAnimation` (`CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`) |
| Implemented | Packet-owned avatar emotion parity | The local avatar still owns a dedicated packet-driven emotion seam instead of relying only on passive blink and hit-face behavior, and this row now mirrors the full currently confirmed `CAvatar::SetEmotion` gate set instead of only the duration path plus morph early-return. IDA-backed packet routing remains wired into the simulator utility surface with the real `CUserLocal::OnPacket` opcodes for `CUser::OnEmotion` (`232`) and `CUser::OnRandomEmotion` (`258`), `PlayerCharacter` still keeps the packet-owned face-expression state with the decoded duration and now stores the decoded item-option bit at the `CUser::OnEmotion` seam even when the active face layer is later suppressed or cleared, still retains that resolved packet-owned emotion snapshot after the live face-expression owner expires or an explicit default clear resets the active owner, and the runtime still routes the active state back through the existing assembler face-owner so packet-authored expressions override blink until the resolved window expires. The same seam still reuses the existing under-face transient avatar-effect owner to play any authored `Etc/EmotionEffect.img/<name>` animation that exists in the current data set, matching the client-side `CAvatar::SetEmotion` `Effect_General("Etc/EmotionEffect/%s")` call without introducing a parallel overlay path; rechecking `Etc/EmotionEffect.img` in the current WZ export still shows the authored overlay set capped at `blink`, `hit`, `smile`, `troubled`, `cry`, `angry`, `bewildered`, `stunned`, `vomit`, and `oops`, and the local player seam now also clears that reused under-face owner when the resolved packet-owned emotion window expires so the overlay lifetime stays tied to the same duration gate as the face layer. WZ was rechecked first for the item-backed random-emotion owner and the raw-action names that the client-side suppression branch actually targets: `item/Cash/0528` still authors `05281000/emotion = {angry=25, cry=20, vomit=40, oops=15}` and `05281001/emotion = {cheers=25, chu=20, glitter=25, love=30}`, `item/Cash/0539` still carries the authored fixed `info/emotion` leg on `05390000` and `05390001`, and `Character/00002000.img` still publishes the raw-action roots `rain`, `paralyze`, and `burster2` that correspond to the client raw codes later checked in IDA. Client evidence was then rechecked in IDA: `CUser::OnRandomEmotion` still resolves the weighted slot and calls `CWvsContext::SendEmotionChange`, `CUser::OnEmotion` still decodes `(emotionId, duration, m_bEmotionByItemOption)` and stores the flag before forwarding only `(emotionId, duration)` into `CAvatar::SetEmotion`, `CAvatar::SetEmotion` / `CAvatar::PrepareFaceLayer` (`0x466b00`, `0x4647d0`) still key face-layer lifetime and `Effect_General("Etc/EmotionEffect/%s")` off the duration path rather than the stored item-option flag, `CAvatar::SetEmotion` still returns immediately when a morph template is active, `CUserLocal::SetDamaged` and `CUserRemote::OnHit` still check the stored `m_bEmotionByItemOption` flag before applying the ordinary damage hit-face emotion through `CAvatar::SetEmotion(1, 1500)`, and `CAvatar::SetEmotion` now rechecked cleanly shows the remaining non-morph suppression branch too: when the active one-time/raw action resolves to `41`, `42`, or `57`, the client suppresses only emotion id `8` before the face layer or under-face overlay is applied. The simulator now mirrors that split inside the existing local-utility/CWvsContext seam: `OnRandomEmotion` no longer short-circuits to a local avatar apply or effect-duration fallback preview, and instead resolves the authored weighted emotion, respects the `CWvsContext::SendEmotionChange` 2-second gate, emits opcode `56` with the client payload layout `(emotionId, duration=-1, byItemOption=0)` through the live bridge/outbox/deferred transport path when those transports are available, waits for the later inbound `OnEmotion` echo before the local avatar expression actually changes, and now also enforces the exact client-shaped inbound packet layouts for both `OnEmotion` and `OnRandomEmotion` instead of accepting truncated or trailing-byte simulator payloads. The local `OnEmotion` duration resolver still treats `m_bEmotionByItemOption` as observational state only, so zero-duration packets fall back through the same effect-duration then face-look-duration order regardless of the bit; the local player seam still consumes but does not apply packet-owned emotions while a morph transform is active; and `PlayerCharacter` now also suppresses blink emotion packets while the active raw/forced action resolves to `rain`, `paralyze`, or `burster2`, using the existing client raw-action table first and falling back to those exact client-published action names only when the raw code is not available. Focused regression coverage now lives in `UnitTest_MapSimulator/PacketOwnedLocalUtilityParityTests.cs` for exact inbound packet decoding on `OnEmotion(232)` and `OnRandomEmotion(258)` plus the zero-duration fallback staying independent of the item-option bit after decode. This pass closed the retained-flag gap: after rechecking the downstream client hit handlers, the simulator now preserves the decoded `m_bEmotionByItemOption` equivalent beyond active face-layer expiry or explicit default clears and uses that retained flag to suppress the existing hit-face owner when damage lands, matching the client `CUserLocal::SetDamaged` / `CUserRemote::OnHit` guard before `CAvatar::SetEmotion(1, 1500)`. No currently confirmed packet-owned avatar emotion gap remains for this row. | Visible avatar-expression parity now covers both direct emotion packets and the client-authenticated random-emotion re-entry path with the client-shaped timing split: random-emotion packets resolve WZ-authored weighted tables and mirror `SendEmotionChange`, the actual expression change still waits for the inbound emotion packet just as the client routes it through `CUser::OnEmotion`, direct emotion duration now follows the client-owned `(emotionId, duration)` lifetime path instead of a simulator-only item-option infinite fallback, the reused `EmotionEffect` overlay now also stops on that same resolved lifetime instead of free-running past the face-expression window, the decoded item-option ownership bit now survives on the same player seam after the active owner expires instead of disappearing with the face layer, the local utility inbox now enforces the same fixed packet shapes the client decodes for opcodes `232` and `258`, morph-transformed avatars now ignore inbound packet-owned face-expression applies instead of forcing impossible face layers over a morph owner, and the remaining `CAvatar::SetEmotion` one-time-action gate is now attached too so blink packets no longer apply during the client-confirmed `rain` / `paralyze` / `burster2` raw-action window. The retained item-option bit now participates in the currently confirmed downstream hit-face suppression owner as well, so this row has no currently confirmed remaining parity gap around weighted selection, `EmotionEffect` overlays, outbound request/throttle mirroring, inbound packet decoding, by-item-option duration handling, morph-template guards, overlay-lifetime clamping, raw-action blink suppression, or retained-flag hit-face behavior. | local avatar expression layer (`CUserLocal::OnPacket`, `CUser::OnEmotion`, `CUser::OnRandomEmotion`, `CAvatar::SetEmotion`, `CAvatar::PrepareFaceLayer`, `get_action_name_from_code`, `CItemInfo::GetAreaBuffItem`, `CWvsContext::SendEmotionChange`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Character/PacketOwnedAvatarEmotionResolver.cs`, `HaCreator/MapSimulator/Interaction/PacketOwnedLocalUtilityContextState.cs`, `HaCreator/MapSimulator/Managers/LocalUtilityPacketInboxManager.cs`, `HaCreator/MapSimulator/MapSimulator.PacketOwnedUtilityParity.cs`, `UnitTest_MapSimulator/PacketOwnedLocalUtilityParityTests.cs`, `item/Cash/0528`, `item/Cash/0539`, `Etc/EmotionEffect.img`, `Character/00002000.img/{rain,paralyze,burster2}`) |
| Partial | Morph avatar pipeline parity | MapSimulator still resolves concrete morph template ids from `common/morph` and `PVPcommon/morph` into `Morph/*.img`, loads those images as a dedicated avatar render owner, resolves UOL-linked morph frames, preserves per-frame anchors such as `head`, backfills through same-family base IMG fallback (`id -> id0 -> id00 -> id000`), paired `100x <-> 110x` fallback, and WZ-backed `info/link` inheritance, and routes hostile polymorph mob skills plus the current flag-only `info/morph = 1` outliers through that same owner seam. This pass kept the work inside that owner rather than widening generic avatar fallback and rechecked the broader cannon and pirate request surface that already lives there. WZ was rechecked first in the mounted export: `Morph/1000.img`, `1001.img`, `1100.img`, and `1101.img` still publish the authored gun branch as `doublefire`, the authored burst branch as `eburster`, generic movement branches such as `jump`, and the stab backstops `stabO1`, `stabO2`, and `proneStab`, while still publishing no verbatim `handgun`, `triplefire`, `airstrike`, `fireburner`, `coolingeffect`, `homing`, `rapidfire`, `cannon`, `torpedo`, `flamesplash`, `swiftShot`, `cannonSmash`, `giganticBackstep`, `rushBoom`, `cannonSlam`, `counterCannon`, `cannonSpike`, `superCannon`, `magneticCannon`, `bombExplosion`, `monkeyBoomboom`, `immolation`, `piratebless`, `pirateSpirit`, `cannonBooster`, `noiseWave`, `noiseWave_pre`, `noiseWave_ing`, or `cannonJump` branch anywhere. The direct skill-side action rows were rechecked too: `Skill/530.img/skill/5301000/action/0 = swiftShot`, `5301001/action/0 = rushBoom`, `5301002/action/0 = cannonBooster`, `5301003/action/0 = piratebless`, `5311000/action/0 = cannonSpike`, `5311001/action/0 = cannonSlam`, `5311002/action/0 = noiseWave`, `5311002/prepare/action = noiseWave_pre`, `5311002/keydown/action = noiseWave_ing`, `5311003/action/0 = cannonJump`, `5311004/action/0 = monkeyBoomboom`, `5321000/action/0 = superCannon`, `5321001/action/0 = bombExplosion`, `5321003/action/0 = magneticCannon`, `5321010/action/0 = pirateSpirit`, and `5321012/action/0 = immolation`, while the published cannon voice rows `5311005/action/0` and `5321005/action/0` still stay on authored `alert3` instead of needing a non-WZ alias. `1002.img` still keeps the non-superman hide-family `fly2*` UOL surfaces, `1003.img` and `1103.img` still expose the archer authored aliases plus generic `shoot1` / `shootF`, `2001.img` and `2002.img` still keep the ice-family authored attack roots plus their double-jump branches, and `2003.img` still remains movement-only. Client evidence remained the second source: `CActionMan::LoadMorphAction` still resolves through `s_sMorphAction`, `CActionMan::Init` still uses `get_action_name_from_code`, and the recovered raw action map still publishes that same pirate and cannon-family request surface around the authored morph branch, including `handgun` (`79`), `doublefire` (`81`), `triplefire` (`82`), `airstrike` (`91`), `shot` (`96`), `fireburner` (`99`), `coolingeffect` (`100`), `homing` (`101`), `rapidfire` (`102`), `cannon` (`111`), `torpedo` (`112`), `burster1` (`56`), `burster2` (`57`), `flamesplash` (`320`), `cannonSmash` (`321`), `giganticBackstep` (`322`), `piratebless` (`323`), `swiftShot` (`324`), `cannonJump` (`325`), `rushBoom` (`326`), `pirateSpirit` (`327`), `counterCannon` (`328`), `cannonSlam` (`329`), `noiseWave_pre` (`330`), `noiseWave_ing` (`331`), `noiseWave` (`332`), `monkeyBoomboom` (`333`), `superCannon` (`334`), `magneticCannon` (`335`), `bombExplosion` (`336`), `cannonSpike` (`337`), `cannonBooster` (`338`), and `immolation` (`339`), so the remaps stay inside the morph owner rather than widening generic avatar fallback. Existing coverage for the earlier `fly2*`, superman climb ordering, candidate-chain, suffix-based flag-only outlier reduction, archer authored aliases, ice-family published roots, and `savage -> stab*` behavior is preserved, and focused regression coverage in `UnitTest_MapSimulator/MorphActionResolverParityTests.cs` now pins the pirate gun-family and broader cannon-family collapse onto `doublefire`, the `noiseWave_pre` / `noiseWave_ing` owner-side fallback, the `cannonJump -> jump` movement fallback, the exact-published `alert3` preservation for cannon voice rows, and verifies that cannon-family remaps do not steal archer-only morph surfaces. Remaining gap: broader client-only morph alias remaps beyond the currently evidenced pirate gun-family, cannon-family skill action requests, cannon-side buff and voice requests that do not already stay on exact published alert branches, `cannonJump`, archer authored aliases, ice-family published roots, and `savage` generic stab surface, plus any future flag-only remap that does not follow the current suffix path, still need additional client evidence before this row can widen non-WZ aliases safely. | Morph-heavy jobs, hostile polymorph skills, hide-family transforms, and the current flag-only morph outliers remain on the dedicated morph-owner seam rather than generic body/equip assembly. With this pass, the same owner now keeps the earlier published non-superman `fly2*` families, the client-published raw `savage` request, the WZ-backed pirate and cannon-family raw request surfaces on the authored `doublefire` / `eburster` morph branches, the `noiseWave_pre` / `noiseWave_ing` prepare and keydown requests on that same owner-side `doublefire` surface, the WZ-backed `cannonJump` movement request on the published `jump` branch, and exact published cannon voice requests on authored `alert3` instead of degrading those requests into generic avatar fallback or a non-morph stand fallback. The row remains narrowed to broader client-only alias remaps that still lack evidence beyond the recovered `LoadMorphAction` table and any future non-suffix flag-only morph remaps, not the currently evidenced WZ-backed owner assembly, `fly2*` runtime ownership, `savage -> stab*`, the pirate/cannon-family `doublefire` collapse, the `eburster` burst alias, the `cannonJump -> jump` morph fallback, the `noiseWave_pre` / `noiseWave_ing` owner-side fallback, the exact `alert3` preservation for cannon voice rows, the `immolation -> doublefire` fallback, or the candidate-chain behavior already landed here. | avatar transform/runtime assembly (`CActionMan::GetMorphImgEntry`, `CAvatar::MoveAction2RawAction`, `CActionMan::LoadMorphAction`, `CActionMan::Init`, `get_action_name_from_code`, `CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUserLocal::SetMoveAction`, `HaCreator/MapSimulator/Character/MorphClientActionResolver.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Character/Skills/SkillLoader.cs`, `UnitTest_MapSimulator/MorphActionResolverParityTests.cs`, `Skill/001.img/0010109`, `Skill/2002.img/20020111`, `Skill/530.img/5301000`, `Skill/530.img/5301001`, `Skill/530.img/5301002`, `Skill/530.img/5301003`, `Skill/531.img/5311000`, `Skill/531.img/5311001`, `Skill/531.img/5311002`, `Skill/531.img/5311003`, `Skill/531.img/5311004`, `Skill/531.img/5311005`, `Skill/532.img/5321000`, `Skill/532.img/5321001`, `Skill/532.img/5321003`, `Skill/532.img/5321005`, `Skill/532.img/5321010`, `Skill/532.img/5321012`, `Morph/0109.img`, `Morph/0111.img`, `Morph/1000.img`, `Morph/1001.img`, `Morph/1002.img`, `Morph/1003.img`, `Morph/1100.img`, `Morph/1101.img`, `Morph/1103.img`, `Morph/2001.img`, `Morph/2002.img`, `Morph/2003.img`) |
| Partial | Mount / vehicle / mechanic mode rendering | `TamingMob` assembly on this row still covers the already-implemented owner paths: normal mount ride frames and `sit` fallback, `eventTamingMob` part equip, Mechanic owner routing to `Character/TamingMob/01932016`, Battleship owner routing to `Character/TamingMob/01932000`, direct `ride2` / `getoff2` transitions, fixed Mechanic state skills, distinct Mechanic/Battleship mounted actions, known-owner authored action preservation, the client-confirmed 1932016-only current-action family, including the vehicle-id-gated `swingT1` / `swingT2` one-time actions, and the earlier local built-in Mechanic transform seam (`35001001`, `35101004`, `35101009`, `35111004`, `35121005`, `35121013`) that keeps `01932016` latched through `SkillManager.UpdateClientOwnedVehicleTamingMobState`, `PlayerCharacter.UpdateOwnedTamingMobRenderState`, and `PlayerCharacter.ResolveMountedStateTamingMobPart`. This pass keeps that tighter boundary and closes the remaining local mounted fallback overlap inside the same owner seam instead of widening ownerless inference. WZ was rechecked first in the current export: `Character/TamingMob/01932016` still publishes the tank presentation roots it reuses for remote and local Mechanic mode (`tank_stand`, `tank_walk`, `tank`, `tank_prone`, `siege_stand`, `tank_siegestand`), still publishes the authored overlap actions that explicit Mechanic mode can surface before or alongside later state skills (`alert2`, `shot`, `ladder2`, `rope2`, `ride2`, `getoff2`), and still omits a dedicated `sit` branch just like `Character/TamingMob/01932000`, whose authored surface still stays on the mounted move and ship-action roots (`stand1`, `stand2`, `walk1`, `walk2`, `jump`, `prone`, `fly`, `alert2`, `alert3`, `cannon`, `torpedo`, `fireburner`, `coolingeffect`). IDA still points the same way for the ownership boundary: `?LoadTamingMobAction@CActionMan@@QAEXJJQAJHAAV?$ZArray@V?$ZRef@UTAMINGMOBACTIONFRAMEENTRY@CActionMan@@@@@@@Z` only loads taming-mob action frames after a vehicle id is already known, `?IsAbleTamingMobAction@@YAHJJ@Z` plus `?IsAbleTamingMobOneTimeAction@@YAHJJ@Z` still gate the extra Mechanic and ride coverage by vehicle id rather than by action name alone, with the latter explicitly admitting raw actions `9` / `10` (`swingT1` / `swingT2`) for `1932016`, and `?SetResistanceRidingMoveAction@CAvatar@@IAEXJH@Z` still shows the client keeping the riding owner alive while Mechanic move actions update. Based on that same explicit-owner seam, `RemoteUserActorPool.ResolveClientVisibleActionName` still keeps the currently evidenced explicit remote Mechanic presentation split inside the remote resolver: explicit `mechanicMode` ids `35001001`, `35101004`, `35101009`, `35111004`, `35121003`, `35121005`, `35121009`, `35121010`, and `35121013` map generic remote `stand1` / `walk1` requests onto the tank or siege presentation roots, generic `jump` / `hit` / `fly` / `swim` requests fall back to the same evidenced stand root instead of fabricating unpublished action names such as `tank_jump`, `tank_hit`, `siege_jump`, `siege_hit`, `tank_siegejump`, or `tank_siegehit`, generic mounted `sit` keeps the same explicit owner alive across the remote resolver, override seam, and follow-eligibility seam, and authored overlap actions that `01932016` itself publishes, including `shot`, stay preserved instead of degrading onto generic presentation aliases. This pass tightens the same rule inside the local taming-mob frame owner and shared taming-mob alias surface as well: `TamingMobActionFrameOwner` now resolves local Mechanic climb fallback directly onto the WZ-published `ladder2` / `rope2` roots instead of walking through unpublished `tank_ladder` / `siege_rope` intermediates first, `CharacterAssembler` keeps the shared synthetic Mechanic aliases collapsed onto the same published stand and climb roots (`tank_stand`, `siege_stand`, `tank_siegestand`, `ladder2`, `rope2`) without a trailing `stand1` escape, mounted `sit` now counts as a published-root render-owner fallback for the known local Mechanic seam, and canonical Battleship owner retention through `stand1` / `stand2` stays explicit. That keeps the local render-owner seam aligned with the already-evidenced remote resolver and the explicit-owner boundary without reopening `ClientOwnedVehicleSkillClassifier.IsOwnerlessMechanicVehicleInferenceActionName` for plain `tank` / `siege` / `tank_*` / `siege_*` names that WZ still publishes on `Character/00002000.img` too. Focused regression coverage now lives in `UnitTest_MapSimulator/RemoteMechanicModeRenderingParityTests.cs`, pinning the explicit remote Mechanic-mode remaps against the WZ-published roots, preservation of explicit overlap actions such as `shot`, continued rejection of unknown positive `mechanicMode` values as ownerless mount inference, known Battleship owner retention through mounted `sit`, direct local Mechanic synthetic-fallback collapse onto published stand and climb roots, and the local Mechanic `sit` render-owner fallback. Remaining gap after this run: broader truly ownerless client-only vehicle update or move-state sources that still surface with no active buff, no active built-in transform owner, no handoff owner, no canonical equipped owner, no explicit `mechanicMode`, and no mounted render owner signal at all still need new client evidence before they can be mirrored safely, and Battleship still has no comparable transform-owned local owner seam beyond the already-implemented named or known-owner paths. | Mounted-state, follow eligibility, local current-action ownership, local render-owner retention, explicit remote `mechanicMode` presentation, and now the local taming-mob published-root fallback all stay aligned across the evidenced blank-tick seam, the named Mechanic/Battleship owner seams, the client-confirmed 1932016-only current-action family, and the built-in local Mechanic transform seam without falling back to generic `tank*` / `siege*` action-name guessing that WZ also publishes on the base body. With the added local `sit` fallback retention for canonical Battleship and Mechanic owners, the direct local remap of remaining synthetic Mechanic fallback names onto WZ-published stand and climb roots, and the checked-in regression file that pins both explicit remote remaps and local owner retention, mounted owners no longer depend on unpublished `tank_jump`-style local aliases to stay render-safe once the simulator already knows which vehicle owner is active. The row is now narrowed to genuinely ownerless client-managed vehicle states and any future Battleship-specific transform-owned local owner seam, not the already-owned Mechanic transform, explicit remote `mechanicMode`, mounted `sit` fallback retention, local published-root fallback mapping, client-gated `swingT1` / `swingT2` known-owner retention, or known-owner paths. | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler`, `TamingMobActionFrameOwner`, `SkillManager`, `ClientOwnedVehicleSkillClassifier`, `FollowCharacterEligibilityResolver`, `EscortFollowController`, `RemoteUserActorPool`, `MapSimulator.PacketOwnedUtilityParity`, `UnitTest_MapSimulator/RemoteMechanicModeRenderingParityTests.cs` (`35001001`, `35101004`, `35101009`, `35111004`, `35121003`, `35121005`, `35121009`, `35121010`, `35121013`, `5211004`, `5211005`, `5221007`, `5221008`, `?LoadTamingMobAction@CActionMan@@QAEXJJQAJHAAV?$ZArray@V?$ZRef@UTAMINGMOBACTIONFRAMEENTRY@CActionMan@@@@@@@Z`, `?IsAbleTamingMobAction@@YAHJJ@Z`, `?IsAbleTamingMobOneTimeAction@@YAHJJ@Z`, `?SetResistanceRidingMoveAction@CAvatar@@IAEXJH@Z`, `Character/00002000.img`, `Character/TamingMob/01932016`, `Character/TamingMob/01932000`, `Skill/3500.img/skill/35001001`, `Skill/3510.img/skill/35101004`, `Skill/3510.img/skill/35101009`, `Skill/3511.img/skill/35111004`, `Skill/3512.img/skill/35121003`, `Skill/3512.img/skill/35121005`, `Skill/3512.img/skill/35121009`, `Skill/3512.img/skill/35121010`, `Skill/3512.img/skill/35121013`, `Skill/521.img/skill/5211004`, `Skill/521.img/skill/5211005`, `Skill/522.img/skill/5221007`, `Skill/522.img/skill/5221008`) |
| Partial | Skill-specific avatar transforms | This run checks in the missing row-focused regression file at `UnitTest_MapSimulator/PreparedSkillAvatarTransformParityTests.cs` without widening the existing prepare-candidate, built-in avatar-transform, or Mechanic transform-owner seams. WZ was rechecked first against the same row-owned surface and still matches the current implementation: `Skill/1411.img/skill/14111006/prepare/action = dash`, `Skill/3310.img/skill/33101005/prepare/action = swallow_loop`, `Skill/3212.img/skill/32121003/action/0 = cyclone_pre`, `Skill/531.img/skill/5311002/{prepare/action = noiseWave_pre, keydown/action = noiseWave_ing, action/0 = noiseWave}`, `Skill/2312.img/skill/23121000/{prepare/action = dualVulcanPrep, keydown/action = dualVulcanLoop, keydownend/action = dualVulcanEnd}`, `Skill/3500.img/skill/35001001/{prepare/action = flamethrower_pre, keydown/action = flamethrower, keydownend/action = flamethrower_after, action/0 = flamethrower}`, `Skill/3510.img/skill/35101004/action/0 = rbooster_pre`, `Skill/3510.img/skill/35101005/action/0 = msummon`, `Skill/3510.img/skill/35101009/{prepare/action = flamethrower_pre2, keydown/action = flamethrower2, keydownend/action = flamethrower_after2}`, `Skill/3511.img/skill/35111002/action/0 = msummon`, `Skill/3511.img/skill/35111004/action/0 = siege_pre`, `Skill/3511.img/skill/35111005/action/0 = msummon`, `Skill/3511.img/skill/35111011/action/0 = msummon`, `Skill/3512.img/skill/35121003/action/0 = msummon2`, `Skill/3512.img/skill/35121005/action/0 = tank_pre`, `Skill/3512.img/skill/35121009/action/0 = msummon`, `Skill/3512.img/skill/35121010/action/0 = msummon`, `Skill/3512.img/skill/35121012/action/0 = tank_laser`, and `Skill/3512.img/skill/35121013/action/0 = tank_siegepre`. The one-shot Mechanic WZ surface was also rechecked and is still only WZ-backed, not client-owned: `Skill/3500.img/skill/35001004/action/0 = gatlingshot`, `Skill/3510.img/skill/35101010/action/0 = gatlingshot2`, `Skill/3500.img/skill/35001003/action/0 = drillrush`, `Skill/3510.img/skill/35101003/action/0 = earthslug`, `Skill/3510.img/skill/35101006/action/0 = mbooster`, and `Skill/3511.img/skill/35111015/action/0 = rpunch`, while `Character/00002000.img` still publishes the body-backed prepared and Mechanic transform families consumed by this seam and `Character/TamingMob/01932016.img` still publishes the overlapping one-shot branches. IDA remained the second source and still keeps the boundary where the checked-in code already sits: `CUserLocal::DoActiveSkill_Prepare` still owns the shared prepared-skill split, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingRocketBooster`, and `CUserLocal::TryDoingRocketBoosterEnd` still back the already-wired swallow sustain, cyclone body-attack, and Rocket Booster startup/end ownership, and `CAvatar::MoveAction2RawAction` at `0x45fa30` still only gives explicit Mechanic-mode raw-action ownership for `35111004 -> lasergun`, `35121005` tank movement / Rocket Booster / attack handling, `35121013` tank-siege handling, `35001001 -> tank_rbooster_pre`, and `35101009 -> tank_mRush`. The new checked-in regression file now pins the prepare / hold / exit families, Demon `bluntSmashEnd` / `soulEater_end -> bluntSmash` fallback, Rocket Booster startup and tank-startup ownership, Mechanic summon / rush aliases, `lasergun` and `tank_laser` attack aliases, the currently wired Mechanic movement families, known-owner mounted presentation for `ride2`, `getoff2`, `ladder2`, and `rope2`, `ClientOwnedVehicleSkillClassifier.IsOwnerlessMechanicVehicleInferenceActionName` staying closed, `SkillManager.ResolveClientOwnedVehicleAvatarTransformMountItemId` latching `1932016` only for the transform-owner skill ids evidenced on this row, and the one-shot Mechanic skills staying outside the built-in avatar-transform seam. Remaining gap: the implemented WZ-backed prepare / hold / exit families, currently wired Mechanic movement candidates, confirmed `lasergun`, preserved `tank_laser`, Mechanic summon / rush aliases, and guarded non-promotion of the one-shot Mechanic skills now match checked-in code plus checked-in regression coverage, but `gatlingshot`, `gatlingshot2`, `drillrush`, `earthslug`, `mbooster`, and `rpunch` still need explicit client owner evidence before this row should promote them into the built-in avatar-transform seam, and any broader client-only prepared / transform aliases beyond `lasergun`, `tank_laser`, `tank_msummon`, `tank_msummon2`, and `tank_mRush`, unevidenced extra exit names, or transform movement / transition branches beyond `tank_*`, `siege_*`, `tank_siege*`, `ride2`, `getoff2`, `ladder2`, and `rope2` still need explicit client evidence before they should be added. | Mechanic avatar state skills, Mechanic summon-family avatar transforms, the earlier WZ-authored `msummon` cast skills, direct `tank_laser`, `tank_mRush`, flamethrower, Rocket Booster, Cyclone, `Monkey Wave`, Wild Hunter `Swallow`, Wild Hunter `More Wild`, Evan dragon-breath charges, Dual Blade `Final Cut` / `Monster Bomb`, Battle Mage `Dual Vulcan`, Night Walker `Poison Bomb`, and the confirmed Demon prepared-skill charge families remain anchored in runtime remaps and now also in a checked-in row-specific regression file. The row has concrete source-side coverage for built-in Mechanic state movement candidates, active-owner mounted presentation actions, and the negative boundary that keeps unevidenced one-shot Mechanic skills out of the built-in transform owner and avatar-transform mount-owner paths. The row remains narrowed to still-unconfirmed client-only aliases beyond the currently surfaced families, WZ-authored but client-owner-unconfirmed Mechanic one-shots, additional exit branches, and transform movement / transition branches beyond the surface already wired here. | `SkillManager`, `PlayerCharacter`, `CharacterAssembler`, `CharacterData`, `ClientOwnedVehicleSkillClassifier`, `TamingMobActionFrameOwner`, `UnitTest_MapSimulator/PreparedSkillAvatarTransformParityTests.cs` (`14111006`, `23121000`, `31001000`, `31101000`, `32121003`, `33101005`, `35001001`, `35001003`, `35001004`, `35101003`, `35101004`, `35101005`, `35101006`, `35101009`, `35101010`, `35111002`, `35111004`, `35111005`, `35111011`, `35111015`, `35121003`, `35121005`, `35121009`, `35121010`, `35121012`, `35121013`, `CAvatar::MoveAction2RawAction`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `Skill/1411.img/skill/14111006/prepare/action`, `Skill/2312.img/skill/23121000/{prepare,keydown,keydownend}`, `Skill/3100.img/skill/31001000/keydownend/action`, `Skill/3110.img/skill/31101000/{action/0,keydownend/action}`, `Skill/3212.img/skill/32121003/action/0`, `Skill/3310.img/skill/33101005/prepare/action`, `Skill/3500.img/skill/35001001/{prepare,keydown,keydownend,action}`, `Skill/3500.img/skill/35001003/action/0`, `Skill/3500.img/skill/35001004/action/0`, `Skill/3510.img/skill/35101003/action/0`, `Skill/3510.img/skill/35101004/action/0`, `Skill/3510.img/skill/35101005/action/0`, `Skill/3510.img/skill/35101006/action/0`, `Skill/3510.img/skill/35101009/{prepare,keydown,keydownend}`, `Skill/3510.img/skill/35101010/action/0`, `Skill/3511.img/skill/35111002/action/0`, `Skill/3511.img/skill/35111004/action/0`, `Skill/3511.img/skill/35111005/action/0`, `Skill/3511.img/skill/35111011/action/0`, `Skill/3511.img/skill/35111015/action/0`, `Skill/3512.img/skill/35121003/action/0`, `Skill/3512.img/skill/35121005/action/0`, `Skill/3512.img/skill/35121009/action/0`, `Skill/3512.img/skill/35121010/action/0`, `Skill/3512.img/skill/35121012/action/0`, `Skill/3512.img/skill/35121013/action/0`, `Skill/531.img/skill/5311002/{prepare,keydown,action}`, `Character/00002000.img/{darkTornado_pre,darkTornado,darkTornado_after,bluntSmash,cyclone_pre,cyclone,cyclone_after,noiseWave_pre,noiseWave_ing,noiseWave,dualVulcanPrep,dualVulcanLoop,dualVulcanEnd,ride2,getoff2,ladder2,rope2,lasergun,rbooster_pre,rbooster,rbooster_after,tank_pre,tank,tank_walk,tank_stand,tank_prone,tank_after,tank_laser,siege_pre,siege,siege_stand,siege_after,tank_siegepre,tank_siegeattack,tank_siegestand,tank_siegeafter,tank_rbooster_pre,tank_rbooster_after,msummon,msummon2,tank_msummon,tank_msummon2,tank_mRush,gatlingshot,gatlingshot2,drillrush,earthslug,mbooster,rpunch}`, `Character/TamingMob/01932016.img/{ride2,getoff2,ladder2,rope2,gatlingshot,gatlingshot2,drillrush,earthslug,mbooster,rpunch}`) |
| Partial | Client-managed avatar effect layers | The simulator keeps the WZ-first avatar-effect seams on this row: persistent `special` / `special0` / `finish` / `finish0` layers, `More Wild` ladder overrides, transient movement-owner `effect` / `effect0` promotion including WZ `casterMove = 1` + `type = 41` rush casts such as `Skill/432.img/4321000`, looping buff-owner `effect` / `effect0` fallbacks for flying, Dark Sight, invisible, reflect, broader timed self-buff shapes, Energy Charge full-charge runtime backed by `Skill/511.img/5110001/common/x` and `Skill/1510.img/15100004/common/x`, race novice flying `*1026` repeat ownership, affected-area support ownership, and the currently evidenced rotate-sensitive suppression set confirmed from `CAvatar::IsOnPlayingRotateAction`. WZ was rechecked first in this pass: `Skill/3512.img/skill/35121010/affected/pos = 1` still publishes the Mechanic support aura as face-owned while `affected/0/z = 0`, `Skill/000.img/skill/0001010` in the current export still publishes the same local self-buff owner surface through `affected/pos = 1` together with `invisible = 1`, `Skill/{000,1000,2000}.img/skill/*1026/repeat/pos = 1` still publishes the same face-owned signal for the race novice flying repeat seam, and `Skill/432.img/4321000` still keeps the transient rush-owner surface on the same `effect` / `special` / `stopEffect` family with `info/casterMove = 1` and `info/type = 41`. Client evidence stayed second: `CUser::UpdateAr01Effect` still loads `35121010` affected through `CAvatar::GetLayerUnderFace`, while `CAvatar::IsOnPlayingRotateAction` remains limited to raw one-time actions `101`, `82`, `114`, `149`, `151`, and `193`. This pass closes the next owner gap inside the existing seams instead of widening unevidenced families: the shared loop-plane picker in `SkillManager` now drives not only local buff-owner `affected` / `affected0` promotion and repeat-only fallback resolution, but also the animation-displayer temporary-stat avatar-effect path, so the same face-owned `pos = 1` rules now survive through remote or deferred temp-stat presentation instead of collapsing back onto the front plane. The earlier local linked-support ownership, merged support-level-data resolution, signature-based in-place refresh, authored `pos` preservation, linked support-plane aggregation, local transient plane routing, and `35121010` / `*1026` local under-face behavior remain intact, and focused regression coverage is now checked in under `UnitTest_MapSimulator/ClientOwnedAvatarEffectParityTests.cs` for local `affected` promotion, animation-displayer temporary-stat under-face promotion, and repeat-only fallback resolution. Remaining gap: broader client-only `CUser::Update` effect families beyond the currently evidenced WZ-authored `special*`, timed `effect/effect0`, `affected/affected0`, `repeat/repeat0`, invisible/effect-only buff, movement-action, `casterMove/type=41`, race novice flying `repeat`, and friendly support-area ownership shapes still need explicit client-backed remaps before they can be mirrored safely; the rotate fallback still only covers the confirmed raw-action set plus the synthetic `*1026` repeat owner; and Energy Charge may still need deeper client confirmation if later IDA work changes its hit cadence or extra `bodyAttack` runtime behavior. | Visible parity is closer because the same evidenced face-owned plane rule now survives across the local buff seam, support-area seam, and the animation-displayer temporary-stat seam instead of leaving `affected`-owned or repeat-only avatar effects to regress back to front-plane fallback once they leave the direct local-player path. Local qualifying player-skill areas, projected remote support areas, shared buff/repeat fallback layers, local self-buff `affected` owners, animation-displayer temporary-stat avatar effects, and local movement-owner cleanup branches now keep the earlier WZ-backed linked-support ownership, linked support level data, in-place signature refresh, and dual-plane preservation while rendering `35121010`-style support auras, face-owned self-buff loops such as `0001010`, novice flying `*1026` repeat owners, and matching transient face-owned branches on the face-owned plane even when WZ leaves per-frame `z` at `0`. The row is narrowed to broader unevidenced `CUser::Update` effect families, wider rotate suppression beyond the confirmed raw-action set, and deeper Energy Charge runtime proof. | `PlayerCharacter`, `PlayerManager`, `SkillLoader`, `SkillManager`, `CharacterAssembler`, `CharacterData`, `ClientOwnedAvatarEffectParity`, `AffectedAreaAvatarEffectResolver`, `RemoteUserActorPool`, `MapSimulator.AnimationDisplayerParity`, `UnitTest_MapSimulator/ClientOwnedAvatarEffectParityTests.cs` (`CUser::Update`, `CUser::UpdateMoreWildEffect`, `CUser::UpdateAr01Effect`, `CUserLocal::TryDoingSwallowBuff`, `CUser::LoadSkillRepeatEffect`, `CAvatar::IsOnPlayingRotateAction`, `get_novice_skill_as_race`, `get_action_name_from_code`, `Skill/3211/skill/32110007/info/affectedSkill`, `Skill/3200/skill/32001003/affected`, `Skill/3511.img/35111011/{affected,info/type,info/minionAbility}`, `Skill/3512.img/35121010/{affected,affected/pos,affected/0/z,info/type,info/minionAbility}`, `Skill/432.img/4321000/{effect,special,stopEffect,info/casterMove,info/type}`, `Skill/511.img/5110001/{special,affected,common/x,info/energyCharge,info/fullChargeEffect}`, `Skill/1510.img/15100004/{special,affected,common/x,info/energyCharge,info/fullChargeEffect}`, `Skill/000.img/0001010/{affected,invisible}`, `Skill/000.img/0001026/repeat`, `Skill/1000.img/10001026/repeat`, `Skill/2000.img/20001026/repeat`, `Character/00002000.img/{doubleJump,backspin,rollingSpin,darkSpin,screw,somersault,homing,triplefire,bamboo,lightingBolt,breathe_prepare,chainPull,finalCut}`) |
| Partial | Remote user / `CUserPool` actor parity | `RemoteUserActorPool` remains the shared remote-user owner for AvatarLook decode, enter/leave, movement snapshots, helper/team state, chair/mount state, prepared-skill world overlays, packet `223` avatar refresh, packet-owned `Effect/ItemEff.img` social effects, carry-item and completed-set rendering, shared `225/226` temporary-stat storage, and the packet-owned active-effect item presentation already wired through that owner. This pass stayed in that same seam again and closed the next broad official `224` slice instead of widening feature-local remotes. WZ was rechecked first for the reused remote surfaces that this packet family can legally feed: `Effect/OnUserEff.img/character/0` is still present for the shared animation-displayer generic user-state owner, and `Effect/OnUserEff.img/itemEffect/quest/2430071` still publishes the authored quest-delivery frame set under the same `itemEffect/quest` branch the simulator already reuses locally, so this slice did not need a new simulator-only overlay family. IDA remained the second source: `CUserPool::OnUserRemotePacket` still keeps official `224` on `CUser::OnEffect`, and the current `CUser::OnEffect` decompile still shows subtype `29` decoding an Int32 and routing straight through `CUser::MakeIncDecHPEffect(value, 0)`, subtype `30` decoding an item id and registering the client animation-displayer quest-delivery user-state owner, and subtype `31` clearing that same quest-delivery repeat owner again. The shared simulator router already admitted official `224`; this pass now mirrors those client-owned packet shapes there too. `RemoteUserPacketCodec` now decodes official `224` after the shared remote-user id, validates the current client-backed `29/30/31` trailing payload shapes, and still preserves the subtype id plus raw trailing bytes for the unsupported branches instead of misclassifying the whole packet as a relationship `ItemEff` payload. `RemoteUserActorPool` now applies subtype `29` through the existing shared remote inc/dec HP presentation owner, stores subtype `30` quest-delivery item state on the shared remote actor, and clears that same packet-owned quest-delivery state on subtype `31`. `MapSimulator.RemoteUsers` now resolves official `224` through that same shared owner instead of the older generic item-effect path, and `MapSimulator.AnimationDisplayerParity` now reuses the existing remote quest-delivery registration seam to register or remove the authored `OnUserEff.img/itemEffect/quest/2430071` user-state layer for remotes while also clearing that registration when a remote actor hides or leaves. Focused regression coverage for this slice now lives in `UnitTest_MapSimulator/RemoteUserEffectOfficialParityTests.cs`, which covers direct official `224` decode for `29`, `30`, `31`, unknown-subtype raw-payload preservation, shared-pool quest-delivery state storage and clear, and shared-pool inc/dec HP presentation dispatch. The row still also carries the earlier official `218/219/220/221/222/227/228/229/230` work in the same owner seam. | The remaining gap is narrower but still row-sized: official `224` still does not cover the broader client `CUser::OnEffect` subtype surface beyond the now-wired `29/30/31` branches, `218` still does not mirror the full client mob-authored hit-string / stance-special-effect / exact zero-delta `MakeIncDecHPEffect(0, byte)` branches, and `230` still only preserves grenade packet metadata until the projectile/trajectory owner is rendered. First-seen messenger member-card contacts that never receive packet-owned room-member `AvatarLook` payloads, plus simulator-profile contacts after packet overrides are cleared, still correctly avoid fabricating or preserving a hidden remote but still have no fuller official visual source than the packet-profile metadata already visible in the messenger UI; and remote temp-stat/update parity is still incomplete beyond the currently decoded hidden-like, morph, Mechanic, Shadow Partner, known Aura, Soul Arrow, Blessing Armor, Barrier-family presentation, plus the shared repeat-effect, magic-shield, Final Cut, receive-HP gauge, hit-feedback, and now `224` inc/dec HP plus quest-delivery owner surfaces already wired through the pool. Remaining work on this row is therefore concentrated on the rest of the `224` subtype catalog, the remaining client-only `218` branches, exact client carry-item orbit choreography and asset choice beyond the current shared orbit reuse, remote afterimages beyond the current melee seam, the official `230` grenade projectile presentation beyond metadata preservation, the other feature-local remote shells that still bypass the shared pool after the wedding generic-item-effect and avatar-modified carry/set fixes, and higher-fidelity movement smoothing beyond the current sampled move-path path. | shared field actor/runtime layer (`CUserPool::OnPacket`, `CUserPool::OnUserRemotePacket`, `CUserPool::Update`, `CUserRemote::Update`, `CUserRemote::OnAvatarModified`, `CUserRemote::OnHit`, `CUserRemote::OnSetActiveEffectItem`, `CUserRemote::OnShowUpgradeTombEffect`, `CUserRemote::OnSetActivePortableChair`, `CUserRemote::OnGuildNameChanged`, `CUserRemote::OnGuildMarkChanged`, `CUserRemote::OnReceiveHP`, `CUserRemote::OnThrowGrenade`, `CUser::OnEmotion`, `CUser::OnEffect`, `CUser::ShowUpgradeTombEffect`, `CUser::MakeIncDecHPEffect`, `CUser::ShowSkillSpecialEffect`, `SecondaryStat::DecodeForRemote`, `CUser::SetCarryItemEffect`, `CAnimationDisplayer::Effect_ByItem`, `CUIMessenger::OnPacket`, `CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUser::Update`, `CMovePath::{Decode,OnMovePacket}`, `Etc/EmotionEffect.img`, `Effect/Tomb.img`, `Effect/ItemEff.img`, `Effect/OnUserEff.img`, `Effect/CharacterEff.img/1112916`, `Effect/CharacterEff.img/1112924`, `Effect/CharacterEff.img/1112926`, `Effect/SetItemInfoEff.img`, `UI/GuildMark.img`, `Mob/0100100.img/hit1`, `Morph/*.img`, `Character/TamingMob/01932016.img`, `Character/01112916.img/info/effect/path`, `Character/01112924.img/info/effect/path`, `Character/01112926.img/info/effect/path`, `HaCreator/MapSimulator/Pools/RemoteUserActorPool.cs`, `HaCreator/MapSimulator/Pools/RemoteUserPacketCodec.cs`, `HaCreator/MapSimulator/MapSimulator.RemoteUsers.cs`, `HaCreator/MapSimulator/MapSimulator.AnimationDisplayerParity.cs`, `HaCreator/MapSimulator/Managers/RemoteUserPacketInboxManager.cs`, `HaCreator/MapSimulator/Interaction/MessengerRuntime.cs`, `HaCreator/MapSimulator/Interaction/MessengerRemoteUserSynchronizer.cs`, `HaCreator/MapSimulator/MapSimulator.WeddingRemoteUsers.cs`, `UnitTest_MapSimulator/RemoteUserEffectOfficialParityTests.cs`, `UnitTest_MapSimulator/RemoteUserRelationshipRecordParityTests.cs`, `UnitTest_MapSimulator/WeddingRemoteAvatarPresentationParityTests.cs`) |

### 2. Taming-mob action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Taming-mob action-loader / frame-build parity | The simulator now keeps the dedicated taming-mob action owner at the loader seam and covers the client-backed mount companion branches that feed `CActionMan::LoadTamingMobAction`. `CharacterLoader.LoadTamingMob` still attaches `TamingMobActionFrameOwner` only to true taming-mob parts, the owner still enforces the confirmed vehicle-id action gates before lazy-loading frames, and the loader still admits `1983xxx` and `199xxxx` assets through `Character/TamingMob` so they reach the same owner instead of being dropped as non-equipment. WZ was rechecked first for the affected current assets: ordinary `190xxxx` rides such as `Character/TamingMob/01902000` still publish locomotion plus `tired`; Mechanic `Character/TamingMob/01932016` still publishes mount-only `tank_*`, `siege_*`, `rbooster*`, `msummon*`, `flamethrower*`, `ride2`, `getoff2`, `rope2`, and `ladder2`; Wild Hunter jaguars such as `01932030` and `01932015` still publish jaguar-only attack and utility families; `191xxxx` saddle assets such as `01912033`, `01912040`, `01912041`, `01912042`, `01912052`, and `01912053` still advertise `info/islot = Sd` and keep authored action trees under the owning vehicle id; `199xxxx` taming-mob assets such as `01992004` still advertise `info/islot = Tm` and publish normal ride locomotion at the image root; and `1983xxx` portable-chair ride mounts such as `01983000`, `01983019`, and `01983039` still live in the same folder but only publish `sit`, which is why they must still enter this seam. The current mounted WZ export does not publish concrete `192xxxx` taming-mob companion images, but IDA now resolves the old optional third merge source directly: `is_correct_bodypart` at `0x46f760` and `get_bodypart_from_item` at `0x46fbe0` map `192xxxx` to avatar hair/equip bodypart `20`, matching the `b[20]` branch in `CActionMan::LoadTamingMobAction` at `0x427a10`. The simulator now represents that branch as `EquipSlot.TamingMobAccessory`, maps avatar-look bodypart `20` and item family `192xxxx` to it, loads it from the existing `Character/TamingMob` folder path, attaches the same taming-mob overlay resolver used by `191xxxx` sources, and merges overlays in client branch order: `b[19]` saddle first, then `b[20]` taming-mob accessory, then any later resolver-backed companion overlays. Focused regression coverage for this seam now lives in `UnitTest_MapSimulator/TamingMobLoaderParityTests.cs`, pinning `192xxxx <-> bodypart 20` codec behavior and the saddle-before-`192` overlay ordering. | Mounted-avatar parity for this loader row is now anchored to the explicit client loader/cache seam: the simulator owns alias fallback, tired remap selection, confirmed vehicle-id gates, `193`-family one-shot exceptions, `1983xxx` action-code-48 remap, non-`190` loader admission, and all confirmed `190xxxx` companion merge branches in one loader path. The previously unresolved `b[20]` source is now identified as the client `192xxxx` bodypart-20 taming-mob companion family and wired into the same merge seam as the known `191xxxx` saddle branch. | mounted action-loader layer (`CActionMan::LoadTamingMobAction`, `is_correct_bodypart`, `get_bodypart_from_item`, `IsAbleTamingMobAction`, `IsAbleTamingMobOneTimeAction`, `HaCreator/MapSimulator/Character/TamingMobActionFrameOwner.cs`, `HaCreator/MapSimulator/Character/CharacterLoader.cs`, `HaCreator/MapSimulator/Character/CharacterData.cs`, `HaCreator/MapSimulator/Managers/LoginAvatarLookCodec.cs`, `UnitTest_MapSimulator/TamingMobLoaderParityTests.cs`, `Character/TamingMob/01902000`, `Character/TamingMob/01912033`, `Character/TamingMob/01912040`, `Character/TamingMob/01912041`, `Character/TamingMob/01912042`, `Character/TamingMob/01912052`, `Character/TamingMob/01912053`, `Character/TamingMob/01932016`, `Character/TamingMob/01932030`, `Character/TamingMob/01932015`, `Character/TamingMob/01983000`, `Character/TamingMob/01983019`, `Character/TamingMob/01983039`, `Character/TamingMob/01992004`) |

### 3. Character image-entry ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Character image-entry / equip-metadata parity | `CharacterLoader` still owns the explicit `GetCharacterImgEntry`-style seam instead of treating equip IMG loading as a flat image fetch. WZ was rechecked first for this row against the same weekly-capable samples `Character/Cap/01002785` and `Character/Weapon/01702191`: both still publish the client-owned base `info/weekly = 1` flag on the base `info` node and still expose weekly override trees through `weekly/0..5` without replacing the root IMG. The runtime-consumer surface was also widened again from WZ first before touching code: `Character/Weapon/01302000`, `01312000`, `01322000`, and `01332000` still publish `info/attack = 1` with one-handed `swingO*` / `stabO*`; `Character/Weapon/01402000`, `01412000`, and `01422000` still publish `info/attack = 5` with two-handed `swingT*`; `Character/Weapon/01432000` and `01442000` still publish `info/attack = 2` while separating spear-side `stabT*` from polearm-side `swingP*`; `Character/Weapon/01372000` and `01382000` still publish `info/attack = 6` with the mixed wand/staff one-handed plus `shoot1` surface; `Character/Weapon/01472000` still publishes claw-side `info/attack = 7`; `Character/Weapon/01482000` still publishes knuckle-side `info/attack = 8`; `Character/Weapon/01452005`, `01462000`, and `01492000` still keep the already-wired ranged targets concrete as `attack = 3` / `shoot1`, `attack = 4` / `shoot2`, and `attack = 9` / `shoot1`; and the weapon-folder subweapon sample `Character/Weapon/01342000` still keeps the shield-lane override seam concrete through `info/islot = Si`, `info/vslot = Si`, `info/sfx = swordB`, `info/attack = 10`, and `info/attackSpeed = 3`. IDA stayed second: `CActionMan::GetCharacterImgEntry` at `0x417fd0` still reads base `info/islot`, `info/vslot`, `info/sfx`, the weekly flag, and weapon-side `afterImage`, `walk`, `stand`, `attack`, and `attackSpeed` metadata before swapping the effective action tree onto `get_weekly_data_path`; `get_weekly_data_path` at `0x5a65f0` plus `CDayOfWeek::GetCurrentDay` at `0x4f1b90` still show that only client day values `1..6` resolve to weekly branch indices `0..5`, so Sunday stays on the base image entry; and `CAvatar::NotifyAvatarModified` at `0x46bb20` plus `get_weapon_type` at `0x46f660` and `CAvatar::MoveAction2RawAction` at `0x45fa30` still confirm the runtime consumer split by copying `nWalk`, `nStand`, and base-weapon attack speed from the character image entry into the avatar, resolving walk metadata `1` to `walk1`, non-`1` to `walk2`, stand metadata `1` to `stand1`, non-`1` to `stand2`, and then letting weapon-typed subweapons override the cached attack-action type while raising effective attack speed through the client max rule. The simulator now mirrors more of that owner split inside `CharacterLoader` and the local runtime: it still reads `islot`, `vslot`, `sfx`, `weekly`, `afterImage`, `walk`, `stand`, `attack`, and `attackSpeed` from the base image metadata, still resolves the action source root through the current Monday-through-Saturday weekly branch when present, still falls back to the base image on Sunday or when the authored weekly branch is missing, and still keeps weapon-folder category-`134` equips on the `Shield` lane instead of collapsing them onto the main weapon slot. Runtime consumers now also use the client-style effective attack owner rather than only the base weapon: subweapon `info/sfx` still wins through `CharacterBuild.GetEffectiveWeaponSfx()`, local attack cooldowns still use the same weapon-vs-subweapon max-attack-speed rule that `NotifyAvatarModified` applies when the shield lane is itself weapon-typed, and basic player attack fallbacks now resolve through `CharacterBuild.GetEffectiveAttackActionWeapon()` so weapon-typed shield-lane equips can override the cached attack-action metadata just like the client path does. `WeaponPart.ResolveClientBasicAttackActionName(...)` also now keeps the WZ-backed non-ranged attack families local to the image-entry metadata seam instead of falling back immediately to generic swing/shoot actions: one-handed `attack = 1`, spear/polearm `attack = 2`, two-handed swing `attack = 5`, wand/staff `attack = 6`, claw `attack = 7`, knuckle `attack = 8`, gun `attack = 9`, and the current katara subweapon `attack = 10` sample all now drive ordered local candidate sets constrained to the authored action roots they actually publish before generic fallback is used. Focused regression coverage for the landed runtime consumers now lives in `UnitTest_MapSimulator/CharacterImageEntryParityTests.cs`. The row remains partial because the simulator still does not have the full client-wide `nAttackActionType` table pinned one-for-one for every remaining attack code, the exact client ordering for some mixed families beyond the current WZ-backed samples still needs more client evidence, and the exact client owner/timing for generic `info/sfx` playback outside the now-wired effective weapon callback is still not fully pinned. | Without this row, avatar parity still overstates how much of the client ownership stack is already named once `LoadCharacterAction` is covered. The client decides which normalized character property tree exists, which slot and sound metadata it exposes, whether a weekly override should replace that tree before the later action-loader and assembler paths run, and which cached weapon metadata drives avatar action selection. With the loader already switching weekly-capable equips onto the same day-indexed weekly branch shape the client uses, falling back to the base image entry on Sunday or missing weekly data, keeping weapon-folder subweapons on the shield lane, and the runtime now consuming the weapon walk/stand metadata, WZ-backed ranged and non-ranged attack-family metadata, subweapon `sfx`, the client-confirmed max attack-speed rule, and the client-confirmed weapon-typed subweapon attack-owner override, the remaining difference is now concentrated in the last unconfirmed attack-type table rows and the final missing proof for the exact global `sfx` playback owner rather than in image-entry selection itself. | character image-entry layer (`CActionMan::GetCharacterImgEntry`, `get_weekly_data_path`, `CDayOfWeek::GetCurrentDay`, `get_weapon_type`, `CAvatar::NotifyAvatarModified`, `CAvatar::MoveAction2RawAction`, simulator seams: `HaCreator/MapSimulator/Character/CharacterLoader.cs`, `HaCreator/MapSimulator/Character/CharacterData.cs`, `HaCreator/MapSimulator/Character/CharacterAssembler.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Character/PlayerManager.cs`, `UnitTest_MapSimulator/CharacterImageEntryParityTests.cs`, `Character/Cap/01002785`, `Character/Weapon/01302000`, `Character/Weapon/01342000`, `Character/Weapon/01372000`, `Character/Weapon/01382000`, `Character/Weapon/01402000`, `Character/Weapon/01432000`, `Character/Weapon/01442000`, `Character/Weapon/01452005`, `Character/Weapon/01462000`, `Character/Weapon/01472000`, `Character/Weapon/01482000`, `Character/Weapon/01492000`, `Character/Weapon/01702191`) |

### 4. Character action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Character action-loader / merged frame-build parity | `CharacterLoader.PrepareActionMergeInput` now covers the full loader-owned merge sanitation confirmed by the targeted client pass. In addition to the previously landed fallback-top / fallback-bottom synthesis, blocked-id stripping, mounted-family shield and weapon suppression, mounted action remaps, ghost collapse, and sticker clears, the simulator now also zeros the always-suppressed face-accessory lane that the client hard-codes as `b.a[2] = 0`. A follow-up client pass through `CCSWnd_Char::InitRing` also resolves the three anonymous global body-part indices touched by `CActionMan::LoadCharacterAction`: they are the remaining ring lanes, so the simulator窶冱 ring suppression table now covers the same client-owned group. The `arrowEruption` special-case lane filter is also pinned exactly to the client array layout from the targeted scan: action `47` preserves only avatar indices `0`, `1`, `3`, and `4`, which in simulator terms means hair plus `Cap`, `EyeAccessory`, and `Earrings`, with hair already owned outside the equipment dictionary. | Avatar parity now includes the full dedicated loader-side ownership bucket beneath `CUser::*`: the simulator decides the same sanitized equip set that the client allows into `load_character_action` and `MergeCharacterSprite`, so future avatar work for this area can stay in higher-level runtime or asset seams instead of reopening the frame-builder ownership path. | character action-loader layer (`CActionMan::LoadCharacterAction`, `CCSWnd_Char::InitRing`, lower `load_character_action`, `MergeCharacterSprite`, simulator seams: `HaCreator/MapSimulator/Character/CharacterLoader.cs`, `HaCreator/MapSimulator/Character/CharacterAssembler.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`) |

### 5. Face-look loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Face-look loader / emotion-canvas composition parity | The simulator now keeps the client-owned face-look build and composition step inside the explicit loader seam instead of leaving it implicit in runtime expression state. `FacePart` still delegates face-expression assembly to `FaceLookLoader`, and that loader now keeps the cache keyed by skin, expression name, and the concrete face-accessory item id; keeps the authored total duration on the cached entry; resolves accessory expression planes through the accessory part's lazy animation resolver instead of only whatever generic equip animations happened to be preloaded; and now owns the accessory emotion remap before any generic accessory fallback can run. WZ was rechecked first for this row: `Character/Face/00020000` still exposes authored face-expression branches with per-frame or branch `delay`, `origin`, `map/brow`, and `z = face`, while `Character/Accessory/01012000` still exposes face-accessory emotion branches through `default/*` canvases and UOL redirects with authored `origin`, `map/brow`, and `z = accessoryFace` metadata, but still omits the face-only `qBlue` branch that remains present under `Character/Face/00020000`. IDA remained the second source: `CActionMan::LoadFaceLook` at `0x41cab0` still caches by skin, face, emotion, and accessory state; still fetches the accessory IMG entry directly from `nAcc`; still remaps the last emotion slot back to emotion `0` before loading the accessory branch; and the targeted caller trace through `CAvatar::PrepareFaceLayer` at `0x4647d0` now closes the old accessory-state uncertainty by showing that `nAcc` is passed straight from `m_avatarLook.anHairEquip[2]` or its forced override, i.e. the equipped face-accessory item id itself for this seam. With that caller evidence in hand, the loader-owned cache key staying at item-id granularity now matches the client path instead of underspecifying it. Each cached `FaceLookFrame` still builds a loader-owned `CompositeFrame` that aligns face and accessory planes around the shared brow anchor, unions their authored bounds, preserves translated map-point metadata, and raster-composites the face and accessory textures in client z-order before caching, and the loader now also explicitly routes accessory-side `qBlue` requests to the authored `default` accessory plane instead of relying on generic fallback order to arrive there incidentally. `CharacterAssembler` still consumes that composed face-look frame first and skips emitting a separate `Face_Accessory` render part whenever the loader produced a real composite texture, so the final face-expression render path still matches the client-owned `LoadFaceLook` ownership split instead of depending on over-face runtime ordering. Focused regression coverage for this row now lives in `UnitTest_MapSimulator/FaceLookLoaderParityTests.cs` for both the loader-owned accessory `qBlue -> default` remap and the confirmed item-id-only accessory cache key. | Facial-expression parity for this loader row is now owned in the same place and at the same granularity as the client: the face-look loader decides which face and accessory emotion planes exist, composes them around the shared brow anchor, preserves authored duration and metadata, applies the confirmed accessory-side `qBlue -> default` rule before fallback, and keys the cache by the same effective accessory input the client passes into `LoadFaceLook`. The remaining work on adjacent avatar rows is no longer this seam's loader ownership, composition order, or accessory-state keying. | face-look loader layer (`CActionMan::LoadFaceLook`, `CAvatar::PrepareFaceLayer`, simulator seams: `HaCreator/MapSimulator/Character/FaceLookLoader.cs`, `HaCreator/MapSimulator/Character/CharacterData.cs`, `HaCreator/MapSimulator/Character/CharacterAssembler.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `UnitTest_MapSimulator/FaceLookLoaderParityTests.cs`, `Character/Face/00020000`, `Character/Accessory/01012000`) |

### 6. Morph action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Morph image/action-loader parity | The simulator still keeps the client-owned morph loader in the existing loader and assembler seams instead of leaking it into generic runtime fallback. WZ was rechecked first for the current row against the mounted IMG export: `Morph/1003.img` still publishes per-frame authored `delay`, `lt`, `rb`, and `head` metadata on its movement and combat branches, and its loader-owned UOL surfaces still resolve through the action tree such as `fly2/0 -> ../fly/3`, `fly2Move/0 -> ../fly/4`, and `fly2Skill/0 -> ../alert2/0`. `1003.img` still publishes `archerDoubleJump`, `windshot`, `windspear`, `stormbreak`, `arrowRain`, `shoot1`, and `shootF`, but still does not publish verbatim `shoot6`, `paralyze`, `shotC1`, `shootDb1`, `jShot`, `speedDualShot`, `piercing`, `crossPiercing`, or `arrowEruption` branches. `Morph/2002.img` still publishes `iceAttack1`, `iceAttack2`, `iceSmash`, `iceTempest`, `iceChop`, `icePanic`, and `iceDoubleJump`, and fresh name scans still return no verbatim `iceStrike`, `magic1`, `magic2`, `magic3`, `magic4`, `magic5`, `magic6`, `explosion`, `meteor`, `blizzard`, `genesis`, or `chainlightning` branches there; `Morph/2001.img` still publishes the older authored `icemanAttack` plus `icemandoubleJump` and still publishes none of those newer spell-family raw names either. `Morph/1001.img` still publishes the pirate-family authored combat roots `doublefire` and `eburster`, and fresh name scans against that mounted export still return no verbatim `burster1`, `burster2`, `triplefire`, `handgun`, `airstrike`, `shot`, `fireburner`, `coolingeffect`, `homing`, `rapidfire`, `cannon`, `torpedo`, `cannonJump`, `flamesplash`, `swiftShot`, `cannonSmash`, `giganticBackstep`, `rushBoom`, `cannonSlam`, `counterCannon`, `cannonSpike`, `superCannon`, `magneticCannon`, `bombExplosion`, `monkeyBoomboom`, `piratebless`, `pirateSpirit`, `cannonBooster`, `noiseWave`, `noiseWave_pre`, or `noiseWave_ing` branches there, so those client raw pirate and cannon-family surfaces still have to collapse onto authored morph branches inside the loader seam. `Skill/000.img/skill/0001105/{common,PVPcommon}/morph` still carries the concrete string morph template `2002`; `Skill/512.img/skill/5121003/{common,PVPcommon}/morph` still carries the concrete string morph template `1001`; the current spell-family skills still keep their non-morph raw action strings in WZ such as `Skill/221.img/skill/2211003/action/0 = magic1` and `Skill/212.img/skill/2121007/action/0 = meteor`; and the current flag-only outliers `Skill/001.img/skill/0010109/info/morph = 1` and `Skill/2002.img/skill/20020111/info/morph = 1` still remain flag-only in WZ, so the earlier suffix-based candidate reduction is still the active owner-side remap until new evidence says otherwise. The current `Skill/2002.img/skill/20020111` outlier also still publishes `action/0 = fastest`, while suffix-resolved `Morph/0111.img` still publishes only movement branches (`walk`, `stand`, `prone`, `fly`, and `jump`) and no verbatim `fastest` action branch. Client evidence remained the second source: `CActionMan::GetMorphImgEntry` at `0x418b30` still owns `Morph/<id>.img` lookup plus `info/link` inheritance, `CActionMan::LoadMorphAction` at `0x4193c0` still caches by `(templateId, action)`, reads authored frame metadata, stores the frame-uol string alongside each cached morph action frame, and exposes the replay-tail flag through string-pool id `0x049F` (`zigzag`), while the recovered client raw action table still publishes the same unresolved surfaces this loader has to own, including `paralyze` (`42`), `shoot6` (`45`), `arrowEruption` (`47`), the spell-family names from `magic1` (`48`) through `magic6` (`53`), `explosion` (`54`), `iceStrike` (`55`), `burster1` (`56`), `burster2` (`57`), `meteor` (`66`), `blizzard` (`67`), `genesis` (`68`), `chainlightning` (`72`), the pirate gun-family roots from `handgun` (`79`) through `torpedo` (`112`), `shotC1` (`319`), `cannonJump` (`325`), the cannon-family requests from `flamesplash` (`320`) through `cannonBooster` (`338`), including `piratebless` (`323`), `pirateSpirit` (`327`), `noiseWave_pre` (`330`), `noiseWave_ing` (`331`), and `noiseWave` (`332`), plus `shootDb1` (`342`), `jShot` (`344`), `speedDualShot` (`349`), and `fastest` (`353`). The earlier loader-side candidate walk and cache ownership remain in place, and this pass narrowed another action-name gap inside the same owner seam instead of widening runtime code: `MorphClientActionResolver` still keeps the published double-jump precedence for this row (`archerDoubleJump`, then `iceDoubleJump`, then `icemandoubleJump`, then any other published `*DoubleJump` surface), still keeps `cannonJump` on the published generic `jump` branch instead of promoting archer-only double-jump surfaces, still keeps `paralyze`, `shoot6`, `shotC1`, `shootDb1`, `jShot`, and `speedDualShot` on the generic published `shoot*` surface before trying authored archer-only combat aliases, still keeps `rain` and `arrowEruption` on the published `arrowRain` branch, still keeps the current client raw piercing-family requests (`piercing`, `crossPiercing`) on the published `windspear` morph branch, still keeps the broader confirmed pirate gun-family, cannon-family, and cannon-adjacent buff/voice raw requests on the published `doublefire` morph branch instead of letting `Morph/1001.img` fall through to unrelated pirate melee aliases, still keeps the raw pirate `burster1` / `burster2` requests on the authored `eburster` branch, still keeps the current flag-only `fastest` morph request on the suffix-resolved movement surface by preferring published `fly`, then `jump`, then `stand`, and now also keeps the older raw magician spell-family requests on the current published ice-family morph surface by preferring `iceAttack1` on `Morph/2002.img` and falling back to `icemanAttack` when only `Morph/2001.img` exposes that family. Focused regression coverage for this row lives in `UnitTest_MapSimulator/MorphActionResolverParityTests.cs`, which now pins `piercing -> windspear`, `crossPiercing -> windspear`, `shoot6 -> shoot1`, `paralyze -> shoot1`, `shotC1 -> shoot1`, `shootDb1 -> shoot1`, `jShot -> shoot1`, `speedDualShot -> shoot1`, `arrowEruption -> arrowRain`, the pirate gun-family, cannon-family, and cannon-adjacent buff/voice collapse onto `doublefire`, `cannonJump -> jump` without promoting `archerDoubleJump`, `burster1` / `burster2 -> eburster`, the `shot -> windshot` archer-only preservation when no pirate gun branch exists, the generic-shot fallback to `windshot` when no published `shoot*` surface exists, the exact-published-ice-over-legacy ordering, the legacy-ice fallback, the new `magic1`-through-`magic6`, `explosion`, `meteor`, `blizzard`, `genesis`, `chainlightning`, and `iceStrike` preference for `iceAttack1` with legacy fallback to `icemanAttack`, `fastest -> fly -> jump -> stand` for the current movement-only flag morph, and the published double-jump precedence including `icemandoubleJump`. The remaining gap is narrower but still row-sized: the morph action-name surface is still not a complete confirmed dump of the client `s_sMorphAction` table, so morph-only names that remain outside the confirmed combat map after the current ice-family, spell-family, archer ranged and piercing, generic-ranged `shoot6`, `paralyze`, `shotC1`, `shootDb1`, `jShot`, and `speedDualShot`, pirate gun-family, cannon-family, cannon-adjacent buff/voice requests, `cannonJump`, current flag-only `fastest`, and pirate `burster*` alias seams, plus any future flag-only remap or client-only merge rule beyond the already-implemented candidate walk plus `info/link` inheritance, still need fresh evidence before widening this owner again. | Morph parity for this loader row stays anchored to the same explicit owner split the client uses: requested template resolution, paired and same-family backfill, linked-template inheritance, per-template action caching, UOL-backed frame identity, authored morph frame metadata, the confirmed `zigzag` replay-tail owner, and the more client-backed action-name fallback order all stay in the morph image/action loader path instead of bleeding into generic runtime fallback code. With this pass, the older WZ-only `Morph/2001.img` ice surface still resolves the newer raw ice-family names inside the loader seam, the spell-family raw requests now also stay on the published ice-family morph surface instead of missing that owner entirely, the explicit double-jump precedence still covers the older `icemandoubleJump` branch, `cannonJump` stays on the generic morph `jump` surface rather than borrowing unrelated published jump-special aliases, the current flag-only `fastest` outlier stays on the suffix-resolved morph movement surface instead of waiting for generic runtime fallback, archer-only morphs keep the current raw ranged, shot-family, and piercing-family requests on published `shoot*`, `arrowRain`, and `windspear` branches instead of degrading onto unrelated authored aliases, and the client raw pirate gun-family, cannon-family, cannon-adjacent buff/voice, and `burster*` requests still stay on the authored `doublefire` and `eburster` branches rather than wandering onto unrelated pirate combat aliases or generic runtime fallback. The remaining differences are concentrated in still-unconfirmed morph-only names outside the recovered client combat map and in any future non-WZ remap or merge rule beyond the currently evidenced candidate-chain plus `info/link` ownership. | morph action-loader layer (`CActionMan::GetMorphImgEntry`, `CActionMan::LoadMorphAction`, simulator seams: `HaCreator/MapSimulator/Character/MorphActionFrameOwner.cs`, `HaCreator/MapSimulator/Character/MorphClientActionResolver.cs`, `HaCreator/MapSimulator/Character/CharacterLoader.cs`, `HaCreator/MapSimulator/Character/CharacterAssembler.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Character/Skills/SkillLoader.cs`, `UnitTest_MapSimulator/MorphActionResolverParityTests.cs`, `Morph/1001.img`, `Morph/1003.img`, `Morph/2001.img`, `Morph/2002.img`, `Skill/000.img/skill/0001105/{common,PVPcommon}/morph`, `Skill/221.img/skill/2211003/action/0`, `Skill/212.img/skill/2121007/action/0`, `Skill/512.img/skill/5121003/{common,PVPcommon}/morph`, `Skill/001.img/skill/0010109/info/morph`, `Skill/2002.img/skill/20020111/{info/morph,action/0}`, `Skill/530.img/skill/5301002/action/0`, `Skill/530.img/skill/5301003/action/0`, `Skill/531.img/skill/5311002/{prepare/action,keydown/action,action/0}`, `Skill/532.img/skill/5321010/action/0`) |

### 7. Avatar action-layer coordinator ownership discovered by follow-up IDA function scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Avatar action-layer coordinator / mounted origin parity | The existing avatar backlog already names the high-level `CUser::*` state seams and the `CActionMan` loaders, but the follow-up IDA scan shows the client has another runtime owner between those layers that is still not modeled as a first-class parity target. `CAvatar::PrepareActionLayer` resolves the active raw action from move action plus one-time action, guards invalid raw-action ranges separately for morph and non-morph avatars, applies Mechanic tank one-time action rewrites, chooses morph versus character/taming-mob loader paths, calls `PrepareCharacterActionLayer` and `PrepareTamingMobActionLayer` when mounted, clamps walk-action timing from walk speed, clamps non-walk timing from action speed, applies held shoot-action infinite frame delays, and inserts the chosen frame canvases into the client GR2 layer stack. `CAvatar::FixCharacterPosition` then uses the current character frame and taming-mob frame to reconcile body, face, muzzle, taming-mob navel, taming-mob head, and taming-mob muzzle origins with flip-aware signs. The simulator has adjacent pieces in `CharacterAssembler`, `PlayerCharacter`, `TamingMobActionFrameOwner`, and the morph loader, but this specific coordinator is not yet represented as one owner with the same frame-delay and origin-relocation responsibilities. | This prevents avatar parity from being overstated once loader rows are implemented. The client does not stop at building frame tables; it also owns a lower action-layer coordinator that decides how morph, character, and taming-mob outputs become live layers, how mounted anchors relocate the body, and how action-speed or walk-speed timing reaches the frame clock. Future mounted, morph, and one-time-action fixes should decide whether they belong in this coordinator row before widening lower loaders or high-level `CUser` state rows. | avatar runtime action-layer coordinator (`CAvatar::PrepareActionLayer`, `CAvatar::PrepareCharacterActionLayer`, `CAvatar::PrepareTamingMobActionLayer`, `CAvatar::FixCharacterPosition`, `CAvatar::CharacterFrameUpdate`, `CAvatar::TamingMobFrameUpdate`, `CAvatar::SetMorphed`, `CAvatar::SetRidingVehicle`, `CActionMan::LoadCharacterAction`, `CActionMan::LoadTamingMobAction`, `CActionMan::LoadMorphAction`; simulator contrast: `HaCreator/MapSimulator/Character/CharacterAssembler.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Character/TamingMobActionFrameOwner.cs`, `HaCreator/MapSimulator/Character/MorphActionFrameOwner.cs`) |


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Remote actor foundation pass:
   Add a real `CUserPool`-style remote-user actor layer before continuing to extend wedding, Ariant, Battlefield, minimap-helper, and follow-request overlays independently.
3. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
4. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
5. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
6. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".

