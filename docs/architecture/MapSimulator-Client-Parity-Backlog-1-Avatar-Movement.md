# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CAnimationDisplayer::Effect_ByItem` at `0x44f330`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Avatar and player parity

- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::SetActivePet` at `0x8e40e0`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CAnimationDisplayer::Effect_ByItem` at `0x44f330`
- `CActionMan::LoadTamingMobAction` at `0x427a10`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::Update` at `0x937330`

Notes:
`CUser::LoadLayer` and `CUser::PrepareActionLayer` are the clearest resolved avatar-composition seams for layer loading and action-layer setup. `CUser::Update` and `CUser::UpdateMoreWildEffect` also show that the client maintains several additional avatar/effect layers outside the base assembler path, while `CUserLocal::Update` contains the local-player emotion reset path and action/state transitions that matter for expression and rare-action parity. A later IDA pass also confirms that remote avatar ownership is a first-class client surface rather than a loose extension of local-player logic: `CUserPool::OnPacket` dispatches enter, leave, common, remote, and local user packet families, `CUserPool::Update` advances local plus remote users and shared social-effect state, `CUserRemote::Update` owns remote move-action, effect, portable-chair, and prepared-skill presentation, and `CAnimationDisplayer::Effect_ByItem` confirms that one-shot item effects are managed by a dedicated client animation-owner seam rather than only by feature-local overlay code.

### 2. Packet-owned avatar emotion parity discovered by direct `CUserLocal::OnPacket` scan

- `CUser::OnEmotion` at `0x8e0150`
- `CUser::OnRandomEmotion` at `0x8e34b0`

Notes:
The latest direct `CUserLocal::OnPacket` re-check exposed another local-avatar owner that the backlog was not naming anywhere else. `CUser::OnEmotion` decodes an emotion id, duration, and the `m_bEmotionByItemOption` flag before calling `CAvatar::SetEmotion`, while `CUser::OnRandomEmotion` decodes an area-buff item id, resolves `CItemInfo::GetAreaBuffItem`, chooses one weighted emotion slot from the authored prop table, and then routes back through the ordinary emotion-change path via `CWvsContext::SendEmotionChange`. That means client expression parity is not only the passive blink/hit reset surface already tracked in `CUserLocal::Update`; it also includes a packet-owned timed-emotion seam and an item-backed random-emotion owner that should stay explicit in the avatar backlog instead of being hidden under generic item-effect or chat feedback rows.

### 3. Taming-mob action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadTamingMobAction` at `0x427a10`

Notes:
The mount row already cited `LoadTamingMobAction` as evidence, but the owner itself was still buried inside a broader render/state row unlike the explicit loader sections already tracked for summons, mobs, pets, employees, and NPCs. IDA confirms this is a distinct client seam: the loader builds taming-mob action frames only after a vehicle id is already known, and the nearby `IsAbleTamingMobAction` / `IsAbleTamingMobOneTimeAction` gates keep the extra Mechanic and ride coverage keyed to vehicle ownership rather than raw action-name inference alone. That means mounted avatar parity is still split the same way as the other actor families in this backlog set: one row for runtime state/ownership and another for the loader that decides which taming-mob action data the runtime can actually consume.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Avatar and player parity

These are the biggest remaining gaps for visible client parity.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Default avatar selection | The simulator keeps the deterministic, WZ-verified SuperGM/Wizet fallback selection of `1002140` (Wizet Invincible Hat), `1042003` (Wizet Plain Suit), `1062007` (Wizet Plain Suit Pants), `1072005` (Leather Sandals), `1092008` (Pan Lid), and `1322013` (Wizet Secret Agent Suitcase) with matching face, hair, level, and SuperGM job metadata, and the no-packet avatar seam is now shared all the way through preset-backed defaults as well. WZ was rechecked again for this row in `Etc/MakeCharInfo.img/000/{male,female}` and still pins the non-sale starter request layout to the client-owned `0..7` face/hair/skin/hairColor/coat/pants/shoes/weapon families (`male/0 = {20000,20001,20002}`, `male/1 = {30030,30020,30000}`, `male/2 = {0,7,3,2}`, `male/3 = {0,1,2,3}`, `male/4 = {1040002,1040006,1040010}`, `male/5 = {1060002,1060006}`, `male/6 = {1072001,1072005,1072037,1072038}`, `male/7 = {1302000,1322005,1312004}`, `female/0 = {21000,21001,21002}`, `female/1 = {31000,31040,31050}`, `female/2 = {0,7,3,2}`, `female/3 = {0,1,2,3}`, `female/4 = {1041002,1041006,1041010,1041011}`, `female/5 = {1061002,1061008}`, `female/6 = {1072001,1072005,1072037,1072038}`, `female/7 = {1302000,1322005,1312004}`). `CharacterPreset` now owns a synthesized `LoginAvatarLook`, build-backed preset capture preserves hidden equip lists, pet ids, and packet-owned `weaponStickerID` visuals through that owner, `CharacterPreset.ToBuild` rebuilds through `CharacterLoader.LoadFromAvatarLook` whenever the synthesized owner is present, and `CharacterConfigManager.CreateDefaultMalePreset` / `CreateDefaultFemalePreset` now create the default preset from the same synthesized `LoginAvatarLook` source used by roster previews and packet emission instead of re-materializing only scalar selection metadata. Legacy preset files without a serialized `AvatarLook` still load through the existing scalar fallback for compatibility, but the default-avatar parity gap for new simulator-owned presets is closed. | Default avatars, synthesized login previews, packet-authored avatars, and preset-driven defaults now converge on the same render-safe avatar owner, so local default tooling no longer risks silently dropping hidden equips, pets, or weapon stickers just because no packet bytes existed. Focus for later avatar work can stay on other backlog rows instead of this ownership split. | `CharacterLoader.LoadDefaultMale`, `CharacterLoader.LoadDefaultFemale`, `CharacterLoader.LoadFromAvatarLook`, `LoginAvatarLookCodec`, `LoginCharacterRosterEntry.CreateRuntimeBuild`, `MapSimulator.ResolveLoginRosterAvatarLook`, `MapSimulator.CreateLoginPacketCharacterEntry`, `MapSimulator.CreateLoginCharacterAccountEntryState`, `CharacterPreset.FromBuild`, `CharacterPreset.ToBuild`, `CharacterConfigManager.CreateDefaultMalePreset`, `CharacterConfigManager.CreateDefaultFemalePreset`, `UnitTest_MapSimulator/DefaultAvatarSelectionPresetParityTests.cs` (`CUser::LoadLayer`, `CUIAvatar::CUIAvatar`, `GW_CharacterStat::Decode`, `AvatarLook::Decode`, `Etc/MakeCharInfo.img/000/{male,female}`) |
| Implemented | Action coverage | Character-part loading now preserves the existing core action order and appends additional WZ action nodes discovered on each part instead of stopping at the old fixed lists | Uncommon body/equipment poses can now load when the source IMG exposes them, which closes the loader-side empty-state gap for rare, scripted, and death-adjacent actions | `CharacterLoader.StandardActions`, `CharacterLoader.AttackActions` (`CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUserLocal::SetMoveAction`) |
| Implemented | Death / rare action frames | Runtime action selection now carries raw one-shot action names through to `CharacterAssembler` instead of forcing uncommon actions back through the limited enum-only render path | Loaded death and other rare WZ actions can now survive from selection to final frame assembly instead of degrading immediately to generic fallbacks | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler` |
| Implemented | Facial expression behavior | The local player runtime now owns blink cadence and short-lived hit expressions, and updates the assembler expression state when those transitions occur | The avatar now reacts over time instead of staying pinned to the static default face | `CharacterAssembler.GetFaceFrame`, `CharacterLoader.LoadFaceExpressions`, `PlayerCharacter` (`CUserLocal::Update` emotion reset path) |
| Implemented | Equip anchor fallback rules | Equipment alignment now resolves through explicit shared map-point precedence instead of using literal pixel nudges when preferred anchors are missing | Parts stay render-safe while remaining tied to WZ anchor data rather than ad hoc offsets | `CharacterAssembler.CalculateEquipOffset`, `CharacterAssembler.TryCalculateHeadEquipOffset` |
| Implemented | Client-verified fallback order | Action lookup now prefers exact action names, then raw-action family reductions, then swim/fly aliasing, before the final `stand1` escape hatch | Character assembly now follows a documented action-family precedence order instead of jumping straight from miss to generic standing frames | `CharacterAssembler`, `CharacterPart.FindAnimation` (`CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`) |
| Partial | Packet-owned avatar emotion parity | The local avatar still owns a dedicated packet-driven emotion seam instead of relying only on passive blink and hit-face behavior, but the random-emotion leg now follows the client more closely. IDA-backed packet routing remains wired into the simulator utility surface with the real `CUserLocal::OnPacket` opcodes for `CUser::OnEmotion` (`232`) and `CUser::OnRandomEmotion` (`258`), `PlayerCharacter` still keeps the timed packet-owned face-expression state with the decoded duration and item-option bit, and the runtime still routes that state back through the existing assembler face-owner so packet-authored expressions override blink until the timed window expires. The same seam still reuses the existing under-face transient avatar-effect owner to play any authored `Etc/EmotionEffect.img/<name>` animation that exists in the current data set, matching the client-side `CAvatar::SetEmotion` `Effect_General("Etc/EmotionEffect/%s")` call without introducing a parallel overlay path; rechecking `Etc/EmotionEffect.img` in the current WZ export still shows the authored overlay set capped at `blink`, `hit`, `smile`, `troubled`, `cry`, `angry`, `bewildered`, `stunned`, `vomit`, and `oops`. WZ was rechecked first for the item-backed random-emotion owner: `item/0528.img` still authors `05281000/emotion = {angry=25, cry=20, vomit=40, oops=15}` and `05281001/emotion = {cheers=25, chu=20, glitter=25, love=30}`, so the simulator still resolves those authored tables into weighted cumulative ranges instead of hardcoded picks. The missing client step that was still absent before is now implemented in the existing local-utility/CWvsContext seam: `OnRandomEmotion` no longer short-circuits straight to a simulator-owned local apply, and instead resolves the authored weighted emotion, respects the `CWvsContext::SendEmotionChange` 2-second gate, locally previews the chosen emotion through the existing timed owner, and mirrors the outbound `SendEmotionChange` request as opcode `56` with the client payload layout `(emotionId, duration=-1, byItemOption=0)` through the live bridge/outbox/deferred transport path when those transports are available. Focused regression coverage is now actually checked in under `UnitTest_MapSimulator/PacketOwnedLocalUtilityParityTests.cs` for the authored weighted-range boundaries, the `SendEmotionChange` outbound payload layout, and the 2-second CWvsContext throttle window. The remaining gap is narrower: the simulator still stores the decoded `m_bEmotionByItemOption` equivalent from `OnEmotion`, but no later runtime seam consumes that flag beyond the immediate timed-emotion owner; the local preview for `OnRandomEmotion` still uses the simulator's existing effect-duration fallback until an inbound `OnEmotion` echo arrives with an authoritative duration, rather than waiting on a guaranteed server ack before showing anything; and any broader client-only consumers of the item-option flag still need more IDA evidence before extra behavior is attached safely. | Visible avatar-expression parity now covers both direct emotion packets and the client-authenticated random-emotion re-entry path. Packet-authored timed emotions and area-buff random-emotion selection both reach the local avatar through the dedicated owner, use WZ-authored weight tables instead of hardcoded emotion picks, reuse the existing under-face avatar-effect plane for authored `EmotionEffect` overlays, and now also mirror the client-side `SendEmotionChange` outbound request/throttle seam instead of treating `OnRandomEmotion` as a detached one-shot. Remaining work is concentrated on downstream uses of the item-option bit and on proving whether any additional wait-for-echo timing needs to be mirrored beyond the current local preview plus later inbound override. | local avatar expression layer (`CUserLocal::OnPacket`, `CUser::OnEmotion`, `CUser::OnRandomEmotion`, `CAvatar::SetEmotion`, `CItemInfo::GetAreaBuffItem`, `CWvsContext::SendEmotionChange`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Character/PacketOwnedAvatarEmotionResolver.cs`, `HaCreator/MapSimulator/Interaction/PacketOwnedLocalUtilityContextState.cs`, `HaCreator/MapSimulator/Managers/LocalUtilityPacketInboxManager.cs`, `HaCreator/MapSimulator/MapSimulator.PacketOwnedUtilityParity.cs`, `UnitTest_MapSimulator/PacketOwnedLocalUtilityParityTests.cs`, `item/0528.img`, `Etc/EmotionEffect.img`) |
| Partial | Morph avatar pipeline parity | MapSimulator still resolves concrete morph template ids from `common/morph` and `PVPcommon/morph` into `Morph/*.img`, loads those images as a dedicated avatar render owner, resolves UOL-linked morph frames, preserves per-frame anchors (for example `head`), backfills through same-family base IMG fallback (`id -> id0 -> id00 -> id000`), paired `100x <-> 110x` family fallback, and WZ-backed `info/link` inheritance, and routes hostile polymorph mob skills plus current flag-only `info/morph = 1` outliers through that same owner seam. Runtime transform coverage remains on morph assets for ladder, rope, swim, fly, superman `ladder2`/`rope2`/`fly2`, airborne `fly2Move`/`fly2Skill`, authored combat, hit, death, sit, and jump-special branches. This run rechecked the existing `MorphClientActionResolver` seam against WZ-first plus client-backed evidence and then checked in the focused regression coverage that this row depends on: raw ranged requests containing `shoot`, `shot`, `spear`, `rain`, `break`, or `eruption` still keep archer-family ordering first; exact published actions still stay first when explicitly requested (for example `stormbreak`, `windspear`, `shoot1`, `swingT1`, `archerDoubleJump`); mixed-family ranged requests still route to published generic `shoot*` before pirate fallback aliases when authored archer aliases are absent; generic melee requests such as `swingT2` still fall through to published generic melee (`swingT1`/`swingT3`) before pirate carry-over aliases; and jump-special promotion remains scoped to explicit `doubleJump` requests instead of generic `jump`. Legacy morph requests remain reduced through this same seam (`alert6`/`alert7` nearest indexed alert fallback before base `alert`, `shoot6` archer-first plus generic `shoot*`, `arrowEruption` and `rain` prioritizing published `arrowRain`, `savage` reducing through published generic swing branches, `stabOF` / `stabT1` / `stabT2` now joining the earlier client-backed stab-family fallback onto published `swing*` when stab branches are not authored, and `paralyze` keeping published generic `shoot*` ahead of any authored carry-over aliases whenever that generic ranged surface exists, while still falling back to authored morph attacks when no published `shoot*` branch is available). WZ was rechecked for `Morph/1000.img`, `1001.img`, `1002.img`, `1003.img`, `1100.img`, `1101.img`, `1103.img`, `2001.img`, `2002.img`, and `2003.img`: `1000`, `1001`, `1003`, `1100`, `1101`, and `1103` still publish `info/superman = 1`; `1000`, `1001`, `1100`, and `1101` still keep pirate-family authored attacks such as `fist`, `straight`, `somersault`, `doublefire`, `backspin`, `doubleupper`, `shockwave`, `demolition`, `snatch`, `eburster`, `edrain`, `dragonstrike`, `eorb`, `timeleap`, and related branches but no generic `shoot*`; `1002` still publishes hide-family `fly2` / `fly2Move` / `fly2Skill` UOL surfaces without generic combat roots; `1003` and `1103` still expose authored archer aliases (`windshot`, `windspear`, `stormbreak`, `arrowRain`, `archerDoubleJump`), generic melee (`swingT1`, `swingT3`), and generic ranged backstop (`shoot1`, `shootF`); `2001` and `2002` still keep only the authored ice-family attack roots (`icemanAttack`, `iceAttack1`, `iceAttack2`, `iceSmash`, `iceTempest`, `iceChop`, `icePanic`) plus their double-jump branches; `2003` still remains movement-only; and no direct `paralyze`/`arrowEruption`/`shoot6`/`alert6`/`alert7` branches are published there, so resolver reduction remains required. Client evidence was rechecked in IDA: `CActionMan::LoadMorphAction` still routes through dedicated `s_sMorphAction` entries, `CActionMan::Init` still uses `get_action_name_from_code`, the recovered morph table still covers the concrete published names (`walk`, `stand`, `jump`, `alert*`, `proneStab`, `swingO*`, `swingT*`, `stabO*`, `stabTF`, `shoot1`, `shootF`, `windshot`, `windspear`, `stormbreak`, `timeleap`, `shockwave`, `demolition`, `snatch`, `fly2*`, and related published entries), and the wider client raw-action table still confirms the stab-family surface includes `stabOF`, `stabT1`, `stabT2`, and `stabTF` before `CAvatar::MoveAction2RawAction` promotes attackable/hide/superman morph move-action `18` to raw action `42` (`paralyze`). Based on that evidence, the resolver keeps avoiding loose substring scoring for authored fallback branches: a raw `rain` request still promotes the evidenced published `arrowRain` surface when WZ publishes it, but pirate-only morph families are not allowed to over-promote unrelated `edrain` just because the name contains `rain`; they fall back through the existing authored pirate order unless a WZ-backed generic or published archer surface exists. Focused regression coverage is now checked in under `UnitTest_MapSimulator/MorphAvatarPipelineParityTests.cs` for `paralyze` generic-vs-authored ordering, `rain`, `shoot6`, `arrowEruption`, exact published-action preservation, explicit `doubleJump` promotion versus generic `jump`, the broadened client stab-family fallback, and the morph template candidate order that keeps paired and linked morph-family backfill inside the existing loader seam. Remaining gap: the core `s_sMorphAction` table, the broader client-backed stab-family fallback, and the confirmed `MoveAction2RawAction` `paralyze` path are covered in this seam, but any broader client-only morph alias parity still depends on confirming additional remaps outside those evidenced paths before adding wider non-WZ aliases. | Morph-heavy jobs, hostile polymorph skills, and current flag-only morph outliers remain on the dedicated morph-owner seam rather than generic body/equip assembly. With this pass, the checked-in regression suite now pins the currently evidenced resolver ordering in addition to the existing runtime behavior: mixed-family builds keep WZ-authored generic melee and ranged surfaces ahead of unrelated fallback-family aliases, preserve exact published branch picks, keep `doubleJump` promotion explicit, preserve fallback-inherited `superman` handling for `ladder2`/`rope2`/`fly2`/`fly2Move`/`fly2Skill`, keep legacy `alert6`/`alert7`/`shoot6`/`arrowEruption`/`rain`/`savage` requests on client-shaped published surfaces, now also keep the broader client stab-family morph raw requests (`stabOF`, `stabT1`, `stabT2`, `stabTF`) on published `swing*` branches when stab branches are not authored, keep the client-confirmed morph `paralyze` raw request on published generic `shoot*` before any authored carry-over aliases when that generic ranged surface is actually published, and avoid over-promoting pirate-only `edrain` for raw `rain` when no published `arrowRain` or generic `shoot*` surface exists. The row remains narrowed to additional remaps that still lack client evidence beyond the recovered `LoadMorphAction` table, the broader client stab-family surface now covered here, and the confirmed `MoveAction2RawAction` `paralyze` path, not WZ-backed owner assembly or the currently evidenced resolver ordering. | avatar transform/runtime assembly (`CActionMan::GetMorphImgEntry`, `CAvatar::MoveAction2RawAction`, `CActionMan::LoadMorphAction`, `CActionMan::Init`, `get_action_name_from_code`, `CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUserLocal::SetMoveAction`, `HaCreator/MapSimulator/Character/MorphClientActionResolver.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `UnitTest_MapSimulator/MorphAvatarPipelineParityTests.cs`, `Morph/1000.img`, `Morph/1001.img`, `Morph/1002.img`, `Morph/1003.img`, `Morph/1100.img`, `Morph/1101.img`, `Morph/1103.img`, `Morph/2001.img`, `Morph/2002.img`, `Morph/2003.img`) |
| Partial | Mount / vehicle / mechanic mode rendering | `TamingMob` assembly on this row still covers the already-implemented owner paths: normal mount ride frames and `sit` fallback, `eventTamingMob` part equip, Mechanic owner routing to `Character/TamingMob/01932016`, Battleship owner routing to `Character/TamingMob/01932000`, direct `ride2` / `getoff2` transitions, fixed Mechanic state skills, distinct Mechanic/Battleship mounted actions, known-owner authored action preservation, and the earlier local built-in Mechanic transform seam (`35001001`, `35101004`, `35101009`, `35111004`, `35121005`, `35121013`) that keeps `01932016` latched through `SkillManager.UpdateClientOwnedVehicleTamingMobState`, `PlayerCharacter.UpdateOwnedTamingMobRenderState`, and `PlayerCharacter.ResolveMountedStateTamingMobPart`. This pass keeps that tighter boundary and rechecks it in-tree again: WZ still publishes the full `tank`, `siege`, `tank_*`, and `siege_*` action-name surface on `Character/00002000.img`, while `Character/TamingMob/01932016` in the current export still publishes the same Mechanic owner families together with the mounted move roots it reuses. Because that action-name overlap is explicit on both owners, `ClientOwnedVehicleSkillClassifier.IsOwnerlessMechanicVehicleInferenceActionName` remains closed for plain `tank` / `siege` / `tank_*` / `siege_*` names, and `FollowCharacterEligibilityResolver.ResolveMountedVehicleId` together with `SkillManager.ResolveClientOwnedVehicleCurrentActionMountItemId` stay on the already-established owner seams only: active mounted owner, equipped owner, canonical blank-tick preservation, explicit remote `mechanicMode`, or the active built-in local Mechanic transform seam. IDA still points the same way for that tighter boundary: `?LoadTamingMobAction@CActionMan@@QAEXJJQAJHAAV?$ZArray@V?$ZRef@UTAMINGMOBACTIONFRAMEENTRY@CActionMan@@@@@@@Z` only loads taming-mob action frames after a vehicle id is already known, and `?IsAbleTamingMobAction@@YAHJJ@Z` plus `?IsAbleTamingMobOneTimeAction@@YAHJJ@Z` still gate the extra Mechanic and ride coverage by vehicle id rather than by action name alone. Focused source-side regression coverage is now checked in under `UnitTest_MapSimulator/ClientOwnedVehicleTransformMountParityTests.cs`, pinning Mechanic owner activation for the active built-in transform ids, rejection for non-Mechanic transform families, rejection for ownerless `tank` / `siege` / `tank_laser` / `tank_siegeattack` / `tank_rbooster_pre`, preservation of those same actions when a trustworthy Mechanic owner is already known, explicit `mechanicMode` preservation, and blank-tick owner preservation for both Mechanic and Battleship owners. Remaining gap: broader truly ownerless client-only vehicle update or move-state sources that still surface with no active buff, no active built-in transform owner, no handoff owner, no canonical equipped owner, no explicit `mechanicMode`, and no mounted render owner signal at all still need new client evidence before they can be mirrored safely, and Battleship still has no comparable transform-owned local owner seam beyond the already-implemented named or known-owner paths. | Mounted-state, follow eligibility, local current-action ownership, and the actual local taming-mob render owner now stay aligned across the evidenced blank-tick and built-in Mechanic transform seams without falling back to generic `tank*` / `siege*` action-name guessing that WZ also publishes on the base body. With the checked-in regression file, this row is now narrowed to genuinely ownerless client-managed vehicle states and any future Battleship-specific local owner seam, not the already-owned Mechanic transform or known-owner paths. | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler`, `SkillManager`, `ClientOwnedVehicleSkillClassifier`, `FollowCharacterEligibilityResolver`, `EscortFollowController`, `MapSimulator.PacketOwnedUtilityParity`, `UnitTest_MapSimulator/ClientOwnedVehicleTransformMountParityTests.cs` (`35001001`, `35101004`, `35101009`, `35111004`, `35121005`, `35121013`, `?LoadTamingMobAction@CActionMan@@QAEXJJQAJHAAV?$ZArray@V?$ZRef@UTAMINGMOBACTIONFRAMEENTRY@CActionMan@@@@@@@Z`, `?IsAbleTamingMobAction@@YAHJJ@Z`, `?IsAbleTamingMobOneTimeAction@@YAHJJ@Z`, `Character/00002000.img`, `Character/TamingMob/01932016`, `Character/TamingMob/01932000`, `Skill/3500.img/skill/35001001`, `Skill/3510.img/skill/35101004`, `Skill/3510.img/skill/35101009`, `Skill/3511.img/skill/35111004`, `Skill/3512.img/skill/35121005`, `Skill/3512.img/skill/35121013`) |
| Partial | Skill-specific avatar transforms | The WZ-backed families on this row remain in the existing seams, and this pass widens the checked-in parity from the earlier prepare/hold/exit fix to the next remaining raw-action-name gap inside the same built-in Mechanic transform owner. Focused regression coverage now exists under `UnitTest_MapSimulator/PreparedSkillAvatarTransformParityTests.cs` for both the shared prepare-candidate seam in `SkillManager` and the direct built-in transform seam in `PlayerCharacter`. The shared prepare resolver still keeps the WZ prepare pose ahead of the hold pose for the families on this row: Night Walker `Poison Bomb` (`dash` -> `darkTornado_pre`), Wild Hunter `Swallow` (`swallow_loop` -> `swallow_pre`, matching the still-confirmed `CUserLocal::DoActiveSkill_Prepare` override for skill `33101005` raw action `251`), `Cyclone` (`cyclone_pre` before body `cyclone`), `Monkey Wave` (`noiseWave_pre` before body `noiseWave_ing`), and Battle Mage `Dual Vulcan` (`dualVulcanPrep` before body `dualVulcanLoop`). `PlayerCharacter.TryCreateBuiltInSkillAvatarTransform` still resolves prepare, hold, and exit as distinct stages instead of collapsing every non-prepare lookup back to the hold pose, and this run widens the direct raw action-name seam for the already-authored Mechanic state families instead of inventing new transform names: it now recognizes the published hold and movement roots `tank_walk`, `tank`, `tank_prone`, `siege`, and `tank_siegeattack` in addition to the previously covered startup / exit names, so the row's WZ-authored Mechanic state families now keep their authored startup, hold, movement, attack, prone, and teardown when the transform lookup arrives through either the skill-id seam or the raw action-name seam. The existing row coverage remains in place for `Cyclone` (`cyclone_pre` -> `cyclone` -> `cyclone_after`), `Monkey Wave` (`noiseWave_pre` -> `noiseWave_ing` -> `noiseWave`), `Dual Vulcan` (`dualVulcanPrep` -> `dualVulcanLoop` -> `dualVulcanEnd`), Night Walker `Poison Bomb` (`dash` / `darkTornado_pre` -> `darkTornado` -> `darkTornado_after`), Wild Hunter `Swallow` (`swallow_pre` -> `swallow_loop` -> `swallow`), both flamethrower families (`flamethrower_pre` / `flamethrower_pre2` -> body -> `_after`), tank-vs-normal Rocket Booster startup and `_after`, Mechanic state-family startup / hold / `_after` for `tank_pre`, `siege_pre`, and `tank_siegepre`, and the earlier client-backed siege alias: `CAvatar::MoveAction2RawAction` still returns raw action `217` (`lasergun`) while Mechanic mode `35111004` is active, and `Character/00002000.img` still publishes `lasergun`, so the built-in transform seam still recognizes `lasergun` as a siege-family attack alias with the same locked-movement Mechanic owner and `siege_after` teardown instead of leaving that client-owned action name outside the checked-in transform set. The existing direct remaps on this row remain in place for Demon charge-release fallback onto renderable `bluntSmash` when WZ asks for `bluntSmashEnd` or `soulEater_end`, `wildbeast`, Evan breath charges, `finalCutPrepare`, and `monsterBombPrepare`. WZ was rechecked again in the current extracted workspace data: `Skill/1411.img/skill/14111006/prepare/action = dash`, `Skill/3212.img/skill/32121003/action/0 = cyclone_pre`, `Skill/3310.img/skill/33101005/prepare/action = swallow_loop`, `Skill/531.img/skill/5311002/{prepare/action = noiseWave_pre, keydown/action = noiseWave_ing, action/0 = noiseWave}`, `Skill/2312.img/skill/23121000/{prepare/action = dualVulcanPrep, keydown/action = dualVulcanLoop, keydownend/action = dualVulcanEnd}`, `Skill/3100.img/skill/31001000/keydownend/action = bluntSmashEnd`, `Skill/3110.img/skill/31101000/{action/0 = bluntSmash, keydownend/action = soulEater_end}`, `Skill/3500.img/skill/35001001/{prepare/action = flamethrower_pre, keydown/action = flamethrower, keydownend/action = flamethrower_after, action/0 = flamethrower}`, `Skill/3510.img/skill/35101009/{prepare/action = flamethrower_pre2, keydown/action = flamethrower2, keydownend/action = flamethrower_after2}`, `Skill/3510.img/skill/35101004/action/0 = rbooster_pre`, `Skill/3511.img/skill/35111004/action/0 = siege_pre`, `Skill/3512.img/skill/35121005/action/0 = tank_pre`, and `Skill/3512.img/skill/35121013/action/0 = tank_siegepre`. `Character/00002000.img` still publishes the body-backed families used here (`darkTornado_pre`, `darkTornado`, `darkTornado_after`, `swallow_pre`, `swallow_loop`, `swallow`, `cyclone_pre`, `cyclone`, `cyclone_after`, `noiseWave_pre`, `noiseWave_ing`, `noiseWave`, `dualVulcanPrep`, `dualVulcanLoop`, `dualVulcanEnd`, `bluntSmash`, `lasergun`, `rbooster_pre`, `rbooster`, `rbooster_after`, `tank_pre`, `tank`, `tank_walk`, `tank_stand`, `tank_prone`, `tank_after`, `siege_pre`, `siege`, `siege_stand`, `siege_after`, `tank_siegepre`, `tank_siegeattack`, `tank_siegestand`, `tank_siegeafter`, `tank_rbooster_pre`, `tank_rbooster_after`) and still does not publish body-backed `bluntSmashEnd`, `soulEater_end`, `soulEater`, or dedicated `tank_*`, `siege_*`, and `tank_siege*` movement-variant jump / ladder / rope / fly / swim / hit branches. The remaining gap is narrower again: the WZ-authored prepare, hold, movement, the currently evidenced exit families, and the confirmed siege-family `lasergun` raw alias on this row all match the checked-in code and have focused regression coverage, but any further client-only prepared or transform aliases such as unevidenced attack-name remaps, extra exit names, or broader movement-family branches still need explicit client evidence before they should be added. | Mechanic avatar state skills, flamethrower, Rocket Booster, Cyclone, `Monkey Wave`, Wild Hunter `Swallow`, Wild Hunter `More Wild`, Evan dragon-breath charges, Dual Blade `Final Cut` / `Monster Bomb`, Battle Mage `Dual Vulcan`, Night Walker `Poison Bomb`, the confirmed Demon prepared-skill charge families, the client-backed siege-mode `lasergun` alias, and now the WZ-authored Mechanic hold / movement / attack roots that arrive through the direct raw action-name seam are anchored in checked-in source-side regression coverage as well as the runtime remaps themselves. The remaining uncertainty is now limited to still-unconfirmed client-only aliases beyond `lasergun`, additional exit branches, or transform movement families beyond the WZ-authored `tank_*`, `siege_*`, and `tank_siege*` state / attack / `_after` families already wired here. | `SkillManager`, `PlayerCharacter`, `CharacterAssembler`, `UnitTest_MapSimulator/PreparedSkillAvatarTransformParityTests.cs` (`14111006`, `22121000`, `22151001`, `23121000`, `31001000`, `31101000`, `32121003`, `33101005`, `33121006`, `4341002`, `4341003`, `5311002`, `35001001`, `35101009`, `35101004`, `35111004`, `35121005`, `35121013`, `CAvatar::MoveAction2RawAction`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::TryDoingSwallowBuff`, `Skill/1411.img/skill/14111006/prepare/action`, `Skill/2212.img/skill/22121000/{prepare,action}`, `Skill/2215.img/skill/22151001/{prepare,action}`, `Skill/2312.img/skill/23121000/{prepare,keydown,keydownend}`, `Skill/3100.img/skill/31001000/keydownend/action`, `Skill/3110.img/skill/31101000/{action/0,keydownend/action}`, `Skill/3212.img/skill/32121003/action/0`, `Skill/3310.img/skill/33101005/prepare/action`, `Skill/3312.img/skill/33121006/action/0`, `Skill/3500.img/skill/35001001/{prepare,keydown,keydownend,action}`, `Skill/3510.img/skill/35101009/{prepare,keydown,keydownend}`, `Skill/3510.img/skill/35101004/action/0`, `Skill/3511.img/skill/35111004/action/0`, `Skill/3512.img/skill/35121005/action/0`, `Skill/3512.img/skill/35121013/action/0`, `Skill/434.img/skill/4341002/{prepare,action}`, `Skill/434.img/skill/4341003/{prepare,action}`, `Skill/531.img/skill/5311002/{prepare,keydown,action}`, `Character/00002000.img/{darkTornado_pre,darkTornado,darkTornado_after,icebreathe_prepare,dragonIceBreathe,breathe_prepare,dragonBreathe,bluntSmash,cyclone_pre,cyclone,cyclone_after,noiseWave_pre,noiseWave_ing,noiseWave,dualVulcanPrep,dualVulcanLoop,dualVulcanEnd,finalCutPrepare,finalCut,monsterBombPrepare,monsterBombThrow,lasergun,rbooster_pre,rbooster,rbooster_after,tank_pre,tank,tank_walk,tank_stand,tank_prone,tank_after,siege_pre,siege,siege_stand,siege_after,tank_siegepre,tank_siegeattack,tank_siegestand,tank_siegeafter,tank_rbooster_pre,tank_rbooster_after}`) |
| Partial | Client-managed avatar effect layers | The simulator now loads persistent avatar-bound skill layer branches from WZ (`special`, `special0`, `finish`, `finish0`, ladder-aware `back` / `back_finish`, and `repeat` on `suddenDeath` skills), attaches them to the player render path as over-character / under-face layers, switches the `More Wild` family onto its ladder override, plays finish cleanup on buff expiry or cancellation, routes `doubleJump` action-family movement skills onto a client-style transient avatar-owned layer instead of the generic cast sprite, extends that same transient seam across the broader non-buff movement families the existing resolver already classifies (`Rush`, backstep, bound-jump / assaulter / flash, and flying-rush variants) so WZ-backed movement casts like `Skill/122.img/1221007/{effect,effect0}`, `Skill/433.img/4331005/{effect,effect0}`, and `Skill/3001.img/30010186/effect` suppress the detached cast sprite and keep authored planes bound to the avatar for the movement window, now also honors movement or buff shapes that only publish the secondary `effect0` branch by treating `effect` and `effect0` as equivalent client-owned sources for promotion instead of requiring the primary branch to exist, promotes fly/flying buff `effect` branches onto looping player-owned avatar layers so wing visuals stay attached to the avatar for the full buff window instead of rendering as detached cast sprites, promotes Dark Sight-family `effect` branches such as `Skill/400.img/4001003/effect` and `Skill/1400.img/14001003/effect` onto the same looping avatar-owned layer path instead of leaving them as detached cast sprites, promotes invisible swallow buff `effect` branches such as `Skill/3310.img/33101006/effect` onto a client-style under-face avatar layer that now hides while the player is on a ladder or rope, now preserves sibling `effect0` branches on transient client-owned movement layers as an extra avatar-bound plane instead of dropping them, also promotes root-`invisible` buff skills whose WZ publishes only timed `effect/effect0` planes without `affected` or `special` branches, such as `Skill/000.img/0001037/{effect,effect0}`, onto the same looping avatar-owned path instead of leaving them detached, also promotes WZ-backed damage-reflect buff visuals that publish timed `effect/effect0` without `affected` or `special` branches, such as `Skill/110.img/1101007`, `Skill/120.img/1201007`, and `Skill/3110.img/31101003`, onto looping avatar-owned layers while still suppressing those client-owned avatar layers whenever Oak Barrel-style morph buffs or full-body Mechanic ride actions own the sprite, now extends that same looping avatar-owned fallback to the broader timed-buff WZ shape where a self-buff only publishes `effect/effect0` companion planes without `affected` or `special*`, no longer mirrors a lone promoted plane onto both avatar planes when WZ only authored one side, which keeps single-plane `effect`-only or `effect0`-only visuals on their authored z side instead of double-drawing them across under-face plus overlay, and now also treats `affected` and `affected0` as the same exclusion signal for those timed-buff fallbacks so a secondary-only active aura cannot be mistaken for a missing `affected` branch and incorrectly re-promoted through the client-owned `effect/effect0` path. This pass also widens the smaller `CUser::Update`-owned affected-area seam that IDA confirmed through `CUser::UpdateAr01Effect`: while the player stands inside a qualifying friendly player-skill affected area that can apply to the local player and WZ publishes a looping `affected` or `affected0` branch, the simulator now tracks that area instance by object id, keeps both planes when both siblings exist, and promotes those authored `affected` / `affected0` animations onto the avatar-owned under-face plus overlay seam instead of leaving the local actor without the client-owned aura or silently dropping the secondary plane, and now also preserves authored anchor metadata on that seam instead of dropping WZ `pos` during the clone step. That keeps the confirmed `Amplifier Robot AF-11` (`Skill/3512.img/35121010/affected`) surface, preserves dual-plane authored auras such as `Skill/000.img/0000086/{affected,affected0}`, and now also covers the broader friendly support-summon area shape the resolver can identify from WZ, including party-owned Mechanic support robots such as `Healing Robot H-LX` (`Skill/3511.img/35111011/affected`), while now also letting those party-support robot auras stay eligible from WZ `info/type = 33` plus `info/minionAbility = heal` / `amplifyDamage` even when localized description text is unavailable, alongside the existing mass-spell or support-zone affected-area families instead of hardcoding only the single `AR01` skill id. The synthetic active-buff owner used for those projected remote support areas now also converts whichever source or linked support skill actually publishes the authored `affected` / `affected0` branch into looping avatar-owned overlay / under-face planes and only applies that owner when the projected area is first created or upgraded from a non-avatar-owned state, so those remote support auras no longer restart every refresh tick and no longer stay dark when the summon metadata resolves but the aura branch lives on a linked support skill instead of the top-level area owner. The local-player affected-area avatar-effect path now mirrors that same linked-support-skill ownership rule instead of checking only the top-level area owner, and now also merges linked support-skill level data before the local promotion and eligibility gates run, so wrapper or summon skills whose WZ `info/affectedSkill` points at the authored aura source can still promote the real `affected` / `affected0` branch and inherit linked party-facing support stats when the wrapper skill itself only publishes the outer area or summon shape. WZ was rechecked again for that narrower seam in the current extracted data set: `Skill/3211/skill/32110007/info/affectedSkill = 32001003`, while the authored aura still lives on `Skill/3200/skill/32001003/affected`; `Skill/3511/skill/35111011/info` still publishes `type = 33` plus `minionAbility = heal`, and `Skill/3512/skill/35121010/affected/pos = 1`, so the simulator now keeps those avatar-owned aura paths eligible instead of leaving the local actor dark when friendliness only arrives through linked support skill ownership, support-summon metadata, linked support level data, or authored non-body anchor metadata. The runtime also now keeps sibling `affected0` planes alive on ordinary looping buff visuals such as `Skill/532.img/5320007/{affected,affected0}` and `Skill/572.img/5720005/{affected,affected0}` instead of discarding the secondary authored layer, and it no longer incorrectly binds Energy Charge-family `special` layers on initial buff activation: WZ marks those skills with `info/energyCharge = 1` and `info/fullChargeEffect = bodyAttack`, while the client keeps a separate `m_tEnergyCharge`-owned runtime in `CUser::Update`, so the simulator now defers those full-charge avatar layers until a dedicated owner exists, tracks active Energy Charge buffs through that separate runtime seam, accumulates charge from successful local attack hits against the WZ-backed `common/x` threshold on `Skill/511.img/5110001` and `Skill/1510.img/15100004`, and only promotes the authored `special` layer once that inferred full-charge state is reached. IDA also rechecked the race novice flying seam in `CUser::Update`: while the player is in the client-owned flying runtime, the client resolves `get_novice_skill_as_race(0x402, nJob)` and routes that family through `CUser::LoadSkillRepeatEffect`, so the simulator now promotes WZ-authored `repeat` / `repeat0` planes from the race `*1026` skill family onto looping avatar-owned layers instead of leaving those visuals detached or absent. WZ recheck in the current export shows `Skill/000.img/0001026/repeat`, `Skill/1000.img/10001026/repeat`, and `Skill/2000.img/20001026/repeat` still publish `pos = 1`, `z = -2`, and `flip = 1`, so the player render path now preserves that anchor metadata, resolves the client-style face-origin attachment through the assembled head `brow` point, keeps those authored flying planes bound under the face while the local player stays in the flying state, and now also marks that synthetic `*1026` owner as rotate-sensitive so those client-owned wings suppress during the same confirmed rotate-action family instead of overdrawing during spin-style skills. The same IDA re-check of `CUser::Update` also shows the client suppressing several avatar-owned layers while `CAvatar::IsOnPlayingRotateAction` is active, so the simulator now marks the currently confirmed sudden-death / `Final Cut` repeat layer path, the flying-wing fallback path, and the race novice flying `*1026` repeat seam as rotate-sensitive and suppresses those avatar-owned effects during the rotate-style action family the current runtime can identify safely. The runtime now consults the player's current client raw-action code when it can resolve one, uses the exact client rotate raw-action set confirmed in IDA (`101`, `82`, `114`, `149`, `151`, `193`), and now also derives the exact client action roots for that raw-action set through the same `get_action_name_from_code`-shaped map the simulator already builds from `Character/00002000.img`, so the named fallback now safely covers `homing`, `triplefire`, `bamboo`, `lightingBolt`, `breathe_prepare`, and `chainPull` alongside the already-confirmed WZ-authored rotate families (`doubleJump`, `backspin`, `rollingSpin`, `darkSpin`, `screw`, `somersault`, and explicit `finalCut`) instead of continuing to overdraw those layers whenever only the action name is visible. That suppression still also consults the player runtime's forced one-time skill action, so client-owned layers hide during skill-driven rotate windows even when the visible current action has already fallen back to a benign standing or generic attack pose. The remaining gap is narrower again: broader client-only `CUser::Update` families beyond `More Wild`, swallow, the race novice flying `*1026` repeat seam, double-jump, the existing movement or buff WZ shapes, and these currently promoted friendly `affected`-area ownership families still need explicit client-backed ownership rules before they can be mirrored safely; the rotate fallback now covers the exact client action roots for the currently confirmed `IsOnPlayingRotateAction` raw-action set plus the current synthetic `*1026` repeat owner, but any wider rotate family still needs new client evidence before more names can be added safely; and the Energy Charge runtime could still use deeper client confirmation if later IDA work proves a different accumulation cadence or extra non-visual full-charge behavior around `bodyAttack`. | Visible parity is closer for `More Wild`, `Final Cut`-style sudden-death buffs, the broader WZ-backed movement cast surface, flying-wing buffs, the race novice flying `*1026` repeat family, Dark Sight-family stealth buffs, root-`invisible` effect-only buffs, Wild Hunter swallow buffs, the confirmed damage-reflect buff family, the secondary-only `effect0` surface, the broader timed self-buff `effect/effect0` pair surface, the Energy Charge family, and now the client-owned friendly affected-area aura surface because the promoted avatar-owned layers now follow the client's ownership rules more closely, no longer require the primary `effect` branch to exist before promotion, no longer double-render single-plane authored visuals on both avatar planes, now also refuse to treat a WZ-authored `affected0` aura as a missing affected branch when deciding whether timed buff `effect/effect0` planes should be rebound onto the avatar, now defer Energy Charge `special` layers until a separate full-charge owner exists instead of attaching them at cast start, now drive that Energy Charge owner forward on successful local hits toward the WZ-backed charge threshold, now let party-owned support robots keep their avatar-owned aura eligibility from WZ summon metadata alone instead of depending on localized description text, now synthesize the projected remote support-area buff owner from whichever source or linked support skill actually publishes `affected` / `affected0`, now let the local affected-area avatar seam pull that same aura plane, linked party-support eligibility, linked support-level stats, and authored `pos` anchor metadata through support skills such as `32110007 -> 32001003` and `35121010/affected/pos = 1` instead of only the wrapper owner or body-origin defaults, now preserve WZ `pos = 1` on player-owned looping avatar layers so the client-style face-origin flight family can anchor from the assembled `brow` point instead of the body origin, now also suppress the synthetic `*1026` flying-repeat owner during the confirmed rotate-action family, and now also preserve sibling `affected0` planes on looping buff / aura visuals while the player stands inside qualifying local- or party-owned player-skill affected areas such as `35121010`, `35111011`, `32001003`, and the same WZ-authored mass-spell or support-zone area shape without re-triggering those avatar-owned auras every refresh tick. Rotate-sensitive avatar-owned layers also now stop depending on a broad visible-action `spin` substring, inspect the same forced one-time action seam the player runtime uses for skill-driven attacks, consult the same client rotate raw-action set (`101`, `82`, `114`, `149`, `151`, `193`) that `CAvatar::IsOnPlayingRotateAction` checks, and now cover the exact client action roots for that set through the simulator's `Character/00002000.img` action-code map (`homing`, `triplefire`, `bamboo`, `lightingBolt`, `breathe_prepare`, `chainPull`) alongside the already-confirmed WZ-authored rotate families (`doubleJump`, `backspin`, `rollingSpin`, `darkSpin`, `screw`, `somersault`, `finalCut`). Focused source-side regression coverage is now present under `UnitTest_MapSimulator/AffectedAreaAvatarEffectParityTests.cs` for linked support-skill promotion, merged support-level-data resolution, and preserved affected-area `pos` metadata on the local affected-area avatar seam. The remaining work is narrower: broader `CUser::Update`-owned effect families that do not publish these WZ-authored `special*`, timed `effect/effect0`, `affected/affected0`, invisible/effect-only buff, movement-action, the race novice flying `repeat` family, or the now-covered friendly affected-area shape still need explicit client-backed remaps before they can be mirrored safely, the currently confirmed rotate family no longer needs raw-action-to-name discovery and now also covers the current synthetic `*1026` repeat seam, but any wider rotate-family expansion still needs fuller client evidence, and Energy Charge may still need deeper client-backed tuning if later evidence shows that its full-charge cadence or combat-side `bodyAttack` runtime differs from the current WZ-shaped accumulation seam. | `PlayerCharacter`, `PlayerManager`, `SkillLoader`, `SkillManager`, `CharacterAssembler`, `CharacterData`, `ClientOwnedAvatarEffectParity`, `AffectedAreaAvatarEffectResolver`, `UnitTest_MapSimulator/AffectedAreaAvatarEffectParityTests.cs` (`CUser::Update`, `CUser::UpdateMoreWildEffect`, `CUser::UpdateAr01Effect`, `CUserLocal::TryDoingSwallowBuff`, `CUser::LoadSkillRepeatEffect`, `CAvatar::IsOnPlayingRotateAction`, `get_novice_skill_as_race`, `get_action_name_from_code`, `Character/00002000.img/{doubleJump,backspin,rollingSpin,darkSpin,screw,somersault,homing,triplefire,bamboo,lightingBolt,breathe_prepare,chainPull,finalCut}`, `Skill/3312.img/33121006/{special,special0,finish,finish0,back,back_finish}`, `Skill/434.img/4341002/repeat`, `Skill/400.img/4001003/effect`, `Skill/1400.img/14001003/effect`, `Skill/310.img/3101003/effect`, `Skill/3300.img/33001002/effect`, `Skill/122.img/1221007/{effect,effect0}`, `Skill/433.img/4331005/{effect,effect0}`, `Skill/3001.img/30010186/effect`, `Skill/3310.img/33101006/effect`, `Skill/000.img/0001037/{effect,effect0,invisible}`, `Skill/000.img/0001026/repeat`, `Skill/1000.img/10001026/repeat`, `Skill/2000.img/20001026/repeat`, `Skill/110.img/1101007/{effect,effect0}`, `Skill/120.img/1201007/{effect,effect0}`, `Skill/3110.img/31101003/{effect,effect0}`, `Skill/1411.img/14111000/effect0`, `Skill/510.img/5101007`, `Skill/511.img/5110001/{special,affected,common/x,info/energyCharge,info/fullChargeEffect}`, `Skill/1510.img/15100004/{special,affected,common/x,info/energyCharge,info/fullChargeEffect}`, `Skill/000.img/0000086/{affected,affected0}`, `Skill/532.img/5320007/{affected,affected0}`, `Skill/572.img/5720005/{affected,affected0}`, `Skill/3511.img/35111011/{affected,info/type,info/minionAbility}`, `Skill/3512.img/35121010/{affected,affected/pos,info/type,info/minionAbility}`, `Skill/3512.img/35121007/affected`, `Skill/3312.img/33121007/affected`, `Skill/3200.img/32001003/affected`, `Skill/3210.img/32101002/affected`, `Skill/3210.img/32101003/affected`, `Skill/3211/skill/32110007/info/affectedSkill`) |
| Partial | Remote user / `CUserPool` actor parity | `RemoteUserActorPool` remains the shared remote-user owner for AvatarLook decode, enter/leave, movement snapshots, helper/team state, chair/mount state, prepared-skill world overlays, packet `223` avatar refresh, packet-owned `Effect/ItemEff.img` social effects, carry-item and completed-set rendering, and shared `225/226` temporary-stat storage instead of feature-local placeholders. This pass stayed in that seam and landed the focused parity fixes plus the source-side regression coverage the checkout had been missing: `UnitTest_MapSimulator/RemoteUserTemporaryStatPresentationTests.cs` now covers the current remote priority orderings for Shadow Partner, Soul Arrow versus Spirit Javelin, the Barrier family, and explicit-versus-job-owned Aura resolution, alongside the hidden-like ghost-family action split, the hidden-like remote `sit -> ghostsit` branch, and the Mechanic visible-action plus `01932016` taming-mob owner seams. Runtime-side, remote temporary-stat reset now immediately re-synchronizes presentation when the mask clears, and `RemoteUserPacketCodec.ApplyResetMask` now keeps the remote Soul Arrow presentation alive while `SpiritJavelin` remains set, matching the same shared-owner interpretation already used when decoding the live mask. WZ was rechecked again for this row scope in the mounted export: `Skill/2112.img/skill/21120007` still publishes both `effect` and `affected`, `Skill/2311.img/skill/23111005` still publishes authored `effect`, `Skill/2001.img/skill/20011010`, `Skill/2000.img/skill/20001010`, and `Skill/1000.img/skill/10001010` still publish authored `effect` and `affected`, `Skill/3511.img/skill/35111004/action/0`, `Skill/3512.img/skill/35121005/action/0`, and `Skill/3512.img/skill/35121013/action/0` still publish the Mechanic authored prepare actions, and `Character/00002000.img` still publishes `{ghoststand,ghostwalk,ghostjump,ghostproneStab,ghostladder,ghostrope,ghostfly,ghostsit}`. This keeps the row aligned with the same client-owned seams already evidenced for `CUserPool::OnPacket`, `CUserPool::Update`, `CUserRemote::Update`, `CUserRemote::OnAvatarModified`, `SecondaryStat::DecodeForRemote`, `CUser::SetCarryItemEffect`, and `CAnimationDisplayer::Effect_ByItem`. | The remaining gap is narrower but still row-sized: the packet router still only covers the implemented enter/leave, move/state, helper/team, chair/mount, prepared-skill, melee, item-effect, avatar-modified, and temp-stat families instead of the wider client packet surface; Messenger member parity still depends on the simulator's optional member-info `AvatarLook` trailer instead of fuller official messenger decode and room-member lifecycle; and remote temp-stat/update parity is still incomplete beyond the currently decoded hidden-like, morph, Mechanic, Shadow Partner, known Aura, Soul Arrow, Blessing Armor, and WZ-backed Barrier-family presentation now covered by the focused regression file. Remaining work on this row is therefore concentrated on packet-authored remote families the shared owner still does not decode or render, exact client carry-item orbit choreography and asset choice, client-only relationship pairing rules that depend on serial tables instead of same-item reconciliation, broader packet-authored user effects beyond `ItemEff`, remote afterimages beyond the current melee seam, any other feature-local remote shells that still bypass the shared pool, and higher-fidelity movement smoothing beyond the current sampled move-path path. | shared field actor/runtime layer (`CUserPool::OnPacket`, `CUserPool::Update`, `CUserRemote::Update`, `CUserRemote::OnAvatarModified`, `SecondaryStat::DecodeForRemote`, `CUser::SetCarryItemEffect`, `CAnimationDisplayer::Effect_ByItem`, `CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUser::Update`, `CMovePath::{Decode,OnMovePacket}`, `Morph/*.img`, `Character/TamingMob/01932016.img`, `Character/01112916.img/info/effect/path`, `Character/01112924.img/info/effect/path`, `Character/01112926.img/info/effect/path`, `Character/00002000.img/{ghoststand,ghostwalk,ghostjump,ghostproneStab,ghostladder,ghostrope,ghostfly,ghostsit,tank_stand,tank_walk,tank_prone,tank,siege_stand,siege,tank_siegestand,tank_siegeattack}`, `Skill/3511.img/35111004/action/0`, `Skill/3512.img/35121005/action/0`, `Skill/3512.img/35121013/action/0`, `Skill/411.img/skill/4111002/special`, `Skill/421.img/skill/4211008/special`, `Skill/1411.img/skill/14111000/special`, `Skill/412.img/4121006/effect`, `Skill/3310.img/33101003/effect`, `Skill/1310.img/13101003/effect`, `Skill/320.img/3201004/effect`, `Skill/310.img/3101004/effect`, `Skill/2112.img/21120007/{effect,affected}`, `Skill/2311.img/23111005/effect`, `Skill/2001.img/20011010/{effect,affected}`, `Skill/2000.img/20001010/{effect,affected}`, `Skill/1000.img/10001010/{effect,affected}`, `Effect/CharacterEff.img/1112916`, `Effect/CharacterEff.img/1112924`, `Effect/CharacterEff.img/1112926`, `Effect/ItemEff.img/1112001`, `Effect/ItemEff.img/1112800`, `Effect/ItemEff.img/1112803`, `Effect/ItemEff.img/2270000`, `Effect/ItemEff.img/2270002`, `Effect/ItemEff.img/2270003`, `Etc/SetItemInfo.img`, `Effect/SetItemInfoEff.img`, `UnitTest_MapSimulator/RemoteUserTemporaryStatPresentationTests.cs`) |

### 2. Taming-mob action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Taming-mob action-loader / frame-build parity | The simulator now keeps a dedicated taming-mob action owner in the loader seam instead of leaving all taming-mob action selection buried inside the broader avatar assembler. `CharacterLoader.LoadTamingMob` now attaches a `TamingMobActionFrameOwner` to taming-mob parts, and that owner is shared by `CharacterAssembler`, `PlayerCharacter`, and `SkillManager` for vehicle-aware lazy frame loading, support checks, and cached action resolution keyed to the concrete taming-mob item id. WZ was rechecked first for the concrete action families this owner has to serve: ordinary rides such as `Character/TamingMob/01902000` still only publish the expected locomotion and fallback families (`walk1`, `walk2`, `stand1`, `stand2`, `tired`, `jump`, `prone`, `ladder`, `rope`, `fly`), while Mechanic `Character/TamingMob/01932016` still publishes the broader client-owned families (`tank`, `tank_walk`, `tank_stand`, `tank_prone`, `tank_laser`, `tank_msummon`, `tank_rbooster_pre`, `tank_rbooster_after`, `siege_pre`, `siege`, `siege_stand`, `siege_after`, `flamethrower`, `coolingeffect`). IDA remains the second source and still pins the seam boundary: `CActionMan::LoadTamingMobAction` at `0x427a10` validates by vehicle id before loading, `IsAbleTamingMobAction` only opens the extra persistent Mechanic family for `1932016`, and `IsAbleTamingMobOneTimeAction` still keeps additional one-shot families behind vehicle-id tables instead of raw action names alone. Focused regression coverage now lives in `UnitTest_MapSimulator/TamingMobActionFrameOwnerParityTests.cs` for cached Mechanic generic-to-`tank_*` resolution, ordinary-mount `ladder -> ladder2` alias loading, and refusal to treat missing `ride2` transitions as generic stand frames. The remaining gap is narrower: the simulator still does not mirror the client's `190xxxx` multi-source `load_tamingmob_action(..., b[18], b[19], b[20])` merge path that feeds `MergeTamingMobSprite`, it still does not own a proven tired-state driver equivalent to the client's `bTamingMobTired` remap to action `55`, and the broader vehicle-id tables behind `IsAbleTamingMobOneTimeAction` for Wild Hunter jaguars and other special rides are not yet modeled as explicit item-id action whitelists beyond the currently covered Mechanic family and WZ-backed alias/cache owner. | Mounted-avatar parity is now anchored to the same kind of explicit loader/cache seam already tracked for other actor families, so future taming-mob work can extend the vehicle-id-validated frame owner directly instead of scattering more action inference across assembler and runtime state code. The remaining differences are now specific client-only loader behaviors: multi-source sprite merging, tired remap ownership, and the wider special-vehicle one-time action tables. | mounted action-loader layer (`CActionMan::LoadTamingMobAction`, `IsAbleTamingMobAction`, `IsAbleTamingMobOneTimeAction`, `HaCreator/MapSimulator/Character/TamingMobActionFrameOwner.cs`, `HaCreator/MapSimulator/Character/CharacterLoader.cs`, `HaCreator/MapSimulator/Character/CharacterAssembler.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`, `UnitTest_MapSimulator/TamingMobActionFrameOwnerParityTests.cs`, `Character/TamingMob/01902000`, `Character/TamingMob/01932016`) |


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Remote actor foundation pass:
   Add a real `CUserPool`-style remote-user actor layer before continuing to extend wedding, Ariant, Battlefield, minimap-helper, and follow-request overlays independently.
3. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
4. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
5. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
6. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".

