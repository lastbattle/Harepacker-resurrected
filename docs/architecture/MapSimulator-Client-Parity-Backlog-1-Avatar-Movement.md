# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CAnimationDisplayer::Effect_ByItem` at `0x44f330`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Avatar and player parity

- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::SetActivePet` at `0x8e40e0`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CAnimationDisplayer::Effect_ByItem` at `0x44f330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::Update` at `0x937330`

Notes:
`CUser::LoadLayer` and `CUser::PrepareActionLayer` are the clearest resolved avatar-composition seams for layer loading and action-layer setup. `CUser::Update` and `CUser::UpdateMoreWildEffect` also show that the client maintains several additional avatar/effect layers outside the base assembler path, while `CUserLocal::Update` contains the local-player emotion reset path and action/state transitions that matter for expression and rare-action parity. A later IDA pass also confirms that remote avatar ownership is a first-class client surface rather than a loose extension of local-player logic: `CUserPool::OnPacket` dispatches enter, leave, common, remote, and local user packet families, `CUserPool::Update` advances local plus remote users and shared social-effect state, `CUserRemote::Update` owns remote move-action, effect, portable-chair, and prepared-skill presentation, and `CAnimationDisplayer::Effect_ByItem` confirms that one-shot item effects are managed by a dedicated client animation-owner seam rather than only by feature-local overlay code.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Avatar and player parity

These are the biggest remaining gaps for visible client parity.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Default avatar selection | The simulator still keeps the deterministic, WZ-verified SuperGM/Wizet fallback selection of `1002140` (Wizet Invincible Hat), `1042003` (Wizet Plain Suit), `1062007` (Wizet Plain Suit Pants), `1072005` (Leather Sandals), `1092008` (Pan Lid), and `1322013` (Wizet Secret Agent Suitcase) with matching face, hair, level, and SuperGM job metadata, but the login-owned avatar/bootstrap seam is broader again. `AvatarLook` decode still validates gender, skin, face, hair, visible equip bodyparts, hidden equip lists, pet ids, and applies the packet-owned `weaponStickerID` visual through the normal weapon render seam; `/loginpacket`, the inbox, raw opcode-framed scripts, and the built-in `LoginOfficialSessionBridgeManager` can all feed the same packet-owned login handlers, including live encrypted login traffic mirrored through Maple crypto instead of only manual hex/Base64 replay; `SelectWorldResult`, `ViewAllCharResult`, `SelectCharacterResult`, `SelectCharacterByVACResult`, `ExtraCharInfoResult`, `CheckDuplicatedIdResult`, `CreateNewCharacterResult`, and `DeleteCharacterResult` now all keep packet-owned roster or avatar ownership in the active path; `SelectCharacterByVACResult` decodes through the dedicated VAC codec and mirrors the distinct `CLogin::OnSelectCharacterByVACResult` failure table, title-return branches, and website-handoff prompts backed by `Login.img/Notice/text/26` and `27`; `CheckPasswordResult`, `GuestIdLoginResult`, and `AccountInfoResult` keep packet-authored account bootstrap on the client-owned chain confirmed from `CLogin::OnCheckPasswordResult` / `CLogin::OnGuestIDLoginResult` / `CLogin::OnAccountInfoResult`; `CreateNewCharacterResult` plus `DeleteCharacterResult` mirror the client-owned notice/error tables from `CLogin::OnCreateNewCharacterResult` / `CLogin::OnDeleteCharacterResult`, including the exact `CLoginUtilDlg::{Error,Notice}` ids for the mapped failure cases plus the client-decoded direct-notice StringPool texts for ids `0xFD9` and `0xFD4`; and the website-handoff branch is no longer simulator-local only. The existing notice seam now preserves a concrete handoff target, prefers packet-owned guest-registration URLs when `GuestIdLoginResult` carries one, falls back to the homepage handoff target for `CheckPasswordResult` / `AccountInfoResult` / `SelectWorldResult` / `SelectCharacterByVACResult` website branches, and opens the external browser from the existing `BtHomePage` / `BtNexon` ownership path instead of stopping at a simulator-only confirmation notice. WZ re-checks of `Etc/MakeCharInfo.img/000/{male,female}` kept the non-sale starter request layout pinned to the client-owned `0..7` face/hair/skin/hairColor/coat/pants/shoes/weapon families, and the simulator still uses that same shape to inject live `CheckDuplicatedId`, non-sale `SendNewCharPacket`, and delete requests back out through the existing login proxy instead of only synthesizing local success packets. The remaining gap is narrower and explicit: this still depends on the simulator owning a proxy-attached authenticated Maple login session rather than attaching in place to an already-running client socket; the website / license service branches can now hand off to a real external browser target but still do not complete the broader authenticated client-owned service flow; and the rest of the wider `CLogin::OnPacket` surface still needs explicit decode and runtime ownership beyond the currently supported packet families. | Default avatars and login previews are no longer owned only by direct preset construction or simulator-authored roster clones: the same `AvatarLook` seam now accepts decoded `GW_CharacterStat` and `AvatarLook` ownership from replayed or live-proxied login traffic, while packet-authored account bootstrap can stay inside the client-shaped ownership chain through `CheckPasswordResult` / `GuestIdLoginResult` / `AccountInfoResult` before roster, preview, and selected-character ownership continue through the selector/result seams. That ownership chain now also keeps packet-authored duplicate-name, create-character, and delete-character follow-up on the same live authenticated request/response seam when a proxied login session is present, surfaces the original transfer-lock strings from client `StringPool` ids `0xFD9` and `0xFD4`, and now carries concrete website-handoff ownership far enough to launch the external target the packet or homepage seam named instead of collapsing every website branch into simulator-only prose. That is materially closer to the client's `OnCheckPasswordResult` / `OnAccountInfoResult` / `OnGuestIDLoginResult` / `OnSelectWorldResult` / `OnViewAllCharResult` / `OnSelectCharacterResult` / `OnSelectCharacterByVACResult` / `OnCheckDuplicatedIDResult` / `OnCreateNewCharacterResult` / `OnDeleteCharacterResult` -> `GW_CharacterStat::Decode` -> `AvatarLook::Decode` ownership than the earlier simulator-owned bootstrap shortcuts. | `CharacterLoader.LoadDefaultMale`, `CharacterLoader.LoadDefaultFemale`, `CharacterLoader.LoadFromAvatarLook`, `CharacterAssembler`, `LoginAvatarLookCodec`, `LoginCheckPasswordResultCodec`, `LoginGuestIdLoginResultCodec`, `LoginSelectWorldResultCodec`, `LoginViewAllCharResultCodec`, `LoginSelectCharacterResultCodec`, `LoginSelectCharacterByVacResultCodec`, `LoginExtraCharInfoResultCodec`, `LoginCreateNewCharacterResultCodec`, `LoginAccountDialogPacketCodec`, `LoginPacketScriptCodec`, `LoginPacketInboxManager`, `LoginOfficialSessionBridgeManager`, `LoginOfficialSessionBridgeManager.{TrySendCheckDuplicateIdRequest,TrySendNewCharacterRequest,TrySendDeleteCharacterRequest}`, `MapSimulator.InitializeLoginCharacterRoster`, `MapSimulator.{ApplyCheckDuplicatedIdResultProfile,ApplyCreateNewCharacterResultProfile,ApplyDeleteCharacterResultProfile,BuildCreateNewCharacterPacketFailureMessage,BuildDeleteCharacterPacketFailureMessage}`, `MapSimulator.TryApplyPacketOwnedSelectCharacterResult`, `MapSimulator.TryApplyPacketOwnedSelectCharacterByVacResult`, `MapSimulator.TryHandlePacketOwnedCheckPasswordResult`, `MapSimulator.TryHandlePacketOwnedGuestIdLoginResult`, `MapSimulator.TryHandlePacketOwnedAccountInfoResult`, `MapSimulator.{TrySendLiveOfficialCheckDuplicatedIdRequest,TrySendLiveOfficialCreateCharacterRequest,TrySendLiveOfficialDeleteCharacterRequest,ExecuteLoginCharacterCreateConfirmation,PrepareLoginWebsiteHandoff,TryOpenLoginWebsiteHandoff}`, `MapSimulator.TryConfigureLoginPacketPayload`, `MapSimulator.DispatchLoginRuntimePacket`, `LoginCharacterRosterEntry.CreateRuntimeBuild`, `CharacterConfigManager.CreateDefaultMalePreset`, `CharacterConfigManager.CreateDefaultFemalePreset`, `LoginClientDirectNoticeText`, `LoginWebsiteHandoffResolver` (`CUser::LoadLayer`, `CUIAvatar::CUIAvatar`, `CLogin::OnPacket`, `CLogin::OnCheckPasswordResult`, `CLogin::OnAccountInfoResult`, `CLogin::OnGuestIDLoginResult`, `CLogin::OnSelectWorldResult`, `CLogin::OnViewAllCharResult`, `CLogin::OnSelectCharacterResult`, `CLogin::OnSelectCharacterByVACResult`, `CLogin::OnExtraCharInfoResult`, `CLogin::OnCheckDuplicatedIDResult`, `CLogin::OnCreateNewCharacterResult`, `CLogin::OnDeleteCharacterResult`, `CLoginUtilDlg::{Error,Notice}`, `CUtilDlg::Notice`, `StringPool::GetString`, `CLogin::SendCheckDuplicateIDPacket`, `CLogin::SendNewCharPacket`, `CLogin::SendDeleteCharPacket`, `GW_CharacterStat::Decode`, `AvatarLook::Decode`, `Login.img/Title/BtHomePage`, `Login.img/Notice/BtNexon`, `Login.img/Notice/text/{26,27}`, `Etc/MakeCharInfo.img/000/{male,female}`) |
| Implemented | Action coverage | Character-part loading now preserves the existing core action order and appends additional WZ action nodes discovered on each part instead of stopping at the old fixed lists | Uncommon body/equipment poses can now load when the source IMG exposes them, which closes the loader-side empty-state gap for rare, scripted, and death-adjacent actions | `CharacterLoader.StandardActions`, `CharacterLoader.AttackActions` (`CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUserLocal::SetMoveAction`) |
| Implemented | Death / rare action frames | Runtime action selection now carries raw one-shot action names through to `CharacterAssembler` instead of forcing uncommon actions back through the limited enum-only render path | Loaded death and other rare WZ actions can now survive from selection to final frame assembly instead of degrading immediately to generic fallbacks | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler` |
| Implemented | Facial expression behavior | The local player runtime now owns blink cadence and short-lived hit expressions, and updates the assembler expression state when those transitions occur | The avatar now reacts over time instead of staying pinned to the static default face | `CharacterAssembler.GetFaceFrame`, `CharacterLoader.LoadFaceExpressions`, `PlayerCharacter` (`CUserLocal::Update` emotion reset path) |
| Implemented | Equip anchor fallback rules | Equipment alignment now resolves through explicit shared map-point precedence instead of using literal pixel nudges when preferred anchors are missing | Parts stay render-safe while remaining tied to WZ anchor data rather than ad hoc offsets | `CharacterAssembler.CalculateEquipOffset`, `CharacterAssembler.TryCalculateHeadEquipOffset` |
| Implemented | Client-verified fallback order | Action lookup now prefers exact action names, then raw-action family reductions, then swim/fly aliasing, before the final `stand1` escape hatch | Character assembly now follows a documented action-family precedence order instead of jumping straight from miss to generic standing frames | `CharacterAssembler`, `CharacterPart.FindAnimation` (`CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`) |
| Partial | Morph avatar pipeline parity | MapSimulator still resolves concrete morph template ids from both `common/morph` and `PVPcommon/morph` into `Morph/*.img`, loads those images as a dedicated avatar render owner, resolves UOL-linked morph frames, preserves direct per-frame anchors like `head`, backfills missing branches through same-family base IMG fallback (`id -> id0 -> id00 -> id000`), the observed paired `100x <-> 110x` families, and WZ-backed `info/link` inheritance, and routes hostile polymorph mob skills plus the current flag-only `info/morph = 1` skill outliers through that same avatar-owner seam. The transform runtime still keeps ladder, rope, swim, fly, superman `ladder2` / `rope2` / `fly2`, airborne `fly2Move` / `fly2Skill`, authored combat, hit, death, sit, and jump-special branches on morph assets instead of degrading immediately to locomotion. This run narrows the remaining resolver seam further without widening it beyond WZ evidence: `MorphClientActionResolver` now treats generic `shot*` inputs such as the client-shaped `shotC1` family as attack aliases alongside `attack*` / `stab*` / `swing*` / `shoot*`, so archer morphs can promote authored WZ branches like `Morph/1003.img/{windshot,windspear,arrowRain}` and `Morph/1103.img/windshot` before falling back to raw generic names, and the resolver now suppresses duplicate aliases across its authored-family and generic fallback passes instead of relying on callers to dedupe later. Focused resolver coverage is now checked in for this row in `UnitTest_MapSimulator/MorphClientActionResolverTests.cs`, including pirate, archer, ice, jump-special, alert-family, duplicate-suppression, and new `shotC1` cases. WZ was rechecked again for this row (`Morph/1000.img`, `1001.img`, `1003.img`, `1100.img`, `1101.img`, `1103.img`, `2001.img`, `2002.img`, `2003.img`), and no further safe non-WZ alias additions surfaced without the missing client table. Remaining gap: the simulator still does not mirror every client-only morph alias that may sit behind the raw `CActionMan::LoadMorphAction` table, especially action-code-to-name remaps not surfaced by explicit WZ branch names or the now-promoted authored combat, alert, jump-special, and `shot*` families; direct execution of the new resolver tests is also still blocked in this workspace by unrelated pre-existing `HaCreator` build errors in `PacketFieldStateRuntime.cs` and `FieldSkillRestrictionEvaluator.cs`, so broader non-WZ alias expansion remains deferred until more client evidence is available. | Morph-heavy jobs, the confirmed hostile morph mob-skill pair including the higher `173` level that omits `x`, the currently known flag-only morph buffs, and the authored ladder/rope/fly/swim, combat, hit, death, sit, alert, and jump-special branches all stay on the dedicated morph-owner path rather than ordinary body/equip assembly or status-only metadata. This run closes the remaining WZ-backed crossbow-style attack hole inside that seam: generic raw names like `shotC1` now reach authored archer morph combat branches instead of bypassing them, and resolver consumers no longer see duplicate alias entries when the same combat or alert branch is discoverable through both the family-specific pass and the broader fallback walk. The checked-in tests now pin those alias-ordering and duplicate-suppression rules directly at the resolver seam; remaining work is limited to any extra client-only morph alias-table cases if later IDA work proves them. | avatar transform/runtime assembly (`CActionMan::GetMorphImgEntry`, `CAvatar::MoveAction2RawAction`, `CActionMan::LoadMorphAction`, `PlayerCharacter.CreateMorphTransform`, `MorphClientActionResolver`, `UnitTest_MapSimulator/MorphClientActionResolverTests.cs`, `MobSkill.img/172/level/1/x`, `MobSkill.img/173/level/{1,2}`, `Skill/001.img/0010109/info/morph`, `Skill/2002.img/20020111/info/morph`, `Morph/0006.img/sit`, `Morph/0109.img`, `Morph/0111.img`, `Morph/1000.img/{info/superman,alert2,alert3,alert4,alert5,fist,recovery,sit,timeleap,shockwave,eburster,edrain,dragonstrike,eorb,ladder2,rope2,fly2,fly2Move,fly2Skill}`, `Morph/1001.img/{alert2,alert3,alert4,demolition,snatch}`, `Morph/1003.img/{alert2,alert4,windshot,windspear,stormbreak,arrowRain,archerDoubleJump,shoot1,shootF}`, `Morph/1100.img/{recovery,timeleap,shockwave,eburster,edrain,dragonstrike,eorb}`, `Morph/1101.img/{demolition,snatch}`, `Morph/1103.img/{windshot,windspear,archerDoubleJump,shoot1,shootF}`, `Morph/2001.img/{icemanAttack,icemandoubleJump}`, `Morph/2002.img/{iceAttack1,iceAttack2,iceDoubleJump,iceSmash,iceTempest,iceChop,icePanic,dead,pvpko}`, `Morph/2003.img/sit`) |
| Partial | Mount / vehicle / mechanic mode rendering | `TamingMob` assembly now preserves exact ride frames for normal mounts, falls back to seated passenger variants when the asset only exposes `sit`, equips ride parts for skills that publish an `eventTamingMob` mount id, routes mechanic vehicle skills onto the shared ride layer while keeping that mount owned across both direct and timer-driven tank-to-siege return transitions, treats the shared Mechanic mount (`Character/TamingMob/01932016`) as the frame-timing and visibility owner for vehicle-only actions such as `tank_*`, `siege_*`, `flamethrower*`, `rbooster*`, `tank_laser`, and other mount-owned mechanic actions confirmed in WZ, auto-equips that shared Mechanic ride whenever a Mechanic skill `action`, `prepare`, `keydown`, `keydownend`, or any additional authored `action` entry maps onto an exact or `tank_`-prefixed mount animation, routes the client-owned Mechanic vehicle toggle buff itself through that same mount seam so `Skill/3500.img/35001002` (`Mech: Prototype`) equips `01932016` even though WZ only publishes vehicle-type buff metadata plus the mount/unmount description and no action node, mirrors the client's explicit vehicle enter/exit ownership surface more closely by auto-detecting non-skill `TamingMob` equip or unequip changes, playing WZ-backed `ride2` / `getoff2` transitions when the active mount exposes them, and preserving the departing mount as a transient render owner so `getoff2` can complete even after the equipment slot has already been cleared. This pass keeps the remaining WZ-authored client-owned vehicle surface on one shared classifier seam as well: the non-`type = 13` timed invisible ride buffs remain recognized through the same helper in `SkillManager`, `PlayerSkillStateRestrictionEvaluator`, and `FieldSkillRestrictionEvaluator`, the short vehicle-valid cancel / handoff window in `SkillManager` now consults that shared classifier seam for the broader currently confirmed vehicle surface instead of a narrower hard-coded subset, and the same ownership resolver now also keeps direct Mechanic mounted-action families on that shared tracking path so cancel-driven or timer-driven ownership handoff no longer quietly drops back to buff-only or fixed-state cases. The same seam now claims the broader currently confirmed Battleship-only mounted action family through both direct mount action names and WZ text evidence: `Skill/522.img/5221007/action/0 = cannon`, `Skill/522.img/5221008/action/0 = torpedo`, `Skill/521.img/5211004/action/0 = fireburner`, `Skill/521.img/5211005/action/0 = coolingeffect`, and `String/Skill.img/5221006/desc` listing Flamethrower and Ice Splitter among the skills available while aboard the Battleship. It now also treats the rest of that same WZ-authored Battleship support list as vehicle-valid for the short ownership-handoff seam, so `Grenade`, `Gun Booster`, `Octopus`, `Gaviota`, `Maple Warrior`, and `Hero's Will` normalize back onto the Battleship owner instead of breaking the grace-window path when the ship is still the active vehicle state. It also continues to route direct Mechanic mounted action families such as `flamethrower*`, `rbooster*`, `alert3`, and the broader `tank_*` / `siege_*` set through that same shared mounted-state classifier so render ownership, airborne or ladder-start restrictions, taming-mob-disabled field checks, and client-owned vehicle tracking stop disagreeing about whether those skills are mounted, and it still reconstructs the shared Mechanic ride for the WZ-authored fixed-state vehicle modes whose client ownership does not come from a standing buff: `Skill/3511.img/35111004/action/0 = siege_pre`, `Skill/3512.img/35121005/action/0 = tank_pre`, `Skill/3512.img/35121013/action/0 = tank_siegepre`, their `info/type = 10` fixed-state metadata, and the matching `String/Skill.img` descriptions for Siege Mode and Missile Tank. Focused regression coverage now exercises the shared classifier and `SkillManager`'s client-owned vehicle tracking helper for the ride-description buff family, the WZ-listed Battleship support family, Battleship mounted attack families, and direct Mechanic vehicle-action skills, while the `Unable_To_Use_Taming_Mob` field gate remains covered for the same mounted-action surface. The remaining gap is narrower again: the simulator still does not consume the rest of the client's broader `SetMoveAction` / `Update` vehicle-valid catalog beyond these WZ-authored vehicle buffs, the known toggle-skill handoff ids, the recognized invisible ride-description buff family, direct equip transitions, the now-confirmed Battleship mounted and support family, the WZ-authored fixed-state Mechanic mode family, the direct Mechanic mounted-action family, and the existing Mechanic ownership path, especially client-only vehicle state sources that do not surface an active buff, a short handoff window, or a renderable current mount through the existing seams. | Event ride buffs, mechanic transforms, direct mechanic vehicle-action skills, WZ-backed fixed-state Mechanic mode skills such as Siege Mode, Missile Tank, and Tank Siege Mode, additional WZ-backed mechanic actions like `alert3` and tank-prefixed summon or rush variants, the basic `Mech: Prototype` mount-toggle buff, non-skill mount equip or teardown swaps that expose `ride2` / `getoff2`, the broader currently authored client-owned `type = 13` vehicle-buff family, the wider invisible ride-description buff family whose WZ omits `info/type` but still publishes timed ride ownership through `invisible` plus ride text, and the Battleship-only `cannon`, `torpedo`, `fireburner`, and `coolingeffect` action family whose WZ action nodes plus `String/Skill.img/5221006/desc` mark them as ship-owned actions now transition the avatar onto the real `TamingMob` asset instead of leaving the player visually unmounted. The shared seam also now treats the remaining WZ-listed “available while aboard the Battleship Esupport family as vehicle-valid for ownership handoff, so explicit Corsair support skills such as `5201002`, `5201003`, `5211001`, `5211002`, `5221000`, and `5221010` keep the existing ship owner instead of collapsing the short client-owned tracking window onto a non-owner support skill id. Remaining work is limited to broader client-managed vehicle state sources outside these detected ownership paths, especially client-only update or move-action cases and any remaining non-WZ vehicle-valid support families that still need explicit client evidence before adding more non-WZ mount ownership remaps or non-equipped ride grants. | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler`, `SkillLoader`, `SkillManager`, `PlayerSkillStateRestrictionEvaluator`, `FieldSkillRestrictionEvaluator`, `ClientOwnedVehicleSkillClassifier`, `UnitTest_MapSimulator/ClientOwnedVehicleSkillTrackingTests.cs` (`CAvatar::MoveAction2RawAction`, `CUser::Update`, `Character/TamingMob/01932016`, `Character/TamingMob/01932016/{ride2,getoff2}`, `Character/TamingMob/01932000/{cannon,torpedo,fireburner,coolingeffect}`, `Skill/3300.img/33001001/info/type`, `Skill/3500.img/35001002/{common,info}`, `Skill/3511.img/35111004/{action,info}`, `Skill/3512.img/35121005/{action,info,req}`, `Skill/3512.img/35121013/{action,info}`, `Skill/520.img/5201002`, `Skill/520.img/5201003`, `Skill/521.img/5211001`, `Skill/521.img/5211002`, `Skill/521.img/5211004/action/0`, `Skill/521.img/5211005/action/0`, `Skill/522.img/5221000`, `Skill/522.img/5221006/info/type`, `Skill/522.img/5221007/{action,req}`, `Skill/522.img/5221008/{action,req}`, `Skill/522.img/5221010`, `Skill/000.img/0001016`, `Skill/000.img/0001069`, `Skill/1000.img/10001017`, `Skill/8000.img/80001016`, `String/Skill.img/33001001`, `String/Skill.img/35001002`, `String/Skill.img/35111004`, `String/Skill.img/35121005`, `String/Skill.img/35121013`, `String/Skill.img/5201002`, `String/Skill.img/5201003`, `String/Skill.img/5211001`, `String/Skill.img/5211002`, `String/Skill.img/5211004`, `String/Skill.img/5211005`, `String/Skill.img/5221000`, `String/Skill.img/5221006`, `String/Skill.img/5221007`, `String/Skill.img/5221008`, `String/Skill.img/5221010`, `String/Skill.img/0001016`, `String/Skill.img/0001069`, `String/Skill.img/10001017`, `String/Skill.img/80001016`, `String/Skill.img/5221006/desc`, `Skill/3500.img/35001001/action/0`, `Skill/3510.img/35101003/action/0`, `Skill/3510.img/35101005/action/0`, `Skill/3510.img/35101006/action/0`, `Skill/3510.img/35101009/{action,prepare,keydown,keydownend}`, `Skill/3511.img/35111015/action/0`, `Skill/3512.img/35121012/action/0`, `Skill/3511.img/35111013/action/0`, `Skill/3512.img/35121007/action/0`) |
| Partial | Skill-specific avatar transforms | Mechanic-mode skills now activate dedicated avatar action families (`tank_*`, `siege_*`, `tank_siege*`) from `action/0` nodes, prefer transform-specific jump / ladder / rope / fly / swim / hit branches when those movement variants exist on the avatar, play the corresponding `_after` exit actions when those transforms clear, the prepare-skill flamethrower family now stays on its `flamethrower` / `flamethrower2` avatar branches until release triggers `flamethrower_after` / `flamethrower_after2`, and Rocket Booster now keeps its WZ-backed ground startup on `rbooster_pre` while airborne states promote onto the dedicated `rbooster` body branch, still preserving the mechanic tank-specific `tank_rbooster_pre` / `tank_rbooster_after` variants when the booster is fired out of an active tank transform. Cyclone now promotes its `cyclone_pre` cast into the persistent `cyclone` avatar branch until buff expiry triggers `cyclone_after`, `Monkey Wave` now promotes `noiseWave_pre` into the sustained `noiseWave_ing` avatar branch before release plays the body `noiseWave` action from the WZ `action/0` node, Wild Hunter `Swallow` now uses the client-confirmed body-only `swallow_pre` branch for its initial prepare windup before holding on `swallow_loop` and clearing into `swallow`, Wild Hunter `More Wild` now keeps the body on the dedicated `wildbeast` avatar branch for the full buff window exposed by `Skill/3312.img/33121006/action/0`, Evan dragon-breath charge skills now hold `icebreathe_prepare` / `breathe_prepare` until release resolves onto `dragonIceBreathe` / `dragonBreathe`, and the shared cast-start seam now routes non-keydown prepared skills through that same resolved prepare-avatar path so body-backed `prepare/action` branches no longer briefly start on `action/0` before the hold window begins. Dual Blade charge skills `Final Cut` and `Monster Bomb` now keep the avatar on `finalCutPrepare` / `monsterBombPrepare` for the hold window before release resolves onto `finalCut` / `monsterBombThrow`, Resistance `Dual Vulcan` now promotes `dualVulcanPrep` into the sustained `dualVulcanLoop` avatar branch until release triggers `dualVulcanEnd`, Night Walker `Poison Bomb` now replaces its WZ-only `dash` prepare and dummy keydown branch with the confirmed body-backed `darkTornado_pre` -> `darkTornado` -> `darkTornado_after` avatar family exposed on `Character/00002000.img`, and raw WZ-owned `dash` prepare requests that still reach the transform seam now normalize onto that same `darkTornado_pre` body branch instead of skipping straight to `darkTornado`. The generic prepared-skill hold path now falls back from missing avatar-only `prepare/action` names onto renderable release-family branches when WZ and the loaded body disagree, and the matching keydown-release path now resolves missing avatar-only `keydownend/action` names onto renderable release-family branches before clearing. That closes the confirmed prepared-skill exit regression on body-backed families such as the Demon blunt-smash charge line (`Skill/3100.img/31001000` / `Skill/3110.img/31101000` -> `Character/00002000.img/bluntSmash`) instead of letting `bluntSmashEnd` / `soulEater_end` fall back to a stand pose after the hold. This pass now also checks in the focused regression coverage the row depends on: `UnitTest_MapSimulator/PreparedSkillAvatarTransformParityTests.cs` directly guards the confirmed `darkTornado_pre` and `swallow_pre` prepare remaps, the Demon charge-release fallback onto renderable `bluntSmash` when WZ requests either `bluntSmashEnd` or `soulEater_end`, and the Rocket Booster startup-to-airborne transform seam on both normal and tank launches. WZ was rechecked again for this row (`Skill/1411.img/14111006/prepare/action = dash`, `Skill/3310.img/33101005/prepare/action = swallow_loop`, `Skill/3100.img/31001000/keydownend/action = bluntSmashEnd`, `Skill/3110.img/31101000/action/0 = bluntSmash`, `Skill/3110.img/31101000/keydownend/action = soulEater_end`, while `Character/00002000.img` still exposes `darkTornado_pre`, `swallow_pre`, `bluntSmash`, `rbooster_pre`, `rbooster`, and `tank_rbooster_pre` but not body-backed `bluntSmashEnd`, `soulEater_end`, or `soulEater` branches). `Swallow` remains the only currently confirmed client-side prepare override in this area from the inspected `CUserLocal::DoActiveSkill_Prepare` seam, and no further WZ-safe remaps surfaced beyond the already implemented families. The remaining gap is limited to candidate client-only prepared or transform families whose bodies expose plausible dedicated branches but whose remaps or exit branches are still not named in WZ and are not otherwise confirmed by the inspected client path, so they remain intentionally unmapped until more client evidence is found. | Mechanic avatar movement now stays on transform-family motion frames when the loaded body actually exposes them instead of collapsing every non-walk state back to the stand pose, while flamethrower, Rocket Booster, Cyclone, Monkey Wave, Wild Hunter Swallow, Wild Hunter More Wild, Evan dragon-breath charges, Dual Blade `Final Cut` / `Monster Bomb`, Battle Mage `Dual Vulcan`, Night Walker `Poison Bomb`, and the confirmed Demon prepared-skill charge families now keep both their visible hold windows, their non-keydown prepare starts, any raw WZ `dash` prepare replay that still reaches the avatar-transform seam, and any renderable release clear on body-backed avatar branches rather than dropping back to generic standing when WZ publishes a missing or non-renderable `prepare/action` or `keydownend/action`. This row now has checked-in direct regression coverage for the confirmed prepared-skill remaps, the Demon release-family fallback onto `bluntSmash`, and Rocket Booster's ground-startup to airborne-body transition; broader transform parity remains incomplete only for the remaining client-shaped remaps whose final avatar family is not named directly in WZ or confirmed in the inspected client path. | `SkillManager`, `PlayerCharacter.TriggerSkillAnimation`, `CharacterAssembler`, `UnitTest_MapSimulator/PreparedSkillAvatarTransformParityTests.cs` (`14111006`, `22121000`, `22151001`, `23121000`, `31001000`, `31101000`, `31101002`, `32121003`, `33101005`, `33121006`, `4341002`, `4341003`, `5311002`, `35001001`, `35101009`, `35101004`, `35121005`, `35111004`, `35121013`, `CAvatar::MoveAction2RawAction`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::TryDoingSwallowBuff`, `Skill/1411.img/14111006/{prepare,keydown}`, `Skill/2212.img/22121000/{prepare,action}`, `Skill/2215.img/22151001/{prepare,action}`, `Skill/2312.img/23121000/{prepare,keydown,keydownend}`, `Skill/3100.img/31001000/{prepare,action,keydownend}`, `Skill/3110.img/31101000/{prepare,action,keydownend}`, `Skill/3110.img/31101002/{prepare,action}`, `Skill/3310.img/33101005/prepare/action`, `Skill/3312.img/33121006/action/0`, `Skill/3510.img/35101004/action/0`, `Skill/434.img/4341002/{prepare,action}`, `Skill/434.img/4341003/{prepare,action}`, `Skill/531.img/5311002/{prepare,keydown,action}`, `Character/00002000.img/{darkTornado_pre,darkTornado,darkTornado_after,icebreathe_prepare,dragonIceBreathe,breathe_prepare,dragonBreathe,bluntSmash,demonTrace,dualVulcanPrep,dualVulcanLoop,dualVulcanEnd,finalCutPrepare,finalCut,monsterBombPrepare,monsterBombThrow,rbooster_pre,rbooster,tank_rbooster_pre,tank_rbooster_after,swallow_pre,swallow_loop,swallow,wildbeast}`) |
| Partial | Client-managed avatar effect layers | The simulator now loads persistent avatar-bound skill layer branches from WZ (`special`, `special0`, `finish`, `finish0`, ladder-aware `back` / `back_finish`, and `repeat` on `suddenDeath` skills), attaches them to the player render path as over-character / under-face layers, switches the `More Wild` family onto its ladder override, plays finish cleanup on buff expiry or cancellation, routes `doubleJump` action-family movement skills onto a client-style transient avatar-owned layer instead of the generic cast sprite, extends that same transient seam across the broader non-buff movement families the existing resolver already classifies (`Rush`, backstep, bound-jump / assaulter / flash, and flying-rush variants) so WZ-backed movement casts like `Skill/122.img/1221007/{effect,effect0}`, `Skill/433.img/4331005/{effect,effect0}`, and `Skill/3001.img/30010186/effect` suppress the detached cast sprite and keep authored planes bound to the avatar for the movement window, now also honors movement or buff shapes that only publish the secondary `effect0` branch by treating `effect` and `effect0` as equivalent client-owned sources for promotion instead of requiring the primary branch to exist, promotes fly/flying buff `effect` branches onto looping player-owned avatar layers so wing visuals stay attached to the avatar for the full buff window instead of rendering as detached cast sprites, promotes Dark Sight-family `effect` branches such as `Skill/400.img/4001003/effect` and `Skill/1400.img/14001003/effect` onto the same looping avatar-owned layer path instead of leaving them as detached cast sprites, promotes invisible swallow buff `effect` branches such as `Skill/3310.img/33101006/effect` onto a client-style under-face avatar layer that now hides while the player is on a ladder or rope, now preserves sibling `effect0` branches on transient client-owned movement layers as an extra avatar-bound plane instead of dropping them, also promotes root-`invisible` buff skills whose WZ publishes only timed `effect/effect0` planes without `affected` or `special` branches, such as `Skill/000.img/0001037/{effect,effect0}`, onto the same looping avatar-owned path instead of leaving them detached, also promotes WZ-backed damage-reflect buff visuals that publish timed `effect/effect0` without `affected` or `special` branches, such as `Skill/110.img/1101007`, `Skill/120.img/1201007`, and `Skill/3110.img/31101003`, onto looping avatar-owned layers while still suppressing those client-owned avatar layers whenever Oak Barrel-style morph buffs or full-body Mechanic ride actions own the sprite, now extends that same looping avatar-owned fallback to the broader timed-buff WZ shape where a self-buff only publishes `effect/effect0` companion planes without `affected` or `special*`, no longer mirrors a lone promoted plane onto both avatar planes when WZ only authored one side, which keeps single-plane `effect`-only or `effect0`-only visuals on their authored z side instead of double-drawing them across under-face plus overlay, and now also treats `affected` and `affected0` as the same exclusion signal for those timed-buff fallbacks so a secondary-only active aura cannot be mistaken for a missing `affected` branch and incorrectly re-promoted through the client-owned `effect/effect0` path. This pass also widens the smaller `CUser::Update`-owned affected-area seam that IDA confirmed through `CUser::UpdateAr01Effect`: while the player stands inside a qualifying friendly player-skill affected area that can apply to the local player and WZ publishes a looping `affected` or `affected0` branch, the simulator now tracks that area instance by object id, keeps both planes when both siblings exist, and promotes those authored `affected` / `affected0` animations onto the avatar-owned under-face plus overlay seam instead of leaving the local actor without the client-owned aura or silently dropping the secondary plane. That keeps the confirmed `Amplifier Robot AF-11` (`Skill/3512.img/35121010/affected`) surface, preserves dual-plane authored auras such as `Skill/000.img/0000086/{affected,affected0}`, and now also covers the broader friendly support-summon area shape the resolver can identify from WZ, including party-owned Mechanic support robots such as `Healing Robot H-LX` (`Skill/3511.img/35111011/affected`), alongside the existing mass-spell or support-zone affected-area families instead of hardcoding only the single `AR01` skill id. The runtime also now keeps sibling `affected0` planes alive on ordinary looping buff visuals such as `Skill/532.img/5320007/{affected,affected0}` and `Skill/572.img/5720005/{affected,affected0}` instead of discarding the secondary authored layer, and it no longer incorrectly binds Energy Charge-family `special` layers on initial buff activation: WZ marks those skills with `info/energyCharge = 1` and `info/fullChargeEffect = bodyAttack`, while the client keeps a separate `m_tEnergyCharge`-owned runtime in `CUser::Update`, so the simulator now defers those full-charge avatar layers until a dedicated owner exists, tracks active Energy Charge buffs through that separate runtime seam, accumulates charge from successful local attack hits against the WZ-backed `common/x` threshold on `Skill/511.img/5110001` and `Skill/1510.img/15100004`, and only promotes the authored `special` layer once that inferred full-charge state is reached. The same IDA re-check of `CUser::Update` also shows the client suppressing several avatar-owned layers while `CAvatar::IsOnPlayingRotateAction` is active, so the simulator now marks the currently confirmed sudden-death / `Final Cut` repeat layer path and the flying-wing fallback path as rotate-sensitive and suppresses those avatar-owned effects during the rotate-style action family the current runtime can identify safely (`doublejump`, `backspin`, `spin`, `screw`, `somersault`, and explicit `finalCut`) instead of continuing to overdraw those layers through the spin window; that suppression now also consults the player runtime's forced one-time skill action, so client-owned layers hide during skill-driven rotate windows even when the visible current action has already fallen back to a benign standing or generic attack pose. The remaining gap is narrower again: broader client-only `CUser::Update` families beyond `More Wild`, swallow, double-jump, the existing movement or buff WZ shapes, and these currently promoted friendly `affected`-area ownership families still need explicit client-backed ownership rules before they can be mirrored safely; the exact raw-action catalog behind `CAvatar::IsOnPlayingRotateAction` still needs deeper client mapping beyond the current action-name-safe subset; and the Energy Charge runtime could still use deeper client confirmation if later IDA work proves a different accumulation cadence or extra non-visual full-charge behavior around `bodyAttack`. | Visible parity is closer for `More Wild`, `Final Cut`-style sudden-death buffs, the broader WZ-backed movement cast surface, flying-wing buffs, Dark Sight-family stealth buffs, root-`invisible` effect-only buffs, Wild Hunter swallow buffs, the confirmed damage-reflect buff family, the secondary-only `effect0` surface, the broader timed self-buff `effect/effect0` pair surface, the Energy Charge family, and now the client-owned friendly affected-area aura surface because the promoted avatar-owned layers now follow the client's ownership rules more closely, no longer require the primary `effect` branch to exist before promotion, no longer double-render single-plane authored visuals on both avatar planes, now also refuse to treat a WZ-authored `affected0` aura as “no affected branch Ewhen deciding whether timed buff `effect/effect0` planes should be rebound onto the avatar, now defer Energy Charge `special` layers until a separate full-charge owner exists instead of attaching them at cast start, now drive that Energy Charge owner forward on successful local hits toward the WZ-backed charge threshold, and now also preserve sibling `affected0` planes on looping buff / aura visuals while the player stands inside qualifying local- or party-owned player-skill affected areas such as `35121010`, `35111011`, and the same WZ-authored mass-spell or support-zone area shape, and now also stop overdrawing the currently confirmed sudden-death / `Final Cut` and flying-wing avatar-owned layers during rotate-style player actions the simulator can classify from the inspected `CUser::Update` seam. Rotate-sensitive avatar-owned layers also now inspect the same forced one-time action seam the player runtime uses for skill-driven attacks, which keeps those layers suppressed through the full rotate window instead of depending only on the coarser visible action name. The remaining work is narrower: broader `CUser::Update`-owned effect families that do not publish these WZ-authored `special*`, timed `effect/effect0`, `affected/affected0`, invisible/effect-only buff, movement-action, or the now-covered friendly affected-area shape still need explicit client-backed remaps before they can be mirrored safely, the exact client raw-action set behind `IsOnPlayingRotateAction` still needs a fuller mapping before widening rotate suppression further, and Energy Charge may still need deeper client-backed tuning if later evidence shows that its full-charge cadence or combat-side `bodyAttack` runtime differs from the current WZ-shaped accumulation seam. | `PlayerCharacter`, `PlayerManager`, `SkillLoader`, `SkillManager`, `CharacterAssembler`, `ClientOwnedAvatarEffectParity`, `UnitTest_MapSimulator/ClientOwnedAvatarEffectParityTests.cs` (`CUser::Update`, `CUser::UpdateMoreWildEffect`, `CUser::UpdateAr01Effect`, `CUserLocal::TryDoingSwallowBuff`, `CAvatar::IsOnPlayingRotateAction`, `Skill/3312.img/33121006/{special,special0,finish,finish0,back,back_finish}`, `Skill/434.img/4341002/repeat`, `Skill/400.img/4001003/effect`, `Skill/1400.img/14001003/effect`, `Skill/310.img/3101003/effect`, `Skill/3300.img/33001002/effect`, `Skill/122.img/1221007/{effect,effect0}`, `Skill/433.img/4331005/{effect,effect0}`, `Skill/3001.img/30010186/effect`, `Skill/3310.img/33101006/effect`, `Skill/000.img/0001037/{effect,effect0,invisible}`, `Skill/110.img/1101007/{effect,effect0}`, `Skill/120.img/1201007/{effect,effect0}`, `Skill/3110.img/31101003/{effect,effect0}`, `Skill/1411.img/14111000/effect0`, `Skill/510.img/5101007`, `Skill/511.img/5110001/{special,affected,common/x,info/energyCharge,info/fullChargeEffect}`, `Skill/1510.img/15100004/{special,affected,common/x,info/energyCharge,info/fullChargeEffect}`, `Skill/000.img/0000086/{affected,affected0}`, `Skill/532.img/5320007/{affected,affected0}`, `Skill/572.img/5720005/{affected,affected0}`, `Skill/3511.img/35111011/affected`, `Skill/3512.img/35121010/affected`, `Skill/3512.img/35121007/affected`, `Skill/3312.img/33121007/affected`, `Skill/3200.img/32001003/affected`, `Skill/3210.img/32101002/affected`, `Skill/3210.img/32101003/affected`) |
| Partial | Remote user / `CUserPool` actor parity | The simulator now has a first-class shared remote-user actor seam instead of only feature-local placeholders. `RemoteUserActorPool` owns remote AvatarLook decode to `CharacterBuild`, per-user insertion/removal, shared world draw, name lookup, chair and mount state, Battlefield team tagging, minimap helper marker registration, prepared-skill world overlays, a decoded movement-snapshot path that samples packet-style `CMovePath` data into per-actor position, facing, foothold, and action updates, packet-owned one-shot social item effects loaded from `Effect/ItemEff.img` through the existing character loader seam, and now also a shared `CUserRemote::OnAvatarModified`-shaped refresh path for packet `223`. `MapSimulator` continues to expose a `CUserPool::OnPacket`-shaped `/remoteuser packet` / `/remoteuser packetraw` dispatcher that routes supported enter/leave plus common-vs-remote packet families (`179/180/181/182/183/184/210/211/212/213`, the simulator-owned melee seam `214`, the simulator-owned item-effect seam `215`, and now avatar-modified `223`) through one owner instead of mutating remotes only through ad hoc feature commands, and the general enter-field owner still mirrors the existing Ariant handoff by applying packet-authored initial portable-chair state immediately after AvatarLook spawn rather than dropping the remote into a stand pose until a later chair packet arrives. The shared seam also now covers the next client-owned social/state slice that the decompile of `CUserRemote::OnAvatarModified` exposed: packet `223` can reload remote `AvatarLook` through the shared loader seam, apply the packet-owned remote speed scalar onto the actor build, clear or refresh couple, friendship, and marriage relationship overlays through the existing `Effect/ItemEff.img` owner instead of leaving those record updates trapped inside minigame-specific packet handlers, and retain the packet-owned carry-item-effect byte plus completed-set-item id in shared remote inspection/status state for later rendering work. The already-landed social effect work remains in place: WZ-backed couple, friendship, and wedding ring effects in `Effect/ItemEff.img` can be loaded as non-looping remote item effects, the pool preserves packet-authored partner targeting when it exists, resolves same-item relationship pairing when the client-style avatar-modified records only expose serial-backed item ownership, renders the owner-side branch on the remote actor, and resolves the larger shared branch once per pair at the midpoint between linked actors or between the remote actor and the local player when the packet names the local character. In the same loader seam, `Effect/ItemEff.img` entries whose primary animation is authored as flat root-level numbered `Canvas` or `UOL` frames, such as the New Year-style `2270002` and `2270003` branches that point back into `2270000`, now resolve their main authored branch first and only drop to the item-local `fail` branch when the primary branch is not actually playable. Wedding's feature-local groom, bride, and audience actors remain migrated onto the shared pool, and Messenger room occupants also continue to collapse onto that same owner with packet-authored `AvatarLook` rehydration for hidden members when present. This now matches more of the client evidence that `CUserPool::OnPacket` owns user enter/leave and common-vs-remote dispatch, `CUserPool::Update` advances remote actors plus shared social state, `CUserRemote::Update` owns remote movement, action, keydown-prepare, portable-chair presentation, and adjacent one-shot effect surfaces, `CUserRemote::OnAvatarModified` owns avatar refresh plus relationship-record updates, and `CAnimationDisplayer::Effect_ByItem` is a dedicated one-shot item-effect owner rather than a feature-local overlay. | The remaining gap is narrower again: any other feature-local remote shells still need to collapse onto the shared pool; the packet router still only covers the implemented enter/leave, move/state, helper/team, chair/mount, prepared-skill, melee, item-effect, and avatar-modified families rather than the broader client packet surface; Messenger member parity still depends on the simulator's optional member-info `AvatarLook` trailer instead of a fuller official messenger packet decode and broader room-member lifecycle; and `CUserRemote::Update` / `CUserPool::Update` parity is still incomplete for the rest of the avatar-modified and one-time-effect surface, especially actual carry-item-effect rendering, completed-set-item rendering, any client-only relationship pairing rules that depend on serial tables instead of the simulator's same-item reconciliation, broader packet-authored user effects beyond `ItemEff` plus relationship records, remote afterimages beyond the existing melee seam, and higher-fidelity movement smoothing beyond the current sampled move-path path. | shared field actor/runtime layer (`CUserPool::OnPacket`, `CUserPool::Update`, `CUserRemote::Update`, `CUserRemote::OnAvatarModified`, `CAnimationDisplayer::Effect_ByItem`, `CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUser::Update`, `CMovePath::{Decode,OnMovePacket}`, `Effect/ItemEff.img/1112001`, `Effect/ItemEff.img/1112800`, `Effect/ItemEff.img/1112803`, `Effect/ItemEff.img/2270000`, `Effect/ItemEff.img/2270002`, `Effect/ItemEff.img/2270003`) |

### 2. Additional item-effect animation ownership discovered by follow-up IDA decompile

The latest IDA re-check found that the current backlog still understates one client-owned item-effect seam.
The shared remote-user row already names `CAnimationDisplayer::Effect_ByItem`, but the decompile at `0x44f330` shows this is a broader manager path than the current ring-only wording suggests: it formats an item-driven effect path through `StringPool`, resolves the corresponding `Effect/ItemEff.img` property, falls back to the `fail` branch when needed, loads the effect through the common animation-layer loader, and always registers it as a dedicated one-shot animation instead of leaving it to feature-local overlays.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Packet-authored one-shot item-effect parity | The shared `ItemEff` loader now covers the broader packet-authored one-shot shapes that the `CAnimationDisplayer::Effect_ByItem` decompile owns: grouped owner/shared branches for ring families, flat root timelines used by New Year and other social or cash effects, root-frame UOL links, and the `fail` branch fallback when the primary item timeline is not renderable. Remote-user `UserItemEffect` playback therefore no longer depends on ring-only `0`/`1` branch assumptions when it resolves `Effect/ItemEff.img`. | The client path is a general item-effect dispatcher rather than feature-local ring polish, so the simulator now resolves the same wider `ItemEff` asset surface through the existing shared seam instead of leaving non-ring families implied or silently unloaded. | one-shot item-effect animation layer (`CAnimationDisplayer::Effect_ByItem`, `CAnimationDisplayer::LoadLayer`, `CAnimationDisplayer::RegisterOneTimeAnimation`, `Effect/ItemEff.img`) |


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Remote actor foundation pass:
   Add a real `CUserPool`-style remote-user actor layer before continuing to extend wedding, Ariant, Battlefield, minimap-helper, and follow-request overlays independently.
3. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
4. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
5. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
6. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".


