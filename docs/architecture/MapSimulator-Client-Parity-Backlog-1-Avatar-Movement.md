# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CAnimationDisplayer::Effect_ByItem` at `0x44f330`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Avatar and player parity

- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::SetActivePet` at `0x8e40e0`
- `CUserPool::OnPacket` at `0x94ddf0`
- `CUserPool::Update` at `0x94c370`
- `CUserRemote::Update` at `0x955d50`
- `CAnimationDisplayer::Effect_ByItem` at `0x44f330`
- `CActionMan::LoadTamingMobAction` at `0x427a10`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::Update` at `0x937330`

Notes:
`CUser::LoadLayer` and `CUser::PrepareActionLayer` are the clearest resolved avatar-composition seams for layer loading and action-layer setup. `CUser::Update` and `CUser::UpdateMoreWildEffect` also show that the client maintains several additional avatar/effect layers outside the base assembler path, while `CUserLocal::Update` contains the local-player emotion reset path and action/state transitions that matter for expression and rare-action parity. A later IDA pass also confirms that remote avatar ownership is a first-class client surface rather than a loose extension of local-player logic: `CUserPool::OnPacket` dispatches enter, leave, common, remote, and local user packet families, `CUserPool::Update` advances local plus remote users and shared social-effect state, `CUserRemote::Update` owns remote move-action, effect, portable-chair, and prepared-skill presentation, and `CAnimationDisplayer::Effect_ByItem` confirms that one-shot item effects are managed by a dedicated client animation-owner seam rather than only by feature-local overlay code.

### 2. Packet-owned avatar emotion parity discovered by direct `CUserLocal::OnPacket` scan

- `CUser::OnEmotion` at `0x8e0150`
- `CUser::OnRandomEmotion` at `0x8e34b0`

Notes:
The latest direct `CUserLocal::OnPacket` re-check exposed another local-avatar owner that the backlog was not naming anywhere else. `CUser::OnEmotion` decodes an emotion id, duration, and the `m_bEmotionByItemOption` flag before calling `CAvatar::SetEmotion`, while `CUser::OnRandomEmotion` decodes an area-buff item id, resolves `CItemInfo::GetAreaBuffItem`, chooses one weighted emotion slot from the authored prop table, and then routes back through the ordinary emotion-change path via `CWvsContext::SendEmotionChange`. That means client expression parity is not only the passive blink/hit reset surface already tracked in `CUserLocal::Update`; it also includes a packet-owned timed-emotion seam and an item-backed random-emotion owner that should stay explicit in the avatar backlog instead of being hidden under generic item-effect or chat feedback rows.

### 3. Taming-mob action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadTamingMobAction` at `0x427a10`

Notes:
The mount row already cited `LoadTamingMobAction` as evidence, but the owner itself was still buried inside a broader render/state row unlike the explicit loader sections already tracked for summons, mobs, pets, employees, and NPCs. IDA confirms this is a distinct client seam: the loader builds taming-mob action frames only after a vehicle id is already known, and the nearby `IsAbleTamingMobAction` / `IsAbleTamingMobOneTimeAction` gates keep the extra Mechanic and ride coverage keyed to vehicle ownership rather than raw action-name inference alone. That means mounted avatar parity is still split the same way as the other actor families in this backlog set: one row for runtime state/ownership and another for the loader that decides which taming-mob action data the runtime can actually consume.

### 4. Character action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadCharacterAction` at `0x427d00`

Notes:
The broader avatar row already cites `CUser::LoadLayer` and `CUser::PrepareActionLayer`, but a targeted `CActionMan` pass shows there is still another distinct client owner underneath those runtime seams. `CActionMan::LoadCharacterAction` does not only fetch the current action frames. The decompile shows it first normalizing the avatar-hair-equip array for the requested action, zeroing body-part lanes the client refuses to merge, falling back to client default top or bottom items when the incoming equip combination would leave the body invalid, stripping known blocked accessory and weapon-sticker ids, suppressing shield and sticker lanes for ride or vehicle families, applying ghost-mode filtering, and only then calling the lower `load_character_action` plus `MergeCharacterSprite` path. That means visible avatar parity is still split three ways rather than two: runtime ownership in `CUser::*`, packet or state ownership in the local or remote actor seams, and a dedicated character frame-builder that decides which merged body-part set the runtime is even allowed to render for a given action.

### 5. Face-look loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadFaceLook` at `0x41cab0`

Notes:
The existing avatar backlog tracks blink, hit-face, and packet-owned emotion timing, but the loader that actually builds those face-expression canvases was still unnamed. IDA shows `CActionMan::LoadFaceLook` is a dedicated client seam: it caches by skin, face, emotion, and accessory state; resolves the face and accessory image entries; enumerates the authored emotion canvases; reads origin and brow metadata; composites face and accessory planes into the final brow canvas list; and records the authored duration on the cached face-look entry. That means facial-expression parity is not only a runtime question in `PlayerCharacter` or `CharacterAssembler`; it also depends on the client loader that decides which face and accessory canvases exist, how they are composited, and how long the resulting emotion entry stays valid.

### 6. Morph action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::GetMorphImgEntry` at `0x418b30`
- `CActionMan::LoadMorphAction` at `0x4193c0`

Notes:
The avatar backlog already tracks morph runtime and resolver parity, but the underlying morph loader was still folded into that broader row unlike the explicit loader sections already called out for character, face, taming-mob, summon, mob, NPC, pet, employee, and dragon paths. IDA shows the morph path is its own client owner: `CActionMan::GetMorphImgEntry` caches per-template morph IMG entries, resolves the formatted `Morph/<id>.img` resource, follows `info/link` indirection by reloading the linked template and merging missing action branches onto the current morph image entry, and stores the normalized property tree that later action requests consume. `CActionMan::LoadMorphAction` then caches by `(templateId, action)`, resolves the action through the client `s_sMorphAction` table rather than ad hoc string guesses, enumerates frame canvases, reads per-frame `delay`, `lt`, `rb`, and `head` metadata, preserves the authored frame-uol string used by the client morph path, and appends the interior-frame reverse pass when the morph action publishes the replay flag. That means morph parity is still split the same way as the other actor families in this backlog set: runtime transform ownership in the player/avatar seams, plus a dedicated morph image and action loader that decides which morph resources and frame tables the runtime is actually allowed to render.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Avatar and player parity

These are the biggest remaining gaps for visible client parity.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Default avatar selection | The simulator keeps the deterministic, WZ-verified SuperGM/Wizet fallback selection of `1002140` (Wizet Invincible Hat), `1042003` (Wizet Plain Suit), `1062007` (Wizet Plain Suit Pants), `1072005` (Leather Sandals), `1092008` (Pan Lid), and `1322013` (Wizet Secret Agent Suitcase) with matching face, hair, level, and SuperGM job metadata, and the no-packet avatar seam is now shared all the way through preset-backed defaults as well. WZ was rechecked again for this row in `Etc/MakeCharInfo.img/000/{male,female}` and still pins the non-sale starter request layout to the client-owned `0..7` face/hair/skin/hairColor/coat/pants/shoes/weapon families (`male/0 = {20000,20001,20002}`, `male/1 = {30030,30020,30000}`, `male/2 = {0,7,3,2}`, `male/3 = {0,1,2,3}`, `male/4 = {1040002,1040006,1040010}`, `male/5 = {1060002,1060006}`, `male/6 = {1072001,1072005,1072037,1072038}`, `male/7 = {1302000,1322005,1312004}`, `female/0 = {21000,21001,21002}`, `female/1 = {31000,31040,31050}`, `female/2 = {0,7,3,2}`, `female/3 = {0,1,2,3}`, `female/4 = {1041002,1041006,1041010,1041011}`, `female/5 = {1061002,1061008}`, `female/6 = {1072001,1072005,1072037,1072038}`, `female/7 = {1302000,1322005,1312004}`). `CharacterPreset` now owns a synthesized `LoginAvatarLook`, build-backed preset capture preserves hidden equip lists, pet ids, and packet-owned `weaponStickerID` visuals through that owner, `CharacterPreset.ToBuild` rebuilds through `CharacterLoader.LoadFromAvatarLook` whenever the synthesized owner is present, and `CharacterConfigManager.CreateDefaultMalePreset` / `CreateDefaultFemalePreset` now create the default preset from the same synthesized `LoginAvatarLook` source used by roster previews and packet emission instead of re-materializing only scalar selection metadata. Legacy preset files without a serialized `AvatarLook` still load through the existing scalar fallback for compatibility, but the default-avatar parity gap for new simulator-owned presets is closed. | Default avatars, synthesized login previews, packet-authored avatars, and preset-driven defaults now converge on the same render-safe avatar owner, so local default tooling no longer risks silently dropping hidden equips, pets, or weapon stickers just because no packet bytes existed. Focus for later avatar work can stay on other backlog rows instead of this ownership split. | `CharacterLoader.LoadDefaultMale`, `CharacterLoader.LoadDefaultFemale`, `CharacterLoader.LoadFromAvatarLook`, `LoginAvatarLookCodec`, `LoginCharacterRosterEntry.CreateRuntimeBuild`, `MapSimulator.ResolveLoginRosterAvatarLook`, `MapSimulator.CreateLoginPacketCharacterEntry`, `MapSimulator.CreateLoginCharacterAccountEntryState`, `CharacterPreset.FromBuild`, `CharacterPreset.ToBuild`, `CharacterConfigManager.CreateDefaultMalePreset`, `CharacterConfigManager.CreateDefaultFemalePreset`, `UnitTest_MapSimulator/DefaultAvatarSelectionPresetParityTests.cs` (`CUser::LoadLayer`, `CUIAvatar::CUIAvatar`, `GW_CharacterStat::Decode`, `AvatarLook::Decode`, `Etc/MakeCharInfo.img/000/{male,female}`) |
| Implemented | Action coverage | Character-part loading now preserves the existing core action order and appends additional WZ action nodes discovered on each part instead of stopping at the old fixed lists | Uncommon body/equipment poses can now load when the source IMG exposes them, which closes the loader-side empty-state gap for rare, scripted, and death-adjacent actions | `CharacterLoader.StandardActions`, `CharacterLoader.AttackActions` (`CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUserLocal::SetMoveAction`) |
| Implemented | Death / rare action frames | Runtime action selection now carries raw one-shot action names through to `CharacterAssembler` instead of forcing uncommon actions back through the limited enum-only render path | Loaded death and other rare WZ actions can now survive from selection to final frame assembly instead of degrading immediately to generic fallbacks | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler` |
| Implemented | Facial expression behavior | The local player runtime now owns blink cadence and short-lived hit expressions, and updates the assembler expression state when those transitions occur | The avatar now reacts over time instead of staying pinned to the static default face | `CharacterAssembler.GetFaceFrame`, `CharacterLoader.LoadFaceExpressions`, `PlayerCharacter` (`CUserLocal::Update` emotion reset path) |
| Implemented | Equip anchor fallback rules | Equipment alignment now resolves through explicit shared map-point precedence instead of using literal pixel nudges when preferred anchors are missing | Parts stay render-safe while remaining tied to WZ anchor data rather than ad hoc offsets | `CharacterAssembler.CalculateEquipOffset`, `CharacterAssembler.TryCalculateHeadEquipOffset` |
| Implemented | Client-verified fallback order | Action lookup now prefers exact action names, then raw-action family reductions, then swim/fly aliasing, before the final `stand1` escape hatch | Character assembly now follows a documented action-family precedence order instead of jumping straight from miss to generic standing frames | `CharacterAssembler`, `CharacterPart.FindAnimation` (`CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`) |
| Partial | Packet-owned avatar emotion parity | The local avatar still owns a dedicated packet-driven emotion seam instead of relying only on passive blink and hit-face behavior, and this row now mirrors the full currently confirmed `CAvatar::SetEmotion` gate set instead of only the duration path plus morph early-return. IDA-backed packet routing remains wired into the simulator utility surface with the real `CUserLocal::OnPacket` opcodes for `CUser::OnEmotion` (`232`) and `CUser::OnRandomEmotion` (`258`), `PlayerCharacter` still keeps the packet-owned face-expression state with the decoded duration and stored item-option bit when a face expression is actually applied, now also retains that resolved packet-owned emotion snapshot after the live face-expression owner expires or an explicit default clear resets the active owner, and the runtime still routes the active state back through the existing assembler face-owner so packet-authored expressions override blink until the resolved window expires. The same seam still reuses the existing under-face transient avatar-effect owner to play any authored `Etc/EmotionEffect.img/<name>` animation that exists in the current data set, matching the client-side `CAvatar::SetEmotion` `Effect_General("Etc/EmotionEffect/%s")` call without introducing a parallel overlay path; rechecking `Etc/EmotionEffect.img` in the current WZ export still shows the authored overlay set capped at `blink`, `hit`, `smile`, `troubled`, `cry`, `angry`, `bewildered`, `stunned`, `vomit`, and `oops`, and the local player seam now also clears that reused under-face owner when the resolved packet-owned emotion window expires so the overlay lifetime stays tied to the same duration gate as the face layer. WZ was rechecked first for the item-backed random-emotion owner and the raw-action names that the client-side suppression branch actually targets: `item/Cash/0528` still authors `05281000/emotion = {angry=25, cry=20, vomit=40, oops=15}` and `05281001/emotion = {cheers=25, chu=20, glitter=25, love=30}`, `item/Cash/0539` still carries the authored fixed `info/emotion` leg on `05390000` and `05390001`, and `Character/00002000.img` still publishes the raw-action roots `rain`, `paralyze`, and `burster2` that correspond to the client raw codes later checked in IDA. Client evidence was then rechecked in IDA: `CUser::OnRandomEmotion` still resolves the weighted slot and calls `CWvsContext::SendEmotionChange`, `CUser::OnEmotion` still decodes `(emotionId, duration, m_bEmotionByItemOption)` and stores the flag before forwarding only `(emotionId, duration)` into `CAvatar::SetEmotion`, `CAvatar::SetEmotion` / `CAvatar::PrepareFaceLayer` (`0x466b00`, `0x4647d0`) still key face-layer lifetime and `Effect_General("Etc/EmotionEffect/%s")` off the duration path rather than the stored item-option flag, `CAvatar::SetEmotion` still returns immediately when a morph template is active, and the same function now rechecked cleanly shows the remaining non-morph suppression branch too: when the active one-time/raw action resolves to `41`, `42`, or `57`, the client suppresses only emotion id `8` before the face layer or under-face overlay is applied. The simulator now mirrors that split inside the existing local-utility/CWvsContext seam: `OnRandomEmotion` no longer short-circuits to a local avatar apply or effect-duration fallback preview, and instead resolves the authored weighted emotion, respects the `CWvsContext::SendEmotionChange` 2-second gate, emits opcode `56` with the client payload layout `(emotionId, duration=-1, byItemOption=0)` through the live bridge/outbox/deferred transport path when those transports are available, waits for the later inbound `OnEmotion` echo before the local avatar expression actually changes, and now also enforces the exact client-shaped inbound packet layouts for both `OnEmotion` and `OnRandomEmotion` instead of accepting truncated or trailing-byte simulator payloads. The local `OnEmotion` duration resolver still treats `m_bEmotionByItemOption` as observational state only, so zero-duration packets fall back through the same effect-duration then face-look-duration order regardless of the bit; the local player seam still consumes but does not apply packet-owned emotions while a morph transform is active; and `PlayerCharacter` now also suppresses blink emotion packets while the active raw/forced action resolves to `rain`, `paralyze`, or `burster2`, using the existing client raw-action table first and falling back to those exact client-published action names only when the raw code is not available. Focused regression coverage now lives in `UnitTest_MapSimulator/PacketOwnedLocalUtilityParityTests.cs` for exact inbound packet decoding on `OnEmotion(232)` and `OnRandomEmotion(258)` plus the zero-duration fallback staying independent of the item-option bit after decode. The remaining gap is narrower again: the simulator now preserves the decoded `m_bEmotionByItemOption` equivalent after the active face-expression owner expires, but no later runtime owner changes behavior from that retained flag yet once the live packet-owned face layer is gone. | Visible avatar-expression parity now covers both direct emotion packets and the client-authenticated random-emotion re-entry path with the client-shaped timing split: random-emotion packets resolve WZ-authored weighted tables and mirror `SendEmotionChange`, the actual expression change still waits for the inbound emotion packet just as the client routes it through `CUser::OnEmotion`, direct emotion duration now follows the client-owned `(emotionId, duration)` lifetime path instead of a simulator-only item-option infinite fallback, the reused `EmotionEffect` overlay now also stops on that same resolved lifetime instead of free-running past the face-expression window, the decoded item-option ownership bit now survives on the same player seam after the active owner expires instead of disappearing with the face layer, the local utility inbox now enforces the same fixed packet shapes the client decodes for opcodes `232` and `258`, morph-transformed avatars now ignore inbound packet-owned face-expression applies instead of forcing impossible face layers over a morph owner, and the remaining `CAvatar::SetEmotion` one-time-action gate is now attached too so blink packets no longer apply during the client-confirmed `rain` / `paralyze` / `burster2` raw-action window. Remaining work is now concentrated on downstream uses of the retained item-option bit, not on missing weighted selection, missing `EmotionEffect` overlays, missing outbound request/throttle mirroring, loose inbound packet decoding, the earlier simulator-only by-item-option duration rule, the morph-template guard, the overlay-lifetime clamp, or the raw-action-specific blink suppression leg. | local avatar expression layer (`CUserLocal::OnPacket`, `CUser::OnEmotion`, `CUser::OnRandomEmotion`, `CAvatar::SetEmotion`, `CAvatar::PrepareFaceLayer`, `get_action_name_from_code`, `CItemInfo::GetAreaBuffItem`, `CWvsContext::SendEmotionChange`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Character/PacketOwnedAvatarEmotionResolver.cs`, `HaCreator/MapSimulator/Interaction/PacketOwnedLocalUtilityContextState.cs`, `HaCreator/MapSimulator/Managers/LocalUtilityPacketInboxManager.cs`, `HaCreator/MapSimulator/MapSimulator.PacketOwnedUtilityParity.cs`, `UnitTest_MapSimulator/PacketOwnedLocalUtilityParityTests.cs`, `item/Cash/0528`, `item/Cash/0539`, `Etc/EmotionEffect.img`, `Character/00002000.img/{rain,paralyze,burster2}`) |
| Partial | Morph avatar pipeline parity | MapSimulator still resolves concrete morph template ids from `common/morph` and `PVPcommon/morph` into `Morph/*.img`, loads those images as a dedicated avatar render owner, resolves UOL-linked morph frames, preserves per-frame anchors (for example `head`), backfills through same-family base IMG fallback (`id -> id0 -> id00 -> id000`), paired `100x <-> 110x` family fallback, and WZ-backed `info/link` inheritance, and routes hostile polymorph mob skills plus current flag-only `info/morph = 1` outliers through that same owner seam. This run closed the next owner-side alias gap inside that same path instead of widening generic avatar logic again: the existing morph resolver now collapses the client-published raw `savage` request onto the published generic stab family before it falls through to pirate authored attacks, so pirate morphs keep their WZ-backed stab surface on the morph owner rather than jumping straight to branches like `fist` just because `savage` is not authored verbatim in `Morph/*.img`. WZ was rechecked first for `Morph/1000.img`, `1001.img`, `1002.img`, `1003.img`, `1100.img`, `1101.img`, `1103.img`, `2001.img`, `2002.img`, `2003.img`: `1000`, `1001`, `1003`, `1100`, `1101`, and `1103` still publish `info/superman = 1`; `1000`, `1001`, `1100`, and `1101` still keep pirate-family authored attacks such as `fist`, `straight`, `somersault`, `doublefire`, `backspin`, `doubleupper`, `shockwave`, `demolition`, `snatch`, `eburster`, `edrain`, `dragonstrike`, `eorb`, `timeleap`, and related branches, and the pirate-family morphs rechecked in this pass still also publish the generic stab backstop the client raw `savage` request needs (`stabO1`, `stabO2`, plus `proneStab` where present) while still publishing no generic `shoot*`; `1002` still publishes hide-family `fly2/0 -> ../fly/1`, `fly2Move/0 -> ../fly/0`, and `fly2Skill/0 -> ../stand/9` UOL surfaces without a matching `info/superman` flag or generic combat roots; `1003` and `1103` still expose authored archer aliases (`windshot`, `windspear`, `stormbreak`, `arrowRain`, `archerDoubleJump`), generic melee (`swingT1`, `swingT3`), and generic ranged backstop (`shoot1`, `shootF`); `2001` and `2002` still keep only the authored ice-family attack roots (`icemanAttack`, `iceAttack1`, `iceAttack2`, `iceSmash`, `iceTempest`, `iceChop`, `icePanic`) plus their double-jump branches; and `2003` still remains movement-only. WZ was also rechecked for the current flag-only `info/morph = 1` outliers that this seam still needs to keep on the same owner path: `Skill/001.img/0010109/info/morph = 1` and `Skill/2002.img/20020111/info/morph = 1` still stay flag-only, while `Morph/0109.img` and `Morph/0111.img` still publish the corresponding movement-only templates, so the suffix-based `0109` / `0111` reduction remains the currently evidenced path for those outliers. Client evidence remained the second source: `CActionMan::LoadMorphAction` still routes through dedicated `s_sMorphAction` entries, `CActionMan::Init` still uses `get_action_name_from_code`, the recovered morph table still covers the published `fly2*` surface alongside the existing movement, melee, ranged, and alert-family names, and that same raw-action map still includes `savage`, so keeping `fly2*` and the now-evidenced `savage -> stab*` collapse on the morph owner remains the client-shaped path when WZ publishes those backstops. Focused regression coverage is now checked in under `UnitTest_MapSimulator/MorphAvatarPipelineParityTests.cs` for the earlier non-superman `fly2*` runtime transform selection, continued superman `ladder2` / `rope2` ordering, the morph template candidate order that keeps paired and family backfill inside the existing loader seam, the suffix-based candidate reduction used by the current `0010109 -> 0109` and `20020111 -> 0111` flag-only outliers, and the new client-shaped `savage -> stabO1/stabO2/proneStab` morph attack fallback. Remaining gap: broader client-only morph alias remaps beyond the currently evidenced published WZ surfaces, plus any future flag-only remap that does not follow the current suffix path, still need additional client evidence before this row can widen non-WZ aliases safely. | Morph-heavy jobs, hostile polymorph skills, hide-family transforms, and the current flag-only morph outliers remain on the dedicated morph-owner seam rather than generic body/equip assembly. With this pass, the same owner now keeps both the earlier published non-superman `fly2*` families and the client-published raw `savage` request on WZ-backed morph branches instead of degrading either one into generic avatar fallback, while still preserving the earlier superman `ladder2` / `rope2` / `fly2` path, paired and same-family backfill, linked-template inheritance, and suffix-based outlier reduction. The row remains narrowed to broader client-only alias remaps that still lack evidence beyond the recovered `LoadMorphAction` table and any future non-suffix flag-only morph remaps, not the currently evidenced WZ-backed owner assembly, `fly2*` runtime ownership, the `savage -> stab*` collapse, or the candidate-chain behavior already landed here. | avatar transform/runtime assembly (`CActionMan::GetMorphImgEntry`, `CAvatar::MoveAction2RawAction`, `CActionMan::LoadMorphAction`, `CActionMan::Init`, `get_action_name_from_code`, `CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUserLocal::SetMoveAction`, `HaCreator/MapSimulator/Character/MorphClientActionResolver.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Character/Skills/SkillLoader.cs`, `UnitTest_MapSimulator/MorphAvatarPipelineParityTests.cs`, `Skill/001.img/0010109`, `Skill/2002.img/20020111`, `Morph/0109.img`, `Morph/0111.img`, `Morph/1000.img`, `Morph/1001.img`, `Morph/1002.img`, `Morph/1003.img`, `Morph/1100.img`, `Morph/1101.img`, `Morph/1103.img`, `Morph/2001.img`, `Morph/2002.img`, `Morph/2003.img`) |
| Partial | Mount / vehicle / mechanic mode rendering | `TamingMob` assembly on this row still covers the already-implemented owner paths: normal mount ride frames and `sit` fallback, `eventTamingMob` part equip, Mechanic owner routing to `Character/TamingMob/01932016`, Battleship owner routing to `Character/TamingMob/01932000`, direct `ride2` / `getoff2` transitions, fixed Mechanic state skills, distinct Mechanic/Battleship mounted actions, known-owner authored action preservation, the client-confirmed 1932016-only current-action family, and the earlier local built-in Mechanic transform seam (`35001001`, `35101004`, `35101009`, `35111004`, `35121005`, `35121013`) that keeps `01932016` latched through `SkillManager.UpdateClientOwnedVehicleTamingMobState`, `PlayerCharacter.UpdateOwnedTamingMobRenderState`, and `PlayerCharacter.ResolveMountedStateTamingMobPart`. This pass keeps that tighter boundary and closes the next explicit remote `mechanicMode` overlap gap instead of widening ownerless inference. WZ was rechecked first in the current export: `Character/TamingMob/01932016` still publishes the tank presentation roots it reuses for remote Mechanic mode (`tank_stand`, `tank_walk`, `tank`, `tank_prone`), and it also still publishes the authored overlap actions that remote explicit Mechanic mode can surface before or alongside later state skills (`alert2`, `shot`, `ladder2`, `rope2`, `ride2`, `getoff2`). `Skill/3500.img/skill/35001001`, `Skill/3510.img/skill/35101004`, and `Skill/3510.img/skill/35101009` still publish the early built-in Mechanic transform action families (`flamethrower*`, `rbooster_pre`, `flamethrower_pre2` / `flamethrower2`) that already keep the same owner active on the local seam. IDA still points the same way for the ownership boundary: `?LoadTamingMobAction@CActionMan@@QAEXJJQAJHAAV?$ZArray@V?$ZRef@UTAMINGMOBACTIONFRAMEENTRY@CActionMan@@@@@@@Z` only loads taming-mob action frames after a vehicle id is already known, `?IsAbleTamingMobAction@@YAHJJ@Z` plus `?IsAbleTamingMobOneTimeAction@@YAHJJ@Z` still gate the extra Mechanic and ride coverage by vehicle id rather than by action name alone, and `?SetResistanceRidingMoveAction@CAvatar@@IAEXJH@Z` still shows the client keeping the riding owner alive while Mechanic move actions update. Based on that same explicit-owner seam, `RemoteUserActorPool.ResolveClientVisibleActionName` now keeps the full currently-evidenced explicit remote Mechanic presentation split inside the existing remote resolver: explicit `mechanicMode` ids `35001001`, `35101004`, `35101009`, `35111004`, `35121003`, `35121005`, `35121009`, `35121010`, and `35121013` still map generic remote `stand1` / `walk1` / `jump` / `hit` requests onto the tank or siege presentation roots, but they now also preserve the authored overlap actions that `01932016` itself publishes, including `shot`, instead of needlessly degrading those explicit mounted requests onto generic `tank` / `siege` attack aliases. That keeps explicit remote Mechanic mode aligned with the already-evidenced local transform-owner seam without reopening `ClientOwnedVehicleSkillClassifier.IsOwnerlessMechanicVehicleInferenceActionName` for plain `tank` / `siege` / `tank_*` / `siege_*` names that WZ still publishes on `Character/00002000.img` too. The remote override and follow-eligibility seams still keep that same explicit boundary as well: `RemoteUserActorPool.TryResolveMechanicTamingMobOverrideItemId` and `FollowCharacterEligibilityResolver.ResolveMountedVehicleId` only reuse the already-classified explicit Mechanic presentation ids instead of treating an arbitrary positive remote `mechanicMode` value as enough to latch `Character/TamingMob/01932016`. Focused regression coverage is now checked in under `UnitTest_MapSimulator/RemoteMechanicModeRenderingParityTests.cs`, pinning generic tank-family and siege-family remaps, preservation of explicit authored overlap actions such as `shot`, `alert2`, `ladder2`, `rope2`, `ride2`, and `getoff2`, continued mounted-owner resolution for the full explicit `mechanicMode` id table already used by the runtime, and rejection of unknown positive `mechanicMode` values as ownerless mount inference. Remaining gap: broader truly ownerless client-only vehicle update or move-state sources that still surface with no active buff, no active built-in transform owner, no handoff owner, no canonical equipped owner, no explicit `mechanicMode`, and no mounted render owner signal at all still need new client evidence before they can be mirrored safely, and Battleship still has no comparable transform-owned local owner seam beyond the already-implemented named or known-owner paths. | Mounted-state, follow eligibility, local current-action ownership, local render-owner retention, and now explicit remote `mechanicMode` presentation stay aligned across the evidenced blank-tick seam, the named Mechanic/Battleship owner seams, the client-confirmed 1932016-only current-action family, and the built-in local Mechanic transform seam without falling back to generic `tank*` / `siege*` action-name guessing that WZ also publishes on the base body. With the added remote preservation for the explicit `01932016` overlap actions, the narrowed explicit remote-owner gate, and the checked-in regression file that now pins both generic tank-family remaps and authored mounted-action preservation, remote Mechanics no longer needlessly collapse explicit `shot`-style mounted requests onto presentation aliases while the simulator already knows they are in an explicit Mechanic mode, and unknown positive `mechanicMode` payloads still do not reopen ownerless mount inference through the remote fallback seam. The row is now narrowed to genuinely ownerless client-managed vehicle states and any future Battleship-specific local owner seam, not the already-owned Mechanic transform, explicit remote `mechanicMode`, or known-owner paths. | `PlayerCharacter`, `CharacterLoader`, `CharacterAssembler`, `SkillManager`, `ClientOwnedVehicleSkillClassifier`, `FollowCharacterEligibilityResolver`, `EscortFollowController`, `RemoteUserActorPool`, `MapSimulator.PacketOwnedUtilityParity`, `UnitTest_MapSimulator/RemoteMechanicModeRenderingParityTests.cs` (`35001001`, `35101004`, `35101009`, `35111004`, `35121003`, `35121005`, `35121009`, `35121010`, `35121013`, `?LoadTamingMobAction@CActionMan@@QAEXJJQAJHAAV?$ZArray@V?$ZRef@UTAMINGMOBACTIONFRAMEENTRY@CActionMan@@@@@@@Z`, `?IsAbleTamingMobAction@@YAHJJ@Z`, `?IsAbleTamingMobOneTimeAction@@YAHJJ@Z`, `?SetResistanceRidingMoveAction@CAvatar@@IAEXJH@Z`, `Character/00002000.img`, `Character/TamingMob/01932016`, `Character/TamingMob/01932000`, `Skill/3500.img/skill/35001001`, `Skill/3510.img/skill/35101004`, `Skill/3510.img/skill/35101009`, `Skill/3511.img/skill/35111004`, `Skill/3512.img/skill/35121003`, `Skill/3512.img/skill/35121005`, `Skill/3512.img/skill/35121009`, `Skill/3512.img/skill/35121010`, `Skill/3512.img/skill/35121013`) |
| Partial | Skill-specific avatar transforms | The WZ-backed families on this row remain in the existing seams, and this pass closes the main repo gap that was still left around them: the row窶冱 focused source-side regression coverage is now actually checked in at `UnitTest_MapSimulator/PreparedSkillAvatarTransformParityTests.cs` against the same prepare-candidate, built-in transform, and Mechanic transform-owner seams already carrying the runtime behavior. WZ was rechecked first in the current extracted data set: `skill/14111006/prepare/action = dash`, `skill/33101005/prepare/action = swallow_loop`, `skill/32121003/action/0 = cyclone_pre`, `skill/5311002/{prepare/action = noiseWave_pre, keydown/action = noiseWave_ing, action/0 = noiseWave}`, `skill/23121000/{prepare/action = dualVulcanPrep, keydown/action = dualVulcanLoop, keydownend/action = dualVulcanEnd}`, `skill/3510.img/skill/35101005/action/0 = msummon`, `skill/3512.img/skill/35121012/action/0 = tank_laser`, and `Character/00002000.img` still publishes the body-backed `darkTornado_pre`, `swallow_pre`, `cyclone`, `noiseWave_ing`, `dualVulcanLoop`, `tank_laser`, `lasergun`, `tank_msummon`, and `tank_msummon2` families this row depends on. IDA remained the second source: `CUserLocal::DoActiveSkill_Prepare` still keeps the shared prepared-skill owner split, including the Wild Hunter `Swallow` special-case prepare raw action and the shared prepare-action path used by the other prepared families on this row, while `TryDoingSwallowMobWriggle`, `TryDoingCyclone`, `TryDoingRocketBooster`, and `TryDoingRocketBoosterEnd` still back the already-wired swallow sustain, cyclone body-attack, and Rocket Booster startup / end ownership. The shared prepare resolver still keeps the WZ prepare pose ahead of the hold pose for Night Walker `Poison Bomb`, Wild Hunter `Swallow`, `Cyclone`, `Monkey Wave`, and Battle Mage `Dual Vulcan`; `PlayerCharacter.TryCreateBuiltInSkillAvatarTransform` still keeps prepare, hold, and exit as distinct stages for those families plus the earlier Demon fallback and Rocket Booster seams; and the built-in Mechanic seam still preserves the currently evidenced aliases through both the skill-id path and direct raw action-name path, including siege `lasergun`, direct `tank_laser`, summon-family `tank_msummon` / `tank_msummon2`, and the already surfaced `tank_mRush` family. The checked-in regression file now pins that behavior directly: ordered prepare candidates for the WZ-backed families on this row, distinct prepare / hold / exit resolution for built-in transforms, fallback onto renderable `bluntSmash` for Demon `bluntSmashEnd` / `soulEater_end`, Rocket Booster startup and tank-startup ownership, Mechanic summon and rush aliases, Mechanic state attack aliases, and `SkillManager.ResolveClientOwnedVehicleAvatarTransformMountItemId` keeping `1932016` latched for the transform-owner skill ids already evidenced on this row, including the earlier WZ-authored `msummon` cast skills and direct `tank_laser`. The remaining gap is narrower again: the WZ-authored prepare, hold, movement, the currently evidenced exit families, the confirmed siege-family `lasergun` raw alias, the preserved `tank_laser` attack alias and direct cast-owner retention, and the preserved Mechanic summon / rush aliases now all match checked-in code plus checked-in regression coverage, but any further client-only prepared or transform aliases beyond the currently surfaced `lasergun`, `tank_laser`, `tank_msummon`, `tank_msummon2`, and `tank_mRush` families, unevidenced extra exit names, or broader movement-family branches still need explicit client evidence before they should be added. | Mechanic avatar state skills, Mechanic summon-family avatar transforms, the earlier WZ-authored `msummon` cast skills, direct `tank_laser`, the surfaced `tank_mRush` alias, flamethrower, Rocket Booster, Cyclone, `Monkey Wave`, Wild Hunter `Swallow`, Wild Hunter `More Wild`, Evan dragon-breath charges, Dual Blade `Final Cut` / `Monster Bomb`, Battle Mage `Dual Vulcan`, Night Walker `Poison Bomb`, and the confirmed Demon prepared-skill charge families are now all anchored in both the runtime remaps and an actual checked-in regression file for this row instead of only backlog prose. The remaining uncertainty is narrowed to still-unconfirmed client-only aliases beyond the currently surfaced `lasergun`, `tank_laser`, `tank_msummon`, `tank_msummon2`, and `tank_mRush` families, additional exit branches, or transform movement families beyond the WZ-authored `tank_*`, `siege_*`, and `tank_siege*` state / attack / `_after` families already wired here. | `SkillManager`, `PlayerCharacter`, `CharacterAssembler`, `CharacterData`, `UnitTest_MapSimulator/PreparedSkillAvatarTransformParityTests.cs` (`14111006`, `23121000`, `31001000`, `31101000`, `32121003`, `33101005`, `35001001`, `35101004`, `35101005`, `35101009`, `35111002`, `35111004`, `35111005`, `35111011`, `35121003`, `35121005`, `35121009`, `35121010`, `35121012`, `35121013`, `CAvatar::MoveAction2RawAction`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `Skill/1411.img/skill/14111006/prepare/action`, `Skill/2312.img/skill/23121000/{prepare,keydown,keydownend}`, `Skill/3100.img/skill/31001000/keydownend/action`, `Skill/3110.img/skill/31101000/{action/0,keydownend/action}`, `Skill/3212.img/skill/32121003/action/0`, `Skill/3310.img/skill/33101005/prepare/action`, `Skill/3500.img/skill/35001001/{prepare,keydown,keydownend,action}`, `Skill/3510.img/skill/35101004/action/0`, `Skill/3510.img/skill/35101005/action/0`, `Skill/3510.img/skill/35101009/{prepare,keydown,keydownend}`, `Skill/3511.img/skill/35111002/action/0`, `Skill/3511.img/skill/35111004/action/0`, `Skill/3511.img/skill/35111005/action/0`, `Skill/3511.img/skill/35111011/action/0`, `Skill/3512.img/skill/35121003/action/0`, `Skill/3512.img/skill/35121005/action/0`, `Skill/3512.img/skill/35121009/action/0`, `Skill/3512.img/skill/35121010/action/0`, `Skill/3512.img/skill/35121012/action/0`, `Skill/3512.img/skill/35121013/action/0`, `Skill/531.img/skill/5311002/{prepare,keydown,action}`, `Character/00002000.img/{darkTornado_pre,darkTornado,darkTornado_after,bluntSmash,cyclone_pre,cyclone,cyclone_after,noiseWave_pre,noiseWave_ing,noiseWave,dualVulcanPrep,dualVulcanLoop,dualVulcanEnd,lasergun,rbooster_pre,rbooster,rbooster_after,tank_pre,tank,tank_walk,tank_stand,tank_prone,tank_after,tank_laser,siege_pre,siege,siege_stand,siege_after,tank_siegepre,tank_siegeattack,tank_siegestand,tank_siegeafter,tank_rbooster_pre,tank_rbooster_after,msummon,msummon2,tank_msummon,tank_msummon2}`) |
| Partial | Client-managed avatar effect layers | The simulator keeps the existing WZ-first avatar-effect seams on this row: persistent `special` / `special0` / `finish` / `finish0` layers, `More Wild` ladder overrides, transient movement-owner `effect` / `effect0` promotion (including WZ `casterMove = 1` + `type = 41` rush casts such as `Skill/432.img/4321000`), looping buff-owner `effect` / `effect0` fallbacks for flying, Dark Sight, invisible, reflect, and broader timed self-buff shapes, the Energy Charge full-charge runtime backed by `Skill/511.img/5110001/common/x` and `Skill/1510.img/15100004/common/x`, the race novice flying `*1026` repeat seam with `pos = 1` face-origin anchoring through `brow`, and the currently evidenced rotate-sensitive suppression set confirmed from `CAvatar::IsOnPlayingRotateAction`. WZ was rechecked again for the linked affected-area seam on this pass: `Skill/3211/skill/32110007/info/affectedSkill = 32001003` still points at the authored aura on `Skill/3200/skill/32001003/affected`, `Skill/3511/skill/35111011/info` still publishes `type = 33` plus `minionAbility = heal`, and `Skill/3512/skill/35121010/affected/pos = 1` still anchors the authored support aura off the face-owned plane. This pass closes the next ownership-refresh gap inside that same seam instead of widening inference: both the local affected-area avatar owner and the projected remote support-area buff owner now compare the resolved avatar-plane signature before reusing an already-active player-owned layer, so wrapper or summon skills that later gain a linked authored contributor can upgrade in place from a wrapper-only or single-plane `affected` layout to the full linked `affected` / `affected0` layout without waiting for the area or synthetic buff to expire. That keeps the existing linked-support ownership, merged support-level-data resolution, authored `pos` preservation, and linked support-plane aggregation intact while finally making the later-arriving sibling-plane upgrade real at runtime instead of only cache-visible. Remaining gap: broader client-only `CUser::Update` effect families beyond the currently evidenced WZ-authored `special*`, timed `effect/effect0`, `affected/affected0`, invisible/effect-only buff, movement-action, `casterMove/type=41`, race novice flying `repeat`, and friendly support-area ownership shapes still need explicit client-backed remaps before they can be mirrored safely; the current rotate fallback still only covers the confirmed raw-action set plus the synthetic `*1026` repeat owner; and Energy Charge may still need deeper client confirmation if later IDA work changes its hit cadence or extra `bodyAttack` runtime behavior. | Visible parity is closer because client-owned support auras no longer get stuck on the first wrapper-only layout once linked support ownership resolves later. Local qualifying player-skill areas and projected remote support areas now keep the earlier WZ-backed ownership rules, linked support eligibility, linked support level data, authored `pos` anchors, and dual-plane `affected0` preservation, while also refreshing the active avatar-owned layer whenever the resolved plane signature changes, which avoids stale one-plane auras on surfaces such as `32110007 -> 32001003` without reintroducing the old every-tick restart problem. The row is now narrowed to broader unevidenced `CUser::Update` effect families, wider rotate suppression beyond the confirmed raw-action set, and deeper Energy Charge runtime proof, not the already-evidenced linked support-owner upgrade seam. | `PlayerCharacter`, `PlayerManager`, `SkillLoader`, `SkillManager`, `CharacterAssembler`, `CharacterData`, `ClientOwnedAvatarEffectParity`, `AffectedAreaAvatarEffectResolver`, `UnitTest_MapSimulator/AffectedAreaAvatarEffectUpgradeParityTests.cs` (`CUser::Update`, `CUser::UpdateMoreWildEffect`, `CUser::UpdateAr01Effect`, `CUserLocal::TryDoingSwallowBuff`, `CUser::LoadSkillRepeatEffect`, `CAvatar::IsOnPlayingRotateAction`, `get_novice_skill_as_race`, `get_action_name_from_code`, `Skill/3211/skill/32110007/info/affectedSkill`, `Skill/3200/skill/32001003/affected`, `Skill/3511.img/35111011/{affected,info/type,info/minionAbility}`, `Skill/3512.img/35121010/{affected,affected/pos,info/type,info/minionAbility}`, `Skill/432.img/4321000/{effect,special,stopEffect,info/casterMove,info/type}`, `Skill/511.img/5110001/{special,affected,common/x,info/energyCharge,info/fullChargeEffect}`, `Skill/1510.img/15100004/{special,affected,common/x,info/energyCharge,info/fullChargeEffect}`, `Skill/000.img/0001026/repeat`, `Skill/1000.img/10001026/repeat`, `Skill/2000.img/20001026/repeat`, `Character/00002000.img/{doubleJump,backspin,rollingSpin,darkSpin,screw,somersault,homing,triplefire,bamboo,lightingBolt,breathe_prepare,chainPull,finalCut}`) |
| Partial | Remote user / `CUserPool` actor parity | `RemoteUserActorPool` remains the shared remote-user owner for AvatarLook decode, enter/leave, movement snapshots, helper/team state, chair/mount state, prepared-skill world overlays, packet `223` avatar refresh, packet-owned `Effect/ItemEff.img` social effects, carry-item and completed-set rendering, shared `225/226` temporary-stat storage, and the currently implemented packet-owned active-effect item presentation. This pass keeps the work inside that owner seam again and closes the Messenger-fed room-member relationship that was still falling back onto simulator-owned profile packets. WZ was rechecked first for the reused owner surfaces already feeding this row and nothing changed: `Effect/CharacterEff.img/1112916`, `Effect/CharacterEff.img/1112924`, and `Effect/CharacterEff.img/1112926` still expose the carry-item token bundle and single-token branches that `CUser::SetCarryItemEffect`-style presentation consumes, while `Effect/SetItemInfoEff.img` still publishes the completed-set owner animation entries that the shared remote pool already loads by set id. The earlier relationship-record dispatch-key ownership remains in place, and the shared Messenger-to-remote-user bridge now also stays aligned with the client-owned room lifecycle already evidenced for `CUIMessenger::OnPacket`: `MessengerRuntime` no longer clears a contact's stored `AvatarLook` when decoded member-info payloads omit the simulator-only optional trailer, `ApplyPacketEnter` and `ApplyPacketMigrated` now preserve the earlier packet-owned avatar on the contact record when later packet shapes do not refresh it, and `MessengerRemoteUserSynchronizer` now removes messenger remotes by exact keep-set instead of dropping the whole source-tag bucket every frame while reusing an existing remote actor build only when the current messenger snapshot has profile metadata but no fresh `AvatarLook`. When there is no packet-owned room-member avatar and no earlier remote actor to reuse, that bridge now refuses to mint a brand-new hidden remote from the local template, so messenger-fed hidden remotes stay anchored to the packet-owned `enter`/`avatar`/`migrated` lifecycle instead of collapsing back to simulator visuals. Focused regression coverage for this narrower owner seam now lives in `UnitTest_MapSimulator/MessengerRemoteUserParityTests.cs`, including direct coverage for the profile-only build gate plus the member-info, enter, and migrated avatar-preservation cases, while the earlier relationship-record and wedding carry/set ownership coverage remains where it was. | The remaining gap is narrower but still row-sized: the packet router still only covers the implemented enter/leave, move/state, helper/team, chair/mount, prepared-skill, melee, item-effect, avatar-modified, active-effect-item, and temp-stat families instead of the wider client packet surface; first-seen messenger member-card contacts that never receive packet-owned room-member `AvatarLook` payloads now correctly avoid fabricating a hidden remote, but they still have no fuller official visual source than the profile metadata already visible in the messenger UI; and remote temp-stat/update parity is still incomplete beyond the currently decoded hidden-like, morph, Mechanic, Shadow Partner, known Aura, Soul Arrow, Blessing Armor, Barrier-family presentation, plus the now-shared repeat-effect, magic-shield, and Final Cut owner surfaces already wired through the remote pool. Remaining work on this row is therefore concentrated on packet-authored remote families the shared owner still does not decode or render, exact client carry-item orbit choreography and asset choice beyond the current shared orbit reuse, broader packet-authored user effects beyond the current `ItemEff` and item-backed emotion coverage, remote afterimages beyond the current melee seam, the other feature-local remote shells that still bypass the shared pool after the wedding generic-item-effect and avatar-modified carry/set fixes, and higher-fidelity movement smoothing beyond the current sampled move-path path. | shared field actor/runtime layer (`CUserPool::OnPacket`, `CUserPool::Update`, `CUserRemote::Update`, `CUserRemote::OnAvatarModified`, `SecondaryStat::DecodeForRemote`, `CUser::SetCarryItemEffect`, `CAnimationDisplayer::Effect_ByItem`, `CUIMessenger::OnPacket`, `CUser::LoadLayer`, `CUser::PrepareActionLayer`, `CUser::SetMoveAction`, `CUser::Update`, `CMovePath::{Decode,OnMovePacket}`, `Morph/*.img`, `Character/TamingMob/01932016.img`, `Character/01112916.img/info/effect/path`, `Character/01112924.img/info/effect/path`, `Character/01112926.img/info/effect/path`, `Character/00002000.img/{ghoststand,ghostwalk,ghostjump,ghostproneStab,ghostladder,ghostrope,ghostfly,ghostsit,tank_stand,tank_walk,tank_prone,tank,siege_stand,siege,tank_siegestand,tank_siegeattack}`, `Skill/3511.img/35111004/action/0`, `Skill/3512.img/35121005/action/0`, `Skill/3512.img/35121013/action/0`, `Skill/411.img/skill/4111002/special`, `Skill/421.img/skill/4211008/special`, `Skill/1411.img/skill/14111000/special`, `Skill/412.img/4121006/effect`, `Skill/3310.img/33101003/effect`, `Skill/1310.img/13101003/effect`, `Skill/320.img/3201004/effect`, `Skill/310.img/3101004/effect`, `Skill/2112.img/21120007/{effect,affected}`, `Skill/2311.img/23111005/effect`, `Skill/2001.img/20011010/{effect,affected}`, `Skill/2000.img/20001010/{effect,affected}`, `Skill/1000.img/10001010/{effect,affected}`, `Item/Cash/0528.img/{05281000,05281001}`, `Item/Cash/0539.img/{05390000,05390001}`, `Etc/EmotionEffect.img`, `Effect/CharacterEff.img/1112916`, `Effect/CharacterEff.img/1112924`, `Effect/CharacterEff.img/1112926`, `Effect/ItemEff.img/1112001`, `Effect/ItemEff.img/1112800`, `Effect/ItemEff.img/1112803`, `Effect/ItemEff.img/2270000`, `Effect/ItemEff.img/2270002`, `Effect/ItemEff.img/2270003`, `Etc/SetItemInfo.img`, `Effect/SetItemInfoEff.img`, `HaCreator/MapSimulator/Pools/RemoteUserActorPool.cs`, `HaCreator/MapSimulator/Interaction/MessengerRuntime.cs`, `HaCreator/MapSimulator/Interaction/MessengerRemoteUserSynchronizer.cs`, `HaCreator/MapSimulator/MapSimulator.WeddingRemoteUsers.cs`, `UnitTest_MapSimulator/MessengerRemoteUserParityTests.cs`, `UnitTest_MapSimulator/RemoteUserRelationshipRecordParityTests.cs`, `UnitTest_MapSimulator/WeddingRemoteAvatarPresentationParityTests.cs`) |

### 2. Taming-mob action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Taming-mob action-loader / frame-build parity | The simulator still keeps the dedicated taming-mob action owner at the loader seam, and this row now also covers the client-backed non-`190` ride families that have to enter that seam before any loader-owned frame rules can apply. `CharacterLoader.LoadTamingMob` still attaches `TamingMobActionFrameOwner` to taming-mob parts, that owner still enforces the explicit vehicle-id action families confirmed in the targeted `CActionMan` pass before lazy-loading frames, and the loader now classifies the additional client-authored `1983xxx` and `199xxxx` taming-mob assets through the same `Character/TamingMob` folder/slot path instead of dropping them as non-equipment before ownership is even established. WZ was rechecked first for the affected assets: ordinary rides such as `Character/TamingMob/01902000` still only publish locomotion plus `tired`; Mechanic `Character/TamingMob/01932016` still publishes the broader mount-only families (`tank_*`, `siege_*`, `rbooster*`, `msummon*`, `flamethrower*`, `ride2`, `getoff2`, `rope2`, `ladder2`); Wild Hunter jaguars such as `Character/TamingMob/01932030` and `Character/TamingMob/01932015` still publish jaguar-only attack and utility families (`swallow_*`, `crossRoad`, `wildbeast`, `sonicBoom`, `clawCut`, `mine`, `ride`, `getoff`, `proneStab_jaguar`, `herbalism_jaguar`, `mining_jaguar`); the `191xxxx` saddle assets still confirm the client-owned companion source format for `190xxxx` rides because their `info/islot` is `Sd` and their authored action trees live under the owning vehicle id instead of at the image root (for example `Character/TamingMob/01912033 -> 1902040`, `01912040 -> 1902047`, `01912041 -> 1902048`, `01912042 -> 1902049`); `199xxxx` taming-mob assets such as `Character/TamingMob/01992004` still advertise `info/islot = Tm` and publish the normal ride locomotion set at the image root; and `1983xxx` portable-chair ride mounts such as `Character/TamingMob/01983000`, `01983019`, and `01983039` still live in the same folder but only publish `sit`, which explains why they must still enter the taming-mob loader seam even though they are not ordinary locomotion rides. IDA remained the second source: `CActionMan::LoadTamingMobAction` at `0x427a10` still remaps exhausted stand requests to action `55` when `bTamingMobTired` is set, `IsAbleTamingMobAction` still keeps the extra persistent table keyed to `1932016`, `LoadTamingMobAction` still hard-codes the `193`-family one-shot tables before falling back to `IsAbleTamingMobOneTimeAction`, the `190xxxx` branch still loads the base source from `b[18]` before optionally merging `b[19]` and `b[20]` through `load_tamingmob_action(..., b[18], b[19], b[20])` into `MergeTamingMobSprite`, and the separate non-`190` branch still hard-special-cases `1983xxx` vehicle ids to action `48` before the usual gating path. The confirmed `191xxxx` saddle companion source still maps to `EquipSlot.Saddle`, and `CharacterLoader.PrepareActionMergeInput` still folds that saddle-authored action source into whichever `190xxxx` taming-mob part the assembler actually marked active, including override-owned mounts passed through the explicit `activeTamingMobPart` seam instead of only mounts already stored back in `build.Equipment`, so the simulator keeps the client-owned merge handoff in one loader path before assembly rather than dropping the overlay for override mounts. Focused regression coverage for this seam now includes `UnitTest_MapSimulator/TamingMobLoaderParityTests.cs`, pinning the loader classification for `180xxxx`, `190xxxx`, `191xxxx`, `193xxxx`, `1983xxx`, and `199xxxx` through the taming-mob folder and slot seam. The remaining gap is narrower and still evidence-bound: the simulator now loads the `1983xxx` family through the right seam, but it still does not have a direct string-backed equivalent for the client's explicit `1983xxx -> action 48` remap, and it still does not have direct evidence for the optional third `b[20]` merge source, so those final two loader-owned legs remain unresolved here. | Mounted-avatar parity stays anchored to an explicit loader/cache seam that owns alias fallback, tired remap selection, the confirmed client vehicle-id gates, the `193`-family one-shot exceptions, the confirmed `190xxxx` saddle merge path, and now the loader admission path for the client-owned `1983xxx` and `199xxxx` taming-mob families in one place instead of scattering those rules across assembler/runtime code. With `191xxxx` sources treated as saddle-authored companion layers and merged into the actual active mount before assembly, and with portable-chair and `199` mounts now able to reach the taming-mob owner instead of failing classification before load, the remaining difference is concentrated in the still-unidentified optional third companion source and the still-unmapped `1983` action-code remap rather than in the primary vehicle tables, one-shot gates, or the override-mount handoff. | mounted action-loader layer (`CActionMan::LoadTamingMobAction`, `IsAbleTamingMobAction`, `IsAbleTamingMobOneTimeAction`, `HaCreator/MapSimulator/Character/TamingMobActionFrameOwner.cs`, `HaCreator/MapSimulator/Character/CharacterLoader.cs`, `HaCreator/MapSimulator/Character/CharacterAssembler.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`, `UnitTest_MapSimulator/TamingMobLoaderParityTests.cs`, `Character/TamingMob/01902000`, `Character/TamingMob/01912033`, `Character/TamingMob/01912040`, `Character/TamingMob/01912041`, `Character/TamingMob/01912042`, `Character/TamingMob/01932016`, `Character/TamingMob/01932030`, `Character/TamingMob/01932015`, `Character/TamingMob/01983000`, `Character/TamingMob/01983019`, `Character/TamingMob/01983039`, `Character/TamingMob/01992004`) |

### 3. Character action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Character action-loader / merged frame-build parity | `CharacterLoader.PrepareActionMergeInput` now covers the full loader-owned merge sanitation confirmed by the targeted client pass. In addition to the previously landed fallback-top / fallback-bottom synthesis, blocked-id stripping, mounted-family shield and weapon suppression, mounted action remaps, ghost collapse, and sticker clears, the simulator now also zeros the always-suppressed face-accessory lane that the client hard-codes as `b.a[2] = 0`. A follow-up client pass through `CCSWnd_Char::InitRing` also resolves the three anonymous global body-part indices touched by `CActionMan::LoadCharacterAction`: they are the remaining ring lanes, so the simulator窶冱 ring suppression table now covers the same client-owned group. The `arrowEruption` special-case lane filter is also pinned exactly to the client array layout from the targeted scan: action `47` preserves only avatar indices `0`, `1`, `3`, and `4`, which in simulator terms means hair plus `Cap`, `EyeAccessory`, and `Earrings`, with hair already owned outside the equipment dictionary. | Avatar parity now includes the full dedicated loader-side ownership bucket beneath `CUser::*`: the simulator decides the same sanitized equip set that the client allows into `load_character_action` and `MergeCharacterSprite`, so future avatar work for this area can stay in higher-level runtime or asset seams instead of reopening the frame-builder ownership path. | character action-loader layer (`CActionMan::LoadCharacterAction`, `CCSWnd_Char::InitRing`, lower `load_character_action`, `MergeCharacterSprite`, simulator seams: `HaCreator/MapSimulator/Character/CharacterLoader.cs`, `HaCreator/MapSimulator/Character/CharacterAssembler.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`) |

### 4. Face-look loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Face-look loader / emotion-canvas composition parity | The simulator now keeps the client-owned face-look build and composition step inside the explicit loader seam instead of leaving it implicit in runtime expression state. `FacePart` still delegates face-expression assembly to `FaceLookLoader`, and that loader now keeps the cache keyed by skin, expression name, and the concrete face-accessory item id; keeps the authored total duration on the cached entry; resolves accessory expression planes through the accessory part's lazy animation resolver instead of only whatever generic equip animations happened to be preloaded; and now owns the accessory emotion remap before any generic accessory fallback can run. WZ was rechecked first for this row: `Character/Face/00020000` still exposes authored face-expression branches with per-frame or branch `delay`, `origin`, `map/brow`, and `z = face`, while `Character/Accessory/01012000` still exposes face-accessory emotion branches through `default/*` canvases and UOL redirects with authored `origin`, `map/brow`, and `z = accessoryFace` metadata, but still omits the face-only `qBlue` branch that remains present under `Character/Face/00020000`. IDA remained the second source: `CActionMan::LoadFaceLook` at `0x41cab0` still caches by skin, face, emotion, and accessory state; still fetches the accessory IMG entry directly from `nAcc`; still remaps the last emotion slot back to emotion `0` before loading the accessory branch; and the targeted caller trace through `CAvatar::PrepareFaceLayer` at `0x4647d0` now closes the old accessory-state uncertainty by showing that `nAcc` is passed straight from `m_avatarLook.anHairEquip[2]` or its forced override, i.e. the equipped face-accessory item id itself for this seam. With that caller evidence in hand, the loader-owned cache key staying at item-id granularity now matches the client path instead of underspecifying it. Each cached `FaceLookFrame` still builds a loader-owned `CompositeFrame` that aligns face and accessory planes around the shared brow anchor, unions their authored bounds, preserves translated map-point metadata, and raster-composites the face and accessory textures in client z-order before caching, and the loader now also explicitly routes accessory-side `qBlue` requests to the authored `default` accessory plane instead of relying on generic fallback order to arrive there incidentally. `CharacterAssembler` still consumes that composed face-look frame first and skips emitting a separate `Face_Accessory` render part whenever the loader produced a real composite texture, so the final face-expression render path still matches the client-owned `LoadFaceLook` ownership split instead of depending on over-face runtime ordering. Focused regression coverage for this row now lives in `UnitTest_MapSimulator/FaceLookLoaderParityTests.cs` for both the loader-owned accessory `qBlue -> default` remap and the confirmed item-id-only accessory cache key. | Facial-expression parity for this loader row is now owned in the same place and at the same granularity as the client: the face-look loader decides which face and accessory emotion planes exist, composes them around the shared brow anchor, preserves authored duration and metadata, applies the confirmed accessory-side `qBlue -> default` rule before fallback, and keys the cache by the same effective accessory input the client passes into `LoadFaceLook`. The remaining work on adjacent avatar rows is no longer this seam's loader ownership, composition order, or accessory-state keying. | face-look loader layer (`CActionMan::LoadFaceLook`, `CAvatar::PrepareFaceLayer`, simulator seams: `HaCreator/MapSimulator/Character/FaceLookLoader.cs`, `HaCreator/MapSimulator/Character/CharacterData.cs`, `HaCreator/MapSimulator/Character/CharacterAssembler.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `UnitTest_MapSimulator/FaceLookLoaderParityTests.cs`, `Character/Face/00020000`, `Character/Accessory/01012000`) |

### 5. Morph action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Morph image/action-loader parity | The simulator still keeps the client-owned morph loader in the existing loader and assembler seams instead of leaking it into generic runtime fallback. WZ was rechecked first for the current row against the currently mounted IMG export: `Morph/1003.img` still publishes per-frame authored `delay`, `lt`, `rb`, and `head` metadata on its movement and combat branches, its loader-owned UOL surfaces still resolve through the action tree such as `fly2/0 -> ../fly/3`, `fly2Move/0 -> ../fly/4`, and `fly2Skill/0 -> ../alert2/0`; in the currently mounted data set `arrowRain` is still a published authored combat branch, but it is no longer exposed here as the older four-frame `../jump/*` UOL chain that this row used to cite. `1003.img` still publishes `archerDoubleJump`, `windshot`, `windspear`, and `stormbreak`; `Morph/2002.img` still publishes the ice-family authored combat roots plus `iceDoubleJump`; and `Morph/2001.img` still publishes the older ice-family authored `icemanAttack` plus `icemandoubleJump`. `Skill/000.img/skill/0001105/{common,PVPcommon}/morph` still carries the concrete string morph template `2002`; `Skill/512.img/skill/5121003/{common,PVPcommon}/morph` still carries the concrete string morph template `1001`; and the current flag-only outliers `Skill/001.img/skill/0010109/info/morph = 1` and `Skill/2002.img/skill/20020111/info/morph = 1` still remain flag-only in WZ, so the earlier suffix-based candidate reduction is still the active owner-side remap until new evidence says otherwise. IDA remained the second source for the owner split: `CActionMan::GetMorphImgEntry` still owns `Morph/<id>.img` lookup plus `info/link` inheritance, while `CActionMan::LoadMorphAction` still caches by `(templateId, action)`, reads authored frame metadata, stores the frame-uol string alongside each cached morph action frame, and exposes the replay-tail flag through string-pool id `0x049F` (`zigzag`). The earlier loader-side candidate walk and cache ownership remain in place, and this pass narrowed another action-name gap inside the same owner seam instead of widening runtime code: `MorphClientActionResolver` still keeps the published double-jump precedence for this row (`archerDoubleJump`, then `iceDoubleJump`, then `icemandoubleJump`, then any other published `*DoubleJump` surface), still remaps the confirmed raw ice-family requests onto the older published `icemanAttack` branch when only `Morph/2001.img` exposes that family, and now also keeps the current client raw piercing-family requests (`piercing`, `crossPiercing`) on the published `windspear` morph branch so archer-only morphs do not fall through to unrelated published combat aliases just because `Morph/*.img` does not publish verbatim `*Piercing` nodes. Focused regression coverage for this row now lives in `UnitTest_MapSimulator/MorphActionResolverParityTests.cs`, which pins the `piercing -> windspear` and `crossPiercing -> windspear` resolver seam, the exact-published-ice-over-legacy ordering, the legacy-ice fallback, and the published double-jump precedence including `icemandoubleJump`. The remaining gap is narrower but still row-sized: the morph action-name surface is still not a complete confirmed dump of the client `s_sMorphAction` table, so morph-published names that remain outside the confirmed client raw-action map beyond the now-wired ice and archer alias seams, plus any future flag-only remap or client-only merge rule beyond the already-implemented candidate walk plus `info/link` inheritance, still need fresh evidence before widening this owner again. | Morph parity for this loader row stays anchored to the same explicit owner split the client uses: requested template resolution, paired and same-family backfill, linked-template inheritance, per-template action caching, UOL-backed frame identity, authored morph frame metadata, the confirmed `zigzag` replay-tail owner, and the more client-backed action-name fallback order all stay in the morph image/action loader path instead of bleeding into generic runtime fallback code. With this pass, the older WZ-only `Morph/2001.img` ice surface still resolves the newer raw ice-family names inside the loader seam, the explicit double-jump precedence still covers the older `icemandoubleJump` branch, and archer-only morphs now keep the current raw piercing-family requests on the published `windspear` branch instead of degrading onto unrelated published aliases. The remaining differences are concentrated in still-unconfirmed morph-only names outside the recovered client map and in any future non-WZ remap or merge rule beyond the currently evidenced candidate-chain plus `info/link` ownership. | morph action-loader layer (`CActionMan::GetMorphImgEntry`, `CActionMan::LoadMorphAction`, simulator seams: `HaCreator/MapSimulator/Character/MorphActionFrameOwner.cs`, `HaCreator/MapSimulator/Character/MorphClientActionResolver.cs`, `HaCreator/MapSimulator/Character/CharacterLoader.cs`, `HaCreator/MapSimulator/Character/CharacterAssembler.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Character/Skills/SkillLoader.cs`, `UnitTest_MapSimulator/MorphActionResolverParityTests.cs`, `Morph/1003.img`, `Morph/2001.img`, `Morph/2002.img`, `Skill/000.img/skill/0001105/{common,PVPcommon}/morph`, `Skill/512.img/skill/5121003/{common,PVPcommon}/morph`, `Skill/001.img/skill/0010109/info/morph`, `Skill/2002.img/skill/20020111/info/morph`) |


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Remote actor foundation pass:
   Add a real `CUserPool`-style remote-user actor layer before continuing to extend wedding, Ariant, Battlefield, minimap-helper, and follow-request overlays independently.
3. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
4. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
5. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
6. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".



