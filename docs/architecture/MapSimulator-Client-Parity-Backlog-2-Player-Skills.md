# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Player skill execution parity

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`

Notes:
`CUserLocal::Update` at `0x937330` orchestrates several of these paths and confirms that key-down gauge updates, queued follow-up attacks, prepared-skill execution, repeat-skill handling, teleport, rush, flying-rush, rocket-booster start/end handling, movement-coupled shoot branches, timer-driven cancellations, and status-bar feedback are coordinated in one local-player update loop rather than isolated one-off handlers. A second IDA pass also showed that summon parity splits across both the summon actor and its packet-owned pool: `CSummoned::Update` drives summon actor motion, active/passive vec-ctrl updates, body-contact damage checks, assist-type dispatch, Tesla Coil state handling, self-destruct timing, and final world-position sync rather than only the attack helper branches, while `CSummonedPool::OnPacket` dispatches created/removed/move/attack/skill/hit packets and `CSummonedPool::OnCreated` ties each summon to its owning user, skill id, level data, and local-user registration path.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Player skill execution parity

The simulator has a generic skill runtime. It does not yet mirror the client's named `CUserLocal` behavior families.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Generic skill casting | The simulator now loads the full player skill catalog from `Skill.wz`, learns active skills into the runtime, supports direct cast invocation from the skill window in addition to hotkey casting, keeps GM/SuperGM skill-book resolution compatible with both `900.img` and `910.img` layouts while focusing those jobs on their own job-book window instead of the global advancement catalog, evaluates Big Bang `common` formulas in IMG-mode data sets so skills like `Power Strike` are treated as castable attacks instead of passive placeholders, mirrors skill effect anchoring/hitbox fallback by facing direction for asymmetric melee effects, sanitizes unsupported SpriteFont glyphs in the skill window so WZ-backed names no longer crash row rendering, and resolves cast SFX from `Sound/Skill.img` so skill use now plays the corresponding `Use` or fallback attack sound through the simulator audio path | This closes the previous "can only use the current job's limited skill book" runtime/UI gap and keeps special-job skill tabs populated and executable across client data variants | `SkillManager.cs`, `SkillLoader.cs`, `SkillUIBigBang.cs`, `MapSimulator.cs`, `UIWindowLoader.cs` |
| Implemented | Buff stat application | Buff logic now applies `PAD`, `PDD`, `MAD`, `MDD`, `ACC`, `EVA`, `Speed`, and `Jump` directly to the player build and cleanly removes them when buffs expire or refresh | Active buff skills now change simulator stats and movement-relevant values instead of only a partial subset | `SkillManager.ApplyBuffStats` |
| Partial | Buff icon / temporary-stat UI parity | Active buffs now drive a WZ-backed status-bar tray with live duration countdowns sourced from `UI/BuffIcon.img`, the tray supports client-style right-click cancellation directly from the live buff list, `UILoader` normalizes and loads the full `BuffIcon.img` icon surface, and the simulator now also consumes the authored `name` strings and `buff/*` traversal order from the same `BuffIcon.img` catalog so the nine observed stat-family icons (`incEVA`, `incPAD`, `incPDD`, `incMAD`, `incMDD`, `incACC`, `incCraft`, `incJump`, `incSpeed`) use the WZ-authored display names and WZ-authored family ordering instead of a duplicated simulator-only table. `SkillManager` continues to infer additional temporary-stat families from loaded skill text, action metadata, and buff payloads so buffs can surface `Craft`, `Max HP`, `Max MP`, `Booster`, `Stance`, `Invincible`, `Critical Rate`, `Damage Reduction`, `Transform`, `EXP Rate`, `Drop Rate`, `Meso Rate`, `Critical Damage`, `Combo`, `Charge`, `Aura`, `Shadow Partner`, `Debuff Resistance`, `All Stats`, `Hyper Body`, `Sharp Eyes`, `Soul Arrow`, `Dark Sight`, `Magic Guard`, `Bless`, `Maple Warrior`, `Dash`, and recovery-style states even when parsed numeric level data is sparse or a fallback description only exposes abbreviated `PAD`/`MAD`/`ACC`-style labels. This run extends the WZ-backed payload seam further for buff families that still depended too heavily on text fallbacks: `SkillLoader` now parses authored `criticaldamageMin` / `criticaldamageMax` and `damR` / `indieDamR` fields from `Skill.wz` into level data, and `SkillManager` consumes those parsed values when resolving tray eligibility and temporary-stat labels so `Sharp Eyes`-style critical-damage buffs no longer depend on generic `y` fallback handling alone and damage-reduction buffs such as `Power Guard` / `Magic Shield`-style families can promote onto the `Damage Reduction` lane without requiring guard- or shield-word matches in the description. The simulator also now consumes direct client-surface metadata already present in `Skill.wz/info` for two previously text-fragile families: `switchDamtoMP=1` promotes `Magic Guard` from authored metadata even with sparse text, and `info/invincible=1` promotes invincibility-style buffs onto the `Invincible` family without requiring the description to spell that out. The broader damage-reduction classifier was tightened at the same time so `Magic Guard` no longer picks up the generic `Damage Reduction` family just because the skill name contains `guard`; that buff now stays on its own identity family for icon choice, tooltip labeling, and tray ordering instead of carrying a second misleading guard bucket in the tooltip payload. Tooltips also now surface the resolved family name and the affected temporary-stat list together, which keeps named non-`buff/*` families such as `Magic Guard`, `Hyper Body`, or `Sharp Eyes` visible without losing the underlying stat list. Generic untyped buffs still carry the authored `united/buff` family name (`Increase Stats`) and anniversary/admin-event buffs whose loaded text explicitly mentions `anniversary` still switch to the authored `united/anniversary` icon family and display name (`Congrats from Admin`) instead of collapsing back onto the same catch-all tray entry. The remaining gap is the client-confirmed exact cross-family order for the full temporary-stat catalog beyond the simulator's current best-effort family-priority table, plus the residual temporary-stat families whose grouping or icon choice still appears to depend on client-owned runtime state or unpublished temp-stat metadata beyond the observed `UI/BuffIcon.img` surface and the simulator's parsed skill metadata | Buff-heavy jobs are visible on the HUD, can be cleared without waiting for expiry, and no longer collapse as often into a fully opaque generic tray entry while tooltip text and primary-family selection stay closer to the WZ stat-family surface for both direct numeric buff fields and several text-, abbreviation-, action-, identity-, or payload-inferred buff families. The known nine `BuffIcon.img/buff/*` families now use the WZ-authored names and family order directly instead of a simulator-side duplicate, percent-based max-stat consumables now stay visible in the live tray instead of silently falling through when their payload is carried only by `mhpR` / `mmpR`, and the tray now also catches more Big Bang-era authored buff payloads directly from parsed WZ metadata instead of waiting for skill-text heuristics: `indieMhp` / `indieMmp` families stay on the `Max HP` / `Max MP` lane, `indieAllStat` and full four-stat payloads can promote onto `All Stats`, `asrR` / `terR` resistance buffs can promote onto `Debuff Resistance`, authored `criticaldamageMin` / `criticaldamageMax` payloads now keep critical-damage buffs on the `Critical Damage` lane even when the text is sparse, and authored `damR` / `indieDamR` payloads now keep damage-reduction buffs on the `Damage Reduction` lane even when their descriptions do not spell out guard/shield wording. `Magic Guard` and invincibility-style buffs are less fragile because the tray now uses authored `info/switchDamtoMP` and `info/invincible` metadata instead of relying only on string matching, and `Magic Guard` no longer drags a misleading generic `Damage Reduction` label into the tooltip just because of the word `guard`. Tooltip content is also less opaque because the status-bar buff tooltip now shows both the resolved buff family and the enumerated temporary stats instead of forcing one or the other. The remaining gap is narrower and is centered on the client's exact runtime ordering rules for the full temporary-stat catalog plus the last client-owned grouping/icon decisions that are not recoverable from `BuffIcon.img` or the currently parsed skill payload surface alone | `SkillManager.ActiveBuffs`, `SkillManager.ConfigureBuffIconCatalog`, `SkillManager.GetStatusBarBuffEntries`, `SkillLoader.CreateLevelData`, `SkillLoader.NormalizePassiveStatAliases`, `StatusBarUI.cs`, `UILoader.cs`, `MapSimulator.Content.cs` (`UI/BuffIcon.img`, `Skill/*/common/indiePad`, `Skill/*/common/indieMad`, `Skill/*/common/indieAcc`, `Skill/*/common/indieEva`, `Skill/*/common/indieSpeed`, `Skill/*/common/indieJump`, `Skill/*/common/indieMhp`, `Skill/*/common/indieMmp`, `Skill/*/common/indieAllStat`, `Skill/*/common/asrR`, `Skill/*/common/terR`, `Skill/*/common/criticaldamageMin`, `Skill/*/common/criticaldamageMax`, `Skill/*/common/damR`, `Skill/*/common/indieDamR`, `Skill/*/info/switchDamtoMP`, `Skill/*/info/invincible`, `CUIStatusBar::Draw`, `CUserLocal::Update`) |
| Partial | Melee / ranged / magic resolution | The runtime now preserves WZ `lt/rb` attack bounds, resolves melee and magic targets with separate ordering rules, gives projectile skills family-specific collision ordering plus homing steering, keeps homing active even when WZ `ball` data also marks the projectile as exploding, detonates WZ `ball/explode` projectiles on impact so nearby mobs within `explodeRadius` are resolved under the same effective target cap, respects the effective projectile hit cap from both `level/mobCount` and `ball/mobCount`, routes WZ `info/type` through the skill-family classifier so no-ball spell and shoot families no longer collapse into melee resolution by default, now also treating client-confirmed shoot-family `info/type=52` skills such as Aran `21120006` as ranged even when WZ omits a `ball` node, routes WZ `info/type=32` plus `info/massSpell=1` spell families such as Mage `Slow` / `Seal` back onto the magic lane instead of leaving those no-ball area spells on melee defaults, and now also keeps the client-confirmed Outlaw no-ball shoot-family helpers `5211004` / `5211005` on the ranged lane despite their melee-looking WZ `info/type=1`, while the shared shoot-family resolver also carries `5211006` so the loader and runtime use one client-backed table instead of drifting apart. Direct no-projectile ranged skills still get their own fallback hitbox and target ordering instead of reusing the melee branch, route through the client-shaped `DoActiveSkill_ShootAttack` fallback split so melee-first families probe the close-range lane before falling back to ranged resolution while the client-confirmed direct-shoot families stay on the shoot lane, preserve the special client one-offs where Gunslinger `Double Fire` (`5001003`) and Outlaw `Triple Fire` (`5210000`) still attempt the melee lane before falling back to shoot despite otherwise living in the shoot-family helper table, and thread that same melee-first-vs-shoot preference through deferred moving-shoot preferred-target selection instead of always snapshotting a ranged-only target. Projectile families still route WZ `info/rectBasedOnTarget=1` through an additional target-anchored rectangle after first impact so client rect-attack families such as `3001004`, `3101005`, and `5201002` no longer depend on raw projectile overlap alone, re-resolve chained follow-up targets from the live impact/serial origin using the WZ `range` bounce radius plus nearest-neighbor chaining instead of only replaying the initial overlap snapshot, stage multi-shot projectile release from WZ `ballDelay` / `ballDelayN` timing instead of front-loading every projectile on cast start, assign WZ `info/multiTargeting=1` projectile volleys to distinct live targets inside the authored ranged hitbox before spawn so skills such as `2301005`, `3001004`, `5301000`, and `20021166` no longer fire every shot through the same generic spread-first path when valid separate targets already exist, derive projectile collision bounds from the live WZ ball frame size and origin while sweeping those bounds across the projectile's previous-to-current travel step so fast or large projectiles no longer rely on a single centered point-instant overlap check, and thread per-target client damage override/decay branches through the live hit order so `Arrow Bomb: Bow` (`3101005`) uses its WZ `x` damage lane instead of the generic `damage` fallback, WZ-published x-based chain falloff still applies where the skill data actually publishes that decay value, and the remaining client-owned `SKILLENTRY::AdjustDamageDecRate` fixed tables now also drive `Energy Orb` (`5121002`), `Iron Arrow: Crossbow` (`3201005`), `Piercing` (`3221001`), `Ricochet` (`33101001`), and `Blade Fury` (`4341004`) through their exact client multipliers instead of flattening those families back onto one generic damage percent | Skills are closer to the client across both classifier and execution seams: no-ball mass-spell debuffs no longer inherit melee ordering, the missing Outlaw shoot-family helpers no longer collapse back onto the melee lane just because WZ publishes `info/type=1` without `ball`, no-projectile ranged skills now use the client-shaped melee-first vs direct-shoot split instead of one unconditional ranged branch, and deferred moving-shoot targeting no longer snapshots only the ranged lane when the live client family would try a close-range strike first. The remaining gap is narrower and is now centered on exact ammo and bullet-position gating plus ladder or vehicle checks inside `DoActiveSkill_ShootAttack`, any residual client-owned shoot-family classifier one-offs that still are not observable from WZ plus the confirmed helper tables, and edge-case timing or packet-owned branch state that still lives outside WZ-authored geometry | `SkillManager.ProcessMeleeAttack`, `ProcessMagicAttack`, `ProcessNoProjectileRangedAttack`, `SpawnProjectile`, `CheckProjectileCollisions`, `ResolveQueuedSerialTargets`, `ResolveDeferredPreferredTargetMobId`, `SkillLoader.ParseSkillInfo`, `SkillLoader.CreateLevelData`, `ClientShootAttackFamilyResolver` (`Skill/521.img/5211004/info/type`, `Skill/521.img/5211005/info/type`, `Skill/521.img/5211006/info/type`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::DoActiveSkill_MagicAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `is_shoot_skill_not_switched_to_melee_attack@0x6edcf0`, `SKILLENTRY::AdjustDamageDecRate`) |
| Partial | `DoActiveSkill_*` family parity | Generic movement, summon, and prepare/charge families now execute, movement-family dispatch now separates teleport snaps, grounded rush, flying-rush glide, and jump-rush launches from client-confirmed `TryDoingTeleport` / `TryDoingRush` / `TryDoingFlyingRush` seams, teleport skills now honor WZ-linked Teleport Mastery body-attack passives by treating `info/affectedSkillEffect=bodyAttack&&stun` skills such as `2111007`, `2211007`, `2311007`, and `32111010` as passive follow-up data instead of directly castable actives and firing their destination hitbox immediately after the associated teleport resolves, WZ-linked helper branches with `info/affectedSkill` plus `info/affectedSkillEffect` now stay off the direct-cast lane as passive follow-up data rather than only the older `psd=1` subset, so linked riders such as Battle Mage `Advanced Dark Aura` (`32120000`), `Advanced Blue Aura` (`32110000`), and Corsair `Bullseye` (`5220011`) no longer surface as standalone castable actives in the simulator, Battle Mage aura actives now honor the WZ text on `Dark Aura` (`32001003`), `Blue Aura` (`32101002`), and `Yellow Aura` (`32101003`) by cancelling on same-skill recast and removing any other active aura before the new one applies so those buffs cannot stack simultaneously, learned aura-dot riders with `info/affectedSkillEffect=dot` plus `info/dotType=aura` now pulse once per second around the player while their linked aura buff is active and stop at 1 HP like the client-facing WZ text, meso-explosion skills flagged by WZ `info/mesoExplosion` now consume nearby owned mesos inside the skill `lt/rb` box before applying their multi-hit payload, smoke-field skills with WZ `info/zoneType=invincible` now create a persistent local protection zone and tile it through the simulator draw path so `Smoke Bomb` and `Party Shield` block mob and touch damage while the player stays inside the skill box, and admin-skill execution now enforces GM/SuperGM job ownership at cast time so broader skill catalogs cannot fire `900.img` / `910.img` skills from normal jobs. Bound-jump start gating now also follows the client-confirmed `CUserLocal::DoActiveSkill_BoundJump` split more closely: WZ `info/avaliableInJumpingState=1` jump-chain skills stay off the ground-start lane by requiring an active airborne jump window instead of firing from a standstill, explicit client-owned bound-jump one-offs `11101005`, `33001002`, and `35101004` now stay on the grounded start lane, the Wild Hunter double-jump branch still rejects swim/fly field states like the client path at `0x93fdf0`, WZ `info/requireHighestJump=1` skills such as `23111001`, `23111003`, and `4331005` now stay behind an airborne near-apex gate instead of firing anywhere in the jump arc or from the ground, the movement-family classifier now inspects both `prepare/action` and the primary `action/*` branch so bound-jump actions such as `assaulter`, `backspin`, `screw`, and other flash-jump families still promote onto the stronger bound-jump lane when a generic prepare token such as `dash` would otherwise hide the real movement family, the deferred moving-shoot startup hold now uses the strongest loaded movement-action animation among those authored movement branches instead of only one prepare-action pick, and the client-confirmed Wind Walk (`11101005`) launch scale now matches the same `level/4` family used by the client-bound flash-jump branch instead of the simulator窶冱 older stronger `level/2` fallback. Rocket Booster deferred launch and active flight now also revalidate through dedicated start-vs-upkeep seams so cancelled, blocked, or interrupted casts tear down the local transform/mount state instead of lingering after the movement family becomes invalid, the client-backed `TryDoingRocketBooster` launch seam now defers MP consumption and cooldown start until the deferred launch actually resolves instead of spending resources on keypress before the start window completes, re-reads the live learned skill level at launch time the same way the client fetches `35101004` inside `TryDoingRocketBooster`, the `TryDoingRocketBoosterEnd` landing branch no longer re-consumes MP for its touchdown attack, the local avatar transform lane now keeps the authored normal-vs-tank `rbooster_pre` / `tank_rbooster_pre` startup branch by resolving Rocket Booster launch through the active tank-mode transform instead of collapsing the normal branch into the generic `rbooster` body state or keying tank startup only off the transient current action, and the Rocket Booster launch seam is now closer to the client-owned `m_nRocketBoosterVY` split because the deferred start stores a dedicated vertical launch value on queue, launches through that stored vertical-only booster speed instead of the generic flash-jump helper, and clears the active booster loop only after the stored launch phase has been consumed and the landing branch resolves | The remaining gap is narrower and centered on the last client-owned per-family special cases rather than generic execution support; linked affected-skill helper branches now stay off the hotkey/direct-cast surface even when WZ omits `psd=1`, Battle Mage aura toggles now cancel/swap in the client-facing WZ manner, aura-dot follow-up data executes from the owning aura instead of being stranded as inert metadata, higher-jump-only attack families now respect their authored WZ gate, and the bound-jump / Rocket Booster row is closer because generic prepare tokens no longer hide authored bound-jump actions, moving-shoot startup holds can follow the longest authored movement branch, Wind Walk now uses the client launch scale, and Rocket Booster now revalidates its live learned level at deferred launch time instead of trusting the original keypress snapshot while still owning a dedicated stored vertical launch seam instead of reusing the generic bound-jump helper all the way through deferred execution. The simulator still simplifies the residual client-only movement one-offs such as the exact moving-shoot entry snapshot beyond the retained facing/origin/preferred-target data, stored bullet or cash-item metadata, exact mob-target derivation and client request gating inside `TryDoingSmoothingMovingShootAttack`, the precise Rocket Booster stored launch magnitude and the rest of the internal flag/packet choreography behind `m_nRocketBoosterVY` / `m_bRocketBoosterStart` / `m_bRocketBoosterLoop`, the final highest-jump timing window beyond the current WZ-backed near-apex gate, and the remaining field-family special cases that are not fully expressible from WZ alone | `SkillManager`, `PlayerCharacter`, `PlayerCombat`, `PlayerSkillStateRestrictionEvaluator`, `UnitTest_MapSimulator/RocketBoosterParityTests.cs` (`CUserLocal::DoActiveSkill_*` family, `CUserLocal::DoActiveSkill_BoundJump`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`) |
| Partial | Shadow Partner render/action parity | The simulator now treats Shadow Partner as its own buff-owned companion render surface instead of only a temporary-stat icon family: `SkillLoader` loads the WZ `special/*` action tables for the confirmed Shadow Partner skills (`4111002`, `4211008`, `14111000`), derives the companion's baseline behind-the-player separation from the authored idle-frame `origin.x` instead of a second simulator-only spacing literal, and `SkillManager` binds that data to the buff lifecycle while `PlayerCharacter` draws a dedicated semi-transparent companion layer behind the avatar using those skill-owned `stand*`, `walk*`, `alert`, `stab*`, `swing*`, `shoot*`, `ladder`, `rope`, `fly`, `jump`, `sit`, `prone`, and `dead` frames instead of trying to reuse the base assembled avatar. Attack-family playback now mirrors through that dedicated action table with a delayed follow-up start keyed from the companion attack animation's first-frame delay rather than a fixed runtime constant, so the clone no longer front-runs the player attack on the same frame, non-attack actions fall back through the companion action catalog instead of dropping immediately to the base body path, and spawn still enters through WZ-backed `create*` branches when the skill provides them, preferring the `_s` or `_f` variants from the current player state before falling back to `create2`/`create3`/`create4`. The companion state machine also holds active `create*` and attack animations until their WZ frame sequence actually finishes before consuming a queued fallback or idle action, which keeps the clone from snapping back to stand/walk as soon as the player leaves the originating action. A WZ pass this run also confirmed that the visible `create2`/`create3`/`create4` spawn surfaces for the confirmed v115 Shadow Partner skills collapse to the same authored alert-family art, so the remaining branch gap is now the client's exact selection rule rather than a missing distinct WZ render path. This run also wires the client-backed ghost helper evidence deeper into the simulator seam: the body's `ghoststand`, `ghostwalk`, `ghostjump`, `ghostproneStab`, `ghostladder`, `ghostrope`, `ghostfly`, and `ghostsit` families still alias back onto the clone's authored `special/*` actions, but `PlayerCharacter` now prioritizes a client-shaped mapping layer derived from `CAvatar::MoveAction2RawAction` plus `action_mapping_for_ghost@0x406500` before the broader alias fallback, so ghost walk/stand/jump/climb/prone/sit resolve through the exact classic movement families first and ghost float/swim states now collapse back onto the stand-family branch before falling through to authored `fly`/`jump` coverage. The runtime also re-evaluates that client-backed float-vs-ground mapping when the player stays on the same body action name but changes between grounded and floating states, which avoids stale clone playback during swim/fly transitions. A client pass this run confirmed that `CActionMan::GetShadowPartnerProp@0x41ba60` resolves the skill's `special` UOL and that `CActionMan::LoadShadowPartnerAction@0x423cf0` caches per `(skillId, rawAction)` while walking action-code aliases before the plain action-name fallback; within the existing simulator seam, `PlayerCharacter` now mirrors three missing pieces that were still visible in play even when the action name itself did not change: Shadow Partner re-syncs its facing when the player turns in place instead of holding the stale previous direction until another action change occurs, the simulator now feeds local `PlayerState.Hit` through the companion resolver as a dedicated `hit -> alert/stand` branch instead of hiding hit-stun behind the base `stand1` body action name, and the draw path now applies the client-confirmed additive layer displacement from `CUser::PrepareShadowPartnerActionLayer@0x8e1730` on top of the WZ origin data by preserving the classic `±30 px` side nudge for the normal lane and `+50 px` vertical offset for ladder/rope back-action playback. `SkillLoader` now also parses authored per-frame `a0` / `a1` alpha ramps from the companion canvases, and `PlayerCharacter` interpolates that frame alpha on top of the existing client-backed 150-alpha companion tint so Shadow Partner no longer flattens every branch to one opacity while the animation advances. A v95 client pass on `is_back_action@0x45f1b0` also narrowed the confirmed back-action surface for the clone lane to the ladder/rope plus ghost ladder/rope raw actions already modeled by the simulator seam, so the remaining back-action gap is no longer an unverified broader family in this build. Queued non-blocking fallback actions also keep their facing updated while a blocking `create*` or attack branch is still running, so a turn during spawn no longer leaves the clone exiting the create animation facing the old direction. The remaining gap is the exact client selection and cache alias table behind `CActionMan::LoadShadowPartnerAction` for raw-action cases beyond the now-modeled movement plus hit-state families, the precise layer depth and any residual alpha choreography still encoded in client-owned frame-entry metadata rather than the visible WZ canvas props, and any final mirror cadence nuances that still require direct client evidence | Shadow Partner buffs now produce an actual mirrored companion animation surface with its own action ownership, WZ-derived spacing, WZ-timed delayed attack playback, WZ-backed spawn/create playback for the classic skills that author it, less abrupt cadence because create and attack branches are allowed to complete before the simulator drops to idle or another non-blocking fallback, and broader client-shaped action fallback because ghost-body aliases and common alert/climb variants now resolve through the companion action catalog instead of snapping straight to generic idle. The clone now also follows the client-backed ghost move-action mapping more closely for grounded vs floating idle/walk/jump/climb/prone/sit branches instead of treating every `ghost*` body family as an equal synonym set, no longer keeps a stale facing snapshot when the player flips direction without changing actions, now surfaces local hit-stun through the companion action resolver instead of silently staying on idle because the simulator body reuses `stand1` during `PlayerState.Hit`, keeps the client-observed Shadow Partner layer displacement on top of the authored WZ origins for the classic behind-the-player lane plus ladder/rope back-action lane, and now respects authored per-frame companion alpha ramps instead of flattening the whole clone surface to one fixed opacity. The remaining mismatch is no longer "no clone exists"; it is the narrower client-owned choreography around the last unmapped raw-action alias-table cases, any deeper frame-entry alpha or layer-depth behavior that does not survive through the visible WZ canvas property surface, and final timing polish that still sits behind `CActionMan::GetShadowPartnerProp` / `CActionMan::LoadShadowPartnerAction` / `CUser::PrepareShadowPartnerActionLayer` | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `UnitTest_MapSimulator/ShadowPartnerParityTests.cs` (`CAvatar::MoveAction2RawAction`, `CActionMan::GetShadowPartnerProp@0x41ba60`, `CActionMan::LoadShadowPartnerAction@0x423cf0`, `CUser::PrepareShadowPartnerActionLayer@0x8e1730`, `is_back_action@0x45f1b0`, `action_mapping_for_ghost@0x406500`, `Skill/*/{4111002,4211008,14111000}/special/*`) |
| Partial | Queued follow-up attack families | Attack skills that publish WZ `finalAttack` follow-up maps now queue a deferred extra strike after the initiating melee or projectile hit resolves, gate that follow-up by the equipped weapon type both when the proc is queued and again when it executes, reuse the learned follow-up skill's proc rate, carry the preferred target forward into the queued execution path, preserve the queued facing through deferred hitbox and projectile resolution even when the queued branch itself takes an extra deferred moving-shoot hop before firing, overwrite any still-pending final-attack proc with the newest one so the simulator matches the client's single pending follow-up slot instead of stacking multiple deferred final attacks, and keep that overwrite scoped to the final-attack slot instead of clearing unrelated queued summon hits. Thunder Breaker `Spark` (`15111006`) still runs through its own timed attack-triggered proc family by treating the WZ `info/condition=attack` + `info/chainAttack=1` + `info/chainattackPenalty=1` cast as a timed buff with a separate pending spark slot, but the queued Spark branch now re-enters the shared shoot/projectile attack path instead of a simulator-only direct-hit loop, carrying the saved trigger mob id and saved source position back into projectile spawn so the deferred proc keeps the client-shaped shoot-entry choreography and stored origin even when the queued branch later takes an extra deferred moving-shoot hop before firing. WZ `info/chainAttack=1` + `info/chainattackPenalty=1` serial families such as `2221006` and `5121002` still use their own single pending serial slot, preserve queued facing, recheck weapon type before the deferred branch executes, and derive later chain hits from the stored origin mob/position even when the opener only resolved one live hit. This run also keeps queued final-attack, Spark, and serial execution closer to the client by re-reading the live learned skill level when the deferred branch actually fires instead of consuming the level snapshot captured when the proc was queued, now applies that same live-level revalidation through the queued final-attack branch's extra deferred moving-shoot hop so the stored follow-up cancels cleanly if the learned level drops to zero before the projectile or ranged payload actually fires instead of falling through `GetLevel(0)` and still executing as level 1, and now also uses the queued cast snapshot when drawing the follow-up branch so Spark and serial cast effects honor the stored source origin and queued facing instead of snapping back to the player's live body position and live turn state while the deferred branch resolves. WZ-authored `x` chain-penalty decay and the client-owned fixed multiplier tables for no-`x` families now both apply through the deferred or timed chain path as well, so queued follow-up families no longer flatten back onto one generic damage percent after the opener. The remaining gap is the last client-only spark/final-attack choreography beyond the current queued-slot, target-retention, shared shoot-entry reuse, queued-origin/facing playback, timing, and live-level revalidation model | Final-attack style procs no longer collapse into the initiating cast timing, queued strikes now cancel if the player changes to a non-matching weapon before the deferred attack fires, if a newer follow-up proc replaces the pending one before it can execute, or if the follow-up skill has been locally unlearned before the queued branch actually fires; replacing a pending final attack also no longer wipes unrelated queued summon damage. Queued follow-up casts that pass through the simulator's extra deferred moving-shoot payload path now keep the stored facing/origin snapshot and revalidate the live learned level again at the actual deferred fire time instead of silently snapping back to the player's live turn state or consuming a stale queued level, queued Spark and serial cast effects now render from the same stored origin/facing snapshot that drives their deferred execution instead of always drawing from the player's current body state, active `Spark` buffs now produce their own delayed follow-up through the same shared shoot/projectile branch the client uses while preserving the stored trigger origin for projectile spawn, and WZ-marked serial-attack plus no-`x` fixed-table families now keep their deferred damage falloff aligned to the same post-opener hit order used by live chain resolution. The remaining mismatch is narrower and is now concentrated in deeper client-owned spark/final-attack choreography such as the exact per-branch attack entry path, the final ranged-final-attack `TryDoingShootAttack(..., 65, ...)` distinction, remaining source-target lifetime quirks, and any residual branch-specific shot metadata the simulator still does not snapshot | `SkillManager`, `SkillLoader`, `UnitTest_MapSimulator/SkillManagerQueuedFollowUpParityTests.cs` (`CUserLocal::TryDoingFinalAttack`, `CUserLocal::TryDoingSerialAttack`, `CUserLocal::TryDoingSparkAttack`) |
| Partial | Repeat-skill sustain families | `CUserLocal::TryDoingRepeatSkill` still owns more of the Mechanic repeat-state machine than the simulator, but the runtime now carries repeat-skill state not just for siege and tank-siege sustain teardown but also for tank mode and SG-88: siege and tank-siege still enforce the WZ `fixedState` / `canNotMoveInState` lock, honor `onlyNormalAttack` by blocking unrelated skill casts, auto-release after their 5-second client timer, emit the client-mapped timeout effect-request ids (`35110004` for siege and `35120013` for tank-siege), and hand tank-siege back to tank mode instead of leaving the transform latched; tank mode (`35121005`) and the two siege sustains now also route normal attack input through their repeat-owned skill shots with the client-confirmed `TryDoingSiege` lockout windows (`420 ms` for tank mode, `180 ms` for siege / tank-siege) instead of falling back to the simulator's generic basic attack; tank-siege timeout and local cancel now follow a closer client-style staged completion path by latching a pending mode-end request when the end effect is requested, suppressing further repeat-owned attacks while that latch is set, and scheduling both the no-listener fallback and an explicit local acknowledgement back through the shared client-timer update seam instead of finishing inline at the acknowledge call site, with `SkillManager` exposing a repeat-mode-end request/ack seam for future server-owned confirmation while preserving the old next-tick local fallback when nothing listens. That pending tank-siege lane now also tracks request time on simulator time instead of `Environment.TickCount`, can complete from an observed return-to-tank transform before the synthetic local acknowledgement fires, now lets `PlayerManager` discard stale local mode-end requests once the shared repeat state is already gone, and delays its listener-owned local fallback until the WZ `skill/35121013/effect` end-effect window finishes (`180 ms` total) instead of acknowledging on the very next update tick. SG-88 still uses the client skill id (`35121003`), starts with assist fire disabled, tracks the local `bDone` / branch-point progression in repeat state, shows the `KeyDownBar4` HUD after the client's `810 ms` delay, arms summon assist after the client's `2790 ms` branch timing, refreshes repeat-state attack bookkeeping from actual summon fire instead of only toggle timestamps, and tears the summon down after the client-style `2520 ms` idle window if assist is toggled off and not re-armed. The remaining gap is the true client/server confirmation path behind tank-siege mode-end completion, the exact packet or request acknowledgement sequencing that would drive that confirmation instead of the simulator's local fallback plus observed-transform shortcut, and deeper per-branch Mechanic assist targeting or bookkeeping that still lives outside the simulator's modeled repeat-state timings | Mechanic repeat skills are now closer on fixed-state timing, in-state attack restrictions, repeat-owned normal attack cadence, SG-88 branch-state timing, and tank-siege teardown sequencing because the pending mode-end hop now stays inside the same client-timer-style update surface for both local fallback and explicit acknowledgement, can also resume from an externally observed return-to-tank mode instead of depending solely on the simulator's synthetic ack, clears stale listener-owned pending requests once repeat state already resolved, waits through the WZ-backed 180 ms tank-siege end-effect window before falling back locally, and keeps SG-88 repeat bookkeeping in sync with real assist fire. The simulator still simplifies the final client-owned confirmation behind tank-siege end plus deeper branch-specific packet or target bookkeeping | `SkillManager`, `PlayerCharacter`, `PlayerManager` (`CUserLocal::TryDoingRepeatSkill`, `CUserLocal::TryDoingSiege`, `CUserLocal::DrawKeyDownBar`, `CUserLocal::UpdateClientTimer`) |
| Partial | Bound-jump, rocket-booster end, and smoothing moving-shoot branches | `Rocket Booster` now follows more of the authored WZ startup and teardown cadence: the deferred launch keeps the cast-start facing, uses the active `rbooster_pre` or `tank_rbooster_pre` body action duration instead of one shared startup window, preserves that launch-facing through the airborne branch, detects touchdown for the landing strike, and immediately clears into the authored `rbooster_after` or `tank_rbooster_after` exit action while keeping recovery latched for the longer of the old 500 ms cleanup window or the loaded body-action duration. This pass also tightened the client-side start gate around that deferred launch: the bound-jump seam now rejects `Rocket Booster` while off-foothold, on ladder or rope, in free-fall, or in swim or fly states before the queued launch begins, matching the client-owned ground-start branch more closely instead of letting the simulator re-enter Rocket Booster from broader generic movement states. `TryDoingRocketBoosterEnd` is closer too: once the touchdown strike has happened, the simulator now raises the same local `35100004` recovery effect request after the client-observed `500 ms` landing-attack window before teardown completes instead of silently waiting out recovery with no effect request. The moving-shoot path still snapshots the cast-start preferred mob, firing direction, and world origin so deferred projectile and direct ranged payloads resolve from that stored origin after movement startup instead of re-reading a later position or facing, and it is now closer to the client because WZ `info/delay` values and movement-action animation duration hold deferred `casterMove` payloads until longer startup branches such as `backstep` or `assaulter` finish instead of forcing the old 60-180 ms fallback window, deferred moving-shoot casts snapshot the rendered `handMove` or `hand` body anchor from the current avatar frame so stored projectile and direct ranged origins follow the client shoot-point branch more closely instead of always falling back to the player body origin, deferred projectile release now keeps that stored hand-anchor Y instead of reapplying the old generic `body - 20` muzzle offset, and the deferred preferred-target snapshot now resolves from that same stored shoot origin instead of a later player-body position. The client `TryDoingSmoothingMovingShootAttack` request surface is also narrower now because the simulator refuses to queue another smoothing moving-shoot cast while an earlier deferred moving-shot payload is still pending, matching the client's single active moving-shoot entry more closely instead of stacking multiple delayed moving shots through the queue. The client `info/type=40` bound-jump family is also closer to `DoActiveSkill_BoundJump`: midair flash-jump families no longer auto-promote from a fresh foothold jump, `Wind Walk` stays a ground-start bound jump, `Jaguar Jump` stays a grounded bound-jump launch instead of sharing the airborne flash-jump gate, and the client velocity formulas now cover the missing `13101004` / `14101004` type-40 family in addition to the earlier Night Lord, Shadower, Dual Blade, and Jaguar-specific cases. The remaining gap is the client's fuller moving-shoot entry snapshot such as stored bullet or cash-item metadata, exact mob-target derivation beyond the retained preferred target plus stored shoot origin, the deeper execute-time anti-repeat check beyond the new single-pending request gate, and the last server-confirmed Rocket Booster or non-WZ bound-jump special cases that still live in client-owned branches | Movement-coupled attack skills now preserve more of the client's traversal, startup timing, landing cadence, moving-shot target retention, cast-origin lock, shoot-point anchoring, and move-family directionality instead of collapsing into the old teleport/rush/flying-rush-only movement path or instant moving-fire hits. Bound-jump starts are less generic now because flash-jump families no longer re-jump off the ground, grounded families such as `Wind Walk`, `Jaguar Jump`, and `Rocket Booster` keep their client-shaped start-state expectations, the Rocket Booster landing branch now also emits the client-mapped post-attack recovery effect request instead of ending silently, and the missing type-40 launch formulas for the Bowman and Night Walker family now use the client's stronger horizontal-first arc instead of the simulator's earlier inverted double-jump fallback. Deferred projectile release also stays aligned to the stored hand anchor instead of snapping back through the older generic body-height offset during the delayed shot, and deferred moving-shoot requests no longer stack multiple pending delayed casts while one client-style moving-shot entry is already armed | `SkillManager`, `SkillLoader`, `SkillData`, `PlayerCharacter`, `CharacterAssembler` (`CUserLocal::DoActiveSkill_BoundJump`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CUserLocal::TryDoingSmoothingMovingShootAttack`) |
| Partial | Key-down / charge skills | The simulator now routes key-down HUD rules through the prepared-skill runtime so client-confirmed bar suppression for `3121004`, `5201002`, `13111002`, `22121000`, `22151001`, `33121009`, `35001001`, and `35101009` no longer shows the generic status-bar gauge, preserves the `KeyDownBar4` skin for `35121003`, mirrors the client's 1-second generic gauge timing for `14111006`, caps mechanic `35001001` / `35101009` sustain after the charge window enters its actual hold phase instead of counting the 8-second branch limit from cast start, loads WZ `repeat` branches into the prepared-skill hold loop so charge families such as `4341002`, `5101004`, and `15101003` swap to their dedicated repeat effect/cadence once the hold phase begins, and now also loads and renders WZ companion prepare/hold/release branches such as `prepare0`, `keydown0`, and `keydownend0` so multi-layer prepared-skill effects like `24121000`, `5221004`, and `5721001` can draw their negative-`z` underlay branch behind the avatar instead of dropping that branch entirely. Prepared hold teardown is now closer to the client's shared timer path because capped keydown sustain starts a `prepared-release` client timer when the real hold phase begins instead of relying only on per-frame hold polling, cancel requests now only acknowledge matching local prepared-skill state while tearing that state down through the shared cancel seam instead of blindly succeeding on unmatched ids, and the local prepared-skill handshake now records both the hotkey slot and a stable per-press physical-input ownership token derived from the exact triggering key/button path so duplicate bindings share one held owner while only that same held input can issue the matching release or same-skill toggle. The client-confirmed `300 ms` exclusive-request gate from `CUserLocal::DoActiveSkill_Prepare` now also rides a broader local prepared-skill request seam instead of only prepare start, so same-family release toggles, hotkey key-up release, and matching cancel requests all refresh the same short exclusive window before the next prepare attempt. The shared prepared-skill release seam now also owns the Dual Blade charge pair `4341002` / `4341003`, the Big Bang trio `2121001` / `2221001` / `2321001`, and Cannon Shooter `5311002`: WZ marks all of them with `info/chargingSkill=1`, the mage descriptions explicitly describe the attack as culminating after the charge finishes, `Skill/531.img/5311002` publishes the same `prepare` -> `keydown` -> `action/0 = noiseWave` body-backed release shape already used by the avatar parity row, and the runtime now follows through by treating those holds as release-triggered execution instead of firing their payload through the generic repeating keydown loop. Their local prepared-skill HUD text now also stays on the client-style `Release` branch once the charge is armed instead of falling back to the generic ready text. Dragon-owned keydown HUD also still suppresses the simulator's player fallback so Evan charge bars only render when the dragon runtime exposes a live dragon anchor instead of floating over the player when the companion is hidden or unavailable. The remaining gap is the true client-owned raw scan-code handshake and the rest of the wider exclusive-request catalog outside the modeled prepared-skill request family, plus any additional charge or follow-up branches whose release ownership still is not confirmed enough to leave their current bespoke paths | Charge-family skills now match more of the client's visible HUD branching, mechanic timeout behavior, timer-owned hold expiry, multi-layer prepare visuals, local input ownership, and dragon-bar ownership instead of always reusing one generic bar, one foreground-only cast effect, or one unowned release path; the mechanic flamethrower family now gets its full sustained window after charging before auto-release, repeated charge holds can follow the WZ-authored repeat branch, Dual Blade `Final Cut` (`4341002`), `Monster Bomb` (`4341003`), the Big Bang trio (`2121001`, `2221001`, `2321001`), and `Monkey Wave` (`5311002`) no longer advertise or animate a release-owned charge while still executing through the generic hold-repeat loop, unrelated duplicate bindings now share one held prepared-skill owner token instead of competing with separate logical slot presses, prepare spam respects the client's local `300 ms` exclusive-request gate across the modeled prepared-skill request family instead of only on prepare start, and Evan dragon charges no longer fall back to a player-anchored world bar when the dragon actor is absent. The underlying prepared-skill execution loop is still simplified vs client behavior because the simulator still does not model the OS-level scan-code path the client stores directly, the broader client/server exclusive-request handshake outside these prepared-skill request seams, or the last release/follow-up branches that are not yet confirmed enough to leave their current bespoke paths | `SkillManager`, `SkillLoader.cs`, `StatusBarUI.cs`, `UILoader.cs`, `PlayerInput.cs`, `PlayerManager.cs` (`Skill/212.img/2121001/{info/chargingSkill,desc}`, `Skill/222.img/2221001/{info/chargingSkill,desc}`, `Skill/232.img/2321001/{info/chargingSkill,desc}`, `Skill/434.img/4341002/{info/chargingSkill,common/time}`, `Skill/434.img/4341003/{info/chargingSkill,common/time}`, `Skill/531.img/5311002/{info/chargingSkill,prepare,keydown,action}`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::DrawKeyDownBar`, `CDragon::DrawKeyDownBar`) |
| Partial | Swallow, mine, cyclone, and sit-down healing branches | The simulator now auto-deploys mine summons from learned `33101008` only while the player is in the Wild Hunter jaguar riding family, suppresses that local `TryDoingMine` loop in town maps, and now mirrors the client startup cadence more closely by seeding the first continuous-movement deploy from the client-observed `update_time - 1000` lead before returning to the steady `1500 ms` mounted-movement interval. Mine movement intent is now also sourced from the same held left/right input direction the client reads before falling back to resolved foothold velocity, so brief startup acceleration no longer stalls the first mounted mine check. Cyclone still sustains as a timed `32121003` body-attack loop with `1000 ms` pulses only while the cyclone action remains active, sit-down healing still loads its support range from the summon branch metadata and honors WZ `minionAbility=heal` plus `condition=whenUserLieDown` only while the sitting player remains inside that heal box, `35111011` recovery still uses its WZ-authored HP-percent heal plus the skill text's `x=5` second re-heal lock, swallow-family detection and ladder/rope gating still stay on loaded WZ metadata only (`info/swallow` plus `dummyOf`), and the swallow cancel/absorb-ack seam still accepts the visible Wild Hunter skill (`33101005`) plus its WZ-linked dummy follow-up ids (`33101006`, `33101007`). Wild Hunter swallow also still follows the client's `TryDoingSwallowMobWriggle` / `TryDoingSwallowBuff` timing more closely after absorb confirmation: the visible skill still uses the `900 ms` keydown gauge before release, early release still cancels without executing, release-triggered payloads still consume cost only once the charge completed, the local absorb-ack seam still gates capture success, a successful absorb still automatically arms the `3000 ms` digest timer plus `500 ms` wriggle cadence instead of waiting for a second local digest cast, failed or invalidated absorb outcomes now route back through the same client cancel seam used by the other swallow exits instead of silently dropping local state, and the hidden attack dummy branch (`33101007`) still consumes the captured mob before firing its follow-up ranged payload. The simulator still does not mirror the client's exact swallow packet ordering, the exact server-owned capture/digest request choreography, or the remaining non-Wild-Hunter swallow-family nuances one-for-one | Several client-only active-skill branches now execute in play instead of falling through the generic cast path, Wild Hunter mine is closer to `CUserLocal::TryDoingMine` because it now respects the client town suppression and input-owned movement-start detection in addition to the existing jaguar-riding gate and first-deploy cadence, sit-down healing follows the authored area, percent-heal, and re-heal cadence instead of restoring on every support pulse, cyclone no longer keeps pulsing once its sustained cyclone action has already fallen off the avatar, and Wild Hunter swallow is closer to the client's visible `33101005` input loop because charge threshold, early cancel, absorb success, absorb failure/target-loss teardown, auto-started wriggle timing, and auto-digest completion now ride the same release-owned swallow state instead of requiring a second local digest trigger or leaving failed captures on a silent local clear. Swallow-family gating is also narrower and more data-backed now because false positives from skill names or action names no longer enter the swallow lane, and swallow cancel/ack callers can target the WZ-linked dummy follow-up ids directly instead of hardcoding the visible skill id. The remaining parity gap is now centered on exact swallow packet sequencing, the server-owned swallow request/ack ordering around `33101005` / `33101006` / `33101007`, the client `TryDoingMine` riding-vehicle-id ownership nuances beyond the simulator's current jaguar/town gating, party-owned or packet-owned sit-down healing robot coverage beyond the local summon seam, and any residual swallow-family behaviors the client owns outside the current local cancel/request seam | `SkillManager`, `SkillLoader.cs`, `SkillData.cs`, `PlayerCharacter`, `MapSimulator` (`CUserLocal::TryDoingSwallowBuff`, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::TryDoingMine`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingSitdownHealing`, `CUserPool::DoHealNearHealingRobot`) |
| Partial | Summon simulation | Summon-family skills now create a generic summon lifecycle with duration, rendering, summon attack-branch playback, periodic nearby attacks, deferred mob removal during summon attack resolution, taunt-only registration into the simulator's puppet/aggro system based on WZ `minionAbility`, client-backed movement-style buckets for stationary emplacement, grounded leash, owner-hover, and anchor-based roaming summons, owner ladder/rope plus hidden-or-mounted attack gating with the client-confirmed octopus / `33111003` / self-destruct exemptions, foothold-settled spawn and follow positioning for stationary, grounded-follow, and anchor-bound summons, WZ-backed summon attack-branch fallback for variant names such as `attackTriangle` instead of only `attack1` / `attack` / `attack0`, summon attack metadata sourced from `summon/attack*/info` so per-skill `attackAfter`, `attackCount`, `mobCount`, rectangular `lt/rb`, circular `sp/r`, and projectile-style `bulletSpeed` now drive the runtime, WZ `common/subTime` fallback cadence for summons that omit `attackAfter`, damaged-trigger reflector summons that now fire back at the mob that hit the owner instead of running on the generic periodic loop, WZ `info/minionAttack` tokens flowing into the summon hit-status inference path, self-destruct summons now consuming WZ `info/selfDestructMinion` plus `common/selfDestruction` to burst once and then remove themselves after their resolved attack/support action, and self-destruct summons that omit `summon/attack*` now fall back to WZ `summon/die` or `summon/die1` attack metadata so grenade-style Mechanic summons can use their authored explosion branch range and timing data instead of a generic box. The summon loader/runtime now also keeps authored `summon/prepare` windup branches, uses separate self-destruct `summon/die` / `summon/die1` animations as a post-hit removal phase instead of reusing the same generic attack clip, lets those die branches backfill missing summon attack metadata when the chosen attack branch is animation-only, and now also carries WZ-authored summon projectile and target-impact branches from `summon/ball/*` and `summon/mob/*` into the runtime instead of dropping them after metadata load. Tesla Coil (`35111002`) is now closer to the client's `CSummoned::TryDoingTaslaCoilAttack` lane too: the deployed three-coil set keeps slot ownership, the lead coil now drives one coordinated attack over a triangle built from the three active coil positions instead of three unrelated nearest-target checks, the queued hit packet lands on mobs whose body boxes intersect that live Tesla triangle, all three coils enter the shared attack timing together rather than behaving as isolated turrets, and the simulator now replays the authored Tesla `summon/ball/*` travel sprites plus `summon/mob/*` target-hit variants as visual-only attack choreography aligned to the already WZ-timed impact window. Summon hits are no longer forced to land on frame 0 either: the runtime now queues summon damage resolution from WZ-backed `hit/*/hitAfter` timing, `summon/attack*/info/bulletSpeed` travel time, and any authored summon prepare-phase duration so octopus, Tesla, and other delayed-impact summons resolve closer to their authored impact window, while summon attackers now also face their resolved target when they enter the attack branch so octopus-style stationary summons stop being locked to the owner-facing direction during projectile playback. Mechanic satellite summons (`35111001`, `35111009`, `35111010`) still use their WZ `common/x` max stack count, persist until manually cancelled instead of expiring on the generic fallback timer, and resolve attacks against the mobs the owner recently hit instead of the generic nearest-target loop. This run also moved summon-family rule selection onto one shared WZ-backed seam used by both the local summon runtime and the packet-owned summoned pool, so fallback duration ownership, assist-bucket selection, taunt-puppet registration, and packet-owned local taunt aggro handoff now stay aligned for reflector summons keyed by `condition=damaged` plus reflective `minionAbility`, SG-88's manual-assist lane, support summons keyed by WZ `minionAbility` (`heal`, `mes`, `amplifyDamage`), `minionAbility=summon` action-only summons, and owner-targeted Mechanic satellites instead of leaving the packet-owned path on an older hardcoded subset. The shared mob-targeting seam now also ranks active puppets by their registered aggro window, aggro value, current target ownership, and stable slot ordering so multiple taunt summons stop collapsing to a last-registered winner on every frame. The packet-owned path is closer too: `CSummonedPool::OnSkill`-style packets now preserve authored summon prepare branches instead of flipping straight to the attack pose, `CSummonedPool::OnAttack`-style packets now keep the WZ prepare-to-attack timing seam alive instead of restarting the clip from a generic attack timestamp, packet-owned attacks now replay the same WZ-authored summon projectile and target-impact branches already loaded from `summon/ball/*` and `summon/mob/*`, and packet-owned Tesla attacks now fan that shared visual timing across the owner's active coil set instead of leaving remote Tesla summons on a single generic hit effect. The packet-owned pool still tracks its own per-instance summon expiry timers instead of polling raw age directly, which keeps remote timed summon teardown closer to the timer-owned local summon seam. The simulator still does not reproduce the client's exact per-coil beam packet ownership and per-mob hit-packet choreography inside `CSummoned::TryDoingTaslaCoilAttack`, the octopus-specific projectile or beam presentation beyond target-facing attack playback and WZ-timed impact delay, the remaining grenade-style expiry choreography beyond WZ-authored prepare/die playback, the exact remote summon packet ordering that the live client uses to interleave `OnSkill` / `OnAttack` / `OnHit` for every summon family, the last client-only assist-target special cases not surfaced by WZ `minionAbility` / `condition` or packet assist bytes, or the final client one-for-one mob aggro retarget timing and heuristics | Summon-heavy jobs are now closer on assist-type dispatch, support-only summon behavior, reflector response timing, self-destruct cadence, summon-origin target ordering, owner-targeted satellite assists, Tesla Coil deployment count, coordinated Tesla triangle targeting, delayed-impact summon timing, cross-runtime summon rule ownership, taunt-puppet aggro arbitration, client-owned owner-state attack gating, and packet-owned summon attack presentation instead of treating every summon as the same instant periodic puppet attacker. Tesla and the self-destruct Mechanic summons also stay closer to their authored summon choreography because the simulator now preserves WZ windup/removal branches, uses die-branch explosion metadata when the attack branch only carries animation, lets the active Tesla trio attack as one field-shaped group instead of three disconnected turrets, replays the authored Tesla projectile-flight and target-hit art instead of resolving that attack as damage-only after the actor animation, keeps packet-owned summon expiry plus fallback assist selection on the same WZ-backed rule surface as local casts, no longer lets later-registered taunt summons trivially steal every mob from earlier ones, suppresses most summon attack dispatch while the owner is hidden by Dark Sight or Wind Walk or using a non-jaguar vehicle mount instead of only checking ladder/rope, and now lets packet-owned summons keep their authored prepare/projectile/impact presentation instead of collapsing remote summon packets back onto a generic attack-plus-hit-effect seam. The remaining parity gap is now concentrated in the last client-only Tesla packet choreography, octopus-specific projectile presentation beyond what WZ exposes directly, exact remote summon packet sequencing, residual client-special assist targeting beyond the WZ/packet-visible families, and the finer aggro retarget timing/details that still live in client-owned mob control paths | `SkillManager`, `SkillLoader.cs`, `SkillData.cs`, `SummonMovementResolver.cs`, `SummonRuntimeRules.cs`, `SummonedPool.cs`, `MobPool.cs`, entity/pool layer (`CUserLocal::DoActiveSkill_Summon`, `CSummoned::Init`, `CVecCtrlSummoned::WorkUpdateActive`, `CSummoned::TryDoingAttack`, `CSummoned::TryDoingTaslaCoilAttack`, `CSummonedPool::OnSkill`, `CSummonedPool::OnAttack`, `Skill/3511.img/35111002/summon/{prepare,ball,mob,die}`) |
| Partial | Remote summoned-pool packet parity | The simulator now has a first-class packet-owned summon field seam in `SummonedPool`: it dispatches the client packet family `0x116`-`0x11B` through explicit created/removed/move/attack/skill/hit handlers, decodes the `CSummonedPool::OnCreated` header-backed owner id / summoned object id / skill id / character level / skill level fields plus the immediately-following spawn position, move action, foothold, move ability, assist type, and enter type fields confirmed from `CSummoned::Init`, now also consumes the trailing `CSummoned::Init` avatar-look flag/body and Tesla Coil state plus three-point triangle payload, resolves owner context against the local player plus `RemoteUserActorPool`, samples packet move-path state into live summon world positions, decodes `CSummoned::OnAttack` one-for-one through the packet-owned character-level byte, packed action/facing byte, mob-count list, repeated mob-object-id / hit-action / damage tuples, and trailing byte, uses those attack tuples to drive summon-facing updates and per-target hit-effect playback against live mobs, plays summon spawn/attack/removal visuals from the existing WZ-loaded summon animations, surfaces hit effects, and now replays the client-confirmed local-owner summon-created side effects far enough to immediately resync packet-owned taunt summons through the shared mob-targeting seam on creation, replay Tesla Coil third-coil duration refresh for local owners via the loaded `35111002/35120001` WZ duration data, and mirror `CSummoned::OnHit` more closely by resolving mob-template attack hit overlays plus mob/summon hit sounds from the packet-owned hit payload instead of treating that packet as damage-only. This run closes more of the remaining ownership and hit-bookkeeping gap as well: IDA-confirmed `CUser::AddSummonedList` ownership is still mirrored through a dedicated per-user `PacketOwnedUserSummonRegistry` on the local `PlayerCharacter` and remote actors, with object-id keyed replace/remove behavior plus AddTail-style ordering that `SummonedPool` now uses for owner summon queries, Tesla third-coil refresh ordering, cancel matching, and puppet slot resolution instead of relying only on the pool-local owner dictionary; `MapSimulator` still exposes the broader packet transport seam through `/summonedpacket` plus `SummonedPacketInboxManager`; `LifeLoader` now reads mob attack hit metadata from the same nested `attack/info/hit/*` surface the client `CMobTemplate::LoadAttackInfo` path uses instead of only the older flattened fallback keys; `SummonedPool` now mirrors the positive-damage `CSummoned::OnHit` bookkeeping more closely by latching a packet-owned one-time action `15` window before the summon returns to idle instead of dropping straight back to idle whenever no authored `summon/hit` animation is present; and remote-user field teardown now also flushes that owner's packet-owned summons when the remote actor leaves or the remote-user pool is cleared so transport ownership stays closer to `CUserPool` lifetime rather than leaving orphaned summons alive behind their owner. The remaining gap is narrower: the simulator still only recovers `bHitAttach` when WZ actually publishes the nested hit flag and does not synthesize every hidden client `MobAttackInfo::bHitAttach` case one-for-one, the one-time-action / prepare-layer path is still modeled through the summon actor state machine rather than a literal client action-layer implementation, and the summon inbox is still a simulator loopback seam rather than full real `CUserPool` transport/ownership decode one-for-one | This closes the packet-owned summon seam beyond packet-body decode alone: remote summons now keep the client-backed `Init` tail data, attack packet body, packed facing, target tuples, hit-overlay/sound playback, signed hit-period feedback, local-owner puppet/Tesla refresh hooks, client-shaped summon-attached-vs-random hit overlay placement, a user-owned summon list/map seam that matches the observed `CUser::AddSummonedList` ownership model more closely, a reusable field-packet inbox surface for feeding the pool, a client-shaped nested mob-hit metadata load path, a packet-owned one-time-action hit window that keeps positive-damage summons on the hit lane long enough to mirror `CSummoned::PrepareActionLayer` more faithfully, and remote-owner teardown that no longer leaves packet summons stranded after the backing `CUserPool` actor is gone. Packet-authored summon presence, action playback, movement sampling, hit feedback, owner registration, and packet injection can now live outside local summon casts without depending only on the pool's internal owner dictionary. The remaining parity work is now concentrated in the last hidden `bHitAttach` cases, the literal client action-layer implementation details, and end-to-end official `CUserPool` transport rather than missing owner teardown, nested hit metadata load, or any packet inbox at all | summon field/pool runtime (`CSummonedPool::OnPacket`, `CSummonedPool::OnCreated`, `CSummonedPool::OnRemoved`, `CSummonedPool::OnMove`, `CSummonedPool::OnAttack`, `CSummonedPool::OnHit`, `CUser::AddSummonedList`) |
| Partial | Summoned actor update/runtime parity | The simulator now gives active summons a dedicated actor-update pass inside `SkillManager` instead of treating them as attack timers only: each summon snapshots previous versus current world position before and after the movement step, keeps active follow movement separate from passive stationary/anchor-bound updates, routes visible state through summon-owned spawn/prepare/attack/hit/die actor states, drives Tesla Coil idle ownership through the WZ `summon/prepare` branch instead of collapsing the armed state into generic stand timing, lets self-destruct summons fire their final attack/support/action branch once their authored duration expires before entering removal playback, and uses previous-to-current summon bounds to resolve body-contact hits against mobs already targeting taunt summons before marking those summons damaged or removed. This run extends that summon-owned action seam with more of the client-confirmed assist choreography instead of collapsing every assist path back onto generic attack timing: `SkillLoader` now preserves additional summon-local WZ branches such as `heal`, `support`, `subsummon`, and Beholder `skill1`..`skill6`, the runtime chooses those named summon branches during actor playback, Beholder (`1321007`) now follows its own client-shaped support split with separate heal vs buff timers, heal support triggers the authored `skill1` branch using `1320008`, buff support rotates one authored stat lane at a time through `1320009` and the corresponding `skill2`..`skill6` branches, and summon-assist families such as `minionAbility="summon"` now drive the summon-owned `subsummon` branch instead of pretending they are normal attacks. The existing summon hit/removal work remains in place: summon-owned hit branches load from WZ `summon/hit` with fallback to the legacy skill `hit` surface, both local and packet-owned summons stay on that hit state before returning to idle or removal playback, authored summon HP now prevents puppet/body-contact damage from collapsing every summon into one-touch removal, packet-owned summon hit packets feed the same hit/removal seam, both draw paths tint-hit summons through the shared hit-period window, Tesla state `1/2` stays on the `0x2A`-backed prepare/armed idle surface, passive stationary or anchor-bound summons now emit the summon-owned passive settle effect pulse, and the draw/runtime paths consume loaded prepare/hit/removal animations plus actor-owned previous/current secure-style position history. The remaining gap is the client's exact summon damage bookkeeping and signed hit-period values beyond the simulator's current authored-HP-plus-hit-animation approximation, the rest of `CVecCtrl::UpdatePassive` beyond the confirmed local effect-trigger hook, Tesla states or refresh rules beyond the confirmed `1/2 -> 0x2A` armed-idle handling, and the last per-skill assist choreography that still lives in client-owned special cases beyond the newly covered Beholder and `subsummon` seams | This closes more of the real `CSummoned::Update` layer rather than only extending summon attack metadata: summon-heavy jobs now keep actor-owned movement, Tesla prepare-state presentation, hit playback, hit-period flashing, passive settle-effect feedback, post-expiry self-destruct timing, summon-contact teardown, previous/current position sync, summon-owned named assist animation playback, and the Beholder heal/buff split inside one per-frame seam. The remaining mismatches are narrower and are now mostly the client's hidden HP or signed hit-period bookkeeping, broader passive-update side effects, deeper Tesla state transitions beyond the confirmed armed-idle states, and the last summon-specific assist special cases rather than a missing actor runtime altogether | summon actor/runtime layer (`CSummoned::Update`, `CVecCtrl::UpdateActive`, `CVecCtrl::UpdatePassive`, `CSummoned::SetMoveAction`, `CSummoned::ProcessAction`, `CSummoned::ProcessAttack`) |
| Partial | Weapon afterimage / melee trail parity | The simulator now parses skill-authored `afterimage` branches plus the shared `Character/Afterimage/<family>/<masteryIndex>` cache surface, resolves local melee-skill afterimage sets by equipped weapon family and mastery, draws the authored per-frame canvases in sync with the player's active attack action, and reuses the matching `lt/rb` arc rectangle as the melee hitbox fallback when a skill does not publish its own explicit range box. Weapon family selection now follows the equipped weapon's own WZ `Character/Weapon/<item>/info/afterImage` value instead of collapsing swords to hardcoded `swordOL` or `swordTL` buckets, so one-hand and two-hand sword `OL/OS` and `TL/TS` splits resolve against the same per-item family surface the client consumes, with narrow sibling-family fallback when a skill or cache branch omits one side of the sword pair. Once a weapon publishes that explicit `info/afterImage` family, the simulator now keeps the same client-shaped explicit-family surface plus the narrow sword sibling fallback only and no longer broadens the lookup back out through generic weapon-code buckets. Basic local melee attacks now also register that same weapon afterimage path through the existing local attack seam instead of limiting afterimage playback to skill-authored casts, and the local player afterimage runtime now preserves its facing snapshot, sampled frame index, and short fade tail after the attack state clears instead of storing fade metadata that never renders. This run also mirrors the client-confirmed charged-element rewrite: when an active charge buff is present, local melee trail selection now overrides both skill-authored and mastery-index afterimages with the shared `Character/Afterimage/<family>/charge/<element>` surface before playback or fallback-range resolution, using the `RegisterAfterimage` / `get_element_by_charged_skillid`-confirmed Page, Paladin, Thunder Breaker, and Aran charge mappings backed by the WZ `charge/<element>` branches. Afterimage action lookup is now alias-aware instead of requiring one exact authored action string: the shared `SkillLoader` seam first walks the existing body-action fallback family, then applies WZ-backed afterimage-family aliases for the client-only or cache-only branches still visible under `Character/Afterimage/*` such as one-hand sword `swingD1` / `swingD2` / `stabD1`, polearm `swingT2PoleArm` / `swingP1PoleArm` / `swingP2PoleArm` / `doubleSwing` / `tripleSwing`, and cannon `swingC*` / `shotC1`, while generic simulator actions like `attack1` / `attack2` can now fall through onto the matching stab or swing family when the catalog exposes only the client-authored concrete branch. Remote-user actors now also own that same WZ-backed afterimage runtime inside `RemoteUserActorPool`: when a remote actor is spawned, moved into an attack action, or has its action explicitly changed, the pool resolves the actor's weapon family through the shared `SkillLoader` seam, registers the matching generic or skill-authored afterimage set, draws the authored per-frame canvases ahead of the remote avatar with a facing snapshot, and keeps a short fade tail after the action advances away from the swing instead of dropping the trail immediately. Packet-owned remote melee registration now reaches that runtime as well: the simulator has a dedicated remote-user melee packet path carrying explicit remote `skillId`, mastery percent, facing snapshot, charged-skill id, and now either an authored action name or a compact action code byte, decodes it through `RemoteUserPacketCodec`, resolves known action codes through a client-backed raw-action table instead of the old enum-ordinal assumption, maps charged skill ids through the same `get_element_by_charged_skillid`-backed charge-element resolver used by the local player, and now also falls back to the skill's WZ-authored `action` when an official remote melee packet arrives with a client raw action code that the shared table still does not name. The shared action-code seam now mirrors the `CAvatar::MoveAction2RawAction`-confirmed raw mappings for `walk1` / `walk2`, `stand1` / `stand2`, `alert`, `jump`, `sit`, `prone`, `proneStab`, `ladder`, and `rope` instead of treating the local enum order as if it were the client raw action table, which keeps decoded remote action changes and any melee packet that reuses those raw codes aligned with the client-shaped values. The remaining gap is now narrower and more explicit: the simulator still does not map the full client raw melee action table that feeds `CUserRemote::OnMeleeAttack` before `CUser::GetAfterimageUOL`, including deeper attack-family codes and the `CActionMan::GetMeleeAttackRange` numeric specials such as `57 -> 41` and the hardcoded `74` rectangle, it still approximates the real `CreateAfterimageLayer` / `RegisterAfterimage` GR2 choreography with forward-frame plus fade-tail playback, and the broader remote field packet surface still needs a fuller one-for-one `CUserRemote::OnMeleeAttack` transport decode beyond the simulator's current compact packet seam | Many melee jobs rely on afterimage trails and authored swing ranges to sell timing and hit shape, and the simulator now consumes the same WZ-backed trail and arc data for local melee skill playback, local basic melee attacks, and simulator-owned remote actors while respecting the weapon item's authored afterimage family instead of forcing every sword through the long-family arcs. Charged melee buffs now also swap those local trails and fallback arcs onto the same shared `charge/<element>` surfaces the client registers instead of leaving charged swings on the uncharged family path, local afterimages now actually stay on-screen through the short post-swing fade window already modeled by the runtime, and remote actor swings no longer render as naked avatar-only pose flips once a simulator caller supplies an authored attack action. This pass also closes one real client-code gap in the remote packet seam: the simulator no longer assumes the enum ordinal is the same thing as the client raw action code, and remote skill melee packets with still-unmapped raw action bytes can now fall back onto the WZ-authored skill action instead of dropping the afterimage registration immediately. The remaining visible gap is concentrated in the still-unmapped attack-family raw action codes that the client uses before `GetAfterimageUOL`, the numeric attack-range specials still owned by `CActionMan::GetMeleeAttackRange`, the real remote melee packet body, and exact GR2 layer choreography rather than missing sword-family selection, charged-element rewrites, explicit-family fallback drift, local normal-attack registration, or the afterimage alias families visible in the WZ cache itself | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `CharacterLoader.cs`, `CharacterAssembler.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `CharacterData.cs`, `MapSimulator.RemoteUsers.cs` (`CAvatar::MoveAction2RawAction`, `CActionMan::GetWeaponAfterImage`, `CActionMan::CreateAfterimageLayer`, `CActionMan::GetMeleeAttackRange`, `CUser::GetAfterimageUOL`, `CUser::RegisterAfterimage`, `CUserRemote::OnMeleeAttack`, `CUserPreview::DoMeleeAttack`) |
| Partial | Skill cooldown UI parity | QuickSlot overlays now show live remaining-seconds countdown text per assigned cooling-down skill, quick-slot hover tooltips now keep the same live ready-vs-remaining cooldown wording even after the skill becomes ready instead of falling back to the static WZ cooldown value, the legacy pre-Big Bang skill window now mirrors the Big Bang window's cooldown dimming, countdown text, and ready-vs-remaining hover text without reverting to the authored WZ cooldown seconds once the skill is ready again, the skill window once again routes icon drag-and-drop into the instantiated quick-slot bar, Big Bang skill-book entries now dim and show live remaining-seconds text while their skill is cooling down with the hover tooltip surfacing ready vs remaining cooldown state, the status bar now renders a compact cooldown tray for hotkeyed skills that are actively cooling down and orders those tray entries by active cooldown start time with a stable hotkey-slot tie-break instead of the simulator's former raw slot-index ordering, non-hotkey cooldowns now also surface in a second compact status-bar tray using the same live icon-mask countdown treatment, the status-bar cooldown tray tooltip now uses the same ready-vs-remaining wording as the other cooldown surfaces instead of falling back to `Cooldown: --`, and off-bar cooldown start/blocked/ready notifications now also instantiate a dedicated WZ-backed floating notice stack using `UI/Basic.img/Notice3` while still driving the existing HUD/chat seams plus the WZ-backed `Sound/UI.img/DlgNotice` notice cue. That notice stack now sizes itself from the actual `Notice3` frame dimensions (`t=266x21`, `c=266x20`, `s=266x55`), preserves the WZ frame's full `266x96` minimum body instead of the simulator's shorter hardcoded panel, wraps longer body text through the `c` stretch seam, stacks multiple cooldown notices by their real rendered heights instead of a fixed panel pitch, promotes newly added or refreshed notices back to the top of the stack, refreshes an already-visible skill notice in place by skill id instead of letting `started` / `blocked` / `ready` variants for the same skill occupy multiple simultaneous rows, drops the simulator-authored divider accent that was not backed by the `Notice3` art, anchors the stack from the client `800x578` HUD canvas height instead of a fixed literal so the top-center placement scales with viewport height, and now nudges that anchor onto the same 44px client-height seam already used by the packet-owned top-center HUD notices instead of the simulator's older 42px guess while continuing to derive the vertical title/body/icon/message lanes and inner horizontal content bounds from the actual `Notice3` frame rows instead of relying on older fixed text baselines plus center-row-only inset guesses. Packet-owned cooldown clears now also tear down the shared cooldown state authoritatively instead of falling back to the authored WZ cooldown timer after the server override is removed, and that same clear path emits the existing ready completion seam so the quick-slot, skill-book, tray, and notice surfaces stop together instead of drifting. Off-bar `started` / `blocked` / `ready` routing still reuses the same direct-hotkey classification that drives the tray split so hotkeyed skills no longer leak duplicate blocked or ready rows into the dedicated off-bar stack. The remaining gap is the client's exact cooldown-notice asset choice versus any other shared notice family, any last `Notice3` micro-offset differences beyond the shared 44px top-center anchor, the exact lifetime or fade timings beyond the current best-effort push ordering, the packet-owned special sentinel branch and any other downstream side effects the client applies beyond the shared cooldown-ready seam, and any additional bespoke cooldown-only HUD widgets beyond the shared seams, the `Notice3` stack, and the off-bar tray | Cooling-down skills are now readable in both skill-book variants, the quick-slot bar, two HUD trays, and a dedicated top-center floating notice stack for off-bar cooldown start/blocked/ready events, the last tooltip regressions where ready skills still echoed authored WZ cooldown seconds are gone from the quick-slot and classic skill-book paths, the hotkey tray still follows runtime cast-recency instead of a static slot walk, off-bar cooldowns no longer fail silently because they now surface as notices, tray entries, and ready chat/HUD feedback until completion, the status-bar tray tooltip no longer falls back to a placeholder when a cooldown reaches ready state, packet-owned cooldown clears no longer leave the UI drifting on a stale authored WZ fallback timer after the server override ends, and the floating notice stack now follows the observed `UI/Basic.img/Notice3` surface dimensions plus its detected vertical and horizontal content bands more closely while sharing the same 44px top-center HUD anchor seam already used by the packet-owned notice layer instead of the simulator's older slightly higher placement. Off-bar cooldown notification routing now stays aligned with the tray split, so skills already represented on the direct hotkey bar no longer also emit duplicate blocked or ready rows through the dedicated off-bar notice stack. The simulator still does not reproduce the client's exact cooldown-notification art selection, final micro-offset polish, timing choreography, or packet-owned sentinel-side cooldown handling one-for-one | `QuickSlotUI.cs`, `SkillUI.cs`, `SkillUIBigBang.cs`, `UIWindowManager.cs`, `StatusBarUI.cs`, `MapSimulator.cs`, `SkillManager.cs`, `SkillCooldownNoticeUI.cs` |
| Partial | Client-timer skill expiry and cancellation | `SkillManager` now owns a centralized client-timer registry that drives buff expiry, timed prepared-skill release, mechanic repeat-sustain expiry, cyclone teardown, Wild Hunter swallow digest completion, WZ-timed local skill-zone expiry, and WZ-timed local summon expiry through one `UpdateClientTimer`-style update seam; the runtime now also publishes ordered timer-expiry batch snapshots that mirror the client's packed expired-timer list more closely, normalizes client cancel requests through WZ-backed `info/type` + `affectedSkill` mappings for affected-skill families such as `32110000 -> 32101002`, `32120000 -> 32001003`, and `32120001 -> 32101003`, now also covering the wider v115 `affectedSkill` cancel surface that WZ publishes outside the old `type=50/51` subset, including observed `type=16`, `type=33`, and `type=1` cases such as `2111007 -> 2101002`, `32111010 -> 32001002`, `35111009 -> 35111001`, `35111010 -> 35111001`, and `35101009 -> 35001001`, and now parses multi-target `affectedSkill` strings such as `21120011 -> 21100002&&21110003` instead of collapsing those authored families back to `0`. It also folds WZ-authored `info/dummyOf` families such as `33101005 -> 33101006&&33101007`, `32111005 -> 32111007&&32111008&&32111009`, and `35100008 -> 35101009&&35101010` back onto their parent cancel id, and routes manual status-bar buff cancellation through that same cancel-request seam with simulator time instead of bypassing it. That shared cancel path now tears down matching prepared-skill state, active buffs, cyclone sustain, repeat-sustain state, active summons, and local skill zones while preserving the existing swallow reset behavior; dummy-child cancel requests can now also tear down parent-owned prepared/key-down state through the same shared matcher instead of only clearing swallow-local bookkeeping, zone skills whose WZ publishes both `info/zoneType` and timed `common` / `level` duration data now expire through the same timer-owned registry instead of only through per-frame local polling, local summons whose WZ publishes timed `common/time` data such as Mechanic summon families now also register per-instance timer-owned expiry keys instead of aging out only through per-frame `Duration` polling, tank-siege cancel now reuses the local pending mode-end latch instead of clearing immediately, swallow-buff state teardown still rides the timer-owned buff expiry path, swallowed-mob loss during Wild Hunter digest still emits the client-style cancel-request hook instead of silently dropping state, and `SendSkillCancelRequest` special-case consumers are now honored even when no tracked local buff or prepared-state object matches the cancel id so vehicle-valid bookkeeping and keydown prepare-animation cleanup no longer get skipped behind the local-state teardown gate. This pass extends that same WZ-backed cancel normalization into the packet-owned summon runtime: `SummonedPool` now resolves affected-skill and `dummyOf` families through the shared cancel matcher before tearing down local-owner packet summons, so client cancel requests emitted by the local skill seam can remove packet-owned summons through the same parent/child family rules instead of leaving those summons outside the cancel surface, and the packet-owned summon pool now also publishes ordered per-instance summon-expiry batch snapshots before processing its WZ-timed removal queue rather than keeping the full expiry list private to the pool update loop. This run closes a smaller but real hole inside that same seam: a single client cancel request now fans out across the full WZ-authored cancel family instead of collapsing to only the first resolved id, so multi-target `affectedSkill` families such as `21120011 -> 21100002&&21110003` now drive local teardown, packet-owned summon teardown, and the existing keydown/vehicle special-case consumers across every authored cancel target rather than only the first branch. The simulator still does not mirror the client's full per-skill timer catalog, especially packet-owned summon families whose duration ownership is not recoverable from local WZ `common/time` / packet state alone, packet-owned expiry-side effects that depend on deeper summon combat/runtime ownership than the pool currently tracks, and broader client-owned cancel side effects beyond those currently modeled remain incomplete | Timed skill teardown now has a real shared runtime surface instead of being split across isolated expiry checks, manual buff cancellation no longer bypasses the local cancel/request path, repeat/cyclone or zone-backed skill state can now be cleared through the same client-style request seam as prepared skills and swallow, WZ `dummyOf` child requests now normalize back onto the parent cancel family before teardown so dummy-linked prepared state follows the same path as active buffs and swallow state, WZ-timed local zones now also publish real client-timer expiry events instead of disappearing only through local age checks, local summon families backed by WZ `common/time` such as `35111011` and `35121009` now expire and self-destruct through the same timer registry with per-instance timer keys instead of a separate duration poll, and affected-skill cancel normalization stays data-backed across more of the real WZ surface instead of a narrow simulator allowlist. Observed non-`50/51` `affectedSkill` families and multi-id `affectedSkill` strings now normalize through the same shared seam, and a single cancel request now fans out across every resolved family id instead of only the first one, so authored families such as `2111007`, `32111010`, `35101009`, `35111009`, `35111010`, and `21120011` can tear down matching local or packet-owned state without leaving secondary affected-skill branches behind. Standalone cancel requests can still tear down ordinary active summons while driving the currently modeled vehicle-valid and keydown cleanup consumers even when the simulator does not have a matching tracked buff or prepared node to remove first, and those special-case consumers now also evaluate the full normalized cancel family rather than only a single resolved target id. Local-owner packet summons no longer sit entirely outside that cancel surface: a normalized client cancel request can now tear down packet-owned local summon instances using the same WZ-backed `affectedSkill` and `dummyOf` family mapping already used by `SkillManager`, and packet-owned summon expiry is less opaque because the pool now emits ordered per-instance expiry batches before applying its removal path. The remaining parity gap is narrower and centered on the last client-owned timer families, especially packet-owned summon-duration ownership and expiry-side effects that depend on deeper runtime state than the simulator's current WZ + packet view, plus any additional `SendSkillCancelRequest` consumers that still live outside the simulator's current list | `SkillManager`, `SummonedPool`, `MapSimulator` (`CUserLocal::UpdateClientTimer`, `CUserLocal::SendSkillCancelRequest`, `CUserLocal::TryDoingSwallowMobWriggle`, `CSummonedPool::OnCreated`, `CSummoned::Update`, `Skill/422.img/4221006/info/zoneType`, `Skill/422.img/4221006/common/time`, `Skill/3211.img/32110000/info/affectedSkill`, `Skill/3212.img/32120000/info/affectedSkill`, `Skill/3212.img/32120001/info/affectedSkill`, `Skill/211.img/2111007/info/affectedSkill`, `Skill/3211.img/32111010/info/affectedSkill`, `Skill/3510.img/35101009/info/affectedSkill`, `Skill/3511.img/35111009/info/affectedSkill`, `Skill/3511.img/35111010/info/affectedSkill`, `Skill/2112.img/21120011/info/affectedSkill`, `Skill/3310.img/33101005/info/dummyOf`, `Skill/3211.img/32111005/info/dummyOf`, `Skill/3510.img/35100008/info/dummyOf`, `Skill/3511.img/35111002/common/time`, `Skill/3511.img/35111011/common/time`, `Skill/3512.img/35121009/common/time`) |
| Partial | Skill restrictions by map/state | The simulator now enforces parsed map `fieldLimit` bits for `Unable_To_Use_Skill`, `Move_Skill_Only`, `Unable_To_Use_Mystic_Door`, the Mechanic-only `Unable_To_Use_Rocket_Boost` branch, and the mount / Mechanic vehicle `Unable_To_Use_Taming_Mob` branch through the shared skill runtime, rejects active skill use while the local player is dead, in hit-stun, seated, or prone so skill-window and hotkey casts no longer bypass those transient local states, mirrors the client's swallow-specific ladder/rope precondition in the shared evaluator, tears down client-timer / upkeep-driven skill paths such as keydown holds, cyclone pulses, mechanic mine auto-deploy, and repeat-sustain mode skills when those same local-state restrictions become invalid after cast start, now also feeds mob-disease lockouts sourced from WZ-authored `MobSkill.img` durations through that same shared state seam so direct casts, prepared-hold upkeep, repeat-sustain upkeep, cyclone, mine teardown, and Rocket Booster sustain all see `Seal` (`120`), `Stun` (`123`), `Attract` (`128`), the freeze family (`131`), and the WZ-authored polymorph pair (`172`, `173`) instead of leaving those branches split between cast-only booleans and isolated upkeep checks, routes simulator direction-mode / scripted-interaction ownership through that same shared state gate so skill-window or hotkey casts are rejected while NPC/script overlays own local control and active prepared/upkeep skills tear down once scripted control begins, widens the shared mount/vehicle classification to WZ-authored `eventTamingMob` riders plus broader client-owned vehicle buffs surfaced through `info/type=13` so `Unable_To_Use_Taming_Mob`, ladder/rope rejection, and airborne vehicle-start gating also catch non-Mechanic ownership cases such as Battleship and later event ride skills instead of a stale hardcoded subset, and now also covers the client-confirmed ladder/rope veto for the Hermit/Shadower taunt branch (`4121003`, `4221003`) through the same shared evaluator instead of leaving those ids outside the common gate. Deeper `CField` availability checks and the remaining client-only forbidden-skill branches are still incomplete | Maps that explicitly suppress skills now block the same broad skill families in the simulator, Rocket Booster can be rejected on field entry and cast attempt and now also tears down if a shared external restriction begins after launch, seated/prone transient states no longer let active skills slip through the generic cast path, swallow casts now fail on ladder/rope like the client branch, ongoing upkeep skills stop once the player falls into a forbidden local or mob-disease state instead of continuing through it, the common mob-driven seal / stun / seduce, freeze, and polymorph lockouts now use the authored WZ timers and surface explicit rejection reasons through the same shared skill gate instead of only blocking fresh casts via side booleans or transform-only state, scripted direction-mode control now also blocks new active-skill requests while cancelling active prepared, repeat-sustain, cyclone, mine, and Rocket Booster upkeep through the same shared restriction seam instead of leaving those paths running under NPC or field-script ownership, the client taunt branch now also rejects ladder/rope casts through the shared evaluator instead of only the old swallow or vehicle subset, and the `Unable_To_Use_Taming_Mob` plus client-side ladder/airborne vehicle checks now reject WZ-authored event mount skills together with broader client-owned vehicle ownership buffs such as Battleship or Jaguar Rider rather than only the earlier Mechanic-focused subset. The remaining gap is now concentrated in the last `CField` gate branches and other per-skill client checks outside `IsSkillAvailable`, especially field-owned special cases whose ownership still is not surfaced through WZ skill metadata alone and any remaining client-only forbidden-state branches beyond the currently modeled mob-disease / scripted-control / taunt-ladder set | `MapSimulator`, `SkillManager`, `FieldSkillRestrictionEvaluator.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `PlayerSkillBlockingStatusMapper.cs`, `PlayerCharacter`, `PlayerCombat`, `PlayerManager`, `PlayerMobStatusController`, `MapInfo.fieldLimit`, `FieldLimitType`, `Skill/MobSkill.img`, `Skill/412.img/4121003/info`, `Skill/422.img/4221003/info` (`CUserLocal::Update`, `CUserLocal::IsSkillAvailable`, `CField::IsUnableToUseSkill`, `CField::IsMoveSkillOnly`, `CField::IsUnableToUseRocketBoost`, `CField::IsUnableToUseTamingMob`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::TryDoingSwallowAbsorb`) |

Buff icon plan:

1. Confirm asset sources and ordering:
   Use `UI/BuffIcon.img` for temporary-stat icon chrome and `Skill/*/icon` for per-skill icon sourcing where the client uses the skill's own icon, then validate row ordering and refresh cadence against `CUIStatusBar::Draw` and `CUserLocal::Update`.
2. Add a runtime-to-UI snapshot:
   Expose a small status-bar model from `SkillManager` that includes `skillId`, applied stat family, start time, duration, and refresh/replace semantics so the UI does not need to inspect buff internals directly.
3. Extend status-bar loading and drawing:
   Teach `UILoader` and `StatusBarUI` to load buff-slot art, render the active icon row in the client-aligned status-bar region, and reserve the interaction seam for later right-click cancel behavior.
4. Verify state synchronization:
   Check short-duration buffs, long-duration buffs, same-skill refresh, natural expiry, and `/job` cleanup so icon removal stays synchronized with the runtime and does not fight quick-slot cooldown overlays.


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. Summon actor/runtime pass:
   Close both the packet-owned `CSummonedPool` gaps and the `CSummoned::Update`-owned movement, touch-damage, Tesla-state, and position-sync gaps instead of only extending attack metadata and per-skill summon heuristics.
4. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
5. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
6. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".

