# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`
- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Player skill execution parity

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
`CUserLocal::Update` at `0x937330` orchestrates several of these paths and confirms that key-down gauge updates, queued follow-up attacks, prepared-skill execution, repeat-skill handling, teleport, rush, flying-rush, rocket-booster start/end handling, movement-coupled shoot branches, timer-driven cancellations, and status-bar feedback are coordinated in one local-player update loop rather than isolated one-off handlers. A second IDA pass also showed that summon parity splits across three owners rather than only the actor/runtime shell: `CSummoned::Update` drives summon actor motion, active/passive vec-ctrl updates, body-contact damage checks, assist-type dispatch, Tesla Coil state handling, self-destruct timing, and final world-position sync; `CSummonedPool::OnPacket` dispatches created/removed/move/attack/skill/hit packets and `CSummonedPool::OnCreated` ties each summon to its owning user, skill id, level data, and local-user registration path; and `CActionMan::{GetSummonedProp,LoadSummonedAction}` own the WZ-side summon UOL lookup, per-action frame build, delay/z metadata, reverse-playback duplication, and cached action-entry reuse before the runtime ever updates or draws that actor.

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
The earlier backlog text tracked summon packet ownership and runtime behavior, but the targeted `CActionMan` scan shows another concrete client owner that was still missing from the document: summon action loading itself. `GetSummonedProp` resolves the summon UOL from `SKILLENTRY::GetSummonedUOL` through the resource manager, while `LoadSummonedAction` uses that property to resolve the action-specific node, enumerate each frame canvas, read per-frame `delay`, `z`, `origin`, and `rb` metadata, compute the frame bounds, and cache the assembled action entry under the summon action key. The same decompile also shows a distinct reverse-playback seam: when the source action node publishes the reverse flag, the client appends a reversed copy of the built frame list instead of leaving playback order entirely to the runtime. That means summon parity is not only `CSummoned::Update` timing and `CSummonedPool::OnPacket` ownership; it also depends on the loader that chooses the summon asset path, builds the frame table, and decides whether the runtime receives a forward-only or mirrored playback list.

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::Update` at `0x8fb8d0`

Notes:
The targeted re-check surfaced a client-owned helper seam that the backlog was not tracking explicitly. IDA decompilation shows `CUser::PrepareActionLayer` calling `CUser::PrepareShadowPartnerActionLayer` and `CUser::PrepareMirrorActionLayer` immediately after the main avatar action layer is prepared, which means clone-style helper visuals are owned by the user action-layer path rather than by the generic buff tray alone. The same `CUser::Update` re-check also exposes mirror-timing state (`m_tStartMirror`) alongside the broader avatar-update pipeline, which is enough evidence to keep shadow-partner and mirror helper behavior as a dedicated parity bucket instead of leaving it implicit inside generic skill or temporary-stat rows.

### 4. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another client-owned skill family that was not represented anywhere in the backlog text. `OnTimeBombAttack` is not just a generic damage callback: the decompile shows it decoding a skill id plus attack/action data, resolving the local skill level, calling `TryDoingMeleeAttack`, then applying hit-period/invincibility and impact side effects before forcing the avatar hit reaction. `OnVengeanceSkillApply` is also more specific than a plain temporary-stat refresh because it gates on skill id `3120010` and then calls `DoActiveSkill_MeleeAttack` through the local skill runtime, while `OnExJablinApply` arms the `m_bNextShootExJablin` flag for the next ranged-shot branch rather than behaving like a generic buff bit. That is a dedicated packet-owned skill-application seam between the broad cast/update families and the existing temporary-stat or helper rows.

### 5. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`

Notes:
The same targeted `CUserLocal::OnPacket` scan surfaced another local skill owner that the backlog still was not naming. `OnIncComboResponse` is very small but concrete: it decodes the server-authored combo count, stores it in `m_nCombo`, stamps `m_tLastSetCombo`, and immediately routes through `DrawCombo`. The `DrawCombo` decompile then shows that combo is not just another temporary-stat label. The client resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them alive on a five-second alpha window, applies fly-origin motion to the combo vectors, and conditionally spawns extra `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` layers when the corresponding Aran combo-command skills are learned. A repo search across `HaCreator/MapSimulator` still only surfaced the generic `Combo` temporary-stat labeling in `SkillManager` and did not surface any simulator-side combo packet owner, combo HUD/effect loader, or `ComboCommand` runtime, so this remains a real missing parity bucket rather than stale wording.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Player skill execution parity

The simulator has a generic skill runtime. It does not yet mirror the client's named `CUserLocal` behavior families.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Generic skill casting | The simulator now loads the full player skill catalog from `Skill.wz`, learns active skills into the runtime, supports direct cast invocation from the skill window in addition to hotkey casting, keeps GM/SuperGM skill-book resolution compatible with both `900.img` and `910.img` layouts while focusing those jobs on their own job-book window instead of the global advancement catalog, evaluates Big Bang `common` formulas in IMG-mode data sets so skills like `Power Strike` are treated as castable attacks instead of passive placeholders, mirrors skill effect anchoring/hitbox fallback by facing direction for asymmetric melee effects, sanitizes unsupported SpriteFont glyphs in the skill window so WZ-backed names no longer crash row rendering, and resolves cast SFX from `Sound/Skill.img` so skill use now plays the corresponding `Use` or fallback attack sound through the simulator audio path | This closes the previous "can only use the current job's limited skill book" runtime/UI gap and keeps special-job skill tabs populated and executable across client data variants | `SkillManager.cs`, `SkillLoader.cs`, `SkillUIBigBang.cs`, `MapSimulator.cs`, `UIWindowLoader.cs` |
| Implemented | Buff stat application | Buff logic now applies `PAD`, `PDD`, `MAD`, `MDD`, `ACC`, `EVA`, `Speed`, and `Jump` directly to the player build and cleanly removes them when buffs expire or refresh | Active buff skills now change simulator stats and movement-relevant values instead of only a partial subset | `SkillManager.ApplyBuffStats` |
| Partial | Buff icon / temporary-stat UI parity | Active buffs still drive the WZ-backed status-bar tray with live duration countdowns sourced from `UI/BuffIcon.img`, the tray still supports client-style right-click cancellation directly from the live buff list, `UILoader` still normalizes and loads the full `BuffIcon.img` icon surface, and the simulator still consumes the authored `name` strings plus `buff/*` traversal order from that same catalog so the nine observed stat-family icons (`incEVA`, `incPAD`, `incPDD`, `incMAD`, `incMDD`, `incACC`, `incCraft`, `incJump`, `incSpeed`) keep their WZ-authored display names and ordering instead of a duplicated simulator-only table. The broader non-`BuffIcon.img` family catalog still resolves through the recovered v95 `SecondaryStat` layout, and this run closes another authored-owner gap inside that same seam without changing tray-lane ownership: `SkillManager.GetStatusBarBuffEntries` now keeps the latest matching authored family override instead of stopping at the first one, so later explicit percent/rate properties can keep their own owner text even when an earlier support or earlier non-icon family still owns tray ordering and icon selection. WZ was rechecked directly in the mounted export for the affected mixed families: `UI/BuffIcon.img/buff` still exposes the same nine ordered authored icon families, `Skill/3112.img/skill/31121001/common` still publishes `cr`, `ignoreMobpdpR`, then `bdR`, `Skill/572.img/skill/5720008/common` still publishes `criticaldamageMin`, `criticaldamageMax`, `cr`, and `bdR`, `Skill/3511.img/skill/35111013/common` still publishes `pddR`, `mhpR`, `mmpR`, `cr`, `damR`, and `expR`, `Skill/422.img/skill/4220009/common` still publishes `mesoR`, and `Skill/7000.img/skill/70000049/common` still publishes `dropR`. The authored-family override table now covers those later explicit owner properties too, including `mhpR` / `mmpR`, `pddR` / `mddR` / `accR` / `evaR`, `asrR` / `terR`, `damR`, `dropR`, `mesoR`, and `bdR`, so `Boss Damage`, `Elemental Resistance`, `Abnormal Status Resistance`, `Drop Rate`, `Meso Rate`, and the other explicit percent-family owner names no longer collapse back to an earlier active family label just because that earlier family still owns icon chrome or tray placement. Focused regression coverage is now on disk in `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` for later-authored-owner-vs-earlier-lane splits on `bdR` + `indiePad`, `criticaldamageMin` + `criticaldamageMax` + `indiePad`, and `terR` + `indieSpeed`. The remaining gap is narrower but still real: exact tray order is client-backed for the WZ-authored icon families plus the recovered v95 `SecondaryStat` slice, but some families still have no direct recovered v95 temp-stat owner or still appear to depend on runtime-owned grouping or icon state beyond `UI/BuffIcon.img` and the currently parsed skill metadata, especially exact icon ownership for standalone `Boss Damage`, mixed reward/offense cases whose final lane choice is still not explicit in the recovered v95 surface, and any later percent-style families whose final client placement or icon choice is still not observable from the current WZ plus recovered-client evidence alone | Buff-heavy jobs are visible on the HUD, can be cleared without waiting for expiry, and no longer collapse as often into a fully generic tray entry while tooltip text and family ownership stay closer to the WZ stat-family surface. The known nine `BuffIcon.img/buff/*` families still use the WZ-authored names and family order directly, Big Bang-era payload families such as `mhpR` / `mmpR`, `pddR` / `mddR` / `accR` / `evaR`, `damR` / `indieDamR`, `criticaldamageMin` / `criticaldamageMax`, `expR` / `dropR` / `mesoR`, `bdR`, `asrR` / `terR`, and both `ignoreMobpdpR` and `ignoreMobDamR` now stay on their resolved tray lanes without depending on tooltip text heuristics first. Named multi-stat support buffs also no longer drift to whichever family sorts first: the tray keeps the authored support-family owner for tooltip text and family display, and this pass narrows the remaining mixed-family gap again by letting the last explicit authored owner property win family text even when an earlier lane still owns the icon and sort position. The remaining gap is now concentrated on exact client lane or icon ownership for the families that still are not directly observable from `UI/BuffIcon.img` plus the recovered v95 `SecondaryStat` surface alone | `SkillManager.ActiveBuffs`, `SkillManager.ConfigureBuffIconCatalog`, `SkillManager.GetStatusBarBuffEntries`, `SkillLoader.CreateLevelData`, `SkillLoader.NormalizePassiveStatAliases`, `RemoteAffectedAreaSupportResolver.cs`, `StatusBarUI.cs`, `UILoader.cs`, `MapSimulator.Content.cs`, `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs`, `docs/GMS v95.h` (`UI/BuffIcon.img`, `UI/BuffIcon.img/united/buff`, `UI/BuffIcon.img/united/anniversary`, `Skill/*/common/indiePad`, `Skill/*/common/indieMad`, `Skill/*/common/indieAcc`, `Skill/*/common/indieEva`, `Skill/*/common/indieSpeed`, `Skill/*/common/indieJump`, `Skill/*/common/indieMhp`, `Skill/*/common/indieMmp`, `Skill/*/common/indieAllStat`, `Skill/*/common/pddR`, `Skill/*/common/mddR`, `Skill/*/common/accR`, `Skill/*/common/evaR`, `Skill/*/common/asrR`, `Skill/*/common/terR`, `Skill/*/common/criticaldamageMin`, `Skill/*/common/criticaldamageMax`, `Skill/*/common/damR`, `Skill/*/common/indieDamR`, `Skill/*/common/expR`, `Skill/*/common/dropR`, `Skill/*/common/mesoR`, `Skill/*/common/bdR`, `Skill/*/common/ignoreMobpdpR`, `Skill/*/common/ignoreMobDamR`, `Skill/131.img/1311008/common/str`, `Skill/312.img/3121007/common/dex`, `Skill/322.img/3221006/common/dex`, `Skill/*/info/switchDamtoMP`, `Skill/*/info/invincible`, `SecondaryStat`, `CUIStatusBar::Draw`, `CUserLocal::Update`) |
| Partial | Melee / ranged / magic resolution | The runtime now preserves WZ `lt/rb` attack bounds, resolves melee and magic targets with separate ordering rules, gives projectile skills family-specific collision ordering plus homing steering, keeps homing active even when WZ `ball` data also marks the projectile as exploding, detonates WZ `ball/explode` projectiles on impact so nearby mobs within `explodeRadius` are resolved under the same effective target cap, respects the effective projectile hit cap from both `level/mobCount` and `ball/mobCount`, routes WZ `info/type` through the skill-family classifier so no-ball spell and shoot families no longer collapse into melee resolution by default, now also treating client-confirmed shoot-family `info/type=52` skills such as Aran `21120006` as ranged even when WZ omits a `ball` node, routes WZ `info/type=32` plus `info/massSpell=1` spell families such as Mage `Slow` / `Seal` back onto the magic lane instead of leaving those no-ball area spells on melee defaults, and now also keeps the client-confirmed Outlaw no-ball shoot-family helpers `5211004` / `5211005` on the ranged lane despite their melee-looking WZ `info/type=1`, while the shared shoot-family resolver also carries `5211006` so the loader and runtime use one client-backed table instead of drifting apart. Direct no-projectile ranged skills still get their own fallback hitbox and target ordering instead of reusing the melee branch, route through the client-shaped `DoActiveSkill_ShootAttack` fallback split so melee-first families probe the close-range lane before falling back to ranged resolution while the client-confirmed direct-shoot families stay on the shoot lane, preserve the special client one-offs where Gunslinger `Double Fire` (`5001003`) and Outlaw `Triple Fire` (`5210000`) still attempt the melee lane before falling back to shoot despite otherwise living in the shoot-family helper table, and thread that same melee-first-vs-shoot preference through deferred moving-shoot preferred-target selection instead of always snapshotting a ranged-only target. Projectile families still route WZ `info/rectBasedOnTarget=1` through an additional target-anchored rectangle after first impact so client rect-attack families such as `3001004`, `3101005`, and `5201002` no longer depend on raw projectile overlap alone, re-resolve chained follow-up targets from the live impact/serial origin using the WZ `range` bounce radius plus nearest-neighbor chaining instead of only replaying the initial overlap snapshot, stage multi-shot projectile release from WZ `ballDelay` / `ballDelayN` timing instead of front-loading every projectile on cast start, assign WZ `info/multiTargeting=1` projectile volleys to distinct live targets inside the authored ranged hitbox before spawn so skills such as `2301005`, `3001004`, `5301000`, and `20021166` no longer fire every shot through the same generic spread-first path when valid separate targets already exist, derive projectile collision bounds from the live WZ ball frame size and origin while sweeping those bounds across the projectile's previous-to-current travel step so fast or large projectiles no longer rely on a single centered point-instant overlap check, thread per-target client damage override/decay branches through the live hit order so `Arrow Bomb: Bow` (`3101005`) uses its WZ `x` damage lane instead of the generic `damage` fallback, WZ-published x-based chain falloff still applies where the skill data actually publishes that decay value, and the remaining client-owned `SKILLENTRY::AdjustDamageDecRate` fixed tables now also drive `Energy Orb` (`5121002`), `Iron Arrow: Crossbow` (`3201005`), `Piercing` (`3221001`), `Ricochet` (`33101001`), and `Blade Fury` (`4341004`) through their exact client multipliers instead of flattening those families back onto one generic damage percent. This run also mirrors more of the client-owned shoot-entry precheck and ammo lane: ranged attack casts still validate the expected shooting-weapon family before firing, still skip that weapon gate for the confirmed client exceptions that do not use a shooting weapon at all (`5121002`, `11101004`, `15111006`, `15111007`, `21100004`, `21110004`, `21120006`, `33101007`, `4121003`, `4221003`), now parse WZ `level/common/bulletConsume` alongside `bulletCount` so client-ammo-heavy skills such as `Skill/411.img/4111005/common/bulletConsume = 3` and `Skill/412.img/4121006/common/bulletConsume = 200` no longer collapse to a generic single-ammo assumption, now also parse WZ `common/itemConsume` for the client-confirmed Outlaw elemental shot helpers `5211004` / `5211005` so the live inventory-ammo resolver can require the authored pellet family instead of accepting any generic bullet stack, and now also parse WZ `info/requireSkill` plus `req/*` support links so client shoot-entry branches such as Wind Breaker `13111006` / `13111007` stay gated behind their authored `13111005` support state instead of entering the generic ranged lane as standalone attacks. The support-state gate now also consumes the full authored required-skill set instead of only the first parsed support id, so future shoot-entry families that publish multiple `req/*` links stay aligned to the loader's complete WZ surface rather than a simulator-only first-entry shortcut. When an inventory runtime is present the simulator now resolves a concrete compatible USE-slot with enough stack quantity instead of only checking aggregate ammo presence, doubles the resolved per-cast ammo requirement while Shadow Partner is active to stay closer to the client-owned `GetProperBulletPosition` consumption lane, now mirrors the client-owned active-bullet-index branch by resolving the marked active projectile item as an exact item-id requirement instead of silently falling back to a different compatible family, still allows another stack of that same selected item id to satisfy the cast because the client scans by item id rather than a single slot index, and now also mirrors the CASH-cosmetic active-index branch by treating a marked active CASH projectile as an exact item-id requirement first, allowing another stack of that same CASH item id to satisfy the cosmetic lookup when the marked slot cannot be used, and only then falling back to generic family scan when no active CASH item is marked (`5020xxx` arrows, `5021xxx` throwing stars, `5022xxx` bullets), while carrying the client helper's `nWeaponItemID == 1472063 -> 2060xxx` arrow one-off into compatible-ammo matching instead of flattening everything to weapon-code-only rules. The live projectile runtime now also snapshots those resolved `pnItemID` / `pnCashItemID` ownership outputs per projectile at spawn time, so delayed `ballDelay` releases keep the exact USE ammo id plus the matched CASH cosmetic family that the cast resolved instead of dropping that client-owned selection as soon as the precheck returns. Focused regression coverage is now on disk in `UnitTest_MapSimulator/ClientShootAmmoResolverParityTests.cs` for exact active USE/CASH ammo-id selection, same-item stack fallback, and the `1472063` special arrow compatibility one-off, plus `UnitTest_MapSimulator/ShootAttackRequiredStateParityTests.cs` for distinct support-id resolution and all-required-support-state gating. | Skills are closer to the client across both classifier and execution seams: no-ball mass-spell debuffs no longer inherit melee ordering, the missing Outlaw shoot-family helpers no longer collapse back onto the melee lane just because WZ publishes `info/type=1` without `ball`, no-projectile ranged skills now use the client-shaped melee-first vs direct-shoot split instead of one unconditional ranged branch, deferred moving-shoot targeting no longer snapshots only the ranged lane when the live client family would try a close-range strike first, and direct shoot casts now reject the wrong weapon family or missing ammo in the same high-level place the client gates `DoActiveSkill_ShootAttack` before attack resolution while also consuming a real compatible ammo stack rather than a simulator-wide count. The elemental Outlaw helpers now go one step further: WZ-authored `itemConsume = 2331000 / 2331001` is carried into the level-data seam and used to filter the resolved USE slot by the same fire-pellet (`2331xxx`) vs ice-pellet (`2332xxx`) family split that the client applies inside `GetProperBulletPosition`, so those skills no longer accept an arbitrary gun-bullet stack just because the weapon family matches. The shoot-entry gate is also closer to the client for required support-state families: WZ-authored `info/requireSkill` plus `req/*` links now flow through the loader seam, Wind Breaker `13111006` / `13111007` no longer cast unless `13111005` is actually active, and the runtime no longer truncates multi-link support-state checks to the first parsed `req/*` entry before entering the ranged lane. The shoot-ammo resolver is also closer to the client-owned selection path itself: a marked active bullet now means "find this exact projectile item id or fail the ammo branch" instead of "prefer this slot and then fall back to anything compatible", another stack of that same selected item id can still satisfy the cast because the client scans the whole USE inventory by item id, compatible CASH projectile cosmetics are now surfaced through the same family scan instead of being entirely ignored, a marked active CASH projectile now follows the same exact-item-id-first behavior (with same-item stack fallback) before falling back to generic family scan, the special client arrow one-off for weapon `1472063` no longer collapses onto the generic weapon-code table, and delayed projectiles now retain the exact resolved USE/CASH ownership snapshot that `GetProperBulletPosition` produced for the cast instead of losing it once the runtime moves on to later skill requests. The remaining gap is narrower and is now centered on the last client-only `GetProperBulletPosition` outputs and precheck side branches beyond the newly modeled exact active-item, elemental-pellet, cash-projectile-family, special-weapon, full required-support-buff set, and per-projectile ownership snapshot one-offs, especially the still-undrawn cosmetic bullet-position presentation itself, any remaining active-index metadata beyond exact USE/CASH item id selection and same-item fallback, the last vehicle or one-time-action and ladder-family nuances that still live inside the client precheck, and edge-case timing or packet-owned branch state that still lives outside WZ-authored geometry | `SkillManager.ProcessMeleeAttack`, `ProcessMagicAttack`, `ProcessNoProjectileRangedAttack`, `SpawnProjectile`, `CheckProjectileCollisions`, `ResolveQueuedSerialTargets`, `ResolveDeferredPreferredTargetMobId`, `SkillLoader.ParseSkillInfo`, `SkillLoader.CreateLevelData`, `ClientShootAttackFamilyResolver`, `ClientShootAmmoResolver`, `UnitTest_MapSimulator/ClientShootAmmoResolverParityTests.cs`, `UnitTest_MapSimulator/ShootAttackRequiredStateParityTests.cs` (`Skill/521.img/5211004/info/type`, `Skill/521.img/5211004/common/itemConsume`, `Skill/521.img/5211005/info/type`, `Skill/521.img/5211005/common/itemConsume`, `Skill/521.img/5211006/info/type`, `Skill/411.img/4111005/common/bulletConsume`, `Skill/412.img/4121006/common/bulletConsume`, `Skill/1311.img/skill/13111006/info/requireSkill`, `Skill/1311.img/skill/13111006/req/13111005`, `Skill/1311.img/skill/13111007/info/requireSkill`, `Skill/1311.img/skill/13111007/req/13111005`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::DoActiveSkill_MagicAttack`, `CUserLocal::GetProperBulletPosition`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `is_shoot_skill_not_switched_to_melee_attack@0x6edcf0`, `is_shoot_skill_not_using_shooting_weapon@0x6edbf0`, `is_shoot_skill_not_consuming_bullet@0x6eeaf0`, `is_correct_bullet_item@0x861360`, `is_correct_bullet_cashitem@0x6ebcf0`, `is_pellet_fire_item@0x861300`, `is_pellet_ice_item@0x861330`, `SKILLENTRY::AdjustDamageDecRate`) |
| Partial | `DoActiveSkill_*` family parity | Generic movement, summon, and prepare/charge families now execute through the existing `SkillManager` seams instead of falling back to one generic cast lane. Movement-family dispatch now separates teleport snaps, grounded rush, flying-rush glide, jump-rush launches, and bound-jump starts from the client-confirmed `TryDoingTeleport` / `TryDoingRush` / `TryDoingFlyingRush` / `DoActiveSkill_BoundJump` splits; Teleport Mastery riders authored as `info/affectedSkillEffect=bodyAttack&&stun` stay off the direct-cast surface and fire as passive follow-up data after the owning teleport resolves; WZ-linked helper branches with `info/affectedSkill` plus `info/affectedSkillEffect` no longer surface as standalone castable actives; Battle Mage aura recasts cancel or replace the active aura instead of stacking; aura-dot riders keyed by `info/affectedSkillEffect=dot` plus `info/dotType=aura` pulse once per second while the owning aura is active; WZ `info/mesoExplosion` skills now resolve pickup ownership against the live local character id before consuming nearby mesos inside the authored `lt/rb` box and then applying their attack payload; WZ `info/zoneType=invincible` skills create a persistent local protection zone; admin-skill execution now enforces GM/SuperGM ownership; bound-jump gating now respects `info/avaliableInJumpingState=1`, client-owned grounded exceptions such as `11101005`, `33001002`, and `35101004`, Wild Hunter swim/fly rejection, and WZ `info/requireHighestJump=1`; moving-shoot startup still resolves from the strongest loaded movement-action branch, deferred moving-shoot execution still mirrors the client-owned queued-level gate by dropping the delayed shot when the live learned level no longer matches the stored moving-shoot entry level instead of silently re-scaling the queued cast at fire time, and this pass keeps the queue-time action-owner chooser aligned with `get_random_shoot_attack_action(...)`: skills whose WZ `info/type` is `3` still bypass authored `action/*` appointment rows on the deferred moving-shoot surface and instead fall back to the shared client random-action lane, while non-`3` skills still keep the authored appointment/randomized branch selection. Queued moving-shoot `nActionSpeed` now mirrors more of the same client `get_attack_speed_degree(...)` seam too: WZ re-check still confirms representative booster families such as `Skill/110.img/skill/1101004/common/x = -2` and `Skill/510.img/skill/5101006/common/x = -2`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare` at `0x912f90` still stores `m_movingShootEntry.nActionSpeed` from that helper, and this pass also lifted the helper's client-owned clamp plus known skill exceptions so deferred moving-shoot snapshots now clamp to the client `[2,10]` degree range, still honor the `4001334` base-speed `-2` adjustment, suppress booster/frozen modifier carry for the client-owned exception set `4311003`, `4321001`, `4331000`, `4331005`, `4341001`, and `4341003`, and now mirror the confirmed `CAntiRepeat::TryRepeat` bypass for `33121009` instead of promoting every WZ `info/rapidAttack=1` moving-shoot skill onto one generic one-repeat lane. Rocket Booster still uses dedicated start/upkeep/landing seams that defer resource spend until launch, re-read the learned level at launch time, preserve the authored normal-vs-tank startup branch, keep the client `250 + 20 * floor(level / 4)` stored vertical launch formula, clear the stored launch marker on the first upkeep tick after startup, and end on the same post-attack clear cadence used by `TryDoingRocketBoosterEnd`. Packet-owned summons now mirror more of the `CSummoned::Update` passive lane too: taunt/puppet summons still resolve body-contact damage against mobs targeting that summon, WZ `info/selfDestructMinion=1` summons still schedule their authored final-attack window before removal, and this pass tightened passive `UpdatePassive` fallback so packet-owned summons without a move snapshot resolve their effective passive movement from the loaded WZ summon branch profile first and then fall back to the packet `moveAbility` when the skill profile is absent or still stationary, which keeps follow/hover summons moving and lets anchor-bound hover summons keep re-emitting their passive effect from motion instead of freezing when the packet stream drops to passive vec-ctrl updates. Focused regression coverage for the meso-ownership lane plus the moving-shoot chooser/action-speed/anti-repeat seams is now on disk in `UnitTest_MapSimulator/MesoExplosionParityTests.cs` and `UnitTest_MapSimulator/MovingShootParityTests.cs`; | The remaining gap is now narrower and concentrated in the last client-owned movement and packet choreography rather than the generic execution lanes. The simulator still simplifies the exact moving-shoot entry snapshot and request choreography inside `TryDoingSmoothingMovingShootAttack`, especially the still-unrecovered client `m_nAttackActionType` table, the full shared `get_random_shoot_attack_action(...)` selection table beyond the now-modeled `info/type == 3` appointed-action bypass, the live temporary-stat inputs that still feed `get_attack_speed_degree(...)` in the client beyond the now-modeled clamp, `4001334` base offset, skill-specific modifier-bypass set, and shared Booster delta, and the unresolved `CAntiRepeat` repeat-count initialization or any additional bypasses beyond the now-modeled position-threshold, level-match, and `33121009` exception gates; the final highest-jump timing window beyond the current WZ-backed near-apex gate; any remaining teleport timing / snap choreography around the client-owned delayed `m_teleport` commit window beyond the now-modeled follow-up mastery trigger; any remaining Rocket Booster packet choreography beyond the now-modeled launch-marker, launch-speed formula, and end-loop timing; deeper packet-owned summon assist dispatch and per-family `OnSkill` / `OnAttack` ownership beyond the now-modeled body-contact / passive-movement / expiry cadence; and the remaining field- or packet-owned special cases that are not recoverable from WZ plus the currently confirmed client seams alone. | `SkillManager`, `SkillLoader`, `PlayerCharacter`, `PlayerCombat`, `PlayerSkillStateRestrictionEvaluator`, `SummonedPool`, `PacketOwnedSummonUpdateRules`, `UnitTest_MapSimulator/MesoExplosionParityTests.cs`, `UnitTest_MapSimulator/MovingShootParityTests.cs` (`CUserLocal::DoActiveSkill_*`, `CUserLocal::DoActiveSkill_BoundJump`, `CUserLocal::TryDoingTeleport`, `CUserLocal::TryDoingRush`, `CUserLocal::TryDoingFlyingRush`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CSummoned::Update`, `CAntiRepeat::TryRepeat`, `get_random_shoot_attack_action`, `get_attack_speed_degree`, `Skill/*/info/type`, `Skill/*/info/affectedSkillEffect`, `Skill/*/info/avaliableInJumpingState`, `Skill/*/info/requireHighestJump`, `Skill/110.img/skill/1101004/common/x`, `Skill/510.img/skill/5101006/common/x`, `Skill/*/info/mesoExplosion`, `Skill/*/info/selfDestructMinion`) |
| Partial | Shadow Partner render/action parity | The simulator now treats Shadow Partner as a buff-owned companion render/action surface with WZ-loaded `special/*` action tables for `4111002`, `4211008`, and `14111000`, client-shaped action resolution order (`LoadShadowPartnerAction` helper-row lookup before plain raw-action name), raw-action-aware alias fallback, delayed attack follow-up keyed from companion first-frame delay, blocking `create*` and attack completion hold semantics, client-lane displacement/interpolation (`+/-30`, `+50`, `200 ms`), per-frame `a0/a1` alpha interpolation, and under-face insertion-layer ownership. This run tightens the packet-owned seam again without widening ownership beyond the existing resolver and draw paths: `ShadowPartnerClientActionResolver` now owns the shared client delay/blocking helpers in addition to the playback-frame remap and offset/back-lane helpers, `PlayerCharacter` still consumes that seam for local playback, and `RemoteUserActorPool` now reuses the same helper set for packet-owned action state instead of resolving a fresh helper frame straight from the actor every draw. Packet-owned Shadow Partner playback therefore keeps the earlier indexed helper remaps (`alert2` / `alert3` / `alert4`, `prone2`, `ladder2`, `rope2`) and the local/client `+/-30` side lane, `+50` back lane, and `200 ms` offset interpolation, but now also keeps its own action-start timestamps, create-action startup branch, create/attack completion hold semantics, and delayed attack follow-up keyed from the helper first-frame delay instead of snapping immediately to the remote actor's current base action. The remote draw path also now samples the helper animation from that stored start time instead of global time, and still inserts Shadow Partner together with remote under-face buff layers at the avatar under-face insertion point rather than before the whole assembled body. WZ-first comparison of the current mounted export still shows the same active non-ghost helper family this row can prove from data: besides the earlier alias-only rows (`avenger`, `assaulter`, `prone2`, `savage`, `showdown`, `assassination`, `smokeshell`, `ninjastorm`, `vampire`), the export also publishes exact helper-facing raw actions such as `alert2`, `alert3`, `alert4`, and `swingO1` at `Skill/411.img/skill/{4111001,4111003,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121004}/action/0`, `Skill/420.img/skill/{4201002,4201003}/action/0`, and `Skill/421.img/skill/{4211003,4211005,4211007}/action/0`, so the current WZ-confirmed non-ghost helper set is still covered by the existing exact-branch pick, indexed `alert*` remap, and helper alias rows. WZ-first comparison of the current exported Shadow Partner skills still narrows the `create*` question the same way: for `4111002` `special/create2` vs `create3` and for `4211008` `special/create2` vs `create4`, the mounted export is still byte-for-byte identical, and the `_s` / `_f` chooser variants remain equivalent in the current data set, so the remaining chooser gap is still not observable from the loaded WZ surface for this row. Remaining gap is narrower but still open: unrecovered client-only `LoadShadowPartnerAction` non-ghost helper rows not surfaced by the current WZ `action/0` set, additional frame-index rewrites beyond the currently evidenced `ladder2` / `rope2` / `prone2` / indexed `alert*` set, exact ordering against every other simulator-owned non-mirror under-face overlay that the client may still own as a literal action-layer canvas object, and any residual packet-only one-shot alpha or follow-up choreography that still depends on hidden client layer state rather than the helper animation catalog itself. | Shadow Partner now mirrors companion playback through a dedicated skill-owned action catalog with client-shaped helper ordering and raw-action fallback, including the earlier helper aliases plus the currently exported exact non-ghost helper rows (`alert2`, `alert3`, `alert4`, `swingO1`) from the active Shadow Partner job families. Helper-frame preservation still includes `prone2` in addition to `ladder2`/`rope2`/indexed `alert*`, the shared local+remote offset seam still uses the same client-shaped back-lane and interpolation helpers, and packet-owned playback is closer to the local/client layer because remote helpers now keep their own create startup, delayed attack follow-up, completion hold, and action-start timing instead of sampling the resolved helper animation statelessly off the remote actor every draw. Focused regression coverage is now on disk in `UnitTest_MapSimulator/ShadowPartnerClientActionResolverTests.cs` for remote create-action queueing and first-frame-delayed remote attack follow-up. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `ShadowPartnerClientActionResolver.cs`, `RemoteUserActorPool.cs`, `UnitTest_MapSimulator/ShadowPartnerClientActionResolverTests.cs`, `docs/GMS v95.h` (`CActionMan::LoadShadowPartnerAction@0x423cf0`, `CUser::PrepareActionLayer@0x8e3070`, `CUser::PrepareShadowPartnerActionLayer@0x8e1730`, `action_mapping_for_ghost@0x406500`), `Skill/*/{4111002,4211008,14111000}/special/*`, `Skill/411.img/skill/{4111001,4111003,4111005,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121003,4121004,4121008}/action/0`, `Skill/420.img/skill/{4201002,4201003,4201005}/action/0`, `Skill/421.img/skill/{4211002,4211003,4211005,4211006,4211007}/action/0`, `Skill/422.img/skill/{4221001,4221006}/action/0`, `Skill/1410.img/skill/14101006/action/0`, `Skill/1411.img/skill/14111002/action/0` |
| Partial | Queued follow-up attack families | Attack skills that publish WZ `finalAttack` follow-up maps still queue a deferred extra strike after the initiating melee or projectile hit resolves, gate that follow-up by weapon type on both queue and execute, reuse the learned follow-up skill's live proc rate, preserve queued facing plus preferred-target mob or position snapshots through direct follow-up resolution and any later deferred moving-shoot or projectile hop, and keep the client's single pending-slot overwrite rules for final attack, Spark, and serial follow-up families without clearing unrelated queued summon work. Thunder Breaker `Spark` (`15111006`) still runs as its own timed attack-triggered proc family from WZ `info/condition=attack` plus `info/chainAttack=1` and `info/chainattackPenalty=1`, queued Spark still re-enters the shared shoot or projectile path with the saved trigger origin and target lane, WZ `info/chainAttack=1` plus `info/chainattackPenalty=1` serial families such as `2221006` and `5121002` still use their own single pending serial slot, queued final-attack, Spark, and serial execution still re-read the live learned skill level when the deferred branch actually fires, still route queued cast startup back through the normal cast-effect seam, and still carry the client-only ranged final-attack `TryDoingShootAttack(..., 65, ...)` search hint through queued entry plus any extra deferred moving-shoot hop. The queued shoot-entry seam is now narrower again on the presentation side as well as the ammo-validation side: queued final-attack and Spark casts still re-qualify their armed USE or CASH ammo selection against the live inventory right before queued fire time, still refresh slot indices while preserving the original item ids, still cancel the queued ranged branch if the armed USE ammo disappeared, and now also consume those snapshotted USE or CASH item ids during queued projectile draw so delayed final-attack or Spark releases can render from the matching WZ `Item/*/*/bullet` frames instead of always collapsing back to the generic skill projectile visual after `GetProperBulletPosition` picked a specific bullet family. This pass tightens that same seam again inside the extra deferred moving-shoot payload hop: when a queued final-attack or queued Spark cast has already been re-armed into a later deferred fire, the simulator now re-checks the live learned skill level's actual ammo count plus skill-specific required ammo family before that delayed fire resolves, refreshes USE or CASH slot indices again while preserving the snapshotted item ids, and cancels the queued ranged branch if the originally armed USE ammo no longer satisfies the live fire-time requirement rather than letting the stale queue-entry snapshot survive to release. The earlier delayed queued projectile-origin refresh stays in place as well, so once a queued final-attack or queued Spark projectile is actually due the simulator still re-reads the live avatar `handMove` / `hand` / body-origin shoot point, reapplies that as projectile spawn plus owner origin, and re-aims from that late client-shaped origin instead of holding the older armed release point all the way through a delayed `ballDelay` seam. WZ-authored `x` chain-penalty decay and the client-owned fixed multiplier tables for no-`x` families still both apply through the deferred or timed chain path as well, so queued follow-up families no longer flatten back onto one generic damage percent after the opener. Focused helper coverage is now on disk in `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` for queued projectile visual-item preference plus queued fire-time ammo refresh behavior, while the remaining gap is now concentrated in the last client-only `TryDoingShootAttack` presentation details beyond the current queued-slot overwrite, delayed-projectile overwrite, queued ammo snapshot, late queued fire-time ammo revalidation, queued cast startup, delayed-fire shoot-origin refresh, fire-time queued slot-index refresh, and the new queued bullet-family visual override, especially exact cosmetic bullet-position placement, any remaining cash-bullet slot-specific visual behavior, or other residual `GetProperBulletPosition` metadata that the client still derives internally at queued fire time | Final-attack style procs still no longer collapse into the initiating cast timing, queued strikes still cancel if the player changes to a non-matching weapon before the deferred attack fires, if a newer follow-up proc replaces the pending one before it can execute, or if the follow-up skill has been locally unlearned before the queued branch actually fires, and replacing a pending final attack still does not wipe unrelated queued summon damage. Once a queued final attack or queued Spark has already progressed into a delayed projectile release, a newer proc in that same client-owned slot still clears the older still-pending projectile instead of letting it survive past the queue replacement point, while unrelated projectile work remains intact. Deferred moving-shoot payloads also stop dropping out when a due queued follow-up family should run first because the payload now stays armed until after that queued final-attack, Spark, or serial branch resolves instead of being dequeued and lost. Queued follow-up casts that pass through the simulator's extra deferred moving-shoot payload path still keep the stored facing or origin snapshot, preserve the ranged final-attack shoot-range override (`65`) through both deferred target acquisition and the actual delayed fire step, revalidate the live learned level again at the actual deferred fire time, keep the queued target-position snapshot alive across both direct execution and deferred projectile targeting so a vanished opener mob does not force those branches straight back onto generic live ordering, and now also revalidate the live fire-time ammo count plus skill-specific ammo family instead of only trusting the older queue-entry ammo snapshot. The queued ranged shoot-entry seam is now closer on both execution and presentation because queued final-attack and Spark casts recheck their armed USE or CASH ammo metadata at queued fire time, keep the original ammo item ownership instead of inheriting unrelated later casts, drop the queued ranged branch when the originally armed ammo family disappeared or no longer meets the live consume count, and now draw delayed queued projectiles from the matching snapshotted bullet-family frames rather than always falling back to the generic skill projectile art. The remaining mismatch is now mostly in deeper client-owned presentation bookkeeping such as exact cosmetic bullet-position placement, any remaining slot-specific cash-bullet visual rules beyond item-id ownership, and other residual queued shot metadata beyond the current USE or CASH projectile item snapshot, queued fire-time ammo-family and consume-count revalidation, delayed fire-time shoot origin refresh, and queued delayed fire-time slot-index refresh. | `SkillManager`, `SkillData`, `SkillLoader`, `ClientShootAmmoResolver`, `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` (`Skill/1511.img/skill/15111006/info/{condition,chainAttack,chainattackPenalty}`, `Skill/222.img/skill/2221006/info/{chainAttack,chainattackPenalty}`, `Skill/512.img/skill/5121002/info/{chainAttack,chainattackPenalty}`, `Item/Consume/0206.img/*/bullet`, `Item/Cash/0502.img/*/bullet`, `CUserLocal::GetProperBulletPosition`, `CUserLocal::TryDoingFinalAttack`, `CUserLocal::TryDoingSerialAttack`, `CUserLocal::TryDoingSparkAttack`, `CUserLocal::TryDoingShootAttack`) |
| Partial | Repeat-skill sustain families | `CUserLocal::TryDoingRepeatSkill` still owns more of the Mechanic repeat-state machine than the simulator, but this pass closes another SG-88 request-transport gap inside the existing repeat-sustain and summon official-session seams instead of inventing another local-only owner. WZ re-check still shows the same authored family surface that this runtime already leans on: `Skill/3512.img/skill/35121003` still owns the SG-88 summon / attack branch metadata (`info/type = 33`, `info/minionAttack = normal`, `summon/attack1`) and `Skill/3512.img/skill/35121013` still owns the tank-siege effect lane (`info/fixedState = 1`, `info/onlyNormalAttack = 1`, `effect/*/delay = 30/60/30/60`), so the new work stays on transport and confirmation plumbing rather than changing authored timings or repeat-state branch selection. Tank-siege mode-end acknowledgement remains request-time keyed end-to-end inside the shared timer seam: `SkillManager` still gates `HasPendingRepeatSkillModeEndRequest`, fallback-delay lookup, and `TryAcknowledgeRepeatSkillModeEndRequest` on the active pending request timestamp, `PlayerManager` still carries the stored pending request tick through the local fallback path, and the packet-owned resolve entry point still refuses to acknowledge anything except the currently pending `(skillId, returnSkillId, requestedAt)` lane. Client evidence still shows `CSummonedPool::OnPacket` dispatching opcode `0x119` (`281`) into `CSummonedPool::OnAttack`, then into `CUser::OnSummonedAttack`, while `CSummoned::TryDoingAttackManual` still owns the outbound request choreography that precedes those packets; the same direct IDA re-check still shows the manual SG-88 branch selecting targets through `CMobPool::CheckMobInTrapezoid`, anchoring clustered follow-up hits from `CMob::GetHitPoint`, and using a `120` ms same-request follow-up delay. The simulator now mirrors more of that transport ownership through the existing summon bridge instead of only the synthetic `1021` fallback: `SummonedOfficialSessionBridgeManager` still proxies inbound `0x116`-`0x11B` traffic into the summon inbox/dispatch path, still supports queued or immediate outbound raw client packet injection against the same live Maple session with `/summonedpacket session sendraw <hex>` and `/summonedpacket session queueraw <hex>`, and now also records the live client-to-server raw packet stream after Maple crypto initializes so the same bridge can preserve exact observed outbound opcode-framed payloads from the official session without an external sniffer. `/summonedpacket session history [count]` now reports the most recent captured outbound opcode/payload pairs, `/summonedpacket session clearhistory` clears that capture ring, and the bridge status now tracks forwarded live outbound count beside queued and injected packet bookkeeping. `SkillManager` still keeps packet-confirmed SG-88 target bookkeeping beside the pending request metadata, unions admitted target ids across authoritative local-owner summon attack packets, and resolves the request as soon as the observed `0x119` target set covers the full pending lane instead of waiting for the last local queued batch to land. Existing repeat-state behavior remains in place for fixed-state lock, `onlyNormalAttack` cast blocking, timer-driven siege/tank-siege timeout, tank-siege->tank handoff, tank/siege repeat-shot cadence (`420 ms`/`180 ms`), SG-88 branch timing (`810 ms` HUD, `2790 ms` arm, `2520 ms` teardown), and client-style manual-target admission/timing. Focused regression coverage for the outbound bridge seam is now on disk in `UnitTest_MapSimulator/SummonedPacketSessionBridgeOutboundTests.cs` for raw opcode-prefix validation, queued outbound bookkeeping, disconnected-session injection rejection, and the new observed-outbound history capture/reset path. The remaining gap is narrower but still real: tank-siege mode-end still lacks the true official client/server confirmation opcode and exact request/ack ordering behind `m_bSendTankSiegeModeEnd`, and SG-88 still does not know which captured outbound opcode/payload sequence is the semantic manual-attack request lane well enough to originate it automatically or bind capture to that request one-for-one without further client or packet evidence. | Mechanic repeat skills are now closer on fixed-state timing, in-state cast restrictions, repeat-owned normal attack cadence, SG-88 branch timing, staged tank-siege teardown, and confirmation ownership because both the tank-siege end lane and SG-88 manual lane now have explicit packet owners instead of only local timers or queued-batch completion. The runtime now goes one step further on SG-88 in both directions of the same live bridge: real local-owner summon attack packets can still reach the existing confirmation seam through `/summonedpacket packetclientraw` or the summon official-session bridge, captured official outbound request packets can still be replayed one-for-one through `/summonedpacket session sendraw|queueraw`, and this pass removes the need for an external sniffer just to preserve those official outbound bytes because the summon bridge now keeps a live decrypted outbound history of the Maple client stream itself. The remaining approximation is now concentrated in the last unknown request semantics rather than raw transport reachability: the simulator still does not know the true tank-siege ack opcode/order, and it still cannot yet label or synthesize the exact SG-88 manual-attack outbound request choreography automatically from the captured stream alone. | `SkillManager`, `PlayerCharacter`, `PlayerManager`, `SkillData.ActiveSummon`, `SummonedPool.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedSummonedParity.cs`, `Managers/PacketOwnedMechanicRepeatSkillRuntime.cs`, `Managers/LocalUtilityPacketInboxManager.cs`, `Managers/SummonedPacketInboxManager.cs`, `Managers/SummonedOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/SummonedPacketSessionBridgeOutboundTests.cs` (`CUserLocal::TryDoingRepeatSkill`, `CUserLocal::TryDoingSiege`, `CUserLocal::DrawKeyDownBar`, `CUserLocal::UpdateClientTimer`, `CSummonedPool::OnPacket`, `CSummonedPool::OnAttack`, `CUser::OnSummonedAttack`, `CSummoned::TryDoingAttackManual`, `CMobPool::CheckMobInTrapezoid`, `CMob::GetHitPoint`, `Skill/3512.img/skill/35121003/{info,summon}`, `Skill/3512.img/skill/35121013/{info,effect}`) |
| Partial | Bound-jump, rocket-booster end, and smoothing moving-shoot branches | `Rocket Booster` keeps the prior parity work from this row (ground-start gate, deferred launch-facing, startup/landing cadence, and exit-action teardown window), and this pass closes another client-owned `TryDoingSmoothingMovingShootAttack` seam in the existing deferred path. Deferred moving-shoot payloads still carry `m_movingShootEntry`-style `nAttackActionType` and `nActionSpeed` metadata, still feed that queued attack-action-type into delayed preferred-target hitbox resolution, and still finish more of the queued action-owner side that the client stores in `m_movingShootEntry.nAction`. The queue-time resolver still prefers the WZ-authored `action/*` candidate list already loaded into `SkillData.ActionNames` and expands those candidates through the existing client action-lookup map, and it still follows the `get_random_shoot_attack_action(...)` client seam more closely when the skill rides client skill type `3`: those deferred moving-shoot requests still snapshot one queue-time owner from the shared client shoot family (`shoot1` / `shoot2` / `shootF`) before the delayed execution path is armed instead of degenerating to unrelated later live avatar state when WZ did not publish a usable appointed action. This pass also tightens the same helper's failure branch. IDA re-check for `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare` confirmed that client skill type `3` does not fall back to the avatar's unrelated live action owner when `get_random_shoot_attack_action(...)` fails; it aborts before `m_movingShootEntry` is armed. The simulator now mirrors that conservative queue-time behavior in the same seam: type-`3` deferred moving-shoot requests no longer arm a delayed payload by silently carrying whatever live action name/raw action code happened to be present when the shared random-action owner cannot be resolved, and instead leave the queued owner unset so the deferred arm is cancelled. The same deferred execution seam still snapshots the queued action owner into the prior execution record and compares it during anti-repeat suppression, so delayed moving-shoot requests whose queued action owner differs still no longer collapse behind one prior-position check just because `nAttackActionType`, `nActionSpeed`, and world-position thresholds still match. The anti-repeat seam still keeps an explicit client-shaped repeat-count structure (`RepeatCount` + `CountLimit`) in the deferred execution state, and this run now lifts one more authored branch out of the zero-limit fallback instead of leaving every moving-shoot family on the same conservative suppression path. WZ re-check still shows `info/rapidAttack=1` on the moving or sustain families this row already touches (`Skill/3312.img/skill/33121009/info/rapidAttack`, `Skill/3500.img/skill/35001001/info/rapidAttack`, `Skill/3510.img/skill/35101009/info/rapidAttack`), so deferred moving-shoot anti-repeat now initializes `CountLimit = 1` for `rapidAttack` skills while keeping the existing explicit bypass for `33121009` and the prior queued-owner comparison seam intact. Focused source-side coverage for queue-time action selection, type-`3` unresolved-owner cancellation, normalized queued-action-owner comparison, and the new `rapidAttack` count-limit branch is now on disk in `UnitTest_MapSimulator/MovingShootParityTests.cs`. The row still has remaining work in the same bucket: confirmed client initialization/bypass rules for `CAntiRepeat` repeat-count limits beyond the now-modeled `\|dx\| < 6` and `\|dy\| < 150` thresholds, the explicit `33121009` bypass, and the new WZ-backed `rapidAttack => CountLimit 1` branch, the exact per-attack-action shared table contents and bypass rules behind `get_random_shoot_attack_action(...)` beyond the now-modeled type-`3` shared shoot-family fallback plus unresolved-owner abort, any additional server-confirmed Rocket Booster end-branch choreography that is still packet/client-owned outside current simulator ownership, and non-WZ bound-jump special cases that still require direct client evidence. | Movement-coupled attack skills now retain more of the client-owned moving-shoot entry payload through delayed execution instead of only preserving position or later live avatar state: delayed target-mode resolution still stays tied to queued attack-action metadata, deferred shots still keep a queue-time moving-shoot action owner resolved from the skill's authored action catalog when WZ appoints one, and type-`3` moving-shoot requests now both snapshot a client-shaped shared shoot-family owner (`shoot1` / `shoot2` / `shootF`) before execution and refuse to arm the deferred branch when that client-owned shared-table owner cannot be resolved instead of silently degrading to unrelated live avatar state. Anti-repeat suppression still distinguishes queued moving-shoot action owners instead of treating nearby deferred shots as the same request solely because broader attack-type, action-speed, and world-position metadata match, and the delayed repeat counter is now a little closer to the client because WZ-authored `rapidAttack` moving-shoot families get one immediate repeat before the fallback suppression path closes while the confirmed `33121009` bypass remains intact. Rocket Booster and bound-jump parity from the previous passes remain intact while this run narrows the remaining gap to the still-incomplete client repeat-count init/bypass table, the exact shared random-action table contents beyond the new type-`3` fallback and abort path, and the last packet/client-owned Rocket Booster or bound-jump special cases. | `SkillManager`, `SkillData`, `PlayerCharacter`, `PlayerSkillStateRestrictionEvaluator`, `UnitTest_MapSimulator/MovingShootParityTests.cs` (`Skill/3510.img/skill/35101004/{action,info}`, `Skill/3312.img/skill/33121009/{prepare,keydown,ball,info}`, `Skill/3500.img/skill/35001001/info/rapidAttack`, `Skill/3510.img/skill/35101009/info/rapidAttack`, `CUserLocal::DoActiveSkill_BoundJump`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare`, `get_random_shoot_attack_action`, `CAntiRepeat::TryRepeat`) |
| Partial | Key-down / charge skills | The prepared-skill seam now covers more of the client-owned key-down/charge lifecycle instead of leaving release-owned families on one generic keydown loop. Existing parity in this row remains in place for HUD suppression/skins, mechanic sustain timing, WZ `repeat` hold branches, negative-`z` prepare/hold/release overlays, scan-code-backed local input ownership, dragon-owned HUD anchoring, release-triggered execution for the Big Bang trio (`2121001`, `2221001`, `2321001`), Marksman Piercing (`3221001`), Evan dragon breath (`22121000`, `22151001`), Dual Blade charge pair (`4341002`, `4341003`), Monkey Wave (`5311002`), and Wild Hunter swallow follow-up ownership, plus remote armed-hold persistence and authored charge-window gauge timing. This pass closes the next client-confirmed release-family gap inside that same seam instead of forking Piercing back onto the generic hold loop: WZ still publishes `Skill/322.img/skill/3221001/{prepare,keydown,action/info}` with the `900 ms` client gauge, and IDA for `get_max_gauge_time` plus `CUserLocal::OnKeyDownSkillEnd` still shows `3221001` as a release-owned keydown branch. The shared prepared-skill rule table now treats Piercing as release-triggered, surfaces the same release-armed HUD text, and keeps its undercharged local release on the same best-effort partial-charge damage seam already used where the client passes charge time back through release execution. Focused parity coverage for the shared HUD/profile helpers is now on disk in `UnitTest_MapSimulator/PreparedSkillHudParityTests.cs`, including the supported release-family rule table and Piercing's prepare-to-hold phase split. | Remaining gap is now narrower and still centered on client-only ownership/choreography outside the WZ-confirmed seams already modeled here. The simulator still does not reproduce the client's full raw make/break scan-code stream ownership, the wider exclusive-request catalog beyond the prepared-skill request family now sharing the `300 ms` gate, or the remaining release/follow-up families whose exact packet-owned charge semantics or non-WZ scaling curves are not yet confirmed enough to replace their current best-effort behavior one-for-one, including any tighter non-linear release scaling the client may apply beyond the simulator's current shared partial-charge approximation. | `PreparedSkillHudRules.cs`, `SkillManager.cs`, `MapSimulator.RemoteUsers.cs`, `PlayerInput.cs`, `PlayerManager.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `UnitTest_MapSimulator/PreparedSkillHudParityTests.cs` (`Skill/212.img/2121001/info/chargingSkill`, `Skill/322.img/skill/3221001/{prepare,keydown,action,info/piercing,info/piercingBouns}`, `Skill/2212.img/skill/22121000/{prepare,action,common/time,info/chargingSkill}`, `Skill/2215.img/skill/22151001/{prepare,action,common/time,info/chargingSkill}`, `Skill/434.img/skill/{4341002,4341003}/info/chargingSkill`, `Skill/531.img/skill/5311002/{prepare,keydown,action,common/time,info/chargingSkill}`, `String/Skill.img/{3221001,22121000,22151001,5311002}`, `get_max_gauge_time`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::OnKeyDownSkillEnd`, `CUserLocal::DrawKeyDownBar`, `CDragon::DrawKeyDownBar`) |
| Partial | Swallow, mine, cyclone, and sit-down healing branches | The simulator now auto-deploys mine summons from learned `33101008` only while the player is in the Wild Hunter jaguar riding family, suppresses that local `TryDoingMine` loop in town maps, and now mirrors the client startup cadence more closely by seeding the first continuous-movement deploy from the client-observed `update_time - 1000` lead before returning to the steady `1500 ms` mounted-movement interval. Mine movement intent is now also sourced from the same held left/right input direction the client reads before falling back to resolved foothold velocity, so brief startup acceleration no longer stalls the first mounted mine check. This pass tightens the hidden mine-ownership fallback again against the same client `m_nRidingVehicleID` seam: when no mounted action is currently visible, the simulator now keeps mine eligibility only while Wild Hunter Jaguar Rider ownership is still active or inside the existing short client-owned-vehicle grace window, and that fallback now resolves its hidden jaguar vehicle id only from a live Jaguar Rider-owned mount or an actually equipped jaguar instead of synthesizing one from the broader jaguar candidate list or from a non-Jaguar owner that merely happens to carry a jaguar item id. The hidden fallback still measures the grace window from the same update-loop `currentTime` / client `update_time` seam instead of a separate wall-clock `Environment.TickCount` read. Cyclone still sustains as a timed `32121003` body-attack loop with `1000 ms` pulses only while the cyclone action remains active, sit-down healing still loads its support range from the summon branch metadata and honors WZ `minionAbility=heal` plus `condition=whenUserLieDown` only while the sitting player remains inside that heal box, `35111011` recovery still uses its WZ-authored HP-percent heal plus the skill text's `x=5` second re-heal lock, and the local plus packet-owned remote healing-robot seam now mirrors `CUserLocal::TryDoingSitdownHealing` more closely by polling those support summons every seated frame instead of waiting on the simulator's generic summon `attackAfter` fallback before the first heal attempt. Successful `35111011` heals now also arm the authored heal-branch support-suspend window in both the local support seam and the packet-owned remote summon skill-action seam, so those robots stop re-entering support while the current heal playback is still in flight, closer to the client's `CSummoned::TryDoingHealingRobot` `m_nSuspend` seam. This pass tightens that same support-suspend seam again against WZ `Skill/3511.img/skill/35111011/summon/heal`: local summon actor playback now measures named summon-branch duration instead of only the generic summon attack clip when deciding whether the robot is still in `Prepare` or `Attack`, and both the local plus packet-owned healing-robot runtimes now clear the authored support suspend as soon as that heal branch returns to idle instead of holding the suspend only on a stale generic-attack or fixed-timer approximation. The same heal/support seam now also consumes nearby packet-owned remote Healing Robots from broader remote-owner state instead of limiting sit-down healing to party-owned summons when another actor owns the robot. Swallow-family detection and ladder/rope gating still stay on loaded WZ metadata only (`info/swallow` plus `dummyOf`), and the swallow cancel/absorb-ack seam still accepts the visible Wild Hunter skill (`33101005`) plus its WZ-linked dummy follow-up ids (`33101006`, `33101007`). Wild Hunter swallow also still follows the client's `TryDoingSwallowMobWriggle` / `TryDoingSwallowBuff` timing more closely after absorb confirmation: the visible skill still uses the `900 ms` keydown gauge before release, early release still cancels without executing, release-triggered payloads still consume cost only once the charge completed, the local absorb-ack seam still gates capture success, a successful absorb still automatically arms the `3000 ms` digest timer plus `500 ms` wriggle cadence instead of waiting for a second local digest cast, failed or invalidated absorb outcomes now route back through the same client cancel seam used by the other swallow exits instead of silently dropping local state, and the hidden attack dummy branch (`33101007`) still consumes the captured mob before firing its follow-up ranged payload. Focused source-side coverage for the tightened hidden mine gate is now on disk in `UnitTest_MapSimulator/PacketOwnedLocalUtilityParityTests.cs`, including the hidden jaguar-owner, active-mount, and live/equipped-vehicle-id eligibility seams. The simulator still does not mirror the client's exact swallow packet ordering, the exact server-owned capture/digest request choreography, the remaining `TryDoingMine` riding-vehicle-id ownership nuances beyond this active-owner plus update-time grace-window gate and live/equipped jaguar vehicle-id fallback, the remaining packet/request-owned Healing Robot suspend choreography beyond the new authored-branch idle clear, or the remaining non-Wild-Hunter swallow-family nuances one-for-one | Several client-only active-skill branches now execute in play instead of falling through the generic cast path, Wild Hunter mine is closer to `CUserLocal::TryDoingMine` because it now respects the client town suppression, input-owned movement-start detection, and update-time-owned hidden-owner grace window in addition to the existing jaguar-riding gate and first-deploy cadence, and it no longer keeps the hidden no-action fallback alive from a synthesized candidate jaguar id, a non-Jaguar owner mount item, or a merely equipped jaguar once Jaguar Rider ownership has dropped. Sit-down healing follows the authored area, percent-heal, and re-heal cadence instead of restoring on every support pulse, now also considers nearby packet-owned remote healing robots through the same support-range and re-heal-lock seam the local summon runtime uses even when those robots are owned by non-party remote actors, no longer stalls the first seated heal attempt behind the simulator's generic support-summon interval gate when the client would already be frame-polling `35111011` support, and now holds successful healing-robot support inside a short authored-action suspend window instead of immediately allowing the same summon to re-enter the heal branch while that playback is still active, even when the remote summon entered that heal playback from a packet-owned `OnSkill` action first. The local summon runtime is closer again because named support branches such as Healing Robot `heal` now own their actual WZ playback window when resolving local summon actor state, and both local plus packet-owned healing robots now drop that suspend when the heal branch genuinely returns to idle instead of waiting on a looser generic-attack approximation. Cyclone no longer keeps pulsing once its sustained cyclone action has already fallen off the avatar, and Wild Hunter swallow is closer to the client's visible `33101005` input loop because charge threshold, early cancel, absorb success, absorb failure/target-loss teardown, and auto-started wriggle timing plus auto-digest completion now all ride the same release-owned swallow state and shared client cancel seam instead of requiring a second local digest trigger or leaving failed captures on a silent local clear. Swallow-family gating is also narrower and more data-backed now because false positives from skill names or action names no longer enter the swallow lane, and swallow cancel/ack callers can target the WZ-linked dummy follow-up ids directly instead of hardcoding the visible skill id. The remaining parity gap is now centered on exact swallow packet sequencing, the server-owned swallow request/ack ordering around `33101005` / `33101006` / `33101007`, the last riding-vehicle-id details in `TryDoingMine` beyond the simulator's current active-owner plus update-time grace-window gate and live/equipped hidden jaguar fallback, the remaining packet/request-owned Healing Robot suspend choreography that still lives outside the simulator's current authored-branch idle-clear seam, and any residual swallow-family behaviors the client owns outside the current local cancel/request seam | `SkillManager`, `SkillLoader.cs`, `SkillData.cs`, `PlayerCharacter`, `MapSimulator`, `SummonedPool.cs`, `UnitTest_MapSimulator/PacketOwnedLocalUtilityParityTests.cs` (`CUserLocal::TryDoingSwallowBuff`, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::TryDoingMine`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingSitdownHealing`, `CUserPool::DoHealNearHealingRobot`, `CSummoned::TryDoingHealingRobot`, `Skill/3300.img/skill/33001001/info/type`, `String/Skill.img/33001001`, `Skill/3310.img/skill/33101005/info/dummyOf`, `Skill/3511.img/skill/35111011/{info,common,summon/heal,summon/heal/info}`) |
| Partial | Summon simulation | Summon-family skills still keep the broader parity work already landed here: generic summon lifecycle/timers, WZ-backed attack branch and metadata loading, projectile and target-impact branch retention, Tesla triangle targeting, self-destruct prepare/die handling, packet-owned prepare/attack visual parity, taunt-only puppet registration, shared summon rule selection between local and packet-owned seams, and the current movement/gating buckets for stationary, grounded-follow, owner-hover, and anchor-bound summons. The earlier client-backed Tesla timing fix remains in place: IDA for `CSummoned::TryDoingTaslaCoilAttack` shows Tesla impact timing is driven by the summon attack-delay window itself, with a fixed `300 ms` floor plus per-target jitter inside the remaining window rather than by per-target projectile travel distance, and both the local `SkillManager` seam and the packet-owned `SummonedPool` seam still follow that rule. This run keeps the shared impact-presentation seam in the same loader/runtime ownership and closes another concrete anchor slice instead of forking summon-family logic. `SkillLoader` still keeps authored summon target-impact presentations as structured metadata (`animation`, `hitAfter`, `pos`), and the local plus packet-owned runtimes still reuse the same client-backed target-anchor resolver when those presentations are target-bound instead of source-bound. Client evidence from `CSummoned::OnAttack` still anchors summon hit points by clamping to the mob body, biasing toward the near body edge from the summon/source side, and capping extreme reach back toward a `600 px` window; the simulator continues to apply that same shared rule for local queued summon damage overlays, local summon projectile destinations and travel delay measurement, packet-owned summon projectile destinations, packet-owned delayed hit overlays, and packet-owned Phoenix/Frostprey tile-overlay anchoring. The earlier `pos=3` split remains in place, and this pass tightens the same resolver again for the hitbox-missing edge case: target-bound summon impacts now still apply the client-backed `600 px` reach cap even when the mob body rect is temporarily unavailable, and the packet-owned fallback target point now reuses the same raised mob-impact fallback height as the local summon seam instead of dropping back to raw movement coordinates. That keeps empty-body fallback handling aligned across local and packet-owned summon projectile destinations and delayed hit overlays while preserving the existing `pos=1` near-edge rule, `pos=2` source anchor, and `pos=3` centered target anchor for the currently observed summon families. The earlier WZ-backed Mechanic support fixes remain in place: hostile support summon classification still follows `String/Skill.img` instead of the older blanket `minionAbility=mes` shortcut, so Acceleration Bot EX-7 (`35111005`) no longer leaks into the friendly support lane; EX-7 still consumes its authored speed-up payload as a positive mob `Speed` status while lowering DEF; genuinely party-facing support summons still route through the dedicated friendly-support path; and AF-11 (`35121010`) still mints a persistent summon-owned damage-buff state from its authored `Damage Buff: +#x%` payload instead of falling through the hostile mob-status branch. Focused regression coverage for the shared resolver seam is now on disk in `UnitTest_MapSimulator/SummonImpactPresentationResolverParityTests.cs` for source-anchor, centered-target, capped empty-hitbox fallback, and client-shaped fallback-height rules. The row remains partial because the remaining parity gap is now narrower and mostly client-only: the simulator still does not reproduce the full local Tesla beam ownership/action packet payloads from `CSummoned::TryDoingTaslaCoilAttack` and the exact ownership effects around `CSummoned::OnSkill` / `OnAttack`, the exact semantic mapping for any authored summon `pos` codes beyond the now-modeled `pos=1` near-edge target anchor, `pos=2` source anchor, and `pos=3` centered target anchor still needs new client evidence, grenade-style expiry choreography beyond authored prepare/die playback is still approximate, the exact remote `OnSkill` / `OnAttack` / `OnHit` choreography for every summon family still goes beyond the current ordered impact handling, assist-target special cases not surfaced by WZ `minionAbility` / `condition` / packet assist bytes are still missing, the last one-for-one mob aggro retarget timing heuristics are still approximate, and party damage amplification still stops at the simulator's current summon-owned damage-buff seam. | Summon-heavy jobs are now closer not just on authored support polarity and Tesla timing, but also on how the authored summon impact seam behaves when target bodies are only partially observable: EX-7 and AF-11 still follow the correct hostile-vs-friendly authored support lane, Tesla impacts still use the client-shaped fixed-delay-plus-jitter window, local Tesla beam visuals still preserve the attack triangle in the same packet-shaped point form already used by remote summons, and target-bound summon impacts now stay on one shared capped fallback rule even when the mob body rect is empty. Local and packet-owned delayed impacts still consume the same indexed `hitAfter` / `pos` metadata that WZ publishes for summon hit branches such as Octopus, source-anchored entries still render from the summon-owned origin seam, `pos=1` target-anchored entries still share the client-backed body-clamped near-edge hit-point rule that also feeds projectile travel and packet-owned tile-overlay anchoring, `pos=3` entries still keep their distinct body-centered target anchor, and empty-hitbox fallback no longer bypasses the client-backed reach cap or diverge between local and packet-owned summon owners. The remaining visible mismatch is now concentrated in client-only payload/ownership details, deeper remote choreography, and any still-unconfirmed summon `pos` codes beyond the now-modeled `1/2/3` split rather than in shared target-anchor handling for the currently observed summon hit families. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `SummonedPool.cs`, `SummonImpactPresentationResolver.cs`, `UnitTest_MapSimulator/SummonImpactPresentationResolverParityTests.cs`, IDA `CSummoned::TryDoingTaslaCoilAttack` (`0x752780`), IDA `CSummoned::OnSkill` (`0x74a940`), IDA `CSummoned::OnAttack` (`0x753340`), IDA `CSkill_HitAni::CreateDefault` (`0x6ec010`), WZ evidence from `String/Skill.img/35111005`, `String/Skill.img/35111002`, `String/Skill.img/35121010`, `Skill/3511.img/skill/35111002/{common,info,summon,hit}`, `Skill/3511.img/skill/35111005/{common,info}`, `Skill/3512.img/skill/35121010/{common,info}`, `Skill/521.img/skill/5211002/hit/0/{hitAfter,pos}`, `Skill/521.img/skill/5211002/hit/1/pos`, `Skill/521.img/skill/5211005/hit/0/pos`, `Skill/311.img/skill/3111005/hit/{0,1}/pos`, `Skill/321.img/skill/3211003/hit/0/pos`, `Skill/321.img/skill/3211005/hit/{0,1}/pos`, and `Skill/521.img/skill/5211002/summon/attack1/info` |
| Partial | Remote summoned-pool packet parity | The simulator still keeps the packet-owned summon transport seam in `SummonedPool`, but the remote attack and hit side is closer to the client than before: it already mirrors the observed `0x116`-`0x11B` packet family, local versus remote owner registration, packet-owned move-path sampling, Tesla third-coil refresh, taunt-side ownership, packet hit damage bookkeeping, mob-authored hit-overlay lookup, and the loader/runtime handling for nested `attack/info/hit` metadata with UOL resolution plus the observed `attach` and `attachfacing` aliases, now including nested `attack/info/hit/<frame>` metadata flags. Earlier passes had already split positive-damage fallback away from the live summon branch by snapshotting the remaining authored branch window into a packet-owned one-time-action clip, keeping named summon branches on their authored timelines without a synthetic extra `prepare` prefix and letting non-Tesla summons already sitting in `Prepare` stay on authored `summon/prepare` during fallback instead of jumping straight to idle or attack. A later pass then closed a concrete `CSummoned::PrepareActionLayer` presentation gap inside that same seam by honoring authored per-frame alpha ramps on both the live summon branch and the detached one-time-action clip instead of forcing every summon frame to draw fully opaque; that change was grounded in both IDA and WZ evidence (`CSummoned::PrepareActionLayer` inserts each action-layer canvas with authored `a0` / `a1`, and WZ summon branches such as `Skill/1211.img/skill/12111004/summon/die/11` publish `delay=120`, `a0=255`, `a1=0`). Earlier on this row, the `4111007` reactive-damage chain also stopped depending only on summon-owned `ball` branches and now accepts the client-owned root `Skill/411.img/skill/4111007/ball` family that `CSummoned::OnAttack` resolves through `GetBallUOL`, with its chain source already moved onto the same move-action-owned `(x +/- 25, y - 25)` summon offset the client feeds into `RegisterChainlightningAnimation`. This run tightens the remaining packet-owned hit-overlay seam without widening transport ownership: packet-owned summon hit overlays now preserve authored attached hit offsets when mob attack metadata marks the hit as attached, and they now mirror that authored X offset when the same metadata publishes `attachfacing`, instead of collapsing every attached overlay back onto the raw summon origin once the overlay follows the summon. That change stays grounded in already loaded WZ metadata rather than simulator-only rules: the current mob loader still resolves `attach` / `bHitAttach` plus `attachfacing` aliases from `attack/info/hit`, info-level alias nodes, nested `info/hit/<frame>` metadata, and per-frame `attack/<frame>/hit` surfaces, while the mounted export still exposes concrete attached-hit examples such as `Mob/5220000.img/attack1/info/hit/attachfacing`, `Mob/3220000.img/attack1/13/hit/attach`, `Mob/5160000.img/attack1/info/attach`, `Mob/6090002.img/attack1/info/attach`, `Mob/9300525.img/attack1/info/attachfacing`, and `Mob/8150302.img/attack1/info/hit/1/attach`. WZ inspection still confirms the current exported summon skills publish the authored post-attack surfaces through `Skill/312.img/skill/3120010/tile`, `Skill/321.img/skill/3211005/tile`, `Skill/411.img/skill/4111007/ball`, and the authored summon attack bounds, while IDA for `CSummoned::OnAttack` still shows the client scheduling the bird tile overlay with a delayed fade window after impact and separately registering the `4111007` ball chain from that clamped summon-side offset to a mob-body target point for `270 ms`. `SummonedPool` therefore continues to reuse the existing packet-owned attack impact timing seam for both confirmed post-effect families: the tile overlay still lands over the first valid target mob with the authored summon attack rectangle plus a client-shaped `200 ms` start offset and `500 ms` `128 -> 255` alpha ramp, and the reactive-damage chain seam still schedules a client-shaped `270 ms` post-hit tether through the existing animation-effects owner without widening summon transport or field-effect ownership. Focused regression coverage for the rule seam is now on disk in `UnitTest_MapSimulator/PacketOwnedSummonUpdateRulesTests.cs`, covering the root-ball chain gate, non-reactive skill rejection, the client-shaped `4111007` chain source / endpoint math, attached-hit authored offset preservation, and attached-hit `attachfacing` mirroring. The remaining gap is narrower but still real: the simulator still does not synthesize every hidden client `MobAttackInfo::sHit` / `bHitAttach` case one-for-one beyond the currently observed WZ surfaces and the newly preserved attached-offset / facing-mirror path; the `CSummoned::OnAttack` post-effect seam is still limited to the currently confirmed Phoenix/Frostprey tile family plus the `4111007` reactive chain instead of every hidden post-effect case; the `4111007` chain still approximates the client `GetBallUOL` / `RegisterChainlightningAnimation` GR2 owner with the simulator's existing lightning effect manager rather than reproducing literal ball-UOL canvas ownership; it still approximates the broader client action-layer stack with detached draw overlays instead of reproducing literal GR2 action-layer canvas ownership and ordering; and `/summonedpacket` plus `SummonedPacketInboxManager` still expose a simulator loopback seam instead of full official `CUserPool` transport and ownership decode. | Packet-owned summons are no longer just spawn/remove stubs: they follow the observed client summon packet family, keep summon ownership ordered per user, route local-owner taunt and Tesla side effects through the same created seam, replay summon attack/hit/removal visuals, decode hit payloads far enough to show the mob-authored overlay and sound feedback from the attacking mob template, and tear down with remote-user lifetime instead of leaving orphaned field actors behind. Hit parity is closer again because the loader/runtime pair now covers nested `attack/info/hit` metadata with UOL resolution, lower-case alias support, explicit `attach` handling, client-shaped `attachfacing`-only `bHitAttach` cases, info-level `attach` / `attachfacing` aliases, nested `info/hit/<frame>` attach metadata fallback, explicit `info/hit` precedence over deeper frame-hit metadata, render-node fallback from missing or empty `info/hit` into authored per-frame `attack/<frame>/hit`, the cached WZ frame-origin anchor math that keeps packet-owned summon hit overlays visually aligned with the live summon, authored attached-hit offsets that now stay on their authored local seam while following the summon, summon-facing X mirroring for attached overlays when `attachfacing` is authored, the same local-runtime summon playback split for named attack branches plus non-Tesla `Prepare` state, authored remaining-window progression when positive-damage fallback has to stand in for the client hit branch, a detached packet-owned one-time-action playback clip that no longer mutates the live summon branch in place, authored per-frame alpha ramps while those summon clips are replaying, reactive-damage summon attack facing that follows the client-confirmed move-action seam instead of a generic packet-facing read, the confirmed Phoenix/Frostprey post-attack tile overlay family with WZ-authored area sizing plus the client-observed delayed fade window, and the client-confirmed `4111007` root-ball gate plus summon-side chain source offset. The remaining parity work is concentrated in the last client-only `sHit` / `bHitAttach` cases outside the currently observed WZ surfaces, broader hidden `CSummoned::OnAttack` post-effect cases beyond the now-confirmed tile and reactive-chain families, literal `GetBallUOL` / GR2 action-layer ownership instead of the current simulator-owned approximations, and end-to-end official `CUserPool` transport rather than owner teardown, hit metadata load, packet-facing correction, confirmed bird tile playback, confirmed attached-offset preservation, or confirmed `4111007` chain scheduling being missing outright. | summon field/pool runtime (`CSummonedPool::OnPacket`, `CSummonedPool::OnCreated`, `CSummonedPool::OnRemoved`, `CSummonedPool::OnMove`, `CSummonedPool::OnAttack`, `CSummoned::OnHit`, `CSummoned::OnAttack`, `CSummoned::PrepareActionLayer`, `CUser::AddSummonedList`, `CMobTemplate::LoadAttackInfo`, `Skill/3511.img/skill/35111002/summon/{prepare,attackTriangle,hit}`, `Skill/3511.img/skill/35111011/summon/{stand,heal}`, `Skill/1211.img/skill/12111004/summon/die/11/{delay,a0,a1}`, `Skill/411.img/skill/4111007/{info,summon,ball}`, `Skill/312.img/skill/3120010/{tile,summon}`, `Skill/321.img/skill/3211005/{tile,summon}`, `Mob/5220000.img/attack1/info/hit/attachfacing`, `Mob/3220000.img/attack2/info/hit/attachfacing`, `Mob/3220000.img/attack1/13/hit/attach`, `Mob/5160000.img/attack1/info/attach`, `Mob/6090002.img/attack1/info/attach`, `Mob/9300525.img/attack1/info/attachfacing`, `Mob/8150302.img/attack1/info/hit/1/attach`, `UnitTest_MapSimulator/SummonedPoolOneTimeActionParityTests.cs`, `UnitTest_MapSimulator/LifeLoaderAttackHitMetadataTests.cs`, `UnitTest_MapSimulator/PacketOwnedSummonUpdateRulesTests.cs`) |
| Partial | Summoned actor update/runtime parity | The simulator still keeps summon-owned actor update/runtime ownership (movement snapshots, spawn/prepare/attack/hit/die routing, Tesla armed-prepare idle fallback, self-destruct expiry handoff, packet-owned hit-overlay interrupt clearing, and named-branch timing that avoids forcing generic `summon/prepare` onto authored branches). This run closes another assist-branch seam on top of the already-landed packet-owned action-layer work. WZ re-check confirms the authored support branch surface is still present in the current extracted data set, including `Skill/132.img/skill/1321007/summon/skill{2,3,4,5,6}` for Beholder buff playback plus the previously confirmed `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}` and `hit` branches. The local support runtime now routes Beholder buff playback through the existing client-shaped weighted branch selector already owned by `SummonRuntimeRules` instead of always taking the first available buff branch, and that selector now reads the currently active synthetic Beholder buff state before choosing the next authored summon branch. This keeps local Beholder runtime ownership closer to the client-facing summon action layer: when one or more Beholder buff branches are already active, the summon can now advance onto the missing or still-underfilled authored `skill2..skill6` branch instead of replaying a coarse first-match branch, while packet-owned `OnSkill` ownership, named-branch prepare suppression, prepare-to-attack promotion, and packet-owned attached-hit-overlay clearing from the previous pass all remain in place. Focused regression coverage for this pass is now on disk in `UnitTest_MapSimulator/BeholderSummonRuntimeParityTests.cs` for weighted branch ordering and active-buff-aware branch promotion. The row remains partial because deeper client-only seams are still outside current simulator visibility: exact hidden summon HP and full `CalcDamage::PDamageSummoned` / `MDamageSummoned`, broader `CVecCtrl::UpdatePassive` side effects beyond the confirmed passive effect trigger, deeper Tesla state-machine transitions and refresh rules, and the remaining assist-family remote branch choices that still depend on client-only state not exposed by current packet/WZ data. | This keeps more of real summon runtime ownership inside the summon layer instead of collapsing local support families to a deterministic fallback: local and packet-owned body-contact damage still share the same mob-authored base-damage surface plus outgoing-damage modifiers, packet-owned body-contact still no longer hides HP loss, packet-owned `OnSkill` branch ownership still lands on authored `skill1..skill6` when packet actions provide those branch ids, packet-owned named summon branches still avoid inheriting a generic prepare window, packet-owned generic prepare branches still promote into live attack state instead of staying pinned on one coarse packet state through the whole action, and packet-owned mob-hit overlays still get interrupted both when summon-owned action-layer changes begin and when packet-owned attack playback completes back into the steady summon layer. Local Beholder support is now closer as well because its authored `skill2..skill6` branch choice is driven by the same weighted summon-runtime selector and current buff ownership instead of a fixed first-available branch order. Remaining mismatch risk is now concentrated in deeper hidden damage formulas, unmodeled passive-update side effects, deeper Tesla transitions, and the last remote/client-only assist selectors rather than in the already-observed local Beholder support branch surface. | summon actor/runtime layer (`CSummoned::Update`, `CVecCtrl::UpdateActive`, `CVecCtrl::UpdatePassive`, `CSummoned::OnHit`, `CSummoned::OnSkill`, `CSummoned::SetAttackAction`, `CSummoned::SetMoveAction`, `CSummoned::ProcessAction`, `CSummoned::ProcessAttack`, `CSummoned::SetDamaged`, `SkillManager.cs`, `UnitTest_MapSimulator/BeholderSummonRuntimeParityTests.cs`, `Skill/132.img/skill/1321007/summon/skill{2,3,4,5,6}`, `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}`, `CSummoned::TryDoingHealingRobot`) |
| Partial | Weapon afterimage / melee trail parity | The simulator still covers the broader WZ-backed afterimage surface already landed for this row: skill-authored plus shared `Character/Afterimage/<family>/<masteryIndex>` catalogs, weapon-item `info/afterImage` family selection with narrow sword-sibling fallback only, local basic-melee registration, charged `charge/<element>` rewrites for the confirmed Page, Paladin, Thunder Breaker, and Aran families, alias-aware action matching, `1221009 blast -> stabO2`, client-backed raw-range normalization for `1221009`, `57 -> 41`, and `74`, remote afterimage playback with fade tails, ordered remote WZ action fallback, and the single-authored plus sole-renderable remote pose promotion seams. Earlier on this row, `RemoteUserActorPool` also closed the active-charge fallback hole with direct `RegisterAfterimage` evidence by recovering the known charged Page, Paladin, Thunder Breaker, and Aran buff ids from the remote actor's last official temporary-stat payload when an official remote melee packet does not carry an explicit `chargeSkillId`, keeping those swings on the same shared `Character/Afterimage/<family>/charge/<element>` surfaces the client would register instead of dropping back to the uncharged mastery catalogs. This run keeps that work in the same seams and narrows another remote `WeaponCharge` recovery hole instead of inventing another owner: `RemoteUserPacketCodec` now preserves whether the official remote temporary-stat blob actually had the `WeaponCharge` bit active plus the aligned payload offset immediately after the client-shaped `rWeaponCharge` `Decode4` seam exposed by `SecondaryStat::DecodeForRemote`, and `RemoteUserActorPool` now consumes that preserved seam in both places that matter for this row. Remote melee afterimage element selection and packet-owned remote weapon-charge avatar-effect selection no longer restart their raw charge-id search at byte `0`; they now only attempt preferred-family recovery when the remote stat mask actually carried `WeaponCharge`, and only from the scoped aligned `WeaponCharge` payload tail that the decoder already walked. That keeps the earlier preferred-known-family behavior for ambiguous confirmed Page/Paladin/Thunder Breaker/Aran payloads while also rejecting stale or unrelated known charge ids that happened to appear elsewhere in the raw remote temporary-stat blob before the `WeaponCharge` seam. The earlier release-owned remote melee transport work remains in place too: `RemoteUserPacketCodec` still keeps the mask-aware `ChargeSkillId` in `RemoteUserTemporaryStatKnownState`, `RemoteUserMeleeAttackPacket` still preserves the client melee `bulletItemId`, decodes the official per-mob attack payload into shared `MobHits` (`mobId`, hit action, per-hit flags, and damage entries, including the variable-count `4211006` case), and keeps the post-attack follow-up `int` that `CUserRemote::OnAttack` reads for the WZ-backed release-owned families already cataloged in `PreparedSkillHudRules` before `OnMeleeAttack` runs. The remote actor path still consumes that preserved release slice through the same owner seam: `PreparedSkillHudRules` resolves the remote prepared-skill release owner for direct release-triggered charge families plus the Wild Hunter swallow attack/release pair, and `RemoteUserActorPool` clears the matching remote prepared hold as soon as the official melee packet arrives instead of leaving the remote HUD armed after the client-owned release packet has already transitioned into attack playback. Focused tests are now on disk in `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` for scoped `WeaponCharge` metadata decode, reset-mask clearing of preserved charge metadata, and remote charge-family recovery that now ignores pre-seam false positives while still honoring preferred-family resolution inside the scoped seam. The remaining gap is narrower again but still real: the client raw-range surface is still limited to the confirmed `1221009`, `57 -> 41`, and `74` seams; remote temporary-stat fallback is now tied to the decoded `WeaponCharge` seam but still does not model the fuller `SecondaryStat::DecodeForRemote` surface beyond that owned branch; the simulator still approximates the real `CreateAfterimageLayer` / `RegisterAfterimage` GR2 choreography with forward-frame plus fade-tail playback; and the broader remote field packet surface still needs the rest of a one-for-one `CUserRemote::OnAttack` / `CUserRemote::OnMeleeAttack` transport lift and client-owned side effects beyond the now-consumed attack envelope, mob-hit, bullet-item, release-follow-up, and scoped preferred-known-charge payload slices | Many melee jobs rely on afterimage trails and authored swing ranges to sell timing and hit shape, and the simulator now consumes the same WZ-backed trail and arc data for local melee skill playback, local basic melee attacks, and simulator-owned remote actors while respecting the weapon item's authored afterimage family instead of forcing every sword through the long-family arcs. Charged melee buffs now stay on a tighter remote seam than before: official remote melee traffic without an explicit charge id can still recover the confirmed charged Page, Paladin, Thunder Breaker, and Aran families from the remote actor's temporary-stat state, but that fallback now only runs when the remote client actually published `WeaponCharge` in its mask and only across the aligned payload tail that begins after the client-owned `rWeaponCharge` decode, which removes another class of false-positive charge-family matches from unrelated raw temp-stat bytes. The shared remote melee owner also no longer leaves release-owned charge HUD state behind once the client packet has already crossed into the actual release attack: direct release-triggered families and the Wild Hunter swallow release pair still tear down their matching remote prepared hold from the official melee packet itself instead of depending on a separate synthetic clear. The remaining visible gap is now concentrated in the broader raw-range table, fuller remote secondary-stat decode beyond the scoped `WeaponCharge` branch, exact GR2 afterimage choreography, and the rest of the official remote melee transport plus client-owned side effects. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `CharacterLoader.cs`, `CharacterAssembler.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `CharacterData.cs`, `MapSimulator.RemoteUsers.cs`, `PreparedSkillHudRules.cs`, `ClientMeleeAfterimageRangeResolver.cs`, `AfterImageChargeSkillResolver.cs`, `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` (`CActionMan::Init`, `CActionMan::GetWeaponAfterImage`, `CActionMan::CreateAfterimageLayer`, `CActionMan::GetMeleeAttackRange`, `get_action_name_from_code`, `CUser::GetAfterimageUOL`, `CUser::RegisterAfterimage`, `CUserRemote::OnAttack`, `CUserRemote::OnMeleeAttack`, `SecondaryStat::DecodeForRemote`, `Character/00002000.img`, `Character/Afterimage/*`, `Skill/122.img/1221009/{action,afterimage}`, remote temporary-stat payloads) |
| Partial | Skill cooldown UI parity | QuickSlot overlays and tooltips, both skill-book variants, the hotkey and off-bar cooldown trays, the shared packet-owned/server-owned cooldown ownership seam in `SkillManager`, the `UI/Basic.img/Notice3` off-bar notice stack, and the Battleship sentinel durability-side effect path are implemented in the existing cooldown UI/runtime seams. The prior pass closed the last tractable packet-owned notice regression in that same seam instead of adding another owner: packet-owned cooldown packets now resolve started/ready notices through the shared authoritative transition helper, so repeated authoritative refreshes and no-op clears no longer re-emit start or ready notices after the cooldown state was already active or already cleared. This pass tightened the remaining Battleship-facing temporary-stat presentation seam that already hangs off the same shared status-bar owner: WZ recheck confirms `Skill/522.img/skill/5221006/common` still authors `epad`, `epdd`, `emdd`, `emhp`, and `emmp` alongside `info/type=13`, so `SkillManager` now prefers the structural `Transform` family as the status-bar family owner for vehicle/morph buffs while preserving the authored temporary-stat order in the tooltip list instead of mislabeling Battleship as a plain PAD-family buff. Focused regression coverage is now on disk in `UnitTest_MapSimulator/SkillCooldownParityTests.cs`, locking the client-backed cases that Battleship-style vehicle buffs surface the `Transform` family while preserving the authored stat order, packet-owned refreshes preserve the original cooldown start tick, packet-owned clears tear the cooldown state down authoritatively instead of falling back to authored WZ cooldowns, repeated authoritative refreshes do not re-fire the start-notice seam, naturally expired server-owned cooldowns complete once without regressing to the authored WZ timer, and the packet-owned transition helper only emits on real authoritative state changes. WZ was also rechecked again for the off-bar notice chrome in the current extracted data source: `UI/Basic.img/Notice3` still exposes the exact three-slice `t/c/s` stack (`266px` wide; heights `21/20/55`) that the simulator already consumes, while nearby `Notice4`, `Notice5`, and `Notice6` are materially different four-slice or boxed families rather than a clearly better match for the current cooldown-owner seam. The remaining parity gap is now limited to the client's exact cooldown-notification art family selection versus the current WZ-backed `Notice3` best fit, any last notice micro-offset or timing/fade differences beyond the shared 44px top-center seam, the exact Battleship `CTemporaryStatView` icon/tray art and any durability-specific HUD widgeting beyond the mounted override plus ordered status-bar tooltip presentation now modeled here, and any additional bespoke cooldown-only HUD widgets beyond the quick-slot, skill windows, status-bar trays, and off-bar notice stack. | Cooling-down skills stay synchronized across quick-slot masks, both skill-book variants, both status-bar trays, hover tooltips, and off-bar started/blocked/ready notices because they all read the same authoritative cooldown seam instead of mixing packet-owned overrides with authored WZ fallback timing. Packet-owned refreshes now preserve the original cooldown start tick without retriggering the start-notice seam, authoritative clears and natural expiry stop every cooldown surface together without falling back to authored WZ timers, the Battleship sentinel keeps the mounted durability override behavior already landed in the runtime, Battleship-style vehicle buffs now identify themselves as `Transform` in the shared status-bar tooltip seam while keeping the WZ-authored stat list order, and the off-bar notice stack remains on the WZ-backed three-slice `Notice3` family until client evidence surfaces a more exact art owner. The simulator still does not reproduce the client's exact cooldown-notification art choice, final notice timing or micro-offset polish, exact Battleship temporary-stat tray/icon visuals, or any extra cooldown-only HUD widgets that may still live outside the currently modeled shared seams. | `QuickSlotUI.cs`, `SkillUI.cs`, `SkillUIBigBang.cs`, `UIWindowManager.cs`, `StatusBarUI.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `SkillManager.cs`, `SkillCooldownNoticeUI.cs`, `SkillCooldownNotificationTransition.cs`, `UnitTest_MapSimulator/SkillCooldownParityTests.cs`, `Skill/522.img/skill/5221006/common`, `UI/Basic.img/Notice3`, `UI/Basic.img/Notice4`, `UI/Basic.img/Notice5`, `UI/Basic.img/Notice6` |
| Partial | Client-timer skill expiry and cancellation | `SkillManager` now owns a centralized client-timer registry that drives buff expiry, timed prepared-skill release, repeat-sustain expiry/mode-end, cyclone teardown, Wild Hunter swallow digest completion, WZ-timed local skill-zone expiry, and WZ-timed local summon expiry through one `UpdateClientTimer`-style seam; it also publishes ordered expiry batches and normalizes cancel requests through connected WZ `info/type` + `affectedSkill` + `dummyOf` families (including observed `type=10`, `type=16`, `type=33`, `type=1`, and multi-target `affectedSkill` links). `SummonedPool` already reused that connected-family matcher for packet-owned local summon cancellation, connected-family level recovery, authored-duration fallback, and inherited local-duration fallback across the full connected cancel family graph instead of only the packet skill id. This run closes the matching packet-owned local-summon expiry batching hole in that same seam instead of leaving packet-owned timer batches on exact-skill de-duplication: `MapSimulator.RouteLocalPacketOwnedSummonExpiryBatchToClientCancel` now primes every naturally expired local packet-owned summon before any cancel request runs, resolves the same connected-family batch key that `SkillManager` already uses for local client timers, and coalesces same-family packet-owned expiry routing through one `RequestClientSkillCancel` request even when multiple linked summon skills expire together. That preserves the shared `SendSkillCancelRequest` fallout for connected-family buffs, prepared state, repeat/cyclone state, local summons, local zones, and packet-owned local summons without regressing summon-side natural-expiry playback that `SummonedPool` still owns. WZ re-checks in the current extracted data source still confirm the connected cancel-family surface this seam depends on: `Skill/3211.img/skill/32110007/info/type=10` with `affectedSkill=32001003`, `Skill/3510.img/skill/35100008/info/dummyOf=35101009&&35101010`, `Skill/3510.img/skill/35101009/info/affectedSkill=35001001`, and `Skill/2112.img/skill/21120011/info/affectedSkill=21100002&&21110003`. Direct IDA re-check of `CUserLocal::SendSkillCancelRequest` at `0x93d730` still confirms that client cancel requests own additional prepared-skill, swallow-reset, vehicle-valid, and keydown-cleanup fallout beyond packet transport alone. Focused regression coverage for this pass is now on disk in `UnitTest_MapSimulator/ClientSkillTimerPacketOwnedSummonExpiryParityTests.cs` for packet-owned full-batch prime-before-cancel ordering and connected-family batch de-duplication on the local summon timer bridge. The row remains partial because the simulator still does not mirror the client's full timer catalog or the full summon-side expiry fallout for packet-owned summons: families that depend on deeper `CSummoned::Update` runtime/combat ownership, packet-owned non-self-destruct expiry teardown after the cancel request has been sent, or other client-only `SendSkillCancelRequest` consumers beyond the currently surfaced cancel-side-effect list are still outside the current local WZ + packet + connected-family timer view. | Timed skill teardown now runs through one shared local seam instead of scattered expiry checks, manual status-bar buff cancellation no longer bypasses client-style cancel routing, and connected WZ cancel families can tear down prepared state, repeat/cyclone state, buffs, local summons, local zones, and packet-owned local summons through one normalized request surface. This run narrows the remaining packet-owned drift in the same seam that already covered local timers: packet-owned local summon-expiry batches now prime all expired local summon objects before any cancel request and no longer spam duplicate cancel requests when multiple linked summon skills from the same connected family expire together on the same tick. Remaining risk is now concentrated on unmodeled client-owned timer families, deeper packet-owned summon-side expiry fallout that still appears to live inside `CSummoned::Update`, and any additional `SendSkillCancelRequest` consumers not yet surfaced in the current cancel-side-effect list. | `ClientSkillCancelResolver`, `SkillManager`, `SummonedPool`, `MapSimulator`, `UnitTest_MapSimulator/ClientSkillTimerPacketOwnedSummonExpiryParityTests.cs` (`CUserLocal::UpdateClientTimer`, `CUserLocal::SendSkillCancelRequest`, `CSummonedPool::OnCreated`, `CSummoned::Update`, `Skill/3211.img/skill/32110007/info/type`, `Skill/3211.img/skill/32110007/info/affectedSkill`, `Skill/3510.img/skill/35100008/info/dummyOf`, `Skill/3510.img/skill/35101009/info/affectedSkill`, `Skill/2112.img/skill/21120011/info/affectedSkill`) |
| Partial | Skill restrictions by map/state | The simulator now enforces parsed map `fieldLimit` bits for `Unable_To_Use_Skill`, `Move_Skill_Only`, `Unable_To_Use_Mystic_Door`, the Mechanic-only `Unable_To_Use_Rocket_Boost` branch, and the mount / Mechanic vehicle `Unable_To_Use_Taming_Mob` branch through the shared skill runtime, rejects active skill use while the local player is dead, in hit-stun, seated, or prone so skill-window and hotkey casts no longer bypass those transient local states, mirrors the client's swallow-specific ladder/rope precondition in the shared evaluator, tears down client-timer / upkeep-driven skill paths such as keydown holds, cyclone pulses, mechanic mine auto-deploy, and repeat-sustain mode skills when those same local-state restrictions become invalid after cast start, now also feeds mob-disease lockouts sourced from WZ-authored `MobSkill.img` durations through that same shared state seam so direct casts, prepared-hold upkeep, repeat-sustain upkeep, cyclone, mine teardown, and Rocket Booster sustain all see `Seal` (`120`), `Stun` (`123`), `Attract` (`128`), the freeze family (`131`), and the WZ-authored polymorph pair (`172`, `173`) instead of leaving those branches split between cast-only booleans and isolated upkeep checks, routes simulator direction-mode / scripted-interaction ownership through that same shared state gate so skill-window or hotkey casts are rejected while NPC/script overlays own local control and active prepared/upkeep skills tear down once scripted control begins, widens the shared mount/vehicle classification to WZ-authored `eventTamingMob` riders plus broader client-owned vehicle buffs surfaced through `info/type=13` so `Unable_To_Use_Taming_Mob`, ladder/rope rejection, and airborne vehicle-start gating also catch non-Mechanic ownership cases such as Battleship and later event ride skills instead of a stale hardcoded subset, now also covers the client-confirmed ladder/rope veto for the Hermit/Shadower taunt branch (`4121003`, `4221003`) through the same shared evaluator instead of leaving those ids outside the common gate, now also routes live `MapInfo` ownership through the shared field restriction seam so skill casts see current map `fieldLimit` values plus the client-owned Coconut and Snowball minigame basic-attack-only wrappers instead of relying on a map-bind-time snapshot, and this pass widens the Coconut side of that seam to accept the authored root `coconut` map property instead of only `info/fieldType`, now also mirrors the WZ-authored root `noSkill` field-owner surface by rejecting casts when the live map forbids the local player's job branch via `noSkill/class` or names the exact skill id via `noSkill/skill`, and earlier work had already widened that same `noSkill` seam to accept direct-int authored entries, exact current job ids, and the existing skill metadata / skill-book fallback path instead of only list-shaped children and derived class digits. This pass closes another authored-shape gap in that same seam by recursively scanning nested `noSkill` descendants and accepting string-authored integer leaves in addition to direct or one-level int children, so deeper WZ-authored wrappers still resolve against the same shared restriction path instead of quietly slipping past the evaluator. The row also still threads the WZ-authored Mu Lung Dojo `mobMassacre/disableSkill` flag through that same shared field gate so direct casts plus prepared/upkeep teardown see the same runtime prohibition instead of leaving Dojo-only skill lockout as HUD state, threads the authored `mobMassacre/countEffect/*/skillUse` counter seam through that same field gate so active casts are rejected when the live massacre skill counter reaches zero instead of leaving that branch as a HUD-only counter, and mirrors the Guild Boss basic-action owner through the same live map seam by rejecting casts when `info/fieldType=8` or the authored root `pulley` / `healer` ownership marks a Guild Boss map. The remaining gap is now narrower but still real: deeper `CField` availability checks and the remaining field-owned or client-only forbidden-skill branches are still incomplete | Maps that explicitly suppress skills now block the same broad skill families in the simulator, Rocket Booster can be rejected on field entry and cast attempt and now also tears down if a shared external restriction begins after launch, seated/prone transient states no longer let active skills slip through the generic cast path, swallow casts now fail on ladder/rope like the client branch, ongoing upkeep skills stop once the player falls into a forbidden local or mob-disease state instead of continuing through it, the common mob-driven seal / stun / seduce, freeze, and polymorph lockouts now use the authored WZ timers and surface explicit rejection reasons through the same shared skill gate instead of only blocking fresh casts via side booleans or transform-only state, scripted direction-mode control now also blocks new active-skill requests while cancelling active prepared, repeat-sustain, cyclone, mine, and Rocket Booster upkeep through the same shared restriction seam instead of leaving those paths running under NPC or field-script ownership, the client taunt branch now also rejects ladder/rope casts through the shared evaluator instead of only the old swallow or vehicle subset, the `Unable_To_Use_Taming_Mob` plus client-side ladder/airborne vehicle checks now reject WZ-authored event mount skills together with broader client-owned vehicle ownership buffs such as Battleship or Jaguar Rider rather than only the earlier Mechanic-focused subset, client-owned Coconut/Snowball maps now reject active skills through the same shared seam even when the restriction needs to come from live map ownership rather than only a captured `fieldLimit` or `fieldType` value, and WZ-authored root `noSkill` maps now also reject whole job branches, exact listed skills, beginner-owned casts on advanced jobs, intermediate branch ids such as `43` / `141`, nested authored descendants, and string-authored integer leaves through that same live field seam instead of depending on only flat direct-int layouts. Mu Lung Dojo massacre maps whose WZ publishes `mobMassacre/disableSkill=1` now reject fresh skill casts while also tearing down prepared or sustained paths through that same shared evaluator instead of only zeroing the HUD-side skill counter, massacre maps whose WZ publishes `mobMassacre/countEffect/*/skillUse=1` now also reject casts whenever the live packet-owned massacre skill counter is exhausted instead of only drawing a depleted skill counter on the timerboard, and Guild Boss maps now reject active skills through the same shared evaluator when either `info/fieldType=8` or the authored `pulley` / `healer` owner roots identify the field even if the live restriction is not just a captured `fieldLimit` bit. Focused evaluator regression coverage is now on disk in `UnitTest_MapSimulator/FieldSkillRestrictionEvaluatorTests.cs` for advanced-job casts of beginner-owned forbidden skills, skill-book fallback to beginner ownership when explicit skill job metadata is absent, intermediate `noSkill/class` branch ids, nested/string-authored `noSkill` descendants, the `noCancelSkill` cancel gate, and the negative unrelated-branch case. The remaining gap is still concentrated in the last `CField` gate branches and other per-skill client checks outside `IsSkillAvailable`, especially any additional field-owned owners beyond the now-modeled Coconut/Snowball, Guild Boss, root `noSkill`, Mu Lung Dojo `disableSkill`, and the massacre `skillUse` counter seam plus any remaining client-only forbidden-state branches beyond the currently modeled mob-disease / scripted-control / taunt-ladder set | `MapSimulator`, `SkillManager`, `FieldSkillRestrictionEvaluator.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `PlayerSkillBlockingStatusMapper.cs`, `PlayerCharacter`, `PlayerCombat`, `PlayerManager`, `PlayerMobStatusController`, `MassacreField.cs`, `MapInfo`, `MapInfo.fieldLimit`, `MapInfo.fieldType`, `FieldLimitType`, `FieldType`, `Skill/MobSkill.img`, `Map/Map1/109080000.img/coconut`, `Map/Map1/109080000.img/info/fieldType`, `Map/Map1/109060000.img/snowBall`, `Map/Map1/109060000.img/info/fieldType`, `Map/Map9/980031100.img/noSkill/class`, `Map/Map9/922240000.img/noSkill/skill`, `Map/Map9/910320100.img/mobMassacre/disableSkill`, `Map/Map9/926010100.img/mobMassacre/countEffect/500/skillUse`, `Map/Map9/990000900.img/info/fieldType`, `Map/Map9/990000900.img/pulley`, `Map/Map9/990000900.img/healer`, `Map/Map2/200110000.img/info/noCancelSkill`, `UnitTest_MapSimulator/FieldSkillRestrictionEvaluatorTests.cs`, `Skill/412.img/4121003/info`, `Skill/422.img/4221003/info` (`CUserLocal::Update`, `CUserLocal::IsSkillAvailable`, `CField::IsUnableToUseSkill`, `CField_Coconut::BasicActionAttack`, `CField_SnowBall::BasicActionAttack`, `CField_GuildBoss::BasicActionAttack`, `CField::IsMoveSkillOnly`, `CField::IsUnableToUseRocketBoost`, `CField::IsUnableToUseTamingMob`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::TryDoingSwallowAbsorb`) |

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Summoned action-loader / frame-build parity | The client-owned summon loader seam is now implemented more directly in the existing `SkillLoader` path instead of being split between documentation and coarse `SkillData` fields. Earlier work had already moved summon frame building into the loader: summon animation loading resolves linked summon assets before branch selection, loads summon action frames through a summon-specific frame builder with the client's `120` ms `delay` fallback, preserves per-frame `z`, prefers authored frame metadata when linked wrapper nodes publish `origin` or bounds, computes frame rectangles from authored `lt`/`rb` when present and otherwise falls back to origin-relative texture bounds, and appends a reversed copy of the action frame list when the branch publishes the reverse-style flag surface now handled by the loader. This pass closes the tractable cache-ownership part of the row in that same seam: `SkillData` now records the resolved summon asset path the loader actually consumed, and `SkillLoader` now keeps a summon-action table keyed by the summon action/branch key (`summon`, `stand`, `prepare`, `attackTriangle`, `hit/...`, etc.) so spawn, idle, attack, removal, hit, and supplemental summon branches all project from one action-key cache instead of each public `SkillData` field loading its own separate animation instance. WZ re-checks in the current extracted data still show the authored summon branch shape that cache is consuming, including direct summon action metadata such as `Skill/3511.img/skill/35111002/summon/attackTriangle/*/delay=120`, authored idle origins such as `Skill/3511.img/skill/35111011/summon/stand/*/origin`, linked-wrapper summon branches such as `Skill/3511.img/skill/35111011/summon/heal/* -> ../stand/*`, and concrete repeat-style summon surfaces still present in the current dataset such as `Skill/3511.img/skill/35111002/mob/repeat` and `Skill/434.img/skill/4341006/summon/repeat`. IDA remains the owner evidence for the final unresolved pieces: `CActionMan::GetSummonedProp` still resolves the summon asset from `SKILLENTRY::GetSummonedUOL`, `SKILLENTRY::GetSummonedUOL` still returns the precomputed `sSummonedUOL` field rather than exposing where that field was originally chosen, and `CActionMan::LoadSummonedAction` still owns the exact `(skillId, slv, action)` cache key plus the exact reverse-playback property string. The remaining gap is therefore narrower again: the simulator still does not mirror the client's exact `SKILLENTRY::GetSummonedUOL` source when that lookup diverges from the local linked `summon` node, its new summon-action cache is still keyed per loaded `SkillData` action branch rather than the client's full `(skillId, slv, action)` table, and the reverse-playback flag name is still inferred from exposed WZ `repeat`-style summon surfaces plus the client branch rather than proven from the exact client string-pool lookup. | Without this row, future summon work is biased toward packet/runtime fixes while leaving the client's actual frame-build owner implicit. Tracking the loader explicitly keeps summon parity anchored to the real asset-selection and frame-construction seam, which is where action-name lookup, cached reuse, delay/z fallback, rect math, and reverse playback are decided before `CSummoned::Update` ever consumes the result. With the loader work now landed in the existing seam and the simulator-side action cache now owned there as well, the remaining parity risk is concentrated on the last client-owned asset-resolution, level-specific cache-key, and reverse-flag-name details rather than on basic summon frame construction. | summon action-loader layer (`SkillLoader`, `SkillData`, `CActionMan::GetSummonedProp`, `CActionMan::LoadSummonedAction`, `SKILLENTRY::GetSummonedUOL`, `CSummoned::Update`, `CSummonedPool::OnCreated`, `Skill/3511.img/skill/35111002/summon/attackTriangle`, `Skill/3511.img/skill/35111011/summon/stand`, `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3511.img/skill/35111002/mob/repeat`, `Skill/434.img/skill/4341006/summon/repeat`) |

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

This helper-owner seam is currently under-tracked by the backlog even though both the client and simulator already expose concrete ownership here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow-partner and mirror helper actors | The simulator now models the client ownership split instead of treating every clone-family helper as the same WZ-backed shadow-partner path. `SkillData` / `SkillManager` recognize `Mirror Image` (`4331002`) as a distinct helper owner, `PlayerCharacter` keeps a separate mirror runtime from the existing `special/*` shadow-partner actor, and the update/render loop now drives mirror helper visibility from player action changes with a dedicated `m_tStartMirror`-style start timestamp, a 200 ms client-shaped rel-move window, and client-backed suppression for ladder, rope, morph, taming-mob, hit, and dead states. WZ re-check still confirms `skill/433.img/skill/4331002` exposes buff/effect data plus `action/0 = alert`, but no `special/*` helper action branch, so Shadow Partner keeps using WZ-authored helper animations while Mirror Image continues to source the assembled avatar instead. This pass tightens the final mirror source seam again against the targeted `CUser::PrepareMirrorActionLayer` / `CUser::Update` re-check: `CharacterAssembler` still classifies every assembled avatar part into explicit `UnderCharacter`, `OverCharacter`, `UnderFace`, `Face`, and `OverFace` render planes, `PlayerCharacter` still keeps a dedicated mirror-owned prepared five-layer cache with per-plane signatures/facing/prepare ticks, and mirror insertion still follows the first visible part in each plane. Mirror draw now prefers the live assembled plane from the current frame whenever the prepared action name, frame index, facing, and per-plane source signature still match, and only falls back to the prepared snapshot when the live plane has drifted. That keeps the simulator closer to the client's `GetMirrorSrcLayer` / `PrepareMirrorActionLayer` flow by sourcing visible plane order, per-part visibility, and source-part tint from the live frame at draw time instead of always rendering the cached cloned plane. Focused regression coverage is now on disk in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` for the live-plane eligibility gate in addition to the existing visible-plane insertion fallback, tint-sensitive source signature behavior, mirror-tint multiplication over source-part tint, facing-sensitive prepared-layer reuse, and layer-transition start selection. The remaining gap is now concentrated at the true live-layer-object boundary inside `CUser::PrepareMirrorActionLayer` / `CUser::Update`: IDA still shows the client creating real `IWzGr2DLayer` mirrors, copying source-layer origin/flip from live layer handles, and then driving first-canvas insertion plus layer-parent synchronization through `InsertCanvas` and shared COM layer ownership. The simulator still does not own those shared COM layer objects, true `InsertCanvas` reparenting against the live client layer graph, exact source-layer origin/flip snapshots from live `IWzGr2DLayer` instances, or the follow-up clone-helper rules that depend on those real client layer objects rather than simulator-owned prepared metadata plus live assembled-part fallback. | Clone-family jobs are not just tray icons or generic buff effects in the client; they own dedicated avatar-adjacent helper layers that participate in action preparation and per-frame user update. Tracking that seam explicitly keeps future parity work focused on helper-layer timing, ownership, and clone-family differentiation instead of accidentally treating shadow-partner and mirror-image visuals as solved just because the simulator already renders one WZ-backed helper actor | `PlayerCharacter.cs`, `CharacterAssembler.cs`, `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` (`skill/433.img/skill/4331002`, `CUser::PrepareActionLayer`, `CUser::PrepareShadowPartnerActionLayer`, `CUser::PrepareMirrorActionLayer`, `CUser::GetMirrorSrcLayer`, `CUser::Update`) |

### 4. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Time Bomb, Vengeance, and ExJablin packet-application parity | The simulator still owns this `CUserLocal::OnPacket` family inside the existing local-utility packet seam: `LocalUtilityPacketInboxManager` recognizes packet types `268` / `271` / `272` plus `timebomb` / `vengeance` / `exjablin` aliases, `MapSimulator.PacketOwnedUtilityParity` decodes and applies the three payload families, and `OnExJablinApply` still arms a simulator-owned next-shot flag that is consumed by the next ranged/projectile attack and forces that whole attack to crit, matching the direct IDA re-check of `CUserLocal::OnExJablinApply` (`m_bNextShootExJablin = 1`). This pass keeps the dedicated Time Bomb execution path and also tightens the packet-body seam to match the client instead of tolerating simulator-side extras. The direct IDA re-check for `CUserLocal::OnTimeBombAttack` still shows the packet body as `(skillId, timeBombX, timeBombY, impactPercent, damage)`, the client-default `1500 ms` hit period before coat-option merges, and the exact `CUserLocal::TryDoingMeleeAttack(..., bTimeBombAttack, nTimeBombX, nTimeBombY, ...)` call; `CUserLocal::OnVengeanceSkillApply` still reads a single `Int32` skill id and only branches on legacy packet id `3120010`; `CUserLocal::OnExJablinApply` still reads no payload bytes at all. The simulator now mirrors those shapes in the existing skill seam: `TryDecodePacketOwnedTimeBombAttackPayload` requires the exact five-`Int32` body, `TryDecodePacketOwnedVengeanceSkillApplyPayload` requires the exact one-`Int32` body, and `TryDecodePacketOwnedExJablinApplyPayload` rejects any non-empty payload before the next-shot flag is armed. `ApplyPacketOwnedTimeBombAttack` still restores the `1500 ms` base before the existing WZ-backed invincibility-option merge and still only applies hit-reaction fallout when impact is nonzero, and `SkillManager.TryApplyPacketOwnedTimeBombAttack` still owns the dedicated packet-owned Time Bomb melee branch that keeps the packet-owned `timeBombX` / `timeBombY` origin, resolves the melee hitbox at the bomb position, and switches Time Bomb target ordering to point-centered selection through `CreatePacketOwnedTimeBombTargetSortKey` so the closest mob to the explosion point wins before player-facing penalties. The shared packet-skill alias seam still stays anchored to WZ and client evidence: `String/Skill.img` resolves `4341003` as `Monster Bomb` with the `explosion occurs 3 seconds after the charm is activated` description fragment, `31101003` as `Vengeance`, and `4120010` as `Expert Throwing Star Handling` with the `the next attack will always be a Critical Attack` fragment, while the client still confirms the legacy Vengeance packet id `3120010`. Focused regression coverage is now actually present in `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs` for the exact packet-body decoders and the Time Bomb target-priority helper. The remaining gap is narrower but still real: Time Bomb still does not have a client-confirmed legacy packet-to-WZ skill-id mapping beyond the current WZ-backed `Monster Bomb` candidate, and the Time Bomb coat-option inference is still intentionally scoped to the client-confirmed invincibility options rather than a general simulator-wide item-option-id population seam for arbitrary potentials. | If these handlers stay hidden inside the broad cast rows, packet-owned skill applications that bypass the normal local input path will keep looking uncovered even though the client gives them their own dispatcher and follow-up state. The simulator now has a concrete owner for those packets, keeps the shared alias seam anchored to both client evidence (`3120010` for Vengeance, `OnExJablinApply` as a next-shot flag, and the `OnTimeBombAttack` / `OnVengeanceSkillApply` / `OnExJablinApply` body shapes plus the `1500 ms` base reaction period / `TryDoingMeleeAttack(..., bTimeBombAttack, nTimeBombX, nTimeBombY, ...)` call) and WZ evidence (`4341003` Monster Bomb by name/description, `31101003` Vengeance by name, and `4120010` ExJablin by description), no longer misreads the Time Bomb coordinates, no longer approximates the Time Bomb attack path as only a generic target/origin override, and no longer silently accepts simulator-side trailing bytes that the client packet readers do not consume. The remaining risk is now concentrated in the still-unconfirmed legacy Time Bomb skill-id mapping and the deliberately narrow visible-potential-to-item-option inference seam, not in missing ownership for the Vengeance/ExJablin packet family, Time Bomb packet decode, or the dedicated `bTimeBombAttack` execution path. | packet-owned local skill-application layer (`CUserLocal::OnPacket`, `CUserLocal::OnTimeBombAttack`, `CUserLocal::OnVengeanceSkillApply`, `CUserLocal::OnExJablinApply`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::ApplyInvicibleOption`, `CUserLocal::SetImpact`, `MapSimulator.PacketOwnedUtilityParity`, `LocalUtilityPacketInboxManager`, `SkillManager`, `PlayerCharacter`, `CharacterPart`, `Character/00002000.img`, `String/Skill.img`, `Item/ItemOption.img`) |

### 5. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Combo counter / combo-command packet parity | `MapSimulator.PacketOwnedComboParity` now owns the simulator-side `OnIncComboResponse` bridge instead of leaving combo implied by the generic temporary-stat tray. The simulator decodes the packet-owned combo count from the combo inbox/chat transport, stores the owner count/timestamp, resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them on the same five-second alpha window, applies the client-matching fly-origin amplitude split across the least-significant digit run, and conditionally spawns the Aran combo-command attack/buff overlays with `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` first and WZ-backed named fallbacks for exports that only ship `ComboSmash`, `ComboDrain`, `ComboFenrir`, `ComboTempest`, and `ComboBarrier`. The row no longer depends on the generic `Combo` temporary-stat heuristics in `SkillManager`; it has a dedicated packet-owned HUD/effect owner, content load path, update lifetime, and draw path in `HaCreator/MapSimulator`. | If combo stays implied only by the temporary-stat tray, the backlog will overstate skill parity while missing the client's actual packet-owned combo counter, visible combo HUD/effect lifetime, and the extra combo-command visuals that hang off learned Aran skills. Tracking it explicitly keeps future work anchored to the real packet and draw owners instead of assuming the generic buff/cooldown surfaces already cover combo gameplay feedback. | packet-owned combo HUD/effect layer (`CUserLocal::OnPacket`, `CUserLocal::OnIncComboResponse`, `CUserLocal::DrawCombo`, `Effect/BasicEff.img/Combo`, `Effect/BasicEff.img/ComboCommand`) |

Buff icon plan:

1. Confirm asset sources and ordering:
   Use `UI/BuffIcon.img` for temporary-stat icon chrome and `Skill/*/icon` for per-skill icon sourcing where the client uses the skill's own icon, then validate row ordering and refresh cadence against `CUIStatusBar::Draw` and `CUserLocal::Update`.
2. Add a runtime-to-UI snapshot:
   Expose a small status-bar model from `SkillManager` that includes `skillId`, applied stat family, start time, duration, and refresh/replace semantics so the UI does not need to inspect buff internals directly.
3. Extend status-bar loading and drawing:
   Teach `UILoader` and `StatusBarUI` to load buff-slot art, render the active icon row in the client-aligned status-bar region, and reserve the interaction seam for later right-click cancel behavior.
4. Verify state synchronization:
   Check short-duration buffs, long-duration buffs, same-skill refresh, natural expiry, and `/job` cleanup so icon removal stays synchronized with the runtime and does not fight quick-slot cooldown overlays.


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. Summon actor/runtime pass:
   Close the missing `CActionMan::LoadSummonedAction` loader seam together with the packet-owned `CSummonedPool` gaps and the `CSummoned::Update`-owned movement, touch-damage, Tesla-state, and position-sync gaps instead of only extending attack metadata and per-skill summon heuristics.
4. Clone-helper parity pass:
   Tighten the dedicated `CUser::PrepareActionLayer` / `CUser::Update` helper-layer seam for shadow-partner and mirror-image families instead of leaving clone behavior folded into generic buff or summon work.
5. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
6. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
7. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".

