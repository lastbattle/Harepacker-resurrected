# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::TryDoingMeleeAttack` at `0x91e780`
- `CUserLocal::TryDoingShootAttack` at `0x925a00`
- `CUserLocal::TryDoingMagicAttack` at `0x92a240`
- `CUserLocal::TryDoingBodyAttack` at `0x930710`
- `CUserLocal::DoActiveSkill_MesoExplosion` at `0x942200`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`
- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`
- `CAnimationDisplayer::Effect_SkillUse` at `0x459100`
- `CAnimationDisplayer::RegisterBulletAnimation` at `0x455410`
- `CAnimationDisplayer::RegisterMagicBulletAnimation` at `0x455570`
- `CAnimationDisplayer::RegisterHookingChainAnimation` at `0x45cbd0`
- `CAnimationDisplayer::RegisterChainlightningAnimation` at `0x45d1b0`
- `CAnimationDisplayer::RegisterTeslacoilAnimation` at `0x45d650`
- `CAnimationDisplayer::RegisterFootHoldAnimation` at `0x45a990`
- `CAnimationDisplayer::RegisterMotionBlurAnimation` at `0x45ad00`
- `CAnimationDisplayer::Effect_SkillPrepare` at `0x45b840`
- `CAnimationDisplayer::RegisterPrepareAnimation` at `0x45b420`
- `CAffectedAreaPool::GetInstance` at `0x52a480`
- `CAfterImageBullet::Update` at `0x477aa0`
- `CActionMan::GetWeaponAfterImage` at `0x428080`
- `CActionMan::CreateAfterimageLayer` at `0x428a20`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Player skill execution parity

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
`CUserLocal::Update` at `0x937330` orchestrates several of these paths and confirms that key-down gauge updates, queued follow-up attacks, prepared-skill execution, repeat-skill handling, teleport, rush, flying-rush, rocket-booster start/end handling, movement-coupled shoot branches, timer-driven cancellations, and status-bar feedback are coordinated in one local-player update loop rather than isolated one-off handlers. A second IDA pass also showed that summon parity splits across three owners rather than only the actor/runtime shell: `CSummoned::Update` drives summon actor motion, active/passive vec-ctrl updates, body-contact damage checks, assist-type dispatch, Tesla Coil state handling, self-destruct timing, and final world-position sync; `CSummonedPool::OnPacket` dispatches created/removed/move/attack/skill/hit packets and `CSummonedPool::OnCreated` ties each summon to its owning user, skill id, level data, and local-user registration path; and `CActionMan::{GetSummonedProp,LoadSummonedAction}` own the WZ-side summon UOL lookup, per-action frame build, delay/z metadata, reverse-playback duplication, and cached action-entry reuse before the runtime ever updates or draws that actor. A follow-up function scan also ties weapon afterimage parity to `CActionMan::GetWeaponAfterImage` and `CActionMan::CreateAfterimageLayer`: the first owns the cached action-indexed trail canvases and attack rectangles, while the second clears and repopulates the target GR2 layer from that cache.

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
This row remains `Partial`. WZ was rechecked first on the same summon-owner surfaces and still matches this seam: `Skill/3510.img/skill/35101005` still mixes wrapper nodes with real summon branches (`cDoor`, `sDoor`, `oDoor`, `mDoor`, `eDoor`), `Skill/501.img/skill/5010005/summon` still exposes custom action branches (`piratebless`, `rushBoom`, `cannonSlam`, `noiseWave`, `jump`, `hit1`) beside `attack1`-`attack4`, and `Skill/434.img/skill/4341006/summon` still remains effect/repeat-only with visible owner hints through `info/requireSkill = 4331002` and root `req/4331002 = 30`. The mounted `skill` category still exposes no authored `sSummonedUOL`/`*UOL*` property names, so the loader remains client-field-driven rather than WZ-authored.

Client evidence stayed second: `SKILLENTRY::GetSummonedUOL` at `0x417f30` still returns only `this->sSummonedUOL`, `CActionMan::GetSummonedProp` at `0x41b830` still resolves that BSTR through `IWzResMan::GetObjectA`, `CActionMan::LoadSummonedAction` at `0x423100` still builds frames from that resolved summon property, and `CSkillInfo::LoadSkill` at `0x70c190` still owns populating `SKILLENTRY::sSummonedUOL` before those consumers read it.

This pass keeps the implementation inside `SkillLoader` and widens recovered hidden-table ingestion for client-extracted mounts again. Current-skill table rows now also accept compact header/value sidecar rows where extractor output splits field names (`header`, `headers`, `columns`, `fieldNames`, `keyNames`) from row values (`value`, `values`, `rowData`, `recordValues`, `fieldValues`) as either delimited strings or indexed child containers. Those compact rows are reconstructed through the same structured-record owner gate, so mounted-path owners, numeric owners, generic owner aliases (`name`, `entryName`, `rowName`, `keyName`, `propertyName`, `nodeName`), and generic value aliases (`data`, `payload`, `targetValue`, `pathValue`, `uolData`) continue to work only when the row owner resolves to the current skill. The already-covered direct, nested, grouped, row-indexed, mounted-path, token-named, owner-field, string/UOL, safe-numeric, encoded, relative, row-name encoded, structured row-name, quoted-field, snake_case, compact source/value, query-string, tab-delimited, CSV, JSON-ish, semicolon, comma, pipe, and newline record forms remain intact. Owner fields are still consumed only for row ownership and are not emitted as raw UOL candidates, so owner-only records do not promote the current owner path as a summon source.

Focused coverage in `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs` now locks current-owner acceptance and other-owner rejection for delimited header/value sidecar rows and indexed header/value containers, alongside generic `name`/`data` rows, query-string `uolData` rows, owner-only `name` rejection, and the existing sidecar, mounted-path, numeric, percent-encoded, escaped path, quoted-field, structured row-name, and hidden-table coverage. Remaining gap: the simulator still does not extract or ship the complete hidden client-owned `SKILLENTRY::sSummonedUOL` table from the client binary. Once that table is provided through mounted/client-extracted sidecar rows, the loader accepts the currently modeled table shapes without promoting unrelated sibling owners; summons whose true owner appears only in the unrecovered client field and no sidecar input still require a dedicated extraction/data-mount follow-up before this row can be marked `Implemented`.

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::Update` at `0x8fb8d0`

Notes:
This row remains `Partial`. WZ was rechecked first: `skill/433.img/skill/4331002` still has `action/0 = alert` plus `effect` and still has no `special`, while representative Shadow Partner roots (`skill/411.img/skill/4111002`, `skill/421.img/skill/4211008`, `skill/1411.img/skill/14111000`) still expose authored `special` helper branches. Client evidence stayed second from the existing IDA notes for this row: `CUser::GetMirrorSrcLayer` returns the five source layers (`UnderCharacter`, `OverCharacter`, `UnderFace`, `Face`, `OverFace`), `CUser::PrepareMirrorActionLayer` recreates helper layers only when missing/flip-mismatched before reparenting under `m_pLayerUnderFace` and seeding rel-move from source-layer current-time + `200 ms`, and `CUser::Update` keeps helper layer objects and inserts the current source-layer `IWzCanvas` rather than rebuilding helper objects per source change.

The simulator keeps the client ownership split in the existing seam (`SkillData` / `SkillManager` still treat Mirror Image `4331002` as mirror-helper owned, while Shadow Partner stays on `special/*` helper-action catalogs). Existing prepared-layer behavior remains intact: UnderFace overlay routing, prepared layer-object recreation on flip or scale-filter divergence, source/current-time snapshot tracking, same-facing placement anchoring, hidden-state prepared-layer release, raw-action suppression/back-offset buckets, stale insert-metadata reset on helper-layer object replacement, source/overlay mismatch rejection priority, same-helper-layer live insert re-admission on source-layer timeline advance/regression, source identity/signature changes, missing-time re-entry, incomplete source/overlay/source-identity metadata re-entry, current canvas/frame identity churn, and live source wrapper cloning that keeps the current frame reference.

This pass narrows the live-layer seam again inside `PlayerCharacter` by recording simulated source-layer handle metadata and source-layer origin/bounds signatures alongside the existing source-parts, source-signature, source-canvas-signature, source-layer, overlay, helper-layer-object, and current-time insert metadata. Same-helper-layer live insert now re-admits when the recorded source-layer handle identity changes, when source-layer origin/bounds metadata changes, or when either of those metadata channels is incomplete, even if wrapper part identity, source-parts list identity, offsets, z, visibility-owner channels, source-layer time, core item identity, source signatures, and current canvas/frame signatures otherwise look stable. The metadata is reset together with stale insert state when helper-layer objects are replaced, matching the existing prepared-layer replacement seam.

Focused regression coverage in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` now also locks source-layer handle metadata churn, source-layer origin/bounds metadata churn, missing source-layer handle metadata re-entry, and origin-signature changes from layer movement or resize, alongside the existing canvas/frame-metadata, same-object source-part metadata, timeline, metadata-missing, mismatch-priority, stable-reject, and visibility-owner signature coverage.

Remaining gap stays `Partial` and is narrowed further toward true native object ownership: the simulator still does not own shared native `IWzGr2DLayer` mirror objects, COM origin/current-time handles, exact `IWzCanvas` object lifetime/reference semantics inside `InsertCanvas`, full native source-layer current-time ownership on shared layer handles, or native avatar-scale filter handle ownership. The new source-layer handle/origin metadata is a simulator-side approximation in the existing live-insert seam, not a recovered native GR2 layer graph.

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::GetShadowPartnerProp` at `0x41ba60`
- `CActionMan::LoadShadowPartnerAction` at `0x423cf0`

Notes:
This row remains `Partial`. WZ was rechecked first on the same mounted loader source (`Character/00002000`) and still exposes concrete helper action-data rows with signed `delay`, optional `move`, optional `flip`/`rotate`, and no additional authored fields beyond the modeled loader surface (`action/frame/delay/flip/filp/move/rotate/event-delay` plus inline canvas children). The previously covered source-gated post-init rows remain modeled (`ride2`, `getoff2`, `flamethrower_pre2`, `flamethrower2`, `flamethrower_after2`, `flamethrower_pre`, `flamethrower`, `flamethrower_after`, `gatlingshot`, `battlecharge`, `mRush`, `herbalism_mechanic`, `mining_mechanic`, `gather0`, `gather1`, `darkTornado_pre`, `darkTornado`, `darkTornado_after`, `pvpko`, `iceAttack1`, `piercing`, `ride3`, and `getoff3`). This pass extends resolver-owned built-in piece plans to the next mounted, parseable, post-init helper slice: `iceAttack2`, `iceSmash`, `iceTempest`, `iceChop`, `icePanic`, `iceDoubleJump`, `darkImpale`, `glacialChain`, `mistEruption`, `archerDoubleJump`, `swingC1`, `swingC2`, and `shotC1`. Those rows now keep mounted piece ordering, signed-delay/event-delay shaping, `move`, and transform metadata when the mounted row is unavailable, while remaining source-gated so unrelated Shadow Partner skills cannot claim them through generic fallback.

Client evidence stayed second: `CActionMan::Init` (`0x41beb0`) still walks raw actions `0..272`, skips raw action `55`, reads `Character/00002000` action piece rows into `ACTIONDATA`, accumulates negative delays into `tEventDelay`, otherwise uses `total-minus-last`, and stores `move` / `flip` / `rotate` before `CActionMan::LoadShadowPartnerAction` (`0x423cf0`) consumes that action-data surface for helper frame build. The new rows sit beyond the recovered init loop, so the simulator keeps them in the same source-gated action-data fallback seam instead of widening runtime state or generic raw-action admission.

Focused regression coverage in `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs` now locks admission and source-action rejection for the newly promoted rows, and verifies representative synthesized timing/event-delay plus transform output for `iceSmash`, `icePanic`, `darkImpale`, `glacialChain`, `mistEruption`, `swingC1`, and `shotC1`. Existing coverage remains in place for total-minus-last timing, negative-delay carryover, owner-level and nested owner-level event-delay shaping, piece-local override, mounted hidden-row synthesis, client-init/fallback-only/generic fallback admission, `magicmissile`, legacy `create0` / `create1`, ghost timing, adjacent post-init rows, and recovered-source gating.

Remaining gap is narrower but still real: more source-gated mounted post-init rows under `Character/00002000` are now covered by resolver-owned fallback plans, but several later parseable rows still rely on mounted-row availability rather than built-in resolver plans, hidden client-only helper rows not present in the mounted export remain unrecovered, exact native action-data classification for every post-init action row is not yet fully mirrored, native action-table fields not represented in mounted WZ remain unconfirmed, and client-build/data-set differences in helper UOL/hidden-row ownership can still introduce extra loader-owned paths.

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another client-owned skill family that was not represented anywhere in the backlog text. `OnTimeBombAttack` is not just a generic damage callback: the decompile shows it decoding a skill id plus attack/action data, resolving the local skill level, calling `TryDoingMeleeAttack`, then applying hit-period/invincibility and impact side effects before forcing the avatar hit reaction. `OnVengeanceSkillApply` is also more specific than a plain temporary-stat refresh because it gates on skill id `3120010` and then calls `DoActiveSkill_MeleeAttack` through the local skill runtime, while `OnExJablinApply` arms the `m_bNextShootExJablin` flag for the next ranged-shot branch rather than behaving like a generic buff bit. That is a dedicated packet-owned skill-application seam between the broad cast/update families and the existing temporary-stat or helper rows.

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`

Notes:
The same targeted `CUserLocal::OnPacket` scan surfaced another local skill owner that the backlog still was not naming. `OnIncComboResponse` is very small but concrete: it decodes the server-authored combo count, stores it in `m_nCombo`, stamps `m_tLastSetCombo`, and immediately routes through `DrawCombo`. The `DrawCombo` decompile then shows that combo is not just another temporary-stat label. The client resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them alive on a five-second alpha window, applies fly-origin motion to the combo vectors, and conditionally spawns extra `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` layers when the corresponding Aran combo-command skills are learned. A repo search across `HaCreator/MapSimulator` still only surfaced the generic `Combo` temporary-stat labeling in `SkillManager` and did not surface any simulator-side combo packet owner, combo HUD/effect loader, or `ComboCommand` runtime, so this remains a real missing parity bucket rather than stale wording.

### 7. Shared skill-use animation-owner parity discovered by targeted `CAnimationDisplayer` scan

- `CAnimationDisplayer::Effect_SkillUse` at `0x459100`

Notes:
Status remains `Partial`. WZ was rechecked first on the same cast-owner surfaces and still supports the split used by this seam: shared cast branches remain authored on `effect`/`special`/`specialAffected`/`keydownend` rows (`Skill/434.img/skill/{4341003,4341007}/special`, `Skill/3212.img/skill/32121003/special`, `Skill/122.img/skill/1220006/{special,info/condition=damaged}`, `Skill/230.img/skill/2301002/specialAffected`, `Skill/532.img/skill/5320007/specialAffected`, `Skill/572.img/skill/5720005/specialAffected`, `Skill/3500.img/skill/35001001/keydownend`, `Skill/3510.img/skill/35101009/keydownend`, `Skill/3510.img/skill/35101004/effect`, `Skill/3511.img/skill/35111004/effect`, `Skill/3512.img/skill/{35121005,35121013}/effect`), while summon-style ownership remains split (`Skill/3510.img/skill/35101005` keeps `info/type=33` with `effect/*` but no `summon/*`; recovered bird rows `Skill/310.img/skill/3101007`, `Skill/311.img/skill/3111005`, `Skill/320.img/skill/3201007`, `Skill/321.img/skill/3211005`, and `Skill/3512.img/skill/35121011` keep `info/type=33` plus concrete `summon/*` branches). Client evidence stayed second: `CUser::ShowSkillEffect` still keeps caller-owned `nActionSpeed`, `bLeft`, `nLast`, and `pPtOffset`; non-melee local callers (`CUserLocal::DoActiveSkill_Heal@0x93a830`, `...TownPortal@0x93be00`, `...SmokeShell@0x93cc30`) still pass `nActionSpeed=0` and `nLast=0x7FFFFFFF`; `CUserLocal::DoActiveSkill_Summon@0x93c0f0` still passes fixed `bLeft=0` with `nLast=0x7FFFFFFF` and null `pPtOffset`; and `CUserLocal::DoActiveSkill_SummonMonster@0x93eff0` still passes fixed `bLeft=0` with `nLast=-1`, seeds `pt.x += 125 * direction`, clamps that point to `m_rcMBR.left + 1` / `right - 1`, accepts an already-owner-aligned clamped point, and only fails when the later 15px back-search crosses the owner without a foothold.

This pass keeps changes in the same seam and tightens placement-owner shaping without widening runtime owners: local non-melee casts remain on the direct-owner registration path (`HandlePlayerSkillCast -> TryRegisterLocalSkillCastThroughClientShowSkillEffectDirectPath`) while explicit caller-shaped requests remain on `OnClientSkillEffectRequested -> TryRegisterAnimationDisplayerLocalSkillUseRequest`; inferred summon-monster fallback still requires concrete summon animation data (actual frames on summon/spawn/attack/hit/remove branches, including variant-frame coverage) instead of non-null wrapper metadata; explicit summon-monster skill-id mapping still covers recovered rows `3101007`, `3111005`, `3201007`, `3211005`, and `35121011`; and `ResolveClientLocalShowSkillEffectSummonMonsterPlacementPositionForTesting(...)` now preserves the client edge where map-boundary clamping lands exactly on the owner position instead of treating that as placement failure.

Focused coverage in `UnitTest_MapSimulator/SharedSkillUseAnimationOwnerParityTests.cs` now also locks the boundary-clamp-onto-owner placement edge, explicit summon-monster id fallback (`3101007`, `3111005`, `3201007`, `3211005`, `35121011`), concrete-branch metadata gating (empty type-33 summon wrappers no longer trigger inferred summon-monster fallback), remote `OnHit` damaged-condition rejection from temporary-state-only hints, fixed-zero `bLeft`, mine owner shaping, and Monster Magnet move-action ownership assertions.

Remaining gap: explicit client skill-id-to-caller mapping for any still-unrecovered `DoActiveSkill_SummonMonster` permutations is incomplete beyond recovered ids `3101007`, `3111005`, `3201007`, `3211005`, and `35121011`; placement-owner path details beyond recovered foothold search, swim/fly bypass, map-boundary clamp, and vertical settle remain approximated for unrecovered per-caller branches; and overlay-parent plus auto-flip ownership remain approximated beyond follow/fixed-facing options.

### 8. Bullet animation-owner parity discovered by follow-up `CAnimationDisplayer` scan

- `CAnimationDisplayer::RegisterBulletAnimation` at `0x455410`
- `CAnimationDisplayer::RegisterMagicBulletAnimation` at `0x455570`
- `CAfterImageBullet::Update` at `0x477aa0`

Notes:
The follow-up IDA scan also exposed a player-projectile presentation owner that was not named anywhere else in the backlog. `RegisterBulletAnimation` allocates a normal `CBullet` animation object with start/end times, source and destination points, target vector, z, bullet-effect UOL, weapon id, and bullet item id, then inserts it into the animation manager's bullet container. `RegisterMagicBulletAnimation` owns the parallel magic-ball path and only creates a bullet object when the resolved ball UOL is non-empty. `CAfterImageBullet::Update` is a deeper bullet subclass path: when afterimage is enabled it creates a child layer from the current bullet layer origin, relocates alpha/origin vectors, copies the source layer canvas into the afterimage layer, animates it, and registers repeat animation state rather than treating afterimage projectile trails as a generic skill effect. This means projectile parity is split one level below `SkillManager.SpawnProjectile`: gameplay/projectile collision can be correct while the client bullet animation owner for normal, magic, and afterimage projectile presentation is still missing.

### 9. Secondary skill animation-owner parity discovered by broad `CAnimationDisplayer` function scan

- `CAnimationDisplayer::RegisterHookingChainAnimation` at `0x45cbd0`
- `CAnimationDisplayer::RegisterChainlightningAnimation` at `0x45d1b0`
- `CAnimationDisplayer::RegisterTeslacoilAnimation` at `0x45d650`
- `CAnimationDisplayer::RegisterFootHoldAnimation` at `0x45a990`
- `CAnimationDisplayer::RegisterMotionBlurAnimation` at `0x45ad00`
- `CAnimationDisplayer::Effect_SkillPrepare` at `0x45b840`
- `CAnimationDisplayer::RegisterPrepareAnimation` at `0x45b420`

Notes:
Status remains `Partial`. WZ was rechecked first for the active-effect branch this row still owns: `Skill/112.img/skill/1121001/info/pullTarget` remains `1` with authored `mob` hook/chain canvases present, `Skill/222.img/skill/2221006/ball/{0,1,2}` remains present for chain-lightning projectile variants, `Skill/3511.img/skill/35111002/summon/attackTriangle` remains present for Tesla, and `Item/Cash/0501/05010044/effect` still publishes `spectrum=1`, `delay=600`, `interval=100`, and `alpha=224` on the eight-digit cash item node (with `follow` absent on this specific item path, so default `0` behavior still applies). Client evidence stayed second: `CUser::SetActiveEffectItem` (`0x8f5500`) still reads `spectrum/follow/delay/interval/alpha`, terminates the previous spectrum `AnimationState`, copies five avatar-layer COM handles into an array (`Face`, `OverFace`, `UnderFace`, `OverCharacter`, `UnderCharacter`), and re-registers motion blur through `CAnimationDisplayer::RegisterMotionBlurAnimation`; `RegisterMotionBlurAnimation` (`0x45ad00`) still stores overlay, layer array, delay, interval, alpha, update-time seed, and animation-state reference in `m_MotionBlur`.

This pass keeps all work in existing seams and tightens both local and remote owner approximation without widening runtime families: local packet-owned active-effect motion blur preserves client-like per-snapshot avatar alignment by anchoring each sampled layer-stack frame to that sample's own assembled-frame offsets (`bounds.Left`, `bounds.Top - feetOffset`) instead of reusing only the registration-time anchor, while still tagging layer stacks in client handle-array order and drawing in stable avatar render order. Local owner capture keeps passive-snapshot-first source precedence, in-range fallback ordering, and move-path duration tails as in-range sample windows. Remote active-effect motion blur keeps the same five-layer snapshot model plus per-layer source metadata (`SourceLayer`, capture-array index), stable simulated layer-handle ids that persist across sampled snapshots for a single active-effect registration, and the same duration-tail-aware movement snapshot sample-time bounds for owner capture. The simulated native lifetime trace now also models the temporary registration argument lifetime visible in `CUser::SetActiveEffectItem` and `RegisterMotionBlurAnimation`: overlay by-value AddRef/release, five-layer `apLayer` temporary capture, copy into `m_MotionBlur`, and caller-array release are recorded separately from persistent owner/snapshot references, with shared native-like handles accumulating/decrementing by simulated handle identity in client copy order. Remote lifecycle ownership remains clear-on-replace/remove or missing-spectrum metadata; sampling still follows authored `interval`; retention/fade still follows authored `delay` and `alpha`; and follow-position/facing behavior remains intact. Local packet `220` routing, local owner clear/re-register semantics, and prepare/hook-chain/foothold/chain-lightning/Tesla secondary-owner seams remain unchanged on this row.

Focused regression coverage in `UnitTest_MapSimulator/ActiveEffectMotionBlurParityTests.cs` now also locks local sample-time owner capture resolution/fallback (including out-of-history fallback), passive-vs-recorded source precedence, packet-owned layer-handle map reuse vs owner-change regeneration, remote movement-snapshot owner capture/fallback, remote layer-handle map reuse, client-ordered five-layer draw-order mapping, linear delay-window alpha fade shaping, duration-tail sample-window acceptance for both local and remote owner capture, shared-handle owner/snapshot ref-count accumulation and release-count decrement, and the new temporary registration argument AddRef/release order for local and remote motion blur. Remaining gap: both local and remote motion-blur owners are still simulator-managed snapshots rather than literal client `IWzGr2DLayer` COM objects with native layer-handle identity and true native `AnimationState` lifetime ownership semantics across the real layer graph, so unmanaged layer identity beyond the simulated handle map, exact COM failure/exception edge cases, and ownership inside the real GR2 layer graph remain approximated rather than 1:1 mirrored.

### 10. Local affected-area attack ownership discovered by broad IDA function scan

- `CAffectedAreaPool::GetInstance` at `0x52a480`
- `CUserLocal::TryDoingMeleeAttack` at `0x91e780`
- `CUserLocal::TryDoingShootAttack` at `0x925a00`
- `CUserLocal::TryDoingMagicAttack` at `0x92a240`
- `CUserLocal::TryDoingBodyAttack` at `0x930710`
- `CUserLocal::DoActiveSkill_MesoExplosion` at `0x942200`

Notes:
Broad IDA xref scan shows local attack owners call into the affected-area pool directly through `CAffectedAreaPool::GetInstance`, not only through packet-owned field handlers. This keeps affected-area parity split across two seams: local skill/attack spawning in the player-skill runtime and packet-owned remote area lifecycle in backlog 5.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Player skill execution parity

The simulator has a generic skill runtime. It does not yet mirror the client's named `CUserLocal` behavior families.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Generic skill casting | The simulator now loads the full player skill catalog from `Skill.wz`, learns active skills into the runtime, supports direct cast invocation from the skill window in addition to hotkey casting, keeps GM/SuperGM skill-book resolution compatible with both `900.img` and `910.img` layouts while focusing those jobs on their own job-book window instead of the global advancement catalog, evaluates Big Bang `common` formulas in IMG-mode data sets so skills like `Power Strike` are treated as castable attacks instead of passive placeholders, mirrors skill effect anchoring/hitbox fallback by facing direction for asymmetric melee effects, sanitizes unsupported SpriteFont glyphs in the skill window so WZ-backed names no longer crash row rendering, and resolves cast SFX from `Sound/Skill.img` so skill use now plays the corresponding `Use` or fallback attack sound through the simulator audio path | This closes the previous "can only use the current job's limited skill book" runtime/UI gap and keeps special-job skill tabs populated and executable across client data variants | `SkillManager.cs`, `SkillLoader.cs`, `SkillUIBigBang.cs`, `MapSimulator.cs`, `UIWindowLoader.cs` |
| Implemented | Buff stat application | Buff logic now applies `PAD`, `PDD`, `MAD`, `MDD`, `ACC`, `EVA`, `Speed`, and `Jump` directly to the player build and cleanly removes them when buffs expire or refresh | Active buff skills now change simulator stats and movement-relevant values instead of only a partial subset | `SkillManager.ApplyBuffStats` |
| Partial | Buff icon / temporary-stat UI parity | Active buffs still drive the WZ-backed status-bar tray with live duration countdowns sourced from `UI/BuffIcon.img`, client-style right-click cancellation still stays on the live buff list, and `UILoader` still normalizes the full `BuffIcon.img` icon surface while preserving known `UI/BuffIcon.img/buff/*` authored names/order. This pass stayed in the same status-bar resolver seam and widened contextual owner cleanup for no-direct families that have no authored payload label. WZ stayed first: the existing mounted transform evidence remains stable (`Skill/3500.img/skill/35001002/common` still authors `emhp`, `emmp`, `epad`, `epdd`, `emdd`, and `prop` without direct `jump`; `String/Skill.img/35001002/desc` still carries non-payload `downward jumps` and `Power Stance` text; `Skill/522.img/skill/5221006/{common,PVPcommon}/x = 1` remains co-authored beside max/direct transform payloads; `String/Skill.img/5221006` still co-authors `Gun Booster`, `Maple Warrior`, and `Hero's Will`; `Skill/1511.img/skill/15111002/PVPcommon` still keeps `PVPdamage`, `epdd`, `emdd`, `speed`, and `jump`; `String/Skill.img/15111002/desc` still co-authors `Knuckle Booster` and `Speed Infusion`; and direct-payload morph rows such as `Skill/1311.img/skill/13111005/common` still publish `morph`, `indiePad`, `epdd`, `emdd`, `speed`, and `jump`). The checked no-direct family evidence is now broader: Magic Guard still stays WZ-first through `Skill/200.img/skill/2001002/info/switchDamtoMP`, `Skill/200.img/skill/2001002/common/x`, and `String/Skill.img/2001002/desc`; Soul Arrow/Spirit Javelin-style rows are represented by `Skill/310.img/skill/3101004/common/x` and `Skill/412.img/skill/4121006/common/x`; Dark Sight keeps authored direct `speed` beside contextual `x/y` at `Skill/400.img/skill/4001003/{common,PVPcommon}`; Invincible publishes `Skill/230.img/skill/2301003/common/x` with `String/Skill.img/2301003` naming the family but no separate payload label; Power Guard publishes `Skill/120.img/skill/1201007/common/{x,y}` with its family name in `String/Skill.img/1201007`; and Meso Guard publishes `Skill/421.img/skill/4211005/common/x` with its family name in `String/Skill.img/4211005`. This pass also checked Battle Mage aura placeholders: `Skill/3200.img/skill/32001003/common/x` is Dark Aura damage, `Skill/3210.img/skill/32101002/common/{x,y}` is Blue Aura damage distribution/absorb, `Skill/3210.img/skill/32101003/common/{x,y}` is Yellow Aura movement/attack speed, `Skill/3212.img/skill/32120000/common/{x,madX}` is Advanced Dark Aura active damage plus passive magic attack, and `Skill/3212.img/skill/32120001/common/{x,y,z,speed,er}` is Advanced Yellow Aura movement/attack speed plus avoidability. The same authored-order model already used for explicit no-direct families now also gates contextual-owner-only cleanup: after WZ-authored payload labels and contextual family owners are resolved, unrelated parsed text-only families are removed before tray family/icon/order selection and before focused tooltip presentation. Client evidence stayed second at `CUIStatusBar::Draw@0x876cd0`, keeping ownership in simulator status-bar resolver paths only. Implementation remains in `SkillManager`: live entry resolution and `ResolveStatusBarBuffTooltipPresentationForParity` now share `RemoveUnauthoredMetadataTemporaryStats`; its contextual cleanup path no longer requires an authored payload-label set before preserving a resolved owner, and `TryResolveContextualFamilyOwnerTemporaryStatLabel` now maps x-only Magic Guard, x-only Soul Arrow, x-only Invincible, payload-less Power Guard `x/y`, payload-less Meso Guard `x`, and Battle Mage Aura `x/y/z` placeholders to their families when the row surface is contextual rather than direct BuffIcon payload, while still mapping Dark Sight `x/y` placeholders to the Dark Sight family when authored `speed` is present. Tooltip authored-label resolution now treats Aura `x` as Speed when a speed payload is active, Aura `y` as Booster when attack-speed payload text is active, and Aura `z` as avoidability/invincible payload when those aura payloads are active, falling back to the Aura family for payload-less aura placeholder rows. Focused regression coverage in `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` now locks the earlier transform cleanup lanes plus non-transform no-direct Bless, Magic Guard, Soul Arrow, Dark Sight, Invincible, Power Guard, Meso Guard, and Battle Mage Aura tooltip/status-entry cleanup for unrelated Holy Symbol, Meso Up, Maple Warrior, Booster, and Dash text-only noise, alongside the prior status-entry and tooltip ordering cases for max/direct, direct speed/jump, mastery-first, prop-first, PVPdamage-first, PVPdamage-only, metadata-only AllStats, Booster, MapleWarrior, DebuffResistance, Jump, Stance, Holy Symbol, and Magic Guard contexts. Remaining gap is narrower but still real: exact tray order/family ownership is now authored for known direct `BuffIcon.img` families, explicit no-direct families, checked contextual placeholders, checked transform direct/max-HP/max-MP contexts, checked transform direct/speed/jump contexts, checked transform mastery-first contexts, checked transform PVPdamage-first and PVPdamage-only contexts, checked prop-first morph contexts, checked tooltip cleanup parity for unauthored direct Speed/Jump metadata noise, checked non-transform contextual no-direct family cleanup for unrelated text-only family mixes, checked x-only Magic Guard, Soul Arrow, and Invincible contextual owner cleanup, checked Power Guard/Meso Guard placeholder owner cleanup, checked Dark Sight contextual owner cleanup around authored speed, and checked Battle Mage Aura x/y/z placeholder owner cleanup plus Yellow Aura speed/booster authored tooltip ordering. Leftover cases without a resolved contextual owner or authored payload label can still depend on unrecovered client/runtime grouping beyond `UI/BuffIcon.img`, recovered `SecondaryStat` order, and currently parsed skill metadata. | Buff-heavy jobs are visible on the HUD, can be cleared without waiting for expiry, and now keep transform, checked non-transform no-direct, x-only Magic Guard/Soul Arrow/Invincible, Power Guard/Meso Guard placeholder, Dark Sight speed-context, and Battle Mage Aura x/y/z placeholder family ownership consistent between live tray entries and tooltip presentation. Remaining risk is concentrated on still-unrecovered client grouping where authored WZ order, recovered `SecondaryStat`, and contextual family mappings do not produce a unique owner. | `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`, `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` (`UI/BuffIcon.img/buff`, `Skill/3500.img/skill/35001002/{common,PVPcommon}`, `Skill/3512.img/skill/35120000/{common,PVPcommon}`, `Skill/522.img/skill/5221006/{common,PVPcommon}`, `Skill/511.img/skill/5111005/{common,PVPcommon}`, `Skill/512.img/skill/5121003/{common,PVPcommon}`, `Skill/1511.img/skill/15111002/{common,PVPcommon}`, `Skill/1311.img/skill/13111005/{common,PVPcommon}`, `Skill/510.img/skill/5101007/{common,PVPcommon}`, `Skill/200.img/skill/2001002/{info/switchDamtoMP,common/x}`, `Skill/310.img/skill/3101004/common/x`, `Skill/412.img/skill/4121006/common/x`, `Skill/400.img/skill/4001003/{common,PVPcommon}`, `Skill/230.img/skill/2301003/common/x`, `Skill/120.img/skill/1201007/common/{x,y}`, `Skill/421.img/skill/4211005/common/x`, `Skill/3200.img/skill/32001003/common/x`, `Skill/3210.img/skill/32101002/common/{x,y}`, `Skill/3210.img/skill/32101003/common/{x,y}`, `Skill/3212.img/skill/32120000/common/{x,madX}`, `Skill/3212.img/skill/32120001/common/{x,y,z,speed,er}`, `String/Skill.img/35001002/desc`, `String/Skill.img/5221006/{desc,pdesc,h,ph}`, `String/Skill.img/15111002/desc`, `String/Skill.img/2001002/desc`, `String/Skill.img/2301003`, `String/Skill.img/1201007`, `String/Skill.img/4211005`, `CUIStatusBar::Draw@0x876cd0`) |
| Partial | Melee / ranged / magic resolution | The runtime keeps the existing player-skill parity work for this seam: WZ `lt/rb` attack bounds stay intact, melee and magic target ordering stay split, no-ball spell and shoot families still route through the client-shaped `info/type` classifier, direct no-projectile ranged skills still use their own fallback hitbox plus the `DoActiveSkill_ShootAttack` melee-first-vs-shoot fallback split, projectile families still honor `rectBasedOnTarget`, `multiTargeting`, `range` chaining, `ballDelay` / `ballDelayN`, authored ball-frame sweep bounds, effective `mobCount` caps from both skill and ball data, WZ `x` damage overrides and client `SKILLENTRY::AdjustDamageDecRate` tables, and the shared shoot-entry / ammo resolver still mirrors the current client-backed weapon-family, support-state, consume-count, USE/CASH ownership, and pellet-family gates. This run stayed in the same shoot-entry ammo selector seam and tightened the client special-pellet branch without widening projectile ownership. WZ was rechecked first and still confirms the authored geometry and mounted anchor surfaces this lane relies on: `Skill/310.img/skill/3101005/info/rectBasedOnTarget = 1`, `Skill/230.img/skill/2301005/info/{multiTargeting,rectBasedOnTarget} = 1`, `Skill/230.img/skill/2301005/common/{lt,rb}`, mounted `map/navel` branches on `character/TamingMob/01932015/wildbeast/{0,5}/0/map/navel` and `character/TamingMob/01932030/wildbeast/{0,5}/0/map/navel`, plus special pellet skill/item surfaces `Skill/521.img/skill/5211004/common/itemConsume = 2331000`, `Skill/521.img/skill/5211005/common/itemConsume = 2331001`, `String/Consume.img/2331000` as Blaze Capsule, `String/Consume.img/2332000` as Glaze Capsule, `Item/Consume/0233.img/{02331000,02332000}` publishing matching pellet item metadata, `Item/Cash/0502/05021000/hit` publishing the cash-bullet hit UOL surface used by the client `Item/Cash/%04d.img/%08d/hit` formatter, and `Character/Afterimage/hit.img` publishing the client fallback hit branches `sword1`/`sword2`/`swordF`, `mace1`/`mace2`/`maceF`, and `anniversary*`. Client evidence stayed second in the same lane: `CUserLocal::TryDoingSmoothingMovingShootAttack@0x92de70`, `CUserLocal::GetProperBulletPosition@0x907310`, `GetShootAttackPt0@0x9039a0`, `CSkill_HitAni::operator()@0x74fc50`, `CSkill_HitAni::GetSubHitAni@0x911a30`, `CSkill_HitAni::CreateForFlashRain@0x6ebef0`, constructor dispatch in `CSkill_HitAni::CSkill_HitAni@0x6eca00`, `CSkill_HitAni::CreateMultipleLayer@0x6ec7d0`, `CSkill_HitAni::CreateFirst@0x6ebd80`, `CSkill_HitAni::CreateDefault@0x6ec010`, and `is_pellet_fire_item@0x861300` / `is_pellet_ice_item@0x861330` remain the owner seams. Existing row behavior stays intact (moving-shoot fire-time origin refresh, mounted body-rel-move fallback, `33121001` base-hit/sub-hit final-line split, no-clamp indexed selectors for `33121009`/`33111006`, first-target split exceptions, authored multi-branch indexed selection, first-target-only `CreateMultipleLayer(hit0)` for `15111004`/`5121007`, and the constructor-owned `CreateFirst(..., 2)` target window for `5101003`). New ammo parity is now explicit and tighter at queued fire time: `ClientShootAmmoResolver` scans the client-owned elemental pellet family before active/generic bullet selection for special pellet skills, maps the ice row through the client-backed `2332` family despite the visible WZ `2331001` consume value, and when the matching elemental pellet is absent falls back only to ordinary non-elemental bullets while rejecting the wrong elemental family. This pass keeps that same normalization through queued execution refresh, so an already-queued Ice Capsule shot still matches the `2332` family at fire time instead of falling back to the visible WZ `2331001`/`2331` family. This run tightens the remaining queued consume seam: refreshed queued shoot entries now mark their ammo selection as client-owned during delayed execution, consume that exact refreshed USE slot instead of re-resolving against the live active/generic bullet list, and preserve queued visual ammo metadata when the queued no-consume bypass is active while still rejecting missing USE ammo when bypass is absent. This run also tightens the target-hit animation variant seam: `CSkill_HitAni` resolution now selects WZ `level/<skillLevel>/hit` branches first, then `CharLevel/<characterLevel>/hit` threshold branches, then the root `hit` branches before applying the existing shuffled/default-random/indexed/sub-hit/first-target/multiple-layer client selectors, and live damage application passes the active skill level and player level into that resolver. This run also tightens the remaining `CSkill_HitAni::CreateDefault` hit-UOL fallback branches: projectile hits backed by resolved CASH ammo now prefer the client cash-bullet hit UOL (`Item/Cash/<family>/<item>/hit`) before authored skill/default weapon fallback, and when authored skill hit data does not produce a renderable hit animation, live damage presentation resolves the client fallback branch from equipped weapon family and current final-action state (`Character/Afterimage/hit.img/sword{1,2,F}`, `mace{1,2,F}`, or recovered anniversary weapon rows) and loads that WZ branch through the existing skill loader instead of dropping the hit effect entirely. Focused coverage in `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` now locks fire/ice pellet preference over active generic bullets, initial wrong-elemental rejection, generic non-elemental fallback, queued WZ-ice refresh against the client `2332` family, queued wrong-elemental rejection after WZ-ice normalization, queued generic non-elemental fallback retention, queued bypass fire-time validation retention without USE ammo, non-bypass queued fire-time rejection when the refreshed USE slot is missing, and target-hit animation variant precedence across skill-level, character-level, root hit-UOL surfaces, client cash-bullet hit-UOL path selection, projectile CASH-only hit override admission, and client default weapon-hit fallback branch selection. Remaining gap stays `Partial` and narrower below this simulator seam: residual cases where live client `m_ptBodyRelMove.y` can still diverge after deeper native layer/runtime ownership updates, post-launch choreography hidden behind native `CSkill_HitAni`/`CMob::AddDamageInfo` internals beyond the now-covered indexed-target/final-line-sub-hit/first-target-split/multiple-layer-first-target/`5101003`-two-target-CreateFirst, level/character hit-UOL variant ownership, cash-bullet hit-UOL fallback, and default weapon afterimage hit fallback set, unrecovered native bullet/pellet consume-side effects beyond the currently mirrored selection, cash-hit presentation lookup, and queued fire-time refresh/bypass gates, and later moving-shoot presentation nudges that depend on unrecovered per-hit timing fields or native layer ownership beyond recovered launch offsets, lane offsets, and skill delay tables. | This row matters because `CUserLocal` keeps separate melee, shoot, and magic execution lanes, and the remaining mismatch is now small enough that future work can stay focused on the last client-owned fire-time/post-launch presentation details instead of reopening projectile family resolution, target ordering, ammo ownership, or basic fallback projectile origin placement again. | `SkillManager.ProcessMeleeAttack`, `SkillManager.ProcessMagicAttack`, `SkillManager.ProcessNoProjectileRangedAttack`, `SkillManager.SpawnProjectile`, `SkillManager.CheckProjectileCollisions`, `SkillManager.ResolveProjectileAttackOrigin`, `SkillManager.ResolveDeferredExecutionAttackOriginOverride`, `SkillManager.RefreshQueuedProjectileFireTimeOrigin`, `SkillManager.ResolveProjectileImpactPresentationBaseTime(...)`, `SkillManager.ResolveClientTargetHitAnimation(...)`, `SkillManager.ResolveClientTargetHitEffects(...)`, `SkillManager.TryResolveClientCharacterLevelTargetHitEffects(...)`, `SkillManager.UsesClientMultipleLayerHitAnimation(...)`, `SkillManager.DrawProjectile`, `SkillManager.ResolveProjectileVisualItemId`, `SkillLoader.ParseSkillInfo`, `SkillLoader.CreateLevelData`, `SkillLoader.LoadPrefixedIndexedSkillAnimations(...)`, `SkillLoader.LoadClientDefaultHitAnimation(...)`, `SkillLoader.LoadClientCashBulletHitAnimation(...)`, `SkillData.MultipleLayerTargetHitEffects`, `ClientShootAttackFamilyResolver`, `ClientShootAmmoResolver`, `SkillManager.ResolveCurrentClientShootAttackVehicleId(...)`, `SkillManager.ResolveSkillSpecificShootAmmoItemId(...)`, `PlayerCharacter.TryGetCurrentBodyRelMoveY(...)`, `PlayerCharacter.TryResolveCurrentRenderedMountedClientBodyRelMoveY(...)`, `PlayerCharacter.TryResolveRenderedMountedClientBodyRelMoveY(...)`, `PlayerCharacter.ResolveMountedBodyRelMoveSourceTamingMobPart(...)`, `PlayerCharacter.TryResolveMountedClientBodyRelMoveY(...)`, `PlayerCharacter.TryResolveCurrentMountedClientBodyRelMoveYFromRawFrames(...)`, `PlayerCharacter.ResolveClientBodyRelMoveY(...)`, `AvatarActionLayerCoordinator.ApplyMountedOriginRelocation(...)`, `AvatarActionLayerCoordinator.ResolveMountedOriginRelocation(...)`, `CharacterAssembler.GetPartAnimation(...)`, `Skill/310.img/skill/3101005/info/rectBasedOnTarget`, `Skill/230.img/skill/2301005/info/{multiTargeting,rectBasedOnTarget}`, `Skill/230.img/skill/2301005/common/{lt,rb}`, `Skill/521.img/skill/5211004/common/itemConsume`, `Skill/521.img/skill/5211005/common/itemConsume`, `Item/Consume/0233.img/{02331000,02332000}`, `Item/Cash/0502/05021000/hit`, `Character/Afterimage/hit.img/{sword1,sword2,swordF,mace1,mace2,maceF,anniversary*}`, `character/TamingMob/01932015/wildbeast/{0,5}/0/map/navel`, `character/TamingMob/01932030/wildbeast/{0,5}/0/map/navel`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::DoActiveSkill_MagicAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::GetProperBulletPosition@0x907310`, `GetShootAttackPt0@0x9039a0`, `CSkill_HitAni::operator()@0x74fc50`, `CSkill_HitAni::GetSubHitAni@0x911a30`, `CSkill_HitAni::CSkill_HitAni@0x6eca00`, `CSkill_HitAni::CreateMultipleLayer@0x6ec7d0`, `CSkill_HitAni::CreateForFlashRain@0x6ebef0`, `CSkill_HitAni::CreateDefault@0x6ec010`, `is_pellet_fire_item@0x861300`, `is_pellet_ice_item@0x861330`, `is_position_up_skill_on_riding@0x9037c0`) |
| Partial | `DoActiveSkill_*` family parity | This pass stays in the same `SkillManager` seam and closes the recovered town-portal action-appointed silent reject in live runtime without widening ownership. WZ was rechecked first: `Skill/231.img/skill/2311002/action/0 = alert2` and beginner `Skill/000.img/skill/0008001/action/0 = alert2` confirm the town-portal rows carry authored appointed action surfaces; existing row-owned WZ surfaces for release-prepared rows (`2121001`, `2221001`, `2321001`, `5311002`), summons, mechanic flamethrower, Wild Hunter swallow, exact prepare weapons, dual-blade subweapon rows, rush predicates, no-root prepare rows, and Tesla-coil request surfaces remain unchanged. Client evidence stayed second: `CUserLocal::DoActiveSkill_TownPortal@0x93be00` checks `SKILLENTRY::IsActionAppointed`, silently returns when `CAvatar::GetOneTimeAction() > -1`, otherwise stores the random appointed action, calls `PrepareActionLayer`, sends packet `103`, then calls `ShowSkillEffect`; existing `DoActiveSkill_Heal`, `DoActiveSkill_Summon`, `DoActiveSkill_SmokeShell`, `DoActiveSkill_Prepare`, `TryDoingPreparedSkill`, `DrawKeyDownBar`, and `is_prepare_bomb_skill` evidence remains in place. Implementation keeps lane precedence, lane-scoped throttle ownership, state/message gates, prepare weapon/ammo StringPool ownership, silent town-portal vec-ctrl/map-category rejects, prepare cooldown/rush predicates, prerequisite rejects, release-prepared sparse fallback rows, and Tesla-coil grouped request bookkeeping in place. The town-portal lane now executes the action-appointed one-time-action gate in the live silent-reject path: action-surfaced Mystic Door and beginner town-portal rows silently reject while another client-owned one-time action is active, without widening that reject to non-town-portal skills or town-portal rows lacking an authored action surface. Focused coverage in `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` now locks the town-portal action-appointed silent reject, beginner town-portal membership, and non-town-portal/no-action negative cases, alongside the existing release-prepared sparse rows, town-portal silent/message split, exact-weapon rows, no-root knuckle rows, ammo-message membership, cooldown/rush membership, metadata-independent prepare fallback, and Tesla-coil target-cover bookkeeping. Remaining gap is narrower and still row-scoped: town-portal full reject matrix remains incomplete beyond currently mirrored field-limit/portal-overlap messages, silent vec-ctrl foothold/ladder, map-category, action-appointed one-time-action branches, and shared request throttle; prepare full branch table still has unrecovered predicates/messages beyond the recovered weapon/ammo/release-lane subset, including deeper one-time/preparing occupancy nuances and native `m_rush` storage/lifetime beyond the explicit `m_rush.nSkillID/tCoolTimeEnd` predicate plus simulator cooldown source; and `DoActiveSkill_Summon` special argument payload permutations beyond grouped Tesla-coil object-id/target-cover request bookkeeping plus deeper native packet/layer choreography remain incomplete. | The simulator keeps explicit non-melee `DoActiveSkill_*` caller-family ownership, mirrors recovered prepare exact-weapon/no-root/release-prepared rows in the same lane table, keeps town-portal client-owned silent reject order more complete, and keeps local Tesla-coil request target-cover ownership grouped across active sibling coil ids. | `SkillManager.cs`, `SkillData.cs`, `SkillLoader.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` (`Skill/231.img/skill/2311002/action/0`, `Skill/000.img/skill/0008001/action/0`, `CUserLocal::DoActiveSkill_TownPortal@0x93be00`, `CUserLocal::DoActiveSkill_Heal`, `CUserLocal::DoActiveSkill_Summon`, `CUserLocal::DoActiveSkill_SmokeShell`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::DrawKeyDownBar`, `is_prepare_bomb_skill`, `CAnimationDisplayer::RegisterTeslacoilAnimation`) |
| Partial | Shadow Partner render/action parity | Shadow Partner remains on the dedicated helper seam with WZ-backed `special/*` catalogs for `4111002`, `4211008`, and `14111000`, helper-first action resolution, shared create/attack blocking hold, raw-action-aware local/remote offsets (`+/-30`, back lane `+50`, `200 ms`), shared under-face insertion ownership, and WZ-backed supported-source admission. WZ was rechecked first: the mounted `special/*` helper branches remain stable (`4111002` and `4211008` expose the same 43 helper branches including create variants; `14111000` exposes the same 34 non-create helper branches), `Character/00002000.img/{create0,create1,create2,chargeBlow,rain}*` timing rows remain stable, and the thief/Night Walker source action surface still resolves to the recovered gate rows (`4001003=darksight`, `4101003/4101004=alert2`, `4111001=alert4`, `4111003=swingO1`, `4111005=avenger`, `4111007=alert2`, `4121000/4121004=alert3`, `4121003=showdown`, `4121008=ninjastorm`, `4201002/4201003=alert2`, `4201004=swingO2`, `4201005=savage`, `4211002=assaulter`, `4211003/4211005=alert4`, `4211006=prone2`, `4211007=alert2`, `4221000/4221004=alert3`, `4221003=showdown`, `4221007=alert5`, `4221001=assassination`, `4221006=smokeshell`, `14001003=darksight`, `14001005=alert2`, `14101002/14101003=alert2`, `14101006=vampire`, `14111001=swingO1`, `14111002=avenger`). Client evidence stays second: `CActionMan::Init@0x41beb0` still walks raw actions `0..272`, skips raw action `55`, redirects raw `124..131` to child row `1`, and reads the modeled piece fields before runtime playback; `CActionMan::LoadShadowPartnerAction@0x423cf0` remains the helper-action loader seam; `CUser::PrepareActionLayer@0x8e3070` still calls `PrepareShadowPartnerActionLayer` before mirror/additional layer updates. This pass keeps ownership in the existing loader/resolver seams and tightens source-action admission at the shared enumerator boundary too: character-owned mounted candidates and mounted WZ/client-init candidates now suppress unsupported canonical helper rows before callers synthesize, rather than relying only on downstream call-site filters. Existing first-job/lower-job source ids, create fallback, create blocking hold, attack retry/refresh, raw back-action/morph/ghost activation offset seeding, remote under-face insertion, alpha clamp, broad WZ-parsed mounted non-ghost helper row intake, and client-owned/character-owned/mounted/fallback synthesis gates remain intact. Focused coverage in `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs` now also locks candidate-enumerator gating for unsupported-vs-supported `darksight`/`alert2`; `ShadowPartnerAttackObservationGateParityTests.cs` continues covering offset, insertion, render-gate, and hold behavior. Remaining gap is narrower but still open: client-only helper rows that are neither visible in `special/*` nor structurally recoverable from mounted `Character/00002000.img` remain unrecovered, and exact ordering/lifetime relative to native `IWzGr2DLayer` under-face objects still approximates client layer-object choreography beyond the shared insertion-index seam, fully-applied source-action gate, and raw-action-aware activation-offset seam. | Shadow Partner parity remains concentrated in the dedicated helper loader/resolver path. This pass removes another source-admission drift by moving the recovered-source gate into shared candidate enumeration as well as synthesis, so future mounted/client-init loader paths cannot enumerate unsupported canonical helper rows such as `darksight`/`alert2` for skills whose WZ-backed source-action set excludes them. Remaining mismatch is hidden client-only rows plus native GR2 layer-object ordering/lifetime semantics. | `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `PlayerCharacter.cs`, `RemoteUserActorPool.cs`, `ShadowPartnerActionLoaderParityTests.cs`, `ShadowPartnerAttackObservationGateParityTests.cs` (`Skill/400.img/skill/4001003/action/0`, `Skill/410.img/skill/{4101003,4101004}/action/0`, `Skill/411.img/skill/{4111001,4111003,4111005,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121003,4121004,4121008}/action/0`, `Skill/420.img/skill/{4201002,4201003,4201004,4201005}/action`, `Skill/421.img/skill/{4211001,4211002,4211003,4211005,4211006,4211007}/action/0`, `Skill/422.img/skill/{4221000,4221001,4221003,4221004,4221006,4221007}/action/0`, `Skill/1400.img/skill/{14001003,14001005}/action/0`, `Skill/1410.img/skill/{14101002,14101003,14101006}/action/0`, `Skill/1411.img/skill/{14111001,14111002}/action/0`, `Character/00002000.img/{create0,create1,create2,chargeBlow,rain}*`, `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, `Skill/1411.img/skill/14111000/special/*`, `CActionMan::Init@0x41beb0`, `CActionMan::LoadShadowPartnerAction@0x423cf0`, `CUser::PrepareActionLayer@0x8e3070`) |
| Partial | Repeat-skill sustain families | Updated in this pass: WZ was rechecked first and still publishes SG-88 as `Skill/3512.img/skill/35121003` with `info/type = 33`, `info/minionAttack = normal`, `info/specialGage = 1`, and `summon/attack1`, while tank-siege still publishes `Skill/3512.img/skill/35121013` with `info/fixedState = 1`, `info/onlyNormalAttack = 1`, `common/time = "5"`, and `effect/*/delay = 30/60/30/60`; `Skill/3511.img/skill/35111004/common/time = "5"` still matches the siege timer surface. Client evidence stayed second: `CUserLocal::TryDoingRepeatSkill` (`0x93d400`) still owns SG-88/tank-siege sustain admission, `CUserLocal::SendSkillEffectRequest` (`0x93aeb0`) still emits opcode `71` (`COutPacket(71)` + `Encode4(skillId)` + `Encode1(level)` + `Encode1(sendLocal)`), tank-siege mode-end still routes through `SendSkillEffectRequest(35120013, learnedLevel(35121013), 1)` after the 5s timer before repeat ownership returns to `35121005`, and `CUserLocal::DoActiveSkill_Summon` (`0x93c0f0`) still writes SG-88 first-use opcode `103` with `(requestTime, skillId, skillLevel, x, y, m_nMoveAction & 1, vecCtrl-owner byte)`. The SG-88 first-use transport seam remains on the same owner path (`SkillManager` -> `PacketOwnedMechanicRepeatSkillRuntime` -> `MapSimulator` authority transport), and this pass tightens capture-validation further without widening gameplay ownership: mismatch-pair intake now accepts dashed pair tokens and decimal byte deltas (while normalizing to canonical `byteN:0x??->0x??` form), replay decode-detail intake continues to resolve vec-ctrl mismatch classes (`highBitsOnly`, `lowBitChanged`, `zeroToNonZero`, `nonZeroToZero`) from dashed/shorthand and JSON payloads, replay classification now promotes vec-ctrl-only deltas into explicit classes (`mismatch:vecCtrlHighBitsOnly`, `mismatch:vecCtrlLowBitChanged`, `mismatch:vecCtrlZeroToNonZero`, `mismatch:vecCtrlNonZeroToZero`) with mismatch-pair evidence first and decode-detail fallback second, and replay-matrix output continues carrying `vecCtrlMismatch` histograms beside `moveActionMismatch` across source/state/equivalence/core-equivalence rows while preserving `sourceRows`, `officialSourceRows`, and `officialCaptureParity` split reporting. This pass additionally hardens real-capture alias intake without changing gameplay ownership: mismatch-pair field parsing now accepts client/decompile-style `m_nMoveAction`/`nMoveAction` labels and `m_pVecCtrl`/`m_pVecCtrlOwnerByte` labels, JSON pair extraction now accepts value aliases such as `officialValue`, `clientValue`, `capturedValue`, `simulatorValue`, `generatedValue`, and `replayValue`, pair-container recursion now admits `byteComparisons`/`comparison` containers used by exported capture diff tooling, and raw/payload byte comparison intake now accepts clearly decimal string byte lists such as `103,0,...`, `[103, 0, ...]`, `Uint8Array(...) [...]`, or `byte[] {...}` without misreading them as hex text. Prior capture hardening remains intact (JSON whitespace mismatch-field lists, explicit nested-object mismatch/matched/status gating, slash/bracket field-path normalization, shorthand alias mapping for request/position/move/vec fields, and false-like assignment filtering). Focused coverage in `UnitTest_MapSimulator/RepeatSkillSustainPacketTransportParityTests.cs` now also locks client-named move-action/vec-ctrl field-pair parsing, JSON value-alias normalization, `byteComparisons`/`comparison` container intake, decimal string raw/payload byte comparison intake for root, wrapper-labeled, and participant-array capture shapes, and combined replay-class promotion from those client-named aliases, in addition to mismatch-pair decimal/dashed intake normalization, vec-ctrl expanded replay-class promotion for both mismatch-pair and decode-detail fallback paths, singular-label, dashed/shorthand, boolean-map, whitespace-list, nested-object signal gating, and decode-detail backfill assertions. The row remains partial because official captured SG-88 first-use traffic is still required to confirm exact vec-ctrl-byte semantics across broader live-state coverage, to verify whether move-action mismatches appear in real traffic beyond replay noise, and to validate the expanded parity-class/equivalence behavior against larger real-session capture sets. | Mechanic repeat sustain still mirrors recovered opcode-`71` effect branches, keeps tank-siege return-owner timing, and sends SG-88 first-use through the existing transport seam with client-shaped opcode-`103` field order plus raw-action low-bit ownership; this pass further hardens replay mismatch analytics with canonicalized mismatch-pair intake, client/decompile-style field/value aliases, comparison-container intake, and explicit vec-ctrl-only replay classes while keeping source-separated official-session parity reporting and state/equivalence/core-equivalence normalization on observed opcode-`103` captures. Remaining risk stays concentrated on capture-backed confirmation of SG-88 first-use vec-ctrl and move-action semantics and broader real-session replay grouping behavior after accepting the currently observed alias, comparison-container, raw/payload packet, hex dump, base64, JSON-array, decimal string byte-list, and common decimal byte-wrapper capture shapes. | `SkillManager.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedSummonedParity.cs`, `Managers/PacketOwnedMechanicRepeatSkillRuntime.cs`, `Managers/SummonedOfficialSessionBridgeManager.cs`, `Physics/CMovePathClientPacketCodec.cs`, `Physics/CVecCtrl.cs`, `UnitTest_MapSimulator/RepeatSkillSustainPacketTransportParityTests.cs` (`CUserLocal::TryDoingRepeatSkill`, `CUserLocal::DoActiveSkill`, `CUserLocal::DoActiveSkill_Summon`, `CUserLocal::SendSkillEffectRequest`, `CMovePath::Encode`, `Skill/3512.img/skill/35121003/{info,summon}`, `Skill/3512.img/skill/35121013/{common,info,effect}`, `Skill/3511.img/skill/35111004/common`) |
| Partial | Bound-jump, rocket-booster end, and smoothing moving-shoot branches | This pass stays in the same moving-shoot / Rocket Booster end / bound-jump seams and tightens the direct bound-jump presentation request after the prior Rocket Booster recovery payload, impact attribute, owner-gate, and deferred moving-shoot recorded-state fixes. WZ evidence remains anchored to the same v95 rows rechecked for this seam: `Skill/3500.img/skill/35001001/action/0 = flamethrower`, `Skill/3510.img/skill/35101009/action/0 = flamethrower2`, `Skill/3510.img/skill/35101004/action/0 = rbooster_pre` with `info/type=10`, `info/casterMove=1`, `info/landingEffect=areaAttack`, six Rocket Booster `hit` children plus `common/{lt,rb}` under the same row, `33121009` publishing `info/type=2`, `info/movingAttack=1`, and `info/rapidAttack=1`, and the constrained type-40 bound-jump profile set staying separate from non-type-40 jump-available rows. Client evidence stays on the same recovered addresses: `CUserLocal::TryDoingSmoothingMovingShootAttack@0x92de70` routes delayed moving-shoot through one local `CAntiRepeat::TryRepeat` object with the explicit `33121009` bypass, `CAntiRepeat::TryRepeat@0x9032b0` owns only X/Y/repeat-count/count-limit state, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare@0x912f90` keeps one-time-action gating before arming moving-shot state, `CUserLocal::TryDoingRocketBoosterEnd@0x93d2d0` gates touchdown on one-time-action completion, clears `m_nRocketBoosterVY`, runs the foothold-checked landing attack, then sends `SendSkillEffectRequest(35100004, SkillLevel(35101004), 0)` after the 500 ms recovery window before clearing Rocket Booster, and `CUserLocal::DoActiveSkill_BoundJump@0x93fdf0` keeps the recovered direct-owner id set (`11101005`, `33001002`, `4111006`, `4211009`, `4321003`, `14101004`, `35101004`) with shared non-direct impact lanes, recovered `CVecCtrl::SetImpactNext` move-path attributes `20` for bound-jump lanes and `21` for Rocket Booster launch, followed by `ShowSkillEffect(pSkill, nSLV, 6, 0, 0x7FFFFFFF, 0)` on successful direct-owner launches. Implementation keeps constrained type-40 bound-jump ownership centralized in `BoundJumpParityProfile`, keeps sparse moving-shoot ownership hardening in `SkillManager`, keeps recorded non-bypass deferred moving-shoot execution preserving the carried anti-repeat position and repeat count, and keeps the `33121009` bypass from tracking or carrying anti-repeat state. Rocket Booster landing-area admission and recovery-effect admission continue to share the recovered `35101004` plus `landingEffect=areaAttack` owner helper, the recovery-effect request carries the live Rocket Booster skill level on the `35100004` request, bound-jump/Rocket Booster launch impact requests stamp the recovered local move-path attributes, and direct bound-jump plus Rocket Booster launches now emit the recovered local `ShowSkillEffect` request with live level, `nActionSpeed=6`, and `nLast=0x7FFFFFFF` while preserving sparse metadata admission and rejecting wrong landing-effect text or non-Rocket-Booster rows. Focused source coverage in `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` now also locks direct bound-jump/Rocket Booster launch effect request action speed, level, and direct-owner gating alongside the Rocket Booster recovery request level helper, Rocket Booster move-path attribute helper, bound-jump direct owners, constrained type-40 sparse rows, non-type-40 exclusions, moving-shoot bypass and carry helpers, anti-repeat thresholds and repeat counts, Rocket Booster touchdown and landing-area/recovery-effect admission, and shared-impact fallback coverage. Remaining gap stays row-scoped and narrower: native `CAntiRepeat` lifetime details outside the modeled deferred execution and recorded-state seam are still not fully recovered, packet/server-owned Rocket Booster end choreography beyond local landing-clear, recovery-effect owner/level request, landing-area admission, local impact attribute stamping, and direct launch effect request remains incomplete, and unrecovered bound-jump special-case owners outside the direct-owner switch set plus constrained checked type-40 profile rows remain open. | This row matters because the simulator now keeps the local moving-shoot anti-repeat object alive across recorded non-bypass executions, applies the recovered Rocket Booster owner gate to both landing attack and recovery effect, carries the live `35101004` level on the `35100004` recovery request, stamps recovered bound-jump/Rocket Booster local impact attributes, and emits the recovered direct-launch skill effect request, reducing remaining mismatch to native lifetime/choreography details rather than the simulator seams already modeled. | `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`, `HaCreator/MapSimulator/Character/Skills/SkillData.cs`, `HaCreator/MapSimulator/Character/Skills/BoundJumpParityProfile.cs`, `HaCreator/MapSimulator/Character/Skills/PlayerSkillStateRestrictionEvaluator.cs`, `HaCreator/MapSimulator/Character/Skills/SkillLoader.cs`, `HaCreator/MapSimulator/MapSimulator.AnimationDisplayerParity.cs`, `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` (`CUserLocal::TryDoingSmoothingMovingShootAttack@0x92de70`, `CAntiRepeat::TryRepeat@0x9032b0`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare@0x912f90`, `CUserLocal::TryDoingRocketBoosterEnd@0x93d2d0`, `CUserLocal::DoActiveSkill_BoundJump@0x93fdf0`, `Skill/3500.img/skill/35001001`, `Skill/3510.img/skill/{35101004,35101009}`, `Skill/3312.img/skill/33121009`) |
| Implemented | Key-down / charge skills | This row stays confined to the same prepared-skill owners: `SkillManager`, `PreparedSkillHudRules`, `MapSimulator.RemoteUsers`, and `RemoteUserActorPool`. WZ was rechecked first in the mounted export: Piercing (`Skill/322.img/skill/3221001`) still publishes the authored startup split with `prepare/time = 1200`, `action/0 = piercing`, and the separate client gauge cap of `900 ms`; Monkey Wave (`Skill/531.img/skill/5311002`) still publishes `prepare/time = 1080`, `prepare/action = noiseWave_pre`, `keydown/action = noiseWave_ing`, and `action/0 = noiseWave`; the Big Bang trio, Evan breath pair, Dual Blade charge pair, Screw Punch, Shark Wave, Poison Bomb, Grenade, and Wild Hunter Swallow still publish zero-startup prepare surfaces such as `prepare/time = 0` with authored prepare actions, so those families belong on the immediate hold/release lane instead of a simulator-owned pseudo-startup. Client evidence stayed second: `get_max_gauge_time` (`0x50ab00`) still hard-caps the recovered branch table at `900 ms` for Piercing and Swallow, `1080 ms` through the Monkey Wave authored window, `600 ms` for Final Cut, `1200 ms` for Monster Bomb, `500 ms` for Evan breath, `1000 ms` for the Big Bang trio / Screw Punch / Shark Wave / Poison Bomb / Grenade, and the default `2000 ms` keydown fallback; `CUserLocal::OnKeyDownSkillEnd` (`0x9301b0`) still clamps elapsed charge time to the shared `30 ms` minimum, caps it through `get_max_gauge_time`, routes release through magic / shoot / melee / HP-cost / skill-specific branches, keeps Final Cut HP cost as `elapsed * nX / get_max_gauge_time` before comparing that percentage cost against current HP, and still makes `CUserLocal::TryDoingSwallowAbsorb` (`0x93af50`) reject undercharged Swallow releases until the elapsed hold reaches the full `900 ms` gauge cap. The runtime seams now cover the full scoped row: `SkillManager.ResolvePreparedSkillStartupDuration` uses authored WZ prepare time only when a release-owned family really has a nonzero startup window, `BeginPreparedSkill` enters hold immediately for zero-startup keydown families, `PreparedSkillHudRules.ResolvePreparedGaugeDuration` and `ResolveReleaseChargeElapsedMs` prefer the shared HUD or `get_max_gauge_time` charge window over longer authored prepare splits, the release damage-scale and Final Cut HP-cost helpers stay on the shared linear undercharge seam, `PreparedSkillHudRules.CanExecuteReleasePayload` keeps Wild Hunter Swallow on the client full-gauge-only release gate, `SkillManager.ReleasePreparedSkill` routes failed release-owned keydown attempts back through the shared cancel-request seam instead of silently dropping them, `PreparedSkillHudRules.ResolveRemotePreparedSkillPhases` preserves both zero-startup auto-hold and authored startup-then-hold packet phases, `RemoteUserActorPool.TryResolvePreparedSkillPhase` preserves both remote phase shapes across prepare and hold time, and `RemoteUserActorPool.TryApplyThrowGrenade` clears the matching remote prepared Grenade state on the same release seam as the official grenade packet instead of leaving the HUD bar latched after release. Focused coverage in `UnitTest_MapSimulator/PreparedSkillChargeStartupParityTests.cs` now covers the authored startup seam across the Big Bang trio, Piercing, Screw Punch, Grenade, Monkey Wave, Poison Bomb, Shark Wave, Evan breath, Dual Blade charge pair, and Wild Hunter Swallow; local zero-startup immediate hold; shared client minimum/cap release elapsed rules; the Swallow full-gauge release gate; failed-release cancel routing for the local Swallow branch; shared linear undercharge scaling where the client evidence supports it; the gauge-window-vs-prepare-window release scaling seam; Final Cut scaled HP-cost / restriction behavior; remote zero-startup phase resolution and persistence; remote authored startup-to-hold phase resolution and transition; remote release-owner recognition for the zero-startup client charge table; and remote grenade prepared-state teardown. | The scoped key-down / charge parity row is now implemented inside the existing prepared-skill seams. Remaining client-owned concerns such as raw scan-code ownership, broader exclusive-request policy, or unrelated packet choreography stay outside this row rather than as open gaps in the prepared startup/gauge/release behavior covered here. | `PreparedSkillHudRules.cs`, `SkillManager.cs`, `MapSimulator.RemoteUsers.cs`, `PlayerInput.cs`, `PlayerManager.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `UnitTest_MapSimulator/PreparedSkillChargeStartupParityTests.cs` (`Skill/{212,222,232}.img/skill/*/info/chargingSkill`, `Skill/322.img/skill/3221001/{prepare/time,prepare/action,keydown,action/0,info}`, `Skill/510.img/skill/5101004/prepare`, `Skill/520.img/skill/5201002/prepare`, `Skill/531.img/skill/5311002/{prepare,keydown,action}`, `Skill/1411.img/skill/14111006/prepare`, `Skill/1510.img/skill/15101003/prepare`, `Skill/2212.img/skill/22121000/{prepare,common,info/chargingSkill}`, `Skill/2215.img/skill/22151001/{prepare,common,info/chargingSkill}`, `Skill/434.img/skill/{4341002,4341003}/{prepare,action,info/chargingSkill}`, `Skill/3310.img/skill/33101005/prepare`, `get_max_gauge_time`, `is_keydown_skill`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::OnKeyDownSkillEnd`, `CUserLocal::TryDoingSwallowAbsorb`, `CUserLocal::DrawKeyDownBar`, `CDragon::DrawKeyDownBar`) |
| Partial | Swallow, mine, cyclone, and sit-down healing branches | This pass remains in the same swallow, mine, cyclone, and sit-down-healing seams and tightens the remaining swallow packet-outcome depth without widening classification or runtime owners. WZ evidence was rechecked first and remains anchored to the authored shape: `Skill/3310.img/skill/33101005/prepare/action=swallow_loop`, `Skill/3310.img/skill/33101005/info/dummyOf=33101006&&33101007`, swallow flags only on `Skill/3310.img/skill/33101006/info/swallow` and `Skill/3310.img/skill/33101007/info/swallow`, no swallow flag on unrelated dummy-linked families (`Skill/3200.img/skill/32001001/info/swallow`, `Skill/3211.img/skill/32111005/info/swallow`, `Skill/3510.img/skill/35100008/info/swallow` absent), Healing Robot owner gates at `Skill/3511.img/skill/35111011/info/{type=33,minionAbility=heal,condition=whenUserLieDown}`, and Healing Robot authored heal range at `Skill/3511.img/skill/35111011/summon/heal/info/range` (`lt=-125,-100`, `rb=125,0`). Client evidence stays second in the same seams: `CUserLocal::TryDoingSwallowMobWriggle@0x93f500`, `TryDoingMine@0x907d70`, `TryDoingCyclone@0x932d60`, `TryDoingSitdownHealing@0x903d60`, `CUserPool::DoHealNearHealingRobot@0x94bb20`, and `CSummoned::TryDoingHealingRobot@0x74b3e0`. `SkillLoader` still avoids broad `ClientInfoType=98` fallback expansion, and swallow-family marking still follows the bounded dummyOf graph (max depth 8, cycle guarded) from the visible swallow action root to authored downstream `info/swallow` leaves, marking only the visible root and authored swallow leaves while rejecting intermediate or unrelated dummy links. Runtime swallow-family request matching still uses the same bounded authored-leaf traversal before buffering/consuming packet outcomes, and the outcome buffer now preserves repeated same-skill/same-target swallow-family packet outcomes as separate FIFO entries instead of replacing the older entry. This keeps repeated packet outcomes consumable first-in-first-out, keeps capacity eviction on the oldest buffered outcome, and preserves non-matching swallow-family outcomes while another swallow state is active. Prior row behavior remains intact: mine, cyclone, and sit-down-healing gates stay in their landed client-owned branches; Healing Robot request building resolves the WZ heal/support branch before action encoding, emits client action 13 independent of stale playback branch state, keeps send-side facing `(moveAction << 7) | 13`, and ignores receive-side high-bit facing. Focused coverage now asserts recursive dummyOf swallow matching, cycle-bounded traversal, loader marking of only authored downstream swallow leaves, repeated same-seam FIFO buffering, capacity FIFO eviction, mismatch buffering while armed, and Healing Robot action-13 branch independence. | Remaining mismatch is narrower and still row-scoped: exact one-for-one swallow packet choreography beyond bounded authored dummyOf-family matching and repeated FIFO outcome preservation is still incomplete, and full Healing Robot request/packet choreography beyond current local poll order, branch-owned action-13 send request, authored range ownership, and packet-owned suspend-window seams remains incomplete. | `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`; `HaCreator/MapSimulator/Character/Skills/WildHunterSwallowAbsorbOutcomeBuffer.cs`; `HaCreator/MapSimulator/Character/Skills/SkillLoader.cs`; `HaCreator/MapSimulator/Character/Skills/SummonRuntimeRules.cs`; `HaCreator/MapSimulator/Pools/SummonedPool.cs`; `UnitTest_MapSimulator/WildHunterSwallowAbsorbOutcomeBufferParityTests.cs`; `UnitTest_MapSimulator/WildHunterSwallowAbsorbOutcomeParityTests.cs`; `UnitTest_MapSimulator/SummonRuntimeActionParityTests.cs` (`CUserLocal::TryDoingSwallowMobWriggle@0x93f500`, `CUserLocal::TryDoingMine@0x907d70`, `CUserLocal::TryDoingCyclone@0x932d60`, `CUserLocal::TryDoingSitdownHealing@0x903d60`, `CUserPool::DoHealNearHealingRobot@0x94bb20`, `CSummoned::TryDoingHealingRobot@0x74b3e0`, `Skill/3310.img/skill/33101005/{prepare/action,info/dummyOf}`, `Skill/3310.img/skill/3310100{6,7}/info/swallow`, `Skill/3200.img/skill/32001001/info/swallow`, `Skill/3211.img/skill/32111005/info/swallow`, `Skill/3510.img/skill/35100008/info/swallow`, `Skill/3511.img/skill/35111011/{info,summon/heal/info/range}`) |
| Partial | Summon simulation | The simulator keeps the broader packet-owned summon parity already landed here (lifecycle/timers, WZ attack-branch metadata loading, projectile + target-impact retention, Tesla triangle handling, self-destruct prepare/die, packet-owned prepare/attack visuals, taunt-only puppet registration, shared local/packet-owned summon rule selection, delayed bird tile handoff, `4111007`/`4211007` reactive-chain playback, hidden-hit source-token normalization/recovery, client-shaped `*HitUOL*` alias recovery, numeric relative-UOL sequence retention, hidden tile/ball UOL normalization, hidden tile-UOL variant row-field retention, hidden ball/flipBall variant-only projectile retention, and client-named `CHARLEVELDATA`/`LEVELDATA` hidden tile/ball/flipBall variant intake). WZ was rechecked first on the same row-owned surfaces and still confirms visible tile/ball branches plus frame-owned hit source chains while exposing no authored `sTileUOL`/`tileUOL` names: `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, `Skill/312.img/skill/3120010/{ball,tile}`, `Skill/411.img/skill/4111007/{summon,ball}`, `Skill/421.img/skill/4211007/{summon,ball}`, `Mob/2400011.img/attack1/info/hit/{0..11}/source`, mounted numeric UOL frames `Mob/2400018.img/attack1/17 -> ../stand/0` and `Mob/3220000.img/attack1/18 -> ../stand/0`, and mixed authored `Mob/2400110.img/attack1/{info/hit,0..11}/source`. Client evidence stayed second on `CMob::GetAttackInfo@0x641330`, `CSummoned::OnHit@0x74bc80`, `SKILLENTRY::GetTileUOL@0x437180`, and `SKILLENTRY::GetBallUOL@0x569390`: attack info still stores client-owned `sHit`, summon-hit presentation still consumes that stored path with attach/facing metadata before `Effect_General`, tile lookup stays on stored `sTileUOL`, and ball lookup stays on char-level first, skill-level second, then root stored `sBallUOL`/`sFlipBallUOL` fallback with the observed client `nCharLevel`/`nSLV` inputs. This pass stays in the existing `MobData` extraction, `SummonedPool` consumption, and `SkillLoader` client-UOL intake seams without adding runtime owners: the structured `MobData` row parser keeps adjacent-field record bounds on the parsed segment collection, and hidden/client-extracted tile/ball/flipBall rows now also retain client-exported `CHARLEVELDATA/<level>/...`, `LEVELDATA/<level>/...`, and row-field `charLevelData` variant ownership in addition to the prior `characterLevel`/`charLevel`/`nSLV`/`slv`/`skillLevel`, encoded row-field, JSON-style owner/variant/path, tuple-value, structured text, and variant-only dictionary forms. The row remains partial because full one-for-one synthesis of every hidden `MobAttackInfo::sHit` shape is still not recovered, exact upstream population of stored `sTileUOL`/`sBallUOL`/`sFlipBallUOL` values is still unresolved beyond accepted absolute/encoded/assignment/relative/path-leaf, row-field variant, and client-named char-level/level-data forms, hidden tile/ball-family construction beyond observed authored nodes and client-extracted variant rows is still incomplete, literal GR2/layer ownership and textured bolt choreography remain unresolved, and official `CUserPool` transport/ownership decode for `/summonedpacket` loopback/session bridge seams is still incomplete. | Packet-owned summon hit playback and tile/ball selection now keep hidden variant-only projectile rows alive when client extraction uses native-looking `CHARLEVELDATA` or `LEVELDATA` containers/fields for char-level and skill-level ball, flipBall, and tile UOLs. Existing lifts remain intact for packet family `0x116-0x11B`, retained live attack-frame reuse, frame-scoped attach/facing behavior, source alias/root-UOL recovery, numeric field-name and tuple-value `sHit` tables, delayed bird tile overlay, and `4111007`/`4211007` reactive chain. Remaining mismatch risk is concentrated in deeper hidden `sHit` synthesis breadth, true stored tile/ball/flipBall provenance beyond observed and client-named row variants, GR2/layer choreography, and incomplete official transport decode. | packet-owned summon runtime (`CSummonedPool::OnPacket`, `CSummoned::OnAttack`, `CSummoned::OnHit`, `CSummoned::Hit`, `SkillManager.cs`, `SummonedPool.cs`, `PacketOwnedSummonUpdateRules.cs`, `SummonClientPostEffectRules.cs`, `SkillLoader.cs`, `SkillLoader.ResolveClientSkillAssetUolPathResolution(...)`, `SkillLoader.TryReadClientSkillAssetUolTableEntryVariantLevel(...)`, `SkillLoader.TryReadClientSkillAssetUolRecordVariantLevel(...)`, `SkillData.cs`, `ZoneEffectData`, `ProjectileData`, `LifeLoader.cs`, `LifeLoader.BuildAttackInfoMetadata(...)`, `MobData.GetMobAttackHitEffectPath(...)`, `MobData.GetMobAttackHitEffectNumericIndexedNameValueSequencePath(...)`, `MobData.TryReadMobAttackHitEffectNumericIndexedNameValue(...)`, `MobData.GetMobAttackHitEffectRecordValuePaths(...)`, `MobAnimationSet.cs`, `MobAnimationSet.GetAttackHitEffectEntry(...)`, `SummonedPool.EnumeratePacketMobAttackGeneralEffectCandidateUols(...)`, `SummonedPool.TryResolvePacketMobAttackGeneralEffectSourceSequenceRootPath(...)`, `UnitTest_MapSimulator/PacketOwnedSummonHitParityTests.cs`, `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs`, `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, `Skill/312.img/skill/3120010/{ball,tile,summon}`, `Skill/411.img/skill/4111007/{summon,ball}`, `Skill/421.img/skill/4211007/{summon,ball}`, `Mob/2400011.img/attack1/info/hit/{0..11}/source`, `Mob/2400110.img/attack1/{info/hit,0..11}/source`, `Mob/2400018.img/attack1/17`, `Mob/3220000.img/attack1/18`, `SKILLENTRY::GetTileUOL@0x437180`, `SKILLENTRY::GetBallUOL@0x569390`, `CMob::GetAttackInfo@0x641330`, `CSummoned::OnAttack@0x753340`, `CSummoned::OnHit@0x74bc80`, `CAnimationDisplayer::Effect_General@0x455d10`, `CAnimationDisplayer::LoadLayer@0x451b10`) |
| Partial | Remote summoned-pool packet parity | The simulator still keeps packet-owned summon transport in `SummonedPool`; this pass narrows hidden hit-effect fallback shaping without widening ownership. WZ was rechecked first on the mounted `Mob` seams: `Mob/2400011.img/attack1/info/hit/*/source -> Mob/2400008.img/attack1/info/hit/*`, `Mob/2400110.img/hit1/0/source -> Mob/2400017.img/hit1/0`, and numeric UOL families beyond the prior single sample (`Mob/3220000.img/attack1/18 -> ../stand/0`, plus `Mob/2400018.img/attack1/17..20 -> ../stand/0..3`). `Mob/3220000.img/attack1/18` still resolves through the mounted UOL resolver to `Mob/3220000.img/stand/0`. The skill-side tile seam was also rechecked: mounted skill search still exposes no authored `sTileUOL`/`tileUOL` property names, while the confirmed bird-tile families keep visible tile branches such as `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, and `Skill/312.img/skill/3120010/tile`. Client evidence stayed second: `CSummoned::OnHit@0x74bc80` still reads decoded attack/template metadata, routes non-empty `MobAttackInfo::sHit` through `Effect_General`, and honors decoded attach flags rather than requiring a live mob actor; `SKILLENTRY::GetTileUOL@0x437180` still keeps tile lookup as a client-owned stored-UOL getter before runtime post-attack overlay playback. Inside the existing loader/live-mob/mob-data seams, `SummonedPool` preserves raw numeric `WzUOLProperty` frame leaves while enumerating source sequences before any `WzInfoTools.GetRealProperty(...)` resolution, so mounted-style numeric relative UOL runs whose resolved target canvases have different names still feed authored relative target text into sequence-root extraction. Existing assignment-noise and signed sibling-offset tolerance remains intact, `MobData` still keeps client-extracted tuple-shaped hidden `sHit` rows from letting numeric owner slot `0` eclipse path slot `1`, and client-extracted structured string `sHit` records still parse query-string, semicolon, tab-delimited, and JSON-style owner/frame/path rows with attach/facing/hitAfter metadata retained as fallback tokens before `SummonedPool` consumes the stored path. This run tightens that same `MobData`/`SummonedPool` seam again: hidden/client-extracted `sHit` rows and raw renderable hit containers now accept generic extractor value aliases (`data`, `payload`, `targetValue`, `pathValue`, `uolData`, `sourceValue`, `hitValue`, `recordValue`) alongside the prior source/path/UOL aliases, and structured string row splitting is now quote-aware so JSON-style quoted path or metadata values containing `,`, `;`, `|`, `&`, or tab characters are retained as one client-owned field instead of being fragmented before fallback consumption. In the existing `SkillLoader` zone-effect seam, visible WZ-authored tile paths are copied into the same stored client tile-UOL slots when hidden `sTileUOL` is absent, including char-level and skill-level variants, and hidden/client-extracted tile-UOL entries retain variant ownership when `characterLevel`/`charLevel`/`skillLevel`/`level` appears as an encoded field-name leaf such as `characterLevel=30`, while preserving hidden/client-extracted values when they exist. Focused coverage in `UnitTest_MapSimulator/PacketOwnedSummonHitParityTests.cs` now also locks generic `uolData` record retention and quote-aware structured row delimiter preservation, alongside the mounted-style numeric UOL sequence starting at frame `17` (`2400018 attack1 -> stand`), prior-frame-context retention across nested assignment-noise tokens, tuple owner/value hidden `sHit` row retention, structured string record path/metadata parsing, tab-delimited row parsing, and structured record sequence ordering. `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs` keeps visible tile root/variant fallback retention, hidden tile-UOL precedence, and encoded variant-level field-name retention for hidden tile-UOL rows. The row remains partial because full one-for-one synthesis of hidden client `MobAttackInfo::sHit` shapes is still unresolved, some cases still depend on unseen predecoded/client-owned path shaping beyond currently covered sequence, alias, assignment-noise, tuple owner/value, generic extractor value aliases, quote-aware structured string rows, and numeric-relative-UOL forms, post-attack effects remain constrained to the confirmed bird-tile family plus `4111007` reactive chain, hidden-only tile-family construction beyond visible WZ tile-branch fallback and accepted client-extracted variant rows remains unresolved, GR2 chain-layer/canvas ownership choreography remains unresolved, and `/summonedpacket` plus session-bridge transport is still short of full official `CUserPool` decode/ownership parity. | Packet-owned summon hit playback now keeps raw numeric WZ UOL relative frame leaves plus client-extracted tuple, generic extractor-value, and quote-aware structured-string `sHit` rows alive in the same general-effect fallback seam as decorated hidden tokens, leading-dot image-relative shells, frame-relative source branches, sibling-frame shorthand, sequence-root alias fallback, assignment-noise-tolerant sequence chains, and source-prefixed nested assignment-noise chains. Existing lifts remain intact (`0x116-0x11B` packet family, retained live attack-frame reuse, frame-scoped attach/facing behavior, tile/ball UOL retention with visible tile-path fallback including encoded variant-level row fields, delayed bird tile overlay, and `4111007` reactive chain). Remaining mismatch risk is concentrated in deeper hidden `sHit` synthesis breadth beyond the now-modeled structured row families and generic aliases, hidden-only tile-family construction beyond visible WZ tile fallback plus accepted row variants, GR2/layer choreography, and incomplete official transport decode. | packet-owned summon runtime (`CSummonedPool::OnPacket`, `CSummoned::OnAttack`, `CSummoned::OnHit`, `CSummoned::Hit`, `SummonedPool`, `PacketOwnedSummonUpdateRules`, `SkillLoader`, `SkillLoader.PopulateClientTileUolFallbackFromZoneEffect(...)`, `SkillLoader.TryReadClientSkillAssetUolTableEntryVariantLevel(...)`, `LifeLoader`, `MobAnimationSet.AttackInfoMetadata`, `MobItem`, `MobData.AttackData`, `MobData.GetMobAttackHitEffectNumericTupleRecordValuePath(...)`, `MobData.GetMobAttackHitEffectStructuredRecordValuePath(...)`, `MobData.SplitMobAttackHitEffectStructuredRecordSegments(...)`, `UnitTest_MapSimulator/PacketOwnedSummonHitParityTests.cs`, `SummonedPool.TryResolvePacketMobAttackGeneralEffectSourceSequenceRootPath(...)`, `SummonedPool.TryResolvePacketMobAttackGeneralEffectRelativeSourcePath(...)`, `SummonedPool.EnumeratePacketMobAttackGeneralEffectNumericFrameProperties(...)`, `Mob/3220000.img/attack1/{18,info/hit,13/hit}`, `Mob/3220000.img/stand/0`, `Mob/2400018.img/attack1/17`, `Mob/2400011.img/attack1/info/hit/0/source`, `Mob/2400110.img/hit1/0/source`, `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, `Skill/312.img/skill/3120010/{CharLevel,ball,tile,summon}`, `SKILLENTRY::GetTileUOL@0x437180`, `SKILLENTRY::GetBallUOL@0x569390`, `CSummoned::OnHit@0x74bc80`) |
| Partial | Summoned actor update/runtime parity | The simulator keeps summon-owned actor update/runtime ownership in the same seams (`SummonRuntimeRules`, `PacketOwnedSummonUpdateRules`, `SkillManager`, `SummonedPool`) for movement snapshots, spawn/prepare/attack/hit/die routing, Tesla armed idle fallback, self-destruct expiry handoff, packet-owned one-time owner routing, and shared authored suspend windows. WZ was rechecked first against the mounted export and still confirms the authored branch constraints this row depends on: `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3512.img/skill/35121009/{info/{type,minionAbility,selfDestructMinion},summon/subsummon}` (`type=33`, `minionAbility=summon`, `selfDestructMinion=1`, authored `subsummon` frames at `120ms`), stand-only support/self-destruct rows at `Skill/3511.img/skill/35111005` (`minionAbility=mes`) and `Skill/3512.img/skill/35121010` (`minionAbility=amplifyDamage`), and Tesla authored branch family at `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}`. Client evidence stayed second: `CSummoned::OnSkill@0x74a940` clears prior one-time owner on the addressed summon before `SetAttackAction`, `CSummoned::SetAttackAction@0x74a0f0` stamps one-time action ownership, and `CSummoned::Update@0x757f70` keeps one-time ownership alive until clip completion while forcing move action `0x2A` during Tesla states `1/2` before `SetMoveAction`. This pass keeps prior packet-owned Tesla/runtime lifts and the tightened no-action summon selector, separates support no-action authored-branch detection from Healing Robot action-13 routing, and now moves shared summon runtime timers onto client-style signed tick-delta ownership: `heal|mes|amplifyDamage` no-action support ownership now has an explicit stand-capable predicate, while summon-owned `action & 0x7F == 0` still stops at client stand and never falls through into indexed `skill1`/`skill2` or arbitrary custom branches when a `summon` minion cue lacks authored `subsummon`. Strict action-`14` routing remains authored-`subsummon` only; self-destruct action-`16` still resolves the authored final branch (`die`/`die1`) ahead of summon-action branches; explicit action-family routing still requires action-family evidence plus authored family branch presence; and conflicting/ambiguous selectors still preserve current ownership. Existing Tesla constraints remain in place: packet-owned runtime move-action ownership still forces the client-style Tesla `0x2A` action family with preserved facing bit while Tesla is in states `1/2`, packet-owned attack-facing still resolves from that forced move-action bit during Tesla armed windows, hit-action interrupts still drop prior skill-packet ownership state before arming hit one-time playback, one-time owner cleanup no longer depends only on `OneTimeAction != 0`, local Tesla still requires armed state `1`, local and packet-owned rearm still return state `2 -> 1` only after one-time playback finishes, Tesla rearm cleanup now also clears stale one-time fallback action codes together with fallback animation/timing and triangle ownership, packet-owned refresh still clears packet triangle points at rearm, packet-owned Tesla skill packets still avoid rebroadcasting one coil branch to siblings, sibling one-time packet-skill ownership is still cleared strictly from the ownership flag even when action/clip fields are already zeroed, and local plus packet-owned summon expiry/removal, packet-owned expiry timer dispatch/order, pending-removal remaining-time, support reheal/suspend windows, one-time action clips/fallbacks, spawn/hit/attack animation windows, passive movement/effect cooldowns, packet-owned mob-attack hit-effect frame timing, and body-contact cooldowns now compare through rollover-stable client tick deltas rather than raw signed absolute-time ordering. Focused regression coverage in `UnitTest_MapSimulator/SummonRuntimeActionParityTests.cs` now also locks stand-only `mes`/`amplifyDamage` no-action support ownership, no-action summon-cue branch rejection when both `subsummon` and `stand` are absent, indexed `skill1` and custom branch fallthrough rejection, self-destruct action-16 `die` selection over `subsummon`, Tesla rearm/refresh stale fallback action-code cleanup, support reheal tick rollover, one-time action fallback rollover, zero-duration one-time fallback-end rollover, packet-owned expiry timer rollover/order, pending-removal remaining-time rollover, and packet-owned body-contact cooldown rollover, alongside prior strict action-`14` authored branch routing/no-fallback checks, strict one-time fallback-admission checks, action-`14` runtime ownership flip/no-flip gating, action-`13` support-owner flip/no-flip coverage, and indexed ownership flip/no-flip coverage for explicit single-family vs conflicting cues. The row remains partial because deeper client-only seams still sit outside current simulator visibility: exact hidden summon HP and full `CalcDamage::PDamageSummoned` / `MDamageSummoned`, native `CVecCtrl::UpdatePassive` details beyond the now rollover-stable modeled passive movement/effect cadence, deeper hidden/packet-owned Tesla transition rules beyond the modeled rearm boundaries, and unresolved remote assist-family selectors when both action-family signals and disambiguating authored/minion cues are absent or conflicting. | No-action summon-cue packet skill updates no longer promote `skill1`/custom action playback unless WZ exposes the client-owned `subsummon` family, stand-only support rows have explicit no-action ownership separate from action-13 Healing Robot routing, and summon runtime/support/expiry/one-time/passive/hit-effect/body-contact timers now use client tick-delta comparisons across rollover. Existing packet-owned Tesla move-action forcing, packet-owned attack-facing ownership, packet-owned hit interrupt ownership reset, one-time playback completion cleanup, sibling stale-owner cleanup, strict action-`14` branch/no-fallback routing, self-destruct final-branch selection, explicit single-family authored assist routing, and stand-only support ownership routing remain intact. Remaining risk is concentrated on hidden damage formulas, native passive-update side effects beyond the modeled cadence, hidden Tesla transitions beyond the now-cleared stale fallback owner fields, and client-only assist selectors not exposed by current packet/WZ inputs. | summon actor/runtime layer (`CSummoned::Update`, `CSummoned::OnSkill`, `CSummoned::SetAttackAction`, `CSummoned::SetMoveAction`, `CSummoned::TryDoingHealingRobot`, `CSummoned::TryDoingSummon`, `CSummoned::TryDoingTaslaCoilAttack`, `SummonRuntimeRules.cs`, `SummonRuntimeRules.ResolvePacketSkillBranch(...)`, `SummonRuntimeRules.IsStrictPacketSkillBranchAction(...)`, `SummonRuntimeRules.ResolveClientTickElapsedMs(...)`, `SummonRuntimeRules.HasClientTickReached(...)`, `SummonRuntimeRules.HasClientTickElapsedAtLeast(...)`, `SummonRuntimeRules.ResolveAuthoredAssistTypeForRuntimeOwnershipWithoutActionFamily(...)`, `PacketOwnedSummonUpdateRules.cs`, `SkillManager.cs`, `SummonedPool.cs`, `SummonedPool.ShouldAllowPacketOwnedSkillOneTimeFallback(...)`, `SummonedPool.ResolvePacketOwnedExplicitSelfDestructPlayback(...)`, `BeginPacketOwnedSkillAnimation(...)`, `SummonedPool.ResolvePacketOwnedMovementSnapshotFacingForParity(...)`, `PacketOwnedSummonUpdateRules.ResolvePacketAttackFacingRight(...)`, `SummonedPool.ResolvePacketOwnedSkillAssistTypeForRuntimeOwnership(...)`, `SummonedPool.ResolvePacketOwnedCreatedAssistTypeForRuntimeOwnership(...)`, `SummonRuntimeRules.ResolvePacketSkillAssistTypeForRuntimeOwnership(...)`, `SummonRuntimeRules.HasAuthoredPacketSkillAssistOwnershipBranch(...)`, `UnitTest_MapSimulator/SummonRuntimeActionParityTests.cs`, IDA `CSummoned::Update` (`0x757f70`), IDA `CSummoned::OnSkill` (`0x74a940`), IDA `CSummoned::SetAttackAction` (`0x74a0f0`), IDA `CSummoned::TryDoingHealingRobot` (`0x74b3e0`), IDA `CSummoned::TryDoingSummon` (`0x74b640`), IDA `CSummoned::TryDoingTaslaCoilAttack` (`0x752780`), `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3512.img/skill/35121009/{info/{type,minionAbility,selfDestructMinion},summon/subsummon}`, `Skill/3511.img/skill/35111005/{info/selfDestructMinion,summon/stand}`, `Skill/3512.img/skill/35121010/{info/selfDestructMinion,summon/stand}`, `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}`) |
| Partial | Weapon afterimage / melee trail parity | This pass stays in the same afterimage seams and narrows the remaining GR2/cache approximation with WZ-first and client-second evidence. WZ was rechecked first in the mounted export: `Character/Afterimage/swordOS/charge/1/stabO2` still exposes action-level `lt = (-83,-30)` / `rb = (-12,-16)`, `charge/1/stabO2/1` still contains two numbered canvases in client order, and those canvases publish origin/delay/alpha metadata (`0` has `origin = (52,46)`, `delay = 50`; `1` has `origin = (83,46)`, `delay = 300`, `a1 = 0`) without authored `z0`/`z1` zoom leaves. Existing WZ evidence remains stable: `Character/Afterimage/swordOS/charge` publishes `{1,2,3,5}`, `Character/Afterimage/poleArm/charge` publishes only `{1}`, `Skill/122.img/skill/1221011` exposes root `elemAttr = h` and no `afterimage/*` branch, `Skill/122.img/skill/1221009/afterimage/swordOS/stabO2/1/0` remains skill-authored, and representative lightning rows such as `Skill/1511.img/skill/15111006/elemAttr = l` still prove non-melee element attributes must not force trails. Client evidence stayed second: `get_element_by_charged_skillid@0x639fe0`, `CActionMan::GetMeleeAttackRange@0x428d00`, `CUserRemote::OnMeleeAttack@0x959d60`, and `CUser::RegisterAfterimage@0x902d90` still support the existing charge/range seams, while `CActionMan::GetWeaponAfterImage@0x428080` confirms cached raw-action-indexed canvas arrays plus action `lt`/`rb` rectangles, `CActionMan::CreateAfterimageLayer@0x428a20` confirms alpha `RelMove(255,0)`, `RemoveCanvas(-2)`, then one `CAnimationDisplayer::LoadCanvas` call per cached numbered canvas, and `CAnimationDisplayer::LoadCanvas@0x446e60` confirms the InsertCanvas argument defaults (`delay=120`, `a0=-1`, `a1=-1`, `z0=0`, `z1=0`), result-variant cleanup, and layer/canvas argument release after `InsertCanvas`. Implementation remains in the same seams and now carries simulator-side cached-canvas identity plus client LoadCanvas argument/lifetime shape through the loader/playback path: loaded afterimage frames retain action name, client raw action code, and numbered canvas ordinal; renderable layers and layer operations expose a stable action-indexed canvas object id; `ResolveCreateAfterimageLayerOperations` models the client reset-alpha, remove-all-canvases, then ordered insert-canvas sequence; and `ResolveCreateAfterimageLayerReferenceOperations` now emits explicit `LoadCanvas`, `InsertCanvas`, result-variant cleanup, layer-release, and canvas-release operations with raw client argument defaults, so missing `a0/a1` remains `-1` and missing `z0/z1` remains `0` in the native trace while the existing SpriteBatch playback still normalizes alpha/zoom for rendering. Earlier implemented behavior remains in place: known-state `WeaponChargePayloadOffset` capture, metadata-scoped and metadata-missing bounded ordering, repeated known-skill/raw-element/combined/adjacent-pair consensus, preferred/prior hint precedence, local and remote effective-charge resolution with melee-only WZ `elemAttr` fallback, skill-authored afterimage precedence under active charge, client range aliases/overrides, authored `z0`/`z1` playback defaults, and remote temporary-stat refresh charge recovery. Focused coverage in `UnitTest_MapSimulator/AfterImageChargeSkillResolverParityTests.cs` now also locks client create-layer ordering, stable action-indexed canvas identity across samples, and LoadCanvas argument/default/reference cleanup ordering for the two-canvas `stabO2` shape. The row remains partial because the unresolved work is now concentrated in truly no-hint/no-consensus temporary-stat payload families and native GR2 object behavior that the SpriteBatch seam still approximates: real COM `IWzGr2DLayer` ownership, `RelMove` interpolation internals, exact `RemoveCanvas` removed-canvas reference identity, true unmanaged `InsertCanvas` result ownership, and unmanaged canvas/layer identity beyond the simulator-side action/ordinal plus LoadCanvas trace. | Weapon trail parity now keeps metadata-scoped, metadata-anchored, metadata-missing mask-base-scoped, metadata-missing nearest-element-before-nearest-id, metadata-missing local-distance-bounded nearest recovery, repeated known-skill consensus recovery, repeated raw-element consensus recovery, combined known-skill/raw-element consensus recovery, adjacent skill/element-pair consensus recovery, skill-authored afterimage precedence under active charge, local and remote melee skill `elemAttr` charge fallback, active remote temporary-stat charge refresh playback, client melee range aliases/overrides, authored alpha/zoom endpoint playback, client create-layer reset/remove/insert ordering, stable simulator-side action-indexed canvas identity, and a client-shaped LoadCanvas argument/default plus reference-cleanup trace. Remaining risk is limited to no-hint/no-consensus payload disambiguation plus native GR2 layer/reference/interpolation fidelity outside the current SpriteBatch renderer. | melee afterimage loader and playback seam (`CUser::RegisterAfterimage`, `get_element_by_charged_skillid`, `CActionMan::GetMeleeAttackRange`, `CActionMan::GetWeaponAfterImage`, `CActionMan::CreateAfterimageLayer`, `CAnimationDisplayer::LoadCanvas`, `CUserRemote::OnMeleeAttack`, `AfterImageChargeSkillResolver`, `SkillManager.ResolveAfterImageChargeElement`, `RemoteUserActorPool.ResolveRemoteAfterImageChargeElement`, `RemoteUserActorPool.ResolveChargeSkillIdFromTemporaryStats`, `RemoteUserActorPool.TryResolveChargeElementFromTemporaryStats`, `RemoteUserPacketCodec.TryResolveChargeSkillIdFromKnownTemporaryStatPayload`, `SkillLoader.TryResolveMeleeAfterImageAction`, `SkillLoader.TryResolveUniqueMeleeAfterImageActionName`, `MeleeAfterimagePlaybackResolver`, `PlayerCharacter.ApplyMeleeAfterImage`, `RemoteUserActorPool.RegisterMeleeAfterImage`, `UnitTest_MapSimulator/AfterImageChargeSkillResolverParityTests.cs`, `Character/Afterimage/swordOS/charge`, `Character/Afterimage/poleArm/charge`, `Skill/122.img/skill/1221011`, `Skill/122.img/skill/1221009/afterimage/swordOS/stabO2/1/0`, `Skill/1511.img/skill/15111006/elemAttr`) |
| Partial | Skill cooldown UI parity | QuickSlot, skill-book, status-bar shortcut, off-bar cooldown, Notice3, and Battleship durability feedback remain on the shared simulator cooldown/status-bar seams, with this pass narrowing the remaining Battleship `CTemporaryStatView` object-lifetime approximation at the simulator-visible release edge. WZ was rechecked first from the row-owned evidence and remains stable: `UI/UIWindow.img/Skill/CoolTime` and `UI/UIWindow2.img/Skill/main/CoolTime` expose `16` mask frames; mounted `UI/UIWindow2.img/Skill/main/tip0..2` remains `193x102` with origins `(195,-25)`, `(-178,-22)`, `(103,-282)` while legacy `UI/UIWindow.img/Skill/tip0` keeps zero origin; `UI/Basic.img/Notice3` keeps `t/c/s` at `266x21`, `266x20`, `266x55`; `UI/StatusBar2.img/mainBar/quickSlot/quickSlot` remains `145x93` at origin `(-143,143)`; and `UI/UIWindow.img/TemporaryStatView` plus `UI/UIWindow2.img/TemporaryStatView` expose only authored canvas `1` (`32x32`, origin `(0,0)`). Client evidence stayed second on the same owner seams (`CUIStatusBar::SetItemMsg@0x86cdf0`, `CUIStatusBar::FloatNotice@0x86d430`, `CUIStatusBar::Update@0x874290`, `CUIStatusBar::CQuickSlot::DrawSkillCooltime@0x86ff50`, `CUISkill::OnMouseMove@0x84b660`, plus existing `CTemporaryStatView::TEMPORARY_STAT` / `SetLeft` Battleship durability notes). Existing shortcut parity remains in place (primary slots `0..7`, fixed `4x2` grid, retained `ShortcutSlotIndex`, macro-display owner resolution, duplicate slot admission, strict `>1000 ms` refresh, surface-owned admission, off-bar/counter/Notice3 behavior, and per-surface mask math). Battleship still resolves `SkillManager.ResolveTemporaryStatViewShadowCanvasPathForParity(...)` to resident authored `UI/UIWindow.img/TemporaryStatView/1` while preserving numeric `ShadowIndex`, stable simulated `TemporaryStatViewOwnerIdentity`, parent/main/shadow layer identities across the allocated object, `-2` remove index, `500 ms` insert delay, `64 -> 210` alpha ramp, low-bucket alert ownership, and status-bar durability-only entry routing. The simulator-visible `CTemporaryStatView::TEMPORARY_STAT` owner naming, parent/main/shadow layer names and ordinals, retained layer/canvas references, and remove/insert sequence counters remain in place; this pass carries the terminal release snapshot through the passive status-bar entry/render-data seam and makes repeated simulated release idempotent after the terminal snapshot, so cooldown clear/removal preserves the final owner/layer identities, records the object and parent/main/shadow layer release sequences once, drops simulated layer/canvas reference counts to zero, advances the shadow-canvas remove/release sequence once, and keeps the tray entry removed rather than rendering a released object. Focused coverage remains in `UnitTest_MapSimulator/SkillCooldownUiParityTests.cs`; `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` now also locks terminal release propagation and idempotent post-release behavior in addition to authored TemporaryStatView canvas-path fallback, stable simulated owner/layer identity propagation into status-bar render data, retained numeric shadow-index metadata, simulator owner/layer names and ordinals, layer/canvas reference-count metadata, paired remove/insert sequence propagation, and no reinsertion when the durability bucket stays stable. Remaining gap: literal client `CFloatNotice` and `CTemporaryStatView` COM-layer identity/lifetime semantics are still approximated; cross-layer status-bar owner identity now carries stable simulator TemporaryStatView identity plus active and terminal layer/canvas lifetime metadata but is still not native `quizPanel`/`floatNotice`/`itemMsg`/temporary-stat object identity; exact Battleship native layer handles and COM ref-count behavior remain outside simulator seams; and cooldown-only HUD widgets beyond quick-slot, skill windows, status-bar trays, Notice3, and the current Battleship durability tray remain unverified. | Surface ownership in the shared cooldown seam is tighter: status-bar shortcut/off-bar intake, shortcut-tray cadence, fixed primary-slot shortcut identity including macro-display skill owners, tooltip anchors, mask ladders, counter-only durability rendering, WZ-authored TemporaryStatView canvas fallback, stable simulated TemporaryStatView owner/layer identity, active layer/canvas lifetime metadata, terminal release metadata, and idempotent post-release snapshots now all stay on per-surface admission/math while preserving the existing owner-gating seam for status notices. The row remains `Partial` because native layer-object identity/lifetime and unverified extra cooldown HUD widgets still sit outside simulator-visible seams. | `SkillTooltipFrameLayout.cs`, `QuickSlotUI.cs`, `SkillUI.cs`, `SkillUIBigBang.cs`, `StatusBarUI.cs`, `SkillManager.cs`, `StatusBarBuffEntry.cs`, `SkillCooldownNoticeUI.cs`, `FieldEffects.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/SkillCooldownUiParityTests.cs`, `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` (`SkillManager.ResolveTemporaryStatViewShadowCanvasPathForParity`, `SkillManager.ResolveTemporaryStatViewOwnerIdentityForParity`, `SkillManager.ResolveTemporaryStatViewLayerIdentityForParity`, `SkillManager.ResolvePassiveVehicleTemporaryStatReleaseForParity`, `StatusBarUI.ResolveTemporaryStatViewShadowTextureFallbackIndexForParity`, `StatusBarUI.ResolveTemporaryStatViewShadowAlphaForParity`, `TemporaryStatViewOwnerIdentity`, `TemporaryStatViewParentLayerReferenceCount`, `TemporaryStatViewObjectReleaseSequence`, `ShadowCanvasReferenceCount`, `ShadowCanvasRemoveSequence`, `ShadowCanvasReleaseSequence`, `UI/UIWindow.img/TemporaryStatView`, `UI/UIWindow.img/Skill/CoolTime`, `UI/UIWindow2.img/Skill/main/CoolTime`, `UI/Basic.img/Notice3`, `UI/StatusBar2.img/mainBar/quickSlot/quickSlot`, `CUIStatusBar::SetItemMsg@0x86cdf0`, `CUIStatusBar::FloatNotice@0x86d430`, `CUIStatusBar::Update@0x874290`, `CUIStatusBar::CQuickSlot::DrawSkillCooltime@0x86ff50`, `CUISkill::OnMouseMove@0x84b660`) |
| Partial | Client-timer skill expiry and cancellation | `SkillManager` still owns the shared `UpdateClientTimer`-style registry for buff expiry, prepared release, repeat-sustain/mode-end, cyclone timeout, swallow digest completion, local skill-zone expiry, and local summon expiry, while `SummonedPool` still owns packet-local summon expiry/cancel ownership plus self-destruct fallout routing. WZ was rechecked first and remains unchanged on the same row-owned timer/cancel surfaces (`Skill/422.img/skill/4221006` `info/type=36`, `info/zoneType=invincible`, `common/time=30+x`; `Skill/3212.img/skill/32121003` `action/0=cyclone_pre`, `info/bodyAttack=1`, `info/movingAttack=1`, `common/time=10+2*d(x/6)`; `Skill/3511.img/skill/35111011` `common/selfDestruction=300+5*x`, `hit/0/hitAfter=990`; repeat-sustain `35111004` / `35121013` `common/time=5`; swallow linkage `Skill/3310.img/skill/33101005/info/dummyOf=33101006&&33101007` with `33101006/common/time=10+4*x`). Connected-family Battle Mage alias rewrites remain mirrored by `ClientSkillCancelResolver` (`32120000 -> 32001003`, `32110000 -> 32101002`, `32120001 -> 32101003`). IDA was rechecked second and still anchors the same owners (`CUserLocal::UpdateClientTimer@0x90e6d0`, `CUserLocal::SendSkillCancelRequest@0x93d730`, caller families `CUserLocal::{Update@0x937330,OnKeyDownSkillEnd@0x9301b0,HandleCtrlKeyDown@0x9326b0,TryDoingSwallowMobWriggle@0x93f500,OnJoystickButton@0x936550,HandleRButtonClk@0x911a60}` plus `CDraggableItem::{WearEquipItem@0x5043e0,GetOffEquipItem@0x502b90/0x502e60,ThrowItem@0x505ba0}` and `CAvatarMegaphone::OnMouseButton@0x46d560`). This pass keeps ownership in the same seams and closes additional non-update ingress drift: Item Upgrade owner start now routes through `ReleaseActiveKeydownSkillForClientCancelIngress(currTickCount)` in `MapSimulator.ItemUpgradeOwnerParity.cs::HandleItemUpgradeOwnerStartRequested(...)`, Vega cast dispatch now routes through the same ingress wrapper in `MapSimulator.VegaSpellParity.cs::DispatchVegaSpellRequest(...)` after request-context admission, Cash Avatar preview owner buttons now route through `CashAvatarPreviewWindow.ClientCancelIngressRequested` wired in `MapSimulator.CashServiceStageParity.cs` and `MapSimulator.Content.cs` to `ReleaseActiveKeydownSkillForClientCancelIngress(currTickCount)` before buy/default/takeoff handling (with weather branch ingress still preserved in `PreviewCashAvatarWeatherAction(...)`), and Big Bang companion drag commit sub-branches now route through `EquipUIBigBang.CompanionDragCommitIngressRequested` wired in `MapSimulator.Content.cs` to `ReleaseActiveKeydownSkillForClientCancelIngress(currTickCount)`, MapleTV owner buttons now route through `ExecuteMapleTvClientCancelIngress(...)` in `MapSimulator.cs::WireMapleTvWindowData(...)` so publish/clear/toggle handlers release active keydown skill before owner action dispatch, alongside the existing content-rebound inventory/equipment/drop/memo/quest ingress mirrors and packet-owned self-destruct fallback ordering (`OrderPacketOwnedExpiryFallbackCandidates(...)` before `ResolvePacketOwnedExpiryFindHitMobInRectTargetOrder(...)`). Status-bar right-click ingress now also mirrors the recovered native suppression branch from `CAvatarMegaphone::OnMouseButton@0x46d560` (`nType == 2 && msg != 1311006`) by suppressing skill-cancel ingress for `1311006` before batch-scope/release/request dispatch. Focused regression coverage now includes MapleTV ingress order/null-action checks in `UnitTest_MapSimulator/ClientTimerMapleTvCancelIngressParityTests.cs`, status-bar suppression checks for `1311006` in `UnitTest_MapSimulator/ClientTimerStatusBarBuffCancelIngressParityTests.cs`, alongside cash-service ingress checks, explicit packet-owned expiry ordering checks, and owner-reference facing tie-break coverage in `UnitTest_MapSimulator/ClientTimerPacketOwnedSelfDestructExpiryParityTests.cs`. Packet-owned self-destruct expiry now also consumes owner-position tie-breaks in `SummonedPool.TryResolvePacketOwnedExpiryProbeFacingRight(...)` and `SummonedPool.ResolvePacketOwnedExpiryFallbackFacingRight(...)` when bidirectional capture candidates are otherwise tied. This pass rechecked `CMobPool::FindHitMobInRect@0x657530` and keeps packet-owned self-destruct fallback order closer to the client by preserving mob-pool source order before side/distance fallback when `SummonedPool.OrderPacketOwnedExpiryFallbackCandidates(...)` feeds `ResolvePacketOwnedExpiryFindHitMobInRectTargetOrder(...)`. This run also narrows the body-rect side of the same native call: `LifeLoader` now keeps authored mob `multiRect` and `rect` metadata separately while preserving the existing combined `MultiBodyBounds` surface, `MobItem` exposes client-shaped `GetClientMultiBodyHitboxes(...)` plus `GetClientBodyHitbox(...)`, and packet-owned self-destruct expiry now feeds `SummonedPool.ResolvePacketOwnedExpiryFindHitMobInRectTargetOrder(...)` with native-like `GetMultiBodyRect` probes first and appended authored `GetBodyRect` data instead of a synthetic live union. A follow-up in the same seam now keeps `FrameMetadata.ClientBodyBounds` empty when WZ does not author `rect`, so packet-owned expiry no longer turns visual/effective fallback bounds into a client fallback body while WZ-authored `multiRect` still probes first and authored `rect` still appends as fallback body data. This pass narrows the same packet-owned `FindHitMobInRect` eligibility surface: live expiry candidates now carry simulator visibility/hidden-life state into `InView`, carry WZ/AI escort ownership separately from `damagedByMob`, and keep template-id matching on the existing candidate surface, so hidden/out-of-view and escort candidates no longer leak through the default self-destruct expiry path. This follow-up tightens the same pointer-lifetime approximation without moving ownership: `SummonedPool.ResolvePacketOwnedExpiryTargetOrder(...)` now coalesces duplicate mob-object snapshots by preferring the first client-eligible `FindHitMobInRect` candidate for that object id, while preserving the previous first-snapshot fallback when all duplicate snapshots are ineligible. Focused coverage in `UnitTest_MapSimulator/ClientTimerPacketOwnedSelfDestructExpiryParityTests.cs` now locks both duplicate-snapshot paths. The row remains partial because packet-owned self-destruct expiry targeting still approximates native `CMobPool::FindHitMobInRect` live pointer lifetime and deeper suspended/team/phase internals; remaining non-simulator-native direct UI ingress ownership outside the mapped seams still lacks one-for-one mirroring. | Cancel dispatch now dedupes on one cancel-family path across timer routes, update-loop non-timer routes, scoped non-update attack/hotkey/macro/func-key routes, content-rebound equipment/inventory callbacks, memo/quest inventory-drag UI ingress, Item Upgrade owner start, Vega cast dispatch, cash-avatar preview buy/default/takeoff/weather ingress, Big Bang companion drag commit ingress, MapleTV publish/clear/toggle owner-button ingress, status-bar right-click suppression parity for skill `1311006`, and packet-owned summon-expiry fallback selection with deterministic mob-pool/source-order candidate pick before in-range capture, eligible duplicate-snapshot coalescing per mob object id, owner-position tie-break consumption when bidirectional capture is otherwise tied, client-shaped `multiRect`-before-authored-`rect` body probe intake, live visibility/hidden-life gating, explicit template matching, and split escort-vs-`damagedByMob` eligibility for packet-owned self-destruct expiry. Remaining risk is narrowed to deeper native `FindHitMobInRect` suspended/team/phase eligibility and live pointer-lifetime semantics, plus still-unmapped direct UI caller internals outside the mapped seams. | `ClientSkillCancelResolver`, `SkillManager`, `SkillManager.BeginClientCancelBatchScope`, `SkillManager.BeginClientCancelBatchScope(ref ...)`, `SkillManager.EndClientCancelBatchScope(ref ...)`, `SkillManager.Update`, `SkillManager.UpdateClientSkillTimers`, `SkillManager.RequestClientSkillCancel`, `SkillManager.ShouldSuppressClientCancelBatchRequest`, `SkillManager.TryRegisterClientTimerBatchCancelFamily`, `SkillManager.TryCastSkill`, `SkillManager.TryCastPacketOwnedFuncKeySkill`, `SkillManager.TryCastHotkey`, `SkillManager.ReleaseHotkeyIfActive`, `SkillManager.ReleasePacketOwnedFuncKeySkillIfActive`, `SkillManager.TryExecuteMacro`, `SkillManager.TryDoingRandomAttack`, `SkillManager.TryDoingMeleeAttack`, `SkillManager.TryDoingShoot`, `SkillManager.TryDoingMagicAttack`, `SkillManager.ReleaseActiveKeydownSkill`, `SkillManager.RouteExpiredRepeatSustainTimerBatchToClientCancel`, `SkillManager.RouteExpiredCycloneTimerBatchToClientCancel`, `SkillManager.RouteExpiredSkillZoneTimerBatchToClientCancel`, `SkillManager.RouteExpiredLocalSummonTimerBatchToClientCancel`, `SkillManager.RouteExpiredBuffTimerBatchToClientCancel`, `SkillManager.TryApplyPacketOwnedSelfDestructSummonAttack`, `MapSimulator.PacketOwnedFuncKeyParity.cs`, `MapSimulator.UpdatePacketOwnedFuncKeyRuntime`, `MapSimulator.cs`, `MapSimulator.ReleaseActiveKeydownSkillForClientCancelIngress`, `MapSimulator.RequestStatusBarBuffCancelForClientCancelIngress`, `MapSimulator.TryExecuteStatusBarBuffCancelForClientCancelIngress`, `MapSimulator.ExecuteMapleTvClientCancelIngress`, `MapSimulator.ExecuteMapleTvClientCancelIngressForTests`, `MapSimulator.WireMapleTvWindowData`, `MapSimulator.TryUseInventoryItem`, `MapSimulator.TryUseInventoryItemAtSlot`, `MapSimulator.HandleMemoDraftAttachmentInventoryDrop`, `MapSimulator.EquipmentWindowParity.cs`, `MapSimulator.SubmitEquipmentChangeRequest`, `MapSimulator.ItemUpgradeOwnerParity.cs`, `MapSimulator.HandleItemUpgradeOwnerStartRequested`, `MapSimulator.VegaSpellParity.cs`, `MapSimulator.DispatchVegaSpellRequest`, `MapSimulator.CashServiceStageParity.cs`, `MapSimulator.PreviewCashAvatarWeatherAction`, `MapSimulator.Content.cs`, `CashAvatarPreviewWindow.cs`, `CashAvatarPreviewWindow.ClientCancelIngressRequested`, `CashAvatarPreviewWindow.HandleBuyAvatar`, `CashAvatarPreviewWindow.HandleDefaultAvatar`, `CashAvatarPreviewWindow.HandleTakeoffAvatar`, `MapleTvWindow.cs`, `MapSimulator.QuestRewardRaiseParity.cs`, `MapSimulator.HandleQuestRewardRaisePieceDropRequested`, `EquipUIBigBang.cs`, `EquipUIBigBang.CompanionDragCommitIngressRequested`, `SummonedPool`, `SummonedPool.ResolvePacketOwnedExpiryTargetOrder`, `SummonedPool.ResolvePacketOwnedExpiryFindHitMobInRectTargetOrder`, `SummonedPool.OrderPacketOwnedExpiryFallbackCandidates`, `SummonedPool.ResolvePacketOwnedExpiryFallbackFacingRight`, `SummonedPool.TryResolvePacketOwnedExpiryProbeFacingRight`, `MapSimulator.PacketOwnedSummonedParity.cs`, `MapSimulator.TryRoutePacketOwnedSelfDestructAttackToRuntime`, `MapSimulator.TryRouteLocalPacketOwnedSelfDestructAttackToRuntime`, `PlayerManager.TryApplyPacketOwnedSelfDestructSummonAttack`, `UnitTest_MapSimulator/ClientTimerCashServiceCancelIngressParityTests.cs`, `UnitTest_MapSimulator/ClientTimerStatusBarBuffCancelIngressParityTests.cs`, `UnitTest_MapSimulator/ClientTimerMapleTvCancelIngressParityTests.cs`, `UnitTest_MapSimulator/ClientTimerPacketOwnedSelfDestructExpiryParityTests.cs` (`CUserLocal::UpdateClientTimer`, `CUserLocal::SendSkillCancelRequest`, `CUserLocal::Update`, `CUserLocal::OnKeyDownSkillEnd`, `CUserLocal::HandleCtrlKeyDown`, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::OnJoystickButton`, `CUserLocal::HandleRButtonClk`, `CDraggableItem::WearEquipItem`, `CDraggableItem::GetOffEquipItem`, `CDraggableItem::ThrowItem`, `CAvatarMegaphone::OnMouseButton`, `CSummoned::TryDoingAttack`, `Skill/422.img/skill/4221006/{info,common}`, `Skill/3212.img/skill/32121003/{action,info,common}`, `Skill/3511.img/skill/35111011/{info,common,summon,hit}`, `Skill/3511.img/skill/35111004/common`, `Skill/3512.img/skill/35121013/{common,info}`, `Skill/3212.img/skill/32120000/info/affectedSkill`, `Skill/3211.img/skill/32110000/info/affectedSkill`, `Skill/3212.img/skill/32120001/info/affectedSkill`, `Skill/3310.img/skill/33101005/{info,common}`, `Skill/3310.img/skill/33101006/common`) |
| Partial | Skill restrictions by map/state | FieldSkillRestrictionEvaluator remains the shared owner for recovered fieldLimit bits, WZ-authored noSkill/skill, noSkill/class, info/onlyUseSkill, info/noCancelSkill, info/fieldType, Mu Lung Dojo mobMassacre/disableSkill, runtime moveLimit, Wild Hunter Mine town/client-field-state blocking, Coconut/Snowball/Guild Boss basic-action ownership, Evan dragon loss, and StopMotion-style status lockouts. WZ evidence remains anchored to the existing v95_2 samples for noSkill/class, noSkill/skill, fieldType-backed Dojo/Balrog/Massacre/special fields, noCancelSkill, moveLimit, mobMassacre/disableSkill, and preserved info/fieldType values; client evidence remains CField::IsUnableToUseSkill, CField::IsSkillForbiden -> Field::SkillInfo::IsSkill, CUserLocal::DoActiveSkill field-type gates, CUserLocal::TryDoingMine, and the recovered CField_Coconut/CField_SnowBall/CField_GuildBoss BasicActionAttack owners. This pass keeps ownership in the same evaluator/runtime seams and extracts preserved info/fieldType resolution into MapInfoFieldTypeResolver so skill event gates and recovered special-field runtime detectors use the same precedence contract: typed fieldType still wins, empty copied info branches fall through to unsupported/live-image fieldType data, and real copied fieldType values keep precedence over live-image data. The resolver now feeds FieldSkillRestrictionEvaluator, SpecialFieldRuntimeCoordinator, SpecialEffectFields, SnowBallField detection, and CoconutField binding, so preserved fieldType-backed Coconut/Snowball/Guild Boss ownership is not limited to typed MapInfo.fieldType. Existing direct-int Field::SkillInfo traversal for noSkill/skill, noSkill/class, and info/onlyUseSkill remains intact, along with indexed lists, empty copied-branch fallback for noSkill/onlyUseSkill/mobMassacre/noCancelSkill/moveLimit/fieldType, get_skill_class class matching without current-job fallback, unsupported-info noCancelSkill, mine/town state, move-limit consumption, Evan gates, and status lockouts. Focused coverage now includes direct-int noSkill/skill, direct-int noSkill/class, direct-int unsupported-info onlyUseSkill, empty copied-branch fallback cases for live-image noSkill/skill, live-image onlyUseSkill, live-image mobMassacre/disableSkill, live-image noCancelSkill, live-image moveLimit, live-image fieldType, special-field unsupported-info fieldType detection for Coconut/Snowball/Guild Boss, empty copied fieldType fallback to live special-field data, and real copied fieldType precedence over live special-field data. Remaining gap: deeper unrecovered CField/CUserLocal availability branches outside the covered caller tables/state gates, special-field ownership outside the recovered basic-action owners, and any additional unrecovered Field::SkillInfo::IsSkill policy beyond covered field-type event gates plus direct-int and indexed class/id/allowlist traversal. | Map/state skill gating remains centralized with tighter WZ contract behavior: fieldLimit subsets, WZ noSkill/onlyUseSkill/noCancelSkill/fieldType, mobMassacre disable-skill, moveLimit, Wild Hunter Mine, and local-state owners stay in shared evaluators/runtimes, and preserved info/fieldType now follows the same empty-branch fallback / real-branch precedence model for both skill event gates and recovered special-field runtime-owner detection. The row remains Partial because remaining mismatch is concentrated in unrecovered native availability branches and special-field owners beyond the recovered basic-action ownership seams. | FieldSkillRestrictionEvaluator.cs, MapInfoFieldTypeResolver.cs, SpecialFieldRuntimeCoordinator.cs, SpecialEffectFields.cs, CoconutField.cs, SnowBallField.cs, FieldInteractionRestrictionEvaluator.cs, FieldRuleRuntime.cs, PlayerSkillStateRestrictionEvaluator.cs, PlayerSkillBlockingStatusMapper.cs, MapSimulator, SkillManager, PlayerCharacter, DragonCompanionRuntime, MassacreField, GuildBossField, MapInfo.unsupportedInfoProperties, MapInfo.additionalProps, MapInfo.additionalNonInfoProps, FieldLimitType, FieldType, WZ noSkill/class, WZ noSkill/skill, WZ info/onlyUseSkill, WZ info/noCancelSkill, WZ info/moveLimit, WZ info/fieldType, WZ mobMassacre/disableSkill, UnitTest_MapSimulator/FieldSkillRestrictionEvaluatorParityTests.cs, UnitTest_MapSimulator/FieldRuleRuntimeMoveLimitTests.cs, UnitTest_MapSimulator/SpecialFieldPreservedFieldTypeParityTests.cs, UnitTest_MapSimulator/PlayerSkillStateRestrictionEvaluatorParityTests.cs, UnitTest_MapSimulator/PlayerSkillBlockingStatusMapperParityTests.cs, client seams CField::IsUnableToUseSkill, CField::BasicActionAttack, CField_Coconut::BasicActionAttack, CField_SnowBall::BasicActionAttack, CField_GuildBoss::BasicActionAttack, CUserLocal::DoActiveSkill, CUserLocal::TryDoingMeleeAttack, CUserLocal::TryDoingShootAttack, CUserLocal::TryDoingMagicAttack, CUserLocal::TryDoingMine, CField::IsSkillForbiden, Field::SkillInfo::IsSkill, get_skill_class, get_job_level |

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Summoned action-loader / frame-build parity | The client-owned summon loader seam remains in `SkillLoader`, and the already-landed frame-build parity remains intact (`120` ms summon delay fallback, per-frame `z` / `a0` / `a1`, authored `origin` / `lt` / `rb` metadata when present, client-style reverse playback, standalone summon-root acceptance, wrapper pruning around `icon*`/`action`/root `effect` nodes). WZ was rechecked first: mounted roots still show the same ownership surfaces (`Skill/3510.img/skill/35101005` wrapper + door branches, `Skill/501.img/skill/5010005/summon` custom actions beside `attack1..4`, `Skill/434.img/skill/4341006/summon` effect/repeat-only with `info/requireSkill = 4331002` and `req/4331002 = 30`, and deep link leaves like `Skill/3512.img/skill/35121006/info/requireSkill`, `.../psdSkill/35111009`, `Skill/3510.img/skill/35100008/psdSkill/35101010`). The mounted `skill` category still exposes no authored `sSummonedUOL`/`*UOL*` property names. Client evidence stayed second (`CActionMan::GetSummonedProp@0x41b830`, `SKILLENTRY::GetSummonedUOL@0x417f30`, `CActionMan::LoadSummonedAction@0x423100`, `CSummoned::_LoadEffect@0x754050`, `CSummoned::_UpdateEffect@0x754970`, plus `CSkillInfo::LoadSkill@0x70c190` showing `SKILLENTRY::sSummonedUOL` is populated before those consumers read it). This run keeps ownership in the same loader seam and widens recovered hidden-table ingestion for client-data mounts: current-skill table rows still work in direct, nested, grouped, row-indexed, mounted-path, token-named, owner-field, string/UOL, safe-numeric, encoded, relative, row-name encoded, and structured row-name forms; owner-field rows still accept extractor-shaped owner/value aliases (`currentSkillId`, `sourceSkillId`, `summonedSkillId`, `ownerSkill`, `ownerId`, `uolPath`, `summonedUolPath`, `summoned`, `targetPath`) plus owner fields encoded as full mounted skill paths. Sibling hidden-table entries for other skill ids remain rejected so a recovered table branch cannot promote unrelated owners, including structured row-name records whose owner resolves to another skill. Existing visible-data fallback discovery remains intact: fallback root-skill extraction, `.img` image-id guarding, delimiter/token/path linked-id parsing, bounded unknown string/UOL/numeric leaf harvest, nested owner-like `info/*` parsing, direct `WzUOLProperty` linked-id parsing, non-numeric `req`/`psdSkill` value/UOL parsing, and relative context path assembly. Focused source coverage in `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs` now locks mounted-path owner-field ingestion, other-owner rejection, recovered value aliases, current-skill row-name owner/target mappings, structured percent-encoded row-name mappings, and other-owner row-name rejection alongside the existing sidecar, numeric, percent-encoded, escaped path, and hidden-table coverage. Remaining gap: the simulator still does not extract or ship the complete hidden client-owned `SKILLENTRY::sSummonedUOL` table from the client binary; once that data is provided through mounted/client-extracted sidecar rows, the loader accepts the currently modeled table shapes, but summons whose true owner appears only in the unrecovered client field and no sidecar input still require a dedicated extraction/data-mount follow-up before this row can be marked implemented. | Keeping this row explicit prevents summon parity from being overstated by packet/runtime work alone: the loader still owns authoritative summon action/effect source selection, and this pass narrows remaining risk to hidden `sSummonedUOL` table extraction/data availability rather than frame-build metadata, replay behavior, wrapper pruning, visible mixed-token/unknown-leaf link recovery, or ingestion of recovered hidden-table rows once provided. | summon action-loader layer (`SkillLoader`, `SkillLoader.EnumerateClientSummonedUolTableCandidateValues(...)`, `SkillLoader.EnumerateClientSummonedUolTableEntryNameCandidateValues(...)`, `SkillData`, `SummonRuntimeRules`, `CActionMan::GetSummonedProp`, `CActionMan::LoadSummonedAction`, `SKILLENTRY::GetSummonedUOL`, `CSkillInfo::LoadSkill@0x70c190`, `CSummoned::PrepareActionLayer`, `CSummoned::_LoadEffect`, `CSummoned::_UpdateEffect`, `CSummoned::Update`, `CSummonedPool::OnCreated`, `Skill/3510.img/skill/35101005`, `Skill/3510.img/skill/35100008/{psdSkill,info/dummyOf}`, `Skill/3511.img/skill/35111002/summon/attackTriangle`, `Skill/3511.img/skill/35111011/summon/stand`, `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3511.img/skill/35111009/info/affectedSkill`, `Skill/3511.img/skill/35111010/info/affectedSkill`, `Skill/3512.img/skill/35121006/{psdSkill,req,info/requireSkill}`, `Skill/3512.img/skill/35121012/{info/requireSkill,req/35121005}`, `Skill/3310.img/skill/33101005/repeat`, `Skill/434.img/skill/4341006/{summon/repeat,info/requireSkill,req/4331002}`, `Skill/3511.img/skill/35111001/summon/die1`, `Skill/501.img/skill/5010005/summon`, `Skill/1311.img/skill/13111004/summon/hit`, `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs`) |

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

This helper-owner seam is currently under-tracked by the backlog even though both the client and simulator already expose concrete ownership here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow-partner and mirror helper actors | The simulator still keeps the client ownership split (`SkillData` / `SkillManager` still treat Mirror Image `4331002` as mirror-helper owned, while Shadow Partner stays on `special/*` helper-action catalogs), and this pass narrows the mirror live-layer seam again inside `PlayerCharacter`. WZ was rechecked first: `skill/433.img/skill/4331002` still has `action/0 = alert` plus `effect` and still has no `special`, while representative Shadow Partner roots (`skill/411.img/skill/4111002`, `skill/421.img/skill/4211008`, `skill/1411.img/skill/14111000`) still expose authored `special` helper branches. Client evidence stayed second from IDA for this row: `CUser::GetMirrorSrcLayer` returns the five source layers (`UnderCharacter`, `OverCharacter`, `UnderFace`, `Face`, `OverFace`), `CUser::PrepareMirrorActionLayer` recreates helper layers only when missing/flip-mismatched before overlaying/reparenting under `m_pLayerUnderFace`, copying source origin, and seeding rel-move from source-layer current-time + 200 ms, and `CUser::Update` keeps helper layer objects, inserts the current source-layer `IWzCanvas`, then rechecks the helper parent against the current UnderFace parent. Existing prepared-layer behavior remains intact: UnderFace overlay routing, prepared layer-object recreation on flip or scale-filter divergence, source/current-time snapshot tracking, same-facing placement anchoring, hidden-state prepared-layer release, raw-action suppression/back-offset buckets, stale insert-metadata reset on helper-layer object replacement, source/overlay mismatch rejection priority, same-helper-layer live insert re-admission on source-layer timeline advance/regression, source identity/signature changes, missing-time re-entry, incomplete source/overlay/source-identity metadata re-entry, current canvas/frame identity churn, prepared-layer filter/z/color/target-offset churn, source-layer handle/origin/clock metadata churn, source-canvas object metadata churn, source-frame metadata churn, and live source wrapper cloning that keeps the current frame reference. New seam tightening records a simulated overlay-parent signature from the current UnderFace insertion seam and re-admits same-helper-layer live insert when that signature changes or is incomplete, approximating the client `CUser::Update` parent recheck after `InsertCanvas` without widening ownership outside `PlayerCharacter`. Focused regression coverage in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` now also locks overlay-parent metadata churn, missing overlay-parent metadata re-entry, and overlay-parent signature changes when the UnderFace insertion parent changes, alongside the existing canvas/frame-metadata, same-object source-part metadata, timeline, metadata-missing, mismatch-priority, stable-reject, source-layer handle/origin/clock, source-canvas object, prepared-layer metadata, and visibility-owner signature coverage. Remaining gap stays `Partial` and is concentrated further at true native object ownership: the simulator still does not own shared native `IWzGr2DLayer` mirror objects, COM origin/current-time handles, exact `IWzCanvas` object lifetime/reference semantics inside `InsertCanvas`, full native source-layer current-time ownership on shared layer handles, exact GR2 parent/list-node handles after reparent, or native avatar-scale filter handle ownership. | Clone-family jobs are not just tray icons or generic buff effects in the client; they own dedicated avatar-adjacent helper layers that participate in action preparation and per-frame user update. Tracking that seam explicitly keeps future parity work focused on helper-layer timing, ownership, and clone-family differentiation instead of accidentally treating shadow-partner and mirror-image visuals as solved just because the simulator already renders one WZ-backed helper actor. | `PlayerCharacter.cs`, `CharacterAssembler.cs`, `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` (`skill/433.img/skill/4331002/action/0`, `skill/411.img/skill/4111002/special`, `skill/421.img/skill/4211008/special`, `skill/1411.img/skill/14111000/special`, `CUser::PrepareActionLayer`, `CUser::PrepareShadowPartnerActionLayer`, `CUser::PrepareMirrorActionLayer`, `CUser::GetMirrorSrcLayer`, `CUser::Update`, `is_back_action`, `is_alert_back_action`) |

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow Partner action-loader / frame-build parity | The shadow-partner helper row remains loader-owned in the same seam, and this pass narrows fallback frame-build/timing parity again without widening runtime state. WZ evidence stayed first: mounted `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, and `Skill/1411.img/skill/14111000/special/*` still publish the same helper branch families, while mounted `Character/00002000` still exposes concrete helper piece rows with signed `delay` plus optional `move`/`flip`/`rotate` and no additional authored fields beyond the modeled loader surface. Client evidence stayed second: `CActionMan::Init` (`0x41beb0`) still seeds helper action-data rows by walking raw actions `0..272` and skipping raw action `55`, while `CActionMan::LoadShadowPartnerAction` (`0x423cf0`) consumes that action-data surface before runtime playback. Existing landed behavior remains in place for linked-special source resolution, named-over-numeric storage precedence, mixed direct+linked helper frame-child ordering, mounted piece-row parsing, replay-tail handling, mounted hidden-row synthesis, client-init/fallback-only/generic helper fallback admission, resolver-owned non-attack and `ghost*` piece plans, legacy `create0` / `create1` fallback, owner-level and nested owner-level event-delay shaping, piece-local event-delay override, and recovered-source gating for client-init raw rows. This run adds source-gated resolver-owned built-in plans for adjacent mounted `Character/00002000` action-data rows that are parseable but sit just beyond the recovered init loop: `ride2`, `getoff2`, `flamethrower_pre2`, `flamethrower2`, `flamethrower_after2`, `flamethrower_pre`, `flamethrower`, `flamethrower_after`, `gatlingshot`, `battlecharge`, `mRush`, `herbalism_mechanic`, `mining_mechanic`, `gather0`, and `gather1`. Those rows now keep mounted piece ordering, signed-delay/event-delay shaping, `move`, and `flip` metadata when the mounted row is unavailable, but only when the recovered `special/*` source-action set names the same row, so unrelated Shadow Partner skills cannot claim them through generic fallback. Focused regression coverage in `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs` now locks fallback admission for those fifteen rows, concrete synthesized timing/event-delay and transform output for representative rows (`battlecharge`, `gather0`, `gatlingshot`), and bounded source-action rejection/admission for the promoted post-init rows, alongside the existing total-minus-last, negative-delay carryover, owner-level event-delay, nested owner-level event-delay, piece-local override, promoted fallback-only, legacy create, ghost timing, and source-action gating expectations. Remaining gap is narrower and still real: the mounted parseable helper-piece rows now covered by resolver-owned fallback extend beyond the recovered init-loop subset, but hidden client-only helper rows not present in the mounted export remain unrecovered, exact native action-data classification for every post-init action row is not yet fully mirrored, native action-table fields not represented in mounted WZ remain unconfirmed, and client-build/data-set differences in helper UOL/hidden-row ownership can still introduce extra loader-owned paths. | Shadow Partner remains a dedicated loader/runtime split where `CActionMan`-shaped helper action catalogs and frame tables are preserved on the loader side and local/remote helper playback consume that metadata without inventing new runtime ownership. This pass reduces fallback timing/transform drift further by extending resolver-owned fallback plans to source-gated adjacent mounted action-data rows, while keeping mounted rows and resolver-owned piece plans ahead of remapped first-frame fallback. The remaining mismatch is concentrated on hidden rows, exact post-init native classification, and unconfirmed extra action-table fields rather than already-modeled frame-build order/metadata/timing surfaces. | `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `PlayerCharacter.cs`, `RemoteUserActorPool.cs`, `SkillData.cs`, `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs`, `CActionMan::GetShadowPartnerProp@0x41ba60`, `CActionMan::LoadShadowPartnerAction@0x423cf0`, `CActionMan::Init@0x41beb0`, `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, `Skill/1411.img/skill/14111000/special/*`, `Character/00002000.img/{rain,paralyze,shoot6,arrowRain,arrowEruption,chargeBlow,rush,rush2,magic1,magic2,magic3,magic4,magic5,magic6,explosion,iceStrike,burster1,burster2,sanctuary,meteor,blizzard,genesis,brandish1,brandish2,chainlightning,blast,straight,handgun,somersault,doublefire,triplefire,fake,doubleupper,eburster,screw,backspin,eorb,dragonstrike,airstrike,edrain,shot,flashBang,fist,fireburner,coolingeffect,homing,rapidfire,backstep,timeleap,recovery,owlDead,holyshield,resurrection,dash,octopus,darksight,cannon,torpedo,bamboo,wave,blade,souldriver,firestrike,flamegear,stormbreak,shockwave,demolition,snatch,windspear,windshot,swingT2PoleArm,swingP1PoleArm,swingP2PoleArm,combatStep,create0,create1,ride2,getoff2,flamethrower_pre2,flamethrower2,flamethrower_after2,flamethrower_pre,flamethrower,flamethrower_after,gatlingshot,battlecharge,mRush,herbalism_mechanic,mining_mechanic,gather0,gather1}` |

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Time Bomb, Vengeance, and ExJablin packet-application parity | The simulator now owns this `CUserLocal::OnPacket` family inside the existing local-utility packet seam without moving ownership elsewhere. WZ was rechecked first against the mounted export currently exposed through wzimg at `E:\MapleStory\WzExport\v95_2`: `String/Skill.img` still resolves `4341003` as `Monster Bomb` with the `explosion occurs 3 seconds after the charm is activated` description fragment, `31101003` as `Vengeance`, and `4120010` as `Expert Throwing Star Handling` with the `the next attack will always be a Critical Attack` fragment, while `3120010` still resolves to `Ultimate Inferno` instead of a WZ-side `Vengeance` alias. `Skill/434.img/skill/4341003` still publishes the authored Time Bomb `action`, `prepare`, and `special` branches together on the same skill node, and the mounted export still exposes only that one authored `4341003` `Monster Bomb` node for this family. `Item/ItemOption.img` still exposes the wider Time Bomb-shaped option-type-`52` family `020366`, `030366`, `040366`, `030371`, and `040371` with the matching invincibility templates, while other option-type-`52` rows such as `020396` and `040111` still remain unrelated non-invincibility options. Client evidence was rechecked second: `CUserLocal::OnTimeBombAttack` still decodes `(skillId, timeBombX, timeBombY, impactPercent, damage)`, passes the decoded `skillId` directly into `CSkillInfo::GetSkillLevel` and `TryDoingMeleeAttack` without an opcode-to-`4341003` fallback, restores the `1500 ms` base hit period before coat-option merges, gates impact on nonzero `nUserImpactDeg`, calls `CUser::MakeIncDecHPEffect(this, -nDamage, 0)`, and then sets the hit emotion/impact; `CUserLocal::ApplyInvicibleOption` still only mutates runtime invincibility state for `020366`, `030366`, and `030371` even though WZ also ships `040366` and `040371`; `CUserLocal::OnVengeanceSkillApply` still decodes exactly one `Int32` and only branches on legacy packet skill id `3120010`; and `CUserLocal::OnExJablinApply` still reads no payload and only arms `m_bNextShootExJablin = 1`. The landed seam still keeps packet ids `268` / `271` / `272` plus aliases, exact Time Bomb/Vengeance/ExJablin payload validation, signed Time Bomb damage, legacy-only Vengeance admission, nonzero Time Bomb impact gating, packet-origin target selection, the dedicated `SkillManager.TryApplyPacketOwnedTimeBombAttack` branch, the WZ-backed visible-potential item-option merge for `CharacterPart.ItemOptionIds`, and the narrow `MakeIncDecHPEffect` miss/heal mirroring. This pass now also splits visible WZ-family inference from client-owned runtime application inside the same helper path: visible potential lines can still infer the broader authored Time Bomb option family onto `CharacterPart.ItemOptionIds`, but only the client-confirmed `020366`, `030366`, and `030371` ids now feed the local Time Bomb invincibility-duration merge that mirrors `ApplyInvicibleOption`. This pass removes the last inferred Time Bomb alias bridge: an unrecognized first `Int32` in opcode-`268` payload now remains that decoded packet skill id, while known WZ/client aliases still resolve through the existing packet-owned skill-id catalog. Focused regression coverage in `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs` covers exact payload shapes, signed Time Bomb damage, nonzero impact gating, direct unknown Time Bomb skill-id retention, legacy-only Vengeance admission, empty ExJablin admission, the client-applied invincibility-id gate, and the retained broader WZ visible-family filter/order behavior that this row depends on. | These handlers matter because they bypass the normal local input/cast path even though the client gives them their own dispatcher and follow-up state. The simulator keeps a concrete owner for those packets, keeps the shared alias seam anchored to WZ first and client second, no longer accepts trailing bytes that the client packet readers would leave unread, no longer accepts non-client Vengeance packet ids, no longer treats only positive Time Bomb impact values as hit reactions, no longer clamps away signed Time Bomb damage payloads before the local reaction branch, no longer drops the zero/negative `MakeIncDecHPEffect` cases that hang off the same decoded damage field, still resolves visible potential lines through the WZ-authored `ItemOption.img` template/level seam instead of limiting `CharacterPart.ItemOptionIds` population to the confirmed Time Bomb invincibility family, and now also no longer over-applies the WZ-only `040366` / `040371` rows to the live Time Bomb hit-period merge when the client `ApplyInvicibleOption` branch would ignore them. The row is implemented because the remaining unconfirmed opcode-`268` alias bridge has been removed rather than preserved as simulator behavior; unknown decoded Time Bomb skill ids now stay unknown just as the direct client handler indicates. | packet-owned local skill-application layer (`CUserLocal::OnPacket`, `CUserLocal::OnTimeBombAttack`, `CUserLocal::OnVengeanceSkillApply`, `CUserLocal::OnExJablinApply`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::ApplyInvicibleOption`, `CUser::MakeIncDecHPEffect`, `CUserLocal::SetImpact`, `MapSimulator.PacketOwnedUtilityParity`, `LocalUtilityPacketInboxManager`, `SkillManager`, `PlayerCharacter`, `CharacterPart`, `ItemUpgradeUI`, `Character/00002000.img`, `String/Skill.img`, `Skill/434.img/skill/4341003`, `Item/ItemOption.img`, `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs`) |

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Combo counter / combo-command packet parity | `MapSimulator.PacketOwnedComboParity` now owns the simulator-side `OnIncComboResponse` bridge instead of leaving combo implied by the generic temporary-stat tray. The simulator decodes the packet-owned combo count from the combo inbox/chat transport, stores the owner count/timestamp, resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them on the same five-second alpha window, applies the client-matching fly-origin amplitude split across the least-significant digit run, and conditionally spawns the Aran combo-command attack/buff overlays with `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` first and WZ-backed named fallbacks for exports that only ship `ComboSmash`, `ComboDrain`, `ComboFenrir`, `ComboTempest`, and `ComboBarrier`. The row no longer depends on the generic `Combo` temporary-stat heuristics in `SkillManager`; it has a dedicated packet-owned HUD/effect owner, content load path, update lifetime, and draw path in `HaCreator/MapSimulator`. | If combo stays implied only by the temporary-stat tray, the backlog will overstate skill parity while missing the client's actual packet-owned combo counter, visible combo HUD/effect lifetime, and the extra combo-command visuals that hang off learned Aran skills. Tracking it explicitly keeps future work anchored to the real packet and draw owners instead of assuming the generic buff/cooldown surfaces already cover combo gameplay feedback. | packet-owned combo HUD/effect layer (`CUserLocal::OnPacket`, `CUserLocal::OnIncComboResponse`, `CUserLocal::DrawCombo`, `Effect/BasicEff.img/Combo`, `Effect/BasicEff.img/ComboCommand`) |

### 7. Shared skill-use animation-owner parity discovered by targeted `CAnimationDisplayer` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shared skill-use animation-owner parity | `MapSimulator.HandlePlayerSkillCast` still keeps non-melee local `ShowSkillEffect`-style casts on the dedicated direct-owner registration path (`TryRegisterLocalSkillCastThroughClientShowSkillEffectDirectPath`) while explicit caller-owned requests stay on `OnClientSkillEffectRequested -> TryRegisterAnimationDisplayerLocalSkillUseRequest`. WZ was rechecked first: shared cast branches remain on the previously documented row surfaces, recovered summon-monster rows remain the first/third-job bird-family surfaces (`Skill/{310,311}.img/skill/{3101007,3111005}`, `Skill/{320,321}.img/skill/{3201007,3211005}`) plus `Skill/3512.img/skill/35121011`, later taunt summons (`Skill/312.img/skill/3120012`, `Skill/322.img/skill/3220012`) still publish concrete `summon/*` with `info/type=33` but no recovered summon-monster caller ownership, and effect-only type-33 rows such as `Skill/3510.img/skill/35101005` still have no summon placement branch. Client evidence stayed second: `CUser::ShowSkillEffect` still carries caller-owned `nActionSpeed`, `bLeft`, `nLast`, and `pPtOffset`; `CUserLocal::DoActiveSkill_SummonMonster@0x93eff0` still owns fixed `nActionSpeed=6`, fixed `bLeft=0`, `nLast=-1`, placement offset creation, `125 * direction` seed distance, map-boundary clamp, foothold search/backstep, swim/fly bypass, and silent returns before placement when the owner is on a ladder/rope or lacks foothold/swim/fly support. This pass keeps changes in the same `SkillManager` seam and adds that recovered silent support gate for summon-monster-family rows only: `ShouldRejectClientDoActiveSummonMonsterFamilyStateWithoutMessageCore(...)` now rejects recovered ids `3101007`, `3111005`, `3201007`, `3211005`, and `35121011` without a generic summon message when ladder/rope or no support would make the client return before placement, while swim/fly support still admits the placement owner path and unrecovered type-33 summons (`3120012`, `3220012`, `5010005`) keep generic summon support messaging. Existing behavior remains intact: explicit summon-monster id mapping and summon-branch metadata gating still prevent type-only over-admission, placement distance still falls back to client `125` only for recovered summon-monster rows without authored distance, boundary clamp can land on the owner, authored distance precedence and non-summon authored-only distance remain intact, remote `OnHit` damaged-condition over-admission remains rejected, and explicit owner shaping still carries `EffectBranchLastIndex`/`FacingRightOverride`, summon-family fixed-zero `bLeft`, mine fixed-zero `bLeft`, default `nLast=0x7FFFFFFF`, and null `pPtOffset` where recovered. Focused coverage in `UnitTest_MapSimulator/SharedSkillUseAnimationOwnerParityTests.cs` now also locks silent missing-support rejection and swim/fly allowance for recovered summon-monster rows plus non-inheritance by unrecovered type-33 summons, alongside the prior placement, metadata, remote-hit, mine, and Monster Magnet assertions. Remaining gap: explicit client skill-id-to-caller mapping for any other `DoActiveSkill_SummonMonster` permutations is still incomplete beyond recovered ids `3101007`, `3111005`, `3201007`, `3211005`, and `35121011`; placement-owner details beyond recovered support gating, foothold search, swim/fly bypass, map-boundary clamp, vertical settle, and back-search remain approximated for unrecovered per-caller branches; captured-mob/random-mob packet payload ownership from `DoActiveSkill_SummonMonster` is not yet modeled in this `ShowSkillEffect` row; and overlay-parent plus auto-flip ownership remain approximated beyond follow/fixed-facing options. | Shared cast presentation remains anchored to `CAnimationDisplayer::Effect_SkillUse` with explicit direct-vs-request ownership boundaries and caller-owned `nLast`/`bLeft`/`pPtOffset` shaping in the existing seam. The explicit summon-monster owner map covers the recovered first/third-job bird-family rows plus `35121011`, now including the recovered silent pre-placement support rejects, while leaving later taunt and unrelated type-33 summons outside the placement-offset owner path. The row remains `Partial` because unrecovered summon-monster caller permutations, captured-mob packet payload ownership, and deeper overlay/auto-flip ownership are still approximated. | shared skill-use animation layer (`CAnimationDisplayer::Effect_SkillUse`, `CAnimationDisplayer::LoadLayer`, `CAnimationDisplayer::RegisterOneTimeAnimation`, `CUser::ShowSkillEffect`, `CUser::ShowSkillSpecialEffect`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::DoActiveSkill_Heal`, `CUserLocal::DoActiveSkill_TownPortal`, `CUserLocal::DoActiveSkill_Summon`, `CUserLocal::DoActiveSkill_SummonMonster`, `CUserLocal::DoActiveSkill_SmokeShell`, `CUserLocal::SetDamaged`, `CUserRemote::OnHit`, `SkillUseEffectRequest`, `MapSimulator.HandlePlayerSkillCast`, `MapSimulator.TryRegisterLocalSkillCastThroughClientShowSkillEffectDirectPath`, `MapSimulator.AnimationDisplayerParity`, `SkillManager.ResolveClientLocalShowSkillEffectPointOffsetOverride`, `SkillManager.ResolveClientLocalShowSkillEffectPlacementDistanceX`, `SkillManager.ResolveClientLocalShowSkillEffectSummonMonsterPlacementPositionForTesting`, `SkillManager.ShouldRejectClientDoActiveSummonMonsterFamilyStateWithoutMessageForTesting`, `SkillManager.HasClientShowSkillEffectSummonPlacementMetadata`, `SkillManager.ResolveClientShowSkillEffectPointOffsetFromWorldPositions`, `SummonMovementResolver.ResolveSpawnPositionForInstance`, `RemoteUserActorPool.ShouldTryRegisterRemoteHitDamageReactiveSpecialEffect`, `SkillManager.ResolveClientLocalShowSkillEffectRequestedBranchNames`, `PreparedSkillHudRules.TryResolveClientKeyDownEndSkillEffectRequestId`, `SkillManager.ResolveClientKeyDownEndRequestedBranchNames`, `Skill/434.img/skill/4341003/special`, `Skill/434.img/skill/4341007/special`, `Skill/122.img/skill/1220006/special`, `Skill/230.img/skill/2301002/specialAffected`, `Skill/3212.img/skill/32121003/special`, `Skill/532.img/skill/5320007/specialAffected`, `Skill/572.img/skill/5720005/specialAffected`, `Skill/3500.img/skill/35001001/keydownend`, `Skill/3510.img/skill/{35101004/effect,35101005/effect,35101009/keydownend}`, `Skill/3511.img/skill/35111004/effect`, `Skill/3512.img/skill/{35121005,35121011,35121013}/{effect,summon}`, `Skill/3310.img/skill/{33101004,33101008}`, `Skill/{310,311}.img/skill/{3101007,3111005}/summon`, `Skill/{320,321}.img/skill/{3201007,3211005}/summon`, `Skill/{312,322}.img/skill/{3120012,3220012}/summon`, `UnitTest_MapSimulator/SharedSkillUseAnimationOwnerParityTests.cs`) |

### 8. Bullet animation-owner parity discovered by follow-up `CAnimationDisplayer` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Normal, magic, and afterimage projectile animation owner | WZ was rechecked first on this row's projectile-owned surfaces and still confirms authored frame metadata on the same branches the seam consumes: `Skill/200.img/skill/2001004/ball` keeps per-frame `origin`/`delay`, `Skill/230.img/skill/2301005/ball` keeps explicit authored frame branches with `origin`, `Skill/434.img/skill/4341003/flipBall` keeps the clockwise flipped branch, and representative normal/cash ammo nodes still keep direct numeric bullet-frame children on `02060000/bullet` and `05021000/bullet` with no exposed item-side specialization branch. Client evidence stayed second: `CAnimationDisplayer::RegisterBulletAnimation` (`0x455410`) still registers `NormalBullet` into the bullet container with explicit `tStart/tEnd` + source/destination/target + z + effect UOL + weapon/bullet ids; `RegisterMagicBulletAnimation` (`0x455570`) still inserts only when the ball UOL string is non-empty; `CAnimationDisplayer::MagicBullet::MagicBullet` (`0x447910`) still seeds `CAfterImageBullet` with `200 ms` repeat interval and `200` start alpha; `CBullet::NeedsUpdate` (`0x476e80`) admits only `tCur - tStart > 0` and `tCur - tEnd < 0`; and `CAfterImageBullet::Update` (`0x477aa0`) still clones the active bullet layer into repeat child layers, rebinds origin/alpha vectors, copies the source canvas, calls `Animate(GA_STOP)`, and registers repeat animation state. This pass keeps implementation in the same owner seam and tightens two remaining timing edges: main-layer draw admission now mirrors `CBullet::NeedsUpdate`'s strict start tick, so a bullet is not visible and cannot register afterimage repeats on the exact `tStart` tick while still stopping at `tEnd`; and repeat-layer registered animation lifetime now detects wrapped `RegisteredAnimationStartTime`/`RegisteredAnimationEndTime` through `ResolveBulletAnimationOwnerTickElapsedMs`, so registered repeat state remains authoritative across `Environment.TickCount` rollover instead of falling back to the simulator `Duration` field when `end < start`. Existing row behavior remains in place: registered owner timeline duration still clamps runtime clocks by authored loaded bullet/effect animation duration; non-authored normal-owner fallback still uses the registration source/destination distance clock clamped with projectile lifetime; runtime owner position still prefers registered source/destination interpolation whenever those anchors are finite; sync attachment remains active for finite registered timelines even when gameplay projectile coordinates are temporarily invalid; owner activation and stop capture keep finite-anchor fallback guards; stop capture remains a hard one-way detach from gameplay projectile identity; owner removal waits for registered timeline completion plus afterimage drain; repeat-layer parent/source ownership stays active-main-layer first with previous-repeat fallback; copied source-canvas frame snapshots, source-canvas alpha fallback, alpha-vector progress, origin-shift progress, registered UOL-driven `ball`/`flipBall` resolution, whitespace-gated magic registration, fire-time queued use-vs-cash lane retention, fire-time weapon-context retention, weapon-key fallback with derived `(weaponItemId / 10000) % 100`, clockwise bounds shaping, and afterimage interval/origin-shift continuity remain unchanged. Focused coverage in `UnitTest_MapSimulator/AnimationDisplayerProjectileOwnerParityTests.cs` now also locks strict start-tick main-layer/afterimage admission and wrapped registered repeat lifetime overriding fallback duration, alongside the existing bullet-owner coverage for tick rollover, authored-duration clamping, distance/lifetime fallback, invalid-input fallback, detached no-resync gating, active-main-layer repeat parent/source identity, timeline-position preference, finite-anchor sync retention, owner initial/stop fallback guards, afterimage origin guards, origin-shift preservation, active-main-layer frame-alpha use, no-main-layer fallback alpha-vector non-double-application, previous-repeat source-canvas alpha/frame fallback, normal effect-layer ownership, item bullet branch resolution, and magic afterimage gating. Remaining gap: WZ still exposes no additional item-bullet specialization branch on the representative normal/cash ammo surfaces, so the simulator still cannot reconstruct the client's hidden/exhaustive native `CBullet` specialization graph or literal `CAfterImageBullet::Update` COM layer handles, COM ref-counting, `IWzGr2DLayer::InsertCanvas` object identity, and true native repeat-registration ownership/lifetime semantics; the current repeat-layer model is a stronger simulator-owned approximation, not a 1:1 native GR2 layer graph. | Projectile presentation remains explicitly owner-split from gameplay projectile state while honoring authored frame timing, registered source/destination travel, explicit detach semantics, client strict-start main-layer admission, tick-wrap-stable owner/repeat timing, registered repeat lifetime precedence, normal effect-layer ownership, active-main-layer-first repeat ownership, and previous-repeat source-canvas fallback when main-layer identity is unavailable. The row remains `Partial` because native bullet specialization tables and literal COM repeat-layer identity/lifetime semantics are still unrecovered. | projectile animation layer (`CAnimationDisplayer::RegisterBulletAnimation`, `CAnimationDisplayer::RegisterMagicBulletAnimation`, `CAnimationDisplayer::MagicBullet::MagicBullet`, `CAfterImageBullet::Update`, `CBullet::NeedsUpdate`, `CBullet`, `CAnimationDisplayer::NormalBullet`, `CAnimationDisplayer::MagicBullet`, `SkillManager.SpawnProjectile`, `SkillManager.RefreshQueuedProjectileFireTimePresentation`, `SkillManager.DrawProjectiles`, `SkillManager.ResolveBulletAnimationOwnerTickElapsedMs`, `SkillManager.HasBulletAnimationOwnerTickReached`, `SkillManager.ResolveBulletAnimationPresentationDurationMs`, `SkillManager.ResolveBulletAnimationDistanceDurationMs`, `SkillManager.ResolveBulletAnimationPresentationAuthoredDurationMs`, `SkillManager.ResolveBulletAnimationInterpolatedPosition`, `SkillManager.ResolveBulletAnimationOwnerInitialPosition`, `SkillManager.TryResolveFiniteBulletAnimationPosition`, `SkillManager.ResolveBulletAnimationOwnerStopPosition`, `SkillManager.ShouldUseRegisteredBulletAnimationTimelinePosition`, `SkillManager.HasFiniteBulletAnimationTimelineAnchors`, `SkillManager.ShouldSyncBulletAnimationOwnerFromProjectile`, `SkillManager.ShouldRemoveBulletAnimationOwner`, `SkillManager.ResolveBulletAfterimageParentRepeatLayerObjectId`, `SkillManager.ResolveBulletAfterimageSourceLayerObjectId`, `SkillManager.ResolveBulletAfterimageLayerFrameSnapshot`, `SkillManager.ResolveBulletAfterimageLayerFrameAlphaMultiplier`, `ActiveBulletAnimationOwner.CanDrawMainAnimation`, `ActiveBulletAnimationOwner.IsDetachedFromProjectile`, `ProjectileAfterimageLayer.IsExpired`, `ProjectileAfterimageLayer.ResolveAlphaVector`, `ProjectileAfterimageLayer.ResolveOriginShift`, `SkillManager.ResolveRegisteredBulletPresentationVisualItemId`, `SkillManager.ResolveRegisteredBulletPresentationWeaponItemId`, `SkillManager.ResolveRegisteredBulletPresentationWeaponCode`, `SkillManager.ResolveBulletLayerDrawX`, `SkillManager.ShouldUseBulletLayerClockwiseBounds`, `SkillManager.ShouldRegisterMagicBulletAnimation`, `SkillLoader.LoadSkillAnimationByNormalizedPath`, `SkillLoader.LoadItemBulletAnimation`, `SkillLoader.ResolveItemBulletAnimationProperty`, `SkillLoader.EnumerateItemBulletAnimationBranchKeys`, `UnitTest_MapSimulator/AnimationDisplayerProjectileOwnerParityTests.cs`, `Skill/200.img/skill/2001004/ball`, `Skill/230.img/skill/2301005/ball`, `Skill/434.img/skill/4341003/flipBall`, `02060000/bullet`, `05021000/bullet`) |

### 9. Secondary skill animation-owner parity discovered by broad `CAnimationDisplayer` function scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Hook-chain, foothold, prepare, motion-blur, chain-lightning, and Tesla animation owners | The simulator keeps this owner family in dedicated secondary-owner seams (`AnimationEffects` prepare/foothold/hook-chain/chain-lightning/Tesla plus packet-owned/remote active-effect motion blur) and no longer treats it as a missing bucket. WZ evidence for this row remains the same: `Skill/112.img/skill/1121001/info/pullTarget = 1` with authored `skill/1121001/mob` hook-chain canvases, `Skill/222.img/skill/2221006/ball/{0,1,2}` chain-lightning variants, `Skill/3511.img/skill/35111002/summon/attackTriangle` Tesla triangle, and `Item/Cash/0501.img/05010044/effect` publishing `spectrum=1`, `delay=600`, `interval=100`, `alpha=224` with `follow` absent (default `0`). Client evidence remains second: `CUserLocal::TryDoingMonsterMagnet` routes hook-chain with skill-owned UOL plus facing-derived owner offset and `>= 660 ms` attack-window clamp; `Effect_SkillPrepare` / `RegisterPrepareAnimation` keeps character-owned replacement behavior; and `CUser::SetActiveEffectItem` + `RegisterMotionBlurAnimation` read `spectrum/follow/delay/interval/alpha`, store `alpha` through the low byte, terminate previous spectrum state, copy the five avatar layer handles (`Face`, `OverFace`, `UnderFace`, `OverCharacter`, `UnderCharacter`), and register the blur in `m_MotionBlur`. Runtime parity remains in-place for local prepare, Monster Magnet hook-chain admission, remote Monster Magnet hook-chain registration, foothold `tile`, chain-lightning `ball`, Tesla `summon/attackTriangle`, local and remote active-effect motion-blur owner capture, WZ interval/delay/alpha fade, move-path duration-tail sample windows, passive-history precedence, stable five-layer draw order, persistent simulated handle ids across re-register/remove, follow-position/facing behavior, and clear-on-replace/remove lifecycle. This pass tightens the simulated native lifetime evidence again: local motion-blur owner termination remains idempotent for registered overlay/layer references, remote motion-blur snapshot teardown still releases copied repeat-animation state alongside copied source-layer and snapshot-layer references, local plus remote shared-handle traces still aggregate AddRef-style counts by simulated layer identity when two avatar slots point at the same layer handle, shared-handle owner release traces still decrement remaining ref counts in client capture order, reference-copy/release operations still follow the client five-layer capture order even when remote snapshots are retained in draw order for rendering, and local plus remote traces now separately model the temporary registration argument lifetime from `CUser::SetActiveEffectItem`/`RegisterMotionBlurAnimation`: overlay by-value AddRef/release, five-layer `apLayer` temporary capture, copy into `m_MotionBlur`, and caller-array release. Focused coverage in `UnitTest_MapSimulator/ActiveEffectMotionBlurParityTests.cs` now also locks shared native-layer handle ref-count accumulation and release-count decrement for local and remote motion blur, temporary registration argument AddRef/release order for local and remote shared handles, and remote copy-order precedence over draw-order storage, alongside idempotent local owner-reference release, remote repeat-animation release on snapshot expiry, cadence, alpha, sample-owner, layer-handle, draw-order, ref-count, and Monster Magnet hook-chain assertions. Remaining gap: local and remote motion-blur owners still emulate client behavior with simulator-managed snapshots and simulated native-lifetime traces rather than literal client `IWzGr2DLayer` / GR2 `AnimationState` COM objects, so unmanaged layer identity beyond the simulated handle map, exact COM failure/exception edge cases, and ownership inside the real GR2 layer graph remain approximated instead of 1:1 mirrored. | Skill and summon gameplay can be correct while the visible client presentation is still wrong if these managers stay folded into generic projectiles or one-shot skill effects. Keeping the row explicit prevents Monster Magnet chains, ground/foothold attack strips, key-down prepare layers, active-effect blur, chain-lightning, and Tesla visuals from being treated as already covered by `Effect_SkillUse`, bullet animation, or summon runtime parity. | secondary skill animation layer (`CAnimationDisplayer::RegisterHookingChainAnimation`, `CAnimationDisplayer::RegisterChainlightningAnimation`, `CAnimationDisplayer::RegisterTeslacoilAnimation`, `CAnimationDisplayer::RegisterFootHoldAnimation`, `CAnimationDisplayer::RegisterMotionBlurAnimation`, `CAnimationDisplayer::Effect_SkillPrepare`, `CAnimationDisplayer::RegisterPrepareAnimation`, `CUserLocal::TryDoingMonsterMagnet`, `CUser::OnEffect`, `CUser::SetActiveEffectItem`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::TryDoingShootAttack`, `CUserLocal::TryDoingMagicAttack`, `CUserRemote::OnMeleeAttack`, `CUserRemote::OnShootAttack`, `CUserRemote::OnMagicAttack`, `CSummoned::OnAttack`, `CSummoned::ShowTeslaCoilTriangle`, `CMob::LoadLayer`, simulator seams: `AnimationEffects`, `SkillLoader`, `SkillManager`, `RemoteUserActorPool`, `ActiveEffectItemMotionBlurResolver`, `MapSimulator.AnimationDisplayerParity`, `UnitTest_MapSimulator/ActiveEffectMotionBlurParityTests.cs`) |

### 10. Local affected-area attack ownership discovered by broad IDA function scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Local attack-owned affected-area lifecycle parity | WZ was rechecked first on this row-owned seam and still supports the split between authored affected-area metadata and client-owned lifecycle branches: explicit local-owner rows keep authored `info/type` where recovered (`3111003=2`, `12111003=1`, `12111005=4`, `2121007=1`, `2221001=1`, `2221007=1`, `2321008=1`, `22161001=1`, `22181002=1`, `32121004=1`, `4211006=1`, body family `32121003=11`, teleport-mastery family `2111007/2211007/2311007/32111010=16`), while known non-owner neighbors remain rejected (`12111004/1321007` summon `type=33`, `2121006/2221006` neighboring `type=1`, and `4221006/32121006` `zoneType=invincible`). WZ also keeps the same element and shape evidence (`12111003/12111005/2121007/3111003` root `elemAttr=f` with burn dot metadata, `2221007` ice/frostbite, `2321008` holy, `3111003` and `32121003` `info/rectBasedOnTarget=1`, and `12111005` type-4 `common/time=10+x` with no authored `info/delay`). This pass rechecked the previously over-broad explicit magic subset: `2221001` still publishes `info/type=1` and `common/time=2+u(x/10)` but only a `special` presentation branch, while `22161001`/`22181002` keep explicit branch ids and no authored local tile/affected-area surface (`22181002` has an `effect` presentation canvas and both lack `common/time` for area lifetime). Client evidence stayed second: broad xrefs to `CAffectedAreaPool::GetInstance@0x52a480` still land in `CUserLocal::{TryDoingMeleeAttack@0x91e780,TryDoingShootAttack@0x925a00,TryDoingMagicAttack@0x92a240,TryDoingBodyAttack@0x930710,DoActiveSkill_MesoExplosion@0x942200}`; `TryDoingMagicAttack` still keeps the `12111005` exclusive preflight through `CAffectedAreaPool::IsAbleToInsertExclusiveArea`, still keeps the affected-area duration subset (`12111003/2121007=>3000`, `2221007/2321008/32121004=>2760`), and now confirms `2221001`, `22161001`, and `22181002` as presentation-only branches that leave the affected-area insertion path. Implementation remains in the existing seams: local dispatch still flows only through `SkillManager.OnLocalAttackAreaResolved`; explicit-owner lane resolution remains first; known non-owner neighbors stay hard-rejected; non-explicit candidates still require authored `info/type` plus area metadata; body non-explicit admission remains constrained; meso explicit routing for `4211006` remains; target-bound shaping remains scoped to recovered `3111003` and `32121003`; `12111005` exclusive insert preflight still rejects overlapping live/delayed player-skill type-4 areas while allowing expired/removing/non-player/non-type-4 areas; stable local object ids and cancel-family teardown remain. New runtime tightening stops the presentation-only explicit magic branches (`2221001`, `22161001`, `22181002`) from becoming local affected-area lifecycle candidates even though their WZ metadata/type helper values remain readable for branch documentation. Focused coverage in `UnitTest_MapSimulator/LocalAttackOwnedAffectedAreaParityTests.cs` now locks that presentation-only split in addition to prior lane precedence, explicit type/phase/duration/start-delay/element fallback, cross-lane non-owner rejection, target-bound shaping, exclusive preflight, delayed-overlap rejection, and overlap allowance cases. Remaining gap: exact native branch tables are still not mirrored 1:1 for every `TryDoing*` / `DoActiveSkill_MesoExplosion` switch branch outside the recovered subset; phase/native packet-shape details remain approximate; and any still-hidden affected-area element/status-token families beyond the currently recovered explicit rows and compact-token guard still need client-backed mapping. | Without this split, affected-area work can look complete because packet-owned remote areas are covered while local skill-cast area ownership remains implicit. Keeping this row separate prevents local player attack-side area spawning from being conflated with remote area packet lifecycle parity. | local skill/attack affected-area layer (CAffectedAreaPool::GetInstance, CUserLocal::TryDoingMeleeAttack, CUserLocal::TryDoingShootAttack, CUserLocal::TryDoingMagicAttack, CUserLocal::TryDoingBodyAttack, CUserLocal::DoActiveSkill_MesoExplosion, simulator seams: SkillManager, SkillManager.ResolveLocalAttackAreaOwnerLane(...), PlayerManager, MapSimulator, AffectedAreaPool.IsAbleToInsertExclusiveArea, UnitTest_MapSimulator/LocalAttackOwnedAffectedAreaParityTests.cs, and backlog-5 packet-owned AffectedAreaPool rows) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, movement-coupled shoot families, and the shared `Effect_SkillUse` plus secondary `CAnimationDisplayer` animation owners rather than extending the generic cast path indefinitely.
3. Summon actor/runtime pass:
   Close the missing `CActionMan::LoadSummonedAction` loader seam together with the packet-owned `CSummonedPool` gaps and the `CSummoned::Update`-owned movement, touch-damage, Tesla-state, and position-sync gaps instead of only extending attack metadata and per-skill summon heuristics.
4. Clone-helper parity pass:
   Tighten the dedicated `CUser::PrepareActionLayer` / `CUser::Update` helper-layer seam for shadow-partner and mirror-image families instead of leaving clone behavior folded into generic buff or summon work.
5. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
6. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
7. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
