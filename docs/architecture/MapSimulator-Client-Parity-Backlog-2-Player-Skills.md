# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`
- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`
- `CAnimationDisplayer::Effect_SkillUse` at `0x459100`
- `CAnimationDisplayer::RegisterBulletAnimation` at `0x455410`
- `CAnimationDisplayer::RegisterMagicBulletAnimation` at `0x455570`
- `CAnimationDisplayer::RegisterHookingChainAnimation` at `0x45cbd0`
- `CAnimationDisplayer::RegisterChainlightningAnimation` at `0x45d1b0`
- `CAnimationDisplayer::RegisterTeslacoilAnimation` at `0x45d650`
- `CAnimationDisplayer::RegisterFootHoldAnimation` at `0x45a990`
- `CAnimationDisplayer::RegisterMotionBlurAnimation` at `0x45ad00`
- `CAnimationDisplayer::Effect_SkillPrepare` at `0x45b840`
- `CAnimationDisplayer::RegisterPrepareAnimation` at `0x45b420`
- `CAfterImageBullet::Update` at `0x477aa0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Player skill execution parity

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
`CUserLocal::Update` at `0x937330` orchestrates several of these paths and confirms that key-down gauge updates, queued follow-up attacks, prepared-skill execution, repeat-skill handling, teleport, rush, flying-rush, rocket-booster start/end handling, movement-coupled shoot branches, timer-driven cancellations, and status-bar feedback are coordinated in one local-player update loop rather than isolated one-off handlers. A second IDA pass also showed that summon parity splits across three owners rather than only the actor/runtime shell: `CSummoned::Update` drives summon actor motion, active/passive vec-ctrl updates, body-contact damage checks, assist-type dispatch, Tesla Coil state handling, self-destruct timing, and final world-position sync; `CSummonedPool::OnPacket` dispatches created/removed/move/attack/skill/hit packets and `CSummonedPool::OnCreated` ties each summon to its owning user, skill id, level data, and local-user registration path; and `CActionMan::{GetSummonedProp,LoadSummonedAction}` own the WZ-side summon UOL lookup, per-action frame build, delay/z metadata, reverse-playback duplication, and cached action-entry reuse before the runtime ever updates or draws that actor.

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
This row remains `Partial`. The earlier backlog text tracked summon packet ownership and runtime behavior, but the targeted `CActionMan` scan shows another concrete client owner that was still missing from the document: summon action loading itself. WZ was rechecked first and the already-landed loader/frame-build parity still holds: `Skill/3510.img/skill/35101005` still exposes wrapper nodes beside real summon branches (`cDoor`, `sDoor`, `oDoor`, `mDoor`, `eDoor`), `Skill/501.img/skill/5010005/summon` still exposes authored custom action branches (`piratebless`, `rushBoom`, `cannonSlam`, `noiseWave`, `jump`, `hit1`) beside `attack1`-`attack4`, `Skill/434.img/skill/4341006/summon` still remains an effect/repeat-only layout while `info/requireSkill = 4331002` still proves a visible prerequisite-style owner link on the same skill surface, and the mounted non-summon roots that feed this seam still expose deep visible-link leaves such as `Skill/3512.img/skill/35121006/info/requireSkill`, `Skill/3512.img/skill/35121006/psdSkill/35111009`, and `Skill/3510.img/skill/35100008/psdSkill/35101010`.

Client evidence stayed second: `CActionMan::GetSummonedProp` at `0x41b830` still resolves the summon UOL from `SKILLENTRY::GetSummonedUOL`, `SKILLENTRY::GetSummonedUOL` at `0x417f30` still only returns `this->sSummonedUOL`, and the same getter still rechecks as the asset base for `CActionMan::LoadSummonedAction` plus summon-owned `/effect`, `/repeat`, `/effect0`, and `/repeat0` effect loading in `CSummoned::{_LoadEffect,_UpdateEffect}`. This pass tightens the loader seam again without widening runtime ownership: when a client-style summon UOL now lands on a deep property under a mounted skill root instead of on the root itself, `SkillLoader.TryResolveClientSummonedUolProperty` retries that resolved root skill node as a summon source before falling back to the existing visible-link graph. That preserves direct root-owned summon recovery for paths under real summon owners and standalone summon roots while leaving the existing `affectedSkill`, `psdSkill`, reverse affected/passive parent discovery, `dummyOf`, and requirement graph in place for intermediate non-summon roots. Focused source coverage is now present in `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs` for deep client-UOL leaf paths under summon-child roots, standalone summon roots, mounted root-skill id parsing from those deep paths, and the non-skill-path empty fallback.

The remaining gap is narrower and still concentrated on the hidden client-owned asset-resolution field itself: the simulator now covers more of the reachable visible graph from both the original skill surface and any resolved client-UOL target root, but the client still consumes the precomputed `SKILLENTRY::sSummonedUOL` string directly. The exact authoring/population path for that field still needs a follow-up client-data pass before this row can be marked implemented, especially for summons whose true owner never appears on any visible WZ-side link surface at all.

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::Update` at `0x8fb8d0`

Notes:
This row remains `Partial`. The targeted re-check surfaced a client-owned helper seam that the backlog was not tracking explicitly, and the simulator now mirrors more of that ownership split instead of treating every clone-family helper as the same WZ-backed shadow-partner path. WZ was rechecked first for `skill/433.img/skill/4331002`: it still exposes `action/0 = alert` plus effect data but no `special/*` helper action branch, which keeps `Mirror Image` (`4331002`) on the assembled-avatar helper seam while Shadow Partner continues to use the WZ-authored helper action catalog. Client evidence stayed second: `CUser::PrepareActionLayer` still calls `CUser::PrepareShadowPartnerActionLayer` and `CUser::PrepareMirrorActionLayer` immediately after the main avatar action layer is prepared; `CUser::GetMirrorSrcLayer` still returns the five avatar layer handles in fixed index order (`UnderCharacter`, `OverCharacter`, `UnderFace`, `Face`, `OverFace`); and `CUser::PrepareMirrorActionLayer` still recreates a mirror helper layer only when that helper layer is missing or its flip diverges from `m_pLayerUnderFace`, then re-parents every recreated mirror plane against `m_pLayerUnderFace`, copies origin from the live source layer, seeds the 200 ms `RelMove` from the live source-layer current time, and uses the live source-layer canvas instead of a per-piece redraw.

The simulator already kept the earlier landed ownership split and render seam for this row, and this pass narrows the remaining mismatch again: `SkillData` / `SkillManager` still recognize `Mirror Image` as a distinct helper owner, `PlayerCharacter` still keeps mirror runtime state separate from the `special/*` Shadow Partner actor, mirror suppression still follows the client-backed raw-action and state gates, and the dedicated prepared five-layer cache still inserts the full mirror package at the shared UnderFace overlay boundary. This pass now aligns the prepared-layer lifetime and transition timing more closely with the newly rechecked client seam: same-facing source refreshes still keep the prepared mirror plane's recreate timestamp even when the observed player action changes and the assembled source signature refreshes, preserved helper planes still reuse the existing prepared render target whenever the refreshed snapshot fits inside it, and the simulator now keeps a separate prepared transition-start timestamp that derives new helper-plane rel-move progress from the live source-layer current-time snapshot instead of always restarting from helper recreation time. `PlayerCharacter` still records the logical snapshot source rectangle separately from the reusable render target so shrink-or-fit refreshes keep drawing against the current prepared bounds/origin rather than the stale texture extent, still preserves the prepared placement anchor across same-facing canvas refreshes even when the incoming canvas bounds grow and the backing render target has to be replaced, and now also keeps the prepared mirror planes resident across temporary suppression states instead of eagerly discarding them the moment ladder/rope/morph/taming/hit/dead gating hides the helper. That keeps the simulator-owned prepared mirror plane closer to the client split where `PrepareMirrorActionLayer` preserves the helper layer object, seeds `RelMove` from the live source-layer timing, and `CUser::Update` keeps inserting the current source-layer canvas into that same helper layer instead of rebuilding it on every visibility or source-canvas change. Focused regression coverage for the helper-start precedence, prepared-layer recreate-time preservation, prepared source-layer transition timing, bounds-fit in-place snapshot refresh, prepared snapshot source-rect fallback, prepared placement-anchor preservation, and live-source seam is now on disk in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs`.

The remaining gap is narrower and still concentrated at the real live-layer-object boundary inside `CUser::PrepareMirrorActionLayer` / `CUser::Update`: the simulator still does not own shared `IWzGr2DLayer` mirror objects, true COM live-layer origin/current-time handles, or true `InsertCanvas` reparenting against the client layer graph, and its preserved prepared planes are still simulator-owned snapshots rather than literal helper objects whose identity survives every texture replacement the way the client keeps the same layer handle and swaps only the inserted source canvas. Follow-up clone-helper behaviors that depend on those live client layer objects rather than simulator-owned prepared plane snapshots also remain outside the current seam.

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::GetShadowPartnerProp` at `0x41ba60`
- `CActionMan::LoadShadowPartnerAction` at `0x423cf0`

Notes:
The current skill backlog already names the shadow-partner runtime/helper surface, but the targeted `CActionMan` pass shows there is still a distinct loader owner underneath it. `GetShadowPartnerProp` resolves the helper property from the skill id before action lookup, while `LoadShadowPartnerAction` caches by `(skillId, action)`, resolves either the named raw-action branch or the numeric fallback branch through `get_action_name_from_code`, enumerates the action canvases into cached frame entries, reads frame-owned metadata from the canvas properties, and appends the mirrored interior-frame tail when the loader's replay table marks that action. That means shadow-partner parity is split the same way as summons, pets, employees, dragons, mobs, NPCs, and taming-mob helpers elsewhere in the backlog set: `CUser::PrepareShadowPartnerActionLayer` and the simulator helper runtime own placement and playback state, while `CActionMan` owns the helper action catalog and the frame table those runtime paths consume.

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another client-owned skill family that was not represented anywhere in the backlog text. `OnTimeBombAttack` is not just a generic damage callback: the decompile shows it decoding a skill id plus attack/action data, resolving the local skill level, calling `TryDoingMeleeAttack`, then applying hit-period/invincibility and impact side effects before forcing the avatar hit reaction. `OnVengeanceSkillApply` is also more specific than a plain temporary-stat refresh because it gates on skill id `3120010` and then calls `DoActiveSkill_MeleeAttack` through the local skill runtime, while `OnExJablinApply` arms the `m_bNextShootExJablin` flag for the next ranged-shot branch rather than behaving like a generic buff bit. That is a dedicated packet-owned skill-application seam between the broad cast/update families and the existing temporary-stat or helper rows.

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`

Notes:
The same targeted `CUserLocal::OnPacket` scan surfaced another local skill owner that the backlog still was not naming. `OnIncComboResponse` is very small but concrete: it decodes the server-authored combo count, stores it in `m_nCombo`, stamps `m_tLastSetCombo`, and immediately routes through `DrawCombo`. The `DrawCombo` decompile then shows that combo is not just another temporary-stat label. The client resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them alive on a five-second alpha window, applies fly-origin motion to the combo vectors, and conditionally spawns extra `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` layers when the corresponding Aran combo-command skills are learned. A repo search across `HaCreator/MapSimulator` still only surfaced the generic `Combo` temporary-stat labeling in `SkillManager` and did not surface any simulator-side combo packet owner, combo HUD/effect loader, or `ComboCommand` runtime, so this remains a real missing parity bucket rather than stale wording.

### 7. Shared skill-use animation-owner parity discovered by targeted `CAnimationDisplayer` scan

- `CAnimationDisplayer::Effect_SkillUse` at `0x459100`

Notes:
The broader skill rows already cover cast logic, hit effects, prepared gauges, and helper actors, but the latest `CAnimationDisplayer` pass exposed another owner that the backlog corpus was not naming anywhere else. `Effect_SkillUse` is not a generic data lookup. The decompile shows it formatting a StringPool-backed effect UOL pattern for the requested effect name plus either the default or indexed variant, loading each resolved layer through `CAnimationDisplayer::LoadLayer`, applying the caller-supplied delay-rate override before playback, and then registering every resolved layer through `RegisterOneTimeAnimation` rather than leaving skill-use presentation on the local runtime or UI owners. The same body also keeps optional overlay-parent and auto-flip ownership at this animation-manager seam, which means visible skill-cast parity is split one level further than the current skill backlog suggests: `CUserLocal::*` owns cast admission and follow-up behavior, feature-local runtime code owns gameplay consequences, and `CAnimationDisplayer::Effect_SkillUse` owns the shared one-shot cast animation composition that many skills route through before those consequences are visible.

### 8. Bullet animation-owner parity discovered by follow-up `CAnimationDisplayer` scan

- `CAnimationDisplayer::RegisterBulletAnimation` at `0x455410`
- `CAnimationDisplayer::RegisterMagicBulletAnimation` at `0x455570`
- `CAfterImageBullet::Update` at `0x477aa0`

Notes:
The follow-up IDA scan also exposed a player-projectile presentation owner that was not named anywhere else in the backlog. `RegisterBulletAnimation` allocates a normal `CBullet` animation object with start/end times, source and destination points, target vector, z, bullet-effect UOL, weapon id, and bullet item id, then inserts it into the animation manager's bullet container. `RegisterMagicBulletAnimation` owns the parallel magic-ball path and only creates a bullet object when the resolved ball UOL is non-empty. `CAfterImageBullet::Update` is a deeper bullet subclass path: when afterimage is enabled it creates a child layer from the current bullet layer origin, relocates alpha/origin vectors, copies the source layer canvas into the afterimage layer, animates it, and registers repeat animation state rather than treating afterimage projectile trails as a generic skill effect. This means projectile parity is split one level below `SkillManager.SpawnProjectile`: gameplay/projectile collision can be correct while the client bullet animation owner for normal, magic, and afterimage projectile presentation is still missing.

### 9. Secondary skill animation-owner parity discovered by broad `CAnimationDisplayer` function scan

- `CAnimationDisplayer::RegisterHookingChainAnimation` at `0x45cbd0`
- `CAnimationDisplayer::RegisterChainlightningAnimation` at `0x45d1b0`
- `CAnimationDisplayer::RegisterTeslacoilAnimation` at `0x45d650`
- `CAnimationDisplayer::RegisterFootHoldAnimation` at `0x45a990`
- `CAnimationDisplayer::RegisterMotionBlurAnimation` at `0x45ad00`
- `CAnimationDisplayer::Effect_SkillPrepare` at `0x45b840`
- `CAnimationDisplayer::RegisterPrepareAnimation` at `0x45b420`

Notes:
The backlog already names shared skill-use effects and bullet animation ownership, but a broader IDA function scan showed another set of skill-facing animation-manager owners that should stay explicit instead of disappearing under generic projectiles, summons, or local cast handling. `RegisterHookingChainAnimation` loads the hook and chain canvases from the supplied image UOL, resolves the owning `CUser` position through `CUserPool`, seeds fixed user/target offsets, tracks the target mob id, creates its own layer at `z - 1`, and runs a two-stage stretch/catch lifetime. Its xrefs are `CUser::OnEffect` and `CUserLocal::TryDoingMonsterMagnet`, so this is a distinct Monster Magnet / hook-chain presentation owner rather than only a normal projectile or skill-use branch. `RegisterChainlightningAnimation` remains reachable from `CSummoned::OnAttack`, `CUser::RegisterSerialBullet`, local magic attack, and remote magic attack paths, while `RegisterTeslacoilAnimation` is xrefed from `CSummoned::ShowTeslaCoilTriangle` as the Tesla-family branch. `RegisterFootHoldAnimation` is even broader: xrefs from local melee, local shoot, local magic, remote melee, remote shoot, remote magic, `CMob::LoadLayer`, and summoned attack paths show that field-anchored attack strips and ground effects are owned by a timed foothold-animation list that loads a WZ property, reads a StringPool-backed duration key, materializes layers against an area rect, and updates them separately from gameplay hit resolution. `RegisterMotionBlurAnimation` is currently xrefed from `CUser::SetActiveEffectItem`, making active-effect-item blur trails another avatar-bound animation-owner seam. `Effect_SkillPrepare` calls `RegisterPrepareAnimation`, and `RegisterPrepareAnimation` replaces any existing prepare entry for the same character id in an animation-manager map/list pair, so key-down/prepared effect presentation has a persistent owner beyond the one-shot `Effect_SkillUse` row.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Player skill execution parity

The simulator has a generic skill runtime. It does not yet mirror the client's named `CUserLocal` behavior families.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Generic skill casting | The simulator now loads the full player skill catalog from `Skill.wz`, learns active skills into the runtime, supports direct cast invocation from the skill window in addition to hotkey casting, keeps GM/SuperGM skill-book resolution compatible with both `900.img` and `910.img` layouts while focusing those jobs on their own job-book window instead of the global advancement catalog, evaluates Big Bang `common` formulas in IMG-mode data sets so skills like `Power Strike` are treated as castable attacks instead of passive placeholders, mirrors skill effect anchoring/hitbox fallback by facing direction for asymmetric melee effects, sanitizes unsupported SpriteFont glyphs in the skill window so WZ-backed names no longer crash row rendering, and resolves cast SFX from `Sound/Skill.img` so skill use now plays the corresponding `Use` or fallback attack sound through the simulator audio path | This closes the previous "can only use the current job's limited skill book" runtime/UI gap and keeps special-job skill tabs populated and executable across client data variants | `SkillManager.cs`, `SkillLoader.cs`, `SkillUIBigBang.cs`, `MapSimulator.cs`, `UIWindowLoader.cs` |
| Implemented | Buff stat application | Buff logic now applies `PAD`, `PDD`, `MAD`, `MDD`, `ACC`, `EVA`, `Speed`, and `Jump` directly to the player build and cleanly removes them when buffs expire or refresh | Active buff skills now change simulator stats and movement-relevant values instead of only a partial subset | `SkillManager.ApplyBuffStats` |
| Partial | Buff icon / temporary-stat UI parity | Active buffs still drive the WZ-backed status-bar tray with live duration countdowns sourced from `UI/BuffIcon.img`, client-style right-click cancellation still stays on the live buff list, `UILoader` still normalizes the full `BuffIcon.img` icon surface, and the known `UI/BuffIcon.img/buff/*` stat-family icons keep their WZ-authored names and traversal order. The broader non-`BuffIcon.img` catalog still resolves through the recovered v95 `SecondaryStat` layout, with `CUIStatusBar::Draw` at `0x876cd0` kept as the client-owned status-bar owner seam; this pass stayed inside `SkillManager.GetStatusBarBuffEntries` / `ResolveStatusBarBuffTooltipPresentationForParity(...)`. WZ was rechecked first for the next leftover contextual placeholders: `Skill/2311.img/skill/23111005/common` for `Water Shield` publishes `x`, then `asrR`, then `terR`, and `String/Skill.img/23111005` describes a water barrier that absorbs damage before the abnormal-status and elemental-resistance rows; `Skill/2411.img/skill/24111003/common` for `Bad Luck Ward` publishes `indieMhpR`, `indieMmpR`, then `x`, then `y`, and `String/Skill.img/24111003` describes HP, MP, Elemental Resistance, and Abnormal Status Resistance. Client evidence stayed second: `CUIStatusBar::Draw` remains the owner seam, so the fix stayed in the simulator tooltip/tray resolver. The earlier direct-family, explicit no-direct-family, and checked contextual-placeholder fixes remain in place for `Bless`, `Dash`, `Hyper Body`, `Sharp Eyes`, `Maple Warrior`, `Power Guard`, `Meso Guard`, `Holy Symbol`, `Meso Up`, and `Combo Barrier`. This pass tightens the authored contextual branch again: `TryResolveContextualTooltipTemporaryStatLabels(...)` and `TryResolveContextualFamilyOwnerTemporaryStatLabel(...)` now treat Water Shield-style `x` plus active resistance ownership as the authored `Damage Reduction` row before `asrR` and `terR`, and Bad Luck Ward-style `x` / `y` plus active Max HP, Max MP, and resistance ownership as authored resistance rows after the HP and MP rows. Focused regression coverage now lives in `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` for Water Shield and Bad Luck Ward. The remaining gap is narrower but still real: exact tray order is now authored for the known direct `BuffIcon.img` families, the supported explicit no-direct families, and the checked contextual placeholder owners `Bless`, `Dash`, `Hyper Body`, `Sharp Eyes`, `Maple Warrior`, `Power Guard`, `Meso Guard`, `Holy Symbol`, `Meso Up`, `Combo Barrier`, `Water Shield`, and `Bad Luck Ward`, but some non-`BuffIcon.img` families still lack stronger recovered v95 icon ownership or still appear to depend on runtime-owned grouping beyond `UI/BuffIcon.img`, the recovered temp-stat surface, and currently parsed skill metadata. | Buff-heavy jobs are visible on the HUD, can be cleared without waiting for expiry, and no longer collapse as often into a fully generic tray entry while tooltip text, tray-family ownership, icon fallback, and the visible `Buff Type` line stay closer to authored WZ and the recovered client temp-stat surface. The remaining gap is concentrated on still-unrecovered client grouping for leftover non-direct family mixes and any still-unchecked placeholder families once WZ-authored family order, the recovered `SecondaryStat` slice, and checked contextual mappings stop providing a unique owner. | `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`, `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` (`UI/BuffIcon.img/buff`, `UI/BuffIcon.img/buff/incPDD/name`, `Skill/230.img/skill/2301004/common`, `Skill/500.img/skill/5001005/common`, `Skill/130.img/skill/1301007/common`, `Skill/312.img/skill/3121002/common`, `Skill/112.img/skill/1121000/common`, `Skill/120.img/skill/1201007/common`, `Skill/421.img/skill/4211005/common`, `Skill/231.img/skill/2311003/common`, `Skill/411.img/skill/4111001/common`, `Skill/2112.img/skill/21120007/common`, `Skill/2311.img/skill/23111005/common`, `String/Skill.img/23111005`, `Skill/2411.img/skill/24111003/common`, `String/Skill.img/24111003`, `Skill/410.img/skill/4101004/common`, `Skill/3112.img/skill/31121001/common`, `Skill/572.img/skill/5720008/common`, `Skill/3511.img/skill/35111013/common`, `Skill/3500.img/skill/35001002/common`, `Skill/3512.img/skill/35120000/common`, `Skill/522.img/skill/5221006/common`, `Skill/000.img/skill/0000110/common`, `Skill/2311.img/skill/23111005/common`, `Skill/2411.img/skill/24111003/common`, `Skill/530.img/skill/5301003/common`, `CUIStatusBar::Draw@0x876cd0`) |
| Partial | Melee / ranged / magic resolution | The runtime keeps the existing player-skill parity work for this seam: WZ `lt/rb` attack bounds stay intact, melee and magic target ordering stay split, no-ball spell and shoot families still route through the client-shaped `info/type` classifier instead of collapsing into melee defaults, direct no-projectile ranged skills still use their own fallback hitbox plus the `DoActiveSkill_ShootAttack` melee-first-vs-shoot fallback split, projectile families still honor `rectBasedOnTarget`, `multiTargeting`, `range` chaining, `ballDelay` / `ballDelayN`, authored ball-frame sweep bounds, effective `mobCount` caps from both skill and ball data, WZ `x` damage overrides and client `SKILLENTRY::AdjustDamageDecRate` tables, and the shared shoot-entry / ammo resolver still mirrors the current client-backed weapon-family, support-state, consume-count, USE/CASH ownership, and pellet-family gates. This run kept the mounted fallback work inside the same owner seam and narrowed the deferred moving-shoot fire-time plus post-launch presentation path instead of widening projectile ownership. WZ was rechecked first and still shows both the authored attack geometry and the mounted-frame signal this pass can safely lean on: `Skill/310.img/skill/3101005/info/rectBasedOnTarget = 1`, `Skill/230.img/skill/2301005/info/{multiTargeting,rectBasedOnTarget} = 1`, `Skill/230.img/skill/2301005/common/{lt,rb,range,mobCount}`, the checked moving-shoot ball families still keep authored ball timelines on `Skill/3511.img/skill/35111004/ball` and `Skill/3512.img/skill/{35121005,35121013}/ball` (all still totaling 180 ms in the mounted export), and the checked jaguar frames still carry real per-frame body-rel-move drift on the exact client vehicle seam (`character/TamingMob/01932015/wildbeast/0/0/map/navel = (1,-54)`, `character/TamingMob/01932015/wildbeast/5/0/map/navel = (4,-51)`, with the same `0` vs `5` Y drift still present on `character/TamingMob/01932030/wildbeast/{0,5}/0/map/navel`). Client evidence was rechecked second in `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::GetProperBulletPosition@0x907310`, `GetShootAttackPt0@0x9039a0`, and `is_position_up_skill_on_riding@0x9037c0`: when no authored shoot point exists, projectile launch Y still uses the client fallback `ptUser.y - 28` baseline plus the recovered skill nudges for `33101007`, `33121005`, `35001004`, `33101001`, `35101010`, and `35111015`; queued delayed projectiles still carry and correctly rebase their fallback-origin offsets at fire time; the owner resolver still keeps the same Mechanic `1932016` riding owner alive through avatar-transform-owned vehicle states instead of only current-action or equipped mount ownership; the live mounted-state seam still preserves the exact `GetShootAttackPt0` body-rel-move vehicle ids (`1932015`, `1932030`, `1932031`, `1932032`, `1932033`, `1932036`); the disassembly now reconfirms that `GetShootAttackPt0` consumes a hidden `nBodyRelMoveY` input only for that exact vehicle table before falling back to the separate Mechanic `1932016 => -17` branch; and the same moving-shoot path still feeds packet damage presentation through a fire-time bullet clock (`tDelayShoot`) before layering the recovered lane offset formula into `CMob::AddDamageInfo`. `PlayerCharacter.TryGetCurrentBodyRelMoveY(...)` now mirrors that client split more tightly inside the existing render seam by resolving the exact mounted vehicle owner first instead of always trusting whichever taming-mob state happens to be live. When the active mounted render owner matches the requested client vehicle id, it still prefers the assembled mounted relocation's `clientBodyOrigin` value that `AvatarActionLayerCoordinator.ApplyMountedOriginRelocation(...)` already writes onto the live frame. When the active assembled frame belongs to a different mount owner, the method now skips that mismatched assembled map point, resolves the exact vehicle owner's taming-mob part from the existing mounted-state / transition / client-owned / transform / equipped / observed-owner seam, reconstructs the live mounted relocation directly from the current body frame plus that exact mounted frame through `AvatarActionLayerCoordinator.ResolveMountedOriginRelocation(...)`, and only then falls back to authored mounted `map/navel` drift and finally raw `CharacterFrame.Origin.Y`. That exact-owner body-rel-move value still flows through `SkillManager.ResolveCurrentClientShootAttackBodyRelMoveY(...)` and `ResolveClientFallbackShootAttackPointYOffset(...)` for both immediate and queued delayed projectile-origin refresh, so position-up shoot skills stay on the client's exact body-rel-move vehicle gate even when the rendered mounted owner and the requested shoot-fallback owner would otherwise diverge. This pass also keeps queue-time moving-shoot action, raw-action, action-type, action-speed, target, ammo, and bypass snapshots intact while `SkillManager.ResolveDeferredExecutionAttackOriginOverride(...)` still drops the queue-time attack origin only for smoothing moving-shoot skills; deferred execution therefore still re-enters the live `ResolveProjectileAttackOrigin(...)` / fallback-offset path instead of treating the queue-time point as an authored shoot point, matching the rechecked `TryDoingSmoothingMovingShootAttack` call into `GetShootAttackPt0` at fire time. Post-launch presentation is now tighter inside the same projectile seam as well: queued final/spark projectile hits keep gameplay collision timing, but their local damage-number / hit-effect presentation now waits for the refreshed fire-time bullet presentation clock before the existing client skill-specific delay table is layered on top, so delayed moving-shoot follow-up visuals no longer present earlier than the fire-time bullet owner the simulator already registers. Focused source-side coverage in `UnitTest_MapSimulator/MeleeRangedMagicResolutionParityTests.cs` now locks both the queued projectile impact-presentation base-time seam and the fire-time/post-launch selector behavior: non-queued collisions still present immediately, queued follow-up bullets still use their refreshed bullet-owner end time when it lands later than collision, stored fire-time presentation clocks still win over stale queue-time values, and the helper still refuses to pull presentation backward when collision already happened later. The row remains `Partial` because the remaining mismatch is now narrower and concentrated below the current simulator seam: any residual cases where the client's live `m_ptBodyRelMove.y` still diverges from the simulator's current body-plus-mount relocation source, deeper post-launch projectile or hit choreography hidden behind client-owned `CSkill_HitAni` data rather than the current bullet-owner end-time seam, and any later moving-shoot presentation nudges that depend on native layer ownership or client-only per-hit timing fields beyond the already recovered launch offsets, lane offsets, and skill-specific delay table. | This row matters because `CUserLocal` keeps separate melee, shoot, and magic execution lanes, and the remaining mismatch is now small enough that future work can stay focused on the last client-owned fire-time/post-launch presentation details instead of reopening projectile family resolution, target ordering, ammo ownership, or basic fallback projectile origin placement again. | `SkillManager.ProcessMeleeAttack`, `SkillManager.ProcessMagicAttack`, `SkillManager.ProcessNoProjectileRangedAttack`, `SkillManager.SpawnProjectile`, `SkillManager.CheckProjectileCollisions`, `SkillManager.ResolveProjectileAttackOrigin`, `SkillManager.ResolveDeferredExecutionAttackOriginOverride`, `SkillManager.RefreshQueuedProjectileFireTimeOrigin`, `SkillManager.ResolveProjectileImpactPresentationBaseTime(...)`, `SkillManager.DrawProjectile`, `SkillManager.ResolveProjectileVisualItemId`, `SkillLoader.ParseSkillInfo`, `SkillLoader.CreateLevelData`, `ClientShootAttackFamilyResolver`, `ClientShootAmmoResolver`, `PlayerCharacter.TryGetCurrentBodyRelMoveY(...)`, `PlayerCharacter.ResolveMountedBodyRelMoveSourceTamingMobPart(...)`, `PlayerCharacter.TryResolveMountedClientBodyRelMoveY(...)`, `PlayerCharacter.TryResolveCurrentMountedClientBodyRelMoveYFromRawFrames(...)`, `PlayerCharacter.ResolveClientBodyRelMoveY(...)`, `AvatarActionLayerCoordinator.ApplyMountedOriginRelocation(...)`, `AvatarActionLayerCoordinator.ResolveMountedOriginRelocation(...)`, `CharacterAssembler.GetPartAnimation(...)`, `UnitTest_MapSimulator/MeleeRangedMagicResolutionParityTests.cs` (`Skill/310.img/skill/3101005/info/rectBasedOnTarget`, `Skill/230.img/skill/2301005/info/{multiTargeting,rectBasedOnTarget}`, `Skill/230.img/skill/2301005/common/{lt,rb,range,mobCount}`, `Skill/3511.img/skill/35111004/ball`, `Skill/3512.img/skill/{35121005,35121013}/ball`, `character/TamingMob/01932015/wildbeast/{0,5}/0/map/navel`, `character/TamingMob/01932030/wildbeast/{0,5}/0/map/navel`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::DoActiveSkill_MagicAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::GetProperBulletPosition@0x907310`, `GetShootAttackPt0@0x9039a0`, `is_position_up_skill_on_riding@0x9037c0`) |
| Partial | `DoActiveSkill_*` family parity | Generic movement, summon, and prepare/charge families still resolve through the explicit generic `DoActiveSkill` fallback lane in `SkillManager`, and this pass tightens that owner seam by spelling the generic lane out in the owning `TryExecuteClientSkillBranch` switch instead of only falling through the default branch. Movement-family dispatch still stays on the client-confirmed four-way split from the recovered local lanes: teleport snaps, grounded rush, flying-rush ownership, and bound-jump starts, with jump/hop launch skills folded into the same `TryDoingFlyingRush` family while preserving their launch-specific jump arc in the existing execution seam. Earlier landings still hold too: Battle Mage aura recasts no longer route through the cast-time cancel lane, distinct aura-family buffs stay stacked while same-skill recasts refresh their own slot, and WZ `info/mesoExplosion` skills still consume authored in-box drops directly before applying the attack payload instead of passing through the live local pickup-owner gate. The helper-only `affectedSkill*` ownership split remains narrowed to rows that actually lack a standalone cast surface after the WZ re-check showed that not every `affectedSkillEffect` row is helper-only. The helper-only follow-up rows remain explicit and suppressed as standalone casts: `Skill/211.img/skill/2111007/info/{type=16,affectedSkill=2101002,affectedSkillEffect=bodyAttack&&stun}`, `Skill/3211.img/skill/32111010/info/{type=16,affectedSkill=32001002,affectedSkillEffect=bodyAttack&&stun}`, `Skill/3212.img/skill/32120000/info/{type=51,affectedSkill=32001003,affectedSkillEffect=dot,dotType=aura}`, and `Skill/3211.img/skill/32110000/info/{type=50,affectedSkill=32101002,affectedSkillEffect=partyDamageSharing}` still only publish owner follow-up metadata and no standalone direct-cast surface. WZ still shows the direct-cast counterexamples that must stay out of that passive-only lane: `Skill/522.img/skill/5220011` still publishes `info/{type=50,affectedSkill=5211006,affectedSkillEffect=amplifyDamage}` together with authored `action`, `ball`, `hit`, and `common`, and `Skill/3510.img/skill/35101009` still publishes `info/{type=1,areaAttack=1,rapidAttack=1,affectedSkill=35001001,affectedSkillEffect=dot,dotType=burn}` together with authored `prepare`, `keydown`, `keydownend`, `hit`, and `common`. `SkillData`, `SkillLoader`, and the affected-skill passive runtime helpers in `SkillManager` still share that same narrowed gate instead of treating every `affectedSkillEffect` row as passive-support data. The same row still rechecks that WZ `zoneType=invincible` is limited here to `Skill/422.img/skill/4221006/info/zoneType` and `Skill/3212.img/skill/32121006/info/zoneType`, preserves `4221006`'s authored `tile` animation while letting `32121006` use its WZ-authored `special` branch when no `tile` branch exists, and keeps the existing GM/SuperGM validation seam as the owner for both active casts and quick-slot assignment. WZ also still shows the summon-owned damaged follow-up surface on `Skill/132.img/skill/1320011/info/{condition=damaged,minionAttack=normal,requireSkill=1321007}` sitting on top of the active `1321007` summon, while `Skill/132.img/skill/1321007/summon/attack1` still provides the summon-owned attack branch that should present that follow-up. `SkillManager` still routes that learned passive through the existing owner-damaged summon seam instead of inventing a new owner, preserving the active Beholder summon as the visual owner while dealing damage and cooldown from the learned `1320011` passive. Focused regression coverage is now actually checked in at `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs`, covering the explicit generic-cast lane, helper-only passive suppression, direct-surface `affectedSkillEffect` cast admission, the jump-rush-to-flying-rush family fold, the invincible-zone tile-vs-special fallback, helper-only passive-runtime rejection for `5220011`, the Beholder linked damaged-follow-up classifier, and the GM/SuperGM quick-slot ownership rule. | The remaining gap is still narrower and concentrated in the last client-owned movement timing, packet choreography, and area/summon ownership rather than the generic cast-family split, helper-only passive suppression, direct-surface `affectedSkillEffect` admission, invincible-zone tile-vs-special selection, the learned Beholder damaged-follow-up ownership, or the admin ownership gate. The simulator still simplifies the exact moving-shoot entry snapshot and request choreography inside `TryDoingSmoothingMovingShootAttack`, especially the still-unrecovered full client `m_nAttackActionType` table values and the exact per-row contents of the shared `get_random_shoot_attack_action(...)` table beyond the now-modeled live family split for shoot, one-handed stab, two-handed stab, one-handed swing, two-handed swing, and polearm swing plus the earlier `info/type == 3` appointed-action bypass. It also still simplifies the live temporary-stat inputs that feed `get_attack_speed_degree(...)` beyond the now-modeled clamp, `4001334` base offset, skill-specific modifier-bypass set, shared Booster delta, and the WZ-backed `rapidAttack => CountLimit 1` fallback, plus any additional `CAntiRepeat` initialization or bypass rules beyond the now-modeled position-threshold, level-match, rapid-attack count-limit, and `33121009` exception gates; the final highest-jump timing window beyond the current WZ-backed near-apex gate; any remaining teleport timing / snap choreography around the client-owned delayed `m_teleport` commit window beyond the now-modeled follow-up mastery trigger; any remaining `zoneType=invincible` client details beyond the current persistent local protection zone, WZ-authored footprint/duration, and tile-or-special visual branch selection; any remaining Rocket Booster packet choreography beyond the now-modeled launch-marker, launch-speed formula, and end-loop timing; deeper packet-owned summon assist dispatch and per-family `OnSkill` / `OnAttack` ownership beyond the now-modeled body-contact / passive-movement / expiry cadence now that the learned Beholder damaged-follow-up seam has been split out explicitly; and the remaining local-vs-party area-footprint choreography for helper-only support rows beyond the now-modeled local owner-follow-up buff projection and linked-owner support merge. | `SkillManager`, `SkillLoader`, `SkillData`, `PlayerCharacter`, `PlayerCombat`, `PlayerSkillStateRestrictionEvaluator`, `SummonedPool`, `PacketOwnedSummonUpdateRules`, `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` (`CUserLocal::DoActiveSkill_*`, `CUserLocal::DoActiveSkill_BoundJump`, `CUserLocal::TryDoingTeleport`, `CUserLocal::TryDoingRush`, `CUserLocal::TryDoingFlyingRush`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CSummoned::Update`, `CAntiRepeat::TryRepeat`, `get_random_shoot_attack_action`, `get_attack_speed_degree`, `Skill/*/info/type`, `Skill/*/info/affectedSkillEffect`, `Skill/*/info/avaliableInJumpingState`, `Skill/*/info/requireHighestJump`, `Skill/110.img/skill/1101004/common/x`, `Skill/510.img/skill/5101006/common/x`, `Skill/132.img/skill/1320011/info/{condition,minionAttack,requireSkill}`, `Skill/132.img/skill/1321007/summon/attack1`, `Skill/211.img/skill/2111007/info/{type,affectedSkill,affectedSkillEffect}`, `Skill/3211.img/skill/32110000/info/{type,affectedSkill,affectedSkillEffect}`, `Skill/3211.img/skill/32111010/info/{type,affectedSkill,affectedSkillEffect}`, `Skill/3212.img/skill/32120000/info/{type,affectedSkill,affectedSkillEffect,dotType}`, `Skill/3212.img/skill/32121003/info`, `Skill/3212.img/skill/32121006/{info/zoneType,special}`, `Skill/3300.img/skill/33001000/info`, `Skill/3310.img/skill/33101001/info`, `Skill/3311.img/skill/33111001/info`, `Skill/3312.img/skill/33121005/info`, `Skill/3312.img/skill/33121009/info`, `Skill/3500.img/skill/35001001/info/rapidAttack`, `Skill/3510.img/skill/35101009/{info/rapidAttack,prepare,keydown,keydownend}`, `Skill/422.img/skill/4221006/{info/zoneType,tile}`, `Skill/421.img/skill/4211006/info/mesoExplosion`, `Skill/522.img/skill/5220011/{info/affectedSkillEffect,action,ball,hit,common}`, `Skill/*/info/selfDestructMinion`, `Skill/900.img/skill/*`) |
| Partial | Shadow Partner render/action parity | The simulator still treats Shadow Partner as a dedicated helper render/action surface rather than a generic buff ornament, with WZ-loaded `special/*` action tables for `4111002`, `4211008`, and `14111000`, client-shaped helper resolution order (`LoadShadowPartnerAction` helper-row lookup before plain raw-action name), raw-action-aware alias fallback, delayed attack follow-up keyed from the helper first-frame delay, blocking `create*` plus attack completion hold semantics, client-lane displacement/interpolation (`+/-30`, `+50`, `200 ms`), per-frame `a0/a1` alpha interpolation, and under-face insertion-layer ownership. WZ was rechecked first in the mounted export for the authored helper surface: `Skill/411.img/skill/4111002/special/*` and `Skill/421.img/skill/4211008/special/*` still publish the generic helper families plus `create2/create3/create4`, while `Skill/1411.img/skill/14111000/special/*` still publishes the same generic helper families without `create*`. The same WZ pass rechecked representative family raw rows such as `Skill/411.img/skill/4111005/action/0 = avenger`, `Skill/421.img/skill/4211002/action/0 = assaulter`, `Skill/421.img/skill/4211006/action/0 = prone2`, and `Skill/1411.img/skill/14111002/action/0 = avenger`, then rechecked the mounted `Character/00002000.img` helper-row surface again through `alert2`, `alert3`, `alert4`, `alert5`, `alert6`, `alert7`, `alert8`, `ladder2`, `rope2`, `smokeshell`, `prone2`, and `fly2*`: those character rows still expose exact source helper action, source frame index, and per-piece timing metadata, with `alert6` still carrying `330/330/330 ms` across `alert` source frames `0/1/2`, `alert7` still carrying `360/300 ms` across `alert` source frames `0/1`, and `alert8` still carrying the mounted single-piece `jump/0/30 ms` helper row. Client evidence stayed second: `CActionMan::LoadShadowPartnerAction` still resolves the action-specific helper table before plain `get_action_name_from_code`, still consumes frame-owned delay data when it builds the helper row, and `CActionMan::Init` still populates that table from character action metadata with source helper action plus numeric source-frame index. This pass keeps the work inside the existing loader/runtime seams and narrows the remaining gap again: `ShadowPartnerClientActionResolver` now treats indexed mounted `alert*` helper rows as a generic helper surface by pattern instead of an explicit finite list, `SkillLoader` now scans the mounted `Character/00002000.img` helper-row image directly so newly admitted generic indexed-alert rows and supported family-owned raw rows can synthesize from the mounted piece catalog without another hardcoded candidate pass, and `SkillLoader.TryParseShadowPartnerClientActionPieces(...)` plus the resolver's mounted built-ins now preserve the authored signed helper-row delays instead of normalizing them away before the Shadow Partner playback path consumes them. This pass rechecked the same WZ/client seam and tightens the mounted-row admission again: `ShadowPartnerClientActionResolver` now models the `CActionMan::Init` helper-table walk as the raw action-code loop itself (`0..272`) with the literal raw-code `55` hole, so mounted `Character/00002000.img` rows such as `rain` through `chargeBlow` are admitted by the client-initialized raw-code range instead of only by the earlier finite synthesized subset or by a string-name skip. The earlier local and remote Shadow Partner runtimes in `PlayerCharacter` and `RemoteUserActorPool` therefore continue to own absolute frame timing, completion holds, first-frame follow-up delay, and per-frame alpha interpolation while now keeping the original mounted delay metadata available on the helper frames themselves. The earlier family gate still remains in place for family-owned raw aliases so `shotC1` still only follows families that publish `avenger`, `stabD1` still only follows the shadower family that publishes `assaulter`, and `swingD1` / `swingD2` / `doubleSwing` / `tripleSwing` still only follow the Night Walker swing family that publishes `swingO1`. The earlier raw-backed back-lane, shared horizontal-origin cadence, same-branch identity tightening, numeric helper-frame ordering, `4331002` / raw-`47` render suppression, and local/remote helper-family identity seams stay in place. Focused regression coverage now lives on disk in `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs` for generic indexed-alert helper-surface admission beyond the old explicit catalog, mounted helper-piece signed-delay preservation, mounted-row synthesis against the existing helper surface, and the exact client-initialized raw-code range plus skipped raw-code hole. Remaining gap is narrower but still open: client-only `LoadShadowPartnerAction` non-ghost helper rows beyond the WZ exact rows, the client-initialized `Character/00002000.img` raw-code loop, and the currently admitted mounted generic/family rows still remain unrecovered, exact source-frame indexes for any later-discovered pieced row outside that subset remain unknown, exact ordering against every other simulator-owned non-mirror under-face overlay that the client may still own as a literal action-layer canvas object remains approximate, and residual packet-only one-shot alpha or follow-up choreography that depends on hidden client layer state rather than the helper animation catalog is still not one-for-one. | Shadow Partner still mirrors companion playback through a dedicated skill-owned action catalog with client-shaped helper ordering and raw-action fallback, but the previously broader mounted-row gap is now narrower again for generic indexed alerts and other mounted helper rows that already exist in `Character/00002000.img`. The simulator keeps the WZ-authored `special/*` helper catalog per Shadow Partner family, keeps remote/local family gates from leaking unsupported family-owned raw aliases back through generic `shoot*`, `stab*`, or `swing*` fallback, now treats indexed `alert*` helper rows as part of one generic helper surface instead of an explicit `alert2`-through-`alert8` list, now preserves the authored mounted helper-row delay signs while timing those rows through the existing local and remote runtimes, and now admits mounted helper rows by the recovered `CActionMan::Init` raw-code loop instead of by string-name skip semantics. The row stays `Partial` because the unrecovered part is now concentrated on any hidden helper-table rows not exposed by the WZ/raw-action, exact special rows, client-initialized mounted-character loop, and admitted mounted generic/family rows, plus deeper literal layer-object ordering and packet-only presentation state outside the helper animation catalog. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `ShadowPartnerClientActionResolver.cs`, `RemoteUserActorPool.cs`, `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs`, `CUser::PrepareShadowPartnerActionLayer@0x8e1730`, `CActionMan::Init@0x41beb0`, `CActionMan::LoadShadowPartnerAction@0x423cf0`, `is_back_action@0x45f1b0`, `get_action_name_from_code`, `Skill/*/{4111002,4211008,14111000}/special/*`, `Skill/411.img/skill/4111005/action/0`, `Skill/421.img/skill/{4211002,4211006}/action/0`, `Skill/1411.img/skill/14111002/action/0`, `Character/00002000.img/{alert2,alert3,alert4,alert5,alert6,alert7,alert8,fly2,fly2Move,fly2Skill,ladder2,rope2,smokeshell,prone2,avenger,assaulter,flyingAssaulter,savage,showdown,assassination,ninjastorm,vampire,stabD1,swingD1,swingD2,doubleSwing,tripleSwing,shotC1}`) |
| Partial | Queued follow-up attack families | Attack skills that publish WZ `finalAttack` follow-up maps still queue a deferred extra strike after the opener resolves, gate that queued branch by weapon type on both queue and execute, reuse the learned follow-up skill's live proc rate, preserve queued facing plus preferred-target mob or position snapshots through direct follow-up resolution and any later deferred moving-shoot or projectile hop, and keep the client's separate single pending-slot overwrite rules for final attack, Spark, and serial follow-up families without clearing unrelated queued summon work. Thunder Breaker `Spark` (`15111006`) still stays on its own timed attack-triggered family from WZ `info/{condition=attack,chainAttack=1,chainattackPenalty=1}`, still snapshots the current weapon code at queue time, still rechecks that same code again when the deferred branch fires, and still re-enters the shared shoot/projectile path with the saved trigger origin and target lane. WZ still exposes the authored item-bullet visual seam directly (`Item/Consume/0206/02060000/bullet/0` for standard arrows and `Item/Cash/0502/05021000/bullet/{0,1,2}` for animated cash bullets), while client evidence from `CUserLocal::GetProperBulletPosition` and `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare@0x912f90` still shows fire-time resolution carrying both USE and CASH projectile ownership plus the queued one-based `nBulletItemPos` / `nBulletCashItemPos` metadata separately from later delayed fire-time presentation. This pass keeps that owner split tighter inside the existing queued projectile presentation seam: delayed final-attack and Spark projectiles now normalize their fire-time bullet-owner metadata onto the same effective visual lane that the refreshed queued selection actually resolved, so once the fire-time slot refresh has already dropped the queued CASH lane and fallen back to the preserved USE lane the later bullet-animation owner can no longer silently re-expose stale CASH ownership metadata while registering the delayed visual. The existing queue-time and fire-time parity already on disk remains intact around that change: queued final-attack, Spark, and serial branches still re-read the live learned level when they actually execute, still route queued startup back through the normal cast-effect seam, still preserve queued-facing and preferred-target snapshots through deferred moving-shoot or `ballDelay` release, still carry the ranged final-attack `TryDoingShootAttack(..., 65, ...)` search hint through queued entry plus any extra deferred moving-shoot hop, still revalidate live ammo family and consume count right before queued fire time, still prefer the preserved client one-based USE or CASH slot positions when duplicate matching stacks survive to fire time, still cancel the queued ranged branch when the armed USE lane disappeared or no longer satisfies the live consume count, and still keep queued serial follow-up damage on the authored/client decay tables instead of flattening later chain hits back onto one generic percent. Focused helper coverage now lives on disk in `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` for the fire-time queued visual-lane resolver, the delayed owner-registration normalization that clears stale CASH ownership after USE fallback, and the preserved USE-art fallback for queued Spark/final-attack projectile presentation. The remaining gap is now narrower and stays below the current simulator seam: the exact cosmetic bullet-position placement and any later post-launch presentation nudges the client still derives after `GetProperBulletPosition` has already resolved the preserved queued USE/CASH item ids and one-based slot positions into the delayed fire step. | Final-attack style procs still no longer collapse into the initiating cast timing, queued strikes still cancel if the player changes to a non-matching weapon before the deferred attack fires, if a newer follow-up proc replaces the pending one before it can execute, or if the follow-up skill has been locally unlearned before the queued branch actually fires, and replacing a pending final attack still does not wipe unrelated queued summon damage. Queued Spark still follows that same queue-time and execute-time weapon snapshot rule, deferred queued Spark shots still keep their Spark-owned projectile marker through the deferred payload handoff so later delayed fire-time refresh, queued-facing presentation, and same-slot overwrite logic stay on the Spark family instead of degrading to generic queued projectile handling, and the queued Spark lane still keeps a concrete target-position fallback when the initial live lookup drops out. Once a queued final attack or queued Spark has already progressed into a delayed projectile release, a newer proc in that same client-owned slot still clears the older still-pending projectile instead of letting it survive past the queue replacement point, while unrelated projectile work remains intact. The queued ranged shoot-entry seam is now closer on both execution and presentation because queued final-attack and Spark casts still recheck their armed USE or CASH ammo metadata at queued fire time even when the queued branch was armed under a no-consume buff snapshot, keep the original ammo item ownership instead of inheriting unrelated later casts, prefer the preserved client one-based queued USE/CASH slot positions when duplicate matching stacks still survive to fire time before live fallback, draw delayed queued projectiles from the matching snapshotted bullet-family frames rather than generic skill projectile art, and now also keep the delayed bullet-animation owner on that same resolved visual lane instead of leaving stale CASH ownership visible after a USE fallback already won at fire time. The remaining mismatch is now concentrated in deeper client-owned `TryDoingShootAttack` presentation bookkeeping that the current simulator seam still does not expose directly, especially exact cosmetic bullet-position placement after the delayed moving-shoot entry has already carried preserved queued item ids and one-based slot positions through fire-time resolution. | `SkillManager`, `SkillData`, `SkillLoader`, `ClientShootAmmoResolver`, `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` (`Skill/1511.img/skill/15111006/info/{condition,chainAttack,chainattackPenalty}`, `Skill/222.img/skill/2221006/info/{chainAttack,chainattackPenalty}`, `Skill/512.img/skill/5121002/info/{chainAttack,chainattackPenalty}`, `Item/Consume/0206/02060000/bullet/0`, `Item/Cash/0502/05021000/bullet/{0,1,2}`, `CUserLocal::GetProperBulletPosition@0x907310`, `CUserLocal::TryDoingFinalAttack@0x93aaa0`, `CUserLocal::TryDoingSparkAttack@0x93abe0`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare@0x912f90`) |
| Partial | Repeat-skill sustain families | Updated in this pass: WZ still publishes SG-88 as `Skill/3512.img/skill/35121003` with `info/type = 33`, `info/minionAttack = normal`, and `summon/attack1`, while tank-siege still publishes `Skill/3512.img/skill/35121013` with `info/fixedState = 1`, `info/onlyNormalAttack = 1`, `common/time = 5`, and `effect/*/delay = 30/60/30/60`; `Skill/3511.img/skill/35111004/common/time = 5` also still matches the siege timer surface. Client recheck still anchors SG-88 and tank-siege in `CUserLocal::TryDoingRepeatSkill`, and additionally `CUserLocal::DoActiveSkill` confirms the live tank-mode toggle-off path sends opcode `71` for effect skill `35120005` with the learned level of `35121005`, clears repeat state immediately, and plays tank-mode skill-use sound when `m_nMechanicMode == 35121005`. `SkillManager` now mirrors that tank-mode immediate end inside the existing repeat/transport seam: when `35121005` is recast while its repeat transform is live, it builds a `PacketOwnedMechanicRepeatSkillRuntime` opcode-`71` request for `35120005`, clears tank-mode repeat/avatar/mount ownership after dispatch, and relays through `PlayerManager` into the existing `MapSimulator.PacketOwnedUtilityParity` mechanic authority planner. Existing tank-siege mode-end transport remains request-time keyed and still emits `35120013`; SG-88 return-to-tank and replay-template promotion behavior remain as previously landed. Focused coverage in `UnitTest_MapSimulator/RepeatSkillSustainPacketTransportParityTests.cs` now pins the `35120005` opcode-`71` payload order alongside the existing tank-siege and SG-88/Tesla transport cases. The row remains partial because tank-siege mode-end still lacks the true official confirmation opcode/order behind `m_bSendTankSiegeModeEnd`, and SG-88 still lacks the exact official first-use outbound opcode plus full field order before captured official traffic teaches a reusable template. | Mechanic repeat skills now emit both recovered repeat effect-request branches through the authority seam: tank-siege mode-end uses pending `35120013`, while live tank-mode end uses immediate `35120005` plus local repeat teardown. Remaining risk is concentrated in unknown tank-siege confirmation semantics and the unknown first-use SG-88 outbound packet shape. | `SkillManager.cs`, `PlayerManager.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `Managers/PacketOwnedMechanicRepeatSkillRuntime.cs`, `Managers/SummonedOfficialSessionBridgeManager.cs`, `Companions/MechanicAuthorityTransportPlanner.cs`, `UnitTest_MapSimulator/RepeatSkillSustainPacketTransportParityTests.cs` (`CUserLocal::TryDoingRepeatSkill`, `CUserLocal::DoActiveSkill`, `CUserLocal::SendSkillEffectRequest`, `CSummoned::Update`, `CSummonedPool::OnPacket`, `CSummoned::TryDoingAttackManual`, `Skill/3512.img/skill/35121003/{info,summon}`, `Skill/3512.img/skill/35121013/{common,info,effect}`, `Skill/3511.img/skill/35111004/common`) |
| Partial | Bound-jump, rocket-booster end, and smoothing moving-shoot branches | This pass stayed inside the same moving-shoot / Rocket Booster / bound-jump seams again and tightened the next exact-row fallback without widening unsupported ownership. WZ was rechecked first in the mounted export at `E:\MapleStory\WzExport\v95_2`: the earlier moving-shoot and Rocket Booster rows still hold for `Skill/3500.img/skill/35001001/action/0 = flamethrower`, `Skill/3510.img/skill/35101009/action/0 = flamethrower2`, the `rapidAttack = 1` rows for `33121009`, `35001001`, and `35101009`, `Skill/3510.img/skill/35101004/{action/0 = rbooster_pre, info/type = 10, info/casterMove = 1, info/landingEffect = areaAttack}`, and the ranged weapon metadata rows `Character/Weapon/01452005/info/attack = 3` with published `shoot1` / `shootF`, `Character/Weapon/01462000/info/attack = 4` with published `shoot2`, and `Character/Weapon/01492000/info/attack = 9` with published `shoot1` / `shoot2` / `shootF`. The same WZ pass also rechecked both sides of the bound-jump seam: the confirmed rows still remain `Skill/3300.img/skill/33001002/{action/0 = doubleJump, info/type = 40}`, `Skill/1310.img/skill/13101004/{action/0 = archerDoubleJump, info/type = 40, info/casterMove = 1, info/avaliableInJumpingState = 1}`, and `Skill/1410.img/skill/14101004/{info/type = 40, info/casterMove = 1, info/avaliableInJumpingState = 1}`, while the broader movement-action names that had still been riding the same simulator seam recheck as non-bound-jump rows in mounted data: `Skill/421.img/skill/4211002/action/0 = assaulter` with `info/type = 1`, `Skill/510.img/skill/5101002/action/0 = backspin` with `info/type = 1`, and `Skill/510.img/skill/5101004/action/0 = screw` with `info/type = 1`, all still alongside `info/casterMove = 1`. Client evidence stayed second: `CUserLocal::DoActiveSkill_BoundJump@0x93fdf0` still lists only the directly recovered skill-id branches before the shared jump rows, keeping `14101004` on the shared flash-jump `nSLV / 4` row and `33001002` on the doubled-`nSLV` branch before the shared quarter-step; `get_random_shoot_attack_action@0x70b7a0` still keys off avatar `m_nAttackActionType` plus the type-`3` vs non-type-`3` split, rejects unsupported rows outside the recovered table surface, and indexes the same `2 * type - 2/-1` row pair; and `CUserLocal::TryDoingRocketBoosterEnd@0x93d2d0` still gates both stored-vertical-marker clearing and the foothold touchdown attack behind `CAvatar::GetOneTimeAction() <= -1` before the local landing attack / `500 ms` recovery-effect clear. `SkillManager` keeps the earlier type-`3` unresolved-owner abort, appointed-action precedence, one-time-action type-`3` prepare exception, queued skill-level revalidation, explicit `33121009` anti-repeat bypass, rapid-attack limit, ranged `info/attack` row preservation for `3` / `4` / `9`, published-surface narrowing, direct `14101004` explicit bound-jump treatment, the recovered `33001002` / `14101004` impact rows, and the earlier Rocket Booster touchdown wait for the startup window plus any active client-owned one-time action to clear. This pass closes the next moving-shoot fallback drift in that same local seam: when queued moving-shoot prep no longer has live weapon `info/attack` metadata to preserve the client row directly, exact current raw owners and exact visible action owners now keep the recovered ranged table rows visible instead of collapsing back to the generic shoot bucket, so recovered current-action owners such as raw `31/36 -> type 3`, `32 -> type 4`, `33/34/35 -> type 7`, `116 -> type 9`, and visible actions `shoot1` / `shoot2` / `shootF` now stay on those same client rows during delayed execution while unsupported melee rows remain closed. The earlier bound-jump narrowing also remains in place: authored movement action names now only keep a skill on the generic bound-jump route when the skill still sits on the WZ/client type-`40` bound-jump surface, so the preserved `archerDoubleJump` profile still routes through bound jump while non-type-`40` mover skills such as `assaulter`, `backspin`, and `screw` no longer inherit the simulator's bound-jump movement arc just because they publish a movement-flavored action string. Focused source-side coverage now really exists under `UnitTest_MapSimulator/PlayerSkillExecutionParityTests.cs` for the preserved current raw and visible-action moving-shoot fallback rows (`3` / `4` / `7` / `9`) alongside the preserved WZ-backed `archerDoubleJump` route and the closed non-type-`40` `assaulter` / `backspin` / `screw` promotion seam. The row remains `Partial`: the remaining gap is now concentrated in the still-hidden exact `get_random_shoot_attack_action(...)` row contents and weighting beyond the recovered ranged rows and constrained fallbacks already kept here, especially the unrecovered melee attack-type rows, fuller `CAntiRepeat` construction and count-limit initialization details beyond the current threshold reset / `33121009` bypass / rapid-attack limit, any packet- or server-owned Rocket Booster end choreography outside the simulator local seam, and any additional bound-jump row mapping beyond the directly recovered client skill ids plus the constrained WZ-authored type-`40` action-backed profiles now kept here. | Movement-coupled attack skills keep the client-owned moving-shoot queue metadata, WZ-authored appointed action preference, one-time-action type-`3` exception, anti-repeat owner surface, Rocket Booster local landing seam, the recovered direct bound-jump rows, and the exact recovered ranged avatar attack-action owners instead of collapsing them to one generic shoot type before delayed execution. `14101004` stays explicit on both the velocity and state-gate seams, `33001002` stays on the client-owned half-level growth table, crossbow moving-shoot fallback still stays on `shoot2`, exact current raw and visible action owners now also preserve the recovered ranged rows `3` / `4` / `7` / `9` when weapon metadata is missing at queue time, hidden melee rows still stay closed even when current-action or weapon-family fallback could have guessed a swing/stab family, Rocket Booster touchdown still waits for the client-owned one-time-action lane to clear before the stored launch marker advances, and bound-jump admission no longer leaks from unrelated non-type-`40` movement action names such as `assaulter`, `backspin`, or `screw`. The remaining mismatch is narrower and concentrated in deeper hidden-table recovery for moving-shoot random action selection, especially exact melee rows and table weighting, fuller anti-repeat construction details, packet-owned Rocket Booster end choreography, and still-unrecovered bound-jump special rows beyond the direct client ids and constrained type-`40` authored profiles now kept here. | `SkillManager.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `UnitTest_MapSimulator/PlayerSkillExecutionParityTests.cs` (`CUserLocal::DoActiveSkill_BoundJump@0x93fdf0`, `CUserLocal::TryDoingRocketBoosterEnd@0x93d2d0`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare@0x912f90`, `CUserLocal::TryDoingSmoothingMovingShootAttack@0x92de70`, `get_random_shoot_attack_action@0x70b7a0`, `CAntiRepeat::TryRepeat@0x9032b0`, `Character/Weapon/01452005`, `Character/Weapon/01462000`, `Character/Weapon/01492000`, `Skill/3300.img/skill/33001002/{action/0,info/type}`, `Skill/3312.img/skill/33121009/info/rapidAttack`, `Skill/3500.img/skill/35001001/action/0`, `Skill/3500.img/skill/35001001/info/rapidAttack`, `Skill/3510.img/skill/35101004/{action/0,info}`, `Skill/3510.img/skill/35101009/action/0`, `Skill/3510.img/skill/35101009/info/rapidAttack`, `Skill/1110.img/skill/11101005/info/type`, `Skill/1310.img/skill/13101004/{action/0,info/type,info/casterMove,info/avaliableInJumpingState}`, `Skill/1410.img/skill/14101004/info`, `Skill/421.img/skill/4211002/{action/0,info/type,info/casterMove}`, `Skill/510.img/skill/5101002/{action/0,info/type,info/casterMove}`, `Skill/510.img/skill/5101004/{action/0,info/type,info/casterMove}`, `String/Skill.img/14101004/name`) |
| Partial | Key-down / charge skills | The prepared-skill startup/release seams for this row still stay confined to the existing `SkillManager`, `PreparedSkillHudRules`, `MapSimulator.RemoteUsers`, and `RemoteUserActorPool` owners. WZ was rechecked first in the mounted export: Piercing (`Skill/322.img/skill/3221001`) still publishes the authored startup split with `prepare/time = 1200`, `action/0 = piercing`, and the separate client gauge cap of `900 ms`; Monkey Wave (`Skill/531.img/skill/5311002`) still publishes `prepare/time = 1080`, `prepare/action = noiseWave_pre`, `keydown/action = noiseWave_ing`, and `action/0 = noiseWave`; the Big Bang trio, Evan breath pair, Dual Blade charge pair, Screw Punch, Shark Wave, Poison Bomb, Grenade, and Wild Hunter Swallow still publish zero-startup prepare surfaces such as `prepare/time = 0` with authored prepare actions, so those families must enter the hold/release lane immediately instead of waiting on a simulator-owned pseudo-startup. Client evidence stayed second: `get_max_gauge_time` (`0x50ab00`) still hard-caps the recovered branch table at `900 ms` for Piercing and Swallow, `1080 ms` through the Monkey Wave authored window, `600 ms` for Final Cut, `1200 ms` for Monster Bomb, `500 ms` for Evan breath, `1000 ms` for the Big Bang trio / Screw Punch / Shark Wave / Poison Bomb / Grenade, and the default `2000 ms` keydown fallback; `CUserLocal::OnKeyDownSkillEnd` (`0x9301b0`) still clamps elapsed charge time to the shared `30 ms` minimum, caps it through `get_max_gauge_time`, routes release through magic / shoot / melee / HP-cost / skill-specific branches, keeps Final Cut HP cost as `elapsed * nX / get_max_gauge_time` before comparing that percentage cost against current HP, and still makes `CUserLocal::TryDoingSwallowAbsorb` (`0x93af50`) reject undercharged Swallow releases until the elapsed hold reaches the full `900 ms` gauge cap. The runtime seams continue to match more of that implemented portion: `SkillManager.ResolvePreparedSkillStartupDuration` uses authored WZ prepare time only when a release-owned family really has a nonzero startup window, `BeginPreparedSkill` enters hold immediately for zero-startup keydown families, `PreparedSkillHudRules.ResolvePreparedGaugeDuration` and `ResolveReleaseChargeElapsedMs` prefer the shared HUD or `get_max_gauge_time` charge window over longer authored prepare splits, the release damage-scale and Final Cut HP-cost helpers stay on the shared linear undercharge seam, `PreparedSkillHudRules.CanExecuteReleasePayload` now keeps Wild Hunter Swallow on the client's full-gauge-only release gate, `SkillManager.ReleasePreparedSkill` now routes failed release-owned keydown attempts back through the shared cancel-request seam instead of silently dropping them, `PreparedSkillHudRules.ResolveRemotePreparedSkillPhases` still auto-arms release-owned zero-startup keydown families onto hold when the packet lane carries a real hold window, `RemoteUserActorPool.TryResolvePreparedSkillPhase` still preserves that zero-startup auto-hold state, and `RemoteUserActorPool.TryApplyThrowGrenade` now clears the matching remote prepared Grenade state on the same release seam as the official grenade packet instead of leaving the HUD bar latched after release. Focused coverage in `UnitTest_MapSimulator/PreparedSkillChargeStartupParityTests.cs` now covers the Swallow full-gauge release gate, early-release cancel routing for the local Swallow branch, and the remote grenade prepared-state teardown in addition to the existing authored startup seam across the Big Bang trio, Piercing, Screw Punch, Grenade, Monkey Wave, Poison Bomb, Shark Wave, Evan breath, Dual Blade charge pair, and Wild Hunter Swallow; local zero-startup immediate hold; remote zero-startup phase resolution and persistence; the shared client minimum/cap release elapsed rule across representative release branches including Grenade, Poison Bomb, and Swallow; remote release-owner recognition for the zero-startup client charge table; shared linear undercharge scaling where the client evidence supports it; the gauge-window-vs-prepare-window release scaling seam; and Final Cut scaled HP-cost / restriction behavior. | The remaining gap is now narrower and still limited to client-owned choreography outside the WZ-confirmed startup/gauge/release seams covered here. The simulator still does not mirror the full raw make/break scan-code ownership stream, the broader exclusive-request catalog beyond the prepared-skill family that currently shares the `300 ms` gate, literal packet-owned charge semantics beyond this prepared-skill phase split, or deeper family-specific post-release choreography where the client branch consumes elapsed time outside the currently modeled shared linear damage, Swallow full-charge gate, failed-release cancel routing, and Final Cut HP-cost seam. The row stays `Partial` because those owner-level gaps remain, but it no longer lacks checked-in coverage for the recovered zero-startup branch table, client gauge caps, shared minimum elapsed clamp, local/remote auto-hold behavior, the Swallow full-gauge release rule, failed-release cancel fallback, remote grenade prepared-state teardown, or the currently implemented release scaling and HP-cost helpers. | `PreparedSkillHudRules.cs`, `SkillManager.cs`, `MapSimulator.RemoteUsers.cs`, `PlayerInput.cs`, `PlayerManager.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `UnitTest_MapSimulator/PreparedSkillChargeStartupParityTests.cs` (`Skill/{212,222,232}.img/skill/*/info/chargingSkill`, `Skill/322.img/skill/3221001/{prepare/time,prepare/action,keydown,action/0,info}`, `Skill/510.img/skill/5101004/prepare`, `Skill/520.img/skill/5201002/prepare`, `Skill/531.img/skill/5311002/{prepare,keydown,action}`, `Skill/1411.img/skill/14111006/prepare`, `Skill/1510.img/skill/15101003/prepare`, `Skill/2212.img/skill/22121000/{prepare,common,info/chargingSkill}`, `Skill/2215.img/skill/22151001/{prepare,common,info/chargingSkill}`, `Skill/434.img/skill/{4341002,4341003}/{prepare,action,info/chargingSkill}`, `Skill/3310.img/skill/33101005/prepare`, `get_max_gauge_time`, `is_keydown_skill`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::OnKeyDownSkillEnd`, `CUserLocal::TryDoingSwallowAbsorb`, `CUserLocal::DrawKeyDownBar`, `CDragon::DrawKeyDownBar`) |
| Partial | Swallow, mine, cyclone, and sit-down healing branches | The simulator keeps the earlier mine/swallow/cyclone/healing parity work on this row and tightens the same client-owned seams again instead of widening into another family. WZ recheck in the current export still shows `Skill/3310.img/skill/33101005/prepare/action = swallow_loop` and `Skill/3310.img/skill/33101005/info/dummyOf = 33101006&&33101007`, while unrelated dummy-linked families such as `Skill/3200.img/skill/32001001/info/dummyOf`, `Skill/3211.img/skill/32111005/info/dummyOf`, and `Skill/3510.img/skill/35100008/info/dummyOf` still do not publish `info/swallow`. This pass tightens `SkillLoader` to that authored WZ signal one step further: the swallow-family root classifier now requires the authored `info/swallow` flag on the visible swallow root itself and no longer accepts the broader `ClientInfoType = 98` fallback, so only the authored swallow root plus its visible `dummyOf` follow-up ids are marked `IsSwallowFamilySkill` even when unrelated dummy families expose swallow-shaped action names beside `dummyOf`. Client recheck against `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500` still narrows the wriggle owner to visible `33101005` only, so `SkillManager` still keeps the `500 ms` wriggle cadence and visible cancel path on that same root plus its linked follow-up ids (`33101005`, `33101006`, `33101007`) through the existing prepared-skill / visible-action seam (`swallow_pre`, `swallow_loop`, `swallow`) without accepting reverse-linked unrelated families. That preserves the earlier `900 ms` keydown threshold before release, early-release cancel, absorb-ack gated capture success, automatic `3000 ms` digest start, cancel-path teardown on failed or invalidated absorb outcomes, and captured-mob consumption before the hidden attack dummy branch fires while tightening the family classifier itself to the same WZ-authored root. The earlier mine and cyclone work remains in place on this row. Client recheck against `CUserLocal::TryDoingMine` at `0x907d70` still narrows mine ownership through the riding-vehicle-id gate, town suppression, `update_time - 1000` first-deploy lead, grounded `CVecCtrl::GetInput` X sign, and the shared `CVecCtrl::IsStopped()` clear, so the simulator still resolves Wild Hunter mine movement from grounded controller input plus the stopped-controller gate instead of foothold velocity fallback, and the hidden mine fallback still stays on the narrowed Jaguar Rider-owned live/grace-window owner plus live/equipped jaguar mount seam already landed earlier. Cyclone still sustains as a timed `32121003` body-attack loop with `1000 ms` pulses only while the cyclone action remains active. The sit-down-healing work also remains in the summon/runtime seam, and this pass tightens its checked-in coverage without broadening generic support ownership. WZ recheck still shows `Skill/3511.img/skill/35111011/info/{condition,minionAbility,type} = {whenUserLieDown, heal, 33}`, `Skill/3511.img/skill/35111011/summon/stand/info/range` publishing `lt=(-150,-100)` / `rb=(150,20)`, `summon/heal/info/range` publishing the narrower `lt=(-125,-100)` / `rb=(125,0)`, `common/x = 5`, and `String/Skill.img/35111011/{desc,pdesc}` still describing nearby party or team healing with the same `5`-second reheal lock. Client recheck against `CUserLocal::TryDoingSitdownHealing` at `0x903d60`, `CUserPool::DoHealNearHealingRobot` at `0x94bb20`, and `CSummoned::TryDoingHealingRobot` at `0x74b3e0` still lands in the same seam: the client only polls Healing Robot while the local player is seated, checks the local robot first, then remote party robots, skips robots already in suspend before the heal attempt, does not pre-check whether HP or MP will actually change before issuing the request, and arms action `13` as `(moveAction << 7) | 13` when the request path succeeds. `SkillManager` still mirrors that owner split through the dedicated seated-only local-first-then-remote Healing Robot poll pass, the generic support-scan skip, and the existing `ProcessSummonHealSupport(...)` no-preheal-stat-check completion path, while `SummonedPool` still keeps the remote-owner seam on the local-or-party filter, authored heal-branch range, packet-owned facing-bit playback, and the narrowed Healing Robot idle-clear gate. Focused regression coverage is now on disk in `UnitTest_MapSimulator/PlayerSkillExecutionParityTests.cs` for the authored swallow-root-only classifier, visible swallow follow-up request matching, the stricter local action-`13` heal-branch mapping, the seated local-first-then-remote poll order, the suspended-candidate skip, the no-stat-change Healing Robot completion gate, the non-Healing-Robot false branch, and the packet-owned facing-bit decode seam. The row remains partial because the simulator still does not mirror the client's exact swallow packet ordering, the exact server-owned swallow capture/digest request choreography, the remaining `TryDoingMine` riding-vehicle-id ownership nuances beyond the current active-owner plus update-time grace-window gate and live/equipped jaguar fallback, the remaining request/packet-owned Healing Robot suspend-reset choreography beyond the current seated poll order, no-stat local request completion, action-`13` facing-bit playback, stricter heal-branch action-`13` ownership, authored heal-branch/range owner, and support-suspend ownership already modeled here, or the remaining non-Wild-Hunter swallow-family nuances one-for-one. | These branches now stay closer to their real client owners instead of flattening back into the generic cast/runtime path. Mine still respects the client town suppression, update-time-owned hidden-owner grace window, narrowed hidden jaguar fallback, and the client-shaped grounded `CVecCtrl` input plus stopped-controller gate instead of a simulator-only foothold-velocity fallback; cyclone still stops pulsing once the sustained cyclone action falls off the avatar; swallow still routes charge, cancel, absorb success/failure, digest timing, and hidden follow-up attack through one WZ-backed swallow-family seam, its wriggle pulses still stop once the visible swallow owner falls off, its linked dummy follow-up ids are now classified only from an authored swallow root that actually publishes `info/swallow`, and its local request matching still stays one-way on that visible root plus linked follow-up ids; and sit-down healing now also stays behind the same client-shaped seated poll gate before the existing local/party owner filter, authored reheal-lock support window, explicit heal-branch/range ownership, narrowed suspend clear path, dedicated local-first-then-remote Healing Robot poll path, the client-shaped local action-`13` heal-branch mapping, the matched no-preheal-stat-check local request path, and the packet-facing decode seam sourced from `CSummoned::TryDoingHealingRobot`. The remaining visible mismatch is narrower and centered on exact swallow packet choreography, the last riding-vehicle-id details in `TryDoingMine`, the last request/packet-owned Healing Robot suspend-reset details outside the current seated poll plus authored playback owner seam and stricter heal-branch action-`13` gate, and any residual non-Wild-Hunter swallow-family behavior the client owns outside the current local cancel/request path. | `SkillManager`, `SkillLoader.cs`, `SkillData.cs`, `MapSimulator`, `SummonedPool.cs`, `SummonRuntimeRules.cs`, `UnitTest_MapSimulator/PlayerSkillExecutionParityTests.cs` (`CUserLocal::TryDoingSwallowBuff`, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::TryDoingMine`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingSitdownHealing`, `CUserPool::DoHealNearHealingRobot`, `CSummoned::TryDoingHealingRobot`, `CVecCtrl::GetInput`, `CVecCtrl::IsStopped`, `Skill/3200.img/skill/32001001/info/dummyOf`, `Skill/3211.img/skill/32111005/info/dummyOf`, `Skill/3300.img/skill/33001001/info/type`, `String/Skill.img/33001001`, `Skill/3310.img/skill/33101005/{prepare/action,info/dummyOf}`, `Skill/3510.img/skill/35100008/info/dummyOf`, `String/Skill.img/35111011/{desc,pdesc}`, `Skill/3511.img/skill/35111011/{info/{condition,minionAbility,type},common/{hp,x},summon/stand/info/range,summon/heal/info/range}`, `CUserLocal::TryDoingMine@0x907d70`, `CUserLocal::TryDoingSwallowMobWriggle@0x93f500`, `CUserLocal::TryDoingSitdownHealing@0x903d60`, `CUserPool::DoHealNearHealingRobot@0x94bb20`, `CSummoned::TryDoingHealingRobot@0x74b3e0`) |
| Partial | Summon simulation | The simulator still keeps the broader packet-owned summon parity already landed here: generic summon lifecycle and timers, WZ-backed attack branch and metadata loading, projectile and target-impact branch retention, Tesla triangle targeting, self-destruct prepare/die handling, packet-owned prepare and attack visual parity, taunt-only puppet registration, shared summon rule selection between local and packet-owned seams, the delayed bird tile overlay handoff, the `4111007` / `4211007` reactive-chain family, and the earlier sourced-attack-frame mob-hit split between `info/hit` and attack-frame-owned hit clips. WZ was rechecked first again in the mounted export for the remaining chain-asset, tile-UOL, and mob-hit slice instead of widening transport ownership: `Mob/3220000.img/attack1/info/hit/attach = 1` still keeps the base attached hit family, `Mob/3220000.img/attack1/13/hit/attach = 1` still publishes the later frame-owned attached clip, `Mob/8150302.img/attack1/info/hit/1/attach = 1` still proves the mounted data also carries indexed `info/hit/<frame>` attach metadata under the shared info-side hit family, `Mob/3000000.img/attack1/info/{range/sp,hit/attach}`, `Mob/4130102.img/attack1/info/{range/sp,hit/attach}`, and `Mob/4220000.img/attack1/info/{range/sp,hit/attach}` still prove attached hit metadata can coexist with authored range origins that belong to detached fallback rather than the client vec-control attach branch, `Mob/9300314.img/attack1/info/attachfacing = 1` still coexists with `Mob/9300314.img/attack1/info/hit/attachfacing = 1`, `Mob/9300275.img/attack1/info/attachfacing = 1` still keeps the same info-side facing alias seam, `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, and `Skill/312.img/skill/3120010/{ball,tile}` still keep the separate authored bird projectile versus tile families, and both `Skill/411.img/skill/4111007/{summon,ball}` and `Skill/421.img/skill/4211007/{summon,ball}` still expose a real authored split between the summon-body branch and the ball-owned chain asset. Client evidence stayed second and remains on the literal summon/runtime owners this row is anchored to: `CSummonedPool::OnPacket` at `0x75ac70` still owns the packet family dispatch, `CSummoned::OnHit` at `0x74bc80` still resolves summon-hit presentation from decoded `(attackIndex, mobTemplateId, facing)` plus template attack info, branches on `MobAttackInfo::sHit`, and chooses the attached-versus-detached presentation from `MobAttackInfo::bHitAttach` instead of requiring a live mob object lookup, `CMob::GetAttackInfo` at `0x641330` still resolves attack metadata from the live action code, `SKILLENTRY::{GetTileUOL,GetBallUOL}` at `0x437180` / `0x569390` still keep the earlier tile-versus-ball ownership split, and `CSummoned::OnAttack` at `0x753340` still hardcodes both `4111007` and `4211007` onto the same `GetBallUOL` + `RegisterChainlightningAnimation` branch instead of consulting summon-owned projectile branches while keeping the bird family on `GetTileUOL`. This pass stayed inside the existing summon loader, local `SkillManager`, and packet-owned post-effect seams instead of widening into new families: the earlier `SummonClientPostEffectRules`, `ProjectileData.ResolveGetBallLikeUolPath(...)`, `ZoneEffectData.ResolveTileUolPath(...)`, and `SummonedPool.ResolveClientOwnedReactiveAttackChainAnimation(...)` owner-path retention remains in place, and `LifeLoader.BuildAttackInfoMetadata(...)` still keeps indexed `info/hit/<frame>` and `attackN/<frame>/hit` `attach` / `attachfacing` flags even when the same attack already publishes an explicit info-root baseline. The additional lift in this run is that the same tile-versus-ball resolver seam now also drives local post-effects instead of only the packet-owned side: local bird tile overlays no longer flatten back to the base `ZoneAnimation` or ignore authored `effectDistance` when the same skill owns a tile-family resolver, and local `4111007` / `4211007` reactive-chain playback no longer falls through to the generic blue-lightning helper whenever the authored ball branch exists. Packet-owned summon hit playback still no longer drops a later authored frame override just because `info/hit/attach` or `info/attachfacing` already existed on the same attack, `MobAnimationSet.GetAttackHitEffectEntry(...)` still keeps the latest frame-owned `attackN/<frame>/hit` entry whose source frame is at or before the current live attack frame before falling back to `info/hit`, and the packet-owned hit side still keeps the client's template-owned fallback shape inside the same `LifeLoader` / `SummonedPool` seam when no live mob instance of the decoded template is present. This pass also keeps attached packet-owned mob-hit playback on the client zero-offset vec-control branch so authored `info/range/sp` positions no longer leak into `bHitAttach` presentation, while detached fallback still keeps the authored range-origin seam. The earlier branch-owned timing, authored projectile and hit `pos` backfill, bird-tile timing, local named attack-branch prepare suppression, packet-owned raw-action branch selection, and the learned-template Tesla / SG-88 bridge work remain in place. Focused regression coverage now exists in `UnitTest_MapSimulator/SummonClientPostEffectParityTests.cs`, pinning the shared tile-variant resolver, shared tile-UOL / `effectDistance` selection, reactive-chain ball-path selection, and non-reactive rejection on the same `SummonClientPostEffectRules` seam that both local and packet-owned playback now consume. The row remains partial because the remaining gap is narrower but still real: the simulator still does not synthesize every hidden client `MobAttackInfo::sHit` string-owned case one-for-one beyond the currently observed WZ hit surfaces and the modeled sourced-frame attach/facing path; `CSummoned::OnAttack` post-effects are still limited to the confirmed bird tile family plus the confirmed `4111007` / `4211007` reactive-chain family instead of every hidden post-effect case; the exact client-side population path for stored `sTileUOL` / `sBallUOL` fields and any hidden tile-family construction beyond the observed WZ family nodes remain unresolved even though the getter-shaped stored-field selection is now mirrored on both the packet-owned and local post-effect sides; literal GR2 chain-layer ownership, exact textured bolt choreography, and literal action-layer canvas ownership or ordering remain unresolved; the simulator-owned `/summonedpacket` loopback plus the live-session `SummonedOfficialSessionBridgeManager` still stop short of a full official `CUserPool` transport and ownership decode; and the late-packet frame-owned hit-entry hold plus template-backed packet-hit fallback still remain render-safe inferences from the authored WZ hit surface and the client `OnHit` template-owner seam rather than from a newly recovered dedicated client decoder for every deeper mob-state-dependent case. | Packet-owned summons are no longer just spawn/remove stubs: they now keep client-shaped owner registration, packet-owned attack, hit, and removal playback, frame-scoped attached-hit behavior, the confirmed bird tile post-effect family, the confirmed `4111007` / `4211007` reactive-chain family, the selected tile-versus-ball branch identity on the same summon owner without collapsing that ownership back onto generic summon projectile branches, and the authored frame-level hit-attach/facing override seam even when an attack also publishes an info-root baseline. The additional gap closed here is that the shared tile/ball getter seam no longer stops at packet-owned playback: local bird tile overlays now use the same tile-family resolver and authored `effectDistance` cadence as the packet-owned side, and local reactive chains now use the same authored ball branch as packet-owned `OnAttack` playback instead of a generic simulator lightning shortcut. Remaining mismatch risk is now concentrated in deeper hidden `sHit` selection, unresolved client-side tile/ball field population, literal GR2 ownership and ordering, and the still-incomplete official transport and ownership decode rather than in the confirmed reactive-chain alias seam, stored tile-vs-ball getter split, authored frame-level attach/facing override retention, or the now-shared local-versus-packet-owned post-effect resolver inside the existing `SkillManager` / `LifeLoader` / `SummonedPool` seam. | summon runtime and post-effect layer (`CSummonedPool::OnPacket`, `CSummoned::OnAttack`, `CSummoned::OnHit`, `CSummoned::Hit`, `SkillManager.cs`, `SummonedPool.cs`, `PacketOwnedSummonUpdateRules.cs`, `SummonClientPostEffectRules.cs`, `SkillLoader.cs`, `ZoneEffectData`, `ProjectileData`, `LifeLoader.cs`, `LifeLoader.BuildAttackInfoMetadata(...)`, `MobAnimationSet.cs`, `MobAnimationSet.GetAttackHitEffectEntry(...)`, `UnitTest_MapSimulator/SummonClientPostEffectParityTests.cs`, `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, `Skill/312.img/skill/3120010/{ball,tile,summon}`, `Skill/411.img/skill/4111007/{summon,ball}`, `Skill/421.img/skill/4211007/{summon,ball}`, `Mob/3220000.img/attack1/{info/hit,13/hit}`, `Mob/8150302.img/attack1/info/hit/1/attach`, `Mob/9300314.img/attack1/{info/attachfacing,info/hit/attachfacing}`, `Mob/9300275.img/attack1/info/attachfacing`, `SKILLENTRY::GetTileUOL@0x437180`, `SKILLENTRY::GetBallUOL@0x569390`, `CSummoned::OnAttack@0x753340`, `CSummoned::OnHit@0x74bc80`, `CMob::GetAttackInfo@0x641330`) |
| Partial | Remote summoned-pool packet parity | The simulator still keeps the packet-owned summon transport seam in `SummonedPool`, but the remote attack and hit side is tighter again. It already mirrors the observed `0x116`-`0x11B` packet family, local-versus-remote owner registration, packet-owned move-path sampling, Tesla third-coil refresh, taunt-side ownership, packet hit damage bookkeeping, nested mob `attack/info/hit` metadata with UOL resolution, frame-scoped `attach` / `attachfacing` runtime behavior, authored per-frame summon alpha ramps on both live and detached one-time-action playback, the delayed bird tile overlay handoff from the filtered packet target list, and the `4111007` reactive chain with char-level -> exact-level -> root `ball` precedence before summon-owned or generic fallback. WZ re-check against the mounted export still keeps the same confirmed post-effect surface: `Skill/311.img/skill/3111005/tile` publishes six root tile bands plus `effectDistance=10`, `Skill/321.img/skill/3211005/tile` publishes three root tile bands plus `effectDistance=20`, and `Skill/312.img/skill/3120010` keeps the broader `{CharLevel,ball,tile,summon}` surface. This pass also rechecked the mob-hit side against the mounted `Mob` export and kept the same authored hidden hit-owner seam in view: `Mob/3220000.img/attack1/info/hit/attach` still keeps the base attached hit family, `Mob/3220000.img/attack1/13/hit/attach` still publishes a later attack-frame-owned attached hit clip, and the mounted `Sound/Mob.img/3220000` surface still keeps separate `CharDam1` and `CharDam2` keys, which matters because `CSummoned::OnHit` resolves the authored damage sound on the same decoded-template seam as the hit visuals instead of flattening it back to whichever live sibling key happens to be loaded. `Mob/9300314.img/attack1/{info/attachfacing,info/hit/attachfacing}` still keeps the same facing-owned alias seam visible on both the info root and the hit branch. Client evidence stayed second and unchanged on the literal owners this row is using: `SKILLENTRY::GetBallUOL` at `0x569390` still selects char-level data first, then exact level, then root, and prefers `flipBall` when the caller asks for it and that family is non-empty; `SKILLENTRY::GetTileUOL` at `0x437180` still only returns the precomputed `sTileUOL` field; and `CSummoned::OnHit` at `0x74bc80` still resolves summon-hit presentation from decoded `(attackIndex, mobTemplateId, facing)` plus template attack info instead of depending on a live mob object. This run stayed inside the same loader, live-mob, mob-data, and packet-owned summon seams: `SkillLoader` still stores family-owned `tile`, `ball`, and `flipBall` UOL paths separately from the per-frame animation paths for root, `level/<slv>`, and `CharLevel/<requiredLevel>` variants; `ZoneEffectData` and `ProjectileData` still resolve those family UOLs with the same render-safe char-level -> exact-level -> root precedence already used for animation-family selection; `PacketOwnedSummonUpdateRules` still feeds the packet-owned delayed bird overlay plus the scheduled/live `4111007` chain owner with those family UOLs instead of flattened selected-frame paths; and the mob-hit playback side still keeps a short retained live attack-frame observation in `MobItem` so `SummonedPool` can still reuse the matching just-observed attack frame for packet-owned summon hits even after the mob has already stepped off the visible attack action. That still keeps late packets on the last authored frame-owned hit clip a little longer before falling back to `info/hit`, which is especially important for surfaces like `attack1/13/hit` where the authored hidden clip appears late in the attack and the packet can land just after the mob leaves `attack1`. This pass narrows the hidden client `MobAttackInfo::sHit` / `bHitAttach` gap one more step inside that same seam instead of widening transport ownership: `MapleLib` now retains optional hidden mob-attack `sHit` / `hit` strings plus `bHitAttach` / `attach` / `hitAttach` flags from `Mob/*/info/attack/*`, `MobItem` now exposes that decoded attack-data slice alongside the live animation metadata, and `SummonedPool` now applies the hidden attach flag back onto the decoded-template hit metadata before presentation selection. When the chosen live or template owner has no renderable `attack/info/hit` clip at all, `SummonedPool` now also retries a best-effort general-effect fallback from that same hidden attack-data seam by probing the authored path first and then the decoded-template-relative `Mob/{template}.img/{path}`, `Mob/{template}.img/{attackAction}/{path}`, and `Mob/{template}.img/{attackAction}/info/{path}` candidates before giving up, so hidden template-owned hit effects no longer have to flatten immediately to "no visual" just because the animation-set seam stayed empty. The earlier duplicate-template and exact-sound tightening also remains in place: when several live mobs share the decoded `mobTemplateId` and still tie on alive state plus observed `attackN` frame progress, `SummonedPool` still uses the decoded packet-facing bit as the last sibling tiebreaker, and when a live sibling only partially covers the authored hit surface the simulator still keeps the live hit frames while borrowing decoded-template attack metadata plus the exact requested `CharDam` key first, only falling back to live `CharDam1` when template `CharDam2` is also missing. Focused regression coverage is now on disk in `UnitTest_MapSimulator/PacketOwnedSummonHitParityTests.cs` for the hidden `bHitAttach` override seam and the decoded-template general-effect fallback candidate ordering that now underpins this best-effort `sHit` recovery path. The row remains partial because the simulator still does not synthesize every hidden client `MobAttackInfo::sHit` case one-for-one, the new general-effect fallback still depends on recoverable authored paths instead of the client's literal predecoded `sHit` field contents, `CSummoned::OnAttack` post-effects are still limited to the confirmed bird tile family plus the `4111007` reactive chain instead of every hidden post-effect case, exact client `sTileUOL` precompute and any hidden tile-family construction beyond the observed WZ family nodes remain unresolved because current client evidence only exposes `GetTileUOL` returning the stored field, not how every tile UOL is authored, literal GR2 chain-layer ownership, exact textured bolt choreography, and literal action-layer canvas ownership or ordering remain unresolved, and the simulator-owned `/summonedpacket` loopback plus the live-session `SummonedOfficialSessionBridgeManager` still stop short of a full official `CUserPool` transport and ownership decode. | Packet-owned summons are no longer just spawn/remove stubs: they now keep client-shaped owner registration, packet-owned attack/hit/removal playback, frame-scoped attached-hit behavior, the confirmed bird tile post-effect family, the confirmed `4111007` reactive chain, the selected tile/ball branch identity on the same packet-owned owner without collapsing that branch identity back to a selected frame path, the real sourced attack-frame owner for hidden mob hit clips instead of flattening those clips back to metadata frame `0`, a short retained live attack-frame observation so late summon-hit packets do not immediately lose a matching frame-owned hidden hit clip the moment the mob steps off the attack action, template fallback that now reuses that retained frame when the hit presentation has to come from a template-built attack set, template attack-metadata plus exact-requested `CharDam` fallback when the chosen live sibling only partially covers the authored hit surface, a tighter live-mob selector when several instantiated mobs share the same decoded template id, and a best-effort hidden `sHit` / `bHitAttach` bridge through the existing mob-data seam when the visible `attack/info/hit` surface is absent. That keeps future parity work anchored to the same `SummonedPool` seam the client uses for packet-owned summon lifecycle and post-attack presentation, while the remaining hidden hit cases, unresolved tile-UOL precompute, GR2 ownership, and official `CUserPool` transport gaps stay explicit. | packet-owned summon runtime (`CSummonedPool::OnPacket`, `CSummoned::OnAttack`, `CSummoned::OnHit`, `CSummoned::Hit`, `SummonedPool`, `PacketOwnedSummonUpdateRules`, `SkillLoader`, `ZoneEffectData`, `ProjectileData`, `LifeLoader`, `MobAnimationSet.AttackInfoMetadata`, `MobItem`, `MobData.AttackData`, `UnitTest_MapSimulator/PacketOwnedSummonHitParityTests.cs`, `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, `Skill/312.img/skill/3120010/{CharLevel,ball,tile,summon}`, `Mob/3220000.img/attack1/{info/hit,13/hit}`, `Mob/9300314.img/attack1/{info/attachfacing,info/hit/attachfacing}`, `Sound/Mob.img/3220000/{CharDam1,CharDam2}`, `SKILLENTRY::GetTileUOL@0x437180`, `SKILLENTRY::GetBallUOL@0x569390`, `CSummoned::OnHit@0x74bc80`) |
| Partial | Summoned actor update/runtime parity | The simulator still keeps summon-owned actor update/runtime ownership in the same runtime seams: movement snapshots, spawn/prepare/attack/hit/die routing, Tesla armed-prepare idle fallback, self-destruct expiry handoff, packet-owned hit-overlay interrupt clearing, named-branch timing that avoids forcing generic `summon/prepare` onto authored branches, and the shared suspend window that keeps authored support or summon-action branches from being immediately retriggered. This pass stayed inside `SummonRuntimeRules`, `PacketOwnedSummonUpdateRules`, `SkillManager`, and `SummonedPool` again and tightened the next Tesla state-machine drift without widening transport or combat ownership. WZ was rechecked first against the mounted export for the authored branches that constrain this runtime split: `Skill/3511.img/skill/35111011/summon/heal` still publishes the Healing Robot support branch; `Skill/3512.img/skill/35121009/{info/{type,minionAbility,selfDestructMinion},summon/subsummon}` still publishes the summon-action family as `type=33`, `minionAbility=summon`, `selfDestructMinion=1`, and an authored six-frame `subsummon` clip totaling `720ms`; the stand-only support skills under `Skill/3511.img/skill/35111005/summon/stand` and `Skill/3512.img/skill/35121010/summon/stand` still keep `info/selfDestructMinion=1` without separate authored `heal` / `support` branches; and `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}` still publishes the Tesla Coil armed/attack branch family that constrains its runtime handoff. Client evidence was then rechecked in IDA: `CSummoned::OnSkill` at `0x74a940` still clears any older one-time owner before routing the decoded packet action into `CSummoned::SetAttackAction`, `CSummoned::SetAttackAction` at `0x74a0f0` still just stamps `m_nOneTimeAction` and re-prepares the action layer, `CSummoned::Update` at `0x757f70` still keeps that one-time owner alive through `ProcessAction` / `ProcessAttack` until the clip finishes while also forcing move action `0x2A` during Tesla states `1` and `2` before `SetMoveAction`, `CSummoned::TryDoingHealingRobot` at `0x74b3e0` still sends action `13` and sets the suspend flag after the authored heal branch begins, `CSummoned::TryDoingSummon` at `0x74b640` still sends action `14` and sets that same suspend flag for summon-action playback, and `CSummoned::TryDoingTaslaCoilAttack` at `0x752780` still only enters the authored Tesla attack path while `m_nTeslaCoilState == 1`. The simulator already mirrored the earlier packet-owned explicit self-destruct owner detail through `SummonedPool.ResolvePacketOwnedExplicitSelfDestructPlayback(...)`, `TryBeginSelfDestructRemoval(...)`, `TryTriggerExpiredSelfDestructAction(...)`, the Tesla sibling stale-owner clear inside `BeginPacketOwnedSkillAnimation(...)`, and the shared authored suspend seam for actions `13` / `14`; this run keeps that same actor/runtime split and closes the next Tesla refresh seam on both owners. Local Tesla attack dispatch still requires armed state `1` before it can fan into the authored triangle branch, idle refresh still re-arms local Tesla runtime state `2` back to armed state `1` once the one-time attack playback has actually finished, and the existing third-coil refresh boundary now also re-arms packet-owned Tesla runtime state back to armed state `1` while clearing stale packet-tracked triangle points instead of letting refresh inherit the prior volley's latched attack shape. Focused regression coverage is now on disk in `UnitTest_MapSimulator/SummonRuntimeActionParityTests.cs` for Tesla re-arm after action playback, Tesla state retention while playback is still active, the armed-state gate, local refresh re-arm plus triangle clear, and the packet-owned refresh re-arm plus packet-triangle clear helper. The row remains partial because the deeper client-only seams are still outside current simulator visibility: exact hidden summon HP and full `CalcDamage::PDamageSummoned` / `MDamageSummoned`, broader `CVecCtrl::UpdatePassive` side effects beyond the confirmed passive effect trigger, deeper packet-owned or hidden Tesla state transitions and refresh rules beyond the now-modeled third-coil re-arm boundary, and the remaining remote assist-family branch choices that still depend on client-only state not exposed by current packet/WZ data. | This keeps more of real summon runtime ownership inside the summon layer instead of collapsing either local or packet-owned assist families to ad hoc runtime fallbacks: local and packet-owned body-contact damage still share the same mob-authored base-damage surface plus outgoing-damage modifiers, packet-owned `OnSkill` branch ownership still lands on authored `skill1..skill6` when packet actions provide those branch ids, packet-owned named summon branches still avoid inheriting a generic prepare window, packet-owned generic prepare branches still promote into live attack state instead of staying pinned on one coarse packet state through the whole action, packet-owned mob-hit overlays still get interrupted both when summon-owned action-layer changes begin and when packet-owned attack playback completes back into the steady summon layer, summon-action playback still shares the same authored suspend seam the client uses for both action `13` and action `14`, and local Tesla attack ownership no longer treats runtime state `2` as if it were permanently armed. The additional gap closed here is that the simulator now keeps the Tesla third-coil refresh boundary on the same re-arm seam for both local and packet-owned runtime state: local refresh still re-arms existing coils and clears the latched local triangle points, while packet-owned refresh now also resets the mirrored Tesla runtime state back to `1` and clears stale packet-tracked triangle vertices before later packet-owned attack playback re-populates them. Remaining mismatch risk is now concentrated in deeper hidden damage formulas, unmodeled passive-update side effects, deeper packet-owned or hidden Tesla transitions beyond the now-modeled third-coil re-arm boundary, and the last remote/client-only assist selectors rather than in the observed authored support-branch surface, authored summon-action suspend ownership, packet-owned explicit self-destruct branch ownership, packet-owned Tesla sibling one-time-owner cleanup, packet-owned named-branch retry suppression, or the recovered Tesla armed-state gate plus refresh re-arm seam. | summon actor/runtime layer (`CSummoned::Update`, `CVecCtrl::UpdateActive`, `CVecCtrl::UpdatePassive`, `CSummoned::OnHit`, `CSummoned::OnSkill`, `CSummoned::SetAttackAction`, `CSummoned::SetMoveAction`, `CSummoned::ProcessAction`, `CSummoned::ProcessAttack`, `CSummoned::SetDamaged`, `CSummoned::TryDoingHealingRobot`, `CSummoned::TryDoingSummon`, `CSummoned::TryDoingTaslaCoilAttack`, `SummonRuntimeRules.cs`, `PacketOwnedSummonUpdateRules.cs`, `SkillManager.cs`, `SummonedPool.cs`, `SummonedPool.ResolvePacketOwnedExplicitSelfDestructPlayback(...)`, `BeginPacketOwnedSkillAnimation(...)`, `TryResolveSummonAttackPlaybackAnimation(...)`, `UnitTest_MapSimulator/SummonRuntimeActionParityTests.cs`, IDA `CSummoned::Update` (`0x757f70`), IDA `CSummoned::OnSkill` (`0x74a940`), IDA `CSummoned::SetAttackAction` (`0x74a0f0`), IDA `CSummoned::TryDoingHealingRobot` (`0x74b3e0`), IDA `CSummoned::TryDoingSummon` (`0x74b640`), IDA `CSummoned::TryDoingTaslaCoilAttack` (`0x752780`), `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3512.img/skill/35121009/{info/{type,minionAbility,selfDestructMinion},summon/subsummon}`, `Skill/3511.img/skill/35111005/{info/selfDestructMinion,summon/stand}`, `Skill/3512.img/skill/35121010/{info/selfDestructMinion,summon/stand}`, `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}`) |
| Partial | Weapon afterimage / melee trail parity | The simulator keeps the broader WZ-backed afterimage surface already landed on this row: skill-authored plus shared `Character/Afterimage/<family>/<masteryIndex>` catalogs, weapon-item `info/afterImage` family selection with narrow sword-sibling fallback only, local basic-melee registration, the confirmed Page / Paladin / Thunder Breaker / Aran `charge/<element>` rewrites, alias-aware action matching, `1221009 blast -> stabO2`, the client-backed raw-range normalization for `1221009`, `57 -> 41`, and `74`, ordered remote WZ action fallback, single-authored remote pose promotion, remote prepared-skill release teardown from the official melee packet, remote charge fallback in the shared remote temporary-stat plus remote melee owner, non-looping action-end snapshot timing, client-style one-sided `a0` / `a1` endpoint defaulting, wrapper-side preservation for lone authored alpha metadata, and the current one-sided `z1` growth fallback from the resident `100%` zoom inside the SpriteBatch seam. This pass stayed inside the same loader plus playback seams and closed the next real playback drift instead of widening ownership. WZ was rechecked first against the mounted export: `Character/Afterimage/swordOS/charge/1/stabO2/1` still publishes two authored canvases (`0`, `1`) with `delay=50` then `delay=300`, `Character/Afterimage/swordOS/0/swingO1/2/0` still publishes a lone canvas with `delay=200` and `a1=0`, `Character/Afterimage/swordOS/0/swingD1/1/2` still publishes a lone authored `a0=255`, `Skill/122.img/skill/1221009/afterimage/swordOS/stabO2/1/0` still publishes `delay=60`, the sampled `swordOS` plus `knuckle` afterimage catalogs still do not expose authored `z0` or `z1`, and the broader `LoadCanvas` surface still proves one-sided zoom authorship through `Skill/131.img/skill/1311008/effect/16` publishing `delay=200`, `a1=64`, and `z1=200` with no sibling `z0`. Client evidence was rechecked second in IDA: `CActionMan::CreateAfterimageLayer` at `0x428a20` still resets layer alpha, calls `RemoveCanvas(-2)`, resolves the weapon afterimage array for the active raw-action branch, and then calls `CAnimationDisplayer::LoadCanvas` once for every authored canvas in that branch, while `CAnimationDisplayer::LoadCanvas` at `0x446e60` still reads each canvas node's `delay`, `a0`, `a1`, `z0`, and `z1` into independent `InsertCanvas` arguments. `MeleeAfterimagePlaybackResolver.ResolveRenderableLayers(...)` now mirrors that multi-canvas ownership more closely by keeping every authored frame-set canvas alive in client authored order against the same shared elapsed time, with each canvas expiring against its own delay and resolving alpha/zoom from its own metadata instead of incorrectly sequencing the whole frame-set down to a single active canvas. This pass also narrows the current SpriteBatch-side `InsertCanvas` approximation: local and remote afterimage drawing now share one resolver that preserves the WZ-authored origin as the SpriteBatch origin, mirrors that origin for flipped layers, and keeps zoom as a floating scale instead of pre-rounding each afterimage canvas into an integer destination rectangle before draw. Focused regression coverage is now on disk in `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` for multi-canvas authored-order preservation, independent per-canvas expiry, shared-elapsed zoom interpolation across simultaneous afterimage layers, mirrored authored-origin selection, and floating zoom-scale retention. The row stays `Partial` because the remaining gap is now concentrated in unobserved afterimage-specific zoom authorship in mounted data, the deeper GR2 layer-object behavior still beyond SpriteBatch (`RelMove`, `RemoveCanvas`, `InsertCanvas`, layer timing/ownership and exact interpolation), further range overrides that still need fresh client evidence beyond `1221009` / `57` / `74`, and the rest of the official remote melee transport and side effects beyond the currently consumed attack envelope, mob-hit, bullet-item, release-follow-up, broader remote charge-family fallback, and explicit-skill-range-owner slices. | Many melee jobs rely on afterimage trails and authored swing ranges to sell timing and hit shape, and this row now keeps the client-backed WZ trail family, alpha endpoints, per-canvas lifetime, multi-canvas authored stack, known range-owner normalization, and current remote/local owner seams aligned more closely with the official client instead of collapsing authored multi-canvas branches into one sequenced sprite. The row remains `Partial` because the visible mismatch is now concentrated in literal GR2 layer-object behavior, unevidenced extra range overrides, absent afterimage-specific zoom samples in the mounted export, and the remaining official remote transport rather than in one-sided alpha handling, release-edge frame capture, simultaneous multi-canvas playback, or the now-shared floating-origin SpriteBatch draw approximation inside the current seam. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `CharacterLoader.cs`, `CharacterAssembler.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `CharacterData.cs`, `MapSimulator.RemoteUsers.cs`, `PreparedSkillHudRules.cs`, `ClientMeleeAfterimageRangeResolver.cs`, `AfterImageChargeSkillResolver.cs`, `MeleeAfterimagePlaybackResolver.cs`, `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` (`CActionMan::Init`, `CActionMan::GetWeaponAfterImage`, `CActionMan::CreateAfterimageLayer`, `CActionMan::GetMeleeAttackRange`, `CAnimationDisplayer::LoadCanvas`, `get_action_name_from_code`, `CUser::GetAfterimageUOL`, `CUser::RegisterAfterimage`, `CUserRemote::OnAttack`, `CUserRemote::OnMeleeAttack`, `SecondaryStat::DecodeForRemote`, `Character/00002000.img`, `Character/Afterimage/swordOS/{0/swingO1/2,0/swingD1/1/2,charge/1/stabO2/1}`, `Character/Afterimage/*`, `Skill/122.img/skill/1221009/{action,afterimage}`, `Skill/131.img/skill/1311008/effect/16`, `Skill/434.img/4341004/common/{lt,rb}`, remote temporary-stat payloads) |
| Partial | Skill cooldown UI parity | QuickSlot overlays and skill hover cooldown presentation, both skill-book icon overlays, the hotkey and off-bar cooldown trays, the shared packet-owned/server-owned cooldown ownership seam in `SkillManager`, the `UI/Basic.img/Notice3` off-bar notice stack, and the Battleship sentinel durability-side effect path still live on the same existing cooldown UI/runtime seam instead of parallel per-surface cooldown math. This run kept that shared owner in place, rechecked the mounted WZ evidence first, and narrowed the notice-family seam without widening the runtime surface: `UI/UIWindow.img/Skill/CoolTime` still exposes `16` stepped cooldown-mask canvases, `UI/UIWindow2.img/Skill/main/CoolTime` still exposes the matching `16` stepped family, and `UI/Basic.img/Notice3` still exposes the exact `t/c/s` stack at `266x21`, `266x20`, and `266x55` while sibling families keep drifting away from that authored shape (`Notice4` adds the extra `s_line` inner panel and a taller `78`-pixel bottom, `Notice2` stays at `258` width, `Notice5` drops the `s` panel entirely, and `Notice6` shrinks to `260x20/16/15`). The mounted tooltip family still anchors the hover surfaces the client owns: `UI/UIWindow2.img/Skill/main/tip0`, `tip1`, and `tip2` still expose the same `193x102` family with authored origins `(195,-25)`, `(-178,-22)`, and `(103,-282)`, while `UI/UIWindow.img/Skill/tip0`, `tip1`, and `tip2` still expose the legacy `193x102` family with zero origins. `Skill/522.img/skill/5221006/common` still authors `mpCon`, `epad`, `epdd`, `emdd`, `emhp`, `emmp`, and `cooltime`, so the existing `SkillLevelData` authored-property seam remains the right owner for Battleship-family tooltip ordering. Client evidence stayed second: `CUIStatusBar::Draw` still owns the tray surface, `CUISkill::Draw` still owns the book surface, and `CUIToolTip::DrawReqSkill` still confirms the client keeps skill-book tooltip composition under the shared skill-tooltip owner rather than duplicating per-surface stat formatting. The source seam remains shared and tighter: `SkillManager.ResolveCooldownMaskFrameIndex` still consumes the full authored `0..15` cooldown-mask family with continuous duration progress, `SkillManager.ResolveCooldownMaskFallbackFillRatio` still preserves the authored `CoolTime/15` last live sliver when WZ masks are missing, `QuickSlotUI`, `SkillUI`, `SkillUIBigBang`, and both status-bar cooldown trays still source mask frames and countdown text through the shared `SkillManager` cooldown-visual helper on top of `TryGetCooldownUiState`, and `SkillCooldownTooltipText` still exports the shared state-line plus secondary-line splitter. `SkillTooltipFrameLayout` still owns the shared mounted tooltip-frame geometry, client-sized `320x114` hover footprint, and authored-origin placement math that `SkillUI`, `SkillUIBigBang`, `QuickSlotUI`, and `StatusBarUI` all consume, so quick-slot skill hover and tray cooldown hover still scale the mounted `tip0`/`tip1`/`tip2` family to the same client-shaped tooltip body instead of drawing a narrow texture-width frame with surface-local anchor math. This pass also hardens the off-bar art resolver itself on that same WZ-backed seam: `SkillCooldownNoticeUI.ResolveNoticeFrameFamilyForClientParity` now fast-paths the exact authored `266x21/20/55` tri-panel family and therefore selects the mounted `Notice3` stack deterministically before falling back to the broader scorer, instead of leaving the final choice entirely to the approximate width/height heuristic when multiple notice families are mounted. Status-bar tray hover still carries the same secondary-line formatter through the existing cooldown render-data cache, and the packet-owned Battleship sentinel durability path still updates `SkillManager.SetAuthoritativeVehicleDurabilityPresentation` so durability text stays on the same authoritative tooltip seam while remaining out of cooldown-mask visuals. Focused regression coverage is now on disk in `UnitTest_MapSimulator/SkillCooldownUiParityTests.cs` for the exact `Notice3` family pick, the legacy same-family tooltip-origin fallback, and the shared no-mask/no-counter durability visual contract. The remaining parity gap is narrower but still real: any last notice micro-offset or timing/fade differences beyond the shared `44px` top-center seam, any still-unrecovered legacy skill-book anchor offsets beyond the current mounted zero-origin fallback on `UI/UIWindow.img/Skill/tip*`, exact Battleship `CTemporaryStatView` icon/tray art and any durability-specific HUD widgeting beyond the mounted override plus shared UI-state durability presentation already modeled here, and any additional bespoke cooldown-only HUD widgets beyond the quick-slot, skill windows, status-bar trays, and off-bar notice stack. | Cooling-down skills remain synchronized across quick-slot masks, both skill-book variants, both status-bar trays, both skill-book hover tooltips, tray hover state lines, tray hover secondary cost/stat lines, and off-bar started/blocked/ready notices because those surfaces all read the same authoritative cooldown UI-state seam, one cooldown-visual helper, and one tooltip formatter family on top of the authored `CoolTime` family. Packet-owned refreshes still preserve the original cooldown start tick without retriggering the start-notice seam, authoritative clears and natural expiry still stop every cooldown surface together without falling back to authored WZ timers, the shared mask helper still reaches the authored `CoolTime/15` last-step frame before cooldown completion instead of flattening to a `0..14` range, and the quick-slot plus tray fallback rectangles still mirror that same final sliver when WZ masks are absent instead of dropping early. Quick-slot hover, tray hover, and both skill-book variants still keep cooldown or durability state on a dedicated shared primary line, keep MP/authored stat costs on the same shared secondary-line formatter, and scale the mounted skill tooltip frame family to the client-sized hover body instead of leaving the quick-slot and tray surfaces on the raw `193px` frame width. Battleship durability/stat presentation still remains routed through the shared formatter, the packet-owned sentinel's `SkillManager` vehicle-durability presentation, and WZ-authored property order, while the notice-family selector is now pinned to the exact mounted `Notice3` shape and the shared durability visual seam now has explicit regression coverage for "tooltip-only, no mask" behavior. The simulator still does not reproduce the client's final notice timing or micro-offset polish, any last legacy skill-book anchor offsets beyond the mounted zero-origin fallback, exact Battleship temporary-stat tray/icon visuals, or any extra cooldown-only HUD widgets that may still live outside the currently modeled shared seams. | `QuickSlotUI.cs`, `SkillUI.cs`, `SkillUIBigBang.cs`, `SkillCooldownTooltipText.cs`, `SkillTooltipFrameLayout.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `StatusBarUI.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `SkillManager.cs`, `SkillCooldownNoticeUI.cs`, `SkillCooldownNotificationTransition.cs`, `Skill/522.img/skill/5221006/common`, `UI/UIWindow.img/Skill/CoolTime`, `UI/UIWindow.img/Skill/tip0`, `UI/UIWindow.img/Skill/tip1`, `UI/UIWindow.img/Skill/tip2`, `UI/UIWindow2.img/Skill/main/CoolTime`, `UI/UIWindow2.img/Skill/main/tip0`, `UI/UIWindow2.img/Skill/main/tip1`, `UI/UIWindow2.img/Skill/main/tip2`, `UI/Basic.img/Notice3`, `UnitTest_MapSimulator/SkillCooldownUiParityTests.cs`, `CUIToolTip::DrawReqSkill@0x88b320`, `CUISkill::Draw@0x84ed90`, `CUIStatusBar::Draw@0x876cd0` |
| Partial | Client-timer skill expiry and cancellation | `SkillManager` still owns the centralized client-timer registry for buff expiry, timed prepared-skill release, repeat-sustain expiry and mode-end, cyclone teardown, Wild Hunter swallow digest completion, WZ-timed local skill-zone expiry, and WZ-timed local summon expiry through one `UpdateClientTimer`-style seam, and cancel routing still normalizes through connected WZ `info/type`, `affectedSkill`, and `dummyOf` families plus the client-owned pre-send alias rewrite in `CUserLocal::SendSkillCancelRequest`. `SummonedPool` still reuses that connected-family matcher for packet-owned local summon cancellation, connected-family level recovery, authored-duration fallback, inherited local-duration fallback, authored pending-removal priming for natural non-self-destruct expiry, and packet-owned self-destruct teardown. WZ was rechecked first for the representative local skill-zone branch as well as the existing support self-destruct branch: `Skill/422.img/skill/4221006` still publishes `info/type=36`, `info/zoneType=invincible`, `common/time=30+x`, and authored `tile` / `effect` frames for the local zone lifetime, while `Skill/3511.img/skill/35111011` still publishes `info/type=33`, `info/minionAbility=heal`, `info/condition=whenUserLieDown`, `info/selfDestructMinion=1`, `common/selfDestruction`, `summon/{summoned,stand,heal,die}`, `summon/heal/info/range lt=(-125,-100) rb=(125,0)`, `summon/die/info/range lt=(-120,-110) rb=(120,10)`, `summon/die/info/mobCount=6`, and `hit/0/hitAfter=990`; the connected cancel-family WZ evidence this timer bridge depends on also remains present in the current extracted data source, including the Battle Mage aura alias rows that line up with the client rewrite (`32120000 -> 32001003`, `32110000 -> 32101002`, `32120001 -> 32101003`) plus the linked `dummyOf` and `affectedSkill` rows already feeding the same matcher. Direct IDA re-check stayed second and still anchors the client seam at `CUserLocal::SendSkillCancelRequest` (`0x93d730`) for cancel-side fallout and `CSummoned::Update` (`0x757f70`) plus `CSummoned::TryDoingAttack` (`0x756860`) for expiry/runtime ownership, including the hardcoded pre-send rewrites, the vehicle-valid branch, the keydown prepare-animation removal gate, the local dragon one-time-action cleanup, the mirrored-rectangle probe, and the owner-side choice before `ProcessAction` / `ProcessAttack`. This pass stayed inside that same seam and closed the next packet-owned cancel drift instead of widening runtime ownership: packet-owned summon timer fallback still preserves the authored natural-expiry pending-removal branch before direct teardown, generic packet-owned local summon cancellation still preserves that same authored branch whenever a matched summon has already crossed its natural-expiry boundary, packet-owned self-destruct expiry target recovery still rejects stale packet-history targets that no longer intersect either WZ-authored summon attack facing before falling back, and that fallback now mirrors more of the client `TryDoingAttack` seam by running an explicit mirrored-rectangle probe plus owner-side tie-break before it falls back to the broader authored-range score. The owner-side tie-break on that fallback no longer stops at local owners either; packet-owned remote expiry presentation now reuses the same recovered owner-position seam when a remote owner actor is available. The earlier local summon-expiry, packet-owned local connected-family batch cancel, and local skill-zone-expiry dedupe seams remain in place on that same owner surface, and focused regression coverage is now on disk in `UnitTest_MapSimulator/ClientSkillTimerCancelParityTests.cs` for the new mirrored-probe owner-side selector together with the row's existing alias-rewrite, vehicle-valid owner recovery, keydown prepare-preserve gating, and timer-batch dedupe coverage. The row remains partial because the simulator still does not mirror the client's full timer catalog or full `CSummoned::Update` combat runtime: packet-owned self-destruct expiry now follows the mirrored-rectangle plus owner-side seam more closely when it has to synthesize a fallback target set, but final target acquisition and combat results still stop at the simulator-authored range/order approximation instead of the full client `FindHitMobInRect` plus damage-runtime path when no authoritative packet target history exists, locally synthesized remote expiry still stops at presentation instead of recreating authoritative remote combat fallout, and any additional `SendSkillCancelRequest` consumers beyond the currently surfaced prepared-skill, swallow-reset, vehicle-valid, keydown-cleanup, dragon one-time-action, local summon-expiry, packet-owned local summon-expiry, packet-owned local expired-on-cancel fallback, and local skill-zone-expiry fallout are still outside the current local WZ plus packet plus connected-family timer view. | Timed skill teardown still runs through one shared local seam instead of scattered expiry checks, and connected WZ cancel families plus the client's hardcoded Battle Mage alias rewrites still tear down prepared state, repeat/cyclone state, buffs, local summons, local zones, and packet-owned local summons through the same normalized request surface. This run narrows the remaining timer drift again by making the packet-owned self-destruct fallback choose facing through the same mirrored-rectangle plus owner-side seam surfaced in `CSummoned::TryDoingAttack`, by reusing owner-position recovery for remote owners when that tie-break is available, and by keeping the earlier natural-expiry pending-removal preservation and stale packet-target rejection in place ahead of that fallback. Remaining risk is now concentrated on unmodeled client-owned timer families, the exact `FindHitMobInRect` and damage-runtime rules that still sit behind the current synthesized fallback once no authoritative packet target evidence exists, and any additional `SendSkillCancelRequest` consumers not yet surfaced after the prepared-skill, swallow-reset, vehicle-valid, keydown-cleanup, dragon one-time-action, local summon-expiry, packet-owned local summon-expiry, packet-owned local expired-on-cancel fallback, and local skill-zone-expiry cancel-side effects. | `ClientSkillCancelResolver`, `SkillManager`, `SummonedPool`, `MapSimulator.PacketOwnedSummonedParity.cs`, `UnitTest_MapSimulator/ClientSkillTimerCancelParityTests.cs` (`CUserLocal::UpdateClientTimer`, `CUserLocal::SendSkillCancelRequest`, `CSummoned::Update`, `CSummoned::TryDoingAttack`, `Skill/422.img/skill/4221006/info`, `Skill/422.img/skill/4221006/common`, `Skill/3511.img/skill/35111011/info`, `Skill/3511.img/skill/35111011/common`, `Skill/3511.img/skill/35111011/summon`, `Skill/3511.img/skill/35111011/hit`, `Skill/3212.img/skill/32120000/info/affectedSkill`, `Skill/3211.img/skill/32110000/info/affectedSkill`, `Skill/3212.img/skill/32120001/info/affectedSkill`) |
| Partial | Skill restrictions by map/state | The simulator still keeps the shared restriction seam already landed for this row: parsed map `fieldLimit` bits for `Unable_To_Use_Skill`, `Move_Skill_Only`, `Unable_To_Use_Mystic_Door`, the Mechanic-only `Unable_To_Use_Rocket_Boost` branch, and the mount / broader vehicle `Unable_To_Use_Taming_Mob` branch; the field-authored `info/moveLimit` movement-skill counter seam in `FieldRuleRuntime`; live `MapInfo` ownership for Coconut, Snowball, and Guild Boss basic-action owners; WZ-authored root `noSkill/skill`, class-number `noSkill/class`, and `info/noCancelSkill`; Mu Lung Dojo `mobMassacre/disableSkill`; the authored massacre `countEffect/*/skillUse` counter seam; shared mob-disease skill lockouts including `StopMotion` (`136`); ladder/rope vetoes for swallow plus the broader `DoActiveSkill_ShootAttack` family; and scripted direction-mode ownership that tears down prepared or upkeep-driven skills once local control is taken away. WZ re-check still shows map-authored `noSkill/skill`, class-number `noSkill/class`, `info/noCancelSkill`, and `info/moveLimit` surfaces on the representative maps already tied to this row; `Map/Map9/980031100.img/noSkill/class` still publishes `3` and `4`, `Map/Map2/200100001.img/noSkill/class` still publishes `1`, `2`, `3`, and `4`, and `Map/Map9/922240000.img/noSkill/skill` still publishes direct skill ids including `5221006`, `1013`, `10001014`, `5111005`, `5121003`, `13111005`, `15111002`, and `5101007`. The same mounted skill data that already backed the earlier Dual Blade tier fix also keeps another pair of irregular first-job roots visible in the active data source: `Skill/2002.img/skill` still publishes direct first-stage Mercedes skill ids such as `20021000`, `20021001`, `20021003`, `20021005`, `20021006`, and `20021013`, and `Skill/3001.img/skill` still publishes direct first-stage Demon skill ids such as `30011000`, `30011001`, `30011003`, `30011005`, `30011006`, and `30011013`. Those roots sit outside the plain `% 10` job-branch heuristic the simulator was still using for `noSkill/class`, so the shared `FieldSkillRestrictionEvaluator` now keeps them on the same client-facing first-job tier as the rest of this surface instead of misclassifying `2002` as fourth job and `3001` as third job. This pass also rechecked another visible special first-job root in WZ: `Skill/501.img/skill` publishes Cannoneer first-stage ids such as `5011000`, `5011001`, `5011002`, `5010003`, `5010004`, and `5010005`, so that root now joins the class-one irregular-root seam instead of being misclassified as third job by the same branch-digit shortcut. This pass rechecked the other side of that generic branch shortcut as well: `Skill/508.img/skill` still publishes direct special-root ids such as `5080000`, `5081001`, `5081002`, `5081003`, `5080004`, and `5081005`, while the recovered `get_job_level` helper only returns a normal non-Evan class grade when `(job % 10) + 2` lands in `2..4`; roots like `508` now stay unmapped for `noSkill/class` unless the map lists the skill id directly instead of being misclassified as second job by the simulator fallback. WZ re-check also confirms the remaining admin-root surface is narrower than the simulator fallback had assumed: `Skill/800.img/skill` still only publishes exact `800`-root entries such as `8001000` and `8001001`, and `Skill/900.img/skill` still only publishes exact `900`-root entries such as `9001000`, `9001001`, `9001002`, `9001003`, `9001004`, and `9001020`. Direct IDA re-check of `Field::SkillInfo::IsSkill` (`0x5657e0`) and its `get_skill_class` / `get_job_level` helpers (`0x565320`, `0x47cb90`) also tightened the class-number matcher itself: Evan roots use the client's grouped `class` ranges (`1 => get_job_level 1-2`, `2 => 3-6`, `3 => 7-8`, `4 => 9-10`), Dual Blade roots use the client's explicit `430` through `434` grouping (`class 1 => 430`, `class 2 => 431-432`, `class 3 => 433`, `class 4 => 434`), the generic hundred-root rule now stays on the client's exact `% 100 == 0` cutoff instead of treating the whole `800..999` band as first job, and the same `skill` / `class` checks still walk nested descendants under the `noSkill` root instead of requiring those child names to sit directly under `noSkill`. Direct IDA re-check of the `CField::IsUnableToUseSkill` callers still confirms the client does not blanket-reject every skill under that field-limit bit. `CUserLocal::TryDoingMeleeAttack` (`0x91e780`), `TryDoingShootAttack` (`0x925a00`), and `TryDoingMagicAttack` (`0x92a240`) each guard `CField::IsUnableToUseSkill` with explicit recovered skill-id tables only, so the shared `FieldSkillRestrictionEvaluator` still blocks just that recovered client set (`4211002`, `4221001`, `1121006`, `1221007`, `1321003`, `4321001`, `4121008`, `5101002`, `5101004`, `5121005`, `21100002`, `21110003`, `21110006`, `4311003`, `4331000`, `4331004`, `4331005`, `4341002`, `33111002`, `35001003`, `1121001`, `1321001`, `3120010`, `3121003`, `3221003`, `5201006`, `32101001`, `32111011`) instead of flattening the whole bit into a simulator-wide "most skills" ban. That keeps representative `noSkill/class = 3,4` maps blocking third- and fourth-job skills, leaves nonmatching second-job skills and `class = 1` branch-ancestry false positives alone, keeps first-job Dual Blade skills blocked on `class = 1` maps, moves the second Dual Blade group back under client `class = 2`, keeps early Evan roots under the client grouped class ranges, now also keeps first-job Mercedes, Demon, Cannoneer, and exact `800` / `900` admin-root skills on the client-facing class-one tier, leaves `801` / `901`-style synthetic non-hundred roots unmapped instead of falsely treating them as first job, and still preserves those same client-facing restrictions when map data wraps the existing `skill` or `class` lists one level deeper under the `noSkill` surface. The simulator still no longer treats Thunder Breaker `Dash` (`15101003`) as client-forbidden under `Unable_To_Use_Skill`, still no longer treats every active Snowball round as if `CField_SnowBall::BasicActionAttack` had already taken local ownership, still no longer treats every active Coconut round as if `CField_Coconut::BasicActionAttack` had already taken local ownership, still no longer treats merely standing in the Guild Boss pulley area, a live pulley state, or a pending pulley preview sequence as if `CField_GuildBoss::BasicActionAttack` had already taken local ownership, still mirrors the recovered `TryDoingMagicAttack` no-foothold gate instead of broadly allowing airborne magic outside prepare-skill cases, and still keys Evan dragon-root magic off the live dragon-owner seam rather than only the map wrapper. Focused on-disk regression coverage for this row is now present in `UnitTest_MapSimulator/PlayerSkillStateRestrictionParityTests.cs` for the added Mercedes `2002`, Demon `3001`, and Cannoneer `501` first-job root cases plus preserved Resistance `3000`, newly covered `508` non-block cases, and the exact-hundreds `800` / `900` admin-root class-one cases plus `801` / `901` non-block cases, alongside the earlier WZ-authored `noSkill/class` class-number matching, class-one ancestry false-positive suppression, nonmatching second-job allowance, direct `noSkill/skill` blocking, irregular Dual Blade first-job tier case, newly client-confirmed Dual Blade second-group and Evan grouped-class cases, and nested-wrapper `noSkill/skill` plus `noSkill/class` handling already tracked for this row. The remaining gap is narrower but still real: deeper `CField` availability checks and remaining field-owned or client-only forbidden-skill branches outside the now-modeled recovered `Unable_To_Use_Skill` table, any still-unrecovered Coconut or Snowball ownership beyond the now-modeled local normal-attack / touch-owner seams, any still-unrecovered `noSkill` child shapes beyond nested `skill` ids and class-number entries, any remaining irregular first-job roots or client-only tier rewrites beyond the WZ-backed `501`, `2002`, `3001`, the client-helper-backed generic branch cutoff now covered by `508`, the exact-hundreds admin-root cutoff now rechecked through `800` and `900`, client-confirmed Evan grouping, and client-confirmed Dual Blade grouping coverage, any remaining local-state cast gates outside the now-modeled magic no-foothold allowlist plus the earlier swallow / shoot / vehicle / one-time-action seams, and any additional client-only dragon-owner loss cases beyond the simulator's current live dragon runtime are still incomplete. | Maps and transient local states now block skill use closer to the client from one shared place instead of scattered path-specific checks: field-authored skill bans still flow through the live map seam, field-authored movement-skill quotas now stay pinned to the same field-rule owner that decrements successful local movement casts, prepared or sustained skills still tear down when local state, mob-disease, scripted control, or massacre counters turn invalid after cast start, active skill requests are still rejected while the client-owned one-time-action window is playing, airborne magic now no longer bypasses the recovered `TryDoingMagicAttack` no-foothold gate, `Unable_To_Use_Skill` maps now no longer suppress unrelated skills that the client does not reject under that bit and no longer overblock `15101003`, Snowball no longer suppresses skills for the whole active round when local basic-action ownership has not actually engaged, Coconut no longer suppresses skills for the whole active round when no fresh local normal-attack request owns the basic-action packet seam, Guild Boss no longer suppresses skills just because the player is inside the pulley rectangle or the pulley state is nonzero, Evan dragon-root magic now follows the live local dragon-owner seam rather than only the no-dragon map wrapper, irregular Dual Blade job roots now honor the same authored `noSkill/class` advancement tiers that the client-facing map data publishes, and the same known `noSkill/skill` plus `noSkill/class` surfaces now continue to apply even when those child lists are wrapped under an intermediate `noSkill` node instead of sitting directly at the root. This pass narrows the remaining class-tier drift further by keeping the WZ-backed Cannoneer `501`, Mercedes `2002`, Demon `3001`, and exact `800` / `900` admin roots on that same client-facing class-one tier, by leaving `508`-root skills unmapped for `noSkill/class` unless directly listed, by leaving `801` / `901`-style non-hundred roots outside that tier, and by mirroring the client-confirmed Evan and Dual Blade grouped `noSkill/class` comparisons instead of treating every root as a plain branch digit. The row stays `Partial` because the remaining mismatch is now concentrated in unrecovered `CField` gate branches and any additional client-only forbidden-state or special-field owners beyond the currently modeled field limits, recovered `Unable_To_Use_Skill` skill-id set, recovered magic no-foothold allowlist, live dragon-owner seam, `moveLimit`, the narrowed Coconut/Snowball/Guild Boss local owner seams, and the currently modeled `noSkill` child plus irregular-root and grouped-class family set. | `MapSimulator`, `SkillManager`, `FieldSkillRestrictionEvaluator.cs`, `FieldRuleRuntime.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `PlayerSkillBlockingStatusMapper.cs`, `PlayerCharacter.cs`, `DragonCompanionRuntime.cs`, `MassacreField.cs`, `CoconutField.cs`, `SnowBallField.cs`, `GuildBossField.cs`, `MapInfo`, `MapInfo.fieldLimit`, `MapInfo.moveLimit`, `MapInfo.fieldType`, `FieldLimitType`, `FieldType`, `Skill/MobSkill.img`, `Map/Map1/109080000.img/coconut`, `Map/Map1/109060000.img/info/fieldType`, `Map/Map9/990000900.img/{info/fieldType,healer,pulley}`, `Map/Map9/980031100.img/noSkill/class`, `Map/Map2/200100001.img/noSkill/class`, `Map/Map9/922240000.img/noSkill/skill`, `Map/Map9/910320100.img/mobMassacre/disableSkill`, `Map/Map9/926010100.img/mobMassacre/countEffect/500/skillUse`, `Map/Map2/200110000.img/info/noCancelSkill`, `Map/info/moveLimit`, `MapInfo.vanishDragon`, `FieldType.FIELDTYPE_NODRAGON`, `Skill/211.img/skill/2111002/info`, `Skill/230.img/skill/2301002/info`, `Skill/2212.img/skill/22121000/info`, `Skill/2215.img/skill/22151001/info`, `Skill/501.img`, `Skill/508.img`, `Skill/800.img`, `Skill/900.img`, `Skill/2002.img`, `Skill/3001.img`, `Skill/430.img`, `Skill/431.img`, `Skill/432.img`, `Skill/433.img`, `Skill/434.img`, `UnitTest_MapSimulator/PlayerSkillStateRestrictionParityTests.cs` (`CUserLocal::Update`, `CUserLocal::IsSkillAvailable`, `CField::IsUnableToUseSkill`, `CField_Coconut::BasicActionAttack`, `CField_SnowBall::BasicActionAttack`, `CField_GuildBoss::BasicActionAttack`, `CField::IsMoveSkillOnly`, `CField::IsUnableToUseRocketBoost`, `CField::IsUnableToUseTamingMob`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::TryDoingSwallowAbsorb`, `CUserLocal::TryDoingMagicAttack`, `CAvatar::IsOnPlayingOneTimeAction`, `CDragon`, `Field::SkillInfo::IsSkill`, `get_skill_class`, `get_job_level`, `get_skill_root_from_skill`) |

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Summoned action-loader / frame-build parity | The client-owned summon loader seam remains in `SkillLoader`, with the already-landed loader/frame-build parity still intact: summon frame building uses the client `120` ms delay fallback, per-frame `z`, `a0` / `a1`, authored `origin` / `lt` / `rb` metadata when wrapper-linked canvases publish it, reverse-playback duplication from the client-consumed reverse-style flag, standalone summon-owner root acceptance for real roots such as `Skill/3510.img/skill/35101005`, and wrapper pruning so root `icon*`, `action`, and root `effect/effect0` nodes do not pollute the playable door branch family. WZ was rechecked first in the mounted export for the same concrete layouts plus the mounted root-skill ownership surfaces that still feed this seam: `Skill/3510.img/skill/35101005` still exposes wrapper nodes beside `cDoor`, `sDoor`, `oDoor`, `mDoor`, and `eDoor`; `Skill/501.img/skill/5010005/summon` still exposes authored custom action branches `{piratebless,rushBoom,cannonSlam,noiseWave,jump,hit1}` beside `attack1` through `attack4`; `Skill/434.img/skill/4341006/summon` still remains an effect/repeat-only layout while both `info/requireSkill = 4331002` and root `req/4331002 = 30` remain visible on that same mounted skill node; `Skill/3512.img/skill/35121012` still likewise publishes both `info/requireSkill = 35121005` and root `req/35121005 = 1`; `Skill/3512.img/skill/35121006` still exposes no playable summon branch of its own while root `req/35111001 = 10`, `psdSkill = {35111001,35111009,35111010}`, and `info/requireSkill = 35121006` stay visible on that mounted root; `Skill/3510.img/skill/35100008` still likewise remains a non-summon root that only publishes `psdSkill = {35101010}` plus `info/dummyOf = 35101009&&35101010`; and those same mounted roots still expose deep leaf surfaces such as `Skill/3512.img/skill/35121006/info/requireSkill`, `Skill/3512.img/skill/35121006/psdSkill/35111009`, and `Skill/3510.img/skill/35100008/psdSkill/35101010` that a client-owned summon UOL could plausibly target instead of the root itself. Client evidence stayed second: `CActionMan::GetSummonedProp` at `0x41b830` still resolves the precomputed `SKILLENTRY::GetSummonedUOL` value through the resource manager, `SKILLENTRY::GetSummonedUOL` at `0x417f30` still only returns `this->sSummonedUOL`, `CActionMan::LoadSummonedAction` at `0x423100` still indexes the recovered `s_sSummonedAction` table before building and caching frames, `CSummoned::PrepareActionLayer` at `0x749cd0` still retries only action `2` for move abilities `4` and `5` or action `0` otherwise before throwing when that retry list is empty, and the same getter still rechecks as the asset base for `CSummoned::_LoadEffect` at `0x754050` plus `CSummoned::_UpdateEffect` at `0x754970` when the client resolves summon-owned `/effect`, `/repeat`, `/effect0`, and `/repeat0` layers from that precomputed UOL. This pass tightens the loader-side fallback again without widening runtime ownership: when `TryResolveClientSummonedUolProperty` resolves a deep client-UOL leaf under a mounted skill root, it now retries that owning root skill node as a summon source before handing off to the existing visible-link graph, so root-owned summon branches and standalone summon roots are no longer missed just because the client path landed on `info/*`, `req/*`, or `psdSkill/*` instead of on the root itself. Focused source coverage is now present in `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs` for deep client-UOL leaf paths under summon-child roots, standalone summon roots, mounted root-skill id parsing from those deep paths, and the non-skill-path empty fallback. The remaining client-owned gap is narrower and still concentrated on the hidden asset-resolution field itself: the simulator can now approximate more of the reachable visible graph from both the original skill surface and the resolved client-UOL target root, but the client still consumes the precomputed `SKILLENTRY::sSummonedUOL` string directly; the exact authoring and population path for that field still needs a follow-up client-data pass before this row can be marked implemented, especially for summons where neither the visible WZ-side graph nor the resolved client-UOL target root exposes the true summon owner at all. | Without this row, future summon work is biased toward packet and runtime fixes while leaving the client's actual frame-build owner implicit. Tracking the loader explicitly keeps summon parity anchored to the real asset-selection and frame-construction seam, and the stricter empty-action retry plus broader visible-link candidate graph now leave the remaining risk concentrated on the hidden summoned-UOL authoring path instead of on frame construction, action-table lookup, authored custom summon-action recovery, standalone-root pruning, or the previously narrower WZ-side source fallback chain. | summon action-loader layer (`SkillLoader`, `SkillData`, `SummonRuntimeRules`, `CActionMan::GetSummonedProp`, `CActionMan::LoadSummonedAction`, `SKILLENTRY::GetSummonedUOL`, `CSummoned::PrepareActionLayer`, `CSummoned::_LoadEffect`, `CSummoned::_UpdateEffect`, `CSummoned::Update`, `CSummonedPool::OnCreated`, `Skill/3510.img/skill/35101005`, `Skill/3510.img/skill/35100008/{psdSkill,info/dummyOf}`, `Skill/3511.img/skill/35111002/summon/attackTriangle`, `Skill/3511.img/skill/35111011/summon/stand`, `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3511.img/skill/35111009/info/affectedSkill`, `Skill/3511.img/skill/35111010/info/affectedSkill`, `Skill/3512.img/skill/35121006/{psdSkill,req,info/requireSkill}`, `Skill/3512.img/skill/35121012/{info/requireSkill,req/35121005}`, `Skill/3310.img/skill/33101005/repeat`, `Skill/434.img/skill/4341006/{summon/repeat,info/requireSkill,req/4331002}`, `Skill/3511.img/skill/35111001/summon/die1`, `Skill/501.img/skill/5010005/summon`, `Skill/1311.img/skill/13111004/summon/hit`, `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs`) |

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

This helper-owner seam is currently under-tracked by the backlog even though both the client and simulator already expose concrete ownership here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow-partner and mirror helper actors | The simulator still models the client ownership split instead of treating every clone-family helper as the same WZ-backed shadow-partner path. `SkillData` / `SkillManager` recognize `Mirror Image` (`4331002`) as a distinct helper owner, `PlayerCharacter` keeps a separate mirror runtime from the existing `special/*` shadow-partner actor, and the update/render loop still drives mirror helper visibility from player action changes with a dedicated `m_tStartMirror`-style start timestamp, a 200 ms client-shaped rel-move window, and client-backed suppression for ladder, rope, morph, taming-mob, hit, and dead states. WZ was rechecked first again for `skill/433.img/skill/4331002`: `action/0 = alert` remains present, `effect` remains present, and `special` remains absent, so Shadow Partner keeps using WZ-authored helper animations while Mirror Image continues to source the assembled avatar instead. IDA for `CUser::GetMirrorSrcLayer` still confirms the client mirror sources are the same five avatar layer handles in index order, while `CUser::PrepareMirrorActionLayer` still recreates each helper layer with `m_pLayerUnderFace` as the overlay parent regardless of which source plane fed that mirror layer, copies origin/position from the live source-layer handle, seeds `RelMove` from the live source-layer current time plus 200 ms, only recreates the helper layer when the layer is missing or its flip diverges from `m_pLayerUnderFace`, and releases `m_apLayerMirror` outright when the client suppression path wins. `CUser::Update` then keeps the existing helper layer and inserts the current source-layer canvas into it instead of rebuilding the helper object per source change. `PlayerCharacter` still reflects the implemented simulator-side seam by routing every prepared mirror plane through a shared UnderFace overlay target, drawing the whole mirror helper package at the UnderFace insertion boundary, keeping mirror rendering on the prepared per-plane composed-snapshot path even when the current live source plane still matches, preserving the prepared layer recreate timestamp across same-action/same-facing source canvas refreshes, recording the source-layer current-time snapshot separately from the recreate timestamp, reusing the prepared render target whenever a refreshed same-facing snapshot still fits inside the existing texture, tracking the logical snapshot source rectangle separately from the reusable texture extent, preserving the prepared placement anchor across same-facing canvas refreshes even when the incoming canvas bounds grow and the simulator has to replace the backing render target, preserving same-facing empty-source gaps while the helper remains visible, and clearing the prepared mirror planes again when client-style suppression hides the helper instead of carrying those cached planes through hidden states. The existing gates remain in place: flip-only changes still force prepared-layer recreation, mirror suppression/back-offset selection still consumes the client raw-action buckets (`is_back_action`, `is_alert_back_action`, raw action `48`) before falling back to action-name heuristics, the mirror-wide `m_tStartMirror` analogue still only re-arms when the helper becomes visible or the observed player action actually changes, and per-plane render placement still stays on the prepared snapshot anchor until the simulator recreates that prepared mirror plane instead of silently rebasing each helper plane to the current live avatar bounds every draw or every same-facing canvas refresh. This pass checks in focused regression coverage at `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` for the implemented seam: same-facing empty-source persistence, hidden-helper prepared-layer clearing, layer transition start precedence, prepared source-layer timing preservation, prepared snapshot source-rect fallback, prepared placement-anchor preservation, the client recovered scale-filter buckets, and the shared UnderFace overlay seam. The remaining gap is narrower and still concentrated at the true live-layer-object boundary inside `CUser::PrepareMirrorActionLayer` / `CUser::Update`: the simulator still does not own shared `IWzGr2DLayer` mirror objects, true COM live-layer origin/current-time handles, true `InsertCanvas` reparenting against the live client layer graph, dynamic filter changes driven by a real client avatar scale owner, or the follow-up clone-helper behaviors that depend on those real client layer objects rather than simulator-owned prepared plane snapshots. | Clone-family jobs are not just tray icons or generic buff effects in the client; they own dedicated avatar-adjacent helper layers that participate in action preparation and per-frame user update. Tracking that seam explicitly keeps future parity work focused on helper-layer timing, ownership, and clone-family differentiation instead of accidentally treating shadow-partner and mirror-image visuals as solved just because the simulator already renders one WZ-backed helper actor | `PlayerCharacter.cs`, `CharacterAssembler.cs`, `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` (`skill/433.img/skill/4331002/action/0`, `CUser::PrepareActionLayer`, `CUser::PrepareShadowPartnerActionLayer`, `CUser::PrepareMirrorActionLayer`, `CUser::GetMirrorSrcLayer`, `CUser::Update`, `is_back_action`, `is_alert_back_action`) |

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow Partner action-loader / frame-build parity | The shadow-partner helper row remains a loader-owned parity seam rather than only a runtime/playback note. WZ was rechecked first again in the mounted export: `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, and `Skill/1411.img/skill/14111000/special/*` still publish the same helper branch families, `Skill/421.img/skill/4211008/special/shoot2/{0,1,2,3,4}` still shows the authored mixed direct-plus-linked frame order that the loader must preserve, the recovered non-ghost `action/0` raw-action names still resolve from mounted skill data (`alert2` / `alert3` / `alert4` / `alert5`, `swingO1`, `avenger`, `assaulter`, `prone2`, `savage`, `showdown`, `assassination`, `smokeshell`, `ninjastorm`, `vampire`), and the mounted `Character/00002000` export still rechecks as a usable loader source for hidden action rows such as `alert2`, `ladder2`, `rope2`, `avenger`, `assaulter`, `prone2`, `savage`, `showdown`, `assassinationS`, `smokeshell`, `ninjastorm`, `flyingAssaulter`, `stabD1`, `swingD1`, `swingD2`, `doubleSwing`, `tripleSwing`, and `shotC1`. Those mounted action rows do not just carry `action`, `frame`, and signed `delay`: some now rechecked rows also publish piece-owned `flip`, `move`, and `rotate` metadata, for example `Character/00002000/doubleSwing/*`, `Character/00002000/tripleSwing/*`, `Character/00002000/flyingAssaulter/*`, and `Character/00002000/shotC1/*`, and `Character/00002000/tripleSwing/1/rotate = 90` still confirms that the loader-owned transform surface is not just a flip-only seam. Client evidence stayed second: the direct `CActionMan::LoadShadowPartnerAction` re-check at `0x423cf0` still shows cache lookup by `(skillId, action)`, action-table-owned named raw-branch precedence before plain numeric `get_action_name_from_code` fallback, hidden action-table piecing before plain raw-action fallback when `ACTIONDATA::bPieced` is populated, frame construction from the enumerated helper canvases, frame-owned canvas-property metadata reads, and mirrored interior-frame replay when the action-data replay flag is set, while `CActionMan::GetShadowPartnerProp` at `0x41ba60` still resolves the helper property through `SKILLENTRY::GetSpecialUOL` plus the resource-manager fetch and `CActionMan::Init` at `0x41beb0` still populates the character action table from `Character/00002000`. The simulator still keeps that ownership in the existing loader seam without widening runtime state: `ShadowPartnerClientActionResolver` still preserves animation-level `PositionCode` metadata when it builds loader-owned helper playback rows, `SkillLoader` still preserves the same frame-build metadata when it synthesizes helper rows, `SkillLoader.EnumerateShadowPartnerActionFrameChildrenInClientOrder` still keeps authored helper-canvas child order while accepting linked-canvas cases after the client-shaped `IWzCanvas` filter, and the loader still builds named-vs-numeric storage plans so numeric helper keys store the client action name while authored named branches win. This run narrows the loader-owned gap again at the mounted-row transform seam itself: the existing mounted-row piece parser and pieced-animation builder still carry the mounted `flip`, `move`, and `rotate` fields onto cloned helper frames, and `ShadowPartnerClientActionResolver.TryResolveFrameDrawTransform` now keeps that loader-owned rotation on the visible helper path so rows such as `tripleSwing` no longer drop their `rotate = 90` transform at draw time in either `PlayerCharacter` or `RemoteUserActorPool`. Focused regression coverage in `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs` now covers the mounted-row parser seam, signed delay retention, and the new rotated plus flipped frame-draw transform math in addition to the earlier exact source-frame-slot coverage. The earlier implemented loader behavior remains in place for linked-`special` source resolution, metadata-owned frame bounds, replay-tail handling for the currently confirmed replay-table actions `stand1`, `stand2`, and `alert`, mounted `Character/00002000` source-frame-slot synthesis for the currently recovered hidden rows, and fallback synthesized helper rows when a mounted client action row is still unavailable while preserving authored branches with the same name. Remaining gap is narrower but still real, and is now concentrated at any hidden Shadow Partner rows that still do not surface through the mounted `action/0` plus `Character/00002000` subset, any mounted action-table fields beyond the now-modeled `action` / `frame` / signed `delay` / `flip` / `move` / `rotate` plus the already-modeled replay flag, whether later client versions or other data sets introduce helper UOLs that do not land on this build's linked-`special` seam, and whether later indexed alert-family or other hidden helper rows ever become loader-owned in some other client build rather than staying outside this mounted surface. The latest direct loader re-check still narrows one prior uncertainty: this build's `LoadShadowPartnerAction` does not show a separate total-delay shaping path for Shadow Partner helper rows; replay-tail ownership remains tied to the action-data replay flag already represented by the confirmed replay-tail set. | Without this row, the backlog still overstates clone-helper parity by treating Shadow Partner as if it were only a `CUser`/`PlayerCharacter` timing and placement problem. The client keeps a dedicated `CActionMan` loader for helper actions, so parity still depends on a named seam for cache keys, branch lookup, numeric-slot fallback ownership, hidden action-table piecing, frame construction, metadata retention, per-piece transform retention where mounted rows publish it, and replay-tail ownership rather than only on helper runtime playback. | shadow-partner action-loader layer (`CActionMan::GetShadowPartnerProp`, `CActionMan::LoadShadowPartnerAction`, `CActionMan::Init`, `get_action_name_from_code`, `ACTIONDATA::bPieced`, simulator seams: `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `RemoteUserActorPool.cs`, `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs`, `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, `Skill/1411.img/skill/14111000/special/*`, `Skill/421.img/skill/4211008/special/shoot2/{0,1,2,3,4}`, `Skill/411.img/skill/{4111001,4111003,4111005,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121003,4121004,4121008}/action/0`, `Skill/420.img/skill/{4201002,4201003,4201005}/action/0`, `Skill/421.img/skill/{4211002,4211003,4211005,4211006,4211007}/action/0`, `Skill/422.img/skill/{4221001,4221003,4221006,4221007}/action/0`, `Skill/1410.img/skill/14101006/action/0`, `Skill/1411.img/skill/{14111001,14111002}/action/0`, `Character/00002000/{alert2,alert3,alert4,alert5,ladder2,rope2,avenger,assaulter,prone2,savage,showdown,assassination,assassinationS,assassinations,smokeshell,ninjastorm,vampire,stabD1,swingD1,swingD2,doubleSwing,tripleSwing,shotC1,flyingAssaulter}`) |

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Time Bomb, Vengeance, and ExJablin packet-application parity | The simulator now owns this `CUserLocal::OnPacket` family inside the existing local-utility packet seam without moving ownership elsewhere. WZ was rechecked first against the mounted export currently exposed through wzimg at `E:\MapleStory\WzExport\v95_2`: `String/Skill.img` still resolves `4341003` as `Monster Bomb` with the `explosion occurs 3 seconds after the charm is activated` description fragment, `31101003` as `Vengeance`, and `4120010` as `Expert Throwing Star Handling` with the `the next attack will always be a Critical Attack` fragment, while `3120010` still resolves to `Ultimate Inferno` instead of a WZ-side `Vengeance` alias. `Skill/434.img/skill/4341003` still publishes the authored Time Bomb `action`, `prepare`, and `special` branches together on the same skill node, and the mounted export still exposes only that one authored `4341003` `Monster Bomb` node for this family. `Item/ItemOption.img` still exposes the wider Time Bomb-shaped option-type-`52` family `020366`, `030366`, `040366`, `030371`, and `040371` with the matching invincibility templates, while other option-type-`52` rows such as `020396` and `040111` still remain unrelated non-invincibility options. Client evidence was rechecked second: `CUserLocal::OnTimeBombAttack` still decodes `(skillId, timeBombX, timeBombY, impactPercent, damage)`, passes the decoded `skillId` directly into `CSkillInfo::GetSkillLevel` and `TryDoingMeleeAttack` without an opcode-to-`4341003` fallback, restores the `1500 ms` base hit period before coat-option merges, gates impact on nonzero `nUserImpactDeg`, calls `CUser::MakeIncDecHPEffect(this, -nDamage, 0)`, and then sets the hit emotion/impact; `CUserLocal::ApplyInvicibleOption` still only mutates runtime invincibility state for `020366`, `030366`, and `030371` even though WZ also ships `040366` and `040371`; `CUserLocal::OnVengeanceSkillApply` still decodes exactly one `Int32` and only branches on legacy packet skill id `3120010`; and `CUserLocal::OnExJablinApply` still reads no payload and only arms `m_bNextShootExJablin = 1`. The landed seam still keeps packet ids `268` / `271` / `272` plus aliases, exact Time Bomb/Vengeance/ExJablin payload validation, signed Time Bomb damage, legacy-only Vengeance admission, nonzero Time Bomb impact gating, packet-origin target selection, the dedicated `SkillManager.TryApplyPacketOwnedTimeBombAttack` branch, the WZ-backed visible-potential item-option merge for `CharacterPart.ItemOptionIds`, and the narrow `MakeIncDecHPEffect` miss/heal mirroring. This pass now also splits visible WZ-family inference from client-owned runtime application inside the same helper path: visible potential lines can still infer the broader authored Time Bomb option family onto `CharacterPart.ItemOptionIds`, but only the client-confirmed `020366`, `030366`, and `030371` ids now feed the local Time Bomb invincibility-duration merge that mirrors `ApplyInvicibleOption`. This pass removes the last inferred Time Bomb alias bridge: an unrecognized first `Int32` in opcode-`268` payload now remains that decoded packet skill id, while known WZ/client aliases still resolve through the existing packet-owned skill-id catalog. Focused regression coverage in `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs` covers exact payload shapes, signed Time Bomb damage, nonzero impact gating, direct unknown Time Bomb skill-id retention, legacy-only Vengeance admission, empty ExJablin admission, the client-applied invincibility-id gate, and the retained broader WZ visible-family filter/order behavior that this row depends on. | These handlers matter because they bypass the normal local input/cast path even though the client gives them their own dispatcher and follow-up state. The simulator keeps a concrete owner for those packets, keeps the shared alias seam anchored to WZ first and client second, no longer accepts trailing bytes that the client packet readers would leave unread, no longer accepts non-client Vengeance packet ids, no longer treats only positive Time Bomb impact values as hit reactions, no longer clamps away signed Time Bomb damage payloads before the local reaction branch, no longer drops the zero/negative `MakeIncDecHPEffect` cases that hang off the same decoded damage field, still resolves visible potential lines through the WZ-authored `ItemOption.img` template/level seam instead of limiting `CharacterPart.ItemOptionIds` population to the confirmed Time Bomb invincibility family, and now also no longer over-applies the WZ-only `040366` / `040371` rows to the live Time Bomb hit-period merge when the client `ApplyInvicibleOption` branch would ignore them. The row is implemented because the remaining unconfirmed opcode-`268` alias bridge has been removed rather than preserved as simulator behavior; unknown decoded Time Bomb skill ids now stay unknown just as the direct client handler indicates. | packet-owned local skill-application layer (`CUserLocal::OnPacket`, `CUserLocal::OnTimeBombAttack`, `CUserLocal::OnVengeanceSkillApply`, `CUserLocal::OnExJablinApply`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::ApplyInvicibleOption`, `CUser::MakeIncDecHPEffect`, `CUserLocal::SetImpact`, `MapSimulator.PacketOwnedUtilityParity`, `LocalUtilityPacketInboxManager`, `SkillManager`, `PlayerCharacter`, `CharacterPart`, `ItemUpgradeUI`, `Character/00002000.img`, `String/Skill.img`, `Skill/434.img/skill/4341003`, `Item/ItemOption.img`, `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs`) |

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Combo counter / combo-command packet parity | `MapSimulator.PacketOwnedComboParity` now owns the simulator-side `OnIncComboResponse` bridge instead of leaving combo implied by the generic temporary-stat tray. The simulator decodes the packet-owned combo count from the combo inbox/chat transport, stores the owner count/timestamp, resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them on the same five-second alpha window, applies the client-matching fly-origin amplitude split across the least-significant digit run, and conditionally spawns the Aran combo-command attack/buff overlays with `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` first and WZ-backed named fallbacks for exports that only ship `ComboSmash`, `ComboDrain`, `ComboFenrir`, `ComboTempest`, and `ComboBarrier`. The row no longer depends on the generic `Combo` temporary-stat heuristics in `SkillManager`; it has a dedicated packet-owned HUD/effect owner, content load path, update lifetime, and draw path in `HaCreator/MapSimulator`. | If combo stays implied only by the temporary-stat tray, the backlog will overstate skill parity while missing the client's actual packet-owned combo counter, visible combo HUD/effect lifetime, and the extra combo-command visuals that hang off learned Aran skills. Tracking it explicitly keeps future work anchored to the real packet and draw owners instead of assuming the generic buff/cooldown surfaces already cover combo gameplay feedback. | packet-owned combo HUD/effect layer (`CUserLocal::OnPacket`, `CUserLocal::OnIncComboResponse`, `CUserLocal::DrawCombo`, `Effect/BasicEff.img/Combo`, `Effect/BasicEff.img/ComboCommand`) |

### 7. Shared skill-use animation-owner parity discovered by targeted `CAnimationDisplayer` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shared skill-use animation-owner parity | `MapSimulator.HandlePlayerSkillCast` still no longer stands alone as the only local ingress for this shared owner. The same animation-displayer seam continues to absorb the client-facing prepared-skill route exposed around `CUser::ShowSkillPrepare`: local prepared starts synthesize shared-owner requests from `prepare` or immediate `keydown` admission, local prepared release feeds `keydownend` back through the same branch loader, remote prepared-start packets honor their explicit branch names instead of falling back to generic `effect*` discovery, and remote prepared-clear packets emit the matching `keydownend` owner effect before the prepared state is dropped. The seam still tracks a live owner position/facing relationship, applies the client `1000 * (speed + 10) / 16` delay-rate override for the remote melee presentation route, preserves structured `LoadLayer`-style branches as avatar-bound transient animations, and carries caller-owned origin offsets plus fixed-vs-owner-followed facing for the one-time fallback and transient avatar branch loaders. This pass narrows another local caller gap without adding a parallel branch path. WZ was rechecked first: `Skill/434.img/skill/4341003/special` and `Skill/434.img/skill/4341007/special` still exist in the mounted export, the export still authors `specialAffected` under `Skill/230.img/skill/2301002`, `Skill/532.img/skill/5320007`, and `Skill/572.img/skill/5720005`, `Skill/122.img/skill/1220006` still authors both `special` and `info/condition = damaged`, and `Skill/3212.img/skill/32121003/special` is still present for Cyclone's sustained attack lane. That last recheck matters because `SkillManager.UpdateCyclone` is one of the direct local melee upkeep seams that can already identify the authored skill id without going back through `HandlePlayerSkillCast`. Client evidence stayed second: `CUser::ShowSkillSpecialEffect` still creates either a grenade-position vector or an explicit `(nTimeBombX,nTimeBombY)` vector, passes `m_pLayerUnderFace` as overlay, uses the caller delay rate, keeps `z = 3`, uses `nLast = 0x7fffffff`, and leaves auto-flip off before calling `CAnimationDisplayer::Effect_SkillUse`; the wider xref set into `CUser::ShowSkillEffect` / `CUser::ShowSkillSpecialEffect` still keeps explicit non-melee callers such as `TryDoingShootAttack`, `TryDoingMagicAttack`, `DoActiveSkill_Summon`, `DoActiveSkill_SmokeShell`, `DoActiveSkill_MobCapture`, `DoActiveSkill_Heal`, `DoActiveSkill_TownPortal`, `DoActiveSkill_OpenGate`, `DoActiveSkill_Flying`, `DoActiveSkill_BoundJump`, `DoActiveSkill_WeaponBooster`, `DoActiveSkill_Aura`, `TryRegisterTeleport`, `TryDoingSmoothingMovingShootAttackPrepare`, `OnKeyDownSkillEnd`, `HandleCtrlKeyDown`, `OnMovingShootAttackPrepare`, and `OnAttack`, and the same `ShowSkillSpecialEffect` xref set still includes direct melee owners such as `TryDoingMeleeAttack` plus local/remote damage handlers such as `CUserLocal::SetDamaged` and `CUserRemote::OnHit`. The simulator now mirrors more of that ownership split in the current seam: generic local client-effect callbacks still feed the shared `SkillUseEffectRequest` surface, the packet-owned local Time Bomb route still uses that recovered request seam, the local attack-payload owner still reuses it whenever runtime code already has an explicit caller-supplied origin or preferred target point for a skill whose WZ node authors `special`, the packet-owned local melee ingress now reuses that same explicit-`special` request builder before its direct `ProcessMeleeAttack` path, the teleport-linked affected-skill body-attack helper still reuses the same explicit-`special` request builder instead of bypassing the shared owner when it fires outside `ExecuteAttackPayload`, Cyclone's sustained local upkeep attacks now also feed their authored `special` branch back through the shared owner instead of staying on a standalone direct-melee lane, remote grenade packets still feed the same presentation owner through `RemoteUserActorPool.TryApplyThrowGrenade`, and local damage callbacks now also raise the shared-owner `special` request for any active buff whose WZ skill actually authors `info/condition = damaged` plus a `special` branch instead of limiting that seam to stance-only temporary-stat heuristics. This pass keeps the same seam for the authored `specialAffected` family as well: `SkillLoader` loads `specialAffected`, local heal admission raises a shared-owner request for the authored branch when that surface exists, and local buff admission requests `affected`, `affected0`, and `specialAffected` together through the shared animation-displayer branch loader instead of silently dropping the one-time authored branch. Existing timeout/recovery client-effect requests still feed their requested skill id back through the same branch loader, branches with authored layer roots still use `PlayerCharacter.ApplyTransientSkillAvatarEffect` for the local owner and matching transient state on `RemoteUserActorPool` for remote owners, and simple non-layered branches still fall back to one-time DX registration with the requested owner relationship. Focused regression coverage now lives in `UnitTest_MapSimulator/SharedSkillUseAnimationOwnerParityTests.cs` for explicit-origin request shaping on the shared `special` seam, authored `specialAffected` request shaping, damaged-condition candidate gating, and the client-style `prop` roll threshold used by the local damage-reactive branch admission. The remaining gap is narrower and still real: the simulator still does not route every explicit non-melee `CUser` caller from the client xref set into this surface yet, especially local `ShowSkillEffect` / `ShowSkillSpecialEffect` callers that do not currently expose their authored branch choice or explicit vector through the local runtime, it still does not mirror the full overlay-parent chaining and auto-flip ownership that client `LoadLayer` / `RegisterOneTimeAnimation` can apply for those callers beyond the current fixed-origin / owner-follow / facing-follow options, and the remote damaged-condition path recovered through `CUserRemote::OnHit` is still only partially mirrored because remote hit handling does not yet resolve the full client-owned damaged-trigger skill selection and chance gate through this same owner. | Naming and partially implementing this owner keeps cast-effect parity from being overstated once gameplay logic works: the client still composes many visible cast and reactive layers through one shared animation manager, and this pass widens the simulator seam from cast-only, remote-melee-only, prepared start/end, buff-start, generic client-effect coverage, local attack-payload origin overrides, and teleport-linked affected-skill helpers into a shared local/remote request surface that now also covers packet-owned local melee entry, Cyclone upkeep, and WZ-authored damaged-condition `special` reactions on the local owner instead of leaving those callbacks stuck behind standalone direct-attack paths or stance-only heuristics. The row stays partial because the simulator still lacks several recovered caller sites, still approximates some `LoadLayer` / `RegisterOneTimeAnimation` ownership knobs, and still has a remaining remote damage-reactive gap, but the risk is now more concentrated on the unrecovered non-melee caller routing and deeper overlay/auto-flip selection than on the absence of a shared owner path for the local direct-melee / local damage-triggered authored `special` branches currently proven in WZ and IDA. | shared skill-use animation layer (`CAnimationDisplayer::Effect_SkillUse`, `CAnimationDisplayer::LoadLayer`, `CAnimationDisplayer::RegisterOneTimeAnimation`, `CUser::ShowSkillEffect`, `CUser::ShowSkillSpecialEffect`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::SetDamaged`, `CUserRemote::OnHit`, `SkillUseEffectRequest`, `MapSimulator.AnimationDisplayerParity`, `RemoteUserActorPool.TryApplyThrowGrenade`, `Skill/434.img/skill/4341003/special`, `Skill/434.img/skill/4341007/special`, `Skill/122.img/skill/1220006/special`, `Skill/230.img/skill/2301002/specialAffected`, `Skill/3212.img/skill/32121003/special`, `Skill/532.img/skill/5320007/specialAffected`, `Skill/572.img/skill/5720005/specialAffected`, `UnitTest_MapSimulator/SharedSkillUseAnimationOwnerParityTests.cs`) |

### 8. Bullet animation-owner parity discovered by follow-up `CAnimationDisplayer` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Normal, magic, and afterimage projectile animation owner | WZ was rechecked first on the authored projectile surfaces this row owns: `Skill/200.img/skill/2001004/ball` and `Skill/230.img/skill/2301005/ball` still publish explicit spell-ball frame sequences with authored `origin` and `delay`, `Skill/434.img/skill/4341003/flipBall` still exposes the flipped projectile branch, and representative normal/cash ammo nodes still publish separate bullet art on `02060000/bullet` and `05021000/bullet`. Client evidence stayed second: `CAnimationDisplayer::RegisterBulletAnimation` at `0x455410` still allocates `NormalBullet` with `tStart`, `tEnd`, source/destination points, target vector, `z`, bullet-effect UOL, weapon id, and bullet item id before inserting it into `m_Bullets`; `CAnimationDisplayer::RegisterMagicBulletAnimation` at `0x455570` still performs the parallel `MagicBullet` insert only when the ball UOL string is non-empty; and `CAfterImageBullet::Update` at `0x477aa0` still clones the current bullet layer into a child repeat layer, relocates origin and alpha vectors, copies the source canvas, and registers repeat animation state. The simulator still mirrors that owner split inside the existing projectile seam instead of widening gameplay ownership again: `SkillManager` keeps a first-class bullet-animation owner list separate from `_projectiles`, activates queued owners only after the fire-time ammo/origin refresh path runs, updates owner positions from the live projectile while gameplay is active, falls back to the registered source/destination timeline when the gameplay projectile drops out, and draws owner animations from that container before falling back to ownerless or exploding projectile sprites. This pass narrows the registration-side approximation again. Owner animation resolution still consumes the registered bullet-effect UOL directly through the shared skill animation loader, so `ball` and `flipBall` branches remain the presentation source for both magic bullets and normal-bullet fallback paths instead of being reduced to passive bookkeeping while the owner container still records the client-shaped weapon id and bullet item id. Ownerless fallback visuals now also preserve the queued fire-time use-versus-cash lane recorded at registration time instead of drifting back to whichever live ammo slot metadata happens to survive later, item bullet visuals still load through the same shared animation loader so representative normal/cash ammo surfaces keep authored animation-level metadata such as root `z`, and magic-bullet afterimage snapshots still preserve the current frame's interpolated alpha ramp while the parity helpers now explicitly lock the clockwise-bound shift shape the client repeat-layer path applies. Focused regression coverage in `UnitTest_MapSimulator/BulletAnimationOwnerParityTests.cs` now locks the queued lane-specific fallback visual id, the queued-use selection strip, the clockwise bounds helper, and the frame-alpha-aware afterimage snapshot behavior in addition to the earlier animation-level `z` fallback and skill-UOL-path parsing. Remaining gap: the simulator still does not reconstruct the client's deeper `CBullet` layer objects or their weapon-driven specialization rules exactly, so native normal-bullet variant selection from weapon item id plus the full `CAfterImageBullet::Update` child-layer origin/alpha/vector-object adjustments are still approximated rather than mirrored 1:1. | Projectile presentation is no longer overstated by gameplay parity alone: normal bullets, magic balls, queued follow-up ammo visuals, and magic afterimage trails now live on a dedicated container owner with their own lifetime instead of disappearing as soon as gameplay state advances, the registered effect-UOL path now participates directly in owner-side draw selection instead of being dropped after registration, and queued ownerless fallback visuals no longer silently switch lanes after registration. This row stays `Partial` because the remaining mismatch is now concentrated in deeper native `CBullet` specialization and repeat-layer transform details rather than in missing owner separation, missing queued-lane retention, missing item-bullet root metadata retention, ignored authored `ball`/`flipBall` UOLs, or completely flat afterimage-alpha snapshots. | projectile animation layer (`CAnimationDisplayer::RegisterBulletAnimation`, `CAnimationDisplayer::RegisterMagicBulletAnimation`, `CAfterImageBullet::Update`, `CBullet`, `CAnimationDisplayer::NormalBullet`, `CAnimationDisplayer::MagicBullet`, `SkillManager.SpawnProjectile`, `SkillManager.RefreshQueuedProjectileFireTimePresentation`, `SkillManager.DrawProjectiles`, `SkillManager.ResolveBulletAnimationOwnerKind`, `SkillManager.ResolveBulletAnimationInterpolatedPosition`, `SkillManager.ResolveRegisteredBulletPresentationVisualItemId`, `SkillLoader.LoadSkillAnimationByNormalizedPath`, `UnitTest_MapSimulator/BulletAnimationOwnerParityTests.cs`, `Skill/200.img/skill/2001004/ball`, `Skill/230.img/skill/2301005/ball`, `Skill/434.img/skill/4341003/flipBall`, `02060000/bullet`, `05021000/bullet`) |

### 9. Secondary skill animation-owner parity discovered by broad `CAnimationDisplayer` function scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Hook-chain, foothold, prepare, motion-blur, chain-lightning, and Tesla animation owners | A broad IDA scan after reviewing the full backlog set found several skill-facing `CAnimationDisplayer` owners that are not yet tracked by the simulator backlog as first-class work. Client evidence is concrete: `RegisterHookingChainAnimation` at `0x45cbd0` loads hook and chain canvases from the supplied image UOL, resolves the owner through `CUserPool`, seeds fixed user/target offsets and two-stage timing, and is xrefed by `CUser::OnEffect` and `CUserLocal::TryDoingMonsterMagnet`; `RegisterFootHoldAnimation` at `0x45a990` is xrefed from local and remote melee/shoot/magic attack paths, `CMob::LoadLayer`, and summoned attack paths, then loads a WZ property, reads a StringPool-backed duration key, builds timed layers over an area rect, and updates them independently of hit resolution; `RegisterMotionBlurAnimation` at `0x45ad00` is xrefed from `CUser::SetActiveEffectItem` and owns active-effect-item blur trail state; `Effect_SkillPrepare` at `0x45b840` calls `RegisterPrepareAnimation` at `0x45b420`, which replaces any existing prepared entry for the same character id in an animation-manager map/list pair; and `RegisterChainlightningAnimation` at `0x45d1b0` plus `RegisterTeslacoilAnimation` at `0x45d650` sit beside the already-modeled projectile and summon post-effect rows but remain deeper animation-manager owners with their own state/lifetime, with the Tesla branch xrefed from `CSummoned::ShowTeslaCoilTriangle`. Remaining gap: these owners are still not represented as dedicated simulator animation containers or request surfaces, and their WZ branch selection, owner-follow rules, timed layer lifetimes, and local-vs-remote ingress paths still need WZ-first verification before implementation. | Skill and summon gameplay can be correct while the visible client presentation is still wrong if these managers stay folded into generic projectiles or one-shot skill effects. Keeping the row explicit prevents Monster Magnet chains, ground/foothold attack strips, key-down prepare layers, active-effect blur, chain-lightning, and Tesla visuals from being treated as already covered by `Effect_SkillUse`, bullet animation, or summon runtime parity. | secondary skill animation layer (`CAnimationDisplayer::RegisterHookingChainAnimation`, `CAnimationDisplayer::RegisterChainlightningAnimation`, `CAnimationDisplayer::RegisterTeslacoilAnimation`, `CAnimationDisplayer::RegisterFootHoldAnimation`, `CAnimationDisplayer::RegisterMotionBlurAnimation`, `CAnimationDisplayer::Effect_SkillPrepare`, `CAnimationDisplayer::RegisterPrepareAnimation`, `CUserLocal::TryDoingMonsterMagnet`, `CUser::OnEffect`, `CUser::SetActiveEffectItem`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::TryDoingShootAttack`, `CUserLocal::TryDoingMagicAttack`, `CUserRemote::OnMeleeAttack`, `CUserRemote::OnShootAttack`, `CUserRemote::OnMagicAttack`, `CSummoned::OnAttack`, `CSummoned::ShowTeslaCoilTriangle`, `CMob::LoadLayer`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, movement-coupled shoot families, and the shared `Effect_SkillUse` plus secondary `CAnimationDisplayer` animation owners rather than extending the generic cast path indefinitely.
3. Summon actor/runtime pass:
   Close the missing `CActionMan::LoadSummonedAction` loader seam together with the packet-owned `CSummonedPool` gaps and the `CSummoned::Update`-owned movement, touch-damage, Tesla-state, and position-sync gaps instead of only extending attack metadata and per-skill summon heuristics.
4. Clone-helper parity pass:
   Tighten the dedicated `CUser::PrepareActionLayer` / `CUser::Update` helper-layer seam for shadow-partner and mirror-image families instead of leaving clone behavior folded into generic buff or summon work.
5. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
6. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
7. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
