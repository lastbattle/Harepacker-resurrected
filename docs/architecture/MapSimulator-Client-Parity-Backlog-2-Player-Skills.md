# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`
- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`
- `CAnimationDisplayer::Effect_SkillUse` at `0x459100`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Player skill execution parity

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
`CUserLocal::Update` at `0x937330` orchestrates several of these paths and confirms that key-down gauge updates, queued follow-up attacks, prepared-skill execution, repeat-skill handling, teleport, rush, flying-rush, rocket-booster start/end handling, movement-coupled shoot branches, timer-driven cancellations, and status-bar feedback are coordinated in one local-player update loop rather than isolated one-off handlers. A second IDA pass also showed that summon parity splits across three owners rather than only the actor/runtime shell: `CSummoned::Update` drives summon actor motion, active/passive vec-ctrl updates, body-contact damage checks, assist-type dispatch, Tesla Coil state handling, self-destruct timing, and final world-position sync; `CSummonedPool::OnPacket` dispatches created/removed/move/attack/skill/hit packets and `CSummonedPool::OnCreated` ties each summon to its owning user, skill id, level data, and local-user registration path; and `CActionMan::{GetSummonedProp,LoadSummonedAction}` own the WZ-side summon UOL lookup, per-action frame build, delay/z metadata, reverse-playback duplication, and cached action-entry reuse before the runtime ever updates or draws that actor.

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
The earlier backlog text tracked summon packet ownership and runtime behavior, but the targeted `CActionMan` scan shows another concrete client owner that was still missing from the document: summon action loading itself. `GetSummonedProp` resolves the summon UOL from `SKILLENTRY::GetSummonedUOL` through the resource manager, while `LoadSummonedAction` uses that property to resolve the action-specific node, enumerate each frame canvas, read per-frame `delay`, `z`, `origin`, and `rb` metadata, compute the frame bounds, and cache the assembled action entry under the summon action key. The same decompile also shows a distinct reverse-playback seam: when the source action node publishes the reverse flag, the client appends a reversed copy of the built frame list instead of leaving playback order entirely to the runtime. That means summon parity is not only `CSummoned::Update` timing and `CSummonedPool::OnPacket` ownership; it also depends on the loader that chooses the summon asset path, builds the frame table, and decides whether the runtime receives a forward-only or mirrored playback list.

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::Update` at `0x8fb8d0`

Notes:
The targeted re-check surfaced a client-owned helper seam that the backlog was not tracking explicitly. IDA decompilation shows `CUser::PrepareActionLayer` calling `CUser::PrepareShadowPartnerActionLayer` and `CUser::PrepareMirrorActionLayer` immediately after the main avatar action layer is prepared, which means clone-style helper visuals are owned by the user action-layer path rather than by the generic buff tray alone. The same `CUser::Update` re-check also exposes mirror-timing state (`m_tStartMirror`) alongside the broader avatar-update pipeline, which is enough evidence to keep shadow-partner and mirror helper behavior as a dedicated parity bucket instead of leaving it implicit inside generic skill or temporary-stat rows.

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::GetShadowPartnerProp` at `0x41ba60`
- `CActionMan::LoadShadowPartnerAction` at `0x423cf0`

Notes:
The current skill backlog already names the shadow-partner runtime/helper surface, but the targeted `CActionMan` pass shows there is still a distinct loader owner underneath it. `GetShadowPartnerProp` resolves the helper property from the skill id before action lookup, while `LoadShadowPartnerAction` caches by `(skillId, action)`, resolves either the named raw-action branch or the numeric fallback branch through `get_action_name_from_code`, enumerates the action canvases into cached frame entries, reads frame-owned metadata from the canvas properties, and appends the mirrored interior-frame tail when the loader's replay table marks that action. That means shadow-partner parity is split the same way as summons, pets, employees, dragons, mobs, NPCs, and taming-mob helpers elsewhere in the backlog set: `CUser::PrepareShadowPartnerActionLayer` and the simulator helper runtime own placement and playback state, while `CActionMan` owns the helper action catalog and the frame table those runtime paths consume.

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another client-owned skill family that was not represented anywhere in the backlog text. `OnTimeBombAttack` is not just a generic damage callback: the decompile shows it decoding a skill id plus attack/action data, resolving the local skill level, calling `TryDoingMeleeAttack`, then applying hit-period/invincibility and impact side effects before forcing the avatar hit reaction. `OnVengeanceSkillApply` is also more specific than a plain temporary-stat refresh because it gates on skill id `3120010` and then calls `DoActiveSkill_MeleeAttack` through the local skill runtime, while `OnExJablinApply` arms the `m_bNextShootExJablin` flag for the next ranged-shot branch rather than behaving like a generic buff bit. That is a dedicated packet-owned skill-application seam between the broad cast/update families and the existing temporary-stat or helper rows.

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`

Notes:
The same targeted `CUserLocal::OnPacket` scan surfaced another local skill owner that the backlog still was not naming. `OnIncComboResponse` is very small but concrete: it decodes the server-authored combo count, stores it in `m_nCombo`, stamps `m_tLastSetCombo`, and immediately routes through `DrawCombo`. The `DrawCombo` decompile then shows that combo is not just another temporary-stat label. The client resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them alive on a five-second alpha window, applies fly-origin motion to the combo vectors, and conditionally spawns extra `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` layers when the corresponding Aran combo-command skills are learned. A repo search across `HaCreator/MapSimulator` still only surfaced the generic `Combo` temporary-stat labeling in `SkillManager` and did not surface any simulator-side combo packet owner, combo HUD/effect loader, or `ComboCommand` runtime, so this remains a real missing parity bucket rather than stale wording.

### 7. Shared skill-use animation-owner parity discovered by targeted `CAnimationDisplayer` scan

- `CAnimationDisplayer::Effect_SkillUse` at `0x459100`

Notes:
The broader skill rows already cover cast logic, hit effects, prepared gauges, and helper actors, but the latest `CAnimationDisplayer` pass exposed another owner that the backlog corpus was not naming anywhere else. `Effect_SkillUse` is not a generic data lookup. The decompile shows it formatting a StringPool-backed effect UOL pattern for the requested effect name plus either the default or indexed variant, loading each resolved layer through `CAnimationDisplayer::LoadLayer`, applying the caller-supplied delay-rate override before playback, and then registering every resolved layer through `RegisterOneTimeAnimation` rather than leaving skill-use presentation on the local runtime or UI owners. The same body also keeps optional overlay-parent and auto-flip ownership at this animation-manager seam, which means visible skill-cast parity is split one level further than the current skill backlog suggests: `CUserLocal::*` owns cast admission and follow-up behavior, feature-local runtime code owns gameplay consequences, and `CAnimationDisplayer::Effect_SkillUse` owns the shared one-shot cast animation composition that many skills route through before those consequences are visible.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Player skill execution parity

The simulator has a generic skill runtime. It does not yet mirror the client's named `CUserLocal` behavior families.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Generic skill casting | The simulator now loads the full player skill catalog from `Skill.wz`, learns active skills into the runtime, supports direct cast invocation from the skill window in addition to hotkey casting, keeps GM/SuperGM skill-book resolution compatible with both `900.img` and `910.img` layouts while focusing those jobs on their own job-book window instead of the global advancement catalog, evaluates Big Bang `common` formulas in IMG-mode data sets so skills like `Power Strike` are treated as castable attacks instead of passive placeholders, mirrors skill effect anchoring/hitbox fallback by facing direction for asymmetric melee effects, sanitizes unsupported SpriteFont glyphs in the skill window so WZ-backed names no longer crash row rendering, and resolves cast SFX from `Sound/Skill.img` so skill use now plays the corresponding `Use` or fallback attack sound through the simulator audio path | This closes the previous "can only use the current job's limited skill book" runtime/UI gap and keeps special-job skill tabs populated and executable across client data variants | `SkillManager.cs`, `SkillLoader.cs`, `SkillUIBigBang.cs`, `MapSimulator.cs`, `UIWindowLoader.cs` |
| Implemented | Buff stat application | Buff logic now applies `PAD`, `PDD`, `MAD`, `MDD`, `ACC`, `EVA`, `Speed`, and `Jump` directly to the player build and cleanly removes them when buffs expire or refresh | Active buff skills now change simulator stats and movement-relevant values instead of only a partial subset | `SkillManager.ApplyBuffStats` |
| Partial | Buff icon / temporary-stat UI parity | Active buffs still drive the WZ-backed status-bar tray with live duration countdowns sourced from `UI/BuffIcon.img`, the tray still supports client-style right-click cancellation directly from the live buff list, `UILoader` still normalizes and loads the full `BuffIcon.img` icon surface, and the simulator still consumes the authored `name` strings plus `buff/*` traversal order from that same catalog so the nine observed stat-family icons (`incEVA`, `incPAD`, `incPDD`, `incMAD`, `incMDD`, `incACC`, `incCraft`, `incJump`, `incSpeed`) keep their WZ-authored display names and ordering instead of a duplicated simulator-only table. The broader non-`BuffIcon.img` family catalog still resolves through the recovered v95 `SecondaryStat` layout, and this run stayed inside `SkillManager.GetStatusBarBuffEntries` again without changing tray-lane ownership. WZ was rechecked directly in the mounted export for the mixed and independent-property families still relevant here: `UI/BuffIcon.img/buff` still exposes only the same nine ordered authored icon families, while `Skill/3112.img/skill/31121001/common` still publishes `cr`, `ignoreMobpdpR`, then `bdR`, `Skill/572.img/skill/5720008/common` still publishes `criticaldamageMin`, `criticaldamageMax`, `cr`, and `bdR`, `Skill/3511.img/skill/35111013/common` still publishes `pddR`, `mhpR`, `mmpR`, `cr`, `damR`, and `expR`, `Skill/422.img/skill/4220009/common` still publishes `mesoR`, `Skill/7000.img/skill/70000049/common` still publishes `dropR`, `Skill/7100.img/skill/71000321/common` still publishes `ignoreMobDamR`, `Skill/000.img/skill/0000110/common` still publishes `mhpR` and `mmpR`, `Skill/2112.img/skill/21120001/common` still publishes `criticaldamageMin`, `Skill/2311.img/skill/23111005/common` still publishes `asrR` and `terR`, and `Skill/530.img/skill/5301003/common` still publishes `indieSpeed`. That evidence still drives the authored-family override table plus the fallback-label bridge for later explicit owner properties such as `mhpR` / `mmpR`, `pddR` / `mddR` / `accR` / `evaR`, `asrR` / `terR`, `criticaldamageMin` / `criticaldamageMax`, `damR`, `dropR`, `mesoR`, `bdR`, `ignoreMobpdpR`, `ignoreMobDamR`, and authored `indie*` tooltip rows such as `indieSpeed` and `indieMhpR`, so those tooltip labels no longer collapse back to one generic family name just because an earlier family still owns tray ordering. This pass tightens icon ownership again inside the same seam instead of widening status-bar ownership: when the resolved tray owner has no direct `BuffIcon.img/buff/*` family icon, `SkillManager` now first reuses only authored `BuffIcon.img/buff/*` families that are actually present in the skill's WZ temporary-stat property order before falling back to the skill's own icon texture. That keeps mixed authored surfaces such as `Skill/3511.img/skill/35111013/common` on a real WZ-authored `incPDD` family icon, but it also stops simulator-only description or identity heuristics from stealing a direct family icon just because they happen to map to one of the nine WZ tray icons when the skill's authored `common` payload never published that family in the first place. Standalone non-`BuffIcon.img` families such as `mesoR`, `dropR`, `bdR`, `ignoreMobpdpR`, `ignoreMobDamR`, `asrR`, `terR`, and later `indie*` rows therefore still stay on the skill-authored icon texture when no authored direct family icon exists. Focused regression coverage is now on disk in `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` for mixed direct-family icon reuse (`pddR` + `expR`), standalone non-`BuffIcon.img` generic-lane retention (`mesoR`), and the new heuristic-only direct-icon rejection case where authored `bdR` data plus descriptive `Speed` text no longer hijacks the tray onto `incSpeed`. The remaining gap is narrower but still real: exact tray order is client-backed for the WZ-authored icon families plus the recovered v95 `SecondaryStat` slice, but some non-`BuffIcon.img` families still have no direct recovered v95 icon owner or still appear to depend on runtime-owned grouping beyond `UI/BuffIcon.img`, the recovered temp-stat surface, and currently parsed skill metadata, especially exact final lane/icon ownership for mixed families once no authored `BuffIcon.img/buff/*` family remains to anchor the icon choice and the client grouping is still not exposed by the available WZ plus recovered-client evidence. | Buff-heavy jobs are visible on the HUD, can be cleared without waiting for expiry, and no longer collapse as often into a fully generic tray entry while tooltip text, tray-family ownership, and icon fallback stay closer to the authored WZ and recovered client temp-stat surface. The known nine `BuffIcon.img/buff/*` families still use the authored WZ icon names and order directly, Big Bang-era payload families such as `mhpR` / `mmpR`, `pddR` / `mddR` / `accR` / `evaR`, `damR` / `indieDamR`, `criticaldamageMin` / `criticaldamageMax`, `expR` / `dropR` / `mesoR`, `bdR`, `asrR` / `terR`, both `ignoreMobpdpR` and `ignoreMobDamR`, and authored `indie*` rows such as `indieSpeed` and `indieMhpR` now stay on their resolved tray lanes without depending on generic tooltip text heuristics first. Mixed buffs also stop dropping to a skill-art or generic icon as soon as the tray owner lands on a non-`BuffIcon.img` family, but only when the skill's authored WZ property order still contains one of the nine direct family icons; heuristic-only direct-family matches no longer override the tray art by themselves. The remaining gap is now concentrated on exact client lane or icon ownership for the families that still are not directly observable from `UI/BuffIcon.img` plus the recovered v95 `SecondaryStat` surface alone once no direct authored family icon remains in the active mix. | `SkillManager.ActiveBuffs`, `SkillManager.ConfigureBuffIconCatalog`, `SkillManager.GetStatusBarBuffEntries`, `SkillLoader.CreateLevelData`, `SkillLoader.NormalizePassiveStatAliases`, `SkillDataLoader.TryResolveFallbackStatPresentation`, `RemoteAffectedAreaSupportResolver.cs`, `StatusBarUI.cs`, `UILoader.cs`, `MapSimulator.Content.cs`, `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` (`UI/BuffIcon.img`, `UI/BuffIcon.img/united/buff`, `UI/BuffIcon.img/united/anniversary`, `Skill/*/common/indiePad`, `Skill/*/common/indieMad`, `Skill/*/common/indieAcc`, `Skill/*/common/indieEva`, `Skill/*/common/indieSpeed`, `Skill/*/common/indieJump`, `Skill/*/common/indieMhp`, `Skill/*/common/indieMmp`, `Skill/*/common/indieAllStat`, `Skill/*/common/pddR`, `Skill/*/common/mddR`, `Skill/*/common/accR`, `Skill/*/common/evaR`, `Skill/*/common/asrR`, `Skill/*/common/terR`, `Skill/*/common/criticaldamageMin`, `Skill/*/common/criticaldamageMax`, `Skill/*/common/damR`, `Skill/*/common/indieDamR`, `Skill/*/common/indieMhpR`, `Skill/*/common/indieMmpR`, `Skill/*/common/expR`, `Skill/*/common/dropR`, `Skill/*/common/mesoR`, `Skill/*/common/bdR`, `Skill/*/common/ignoreMobpdpR`, `Skill/*/common/ignoreMobDamR`, `Skill/000.img/skill/0000110/common`, `Skill/2112.img/skill/21120001/common`, `Skill/2311.img/skill/23111005/common`, `Skill/3112.img/skill/31121001/common`, `Skill/3511.img/skill/35111013/common`, `Skill/422.img/skill/4220009/common`, `Skill/530.img/skill/5301003/common`, `Skill/572.img/skill/5720008/common`, `Skill/7000.img/skill/70000049/common`, `Skill/7100.img/skill/71000321/common`, `SecondaryStat`, `CUIStatusBar::Draw`, `CUserLocal::Update`) |
| Partial | Melee / ranged / magic resolution | The runtime now preserves WZ `lt/rb` attack bounds, resolves melee and magic targets with separate ordering rules, gives projectile skills family-specific collision ordering plus homing steering, keeps homing active even when WZ `ball` data also marks the projectile as exploding, detonates WZ `ball/explode` projectiles on impact so nearby mobs within `explodeRadius` are resolved under the same effective target cap, respects the effective projectile hit cap from both `level/mobCount` and `ball/mobCount`, routes WZ `info/type` through the skill-family classifier so no-ball spell and shoot families no longer collapse into melee resolution by default, now also treating client-confirmed shoot-family `info/type=52` skills such as Aran `21120006` as ranged even when WZ omits a `ball` node, routes WZ `info/type=32` plus `info/massSpell=1` spell families such as Mage `Slow` / `Seal` back onto the magic lane instead of leaving those no-ball area spells on melee defaults, and now also keeps the client-confirmed Outlaw no-ball shoot-family helpers `5211004` / `5211005` on the ranged lane despite their melee-looking WZ `info/type=1`, while the shared shoot-family resolver also carries `5211006` so the loader and runtime use one client-backed table instead of drifting apart. Direct no-projectile ranged skills still get their own fallback hitbox and target ordering instead of reusing the melee branch, route through the client-shaped `DoActiveSkill_ShootAttack` fallback split so melee-first families probe the close-range lane before falling back to ranged resolution while the client-confirmed direct-shoot families stay on the shoot lane, preserve the special client one-offs where Gunslinger `Double Fire` (`5001003`) and Outlaw `Triple Fire` (`5210000`) still attempt the melee lane before falling back to shoot despite otherwise living in the shoot-family helper table, and thread that same melee-first-vs-shoot preference through deferred moving-shoot preferred-target selection instead of always snapshotting a ranged-only target. Projectile families still route WZ `info/rectBasedOnTarget=1` through an additional target-anchored rectangle after first impact so client rect-attack families such as `3001004`, `3101005`, and `5201002` no longer depend on raw projectile overlap alone, re-resolve chained follow-up targets from the live impact/serial origin using the WZ `range` bounce radius plus nearest-neighbor chaining instead of only replaying the initial overlap snapshot, stage multi-shot projectile release from WZ `ballDelay` / `ballDelayN` timing instead of front-loading every projectile on cast start, assign WZ `info/multiTargeting=1` projectile volleys to distinct live targets inside the authored ranged hitbox before spawn so skills such as `2301005`, `3001004`, `5301000`, and `20021166` no longer fire every shot through the same generic spread-first path when valid separate targets already exist, derive projectile collision bounds from the live WZ ball frame size and origin while sweeping those bounds across the projectile's previous-to-current travel step so fast or large projectiles no longer rely on a single centered point-instant overlap check, thread per-target client damage override/decay branches through the live hit order so `Arrow Bomb: Bow` (`3101005`) uses its WZ `x` damage lane instead of the generic `damage` fallback, WZ-published x-based chain falloff still applies where the skill data actually publishes that decay value, and the remaining client-owned `SKILLENTRY::AdjustDamageDecRate` fixed tables now also drive `Energy Orb` (`5121002`), `Iron Arrow: Crossbow` (`3201005`), `Piercing` (`3221001`), `Ricochet` (`33101001`), and `Blade Fury` (`4341004`) through their exact client multipliers instead of flattening those families back onto one generic damage percent. The shoot-entry precheck and ammo lane stay inside the same client-backed seam too: ranged attack casts still validate the expected shooting-weapon family before firing, still skip that weapon gate for the confirmed client exceptions that do not use a shooting weapon at all (`5121002`, `11101004`, `15111006`, `15111007`, `21100004`, `21110004`, `21120006`, `33101007`, `4121003`, `4221003`), still reject true shooting-weapon ranged entries while the player is attached to a ladder or rope unless the mounted taming-mob owner is the Wild Hunter jaguar family mirrored by `Jaguar Rider` (`Skill/3300.img/skill/33001001/info/type = 13`), still parse WZ `level/common/bulletConsume` alongside `bulletCount`, still carry WZ `common/itemConsume` into the elemental Outlaw pellet-family filter for `5211004` / `5211005`, and still thread WZ `info/requireSkill` plus `req/*` support links through the loader/runtime gate so shoot-entry families such as Wind Breaker `13111006` / `13111007` stay behind their authored `13111005` support state without truncating future multi-link `req/*` sets to one entry. When an inventory runtime is present the simulator still resolves a concrete compatible USE slot with enough stack quantity instead of only checking aggregate ammo presence, still doubles the resolved per-cast ammo requirement while Shadow Partner is active to stay closer to the client-owned `GetProperBulletPosition` consumption lane, still treats the marked active projectile item as an exact item-id requirement before allowing another stack of that same item id to satisfy the cast, still preserves the exact requested USE ammo id in failed selection metadata when no usable stack of the marked item remains, still carries the client helper's `nWeaponItemID == 1472063 -> 2060xxx` arrow one-off into compatible-ammo matching, and still snapshots the resolved `pnItemID` / `pnCashItemID` ownership outputs per projectile so delayed `ballDelay` releases and missing-ball projectile visuals keep the cast's USE/CASH ownership instead of inheriting later casts. Focused source-side coverage for those already-landed behaviors is now actually on disk in `UnitTest_MapSimulator/PlayerSkillExecutionParityTests.cs` and `UnitTest_MapSimulator/ClientShootAmmoResolverParityTests.cs`, covering the client shoot-family one-offs (`5001003`, `5211004`), WZ `ballDelay` release-schedule layouts, the client-owned `AdjustDamageDecRate` and `Arrow Bomb: Bow` damage lanes, no-ball or queued projectile visual ownership, exact active USE-ammo failure preservation, same-item stack fallback, the `1472063 -> 2060xxx` arrow one-off, skill-specific pellet-family filtering, CASH ownership preservation, and queued slot refresh by item id rather than stale slot index. | Skills are closer to the client across both classifier and execution seams: no-ball mass-spell debuffs no longer inherit melee ordering, the missing Outlaw shoot-family helpers no longer collapse back onto the melee lane just because WZ publishes `info/type=1` without `ball`, no-projectile ranged skills now use the client-shaped melee-first vs direct-shoot split instead of one unconditional ranged branch, deferred moving-shoot targeting no longer snapshots only the ranged lane when the live client family would try a close-range strike first, and direct shoot casts now reject the wrong weapon family or missing ammo in the same high-level place the client gates `DoActiveSkill_ShootAttack` before attack resolution while also consuming a real compatible ammo stack rather than a simulator-wide count. The ladder/rope shoot-entry seam is closer too: true shooting-weapon ranged attacks now stop on ladder or rope unless the active mounted owner is the Wild Hunter jaguar family, matching the direct client branch that still allows the jaguar exception while skipping the generic shoot lane. The elemental Outlaw helpers now go one step further: WZ-authored `itemConsume = 2331000 / 2331001` is carried into the level-data seam and used to filter the resolved USE slot by the same fire-pellet (`2331xxx`) vs ice-pellet (`2332xxx`) family split that the client applies inside `GetProperBulletPosition`, so those skills no longer accept an arbitrary gun-bullet stack just because the weapon family matches. The shoot-entry gate is also closer to the client for required support-state families: WZ-authored `info/requireSkill` plus `req/*` links now flow through the loader seam, Wind Breaker `13111006` / `13111007` no longer cast unless `13111005` is actually active, and the runtime no longer truncates multi-link support-state checks to the first parsed `req/*` entry before entering the ranged lane. The shoot-ammo resolver is also closer to the client-owned selection path itself: a marked active bullet now means "find this exact projectile item id or fail the ammo branch" instead of "prefer this slot and then fall back to anything compatible", another stack of that same selected item id can still satisfy the cast because the client scans the whole USE inventory by item id, compatible CASH projectile cosmetics are now surfaced through the same family scan instead of being entirely ignored, failure paths keep the exact requested USE/CASH item ids instead of dropping that client-owned ownership metadata, the special client arrow one-off for weapon `1472063` no longer collapses onto the generic weapon-code table, delayed projectiles retain the exact resolved USE/CASH ownership snapshot that `GetProperBulletPosition` produced for the cast, and projectile visuals reuse that snapshotted ownership when the skill itself does not publish usable `ball` art. The remaining gap is now concentrated in the client-only `GetProperBulletPosition` presentation and precheck residue that still sits beyond the modeled ownership and family outputs. IDA recheck of `CUserLocal::GetProperBulletPosition` at `0x907310` still cleanly exposes ammo-count resolution, exact USE-item ownership, compatible CASH-item ownership, and the elemental/special-weapon family gates, but it does not directly surface the exact fire-time cosmetic bullet-origin math that the client derives elsewhere. The row therefore remains `Partial` for the last exact cosmetic bullet-position placement details, any remaining vehicle or one-time-action nuances that still live deeper inside the client precheck, and edge-case timing or packet-owned branch state that still sits outside WZ-authored geometry plus the now-covered ammo-selection outputs. | `SkillManager.ProcessMeleeAttack`, `ProcessMagicAttack`, `ProcessNoProjectileRangedAttack`, `SpawnProjectile`, `CheckProjectileCollisions`, `ResolveQueuedSerialTargets`, `ResolveDeferredPreferredTargetMobId`, `SkillManager.DrawProjectile`, `SkillManager.ResolveProjectileVisualItemId`, `SkillLoader.ParseSkillInfo`, `SkillLoader.CreateLevelData`, `ClientShootAttackFamilyResolver`, `ClientShootAmmoResolver`, `PlayerSkillStateRestrictionEvaluator.cs`, `UnitTest_MapSimulator/ClientShootAmmoResolverParityTests.cs`, `UnitTest_MapSimulator/PlayerSkillExecutionParityTests.cs` (`Skill/3300.img/skill/33001001/info/type`, `Skill/521.img/5211004/info/type`, `Skill/521.img/5211004/common/itemConsume`, `Skill/521.img/5211005/info/type`, `Skill/521.img/5211005/common/itemConsume`, `Skill/521.img/5211006/info/type`, `Skill/411.img/4111005/common/bulletConsume`, `Skill/412.img/4121006/common/bulletConsume`, `Skill/1311.img/skill/13111006/info/requireSkill`, `Skill/1311.img/skill/13111006/req/13111005`, `Skill/1311.img/skill/13111007/info/requireSkill`, `Skill/1311.img/skill/13111007/req/13111005`, `Character/*/*/bullet`, `Item/Consume/0206/02060000/bullet/0`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::DoActiveSkill_MagicAttack`, `CUserLocal::GetProperBulletPosition@0x907310`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `is_shoot_skill_not_switched_to_melee_attack@0x6edcf0`, `is_shoot_skill_not_using_shooting_weapon@0x6edbf0`, `is_shoot_skill_not_consuming_bullet@0x6eeaf0`, `is_correct_bullet_item@0x861360`, `is_correct_bullet_cashitem@0x6ebcf0`, `is_pellet_fire_item@0x861300`, `is_pellet_ice_item@0x861330`, `SKILLENTRY::AdjustDamageDecRate`) |
| Partial | `DoActiveSkill_*` family parity | Generic movement, summon, and prepare/charge families still execute through the existing `SkillManager` seams instead of falling back to one generic cast lane. Movement-family dispatch still separates teleport snaps, grounded rush, flying-rush glide, jump-rush launches, and bound-jump starts from the client-confirmed `TryDoingTeleport` / `TryDoingRush` / `TryDoingFlyingRush` / `DoActiveSkill_BoundJump` splits; Teleport Mastery riders authored as `info/affectedSkillEffect=bodyAttack&&stun` still stay off the direct-cast surface and fire as passive follow-up data after the owning teleport resolves; WZ-linked helper branches with `info/affectedSkill` plus `info/affectedSkillEffect` still no longer surface as standalone castable actives; Battle Mage aura recasts still cancel or replace the active aura instead of stacking; aura-dot riders keyed by `info/affectedSkillEffect=dot` plus `info/dotType=aura` still pulse once per second while the owning aura is active; WZ `info/mesoExplosion` skills still resolve pickup ownership against the live local character id before consuming nearby mesos inside the authored `lt/rb` box and then applying their attack payload; WZ `info/zoneType=invincible` skills still create a persistent local protection zone; admin-skill execution still enforces GM/SuperGM ownership; bound-jump gating still respects `info/avaliableInJumpingState=1`, client-owned grounded exceptions such as `11101005`, `33001002`, and `35101004`, Wild Hunter swim/fly rejection, and WZ `info/requireHighestJump=1`; moving-shoot startup still resolves from the strongest loaded movement-action branch; deferred moving-shoot still mirrors the client-owned queued-level gate and the tighter queue-time action-owner chooser described earlier; Rocket Booster still uses its dedicated start/upkeep/landing seams; and packet-owned summons still mirror more of the `CSummoned::Update` passive lane instead of freezing on passive vec-ctrl updates. WZ re-check in the mounted export for the helper-only support surface on this same seam still keeps the follow-up-only rows explicit: `Skill/211.img/skill/2111007/info/{affectedSkill=2101002,affectedSkillEffect=bodyAttack&&stun}`, `Skill/3211.img/skill/32111010/info/{affectedSkill=32001002,affectedSkillEffect=bodyAttack&&stun}`, `Skill/3212.img/skill/32120000/info/{affectedSkill=32001003,affectedSkillEffect=dot,dotType=aura}`, and `Skill/3211.img/skill/32110000/info/{type=50,affectedSkill=32101002,affectedSkillEffect=partyDamageSharing}` still publish owner-follow-up metadata only and no standalone direct-cast lane. This pass keeps that helper-only ownership split inside the existing passive seam instead of re-exposing those rows as actives, but tightens the local owner-follow-up branch another step further: affected-skill helper rows still resolve their runtime level from the learned helper level when it exists and otherwise fall back to the live owning linked-buff level, and the local projected-support follow-up lane now also threads the linked owner/support skill metadata through the same friendly-support qualification and synthetic support-buff builder already used by remote affected areas. That means helper-only friendly-support rows such as Battle Mage `32110000` no longer drop out after being hidden from the direct-cast surface; their helper-owned projected stats plus `affected` visual/effect data now survive on the owner-follow-up seam while the linked owner buff stays active, without inventing a separate active-cast owner. Focused regression coverage for the helper-surface seam is now actually on disk in `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` for representative Teleport Mastery helper-level precedence, aura-dot owner-buff fallback, and Battle Mage `partyDamageSharing` helper-support qualification, while the checked-in moving-shoot source-side coverage remains in `UnitTest_MapSimulator/MovingShootParityTests.cs` for the type-`3` live-action-family fallback, weapon-family fallback, unresolved-owner abort path, queue-time action-speed clamp plus the `4001334` base-speed offset, and the client exception set that ignores booster/frozen carry. | The remaining gap is now narrower and concentrated in the last client-owned movement, packet, and area choreography rather than the generic execution lanes. The simulator still simplifies the exact moving-shoot entry snapshot and request choreography inside `TryDoingSmoothingMovingShootAttack`, especially the still-unrecovered full client `m_nAttackActionType` table values and the exact per-row contents of the shared `get_random_shoot_attack_action(...)` table beyond the now-modeled live family split for shoot, one-handed stab, two-handed stab, one-handed swing, two-handed swing, and polearm swing plus the earlier `info/type == 3` appointed-action bypass. It also still simplifies the live temporary-stat inputs that feed `get_attack_speed_degree(...)` beyond the now-modeled clamp, `4001334` base offset, skill-specific modifier-bypass set, shared Booster delta, and the WZ-backed `rapidAttack => CountLimit 1` fallback, plus any additional `CAntiRepeat` initialization or bypass rules beyond the now-modeled position-threshold, level-match, rapid-attack count-limit, and `33121009` exception gates; the final highest-jump timing window beyond the current WZ-backed near-apex gate; any remaining teleport timing / snap choreography around the client-owned delayed `m_teleport` commit window beyond the now-modeled follow-up mastery trigger; any remaining Rocket Booster packet choreography beyond the now-modeled launch-marker, launch-speed formula, and end-loop timing; deeper packet-owned summon assist dispatch and per-family `OnSkill` / `OnAttack` ownership beyond the now-modeled body-contact / passive-movement / expiry cadence; Battle Mage aura restack semantics beyond the current owner-follow-up helper projection; and the remaining local-vs-party area-footprint choreography for helper-only support rows beyond the now-modeled local owner-follow-up buff projection and linked-owner support qualification. | `SkillManager`, `SkillLoader`, `PlayerCharacter`, `PlayerCombat`, `PlayerSkillStateRestrictionEvaluator`, `SummonedPool`, `PacketOwnedSummonUpdateRules`, `UnitTest_MapSimulator/MovingShootParityTests.cs`, `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` (`CUserLocal::DoActiveSkill_*`, `CUserLocal::DoActiveSkill_BoundJump`, `CUserLocal::TryDoingTeleport`, `CUserLocal::TryDoingRush`, `CUserLocal::TryDoingFlyingRush`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CSummoned::Update`, `CAntiRepeat::TryRepeat`, `get_random_shoot_attack_action`, `get_attack_speed_degree`, `Skill/*/info/type`, `Skill/*/info/affectedSkillEffect`, `Skill/*/info/avaliableInJumpingState`, `Skill/*/info/requireHighestJump`, `Skill/110.img/skill/1101004/common/x`, `Skill/510.img/skill/5101006/common/x`, `Skill/211.img/skill/2111007/info/{affectedSkill,affectedSkillEffect}`, `Skill/3211.img/skill/32110000/info/{type,affectedSkill,affectedSkillEffect}`, `Skill/3211.img/skill/32111010/info/{affectedSkill,affectedSkillEffect}`, `Skill/3212.img/skill/32120000/info/{affectedSkill,affectedSkillEffect,dotType}`, `Skill/3212.img/skill/32121003/info`, `Skill/3300.img/skill/33001000/info`, `Skill/3310.img/skill/33101001/info`, `Skill/3311.img/skill/33111001/info`, `Skill/3312.img/skill/33121005/info`, `Skill/3312.img/skill/33121009/info`, `Skill/3500.img/skill/35001001/info/rapidAttack`, `Skill/3510.img/skill/35101009/info/rapidAttack`, `Skill/*/info/mesoExplosion`, `Skill/*/info/selfDestructMinion`) |
| Partial | Shadow Partner render/action parity | The simulator still treats Shadow Partner as a dedicated helper render/action surface rather than a generic buff ornament, with WZ-loaded `special/*` action tables for `4111002`, `4211008`, and `14111000`, client-shaped helper resolution order (`LoadShadowPartnerAction` helper-row lookup before plain raw-action name), raw-action-aware alias fallback, delayed attack follow-up keyed from the helper first-frame delay, blocking `create*` plus attack completion hold semantics, client-lane displacement/interpolation (`+/-30`, `+50`, `200 ms`), per-frame `a0/a1` alpha interpolation, and under-face insertion-layer ownership. WZ was rechecked first in the mounted export for the exact authored helper surface this pass is allowed to lean on: `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, and `Skill/1411.img/skill/14111000/special/*` still publish the same generic helper families, while `Skill/411.img/skill/{4111001,4111003,4111005,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121003,4121004,4121008}/action/0`, `Skill/420.img/skill/{4201002,4201003,4201005}/action/0`, `Skill/421.img/skill/{4211002,4211003,4211005,4211006,4211007}/action/0`, `Skill/422.img/skill/{4221001,4221006,4221007}/action/0`, `Skill/1410.img/skill/14101006/action/0`, and `Skill/1411.img/skill/{14111001,14111002}/action/0` still prove the currently loaded exact rows (`alert2`, `alert3`, `alert4`, `alert5`, `swingO1`) plus the earlier alias-only rows (`avenger`, `assaulter`, `prone2`, `savage`, `showdown`, `assassination`, `smokeshell`, `ninjastorm`, `vampire`). The same export still leaves the `create*` ambiguity narrowed but unresolved because `4111002` `special/create2` vs `create3` and `4211008` `special/create2` vs `create4` remain byte-for-byte identical. Client evidence from `CActionMan::LoadShadowPartnerAction` still keeps the action-specific helper-row table ahead of plain raw-action-name lookup, so this run stayed inside the existing resolver and remote same-branch matcher to close another packet-owned helper-identity seam instead of widening ownership. `ShadowPartnerClientActionResolver` now suppresses generic `attack*` names as helper-identity candidates when a packet raw action already resolves to a more specific client action, and `RemoteUserActorPool` now routes same-branch comparison entirely through that shared raw-aware helper-identity surface instead of short-circuiting on generic action-name equality. Packet-owned same-branch matching therefore no longer collapses back to generic `attack*` overlap when the remote actor is on a generic packet action but the client raw action still resolves to one authored helper family, so hidden assaulter-family raws such as `get_action_name_from_code(182) = flyingAssaulter` stay on the authored `stabO*` lane and generic attack-family packet rows stay on the authored stab or shoot helper branch once raw code or weapon family disambiguates them. Packet-owned playback still keeps the earlier indexed helper remaps (`alert2` / `alert3` / `alert4`, `prone2`, `ladder2`, `rope2`) plus the shared local/remote `+/-30` side lane, `+50` back lane, and `200 ms` interpolation helpers, and focused regression coverage now lives in `UnitTest_MapSimulator/RemoteShadowPartnerParityTests.cs` for raw-backed generic attack identity suppression, same-branch mismatch across disjoint helper families, same-branch retention across matching raw-backed helper families, and the `flyingAssaulter -> assaulter` alias seam. Remaining gap is narrower but still open: unrecovered client-only `LoadShadowPartnerAction` non-ghost helper rows beyond the now-covered WZ exact rows plus the raw-backed generic-attack identity seam, additional frame-index rewrites beyond the currently evidenced `ladder2` / `rope2` / `prone2` / indexed `alert*` set, exact ordering against every other simulator-owned non-mirror under-face overlay that the client may still own as a literal action-layer canvas object, and any residual packet-only one-shot alpha or follow-up choreography that still depends on hidden client layer state rather than the helper animation catalog itself. | Shadow Partner still mirrors companion playback through a dedicated skill-owned action catalog with client-shaped helper ordering and raw-action fallback, including the earlier helper aliases plus the currently exported exact non-ghost helper rows (`alert2`, `alert3`, `alert4`, `alert5`, `swingO1`) from the active Shadow Partner job families. Helper-frame preservation still includes `prone2` in addition to `ladder2` / `rope2` / indexed `alert*`, the shared local+remote offset seam still uses the same client-shaped back-lane and interpolation helpers, and packet-owned playback keeps one packet-owned raw-action choice all the way through helper branch selection and playback-frame remap instead of resolving the helper branch from one raw action and then sampling remap frames from a stale movement raw action. The packet-owned same-branch matcher now consumes the same raw-aware helper-identity seam as real helper resolution, suppresses generic packet `attack*` names once a more specific raw action is known, and therefore keeps both hidden assaulter-family raws and generic attack-family packet rows on the authored helper branch when raw action or weapon family is the only disambiguating signal. Focused regression coverage is now on disk in `UnitTest_MapSimulator/RemoteShadowPartnerParityTests.cs` for raw-backed generic attack identity suppression, raw-backed same-branch fallback, same-branch mismatch across disjoint helper families, weapon-family disambiguation on generic attack rows, and the `flyingAssaulter` alias seam. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `ShadowPartnerClientActionResolver.cs`, `RemoteUserActorPool.cs`, `UnitTest_MapSimulator/RemoteShadowPartnerParityTests.cs`, `CUser::PrepareShadowPartnerActionLayer@0x8e1730`, `CActionMan::LoadShadowPartnerAction@0x423cf0`, `get_action_name_from_code`, `Skill/*/{4111002,4211008,14111000}/special/*`, `Skill/411.img/skill/{4111001,4111003,4111005,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121003,4121004,4121008}/action/0`, `Skill/420.img/skill/{4201002,4201003,4201005}/action/0`, `Skill/421.img/skill/{4211002,4211003,4211005,4211006,4211007}/action/0`, `Skill/422.img/skill/{4221001,4221006,4221007}/action/0`, `Skill/1410.img/skill/14101006/action/0`, `Skill/1411.img/skill/{14111001,14111002}/action/0` |
| Partial | Queued follow-up attack families | Attack skills that publish WZ `finalAttack` follow-up maps still queue a deferred extra strike after the opener resolves, gate that queued branch by weapon type on both queue and execute, reuse the learned follow-up skill's live proc rate, preserve queued facing plus preferred-target mob or position snapshots through direct follow-up resolution and any later deferred moving-shoot or projectile hop, and keep the client's separate single pending-slot overwrite rules for final attack, Spark, and serial follow-up families without clearing unrelated queued summon work. Thunder Breaker `Spark` (`15111006`) still runs as its own timed attack-triggered family from WZ `info/condition=attack` plus `info/chainAttack=1` and `info/chainattackPenalty=1`, snapshots the current weapon code at queue time, rechecks that same code again when the deferred branch fires, and still re-enters the shared shoot/projectile path with the saved trigger origin and target lane. WZ `info/chainAttack=1` plus `info/chainattackPenalty=1` serial families such as `2221006` and `5121002` still use their own single pending serial slot, queued final-attack, Spark, and serial execution still re-read the live learned skill level when the deferred branch actually fires, still route queued cast startup back through the normal cast-effect seam, and still carry the client-only ranged final-attack `TryDoingShootAttack(..., 65, ...)` search hint through queued entry plus any extra deferred moving-shoot hop. The queued shoot-entry seam remains tighter on both validation and presentation: queued final-attack and Spark casts still re-qualify their armed USE or CASH ammo selection against the live inventory right before queued fire time, still refresh slot indices while preserving the original item ids even when a queued no-consume buff snapshot is active, still cancel the queued ranged branch if the armed USE ammo disappeared or no longer satisfies the live consume count, and still consume those snapshotted USE or CASH item ids during queued projectile draw so delayed final-attack or Spark releases render from the matching WZ `Item/*/*/bullet` frames instead of collapsing back to generic skill projectile art. WZ still exposes the authored item-bullet visual seam directly (`Item/Consume/0206/02060000/bullet/0` for standard arrows and `Item/Cash/0502/05021000/bullet/{0,1,2}` for animated cash bullets), while client evidence from `CUserLocal::GetProperBulletPosition` still shows fire-time resolution returning both USE and CASH projectile ownership. The simulator therefore keeps queued projectile visuals on the queued cast's already-resolved weapon family during later `ballDelay` refresh, prefers the cash-bullet family only while that cash slot still resolves at fire time after the queued slot refresh, otherwise falls back to the preserved USE family, still mirrors the live `handMove` / `hand` / body-origin shoot point across the avatar pivot when delayed queued fire finally resolves under a saved facing that no longer matches the avatar's current direction, and now also preserves any already-armed owner-relative queued projectile launch offset when that late fire-time refresh re-anchors the shot onto the mirrored live shoot point instead of flattening the delayed projectile back onto the raw owner origin. WZ-authored `x` chain-penalty decay and the client-owned fixed multiplier tables for no-`x` families still both apply through the deferred or timed chain path as well, so queued follow-up families no longer flatten back onto one generic damage percent after the opener. Focused helper coverage is now on disk in `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` for fire-time queued weapon-family retention, late queued ammo revalidation, stale cash-lane visual fallback back to the preserved USE family, mirrored delayed-origin refresh, queued delayed-origin offset preservation, and queued bullet-family visual override. The remaining gap is now concentrated in the last client-only `TryDoingShootAttack` presentation details beyond the current queued-slot overwrite, delayed-projectile overwrite, queued ammo snapshot, preserved fire-time queued weapon family through delayed projectile refresh, mirrored queued-facing live-origin refresh, preserved owner-relative delayed projectile offset, late queued fire-time ammo revalidation, queued cast startup, fire-time queued slot-index refresh, shared queued weapon gate, queued bullet-family visual override, and the now-tightened fire-time cash-slot visual fallback, especially the exact cosmetic bullet-position math and any other residual `GetProperBulletPosition` metadata the client still derives internally at queued fire time. | Final-attack style procs still no longer collapse into the initiating cast timing, queued strikes still cancel if the player changes to a non-matching weapon before the deferred attack fires, if a newer follow-up proc replaces the pending one before it can execute, or if the follow-up skill has been locally unlearned before the queued branch actually fires, and replacing a pending final attack still does not wipe unrelated queued summon damage. Queued Spark now follows that same queue-time and execute-time weapon snapshot rule instead of surviving a weapon swap that the client would reject. Once a queued final attack or queued Spark has already progressed into a delayed projectile release, a newer proc in that same client-owned slot still clears the older still-pending projectile instead of letting it survive past the queue replacement point, while unrelated projectile work remains intact. Deferred moving-shoot payloads also stop dropping out when a due queued follow-up family should run first because the payload stays armed until after that queued final-attack, Spark, or serial branch resolves instead of being dequeued and lost. Queued follow-up casts that pass through the simulator's extra deferred moving-shoot payload path still keep the stored facing or origin snapshot, preserve the ranged final-attack shoot-range override (`65`) through both deferred target acquisition and the actual delayed fire step, revalidate the live learned level again at the actual deferred fire time, keep the queued target-position snapshot alive across both direct execution and deferred projectile targeting so a vanished opener mob does not force those branches straight back onto generic live ordering, and still revalidate the live fire-time ammo count plus skill-specific ammo family instead of only trusting the older queue-entry ammo snapshot. The queued ranged shoot-entry seam is now closer on both execution and presentation because queued final-attack and Spark casts recheck their armed USE or CASH ammo metadata at queued fire time even when the queued branch was armed under a no-consume buff snapshot, keep the original ammo item ownership instead of inheriting unrelated later casts, drop the queued ranged branch when the originally armed ammo family disappeared or no longer meets the live consume count, draw delayed queued projectiles from the matching snapshotted bullet-family frames rather than always falling back to the generic skill projectile art, stop preferring a stale cash-bullet family once the fire-time slot refresh has already shown that the queued cash lane disappeared while the armed USE lane still remains valid, keep that delayed projectile refresh on the queued cast's already-resolved weapon family even if the player changes to a different ranged weapon only after the queued shot has already fired, still keep the delayed queued projectile anchored to a mirrored live shoot point when the avatar turns around after the shot was armed but before the preserved queued-facing projectile actually releases, and now also keep any already-armed owner-relative projectile offset through that late mirrored refresh instead of collapsing the projectile back onto the owner origin. The remaining mismatch is now mostly in deeper client-owned presentation bookkeeping such as the exact cosmetic bullet-position placement and other residual queued shot metadata beyond the current USE or CASH projectile item snapshot, queued fire-time ammo-family and consume-count revalidation, mirrored delayed fire-time shoot origin refresh, preserved delayed owner-relative offset, queued delayed fire-time slot-index refresh, fire-time cash-slot visual fallback edge metadata beyond the current slot-live check, and the shared queued weapon gate now covered by `Spark` too. | `SkillManager`, `SkillData`, `SkillLoader`, `ClientShootAmmoResolver`, `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` (`Skill/1511.img/skill/15111006/info/{condition,chainAttack,chainattackPenalty}`, `Skill/222.img/skill/2221006/info/{chainAttack,chainattackPenalty}`, `Skill/512.img/skill/5121002/info/{chainAttack,chainattackPenalty}`, `Item/Consume/0206/02060000/bullet/0`, `Item/Cash/0502/05021000/bullet/{0,1,2}`, `CUserLocal::GetProperBulletPosition`, `CUserLocal::TryDoingFinalAttack`, `CUserLocal::TryDoingSerialAttack`, `CUserLocal::TryDoingSparkAttack`, `CUserLocal::TryDoingShootAttack`) |
| Partial | Repeat-skill sustain families | `CUserLocal::TryDoingRepeatSkill` still owns more of the Mechanic repeat-state machine than the simulator, but this pass again stays inside the existing repeat-sustain and summon official-session seams instead of inventing a second local-only owner. WZ re-check still shows the same authored family surface that this runtime already leans on: `Skill/3512.img/skill/35121003` still owns the SG-88 summon / attack branch metadata (`info/type = 33`, `info/minionAttack = normal`, `summon/attack1`) and `Skill/3512.img/skill/35121013` still owns the tank-siege effect lane (`info/fixedState = 1`, `info/onlyNormalAttack = 1`, `effect/*/delay = 30/60/30/60`), so the work remains on transport and confirmation plumbing rather than changing authored timings or repeat-state branch choice. Tank-siege mode-end acknowledgement still remains request-time keyed end-to-end inside the shared timer seam: `SkillManager` still gates `HasPendingRepeatSkillModeEndRequest`, fallback-delay lookup, and `TryAcknowledgeRepeatSkillModeEndRequest` on the active pending request timestamp, `PlayerManager` still carries the stored pending request tick through the local fallback path, and the packet-owned resolve entry point still refuses to acknowledge anything except the currently pending `(skillId, returnSkillId, requestedAt)` lane. Direct IDA re-check still shows `CSummonedPool::OnPacket` dispatching opcode `0x119` (`281`) into `CSummonedPool::OnAttack`, then into `CUser::OnSummonedAttack`, while `CSummoned::TryDoingAttackManual` still owns the outbound request choreography that precedes those packets and still uses `CMobPool::CheckMobInTrapezoid`, `CMob::GetHitPoint`, and a `120` ms same-request follow-up delay for the clustered SG-88 lane. A second client re-check on `CUserLocal::TryDoingRepeatSkill` also keeps the current tank-siege approximation scoped correctly: once `m_bSendTankSiegeModeEnd` is raised, the client keeps polling the same repeat-sustain owner and only rewrites the repeat skill back to tank after the mechanic mode has already flipped to `35121005`, which matches keeping the simulator handoff inside the existing pending-mode-end seam instead of creating a separate acknowledgement owner. The simulator still keeps the SG-88 outbound bridge pinned to captured official evidence in both directions of that same summon seam: `SkillManager.OnSg88ManualAttackRequested` still feeds `SummonedOfficialSessionBridgeManager.TrackSg88ManualAttackRequest`, authoritative local-owner `0x119` target coverage still resolves and archives the tracked request with its original `requestedAt`, packet-owned `1021` confirmation still archives that same tracked request lane as a `1021-confirm` resolution, expired request windows are still pruned into recent bridge history instead of lingering indefinitely as pending captures, and the bridge can still synthesize a live outbound SG-88 request packet when a recent captured semantic request packet exposes a reusable field layout. This pass tightens that learned replay one step further without widening ownership: `SummonedOfficialSessionBridgeManager` still refuses simulator-originated replay traces as learned request-packet evidence and still keeps multiple learned official templates per admitted target-count bucket, but it now also keeps all simulator-originated outbound injection paths inside the same bridge-owned observation seam instead of making SG-88 learned replay the only caller that rebinds its own packet history. Immediate learned replay, `/summonedpacket session sendraw`, and queued `/summonedpacket session queueraw` flushes now all archive their outbound packet with an explicit simulator source tag through the same history/binding path that the bridge already used for official captures, so pending SG-88 requests keep one shared capture and confirmation owner even when the transport path changes. Template reuse also now narrows same-count fallback by ordered-lane closeness inside the existing official-capture cache: exact `(primary target, admitted target order)` still wins first, same-primary same-target-set captures now prefer the closer ordered lane before recency breaks ties, and true no-match fallback still biases to the newest official capture for that target-count bucket instead of letting incidental partial ordering override recency. Focused regression coverage for that outbound bridge seam is now on disk in `UnitTest_MapSimulator/SummonedPacketSessionBridgeTemplateParityTests.cs` for same-primary same-set ordered-lane preference and for preserving newest same-count fallback when there is still no lane match at all, alongside the existing official-session-only template-promotion and simulator-replay refusal rules this seam already owned. The remaining gap is narrower but still real: tank-siege mode-end still lacks the true official client/server confirmation opcode and exact request/ack ordering behind `m_bSendTankSiegeModeEnd`, and SG-88 still does not know the exact official outbound opcode plus full field order a priori, so automatic request origination still depends on first learning a reusable template from captured official traffic and still cannot yet prove every non-target field in that request one-for-one without further client or packet evidence. | Mechanic repeat skills are now closer on fixed-state timing, in-state cast restrictions, repeat-owned normal attack cadence, SG-88 branch timing, staged tank-siege teardown, and confirmation ownership because both the tank-siege end lane and the SG-88 manual lane stay request-time keyed deeper into the live bridge instead of stopping at local summon bookkeeping. The runtime now goes one step further on SG-88 replay-template ownership inside that same bridge seam: real local-owner summon attack packets can still reach the existing confirmation seam through `/summonedpacket packetclientraw` or the summon official-session bridge, captured official outbound request packets can still be replayed one-for-one through `/summonedpacket session sendraw|queueraw`, replayed outbound packets from any bridge-owned injection path now re-enter the same observed-history binding lane with explicit simulator source tags, same-primary same-set official captures now prefer the closer admitted-target order before a newer but weaker lane wins on recency alone, and no-match fallback still uses the newest official capture for that target-count bucket instead of stalling. The remaining approximation is now concentrated in the last unknown packet semantics rather than generic bridge bookkeeping: the simulator still does not know the true tank-siege ack opcode/order, and SG-88 replay still depends on captured-template evidence instead of a fully client-proven hardcoded opcode/field map. | `SkillManager.cs`, `PlayerManager.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedSummonedParity.cs`, `Managers/SummonedOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/SummonedPacketSessionBridgeTemplateParityTests.cs` (`CUserLocal::TryDoingRepeatSkill`, `CUserLocal::TryDoingSiege`, `CUserLocal::UpdateClientTimer`, `CSummonedPool::OnPacket`, `CSummonedPool::OnAttack`, `CUser::OnSummonedAttack`, `CSummoned::TryDoingAttackManual`, `CMobPool::CheckMobInTrapezoid`, `CMob::GetHitPoint`, `Skill/3512.img/skill/35121003/{info,summon}`, `Skill/3512.img/skill/35121013/{info,effect}`) |
| Partial | Bound-jump, rocket-booster end, and smoothing moving-shoot branches | `Rocket Booster` keeps the prior parity work from this row (ground-start gate, deferred launch-facing, startup/landing cadence, and exit-action teardown window), and this pass tightens the same `TryDoingSmoothingMovingShootAttackPrepare` / `TryDoingSmoothingMovingShootAttack` seam instead of widening into another family. WZ was rechecked first in the mounted export: `Skill/3500.img/skill/35001001/action/0 = flamethrower`, `Skill/3510.img/skill/35101009/action/0 = flamethrower2`, and `Skill/{3312,3500,3510}.img/skill/*/info/rapidAttack = 1` still confirm the authored moving-shoot action and rapid-attack surfaces this seam already consumes. The fallback resolver in `SkillManager` now actually keeps the queue-time owner preference on that WZ-authored `action/*` list by walking `SkillData.ActionNames` before the older singular `ActionName` when the authored entry cannot be mapped onto a client raw action code, which keeps the simulator's non-random fallback on the same authored list the client-appointed branch exposes instead of letting the legacy singular field win first. Client re-check still grounds the rest of the seam in the same two functions already cited on this row: `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare` at `0x912f90` still copies `m_nAttackActionType` into `m_movingShootEntry.nAttackActionType`, stores the chosen queued action separately in `m_movingShootEntry.nAction`, and routes client skill type `3` through `get_random_shoot_attack_action(...)`; the helper itself still shows the client using appointed WZ actions for non-type-`3` skills when available, switching to the shared random action table for type-`3` or unappointed paths, and aborting with `-1` when that table cannot resolve a valid owner. `CAntiRepeat::TryRepeat` at `0x9032b0` still confirms the same repeat gate already modeled here: `\|dx\| < 6` and `\|dy\| < 150` stay inside the suppression window, position escapes reset the stored point plus repeat count, and `m_nRepeatCount < m_nCountLimit` still allows one more pass before suppression closes. The existing simulator behavior from earlier passes remains in place on that evidence: deferred moving-shoot payloads still snapshot `nAttackActionType`, `nActionSpeed`, queued bullet metadata, and queued owner state into the delayed payload; delayed preferred-target hitbox resolution still consumes the queued attack-action-type rather than later live state; queued owner comparison still participates in anti-repeat suppression; type-`3` requests still choose from the shared shoot-family owner set (`shoot1` / `shoot2` / `shootF`) and still cancel arming when that owner cannot be resolved instead of falling back to unrelated later avatar state; and deferred moving-shoot anti-repeat still keeps the explicit `33121009` bypass plus the WZ-backed `rapidAttack => CountLimit 1` branch. Focused source-side coverage is now actually on disk in `UnitTest_MapSimulator/MovingShootParityTests.cs` for live-action raw-code classification (`22` / `23` / `24` / `45` => shoot, `48`-`53` => magic), the extra `doubleSwing` / `tripleSwing` / `proneStab` melee-family fallback, type-`3` shared shoot-family selection, type-`3` unresolved-owner cancellation, the type-`3` random-table-vs-authored-action gate, queued owner normalization vs distinct-owner suppression, the `rapidAttack` count-limit branch, and the WZ-authored `ActionNames`-before-legacy-`ActionName` fallback order. The row still has remaining work in the same bucket: confirmed client initialization and bypass rules for `CAntiRepeat` count limits beyond the currently modeled threshold reset, explicit `33121009` bypass, and `rapidAttack => CountLimit 1` branch; the exact enum-to-row mapping and per-row contents behind the hidden `aShootAttackAction` / `dword_C5917C` tables that power `get_random_shoot_attack_action(...)` beyond the now-modeled shared shoot-family fallback and unresolved-owner abort; any additional packet- or server-owned Rocket Booster end choreography outside the simulator's current local seam; and bound-jump special cases that still require direct client evidence instead of WZ-backed inference. | Movement-coupled attack skills now keep the client-owned moving-shoot queue metadata, WZ-authored action preference, and anti-repeat owner surface closer to the real client from one seam, and the repo finally has source-side tests covering the same moving-shoot branches this row has been claiming. The remaining mismatch is now narrower and concentrated in deeper hidden-table recovery for `get_random_shoot_attack_action(...)`, fuller `CAntiRepeat` initialization details, and any packet-owned Rocket Booster or bound-jump choreography still outside current simulator ownership. | `SkillManager.cs`, `UnitTest_MapSimulator/MovingShootParityTests.cs` (`CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `get_random_shoot_attack_action`, `CAntiRepeat::TryRepeat`, `Skill/3312.img/skill/33121009/info/rapidAttack`, `Skill/3500.img/skill/35001001/action/0`, `Skill/3500.img/skill/35001001/info/rapidAttack`, `Skill/3510.img/skill/35101009/action/0`, `Skill/3510.img/skill/35101009/info/rapidAttack`) |
| Partial | Key-down / charge skills | The prepared-skill seam no longer forces every supported release-owned family through a gauge-duration-sized startup window before the generic keydown loop begins, and the same split now survives on the remote prepared-skill path instead of dropping zero-startup charge families back to a non-holding armed state. WZ was rechecked first for the authored startup split the client actually exposes in the mounted export: `Skill/322.img/skill/3221001/{prepare,keydown,action,info}` still publishes a real prepare window with `prepare/time = 1200`, `action/0 = piercing`, and the same `900 ms` gauge window the simulator already resolves through the shared HUD rule table; `Skill/531.img/skill/5311002/{prepare,keydown,action}` still publishes `prepare/time = 1080`, `prepare/action = noiseWave_pre`, `keydown/action = noiseWave_ing`, and `action/0 = noiseWave`; but the Big Bang trio under `Skill/{212,222,232}.img/skill/*`, Evan dragon breath under `Skill/2212.img/skill/22121000` and `Skill/2215.img/skill/22151001`, and the Dual Blade charge pair under `Skill/434.img/skill/{4341002,4341003}` still only expose `info/chargingSkill = 1` plus authored prepare actions with `prepare/time = 0`, which means those families should arm the generic hold loop immediately rather than waiting for the HUD gauge window to expire before hold begins. `SkillManager` still keeps the local startup split inside the existing seam: `ResolvePreparedSkillStartupDuration` consumes the authored WZ prepare window for release-owned families when one actually exists, leaves zero-time authored prepare surfaces on an immediate hold transition, and `BeginPreparedSkill` still enters hold immediately for zero-startup keydown families instead of delaying until the next gauge-sized phase boundary. This pass closes the mirrored remote seam too: `PreparedSkillHudRules.ResolveRemotePreparedSkillPhases` now auto-arms release-owned zero-startup keydown families onto hold whenever the packet/session lane carries a real hold window, and `RemoteUserActorPool.TrySetPreparedSkill` no longer strips `AutoEnterHold` just because the authored prepare duration is `0`. Existing parity in this row remains in place for HUD suppression/skins, mechanic sustain timing, WZ `repeat` hold branches, negative-`z` prepare/hold/release overlays, scan-code-backed local input ownership, dragon-owned HUD anchoring, release-triggered execution for the Big Bang trio (`2121001`, `2221001`, `2321001`), Marksman Piercing (`3221001`), Evan dragon breath (`22121000`, `22151001`), Dual Blade charge pair (`4341002`, `4341003`), Monkey Wave (`5311002`), and Wild Hunter swallow follow-up ownership, plus authored charge-window gauge timing, the shared `30 ms` minimum clamp plus gauge-window cap from `get_max_gauge_time` / `CUserLocal::OnKeyDownSkillEnd`, and the widened shared partial-charge scaling owner table that now keeps the Big Bang trio and both Dual Blade charge branches on the same linear undercharge seam as Piercing / Evan breath / Monkey Wave. Focused source-side regression coverage is now checked in under `UnitTest_MapSimulator/PreparedSkillChargeStartupParityTests.cs` for the WZ-authored startup-duration seam across the Big Bang trio, Piercing, Monkey Wave, Evan breath, and the Dual Blade charge pair, plus the remote zero-startup auto-hold persistence seam. | Remaining gap is narrower and still centered on client-only ownership/choreography outside the WZ-confirmed seams already modeled here. The simulator still does not reproduce the client's full raw make/break scan-code stream ownership, the wider exclusive-request catalog beyond the prepared-skill request family now sharing the `300 ms` gate, or the exact family-specific post-release math for every release/follow-up branch that consumes the normalized charge time after release. The current seam now guarantees that release-owned families use authored WZ startup windows when they exist, immediately arm zero-startup charge families onto the generic hold loop when WZ leaves `prepare/time = 0`, carry that same zero-startup auto-hold split through the remote prepared-skill owner path when a hold window is present, take the release path with the client-shaped minimum elapsed clamp, retain the shared authored gauge windows surfaced by WZ, and keep the Big Bang trio plus both Dual Blade charge branches on the same shared linear undercharge scaling seam as the earlier Piercing / Evan breath / Monkey Wave set, but any family-specific non-linear release scaling, packet-owned charge semantics, or follow-up choreography beyond that widened shared owner table is still not fully one-for-one. | `PreparedSkillHudRules.cs`, `SkillManager.cs`, `MapSimulator.RemoteUsers.cs`, `PlayerInput.cs`, `PlayerManager.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `UnitTest_MapSimulator/PreparedSkillChargeStartupParityTests.cs` (`Skill/{212,222,232}.img/skill/*/info/chargingSkill`, `Skill/322.img/skill/3221001/{prepare/time,prepare/action,keydown/0/delay,action/0,info/piercing,info/piercingBouns}`, `Skill/2212.img/skill/22121000/{prepare/action,prepare/time,common/time,info/chargingSkill}`, `Skill/2215.img/skill/22151001/{prepare/action,prepare/time,common/time,info/chargingSkill}`, `Skill/434.img/skill/{4341002,4341003}/{prepare/action,prepare/time,info/chargingSkill}`, `Skill/531.img/skill/5311002/{prepare/time,prepare/action,keydown/action,action/0,common/time,info/chargingSkill}`, `get_max_gauge_time`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::OnKeyDownSkillEnd`, `CUserLocal::DrawKeyDownBar`, `CDragon::DrawKeyDownBar`) |
| Partial | Swallow, mine, cyclone, and sit-down healing branches | The simulator still keeps the earlier mine/swallow/cyclone/healing work already landed on this row, and this pass tightens the same client-owned seams again instead of widening support into another family. WZ recheck in the current export still shows `Skill/3310.img/skill/33101005/prepare/action = swallow_loop`, which keeps the visible swallow branch anchored on the existing swallow action family already loaded through the player transform seam. Client recheck against `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500` still narrows the wriggle owner to the visible `33101005` swallow owner only: the client does the `500 ms` mob wriggle work only while `m_preparingSkill.nSkillID == 33101005`, and it sends a visible `33101005` cancel when the swallowed mob can no longer be resolved. `SkillManager` still follows that narrower shape by only advancing the swallow digest wriggle cadence while the visible swallow owner is still active through the existing prepared-skill or visible swallow action seam (`swallow_pre`, `swallow_loop`, `swallow`) instead of continuing to pulse after the visible swallow action has already fallen off. The earlier swallow work on this row remains in place around that tightened owner, and this pass corrects the WZ-backed swallow-family traversal itself so it follows the visible swallow root's linked dummy follow-up ids from `Skill/3310.img/skill/33101005/info/dummyOf` instead of reverse-searching every unrelated `dummyOf` family. Current WZ recheck still shows the real swallow root on `Skill/3310.img/skill/33101005/{prepare/action,info/dummyOf}`, while unrelated `dummyOf` families such as `Skill/3200.img/skill/32001001/info/dummyOf`, `Skill/3211.img/skill/32111005/info/dummyOf`, and `Skill/3510.img/skill/35100008/info/dummyOf` still do not publish `info/swallow`, so `SkillLoader` now marks only explicit swallow roots and their linked follow-up ids as `IsSwallowFamilySkill`, and the local swallow cancel/absorb matcher now stays one-way on that same visible root plus its linked follow-up ids instead of accepting reverse-linked unrelated dummy families. That keeps cancel/absorb-ack matching on the visible swallow skill plus linked dummy follow-up ids (`33101005`, `33101006`, `33101007`), preserves the existing `900 ms` keydown threshold before release, early-release cancel, absorb-ack gated capture success, automatic `3000 ms` digest start, cancel-path teardown on failed or invalidated absorb outcomes, and captured-mob consumption before the hidden attack dummy branch fires, while no longer leaking swallow-only restriction or runtime ownership onto unrelated dummy-skill families. The earlier mine work also remains in place on the same row: client recheck against `CUserLocal::TryDoingMine` at `0x907d70` still narrows mounted movement through the riding-vehicle-id gate, town suppression, `update_time - 1000` first-deploy lead, grounded `CVecCtrl::GetInput` X sign, and the shared `CVecCtrl::IsStopped()` clear, so the simulator still resolves Wild Hunter mine movement from grounded controller input plus the stopped-controller gate instead of foothold velocity fallback. The hidden mine fallback still stays on the narrowed client-owned vehicle seam already landed earlier: no visible mounted action means no deploy unless Jaguar Rider ownership is still active or inside the same short update-time grace window, and the hidden jaguar vehicle id still only resolves from a live Jaguar Rider-owned mount or an actually equipped jaguar. Cyclone still sustains as a timed `32121003` body-attack loop with `1000 ms` pulses only while the cyclone action remains active. The sit-down-healing work remains in the existing summon/runtime seam. WZ recheck still shows `Skill/3511.img/skill/35111011/summon/stand/info/range` publishing `lt=(-150,-100)` / `rb=(150,20)` while `summon/heal/info/range` publishes the narrower `lt=(-125,-100)` / `rb=(125,0)`, so the authored named-branch support-range fix remains the owner for the heal box itself. Client recheck still narrows remote Healing Robot discovery to the local robot plus remote-party robots only: `String/Skill.img/35111011/desc` and `pdesc` still describe nearby party or team healing, `CUserPool::DoHealNearHealingRobot` still probes the local robot plus remote-party robots only, and `CSummoned::TryDoingHealingRobot` still owns the range check, per-user reheal lock, action `13` request, and suspend flip on the summon. `SummonedPool` therefore still routes remote sit-down healing through the existing party-membership evaluator instead of the older broader remote-owner shortcut for `35111011`, while preserving the existing percent-HP heal (`common/hp`), `x=5` second re-heal lock, every-seated-frame local polling, authored heal-branch suspend duration, packet-owned heal action `13` playback, and named-branch playback timing. This pass still keeps the authored idle-clear seam on the narrower Healing Robot-only owner gate so the suspend-reset helper clears only Healing Robot-owned support suspend windows instead of any support summon that happens to publish a suspendable branch. Focused row coverage is now on disk in `UnitTest_MapSimulator/SummonSupportRangeParityTests.cs` for the authored heal-branch range seam, the party-only remote Healing Robot owner filter, and the narrowed Healing Robot suspend-clear owner gate, `UnitTest_MapSimulator/WildHunterMineParityTests.cs` for the client-owned grounded-input plus stopped-controller mine gate and hidden-mount fallback gate, and `UnitTest_MapSimulator/WildHunterSwallowParityTests.cs` for both the visible swallow-family classification seam and the narrowed visible-root request matcher. The row remains partial because the simulator still does not mirror the client's exact swallow packet ordering, the exact server-owned swallow capture/digest request choreography, the remaining `TryDoingMine` riding-vehicle-id ownership nuances beyond the current active-owner plus update-time grace-window gate and live/equipped jaguar fallback, the exact request/skill-packet-owned Healing Robot suspend reset choreography beyond the current heal-action playback owner plus Healing Robot-only idle-clear seam, or the remaining non-Wild-Hunter swallow-family nuances one-for-one | These branches now stay closer to their real client owners instead of flattening back into the generic cast/runtime path. Mine still respects the client town suppression, update-time-owned hidden-owner grace window, narrowed hidden jaguar fallback, and the client-shaped grounded `CVecCtrl` input plus stopped-controller gate instead of a simulator-only foothold-velocity fallback; cyclone still stops pulsing once the sustained cyclone action falls off the avatar; swallow still routes charge, cancel, absorb success/failure, digest timing, and hidden follow-up attack through one WZ-backed swallow-family seam, its wriggle pulses still stop once the visible swallow owner falls off instead of continuing off-action, the linked dummy follow-up ids are now classified from the actual visible swallow root's authored `dummyOf` list, its local request matching no longer accepts reverse-linked unrelated dummy families, and unrelated `dummyOf` families no longer get misclassified as swallow-owned; and sit-down healing still both matches the authored Healing Robot heal box itself and limits remote healing discovery to the client-shaped local-or-party owner seam instead of accepting any nearby remote owner or clearing unrelated support suspend windows through the same helper. The remaining visible mismatch is narrower and centered on exact swallow packet choreography, the last riding-vehicle-id details in `TryDoingMine`, the last request/skill-packet-owned Healing Robot suspend-reset details outside the current authored playback owner, and any residual non-Wild-Hunter swallow-family behavior the client owns outside the current local cancel/request path | `SkillManager`, `SkillLoader.cs`, `SkillData.cs`, `PlayerCharacter`, `MapSimulator`, `SummonedPool.cs`, `SummonRuntimeRules.cs`, `UnitTest_MapSimulator/SummonSupportRangeParityTests.cs`, `UnitTest_MapSimulator/WildHunterMineParityTests.cs`, `UnitTest_MapSimulator/WildHunterSwallowParityTests.cs` (`CUserLocal::TryDoingSwallowBuff`, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::TryDoingMine`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingSitdownHealing`, `CUserPool::DoHealNearHealingRobot`, `CSummoned::TryDoingHealingRobot`, `CVecCtrl::GetInput`, `CVecCtrl::IsStopped`, `Skill/3200.img/skill/32001001/info/dummyOf`, `Skill/3211.img/skill/32111005/info/dummyOf`, `Skill/3300.img/skill/33001001/info/type`, `String/Skill.img/33001001`, `Skill/3310.img/skill/33101005/{prepare/action,info/dummyOf}`, `Skill/3510.img/skill/35100008/info/dummyOf`, `String/Skill.img/35111011/{desc,pdesc}`, `Skill/3511.img/skill/35111011/{common/hp,common/x,summon/stand/info/range,summon/heal/info/range}`) |
| Partial | Summon simulation | Summon-family skills still keep the broader parity work already landed here: generic summon lifecycle/timers, WZ-backed attack branch and metadata loading, projectile and target-impact branch retention, Tesla triangle targeting, self-destruct prepare/die handling, packet-owned prepare/attack visual parity, taunt-only puppet registration, shared summon rule selection between local and packet-owned seams, and the current movement/gating buckets for stationary, grounded-follow, owner-hover, and anchor-bound summons. The earlier client-backed Tesla timing fix remains in place: IDA for `CSummoned::TryDoingTaslaCoilAttack` still shows Tesla impact timing is driven by the summon attack-delay window itself, with a fixed `300 ms` floor plus per-target jitter inside the remaining window rather than by per-target projectile travel distance, and both the local `SkillManager` seam and the packet-owned `SummonedPool` seam still follow that rule. The earlier branch-selection lift also remains in place: packet-owned summon attacks still consume the same authored branch surface the loader keeps for `attackN`, `attackTriangle`, `heal` / `support`, and self-destruct removal branches instead of collapsing back to `attack1`, and packet-owned skill/attack playback still arms the same raw-action-shaped one-time-action window the client feeds through `CSummoned::SetAttackAction`. This pass stays in that same loader/runtime seam and closes the next concrete summon-impact timing gap instead of inventing another summon-family special path. WZ was rechecked first for the authored branch/timing metadata that the simulator was still only partially consuming: the current mounted export still publishes `Skill/311.img/skill/3111005/summon/attack1/info/attackAfter = 360` with `range lt=(-100,-60) rb=(100,60)`, still publishes the same bird post-effect rows through `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, and `Skill/312.img/skill/3120010/tile`, still publishes authored summon `pos` metadata on both the projectile and hit surfaces the runtime already loads, including `Skill/311.img/skill/3111005/mob/pos = 2`, `Skill/311.img/skill/3111005/hit/0/pos = 1`, `Skill/311.img/skill/3111005/hit/1/pos = 3`, `Skill/321.img/skill/3211005/hit/0/pos = 1`, `Skill/321.img/skill/3211005/hit/1/pos = 1`, `Skill/3511.img/skill/35111002/mob/pos = 2`, and `Skill/3511.img/skill/35111005/mob/pos = 2`, and still keeps distinct named summon attack owners such as `Skill/3511.img/skill/35111002/summon/{prepare,attackTriangle}`, `Skill/3511.img/skill/35111011/summon/{stand,heal}`, and `Skill/3511.img/skill/35111001/summon/{die,die1}` beside the generic summon `prepare` branch. Client evidence was then rechecked directly in IDA to anchor how those summon-owned source, target, and timing points are shaped. `CSummoned::OnAttack` at `0x753340` still keeps the loaded summon attack info's `tAttackAfter` in the per-hit entry, still schedules the bird tile overlay with `RegisterFootHoldAnimation(..., tDelay1 + 200, 500, 128, 255, ...)`, still schedules the `4111007` reactive chain with `RegisterChainlightningAnimation(tCur + tDelay1, tCur + tDelay1 + 270, ...)`, and for the general summon-hit path still seeds the source point from the summon position with the same client-owned `y - 25` source anchor before clamping target Y inside the mob body rect, selecting near-edge target X by default, switching to target-center X for the centered branch, and keeping the same `600 px` cap. `CSummoned::SetAttackAction` at `0x74a0f0` also still only stamps `m_nOneTimeAction` and refreshes the prepared action layer; it does not append another generic prepare delay after the branch/action has already been chosen. The simulator now mirrors that narrower client-owned anchor/timing slice on both sides of the existing summon seam instead of only on the packet-owned half: `SummonImpactPresentationResolver` still treats authored `pos=2` as the same client source anchor (`summonX`, `summonY - 25`) instead of the raw summon origin, `SkillData` still exposes authored summon projectile `pos` selection through the same branch-owned projectile surface the loader already keeps, and both the local `SkillManager` seam and the packet-owned `SummonedPool` seam still let that authored projectile `pos` backfill the impact-anchor choice when the hit-presentation surface does not publish its own `pos`. The new parity landed in this pass is that `SummonRuntimeRules` now centralizes the branch-aware impact-delay rule the client is following, both the local `SkillManager` seam and the packet-owned `SummonedPool` seam now stop adding `SummonAttackPrepareAnimation` time on top of authored `tAttackAfter` when the active summon branch already owns playback through a named branch such as `attack1`, `attackTriangle`, support/heal, or self-destruct, both seams now also stop inflating non-Tesla summon impact execution time from projectile travel distance after `CSummoned::OnAttack` has already fixed that timing at `tAttackAfter`, and packet-owned attack playback now also resets its restart gate from that same branch-aware prepare-duration seam instead of the raw generic prepare clip length. The earlier shared post-effect work remains in place too: `SummonClientPostEffectRules` still centralizes the client-confirmed bird-tile / `4111007` post-effect gates, authored branch-delay lookup, reactive-chain endpoint math, and branch-rectangle tile placement; packet-owned bird-tile overlays and `4111007` reactive chains still use that authored branch-owned delay surface; and the local queued summon-attack seam still reuses the same client-owned rule owner instead of stopping at generic projectile and hit playback. Focused regression coverage for this shared anchor/timing slice is now on disk in `UnitTest_MapSimulator/SummonImpactPresentationResolverTests.cs` for client source-anchor handling on `pos=2`, projectile-owned `pos` fallback when no hit presentation owns the anchor, and branch-variant projectile `pos` selection, plus `UnitTest_MapSimulator/SummonAttackTimingParityTests.cs` for the branch-aware impact-delay seam that now skips the generic prepare window for named summon attack/support branches, preserves it for the unnamed fallback path, and keeps projectile speed out of non-Tesla impact execution timing. The row remains partial because the remaining parity gap is now narrower and still mostly client-only: the simulator still does not reproduce the full local Tesla beam ownership/action packet payloads from `CSummoned::TryDoingTaslaCoilAttack`, any additional raw summon action semantics beyond the now-modeled branch-aware `tAttackAfter` split plus the exact client special action ids `6`, `13`, and `16` still need more client evidence, the exact semantic mapping for any authored summon `pos` codes beyond the now-confirmed `pos=1` near-edge target anchor, `pos=2` source anchor, and `pos=3` centered target anchor still needs new client evidence, grenade-style expiry choreography beyond authored prepare/die playback is still approximate, the exact remote `OnSkill` / `OnAttack` / `OnHit` choreography for every summon family still goes beyond the current ordered impact handling, assist-target special cases not surfaced by WZ `minionAbility` / `condition` / packet assist bytes are still missing, the last one-for-one mob aggro retarget timing heuristics are still approximate, and party damage amplification still stops at the simulator's current summon-owned damage-buff seam. | Summon-heavy jobs are now closer not just on authored support polarity, Tesla timing, and raw packet attack-branch retention, but also on how the client keeps summon-owned post-effects, hit anchors, and attack timing tied to loaded summon attack info instead of recomputing them from one generic projectile or prepare fallback. Packet-owned summons that already kept their authored `attackN` / Tesla / support / self-destruct branches now still keep authored branch-owned post-effect timing for confirmed bird-tile and `4111007` reactive-chain families, and both the local and packet-owned summon execution seams now also consume authored projectile `pos` metadata when the hit-presentation surface does not override the anchor while no longer double-counting the generic prepare clip on top of authored `tAttackAfter` for named summon branches or stretching non-Tesla impact timing from projectile travel distance. The visible gaps closed here are that authored `pos=2` summon families now really use the client-shaped source anchor at `summonY - 25` instead of the raw summon origin, branch-owned projectile `pos` no longer gets silently dropped just because the corresponding hit presentation did not publish its own `pos` node, named summon attack/support/self-destruct branches no longer inherit an extra generic prepare-delay tax that the client does not append after branch selection, and non-Tesla summon hits no longer arrive later than the client simply because the target is farther away. The remaining mismatch is concentrated in deeper client-only payload ownership, broader remote choreography, and still-unconfirmed raw-action / `pos` semantics beyond the now-confirmed branch-aware `tAttackAfter` timing split, the exact client special raw-action ids `6` / `13` / `16`, and the `1/2/3` summon-anchor split. | `SkillData.cs`, `SummonClientPostEffectRules.cs`, `SummonImpactPresentationResolver.cs`, `SummonRuntimeRules.cs`, `SkillManager.cs`, `SummonedPool.cs`, `PacketOwnedSummonUpdateRules.cs`, `UnitTest_MapSimulator/SummonImpactPresentationResolverTests.cs`, `UnitTest_MapSimulator/SummonAttackTimingParityTests.cs`, prior summon branch/runtime coverage in `SkillLoader.cs`, IDA `CSummoned::TryDoingTaslaCoilAttack` (`0x752780`), IDA `CSummoned::OnSkill` (`0x74a940`), IDA `CSummoned::OnAttack` (`0x753340`), IDA `CSummoned::SetAttackAction` (`0x74a0f0`), IDA `CSummonedBase::Action2AIType` (`0x748df0`), IDA `CSummonedBase::AIType2Action` (`0x748da0`), IDA `CSummonedBase::LoadAttackInfo` (`0x758b20`), WZ evidence from `Skill/3511.img/skill/35111002/{mob,summon/prepare,summon/attackTriangle}`, `Skill/3511.img/skill/35111011/summon/{stand,heal}`, `Skill/3511.img/skill/35111005/mob/pos`, `Skill/3511.img/skill/35111001/summon/{die,die1}`, `Skill/311.img/skill/3111005/summon/attack1/info`, `Skill/311.img/skill/3111005/{mob,hit,tile}`, `Skill/321.img/skill/3211005/summon/attack1/info`, `Skill/321.img/skill/3211005/{hit,tile}`, `Skill/312.img/skill/3120010/tile`, `Skill/411.img/skill/4111007/{summon/attack1/info,ball}`, `Skill/522.img/skill/5220002/summon/attack{1,2}/info`, `Skill/532.img/skill/5321003/summon/attack{1,2}/info`, and `Skill/501.img/skill/5010005/summon/attack{1,2,3,4}` |
| Partial | Remote summoned-pool packet parity | The simulator still keeps the packet-owned summon transport seam in `SummonedPool`, but the remote attack and hit side is tighter again. It already mirrors the observed `0x116`-`0x11B` packet family, local-versus-remote owner registration, packet-owned move-path sampling, Tesla third-coil refresh, taunt-side ownership, packet hit damage bookkeeping, nested mob `attack/info/hit` metadata with UOL resolution, frame-scoped `attach` / `attachfacing` runtime behavior, authored per-frame summon alpha ramps on both live and detached one-time-action playback, the delayed bird tile overlay handoff from the filtered packet target list, and the `4111007` reactive chain with char-level -> exact-level -> root `ball` precedence before summon-owned or generic fallback. WZ re-check against the mounted export now tightened the bird post-effect seam too: `Skill/311.img/skill/3111005/tile` still publishes six root tile bands plus `effectDistance=10`, `Skill/321.img/skill/3211005/tile` still publishes three root tile bands plus `effectDistance=20`, and `Skill/312.img/skill/3120010` still keeps the broader `{CharLevel,ball,tile,summon}` surface. This run keeps the work inside that same post-effect owner: the loader now preserves packet-owned summon tile families in a dedicated `ZoneEffectData` surface instead of flattening every bird row to the first numeric `tile/0` band, it also keeps `level/<slv>/tile` and `CharLevel/<requiredLevel>/tile` families when WZ publishes them, and `SummonedPool` now resolves the remote delayed tile overlay from that stored tile-family surface at registration time using the same char-level -> exact-level -> root precedence already used for the client-owned `4111007` ball seam before falling back to the legacy single-band `ZoneAnimation`. Focused regression coverage for this tile-owner seam is now on disk in `UnitTest_MapSimulator/PacketOwnedSummonTileOverlayParityTests.cs` for root tile-band selection by skill level and for packet-owned tile registration when only a renderable variant band exists. The remaining gap is narrower but still real: the simulator still does not synthesize every hidden client `MobAttackInfo::sHit` / `bHitAttach` case one-for-one beyond the currently observed WZ surfaces and the modeled frame-scoped attach/facing plus attack-frame-hit fallback path; the `CSummoned::OnAttack` post-effect seam is still limited to the currently confirmed bird tile family plus the `4111007` reactive chain instead of every hidden post-effect case; the chain now follows the client-shaped char-level -> exact-level -> root-ball precedence, but it still does not reproduce literal UOL strings, GR2 chain-layer ownership, or exact textured bolt choreography one-for-one; the tile overlay no longer flattens all authored bird rows to band `0`, but exact client tile-family precedence beyond the currently observed WZ surface and literal `effectDistance` placement or GR2 layer ownership are still unresolved; it still approximates the broader client action-layer stack with detached draw overlays instead of reproducing literal GR2 action-layer canvas ownership and ordering; and `/summonedpacket` plus `SummonedPacketInboxManager` still expose a simulator loopback seam instead of full official `CUserPool` transport and ownership decode. | Packet-owned summons are no longer just spawn/remove stubs: they now keep client-shaped owner registration, packet-owned attack/hit/removal playback, frame-scoped attached-hit behavior, the confirmed bird tile post-effect family, and the confirmed `4111007` reactive-chain owner on one summon-side seam. This run matters because bird post-effects no longer collapse every authored WZ tile surface to the first exported band; the remote delayed tile overlay now preserves the same kind of variant-family selection the client-shaped ball seam already needed, which keeps higher-level bird summons closer to their authored presentation while leaving the remaining uncertainty concentrated in hidden post-effect families, exact tile spacing/layer ownership, and the last client-only transport details. | summon field/pool runtime (`CSummonedPool::OnPacket`, `CSummoned::OnAttack`, `SKILLENTRY::GetBallUOL`, simulator seams: `HaCreator/MapSimulator/Pools/SummonedPool.cs`, `HaCreator/MapSimulator/Pools/PacketOwnedSummonUpdateRules.cs`, `HaCreator/MapSimulator/Character/Skills/SkillData.cs`, `HaCreator/MapSimulator/Character/Skills/SkillLoader.cs`, `UnitTest_MapSimulator/PacketOwnedSummonTileOverlayParityTests.cs`, WZ refs: `Skill/311.img/skill/3111005/{info,summon,tile}`, `Skill/312.img/skill/3120010/{CharLevel,ball,tile,summon}`, `Skill/321.img/skill/3211005/{tile,summon}`, `Skill/411.img/skill/4111007/{info,summon,ball}`, mob hit refs under `Mob/*/attack*/info/hit`) |
| Partial | Summoned actor update/runtime parity | The simulator still keeps summon-owned actor update/runtime ownership (movement snapshots, spawn/prepare/attack/hit/die routing, Tesla armed-prepare idle fallback, self-destruct expiry handoff, packet-owned hit-overlay interrupt clearing, and named-branch timing that avoids forcing generic `summon/prepare` onto authored branches). This pass closes the next authored support-branch seam inside that same `SummonRuntimeRules` owner instead of leaving local Beholder playback on a separate path. WZ re-check against the mounted export still shows the authored support and assist surfaces that matter here: `Skill/132.img/skill/1321007/summon/skill{2,3,4,5,6}` for Beholder buff playback, `Skill/3511.img/skill/35111011/summon/heal` for Healing Robot, `Skill/3512.img/skill/35121009/summon/subsummon` for the summon-action family, and the stand-only support surfaces under `Skill/3511.img/skill/35111005/summon/stand` and `Skill/3512.img/skill/35121010/summon/stand`, with `info/selfDestructMinion=1` still present on both stand-only support skills as well as the previously confirmed `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}` and `hit` branches. The earlier local-vs-packet support-suspend work on this row remains in place, but the explicit current-branch owner is now tighter and Beholder no longer falls through it: local Beholder heal playback and authored Beholder buff playback now arm the same support-suspend window used by the other local support families, packet-owned support-suspend arming now validates the current authored branch instead of only the generic support-family default, and the shared `ShouldClearSupportSuspend(...)` path now checks the live authored branch name before clearing the suspend window. `SummonRuntimeRules` therefore now admits Beholder's authored `skill1` and `skill2..skill6` support branches as valid suspend owners alongside the previously covered stand-only and non-heal support families, so local and packet-owned support playback both stay on the same branch-owned timing seam. Focused regression coverage is now on disk in `UnitTest_MapSimulator/BeholderSummonRuntimeParityTests.cs` for explicit Beholder buff-branch suspend duration, explicit Beholder support-suspend tracking, and explicit current-branch suspend clearing. The row remains partial because deeper client-only seams are still outside current simulator visibility: exact hidden summon HP and full `CalcDamage::PDamageSummoned` / `MDamageSummoned`, broader `CVecCtrl::UpdatePassive` side effects beyond the confirmed passive effect trigger, deeper Tesla state-machine transitions and refresh rules, and the remaining assist-family remote branch choices that still depend on client-only state not exposed by current packet/WZ data. | This keeps more of real summon runtime ownership inside the summon layer instead of collapsing either local or packet-owned assist families to ad hoc runtime fallbacks: local and packet-owned body-contact damage still share the same mob-authored base-damage surface plus outgoing-damage modifiers, packet-owned `OnSkill` branch ownership still lands on authored `skill1..skill6` when packet actions provide those branch ids, packet-owned named summon branches still avoid inheriting a generic prepare window, packet-owned generic prepare branches still promote into live attack state instead of staying pinned on one coarse packet state through the whole action, and packet-owned mob-hit overlays still get interrupted both when summon-owned action-layer changes begin and when packet-owned attack playback completes back into the steady summon layer. The important gap closed here is that authored support-suspend ownership no longer diverges for Beholder versus the rest of the support family: packet special actions, packet-owned expiry playback, local Beholder heal/buff playback, other local support playback, and idle-time suspend clearing now all stay on the same authored branch-selection and branch-duration owner instead of leaving Beholder on a parallel local rule. Remaining mismatch risk is now concentrated in deeper hidden damage formulas, unmodeled passive-update side effects, deeper Tesla transitions, and the last remote/client-only assist selectors rather than in the observed authored support-branch surface, Beholder support-branch timing, the packet special-action fallback order, or the support-suspend owner/clear path. | summon actor/runtime layer (`CSummoned::Update`, `CVecCtrl::UpdateActive`, `CVecCtrl::UpdatePassive`, `CSummoned::OnHit`, `CSummoned::OnSkill`, `CSummoned::SetAttackAction`, `CSummoned::SetMoveAction`, `CSummoned::ProcessAction`, `CSummoned::ProcessAttack`, `CSummoned::SetDamaged`, `SummonRuntimeRules.cs`, `SkillManager.cs`, `SummonedPool.cs`, `UnitTest_MapSimulator/BeholderSummonRuntimeParityTests.cs`, `Skill/132.img/skill/1321007/summon/skill{2,3,4,5,6}`, `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3512.img/skill/35121009/summon/subsummon`, `Skill/3511.img/skill/35111005/{info/selfDestructMinion,summon/stand}`, `Skill/3512.img/skill/35121010/{info/selfDestructMinion,summon/stand}`, `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}`, `CSummoned::TryDoingHealingRobot`) |
| Partial | Weapon afterimage / melee trail parity | The simulator still keeps the broader WZ-backed afterimage surface already landed for this row: skill-authored plus shared `Character/Afterimage/<family>/<masteryIndex>` catalogs, weapon-item `info/afterImage` family selection with narrow sword-sibling fallback only, local basic-melee registration, charged `charge/<element>` rewrites for the confirmed Page, Paladin, Thunder Breaker, and Aran families, alias-aware action matching, `1221009 blast -> stabO2`, client-backed raw-range normalization for `1221009`, `57 -> 41`, and `74`, remote afterimage playback with fade tails, ordered remote WZ action fallback, the single-authored plus sole-renderable remote pose promotion seams, and remote prepared-skill release teardown from the official melee packet. Earlier on this row, the remote charge fallback had already moved into the same remote temporary-stat plus remote melee owner instead of a new packet or buff seam: `RemoteUserPacketCodec` recovers a single known charge skill from the broader stored remote temporary-stat payload after the mask prefix when the direct branch-specific decode does not yield one, and `RemoteUserActorPool` reuses that broader payload scan with the remote job-preferred charge family when remote melee playback needs to choose an afterimage `charge/<element>` family or visible charge effect without an explicit charge id in the attack packet. This pass keeps the work in the existing local/remote afterimage seams and narrows the remaining `CreateAfterimageLayer` drift again without widening transport ownership. WZ was rechecked first for the authored subframe-plus-alpha surface the simulator had already started mirroring: `Character/Afterimage/swordOS/0/swingO1/2/0` still publishes a single authored afterimage canvas with `delay=200` and `a1=0`, `Character/Afterimage/swordOS/charge/1/stabO2/1/{0,1}` still publishes ordered charge subframes with `delay=50` then `delay=300` and the terminal subframe still publishes `a1=0`, and the `1221009` skill-authored afterimage branch still keeps its own sparse authored surface under `Skill/122.img/skill/1221009/afterimage/swordOS/stabO2/1/0` with `delay=60`. Client evidence was then rechecked directly in IDA for the owner seams the simulator is approximating: `CActionMan::GetMeleeAttackRange` in this client still only proves the already-modeled raw-range overrides (`74`, `57 -> 41`) while `1221009` remains the caller-owned raw-action redirect already modeled outside that function, `CActionMan::CreateAfterimageLayer` still resets the layer alpha, removes the old canvases, resolves `GetWeaponAfterImage`, and loads the ordered canvases for the chosen raw action into the layer, and `CActionMan::GetWeaponAfterImage` still does not trust raw WZ child iteration for those canvases because it allocates by count and then requests child names `0..u-1` explicitly after first resolving the authored property child for that raw action. The simulator now mirrors three smaller owner details more closely in the existing seams: `SkillLoader` still enumerates afterimage frame-set canvases by the same dense `0..count-1` numeric probe the client uses instead of by raw WZ child iteration or sparse numeric sort, `MeleeAfterimagePlaybackResolver` now also promotes a sole authored frame-set when the current avatar outer-frame index has no exact numeric branch so sparse actions like `1221009` no longer wait for an outer-frame match before the authored trail can render, and both `PlayerCharacter` and `RemoteUserActorPool` still capture the exact authored subframe plus authored alpha at fade start through that resolver before the retained tail begins fading instead of only pinning the outer action-frame index. Focused tests are now on disk in `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` for the sole-authored frame-set fallback seam, the dense numeric frame-set probe seam, and the authored fade-start snapshot seam. The remaining gap is narrower again but still real: direct IDA recheck for this client version still only proves the current raw-range owner surface, so any further range overrides need new client evidence; the simulator still does not reproduce literal GR2 layer ownership and side effects such as the exact `RelMove` / `RemoveCanvas` / `LoadCanvas` choreography inside `CreateAfterimageLayer`; and the broader remote field packet surface still needs the rest of a one-for-one `CUserRemote::OnAttack` / `CUserRemote::OnMeleeAttack` transport lift and client-owned side effects beyond the currently consumed attack envelope, mob-hit, bullet-item, release-follow-up, broader remote charge-family fallback, and explicit-skill-range-owner slices | Many melee jobs rely on afterimage trails and authored swing ranges to sell timing and hit shape, and the simulator now consumes the same WZ-backed trail and arc data for local melee skill playback, local basic melee attacks, packet-owned local melee hitbox resolution, and simulator-owned remote actors while respecting the weapon item's authored afterimage family instead of forcing every sword through the long-family arcs. The important visual gap that remained in the current row had already narrowed when local and remote afterimages stopped flattening every authored subframe into one simultaneous fully opaque draw and started preserving authored WZ alpha ramps; this pass closes the next smaller loader and fade-entry drifts by matching the client's dense numeric canvas probing, by letting sparse single-authored actions keep their client-shaped afterimage surface even when the outer action frame does not publish the same numeric key, and by pinning the real authored subframe state when the retained tail begins. The earlier remote charge-family recovery still remains in place, and the current client recheck also shows the simulator is no longer missing any additional raw-range overrides inside `CActionMan::GetMeleeAttackRange` for this version beyond the seams already implemented. The remaining visible gap is now concentrated in literal GR2 afterimage layer choreography and the rest of the official remote melee transport rather than in charge-family fallback, sole-authored sparse-frame recovery, authored alpha ramps, dense numeric subframe selection, or fade-start snapshot ownership inside the existing loader/runtime seams. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `CharacterLoader.cs`, `CharacterAssembler.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `CharacterData.cs`, `MapSimulator.RemoteUsers.cs`, `PreparedSkillHudRules.cs`, `ClientMeleeAfterimageRangeResolver.cs`, `AfterImageChargeSkillResolver.cs`, `MeleeAfterimagePlaybackResolver.cs`, `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` (`CActionMan::Init`, `CActionMan::GetWeaponAfterImage`, `CActionMan::CreateAfterimageLayer`, `CActionMan::GetMeleeAttackRange`, `get_action_name_from_code`, `CUser::GetAfterimageUOL`, `CUser::RegisterAfterimage`, `CUserRemote::OnAttack`, `CUserRemote::OnMeleeAttack`, `SecondaryStat::DecodeForRemote`, `Character/00002000.img`, `Character/Afterimage/swordOS/{0/swingO1/2,charge/1/stabO2/1}`, `Character/Afterimage/*`, `Skill/122.img/skill/1221009/{action,afterimage}`, `Skill/434.img/4341004/common/{lt,rb}`, remote temporary-stat payloads) |
| Partial | Skill cooldown UI parity | QuickSlot overlays and skill hover cost lines, both skill-book icon overlays, the hotkey and off-bar cooldown trays, the shared packet-owned/server-owned cooldown ownership seam in `SkillManager`, the `UI/Basic.img/Notice3` off-bar notice stack, and the Battleship sentinel durability-side effect path now stay on the same existing cooldown UI/runtime seam instead of parallel per-surface cooldown math. The earlier pass had already routed packet-owned started/ready notices through the shared authoritative transition helper so repeated authoritative refreshes and no-op clears no longer re-emit start or ready notices after the cooldown state was already active or already cleared. This run keeps the work in that same owner and narrows the last cooldown-tooltip presentation drift instead of reopening another per-surface timer string or color path: `SkillUI`, `SkillUIBigBang`, `QuickSlotUI`, and both status-bar cooldown trays still source their `CoolTime` frame index and counter text through the same shared `SkillManager` cooldown-visual helper that sits on top of `TryGetCooldownUiState`, the two skill-book hover tooltips plus quick-slot hover still source their MP/cooldown line through that same authoritative cooldown UI-state plus the existing WZ-authored `SkillLevelData` seam, and the hovered hotkey plus off-bar cooldown trays now also consume the same shared inline cooldown-state formatter instead of flattening their state text back to a status-bar-only white string. `SkillCooldownTooltipText` remains the single formatter owner for both the tooltip cost line and the tray-hover state line, and it now drives shared inline tooltip segments across those surfaces so MP, `Cooldown:`, `Ready`, active remaining-seconds text, and explicit Battleship-style durability text stay on one packet-owned/server-owned formatting seam rather than drifting by widget. WZ re-check still confirms the cooldown-mask and notice-art owners the simulator is using: the pre-Big Bang book still sources its `16` stepped `32x32` masks from `UI/UIWindow.img/Skill/CoolTime`, the Big Bang book and quick-slot still source their matching `16` stepped `32x32` masks from `UI/UIWindow2.img/Skill/main/CoolTime`, both families still expose the same `0..15` canvas set, `UI/Basic.img/Notice3` still remains the closest currently extracted off-bar notice owner with the exact three-slice `t/c/s` stack (`266px` wide; heights `21/20/55`), and nearby `Notice4`, `Notice5`, and `Notice6` still remain materially different four-slice or boxed families rather than a clearly better match. `Skill/522.img/skill/5221006/common` still authors `epad`, `epdd`, `emdd`, `emhp`, and `emmp` beside `info/type=13`, so the existing shared status-bar tooltip seam remains the right owner for Battleship-family stat labeling while the exact tray/icon art stays unresolved. Focused regression coverage is now on disk in `UnitTest_MapSimulator/SkillCooldownTooltipTextTests.cs` for shared plain-text output, shared ready/active cooldown tooltip markup, shared tray-hover state-line markup, and explicit durability-state tooltip markup. The remaining parity gap is narrower again: exact cooldown-notification art family selection versus the current WZ-backed `Notice3` best fit, any last notice micro-offset or timing/fade differences beyond the shared 44px top-center seam, the final client compositor spacing and fine-grained per-fragment emphasis polish beyond the now-shared tooltip markup owner across book, quick-slot, and tray hover surfaces, the exact Battleship `CTemporaryStatView` icon/tray art and any durability-specific HUD widgeting beyond the mounted override plus ordered status-bar tooltip presentation now modeled here, and any additional bespoke cooldown-only HUD widgets beyond the quick-slot, skill windows, status-bar trays, and off-bar notice stack. | Cooling-down skills now stay synchronized across quick-slot masks, both skill-book variants, both status-bar trays, both skill-book hover cost lines, tray hover state lines, and off-bar started/blocked/ready notices because those surfaces all read the same authoritative cooldown UI-state seam and now also share the same WZ-authored `CoolTime` mask family plus one shared cooldown-visual helper and one shared tooltip formatter instead of mixing packet-owned overrides, authored WZ fallback timing, omitted hover state, and per-widget overlay implementations. Packet-owned refreshes still preserve the original cooldown start tick without retriggering the start-notice seam, authoritative clears and natural expiry still stop every cooldown surface together without falling back to authored WZ timers, quick-slot plus both skill books still consume the same shared inline tooltip markup so MP cost, `Ready`, active cooldown, and Battleship durability presentation no longer diverge by window-specific tinting or concatenation rules, and the hovered hotkey plus off-bar cooldown trays now use that same shared state-line markup so `Cooldown:`, `Ready`, active remaining-seconds text, and durability text no longer revert to a flat tray-only white line. Battleship-style vehicle buffs still identify themselves as `Transform` in the shared status-bar tooltip seam while keeping the WZ-authored temporary-stat order. The simulator still does not reproduce the client's exact cooldown-notification art choice, final notice timing or micro-offset polish, exact skill-tooltip compositor spacing/color polish, exact Battleship temporary-stat tray/icon visuals, or any extra cooldown-only HUD widgets that may still live outside the currently modeled shared seams. | `QuickSlotUI.cs`, `SkillUI.cs`, `SkillUIBigBang.cs`, `SkillCooldownTooltipText.cs`, `UIWindowLoader.EquipmentAndStats.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `StatusBarUI.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `SkillManager.cs`, `SkillCooldownNoticeUI.cs`, `SkillCooldownNotificationTransition.cs`, `Skill/522.img/skill/5221006/common`, `UI/UIWindow.img/Skill/CoolTime`, `UI/UIWindow2.img/Skill/main/CoolTime`, `UI/Basic.img/Notice3`, `UI/Basic.img/Notice4`, `UI/Basic.img/Notice5`, `UI/Basic.img/Notice6`, `UnitTest_MapSimulator/SkillCooldownTooltipTextTests.cs` |
| Partial | Client-timer skill expiry and cancellation | `SkillManager` now owns a centralized client-timer registry that drives buff expiry, timed prepared-skill release, repeat-sustain expiry/mode-end, cyclone teardown, Wild Hunter swallow digest completion, WZ-timed local skill-zone expiry, and WZ-timed local summon expiry through one `UpdateClientTimer`-style seam; it also publishes ordered expiry batches and normalizes cancel requests through connected WZ `info/type` + `affectedSkill` + `dummyOf` families (including observed `type=10`, `type=16`, `type=33`, `type=1`, and multi-target `affectedSkill` links). `SummonedPool` already reused that connected-family matcher for packet-owned local summon cancellation, connected-family level recovery, authored-duration fallback, and inherited local-duration fallback across the full connected cancel family graph instead of only the packet skill id. Earlier work in this seam had already moved naturally expired packet-owned non-self-destruct summons into authored pending-removal playback before the shared family cancel request fans out, keeping local packet-owned expiry teardown off the generic immediate-die cancel path. This pass keeps that same seam and tightens the synthesized self-destruct expiry branch another step toward the client runtime that still lives in `CSummoned::Update`: once the timer bridge has already routed a local packet-owned summon family through shared expiry/cancel handling, `SummonedPool` still dispatches local self-destruct expiry side effects before the authored removal window starts, still preserves the final support-owned suspend window for packet-owned support summons, now also reuses the authored local support hit presentation when WZ publishes one instead of reducing support expiry to suspend bookkeeping alone, and still synthesizes one last local projectile / hit / tile-overlay presentation for attack-style self-destruct summons instead of treating expiry as teardown-only playback. The earlier mirrored fallback work also remains in place on the attack-style branch: natural-expiry self-destruct target selection still treats prior packet attack targets as authoritative candidate slots, still fills remaining slots from a mirrored authored-range fallback that probes both sides, and still resolves tied mirrored candidates from the local owner's position rather than summon facing before continuing into the shared local expiry presentation path. Direct IDA re-check of `CSummoned::TryDoingAttack` at `0x756860` still shows ranged summon attacks probing both the forward and mirrored rectangles before the client commits to a side, and when both sides stay live through that branch the client still resolves the chosen side from the local user's position relative to the summon before continuing into `ProcessAction` and `ProcessAttack`. WZ was rechecked first for the representative timed packet-owned support summon that anchors this branch: `Skill/3511.img/skill/35111011` still publishes `info/type=33`, `info/minionAbility=heal`, `info/selfDestructMinion=1`, `summon/{summoned,stand,heal,die}`, `hit/0/hitAfter=990`, and `hit`, which is the authored summon surface this expiry bridge now preserves more completely on the local packet-owned path. The connected cancel-family WZ surface this timer bridge depends on is still the same in the current extracted data source: `Skill/3211.img/skill/32110007/info/type=10` with `affectedSkill=32001003`, `Skill/3510.img/skill/35100008/info/dummyOf=35101009&&35101010`, `Skill/3510.img/skill/35101009/info/affectedSkill=35001001`, and `Skill/2112.img/skill/21120011/info/affectedSkill=21100002&&21110003`. Direct IDA re-check of `CUserLocal::SendSkillCancelRequest` at `0x93d730` still confirms that client cancel requests own additional prepared-skill, swallow-reset, vehicle-valid, and keydown-cleanup fallout beyond packet transport alone, while `CSummoned::Update` at `0x757f70` still shows that deeper summon runtime ownership remains separate through its vec-ctrl, body-contact, attack/support dispatch, self-destruct, `ProcessAction`, and `ProcessAttack` paths; crucially for this row, the natural-expiry self-destruct branch there still routes through `TryDoingHeal` / `TryDoingGiveBuff` / `TryDoingAttack` before `ProcessAction` and `ProcessAttack`. Focused regression coverage for this pass is now on disk in `UnitTest_MapSimulator/ClientSkillTimerPacketOwnedSummonExpiryParityTests.cs` for owner-position tie breaks on mirrored fallback selection, fallback slot fill ordering after authoritative targets are reserved, and the local packet-owned support self-destruct path that now keeps both the explicit support suspend window and the authored hit presentation when WZ publishes one. The row remains partial because the simulator still does not mirror the client's full timer catalog or the full summon-side expiry fallout for packet-owned summons: local synthesized self-destruct expiry attacks now keep prior packet targets even when the local authored-range fallback disagrees, no longer stay pinned to only the current-facing authored rectangle when packet target history is missing, and now also break mirrored fallback ties from the local owner position instead of summon facing, while local packet-owned support expiry also keeps the authored hit surface plus branch-owned suspend timing when available, but target acquisition and combat results still fall back to the current authored-range approximation instead of the full `CSummoned::Update` runtime when no authoritative packet target history is available, remote packet-owned self-destruct expiry still relies on incoming packet/runtime ownership rather than local synthesis, and other client-only `SendSkillCancelRequest` consumers beyond the currently surfaced cancel-side-effect list are still outside the current local WZ + packet + connected-family timer view. | Timed skill teardown now runs through one shared local seam instead of scattered expiry checks, manual status-bar buff cancellation no longer bypasses client-style cancel routing, and connected WZ cancel families can tear down prepared state, repeat/cyclone state, buffs, local summons, local zones, and packet-owned local summons through one normalized request surface. This run narrows the remaining packet-owned drift in the same seam that already covered local timers: packet-owned local summon-expiry batches still prime authored natural-expiry removal before the family cancel request, still coalesce same-family cancel requests, still keep local packet-owned self-destruct expiry from collapsing into teardown-only playback by restoring the client-shaped final support suspend / final attack presentation branch before removal starts, now also reuse the authored support hit surface for local packet-owned self-destruct support summons when WZ publishes one, and still resolve tied mirrored fallback candidates from the local owner position after authoritative packet targets have been reserved instead of letting summon-facing win those ties. Remaining risk is now concentrated on unmodeled client-owned timer families, the exact `CSummoned::Update` target-selection and combat-resolution rules that still sit behind the current local packet-owned self-destruct fallback when no packet target evidence exists, remote packet-owned expiry fallout that should remain packet/runtime-owned, and any additional `SendSkillCancelRequest` consumers not yet surfaced in the current cancel-side-effect list. | `ClientSkillCancelResolver`, `SkillManager`, `SummonedPool`, `MapSimulator`, `UnitTest_MapSimulator/ClientSkillTimerPacketOwnedSummonExpiryParityTests.cs` (`CUserLocal::UpdateClientTimer`, `CUserLocal::SendSkillCancelRequest`, `CSummonedPool::OnCreated`, `CSummoned::Update`, `CSummoned::TryDoingAttack`, `Skill/3511.img/skill/35111011/info`, `Skill/3511.img/skill/35111011/summon`, `Skill/3511.img/skill/35111011/hit`, `Skill/3211.img/skill/32110007/info/type`, `Skill/3211.img/skill/32110007/info/affectedSkill`, `Skill/3510.img/skill/35100008/info/dummyOf`, `Skill/3510.img/skill/35101009/info/affectedSkill`, `Skill/2112.img/skill/21120011/info/affectedSkill`) |
| Partial | Skill restrictions by map/state | The simulator still keeps the shared restriction seam already landed for this row: parsed map `fieldLimit` bits for `Unable_To_Use_Skill`, `Move_Skill_Only`, `Unable_To_Use_Mystic_Door`, the Mechanic-only `Unable_To_Use_Rocket_Boost` branch, and the mount / broader vehicle `Unable_To_Use_Taming_Mob` branch; the field-authored `info/moveLimit` movement-skill counter seam in `FieldRuleRuntime`; live `MapInfo` ownership for Coconut, Snowball, and Guild Boss basic-action owners; WZ-authored root `noSkill` and `info/noCancelSkill`; Mu Lung Dojo `mobMassacre/disableSkill`; the authored massacre `countEffect/*/skillUse` counter seam; shared mob-disease skill lockouts including `StopMotion` (`136`); ladder/rope vetoes for swallow plus the broader `DoActiveSkill_ShootAttack` family; and scripted direction-mode ownership that tears down prepared or upkeep-driven skills once local control is taken away. This pass stays on that same shared evaluator and narrows one of the remaining overbroad field branches with direct client evidence instead of adding another cast-path-specific special case: WZ re-check still shows map-authored `noSkill/skill`, `noSkill/class`, `info/noCancelSkill`, and `info/moveLimit` surfaces on the representative maps already tied to this row, while direct IDA re-check of the `CField::IsUnableToUseSkill` callers now confirms the client does not blanket-reject every skill under that field-limit bit. `CUserLocal::TryDoingMeleeAttack` (`0x91e780`), `TryDoingShootAttack` (`0x925a00`), and `TryDoingMagicAttack` (`0x92a240`) each guard `CField::IsUnableToUseSkill` with explicit recovered skill-id tables only, so the shared `FieldSkillRestrictionEvaluator` now blocks just that recovered client set (`4211002`, `4221001`, `1121006`, `1221007`, `1321003`, `4321001`, `4121008`, `5101002`, `5101004`, `5121005`, `21100002`, `21110003`, `21110006`, `4311003`, `4331000`, `4331004`, `4331005`, `4341002`, `33111002`, `35001003`, `1121001`, `1321001`, `3120010`, `3121003`, `3221003`, `5201006`, `32101001`, `32111011`) instead of flattening the whole bit into a simulator-wide "most skills" ban. Direct IDA re-check of `CUserLocal::TryDoingMagicAttack` at `0x92a240` still also shows a hard reject while `CAvatar::IsOnPlayingOneTimeAction` is active, and the simulator already exposes the matching local state through `PlayerCharacter.IsPlayingClientOwnedOneTimeAction`, so the shared `PlayerSkillStateRestrictionEvaluator` continues to reject active skill requests during that client-owned one-time-action window while the existing prepared/upkeep teardown paths consume the same message if that transient state begins after cast start. Focused on-disk regression coverage for this row is now in `UnitTest_MapSimulator/PlayerSkillStateRestrictionParityTests.cs` for the one-time-action message path used by the shared evaluator, the client-confirmed `Unable_To_Use_Skill` allow-vs-block split, the WZ-authored `noSkill/skill` and `noSkill/class` field bans, and the field-authored `moveLimit` counter seam that blocks additional movement-skill casts once the configured map quota is exhausted. The remaining gap is narrower but still real: deeper `CField` availability checks and the remaining field-owned or client-only forbidden-skill branches are still incomplete. | Maps and transient local states now block skill use closer to the client from one shared place instead of scattered path-specific checks: field-authored skill bans still flow through the live map seam, field-authored movement-skill quotas now stay pinned to the same field-rule owner that decrements successful local movement casts, prepared or sustained skills still tear down when local state, mob-disease, scripted control, or massacre counters turn invalid after cast start, active skill requests are still rejected while the client-owned one-time-action window is playing, and `Unable_To_Use_Skill` maps now no longer suppress unrelated skills that the client does not reject under that bit. The row stays `Partial` because the remaining mismatch is still concentrated in unrecovered `CField` gate branches and any additional client-only forbidden-state or special-field owners beyond the currently modeled field limits, recovered `Unable_To_Use_Skill` skill-id set, `moveLimit`, live Coconut/Snowball/Guild Boss ownership, root `noSkill`, Dojo massacre restrictions, mob-disease lockouts, scripted control, ladder/rope gates, and the one-time-action state seam. | `MapSimulator`, `SkillManager`, `FieldSkillRestrictionEvaluator.cs`, `FieldRuleRuntime.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `PlayerSkillBlockingStatusMapper.cs`, `PlayerCharacter.cs`, `MassacreField.cs`, `MapInfo`, `MapInfo.fieldLimit`, `MapInfo.moveLimit`, `MapInfo.fieldType`, `FieldLimitType`, `FieldType`, `Skill/MobSkill.img`, `Map/Map1/109080000.img/coconut`, `Map/Map1/109060000.img/info/fieldType`, `Map/Map9/980031100.img/noSkill/class`, `Map/Map9/922240000.img/noSkill/skill`, `Map/Map9/910320100.img/mobMassacre/disableSkill`, `Map/Map9/926010100.img/mobMassacre/countEffect/500/skillUse`, `Map/Map2/200110000.img/info/noCancelSkill`, `Map/info/moveLimit`, `UnitTest_MapSimulator/PlayerSkillStateRestrictionParityTests.cs` (`CUserLocal::Update`, `CUserLocal::IsSkillAvailable`, `CField::IsUnableToUseSkill`, `CField_Coconut::BasicActionAttack`, `CField_SnowBall::BasicActionAttack`, `CField_GuildBoss::BasicActionAttack`, `CField::IsMoveSkillOnly`, `CField::IsUnableToUseRocketBoost`, `CField::IsUnableToUseTamingMob`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::TryDoingSwallowAbsorb`, `CUserLocal::TryDoingMagicAttack`, `CAvatar::IsOnPlayingOneTimeAction`) |

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Summoned action-loader / frame-build parity | The client-owned summon loader seam remains in `SkillLoader`, and this pass narrows the hidden action-slot gap again without inventing a second runtime owner. Earlier work in the same seam already moved summon frame building into the loader, including the client's `120` ms frame-delay fallback, per-frame `z`, per-frame `a0` / `a1`, authored `origin` / bounds preference when linked wrapper nodes publish them, `lt` / `rb`-driven rect construction, reverse-playback duplication when the action branch publishes the reverse-style flag the client loader consumes, and the WZ-backed summon-source acceptance update that now admits real standalone summon-owner roots such as `Skill/3510.img/skill/35101005` while still rejecting non-summon repeat wrappers such as `Skill/3310.img/skill/33101005`. This run tightens what happens after that source is accepted. WZ was rechecked first in the mounted export for the same concrete families that still define the seam: `Skill/3510.img/skill/35101005` still exposes root-owned wrapper nodes `icon`, `iconMouseOver`, `iconDisabled`, `action`, and `effect` alongside the playable door branches `cDoor`, `sDoor`, `oDoor`, `mDoor`, and `eDoor`; `Skill/501.img/skill/5010005/summon` still exposes authored custom summon-action branches `{piratebless,rushBoom,cannonSlam,noiseWave,jump,hit1}` alongside `attack1`-`attack4`; and repeat/effect-only layouts such as `Skill/434.img/skill/4341006/summon/{effect,effect0,repeat,repeat0}` still exist and still should not be treated like summon-skill action slots. The loader now keeps that distinction inside the existing cache seam instead of letting standalone-root wrappers pollute the action family: when the accepted summon owner is a standalone root, `SkillLoader` now skips root wrapper branches such as `icon*`, `action`, and root `effect/effect0` during idle fallback selection and during supplemental/remaining action caching, so `35101005`-style door roots now bind their playable branch family in authored WZ order rather than accidentally promoting wrapper content ahead of `cDoor`. The runtime-facing summon-action resolver now consumes more of that authored cache too: local and packet summon-skill action resolution both fall back through the authored custom summon-action keys in WZ order when client-shaped `subsummon` / `skillN` names are absent, instead of limiting the packet path to the named branches alone. That means `SummonRuntimeRules` now gives `35101005`-style door roots a stable first custom action branch even when wrapper nodes are present on the standalone owner, maps `5010005`-style custom branches across packet skill actions `1..6` in authored order when no `skill1..skill6` branches exist, lets packet action `14` (`subsummon`) fall back onto that same first authored custom branch when no named `subsummon` branch exists, and still leaves pure repeat/effect layouts out of that fallback path. The broader summon cache work from earlier passes still stands: repeat-style summon branches remain first-class action keys, the loader still records the concrete branch names it bound, additional authored summon action names are still mirrored into `SummonNamedAnimations`, and the resolved summon hit branch key is still recorded separately while nested `hit/0` and `hit/0/1` keys remain cached under the summon-action cache. Focused regression coverage is now actually on disk in `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs` for `35101005`-style custom door-branch fallback, `5010005`-style authored custom action ordering, subsummon fallback onto the first authored custom branch when generic names are absent, rejection of repeat/effect-only layouts as summon-skill slots, and standalone-root wrapper pruning for `icon*` / `action` / `effect*` versus playable door branches. IDA still defines the remaining unresolved client-owned pieces: `CActionMan::GetSummonedProp` still resolves the summon asset from `SKILLENTRY::GetSummonedUOL`, `SKILLENTRY::GetSummonedUOL` still only exposes the precomputed `sSummonedUOL` field rather than where that field is authored, and `CActionMan::LoadSummonedAction` still indexes the hidden `s_sSummonedAction[nAction]` table before branch lookup. The remaining gap is therefore narrower again: linked summon owners, standalone summon roots, wrapper-pruned standalone-root branch order, and authored custom summon-action branches now flow through the existing loader/runtime seam more like the client-owned path, but the simulator still does not mirror the client's exact hidden `sSummonedUOL` producer or the exact enum-to-string contents of `s_sSummonedAction` for every summon action slot, so the new custom-branch fallback still relies on WZ-authored branch order where the client's internal table has not yet been recovered. | Without this row, future summon work is biased toward packet/runtime fixes while leaving the client's actual frame-build owner implicit. Tracking the loader explicitly keeps summon parity anchored to the real asset-selection and frame-construction seam, which is where action-name lookup, cached reuse, delay/z fallback, rect math, reverse playback, standalone-root wrapper pruning, custom action-branch recovery, and the WZ-backed summon-source recovery order are decided before `CSummoned::Update` ever consumes the result. With the loader work now landed in the existing seam and the simulator runtime now consuming the summon-action cache more directly on both the local and packet summon-skill paths, the remaining parity risk is concentrated on the hidden asset-resolution field and unrecovered action-table details rather than on basic summon frame construction, generic reverse playback, standalone-root acceptance for WZ-authored summon owners, or authored custom action branches being silently dropped or misordered once loaded. | summon action-loader layer (`SkillLoader`, `SkillData`, `SummonRuntimeRules`, `CActionMan::GetSummonedProp`, `CActionMan::LoadSummonedAction`, `SKILLENTRY::GetSummonedUOL`, `CSummoned::Update`, `CSummonedPool::OnCreated`, `Skill/3510.img/skill/35101005`, `Skill/3511.img/skill/35111002/summon/attackTriangle`, `Skill/3511.img/skill/35111011/summon/stand`, `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3511.img/skill/35111009/info/affectedSkill`, `Skill/3511.img/skill/35111010/info/affectedSkill`, `Skill/3310.img/skill/33101005/repeat`, `Skill/434.img/skill/4341006/summon/repeat`, `Skill/3511.img/skill/35111001/summon/die1`, `Skill/501.img/skill/5010005/summon`, `Skill/1311.img/skill/13111004/summon/hit`, `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs`) |

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

This helper-owner seam is currently under-tracked by the backlog even though both the client and simulator already expose concrete ownership here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow-partner and mirror helper actors | The simulator now models the client ownership split instead of treating every clone-family helper as the same WZ-backed shadow-partner path. `SkillData` / `SkillManager` recognize `Mirror Image` (`4331002`) as a distinct helper owner, `PlayerCharacter` keeps a separate mirror runtime from the existing `special/*` shadow-partner actor, and the update/render loop now drives mirror helper visibility from player action changes with a dedicated `m_tStartMirror`-style start timestamp, a 200 ms client-shaped rel-move window, and client-backed suppression for ladder, rope, morph, taming-mob, hit, and dead states. WZ re-check still confirms `skill/433.img/skill/4331002` exposes `action/0 = alert` plus buff/effect data but no `special/*` helper action branch, so Shadow Partner keeps using WZ-authored helper animations while Mirror Image continues to source the assembled avatar instead. This pass tightens the mirror source seam another step toward the targeted `CUser::PrepareMirrorActionLayer` / `CUser::GetMirrorSrcLayer` re-check without leaving the current simulator ownership model: `CharacterAssembler` still classifies every assembled avatar part into explicit `UnderCharacter`, `OverCharacter`, `UnderFace`, `Face`, and `OverFace` render planes, `PlayerCharacter` still keeps a dedicated mirror-owned prepared five-layer cache, and the earlier live-plane reuse refinement remains in place so no-op per-plane reuse no longer restamps the recreate time or silently re-arms the 200 ms rel-move. IDA for `CUser::GetMirrorSrcLayer` confirms the client mirror sources are the same five avatar layer handles in index order, while `CUser::PrepareMirrorActionLayer` still recreates each helper layer with `m_pLayerUnderFace` as the overlay parent regardless of which source plane fed that mirror layer, copies position from the live source-layer handle, and drives the helper through an inserted layer-owned canvas instead of re-drawing avatar pieces individually. `PlayerCharacter` now reflects more of that seam by routing every prepared mirror plane through a shared UnderFace overlay target, drawing the whole mirror helper package at the UnderFace insertion boundary, keeping mirror rendering on the prepared per-plane composed-snapshot path even when the current live source plane still matches, and now also mirroring two more client-side gates that fell out of the same IDA pass: flip-only changes no longer count as reusable prepared mirror layers because the client recreates those helper layers whenever their flip diverges from `m_pLayerUnderFace`, and mirror suppression/back-offset selection now consumes the client raw-action buckets (`is_back_action`, `is_alert_back_action`, raw action `48`) before falling back to action-name heuristics. Focused regression coverage is now on disk in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` for the flip-sensitive reuse rule and the client raw-action suppression/offset rules alongside the earlier shared-UnderFace insertion and prepared-snapshot placement coverage. The remaining gap is now narrower and still concentrated at the true live-layer-object boundary inside `CUser::PrepareMirrorActionLayer` / `CUser::Update`: the simulator still does not own shared `IWzGr2DLayer` mirror objects, exact origin/current-time snapshots from live layer handles, true `InsertCanvas` reparenting against the live client layer graph, or the follow-up clone-helper behaviors that depend on those real client layer objects rather than simulator-owned prepared plane snapshots. | Clone-family jobs are not just tray icons or generic buff effects in the client; they own dedicated avatar-adjacent helper layers that participate in action preparation and per-frame user update. Tracking that seam explicitly keeps future parity work focused on helper-layer timing, ownership, and clone-family differentiation instead of accidentally treating shadow-partner and mirror-image visuals as solved just because the simulator already renders one WZ-backed helper actor | `PlayerCharacter.cs`, `CharacterAssembler.cs`, `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` (`skill/433.img/skill/4331002`, `CUser::PrepareActionLayer`, `CUser::PrepareShadowPartnerActionLayer`, `CUser::PrepareMirrorActionLayer`, `CUser::GetMirrorSrcLayer`, `CUser::Update`, `is_back_action`, `is_alert_back_action`) |

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow Partner action-loader / frame-build parity | The shadow-partner helper row is no longer only a runtime/action-playback note. IDA-backed loader ownership is now explicit in this backlog too: `CActionMan::GetShadowPartnerProp` resolves the helper property for the owning skill id, and the direct re-check for this build tightened that seam further by showing `GetShadowPartnerProp` is just `SKILLENTRY::GetSpecialUOL` plus the resource-manager fetch rather than a wider hidden property-name probe ladder. `CActionMan::LoadShadowPartnerAction` then caches by `(skillId, action)`, first tries the named raw-action branch, falls back through `get_action_name_from_code` when the helper property uses numeric action slots, synthesizes pieced helper actions from hidden action-table alias rows before the plain raw-action fallback when that table is populated, enumerates the action canvases into cached helper frames, reads frame-owned canvas-property metadata before storing each frame entry, and appends the mirrored interior-frame tail when the loader's replay table marks that action. This run tightened the simulator's loader seam itself rather than the broader helper runtime: `SkillLoader` still builds a named-vs-numeric storage plan for Shadow Partner action rows so helper catalogs store the client action name when a helper property uses numeric action keys while preserving named-branch precedence when both the raw-action name and numeric fallback resolve to the same helper action, still resolves the helper source property through an explicit linked-`special` seam instead of assuming only a direct `special` child, still enumerates numeric helper frame children in client order instead of stopping on the first missing index, still routes Shadow Partner frames through metadata-owned bounds plus a client-backed replay-tail seam for the currently confirmed replay-table actions `stand1`, `stand2`, and `alert`, and now also synthesizes loader-owned pieced helper rows for the currently recovered action-table family by concatenating available authored helper branches in client order for the recovered raw-action aliases `avenger`, `assaulter`, `flyingAssaulter`, `savage`, `showdown`, `assassination`, `assassinations`, `ninjastorm`, `vampire`, `stabD1`, `swingD1`, `swingD2`, `doubleSwing`, `tripleSwing`, and `shotC1` while preserving any authored helper branch that already owns the same name. WZ-first recheck in the mounted export still shows the currently shipped Shadow Partner helper surfaces publishing named `special/*` branches for `4111002`, `4211008`, and `14111000`, plus the already recovered non-ghost `action/0` raw-action names (`alert2` / `alert3` / `alert4` / `alert5`, `swingO1`, `avenger`, `assaulter`, `prone2`, `savage`, `showdown`, `assassination`, `smokeshell`, `ninjastorm`, `vampire`), so the numeric-slot fallback, linked-`special` resolver, client-order frame enumeration, replay-tail work, and the new pieced-action synthesis remain loader-parity hardening inside the existing seam rather than a newly observed WZ branch shape. Client-backed alias coverage is tighter now as well: the simulator no longer only collapses raw action code `182` (`flyingAssaulter`) onto the authored `assaulter` helper branch family during lookup, it also synthesizes the same loader-owned helper frame row the client would consume from that alias seam even though that airborne action name does not appear in the mounted `action/0` WZ rows. Focused regression coverage is now on disk under `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs` for pieced helper-frame concatenation order, missing-piece retention, synthesized alias-row injection, authored-name precedence, numeric raw-action slot normalization, named-branch precedence over numeric fallback regardless of authored order, safe retention of unknown numeric helper keys, linked-`special` helper source resolution, client-order numeric frame enumeration with gaps preserved, the client-backed replay-tail rule for the confirmed replay-table actions, and the `flyingAssaulter -> assaulter` helper-family seam. Remaining gap is narrower and now specific to the still client-only pieces this run did not fully recover from the hidden `LoadShadowPartnerAction` action table: any additional action-specific piece lists or non-piece alias rows in that table beyond the currently recovered `action/0` plus raw-action-code alias surface, whether any table entries carry action-specific total-delay or zigzag semantics that should shape the synthesized helper row beyond straight branch concatenation for this build, and whether later client versions or other data sets introduce helper UOLs that do not land on the current linked-`special` seam even though this build's `GetShadowPartnerProp` path itself is now explicit. | Without this row, the backlog overstates clone-helper parity by treating Shadow Partner as if it were only a `CUser`/`PlayerCharacter` timing and placement problem. The client also keeps a dedicated `CActionMan` loader for helper actions, so future work needs a named seam for cache keys, branch lookup, numeric-slot fallback ownership, hidden action-table piecing, frame construction, and replay-tail ownership instead of letting those questions stay buried inside the helper runtime row. | shadow-partner action-loader layer (`CActionMan::GetShadowPartnerProp`, `CActionMan::LoadShadowPartnerAction`, `get_action_name_from_code`, simulator seams: `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `RemoteUserActorPool.cs`, `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs`, `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, `Skill/1411.img/skill/14111000/special/*`, `Skill/411.img/skill/{4111001,4111003,4111005,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121003,4121004,4121008}/action/0`, `Skill/420.img/skill/{4201002,4201003,4201005}/action/0`, `Skill/421.img/skill/{4211002,4211003,4211005,4211006,4211007}/action/0`, `Skill/422.img/skill/{4221001,4221003,4221006,4221007}/action/0`, `Skill/1410.img/skill/14101006/action/0`, `Skill/1411.img/skill/{14111001,14111002}/action/0`) |

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Time Bomb, Vengeance, and ExJablin packet-application parity | The simulator still owns this `CUserLocal::OnPacket` family inside the existing local-utility packet seam, and this row is now narrowed to evidence-confirmation gaps rather than missing packet-shape handling. WZ was rechecked first against the mounted export: `String/Skill.img` still resolves `4341003` as `Monster Bomb` with the `explosion occurs 3 seconds after the charm is activated` description fragment, `31101003` as `Vengeance`, and `4120010` as `Expert Throwing Star Handling` with the `the next attack will always be a Critical Attack` fragment. Direct IDA re-check still confirms the client-owned packet bodies and gates: `CUserLocal::OnTimeBombAttack` decodes `(skillId, timeBombX, timeBombY, impactPercent, damage)`, calls `CUserLocal::TryDoingMeleeAttack(..., bTimeBombAttack, nTimeBombX, nTimeBombY, ...)`, restores the `1500 ms` base hit period before coat-option merges, branches on `if (nUserImpactDeg)` instead of a positive-only test, applies `CUser::MakeIncDecHPEffect(this, -nDamage, 0)`, and then sets the hit emotion/impact; `CUserLocal::OnVengeanceSkillApply` still reads exactly one `Int32` and only branches on legacy packet skill id `3120010`; `CUserLocal::OnExJablinApply` still reads no payload bytes and only arms `m_bNextShootExJablin = 1`. The simulator still mirrors those shapes in the existing seam: `LocalUtilityPacketInboxManager` still recognizes packet ids `268` / `271` / `272` plus the `timebomb` / `vengeance` / `exjablin` aliases, `TryDecodePacketOwnedTimeBombAttackPayload` still requires the exact five-`Int32` Time Bomb body and preserves signed `damage`, `TryDecodePacketOwnedVengeanceSkillApplyPayload` still requires the exact one-`Int32` body, `TryDecodePacketOwnedExJablinApplyPayload` still rejects any non-empty payload before the next-shot flag is armed, `ApplyPacketOwnedVengeanceSkillApply` still ignores any packet skill id except the client-confirmed legacy `3120010` before resolving the learned local alias through the WZ-backed skill-id candidate seam, and `ApplyPacketOwnedTimeBombAttack` still uses the client-owned nonzero impact gate instead of a positive-only check. `ApplyPacketOwnedTimeBombAttack` still restores the client-default `1500 ms` base before the existing WZ-backed invincibility-option merge, still keeps the dedicated packet-owned Time Bomb melee branch in `SkillManager.TryApplyPacketOwnedTimeBombAttack`, still resolves the hitbox at the packet-owned `timeBombX` / `timeBombY` origin, and still switches target ordering to point-centered selection through `CreatePacketOwnedTimeBombTargetSortKey` so the closest mob to the explosion point wins before player-facing penalties. The same seam also still mirrors the narrow `MakeIncDecHPEffect` branch more literally than the generic player-damage path alone: positive damage still surfaces through the existing received-damage path, while zero damage now explicitly shows the local miss case and negative damage now explicitly shows the heal-number case through the shared combat-effect seam. This run also made the WZ-backed alias seam itself directly regression-testable without widening runtime ownership: the row now has explicit internal builders for the Time Bomb / Vengeance / ExJablin WZ alias catalogs plus the shared preferred-candidate ordering used by `ResolvePacketOwnedLocalSkillId`. Focused regression coverage is now on disk in `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs` for those WZ-backed alias builders and shared alias ordering, alongside the strict Time Bomb / Vengeance / ExJablin payload decoders, the legacy-only Vengeance packet-id gate, the nonzero Time Bomb impact gate, the signed Time Bomb HP-effect classifier, and the Time Bomb target-priority helper. The remaining parity gap is narrower but still real: Time Bomb still does not have a client-confirmed legacy packet-to-WZ skill-id mapping beyond the current WZ-backed `Monster Bomb` candidate, and the Time Bomb coat-option inference is still intentionally scoped to the client-confirmed invincibility options rather than a general simulator-wide item-option-id population seam for arbitrary potentials. | These handlers matter because they bypass the normal local input/cast path even though the client gives them their own dispatcher and follow-up state. The simulator now has a concrete owner for those packets, keeps the shared alias seam anchored to WZ first and client second, no longer accepts trailing bytes that the client packet readers would leave unread, no longer accepts non-client Vengeance packet ids, no longer treats only positive Time Bomb impact values as hit reactions, no longer clamps away signed Time Bomb damage payloads before the local reaction branch, no longer drops the zero/negative `MakeIncDecHPEffect` cases that hang off the same decoded damage field, and now has focused regression coverage around both the WZ-backed alias-candidate seam and the packet-owned decoder/helper seams instead of leaving this row dependent on manual re-checks alone. The remaining risk is concentrated in the still-unconfirmed legacy Time Bomb skill-id mapping and the deliberately narrow visible-potential-to-item-option inference seam, not in missing ownership for the Vengeance / ExJablin packet family, Time Bomb packet decode, raw signed damage consumption, the nonzero impact gate, the dedicated `bTimeBombAttack` execution path, the WZ-backed alias-candidate ordering, or the narrow helper coverage that now protects those branches. | packet-owned local skill-application layer (`CUserLocal::OnPacket`, `CUserLocal::OnTimeBombAttack`, `CUserLocal::OnVengeanceSkillApply`, `CUserLocal::OnExJablinApply`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::ApplyInvicibleOption`, `CUser::MakeIncDecHPEffect`, `CUserLocal::SetImpact`, `MapSimulator.PacketOwnedUtilityParity`, `LocalUtilityPacketInboxManager`, `SkillManager`, `PlayerCharacter`, `CharacterPart`, `Character/00002000.img`, `String/Skill.img`, `Item/ItemOption.img`, `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs`) |

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Combo counter / combo-command packet parity | `MapSimulator.PacketOwnedComboParity` now owns the simulator-side `OnIncComboResponse` bridge instead of leaving combo implied by the generic temporary-stat tray. The simulator decodes the packet-owned combo count from the combo inbox/chat transport, stores the owner count/timestamp, resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them on the same five-second alpha window, applies the client-matching fly-origin amplitude split across the least-significant digit run, and conditionally spawns the Aran combo-command attack/buff overlays with `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` first and WZ-backed named fallbacks for exports that only ship `ComboSmash`, `ComboDrain`, `ComboFenrir`, `ComboTempest`, and `ComboBarrier`. The row no longer depends on the generic `Combo` temporary-stat heuristics in `SkillManager`; it has a dedicated packet-owned HUD/effect owner, content load path, update lifetime, and draw path in `HaCreator/MapSimulator`. | If combo stays implied only by the temporary-stat tray, the backlog will overstate skill parity while missing the client's actual packet-owned combo counter, visible combo HUD/effect lifetime, and the extra combo-command visuals that hang off learned Aran skills. Tracking it explicitly keeps future work anchored to the real packet and draw owners instead of assuming the generic buff/cooldown surfaces already cover combo gameplay feedback. | packet-owned combo HUD/effect layer (`CUserLocal::OnPacket`, `CUserLocal::OnIncComboResponse`, `CUserLocal::DrawCombo`, `Effect/BasicEff.img/Combo`, `Effect/BasicEff.img/ComboCommand`) |

### 7. Shared skill-use animation-owner parity discovered by targeted `CAnimationDisplayer` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shared skill-use animation-owner parity | `MapSimulator.HandlePlayerSkillCast` still owns the local cast callback path, but the shared animation-displayer seam now also tracks a live owner position/facing relationship instead of freezing every `Effect_SkillUse` branch at the initial cast coordinates, and `RemoteUserActorPool.TryRegisterMeleeAfterImage` now emits the same owner through the remote official/Ariant melee packet presentation route with the packet action-speed byte mapped to the client `1000 * (speed + 10) / 16` delay-rate formula before `effect` plus indexed `effectN` branches are registered as one-time animations. The remaining gap is that the simulator still collapses the client layer graph into attached DX frame playback instead of preserving `LoadLayer`/`RegisterOneTimeAnimation`'s explicit overlay-parent, under-face plane, and auto-flip origin relationships per resolved layer, and several non-melee `CUser` callers that can reach `Effect_SkillUse` in the client are not yet routed through this shared owner. | Naming and partially implementing this owner keeps cast-effect parity from being overstated once gameplay logic works: the client still composes many visible cast layers through one shared animation manager, so parity work now has an explicit seam for branch enumeration, delay-rate override, owner-followed registration, and remote melee packet reuse, while the unresolved overlay/under-face layering and remaining `CUser` callsite coverage stay visible as the remaining parity risk. | shared skill-use animation layer (`CAnimationDisplayer::Effect_SkillUse`, `CAnimationDisplayer::LoadLayer`, `CAnimationDisplayer::RegisterOneTimeAnimation`) |

Buff icon plan:

1. Confirm asset sources and ordering:
   Use `UI/BuffIcon.img` for temporary-stat icon chrome and `Skill/*/icon` for per-skill icon sourcing where the client uses the skill's own icon, then validate row ordering and refresh cadence against `CUIStatusBar::Draw` and `CUserLocal::Update`.
2. Add a runtime-to-UI snapshot:
   Expose a small status-bar model from `SkillManager` that includes `skillId`, applied stat family, start time, duration, and refresh/replace semantics so the UI does not need to inspect buff internals directly.
3. Extend status-bar loading and drawing:
   Teach `UILoader` and `StatusBarUI` to load buff-slot art, render the active icon row in the client-aligned status-bar region, and reserve the interaction seam for later right-click cancel behavior.
4. Verify state synchronization:
   Check short-duration buffs, long-duration buffs, same-skill refresh, natural expiry, and `/job` cleanup so icon removal stays synchronized with the runtime and does not fight quick-slot cooldown overlays.


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, movement-coupled shoot families, and the shared `Effect_SkillUse` cast-animation owner rather than extending the generic cast path indefinitely.
3. Summon actor/runtime pass:
   Close the missing `CActionMan::LoadSummonedAction` loader seam together with the packet-owned `CSummonedPool` gaps and the `CSummoned::Update`-owned movement, touch-damage, Tesla-state, and position-sync gaps instead of only extending attack metadata and per-skill summon heuristics.
4. Clone-helper parity pass:
   Tighten the dedicated `CUser::PrepareActionLayer` / `CUser::Update` helper-layer seam for shadow-partner and mirror-image families instead of leaving clone behavior folded into generic buff or summon work.
5. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
6. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
7. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".



