# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::TryDoingMeleeAttack` at `0x91e780`
- `CUserLocal::TryDoingShootAttack` at `0x925a00`
- `CUserLocal::TryDoingMagicAttack` at `0x92a240`
- `CUserLocal::TryDoingBodyAttack` at `0x930710`
- `CUserLocal::DoActiveSkill_MesoExplosion` at `0x942200`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`
- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`
- `CAnimationDisplayer::Effect_SkillUse` at `0x459100`
- `CAnimationDisplayer::RegisterBulletAnimation` at `0x455410`
- `CAnimationDisplayer::RegisterMagicBulletAnimation` at `0x455570`
- `CAnimationDisplayer::RegisterHookingChainAnimation` at `0x45cbd0`
- `CAnimationDisplayer::RegisterChainlightningAnimation` at `0x45d1b0`
- `CAnimationDisplayer::RegisterTeslacoilAnimation` at `0x45d650`
- `CAnimationDisplayer::RegisterFootHoldAnimation` at `0x45a990`
- `CAnimationDisplayer::RegisterMotionBlurAnimation` at `0x45ad00`
- `CAnimationDisplayer::Effect_SkillPrepare` at `0x45b840`
- `CAnimationDisplayer::RegisterPrepareAnimation` at `0x45b420`
- `CAffectedAreaPool::GetInstance` at `0x52a480`
- `CAfterImageBullet::Update` at `0x477aa0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Player skill execution parity

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
`CUserLocal::Update` at `0x937330` orchestrates several of these paths and confirms that key-down gauge updates, queued follow-up attacks, prepared-skill execution, repeat-skill handling, teleport, rush, flying-rush, rocket-booster start/end handling, movement-coupled shoot branches, timer-driven cancellations, and status-bar feedback are coordinated in one local-player update loop rather than isolated one-off handlers. A second IDA pass also showed that summon parity splits across three owners rather than only the actor/runtime shell: `CSummoned::Update` drives summon actor motion, active/passive vec-ctrl updates, body-contact damage checks, assist-type dispatch, Tesla Coil state handling, self-destruct timing, and final world-position sync; `CSummonedPool::OnPacket` dispatches created/removed/move/attack/skill/hit packets and `CSummonedPool::OnCreated` ties each summon to its owning user, skill id, level data, and local-user registration path; and `CActionMan::{GetSummonedProp,LoadSummonedAction}` own the WZ-side summon UOL lookup, per-action frame build, delay/z metadata, reverse-playback duplication, and cached action-entry reuse before the runtime ever updates or draws that actor.

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
This row remains `Partial`. WZ was rechecked first on the same summon-owner surfaces and still matches this seam: `Skill/3510.img/skill/35101005` still mixes wrapper nodes with real summon branches (`cDoor`, `sDoor`, `oDoor`, `mDoor`, `eDoor`), `Skill/501.img/skill/5010005/summon` still exposes custom action branches (`piratebless`, `rushBoom`, `cannonSlam`, `noiseWave`, `jump`, `hit1`) beside `attack1`-`attack4`, `Skill/434.img/skill/4341006/summon` still remains effect/repeat-only while `info/requireSkill = 4331002` and root `req/4331002 = 30` remain visible, and mounted non-summon roots still publish deep link leaves like `Skill/3512.img/skill/35121006/info/requireSkill`, `Skill/3512.img/skill/35121006/psdSkill/35111009`, and `Skill/3510.img/skill/35100008/psdSkill/35101010`. The mounted `skill` category still exposes no authored `sSummonedUOL`/`*UOL*` property names, so this seam remains client-field-driven.

Client evidence stayed second: `CActionMan::GetSummonedProp` at `0x41b830` still resolves through `SKILLENTRY::GetSummonedUOL`, `SKILLENTRY::GetSummonedUOL` at `0x417f30` still returns only `this->sSummonedUOL`, and that same getter still feeds `CActionMan::LoadSummonedAction` plus `CSummoned::{_LoadEffect,_UpdateEffect}` effect/repeat base lookup. This pass keeps changes inside the existing loader seam and extends visible-leaf candidate intake again: direct summon-UOL candidate expansion still keeps fallback root-skill extraction even when normalized path tokens already exist, fallback numeric extraction still rejects `.img` image-name numbers so mounted image ids do not pollute candidate roots, resolved client-UOL value parsing still consumes fallback numeric/tokenized ids (with mounted-skill context), and unknown-leaf harvest now also accepts numeric leaf properties (`Int`/`Short`/`Long`) in the same bounded info/skill traversal path that already handled string/UOL leaves, so owner-like nested leaves such as `info/*/requireSkill*` can seed linked summon roots even when no string/UOL text is authored. Nested owner-like `info/*` branches now also parse linked skill ids from branch and child names (including empty subproperty leaves), so visible owner chains are still recoverable when values are not authored on the leaf itself. Shared linked-id parsing now also consumes direct `WzUOLProperty` values so UOL-authored owner leaves feed the same visible-link fallback chain as string leaves. `req`/`psdSkill` branch parsing now also admits non-numeric child value/UOL leaves when those values carry explicit linked skill ids or path tokens, while guarding against false level-to-context promotion from authored `req/* = level` values.

Focused coverage in `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs` now also locks nested owner-like numeric-leaf harvest and non-owner numeric-leaf rejection, nested owner-branch child-name extraction/rejection under `info/*`, direct `WzUOLProperty` linked-id parsing (both leaf and subproperty-child paths), and `req`/`psdSkill` non-numeric child value/UOL parsing with non-contextual level-value guard behavior, in addition to mixed-value direct candidate expansion (relative path + tokenized ids), `.img` image-id filtering, and tokenized resolved-leaf value parsing for `dummyOf`/`requireSkill`-style strings from prior updates. Remaining gap: the simulator still cannot mirror the client-owned authoring/population path for `SKILLENTRY::sSummonedUOL` itself; parity is now stronger across visible WZ links, resolved target chains, tokenized candidate text, owner-like unknown leaf values (string/UOL/numeric), nested owner-branch child names, and non-numeric `req`/`psdSkill` value leaves, but summons whose true owner appears only through that hidden client field still require a dedicated client-data follow-up before this row can be marked implemented.

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::Update` at `0x8fb8d0`

Notes:
This row remains `Partial`. WZ was rechecked first: `skill/433.img/skill/4331002` still has `action/0 = alert` plus `effect` and still has no `special`, while representative Shadow Partner roots (`skill/411.img/skill/4111002`, `skill/421.img/skill/4211008`, `skill/1411.img/skill/14111000`) still expose authored `special` helper branches. Client evidence stayed second: `CUser::GetMirrorSrcLayer` still returns the same five source layers (`UnderCharacter`, `OverCharacter`, `UnderFace`, `Face`, `OverFace`), `CUser::PrepareMirrorActionLayer` still recreates helper layers only when missing/flip-mismatched before reparenting under `m_pLayerUnderFace` and seeding rel-move from source-layer current time + `200 ms`, and `CUser::Update` still keeps helper layer objects and inserts current source-layer canvas instead of rebuilding helper objects per source change.

The simulator keeps the same ownership split in the existing seam (`SkillData` / `SkillManager` still treat Mirror Image `4331002` as mirror-helper owned while Shadow Partner stays on `special/*` helper-action catalogs), and this pass narrows mirror live-insert admission again without widening ownership. Existing behavior remains intact (prepared-layer recreation/reset on flip or scale-filter divergence, stale insert-metadata reset on helper-layer object replacement, source/overlay mismatch rejection for same-object live insert, same-helper-layer live insert re-admission on source-layer timeline advance/regression and on source identity/signature changes, missing-time re-entry, UnderFace overlay routing, hidden-state prepared-layer release, and prepared snapshot fallback). New seam tightening in this pass: same-helper-layer live insert now also re-admits when either recorded source-identity metadata channel is missing (`sourcePartsObjectId` or source-signature metadata) instead of only when both are missing, which better mirrors the client's update-side "insert current source canvas" behavior when cached metadata is incomplete. Mirror source-signature identity now also includes visibility-token owner-list object identity alongside existing visibility-owner channels (`VisibilityPriority`, ordered `VisibilityTokens`, and `SourcePart` object identity) in the existing `ComputeMirrorImageSourceLayerSignature` seam, so same-helper-layer live insert re-admission catches additional source refreshes that previously looked unchanged when token values stayed stable but ownership changed.

Focused regression coverage in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` now also locks same-helper-layer timeline-driven live-insert re-admission (advance/regression), stable no-change rejection when source identity and source-layer time are unchanged, source and overlay mismatch rejection priority, live-insert re-admission when either source-identity metadata channel is missing, and source-signature re-admission when visibility-owner channels change (priority/token owner identity and `SourcePart` object identity) in the same insert-metadata seam.

Remaining gap stays `Partial` and concentrated at native client object ownership: the simulator still does not own shared `IWzGr2DLayer` mirror objects, COM origin/current-time handles, full client layer-graph `InsertCanvas` object identity/lifetime semantics, full native source-layer current-time ownership on shared layer handles, or native avatar-scale filter handle ownership.

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::GetShadowPartnerProp` at `0x41ba60`
- `CActionMan::LoadShadowPartnerAction` at `0x423cf0`

Notes:
This row remains `Partial`. WZ was rechecked first on the same mounted loader source (`Character/00002000`) and still exposes concrete client-init piece rows with signed `delay`, optional `move`, and optional `flip` for additional raw-action families used by helper fallback. Beyond the previously locked set, this pass mounted and confirmed concrete piece rows for `doublefire`, `triplefire`, `doubleupper`, `eburster`, `screw`, `backspin`, `eorb`, `dragonstrike`, `airstrike`, `edrain`, `shot`, `fireburner`, `coolingeffect`, `rapidfire`, `cannon`, `torpedo`, `wave`, `blade`, `souldriver`, `firestrike`, `flamegear`, `stormbreak`, `shockwave`, `demolition`, `snatch`, `windspear`, `windshot`, `swingT2PoleArm`, `swingP1PoleArm`, `swingP2PoleArm`, and `combatStep`; mounted `ghostwalk`, `ghoststand`, `ghostjump`, `ghostproneStab`, `ghostladder`, `ghostrope`, `ghostfly`, and `ghostsit` still remain direct-frame helper rows under child `1/*` with authored frame delays. Client evidence stayed second: `CActionMan::LoadShadowPartnerAction` (`0x423cf0`) still consumes the helper action table initialized from `CActionMan::Init` (`0x41beb0`) before runtime playback (including raw codes `103..110` for the `ghost*` family), so fallback behavior remains loader-owned rather than runtime-owned.

This pass keeps changes in the same seam and narrows fallback parity again without widening runtime state: resolver-owned built-in piece plans now include those additional mounted client-init definitions, preserving client-shaped piece ordering, transform metadata, and signed-delay timing when mounted rows are unavailable; event-delay shaping remains on the same loader model (`negative-delay accumulation` first, otherwise `total-last`, synthetic zigzag tails excluded); and mounted fallback synthesis now enumerates the client-init fallback candidate surface directly (raw-action rows + promoted fallback-only helper rows + generic helper rows) before remapped plain-action fallback. The promoted fallback-only set now also admits the mounted `ghost*` helper family through the same loader seam so missing mounted rows still resolve through loader-owned alias fallback instead of falling out of fallback admission entirely.

Focused regression coverage in `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs` now also locks the client-init fallback-admission seam for `fake`, `flashBang`, `timeleap`, `owlDead`, `homing`, `recovery`, `backstep`, `holyshield`, `resurrection`, `dash`, `octopus`, `darksight`, `ghostwalk`, `ghoststand`, `ghostjump`, `ghostproneStab`, `ghostladder`, `ghostrope`, `ghostfly`, and `ghostsit`; piece-backed rows remain locked on synthesized signed-delay/event-delay output, while the promoted `ghost*` rows are now locked on remapped fallback synthesis output when mounted rows are absent.

Remaining gap is narrower but still real: additional hidden helper rows outside the promoted fallback set plus still-unrecovered `Character/00002000` piece rows remain, additional mounted action-table fields beyond modeled `action/frame/delay/flip/move/rotate/replay/event-delay` remain unconfirmed, and client-build/data-set differences in helper UOL/hidden-row ownership can still introduce extra loader-owned paths.

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another client-owned skill family that was not represented anywhere in the backlog text. `OnTimeBombAttack` is not just a generic damage callback: the decompile shows it decoding a skill id plus attack/action data, resolving the local skill level, calling `TryDoingMeleeAttack`, then applying hit-period/invincibility and impact side effects before forcing the avatar hit reaction. `OnVengeanceSkillApply` is also more specific than a plain temporary-stat refresh because it gates on skill id `3120010` and then calls `DoActiveSkill_MeleeAttack` through the local skill runtime, while `OnExJablinApply` arms the `m_bNextShootExJablin` flag for the next ranged-shot branch rather than behaving like a generic buff bit. That is a dedicated packet-owned skill-application seam between the broad cast/update families and the existing temporary-stat or helper rows.

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`

Notes:
The same targeted `CUserLocal::OnPacket` scan surfaced another local skill owner that the backlog still was not naming. `OnIncComboResponse` is very small but concrete: it decodes the server-authored combo count, stores it in `m_nCombo`, stamps `m_tLastSetCombo`, and immediately routes through `DrawCombo`. The `DrawCombo` decompile then shows that combo is not just another temporary-stat label. The client resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them alive on a five-second alpha window, applies fly-origin motion to the combo vectors, and conditionally spawns extra `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` layers when the corresponding Aran combo-command skills are learned. A repo search across `HaCreator/MapSimulator` still only surfaced the generic `Combo` temporary-stat labeling in `SkillManager` and did not surface any simulator-side combo packet owner, combo HUD/effect loader, or `ComboCommand` runtime, so this remains a real missing parity bucket rather than stale wording.

### 7. Shared skill-use animation-owner parity discovered by targeted `CAnimationDisplayer` scan

- `CAnimationDisplayer::Effect_SkillUse` at `0x459100`

Notes:
Status remains `Partial`. WZ was rechecked first on the same cast-owner surfaces and still supports the split used by this seam: shared cast branches remain authored on `effect`/`special`/`specialAffected`/`keydownend` rows (`Skill/434.img/skill/{4341003,4341007}/special`, `Skill/3212.img/skill/32121003/special`, `Skill/122.img/skill/1220006/{special,info/condition=damaged}`, `Skill/230.img/skill/2301002/specialAffected`, `Skill/532.img/skill/5320007/specialAffected`, `Skill/572.img/skill/5720005/specialAffected`, `Skill/3500.img/skill/35001001/keydownend`, `Skill/3510.img/skill/35101009/keydownend`, `Skill/3510.img/skill/35101004/effect`, `Skill/3511.img/skill/35111004/effect`, `Skill/3512.img/skill/{35121005,35121013}/effect`), while summon-style ownership remains split (`Skill/3510.img/skill/35101005` keeps `info/type=33` with `effect/*` but no `summon/*`; `Skill/311.img/skill/3111005`, `Skill/321.img/skill/3211005`, and `Skill/3512.img/skill/35121011` keep `info/type=33` plus concrete `summon/*` branches). Client evidence stayed second: `CUser::ShowSkillEffect` still keeps caller-owned `nActionSpeed`, `bLeft`, `nLast`, and `pPtOffset`; non-melee local callers (`CUserLocal::DoActiveSkill_Heal@0x93a830`, `...TownPortal@0x93be00`, `...SmokeShell@0x93cc30`) still pass `nActionSpeed=0` and `nLast=0x7FFFFFFF`; `CUserLocal::DoActiveSkill_Summon@0x93c0f0` still passes fixed `bLeft=0` with `nLast=0x7FFFFFFF` and null `pPtOffset`; and `CUserLocal::DoActiveSkill_SummonMonster@0x93eff0` still passes fixed `bLeft=0` with `nLast=-1` and caller-owned placement offset.

This pass keeps changes in the same seam and narrows fallback ownership again without widening runtime owners: local non-melee casts remain on the direct-owner registration path (`HandlePlayerSkillCast -> TryRegisterLocalSkillCastThroughClientShowSkillEffectDirectPath`) while explicit caller-shaped requests remain on `OnClientSkillEffectRequested -> TryRegisterAnimationDisplayerLocalSkillUseRequest`; inferred summon-monster fallback now requires concrete summon animation data (actual frames on summon/spawn/attack/hit/remove branches, including variant-frame coverage) instead of non-null wrapper metadata; and explicit summon-monster skill-id mapping now includes recovered rows `3111005`, `3211005`, and `35121011`, so those ids keep caller-owned `nLast=-1` and placement-offset shaping even when local metadata loading is sparse.

Focused coverage in `UnitTest_MapSimulator/SharedSkillUseAnimationOwnerParityTests.cs` now also locks explicit summon-monster id fallback (`3111005`, `3211005`, `35121011`) and concrete-branch metadata gating (empty type-33 summon wrappers no longer trigger inferred summon-monster fallback), alongside existing fixed-zero `bLeft`, mine owner shaping, and Monster Magnet move-action ownership assertions.

Remaining gap: explicit client skill-id-to-caller mapping for all `DoActiveSkill_SummonMonster` permutations is still incomplete beyond the recovered explicit ids above; per-caller placement-offset ownership beyond currently recovered ids remains approximated; overlay-parent and auto-flip ownership remain approximated outside the existing follow/fixed-facing shaping; and remote damaged-condition trigger/chance ownership still depends on decoded temporary-state hints rather than full `CUserRemote::OnHit` trigger ownership.

### 8. Bullet animation-owner parity discovered by follow-up `CAnimationDisplayer` scan

- `CAnimationDisplayer::RegisterBulletAnimation` at `0x455410`
- `CAnimationDisplayer::RegisterMagicBulletAnimation` at `0x455570`
- `CAfterImageBullet::Update` at `0x477aa0`

Notes:
The follow-up IDA scan also exposed a player-projectile presentation owner that was not named anywhere else in the backlog. `RegisterBulletAnimation` allocates a normal `CBullet` animation object with start/end times, source and destination points, target vector, z, bullet-effect UOL, weapon id, and bullet item id, then inserts it into the animation manager's bullet container. `RegisterMagicBulletAnimation` owns the parallel magic-ball path and only creates a bullet object when the resolved ball UOL is non-empty. `CAfterImageBullet::Update` is a deeper bullet subclass path: when afterimage is enabled it creates a child layer from the current bullet layer origin, relocates alpha/origin vectors, copies the source layer canvas into the afterimage layer, animates it, and registers repeat animation state rather than treating afterimage projectile trails as a generic skill effect. This means projectile parity is split one level below `SkillManager.SpawnProjectile`: gameplay/projectile collision can be correct while the client bullet animation owner for normal, magic, and afterimage projectile presentation is still missing.

### 9. Secondary skill animation-owner parity discovered by broad `CAnimationDisplayer` function scan

- `CAnimationDisplayer::RegisterHookingChainAnimation` at `0x45cbd0`
- `CAnimationDisplayer::RegisterChainlightningAnimation` at `0x45d1b0`
- `CAnimationDisplayer::RegisterTeslacoilAnimation` at `0x45d650`
- `CAnimationDisplayer::RegisterFootHoldAnimation` at `0x45a990`
- `CAnimationDisplayer::RegisterMotionBlurAnimation` at `0x45ad00`
- `CAnimationDisplayer::Effect_SkillPrepare` at `0x45b840`
- `CAnimationDisplayer::RegisterPrepareAnimation` at `0x45b420`

Notes:
Status remains `Partial`. WZ was rechecked first for the active-effect branch this row still owns: `Skill/112.img/skill/1121001/info/pullTarget` remains `1` with authored `mob` hook/chain canvases present, `Skill/222.img/skill/2221006/ball/{0,1,2}` remains present for chain-lightning projectile variants, `Skill/3511.img/skill/35111002/summon/attackTriangle` remains present for Tesla, and `Item/Cash/0501/05010044/effect` still publishes `spectrum=1`, `delay=600`, `interval=100`, and `alpha=224` on the eight-digit cash item node (with `follow` absent on this specific item path, so default `0` behavior still applies). Client evidence stayed second: `CUser::SetActiveEffectItem` (`0x8f5500`) still reads `spectrum/follow/delay/interval/alpha`, terminates the previous spectrum `AnimationState`, copies five avatar-layer COM handles into an array (`Face`, `OverFace`, `UnderFace`, `OverCharacter`, `UnderCharacter`), and re-registers motion blur through `CAnimationDisplayer::RegisterMotionBlurAnimation`; `RegisterMotionBlurAnimation` (`0x45ad00`) still stores overlay, layer array, delay, interval, alpha, update-time seed, and animation-state reference in `m_MotionBlur`.

This pass keeps all work in existing seams and tightens both local and remote owner approximation without widening runtime families: local packet-owned active-effect motion blur now preserves client-like per-snapshot avatar alignment by anchoring each sampled layer-stack frame to that sample's own assembled-frame offsets (`bounds.Left`, `bounds.Top - feetOffset`) instead of reusing only the registration-time anchor, while still tagging layer stacks in client handle-array order and drawing in stable avatar render order. Local owner capture keeps passive-snapshot-first source precedence and in-range fallback ordering, and now treats move-path duration tails as in-range sample windows rather than timestamp-only windows. Remote active-effect motion blur keeps the same five-layer snapshot model plus per-layer source metadata (`SourceLayer`, capture-array index), stable simulated layer-handle ids that persist across sampled snapshots for a single active-effect registration, and the same duration-tail-aware movement snapshot sample-time bounds for owner capture. Remote lifecycle ownership remains clear-on-replace/remove or missing-spectrum metadata; sampling still follows authored `interval`; retention/fade still follows authored `delay` and `alpha`; and follow-position/facing behavior remains intact. Local packet `220` routing, local owner clear/re-register semantics, and prepare/hook-chain/foothold/chain-lightning/Tesla secondary-owner seams remain unchanged on this row.

Focused regression coverage in `UnitTest_MapSimulator/ActiveEffectMotionBlurParityTests.cs` now also locks local sample-time owner capture resolution/fallback (including out-of-history fallback), passive-vs-recorded source precedence, packet-owned layer-handle map reuse vs owner-change regeneration, remote movement-snapshot owner capture/fallback, remote layer-handle map reuse, client-ordered five-layer draw-order mapping, linear delay-window alpha fade shaping, and duration-tail sample-window acceptance for both local and remote owner capture. Remaining gap: both local and remote motion-blur owners are still simulator-managed snapshots rather than literal client `IWzGr2DLayer` COM objects with native layer-handle identity and true native `AnimationState` lifetime ownership semantics across the real layer graph, so full native identity/lifetime parity remains approximated rather than 1:1 mirrored.

### 10. Local affected-area attack ownership discovered by broad IDA function scan

- `CAffectedAreaPool::GetInstance` at `0x52a480`
- `CUserLocal::TryDoingMeleeAttack` at `0x91e780`
- `CUserLocal::TryDoingShootAttack` at `0x925a00`
- `CUserLocal::TryDoingMagicAttack` at `0x92a240`
- `CUserLocal::TryDoingBodyAttack` at `0x930710`
- `CUserLocal::DoActiveSkill_MesoExplosion` at `0x942200`

Notes:
Broad IDA xref scan shows local attack owners call into the affected-area pool directly through `CAffectedAreaPool::GetInstance`, not only through packet-owned field handlers. This keeps affected-area parity split across two seams: local skill/attack spawning in the player-skill runtime and packet-owned remote area lifecycle in backlog 5.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Player skill execution parity

The simulator has a generic skill runtime. It does not yet mirror the client's named `CUserLocal` behavior families.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Generic skill casting | The simulator now loads the full player skill catalog from `Skill.wz`, learns active skills into the runtime, supports direct cast invocation from the skill window in addition to hotkey casting, keeps GM/SuperGM skill-book resolution compatible with both `900.img` and `910.img` layouts while focusing those jobs on their own job-book window instead of the global advancement catalog, evaluates Big Bang `common` formulas in IMG-mode data sets so skills like `Power Strike` are treated as castable attacks instead of passive placeholders, mirrors skill effect anchoring/hitbox fallback by facing direction for asymmetric melee effects, sanitizes unsupported SpriteFont glyphs in the skill window so WZ-backed names no longer crash row rendering, and resolves cast SFX from `Sound/Skill.img` so skill use now plays the corresponding `Use` or fallback attack sound through the simulator audio path | This closes the previous "can only use the current job's limited skill book" runtime/UI gap and keeps special-job skill tabs populated and executable across client data variants | `SkillManager.cs`, `SkillLoader.cs`, `SkillUIBigBang.cs`, `MapSimulator.cs`, `UIWindowLoader.cs` |
| Implemented | Buff stat application | Buff logic now applies `PAD`, `PDD`, `MAD`, `MDD`, `ACC`, `EVA`, `Speed`, and `Jump` directly to the player build and cleanly removes them when buffs expire or refresh | Active buff skills now change simulator stats and movement-relevant values instead of only a partial subset | `SkillManager.ApplyBuffStats` |
| Partial | Buff icon / temporary-stat UI parity | Active buffs still drive the WZ-backed status-bar tray with live duration countdowns sourced from `UI/BuffIcon.img`, client-style right-click cancellation still stays on the live buff list, and `UILoader` still normalizes the full `BuffIcon.img` icon surface while preserving known `UI/BuffIcon.img/buff/*` authored names/order. This pass tightened another non-`BuffIcon.img` transform ownership lane in the same status-bar seam. WZ was rechecked first: prior transform profiles remain (`Skill/3500.img/skill/35001002/common`, `Skill/522.img/skill/5221006/common`, `Skill/511.img/skill/5111005/common`, `Skill/512.img/skill/5121003/common`, `Skill/1511.img/skill/15111002/common`, `Skill/3512.img/skill/35120000/common`) with `PVPcommon` `PVPdamage` companions still present on those rows, the prop-first morph profile still remains (`Skill/510.img/skill/5101007/{common,PVPcommon}` keeps authored `prop` before `morph`), `String/Skill.img/5221006` still co-authors `Gun Booster`, `Maple Warrior`, and `Hero's Will`, and `String/Skill.img/15111002/desc` still co-authors `Knuckle Booster` and `Speed Infusion` while `Skill/1511.img/skill/15111002/PVPcommon` still keeps authored `PVPdamage`, `epdd`, `emdd`, `speed`, and `jump` in that order. Client evidence stayed second at the same owner seam (`CUIStatusBar::Draw@0x876cd0`): ownership remains in simulator status-bar resolver paths only. Implementation remains in that seam and now keeps transform-context ownership stable while trimming another parsed metadata-noise lane: beyond the prior `DebuffResistance` transform support admission, `ShouldSuppressCompoundFamilyTooltipDisplayName(...)` now also suppresses metadata-only `Booster` and `MapleWarrior` labels in vehicle-transform placeholder contexts, so mixed `PVPdamage`-first transform authored orders with direct `epdd/emdd/speed/jump` payloads no longer retain stray booster/maple-warrior tooltip families from co-authored text surfaces. Focused regression coverage in `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` now also locks both family owner and tooltip ordering parity for that mixed `PVPdamage` + `PDD` + `MDD` + `Speed` + `Jump` transform payload with parsed `Booster` and `MapleWarrior` metadata surfaces, alongside prior booster/`MapleWarrior`/`DebuffResistance` coverage. Remaining gap is narrower but still real: exact tray order/family ownership is now authored for known direct `BuffIcon.img` families, explicit no-direct families, checked contextual placeholders, checked transform direct/max-HP/max-MP contexts, checked transform direct/speed/jump contexts, checked transform mastery-first contexts, checked transform `PVPdamage`-first contexts, checked transform mastery/`PVPdamage` contexts with parsed booster metadata present, checked transform `PVPdamage` contexts with parsed `MapleWarrior` metadata present, checked transform `PVPdamage` contexts with parsed booster/`MapleWarrior`/`DebuffResistance` metadata present, and now checked transform `PVPdamage` + direct `speed/jump` contexts with parsed booster metadata present, and now checked transform `PVPdamage` + direct `speed/jump` contexts with parsed `MapleWarrior` metadata present, but some non-`BuffIcon.img` family mixes still appear to depend on unrecovered client/runtime grouping beyond `UI/BuffIcon.img`, recovered `SecondaryStat` order, and currently parsed skill metadata. | Buff-heavy jobs are visible on the HUD, can be cleared without waiting for expiry, and now keep transform-family ownership across hp-first, morph direct/speed/jump, mastery-first Mechanic, `PVPdamage`-first transform payloads, booster-noise variants, `MapleWarrior` metadata noise variants, booster+`MapleWarrior`+`DebuffResistance` metadata noise variants, and `PVPdamage`+direct speed/jump payloads with booster/maple-warrior metadata noise before icon-family resolution while keeping tooltip family ordering on the same transform owner seam for non-direct transform-context properties. Remaining risk stays concentrated on still-unrecovered client grouping for leftover non-direct family mixes once authored WZ order, recovered `SecondaryStat`, and current contextual mappings stop producing a unique owner. | `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`, `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` (`UI/BuffIcon.img/buff`, `Skill/3500.img/skill/35001002/{common,PVPcommon}`, `Skill/3512.img/skill/35120000/{common,PVPcommon}`, `Skill/522.img/skill/5221006/{common,PVPcommon}`, `Skill/511.img/skill/5111005/{common,PVPcommon}`, `Skill/512.img/skill/5121003/{common,PVPcommon}`, `Skill/1511.img/skill/15111002/{common,PVPcommon}`, `Skill/510.img/skill/5101007/{common,PVPcommon}`, `String/Skill.img/5221006/{desc,pdesc,h,ph}`, `String/Skill.img/15111002/desc`, `CUIStatusBar::Draw@0x876cd0`) |
| Partial | Melee / ranged / magic resolution | The runtime keeps the existing player-skill parity work for this seam: WZ `lt/rb` attack bounds stay intact, melee and magic target ordering stay split, no-ball spell and shoot families still route through the client-shaped `info/type` classifier instead of collapsing into melee defaults, direct no-projectile ranged skills still use their own fallback hitbox plus the `DoActiveSkill_ShootAttack` melee-first-vs-shoot fallback split, projectile families still honor `rectBasedOnTarget`, `multiTargeting`, `range` chaining, `ballDelay` / `ballDelayN`, authored ball-frame sweep bounds, effective `mobCount` caps from both skill and ball data, WZ `x` damage overrides and client `SKILLENTRY::AdjustDamageDecRate` tables, and the shared shoot-entry / ammo resolver still mirrors the current client-backed weapon-family, support-state, consume-count, USE/CASH ownership, and pellet-family gates. This run stayed in the same projectile/hit selector seam and tightened mounted fire-time body-rel-move fallback again without widening projectile ownership. WZ was rechecked first and still confirms the same authored geometry and mounted anchor surfaces this lane relies on: `Skill/310.img/skill/3101005/info/rectBasedOnTarget = 1`, `Skill/230.img/skill/2301005/info/{multiTargeting,rectBasedOnTarget} = 1`, `Skill/230.img/skill/2301005/common/{lt,rb}`, and mounted `map/navel` branches on `character/TamingMob/01932015/wildbeast/{0,5}/0/map/navel` and `character/TamingMob/01932030/wildbeast/{0,5}/0/map/navel` (both still `(1,-54)` on branch `0` and `(4,-51)` on branch `5`). Client evidence stayed second in the same lane: `CUserLocal::TryDoingSmoothingMovingShootAttack@0x92de70`, `CUserLocal::GetProperBulletPosition@0x907310`, `GetShootAttackPt0@0x9039a0`, `CSkill_HitAni::operator()@0x74fc50`, `CSkill_HitAni::GetSubHitAni@0x911a30`, `CSkill_HitAni::CreateForFlashRain@0x6ebef0`, constructor dispatch in `CSkill_HitAni::CSkill_HitAni@0x6eca00`, `CSkill_HitAni::CreateMultipleLayer@0x6ec7d0`, and `CSkill_HitAni::CreateFirst@0x6ebd80` remain the owner seams for fire-time positioning and hit selection, including `m_ptBodyRelMove.y` use in moving-shoot execution and the constructor-owned `CreateFirst(..., 2)` branch for `5101003`. Existing row behavior stays intact (moving-shoot fire-time origin refresh, `33121001` base-hit/sub-hit final-line split, no-clamp indexed selectors for client-owned hit lanes (`33121009`, `33111006`), first-target split exceptions, authored multi-branch indexed selection, first-target-only `CreateMultipleLayer(hit0)` for `15111004`/`5121007`, and the constructor-owned `CreateFirst(..., 2)` target window for `5101003`). Mounted body-rel-move fallback is now tighter in-place: `PlayerCharacter.TryGetCurrentBodyRelMoveY(...)` now resolves rendered mounted-frame `ClientBodyOriginMapPoint`/`navel` before any action-name guard, and rendered-frame reuse now also admits explicit requested-vehicle ownership when active mounted-state lookup is transiently empty but the resolved mount source still matches that requested vehicle id, so temporary blank `CurrentActionName` windows no longer drop to zero during these mount-state holes while still rejecting mismatched requested-vehicle ownership. Fire-time fallback mount resolution is now tighter in the same lane: `SkillManager.ResolveCurrentClientShootAttackVehicleId(...)` now prioritizes fallback-capable mounted-state ids when `CurrentActionName` is blank, so moving-shoot offset recovery no longer gets stuck on non-body-rel owner ids during blank-action windows. Implicit mount-source fallback is tighter in-place: `PlayerCharacter.ResolveMountedBodyRelMoveSourceTamingMobPart(...)` now checks transition/state-driven/client-owned/transform mount-owner parts before equipped mount fallback when no explicit vehicle id is requested, so fire-time body-rel-move recovery stays anchored to active mounted-owner surfaces during transient no-vehicle windows without widening requested-vehicle ownership matching. Focused coverage in `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` now also locks requested-vehicle rendered-frame reuse admission/rejection in mounted-state-missing windows (explicit source match admitted, mismatched source rejected, implicit vehicle id not widened). Remaining gap stays `Partial` and narrower below this simulator seam: residual cases where live client `m_ptBodyRelMove.y` can still diverge after deeper native layer/runtime ownership updates, post-launch choreography hidden behind native `CSkill_HitAni`/`CMob::AddDamageInfo` internals beyond the now-covered indexed-target/final-line-sub-hit/first-target-split/multiple-layer-first-target/`5101003`-two-target-CreateFirst ownership set, and later moving-shoot presentation nudges that depend on unrecovered per-hit timing fields or native layer ownership beyond recovered launch offsets, lane offsets, and skill delay tables. | This row matters because `CUserLocal` keeps separate melee, shoot, and magic execution lanes, and the remaining mismatch is now small enough that future work can stay focused on the last client-owned fire-time/post-launch presentation details instead of reopening projectile family resolution, target ordering, ammo ownership, or basic fallback projectile origin placement again. | `SkillManager.ProcessMeleeAttack`, `SkillManager.ProcessMagicAttack`, `SkillManager.ProcessNoProjectileRangedAttack`, `SkillManager.SpawnProjectile`, `SkillManager.CheckProjectileCollisions`, `SkillManager.ResolveProjectileAttackOrigin`, `SkillManager.ResolveDeferredExecutionAttackOriginOverride`, `SkillManager.RefreshQueuedProjectileFireTimeOrigin`, `SkillManager.ResolveProjectileImpactPresentationBaseTime(...)`, `SkillManager.ResolveClientTargetHitAnimation(...)`, `SkillManager.UsesClientMultipleLayerHitAnimation(...)`, `SkillManager.DrawProjectile`, `SkillManager.ResolveProjectileVisualItemId`, `SkillLoader.ParseSkillInfo`, `SkillLoader.CreateLevelData`, `SkillLoader.LoadPrefixedIndexedSkillAnimations(...)`, `SkillData.MultipleLayerTargetHitEffects`, `ClientShootAttackFamilyResolver`, `ClientShootAmmoResolver`, `SkillManager.ResolveCurrentClientShootAttackVehicleId(...)`, `PlayerCharacter.TryGetCurrentBodyRelMoveY(...)`, `PlayerCharacter.TryResolveCurrentRenderedMountedClientBodyRelMoveY(...)`, `PlayerCharacter.TryResolveRenderedMountedClientBodyRelMoveY(...)`, `PlayerCharacter.ResolveMountedBodyRelMoveSourceTamingMobPart(...)`, `PlayerCharacter.TryResolveMountedClientBodyRelMoveY(...)`, `PlayerCharacter.TryResolveCurrentMountedClientBodyRelMoveYFromRawFrames(...)`, `PlayerCharacter.ResolveClientBodyRelMoveY(...)`, `AvatarActionLayerCoordinator.ApplyMountedOriginRelocation(...)`, `AvatarActionLayerCoordinator.ResolveMountedOriginRelocation(...)`, `CharacterAssembler.GetPartAnimation(...)`, `Skill/310.img/skill/3101005/info/rectBasedOnTarget`, `Skill/230.img/skill/2301005/info/{multiTargeting,rectBasedOnTarget}`, `Skill/230.img/skill/2301005/common/{lt,rb}`, `character/TamingMob/01932015/wildbeast/{0,5}/0/map/navel`, `character/TamingMob/01932030/wildbeast/{0,5}/0/map/navel`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::DoActiveSkill_MagicAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::GetProperBulletPosition@0x907310`, `GetShootAttackPt0@0x9039a0`, `CSkill_HitAni::operator()@0x74fc50`, `CSkill_HitAni::GetSubHitAni@0x911a30`, `CSkill_HitAni::CSkill_HitAni@0x6eca00`, `CSkill_HitAni::CreateMultipleLayer@0x6ec7d0`, `CSkill_HitAni::CreateForFlashRain@0x6ebef0`, `is_position_up_skill_on_riding@0x9037c0`) |
| Partial | `DoActiveSkill_*` family parity | This pass stays in the same `SkillManager` seam and closes another recovered non-melee prepare/town-portal predicate slice without widening ownership. WZ was rechecked first on the same row-owned surfaces and remains aligned: `Skill/230.img/skill/2301002/action/0 = alert2`, `Skill/231.img/skill/2311002/action/0 = alert2`, `Skill/000.img/skill/0008001/action/0 = alert2`, `Skill/311.img/skill/3111002/summon`, `Skill/3511.img/skill/35111011/summon`, `Skill/3510.img/skill/35101009/action/0 = flamethrower2` with `keydown/*`, `Skill/3310.img/skill/33101005/info/type = 98`, and this pass's prepare weapon-root checks on `Skill/312.img/skill/3121004/weapon=45`, `Skill/322.img/skill/3221001/weapon=46`, `Skill/3310.img/skill/33101005/weapon=46`, `Skill/3312.img/skill/33121009/weapon=46`, `Skill/3500.img/skill/35001001/weapon=49`, `Skill/3510.img/skill/35101009/weapon=49`, `Skill/522.img/skill/5221004/weapon=49`, `Skill/1311.img/skill/13111002/weapon=45`, plus retained dual-blade rows `Skill/434.img/skill/{4341002,4341003}/{weapon=33,subWeapon=34}` and no-root rows (`Skill/520.img/skill/5201002`, `Skill/510.img/skill/5101004`, `Skill/1510.img/skill/15101003`) that still require client-side predicate ownership. Client evidence stayed second: `CUserLocal::DoActiveSkill_Heal@0x93a830`, `DoActiveSkill_TownPortal@0x93be00`, `DoActiveSkill_Summon@0x93c0f0`, `DoActiveSkill_SmokeShell@0x93cc30`, `DoActiveSkill_Prepare@0x941710`, and `is_prepare_bomb_skill@0x6ee270`; decoded prepare constants remain (`0x4F5C6A=5201002`, `0x4DD5CC=5101004`, `0xE66C4B=15101003`, `0x423D0A=4341002`, `0x4FAA8C=5221004`, `0xC80EDA=13111002`). Family split and shared non-prepare `ShowSkillEffect(..., nActionSpeed=0, nLast=0x7FFFFFFF)` behavior remain unchanged; lane request cadence remains caller-owned (heal/summon/prepare ~300 ms, town-portal ~200 ms, smoke-shell ~1000 ms). Implementation now mirrors additional recovered gates in-place: lane precedence and lane-scoped throttle ownership remain; `ResolveClientDoActiveSkillFamilyStateRestrictionMessageCore(...)` keeps the previously mirrored heal/town-portal/smoke-shell/prepare/summon state gates; prepare restriction ownership now keeps skill-specific weapon/ammo rules using client StringPool ids (`0x1127`, `0x0B4C`, `0x0B4F`, `0x0B50`, `0x0B49`) and no longer over-admits shooter-family fallback on recovered exact-weapon prepare rows. Recovered exact `weapon` rows now gate directly in the prepare predicate table (`3121004=>45`, `3221001=>46`, `33101005=>46`, `33121009=>46`, `35001001=>49`, `35101009=>49`, `5221004=>49`, `13111002=>45`, `14111006=>47`), dual-blade rows still require dagger+katara (`33/34`), no-root recovered rows remain client-owned (`5101004`/`15101003=>39|48`, `5201002=>49`), recovered ammo-message rows still include `5221004` and `13111002`, and soul-arrow + mechanic flamethrower ammo exceptions remain preserved. Silent non-message rejects remain aggregated in the same seam: town-portal silent reject now mirrors the recovered vec-ctrl pre-send gate (`!foothold` and ladder/rope-like state) plus blocked map category `((mapId / 1,000,000) % 100) == 9`, `35101009` prerequisite reject when `35001001` is not learned, prepare cool-end silent subset, and prepare rush-family silent subset (`4341002`, `5101004`, `15101003`) while preserving summon `(m_nMoveAction & ~1) == 18` reject. Prepare-lane fallback ownership remains explicit for recovered prepare rows (`35001001`, `35101009`, `33101005`, `4341002`, `4341003`, `5201002`, `5101004`, `14111006`, `15101003`, `5221004`, `13111002`, `4211001`). `TryExecuteClientSkillBranch(...)` still keeps non-melee families as intentional fallthrough to shared movement/buff/summon/attack seams. Focused coverage in `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` now also locks town-portal silent no-message foothold/ladder/map-category gating and the non-message foothold branch split from message-only field-limit/portal-overlap, plus recovered exact-weapon prepare rows and retained no-root client-owned rows, in addition to existing ammo-message membership, cooldown/rush membership, and metadata-independent prepare-lane fallback coverage. Earlier row landings remain preserved (helper-only suppression, invincible-zone ownership limited to `4221006`/`32121006`, Beholder follow-up routing, GM/SuperGM ownership, meso in-box consume ordering, aura recast stacking, moving-shoot action gate + post-v95 fallback). Remaining gap is narrower and still row-scoped: town-portal full reject matrix is still incomplete beyond currently mirrored field-limit/portal-overlap plus silent vec-ctrl foothold/ladder and map-category branches; prepare full branch table still has uncovered predicates/messages (including remaining one-time/preparing occupancy nuances and exact `m_rush.nSkillID/tCoolTimeEnd` native gating semantics beyond current cooldown-seam approximation); and `DoActiveSkill_Summon` special argument payload permutations (notably Tesla-coil count/target/id variants) plus deeper native packet/layer choreography remain incomplete. | The simulator keeps explicit non-melee `DoActiveSkill_*` caller-family ownership and now mirrors recovered prepare exact-weapon predicate rows (WZ-root + client-only no-root rows) in the same seam, further concentrating remaining mismatch in unrecovered branch-table predicates and deeper native choreography rather than lane ownership/cadence/state wiring. | `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `HaCreator/MapSimulator/Character/Skills/SkillData.cs`, `HaCreator/MapSimulator/Character/Skills/SkillLoader.cs`, `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` (`CUserLocal::DoActiveSkill_Heal@0x93a830`, `CUserLocal::DoActiveSkill_TownPortal@0x93be00`, `CUserLocal::DoActiveSkill_Summon@0x93c0f0`, `CUserLocal::DoActiveSkill_SmokeShell@0x93cc30`, `CUserLocal::DoActiveSkill_Prepare@0x941710`, `is_prepare_bomb_skill@0x6ee270`, `CUser::ShowSkillEffect`, `CUserLocal::TryDoingTeleport`, `CUserLocal::TryDoingRush`, `CUserLocal::TryDoingFlyingRush`, `CUserLocal::DoActiveSkill_MesoExplosion`, `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo`, `CPortalList::FindPortal`, `Skill/230.img/skill/2301002`, `Skill/231.img/skill/2311002`, `Skill/000.img/skill/0008001`, `Skill/311.img/skill/3111002`, `Skill/3511.img/skill/35111011`, `Skill/3310.img/skill/33101005`, `Skill/3510.img/skill/35101009`, `Skill/312.img/skill/3121004`, `Skill/322.img/skill/3221001`, `Skill/3312.img/skill/33121009`, `Skill/3500.img/skill/35001001`, `Skill/522.img/skill/5221004`, `Skill/1311.img/skill/13111002`, `Skill/520.img/skill/5201002`, `Skill/1411.img/skill/14111006`, `Skill/510.img/skill/5101004`, `Skill/1510.img/skill/15101003`, `Skill/434.img/skill/{4341002,4341003}`, `Skill/422.img/skill/4221006`, `Skill/3212.img/skill/32121006`, `Skill/132.img/skill/{1320011,1321007}`) |
| Partial | Shadow Partner render/action parity | Shadow Partner remains on the dedicated helper seam (not a generic buff ornament) with WZ-backed `special/*` catalogs for `4111002`, `4211008`, and `14111000`, helper-first action resolution before plain raw-action fallback, helper follow-up timing from first-frame / `ClientEventDelayMs`, shared blocking hold for attack plus `create*`, local/remote lane offset interpolation (`+/-30`, back-lane `+50`, `200 ms`), and under-face insertion ownership in both local and remote draw paths. WZ was rechecked first and still matches the same anchors, including authored `create*` timing on `Character/00002000.img/create2/*/delay = 120`, `Character/00002000.img/chargeBlow/{0,1,2}` (`stabO1/0/-300`, `stabO1/1/420`, `alert/0/90`), `Character/00002000.img/rain/*` signed helper-piece delays, and explicitly revalidated legacy helper create rows `Character/00002000.img/create0/{0,1}` (`shoot2/2/450`, `shoot2/1/450`) plus `Character/00002000.img/create1/{0,1,2,3}` (`shoot2/2/180`, `shoot2/0/270`, `shoot2/2/180`, `shoot2/0/270`). Client evidence stayed second (`CActionMan::Init@0x41beb0`, `CActionMan::LoadShadowPartnerAction@0x423cf0`). This pass keeps ownership in the same shared helper resolver seam used by both local and remote lanes and tightens under-face ordering parity again without widening runtime state: resolver-owned built-in piece plans still cover legacy `create0`/`create1` fallback and shared create gating/hold behavior remains unchanged (`StartsWith("create")` blocking ownership with attack retry gating after create), while remote under-face insertion now resolves through the same render-layer insertion seam used locally (`PlayerCharacter.GetAvatarRenderLayerInsertionIndices(...)` + `ResolveMirrorImageOverlayInsertionIndex(...)`) with legacy part-type fallback only when render-layer metadata is unavailable. Existing shared attack-observation admission (`state == Attacking` or attack action alias), raw-action observation-refresh admission, catalog-fallback blocking-hold behavior, and looped/one-shot authored-alpha terminal clamp behavior remain intact. Focused coverage in `UnitTest_MapSimulator/ShadowPartnerAttackObservationGateParityTests.cs` now also locks remote under-face insertion seam preference/fallback behavior alongside existing gate/retry/refresh/hold, legacy create fallback selection (`create1` then `create0`), create blocking-hold lifetime, and alpha-clamp assertions. Remaining gap is narrower but still open: client-only non-ghost helper rows absent from the `special/*` helper-action surface remain unrecovered, and exact ordering/lifetime relative to every non-mirror under-face overlay still approximates native layer-object choreography beyond the now-shared insertion-index seam. | This keeps Shadow Partner parity concentrated in the existing helper seam while reducing another local/remote under-face ordering divergence candidate without widening ownership. Unresolved mismatch is increasingly limited to unrecovered client-only helper rows plus hidden native layer-object ordering/lifetime semantics. | `HaCreator/MapSimulator/Character/ShadowPartnerClientActionResolver.cs`, `HaCreator/MapSimulator/Character/PlayerCharacter.cs`, `HaCreator/MapSimulator/Pools/RemoteUserActorPool.cs`, `UnitTest_MapSimulator/ShadowPartnerAttackObservationGateParityTests.cs` (`Character/00002000.img/{create0,create1,create2,chargeBlow,rain}*`, `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, `Skill/1411.img/skill/14111000/special/*`, `CActionMan::Init@0x41beb0`, `CActionMan::LoadShadowPartnerAction@0x423cf0`) |
| Partial | Repeat-skill sustain families | Updated in this pass: WZ was rechecked first and still publishes SG-88 as `Skill/3512.img/skill/35121003` with `info/type = 33`, `info/minionAttack = normal`, `info/specialGage = 1`, and `summon/attack1`, while tank-siege still publishes `Skill/3512.img/skill/35121013` with `info/fixedState = 1`, `info/onlyNormalAttack = 1`, `common/time = "5"`, and `effect/*/delay = 30/60/30/60`; `Skill/3511.img/skill/35111004/common/time = "5"` still matches the siege timer surface. Client evidence stayed second: `CUserLocal::TryDoingRepeatSkill` (`0x93d400`) still owns SG-88/tank-siege sustain admission, `CUserLocal::SendSkillEffectRequest` (`0x93aeb0`) still emits opcode `71` (`COutPacket(71)` + `Encode4(skillId)` + `Encode1(level)` + `Encode1(sendLocal)`), tank-siege mode-end still routes through `SendSkillEffectRequest(35120013, learnedLevel(35121013), 1)` after the 5s timer before repeat ownership returns to `35121005`, and `CUserLocal::DoActiveSkill_Summon` (`0x93c0f0`) still writes SG-88 first-use opcode `103` with `(requestTime, skillId, skillLevel, x, y, m_nMoveAction & 1, vecCtrl-owner byte)`. The SG-88 first-use transport seam remains on the same owner path (`SkillManager` -> `PacketOwnedMechanicRepeatSkillRuntime` -> `MapSimulator` authority transport), and this pass tightens capture-validation further without widening gameplay ownership: mismatch-pair intake now accepts dashed pair tokens and decimal byte deltas (while normalizing to canonical `byteN:0x??->0x??` form), replay decode-detail intake continues to resolve vec-ctrl mismatch classes (`highBitsOnly`, `lowBitChanged`, `zeroToNonZero`, `nonZeroToZero`) from dashed/shorthand and JSON payloads, replay classification now promotes vec-ctrl-only deltas into explicit classes (`mismatch:vecCtrlHighBitsOnly`, `mismatch:vecCtrlLowBitChanged`, `mismatch:vecCtrlZeroToNonZero`, `mismatch:vecCtrlNonZeroToZero`) with mismatch-pair evidence first and decode-detail fallback second, and replay-matrix output continues carrying `vecCtrlMismatch` histograms beside `moveActionMismatch` across source/state/equivalence/core-equivalence rows while preserving `sourceRows`, `officialSourceRows`, and `officialCaptureParity` split reporting. Prior capture hardening remains intact (JSON whitespace mismatch-field lists, explicit nested-object mismatch/matched/status gating, slash/bracket field-path normalization, shorthand alias mapping for request/position/move/vec fields, and false-like assignment filtering). Focused coverage in `UnitTest_MapSimulator/RepeatSkillSustainPacketTransportParityTests.cs` now also locks mismatch-pair decimal/dashed intake normalization plus vec-ctrl expanded replay-class promotion for both mismatch-pair and decode-detail fallback paths, in addition to earlier singular-label, dashed/shorthand, boolean-map, whitespace-list, nested-object signal gating, and decode-detail backfill assertions. The row remains partial because official captured SG-88 first-use traffic is still required to confirm exact vec-ctrl-byte semantics across broader live-state coverage, to verify whether move-action mismatches appear in real traffic beyond replay noise, and to validate the expanded parity-class/equivalence behavior against larger real-session capture sets. | Mechanic repeat sustain still mirrors recovered opcode-`71` effect branches, keeps tank-siege return-owner timing, and sends SG-88 first-use through the existing transport seam with client-shaped opcode-`103` field order plus raw-action low-bit ownership; this pass further hardens replay mismatch analytics with canonicalized mismatch-pair intake and explicit vec-ctrl-only replay classes while keeping source-separated official-session parity reporting and state/equivalence/core-equivalence normalization on observed opcode-`103` captures. Remaining risk stays concentrated on capture-backed confirmation of SG-88 first-use vec-ctrl and move-action semantics and broader real-session replay grouping behavior. | `SkillManager.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedSummonedParity.cs`, `Managers/PacketOwnedMechanicRepeatSkillRuntime.cs`, `Managers/SummonedOfficialSessionBridgeManager.cs`, `Physics/CMovePathClientPacketCodec.cs`, `Physics/CVecCtrl.cs`, `UnitTest_MapSimulator/RepeatSkillSustainPacketTransportParityTests.cs` (`CUserLocal::TryDoingRepeatSkill`, `CUserLocal::DoActiveSkill`, `CUserLocal::DoActiveSkill_Summon`, `CUserLocal::SendSkillEffectRequest`, `CMovePath::Encode`, `Skill/3512.img/skill/35121003/{info,summon}`, `Skill/3512.img/skill/35121013/{common,info,effect}`, `Skill/3511.img/skill/35111004/common`) |
| Partial | Bound-jump, rocket-booster end, and smoothing moving-shoot branches | This pass stays in the same moving-shoot / Rocket Booster end / bound-jump seams and closes one more recovered anti-repeat ownership detail while keeping prior sparse-metadata, touchdown, and constrained type-40 behavior. WZ was rechecked first in `E:\MapleStory\WzExport\v95_2`: moving-shoot and Rocket Booster authoring remains unchanged (`Skill/3500.img/skill/35001001/action/0 = flamethrower`, `Skill/3510.img/skill/35101009/action/0 = flamethrower2`, `Skill/3510.img/skill/35101004/action/0 = rbooster_pre` with `info/type=10`, `info/casterMove=1`, `info/landingEffect=areaAttack`), anti-repeat bypass row `33121009` still publishes `info/type=2` with `info/movingAttack=1` and `info/rapidAttack=1`, constrained type-40 bound-jump rows remain on the same authored profile set, and rechecked `avaliableInJumpingState` rows outside that set (`Skill/2311.img/skill/23111001`, `Skill/2311.img/skill/23111003`, `Skill/433.img/skill/4331005`) still publish non-type-40 metadata (`info/type=1`). Client evidence stayed second: `CUserLocal::TryDoingSmoothingMovingShootAttack@0x92de70` still routes delayed moving-shoot through `CAntiRepeat::TryRepeat@0x9032b0` with explicit `33121009` short-circuit bypass, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare@0x912f90` still keeps one-time-action gating before arming moving-shot state, `CUserLocal::TryDoingRocketBoosterEnd@0x93d2d0` still gates touchdown progression on one-time-action completion before clearing `m_nRocketBoosterVY` and foothold-checked landing attack with a `500 ms` clear/effect window, and `CUserLocal::DoActiveSkill_BoundJump@0x93fdf0` still keeps the recovered direct-owner id set (`11101005`, `33001002`, `4111006`, `4211009`, `4321003`, `14101004`, `35101004`) with shared non-direct impact lanes. Implementation keeps constrained type-40 bound-jump ownership centralized in `BoundJumpParityProfile` across impact/state-gate seams and keeps sparse moving-shoot ownership hardening in `SkillManager` (`35001001`, `35101009`, `33121009` smoothing ownership; `35001001`, `35101009` rapid count-limit ownership), and now also mirrors two recovered local-owner rules: deferred execution no longer records carried anti-repeat state for `33121009`, matching the client short-circuit path that bypasses `CAntiRepeat::TryRepeat` for that row, and Rocket Booster touchdown progression no longer incurs an extra simulator-only post-gate tick before foothold-gated landing attack attempt once one-time-action gating has released. Focused source coverage in `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` now also locks deferred anti-repeat state tracking rejection for `33121009` and continued tracking for `35001001` / `35101009`, plus Rocket Booster landing-attempt admission on one-time-action gate release and continued deferral while one-time action remains active, in addition to prior moving-shoot bypass/threshold helpers, sparse metadata ownership checks, Rocket Booster touchdown/clear helpers, direct-owner airborne-vs-grounded ownership, constrained type-40 profile ownership, and shared-impact fallback coverage. Remaining gap stays row-scoped but narrower: hidden `CAntiRepeat` construction/field-initialization ownership outside the deferred moving-shoot seam, packet/server-owned Rocket Booster end choreography beyond the local landing-clear path, and still-unrecovered bound-jump special-case owners outside the recovered direct-owner switch set plus constrained checked type-40 profile rows. | Movement-coupled attack skills remain on client-owned moving-shoot anti-repeat and prepare one-time-action ownership, Rocket Booster local landing-clear timing/order, and constrained bound-jump ownership instead of simulator-only fallbacks. This pass preserves the existing parity surface and narrows mismatch by aligning both deferred anti-repeat carry-state behavior for `33121009` and Rocket Booster touchdown post-gate timing with recovered client ownership; remaining mismatch is concentrated in hidden anti-repeat initialization ownership, packet/server Rocket Booster choreography, and unrecovered bound-jump special-case owner branches outside recovered direct ids plus the constrained checked type-40 profile set. | `SkillManager.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `BoundJumpParityProfile.cs`, `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` (`CUserLocal::TryDoingSmoothingMovingShootAttack@0x92de70`, `CAntiRepeat::TryRepeat@0x9032b0`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare@0x912f90`, `CUserLocal::TryDoingRocketBoosterEnd@0x93d2d0`, `CUserLocal::DoActiveSkill_BoundJump@0x93fdf0`, `Skill/3500.img/skill/35001001/info/{type,casterMove,rapidAttack}`, `Skill/3510.img/skill/35101009/info/{type,casterMove,rapidAttack}`, `Skill/3312.img/skill/33121009/info/{type,movingAttack,rapidAttack}`, `Skill/3510.img/skill/35101004/info/{type,casterMove,landingEffect}`, `Skill/2311.img/skill/{23111001,23111003}/info/{type,avaliableInJumpingState}`, `Skill/433.img/skill/4331005/info/{type,avaliableInJumpingState}`, `Skill/1310.img/skill/13101004/info`, `Skill/3300.img/skill/33001002/info`, `Skill/1410.img/skill/14101004/info`, `Skill/{000,001,1000,2000,2001,2002,3000,3001,5000}.img/skill/*1098/info`, `Skill/310.img/skill/3101003/info`, `Skill/320.img/skill/3201003/info`, `Skill/5100.img/skill/51001003/info`, `Skill/2300.img/skill/23001002/info`, `Skill/2400.img/skill/24001002/info`, `Skill/3001.img/skill/{30010183,30010184,30010186}/info`, `Skill/508.img/skill/5081003/info`) |
| Implemented | Key-down / charge skills | This row stays confined to the same prepared-skill owners: `SkillManager`, `PreparedSkillHudRules`, `MapSimulator.RemoteUsers`, and `RemoteUserActorPool`. WZ was rechecked first in the mounted export: Piercing (`Skill/322.img/skill/3221001`) still publishes the authored startup split with `prepare/time = 1200`, `action/0 = piercing`, and the separate client gauge cap of `900 ms`; Monkey Wave (`Skill/531.img/skill/5311002`) still publishes `prepare/time = 1080`, `prepare/action = noiseWave_pre`, `keydown/action = noiseWave_ing`, and `action/0 = noiseWave`; the Big Bang trio, Evan breath pair, Dual Blade charge pair, Screw Punch, Shark Wave, Poison Bomb, Grenade, and Wild Hunter Swallow still publish zero-startup prepare surfaces such as `prepare/time = 0` with authored prepare actions, so those families belong on the immediate hold/release lane instead of a simulator-owned pseudo-startup. Client evidence stayed second: `get_max_gauge_time` (`0x50ab00`) still hard-caps the recovered branch table at `900 ms` for Piercing and Swallow, `1080 ms` through the Monkey Wave authored window, `600 ms` for Final Cut, `1200 ms` for Monster Bomb, `500 ms` for Evan breath, `1000 ms` for the Big Bang trio / Screw Punch / Shark Wave / Poison Bomb / Grenade, and the default `2000 ms` keydown fallback; `CUserLocal::OnKeyDownSkillEnd` (`0x9301b0`) still clamps elapsed charge time to the shared `30 ms` minimum, caps it through `get_max_gauge_time`, routes release through magic / shoot / melee / HP-cost / skill-specific branches, keeps Final Cut HP cost as `elapsed * nX / get_max_gauge_time` before comparing that percentage cost against current HP, and still makes `CUserLocal::TryDoingSwallowAbsorb` (`0x93af50`) reject undercharged Swallow releases until the elapsed hold reaches the full `900 ms` gauge cap. The runtime seams now cover the full scoped row: `SkillManager.ResolvePreparedSkillStartupDuration` uses authored WZ prepare time only when a release-owned family really has a nonzero startup window, `BeginPreparedSkill` enters hold immediately for zero-startup keydown families, `PreparedSkillHudRules.ResolvePreparedGaugeDuration` and `ResolveReleaseChargeElapsedMs` prefer the shared HUD or `get_max_gauge_time` charge window over longer authored prepare splits, the release damage-scale and Final Cut HP-cost helpers stay on the shared linear undercharge seam, `PreparedSkillHudRules.CanExecuteReleasePayload` keeps Wild Hunter Swallow on the client full-gauge-only release gate, `SkillManager.ReleasePreparedSkill` routes failed release-owned keydown attempts back through the shared cancel-request seam instead of silently dropping them, `PreparedSkillHudRules.ResolveRemotePreparedSkillPhases` preserves both zero-startup auto-hold and authored startup-then-hold packet phases, `RemoteUserActorPool.TryResolvePreparedSkillPhase` preserves both remote phase shapes across prepare and hold time, and `RemoteUserActorPool.TryApplyThrowGrenade` clears the matching remote prepared Grenade state on the same release seam as the official grenade packet instead of leaving the HUD bar latched after release. Focused coverage in `UnitTest_MapSimulator/PreparedSkillChargeStartupParityTests.cs` now covers the authored startup seam across the Big Bang trio, Piercing, Screw Punch, Grenade, Monkey Wave, Poison Bomb, Shark Wave, Evan breath, Dual Blade charge pair, and Wild Hunter Swallow; local zero-startup immediate hold; shared client minimum/cap release elapsed rules; the Swallow full-gauge release gate; failed-release cancel routing for the local Swallow branch; shared linear undercharge scaling where the client evidence supports it; the gauge-window-vs-prepare-window release scaling seam; Final Cut scaled HP-cost / restriction behavior; remote zero-startup phase resolution and persistence; remote authored startup-to-hold phase resolution and transition; remote release-owner recognition for the zero-startup client charge table; and remote grenade prepared-state teardown. | The scoped key-down / charge parity row is now implemented inside the existing prepared-skill seams. Remaining client-owned concerns such as raw scan-code ownership, broader exclusive-request policy, or unrelated packet choreography stay outside this row rather than as open gaps in the prepared startup/gauge/release behavior covered here. | `PreparedSkillHudRules.cs`, `SkillManager.cs`, `MapSimulator.RemoteUsers.cs`, `PlayerInput.cs`, `PlayerManager.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `UnitTest_MapSimulator/PreparedSkillChargeStartupParityTests.cs` (`Skill/{212,222,232}.img/skill/*/info/chargingSkill`, `Skill/322.img/skill/3221001/{prepare/time,prepare/action,keydown,action/0,info}`, `Skill/510.img/skill/5101004/prepare`, `Skill/520.img/skill/5201002/prepare`, `Skill/531.img/skill/5311002/{prepare,keydown,action}`, `Skill/1411.img/skill/14111006/prepare`, `Skill/1510.img/skill/15101003/prepare`, `Skill/2212.img/skill/22121000/{prepare,common,info/chargingSkill}`, `Skill/2215.img/skill/22151001/{prepare,common,info/chargingSkill}`, `Skill/434.img/skill/{4341002,4341003}/{prepare,action,info/chargingSkill}`, `Skill/3310.img/skill/33101005/prepare`, `get_max_gauge_time`, `is_keydown_skill`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::OnKeyDownSkillEnd`, `CUserLocal::TryDoingSwallowAbsorb`, `CUserLocal::DrawKeyDownBar`, `CDragon::DrawKeyDownBar`) |
| Partial | Swallow, mine, cyclone, and sit-down healing branches | This pass stays in the same swallow, mine, cyclone, and sit-down-healing seams and tightens swallow absorb packet-order ownership without widening classification or runtime owners. WZ was rechecked first on the same anchors: `Skill/3310.img/skill/33101005/prepare/action=swallow_loop`, `Skill/3310.img/skill/33101005/info/dummyOf=33101006&&33101007`, swallow flags only on `Skill/3310.img/skill/33101006/info/swallow` and `Skill/3310.img/skill/33101007/info/swallow`, no swallow flag on unrelated dummy-linked families (`Skill/3200.img/skill/32001001/info/swallow`, `Skill/3211.img/skill/32111005/info/swallow`, `Skill/3510.img/skill/35100008/info/swallow` absent), and Healing Robot authored heal range at `Skill/3511.img/skill/35111011/summon/heal/info/range` (`lt=-125,-100`, `rb=125,0`). Client evidence stayed second in the same seams: `CUserLocal::TryDoingSwallowMobWriggle@0x93f500`, `TryDoingMine@0x907d70`, `TryDoingCyclone@0x932d60`, `TryDoingSitdownHealing@0x903d60`, `CUserPool::DoHealNearHealingRobot@0x94bb20`, and `CSummoned::TryDoingHealingRobot@0x74b3e0`. `SkillLoader` still anchors swallow-family classification to authored follow-up swallow flags and still avoids broad `ClientInfoType=98` fallback expansion; mine, cyclone, and sit-down-healing gates stay in their landed client-owned branches. Swallow runtime packet outcome buffering now keeps bounded FIFO arrival order (max 8) for repeated same-seam `(skillId,targetMobId)` outcomes instead of replacing older entries, consume order stays first-in-first-out once the matching swallow seam arms, and non-matching swallow-family outcomes now remain buffered even while another swallow state is active. Focused coverage in `WildHunterSwallowAbsorbOutcomeBufferParityTests` and `WildHunterSwallowAbsorbOutcomeParityTests` now asserts repeated same-seam success/failure outcomes are consumed in arrival order and swallow-family mismatches stay buffer-eligible while armed. | Remaining mismatch is narrower and still row-scoped: exact one-for-one swallow packet choreography across broader swallow-family depth beyond current Wild Hunter-visible linkage is still incomplete, and full Healing Robot request and packet choreography beyond current local poll order, action-13 encoding, authored range ownership, and packet-owned suspend-window seams remains incomplete. | `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`; `HaCreator/MapSimulator/Character/Skills/WildHunterSwallowAbsorbOutcomeBuffer.cs`; `HaCreator/MapSimulator/Character/Skills/SkillLoader.cs`; `HaCreator/MapSimulator/Character/Skills/SummonRuntimeRules.cs`; `HaCreator/MapSimulator/Pools/SummonedPool.cs`; `UnitTest_MapSimulator/WildHunterSwallowAbsorbOutcomeBufferParityTests.cs`; `UnitTest_MapSimulator/WildHunterSwallowAbsorbOutcomeParityTests.cs` (`CUserLocal::TryDoingSwallowMobWriggle@0x93f500`, `CUserLocal::TryDoingMine@0x907d70`, `CUserLocal::TryDoingCyclone@0x932d60`, `CUserLocal::TryDoingSitdownHealing@0x903d60`, `CUserPool::DoHealNearHealingRobot@0x94bb20`, `CSummoned::TryDoingHealingRobot@0x74b3e0`, `Skill/3310.img/skill/33101005/{prepare/action,info/dummyOf}`, `Skill/3310.img/skill/3310100{6,7}/info/swallow`, `Skill/3200.img/skill/32001001/info/swallow`, `Skill/3211.img/skill/32111005/info/swallow`, `Skill/3510.img/skill/35100008/info/swallow`, `Skill/3511.img/skill/35111011/summon/heal/info/range`) |
| Partial | Summon simulation | The simulator still keeps the broader packet-owned summon parity already landed here (lifecycle/timers, WZ attack-branch metadata loading, projectile + target-impact retention, Tesla triangle handling, self-destruct prepare/die, packet-owned prepare/attack visuals, taunt-only puppet registration, shared local/packet-owned summon rule selection, delayed bird tile handoff, `4111007`/`4211007` reactive-chain playback, and hidden-hit source-token normalization/recovery). WZ was rechecked first again on this pass and the tile + ball surfaces still hold (`Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, `Skill/312.img/skill/3120010/tile`, `Skill/411.img/skill/4111007/{summon,ball}`, `Skill/421.img/skill/4211007/{summon,ball}`), while hidden source-sequence surfaces still publish frame-owned source chains consumed by this seam (`Mob/2400011.img/attack1/info/hit/{0..11}/source`) plus mixed authored families where `info/hit/*/source` coexists with frame branches (`Mob/2400110.img/attack1/{info/hit,0..11}/source`). Client evidence stayed second on the same owners and rechecked `CMobTemplate::LoadAttackInfo@0x65b890`, `CMob::GetAttackInfo@0x641330`, `CSummoned::OnHit@0x74bc80`, and `SKILLENTRY::GetBallUOL@0x569390`: attack info still stores client-owned hit path strings (`sHit`), summon-hit presentation still consumes those stored paths with attach/facing metadata before effect loading, and ball-UOL lookup still keeps the client-owned char-level/level/root fallback owner chain. This run stayed in the same seams (`SkillLoader`, `SkillData`, `SummonClientPostEffectRules`, `PacketOwnedSummonUpdateRules`, `SummonedPool`, `SkillManager`) and widened hidden `sHit` recovery again without widening runtime ownership: sibling-frame sequence parsing still preserves source-alias assignment tokens (`source=3`, `source->3`, `source=>3`), dot-prefixed alias tokens (`.source[1]`, `.source/1`), wrapped alias forms (`source(2)`, `source{3}`, `source<3>`), colon-delimited alias forms (`source:3`), and whitespace-delimited signed alias forms (`source + 5`, `source - 6`) through the same source-alias frame resolver; candidate enumeration still normalizes single-token alias paths against mob/action base seeds; alias-number parsing still accepts quoted frame-number payloads across assignment/wrapper/signed-token forms (`source="5"`, `source('4')`, `source=>"2"`, `source = - "3"`) plus single-token quoted signed assignment/wrapped variants (`source=>+"2"`, `source( +"2" )`, `source=-"3"`); and token-shell decoding now also normalizes escaped and percent-encoded alias syntax (`source=\\\"2\\\"`, `source%3D%224%22`, `source%3D%3E%2B%222%22`) so hidden encoded variants still route into the same sibling-frame/seeded-root resolution path instead of dropping to non-resolvable literals. Focused regression coverage in `UnitTest_MapSimulator/PacketOwnedSummonHitParityTests.cs` now also includes escaped quoted assignment alias resolution, percent-encoded quoted assignment alias resolution, percent-encoded quoted arrow-assignment alias resolution, percent-encoded source-preserving token normalization, and seeded candidate-root recovery for percent-encoded quoted signed arrow aliases alongside existing summon tile/ball and action-loader coverage named on this row. The row remains partial because deeper client-only gaps still remain: full one-for-one synthesis of every hidden `MobAttackInfo::sHit` shape beyond currently observed WZ/client surfaces, unresolved exact upstream population source for stored `sTileUOL`/`sBallUOL`/`sFlipBallUOL` values (and hidden tile-family construction beyond observed authored nodes), unresolved literal GR2/layer ownership and textured bolt choreography, and still-incomplete official `CUserPool` transport/ownership decode used by `/summonedpacket` loopback/session bridge seams. | Packet-owned summon hit playback and tile/ball selection now keep more literal hidden client-owned path shapes alive without introducing new runtime owners: normalized hidden token chains can elevate directly into source-sequence root candidates, alias-framed sibling tokens (including assignment forms `source=3`/`source->3`/`source=>3`, bracketed/parenthesized/wrapped `source[n]`/`source(n)`/`source{n}`/`source<n>` forms, colon-delimited shorthand like `source:n`, dot-prefixed shorthand like `.source[n]` and `.source/n`, quoted-frame variants such as `source="5"` and `source = - "3"`, and now escaped/percent-encoded assignment-arrow forms like `source=\\\"2\\\"` and `source%3D%3E%2B%222%22`) can resolve to the same frame-root path, and single-token alias entries still recover roots through mob/action base seeding when the only token is alias-coded. Existing parity lifts remain intact (packet family `0x116-0x11B`, retained live attack-frame reuse, frame-scoped attach/facing behavior, delayed bird tile overlay, and `4111007` reactive chain). Remaining mismatch risk is now concentrated in deeper hidden `sHit` synthesis breadth beyond currently observed assignment/wrapper/sibling-token families, unresolved true upstream `sTileUOL`/`sBallUOL` population provenance, GR2/layer choreography, and incomplete official transport decode. | packet-owned summon runtime (`CSummonedPool::OnPacket`, `CSummoned::OnAttack`, `CSummoned::OnHit`, `CSummoned::Hit`, `SkillManager.cs`, `SummonedPool.cs`, `PacketOwnedSummonUpdateRules.cs`, `SummonClientPostEffectRules.cs`, `SkillLoader.cs`, `SkillData.cs`, `ZoneEffectData`, `ProjectileData`, `LifeLoader.cs`, `LifeLoader.BuildAttackInfoMetadata(...)`, `MobAnimationSet.cs`, `MobAnimationSet.GetAttackHitEffectEntry(...)`, `SummonedPool.SelectPacketMobAttackInfoForLiveHitEntry(...)`, `SummonedPool.EnumeratePacketMobAttackGeneralEffectPathTokens(...)`, `SummonedPool.EnumeratePacketMobAttackGeneralEffectCandidateUols(...)`, `SummonedPool.TryResolvePacketMobAttackGeneralEffectSourceSequenceRootPath(...)`, `SummonedPool.TryNormalizePacketMobAttackGeneralEffectSiblingFrameRelativeSegments(...)`, `SummonedPool.TryParsePacketMobAttackGeneralEffectSourceAliasFrameIndex(...)`, `SummonedPool.FindPacketMobAttackGeneralEffectEmbeddedImagePathStartIndex(...)`, `SummonedPool.IsPacketMobAttackGeneralEffectEscapedSyntaxCharacter(...)`, `UnitTest_MapSimulator/SummonSimulationParityTests.cs`, `UnitTest_MapSimulator/PacketOwnedSummonHitParityTests.cs`, `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs`, `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, `Skill/312.img/skill/3120010/{ball,tile,summon}`, `Skill/411.img/skill/4111007/{summon,ball}`, `Skill/421.img/skill/4211007/{summon,ball}`, `Mob/2400011.img/attack1/info/hit/{0..11}/source`, `Mob/2400110.img/attack1/{info/hit,0..11}/source`, `Mob/3220000.img/attack1/{info/hit,13/hit}`, `Mob/8150302.img/attack1/info/hit/1/attach`, `Mob/9300314.img/attack1/{info/attachfacing,info/hit/attachfacing}`, `Mob/9300275.img/attack1/info/attachfacing`, `SKILLENTRY::GetTileUOL@0x437180`, `SKILLENTRY::GetBallUOL@0x569390`, `CMobTemplate::LoadAttackInfo@0x65b890`, `CMob::GetAttackInfo@0x641330`, `CSummoned::OnAttack@0x753340`, `CSummoned::OnHit@0x74bc80`, `CAnimationDisplayer::Effect_General@0x455d10`, `CAnimationDisplayer::LoadLayer@0x451b10`) |
| Partial | Remote summoned-pool packet parity | The simulator still keeps packet-owned summon transport in `SummonedPool`, and this pass narrows hidden hit-effect fallback shaping again without widening ownership. WZ was rechecked first on the same mounted `Mob` seams and remains consistent: `Mob/2400011.img/attack1/info/hit/*/source -> Mob/2400008.img/attack1/info/hit/*` still publishes frame-sequenced source indirection, `Mob/2400110.img/hit1/0/source -> Mob/2400017.img/hit1/0` still keeps literal client-consumable `category/image/property` source chains, and adjacent action leaves still expose relative UOL-style aliases (`Mob/3220000.img/attack1/18 -> ../stand/0`) that can leak into hidden fallback shaping. Client evidence stayed second on the same owner seam: `CSummoned::OnHit@0x74bc80` still reads decoded attack/template metadata, routes non-empty `MobAttackInfo::sHit` through `Effect_General`, and still honors attach flags from decoded metadata instead of requiring a live mob actor. Inside the existing loader/live-mob/mob-data seams, `SummonedPool` now also tolerates deeper source-prefixed nested assignment noise while resolving source sequences: sequence-root resolution still skips non-source nested assignments wrapped by source aliases (`source=attach=1` / `source:attachfacing:1`), and now also skips chained source-prefixed variants (equals/colon/wrapped/percent-encoded forms such as `source=source=attach=1`, `source:source:attachfacing:1`, `source=(source=attach=1)`, `source%3Asource%3Aattachfacing%3A1`) so those tokens no longer collapse into sibling-frame `1`; prior resolved frame context stays intact for subsequent relative aliases, and only resolved frame-shaped source paths are admitted into sequence-root consensus. Existing non-frame assignment-noise tolerance remains intact (`attach=1` / `attachfacing=1` still skip), and signed sibling-offset recovery still accepts whitespace-delimited signed magnitudes in slash forms (`source/- 1`) through the same signed-offset parser used by `./source(-1)`, `./source/-1`, and `./source.-1`. Focused regression coverage in `UnitTest_MapSimulator/PacketOwnedSummonHitParityTests.cs` now also locks chained source-prefixed nested assignment-noise skipping (equals, colon, wrapped, and percent-encoded forms) in `TryResolvePacketMobAttackGeneralEffectSourceSequenceRootPath(...)`, alongside prior leading/mid-chain assignment-noise and signed slash alias whitespace-magnitude coverage. The row remains partial because full one-for-one synthesis of all hidden client `MobAttackInfo::sHit` shapes is still unresolved, some cases still depend on unseen predecoded/client-owned path shaping beyond currently covered sequence and alias forms, post-attack effects remain constrained to the confirmed bird-tile family plus `4111007` reactive chain, stored `sTileUOL` population plus hidden tile-family construction remain unresolved beyond getter behavior, GR2 chain-layer/canvas ownership choreography remains unresolved, and `/summonedpacket` + session-bridge transport is still short of full official `CUserPool` decode/ownership parity. | Packet-owned summon hit playback now keeps more hidden-owner path shapes alive without adding new runtime owners: decorated hidden tokens, leading-dot image-relative shells, frame-relative source branches, sibling-frame shorthand (unsigned and signed), sequence-root alias fallback, assignment-noise-tolerant sequence chains, source-prefixed nested assignment-noise chains, and now chained/wrapped/percent-encoded source-prefixed assignment-noise chains all survive into the same general-effect fallback seam so fewer packet-owned summon hits collapse to no visual when authored clips are hidden behind indirection. Existing lifts remain intact (packet family `0x116-0x11B`, retained live attack-frame reuse, frame-scoped attach/facing behavior, tile/ball UOL family retention, delayed bird tile overlay, and `4111007` reactive chain). Remaining mismatch risk is now concentrated in deeper hidden `sHit` synthesis breadth, unresolved tile-UOL precompute/population, GR2/layer choreography, and incomplete official transport decode. | packet-owned summon runtime (`CSummonedPool::OnPacket`, `CSummoned::OnAttack`, `CSummoned::OnHit`, `CSummoned::Hit`, `SummonedPool`, `PacketOwnedSummonUpdateRules`, `SkillLoader`, `ZoneEffectData`, `ProjectileData`, `LifeLoader`, `MobAnimationSet.AttackInfoMetadata`, `MobItem`, `MobData.AttackData`, `UnitTest_MapSimulator/PacketOwnedSummonHitParityTests.cs`, `SummonedPool.TryResolvePacketMobAttackGeneralEffectSourceSequenceRootPath(...)`, `SummonedPool.TryExtractPacketMobAttackGeneralEffectSourceSequenceFrameRootPath(...)`, `SummonedPool.TryResolvePacketMobAttackGeneralEffectSignedSiblingFrameSourcePath(...)`, `SummonedPool.TryResolvePacketMobAttackGeneralEffectSiblingFrameSourcePath(...)`, `SummonedPool.TryResolvePacketMobAttackGeneralEffectRelativeSourcePath(...)`, `SummonedPool.EnumeratePacketMobAttackGeneralEffectPathTokens(...)`, `SummonedPool.TryExtractPacketMobAttackGeneralEffectEmbeddedPathToken(...)`, `SummonedPool.TrimPacketMobAttackGeneralEffectSourceLeafSuffixSegments(...)`, `SummonedPool.IsPacketMobAttackGeneralEffectNestedNonSourceAssignmentNoiseToken(...)`, `Mob/3220000.img/attack1/{info/hit,13/hit}`, `Mob/2400011.img/attack1/info/hit/0/source`, `Mob/2400110.img/hit1/0/source`, `Mob/9300314.img/attack1/{info/attachfacing,info/hit/attachfacing}`, `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, `Skill/312.img/skill/3120010/{CharLevel,ball,tile,summon}`, `SKILLENTRY::GetTileUOL@0x437180`, `SKILLENTRY::GetBallUOL@0x569390`, `CSummoned::OnHit@0x74bc80`) |
| Partial | Summoned actor update/runtime parity | The simulator keeps summon-owned actor update/runtime ownership in the same seams (`SummonRuntimeRules`, `PacketOwnedSummonUpdateRules`, `SkillManager`, `SummonedPool`) for movement snapshots, spawn/prepare/attack/hit/die routing, Tesla armed idle fallback, self-destruct expiry handoff, packet-owned one-time owner routing, and shared authored suspend windows. WZ was rechecked first against the mounted export in this pass and still confirms the authored branch constraints this row depends on: `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3512.img/skill/35121009/{info/{type,minionAbility,selfDestructMinion},summon/subsummon}` (`type=33`, `minionAbility=summon`, `selfDestructMinion=1`, six frames at `120ms` each), stand-only support/self-destruct rows at `Skill/3511.img/skill/35111005` and `Skill/3512.img/skill/35121010`, and Tesla authored branch family at `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}`. Client evidence stayed second: `CSummoned::OnSkill@0x74a940` clears prior one-time owner on the addressed summon before `SetAttackAction`, `CSummoned::SetAttackAction@0x74a0f0` stamps one-time action ownership, and `CSummoned::Update@0x757f70` keeps one-time ownership alive until clip completion while forcing move action `0x2A` during Tesla states `1/2` before `SetMoveAction`. This pass keeps prior packet-owned Tesla/runtime lifts and closes another no-action assist-selector gap in the same seam: packet-skill ownership handoff still requires action-family evidence plus explicit family-owned authored branch presence (`13`/Beholder support families => support-owned, `14` => summon-action-owned, indexed `1..6` biased by authored `minionAbility`), strict action-`14` routing remains authored-`subsummon` only with no pending/attack fallback admission when `subsummon` is missing, and no-action-family packet-skill updates (`action & 0x7F == 0`) now also honor single-family `minionAbility` cues when explicit `subsummon`/`heal`/`support` cues are absent (`heal|mes|amplifyDamage` + authored support family, including stand-only support rows, => support ownership; summon cue still requires authored `subsummon`; conflicting/ambiguous cues => keep current ownership). Existing Tesla constraints remain in place: packet-owned runtime move-action ownership still forces the client-style Tesla `0x2A` action family (with preserved facing bit) while Tesla is in states `1/2`; packet-owned attack-facing still resolves from that forced move-action bit during Tesla armed windows; packet-owned hit-action interrupts still explicitly drop prior skill-packet ownership state before arming hit one-time playback; packet-owned one-time owner cleanup no longer depends only on `OneTimeAction != 0`; local Tesla still requires armed state `1`; local and packet-owned rearm still return state `2 -> 1` only after one-time playback finishes; packet-owned refresh still clears packet triangle points at rearm; packet-owned Tesla skill packets still avoid rebroadcasting one coil branch to siblings; and sibling one-time packet-skill ownership is still cleared strictly from the ownership flag even when action/clip fields are already zeroed. Focused regression coverage in `UnitTest_MapSimulator/SummonRuntimeActionParityTests.cs` now also locks no-action-family stand-only support ownership flip/no-flip behavior (support minion cue flip, no-cue keep-current) through `SummonRuntimeRules.ResolvePacketSkillAssistTypeForRuntimeOwnership(...)`, `SummonedPool.ResolvePacketOwnedSkillAssistTypeForRuntimeOwnership(...)`, and `SummonRuntimeRules.ResolveAuthoredAssistTypeForRuntimeOwnershipWithoutActionFamily(...)`, alongside existing strict action-`14` authored branch routing/no-fallback checks, strict one-time fallback-admission checks, action-`14` runtime ownership flip/no-flip gating, action-`13` support-owner flip/no-flip coverage for explicit authored support branches with and without `minionAbility` support cues, and indexed ownership flips/no-flip coverage for explicit single-family vs conflicting cues. The row remains partial because deeper client-only seams still sit outside current simulator visibility: exact hidden summon HP and full `CalcDamage::PDamageSummoned` / `MDamageSummoned`, broader `CVecCtrl::UpdatePassive` side effects beyond currently modeled passive effects, deeper hidden/packet-owned Tesla transition rules beyond the modeled rearm boundaries, and unresolved remote assist-family selectors when both action-family signals and disambiguating authored/minion cues are absent or conflicting. | This keeps summon runtime ownership closer to the recovered client split instead of leaking packet-owned one-time state and Tesla runtime state across seams: addressed summon skill ownership stays isolated, Tesla move-action runtime still mirrors the recovered `0x2A` forced-action seam during armed/attacking Tesla states, packet Tesla attack-facing still stays aligned to that forced move-action family during armed states, hit interrupts no longer preserve stale packet-skill ownership flags, one-time owner cleanup still releases expired action-code-zero ownership, packet skill assist-family ownership still re-homes only when action-family evidence plus authored/minion disambiguation cues agree, and action-family-missing skill packets now cover stand-only support-family ownership in addition to explicit single-family authored ownership cues (while preserving current ownership for conflicting/absent signals). Remaining risk is concentrated on hidden damage formulas, deeper passive-update side effects, hidden Tesla transitions, and unresolved client-only assist selectors not exposed by current packet/WZ inputs rather than on packet-owned Tesla move-action forcing, packet-owned attack-facing ownership during forced Tesla runtime states, packet-owned hit interrupt ownership reset, one-time playback completion cleanup, sibling stale-owner cleanup, strict action-`14` branch/no-fallback routing, explicit single-family authored assist routing, or stand-only support ownership routing for action-family-missing packets. | summon actor/runtime layer (`CSummoned::Update`, `CSummoned::OnSkill`, `CSummoned::SetAttackAction`, `CSummoned::SetMoveAction`, `CSummoned::TryDoingHealingRobot`, `CSummoned::TryDoingSummon`, `CSummoned::TryDoingTaslaCoilAttack`, `SummonRuntimeRules.cs`, `SummonRuntimeRules.IsStrictPacketSkillBranchAction(...)`, `SummonRuntimeRules.ResolveAuthoredAssistTypeForRuntimeOwnershipWithoutActionFamily(...)`, `PacketOwnedSummonUpdateRules.cs`, `SkillManager.cs`, `SummonedPool.cs`, `SummonedPool.ShouldAllowPacketOwnedSkillOneTimeFallback(...)`, `SummonedPool.ResolvePacketOwnedExplicitSelfDestructPlayback(...)`, `BeginPacketOwnedSkillAnimation(...)`, `SummonedPool.ResolvePacketOwnedMovementSnapshotFacingForParity(...)`, `PacketOwnedSummonUpdateRules.ResolvePacketAttackFacingRight(...)`, `SummonedPool.ResolvePacketOwnedSkillAssistTypeForRuntimeOwnership(...)`, `SummonedPool.ResolvePacketOwnedCreatedAssistTypeForRuntimeOwnership(...)`, `SummonRuntimeRules.ResolvePacketSkillAssistTypeForRuntimeOwnership(...)`, `SummonRuntimeRules.HasAuthoredPacketSkillAssistOwnershipBranch(...)`, `UnitTest_MapSimulator/SummonRuntimeActionParityTests.cs`, IDA `CSummoned::Update` (`0x757f70`), IDA `CSummoned::OnSkill` (`0x74a940`), IDA `CSummoned::SetAttackAction` (`0x74a0f0`), IDA `CSummoned::TryDoingHealingRobot` (`0x74b3e0`), IDA `CSummoned::TryDoingSummon` (`0x74b640`), IDA `CSummoned::TryDoingTaslaCoilAttack` (`0x752780`), `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3512.img/skill/35121009/{info/{type,minionAbility,selfDestructMinion},summon/subsummon}`, `Skill/3511.img/skill/35111005/{info/selfDestructMinion,summon/stand}`, `Skill/3512.img/skill/35121010/{info/selfDestructMinion,summon/stand}`, `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}`) |
| Partial | Weapon afterimage / melee trail parity | This pass stays in the same afterimage seams and tightens metadata-missing no-hint disambiguation again with WZ-first and client-second evidence. WZ was rechecked first in the mounted export: `Character/Afterimage/swordOS/charge` still publishes `{1,2,3,5}` while `Character/Afterimage/poleArm/charge` still publishes only `{1}`; `Skill/122.img/skill/1221011` still exposes root `elemAttr = h` and still has no `afterimage/*` branch; `Skill/122.img/skill/1221009/afterimage/swordOS/stabO2/1/0` still remains a separate skill-authored branch; and representative lightning rows still publish `elemAttr = l` (`Skill/1511.img/skill/15111006/elemAttr = l`). Client evidence was rechecked second: `get_element_by_charged_skillid@0x639fe0` still maps the same charge ids/elements, `CActionMan::GetMeleeAttackRange@0x428d00` still only normalizes `57 -> 41` plus hardcoded `74`, `CUserRemote::OnMeleeAttack@0x959d60` still forces `1221009 -> 17` before range lookup, and `CUser::RegisterAfterimage@0x902d90` still formats the afterimage UOL with explicit charged-skill input. Implementation remains in the same seams (`AfterImageChargeSkillResolver`, `RemoteUserActorPool.ResolveChargeSkillIdFromTemporaryStats`, `RemoteUserActorPool.TryResolveChargeElementFromTemporaryStats`, `RemoteUserPacketCodec.TryResolveChargeSkillIdFromKnownTemporaryStatPayload`) and now adds two scoped recoveries for metadata-missing families: known-state decode now captures `WeaponChargePayloadOffset` at the start of the decoded charge value, and metadata-missing unbounded fallback now only admits far payload evidence when repeated known charge-skill markers converge on a single charge element (`ChargeMetadataMissingConsensusMinimumMatches = 2`) instead of accepting one far word. Prior metadata-scoped and metadata-missing mask-base bounded ordering (`ChargeMetadataScopedScanBytes`, `ChargeMetadataMissingMaskBaseNearestScanBytes`) stays unchanged, and preferred/prior skill hints still outrank far payload words. Focused coverage in `UnitTest_MapSimulator/AfterImageChargeSkillResolverParityTests.cs` now also locks metadata-missing repeated far-known-skill consensus recovery (runtime + known decode) and mixed-element consensus tie rejection, alongside prior far-known single-word no-hint rejection and preferred-skill fallback coverage. The row remains partial because unresolved gaps are now concentrated in truly no-hint temporary-stat payload families with no local signal and no repeated far-known markers, plus unobserved afterimage-specific zoom authorship and deeper GR2 layer-object behavior (`RelMove`, `RemoveCanvas`, `InsertCanvas`, layer timing/ownership, interpolation) outside current SpriteBatch seams. | Weapon trail parity now keeps metadata-scoped, metadata-anchored, metadata-missing mask-base-scoped, metadata-missing nearest-element-before-nearest-id, metadata-missing local-distance-bounded nearest recovery, and metadata-missing repeated-known-skill consensus recovery deterministic; single far unbounded markers are still rejected without stable hints. Remaining risk is concentrated in true no-hint/no-consensus payload disambiguation plus unresolved zoom/GR2 layer-object fidelity. | melee afterimage loader and playback seam (`CUser::RegisterAfterimage`, `get_element_by_charged_skillid`, `CActionMan::GetMeleeAttackRange`, `CUserRemote::OnMeleeAttack`, `AfterImageChargeSkillResolver`, `RemoteUserActorPool.ResolveRemoteAfterImageChargeElement`, `RemoteUserActorPool.ResolveChargeSkillIdFromTemporaryStats`, `RemoteUserActorPool.TryResolveChargeElementFromTemporaryStats`, `RemoteUserPacketCodec.TryResolveChargeSkillIdFromKnownTemporaryStatPayload`, `SkillLoader.TryResolveMeleeAfterImageAction`, `MeleeAfterimagePlaybackResolver`, `PlayerCharacter.ApplyMeleeAfterImage`, `RemoteUserActorPool.RegisterMeleeAfterImage`, `UnitTest_MapSimulator/AfterImageChargeSkillResolverParityTests.cs`, `Character/Afterimage/swordOS/charge`, `Character/Afterimage/poleArm/charge`, `Skill/122.img/skill/1221011`, `Skill/122.img/skill/1221009/afterimage/swordOS/stabO2/1/0`, `Skill/1511.img/skill/15111006/elemAttr`) |
| Partial | Skill cooldown UI parity | QuickSlot/skill-book/status-bar cooldown UI still stays on one shared runtime seam, and this pass narrows remaining drift in the same seam again. WZ was rechecked first and remains stable on the same authored surfaces: `UI/UIWindow.img/Skill/CoolTime` and `UI/UIWindow2.img/Skill/main/CoolTime` still expose `16` mask frames, mounted `UI/UIWindow2.img/Skill/main/tip0..2` still stays `193x102` with origins `(195,-25)`, `(-178,-22)`, `(103,-282)` while legacy `UI/UIWindow.img/Skill/tip0` still keeps zero origin, `UI/Basic.img/Notice3` still keeps `t/c/s` at `266x21`, `266x20`, `266x55`, and `UI/StatusBar2.img/mainBar/quickSlot/quickSlot` remains `145x93` at origin `(-143,143)`. Client evidence stayed second on the same owner seams (`CUIStatusBar::SetItemMsg@0x86cdf0`, `CUIStatusBar::FloatNotice@0x86d430`, `CUIStatusBar::Update@0x874290`, `CUISkill::OnMouseMove@0x84b660`), and this pass also keeps the recovered `CUIStatusBar::Update` shortcut cooltime cadence (`DrawSkillCooltime` only when `timeGetTime() - m_tLastUpdateSkillCooltime > 1000`) in scope. Implementation remains in existing seams: status-bar shortcut tray intake still routes through surface-owned cooldown UI-state admission (`TryGetCooldownUiState(..., StatusBarShortcutTray)` + `ShouldAdmitStatusBarShortcutCooldownForClientParity(...)`) rather than a separate `IsOnCooldown(...)` timer gate, and status-bar shortcut tray shaping now also mirrors the recovered client update cadence through `ShouldRefreshStatusBarShortcutCooldownUiForClientParity(...)` so shortcut cooldown mask/counter rows refresh on the same strict `>1000 ms` update gate instead of every frame. Prior row behavior remains in place: off-bar candidate intake through `GetActiveCooldownUiSkillIdsForSurface(..., StatusBarOffBarTray)`, counter-text fallback when visuals are suppressed (`ResolveCooldownCounterTextForSurface`), counter-only status-bar draw admission (`ShouldDrawCooldownCounterTextForClientParity`), fixed `5000` ms notice duration/single-owner gating/non-preemptive same-skill refresh, owner blocking filters, per-surface tooltip offsets, mounted `193x102` tip-origin fallback gating, Notice3 family preference, and per-surface cooldown mask math (QuickSlot/shortcut 14-step discrete vs skill-book/off-bar 16-step). Focused regression coverage in `UnitTest_MapSimulator/SkillCooldownUiParityTests.cs` now also locks status-bar shortcut tray cadence gating (`ShouldRefreshStatusBarShortcutCooldownUiForClientParity`: strict `>1000 ms`, first-refresh, and regressed-tick refresh) alongside existing status-bar shortcut admission/rejection, off-bar counter-only, and suppressed-visual counter fallback checks. Remaining gap: literal client `CFloatNotice` COM-layer identity/lifetime semantics are still approximated, cross-layer status-bar owner identity still stays simulator-probed rather than native `quizPanel`/`floatNotice`/`itemMsg` objects, exact Battleship `CTemporaryStatView` tray/icon artwork and durability-only HUD widgeting remain outside current seams, and cooldown-only HUD widgets beyond quick-slot, skill windows, status-bar trays, and Notice3 remain unverified. | Surface ownership in the shared cooldown seam is tighter: status-bar shortcut/off-bar intake, shortcut-tray update cadence, tooltip anchors, mask ladders, and counter-only durability rendering now all stay on per-surface admission/math while preserving the existing owner-gating seam for status notices. The row remains `Partial` because native layer-object identity/lifetime and unverified extra cooldown HUD widgets still sit outside simulator-visible seams. | `SkillTooltipFrameLayout.cs`, `QuickSlotUI.cs`, `SkillUI.cs`, `SkillUIBigBang.cs`, `StatusBarUI.cs`, `SkillManager.cs`, `SkillCooldownNoticeUI.cs`, `FieldEffects.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/SkillCooldownUiParityTests.cs` (`MapSimulator.ShouldAdmitStatusBarShortcutCooldownForClientParity`, `MapSimulator.ShouldRefreshStatusBarShortcutCooldownUiForClientParity`, `SkillManager.GetActiveCooldownUiSkillIdsForSurface`, `SkillManager.ResolveCooldownCounterTextForSurface`, `StatusBarUI.ShouldDrawCooldownCounterTextForClientParity`, `UI/UIWindow.img/Skill/CoolTime`, `UI/UIWindow2.img/Skill/main/CoolTime`, `UI/UIWindow.img/Skill/tip0..2`, `UI/UIWindow2.img/Skill/main/tip0..2`, `UI/Basic.img/Notice3`, `UI/StatusBar2.img/mainBar/quickSlot/quickSlot`, `CUIStatusBar::SetItemMsg@0x86cdf0`, `CUIStatusBar::FloatNotice@0x86d430`, `CUIStatusBar::Update@0x874290`, `CUISkill::OnMouseMove@0x84b660`) |
| Partial | Client-timer skill expiry and cancellation | `SkillManager` still owns the shared `UpdateClientTimer`-style registry for buff expiry, prepared release, repeat-sustain/mode-end, cyclone timeout, swallow digest completion, local skill-zone expiry, and local summon expiry, while `SummonedPool` still owns packet-local summon expiry/cancel ownership plus self-destruct fallout routing. WZ was rechecked first and remains unchanged on the same row-owned timer/cancel surfaces (`Skill/422.img/skill/4221006` `info/type=36`, `info/zoneType=invincible`, `common/time=30+x`; `Skill/3212.img/skill/32121003` `action/0=cyclone_pre`, `info/bodyAttack=1`, `info/movingAttack=1`, `common/time=10+2*d(x/6)`; `Skill/3511.img/skill/35111011` `common/selfDestruction=300+5*x`, `hit/0/hitAfter=990`; repeat-sustain `35111004` / `35121013` `common/time=5`; swallow linkage `Skill/3310.img/skill/33101005/info/dummyOf=33101006&&33101007` with `33101006/common/time=10+4*x`). Connected-family Battle Mage alias rewrites remain mirrored by `ClientSkillCancelResolver` (`32120000 -> 32001003`, `32110000 -> 32101002`, `32120001 -> 32101003`). IDA was rechecked second and still anchors the same owners (`CUserLocal::UpdateClientTimer@0x90e6d0`, `CUserLocal::SendSkillCancelRequest@0x93d730`, caller families `CUserLocal::{Update@0x937330,OnKeyDownSkillEnd@0x9301b0,HandleCtrlKeyDown@0x9326b0,TryDoingSwallowMobWriggle@0x93f500,OnJoystickButton@0x936550,HandleRButtonClk@0x911a60}` plus `CDraggableItem::{WearEquipItem@0x5043e0,GetOffEquipItem@0x502b90/0x502e60,ThrowItem@0x505ba0}` and `CAvatarMegaphone::OnMouseButton@0x46d560`). This pass keeps ownership in the same seams and closes additional non-update ingress drift: Item Upgrade owner start now routes through `ReleaseActiveKeydownSkillForClientCancelIngress(currTickCount)` in `MapSimulator.ItemUpgradeOwnerParity.cs::HandleItemUpgradeOwnerStartRequested(...)`, Vega cast dispatch now routes through the same ingress wrapper in `MapSimulator.VegaSpellParity.cs::DispatchVegaSpellRequest(...)` after request-context admission, Cash Avatar preview owner buttons now route through `CashAvatarPreviewWindow.ClientCancelIngressRequested` wired in `MapSimulator.CashServiceStageParity.cs` and `MapSimulator.Content.cs` to `ReleaseActiveKeydownSkillForClientCancelIngress(currTickCount)` before buy/default/takeoff handling (with weather branch ingress still preserved in `PreviewCashAvatarWeatherAction(...)`), and Big Bang companion drag commit sub-branches now route through `EquipUIBigBang.CompanionDragCommitIngressRequested` wired in `MapSimulator.Content.cs` to `ReleaseActiveKeydownSkillForClientCancelIngress(currTickCount)`, MapleTV owner buttons now route through `ExecuteMapleTvClientCancelIngress(...)` in `MapSimulator.cs::WireMapleTvWindowData(...)` so publish/clear/toggle handlers release active keydown skill before owner action dispatch, alongside the existing content-rebound inventory/equipment/drop/memo/quest ingress mirrors and packet-owned self-destruct fallback ordering (`OrderPacketOwnedExpiryFallbackCandidates(...)` before `ResolvePacketOwnedExpiryFindHitMobInRectTargetOrder(...)`). Status-bar right-click ingress now also mirrors the recovered native suppression branch from `CAvatarMegaphone::OnMouseButton@0x46d560` (`nType == 2 && msg != 1311006`) by suppressing skill-cancel ingress for `1311006` before batch-scope/release/request dispatch. Focused regression coverage now includes MapleTV ingress order/null-action checks in `UnitTest_MapSimulator/ClientTimerMapleTvCancelIngressParityTests.cs`, status-bar suppression checks for `1311006` in `UnitTest_MapSimulator/ClientTimerStatusBarBuffCancelIngressParityTests.cs`, alongside cash-service ingress checks, explicit packet-owned expiry ordering checks, and owner-reference facing tie-break coverage in `UnitTest_MapSimulator/ClientTimerPacketOwnedSelfDestructExpiryParityTests.cs`. Packet-owned self-destruct expiry now also consumes owner-position tie-breaks in `SummonedPool.TryResolvePacketOwnedExpiryProbeFacingRight(...)` and `SummonedPool.ResolvePacketOwnedExpiryFallbackFacingRight(...)` when bidirectional capture candidates are otherwise tied. The row remains partial because packet-owned self-destruct expiry targeting still approximates native `CMobPool::FindHitMobInRect` object-iteration/pointer-lifetime internals, and remaining non-simulator-native direct UI ingress ownership outside the mapped seams still lacks one-for-one mirroring. | Cancel dispatch now dedupes on one cancel-family path across timer routes, update-loop non-timer routes, scoped non-update attack/hotkey/macro/func-key routes, content-rebound equipment/inventory callbacks, memo/quest inventory-drag UI ingress, Item Upgrade owner start, Vega cast dispatch, cash-avatar preview buy/default/takeoff/weather ingress, Big Bang companion drag commit ingress, MapleTV publish/clear/toggle owner-button ingress, status-bar right-click suppression parity for skill `1311006`, and packet-owned summon-expiry fallback selection with deterministic facing-ordered candidate pick before in-range capture plus owner-position tie-break consumption when bidirectional capture is otherwise tied. Remaining risk is narrowed to native `FindHitMobInRect` internals plus still-unmapped direct UI caller internals outside the mapped seams. | `ClientSkillCancelResolver`, `SkillManager`, `SkillManager.BeginClientCancelBatchScope`, `SkillManager.BeginClientCancelBatchScope(ref ...)`, `SkillManager.EndClientCancelBatchScope(ref ...)`, `SkillManager.Update`, `SkillManager.UpdateClientSkillTimers`, `SkillManager.RequestClientSkillCancel`, `SkillManager.ShouldSuppressClientCancelBatchRequest`, `SkillManager.TryRegisterClientTimerBatchCancelFamily`, `SkillManager.TryCastSkill`, `SkillManager.TryCastPacketOwnedFuncKeySkill`, `SkillManager.TryCastHotkey`, `SkillManager.ReleaseHotkeyIfActive`, `SkillManager.ReleasePacketOwnedFuncKeySkillIfActive`, `SkillManager.TryExecuteMacro`, `SkillManager.TryDoingRandomAttack`, `SkillManager.TryDoingMeleeAttack`, `SkillManager.TryDoingShoot`, `SkillManager.TryDoingMagicAttack`, `SkillManager.ReleaseActiveKeydownSkill`, `SkillManager.RouteExpiredRepeatSustainTimerBatchToClientCancel`, `SkillManager.RouteExpiredCycloneTimerBatchToClientCancel`, `SkillManager.RouteExpiredSkillZoneTimerBatchToClientCancel`, `SkillManager.RouteExpiredLocalSummonTimerBatchToClientCancel`, `SkillManager.RouteExpiredBuffTimerBatchToClientCancel`, `SkillManager.TryApplyPacketOwnedSelfDestructSummonAttack`, `MapSimulator.PacketOwnedFuncKeyParity.cs`, `MapSimulator.UpdatePacketOwnedFuncKeyRuntime`, `MapSimulator.cs`, `MapSimulator.ReleaseActiveKeydownSkillForClientCancelIngress`, `MapSimulator.RequestStatusBarBuffCancelForClientCancelIngress`, `MapSimulator.TryExecuteStatusBarBuffCancelForClientCancelIngress`, `MapSimulator.ExecuteMapleTvClientCancelIngress`, `MapSimulator.ExecuteMapleTvClientCancelIngressForTests`, `MapSimulator.WireMapleTvWindowData`, `MapSimulator.TryUseInventoryItem`, `MapSimulator.TryUseInventoryItemAtSlot`, `MapSimulator.HandleMemoDraftAttachmentInventoryDrop`, `MapSimulator.EquipmentWindowParity.cs`, `MapSimulator.SubmitEquipmentChangeRequest`, `MapSimulator.ItemUpgradeOwnerParity.cs`, `MapSimulator.HandleItemUpgradeOwnerStartRequested`, `MapSimulator.VegaSpellParity.cs`, `MapSimulator.DispatchVegaSpellRequest`, `MapSimulator.CashServiceStageParity.cs`, `MapSimulator.PreviewCashAvatarWeatherAction`, `MapSimulator.Content.cs`, `CashAvatarPreviewWindow.cs`, `CashAvatarPreviewWindow.ClientCancelIngressRequested`, `CashAvatarPreviewWindow.HandleBuyAvatar`, `CashAvatarPreviewWindow.HandleDefaultAvatar`, `CashAvatarPreviewWindow.HandleTakeoffAvatar`, `MapleTvWindow.cs`, `MapSimulator.QuestRewardRaiseParity.cs`, `MapSimulator.HandleQuestRewardRaisePieceDropRequested`, `EquipUIBigBang.cs`, `EquipUIBigBang.CompanionDragCommitIngressRequested`, `SummonedPool`, `SummonedPool.ResolvePacketOwnedExpiryTargetOrder`, `SummonedPool.ResolvePacketOwnedExpiryFindHitMobInRectTargetOrder`, `SummonedPool.OrderPacketOwnedExpiryFallbackCandidates`, `SummonedPool.ResolvePacketOwnedExpiryFallbackFacingRight`, `SummonedPool.TryResolvePacketOwnedExpiryProbeFacingRight`, `MapSimulator.PacketOwnedSummonedParity.cs`, `MapSimulator.TryRoutePacketOwnedSelfDestructAttackToRuntime`, `MapSimulator.TryRouteLocalPacketOwnedSelfDestructAttackToRuntime`, `PlayerManager.TryApplyPacketOwnedSelfDestructSummonAttack`, `UnitTest_MapSimulator/ClientTimerCashServiceCancelIngressParityTests.cs`, `UnitTest_MapSimulator/ClientTimerStatusBarBuffCancelIngressParityTests.cs`, `UnitTest_MapSimulator/ClientTimerMapleTvCancelIngressParityTests.cs`, `UnitTest_MapSimulator/ClientTimerPacketOwnedSelfDestructExpiryParityTests.cs` (`CUserLocal::UpdateClientTimer`, `CUserLocal::SendSkillCancelRequest`, `CUserLocal::Update`, `CUserLocal::OnKeyDownSkillEnd`, `CUserLocal::HandleCtrlKeyDown`, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::OnJoystickButton`, `CUserLocal::HandleRButtonClk`, `CDraggableItem::WearEquipItem`, `CDraggableItem::GetOffEquipItem`, `CDraggableItem::ThrowItem`, `CAvatarMegaphone::OnMouseButton`, `CSummoned::TryDoingAttack`, `Skill/422.img/skill/4221006/{info,common}`, `Skill/3212.img/skill/32121003/{action,info,common}`, `Skill/3511.img/skill/35111011/{info,common,summon,hit}`, `Skill/3511.img/skill/35111004/common`, `Skill/3512.img/skill/35121013/{common,info}`, `Skill/3212.img/skill/32120000/info/affectedSkill`, `Skill/3211.img/skill/32110000/info/affectedSkill`, `Skill/3212.img/skill/32120001/info/affectedSkill`, `Skill/3310.img/skill/33101005/{info,common}`, `Skill/3310.img/skill/33101006/common`) |
| Partial | Skill restrictions by map/state | The shared restriction seam remains in place and this pass tightens map/state parity without widening runtime owners: `FieldSkillRestrictionEvaluator` still owns `fieldLimit` bits for `Unable_To_Use_Skill`, `Move_Skill_Only`, `Unable_To_Use_Mystic_Door`, `Unable_To_Use_Rocket_Boost` (Mechanic), and `Unable_To_Use_Taming_Mob` (mount/vehicle), WZ-authored `noSkill/skill`, `noSkill/class`, and `info/noCancelSkill`, Mu Lung Dojo `mobMassacre/disableSkill`, runtime `info/moveLimit` counters in `FieldRuleRuntime`, Coconut/Snowball/Guild Boss local basic-action ownership, Evan dragon-owner loss, and shared mob-disease lockouts including `StopMotion (136)`. WZ re-check stayed first against active `v95_2` and reconfirmed the same anchors (`Map/Map9/980031100/noSkill/class`, `Map/Map2/200100001/noSkill/class`, `Map/Map9/922240000/noSkill/skill`, `Map/Map9/970050010/noSkill/skill`, `Map/Map2/200110000/info/noCancelSkill = 1`, Mu Lung Dojo `mobMassacre/disableSkill = 1` examples `Map/Map9/910320100`, `910320200`, `910320300`), plus sampled `noSkill` roots still present only a single direct `class` or `skill` child branch. Client evidence stayed second: `CField::IsUnableToUseSkill@0x655ef0` still resolves to a pure `m_dwOption` bit gate with xrefs only in recovered attack lanes, `CField::IsSkillForbiden@0x52a320 -> Field::SkillInfo::IsSkill@0x5657e0` still routes class checks through `get_skill_class(skillId)` (no current-job fallback for rootless skill ids), `CUserLocal::TryDoingMeleeAttack@0x91e780` still keeps its airborne-without-foothold/non-flying deny-table branch, and the recovered basic-action ownership handlers remain field-specific (`CField_Coconut::BasicActionAttack@0x54a5e0`, `CField_SnowBall::BasicActionAttack@0x5617b0`, `CField_GuildBoss::BasicActionAttack@0x5517d0`, base `CField::BasicActionAttack@0x53cad0`). Source seams keep that ownership and now also mirror `Field::SkillInfo::IsSkill` class gating for `noSkill/class` by deriving class from `skillId / 10000` (`get_skill_class`) without current-job fallback for rootless skill ids, and resolve `info/noCancelSkill` from `MapInfo.unsupportedInfoProperties` in the existing evaluator path before `info` image fallback, so map contracts preserved through copied-info seams continue to gate active-skill cancellation even when the live map image is unavailable. Focused coverage in `UnitTest_MapSimulator` now includes this unsupported-info no-cancel fallback in `FieldSkillRestrictionEvaluatorMassacreDisableSkillTests.cs` (including image-null fallback) alongside prior matching-field block plus non-matching-field allow for Coconut/Snowball/Guild Boss, `noSkill/class` rootless non-match and valid-root non-fallback, direct `noSkill/skill` allow/deny, `mobMassacre/disableSkill`, recovered `Unable_To_Use_Skill` caller-table coverage and neighbor allow checks, Evan no-dragon and dragon-owner gates, and recovered melee airborne no-foothold deny-table admission; plus `FieldRuleRuntimeMoveLimitTests.cs` (`moveLimit` movement-only consumption/exhaustion) and `PlayerSkillBlockingStatusMapperParityTests.cs` (`mob skill 136 -> StopMotion` mapping/lockout text). Remaining gap: deeper unrecovered `CField`/`CUserLocal` availability branches outside the now-covered recovered `Unable_To_Use_Skill` caller table, recovered melee airborne deny-table branch, and recovered Coconut/Snowball/Guild Boss field-owned basic-action seams; still-unrecovered special-field ownership outside those recovered owners; and any additional unrecovered `Field::SkillInfo::IsSkill` branches beyond the now-covered field-type event tables, class/id traversal order, and rootless no-fallback class checks. | Map/state skill gating remains centralized in one shared seam with tighter field-type ownership correctness for local basic-action owners while preserving recovered deny-table and map-authored behavior: recovered `Unable_To_Use_Skill` subsets remain denied while neighboring non-table skills stay allowed, field-authored `noSkill`/`noCancelSkill` and `mobMassacre/disableSkill` stay live-map driven, runtime `moveLimit` exhaustion remains tied to successful movement-skill use, and local-state owners (massacre disable-skill, Coconut/Snowball/Guild Boss field-owned basic-action routing, dragon-owner loss, disease/rope/one-time-action gates plus recovered no-foothold/no-flying melee deny-table admission) remain centralized through shared evaluators. The row remains `Partial` because remaining mismatch is now concentrated in unrecovered client availability branches, unrecovered special-field ownership outside the recovered deny-table/field-owned basic-action/`get_skill_class` seams, and any still-unrecovered `Field::SkillInfo::IsSkill` branches outside those now-covered paths. | `MapSimulator`, `SkillManager`, `FieldSkillRestrictionEvaluator.cs`, `FieldRuleRuntime.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `PlayerSkillBlockingStatusMapper.cs`, `PlayerCharacter.cs`, `DragonCompanionRuntime.cs`, `MassacreField.cs`, `CoconutField.cs`, `SnowBallField.cs`, `GuildBossField.cs`, `MapInfo.unsupportedInfoProperties`, `MapInfo`, `FieldLimitType`, `FieldType`, `Skill/MobSkill.img`, `Map/Map9/980031100/noSkill/class`, `Map/Map2/200100001/noSkill/class`, `Map/Map9/922240000/noSkill/skill`, `Map/Map9/970050010/noSkill/skill`, `Map/Map9/910320100/mobMassacre/disableSkill`, `Map/Map2/200110000/info/noCancelSkill`, `Skill/2001.img`, `Skill/501.img`, `Skill/2002.img`, `Skill/3001.img`, `Skill/508.img`, `Skill/800.img`, `Skill/900.img`, `UnitTest_MapSimulator/FieldSkillRestrictionEvaluatorMassacreDisableSkillTests.cs`, `UnitTest_MapSimulator/FieldRuleRuntimeMoveLimitTests.cs`, `UnitTest_MapSimulator/PlayerSkillBlockingStatusMapperParityTests.cs` (`CField::IsUnableToUseSkill`, `CField::BasicActionAttack@0x53cad0`, `CField_Coconut::BasicActionAttack@0x54a5e0`, `CField_SnowBall::BasicActionAttack@0x5617b0`, `CField_GuildBoss::BasicActionAttack@0x5517d0`, `CUserLocal::TryDoingMeleeAttack@0x91e780`, `CUserLocal::TryDoingShootAttack@0x925a00`, `CUserLocal::TryDoingMagicAttack@0x92a240`, `CField::IsSkillForbiden@0x52a320`, `Field::SkillInfo::IsSkill@0x5657e0`, `get_skill_class@0x565320`, `get_job_level@0x47cb90`) |

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Summoned action-loader / frame-build parity | The client-owned summon loader seam remains in `SkillLoader`, and the already-landed frame-build parity remains intact (`120` ms summon delay fallback, per-frame `z` / `a0` / `a1`, authored `origin` / `lt` / `rb` metadata when present, client-style reverse playback, standalone summon-root acceptance, wrapper pruning around `icon*`/`action`/root `effect` nodes). WZ was rechecked first: mounted roots still show the same ownership surfaces (`Skill/3510.img/skill/35101005` wrapper + door branches, `Skill/501.img/skill/5010005/summon` custom actions beside `attack1..4`, `Skill/434.img/skill/4341006/summon` effect/repeat-only with `info/requireSkill = 4331002` and `req/4331002 = 30`, and deep link leaves like `Skill/3512.img/skill/35121006/info/requireSkill`, `.../psdSkill/35111009`, `Skill/3510.img/skill/35100008/psdSkill/35101010`). The mounted `skill` category still exposes no authored `sSummonedUOL`/`*UOL*` property names. Client evidence stayed second (`CActionMan::GetSummonedProp@0x41b830`, `SKILLENTRY::GetSummonedUOL@0x417f30`, `CActionMan::LoadSummonedAction@0x423100`, `CSummoned::_LoadEffect@0x754050`, `CSummoned::_UpdateEffect@0x754970`). This run keeps ownership in the same loader seam and extends fallback discovery from visible mounted data: direct candidate expansion still retains fallback root-skill extraction even when a normalized path token is present; fallback token parsing still skips `.img` image-id numbers; resolved leaf-value parsing still consumes delimiter, tokenized numeric, and path-token skill ids; and candidate harvest now also scans unknown string/UOL leaves under visible `info`/skill roots when they look like summon-owner path text, now including nested `info/*` descendants with bounded traversal depth so non-`sSummonedUOL` mounted fields still seed normalized summon source candidates. Unknown-leaf harvest also admits numeric leaf properties (`Int`/`Short`/`Long`), and nested owner-like `info/*` branches now parse linked skill ids from branch and child names (including empty subproperty leaves) so visible owner chains stay recoverable even when values are absent. Shared linked-id parsing now also consumes direct `WzUOLProperty` values so UOL-authored owner leaves feed the same visible-link fallback chain as string leaves. `req`/`psdSkill` branch parsing now also admits non-numeric child value/UOL leaves when those values carry explicit linked skill ids or path tokens, while guarding against false level-to-context promotion from authored `req/* = level` values. Context path assembly now also splits nested property-path segments so relative unknown-leaf path tokens normalize against the correct mounted skill branch. Focused source coverage in `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs` now also locks nested unknown-`info` string/UOL and numeric-leaf harvest, bounded-depth traversal behavior, nested owner-branch child-name extraction/rejection, unknown-info path-value harvest, embedded path plus tokenized-id extraction, relative unknown-leaf normalization, direct `WzUOLProperty` linked-id parsing (leaf + subproperty-child), `req`/`psdSkill` non-numeric child value/UOL parsing, and non-contextual level-value guard behavior from prior mixed-value/root seeding cases. Remaining gap: the simulator still does not mirror the client-owned authoring/population path of `SKILLENTRY::sSummonedUOL` itself; summons whose true owner appears only through that hidden field (and not visible WZ links, resolved target chains, tokenized candidate text, visible unknown-leaf path text, nested owner-branch child names, or non-numeric `req`/`psdSkill` value leaves) still require a dedicated client-data follow-up before this row can be marked implemented. | Keeping this row explicit prevents summon parity from being overstated by packet/runtime work alone: the loader still owns authoritative summon action/effect source selection, and this pass narrows remaining risk to hidden `sSummonedUOL` population rather than frame-build metadata, replay behavior, wrapper pruning, or mixed-token/unknown-leaf link recovery. | summon action-loader layer (`SkillLoader`, `SkillData`, `SummonRuntimeRules`, `CActionMan::GetSummonedProp`, `CActionMan::LoadSummonedAction`, `SKILLENTRY::GetSummonedUOL`, `CSummoned::PrepareActionLayer`, `CSummoned::_LoadEffect`, `CSummoned::_UpdateEffect`, `CSummoned::Update`, `CSummonedPool::OnCreated`, `Skill/3510.img/skill/35101005`, `Skill/3510.img/skill/35100008/{psdSkill,info/dummyOf}`, `Skill/3511.img/skill/35111002/summon/attackTriangle`, `Skill/3511.img/skill/35111011/summon/stand`, `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3511.img/skill/35111009/info/affectedSkill`, `Skill/3511.img/skill/35111010/info/affectedSkill`, `Skill/3512.img/skill/35121006/{psdSkill,req,info/requireSkill}`, `Skill/3512.img/skill/35121012/{info/requireSkill,req/35121005}`, `Skill/3310.img/skill/33101005/repeat`, `Skill/434.img/skill/4341006/{summon/repeat,info/requireSkill,req/4331002}`, `Skill/3511.img/skill/35111001/summon/die1`, `Skill/501.img/skill/5010005/summon`, `Skill/1311.img/skill/13111004/summon/hit`, `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs`) |

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

This helper-owner seam is currently under-tracked by the backlog even though both the client and simulator already expose concrete ownership here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow-partner and mirror helper actors | The simulator still keeps the client ownership split (`SkillData` / `SkillManager` still treat `Mirror Image` `4331002` as mirror-helper owned, while Shadow Partner stays on `special/*` helper-action catalogs), but this pass narrows the mirror live-layer seam again. WZ was rechecked first: `skill/433.img/skill/4331002` still has `action/0 = alert` plus `effect` and still has no `special`, while representative Shadow Partner roots (`skill/411.img/skill/4111002`, `skill/421.img/skill/4211008`, `skill/1411.img/skill/14111000`) still expose authored `special` helper branches. Client evidence stayed second: IDA for `CUser::GetMirrorSrcLayer` still returns the same five source layers (`UnderCharacter`, `OverCharacter`, `UnderFace`, `Face`, `OverFace`), `CUser::PrepareMirrorActionLayer` still recreates helper layers only when missing/flip-mismatched before reparenting under `m_pLayerUnderFace` and seeding rel-move from source-layer current-time + 200 ms, and `CUser::Update` still keeps helper layer objects and inserts current source-layer canvas instead of rebuilding helper objects per source change. The simulator now mirrors that update-side insert behavior more closely in the existing seam: mirror insert metadata still resets when prepared helper layer objects are recreated so stale layer-object insert metadata cannot latch live insert admission off on the replacement layer object, same-helper-layer live insert admission still allows source refreshes when source-parts identity/signature changes, and same-object live-insert paths still reject recorded helper-layer source-plane or overlay-plane mismatches so fallback remains on prepared snapshots. Missing source-plane or overlay-plane insert metadata now also re-admits same-helper-layer live insert in that same seam, so incomplete plane metadata cannot latch live insert admission off when source identity/timeline channels are otherwise stable. This pass also tracks source-layer current-time in the same insert-metadata seam and re-admits same-helper-layer live insert when source-layer time advances or regresses (loop/wrap), narrowing drift where source-layer canvas updates happen without source-parts identity/signature churn. Existing prepared-layer behavior remains intact (UnderFace overlay routing, prepared layer-object recreation on flip divergence, source/current-time snapshot tracking, same-facing placement anchoring, hidden-state prepared-layer release, and raw-action suppression/back-offset buckets). New seam tightening in this pass also extends mirror source-signature identity in `ComputeMirrorImageSourceLayerSignature` with visibility-owner channels (`VisibilityPriority`, ordered `VisibilityTokens`, and `SourcePart` object identity), so same-helper-layer live insert can re-admit source refreshes that previously looked unchanged when texture/offset channels were stable. Focused regression coverage in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` now also locks same-helper-layer timeline-driven live-insert re-admission (advance/regression), stable no-change rejection when source identity and source-layer time are unchanged, source/overlay mismatch rejection priority, source-plane/overlay-plane metadata-missing re-admission, and source-signature re-admission when visibility-owner channels change (priority/token identity). Remaining gap stays `Partial` and is now concentrated further at native object ownership: the simulator still does not own shared `IWzGr2DLayer` mirror objects, COM origin/current-time handles, full client layer-graph `InsertCanvas` object identity/lifetime semantics, full native source-layer current-time ownership on shared layer handles, or native avatar-scale filter handle ownership. | Clone-family jobs are not just tray icons or generic buff effects in the client; they own dedicated avatar-adjacent helper layers that participate in action preparation and per-frame user update. Tracking that seam explicitly keeps future parity work focused on helper-layer timing, ownership, and clone-family differentiation instead of accidentally treating shadow-partner and mirror-image visuals as solved just because the simulator already renders one WZ-backed helper actor | `PlayerCharacter.cs`, `CharacterAssembler.cs`, `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` (`skill/433.img/skill/4331002/action/0`, `skill/411.img/skill/4111002/special`, `skill/421.img/skill/4211008/special`, `skill/1411.img/skill/14111000/special`, `CUser::PrepareActionLayer`, `CUser::PrepareShadowPartnerActionLayer`, `CUser::PrepareMirrorActionLayer`, `CUser::GetMirrorSrcLayer`, `CUser::Update`, `is_back_action`, `is_alert_back_action`) |

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow Partner action-loader / frame-build parity | The shadow-partner helper row remains loader-owned in the same seam, and this pass narrows fallback frame-build/timing parity again without widening runtime state. WZ evidence stayed first: mounted `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, and `Skill/1411.img/skill/14111000/special/*` still publish the same helper branch families, while mounted `Character/00002000` still exposes concrete client-init piece rows with signed `delay` plus optional `move`/`flip` for fallback actions. In addition to the previously promoted mounted rows (including `doublefire`, `triplefire`, `doubleupper`, `eburster`, `screw`, `backspin`, `eorb`, `dragonstrike`, `airstrike`, `edrain`, `shot`, `fist`, `fireburner`, `coolingeffect`, `rapidfire`, `cannon`, `torpedo`, `bamboo`, `wave`, `blade`, `souldriver`, `firestrike`, `flamegear`, `stormbreak`, `shockwave`, `demolition`, `snatch`, `windspear`, `windshot`, `swingT2PoleArm`, `swingP1PoleArm`, `swingP2PoleArm`, `combatStep`), this row's fallback set now also keeps mounted definitions for `fake`, `flashBang`, `timeleap`, `owlDead`, `homing`, `recovery`, `backstep`, `holyshield`, `resurrection`, `dash`, `octopus`, `darksight`, `ghostwalk`, `ghoststand`, `ghostjump`, `ghostproneStab`, `ghostladder`, `ghostrope`, `ghostfly`, and `ghostsit`. Client evidence stayed second: `CActionMan::LoadShadowPartnerAction` (`0x423cf0`) still consumes helper action-table data produced from `CActionMan::Init` (`0x41beb0`) and uses that loader-side timing/event-delay surface before runtime playback. Existing landed behavior remains in place for linked-special source resolution, named-over-numeric storage precedence, mixed direct+linked helper frame-child ordering, mounted piece-row parsing, replay-tail handling, mounted hidden-row synthesis, and event-delay shaping (`negative-delay accumulation`, `total-last`, `zigzag=0`). This run closes another fallback seam in the same loader path: fallback synthesis now walks the dedicated client-init fallback candidate surface (raw-action rows plus fallback-only rows and generic helper rows), and resolver-owned built-in piece plans remain first priority for the promoted non-attack rows (`fake`, `flashBang`, `timeleap`, `owlDead`, `homing`, `recovery`, `backstep`, `holyshield`, `resurrection`, `dash`, `octopus`, `darksight`, `ghostwalk`, `ghoststand`, `ghostjump`, `ghostproneStab`, `ghostladder`, `ghostrope`, `ghostfly`, `ghostsit`) before remapped first-frame fallback, so missing mounted rows for the piece-backed subset continue to keep client-init `ClientEventDelayMs` shaping and transform metadata instead of degrading to remapped first-frame semantics, while the promoted `ghost*` subset now keeps resolver-owned client-init piece plans first (using mounted `Character/00002000/ghost*/1/*` timing) and only falls back to loader-owned alias remap when those plans cannot be built. Focused regression coverage in `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs` now locks fallback-admission for those twenty rows, with concrete synthesized signed-delay/event-delay output locked for the piece-backed subset and concrete synthesized ghost timing/event-delay output locked for the promoted `ghost*` subset (with remapped fallback still retained as last resort), alongside existing total-minus-last and negative-delay carryover expectations. Remaining gap is narrower and still real: additional hidden helper rows outside the promoted fallback set plus still-unrecovered `Character/00002000` piece rows remain, additional mounted action-table fields beyond modeled `action/frame/delay/flip/move/rotate/replay/event-delay` remain unconfirmed, and client-build/data-set differences in helper UOL/hidden-row ownership could still introduce extra loader-owned paths. | Shadow Partner remains a dedicated loader/runtime split where `CActionMan`-shaped helper action catalogs and frame tables are preserved on the loader side and local/remote helper playback consume that metadata without inventing new runtime ownership. This pass reduces fallback timing/transform drift further by keeping client-init piece timing plus move/flip shaping on a broader mounted-action fallback set, by keeping those rows on an explicit fallback-candidate admission path before remapped first-frame fallback, and by promoting the `ghost*` lane onto client-init piece-plan timing/event-delay synthesis before alias remap fallback. The remaining mismatch is concentrated on unrecovered hidden rows and unconfirmed extra action-table fields rather than already-modeled frame-build order/metadata/timing surfaces. | `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `PlayerCharacter.cs`, `RemoteUserActorPool.cs`, `SkillData.cs`, `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs`, `CActionMan::GetShadowPartnerProp@0x41ba60`, `CActionMan::LoadShadowPartnerAction@0x423cf0`, `CActionMan::Init@0x41beb0`, `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, `Skill/1411.img/skill/14111000/special/*`, `Character/00002000.img/{rain,paralyze,shoot6,arrowRain,arrowEruption,chargeBlow,rush,rush2,magic1,magic2,magic3,magic4,magic5,magic6,explosion,iceStrike,burster1,burster2,sanctuary,meteor,blizzard,genesis,brandish1,brandish2,chainlightning,blast,straight,handgun,somersault,doublefire,triplefire,fake,doubleupper,eburster,screw,backspin,eorb,dragonstrike,airstrike,edrain,shot,flashBang,fist,fireburner,coolingeffect,homing,rapidfire,backstep,timeleap,recovery,owlDead,holyshield,resurrection,dash,octopus,darksight,cannon,torpedo,bamboo,wave,blade,souldriver,firestrike,flamegear,stormbreak,shockwave,demolition,snatch,windspear,windshot,swingT2PoleArm,swingP1PoleArm,swingP2PoleArm,combatStep}` |

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Time Bomb, Vengeance, and ExJablin packet-application parity | The simulator now owns this `CUserLocal::OnPacket` family inside the existing local-utility packet seam without moving ownership elsewhere. WZ was rechecked first against the mounted export currently exposed through wzimg at `E:\MapleStory\WzExport\v95_2`: `String/Skill.img` still resolves `4341003` as `Monster Bomb` with the `explosion occurs 3 seconds after the charm is activated` description fragment, `31101003` as `Vengeance`, and `4120010` as `Expert Throwing Star Handling` with the `the next attack will always be a Critical Attack` fragment, while `3120010` still resolves to `Ultimate Inferno` instead of a WZ-side `Vengeance` alias. `Skill/434.img/skill/4341003` still publishes the authored Time Bomb `action`, `prepare`, and `special` branches together on the same skill node, and the mounted export still exposes only that one authored `4341003` `Monster Bomb` node for this family. `Item/ItemOption.img` still exposes the wider Time Bomb-shaped option-type-`52` family `020366`, `030366`, `040366`, `030371`, and `040371` with the matching invincibility templates, while other option-type-`52` rows such as `020396` and `040111` still remain unrelated non-invincibility options. Client evidence was rechecked second: `CUserLocal::OnTimeBombAttack` still decodes `(skillId, timeBombX, timeBombY, impactPercent, damage)`, passes the decoded `skillId` directly into `CSkillInfo::GetSkillLevel` and `TryDoingMeleeAttack` without an opcode-to-`4341003` fallback, restores the `1500 ms` base hit period before coat-option merges, gates impact on nonzero `nUserImpactDeg`, calls `CUser::MakeIncDecHPEffect(this, -nDamage, 0)`, and then sets the hit emotion/impact; `CUserLocal::ApplyInvicibleOption` still only mutates runtime invincibility state for `020366`, `030366`, and `030371` even though WZ also ships `040366` and `040371`; `CUserLocal::OnVengeanceSkillApply` still decodes exactly one `Int32` and only branches on legacy packet skill id `3120010`; and `CUserLocal::OnExJablinApply` still reads no payload and only arms `m_bNextShootExJablin = 1`. The landed seam still keeps packet ids `268` / `271` / `272` plus aliases, exact Time Bomb/Vengeance/ExJablin payload validation, signed Time Bomb damage, legacy-only Vengeance admission, nonzero Time Bomb impact gating, packet-origin target selection, the dedicated `SkillManager.TryApplyPacketOwnedTimeBombAttack` branch, the WZ-backed visible-potential item-option merge for `CharacterPart.ItemOptionIds`, and the narrow `MakeIncDecHPEffect` miss/heal mirroring. This pass now also splits visible WZ-family inference from client-owned runtime application inside the same helper path: visible potential lines can still infer the broader authored Time Bomb option family onto `CharacterPart.ItemOptionIds`, but only the client-confirmed `020366`, `030366`, and `030371` ids now feed the local Time Bomb invincibility-duration merge that mirrors `ApplyInvicibleOption`. This pass removes the last inferred Time Bomb alias bridge: an unrecognized first `Int32` in opcode-`268` payload now remains that decoded packet skill id, while known WZ/client aliases still resolve through the existing packet-owned skill-id catalog. Focused regression coverage in `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs` covers exact payload shapes, signed Time Bomb damage, nonzero impact gating, direct unknown Time Bomb skill-id retention, legacy-only Vengeance admission, empty ExJablin admission, the client-applied invincibility-id gate, and the retained broader WZ visible-family filter/order behavior that this row depends on. | These handlers matter because they bypass the normal local input/cast path even though the client gives them their own dispatcher and follow-up state. The simulator keeps a concrete owner for those packets, keeps the shared alias seam anchored to WZ first and client second, no longer accepts trailing bytes that the client packet readers would leave unread, no longer accepts non-client Vengeance packet ids, no longer treats only positive Time Bomb impact values as hit reactions, no longer clamps away signed Time Bomb damage payloads before the local reaction branch, no longer drops the zero/negative `MakeIncDecHPEffect` cases that hang off the same decoded damage field, still resolves visible potential lines through the WZ-authored `ItemOption.img` template/level seam instead of limiting `CharacterPart.ItemOptionIds` population to the confirmed Time Bomb invincibility family, and now also no longer over-applies the WZ-only `040366` / `040371` rows to the live Time Bomb hit-period merge when the client `ApplyInvicibleOption` branch would ignore them. The row is implemented because the remaining unconfirmed opcode-`268` alias bridge has been removed rather than preserved as simulator behavior; unknown decoded Time Bomb skill ids now stay unknown just as the direct client handler indicates. | packet-owned local skill-application layer (`CUserLocal::OnPacket`, `CUserLocal::OnTimeBombAttack`, `CUserLocal::OnVengeanceSkillApply`, `CUserLocal::OnExJablinApply`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::ApplyInvicibleOption`, `CUser::MakeIncDecHPEffect`, `CUserLocal::SetImpact`, `MapSimulator.PacketOwnedUtilityParity`, `LocalUtilityPacketInboxManager`, `SkillManager`, `PlayerCharacter`, `CharacterPart`, `ItemUpgradeUI`, `Character/00002000.img`, `String/Skill.img`, `Skill/434.img/skill/4341003`, `Item/ItemOption.img`, `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs`) |

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Combo counter / combo-command packet parity | `MapSimulator.PacketOwnedComboParity` now owns the simulator-side `OnIncComboResponse` bridge instead of leaving combo implied by the generic temporary-stat tray. The simulator decodes the packet-owned combo count from the combo inbox/chat transport, stores the owner count/timestamp, resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them on the same five-second alpha window, applies the client-matching fly-origin amplitude split across the least-significant digit run, and conditionally spawns the Aran combo-command attack/buff overlays with `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` first and WZ-backed named fallbacks for exports that only ship `ComboSmash`, `ComboDrain`, `ComboFenrir`, `ComboTempest`, and `ComboBarrier`. The row no longer depends on the generic `Combo` temporary-stat heuristics in `SkillManager`; it has a dedicated packet-owned HUD/effect owner, content load path, update lifetime, and draw path in `HaCreator/MapSimulator`. | If combo stays implied only by the temporary-stat tray, the backlog will overstate skill parity while missing the client's actual packet-owned combo counter, visible combo HUD/effect lifetime, and the extra combo-command visuals that hang off learned Aran skills. Tracking it explicitly keeps future work anchored to the real packet and draw owners instead of assuming the generic buff/cooldown surfaces already cover combo gameplay feedback. | packet-owned combo HUD/effect layer (`CUserLocal::OnPacket`, `CUserLocal::OnIncComboResponse`, `CUserLocal::DrawCombo`, `Effect/BasicEff.img/Combo`, `Effect/BasicEff.img/ComboCommand`) |

### 7. Shared skill-use animation-owner parity discovered by targeted `CAnimationDisplayer` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shared skill-use animation-owner parity | `MapSimulator.HandlePlayerSkillCast` still keeps non-melee local `ShowSkillEffect`-style casts on the dedicated direct-owner registration path (`TryRegisterLocalSkillCastThroughClientShowSkillEffectDirectPath`) while explicit caller-owned requests stay on `OnClientSkillEffectRequested -> TryRegisterAnimationDisplayerLocalSkillUseRequest`. WZ was rechecked first: shared cast branches remain authored on the same row surfaces (`Skill/434.img/skill/{4341003,4341007}/special`, `Skill/3212.img/skill/32121003/special`, `Skill/122.img/skill/1220006/{special,info/condition=damaged}`, `Skill/230.img/skill/2301002/specialAffected`, `Skill/532.img/skill/5320007/specialAffected`, `Skill/572.img/skill/5720005/specialAffected`, `Skill/3500.img/skill/35001001/keydownend`, `Skill/3510.img/skill/35101009/keydownend`, `Skill/3510.img/skill/35101004/effect`, `Skill/3511.img/skill/35111004/effect`, `Skill/3512.img/skill/{35121005,35121013}/effect`), mine/summon authoring remains split (`Skill/3310.img/skill/33101004` keeps `effect`/`special` with no `summon`, `Skill/3310.img/skill/33101008` keeps `summon/*` with `info/type=33`), unrecovered summon-style rows such as `Skill/311.img/skill/3111005`, `Skill/321.img/skill/3211005`, and `Skill/3512.img/skill/35121011` still expose `summon/*` with `info/type=33`, and `Skill/3510.img/skill/35101005` remains `info/type=33` with `effect/*` but no `summon/*`. Client evidence stayed second: `CUser::ShowSkillEffect` still carries caller-owned `nActionSpeed`, `bLeft`, `nLast`, `pPtOffset`; non-melee local callers (`CUserLocal::DoActiveSkill_Heal@0x93a830`, `...TownPortal@0x93be00`, `...SmokeShell@0x93cc30`) still pass `nActionSpeed = 0` with `nLast = 0x7FFFFFFF`; `CUserLocal::DoActiveSkill_Summon@0x93c0f0` still passes fixed `bLeft = 0` with `nLast = 0x7FFFFFFF` and `pPtOffset = 0`; and `CUserLocal::DoActiveSkill_SummonMonster@0x93eff0` remains the placement-offset caller path (`ShowSkillEffect(..., 6, 0, -1, &ptOffset)`). This pass keeps changes in the same seam and narrows fallback ownership: `SkillManager.HasClientShowSkillEffectSummonPlacementMetadata(...)` now requires concrete summon animation branches (`summon/spawn/attack/hit/remove`) instead of admitting `info/type=33` (`IsSummon`) by itself, so effect-only type-33 rows without summon branches no longer receive `SummonMonster`-style `nLast=-1` / placement-offset fallback, while unrecovered summon-authored rows still do. Placement shaping is also tighter in the same resolver seam: `ResolveClientLocalShowSkillEffectPlacementDistanceX(...)` now mirrors recovered `CUserLocal::DoActiveSkill_SummonMonster@0x93eff0` owner distance fallback (`pt.x += 125 * direction`) when summon-monster-family rows have no authored summon spawn distance, while keeping authored distance precedence and preserving non-summon-monster rows on authored-only distance. Existing explicit owner shaping remains intact (`EffectBranchLastIndex`/`FacingRightOverride` carried on request and cast paths, summon-family fixed-zero `bLeft`, mine ids `33101008`/`33101004` fixed-zero `bLeft` + default `nLast=0x7FFFFFFF` + null `pPtOffset`). Focused coverage in `UnitTest_MapSimulator/SharedSkillUseAnimationOwnerParityTests.cs` now also locks this new branch-metadata gate (`35101005` effect-only type-33 rejects summon-monster fallback; `3111005` summon-authored type-33 keeps summon-monster fallback) plus placement-distance fallback shaping (`3111005`/`3211005`/`35121011` default to client `125` when authored distance is missing, authored distance precedence remains intact, and non-summon-monster type-33 rows stay authored-only), alongside existing fixed-zero `bLeft`, mine owner shaping, and Monster Magnet move-action ownership assertions. Remaining gap: explicit client skill-id-to-caller mapping for all `DoActiveSkill_SummonMonster` permutations is still incomplete, placement-owner path details beyond currently modeled distance fallback (native foothold search/vertical settle and per-caller branches) remain approximated, overlay-parent and auto-flip ownership remain approximated beyond follow/fixed-facing options, and remote damaged-condition trigger/chance ownership still depends on decoded temporary-state hints instead of full `CUserRemote::OnHit` trigger ownership. | Shared cast presentation remains anchored to `CAnimationDisplayer::Effect_SkillUse` with explicit direct-vs-request ownership boundaries and caller-owned `nLast`/`bLeft`/`pPtOffset` shaping in the existing seam. The new summon-branch metadata gate removes a type-only over-admission path for summon-monster fallback while preserving recovered summon/mine ownership behavior, keeping this row `Partial` but narrowing remaining mismatch to unrecovered caller permutations and deeper overlay/remote trigger ownership. | shared skill-use animation layer (`CAnimationDisplayer::Effect_SkillUse`, `CAnimationDisplayer::LoadLayer`, `CAnimationDisplayer::RegisterOneTimeAnimation`, `CUser::ShowSkillEffect`, `CUser::ShowSkillSpecialEffect`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::DoActiveSkill_Heal`, `CUserLocal::DoActiveSkill_TownPortal`, `CUserLocal::DoActiveSkill_Summon`, `CUserLocal::DoActiveSkill_SummonMonster`, `CUserLocal::DoActiveSkill_SmokeShell`, `CUserLocal::SetDamaged`, `CUserRemote::OnHit`, `SkillUseEffectRequest`, `MapSimulator.HandlePlayerSkillCast`, `MapSimulator.TryRegisterLocalSkillCastThroughClientShowSkillEffectDirectPath`, `MapSimulator.AnimationDisplayerParity.ShouldRegisterLocalSkillCastThroughClientShowSkillEffectDirectPathForTesting`, `MapSimulator.AnimationDisplayerParity.ShouldRouteLocalSkillCastThroughClientSkillEffectRequestSeamForTesting`, `MapSimulator.AnimationDisplayerParity.ResolveAnimationDisplayerLocalRequestBranchSkillId`, `MapSimulator.AnimationDisplayerParity.ShouldIncludeAnimationDisplayerEffectBranchForTesting`, `SkillManager.ResolveClientShowSkillEffectEffectBranchLastIndex`, `SkillManager.ResolveClientLocalShowSkillEffectEffectBranchLastIndexOverride`, `SkillManager.ResolveClientShowSkillEffectFacingRightOverrideFromBLeft`, `SkillManager.ResolveClientLocalShowSkillEffectFacingRightOverride`, `SkillManager.ResolveClientLocalShowSkillEffectBLeftOverride`, `SkillManager.ResolveClientLocalShowSkillEffectPointOffsetOverride`, `SkillManager.ResolveClientLocalShowSkillEffectPlacementDistanceX`, `SkillManager.HasClientShowSkillEffectSummonPlacementMetadata`, `SkillManager.ResolveClientShowSkillEffectPointOffsetFromWorldPositions`, `SummonMovementResolver.ResolveSpawnPositionForInstance`, `RemoteUserActorPool.TryApplyThrowGrenade`, `SkillManager.ResolveClientLocalShowSkillEffectRequestedBranchNames`, `PreparedSkillHudRules.TryResolveClientKeyDownEndSkillEffectRequestId`, `SkillManager.ResolveClientKeyDownEndRequestedBranchNames`, `Skill/434.img/skill/4341003/special`, `Skill/434.img/skill/4341007/special`, `Skill/122.img/skill/1220006/special`, `Skill/230.img/skill/2301002/specialAffected`, `Skill/3212.img/skill/32121003/special`, `Skill/532.img/skill/5320007/specialAffected`, `Skill/572.img/skill/5720005/specialAffected`, `Skill/3500.img/skill/35001001/keydownend`, `Skill/3510.img/skill/{35101004/effect,35101005/effect,35101009/keydownend}`, `Skill/3511.img/skill/35111004/effect`, `Skill/3512.img/skill/{35121005,35121011,35121013}/{effect,summon}`, `Skill/3310.img/skill/{33101004,33101008}`, `Skill/{311,321}.img/skill/{3111005,3211005}/summon`, `UnitTest_MapSimulator/SharedSkillUseAnimationOwnerParityTests.cs`) |

### 8. Bullet animation-owner parity discovered by follow-up `CAnimationDisplayer` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Normal, magic, and afterimage projectile animation owner | WZ was rechecked first on this row's projectile-owned surfaces and still confirms authored frame metadata on the same branches the seam consumes: `Skill/200.img/skill/2001004/ball` keeps per-frame `origin`/`delay`, `Skill/230.img/skill/2301005/ball` keeps explicit authored frame branches with `origin`, `Skill/434.img/skill/4341003/flipBall` keeps the clockwise flipped branch, and representative normal/cash ammo nodes still keep direct numeric bullet-frame children on `02060000/bullet` and `05021000/bullet`. Client evidence stayed second: `CAnimationDisplayer::RegisterBulletAnimation` (`0x455410`) still registers `NormalBullet` into the bullet container with explicit `tStart/tEnd` + source/destination/target + z + effect UOL + weapon/bullet ids; `RegisterMagicBulletAnimation` (`0x455570`) still inserts only when the ball UOL string is non-empty; and `CAfterImageBullet::Update` (`0x477aa0`) still clones the active bullet layer into repeat child layers, rebinds origin/alpha vectors, copies the source canvas, and registers repeat animation state. This pass keeps implementation in the same owner seam and tightens parity further: registered owner timeline duration still clamps runtime clocks by authored loaded bullet/effect animation duration; non-authored normal-owner fallback still uses the registration source/destination distance clock (clamped with projectile lifetime) so `tEnd` shaping stays on the same source/destination owner surface as queued registrations; runtime owner position now prefers registered source/destination timeline interpolation whenever those anchors are finite (normal and queued) instead of drifting to live projectile coordinates; sync attachment now also remains active for finite registered timelines even when gameplay projectile coordinates are temporarily invalid; owner activation now guards non-finite source/destination anchor input by falling back through finite destination and finite live projectile coordinates before zero so registered main-layer origin capture does not start from invalid coordinates; stop capture remains a hard one-way detach from gameplay projectile identity (`IsDetachedFromProjectile` + cleared projectile link), and stop-position capture now also guards invalid live and invalid previous projectile coordinates by falling back to registered interpolation before zero rather than preserving stale invalid position state; owner removal still waits for registered timeline completion plus afterimage drain; repeat-layer parent ownership still prefers the active bullet main-layer object id (with previous-repeat fallback only when main-layer identity is unavailable), and repeat-layer source ownership now mirrors that same main-layer-first rule so repeat child registration stays anchored to the active bullet layer whenever available. Existing row behavior remains in place: registered UOL-driven `ball`/`flipBall` resolution, whitespace-gated magic registration, fire-time queued use-vs-cash lane retention, fire-time weapon-context retention, weapon-key fallback with derived `(weaponItemId / 10000) % 100`, clockwise bounds shaping, and afterimage interval/origin-shift continuity, afterimage alpha-vector start now preferring the active main bullet layer fallback start (with previous-repeat fallback when main-layer identity is unavailable), plus finite-vector guards on afterimage origin rebind (`ResolveBulletAfterimageLayerOrigin` zero fallback + `ResolveBulletAfterimageLayerOriginShiftEnd` invalid-travel preservation). Focused coverage in `UnitTest_MapSimulator/BulletAnimationOwnerParityTests.cs` now locks authored-duration timeline clamping (normal and queued), normal-owner distance/lifetime fallback when authored duration is absent, invalid-input one-tick fallback, detached-owner no-resync gating, afterimage repeat-parent identity preferring the active main bullet layer (with previous-repeat fallback when main-layer identity is unavailable), afterimage repeat-source identity preferring the active main bullet layer (with previous-repeat fallback when main-layer identity is unavailable), registered timeline-position preference for normal-owner runtime sync, sync retention with finite timeline anchors + invalid live projectile coordinates, owner-initial-position finite destination/projectile fallback guards, stop-capture interpolation fallback when live + previous coordinates are invalid, afterimage-origin zero fallback for non-finite owner positions, afterimage-origin-shift preservation when previous/current owner travel input is non-finite, and afterimage alpha-vector start preferring active main-layer fallback start (with previous-repeat fallback when main-layer identity is unavailable). Remaining gap: WZ recheck on the item-bullet seam still shows direct numeric bullet-frame children on representative normal/cash ammo surfaces (`02060000/bullet`, `05021000/bullet`) with no additional authored branch-key specialization exposed there, so the simulator still cannot reconstruct the client's deeper native `CBullet` specialization graph (including hidden/exhaustive per-weapon specialization tables) or full `CAfterImageBullet::Update` COM repeat-layer identity/registration/lifetime wiring (native COM layer handles plus true native repeat-registration ownership/lifetime semantics remain simulator approximations). | Projectile presentation remains explicitly owner-split from gameplay projectile state while now honoring authored frame timing in registered bullet timelines, keeping normal-owner non-authored timing and runtime travel anchored to the same source/destination registration seam as queued bullets, and enforcing explicit one-way detach from gameplay sync once stop capture occurs. This keeps normal bullets, magic balls, and afterimage repeat layers in the dedicated registered-owner model, including active-main-layer repeat parent/source ownership and active-main-layer-first afterimage alpha-vector start ownership (with previous-repeat fallback when main-layer identity is unavailable), with additional finite-anchor guards for owner activation and stop capture fallback behavior, without regressing fire-time lane/weapon context behavior. The row remains `Partial` because native bullet object-graph specialization and full COM identity semantics are still approximated. | projectile animation layer (`CAnimationDisplayer::RegisterBulletAnimation`, `CAnimationDisplayer::RegisterMagicBulletAnimation`, `CAfterImageBullet::Update`, `CBullet`, `CAnimationDisplayer::NormalBullet`, `CAnimationDisplayer::MagicBullet`, `SkillManager.SpawnProjectile`, `SkillManager.RefreshQueuedProjectileFireTimePresentation`, `SkillManager.DrawProjectiles`, `SkillManager.ResolveBulletAnimationPresentationDurationMs`, `SkillManager.ResolveBulletAnimationDistanceDurationMs`, `SkillManager.ResolveBulletAnimationPresentationAuthoredDurationMs`, `SkillManager.ResolveBulletAnimationInterpolatedPosition`, `SkillManager.ResolveBulletAnimationOwnerInitialPosition`, `SkillManager.TryResolveFiniteBulletAnimationPosition`, `SkillManager.ResolveBulletAnimationOwnerStopPosition`, `SkillManager.ShouldUseRegisteredBulletAnimationTimelinePosition`, `SkillManager.HasFiniteBulletAnimationTimelineAnchors`, `SkillManager.ShouldSyncBulletAnimationOwnerFromProjectile`, `SkillManager.ShouldRemoveBulletAnimationOwner`, `SkillManager.ResolveBulletAfterimageParentRepeatLayerObjectId`, `SkillManager.ResolveBulletAfterimageSourceLayerObjectId`, `ActiveBulletAnimationOwner.IsDetachedFromProjectile`, `SkillManager.ResolveRegisteredBulletPresentationVisualItemId`, `SkillManager.ResolveRegisteredBulletPresentationWeaponItemId`, `SkillManager.ResolveRegisteredBulletPresentationWeaponCode`, `SkillManager.ResolveBulletLayerDrawX`, `SkillManager.ShouldUseBulletLayerClockwiseBounds`, `SkillManager.ShouldRegisterMagicBulletAnimation`, `SkillLoader.LoadSkillAnimationByNormalizedPath`, `SkillLoader.LoadItemBulletAnimation`, `SkillLoader.ResolveItemBulletAnimationProperty`, `SkillLoader.EnumerateItemBulletAnimationBranchKeys`, `UnitTest_MapSimulator/BulletAnimationOwnerParityTests.cs`, `Skill/200.img/skill/2001004/ball`, `Skill/230.img/skill/2301005/ball`, `Skill/434.img/skill/4341003/flipBall`, `02060000/bullet`, `05021000/bullet`) |

### 9. Secondary skill animation-owner parity discovered by broad `CAnimationDisplayer` function scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Hook-chain, foothold, prepare, motion-blur, chain-lightning, and Tesla animation owners | The simulator keeps this owner family in dedicated secondary-owner seams (`AnimationEffects` prepare/foothold/hook-chain/chain-lightning/Tesla plus packet-owned/remote active-effect motion blur) and no longer treats it as a missing bucket. WZ was rechecked first on the same row-owned branches and still matches the seam inputs: `Skill/112.img/skill/1121001/info/pullTarget = 1` with authored `skill/1121001/mob` hook-chain canvases, `Skill/222.img/skill/2221006/ball/{0,1,2}` chain-lightning variants, `Skill/3511.img/skill/35111002/summon/attackTriangle` Tesla triangle, and `Item/Cash/0501.img/05010044/effect` publishing `spectrum=1`, `delay=600`, `interval=100`, `alpha=224` with `follow` absent (default `0`). Client evidence stayed second: `CUserLocal::TryDoingMonsterMagnet` still routes hook-chain with skill-owned UOL + facing-derived owner offset + resolved mob + attack window clamped to `>= 660 ms`; `Effect_SkillPrepare` / `RegisterPrepareAnimation` remains character-owned replacement behavior; and `CUser::SetActiveEffectItem` + `RegisterMotionBlurAnimation` still read `spectrum/follow/delay/interval/alpha`, terminate previous spectrum state, copy the five avatar layer handles (`Face`, `OverFace`, `UnderFace`, `OverCharacter`, `UnderCharacter`), and register the blur in `m_MotionBlur`. Runtime parity is tightened further in-place: local prepare and Monster Magnet owner behavior remains aligned; remote Monster Magnet hook-chain registration from melee packet mob-hit decode remains in place; local packet-owned motion blur still preserves persistent simulated layer-handle ids across re-registration while anchoring sampled layer-stack frames with assembled-frame offsets (`bounds.Left`, `bounds.Top - feetOffset`), WZ-authored interval cadence (authored `interval` now kept with only a `>= 1 ms` guard and no extra frame-floor clamp), and WZ `delay` retention/fade (`snapshotRetentionMs: definition.DelayMs` + shared linear alpha shape); and local/remote sample-time owner capture windows now include move-path duration tails instead of timestamp-only bounds so in-tail samples stay movement-owned before current-position fallback. Local sample source precedence remains passive-history first, then in-range recorded snapshot, then current actor fallback. Remote active-effect blur still keeps movement-snapshot sample-time owner capture, stable five-layer snapshot draw order, persistent per-layer simulated handle ids across re-register/remove cycles, WZ interval sampling, WZ delay-window retention, WZ alpha fade, follow-position/facing behavior, and clear-on-replace/remove lifecycle. Foothold / chain-lightning / Tesla seams remain unchanged on this row and continue to register from `tile`, `ball`, and `summon/attackTriangle`. Focused regression coverage in `UnitTest_MapSimulator/ActiveEffectMotionBlurParityTests.cs` now also locks local sample-time owner capture resolution/fallback (including out-of-history fallback), local passive-vs-recorded snapshot source precedence/fallback, local packet-owned layer-handle map reuse vs owner-change regeneration, remote movement-snapshot owner capture/fallback, remote layer-handle map reuse, client-ordered five-layer draw-order mapping, linear delay-window alpha fade shaping, duration-tail sample-window acceptance, local secondary motion-blur interval normalization to authored WZ cadence (`>= 1 ms`), and Monster Magnet hook-chain owner admission/attack-window clamp behavior (`pullTarget` + authored `mob` frames, window `>= 660 ms`). Remaining gap: both local and remote motion-blur owners still emulate client behavior with simulator-managed snapshots rather than literal client `IWzGr2DLayer` COM objects, so true native layer-handle identity and native `AnimationState` lifetime ownership semantics remain approximated instead of 1:1 mirrored. | Skill and summon gameplay can be correct while the visible client presentation is still wrong if these managers stay folded into generic projectiles or one-shot skill effects. Keeping the row explicit prevents Monster Magnet chains, ground/foothold attack strips, key-down prepare layers, active-effect blur, chain-lightning, and Tesla visuals from being treated as already covered by `Effect_SkillUse`, bullet animation, or summon runtime parity. | secondary skill animation layer (`CAnimationDisplayer::RegisterHookingChainAnimation`, `CAnimationDisplayer::RegisterChainlightningAnimation`, `CAnimationDisplayer::RegisterTeslacoilAnimation`, `CAnimationDisplayer::RegisterFootHoldAnimation`, `CAnimationDisplayer::RegisterMotionBlurAnimation`, `CAnimationDisplayer::Effect_SkillPrepare`, `CAnimationDisplayer::RegisterPrepareAnimation`, `CUserLocal::TryDoingMonsterMagnet`, `CUser::OnEffect`, `CUser::SetActiveEffectItem`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::TryDoingShootAttack`, `CUserLocal::TryDoingMagicAttack`, `CUserRemote::OnMeleeAttack`, `CUserRemote::OnShootAttack`, `CUserRemote::OnMagicAttack`, `CSummoned::OnAttack`, `CSummoned::ShowTeslaCoilTriangle`, `CMob::LoadLayer`, simulator seams: `AnimationEffects`, `SkillLoader`, `SkillManager`, `RemoteUserActorPool`, `ActiveEffectItemMotionBlurResolver`, `MapSimulator.AnimationDisplayerParity`, `UnitTest_MapSimulator/ActiveEffectMotionBlurParityTests.cs`) |

### 10. Local affected-area attack ownership discovered by broad IDA function scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Local attack-owned affected-area lifecycle parity | WZ was rechecked first on this row-owned seam and still supports the local owner split: explicit local owner families keep authored `info/type` (`3111003=2`, `12111003=1`, `12111005=4`, `2121007=1`, `2221001=1`, `2221007=1`, `2321008=1`, `22161001=1`, `22181002=1`, `32121004=1`, `4211006=1`, body family `32121003=11`, teleport-mastery family `2111007/2211007/2311007/32111010=16`), while non-owner neighbors remain published nearby (`12111004/1321007` as summon `type=33`, `2121006/2221006` as neighboring `type=1`, and `4221006/32121006` as `zoneType=invincible`). WZ element rows used by this seam remain stable (`12111003/12111005/2121007/3111003` publish root `elemAttr=f` with `info/dotType=burn`; `2221007` publishes `elemAttr=i` + `info/dotType=frostbite`; `2321008` publishes `elemAttr=h`). Client evidence stayed second: broad xrefs to `CAffectedAreaPool::GetInstance@0x52a480` still land in `CUserLocal::{TryDoingMeleeAttack@0x91e780,TryDoingShootAttack@0x925a00,TryDoingMagicAttack@0x92a240,TryDoingBodyAttack@0x930710,DoActiveSkill_MesoExplosion@0x942200}`; shoot still keeps explicit `3111003`; body still keeps explicit `32121003` plus `is_teleport_mastery_skill`; magic still keeps the recovered explicit owner family and neighboring non-owner checks; meso lifecycle remains owner-routed through `DoActiveSkill_MesoExplosion`; and `TryDoingMagicAttack` still shows split explicit duration constants (`3000` and `2760`) on its recovered fallback branch table. Implementation remains in the existing seams and narrows parity without widening runtime owners: local dispatch still flows only through `SkillManager.OnLocalAttackAreaResolved`; explicit-owner admission remains first-class before metadata fallback; known non-owner neighbors stay hard-rejected across lanes (`12111004`, `1321007`, `2121006`, `2221006`, `2301002`, `4221006`, `32121006`); non-explicit local candidates still require authored `info/type` (`ClientInfoType > 0`) plus area metadata; meso explicit owner routing for `4211006` remains; and body-lane non-explicit admission remains tightened (`bodyAttack` metadata or `areaAttack + info/type=1`, not generic non-empty effect strings). This pass tightened branch-owner resolution inside `SkillManager` without changing ownership scope: `ResolveLocalAttackAreaOwnerLane(mode, skillId)` now resolves explicit client owner families first (`3111003` shoot, explicit magic-owner family, body/teleport-mastery family, `4211006` meso) before mode fallback, so local affected-area dispatch no longer depends only on generic attack-type lane inference when those explicit owner ids execute through shared attack paths. Local dispatch gating by resolved owner count remains in place (`targets.Count > 0` for directional lanes, explosive-drop count > 0 for meso) while generic `OnAttackAreaResolved` behavior stays unchanged. Local create shaping remains in the same helper seam: type stays WZ `info/type` driven with explicit numeric fallback (`3111003=>2`, `12111005=>4`, explicit magic-owner family=>`1`, `4211006=>1`, body/teleport explicit types retained); start-delay ownership stays delay-derived for directional lanes with body/meso forced immediate plus recovered explicit-magic fallback when WZ `info/delay` is absent (`12111003/2121007/2221007/2321008/32121004=>180ms`); phase stays explicit-owner keyed; element shaping keeps `elemAttr` numeric/token parsing precedence with fallback to authored `dotType`/`affectedSkillEffect` aliases (including long-form aliases and compact-token guards); and explicit duration fallback keeps client-shaped millisecond constants via `durationOverrideMs` when authored duration resolves to zero (`3111003=>1000ms`, `12111003/2121007=>3000ms`, `2221007/2321008/32121004=>2760ms`). Stable local object ids (`1700000000+`) and cancel-family teardown via `ClientSkillCancelResolver` remain in place. Focused coverage in `UnitTest_MapSimulator/LocalAttackOwnedAffectedAreaParityTests.cs` now also locks explicit-owner lane resolution precedence over mode fallback and retained mode fallback for non-explicit skills, in addition to prior local-owner dispatch gating, cross-lane known non-owner rejection, explicit owner type fallback (`3111003`, `12111005`, `4211006`), explicit duration-override milliseconds (including 3000/2760 split), explicit magic start-delay fallback milliseconds (`180`) with unit conversion fallback (`2` units) when authored delay is missing, authored-delay precedence over fallback, and no-overapply of explicit duration/start-delay fallback to neighboring explicit magic ids. Remaining gap: exact client branch tables and predicate combinations are still not mirrored 1:1 for every individual `TryDoing*` / `DoActiveSkill_MesoExplosion` switch branch (including still-unrecovered explicit or neighboring skill ids outside the recovered subset), and phase/element packet-shape parity still needs deeper native mapping for unrecovered token/phase families beyond the currently recovered explicit rows. | Without this split, affected-area work can look complete because packet-owned remote areas are covered while local skill-cast area ownership remains implicit. Keeping this row separate prevents local player attack-side area spawning from being conflated with remote area packet lifecycle parity. | local skill/attack affected-area layer (CAffectedAreaPool::GetInstance, CUserLocal::TryDoingMeleeAttack, CUserLocal::TryDoingShootAttack, CUserLocal::TryDoingMagicAttack, CUserLocal::TryDoingBodyAttack, CUserLocal::DoActiveSkill_MesoExplosion, simulator seams: SkillManager, SkillManager.ResolveLocalAttackAreaOwnerLane(...), PlayerManager, MapSimulator, UnitTest_MapSimulator/LocalAttackOwnedAffectedAreaParityTests.cs, and backlog-5 packet-owned AffectedAreaPool rows) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, movement-coupled shoot families, and the shared `Effect_SkillUse` plus secondary `CAnimationDisplayer` animation owners rather than extending the generic cast path indefinitely.
3. Summon actor/runtime pass:
   Close the missing `CActionMan::LoadSummonedAction` loader seam together with the packet-owned `CSummonedPool` gaps and the `CSummoned::Update`-owned movement, touch-damage, Tesla-state, and position-sync gaps instead of only extending attack metadata and per-skill summon heuristics.
4. Clone-helper parity pass:
   Tighten the dedicated `CUser::PrepareActionLayer` / `CUser::Update` helper-layer seam for shadow-partner and mirror-image families instead of leaving clone behavior folded into generic buff or summon work.
5. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
6. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
7. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".


