# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`
- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Player skill execution parity

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
`CUserLocal::Update` at `0x937330` orchestrates several of these paths and confirms that key-down gauge updates, queued follow-up attacks, prepared-skill execution, repeat-skill handling, teleport, rush, flying-rush, rocket-booster start/end handling, movement-coupled shoot branches, timer-driven cancellations, and status-bar feedback are coordinated in one local-player update loop rather than isolated one-off handlers. A second IDA pass also showed that summon parity splits across three owners rather than only the actor/runtime shell: `CSummoned::Update` drives summon actor motion, active/passive vec-ctrl updates, body-contact damage checks, assist-type dispatch, Tesla Coil state handling, self-destruct timing, and final world-position sync; `CSummonedPool::OnPacket` dispatches created/removed/move/attack/skill/hit packets and `CSummonedPool::OnCreated` ties each summon to its owning user, skill id, level data, and local-user registration path; and `CActionMan::{GetSummonedProp,LoadSummonedAction}` own the WZ-side summon UOL lookup, per-action frame build, delay/z metadata, reverse-playback duplication, and cached action-entry reuse before the runtime ever updates or draws that actor.

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
The earlier backlog text tracked summon packet ownership and runtime behavior, but the targeted `CActionMan` scan shows another concrete client owner that was still missing from the document: summon action loading itself. `GetSummonedProp` resolves the summon UOL from `SKILLENTRY::GetSummonedUOL` through the resource manager, while `LoadSummonedAction` uses that property to resolve the action-specific node, enumerate each frame canvas, read per-frame `delay`, `z`, `origin`, and `rb` metadata, compute the frame bounds, and cache the assembled action entry under the summon action key. The same decompile also shows a distinct reverse-playback seam: when the source action node publishes the reverse flag, the client appends a reversed copy of the built frame list instead of leaving playback order entirely to the runtime. That means summon parity is not only `CSummoned::Update` timing and `CSummonedPool::OnPacket` ownership; it also depends on the loader that chooses the summon asset path, builds the frame table, and decides whether the runtime receives a forward-only or mirrored playback list.

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::Update` at `0x8fb8d0`

Notes:
The targeted re-check surfaced a client-owned helper seam that the backlog was not tracking explicitly. IDA decompilation shows `CUser::PrepareActionLayer` calling `CUser::PrepareShadowPartnerActionLayer` and `CUser::PrepareMirrorActionLayer` immediately after the main avatar action layer is prepared, which means clone-style helper visuals are owned by the user action-layer path rather than by the generic buff tray alone. The same `CUser::Update` re-check also exposes mirror-timing state (`m_tStartMirror`) alongside the broader avatar-update pipeline, which is enough evidence to keep shadow-partner and mirror helper behavior as a dedicated parity bucket instead of leaving it implicit inside generic skill or temporary-stat rows.

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::GetShadowPartnerProp` at `0x41ba60`
- `CActionMan::LoadShadowPartnerAction` at `0x423cf0`

Notes:
The current skill backlog already names the shadow-partner runtime/helper surface, but the targeted `CActionMan` pass shows there is still a distinct loader owner underneath it. `GetShadowPartnerProp` resolves the helper property from the skill id before action lookup, while `LoadShadowPartnerAction` caches by `(skillId, action)`, resolves either the named raw-action branch or the numeric fallback branch through `get_action_name_from_code`, enumerates the action canvases into cached frame entries, reads frame-owned metadata from the canvas properties, and appends the mirrored interior-frame tail when the loader's replay table marks that action. That means shadow-partner parity is split the same way as summons, pets, employees, dragons, mobs, NPCs, and taming-mob helpers elsewhere in the backlog set: `CUser::PrepareShadowPartnerActionLayer` and the simulator helper runtime own placement and playback state, while `CActionMan` owns the helper action catalog and the frame table those runtime paths consume.

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another client-owned skill family that was not represented anywhere in the backlog text. `OnTimeBombAttack` is not just a generic damage callback: the decompile shows it decoding a skill id plus attack/action data, resolving the local skill level, calling `TryDoingMeleeAttack`, then applying hit-period/invincibility and impact side effects before forcing the avatar hit reaction. `OnVengeanceSkillApply` is also more specific than a plain temporary-stat refresh because it gates on skill id `3120010` and then calls `DoActiveSkill_MeleeAttack` through the local skill runtime, while `OnExJablinApply` arms the `m_bNextShootExJablin` flag for the next ranged-shot branch rather than behaving like a generic buff bit. That is a dedicated packet-owned skill-application seam between the broad cast/update families and the existing temporary-stat or helper rows.

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`

Notes:
The same targeted `CUserLocal::OnPacket` scan surfaced another local skill owner that the backlog still was not naming. `OnIncComboResponse` is very small but concrete: it decodes the server-authored combo count, stores it in `m_nCombo`, stamps `m_tLastSetCombo`, and immediately routes through `DrawCombo`. The `DrawCombo` decompile then shows that combo is not just another temporary-stat label. The client resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them alive on a five-second alpha window, applies fly-origin motion to the combo vectors, and conditionally spawns extra `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` layers when the corresponding Aran combo-command skills are learned. A repo search across `HaCreator/MapSimulator` still only surfaced the generic `Combo` temporary-stat labeling in `SkillManager` and did not surface any simulator-side combo packet owner, combo HUD/effect loader, or `ComboCommand` runtime, so this remains a real missing parity bucket rather than stale wording.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Player skill execution parity

The simulator has a generic skill runtime. It does not yet mirror the client's named `CUserLocal` behavior families.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Generic skill casting | The simulator now loads the full player skill catalog from `Skill.wz`, learns active skills into the runtime, supports direct cast invocation from the skill window in addition to hotkey casting, keeps GM/SuperGM skill-book resolution compatible with both `900.img` and `910.img` layouts while focusing those jobs on their own job-book window instead of the global advancement catalog, evaluates Big Bang `common` formulas in IMG-mode data sets so skills like `Power Strike` are treated as castable attacks instead of passive placeholders, mirrors skill effect anchoring/hitbox fallback by facing direction for asymmetric melee effects, sanitizes unsupported SpriteFont glyphs in the skill window so WZ-backed names no longer crash row rendering, and resolves cast SFX from `Sound/Skill.img` so skill use now plays the corresponding `Use` or fallback attack sound through the simulator audio path | This closes the previous "can only use the current job's limited skill book" runtime/UI gap and keeps special-job skill tabs populated and executable across client data variants | `SkillManager.cs`, `SkillLoader.cs`, `SkillUIBigBang.cs`, `MapSimulator.cs`, `UIWindowLoader.cs` |
| Implemented | Buff stat application | Buff logic now applies `PAD`, `PDD`, `MAD`, `MDD`, `ACC`, `EVA`, `Speed`, and `Jump` directly to the player build and cleanly removes them when buffs expire or refresh | Active buff skills now change simulator stats and movement-relevant values instead of only a partial subset | `SkillManager.ApplyBuffStats` |
| Partial | Buff icon / temporary-stat UI parity | Active buffs still drive the WZ-backed status-bar tray with live duration countdowns sourced from `UI/BuffIcon.img`, the tray still supports client-style right-click cancellation directly from the live buff list, `UILoader` still normalizes and loads the full `BuffIcon.img` icon surface, and the simulator still consumes the authored `name` strings plus `buff/*` traversal order from that same catalog so the nine observed stat-family icons (`incEVA`, `incPAD`, `incPDD`, `incMAD`, `incMDD`, `incACC`, `incCraft`, `incJump`, `incSpeed`) keep their WZ-authored display names and ordering instead of a duplicated simulator-only table. The broader non-`BuffIcon.img` family catalog still resolves through the recovered v95 `SecondaryStat` layout, and this run stays inside `SkillManager.GetStatusBarBuffEntries` again without changing tray-lane ownership: authored tooltip and family naming now fall back through the existing `SkillDataLoader` fallback-stat label surface whenever the row-specific explicit override table does not already own the label, so later `indie*` and percent-style properties keep their authored owner text and authored per-property tooltip names even when an earlier support or earlier non-icon family still owns tray ordering and icon selection. WZ was rechecked directly in the mounted export for the affected mixed and independent-property cases: `UI/BuffIcon.img/buff` still exposes the same nine ordered authored icon families, `Skill/3112.img/skill/31121001/common` still publishes `cr`, `ignoreMobpdpR`, then `bdR`, `Skill/572.img/skill/5720008/common` still publishes `criticaldamageMin`, `criticaldamageMax`, `cr`, and `bdR`, `Skill/3511.img/skill/35111013/common` still publishes `pddR`, `mhpR`, `mmpR`, `cr`, `damR`, and `expR`, `Skill/422.img/skill/4220009/common` still publishes `mesoR`, `Skill/7000.img/skill/70000049/common` still publishes `dropR`, `Skill/7100.img/skill/71000321/common` still publishes `ignoreMobDamR`, `Skill/000.img/skill/0000110/common` still publishes `mhpR` and `mmpR`, `Skill/2112.img/skill/21120001/common` still publishes `criticaldamageMin`, `Skill/2311.img/skill/23111005/common` still publishes `terR`, and `Skill/530.img/skill/5301003/common` still publishes `indieSpeed`. The authored-family override table plus the new fallback-label bridge now cover those later explicit owner properties together, including `mhpR` / `mmpR`, `pddR` / `mddR` / `accR` / `evaR`, `asrR` / `terR`, `criticaldamageMin` / `criticaldamageMax`, `damR`, `dropR`, `mesoR`, `bdR`, `ignoreMobpdpR`, `ignoreMobDamR`, and authored `indie*` tooltip rows such as `indieSpeed` and `indieMhpR`, so `Boss Damage`, `Elemental Resistance`, `Abnormal Status Resistance`, `Ignore Enemy DEF`, `Ignore Enemy Damage Reduction`, `Critical Damage (Min)`, `Critical Damage (Max)`, `Drop Rate`, `Meso Rate`, `Independent Speed`, `Independent Max HP`, and the other authored later-property labels no longer collapse back to one generic family name inside the tooltip just because an earlier family still owns icon chrome or tray placement. Focused regression coverage is now on disk in `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` for authored tooltip ordering and naming across `criticaldamageMin` + `criticaldamageMax` + `bdR`, `terR` + `indieSpeed`, `indieMhpR`, and explicit-override preservation for `pddR`. The remaining gap is narrower but still real: exact tray order is client-backed for the WZ-authored icon families plus the recovered v95 `SecondaryStat` slice, but some families still have no direct recovered v95 temp-stat owner or still appear to depend on runtime-owned grouping or icon state beyond `UI/BuffIcon.img` and the currently parsed skill metadata, especially exact icon ownership for standalone `Boss Damage`, mixed reward/offense cases whose final lane choice is still not explicit in the recovered v95 surface, and any family whose final client lane or icon still depends on runtime-owned grouping the current WZ plus recovered-client evidence does not expose. | Buff-heavy jobs are visible on the HUD, can be cleared without waiting for expiry, and no longer collapse as often into a fully generic tray entry while tooltip text and family ownership stay closer to the WZ stat-family surface. The known nine `BuffIcon.img/buff/*` families still use the WZ-authored names and family order directly, Big Bang-era payload families such as `mhpR` / `mmpR`, `pddR` / `mddR` / `accR` / `evaR`, `damR` / `indieDamR`, `criticaldamageMin` / `criticaldamageMax`, `expR` / `dropR` / `mesoR`, `bdR`, `asrR` / `terR`, both `ignoreMobpdpR` and `ignoreMobDamR`, and authored `indie*` rows such as `indieSpeed` and `indieMhpR` now stay on their resolved tray lanes without depending on generic tooltip text heuristics first. Named multi-stat support buffs also no longer drift to whichever family sorts first: the tray keeps the authored support-family owner for family display, while the tooltip stat list now follows authored property order and explicit per-property names before appending leftover support labels. The remaining gap is now concentrated on exact client lane or icon ownership for the families that still are not directly observable from `UI/BuffIcon.img` plus the recovered v95 `SecondaryStat` surface alone. | `SkillManager.ActiveBuffs`, `SkillManager.ConfigureBuffIconCatalog`, `SkillManager.GetStatusBarBuffEntries`, `SkillLoader.CreateLevelData`, `SkillLoader.NormalizePassiveStatAliases`, `SkillDataLoader.TryResolveFallbackStatPresentation`, `RemoteAffectedAreaSupportResolver.cs`, `StatusBarUI.cs`, `UILoader.cs`, `MapSimulator.Content.cs`, `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs`, `docs/GMS v95.h` (`UI/BuffIcon.img`, `UI/BuffIcon.img/united/buff`, `UI/BuffIcon.img/united/anniversary`, `Skill/*/common/indiePad`, `Skill/*/common/indieMad`, `Skill/*/common/indieAcc`, `Skill/*/common/indieEva`, `Skill/*/common/indieSpeed`, `Skill/*/common/indieJump`, `Skill/*/common/indieMhp`, `Skill/*/common/indieMmp`, `Skill/*/common/indieAllStat`, `Skill/*/common/pddR`, `Skill/*/common/mddR`, `Skill/*/common/accR`, `Skill/*/common/evaR`, `Skill/*/common/asrR`, `Skill/*/common/terR`, `Skill/*/common/criticaldamageMin`, `Skill/*/common/criticaldamageMax`, `Skill/*/common/damR`, `Skill/*/common/indieDamR`, `Skill/*/common/indieMhpR`, `Skill/*/common/indieMmpR`, `Skill/*/common/expR`, `Skill/*/common/dropR`, `Skill/*/common/mesoR`, `Skill/*/common/bdR`, `Skill/*/common/ignoreMobpdpR`, `Skill/*/common/ignoreMobDamR`, `Skill/000.img/skill/0000110/common`, `Skill/2112.img/skill/21120001/common`, `Skill/2311.img/skill/23111005/common`, `Skill/3112.img/skill/31121001/common`, `Skill/3511.img/skill/35111013/common`, `Skill/422.img/skill/4220009/common`, `Skill/530.img/skill/5301003/common`, `Skill/572.img/skill/5720008/common`, `Skill/7000.img/skill/70000049/common`, `Skill/7100.img/skill/71000321/common`, `Skill/*/info/switchDamtoMP`, `Skill/*/info/invincible`, `SecondaryStat`, `CUIStatusBar::Draw`, `CUserLocal::Update`) |
| Partial | Melee / ranged / magic resolution | The runtime now preserves WZ `lt/rb` attack bounds, resolves melee and magic targets with separate ordering rules, gives projectile skills family-specific collision ordering plus homing steering, keeps homing active even when WZ `ball` data also marks the projectile as exploding, detonates WZ `ball/explode` projectiles on impact so nearby mobs within `explodeRadius` are resolved under the same effective target cap, respects the effective projectile hit cap from both `level/mobCount` and `ball/mobCount`, routes WZ `info/type` through the skill-family classifier so no-ball spell and shoot families no longer collapse into melee resolution by default, now also treating client-confirmed shoot-family `info/type=52` skills such as Aran `21120006` as ranged even when WZ omits a `ball` node, routes WZ `info/type=32` plus `info/massSpell=1` spell families such as Mage `Slow` / `Seal` back onto the magic lane instead of leaving those no-ball area spells on melee defaults, and now also keeps the client-confirmed Outlaw no-ball shoot-family helpers `5211004` / `5211005` on the ranged lane despite their melee-looking WZ `info/type=1`, while the shared shoot-family resolver also carries `5211006` so the loader and runtime use one client-backed table instead of drifting apart. Direct no-projectile ranged skills still get their own fallback hitbox and target ordering instead of reusing the melee branch, route through the client-shaped `DoActiveSkill_ShootAttack` fallback split so melee-first families probe the close-range lane before falling back to ranged resolution while the client-confirmed direct-shoot families stay on the shoot lane, preserve the special client one-offs where Gunslinger `Double Fire` (`5001003`) and Outlaw `Triple Fire` (`5210000`) still attempt the melee lane before falling back to shoot despite otherwise living in the shoot-family helper table, and thread that same melee-first-vs-shoot preference through deferred moving-shoot preferred-target selection instead of always snapshotting a ranged-only target. Projectile families still route WZ `info/rectBasedOnTarget=1` through an additional target-anchored rectangle after first impact so client rect-attack families such as `3001004`, `3101005`, and `5201002` no longer depend on raw projectile overlap alone, re-resolve chained follow-up targets from the live impact/serial origin using the WZ `range` bounce radius plus nearest-neighbor chaining instead of only replaying the initial overlap snapshot, stage multi-shot projectile release from WZ `ballDelay` / `ballDelayN` timing instead of front-loading every projectile on cast start, assign WZ `info/multiTargeting=1` projectile volleys to distinct live targets inside the authored ranged hitbox before spawn so skills such as `2301005`, `3001004`, `5301000`, and `20021166` no longer fire every shot through the same generic spread-first path when valid separate targets already exist, derive projectile collision bounds from the live WZ ball frame size and origin while sweeping those bounds across the projectile's previous-to-current travel step so fast or large projectiles no longer rely on a single centered point-instant overlap check, thread per-target client damage override/decay branches through the live hit order so `Arrow Bomb: Bow` (`3101005`) uses its WZ `x` damage lane instead of the generic `damage` fallback, WZ-published x-based chain falloff still applies where the skill data actually publishes that decay value, and the remaining client-owned `SKILLENTRY::AdjustDamageDecRate` fixed tables now also drive `Energy Orb` (`5121002`), `Iron Arrow: Crossbow` (`3201005`), `Piercing` (`3221001`), `Ricochet` (`33101001`), and `Blade Fury` (`4341004`) through their exact client multipliers instead of flattening those families back onto one generic damage percent. This run also mirrors more of the client-owned shoot-entry precheck and ammo lane: ranged attack casts still validate the expected shooting-weapon family before firing, still skip that weapon gate for the confirmed client exceptions that do not use a shooting weapon at all (`5121002`, `11101004`, `15111006`, `15111007`, `21100004`, `21110004`, `21120006`, `33101007`, `4121003`, `4221003`), now also reject true shooting-weapon ranged entries while the player is attached to a ladder or rope unless the mounted taming-mob owner is the Wild Hunter jaguar family mirrored by `Jaguar Rider` (`Skill/3300.img/skill/33001001/info/type = 13`), now parse WZ `level/common/bulletConsume` alongside `bulletCount` so client-ammo-heavy skills such as `Skill/411.img/4111005/common/bulletConsume = 3` and `Skill/412.img/4121006/common/bulletConsume = 200` no longer collapse to a generic single-ammo assumption, now also parse WZ `common/itemConsume` for the client-confirmed Outlaw elemental shot helpers `5211004` / `5211005` so the live inventory-ammo resolver can require the authored pellet family instead of accepting any generic bullet stack, and now also parse WZ `info/requireSkill` plus `req/*` support links so client shoot-entry branches such as Wind Breaker `13111006` / `13111007` stay gated behind their authored `13111005` support state instead of entering the generic ranged lane as standalone attacks. The support-state gate now also consumes the full authored required-skill set instead of only the first parsed support id, so future shoot-entry families that publish multiple `req/*` links stay aligned to the loader's complete WZ surface rather than a simulator-only first-entry shortcut. When an inventory runtime is present the simulator now resolves a concrete compatible USE-slot with enough stack quantity instead of only checking aggregate ammo presence, doubles the resolved per-cast ammo requirement while Shadow Partner is active to stay closer to the client-owned `GetProperBulletPosition` consumption lane, now mirrors the client-owned active-bullet-index branch by resolving the marked active projectile item as an exact item-id requirement instead of silently falling back to a different compatible family, still allows another stack of that same selected item id to satisfy the cast because the client scans by item id rather than a single slot index, and now also mirrors the CASH-cosmetic active-index branch by treating a marked active CASH projectile as an exact item-id requirement first, allowing another stack of that same CASH item id to satisfy the cosmetic lookup when the marked slot cannot be used, and only then falling back to generic family scan when no active CASH item is marked (`5020xxx` arrows, `5021xxx` throwing stars, `5022xxx` bullets), while carrying the client helper's `nWeaponItemID == 1472063 -> 2060xxx` arrow one-off into compatible-ammo matching instead of flattening everything to weapon-code-only rules. The live projectile runtime now also snapshots those resolved `pnItemID` / `pnCashItemID` ownership outputs per projectile at spawn time, so delayed `ballDelay` releases keep the exact USE ammo id plus the matched CASH cosmetic family that the cast resolved instead of dropping that client-owned selection as soon as the precheck returns, and projectile draw now reuses that snapshotted CASH-or-USE item ownership as a bullet-art fallback whenever the skill projectile lacks authored `ball` frames instead of leaving those delayed shots with no client-shaped ammo visual at all. Focused regression coverage is now on disk in `UnitTest_MapSimulator/ClientShootAmmoResolverParityTests.cs` for exact active USE/CASH ammo-id selection, same-item stack fallback, and the `1472063` special arrow compatibility one-off, `UnitTest_MapSimulator/ShootAttackRequiredStateParityTests.cs` for distinct support-id resolution and all-required-support-state gating, `UnitTest_MapSimulator/ProjectileAmmoVisualParityTests.cs` for queued and no-authored-ball projectile visual-item resolution, plus `UnitTest_MapSimulator/ShootAttackStateRestrictionParityTests.cs` for the shooting-weapon ladder-or-rope gate and the Wild Hunter jaguar exception. | Skills are closer to the client across both classifier and execution seams: no-ball mass-spell debuffs no longer inherit melee ordering, the missing Outlaw shoot-family helpers no longer collapse back onto the melee lane just because WZ publishes `info/type=1` without `ball`, no-projectile ranged skills now use the client-shaped melee-first vs direct-shoot split instead of one unconditional ranged branch, deferred moving-shoot targeting no longer snapshots only the ranged lane when the live client family would try a close-range strike first, and direct shoot casts now reject the wrong weapon family or missing ammo in the same high-level place the client gates `DoActiveSkill_ShootAttack` before attack resolution while also consuming a real compatible ammo stack rather than a simulator-wide count. The ladder/rope shoot-entry seam is closer too: true shooting-weapon ranged attacks now stop on ladder or rope unless the active mounted owner is the Wild Hunter jaguar family, matching the direct client branch that still allows the jaguar exception while skipping the generic shoot lane. The elemental Outlaw helpers now go one step further: WZ-authored `itemConsume = 2331000 / 2331001` is carried into the level-data seam and used to filter the resolved USE slot by the same fire-pellet (`2331xxx`) vs ice-pellet (`2332xxx`) family split that the client applies inside `GetProperBulletPosition`, so those skills no longer accept an arbitrary gun-bullet stack just because the weapon family matches. The shoot-entry gate is also closer to the client for required support-state families: WZ-authored `info/requireSkill` plus `req/*` links now flow through the loader seam, Wind Breaker `13111006` / `13111007` no longer cast unless `13111005` is actually active, and the runtime no longer truncates multi-link support-state checks to the first parsed `req/*` entry before entering the ranged lane. The shoot-ammo resolver is also closer to the client-owned selection path itself: a marked active bullet now means "find this exact projectile item id or fail the ammo branch" instead of "prefer this slot and then fall back to anything compatible", another stack of that same selected item id can still satisfy the cast because the client scans the whole USE inventory by item id, compatible CASH projectile cosmetics are now surfaced through the same family scan instead of being entirely ignored, a marked active CASH projectile now follows the same exact-item-id-first behavior (with same-item stack fallback) before falling back to generic family scan, the special client arrow one-off for weapon `1472063` no longer collapses onto the generic weapon-code table, delayed projectiles now retain the exact resolved USE/CASH ownership snapshot that `GetProperBulletPosition` produced for the cast instead of losing it once the runtime moves on to later skill requests, and projectile visuals now reuse that snapshotted ownership when the skill itself does not publish usable `ball` art. The remaining gap is narrower and is now centered on the last client-only `GetProperBulletPosition` outputs and precheck side branches beyond the newly modeled exact active-item, elemental-pellet, cash-projectile-family, special-weapon, full required-support-buff set, per-projectile ownership snapshot, missing-ball visual fallback, and shoot-weapon ladder/jaguar gate seams, especially exact cosmetic bullet-position placement, any remaining active-index metadata beyond exact USE/CASH item id selection and same-item fallback, the last vehicle or one-time-action nuances that still live inside the client precheck, and edge-case timing or packet-owned branch state that still lives outside WZ-authored geometry | `SkillManager.ProcessMeleeAttack`, `ProcessMagicAttack`, `ProcessNoProjectileRangedAttack`, `SpawnProjectile`, `CheckProjectileCollisions`, `ResolveQueuedSerialTargets`, `ResolveDeferredPreferredTargetMobId`, `SkillManager.DrawProjectile`, `SkillManager.ResolveProjectileVisualItemId`, `SkillLoader.ParseSkillInfo`, `SkillLoader.CreateLevelData`, `ClientShootAttackFamilyResolver`, `ClientShootAmmoResolver`, `PlayerSkillStateRestrictionEvaluator.cs`, `UnitTest_MapSimulator/ClientShootAmmoResolverParityTests.cs`, `UnitTest_MapSimulator/ShootAttackRequiredStateParityTests.cs`, `UnitTest_MapSimulator/ProjectileAmmoVisualParityTests.cs`, `UnitTest_MapSimulator/ShootAttackStateRestrictionParityTests.cs` (`Skill/3300.img/skill/33001001/info/type`, `Skill/521.img/5211004/info/type`, `Skill/521.img/5211004/common/itemConsume`, `Skill/521.img/5211005/info/type`, `Skill/521.img/5211005/common/itemConsume`, `Skill/521.img/5211006/info/type`, `Skill/411.img/4111005/common/bulletConsume`, `Skill/412.img/4121006/common/bulletConsume`, `Skill/1311.img/skill/13111006/info/requireSkill`, `Skill/1311.img/skill/13111006/req/13111005`, `Skill/1311.img/skill/13111007/info/requireSkill`, `Skill/1311.img/skill/13111007/req/13111005`, `Character/*/*/bullet`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::DoActiveSkill_MagicAttack`, `CUserLocal::GetProperBulletPosition`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `is_shoot_skill_not_switched_to_melee_attack@0x6edcf0`, `is_shoot_skill_not_using_shooting_weapon@0x6edbf0`, `is_shoot_skill_not_consuming_bullet@0x6eeaf0`, `is_correct_bullet_item@0x861360`, `is_correct_bullet_cashitem@0x6ebcf0`, `is_pellet_fire_item@0x861300`, `is_pellet_ice_item@0x861330`, `SKILLENTRY::AdjustDamageDecRate`) |
| Partial | `DoActiveSkill_*` family parity | Generic movement, summon, and prepare/charge families now execute through the existing `SkillManager` seams instead of falling back to one generic cast lane. Movement-family dispatch now separates teleport snaps, grounded rush, flying-rush glide, jump-rush launches, and bound-jump starts from the client-confirmed `TryDoingTeleport` / `TryDoingRush` / `TryDoingFlyingRush` / `DoActiveSkill_BoundJump` splits; Teleport Mastery riders authored as `info/affectedSkillEffect=bodyAttack&&stun` stay off the direct-cast surface and fire as passive follow-up data after the owning teleport resolves; WZ-linked helper branches with `info/affectedSkill` plus `info/affectedSkillEffect` no longer surface as standalone castable actives; Battle Mage aura recasts cancel or replace the active aura instead of stacking; aura-dot riders keyed by `info/affectedSkillEffect=dot` plus `info/dotType=aura` pulse once per second while the owning aura is active; WZ `info/mesoExplosion` skills now resolve pickup ownership against the live local character id before consuming nearby mesos inside the authored `lt/rb` box and then applying their attack payload; WZ `info/zoneType=invincible` skills create a persistent local protection zone; admin-skill execution now enforces GM/SuperGM ownership; bound-jump gating now respects `info/avaliableInJumpingState=1`, client-owned grounded exceptions such as `11101005`, `33001002`, and `35101004`, Wild Hunter swim/fly rejection, and WZ `info/requireHighestJump=1`; moving-shoot startup still resolves from the strongest loaded movement-action branch, deferred moving-shoot execution still mirrors the client-owned queued-level gate by dropping the delayed shot when the live learned level no longer matches the stored moving-shoot entry level instead of silently re-scaling the queued cast at fire time, and this pass tightens the queue-time action-owner chooser one step closer to the client `m_nAttackActionType` seam instead of leaving every client-type-`3` request on one hard-wired shoot-family fallback. WZ re-check still shows the same moving-shoot skill surface already tracked by the row (`Skill/3212.img/skill/32121003/info/{type=11,movingAttack=1}`, `Skill/3300.img/skill/33001000/info/{type=2,movingAttack=1}`, `Skill/3310.img/skill/33101001/info/{type=2,movingAttack=1}`, `Skill/3311.img/skill/33111001/info/{type=2,movingAttack=1}`, `Skill/3312.img/skill/33121005/info/{type=2,movingAttack=1}`, `Skill/3312.img/skill/33121009/info/{type=2,movingAttack=1,rapidAttack=1}`) and still does not publish the shared client attack-action table, while the direct IDA re-check of `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare` at `0x912f90` shows the client snapshotting `m_nAttackActionType` from the live user state before calling `get_random_shoot_attack_action(...)`. The simulator now follows that client-owned ownership split more closely inside the same deferred seam: skills whose WZ `info/type` is `3` still bypass authored `action/*` appointment rows, but the fallback lane no longer assumes every unresolved client random-action request should degrade to `shoot1` / `shoot2` / `shootF`. Type-`3` deferred moving-shoot now resolves its queue-time fallback owner from the live current action family first, using the normalized current raw action or action name to select the corresponding shared client family (`shoot`, one-handed stab, two-handed stab, one-handed swing, two-handed swing, or polearm swing), and only falls back to weapon-family inference when the live action owner is unavailable. When neither live action nor weapon family can recover a client family, the deferred arm still aborts instead of silently inventing a simulator-only owner. Queued moving-shoot `nActionSpeed` now mirrors more of the same client `get_attack_speed_degree(...)` seam too: WZ re-check still confirms representative booster families such as `Skill/110.img/skill/1101004/common/x = -2` and `Skill/510.img/skill/5101006/common/x = -2`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare` at `0x912f90` still stores `m_movingShootEntry.nActionSpeed` from that helper, and this pass also lifted the helper's client-owned clamp plus known skill exceptions so deferred moving-shoot snapshots now clamp to the client `[2,10]` degree range, still honor the `4001334` base-speed `-2` adjustment, suppress booster/frozen modifier carry for the client-owned exception set `4311003`, `4321001`, `4331000`, `4331005`, `4341001`, and `4341003`, now give other WZ `info/rapidAttack=1` moving-shoot skills one immediate repeat before the fallback position gate closes, and still mirror the confirmed `CAntiRepeat::TryRepeat` bypass for `33121009` instead of promoting every rapid-attack moving-shoot skill onto one generic lane. Rocket Booster still uses dedicated start/upkeep/landing seams that defer resource spend until launch, re-read the learned level at launch time, preserve the authored normal-vs-tank startup branch, keep the client `250 + 20 * floor(level / 4)` stored vertical launch formula, clear the stored launch marker on the first upkeep tick after startup, and end on the same post-attack clear cadence used by `TryDoingRocketBoosterEnd`. Packet-owned summons now mirror more of the `CSummoned::Update` passive lane too: taunt/puppet summons still resolve body-contact damage against mobs targeting that summon, WZ `info/selfDestructMinion=1` summons still schedule their authored final-attack window before removal, and this pass tightened passive `UpdatePassive` fallback so packet-owned summons without a move snapshot resolve their effective passive movement from the loaded WZ summon branch profile first and then fall back to the packet `moveAbility` when the skill profile is absent or still stationary, which keeps follow/hover summons moving and lets anchor-bound hover summons keep re-emitting their passive effect from motion instead of freezing when the packet stream drops to passive vec-ctrl updates. Focused regression coverage for the meso-ownership lane plus the moving-shoot chooser/action-speed/anti-repeat seams is now on disk in `UnitTest_MapSimulator/MesoExplosionParityTests.cs` and `UnitTest_MapSimulator/MovingShootParityTests.cs`; the moving-shoot source-side coverage now also exercises the new type-`3` live-action-family fallback, weapon-family fallback, and unresolved-owner abort path. | The remaining gap is now narrower and concentrated in the last client-owned movement and packet choreography rather than the generic execution lanes. The simulator still simplifies the exact moving-shoot entry snapshot and request choreography inside `TryDoingSmoothingMovingShootAttack`, especially the still-unrecovered full client `m_nAttackActionType` table values and the exact per-row contents of the shared `get_random_shoot_attack_action(...)` table beyond the now-modeled live family split for shoot, one-handed stab, two-handed stab, one-handed swing, two-handed swing, and polearm swing plus the earlier `info/type == 3` appointed-action bypass. It also still simplifies the live temporary-stat inputs that feed `get_attack_speed_degree(...)` beyond the now-modeled clamp, `4001334` base offset, skill-specific modifier-bypass set, shared Booster delta, and the WZ-backed `rapidAttack => CountLimit 1` fallback, plus any additional `CAntiRepeat` initialization or bypass rules beyond the now-modeled position-threshold, level-match, rapid-attack count-limit, and `33121009` exception gates; the final highest-jump timing window beyond the current WZ-backed near-apex gate; any remaining teleport timing / snap choreography around the client-owned delayed `m_teleport` commit window beyond the now-modeled follow-up mastery trigger; any remaining Rocket Booster packet choreography beyond the now-modeled launch-marker, launch-speed formula, and end-loop timing; deeper packet-owned summon assist dispatch and per-family `OnSkill` / `OnAttack` ownership beyond the now-modeled body-contact / passive-movement / expiry cadence; and the remaining field- or packet-owned special cases that are not recoverable from WZ plus the currently confirmed client seams alone. | `SkillManager`, `SkillLoader`, `PlayerCharacter`, `PlayerCombat`, `PlayerSkillStateRestrictionEvaluator`, `SummonedPool`, `PacketOwnedSummonUpdateRules`, `UnitTest_MapSimulator/MesoExplosionParityTests.cs`, `UnitTest_MapSimulator/MovingShootParityTests.cs` (`CUserLocal::DoActiveSkill_*`, `CUserLocal::DoActiveSkill_BoundJump`, `CUserLocal::TryDoingTeleport`, `CUserLocal::TryDoingRush`, `CUserLocal::TryDoingFlyingRush`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CSummoned::Update`, `CAntiRepeat::TryRepeat`, `get_random_shoot_attack_action`, `get_attack_speed_degree`, `Skill/*/info/type`, `Skill/*/info/affectedSkillEffect`, `Skill/*/info/avaliableInJumpingState`, `Skill/*/info/requireHighestJump`, `Skill/110.img/skill/1101004/common/x`, `Skill/510.img/skill/5101006/common/x`, `Skill/3212.img/skill/32121003/info`, `Skill/3300.img/skill/33001000/info`, `Skill/3310.img/skill/33101001/info`, `Skill/3311.img/skill/33111001/info`, `Skill/3312.img/skill/33121005/info`, `Skill/3312.img/skill/33121009/info`, `Skill/3500.img/skill/35001001/info/rapidAttack`, `Skill/3510.img/skill/35101009/info/rapidAttack`, `Skill/*/info/mesoExplosion`, `Skill/*/info/selfDestructMinion`) |
| Partial | Shadow Partner render/action parity | The simulator now treats Shadow Partner as a buff-owned companion render/action surface with WZ-loaded `special/*` action tables for `4111002`, `4211008`, and `14111000`, client-shaped action resolution order (`LoadShadowPartnerAction` helper-row lookup before plain raw-action name), raw-action-aware alias fallback, delayed attack follow-up keyed from companion first-frame delay, blocking `create*` and attack completion hold semantics, client-lane displacement/interpolation (`+/-30`, `+50`, `200 ms`), per-frame `a0/a1` alpha interpolation, and under-face insertion-layer ownership. This run tightens the packet-owned seam again without widening ownership beyond the existing resolver and draw paths: `ShadowPartnerClientActionResolver` still owns the shared client delay/blocking helpers in addition to the playback-frame remap and offset/back-lane helpers, `PlayerCharacter` still consumes that seam for local playback, and `RemoteUserActorPool` now keeps the packet-owned raw-action ownership that the helper presentation actually needs instead of resolving the helper branch from one raw-action source and then remapping frames from a stale movement raw action. Packet-owned Shadow Partner playback therefore still keeps the earlier indexed helper remaps (`alert2` / `alert3` / `alert4`, `prone2`, `ladder2`, `rope2`) and the local/client `+/-30` side lane, `+50` back lane, and `200 ms` offset interpolation, but now also keeps packet-owned raw-action ownership for repeated attack playback, packet-owned non-attack same-branch helper playback when the remote base action and observed helper action still match, packet-owned attack start ticks on the remote actor seam, and helper replay when a fresh attack starts even if the base helper action name and facing do not change. The same replay fix remains mirrored on the local helper seam too, so repeated same-action local Shadow Partner attacks no longer get swallowed just because the helper was already sitting on the same authored `special/*` branch. The remote draw path still samples the helper animation from the stored helper start time instead of global time, and still inserts Shadow Partner together with remote under-face buff layers at the avatar under-face insertion point rather than before the whole assembled body. WZ-first comparison of the current mounted export now widens the data-backed non-ghost helper set this row can prove: besides the earlier alias-only rows (`avenger`, `assaulter`, `prone2`, `savage`, `showdown`, `assassination`, `smokeshell`, `ninjastorm`, `vampire`), the export also publishes exact helper-facing raw actions such as `alert2`, `alert3`, `alert4`, `alert5`, and `swingO1` at `Skill/411.img/skill/{4111001,4111003,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121004}/action/0`, `Skill/420.img/skill/{4201002,4201003}/action/0`, `Skill/421.img/skill/{4211003,4211005,4211007}/action/0`, `Skill/422.img/skill/4221007/action/0`, and `Skill/1411.img/skill/14111001/action/0`, so the current WZ-confirmed non-ghost helper set is still covered by the existing exact-branch pick, indexed `alert*` remap, and helper alias rows. WZ-first comparison of the current exported Shadow Partner skills still narrows the `create*` question the same way: for `4111002` `special/create2` vs `create3` and for `4211008` `special/create2` vs `create4`, the mounted export is still byte-for-byte identical, and the `_s` / `_f` chooser variants remain equivalent in the current data set, so the remaining chooser gap is still not observable from the loaded WZ surface for this row. Focused regression coverage is now on disk in `UnitTest_MapSimulator/RemoteShadowPartnerParityTests.cs` for packet-owned base-raw preference outside attack playback and packet-owned playback-raw preference over a stale movement raw action. Remaining gap is narrower but still open: unrecovered client-only `LoadShadowPartnerAction` non-ghost helper rows not surfaced by the current WZ `action/0` set, additional frame-index rewrites beyond the currently evidenced `ladder2` / `rope2` / `prone2` / indexed `alert*` set, exact ordering against every other simulator-owned non-mirror under-face overlay that the client may still own as a literal action-layer canvas object, and any residual packet-only one-shot alpha or follow-up choreography that still depends on hidden client layer state rather than the helper animation catalog itself. | Shadow Partner now mirrors companion playback through a dedicated skill-owned action catalog with client-shaped helper ordering and raw-action fallback, including the earlier helper aliases plus the currently exported exact non-ghost helper rows (`alert2`, `alert3`, `alert4`, `alert5`, `swingO1`) from the active Shadow Partner job families. Helper-frame preservation still includes `prone2` in addition to `ladder2`/`rope2`/indexed `alert*`, the shared local+remote offset seam still uses the same client-shaped back-lane and interpolation helpers, and packet-owned playback is closer to the local/client layer because remote helpers now keep one packet-owned raw-action choice all the way through helper branch selection and playback-frame remap instead of resolving the helper branch from one raw action and then sampling remap frames from a stale movement raw action. The same helper-replay correction still applies on the local path as well, so repeated same-branch local attacks restart the helper animation rather than preserving the earlier completed clip. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `ShadowPartnerClientActionResolver.cs`, `RemoteUserActorPool.cs`, `UnitTest_MapSimulator/RemoteShadowPartnerParityTests.cs`, `CUser::PrepareShadowPartnerActionLayer@0x8e1730`, `CActionMan::LoadShadowPartnerAction@0x423cf0`, `Skill/*/{4111002,4211008,14111000}/special/*`, `Skill/411.img/skill/{4111001,4111003,4111005,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121003,4121004,4121008}/action/0`, `Skill/420.img/skill/{4201002,4201003,4201005}/action/0`, `Skill/421.img/skill/{4211002,4211003,4211005,4211006,4211007}/action/0`, `Skill/422.img/skill/{4221001,4221006,4221007}/action/0`, `Skill/1410.img/skill/14101006/action/0`, `Skill/1411.img/skill/{14111001,14111002}/action/0` |
| Partial | Queued follow-up attack families | Attack skills that publish WZ `finalAttack` follow-up maps still queue a deferred extra strike after the initiating melee or projectile hit resolves, gate that follow-up by weapon type on both queue and execute, reuse the learned follow-up skill's live proc rate, preserve queued facing plus preferred-target mob or position snapshots through direct follow-up resolution and any later deferred moving-shoot or projectile hop, and keep the client's single pending-slot overwrite rules for final attack, Spark, and serial follow-up families without clearing unrelated queued summon work. Thunder Breaker `Spark` (`15111006`) still runs as its own timed attack-triggered proc family from WZ `info/condition=attack` plus `info/chainAttack=1` and `info/chainattackPenalty=1`, queued Spark now snapshots the current weapon code at queue time and rechecks that same code again when the deferred branch actually fires so it follows the same fire-time weapon gate as queued final-attack and serial families, and queued Spark still re-enters the shared shoot or projectile path with the saved trigger origin and target lane. WZ `info/chainAttack=1` plus `info/chainattackPenalty=1` serial families such as `2221006` and `5121002` still use their own single pending serial slot, queued final-attack, Spark, and serial execution still re-read the live learned skill level when the deferred branch actually fires, still route queued cast startup back through the normal cast-effect seam, and still carry the client-only ranged final-attack `TryDoingShootAttack(..., 65, ...)` search hint through queued entry plus any extra deferred moving-shoot hop. The queued shoot-entry seam is still narrower again on the presentation side as well as the ammo-validation side: queued final-attack and Spark casts still re-qualify their armed USE or CASH ammo selection against the live inventory right before queued fire time, still refresh slot indices while preserving the original item ids, still cancel the queued ranged branch if the armed USE ammo disappeared, and still consume those snapshotted USE or CASH item ids during queued projectile draw so delayed final-attack or Spark releases can render from the matching WZ `Item/*/*/bullet` frames instead of always collapsing back to the generic skill projectile visual after `GetProperBulletPosition` picked a specific bullet family. WZ re-check in the mounted export still confirms that the queued presentation seam depends on authored item bullet frames rather than only skill-owned ball art: `Item/Consume/0206/02060000/bullet/0` still publishes an authored `origin = (14, 1)` for standard arrow visuals, while `Item/Cash/0502/05021000/bullet/{0,1,2}` still publishes a separate animated cash-bullet family with `origin = (12, 11)` across its frames. Client evidence from `CUserLocal::GetProperBulletPosition` still shows fire-time resolution returning both the resolved USE ammo slot/item and the resolved cash bullet slot/item, so this pass tightens the queued draw seam one step further without changing the queue owner: queued final-attack and Spark projectile visuals now prefer the cash-bullet family only while that cash slot still resolves at fire time after the queued slot refresh, and otherwise fall back to the preserved USE ammo family instead of holding a stale cash-only visual after the client-shaped fire-time slot refresh has already invalidated that cash lane. The same seam remains tightened inside the extra deferred moving-shoot payload hop as well: when a queued final-attack or queued Spark cast has already been re-armed into a later deferred fire, the simulator still re-checks the live learned skill level's actual ammo count plus skill-specific required ammo family before that delayed fire resolves, refreshes USE or CASH slot indices again while preserving the snapshotted item ids, and cancels the queued ranged branch if the originally armed USE ammo no longer satisfies the live fire-time requirement rather than letting the stale queue-entry snapshot survive to release. The earlier delayed queued projectile-origin refresh stays in place as well, so once a queued final-attack or queued Spark projectile is actually due the simulator still re-reads the live avatar `handMove` / `hand` / body-origin shoot point, reapplies that as projectile spawn plus owner origin, and re-aims from that late client-shaped origin instead of holding the older armed release point all the way through a delayed `ballDelay` seam. This pass tightens that late projectile seam one step further too: when the saved queued facing no longer matches the avatar's current facing by the time that delayed fire resolves, the simulator now mirrors the live `handMove` / `hand` / body-origin shoot point back across the avatar pivot instead of collapsing that preserved queued-facing shot to the raw player position, and queued final-attack plus Spark projectiles still preserve the fire-time resolved weapon code plus weapon item id on the projectile payload itself so the later delayed queued slot refresh keeps using the same fire-time weapon family that armed the shot instead of reinterpreting the bullet-family visual through an unrelated weapon swap that happened only after the queued shot had already resolved. WZ-authored `x` chain-penalty decay and the client-owned fixed multiplier tables for no-`x` families still both apply through the deferred or timed chain path as well, so queued follow-up families no longer flatten back onto one generic damage percent after the opener. Focused helper coverage is now on disk in `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` for the mirrored queued shoot-origin seam, queued follow-up projectile visual selection, the shared queued follow-up weapon gate, the delayed-projectile fire-time weapon-context refresh, and queued fire-time ammo refresh behavior already on this row. The remaining gap is now concentrated even more tightly in the last client-only `TryDoingShootAttack` presentation details beyond the current queued-slot overwrite, delayed-projectile overwrite, queued ammo snapshot, the preserved fire-time queued weapon family now carried through delayed projectile refresh, the mirrored queued-facing live-origin refresh, late queued fire-time ammo revalidation, queued cast startup, fire-time queued slot-index refresh, shared queued weapon gate, queued bullet-family visual override, and the tightened fire-time cash-slot visual fallback, especially exact cosmetic bullet-position placement or other residual `GetProperBulletPosition` metadata that the client still derives internally at queued fire time | Final-attack style procs still no longer collapse into the initiating cast timing, queued strikes still cancel if the player changes to a non-matching weapon before the deferred attack fires, if a newer follow-up proc replaces the pending one before it can execute, or if the follow-up skill has been locally unlearned before the queued branch actually fires, and replacing a pending final attack still does not wipe unrelated queued summon damage. Queued Spark now follows that same queue-time and execute-time weapon snapshot rule instead of surviving a weapon swap that the client would reject. Once a queued final attack or queued Spark has already progressed into a delayed projectile release, a newer proc in that same client-owned slot still clears the older still-pending projectile instead of letting it survive past the queue replacement point, while unrelated projectile work remains intact. Deferred moving-shoot payloads also stop dropping out when a due queued follow-up family should run first because the payload now stays armed until after that queued final-attack, Spark, or serial branch resolves instead of being dequeued and lost. Queued follow-up casts that pass through the simulator's extra deferred moving-shoot payload path still keep the stored facing or origin snapshot, preserve the ranged final-attack shoot-range override (`65`) through both deferred target acquisition and the actual delayed fire step, revalidate the live learned level again at the actual deferred fire time, keep the queued target-position snapshot alive across both direct execution and deferred projectile targeting so a vanished opener mob does not force those branches straight back onto generic live ordering, and still revalidate the live fire-time ammo count plus skill-specific ammo family instead of only trusting the older queue-entry ammo snapshot. The queued ranged shoot-entry seam is now closer on both execution and presentation because queued final-attack and Spark casts recheck their armed USE or CASH ammo metadata at queued fire time, keep the original ammo item ownership instead of inheriting unrelated later casts, drop the queued ranged branch when the originally armed ammo family disappeared or no longer meets the live consume count, draw delayed queued projectiles from the matching snapshotted bullet-family frames rather than always falling back to the generic skill projectile art, stop preferring a stale cash-bullet family once the fire-time slot refresh has already shown that the queued cash lane disappeared while the armed USE lane still remains valid, keep that delayed projectile refresh on the fire-time weapon family even if the player changes to a different ranged weapon only after the queued shot has already fired, and now also keep the delayed queued projectile anchored to a mirrored live shoot point when the avatar turns around after the shot was armed but before the preserved queued-facing projectile actually releases. The remaining mismatch is now mostly in deeper client-owned presentation bookkeeping such as exact cosmetic bullet-position placement and other residual queued shot metadata beyond the current USE or CASH projectile item snapshot, queued fire-time ammo-family and consume-count revalidation, mirrored delayed fire-time shoot origin refresh, queued delayed fire-time slot-index refresh, queued fire-time cash-slot visual fallback, and the shared queued weapon gate now covered by `Spark` too. | `SkillManager`, `SkillData`, `SkillLoader`, `ClientShootAmmoResolver`, `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` (`Skill/1511.img/skill/15111006/info/{condition,chainAttack,chainattackPenalty}`, `Skill/222.img/skill/2221006/info/{chainAttack,chainattackPenalty}`, `Skill/512.img/skill/5121002/info/{chainAttack,chainattackPenalty}`, `Item/Consume/0206/02060000/bullet/0`, `Item/Cash/0502/05021000/bullet/{0,1,2}`, `CUserLocal::GetProperBulletPosition`, `CUserLocal::TryDoingFinalAttack`, `CUserLocal::TryDoingSerialAttack`, `CUserLocal::TryDoingSparkAttack`, `CUserLocal::TryDoingShootAttack`) |
| Partial | Repeat-skill sustain families | `CUserLocal::TryDoingRepeatSkill` still owns more of the Mechanic repeat-state machine than the simulator, but this pass keeps the work inside the existing repeat-sustain and summon official-session seams and pushes the SG-88 request bridge further toward client-shaped live replay without inventing another local-only owner. WZ re-check still shows the same authored family surface that this runtime already leans on: `Skill/3512.img/skill/35121003` still owns the SG-88 summon / attack branch metadata (`info/type = 33`, `info/minionAttack = normal`, `summon/attack1`) and `Skill/3512.img/skill/35121013` still owns the tank-siege effect lane (`info/fixedState = 1`, `info/onlyNormalAttack = 1`, `effect/*/delay = 30/60/30/60`), so the new work again stays on transport and confirmation plumbing rather than changing authored timings or repeat-state branch selection. Tank-siege mode-end acknowledgement still remains request-time keyed end-to-end inside the shared timer seam: `SkillManager` still gates `HasPendingRepeatSkillModeEndRequest`, fallback-delay lookup, and `TryAcknowledgeRepeatSkillModeEndRequest` on the active pending request timestamp, `PlayerManager` still carries the stored pending request tick through the local fallback path, and the packet-owned resolve entry point still refuses to acknowledge anything except the currently pending `(skillId, returnSkillId, requestedAt)` lane. Client evidence still shows `CSummonedPool::OnPacket` dispatching opcode `0x119` (`281`) into `CSummonedPool::OnAttack`, then into `CUser::OnSummonedAttack`, while `CSummoned::TryDoingAttackManual` still owns the outbound request choreography that precedes those packets; the same direct IDA re-check still shows the manual SG-88 branch selecting targets through `CMobPool::CheckMobInTrapezoid`, anchoring clustered follow-up hits from `CMob::GetHitPoint`, and using a `120` ms same-request follow-up delay. The simulator now keeps more of that outbound request semantics inside the existing summon bridge in both directions of the same seam: `SkillManager.OnSg88ManualAttackRequested` still feeds `SummonedOfficialSessionBridgeManager.TrackSg88ManualAttackRequest`, authoritative local-owner `0x119` target coverage still resolves and archives the tracked request with its original `requestedAt`, packet-owned `1021` confirmation still archives that same tracked request lane as a `1021-confirm` resolution, expired request windows are still pruned into recent bridge history instead of lingering indefinitely as pending captures, and the bridge can now also synthesize a live outbound SG-88 request packet when a recent captured semantic request packet exposes a reusable field layout for the same admitted target count. That reuse stays anchored to captured official evidence rather than a simulator-only packet guess: `SummonedOfficialSessionBridgeManager` now retains each observed outbound packet's full raw packet bytes beside the existing opcode/payload history, still scores live outbound packets against the tracked SG-88 summon object id, primary target mob id, and admitted target-id set before choosing the semantic request packet, now refuses to promote a candidate request packet unless that captured raw packet can actually be turned back into a replay template for the same summon / primary / target lane, and now extracts a stricter replay layout from that selected packet by preferring an ordered target-slot walk and isolating the reusable primary-target field instead of rewriting every earlier duplicate mob-id occurrence that happens to share the same integer. `MapSimulator` still attempts that learned replay immediately from the existing `OnSg88ManualAttackRequested` seam when the official session bridge is actually connected, and the replayed packet is fed back through the same outbound history/binding path so the pending request keeps one shared capture and confirmation owner instead of creating a second simulator-only transport lane. `/summonedpacket session history [count]`, `/summonedpacket session clearhistory`, and `/summonedpacket session sg88history [count]` still cover the same live bridge history surfaces, but the SG-88 request ring now also carries enough raw packet context to distinguish reusable ordered-slot request packets from earlier duplicate-value bystanders rather than only post-hoc semantic evidence scoring. `SkillManager` still keeps packet-confirmed SG-88 target bookkeeping beside the pending request metadata, unions admitted target ids across authoritative local-owner summon attack packets, and resolves the request as soon as the observed `0x119` target set covers the full pending lane instead of waiting for the last local queued batch to land. Existing repeat-state behavior remains in place for fixed-state lock, `onlyNormalAttack` cast blocking, timer-driven siege/tank-siege timeout, tank-siege->tank handoff, tank/siege repeat-shot cadence (`420 ms`/`180 ms`), SG-88 branch timing (`810 ms` HUD, `2790 ms` arm, `2520 ms` teardown), and client-style manual-target admission/timing. Focused regression coverage for the outbound bridge seam is now on disk in `UnitTest_MapSimulator/SummonedPacketSessionBridgeTemplateParityTests.cs` for captured-template extraction, learned SG-88 raw-packet rewriting, primary-target sharing, ordered target-slot preference over earlier duplicate values, and target-count mismatch rejection. The remaining gap is narrower but still real: tank-siege mode-end still lacks the true official client/server confirmation opcode and exact request/ack ordering behind `m_bSendTankSiegeModeEnd`, and SG-88 still does not know the exact official outbound opcode plus full field order a priori, so automatic request origination still depends on first learning a reusable template from captured official traffic and still cannot yet prove every non-target field in that request one-for-one without further client or packet evidence. | Mechanic repeat skills are now closer on fixed-state timing, in-state cast restrictions, repeat-owned normal attack cadence, SG-88 branch timing, staged tank-siege teardown, and confirmation ownership because both the tank-siege end lane and the SG-88 manual lane now stay request-time keyed deeper into the live summon bridge instead of stopping at local summon bookkeeping. The runtime now goes one step further on SG-88 in both directions of the same bridge: real local-owner summon attack packets can still reach the existing confirmation seam through `/summonedpacket packetclientraw` or the summon official-session bridge, captured official outbound request packets can still be replayed one-for-one through `/summonedpacket session sendraw|queueraw`, and the simulator can now also auto-originate a best-known SG-88 request when a previously captured semantic request packet with the same target-count layout is available. The approximation is narrower than before because learned replay now only promotes packets that expose a reusable summon / primary / ordered-target layout and no longer rewrites stray duplicate mob-id integers that happen to appear earlier in the raw payload. The remaining approximation is now concentrated in the last unknown packet semantics rather than generic bridge bookkeeping: the simulator still does not know the true tank-siege ack opcode/order, and SG-88 replay still depends on captured-template evidence instead of a fully client-proven hardcoded opcode/field map. | `SkillManager.cs`, `PlayerManager.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedSummonedParity.cs`, `Managers/SummonedOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/SummonedPacketSessionBridgeTemplateParityTests.cs` (`CUserLocal::TryDoingRepeatSkill`, `CUserLocal::TryDoingSiege`, `CUserLocal::DrawKeyDownBar`, `CUserLocal::UpdateClientTimer`, `CSummonedPool::OnPacket`, `CSummonedPool::OnAttack`, `CUser::OnSummonedAttack`, `CSummoned::TryDoingAttackManual`, `CMobPool::CheckMobInTrapezoid`, `CMob::GetHitPoint`, `Skill/3512.img/skill/35121003/{info,summon}`, `Skill/3512.img/skill/35121013/{info,effect}`) |
| Partial | Bound-jump, rocket-booster end, and smoothing moving-shoot branches | `Rocket Booster` keeps the prior parity work from this row (ground-start gate, deferred launch-facing, startup/landing cadence, and exit-action teardown window), and this pass closes the next client-owned `TryDoingSmoothingMovingShootAttack` seam inside the existing deferred path while bringing the checked-in coverage back in line with the implemented behavior. Deferred moving-shoot payloads still carry `m_movingShootEntry`-style `nAttackActionType` and `nActionSpeed` metadata, still feed that queued attack-action-type into delayed preferred-target hitbox resolution, and still finish more of the queued action-owner side that the client stores in `m_movingShootEntry.nAction`, but the queue-time `nAttackActionType` seam is now tighter to the client than before. IDA re-check of `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare` at `0x912f90` still shows the client copying `m_nAttackActionType` into `m_movingShootEntry.nAttackActionType` before `SetAttackAction(...)`, then separately storing the chosen queued action in `m_movingShootEntry.nAction`, so the simulator now resolves queued moving-shoot attack-action-type from the live avatar action first and only falls back to the older skill-lane inference when the current action cannot be classified. The helper stays grounded in the current client action table already loaded into the simulator: raw action codes `22` / `23` / `24` / `45` still classify to the shared shoot family, `48` through `53` still classify to magic, melee attack codes still map back to the melee lane, and current-action-name fallback now also stays on the same client-owned action-family surface for deferred classification by recognizing `doubleSwing` / `tripleSwing` in addition to the earlier shoot, shot, stab, swing, and `proneStab` families when the raw code is unavailable. The queue-time resolver still prefers the WZ-authored `action/*` candidate list already loaded into `SkillData.ActionNames` and still follows the `get_random_shoot_attack_action(...)` client seam more closely when the skill rides client skill type `3`: those deferred moving-shoot requests still snapshot one queue-time owner from the shared client shoot family (`shoot1` / `shoot2` / `shootF`) before the delayed execution path is armed instead of degenerating to unrelated later live avatar state when WZ did not publish a usable appointed action. IDA re-check for `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare` still confirms that client skill type `3` does not fall back to the avatar's unrelated live action owner when `get_random_shoot_attack_action(...)` fails; it aborts before `m_movingShootEntry` is armed. The simulator keeps that conservative queue-time behavior in the same seam: type-`3` deferred moving-shoot requests still leave the queued owner unset so the deferred arm is cancelled when the shared random-action owner cannot be resolved. The same deferred execution seam still snapshots the queued action owner into the prior execution record and compares it during anti-repeat suppression, so delayed moving-shoot requests whose queued action owner differs still no longer collapse behind one prior-position check just because `nAttackActionType`, `nActionSpeed`, and world-position thresholds match. The earlier anti-repeat lift remains in place in that same seam instead of leaving every moving-shoot family on the same zero-limit fallback path. WZ re-check still shows `info/rapidAttack=1` on the moving or sustain families this row already touches (`Skill/3312.img/skill/33121009/info/rapidAttack`, `Skill/3500.img/skill/35001001/info/rapidAttack`, `Skill/3510.img/skill/35101009/info/rapidAttack`), and the client `CAntiRepeat::TryRepeat` gate at `0x9032b0` still applies repeat-count allowance before the suppress branch closes, so deferred moving-shoot anti-repeat still initializes `CountLimit = 1` for `rapidAttack` skills while keeping the existing explicit bypass for `33121009` and the prior queued-owner comparison seam intact. Focused source-side coverage for live-action attack-type classification, the extra `doubleSwing` / `tripleSwing` melee-family fallback, type-`3` shared shoot-family selection, type-`3` unresolved-owner cancellation, and the `rapidAttack` count-limit branch is now on disk in `UnitTest_MapSimulator/MovingShootParityTests.cs`. The row still has remaining work in the same bucket: confirmed client initialization and bypass rules for `CAntiRepeat` repeat-count limits beyond the now-modeled `\|dx\| < 6` and `\|dy\| < 150` thresholds, the explicit `33121009` bypass, and the WZ-backed `rapidAttack => CountLimit 1` branch; the exact per-attack-action shared-table contents and bypass rules behind `get_random_shoot_attack_action(...)` beyond the now-modeled type-`3` shared shoot-family fallback plus unresolved-owner abort; any additional server-confirmed Rocket Booster end-branch choreography that is still packet/client-owned outside current simulator ownership; and non-WZ bound-jump special cases that still require direct client evidence. | Movement-coupled attack skills now retain more of the client-owned moving-shoot queue metadata, anti-repeat ownership, and live-action classification surface because the deferred smoothing branch no longer falls back as early when the avatar is in a client-owned melee swing family that is named outside the simple `swing*` prefix. The remaining approximation is concentrated in deeper `CAntiRepeat` initialization details, the exact shared-table contents behind `get_random_shoot_attack_action(...)`, and any packet-owned Rocket Booster or bound-jump choreography that still sits outside the simulator's current seams. | `SkillManager.cs`, `UnitTest_MapSimulator/MovingShootParityTests.cs` (`CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CAntiRepeat::TryRepeat`, `Skill/3312.img/skill/33121009/info/rapidAttack`, `Skill/3500.img/skill/35001001/info/rapidAttack`, `Skill/3510.img/skill/35101009/info/rapidAttack`) |
| Partial | Key-down / charge skills | The prepared-skill seam still covers the client-owned key-down/charge lifecycle instead of leaving release-owned families on one generic keydown loop, but the shared release-owned helpers on that seam are now source-covered instead of only being described in the backlog. Existing parity in this row remains in place for HUD suppression/skins, mechanic sustain timing, WZ `repeat` hold branches, negative-`z` prepare/hold/release overlays, scan-code-backed local input ownership, dragon-owned HUD anchoring, release-triggered execution for the Big Bang trio (`2121001`, `2221001`, `2321001`), Marksman Piercing (`3221001`), Evan dragon breath (`22121000`, `22151001`), Dual Blade charge pair (`4341002`, `4341003`), Monkey Wave (`5311002`), and Wild Hunter swallow follow-up ownership, plus remote armed-hold persistence and authored charge-window gauge timing. WZ still publishes the authored prepare/hold surface for the supported release-owned families, including `Skill/322.img/skill/3221001/{prepare,keydown,action,info}` with `prepare/time = 1200`, `action/0 = piercing`, and the same `900 ms` gauge profile the simulator already resolves through the shared prepared-skill HUD rule table; `Skill/531.img/skill/5311002/{prepare,keydown,action}` still publishes `prepare/time = 1080`, `prepare/action = noiseWave_pre`, `keydown/action = noiseWave_ing`, and `action/0 = noiseWave`; and the Evan dragon-breath charges still publish `common/time = 5` plus `info/chargingSkill = 1` under `Skill/2212.img/skill/22121000` and `Skill/2215.img/skill/22151001`. IDA for `get_max_gauge_time` plus `CUserLocal::OnKeyDownSkillEnd` still shows the client clamping release-owned keydown elapsed time to a minimum `30 ms` and to the family gauge window before the release branch executes, and the shared local release path in `SkillManager` still consumes that same normalized release elapsed time for every supported release-owned family instead of only letting undercharged locals through after a full hold state had armed. Focused regression coverage is now checked in under `UnitTest_MapSimulator/PreparedSkillHudParityTests.cs` for the supported release-family rule table, release-armed HUD text ownership, Piercing's prepare-to-hold phase split, the shared `30 ms` minimum clamp plus gauge-window cap for release-owned charge time normalization, the narrower partial-charge scaling owner table (`3221001`, `22121000`, `22151001`, `5311002`), the shared gauge-duration table that still preserves Piercing, Monkey Wave, and Evan breath charge windows, and Wild Hunter swallow's remote release-owner follow-up mapping. | Remaining gap is narrower and still centered on client-only ownership/choreography outside the WZ-confirmed seams already modeled here. The simulator still does not reproduce the client's full raw make/break scan-code stream ownership, the wider exclusive-request catalog beyond the prepared-skill request family now sharing the `300 ms` gate, or the exact per-skill undercharge math for every release/follow-up family whose client branch consumes the normalized charge time after release. The current seam guarantees that release-owned families take the release path with the client-shaped minimum elapsed clamp, retains the shared authored gauge windows surfaced by WZ, and now has focused regression coverage around those helper surfaces, but any family-specific non-linear release scaling, packet-owned charge semantics, or follow-up choreography beyond the simulator's current shared best-effort scaling owner table is still not fully one-for-one. | `PreparedSkillHudRules.cs`, `SkillManager.cs`, `MapSimulator.RemoteUsers.cs`, `PlayerInput.cs`, `PlayerManager.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `UnitTest_MapSimulator/PreparedSkillHudParityTests.cs` (`Skill/212.img/2121001/info/chargingSkill`, `Skill/322.img/skill/3221001/{prepare/time,prepare/action,keydown/0/delay,action/0,info/piercing,info/piercingBouns}`, `Skill/2212.img/skill/22121000/{prepare/action,common/time,info/chargingSkill}`, `Skill/2215.img/skill/22151001/{common/time,info/chargingSkill}`, `Skill/434.img/skill/{4341002,4341003}/info/chargingSkill`, `Skill/531.img/skill/5311002/{prepare/time,prepare/action,keydown/action,action/0,common/time,info/chargingSkill}`, `String/Skill.img/{3221001,22121000,22151001,5311002}`, `get_max_gauge_time`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::OnKeyDownSkillEnd`, `CUserLocal::DrawKeyDownBar`, `CDragon::DrawKeyDownBar`) |
| Partial | Swallow, mine, cyclone, and sit-down healing branches | The simulator still keeps the earlier mine/swallow/cyclone/healing work already landed on this row, and this pass closes another concrete `TryDoingMine` owner gap inside the same seam instead of widening Wild Hunter support. Client recheck against `CUserLocal::TryDoingMine` at `0x907d70` narrowed the mounted-movement owner further than the previous simulator approximation: after the existing riding-vehicle-id gate, town suppression, and `update_time - 1000` first-deploy lead, the client derives mine state from `CVecCtrl::GetInput` X sign and then clears it again when `CVecCtrl::IsStopped()` says the controller is stopped. `SkillManager` now follows that client shape by resolving Wild Hunter mine movement intent from grounded controller input plus the shared `CVecCtrl.IsStopped()` gate instead of falling back to foothold velocity when no held left/right input exists. The hidden mine fallback still stays on the narrowed client-owned vehicle seam already landed earlier: no visible mounted action means no deploy unless Jaguar Rider ownership is still active or inside the same short update-time grace window, and the hidden jaguar vehicle id still only resolves from a live Jaguar Rider-owned mount or an actually equipped jaguar. Cyclone still sustains as a timed `32121003` body-attack loop with `1000 ms` pulses only while the cyclone action remains active. Wild Hunter swallow still stays on WZ-backed swallow-family detection (`info/swallow` plus `dummyOf`), still accepts cancel/absorb-ack traffic for the visible skill plus its linked dummy follow-up ids (`33101005`, `33101006`, `33101007`), and still mirrors the client more closely on the visible swallow loop: `900 ms` keydown threshold before release, early-release cancel, absorb-ack gated capture success, automatic `3000 ms` digest start plus `500 ms` wriggle cadence after success, cancel-path teardown on failed or invalidated absorb outcomes, and captured-mob consumption before the hidden attack dummy branch fires. The sit-down-healing work remains in the existing summon/runtime seam. WZ recheck still shows `Skill/3511.img/skill/35111011/summon/stand/info/range` publishing `lt=(-150,-100)` / `rb=(150,20)` while `summon/heal/info/range` publishes the narrower `lt=(-125,-100)` / `rb=(125,0)`, so the earlier named-branch support-range fix remains the owner for the heal box itself. Client recheck still narrows remote Healing Robot discovery to the local robot plus remote-party robots only: `String/Skill.img/35111011/desc` and `pdesc` still describe nearby party or team healing, `CUserPool::DoHealNearHealingRobot` still probes the local robot plus remote-party robots only, and `CSummoned::TryDoingHealingRobot` still owns the range check, per-user reheal lock, action `13` request, and suspend flip on the summon. `SummonedPool` therefore still routes remote sit-down healing through the existing party-membership evaluator instead of the older broader remote-owner shortcut for `35111011`, while preserving the existing percent-HP heal (`common/hp`), `x=5` second re-heal lock, every-seated-frame local polling, authored heal-branch suspend duration, packet-owned heal action `13` playback, named-branch playback timing, and authored idle-clear for the suspend window. Focused row coverage is now on disk in `UnitTest_MapSimulator/SummonSupportRangeParityTests.cs` for the authored heal-branch range seam and the party-only remote Healing Robot owner filter, plus `UnitTest_MapSimulator/WildHunterMineParityTests.cs` for the client-owned grounded-input plus stopped-controller mine gate. The row remains partial because the simulator still does not mirror the client's exact swallow packet ordering, the exact server-owned swallow capture/digest request choreography, the remaining `TryDoingMine` riding-vehicle-id ownership nuances beyond the current active-owner plus update-time grace-window gate and live/equipped jaguar fallback, the exact request/skill-packet-owned Healing Robot suspend reset choreography beyond the current duration-plus-idle seam, or the remaining non-Wild-Hunter swallow-family nuances one-for-one | These branches now stay closer to their real client owners instead of flattening back into the generic cast/runtime path. Mine still respects the client town suppression, update-time-owned hidden-owner grace window, narrowed hidden jaguar fallback, and now also uses the client-shaped grounded `CVecCtrl` input plus stopped-controller gate instead of a simulator-only foothold-velocity fallback; cyclone still stops pulsing once the sustained cyclone action falls off the avatar; swallow still routes charge, cancel, absorb success/failure, wriggle timing, and hidden follow-up attack through one WZ-backed swallow-family seam; and sit-down healing still both matches the authored Healing Robot heal box itself and limits remote healing discovery to the client-shaped local-or-party owner seam instead of accepting any nearby remote owner. The remaining visible mismatch is narrower and centered on exact swallow packet choreography, the last riding-vehicle-id details in `TryDoingMine`, the last request/skill-packet-owned Healing Robot suspend reset details outside the current authored playback window, and any residual non-Wild-Hunter swallow-family behavior the client owns outside the current local cancel/request path | `SkillManager`, `SkillLoader.cs`, `SkillData.cs`, `PlayerCharacter`, `MapSimulator`, `SummonedPool.cs`, `UnitTest_MapSimulator/SummonSupportRangeParityTests.cs`, `UnitTest_MapSimulator/WildHunterMineParityTests.cs` (`CUserLocal::TryDoingSwallowBuff`, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::TryDoingMine`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingSitdownHealing`, `CUserPool::DoHealNearHealingRobot`, `CSummoned::TryDoingHealingRobot`, `CVecCtrl::GetInput`, `CVecCtrl::IsStopped`, `Skill/3300.img/skill/33001001/info/type`, `String/Skill.img/33001001`, `Skill/3310.img/skill/33101005/info/dummyOf`, `String/Skill.img/35111011/{desc,pdesc}`, `Skill/3511.img/skill/35111011/{info,common,summon/stand/info,summon/heal,summon/heal/info}`) |
| Partial | Summon simulation | Summon-family skills still keep the broader parity work already landed here: generic summon lifecycle/timers, WZ-backed attack branch and metadata loading, projectile and target-impact branch retention, Tesla triangle targeting, self-destruct prepare/die handling, packet-owned prepare/attack visual parity, taunt-only puppet registration, shared summon rule selection between local and packet-owned seams, and the current movement/gating buckets for stationary, grounded-follow, owner-hover, and anchor-bound summons. The earlier client-backed Tesla timing fix remains in place: IDA for `CSummoned::TryDoingTaslaCoilAttack` shows Tesla impact timing is driven by the summon attack-delay window itself, with a fixed `300 ms` floor plus per-target jitter inside the remaining window rather than by per-target projectile travel distance, and both the local `SkillManager` seam and the packet-owned `SummonedPool` seam still follow that rule. This run keeps the summon attack-branch ownership in the same loader/runtime seam and closes the next concrete branch-selection gap instead of forking another summon-family special case. WZ was rechecked first for authored multi-branch summon attacks that the simulator was still flattening: the current mounted export still publishes alternate summon attack branches such as `Skill/522.img/skill/5220002/summon/attack{1,2}` with distinct `info/range/sp`, `Skill/532.img/skill/5321003/summon/attack{1,2}` with branch-scoped `info/attackAfter`, and `Skill/501.img/skill/5010005/summon/attack{1,2,3,4}` as distinct authored action branches. The loader now preserves those branches and their metadata instead of only keeping the first preferred attack branch as the live runtime surface: `SkillLoader` now records branch-scoped summon projectile lists, branch-scoped target-impact presentation lists, branch-scoped `attackAfter` and projectile-speed metadata, branch-scoped rectangle/circle attack ranges, and the additional authored `attack2` through `attack8` / `attackF` action branches in the same summon action cache that already owned `attack1`, `support`, `heal`, `subsummon`, and Tesla's `attackTriangle`. Client evidence was then rechecked to anchor how packet-owned summons should consume those authored branches without inventing a simulator-only selector. `CSummoned::OnSkill` still clears any previous one-time action and then calls `CSummoned::SetAttackAction(rawAction)`, `CSummoned::SetAttackAction` still writes that raw attack action directly into the summon's one-time-action field before `PrepareActionLayer`, `CSummoned::GetCurrentAction` still prefers that one-time-action field over move-action-derived idle/move/fly states, `CSummoned::PrepareActionLayer` still loads summon frames through `CActionMan::LoadSummonedAction(this->m_nSkillID, this->m_nSLV, CurrentAction, ...)`, and `CSummoned::OnAttack` still keeps the raw packet attack action as `nAction` before loading the summon attack info. The exact client raw-action-family split was then rechecked directly in IDA instead of extending the old simulator-only `attack1+` numbering guess: `CSummonedBase::Action2AIType` only special-cases raw attack actions `6`, `13`, and `16`, `CSummonedBase::AIType2Action` still maps those families back to the same exact raw-action anchors, and `CSummoned::OnAttack` still subtracts the family-local bases `6`, `13`, and `16` before `CSummonedBase::LoadAttackInfo` resolves the authored summon attack metadata. WZ rechecks on the mounted export still show those exact family anchors line up with authored non-default summon branches the simulator was previously skipping: Tesla Coil (`Skill/3511.img/skill/35111002/summon`) still publishes `attackTriangle` instead of `attack3`, Healing Robot H-LX (`Skill/3511.img/skill/35111011/summon`) still publishes `heal` rather than any `attack10` surface, and Satellite 1 / 2 (`Skill/3511.img/skill/35111001/summon`) still publishes self-destruct removal branches `die` / `die1`. The simulator now mirrors that wider client-backed slice inside the existing packet-owned seam: packet-owned summon attacks still resolve authored `attackN` branch playback from the client default attack-family raw action window when the skill actually publishes that exact branch, and they now also keep client raw action `6` on authored Tesla-style `attackTriangle`, client raw action `13` on authored support-owned `heal` / `support` playback, and client raw action `16` on the loader-owned self-destruct removal branch (`die` / `die1`) when those branches exist instead of drifting through the old global `attack1+` numbering. The selected branch name now drives packet-owned attack playback, packet-owned projectile animation selection, packet-owned authored impact-delay fallback, packet-owned projectile-speed fallback, and packet-owned attack-range fallback instead of collapsing every remote summon back onto the default `attack1` metadata once the packet is in flight. The shared local/runtime side now consumes the same branch-aware metadata surface as well: `SkillData` exposes branch-scoped summon projectile, impact, range, and delay resolution with fallback to the existing generic summon metadata, so local summon hit overlays, projectile travel timing, and attack bounds stay on the same loader-owned metadata seam whenever a local branch name is already known instead of reimplementing a second lookup path. This pass keeps the same client-backed ownership split for playback as well instead of leaving packet-owned `OnSkill` / `OnAttack` on branch state alone: packet-owned summon skill and attack packets now arm a temporary one-time-action playback window from the same raw action that the client writes through `CSummoned::SetAttackAction`, using the already-loaded authored summon branch/action cache so the packet-owned summon layer prefers the raw packet action over idle or move playback until that authored branch window completes, and Tesla skill packets fan that same branch ownership across the live owner-linked Tesla set rather than only changing actor state. The local Tesla seam now stays on that same authored branch owner too: WZ still only publishes `Skill/3511.img/skill/35111002/summon/attackTriangle` instead of an `attack3` branch, `CSummonedBase::AIType2Action` still maps the Tesla family back to raw action `6`, and local `SkillManager` Tesla attacks now keep `CurrentAnimationBranchName = attackTriangle` plus branch-scoped projectile metadata instead of clearing the branch name before playback. The earlier client-backed target-anchor work remains in place on top of that branch retention: `SkillLoader` still keeps authored summon target-impact presentations as structured metadata (`animation`, `hitAfter`, `pos`), and the local plus packet-owned runtimes still reuse the same client-backed target-anchor resolver when those presentations are target-bound instead of source-bound. Client evidence from `CSummoned::OnAttack` still anchors summon hit points by clamping to the mob body, biasing toward the near body edge from the summon/source side, capping extreme reach back toward a `600 px` window, and measuring that clamp from a raised summon source point `(x, y - 25)` rather than from the raw summon feet position; the simulator still reuses that same lifted source anchor for local summon projectile spawn points, local summon projectile travel delay measurement, packet-owned summon projectile spawn points, packet-owned summon projectile travel delay measurement, local queued summon damage overlays, packet-owned delayed hit overlays, and packet-owned Phoenix/Frostprey tile-overlay anchoring. The earlier `pos=3` split also remains in place, and the shared resolver keeps the same hitbox-missing behavior already landed on the previous pass: target-bound summon impacts still apply the client-backed `600 px` reach cap even when the mob body rect is temporarily unavailable, and the packet-owned fallback target point still reuses the same raised mob-impact fallback height as the local summon seam instead of dropping back to raw movement coordinates. The earlier WZ-backed Mechanic support fixes remain in place too: hostile support summon classification still follows `String/Skill.img` instead of the older blanket `minionAbility=mes` shortcut, so Acceleration Bot EX-7 (`35111005`) no longer leaks into the friendly support lane; EX-7 still consumes its authored speed-up payload as a positive mob `Speed` status while lowering DEF; genuinely party-facing support summons still route through the dedicated friendly-support path; and AF-11 (`35121010`) still mints a persistent summon-owned damage-buff state from its authored `Damage Buff: +#x%` payload instead of falling through the hostile mob-status branch. Focused regression coverage for this new branch-selection slice is now on disk in `UnitTest_MapSimulator/SummonAttackBranchParityTests.cs` for the client default `attackN` window, the exact client special raw attack actions `6` / `13` / `16`, the matching packet-skill action branch surface for `6` and `13`, and the existing authored branch fallback surface. The row remains partial because the remaining parity gap is now narrower and mostly client-only: the simulator still does not reproduce the full local Tesla beam ownership/action packet payloads from `CSummoned::TryDoingTaslaCoilAttack`, any additional raw summon action semantics beyond the now-modeled client-default `attack1+` window plus the exact client special anchors `6`, `13`, and `16` still need more client evidence, the exact semantic mapping for any authored summon `pos` codes beyond the now-modeled `pos=1` near-edge target anchor, `pos=2` source anchor, and `pos=3` centered target anchor still needs new client evidence, grenade-style expiry choreography beyond authored prepare/die playback is still approximate, the exact remote `OnSkill` / `OnAttack` / `OnHit` choreography for every summon family still goes beyond the current ordered impact handling, assist-target special cases not surfaced by WZ `minionAbility` / `condition` / packet assist bytes are still missing, the last one-for-one mob aggro retarget timing heuristics are still approximate, and party damage amplification still stops at the simulator's current summon-owned damage-buff seam. | Summon-heavy jobs are now closer not just on authored support polarity and Tesla timing, but also on how the client keeps authored summon attack branches alive after packet decode instead of flattening them back to one default attack surface. EX-7 and AF-11 still follow the correct hostile-vs-friendly authored support lane, Tesla impacts still use the client-shaped fixed-delay-plus-jitter window, local Tesla beam visuals still preserve the attack triangle in the same packet-shaped point form already used by remote summons, and target-bound summon impacts still stay on one shared capped fallback rule even when the mob body rect is empty. On top of that, packet-owned summons that actually publish authored `attack2+` surfaces now keep their authored branch playback, authored branch projectile chrome, authored branch impact timing fallback, and authored branch range metadata on the same loader-owned seam the client feeds through its one-time raw action field rather than collapsing back to `attack1` once the runtime updates, the packet-owned summon layer now keeps that same raw-action playback ownership alive through a one-time-action branch window instead of only swapping actor state, and local Tesla playback now stays on authored `attackTriangle` branch ownership rather than silently dropping back to a generic branchless lane. Local and packet-owned delayed impacts still consume the same indexed `hitAfter` / `pos` metadata that WZ publishes for summon hit branches such as Octopus, source-anchored entries still render from the summon-owned origin seam, `pos=1` target-anchored entries still share the client-backed body-clamped near-edge hit-point rule, `pos=3` entries still keep their distinct body-centered target anchor, and projectile start/timing still reuse the same raised summon source anchor the client uses for its `OnAttack` clamp instead of measuring from the raw summon feet position. The remaining visible mismatch is now concentrated in client-only payload/ownership details, deeper remote choreography, and still-unconfirmed raw action / `pos` semantics beyond the now-modeled default `attack1+` window, the exact client special raw-action anchors `6` / `13` / `16`, and the `1/2/3` target-anchor split rather than in authored branch retention for the observed multi-attack summon families. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `SummonedPool.cs`, `SummonRuntimeRules.cs`, `SummonImpactPresentationResolver.cs`, `UnitTest_MapSimulator/SummonAttackBranchParityTests.cs`, IDA `CSummoned::TryDoingTaslaCoilAttack` (`0x752780`), IDA `CSummoned::OnSkill` (`0x74a940`), IDA `CSummoned::OnAttack` (`0x753340`), IDA `CSummoned::SetAttackAction` (`0x74a0f0`), IDA `CSummoned::GetCurrentAction` (`0x7494c0`), IDA `CSummoned::PrepareActionLayer` (`0x749cd0`), IDA `CSummonedBase::Action2AIType` (`0x748df0`), IDA `CSummonedBase::AIType2Action` (`0x748da0`), IDA `CSummonedBase::LoadAttackInfo` (`0x758b20`), IDA `CActionMan::LoadSummonedAction` (`0x423100`), IDA `CSkill_HitAni::CreateDefault` (`0x6ec010`), WZ evidence from `String/Skill.img/35111005`, `String/Skill.img/35111002`, `String/Skill.img/35121010`, `Skill/3511.img/skill/35111002/{common,info,summon,hit}`, `Skill/3511.img/skill/35111011/summon`, `Skill/3511.img/skill/35111001/summon`, `Skill/3511.img/skill/35111005/{common,info}`, `Skill/3512.img/skill/35121010/{common,info}`, `Skill/521.img/skill/5211002/hit/0/{hitAfter,pos}`, `Skill/521.img/skill/5211002/hit/1/pos`, `Skill/521.img/skill/5211005/hit/0/pos`, `Skill/311.img/skill/3111005/hit/{0,1}/pos`, `Skill/321.img/skill/3211003/hit/0/pos`, `Skill/321.img/skill/3211005/hit/{0,1}/pos`, `Skill/521.img/skill/5211002/summon/attack1/info`, `Skill/522.img/skill/5220002/summon/attack{1,2}/info`, `Skill/532.img/skill/5321003/summon/attack{1,2}/info`, and `Skill/501.img/skill/5010005/summon/attack{1,2,3,4}` |
| Partial | Remote summoned-pool packet parity | The simulator still keeps the packet-owned summon transport seam in `SummonedPool`, but the remote attack and hit side is closer to the client than before: it already mirrors the observed `0x116`-`0x11B` packet family, local versus remote owner registration, packet-owned move-path sampling, Tesla third-coil refresh, taunt-side ownership, packet hit damage bookkeeping, mob-authored hit-overlay lookup, and the loader/runtime handling for nested `attack/info/hit` metadata with UOL resolution plus the observed `attach` and `attachfacing` aliases. Earlier passes had already split positive-damage fallback away from the live summon branch by snapshotting the remaining authored branch window into a packet-owned one-time-action clip, keeping named summon branches on their authored timelines without a synthetic extra `prepare` prefix and letting non-Tesla summons already sitting in `Prepare` stay on authored `summon/prepare` during fallback instead of jumping straight to idle or attack. A later pass then closed a concrete `CSummoned::PrepareActionLayer` presentation gap inside that same seam by honoring authored per-frame alpha ramps on both the live summon branch and the detached one-time-action clip instead of forcing every summon frame to draw fully opaque; that change was grounded in both IDA and WZ evidence (`CSummoned::PrepareActionLayer` inserts each action-layer canvas with authored `a0` / `a1`, and WZ summon branches such as `Skill/1211.img/skill/12111004/summon/die/11` publish `delay=120`, `a0=255`, `a1=0`). Earlier on this row, the `4111007` reactive-damage chain also stopped depending only on summon-owned `ball` branches and now accepts the client-owned root `Skill/411.img/skill/4111007/ball` family that `CSummoned::OnAttack` resolves through `GetBallUOL`, with its chain source already moved onto the same move-action-owned `(x +/- 25, y - 25)` summon offset the client feeds into `RegisterChainlightningAnimation`. This run tightens the same packet-owned hit-overlay seam again without widening transport ownership and also widens the already-confirmed bird tile family by one more concrete WZ-backed row instead of inventing a generic tile heuristic: packet-owned summon hit overlays still consume frame-scoped `attack/info/hit/<frame>` attach and `attachfacing` flags as frame-scoped runtime state instead of flattening those WZ flags into one overlay-wide decision at load time, the loader now also recovers the lower-precedence per-frame `attach` / `bHitAttach` and `attachfacing` / `bFacingAttach` flags authored directly on `attack/<frame>/hit` branches when no higher-precedence `info/hit` or info-alias flag owns that state, and the packet-owned delayed tile-overlay seam now also admits the legacy Silver Hawk row exported as `Skill/311.img/skill/3111005/{info/type=33,info/minionAttack=stun,summon,tile}` beside the previously confirmed Phoenix/Frostprey rows. The current mounted export still exposes the concrete WZ surfaces behind that fallback order rather than simulator-only heuristics: explicit frame-scoped attached-hit metadata such as `Mob/8150302.img/attack1/info/hit/1/attach`, top-level facing-attach examples such as `Mob/5220000.img/attack1/info/hit/attachfacing` and `Mob/3220000.img/attack2/info/hit/attachfacing`, info-level alias cases such as `Mob/5160000.img/attack1/info/attach`, `Mob/6090002.img/attack1/info/attach`, and `Mob/9300525.img/attack1/info/attachfacing`, plus deeper attached-hit branches such as `Mob/3220000.img/attack1/13/hit/attach`. `SummonedPool` still resolves packet-owned hit-overlay follow state, summon-facing mirroring, and authored `range/sp` offset placement from the current hit frame instead of only from the frame-zero/global fallback, including frame-scoped facing-only follow on frames that only flip the authored overlay. WZ inspection now confirms the current exported summon skills publish the authored bird post-attack tile surfaces through `Skill/311.img/skill/3111005/tile`, `Skill/312.img/skill/3120010/tile`, `Skill/321.img/skill/3211005/tile`, plus the reactive chain surface at `Skill/411.img/skill/4111007/ball` and the authored summon attack bounds, while IDA for `CSummoned::OnAttack` still shows the client scheduling the bird tile overlay with a delayed fade window after impact and separately registering the `4111007` ball chain from that clamped summon-side offset to a mob-body target point for `270 ms`. `SummonedPool` therefore continues to reuse the existing packet-owned attack impact timing seam for both confirmed post-effect families: the bird tile overlay still lands over the first valid target mob with the authored summon attack rectangle plus a client-shaped `200 ms` start offset and `500 ms` `128 -> 255` alpha ramp, and the reactive-damage chain seam still schedules a client-shaped `270 ms` post-hit tether through the existing animation-effects owner without widening summon transport or field-effect ownership. Focused regression coverage for the rule seam is now on disk in `UnitTest_MapSimulator/PacketOwnedSummonUpdateRulesTests.cs`, covering the newly admitted Silver Hawk tile-family rule, the authored-tile gate, and rejection of unrelated summon families. The remaining gap is narrower but still real: the simulator still does not synthesize every hidden client `MobAttackInfo::sHit` / `bHitAttach` case one-for-one beyond the currently observed WZ surfaces and the now-modeled frame-scoped attach/facing plus attack-frame-hit fallback path; the `CSummoned::OnAttack` post-effect seam is still limited to the currently confirmed bird tile family plus the `4111007` reactive chain instead of every hidden post-effect case; the `4111007` chain still approximates the client `GetBallUOL` / `RegisterChainlightningAnimation` GR2 owner with the simulator's existing lightning effect manager rather than reproducing literal ball-UOL canvas ownership; it still approximates the broader client action-layer stack with detached draw overlays instead of reproducing literal GR2 action-layer canvas ownership and ordering; and `/summonedpacket` plus `SummonedPacketInboxManager` still expose a simulator loopback seam instead of full official `CUserPool` transport and ownership decode. | Packet-owned summons are no longer just spawn/remove stubs: they follow the observed client summon packet family, keep summon ownership ordered per user, route local-owner taunt and Tesla side effects through the same created seam, replay summon attack/hit/removal visuals, decode hit payloads far enough to show the mob-authored overlay and sound feedback from the attacking mob template, and tear down with remote-user lifetime instead of leaving orphaned field actors behind. Hit parity is closer again because the loader/runtime pair now covers nested `attack/info/hit` metadata with UOL resolution, lower-case alias support, explicit `attach` handling, client-shaped `attachfacing`-only `bHitAttach` cases, info-level `attach` / `attachfacing` aliases, explicit `info/hit` precedence over deeper attack-frame hit metadata, render-node fallback from missing or empty `info/hit` into authored per-frame `attack/<frame>/hit`, the cached WZ frame-origin anchor math that keeps packet-owned summon hit overlays visually aligned with the live summon, authored attached-hit offsets that stay on their authored local seam while following the summon, summon-facing X mirroring for attached overlays when `attachfacing` is authored, and frame-scoped `info/hit/<frame>` attach/facing flags that can still change packet-owned overlay follow and facing behavior mid-playback instead of being flattened once at load time, including frame-scoped facing-only follow when the authored frame only publishes `attachfacing`. This run also keeps the existing client-owned bird tile seam narrow but less incomplete by admitting the legacy Silver Hawk WZ row rather than only the later Phoenix/Frostprey exports, while preserving the same delayed fade window and summon-attack-rectangle placement owner. The same local-runtime summon playback split for named attack branches plus non-Tesla `Prepare` state, authored remaining-window progression when positive-damage fallback has to stand in for the client hit branch, a detached packet-owned one-time-action playback clip that no longer mutates the live summon branch in place, authored per-frame alpha ramps while those summon clips are replaying, reactive-damage summon attack facing that follows the client-confirmed move-action seam instead of a generic packet-facing read, and the client-confirmed `4111007` root-ball gate plus summon-side chain source offset all remain in place. The remaining parity work is concentrated in the last client-only `sHit` / `bHitAttach` cases outside the currently observed WZ surfaces, broader hidden `CSummoned::OnAttack` post-effect cases beyond the now-confirmed bird-tile and reactive-chain families, literal `GetBallUOL` / GR2 action-layer ownership instead of the current simulator-owned approximations, and end-to-end official `CUserPool` transport rather than owner teardown, hit metadata load, packet-facing correction, confirmed bird tile playback, or confirmed `4111007` chain scheduling being missing outright. | summon field/pool runtime (`CSummonedPool::OnPacket`, `CSummonedPool::OnCreated`, `CSummonedPool::OnRemoved`, `CSummonedPool::OnMove`, `CSummonedPool::OnAttack`, `CSummoned::OnHit`, `CSummoned::OnAttack`, `CSummoned::PrepareActionLayer`, `CUser::AddSummonedList`, `CMobTemplate::LoadAttackInfo`, `Skill/3511.img/skill/35111002/summon/{prepare,attackTriangle,hit}`, `Skill/3511.img/skill/35111011/summon/{stand,heal}`, `Skill/1211.img/skill/12111004/summon/die/11/{delay,a0,a1}`, `Skill/411.img/skill/4111007/{info,summon,ball}`, `Skill/311.img/skill/3111005/{info,summon,tile}`, `Skill/312.img/skill/3120010/{tile,summon}`, `Skill/321.img/skill/3211005/{tile,summon}`, `Mob/5220000.img/attack1/info/hit/attachfacing`, `Mob/3220000.img/attack2/info/hit/attachfacing`, `Mob/3220000.img/attack1/13/hit/attach`, `Mob/5160000.img/attack1/info/attach`, `Mob/6090002.img/attack1/info/attach`, `Mob/9300525.img/attack1/info/attachfacing`, `Mob/8150302.img/attack1/info/hit/1/attach`, `UnitTest_MapSimulator/SummonedPoolOneTimeActionParityTests.cs`, `UnitTest_MapSimulator/PacketOwnedSummonUpdateRulesTests.cs`) |
| Partial | Summoned actor update/runtime parity | The simulator still keeps summon-owned actor update/runtime ownership (movement snapshots, spawn/prepare/attack/hit/die routing, Tesla armed-prepare idle fallback, self-destruct expiry handoff, packet-owned hit-overlay interrupt clearing, and named-branch timing that avoids forcing generic `summon/prepare` onto authored branches). This run closes another packet-vs-local assist-branch seam inside the same `SummonRuntimeRules` owner that already drives authored summon branch selection. WZ re-check against the current extracted data still shows the authored support and assist surfaces that matter here: `Skill/132.img/skill/1321007/summon/skill{2,3,4,5,6}` for Beholder buff playback, `Skill/3511.img/skill/35111011/summon/heal` for Healing Robot, `Skill/3512.img/skill/35121009/summon/subsummon` for the summon-action family, and the stand-only support surfaces under `Skill/3511.img/skill/35111005/summon/stand` and `Skill/3512.img/skill/35121010/summon/stand`, with `info/selfDestructMinion=1` still present on both stand-only support skills as well as the previously confirmed `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}` and `hit` branches. The local side still routes Beholder weighted branch choice through the shared summon-runtime selector that reads the active synthetic Beholder buff state before choosing the next authored `skill2..skill6` branch, and local Beholder heal, local support heal/buff playback, and local summon-action playback still resolve through the same shared authored-branch preference order. This pass extends that same ownership to the packet-special action ids and the packet-owned support suspend window: packet-owned Beholder-heal, support-heal, and subsummon packets still try their explicit authored branch first but now fall back through the same assist-owned resolver instead of returning `null` early, and packet-owned support playback plus natural self-destruct expiry now arm their suspend window through the same authored support-branch order instead of a heal-first-only shortcut. That keeps stand-only support summons plus non-heal support families on one shared authored branch owner even when the packet used the client special action id or the support summon reaches its self-destruct expiry branch. Focused regression coverage is now on disk in `UnitTest_MapSimulator/BeholderSummonRuntimeParityTests.cs` for stand-only support suspend tracking, non-heal support-branch suspend preference, and the widened support-suspend clear helper. The row remains partial because deeper client-only seams are still outside current simulator visibility: exact hidden summon HP and full `CalcDamage::PDamageSummoned` / `MDamageSummoned`, broader `CVecCtrl::UpdatePassive` side effects beyond the confirmed passive effect trigger, deeper Tesla state-machine transitions and refresh rules, and the remaining assist-family remote branch choices that still depend on client-only state not exposed by current packet/WZ data. | This keeps more of real summon runtime ownership inside the summon layer instead of collapsing either local or packet-owned assist families to ad hoc runtime fallbacks: local and packet-owned body-contact damage still share the same mob-authored base-damage surface plus outgoing-damage modifiers, packet-owned `OnSkill` branch ownership still lands on authored `skill1..skill6` when packet actions provide those branch ids, packet-owned named summon branches still avoid inheriting a generic prepare window, packet-owned generic prepare branches still promote into live attack state instead of staying pinned on one coarse packet state through the whole action, and packet-owned mob-hit overlays still get interrupted both when summon-owned action-layer changes begin and when packet-owned attack playback completes back into the steady summon layer. The important gap closed here is that packet special actions and packet-owned support suspend timing no longer bypass the same authored support/summon-action fallback order that local support already uses, so stand-only support families, non-heal support families, and `skill1` / `skill2` summon-action families stay on one shared branch-selection owner. Remaining mismatch risk is now concentrated in deeper hidden damage formulas, unmodeled passive-update side effects, deeper Tesla transitions, and the last remote/client-only assist selectors rather than in the observed authored support-branch surface, the packet special-action fallback order, or the packet-owned support suspend owner. | summon actor/runtime layer (`CSummoned::Update`, `CVecCtrl::UpdateActive`, `CVecCtrl::UpdatePassive`, `CSummoned::OnHit`, `CSummoned::OnSkill`, `CSummoned::SetAttackAction`, `CSummoned::SetMoveAction`, `CSummoned::ProcessAction`, `CSummoned::ProcessAttack`, `CSummoned::SetDamaged`, `SummonRuntimeRules.cs`, `SkillManager.cs`, `UnitTest_MapSimulator/BeholderSummonRuntimeParityTests.cs`, `Skill/132.img/skill/1321007/summon/skill{2,3,4,5,6}`, `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3512.img/skill/35121009/summon/subsummon`, `Skill/3511.img/skill/35111005/{info/selfDestructMinion,summon/stand}`, `Skill/3512.img/skill/35121010/{info/selfDestructMinion,summon/stand}`, `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}`, `CSummoned::TryDoingHealingRobot`) |
| Partial | Weapon afterimage / melee trail parity | The simulator still keeps the broader WZ-backed afterimage surface already landed for this row: skill-authored plus shared `Character/Afterimage/<family>/<masteryIndex>` catalogs, weapon-item `info/afterImage` family selection with narrow sword-sibling fallback only, local basic-melee registration, charged `charge/<element>` rewrites for the confirmed Page, Paladin, Thunder Breaker, and Aran families, alias-aware action matching, `1221009 blast -> stabO2`, client-backed raw-range normalization for `1221009`, `57 -> 41`, and `74`, remote afterimage playback with fade tails, ordered remote WZ action fallback, the single-authored plus sole-renderable remote pose promotion seams, and remote prepared-skill release teardown from the official melee packet. Earlier on this row, the remote charge fallback had already moved into the same remote temporary-stat plus remote melee owner instead of a new packet or buff seam: `RemoteUserPacketCodec` recovers a single known charge skill from the broader stored remote temporary-stat payload after the mask prefix when the direct branch-specific decode does not yield one, and `RemoteUserActorPool` reuses that broader payload scan with the remote job-preferred charge family when remote melee playback needs to choose an afterimage `charge/<element>` family or visible charge effect without an explicit charge id in the attack packet. This pass keeps the work in the existing local/remote afterimage draw seams and closes the next concrete `CreateAfterimageLayer` gap instead of widening transport ownership. WZ was rechecked first for the authored subframe surface that the simulator was still flattening: `Character/Afterimage/swordOS/0/swingO1/2/0` still publishes a single authored afterimage canvas with `delay=200`, `Character/Afterimage/swordOS/charge/1/stabO2/1/{0,1}` still publishes ordered charge subframes with `delay=50` then `delay=300`, and the `1221009` skill-authored afterimage branch still keeps its own sparse authored surface under `Skill/122.img/skill/1221009/afterimage/swordOS/stabO2/1/0` with `delay=60`. Client evidence was then rechecked directly in IDA for the owner seam the simulator was approximating: `CUser::RegisterAfterimage` still stores the start tick plus optional `charge/<element>` UOL rewrite, while `CActionMan::CreateAfterimageLayer` still clears the old layer, resolves `GetWeaponAfterImage`, and loads the ordered canvases for the chosen raw action into the layer instead of treating every authored subframe as one simultaneous draw. The simulator now mirrors that part of the client-owned playback shape more closely without introducing a new layer owner: `CharacterAssembler` exposes per-action frame elapsed time, `MeleeAfterimagePlaybackResolver` now resolves a single authored afterimage subframe from the matched action-frame index plus elapsed milliseconds inside that frame, and both `PlayerCharacter` and `RemoteUserActorPool` now cache and fade the last resolved authored subframe instead of drawing every canvas inside the matched WZ frame-set at once. Focused tests are now on disk in `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` for ordered subframe playback selection, action-frame-index plus elapsed-time subframe resolution, and the confirmed client range remaps and hardcoded `74` rectangle. The remaining gap is narrower again but still real: the client raw-range surface is still limited to the confirmed `1221009`, `57 -> 41`, and `74` seams; the simulator still does not reproduce literal GR2 layer ownership and side effects such as the exact `RemoveCanvas` / `LoadCanvas` / alpha-vector choreography inside `CreateAfterimageLayer`; and the broader remote field packet surface still needs the rest of a one-for-one `CUserRemote::OnAttack` / `CUserRemote::OnMeleeAttack` transport lift and client-owned side effects beyond the currently consumed attack envelope, mob-hit, bullet-item, release-follow-up, broader remote charge-family fallback, and explicit-skill-range-owner slices | Many melee jobs rely on afterimage trails and authored swing ranges to sell timing and hit shape, and the simulator now consumes the same WZ-backed trail and arc data for local melee skill playback, local basic melee attacks, packet-owned local melee hitbox resolution, and simulator-owned remote actors while respecting the weapon item's authored afterimage family instead of forcing every sword through the long-family arcs. The important visual gap that remained in the current row was also still visible even after the earlier remote charge-family lift: authored afterimage frame-sets were being flattened into one draw, which made multi-canvas charge and swing trails read more like stacked sprites than the client-owned ordered layer playback. That seam is tighter now because both local and remote afterimages resolve one authored subframe from the matched action frame and its elapsed milliseconds, then keep only the last resolved subframe alive through the existing fade tail instead of drawing every subframe at once. The earlier remote charge-family recovery still remains in place, so charged remote melee playback no longer depends only on the explicit `WeaponCharge` branch offset surviving decode. The remaining visible gap is now concentrated in any still-unrecovered raw-range owners, the literal GR2 afterimage layer choreography and side effects, and the rest of the official remote melee transport rather than in charge-family fallback or basic ordered subframe playback. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `CharacterLoader.cs`, `CharacterAssembler.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `CharacterData.cs`, `MapSimulator.RemoteUsers.cs`, `PreparedSkillHudRules.cs`, `ClientMeleeAfterimageRangeResolver.cs`, `AfterImageChargeSkillResolver.cs`, `MeleeAfterimagePlaybackResolver.cs`, `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` (`CActionMan::Init`, `CActionMan::GetWeaponAfterImage`, `CActionMan::CreateAfterimageLayer`, `CActionMan::GetMeleeAttackRange`, `get_action_name_from_code`, `CUser::GetAfterimageUOL`, `CUser::RegisterAfterimage`, `CUserRemote::OnAttack`, `CUserRemote::OnMeleeAttack`, `SecondaryStat::DecodeForRemote`, `Character/00002000.img`, `Character/Afterimage/swordOS/{0/swingO1/2,charge/1/stabO2/1}`, `Character/Afterimage/*`, `Skill/122.img/skill/1221009/{action,afterimage}`, `Skill/434.img/4341004/common/{lt,rb}`, remote temporary-stat payloads) |
| Partial | Skill cooldown UI parity | QuickSlot overlays and skill hover cost lines, both skill-book icon overlays, the hotkey and off-bar cooldown trays, the shared packet-owned/server-owned cooldown ownership seam in `SkillManager`, the `UI/Basic.img/Notice3` off-bar notice stack, and the Battleship sentinel durability-side effect path now stay on the same existing cooldown UI/runtime seam instead of parallel per-surface cooldown math. The earlier pass had already routed packet-owned started/ready notices through the shared authoritative transition helper so repeated authoritative refreshes and no-op clears no longer re-emit start or ready notices after the cooldown state was already active or already cleared. This run closes the remaining tractable cooldown-mask drift inside that same owner instead of leaving the new book masks on a duplicate stepping path: `SkillUI`, `SkillUIBigBang`, `QuickSlotUI`, and both status-bar cooldown trays now all source their `CoolTime` frame index and counter text through the same shared `SkillManager` cooldown-visual helper that sits on top of `TryGetCooldownUiState`, so the same packet-owned/server-owned state now drives mask progression, counter suppression, and Battleship-style progress-overlay suppression on every cooldown surface rather than each widget re-deriving the stepped `0..14` frame locally. The pre-Big Bang book still sources its `16` stepped `32x32` masks from `UI/UIWindow.img/Skill/CoolTime`, the Big Bang book and quick-slot still source their matching `16` stepped `32x32` masks from `UI/UIWindow2.img/Skill/main/CoolTime`, and WZ recheck still confirms both families expose the same `0..15` canvas set while `UI/Basic.img/Notice3` remains the closest currently extracted off-bar notice owner with the exact three-slice `t/c/s` stack (`266px` wide; heights `21/20/55`) and nearby `Notice4`, `Notice5`, and `Notice6` remain materially different four-slice or boxed families rather than a clearly better match. `Skill/522.img/skill/5221006/common` still authors `epad`, `epdd`, `emdd`, `emhp`, and `emmp` beside `info/type=13`, so the existing shared status-bar tooltip seam remains the right owner for Battleship-family stat labeling while the exact tray/icon art stays unresolved. The remaining parity gap is now narrower: exact cooldown-notification art family selection versus the current WZ-backed `Notice3` best fit, any last notice micro-offset or timing/fade differences beyond the shared 44px top-center seam, the exact Battleship `CTemporaryStatView` icon/tray art and any durability-specific HUD widgeting beyond the mounted override plus ordered status-bar tooltip presentation now modeled here, and any additional bespoke cooldown-only HUD widgets beyond the quick-slot, skill windows, status-bar trays, and off-bar notice stack. | Cooling-down skills now stay synchronized across quick-slot masks, both skill-book variants, both status-bar trays, and off-bar started/blocked/ready notices because those surfaces all read the same authoritative cooldown UI-state seam and now also share the same WZ-authored `CoolTime` mask family plus one shared cooldown-visual helper for frame stepping and counter text instead of mixing packet-owned overrides, authored WZ fallback timing, and per-widget overlay implementations. Packet-owned refreshes still preserve the original cooldown start tick without retriggering the start-notice seam, authoritative clears and natural expiry still stop every cooldown surface together without falling back to authored WZ timers, and Battleship-style vehicle buffs still identify themselves as `Transform` in the shared status-bar tooltip seam while keeping the WZ-authored temporary-stat order. The simulator still does not reproduce the client’s exact cooldown-notification art choice, final notice timing or micro-offset polish, exact Battleship temporary-stat tray/icon visuals, or any extra cooldown-only HUD widgets that may still live outside the currently modeled shared seams. | `QuickSlotUI.cs`, `SkillUI.cs`, `SkillUIBigBang.cs`, `UIWindowLoader.EquipmentAndStats.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `StatusBarUI.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `SkillManager.cs`, `SkillCooldownNoticeUI.cs`, `SkillCooldownNotificationTransition.cs`, `Skill/522.img/skill/5221006/common`, `UI/UIWindow.img/Skill/CoolTime`, `UI/UIWindow2.img/Skill/main/CoolTime`, `UI/Basic.img/Notice3`, `UI/Basic.img/Notice4`, `UI/Basic.img/Notice5`, `UI/Basic.img/Notice6` |
| Partial | Client-timer skill expiry and cancellation | `SkillManager` now owns a centralized client-timer registry that drives buff expiry, timed prepared-skill release, repeat-sustain expiry/mode-end, cyclone teardown, Wild Hunter swallow digest completion, WZ-timed local skill-zone expiry, and WZ-timed local summon expiry through one `UpdateClientTimer`-style seam; it also publishes ordered expiry batches and normalizes cancel requests through connected WZ `info/type` + `affectedSkill` + `dummyOf` families (including observed `type=10`, `type=16`, `type=33`, `type=1`, and multi-target `affectedSkill` links). `SummonedPool` already reused that connected-family matcher for packet-owned local summon cancellation, connected-family level recovery, authored-duration fallback, and inherited local-duration fallback across the full connected cancel family graph instead of only the packet skill id. Earlier work in this seam had already moved naturally expired packet-owned non-self-destruct summons into authored pending-removal playback before the shared family cancel request fans out, keeping local packet-owned expiry teardown off the generic immediate-die cancel path. This pass closes the next packet-owned summon-side hole in that same seam by mirroring the client-owned self-destruct expiry fallout for local packet-owned summons instead of only scheduling removal playback: once the timer bridge has already routed the local summon family through the shared expiry/cancel seam, `SummonedPool` now also dispatches local self-destruct expiry side effects before the authored removal window starts. Local packet-owned support summons keep their final support-owned suspend window on the expiry branch, and local packet-owned attack-style self-destruct summons now synthesize one last local target selection plus projectile / hit / tile-overlay presentation from the authored summon attack range instead of treating the expiry branch as visuals-only teardown. This run tightens that synthesized attack branch one step closer to the packet-owned runtime the client keeps in `CSummoned::Update`: natural-expiry self-destruct target selection no longer reorders purely by geometric range when the summon already has packet-owned attack history, and instead reuses the last authoritative packet target order first, then falls back to the existing authored-range filter and distance sort for any remaining slots. WZ was rechecked first for the representative timed packet-owned support summon that anchors this branch: `Skill/3511.img/skill/35111011` still publishes `info/type=33`, `info/minionAbility=heal`, `info/selfDestructMinion=1`, `summon/{summoned,stand,heal,die}`, and `hit`, which is the authored summon surface this expiry bridge now preserves on the local packet-owned path. The connected cancel-family WZ surface this timer bridge depends on is still the same in the current extracted data source: `Skill/3211.img/skill/32110007/info/type=10` with `affectedSkill=32001003`, `Skill/3510.img/skill/35100008/info/dummyOf=35101009&&35101010`, `Skill/3510.img/skill/35101009/info/affectedSkill=35001001`, and `Skill/2112.img/skill/21120011/info/affectedSkill=21100002&&21110003`. Direct IDA re-check of `CUserLocal::SendSkillCancelRequest` at `0x93d730` still confirms that client cancel requests own additional prepared-skill, swallow-reset, vehicle-valid, and keydown-cleanup fallout beyond packet transport alone, while `CSummoned::Update` at `0x757f70` still shows that deeper summon runtime ownership remains separate through its vec-ctrl, body-contact, attack/support dispatch, self-destruct, `ProcessAction`, and `ProcessAttack` paths; crucially for this pass, the natural-expiry self-destruct branch there still routes through `TryDoingHeal` / `TryDoingGiveBuff` / `TryDoingAttack` before `ProcessAction` and `ProcessAttack`, which is why the packet-owned local expiry seam now restores local final support/attack fallout before removal instead of treating self-destruct expiry as teardown-only. Focused regression coverage for this pass is now on disk in `UnitTest_MapSimulator/ClientSkillTimerPacketOwnedSummonExpiryParityTests.cs` for local-vs-remote Healing Robot self-destruct expiry suspend ownership on the packet-owned timer seam and for the new packet-target-order preference inside synthesized local self-destruct expiry attacks. The row remains partial because the simulator still does not mirror the client's full timer catalog or the full summon-side expiry fallout for packet-owned summons: local synthesized self-destruct expiry attacks now preserve prior packet target order when that evidence exists, but their target acquisition and combat results still fall back to the current authored-range approximation instead of the full `CSummoned::Update` runtime when no authoritative target history is available, remote packet-owned self-destruct expiry still relies on incoming packet/runtime ownership rather than local synthesis, and other client-only `SendSkillCancelRequest` consumers beyond the currently surfaced cancel-side-effect list are still outside the current local WZ + packet + connected-family timer view. | Timed skill teardown now runs through one shared local seam instead of scattered expiry checks, manual status-bar buff cancellation no longer bypasses client-style cancel routing, and connected WZ cancel families can tear down prepared state, repeat/cyclone state, buffs, local summons, local zones, and packet-owned local summons through one normalized request surface. This run narrows the remaining packet-owned drift in the same seam that already covered local timers: packet-owned local summon-expiry batches still prime authored natural-expiry removal before the family cancel request, still coalesce same-family cancel requests, still keep local packet-owned self-destruct expiry from collapsing into teardown-only playback by restoring the client-shaped final support suspend / final attack presentation branch before removal starts, and now also preserve last authoritative packet target ordering when synthesizing the local final self-destruct attack. Remaining risk is now concentrated on unmodeled client-owned timer families, the exact `CSummoned::Update` target-selection and combat-resolution rules that still sit behind the current local packet-owned self-destruct fallback when no packet target evidence exists, remote packet-owned expiry fallout that should remain packet/runtime-owned, and any additional `SendSkillCancelRequest` consumers not yet surfaced in the current cancel-side-effect list. | `ClientSkillCancelResolver`, `SkillManager`, `SummonedPool`, `MapSimulator`, `UnitTest_MapSimulator/ClientSkillTimerPacketOwnedSummonExpiryParityTests.cs` (`CUserLocal::UpdateClientTimer`, `CUserLocal::SendSkillCancelRequest`, `CSummonedPool::OnCreated`, `CSummoned::Update`, `Skill/3511.img/skill/35111011/info`, `Skill/3511.img/skill/35111011/summon`, `Skill/3511.img/skill/35111011/hit`, `Skill/3211.img/skill/32110007/info/type`, `Skill/3211.img/skill/32110007/info/affectedSkill`, `Skill/3510.img/skill/35100008/info/dummyOf`, `Skill/3510.img/skill/35101009/info/affectedSkill`, `Skill/2112.img/skill/21120011/info/affectedSkill`) |
| Partial | Skill restrictions by map/state | The simulator still keeps the broader shared restriction seam already landed for this row: parsed map `fieldLimit` bits for `Unable_To_Use_Skill`, `Move_Skill_Only`, `Unable_To_Use_Mystic_Door`, the Mechanic-only `Unable_To_Use_Rocket_Boost` branch, and the mount / broader vehicle `Unable_To_Use_Taming_Mob` branch; live `MapInfo` ownership for Coconut, Snowball, and Guild Boss basic-action owners; WZ-authored root `noSkill` and `info/noCancelSkill`; Mu Lung Dojo `mobMassacre/disableSkill`; the authored massacre `countEffect/*/skillUse` counter seam; shared mob-disease skill lockouts including `StopMotion` (`136`); ladder/rope vetoes for swallow plus the broader `DoActiveSkill_ShootAttack` family; and scripted direction-mode ownership that tears down prepared or upkeep-driven skills once local control is taken away. This pass closes another remaining client-only transient-state branch in that same evaluator instead of introducing a new attack-path-only guard: direct IDA re-check of `CUserLocal::TryDoingMagicAttack` at `0x92a240` still shows a hard reject while `CAvatar::IsOnPlayingOneTimeAction` is active, and the simulator now exposes the matching local state through `PlayerCharacter.IsPlayingClientOwnedOneTimeAction` so the shared `PlayerSkillStateRestrictionEvaluator` rejects active skill requests during that client-owned one-time-action window and the existing prepared/upkeep teardown paths consume the same message if that transient state begins after cast start. Focused on-disk regression coverage for this pass is now in `UnitTest_MapSimulator/PlayerSkillStateRestrictionParityTests.cs` for the one-time-action restriction message path used by the shared evaluator. The remaining gap is narrower but still real: deeper `CField` availability checks and the remaining field-owned or client-only forbidden-skill branches are still incomplete. | Maps and transient local states now block skill use closer to the client from one shared place instead of scattered path-specific checks: field-authored skill bans still flow through the live map seam, prepared or sustained skills still tear down when local state, mob-disease, scripted control, or massacre counters turn invalid after cast start, and active skill requests are now also rejected while the client-owned one-time-action window is playing rather than leaving that `TryDoingMagicAttack` precondition outside the shared evaluator. The row stays `Partial` because the remaining mismatch is still concentrated in unrecovered `CField` gate branches and any additional client-only forbidden-state or special-field owners beyond the currently modeled field limits, live Coconut/Snowball/Guild Boss ownership, root `noSkill`, Dojo massacre restrictions, mob-disease lockouts, scripted control, ladder/rope gates, and the new one-time-action state seam. | `MapSimulator`, `SkillManager`, `FieldSkillRestrictionEvaluator.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `PlayerSkillBlockingStatusMapper.cs`, `PlayerCharacter.cs`, `MassacreField.cs`, `MapInfo`, `MapInfo.fieldLimit`, `MapInfo.fieldType`, `FieldLimitType`, `FieldType`, `Skill/MobSkill.img`, `Map/Map1/109080000.img/coconut`, `Map/Map1/109060000.img/info/fieldType`, `Map/Map9/980031100.img/noSkill/class`, `Map/Map9/922240000.img/noSkill/skill`, `Map/Map9/910320100.img/mobMassacre/disableSkill`, `Map/Map9/926010100.img/mobMassacre/countEffect/500/skillUse`, `Map/Map9/990000900.img/pulley`, `Map/Map9/990000900.img/healer`, `Map/Map2/200110000.img/info/noCancelSkill`, `UnitTest_MapSimulator/PlayerSkillStateRestrictionParityTests.cs` (`CUserLocal::Update`, `CUserLocal::IsSkillAvailable`, `CField::IsUnableToUseSkill`, `CField_Coconut::BasicActionAttack`, `CField_SnowBall::BasicActionAttack`, `CField_GuildBoss::BasicActionAttack`, `CField::IsMoveSkillOnly`, `CField::IsUnableToUseRocketBoost`, `CField::IsUnableToUseTamingMob`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::TryDoingSwallowAbsorb`, `CUserLocal::TryDoingMagicAttack`, `CAvatar::IsOnPlayingOneTimeAction`) |

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Summoned action-loader / frame-build parity | The client-owned summon loader seam is now implemented more directly in the existing `SkillLoader` path instead of being split between documentation and coarse `SkillData` fields. Earlier work had already moved summon frame building into the loader: summon animation loading resolves linked summon assets before branch selection, loads summon action frames through a summon-specific frame builder with the client's `120` ms `delay` fallback, preserves per-frame `z`, preserves per-frame `a0` / `a1`, prefers authored frame metadata when linked wrapper nodes publish `origin` or bounds, computes frame rectangles from authored `lt` / `rb` when present and otherwise falls back to origin-relative texture bounds, and appends a reversed copy of the action frame list when the branch publishes the reverse-style surface handled by the loader. This pass closes another tractable ownership gap inside that same seam as well. The loader now uses exact client-style summon branch groups for spawn / idle / support selection instead of letting those selectors fall through to the first available top-level branch, so `summoned` / `create` / `summon`, `stand` / `fly` / `move` / `walk` / `repeat`, and `heal` / `support` / `stand` are treated as first-class client branch families before any broader WZ-backed recovery. When none of those exact idle keys exist, the loader now falls back through the existing seam more deliberately by preferring authored attack-style idle substitutes and then the first top-level node that actually looks like a summon action branch, rather than blindly inheriting any first child. The loader also now mirrors the exact reverse-playback property string proven from IDA: `CActionMan::LoadSummonedAction` checks string-pool id `0x049F`, which resolves to `zigzag`, after frame build. The simulator now does the same, using `zigzag` as the authoritative reverse-playback flag when that property exists and only falling back to repeat-style branch-name heuristics or older exposed reverse-style properties when the client-owned key is absent from the extracted WZ surface. The earlier broader summon cache work remains in place: repeat-style summon branches are still treated as first-class summon action keys, the loader records the selected summon branch names it actually bound (`summoned`, `repeat`, `prepare`, `die`, etc.), top-level summon action branches are no longer limited to the earlier supplemental allowlist, additional authored summon action names are recovered whenever the top-level `summon/*` node actually looks like an action branch, those keys are mirrored into `SummonNamedAnimations`, and the resolved summon hit branch key is still recorded separately while nested keys such as `hit/0` and `hit/0/1` stay cached under the summon-action cache. The runtime side continues to consume that loader-owned cache directly through `SkillManager` for summon spawn, idle, prepare, removal, current branch playback, Tesla Coil's `attackTriangle`, summon hit playback, queued removal timing, and pending-removal action resolution. WZ re-checks in the current extracted data still show the authored summon branch shape that cache is consuming, including direct summon action metadata such as `Skill/3511.img/skill/35111002/summon/attackTriangle/*/delay=120`, authored idle origins such as `Skill/3511.img/skill/35111011/summon/stand/*/origin`, linked-wrapper summon branches such as `Skill/3511.img/skill/35111011/summon/heal/* -> ../stand/*`, repeat-style summon-only layouts still present in the current dataset such as `Skill/434.img/skill/4341006/summon/{effect,effect0,repeat,repeat0}`, self-destruct/removal variants such as `Skill/3511.img/skill/35111001/summon/die1`, alternate attack branches such as `Skill/501.img/skill/5010005/summon/attack2`, and explicit summon hit branches such as `Skill/1311.img/skill/13111004/summon/hit`. Focused regression coverage is now on disk in `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs` for exact spawn / idle branch selection and `zigzag` reverse-playback handling. IDA remains the owner evidence for the final unresolved pieces: `CActionMan::GetSummonedProp` still resolves the summon asset from `SKILLENTRY::GetSummonedUOL`, `SKILLENTRY::GetSummonedUOL` still returns the precomputed `sSummonedUOL` field rather than exposing where that field was originally chosen, and the simulator still does not model the full client summon action enum-to-string table directly beyond the now tighter exact branch groups and WZ-backed fallback recovery that `SkillLoader` performs. The remaining gap is therefore narrower again: the simulator still does not mirror the client's exact `SKILLENTRY::GetSummonedUOL` source when that lookup diverges from the local linked `summon` node, and it still does not own the full client enum-to-string summon action table for every summon action slot beyond the exact spawn / idle / support / attack / removal / hit branch families now recovered in the loader seam. | Without this row, future summon work is biased toward packet/runtime fixes while leaving the client's actual frame-build owner implicit. Tracking the loader explicitly keeps summon parity anchored to the real asset-selection and frame-construction seam, which is where action-name lookup, cached reuse, delay/z fallback, rect math, and reverse playback are decided before `CSummoned::Update` ever consumes the result. With the loader work now landed in the existing seam and the simulator runtime now consuming the summon-action cache more directly, the remaining parity risk is concentrated on the last client-owned asset-resolution and unrecovered action-table details rather than on basic summon frame construction, generic first-branch selection, or the reverse-flag-name seam. | summon action-loader layer (`SkillLoader`, `SkillData`, `CActionMan::GetSummonedProp`, `CActionMan::LoadSummonedAction`, `SKILLENTRY::GetSummonedUOL`, `CSummoned::Update`, `CSummonedPool::OnCreated`, `Skill/3511.img/skill/35111002/summon/attackTriangle`, `Skill/3511.img/skill/35111011/summon/stand`, `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3511.img/skill/4341006/summon/repeat`, `Skill/3511.img/skill/35111001/summon/die1`, `Skill/501.img/skill/5010005/summon/attack2`, `Skill/1311.img/skill/13111004/summon/hit`, `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs`) |

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

This helper-owner seam is currently under-tracked by the backlog even though both the client and simulator already expose concrete ownership here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow-partner and mirror helper actors | The simulator now models the client ownership split instead of treating every clone-family helper as the same WZ-backed shadow-partner path. `SkillData` / `SkillManager` recognize `Mirror Image` (`4331002`) as a distinct helper owner, `PlayerCharacter` keeps a separate mirror runtime from the existing `special/*` shadow-partner actor, and the update/render loop now drives mirror helper visibility from player action changes with a dedicated `m_tStartMirror`-style start timestamp, a 200 ms client-shaped rel-move window, and client-backed suppression for ladder, rope, morph, taming-mob, hit, and dead states. WZ re-check still confirms `skill/433.img/skill/4331002` exposes `action/0 = alert` plus buff/effect data but no `special/*` helper action branch, so Shadow Partner keeps using WZ-authored helper animations while Mirror Image continues to source the assembled avatar instead. This pass tightens the mirror source seam one more step toward the targeted `CUser::PrepareMirrorActionLayer` / `CUser::GetMirrorSrcLayer` re-check without leaving the current simulator ownership model: `CharacterAssembler` still classifies every assembled avatar part into explicit `UnderCharacter`, `OverCharacter`, `UnderFace`, `Face`, and `OverFace` render planes, `PlayerCharacter` still keeps a dedicated mirror-owned prepared five-layer cache, and mirror insertion still follows the first visible part in each plane. The new client-backed refinement is that the simulator no longer treats prepared action name, frame index, or prepare-time facing as mandatory live-plane reuse keys once the current assembled plane for that mirror source still resolves to the same per-plane source signature. IDA still shows `CUser::PrepareMirrorActionLayer` rebuilding the client mirror layer only when the helper plane is missing or its `flip` no longer matches the live `UnderFace` layer, not when the avatar simply advanced to another frame that keeps the same source-layer contents, so `PlayerCharacter` now keeps mirror reuse inside that existing seam by preferring the current live assembled plane whenever the current plane exists and still hashes to the same per-plane source signature. The prepared plane cache also no longer forces a simulator-side snapshot rebuild on turn-only drift: the cached composed mirror-plane surface is now reused across facing changes while fallback draw continues to apply the current player-facing flip at render time, which is closer to the client's `Putflip`-driven layer-state ownership than recreating a new fallback surface every turn. Focused regression coverage is now on disk in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` for the turn-only prepared-snapshot reuse rule and the relaxed live-plane reuse rule. The remaining gap is narrower and concentrated at the true live-layer-object boundary inside `CUser::PrepareMirrorActionLayer` / `CUser::Update`: IDA still shows the client creating real `IWzGr2DLayer` mirrors, copying source-layer origin from live layer handles, and then driving first-canvas insertion plus layer-parent synchronization through `InsertCanvas` and shared COM layer ownership. The simulator still does not own those shared COM layer objects, true `InsertCanvas` reparenting against the live client layer graph, exact source-layer origin snapshots from live `IWzGr2DLayer` instances, or the follow-up clone-helper rules that depend on those real client layer objects rather than simulator-owned prepared plane snapshots plus live assembled-plane fallback. | Clone-family jobs are not just tray icons or generic buff effects in the client; they own dedicated avatar-adjacent helper layers that participate in action preparation and per-frame user update. Tracking that seam explicitly keeps future parity work focused on helper-layer timing, ownership, and clone-family differentiation instead of accidentally treating shadow-partner and mirror-image visuals as solved just because the simulator already renders one WZ-backed helper actor | `PlayerCharacter.cs`, `CharacterAssembler.cs`, `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` (`skill/433.img/skill/4331002`, `CUser::PrepareActionLayer`, `CUser::PrepareShadowPartnerActionLayer`, `CUser::PrepareMirrorActionLayer`, `CUser::GetMirrorSrcLayer`, `CUser::Update`) |

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow Partner action-loader / frame-build parity | The shadow-partner helper row is no longer only a runtime/action-playback note. IDA-backed loader ownership is now explicit in this backlog too: `CActionMan::GetShadowPartnerProp` resolves the helper property for the owning skill id, and `CActionMan::LoadShadowPartnerAction` then caches by `(skillId, action)`, first tries the named raw-action branch, falls back through `get_action_name_from_code` when the helper property uses numeric action slots, enumerates the action canvases into cached helper frames, reads frame-owned canvas-property metadata before storing each frame entry, and appends the mirrored interior-frame tail when the loader's replay table marks that action. This run tightened the simulator's loader seam itself rather than the broader helper runtime: `SkillLoader` now builds a named-vs-numeric storage plan for Shadow Partner action rows so helper catalogs still store the client action name when a helper property uses numeric action keys, while preserving named-branch precedence when both the raw-action name and numeric fallback resolve to the same helper action. WZ-first recheck in the mounted export still shows the currently shipped Shadow Partner helper surfaces publishing named `special/*` branches for `4111002`, `4211008`, and `14111000`, plus the already recovered non-ghost `action/0` raw-action names (`alert2` / `alert3` / `alert4` / `alert5`, `swingO1`, `avenger`, `assaulter`, `prone2`, `savage`, `showdown`, `assassination`, `smokeshell`, `ninjastorm`, `vampire`), so the new numeric-slot fallback is a loader-parity hardening inside the existing seam rather than a newly observed WZ branch shape. Focused regression coverage is now checked in under `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs` for numeric raw-action slot normalization, named-branch precedence over numeric fallback regardless of authored order, and safe retention of unknown numeric helper keys. Remaining gap is narrower and now specific to the client-only pieces this run could not prove from WZ alone: the exact `GetShadowPartnerProp` property-name probe order beyond the currently observed `special/*` surface, the hidden `LoadShadowPartnerAction` replay-table action set that decides which helper actions append the mirrored interior-frame tail, and any additional non-WZ helper action aliases outside the currently recovered `action/0` plus raw-action table surface. | Without this row, the backlog overstates clone-helper parity by treating Shadow Partner as if it were only a `CUser`/`PlayerCharacter` timing and placement problem. The client also keeps a dedicated `CActionMan` loader for helper actions, so future work needs a named seam for cache keys, branch lookup, numeric-slot fallback ownership, frame construction, and replay-tail ownership instead of letting those questions stay buried inside the helper runtime row. | shadow-partner action-loader layer (`CActionMan::GetShadowPartnerProp`, `CActionMan::LoadShadowPartnerAction`, `get_action_name_from_code`, simulator seams: `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `RemoteUserActorPool.cs`, `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs`, `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, `Skill/1411.img/skill/14111000/special/*`, `Skill/411.img/skill/{4111001,4111003,4111005,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121003,4121004,4121008}/action/0`, `Skill/420.img/skill/{4201002,4201003,4201005}/action/0`, `Skill/421.img/skill/{4211002,4211003,4211005,4211006,4211007}/action/0`, `Skill/422.img/skill/{4221001,4221003,4221006,4221007}/action/0`, `Skill/1410.img/skill/14101006/action/0`, `Skill/1411.img/skill/{14111001,14111002}/action/0`) |

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Time Bomb, Vengeance, and ExJablin packet-application parity | The simulator still owns this `CUserLocal::OnPacket` family inside the existing local-utility packet seam: `LocalUtilityPacketInboxManager` recognizes packet types `268` / `271` / `272` plus `timebomb` / `vengeance` / `exjablin` aliases, `MapSimulator.PacketOwnedUtilityParity` decodes and applies the three payload families, and `OnExJablinApply` still arms a simulator-owned next-shot flag that is consumed by the next ranged/projectile attack and forces that whole attack to crit, matching the direct IDA re-check of `CUserLocal::OnExJablinApply` (`m_bNextShootExJablin = 1`). This pass tightens the seam one step further against the direct IDA evidence instead of only treating it as a broad alias family. The direct IDA re-check for `CUserLocal::OnTimeBombAttack` still shows the packet body as `(skillId, timeBombX, timeBombY, impactPercent, damage)`, the client-default `1500 ms` hit period before coat-option merges, the exact `if (nUserImpactDeg)` nonzero gate around the hit-reaction branch, the direct `CUser::MakeIncDecHPEffect(this, -nDamage, 0)` use of the decoded damage field, and the exact `CUserLocal::TryDoingMeleeAttack(..., bTimeBombAttack, nTimeBombX, nTimeBombY, ...)` call; `CUserLocal::OnVengeanceSkillApply` still reads a single `Int32` skill id and only branches on legacy packet id `3120010`; `CUserLocal::OnExJablinApply` still reads no payload bytes at all. The simulator now mirrors those shapes more literally in the existing skill seam: `TryDecodePacketOwnedTimeBombAttackPayload` requires the exact five-`Int32` body and now always consumes the fifth `damage` field directly without clamping away signed payload values, `TryDecodePacketOwnedVengeanceSkillApplyPayload` requires the exact one-`Int32` body, `TryDecodePacketOwnedExJablinApplyPayload` rejects any non-empty payload before the next-shot flag is armed, `ApplyPacketOwnedVengeanceSkillApply` now ignores any packet skill id except the client-confirmed legacy `3120010` before resolving the local learned alias through the existing WZ-backed skill-id candidate seam, and `ApplyPacketOwnedTimeBombAttack` now follows the client's nonzero `nUserImpactDeg` gate instead of treating only positive impact values as hit-reaction payloads. `ApplyPacketOwnedTimeBombAttack` still restores the `1500 ms` base before the existing WZ-backed invincibility-option merge, and `SkillManager.TryApplyPacketOwnedTimeBombAttack` still owns the dedicated packet-owned Time Bomb melee branch that keeps the packet-owned `timeBombX` / `timeBombY` origin, resolves the melee hitbox at the bomb position, and switches Time Bomb target ordering to point-centered selection through `CreatePacketOwnedTimeBombTargetSortKey` so the closest mob to the explosion point wins before player-facing penalties. The shared packet-skill evidence stays anchored to WZ first and client second: `String/Skill.img` resolves `4341003` as `Monster Bomb` with the `explosion occurs 3 seconds after the charm is activated` description fragment, `31101003` as `Vengeance`, and `4120010` as `Expert Throwing Star Handling` with the `the next attack will always be a Critical Attack` fragment, while the client still confirms the legacy Vengeance packet id `3120010`. Focused regression coverage is now on disk in `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs` for the strict Time Bomb / Vengeance / ExJablin payload decoders, the legacy-only Vengeance packet-id gate, the nonzero Time Bomb impact gate, and the Time Bomb target-priority helper. The remaining gap is narrower but still real: Time Bomb still does not have a client-confirmed legacy packet-to-WZ skill-id mapping beyond the current WZ-backed `Monster Bomb` candidate, and the Time Bomb coat-option inference is still intentionally scoped to the client-confirmed invincibility options rather than a general simulator-wide item-option-id population seam for arbitrary potentials. | If these handlers stay hidden inside the broad cast rows, packet-owned skill applications that bypass the normal local input path will keep looking uncovered even though the client gives them their own dispatcher and follow-up state. The simulator now has a concrete owner for those packets, keeps the shared alias seam anchored to both client evidence (`3120010` for the only client-accepted Vengeance packet id, `OnExJablinApply` as a next-shot flag, and the `OnTimeBombAttack` / `OnVengeanceSkillApply` / `OnExJablinApply` body shapes plus the `1500 ms` base reaction period / nonzero `nUserImpactDeg` gate / raw signed `damage` consumption / `TryDoingMeleeAttack(..., bTimeBombAttack, nTimeBombX, nTimeBombY, ...)` call) and WZ evidence (`4341003` Monster Bomb by name/description, `31101003` Vengeance by name for local alias resolution, and `4120010` ExJablin by description), no longer misreads the Time Bomb coordinates, no longer approximates the Time Bomb attack path as only a generic target/origin override, no longer drops nonzero negative impact payloads that the client would still treat as hit reactions, no longer clamps away signed Time Bomb damage payload values before the local reaction branch, no longer accepts non-client Vengeance packet ids, and no longer silently accepts simulator-side trailing bytes that the client packet readers do not consume. The remaining risk is now concentrated in the still-unconfirmed legacy Time Bomb skill-id mapping and the deliberately narrow visible-potential-to-item-option inference seam, not in missing ownership for the Vengeance/ExJablin packet family, Time Bomb packet decode, raw damage consumption, the nonzero impact gate, or the dedicated `bTimeBombAttack` execution path. | packet-owned local skill-application layer (`CUserLocal::OnPacket`, `CUserLocal::OnTimeBombAttack`, `CUserLocal::OnVengeanceSkillApply`, `CUserLocal::OnExJablinApply`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::ApplyInvicibleOption`, `CUser::MakeIncDecHPEffect`, `CUserLocal::SetImpact`, `MapSimulator.PacketOwnedUtilityParity`, `LocalUtilityPacketInboxManager`, `SkillManager`, `PlayerCharacter`, `CharacterPart`, `Character/00002000.img`, `String/Skill.img`, `Item/ItemOption.img`) |

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Combo counter / combo-command packet parity | `MapSimulator.PacketOwnedComboParity` now owns the simulator-side `OnIncComboResponse` bridge instead of leaving combo implied by the generic temporary-stat tray. The simulator decodes the packet-owned combo count from the combo inbox/chat transport, stores the owner count/timestamp, resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them on the same five-second alpha window, applies the client-matching fly-origin amplitude split across the least-significant digit run, and conditionally spawns the Aran combo-command attack/buff overlays with `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` first and WZ-backed named fallbacks for exports that only ship `ComboSmash`, `ComboDrain`, `ComboFenrir`, `ComboTempest`, and `ComboBarrier`. The row no longer depends on the generic `Combo` temporary-stat heuristics in `SkillManager`; it has a dedicated packet-owned HUD/effect owner, content load path, update lifetime, and draw path in `HaCreator/MapSimulator`. | If combo stays implied only by the temporary-stat tray, the backlog will overstate skill parity while missing the client's actual packet-owned combo counter, visible combo HUD/effect lifetime, and the extra combo-command visuals that hang off learned Aran skills. Tracking it explicitly keeps future work anchored to the real packet and draw owners instead of assuming the generic buff/cooldown surfaces already cover combo gameplay feedback. | packet-owned combo HUD/effect layer (`CUserLocal::OnPacket`, `CUserLocal::OnIncComboResponse`, `CUserLocal::DrawCombo`, `Effect/BasicEff.img/Combo`, `Effect/BasicEff.img/ComboCommand`) |

Buff icon plan:

1. Confirm asset sources and ordering:
   Use `UI/BuffIcon.img` for temporary-stat icon chrome and `Skill/*/icon` for per-skill icon sourcing where the client uses the skill's own icon, then validate row ordering and refresh cadence against `CUIStatusBar::Draw` and `CUserLocal::Update`.
2. Add a runtime-to-UI snapshot:
   Expose a small status-bar model from `SkillManager` that includes `skillId`, applied stat family, start time, duration, and refresh/replace semantics so the UI does not need to inspect buff internals directly.
3. Extend status-bar loading and drawing:
   Teach `UILoader` and `StatusBarUI` to load buff-slot art, render the active icon row in the client-aligned status-bar region, and reserve the interaction seam for later right-click cancel behavior.
4. Verify state synchronization:
   Check short-duration buffs, long-duration buffs, same-skill refresh, natural expiry, and `/job` cleanup so icon removal stays synchronized with the runtime and does not fight quick-slot cooldown overlays.


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. Summon actor/runtime pass:
   Close the missing `CActionMan::LoadSummonedAction` loader seam together with the packet-owned `CSummonedPool` gaps and the `CSummoned::Update`-owned movement, touch-damage, Tesla-state, and position-sync gaps instead of only extending attack metadata and per-skill summon heuristics.
4. Clone-helper parity pass:
   Tighten the dedicated `CUser::PrepareActionLayer` / `CUser::Update` helper-layer seam for shadow-partner and mirror-image families instead of leaving clone behavior folded into generic buff or summon work.
5. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
6. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
7. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".


