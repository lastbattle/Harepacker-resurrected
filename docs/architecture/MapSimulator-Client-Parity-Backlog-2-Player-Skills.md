# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`
- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Player skill execution parity

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
`CUserLocal::Update` at `0x937330` orchestrates several of these paths and confirms that key-down gauge updates, queued follow-up attacks, prepared-skill execution, repeat-skill handling, teleport, rush, flying-rush, rocket-booster start/end handling, movement-coupled shoot branches, timer-driven cancellations, and status-bar feedback are coordinated in one local-player update loop rather than isolated one-off handlers. A second IDA pass also showed that summon parity splits across three owners rather than only the actor/runtime shell: `CSummoned::Update` drives summon actor motion, active/passive vec-ctrl updates, body-contact damage checks, assist-type dispatch, Tesla Coil state handling, self-destruct timing, and final world-position sync; `CSummonedPool::OnPacket` dispatches created/removed/move/attack/skill/hit packets and `CSummonedPool::OnCreated` ties each summon to its owning user, skill id, level data, and local-user registration path; and `CActionMan::{GetSummonedProp,LoadSummonedAction}` own the WZ-side summon UOL lookup, per-action frame build, delay/z metadata, reverse-playback duplication, and cached action-entry reuse before the runtime ever updates or draws that actor.

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
The earlier backlog text tracked summon packet ownership and runtime behavior, but the targeted `CActionMan` scan shows another concrete client owner that was still missing from the document: summon action loading itself. `GetSummonedProp` resolves the summon UOL from `SKILLENTRY::GetSummonedUOL` through the resource manager, while `LoadSummonedAction` uses that property to resolve the action-specific node, enumerate each frame canvas, read per-frame `delay`, `z`, `origin`, and `rb` metadata, compute the frame bounds, and cache the assembled action entry under the summon action key. The same decompile also shows a distinct reverse-playback seam: when the source action node publishes the reverse flag, the client appends a reversed copy of the built frame list instead of leaving playback order entirely to the runtime. That means summon parity is not only `CSummoned::Update` timing and `CSummonedPool::OnPacket` ownership; it also depends on the loader that chooses the summon asset path, builds the frame table, and decides whether the runtime receives a forward-only or mirrored playback list.

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::Update` at `0x8fb8d0`

Notes:
The targeted re-check surfaced a client-owned helper seam that the backlog was not tracking explicitly. IDA decompilation shows `CUser::PrepareActionLayer` calling `CUser::PrepareShadowPartnerActionLayer` and `CUser::PrepareMirrorActionLayer` immediately after the main avatar action layer is prepared, which means clone-style helper visuals are owned by the user action-layer path rather than by the generic buff tray alone. The same `CUser::Update` re-check also exposes mirror-timing state (`m_tStartMirror`) alongside the broader avatar-update pipeline, which is enough evidence to keep shadow-partner and mirror helper behavior as a dedicated parity bucket instead of leaving it implicit inside generic skill or temporary-stat rows.

### 4. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another client-owned skill family that was not represented anywhere in the backlog text. `OnTimeBombAttack` is not just a generic damage callback: the decompile shows it decoding a skill id plus attack/action data, resolving the local skill level, calling `TryDoingMeleeAttack`, then applying hit-period/invincibility and impact side effects before forcing the avatar hit reaction. `OnVengeanceSkillApply` is also more specific than a plain temporary-stat refresh because it gates on skill id `3120010` and then calls `DoActiveSkill_MeleeAttack` through the local skill runtime, while `OnExJablinApply` arms the `m_bNextShootExJablin` flag for the next ranged-shot branch rather than behaving like a generic buff bit. That is a dedicated packet-owned skill-application seam between the broad cast/update families and the existing temporary-stat or helper rows.

### 5. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`

Notes:
The same targeted `CUserLocal::OnPacket` scan surfaced another local skill owner that the backlog still was not naming. `OnIncComboResponse` is very small but concrete: it decodes the server-authored combo count, stores it in `m_nCombo`, stamps `m_tLastSetCombo`, and immediately routes through `DrawCombo`. The `DrawCombo` decompile then shows that combo is not just another temporary-stat label. The client resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them alive on a five-second alpha window, applies fly-origin motion to the combo vectors, and conditionally spawns extra `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` layers when the corresponding Aran combo-command skills are learned. A repo search across `HaCreator/MapSimulator` still only surfaced the generic `Combo` temporary-stat labeling in `SkillManager` and did not surface any simulator-side combo packet owner, combo HUD/effect loader, or `ComboCommand` runtime, so this remains a real missing parity bucket rather than stale wording.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Player skill execution parity

The simulator has a generic skill runtime. It does not yet mirror the client's named `CUserLocal` behavior families.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Generic skill casting | The simulator now loads the full player skill catalog from `Skill.wz`, learns active skills into the runtime, supports direct cast invocation from the skill window in addition to hotkey casting, keeps GM/SuperGM skill-book resolution compatible with both `900.img` and `910.img` layouts while focusing those jobs on their own job-book window instead of the global advancement catalog, evaluates Big Bang `common` formulas in IMG-mode data sets so skills like `Power Strike` are treated as castable attacks instead of passive placeholders, mirrors skill effect anchoring/hitbox fallback by facing direction for asymmetric melee effects, sanitizes unsupported SpriteFont glyphs in the skill window so WZ-backed names no longer crash row rendering, and resolves cast SFX from `Sound/Skill.img` so skill use now plays the corresponding `Use` or fallback attack sound through the simulator audio path | This closes the previous "can only use the current job's limited skill book" runtime/UI gap and keeps special-job skill tabs populated and executable across client data variants | `SkillManager.cs`, `SkillLoader.cs`, `SkillUIBigBang.cs`, `MapSimulator.cs`, `UIWindowLoader.cs` |
| Implemented | Buff stat application | Buff logic now applies `PAD`, `PDD`, `MAD`, `MDD`, `ACC`, `EVA`, `Speed`, and `Jump` directly to the player build and cleanly removes them when buffs expire or refresh | Active buff skills now change simulator stats and movement-relevant values instead of only a partial subset | `SkillManager.ApplyBuffStats` |
| Partial | Buff icon / temporary-stat UI parity | Active buffs still drive the WZ-backed status-bar tray with live duration countdowns sourced from `UI/BuffIcon.img`, the tray still supports client-style right-click cancellation directly from the live buff list, `UILoader` still normalizes and loads the full `BuffIcon.img` icon surface, and the simulator still consumes the authored `name` strings plus `buff/*` traversal order from that same catalog so the nine observed stat-family icons (`incEVA`, `incPAD`, `incPDD`, `incMAD`, `incMDD`, `incACC`, `incCraft`, `incJump`, `incSpeed`) keep their WZ-authored display names and ordering instead of a duplicated simulator-only table. The broader non-`BuffIcon.img` family catalog still resolves through the recovered v95 `SecondaryStat` layout, and this run closes the remaining family-owner drift inside that seam: later-authored `ignoreMobDamR` still folds into the existing `Ignore DEF` family path, and `SkillManager.GetStatusBarBuffEntries` now separates the authored family owner from the earliest resolved client-backed tray lane instead of using one shared “primary” family for both. That means multi-family buffs can now keep the WZ-authored support-family owner in tooltip/family text while still sorting and iconing on the earliest recovered lane they actually publish. Focused regression coverage is now on disk in `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` for authored-owner-vs-lane splits on `bdR` + `indiePad` and `ignoreMobDamR` + `indieSpeed`. The remaining gap is narrower but still real: exact tray order is client-backed for the WZ-authored icon families plus the recovered v95 `SecondaryStat` slice, but some families still have no direct recovered v95 temp-stat owner or still appear to depend on runtime-owned grouping/icon state beyond `UI/BuffIcon.img` and the currently parsed skill metadata, especially exact icon ownership for `Boss Damage`, some generic reward/offense cases that are not explicit in the recovered v95 surface, and other later percent-style families whose final client placement or icon choice is still not observable from the current WZ plus recovered-client evidence | Buff-heavy jobs are visible on the HUD, can be cleared without waiting for expiry, and no longer collapse as often into a fully generic tray entry while tooltip text and family ownership stay closer to the WZ stat-family surface. The known nine `BuffIcon.img/buff/*` families still use the WZ-authored names and family order directly, Big Bang-era payload families such as `mhpR` / `mmpR`, `pddR` / `mddR` / `accR` / `evaR`, `damR` / `indieDamR`, `criticaldamageMin` / `criticaldamageMax`, `expR` / `dropR` / `mesoR`, `bdR`, and both `ignoreMobpdpR` and `ignoreMobDamR` now stay on their resolved tray lanes without depending on tooltip text heuristics first. Named multi-stat support buffs also no longer drift to whichever family sorts first: the tray keeps the authored support-family owner for tooltip text and family display while still ordering and iconing the entry by the earliest resolved client-backed lane it publishes. The remaining gap is now concentrated on exact client lane/icon ownership for the families that still are not directly observable from `UI/BuffIcon.img` plus the recovered v95 `SecondaryStat` surface alone | `SkillManager.ActiveBuffs`, `SkillManager.ConfigureBuffIconCatalog`, `SkillManager.GetStatusBarBuffEntries`, `SkillLoader.CreateLevelData`, `SkillLoader.NormalizePassiveStatAliases`, `RemoteAffectedAreaSupportResolver.cs`, `StatusBarUI.cs`, `UILoader.cs`, `MapSimulator.Content.cs`, `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs`, `docs/GMS v95.h` (`UI/BuffIcon.img`, `UI/BuffIcon.img/united/buff`, `UI/BuffIcon.img/united/anniversary`, `Skill/*/common/indiePad`, `Skill/*/common/indieMad`, `Skill/*/common/indieAcc`, `Skill/*/common/indieEva`, `Skill/*/common/indieSpeed`, `Skill/*/common/indieJump`, `Skill/*/common/indieMhp`, `Skill/*/common/indieMmp`, `Skill/*/common/indieAllStat`, `Skill/*/common/pddR`, `Skill/*/common/mddR`, `Skill/*/common/accR`, `Skill/*/common/evaR`, `Skill/*/common/asrR`, `Skill/*/common/terR`, `Skill/*/common/criticaldamageMin`, `Skill/*/common/criticaldamageMax`, `Skill/*/common/damR`, `Skill/*/common/indieDamR`, `Skill/*/common/expR`, `Skill/*/common/dropR`, `Skill/*/common/mesoR`, `Skill/*/common/bdR`, `Skill/*/common/ignoreMobpdpR`, `Skill/*/common/ignoreMobDamR`, `Skill/131.img/1311008/common/str`, `Skill/312.img/3121007/common/dex`, `Skill/322.img/3221006/common/dex`, `Skill/*/info/switchDamtoMP`, `Skill/*/info/invincible`, `SecondaryStat`, `CUIStatusBar::Draw`, `CUserLocal::Update`) |
| Partial | Melee / ranged / magic resolution | The runtime now preserves WZ `lt/rb` attack bounds, resolves melee and magic targets with separate ordering rules, gives projectile skills family-specific collision ordering plus homing steering, keeps homing active even when WZ `ball` data also marks the projectile as exploding, detonates WZ `ball/explode` projectiles on impact so nearby mobs within `explodeRadius` are resolved under the same effective target cap, respects the effective projectile hit cap from both `level/mobCount` and `ball/mobCount`, routes WZ `info/type` through the skill-family classifier so no-ball spell and shoot families no longer collapse into melee resolution by default, now also treating client-confirmed shoot-family `info/type=52` skills such as Aran `21120006` as ranged even when WZ omits a `ball` node, routes WZ `info/type=32` plus `info/massSpell=1` spell families such as Mage `Slow` / `Seal` back onto the magic lane instead of leaving those no-ball area spells on melee defaults, and now also keeps the client-confirmed Outlaw no-ball shoot-family helpers `5211004` / `5211005` on the ranged lane despite their melee-looking WZ `info/type=1`, while the shared shoot-family resolver also carries `5211006` so the loader and runtime use one client-backed table instead of drifting apart. Direct no-projectile ranged skills still get their own fallback hitbox and target ordering instead of reusing the melee branch, route through the client-shaped `DoActiveSkill_ShootAttack` fallback split so melee-first families probe the close-range lane before falling back to ranged resolution while the client-confirmed direct-shoot families stay on the shoot lane, preserve the special client one-offs where Gunslinger `Double Fire` (`5001003`) and Outlaw `Triple Fire` (`5210000`) still attempt the melee lane before falling back to shoot despite otherwise living in the shoot-family helper table, and thread that same melee-first-vs-shoot preference through deferred moving-shoot preferred-target selection instead of always snapshotting a ranged-only target. Projectile families still route WZ `info/rectBasedOnTarget=1` through an additional target-anchored rectangle after first impact so client rect-attack families such as `3001004`, `3101005`, and `5201002` no longer depend on raw projectile overlap alone, re-resolve chained follow-up targets from the live impact/serial origin using the WZ `range` bounce radius plus nearest-neighbor chaining instead of only replaying the initial overlap snapshot, stage multi-shot projectile release from WZ `ballDelay` / `ballDelayN` timing instead of front-loading every projectile on cast start, assign WZ `info/multiTargeting=1` projectile volleys to distinct live targets inside the authored ranged hitbox before spawn so skills such as `2301005`, `3001004`, `5301000`, and `20021166` no longer fire every shot through the same generic spread-first path when valid separate targets already exist, derive projectile collision bounds from the live WZ ball frame size and origin while sweeping those bounds across the projectile's previous-to-current travel step so fast or large projectiles no longer rely on a single centered point-instant overlap check, thread per-target client damage override/decay branches through the live hit order so `Arrow Bomb: Bow` (`3101005`) uses its WZ `x` damage lane instead of the generic `damage` fallback, WZ-published x-based chain falloff still applies where the skill data actually publishes that decay value, and the remaining client-owned `SKILLENTRY::AdjustDamageDecRate` fixed tables now also drive `Energy Orb` (`5121002`), `Iron Arrow: Crossbow` (`3201005`), `Piercing` (`3221001`), `Ricochet` (`33101001`), and `Blade Fury` (`4341004`) through their exact client multipliers instead of flattening those families back onto one generic damage percent. This run also mirrors more of the client-owned shoot-entry precheck and ammo lane: ranged attack casts still validate the expected shooting-weapon family before firing, still skip that weapon gate for the confirmed client exceptions that do not use a shooting weapon at all (`5121002`, `11101004`, `15111006`, `15111007`, `21100004`, `21110004`, `21120006`, `33101007`, `4121003`, `4221003`), now parse WZ `level/common/bulletConsume` alongside `bulletCount` so client-ammo-heavy skills such as `Skill/411.img/4111005/common/bulletConsume = 3` and `Skill/412.img/4121006/common/bulletConsume = 200` no longer collapse to a generic single-ammo assumption, and now also parse WZ `common/itemConsume` for the client-confirmed Outlaw elemental shot helpers `5211004` / `5211005` so the live inventory-ammo resolver can require the authored pellet family instead of accepting any generic bullet stack. When an inventory runtime is present the simulator now resolves a concrete compatible USE-slot with enough stack quantity instead of only checking aggregate ammo presence, doubles the resolved per-cast ammo requirement while Shadow Partner is active to stay closer to the client-owned `GetProperBulletPosition` consumption lane, now mirrors the client-owned active-bullet-index branch by resolving the marked active projectile item as an exact item-id requirement instead of silently falling back to a different compatible family, still allows another stack of that same selected item id to satisfy the cast because the client scans by item id rather than a single slot index, and now also mirrors the CASH-cosmetic active-index branch by treating a marked active CASH projectile as an exact item-id requirement first, allowing another stack of that same CASH item id to satisfy the cosmetic lookup when the marked slot cannot be used, and only then falling back to generic family scan when no active CASH item is marked (`5020xxx` arrows, `5021xxx` throwing stars, `5022xxx` bullets), while carrying the client helper's `nWeaponItemID == 1472063 -> 2060xxx` arrow one-off into compatible-ammo matching instead of flattening everything to weapon-code-only rules. The live projectile runtime now also snapshots those resolved `pnItemID` / `pnCashItemID` ownership outputs per projectile at spawn time, so delayed `ballDelay` releases keep the exact USE ammo id plus the matched CASH cosmetic family that the cast resolved instead of dropping that client-owned selection as soon as the precheck returns. | Skills are closer to the client across both classifier and execution seams: no-ball mass-spell debuffs no longer inherit melee ordering, the missing Outlaw shoot-family helpers no longer collapse back onto the melee lane just because WZ publishes `info/type=1` without `ball`, no-projectile ranged skills now use the client-shaped melee-first vs direct-shoot split instead of one unconditional ranged branch, deferred moving-shoot targeting no longer snapshots only the ranged lane when the live client family would try a close-range strike first, and direct shoot casts now reject the wrong weapon family or missing ammo in the same high-level place the client gates `DoActiveSkill_ShootAttack` before attack resolution while also consuming a real compatible ammo stack rather than a simulator-wide count. The elemental Outlaw helpers now go one step further: WZ-authored `itemConsume = 2331000 / 2331001` is carried into the level-data seam and used to filter the resolved USE slot by the same fire-pellet (`2331xxx`) vs ice-pellet (`2332xxx`) family split that the client applies inside `GetProperBulletPosition`, so those skills no longer accept an arbitrary gun-bullet stack just because the weapon family matches. The shoot-ammo resolver is also closer to the client-owned selection path itself: a marked active bullet now means "find this exact projectile item id or fail the ammo branch" instead of "prefer this slot and then fall back to anything compatible", another stack of that same selected item id can still satisfy the cast because the client scans the whole USE inventory by item id, compatible CASH projectile cosmetics are now surfaced through the same family scan instead of being entirely ignored, a marked active CASH projectile now follows the same exact-item-id-first behavior (with same-item stack fallback) before falling back to generic family scan, the special client arrow one-off for weapon `1472063` no longer collapses onto the generic weapon-code table, and delayed projectiles now retain the exact resolved USE/CASH ownership snapshot that `GetProperBulletPosition` produced for the cast instead of losing it once the runtime moves on to later skill requests. The remaining gap is narrower and is now centered on the last client-only `GetProperBulletPosition` outputs and precheck side branches beyond the newly modeled exact active-item, elemental-pellet, cash-projectile-family, special-weapon, and per-projectile ownership snapshot one-offs, especially the still-undrawn cosmetic bullet-position presentation itself, any remaining active-index metadata beyond exact USE/CASH item id selection and same-item fallback, the last vehicle or one-time-action and ladder-family nuances that still live inside the client precheck, any residual shoot-family classifier one-offs beyond the confirmed helper tables, and edge-case timing or packet-owned branch state that still lives outside WZ-authored geometry | `SkillManager.ProcessMeleeAttack`, `ProcessMagicAttack`, `ProcessNoProjectileRangedAttack`, `SpawnProjectile`, `CheckProjectileCollisions`, `ResolveQueuedSerialTargets`, `ResolveDeferredPreferredTargetMobId`, `SkillLoader.ParseSkillInfo`, `SkillLoader.CreateLevelData`, `ClientShootAttackFamilyResolver`, `ClientShootAmmoResolver`, `UnitTest_MapSimulator/ClientShootAmmoResolverParityTests.cs` (`Skill/521.img/5211004/info/type`, `Skill/521.img/5211004/common/itemConsume`, `Skill/521.img/5211005/info/type`, `Skill/521.img/5211005/common/itemConsume`, `Skill/521.img/5211006/info/type`, `Skill/411.img/4111005/common/bulletConsume`, `Skill/412.img/4121006/common/bulletConsume`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::DoActiveSkill_MagicAttack`, `CUserLocal::GetProperBulletPosition`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `is_shoot_skill_not_switched_to_melee_attack@0x6edcf0`, `is_shoot_skill_not_using_shooting_weapon@0x6edbf0`, `is_shoot_skill_not_consuming_bullet@0x6eeaf0`, `is_correct_bullet_item@0x861360`, `is_correct_bullet_cashitem@0x6ebcf0`, `is_pellet_fire_item@0x861300`, `is_pellet_ice_item@0x861330`, `SKILLENTRY::AdjustDamageDecRate`) |
| Partial | `DoActiveSkill_*` family parity | Generic movement, summon, and prepare/charge families now execute through the existing `SkillManager` seams instead of falling back to one generic cast lane. Movement-family dispatch now separates teleport snaps, grounded rush, flying-rush glide, jump-rush launches, and bound-jump starts from the client-confirmed `TryDoingTeleport` / `TryDoingRush` / `TryDoingFlyingRush` / `DoActiveSkill_BoundJump` splits; Teleport Mastery riders authored as `info/affectedSkillEffect=bodyAttack&&stun` stay off the direct-cast surface and fire as passive follow-up data after the owning teleport resolves; WZ-linked helper branches with `info/affectedSkill` plus `info/affectedSkillEffect` no longer surface as standalone castable actives; Battle Mage aura recasts cancel or replace the active aura instead of stacking; aura-dot riders keyed by `info/affectedSkillEffect=dot` plus `info/dotType=aura` pulse once per second while the owning aura is active; WZ `info/mesoExplosion` skills now resolve pickup ownership against the live local character id before consuming nearby mesos inside the authored `lt/rb` box and then applying their attack payload; WZ `info/zoneType=invincible` skills create a persistent local protection zone; admin-skill execution now enforces GM/SuperGM ownership; bound-jump gating now respects `info/avaliableInJumpingState=1`, client-owned grounded exceptions such as `11101005`, `33001002`, and `35101004`, Wild Hunter swim/fly rejection, and WZ `info/requireHighestJump=1`; moving-shoot startup still resolves from the strongest loaded movement-action branch, deferred moving-shoot execution still mirrors the client-owned queued-level gate by dropping the delayed shot when the live learned level no longer matches the stored moving-shoot entry level instead of silently re-scaling the queued cast at fire time, and this pass also tightened the queue-time action-owner chooser against `get_random_shoot_attack_action(...)`: skills whose WZ `info/type` is `3` no longer consume authored `action/*` appointment rows on the deferred moving-shoot surface and instead fall back to the shared client random-action lane, while non-`3` skills still keep the authored appointment/randomized branch selection. Rocket Booster still uses dedicated start/upkeep/landing seams that defer resource spend until launch, re-read the learned level at launch time, preserve the authored normal-vs-tank startup branch, keep the client `250 + 20 * floor(level / 4)` stored vertical launch formula, clear the stored launch marker on the first upkeep tick after startup, and end on the same post-attack clear cadence used by `TryDoingRocketBoosterEnd`. Packet-owned summons now mirror more of the `CSummoned::Update` passive lane too: taunt/puppet summons still resolve body-contact damage against mobs targeting that summon, WZ `info/selfDestructMinion=1` summons still schedule their authored final-attack window before removal, and this pass tightened passive `UpdatePassive` fallback so packet-owned summons without a move snapshot resolve their effective passive movement from the loaded WZ summon branch profile first and then fall back to the packet `moveAbility` when the skill profile is absent or still stationary, which keeps follow/hover summons moving and lets anchor-bound hover summons keep re-emitting their passive effect from motion instead of freezing when the packet stream drops to passive vec-ctrl updates. Focused regression coverage for the meso-ownership lane plus the moving-shoot type-`3` chooser split is now on disk in `UnitTest_MapSimulator/MesoExplosionParityTests.cs` and `UnitTest_MapSimulator/MovingShootParityTests.cs`; | The remaining gap is now narrower and concentrated in the last client-owned movement and packet choreography rather than the generic execution lanes. The simulator still simplifies the exact moving-shoot entry snapshot and request choreography inside `TryDoingSmoothingMovingShootAttack`, especially the still-unrecovered client `m_nAttackActionType` table, the full shared `get_random_shoot_attack_action(...)` selection table beyond the now-modeled `info/type == 3` appointed-action bypass, the exact `nActionSpeed` calculation path from live temporary stats, and the unresolved `CAntiRepeat` repeat-count initialization or bypasses beyond the now-modeled position-threshold and level-match gates; the final highest-jump timing window beyond the current WZ-backed near-apex gate; any remaining teleport timing / snap choreography around the client-owned delayed `m_teleport` commit window beyond the now-modeled follow-up mastery trigger; any remaining Rocket Booster packet choreography beyond the now-modeled launch-marker, launch-speed formula, and end-loop timing; deeper packet-owned summon assist dispatch and per-family `OnSkill` / `OnAttack` ownership beyond the now-modeled body-contact / passive-movement / expiry cadence; and the remaining field- or packet-owned special cases that are not recoverable from WZ plus the currently confirmed client seams alone. | `SkillManager`, `SkillLoader`, `PlayerCharacter`, `PlayerCombat`, `PlayerSkillStateRestrictionEvaluator`, `SummonedPool`, `PacketOwnedSummonUpdateRules`, `UnitTest_MapSimulator/MesoExplosionParityTests.cs`, `UnitTest_MapSimulator/MovingShootParityTests.cs` (`CUserLocal::DoActiveSkill_*`, `CUserLocal::DoActiveSkill_BoundJump`, `CUserLocal::TryDoingTeleport`, `CUserLocal::TryDoingRush`, `CUserLocal::TryDoingFlyingRush`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CSummoned::Update`, `CAntiRepeat::TryRepeat`, `get_random_shoot_attack_action`, `Skill/*/info/type`, `Skill/*/info/affectedSkillEffect`, `Skill/*/info/avaliableInJumpingState`, `Skill/*/info/requireHighestJump`, `Skill/*/info/mesoExplosion`, `Skill/*/info/selfDestructMinion`) |
| Partial | Shadow Partner render/action parity | The simulator now treats Shadow Partner as a buff-owned companion render/action surface with WZ-loaded `special/*` action tables for `4111002`, `4211008`, and `14111000`, client-shaped action resolution order (`LoadShadowPartnerAction` helper-row lookup before plain raw-action name), raw-action-aware alias fallback, delayed attack follow-up keyed from companion first-frame delay, blocking `create*` and attack completion hold semantics, client-lane displacement/interpolation (`+/-30`, `+50`, `200 ms`), per-frame `a0/a1` alpha interpolation, and under-face insertion-layer ownership. This run closes another playback seam without widening ownership beyond the existing resolver and draw paths. WZ-first comparison of the current exported thief/night-walker `action/0` surface tightened the non-ghost helper-row coverage again: besides the earlier alias-only rows (`avenger`, `assaulter`, `prone2`, `savage`, `showdown`, `assassination`, `smokeshell`, `ninjastorm`, `vampire`), the currently mounted export also publishes exact helper-facing raw actions such as `alert2`, `alert3`, `alert4`, and `swingO1` at `Skill/411.img/skill/{4111001,4111003,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121004}/action/0`, `Skill/420.img/skill/{4201002,4201003}/action/0`, and `Skill/421.img/skill/{4211003,4211005,4211007}/action/0`, so the active WZ-confirmed non-ghost helper family for Shadow Partner jobs is now covered by the existing exact-branch pick, indexed `alert*` remap, and helper alias rows instead of only the earlier subset. The shared playback seam is also tighter now: `ShadowPartnerClientActionResolver` owns the frame-remap rewrite helper directly, `PlayerCharacter` still uses that shared resolver for local playback, and `RemoteUserActorPool` now reuses the same helper with the remote raw-action code so indexed helper playback such as `alert2` / `alert3` / `alert4` plus `prone2`, `ladder2`, and `rope2` no longer collapses back to generic first-frame helper playback on the packet-owned remote surface. WZ-first comparison of the current exported Shadow Partner skills still narrows the `create*` question the same way: for `4111002` and `4211008`, `special/create2`, `create3`, `create4`, and their `_s` / `_f` variants are byte-for-byte equivalent in the current data set, so the remaining chooser gap is not observable in the loaded WZ surface for this row and remains only a future-data/client-only risk. Remaining gap is narrower but still open: unrecovered client-only `LoadShadowPartnerAction` non-ghost helper rows not surfaced by the current WZ `action/0` set, additional frame-index rewrites beyond the currently evidenced `ladder2` / `rope2` / `prone2` / indexed `alert*` set, exact under-face ordering relative to other non-mirror under-face overlays that are still simulator-owned rather than real client layer objects, and any residual one-shot alpha/follow-up choreography kept only in client layer state. | Shadow Partner now mirrors companion playback through a dedicated skill-owned action catalog with client-shaped helper ordering and raw-action fallback, including the earlier helper aliases plus the currently exported exact non-ghost helper rows (`alert2`, `alert3`, `alert4`, `swingO1`) from the active Shadow Partner job families. Helper-frame preservation still includes `prone2` in addition to `ladder2`/`rope2`/indexed `alert*`, the shared under-face seam still keeps Shadow Partner ahead of mirror-image insertion in the same order the client prepares those helper layers, and the remote Shadow Partner draw path now reuses that same playback-remap seam instead of degrading indexed helper actions back to generic first-frame playback. Focused resolver tests for the `showdown` raw-action alias path, indexed helper-frame remaps, and shared playback remap are now on disk. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `ShadowPartnerClientActionResolver.cs`, `RemoteUserActorPool.cs`, `UnitTest_MapSimulator/ShadowPartnerClientActionResolverTests.cs`, `docs/GMS v95.h` (`CActionMan::LoadShadowPartnerAction@0x423cf0`, `CUser::PrepareActionLayer@0x8e3070`, `CUser::PrepareShadowPartnerActionLayer@0x8e1730`, `action_mapping_for_ghost@0x406500`), `Skill/*/{4111002,4211008,14111000}/special/*`, `Skill/411.img/skill/{4111001,4111003,4111005,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121003,4121004,4121008}/action/0`, `Skill/420.img/skill/{4201002,4201003,4201005}/action/0`, `Skill/421.img/skill/{4211002,4211003,4211005,4211006,4211007}/action/0`, `Skill/422.img/skill/{4221001,4221006}/action/0`, `Skill/1410.img/skill/14101006/action/0`, `Skill/1411.img/skill/14111002/action/0` |
| Partial | Queued follow-up attack families | Attack skills that publish WZ `finalAttack` follow-up maps still queue a deferred extra strike after the initiating melee or projectile hit resolves, gate that follow-up by the equipped weapon type both when the proc is queued and again when it executes, reuse the learned follow-up skill's proc rate, carry the preferred target forward into the queued execution path, and retain that queued target's world-position snapshot through direct follow-up resolution plus any later deferred moving-shoot or projectile hop so the simulator can keep biasing back toward the original target lane even if the source mob disappears before fire time. The queued branch still preserves the stored facing through deferred hitbox and projectile resolution even when it takes an extra deferred moving-shoot hop before firing, overwrites any still-pending final-attack proc with the newest one so the simulator matches the client's single pending follow-up slot instead of stacking multiple deferred final attacks, keeps that overwrite scoped to the final-attack slot instead of clearing unrelated queued summon hits, and still carries that same overwrite one step farther by tagging queued final-attack projectile releases themselves so a newer final attack also evicts any older still-pending delayed projectile spawn that was already armed past the moving-shoot payload seam but had not fired yet. Thunder Breaker `Spark` (`15111006`) still runs through its own timed attack-triggered proc family by treating the WZ `info/condition=attack` + `info/chainAttack=1` + `info/chainattackPenalty=1` cast as a timed buff with a separate pending spark slot, and the queued Spark branch still re-enters the shared shoot/projectile attack path instead of a simulator-only direct-hit loop, carrying the saved trigger mob id plus both the saved source origin and target-position snapshot back into projectile spawn so the deferred proc keeps the client-shaped shoot-entry choreography and stored target lane even when the queued branch later takes an extra deferred moving-shoot hop before firing; the simulator also still mirrors that single-slot behavior through Spark's own delayed projectile-release seam, so a newer queued Spark can evict an older still-pending delayed Spark projectile before fire time instead of only replacing the earlier queue entry. WZ `info/chainAttack=1` + `info/chainattackPenalty=1` serial families such as `2221006` and `5121002` still use their own single pending serial slot, preserve queued facing, recheck weapon type before the deferred branch executes, derive later chain hits from the stored origin mob/position even when the opener only resolved one live hit, and keep deferred moving-shoot payloads from jumping ahead of a due queued serial branch by treating that serial slot as part of the same queued follow-up ordering gate that already protected due final-attack and Spark playback. Queued final-attack, Spark, and serial execution still re-read the live learned skill level when the deferred branch actually fires instead of consuming the level snapshot captured when the proc was queued, still route queued cast startup back through the normal cast-effect seam so those deferred branches force the stored facing onto the avatar before the queued action starts, keep secondary cast-effect layers, and honor the same client-owned transient avatar-effect suppression path the immediate cast entry already uses, and still carry the client-only ranged final-attack `TryDoingShootAttack(..., 65, ...)` shoot-range hint through the queued entry plus any extra deferred moving-shoot hop so direct ranged final attacks keep the wider client-shaped deferred target search instead of falling back to the simulator's generic ranged offset. This run narrows the remaining `TryDoingShootAttack` gap again by extending the delayed queued projectile-origin refresh path from queued final attacks to queued Spark projectile releases as well: once a queued final-attack or queued Spark projectile is actually due, the simulator now re-reads the live avatar `handMove` / `hand` / body-origin shoot point, reapplies that as the projectile spawn plus owner origin, and re-aims from that late client-shaped origin instead of holding the older armed release point all the way through a delayed `ballDelay` seam. Queued final attacks and queued Spark also still restore the compatible USE/CASH projectile selection they armed with before re-entering cast startup, moving-shoot scheduling, and any later delayed projectile release, so those branches no longer inherit unrelated live ammo metadata from an intervening cast. WZ-authored `x` chain-penalty decay and the client-owned fixed multiplier tables for no-`x` families still both apply through the deferred or timed chain path as well, so queued follow-up families no longer flatten back onto one generic damage percent after the opener. This run also preserves the earlier queued delayed-projectile fire-time ammo-slot refresh for queued final-attack and queued Spark projectile releases while keeping the originally armed ammo item ids, so delayed queued releases keep client-shaped late `GetProperBulletPosition` slot bookkeeping without inheriting unrelated ammo families. Focused helper coverage is now on disk in `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` for queued final-attack and queued Spark fire-time-origin qualification plus the shared projectile-origin refresh helper. The remaining gap is now concentrated in the last client-only shoot-entry bookkeeping beyond the current queued-slot overwrite, delayed-projectile overwrite, queued ammo snapshot, queued cast startup, delayed-fire shoot-origin refresh, and fire-time queued slot-index refresh, especially exact `GetProperBulletPosition` / `TryDoingShootAttack` side effects, cosmetic bullet-position rendering, cash-bullet visual ownership, or any residual shot metadata that the client still derives internally at queued fire time | Final-attack style procs still no longer collapse into the initiating cast timing, queued strikes still cancel if the player changes to a non-matching weapon before the deferred attack fires, if a newer follow-up proc replaces the pending one before it can execute, or if the follow-up skill has been locally unlearned before the queued branch actually fires, and replacing a pending final attack still does not wipe unrelated queued summon damage. Once a queued final attack or queued Spark has already progressed into a delayed projectile release, a newer proc in that same client-owned slot still clears the older still-pending projectile instead of letting it survive past the queue replacement point, while unrelated projectile work remains intact. Deferred moving-shoot payloads also stop dropping out when a due queued follow-up family should run first because the payload now stays armed until after that queued final-attack, Spark, or serial branch resolves instead of being dequeued and lost. Queued follow-up casts that pass through the simulator's extra deferred moving-shoot payload path still keep the stored facing/origin snapshot, preserve the client-confirmed ranged final-attack shoot-range override (`65`) through both deferred target acquisition and the actual delayed fire step, revalidate the live learned level again at the actual deferred fire time instead of silently snapping back to the player's live turn state or consuming a stale queued level, and keep the queued target-position snapshot alive across both direct execution and deferred projectile targeting so a vanished opener mob does not force those branches straight back onto generic live ordering. This run also closes the delayed queued projectile-origin hole that was still open inside the queued Spark delayed-release seam: queued ranged final attacks and queued Spark delayed projectile releases now both refresh back onto the avatar's live `handMove` / `hand` / body-origin shoot point before fire instead of holding the older armed origin through the entire delayed release path. Active `Spark` buffs still produce their own delayed follow-up through the same shared shoot/projectile branch the client uses while preserving the stored trigger origin and target-position lane for projectile spawn, and WZ-marked serial-attack plus no-`x` fixed-table families still keep their deferred damage falloff aligned to the same post-opener hit order used by live chain resolution. The remaining mismatch is now mostly in deeper client-owned shoot-entry bookkeeping such as exact `GetProperBulletPosition` / `TryDoingShootAttack` side effects, cosmetic bullet-position rendering, cash-bullet slot visuals, and any residual shot metadata beyond the now-snapshotted USE/CASH projectile item ownership, queued final-attack and Spark delayed fire-time shoot origin, and queued delayed fire-time slot-index refresh | `SkillManager`, `SkillData`, `SkillLoader`, `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` (`Skill/1511.img/skill/15111006/info/{condition,chainAttack,chainattackPenalty}`, `Skill/222.img/skill/2221006/info/{chainAttack,chainattackPenalty}`, `Skill/512.img/skill/5121002/info/{chainAttack,chainattackPenalty}`, `CUserLocal::GetProperBulletPosition`, `CUserLocal::TryDoingFinalAttack`, `CUserLocal::TryDoingSerialAttack`, `CUserLocal::TryDoingSparkAttack`, `CUserLocal::TryDoingShootAttack`) |
| Partial | Repeat-skill sustain families | `CUserLocal::TryDoingRepeatSkill` still owns more of the Mechanic repeat-state machine than the simulator, but this pass closes another remaining SG-88 ownership gap inside the existing repeat-sustain and summon-packet seams instead of inventing another local-only owner. WZ re-check still shows the same authored family surface that this runtime already leans on: `Skill/3512.img/skill/35121003` still owns the SG-88 summon / attack branch metadata (`info/type = 33`, `info/minionAttack = normal`, `summon/attack1`) and `Skill/3512.img/skill/35121013` still owns the tank-siege effect lane (`info/fixedState = 1`, `info/onlyNormalAttack = 1`, `effect/*/delay = 30/60/30/60`), so the new work stays on transport and confirmation plumbing rather than changing authored timings or repeat-state branch selection. Tank-siege mode-end acknowledgement remains request-time keyed end-to-end inside the shared timer seam: `SkillManager` still gates `HasPendingRepeatSkillModeEndRequest`, fallback-delay lookup, and `TryAcknowledgeRepeatSkillModeEndRequest` on the active pending request timestamp, `PlayerManager` still carries the stored pending request tick through the local fallback path, and the packet-owned resolve entry point still refuses to acknowledge anything except the currently pending `(skillId, returnSkillId, requestedAt)` lane. SG-88 manual-assist now has a real official-session ingress path in addition to the earlier synthetic `1021` fallback. Client evidence still shows `CSummonedPool::OnPacket` dispatching opcode `0x119` (`281`) into `CSummonedPool::OnAttack`, then into `CUser::OnSummonedAttack`, and the simulator now mirrors that ownership through the existing summon transport seam end-to-end: `SummonedPacketInboxManager` now decodes opcode-framed raw client packets, `/summonedpacket` now accepts `packetclientraw`, `SummonedOfficialSessionBridgeManager` can proxy a live Maple session into the existing summon inbox/dispatch path, and `MapSimulator` now drains that bridged `0x116`-`0x11B` traffic into the same `SummonedPool -> PlayerManager -> SkillManager` local-owner attack callback that already advances SG-88 confirmation bookkeeping. `SkillManager` still keeps packet-confirmed SG-88 target bookkeeping beside the pending request metadata, unions admitted target ids across authoritative local-owner summon attack packets, and resolves the request as soon as the observed `0x119` target set covers the full pending lane instead of waiting for the last local queued batch to land. Existing repeat-state behavior remains in place for fixed-state lock, `onlyNormalAttack` cast blocking, timer-driven siege/tank-siege timeout, tank-siege->tank handoff, tank/siege repeat-shot cadence (`420 ms`/`180 ms`), SG-88 branch timing (`810 ms` HUD, `2790 ms` arm, `2520 ms` teardown), and client-style manual-target admission/timing (`CheckMobInTrapezoid` strip walk, `CMob::GetHitPoint`-shaped cluster origin, `120 ms` clustered follow-up batch). Focused regression coverage for the new summon transport seam is now on disk in `UnitTest_MapSimulator/SummonedOfficialSessionBridgeManagerTests.cs` for raw opcode decode, start-auto discovery filtering, and official-session bridge start/attachment behavior. The remaining gap is narrower but still real: tank-siege mode-end still lacks the true official client/server confirmation opcode and exact request/ack ordering behind `m_bSendTankSiegeModeEnd`, and SG-88 now has a live official-session bridge for inbound `0x119/281` summon attack packets but still does not own the exact outbound request choreography that produces those packets in the first place, so the synthetic `1021` confirm path plus local queued-batch completion remain the fallback when no authoritative external request owner is present. | Mechanic repeat skills are now closer on fixed-state timing, in-state cast restrictions, repeat-owned normal attack cadence, SG-88 branch timing, staged tank-siege teardown, and confirmation ownership because both the tank-siege end lane and SG-88 manual lane now have explicit packet owners instead of only local timers or queued-batch completion. The runtime now goes one step further on SG-88 than the prior row state: real local-owner summon attack packets can reach the existing confirmation seam through `/summonedpacket packetclientraw` or the new summon official-session bridge, so authoritative `0x119/281` traffic no longer has to be replayed only through the synthetic utility path before `SkillManager` can resolve the pending target set. The remaining approximation is now concentrated in the last unknown request-side choreography: the simulator still does not know the true tank-siege ack opcode/order and still does not originate or capture the official outbound SG-88 manual-attack request lane one-for-one before the inbound summon attack packets arrive. | `SkillManager`, `PlayerCharacter`, `PlayerManager`, `SkillData.ActiveSummon`, `SummonedPool.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedSummonedParity.cs`, `Managers/PacketOwnedMechanicRepeatSkillRuntime.cs`, `Managers/LocalUtilityPacketInboxManager.cs`, `Managers/SummonedPacketInboxManager.cs`, `Managers/SummonedOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/SummonedOfficialSessionBridgeManagerTests.cs` (`CUserLocal::TryDoingRepeatSkill`, `CUserLocal::TryDoingSiege`, `CUserLocal::DrawKeyDownBar`, `CUserLocal::UpdateClientTimer`, `CSummonedPool::OnPacket`, `CSummonedPool::OnAttack`, `CUser::OnSummonedAttack`, `CSummoned::TryDoingAttackManual`, `CMobPool::CheckMobInTrapezoid`, `CMob::GetHitPoint`, `Skill/3512.img/skill/35121003/{info,summon}`, `Skill/3512.img/skill/35121013/{info,effect}`) |
| Partial | Bound-jump, rocket-booster end, and smoothing moving-shoot branches | `Rocket Booster` keeps the prior parity work from this row (ground-start gate, deferred launch-facing, startup/landing cadence, and exit-action teardown window), and this pass closes another client-owned `TryDoingSmoothingMovingShootAttack` seam in the existing deferred path. Deferred moving-shoot payloads still carry `m_movingShootEntry`-style `nAttackActionType` and `nActionSpeed` metadata, still feed that queued attack-action-type into delayed preferred-target hitbox resolution, and now finish more of the queued action-owner side that the client stores in `m_movingShootEntry.nAction`. The queue-time resolver now prefers the WZ-authored `action/*` candidate list already loaded into `SkillData.ActionNames`, expands those candidates through the existing client action-lookup map, and snapshots one queue-time moving-shoot owner before execution instead of falling back immediately to whatever live avatar action name/raw action code exists when the delayed shot fires. The same deferred execution seam now also snapshots that queued action owner into the prior execution record and compares it during anti-repeat suppression, so delayed moving-shoot requests whose queued action owner differs no longer collapse behind one prior-position check just because `nAttackActionType`, `nActionSpeed`, and world-position thresholds still match. The anti-repeat seam still keeps an explicit client-shaped repeat-count structure (`RepeatCount` + `CountLimit`) in the deferred execution state, but the exact client initialization table for moving-shoot count limits remains unresolved, so this run still keeps `CountLimit` conservative while preserving the newer queued-entry ownership seams for later IDA-backed tuning. Focused source-side coverage for queue-time action selection plus normalized queued-action-owner comparison is now on disk in `UnitTest_MapSimulator/MovingShootParityTests.cs`. The row still has remaining work in the same bucket: confirmed client initialization/bypass rules for `CAntiRepeat` repeat-count limits beyond the currently mirrored `\|dx\| < 6` and `\|dy\| < 150` thresholds plus skill `33121009`, the exact `get_random_shoot_attack_action(...)` selection table and any bypass rules that refine which authored/client-mapped queued moving-shoot action should be chosen when multiple candidates exist, any additional server-confirmed Rocket Booster end-branch choreography that is still packet/client-owned outside current simulator ownership, and non-WZ bound-jump special cases that still require direct client evidence. | Movement-coupled attack skills now retain more of the client-owned moving-shoot entry payload through delayed execution instead of only preserving position or later live avatar state: delayed target-mode resolution stays tied to queued attack-action metadata, deferred shots now keep a queue-time moving-shoot action owner resolved from the skill's authored action catalog instead of whatever generic live action the avatar has drifted onto by fire time, and anti-repeat suppression no longer treats different queued moving-shoot action owners as the same deferred shot solely because they are close in world position and still share broader attack-type or speed metadata. Rocket Booster and bound-jump parity from the previous passes remain intact while this run narrows the remaining gap to the unresolved client repeat-count init/bypass table, the exact moving-shoot random action-selection rules, and the last packet/client-owned Rocket Booster or bound-jump special cases. | `SkillManager`, `SkillData`, `PlayerCharacter`, `PlayerSkillStateRestrictionEvaluator`, `UnitTest_MapSimulator/MovingShootParityTests.cs` (`Skill/*/action/*`, `CUserLocal::DoActiveSkill_BoundJump`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare`, `CAntiRepeat::TryRepeat`) |
| Partial | Key-down / charge skills | The simulator now routes key-down HUD rules through the prepared-skill runtime so client-confirmed bar suppression for `3121004`, `5201002`, `13111002`, `22121000`, `22151001`, `33121009`, `35001001`, and `35101009` no longer shows the generic status-bar gauge, preserves the `KeyDownBar4` skin for `35121003`, mirrors the client's 1-second generic gauge timing for `14111006`, caps mechanic `35001001` / `35101009` sustain after the charge window enters its actual hold phase instead of counting the 8-second branch limit from cast start, loads WZ `repeat` branches into the prepared-skill hold loop so charge families such as `4341002`, `5101004`, and `15101003` swap to their dedicated repeat effect/cadence once the hold phase begins, and now also loads and renders WZ companion prepare/hold/release branches such as `prepare0`, `keydown0`, and `keydownend0` so multi-layer prepared-skill effects like `24121000`, `5221004`, and `5721001` can draw their negative-`z` underlay branch behind the avatar instead of dropping that branch entirely. Prepared hold teardown is now closer to the client's shared timer path because capped keydown sustain starts a `prepared-release` client timer when the real hold phase begins instead of relying only on per-frame hold polling, cancel requests now only acknowledge matching local prepared-skill state while tearing that state down through the shared cancel seam instead of blindly succeeding on unmatched ids, and the local prepared-skill handshake now records both the hotkey slot and a stable per-press physical-input ownership token derived from the exact triggering key/button path so duplicate bindings share one held owner while only that same held input can issue the matching release or same-skill toggle. The client-confirmed `300 ms` exclusive-request gate from `CUserLocal::DoActiveSkill_Prepare` now also rides a broader local prepared-skill request seam instead of only prepare start, so same-family release toggles, hotkey key-up release, matching cancel requests, and the shared forced-local-release path used when control drops, the player dies, job state reloads, or other local reset paths tear down prepared state all refresh the same short exclusive window before the next prepare attempt instead of letting state clears bypass that local client throttle. Focused request-gate parity tests now lock that forced-release refresh seam in place alongside the earlier HUD coverage. The shared prepared-skill release seam now also owns the Dual Blade charge pair `4341002` / `4341003`, the Big Bang trio `2121001` / `2221001` / `2321001`, Cannon Shooter `5311002`, and Evan dragon breath `22121000` / `22151001`: WZ marks all of them with `info/chargingSkill=1`, the mage and Evan descriptions explicitly describe the attack as culminating after the charge finishes, `Skill/531.img/5311002` publishes the same `prepare` -> `keydown` -> `action/0 = noiseWave` body-backed release shape already used by the avatar parity row, `Skill/2212.img/22121000` and `Skill/2215.img/22151001` publish `prepare/action` plus release `action` branches without a looping hold branch, and the runtime now follows through by treating those holds as release-triggered execution instead of firing their payload through the generic repeating keydown loop or auto-releasing them as ordinary non-keydown prepares. This pass also closes the remaining generic-gauge timing hole in that same release-triggered seam again at the remote prepared-skill surface: when a release-owned charge family does not publish a dedicated client gauge override, the shared prepared-gauge resolver now uses the authored WZ charge window instead of the shorter prepare-animation branch length for both the local runtime and the remote prepared-skill / `/remoteuser prepare` HUD seams, so `5311002` follows `Skill/531.img/5311002/common/time` rather than arming or drawing from only `prepare/time = 1080`, while explicit client gauge overrides such as Big Bang and Evan dragon breath still keep their observed bar timings. Focused HUD parity tests now lock that shared prepared-gauge fallback in place together with the existing `Release` caption branch. The same WZ-backed remote seam now also keeps release-owned charges armed after the charge window finishes even when the hold phase is effectively unbounded: remote prepared-skill packets and the `/remoteuser prepare ... state=auto` helper now auto-enter the prepared hold phase for release-triggered families such as `5311002`, the Big Bang trio, Evan dragon breath, and the Dual Blade charge pair even when `maxHoldDurationMs = 0`, so the remote HUD stays on the client-style `Release` state until an explicit clear or release instead of aging out exactly at charge completion. The same WZ-backed release seam now also carries partial-release damage scaling for the charge families whose published descriptions explicitly say longer charging increases damage: early release for Evan `22121000` / `22151001` and Cannon Shooter `5311002` now executes through the existing release-owned prepared-skill path with damage scaled by authored charge progress instead of cancelling outright until full arm, while fully armed release still lands at full strength. `String/Skill.img/5311002/desc` also says Monkey Wave can be used immediately at full strength on a critical hit, and the local prepared-skill runtime now mirrors that WZ-backed rule by snapping an in-progress `5311002` charge straight into its armed hold state on any local critical hit instead of making the player finish the remaining charge window manually. Their local prepared-skill HUD text now also stays on the client-style `Release` branch once the charge is armed instead of falling back to the generic ready text. Dragon-owned keydown HUD also still suppresses the simulator's player fallback so Evan charge bars only render when the dragon runtime exposes a live dragon anchor instead of floating over the player when the companion is hidden or unavailable, and the same suppression now also gates packet-owned remote prepared overlays instead of letting those remote Evan bars fall back to the remote avatar when no dragon anchor resolves. This pass also moves one more release/follow-up owner table into the shared prepared-skill seam: `PreparedSkillHudRules` now owns the release-triggered and release-follow-up charge-family catalog that `RemoteUserPacketCodec` consults for official melee post-attack payload decode, so the Big Bang trio, Evan dragon breath pair, Dual Blade charge pair, Monkey Wave, and the Wild Hunter swallow release follow-up no longer rely on a stale remote-only hardcoded subset outside the prepared-skill owner seam. The remaining gap is now narrower: prepared-skill input ownership now resolves keyboard paths through scan-code-backed tokens (including extended-key separation) instead of only virtual-key ids, but the simulator still does not model the client's full raw make/break scan-code stream ownership, the rest of the wider exclusive-request catalog outside the modeled prepared-skill request family, or the release/follow-up families whose exact packet-owned charge semantics or non-WZ scaling curves are still not confirmed enough to replace their current best-effort paths one-for-one. Focused remote armed-hold coverage is now on disk in `UnitTest_MapSimulator/PreparedSkillRemoteHudParityTests.cs`, butcs` because `TrunkUI` does not yet implement the newer `ISoftKeyboardHost` members. | Charge-family skills now match more of the client's visible HUD branching, mechanic timeout behavior, timer-owned hold expiry, multi-layer prepare visuals, local input ownership, dragon-bar ownership, remote prepared-skill gauge timing, remote armed-hold persistence, and local forced-release throttling instead of always reusing one generic bar, one foreground-only cast effect, or one unowned release path; the mechanic flamethrower family now gets its full sustained window after charging before auto-release, repeated charge holds can follow the WZ-authored repeat branch, Dual Blade `Final Cut` (`4341002`), `Monster Bomb` (`4341003`), the Big Bang trio (`2121001`, `2221001`, `2321001`), `Monkey Wave` (`5311002`), and Evan dragon breath `22121000` / `22151001` no longer advertise or animate a release-owned charge while still executing through the generic hold-repeat loop or ordinary prepare timer, unrelated duplicate bindings now share one held prepared-skill owner token instead of competing with separate logical slot presses, and prepare spam respects the client's local `300 ms` exclusive-request gate across the modeled prepared-skill request family even when a keydown hold is torn down through shared local reset paths instead of a direct hotkey key-up. Local and remote Evan dragon charges also no longer fall back to a player-anchored world bar when the dragon actor is absent, remote prepared-skill overlays now keep the same authored charge-window fill timing that the local release-owned runtime already uses when the packet or command seam does not publish a separate client gauge override, and those same remote release-owned charges now stay armed on `Release` after the charge fills even when the hold phase has no authored max duration instead of disappearing at the end of the gauge. `Monkey Wave` also no longer arms and fills its charge bar off the shorter prepare animation when WZ already publishes a longer authored charge window, its partial-release attack together with Evan breath partial-release attacks now use charge-progress-scaled damage because their `String/Skill.img` descriptions explicitly say the attack grows stronger with more gathering time, and an in-progress Monkey Wave charge now also jumps straight to the fully armed release state on a local critical hit because the same WZ description explicitly says the skill can be used immediately at full strength on crit. The same release/follow-up owner list now also stays aligned between the local prepared-skill runtime and the remote-user official melee packet decode seam instead of leaving remote post-attack payload handling on an older manually maintained subset. The underlying prepared-skill execution loop is still simplified vs client behavior because, while ownership tokens now resolve from keyboard scan-code paths instead of only virtual-key ids, the simulator still does not model the client's full OS raw-input make/break scan-code stream, the broader client/server exclusive-request handshake outside these prepared-skill request seams, or the remaining release/follow-up families whose exact packet-owned charge semantics and scaling curves are not yet confirmed enough to replace their current best-effort paths. | `PreparedSkillHudRules.cs`, `SkillManager`, `StatusBarUI.cs`, `MapSimulator.RemoteUsers.cs`, `PlayerInput.cs`, `PlayerManager.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `UnitTest_MapSimulator/PreparedSkillRemoteHudParityTests.cs` (`Skill/212.img/2121001/{info/chargingSkill,desc}`, `Skill/222.img/2221001/{info/chargingSkill,desc}`, `Skill/232.img/2321001/{info/chargingSkill,desc}`, `Skill/2212.img/22121000/{info/chargingSkill,prepare,action}`, `Skill/2215.img/22151001/{info/chargingSkill,prepare,action}`, `String/Skill.img/{22121000,22151001,5311002}`, `Skill/434.img/4341002/{info/chargingSkill,common/time}`, `Skill/434.img/4341003/{info/chargingSkill,common/time}`, `Skill/531.img/5311002/{info/chargingSkill,common/time,prepare/time,keydown,time,action}`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::DrawKeyDownBar`, `CDragon::DrawKeyDownBar`) |
| Partial | Swallow, mine, cyclone, and sit-down healing branches | The simulator now auto-deploys mine summons from learned `33101008` only while the player is in the Wild Hunter jaguar riding family, suppresses that local `TryDoingMine` loop in town maps, and now mirrors the client startup cadence more closely by seeding the first continuous-movement deploy from the client-observed `update_time - 1000` lead before returning to the steady `1500 ms` mounted-movement interval. Mine movement intent is now also sourced from the same held left/right input direction the client reads before falling back to resolved foothold velocity, so brief startup acceleration no longer stalls the first mounted mine check. This pass also tightens the remaining hidden mine-ownership fallback against the same client evidence: when no mounted action is currently visible, the simulator now keeps mine eligibility only while Wild Hunter Jaguar Rider ownership is still active or inside the existing short client-owned-vehicle grace window, instead of letting an equipped jaguar alone stand in for the client's live `m_nRidingVehicleID` gate from `CUserLocal::TryDoingMine`. Cyclone still sustains as a timed `32121003` body-attack loop with `1000 ms` pulses only while the cyclone action remains active, sit-down healing still loads its support range from the summon branch metadata and honors WZ `minionAbility=heal` plus `condition=whenUserLieDown` only while the sitting player remains inside that heal box, `35111011` recovery still uses its WZ-authored HP-percent heal plus the skill text's `x=5` second re-heal lock, and the local plus packet-owned remote healing-robot seam now mirrors `CUserLocal::TryDoingSitdownHealing` more closely by polling those support summons every seated frame instead of waiting on the simulator's generic summon `attackAfter` fallback before the first heal attempt. Successful `35111011` heals now also arm the authored heal-branch support-suspend window in both the local support seam and the packet-owned remote summon skill-action seam, so those robots stop re-entering support while the current heal playback is still in flight, closer to the client's `CSummoned::TryDoingHealingRobot` `m_nSuspend` seam. This pass tightens that same support-suspend seam again against WZ `Skill/3511.img/skill/35111011/summon/heal`: local summon actor playback now measures named summon-branch duration instead of only the generic summon attack clip when deciding whether the robot is still in `Prepare` or `Attack`, and both the local plus packet-owned healing-robot runtimes now clear the authored support suspend as soon as that heal branch returns to idle instead of holding the suspend only on a stale generic-attack or fixed-timer approximation. The same heal/support seam now also consumes nearby packet-owned remote Healing Robots from broader remote-owner state instead of limiting sit-down healing to party-owned summons when another actor owns the robot. Swallow-family detection and ladder/rope gating still stay on loaded WZ metadata only (`info/swallow` plus `dummyOf`), and the swallow cancel/absorb-ack seam still accepts the visible Wild Hunter skill (`33101005`) plus its WZ-linked dummy follow-up ids (`33101006`, `33101007`). Wild Hunter swallow also still follows the client's `TryDoingSwallowMobWriggle` / `TryDoingSwallowBuff` timing more closely after absorb confirmation: the visible skill still uses the `900 ms` keydown gauge before release, early release still cancels without executing, release-triggered payloads still consume cost only once the charge completed, the local absorb-ack seam still gates capture success, a successful absorb still automatically arms the `3000 ms` digest timer plus `500 ms` wriggle cadence instead of waiting for a second local digest cast, failed or invalidated absorb outcomes now route back through the same client cancel seam used by the other swallow exits instead of silently dropping local state, and the hidden attack dummy branch (`33101007`) still consumes the captured mob before firing its follow-up ranged payload. Focused source-side coverage for the newly tightened mine fallback is now on disk in `UnitTest_MapSimulator/MineParityTests.cs`. The simulator still does not mirror the client's exact swallow packet ordering, the exact server-owned capture/digest request choreography, the remaining `TryDoingMine` riding-vehicle-id ownership nuances beyond this active-owner plus grace-window gate, the remaining packet/request-owned Healing Robot suspend choreography beyond the new authored-branch idle clear, or the remaining non-Wild-Hunter swallow-family nuances one-for-one | Several client-only active-skill branches now execute in play instead of falling through the generic cast path, Wild Hunter mine is closer to `CUserLocal::TryDoingMine` because it now respects the client town suppression and input-owned movement-start detection in addition to the existing jaguar-riding gate and first-deploy cadence, and it no longer keeps the hidden no-action fallback alive from a merely equipped jaguar once Jaguar Rider ownership has dropped. Sit-down healing follows the authored area, percent-heal, and re-heal cadence instead of restoring on every support pulse, now also considers nearby packet-owned remote healing robots through the same support-range and re-heal-lock seam the local summon runtime uses even when those robots are owned by non-party remote actors, no longer stalls the first seated heal attempt behind the simulator's generic support-summon interval gate when the client would already be frame-polling `35111011` support, and now holds successful healing-robot support inside a short authored-action suspend window instead of immediately allowing the same summon to re-enter the heal branch while that playback is still active, even when the remote summon entered that heal playback from a packet-owned `OnSkill` action first. The local summon runtime is closer again because named support branches such as Healing Robot `heal` now own their actual WZ playback window when resolving local summon actor state, and both local plus packet-owned healing robots now drop that suspend when the heal branch genuinely returns to idle instead of waiting on a looser generic-attack approximation. Cyclone no longer keeps pulsing once its sustained cyclone action has already fallen off the avatar, and Wild Hunter swallow is closer to the client's visible `33101005` input loop because charge threshold, early cancel, absorb success, absorb failure/target-loss teardown, and auto-started wriggle timing plus auto-digest completion now all ride the same release-owned swallow state and shared client cancel seam instead of requiring a second local digest trigger or leaving failed captures on a silent local clear. Swallow-family gating is also narrower and more data-backed now because false positives from skill names or action names no longer enter the swallow lane, and swallow cancel/ack callers can target the WZ-linked dummy follow-up ids directly instead of hardcoding the visible skill id. The remaining parity gap is now centered on exact swallow packet sequencing, the server-owned swallow request/ack ordering around `33101005` / `33101006` / `33101007`, the last riding-vehicle-id details in `TryDoingMine` beyond the simulator's current active-owner plus grace-window gate, the remaining packet/request-owned Healing Robot suspend choreography that still lives outside the simulator's current authored-branch idle-clear seam, and any residual swallow-family behaviors the client owns outside the current local cancel/request seam | `SkillManager`, `SkillLoader.cs`, `SkillData.cs`, `PlayerCharacter`, `MapSimulator`, `SummonedPool.cs`, `UnitTest_MapSimulator/MineParityTests.cs` (`CUserLocal::TryDoingSwallowBuff`, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::TryDoingMine`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingSitdownHealing`, `CUserPool::DoHealNearHealingRobot`, `CSummoned::TryDoingHealingRobot`, `Skill/3300.img/skill/33001001/info/type`, `String/Skill.img/33001001`, `Skill/3511.img/skill/35111011/{info,common,summon/heal,summon/heal/info}`) |
| Partial | Summon simulation | Summon-family skills still keep the broader parity work already landed here: generic summon lifecycle/timers, WZ-backed attack branch and metadata loading, projectile and target-impact branch retention, Tesla triangle targeting, self-destruct prepare/die handling, packet-owned prepare/attack visual parity, taunt-only puppet registration, shared summon rule selection between local and packet-owned seams, and the current movement/gating buckets for stationary, grounded-follow, owner-hover, and anchor-bound summons. The earlier client-backed Tesla timing fix remains in place: IDA for `CSummoned::TryDoingTaslaCoilAttack` shows Tesla impact timing is driven by the summon attack-delay window itself, with a fixed `300ms` floor plus per-target jitter inside the remaining window rather than by per-target projectile travel distance, and both the local `SkillManager` seam and the packet-owned `SummonedPool` seam still follow that rule. This run pushes the shared WZ-backed impact-presentation seam further instead of adding a new special case. `SkillLoader` now keeps authored summon target-impact presentations as structured metadata (`animation`, `hitAfter`, `pos`) rather than flattening them to animation lists alone, and both the local and packet-owned summon runtimes now consult that same per-index metadata when scheduling delayed impacts. That closes another Octopus/Tesla-family gap: local queued summon damage, local projectile lifetimes, and packet-owned delayed hit overlays now use the authored per-presentation `hitAfter` value when one exists instead of only one skill-wide fallback delay, and source-anchored target-impact entries that publish `pos=2` now render from the summon-owned/source-origin seam instead of always snapping every impact overlay to the mob center. Local Tesla queues also now retain the summon object id on their delayed attack payloads instead of leaving that field empty. The earlier WZ-backed Mechanic support fixes remain in place: hostile support summon classification still follows `String/Skill.img` instead of the older blanket `minionAbility=mes` shortcut, so Acceleration Bot EX-7 (`35111005`) no longer leaks into the friendly support lane; EX-7 still consumes its authored speed-up payload as a positive mob `Speed` status while lowering DEF; genuinely party-facing support summons still route through the dedicated friendly-support path; and AF-11 (`35121010`) still mints a persistent summon-owned damage-buff state from its authored `Damage Buff: +#x%` payload instead of falling through the hostile mob-status branch. The row remains partial because the remaining parity gap is now concentrated in the seams that still need client-only ownership data rather than more WZ plumbing: the simulator still does not reproduce the full client-exact local Tesla beam ownership/action packet payloads from `CSummoned::TryDoingTaslaCoilAttack` and `CSummoned::OnSkill` / `OnAttack`, the exact semantic mapping for every authored summon `pos` code beyond the now-modeled source-vs-target split is still incomplete, grenade-style expiry choreography beyond authored prepare/die playback is still approximate, the exact remote `OnSkill` / `OnAttack` / `OnHit` choreography for every summon family still goes beyond the current ordered impact handling, assist-target special cases not surfaced by WZ `minionAbility` / `condition` / packet assist bytes are still missing, the last one-for-one mob aggro retarget timing heuristics are still approximate, and party damage amplification still stops at the simulator's current summon-owned damage-buff seam.cs`, `ReactorPoolOfficialSessionBridgeManager.cs`, `RemoteUserPacketCodec.cs`, `MorphClientActionResolver.cs`, `MapSimulator.PacketOwnedFuncKeyParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, and `MapSimulator.ReviveOwnerParity.cs`, plus an unrelated baseline `SkillManager.cs` `currentTime` error in the Wild Hunter mine auto-deploy path. | Summon-heavy jobs are now closer not just on authored support polarity and Tesla timing, but also on the WZ-authored hit-presentation seam itself: EX-7 and AF-11 still follow the correct hostile-vs-friendly authored support lane, Tesla impacts still use the client-shaped fixed-delay-plus-jitter window, local Tesla beam visuals still preserve the attack triangle in the same packet-shaped point form already used by remote summons, and ordinary summon impacts no longer depend on one flattened target-hit surface because local and packet-owned delayed impacts now consume the same indexed `hitAfter` / `pos` metadata that WZ publishes for summon hit branches such as Octopus. Packet-owned delayed hit overlays still keep same-tick insertion order, and source-anchored impact entries now render from the summon-owned origin seam instead of always attaching to the mob. The remaining visible mismatch is now concentrated in client-only payload/ownership details, deeper remote choreography, and the still-unfinished exact `pos` semantics rather than missing WZ hit-metadata ingestion itself. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `SummonedPool.cs`, `RemoteAffectedAreaSupportResolver.cs`, IDA `CSummoned::TryDoingTaslaCoilAttack` (`0x752780`), IDA `CSummoned::OnSkill` (`0x74a940`), IDA `CSummoned::OnAttack` (`0x753340`), WZ evidence from `String/Skill.img/35111005`, `String/Skill.img/35111002`, `String/Skill.img/35121010`, `Skill/3511.img/skill/35111002/{common,info,summon,hit}`, `Skill/3511.img/skill/35111005/{common,info}`, `Skill/3512.img/skill/35121010/{common,info}`, `Skill/521.img/skill/5211002/hit/0/{hitAfter,pos}`, `Skill/521.img/skill/5211002/hit/1/pos`, and `Skill/521.img/skill/5211002/summon/attack1/info` |
| Partial | Remote summoned-pool packet parity | The simulator still keeps the packet-owned summon transport seam in `SummonedPool`, but the remote attack and hit side is closer to the client than before: it already mirrors the observed `0x116`-`0x11B` packet family, local versus remote owner registration, packet-owned move-path sampling, Tesla third-coil refresh, taunt-side ownership, packet hit damage bookkeeping, mob-authored hit-overlay lookup, and the loader/runtime handling for nested `attack/info/hit` metadata with UOL resolution plus the observed `attach` and `attachfacing` aliases, now including nested `attack/info/hit/<frame>` metadata flags. Earlier passes had already split positive-damage fallback away from the live summon branch by snapshotting the remaining authored branch window into a packet-owned one-time-action clip, keeping named summon branches on their authored timelines without a synthetic extra `prepare` prefix and letting non-Tesla summons already sitting in `Prepare` stay on authored `summon/prepare` during fallback instead of jumping straight to idle or attack. A later pass then closed a concrete `CSummoned::PrepareActionLayer` presentation gap inside that same seam by honoring authored per-frame alpha ramps on both the live summon branch and the detached one-time-action clip instead of forcing every summon frame to draw fully opaque; that change was grounded in both IDA and WZ evidence (`CSummoned::PrepareActionLayer` inserts each action-layer canvas with authored `a0` / `a1`, and WZ summon branches such as `Skill/1211.img/skill/12111004/summon/die/11` publish `delay=120`, `a0=255`, `a1=0`). This run tightens two more client-confirmed packet-owned attack seams without widening transport ownership: packet-owned attack facing now follows the current move-action bit for reactive-damage summons instead of always trusting the packet-facing flag, matching the observed `CSummoned::OnAttack` special case for `Skill/411.img/skill/4111007`, and packet-owned Phoenix/Frostprey attacks now register the same client-owned post-hit tile overlay family the client applies after attack playback. WZ inspection confirms the current exported summon skills still publish those tile surfaces and ranges through `Skill/312.img/skill/3120010/tile`, `Skill/321.img/skill/3211005/tile`, and the authored summon attack bounds, while IDA for `CSummoned::OnAttack` shows the client scheduling that overlay with a delayed fade window after the attack impact. `SummonedPool` now reuses the existing packet-owned attack impact timing seam to place that overlay over the first valid target mob with the authored summon attack rectangle and a client-shaped `200 ms` start offset plus `500 ms` `128 -> 255` alpha ramp, keeping the ownership local to the summon pool instead of broadening transport or field-effect owners. Focused regression coverage for the new rule seam is now on disk in `UnitTest_MapSimulator/PacketOwnedSummonUpdateRulesTests.cs`, covering reactive-damage facing fallback, confirmed tile-overlay skill gating, and authored tile-area construction. The remaining gap is narrower but still real: the simulator still does not synthesize every hidden client `MobAttackInfo::sHit` / `bHitAttach` case one-for-one beyond the currently observed `info/hit`, nested `info/hit/<frame>`, info-level alias, and per-frame `attack/<frame>/hit` WZ surfaces; the tile-overlay seam is still limited to the currently confirmed Phoenix/Frostprey family instead of every possible hidden `CSummoned::OnAttack` post-effect case; it still approximates the client action-layer stack with detached draw overlays instead of reproducing literal GR2 action-layer canvas ownership and ordering; and `/summonedpacket` plus `SummonedPacketInboxManager` still expose a simulator loopback seam instead of full official `CUserPool` transport and ownership decode. `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter PacketOwnedSummonUpdateRulesTests --no-restore` and | Packet-owned summons are no longer just spawn/remove stubs: they follow the observed client summon packet family, keep summon ownership ordered per user, route local-owner taunt and Tesla side effects through the same created seam, replay summon attack/hit/removal visuals, decode hit payloads far enough to show the mob-authored overlay and sound feedback from the attacking mob template, and tear down with remote-user lifetime instead of leaving orphaned field actors behind. Hit parity is closer again because the loader/runtime pair now covers nested `attack/info/hit` metadata with UOL resolution, lower-case alias support, explicit `attach` handling, client-shaped `attachfacing`-only `bHitAttach` cases, info-level `attach` / `attachfacing` aliases, nested `info/hit/<frame>` attach metadata fallback, explicit `info/hit` precedence over deeper frame-hit metadata, render-node fallback from missing or empty `info/hit` into authored per-frame `attack/<frame>/hit`, the cached WZ frame-origin anchor math that keeps packet-owned summon hit overlays visually aligned with the live summon, the same local-runtime summon playback split for named attack branches plus non-Tesla `Prepare` state, authored remaining-window progression when positive-damage fallback has to stand in for the client hit branch, a detached packet-owned one-time-action playback clip that no longer mutates the live summon branch in place, authored per-frame alpha ramps while those summon clips are replaying, reactive-damage summon attack facing that now follows the client-confirmed move-action seam instead of a generic packet-facing read, and the confirmed Phoenix/Frostprey post-attack tile overlay family with WZ-authored area sizing and the client-observed delayed fade window. The remaining parity work is concentrated in the last client-only `sHit` / `bHitAttach` cases outside the currently observed WZ surfaces, broader hidden `CSummoned::OnAttack` post-effect cases beyond the now-confirmed tile-overlay family, the literal client GR2 action-layer ownership and ordering beyond these detached overlays, and end-to-end official `CUserPool` transport rather than owner teardown, hit metadata load, packet-facing correction, or confirmed tile overlay playback being missing outright. | summon field/pool runtime (`CSummonedPool::OnPacket`, `CSummonedPool::OnCreated`, `CSummonedPool::OnRemoved`, `CSummonedPool::OnMove`, `CSummonedPool::OnAttack`, `CSummoned::OnHit`, `CSummoned::OnAttack`, `CSummoned::PrepareActionLayer`, `CUser::AddSummonedList`, `CMobTemplate::LoadAttackInfo`, `Skill/3511.img/skill/35111002/summon/{prepare,attackTriangle,hit}`, `Skill/3511.img/skill/35111011/summon/{stand,heal}`, `Skill/1211.img/skill/12111004/summon/die/11/{delay,a0,a1}`, `Skill/411.img/skill/4111007/{info,summon}`, `Skill/312.img/skill/3120010/{tile,summon}`, `Skill/321.img/skill/3211005/{tile,summon}`, `Mob/5220000.img/attack1/info/hit/attachfacing`, `Mob/3220000.img/attack2/info/hit/attachfacing`, `Mob/3220000.img/attack1/13/hit/attach`, `Mob/5160000.img/attack1/info/attach`, `Mob/6090002.img/attack1/info/attach`, `Mob/9300525.img/attack1/info/attachfacing`, `Mob/8150302.img/attack1/info/hit/1/attach`, `UnitTest_MapSimulator/SummonedPoolOneTimeActionParityTests.cs`, `UnitTest_MapSimulator/LifeLoaderAttackHitMetadataTests.cs`, `UnitTest_MapSimulator/PacketOwnedSummonUpdateRulesTests.cs`) |
| Partial | Summoned actor update/runtime parity | The simulator still keeps summon-owned actor update/runtime ownership (movement snapshots, spawn/prepare/attack/hit/die routing, Tesla armed-prepare idle fallback, self-destruct expiry handoff, packet-owned hit-overlay interrupt clearing, and named-branch timing that avoids forcing generic `summon/prepare` onto authored branches). This run closes another packet-owned action-layer seam on top of the already-landed body-contact damage and `skill1..skill6` branch ownership work. WZ re-check confirms this row's authored summon-branch surface is still present in the current extracted data set, including `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}` plus `hit`, so named summon attack branches and separate prepare/hit playback remain WZ-observable. Client confirmation from `CSummoned::OnHit`, `CSummoned::SetAttackAction`, and `CSummoned::ProcessAction` shows summon-owned action-layer refreshes rebuilding the current layer whenever positive-damage hit or attack one-time actions begin or complete. The simulator now mirrors that ownership more closely in two places: packet-owned mob-hit overlays attached to the summon still clear when packet-owned attack, skill, hit, self-destruct, removal, or one-time-action completion refreshes the summon action layer, and packet-owned attack playback completion now also clears those attached overlays exactly once when the summon action layer falls back from the finished attack branch to the steady summon state. Focused regression coverage for this pass is now on disk in `UnitTest_MapSimulator/SummonRuntimeRulesBranchParityTests.cs` and `UnitTest_MapSimulator/PacketOwnedSummonedHitOverlayParityTests.cs` for named-branch attack-duration ownership plus single-fire attack-completion refresh detection. The row remains partial because deeper client-only seams are still outside current simulator visibility: exact hidden summon HP and full `CalcDamage::PDamageSummoned` / `MDamageSummoned`, broader `CVecCtrl::UpdatePassive` side effects beyond the confirmed passive effect trigger, deeper Tesla state-machine transitions and refresh rules, and assist-family remote branch choices that depend on client-only state not exposed by current packet/WZ data. | This keeps more of real `CSummoned::Update` and `CSummoned::ProcessAction` behavior inside summon-owned runtime instead of collapsing to generic attack/removal timing: local and packet-owned body-contact damage now share the same mob-authored base-damage surface plus outgoing-damage modifiers, packet-owned body-contact no longer hides HP loss, packet-owned `OnSkill` branch ownership now lands on authored `skill1..skill6` when packet actions provide those branch ids, packet-owned named summon branches still avoid inheriting a generic prepare window, and packet-owned mob-hit overlays now get interrupted both when summon-owned action-layer changes begin and when packet-owned attack playback completes back into the steady summon layer. Remaining mismatch risk is concentrated in deeper hidden damage formulas, unmodeled passive-update side effects, deeper Tesla transitions, and client-only assist branch selectors that still cannot be derived from current packet/WZ evidence alone. | summon actor/runtime layer (`CSummoned::Update`, `CVecCtrl::UpdateActive`, `CVecCtrl::UpdatePassive`, `CSummoned::OnHit`, `CSummoned::OnSkill`, `CSummoned::SetAttackAction`, `CSummoned::SetMoveAction`, `CSummoned::ProcessAction`, `CSummoned::ProcessAttack`, `CSummoned::SetDamaged`) |
| Partial | Weapon afterimage / melee trail parity | The simulator still covers the broader WZ-backed afterimage surface already landed for this row: skill-authored plus shared `Character/Afterimage/<family>/<masteryIndex>` catalogs, weapon-item `info/afterImage` family selection with narrow sword-sibling fallback only, local basic-melee registration, charged `charge/<element>` rewrites for the confirmed Page, Paladin, Thunder Breaker, and Aran families, alias-aware action matching, `1221009 blast -> stabO2`, client-backed raw-range normalization for `1221009`, `57 -> 41`, and `74`, remote afterimage playback with fade tails, ordered remote WZ action fallback, and the single-authored plus sole-renderable remote pose promotion seams. Earlier on this row, `RemoteUserActorPool` also closed the active-charge fallback hole with direct `RegisterAfterimage` evidence by recovering the known charged Page, Paladin, Thunder Breaker, and Aran buff ids from the remote actor's last official temporary-stat payload when an official remote melee packet does not carry an explicit `chargeSkillId`, keeping those swings on the same shared `Character/Afterimage/<family>/charge/<element>` surfaces the client would register instead of dropping back to the uncharged mastery catalogs. This run keeps that work in the same seams and lifts another client-owned release slice instead of inventing another owner: `RemoteUserPacketCodec` still keeps the mask-aware `ChargeSkillId` in `RemoteUserTemporaryStatKnownState`, `RemoteUserMeleeAttackPacket` still preserves the client melee `bulletItemId`, decodes the official per-mob attack payload into shared `MobHits` (`mobId`, hit action, per-hit flags, and damage entries, including the variable-count `4211006` case), and keeps the post-attack follow-up `int` that `CUserRemote::OnAttack` reads for the WZ-backed release-owned families already cataloged in `PreparedSkillHudRules` before `OnMeleeAttack` runs. The remote actor path now also consumes that preserved release slice through the same owner seam: `PreparedSkillHudRules` resolves the remote prepared-skill release owner for direct release-triggered charge families plus the Wild Hunter swallow attack/release pair, and `RemoteUserActorPool` clears the matching remote prepared hold as soon as the official melee packet arrives instead of leaving the remote HUD armed after the client-owned release packet has already transitioned into attack playback. Focused tests are now on disk in `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` for direct release-owner resolution, the Wild Hunter swallow release-owner mapping, and representative official remote melee follow-up decode on the shared release-owned melee seam. The remaining gap is narrower again but still real: the client raw-range surface is still limited to the confirmed `1221009`, `57 -> 41`, and `74` seams; the temporary-stat fallback still only understands the already confirmed charged melee buff ids instead of a fuller remote secondary-stat decode; the simulator still approximates the real `CreateAfterimageLayer` / `RegisterAfterimage` GR2 choreography with forward-frame plus fade-tail playback; and the broader remote field packet surface still needs the rest of a one-for-one `CUserRemote::OnAttack` / `CUserRemote::OnMeleeAttack` transport lift and client-owned side effects beyond the now-consumed attack envelope, mob-hit, bullet-item, and release-follow-up payload slices | Many melee jobs rely on afterimage trails and authored swing ranges to sell timing and hit shape, and the simulator now consumes the same WZ-backed trail and arc data for local melee skill playback, local basic melee attacks, and simulator-owned remote actors while respecting the weapon item's authored afterimage family instead of forcing every sword through the long-family arcs. Charged melee buffs now stay on a less heuristic remote seam: official remote melee traffic without an explicit charge id can still recover the confirmed charged Page, Paladin, Thunder Breaker, and Aran families from the remote actor's temporary-stat state, and that recovery now prefers a mask-aware `WeaponCharge` decode path before touching the broader raw payload. The shared remote melee owner also no longer leaves release-owned charge HUD state behind once the client packet has already crossed into the actual release attack: direct release-triggered families and the Wild Hunter swallow release pair now tear down their matching remote prepared hold from the official melee packet itself instead of depending on a separate synthetic clear. The remaining visible gap is now concentrated in the broader raw-range table, fuller remote secondary-stat decode, exact GR2 afterimage choreography, and the rest of the official remote melee transport plus client-owned side effects | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `CharacterLoader.cs`, `CharacterAssembler.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `CharacterData.cs`, `MapSimulator.RemoteUsers.cs`, `PreparedSkillHudRules.cs`, `ClientMeleeAfterimageRangeResolver.cs`, `AfterImageChargeSkillResolver.cs`, `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` (`CActionMan::Init`, `CActionMan::GetWeaponAfterImage`, `CActionMan::CreateAfterimageLayer`, `CActionMan::GetMeleeAttackRange`, `get_action_name_from_code`, `CUser::GetAfterimageUOL`, `CUser::RegisterAfterimage`, `CUserRemote::OnAttack`, `CUserRemote::OnMeleeAttack`, `Character/00002000.img`, `Character/Afterimage/*`, `Skill/122.img/1221009/{action,afterimage}`, remote temporary-stat payloads) |
| Partial | Skill cooldown UI parity | QuickSlot overlays and tooltips, both skill-book variants, the hotkey and off-bar cooldown trays, the shared packet-owned/server-owned cooldown ownership seam in `SkillManager`, the `UI/Basic.img/Notice3` off-bar notice stack, and the Battleship sentinel durability-side effect path are implemented in the existing cooldown UI/runtime seams. This run closes the last tractable packet-owned notice regression in that same seam instead of adding another owner: packet-owned cooldown packets now resolve started/ready notices through the shared authoritative transition helper, so repeated authoritative refreshes and no-op clears no longer re-emit start or ready notices after the cooldown state was already active or already cleared. Focused regression coverage is now on disk in `UnitTest_MapSimulator/SkillCooldownParityTests.cs`, locking the client-backed cases that packet-owned refreshes preserve the original cooldown start tick, packet-owned clears tear the cooldown state down authoritatively instead of falling back to authored WZ cooldowns, repeated authoritative refreshes do not re-fire the start-notice seam, naturally expired server-owned cooldowns complete once without regressing to the authored WZ timer, and the packet-owned transition helper only emits on real authoritative state changes. WZ was rechecked for the off-bar notice chrome in the current extracted data source: `UI/Basic.img/Notice3` still exposes the exact three-slice `t/c/s` stack (`266px` wide; heights `21/20/55`) that the simulator already consumes, while nearby `Notice4`, `Notice5`, and `Notice6` are materially different four-slice or boxed families rather than a clearly better match for the current cooldown-owner seam. The remaining parity gap is now limited to the client's exact cooldown-notification art family selection versus the current WZ-backed `Notice3` best fit, any last notice micro-offset or timing/fade differences beyond the shared 44px top-center seam, the exact `CTemporaryStatView` / temporary-stat HUD presentation for the Battleship sentinel beyond the mounted durability override already modeled here, and any additional bespoke cooldown-only HUD widgets beyond the quick-slot, skill windows, status-bar trays, and off-bar notice stack. | Cooling-down skills stay synchronized across quick-slot masks, both skill-book variants, both status-bar trays, hover tooltips, and off-bar started/blocked/ready notices because they all read the same authoritative cooldown seam instead of mixing packet-owned overrides with authored WZ fallback timing. Packet-owned refreshes now preserve the original cooldown start tick without retriggering the start-notice seam, authoritative clears and natural expiry stop every cooldown surface together without falling back to authored WZ timers, the Battleship sentinel keeps the mounted durability override behavior already landed in the runtime, and the off-bar notice stack remains on the WZ-backed three-slice `Notice3` family until client evidence surfaces a more exact art owner. The simulator still does not reproduce the client's exact cooldown-notification art choice, final notice timing or micro-offset polish, exact Battleship temporary-stat HUD visuals, or any extra cooldown-only HUD widgets that may still live outside the currently modeled shared seams. | `QuickSlotUI.cs`, `SkillUI.cs`, `SkillUIBigBang.cs`, `UIWindowManager.cs`, `StatusBarUI.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `SkillManager.cs`, `SkillCooldownNoticeUI.cs`, `SkillCooldownNotificationTransition.cs`, `UnitTest_MapSimulator/SkillCooldownParityTests.cs`, `UI/Basic.img/Notice3`, `UI/Basic.img/Notice4`, `UI/Basic.img/Notice5`, `UI/Basic.img/Notice6` |
| Partial | Client-timer skill expiry and cancellation | `SkillManager` now owns a centralized client-timer registry that drives buff expiry, timed prepared-skill release, repeat-sustain expiry/mode-end, cyclone teardown, Wild Hunter swallow digest completion, WZ-timed local skill-zone expiry, and WZ-timed local summon expiry through one `UpdateClientTimer`-style seam; it also publishes ordered expiry batches and normalizes cancel requests through connected WZ `info/type` + `affectedSkill` + `dummyOf` families (including observed `type=10`, `type=16`, `type=33`, `type=1`, and multi-target `affectedSkill` links). `SummonedPool` already reused that connected-family matcher for packet-owned local summon cancellation, connected-family level recovery, authored-duration fallback, and inherited local-duration fallback across the full connected cancel family graph instead of only the packet skill id. This run closes another packet-owned summon expiry hole in that same seam: ordered packet-owned summon timer expirations now drive the local-owner path through the batch owner instead of only the one-off callback, the ordered payload still carries local-owner metadata, and each local natural expiry now primes summon-side natural-expiry teardown for the expired packet-owned summon object before routing back through the same `RequestClientSkillCancel` / `SendSkillCancelRequest`-style cancel surface. That keeps connected-family cancel side effects for local buffs, prepared state, repeat/cyclone state, local summons, and local zones while preserving the summon-side natural-expiry self-destruct / final-action playback that would otherwise be skipped when the cancel callback immediately queued generic summon removal. WZ checks in this run reconfirm `Skill/3211.img/skill/32110007/info/type=10` with `affectedSkill=32001003`, `Skill/3510.img/skill/35100008/info/dummyOf=35101009&&35101010`, `Skill/3510.img/skill/35101009/info/affectedSkill=35001001`, and `Skill/2112.img/skill/21120011/info/affectedSkill=21100002&&21110003`; direct IDA re-check of `CUserLocal::SendSkillCancelRequest` at `0x93d730` still confirms that client cancel requests own additional prepared-skill, swallow-reset, vehicle-valid, and keydown-cleanup fallout beyond packet transport alone. Focused regression coverage is now on disk in `UnitTest_MapSimulator/PacketOwnedSummonedTimerParityTests.cs` for ordered batch routing, prime-before-cancel ordering, and local-only filtering in the packet-owned summon expiry bridge in addition to the earlier connected-family timer inheritance coverage already kept in this row. The row remains partial because the simulator still does not mirror the client's full timer catalog and full summon-side expiry fallout: packet-owned summon families that depend on deeper `CSummoned::Update` runtime/combat ownership, summon attack-state teardown after non-self-destruct natural expiry, or other client-only `SendSkillCancelRequest` consumers beyond the currently surfaced cancel-side-effect list are still outside the current local WZ + packet + connected-family timer view. | Timed skill teardown now runs through one shared local seam instead of scattered expiry checks, manual status-bar buff cancellation no longer bypasses client-style cancel routing, and connected WZ cancel families can tear down prepared state, repeat/cyclone state, buffs, local summons, local zones, and packet-owned local summons through one normalized request surface. This run narrows packet-owned summon drift again in two places: connected-family local timers (not just the packet skill id) still drive fallback duration ownership for child/toggle summon creates, and local packet-owned summon expiry now uses the pool's ordered expiry batch plus summon-side natural-expiry priming before the shared client cancel seam runs. That means timed local packet-owned summons keep the broader client-owned cancel fallout without dropping the summon-side natural-expiry self-destruct / final-action branch first. Remaining risk is now concentrated on unmodeled client-owned timer families, non-self-destruct summon-side expiry fallout that still appears to live deeper inside `CSummoned::Update`, and any additional `SendSkillCancelRequest` consumers not yet surfaced in the current cancel-side-effect list. | `ClientSkillCancelResolver`, `SkillManager`, `SummonedPool`, `MapSimulator`, `UnitTest_MapSimulator/PacketOwnedSummonedTimerParityTests.cs` (`CUserLocal::UpdateClientTimer`, `CUserLocal::SendSkillCancelRequest`, `CSummonedPool::OnCreated`, `CSummoned::Update`, `Skill/3211.img/skill/32110007/info/type`, `Skill/3211.img/skill/32110007/info/affectedSkill`, `Skill/3211.img/skill/32110008/info/affectedSkill`, `Skill/3211.img/skill/32110009/info/affectedSkill`, `Skill/3510.img/skill/35100008/info/dummyOf`, `Skill/3510.img/skill/35101009/info/affectedSkill`, `Skill/2112.img/skill/21120011/info/affectedSkill`) |
| Partial | Skill restrictions by map/state | The simulator now enforces parsed map `fieldLimit` bits for `Unable_To_Use_Skill`, `Move_Skill_Only`, `Unable_To_Use_Mystic_Door`, the Mechanic-only `Unable_To_Use_Rocket_Boost` branch, and the mount / Mechanic vehicle `Unable_To_Use_Taming_Mob` branch through the shared skill runtime, rejects active skill use while the local player is dead, in hit-stun, seated, or prone so skill-window and hotkey casts no longer bypass those transient local states, mirrors the client's swallow-specific ladder/rope precondition in the shared evaluator, tears down client-timer / upkeep-driven skill paths such as keydown holds, cyclone pulses, mechanic mine auto-deploy, and repeat-sustain mode skills when those same local-state restrictions become invalid after cast start, now also feeds mob-disease lockouts sourced from WZ-authored `MobSkill.img` durations through that same shared state seam so direct casts, prepared-hold upkeep, repeat-sustain upkeep, cyclone, mine teardown, and Rocket Booster sustain all see `Seal` (`120`), `Stun` (`123`), `Attract` (`128`), the freeze family (`131`), and the WZ-authored polymorph pair (`172`, `173`) instead of leaving those branches split between cast-only booleans and isolated upkeep checks, routes simulator direction-mode / scripted-interaction ownership through that same shared state gate so skill-window or hotkey casts are rejected while NPC/script overlays own local control and active prepared/upkeep skills tear down once scripted control begins, widens the shared mount/vehicle classification to WZ-authored `eventTamingMob` riders plus broader client-owned vehicle buffs surfaced through `info/type=13` so `Unable_To_Use_Taming_Mob`, ladder/rope rejection, and airborne vehicle-start gating also catch non-Mechanic ownership cases such as Battleship and later event ride skills instead of a stale hardcoded subset, now also covers the client-confirmed ladder/rope veto for the Hermit/Shadower taunt branch (`4121003`, `4221003`) through the same shared evaluator instead of leaving those ids outside the common gate, now also routes live `MapInfo` ownership through the shared field restriction seam so skill casts see current map `fieldLimit` values plus the client-owned Coconut and Snowball minigame basic-attack-only wrappers instead of relying on a map-bind-time snapshot, and this pass widens the Coconut side of that seam to accept the authored root `coconut` map property instead of only `info/fieldType`, now also mirrors the WZ-authored root `noSkill` field-owner surface by rejecting casts when the live map forbids the local player's job branch via `noSkill/class` or names the exact skill id via `noSkill/skill`, and this pass widens that same `noSkill` seam to accept direct-int authored entries, exact current job ids, and the existing skill metadata / skill-book fallback path instead of only list-shaped children and derived class digits, now also threads the WZ-authored Mu Lung Dojo `mobMassacre/disableSkill` flag through that same shared field gate so direct casts plus prepared/upkeep teardown see the same runtime prohibition instead of leaving Dojo-only skill lockout as HUD state, this pass also threads the authored `mobMassacre/countEffect/*/skillUse` counter seam through that same field gate so active casts are rejected when the live massacre skill counter reaches zero instead of leaving that branch as a HUD-only counter, and now also mirrors the Guild Boss basic-action owner through the same live map seam by rejecting casts when `info/fieldType=8` or the authored root `pulley` / `healer` ownership marks a Guild Boss map. Deeper `CField` availability checks and the remaining field-owned or client-only forbidden-skill branches are still incomplete | Maps that explicitly suppress skills now block the same broad skill families in the simulator, Rocket Booster can be rejected on field entry and cast attempt and now also tears down if a shared external restriction begins after launch, seated/prone transient states no longer let active skills slip through the generic cast path, swallow casts now fail on ladder/rope like the client branch, ongoing upkeep skills stop once the player falls into a forbidden local or mob-disease state instead of continuing through it, the common mob-driven seal / stun / seduce, freeze, and polymorph lockouts now use the authored WZ timers and surface explicit rejection reasons through the same shared skill gate instead of only blocking fresh casts via side booleans or transform-only state, scripted direction-mode control now also blocks new active-skill requests while cancelling active prepared, repeat-sustain, cyclone, mine, and Rocket Booster upkeep through the same shared restriction seam instead of leaving those paths running under NPC or field-script ownership, the client taunt branch now also rejects ladder/rope casts through the shared evaluator instead of only the old swallow or vehicle subset, the `Unable_To_Use_Taming_Mob` plus client-side ladder/airborne vehicle checks now reject WZ-authored event mount skills together with broader client-owned vehicle ownership buffs such as Battleship or Jaguar Rider rather than only the earlier Mechanic-focused subset, client-owned Coconut/Snowball maps now reject active skills through the same shared seam even when the restriction needs to come from live map ownership rather than only a captured `fieldLimit` or `fieldType` value, WZ-authored root `noSkill` maps now also reject either whole job branches, exact listed skills, direct-int authored entries, exact current job ids, or the existing skill-job fallback path through that same live field seam, Mu Lung Dojo massacre maps whose WZ publishes `mobMassacre/disableSkill=1` now reject fresh skill casts while also tearing down prepared or sustained paths through that same shared evaluator instead of only zeroing the HUD-side skill counter, massacre maps whose WZ publishes `mobMassacre/countEffect/*/skillUse=1` now also reject casts whenever the live packet-owned massacre skill counter is exhausted instead of only drawing a depleted skill counter on the timerboard, and Guild Boss maps now reject active skills through the same shared evaluator when either `info/fieldType=8` or the authored `pulley` / `healer` owner roots identify the field even if the live restriction is not just a captured `fieldLimit` bit. Focused evaluator regression coverage is now on disk in `UnitTest_MapSimulator/FieldSkillRestrictionEvaluatorTests.cs` for the Guild Boss `fieldType` branch, the Guild Boss root-owner fallback, and the negative regular-map case. The remaining gap is still concentrated in the last `CField` gate branches and other per-skill client checks outside `IsSkillAvailable`, especially any additional field-owned owners beyond the now-modeled Coconut/Snowball, Guild Boss, root `noSkill`, Mu Lung Dojo `disableSkill`, and the massacre `skillUse` counter seam plus any remaining client-only forbidden-state branches beyond the currently modeled mob-disease / scripted-control / taunt-ladder set | `MapSimulator`, `SkillManager`, `FieldSkillRestrictionEvaluator.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `PlayerSkillBlockingStatusMapper.cs`, `PlayerCharacter`, `PlayerCombat`, `PlayerManager`, `PlayerMobStatusController`, `MassacreField.cs`, `MapInfo`, `MapInfo.fieldLimit`, `MapInfo.fieldType`, `FieldLimitType`, `FieldType`, `Skill/MobSkill.img`, `Map/Map1/109080000.img/coconut`, `Map/Map1/109080000.img/info/fieldType`, `Map/Map1/109060000.img/snowBall`, `Map/Map1/109060000.img/info/fieldType`, `Map/Map9/980031100.img/noSkill/class`, `Map/Map9/922240000.img/noSkill/skill`, `Map/Map9/910320100.img/mobMassacre/disableSkill`, `Map/Map9/926010100.img/mobMassacre/countEffect/500/skillUse`, `Map/Map9/990000900.img/info/fieldType`, `Map/Map9/990000900.img/pulley`, `Map/Map9/990000900.img/healer`, `Map/Map2/200110000.img/info/noCancelSkill`, `UnitTest_MapSimulator/FieldSkillRestrictionEvaluatorTests.cs`, `Skill/412.img/4121003/info`, `Skill/422.img/4221003/info` (`CUserLocal::Update`, `CUserLocal::IsSkillAvailable`, `CField::IsUnableToUseSkill`, `CField_Coconut::BasicActionAttack`, `CField_SnowBall::BasicActionAttack`, `CField_GuildBoss::BasicActionAttack`, `CField::IsMoveSkillOnly`, `CField::IsUnableToUseRocketBoost`, `CField::IsUnableToUseTamingMob`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::TryDoingSwallowAbsorb`) |

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Summoned action-loader / frame-build parity | The simulator already tracks summon packet ownership and actor/runtime behavior, but the docs were still missing the client owner that decides what that runtime actually receives. IDA decompilation shows `CActionMan::GetSummonedProp` resolving the summon asset UOL from `SKILLENTRY::GetSummonedUOL`, and `CActionMan::LoadSummonedAction` then resolving the action-specific branch, enumerating frame canvases, loading per-frame `delay` with the client's `120` ms fallback, reading per-frame `z`, `origin`, and `rb` metadata, computing each frame rectangle, caching the assembled action entry by summon action key, and appending a reversed copy of the frame list when the action node publishes the client reverse flag. The current backlog corpus does mention `CSummoned::Update`, `CSummonedPool::OnPacket`, tutor summon height lookup, and broader summon runtime rules, but it never names this loader seam or the client-owned reverse-playback branch at all, so summon parity can still look more complete than it is even when action-table selection or frame ordering diverges before the runtime starts updating. | Without this row, future summon work is biased toward packet/runtime fixes while leaving the client's actual frame-build owner implicit. Tracking the loader explicitly keeps summon parity anchored to the real asset-selection and frame-construction seam, which is where action-name lookup, cached reuse, delay/z fallback, rect math, and reverse playback are decided before `CSummoned::Update` ever consumes the result. | summon action-loader layer (`CActionMan::GetSummonedProp`, `CActionMan::LoadSummonedAction`, `SKILLENTRY::GetSummonedUOL`, `CSummoned::Update`, `CSummonedPool::OnCreated`) |

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

This helper-owner seam is currently under-tracked by the backlog even though both the client and simulator already expose concrete ownership here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow-partner and mirror helper actors | The simulator now models the client ownership split instead of treating every clone-family helper as the same WZ-backed shadow-partner path. `SkillData` / `SkillManager` recognize `Mirror Image` (`4331002`) as a distinct helper owner, `PlayerCharacter` keeps a separate mirror runtime from the existing `special/*` shadow-partner actor, and the update/render loop now drives mirror helper visibility from player action changes with a dedicated `m_tStartMirror`-style start timestamp, a 200 ms client-shaped rel-move window, and client-backed suppression for ladder, rope, morph, taming-mob, hit, and dead states. WZ re-check still confirms `skill/433.img/skill/4331002` exposes buff/effect data but no `special/*` helper action branch, so Shadow Partner keeps using WZ-authored helper animations while Mirror Image continues to source the live assembled avatar instead. This pass tightens the mirror source seam again against the targeted IDA re-check: `CharacterAssembler` classifies every assembled avatar part into explicit `UnderCharacter`, `OverCharacter`, `UnderFace`, `Face`, and `OverFace` render planes during assembly, portable-chair frame swaps preserve that plane ownership, and `PlayerCharacter` now keeps a dedicated mirror-owned prepared five-layer cache that reuses stable per-plane layer objects in the same five-layer order that `CUser::GetMirrorSrcLayer` exposes. The cache is still prepared from `UpdateMirrorImageRenderState` rather than only at draw time, the live local-player construction path still feeds `PlayerCharacter` the `GraphicsDevice`, and the prepared mirror-plane metadata now keeps the client-facing destination plane instead of flattening every prepared plane back onto one under-face-only presentation seam. The targeted `CUser::Update` re-check narrowed one more concrete seam in this pass: client mirror insertion follows live source layers rather than hidden canvas placeholders, so `PlayerCharacter.GetAvatarRenderLayerInsertionIndices` now skips null/hidden assembled parts before falling forward to the next visible plane, and mirror cache invalidation now also tracks source-part tint changes so prepared composed textures stay synchronized when the source avatar layer recolors without changing the underlying part object. Focused regression coverage is now on disk in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` for the visible-plane insertion fallback and tint-sensitive mirror source signature behavior. The remaining gap is now concentrated at the last client-only layer-object boundary inside `CUser::PrepareMirrorActionLayer` / `CUser::Update`: IDA still shows the client creating real `IWzGr2DLayer` mirrors, copying source-layer origin/flip, and then driving first-canvas insertion plus layer-parent synchronization through `InsertCanvas` and live layer handles. The simulator still does not own those shared COM layer objects, true `InsertCanvas` reparenting against the live client layer graph, exact per-layer visibility / first-canvas synchronization from source `IWzGr2DLayer` instances, or the follow-up clone-helper rules that depend on those real client layer objects rather than simulator-owned prepared descriptors, insertion indices, and textures. | Clone-family jobs are not just tray icons or generic buff effects in the client; they own dedicated avatar-adjacent helper layers that participate in action preparation and per-frame user update. Tracking that seam explicitly keeps future parity work focused on helper-layer timing, ownership, and clone-family differentiation instead of accidentally treating shadow-partner and mirror-image visuals as solved just because the simulator already renders one WZ-backed helper actor | `PlayerCharacter.cs`, `CharacterAssembler.cs`, `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` (`skill/433.img/skill/4331002`, `CUser::PrepareActionLayer`, `CUser::PrepareShadowPartnerActionLayer`, `CUser::PrepareMirrorActionLayer`, `CUser::GetMirrorSrcLayer`, `CUser::Update`) |

### 4. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Time Bomb, Vengeance, and ExJablin packet-application parity | The simulator still owns this `CUserLocal::OnPacket` family inside the existing local-utility packet seam: `LocalUtilityPacketInboxManager` recognizes packet types `268` / `271` / `272` plus `timebomb` / `vengeance` / `exjablin` aliases, `MapSimulator.PacketOwnedUtilityParity` decodes and applies the three payload families, `OnExJablinApply` still arms a simulator-owned next-shot flag that is consumed by the next ranged/projectile attack and forces that whole attack to crit, and packet-owned Vengeance/Time Bomb skill lookup still runs through the shared packet-skill alias seam. This pass tightens the Time Bomb handler itself against the direct IDA re-check instead of only broadening aliases: `TryDecodePacketOwnedTimeBombAttackPayload` now treats the packet body as `(skillId, timeBombX, timeBombY, impactPercent, damage)` rather than `(skillId, action, hitPeriodMs, impactPercent, damage)`, `ApplyPacketOwnedTimeBombAttack` no longer misuses the client Time Bomb coordinates as a raw action code, and the reaction side now follows the client-owned `OnTimeBombAttack` gate more closely by only applying hit-reaction fallout when impact is nonzero. The client-default `1500 ms` hit period is now restored as the base before the existing WZ-backed invincibility-option merge runs, so the simulator still preserves the earlier item-option parity seam while no longer trusting a packet-authored hit period the client does not decode. Earlier Vengeance/ExJablin work stays in place: the client-hardcoded Vengeance legacy/current pair (`3120010` -> `31101003`) still resolves through one candidate path, and ExJablin still contributes a WZ-backed current skill catalog entry via `String/Skill.img` description evidence for `4120010` (`Expert Throwing Star Handling`, desc fragment `the next attack will always be a Critical Attack`). Focused source coverage is now on disk in `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs` for packet-skill alias helper ordering, the exact Vengeance and ExJablin name/description matches, and the corrected Time Bomb payload field decode. The remaining gap is narrower but still real: Time Bomb still does not have a client-confirmed cross-version packet-to-WZ skill-id mapping in this alias catalog, the simulator still does not thread the client `timeBombX` / `timeBombY` coordinates all the way through a dedicated Time Bomb melee-execution branch equivalent to `TryDoingMeleeAttack(..., bTimeBombAttack = 1, nTimeBombX, nTimeBombY, ...)`, and the Time Bomb coat-option inference is still intentionally scoped to the client-confirmed invincibility options rather than a general simulator-wide item-option-id population seam for arbitrary potentials. | If these handlers stay hidden inside the broad cast rows, packet-owned skill applications that bypass the normal local input path will keep looking uncovered even though the client gives them their own dispatcher and follow-up state. The simulator now has a concrete owner for those packets, keeps the shared alias seam anchored to both client evidence (`3120010` for Vengeance and the `OnTimeBombAttack` field layout / `1500 ms` base reaction period) and WZ evidence (`31101003` Vengeance by name and `4120010` ExJablin by description), and no longer misreads the Time Bomb coordinates or forces hit-reaction fallout when the client would skip it. The remaining risk is now concentrated in the still-unconfirmed Time Bomb skill-id mapping, the still-missing dedicated Time Bomb X/Y attack lane inside the melee runtime, and the deliberately narrow visible-potential-to-item-option inference seam, not in missing ownership for the Vengeance/ExJablin packet family or in mis-decoding the Time Bomb packet body. | packet-owned local skill-application layer (`CUserLocal::OnPacket`, `CUserLocal::OnTimeBombAttack`, `CUserLocal::OnVengeanceSkillApply`, `CUserLocal::OnExJablinApply`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::ApplyInvicibleOption`, `CUserLocal::SetImpact`, `MapSimulator.PacketOwnedUtilityParity`, `LocalUtilityPacketInboxManager`, `SkillManager`, `PlayerCharacter`, `CharacterPart`, `Character/00002000.img`, `String/Skill.img`, `Item/ItemOption.img`) |

### 5. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Combo counter / combo-command packet parity | `MapSimulator.PacketOwnedComboParity` now owns the simulator-side `OnIncComboResponse` bridge instead of leaving combo implied by the generic temporary-stat tray. The simulator decodes the packet-owned combo count from the combo inbox/chat transport, stores the owner count/timestamp, resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them on the same five-second alpha window, applies the client-matching fly-origin amplitude split across the least-significant digit run, and conditionally spawns the Aran combo-command attack/buff overlays with `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` first and WZ-backed named fallbacks for exports that only ship `ComboSmash`, `ComboDrain`, `ComboFenrir`, `ComboTempest`, and `ComboBarrier`. The row no longer depends on the generic `Combo` temporary-stat heuristics in `SkillManager`; it has a dedicated packet-owned HUD/effect owner, content load path, update lifetime, and draw path in `HaCreator/MapSimulator`. | If combo stays implied only by the temporary-stat tray, the backlog will overstate skill parity while missing the client's actual packet-owned combo counter, visible combo HUD/effect lifetime, and the extra combo-command visuals that hang off learned Aran skills. Tracking it explicitly keeps future work anchored to the real packet and draw owners instead of assuming the generic buff/cooldown surfaces already cover combo gameplay feedback. | packet-owned combo HUD/effect layer (`CUserLocal::OnPacket`, `CUserLocal::OnIncComboResponse`, `CUserLocal::DrawCombo`, `Effect/BasicEff.img/Combo`, `Effect/BasicEff.img/ComboCommand`) |

Buff icon plan:

1. Confirm asset sources and ordering:
   Use `UI/BuffIcon.img` for temporary-stat icon chrome and `Skill/*/icon` for per-skill icon sourcing where the client uses the skill's own icon, then validate row ordering and refresh cadence against `CUIStatusBar::Draw` and `CUserLocal::Update`.
2. Add a runtime-to-UI snapshot:
   Expose a small status-bar model from `SkillManager` that includes `skillId`, applied stat family, start time, duration, and refresh/replace semantics so the UI does not need to inspect buff internals directly.
3. Extend status-bar loading and drawing:
   Teach `UILoader` and `StatusBarUI` to load buff-slot art, render the active icon row in the client-aligned status-bar region, and reserve the interaction seam for later right-click cancel behavior.
4. Verify state synchronization:
   Check short-duration buffs, long-duration buffs, same-skill refresh, natural expiry, and `/job` cleanup so icon removal stays synchronized with the runtime and does not fight quick-slot cooldown overlays.


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. Summon actor/runtime pass:
   Close the missing `CActionMan::LoadSummonedAction` loader seam together with the packet-owned `CSummonedPool` gaps and the `CSummoned::Update`-owned movement, touch-damage, Tesla-state, and position-sync gaps instead of only extending attack metadata and per-skill summon heuristics.
4. Clone-helper parity pass:
   Tighten the dedicated `CUser::PrepareActionLayer` / `CUser::Update` helper-layer seam for shadow-partner and mirror-image families instead of leaving clone behavior folded into generic buff or summon work.
5. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
6. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
7. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".

