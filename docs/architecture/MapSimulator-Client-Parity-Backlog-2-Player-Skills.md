# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`
- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`
- `CAnimationDisplayer::Effect_SkillUse` at `0x459100`
- `CAnimationDisplayer::RegisterBulletAnimation` at `0x455410`
- `CAnimationDisplayer::RegisterMagicBulletAnimation` at `0x455570`
- `CAfterImageBullet::Update` at `0x477aa0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Player skill execution parity

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
`CUserLocal::Update` at `0x937330` orchestrates several of these paths and confirms that key-down gauge updates, queued follow-up attacks, prepared-skill execution, repeat-skill handling, teleport, rush, flying-rush, rocket-booster start/end handling, movement-coupled shoot branches, timer-driven cancellations, and status-bar feedback are coordinated in one local-player update loop rather than isolated one-off handlers. A second IDA pass also showed that summon parity splits across three owners rather than only the actor/runtime shell: `CSummoned::Update` drives summon actor motion, active/passive vec-ctrl updates, body-contact damage checks, assist-type dispatch, Tesla Coil state handling, self-destruct timing, and final world-position sync; `CSummonedPool::OnPacket` dispatches created/removed/move/attack/skill/hit packets and `CSummonedPool::OnCreated` ties each summon to its owning user, skill id, level data, and local-user registration path; and `CActionMan::{GetSummonedProp,LoadSummonedAction}` own the WZ-side summon UOL lookup, per-action frame build, delay/z metadata, reverse-playback duplication, and cached action-entry reuse before the runtime ever updates or draws that actor.

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
This row remains `Partial`. The earlier backlog text tracked summon packet ownership and runtime behavior, but the targeted `CActionMan` scan shows another concrete client owner that was still missing from the document: summon action loading itself. WZ was rechecked first and the already-landed loader/frame-build parity still holds: `Skill/3510.img/skill/35101005` still exposes wrapper nodes beside real summon branches (`cDoor`, `sDoor`, `oDoor`, `mDoor`, `eDoor`), `Skill/501.img/skill/5010005/summon` still exposes authored custom action branches (`piratebless`, `rushBoom`, `cannonSlam`, `noiseWave`, `jump`, `hit1`) beside `attack1`-`attack4`, and `Skill/434.img/skill/4341006/summon` still remains an effect/repeat-only layout while `info/requireSkill = 4331002` still proves a visible prerequisite-style owner link on the same skill surface. This pass tightens the visible-link fallback again through another mounted skill surface the loader had not been consuming yet: passive-link nodes. The mounted export still shows summon-adjacent `psdSkill` links such as `Skill/3512.img/skill/35121006/psdSkill = {35111001,35111009,35111010}`, `Skill/3512.img/skill/35121005/psdSkill = {35121005}`, and `Skill/3510.img/skill/35100008/psdSkill = {35101010}`, which confirms that visible passive skill chaining exists beside the already-consumed `affectedSkill`, `dummyOf`, and `requireSkill` surfaces.

Client evidence stayed second: `CActionMan::GetSummonedProp` at `0x41b830` still resolves the summon UOL from `SKILLENTRY::GetSummonedUOL`, `SKILLENTRY::GetSummonedUOL` at `0x417f30` still only returns `this->sSummonedUOL`, and the same getter still rechecks as the asset base for `CActionMan::LoadSummonedAction` plus summon-owned `/effect`, `/repeat`, `/effect0`, and `/repeat0` effect loading in `CSummoned::{_LoadEffect,_UpdateEffect}`. The simulator now mirrors more of the visible graph without widening runtime ownership: `SkillLoader.EnumerateSummonSourceCandidateSkillIds` now treats `psdSkill` exactly like the other visible summon-source link families during direct and nested candidate expansion, and `FindVisibleSummonSourceParentIds` now also scans reverse passive-link parents so summon-root discovery can climb back through passive-linked owner skills before it gives up. Focused source coverage is now present in `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs` for direct passive-link ordering ahead of reverse-parent retries, nested passive-link expansion, and reverse passive-parent discovery through mounted-style job images.

The remaining gap is narrower and still concentrated on the hidden client-owned asset-resolution field itself: the simulator can now approximate more of the reachable visible graph (`affectedSkill`, `psdSkill`, reverse affected/passive parent discovery across linked candidates, dummy parents, requirements, and any linked candidate that already publishes a client-style summon UOL string), but the client still consumes the precomputed `SKILLENTRY::sSummonedUOL` string directly. The exact authoring/population path for that field still needs a follow-up client-data pass before this row can be marked implemented, especially for summons whose true owner never appears on any visible WZ-side link surface at all.

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::Update` at `0x8fb8d0`

Notes:
This row remains `Partial`. The targeted re-check surfaced a client-owned helper seam that the backlog was not tracking explicitly, and the simulator now mirrors more of that ownership split instead of treating every clone-family helper as the same WZ-backed shadow-partner path. WZ was rechecked first for `skill/433.img/skill/4331002`: it still exposes `action/0 = alert` plus effect data but no `special/*` helper action branch, which keeps `Mirror Image` (`4331002`) on the assembled-avatar helper seam while Shadow Partner continues to use the WZ-authored helper action catalog. Client evidence stayed second: `CUser::PrepareActionLayer` still calls `CUser::PrepareShadowPartnerActionLayer` and `CUser::PrepareMirrorActionLayer` immediately after the main avatar action layer is prepared; `CUser::GetMirrorSrcLayer` still returns the five avatar layer handles in fixed index order (`UnderCharacter`, `OverCharacter`, `UnderFace`, `Face`, `OverFace`); and `CUser::PrepareMirrorActionLayer` still recreates a mirror helper layer only when that helper layer is missing or its flip diverges from `m_pLayerUnderFace`, then re-parents every recreated mirror plane against `m_pLayerUnderFace`, copies origin from the live source layer, seeds the 200 ms `RelMove` from the live source-layer current time, and uses the live source-layer canvas instead of a per-piece redraw.

The simulator already kept the earlier landed ownership split and render seam for this row, and this pass narrows the remaining mismatch again: `SkillData` / `SkillManager` still recognize `Mirror Image` as a distinct helper owner, `PlayerCharacter` still keeps mirror runtime state separate from the `special/*` Shadow Partner actor, mirror suppression still follows the client-backed raw-action and state gates, and the dedicated prepared five-layer cache still inserts the full mirror package at the shared UnderFace overlay boundary. This pass now aligns the prepared-layer lifetime and transition timing more closely with the newly rechecked client seam: same-facing source refreshes still keep the prepared mirror plane's recreate timestamp even when the observed player action changes and the assembled source signature refreshes, preserved helper planes still reuse the existing prepared render target whenever the refreshed snapshot fits inside it, and the simulator now keeps a separate prepared transition-start timestamp that derives new helper-plane rel-move progress from the live source-layer current-time snapshot instead of always restarting from helper recreation time. `PlayerCharacter` still records the logical snapshot source rectangle separately from the reusable render target so shrink-or-fit refreshes keep drawing against the current prepared bounds/origin rather than the stale texture extent, still preserves the prepared placement anchor across same-facing canvas refreshes even when the incoming canvas bounds grow and the backing render target has to be replaced, and now also keeps the prepared mirror planes resident across temporary suppression states instead of eagerly discarding them the moment ladder/rope/morph/taming/hit/dead gating hides the helper. That keeps the simulator-owned prepared mirror plane closer to the client split where `PrepareMirrorActionLayer` preserves the helper layer object, seeds `RelMove` from the live source-layer timing, and `CUser::Update` keeps inserting the current source-layer canvas into that same helper layer instead of rebuilding it on every visibility or source-canvas change. Focused regression coverage for the helper-start precedence, prepared-layer recreate-time preservation, prepared source-layer transition timing, bounds-fit in-place snapshot refresh, prepared snapshot source-rect fallback, prepared placement-anchor preservation, and live-source seam is now on disk in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs`.

The remaining gap is narrower and still concentrated at the real live-layer-object boundary inside `CUser::PrepareMirrorActionLayer` / `CUser::Update`: the simulator still does not own shared `IWzGr2DLayer` mirror objects, true COM live-layer origin/current-time handles, or true `InsertCanvas` reparenting against the client layer graph, and its preserved prepared planes are still simulator-owned snapshots rather than literal helper objects whose identity survives every texture replacement the way the client keeps the same layer handle and swaps only the inserted source canvas. Follow-up clone-helper behaviors that depend on those live client layer objects rather than simulator-owned prepared plane snapshots also remain outside the current seam.

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::GetShadowPartnerProp` at `0x41ba60`
- `CActionMan::LoadShadowPartnerAction` at `0x423cf0`

Notes:
The current skill backlog already names the shadow-partner runtime/helper surface, but the targeted `CActionMan` pass shows there is still a distinct loader owner underneath it. `GetShadowPartnerProp` resolves the helper property from the skill id before action lookup, while `LoadShadowPartnerAction` caches by `(skillId, action)`, resolves either the named raw-action branch or the numeric fallback branch through `get_action_name_from_code`, enumerates the action canvases into cached frame entries, reads frame-owned metadata from the canvas properties, and appends the mirrored interior-frame tail when the loader's replay table marks that action. That means shadow-partner parity is split the same way as summons, pets, employees, dragons, mobs, NPCs, and taming-mob helpers elsewhere in the backlog set: `CUser::PrepareShadowPartnerActionLayer` and the simulator helper runtime own placement and playback state, while `CActionMan` owns the helper action catalog and the frame table those runtime paths consume.

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another client-owned skill family that was not represented anywhere in the backlog text. `OnTimeBombAttack` is not just a generic damage callback: the decompile shows it decoding a skill id plus attack/action data, resolving the local skill level, calling `TryDoingMeleeAttack`, then applying hit-period/invincibility and impact side effects before forcing the avatar hit reaction. `OnVengeanceSkillApply` is also more specific than a plain temporary-stat refresh because it gates on skill id `3120010` and then calls `DoActiveSkill_MeleeAttack` through the local skill runtime, while `OnExJablinApply` arms the `m_bNextShootExJablin` flag for the next ranged-shot branch rather than behaving like a generic buff bit. That is a dedicated packet-owned skill-application seam between the broad cast/update families and the existing temporary-stat or helper rows.

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`

Notes:
The same targeted `CUserLocal::OnPacket` scan surfaced another local skill owner that the backlog still was not naming. `OnIncComboResponse` is very small but concrete: it decodes the server-authored combo count, stores it in `m_nCombo`, stamps `m_tLastSetCombo`, and immediately routes through `DrawCombo`. The `DrawCombo` decompile then shows that combo is not just another temporary-stat label. The client resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them alive on a five-second alpha window, applies fly-origin motion to the combo vectors, and conditionally spawns extra `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` layers when the corresponding Aran combo-command skills are learned. A repo search across `HaCreator/MapSimulator` still only surfaced the generic `Combo` temporary-stat labeling in `SkillManager` and did not surface any simulator-side combo packet owner, combo HUD/effect loader, or `ComboCommand` runtime, so this remains a real missing parity bucket rather than stale wording.

### 7. Shared skill-use animation-owner parity discovered by targeted `CAnimationDisplayer` scan

- `CAnimationDisplayer::Effect_SkillUse` at `0x459100`

Notes:
The broader skill rows already cover cast logic, hit effects, prepared gauges, and helper actors, but the latest `CAnimationDisplayer` pass exposed another owner that the backlog corpus was not naming anywhere else. `Effect_SkillUse` is not a generic data lookup. The decompile shows it formatting a StringPool-backed effect UOL pattern for the requested effect name plus either the default or indexed variant, loading each resolved layer through `CAnimationDisplayer::LoadLayer`, applying the caller-supplied delay-rate override before playback, and then registering every resolved layer through `RegisterOneTimeAnimation` rather than leaving skill-use presentation on the local runtime or UI owners. The same body also keeps optional overlay-parent and auto-flip ownership at this animation-manager seam, which means visible skill-cast parity is split one level further than the current skill backlog suggests: `CUserLocal::*` owns cast admission and follow-up behavior, feature-local runtime code owns gameplay consequences, and `CAnimationDisplayer::Effect_SkillUse` owns the shared one-shot cast animation composition that many skills route through before those consequences are visible.

### 8. Bullet animation-owner parity discovered by follow-up `CAnimationDisplayer` scan

- `CAnimationDisplayer::RegisterBulletAnimation` at `0x455410`
- `CAnimationDisplayer::RegisterMagicBulletAnimation` at `0x455570`
- `CAfterImageBullet::Update` at `0x477aa0`

Notes:
The follow-up IDA scan also exposed a player-projectile presentation owner that was not named anywhere else in the backlog. `RegisterBulletAnimation` allocates a normal `CBullet` animation object with start/end times, source and destination points, target vector, z, bullet-effect UOL, weapon id, and bullet item id, then inserts it into the animation manager's bullet container. `RegisterMagicBulletAnimation` owns the parallel magic-ball path and only creates a bullet object when the resolved ball UOL is non-empty. `CAfterImageBullet::Update` is a deeper bullet subclass path: when afterimage is enabled it creates a child layer from the current bullet layer origin, relocates alpha/origin vectors, copies the source layer canvas into the afterimage layer, animates it, and registers repeat animation state rather than treating afterimage projectile trails as a generic skill effect. This means projectile parity is split one level below `SkillManager.SpawnProjectile`: gameplay/projectile collision can be correct while the client bullet animation owner for normal, magic, and afterimage projectile presentation is still missing.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Player skill execution parity

The simulator has a generic skill runtime. It does not yet mirror the client's named `CUserLocal` behavior families.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Generic skill casting | The simulator now loads the full player skill catalog from `Skill.wz`, learns active skills into the runtime, supports direct cast invocation from the skill window in addition to hotkey casting, keeps GM/SuperGM skill-book resolution compatible with both `900.img` and `910.img` layouts while focusing those jobs on their own job-book window instead of the global advancement catalog, evaluates Big Bang `common` formulas in IMG-mode data sets so skills like `Power Strike` are treated as castable attacks instead of passive placeholders, mirrors skill effect anchoring/hitbox fallback by facing direction for asymmetric melee effects, sanitizes unsupported SpriteFont glyphs in the skill window so WZ-backed names no longer crash row rendering, and resolves cast SFX from `Sound/Skill.img` so skill use now plays the corresponding `Use` or fallback attack sound through the simulator audio path | This closes the previous "can only use the current job's limited skill book" runtime/UI gap and keeps special-job skill tabs populated and executable across client data variants | `SkillManager.cs`, `SkillLoader.cs`, `SkillUIBigBang.cs`, `MapSimulator.cs`, `UIWindowLoader.cs` |
| Implemented | Buff stat application | Buff logic now applies `PAD`, `PDD`, `MAD`, `MDD`, `ACC`, `EVA`, `Speed`, and `Jump` directly to the player build and cleanly removes them when buffs expire or refresh | Active buff skills now change simulator stats and movement-relevant values instead of only a partial subset | `SkillManager.ApplyBuffStats` |
| Partial | Buff icon / temporary-stat UI parity | Active buffs still drive the WZ-backed status-bar tray with live duration countdowns sourced from `UI/BuffIcon.img`, the tray still supports client-style right-click cancellation directly from the live buff list, `UILoader` still normalizes and loads the full `BuffIcon.img` icon surface, and the simulator still consumes the authored `name` strings plus `buff/*` traversal order from that same catalog so the nine observed stat-family icons (`incEVA`, `incPAD`, `incPDD`, `incMAD`, `incMDD`, `incACC`, `incCraft`, `incJump`, `incSpeed`) keep their WZ-authored display names and ordering instead of a duplicated simulator-only table. The broader non-`BuffIcon.img` family catalog still resolves through the recovered v95 `SecondaryStat` layout, `CUIStatusBar::Draw` at `0x876cd0` still remains the client-owned status-bar owner seam, and this run stayed inside `SkillManager.GetStatusBarBuffEntries` / `ResolveStatusBarBuffTooltipPresentationForParity(...)` again without widening tray ownership outside that same seam. WZ was rechecked directly in the mounted export for the compound tooltip families that were still leaking generic placeholder order back into the visible stat list: `UI/BuffIcon.img/buff` still exposes only the same nine ordered authored icon families; `UI/BuffIcon.img/buff/incPDD/name` still resolves to `Physical Defense`; `Skill/410.img/skill/4101004/common` still publishes `speed` then `jump`; `Skill/3112.img/skill/31121001/common` still publishes `cr`, `ignoreMobpdpR`, then `bdR`; `Skill/572.img/skill/5720008/common` still publishes `criticaldamageMin`, `criticaldamageMax`, `cr`, and `bdR`; `Skill/3511.img/skill/35111013/common` still publishes `pddR`, `mhpR`, `mmpR`, `cr`, `damR`, and `expR`; the enhanced-max mixed books still publish the same authored order too: `Skill/3500.img/skill/35001002/common` still publishes `emhp`, `emmp`, `epad`, `epdd`, and `emdd`, `Skill/3512.img/skill/35120000/common` still publishes `mastery`, then `emhp`, `emmp`, `epad`, `epdd`, and `emdd`, and `Skill/522.img/skill/5221006/common` still publishes `epad`, `epdd`, `emdd`, `emhp`, and `emmp`; and the compound single-letter families this pass tightened still keep their authored order in mounted WZ too: `Skill/130.img/skill/1301007/common` still publishes `x`, then `y`, `Skill/312.img/skill/3121002/common` still publishes `x`, then `criticaldamageMax`, and `Skill/112.img/skill/1121000/common` still publishes `x`. That evidence now tightens the tooltip resolver one more step inside the same seam. The earlier direct-family and no-direct-family fixes remain in place: mixed direct-family books such as `Skill/410.img/skill/4101004/common` still stay on the authored `Speed` family and `buff/incSpeed` icon instead of sliding to the globally earlier `Jump` lane; mixed direct-family `pddR` books such as `Skill/3511.img/skill/35111013/common` still keep the visible `Buff Type` owner on the real `incPDD` icon and `Physical Defense` label; authored explicit non-direct owners still keep the visible family name even when a later direct icon surface wins the icon; and the no-direct-icon branch still prefers the earliest authored explicit property-family owner from the skill's `common` property order before falling back to generic helper rows such as `cr`, which keeps mixed non-direct surfaces such as `Skill/3112.img/skill/31121001/common`, `Skill/572.img/skill/5720008/common`, `Skill/000.img/skill/0000110/common`, `Skill/2311.img/skill/23111005/common`, and later `indie*` mixes such as `Skill/2411.img/skill/24111003/common` and `Skill/530.img/skill/5301003/common` on their first authored explicit family instead of collapsing back to generic helper rows or a later direct-stat alias. This pass closes another visible parity hole for compound non-`BuffIcon.img` families whose authored `common` order uses contextual single-letter slots rather than direct stat property names: `ResolveAuthoredTemporaryStatDisplayNames(...)` now interprets the already-recovered active family context for `Hyper Body`, `Sharp Eyes`, and `Maple Warrior`, so the tooltip stat list follows the authored `x`/`y` property order for those rows (`Max HP` then `Max MP`, `Critical Rate` then `Critical Damage (Max)`, and `All Stats`), and those family-owner helper labels no longer echo back into `TemporaryStatDisplayNames` once the same family is already shown separately in the visible `Buff Type` / family line. Focused regression coverage is now on disk in `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` for enhanced-max mixed-family display ownership, tooltip-family ownership, direct-icon mixes whose authored owner is a non-direct primary-stat family, and the newly tightened `Hyper Body`, `Sharp Eyes`, and `Maple Warrior` compound-tooltip ordering/suppression cases. The remaining gap is narrower but still real: exact tray order is now authored for the known direct `BuffIcon.img` families, for the broader supported explicit no-direct families that the recovered v95 `SecondaryStat` slice plus parsed `common` property order can already identify, and now for the checked compound `x`/`y` families whose active temp-stat owner is already known, but some non-`BuffIcon.img` families still have no stronger recovered v95 icon owner or still appear to depend on runtime-owned grouping beyond `UI/BuffIcon.img`, the recovered temp-stat surface, and currently parsed skill metadata. That remaining uncertainty still shows up most on leftover non-direct family mixes and on un-rechecked contextual placeholder families whose authored slot meaning is not yet as tight as the checked `Hyper Body`, `Sharp Eyes`, and `Maple Warrior` cases. | Buff-heavy jobs are visible on the HUD, can be cleared without waiting for expiry, and no longer collapse as often into a fully generic tray entry while tooltip text, tray-family ownership, icon fallback, and the visible `Buff Type` line stay closer to the authored WZ and recovered client temp-stat surface. The known nine `BuffIcon.img/buff/*` families still use the authored WZ icon names and order directly, direct-family rows still honor authored property order for tray-lane ownership, authored explicit non-direct owners still keep the visible family label even when a later direct icon family provides the icon surface, and the checked compound `x`/`y` families no longer leak helper-family names back into the visible stat list or lose their authored tooltip order. The remaining gap is now concentrated on the still-unrecovered client grouping that decides exact final lane or icon ownership for the leftover non-direct family mixes once WZ-authored family order, the recovered `SecondaryStat` slice, and the checked contextual placeholder mappings stop providing a unique owner. | `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`, `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` (`UI/BuffIcon.img/buff`, `UI/BuffIcon.img/buff/incPDD/name`, `Skill/410.img/skill/4101004/common`, `Skill/3112.img/skill/31121001/common`, `Skill/572.img/skill/5720008/common`, `Skill/3511.img/skill/35111013/common`, `Skill/3500.img/skill/35001002/common`, `Skill/3512.img/skill/35120000/common`, `Skill/522.img/skill/5221006/common`, `Skill/130.img/skill/1301007/common`, `Skill/312.img/skill/3121002/common`, `Skill/112.img/skill/1121000/common`, `Skill/000.img/skill/0000110/common`, `Skill/2311.img/skill/23111005/common`, `Skill/2411.img/skill/24111003/common`, `Skill/530.img/skill/5301003/common`, `CUIStatusBar::Draw@0x876cd0`) |
| Partial | Melee / ranged / magic resolution | The runtime keeps the existing player-skill parity work for this seam: WZ `lt/rb` attack bounds stay intact, melee and magic target ordering stay split, no-ball spell and shoot families still route through the client-shaped `info/type` classifier instead of collapsing into melee defaults, direct no-projectile ranged skills still use their own fallback hitbox plus the `DoActiveSkill_ShootAttack` melee-first-vs-shoot fallback split, projectile families still honor `rectBasedOnTarget`, `multiTargeting`, `range` chaining, `ballDelay` / `ballDelayN`, authored ball-frame sweep bounds, effective `mobCount` caps from both skill and ball data, WZ `x` damage overrides and client `SKILLENTRY::AdjustDamageDecRate` tables, and the shared shoot-entry / ammo resolver still mirrors the current client-backed weapon-family, support-state, consume-count, USE/CASH ownership, and pellet-family gates. This run kept the remaining mounted fallback work inside the same owner seam instead of widening projectile ownership again. WZ was rechecked first and still shows both the authored attack geometry and the mounted-frame signal this pass can safely lean on: `Skill/310.img/skill/3101005/info/rectBasedOnTarget = 1`, `Skill/230.img/skill/2301005/info/{multiTargeting,rectBasedOnTarget} = 1`, `Skill/230.img/skill/2301005/common/{lt,rb,range,mobCount}`, and the mounted jaguar / mechanic wildbeast-family frames on the exact body-rel-move vehicle seam still carry per-frame `map/navel` drift on the checked `character/TamingMob/01932015/*` and `character/TamingMob/01932030/*` actions, including confirmed `wildbeast` frame `0` vs `5` `map/navel` Y drift in the extracted data. Client evidence was rechecked second in `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::GetProperBulletPosition@0x907310`, `GetShootAttackPt0@0x9039a0`, and `is_position_up_skill_on_riding@0x9037c0`: when no authored shoot point exists, projectile launch Y still uses the client fallback `ptUser.y - 28` baseline plus the recovered skill nudges for `33101007`, `33121005`, `35001004`, `33101001`, `35101010`, and `35111015`; queued delayed projectiles still carry and correctly rebase their fallback-origin offsets at fire time; the owner resolver still keeps the same Mechanic `1932016` riding owner alive through avatar-transform-owned vehicle states instead of only current-action or equipped mount ownership; the live mounted-state seam still preserves the exact `GetShootAttackPt0` body-rel-move vehicle ids (`1932015`, `1932030`, `1932031`, `1932032`, `1932033`, `1932036`); and the disassembly confirms that `GetShootAttackPt0` really consumes a hidden `nBodyRelMoveY` input for only that exact vehicle table before falling back to the separate Mechanic `1932016 => -17` branch. `PlayerCharacter.TryGetCurrentBodyRelMoveY(...)` now mirrors that client split more closely in the existing render seam: when the live mounted state owns the current action, it still prefers the assembled mounted relocation's `clientBodyOrigin` value that `AvatarActionLayerCoordinator.ApplyMountedOriginRelocation(...)` already writes onto the live frame, so the body-rel-move input fed back into `GetShootAttackPt0` stays attached to the same mounted owner and current-action relocation the simulator is actually drawing. If that mounted assembled point is unavailable, the method now stays in the same seam and reconstructs the live mounted relocation directly from the current body frame plus mounted frame through `AvatarActionLayerCoordinator.ResolveMountedOriginRelocation(...)` before only then falling back to mounted-animation drift from authored `map/navel` and finally raw `CharacterFrame.Origin.Y`. That tighter mounted-owner value still flows through the existing `SkillManager.ResolveCurrentClientShootAttackBodyRelMoveY(...)` and `ResolveClientFallbackShootAttackPointYOffset(...)` path for both immediate and queued delayed projectile-origin refresh, so position-up shoot skills keep the client's exact body-rel-move vehicle gate while the simulator now prefers the same live body-plus-mount relocation the renderer is using even when the assembled map point is absent. Focused source-side coverage in `UnitTest_MapSimulator/MeleeRangedMagicResolutionParityTests.cs` now includes mounted assembled `clientBodyOrigin` extraction, raw body-plus-mounted relocation reconstruction, the missing-mounted-point raw-frame failure gate, and the raw frame origin-delta fallback when `navel` is absent. The row remains `Partial` because the remaining mismatch is now narrower and concentrated in deeper client-only fire-time presentation details that still sit below the current simulator seam: any residual cases where the client's live `m_ptBodyRelMove.y` still diverges from the simulator's current body-plus-mount relocation source, and later moving-shoot or projectile-hit presentation nudges applied after launch rather than exposed through WZ-authored map points, ball frames, ammo ownership, or the recovered fallback launch offsets. | This row matters because `CUserLocal` keeps separate melee, shoot, and magic execution lanes, and the remaining mismatch is now small enough that future work can stay focused on the last client-owned fire-time presentation details instead of reopening projectile family resolution, target ordering, ammo ownership, or basic fallback projectile origin placement again. | `SkillManager.ProcessMeleeAttack`, `SkillManager.ProcessMagicAttack`, `SkillManager.ProcessNoProjectileRangedAttack`, `SkillManager.SpawnProjectile`, `SkillManager.CheckProjectileCollisions`, `SkillManager.ResolveProjectileAttackOrigin`, `SkillManager.RefreshQueuedProjectileFireTimeOrigin`, `SkillManager.DrawProjectile`, `SkillManager.ResolveProjectileVisualItemId`, `SkillLoader.ParseSkillInfo`, `SkillLoader.CreateLevelData`, `ClientShootAttackFamilyResolver`, `ClientShootAmmoResolver`, `PlayerCharacter.TryGetCurrentBodyRelMoveY(...)`, `PlayerCharacter.TryResolveMountedClientBodyRelMoveY(...)`, `PlayerCharacter.TryResolveCurrentMountedClientBodyRelMoveYFromRawFrames(...)`, `PlayerCharacter.ResolveClientBodyRelMoveY(...)`, `AvatarActionLayerCoordinator.ApplyMountedOriginRelocation(...)`, `AvatarActionLayerCoordinator.ResolveMountedOriginRelocation(...)`, `CharacterAssembler.GetPartAnimation(...)`, `UnitTest_MapSimulator/MeleeRangedMagicResolutionParityTests.cs` (`Skill/310.img/skill/3101005/info/rectBasedOnTarget`, `Skill/230.img/skill/2301005/info/{multiTargeting,rectBasedOnTarget}`, `Skill/230.img/skill/2301005/common/{lt,rb,range,mobCount}`, `character/TamingMob/01932015/wildbeast/{0,5}/0/map/navel`, `character/TamingMob/01932015/wildbeast/{0,5}/1/map/navel`, `character/TamingMob/01932030/wildbeast/{0,5}/0/map/navel`, `character/TamingMob/01932030/wildbeast/{0,5}/1/map/navel`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::DoActiveSkill_MagicAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::GetProperBulletPosition@0x907310`, `GetShootAttackPt0@0x9039a0`, `is_position_up_skill_on_riding@0x9037c0`) |
| Partial | `DoActiveSkill_*` family parity | Generic movement, summon, and prepare/charge families now resolve through an explicit generic `DoActiveSkill` fallback lane in `SkillManager` instead of only by implication across separate seams. Movement-family dispatch still stays on the client-confirmed four-way split from the recovered local lanes: teleport snaps, grounded rush, flying-rush ownership, and bound-jump starts, with jump/hop launch skills folded into the same `TryDoingFlyingRush` family while preserving their launch-specific jump arc in the existing execution seam. Earlier landings still hold too: Battle Mage aura recasts no longer route through the cast-time cancel lane, distinct aura-family buffs stay stacked while same-skill recasts refresh their own slot, and WZ `info/mesoExplosion` skills still consume authored in-box drops directly before applying the attack payload instead of passing through the live local pickup-owner gate. This pass tightened the helper-only `affectedSkill*` ownership split inside the existing passive seam after a direct WZ re-check showed that not every `affectedSkillEffect` row is helper-only. The helper-only follow-up rows remain explicit and suppressed as standalone casts: `Skill/211.img/skill/2111007/info/{type=16,affectedSkill=2101002,affectedSkillEffect=bodyAttack&&stun}`, `Skill/3211.img/skill/32111010/info/{type=16,affectedSkill=32001002,affectedSkillEffect=bodyAttack&&stun}`, `Skill/3212.img/skill/32120000/info/{type=51,affectedSkill=32001003,affectedSkillEffect=dot,dotType=aura}`, and `Skill/3211.img/skill/32110000/info/{type=50,affectedSkill=32101002,affectedSkillEffect=partyDamageSharing}` still only publish owner follow-up metadata and no standalone direct-cast surface. WZ also showed direct-cast counterexamples that the old blanket suppression rule was wrongly collapsing into that same passive lane: `Skill/522.img/skill/5220011` still publishes `info/{type=50,affectedSkill=5211006,affectedSkillEffect=amplifyDamage}` together with authored `action`, `ball`, `hit`, and `common`, and `Skill/3510.img/skill/35101009` still publishes `info/{type=1,areaAttack=1,rapidAttack=1,affectedSkill=35001001,affectedSkillEffect=dot,dotType=burn}` together with authored `prepare`, `keydown`, `keydownend`, `hit`, and `common`. `SkillData` and `SkillLoader` now only suppress helper-only `affectedSkill*` rows that lack a standalone cast surface, while the affected-skill passive runtime helpers in `SkillManager` now use that same narrowed gate instead of treating every `affectedSkillEffect` row as passive-support data. The same row still rechecks that WZ `zoneType=invincible` is limited here to `Skill/422.img/skill/4221006/info/zoneType` and `Skill/3212.img/skill/32121006/info/zoneType`, preserves `4221006`'s authored `tile` animation while letting `32121006` use its WZ-authored `special` branch when no `tile` branch exists, and keeps the existing GM/SuperGM validation seam as the owner for both active casts and quick-slot assignment. Focused regression coverage now lives in `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs`, covering the explicit generic-cast lane, helper-only passive suppression, direct-surface `affectedSkillEffect` cast admission, the jump-rush-to-flying-rush family fold, the invincible-zone tile-vs-special fallback, helper-only passive-runtime rejection for `5220011`, and the GM/SuperGM quick-slot ownership rule. | The remaining gap is now narrower and concentrated in the last client-owned movement timing, packet choreography, and area/summon ownership rather than the generic cast-family split, helper-only passive suppression, direct-surface `affectedSkillEffect` admission, invincible-zone tile-vs-special selection, or the admin ownership gate. The simulator still simplifies the exact moving-shoot entry snapshot and request choreography inside `TryDoingSmoothingMovingShootAttack`, especially the still-unrecovered full client `m_nAttackActionType` table values and the exact per-row contents of the shared `get_random_shoot_attack_action(...)` table beyond the now-modeled live family split for shoot, one-handed stab, two-handed stab, one-handed swing, two-handed swing, and polearm swing plus the earlier `info/type == 3` appointed-action bypass. It also still simplifies the live temporary-stat inputs that feed `get_attack_speed_degree(...)` beyond the now-modeled clamp, `4001334` base offset, skill-specific modifier-bypass set, shared Booster delta, and the WZ-backed `rapidAttack => CountLimit 1` fallback, plus any additional `CAntiRepeat` initialization or bypass rules beyond the now-modeled position-threshold, level-match, rapid-attack count-limit, and `33121009` exception gates; the final highest-jump timing window beyond the current WZ-backed near-apex gate; any remaining teleport timing / snap choreography around the client-owned delayed `m_teleport` commit window beyond the now-modeled follow-up mastery trigger; any remaining `zoneType=invincible` client details beyond the current persistent local protection zone, WZ-authored footprint/duration, and tile-or-special visual branch selection; any remaining Rocket Booster packet choreography beyond the now-modeled launch-marker, launch-speed formula, and end-loop timing; deeper packet-owned summon assist dispatch and per-family `OnSkill` / `OnAttack` ownership beyond the now-modeled body-contact / passive-movement / expiry cadence; and the remaining local-vs-party area-footprint choreography for helper-only support rows beyond the now-modeled local owner-follow-up buff projection and linked-owner support merge. | `SkillManager`, `SkillLoader`, `SkillData`, `PlayerCharacter`, `PlayerCombat`, `PlayerSkillStateRestrictionEvaluator`, `SummonedPool`, `PacketOwnedSummonUpdateRules`, `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` (`CUserLocal::DoActiveSkill_*`, `CUserLocal::DoActiveSkill_BoundJump`, `CUserLocal::TryDoingTeleport`, `CUserLocal::TryDoingRush`, `CUserLocal::TryDoingFlyingRush`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CSummoned::Update`, `CAntiRepeat::TryRepeat`, `get_random_shoot_attack_action`, `get_attack_speed_degree`, `Skill/*/info/type`, `Skill/*/info/affectedSkillEffect`, `Skill/*/info/avaliableInJumpingState`, `Skill/*/info/requireHighestJump`, `Skill/110.img/skill/1101004/common/x`, `Skill/510.img/skill/5101006/common/x`, `Skill/211.img/skill/2111007/info/{type,affectedSkill,affectedSkillEffect}`, `Skill/3211.img/skill/32110000/info/{type,affectedSkill,affectedSkillEffect}`, `Skill/3211.img/skill/32111010/info/{type,affectedSkill,affectedSkillEffect}`, `Skill/3212.img/skill/32120000/info/{type,affectedSkill,affectedSkillEffect,dotType}`, `Skill/3212.img/skill/32121003/info`, `Skill/3212.img/skill/32121006/{info/zoneType,special}`, `Skill/3300.img/skill/33001000/info`, `Skill/3310.img/skill/33101001/info`, `Skill/3311.img/skill/33111001/info`, `Skill/3312.img/skill/33121005/info`, `Skill/3312.img/skill/33121009/info`, `Skill/3500.img/skill/35001001/info/rapidAttack`, `Skill/3510.img/skill/35101009/{info/rapidAttack,prepare,keydown,keydownend}`, `Skill/422.img/skill/4221006/{info/zoneType,tile}`, `Skill/421.img/skill/4211006/info/mesoExplosion`, `Skill/522.img/skill/5220011/{info/affectedSkillEffect,action,ball,hit,common}`, `Skill/*/info/selfDestructMinion`, `Skill/900.img/skill/*`) |
| Partial | Shadow Partner render/action parity | The simulator still treats Shadow Partner as a dedicated helper render/action surface rather than a generic buff ornament, with WZ-loaded `special/*` action tables for `4111002`, `4211008`, and `14111000`, client-shaped helper resolution order (`LoadShadowPartnerAction` helper-row lookup before plain raw-action name), raw-action-aware alias fallback, delayed attack follow-up keyed from the helper first-frame delay, blocking `create*` plus attack completion hold semantics, client-lane displacement/interpolation (`+/-30`, `+50`, `200 ms`), per-frame `a0/a1` alpha interpolation, and under-face insertion-layer ownership. WZ was rechecked first in the mounted export for the authored helper surface: `Skill/411.img/skill/4111002/special/*` and `Skill/421.img/skill/4211008/special/*` still publish the generic helper families plus `create2/create3/create4`, while `Skill/1411.img/skill/14111000/special/*` still publishes the same generic helper families without `create*`. The same WZ pass rechecked representative family raw rows such as `Skill/411.img/skill/4111005/action/0 = avenger`, `Skill/421.img/skill/4211002/action/0 = assaulter`, `Skill/421.img/skill/4211006/action/0 = prone2`, and `Skill/1411.img/skill/14111002/action/0 = avenger`, then rechecked the mounted `Character/00002000.img` helper rows for `alert2`, `alert3`, `alert4`, `alert5`, `ladder2`, `rope2`, `smokeshell`, and `prone2`: those character rows still expose exact source helper action, source frame index, signed per-piece delay metadata, and the per-piece `flip` flag instead of a single remapped frame. Client evidence stayed second: `CActionMan::LoadShadowPartnerAction` still resolves the action-specific helper table before plain `get_action_name_from_code`, still consumes frame-owned delay data when it builds the helper row, and `CActionMan::Init` still populates that table from character action metadata with source helper action plus numeric source-frame index. This pass keeps the work inside the existing loader/runtime seams and narrows the remaining gap again: `SkillLoader.TryParseShadowPartnerClientActionPieces(...)` now preserves the mounted signed helper delays and piece flip flags from `Character/00002000.img` instead of normalizing them away, while `ShadowPartnerClientActionResolver`, `PlayerCharacter`, and `RemoteUserActorPool` now consume those signed rows with absolute frame timing for helper playback, completion holds, first-frame follow-up delay, and per-frame alpha interpolation. The earlier evidenced `prone2` row therefore stays pinned to source frame `0` from the mounted character row while still keeping the client-shaped `700/300 ms` timing cadence, and the recovered `alert3` / `alert4` / `alert5` plus `smokeshell` rows now keep their signed mounted delays on the loader-owned helper frames without breaking local or remote runtime playback. The earlier family gate remains in place so `shotC1` still only follows families that publish `avenger`, `stabD1` still only follows the shadower family that publishes `assaulter`, and `swingD1` / `swingD2` / `doubleSwing` / `tripleSwing` still only follow the Night Walker swing family that publishes `swingO1`. The earlier raw-backed back-lane, shared horizontal-origin cadence, same-branch identity tightening, numeric helper-frame ordering, `4331002` / raw-`47` render suppression, and local/remote helper-family identity seams stay in place. Focused regression coverage now lives on disk in `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs` for signed mounted delay parsing, signed first-frame delay follow-up resolution, signed-row playback timing, and the pre-existing exact source-frame-slot synthesis this seam depends on. Remaining gap is narrower but still open: client-only `LoadShadowPartnerAction` non-ghost helper rows beyond the WZ exact rows and the now-recovered `Character/00002000.img` subset remain unrecovered, exact source-frame indexes for any later-discovered pieced row outside that subset remain unknown, exact ordering against every other simulator-owned non-mirror under-face overlay that the client may still own as a literal action-layer canvas object remains approximate, and residual packet-only one-shot alpha or follow-up choreography that depends on hidden client layer state rather than the helper animation catalog is still not one-for-one. | Shadow Partner still mirrors companion playback through a dedicated skill-owned action catalog with client-shaped helper ordering and raw-action fallback, but the previously broad hidden source-frame-index gap is now narrower for the client-only aliases whose `Character/00002000.img` piece rows are visible in the mounted export. The simulator keeps the WZ-authored `special/*` helper catalog per Shadow Partner family, keeps remote/local family gates from leaking unsupported raw aliases back through generic `shoot*`, `stab*`, or `swing*` fallback, and now preserves the signed mounted helper-delay payloads that the client loader consumes while still timing those rows with the client-shaped absolute delay cadence in both the local and remote helper runtimes. The row stays `Partial` because the unrecovered part is now concentrated on any hidden helper-table rows not exposed by the WZ/raw-action subset, deeper literal layer-object ordering, and packet-only presentation state outside the helper animation catalog. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `ShadowPartnerClientActionResolver.cs`, `RemoteUserActorPool.cs`, `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs`, `CUser::PrepareShadowPartnerActionLayer@0x8e1730`, `CActionMan::Init@0x41beb0`, `CActionMan::LoadShadowPartnerAction@0x423cf0`, `is_back_action@0x45f1b0`, `get_action_name_from_code`, `Skill/*/{4111002,4211008,14111000}/special/*`, `Skill/411.img/skill/4111005/action/0`, `Skill/421.img/skill/{4211002,4211006}/action/0`, `Skill/1411.img/skill/14111002/action/0`, `Character/00002000.img/{alert2,alert3,alert4,alert5,ladder2,rope2,smokeshell,prone2,avenger,assaulter,flyingAssaulter,savage,showdown,assassination,ninjastorm,vampire,stabD1,swingD1,swingD2,doubleSwing,tripleSwing,shotC1}`) |
| Partial | Queued follow-up attack families | Attack skills that publish WZ `finalAttack` follow-up maps still queue a deferred extra strike after the opener resolves, gate that queued branch by weapon type on both queue and execute, reuse the learned follow-up skill's live proc rate, preserve queued facing plus preferred-target mob or position snapshots through direct follow-up resolution and any later deferred moving-shoot or projectile hop, and keep the client's separate single pending-slot overwrite rules for final attack, Spark, and serial follow-up families without clearing unrelated queued summon work. Thunder Breaker `Spark` (`15111006`) still runs as its own timed attack-triggered family from WZ `info/condition=attack` plus `info/chainAttack=1` and `info/chainattackPenalty=1`, snapshots the current weapon code at queue time, rechecks that same code again when the deferred branch fires, and still re-enters the shared shoot/projectile path with the saved trigger origin and target lane. The deferred Spark handoff now also keeps that Spark-owned family marker when queued execution passes back through the shared payload path, so later `ballDelay` release, queued-facing presentation, and same-slot overwrite cleanup continue to treat the delayed shot as Spark instead of silently degrading to a generic queued projectile. WZ `info/chainAttack=1` plus `info/chainattackPenalty=1` serial families such as `2221006` and `5121002` still use their own single pending serial slot, queued final-attack, Spark, and serial execution still re-read the live learned skill level when the deferred branch actually fires, still route queued cast startup back through the normal cast-effect seam, and still carry the client-only ranged final-attack `TryDoingShootAttack(..., 65, ...)` search hint through queued entry plus any extra deferred moving-shoot hop. The queued shoot-entry seam remains tighter on both validation and presentation: queued final-attack and Spark casts still re-qualify their armed USE or CASH ammo selection against the live inventory right before queued fire time, still refresh slot indices while preserving the original item ids even when a queued no-consume buff snapshot is active, still cancel the queued ranged branch if the armed USE ammo disappeared or no longer satisfies the live consume count, and still consume those snapshotted USE or CASH item ids during queued projectile draw so delayed final-attack or Spark releases render from the matching WZ `Item/*/*/bullet` frames instead of collapsing back to generic skill projectile art. WZ still exposes the authored item-bullet visual seam directly (`Item/Consume/0206/02060000/bullet/0` for standard arrows and `Item/Cash/0502/05021000/bullet/{0,1,2}` for animated cash bullets), while client evidence from `CUserLocal::GetProperBulletPosition` still shows fire-time resolution returning both USE and CASH projectile ownership. Client re-check of `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare` at `0x912f90` still also shows the delayed moving-shoot entry keeping the queued action plus `nBulletItemPos` / `nBulletCashItemPos` metadata separately from later fire-time resolution. The simulator therefore keeps queued projectile visuals on the queued cast's already-resolved weapon family during later `ballDelay` refresh, prefers the cash-bullet family only while that cash slot still resolves at fire time after the queued slot refresh, otherwise falls back to the preserved USE family, now prefers those preserved client one-based USE or CASH slot positions first when duplicate matching stacks still exist at fire time, and keeps that same stale-cash fallback aligned across both delayed projectile draw and the bullet-animation owner registration path instead of letting the fire-time presentation helper silently drift back to preserved CASH art after the refreshed queued selection had already fallen back to USE. It still mirrors the live `handMove` / `hand` / body-origin shoot point across the avatar pivot when delayed queued fire finally resolves under a saved facing that no longer matches the avatar's current direction, now keeps that queued-facing snapshot on the projectile itself so later target re-aim no longer mutates the mirrored delayed launch side, and still preserves any already-armed owner-relative queued projectile launch offset when that late fire-time refresh re-anchors the shot onto the mirrored live shoot point instead of flattening the delayed projectile back onto the raw owner origin. WZ-authored `x` chain-penalty decay and the client-owned fixed multiplier tables for no-`x` families still both apply through the deferred or timed chain path as well, so queued follow-up families no longer flatten back onto one generic damage percent after the opener. Focused helper coverage is now on disk in `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` for queued Spark family-marker preservation through the deferred payload handoff, queued-facing launch preservation during delayed fire-time origin refresh, late queued ammo revalidation and slot refresh, duplicate-stack fire-time slot refresh preferring the preserved queued USE/CASH client slot positions before falling back to another matching live stack, stale cash-lane visual fallback back to the preserved USE family, the delayed bullet-animation owner using that same fallback after fire-time slot refresh, mirrored delayed-origin refresh, queued delayed-origin offset preservation, queued bullet-family visual override, queued weapon-context preservation, the client one-based moving-shoot USE/CASH bullet-position metadata retained separately from the simulator zero-based runtime slot refresh, and the authored-`x` plus client-table serial damage-decay seams that still feed queued chain follow-ups. The remaining gap is now concentrated in the last client-only `TryDoingShootAttack` fire-time presentation details that still sit below the current simulator seam, especially the exact cosmetic projectile placement the client derives after those preserved moving-shoot bullet item ids and one-based slot positions have been carried into the delayed fire step. | Final-attack style procs still no longer collapse into the initiating cast timing, queued strikes still cancel if the player changes to a non-matching weapon before the deferred attack fires, if a newer follow-up proc replaces the pending one before it can execute, or if the follow-up skill has been locally unlearned before the queued branch actually fires, and replacing a pending final attack still does not wipe unrelated queued summon damage. Queued Spark now follows that same queue-time and execute-time weapon snapshot rule instead of surviving a weapon swap that the client would reject, and deferred queued Spark shots now also keep their Spark-owned projectile marker through the deferred payload handoff so later delayed fire-time refresh, queued-facing presentation, and same-slot overwrite logic still stay on the Spark family instead of degrading to generic queued projectile handling. Once a queued final attack or queued Spark has already progressed into a delayed projectile release, a newer proc in that same client-owned slot still clears the older still-pending projectile instead of letting it survive past the queue replacement point, while unrelated projectile work remains intact. Deferred moving-shoot payloads also stop dropping out when a due queued follow-up family should run first because the payload stays armed until after that queued final-attack, Spark, or serial branch resolves instead of being dequeued and lost. Queued follow-up casts that pass through the simulator's extra deferred moving-shoot payload path still keep the stored facing or origin snapshot, preserve the ranged final-attack shoot-range override (`65`) through both deferred target acquisition and the actual delayed fire step, revalidate the live learned level again at the actual deferred fire time, keep the queued target-position snapshot alive across both direct execution and deferred projectile targeting so a vanished opener mob does not force those branches straight back onto generic live ordering, and still revalidate the live fire-time ammo count plus skill-specific ammo family instead of only trusting the older queue-entry ammo snapshot. The queued ranged shoot-entry seam is now closer on both execution and presentation because queued final-attack and Spark casts recheck their armed USE or CASH ammo metadata at queued fire time even when the queued branch was armed under a no-consume buff snapshot, keep the original ammo item ownership instead of inheriting unrelated later casts, prefer the preserved client one-based queued USE/CASH slot positions when duplicate matching stacks still survive to fire time before falling back to another valid live stack, drop the queued ranged branch when the originally armed ammo family disappeared or no longer meets the live consume count, draw delayed queued projectiles from the matching snapshotted bullet-family frames rather than always falling back to the generic skill projectile art, stop preferring a stale cash-bullet family once the fire-time slot refresh has already shown that the queued cash lane disappeared while the armed USE lane still remains valid, and now keep the delayed bullet-animation owner on that same USE-family fallback instead of letting the fire-time presentation helper drift onto the stale preserved cash item id. The simulator still keeps that delayed projectile refresh on the queued cast's already-resolved weapon family even if the player changes to a different ranged weapon only after the queued shot has already fired, still keeps the delayed queued projectile anchored to a mirrored live shoot point when the avatar turns around after the shot was armed but before the preserved queued-facing projectile actually releases, now keeps that mirrored refresh tied to the queued cast-facing snapshot even after the projectile has already re-aimed toward a target lane, and keeps queued serial follow-up damage on the authored/client decay tables instead of flattening later chain hits back onto one generic percent. The remaining mismatch is now mostly in deeper client-owned fire-time presentation bookkeeping such as the exact cosmetic bullet-position placement that still hangs off the client's delayed moving-shoot entry after `GetProperBulletPosition` resolves USE or CASH ownership. The current simulator seam already preserves the queued projectile item snapshot, queued `m_movingShootEntry` USE/CASH slot-position metadata in the client's one-based inventory-position form, queued fire-time ammo-family and consume-count revalidation, duplicate-stack preference for that preserved queued slot metadata before live fallback, mirrored delayed fire-time shoot origin refresh, delayed owner-relative offset, refreshed fire-time zero-based runtime slot indices, fire-time cash-slot visual fallback, the queued cast-facing snapshot through delayed re-aim, the queued serial damage-decay surface, and the shared queued weapon gate now covered by `Spark` too. | `SkillManager`, `SkillData`, `SkillLoader`, `ClientShootAmmoResolver`, `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` (`Skill/1511.img/skill/15111006/info/{condition,chainAttack,chainattackPenalty}`, `Skill/222.img/skill/2221006/info/{chainAttack,chainattackPenalty}`, `Skill/512.img/skill/5121002/info/{chainAttack,chainattackPenalty}`, `Item/Consume/0206/02060000/bullet/0`, `Item/Cash/0502/05021000/bullet/{0,1,2}`, `CUserLocal::GetProperBulletPosition`, `CUserLocal::TryDoingFinalAttack`, `CUserLocal::TryDoingSerialAttack`, `CUserLocal::TryDoingSparkAttack`, `CUserLocal::TryDoingShootAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare`) |
| Partial | Repeat-skill sustain families | `CUserLocal::TryDoingRepeatSkill` still owns more of the Mechanic repeat-state machine than the simulator, so this run stayed inside the existing repeat-sustain, player-manager, summon official-session, and local-utility transport seams rather than adding another local-only owner. WZ was rechecked first in the mounted export: `Skill/3512.img/skill/35121003` still publishes the SG-88 summon / manual-attack surface (`info/type = 33`, `info/minionAttack = normal`, `summon/attack1`) and `Skill/3512.img/skill/35121013` still publishes the tank-siege lane (`info/fixedState = 1`, `info/onlyNormalAttack = 1`, `effect/*/delay = 30/60/30/60`), so no authored timing or repeat-branch data changed. Client recheck in IDA still anchors the same owners and constants: `CUserLocal::TryDoingRepeatSkill` still creates the SG-88 keydown bar after `810 ms`, arms manual assist after `2790 ms`, removes the manual summon after `2520 ms` once assist is done, sends the tank-siege mode-end skill effect request through the same repeat owner once `m_tLastTankSiegeMode` reaches `5000 ms` as opcode `71` with payload order `Int32 skillId`, `Byte skillLevel`, `Byte sendLocal`, sets `m_bSendTankSiegeModeEnd`, and only rewrites the repeat skill back to tank mode after `m_nMechanicMode == 35121005`; `CUserLocal::SendSkillEffectRequest` still confirms that opcode `71` really is just that `Encode4/Encode1/Encode1` payload order; and `CSummoned::TryDoingAttackManual` still owns the SG-88 outbound request choreography via `CMobPool::CheckMobInTrapezoid`, `CMob::GetHitPoint`, and the `120 ms` same-request clustered follow-up delay. No additional client recovery in this run exposed the unknown SG-88 first-use opcode or the real tank-siege confirmation opcode/order, so behavior stayed inside the already-landed transport seam rather than guessing at hidden packet fields. The earlier tank-siege request transport remains in place: mode-end acknowledgement is still request-time keyed through `SkillManager` / `PlayerManager`, pending mode-end still materializes the client-shaped opcode-`71` request through `PacketOwnedMechanicRepeatSkillRuntime`, `SkillManager` still builds that request as `35120013` with the live learned level of `35121013`, `PlayerManager` still hands the request to the outer transport seam, and `MapSimulator.PacketOwnedUtilityParity` still routes it through the established live-bridge / outbox / deferred authority planner instead of leaving tank-siege mode-end as a local-only pending ack. The SG-88 / summon-bridge replay promotion seam from the earlier landing also remains intact: simulator-originated replay traces can still attach as candidate request captures, but they only become reusable learned templates after the same official confirmation lanes already tracked by the row (`1021` for SG-88 or `0x119` target coverage for Tesla) upgrade the capture resolution. This run lands the focused on-disk coverage in `UnitTest_MapSimulator/RepeatSkillSustainPacketTransportParityTests.cs`, covering the opcode-`71` payload order, replay-trace non-promotion before confirmation, SG-88 replay promotion after `1021`, Tesla replay promotion after `0x119` target coverage, and the exact-lane / newest-same-count template-selection behavior described by this bridge seam. The row remains partial because tank-siege mode-end still lacks the true official client/server confirmation opcode and exact request/ack dispatch ordering behind `m_bSendTankSiegeModeEnd`, and SG-88 still does not know the exact official outbound opcode plus full field order a priori; simulator replay can now become confirmed reusable evidence only after an official lane validates it, but first-time SG-88 automatic request origination still depends on learning an initial template from captured official traffic and the simulator still cannot prove every non-target field one-for-one without further client or packet evidence. | Mechanic repeat skills still keep the same WZ-authored repeat/summon timing surface and the same client-confirmed SG-88/tank-siege ownership model, while the simulator continues to emit the evidenced tank-siege mode-end request through the existing local-utility authority seam instead of only remembering that a request should exist. SG-88 bridge replay and archive-strength behavior still no longer stop at request replay: once live official confirmation arrives on the already-tracked `1021` / `0x119` lanes, those replay traces can graduate into reusable exact-lane templates for future requests, while tank-siege mode-end still uses the client-confirmed opcode `71` payload shape (`Int32 skillId`, `Byte skillLevel`, `Byte sendLocal`) with the correct `35120013` effect skill id and live repeat-skill level before later ack fallback logic runs. The newly-landed part in this run is the row's focused regression coverage rather than another speculative transport heuristic, so the remaining approximation stays concentrated in unknown confirmation semantics and the initial unknown outbound packet shape: tank-siege still needs the real ack opcode plus exact dispatch/ack ordering, and SG-88 still needs a fully client-proven first-use outbound opcode/field map instead of captured-template replay. | `SkillManager.cs`, `PlayerManager.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedSummonedParity.cs`, `Managers/PacketOwnedMechanicRepeatSkillRuntime.cs`, `Managers/SummonedOfficialSessionBridgeManager.cs`, `Companions/MechanicAuthorityTransportPlanner.cs`, `UnitTest_MapSimulator/RepeatSkillSustainPacketTransportParityTests.cs` (`CUserLocal::TryDoingRepeatSkill`, `CUserLocal::SendSkillEffectRequest`, `CUserLocal::TryDoingSiege`, `CUserLocal::UpdateClientTimer`, `CSummonedPool::OnPacket`, `CSummonedPool::OnAttack`, `CUser::OnSummonedAttack`, `CSummoned::TryDoingAttackManual`, `CMobPool::CheckMobInTrapezoid`, `CMob::GetHitPoint`, `Skill/3512.img/skill/35121003/{info,summon}`, `Skill/3512.img/skill/35121013/{info,effect}`) |
| Partial | Bound-jump, rocket-booster end, and smoothing moving-shoot branches | This pass stayed inside the existing moving-shoot / Rocket Booster / bound-jump seams again and tightened the remaining bound-jump admission inference without widening unsupported row ownership elsewhere. WZ was rechecked first in the mounted export at `E:\MapleStory\WzExport\v95_2`: the earlier moving-shoot and Rocket Booster rows still hold for `Skill/3500.img/skill/35001001/action/0 = flamethrower`, `Skill/3510.img/skill/35101009/action/0 = flamethrower2`, the `rapidAttack = 1` rows for `33121009`, `35001001`, and `35101009`, `Skill/3510.img/skill/35101004/{action/0 = rbooster_pre, info/type = 10, info/casterMove = 1, info/landingEffect = areaAttack}`, and the ranged weapon metadata rows `Character/Weapon/01452005/info/attack = 3` with published `shoot1` / `shootF`, `Character/Weapon/01462000/info/attack = 4` with published `shoot2`, and `Character/Weapon/01492000/info/attack = 9` with published `shoot1` / `shoot2` / `shootF`. The same WZ pass also rechecked the bound-jump surface that constrains this seam: `Skill/3300.img/skill/33001002/{action/0 = doubleJump, info/type = 40}`, `Skill/1310.img/skill/13101004/{action/0 = archerDoubleJump, info/type = 40, info/casterMove = 1, info/avaliableInJumpingState = 1}`, and `Skill/1410.img/skill/14101004/{info/type = 40, info/casterMove = 1, info/avaliableInJumpingState = 1}` still match the earlier notes, with `14101004` still lacking an authored `action/0`. Client evidence stayed second: `CUserLocal::DoActiveSkill_BoundJump@0x93fdf0` still lists only the directly recovered skill-id branches before the shared jump rows, keeping `14101004` on the shared flash-jump `nSLV / 4` row and `33001002` on the doubled-`nSLV` branch before the shared quarter-step; `get_random_shoot_attack_action@0x70b7a0` still keys off avatar `m_nAttackActionType` plus the type-`3` vs non-type-`3` split and rejects unsupported rows before consulting `aShootAttackAction`; and `CUserLocal::TryDoingRocketBoosterEnd@0x93d2d0` still gates both stored-vertical-marker clearing and the foothold touchdown attack behind `CAvatar::GetOneTimeAction() <= -1` before the local landing attack / `500 ms` recovery-effect clear. `SkillManager` keeps the earlier type-`3` unresolved-owner abort, appointed-action precedence, one-time-action type-`3` prepare exception, queued skill-level revalidation, explicit `33121009` anti-repeat bypass, rapid-attack limit, ranged `info/attack` row preservation for `3` / `4` / `9`, published-surface narrowing, direct `14101004` explicit bound-jump treatment, the recovered `33001002` / `14101004` impact rows, and the earlier Rocket Booster touchdown wait for the startup window plus any active client-owned one-time action to clear. This pass closes the next bound-jump overreach in the same local seams: generic `info/type = 40` plus `casterMove` / jumping-state metadata no longer promotes a skill into bound-jump movement or the airborne/grounded state gate by itself, while the WZ-authored `archerDoubleJump` profile and the direct client-owned skills still stay on the bound-jump route. That keeps unrecovered type-`40` rows with no authored bound-jump action surface from silently inheriting the simulator’s generic bound-jump movement arc just because they share coarse metadata with the confirmed rows. Focused source-side coverage under `UnitTest_MapSimulator/MovingShootParityTests.cs` now pins the exact ranged `shoot1` / `shoot2` rows, the closed melee-row fallback, the recovered `33001002` / `14101004` impact rows, the Rocket Booster touchdown gate, the preserved WZ-backed `archerDoubleJump` route, and the closed generic type-`40` bound-jump promotion seam. The row remains `Partial`: the remaining gap is now concentrated in the still-hidden exact `get_random_shoot_attack_action(...)` row contents and weighting beyond this WZ-backed ranged-row preservation and published-surface narrowing, especially the unrecovered melee attack-type rows, fuller `CAntiRepeat` construction and count-limit initialization details beyond the current threshold reset / `33121009` bypass / rapid-attack limit, any packet- or server-owned Rocket Booster end choreography outside the simulator local seam, and any additional bound-jump row mapping beyond the directly recovered client skill ids plus WZ-authored action-backed profiles. | Movement-coupled attack skills keep the client-owned moving-shoot queue metadata, WZ-authored appointed action preference, one-time-action type-`3` exception, anti-repeat owner surface, Rocket Booster local landing seam, the recovered direct bound-jump rows, and the exact recovered ranged avatar attack-action owners (`3`, `4`, `9`) instead of collapsing them to one generic shoot type before delayed execution. `14101004` stays explicit on both the velocity and state-gate seams, `33001002` stays on the client-owned half-level growth table, crossbow moving-shoot fallback still stays on `shoot2`, hidden melee rows now stay closed even when current-action or weapon-family fallback could have guessed a swing/stab family, generic type-`40` rows without an authored bound-jump profile no longer inherit bound-jump execution just from coarse metadata, and Rocket Booster touchdown still waits for the client-owned one-time-action lane to clear before the stored launch marker advances. The remaining mismatch is narrower and concentrated in deeper hidden-table recovery for moving-shoot random action selection, especially exact melee rows and table weighting, fuller anti-repeat construction details, packet-owned Rocket Booster end choreography, and still-unrecovered bound-jump special rows beyond the direct client ids and WZ-authored action-backed cases now kept here. | `SkillManager.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `UnitTest_MapSimulator/MovingShootParityTests.cs` (`CUserLocal::DoActiveSkill_BoundJump@0x93fdf0`, `CUserLocal::TryDoingRocketBoosterEnd@0x93d2d0`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare@0x912f90`, `CUserLocal::TryDoingSmoothingMovingShootAttack@0x92de70`, `get_random_shoot_attack_action@0x70b7a0`, `CAntiRepeat::TryRepeat@0x9032b0`, `Character/Weapon/01452005`, `Character/Weapon/01462000`, `Character/Weapon/01492000`, `Skill/3300.img/skill/33001002/{action/0,info/type}`, `Skill/3312.img/skill/33121009/info/rapidAttack`, `Skill/3500.img/skill/35001001/action/0`, `Skill/3500.img/skill/35001001/info/rapidAttack`, `Skill/3510.img/skill/35101004/{action/0,info}`, `Skill/3510.img/skill/35101009/action/0`, `Skill/3510.img/skill/35101009/info/rapidAttack`, `Skill/1110.img/skill/11101005/info/type`, `Skill/1310.img/skill/13101004/{action/0,info/type,info/casterMove,info/avaliableInJumpingState}`, `Skill/1410.img/skill/14101004/info`, `String/Skill.img/14101004/name`) |
| Partial | Key-down / charge skills | The prepared-skill startup/release seams for this row still stay confined to the existing `SkillManager`, `PreparedSkillHudRules`, `MapSimulator.RemoteUsers`, and `RemoteUserActorPool` owners, and this pass strengthened the checked-in regression coverage for those same seams without widening ownership. WZ was rechecked first in the mounted export: `Skill/322.img/skill/3221001/{prepare,keydown,action,info}` still publishes a real authored startup window with `prepare/time = 1200`, `action/0 = piercing`, and a separate client gauge cap of `900 ms` through `get_max_gauge_time`; `Skill/531.img/skill/5311002/{prepare,keydown,action}` still publishes `prepare/time = 1080`, `prepare/action = noiseWave_pre`, `keydown/action = noiseWave_ing`, and `action/0 = noiseWave`; while the Big Bang trio under `Skill/{212,222,232}.img/skill/*`, Evan breath under `Skill/2212.img/skill/22121000` and `Skill/2215.img/skill/22151001`, and the Dual Blade charge pair under `Skill/434.img/skill/{4341002,4341003}` still keep `info/chargingSkill = 1` with authored prepare actions but `prepare/time = 0`, so those families still need to arm the generic hold loop immediately rather than waiting out a simulator-owned pseudo-startup. Client evidence stayed second and unchanged: `CUserLocal::OnKeyDownSkillEnd` (`0x9301b0`) still clamps elapsed charge time to a shared `30 ms` minimum, caps it through `get_max_gauge_time`, routes release through the magic / shoot / melee / HP-cost branches, and keeps the Final Cut branch scaling HP cost as `elapsed * nX / get_max_gauge_time` before comparing that percentage cost against current HP. The runtime seams already matched that behavior before this pass and still do now: `SkillManager.ResolvePreparedSkillStartupDuration` uses the authored WZ prepare window only when a release-owned family really has one, `BeginPreparedSkill` still enters hold immediately for zero-startup keydown families, `PreparedSkillHudRules.ResolveReleaseChargeElapsedMs` and the release-damage / HP-cost helpers still prefer the shared HUD or `get_max_gauge_time` charge window over a longer authored prepare split, `PreparedSkillHudRules.ResolveRemotePreparedSkillPhases` still auto-arms release-owned zero-startup keydown families onto hold whenever the packet lane carries a real hold window, and `RemoteUserActorPool.TryResolvePreparedSkillPhase` still preserves that zero-startup auto-hold state instead of falling back to a non-holding armed phase. Focused coverage is now on disk in `UnitTest_MapSimulator/PreparedSkillChargeStartupParityTests.cs` for the authored startup seam across the Big Bang trio, Piercing, Monkey Wave, Evan breath, and the Dual Blade charge pair; the local zero-startup immediate-hold seam; the remote zero-startup phase-resolution seam; remote zero-startup auto-hold persistence; the shared client minimum/cap release elapsed rule across representative release families; the shared linear undercharge scaling seam across the Big Bang trio, Piercing, Monkey Wave, Evan breath, and both Dual Blade charge branches; the gauge-window-vs-prepare-window release scaling seam; and the Final Cut scaled HP-cost / restriction branch. | Remaining gap is now narrower and still limited to client-owned choreography outside the WZ-confirmed seams covered here. The simulator still does not mirror the client's full raw make/break scan-code ownership stream, the broader exclusive-request catalog beyond the prepared-skill family that currently shares the `300 ms` gate, or family-specific post-release math and follow-up choreography beyond the evidenced shared elapsed clamp, `get_max_gauge_time` cap, shared linear undercharge seam, and Final Cut HP-cost branch. The current seam is now covered on disk for the behaviors already implemented: release-owned families use authored WZ startup windows when they exist, zero-startup charge families enter hold immediately when WZ leaves `prepare/time = 0`, that same zero-startup auto-hold split survives the remote prepared-skill owner/runtime path whenever a hold window is present, release uses the client-shaped minimum elapsed clamp and gauge cap instead of authored prepare time, and the Big Bang trio plus both Dual Blade charge branches stay on the same shared linear undercharge scaling seam as Piercing, Evan breath, and Monkey Wave. Unevidenced family-specific non-linear release scaling, deeper packet-owned charge semantics beyond this prepared-skill phase split, and broader follow-up choreography are still not one-for-one. | `PreparedSkillHudRules.cs`, `SkillManager.cs`, `MapSimulator.RemoteUsers.cs`, `PlayerInput.cs`, `PlayerManager.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `UnitTest_MapSimulator/PreparedSkillChargeStartupParityTests.cs` (`Skill/{212,222,232}.img/skill/*/info/chargingSkill`, `Skill/322.img/skill/3221001/{prepare/time,prepare/action,keydown/0/delay,action/0,info/piercing,info/piercingBouns}`, `Skill/2212.img/skill/22121000/{prepare/action,prepare/time,common/time,info/chargingSkill}`, `Skill/2215.img/skill/22151001/{prepare/action,prepare/time,common/time,info/chargingSkill}`, `Skill/434.img/skill/{4341002,4341003}/{prepare/action,prepare/time,action/0,info/chargingSkill}`, `Skill/531.img/skill/5311002/{prepare/time,prepare/action,keydown/action,action/0,common/time,info/chargingSkill}`, `get_max_gauge_time`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::OnKeyDownSkillEnd`, `CUserLocal::DrawKeyDownBar`, `CDragon::DrawKeyDownBar`) |
| Partial | Swallow, mine, cyclone, and sit-down healing branches | The simulator keeps the earlier mine/swallow/cyclone/healing parity work on this row and tightens the same client-owned seams again instead of widening into another family. WZ recheck in the current export still shows `Skill/3310.img/skill/33101005/prepare/action = swallow_loop` and `Skill/3310.img/skill/33101005/info/dummyOf = 33101006&&33101007`, while unrelated dummy-linked families such as `Skill/3200.img/skill/32001001/info/dummyOf`, `Skill/3211.img/skill/32111005/info/dummyOf`, and `Skill/3510.img/skill/35100008/info/dummyOf` still do not publish `info/swallow`. `SkillLoader` therefore still keeps the swallow-family classifier on the authored swallow root itself instead of trusting swallow-looking action names alone: only skills that still carry the authored swallow flag plus the visible swallow root's linked dummy follow-up ids are marked `IsSwallowFamilySkill`, so unrelated dummy families no longer inherit swallow-only ownership just because they expose `dummyOf` data beside a swallow-shaped action. Client recheck against `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500` still narrows the wriggle owner to visible `33101005` only, so `SkillManager` still keeps the `500 ms` wriggle cadence and visible cancel path on that same root plus its linked follow-up ids (`33101005`, `33101006`, `33101007`) through the existing prepared-skill / visible-action seam (`swallow_pre`, `swallow_loop`, `swallow`) without accepting reverse-linked unrelated families. That preserves the earlier `900 ms` keydown threshold before release, early-release cancel, absorb-ack gated capture success, automatic `3000 ms` digest start, cancel-path teardown on failed or invalidated absorb outcomes, and captured-mob consumption before the hidden attack dummy branch fires while tightening the family classifier itself to the same WZ-authored root. The earlier mine and cyclone work remains in place on this row. Client recheck against `CUserLocal::TryDoingMine` at `0x907d70` still narrows mine ownership through the riding-vehicle-id gate, town suppression, `update_time - 1000` first-deploy lead, grounded `CVecCtrl::GetInput` X sign, and the shared `CVecCtrl::IsStopped()` clear, so the simulator still resolves Wild Hunter mine movement from grounded controller input plus the stopped-controller gate instead of foothold velocity fallback, and the hidden mine fallback still stays on the narrowed Jaguar Rider-owned live/grace-window owner plus live/equipped jaguar mount seam already landed earlier. Cyclone still sustains as a timed `32121003` body-attack loop with `1000 ms` pulses only while the cyclone action remains active. The sit-down-healing work also remains in the summon/runtime seam, and this pass narrows that seam again instead of broadening generic support ownership. WZ recheck still shows `Skill/3511.img/skill/35111011/info/{condition,minionAbility,type} = {whenUserLieDown, heal, 33}`, `Skill/3511.img/skill/35111011/summon/stand/info/range` publishing `lt=(-150,-100)` / `rb=(150,20)`, `summon/heal/info/range` publishing the narrower `lt=(-125,-100)` / `rb=(125,0)`, `common/x = 5`, and `String/Skill.img/35111011/{desc,pdesc}` still describing nearby party or team healing with the same `5`-second reheal lock. Client recheck against `CUserLocal::TryDoingSitdownHealing` at `0x903d60`, `CUserPool::DoHealNearHealingRobot` at `0x94bb20`, and `CSummoned::TryDoingHealingRobot` at `0x74b3e0` still lands in the same seam: the client only polls Healing Robot while the local player is seated, checks the local robot first, then remote party robots, skips robots already in suspend before the heal attempt, does not pre-check whether HP or MP will actually change before issuing the request, and arms action `13` as `(moveAction << 7) | 13` when the request path succeeds. `SkillManager` still mirrors that owner split through the dedicated seated-only local-first-then-remote Healing Robot poll pass and the generic support-scan skip, while `SummonedPool` still keeps the remote-owner seam on the local-or-party filter, authored heal-branch range, packet-owned facing-bit playback, and the narrowed Healing Robot idle-clear gate. This pass tightens the request seam itself in the existing local support path: `ProcessSummonHealSupport(...)` now completes the sit-down Healing Robot action once the seated range probe succeeds even when the local player is already full HP/MP, so the simulator now mirrors the client-side no-preheal-stat-check owner for the local request, reheal-lock start, suspend window, and action playback instead of treating "no stat delta" as a local-only cancellation path. Focused regression coverage is now on disk in `UnitTest_MapSimulator/PlayerSkillExecutionParityTests.cs` for the new no-stat-change Healing Robot completion gate plus the non-Healing-Robot false branch. The row remains partial because the simulator still does not mirror the client's exact swallow packet ordering, the exact server-owned swallow capture/digest request choreography, the remaining `TryDoingMine` riding-vehicle-id ownership nuances beyond the current active-owner plus update-time grace-window gate and live/equipped jaguar fallback, the remaining request/packet-owned Healing Robot suspend-reset choreography beyond the current seated poll order, no-stat local request completion, action-`13` facing-bit playback, authored heal-branch/range owner, and support-suspend ownership already modeled here, or the remaining non-Wild-Hunter swallow-family nuances one-for-one. | These branches now stay closer to their real client owners instead of flattening back into the generic cast/runtime path. Mine still respects the client town suppression, update-time-owned hidden-owner grace window, narrowed hidden jaguar fallback, and the client-shaped grounded `CVecCtrl` input plus stopped-controller gate instead of a simulator-only foothold-velocity fallback; cyclone still stops pulsing once the sustained cyclone action falls off the avatar; swallow still routes charge, cancel, absorb success/failure, digest timing, and hidden follow-up attack through one WZ-backed swallow-family seam, its wriggle pulses still stop once the visible swallow owner falls off, its linked dummy follow-up ids are now classified only from an authored swallow root instead of from any dummy-linked swallow-looking family, and its local request matching still stays one-way on that visible root plus linked follow-up ids; and sit-down healing now also stays behind the same client-shaped seated poll gate before the existing local/party owner filter, authored reheal-lock support window, explicit heal-branch/range ownership, narrowed suspend clear path, dedicated local-first-then-remote Healing Robot poll path, and the newly matched no-preheal-stat-check local request path. The remaining visible mismatch is narrower and centered on exact swallow packet choreography, the last riding-vehicle-id details in `TryDoingMine`, the last request/packet-owned Healing Robot suspend-reset details outside the current seated poll plus authored playback owner seam, and any residual non-Wild-Hunter swallow-family behavior the client owns outside the current local cancel/request path. | `SkillManager`, `SkillLoader.cs`, `SkillData.cs`, `MapSimulator`, `SummonedPool.cs`, `SummonRuntimeRules.cs`, `UnitTest_MapSimulator/PlayerSkillExecutionParityTests.cs` (`CUserLocal::TryDoingSwallowBuff`, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::TryDoingMine`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingSitdownHealing`, `CUserPool::DoHealNearHealingRobot`, `CSummoned::TryDoingHealingRobot`, `CVecCtrl::GetInput`, `CVecCtrl::IsStopped`, `Skill/3200.img/skill/32001001/info/dummyOf`, `Skill/3211.img/skill/32111005/info/dummyOf`, `Skill/3300.img/skill/33001001/info/type`, `String/Skill.img/33001001`, `Skill/3310.img/skill/33101005/{prepare/action,info/dummyOf}`, `Skill/3510.img/skill/35100008/info/dummyOf`, `String/Skill.img/35111011/{desc,pdesc}`, `Skill/3511.img/skill/35111011/{info/{condition,minionAbility,type},common/{hp,x},summon/stand/info/range,summon/heal/info/range}`, `CUserLocal::TryDoingMine@0x907d70`, `CUserLocal::TryDoingSwallowMobWriggle@0x93f500`, `CUserLocal::TryDoingSitdownHealing@0x903d60`, `CUserPool::DoHealNearHealingRobot@0x94bb20`, `CSummoned::TryDoingHealingRobot@0x74b3e0`) |
| Partial | Summon simulation | The simulator still keeps the broader packet-owned summon parity already landed here: generic summon lifecycle and timers, WZ-backed attack branch and metadata loading, projectile and target-impact branch retention, Tesla triangle targeting, self-destruct prepare/die handling, packet-owned prepare and attack visual parity, taunt-only puppet registration, shared summon rule selection between local and packet-owned seams, the delayed bird tile overlay handoff, the `4111007` / `4211007` reactive-chain family, and the earlier sourced-attack-frame mob-hit split between `info/hit` and attack-frame-owned hit clips. WZ was rechecked first again in the mounted export for the remaining chain-asset, tile-UOL, and mob-hit slice instead of widening transport ownership: `Mob/3220000.img/attack1/info/hit/attach = 1` still keeps the base attached hit family, `Mob/3220000.img/attack1/13/hit/attach = 1` still publishes the later frame-owned attached clip, `Mob/9300314.img/attack1/info/attachfacing = 1` still coexists with `Mob/9300314.img/attack1/info/hit/attachfacing = 1`, `Mob/9300275.img/attack1/info/attachfacing = 1` still keeps the same info-side facing alias seam, `Skill/312.img/skill/3120010/{ball,tile}` still keeps the separate authored bird projectile versus tile families, and both `Skill/411.img/skill/4111007/{summon,ball}` and `Skill/421.img/skill/4211007/{summon,ball}` still expose a real authored split between the summon-body branch and the ball-owned chain asset. Client evidence stayed second and remains on the literal summon/runtime owners this row is anchored to: `CSummonedPool::OnPacket` at `0x75ac70` still owns the packet family dispatch, `CSummoned::OnHit` at `0x74bc80` still resolves summon-hit presentation from decoded `(attackIndex, mobTemplateId, facing)` plus mob-template attack info instead of requiring a live mob object lookup, `CMob::GetAttackInfo` at `0x641330` still resolves attack metadata from the live action code, `SKILLENTRY::{GetTileUOL,GetBallUOL}` at `0x437180` / `0x569390` still keep the earlier tile-versus-ball ownership split, and `CSummoned::OnAttack` at `0x753340` still hardcodes both `4111007` and `4211007` onto the same `GetBallUOL` + `RegisterChainlightningAnimation` branch instead of consulting summon-owned projectile branches while keeping the bird family on `GetTileUOL`. This pass stayed inside the existing summon post-effect seam and closes the next client-confirmed loader fallback drift instead of widening into new families: `SummonClientPostEffectRules`, `ProjectileData.ResolveGetBallLikeUolPath(...)`, `ZoneEffectData.ResolveTileUolPath(...)`, and `SummonedPool.ResolveClientOwnedReactiveAttackChainAnimation(...)` now keep the confirmed client-owned tile and ball UOL families on their stored owner paths end-to-end, so packet-owned summon effects no longer require separately loaded renderable variant clips before they can keep a char-level or skill-level `ball` / `tile` UOL, and the `4111007` / `4211007` chain still never falls back to `SummonProjectileAnimations` or other summon-owned projectile branches for that client-hardcoded branch. The earlier sourced-frame carry-through remains in place too: `MobAnimationSet.GetAttackHitEffectEntry(...)` still keeps the latest frame-owned `attackN/<frame>/hit` entry whose source frame is at or before the current live attack frame before falling back to `info/hit`, and the packet-owned hit side still keeps the client's template-owned fallback shape inside the same `LifeLoader` / `SummonedPool` seam when no live mob instance of the decoded template is present. The earlier branch-owned timing, authored projectile and hit `pos` backfill, bird-tile timing, local named attack-branch prepare suppression, packet-owned raw-action branch selection, and the learned-template Tesla / SG-88 bridge work remain in place. Focused regression coverage is now on disk in `UnitTest_MapSimulator/SummonReactiveChainParityTests.cs`, covering rejection of summon-projectile-only fallback for the confirmed chain family, acceptance of ball-only char-level variants, the `4211007` alias keeping ball-path animation resolution instead of drifting back to summon-owned projectile branches, and char-level tile / ball UOL path retention even when those variant owners do not also publish separately loaded renderable clips. The row remains partial because the remaining gap is narrower but still real: the simulator still does not synthesize every hidden client `MobAttackInfo::sHit` / `bHitAttach` case one-for-one beyond the currently observed WZ surfaces and the modeled sourced-frame attach and facing path; `CSummoned::OnAttack` post-effects are still limited to the confirmed bird tile family plus the confirmed `4111007` / `4211007` reactive-chain family instead of every hidden post-effect case; the exact client-side population path for stored `sTileUOL` / `sBallUOL` fields and any hidden tile-family construction beyond the observed WZ family nodes remain unresolved even though the getter-shaped stored-field selection is now mirrored; literal GR2 chain-layer ownership, exact textured bolt choreography, and literal action-layer canvas ownership or ordering remain unresolved; the simulator-owned `/summonedpacket` loopback plus the live-session `SummonedOfficialSessionBridgeManager` still stop short of a full official `CUserPool` transport and ownership decode; and the late-packet frame-owned hit-entry hold plus template-backed packet-hit fallback still remain render-safe inferences from the authored WZ hit surface and the client `OnHit` template-owner seam rather than from a newly recovered dedicated client decoder for every deeper mob-state-dependent case. | Packet-owned summons are no longer just spawn/remove stubs: they now keep client-shaped owner registration, packet-owned attack, hit, and removal playback, frame-scoped attached-hit behavior, the confirmed bird tile post-effect family, the confirmed `4111007` / `4211007` reactive-chain family, and the selected tile-versus-ball branch identity on the same summon owner without collapsing that ownership back onto generic summon projectile branches. The additional gap closed here is that the client-hardcoded reactive-chain family now stays strictly ball-owned and the packet-owned tile / ball UOL selectors now keep the stored char-level or skill-level owner paths even when no separately loaded renderable variant clip exists, so packet-owned summon effects no longer drift back to top-level or summon-owned branches just because the simulator did not materialize an extra variant animation object. Remaining mismatch risk is now concentrated in deeper hidden mob-hit metadata cases, the unresolved client-side population of stored tile/ball UOL fields, literal GR2 ownership and ordering, and the still-incomplete official transport and ownership decode rather than in the confirmed reactive-chain alias seam or stored tile-vs-ball getter split. | packet-owned summon runtime (`CSummonedPool::OnPacket`, `CSummoned::OnAttack`, `CSummoned::OnHit`, `CSummoned::Hit`, `SummonedPool.cs`, `PacketOwnedSummonUpdateRules.cs`, `SummonClientPostEffectRules.cs`, `SkillLoader.cs`, `ZoneEffectData`, `ProjectileData`, `LifeLoader.cs`, `MobAnimationSet.cs`, `MobAnimationSet.GetAttackHitEffectEntry(...)`, `UnitTest_MapSimulator/SummonReactiveChainParityTests.cs`, `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, `Skill/312.img/skill/3120010/{ball,tile,summon}`, `Skill/411.img/skill/4111007/{summon,ball}`, `Skill/421.img/skill/4211007/{summon,ball}`, `Mob/3220000.img/attack1/{info/hit,13/hit}`, `Mob/9300314.img/attack1/{info/attachfacing,info/hit/attachfacing}`, `Mob/9300275.img/attack1/info/attachfacing`, `SKILLENTRY::GetTileUOL@0x437180`, `SKILLENTRY::GetBallUOL@0x569390`, `CSummoned::OnAttack@0x753340`, `CMob::GetAttackInfo@0x641330`) |
| Partial | Remote summoned-pool packet parity | The simulator still keeps the packet-owned summon transport seam in `SummonedPool`, but the remote attack and hit side is tighter again. It already mirrors the observed `0x116`-`0x11B` packet family, local-versus-remote owner registration, packet-owned move-path sampling, Tesla third-coil refresh, taunt-side ownership, packet hit damage bookkeeping, nested mob `attack/info/hit` metadata with UOL resolution, frame-scoped `attach` / `attachfacing` runtime behavior, authored per-frame summon alpha ramps on both live and detached one-time-action playback, the delayed bird tile overlay handoff from the filtered packet target list, and the `4111007` reactive chain with char-level -> exact-level -> root `ball` precedence before summon-owned or generic fallback. WZ re-check against the mounted export still keeps the same confirmed post-effect surface: `Skill/311.img/skill/3111005/tile` publishes six root tile bands plus `effectDistance=10`, `Skill/321.img/skill/3211005/tile` publishes three root tile bands plus `effectDistance=20`, and `Skill/312.img/skill/3120010` keeps the broader `{CharLevel,ball,tile,summon}` surface. This pass also rechecked the mob-hit side against the mounted `Mob` export and kept the same authored hidden hit-owner seam in view: `Mob/3220000.img/attack1/info/hit/attach` still keeps the base attached hit family, `Mob/3220000.img/attack1/13/hit/attach` still publishes a later attack-frame-owned attached hit clip, and `Mob/9300314.img/attack1/{info/attachfacing,info/hit/attachfacing}` still keeps the same facing-owned alias seam visible on both the info root and the hit branch. Client evidence stayed second and unchanged on the literal owners this row is using: `SKILLENTRY::GetBallUOL` at `0x569390` still selects char-level data first, then exact level, then root, and prefers `flipBall` when the caller asks for it and that family is non-empty; `SKILLENTRY::GetTileUOL` at `0x437180` still only returns the precomputed `sTileUOL` field; and `CSummoned::OnHit` at `0x74bc80` still resolves summon-hit presentation from decoded `(attackIndex, mobTemplateId, facing)` plus template attack info instead of depending on a live mob object. This run stayed inside the same loader, live-mob, and packet-owned summon seams: `SkillLoader` still stores family-owned `tile`, `ball`, and `flipBall` UOL paths separately from the per-frame animation paths for root, `level/<slv>`, and `CharLevel/<requiredLevel>` variants; `ZoneEffectData` and `ProjectileData` still resolve those family UOLs with the same render-safe char-level -> exact-level -> root precedence already used for animation-family selection; `PacketOwnedSummonUpdateRules` still feeds the packet-owned delayed bird overlay plus the scheduled/live `4111007` chain owner with those family UOLs instead of flattened selected-frame paths; and the mob-hit playback side still keeps a short retained live attack-frame observation in `MobItem` so `SummonedPool` can still reuse the matching just-observed attack frame for packet-owned summon hits even after the mob has already stepped off the visible attack action. That still keeps late packets on the last authored frame-owned hit clip a little longer before falling back to `info/hit`, which is especially important for surfaces like `attack1/13/hit` where the authored hidden clip appears late in the attack and the packet can land just after the mob leaves `attack1`. This pass narrows the duplicate-template lookup drift one more step inside that same seam instead of widening transport ownership: when several live mobs share the decoded `mobTemplateId` and still tie on alive state plus observed `attackN` frame progress, `SummonedPool` now uses the decoded packet-facing bit as the last sibling tiebreaker, so packet-owned summon hits are less likely to borrow the wrong same-template sibling's facing just because it appeared first in the pool. The template-owner fallback is also tighter again on that same `CSummoned::OnHit` seam: when a live sibling already supplies the authored hit frames but not the authored attack metadata or `CharDam` key, `SummonedPool` now keeps the live hit frames while still borrowing the template attack info and template `CharDam` fallback from the decoded `mobTemplateId` owner instead of dropping back to metadata-free detached playback or losing the client-shaped damage sound just because a live sibling object existed. Template fallback therefore now stays on the same decoded-template seam in both directions: retained observed frame indices still carry through onto template `attackN/<frame>/hit` selection when the presentation has to fall back to a template-built attack set, and live hit-frame reuse now still keeps the template-owned `attach` / `attachfacing` / range-origin metadata plus sound fallback when the live sibling only partially covers the authored surface. Focused regression coverage is now actually on disk in `UnitTest_MapSimulator/PacketOwnedSummonHitParityTests.cs`, covering latest-prior frame-owned template hit-entry selection, fallback to `info/hit` before the first frame-owned clip, duplicate-template facing-bit tiebreaking after attack-observation ties, live-hit-frame retention with template attack-metadata fallback, and template `CharDam` fallback when the chosen live sibling has no loaded authored damage sound. The row remains partial because the simulator still does not synthesize every hidden client `MobAttackInfo::sHit` / `bHitAttach` case one-for-one beyond the currently observed WZ surfaces and the modeled sourced-frame attach/facing plus short retained late-packet live-frame path; `CSummoned::OnAttack` post-effects are still limited to the confirmed bird tile family plus the `4111007` reactive chain instead of every hidden post-effect case; exact client `sTileUOL` precompute and any hidden tile-family construction beyond the observed WZ family nodes remain unresolved because current client evidence only exposes `GetTileUOL` returning the stored field, not how every tile UOL is authored; literal GR2 chain-layer ownership, exact textured bolt choreography, and literal action-layer canvas ownership or ordering remain unresolved; and the simulator-owned `/summonedpacket` loopback plus the live-session `SummonedOfficialSessionBridgeManager` still stop short of a full official `CUserPool` transport and ownership decode. | Packet-owned summons are no longer just spawn/remove stubs: they now keep client-shaped owner registration, packet-owned attack/hit/removal playback, frame-scoped attached-hit behavior, the confirmed bird tile post-effect family, the confirmed `4111007` reactive chain, the selected tile/ball branch identity on the same packet-owned owner without collapsing that branch identity back to a selected frame path, the real sourced attack-frame owner for hidden mob hit clips instead of flattening those clips back to metadata frame `0`, a short retained live attack-frame observation so late summon-hit packets do not immediately lose a matching frame-owned hidden hit clip the moment the mob steps off the attack action, template fallback that now reuses that retained frame when the hit presentation has to come from a template-built attack set, template attack-metadata plus `CharDam` fallback when the chosen live sibling only partially covers the authored hit surface, and a tighter live-mob selector when several instantiated mobs share the same decoded template id. That keeps future parity work anchored to the same `SummonedPool` seam the client uses for packet-owned summon lifecycle and post-attack presentation, while the remaining hidden hit cases, unresolved tile-UOL precompute, GR2 ownership, and official `CUserPool` transport gaps stay explicit. | packet-owned summon runtime (`CSummonedPool::OnPacket`, `CSummoned::OnAttack`, `CSummoned::OnHit`, `CSummoned::Hit`, `SummonedPool`, `PacketOwnedSummonUpdateRules`, `SkillLoader`, `ZoneEffectData`, `ProjectileData`, `LifeLoader`, `MobAnimationSet.AttackInfoMetadata`, `MobItem`, `UnitTest_MapSimulator/PacketOwnedSummonHitParityTests.cs`, `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, `Skill/312.img/skill/3120010/{CharLevel,ball,tile,summon}`, `Mob/3220000.img/attack1/{info/hit,13/hit}`, `Mob/9300314.img/attack1/{info/attachfacing,info/hit/attachfacing}`, `SKILLENTRY::GetTileUOL@0x437180`, `SKILLENTRY::GetBallUOL@0x569390`, `CSummoned::OnHit@0x74bc80`) |
| Partial | Summoned actor update/runtime parity | The simulator still keeps summon-owned actor update/runtime ownership in the same runtime seams: movement snapshots, spawn/prepare/attack/hit/die routing, Tesla armed-prepare idle fallback, self-destruct expiry handoff, packet-owned hit-overlay interrupt clearing, named-branch timing that avoids forcing generic `summon/prepare` onto authored branches, and the shared suspend window that keeps authored support or summon-action branches from being immediately retriggered. This pass stayed inside the existing `SummonRuntimeRules`, `SkillManager`, and `SummonedPool` seams again and closed the next assist-family drift after the earlier packet-owned named-branch retry fix. WZ was rechecked first against the mounted export for the authored branches that constrain this runtime split: `Skill/3511.img/skill/35111011/summon/heal` still publishes the Healing Robot support branch; `Skill/3512.img/skill/35121009/{info/{type,minionAbility,selfDestructMinion},summon/subsummon}` still publishes the summon-action family as `type=33`, `minionAbility=summon`, `selfDestructMinion=1`, and an authored six-frame `subsummon` clip totaling `720ms`; the stand-only support skills under `Skill/3511.img/skill/35111005/summon/stand` and `Skill/3512.img/skill/35121010/summon/stand` still keep `info/selfDestructMinion=1` without separate authored `heal` / `support` branches; and `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}` still publishes the Tesla Coil armed/attack branch family that constrains its packet-owned runtime handoff. Client evidence was then rechecked in IDA: `CSummoned::OnSkill` at `0x74a940` still clears any older one-time owner before routing the decoded packet action into `CSummoned::SetAttackAction`, `CSummoned::SetAttackAction` at `0x74a0f0` still just stamps `m_nOneTimeAction` and re-prepares the action layer, `CSummoned::Update` at `0x757f70` still keeps that one-time owner alive through `ProcessAction` / `ProcessAttack` until the clip finishes while also forcing move action `0x2A` during Tesla states `1` and `2` before `SetMoveAction`, `CSummoned::TryDoingHealingRobot` at `0x74b3e0` still sends action `13` and sets the suspend flag after the authored heal branch begins, and `CSummoned::TryDoingSummon` at `0x74b640` still sends action `14` and sets that same suspend flag for summon-action playback. The simulator already mirrored the earlier packet-owned explicit self-destruct owner detail through `SummonedPool.ResolvePacketOwnedExplicitSelfDestructPlayback(...)`, `TryBeginSelfDestructRemoval(...)`, `TryTriggerExpiredSelfDestructAction(...)`, and the earlier Tesla sibling stale-owner clear inside `BeginPacketOwnedSkillAnimation(...)`; this pass closes the next ownership leak in that same seam by extending the existing `SupportSuspendUntilTime` owner to summon-action playback as well. Local `ProcessSummonAction(...)` now honors an active suspend window before retriggering, arms the existing suspend timer from the authored `subsummon` branch just like the support path already did, and packet-owned `BeginPacketOwnedSkillAnimation(...)` / idle clear now use the same shared branch-duration resolver so action `14` stays suspended through the authored `subsummon` clip instead of behaving like an unsuspended generic attack branch. Focused regression coverage is now on disk in `UnitTest_MapSimulator/SummonRuntimeActionParityTests.cs` for summon-action suspend duration resolution, summon-action suspend clear timing, and packet-owned summon-action suspend duration resolution. The row remains partial because the deeper client-only seams are still outside current simulator visibility: exact hidden summon HP and full `CalcDamage::PDamageSummoned` / `MDamageSummoned`, broader `CVecCtrl::UpdatePassive` side effects beyond the confirmed passive effect trigger, deeper Tesla state-machine transitions and refresh rules beyond the current armed/attack fallback and packet fan-out ownership, and the remaining remote assist-family branch choices that still depend on client-only state not exposed by current packet/WZ data. | This keeps more of real summon runtime ownership inside the summon layer instead of collapsing either local or packet-owned assist families to ad hoc runtime fallbacks: local and packet-owned body-contact damage still share the same mob-authored base-damage surface plus outgoing-damage modifiers, packet-owned `OnSkill` branch ownership still lands on authored `skill1..skill6` when packet actions provide those branch ids, packet-owned named summon branches still avoid inheriting a generic prepare window, packet-owned generic prepare branches still promote into live attack state instead of staying pinned on one coarse packet state through the whole action, packet-owned mob-hit overlays still get interrupted both when summon-owned action-layer changes begin and when packet-owned attack playback completes back into the steady summon layer, and summon-action playback now shares the same authored suspend seam the client uses for both action `13` and action `14` instead of treating `subsummon` like an unsuspended generic branch. The additional gap closed here is that the simulator no longer keeps the suspend owner support-only: the existing authored-branch suspend window now covers local and packet-owned summon-action playback too, so Mechanic summon-action branches stay inside the same action-layer ownership seam the client uses. Remaining mismatch risk is now concentrated in deeper hidden damage formulas, unmodeled passive-update side effects, deeper Tesla transitions and refresh rules, and the last remote/client-only assist selectors rather than in the observed authored support-branch surface, authored summon-action suspend ownership, packet-owned explicit self-destruct branch ownership, packet-owned Tesla sibling one-time-owner cleanup, or packet-owned named-branch retry suppression. | summon actor/runtime layer (`CSummoned::Update`, `CVecCtrl::UpdateActive`, `CVecCtrl::UpdatePassive`, `CSummoned::OnHit`, `CSummoned::OnSkill`, `CSummoned::SetAttackAction`, `CSummoned::SetMoveAction`, `CSummoned::ProcessAction`, `CSummoned::ProcessAttack`, `CSummoned::SetDamaged`, `CSummoned::TryDoingHealingRobot`, `CSummoned::TryDoingSummon`, `SummonRuntimeRules.cs`, `SkillManager.cs`, `SummonedPool.cs`, `SummonedPool.ResolvePacketOwnedExplicitSelfDestructPlayback(...)`, `BeginPacketOwnedSkillAnimation(...)`, `TryResolveSummonAttackPlaybackAnimation(...)`, `UnitTest_MapSimulator/SummonRuntimeActionParityTests.cs`, IDA `CSummoned::Update` (`0x757f70`), IDA `CSummoned::OnSkill` (`0x74a940`), IDA `CSummoned::SetAttackAction` (`0x74a0f0`), IDA `CSummoned::TryDoingHealingRobot` (`0x74b3e0`), IDA `CSummoned::TryDoingSummon` (`0x74b640`), `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3512.img/skill/35121009/{info/{type,minionAbility,selfDestructMinion},summon/subsummon}`, `Skill/3511.img/skill/35111005/{info/selfDestructMinion,summon/stand}`, `Skill/3512.img/skill/35121010/{info/selfDestructMinion,summon/stand}`, `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}`) |
| Partial | Weapon afterimage / melee trail parity | The simulator still keeps the broader WZ-backed afterimage surface already landed for this row: skill-authored plus shared `Character/Afterimage/<family>/<masteryIndex>` catalogs, weapon-item `info/afterImage` family selection with narrow sword-sibling fallback only, local basic-melee registration, charged `charge/<element>` rewrites for the confirmed Page, Paladin, Thunder Breaker, and Aran families, alias-aware action matching, `1221009 blast -> stabO2`, client-backed raw-range normalization for `1221009`, `57 -> 41`, and `74`, ordered remote WZ action fallback, the single-authored plus sole-renderable remote pose promotion seams, remote prepared-skill release teardown from the official melee packet, remote charge fallback inside the shared remote temporary-stat plus remote melee owner, the newer per-frame-set layer-list playback that preserves the client-ordered authored `0..u-1` canvas stack, the client-style independent `a0` / `a1` endpoint defaulting for one-sided afterimage alpha authorship, and now the matching SpriteBatch-side one-sided `z1` growth fallback from the resident 100% layer zoom when generic `LoadCanvas` metadata authors only a terminal zoom endpoint. This pass stayed inside the same local and remote afterimage loader plus playback seams again and narrowed the next metadata-default drift instead of widening transport or renderer ownership. WZ was rechecked first against the mounted export: `Character/Afterimage/swordOS/0/swingO1/2/0` still publishes a single canvas with `delay=200` and `a1=0`, `Character/Afterimage/swordOS/charge/1/stabO2/1/{0,1}` still publishes the two authored canvases with `delay=50` then `delay=300` and terminal `a1=0`, `Skill/122.img/skill/1221009/afterimage/swordOS/stabO2/1/0` still publishes `delay=60`, `Character/Afterimage/swordOS/0/swingD1/1/2` still publishes a lone authored `a0=255` with no sibling `a1`, the sampled `swordOS` plus `knuckle` afterimage catalogs still do not expose authored `z0` or `z1` properties in this mounted data set, and the broader mounted `LoadCanvas` surface still proves one-sided zoom authorship exists via `Skill/131.img/skill/1311008/effect/16` publishing `delay=200`, `a1=64`, and `z1=200` with no sibling `z0`. Client evidence was rechecked second in IDA: `CActionMan::CreateAfterimageLayer` at `0x428a20` still resets layer alpha, calls `RemoveCanvas(-2)`, resolves the weapon afterimage array for the active raw-action branch, and calls `CAnimationDisplayer::LoadCanvas` for every authored canvas in that branch, while `CAnimationDisplayer::LoadCanvas` at `0x446e60` still reads each canvas node's `delay`, `a0`, `a1`, `z0`, and `z1` metadata into separate `InsertCanvas` arguments with independent default sentinels before passing them to `IWzGr2DLayer::InsertCanvas`. `MeleeAfterimagePlaybackResolver` still keeps the earlier non-looping action-frame timing path for afterimage snapshot refresh and fade capture so local `PlayerCharacter` and remote `RemoteUserActorPool` no longer wrap the cached afterimage snapshot back toward earlier action frames when fade capture happens at or after the melee action's authored end. `SkillLoader.LoadSkillFrame(...)` still preserves the client-style one-sided alpha matrix instead of normalizing missing endpoints to an implicit `255 -> 255` pair: a lone authored `a0` resolves to a constant start/end alpha pair, a lone authored `a1` resolves from the layer-reset `255` start alpha, and fully missing alpha metadata stays on the prior opaque default. `MeleeAfterimagePlaybackResolver.ResolveFrameZoom(...)` now applies the same recovered generic `LoadCanvas` endpoint contract for the currently simulated SpriteBatch zoom path by letting a lone authored `z1` interpolate from the resident `100` percent zoom instead of flattening immediately to the terminal scale, while keeping missing `z0`/`z1` at the existing 1x fallback and lone `z0` at the prior constant authored-start behavior. Focused regression coverage is now on disk in `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` for generic afterimage zoom endpoint playback over fully missing zoom metadata, paired `z0` / `z1` interpolation, lone-`z1` resident-100% growth, and lone-`z0` constant playback; the earlier row-scoped coverage target still includes lone-`a0` endpoint preservation, lone-`a1` reset-alpha fallback, lone-`a0` constant-alpha interpolation, exact action-end final-frame capture, post-end final-frame clamping, and release-boundary fade continuation from the preserved final-frame elapsed time. The remaining gap is narrower again but still real: the mounted export still does not show visible afterimage-specific zoom authorship, so the corrected one-sided `z1` behavior is still anchored to the shared client `LoadCanvas` contract and broader skill-effect WZ evidence rather than a visible afterimage trail sample; the simulator still maps client zoom metadata onto SpriteBatch scaling instead of reproducing literal GR2 `InsertCanvas` interpolation; direct IDA recheck for this client version still only proves the current raw-range owner surface, so any further range overrides need new client evidence; the simulator still does not reproduce exact GR2 layer ownership and side effects such as the precise `RelMove`, `RemoveCanvas`, `InsertCanvas`, and layer-engine timing choreography; and the broader remote field packet surface still needs the rest of a one-for-one `CUserRemote::OnAttack` / `CUserRemote::OnMeleeAttack` transport lift and client-owned side effects beyond the currently consumed attack envelope, mob-hit, bullet-item, release-follow-up, broader remote charge-family fallback, and explicit-skill-range-owner slices. | Many melee jobs rely on afterimage trails and authored swing ranges to sell timing and hit shape, and the simulator now consumes the same WZ-backed trail, alpha, lifetime, range, and generic `LoadCanvas` metadata for local melee skill playback, local basic melee attacks, packet-owned local melee hitbox resolution, and simulator-owned remote actors while respecting the weapon item's authored afterimage family instead of forcing every sword through the long-family arcs. The row stays `Partial` because the remaining visible mismatch is now concentrated in literal GR2 layer choreography, unobserved or unevidenced range overrides, afterimage-specific zoom samples that remain absent from the mounted export even though the generic one-sided `z1` fallback is now modeled, and the rest of the official remote melee transport rather than in post-release per-canvas lifetime ownership, one-sided alpha-endpoint defaulting, one-sided generic zoom growth defaulting, or release-edge frame timing inside the current local and remote seams. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `CharacterLoader.cs`, `CharacterAssembler.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `CharacterData.cs`, `MapSimulator.RemoteUsers.cs`, `PreparedSkillHudRules.cs`, `ClientMeleeAfterimageRangeResolver.cs`, `AfterImageChargeSkillResolver.cs`, `MeleeAfterimagePlaybackResolver.cs`, `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` (`CActionMan::Init`, `CActionMan::GetWeaponAfterImage`, `CActionMan::CreateAfterimageLayer`, `CActionMan::GetMeleeAttackRange`, `CAnimationDisplayer::LoadCanvas`, `get_action_name_from_code`, `CUser::GetAfterimageUOL`, `CUser::RegisterAfterimage`, `CUserRemote::OnAttack`, `CUserRemote::OnMeleeAttack`, `SecondaryStat::DecodeForRemote`, `Character/00002000.img`, `Character/Afterimage/swordOS/{0/swingO1/2,0/swingD1/1/2,charge/1/stabO2/1}`, `Character/Afterimage/*`, `Skill/122.img/skill/1221009/{action,afterimage}`, `Skill/434.img/4341004/common/{lt,rb}`, remote temporary-stat payloads) |
| Partial | Skill cooldown UI parity | QuickSlot overlays and skill hover cooldown presentation, both skill-book icon overlays, the hotkey and off-bar cooldown trays, the shared packet-owned/server-owned cooldown ownership seam in `SkillManager`, the `UI/Basic.img/Notice3` off-bar notice stack, and the Battleship sentinel durability-side effect path still live on the same existing cooldown UI/runtime seam instead of parallel per-surface cooldown math. This run kept that shared owner in place, rechecked the mounted WZ evidence first, and closed the next mounted skill-book tooltip-frame gap without widening the runtime surface: `UI/UIWindow.img/Skill/CoolTime` still exposes `16` stepped cooldown-mask canvases, `UI/UIWindow2.img/Skill/main/CoolTime` still exposes the matching `16` stepped family, `UI/Basic.img/Notice3` still exposes the `t/c/s` stack at `266x21`, `266x20`, and `266x55`, and the sibling notice families still leave `Notice3` as the closest mounted fit for the current top-center cooldown stack (`Notice4` adds the extra inner box instead of the plain bottom panel the simulator already targets). The mounted skill-book tooltip frames now also stay in view instead of being ignored: `UI/UIWindow2.img/Skill/main/tip0`, `tip1`, and `tip2` still expose the same `193x102` family with authored origins `(195,-25)`, `(-178,-22)`, and `(103,-282)`, while `UI/UIWindow.img/Skill/tip0`, `tip1`, and `tip2` still expose the legacy `193x102` family with zero-origin fallback. `Skill/522.img/skill/5221006/common` still authors `mpCon`, `epad`, `epdd`, `emdd`, `emhp`, `emmp`, and `cooltime`, so the existing `SkillLevelData` authored-property seam remains the right owner for Battleship-family tooltip ordering. Client evidence stayed second: `CUIStatusBar::Draw` still owns the tray surface, `CUISkill::Draw` still owns the book surface, and `CUIToolTip::DrawReqSkill` still confirms the client keeps skill-book tooltip composition under the shared skill-tooltip owner rather than duplicating per-surface stat formatting. The source seam remains shared and tighter: `SkillManager.ResolveCooldownMaskFrameIndex` still consumes the full authored `0..15` cooldown-mask family with continuous duration progress, `SkillManager.ResolveCooldownMaskFallbackFillRatio` still preserves the authored `CoolTime/15` last live sliver when WZ masks are missing, `QuickSlotUI`, `SkillUI`, `SkillUIBigBang`, and both status-bar cooldown trays still source mask frames and countdown text through the shared `SkillManager` cooldown-visual helper on top of `TryGetCooldownUiState`, `SkillCooldownTooltipText` still exports the shared state-line plus secondary-line splitter, and `SkillUI` plus `SkillUIBigBang` now route skill-book hover frame selection through a shared `SkillTooltipFrameLayout` resolver so the mounted `tip0`/`tip1`/`tip2` family and authored origins drive hover tooltip placement/background choice instead of a generic dark fallback rectangle. Status-bar tray hover still carries the same secondary-line formatter through the existing cooldown render-data cache, and the packet-owned Battleship sentinel durability path still updates `SkillManager.SetAuthoritativeVehicleDurabilityPresentation` so durability text stays on the same authoritative tooltip seam while remaining out of cooldown-mask visuals. Focused regression coverage is now actually on disk in `UnitTest_MapSimulator/SkillTooltipFrameLayoutTests.cs` for mounted Big Bang tip-frame preference, alternate-frame fallback when the preferred frame overflows, and legacy zero-origin fallback clamping on the same resolver seam. The remaining parity gap is narrower but still real: exact cooldown-notification art family selection beyond the current WZ-backed `Notice3` best fit, any last notice micro-offset or timing/fade differences beyond the shared `44px` top-center seam, any still-unrecovered legacy skill-book anchor offsets beyond the current mounted zero-origin fallback on `UI/UIWindow.img/Skill/tip*`, final client text spacing/color/compositor polish beyond the shared state-line plus secondary-line split plus mounted frame selection across book, quick-slot, and tray hover tooltips, exact Battleship `CTemporaryStatView` icon/tray art and any durability-specific HUD widgeting beyond the mounted override plus shared UI-state durability presentation already modeled here, and any additional bespoke cooldown-only HUD widgets beyond the quick-slot, skill windows, status-bar trays, and off-bar notice stack. | Cooling-down skills remain synchronized across quick-slot masks, both skill-book variants, both status-bar trays, both skill-book hover tooltips, tray hover state lines, tray hover secondary cost/stat lines, and off-bar started/blocked/ready notices because those surfaces all read the same authoritative cooldown UI-state seam, one cooldown-visual helper, and one tooltip formatter family on top of the authored `CoolTime` family. Packet-owned refreshes still preserve the original cooldown start tick without retriggering the start-notice seam, authoritative clears and natural expiry still stop every cooldown surface together without falling back to authored WZ timers, the shared mask helper still reaches the authored `CoolTime/15` last-step frame before cooldown completion instead of flattening to a `0..14` range, and the quick-slot plus tray fallback rectangles still mirror that same final sliver when WZ masks are absent instead of dropping early. Quick-slot hover plus both skill-book variants still keep cooldown or durability state on a dedicated shared primary line and keep MP/authored stat costs on the same shared secondary-line formatter that tray hover already used, so Battleship durability still does not displace authored stat ordering or client-shaped cooldown emphasis inside those hover surfaces. The additional lift in this run is that both skill-book hover surfaces now use the mounted tooltip frame family instead of a simulator-only background box, and the Big Bang window now honors the mounted tip origins when it chooses which tooltip frame can fit on screen. Battleship durability/stat presentation still remains routed through the shared formatter, the packet-owned sentinel's `SkillManager` vehicle-durability presentation, and WZ-authored property order, and this row now also has on-disk regression coverage over the shared skill-book tooltip-frame resolver rather than only the text split. The simulator still does not reproduce the client's exact cooldown-notification art choice, final notice timing or micro-offset polish, any last legacy skill-book anchor offsets beyond the mounted zero-origin fallback, exact Battleship temporary-stat tray/icon visuals, or any extra cooldown-only HUD widgets that may still live outside the currently modeled shared seams. | `QuickSlotUI.cs`, `SkillUI.cs`, `SkillUIBigBang.cs`, `SkillCooldownTooltipText.cs`, `SkillTooltipFrameLayout.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `StatusBarUI.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `SkillManager.cs`, `SkillCooldownNoticeUI.cs`, `SkillCooldownNotificationTransition.cs`, `Skill/522.img/skill/5221006/common`, `UI/UIWindow.img/Skill/CoolTime`, `UI/UIWindow.img/Skill/tip0`, `UI/UIWindow.img/Skill/tip1`, `UI/UIWindow.img/Skill/tip2`, `UI/UIWindow2.img/Skill/main/CoolTime`, `UI/UIWindow2.img/Skill/main/tip0`, `UI/UIWindow2.img/Skill/main/tip1`, `UI/UIWindow2.img/Skill/main/tip2`, `UI/Basic.img/Notice3`, `UnitTest_MapSimulator/SkillTooltipFrameLayoutTests.cs`, `CUIToolTip::DrawReqSkill@0x88b320`, `CUISkill::Draw@0x84ed90`, `CUIStatusBar::Draw@0x876cd0` |
| Partial | Client-timer skill expiry and cancellation | `SkillManager` still owns the centralized client-timer registry for buff expiry, timed prepared-skill release, repeat-sustain expiry and mode-end, cyclone teardown, Wild Hunter swallow digest completion, WZ-timed local skill-zone expiry, and WZ-timed local summon expiry through one `UpdateClientTimer`-style seam, and cancel routing still normalizes through connected WZ `info/type`, `affectedSkill`, and `dummyOf` families plus the client-owned pre-send alias rewrite in `CUserLocal::SendSkillCancelRequest`. `SummonedPool` still reuses that connected-family matcher for packet-owned local summon cancellation, connected-family level recovery, authored-duration fallback, inherited local-duration fallback, authored pending-removal priming for natural non-self-destruct expiry, and packet-owned self-destruct teardown. WZ was rechecked first for the representative local skill-zone branch as well as the existing support self-destruct branch: `Skill/422.img/skill/4221006` still publishes `info/type=36`, `info/zoneType=invincible`, `common/time=30+x`, and authored `tile` / `effect` frames for the local zone lifetime, while `Skill/3511.img/skill/35111011` still publishes `info/type=33`, `info/minionAbility=heal`, `info/condition=whenUserLieDown`, `info/selfDestructMinion=1`, `common/selfDestruction`, `summon/{summoned,stand,heal,die}`, `summon/heal/info/range lt=(-125,-100) rb=(125,0)`, `summon/die/info/range lt=(-120,-110) rb=(120,10)`, `summon/die/info/mobCount=6`, and `hit/0/hitAfter=990`; the connected cancel-family WZ evidence this timer bridge depends on also remains present in the current extracted data source, including the Battle Mage aura alias rows that line up with the client rewrite (`32120000 -> 32001003`, `32110000 -> 32101002`, `32120001 -> 32101003`) plus the linked `dummyOf` and `affectedSkill` rows already feeding the same matcher. Direct IDA re-check stayed second and still anchors the client seam at `CUserLocal::SendSkillCancelRequest` (`0x93d730`) for cancel-side fallout and `CSummoned::Update` (`0x757f70`) plus `CSummoned::TryDoingAttack` (`0x756860`) for expiry/runtime ownership, including the hardcoded pre-send rewrites, the vehicle-valid branch, the keydown prepare-animation removal gate, the local dragon one-time-action cleanup, the mirrored-rectangle probe, and the owner-side choice before `ProcessAction` / `ProcessAttack`. This pass stayed inside that same seam and closed the next timer-side drift instead of widening runtime ownership: packet-owned summon timer fallback now preserves the authored natural-expiry pending-removal branch before direct teardown, so non-self-destruct packet-owned summons keep the same WZ-backed pending-action delay plus removal playback boundary that the existing local summon-expiry priming seam already uses, while self-destruct branches still stay on their existing explicit teardown path. The earlier local summon-expiry, packet-owned local connected-family batch cancel, and local skill-zone-expiry dedupe seams remain in place on that same owner surface, and focused regression coverage is now on disk in `UnitTest_MapSimulator/ClientSkillTimerCancelParityTests.cs` for the client alias rewrites, successful-prime-only connected-family batch dedupe on the local summon-expiry, packet-owned local summon-expiry, and local skill-zone-expiry timer seams, plus pending-removal priming for the packet-owned natural-expiry fallback itself. The row remains partial because the simulator still does not mirror the client's full timer catalog or full `CSummoned::Update` combat runtime: target acquisition and combat results still fall back to the current authored-range approximation instead of the full client runtime when no authoritative packet target history exists, locally synthesized remote expiry still stops at presentation instead of recreating authoritative remote combat fallout, and any additional `SendSkillCancelRequest` consumers beyond the currently surfaced prepared-skill, swallow-reset, vehicle-valid, keydown-cleanup, dragon one-time-action, local summon-expiry, packet-owned local summon-expiry, and local skill-zone-expiry fallout are still outside the current local WZ plus packet plus connected-family timer view. | Timed skill teardown still runs through one shared local seam instead of scattered expiry checks, and connected WZ cancel families plus the client's hardcoded Battle Mage alias rewrites still tear down prepared state, repeat/cyclone state, buffs, local summons, local zones, and packet-owned local summons through the same normalized request surface. This run narrows the remaining timer drift by making packet-owned natural non-self-destruct expiry preserve the authored pending-removal seam instead of collapsing straight to direct die playback, while keeping the earlier local summon-expiry, packet-owned local summon-expiry dedupe, packet-owned self-destruct presentation, and local skill-zone-expiry dedupe behavior on the same WZ-backed owner seam. Remaining risk is now concentrated on unmodeled client-owned timer families, the exact `CSummoned::Update` target-selection and combat-resolution rules that still sit behind the current authored-range fallback when no packet target evidence exists, and any additional `SendSkillCancelRequest` consumers not yet surfaced after the prepared-skill, swallow-reset, vehicle-valid, keydown-cleanup, dragon one-time-action, local summon-expiry, packet-owned local summon-expiry, and local skill-zone-expiry cancel-side effects. | `ClientSkillCancelResolver`, `SkillManager`, `SummonedPool`, `MapSimulator.PacketOwnedSummonedParity.cs`, `UnitTest_MapSimulator/ClientSkillTimerCancelParityTests.cs` (`CUserLocal::UpdateClientTimer`, `CUserLocal::SendSkillCancelRequest`, `CSummoned::Update`, `CSummoned::TryDoingAttack`, `Skill/422.img/skill/4221006/info`, `Skill/422.img/skill/4221006/common`, `Skill/3511.img/skill/35111011/info`, `Skill/3511.img/skill/35111011/common`, `Skill/3511.img/skill/35111011/summon`, `Skill/3511.img/skill/35111011/hit`, `Skill/3212.img/skill/32120000/info/affectedSkill`, `Skill/3211.img/skill/32110000/info/affectedSkill`, `Skill/3212.img/skill/32120001/info/affectedSkill`) |
| Partial | Skill restrictions by map/state | The simulator still keeps the shared restriction seam already landed for this row: parsed map `fieldLimit` bits for `Unable_To_Use_Skill`, `Move_Skill_Only`, `Unable_To_Use_Mystic_Door`, the Mechanic-only `Unable_To_Use_Rocket_Boost` branch, and the mount / broader vehicle `Unable_To_Use_Taming_Mob` branch; the field-authored `info/moveLimit` movement-skill counter seam in `FieldRuleRuntime`; live `MapInfo` ownership for Coconut, Snowball, and Guild Boss basic-action owners; WZ-authored root `noSkill/skill`, class-number `noSkill/class`, and `info/noCancelSkill`; Mu Lung Dojo `mobMassacre/disableSkill`; the authored massacre `countEffect/*/skillUse` counter seam; shared mob-disease skill lockouts including `StopMotion` (`136`); ladder/rope vetoes for swallow plus the broader `DoActiveSkill_ShootAttack` family; and scripted direction-mode ownership that tears down prepared or upkeep-driven skills once local control is taken away. WZ re-check still shows map-authored `noSkill/skill`, class-number `noSkill/class`, `info/noCancelSkill`, and `info/moveLimit` surfaces on the representative maps already tied to this row; `Map/Map9/980031100.img/noSkill/class` still publishes `3` and `4`, `Map/Map2/200100001.img/noSkill/class` still publishes `1`, `2`, `3`, and `4`, and `Map/Map9/922240000.img/noSkill/skill` still publishes direct skill ids including `5221006`, `1013`, `10001014`, `5111005`, `5121003`, `13111005`, `15111002`, and `5101007`. The same WZ-side irregular-root evidence the simulator already consumes for Dual Blade progression also stays visible on the mounted skill surface through the separate `Skill/430.img`, `Skill/431.img`, `Skill/432.img`, `Skill/433.img`, and `Skill/434.img` roots, so the authored class-number surface remains a small advancement-tier gate rather than a full irregular job-id list. Direct IDA re-check of the `CField::IsUnableToUseSkill` callers still confirms the client does not blanket-reject every skill under that field-limit bit. `CUserLocal::TryDoingMeleeAttack` (`0x91e780`), `TryDoingShootAttack` (`0x925a00`), and `TryDoingMagicAttack` (`0x92a240`) each guard `CField::IsUnableToUseSkill` with explicit recovered skill-id tables only, so the shared `FieldSkillRestrictionEvaluator` still blocks just that recovered client set (`4211002`, `4221001`, `1121006`, `1221007`, `1321003`, `4321001`, `4121008`, `5101002`, `5101004`, `5121005`, `21100002`, `21110003`, `21110006`, `4311003`, `4331000`, `4331004`, `4331005`, `4341002`, `33111002`, `35001003`, `1121001`, `1321001`, `3120010`, `3121003`, `3221003`, `5201006`, `32101001`, `32111011`) instead of flattening the whole bit into a simulator-wide "most skills" ban. This pass keeps that same shared evaluator and widens the already-modeled `noSkill` seam only along the same WZ-authored child names: small authored class numbers still keep the earlier skill/current-job advancement-tier match (`1` for first job, `2` for second job, `3` for third job, and `4` for fourth job, with the loaded fourth-job flag preserved for irregular roots), the irregular Dual Blade roots `430`-`434` still map onto those same client-facing tiers instead of offsetting them upward by one, and the same `skill` / `class` checks now also walk nested descendants under the `noSkill` root instead of requiring those child names to sit directly under `noSkill`. That keeps representative `noSkill/class = 3,4` maps blocking third- and fourth-job skills, leaves nonmatching second-job skills and `class = 1` branch-ancestry false positives alone, keeps first-job Dual Blade skills blocked on `class = 1` maps, and now also preserves those same client-facing restrictions when map data wraps the existing `skill` or `class` lists one level deeper under the `noSkill` surface. The simulator still no longer treats Thunder Breaker `Dash` (`15101003`) as client-forbidden under `Unable_To_Use_Skill`, still no longer treats every active Snowball round as if `CField_SnowBall::BasicActionAttack` had already taken local ownership, still no longer treats every active Coconut round as if `CField_Coconut::BasicActionAttack` had already taken local ownership, still no longer treats merely standing in the Guild Boss pulley area, a live pulley state, or a pending pulley preview sequence as if `CField_GuildBoss::BasicActionAttack` had already taken local ownership, still mirrors the recovered `TryDoingMagicAttack` no-foothold gate instead of broadly allowing airborne magic outside prepare-skill cases, and still keys Evan dragon-root magic off the live dragon-owner seam rather than only the map wrapper. Focused on-disk regression coverage for this row is now present in `UnitTest_MapSimulator/PlayerSkillStateRestrictionParityTests.cs` for WZ-authored `noSkill/class` class-number matching, class-one ancestry false-positive suppression, nonmatching second-job allowance, direct `noSkill/skill` blocking, the irregular Dual Blade first-job tier case, and nested-wrapper `noSkill/skill` plus `noSkill/class` handling alongside the earlier airborne magic, Coconut, Snowball, `Unable_To_Use_Skill`, `moveLimit`, one-time-action, and no-dragon seams already tracked for this row. The remaining gap is narrower but still real: deeper `CField` availability checks and remaining field-owned or client-only forbidden-skill branches outside the now-modeled recovered `Unable_To_Use_Skill` table, any still-unrecovered Coconut or Snowball ownership beyond the now-modeled local normal-attack / touch-owner seams, any still-unrecovered `noSkill` child shapes beyond nested `skill` ids and class-number entries, any remaining local-state cast gates outside the now-modeled magic no-foothold allowlist plus the earlier swallow / shoot / vehicle / one-time-action seams, and any additional client-only dragon-owner loss cases beyond the simulator's current live dragon runtime are still incomplete. | Maps and transient local states now block skill use closer to the client from one shared place instead of scattered path-specific checks: field-authored skill bans still flow through the live map seam, field-authored movement-skill quotas now stay pinned to the same field-rule owner that decrements successful local movement casts, prepared or sustained skills still tear down when local state, mob-disease, scripted control, or massacre counters turn invalid after cast start, active skill requests are still rejected while the client-owned one-time-action window is playing, airborne magic now no longer bypasses the recovered `TryDoingMagicAttack` no-foothold gate, `Unable_To_Use_Skill` maps now no longer suppress unrelated skills that the client does not reject under that bit and no longer overblock `15101003`, Snowball no longer suppresses skills for the whole active round when local basic-action ownership has not actually engaged, Coconut no longer suppresses skills for the whole active round when no fresh local normal-attack request owns the basic-action packet seam, Guild Boss no longer suppresses skills just because the player is inside the pulley rectangle or the pulley state is nonzero, Evan dragon-root magic now follows the live local dragon-owner seam rather than only the no-dragon map wrapper, irregular Dual Blade job roots now honor the same authored `noSkill/class` advancement tiers that the client-facing map data publishes, and the same known `noSkill/skill` plus `noSkill/class` surfaces now continue to apply even when those child lists are wrapped under an intermediate `noSkill` node instead of sitting directly at the root. The row stays `Partial` because the remaining mismatch is now concentrated in unrecovered `CField` gate branches and any additional client-only forbidden-state or special-field owners beyond the currently modeled field limits, recovered `Unable_To_Use_Skill` skill-id set, recovered magic no-foothold allowlist, live dragon-owner seam, `moveLimit`, the narrowed Coconut/Snowball/Guild Boss local owner seams, and `noSkill` child families beyond the currently modeled nested `skill` plus `class` surfaces. | `MapSimulator`, `SkillManager`, `FieldSkillRestrictionEvaluator.cs`, `FieldRuleRuntime.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `PlayerSkillBlockingStatusMapper.cs`, `PlayerCharacter.cs`, `DragonCompanionRuntime.cs`, `MassacreField.cs`, `CoconutField.cs`, `SnowBallField.cs`, `GuildBossField.cs`, `MapInfo`, `MapInfo.fieldLimit`, `MapInfo.moveLimit`, `MapInfo.fieldType`, `FieldLimitType`, `FieldType`, `Skill/MobSkill.img`, `Map/Map1/109080000.img/coconut`, `Map/Map1/109060000.img/info/fieldType`, `Map/Map9/990000900.img/{info/fieldType,healer,pulley}`, `Map/Map9/980031100.img/noSkill/class`, `Map/Map2/200100001.img/noSkill/class`, `Map/Map9/922240000.img/noSkill/skill`, `Map/Map9/910320100.img/mobMassacre/disableSkill`, `Map/Map9/926010100.img/mobMassacre/countEffect/500/skillUse`, `Map/Map2/200110000.img/info/noCancelSkill`, `Map/info/moveLimit`, `MapInfo.vanishDragon`, `FieldType.FIELDTYPE_NODRAGON`, `Skill/211.img/skill/2111002/info`, `Skill/230.img/skill/2301002/info`, `Skill/2212.img/skill/22121000/info`, `Skill/2215.img/skill/22151001/info`, `Skill/430.img`, `Skill/431.img`, `Skill/432.img`, `Skill/433.img`, `Skill/434.img`, `UnitTest_MapSimulator/PlayerSkillStateRestrictionParityTests.cs` (`CUserLocal::Update`, `CUserLocal::IsSkillAvailable`, `CField::IsUnableToUseSkill`, `CField_Coconut::BasicActionAttack`, `CField_SnowBall::BasicActionAttack`, `CField_GuildBoss::BasicActionAttack`, `CField::IsMoveSkillOnly`, `CField::IsUnableToUseRocketBoost`, `CField::IsUnableToUseTamingMob`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::TryDoingSwallowAbsorb`, `CUserLocal::TryDoingMagicAttack`, `CAvatar::IsOnPlayingOneTimeAction`, `CDragon`, `get_skill_root_from_skill`) |

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Summoned action-loader / frame-build parity | The client-owned summon loader seam remains in `SkillLoader`, with the already-landed loader/frame-build parity still intact: summon frame building uses the client `120` ms delay fallback, per-frame `z`, `a0` / `a1`, authored `origin` / `lt` / `rb` metadata when wrapper-linked canvases publish it, reverse-playback duplication from the client-consumed reverse-style flag, standalone summon-owner root acceptance for real roots such as `Skill/3510.img/skill/35101005`, and wrapper pruning so root `icon*`, `action`, and root `effect/effect0` nodes do not pollute the playable door branch family. WZ was rechecked first in the mounted export for the same concrete layouts plus the mounted root-skill ownership surface that still feeds this seam: `Skill/3510.img/skill/35101005` still exposes wrapper nodes beside `cDoor`, `sDoor`, `oDoor`, `mDoor`, and `eDoor`; `Skill/501.img/skill/5010005/summon` still exposes authored custom action branches `{piratebless,rushBoom,cannonSlam,noiseWave,jump,hit1}` beside `attack1`-`attack4`; `Skill/434.img/skill/4341006/summon` still remains an effect/repeat-only layout while both `info/requireSkill = 4331002` and root `req/4331002 = 30` remain visible on that same mounted skill node; `Skill/3512.img/skill/35121012` still likewise publishes both `info/requireSkill = 35121005` and root `req/35121005 = 1`; and the satellite summon links at `Skill/3511.img/skill/35111009/info` and `Skill/3511.img/skill/35111010/info` still expose `affectedSkill = 35111001` as the visible WZ-side link rather than proving the hidden client `sSummonedUOL` authoring site. Client evidence stayed second: `CActionMan::GetSummonedProp` at `0x41b830` still resolves the precomputed `SKILLENTRY::GetSummonedUOL` value through the resource manager, `SKILLENTRY::GetSummonedUOL` at `0x417f30` still only returns `this->sSummonedUOL`, `CActionMan::LoadSummonedAction` at `0x423100` still indexes the recovered `s_sSummonedAction` table before building/caching frames, `CSummoned::PrepareActionLayer` at `0x749cd0` still retries only action `2` for move abilities `4/5` or action `0` otherwise before throwing when that retry list is empty, and the same getter now also rechecked as the asset base for `CSummoned::_LoadEffect` at `0x754050` plus `CSummoned::_UpdateEffect` at `0x754970` when the client resolves summon-owned `/effect`, `/repeat`, `/effect0`, and `/repeat0` layers from that precomputed UOL. This pass tightens the loader-side visible-link fallback again without widening runtime ownership: alongside the earlier empty-action retry/runtime fixes, `TryResolveClientSummonedUolProperty` now accepts client-UOL strings that normalize not only from mounted root forms such as `Skill.wz/3511.img/35111001` or `3511.img/35111001/summon`, but also from skill-id-root forms such as `35111001`, `35111001/summon`, `skill/35111001`, or `Skill/35111001/summon`, canonicalizes them onto the mounted `Skill/<job>.img/skill/<skillId>` path shape the simulator actually loads, and then re-enters raw summon-root detection on the resolved node instead of requiring the UOL to already land on an explicit summon-like branch. That means linked candidates that publish client-style owner paths at the mounted skill root can now still recover their playable summon node through the existing loader seam before the broader visible-link graph has to keep searching. Focused source coverage is now present in `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs` for both mounted-root and skill-id-root client-UOL normalization plus deduped nested reverse-parent traversal. The remaining client-owned gap is narrower and still concentrated on the hidden asset-resolution field itself: the simulator can now approximate more of the reachable visible graph (`affectedSkill`, `psdSkill`, reverse affected/passive parent discovery across linked candidates, dummy parents, requirements/req nodes, mounted root-form client-UOL strings, skill-id-root client-UOL strings, and any linked candidate that already publishes a client-style summon UOL string), but the client still consumes the precomputed `SKILLENTRY::sSummonedUOL` string directly; the exact authoring/population path for that field still needs a follow-up client-data pass before this row can be marked implemented, especially for cases where no visible WZ-side link surface exposes the summon owner at all. | Without this row, future summon work is biased toward packet/runtime fixes while leaving the client's actual frame-build owner implicit. Tracking the loader explicitly keeps summon parity anchored to the real asset-selection and frame-construction seam, and the stricter empty-action retry plus broader visible-link candidate graph now leave the remaining risk concentrated on the hidden summoned-UOL authoring path instead of on frame construction, action-table lookup, authored custom summon-action recovery, standalone-root pruning, or the previously narrower WZ-side source fallback chain. | summon action-loader layer (`SkillLoader`, `SkillData`, `SummonRuntimeRules`, `CActionMan::GetSummonedProp`, `CActionMan::LoadSummonedAction`, `SKILLENTRY::GetSummonedUOL`, `CSummoned::PrepareActionLayer`, `CSummoned::_LoadEffect`, `CSummoned::_UpdateEffect`, `CSummoned::Update`, `CSummonedPool::OnCreated`, `Skill/3510.img/skill/35101005`, `Skill/3511.img/skill/35111002/summon/attackTriangle`, `Skill/3511.img/skill/35111011/summon/stand`, `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3511.img/skill/35111009/info/affectedSkill`, `Skill/3511.img/skill/35111010/info/affectedSkill`, `Skill/3512.img/skill/35121012/{info/requireSkill,req/35121005}`, `Skill/3310.img/skill/33101005/repeat`, `Skill/434.img/skill/4341006/{summon/repeat,info/requireSkill,req/4331002}`, `Skill/3511.img/skill/35111001/summon/die1`, `Skill/501.img/skill/5010005/summon`, `Skill/1311.img/skill/13111004/summon/hit`, `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs`) |

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

This helper-owner seam is currently under-tracked by the backlog even though both the client and simulator already expose concrete ownership here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow-partner and mirror helper actors | The simulator now models the client ownership split instead of treating every clone-family helper as the same WZ-backed shadow-partner path. `SkillData` / `SkillManager` recognize `Mirror Image` (`4331002`) as a distinct helper owner, `PlayerCharacter` keeps a separate mirror runtime from the existing `special/*` shadow-partner actor, and the update/render loop now drives mirror helper visibility from player action changes with a dedicated `m_tStartMirror`-style start timestamp, a 200 ms client-shaped rel-move window, and client-backed suppression for ladder, rope, morph, taming-mob, hit, and dead states. WZ was rechecked first again for `skill/433.img/skill/4331002`: `action/0 = alert` remains present, `effect` remains present, and `special` remains absent, so Shadow Partner keeps using WZ-authored helper animations while Mirror Image continues to source the assembled avatar instead. IDA for `CUser::GetMirrorSrcLayer` still confirms the client mirror sources are the same five avatar layer handles in index order, while `CUser::PrepareMirrorActionLayer` still recreates each helper layer with `m_pLayerUnderFace` as the overlay parent regardless of which source plane fed that mirror layer, copies origin/position from the live source-layer handle, seeds `RelMove` from the live source-layer current time plus 200 ms, and only recreates the helper layer when the layer is missing or its flip diverges from `m_pLayerUnderFace`. `CUser::Update` then keeps the existing helper layer and inserts the current source-layer canvas into it instead of rebuilding the helper object per source change. `PlayerCharacter` now reflects more of that seam by routing every prepared mirror plane through a shared UnderFace overlay target, drawing the whole mirror helper package at the UnderFace insertion boundary, keeping mirror rendering on the prepared per-plane composed-snapshot path even when the current live source plane still matches, preserving the prepared layer recreate timestamp across same-action/same-facing source canvas refreshes, recording the source-layer current-time snapshot separately from the recreate timestamp, reusing the prepared render target whenever a refreshed same-facing snapshot still fits inside the existing texture, tracking the logical snapshot source rectangle separately from the reusable texture extent, preserving the prepared placement anchor across same-facing canvas refreshes even when the incoming canvas bounds grow and the simulator has to replace the backing render target, and now keeping a prepared mirror plane alive across same-facing empty-source gaps instead of eagerly clearing that plane the moment the live assembled source list goes empty. The existing gates remain in place: flip-only changes still force prepared-layer recreation, mirror suppression/back-offset selection still consumes the client raw-action buckets (`is_back_action`, `is_alert_back_action`, raw action `48`) before falling back to action-name heuristics, the mirror-wide `m_tStartMirror` analogue still only re-arms when the helper becomes visible or the observed player action actually changes, and per-plane render placement still stays on the prepared snapshot anchor until the simulator recreates that prepared mirror plane instead of silently rebasing each helper plane to the current live avatar bounds every draw or every same-facing canvas refresh. Focused regression coverage is now on disk in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` for same-facing empty-source persistence, layer transition start precedence, prepared source-layer timing preservation, prepared snapshot source-rect fallback, prepared placement-anchor preservation, and the shared UnderFace overlay seam. The remaining gap is narrower and still concentrated at the true live-layer-object boundary inside `CUser::PrepareMirrorActionLayer` / `CUser::Update`: the simulator still does not own shared `IWzGr2DLayer` mirror objects, true COM live-layer origin/current-time handles, true `InsertCanvas` reparenting against the live client layer graph, literal helper-object persistence independent of simulator render-target replacement, or the follow-up clone-helper behaviors that depend on those real client layer objects rather than simulator-owned prepared plane snapshots. | Clone-family jobs are not just tray icons or generic buff effects in the client; they own dedicated avatar-adjacent helper layers that participate in action preparation and per-frame user update. Tracking that seam explicitly keeps future parity work focused on helper-layer timing, ownership, and clone-family differentiation instead of accidentally treating shadow-partner and mirror-image visuals as solved just because the simulator already renders one WZ-backed helper actor | `PlayerCharacter.cs`, `CharacterAssembler.cs`, `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` (`skill/433.img/skill/4331002/action/0`, `CUser::PrepareActionLayer`, `CUser::PrepareShadowPartnerActionLayer`, `CUser::PrepareMirrorActionLayer`, `CUser::GetMirrorSrcLayer`, `CUser::Update`, `is_back_action`, `is_alert_back_action`) |

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow Partner action-loader / frame-build parity | The shadow-partner helper row remains a loader-owned parity seam rather than only a runtime/playback note. WZ was rechecked first again in the mounted export: `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, and `Skill/1411.img/skill/14111000/special/*` still publish the same helper branch families, `Skill/421.img/skill/4211008/special/shoot2/{0,1,2,3,4}` still shows the authored mixed direct-plus-linked frame order that the loader must preserve, the recovered non-ghost `action/0` raw-action names still resolve from mounted skill data (`alert2` / `alert3` / `alert4` / `alert5`, `swingO1`, `avenger`, `assaulter`, `prone2`, `savage`, `showdown`, `assassination`, `smokeshell`, `ninjastorm`, `vampire`), and the mounted `Character/00002000` export still rechecks as a usable loader source for hidden action rows such as `alert2`, `ladder2`, `rope2`, `avenger`, `assaulter`, `prone2`, `savage`, `showdown`, `assassinationS`, `smokeshell`, `ninjastorm`, `flyingAssaulter`, `stabD1`, `swingD1`, `swingD2`, `doubleSwing`, `tripleSwing`, and `shotC1`. Those mounted action rows do not just carry `action`, `frame`, and signed `delay`: some now rechecked rows also publish piece-owned `flip`, `move`, and `rotate` metadata, for example `Character/00002000/doubleSwing/*`, `Character/00002000/tripleSwing/*`, `Character/00002000/flyingAssaulter/*`, and `Character/00002000/shotC1/*`. Client evidence stayed second: the direct `CActionMan::LoadShadowPartnerAction` re-check at `0x423cf0` still shows cache lookup by `(skillId, action)`, action-table-owned named raw-branch precedence before plain numeric `get_action_name_from_code` fallback, hidden action-table piecing before plain raw-action fallback when `ACTIONDATA::bPieced` is populated, frame construction from the enumerated helper canvases, frame-owned canvas-property metadata reads, and mirrored interior-frame replay when the action-data replay flag is set, while `CActionMan::GetShadowPartnerProp` at `0x41ba60` still resolves the helper property through `SKILLENTRY::GetSpecialUOL` plus the resource-manager fetch and `CActionMan::Init` at `0x41beb0` still populates the character action table from `Character/00002000`. The simulator still keeps that ownership in the existing loader seam without widening runtime state: `ShadowPartnerClientActionResolver` still preserves animation-level `PositionCode` metadata when it builds loader-owned helper playback rows, `SkillLoader` still preserves the same frame-build metadata when it synthesizes helper rows, `SkillLoader.EnumerateShadowPartnerActionFrameChildrenInClientOrder` still keeps authored helper-canvas child order while accepting linked-canvas cases after the client-shaped `IWzCanvas` filter, and the loader still builds named-vs-numeric storage plans so numeric helper keys store the client action name while authored named branches win. This run narrows the loader-owned gap again in the mounted-row path itself: `SkillLoader.TryParseShadowPartnerClientActionPieces` now mirrors the client-owned delay shaping more closely by converting signed mounted `delay` payloads to absolute per-piece playback durations, and it also preserves mounted piece-level `flip` bits so helper rows such as `doubleSwing`, `tripleSwing`, and `flyingAssaulter` no longer lose that loader-owned transform when cloned from authored helper canvases. `ShadowPartnerClientActionResolver.TryBuildPiecedShadowPartnerActionAnimation` now applies that piece-level flip when it clones source frames while still preserving the existing authored frame metadata and animation-level `PositionCode`. Focused regression coverage in `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs` now covers the mounted-row parser seam for absolute delay shaping plus piece-level flip preservation in addition to the earlier delay-override and exact source-frame-slot coverage. The earlier implemented loader behavior remains in place for linked-`special` source resolution, metadata-owned frame bounds, replay-tail handling for the currently confirmed replay-table actions `stand1`, `stand2`, and `alert`, mounted `Character/00002000` source-frame-slot synthesis for the currently recovered hidden rows, and fallback synthesized helper rows when a mounted client action row is still unavailable while preserving authored branches with the same name. Remaining gap is narrower but still real, and is now concentrated at any hidden Shadow Partner rows that still do not surface through the mounted `action/0` plus `Character/00002000` subset, the mounted per-piece `move` / `rotate` and any other action-table fields beyond the now-modeled `action` / `frame` / absolute `delay` / `flip` plus the already-modeled replay flag, whether later client versions or other data sets introduce helper UOLs that do not land on this build's linked-`special` seam, and whether later indexed alert-family or other hidden helper rows ever become loader-owned in some other client build rather than staying outside this mounted surface. The latest direct loader re-check still narrows one prior uncertainty: this build's `LoadShadowPartnerAction` does not show a separate total-delay shaping path for Shadow Partner helper rows; replay-tail ownership remains tied to the action-data replay flag already represented by the confirmed replay-tail set. | Without this row, the backlog still overstates clone-helper parity by treating Shadow Partner as if it were only a `CUser`/`PlayerCharacter` timing and placement problem. The client keeps a dedicated `CActionMan` loader for helper actions, so parity still depends on a named seam for cache keys, branch lookup, numeric-slot fallback ownership, hidden action-table piecing, frame construction, metadata retention, per-piece delay shaping from the client action table, per-piece flip retention where mounted rows publish it, and replay-tail ownership rather than only on helper runtime playback. | shadow-partner action-loader layer (`CActionMan::GetShadowPartnerProp`, `CActionMan::LoadShadowPartnerAction`, `CActionMan::Init`, `get_action_name_from_code`, `ACTIONDATA::bPieced`, simulator seams: `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `RemoteUserActorPool.cs`, `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs`, `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, `Skill/1411.img/skill/14111000/special/*`, `Skill/421.img/skill/4211008/special/shoot2/{0,1,2,3,4}`, `Skill/411.img/skill/{4111001,4111003,4111005,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121003,4121004,4121008}/action/0`, `Skill/420.img/skill/{4201002,4201003,4201005}/action/0`, `Skill/421.img/skill/{4211002,4211003,4211005,4211006,4211007}/action/0`, `Skill/422.img/skill/{4221001,4221003,4221006,4221007}/action/0`, `Skill/1410.img/skill/14101006/action/0`, `Skill/1411.img/skill/{14111001,14111002}/action/0`, `Character/00002000/{alert2,alert3,alert4,alert5,ladder2,rope2,avenger,assaulter,prone2,savage,showdown,assassination,assassinationS,assassinations,smokeshell,ninjastorm,vampire,stabD1,swingD1,swingD2,doubleSwing,tripleSwing,shotC1,flyingAssaulter}`) |

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Time Bomb, Vengeance, and ExJablin packet-application parity | The simulator still owns this `CUserLocal::OnPacket` family inside the existing local-utility packet seam, and this pass closes the missing checked-in regression-coverage part of the row without moving ownership elsewhere. WZ was rechecked first against the mounted export currently exposed through wzimg: `String/Skill.img` still resolves `4341003` as `Monster Bomb` with the `explosion occurs 3 seconds after the charm is activated` description fragment, `31101003` as `Vengeance`, and `4120010` as `Expert Throwing Star Handling` with the `the next attack will always be a Critical Attack` fragment, while `3120010` still resolves to `Ultimate Inferno` instead of a WZ-side `Vengeance` alias. `Skill/434.img/skill/4341003` still publishes the authored Time Bomb `action`, `prepare`, and `special` branches together on the same skill node, and the mounted export still exposes only that one authored `4341003` `Monster Bomb` node for this family. `Item/ItemOption.img` still exposes the client-shaped Time Bomb invincibility option family `020366`, `030366`, `040366`, `030371`, and `040371` under option type `52`, while other option-type-`52` rows such as `020396` and `040111` remain unrelated non-invincibility options. Client evidence was rechecked second: `CUserLocal::OnTimeBombAttack` still decodes `(skillId, timeBombX, timeBombY, impactPercent, damage)`, routes the packet origin into `TryDoingMeleeAttack`, restores the `1500 ms` base hit period before coat-option merges, gates impact on nonzero `nUserImpactDeg`, calls `CUser::MakeIncDecHPEffect(this, -nDamage, 0)`, and then sets the hit emotion/impact; `CUserLocal::OnVengeanceSkillApply` still decodes exactly one `Int32` and only branches on legacy packet skill id `3120010`; `CUserLocal::OnExJablinApply` still reads no payload and only arms `m_bNextShootExJablin = 1`. The landed seam still keeps packet ids `268` / `271` / `272` plus aliases, exact Time Bomb/Vengeance/ExJablin payload validation, signed Time Bomb damage, legacy-only Vengeance admission, nonzero Time Bomb impact gating, packet-origin target selection, the dedicated `SkillManager.TryApplyPacketOwnedTimeBombAttack` branch, the WZ-backed visible-potential item-option merge for `CharacterPart.ItemOptionIds`, and the narrow `MakeIncDecHPEffect` miss/heal mirroring. Focused regression coverage now really lives on disk in `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs` and covers the family alias catalogs, authored Time Bomb candidate filter/order helpers, dedicated-family fallback ordering, exact Time Bomb/Vengeance/ExJablin payload-shape gates, legacy-only Vengeance admission, signed Time Bomb damage / impact interpretation helpers, authored Time Bomb invincibility option filtering, potential-line inference, Time Bomb-only and general visible-potential merge behavior, and the client-style equip-level / invincibility-duration helpers that this row depends on. The remaining parity gap is narrower but still real: the simulator still limits unknown Time Bomb fallback to the sole WZ-authored candidate with the expected local branches, but the exact legacy numeric packet-to-WZ Time Bomb mapping is still unconfirmed by direct client evidence, so the final bridge from an unrecognized opcode-`268` skill id into `4341003` remains an inference from the dedicated opcode plus the unique authored `Monster Bomb` match rather than a client-confirmed alias id. | These handlers matter because they bypass the normal local input/cast path even though the client gives them their own dispatcher and follow-up state. The simulator keeps a concrete owner for those packets, keeps the shared alias seam anchored to WZ first and client second, no longer accepts trailing bytes that the client packet readers would leave unread, no longer accepts non-client Vengeance packet ids, no longer treats only positive Time Bomb impact values as hit reactions, no longer clamps away signed Time Bomb damage payloads before the local reaction branch, no longer drops the zero/negative `MakeIncDecHPEffect` cases that hang off the same decoded damage field, still resolves visible potential lines through the WZ-authored `ItemOption.img` template/level seam instead of limiting `CharacterPart.ItemOptionIds` population to the confirmed Time Bomb invincibility family, and now also has a checked-in row-specific regression file over the alias, fallback, decode, signed-damage, and option-id helpers that keep this packet family stable. The row stays `Partial` because the remaining uncertainty is now concentrated on the exact legacy Time Bomb alias number rather than on missing packet-family ownership, missing decode, missing nonzero impact behavior, missing signed-damage consumption, missing target-priority coverage, missing `ItemOption.img` fallback coverage, missing general visible-potential option-id population, or missing targeted regression coverage around the current best-effort bridge from an unknown Time Bomb packet id into the confirmed `Monster Bomb` family seam. | packet-owned local skill-application layer (`CUserLocal::OnPacket`, `CUserLocal::OnTimeBombAttack`, `CUserLocal::OnVengeanceSkillApply`, `CUserLocal::OnExJablinApply`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::ApplyInvicibleOption`, `CUser::MakeIncDecHPEffect`, `CUserLocal::SetImpact`, `MapSimulator.PacketOwnedUtilityParity`, `LocalUtilityPacketInboxManager`, `SkillManager`, `PlayerCharacter`, `CharacterPart`, `ItemUpgradeUI`, `Character/00002000.img`, `String/Skill.img`, `Skill/434.img/skill/4341003`, `Item/ItemOption.img`, `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs`) |

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Combo counter / combo-command packet parity | `MapSimulator.PacketOwnedComboParity` now owns the simulator-side `OnIncComboResponse` bridge instead of leaving combo implied by the generic temporary-stat tray. The simulator decodes the packet-owned combo count from the combo inbox/chat transport, stores the owner count/timestamp, resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them on the same five-second alpha window, applies the client-matching fly-origin amplitude split across the least-significant digit run, and conditionally spawns the Aran combo-command attack/buff overlays with `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` first and WZ-backed named fallbacks for exports that only ship `ComboSmash`, `ComboDrain`, `ComboFenrir`, `ComboTempest`, and `ComboBarrier`. The row no longer depends on the generic `Combo` temporary-stat heuristics in `SkillManager`; it has a dedicated packet-owned HUD/effect owner, content load path, update lifetime, and draw path in `HaCreator/MapSimulator`. | If combo stays implied only by the temporary-stat tray, the backlog will overstate skill parity while missing the client's actual packet-owned combo counter, visible combo HUD/effect lifetime, and the extra combo-command visuals that hang off learned Aran skills. Tracking it explicitly keeps future work anchored to the real packet and draw owners instead of assuming the generic buff/cooldown surfaces already cover combo gameplay feedback. | packet-owned combo HUD/effect layer (`CUserLocal::OnPacket`, `CUserLocal::OnIncComboResponse`, `CUserLocal::DrawCombo`, `Effect/BasicEff.img/Combo`, `Effect/BasicEff.img/ComboCommand`) |

### 7. Shared skill-use animation-owner parity discovered by targeted `CAnimationDisplayer` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shared skill-use animation-owner parity | `MapSimulator.HandlePlayerSkillCast` still no longer stands alone as the only local ingress for this shared owner. The same animation-displayer seam continues to absorb the client-facing prepared-skill route exposed around `CUser::ShowSkillPrepare`: local prepared starts synthesize shared-owner requests from `prepare` or immediate `keydown` admission, local prepared release feeds `keydownend` back through the same branch loader, remote prepared-start packets honor their explicit branch names instead of falling back to generic `effect*` discovery, and remote prepared-clear packets emit the matching `keydownend` owner effect before the prepared state is dropped. The seam still tracks a live owner position/facing relationship, applies the client `1000 * (speed + 10) / 16` delay-rate override for the remote melee presentation route, preserves structured `LoadLayer`-style branches as avatar-bound transient animations, and carries caller-owned origin offsets plus fixed-vs-owner-followed facing for the one-time fallback and transient avatar branch loaders. This pass narrows another local caller gap without adding a parallel branch path. WZ was rechecked first: `Skill/434.img/skill/4341003/special` and `Skill/434.img/skill/4341007/special` still exist in the mounted export, the export still authors `specialAffected` under `Skill/230.img/skill/2301002`, `Skill/532.img/skill/5320007`, and `Skill/572.img/skill/5720005`, and `Skill/122.img/skill/1220006` still authors both `special` and `info/condition = damaged`. That last surface matters because the preexisting simulator gate only recognized stance-labeled damage callbacks instead of the wider authored damaged-condition family that still routes through the same shared owner on the client. Client evidence stayed second: `CUser::ShowSkillSpecialEffect` still creates either a grenade-position vector or an explicit `(nTimeBombX,nTimeBombY)` vector, passes `m_pLayerUnderFace` as overlay, uses the caller delay rate, keeps `z = 3`, uses `nLast = 0x7fffffff`, and leaves auto-flip off before calling `CAnimationDisplayer::Effect_SkillUse`; the wider xref set into `CUser::ShowSkillEffect` / `CUser::ShowSkillSpecialEffect` still keeps explicit non-melee callers such as `TryDoingShootAttack`, `TryDoingMagicAttack`, `DoActiveSkill_Summon`, `DoActiveSkill_SmokeShell`, `DoActiveSkill_MobCapture`, `DoActiveSkill_Heal`, `DoActiveSkill_TownPortal`, `DoActiveSkill_OpenGate`, `DoActiveSkill_Flying`, `DoActiveSkill_BoundJump`, `DoActiveSkill_WeaponBooster`, `DoActiveSkill_Aura`, `TryRegisterTeleport`, `TryDoingSmoothingMovingShootAttackPrepare`, `OnKeyDownSkillEnd`, `HandleCtrlKeyDown`, `OnMovingShootAttackPrepare`, and `OnAttack`, and the same `ShowSkillSpecialEffect` xref set still includes local/remote damage handlers such as `CUserLocal::SetDamaged` and `CUserRemote::OnHit`. The simulator now mirrors more of that ownership split in the current seam: generic local client-effect callbacks still feed the shared `SkillUseEffectRequest` surface, the packet-owned local Time Bomb route still uses that recovered request seam, the local attack-payload owner still reuses it whenever runtime code already has an explicit caller-supplied origin or preferred target point for a skill whose WZ node authors `special`, the teleport-linked affected-skill body-attack helper still reuses the same explicit-`special` request builder instead of bypassing the shared owner when it fires outside `ExecuteAttackPayload`, remote grenade packets still feed the same presentation owner through `RemoteUserActorPool.TryApplyThrowGrenade`, and local damage callbacks now also raise the shared-owner `special` request for any active buff whose WZ skill actually authors `info/condition = damaged` plus a `special` branch instead of limiting that seam to stance-only temporary-stat heuristics. This pass keeps the same seam for the authored `specialAffected` family as well: `SkillLoader` loads `specialAffected`, local heal admission raises a shared-owner request for the authored branch when that surface exists, and local buff admission requests `affected`, `affected0`, and `specialAffected` together through the shared animation-displayer branch loader instead of silently dropping the one-time authored branch. Existing timeout/recovery client-effect requests still feed their requested skill id back through the same branch loader, branches with authored layer roots still use `PlayerCharacter.ApplyTransientSkillAvatarEffect` for the local owner and matching transient state on `RemoteUserActorPool` for remote owners, and simple non-layered branches still fall back to one-time DX registration with the requested owner relationship. Focused regression coverage now lives in `UnitTest_MapSimulator/SharedSkillUseAnimationOwnerParityTests.cs` for damaged-condition candidate gating and the client-style `prop` roll threshold used by that new local damage-reactive branch admission. The remaining gap is narrower and still real: the simulator still does not route every explicit non-melee `CUser` caller from the client xref set into this surface yet, especially local `ShowSkillEffect` / `ShowSkillSpecialEffect` callers that do not currently expose their authored branch choice or explicit vector through the local runtime, it still does not mirror the full overlay-parent chaining and auto-flip ownership that client `LoadLayer` / `RegisterOneTimeAnimation` can apply for those callers beyond the current fixed-origin / owner-follow / facing-follow options, and the remote damaged-condition path recovered through `CUserRemote::OnHit` is still only partially mirrored because remote hit handling does not yet resolve the full client-owned damaged-trigger skill selection and chance gate through this same owner. | Naming and partially implementing this owner keeps cast-effect parity from being overstated once gameplay logic works: the client still composes many visible cast and reactive layers through one shared animation manager, and this pass widens the simulator seam from cast-only, remote-melee-only, prepared start/end, buff-start, generic client-effect coverage, local attack-payload origin overrides, and teleport-linked affected-skill helpers into a shared local/remote request surface that now also covers WZ-authored damaged-condition `special` reactions on the local owner instead of leaving those callbacks stuck behind stance-only heuristics. The row stays partial because the simulator still lacks several recovered caller sites, still approximates some `LoadLayer` / `RegisterOneTimeAnimation` ownership knobs, and still has a remaining remote damage-reactive gap, but the risk is now more concentrated on missing caller routing and deeper overlay/auto-flip selection than on the absence of a shared owner path for authored local damage-triggered `special` branches. | shared skill-use animation layer (`CAnimationDisplayer::Effect_SkillUse`, `CAnimationDisplayer::LoadLayer`, `CAnimationDisplayer::RegisterOneTimeAnimation`, `CUser::ShowSkillEffect`, `CUser::ShowSkillSpecialEffect`, `CUserLocal::SetDamaged`, `CUserRemote::OnHit`, `SkillUseEffectRequest`, `MapSimulator.AnimationDisplayerParity`, `RemoteUserActorPool.TryApplyThrowGrenade`, `Skill/434.img/skill/4341003/special`, `Skill/434.img/skill/4341007/special`, `Skill/122.img/skill/1220006/special`, `Skill/230.img/skill/2301002/specialAffected`, `Skill/532.img/skill/5320007/specialAffected`, `Skill/572.img/skill/5720005/specialAffected`, `UnitTest_MapSimulator/SharedSkillUseAnimationOwnerParityTests.cs`) |

### 8. Bullet animation-owner parity discovered by follow-up `CAnimationDisplayer` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Normal, magic, and afterimage projectile animation owner | WZ was rechecked first on the authored projectile surfaces this row owns: `Skill/200.img/skill/2001004/ball` and `Skill/230.img/skill/2301005/ball` still publish explicit spell-ball frame sequences with authored `origin` and `delay`, `Skill/434.img/skill/4341003/flipBall` still exposes the flipped projectile branch, and representative normal/cash ammo nodes still publish separate bullet art on `02060000/bullet` and `05021000/bullet`. Client evidence stayed second: `CAnimationDisplayer::RegisterBulletAnimation` at `0x455410` still allocates `NormalBullet` with `tStart`, `tEnd`, source/destination points, target vector, `z`, bullet-effect UOL, weapon id, and bullet item id before inserting it into `m_Bullets`; `CAnimationDisplayer::RegisterMagicBulletAnimation` at `0x455570` still performs the parallel `MagicBullet` insert only when the ball UOL string is non-empty; and `CAfterImageBullet::Update` at `0x477aa0` still clones the current bullet layer into a child repeat layer, relocates origin and alpha vectors, copies the source canvas, and registers repeat animation state. The simulator still mirrors that owner split inside the existing projectile seam instead of widening gameplay ownership again: `SkillManager` keeps a first-class bullet-animation owner list separate from `_projectiles`, activates queued owners only after the fire-time ammo/origin refresh path runs, updates owner positions from the live projectile while gameplay is active, falls back to the registered source/destination timeline when the gameplay projectile drops out, and draws owner animations from that container before falling back to ownerless or exploding projectile sprites. This pass narrows the registration-side approximation again. Owner animation resolution now consumes the registered bullet-effect UOL directly through the shared skill animation loader, so `ball` and `flipBall` branches remain the presentation source for both magic bullets and normal-bullet fallback paths instead of being reduced to passive bookkeeping while the owner container still records the client-shaped weapon id and bullet item id. Item bullet visuals still load through the same shared animation loader, so representative normal/cash ammo surfaces keep authored animation-level metadata such as root `z`, and magic-bullet afterimage snapshots still preserve the current frame's interpolated alpha ramp when each repeat layer is captured. Focused regression coverage in `UnitTest_MapSimulator/BulletAnimationOwnerParityTests.cs` now also locks the skill-UOL path parsing used by that owner-resolution path in addition to the earlier animation-level `z` fallback and frame-alpha-aware afterimage snapshots. Remaining gap: the simulator still does not reconstruct the client's deeper `CBullet` layer objects or their weapon-driven specialization rules exactly, so native normal-bullet variant selection from weapon item id plus the full `CAfterImageBullet::Update` child-layer origin/alpha/clockwise-bound adjustments are still approximated rather than mirrored 1:1. | Projectile presentation is no longer overstated by gameplay parity alone: normal bullets, magic balls, queued follow-up ammo visuals, and magic afterimage trails now live on a dedicated container owner with their own lifetime instead of disappearing as soon as gameplay state advances, and the registered effect-UOL path now participates directly in owner-side draw selection instead of being dropped after registration. This row stays `Partial` because the remaining mismatch is now concentrated in deeper native `CBullet` specialization and repeat-layer transform details rather than in missing owner separation, missing item-bullet root metadata retention, ignored authored `ball`/`flipBall` UOLs, or completely flat afterimage-alpha snapshots. | projectile animation layer (`CAnimationDisplayer::RegisterBulletAnimation`, `CAnimationDisplayer::RegisterMagicBulletAnimation`, `CAfterImageBullet::Update`, `CBullet`, `CAnimationDisplayer::NormalBullet`, `CAnimationDisplayer::MagicBullet`, `SkillManager.SpawnProjectile`, `SkillManager.RefreshQueuedProjectileFireTimePresentation`, `SkillManager.DrawProjectiles`, `SkillManager.ResolveBulletAnimationOwnerKind`, `SkillManager.ResolveBulletAnimationInterpolatedPosition`, `SkillLoader.LoadSkillAnimationByNormalizedPath`, `UnitTest_MapSimulator/BulletAnimationOwnerParityTests.cs`, `Skill/200.img/skill/2001004/ball`, `Skill/230.img/skill/2301005/ball`, `Skill/434.img/skill/4341003/flipBall`, `02060000/bullet`, `05021000/bullet`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, movement-coupled shoot families, and the shared `Effect_SkillUse` cast-animation owner rather than extending the generic cast path indefinitely.
3. Summon actor/runtime pass:
   Close the missing `CActionMan::LoadSummonedAction` loader seam together with the packet-owned `CSummonedPool` gaps and the `CSummoned::Update`-owned movement, touch-damage, Tesla-state, and position-sync gaps instead of only extending attack metadata and per-skill summon heuristics.
4. Clone-helper parity pass:
   Tighten the dedicated `CUser::PrepareActionLayer` / `CUser::Update` helper-layer seam for shadow-partner and mirror-image families instead of leaving clone behavior folded into generic buff or summon work.
5. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
6. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
7. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
