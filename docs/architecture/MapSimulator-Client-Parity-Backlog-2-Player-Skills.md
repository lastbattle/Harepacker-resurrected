# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`
- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Player skill execution parity

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`

Notes:
`CUserLocal::Update` at `0x937330` orchestrates several of these paths and confirms that key-down gauge updates, queued follow-up attacks, prepared-skill execution, repeat-skill handling, teleport, rush, flying-rush, rocket-booster start/end handling, movement-coupled shoot branches, timer-driven cancellations, and status-bar feedback are coordinated in one local-player update loop rather than isolated one-off handlers. A second IDA pass also showed that summon parity splits across both the summon actor and its packet-owned pool: `CSummoned::Update` drives summon actor motion, active/passive vec-ctrl updates, body-contact damage checks, assist-type dispatch, Tesla Coil state handling, self-destruct timing, and final world-position sync rather than only the attack helper branches, while `CSummonedPool::OnPacket` dispatches created/removed/move/attack/skill/hit packets and `CSummonedPool::OnCreated` ties each summon to its owning user, skill id, level data, and local-user registration path.

### 2. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::Update` at `0x8fb8d0`

Notes:
The targeted re-check surfaced a client-owned helper seam that the backlog was not tracking explicitly. IDA decompilation shows `CUser::PrepareActionLayer` calling `CUser::PrepareShadowPartnerActionLayer` and `CUser::PrepareMirrorActionLayer` immediately after the main avatar action layer is prepared, which means clone-style helper visuals are owned by the user action-layer path rather than by the generic buff tray alone. The same `CUser::Update` re-check also exposes mirror-timing state (`m_tStartMirror`) alongside the broader avatar-update pipeline, which is enough evidence to keep shadow-partner and mirror helper behavior as a dedicated parity bucket instead of leaving it implicit inside generic skill or temporary-stat rows.

### 3. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another client-owned skill family that was not represented anywhere in the backlog text. `OnTimeBombAttack` is not just a generic damage callback: the decompile shows it decoding a skill id plus attack/action data, resolving the local skill level, calling `TryDoingMeleeAttack`, then applying hit-period/invincibility and impact side effects before forcing the avatar hit reaction. `OnVengeanceSkillApply` is also more specific than a plain temporary-stat refresh because it gates on skill id `3120010` and then calls `DoActiveSkill_MeleeAttack` through the local skill runtime, while `OnExJablinApply` arms the `m_bNextShootExJablin` flag for the next ranged-shot branch rather than behaving like a generic buff bit. That is a dedicated packet-owned skill-application seam between the broad cast/update families and the existing temporary-stat or helper rows.

### 4. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`

Notes:
The same targeted `CUserLocal::OnPacket` scan surfaced another local skill owner that the backlog still was not naming. `OnIncComboResponse` is very small but concrete: it decodes the server-authored combo count, stores it in `m_nCombo`, stamps `m_tLastSetCombo`, and immediately routes through `DrawCombo`. The `DrawCombo` decompile then shows that combo is not just another temporary-stat label. The client resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them alive on a five-second alpha window, applies fly-origin motion to the combo vectors, and conditionally spawns extra `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` layers when the corresponding Aran combo-command skills are learned. A repo search across `HaCreator/MapSimulator` still only surfaced the generic `Combo` temporary-stat labeling in `SkillManager` and did not surface any simulator-side combo packet owner, combo HUD/effect loader, or `ComboCommand` runtime, so this remains a real missing parity bucket rather than stale wording.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Player skill execution parity

The simulator has a generic skill runtime. It does not yet mirror the client's named `CUserLocal` behavior families.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Generic skill casting | The simulator now loads the full player skill catalog from `Skill.wz`, learns active skills into the runtime, supports direct cast invocation from the skill window in addition to hotkey casting, keeps GM/SuperGM skill-book resolution compatible with both `900.img` and `910.img` layouts while focusing those jobs on their own job-book window instead of the global advancement catalog, evaluates Big Bang `common` formulas in IMG-mode data sets so skills like `Power Strike` are treated as castable attacks instead of passive placeholders, mirrors skill effect anchoring/hitbox fallback by facing direction for asymmetric melee effects, sanitizes unsupported SpriteFont glyphs in the skill window so WZ-backed names no longer crash row rendering, and resolves cast SFX from `Sound/Skill.img` so skill use now plays the corresponding `Use` or fallback attack sound through the simulator audio path | This closes the previous "can only use the current job's limited skill book" runtime/UI gap and keeps special-job skill tabs populated and executable across client data variants | `SkillManager.cs`, `SkillLoader.cs`, `SkillUIBigBang.cs`, `MapSimulator.cs`, `UIWindowLoader.cs` |
| Implemented | Buff stat application | Buff logic now applies `PAD`, `PDD`, `MAD`, `MDD`, `ACC`, `EVA`, `Speed`, and `Jump` directly to the player build and cleanly removes them when buffs expire or refresh | Active buff skills now change simulator stats and movement-relevant values instead of only a partial subset | `SkillManager.ApplyBuffStats` |
| Partial | Buff icon / temporary-stat UI parity | Active buffs still drive the WZ-backed status-bar tray with live duration countdowns sourced from `UI/BuffIcon.img`, the tray still supports client-style right-click cancellation directly from the live buff list, `UILoader` still normalizes and loads the full `BuffIcon.img` icon surface, and the simulator still consumes the authored `name` strings plus `buff/*` traversal order from that same catalog so the nine observed stat-family icons (`incEVA`, `incPAD`, `incPDD`, `incMAD`, `incMDD`, `incACC`, `incCraft`, `incJump`, `incSpeed`) keep their WZ-authored display names and ordering instead of a duplicated simulator-only table. The broader non-`BuffIcon.img` family catalog still resolves through the recovered v95 `SecondaryStat` layout, and the tray still sorts each buff by the earliest resolved client-backed lane it actually publishes while preserving the named support-family owner for tooltip text and family display. This run closes another WZ-backed payload hole inside that same seam: WZ inspection now confirms later skill data also authors `ignoreMobDamR`, and `SkillLoader` plus `SkillManager` now fold that field into the existing `Ignore DEF` family path instead of only recognizing `ignoreMobpdpR`. That means `Ignore DEF` buffs can now stay visible, keep the authored family owner, and participate in authored temporary-stat ordering even when the loaded skill data only publishes the later `ignoreMobDamR` payload. Focused regression coverage is now on disk in `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` for `ignoreMobDamR` loader fallback, `ignoreMobpdpR` precedence when both ignore fields exist, and authored family ordering when `ignoreMobDamR` appears before other buff payloads. The remaining gap is narrower but still real: exact tray order is client-backed for the WZ-authored icon families plus the recovered v95 `SecondaryStat` slice, but some families still have no direct recovered v95 temp-stat owner or still appear to depend on runtime-owned grouping/icon state beyond `UI/BuffIcon.img` and the currently parsed skill metadata, especially `Boss Damage`, some generic reward/offense cases that are not explicit in the recovered v95 surface, and other later percent-style families whose exact client placement or icon choice is still not observable from the current WZ plus recovered-client evidence | Buff-heavy jobs are visible on the HUD, can be cleared without waiting for expiry, and no longer collapse as often into a fully generic tray entry while tooltip text and family ownership stay closer to the WZ stat-family surface. The known nine `BuffIcon.img/buff/*` families still use the WZ-authored names and family order directly, Big Bang-era payload families such as `mhpR` / `mmpR`, `pddR` / `mddR` / `accR` / `evaR`, `damR` / `indieDamR`, `criticaldamageMin` / `criticaldamageMax`, `expR` / `dropR` / `mesoR`, `bdR`, and `ignoreMobpdpR` still stay on their resolved tray lanes without depending on tooltip text heuristics first, and this pass extends that same WZ-backed seam so later-authored `ignoreMobDamR` buffs now land on the `Ignore DEF` family instead of silently falling back to a generic tray entry. Named multi-stat support buffs still keep support-family ownership while sorting beside the earliest client-backed lane they publish, rather than drifting because of a family-only fallback. The remaining gap is now concentrated on exact client lane/icon ownership for the families that still are not directly observable from `UI/BuffIcon.img` plus the recovered v95 `SecondaryStat` surface alone | `SkillManager.ActiveBuffs`, `SkillManager.ConfigureBuffIconCatalog`, `SkillManager.GetStatusBarBuffEntries`, `SkillLoader.CreateLevelData`, `SkillLoader.NormalizePassiveStatAliases`, `RemoteAffectedAreaSupportResolver.cs`, `StatusBarUI.cs`, `UILoader.cs`, `MapSimulator.Content.cs`, `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs`, `docs/GMS v95.h` (`UI/BuffIcon.img`, `UI/BuffIcon.img/united/buff`, `UI/BuffIcon.img/united/anniversary`, `Skill/*/common/indiePad`, `Skill/*/common/indieMad`, `Skill/*/common/indieAcc`, `Skill/*/common/indieEva`, `Skill/*/common/indieSpeed`, `Skill/*/common/indieJump`, `Skill/*/common/indieMhp`, `Skill/*/common/indieMmp`, `Skill/*/common/indieAllStat`, `Skill/*/common/pddR`, `Skill/*/common/mddR`, `Skill/*/common/accR`, `Skill/*/common/evaR`, `Skill/*/common/asrR`, `Skill/*/common/terR`, `Skill/*/common/criticaldamageMin`, `Skill/*/common/criticaldamageMax`, `Skill/*/common/damR`, `Skill/*/common/indieDamR`, `Skill/*/common/expR`, `Skill/*/common/dropR`, `Skill/*/common/mesoR`, `Skill/*/common/bdR`, `Skill/*/common/ignoreMobpdpR`, `Skill/*/common/ignoreMobDamR`, `Skill/131.img/1311008/common/str`, `Skill/312.img/3121007/common/dex`, `Skill/322.img/3221006/common/dex`, `Skill/*/info/switchDamtoMP`, `Skill/*/info/invincible`, `SecondaryStat`, `CUIStatusBar::Draw`, `CUserLocal::Update`) |
| Partial | Melee / ranged / magic resolution | The runtime now preserves WZ `lt/rb` attack bounds, resolves melee and magic targets with separate ordering rules, gives projectile skills family-specific collision ordering plus homing steering, keeps homing active even when WZ `ball` data also marks the projectile as exploding, detonates WZ `ball/explode` projectiles on impact so nearby mobs within `explodeRadius` are resolved under the same effective target cap, respects the effective projectile hit cap from both `level/mobCount` and `ball/mobCount`, routes WZ `info/type` through the skill-family classifier so no-ball spell and shoot families no longer collapse into melee resolution by default, now also treating client-confirmed shoot-family `info/type=52` skills such as Aran `21120006` as ranged even when WZ omits a `ball` node, routes WZ `info/type=32` plus `info/massSpell=1` spell families such as Mage `Slow` / `Seal` back onto the magic lane instead of leaving those no-ball area spells on melee defaults, and now also keeps the client-confirmed Outlaw no-ball shoot-family helpers `5211004` / `5211005` on the ranged lane despite their melee-looking WZ `info/type=1`, while the shared shoot-family resolver also carries `5211006` so the loader and runtime use one client-backed table instead of drifting apart. Direct no-projectile ranged skills still get their own fallback hitbox and target ordering instead of reusing the melee branch, route through the client-shaped `DoActiveSkill_ShootAttack` fallback split so melee-first families probe the close-range lane before falling back to ranged resolution while the client-confirmed direct-shoot families stay on the shoot lane, preserve the special client one-offs where Gunslinger `Double Fire` (`5001003`) and Outlaw `Triple Fire` (`5210000`) still attempt the melee lane before falling back to shoot despite otherwise living in the shoot-family helper table, and thread that same melee-first-vs-shoot preference through deferred moving-shoot preferred-target selection instead of always snapshotting a ranged-only target. Projectile families still route WZ `info/rectBasedOnTarget=1` through an additional target-anchored rectangle after first impact so client rect-attack families such as `3001004`, `3101005`, and `5201002` no longer depend on raw projectile overlap alone, re-resolve chained follow-up targets from the live impact/serial origin using the WZ `range` bounce radius plus nearest-neighbor chaining instead of only replaying the initial overlap snapshot, stage multi-shot projectile release from WZ `ballDelay` / `ballDelayN` timing instead of front-loading every projectile on cast start, assign WZ `info/multiTargeting=1` projectile volleys to distinct live targets inside the authored ranged hitbox before spawn so skills such as `2301005`, `3001004`, `5301000`, and `20021166` no longer fire every shot through the same generic spread-first path when valid separate targets already exist, derive projectile collision bounds from the live WZ ball frame size and origin while sweeping those bounds across the projectile's previous-to-current travel step so fast or large projectiles no longer rely on a single centered point-instant overlap check, thread per-target client damage override/decay branches through the live hit order so `Arrow Bomb: Bow` (`3101005`) uses its WZ `x` damage lane instead of the generic `damage` fallback, WZ-published x-based chain falloff still applies where the skill data actually publishes that decay value, and the remaining client-owned `SKILLENTRY::AdjustDamageDecRate` fixed tables now also drive `Energy Orb` (`5121002`), `Iron Arrow: Crossbow` (`3201005`), `Piercing` (`3221001`), `Ricochet` (`33101001`), and `Blade Fury` (`4341004`) through their exact client multipliers instead of flattening those families back onto one generic damage percent. This run also mirrors more of the client-owned shoot-entry precheck and ammo lane: ranged attack casts still validate the expected shooting-weapon family before firing, still skip that weapon gate for the confirmed client exceptions that do not use a shooting weapon at all (`5121002`, `11101004`, `15111006`, `15111007`, `21100004`, `21110004`, `21120006`, `33101007`, `4121003`, `4221003`), now parse WZ `level/common/bulletConsume` alongside `bulletCount` so client-ammo-heavy skills such as `Skill/411.img/4111005/common/bulletConsume = 3` and `Skill/412.img/4121006/common/bulletConsume = 200` no longer collapse to a generic single-ammo assumption, and now also parse WZ `common/itemConsume` for the client-confirmed Outlaw elemental shot helpers `5211004` / `5211005` so the live inventory-ammo resolver can require the authored pellet family instead of accepting any generic bullet stack. When an inventory runtime is present the simulator now resolves a concrete compatible USE-slot with enough stack quantity instead of only checking aggregate ammo presence, doubles the resolved per-cast ammo requirement while Shadow Partner is active to stay closer to the client-owned `GetProperBulletPosition` consumption lane, now mirrors the client-owned active-bullet-index branch by resolving the marked active projectile item as an exact item-id requirement instead of silently falling back to a different compatible family, still allows another stack of that same selected item id to satisfy the cast because the client scans by item id rather than a single slot index, and now also records the first compatible CASH projectile cosmetic from the same client family scan (`5020xxx` arrows, `5021xxx` throwing stars, `5022xxx` bullets) while carrying the client helper's `nWeaponItemID == 1472063 -> 2060xxx` arrow one-off into compatible-ammo matching instead of flattening everything to weapon-code-only rules. The live projectile runtime now also snapshots those resolved `pnItemID` / `pnCashItemID` ownership outputs per projectile at spawn time, so delayed `ballDelay` releases keep the exact USE ammo id plus the matched CASH cosmetic family that the cast resolved instead of dropping that client-owned selection as soon as the precheck returns. | Skills are closer to the client across both classifier and execution seams: no-ball mass-spell debuffs no longer inherit melee ordering, the missing Outlaw shoot-family helpers no longer collapse back onto the melee lane just because WZ publishes `info/type=1` without `ball`, no-projectile ranged skills now use the client-shaped melee-first vs direct-shoot split instead of one unconditional ranged branch, deferred moving-shoot targeting no longer snapshots only the ranged lane when the live client family would try a close-range strike first, and direct shoot casts now reject the wrong weapon family or missing ammo in the same high-level place the client gates `DoActiveSkill_ShootAttack` before attack resolution while also consuming a real compatible ammo stack rather than a simulator-wide count. The elemental Outlaw helpers now go one step further: WZ-authored `itemConsume = 2331000 / 2331001` is carried into the level-data seam and used to filter the resolved USE slot by the same fire-pellet (`2331xxx`) vs ice-pellet (`2332xxx`) family split that the client applies inside `GetProperBulletPosition`, so those skills no longer accept an arbitrary gun-bullet stack just because the weapon family matches. The shoot-ammo resolver is also closer to the client-owned selection path itself: a marked active bullet now means "find this exact projectile item id or fail the ammo branch" instead of "prefer this slot and then fall back to anything compatible", another stack of that same selected item id can still satisfy the cast because the client scans the whole USE inventory by item id, compatible CASH projectile cosmetics are now surfaced through the same family scan instead of being entirely ignored, the special client arrow one-off for weapon `1472063` no longer collapses onto the generic weapon-code table, and delayed projectiles now retain the exact resolved USE/CASH ownership snapshot that `GetProperBulletPosition` produced for the cast instead of losing it once the runtime moves on to later skill requests. The remaining gap is narrower and is now centered on the last client-only `GetProperBulletPosition` outputs and precheck side branches beyond the newly modeled exact active-item, elemental-pellet, cash-projectile-family, special-weapon, and per-projectile ownership snapshot one-offs, especially the still-undrawn cosmetic bullet-position presentation itself, any remaining active-index metadata beyond exact item id, the last vehicle or one-time-action and ladder-family nuances that still live inside the client precheck, any residual shoot-family classifier one-offs beyond the confirmed helper tables, and edge-case timing or packet-owned branch state that still lives outside WZ-authored geometry | `SkillManager.ProcessMeleeAttack`, `ProcessMagicAttack`, `ProcessNoProjectileRangedAttack`, `SpawnProjectile`, `CheckProjectileCollisions`, `ResolveQueuedSerialTargets`, `ResolveDeferredPreferredTargetMobId`, `SkillLoader.ParseSkillInfo`, `SkillLoader.CreateLevelData`, `ClientShootAttackFamilyResolver`, `ClientShootAmmoResolver`, `UnitTest_MapSimulator/ClientShootAmmoResolverParityTests.cs` (`Skill/521.img/5211004/info/type`, `Skill/521.img/5211004/common/itemConsume`, `Skill/521.img/5211005/info/type`, `Skill/521.img/5211005/common/itemConsume`, `Skill/521.img/5211006/info/type`, `Skill/411.img/4111005/common/bulletConsume`, `Skill/412.img/4121006/common/bulletConsume`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::DoActiveSkill_MagicAttack`, `CUserLocal::GetProperBulletPosition`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `is_shoot_skill_not_switched_to_melee_attack@0x6edcf0`, `is_shoot_skill_not_using_shooting_weapon@0x6edbf0`, `is_shoot_skill_not_consuming_bullet@0x6eeaf0`, `is_correct_bullet_item@0x861360`, `is_correct_bullet_cashitem@0x6ebcf0`, `is_pellet_fire_item@0x861300`, `is_pellet_ice_item@0x861330`, `SKILLENTRY::AdjustDamageDecRate`) |
| Partial | `DoActiveSkill_*` family parity | Generic movement, summon, and prepare/charge families now execute through the existing `SkillManager` seams instead of falling back to one generic cast lane. Movement-family dispatch now separates teleport snaps, grounded rush, flying-rush glide, jump-rush launches, and bound-jump starts from the client-confirmed `TryDoingTeleport` / `TryDoingRush` / `TryDoingFlyingRush` / `DoActiveSkill_BoundJump` splits; Teleport Mastery riders authored as `info/affectedSkillEffect=bodyAttack&&stun` stay off the direct-cast surface and fire as passive follow-up data after the owning teleport resolves; WZ-linked helper branches with `info/affectedSkill` plus `info/affectedSkillEffect` no longer surface as standalone castable actives; Battle Mage aura recasts cancel or replace the active aura instead of stacking; aura-dot riders keyed by `info/affectedSkillEffect=dot` plus `info/dotType=aura` pulse once per second while the owning aura is active; WZ `info/mesoExplosion` skills consume nearby owned mesos inside the authored `lt/rb` box before applying their attack payload; WZ `info/zoneType=invincible` skills create a persistent local protection zone; admin-skill execution now enforces GM/SuperGM ownership; bound-jump gating now respects `info/avaliableInJumpingState=1`, client-owned grounded exceptions such as `11101005`, `33001002`, and `35101004`, Wild Hunter swim/fly rejection, and WZ `info/requireHighestJump=1`; moving-shoot startup still resolves from the strongest loaded movement-action branch, and deferred moving-shoot execution now also mirrors the client-owned queued-level gate by dropping the delayed shot when the live learned level no longer matches the stored moving-shoot entry level instead of silently re-scaling the queued cast at fire time; and Rocket Booster now uses dedicated start/upkeep/landing seams that defer resource spend until launch, re-read the learned level at launch time, preserve the authored normal-vs-tank startup branch, keep the client `250 + 20 * floor(level / 4)` stored vertical launch formula, clear the stored launch marker on the first upkeep tick after startup, and end on the same post-attack clear cadence used by `TryDoingRocketBoosterEnd`. Packet-owned summons now mirror more of the `CSummoned::Update` passive lane too: taunt/puppet summons still resolve body-contact damage against mobs targeting that summon, WZ `info/selfDestructMinion=1` summons still schedule their authored final-attack window before removal, and this pass tightened passive `UpdatePassive` fallback so packet-owned summons without a move snapshot resolve their effective passive movement from the loaded WZ summon branch profile first and then fall back to the packet `moveAbility` when the skill profile is absent or still stationary, which keeps follow/hover summons moving and lets anchor-bound hover summons keep re-emitting their passive effect from motion instead of freezing when the packet stream drops to passive vec-ctrl updates. Focused test coverage for the packet-owned summon helper seam is now on disk in `UnitTest_MapSimulator/PacketOwnedSummonUpdateRulesTests.cs`, covering body-contact gating, natural-expiry self-destruct timing, and move-ability-based passive-movement fallback, and focused moving-shoot regression coverage now also guards the queued-level gate in `UnitTest_MapSimulator/MovingShootProjectileParityTests.cs`; running either targeted filter in this workspace is still blocked by unrelated pre-existing `HaCreator` compile errors before `UnitTest_MapSimulator` finishes building. | The remaining gap is now narrower and is concentrated in the last client-owned special cases rather than the generic execution lanes. The simulator still simplifies the exact moving-shoot entry snapshot and request choreography inside `TryDoingSmoothingMovingShootAttack`, especially the still-missing queued attack metadata such as action type or action speed and the unresolved `CAntiRepeat` repeat-count initialization or bypasses beyond the now-modeled position-threshold and level-match gates; the final highest-jump timing window beyond the current WZ-backed near-apex gate; any remaining Rocket Booster packet choreography beyond the now-modeled launch-marker, launch-speed formula, and end-loop timing; deeper packet-owned summon assist dispatch and per-family `OnSkill` / `OnAttack` ownership beyond the now-modeled body-contact / passive-movement / expiry cadence; and the remaining field- or packet-owned special cases that are not recoverable from WZ plus the currently confirmed client seams alone. | `SkillManager`, `SkillLoader`, `PlayerCharacter`, `PlayerCombat`, `PlayerSkillStateRestrictionEvaluator`, `SummonedPool`, `PacketOwnedSummonUpdateRules`, `UnitTest_MapSimulator/PacketOwnedSummonUpdateRulesTests.cs`, `UnitTest_MapSimulator/MovingShootProjectileParityTests.cs` (`CUserLocal::DoActiveSkill_*`, `CUserLocal::DoActiveSkill_BoundJump`, `CUserLocal::TryDoingTeleport`, `CUserLocal::TryDoingRush`, `CUserLocal::TryDoingFlyingRush`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CSummoned::Update`, `Skill/*/info/affectedSkillEffect`, `Skill/*/info/avaliableInJumpingState`, `Skill/*/info/requireHighestJump`, `Skill/*/info/selfDestructMinion`) |
| Partial | Shadow Partner render/action parity | The simulator now treats Shadow Partner as its own buff-owned companion render surface instead of only a temporary-stat icon family: `SkillLoader` loads the WZ `special/*` action tables for the confirmed Shadow Partner skills (`4111002`, `4211008`, `14111000`), derives the companion's baseline behind-the-player separation from the authored idle-frame `origin.x` instead of a second simulator-only spacing literal, and `SkillManager` binds that data to the buff lifecycle while `PlayerCharacter` drives a dedicated semi-transparent companion animation surface from those skill-owned `stand*`, `walk*`, `alert`, `stab*`, `swing*`, `shoot*`, `ladder`, `rope`, `fly`, `jump`, `sit`, `prone`, and `dead` frames instead of trying to reuse the base assembled avatar. Attack-family playback now mirrors through that dedicated action table with a delayed follow-up start keyed from the companion attack animation's first-frame delay rather than a fixed runtime constant, non-attack actions fall back through the companion action catalog instead of dropping immediately to the base body path, spawn still enters through WZ-backed `create*` branches when the skill provides them, and blocking `create*` plus attack playback now holds until the authored WZ frame sequence actually finishes before consuming a queued fallback or idle action. A WZ re-check against the current extracted data source currently reporting still shows the visible `create2` / `create3` / `create4` spawn surfaces for `4111002` and `4211008` collapsing to the same authored alert-family art while `14111000` still publishes no `create*` branch at all, so the remaining spawn branch gap is the client's exact chooser rather than a missing distinct WZ render path. The resolver seam is now more client-shaped too: `PlayerCharacter` prioritizes a mapping layer derived from `CAvatar::MoveAction2RawAction`, `action_mapping_for_ghost@0x406500`, and `CActionMan::LoadShadowPartnerAction@0x423cf0`, and this run tightens that ordering again with direct client evidence from the latest disassembly pass. `LoadShadowPartnerAction` does not simply try the plain raw action name first; it walks action-specific remap rows before it falls back to `get_action_name_from_code(raw)`, so `ShadowPartnerClientActionResolver` now mirrors that order not only for the ghost family but also for the alias-backed non-ghost helper families we have already recovered in local code (`move` / `walk` / `stand`, `ladder2`, `rope2`, and `alert*`). This pass pushes that same client-owned seam one step deeper again instead of only helping when the visible simulator action string already matches the helper alias: when the player's live client raw action resolves to one of those recovered helper rows, the resolver now consults that raw-action-specific alias table before yielding the plain raw action name, and the playback path now applies the same raw-action-specific frame rewrite before rendering the companion. That keeps client raw actions such as `ladder2`, `rope2`, and indexed `alert*` on their authored helper-frame behavior even when the visible local body action has already collapsed to a more generic name, so the clone holds on the second climb frame or the matching alert frame instead of replaying the whole authored branch. This run broadens that same explicit raw-action alias surface again with WZ-backed Shadow Partner job actions that the simulator was still letting fall through to idle fallback: `Skill/411.img/skill/4111005/action/0` and `Skill/1411.img/skill/14111002/action/0` both publish `avenger`, `Skill/421.img/skill/4211002/action/0` publishes `assaulter`, and `Skill/421.img/skill/4211006/action/0` publishes `prone2`, so the resolver now treats those names as client-owned helper rows ahead of the plain raw action string too. That keeps those local thief-family actions on the authored `shoot*`, `stab*`, and `prone*` companion families instead of collapsing the clone back to stand once the visible body action has already normalized. Focused resolver tests are now checked in for both the alias ordering and these recovered frame-remap cases, including the new raw-action-backed ordering/remap coverage, although direct execution is still blocked in this workspace by unrelated pre-existing `HaCreator` compile failures before `UnitTest_MapSimulator` can finish building. The broader alias surface still covers `ghoststand`, `ghostwalk`, `ghostjump`, `ghostprone`, `ghostproneStab`, `ghostladder`, `ghostrope`, `ghostfly`, `ghostsit`, `ghost`, `hit`, `dead`, `swim`, `fly2`, `fly2Move`, `fly2Skill`, generic `move` / `walk` / `stand`, generic `attack` / `attack1` / `attack2`, direct authored action names such as `heal`, the observed raw aliases `stabD1`, `swingD1`, `swingD2`, `doubleSwing`, `tripleSwing`, and `shotC1`, the newly recovered Shadow Partner-job helper rows `avenger`, `assaulter`, and `prone2`, and a broader heuristic raw-combat surface now collapsed from the client action-name catalog onto the authored companion families, including pirate-style names such as `fist`, `straight`, `somersault`, `backspin`, `doubleupper`, `screw`, `shockwave`, `demolition`, `snatch`, `eburster`, `edrain`, `dragonstrike`, `eorb`, and `wave`, archer-style names such as `windshot`, `windspear`, `stormbreak`, and `arrowRain`, plus ice-warrior-style names such as `iceAttack1`, `iceAttack2`, `iceSmash`, `iceTempest`, `iceChop`, and `icePanic`, all of which now stay in `stab*` / `swing*` / `shoot*` before later idle fallback instead of dropping straight to stand. The runtime also re-evaluates that client-backed float-vs-ground mapping when the player stays on the same body action name but changes between grounded and floating states, re-syncs the clone facing when the player turns in place, re-triggers repeated same-name attack casts from the local attack-start timestamp, preserves queued fallback facing while a blocking action is still finishing, applies the client-confirmed `+/-30 px` normal-lane and `+50 px` ladder/rope lane displacement from `CUser::PrepareShadowPartnerActionLayer@0x8e1730`, and mirrors the helper layer's `RelMove(..., currentTime + 200)` cadence so lane changes transition over 200 ms instead of snapping. `SkillLoader` also parses authored per-frame `a0` / `a1` alpha ramps from the companion canvases, `PlayerCharacter` interpolates that frame alpha on top of the client-backed 150-alpha companion tint, and the draw path now inserts the companion at the assembled avatar's under-face insertion point instead of a blanket pre-avatar pass, matching the client evidence that Shadow Partner is layered off `m_pLayerUnderFace` rather than only as a coarse behind-character draw. The remaining gap is narrower again: the unresolved alias seam is no longer limited to ghost-family order nor to visible-action-only handling of the recovered `ladder2` / `rope2` and indexed `alert*` frame rewrites, and it is no longer missing the first recovered thief-specific helper rows either, but the simulator still lacks the rest of the non-ghost action-specific alias rows and any additional raw-action-specific frame-index rewrites hidden inside `LoadShadowPartnerAction`, plus the client's exact `create2` / `create3` / `create4` spawn-branch choice when multiple authored variants exist, the exact intra-under-face ordering versus other avatar-owned under-face overlays, and any residual alpha or one-time follow-up choreography still encoded in client layer state rather than visible WZ canvas props | Shadow Partner buffs now produce an actual mirrored companion animation surface with its own action ownership, WZ-derived spacing, WZ-timed delayed attack playback, WZ-backed spawn/create playback for the classic skills that author it, less abrupt cadence because create and attack branches are allowed to complete before the simulator drops to idle or another non-blocking fallback, broader client-shaped action fallback through the companion catalog, and client-shaped local timing for repeated same-name attacks and turn-in-place facing changes. This pass widens the direct disassembly-backed helper-table mirror beyond visible action strings alone: the recovered climb and indexed alert helper rows now stay on the same authored companion frames even when only the client raw action still names that helper row, and the newly recovered WZ-published thief actions `avenger`, `assaulter`, and `prone2` now stay on the authored companion shoot/stab/prone families instead of replaying idle fallback after the body action has already normalized. The remaining mismatch is now concentrated in unrecovered non-ghost alias-table rows and frame-index rewrites beyond these confirmed helper cases, the precise spawn-branch chooser, and any last under-face ordering or client-owned alpha/follow-up metadata that the simulator still cannot read from WZ alone. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `ShadowPartnerClientActionResolver.cs`, `UnitTest_MapSimulator/ShadowPartnerClientActionResolverTests.cs`, `docs/GMS v95.h` (`CAvatar::MoveAction2RawAction`, `CActionMan::GetShadowPartnerProp@0x41ba60`, `CActionMan::LoadShadowPartnerAction@0x423cf0`, `CUser::PrepareShadowPartnerActionLayer@0x8e1730`, `is_back_action@0x45f1b0`, `action_mapping_for_ghost@0x406500`, `SecondaryStat`, `ACT_ALERT3`, `ACT_ALERT4`, `ACT_ALERT5`, `ACT_LADDER2`, `ACT_ROPE2`, `Skill/*/{4111002,4211008,14111000}/special/*`, `Skill/411.img/skill/4111005/action/0`, `Skill/1411.img/skill/14111002/action/0`, `Skill/421.img/skill/4211002/action/0`, `Skill/421.img/skill/4211006/action/0`) |
| Partial | Queued follow-up attack families | Attack skills that publish WZ `finalAttack` follow-up maps still queue a deferred extra strike after the initiating melee or projectile hit resolves, gate that follow-up by the equipped weapon type both when the proc is queued and again when it executes, reuse the learned follow-up skill's proc rate, carry the preferred target forward into the queued execution path, and retain that queued target's world-position snapshot through direct follow-up resolution plus any later deferred moving-shoot or projectile hop so the simulator can keep biasing back toward the original target lane even if the source mob disappears before fire time. The queued branch still preserves the stored facing through deferred hitbox and projectile resolution even when it takes an extra deferred moving-shoot hop before firing, overwrites any still-pending final-attack proc with the newest one so the simulator matches the client's single pending follow-up slot instead of stacking multiple deferred final attacks, keeps that overwrite scoped to the final-attack slot instead of clearing unrelated queued summon hits, and still carries that same overwrite one step farther by tagging queued final-attack projectile releases themselves so a newer final attack also evicts any older still-pending delayed projectile spawn that was already armed past the moving-shoot payload seam but had not fired yet. Thunder Breaker `Spark` (`15111006`) still runs through its own timed attack-triggered proc family by treating the WZ `info/condition=attack` + `info/chainAttack=1` + `info/chainattackPenalty=1` cast as a timed buff with a separate pending spark slot, and the queued Spark branch still re-enters the shared shoot/projectile attack path instead of a simulator-only direct-hit loop, carrying the saved trigger mob id plus both the saved source origin and target-position snapshot back into projectile spawn so the deferred proc keeps the client-shaped shoot-entry choreography and stored target lane even when the queued branch later takes an extra deferred moving-shoot hop before firing; the simulator also still mirrors that single-slot behavior through Spark's own delayed projectile-release seam, so a newer queued Spark can evict an older still-pending delayed Spark projectile before fire time instead of only replacing the earlier queue entry. WZ `info/chainAttack=1` + `info/chainattackPenalty=1` serial families such as `2221006` and `5121002` still use their own single pending serial slot, preserve queued facing, recheck weapon type before the deferred branch executes, derive later chain hits from the stored origin mob/position even when the opener only resolved one live hit, and keep deferred moving-shoot payloads from jumping ahead of a due queued serial branch by treating that serial slot as part of the same queued follow-up ordering gate that already protected due final-attack and Spark playback. Queued final-attack, Spark, and serial execution still re-read the live learned skill level when the deferred branch actually fires instead of consuming the level snapshot captured when the proc was queued, still route queued cast startup back through the normal cast-effect seam so those deferred branches force the stored facing onto the avatar before the queued action starts, keep secondary cast-effect layers, and honor the same client-owned transient avatar-effect suppression path the immediate cast entry already uses, and still carry the client-only ranged final-attack `TryDoingShootAttack(..., 65, ...)` shoot-range hint through the queued entry plus any extra deferred moving-shoot hop so direct ranged final attacks keep the wider client-shaped deferred target search instead of falling back to the simulator's generic ranged offset. This run narrows the remaining `TryDoingShootAttack` gap again by carrying the queued ranged final-attack shoot-point itself through the normal cast seam: after queued cast startup reapplies the stored facing, deferred ranged final attacks now resolve the live `handMove` / `hand` / body-origin shoot point from the avatar frame and feed that origin back into the shared shoot path instead of falling straight back to the simulator's generic `player Y - 20` projectile spawn. Queued final attacks and queued Spark also still restore the compatible USE/CASH projectile selection they armed with before re-entering cast startup, moving-shoot scheduling, and any later delayed projectile release, so those branches no longer inherit unrelated live ammo metadata from an intervening cast. WZ-authored `x` chain-penalty decay and the client-owned fixed multiplier tables for no-`x` families still both apply through the deferred or timed chain path as well, so queued follow-up families no longer flatten back onto one generic damage percent after the opener. The remaining gap is now concentrated in the last client-only spark/final-attack shoot bookkeeping beyond the current queued-slot, delayed-projectile overwrite, queued ammo snapshot, and queued final-attack shoot-origin coverage, especially exact `GetProperBulletPosition` / `TryDoingShootAttack` side effects, cosmetic bullet-position rendering, cash-bullet slot metadata usage, or any residual shot metadata that the client still derives internally at queued fire time | Final-attack style procs still no longer collapse into the initiating cast timing, queued strikes still cancel if the player changes to a non-matching weapon before the deferred attack fires, if a newer follow-up proc replaces the pending one before it can execute, or if the follow-up skill has been locally unlearned before the queued branch actually fires, and replacing a pending final attack still does not wipe unrelated queued summon damage. Once a queued final attack or queued Spark has already progressed into a delayed projectile release, a newer proc in that same client-owned slot still clears the older still-pending projectile instead of letting it survive past the queue replacement point, while unrelated projectile work remains intact. Deferred moving-shoot payloads also stop dropping out when a due queued follow-up family should run first because the payload now stays armed until after that queued final-attack, Spark, or serial branch resolves instead of being dequeued and lost. Queued follow-up casts that pass through the simulator's extra deferred moving-shoot payload path still keep the stored facing/origin snapshot, preserve the client-confirmed ranged final-attack shoot-range override (`65`) through both deferred target acquisition and the actual delayed fire step, revalidate the live learned level again at the actual deferred fire time instead of silently snapping back to the player's live turn state or consuming a stale queued level, and keep the queued target-position snapshot alive across both direct execution and deferred projectile targeting so a vanished opener mob does not force those branches straight back onto generic live ordering. This run also closes the direct queued projectile-origin hole that was still open inside the ranged final-attack branch: once queued cast startup reapplies the stored facing, deferred ranged final attacks now reuse the avatar's live `handMove` / `hand` / body-origin shoot point before projectile spawn instead of dropping back to the simulator's generic body-height fallback, while queued Spark keeps its separate saved-source-origin path. Active `Spark` buffs still produce their own delayed follow-up through the same shared shoot/projectile branch the client uses while preserving the stored trigger origin and target-position lane for projectile spawn, and WZ-marked serial-attack plus no-`x` fixed-table families still keep their deferred damage falloff aligned to the same post-opener hit order used by live chain resolution. The remaining mismatch is now mostly in deeper client-owned shoot-entry bookkeeping such as exact `GetProperBulletPosition` / `TryDoingShootAttack` side effects, cosmetic bullet-position rendering, cash-bullet slot bookkeeping, and any residual shot metadata beyond the now-snapshotted USE/CASH projectile item ownership plus queued final-attack shoot origin | `SkillManager`, `SkillData`, `SkillLoader` (`Skill/1511.img/skill/15111006/info/{condition,chainAttack,chainattackPenalty}`, `Skill/222.img/skill/2221006/info/{chainAttack,chainattackPenalty}`, `Skill/512.img/skill/5121002/info/{chainAttack,chainattackPenalty}`, `CUserLocal::GetProperBulletPosition`, `CUserLocal::TryDoingFinalAttack`, `CUserLocal::TryDoingSerialAttack`, `CUserLocal::TryDoingSparkAttack`, `CUserLocal::TryDoingShootAttack`) |
| Partial | Repeat-skill sustain families | `CUserLocal::TryDoingRepeatSkill` still owns more of the Mechanic repeat-state machine than the simulator, but the runtime now carries repeat-skill state not just for siege and tank-siege sustain teardown but also for tank mode and SG-88: siege and tank-siege still enforce the WZ `fixedState` / `canNotMoveInState` lock, honor `onlyNormalAttack` by blocking unrelated skill casts, auto-release after their 5-second client timer, emit the client-mapped timeout effect-request ids (`35110004` for siege and `35120013` for tank-siege), and hand tank-siege back to tank mode instead of leaving the transform latched; tank mode (`35121005`) and the two siege sustains now also route normal attack input through their repeat-owned skill shots with the client-confirmed `TryDoingSiege` lockout windows (`420 ms` for tank mode, `180 ms` for siege / tank-siege) instead of falling back to the simulator's generic basic attack; tank-siege timeout and local cancel now follow a closer client-style staged completion path by latching a pending mode-end request when the end effect is requested, suppressing further repeat-owned attacks while that latch is set, and scheduling both the no-listener fallback and an explicit local acknowledgement back through the shared client-timer update seam instead of finishing inline at the acknowledge call site, with `SkillManager` exposing a repeat-mode-end request/ack seam for future server-owned confirmation while preserving the old next-tick local fallback when nothing listens. That pending tank-siege lane now also tracks request time on simulator time instead of `Environment.TickCount`, can complete from an observed return-to-tank transform before the synthetic local acknowledgement fires, now lets `PlayerManager` discard stale local mode-end requests once the shared repeat state is already gone, derives its listener-owned local fallback delay from the loaded WZ `skill/35121013/effect` end-effect window (`180 ms` total in client) instead of a simulator-only literal before acknowledging on the shared timer seam, and now mirrors the client one-shot `m_bSendTankSiegeModeEnd` latch more closely by refusing to reissue or retime a tank-siege mode-end request once that pending path is already active. SG-88 still uses the client skill id (`35121003`), starts with assist fire disabled, tracks the local `bDone` / branch-point progression in repeat state, shows the `KeyDownBar4` HUD after the client's `810 ms` delay, arms summon assist after the client's `2790 ms` branch timing, now clears its manual-assist request after the first client-style summon shot instead of leaving the summon latched in a persistent assist-fire state, refreshes repeat-state attack bookkeeping from actual summon fire instead of only toggle timestamps, tears the summon down after the client-style `2520 ms` idle window if assist is toggled off and not re-armed, and now routes manual assist targeting through a closer `CSummoned::TryDoingAttackManual`-shaped lane by reusing the owner-first nearest-target search, deriving the primary hit line from the raw loaded `skill/35121003/summon/attack1/info/range` offset (`lt.y = -350`), matching the client `CMobPool::CheckMobInTrapezoid` strip walk for first-target admission even when a straddling mob's center lands on the opposite side of the summon, translating the authored follow-up cluster from the resolved client-style `CMob::GetHitPoint` center instead of the simulator's earlier edge clamp, and staging admitted clustered targets through the client-observed post-admission timing split by keeping the primary hit on the base summon delay while batching the remaining clustered hits behind the shared `120 ms` follow-up window instead of resolving every admitted mob as an independent same-tick queued hit. Focused regression tests are now on disk in `UnitTest_MapSimulator/Sg88ManualAttackParityTests.cs` for the straddling-hitbox admission case plus the shared `120 ms` batch split, although `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter Sg88ManualAttackParityTests` is still blocked in this workspace by unrelated pre-existing `HaCreator` compile failures in `MapSimulator.PacketOwnedTeleportParity.cs`, `LoginCreateCharacterWindow.cs`, `LoginTitleWindow.cs`, `LoginUtilityDialogWindow.cs`, `SocialRoomModels.cs`, and `MapTransferUI.cs`. The remaining gap is the true client/server confirmation path behind tank-siege mode-end completion, the exact packet or request acknowledgement sequencing that would drive that confirmation instead of the simulator's local fallback plus observed-transform shortcut, and the last packet-owned bookkeeping inside `CSummoned::TryDoingAttackManual` after target admission and clustered hit timing are resolved | Mechanic repeat skills are now closer on fixed-state timing, in-state attack restrictions, repeat-owned normal attack cadence, SG-88 branch-state timing, and tank-siege teardown sequencing because the pending mode-end hop now stays inside the same client-timer-style update surface for both local fallback and explicit acknowledgement, can also resume from an externally observed return-to-tank mode instead of depending solely on the simulator's synthetic ack, clears stale listener-owned pending requests once repeat state already resolved, waits through the loaded WZ-backed tank-siege end-effect window before falling back locally, preserves the first pending tank-siege request window instead of letting repeated local cancels re-send or re-time that mode-end request after the client-style latch is already set, keeps SG-88 repeat bookkeeping in sync with real assist fire, no longer leaves SG-88 manual assist stuck in a repeat-fire state after the first armed shot because the summon now consumes that client-style request once the manual attack actually dispatches, and now keeps SG-88 manual assist closer to the client by admitting cross-over hitboxes through the same left-then-right trapezoid test order the client uses before the existing client-style hit-point translation and delayed secondary-cluster timing after target admission take over. The simulator still simplifies the final client-owned confirmation behind tank-siege end plus any packet-owned SG-88 bookkeeping that happens after `TryDoingAttackManual` has already admitted targets and scheduled the clustered follow-up hits | `SkillManager`, `PlayerCharacter`, `PlayerManager`, `UnitTest_MapSimulator/Sg88ManualAttackParityTests.cs` (`CUserLocal::TryDoingRepeatSkill`, `CUserLocal::TryDoingSiege`, `CUserLocal::DrawKeyDownBar`, `CUserLocal::UpdateClientTimer`, `CSummoned::TryDoingAttackManual`, `CMobPool::CheckMobInTrapezoid`, `CMob::GetHitPoint`, `Skill/3512.img/skill/35121003/summon/attack1/info/range`, `Skill/3512.img/skill/35121013/effect`) |
| Partial | Bound-jump, rocket-booster end, and smoothing moving-shoot branches | `Rocket Booster` now follows more of the authored WZ startup and teardown cadence: the deferred launch keeps the cast-start facing, uses the active `rbooster_pre` or `tank_rbooster_pre` body action duration instead of one shared startup window, preserves that launch-facing through the airborne branch, detects touchdown for the landing strike, and now really does clear into the authored `rbooster_after` or `tank_rbooster_after` exit action while keeping teardown latched for the longer of the old `500 ms` cleanup window or the loaded exit-action duration. This pass also tightened the client-side start gate around that deferred launch: the bound-jump seam now rejects `Rocket Booster` while off-foothold, on ladder or rope, in free-fall, or in swim or fly states before the queued launch begins, matching the client-owned ground-start branch more closely instead of letting the simulator re-enter Rocket Booster from broader generic movement states. `TryDoingRocketBoosterEnd` is closer too: once the touchdown strike has happened, the simulator now raises the same local `35100004` recovery effect request after the client-observed `500 ms` landing-attack window, but keeps the landing state alive until the longer authored exit-action window ends instead of tearing the branch down immediately after the effect request. The moving-shoot path still snapshots the cast-start preferred mob, firing direction, and world origin so deferred projectile and direct ranged payloads resolve from that stored origin after movement startup instead of re-reading a later position or facing, and it is now closer to the client because WZ `info/delay` values and movement-action animation duration hold deferred `casterMove` payloads until longer startup branches such as `backstep` or `assaulter` finish instead of forcing the old `60-180 ms` fallback window, deferred moving-shoot casts snapshot the rendered `handMove` or `hand` body anchor from the current avatar frame so stored projectile and direct ranged origins follow the client shoot-point branch more closely instead of always falling back to the player body origin, deferred projectile release now keeps that stored hand-anchor Y instead of reapplying the old generic `body - 20` muzzle offset, and the deferred preferred-target snapshot now resolves from that same stored shoot origin instead of a later player-body position. This run carries more of the client-owned `TryDoingSmoothingMovingShootAttack` execute surface into the existing seam as well: deferred moving-shoot payloads now retain the preferred-target world position alongside the preferred mob id, so execution can fall back to the nearest still-attackable mob inside the stored hitbox when the original mob disappears before the delayed projectile or ranged payload fires instead of dropping straight to a fresh generic target ordering pass, the execute-time gate now also mirrors the client `CAntiRepeat::TryRepeat` movement window more closely by treating live world positions within `|dx| < 6` and `|dy| < 150` as the same delayed moving-shot location instead of only rejecting exact integer position matches, delayed moving-shoot projectiles now preserve the queued target-world-position snapshot ahead of any execute-time live mob recentering so homing, collision, explosion, and target-rect fallback ordering stay centered on the cast-start shoot-point even when the preferred mob is still alive but has moved before impact, and that stored-point-first rule now really reaches the projectile runtime because deferred moving-shoot projectiles carry an explicit stored-target preference flag through the existing `ActiveProjectile` seam so homing aim plus live collision or rect-target ordering stop short-circuiting back to the later mob center as soon as the original mob remains alive. Deferred moving-shoot projectile payloads now also snapshot the queue-time resolved shoot-ammo selection so the later projectile spawn reuses the stored use-slot and cash-slot bullet choice instead of re-reading whatever ammo state happened to be current when the delayed fire finally executed, queued moving-shoot, final-attack, and Spark ranged follow-up branches now also preserve the queue-time no-consume temporary-stat state alongside that stored ammo selection so client-style Soul Arrow or related no-consume entry state survives the delayed execution path instead of being dropped as soon as the queue is armed, and delayed moving-shoot hitbox resolution now keeps the queued body action metadata from cast start so execute-time melee-fallback and afterimage-range branches do not re-read a later idle or landing action before choosing their stored hitbox; the deferred payload now also retains the queued client raw action code and feeds that back through the existing melee afterimage/range seam when the queued action name is missing, carrying more of the client's `m_movingShootEntry` action snapshot into delayed hitbox resolution instead of depending only on the later live action family. That same queued raw action snapshot now also reaches deferred preferred-target hitbox resolution before execution, so queued moving-shoot target selection no longer drops back to a later live action family when the stored action name is blank but the queued raw client action still resolves the correct afterimage-backed or client-range-backed hitbox. The client `TryDoingSmoothingMovingShootAttack` request surface is also narrower now because the simulator refuses to queue another smoothing moving-shoot cast while an earlier deferred moving-shot payload is still pending, matching the client's single active moving-shoot entry more closely instead of stacking multiple delayed moving shots through the queue. The client `info/type=40` bound-jump family is also closer to `DoActiveSkill_BoundJump`: midair flash-jump families no longer auto-promote from a fresh foothold jump, the loaded WZ `info/avaliableInJumpingState` flag now decides whether generic type-40 skills must start airborne instead of forcing every type-40 skill through one airborne gate, `Wind Walk` stays a ground-start bound jump, `Jaguar Jump` stays a grounded bound-jump launch instead of sharing the airborne flash-jump gate, and the client velocity formulas now cover the missing `13101004` / `14101004` type-40 family in addition to the earlier Night Lord, Shadower, Dual Blade, and Jaguar-specific cases. Focused moving-shoot resolver tests are now on disk for the stored-target-priority seam in `UnitTest_MapSimulator/MovingShootProjectileParityTests.cs`, although `dotnet build UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --no-restore` is still blocked in this workspace by unrelated pre-existing `HaCreator` compile failures in `SkillDataLoader.cs`, `SocialRoomModels.cs`, and `LoginUtilityDialogWindow.cs` before the row-specific tests can run. The remaining gap is now narrower and centered on the still-unmodeled `m_movingShootEntry` fields the simulator does not yet consume after the queued raw-action path now covers both deferred target selection and delayed execution, especially queued `nAttackActionType` and `nActionSpeed`, the unresolved `CAntiRepeat` repeat-count initialization and any special-case bypasses beyond the now-mirrored position thresholds, and the last server-confirmed Rocket Booster or non-WZ bound-jump special cases that still live in client-owned branches | Movement-coupled attack skills now preserve more of the client's traversal, startup timing, landing cadence, moving-shot target retention, cast-origin lock, shoot-point anchoring, and move-family directionality instead of collapsing into the old teleport/rush/flying-rush-only movement path or instant moving-fire hits. Bound-jump starts are less generic now because flash-jump families no longer re-jump off the ground, generic type-40 branches can now honor the WZ-authored airborne-vs-ground start split through `info/avaliableInJumpingState` instead of a simulator-only blanket airborne rule, grounded families such as `Wind Walk`, `Jaguar Jump`, and `Rocket Booster` keep their client-shaped start-state expectations, the Rocket Booster landing branch now emits the client-mapped post-attack recovery effect request and stays latched through the authored exit action instead of ending silently or collapsing to a fixed `500 ms` recovery only, and the missing type-40 launch formulas for the Bowman and Night Walker family now use the client's stronger horizontal-first arc instead of the simulator's earlier inverted double-jump fallback. Deferred projectile release also stays aligned to the stored hand anchor instead of snapping back through the older generic body-height offset during the delayed shot, deferred moving-shoot requests no longer stack multiple pending delayed casts while one client-style moving-shot entry is already armed, the delayed moving-shot target snapshot now survives loss of the original mob by falling back through the stored target position inside the authored hitbox instead of restarting from a generic live ordering pass, and delayed moving-shoot projectiles now also keep honoring that queued target point even when the original mob survives and drifts away before impact because the active projectile runtime itself now prefers the stored queued target position over later live preferred-mob recentering for those deferred moving-shoot shots. Repeated delayed moving-shot executions for the same skill now also use the client-shaped `CAntiRepeat` movement window (`6 px` horizontal, `150 px` vertical) instead of relying on exact live-position matches after the earlier rendered-body-origin tolerance pass was removed, delayed projectile fire no longer re-reads a later ammo selection because the deferred payload now restores both the queue-time resolved use-slot and cash-slot shoot-ammo snapshot and the queued no-consume temporary-stat state before projectile creation, and delayed moving-shoot execute-time hitbox selection now keeps the queued action-family context for melee-fallback or afterimage-backed branches instead of always consulting the player's later current action while also preserving the queued client raw action code for raw-action-backed afterimage or client range override resolution when the delayed shot executes. That same queued raw action context now also drives deferred preferred-target hitbox resolution before the delayed shot fires, so stored-target fallback ordering stays on the queued client action family even when only the queued raw action code survives and the live avatar has already moved on to another state | `SkillManager`, `SkillLoader`, `SkillData`, `PlayerCharacter`, `CharacterAssembler`, `UnitTest_MapSimulator/MovingShootProjectileParityTests.cs` (`CUserLocal::DoActiveSkill_BoundJump`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CUserLocal::TryDoingSmoothingMovingShootAttack`) |
| Partial | Key-down / charge skills | The simulator now routes key-down HUD rules through the prepared-skill runtime so client-confirmed bar suppression for `3121004`, `5201002`, `13111002`, `22121000`, `22151001`, `33121009`, `35001001`, and `35101009` no longer shows the generic status-bar gauge, preserves the `KeyDownBar4` skin for `35121003`, mirrors the client's 1-second generic gauge timing for `14111006`, caps mechanic `35001001` / `35101009` sustain after the charge window enters its actual hold phase instead of counting the 8-second branch limit from cast start, loads WZ `repeat` branches into the prepared-skill hold loop so charge families such as `4341002`, `5101004`, and `15101003` swap to their dedicated repeat effect/cadence once the hold phase begins, and now also loads and renders WZ companion prepare/hold/release branches such as `prepare0`, `keydown0`, and `keydownend0` so multi-layer prepared-skill effects like `24121000`, `5221004`, and `5721001` can draw their negative-`z` underlay branch behind the avatar instead of dropping that branch entirely. Prepared hold teardown is now closer to the client's shared timer path because capped keydown sustain starts a `prepared-release` client timer when the real hold phase begins instead of relying only on per-frame hold polling, cancel requests now only acknowledge matching local prepared-skill state while tearing that state down through the shared cancel seam instead of blindly succeeding on unmatched ids, and the local prepared-skill handshake now records both the hotkey slot and a stable per-press physical-input ownership token derived from the exact triggering key/button path so duplicate bindings share one held owner while only that same held input can issue the matching release or same-skill toggle. The client-confirmed `300 ms` exclusive-request gate from `CUserLocal::DoActiveSkill_Prepare` now also rides a broader local prepared-skill request seam instead of only prepare start, so same-family release toggles, hotkey key-up release, matching cancel requests, and the shared forced-local-release path used when control drops, the player dies, job state reloads, or other local reset paths tear down prepared state all refresh the same short exclusive window before the next prepare attempt instead of letting state clears bypass that local client throttle. Focused request-gate parity tests now lock that forced-release refresh seam in place alongside the earlier HUD coverage. The shared prepared-skill release seam now also owns the Dual Blade charge pair `4341002` / `4341003`, the Big Bang trio `2121001` / `2221001` / `2321001`, Cannon Shooter `5311002`, and Evan dragon breath `22121000` / `22151001`: WZ marks all of them with `info/chargingSkill=1`, the mage and Evan descriptions explicitly describe the attack as culminating after the charge finishes, `Skill/531.img/5311002` publishes the same `prepare` -> `keydown` -> `action/0 = noiseWave` body-backed release shape already used by the avatar parity row, `Skill/2212.img/22121000` and `Skill/2215.img/22151001` publish `prepare/action` plus release `action` branches without a looping hold branch, and the runtime now follows through by treating those holds as release-triggered execution instead of firing their payload through the generic repeating keydown loop or auto-releasing them as ordinary non-keydown prepares. This pass also closes the remaining generic-gauge timing hole in that same release-triggered seam again at the remote prepared-skill surface: when a release-owned charge family does not publish a dedicated client gauge override, the shared prepared-gauge resolver now uses the authored WZ charge window instead of the shorter prepare-animation branch length for both the local runtime and the remote prepared-skill / `/remoteuser prepare` HUD seams, so `5311002` follows `Skill/531.img/5311002/common/time` rather than arming or drawing from only `prepare/time = 1080`, while explicit client gauge overrides such as Big Bang and Evan dragon breath still keep their observed bar timings. Focused HUD parity tests now lock that shared prepared-gauge fallback in place together with the existing `Release` caption branch. The same WZ-backed release seam now also carries partial-release damage scaling for the charge families whose published descriptions explicitly say longer charging increases damage: early release for Evan `22121000` / `22151001` and Cannon Shooter `5311002` now executes through the existing release-owned prepared-skill path with damage scaled by authored charge progress instead of cancelling outright until full arm, while fully armed release still lands at full strength. `String/Skill.img/5311002/desc` also says Monkey Wave can be used immediately at full strength on a critical hit, and the local prepared-skill runtime now mirrors that WZ-backed rule by snapping an in-progress `5311002` charge straight into its armed hold state on any local critical hit instead of making the player finish the remaining charge window manually. Their local prepared-skill HUD text now also stays on the client-style `Release` branch once the charge is armed instead of falling back to the generic ready text. Dragon-owned keydown HUD also still suppresses the simulator's player fallback so Evan charge bars only render when the dragon runtime exposes a live dragon anchor instead of floating over the player when the companion is hidden or unavailable. This pass also moves one more release/follow-up owner table into the shared prepared-skill seam: `PreparedSkillHudRules` now owns the release-triggered and release-follow-up charge-family catalog that `RemoteUserPacketCodec` consults for official melee post-attack payload decode, so the Big Bang trio, Evan dragon breath pair, Dual Blade charge pair, Monkey Wave, and the Wild Hunter swallow release follow-up no longer rely on a stale remote-only hardcoded subset outside the prepared-skill owner seam. The remaining gap is now the true client-owned raw scan-code handshake, the rest of the wider exclusive-request catalog outside the modeled prepared-skill request family, and the release or follow-up families whose exact packet-owned charge semantics or non-WZ scaling curves are still not confirmed enough to replace their current best-effort paths one-for-one | Charge-family skills now match more of the client's visible HUD branching, mechanic timeout behavior, timer-owned hold expiry, multi-layer prepare visuals, local input ownership, dragon-bar ownership, remote prepared-skill gauge timing, and local forced-release throttling instead of always reusing one generic bar, one foreground-only cast effect, or one unowned release path; the mechanic flamethrower family now gets its full sustained window after charging before auto-release, repeated charge holds can follow the WZ-authored repeat branch, Dual Blade `Final Cut` (`4341002`), `Monster Bomb` (`4341003`), the Big Bang trio (`2121001`, `2221001`, `2321001`), `Monkey Wave` (`5311002`), and Evan dragon breath `22121000` / `22151001` no longer advertise or animate a release-owned charge while still executing through the generic hold-repeat loop or ordinary prepare timer, unrelated duplicate bindings now share one held prepared-skill owner token instead of competing with separate logical slot presses, and prepare spam respects the client's local `300 ms` exclusive-request gate across the modeled prepared-skill request family even when a keydown hold is torn down through shared local reset paths instead of a direct hotkey key-up. Evan dragon charges also no longer fall back to a player-anchored world bar when the dragon actor is absent, and remote prepared-skill overlays now keep the same authored charge-window fill timing that the local release-owned runtime already uses when the packet or command seam does not publish a separate client gauge override. `Monkey Wave` also no longer arms and fills its charge bar off the shorter prepare animation when WZ already publishes a longer authored charge window, its partial-release attack together with Evan breath partial-release attacks now use charge-progress-scaled damage because their `String/Skill.img` descriptions explicitly say the attack grows stronger with more gathering time, and an in-progress Monkey Wave charge now also jumps straight to the fully armed release state on a local critical hit because the same WZ description explicitly says the skill can be used immediately at full strength on crit. The same release/follow-up owner list now also stays aligned between the local prepared-skill runtime and the remote-user official melee packet decode seam instead of leaving remote post-attack payload handling on an older manually maintained subset. The underlying prepared-skill execution loop is still simplified vs client behavior because the simulator still does not model the OS-level scan-code path the client stores directly, the broader client/server exclusive-request handshake outside these prepared-skill request seams, or the remaining release/follow-up families whose exact packet-owned charge semantics and scaling curves are not yet confirmed enough to replace their current best-effort paths. `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter PreparedSkillRequestParityTests` is currently blocked in this workspace by unrelated pre-existing `HaCreator` syntax errors in `MapSimulator.CashServiceStageParity.cs` around lines `581-625`. | `PreparedSkillHudRules.cs`, `SkillManager`, `StatusBarUI.cs`, `MapSimulator.RemoteUsers.cs`, `PlayerInput.cs`, `PlayerManager.cs`, `RemoteUserPacketCodec.cs`, `UnitTest_MapSimulator/UI/PreparedSkillHudParityTests.cs`, `UnitTest_MapSimulator/PreparedSkillRequestParityTests.cs` (`Skill/212.img/2121001/{info/chargingSkill,desc}`, `Skill/222.img/2221001/{info/chargingSkill,desc}`, `Skill/232.img/2321001/{info/chargingSkill,desc}`, `Skill/2212.img/22121000/{info/chargingSkill,prepare,action}`, `Skill/2215.img/22151001/{info/chargingSkill,prepare,action}`, `String/Skill.img/{22121000,22151001,5311002}`, `Skill/434.img/4341002/{info/chargingSkill,common/time}`, `Skill/434.img/4341003/{info/chargingSkill,common/time}`, `Skill/531.img/5311002/{info/chargingSkill,common/time,prepare/time,keydown,time,action}`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::DrawKeyDownBar`, `CDragon::DrawKeyDownBar`) |
| Partial | Swallow, mine, cyclone, and sit-down healing branches | The simulator now auto-deploys mine summons from learned `33101008` only while the player is in the Wild Hunter jaguar riding family, suppresses that local `TryDoingMine` loop in town maps, and now mirrors the client startup cadence more closely by seeding the first continuous-movement deploy from the client-observed `update_time - 1000` lead before returning to the steady `1500 ms` mounted-movement interval. Mine movement intent is now also sourced from the same held left/right input direction the client reads before falling back to resolved foothold velocity, so brief startup acceleration no longer stalls the first mounted mine check. Cyclone still sustains as a timed `32121003` body-attack loop with `1000 ms` pulses only while the cyclone action remains active, sit-down healing still loads its support range from the summon branch metadata and honors WZ `minionAbility=heal` plus `condition=whenUserLieDown` only while the sitting player remains inside that heal box, `35111011` recovery still uses its WZ-authored HP-percent heal plus the skill text's `x=5` second re-heal lock, and the local plus packet-owned remote healing-robot seam now mirrors `CUserLocal::TryDoingSitdownHealing` more closely by polling those support summons every seated frame instead of waiting on the simulator's generic summon `attackAfter` fallback before the first heal attempt. Successful `35111011` heals now also arm the authored heal-branch support-suspend window in both the local support seam and the packet-owned remote summon skill-action seam, so those robots stop re-entering support while the current heal playback is still in flight, closer to the client's `CSummoned::TryDoingHealingRobot` `m_nSuspend` seam. This pass tightens that same support-suspend seam again against WZ `Skill/3511.img/skill/35111011/summon/heal`: local summon actor playback now measures named summon-branch duration instead of only the generic summon attack clip when deciding whether the robot is still in `Prepare` or `Attack`, and both the local plus packet-owned healing-robot runtimes now clear the authored support suspend as soon as that heal branch returns to idle instead of holding the suspend only on a stale generic-attack or fixed-timer approximation. The same heal/support seam now also consumes nearby packet-owned remote Healing Robots from broader remote-owner state instead of limiting sit-down healing to party-owned summons when another actor owns the robot. Swallow-family detection and ladder/rope gating still stay on loaded WZ metadata only (`info/swallow` plus `dummyOf`), and the swallow cancel/absorb-ack seam still accepts the visible Wild Hunter skill (`33101005`) plus its WZ-linked dummy follow-up ids (`33101006`, `33101007`). Wild Hunter swallow also still follows the client's `TryDoingSwallowMobWriggle` / `TryDoingSwallowBuff` timing more closely after absorb confirmation: the visible skill still uses the `900 ms` keydown gauge before release, early release still cancels without executing, release-triggered payloads still consume cost only once the charge completed, the local absorb-ack seam still gates capture success, a successful absorb still automatically arms the `3000 ms` digest timer plus `500 ms` wriggle cadence instead of waiting for a second local digest cast, failed or invalidated absorb outcomes now route back through the same client cancel seam used by the other swallow exits instead of silently dropping local state, and the hidden attack dummy branch (`33101007`) still consumes the captured mob before firing its follow-up ranged payload. The simulator still does not mirror the client's exact swallow packet ordering, the exact server-owned capture/digest request choreography, the client `TryDoingMine` riding-vehicle-id ownership nuances beyond the simulator's current jaguar/town gating, the remaining packet/request-owned Healing Robot suspend choreography beyond the new authored-branch idle clear, or the remaining non-Wild-Hunter swallow-family nuances one-for-one | Several client-only active-skill branches now execute in play instead of falling through the generic cast path, Wild Hunter mine is closer to `CUserLocal::TryDoingMine` because it now respects the client town suppression and input-owned movement-start detection in addition to the existing jaguar-riding gate and first-deploy cadence, sit-down healing follows the authored area, percent-heal, and re-heal cadence instead of restoring on every support pulse, now also considers nearby packet-owned remote healing robots through the same support-range and re-heal-lock seam the local summon runtime uses even when those robots are owned by non-party remote actors, no longer stalls the first seated heal attempt behind the simulator's generic support-summon interval gate when the client would already be frame-polling `35111011` support, and now holds successful healing-robot support inside a short authored-action suspend window instead of immediately allowing the same summon to re-enter the heal branch while that playback is still active, even when the remote summon entered that heal playback from a packet-owned `OnSkill` action first. The local summon runtime is closer again because named support branches such as Healing Robot `heal` now own their actual WZ playback window when resolving local summon actor state, and both local plus packet-owned healing robots now drop that suspend when the heal branch genuinely returns to idle instead of waiting on a looser generic-attack approximation. Cyclone no longer keeps pulsing once its sustained cyclone action has already fallen off the avatar, and Wild Hunter swallow is closer to the client's visible `33101005` input loop because charge threshold, early cancel, absorb success, absorb failure/target-loss teardown, and auto-started wriggle timing plus auto-digest completion now all ride the same release-owned swallow state and shared client cancel seam instead of requiring a second local digest trigger or leaving failed captures on a silent local clear. Swallow-family gating is also narrower and more data-backed now because false positives from skill names or action names no longer enter the swallow lane, and swallow cancel/ack callers can target the WZ-linked dummy follow-up ids directly instead of hardcoding the visible skill id. The remaining parity gap is now centered on exact swallow packet sequencing, the server-owned swallow request/ack ordering around `33101005` / `33101006` / `33101007`, the client `TryDoingMine` riding-vehicle-id ownership nuances beyond the simulator's current jaguar/town gating, the remaining packet/request-owned Healing Robot suspend choreography that still lives outside the simulator's current authored-branch idle-clear seam, and any residual swallow-family behaviors the client owns outside the current local cancel/request seam | `SkillManager`, `SkillLoader.cs`, `SkillData.cs`, `PlayerCharacter`, `MapSimulator`, `SummonedPool.cs`, `UnitTest_MapSimulator/SupportSummonParityTests.cs` (`CUserLocal::TryDoingSwallowBuff`, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::TryDoingMine`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingSitdownHealing`, `CUserPool::DoHealNearHealingRobot`, `CSummoned::TryDoingHealingRobot`, `Skill/3511.img/skill/35111011/{info,common,summon/heal/info}`) |
| Partial | Summon simulation | Summon-family skills still keep the broader parity work already landed here: generic summon lifecycle/timers, WZ-backed attack branch and metadata loading, projectile and target-impact branch retention, Tesla triangle targeting, self-destruct prepare/die handling, packet-owned prepare/attack visual parity, taunt-only puppet registration, shared summon rule selection between local and packet-owned seams, and the current movement/gating buckets for stationary, grounded-follow, owner-hover, and anchor-bound summons. This run closed another client-backed Tesla gap on top of the earlier Mechanic support fixes. IDA for `CSummoned::TryDoingTaslaCoilAttack` shows Tesla impact timing is driven by the summon attack-delay window itself, with a fixed `300ms` floor plus per-target jitter inside the remaining window, rather than by per-target projectile travel distance; both the local `SkillManager` seam and the packet-owned `SummonedPool` seam now follow that rule instead of stretching Tesla impact timing by distance. The Tesla projectile/presentation seam is closer again: local Tesla attacks now cache the same three triangle points in summon-owned state that the packet-owned create/runtime seam already consumes, local Tesla beam visuals now resolve their source points from that cached packet-shaped triangle data instead of recomputing purely from live summon positions during projectile spawn, and local summon projectile visuals still keep the summon object id that owns them instead of falling back to owner `0`. Generic local summon impacts are closer too because the simulator no longer batches every target onto one furthest-target delay window: queued summon damage now resolves per target with its own authored hit-delay/projectile-delay timestamp, and due summon impacts are processed in ascending execute-time plus target-order sequence instead of the older reverse list walk. The packet-owned runtime also keeps same-tick delayed hit overlays in insertion order now instead of reversing those hit effects when multiple scheduled impacts mature together. The earlier WZ-backed Mechanic support fixes remain in place: hostile support summon classification now follows `String/Skill.img` instead of the older blanket `minionAbility=mes` shortcut, so Acceleration Bot EX-7 (`35111005`) no longer leaks into the friendly support lane; EX-7 now consumes its authored speed-up payload as a positive mob `Speed` status while still lowering DEF; genuinely party-facing support summons still route through the dedicated friendly-support path; and AF-11 (`35121010`) still mints a persistent summon-owned damage-buff state from its authored `Damage Buff: +#x%` payload instead of falling through the hostile mob-status branch. The remaining parity gap is narrower but still real: the simulator still does not reproduce the full client-exact local Tesla beam ownership/action packet payloads from `CSummoned::TryDoingTaslaCoilAttack` and `CSummoned::OnSkill` / `OnAttack`, the octopus-specific beam and any remaining client-only projectile presentation beyond the WZ-backed `attack*/info/ball` data now loaded, the last grenade-style expiry choreography beyond authored prepare/die playback, the exact remote `OnSkill` / `OnAttack` / `OnHit` choreography for every summon family beyond the current ordered impact handling, the last client-only assist-target special cases not surfaced by WZ `minionAbility` / `condition` / packet assist bytes, the final one-for-one mob aggro retarget timing heuristics, and a fuller official-client treatment for party damage amplification beyond the simulator's current summon-owned damage-buff seam. | Summon-heavy jobs are now closer not just on authored support polarity but also on Tesla combat feel and multi-target hit timing: EX-7 and AF-11 still follow the correct hostile-vs-friendly authored support lane, Tesla impacts no longer drift with target distance because both local and packet-owned runtimes now use the client-shaped fixed-delay-plus-jitter window, local Tesla beam visuals preserve the attack triangle in the same packet-shaped point form already used by remote summons, and ordinary summon projectile or hit timing no longer stretches every target onto one max-distance batch delay. Packet-owned delayed hit overlays also stop flipping same-tick order when multiple summon impacts mature together. Remote-player support projection still stays safer because party-facing support collection no longer picks up the hostile `mes` robot as if it were a teammate aura. | `SkillManager.cs`, `SummonedPool.cs`, `RemoteAffectedAreaSupportResolver.cs`, IDA `CSummoned::TryDoingTaslaCoilAttack` (`0x752780`), IDA `CSummoned::OnSkill` (`0x74a940`), IDA `CSummoned::OnAttack` (`0x753340`), WZ evidence from `String/Skill.img/35111005`, `String/Skill.img/35111002`, `String/Skill.img/35121010`, `Skill/3511.img/35111002/{common,info,summon,ball}`, `Skill/3511.img/35111005/{common,info}`, and `Skill/3512.img/35121010/{common,info}` |
| Partial | Remote summoned-pool packet parity | The simulator still keeps the packet-owned summon transport seam in `SummonedPool`, but the hit side is closer to the client than before: it already mirrors the observed `0x116`-`0x11B` packet family, local versus remote owner registration, packet-owned move-path sampling, Tesla third-coil refresh, taunt-side ownership, packet hit damage bookkeeping, mob-authored hit-overlay lookup, and the loader/runtime handling for nested `attack/info/hit` metadata with UOL resolution plus the observed `attach` and `attachfacing` aliases. Earlier passes had already split positive-damage fallback away from the live summon branch by snapshotting the remaining authored branch window into a packet-owned one-time-action clip, keeping named summon branches on their authored timelines without a synthetic extra `prepare` prefix and letting non-Tesla summons already sitting in `Prepare` stay on authored `summon/prepare` during fallback instead of jumping straight to idle or attack. A later pass then closed a concrete `CSummoned::PrepareActionLayer` presentation gap inside that same seam by honoring authored per-frame alpha ramps on both the live summon branch and the detached one-time-action clip instead of forcing every summon frame to draw fully opaque; that change was grounded in both IDA and WZ evidence (`CSummoned::PrepareActionLayer` inserts each action-layer canvas with authored `a0` / `a1`, and WZ summon branches such as `Skill/1211.img/skill/12111004/summon/die/11` publish `delay=120`, `a0=255`, `a1=0`). This run tightens the existing `MobAttackInfo::sHit` / `bHitAttach` loader seam without widening transport ownership: mob attack metadata still scans authored per-frame `attack/<frame>/hit` nodes in addition to `attack/info/hit`, but explicit top-level `info/hit` flags now keep priority over deeper frame-hit aliases while frame-hit metadata stays available as fallback, and the render-node resolver now keeps walking per-frame `attack/<frame>/hit` when the top-level `info/hit` node is missing or non-renderable. That matches the intended client-shaped ownership split more closely for mixed surfaces such as `Mob/5220000.img/attack1/info/hit/attachfacing`, hidden deeper attach cases such as `Mob/3220000.img/attack1/13/hit/attach=1`, and alias-only top-level cases such as `Mob/5160000.img/attack1/info/attach`, `Mob/6090002.img/attack1/info/attach`, and `Mob/9300525.img/attack1/info/attachfacing` without letting a later frame override explicit authored top-level metadata. Focused loader tests are now on disk for this seam, covering explicit `info/hit` precedence over deeper frame metadata, info-alias `attachfacing -> bHitAttach` fallback, and render-node fallback when `info/hit` is missing or empty. The remaining gap is narrower but still real: the simulator still does not synthesize every hidden client `MobAttackInfo::sHit` / `bHitAttach` case one-for-one beyond the currently observed `info/hit`, info-level alias, and per-frame `attack/<frame>/hit` WZ surface, it still approximates the client action-layer stack with a detached draw overlay instead of reproducing the literal GR2 action-layer canvas ownership and ordering, and `/summonedpacket` plus `SummonedPacketInboxManager` still expose a simulator loopback seam instead of full official `CUserPool` transport and ownership decode. `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter LifeLoaderAttackHitMetadataTests --no-restore` is still blocked in this workspace by unrelated pre-existing `HaCreator` compile failures in `MapSimulator/Interaction/SocialRoomModels.cs(468)`, `MapSimulator/UI/Windows/LoginCreateCharacterWindow.cs(17)`, `MapSimulator/UI/Windows/MapTransferUI.cs(12)`, `MapSimulator/UI/Windows/LoginTitleWindow.cs(29)`, and `MapSimulator/UI/Windows/LoginUtilityDialogWindow.cs(14,131)`. | Packet-owned summons are no longer just spawn/remove stubs: they follow the observed client summon packet family, keep summon ownership ordered per user, route local-owner taunt and Tesla side effects through the same created seam, replay summon attack/hit/removal visuals, decode hit payloads far enough to show the mob-authored overlay and sound feedback from the attacking mob template, and tear down with remote-user lifetime instead of leaving orphaned field actors behind. Hit parity is closer again because the loader/runtime pair now covers nested `attack/info/hit` metadata with UOL resolution, lower-case alias support, explicit `attach` handling, client-shaped `attachfacing`-only `bHitAttach` cases, info-level `attach` / `attachfacing` aliases, explicit `info/hit` precedence over deeper frame-hit metadata, render-node fallback from missing or empty `info/hit` into authored per-frame `attack/<frame>/hit`, the cached WZ frame-origin anchor math that keeps packet-owned summon hit overlays visually aligned with the live summon, the same local-runtime summon playback split for named attack branches plus non-Tesla `Prepare` state, authored remaining-window progression when positive-damage fallback has to stand in for the client hit branch, a detached packet-owned one-time-action playback clip that no longer mutates the live summon branch in place, and authored per-frame alpha ramps while those summon clips are replaying. The remaining parity work is concentrated in the last client-only `sHit` / `bHitAttach` cases outside the currently observed WZ surfaces, the literal client GR2 action-layer ownership and ordering beyond this alpha-correct detached clip, and end-to-end official `CUserPool` transport rather than owner teardown, nested hit metadata load, packet-owned branch timing, or summon-frame alpha playback being missing outright. | summon field/pool runtime (`CSummonedPool::OnPacket`, `CSummonedPool::OnCreated`, `CSummonedPool::OnRemoved`, `CSummonedPool::OnMove`, `CSummonedPool::OnAttack`, `CSummoned::OnHit`, `CSummoned::PrepareActionLayer`, `CUser::AddSummonedList`, `CMobTemplate::LoadAttackInfo`, `Skill/3511.img/skill/35111002/summon/{prepare,attackTriangle,hit}`, `Skill/3511.img/skill/35111011/summon/{stand,heal}`, `Skill/1211.img/skill/12111004/summon/die/11/{delay,a0,a1}`, `Mob/5220000.img/attack1/info/hit/attachfacing`, `Mob/3220000.img/attack2/info/hit/attachfacing`, `Mob/3220000.img/attack1/13/hit/attach`, `Mob/5160000.img/attack1/info/attach`, `Mob/6090002.img/attack1/info/attach`, `Mob/9300525.img/attack1/info/attachfacing`, `UnitTest_MapSimulator/SummonedPoolOneTimeActionParityTests.cs`, `UnitTest_MapSimulator/LifeLoaderAttackHitMetadataTests.cs`) |
| Partial | Summoned actor update/runtime parity | The simulator now gives active summons a dedicated actor-update pass inside `SkillManager` instead of treating them as attack timers only: each summon snapshots previous versus current world position before and after the movement step, keeps active follow movement separate from passive stationary/anchor-bound updates, routes visible state through summon-owned spawn/prepare/attack/hit/die actor states, drives Tesla Coil idle ownership through the WZ `summon/prepare` branch instead of collapsing the armed state into generic stand timing, lets self-destruct summons fire their final attack/support/action branch once their authored duration expires before entering removal playback, and uses previous-to-current summon bounds to resolve body-contact hits against mobs already targeting taunt summons before marking those summons damaged or removed. This run keeps the remaining named-branch interrupt seam inside summon-owned runtime as well: when a packet-owned summon receives a new attack packet, skill packet, self-destruct final-action handoff, or removal start while a packet-owned hit one-time clip is still active, the pool now clears that stale one-time overlay first so the authored summon branch can take ownership immediately, matching the client `CSummoned::OnSkill` / `CSummoned::SetAttackAction` handoff instead of letting an old hit fallback continue drawing over `heal`, `subsummon`, `skill1`..`skill6`, or removal playback. The earlier WZ-backed named-branch timing fix remains in place too, so summons already playing authored local branches such as Healing Robot `heal` (`Skill/3511.img/skill/35111011/summon/heal`), Support Unit H-EX `subsummon` (`Skill/3512.img/skill/35121009/summon/subsummon`), and Beholder `skill1`..`skill6` still do not inherit generic `summon/prepare` delay before packet-owned playback or self-destruct expiry scheduling. The packet-owned `CSummoned::Update` seam also stays tightened against both WZ and client evidence: packet-owned summons keep their authored `summon/summoned` spawn ownership until the spawn animation window really finishes instead of collapsing straight to generic idle on the first settle tick, and Tesla Coil keeps the same prepare-owned armed presentation before its first authored attack playback even when no prior attack timestamp exists yet. This pass narrows the summon-damage mismatch inside that same owner seam using direct `CSummoned::SetDamaged` evidence: local summon body-contact hits no longer subtract a fixed `1` HP, but now derive their base damage from the mob's authored combat surface (`Mob.wz` physical damage first, then current attack damage, then magical fallback) before running that through the existing mob outgoing-damage modifiers, and local summon hits now emit the same visible inc/dec HP feedback that packet-owned summon hits already use instead of keeping local summon HP loss hidden. Shared summon-health bookkeeping now also resolves through one helper in both the local and packet-owned runtimes so fallback-to-max and floor-to-zero behavior no longer drift between the two paths. Focused regression coverage for the new helper is now on disk in `UnitTest_MapSimulator/SummonDamageRuntimeRulesTests.cs`. The remaining gap is narrower again: the simulator still approximates the client's deeper hidden summon HP and exact damage formula because it does not mirror the full `CalcDamage::PDamageSummoned` / `MDamageSummoned` path, still only mirrors the confirmed local `CVecCtrl::UpdatePassive` effect-trigger hook rather than the rest of that passive update path, still only models Tesla states and refresh rules through the confirmed armed-idle seam plus this initial prepare fallback instead of the deeper state machine, and still lacks the assist-family remote branch choices whose exact selection depends on client-only state not exposed by the current packet/WZ view | This keeps more of the real `CSummoned::Update` seam inside summon-owned runtime instead of collapsing expiry or assist playback back onto generic attack/removal timing: summon-heavy jobs now keep actor-owned movement, Tesla prepare-state presentation, hit playback, hit-period flashing, passive settle-effect feedback, post-expiry self-destruct timing, summon-contact teardown, previous/current position sync, summon-owned named assist animation playback, the Beholder heal/buff split inside one per-frame seam, the confirmed packet action-to-branch mapping for remote Beholder heal vs buff lanes, Healing Robot heal playback, authored subsummon families, the delayed self-destruct handoff that preserves authored final support or subsummon branches before removal playback begins for both local and packet-owned summons, the client-shaped rule that summon-owned named branches do not inherit generic prepare timing when they already publish their own playback, the client-confirmed interrupt rule that packet-owned skill/attack/removal ownership clears an older one-time hit overlay before the new summon-local branch is drawn, the client-shaped rule that packet-owned spawn/Tesla idle fallback does not drop to generic idle before the summon-owned spawn or armed-prepare presentation has had a chance to finish, and now the local summon-damage path no longer hides body-contact HP loss behind a fixed `1`-damage placeholder with no HP feedback. The remaining mismatches are concentrated in the deeper hidden HP or exact summon damage formula, broader passive-update side effects, deeper Tesla transitions beyond the confirmed armed-idle states, and only the assist special cases whose exact remote branch still depends on packet/state detail the simulator does not yet receive | summon actor/runtime layer (`CSummoned::Update`, `CVecCtrl::UpdateActive`, `CVecCtrl::UpdatePassive`, `CSummoned::OnSkill`, `CSummoned::SetAttackAction`, `CSummoned::SetMoveAction`, `CSummoned::ProcessAction`, `CSummoned::ProcessAttack`, `CSummoned::SetDamaged`) |
| Partial | Weapon afterimage / melee trail parity | The simulator still covers the broader WZ-backed afterimage surface already landed for this row: skill-authored plus shared `Character/Afterimage/<family>/<masteryIndex>` catalogs, weapon-item `info/afterImage` family selection with narrow sword-sibling fallback only, local basic-melee registration, charged `charge/<element>` rewrites for the confirmed Page, Paladin, Thunder Breaker, and Aran families, alias-aware action matching, `1221009 blast -> stabO2`, client-backed raw-range normalization for `1221009`, `57 -> 41`, and `74`, remote afterimage playback with fade tails, ordered remote WZ action fallback, and the single-authored plus sole-renderable remote pose promotion seams. Earlier on this row, `RemoteUserActorPool` also closed the active-charge fallback hole with direct `RegisterAfterimage` evidence by recovering the known charged Page, Paladin, Thunder Breaker, and Aran buff ids from the remote actor's last official temporary-stat payload when an official remote melee packet does not carry an explicit `chargeSkillId`, keeping those swings on the same shared `Character/Afterimage/<family>/charge/<element>` surfaces the client would register instead of dropping back to the uncharged mastery catalogs. This run tightens that same remote charged-melee seam instead of widening it with another payload-wide heuristic: `RemoteUserPacketCodec` now keeps a mask-aware `ChargeSkillId` in `RemoteUserTemporaryStatKnownState`, capturing the confirmed charged-melee reason from the scoped `WeaponCharge` remote temporary-stat payload slice and clearing it through the existing reset-mask path, while `RemoteUserActorPool` consults that structured known-state value before falling back to the older raw-payload scan. The official remote-melee transport seam is also closer to `CUserRemote::OnAttack`: `RemoteUserMeleeAttackPacket` now preserves the client `hitCount`, `damagePerMob`, `actionSpeed`, and serial-attack flag from the official packet body instead of discarding them during decode, keeping those envelope fields available in the existing shared codec rather than buried in the packet reader. Focused tests are now on disk in `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` for scoped `WeaponCharge` decode, reset-mask clearing of that known-state charge owner, and official remote melee decode for the client envelope fields on a representative `1221009` packet. Executing `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter WeaponAfterimageClientParityTests --no-restore -v minimal` on April 3, 2026 is still blocked in this workspace by unrelated pre-existing `HaCreator` compile failures outside this row in `MapSimulator/Interaction/SocialRoomModels.cs(468)`, `MapSimulator/UI/Windows/LoginTitleWindow.cs(29)`, `MapSimulator/UI/Windows/LoginUtilityDialogWindow.cs(14,131)`, `MapSimulator/UI/Windows/MapTransferUI.cs(12)`, and `MapSimulator/UI/Windows/LoginCreateCharacterWindow.cs(17)`. The remaining gap is narrower again but still real: the client raw-range surface is still limited to the confirmed `1221009`, `57 -> 41`, and `74` seams; the temporary-stat fallback still only understands the already confirmed charged melee buff ids instead of a fuller remote secondary-stat decode; the simulator still approximates the real `CreateAfterimageLayer` / `RegisterAfterimage` GR2 choreography with forward-frame plus fade-tail playback; and the broader remote field packet surface still needs the rest of a one-for-one `CUserRemote::OnAttack` / `CUserRemote::OnMeleeAttack` transport lift beyond the now-preserved attack envelope fields | Many melee jobs rely on afterimage trails and authored swing ranges to sell timing and hit shape, and the simulator now consumes the same WZ-backed trail and arc data for local melee skill playback, local basic melee attacks, and simulator-owned remote actors while respecting the weapon item's authored afterimage family instead of forcing every sword through the long-family arcs. Charged melee buffs now stay on a less heuristic remote seam: official remote melee traffic without an explicit charge id can still recover the confirmed charged Page, Paladin, Thunder Breaker, and Aran families from the remote actor's temporary-stat state, and that recovery now prefers a mask-aware `WeaponCharge` decode path before touching the broader raw payload. The codec also now preserves more of the official remote melee attack envelope the client actually decodes, which narrows the remaining transport gap to the deeper packet body and client-owned side effects instead of still dropping those basic envelope fields on the floor. The remaining visible gap is now concentrated in the broader raw-range table, fuller remote secondary-stat decode, exact GR2 afterimage choreography, and the rest of the official remote melee packet body | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `CharacterLoader.cs`, `CharacterAssembler.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `CharacterData.cs`, `MapSimulator.RemoteUsers.cs`, `ClientMeleeAfterimageRangeResolver.cs`, `AfterImageChargeSkillResolver.cs`, `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` (`CActionMan::Init`, `CActionMan::GetWeaponAfterImage`, `CActionMan::CreateAfterimageLayer`, `CActionMan::GetMeleeAttackRange`, `get_action_name_from_code`, `CUser::GetAfterimageUOL`, `CUser::RegisterAfterimage`, `CUserRemote::OnAttack`, `CUserRemote::OnMeleeAttack`, `Character/00002000.img`, `Character/Afterimage/*`, `Skill/122.img/1221009/{action,afterimage}`, remote temporary-stat payloads) |
| Partial | Skill cooldown UI parity | QuickSlot overlays and tooltips, both skill-book variants, the hotkey and off-bar cooldown trays, the `UI/Basic.img/Notice3` off-bar notice stack, the shared packet-owned/server-owned cooldown ownership seam in `SkillManager`, and the Battleship sentinel durability-side effect path are all implemented in the existing cooldown UI/runtime seams. This run adds the missing focused regression file `UnitTest_MapSimulator/SkillCooldownParityTests.cs` to lock the shared cooldown seam against the client-backed cases that packet-owned refreshes preserve the original cooldown start tick, packet-owned clears tear the shared cooldown state down authoritatively instead of falling back to authored WZ cooldowns, repeated server refreshes do not re-fire the start-notice seam, and naturally expired server-owned cooldowns complete once without regressing to the authored WZ timer. The targeted test invocation for that file is still blocked in this workspace by unrelated pre-existing `HaCreator` compile failures in the soft-keyboard/login/social-room seams, so the row stays partial. The remaining parity gap is now limited to the client's exact cooldown-notification art family selection versus the current `Notice3` best fit, any last notice micro-offset or timing/fade differences beyond the shared 44px top-center seam, the exact `CTemporaryStatView`/temporary-stat HUD presentation for the Battleship sentinel beyond the mounted durability override already modeled here, and any additional bespoke cooldown-only HUD widgets beyond the quick-slot, skill windows, status-bar trays, and off-bar notice stack. | Cooling-down skills now stay synchronized across quick-slot masks, both skill-book variants, both status-bar trays, hover tooltips, and off-bar started/blocked/ready notices because they all read the same authoritative cooldown seam instead of mixing packet-owned overrides with authored WZ fallback timing. The `Notice3` stack continues to use WZ-backed frame sizing and layout seams, packet-owned clears and natural expiry now stop all cooldown surfaces together, the Battleship sentinel keeps the mounted durability override behavior already landed in the runtime, and this run puts focused regression coverage for the shared cooldown ownership seam on disk even though executing it is presently blocked by unrelated `HaCreator` build errors outside this row. The simulator still does not reproduce the client's exact cooldown-notification art choice, final notice timing/micro-offset polish, exact Battleship temporary-stat HUD visuals, or any extra cooldown-only HUD widgets that may still live outside the currently modeled shared seams. | `QuickSlotUI.cs`, `SkillUI.cs`, `SkillUIBigBang.cs`, `UIWindowManager.cs`, `StatusBarUI.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `SkillManager.cs`, `SkillCooldownNoticeUI.cs`, `UnitTest_MapSimulator/SkillCooldownParityTests.cs` |
| Partial | Client-timer skill expiry and cancellation | `SkillManager` now owns a centralized client-timer registry that drives buff expiry, timed prepared-skill release, mechanic repeat-sustain expiry, cyclone teardown, Wild Hunter swallow digest completion, WZ-timed local skill-zone expiry, and WZ-timed local summon expiry through one `UpdateClientTimer`-style update seam; the runtime now also publishes ordered timer-expiry batch snapshots that mirror the client's packed expired-timer list more closely, normalizes client cancel requests through WZ-backed `info/type` + `affectedSkill` mappings for affected-skill families such as `32110000 -> 32101002`, `32120000 -> 32001003`, and `32120001 -> 32101003`, now also covering the wider `affectedSkill` cancel surface that WZ publishes outside the old `type=50/51` subset, including observed `type=16`, `type=33`, `type=1`, and now `type=10` cases such as `2111007 -> 2101002`, `32111010 -> 32001002`, `35111009 -> 35111001`, `35111010 -> 35111001`, `35101009 -> 35001001`, and `32110007 -> 32001003` (with the same WZ pattern also present on `32110008` and `32110009`), and now parses multi-target `affectedSkill` strings such as `21120011 -> 21100002&&21110003` instead of collapsing those authored families back to `0`. It also folds WZ-authored `info/dummyOf` families such as `33101005 -> 33101006&&33101007`, `32111005 -> 32111007&&32111008&&32111009`, and `35100008 -> 35101009&&35101010` back onto their parent cancel id, and routes manual status-bar buff cancellation through that same cancel-request seam with simulator time instead of bypassing it. That shared cancel path now tears down matching prepared-skill state, active buffs, cyclone sustain, repeat-sustain state, active summons, and local skill zones while preserving the existing swallow reset behavior; dummy-child cancel requests can now also tear down parent-owned prepared/key-down state through the same shared matcher instead of only clearing swallow-local bookkeeping, zone skills whose WZ publishes both `info/zoneType` and timed `common` / `level` duration data now expire through the same timer-owned registry instead of only through per-frame local polling, local summons whose WZ publishes timed `common/time` data such as Mechanic summon families now also register per-instance timer-owned expiry keys instead of aging out only through per-frame `Duration` polling, tank-siege cancel now reuses the local pending mode-end latch instead of clearing immediately, swallow-buff state teardown still rides the timer-owned buff expiry path, swallowed-mob loss during Wild Hunter digest still emits the client-style cancel-request hook instead of silently dropping state, and `SendSkillCancelRequest` special-case consumers are now honored even when no tracked local buff or prepared-state object matches the cancel id so vehicle-valid bookkeeping and keydown prepare-animation cleanup no longer get skipped behind the local-state teardown gate. This pass extends that same WZ-backed cancel normalization into the packet-owned summon runtime: `SummonedPool` now resolves affected-skill and `dummyOf` families through the shared cancel matcher before tearing down local-owner packet summons, so client cancel requests emitted by the local skill seam can remove packet-owned summons through the same parent/child family rules instead of leaving those summons outside the cancel surface, and the packet-owned summon pool now also publishes ordered per-instance summon-expiry batch snapshots before processing its WZ-timed removal queue rather than keeping the full expiry list private to the pool update loop. The local packet-owned summon seam also closes another timer-ownership hole: when the packet create data cannot resolve a valid WZ skill level, `SummonedPool` now falls back across the full connected local cancel family instead of only the packet skill id, so WZ-linked child summons can recover a learned parent-family level before duration evaluation. Packet-owned local summons whose WZ record has no authored duration now evaluate that inherited local cancel-family surface before the runtime's generic untimed fallback, and that inherited surface now also includes remaining local summon-instance lifetimes instead of only buff timers. This run closes another real hole inside that same seam: the shared resolver now walks the full connected WZ cancel graph instead of only a single authored edge, so mixed families that combine `affectedSkill` and `dummyOf` links such as `35101009 -> 35001001` plus `35100008 -> 35101009&&35101010` now normalize together whether the cancel request starts from the source skill, a dummy child, or the affected target, and multi-target `affectedSkill` families such as `21120011 -> 21100002&&21110003` still fan out across every authored branch instead of only the first resolved id. Another timer-ownership hole is now closed in that same packet-owned summon seam using WZ evidence first: when a local packet summon is created from a dummy or affected child whose own WZ node has no usable level entry or `common/time`, `SummonedPool` now keeps walking the full connected cancel family and can inherit a learned family level plus authored family duration before degrading to the simulator's generic untimed fallback. That same inheritance seam now covers the observed `type=10` WZ cancel family as well, so child packet summons linked through `32110007 -> 32001003` can recover the learned parent-family level and authored duration instead of falling straight to the generic fallback because the resolver ignored that WZ-published family type. That keeps WZ-linked families such as `35100008 -> 35101009&&35101010`, `33101005 -> 33101006&&33101007`, and `32110007 -> 32001003` on their authored family timing when the packet-owned child node is only a cancel/toggle owner, and focused regression tests are now on disk in `UnitTest_MapSimulator/PacketOwnedSummonedTimerParityTests.cs` for the `type=10` connected-family graph, connected-family level recovery, and connected-family authored-duration recovery. Executing `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter PacketOwnedSummonedTimerParityTests` is still blocked in this workspace by unrelated pre-existing `HaCreator` compile failures in `SocialRoomModels.cs` and the soft-keyboard UI seam plus unrelated pre-existing `UnitTest_MapSimulator` compile failures in older test files (`WhisperTargetValidationResult`, `NpcInteractionOverlayCloseKind`, `Physics`). The simulator still does not mirror the client's full per-skill timer catalog, especially packet-owned summon families whose duration ownership or expiry-side effects depend on client-owned state that is still outside the simulator's current local WZ + cancel-family timer view, deeper summon combat/runtime ownership than the pool currently tracks, and broader client-owned cancel side effects beyond those currently modeled remain incomplete | Timed skill teardown now has a real shared runtime surface instead of being split across isolated expiry checks, manual buff cancellation no longer bypasses the local cancel/request path, repeat/cyclone or zone-backed skill state can now be cleared through the same client-style request seam as prepared skills and swallow, WZ `dummyOf` child requests now normalize back onto the parent cancel family before teardown so dummy-linked prepared state follows the same path as active buffs and swallow state, WZ-timed local zones now also publish real client-timer expiry events instead of disappearing only through local age checks, local summon families backed by WZ `common/time` such as `35111011` and `35121009` now expire and self-destruct through the same timer registry with per-instance timer keys instead of a separate duration poll, and affected-skill cancel normalization stays data-backed across more of the real WZ surface instead of a narrow simulator allowlist. Observed non-`50/51` `affectedSkill` families and multi-id `affectedSkill` strings now normalize through the same shared seam, and a single cancel request now fans out across every resolved family id instead of only the first one; this run extends that fan-out across the whole connected WZ family graph, so authored combinations such as `35100008` / `35101009` / `35101010` / `35001001`, `21120011`, and the `type=10` `32110007` / `32110008` / `32110009` -> `32001003` family can tear down matching local or packet-owned state without leaving sibling or reverse-linked branches behind. Standalone cancel requests can still tear down ordinary active summons while driving the currently modeled vehicle-valid and keydown cleanup consumers even when the simulator does not have a matching tracked buff or prepared node to remove first, and those special-case consumers now also evaluate the full normalized cancel family rather than only a single resolved target id. Local-owner packet summons no longer sit entirely outside that cancel surface: a normalized client cancel request can now tear down packet-owned local summon instances using the same WZ-backed `affectedSkill` and `dummyOf` family mapping already used by `SkillManager`, packet-owned summon expiry is less opaque because the pool now emits ordered per-instance expiry batches before applying its removal path, and local-owner packet summons whose own WZ record does not publish a usable duration can now recover either a connected-family learned level, a running local family timer, or a connected-family authored duration before degrading to the generic fallback lifetime. Focused tests now lock in the newly covered `type=10` family seam plus the connected-family level and authored-duration recovery path, although the current workspace still cannot execute them because of unrelated compile failures outside this row. The remaining parity gap is narrower and centered on the last client-owned timer families, especially packet-owned summon-duration ownership and expiry-side effects that still depend on client-only runtime state beyond the simulator's current local WZ, packet create, and cancel-family timer view, plus any additional `SendSkillCancelRequest` consumers that still live outside the simulator's current list | `ClientSkillCancelResolver`, `SkillManager`, `SummonedPool`, `MapSimulator`, `UnitTest_MapSimulator/PacketOwnedSummonedTimerParityTests.cs` (`CUserLocal::UpdateClientTimer`, `CUserLocal::SendSkillCancelRequest`, `CUserLocal::TryDoingSwallowMobWriggle`, `CSummonedPool::OnCreated`, `CSummoned::Update`, `Skill/422.img/4221006/info/zoneType`, `Skill/422.img/4221006/common/time`, `Skill/3211.img/32110000/info/affectedSkill`, `Skill/3212.img/32120000/info/affectedSkill`, `Skill/3212.img/32120001/info/affectedSkill`, `Skill/211.img/2111007/info/affectedSkill`, `Skill/3211.img/32111010/info/affectedSkill`, `Skill/3211.img/32110007/info/affectedSkill`, `Skill/3211.img/32110008/info/affectedSkill`, `Skill/3211.img/32110009/info/affectedSkill`, `Skill/3510.img/35101009/info/affectedSkill`, `Skill/3511.img/35111009/info/affectedSkill`, `Skill/3511.img/35111010/info/affectedSkill`, `Skill/2112.img/21120011/info/affectedSkill`, `Skill/3310.img/33101005/info/dummyOf`, `Skill/3211.img/32111005/info/dummyOf`, `Skill/3510.img/35100008/info/dummyOf`, `Skill/132.img/skill/1321007/common/time`, `Skill/3511.img/skill/35111002/common/time`, `Skill/3511.img/skill/35111011/common/time`, `Skill/3512.img/skill/35121009/common/time`, `Skill/3511.img/skill/35111001/common`, `Skill/3511.img/skill/35111009/info/affectedSkill`, `Skill/3510.img/skill/35100008/info/dummyOf`) |
| Partial | Skill restrictions by map/state | The simulator now enforces parsed map `fieldLimit` bits for `Unable_To_Use_Skill`, `Move_Skill_Only`, `Unable_To_Use_Mystic_Door`, the Mechanic-only `Unable_To_Use_Rocket_Boost` branch, and the mount / Mechanic vehicle `Unable_To_Use_Taming_Mob` branch through the shared skill runtime, rejects active skill use while the local player is dead, in hit-stun, seated, or prone so skill-window and hotkey casts no longer bypass those transient local states, mirrors the client's swallow-specific ladder/rope precondition in the shared evaluator, tears down client-timer / upkeep-driven skill paths such as keydown holds, cyclone pulses, mechanic mine auto-deploy, and repeat-sustain mode skills when those same local-state restrictions become invalid after cast start, now also feeds mob-disease lockouts sourced from WZ-authored `MobSkill.img` durations through that same shared state seam so direct casts, prepared-hold upkeep, repeat-sustain upkeep, cyclone, mine teardown, and Rocket Booster sustain all see `Seal` (`120`), `Stun` (`123`), `Attract` (`128`), the freeze family (`131`), and the WZ-authored polymorph pair (`172`, `173`) instead of leaving those branches split between cast-only booleans and isolated upkeep checks, routes simulator direction-mode / scripted-interaction ownership through that same shared state gate so skill-window or hotkey casts are rejected while NPC/script overlays own local control and active prepared/upkeep skills tear down once scripted control begins, widens the shared mount/vehicle classification to WZ-authored `eventTamingMob` riders plus broader client-owned vehicle buffs surfaced through `info/type=13` so `Unable_To_Use_Taming_Mob`, ladder/rope rejection, and airborne vehicle-start gating also catch non-Mechanic ownership cases such as Battleship and later event ride skills instead of a stale hardcoded subset, now also covers the client-confirmed ladder/rope veto for the Hermit/Shadower taunt branch (`4121003`, `4221003`) through the same shared evaluator instead of leaving those ids outside the common gate, now also routes live `MapInfo` ownership through the shared field restriction seam so skill casts see current map `fieldLimit` values plus the client-owned Coconut and Snowball minigame basic-attack-only wrappers instead of relying on a map-bind-time snapshot, and this pass widens the Coconut side of that seam to accept the authored root `coconut` map property instead of only `info/fieldType`, now also mirrors the WZ-authored root `noSkill` field-owner surface by rejecting casts when the live map forbids the local player's job branch via `noSkill/class` or names the exact skill id via `noSkill/skill`, and this pass widens that same `noSkill` seam to accept direct-int authored entries, exact current job ids, and the existing skill metadata / skill-book fallback path instead of only list-shaped children and derived class digits, and now also threads the WZ-authored Mu Lung Dojo `mobMassacre/disableSkill` flag through that same shared field gate so direct casts plus prepared/upkeep teardown see the same runtime prohibition instead of leaving Dojo-only skill lockout as HUD state. Deeper `CField` availability checks and the remaining field-owned or client-only forbidden-skill branches are still incomplete | Maps that explicitly suppress skills now block the same broad skill families in the simulator, Rocket Booster can be rejected on field entry and cast attempt and now also tears down if a shared external restriction begins after launch, seated/prone transient states no longer let active skills slip through the generic cast path, swallow casts now fail on ladder/rope like the client branch, ongoing upkeep skills stop once the player falls into a forbidden local or mob-disease state instead of continuing through it, the common mob-driven seal / stun / seduce, freeze, and polymorph lockouts now use the authored WZ timers and surface explicit rejection reasons through the same shared skill gate instead of only blocking fresh casts via side booleans or transform-only state, scripted direction-mode control now also blocks new active-skill requests while cancelling active prepared, repeat-sustain, cyclone, mine, and Rocket Booster upkeep through the same shared restriction seam instead of leaving those paths running under NPC or field-script ownership, the client taunt branch now also rejects ladder/rope casts through the shared evaluator instead of only the old swallow or vehicle subset, the `Unable_To_Use_Taming_Mob` plus client-side ladder/airborne vehicle checks now reject WZ-authored event mount skills together with broader client-owned vehicle ownership buffs such as Battleship or Jaguar Rider rather than only the earlier Mechanic-focused subset, client-owned Coconut/Snowball maps now reject active skills through the same shared seam even when the restriction needs to come from live map ownership rather than only a captured `fieldLimit` or `fieldType` value, WZ-authored root `noSkill` maps now also reject either whole job branches, exact listed skills, direct-int authored entries, exact current job ids, or the existing skill-job fallback path through that same live field seam, and Mu Lung Dojo massacre maps whose WZ publishes `mobMassacre/disableSkill=1` now reject fresh skill casts while also tearing down prepared or sustained paths through that same shared evaluator instead of only zeroing the HUD-side skill counter. Focused evaluator regression tests are now on disk for Coconut root-property ownership, Snowball root-property ownership, branch-list `noSkill/class`, direct-int `noSkill/class`, `noSkill/class` fallback through `SkillData.Job` and the skill-book id, direct-int `noSkill/skill`, and `info/noCancelSkill` cancellation blocking in `UnitTest_MapSimulator/FieldSkillRestrictionEvaluatorTests.cs`, although executing `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter FieldSkillRestrictionEvaluatorTests` is still blocked in this workspace by unrelated pre-existing `HaCreator` syntax errors in `MapSimulator.cs` around lines `23336-23622`. The remaining gap is still concentrated in the last `CField` gate branches and other per-skill client checks outside `IsSkillAvailable`, especially any additional field-owned owners beyond the now-modeled Coconut/Snowball, root `noSkill`, and Mu Lung Dojo `disableSkill` surfaces plus any remaining client-only forbidden-state branches beyond the currently modeled mob-disease / scripted-control / taunt-ladder set | `MapSimulator`, `SkillManager`, `FieldSkillRestrictionEvaluator.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `PlayerSkillBlockingStatusMapper.cs`, `PlayerCharacter`, `PlayerCombat`, `PlayerManager`, `PlayerMobStatusController`, `MassacreField.cs`, `MapInfo`, `MapInfo.fieldLimit`, `MapInfo.fieldType`, `FieldLimitType`, `FieldType`, `Skill/MobSkill.img`, `Map/Map1/109080000.img/coconut`, `Map/Map1/109080000.img/info/fieldType`, `Map/Map1/109060000.img/snowBall`, `Map/Map1/109060000.img/info/fieldType`, `Map/Map9/980031100.img/noSkill/class`, `Map/Map9/922240000.img/noSkill/skill`, `Map/Map9/910320100.img/mobMassacre/disableSkill`, `Map/Map2/200110000.img/info/noCancelSkill`, `UnitTest_MapSimulator/FieldSkillRestrictionEvaluatorTests.cs`, `Skill/412.img/4121003/info`, `Skill/422.img/4221003/info` (`CUserLocal::Update`, `CUserLocal::IsSkillAvailable`, `CField::IsUnableToUseSkill`, `CField_Coconut::BasicActionAttack`, `CField_SnowBall::BasicActionAttack`, `CField::IsMoveSkillOnly`, `CField::IsUnableToUseRocketBoost`, `CField::IsUnableToUseTamingMob`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::TryDoingSwallowAbsorb`) |

### 2. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

This helper-owner seam is currently under-tracked by the backlog even though both the client and simulator already expose concrete ownership here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow-partner and mirror helper actors | The simulator now models the client ownership split instead of treating every clone-family helper as the same WZ-backed shadow-partner path. `SkillData` / `SkillManager` recognize `Mirror Image` (`4331002`) as a distinct helper owner, `PlayerCharacter` keeps a separate mirror runtime from the existing `special/*` shadow-partner actor, and the update/render loop now drives mirror helper visibility from player action changes with a dedicated `m_tStartMirror`-style start timestamp, a 200 ms client-shaped rel-move window, and client-backed suppression for ladder, rope, morph, taming-mob, hit, and dead states. WZ re-check still confirms `skill/433.img/skill/4331002` exposes buff/effect data but no `special/*` helper action branch, so Shadow Partner keeps using WZ-authored helper animations while Mirror Image continues to source the live assembled avatar instead. This pass tightens the mirror source seam again against the targeted IDA re-check: `CharacterAssembler` classifies every assembled avatar part into explicit `UnderCharacter`, `OverCharacter`, `UnderFace`, `Face`, and `OverFace` render planes during assembly, portable-chair frame swaps preserve that plane ownership, and `PlayerCharacter` now keeps a dedicated mirror-owned prepared five-layer cache that reuses stable per-plane layer objects in the same five-layer order that `CUser::GetMirrorSrcLayer` exposes. The cache is still prepared from `UpdateMirrorImageRenderState` rather than only at draw time, the live local-player construction path still feeds `PlayerCharacter` the `GraphicsDevice`, and the prepared mirror-plane metadata now keeps the client-facing destination plane instead of flattening every prepared plane back onto one under-face-only presentation seam. That closes a concrete mismatch surfaced by the targeted `CUser::Update` re-check: the same five prepared mirror source planes are now inserted back through per-plane avatar render boundaries in `PlayerCharacter.DrawFrameWithAvatarEffects`, with missing-plane insertion falling forward to the next visible plane instead of collapsing the whole mirror stack onto the under-face point. Device-backed composed canvas textures plus cloned-part fallback remain in place, portable-chair layer swaps can still invalidate the affected mirror plane even when the live avatar frame index stays unchanged, and focused regression coverage is now on disk in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` for the missing-plane insertion order plus per-plane overlay-target ownership helper. The remaining gap is narrower and is now concentrated at the last client-only layer-object boundary inside `CUser::PrepareMirrorActionLayer` / `CUser::Update`: the simulator now keeps five prepared mirror planes with matching per-plane insertion ownership, but it still does not own real shared COM layer objects, true `InsertCanvas` reparenting against the live client layer graph, exact per-layer visibility or first-canvas synchronization driven by the source `IWzGr2DLayer` instances, or the follow-up clone-helper rules that depend on those real client layer objects rather than simulator-owned prepared descriptors, insertion indices, and textures. `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter MirrorImageLayerParityTests` is still blocked in this workspace by unrelated pre-existing `HaCreator` compile failures in `MapSimulator/Interaction/SocialRoomModels.cs(468)`, `MapSimulator/UI/Windows/LoginCreateCharacterWindow.cs(17)`, `MapSimulator/UI/Windows/LoginTitleWindow.cs(29)`, `MapSimulator/UI/Windows/LoginUtilityDialogWindow.cs(14,131)`, and `MapSimulator/UI/Windows/MapTransferUI.cs(12)`. | Clone-family jobs are not just tray icons or generic buff effects in the client; they own dedicated avatar-adjacent helper layers that participate in action preparation and per-frame user update. Tracking that seam explicitly keeps future parity work focused on helper-layer timing, ownership, and clone-family differentiation instead of accidentally treating shadow-partner and mirror-image visuals as solved just because the simulator already renders one WZ-backed helper actor | `PlayerCharacter.cs`, `CharacterAssembler.cs`, `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` (`skill/433.img/skill/4331002`, `CUser::PrepareActionLayer`, `CUser::PrepareShadowPartnerActionLayer`, `CUser::PrepareMirrorActionLayer`, `CUser::GetMirrorSrcLayer`, `CUser::Update`) |

### 3. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Time Bomb, Vengeance, and ExJablin packet-application parity | The simulator still owns this `CUserLocal::OnPacket` family inside the existing local-utility packet seam: `LocalUtilityPacketInboxManager` recognizes packet types `268` / `271` / `272` plus `timebomb` / `vengeance` / `exjablin` aliases, `MapSimulator.PacketOwnedUtilityParity` decodes and applies the three payload families, `OnExJablinApply` arms a simulator-owned next-shot flag that is consumed by the next ranged/projectile attack and forces that whole attack to crit, and packet-owned Vengeance/Time Bomb skill lookup still goes through the shared packet-skill alias seam so the confirmed legacy/current Vengeance pair (`3120010` -> `31101003`) resolves through one candidate path. `SkillManager.TryApplyPacketOwnedMeleeAttack` still owns the packet-action branch for Time Bomb, and this pass keeps that seam but closes one more client-owned behavior gap in `OnTimeBombAttack`: if the packet-owned melee-attack branch cannot resolve the local skill runtime or current WZ action, the simulator now still applies the packet-owned hit-period, invincibility-option merge, damage reaction, and impact side effects instead of aborting the whole packet before `ApplyInvicibleOption` / `SetImpact`-shaped fallout can land. The raw client action-code coverage also stays lifted across the full currently exported `Character/00002000.img` ordered base-body action roots, covering the previously missing tail from `ride2` / `flamethrower*` / `swingC*` / `shotC1` / `swingDb*` / `jShot` through `dualVulcanPrep` / `dualVulcanLoop` / `dualVulcanEnd` / `darkSpin` / `demonGravity` / `darkThrust`, so packet-owned melee raw action codes in that range now resolve to concrete action names first instead of immediately falling back through family or WZ/current-action heuristics. Focused source coverage is now on disk in `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs` for that extended raw-action tail, exact packet-owned melee action resolution on higher codes, the visible-potential inference and Time Bomb hit-period merge helpers, and the equip-level bucket helper that mirrors the client coat-option lookup. The remaining gap is narrower but still real: the packet-skill alias layer is still only populated with the currently confirmed Vengeance legacy/current pair rather than a broader cross-version packet-to-WZ catalog, and the Time Bomb coat-option inference is still intentionally scoped to the client-confirmed invincibility options rather than a general simulator-wide item-option-id population seam for arbitrary potentials. `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter PacketOwnedSkillApplicationParityTests` is still blocked in this workspace by unrelated pre-existing `HaCreator` compile failures outside this row, now including `MapSimulator/MapSimulator.CashServiceStageParity.cs` and `MapSimulator/UI/ClientTextRasterizer.cs` in addition to the earlier backlog note. | If these handlers stay hidden inside the broad cast rows, packet-owned skill applications that bypass the normal local input path will keep looking uncovered even though the client gives them their own dispatcher and follow-up state. The simulator now has a concrete owner for those packets, keeps the extended raw action table for packet-authored melee actions, and no longer drops the rest of the Time Bomb reaction path just because the attack animation branch could not resolve through the current packet-skill alias catalog or local skill runtime. The remaining risk is now concentrated in the still-small packet-skill alias catalog and the deliberately narrow visible-potential-to-item-option inference seam, not in missing raw action-code names for the current exported base-body action table or in prematurely aborting the Time Bomb reaction side effects when the attack branch fails to resolve. | packet-owned local skill-application layer (`CUserLocal::OnPacket`, `CUserLocal::OnTimeBombAttack`, `CUserLocal::OnVengeanceSkillApply`, `CUserLocal::OnExJablinApply`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::ApplyInvicibleOption`, `CUserLocal::SetImpact`, `MapSimulator.PacketOwnedUtilityParity`, `LocalUtilityPacketInboxManager`, `SkillManager`, `PlayerCharacter`, `CharacterPart`, `Character/00002000.img`, `Item/ItemOption.img`) |

### 4. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Combo counter / combo-command packet parity | `MapSimulator.PacketOwnedComboParity` now owns the simulator-side `OnIncComboResponse` bridge instead of leaving combo implied by the generic temporary-stat tray. The simulator decodes the packet-owned combo count from the combo inbox/chat transport, stores the owner count/timestamp, resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them on the same five-second alpha window, applies the client-matching fly-origin amplitude split across the least-significant digit run, and conditionally spawns the Aran combo-command attack/buff overlays with `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` first and WZ-backed named fallbacks for exports that only ship `ComboSmash`, `ComboDrain`, `ComboFenrir`, `ComboTempest`, and `ComboBarrier`. The row no longer depends on the generic `Combo` temporary-stat heuristics in `SkillManager`; it has a dedicated packet-owned HUD/effect owner, content load path, update lifetime, and draw path in `HaCreator/MapSimulator`. | If combo stays implied only by the temporary-stat tray, the backlog will overstate skill parity while missing the client's actual packet-owned combo counter, visible combo HUD/effect lifetime, and the extra combo-command visuals that hang off learned Aran skills. Tracking it explicitly keeps future work anchored to the real packet and draw owners instead of assuming the generic buff/cooldown surfaces already cover combo gameplay feedback. | packet-owned combo HUD/effect layer (`CUserLocal::OnPacket`, `CUserLocal::OnIncComboResponse`, `CUserLocal::DrawCombo`, `Effect/BasicEff.img/Combo`, `Effect/BasicEff.img/ComboCommand`) |

Buff icon plan:

1. Confirm asset sources and ordering:
   Use `UI/BuffIcon.img` for temporary-stat icon chrome and `Skill/*/icon` for per-skill icon sourcing where the client uses the skill's own icon, then validate row ordering and refresh cadence against `CUIStatusBar::Draw` and `CUserLocal::Update`.
2. Add a runtime-to-UI snapshot:
   Expose a small status-bar model from `SkillManager` that includes `skillId`, applied stat family, start time, duration, and refresh/replace semantics so the UI does not need to inspect buff internals directly.
3. Extend status-bar loading and drawing:
   Teach `UILoader` and `StatusBarUI` to load buff-slot art, render the active icon row in the client-aligned status-bar region, and reserve the interaction seam for later right-click cancel behavior.
4. Verify state synchronization:
   Check short-duration buffs, long-duration buffs, same-skill refresh, natural expiry, and `/job` cleanup so icon removal stays synchronized with the runtime and does not fight quick-slot cooldown overlays.


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. Summon actor/runtime pass:
   Close both the packet-owned `CSummonedPool` gaps and the `CSummoned::Update`-owned movement, touch-damage, Tesla-state, and position-sync gaps instead of only extending attack metadata and per-skill summon heuristics.
4. Clone-helper parity pass:
   Tighten the dedicated `CUser::PrepareActionLayer` / `CUser::Update` helper-layer seam for shadow-partner and mirror-image families instead of leaving clone behavior folded into generic buff or summon work.
5. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
6. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
7. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
