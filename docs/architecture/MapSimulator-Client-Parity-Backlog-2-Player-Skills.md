# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`
- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`
- `CAnimationDisplayer::Effect_SkillUse` at `0x459100`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Player skill execution parity

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CSummonedPool::OnPacket` at `0x75ac70`
- `CSummonedPool::OnCreated` at `0x75a9a0`
- `CSummoned::Update` at `0x757f70`
- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
`CUserLocal::Update` at `0x937330` orchestrates several of these paths and confirms that key-down gauge updates, queued follow-up attacks, prepared-skill execution, repeat-skill handling, teleport, rush, flying-rush, rocket-booster start/end handling, movement-coupled shoot branches, timer-driven cancellations, and status-bar feedback are coordinated in one local-player update loop rather than isolated one-off handlers. A second IDA pass also showed that summon parity splits across three owners rather than only the actor/runtime shell: `CSummoned::Update` drives summon actor motion, active/passive vec-ctrl updates, body-contact damage checks, assist-type dispatch, Tesla Coil state handling, self-destruct timing, and final world-position sync; `CSummonedPool::OnPacket` dispatches created/removed/move/attack/skill/hit packets and `CSummonedPool::OnCreated` ties each summon to its owning user, skill id, level data, and local-user registration path; and `CActionMan::{GetSummonedProp,LoadSummonedAction}` own the WZ-side summon UOL lookup, per-action frame build, delay/z metadata, reverse-playback duplication, and cached action-entry reuse before the runtime ever updates or draws that actor.

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::LoadSummonedAction` at `0x423100`
- `CActionMan::GetSummonedProp` at `0x41b830`

Notes:
The earlier backlog text tracked summon packet ownership and runtime behavior, but the targeted `CActionMan` scan shows another concrete client owner that was still missing from the document: summon action loading itself. `GetSummonedProp` resolves the summon UOL from `SKILLENTRY::GetSummonedUOL` through the resource manager, while `LoadSummonedAction` uses that property to resolve the action-specific node, enumerate each frame canvas, read per-frame `delay`, `z`, `origin`, and `rb` metadata, compute the frame bounds, and cache the assembled action entry under the summon action key. The same decompile also shows a distinct reverse-playback seam: when the source action node publishes the reverse flag, the client appends a reversed copy of the built frame list instead of leaving playback order entirely to the runtime. That means summon parity is not only `CSummoned::Update` timing and `CSummonedPool::OnPacket` ownership; it also depends on the loader that chooses the summon asset path, builds the frame table, and decides whether the runtime receives a forward-only or mirrored playback list.

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::Update` at `0x8fb8d0`

Notes:
This row remains `Partial`. The targeted re-check surfaced a client-owned helper seam that the backlog was not tracking explicitly, and the simulator now mirrors more of that ownership split instead of treating every clone-family helper as the same WZ-backed shadow-partner path. WZ was rechecked first for `skill/433.img/skill/4331002`: it still exposes `action/0 = alert` plus buff and effect data but no `special/*` helper action branch, which keeps `Mirror Image` (`4331002`) on the assembled-avatar helper seam while Shadow Partner continues to use the WZ-authored helper action catalog. Client evidence stayed second: `CUser::PrepareActionLayer` still calls `CUser::PrepareShadowPartnerActionLayer` and `CUser::PrepareMirrorActionLayer` immediately after the main avatar action layer is prepared; `CUser::GetMirrorSrcLayer` still returns the five avatar layer handles in fixed index order (`UnderCharacter`, `OverCharacter`, `UnderFace`, `Face`, `OverFace`); and `CUser::PrepareMirrorActionLayer` still recreates a mirror helper layer only when that helper layer is missing or its flip diverges from `m_pLayerUnderFace`, then re-parents every recreated mirror plane against `m_pLayerUnderFace`, copies origin from the live source layer, seeds the 200 ms `RelMove` from the live source-layer current time, and uses the live source-layer canvas instead of a per-piece redraw.

The simulator already kept the earlier landed ownership split and render seam for this row, and this pass narrows the remaining mismatch again: `SkillData` / `SkillManager` still recognize `Mirror Image` as a distinct helper owner, `PlayerCharacter` still keeps mirror runtime state separate from the `special/*` Shadow Partner actor, mirror suppression still follows the client-backed raw-action and state gates, and the dedicated prepared five-layer cache still inserts the full mirror package at the shared UnderFace overlay boundary. This pass now aligns the prepared-layer lifetime and snapshot refresh path more closely with the newly rechecked client seam: same-facing source refreshes still keep the prepared mirror plane's recreate timestamp even when the observed player action changes and the assembled source signature refreshes, and when that preserved helper plane refreshes into the same snapshot size the simulator now repaints the existing prepared render target in place instead of replacing the texture object outright. That keeps the simulator-owned prepared mirror plane closer to the client split where `PrepareMirrorActionLayer` preserves the helper layer object and `CUser::Update` keeps inserting the current source-layer canvas into it without recreating the helper object on every action-driven canvas change. Focused regression coverage for the helper-start precedence, prepared-layer recreate-time preservation, in-place snapshot-refresh gate, and live-source seam is now on disk in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs`.

The remaining gap is narrower and still concentrated at the real live-layer-object boundary inside `CUser::PrepareMirrorActionLayer` / `CUser::Update`: the simulator still does not own shared `IWzGr2DLayer` mirror objects, true COM live-layer origin/current-time handles, true `InsertCanvas` reparenting against the client layer graph, or helper-object persistence across bounds changes the way the client keeps the same layer handle and swaps only the inserted source canvas. Follow-up clone-helper behaviors that depend on those live client layer objects rather than simulator-owned prepared plane snapshots also remain outside the current seam.

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

- `CActionMan::GetShadowPartnerProp` at `0x41ba60`
- `CActionMan::LoadShadowPartnerAction` at `0x423cf0`

Notes:
The current skill backlog already names the shadow-partner runtime/helper surface, but the targeted `CActionMan` pass shows there is still a distinct loader owner underneath it. `GetShadowPartnerProp` resolves the helper property from the skill id before action lookup, while `LoadShadowPartnerAction` caches by `(skillId, action)`, resolves either the named raw-action branch or the numeric fallback branch through `get_action_name_from_code`, enumerates the action canvases into cached frame entries, reads frame-owned metadata from the canvas properties, and appends the mirrored interior-frame tail when the loader's replay table marks that action. That means shadow-partner parity is split the same way as summons, pets, employees, dragons, mobs, NPCs, and taming-mob helpers elsewhere in the backlog set: `CUser::PrepareShadowPartnerActionLayer` and the simulator helper runtime own placement and playback state, while `CActionMan` owns the helper action catalog and the frame table those runtime paths consume.

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnTimeBombAttack` at `0x9323f0`
- `CUserLocal::OnVengeanceSkillApply` at `0x909b10`
- `CUserLocal::OnExJablinApply` at `0x9034e0`

Notes:
The direct `CUserLocal::OnPacket` re-check surfaced another client-owned skill family that was not represented anywhere in the backlog text. `OnTimeBombAttack` is not just a generic damage callback: the decompile shows it decoding a skill id plus attack/action data, resolving the local skill level, calling `TryDoingMeleeAttack`, then applying hit-period/invincibility and impact side effects before forcing the avatar hit reaction. `OnVengeanceSkillApply` is also more specific than a plain temporary-stat refresh because it gates on skill id `3120010` and then calls `DoActiveSkill_MeleeAttack` through the local skill runtime, while `OnExJablinApply` arms the `m_bNextShootExJablin` flag for the next ranged-shot branch rather than behaving like a generic buff bit. That is a dedicated packet-owned skill-application seam between the broad cast/update families and the existing temporary-stat or helper rows.

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnIncComboResponse` at `0x91a970`
- `CUserLocal::DrawCombo` at `0x917040`

Notes:
The same targeted `CUserLocal::OnPacket` scan surfaced another local skill owner that the backlog still was not naming. `OnIncComboResponse` is very small but concrete: it decodes the server-authored combo count, stores it in `m_nCombo`, stamps `m_tLastSetCombo`, and immediately routes through `DrawCombo`. The `DrawCombo` decompile then shows that combo is not just another temporary-stat label. The client resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them alive on a five-second alpha window, applies fly-origin motion to the combo vectors, and conditionally spawns extra `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` layers when the corresponding Aran combo-command skills are learned. A repo search across `HaCreator/MapSimulator` still only surfaced the generic `Combo` temporary-stat labeling in `SkillManager` and did not surface any simulator-side combo packet owner, combo HUD/effect loader, or `ComboCommand` runtime, so this remains a real missing parity bucket rather than stale wording.

### 7. Shared skill-use animation-owner parity discovered by targeted `CAnimationDisplayer` scan

- `CAnimationDisplayer::Effect_SkillUse` at `0x459100`

Notes:
The broader skill rows already cover cast logic, hit effects, prepared gauges, and helper actors, but the latest `CAnimationDisplayer` pass exposed another owner that the backlog corpus was not naming anywhere else. `Effect_SkillUse` is not a generic data lookup. The decompile shows it formatting a StringPool-backed effect UOL pattern for the requested effect name plus either the default or indexed variant, loading each resolved layer through `CAnimationDisplayer::LoadLayer`, applying the caller-supplied delay-rate override before playback, and then registering every resolved layer through `RegisterOneTimeAnimation` rather than leaving skill-use presentation on the local runtime or UI owners. The same body also keeps optional overlay-parent and auto-flip ownership at this animation-manager seam, which means visible skill-cast parity is split one level further than the current skill backlog suggests: `CUserLocal::*` owns cast admission and follow-up behavior, feature-local runtime code owns gameplay consequences, and `CAnimationDisplayer::Effect_SkillUse` owns the shared one-shot cast animation composition that many skills route through before those consequences are visible.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. Player skill execution parity

The simulator has a generic skill runtime. It does not yet mirror the client's named `CUserLocal` behavior families.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Generic skill casting | The simulator now loads the full player skill catalog from `Skill.wz`, learns active skills into the runtime, supports direct cast invocation from the skill window in addition to hotkey casting, keeps GM/SuperGM skill-book resolution compatible with both `900.img` and `910.img` layouts while focusing those jobs on their own job-book window instead of the global advancement catalog, evaluates Big Bang `common` formulas in IMG-mode data sets so skills like `Power Strike` are treated as castable attacks instead of passive placeholders, mirrors skill effect anchoring/hitbox fallback by facing direction for asymmetric melee effects, sanitizes unsupported SpriteFont glyphs in the skill window so WZ-backed names no longer crash row rendering, and resolves cast SFX from `Sound/Skill.img` so skill use now plays the corresponding `Use` or fallback attack sound through the simulator audio path | This closes the previous "can only use the current job's limited skill book" runtime/UI gap and keeps special-job skill tabs populated and executable across client data variants | `SkillManager.cs`, `SkillLoader.cs`, `SkillUIBigBang.cs`, `MapSimulator.cs`, `UIWindowLoader.cs` |
| Implemented | Buff stat application | Buff logic now applies `PAD`, `PDD`, `MAD`, `MDD`, `ACC`, `EVA`, `Speed`, and `Jump` directly to the player build and cleanly removes them when buffs expire or refresh | Active buff skills now change simulator stats and movement-relevant values instead of only a partial subset | `SkillManager.ApplyBuffStats` |
| Partial | Buff icon / temporary-stat UI parity | Active buffs still drive the WZ-backed status-bar tray with live duration countdowns sourced from `UI/BuffIcon.img`, the tray still supports client-style right-click cancellation directly from the live buff list, `UILoader` still normalizes and loads the full `BuffIcon.img` icon surface, and the simulator still consumes the authored `name` strings plus `buff/*` traversal order from that same catalog so the nine observed stat-family icons (`incEVA`, `incPAD`, `incPDD`, `incMAD`, `incMDD`, `incACC`, `incCraft`, `incJump`, `incSpeed`) keep their WZ-authored display names and ordering instead of a duplicated simulator-only table. The broader non-`BuffIcon.img` family catalog still resolves through the recovered v95 `SecondaryStat` layout, and this run stayed inside `SkillManager.GetStatusBarBuffEntries` again without widening tray ownership outside that same seam. WZ was rechecked directly in the mounted export for the mixed and independent-property families still relevant here: `UI/BuffIcon.img/buff` still exposes only the same nine ordered authored icon families, `UI/BuffIcon.img/buff/incPDD/name` still resolves to `Physical Defense`, `Skill/410.img/skill/4101004/common` still publishes `speed` then `jump`, `Skill/3112.img/skill/31121001/common` still publishes `cr`, `ignoreMobpdpR`, then `bdR`, `Skill/572.img/skill/5720008/common` still publishes `criticaldamageMin`, `criticaldamageMax`, `cr`, and `bdR`, `Skill/3511.img/skill/35111013/common` still publishes `pddR`, `mhpR`, `mmpR`, `cr`, `damR`, and `expR`, `Skill/422.img/skill/4220009/common` still publishes `mesoR`, `Skill/7000.img/skill/70000049/common` still publishes `dropR`, `Skill/7100.img/skill/71000321/common` still publishes `ignoreMobDamR`, `Skill/000.img/skill/0000110/common` still publishes `mhpR` and `mmpR`, `Skill/2112.img/skill/21120001/common` still publishes `criticaldamageMin`, `Skill/2311.img/skill/23111005/common` still publishes `asrR` and `terR`, `Skill/2411.img/skill/24111003/common` still publishes `indieMhpR` then `indieMmpR`, and `Skill/530.img/skill/5301003/common` still publishes `indieMhp`, `indieMmp`, `indieAllStat`, `indieSpeed`, `indieJump`, `indieAcc`, and `indieEva` in authored order. That evidence still drives the authored-family override table plus the fallback-label bridge for later explicit owner properties such as `mhpR` / `mmpR`, `pddR` / `mddR` / `accR` / `evaR`, `asrR` / `terR`, `criticaldamageMin` / `criticaldamageMax`, `damR`, `dropR`, `mesoR`, `bdR`, `ignoreMobpdpR`, `ignoreMobDamR`, and authored `indie*` rows such as `indieSpeed`, `indieMhp`, and `indieMhpR`, so those tooltip labels no longer collapse back to one generic family name just because an earlier family still owns tray ordering. The owner seam now also keeps the first authored explicit family on the tray even when that family is one of the nine direct `BuffIcon.img/buff/*` icons instead of only when the buff has no direct icon family. That closes the next direct-family drift the earlier pass still left behind: `SkillManager` now promotes the authored explicit family before resolving tray lane, icon owner, and tray sort order, so mixed direct-family books such as `Skill/410.img/skill/4101004/common` stay on the authored `Speed` family and `buff/incSpeed` icon instead of sliding to the globally earlier `Jump` lane just because `incJump` sorts before `incSpeed` inside the tray catalog. The earlier direct-family fix remains in place too: when a mixed buff already resolves onto an authored direct `BuffIcon.img/buff/*` family icon, `SkillManager` still keeps the visible `Buff Type` owner on that same direct family, so `Skill/3511.img/skill/35111013/common` stays on the real `incPDD` icon and `Physical Defense` label instead of drifting to later non-generic properties such as Critical Rate. The no-direct-icon branch in that same owner seam still prefers the earliest authored explicit property-family owner from the skill's `common` property order before falling back to generic helper rows such as `cr`, which keeps mixed non-direct surfaces such as `Skill/3112.img/skill/31121001/common` on `Ignore Enemy DEF`, `Skill/572.img/skill/5720008/common` on `Critical Damage (Min)`, `Skill/000.img/skill/0000110/common` on `Max HP`, `Skill/2311.img/skill/23111005/common` on `Abnormal Status Resistance`, and later `indie*` mixes such as `Skill/2411.img/skill/24111003/common` and `Skill/530.img/skill/5301003/common` on their first authored explicit family instead of collapsing back to generic helper rows or a later direct-stat alias. Standalone non-`BuffIcon.img` families such as `mesoR`, `dropR`, `bdR`, `ignoreMobpdpR`, `ignoreMobDamR`, `asrR`, `terR`, and later `indie*` rows still stay on the skill-authored icon texture when no authored direct family icon exists, and heuristic-only description or identity matches still cannot steal one of the nine direct tray families by themselves. Focused regression coverage is now actually on disk in `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` for authored direct-family precedence on `speed` + `jump`, mixed `mhpR` + `mmpR` ownership and sort retention, mixed `asrR` + `terR` ownership, later no-direct `indie*` mix retention (`indieMhp` + `indieMmp` + `indieAllStat` + `indieSpeed` + `indieJump` + `indieAcc` + `indieEva`), and the existing mixed direct-family `pddR` ownership case. The remaining gap is narrower but still real: exact tray order is now authored for the known direct `BuffIcon.img` families and for the supported explicit no-direct families that the recovered v95 `SecondaryStat` slice plus parsed `common` property order can already identify, but some non-`BuffIcon.img` families still have no direct recovered v95 icon owner or still appear to depend on runtime-owned grouping beyond `UI/BuffIcon.img`, the recovered temp-stat surface, and currently parsed skill metadata, especially once multiple explicit non-direct families still compete in a buff without a stronger recovered client owner than the current `SecondaryStat` slice and authored property order. | Buff-heavy jobs are visible on the HUD, can be cleared without waiting for expiry, and no longer collapse as often into a fully generic tray entry while tooltip text, tray-family ownership, icon fallback, and the visible `Buff Type` line stay closer to the authored WZ and recovered client temp-stat surface. The known nine `BuffIcon.img/buff/*` families still use the authored WZ icon names and order directly, and mixed direct-family books now also keep their first authored direct family on both the icon and tray lane instead of letting global stat sort pull them onto a later WZ tray position. Big Bang-era payload families such as `mhpR` / `mmpR`, `pddR` / `mddR` / `accR` / `evaR`, `damR` / `indieDamR`, `criticaldamageMin` / `criticaldamageMax`, `expR` / `dropR` / `mesoR`, `bdR`, `asrR` / `terR`, both `ignoreMobpdpR` and `ignoreMobDamR`, and authored `indie*` rows such as `indieSpeed`, `indieMhp`, and `indieMhpR` now keep explicit authored-family labels and authored mixed-family owner precedence instead of collapsing back to generic tooltip heuristics. The remaining gap is now concentrated on the still-unrecovered client grouping that decides exact final lane or icon ownership for the leftover non-direct family mixes once WZ-authored family order and the recovered `SecondaryStat` slice stop providing a unique owner. | `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`, `UnitTest_MapSimulator/StatusBarBuffTemporaryStatParityTests.cs` (`UI/BuffIcon.img/buff`, `UI/BuffIcon.img/buff/incPDD/name`, `Skill/410.img/skill/4101004/common`, `Skill/3112.img/skill/31121001/common`, `Skill/572.img/skill/5720008/common`, `Skill/3511.img/skill/35111013/common`, `Skill/000.img/skill/0000110/common`, `Skill/2311.img/skill/23111005/common`, `Skill/2411.img/skill/24111003/common`, `Skill/530.img/skill/5301003/common`) |
| Partial | Melee / ranged / magic resolution | The runtime keeps the existing player-skill parity work for this seam: WZ `lt/rb` attack bounds stay intact, melee and magic target ordering stay split, no-ball spell and shoot families still route through the client-shaped `info/type` classifier instead of collapsing into melee defaults, direct no-projectile ranged skills still use their own fallback hitbox plus the `DoActiveSkill_ShootAttack` melee-first-vs-shoot fallback split, projectile families still honor `rectBasedOnTarget`, `multiTargeting`, `range` chaining, `ballDelay` / `ballDelayN`, authored ball-frame sweep bounds, effective `mobCount` caps from both skill and ball data, WZ `x` damage overrides and client `SKILLENTRY::AdjustDamageDecRate` tables, and the shared shoot-entry / ammo resolver still mirrors the current client-backed weapon-family, support-state, consume-count, USE/CASH ownership, and pellet-family gates. This run kept the fallback projectile presentation work inside the same owner seam. WZ was rechecked first and still shows the authored data driving this row (`Skill/310.img/skill/3101005/info/rectBasedOnTarget = 1`, `Skill/230.img/skill/2301005/info/{multiTargeting,rectBasedOnTarget} = 1`, `Skill/230.img/skill/2301005/common/{lt,rb,range,mobCount}`), so authored `handMove`, then `hand`, then body-origin shoot points still win unchanged. Client evidence was rechecked second in `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::GetProperBulletPosition@0x907310`, `GetShootAttackPt0@0x9039a0`, and `is_position_up_skill_on_riding@0x9037c0`: when no authored shoot point exists, projectile launch Y still uses the client fallback `ptUser.y - 28` baseline plus the recovered skill nudges for `33101007`, `33121005`, `35001004`, `33101001`, `35101010`, and `35111015`; queued delayed projectiles still carry and correctly rebase their fallback-origin offsets at fire time; the current owner resolver still keeps the same Mechanic `1932016` riding owner alive through avatar-transform-owned vehicle states instead of only current-action or equipped mount ownership; and the live mounted-state seam now also preserves the exact `GetShootAttackPt0` body-rel-move vehicle ids (`1932015`, `1932030`, `1932031`, `1932032`, `1932033`, `1932036`) instead of collapsing them back to the generic Mechanic owner before fallback-origin resolution. That means position-up shoot skills now keep the confirmed Mechanic riding nudge even when the live mount owner is coming from the transform seam, while exact mounted-state vehicle ids can now still take the client body-rel-move branch ahead of that Mechanic fallback. Focused source-side coverage in `UnitTest_MapSimulator/MeleeRangedMagicResolutionParityTests.cs` now includes the exact mounted-body-rel-move vehicle table, mounted-state vehicle-id preservation, and current-action-owner-over-mounted-state precedence, alongside the existing fallback Y-offset table, authored shoot-point preservation, queued fallback-offset rebasing, authored projectile-origin priority, mirrored queued-facing origin refresh, explicit deferred-origin preservation, `ballDelay` release scheduling, and queued or missing-ball ammo-visual ownership. The row remains `Partial` because the remaining mismatch is now concentrated in deeper client-only fire-time presentation details: any residual divergence between the simulator's frame-derived mounted-state `bodyRelMove` approximation and the client's live `m_ptBodyRelMove.y`, and later moving-shoot or projectile-hit presentation nudges applied after launch rather than exposed through WZ-authored map points, ball frames, ammo ownership, or the recovered fallback launch offsets. | This row matters because `CUserLocal` keeps separate melee, shoot, and magic execution lanes, and the remaining mismatch is now small enough that future work can stay focused on the last client-owned fire-time presentation details instead of reopening projectile family resolution, target ordering, ammo ownership, or basic fallback projectile origin placement again. | `SkillManager.ProcessMeleeAttack`, `SkillManager.ProcessMagicAttack`, `SkillManager.ProcessNoProjectileRangedAttack`, `SkillManager.SpawnProjectile`, `SkillManager.CheckProjectileCollisions`, `SkillManager.ResolveProjectileAttackOrigin`, `SkillManager.RefreshQueuedProjectileFireTimeOrigin`, `SkillManager.DrawProjectile`, `SkillManager.ResolveProjectileVisualItemId`, `SkillLoader.ParseSkillInfo`, `SkillLoader.CreateLevelData`, `ClientShootAttackFamilyResolver`, `ClientShootAmmoResolver`, `UnitTest_MapSimulator/MeleeRangedMagicResolutionParityTests.cs` (`Skill/310.img/skill/3101005/info/rectBasedOnTarget`, `Skill/230.img/skill/2301005/info/{multiTargeting,rectBasedOnTarget}`, `Skill/230.img/skill/2301005/common/{lt,rb,range,mobCount}`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::DoActiveSkill_MagicAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::GetProperBulletPosition@0x907310`, `GetShootAttackPt0@0x9039a0`, `is_position_up_skill_on_riding@0x9037c0`) |
| Partial | `DoActiveSkill_*` family parity | Generic movement, summon, and prepare/charge families now resolve through an explicit generic `DoActiveSkill` fallback lane in `SkillManager` instead of only by implication across separate seams. Movement-family dispatch now stays on the client-confirmed four-way split from the recovered local lanes: teleport snaps, grounded rush, flying-rush ownership, and bound-jump starts, with jump/hop launch skills folded into the same `TryDoingFlyingRush` family instead of remaining a simulator-only fifth lane while still preserving their launch-specific jump arc in the existing execution seam. Earlier landings still hold: Battle Mage aura recasts no longer route through the cast-time cancel lane, distinct aura-family buffs stay stacked while same-skill recasts refresh their own slot, and WZ `info/mesoExplosion` skills consume authored in-box drops directly before applying the attack payload instead of passing through the live local pickup-owner gate. WZ re-check still keeps the helper-only support surface explicit: `Skill/421.img/skill/4211006/info/{type=1,mesoExplosion=1}` publishes the local explosion marker only, while `Skill/211.img/skill/2111007/info/{type=16,affectedSkill=2101002,affectedSkillEffect=bodyAttack&&stun}`, `Skill/3211.img/skill/32111010/info/{type=16,affectedSkill=32001002,affectedSkillEffect=bodyAttack&&stun}`, `Skill/3212.img/skill/32120000/info/{type=51,affectedSkill=32001003,affectedSkillEffect=dot,dotType=aura}`, and `Skill/3211.img/skill/32110000/info/{type=50,affectedSkill=32101002,affectedSkillEffect=partyDamageSharing}` still publish owner-follow-up metadata only and no standalone direct-cast lane. This pass kept that helper-only ownership split inside the existing passive seam, confirmed WZ `zoneType=invincible` is still limited here to `Skill/422.img/skill/4221006/info/zoneType` and `Skill/3212.img/skill/32121006/info/zoneType`, preserved `4221006`'s authored `tile` animation while letting `32121006` use its WZ-authored `special` branch when no `tile` branch exists, kept the existing GM/SuperGM validation seam as the owner for both active casts and quick-slot assignment, and restored the focused regression file that was missing from this repo: `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs`, covering the explicit generic-cast lane, helper-only passive suppression, the jump-rush-to-flying-rush family fold, the invincible-zone tile-vs-special fallback, and the GM/SuperGM quick-slot ownership rule. | The remaining gap is now narrower and concentrated in the last client-owned movement timing, packet choreography, and area/summon ownership rather than the generic cast-family split, helper-only passive suppression, invincible-zone tile-vs-special selection, or the admin ownership gate. The simulator still simplifies the exact moving-shoot entry snapshot and request choreography inside `TryDoingSmoothingMovingShootAttack`, especially the still-unrecovered full client `m_nAttackActionType` table values and the exact per-row contents of the shared `get_random_shoot_attack_action(...)` table beyond the now-modeled live family split for shoot, one-handed stab, two-handed stab, one-handed swing, two-handed swing, and polearm swing plus the earlier `info/type == 3` appointed-action bypass. It also still simplifies the live temporary-stat inputs that feed `get_attack_speed_degree(...)` beyond the now-modeled clamp, `4001334` base offset, skill-specific modifier-bypass set, shared Booster delta, and the WZ-backed `rapidAttack => CountLimit 1` fallback, plus any additional `CAntiRepeat` initialization or bypass rules beyond the now-modeled position-threshold, level-match, rapid-attack count-limit, and `33121009` exception gates; the final highest-jump timing window beyond the current WZ-backed near-apex gate; any remaining teleport timing / snap choreography around the client-owned delayed `m_teleport` commit window beyond the now-modeled follow-up mastery trigger; any remaining `zoneType=invincible` client details beyond the current persistent local protection zone, WZ-authored footprint/duration, and tile-or-special visual branch selection; any remaining Rocket Booster packet choreography beyond the now-modeled launch-marker, launch-speed formula, and end-loop timing; deeper packet-owned summon assist dispatch and per-family `OnSkill` / `OnAttack` ownership beyond the now-modeled body-contact / passive-movement / expiry cadence; and the remaining local-vs-party area-footprint choreography for helper-only support rows beyond the now-modeled local owner-follow-up buff projection and linked-owner support merge. | `SkillManager`, `SkillLoader`, `PlayerCharacter`, `PlayerCombat`, `PlayerSkillStateRestrictionEvaluator`, `SummonedPool`, `PacketOwnedSummonUpdateRules`, `UnitTest_MapSimulator/DoActiveSkillParitySurfaceTests.cs` (`CUserLocal::DoActiveSkill_*`, `CUserLocal::DoActiveSkill_BoundJump`, `CUserLocal::TryDoingTeleport`, `CUserLocal::TryDoingRush`, `CUserLocal::TryDoingFlyingRush`, `CUserLocal::TryDoingSmoothingMovingShootAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare`, `CUserLocal::TryDoingRocketBooster`, `CUserLocal::TryDoingRocketBoosterEnd`, `CSummoned::Update`, `CAntiRepeat::TryRepeat`, `get_random_shoot_attack_action`, `get_attack_speed_degree`, `Skill/*/info/type`, `Skill/*/info/affectedSkillEffect`, `Skill/*/info/avaliableInJumpingState`, `Skill/*/info/requireHighestJump`, `Skill/110.img/skill/1101004/common/x`, `Skill/510.img/skill/5101006/common/x`, `Skill/211.img/skill/2111007/info/{type,affectedSkill,affectedSkillEffect}`, `Skill/3211.img/skill/32110000/info/{type,affectedSkill,affectedSkillEffect}`, `Skill/3211.img/skill/32111010/info/{type,affectedSkill,affectedSkillEffect}`, `Skill/3212.img/skill/32120000/info/{type,affectedSkill,affectedSkillEffect,dotType}`, `Skill/3212.img/skill/32121003/info`, `Skill/3212.img/skill/32121006/{info/zoneType,special}`, `Skill/3300.img/skill/33001000/info`, `Skill/3310.img/skill/33101001/info`, `Skill/3311.img/skill/33111001/info`, `Skill/3312.img/skill/33121005/info`, `Skill/3312.img/skill/33121009/info`, `Skill/3500.img/skill/35001001/info/rapidAttack`, `Skill/3510.img/skill/35101009/info/rapidAttack`, `Skill/422.img/skill/4221006/{info/zoneType,tile}`, `Skill/*/info/mesoExplosion`, `Skill/*/info/selfDestructMinion`, `Skill/900.img/skill/*`) |
| Partial | Shadow Partner render/action parity | The simulator still treats Shadow Partner as a dedicated helper render/action surface rather than a generic buff ornament, with WZ-loaded `special/*` action tables for `4111002`, `4211008`, and `14111000`, client-shaped helper resolution order (`LoadShadowPartnerAction` helper-row lookup before plain raw-action name), raw-action-aware alias fallback, delayed attack follow-up keyed from the helper first-frame delay, blocking `create*` plus attack completion hold semantics, client-lane displacement/interpolation (`+/-30`, `+50`, `200 ms`), per-frame `a0/a1` alpha interpolation, and under-face insertion-layer ownership. WZ was rechecked first in the mounted export for the exact authored helper surface this pass is allowed to lean on: `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, and `Skill/1411.img/skill/14111000/special/*` still publish the same generic helper families, with `4111002` and `4211008` publishing the `create2/create3/create4` variants while `14111000` does not publish `create*` in this export. The same WZ pass rechecked the non-ghost raw rows from `Skill/411.img/skill/{4111001,4111003,4111005,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121003,4121004,4121008}/action/0`, `Skill/420.img/skill/{4201002,4201003,4201005}/action/0`, `Skill/421.img/skill/{4211002,4211003,4211005,4211006,4211007}/action/0`, `Skill/422.img/skill/{4221001,4221003,4221006,4221007}/action/0`, `Skill/1410.img/skill/14101006/action/0`, and `Skill/1411.img/skill/{14111001,14111002}/action/0`, which still prove the currently loaded exact rows (`alert2`, `alert3`, `alert4`, `alert5`, `swingO1`) plus alias-only rows (`avenger`, `assaulter`, `prone2`, `savage`, `showdown`, `assassination`, `smokeshell`, `ninjastorm`, `vampire`). Client evidence from `CActionMan::LoadShadowPartnerAction` still keeps the hidden action-specific helper-row table ahead of plain raw-action-name lookup; the same disassembly shows that pieced rows resolve a source helper action plus numeric source-frame index before adding the synthetic helper canvas. This run tightened that seam in two places without widening ownership: every synthesized pieced helper alias still defaults to one source helper frame per piece while the already-evidenced non-first-frame `prone2` case stays pinned to source frame `1`, and `SkillLoader` / `SkillData` now also cache the WZ-backed family-specific `action/0` surface per Shadow Partner buff (`4111002`, `4211008`, `14111000`) so `ShadowPartnerClientActionResolver`, `PlayerCharacter`, and `RemoteUserActorPool` stop treating assassin/shadower/night-walker raw helper rows as if they were interchangeable across all three helper families. That keeps `assaulter`-backed synthesis and playback on the shadower family, keeps Night Walker helper ownership from opportunistically claiming those shadower-only raw rows, and leaves the earlier raw-backed back-lane, shared horizontal-origin cadence, same-branch identity tightening, raw-alias delayed-attack, numeric helper-frame ordering, `a0/a1` alpha interpolation, and `4331002` / raw-`47` render-suppression seams in place. Focused regression coverage now lives on disk in `UnitTest_MapSimulator/ShadowPartnerClientParityTests.cs` for unsupported-vs-supported pieced helper synthesis, canonical compatibility-alias support (`flyingAssaulter -> assaulter`), and the supported-vs-unsupported `prone2` frame-remap path. Remaining gap is narrower but still open: unrecovered client-only `LoadShadowPartnerAction` non-ghost helper rows beyond the now-covered WZ exact rows plus the per-family WZ-backed raw-row gate, exact hidden source-frame indexes for any pieced row where the client does not use the first source frame, additional frame-index rewrites beyond the currently evidenced `ladder2` / `rope2` / `prone2` / indexed `alert*` set, exact ordering against every other simulator-owned non-mirror under-face overlay that the client may still own as a literal action-layer canvas object, and any residual packet-only one-shot alpha or follow-up choreography that still depends on hidden client layer state rather than the helper animation catalog itself. | Shadow Partner still mirrors companion playback through a dedicated skill-owned action catalog with client-shaped helper ordering and raw-action fallback, including the earlier helper aliases plus the currently exported exact non-ghost helper rows (`alert2`, `alert3`, `alert4`, `alert5`, `swingO1`) from the active Shadow Partner job families. Helper-frame preservation still includes `prone2` in addition to `ladder2` / `rope2` / indexed `alert*`, the shared local+remote offset seam still uses the same client-shaped side-lane and interpolation helpers, the loader still preserves deterministic numeric helper-frame order for mixed direct and linked frame rows such as `shoot2`, and the shared resolver now narrows WZ-backed raw helper ownership to the family that actually publishes that row instead of letting every Shadow Partner buff claim every recovered non-ghost alias. That seam stays tighter on both visibility and placement because the simulator already honors the client's `4331002` / raw-`47` helper suppression while also allowing remote morph actors to keep Shadow Partner when the client-backed raw-action path still owns the helper. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `ShadowPartnerClientActionResolver.cs`, `RemoteUserActorPool.cs`, `UnitTest_MapSimulator/ShadowPartnerClientParityTests.cs`, `CUser::PrepareShadowPartnerActionLayer@0x8e1730`, `CActionMan::LoadShadowPartnerAction@0x423cf0`, `is_back_action@0x45f1b0`, `get_action_name_from_code`, `Skill/*/{4111002,4211008,14111000}/special/*`, `Skill/421.img/skill/4211008/special/shoot2/{0,1,2,3,4}`, `Skill/411.img/skill/{4111001,4111003,4111005,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121003,4121004,4121008}/action/0`, `Skill/420.img/skill/{4201002,4201003,4201005}/action/0`, `Skill/421.img/skill/{4211002,4211003,4211005,4211006,4211007}/action/0`, `Skill/422.img/skill/{4221001,4221003,4221006,4221007}/action/0`, `Skill/1410.img/skill/14101006/action/0`, `Skill/1411.img/skill/{14111001,14111002}/action/0` |
| Partial | Queued follow-up attack families | Attack skills that publish WZ `finalAttack` follow-up maps still queue a deferred extra strike after the opener resolves, gate that queued branch by weapon type on both queue and execute, reuse the learned follow-up skill's live proc rate, preserve queued facing plus preferred-target mob or position snapshots through direct follow-up resolution and any later deferred moving-shoot or projectile hop, and keep the client's separate single pending-slot overwrite rules for final attack, Spark, and serial follow-up families without clearing unrelated queued summon work. Thunder Breaker `Spark` (`15111006`) still runs as its own timed attack-triggered family from WZ `info/condition=attack` plus `info/chainAttack=1` and `info/chainattackPenalty=1`, snapshots the current weapon code at queue time, rechecks that same code again when the deferred branch fires, and still re-enters the shared shoot/projectile path with the saved trigger origin and target lane. The deferred Spark handoff now also keeps that Spark-owned family marker when queued execution passes back through `ExecuteSkillPayload`, so later `ballDelay` release, queued-facing presentation, and same-slot overwrite cleanup continue to treat the delayed shot as Spark instead of silently degrading to a generic queued projectile. WZ `info/chainAttack=1` plus `info/chainattackPenalty=1` serial families such as `2221006` and `5121002` still use their own single pending serial slot, queued final-attack, Spark, and serial execution still re-read the live learned skill level when the deferred branch actually fires, still route queued cast startup back through the normal cast-effect seam, and still carry the client-only ranged final-attack `TryDoingShootAttack(..., 65, ...)` search hint through queued entry plus any extra deferred moving-shoot hop. The queued shoot-entry seam remains tighter on both validation and presentation: queued final-attack and Spark casts still re-qualify their armed USE or CASH ammo selection against the live inventory right before queued fire time, still refresh slot indices while preserving the original item ids even when a queued no-consume buff snapshot is active, still cancel the queued ranged branch if the armed USE ammo disappeared or no longer satisfies the live consume count, and still consume those snapshotted USE or CASH item ids during queued projectile draw so delayed final-attack or Spark releases render from the matching WZ `Item/*/*/bullet` frames instead of collapsing back to generic skill projectile art. WZ still exposes the authored item-bullet visual seam directly (`Item/Consume/0206/02060000/bullet/0` for standard arrows and `Item/Cash/0502/05021000/bullet/{0,1,2}` for animated cash bullets), while client evidence from `CUserLocal::GetProperBulletPosition` still shows fire-time resolution returning both USE and CASH projectile ownership. Client re-check of `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare` at `0x912f90` still also shows the delayed moving-shoot entry keeping the queued action plus `nBulletItemPos` / `nBulletCashItemPos` metadata separately from later fire-time resolution. The simulator therefore keeps queued projectile visuals on the queued cast's already-resolved weapon family during later `ballDelay` refresh, prefers the cash-bullet family only while that cash slot still resolves at fire time after the queued slot refresh, otherwise falls back to the preserved USE family, still mirrors the live `handMove` / `hand` / body-origin shoot point across the avatar pivot when delayed queued fire finally resolves under a saved facing that no longer matches the avatar's current direction, now keeps that queued-facing snapshot on the projectile itself so later target re-aim no longer mutates the mirrored delayed launch side, and still preserves any already-armed owner-relative queued projectile launch offset when that late fire-time refresh re-anchors the shot onto the mirrored live shoot point instead of flattening the delayed projectile back onto the raw owner origin. WZ-authored `x` chain-penalty decay and the client-owned fixed multiplier tables for no-`x` families still both apply through the deferred or timed chain path as well, so queued follow-up families no longer flatten back onto one generic damage percent after the opener. This pass also checks in the focused helper coverage that the row had been calling for in `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` for queued Spark family-marker preservation through the `ExecuteSkillPayload` handoff, queued-facing launch preservation during delayed fire-time origin refresh, late queued ammo revalidation and slot refresh, stale cash-lane visual fallback back to the preserved USE family, mirrored delayed-origin refresh, queued delayed-origin offset preservation, queued bullet-family visual override, and the authored-`x` plus client-table serial damage-decay seams that still feed queued chain follow-ups. The remaining gap is now concentrated in the last client-only `TryDoingShootAttack` fire-time presentation details that still sit below the current simulator seam, especially the exact cosmetic projectile placement the client derives after those preserved moving-shoot bullet slot positions and item ids have been carried into the delayed fire step. | Final-attack style procs still no longer collapse into the initiating cast timing, queued strikes still cancel if the player changes to a non-matching weapon before the deferred attack fires, if a newer follow-up proc replaces the pending one before it can execute, or if the follow-up skill has been locally unlearned before the queued branch actually fires, and replacing a pending final attack still does not wipe unrelated queued summon damage. Queued Spark now follows that same queue-time and execute-time weapon snapshot rule instead of surviving a weapon swap that the client would reject, and deferred queued Spark shots now also keep their Spark-owned projectile marker through the `ExecuteSkillPayload` handoff so later delayed fire-time refresh, queued-facing presentation, and same-slot overwrite logic still stay on the Spark family instead of degrading to generic queued projectile handling. Once a queued final attack or queued Spark has already progressed into a delayed projectile release, a newer proc in that same client-owned slot still clears the older still-pending projectile instead of letting it survive past the queue replacement point, while unrelated projectile work remains intact. Deferred moving-shoot payloads also stop dropping out when a due queued follow-up family should run first because the payload stays armed until after that queued final-attack, Spark, or serial branch resolves instead of being dequeued and lost. Queued follow-up casts that pass through the simulator's extra deferred moving-shoot payload path still keep the stored facing or origin snapshot, preserve the ranged final-attack shoot-range override (`65`) through both deferred target acquisition and the actual delayed fire step, revalidate the live learned level again at the actual deferred fire time, keep the queued target-position snapshot alive across both direct execution and deferred projectile targeting so a vanished opener mob does not force those branches straight back onto generic live ordering, and still revalidate the live fire-time ammo count plus skill-specific ammo family instead of only trusting the older queue-entry ammo snapshot. The queued ranged shoot-entry seam is now closer on both execution and presentation because queued final-attack and Spark casts recheck their armed USE or CASH ammo metadata at queued fire time even when the queued branch was armed under a no-consume buff snapshot, keep the original ammo item ownership instead of inheriting unrelated later casts, drop the queued ranged branch when the originally armed ammo family disappeared or no longer meets the live consume count, draw delayed queued projectiles from the matching snapshotted bullet-family frames rather than always falling back to the generic skill projectile art, stop preferring a stale cash-bullet family once the fire-time slot refresh has already shown that the queued cash lane disappeared while the armed USE lane still remains valid, keep that delayed projectile refresh on the queued cast's already-resolved weapon family even if the player changes to a different ranged weapon only after the queued shot has already fired, still keep the delayed queued projectile anchored to a mirrored live shoot point when the avatar turns around after the shot was armed but before the preserved queued-facing projectile actually releases, now keep that mirrored refresh tied to the queued cast-facing snapshot even after the projectile has already re-aimed toward a target lane, and keep queued serial follow-up damage on the authored/client decay tables instead of flattening later chain hits back onto one generic percent. The remaining mismatch is now mostly in deeper client-owned fire-time presentation bookkeeping such as the exact cosmetic bullet-position placement that still hangs off the client's delayed moving-shoot entry after `GetProperBulletPosition` resolves USE or CASH ownership. The current simulator seam already preserves the queued projectile item snapshot, queued `m_movingShootEntry` USE/CASH slot-position metadata, queued fire-time ammo-family and consume-count revalidation, mirrored delayed fire-time shoot origin refresh, delayed owner-relative offset, refreshed fire-time slot indices, fire-time cash-slot visual fallback, the queued cast-facing snapshot through delayed re-aim, the queued serial damage-decay surface, and the shared queued weapon gate now covered by `Spark` too. | `SkillManager`, `SkillData`, `SkillLoader`, `ClientShootAmmoResolver`, `UnitTest_MapSimulator/QueuedFollowUpAttackParityTests.cs` (`Skill/1511.img/skill/15111006/info/{condition,chainAttack,chainattackPenalty}`, `Skill/222.img/skill/2221006/info/{chainAttack,chainattackPenalty}`, `Skill/512.img/skill/5121002/info/{chainAttack,chainattackPenalty}`, `Item/Consume/0206/02060000/bullet/0`, `Item/Cash/0502/05021000/bullet/{0,1,2}`, `CUserLocal::GetProperBulletPosition`, `CUserLocal::TryDoingFinalAttack`, `CUserLocal::TryDoingSerialAttack`, `CUserLocal::TryDoingSparkAttack`, `CUserLocal::TryDoingShootAttack`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare`) |
| Partial | Repeat-skill sustain families | `CUserLocal::TryDoingRepeatSkill` still owns more of the Mechanic repeat-state machine than the simulator, so this pass again stayed inside the existing repeat-sustain, player-manager, summon official-session, and local-utility transport seams rather than adding another local-only owner. WZ was rechecked first in the mounted export: `Skill/3512.img/skill/35121003` still publishes the SG-88 summon / manual-attack surface (`info/type = 33`, `info/minionAttack = normal`, `summon/attack1`) and `Skill/3512.img/skill/35121013` still publishes the tank-siege lane (`info/fixedState = 1`, `info/onlyNormalAttack = 1`, `effect/*/delay = 30/60/30/60`), so no authored timing or repeat-branch data changed. Client recheck in IDA still anchors the same owners and constants: `CUserLocal::TryDoingRepeatSkill` still creates the SG-88 keydown bar after `810 ms`, arms manual assist after `2790 ms`, removes the manual summon after `2520 ms` once assist is done, sends the tank-siege mode-end skill effect request through the same repeat owner once `m_tLastTankSiegeMode` reaches `5000 ms` as opcode `71` with payload order `Int32 skillId`, `Byte skillLevel`, `Byte sendLocal`, sets `m_bSendTankSiegeModeEnd`, and only rewrites the repeat skill back to tank mode after `m_nMechanicMode == 35121005`; `CUserLocal::SendSkillEffectRequest` still confirms that opcode `71` really is just that `Encode4/Encode1/Encode1` payload order; and `CSummoned::TryDoingAttackManual` still owns the SG-88 outbound request choreography via `CMobPool::CheckMobInTrapezoid`, `CMob::GetHitPoint`, and the `120 ms` same-request clustered follow-up delay. The earlier bridge behavior remains in place: tank-siege mode-end acknowledgement is still request-time keyed through `SkillManager` / `PlayerManager`, SG-88 manual requests are still tracked through `SummonedOfficialSessionBridgeManager`, local-owner `0x119` target coverage and packet-owned `1021` confirmation still converge on the same `(summon, requestedAt)` bridge lane, simulator-originated replay traces still cannot become learned official template evidence, and template reuse still prefers exact `(primary target, admitted target order)`, then same-primary same-set ordered-lane closeness, with true no-match fallback biased to newest same-count official capture. This pass closes the remaining request-origination gap that was already evidenced by the client: pending tank-siege mode-end now materializes the opcode-`71` request through `PacketOwnedMechanicRepeatSkillRuntime`, `SkillManager` now builds the client-shaped `35120013` effect request using the live learned level of `35121013`, `PlayerManager` still keeps the same request-time key for later acknowledgement but now also hands that request to the outer transport seam, and `MapSimulator.PacketOwnedUtilityParity` now routes the request through the existing local-utility bridge/outbox planner so live official-session injection, generic outbox delivery, and deferred bridge/outbox fallback all stay on the already-established authority lane instead of leaving tank-siege mode-end as a local-only pending ack. Focused coverage for what landed this pass is now on disk in `UnitTest_MapSimulator/RepeatSkillSustainPacketTransportParityTests.cs` for the client payload order of the opcode-`71` request and the existing bridge/outbox route preference. The row remains partial because tank-siege mode-end still lacks the true official client/server confirmation opcode and exact request/ack dispatch ordering behind `m_bSendTankSiegeModeEnd`, and SG-88 still does not know the exact official outbound opcode plus full field order a priori; SG-88 automatic request origination therefore still depends on first learning a reusable template from captured official traffic and the simulator still cannot prove every non-target field one-for-one without further client or packet evidence. | Mechanic repeat skills now keep the same WZ-authored repeat/summon timing surface and the same client-confirmed SG-88/tank-siege ownership model, while the simulator now also emits the evidenced tank-siege mode-end request through the existing local-utility authority seam instead of only remembering that a request should exist. SG-88 bridge replay and archive-strength behavior still stay aligned with the strongest observed official lane, and tank-siege mode-end now uses the client-confirmed opcode `71` payload shape (`Int32 skillId`, `Byte skillLevel`, `Byte sendLocal`) with the correct `35120013` effect skill id and live repeat-skill level before the later ack fallback logic runs. The remaining approximation is now concentrated in unknown confirmation semantics rather than missing request origination: tank-siege still needs the real ack opcode plus exact dispatch/ack ordering, and SG-88 still needs a fully client-proven outbound opcode/field map instead of captured-template replay. | `SkillManager.cs`, `PlayerManager.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.PacketOwnedSummonedParity.cs`, `Managers/PacketOwnedMechanicRepeatSkillRuntime.cs`, `Managers/SummonedOfficialSessionBridgeManager.cs`, `Companions/MechanicAuthorityTransportPlanner.cs`, `UnitTest_MapSimulator/RepeatSkillSustainPacketTransportParityTests.cs` (`CUserLocal::TryDoingRepeatSkill`, `CUserLocal::SendSkillEffectRequest`, `CUserLocal::TryDoingSiege`, `CUserLocal::UpdateClientTimer`, `CSummonedPool::OnPacket`, `CSummonedPool::OnAttack`, `CUser::OnSummonedAttack`, `CSummoned::TryDoingAttackManual`, `CMobPool::CheckMobInTrapezoid`, `CMob::GetHitPoint`, `Skill/3512.img/skill/35121003/{info,summon}`, `Skill/3512.img/skill/35121013/{info,effect}`) |
| Partial | Bound-jump, rocket-booster end, and smoothing moving-shoot branches | This pass stayed inside the existing moving-shoot / Rocket Booster / bound-jump seams again and tightened the next hidden moving-shoot row owner instead of widening unsupported melee inference. WZ was rechecked first in the mounted export at `E:\MapleStory\WzExport\v95_2`: the previous row facts still hold for `Skill/3500.img/skill/35001001/action/0 = flamethrower`, `Skill/3510.img/skill/35101009/action/0 = flamethrower2`, the `rapidAttack = 1` rows for `33121009`, `35001001`, and `35101009`, `Skill/3300.img/skill/33001002/{action/0 = doubleJump, info/type = 40}`, `Skill/1310.img/skill/13101004/{action/0 = archerDoubleJump, info/type = 40, info/casterMove = 1, info/avaliableInJumpingState = 1}`, `Skill/1110.img/skill/11101005/info/type = 41`, `Skill/3510.img/skill/35101004/{action/0 = rbooster_pre, info/type = 10, info/casterMove = 1, info/landingEffect = areaAttack}`, `Skill/1410.img/skill/14101004/{info/type = 40, info/casterMove = 1, info/avaliableInJumpingState = 1}` with no authored `action/0`, and `String/Skill.img/14101004/name = Flash Jump`. This pass also rechecked the weapon-side published action surface that the hidden moving-shoot table consumes through avatar attack metadata: `Character/Weapon/01452005/info/attack = 3` with published `shoot1` / `shootF`, `Character/Weapon/01462000/info/attack = 4` with published `shoot2`, and `Character/Weapon/01492000/info/attack = 9` with published `shoot1` / `shoot2` / `shootF`. Client re-check remained second: `CUserLocal::DoActiveSkill_BoundJump` at `0x93fdf0` still directly admits `14101004` on the shared flash-jump `nSLV / 4` row (`|vx| = 350 + 40 * floor(slv / 4)`, `vy = -(250 + 20 * floor(slv / 4))`), still keeps `13101004` out of that recovered direct special-case list, and still sends `33001002` through the doubled-`nSLV` branch before the shared quarter-step (`|vx| = 250 + 20 * floor(slv / 2)`, `vy = -(350 + 40 * floor(slv / 2))`). `get_random_shoot_attack_action` at `0x70b7a0` also still chooses the random moving-shoot row from `m_nAttackActionType` plus the type-`3` vs non-type-`3` split before returning a table action, so the simulator now keeps that row owner tighter by filtering the shared random moving-shoot candidates through the equipped weapon's currently published WZ action surface before queue-time random selection. Concretely, `SkillManager` now leaves the earlier type-`3` unresolved-owner abort, appointed-action precedence, one-time-action type-`3` prepare exception, queued skill-level revalidation, explicit `33121009` anti-repeat bypass, rapid-attack limit, and Rocket Booster startup-finished / stored-vertical-marker-cleared / foothold end seam plus the separate `500 ms` post-attack clear window intact, while narrowing the shared random shoot fallback so crossbows no longer randomize onto unpublished `shoot1` / `shootF` rows and bow / gun fallback stay on the currently published row surface instead of the previous broad shared shoot set. The earlier direct bound-jump work stays in place too: `SkillManager.TryResolveClientBoundJumpVelocity` still keeps `14101004` explicit on the client-proven `slv / 4` row, keeps Wild Hunter Jaguar Jump on the recovered `floor(slv / 2)` table, and still lets the WZ-only `13101004` path fall back through the existing authored movement profile, while `SkillManager` and `PlayerSkillStateRestrictionEvaluator` still treat `14101004` as part of the direct explicit bound-jump set rather than only through the WZ-authored type-40 / jumping-state heuristics. Focused source-side coverage under `UnitTest_MapSimulator/MovingShootParityTests.cs` now also pins the crossbow published-surface suppression, crossbow queued moving-shoot fallback staying on `shoot2`, the preserved broad unfiltered shared shoot family when no WZ surface is available, plus the recovered `33001002` and `14101004` impact rows. The row remains `Partial`: the remaining gap is now concentrated in the still-hidden exact `get_random_shoot_attack_action(...)` row contents and weighting beyond this WZ-backed published-surface narrowing, especially the unconfirmed melee attack-type rows, fuller `CAntiRepeat` construction and count-limit initialization details beyond the current threshold reset / `33121009` bypass / rapid-attack limit, any packet- or server-owned Rocket Booster end choreography outside the simulator local seam, and bound-jump row mapping that still has neither direct client recovery nor enough WZ-authored action data to safely hardcode. | Movement-coupled attack skills keep the client-owned moving-shoot queue metadata, WZ-authored appointed action preference, one-time-action type-`3` exception, anti-repeat owner surface, Rocket Booster local landing seam, the recovered direct bound-jump rows, and now a tighter WZ-backed hidden-row fallback for shared moving-shoot shooting weapons. `14101004` stays explicit on both the velocity and state-gate seams, `33001002` stays on the client-owned half-level growth table, and queued moving-shoot fallback no longer invents unpublished crossbow shoot rows when the equipped weapon's authored action surface says otherwise. The remaining mismatch is narrower and concentrated in deeper hidden-table recovery for moving-shoot random action selection, especially melee rows and exact table weighting, fuller anti-repeat construction details, packet-owned Rocket Booster end choreography, and still-unrecovered bound-jump special rows. | `SkillManager.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `UnitTest_MapSimulator/MovingShootParityTests.cs` (`CUserLocal::DoActiveSkill_BoundJump@0x93fdf0`, `CUserLocal::TryDoingRocketBoosterEnd@0x93d2d0`, `CUserLocal::TryDoingSmoothingMovingShootAttackPrepare@0x912f90`, `CUserLocal::TryDoingSmoothingMovingShootAttack@0x92de70`, `get_random_shoot_attack_action@0x70b7a0`, `CAntiRepeat::TryRepeat@0x9032b0`, `Character/Weapon/01452005`, `Character/Weapon/01462000`, `Character/Weapon/01492000`, `Skill/3300.img/skill/33001002/{action/0,info/type}`, `Skill/3312.img/skill/33121009/info/rapidAttack`, `Skill/3500.img/skill/35001001/action/0`, `Skill/3500.img/skill/35001001/info/rapidAttack`, `Skill/3510.img/skill/35101004/{action/0,info}`, `Skill/3510.img/skill/35101009/action/0`, `Skill/3510.img/skill/35101009/info/rapidAttack`, `Skill/1110.img/skill/11101005/info/type`, `Skill/1310.img/skill/13101004/{action/0,info/type}`, `Skill/1410.img/skill/14101004/info`, `String/Skill.img/14101004/name`) |
| Partial | Key-down / charge skills | The prepared-skill seam now keeps the WZ-authored startup split for release-owned charge families on both the local and remote owners, and this pass closes the remaining shared release-window drift instead of treating every release family like a full-strength hold once `IsHolding` flips true. WZ was rechecked first in the mounted export: `Skill/322.img/skill/3221001/{prepare,keydown,action,info}` still publishes a real prepare window with `prepare/time = 1200`, `action/0 = piercing`, `keydown/0/delay = 60`, and the separate `900 ms` gauge window the client resolves through `get_max_gauge_time`; `Skill/531.img/skill/5311002/{prepare,keydown,action}` still publishes `prepare/time = 1080`, `prepare/action = noiseWave_pre`, `keydown/action = noiseWave_ing`, and `action/0 = noiseWave`; while the Big Bang trio under `Skill/{212,222,232}.img/skill/*`, Evan dragon breath under `Skill/2212.img/skill/22121000` and `Skill/2215.img/skill/22151001`, and the Dual Blade charge pair under `Skill/434.img/skill/{4341002,4341003}` still only expose `info/chargingSkill = 1` plus authored prepare actions with `prepare/time = 0`, which means those families should arm the generic hold loop immediately rather than waiting for the HUD gauge window to expire before hold begins. Client evidence from `CUserLocal::OnKeyDownSkillEnd` still shows the shared `30 ms` elapsed clamp, cap through `get_max_gauge_time`, magic / shoot / melee / HP-cost release branches, and the Final Cut HP-cost branch using `elapsed * nX / get_max_gauge_time` before comparing the resulting max-HP percentage cost against current HP. `SkillManager` keeps the local startup split inside the existing seam: `ResolvePreparedSkillStartupDuration` still consumes the authored WZ prepare window for release-owned families when one actually exists, leaves zero-time authored prepare surfaces on an immediate hold transition, and `BeginPreparedSkill` still enters hold immediately for zero-startup keydown families instead of delaying until the next gauge-sized phase boundary. This pass also keeps the client release-window rule on the same seam: release elapsed, undercharge damage scaling, and Final Cut HP-cost scaling now prefer the shared HUD / `get_max_gauge_time` window ahead of the longer authored prepare window, so Piercing no longer scales against its `1200 ms` startup split when the client caps at `900 ms`, and zero-startup charge families such as the Big Bang trio, Evan breath, and the Dual Blade charge pair no longer collapse to full-strength release merely because they entered hold immediately. The earlier remote packet/runtime fix remains in place too: `PreparedSkillHudRules.ResolveRemotePreparedSkillPhases` still auto-arms release-owned zero-startup keydown families onto hold whenever the packet/session lane carries a real hold window, even without an explicit packet `auto` flag, and `RemoteUserActorPool.TryResolvePreparedSkillPhase` still preserves that zero-startup auto-hold state instead of falling back to a non-holding armed phase whenever `PrepareDurationMs = 0`. Existing parity in this row remains in place for HUD suppression/skins, mechanic sustain timing, WZ `repeat` hold branches, negative-`z` prepare/hold/release overlays, scan-code-backed local input ownership, dragon-owned HUD anchoring, release-triggered execution for the Big Bang trio (`2121001`, `2221001`, `2321001`), Marksman Piercing (`3221001`), Evan dragon breath (`22121000`, `22151001`), Dual Blade charge pair (`4341002`, `4341003`), Monkey Wave (`5311002`), and Wild Hunter swallow follow-up ownership, plus the shared minimum/cap release elapsed rule from `get_max_gauge_time` / `CUserLocal::OnKeyDownSkillEnd` and the Final Cut release HP-cost check. Focused regression coverage is now actually on disk in `UnitTest_MapSimulator/PreparedSkillChargeStartupParityTests.cs` for the WZ-authored startup-duration seam across the Big Bang trio, Piercing, Monkey Wave, Evan breath, and the Dual Blade charge pair; the remote zero-startup phase-resolution seam; remote zero-startup auto-hold persistence; the client minimum/cap release elapsed rule; the gauge-window-vs-prepare-window release scaling seam; and the Final Cut scaled HP-cost / restriction branch. | Remaining gap is narrower and still centered on client-only ownership/choreography outside the WZ-confirmed seams now modeled here. The simulator still does not reproduce the client's full raw make/break scan-code stream ownership, the wider exclusive-request catalog beyond the prepared-skill request family now sharing the `300 ms` gate, or exact family-specific post-release math/follow-up choreography beyond the currently evidenced shared elapsed clamp, `get_max_gauge_time` cap, shared linear undercharge seam, and Final Cut HP-cost branch. The current seam now guarantees that release-owned families use authored WZ startup windows when they exist, immediately arm zero-startup charge families onto the generic hold loop when WZ leaves `prepare/time = 0`, carry that same zero-startup auto-hold split through the remote prepared-skill owner path and remote runtime whenever a hold window is present, take the release path with the client-shaped minimum elapsed clamp and gauge cap instead of flattening to authored prepare time, and keep the Big Bang trio plus both Dual Blade charge branches on the same shared linear undercharge scaling seam as the earlier Piercing / Evan breath / Monkey Wave set, but unevidenced family-specific non-linear release scaling, packet-owned charge semantics beyond this prepared-skill phase split, and broader follow-up choreography are still not fully one-for-one. | `PreparedSkillHudRules.cs`, `SkillManager.cs`, `MapSimulator.RemoteUsers.cs`, `PlayerInput.cs`, `PlayerManager.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `UnitTest_MapSimulator/PreparedSkillChargeStartupParityTests.cs` (`Skill/{212,222,232}.img/skill/*/info/chargingSkill`, `Skill/322.img/skill/3221001/{prepare/time,prepare/action,keydown/0/delay,action/0,info/piercing,info/piercingBouns}`, `Skill/2212.img/skill/22121000/{prepare/action,prepare/time,common/time,info/chargingSkill}`, `Skill/2215.img/skill/22151001/{prepare/action,prepare/time,common/time,info/chargingSkill}`, `Skill/434.img/skill/{4341002,4341003}/{prepare/action,prepare/time,action/0,info/chargingSkill}`, `Skill/531.img/skill/5311002/{prepare/time,prepare/action,keydown/action,action/0,common/time,info/chargingSkill}`, `get_max_gauge_time`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::OnKeyDownSkillEnd`, `CUserLocal::DrawKeyDownBar`, `CDragon::DrawKeyDownBar`) |
| Partial | Swallow, mine, cyclone, and sit-down healing branches | The simulator keeps the earlier mine/swallow/cyclone/healing parity work on this row and tightens the same client-owned seams again instead of widening into another family. WZ recheck in the current export still shows `Skill/3310.img/skill/33101005/prepare/action = swallow_loop` and `Skill/3310.img/skill/33101005/info/dummyOf = 33101006&&33101007`, while unrelated dummy-linked families such as `Skill/3200.img/skill/32001001/info/dummyOf`, `Skill/3211.img/skill/32111005/info/dummyOf`, and `Skill/3510.img/skill/35100008/info/dummyOf` still do not publish `info/swallow`. `SkillLoader` therefore still keeps the swallow-family classifier on the authored swallow root itself instead of trusting swallow-looking action names alone: only skills that still carry the authored swallow flag plus the visible swallow root's linked dummy follow-up ids are marked `IsSwallowFamilySkill`, so unrelated dummy families no longer inherit swallow-only ownership just because they expose `dummyOf` data beside a swallow-shaped action. Client recheck against `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500` still narrows the wriggle owner to visible `33101005` only, so `SkillManager` still keeps the `500 ms` wriggle cadence and visible cancel path on that same root plus its linked follow-up ids (`33101005`, `33101006`, `33101007`) through the existing prepared-skill / visible-action seam (`swallow_pre`, `swallow_loop`, `swallow`) without accepting reverse-linked unrelated families. That preserves the earlier `900 ms` keydown threshold before release, early-release cancel, absorb-ack gated capture success, automatic `3000 ms` digest start, cancel-path teardown on failed or invalidated absorb outcomes, and captured-mob consumption before the hidden attack dummy branch fires while tightening the family classifier itself to the same WZ-authored root. The earlier mine and cyclone work remains in place on this row. Client recheck against `CUserLocal::TryDoingMine` at `0x907d70` still narrows mine ownership through the riding-vehicle-id gate, town suppression, `update_time - 1000` first-deploy lead, grounded `CVecCtrl::GetInput` X sign, and the shared `CVecCtrl::IsStopped()` clear, so the simulator still resolves Wild Hunter mine movement from grounded controller input plus the stopped-controller gate instead of foothold velocity fallback, and the hidden mine fallback still stays on the narrowed Jaguar Rider-owned live/grace-window owner plus live/equipped jaguar mount seam already landed earlier. Cyclone still sustains as a timed `32121003` body-attack loop with `1000 ms` pulses only while the cyclone action remains active. The sit-down-healing work also remains in the summon/runtime seam, but this pass closes the next client-owned poll gate there instead of broadening support ownership. WZ recheck still shows `Skill/3511.img/skill/35111011/info/{condition,minionAbility,type} = {whenUserLieDown, heal, 33}`, `Skill/3511.img/skill/35111011/summon/stand/info/range` publishing `lt=(-150,-100)` / `rb=(150,20)`, `summon/heal/info/range` publishing the narrower `lt=(-125,-100)` / `rb=(125,0)`, `common/x = 5`, and `String/Skill.img/35111011/{desc,pdesc}` still describing nearby party or team healing with the same 5-second reheal lock. Client recheck against `CUserLocal::TryDoingSitdownHealing` at `0x903d60`, `CUserPool::DoHealNearHealingRobot` at `0x94bb20`, and `CSummoned::TryDoingHealingRobot` at `0x74b3e0` now also lands directly in the same seam: the client only polls Healing Robot while the local player is seated, then checks the local robot first, remote party robots second, and skips any robot already in suspend before arming action `13` and the per-user reheal lock. `SkillManager` now mirrors that seated poll gate in the existing friendly-support path via `SummonRuntimeRules.IsSitdownHealingSupportSummon(...)`, so authored `whenUserLieDown` support summons such as Healing Robot no longer trigger while the local player is standing. `SummonedPool` still routes remote Healing Robot discovery through the local-or-party owner seam instead of the older broader remote-owner shortcut, still keeps the authored heal-branch range as the heal box owner, and still aligns packet-owned Healing Robot support suspend fallback timing with the same heal-action / hit-playback surface the local path uses when authored suspend timing is absent, while keeping the existing Healing Robot-only idle-clear owner gate so unrelated support summons do not get cleared by the same helper. Focused row coverage is now on disk in `UnitTest_MapSimulator/WildHunterSwallowParityTests.cs` for the authored swallow-root classifier, visible-root request matcher, and visible swallow transform surface, `UnitTest_MapSimulator/WildHunterMineParityTests.cs` for the grounded-input plus stopped-controller mine gate and hidden-mount fallback ownership seam, and `UnitTest_MapSimulator/SummonSupportRangeParityTests.cs` for the authored heal-branch range seam, the party-only remote Healing Robot owner filter, the seated Healing Robot poll gate, the narrowed Healing Robot suspend-clear owner gate, and the packet-owned Healing Robot suspend fallback timing seam. The row remains partial because the simulator still does not mirror the client's exact swallow packet ordering, the exact server-owned swallow capture/digest request choreography, the remaining `TryDoingMine` riding-vehicle-id ownership nuances beyond the current active-owner plus update-time grace-window gate and live/equipped jaguar fallback, the exact request/skill-packet-owned Healing Robot suspend reset choreography beyond the current seated poll gate plus authored playback owner, or the remaining non-Wild-Hunter swallow-family nuances one-for-one | These branches now stay closer to their real client owners instead of flattening back into the generic cast/runtime path. Mine still respects the client town suppression, update-time-owned hidden-owner grace window, narrowed hidden jaguar fallback, and the client-shaped grounded `CVecCtrl` input plus stopped-controller gate instead of a simulator-only foothold-velocity fallback; cyclone still stops pulsing once the sustained cyclone action falls off the avatar; swallow still routes charge, cancel, absorb success/failure, digest timing, and hidden follow-up attack through one WZ-backed swallow-family seam, its wriggle pulses still stop once the visible swallow owner falls off, its linked dummy follow-up ids are now classified only from an authored swallow root instead of from any dummy-linked swallow-looking family, and its local request matching still stays one-way on that visible root plus linked follow-up ids; and sit-down healing now also stays behind the same client-shaped seated poll gate before the existing local/party owner filter, authored heal range, reheal-lock support window, and narrowed suspend clear path run. The remaining visible mismatch is narrower and centered on exact swallow packet choreography, the last riding-vehicle-id details in `TryDoingMine`, the last request/packet-owned Healing Robot suspend-reset details outside the current seated poll plus authored playback owner seam, and any residual non-Wild-Hunter swallow-family behavior the client owns outside the current local cancel/request path | `SkillManager`, `SkillLoader.cs`, `SkillData.cs`, `PlayerCharacter`, `MapSimulator`, `SummonedPool.cs`, `SummonRuntimeRules.cs`, `UnitTest_MapSimulator/SummonSupportRangeParityTests.cs`, `UnitTest_MapSimulator/WildHunterMineParityTests.cs`, `UnitTest_MapSimulator/WildHunterSwallowParityTests.cs` (`CUserLocal::TryDoingSwallowBuff`, `CUserLocal::TryDoingSwallowMobWriggle`, `CUserLocal::TryDoingMine`, `CUserLocal::TryDoingCyclone`, `CUserLocal::TryDoingSitdownHealing`, `CUserPool::DoHealNearHealingRobot`, `CSummoned::TryDoingHealingRobot`, `CVecCtrl::GetInput`, `CVecCtrl::IsStopped`, `Skill/3200.img/skill/32001001/info/dummyOf`, `Skill/3211.img/skill/32111005/info/dummyOf`, `Skill/3300.img/skill/33001001/info/type`, `String/Skill.img/33001001`, `Skill/3310.img/skill/33101005/{prepare/action,info/dummyOf}`, `Skill/3510.img/skill/35100008/info/dummyOf`, `String/Skill.img/35111011/{desc,pdesc}`, `Skill/3511.img/skill/35111011/{info/{condition,minionAbility,type},common/{hp,x},summon/stand/info/range,summon/heal/info/range}`, `CUserLocal::TryDoingMine@0x907d70`, `CUserLocal::TryDoingSwallowMobWriggle@0x93f500`, `CUserLocal::TryDoingSitdownHealing@0x903d60`, `CUserPool::DoHealNearHealingRobot@0x94bb20`, `CSummoned::TryDoingHealingRobot@0x74b3e0`) |
| Partial | Summon simulation | The simulator still keeps the broader packet-owned summon parity already landed here: generic summon lifecycle/timers, WZ-backed attack branch and metadata loading, projectile and target-impact branch retention, Tesla triangle targeting, self-destruct prepare/die handling, packet-owned prepare/attack visual parity, taunt-only puppet registration, shared summon rule selection between local and packet-owned seams, the delayed bird tile overlay handoff, the `4111007` reactive chain with char-level -> exact-level -> root `ball` precedence, and the earlier sourced-attack-frame hit-path split between `info/hit` and attack-frame-owned hit clips. WZ was rechecked first again in the mounted export for the remaining packet-owned mob-hit slice instead of widening transport ownership: `Mob/3220000.img/attack1/info/hit/attach = 1` still keeps the base attached hit family, `Mob/3220000.img/attack1/13/hit/attach = 1` still publishes the later frame-owned attached clip, and the same attack continues through later authored frames after `13`, which keeps a real authored gap between the source frame that owns the hit clip and the later live attack frame the runtime may already be on when packet-owned hit playback is resolved. The adjacent alias surface also remains real in the mounted export: `Mob/9300314.img/attack1/info/attachfacing = 1` still coexists with `Mob/9300314.img/attack1/info/hit/attachfacing = 1`, and `Mob/9300275.img/attack1/info/attachfacing = 1` still keeps the same info-side facing alias seam the loader already honors. Client evidence stayed second and remains the same on the literal packet/runtime owners this row is anchored to: `CSummonedPool::OnPacket` at `0x75ac70` still owns the packet family dispatch, `CSummoned::OnAttack` / `CSummoned::Hit` remain the relevant summon-side playback owners for this row, `CMob::GetAttackInfo` at `0x641330` still resolves attack metadata from the live action code rather than exposing another packet-side hit-owner decoder, `CSummoned::OnHit` at `0x74bc80` still resolves summon-hit presentation from decoded `(attackIndex, mobTemplateId, facing)` plus mob-template attack info instead of requiring a live mob object lookup, and `SKILLENTRY::{GetTileUOL,GetBallUOL}` at `0x437180` / `0x569390` still keep the earlier tile/ball ownership split already modeled by `SkillLoader`, `ZoneEffectData`, and `ProjectileData`. This pass stayed inside the same mob-hit and packet-owned summon seams and closes the next sourced-frame drift instead of widening into new post-effect families: `MobAnimationSet.GetAttackHitEffectEntry(...)` now keeps the latest frame-owned `attackN/<frame>/hit` entry whose source frame is at or before the current live attack frame before falling back to `info/hit`, so packet-owned summon hit playback no longer drops straight back to metadata frame `0` as soon as the mob runtime has advanced one authored frame past the hidden source clip. The packet-owned hit side also now keeps the client's template-owned fallback shape inside the same `LifeLoader` / `SummonedPool` seam: when no live mob instance of the decoded template is present, `SummonedPool` can still resolve the mob hit clip and `CharDam` sound from cached template attack assets instead of silently dropping summon-hit feedback altogether. That keeps the earlier sourced-attack-frame carry-through effective for the common late-packet case and also keeps packet-owned summon-hit presentation alive for template-only packet hits that the client still renders from decoded mob-template data. The earlier branch-owned timing, authored projectile/hit `pos` backfill, bird-tile and `4111007` post-effect timing, local named attack-branch prepare suppression, packet-owned raw-action branch selection, and the learned-template Tesla / SG-88 bridge work remain in place. Focused regression coverage is now actually on disk in `UnitTest_MapSimulator/PacketOwnedSummonHitAttachParityTests.cs`, covering latest-prior frame-owned hit-entry selection, fallback to `info/hit` before the first authored frame-owned hit clip, and sourced-frame attach resolution at a nonzero sourced attack frame, plus `UnitTest_MapSimulator/SummonedPoolPacketHitFallbackParityTests.cs` for live-hit precedence versus template-backed packet-hit fallback selection. The row remains partial because the remaining gap is narrower but still real: the simulator still does not synthesize every hidden client `MobAttackInfo::sHit` / `bHitAttach` case one-for-one beyond the currently observed WZ surfaces and the modeled sourced-frame attach/facing path; `CSummoned::OnAttack` post-effects are still limited to the confirmed bird tile family plus the `4111007` reactive chain instead of every hidden post-effect case; exact client `sTileUOL` precompute and any hidden tile-family construction beyond the observed WZ family nodes remain unresolved because current client evidence still only proves `GetTileUOL` returning the stored field; literal GR2 chain-layer ownership, exact textured bolt choreography, and literal action-layer canvas ownership or ordering remain unresolved; the simulator-owned `/summonedpacket` loopback plus the live-session `SummonedOfficialSessionBridgeManager` still stop short of a full official `CUserPool` transport and ownership decode; and the late-packet frame-owned hit-entry hold plus template-backed packet-hit fallback still remain render-safe inferences from the authored WZ hit surface and the client `OnHit` template-owner seam rather than from a newly recovered dedicated client decoder for every deeper mob-state-dependent case. | Packet-owned summons are no longer just spawn/remove stubs: they now keep client-shaped owner registration, packet-owned attack/hit/removal playback, frame-scoped attached-hit behavior, the confirmed bird tile post-effect family, the confirmed `4111007` reactive chain, the selected tile/ball branch identity on the same packet-owned owner without collapsing that branch identity back to a selected frame path, the real sourced attack-frame owner for hidden mob hit clips, the nearest authored prior frame-owned hit clip when packet-owned hit playback lands after that sourced frame has already advanced, and template-backed summon-hit clip/audio fallback when the decoded mob template is not currently instantiated on the map. That keeps future parity work anchored to the same `SummonedPool` / `MobAnimationSet` / `LifeLoader` seam the client-backed row already uses for packet-owned summon lifecycle and mob-hit presentation, while the remaining hidden hit cases, unresolved tile-UOL precompute, GR2 ownership, and official `CUserPool` transport gaps stay explicit. | packet-owned summon runtime (`CSummonedPool::OnPacket`, `CSummoned::OnAttack`, `CSummoned::OnHit`, `CSummoned::Hit`, `SummonedPool.cs`, `PacketOwnedSummonUpdateRules.cs`, `SkillLoader.cs`, `ZoneEffectData`, `ProjectileData`, `LifeLoader.cs`, `MobAnimationSet.cs`, `MobAnimationSet.GetAttackHitEffectEntry(...)`, `UnitTest_MapSimulator/PacketOwnedSummonHitAttachParityTests.cs`, `UnitTest_MapSimulator/SummonedPoolPacketHitFallbackParityTests.cs`, `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, `Skill/312.img/skill/3120010/{CharLevel,ball,tile,summon}`, `Skill/434.img/skill/4341003/flipBall`, `Mob/3220000.img/attack1/{info/hit,13/hit}`, `Mob/9300314.img/attack1/{info/attachfacing,info/hit/attachfacing}`, `Mob/9300275.img/attack1/info/attachfacing`, `SKILLENTRY::GetTileUOL@0x437180`, `SKILLENTRY::GetBallUOL@0x569390`, `CMob::GetAttackInfo@0x641330`) |
| Partial | Remote summoned-pool packet parity | The simulator still keeps the packet-owned summon transport seam in `SummonedPool`, but the remote attack and hit side is tighter again. It already mirrors the observed `0x116`-`0x11B` packet family, local-versus-remote owner registration, packet-owned move-path sampling, Tesla third-coil refresh, taunt-side ownership, packet hit damage bookkeeping, nested mob `attack/info/hit` metadata with UOL resolution, frame-scoped `attach` / `attachfacing` runtime behavior, authored per-frame summon alpha ramps on both live and detached one-time-action playback, the delayed bird tile overlay handoff from the filtered packet target list, and the `4111007` reactive chain with char-level -> exact-level -> root `ball` precedence before summon-owned or generic fallback. WZ re-check against the mounted export still keeps the same confirmed post-effect surface: `Skill/311.img/skill/3111005/tile` publishes six root tile bands plus `effectDistance=10`, `Skill/321.img/skill/3211005/tile` publishes three root tile bands plus `effectDistance=20`, and `Skill/312.img/skill/3120010` keeps the broader `{CharLevel,ball,tile,summon}` surface. This pass also rechecked the mob-hit side against the mounted `Mob` export and confirmed that the currently modeled hidden hit-owner seam is real in authored data, not just a client guess: `Mob/3220000.img/attack1/info/hit/attach` still keeps the base attached hit family, while `Mob/3220000.img/attack1/13/hit/attach` still publishes a later attack-frame-owned attached hit clip. Client evidence remains the same on the literal UOL owner this row was still approximating: `SKILLENTRY::GetBallUOL` at `0x569390` still selects char-level data first, then exact level, then root, and prefers `flipBall` when the caller asks for it and that family is non-empty; `SKILLENTRY::GetTileUOL` at `0x437180` still only returns the precomputed `sTileUOL` field. This run stayed inside the same loader and packet-owned summon seams: `SkillLoader` still stores family-owned `tile`, `ball`, and `flipBall` UOL paths separately from the per-frame animation paths for root, `level/<slv>`, and `CharLevel/<requiredLevel>` variants; `ZoneEffectData` and `ProjectileData` still resolve those family UOLs with the same render-safe char-level -> exact-level -> root precedence already used for animation-family selection; `PacketOwnedSummonUpdateRules` still feeds the packet-owned delayed bird overlay plus the scheduled/live `4111007` chain owner with those family UOLs instead of flattened selected-frame paths; and the mob-hit playback side now also carries the renderable hit clip's source attack-frame index through `LifeLoader`, `MobAnimationSet.AttackInfoMetadata`, `PacketOwnedSummonUpdateRules`, and `SummonedPool` so packet-owned summon hit playback resolves `attach` / `attachfacing` against the actual sourced attack frame instead of always rebasing hidden attack-frame hit clips to metadata frame `0`. That keeps later frame-owned hit clips like `attack1/13/hit` on the same attach owner the client/WZ data authored, while the earlier detached fallback point still prevents later `attach = 0` frames from inheriting the frame-0 attached coordinate when ownership flips mid-hit. Focused regression coverage for that helper is now on disk in `UnitTest_MapSimulator/PacketOwnedSummonHitAttachParityTests.cs`, covering renderable attack-frame source-index carry-through, hit-anchor attach resolution against a non-zero sourced attack frame, and `ResolveAttackHitNode(...)` keeping `info/hit` at source frame `0` while frame-owned hit clips preserve their real attack-frame index. The row remains partial because the simulator still does not synthesize every hidden client `MobAttackInfo::sHit` / `bHitAttach` case one-for-one beyond the currently observed WZ surfaces and the modeled frame-scoped attach/facing plus sourced-attack-frame-hit path; `CSummoned::OnAttack` post-effects are still limited to the confirmed bird tile family plus the `4111007` reactive chain instead of every hidden post-effect case; exact client `sTileUOL` precompute and any hidden tile-family construction beyond the observed WZ family nodes remain unresolved because current client evidence only exposes `GetTileUOL` returning the stored field, not how every tile UOL is authored; literal GR2 chain-layer ownership, exact textured bolt choreography, and literal action-layer canvas ownership or ordering remain unresolved; and the simulator-owned `/summonedpacket` loopback plus the live-session `SummonedOfficialSessionBridgeManager` still stop short of a full official `CUserPool` transport and ownership decode. | Packet-owned summons are no longer just spawn/remove stubs: they now keep client-shaped owner registration, packet-owned attack/hit/removal playback, frame-scoped attached-hit behavior, the confirmed bird tile post-effect family, the confirmed `4111007` reactive chain, the selected tile/ball branch identity on the same packet-owned owner without collapsing that branch identity back to a selected frame path, and the real sourced attack-frame owner for hidden mob hit clips instead of flattening those clips back to metadata frame `0`. That keeps future parity work anchored to the same `SummonedPool` seam the client uses for packet-owned summon lifecycle and post-attack presentation, while the remaining hidden hit cases, unresolved tile-UOL precompute, GR2 ownership, and official `CUserPool` transport gaps stay explicit. | packet-owned summon runtime (`CSummonedPool::OnPacket`, `CSummoned::OnAttack`, `CSummoned::Hit`, `SummonedPool`, `PacketOwnedSummonUpdateRules`, `SkillLoader`, `ZoneEffectData`, `ProjectileData`, `LifeLoader`, `MobAnimationSet.AttackInfoMetadata`, `UnitTest_MapSimulator/PacketOwnedSummonTileOverlayParityTests.cs`, `UnitTest_MapSimulator/PacketOwnedSummonBranchPathParityTests.cs`, `UnitTest_MapSimulator/PacketOwnedSummonHitAttachParityTests.cs`, `Skill/311.img/skill/3111005/tile`, `Skill/321.img/skill/3211005/tile`, `Skill/312.img/skill/3120010/{CharLevel,ball,tile,summon}`, `Skill/434.img/skill/4341003/flipBall`, `Mob/3220000.img/attack1/{info/hit,13/hit}`, `SKILLENTRY::GetTileUOL@0x437180`, `SKILLENTRY::GetBallUOL@0x569390`) |
| Partial | Summoned actor update/runtime parity | The simulator still keeps summon-owned actor update/runtime ownership (movement snapshots, spawn/prepare/attack/hit/die routing, Tesla armed-prepare idle fallback, self-destruct expiry handoff, packet-owned hit-overlay interrupt clearing, and named-branch timing that avoids forcing generic `summon/prepare` onto authored branches). This pass stayed in the same `SummonRuntimeRules` seam and closed the next local explicit-self-destruct leak that was still falling back to caller-local removal playback when no ordinary support or summon-action branch actually fired before expiry or forced self-destruct. WZ was rechecked first against the mounted export for the authored branches that constrain that fallback: `Skill/132.img/skill/1321007/summon/skill{2,3,4,5,6}` still publishes the authored Beholder buff branches; `Skill/3511.img/skill/35111011/summon/heal` still publishes the Healing Robot support branch; `Skill/3512.img/skill/35121009/{info/{type,minionAbility,selfDestructMinion},summon/subsummon}` still publishes the summon-action family as `type=33`, `minionAbility=summon`, `selfDestructMinion=1`; and the stand-only support skills under `Skill/3511.img/skill/35111005/summon/stand` and `Skill/3512.img/skill/35121010/summon/stand` still keep `info/selfDestructMinion=1` without separate authored `heal` / `support` branches. Client evidence was then rechecked in IDA: `CSummoned::OnSkill` at `0x74a940` still clears any older one-time owner and immediately routes the decoded packet action into `CSummoned::SetAttackAction`, while `CSummoned::SetAttackAction` at `0x74a0f0` still just stamps `m_nOneTimeAction` and re-prepares the action layer. The simulator now keeps that same raw-action owner even when local self-destruct playback reaches the explicit branch without first landing in a normal performed support or attack owner: `SummonRuntimeRules.TryResolveExplicitSelfDestructPlayback(...)` centralizes the explicit final-branch plus action-`16` resolution, and `SkillManager` now uses that helper both for natural-expiry self-destruct handoff and for forced self-destruct damage handling before pending-removal scheduling begins. That means stand-only support summons and summon-action summons no longer fall back to generic removal or unnamed attack playback when expiry or forced self-destruct reaches the explicit self-destruct window with no earlier performed support or target-hit branch, while packet-owned `OnSkill` action `16` still lands on the same authored self-destruct final branch instead of dropping into assist-owned fallback selection. Focused regression coverage is now on disk in `UnitTest_MapSimulator/SummonRuntimeActionParityTests.cs` for stand-only support explicit self-destruct ownership, summon-action explicit self-destruct ownership, and preservation of authored removal-branch action-`16` ownership. The row remains partial because the deeper client-only seams are still outside current simulator visibility: exact hidden summon HP and full `CalcDamage::PDamageSummoned` / `MDamageSummoned`, broader `CVecCtrl::UpdatePassive` side effects beyond the confirmed passive effect trigger, deeper Tesla state-machine transitions and refresh rules, and the remaining remote assist-family branch choices that still depend on client-only state not exposed by current packet/WZ data. | This keeps more of real summon runtime ownership inside the summon layer instead of collapsing either local or packet-owned assist families to ad hoc runtime fallbacks: local and packet-owned body-contact damage still share the same mob-authored base-damage surface plus outgoing-damage modifiers, packet-owned `OnSkill` branch ownership still lands on authored `skill1..skill6` when packet actions provide those branch ids, packet-owned named summon branches still avoid inheriting a generic prepare window, packet-owned generic prepare branches still promote into live attack state instead of staying pinned on one coarse packet state through the whole action, and packet-owned mob-hit overlays still get interrupted both when summon-owned action-layer changes begin and when packet-owned attack playback completes back into the steady summon layer. The important gap closed here is that explicit self-destruct playback now stays on the same raw-action seam for both the already-performed assist owners and the previously leaking no-target/no-support local fallback path, so local expiry or forced self-destruct no longer drops stand-only support or authored summon-action summons back to generic removal playback just because no earlier performed branch set `CurrentAnimationBranchName` first. Remaining mismatch risk is now concentrated in deeper hidden damage formulas, unmodeled passive-update side effects, deeper Tesla transitions, and the last remote/client-only assist selectors rather than in the observed authored support-branch surface, authored summon-action branch ownership, or explicit self-destruct branch ownership. | summon actor/runtime layer (`CSummoned::Update`, `CVecCtrl::UpdateActive`, `CVecCtrl::UpdatePassive`, `CSummoned::OnHit`, `CSummoned::OnSkill`, `CSummoned::SetAttackAction`, `CSummoned::SetMoveAction`, `CSummoned::ProcessAction`, `CSummoned::ProcessAttack`, `CSummoned::SetDamaged`, `SummonRuntimeRules.cs`, `SkillManager.cs`, `SummonedPool.cs`, `UnitTest_MapSimulator/SummonRuntimeActionParityTests.cs`, IDA `CSummoned::OnSkill` (`0x74a940`), IDA `CSummoned::SetAttackAction` (`0x74a0f0`), `Skill/132.img/skill/1321007/summon/skill{2,3,4,5,6}`, `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3512.img/skill/35121009/{info/{type,minionAbility,selfDestructMinion},summon/subsummon}`, `Skill/3511.img/skill/35111005/{info/selfDestructMinion,summon/stand}`, `Skill/3512.img/skill/35121010/{info/selfDestructMinion,summon/stand}`, `Skill/3511.img/skill/35111002/summon/{stand,prepare,attackTriangle,die}`, `CSummoned::TryDoingHealingRobot`) |
| Partial | Weapon afterimage / melee trail parity | The simulator still keeps the broader WZ-backed afterimage surface already landed for this row: skill-authored plus shared `Character/Afterimage/<family>/<masteryIndex>` catalogs, weapon-item `info/afterImage` family selection with narrow sword-sibling fallback only, local basic-melee registration, charged `charge/<element>` rewrites for the confirmed Page, Paladin, Thunder Breaker, and Aran families, alias-aware action matching, `1221009 blast -> stabO2`, client-backed raw-range normalization for `1221009`, `57 -> 41`, and `74`, remote afterimage playback with fade tails, ordered remote WZ action fallback, the single-authored plus sole-renderable remote pose promotion seams, remote prepared-skill release teardown from the official melee packet, and the earlier remote charge fallback that already stayed inside the shared remote temporary-stat plus remote melee owner. This pass stayed inside the existing local or remote afterimage seams again and narrowed the remaining `CreateAfterimageLayer` drift without widening transport ownership. WZ was rechecked first for the sparse authored surfaces that make the reset behavior visible: `Character/Afterimage/swordOS/0/swingO1/2/0` still publishes a single authored afterimage canvas with `delay=200` and `a1=0`, `Character/Afterimage/swordOS/charge/1/stabO2/1/{0,1}` still publishes ordered charge subframes with `delay=50` then `delay=300` and the terminal subframe still publishes `a1=0`, and the `1221009` skill-authored branch still keeps its sparse surface under `Skill/122.img/skill/1221009/afterimage/swordOS/stabO2/1/0` with `delay=60`. Client evidence was then rechecked directly in IDA for the owner seams the simulator is approximating: `CActionMan::GetMeleeAttackRange` in this client still only proves the already-modeled raw-range overrides (`74`, `57 -> 41`) while `1221009` remains the caller-owned raw-action redirect already modeled outside that function; `CActionMan::GetWeaponAfterImage` still allocates per-action canvas arrays by count and then requests child names `0..u-1` explicitly after resolving the authored property child for that raw action; and `CActionMan::CreateAfterimageLayer` at `0x428a20` still resets layer alpha, calls `RemoveCanvas(-2)`, resolves `GetWeaponAfterImage`, and returns immediately when the current raw-action branch has no canvas array instead of keeping the previously loaded canvas alive. The simulator now mirrors that smaller owner detail more closely through a single shared playback seam: `MeleeAfterimagePlaybackResolver` now owns both the active-frame snapshot refresh and the fade-start snapshot capture or clear rules, `PlayerCharacter` and `RemoteUserActorPool` both route their retained-cache updates through that same resolver, null authored snapshots still overwrite the retained cache with the exact resolved raw frame index, and full current-frame or fade-start lookup misses now clear the retained cache instead of leaving the previous rendered trail alive through the client-shaped reset miss. The earlier dense numeric frame probe, sole-authored frame-set promotion, authored alpha-ramp preservation, authored activation-delay seam, and fade-start frame-index snapshot seam all remain in place, but the retained cache no longer survives either the no-canvas branch or the broader lookup-miss branch that still sits behind the same `RemoveCanvas(-2)` owner detail. Focused regression tests are now on disk in `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` for the no-canvas snapshot seam, null-snapshot cache overwrite, current-frame cache-clear seam, and fade-start cache-clear seam. The remaining gap is narrower again but still real: direct IDA recheck for this client version still only proves the current raw-range owner surface, so any further range overrides need new client evidence; the simulator still does not reproduce literal GR2 layer ownership and side effects such as the exact `RelMove` / `RemoveCanvas` / `LoadCanvas` choreography inside `CreateAfterimageLayer`; and the broader remote field packet surface still needs the rest of a one-for-one `CUserRemote::OnAttack` / `CUserRemote::OnMeleeAttack` transport lift and client-owned side effects beyond the currently consumed attack envelope, mob-hit, bullet-item, release-follow-up, broader remote charge-family fallback, and explicit-skill-range-owner slices | Many melee jobs rely on afterimage trails and authored swing ranges to sell timing and hit shape, and the simulator now consumes the same WZ-backed trail and arc data for local melee skill playback, local basic melee attacks, packet-owned local melee hitbox resolution, and simulator-owned remote actors while respecting the weapon item's authored afterimage family instead of forcing every sword through the long-family arcs. The important visual gap that remained in the current row had already narrowed when local and remote afterimages stopped flattening every authored subframe into one simultaneous fully opaque draw and started preserving authored WZ alpha ramps; this pass closes the next smaller runtime drift by putting the client's layer-reset ownership behind one shared active or fade snapshot resolver instead of leaving the local and remote owners to drift. Dense numeric canvas probing, sole-authored sparse-frame recovery, authored alpha ramps, activation delay, fade-start frame snapshots, and remote charge-family fallback all stay on the same existing loader/runtime seams, while the remaining visible mismatch is now concentrated in literal GR2 layer choreography and the rest of the official remote melee transport rather than stale retained-frame behavior inside the current playback owners. | `SkillLoader.cs`, `SkillData.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `CharacterLoader.cs`, `CharacterAssembler.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `CharacterData.cs`, `MapSimulator.RemoteUsers.cs`, `PreparedSkillHudRules.cs`, `ClientMeleeAfterimageRangeResolver.cs`, `AfterImageChargeSkillResolver.cs`, `MeleeAfterimagePlaybackResolver.cs`, `UnitTest_MapSimulator/WeaponAfterimageClientParityTests.cs` (`CActionMan::Init`, `CActionMan::GetWeaponAfterImage`, `CActionMan::CreateAfterimageLayer`, `CActionMan::GetMeleeAttackRange`, `get_action_name_from_code`, `CUser::GetAfterimageUOL`, `CUser::RegisterAfterimage`, `CUserRemote::OnAttack`, `CUserRemote::OnMeleeAttack`, `SecondaryStat::DecodeForRemote`, `Character/00002000.img`, `Character/Afterimage/swordOS/{0/swingO1/2,charge/1/stabO2/1}`, `Character/Afterimage/*`, `Skill/122.img/skill/1221009/{action,afterimage}`, `Skill/434.img/4341004/common/{lt,rb}`, remote temporary-stat payloads) |
| Partial | Skill cooldown UI parity | QuickSlot overlays and skill hover cost lines, both skill-book icon overlays, the hotkey and off-bar cooldown trays, the shared packet-owned/server-owned cooldown ownership seam in `SkillManager`, the `UI/Basic.img/Notice3` off-bar notice stack, and the Battleship sentinel durability-side effect path still live on the same existing cooldown UI/runtime seam instead of parallel per-surface cooldown math. This run kept that shared owner in place and closed the missing regression-coverage part of the row without widening the runtime surface. WZ was rechecked first against the current mounted export: `UI/UIWindow.img/Skill/CoolTime` still exposes `16` stepped cooldown-mask canvases, `UI/UIWindow2.img/Skill/main/CoolTime` still exposes the matching `16` stepped family, and `UI/Basic.img/Notice3` still exposes the `t/c/s` stack at `266x21`, `266x20`, and `266x55`. `Skill/522.img/skill/5221006/common` still authors `mpCon`, `epad`, `epdd`, `emdd`, `emhp`, `emmp`, and `cooltime`, so the existing `SkillLevelData` authored-property seam remains the right owner for Battleship-family tooltip ordering. The source seam remains unchanged: `SkillManager.ResolveCooldownMaskFrameIndex` still consumes the full authored `0..15` cooldown-mask family with continuous duration progress, `SkillManager.ResolveCooldownMaskFallbackFillRatio` still preserves the authored `CoolTime/15` last live sliver when WZ masks are missing, `QuickSlotUI`, `SkillUI`, `SkillUIBigBang`, and both status-bar cooldown trays still source mask frames and countdown text through the shared `SkillManager` cooldown-visual helper on top of `TryGetCooldownUiState`, `SkillCooldownTooltipText` still keeps the shared `Cooldown:` label segmentation for ready-state book and quick-slot cost lines, tray hover still consumes the same shared inline formatter, and the packet-owned Battleship sentinel durability path still updates `SkillManager.SetAuthoritativeVehicleDurabilityPresentation` so durability text stays on the same authoritative tooltip seam while remaining out of cooldown-mask visuals. Focused regression coverage is now actually on disk in `UnitTest_MapSimulator/SkillCooldownTooltipTextTests.cs` for shared plain-text tooltip output, shared ready/active cooldown tooltip markup, shared tray-hover state-line markup, explicit durability-state tooltip markup, Battleship authored stat ordering through durability-state markup, vehicle-durability states staying out of cooldown-mask visuals, the shared `0..15` cooldown-mask frame progression, the fallback fill-ratio seam, and the final live-sliver counter path near cooldown completion. The remaining parity gap is narrower again: exact cooldown-notification art family selection versus the current WZ-backed `Notice3` best fit, any last notice micro-offset or timing/fade differences beyond the shared `44px` top-center seam, final client compositor spacing and per-fragment emphasis polish across book, quick-slot, and tray hover tooltips, exact Battleship `CTemporaryStatView` icon/tray art and any durability-specific HUD widgeting beyond the mounted override plus shared UI-state durability presentation already modeled here, and any additional bespoke cooldown-only HUD widgets beyond the quick-slot, skill windows, status-bar trays, and off-bar notice stack. | Cooling-down skills remain synchronized across quick-slot masks, both skill-book variants, both status-bar trays, both skill-book hover cost lines, tray hover state lines, and off-bar started/blocked/ready notices because those surfaces all read the same authoritative cooldown UI-state seam, one cooldown-visual helper, and one tooltip formatter on top of the authored `CoolTime` family. Packet-owned refreshes still preserve the original cooldown start tick without retriggering the start-notice seam, authoritative clears and natural expiry still stop every cooldown surface together without falling back to authored WZ timers, quick-slot plus both skill books still keep `Cooldown: Ready` segmented through the same markup owner used by tray hover, the shared mask helper still reaches the authored `CoolTime/15` last-step frame before cooldown completion instead of flattening to a `0..14` range, and the quick-slot plus tray fallback rectangles still mirror that same final sliver when WZ masks are absent instead of dropping early. Battleship durability/stat presentation remains routed through the shared formatter, the packet-owned sentinel's `SkillManager` vehicle-durability presentation, and WZ-authored property order, and this row now has an on-disk regression harness that covers that shared cooldown seam instead of just documenting it. The simulator still does not reproduce the client's exact cooldown-notification art choice, final notice timing or micro-offset polish, exact skill-tooltip compositor spacing/color polish, exact Battleship temporary-stat tray/icon visuals, or any extra cooldown-only HUD widgets that may still live outside the currently modeled shared seams. | `QuickSlotUI.cs`, `SkillUI.cs`, `SkillUIBigBang.cs`, `SkillCooldownTooltipText.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `StatusBarUI.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `SkillManager.cs`, `SkillCooldownNoticeUI.cs`, `SkillCooldownNotificationTransition.cs`, `Skill/522.img/skill/5221006/common`, `UI/UIWindow.img/Skill/CoolTime`, `UI/UIWindow2.img/Skill/main/CoolTime`, `UI/Basic.img/Notice3`, `UnitTest_MapSimulator/SkillCooldownTooltipTextTests.cs` |
| Partial | Client-timer skill expiry and cancellation | `SkillManager` still owns the centralized client-timer registry for buff expiry, timed prepared-skill release, repeat-sustain expiry and mode-end, cyclone teardown, Wild Hunter swallow digest completion, WZ-timed local skill-zone expiry, and WZ-timed local summon expiry through one `UpdateClientTimer`-style seam, and cancel routing still normalizes through connected WZ `info/type` + `affectedSkill` + `dummyOf` families plus the client-owned pre-send alias rewrite in `CUserLocal::SendSkillCancelRequest`. `SummonedPool` still reuses that connected-family matcher for packet-owned local summon cancellation, connected-family level recovery, authored-duration fallback, inherited local-duration fallback, authored pending-removal priming for natural non-self-destruct expiry, and the local packet-owned self-destruct branch that preserves support suspend timing, authored support-hit timing/surface, target-backed packet attack presentation, summon-position fallback when no target resolves, and the authored self-destruct target cap. WZ was rechecked first for the representative support self-destruct branch: `Skill/3511.img/skill/35111011` still publishes `info/type=33`, `info/minionAbility=heal`, `info/condition=whenUserLieDown`, `info/selfDestructMinion=1`, `common/selfDestruction`, `summon/{summoned,stand,heal,die}`, `summon/heal/info/range lt=(-125,-100) rb=(125,0)`, `summon/die/info/range lt=(-120,-110) rb=(120,10)`, `summon/die/info/mobCount=6`, and `hit/0/hitAfter=990`; the connected cancel-family WZ evidence this timer bridge depends on also remains present in the current extracted data source, now including the Battle Mage aura alias rows that line up with the client rewrite (`Skill/3212.img/skill/32120000/info/type=51` with `affectedSkill=32001003`, `Skill/3211.img/skill/32110000/info/type=50` with `affectedSkill=32101002`, `Skill/3212.img/skill/32120001/info/type=50` with `affectedSkill=32101003`, `Skill/3211.img/skill/32110007/info/type=10` with `affectedSkill=32001003`, `Skill/3510.img/skill/35100008/info/dummyOf=35101009&&35101010`, `Skill/3510.img/skill/35101009/info/affectedSkill=35001001`, `Skill/2112.img/skill/21120011/info/affectedSkill=21100002&&21110003`). Direct IDA re-check stayed second and still anchors the client seam at `CUserLocal::SendSkillCancelRequest` (`0x93d730`) for cancel-side fallout and `CSummoned::Update` (`0x757f70`) plus `CSummoned::TryDoingAttack` (`0x756860`) for expiry/runtime ownership, including the hardcoded pre-send rewrites `32120000 -> 32001003`, `32110000 -> 32101002`, and `32120001 -> 32101003`, the vehicle-valid branch, the keydown prepare-animation removal gate, the local dragon one-time-action cleanup, the mirrored-rectangle probe, and the local-owner side choice before `ProcessAction` / `ProcessAttack`. This pass keeps work inside that same seam and closes another remaining cancel-side drift: `ClientSkillCancelResolver` now mirrors those hardcoded pre-send alias rewrites even when WZ reverse-link evidence is absent, and `SkillManager.RequestClientSkillCancel` now applies the packet-visible cancel side effects and packet-owned local summon callback through that normalized packet skill id instead of replaying the full connected family through the post-send fallout branch. The previously landed packet-owned natural-expiry fallback remains in place: stale/dead mobs are still dropped from both prior packet-target reuse and authored-range fallback candidate sets, and once the mirrored fallback has chosen a side it still ranks fallback candidates using that chosen side instead of the summon's pre-existing facing. Focused regression coverage is now on disk in `UnitTest_MapSimulator/ClientSkillTimerCancelParityTests.cs` for the client alias rewrites and the keydown cancel inclusion/exclusion gate used by the dragon cleanup branch. The row remains partial because the simulator still does not mirror the client's full timer catalog or full summon-side expiry fallout for packet-owned summons: target acquisition and combat results still fall back to the current authored-range approximation instead of the full `CSummoned::Update` runtime when no authoritative packet target history exists, remote packet-owned self-destruct expiry still relies on incoming packet/runtime ownership rather than local synthesis, and any additional `SendSkillCancelRequest` consumers beyond the currently surfaced prepared-skill, swallow-reset, vehicle-valid, keydown-cleanup, and dragon one-time-action fallout are still outside the current local WZ + packet + connected-family timer view. | Timed skill teardown still runs through one shared local seam instead of scattered expiry checks, and connected WZ cancel families plus the client's hardcoded Battle Mage alias rewrites now tear down prepared state, repeat/cyclone state, buffs, local summons, local zones, and packet-owned local summons through the same normalized request surface. This run narrows the remaining cancel drift by making the simulated post-send fallout use the same packet-visible skill id that the client emits after `SendSkillCancelRequest` rewrites the request, while leaving the already-landed packet-owned natural-expiry target filtering and owner-side fallback ordering in place. Remaining risk is now concentrated on unmodeled client-owned timer families, the exact `CSummoned::Update` target-selection and combat-resolution rules that still sit behind the current local packet-owned self-destruct fallback when no packet target evidence exists, remote packet-owned expiry fallout that should remain packet/runtime-owned, and any additional `SendSkillCancelRequest` consumers not yet surfaced after the prepared-skill, swallow-reset, vehicle-valid, keydown-cleanup, and dragon one-time-action cancel-side effects. | `ClientSkillCancelResolver`, `SkillManager`, `SummonedPool`, `PlayerManager`, `UnitTest_MapSimulator/ClientSkillTimerCancelParityTests.cs` (`CUserLocal::UpdateClientTimer`, `CUserLocal::SendSkillCancelRequest`, `CSummonedPool::OnCreated`, `CSummoned::Update`, `CSummoned::TryDoingAttack`, `Skill/3511.img/skill/35111011/info`, `Skill/3511.img/skill/35111011/summon`, `Skill/3511.img/skill/35111011/hit`, `Skill/3212.img/skill/32120000/info/type`, `Skill/3212.img/skill/32120000/info/affectedSkill`, `Skill/3211.img/skill/32110000/info/type`, `Skill/3211.img/skill/32110000/info/affectedSkill`, `Skill/3212.img/skill/32120001/info/type`, `Skill/3212.img/skill/32120001/info/affectedSkill`, `Skill/3211.img/skill/32110007/info/type`, `Skill/3211.img/skill/32110007/info/affectedSkill`, `Skill/3510.img/skill/35100008/info/dummyOf`, `Skill/3510.img/skill/35101009/info/affectedSkill`, `Skill/2112.img/skill/21120011/info/affectedSkill`) |
| Partial | Skill restrictions by map/state | The simulator still keeps the shared restriction seam already landed for this row: parsed map `fieldLimit` bits for `Unable_To_Use_Skill`, `Move_Skill_Only`, `Unable_To_Use_Mystic_Door`, the Mechanic-only `Unable_To_Use_Rocket_Boost` branch, and the mount / broader vehicle `Unable_To_Use_Taming_Mob` branch; the field-authored `info/moveLimit` movement-skill counter seam in `FieldRuleRuntime`; live `MapInfo` ownership for Coconut, Snowball, and Guild Boss basic-action owners; WZ-authored root `noSkill` and `info/noCancelSkill`; Mu Lung Dojo `mobMassacre/disableSkill`; the authored massacre `countEffect/*/skillUse` counter seam; shared mob-disease skill lockouts including `StopMotion` (`136`); ladder/rope vetoes for swallow plus the broader `DoActiveSkill_ShootAttack` family; and scripted direction-mode ownership that tears down prepared or upkeep-driven skills once local control is taken away. WZ re-check still shows map-authored `noSkill/skill`, `noSkill/class`, `info/noCancelSkill`, and `info/moveLimit` surfaces on the representative maps already tied to this row. The Coconut representative map still publishes `Map/Map1/109080000.img/info/fieldType = 4` plus the authored `coconut` contract, the Snowball representative map still publishes `Map/Map1/109060000.img/info/fieldType = 1`, and the Guild Boss representative map still publishes `Map/Map9/990000900.img/info/fieldType = 8` with authored `healer` and `pulley` contracts (`healer/x`, `yMin`, `yMax`, `rise`, `fall`, `healMin`, `healMax`, `healer`; `pulley/x`, `y`, `pulley`). Direct IDA re-check of the `CField::IsUnableToUseSkill` callers still confirms the client does not blanket-reject every skill under that field-limit bit. `CUserLocal::TryDoingMeleeAttack` (`0x91e780`), `TryDoingShootAttack` (`0x925a00`), and `TryDoingMagicAttack` (`0x92a240`) each guard `CField::IsUnableToUseSkill` with explicit recovered skill-id tables only, so the shared `FieldSkillRestrictionEvaluator` still blocks just that recovered client set (`4211002`, `4221001`, `1121006`, `1221007`, `1321003`, `4321001`, `4121008`, `5101002`, `5101004`, `5121005`, `21100002`, `21110003`, `21110006`, `4311003`, `4331000`, `4331004`, `4331005`, `4341002`, `33111002`, `35001003`, `1121001`, `1321001`, `3120010`, `3121003`, `3221003`, `5201006`, `32101001`, `32111011`) instead of flattening the whole bit into a simulator-wide "most skills" ban. This pass keeps that same shared evaluator and tightens the local dragon-owner branch without widening cast-path-specific checks: the simulator still no longer treats Thunder Breaker `Dash` (`15101003`) as client-forbidden under `Unable_To_Use_Skill`, still no longer treats every active Snowball round as if `CField_SnowBall::BasicActionAttack` had already taken local ownership, still no longer treats every active Coconut round as if `CField_Coconut::BasicActionAttack` had already taken local ownership, still no longer treats merely standing in the Guild Boss pulley area, a live pulley state, or a pending pulley preview sequence as if `CField_GuildBoss::BasicActionAttack` had already taken local ownership, and now also no longer relies only on the map-authored no-dragon wrapper to reject Evan dragon-root magic. Direct IDA re-check of `CField_Coconut::BasicActionAttack` (`0x54a5e0`) still shows the field building coconut object rects, calling `CUserLocal::TryDoingNormalAttack`, and only then sending the coconut hit packet with the selected target and `tDelay`, so the simulator still reports `CoconutBasicActionOwned` only while a fresh local normal-attack request is pending in `CoconutField`, with a bounded request/delay window so unacknowledged local preview requests do not poison later skill casts. The local Snowball runtime already exposed the closer client-shaped ownership signals through the touch-request / touch-impact seam used by `OnSnowBallTouch`, so `MapSimulator.GetCurrentFieldSkillRestrictionRuntimeState` still only reports `SnowBallBasicActionOwned` while the local player is actually inside that touch-owned basic-action window rather than for the entire active round. Direct IDA re-check of `CField_GuildBoss::BasicActionAttack` (`0x5517d0`) still anchors the Guild Boss fix the same way: the client first routes the local hit through `CUserLocal::TryDoingNormalAttack`, calls `CPulley::Hit`, and only then sends packet `259` when the pulley state is idle, so the simulator still mirrors that ownership shape by reporting `GuildBossBasicActionOwned` only during a short fresh local pulley-hit ownership window in `GuildBossField` instead of while the broader pulley area or field state remains active. Direct IDA re-check of `CUserLocal::TryDoingMagicAttack` at `0x92a240` still also shows a hard reject while `CAvatar::IsOnPlayingOneTimeAction` is active, and the simulator already exposes the matching local state through `PlayerCharacter.IsPlayingClientOwnedOneTimeAction`, so the shared `PlayerSkillStateRestrictionEvaluator` continues to reject active skill requests during that client-owned one-time-action window while the existing prepared/upkeep teardown paths consume the same message if that transient state begins after cast start. The no-dragon branch stays on that same shared evaluator, but the runtime-state seam is now closer to the client owner the IDA callsite actually checks: the existing map ownership seam still exposes `fieldType = FIELDTYPE_NODRAGON` / `MapInfo.vanishDragon` as the simulator's no-dragon presentation seam, WZ Evan dragon magic rows such as `Skill/2212.img/skill/22121000/info` and `Skill/2215.img/skill/22151001/info` still publish magic attack data, and direct IDA re-check of `CUserLocal::TryDoingMagicAttack` (`0x92a240`) still shows the client rejecting dragon-root skills (`get_skill_root_from_skill(nSkillID) / 100 == 22`) when the local `CDragon` actor is absent. `MapSimulator.GetCurrentFieldSkillRestrictionRuntimeState` now carries that live local ownership signal through `DragonCompanionRuntime`, so the shared evaluator rejects Evan dragon-root magic both on no-dragon maps and in other local states where the simulator's dragon actor is intentionally absent (for example when the current presentation seam suppresses the dragon owner) instead of keying only off the map wrapper. Focused on-disk regression coverage for this row is now in `UnitTest_MapSimulator/PlayerSkillStateRestrictionParityTests.cs` for local-dragon-present allow, local-dragon-absent reject, and the no-dragon fallback path alongside the earlier Coconut, Snowball, `Unable_To_Use_Skill`, `noSkill`, `moveLimit`, one-time-action, and No-Dragon coverage already tracked for this row. The remaining gap is narrower but still real: deeper `CField` availability checks and remaining field-owned or client-only forbidden-skill branches outside the now-modeled recovered `Unable_To_Use_Skill` table, any still-unrecovered Coconut or Snowball ownership beyond the now-modeled local normal-attack / touch-owner seams, root `noSkill`, Dojo massacre restrictions, mob-disease lockouts, scripted control, ladder/rope gates, the one-time-action state seam, and any additional client-only dragon-owner loss cases beyond the simulator's current live dragon runtime are still incomplete. | Maps and transient local states now block skill use closer to the client from one shared place instead of scattered path-specific checks: field-authored skill bans still flow through the live map seam, field-authored movement-skill quotas now stay pinned to the same field-rule owner that decrements successful local movement casts, prepared or sustained skills still tear down when local state, mob-disease, scripted control, or massacre counters turn invalid after cast start, active skill requests are still rejected while the client-owned one-time-action window is playing, `Unable_To_Use_Skill` maps now no longer suppress unrelated skills that the client does not reject under that bit and no longer overblock `15101003`, Snowball no longer suppresses skills for the whole active round when local basic-action ownership has not actually engaged, Coconut no longer suppresses skills for the whole active round when no fresh local normal-attack request owns the basic-action packet seam, Guild Boss no longer suppresses skills just because the player is inside the pulley rectangle or the pulley state is nonzero, and Evan dragon-root magic now follows the live local dragon-owner seam rather than only the no-dragon map wrapper. The row stays `Partial` because the remaining mismatch is now concentrated in unrecovered `CField` gate branches and any additional client-only forbidden-state or special-field owners beyond the currently modeled field limits, recovered `Unable_To_Use_Skill` skill-id set, live dragon-owner seam, `moveLimit`, the narrowed Coconut/Snowball/Guild Boss local owner seams, root `noSkill`, Dojo massacre restrictions, mob-disease lockouts, scripted control, ladder/rope gates, and the one-time-action state seam. | `MapSimulator`, `SkillManager`, `FieldSkillRestrictionEvaluator.cs`, `FieldRuleRuntime.cs`, `PlayerSkillStateRestrictionEvaluator.cs`, `PlayerSkillBlockingStatusMapper.cs`, `PlayerCharacter.cs`, `DragonCompanionRuntime.cs`, `MassacreField.cs`, `CoconutField.cs`, `SnowBallField.cs`, `GuildBossField.cs`, `MapInfo`, `MapInfo.fieldLimit`, `MapInfo.moveLimit`, `MapInfo.fieldType`, `FieldLimitType`, `FieldType`, `Skill/MobSkill.img`, `Map/Map1/109080000.img/coconut`, `Map/Map1/109060000.img/info/fieldType`, `Map/Map9/990000900.img/{info/fieldType,healer,pulley}`, `Map/Map9/980031100.img/noSkill/class`, `Map/Map9/922240000.img/noSkill/skill`, `Map/Map9/910320100.img/mobMassacre/disableSkill`, `Map/Map9/926010100.img/mobMassacre/countEffect/500/skillUse`, `Map/Map2/200110000.img/info/noCancelSkill`, `Map/info/moveLimit`, `MapInfo.vanishDragon`, `FieldType.FIELDTYPE_NODRAGON`, `Skill/2212.img/skill/22121000/info`, `Skill/2215.img/skill/22151001/info`, `UnitTest_MapSimulator/PlayerSkillStateRestrictionParityTests.cs` (`CUserLocal::Update`, `CUserLocal::IsSkillAvailable`, `CField::IsUnableToUseSkill`, `CField_Coconut::BasicActionAttack`, `CField_SnowBall::BasicActionAttack`, `CField_GuildBoss::BasicActionAttack`, `CField::IsMoveSkillOnly`, `CField::IsUnableToUseRocketBoost`, `CField::IsUnableToUseTamingMob`, `CUserLocal::DoActiveSkill_ShootAttack`, `CUserLocal::TryDoingSwallowAbsorb`, `CUserLocal::TryDoingMagicAttack`, `CAvatar::IsOnPlayingOneTimeAction`, `CDragon`, `get_skill_root_from_skill`) |

### 2. Summoned action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Summoned action-loader / frame-build parity | The client-owned summon loader seam remains in `SkillLoader`, with the already-landed loader/frame-build parity still intact: summon frame building uses the client `120` ms delay fallback, per-frame `z`, `a0` / `a1`, authored `origin` / `lt` / `rb` metadata when wrapper-linked canvases publish it, reverse-playback duplication from the client-consumed reverse-style flag, standalone summon-owner root acceptance for real roots such as `Skill/3510.img/skill/35101005`, and wrapper pruning so root `icon*`, `action`, and root `effect/effect0` nodes do not pollute the playable door branch family. WZ was rechecked first in the mounted export for the same concrete layouts: `Skill/3510.img/skill/35101005` still exposes wrapper nodes beside `cDoor`, `sDoor`, `oDoor`, `mDoor`, and `eDoor`; `Skill/501.img/skill/5010005/summon` still exposes authored custom action branches `{piratebless,rushBoom,cannonSlam,noiseWave,jump,hit1}` beside `attack1`-`attack4`; `Skill/434.img/skill/4341006/summon` still remains an effect/repeat-only layout with a visible prerequisite link at `info/requireSkill = 4331002`; `Skill/3512.img/skill/35121012/info/requireSkill = 35121005` still shows that visible prerequisite-style skill linking exists on the same mounted skill data surface; and the satellite summon links at `Skill/3511.img/skill/35111009/info` and `Skill/3511.img/skill/35111010/info` still expose `affectedSkill = 35111001` as the visible WZ-side link rather than proving the hidden client `sSummonedUOL` authoring site. Client evidence stayed second: `CActionMan::GetSummonedProp` at `0x41b830` still resolves the precomputed `SKILLENTRY::GetSummonedUOL` value through the resource manager, `SKILLENTRY::GetSummonedUOL` at `0x417f30` still only returns `this->sSummonedUOL`, `CActionMan::LoadSummonedAction` at `0x423100` still indexes the recovered `s_sSummonedAction` table before building/caching frames, `CSummoned::PrepareActionLayer` at `0x749cd0` still retries only action `2` for move abilities `4/5` or action `0` otherwise before throwing when that retry list is empty, and the same getter now also rechecked as the asset base for `CSummoned::_LoadEffect` at `0x754050` plus `CSummoned::_UpdateEffect` at `0x754970` when the client resolves summon-owned `/effect`, `/repeat`, `/effect0`, and `/repeat0` layers from that precomputed UOL. This pass tightens the loader-side visible-link fallback without widening runtime ownership: alongside the earlier empty-action retry/runtime fixes, `SkillLoader.EnumerateSummonSourceCandidateSkillIds` now keeps visible `requireSkill` / `req/*` links in the summon-source candidate chain after direct `affectedSkill` and dummy-parent links, and `TryResolveSummonSourceProperty` now also retries through reverse visible `affectedSkill` parents before giving up when the current skill has no direct summon owner node. Focused source coverage is now present in `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs` for prerequisite-link candidate ordering, nested prerequisite traversal without duplicate skill ids, and mounted-style client-UOL path normalization. The remaining client-owned gap is now narrower and still concentrated on the hidden asset-resolution field itself: the simulator can only approximate `SKILLENTRY::sSummonedUOL` from visible WZ link surfaces (`affectedSkill`, reverse affected-parent discovery, dummy parents, and requirements), while the client consumes the precomputed `sSummonedUOL` string directly; the exact authoring/population path for that field still needs a follow-up client-data pass before this row can be marked implemented. | Without this row, future summon work is biased toward packet/runtime fixes while leaving the client's actual frame-build owner implicit. Tracking the loader explicitly keeps summon parity anchored to the real asset-selection and frame-construction seam, and the stricter empty-action retry plus broader visible-link candidate graph now leave the remaining risk concentrated on the hidden summoned-UOL authoring path instead of on frame construction, action-table lookup, authored custom summon-action recovery, standalone-root pruning, or the previously narrower WZ-side source fallback chain. | summon action-loader layer (`SkillLoader`, `SkillData`, `SummonRuntimeRules`, `CActionMan::GetSummonedProp`, `CActionMan::LoadSummonedAction`, `SKILLENTRY::GetSummonedUOL`, `CSummoned::PrepareActionLayer`, `CSummoned::_LoadEffect`, `CSummoned::_UpdateEffect`, `CSummoned::Update`, `CSummonedPool::OnCreated`, `Skill/3510.img/skill/35101005`, `Skill/3511.img/skill/35111002/summon/attackTriangle`, `Skill/3511.img/skill/35111011/summon/stand`, `Skill/3511.img/skill/35111011/summon/heal`, `Skill/3511.img/skill/35111009/info/affectedSkill`, `Skill/3511.img/skill/35111010/info/affectedSkill`, `Skill/3512.img/skill/35121012/info/requireSkill`, `Skill/3310.img/skill/33101005/repeat`, `Skill/434.img/skill/4341006/summon/repeat`, `Skill/434.img/skill/4341006/info/requireSkill`, `Skill/3511.img/skill/35111001/summon/die1`, `Skill/501.img/skill/5010005/summon`, `Skill/1311.img/skill/13111004/summon/hit`, `UnitTest_MapSimulator/SummonedActionLoaderParityTests.cs`) |

### 3. Shadow-partner and mirror helper parity discovered by targeted IDA re-check

This helper-owner seam is currently under-tracked by the backlog even though both the client and simulator already expose concrete ownership here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow-partner and mirror helper actors | The simulator now models the client ownership split instead of treating every clone-family helper as the same WZ-backed shadow-partner path. `SkillData` / `SkillManager` recognize `Mirror Image` (`4331002`) as a distinct helper owner, `PlayerCharacter` keeps a separate mirror runtime from the existing `special/*` shadow-partner actor, and the update/render loop now drives mirror helper visibility from player action changes with a dedicated `m_tStartMirror`-style start timestamp, a 200 ms client-shaped rel-move window, and client-backed suppression for ladder, rope, morph, taming-mob, hit, and dead states. WZ re-check still confirms `skill/433.img/skill/4331002` exposes `action/0 = alert` plus buff/effect data but no `special/*` helper action branch, so Shadow Partner keeps using WZ-authored helper animations while Mirror Image continues to source the assembled avatar instead. This pass tightens the mirror source seam another step toward the targeted `CUser::PrepareMirrorActionLayer` / `CUser::GetMirrorSrcLayer` re-check without leaving the current simulator ownership model: `CharacterAssembler` still classifies every assembled avatar part into explicit `UnderCharacter`, `OverCharacter`, `UnderFace`, `Face`, and `OverFace` render planes, `PlayerCharacter` still keeps a dedicated mirror-owned prepared five-layer cache, and the earlier live-plane reuse refinement remains in place so no-op per-plane reuse no longer restamps the recreate time or silently re-arms the 200 ms rel-move. WZ was rechecked first again for `skill/433.img/skill/4331002`: `action/0 = alert` remains present, `effect` remains present, and `special` remains absent. IDA for `CUser::GetMirrorSrcLayer` still confirms the client mirror sources are the same five avatar layer handles in index order, while `CUser::PrepareMirrorActionLayer` still recreates each helper layer with `m_pLayerUnderFace` as the overlay parent regardless of which source plane fed that mirror layer, copies origin/position from the live source-layer handle, seeds `RelMove` from the live source-layer current time plus 200 ms, and only recreates the helper layer when the layer is missing or its flip diverges from `m_pLayerUnderFace`. `CUser::Update` then keeps the existing helper layer and inserts the current source-layer canvas into it. `PlayerCharacter` now reflects more of that seam by routing every prepared mirror plane through a shared UnderFace overlay target, drawing the whole mirror helper package at the UnderFace insertion boundary, keeping mirror rendering on the prepared per-plane composed-snapshot path even when the current live source plane still matches, preserving the prepared layer recreate timestamp across same-action/same-facing source canvas refreshes, and recording the source-layer current-time snapshot separately from the recreate timestamp. The existing gates remain in place: flip-only changes still force prepared-layer recreation, mirror suppression/back-offset selection still consumes the client raw-action buckets (`is_back_action`, `is_alert_back_action`, raw action `48`) before falling back to action-name heuristics, the mirror-wide `m_tStartMirror` analogue still only re-arms when the helper becomes visible or the observed player action actually changes, and per-plane render placement still stays on the prepared snapshot bounds/origin until the simulator recreates that prepared mirror plane instead of silently rebasing each helper plane to the current live avatar bounds every draw. Focused regression coverage is now on disk in `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` for same-action canvas refresh timing, layer transition start precedence, captured-origin placement, prepared-snapshot position precedence, and the shared mirror placement seam alongside the earlier shared-UnderFace insertion and prepared-snapshot placement coverage. The remaining gap is narrower and still concentrated at the true live-layer-object boundary inside `CUser::PrepareMirrorActionLayer` / `CUser::Update`: the simulator still does not own shared `IWzGr2DLayer` mirror objects, true COM live-layer origin/current-time handles, true `InsertCanvas` reparenting against the live client layer graph, or the follow-up clone-helper behaviors that depend on those real client layer objects rather than simulator-owned prepared plane snapshots. | Clone-family jobs are not just tray icons or generic buff effects in the client; they own dedicated avatar-adjacent helper layers that participate in action preparation and per-frame user update. Tracking that seam explicitly keeps future parity work focused on helper-layer timing, ownership, and clone-family differentiation instead of accidentally treating shadow-partner and mirror-image visuals as solved just because the simulator already renders one WZ-backed helper actor | `PlayerCharacter.cs`, `CharacterAssembler.cs`, `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerManager.cs`, `UnitTest_MapSimulator/MirrorImageLayerParityTests.cs` (`skill/433.img/skill/4331002/action/0`, `CUser::PrepareActionLayer`, `CUser::PrepareShadowPartnerActionLayer`, `CUser::PrepareMirrorActionLayer`, `CUser::GetMirrorSrcLayer`, `CUser::Update`, `is_back_action`, `is_alert_back_action`) |

### 4. Shadow-partner action-loader ownership discovered by targeted `CActionMan` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shadow Partner action-loader / frame-build parity | The shadow-partner helper row is no longer only a runtime/action-playback note. IDA-backed loader ownership remains explicit here: `CActionMan::GetShadowPartnerProp` at `0x41ba60` resolves the helper property for the owning skill id through `SKILLENTRY::GetSpecialUOL` plus the resource-manager fetch, and the direct `CActionMan::LoadShadowPartnerAction` re-check at `0x423cf0` still shows cache lookup by `(skillId, action)`, named raw-action branch precedence, numeric `get_action_name_from_code` fallback, hidden action-table piecing before plain raw-action fallback when `ACTIONDATA::bPieced` is populated, cached frame construction from enumerated helper canvases, frame-owned canvas-property metadata reads, and mirrored interior-frame replay when the action data replay flag is set. The simulator still keeps that ownership in the existing loader seam without widening runtime state: `ShadowPartnerClientActionResolver` still preserves animation-level `PositionCode` metadata when it builds remapped helper playback rows, `SkillLoader` still preserves that same frame-build metadata when it synthesizes loader-owned helper rows, `SkillLoader.EnumerateShadowPartnerActionFrameChildrenInClientOrder` still keeps authored helper-canvas child order while accepting linked-canvas cases after the client-shaped `IWzCanvas` filter, and the loader still builds named-vs-numeric storage plans so numeric helper keys store the client action name while authored named branches win. WZ was rechecked first again in the mounted export for this row: `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, and `Skill/1411.img/skill/14111000/special/*` still publish the same helper branch families, while the recovered non-ghost `action/0` raw-action names still resolve from the mounted skill data (`alert2` / `alert3` / `alert4` / `alert5`, `swingO1`, `avenger`, `assaulter`, `prone2`, `savage`, `showdown`, `assassination`, `smokeshell`, `ninjastorm`, `vampire`). This pass tightened the hidden-action seam instead of widening runtime state: the earlier synthesized pieced helper rows still win candidate and identity ordering ahead of generic helper aliases, so recovered rows such as `avenger` are not hidden behind `shoot1` / `shoot2` while the client would serve the hidden action-table row first; `prone2` remains represented as a loader-owned pieced row using the recovered `Skill/421.img/skill/4211006/action/0 = prone2` surface plus the `LoadShadowPartnerAction` source-frame-slot shape; and the loader now also synthesizes the confirmed non-piece alias rows directly into the helper animation table instead of leaving them only in playback fallback. Concretely, the mounted `action/0` surface for `alert2` / `alert3` / `alert4` / `alert5` and `smokeshell` now lands on loader-owned helper rows with the client-shaped authored-branch fallback (`alert* -> alert`, `smokeshell -> alert`) while preserving frame-owned metadata and authored branches with the same name, and the same remap seam also covers the direct client raw-action-code back aliases `ladder2 -> ladder` and `rope2 -> rope`. Focused regression coverage on disk under `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs` now covers indexed alert-row synthesis, `smokeshell` alias synthesis, and authored-branch preservation for the loader-owned remap path. The earlier implemented loader behavior remains in place for linked-`special` source resolution, metadata-owned frame bounds, replay-tail handling for the currently confirmed replay-table actions `stand1`, `stand2`, and `alert`, and synthesized pieced helper rows for `avenger`, `assaulter`, `flyingAssaulter`, `prone2`, `savage`, `showdown`, `assassination`, `assassinationS`, `assassinations`, `ninjastorm`, `vampire`, `stabD1`, `swingD1`, `swingD2`, `doubleSwing`, `tripleSwing`, and `shotC1` while preserving authored branches with the same name. Remaining gap is now concentrated at the client-only `ACTIONDATA::bPieced` payloads and any still-unrecovered hidden rows beyond the confirmed surface above: additional action-specific piece lists beyond the currently recovered `action/0` plus raw-action-code alias surface, exact source-frame slot indexes for the other hidden piece lists beyond the newly represented `prone2` source-slot shape, and whether later client versions or other data sets introduce helper UOLs that do not land on this build's linked-`special` seam. The direct loader re-check still narrows one prior uncertainty: this build's `LoadShadowPartnerAction` does not show a separate total-delay shaping path for Shadow Partner helper rows; replay-tail ownership remains tied to the action-data replay flag already represented by the confirmed replay-tail set. | Without this row, the backlog overstates clone-helper parity by treating Shadow Partner as if it were only a `CUser`/`PlayerCharacter` timing and placement problem. The client also keeps a dedicated `CActionMan` loader for helper actions, so future work needs a named seam for cache keys, branch lookup, numeric-slot fallback ownership, hidden action-table piecing, frame construction, metadata retention, and replay-tail ownership instead of letting those questions stay buried inside the helper runtime row. | shadow-partner action-loader layer (`CActionMan::GetShadowPartnerProp`, `CActionMan::LoadShadowPartnerAction`, `get_action_name_from_code`, `ACTIONDATA::bPieced`, simulator seams: `ShadowPartnerClientActionResolver.cs`, `SkillLoader.cs`, `SkillManager.cs`, `PlayerCharacter.cs`, `RemoteUserActorPool.cs`, `UnitTest_MapSimulator/ShadowPartnerActionLoaderParityTests.cs`, `Skill/411.img/skill/4111002/special/*`, `Skill/421.img/skill/4211008/special/*`, `Skill/1411.img/skill/14111000/special/*`, `Skill/411.img/skill/{4111001,4111003,4111005,4111007}/action/0`, `Skill/412.img/skill/{4121000,4121003,4121004,4121008}/action/0`, `Skill/420.img/skill/{4201002,4201003,4201005}/action/0`, `Skill/421.img/skill/{4211002,4211003,4211005,4211006,4211007}/action/0`, `Skill/422.img/skill/{4221001,4221003,4221006,4221007}/action/0`, `Skill/1410.img/skill/14101006/action/0`, `Skill/1411.img/skill/{14111001,14111002}/action/0`) |

### 5. Skill-specific local packet application parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Time Bomb, Vengeance, and ExJablin packet-application parity | The simulator still owns this `CUserLocal::OnPacket` family inside the existing local-utility packet seam, and this pass narrows the remaining Time Bomb alias side of the row without widening packet ownership. WZ was rechecked first against the mounted export: `String/Skill.img` still resolves `4341003` as `Monster Bomb` with the `explosion occurs 3 seconds after the charm is activated` description fragment, `31101003` as `Vengeance`, and `4120010` as `Expert Throwing Star Handling` with the `the next attack will always be a Critical Attack` fragment, while `3120010` still resolves to `Ultimate Inferno` instead of a WZ-side `Vengeance` alias. `Skill/434.img/skill/4341003` still carries the authored Time Bomb action / prepare / `special` branches, and `Item/ItemOption.img` still exposes the client-shaped Time Bomb invincibility option family `020366`, `030366`, `040366`, `030371`, and `040371` under option type `52`, with templates and level values that still match the fallback definitions used by the Time Bomb hit-period merge. Client evidence was rechecked second: `CUserLocal::OnTimeBombAttack` still decodes `(skillId, timeBombX, timeBombY, impactPercent, damage)`, routes the packet origin into `TryDoingMeleeAttack`, restores the `1500 ms` base hit period before coat-option merges, gates impact on nonzero `nUserImpactDeg`, calls `CUser::MakeIncDecHPEffect(this, -nDamage, 0)`, and then sets the hit emotion/impact; `CUserLocal::OnVengeanceSkillApply` still decodes exactly one `Int32` and only branches on legacy packet skill id `3120010`; `CUserLocal::OnExJablinApply` still reads no payload and only arms `m_bNextShootExJablin = 1`. The existing packet shapes and local skill branches remain in place: packet ids `268` / `271` / `272` plus aliases, exact Time Bomb/Vengeance/ExJablin payload validation, signed Time Bomb damage, legacy-only Vengeance admission, nonzero Time Bomb impact gating, packet-origin target selection, the dedicated `SkillManager.TryApplyPacketOwnedTimeBombAttack` branch, the WZ-backed visible-potential item-option merge for `CharacterPart.ItemOptionIds`, and the narrow MakeIncDecHPEffect miss/heal mirroring are still owned by the same local utility seam. This pass adds a dedicated Time Bomb skill-resolution fallback in that seam instead of inventing a new packet owner: when opcode `268` arrives with an unconfirmed or stale packet skill id that is neither learned nor already in the confirmed Time Bomb alias catalog, `MapSimulator.PacketOwnedUtilityParity` now falls back through the preferred Time Bomb family candidates for that packet family, preferring a learned alias and otherwise the WZ-confirmed `4341003` `Monster Bomb` node before driving the existing local melee-attack and `special` effect paths. Focused regression coverage in `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs` now covers the dedicated-family fallback helper branches for learned packet ids, learned preferred aliases, unusable candidates, preferred-current fallback, and the no-candidate raw-id case. The remaining parity gap is narrower but still real: the simulator now bridges unknown Time Bomb packet ids to the sole WZ-confirmed Time Bomb family candidate inside the existing seam, but the exact legacy numeric packet-to-WZ Time Bomb mapping is still unconfirmed by direct client evidence, so that final fallback remains an inference from the dedicated opcode plus the unique WZ `Monster Bomb` match rather than a client-confirmed alias id. | These handlers matter because they bypass the normal local input/cast path even though the client gives them their own dispatcher and follow-up state. The simulator keeps a concrete owner for those packets, keeps the shared alias seam anchored to WZ first and client second, no longer accepts trailing bytes that the client packet readers would leave unread, no longer accepts non-client Vengeance packet ids, no longer treats only positive Time Bomb impact values as hit reactions, no longer clamps away signed Time Bomb damage payloads before the local reaction branch, no longer drops the zero/negative `MakeIncDecHPEffect` cases that hang off the same decoded damage field, still resolves visible potential lines through the WZ-authored `ItemOption.img` template/level seam instead of limiting `CharacterPart.ItemOptionIds` population to the confirmed Time Bomb invincibility family, and now also keeps dedicated Time Bomb packets usable when the raw packet skill id is stale or still-legacy but the only WZ-confirmed family candidate is known. The row stays `Partial` because the last uncertainty is now concentrated on the exact legacy Time Bomb alias number rather than on missing packet-family ownership, missing decode, missing nonzero impact behavior, missing signed-damage consumption, missing target-priority coverage, missing `ItemOption.img` fallback coverage, missing general visible-potential option-id population, or missing a best-effort bridge from an unknown Time Bomb packet id into the confirmed `Monster Bomb` family seam. | packet-owned local skill-application layer (`CUserLocal::OnPacket`, `CUserLocal::OnTimeBombAttack`, `CUserLocal::OnVengeanceSkillApply`, `CUserLocal::OnExJablinApply`, `CUserLocal::TryDoingMeleeAttack`, `CUserLocal::DoActiveSkill_MeleeAttack`, `CUserLocal::ApplyInvicibleOption`, `CUser::MakeIncDecHPEffect`, `CUserLocal::SetImpact`, `MapSimulator.PacketOwnedUtilityParity`, `LocalUtilityPacketInboxManager`, `SkillManager`, `PlayerCharacter`, `CharacterPart`, `ItemUpgradeUI`, `Character/00002000.img`, `String/Skill.img`, `Skill/434.img/skill/4341003`, `Item/ItemOption.img`, `UnitTest_MapSimulator/PacketOwnedSkillApplicationParityTests.cs`) |

### 6. Packet-owned combo counter and combo-command parity discovered by direct `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Combo counter / combo-command packet parity | `MapSimulator.PacketOwnedComboParity` now owns the simulator-side `OnIncComboResponse` bridge instead of leaving combo implied by the generic temporary-stat tray. The simulator decodes the packet-owned combo count from the combo inbox/chat transport, stores the owner count/timestamp, resolves combo tiers at `30`, `100`, and `200`, loads digit and banner layers from `Effect/BasicEff.img/Combo/<level>/*`, keeps them on the same five-second alpha window, applies the client-matching fly-origin amplitude split across the least-significant digit run, and conditionally spawns the Aran combo-command attack/buff overlays with `Effect/BasicEff.img/ComboCommand/<level>/{0,1}` first and WZ-backed named fallbacks for exports that only ship `ComboSmash`, `ComboDrain`, `ComboFenrir`, `ComboTempest`, and `ComboBarrier`. The row no longer depends on the generic `Combo` temporary-stat heuristics in `SkillManager`; it has a dedicated packet-owned HUD/effect owner, content load path, update lifetime, and draw path in `HaCreator/MapSimulator`. | If combo stays implied only by the temporary-stat tray, the backlog will overstate skill parity while missing the client's actual packet-owned combo counter, visible combo HUD/effect lifetime, and the extra combo-command visuals that hang off learned Aran skills. Tracking it explicitly keeps future work anchored to the real packet and draw owners instead of assuming the generic buff/cooldown surfaces already cover combo gameplay feedback. | packet-owned combo HUD/effect layer (`CUserLocal::OnPacket`, `CUserLocal::OnIncComboResponse`, `CUserLocal::DrawCombo`, `Effect/BasicEff.img/Combo`, `Effect/BasicEff.img/ComboCommand`) |

### 7. Shared skill-use animation-owner parity discovered by targeted `CAnimationDisplayer` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shared skill-use animation-owner parity | `MapSimulator.HandlePlayerSkillCast` no longer stands alone as the only local ingress for this shared owner. The same animation-displayer seam still absorbs the client-facing prepared-skill route that the xref set exposed around `CUser::ShowSkillPrepare`: local prepared starts synthesize shared-owner requests from `prepare` or immediate `keydown` admission, local prepared release feeds `keydownend` back through the same branch loader, remote prepared-start packets honor their explicit branch names instead of falling back to generic `effect*` discovery, and remote prepared-clear packets emit the matching `keydownend` owner effect before the prepared state is dropped. The seam still tracks a live owner position/facing relationship, applies the client `1000 * (speed + 10) / 16` delay-rate override for the remote melee presentation route, preserves structured `LoadLayer`-style branches as avatar-bound transient animations, and carries caller-owned origin offsets plus fixed-vs-owner-followed facing for the one-time fallback and transient avatar branch loaders. This run narrows the explicit-caller gap again without adding another parallel branch path. WZ was rechecked first: `Skill/434.img/skill/4341003/special` and `Skill/434.img/skill/4341007/special` both still exist in the mounted export, which keeps the missing `ShowSkillSpecialEffect` surface anchored to authored `special` branches rather than inferred effect aliases. Client evidence stayed second: `CUser::ShowSkillSpecialEffect` still creates either a grenade-position vector or an explicit `(nTimeBombX,nTimeBombY)` vector, passes `m_pLayerUnderFace` as overlay, uses the caller delay rate, keeps `z = 3`, uses `nLast = 0x7fffffff`, and leaves auto-flip off before calling `CAnimationDisplayer::Effect_SkillUse`; the wider xref set into `CUser::ShowSkillEffect` / `CUser::ShowSkillSpecialEffect` still keeps explicit non-melee callers such as `TryDoingShootAttack`, `TryDoingMagicAttack`, `DoActiveSkill_Summon`, `DoActiveSkill_SmokeShell`, `DoActiveSkill_MobCapture`, `DoActiveSkill_Heal`, `DoActiveSkill_TownPortal`, `DoActiveSkill_OpenGate`, `DoActiveSkill_Flying`, `DoActiveSkill_BoundJump`, `DoActiveSkill_WeaponBooster`, `DoActiveSkill_Aura`, `TryRegisterTeleport`, `TryDoingSmoothingMovingShootAttackPrepare`, `OnKeyDownSkillEnd`, `HandleCtrlKeyDown`, `OnMovingShootAttackPrepare`, and `OnAttack` on the client side. The simulator now mirrors more of that ownership split in the current seam: generic local client-effect callbacks no longer carry only `(effectSkillId, sourceSkillId)`; they now feed a shared `SkillUseEffectRequest` object that can preserve explicit branch names, caller-owned world origins, and fixed-vs-followed owner position/facing into the same branch loader used by casts and prepared skills. The packet-owned local Time Bomb route still uses that recovered request surface, and the local attack-payload owner now also reuses it whenever runtime code already has an explicit caller-supplied origin or preferred target point for a skill whose WZ node authors `special`, instead of silently falling back to owner-origin `effect*` discovery. Remote grenade packets now feed the same shared presentation seam too: `RemoteUserActorPool.TryApplyThrowGrenade` raises a shared-owner request for `special` at the packet `(x,y)` with owner-follow disabled and a fixed delay-rate, so the previously missing `4341007/special` grenade-vector path no longer waits for generic owner-origin fallback. WZ-backed local buff admission still routes `affected` and `affected0` through this owner, existing timeout/recovery client-effect requests still feed their requested skill id back through the shared branch loader, branches with authored layer roots still use `PlayerCharacter.ApplyTransientSkillAvatarEffect` for the local owner and matching transient state on `RemoteUserActorPool` for remote owners, and simple non-layered branches still fall back to one-time DX registration with the requested owner relationship. The remaining gap is narrower and still real: the simulator still does not route every explicit non-melee `CUser` caller from the client xref set into this surface yet, especially local `ShowSkillEffect` / `ShowSkillSpecialEffect` callers that do not currently expose their authored branch choice or explicit vector through the local runtime, and it still does not mirror the full overlay-parent chaining and auto-flip ownership that client `LoadLayer` / `RegisterOneTimeAnimation` can apply for those callers beyond the current fixed-origin / owner-follow / facing-follow options. | Naming and partially implementing this owner keeps cast-effect parity from being overstated once gameplay logic works: the client still composes many visible cast layers through one shared animation manager, and this pass widens the simulator seam from cast-only, remote-melee-only, prepared start/end, buff-start, and generic client-effect coverage into a shared local/remote request surface that can carry explicit branch/world-origin ownership for packet-owned, grenade-vector, or future non-melee callers. The row stays partial because the simulator still lacks several recovered caller sites and still approximates some `LoadLayer` / `RegisterOneTimeAnimation` ownership knobs, but the remaining risk is now concentrated on missing caller routing and the deeper overlay/auto-flip options rather than on the absence of a shared request shape for explicit branch names and caller-owned world origins. | shared skill-use animation layer (`CAnimationDisplayer::Effect_SkillUse`, `CAnimationDisplayer::LoadLayer`, `CAnimationDisplayer::RegisterOneTimeAnimation`, `CUser::ShowSkillEffect`, `CUser::ShowSkillSpecialEffect`, `SkillUseEffectRequest`, `MapSimulator.AnimationDisplayerParity`, `RemoteUserActorPool.TryApplyThrowGrenade`, `Skill/434.img/skill/4341003/special`, `Skill/434.img/skill/4341007/special`) |

Buff icon plan:

1. Confirm asset sources and ordering:
   Use `UI/BuffIcon.img` for temporary-stat icon chrome and `Skill/*/icon` for per-skill icon sourcing where the client uses the skill's own icon, then validate row ordering and refresh cadence against `CUIStatusBar::Draw` and `CUserLocal::Update`.
2. Add a runtime-to-UI snapshot:
   Expose a small status-bar model from `SkillManager` that includes `skillId`, applied stat family, start time, duration, and refresh/replace semantics so the UI does not need to inspect buff internals directly.
3. Extend status-bar loading and drawing:
   Teach `UILoader` and `StatusBarUI` to load buff-slot art, render the active icon row in the client-aligned status-bar region, and reserve the interaction seam for later right-click cancel behavior.
4. Verify state synchronization:
   Check short-duration buffs, long-duration buffs, same-skill refresh, natural expiry, and `/job` cleanup so icon removal stays synchronized with the runtime and does not fight quick-slot cooldown overlays.


## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, movement-coupled shoot families, and the shared `Effect_SkillUse` cast-animation owner rather than extending the generic cast path indefinitely.
3. Summon actor/runtime pass:
   Close the missing `CActionMan::LoadSummonedAction` loader seam together with the packet-owned `CSummonedPool` gaps and the `CSummoned::Update`-owned movement, touch-damage, Tesla-state, and position-sync gaps instead of only extending attack metadata and per-skill summon heuristics.
4. Clone-helper parity pass:
   Tighten the dedicated `CUser::PrepareActionLayer` / `CUser::Update` helper-layer seam for shadow-partner and mirror-image families instead of leaving clone behavior folded into generic buff or summon work.
5. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
6. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
7. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".




