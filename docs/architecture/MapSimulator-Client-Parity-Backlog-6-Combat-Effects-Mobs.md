# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CMob::DoAttack` at `0x6504d0`
- `CMob::OnNextAttack` at `0x6528a0`
- `CMob::ProcessAttack` at `0x652950`
- `CMob::GetAttackInfo` at `0x641330`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. UI parity

- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`

Notes:
`CUIStatusBar::Draw` confirms that name/job-or-level selection, HP/MP/EXP number updates, and quick-slot redraw are part of a single status-bar draw pass, while `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` shows the client also revalidates slot contents against learned skills and inventory state before redraw. `CUISkill::Draw` confirms visible skill-root selection, recommend-skill highlighting, bonus-SP rendering, skill icon row layout, and skill-level text placement.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. UI parity

This area is more advanced than older parity notes suggested, but still far from complete client parity.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Status bar layout | Positions are informed by client analysis, bitmap/WZ-backed rendering exists for gauges and text, the overlay now stays anchored to the composed `StatusBar2.img/mainBar` frame, the Big Bang loader now composes the `mainBar` left cluster and gauge cluster from the client canvas origins and WZ `z` order instead of the older stack-panel approximation so `gaugeCover` no longer ends up underneath `gaugeBackgrd`, the left status cluster now includes the client `lvCover` overlay instead of only `lvBacktrnd`, now anchors both the left cluster and gauge cluster from the composed `backgrnd` frame origin plus the resolved `lvBacktrnd` / `gaugeBackgrd` canvas origins instead of leaving the gauge text on a simulator-side base offset, the level label now uses the client `mainBar/lvNumber` digits and follows the exact `CUIStatusBar::SetStatusValue` one-digit/two-digit/three-digit X slots (`45`, `39`, `33`) instead of width-centering, the left cluster's level/job/name baseline now matches the client `SetStatusValue` Y slots more closely, the name shadow now follows the client `SetStatusValue` diagonal passes at `(74,560)`, `(76,560)`, `(74,562)`, `(76,562)` around the white `(75,561)` main draw more closely, the status bar now renders those left-side text labels from the simulator's chat/basic-font path instead of the older debug overlay font so the name/job baselines and glyph widths track the client branch more closely, now also snaps the status-bar text draws and bitmap-number placement back onto integer pixel positions so the client-derived `SetStatusValue` / `SetNumberValue` slots are no longer blurred by XNA subpixel placement, the HP/MP/EXP readouts now right-align against the exact client-derived `SetNumberValue` anchor slots (`163,4`, `332,4`, `332,20`) after the WZ-resolved `gaugeBackgrd` offset is applied, the EXP label now shows live raw EXP plus percentage instead of only a percentage stub, the job label now collapses hierarchical names to the client-facing title slot, renders in a dim-green narrower fit instead of the older generic yellow string, no longer applies the generic four-way shadow pass that `CUIStatusBar::SetStatusValue` reserves for the character name, and now also sanitizes both job and name text onto a single-line client-facing draw path and clips the left-side labels against the WZ-resolved `lvBacktrnd` / `lvCover` cluster width instead of letting long simulator-owned labels spill past the composed frame; the Big Bang name draw also now softens its black/white passes toward the client `FONT_SMALL_BLACK` / `FONT_SMALL_WHITE` pair instead of using a harsher fully opaque SpriteFont shadow. The Big Bang chat-strip branch now composes `chatSpace`, `chatSpace2`, `chatCover`, and `notice` from the client canvas origins and WZ `z` order, positions its chat-target/chat-toggle/scroll/status buttons from the same `mainBar` button origins instead of the older width-stacked approximation, aligns the target badge and `chatEnter` field from the WZ origins that the client canvases publish, now derives the chat input X start from the resolved `chatTarget/base` button width plus the `chatEnter` field origin instead of the older fixed `+70` delta, now lets the input baseline center directly from the loaded chat font plus the WZ-backed `chatEnter` field instead of preserving the last simulator-side `+3` lower bound, keeps the chat-log text origin and wrap width tied to the resolved `chatSpace2` / `chatEnter` layout instead of the older fixed `452`-pixel strip assumption, now also snaps the chat-strip target badge, recent-log rows, whisper prompt, active input text, and caret back onto integer pixel positions so the client-derived layout is not softened by XNA subpixel placement, switches between the client `chatOpen` and `chatClose` button families as chat input opens and closes instead of always reusing one art set, and continues to replay the client `ApNotify` / `SpNotify` badge animations when the simulator has distributable AP or SP. Remaining gap is now limited to the final client-owned `IWzFont` glyph raster and exact palette/alpha routing details that XNA `SpriteFont` still cannot reproduce exactly | The status bar now uses the correct WZ layering for both the main bar and the Big Bang chat strip, both major overlay clusters are anchored from the same composed `mainBar` frame the client art uses, the gauge readouts now land on the exact `SetNumberValue` slot family after applying the WZ gauge-cluster offset, the left text branch no longer uses the visibly different debug overlay font or lets long simulator-owned labels spill outside the WZ left cluster, and both the status-bar text path and the Big Bang chat-strip text/caret path no longer add avoidable subpixel drift on top of those client slots; the row is still short of full parity because the remaining visible differences are now the last client `IWzFont` glyph-shape, palette, and alpha-routing details inside `CUIStatusBar`, not missing art families or broad frame assembly | `StatusBarUI.cs`, `StatusBarChatUI.cs`, `UILoader.cs`, `MapSimulator.cs` (`CUIStatusBar::Draw`, `CUIStatusBar::SetStatusValue`, `CUIStatusBar::SetNumberValue`, `UI/StatusBar2.img/mainBar/backgrnd`, `UI/StatusBar2.img/mainBar/lvNumber`, `UI/StatusBar2.img/mainBar/lvCover`, `UI/StatusBar2.img/mainBar/gaugeBackgrd`, `UI/StatusBar2.img/mainBar/gaugeCover`, `UI/StatusBar2.img/mainBar/chatSpace`, `UI/StatusBar2.img/mainBar/chatSpace2`, `UI/StatusBar2.img/mainBar/chatCover`, `UI/StatusBar2.img/mainBar/chatEnter`, `UI/StatusBar2.img/mainBar/chatTarget`, `UI/StatusBar2.img/mainBar/chatOpen`, `UI/StatusBar2.img/mainBar/chatClose`, `UI/StatusBar2.img/mainBar/ApNotify`, `UI/StatusBar2.img/mainBar/SpNotify`) |
| Partial | Skill UI layout | Row height, positions, hit boxes, SP placement, quick-slot assignment wiring, and a client-backed `Basic.img/VScr` scrollbar at the confirmed `153,93,155` placement are now present, with both skill-window variants reusing the client `tip0..tip2` frames, the scrollbar supporting arrow clicks, track paging, thumb dragging, and wheel-over-scrollbar input, the row list drawing the client `Skill/main/recommend/0` banner from `Etc/RecommendSkill.img/<skillRootId>` SP-threshold table at the client `47,nTop-19` overlay offset plus the existing visible-tab levelable-skill fallback, the book header now following the client width-driven two-line wrap path and centering each resolved line inside the title band instead of pinning wrapped text to one left edge, the visible and inactive skill-book tabs now drawing and hit-testing from their actual `UIWindow2.img/Skill/main/Tab/*` origins and heights so the enabled `30x20 @ y=27` and disabled `30x18 @ y=29` states no longer share one flattened baseline, the `BtSpUp` control now using its normal/hover/pressed/disabled button states with a simulator-derived remaining-SP pool per tab plus loaded `req` / `reqLevel` prerequisite gates so locked skills no longer advertise live SP-up affordances or fallback recommendation highlights, the skill-book row text and SP-count draw now share the same scaled measurement path instead of the older full-font approximation, both skill-window variants now stop adding a simulator-only extra shadow pass on book-header, row, level, bonus, and SP text so those labels follow the single-pass `IWzFont::DrawTextA` client path more closely, and the skill-window loaders now also propagate the exported `tip0` / `tip1` / `tip2` canvas origins so the fixed-size hint balloons in both variants resolve from the client WZ geometry rather than the earlier generic offset boxes. Remaining layout parity is now narrower and mostly limited to the original client's exact `IWzFont` glyph raster, palette, and any still-unmodeled micro-offsets on the resizable hover balloon and skill-row text formatting path | The window is usable and materially closer to the client than the old notes implied, and the remaining gap is now constrained to the original font-object rendering details and final tooltip-offset polish rather than the earlier structural layout misses, extra simulator-side text effects, or obvious text-overlap cases | `SkillUI.cs`, `SkillUIBigBang.cs`, `SkillDataLoader.cs`, `UIWindowLoader.cs`, `MapSimulator.cs` (`CUISkill::Draw`, `CUISkill::OnCreate`, `CUISkill::OnMouseMove`, `CUISkill::GetRecommendSKill`, `Etc/RecommendSkill.img`, `UI/UIWindow2.img/Skill/main/Tab`, `UI/UIWindow2.img/Skill/main/recommend`, `UI/UIWindow2.img/Skill/main/BtSpUp`, `UI/UIWindow2.img/Skill/main/tip0`, `UI/UIWindow2.img/Skill/main/tip1`, `UI/UIWindow2.img/Skill/main/tip2`) |
| Implemented | Quick slot cooldown overlay | Cooldown masking now uses the client `UIWindow2.img/Skill/main/CoolTime` frame family, remaining-seconds countdown text renders on assigned quick-slot skills, and the stepped mask holds the initial full-overlay frame until the next visible whole-second boundary instead of advancing immediately after cast | The core client-facing quick-slot cooldown feedback is now present on the simulator HUD | `QuickSlotUI.cs` (`CUIStatusBar::CQuickSlot::Draw`) |
| Implemented | Quick-slot validation and quantity sync | The simulator now rejects passive, hidden, and unlearned skills at hotkey assignment time, restores saved quick-slot bindings through the same validation path after preset load instead of trusting raw slot data, eagerly prunes stale skill slots as soon as learned levels change, cancels an in-flight quick-slot drag if its source binding becomes invalid before the next redraw, and now mirrors the client's `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` inventory-backed recount for owned `USE` / `CASH` quick-slot item entries so stack counts redraw live and empty item slots clear as soon as their backing inventory runs out | Quick-slot contents now stay synchronized with both learned-skill state and live inventory stacks, so the HUD no longer keeps stale consumable or cash-item entries after crafting, pickup, preset restore, or manual reassignment | `QuickSlotUI.cs`, `SkillManager.cs`, `CharacterConfig.cs`, `MapSimulator.cs` (`CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo`) |
| Implemented | HP/MP warning flash | HP/MP gauge warnings now replay the client `aniHPGauge` / `aniMPGauge` and `hpFlash` / `mpFlash` assets for 500 ms when the resource drops further while already below the configured threshold, and the simulator exposes live `/hpwarn` + `/mpwarn` commands to adjust those thresholds in-session | The client-visible low-resource cue is present across both modern and pre-BigBang status-bar loaders, and the trigger point is no longer hardcoded at 20% | `StatusBarUI.cs`, `UILoader.cs`, `MapSimulator.cs` (`CUIStatusBar::Draw`, `CUIStatusBar::SetNumberValue`) |
| Partial | Chat log / whisper flows | The Big Bang status-bar chat strip now renders wrapped recent chat lines from the simulator chat history, keeps wrapped continuation rows indented like the client `ChatLogAdd` flow, now skips that continuation indent for the client `lType 7..12` branch the way `ChatLogAdd` does for system-style entries, now applies the client `ChatLogAdd` first-line width reduction for the observed whisper or special-entry `lType 14/16/19/20` branch before wrapped continuations fall back to the normal width, supports both button-driven and wheel-over-chat log scrolling, preserves manually scrolled history when new chat arrives until the log has been idle long enough to snap back to the newest line like the client `_RefreshChatLog` path, cycles the `chatTarget` badge through the client WZ labels, now applies the WZ `chatTarget/*/origin` deltas when drawing those labels so the wider alliance badge no longer reuses the base/all offset, supports `/w` + `/r` whisper targeting with an on-bar prompt plus an armed whisper mode where `/w <name>` selects the active recipient and subsequent plain Enter sends continue to that target until the target changes, keeps slash-mode target-selection commands armed on the bar instead of closing chat when they only switch channel, now lets the visible chat-log first-line sender prefix on the client `TryBeginWhisper`-eligible `lType 14/15/16/18/19/20/21/22` branch arm whisper mode directly from the strip instead of forcing slash-only target selection, now routes rendered chat-strip line colors through the simulator's client chat-log type mapping for the modeled `lType` branches instead of blindly reusing caller-provided colors, now also covers the additional `ChatLogAdd` color branch observed in IDA for `lType 11/18/19/20/21/22/23` so those rows no longer fall back to the generic white strip treatment when callers do provide the client-side `lType`, now infers omitted simulator-side incoming `lType` values from the same client-backed color/prefix matrix so yellow system notices, cyan notice-style rows, prefixed party/friend/guild/alliance/expedition lines, and explicit `[System]` / `[Notice]` / `[Error]` / `[Association]` rows stop falling back to the generic row treatment when callers only supply text plus color, now distinguishes outgoing whisper rows from incoming whisper rows so `> name:` and directed `[Whisper] a -> b:` lines enter the client `lType 14` branch while inbound `[Whisper] name:` rows stay on `lType 16`, now feeds the simulated Messenger remote-whisper packet path back into the status-bar strip as an actual whisper chat row instead of only surfacing a generic success summary, and now lets the packet-owned local-utility chat path inject explicit client chat branches for party/friend/guild/alliance/association/expedition/system/notice/error rows, typed `lType 11/18/19/20/21/22/23` rows, and incoming or outgoing whisper rows either from explicit `/localutility chat <channel>` routing or structured packet payloads such as `type19|...` and `whisper:Rondo|...` instead of forcing those packet-owned lines through the generic chat fallback; exact client chat-log font selection, the remaining unmodeled font-routing details, channel-owned sub-branches such as the `lType 19` `nChannelID` split, and wider packet-owned incoming chat coverage outside the now-modeled Messenger and local-utility chat paths are still missing | The status-bar chat surface is closer to the client's visible log behavior and no longer forces button-only review of older lines, while keyboard chat-mode switching and whisper arming now stay on the bar instead of bouncing the player back out of chat after every target change, visible whisperable sender prefixes can now arm a new whisper target directly from the strip the way the client `TryBeginWhisper` path does, the label placement honors the WZ origin data instead of one flattened badge offset, the wrap pass follows the observed narrower first-line branch for whisper or special rows, modeled client `lType` rows now render in the strip with the simulator's client palette instead of whichever fallback color a caller happened to provide, explicit client-owned `lType 11/18/19/20/21/22/23` rows no longer collapse back to the generic white line treatment, simulator-owned incoming lines that already expose client-like colors or channel prefixes now enter the chat strip through matching `lType` branches instead of the generic fallback, packet-simulated Messenger whispers now land on the visible strip as whisper rows rather than disappearing into a generic acknowledgement, and packet-owned local-utility chat payloads can now drive more of the client's visible chat-log branches without rewriting the simulator-side text format by hand; it is still short of a full client chat log and whisper workflow clone until the remaining client font selection, the `lType 19` channel split, and additional packet-owned incoming chat ownership outside these modeled paths are matched | `MapSimulatorChat.cs`, `StatusBarChatUI.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `UILoader.cs`, `MessengerRuntime.cs` (`CUIStatusBar::ChatLogAdd`, `CUIStatusBar::_RefreshChatLog`, `CUIStatusBar::TryBeginWhisper`, `UI/StatusBar2.img/mainBar/chatTarget/*`) |
| Partial | Full skill UI behavior | The Big Bang skill window now mirrors `CUISkill::GetSkillRootVisible` at the tab layer by only exposing the visible skill-book path for the current job, keeps keyboard-selected entries in view while accepting pane-hover wheel scrolling plus `Up` / `Down` / `PageUp` / `PageDown` / `Home` / `End` navigation, lets `Left` / `Right` cycle across the currently visible skill-book tabs without reaching for the mouse, renders the client recommendation banner from a WZ-loaded skill-root/SP-threshold recommendation table with the existing visible-tab levelable-skill fallback when no applicable entry can level, routes `BtSpUp` clicks into live skill-level increments with per-tab SP countdown sourced from the remaining unmaxed levels in the loaded book, loads the Aran-only `Skill/main/Tab/AranButton/Bt1..Bt4` guide buttons so the client `3001..3004 -> OpenSkillGuide` path opens the matching `UIWindow2.img/AranSkillGuide/<grade-1>` overlay page, instantiates the WZ-backed bottom-row `BtRide` / `BtGuildSkill` controls, now loads the WZ-authored `Skill/main/Tab/DualTab` strip so Dual Blade jobs walk the client book path across `0, 400, 430, 431, 432, 433, 434` instead of collapsing their extra books onto the standard five-tab branch, and now also opens a dedicated WZ-backed `UIWindow2.img/UserList/GuildSkill` owner from both the skill-window guild shortcut and the guild member-list `BtSkill` action, loading the visible guild-skill catalog from `Skill/9100.img` plus `String/Skill.img` with the row recommendation banner, `BtRenewal`, and `BtUp` actions gated by the parsed `reqGuildLevel` formulas; the dedicated guild-skill runtime now suppresses recommendation cycling and hides both the renewal and level-up affordances when the local build is not actually in a guild, restores its seeded local state when guild membership comes back, keeps both the skill-window guild shortcut and the member-list `BtSkill` route disabled until the build exposes a real non-placeholder guild name, and now also preserves the local Master vs. Jr. Master vs. Member role already surfaced in the social list across runtime refreshes so `BtRenewal` / `BtUp` stay view-only for non-manager roles instead of silently re-promoting the local shell back to full authority | The simulator now respects the client's visible-root job path, no longer leaves the level-up button dead, follows the client-style recommendation progression more closely, no longer drops the Aran guide or the WZ-backed ride / guild shortcut controls that the Big Bang skill window exposes, no longer flattens Dual Blade books onto the standard tab strip, no longer treats guild-skill access as a dead generic-tab fallback, and no longer lets placeholder guild labels or social-list refreshes reopen the dedicated guild-skill owner with fake membership or reset the simulated local guild role back to `Master`; the remaining gap is now concentrated in packet-owned guild membership, contribution, and permission state plus true server-backed renewal and level-up semantics beyond this local owner shell | `SkillUI.cs`, `SkillUIBigBang.cs`, `UIWindowLoader.cs`, `SkillDataLoader.cs`, `MapSimulator.cs`, `GuildSkillWindow.cs`, `GuildSkillRuntime.cs`, `UserInfoUI.cs`, `SocialListWindow.cs` (`CUISkill::Draw`, `CUISkill::GetSkillRootVisible`, `CUISkill::GetRecommendSKill`, `CUISkill::OnButtonClicked`, `CUISkill::OpenSkillGuide`, `UI/UIWindow2.img/Skill/main/BtRide`, `UI/UIWindow2.img/Skill/main/BtGuildSkill`, `UI/UIWindow2.img/Skill/main/Tab/DualTab`, `UI/UIWindow2.img/Skill/main/Tab/AranButton`, `UI/UIWindow2.img/AranSkillGuide`, `UI/UIWindow2.img/UserList/GuildSkill`, `Skill/9100.img`, `String/Skill.img`) |
| Partial | Item/skill tooltip behavior | Skill hover tooltips now reuse the client `UIWindow2.img/Skill/main/tip0..tip2` frames in the skill window, quick-slot skill hovers, status-bar buff tray, and status-bar cooldown tray; the status-bar buff/cooldown hovers now also follow the same icon-led client-style layout and select the correct left/center/right frame variant instead of stretching those textures as a composite strip, while the cooldown tray continues to surface the live skill description plus remaining cooldown time instead of forcing the player to infer entries from icon art alone, the quick-slot item hover now also pulls live inventory icon art plus String.wz-backed type and description text instead of stopping at name-plus-count, and the Big Bang equipment window now renders live equipped-item icons from the current `CharacterBuild` plus a framed hover tooltip that pulls String.wz category/description text and parsed equip `info` metadata for stat gains, hands, upgrade slots, durability, `reqJob`, `reqPOP`, trade-availability counts, knockback resistance, time-limit flags, cash-layer visibility, and slot-specific eligibility lines instead of stopping at slot-plus-item-id stubs; those Big Bang equip hovers now also load the client `UIWindow2.img/ToolTip/Equip` canvas set so recognized equipment categories, property labels, requirement labels, job-filter badges, cash-item marker art, attack-speed labels, and more of the client numeric value presentation render from WZ art inside the existing tooltip shell instead of remaining entirely font-drawn, the richer `ToolTip/Equip`-backed presentation now carries into the Big Bang dragon, mechanic, android, and pet equipment-tab hovers even when those companion items only expose parsed WZ `info` metadata instead of a full `CharacterPart`, with pet-equip hovers also surfacing supported-pet and exclusion lines when that compatibility data is present, drag-hover comparisons in the Big Bang equipment page now stay on the richer equip tooltip path instead of dropping back to the older plain text bubble, and the main inventory equip tab now also lazily resolves `CharacterLoader.LoadEquipment(itemId)` data so its equip-slot hovers reuse the same `ToolTip/Equip` category labels, property labels, requirement labels, job badges, cash marker, bitmap-backed numeric rows, and live `CharacterBuild` requirement checks instead of falling back to the older generic item bubble. Full client `ToolTip/Equip` shell framing, complete bitmap numeric composition for every stat/value branch, potential/additional-option sections, expiration-instance parity, and broader item-tooltip parity for non-equip inventory/storage or other windows are still incomplete | The simulator now has a shared client-style tooltip surface across the main skill HUDs, quick-slot item hovers no longer flatten to a count-only stub when richer item metadata is already available, the Big Bang equipment window no longer leaves equipped-item hovers blank, metadata-thin, or drag-state-regressed, the companion equipment tabs no longer fall back to item-id-only hover stubs when parsed equip data or standalone companion `info` metadata is present, and regular inventory equip hovers no longer collapse back to the generic name-type-description bubble when the simulator can resolve the same WZ equip metadata it already uses elsewhere; equipment hovers now consume more of the client `ToolTip/Equip` art set for labels, badges, cash markers, and numeric rendering across both the equipment window and the inventory equip tab, but the remaining gap is still the rest of the client's fuller equip-tooltip shell, the remaining bitmap-number branches, potential/additional-option and live-expiration sections, and broader item-tooltip parity outside these owners | `SkillUIBigBang.cs`, `QuickSlotUI.cs`, `StatusBarUI.cs`, `EquipUIBigBang.cs`, `InventoryUI.cs`, `CharacterData.cs`, `CharacterLoader.cs`, `UIWindowLoader.cs`, `MapSimulator.cs` |
| Partial | Charge bars and richer skill HUD | The simulator still renders the client-timed prepared-skill gauge set for the supported status-bar branches, now keeps longer prepare windows from flipping to the generic `Ready` caption as soon as the short client gauge fill completes by surfacing a `Preparing <n> sec` state until the real hold or release point arrives, now also keeps the client-observed release-owned branches from reusing that generic ready caption by surfacing a `Release` state once the charge window is armed for `4341002`, `4341003`, and the Wild Hunter swallow family `33101005`, now gives the SG-88 `35121003` `KeyDownBar4` branch its own `Amplifying <n>%` / `Amplified` caption path instead of the shared generic charge text, and still routes the Evan/dragon `22151001` / `22121000` family through a separate world-overlay pass instead of the bottom status-bar surface while keeping the client `KeyDownBar2` / `KeyDownBar3` WZ skins on that split path; that dragon overlay now follows the client `CreateKeyDownBar` placement math from the live dragon vector and active frame height, anchoring the bar at `(-36, -frameHeight-30)` relative to the dragon instead of the older frame-top approximation, keeps the local dragon-owned bar out of the late UI pass, and now draws it in the world character pass between dragon and player so the simulator follows the client `CDragon::CreateKeyDownBar` action-layer ownership more closely instead of treating it like a HUD-only surface, while the generic skill-name plus ready-label text stays suppressed on the dragon-owned branch so it no longer reuses the status-bar caption treatment. The supported branch resolver is now shared with the remote prepared-skill seams too, so remote packet or command-driven overlays inherit the same client-backed skin, gauge-duration, release/amplify caption, and dragon text-suppression defaults instead of falling back to a generic `KeyDownBar` + `Ready` treatment, and remote non-dragon world overlays now anchor from the remote actor's live assembled frame top with the same `topY - 18` seam the local world-overlay path uses instead of a flat `actorY - 92` fallback. The remaining gap is now limited to any still-unmodeled client-only caption edge cases outside these supported branch families plus the lack of true remote dragon-anchor ownership because the simulator still has no remote dragon runtime or packet-owned dragon body state to anchor those overlays against the companion like the local path does | Prepared/keydown skills now surface closer client timing and HUD feedback instead of a single generic bar, longer prepare branches no longer advertise `Ready` while their actual prepare timer is still running, release-armed branches now advertise release ownership instead of a generic ready state, SG-88 now exposes a branch-specific amplify caption on its `KeyDownBar4` HUD, sustained keydown branches expose both their live maintain label and remaining capped-hold bar state more clearly, the Evan/dragon branch no longer falls back to the status-bar path, the older player-top/frame-top approximation, or the late UI layer because it uses the client dragon bar offsets from `CDragon::CreateKeyDownBar` and now renders in the world character pass closer to the client-owned dragon action layer, and remote prepared-skill overlays no longer regress those modeled skin or caption branches back to the generic defaults or the older flat actor-top placement for regular non-dragon skills; the remaining gap is now limited to unsupported caption edge cases outside these modeled HUD branches plus remote dragon-anchor ownership the current simulator still cannot derive yet | `StatusBarUI.cs`, `UILoader.cs`, `SkillManager.cs`, `MapSimulator.cs`, `MapSimulator.RemoteUsers.cs`, `RemoteUserActorPool.cs`, `PlayerCharacter.cs`, `DragonCompanionRuntime.cs` (`CUserLocal::DrawKeyDownBar`, `CDragon::CreateKeyDownBar`, `CDragon::DrawKeyDownBar`, `CUserLocal::Update`, `CUserLocal::TryDoingPreparedSkill`, `get_max_gauge_time`, `UI/Basic.img/KeyDownBar*/bar/origin`, `String/Skill.img/35121003`) |

### 2. Full CMob action pipeline

This area tracks runtime parity for mob-driven attacks, warnings, and combat effects after the AI commits to an action.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Combat Effects Mobs | Mob attack runtime now consumes the parsed `rush` and `jumpAttack` flags during action playback, `jumpAttack` no longer stays locked to base `MobMoveType.Jump` mobs because grounded movers can now launch their attack jump branch on demand, attack or skill completion can immediately chain into the next eligible combat branch instead of always dropping through a visible chase reset first, chained follow-up selection now waits for the longer of the action's WZ `attackAfter` / `skillAfter` or `effectAfter` recovery window instead of treating raw animation-end as the only completion gate, and structured `attack/info/effect` nodes now load and replay the WZ-backed `effectType = 2` repeated range-impact family plus the `fall` / `x` / `y` motion hints those nodes publish, while `effectType = 1` range sweeps now honor both WZ `start` and `duration` plus generic structured-node `interval` pacing when staggering their spawned lanes instead of front-loading the full pattern on one tick, now preserve authored `randomPos = 1` lane order instead of re-sorting those sweeps back into a left-to-right reveal, and now keep the WZ `effectDistance` stride as the actual lane spacing when that spacing itself determines how many sweep placements fit in the published range rather than redistributing those lanes to an evenly spread simulator step, sweep placements that outnumber the published sequence variants still cycle those WZ variants instead of pinning every extra lane to the last sequence, authored `attack/info/range` slot families now treat the published `lt/rb` box as the per-lane footprint when expanding `start` or `areaCount` driven boss lanes instead of compressing every spawned hazard back inside that one local box, and plain multi-node `attack/info/effect` families without structured metadata now keep the authored base `effect` branch as the primary playback while spreading only the numbered `effectN` siblings across the authored attack range instead of demoting that base branch into the grouped-lane path. Remaining gap is now narrower and limited to per-mob/client-only follow-up heuristics beyond this generic recovery gate plus any still-unmodeled combat-effect branches if the client uses payloads outside the now-observed structured `effectType = 1/2` and plain grouped `effect/effectN` families | The simulator now carries more of the client `CMob::DoAttack` movement payload into visible mob attacks, closes one of the more obvious `CMob::OnNextAttack` gaps by preserving combat flow across chained actions without ignoring published post-action recovery, no longer collapses authored negative-`start` or multi-`areaCount` lane attacks such as `8200011 attack2/info/range` or `8510000 attack2/info/range` into a tightly overlapping cluster, and no longer drops the meteor or repeated-impact style attack-effect branch that several mobs expose through `attack/info/effect`, including the falling offset path used by mobs such as `4240000`, the WZ `start`-offset and duration-spread sweeps used by `effectType = 1` nodes such as `8850001`, the repeated sweep or impact variants seen on boss-style `effect0` families such as `8200011`, `8500001`, `8510000`, `8520000`, and `8800000`, plus the plain Horntail-style grouped `effect/effect0..effectN` sequences exposed by mobs such as `8850002`, `8850004`, and `8850009` without losing the authored base `effect` playback those families still publish; those range sweeps also stop ironing `randomPos = 1` back into a deterministic left-to-right order and stop replacing WZ-authored `effectDistance` spacing with an evenly spread simulator spacing when the published distance is the thing that determines the sweep lane count, so repeated mob attack sequences now read closer to the client even though the full action pipeline is still incomplete | `MobItem.cs`, `MobMovementInfo.cs`, `LifeLoader.cs`, `MobAttackSystem.cs`, `MobAI.cs` (`CMob::DoAttack`, `CMob::OnNextAttack`, `CMob::ProcessAttack`, `CMob::GetAttackInfo`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
