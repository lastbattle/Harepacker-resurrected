# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. UI parity

- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`

Notes:
`CUIStatusBar::Draw` confirms that name/job-or-level selection, HP/MP/EXP number updates, and quick-slot redraw are part of a single status-bar draw pass, while `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` shows the client also revalidates slot contents against learned skills and inventory state before redraw. `CUISkill::Draw` confirms visible skill-root selection, recommend-skill highlighting, bonus-SP rendering, skill icon row layout, and skill-level text placement.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. UI parity

This area is more advanced than older parity notes suggested, but still far from complete client parity.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Status bar layout | Positions remain grounded in WZ plus client analysis and the prior parity work stays in place: the Big Bang `StatusBar2.img/mainBar` owner still composes the left cluster, gauge cluster, right-side buttons, and chat strip from authored canvas origins and WZ `z` order; left-cluster name/job/level slots and gauge numbers still follow the client `SetStatusValue` / `SetNumberValue` seams; the chat strip still uses the authored `chatSpace2` lane, `ChatLogDraw` left inset, client-shaped `ChatLogAdd` wrapping, and the owner-local embedded-font-preferred rasterizer path. The whisper-target picker remains on the recovered client `CUIStatusBar::ChangeWhisperTarget` -> no-NPC `CUtilDlgEx` type-7 owner seam: WZ still confirms `UI/UIWindow2.img/UtilDlgEx/list4/origin = (0,1)`, `list5/origin = (1,-1)`, shared shell slices (`t,c,s,line,list4,list5`), `UI/Basic.img/ComboBox2/normal/0` at `81x20`, `UI/Basic.img/BtComboBox/normal/0` at `17x16`, `UI/Basic.img/ComboBox` row slices at `5/5/18` with 17-pixel canvases, and `UI/Basic.img/VScr9` enabled arrows/thumb/base at `11x12` / `11x26`, while client re-check still confirms the 260-pixel util-dialog owner, editable combo at `(21, height - 63, 222, 18)`, footer buttons at `x=157` / `x=198`, `CCtrlComboBox::OnKey` routing Left / Right / Down through `BtClicked`, `CCtrlComboBox::OnMouseButton` routing left-button down / double-click (`513` / `515`) through `BtClicked`, `CCtrlComboBoxSelect::OnKey` handling only Up / Down / Enter before forwarding other keys, `CCtrlComboBoxSelect::OnMouseMove` using `ry / 16` highlight math plus scrollbar `curPos`, `CCtrlComboBoxSelect::OnMouseButton` using left release (`514`) for `SetSelect` with scrollbar `curPos` offset and right release (`517`) for raw-row delete, `CCtrlComboBox::MakeSelectWnd` placing the selector above the combo with `16 * count + 1` non-scrollbar height or `16 * maxShown` capped height, and `CCtrlComboBox::MakeScrollBar` creating the selector scrollbar with `16 * maxShown - 2` travel height, right-edge placement, scroll range `count - maxShown + 1`, and thumb position seeded from the current selection. The simulator keeps the earlier shipped modal work: clipped 519-pixel util-dialog frame slices, `m_scrHeight + 93` no-NPC height, dedicated combo-button toggle hotspot, left-button-down toggle timing, popup rows at a client 16-pixel selector step, above-combo dropdown offset, non-scrollbar bottom-edge row hit mapping, raw-row right-click delete, highlight-only popup navigation, same-row press/release guards, modal row accept into edit text without auto-confirming the util dialog, and focused regression coverage in `UnitTest_MapSimulator/StatusBarChatWhisperModalKeyParityTests.cs`, `UnitTest_MapSimulator/StatusBarChatWhisperModalCaptureParityTests.cs`, and `UnitTest_MapSimulator/StatusBarChatWhisperModalDeleteParityTests.cs`. This run keeps the combo owner on that same seam while closing the selector-scrollbar approximation instead of adding a simulator-owned list control: `ActivateWhisperTargetPickerModalComboFocus` still leaves the popup alive for native `BtClicked`-style close/reopen ordering, and the modal dropdown now also loads the authored `VScr9` chrome, reserves the client right-edge scrollbar lane, scrolls by first-visible row instead of mutating the editable owner text, pages on track clicks, drags the thumb against the client-shaped scroll range, and keeps hover/left-release selection on `firstVisible + ry / 16` while preserving the client raw-row right-click delete path. Remaining gap is now concentrated in byte-exact `IWzFont` face data and final COM-side shaping/hinting, the missing native COM child-window ownership for the editable combo, exact popup drop-list chrome and text metrics beyond the recovered row-step/offset/height/scrollbar geometry, remaining capture and release edge cases outside the modeled same-row accept/delete, combo-button `BtClicked`, and scrollbar press/drag paths, and the full edit-history lifecycle inside the native combo box. | The status bar and Big Bang chat strip stay materially closer to the client than before: major overlays and buttons remain anchored from authored `mainBar`, wrapped chat rows and clipping remain on the client-shaped chat seams, the modal picker remains on `CUtilDlgEx` / `CCtrlComboBox` ownership instead of a simulator-owned picker, and this pass now closes both the combo-owner focus split and the selector scrollbar ownership on the same client seam. Focus handoff no longer tears down the selector before toggle/delete logic runs, button press can close an open selector after owner focus returns, and long-history dropdowns now use a client-shaped scrollbar lane, row-hover offset, track paging, thumb dragging, and wheel-driven first-visible scrolling instead of the older first-visible/raw-row approximation alone. The row stays partial because final glyph rendering and several native combo child-window, text-metric, capture/release, and edit-history details are still approximated rather than embedded from the client implementation. | `StatusBarChatUI.cs`, `StatusBarChatLayoutRules.cs`, `MapSimulatorChat.cs`, `UILoader.cs`, `UnitTest_MapSimulator/StatusBarChatWhisperModalKeyParityTests.cs`, `UnitTest_MapSimulator/StatusBarChatWhisperModalCaptureParityTests.cs`, `UnitTest_MapSimulator/StatusBarChatWhisperModalDeleteParityTests.cs` (`CUIStatusBar::Draw`, `CUIStatusBar::SetStatusValue`, `CUIStatusBar::SetNumberValue`, `CUIStatusBar::ChatLogDraw`, `CUIStatusBar::ChatLogAdd`, `CUIStatusBar::ChangeWhisperTarget`, `CUtilDlgEx::Layout_INPUT`, `CUtilDlgEx::OnCreate_COMBOBOX_EDITABLE`, `CCtrlComboBox::OnKey`, `CCtrlComboBox::OnMouseButton`, `CCtrlComboBox::BtClicked`, `CCtrlComboBox::SetSelect`, `CCtrlComboBox::Delete`, `CCtrlComboBox::MakeSelectWnd`, `CCtrlComboBox::MakeScrollBar`, `CCtrlComboBoxSelect::OnKey`, `CCtrlComboBoxSelect::OnMouseMove`, `CCtrlComboBoxSelect::OnMouseButton`, `get_basic_font`, `UI/StatusBar2.img/mainBar/*`, `UI/UIWindow2.img/UtilDlgEx/{t,c,s,line,list4,list5,BtOK,BtClose}`, `UI/Basic.img/ComboBox2`, `UI/Basic.img/BtComboBox`, `UI/Basic.img/ComboBox`, `UI/Basic.img/VScr9`) |
| Partial | Skill UI layout | Row height, positions, hit boxes, SP placement, quick-slot assignment wiring, and a client-backed `Basic.img/VScr` scrollbar at the confirmed `153,93,155` placement are now present, with both skill-window variants reusing the client `tip0..tip2` frames for the fixed-size SP and hint balloons, the scrollbar supporting arrow clicks, track paging, thumb dragging, and wheel-over-scrollbar input, the row list drawing the client `Skill/main/recommend/0` banner from `Etc/RecommendSkill.img/<skillRootId>` SP-threshold table at the client `47,nTop-19` overlay offset plus the existing visible-tab levelable-skill fallback, the book header now following the client width-driven two-line wrap path by measuring the authored `110`-pixel text lane and breaking at the first overflowing word boundary instead of the older fixed-index split heuristic, the visible and inactive skill-book tabs drawing and hit-testing from their authored `UIWindow2.img/Skill/main/Tab/*` origins and heights, the `BtSpUp` control using its normal/hover/pressed/disabled states with WZ-backed `req` / `reqLevel` gates, and the local SP recompute seam deriving each visible book's pool from the WZ-backed book-open thresholds plus spent levels instead of fabricating points from every unfilled max level. This run also closes the long-stale legacy owner seam instead of leaving parity work Big Bang-only: WZ re-check on the mounted export still shows the same authored pre-Big Bang assets under `UI/UIWindow.img/Skill/{skill0,skill1,line,recommend/0,BtSpUp,Tab,tip0,tip1,tip2,DualTab}` plus `UI/Basic.img/VScr`, and the pre-Big Bang `SkillUI` owner now actually loads that authored row/tab/button/scrollbar art, receives initial and job-refresh skill-book population through the shared runtime path, and mirrors the same row backgrounds, recommendation banner, level-up button states, scrollbar interaction, keyboard navigation, and click/invoke routing instead of staying on the older bare-icon fallback while only `SkillUIBigBang` was wired. This pass closes the remaining legacy live-visible-root refresh seam on that same owner: WZ re-check keeps representative hidden skills such as `Skill/000.img/skill/0001014/invisible = 1` and `Skill/1000.img/skill/10001015/invisible = 1` authored in the mounted export, and IDA re-check of `CUISkill::GetSkillRootVisible` still shows reload flowing through `CSkillInfo::GetSkillRootVisible`, so the pre-Big Bang `SkillUI` now uses the same live learned-record refresh path as `SkillUIBigBang`, reloads and drops visible roots on skill-record mutation, filters hidden or time-limited unlearned rows through `SkillRootVisibilityResolver`, synchronizes current and max levels from `SkillManager`, recomputes local SP after refresh, and hides or coerces inactive standard and DualTab tabs instead of leaving stale visible-root tabs from the previous static load. This run closes the remaining pre-Big Bang `DualTab` strip seam on that same owner: WZ re-check confirms the legacy Dual Blade tab art lives under `UI/UIWindow.img/Skill/DualTab/{enabled,disabled}/0..6` while IDA re-check of `CUISkill::OnCreate` keeps the tab control at `(10,27)` with width `154` and height `20`, so the legacy `SkillUI` now loads those seven authored enabled/disabled tab canvases, switches Dual Blade jobs onto seven tabs instead of clamping 433/434 back into the fourth-tab bucket, hit-tests the seven client-width slots, carries Left / Right keyboard tab navigation across the same seven-tab range, and keeps the old five-tab buttons hidden while the DualTab strip owns the legacy surface. The shared hover tooltip path already keeps the client `320`-pixel body width, the `x=10/10,32/87` title-icon-text column layout, the live-mouse `+20` Y anchor, and the dedicated prerequisite footer, which now resolves its title and level caption through the client `StringPool` ids used by `CUIToolTip::DrawReqSkill` (`0x801` / `0x800`) instead of staying on simulator-only literals. This run narrows the same client-text seam again instead of adding another owner: the hover tooltip's current- and next-level section headers now format through the extracted client `StringPool` entries `[Current Level %d]` and `[Next Level %d]` (`0x2B3` / `0x2B4`) rather than the older simulator-owned `Current Level:` / `Next Level:` literals, so both skill-window variants stay on the same bracketed caption path the client ships for that header row. That same footer now also uses the client requirement-row fill color (`0xA0FFFFFF`) rather than the older simulator-owned red swatch, while preserved `#c...#` formatting markers and WZ-property-order fallback stat text still cover unauthored `h*` rows, including `itemCon` / `itemConNo`, `itemConsume`, `bulletConsume`, damage-over-time fields, `damagebymoneyCon`, `massSpell`, `magicSteal`, `fixdamage`, `selfDestruction`, `subProp`, `subTime`, `reqGuildLevel`, `ar`, `er`, `psdSpeed`, and `psdJump`. This run further narrows that fallback seam by keeping otherwise-unmodeled `level` / `common` properties in their authored WZ order when no authored `h*` text exists, so raw-key lines like `u`, `v`, or `actionSpeed` no longer disappear entirely while metadata-only keys such as `maxLevel` and `priceUnit` stay suppressed. This run also promotes another small class of fallback rows onto client-facing wording by formatting WZ-authored `actionSpeed` values as `Attack Speed` levels, by renaming `padX` / `madX` to the same `Weapon ATT` / `Magic ATT` phrasing that authored `String/Skill.img` `h*` lines already use, and by mining the authored `String/Skill.img` clause text around unresolved placeholders so fallback rows can now recover client-facing labels such as `Max Movement Speed`, `Movement Speed`, `Enemy Accuracy`, `Enemy Attack / DEF`, `Enemy DEF`, `Dodge Chance`, `Meso Drop Rate`, `Item Drop Rate`, `EXP / Item Drop Rate`, `Damage Guard Rate`, `Meso Guard Cost`, `Attack Count`, `Teleport Distance`, `Full Strength Chance`, `Distance`, `Damage Reflected`, `Damage Distributed`, `Amount Absorbed`, `HP Recovery`, `Attacks Absorbed`, `Time to Next Shield Available`, `Critical Rate`, `DEF`, or `Mob Count` instead of dropping back to raw single-letter captions in those common cases, and this pass extends that same description-backed seam to additional authored client clauses so otherwise-unmodeled fallback rows can now surface labels such as `Dash Speed`, `Robot Factory Duration`, `Summon Interval`, `Toy Robot Damage`, `Spin Attack Damage`, `Toy Robot Count`, and `Potion Count` instead of collapsing back to generic `Speed`, `Duration`, `Interval`, `Damage`, `Mob Count`, or raw single-letter captions. This run narrows that same seam again by preferring the exact authored clause prefix when it is a safe upgrade over those older generic captions, so fallback rows can now keep client-facing wording such as `Max Enemies Hit`, `Final Attack Count`, `Seal Duration`, `Max Toy Robots Summoned`, and `With extra MP Cost` instead of flattening those cases back to `Mob Count`, `Attack Count`, `Duration`, or `MP Cost`. This pass keeps tightening that same authored-clause path for otherwise raw generic keys as well: WZ-authored clauses such as `Fury Cost: #forceCon`, `Abnormal Status Resistance/Elemental Resistance +#x%`, `DEF +#s%`, `When attacked #z% of damage absorbed as HP`, and `All Elemental Resistance: +#z%` can now recover `Fury Cost`, `Abnormal Status Resistance / Elemental Resistance`, percent-formatted `DEF`, `Damage Absorbed as HP`, and `All Elemental Resistance` fallback rows instead of degrading to `Force Con`, `Weapon ATT`, `X`, or `S` raw-key captions. This run keeps narrowing the same fallback seam by making the single-letter placeholder matcher token-local for WZ-authored mixed clauses such as `Fire Spirit - Damage Over Time: #v% chance of causing #y% damage every #x sec for #w sec` and `at #w% chance for #v sec`, so those rows now resolve to damage-over-time chance, damage, interval, duration, and status-duration captions instead of letting earlier `chance` or `every sec` text elsewhere in the clause steal the label. This pass tightens the same authored-clause seam for named fallback stats that were still sticking on simulator-generic captions even when the authored clause exposed a safer prefix: WZ re-check still shows representative cases such as `1111002/h` (`Damage: +#damR% per combo orb`), `1221009/h` / `14111005/h` / `2121003/h` (`DEF Ignore Chance` / `Defense Ignored` / `Monster Defense Ignored`), `31121001/ph` (`Final Damage against Boss Monster: +#bdR%`), and `4220009/h` (`Mesos Dropped (by monsters): #mesoR%`), so the fallback presenter now prefers those authored prefixes for `damR`, `ignoreMobpdpR`, `bdR`, and `mesoR` instead of flattening them back to generic `Damage Reduction`, `Ignore Enemy DEF`, `Boss Damage`, or `Meso Rate`. WZ re-check on the mounted export still shows the earlier token-local labels authored directly in `String/Skill.img` clauses such as `1001005/h`, `12111002/ph`, `23100006/ph`, `23111008/h`, `23111008/ph`, `23111009/h`, `23111009/ph`, `23111010/h`, `23111010/ph`, `24121054/h`, `31121003/h`, `31121000/h`, `31121000/ph`, `31111004/h`, `35121009/h`, `35121009/ph`, `4321000/h`, `4321000/ph`, `5311004/h`, `5311004/ph`, and `91000006/h`, and the current/next header strings still live in the extracted client `StringPool` at `0x2B3` / `0x2B4`, so this pass keeps the work in the existing authored-string and extracted-client-text seams rather than inventing simulator-only wording; focused regression coverage for that seam now includes `UnitTest_MapSimulator/SkillStatDescriptionAliasTests.cs` for both token-local and named-stat WZ-authored fallback labels. This pass also keeps the dedicated guild-skill owner improvements in the existing seams: the local owner now persists learned levels, remaining local SP, guild fund, and active renewal expirations per guild identity instead of one shared save bucket, so switching between no-guild, guild A, and guild B contexts no longer cross-pollinates local guild-skill state, and the dedicated guild-skill hover detail now uses the authored `UIWindow2.img/UserList/GuildSkill/base` width (`244`) as its bounded text surface, reuses that same `base` canvas as the tooltip shell instead of stretching the broader window frame texture, preserves embedded line breaks and blank spacer rows, wraps title and body text at their actual draw scales, and clamps the tooltip to the viewport instead of letting the older generic box overflow off the window edge. This run narrows that same dedicated guild-tooltip text seam by moving its current and next sections off the simulator-owned `Current:` / `Next:` prefixes and onto the existing `SkillTooltipClientText` formatter backed by extracted client `StringPool` entries `[Current Level %d]` / `[Next Level %d]` (`0x2B3` / `0x2B4`), with WZ re-check still showing guild-skill names, descriptions, and `h` clauses authored under `String/Skill.img/91000000..91000006` and the tooltip shell still owned by `UIWindow2.img/UserList/GuildSkill/base`. The `sheet1` footer path remains on strip-width-aware wrapping with clipped draw bounds so long current/next/cost summaries stay anchored to the loaded strip geometry. Remaining layout parity is now narrower and concentrated in the original client's exact `IWzFont` glyph raster and palette, the last micro-offset or line-break differences on row or tooltip text outside those string-pool-backed requirement, level-section, and guild-skill current/next captions, the remaining fallback cases whose authored WZ text still does not expose enough clause context or uses unrecovered localization patterns beyond this description-backed seam, the absence of true packet- or login-authored per-book SP, guild-fund, contribution, renewal, and ledger/session ownership where the simulator still has to derive or persist those values locally. | The window is materially closer to the client than the older row implied: recommendation overlays, row and tab geometry, hover-skill requirements, fallback stat coverage, and local SP affordances already live in WZ/client-backed seams, and the remaining gap is now mostly the final font-object and authoritative-session polish rather than structural layout misses. This run keeps the measured `110`-pixel book-name wrap path and the earlier hover-tooltip gains in place, narrows the shared tooltip text again by moving the current/next level headers off simulator literals and onto the extracted client bracketed captions, keeps the shared fallback wording anchored to authored `String/Skill.img` clauses instead of flattening more one-letter properties to raw keys, and now brings the legacy `UIWindow.img/Skill` owner back onto the same authored row/button/scrollbar/runtime seams instead of leaving pre-Big Bang skill books effectively unowned. It also extends that same authored-clause seam to more specific client-facing fallback labels from mechanic, dual-blade, demon-slayer, evan spirit, corsair, and guild-skill text instead of letting those rows degrade to generic captions like `Speed`, `Duration`, `Damage`, `Chance`, or raw placeholder keys, now prefers the exact authored placeholder prefix when the WZ clause exposes a safer client phrase than the older generic fallback label, uses token-local wording for mixed damage-over-time and status-duration clauses so nearby words in the same authored sentence no longer steal another placeholder label, and now also lets named fallback stats keep authored client-facing prefixes such as `Damage`, `DEF Ignore Chance`, `Final Damage against Boss Monster`, and `Mesos Dropped (by monsters)` instead of flattening those rows back to simulator-generic named labels. This run also applies that same bracketed client current/next header seam to the dedicated guild-skill hover detail while keeping the guild skill effect text WZ-authored from `String/Skill.img/91000000..91000006` and the shell geometry WZ-authored from `UIWindow2.img/UserList/GuildSkill/base`. Focused regression in this row now covers token-local plus named-stat WZ-authored fallback labels in `UnitTest_MapSimulator/SkillStatDescriptionAliasTests.cs`. The row stays partial because the simulator still lacks the final client `IWzFont` rendering path, the last micro-offset or line-break differences on row or tooltip text, the remaining fallback cases whose WZ text still does not describe the placeholder strongly enough to infer a safe client-facing label or whose localization pattern needs a narrower token-local rule than the current English authored-clause matcher, true packet-owned guild contribution, fund, renewal, ledger, and per-book SP ownership. | `SkillUI.cs`, `SkillUIBigBang.cs`, `SkillTooltipClientText.cs`, `GuildSkillWindow.cs`, `GuildSkillRuntime.cs`, `GuildSkillTooltipContentBuilder.cs`, `SkillDataLoader.cs`, `UIWindowLoader.EquipmentAndStats.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `UIWindowLoader.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/SkillStatDescriptionAliasTests.cs` (`CUISkill::Draw`, `CUISkill::OnCreate`, `CUISkill::OnMouseMove`, `CUIToolTip::SetToolTip_Skill`, `CUIToolTip::DrawReqSkill`, `CUISkill::GetRecommendSKill`, `Etc/RecommendSkill.img`, `Skill/*/skill/*/req`, `UI/UIWindow.img/Skill`, `UI/UIWindow.img/Skill/DualTab`, `UI/UIWindow2.img/Skill/main/Tab`, `UI/UIWindow2.img/Skill/main/recommend`, `UI/UIWindow2.img/Skill/main/BtSpUp`, `UI/UIWindow2.img/Skill/main/tip0`, `UI/UIWindow2.img/Skill/main/tip1`, `UI/UIWindow2.img/Skill/main/tip2`, `UI/UIWindow2.img/UserList/GuildSkill`, `UI/UIWindow2.img/UserList/GuildSkill/base`, `UI/UIWindow2.img/UserList/GuildSkill/sheet1`, `UI/Basic.img/VScr`, `String/Skill.img`, `String/Skill.img/91000000..91000006`, `StringPool 0x2B3`, `StringPool 0x2B4`, `StringPool 0x800`, `StringPool 0x801`) |
| Implemented | Quick slot cooldown overlay | Cooldown masking now uses the client `UIWindow2.img/Skill/main/CoolTime` frame family, remaining-seconds countdown text renders on assigned quick-slot skills, and the stepped mask holds the initial full-overlay frame until the next visible whole-second boundary instead of advancing immediately after cast | The core client-facing quick-slot cooldown feedback is now present on the simulator HUD | `QuickSlotUI.cs` (`CUIStatusBar::CQuickSlot::Draw`) |
| Implemented | Quick-slot validation and quantity sync | The simulator now rejects passive, hidden, and unlearned skills at hotkey assignment time, restores saved quick-slot bindings through the same validation path after preset load instead of trusting raw slot data, eagerly prunes stale skill slots as soon as learned levels change, cancels an in-flight quick-slot drag if its source binding becomes invalid before the next redraw, and now mirrors the client's `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` inventory-backed recount for owned `USE` / `CASH` quick-slot item entries so stack counts redraw live and empty item slots clear as soon as their backing inventory runs out | Quick-slot contents now stay synchronized with both learned-skill state and live inventory stacks, so the HUD no longer keeps stale consumable or cash-item entries after crafting, pickup, preset restore, or manual reassignment | `QuickSlotUI.cs`, `SkillManager.cs`, `CharacterConfig.cs`, `MapSimulator.cs` (`CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo`) |
| Implemented | HP/MP warning flash | HP/MP gauge warnings now replay the client `aniHPGauge` / `aniMPGauge` and `hpFlash` / `mpFlash` assets for 500 ms when the resource drops further while already below the configured threshold, and the simulator exposes live `/hpwarn` + `/mpwarn` commands to adjust those thresholds in-session | The client-visible low-resource cue is present across both modern and pre-BigBang status-bar loaders, and the trigger point is no longer hardcoded at 20% | `StatusBarUI.cs`, `UILoader.cs`, `MapSimulator.cs` (`CUIStatusBar::Draw`, `CUIStatusBar::SetNumberValue`) |
| Partial | Chat log / whisper flows | The Big Bang status-bar chat strip still keeps the earlier parity work in place: wrapped recent chat lines follow the client `ChatLogAdd` continuation-indent rules, apply the observed first-line width reduction for `lType 14/16/19/20`, cap visible wrapped history at the client `0x40` entry ceiling, keep manual scroll position until the `_RefreshChatLog` idle snap-back window expires, honor WZ `chatTarget/*/origin` deltas when drawing the target badge, and let both strip clicks and `/w` or `/r` arm whisper mode through the tighter client-shaped `ExtractCharacterName` plus `ChangeWhisperTarget` validation seam confirmed in IDA. The WZ-backed whisper picker still uses `UIWindow2.img/UtilDlgEx/list4` and `list5`, keeps the full remembered candidate history scrollable by wheel, keyboard row navigation, and page movement over the visible six-row window, restores the preserved draft on `Escape`, confirms on row click or `Enter`, and keeps strip-row confirmation on the active picker presentation path instead of transiently reopening the inline picker. The modal-owner gap now also keeps the closer recovered `ChangeWhisperTarget` behavior from the client help and IDA seam: `/w` with no explicit name now reuses the current whisper/reply target when one exists and only falls back to the client-shaped modal picker when no current target is armed, while `/r` with no current target still opens that same modal even when remembered history is empty. The modal still opens on the editable combo row because the client `CUtilDlgEx::OnCreate_COMBOBOX_EDITABLE` path calls `CWnd::SetFocusChild(this, this->m_pCombobox.p)`, button `keyFocused` canvases still only render after the footer lane is actually armed, `Tab` still toggles between the combo lane and the `BtOK` / `BtClose` footer lane, and left/right still only swap OK versus close while that footer lane is active. WZ re-check still confirms the same owner assets for this seam without widening it: `UI/UIWindow2.img/UtilDlgEx/list4/origin = (0,1)`, `list5/origin = (1,-1)`, `UI/Basic.img/ComboBox2/normal/0 = 81x20`, and `UI/Basic.img/BtComboBox/normal/0 = 17x16`. Client re-check still keeps the same modal-key branch inside that owner: `CCtrlComboBox::OnKey` only sends `VK_LEFT`, `VK_RIGHT`, and `VK_DOWN` through `BtClicked` on key-down, otherwise forwarding the key to the child edit control. The simulator now keeps the recovered `11`-pixel chat-font height, routes the shared chat/whisper-picker renderer onto the recovered `StringPool 0x1A25 -> Arial` face request instead of the earlier embedded-font-preferred fallback path, matches the recovered `ChatLogAdd` palette for `lType 13/14/16/21`, and also keeps the stricter editable-combo seam in place: the shared character-name extractor no longer falls through to arbitrary trailing tokens after stripping chat prefixes, typed modal input still keeps the live edit text while auto-selecting the first remembered candidate whose prefix matches the current combo text with exact matches winning over earlier prefix hits, the explicit typed-target arm path now validates the raw combo/edit text through the closer native `is_valid_character_name`-shaped branch instead of normalizing decorated chat fragments like `[Whisper] Name` into valid character names, the confirm path now also trims leading and trailing combo text the same way `ChangeWhisperTarget` runs `TrimRight` / `TrimLeft` before the native empty-name and `is_valid_character_name` checks so ` Alice ` now arms `Alice` while decorated fragments still fail validation, and failed modal `Enter` / `BtOK` confirms now follow the closer recovered `ChangeWhisperTarget` return seam by closing the modal before empty-name, invalid-name, or self-target results fall back to chat instead of leaving the editable combo owner hanging open. Closed-popup combo-lane `Down` now only opens the dropdown instead of immediately stepping the remembered selection and overwriting the live edit text, footer-lane `Up` / `Home` / `End` / `PageUp` / `PageDown` now return focus to the combo lane before continuing the existing edit or navigation path, and the full modal whisper-picker event seam now survives status-bar content reloads instead of falling back to the earlier reduced rebinding path that dropped hover, delete, confirm, and focus routes after the UI refreshed. Focused simulator coverage for this row now includes `UnitTest_MapSimulator/StatusBarChatFontParityTests.cs` for the recovered `0x1A25` face route plus the shared `11`-pixel chat-font constants, `UnitTest_MapSimulator/StatusBarChatWhisperModalKeyParityTests.cs` for the closed-popup modal `Down` path, the footer-to-combo focus handoff on `Home` and `PageDown`, and visible-row hover highlight preserving the live edit text while restoring combo-lane focus, `UnitTest_MapSimulator/StatusBarChatWhisperCommandParityTests.cs` for `/w` current-target reuse, trimmed modal combo text acceptance, explicit decorated-target rejection, and failed modal confirm closure on invalid, self, and empty typed targets, and `UnitTest_MapSimulator/StatusBarChatRemoteUserOfficialBridgeParityTests.cs` for the official `181` / `182` common-chat bridge mapping. This run also tightens the native combo select-window height seam from client evidence: `CCtrlComboBox::MakeSelectWnd` computes the popup height as `16 * itemCount + 1` when the history list is not scrollbar-capped and as `16 * m_nMaxItem_Shown` when the popup needs its scrollbar, so the simulator now keeps the one-pixel bottom edge for short modal whisper histories while keeping longer remembered histories capped to the visible six-row `16`-pixel selector height. This run also tightens the packet-owned incoming-chat bridge from client evidence: `CUserPool::OnUserCommonPacket` at `0x94cdb0` dispatches common packet type `181` to `CUser::OnChat(..., 0)` and type `182` to `CUser::OnChat(..., 1)` after consuming the character id, with the outside-map branch falling back to the local user when no remote user is present. The simulator now carries those official-session opcodes through `RemoteUserOfficialSessionBridgeManager` by default into the existing non-conflicting `UserChat` / `UserChatFromOutsideMap` seam, which continues to decode character id, chat flag, text, balloon-only flag, and outside-map speaker name before appending the matching status-bar chat line. Remaining gap is now narrower and concentrated in the exact native `IWzFont` COM objects plus any still-unrecovered font-style or shaping differences beyond the recovered `Arial` face, `11`-pixel height, and palette routes, the rest of the native combo-box edit/history semantics and unrecovered modal key choreography inside `ChangeWhisperTarget` outside the now-modeled `/w` current-target reuse seam, failed-modal-confirm closure seam, footer-lane left/right, closed-popup combo `Down` toggle path, visible-row hover/current-position path, exact popup drop-list chrome and text metrics beyond the now-modeled `16 * itemCount + 1` short-list height, `16 * visibleRows` scrollbar-capped height, 16-pixel selector step, and above-combo offset, and broader packet-owned incoming chat ownership outside the already modeled whisper, official remote-user common chat, Messenger, field-feedback, local-utility, summoned, local-overlay, MapleTV, packet-script, and quest-result families. | The status-bar chat surface is materially closer to the client's visible log and whisper flow than before: older lines no longer require button-only review, visible whisperable sender prefixes can arm a new target directly from the strip, the chat-target badge honors the authored WZ origin deltas, modeled client `lType` rows keep the simulator's client palette instead of caller-provided fallback colors, and packet-shaped whisper or messenger lines no longer collapse back to a generic visible-strip treatment. This run narrows the remaining owner drift inside the same `CUtilDlgEx` seam by keeping the shared chat strip and whisper picker on the recovered `11`-pixel `Arial` request, by matching the recovered `ChatLogAdd` colors for notice, outgoing-whisper, incoming-whisper, and shared type-21 rows, by reusing the current whisper contact for `/w` before opening the modal picker, by trimming leading/trailing modal combo text through the same native pre-validation seam while still rejecting decorated modal/direct-whisper text instead of auto-sanitizing it into a valid target, and by following the native post-dialog return path when the modal confirm fails so the picker closes and the preserved pre-modal draft resumes instead of leaving the modal open. It also keeps the official-session common chat bridge on the same packet-owned `CUserPool::OnUserCommonPacket` / `CUser::OnChat` seam instead of leaving opcodes `181` / `182` to manual aliases only, keeps the modal combo-box key route already confirmed through `CUtilDlgEx::OnCreate_COMBOBOX_EDITABLE` and `CCtrlComboBox::OnKey`, and now also keeps that same modal owner fully wired after status-bar content refreshes instead of silently dropping back to toggle-only rebinding after load/reload. The row stays partial because the simulator still approximates the client's final `IWzFont` COM object behavior, any richer per-`lType` font-style switching or shaping, still does not mirror every internal combo-box text-entry/history behavior or modal key path inside `ChangeWhisperTarget`, and still lacks broader packet-owned incoming chat ownership outside the already modeled feedback families now including the official remote-user common chat bridge. | `MapSimulatorChat.cs`, `StatusBarChatUI.cs`, `StatusBarChatLayoutRules.cs`, `UILoader.cs`, `MapSimulator.Content.cs`, `MapSimulator.PacketOwnedFieldFeedbackParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapSimulator.RemoteUsers.cs`, `RemoteUserPacketCodec.cs`, `RemoteUserPacketInboxManager.cs`, `RemoteUserOfficialSessionBridgeManager.cs`, `MapleTvRuntime.cs`, `MessengerRuntime.cs`, `UnitTest_MapSimulator/StatusBarChatFontParityTests.cs`, `UnitTest_MapSimulator/StatusBarChatWhisperModalKeyParityTests.cs`, `UnitTest_MapSimulator/StatusBarChatWhisperCommandParityTests.cs`, `UnitTest_MapSimulator/StatusBarChatRemoteUserOfficialBridgeParityTests.cs` (`CUIStatusBar::ChatLogAdd`, `CUIStatusBar::_RefreshChatLog`, `CUIStatusBar::TryBeginWhisper`, `CUIStatusBar::ChangeWhisperTarget`, `CUIStatusBar::OnCreate`, `is_valid_character_name`, `StringPool 0x31E`, `StringPool 0x31F`, `StringPool 0x320`, `StringPool 0x1A25`, `CCtrlComboBox::OnKey`, `CUtilDlgEx::OnCreate_COMBOBOX_EDITABLE`, `CWnd::SetFocusChild`, `CField::OnWhisper`, `CUserPool::OnUserCommonPacket`, `CUser::OnChat`, `UI/StatusBar2.img/mainBar/chatTarget/*`, `UI/UIWindow2.img/UtilDlgEx/{t,c,s,bar,line,BtOK,BtClose,list4,list5}`, `UI/Basic.img/{ComboBox2,BtComboBox}`) |
  Update for Chat log / whisper flows: status remains `Partial`. This pass keeps the WZ evidence unchanged for the modal owner assets (`UI/UIWindow2.img/UtilDlgEx/list4/origin = (0,1)`, `list5/origin = (1,-1)`, `UI/Basic.img/ComboBox2/normal/0 = 81x20`, and `UI/Basic.img/BtComboBox/normal/0 = 17x16`) and tightens another client-owned combo-box seam confirmed in IDA. `CCtrlComboBoxSelect::OnKey` at `0x4d91f0` keeps a scrollbar/current-position value and only adjusts it when `m_bShown` moves outside the visible range; `CCtrlComboBoxSelect::OnMouseMove` at `0x4d82b0` uses that same current position when mapping `ry / 16` to the highlighted row; and `CCtrlComboBox::MakeSelectWnd` at `0x4d94e0` still owns the six-row capped selector window. The simulator now carries an explicit first-visible whisper-history row through `MapSimulatorChatRenderState`, renders the inline and modal picker from that state instead of recomputing the window around the selected row every draw, keeps open-popup `Up` / `Down` highlight movement from scrolling the six-row selector window until the highlight leaves the visible range, preserves an already-open selector window when focus simply returns to the combo owner, and now also keeps the full modal whisper-picker callback seam wired after status-bar content reloads by reusing the same `MapSimulator.Content.cs` hookup path for initial load and later refreshes instead of falling back to a reduced post-refresh binding set. Focused coverage for this row was extended in `UnitTest_MapSimulator/StatusBarChatWhisperModalKeyParityTests.cs` for combo-owner focus preserving the selector window, for the native current-position rule on popup `Up` / `Down`, and for visible-row hover highlight preserving the live edit text while restoring combo-lane focus. The remaining gap is now narrower: exact `IWzFont` COM object shaping and any unrecovered per-`lType` style differences still remain, and the modal picker still approximates deeper native `CCtrlComboBox` edit/history lifecycle and unrecovered `ChangeWhisperTarget` choreography outside the modeled focus, toggle, status-bar reload rebinding, height, selector-current-position, validation, failed-confirm, and packet-bridge seams.

| Partial | Full skill UI behavior | The Big Bang skill window now mirrors `CUISkill::GetSkillRootVisible` at the tab layer by only exposing the visible skill-book path for the current job, keeps keyboard-selected entries in view while accepting pane-hover wheel scrolling plus `Up` / `Down` / `PageUp` / `PageDown` / `Home` / `End` navigation, lets `Left` / `Right` cycle across the currently visible skill-book tabs without reaching for the mouse, renders the client recommendation banner from a WZ-loaded skill-root/SP-threshold recommendation table with the existing visible-tab levelable-skill fallback when no applicable entry can level, and now also tracks the displayed skill root per tab so the `CUISkill::GetRecommendSKill` branch for Dual Blade's early `400/430` books follows the client more closely by resolving threshold progress from the combined learned levels across those two visible roots while sourcing the recommendation table from the `430` root instead of treating each tab as a fully isolated recommendation bucket. It routes `BtSpUp` clicks into live skill-level increments with per-tab SP countdown sourced from the remaining unmaxed levels in the loaded book, loads the Aran-only `Skill/main/Tab/AranButton/Bt1..Bt4` guide buttons so the client `3001..3004 -> OpenSkillGuide` path opens the matching `UIWindow2.img/AranSkillGuide/<grade-1>` overlay page, instantiates the WZ-backed bottom-row `BtRide` / `BtGuildSkill` controls, now loads the WZ-authored `Skill/main/Tab/DualTab` strip so Dual Blade jobs walk the client book path across `0, 400, 430, 431, 432, 433, 434` instead of collapsing their extra books onto the standard five-tab branch, and now also opens a dedicated WZ-backed `UIWindow2.img/UserList/GuildSkill` owner from both the skill-window guild shortcut and the guild member-list `BtSkill` action, loading the visible guild-skill catalog from `Skill/9100.img` plus `String/Skill.img` with the row recommendation banner, `BtRenewal`, and `BtUp` actions gated by the parsed `reqGuildLevel` formulas. The dedicated guild-skill runtime now suppresses recommendation cycling, treats `BtRenewal` as a local timed guild-skill renewal action backed by `Skill/9100.img/common/{extendPrice,period,priceUnit}` instead of as a recommendation toggle, persists local learned levels plus active-renewal timers and a simulator-owned guild-fund ledger across placeholder membership loss and restoration, preserves a simulator-owned fallback guild level instead of resetting every in-guild refresh to Guild Lv. 1, refreshes both the skill-window guild shortcut state and the dedicated guild-skill owner from the current build plus social authority context instead of only at initial wire-up, now also persists same-guild packet-owned guild-level and guild-point updates back into the per-guild saved-state bucket so later local-context refreshes keep the last authoritative `CWvsContext::OnGuildResult(75)` point total and guild level instead of falling back to the simulator seam, and now also keeps the shared social-list UI context on that same `OnGuildResult(75)` level echo even before a separate guild-UI packet arrives so the skill-window guild shortcut and dedicated owner do not briefly snap back to the fallback guild-level seam during packet-owned refreshes. It keeps both the skill-window guild shortcut and the member-list `BtSkill` route disabled until the build exposes a real non-placeholder guild name, preserves the local Master vs. Jr. Master vs. Member role already surfaced in the social list across runtime refreshes so `BtRenewal` / `BtUp` stay view-only for non-manager roles instead of silently re-promoting the local shell back to full authority, and now also gates both actions against the WZ-authored learn and renewal meso costs so the dedicated owner spends from a local guild-fund pool instead of letting every manager action succeed for free. When that guild authority path is packet-owned, the dedicated runtime now stages `BtRenewal` and `BtUp` as pending requests instead of mutating the local shell immediately, feeds the pending learn-vs-renew label into the dedicated summary and hover detail seams, blocks duplicate manager actions while approval is outstanding, and resolves the staged result through the existing `/sociallist packet resolve guild approve|reject` path so packet-authority scenarios can now model an approve-vs-reject round trip instead of always auto-succeeding locally. That resolve seam now also accepts authoritative `level=<n>`, `remain=<minutes>`, and `fund=<mesos>` outcome tokens for pending guild-skill approvals, letting the dedicated runtime mirror packet-owned learn and renewal results with server-shaped skill levels, renewal timers, and post-action guild-fund totals instead of always deriving those values from the local cost-and-duration shell after approval. It also surfaces `Skill/9100.img/common/{price,extendPrice,period,priceUnit}` plus `String/Skill.img/9100000x/h` in the dedicated owner so hover or selection details show the authored current/next effect text, learn cost, renewal cost, duration, local remaining renewal time, current fund state, the pending-action state when packet approval is outstanding, and the packet-owned guild point total carried by `CWvsContext::OnGuildResult(75)`, with packet-owned guild points now staying labeled as guild points instead of being formatted through the meso path and true no-guild detail states no longer leaking the simulator-owned guild-fund or guild-point rows. Unlearned guild skills no longer duplicate the same level-1 WZ effect text as both `Current` and `Next`; the dedicated owner now also loads and draws `UIWindow2.img/UserList/GuildSkill/sheet1/0..2` so the footer uses the client-authored activation-effect strips instead of a simulator rectangle, and the selection summary feeding that footer now follows the effect-focused WZ data rather than reusing a generic guild-name/SP status block. This run also keeps the first resolved guild recommendation banner stable after that entry caps or becomes temporarily unlevelable instead of cycling the highlight to another row on every local state change, makes the footer-summary text flow follow the authored strips more closely by wrapping into the available strip width/height and vertically centering line placement against the loaded `sheet1` row textures instead of relying on a fixed single-line truncation seam, while `Up` / `Down` / `PageUp` / `PageDown` / `Home` / `End` can move the guild-skill selection without reaching back to the mouse and `PageUp` / `PageDown` now step by the visible page size instead of a shorter local jump. The dedicated guild-skill owner now also loads `UI/Basic.img/VScr` and applies the client-style vertical scrollbar seam over the list, so arrow clicks, track paging, and thumb dragging follow the same visible-page scroll state as the existing wheel and keyboard navigation instead of leaving longer guild-skill catalogs mouse-wheel-only. This run further narrows the dedicated-owner detail gap by keeping the generic guild-skill description separate from the authored current-effect slot, explicitly surfacing the simulator-owned guild fund, packet-owned guild points, pending approval, the timed active-vs.-inactive state, and a separate view-only authority label in the hover detail path, honoring embedded line breaks plus overlong-token wrapping in the dedicated owner tooltip instead of flattening everything through a single whitespace-only wrapper, keeping wrapped tooltip title fragments on the same highlighted title palette and scale instead of demoting every fragment after the first to body text, and labeling the true no-guild detail state separately from generic member view-only mode. Focused simulator coverage now exists for the dedicated-owner same-guild packet-context persistence seam, the non-cycling guild recommendation seam, the split active-state-vs.-view-only-authority tooltip/runtime seam, the shared social-list guild-skill UI-context seam that now carries standalone `OnGuildResult(75)` guild level and zero-point echoes without requiring a separate guild-UI packet, and the pending-approval resolution path that now lets authoritative `CWvsContext::OnGuildResult(81)` guild-skill record echoes clear staged `BtUp` / `BtRenewal` requests while syncing the resulting level or renewal timer and spending the staged guild-fund cost in `UnitTest_MapSimulator/SocialListGuildSkillUiContextParityTests.cs`, alongside the existing runtime coverage. This pass also aligns the shared/classic skill-book visibility helper with the confirmed client `CSkillInfo::IsSkillVisible` hard-hide list by suppressing `10001015` in the same path that already hid `4321001`, `1014`, and the Dual Blade-born `4000001` / `4001344` pair, with WZ confirming `Skill/1000.img/skill/10001015` is an invisible, time-limited, disabled skill entry. This pass also persists the last resolved guild-point total inside the per-guild saved-state bucket so `OnGuildResult(75)` point updates survive same-guild or no-guild context round-trips that momentarily omit a fresh point value, now treats packet-owned guild points as authoritative even when that payload resolves to `0` instead of silently reviving a stale saved total on refresh, keeps hovered guild-skill rows on the authored `iconMouseOver` art branch, and keeps the dedicated tooltip on the explicit no-guild `Current: Join a guild.` / `Next: Requires guild membership.` wording instead of falling back to the generic not-learned branch while the authored WZ description, requirement, duration, and cost rows remain visible. | The simulator now respects the client's visible-root job path, no longer leaves the level-up button dead, follows the client-style recommendation progression more closely, now also avoids undercounting Dual Blade `400/430` recommendation thresholds by mirroring the client-owned combined spent-skill-level branch for those first two books, no longer drops the Aran guide or the WZ-backed ride / guild shortcut controls that the Big Bang skill window exposes, no longer flattens Dual Blade books onto the standard tab strip, no longer treats guild-skill access as a dead generic-tab fallback, no longer leaves the guild shortcut or dedicated guild-skill owner stuck on stale placeholder-membership or stale-role state after the local social/build context refreshes, no longer reuses `BtRenewal` as a recommendation cycler, no longer collapses every in-guild refresh back to a fabricated Guild Lv. 1, now keeps packet-owned no-guild or alternate-guild UI state from snapping back to the local build name or guild level when the dedicated owner refreshes, now also keeps same-guild packet-owned guild-point and guild-level echoes alive across later local-context refreshes, and now also keeps standalone `OnGuildResult(75)` guild-level echoes authoritative inside the shared social-list UI context even before a dedicated guild-UI packet shows up, so the guild shortcut and dedicated owner stay on the packet-owned level instead of momentarily falling back to the simulator seam. It no longer duplicates the same level-1 guild-skill effect string in both detail slots before a skill is learned, no longer reuses the generic guild-skill description as a fake `Current` effect before a skill is learned, now exposes the current guild-fund, packet-owned guild-point total, pending approval, timed activation state, and separate view-only authority directly in the dedicated tooltip path, no longer formats guild points as meso-valued currency in the dedicated owner summary or tooltip seams, no longer shows guild-point or guild-fund rows when the dedicated owner is in a true no-guild detail state, no longer flattens multiline guild-skill detail text through a whitespace-only wrapper, no longer fills the dedicated owner's WZ-titled `Guild Skill Activation Effects` footer with a simulator-owned guild-name/status block because the footer now uses the authored `sheet1` strip art plus selected-skill current/next/cost-duration text plus packet-owned guild points, no longer leaves the dedicated owner without the client-style `Basic.img/VScr` scrollbar when the guild catalog extends beyond the visible seven-row page, no longer reports a true no-guild tooltip as generic view-only state, and no longer drops the highlighted title treatment after the first wrapped guild-skill tooltip fragment. This run also narrows the packet-context gap by feeding `OnGuildResult(75)` guild points through the shared social-list context into the dedicated guild-skill owner summary and tooltip seams without routing them through the meso formatter, now also keeps an authoritative packet-owned guild-point value of exactly `0` alive across runtime refreshes instead of reviving stale saved totals, now also keeps the paired authoritative guild level from that same packet family alive even when packet authority has not yet emitted a separate guild-UI update, narrows the economy gap by teaching the dedicated owner to spend and persist a simulator-owned guild fund using the WZ-authored learn and renewal price formulas instead of treating those authored costs as read-only text, narrows the server-outcome gap by staging packet-owned guild-skill learn and renewal requests behind the shared guild approval path instead of mutating locally up front, further narrows that outcome gap by letting the same approval seam carry authoritative resulting level, remaining-time, and guild-fund values, and now also reconciles those staged requests when the client only echoes the authoritative result through `CWvsContext::OnGuildResult(81)` skill-record updates instead of the custom `guildskillresult` helper payload. This run also adds a simulator-owned standalone `GuildSkillResultPacketKind.FundSync` packet-helper branch for the dedicated guild-skill owner, so packet/session harnesses can now echo an authoritative guild-fund total into the existing per-guild saved-state ledger without staging a fake `BtUp` / `BtRenewal` approval first; the official-client evidence remains unchanged here, with `CWvsContext::OnGuildResult(75)` carrying guild points plus guild level and `CWvsContext::OnGuildResult(81)` carrying the skill-record echo rather than a guild-fund total. The remaining gap is now concentrated in identifying and wiring any true official client guild-fund or ledger packet owner if one exists outside the checked `OnGuildResult(75)` / `OnGuildResult(81)` paths, any still-missing wire-level client packet/result handlers for guild-skill outcome families beyond the now-handled `OnGuildResult(81)` skill-record and simulator-owned `guildskillresult` / fund-sync helper paths, plus still-unmodeled recommendation edge cases outside the now-covered Dual Blade `400/430` recommendation-root exception. | `SkillUI.cs`, `SkillUIBigBang.cs`, `UIWindowLoader.cs`, `SkillDataLoader.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `GuildSkillWindow.cs`, `GuildSkillTooltipContentBuilder.cs`, `GuildSkillRuntime.cs`, `SocialListPacketCodec.cs`, `SocialListRuntime.cs`, `SocialListRuntime.PacketModel.cs`, `UserInfoUI.cs`, `SocialListWindow.cs`, `UnitTest_MapSimulator/GuildSkillRuntimeParityTests.cs`, `UnitTest_MapSimulator/SocialListGuildSkillUiContextParityTests.cs`, `UnitTest_MapSimulator/SkillBookVisibilityResolverTests.cs` (`CUISkill::Draw`, `CUISkill::GetSkillRootVisible`, `CUISkill::GetRecommendSKill`, `CUISkill::OnButtonClicked`, `CUISkill::OpenSkillGuide`, `CWvsContext::OnGuildResult(75)`, `CWvsContext::OnGuildResult(81)`, `UI/UIWindow2.img/Skill/main/BtRide`, `UI/UIWindow2.img/Skill/main/BtGuildSkill`, `UI/UIWindow2.img/Skill/main/Tab/DualTab`, `UI/UIWindow2.img/Skill/main/Tab/AranButton`, `UI/UIWindow2.img/AranSkillGuide`, `UI/UIWindow2.img/UserList/GuildSkill`, `UI/UIWindow2.img/UserList/GuildSkill/iconMouseOver`, `UI/Basic.img/VScr`, `Skill/9100.img`, `String/Skill.img`) |
  Update for this row: status remains `Partial`. This pass tightens the official packet-owner seam inside the dedicated `UIWindow2.img/UserList/GuildSkill` flow for the confirmed client `CWvsContext::OnGuildResult(82)` and default-result branches. WZ re-check still keeps the dedicated owner anchored to `UIWindow2.img/UserList/GuildSkill` (`backgrnd`, `backgrnd2`, `base`, `skill0`, `skill1`, `recommend/0`, `sheet1/0..2`, `BtRenewal`, `BtUp`) and the guild-skill data/text on `Skill/9100.img/skill/91000000..91000006` plus `String/Skill.img/91000000..91000006`. IDA now backs the simulator decode shape more precisely: `OnGuildResult(82)` decodes a one-byte explicit-notice flag, sends the following packet string to chat when the flag is nonzero, and otherwise falls back to the client's shared `StringPool 0x176` notice; the function's default case also emits that same shared `StringPool 0x176` notice instead of treating unknown result subtypes as a hard decode failure. The social-list packet summary path now keeps both branches on that client notice seam, so unknown guild-result subtypes surface the shared client fallback and update the packet-owned guild summary instead of being labeled as unsupported.
  Focused regression coverage for this official notice family now lives in `UnitTest_MapSimulator/SocialListGuildResultParityTests.cs` for the explicit `OnGuildResult(82)` message, the `OnGuildResult(82)` shared `StringPool 0x176` fallback, and the client default-branch shared-notice fallback for an unknown subtype. The previously narrowed dedicated `UIWindow2.img/UserList/GuildSkill/sheet1` footer text seam for the empty/no-guild branch remains in place through `SkillTooltipClientText`'s `[Current Level %d]` / `[Next Level %d]` entries (`0x2B3` / `0x2B4`) and `UnitTest_MapSimulator/GuildSkillRuntimeParityTests.cs`. Client re-check of `CUISkill::GetRecommendSKill` still shows only the already-modeled beginner Dual Blade `subJob == 1` exception for visible roots `400` / `430` before the generic `Etc/RecommendSkill.img/<root>` table walk, so the recommendation exception gap is narrowed to future data/version drift rather than a currently observed missing branch. The remaining gap for this row is now identifying any true official client guild-fund owner outside the checked `OnGuildResult(75)` / `OnGuildResult(81)` / `OnGuildResult(82)` / default-notice paths, wiring any still-missing guild-skill-specific wire-level packet/result families beyond the handled `OnGuildResult(81)`, `OnGuildResult(82)`, default shared-notice, and simulator-owned `guildskillresult` / `FundSync` seams, and recovering any remaining guild-skill no-guild or state/footer wording that the authored WZ text and current extracted StringPool seam still do not expose directly.
  Update for this row: status remains `Partial`. This pass keeps the dedicated guild-skill owner on the same WZ-backed `UIWindow2.img/UserList/GuildSkill` / `Skill/9100.img` / `String/Skill.img` data seam and tightens another official `CWvsContext::OnGuildResult` branch discovered next to the handled guild-skill record path. IDA re-check of `OnGuildResult(79)` shows the client decodes a channel byte and a four-byte guild-quest queue status, clears the temporary guild notice state when that status is `0`, and otherwise writes one of StringPool `0xDFF`, `0xE00`, or `0xE01` to the status-bar chat log, using the raw channel name for statuses `1` and `2` and `status - 1` for the waitlist position. The simulator now decodes `OnGuildResult(79)` through the existing social-list guild-result packet owner, pins the recovered StringPool text in the shared `MapleStoryStringPool` override seam so the URL-escaped generated table does not leak into UI text, and exposes the same queue-enter, queue-wait, waitlist, and clear-notice summaries through `SocialListGuildResultClientText` / `SocialListRuntime.PacketInbox`. Focused coverage for this branch was added to `UnitTest_MapSimulator/SocialListGuildResultParityTests.cs` alongside the existing `OnGuildResult(82)` and default-notice cases. The remaining gap for this row is now identifying any true official guild-fund or ledger owner outside the checked `OnGuildResult(75)` / `OnGuildResult(79)` / `OnGuildResult(81)` / `OnGuildResult(82)` / default-notice paths, wiring any still-missing guild-skill-specific wire-level packet/result families beyond the handled `OnGuildResult(79)`, `OnGuildResult(81)`, `OnGuildResult(82)`, default shared-notice, and simulator-owned `guildskillresult` / `FundSync` seams, and recovering any remaining guild-skill no-guild or state/footer wording that the authored WZ text and current extracted StringPool seam still do not expose directly.
  Update for this row: status remains `Partial`. This pass tightens the already-modeled packet-owned `BtUp` / `BtRenewal` approval lockout inside `GuildSkillRuntime` rather than adding a new owner. WZ re-check still keeps the dedicated guild-skill owner on `UIWindow2.img/UserList/GuildSkill` (`skill0`, `skill1`, `recommend/0`, `sheet1/0..2`, `BtRenewal`, `BtUp`) with the catalog and formulas coming from `Skill/9100.img/skill/91000000..91000006/common` and labels from `String/Skill.img/91000000..91000006`; `91000000` still carries `price = 160*x*x*x`, `extendPrice = 250+150*x`, `reqGuildLevel = -1+2*x`, `period = 60`, and `priceUnit = 10000`. The pending packet-authority path now short-circuits duplicate manager actions before the local level-up validation can fall through to a misleading cap, fund, or requirement message, so a staged packet-owned level-up or renewal consistently reports the outstanding learn-vs-renew approval and leaves the local level, timer, and guild-fund ledger untouched until the existing approval or authoritative packet-echo seam resolves it. Focused coverage for this WZ-priced pending-action seam now lives in `UnitTest_MapSimulator/GuildSkillRuntimePacketOwnedParityTests.cs`, including duplicate packet-owned level-up clicks and the disabled learn/renew snapshot state while an approval is outstanding. The remaining gap for this row is unchanged in scope but narrower in behavior: identify any true official guild-fund or ledger owner outside the checked `OnGuildResult(75)` / `OnGuildResult(79)` / `OnGuildResult(81)` / `OnGuildResult(82)` / default-notice paths, wire any still-missing guild-skill-specific wire-level packet/result families beyond the handled `OnGuildResult(79)`, `OnGuildResult(81)`, `OnGuildResult(82)`, default shared-notice, and simulator-owned `guildskillresult` / `FundSync` seams, and recover any remaining guild-skill no-guild or state/footer wording that the authored WZ text and current extracted StringPool seam still do not expose directly.
  Update for this row: status remains `Partial`. This pass tightens the same WZ-backed guild-skill loader seam rather than adding another owner. WZ re-check on `Skill/9100.img/skill/91000000/common/maxLevel = 5` and `Skill/9100.img/skill/91000006/common/maxLevel = 10` shows the dedicated guild-skill book publishes its visible cap in `common/maxLevel`, not `info/maxLevel` or a `level/*` branch. `SkillDataLoader` now resolves the displayed max level from the largest authored cap across `info/maxLevel`, `level/*`, and `common/maxLevel`, so the dedicated guild-skill runtime no longer collapses those books to a Lv. 1-only shell when building WZ-derived effect text, learn-cost tables, renewal-cost tables, duration tables, and guild-level requirement thresholds. Focused simulator coverage for this loader seam now exists in `UnitTest_MapSimulator/GuildSkillLoaderParityTests.cs`. The remaining gap is now narrower and stays on the packet- and wording-owned side of the same row: identify any true official guild-fund or ledger owner outside the checked `OnGuildResult(75)` / `OnGuildResult(79)` / `OnGuildResult(81)` / `OnGuildResult(82)` / default-notice paths, wire any still-missing guild-skill-specific wire-level packet/result families beyond the handled `OnGuildResult(79)`, `OnGuildResult(81)`, `OnGuildResult(82)`, default shared-notice, and simulator-owned `guildskillresult` / `FundSync` seams, and recover any remaining guild-skill no-guild or state/footer wording that the authored WZ text and current extracted StringPool seam still do not expose directly.
  Update for this row: status remains `Partial`. This pass tightens one more official guild-result packet branch that sits next to the dedicated guild-skill result seams without adding a new UI owner. WZ re-check still anchors the dedicated guild-skill window to `UIWindow2.img/UserList/GuildSkill` (`backgrnd`, `backgrnd2`, `base`, `skill0`, `skill1`, `recommend/0`, `sheet1/0..2`, `BtRenewal`, `BtUp`) and the catalog/formula text to `Skill/9100.img/skill/91000000/common` plus `String/Skill.img/91000000`. Client re-check of `CWvsContext::OnGuildResult(80)` shows that branch decodes a single packet string, stores it as the guild-board auth key, and does not emit a chat notice, while the adjacent checked guild-skill branch remains `OnGuildResult(81)` for `GUILDDATA::SKILLENTRY::Decode`. The social-list guild-result packet seam now decodes subtype `80`, stores the auth-key state without leaking the key into status text, and marks the guild summary as packet-owned instead of falling back to the default shared-notice path. Focused coverage for this official branch now lives in `UnitTest_MapSimulator/SocialListGuildResultParityTests.cs` for decoding `OnGuildResult(80)` and confirming the runtime records packet-owned auth-key state without exposing the raw key. The remaining gap is now narrower: identify any true official guild-fund or ledger owner outside the checked `OnGuildResult(75)` / `OnGuildResult(79)` / `OnGuildResult(80)` / `OnGuildResult(81)` / `OnGuildResult(82)` / default-notice paths, wire any still-missing guild-skill-specific wire-level packet/result families beyond the handled `OnGuildResult(79)`, `OnGuildResult(80)`, `OnGuildResult(81)`, `OnGuildResult(82)`, default shared-notice, and simulator-owned `guildskillresult` / `FundSync` seams, and recover any remaining guild-skill no-guild or state/footer wording that the authored WZ text and current extracted StringPool seam still do not expose directly.
  Update for this row: status remains `Partial`. This pass tightens the already-modeled `CWvsContext::OnGuildResult(82)` notice decoder in the same social-list packet seam. WZ re-check still anchors the dedicated guild-skill owner to `UIWindow2.img/UserList/GuildSkill` (`backgrnd`, `backgrnd2`, `base`, `skill0`, `skill1`, `recommend/0`, `sheet1/0..2`, `BtRenewal`, `BtUp`) and keeps the catalog/formula text on `Skill/9100.img/skill/91000000/common` plus `String/Skill.img/91000000`. Client re-check shows `OnGuildResult(82)` decodes the one-byte explicit-notice flag and, when that flag is nonzero, immediately calls `DecodeStr`; the simulator no longer accepts a truncated explicit-notice packet as an empty notice, while the flag-clear path still uses the shared `StringPool 0x176` notice and unknown guild-result subtypes still stay on the client default shared-notice branch. Focused coverage in `UnitTest_MapSimulator/SocialListGuildResultParityTests.cs` now covers explicit `OnGuildResult(82)` notice decode, truncated explicit-notice rejection, the shared `StringPool 0x176` fallback, the default shared-notice branch, and the existing `OnGuildResult(80)` auth-key branch. The remaining gap stays narrowed to identifying any true official guild-fund or ledger owner outside the checked `OnGuildResult(75)` / `OnGuildResult(79)` / `OnGuildResult(80)` / `OnGuildResult(81)` / `OnGuildResult(82)` / default-notice paths, wiring any still-missing guild-skill-specific wire-level packet/result families beyond those handled branches plus simulator-owned `guildskillresult` / `FundSync`, and recovering any remaining guild-skill no-guild or state/footer wording that the authored WZ text and current extracted StringPool seam still do not expose directly.
| Partial | Item/skill tooltip behavior | Skill hover tooltips still reuse the client `UIWindow2.img/Skill/main/tip0..tip2` frames in the skill window, quick-slot skill hovers, status-bar buff tray, and status-bar cooldown tray, with the status-bar buff/cooldown owners keeping the icon-led client layout plus live cooldown description and remaining-time text. The shared item tooltip placement seam remains on the WZ-authored `tip0..tip2/origin` anchors for quick-slot, main inventory, trunk, status-bar, and quest log/detail item tooltip surfaces, including the extracted `UI/UIWindow2.img/Skill/main/tip0..tip2/origin` vectors `(195,-25)`, `(-178,-22)`, and `(103,-282)` for the right, left, and lower frame variants. Equip hovers still stay on the richer `UIWindow2.img/ToolTip/Equip` path across the Big Bang equipment window, main inventory equip tab, trunk equip rows, drag comparisons, and dragon/mechanic/android/pet equipment tabs, including the existing authored equip metadata coverage and simulator-owned equip metadata persistence. The shared non-equip resolver now keeps the earlier stack-cap, growth, quest-link, cash schedule, cash equip-level-bypass, cash extension slot-label, recipe, use-level, scroll random-variance, capture, pickup, random-morph, targeted-use, `spec` / `specEx` mob-skill, pet-owned metadata, quick-slot, and quest reward quantity coverage on one seam. The previous pass added another WZ-authored non-equip slice to that resolver without adding a new owner: cash/NPC-style hovers now surface authored `info/npc` links such as `Item/Cash/0545/05450000/info/npc = 9090000` while suppressing duplicate rows when the same NPC is already owned by `spec/npc`; scroll hovers now surface authored `info/tuc` upgrade-slot counts from rows such as `Item/Consume/0204/02046060/info/tuc = 2`; pet/cash hovers now surface authored `info/setItemID` membership from pet rows such as `Item/Pet/5000084.img/info/setItemID = 40`; cash limit hovers now surface authored `info/limitCount` and `info/limitSec` rows such as `Item/Cash/0512/05123000/info/limitCount = 1` and `Item/Cash/0523/05230002/info/limitSec = 1`; and pet hovers now surface authored `info/limitedLife` rows such as `Item/Pet/5000054.img/info/limitedLife = 18000`. This run keeps tightening that same resolver with WZ-authored pendant-slot extension and repeat-buff rows: cash pendant hovers now surface `info/slotIndex` plus `info/addDay` from rows such as `Item/Cash/0555/05550000/info/slotIndex = 0` and `info/addDay = 30`, and repeat buff hovers now surface `spec/repeatEffect` from rows such as `Item/Consume/0202/02022585/spec/repeatEffect = 1`. WZ re-check also keeps the previous evidence for `UI/UIWindow2.img/Skill/main/tip0..tip2/origin`, `Item/Consume/0251/02510045/spec` recipe requirements, `Item/Consume/0204/02040032/info` scroll random-variance fields, `Item/Cash/0559/05590000/info/incLEV`, `Item/Cash/0550/05502000..05502007/info/selectedSlot`, `Item/Install/0301/03010195/info/qid`, `Item/Cash/0521/05210000/info/{rate,time}`, shared `info/slotMax` stack caps, `Item/Cash/0512/05123000..05123002/info/limitCount`, `Item/Cash/0523/05230002/info/limitSec`, `Item/Cash/0555/05550000..05550001/info/{slotIndex,addDay}`, `Item/Consume/0202/02022585..02022588/spec/repeatEffect`, and `Item/Pet/5000054.img`, `5000087.img`, and `5000088.img` `info/limitedLife` values. Full client `ToolTip/Equip` shell framing, remaining bitmap-value branches beyond the current mixed slash-sensitive rows, richer growth-instance progress beyond authored default growth metadata, broader potential/additional-option coverage outside the local equipped-item path, true server- or packet-authored instance expiration parity for arbitrary inventory/storage equips, and fuller client-localized non-equip item-tooltip parity for `info` / `spec` families beyond the currently modeled stack-cap, growth, quest-link, cash schedule, cash equip-level-bypass, cash extension slot labels and add-day extension, recipe requirement, use-level, scroll random-variance / upgrade-slot, info-NPC, pet set-membership, cash limit-count / second-limit, pet limited-life, and repeat-effect branches are still incomplete. | The simulator keeps one shared client-style tooltip surface across the main skill HUDs, quick-slot, status-bar, inventory, trunk, and quest owners, and this pass tightens the shared non-equip resolver with additional WZ-authored metadata rows instead of adding owner-specific tooltip fallbacks. Quick-slot skill hovers continue to use the extracted client `StringPool 0x2B3` `[Current Level %d]` surface, quick-slot item and quest reward-item hovers keep the shared stack-cap and known `Quantity:` footer behavior, cash availability previews remain ordered authored `Available:` rows, and inventory/trunk stack-cap footers remain on the dedicated `Max per Slot:` row. The resolver now also emits linked-NPC rows for `info/npc`, upgrade-slot rows for scroll `info/tuc`, pet set-membership rows for `info/setItemID`, cash use-count / second-limit rows for `info/limitCount` and `info/limitSec`, pet limited-life rows for `info/limitedLife`, pendant-slot extension rows for `info/slotIndex` / `info/addDay`, and repeat-effect rows for `spec/repeatEffect` / `specEx/repeatEffect`, with focused regression coverage in `UnitTest_MapSimulator/InventoryItemMetadataResolverParityTests.cs` covering those branches plus the duplicate info/spec NPC guard. The row stays partial because the remaining work is still the broader client equip shell, server-owned instance fields, bitmap-value branches, richer growth-instance state, potential/additional-option breadth, and long-tail localized non-equip `info` / `spec` formatting outside the modeled families. | `SkillUIBigBang.cs`, `QuickSlotUI.cs`, `StatusBarUI.cs`, `EquipUIBigBang.cs`, `InventoryUI.cs`, `InventoryItemMetadataResolver.cs`, `TrunkUI.cs`, `TrunkUI.Tooltips.cs`, `QuestUI.cs`, `QuestDetailWindow.cs`, `QuestRuntimeManager.cs`, `ItemUpgradeUI.cs`, `CharacterData.cs`, `CharacterLoader.cs`, `UIWindowLoader.cs`, `UILoader.cs`, `MapSimulator.cs`, `StorageAccountStore.cs`, `SimulatorStorageRuntime.cs`, `UnitTest_MapSimulator/InventoryItemMetadataResolverParityTests.cs` |
| Partial | Charge bars and richer skill HUD | The simulator keeps the earlier client-timed prepared-skill gauge set for the supported status-bar branches, including the longer prepare-window `Preparing <n> sec` state, the dedicated release-owned `Release` caption path, SG-88 `35121003` `Amplifying <n>%` / `Amplified` captions on `KeyDownBar4`, Dual Blade `4341002` / `4341003` routing to `UI/Basic.img/KeyDownBar1`, the shared supported-family gauge-duration resolver, the client-cleared no-local-keydown-layer branch for `3121004` / `5221004` / `13111002` / `35001001` / `35101009` / `33121009` while keeping those skills in the held-skill execution path, the Monkey Wave `prepare/time` seam, the shared remote prepare/release tables plus caption rules, the now-modeled release-owned keydown execution/caption branches for `14111006` Poison Bomb and `5201002` Grenade on their client `1000ms` `KeyDownBar` profiles, and this pass's matching release-caption coverage for the non-keydown release-owned `2111002` Fire Arrow / Ice Arrow and `4211001` Assaulter / Shadow Charge families on the same existing caption seam instead of leaving them on the generic ready text. The remote dragon branch still stays inside the existing HUD seam instead of adding a new companion system, but the earlier pass narrowed that branch by tightening the alias-and-selection ownership rather than widening systems: remote HUD metadata already resolved the dragon image through the shared `StringPool 0x1330` path format/fallback seam, enumerated renderable dragon actions through `DragonActionLoader`, and read linked-canvas frame metrics before building the keyed-HUD timeline/origin table; the earlier pass already normalized those loader-enumerated action names through the same client alias seam the loader uses for runtime playback and carried that normalized action name into the cached timeline loop decision, and the same alias seam now carries through remote explicit-action selection as well, so owner aliases such as `walk1` / `jump` collapse fully onto the shared looping `move` surface instead of still being treated as explicit-owner timeline branches after the metadata cache had already normalized them. The earlier real-explicit-action behavior still holds on that tighter seam: when remote packet state carries a real client raw-action code that resolves to an authored explicit dragon action, `22121000` / `22151001` still consume that explicit action before falling back to generic hold `move` / `stand`, those true explicit raw-action branches still measure elapsed time from the remote owner's packet action start tick including same-action replays, and the keyed HUD anchor still follows the authored explicit dragon-frame `origin.x` rather than collapsing every explicit branch back to the older `stand`/`move` pair. WZ/client evidence was rechecked before keeping this branch on the existing seams: `Skill/522.img/skill/5221004` still publishes authored `prepare`, `keydown`, `keydown0`, and `keydownend` skill-animation nodes rather than a dedicated `UI/Basic.img/KeyDownBar*` owner, `Skill/531.img/skill/5311002/prepare/time` still resolves to `1080`, `Skill/1411.img/skill/14111006/prepare/time = 0` and `Skill/520.img/skill/5201002/prepare/time = 0` keep Poison Bomb and Grenade on the client hardcoded `get_max_gauge_time` `1000ms` profile rather than a WZ-authored prepare window, `Skill/Dragon/2215.img/breathe_prepare/0/origin = (103,143)` and `/1/origin = (102,173)` both still carry `delay = 120`, `Skill/Dragon/2212.img/icebreathe_prepare/0/origin = (71,107)` still matches the Ice Breath stand-origin family, `CUserLocal::DoActiveSkill_Prepare` still clears or avoids the local `m_pLayerKeyDownBar` for `3121004`, `5221004`, `13111002`, `22121000`, `22151001`, `35001001`, `35101009`, and `33121009`, `CUserLocal::OnKeyDownSkillEnd` routes `14111006` and `5201002` through release-time skill execution after clamping keydown time through `get_max_gauge_time`, the same client path still routes the dragon-owned `22121000` / `22151001` bars through `CDragon::CreateKeyDownBar`, where the client creates a `72x12` keydown layer and moves it relative to the dragon body by `(-36, rc.top - rc.bottom - 30)`, and `DragonActionLoader` still keeps the client alias table where `stand1` / `stand2` / `alert -> stand` and `walk1` / `walk2` / `jump` / `fly` / `ladder` / `rope -> move`; this keeps the local-vs-dragon keydown-bar owner split intact while removing the last duplicated remote alias branch inside the HUD selector. The earlier remote overlay enumeration drift is also closed inside that same HUD seam: remote dragon-owned prepared skills are no longer filtered out before `TryResolveRemoteDragonKeyDownBarAnchor`, so `22121000` / `22151001` packet-owned prepared states can now reach the shared remote world-overlay draw path while hidden remote actors still stay suppressed. Focused regression coverage for the row now lives in `UnitTest_MapSimulator/PreparedSkillHudProfileParityTests.cs`, covering the client-cleared local profile split, dragon world-overlay profiles, the shipped caption families including the non-keydown release-caption pair, the Poison Bomb/Grenade release-owned keydown split, the longer prepare-window caption, the tightened remote dragon alias-vs-explicit timeline boundary, and the remote dragon overlay enumeration guard. This run narrows remote dragon ownership inside the same HUD seam: remote dragon-owned prepared overlays now retain packet-owned dragon HUD follow state across frames, initialize from the resolved WZ/client dragon body anchor, advance toward subsequent packet-owned target anchors through the same client-shaped active/passive follow constants used by the local dragon runtime, and still snap on large packet discontinuities before applying the existing `72x12` keydown-bar offset. Focused regression coverage now also covers first-anchor initialization, active follow stepping instead of direct owner-frame snapping, and large-discontinuity snapping. This pass also closes the simulator-command drift on the same prepared-HUD resolver seam: `/remoteuser prepare` now routes its locally injected remote prepared state through `PreparedSkillHudRules.ResolveKeyDownSkillState` instead of forcing every command-authored prepared overlay to keydown, so the client-observed non-keydown release-owned `2111002` / `4211001` families stay on their release-caption path even when exercised through the remote HUD command path. Focused coverage now also pins that non-keydown release-owned split plus the SG-88 `KeyDownBar4` amplify caption and the dragon world-overlay no-text profiles. This run closes another WZ/client ownership split inside the same HUD resolver seam: WZ re-check shows `Skill/2312.img/skill/23121000`, `Skill/2412.img/skill/24121000`, `Skill/2412.img/skill/24121005`, `Skill/3100.img/skill/31001000`, `Skill/3110.img/skill/31101000`, `Skill/3111.img/skill/31111005`, and `Skill/572.img/skill/5721001` publish authored `keydown` animation nodes, while client re-check of `is_keydown_skill` keeps them out of the hardcoded local keydown-layer owner table; `PreparedSkillHudRules.ResolveKeyDownSkillState` now preserves those authored animation branches without promoting them to a local keydown HUD layer. Focused coverage now also pins that authored-WZ-keydown / no-client-layer split. The remaining gap is now narrower: unsupported caption edge cases outside these modeled release-owned and client-owned keydown families still remain, and remote dragon rendering/action ownership beyond the HUD anchor still lacks a full packet-owned companion entity, so future client-side dragon-only visual branches outside the current keydown overlay continue to be inferred from the remote owner body frame, raw action code, owner action tick, exported dragon timelines, and the new packet-owned HUD follow state rather than a live remote dragon actor. | Prepared/keydown skills keep the closer client timing and HUD feedback from the earlier parity passes, and the supported release-owned set now includes the client-observed `14111006` and `5201002` keydown branches plus the matching non-keydown `2111002` / `4211001` release-caption behavior instead of leaving those older prepare skills on the generic ready text. Remote dragon-owned overlays also stay closer to client ownership because packet-carried raw owner actions can still drive true explicit dragon timelines while non-explicit owner aliases now collapse all the way onto the shared `stand` / `move` surface instead of lingering on a separate explicit-owner timing branch after alias normalization. The earlier run narrowed the same remote branch by removing that last duplicated alias-vs-selection split inside `RemoteUserActorPool`, while keeping the already-landed metadata-cache normalization and loop-flag normalization on the shared loader seam. It also fixed the remote overlay list ownership so dragon-owned prepared-skill states now remain in the world-overlay enumeration and can consume the existing dragon anchor/timeline resolver instead of being dropped before render. Row-owned regression coverage now includes the supported HUD families, the non-keydown release-caption pair, the release-owned Poison Bomb/Grenade table entries, the SG-88 amplify caption/profile, the dragon world-overlay no-text profiles, the remote dragon overlay enumeration guard, and the WZ-authored-keydown/no-client-layer split for `23121000`, `24121000`, `24121005`, `31001000`, `31101000`, `31111005`, and `5721001` instead of leaving the current behavior pinned only by backlog prose. The row stays partial because only the supported caption families are modeled, and remote dragon behavior now owns HUD-anchor follow state but still reconstructs the broader dragon actor from owner-plus-WZ data rather than a full remote companion runtime. | `PreparedSkillHudRules.cs`, `PreparedSkillHudTextResolver.cs`, `StatusBarUI.cs`, `SkillManager.cs`, `MapSimulator.cs`, `MapSimulator.RemoteUsers.cs`, `RemoteUserActorPool.cs`, `PlayerCharacter.cs`, `DragonCompanionRuntime.cs`, `DragonActionLoader.cs`, `CharacterData.cs`, `UnitTest_MapSimulator/PreparedSkillHudProfileParityTests.cs` (`CUserLocal::DrawKeyDownBar`, `CDragon::CreateKeyDownBar`, `CDragon::DrawKeyDownBar`, `CUserLocal::Update`, `CUserLocal::TryDoingPreparedSkill`, `CUserLocal::DoActiveSkill_Prepare`, `CUserLocal::OnKeyDownSkillEnd`, `StringPool 0x1330`, `is_keydown_skill`, `get_max_gauge_time`, `UI/Basic.img/KeyDownBar*/bar/origin`, `Skill/522.img/skill/5221004`, `Skill/531.img/skill/5311002/prepare/time`, `Skill/1411.img/skill/14111006/prepare/time`, `Skill/520.img/skill/5201002/prepare/time`, `Skill/Dragon/2212.img/icebreathe_prepare/0/origin`, `Skill/Dragon/2215.img/breathe_prepare/0/origin`, `Skill/Dragon/2215.img/breathe_prepare/1/origin`, `Skill/Dragon/<job>.img`, `String/Skill.img/35121003`) |

### 2. Additional quick-slot validation and skill-book recommendation owners discovered by targeted UI decompiles

The latest UI decompiles exposed two owner seams that were still only implied inside the broad status-bar and skill-window rows.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Quick-slot validation parity | WZ re-check still confirms the same owners for this seam: `UI/StatusBar2.img/mainBar/quickSlot/quickSlot` remains the status-bar quick-slot surface and `UI/UIWindow2.img/KeyConfig/quickslotConfig` remains the settings owner for the eight-slot primary bar. The earlier owner work stays in place: `QuickSlotUI` still keeps an eight-slot compare-and-validate snapshot for the primary bar, revalidates only that primary range, and lets stale learned-skill, macro, and item bindings fall out through the shared hotkey validation path before redraw, while `SkillManager` still revalidates live counts, clears empty item bindings instead of leaving zero-count ghosts behind, and restricts quick-slot item bindings to the client-observed consumable and verified cash-item families from `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` (`200`, `201`, `202`, `205`, `210`, `212`, `221`, `226`, `227`, `236`, `238`, `245`, plus the already-verified cash quick-slot families modeled in the simulator inventory flow). Direct IDA re-check now closes the remaining redraw gap on the same owner seam instead of widening it: `CUIStatusBar::Draw` at `0x876cd0` only calls `CQuickSlot::Draw` when `CompareValidateFuncKeyMappedInfo` returns false, while `CUIStatusBar::ReDrawQuickslot` at `0x876ff0` explicitly forces `CompareValidateFuncKeyMappedInfo` plus `Draw` on mutation-driven redraws. `QuickSlotUI` now mirrors that split more closely by keeping binding-state invalidation separate from presentation-surface invalidation: same-frame skill, macro, item, clear, hotkey-event, and drag-swap edits still reset the compare cache so the next visible pass rechecks the primary eight-slot snapshot, but they no longer discard the cached primary render surface when the resulting snapshot settles back to the same validated state before draw. Presentation-affecting changes such as owner wiring, font, key-label, or icon-cache changes still mark the primary surface dirty directly, and live item-count scans still force redraw when `CompareValidateFuncKeyMappedInfo` would return false for the client. | Quick-slot parity now follows the client-owned validate-versus-redraw decision instead of treating every primary-bar mutation as an unconditional surface rebuild. The simulator still clears stale skills and empty item stacks through the shared hotkey validation path, still rejects unsupported quick-slot item families, and now preserves the validated primary quick-slot surface across same-frame no-op or reverted binding edits the same way the client keeps its cached no-redraw path behind `CUIStatusBar::Draw`. | `QuickSlotUI.cs`, `SkillManager.cs`, `MapSimulator.ChatCommands.cs` (`CUIStatusBar::Draw`, `CUIStatusBar::ReDrawQuickslot`, `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo`, `get_cashslot_item_type`, `get_etc_cash_item_type`, `CFuncKeyMappedMan`, `CQuickslotKeyMappedMan`, `CWvsContext::GetCharacterData`, `UI/StatusBar2.img/mainBar/quickSlot/quickSlot`, `UI/UIWindow2.img/KeyConfig/quickslotConfig`) |
| Implemented | Skill-root visibility and recommendation parity | This row now keeps the simulator on the same client-owned skill-root visibility and recommendation seams instead of letting the window predeclare books from static skill-path heuristics. The earlier work remains in place: `SkillDataLoader` still preserves invisible and time-limited skill entries in loaded root data, `SkillUIBigBang` still applies the live `IsSkillVisible`-shaped filter before drawing or counting spent SP so unlearned invisible/time-limited entries stay hidden while learned record-owned entries remain visible, and the recommendation path still loads and sorts the full `Etc/RecommendSkill.img/<skillRoot>` SP-threshold table before the existing Dual Blade `400 + 430` shared-table / combined-SP branch resolves the highlight. This pass closes the remaining live root-refresh gap with the same WZ-first / client-second evidence chain: WZ still owns the root-specific recommend tables and per-skill invisible/time-limited flags, while IDA for `CUISkill::GetSkillRootVisible`, `CSkillInfo::GetSkillRootVisible`, and `CSkillInfo::IsSkillVisible` confirms the client rebuilds the visible root object by copying root metadata and re-filtering the root's live `aSkill` list through the visibility owner on reload. `SkillManager.SkillRecordsChanged` is now the common mutation seam for local level-ups, quest rewards, learned-skill helpers, and authoritative packet-owned snapshots, `MapSimulator` routes that event back into `RefreshVisibleSkillRootsFromCurrentRecords`, and `UIWindowLoader.SkillsAndQuests` now both loads newly visible root objects and resets tabs whose displayed root drops out of the resolved visible-root set before recommendations and live levels are re-applied. That keeps the book list synchronized in both directions: newly learned record-owned roots appear without a full reload, and roots that become invisible again no longer linger as stale tabs because their old level-0 visible skills were still loaded in the window. | Skill-window parity here is about ownership, not just presentation. The client owns both the per-root visibility rebuild and the recommend-skill threshold cache, so mirroring those owners keeps the visible book list and highlight attached to live records and WZ data instead of simulator-side tab guesses. After this run, the Big Bang skill window rebuilds visible roots through one shared live-record refresh seam for both local and packet-owned skill-record changes, refreshes the correct recommendation table for the active root, and actively drops stale roots when they disappear from the client-shaped visible set. | `SkillDataLoader.cs`, `SkillManager.cs`, `SkillUIBigBang.cs`, `SkillRootVisibilityResolver.cs`, `SkillRootRecommendationResolver.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `MapSimulator.cs`, `MapSimulator.PacketOwnedStageTransitionParity.cs` (`CUISkill::GetSkillRootVisible`, `CUISkill::GetRecommendSKill`, `CSkillInfo::GetSkillRootVisible`, `CSkillInfo::IsSkillVisible`, `Skill/000.img/skill/0001014`, `Skill/1000.img/skill/10001015`, `Etc/RecommendSkill.img`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
