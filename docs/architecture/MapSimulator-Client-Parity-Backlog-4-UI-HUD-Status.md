# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. UI parity

- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`

Notes:
`CUIStatusBar::Draw` confirms that name/job-or-level selection, HP/MP/EXP number updates, and quick-slot redraw are part of a single status-bar draw pass, while `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` shows the client also revalidates slot contents against learned skills and inventory state before redraw. `CUISkill::Draw` confirms visible skill-root selection, recommend-skill highlighting, bonus-SP rendering, skill icon row layout, and skill-level text placement.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. UI parity

This area is more advanced than older parity notes suggested, but still far from complete client parity.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Status bar layout | Positions are informed by client analysis, bitmap/WZ-backed rendering exists for gauges and text, the overlay now stays anchored to the composed `StatusBar2.img/mainBar` frame, the Big Bang loader now composes the `mainBar` left cluster and gauge cluster from the client canvas origins and WZ `z` order instead of the older stack-panel approximation so `gaugeCover` no longer ends up underneath `gaugeBackgrd`, the left status cluster now includes the client `lvCover` overlay instead of only `lvBacktrnd`, now anchors both the left cluster and gauge cluster from the composed `backgrnd` frame origin plus the resolved `lvBacktrnd` / `gaugeBackgrd` canvas origins instead of leaving the gauge text on a simulator-side base offset, and now also anchors the Big Bang right-side `BtCashShop`, `BtMTS`/`BtNPT`, `BtMenu`, `BtSystem`, and `BtChannel` controls from the same `mainBar/backgrnd` WZ origins instead of the older width-stacked approximation. This run also keeps the rendered status-bar and chat-strip button art on each WZ-authored state origin instead of freezing every button at the `normal/0` origin, so client-published per-state nudges such as `mainBar/BtClaim/pressed/0/origin = (-28,59)` relative to `normal/0/origin = (-28,58)` now land on the pressed frame instead of drawing one pixel too high. The level label now uses the client `mainBar/lvNumber` digits and follows the exact `CUIStatusBar::SetStatusValue` one-digit/two-digit/three-digit X slots (`45`, `39`, `33`) instead of width-centering, the left cluster's level/job/name baseline now matches the client `SetStatusValue` Y slots more closely, the name shadow now follows the client `SetStatusValue` diagonal passes at `(74,560)`, `(76,560)`, `(74,562)`, `(76,562)` around the white `(75,561)` main draw more closely, the status bar now renders those left-side text labels from the simulator's chat/basic-font path instead of the older debug overlay font so the name/job baselines and glyph widths track the client branch more closely, and those Big Bang left-side labels now also route both measurement and raster through a shared GDI-backed client-text cache instead of raw XNA `SpriteFont` glyphs so width-fitting, clipping, and the black/white `FONT_SMALL_*` passes land closer to the client-owned text branch. That shared raster path now uses a stricter GDI `TextRenderer` single-bit pass, trims cached textures to ink bounds before reuse while preserving the original layout extents for measurement and width-fit decisions, resolves the small-font face through Maple-like installed-family candidates instead of hard-wiring `Segoe UI`, defaults its shared small-font height to the client-observed `get_basic_font(FONT_SMALL_WHITE/BLACK)` 12-pixel branch instead of the earlier simulator-side 13-pixel guess, still accepts a private font override through `MAPSIM_CLIENT_TEXT_FONT_PATH` / `MAPSIM_CLIENT_TEXT_FONT_FACE`, now accepts that override as either a direct font file or a directory to probe, auto-scans nearby runtime roots plus their ancestor `Fonts` / `Content` / `Content\\Fonts` subtrees for Maple-like Dotum/Gulim/Batang font files before falling back to installed families, and now also searches registry-discovered Maple install roots for both named and arbitrary `.ttf` / `.ttc` / `.otf` payloads, ranking them by Maple-like file-name and install-path hints so a bundled client-adjacent font file can win even when its filename is not one of the older hard-coded Dotum/Gulim candidates. It still prefers the client-observed DotumChe/DotoomChe face family when a discovered TTC/TTF exposes multiple Maple-like families instead of accepting whichever face the collection returns first. The same client-text cache now bakes the requested glyph color into the GDI raster itself instead of drawing a white atlas and tinting it later in XNA, which keeps the status-bar and chat-strip `FONT_SMALL_WHITE` / `FONT_SMALL_BLACK` palette branches closer to the client's own font-object color and alpha path. This run also makes the status-bar and Big Bang chat-strip owners prefer Maple client embedded font payloads when their local client-text rasterizers pick a private face, and keeps the status-bar label plus tooltip draw passes on that same owner-local rasterizer instead of leaking back to the broader shared cache after width-fit and clip decisions were already made. The status-bar text draws and bitmap-number placement remain snapped onto integer pixel positions so the client-derived `SetStatusValue` / `SetNumberValue` slots are no longer blurred by XNA subpixel placement, the HP/MP/EXP readouts still right-align against the exact client-derived `SetNumberValue` anchor slots (`163,4`, `332,4`, `332,20`) after the WZ-resolved `gaugeBackgrd` offset is applied, the EXP label shows live raw EXP plus percentage instead of only a percentage stub, and the job label still collapses hierarchical names to the client-facing title slot, renders in a dim-green narrower fit instead of the older generic yellow string, no longer applies the generic four-way shadow pass that `CUIStatusBar::SetStatusValue` reserves for the character name, and now keeps both the job and name on a sanitized single-line client-text draw path that clips directly against the WZ-resolved `lvBacktrnd` / `lvCover` cluster width instead of swapping in a simulator-owned ellipsis treatment when long labels overflow. This run also moves the left-cluster draw path fully onto the rasterizer-side clip branch whenever a live graphics device is present, routes the status-bar tooltip wrap/width seam, tooltip line spacing, buff/cooldown counter placement, prepared-skill caption placement, and the non-bitmap fallback right-alignment seam through that same live client-text measurement path instead of stale `SpriteFont` metrics when the GDI rasterizer is active, factors the level-slot/name/job formatting rules into a dedicated shared helper with focused parity coverage, and anchors the Big Bang AP/SP point-notification animations from the authored `mainBar/backgrnd` origin instead of accidentally inheriting the shorter `chatSpace2` frame origin. The Big Bang chat-strip branch composes `chatSpace`, `chatSpace2`, `chatCover`, and `notice` from the client canvas origins and WZ `z` order, now also places that composed strip itself from the authored `mainBar/backgrnd -> chatSpace2` origin delta instead of a simulator-owned viewport-bottom `-36` offset, positions its chat-target/chat-toggle/scroll/status buttons from the same `mainBar` button origins instead of the older width-stacked approximation, aligns the target badge and `chatEnter` field from the WZ origins that the client canvases publish, derives the chat input X start from the resolved `chatTarget/base` button width plus the `chatEnter` field origin instead of the older fixed `+70` delta, now derives the visible chat-log wrap width and wheel-scroll hit region from the resolved `chatEnter` / `chatSpace2` right edge plus the composed strip bounds instead of the older local `452`-pixel or padding approximations, and now also uses the client `CUIStatusBar::ChatLogDraw` left text slot (`9`) for the Big Bang chat-log inset instead of the older simulator-side `4`-pixel lane while recomputing the wheel-scroll interaction height from the live client-text line spacing rather than the older one-time 14-pixel loader guess. It still routes visible chat-strip row/input measurement plus raster through that same client-text cache so wrap decisions, caret placement, whisper-prefix hit boxes, and the visible local-input window track the authored field more closely than the older pure `SpriteFont` path. The chat-log wrap seam now also follows `CUIStatusBar::ChatLogAdd` more closely by mirroring the client `IWzFont::CalcLongestText` longest-prefix fit, the special first-line `-38` width reduction for chat types `14`, `16`, `19`, and `20`, the trim-left remainder cleanup between wrapped rows, and the five-space continuation indent that the client reapplies outside chat types `7..12` instead of using the older simulator-side word-splitting approximation. This run also trims the shared wrap buffer on the same outer-left and outer-right seams the client loop uses before its `CalcLongestText` passes, removes the owner-local duplicate no-indent splitter so every visible Big Bang chat row now flows through the same client-shaped wrapper, and aligns the shared retained chat-log ceiling with the client `0x40` wrapped-entry limit instead of the older simulator-side `50`-entry constant. The shared font resolver now also probes Maple client executables and DLLs for embedded `RT_RCDATA` / `RT_FONT` sfnt payloads, can register a bundled TrueType/OpenType face directly from the client binary when no sidecar TTC/TTF is available, and now also extracts embedded sfnt payloads when the resource bytes are wrapper-prefixed or zlib/gzip-compressed before the font header so those module-owned faces can still enter the same private-font registration path. It now also walks nearby runtime-root ancestors through that same client-binary container search instead of only registry-discovered Maple installs, so a local client-adjacent `MapleStory.exe` or `MapleStory.dll` can feed the status-bar/chat-strip rasterizer even when the install is not registered. The status-bar and Big Bang chat-strip text owners also no longer hard-stop on the fallback `SpriteFont` when their owner-local GDI rasterizer is already live, so those client-text passes can keep rendering, measuring, clipping, and hover-hit sizing off the private-face rasterizer alone. Remaining gap is now narrower and limited to reproducing the exact client-bundled `IWzFont` face data and any remaining COM-side glyph shaping, hinting, kerning, or client-specific decompression/encryption nuances byte-for-byte instead of approximating them through embedded or discovered Dotum/Gulim/Batang bytes plus GDI `TextRenderer`, so the simulator can still miss the live client's final glyph shape when the relevant font is encrypted or packaged in an unsupported proprietary container, or where local font bytes and native runtime shaping still differ from the live client even though the embedded-font preference, default face selection, private-face preference, search coverage, embedded-resource fallback, wrapped/compressed resource extraction, nearby runtime-root container probing, rasterizer-owned fallback independence, layout-width measurement, palette routing, ink bounds, overflow behavior, authored last-glyph field clipping, authored tooltip wrap widths, tooltip line spacing, buff/cooldown counter placement, prepared-skill caption placement, authored AP/SP notifier placement, authored chat-field extents, the main-bar-derived strip lane, client-shaped chat-log line splitting, the client `ChatLogDraw` text inset, the live-height interaction region, and the pressed-state button origin deltas plus the wrapped-entry retention cap are materially closer. | The status bar now uses the correct WZ layering for both the main bar and the Big Bang chat strip, both major overlay clusters and the right-side main-bar buttons are anchored from the same composed `mainBar` frame the client art uses, the gauge readouts land on the exact `SetNumberValue` slot family after applying the WZ gauge-cluster offset, and the left-side name/job overflow path now clips through the shared client-text raster instead of appending a simulator-owned `...`. This run also makes the left-side name shadow match the client `SetStatusValue` diagonal corner passes instead of the older generic cross-outline helper, keeps left-label overflow on rasterizer-side pixel clipping rather than whole-character pre-trim when a live device is present, makes the shared text seam pick a closer private DotumChe-style face when a registered Maple-like TTC exposes multiple families, search higher runtime ancestors for nearby font payloads automatically, keep wrap or clipping decisions on the original `TextRenderer` layout box even though the cached texture is trimmed to visible ink, scan registry-discovered Maple install roots for arbitrary font payloads instead of only a small fixed filename list, probe top-level Maple client binaries plus nearby runtime-root ancestors for embedded sfnt resources when no sidecar file is present, keep the status-bar tooltip, left-label, non-bitmap fallback draw, buff/cooldown counter, prepared-skill caption, chat input, whisper-target hit testing, and shortcut-tooltip passes on the same owner-local embedded-font-preferred rasterizer without requiring the older fallback `SpriteFont` gate first, correct the AP/SP notifier anchor back to the authored `mainBar/backgrnd` frame instead of the chat-strip frame, replace the Big Bang chat-strip container's old viewport-bottom `-36` placement fudge with the same authored `mainBar/backgrnd -> chatSpace2` origin delta that owns the rest of the strip art, lock the client label-formatting plus level-slot rules behind focused unit coverage instead of leaving them buried as private UI-only arithmetic, keep pressed-state status-bar/chat-strip buttons on their authored WZ state origins instead of drawing every state at the normal-state anchor, and now also keep Big Bang chat-log row splitting plus retention on the same client-shaped `ChatLogAdd` seam the live UI uses instead of a simulator-owned word wrapper and older `50`-entry cap. It now also uses the client `ChatLogDraw` `nLeft=9` chat-log inset instead of the older local `4`-pixel gutter and keeps the chat wheel-hit region synchronized with the live client-text line height rather than the loader's fixed 14-pixel fallback, so the Big Bang strip no longer reuses a stale interaction box when the active GDI rasterizer changes row height. The row is still partial because the simulator still approximates the client's final `IWzFont` objects through locally discovered or extracted Dotum/Gulim/Batang plus GDI rather than reproducing the bundled font data and final COM-side shaping/hinting byte-for-byte, but the remaining work is now narrower than missing anchors, missing art layers, the old 13-pixel small-font guess, generic `Segoe UI` metrics, a file-only font override path, the older first-face private-font fallback, the old ellipsis-based overflow behavior, the older cardinal-outline name shadow, the older whole-character pre-trim at the field edge, the earlier tooltip-wrap, tooltip line-height, prepared-skill caption, buff-timer, and fallback-number placement mismatches against the active client-text rasterizer, the leftover fixed chat-strip width/interaction box assumptions, the earlier inability to notice install-local Maple font files whose filenames did not already match the hard-coded candidate list, the earlier inability to use an uncompressed font payload that only shipped inside the Maple client executable, the earlier inability to find that same embedded payload when the client sat beside the simulator instead of in a registered install, the older fallback-font gate that could still suppress client-text-owned status-bar/chat-strip passes even after the private rasterizer was available, the earlier simulator-side chat word wrapping that ignored the client's trim-left and continuation-indent behavior, or the older simulator-side wrapped-entry history cap that trimmed earlier than the client. | `StatusBarUI.cs`, `StatusBarLayoutRules.cs`, `StatusBarChatUI.cs`, `StatusBarChatLayoutRules.cs`, `ClientTextRasterizer.cs`, `UILoader.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/ClientTextRasterizerParityTests.cs`, `UnitTest_MapSimulator/StatusBarButtonStateLayoutParityTests.cs` (`CUIStatusBar::Draw`, `CUIStatusBar::SetStatusValue`, `CUIStatusBar::SetNumberValue`, `CUIStatusBar::ChatLogDraw`, `CUIStatusBar::ChatLogAdd`, `get_basic_font`, `UI/StatusBar2.img/mainBar/backgrnd`, `UI/StatusBar2.img/mainBar/lvNumber`, `UI/StatusBar2.img/mainBar/lvCover`, `UI/StatusBar2.img/mainBar/gaugeBackgrd`, `UI/StatusBar2.img/mainBar/gaugeCover`, `UI/StatusBar2.img/mainBar/BtCashShop`, `UI/StatusBar2.img/mainBar/BtMTS`, `UI/StatusBar2.img/mainBar/BtMenu`, `UI/StatusBar2.img/mainBar/BtSystem`, `UI/StatusBar2.img/mainBar/BtChannel`, `UI/StatusBar2.img/mainBar/chatSpace`, `UI/StatusBar2.img/mainBar/chatSpace2`, `UI/StatusBar2.img/mainBar/chatCover`, `UI/StatusBar2.img/mainBar/chatEnter`, `UI/StatusBar2.img/mainBar/chatTarget`, `UI/StatusBar2.img/mainBar/chatOpen`, `UI/StatusBar2.img/mainBar/chatClose`, `UI/StatusBar2.img/mainBar/ApNotify`, `UI/StatusBar2.img/mainBar/SpNotify`) |
| Partial | Skill UI layout | Row height, positions, hit boxes, SP placement, quick-slot assignment wiring, and a client-backed `Basic.img/VScr` scrollbar at the confirmed `153,93,155` placement are now present, with both skill-window variants reusing the client `tip0..tip2` frames for the fixed-size SP and hint balloons, the scrollbar supporting arrow clicks, track paging, thumb dragging, and wheel-over-scrollbar input, the row list drawing the client `Skill/main/recommend/0` banner from `Etc/RecommendSkill.img/<skillRootId>` SP-threshold table at the client `47,nTop-19` overlay offset plus the existing visible-tab levelable-skill fallback, the book header following the client width-driven two-line wrap path, the visible and inactive skill-book tabs drawing and hit-testing from their authored `UIWindow2.img/Skill/main/Tab/*` origins and heights, the `BtSpUp` control using its normal/hover/pressed/disabled states with WZ-backed `req` / `reqLevel` gates, and the local SP recompute seam deriving each visible book's pool from the WZ-backed book-open thresholds plus spent levels instead of fabricating points from every unfilled max level. The shared hover tooltip path already keeps the client `320`-pixel body width, the `x=10/10,32/87` title-icon-text column layout, the live-mouse `+20` Y anchor, and the dedicated prerequisite footer, which now resolves its title and level caption through the client `StringPool` ids used by `CUIToolTip::DrawReqSkill` (`0x801` / `0x800`) instead of staying on simulator-only literals. That same footer now also uses the client requirement-row fill color (`0xA0FFFFFF`) rather than the older simulator-owned red swatch, while preserved `#c...#` formatting markers and WZ-property-order fallback stat text still cover unauthored `h*` rows, including `itemCon` / `itemConNo`, `itemConsume`, `bulletConsume`, damage-over-time fields, `damagebymoneyCon`, `massSpell`, `magicSteal`, `fixdamage`, `selfDestruction`, `subProp`, `subTime`, `reqGuildLevel`, `ar`, `er`, `psdSpeed`, and `psdJump`. This run further narrows that fallback seam by keeping otherwise-unmodeled `level` / `common` properties in their authored WZ order when no authored `h*` text exists, so raw-key lines like `u`, `v`, or `actionSpeed` no longer disappear entirely while metadata-only keys such as `maxLevel` and `priceUnit` stay suppressed. This run also promotes another small class of fallback rows onto client-facing wording by formatting WZ-authored `actionSpeed` values as `Attack Speed` levels and by renaming `padX` / `madX` to the same `Weapon ATT` / `Magic ATT` phrasing that authored `String/Skill.img` `h*` lines already use. This pass also keeps the dedicated guild-skill owner improvements in the existing seams: the local owner now persists learned levels, remaining local SP, guild fund, and active renewal expirations per guild identity instead of one shared save bucket, so switching between no-guild, guild A, and guild B contexts no longer cross-pollinates local guild-skill state, and the dedicated guild-skill hover detail now uses the authored `UIWindow2.img/UserList/GuildSkill/base` width (`244`) as its bounded text surface, reuses that same `base` canvas as the tooltip shell instead of stretching the broader window frame texture, preserves embedded line breaks and blank spacer rows, wraps title and body text at their actual draw scales, and clamps the tooltip to the viewport instead of letting the older generic box overflow off the window edge. The `sheet1` footer path remains on strip-width-aware wrapping with clipped draw bounds so long current/next/cost summaries stay anchored to the loaded strip geometry. Remaining layout parity is now narrower and concentrated in the original client's exact `IWzFont` glyph raster and palette, the last micro-offset or line-break differences on row or tooltip text outside those string-pool-backed requirement captions, the still-unrecovered client-side labels for some single-letter fallback stat keys that remain outside the explicit generator map, and the absence of true packet- or login-authored per-book SP, guild-fund, contribution, renewal, and ledger/session ownership where the simulator still has to derive or persist those values locally. | The window is materially closer to the client than the older row implied: recommendation overlays, row and tab geometry, hover-skill requirements, fallback stat coverage, and local SP affordances already live in WZ/client-backed seams, and the remaining gap is now mostly the final font-object and authoritative-session polish rather than structural layout misses. This run further narrows the shared hover-skill side of the row by keeping no-string fallback descriptions from silently dropping WZ-authored stat properties, while also moving the recovered `actionSpeed`, `padX`, and `madX` rows onto the same client-facing `Attack Speed`, `Weapon ATT`, and `Magic ATT` wording that authored `String/Skill.img` descriptions already use instead of leaving those fallback rows on generic simulator labels. The dedicated guild-skill side remains improved from the existing local-owner and bounded-tooltip work, and the row stays partial because the simulator still lacks true packet-owned guild contribution or ledger feeders, real server success or failure outcomes for learn and renewal actions, true authoritative renewal expiry and level-up ownership, and the final client `IWzFont` rendering path. | `SkillUI.cs`, `SkillUIBigBang.cs`, `GuildSkillWindow.cs`, `GuildSkillRuntime.cs`, `SkillDataLoader.cs`, `UIWindowLoader.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/GuildSkillRuntimeTests.cs`, `UnitTest_MapSimulator/SkillDataLoaderFallbackStatTests.cs` (`CUISkill::Draw`, `CUISkill::OnMouseMove`, `CUIToolTip::SetToolTip_Skill`, `CUIToolTip::DrawReqSkill`, `CUISkill::GetRecommendSKill`, `Etc/RecommendSkill.img`, `Skill/*/skill/*/req`, `UI/UIWindow2.img/Skill/main/Tab`, `UI/UIWindow2.img/Skill/main/recommend`, `UI/UIWindow2.img/Skill/main/BtSpUp`, `UI/UIWindow2.img/Skill/main/tip0`, `UI/UIWindow2.img/Skill/main/tip1`, `UI/UIWindow2.img/Skill/main/tip2`, `UI/UIWindow2.img/UserList/GuildSkill`, `UI/UIWindow2.img/UserList/GuildSkill/base`, `UI/UIWindow2.img/UserList/GuildSkill/sheet1`, `UI/Basic.img/VScr`, `String/Skill.img`, `StringPool 0x800`, `StringPool 0x801`) |
| Implemented | Quick slot cooldown overlay | Cooldown masking now uses the client `UIWindow2.img/Skill/main/CoolTime` frame family, remaining-seconds countdown text renders on assigned quick-slot skills, and the stepped mask holds the initial full-overlay frame until the next visible whole-second boundary instead of advancing immediately after cast | The core client-facing quick-slot cooldown feedback is now present on the simulator HUD | `QuickSlotUI.cs` (`CUIStatusBar::CQuickSlot::Draw`) |
| Implemented | Quick-slot validation and quantity sync | The simulator now rejects passive, hidden, and unlearned skills at hotkey assignment time, restores saved quick-slot bindings through the same validation path after preset load instead of trusting raw slot data, eagerly prunes stale skill slots as soon as learned levels change, cancels an in-flight quick-slot drag if its source binding becomes invalid before the next redraw, and now mirrors the client's `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` inventory-backed recount for owned `USE` / `CASH` quick-slot item entries so stack counts redraw live and empty item slots clear as soon as their backing inventory runs out | Quick-slot contents now stay synchronized with both learned-skill state and live inventory stacks, so the HUD no longer keeps stale consumable or cash-item entries after crafting, pickup, preset restore, or manual reassignment | `QuickSlotUI.cs`, `SkillManager.cs`, `CharacterConfig.cs`, `MapSimulator.cs` (`CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo`) |
| Implemented | HP/MP warning flash | HP/MP gauge warnings now replay the client `aniHPGauge` / `aniMPGauge` and `hpFlash` / `mpFlash` assets for 500 ms when the resource drops further while already below the configured threshold, and the simulator exposes live `/hpwarn` + `/mpwarn` commands to adjust those thresholds in-session | The client-visible low-resource cue is present across both modern and pre-BigBang status-bar loaders, and the trigger point is no longer hardcoded at 20% | `StatusBarUI.cs`, `UILoader.cs`, `MapSimulator.cs` (`CUIStatusBar::Draw`, `CUIStatusBar::SetNumberValue`) |
| Partial | Chat log / whisper flows | The Big Bang status-bar chat strip still keeps the earlier parity work in place: wrapped recent chat lines now follow the client `ChatLogAdd` continuation-indent rules, apply the observed first-line width reduction for `lType 14/16/19/20`, cap visible wrapped history at the client `0x40` entry ceiling, keep manual scroll position until the `_RefreshChatLog` idle snap-back window expires, honor WZ `chatTarget/*/origin` deltas when drawing the target badge, and let both strip clicks and `/w` or `/r` arm whisper mode through the tighter client-shaped `ExtractCharacterName` plus `ChangeWhisperTarget` validation seam confirmed in IDA. The WZ-backed whisper picker still uses `UIWindow2.img/UtilDlgEx/list4` and `list5`, keeps the full remembered candidate history scrollable, pages modal `BtPrev` and `BtNext` by the visible six-row window, restores the preserved draft on `Escape`, confirms on row click or `Enter`, and keeps strip-row confirmation on the active picker presentation path instead of transiently reopening the inline picker. This run narrows the remaining modal-owner gap further with WZ-backed layout and client-string wiring: the inline picker still keeps at least the authored `list4/list5` row width, the modal picker still respects the authored `UIWindow2.img/UtilDlgEx/{t,c,s}` frame width (`519`), the modal `ChangeWhisperTarget` owner still draws its editable combo row and remembered-history list on separate client-shaped bounds, and the combo row still stays on the client bottom-anchor seam (`left = 21`, `width = 222`, `bottom offset = 63`) after the divider-width centering rule is applied. The remembered-history lane now also resolves each slot through the shared modal-row helper across the full six-row window, so the client-shaped list height stays stable even when fewer than six remembered candidates are visible, the first remembered entry starts at the list top instead of leaving an inline-style blank row, and the sixth visible row stays inside the authored list height instead of spilling a row-height past it. The modal title now resolves through the same client `StringPool` id (`0x31E`) that `CUIStatusBar::ChangeWhisperTarget` passes into `CUtilDlgEx::SetUtilDlgEx`, the validation feedback now routes the invalid-name and self-target notices through the client `StringPool` ids (`0x31F`, `0x320`) that the same IDA-confirmed seam logs after `is_valid_character_name` and local-player checks instead of staying on simulator-owned literals, and the modal `BtPrev` / `BtNext` owner now also consumes the authored `UtilDlgEx` disabled canvases so paging only stays clickable when another six-row window exists. Focused simulator tests now exist in `UnitTest_MapSimulator/StatusBarChatWhisperPickerTests.cs` and `UnitTest_MapSimulator/StatusBarChatLayoutRulesTests.cs`, covering the client string-pool validation text plus the modal visible-row anchoring and page-availability helpers in addition to the earlier wrap, validation, and picker-navigation seams. Remaining gap is now narrower and concentrated in the exact client chat-log font objects plus any remaining font-routing details, the exact combo-box edit/history semantics and remaining hover/key-focus or fine placement details inside the modal `ChangeWhisperTarget` owner beyond this row-window and disabled-button wiring, and broader packet-owned incoming chat ownership outside the already modeled whisper, Messenger, field-feedback, local-utility, summoned, local-overlay, MapleTV, packet-script, and quest-result families. | The status-bar chat surface is materially closer to the client's visible log and whisper flow than before: older lines no longer require button-only review, visible whisperable sender prefixes can arm a new target directly from the strip, the chat-target badge honors the authored WZ origin deltas, modeled client `lType` rows keep the simulator's client palette instead of caller-provided fallback colors, and packet-shaped whisper or messenger lines no longer collapse back to a generic visible-strip treatment. This run further reduces visual drift by keeping the modal whisper-owner frame at the authored `UtilDlgEx` shell width, keeping the inline picker at or above the authored row-art width, keeping the modal edit row separate from the remembered-history lane, holding the remembered-history lane on a fixed six-row client window instead of shrinking with the candidate count, and routing both modal validation and page-button availability through the same client-backed string-pool and WZ-button seams the IDA-confirmed `ChangeWhisperTarget` branch implies. The row stays partial because the simulator still approximates the client's final font objects, still does not mirror the exact modal combo-box text-entry/history behavior and every hover/key-focus or fine placement detail inside `ChangeWhisperTarget`, and still lacks broader packet-owned incoming chat ownership outside the already modeled feedback families. | `MapSimulatorChat.cs`, `StatusBarChatUI.cs`, `StatusBarChatLayoutRules.cs`, `UILoader.cs`, `MapSimulator.Content.cs`, `MapSimulator.PacketOwnedFieldFeedbackParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapleTvRuntime.cs`, `MessengerRuntime.cs`, `UnitTest_MapSimulator/StatusBarChatWhisperPickerTests.cs`, `UnitTest_MapSimulator/StatusBarChatLayoutRulesTests.cs` (`CUIStatusBar::ChatLogAdd`, `CUIStatusBar::_RefreshChatLog`, `CUIStatusBar::TryBeginWhisper`, `CUIStatusBar::ChangeWhisperTarget`, `is_valid_character_name`, `StringPool 0x31E`, `StringPool 0x31F`, `StringPool 0x320`, `CField::OnWhisper`, `UI/StatusBar2.img/mainBar/chatTarget/*`, `UI/UIWindow2.img/UtilDlgEx/{t,c,s,bar,line,BtPrev,BtNext,BtOK,BtClose,list4,list5}`) |
| Partial | Full skill UI behavior | The Big Bang skill window now mirrors `CUISkill::GetSkillRootVisible` at the tab layer by only exposing the visible skill-book path for the current job, keeps keyboard-selected entries in view while accepting pane-hover wheel scrolling plus `Up` / `Down` / `PageUp` / `PageDown` / `Home` / `End` navigation, lets `Left` / `Right` cycle across the currently visible skill-book tabs without reaching for the mouse, renders the client recommendation banner from a WZ-loaded skill-root/SP-threshold recommendation table with the existing visible-tab levelable-skill fallback when no applicable entry can level, and now also tracks the displayed skill root per tab so the `CUISkill::GetRecommendSKill` branch for Dual Blade's early `400/430` books follows the client more closely by resolving threshold progress from the combined learned levels across those two visible roots while sourcing the recommendation table from the `430` root instead of treating each tab as a fully isolated recommendation bucket. It routes `BtSpUp` clicks into live skill-level increments with per-tab SP countdown sourced from the remaining unmaxed levels in the loaded book, loads the Aran-only `Skill/main/Tab/AranButton/Bt1..Bt4` guide buttons so the client `3001..3004 -> OpenSkillGuide` path opens the matching `UIWindow2.img/AranSkillGuide/<grade-1>` overlay page, instantiates the WZ-backed bottom-row `BtRide` / `BtGuildSkill` controls, now loads the WZ-authored `Skill/main/Tab/DualTab` strip so Dual Blade jobs walk the client book path across `0, 400, 430, 431, 432, 433, 434` instead of collapsing their extra books onto the standard five-tab branch, and now also opens a dedicated WZ-backed `UIWindow2.img/UserList/GuildSkill` owner from both the skill-window guild shortcut and the guild member-list `BtSkill` action, loading the visible guild-skill catalog from `Skill/9100.img` plus `String/Skill.img` with the row recommendation banner, `BtRenewal`, and `BtUp` actions gated by the parsed `reqGuildLevel` formulas. The dedicated guild-skill runtime now suppresses recommendation cycling, treats `BtRenewal` as a local timed guild-skill renewal action backed by `Skill/9100.img/common/{extendPrice,period,priceUnit}` instead of as a recommendation toggle, persists local learned levels plus active-renewal timers and a simulator-owned guild-fund ledger across placeholder membership loss and restoration, preserves a simulator-owned fallback guild level instead of resetting every in-guild refresh to Guild Lv. 1, refreshes both the skill-window guild shortcut state and the dedicated guild-skill owner from the current build plus social authority context instead of only at initial wire-up, now also applies the resolved packet-owned guild UI context directly to the dedicated owner runtime so packet-overridden membership, guild name, guild level, authority state, and packet-owned `OnGuildResult(75)` guild points no longer fall back to the local build seam between refreshes, keeps both the skill-window guild shortcut and the member-list `BtSkill` route disabled until the build exposes a real non-placeholder guild name, preserves the local Master vs. Jr. Master vs. Member role already surfaced in the social list across runtime refreshes so `BtRenewal` / `BtUp` stay view-only for non-manager roles instead of silently re-promoting the local shell back to full authority, and now also gates both actions against the WZ-authored learn and renewal meso costs so the dedicated owner spends from a local guild-fund pool instead of letting every manager action succeed for free. When that guild authority path is packet-owned, the dedicated runtime now stages `BtRenewal` and `BtUp` as pending requests instead of mutating the local shell immediately, feeds the pending learn-vs-renew label into the dedicated summary and hover detail seams, blocks duplicate manager actions while approval is outstanding, and resolves the staged result through the existing `/sociallist packet resolve guild approve|reject` path so packet-authority scenarios can now model an approve-vs-reject round trip instead of always auto-succeeding locally. That resolve seam now also accepts authoritative `level=<n>`, `remain=<minutes>`, and `fund=<mesos>` outcome tokens for pending guild-skill approvals, letting the dedicated runtime mirror packet-owned learn and renewal results with server-shaped skill levels, renewal timers, and post-action guild-fund totals instead of always deriving those values from the local cost-and-duration shell after approval. It also surfaces `Skill/9100.img/common/{price,extendPrice,period,priceUnit}` plus `String/Skill.img/9100000x/h` in the dedicated owner so hover or selection details show the authored current/next effect text, learn cost, renewal cost, duration, local remaining renewal time, current fund state, the pending-action state when packet approval is outstanding, and the packet-owned guild point total carried by `CWvsContext::OnGuildResult(75)`, with packet-owned guild points now staying labeled as guild points instead of being formatted through the meso path and true no-guild detail states no longer leaking the simulator-owned guild-fund or guild-point rows. Unlearned guild skills no longer duplicate the same level-1 WZ effect text as both `Current` and `Next`; the dedicated owner now also loads and draws `UIWindow2.img/UserList/GuildSkill/sheet1/0..2` so the footer uses the client-authored activation-effect strips instead of a simulator rectangle, and the selection summary feeding that footer now follows the effect-focused WZ data rather than reusing a generic guild-name/SP status block. This run also makes the footer-summary text flow follow the authored strips more closely by wrapping into the available strip width/height and vertically centering line placement against the loaded `sheet1` row textures instead of relying on a fixed single-line truncation seam, while `Up` / `Down` / `PageUp` / `PageDown` / `Home` / `End` can move the guild-skill selection without reaching back to the mouse and `PageUp` / `PageDown` now step by the visible page size instead of a shorter local jump. The dedicated guild-skill owner now also loads `UI/Basic.img/VScr` and applies the client-style vertical scrollbar seam over the list, so arrow clicks, track paging, and thumb dragging follow the same visible-page scroll state as the existing wheel and keyboard navigation instead of leaving longer guild-skill catalogs mouse-wheel-only. This run further narrows the dedicated-owner detail gap by keeping the generic guild-skill description separate from the authored current-effect slot, explicitly surfacing the simulator-owned guild fund, packet-owned guild points, pending approval vs. active vs. inactive vs. view-only state in the hover detail path, honoring embedded line breaks plus overlong-token wrapping in the dedicated owner tooltip instead of flattening everything through a single whitespace-only wrapper, keeping wrapped tooltip title fragments on the same highlighted title palette and scale instead of demoting every fragment after the first to body text, and labeling the true no-guild detail state separately from generic member view-only mode. | The simulator now respects the client's visible-root job path, no longer leaves the level-up button dead, follows the client-style recommendation progression more closely, now also avoids undercounting Dual Blade `400/430` recommendation thresholds by mirroring the client-owned combined spent-skill-level branch for those first two books, no longer drops the Aran guide or the WZ-backed ride / guild shortcut controls that the Big Bang skill window exposes, no longer flattens Dual Blade books onto the standard tab strip, no longer treats guild-skill access as a dead generic-tab fallback, no longer leaves the guild shortcut or dedicated guild-skill owner stuck on stale placeholder-membership or stale-role state after the local social/build context refreshes, no longer reuses `BtRenewal` as a recommendation cycler, no longer collapses every in-guild refresh back to a fabricated Guild Lv. 1, now keeps packet-owned no-guild or alternate-guild UI state from snapping back to the local build name or guild level when the dedicated owner refreshes, no longer duplicates the same level-1 guild-skill effect string in both detail slots before a skill is learned, no longer reuses the generic guild-skill description as a fake `Current` effect before a skill is learned, now exposes the current guild-fund, packet-owned guild-point total, pending approval, and active vs. inactive vs. view-only owner state directly in the dedicated tooltip path, no longer formats guild points as meso-valued currency in the dedicated owner summary or tooltip seams, no longer shows guild-point or guild-fund rows when the dedicated owner is in a true no-guild detail state, no longer flattens multiline guild-skill detail text through a whitespace-only wrapper, no longer fills the dedicated owner's WZ-titled `Guild Skill Activation Effects` footer with a simulator-owned guild-name/status block because the footer now uses the authored `sheet1` strip art plus selected-skill current/next/cost-duration text plus packet-owned guild points, no longer leaves the dedicated owner without the client-style `Basic.img/VScr` scrollbar when the guild catalog extends beyond the visible seven-row page, no longer reports a true no-guild tooltip as generic view-only state, and no longer drops the highlighted title treatment after the first wrapped guild-skill tooltip fragment. This run also narrows the packet-context gap by feeding `OnGuildResult(75)` guild points through the shared social-list context into the dedicated guild-skill owner summary and tooltip seams without routing them through the meso formatter, narrows the economy gap by teaching the dedicated owner to spend and persist a simulator-owned guild fund using the WZ-authored learn and renewal price formulas instead of treating those authored costs as read-only text, narrows the server-outcome gap by staging packet-owned guild-skill learn and renewal requests behind the shared guild approval path instead of mutating locally up front, further narrows that outcome gap by letting the same approval seam carry authoritative resulting level, remaining-time, and guild-fund values, narrows the footer-layout gap by using strip-constrained wrapping and centered line placement on the loaded `sheet1` strips, narrows the dedicated-owner navigation gap by restoring the authored scrollbar click-drag seam on top of the existing wheel and keyboard paths, and narrows the detail-tooltip gap by separating base description text from effect rows while keeping authored line breaks intact, preserving title styling across wrapped fragments, and distinguishing no-guild from ordinary member view-only state. The remaining gap is now concentrated in true packet-owned guild-fund or ledger feeders beyond those command-fed approval outcomes, true wire-level client packet/result handlers for guild-skill learn or renewal outcomes instead of this extended resolve seam, renewal-expiry or level-up echoes that arrive independently of local approval resolution, plus any still-unmodeled client recommendation edge cases outside the now-covered Dual Blade `400/430` recommendation-root exception. | `SkillUI.cs`, `SkillUIBigBang.cs`, `UIWindowLoader.cs`, `SkillDataLoader.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `GuildSkillWindow.cs`, `GuildSkillTooltipContentBuilder.cs`, `GuildSkillRuntime.cs`, `SocialListRuntime.PacketOwned.cs`, `UserInfoUI.cs`, `SocialListWindow.cs`, `UnitTest_MapSimulator/GuildSkillRuntimeTests.cs` (`CUISkill::Draw`, `CUISkill::GetSkillRootVisible`, `CUISkill::GetRecommendSKill`, `CUISkill::OnButtonClicked`, `CUISkill::OpenSkillGuide`, `CWvsContext::OnGuildResult(75)`, `UI/UIWindow2.img/Skill/main/BtRide`, `UI/UIWindow2.img/Skill/main/BtGuildSkill`, `UI/UIWindow2.img/Skill/main/Tab/DualTab`, `UI/UIWindow2.img/Skill/main/Tab/AranButton`, `UI/UIWindow2.img/AranSkillGuide`, `UI/UIWindow2.img/UserList/GuildSkill`, `UI/Basic.img/VScr`, `Skill/9100.img`, `String/Skill.img`) |
| Partial | Item/skill tooltip behavior | Skill hover tooltips still reuse the client `UIWindow2.img/Skill/main/tip0..tip2` frames in the skill window, quick-slot skill hovers, status-bar buff tray, and status-bar cooldown tray, with the status-bar buff/cooldown owners keeping the icon-led client layout plus live cooldown description and remaining-time text. Equip hovers still stay on the richer `UIWindow2.img/ToolTip/Equip` path across the Big Bang equipment window, main inventory equip tab, trunk equip rows, drag comparisons, and the dragon/mechanic/android/pet equipment tabs, including authored WZ `tradeBlock` / `only`, minute-precision `dateExpire`, live remaining-vs-total upgrade-slot counts, bitmap-backed star / mesos / growth `itemLEV` / `itemEXP` rows, and simulator-owned equip metadata persistence through unequip, storage, reload, merge, and re-equip flows. This run keeps the shared non-equip item seam and extends authored install, consume, and cash coverage without adding new owners: quick-slot item hovers, main inventory non-equip rows, trunk storage/local-inventory non-equip rows, and the quest log/detail item hovers now also surface authored `info/recoveryRate` percentage rows, weighted chair-expression rows from `info/randEffect`, `spec/runOnPickup` execution markers, authored `spec/expinc` / `spec/exp` EXP-grant rows, authored `info/exp` level-range rows, authored `info/maxLevel` rows, authored `info/lvOptimum` / `info/lvRange` effective level-band rows, authored `spec/con/*/sMap..eMap` map-range restriction rows, authored `info/expireOnLogout` and `info/noExpend` flags, and authored `info/cantAccountSharable/tooltip` text on top of the earlier chair recovery, wait-interval, buff-chair, cooldown, and `consumeItem` coverage. WZ was rechecked for concrete owning branches in the current extracted data set before wiring those rows: `Item/Cash/0513/05130000/info/recoveryRate`, `05130003/info/recoveryRate`, `05130004/info/recoveryRate`, `Item/Cash/0551/05511000/info/recoveryRate`, `Item/Install/0301/03010289/info/randEffect/0..1` (`face=glitter` at `70`, `face=despair` at `30`), `Item/Consume/0243/02430096/spec/runOnPickup`, `02430096/info/expireOnLogout`, `02430473/info/noExpend`, `02430479/info/noExpend`, `Item/Consume/0202/02022442/spec/expinc`, `Item/Consume/0237/02370000/spec/exp`, `Item/Consume/0223/02230000/info/exp`, `info/maxLevel`, and `info/cantAccountSharable/tooltip`, plus the newly wired level-band and map-restriction owners `Item/Consume/0243/02430010/info/lvOptimum`, `02430010/info/lvRange`, `Item/Consume/0238/02382003/spec/con/0..1`, and `Item/Consume/0202/02022585/spec/con/0`. The earlier non-equip expansion remains in place, including authored `dropBlock`, `noMoveToLocker`, `expRate`, `bonusEXPRate`, `cashExpTicketOn`, `partyExpOn`, and `maxExp` install-item rows, authored personality-exp rows resolved from either `info` or `spec` (`charismaEXP`, `insightEXP`, `willEXP`, `craftEXP`, `senseEXP`, `charmEXP`), authored `create` / `createPeriod`, `period`, and `stateChangeItem` rows, authored `info/replace/itemid`, replacement-duration, and `replace/msg` rows, authored reward-preview expiration rows from `reward/*/period`, quest-name-backed `returnMapQR` labels when quest info is loaded, both the structured and simple numeric-list `consumeItem` shapes, scroll / mastery-book rows for success / cursed rates, target skill names, required skill levels, master levels, scroll stat deltas such as `incINT` / `incCraft`, `reqMap` restrictions, monster-book `mob` ownership, associated mob-card labels, scripted-item / pet-food rows from `spec/script`, `spec/npc`, and `spec/inc`, and the earlier `Item.wz/spec` consumable branches for `moveTo`, cure-status, morph, `party`, `consumeOnPickup`, price, unit-price, level, expiration, trade-block, one-of-a-kind, quest/not-sale, HP/MP recovery, attack/defense, movement, duration, `mhpR` / `mmpR`, `pddR` / `mddR` / `accR` / `evaR`, `asrR`, `booster`, `berserk`, `thaw`, `ignoreContinent`, `returnMapQR`, `indieMhp` / `indieMmp` / `indiePad` / `indieMad` / `indiePdd` / `indieMdd` / `indieSpeed` / `indieJump` / `indieAllStat`, `expBuff` / `expR` / `plusExpRate`, `dropRate` / `dropR`, `mesoR`, `indieBooster`, root or `spec/mob` summon rows, and the authored probabilistic pickup-modifier branches from `spec/mesoupbyitem`, `spec/itemupbyitem`, `spec/itemCode`, and `spec/prob`. Full client `ToolTip/Equip` shell framing, the remaining bitmap-value branches beyond the current mixed slash-sensitive rows, richer growth-instance progress beyond the authored default state, broader potential/additional-option coverage outside the local equipped-item path, true server- or packet-authored instance expiration parity for arbitrary inventory/storage equips, and fuller client-localized non-equip item-tooltip parity for other windows or still-unmodeled `info` / `spec` branches are still incomplete | The simulator now has a shared client-style tooltip surface across the main skill HUDs, the Big Bang equipment window no longer leaves equipped-item hovers blank, metadata-thin, or drag-state-regressed, companion equipment tabs no longer fall back to item-id-only hover stubs when parsed equip data is available, regular inventory and trunk equip rows no longer collapse back to the older generic bubble when the simulator can resolve live equip metadata, and non-equip item hovers in quick-slot, inventory, trunk, and the quest log/detail windows now surface broader authored WZ item metadata and consumable effects instead of flattening to a sparse name/type/count or name-plus-description stub. Equipment hovers consume more of the client `ToolTip/Equip` art set for labels, badges, cash markers, numeric rendering, trade-block / one-of-a-kind metadata, star rows, mesos rows, and growth rows across the equipment window, inventory equip tab, and trunk; authored WZ expiration dates now surface with minute precision; locally upgraded equipped items keep their richer tooltip metadata as they move through inventory/storage; and the broader generic item path now also covers chair recovery rows with authored wait intervals, buff-chair and cooldown metadata, weighted random chair expressions, authored `recoveryRate` percentages, pickup-execution markers, authored EXP-grant rows, authored EXP item level-range and max-level rows, authored effective level-band rows from `info/lvOptimum` / `info/lvRange`, authored map-range restriction rows from `spec/con/*/sMap..eMap`, authored class-sharing restriction text, logout-expiry and non-consumption flags, teleport destinations, cure-item status rows, morph-item markers, party / consume-on-pickup markers, percent-based HP/MP and defensive/offensive stat rows, independent flat-stat and attack-speed buffs, scroll or mastery-book info rows, scripted NPC-item markers, pet-food fullness rows, continent / return-map or req-map markers, monster-book or summon-mob rows, raw authored pickup-modifier rows for meso or item pickup effects, authored `create` and `createPeriod` outputs, authored self-expiration `period` rows, authored `replace` result / duration / message rows, reward-box preview expiration rows, linked cash-effect `stateChangeItem` rows, quest-name-backed return-map record labels, both structured and plain numeric `consumeItem` requirement lists, authored coupon EXP-rate aliases, authored EXP-ticket rows from `info/expRate`, `info/bonusEXPRate`, `info/cashExpTicketOn`, `info/partyExpOn`, and `info/maxExp`, authored locker/drop restrictions from `info/noMoveToLocker` and `info/dropBlock`, and authored personality EXP rows from either `info` or `spec` through the same shared resolver. The remaining gap is the rest of the client's fuller equip-tooltip shell, the remaining bitmap-value branches beyond these mixed rows, richer growth-instance progress beyond the authored default `1` / `0%` state, broader potential or additional-option coverage outside the local equipped-item path, true server-owned expiration or other packet-authored instance data for arbitrary inventory/storage equips, and fuller non-equip item-tooltip parity in the rest of the UI plus more client-localized phrasing or row coverage for still-unmodeled or currently raw `info` / `spec` fields such as wider coupon or consumable-effect families, broader install-item special-case families beyond the now-covered chair recovery, cooldown, random-expression, `recoveryRate`, pickup-execution, authored EXP, authored level-band, and authored map-range-restriction branches, and the exact client wording for the pickup-modifier and several newly surfaced generic non-equip rows | `SkillUIBigBang.cs`, `QuickSlotUI.cs`, `StatusBarUI.cs`, `EquipUIBigBang.cs`, `InventoryUI.cs`, `InventoryItemMetadataResolver.cs`, `TrunkUI.cs`, `TrunkUI.Tooltips.cs`, `QuestUI.cs`, `QuestDetailWindow.cs`, `ItemUpgradeUI.cs`, `CharacterData.cs`, `CharacterLoader.cs`, `UIWindowLoader.cs`, `MapSimulator.cs`, `StorageAccountStore.cs`, `SimulatorStorageRuntime.cs`, `UnitTest_MapSimulator/InventoryItemMetadataResolverParityTests.cs` |
| Partial | Charge bars and richer skill HUD | The simulator now renders the client-timed prepared-skill gauge set for the supported status-bar branches, keeps longer prepare windows from flipping to the generic `Ready` caption as soon as the short client gauge fill completes by surfacing a `Preparing <n> sec` state until the real hold or release point arrives, keeps the client-observed release-owned branches from reusing that generic ready caption by surfacing a `Release` state once the charge window is armed for `4341002`, `4341003`, `5311002`, and the Wild Hunter swallow family `33101005`, gives the SG-88 `35121003` `KeyDownBar4` branch its own `Amplifying <n>%` / `Amplified` caption path instead of the shared generic charge text, and now also mirrors the client `CUserLocal::DrawKeyDownBar` special-case skin branch by routing both Dual Blade prepared skills `4341002` and `4341003` through `UI/Basic.img/KeyDownBar1` instead of leaving `4341003` on the generic `KeyDownBar` art. It still routes the Evan/dragon `22151001` / `22121000` family through a separate world-overlay pass instead of the bottom status-bar surface while keeping the client `KeyDownBar2` / `KeyDownBar3` WZ skins on that split path; that dragon overlay now follows the client `CreateKeyDownBar` placement math from the live dragon vector and active frame height, anchoring the bar at `(-36, -frameHeight-30)` relative to the dragon instead of the older frame-top approximation, keeps the local dragon-owned bar out of the late UI pass, and now draws it in the world character pass between dragon and player so the simulator follows the client `CDragon::CreateKeyDownBar` action-layer ownership more closely instead of treating it like a HUD-only surface, while the generic skill-name plus ready-label text stays suppressed on the dragon-owned branch so it no longer reuses the status-bar caption treatment. The shared HUD-rule seam still mirrors the client `is_keydown_skill` and `get_max_gauge_time` branch set for the supported v95 keydown family, so explicit client timings such as `2121001/2221001/2321001 -> 1000`, `3221001 -> 900`, `4341002 -> 600`, `4341003 -> 1200`, `22121000/22151001 -> 500`, `33101005 -> 900`, the now-normalized `5221004` client-owned default branch, and the generic `2000` ms fallback all flow through one resolver for local prepared skills, repeat-sustain HUD snapshots, remote packet overlays, and `/remoteuser prepare` command overlays. WZ was rechecked for that newly normalized branch before wiring it into the shared table, and `Skill/522.img/skill/5221004` does publish the same authored `prepare`, `keydown`, `keydown0`, and `keydownend` structure the client-owned keyed HUD flow expects, so metadata-thin local or remote paths no longer collapse that skill back to a one-phase generic snapshot when the explicit keydown flag is absent. The Monkey Wave branch now also keeps its local prepare-to-hold timing on that same shared resolver instead of drifting to the longer prepare-animation sum, so `Skill/531.img/skill/5311002/prepare/time = 1080` drives both the local armed window and local/remote overlay gauge timing from one WZ-backed source rather than letting the local branch wait on the `prepare` canvas delays. The supported branch resolver is shared with the remote prepared-skill seams too, so remote packet or command-driven overlays inherit the same client-backed skin, gauge-duration, release/amplify caption, and dragon text-suppression defaults instead of falling back to a generic `KeyDownBar` + `Ready` treatment, remote non-dragon world overlays now anchor from the remote actor's live assembled frame top with the same `topY - 18` seam the local world-overlay path uses instead of a flat `actorY - 92` fallback, and remote dragon-owned overlays now leave the late UI pass as well and render in the remote actor world pass before the avatar draw, using the same supported-branch resolver plus a WZ-backed pseudo-dragon anchor derived from `Skill/Dragon/<job>.img` frame metrics. When packet or command overlays enter an inferred prepare-to-hold split, the pseudo-dragon anchor now switches from the `_prepare` timeline to the resolved hold timeline based on both the active phase and the remote owner's motion state instead of the raw packet `isHolding` bit, choosing the WZ `move` timeline whenever the remote avatar is in a move-family action and falling back to `stand` only when the dragon data lacks that branch. That same shared client branch table now also normalizes the local and remote keydown state for supported families even when the incoming skill metadata or remote packet omits the keydown flag, so known client-owned branches such as `5101004`, `5221004`, `3221001`, `22121000`, `22151001`, and `33101005` still enter the keyed prepare-to-hold gauge path instead of collapsing to a one-phase non-keydown snapshot. The remote release-owner seam now also treats the Wild Hunter swallow release attack `33101007` as a release of the prepared `33101005` branch even when the incoming remote packet omits the extra follow-up payload, matching the client-owned prepare/repeat-vs-release family split more closely instead of leaving that overlay stuck until timeout. That same remote pseudo-dragon seam now also mirrors the local dragon ladder-vs-ground anchor offsets while choosing between the `Skill/Dragon/<job>.img` `move` and `stand` origins, so ladder or rope states no longer reuse the grounded offset math. This run checks in the focused `UnitTest_MapSimulator/PreparedSkillHudParityTests.cs` source coverage that the row depends on, guarding the supported client skin table, the explicit client gauge-duration branches, the normalized `5221004` default-duration branch, the generic `2000` ms fallback, the Monkey Wave prepare-to-hold seam, the remote prepare-vs-hold phase splitter, the Wild Hunter release-owner mapping, and the modeled `Preparing`, `Release`, `Amplifying`, `Amplified`, and `Maintaining` captions. The remote command seam still accepts explicit `gauge=<ms>`, `hold=<ms>`, and `state=prepare|holding|auto` options so command-driven overlays can stay in the same client-shaped prepare-to-hold or immediate-hold phases that drive those captions instead of collapsing to a one-shot pre-hold timer. Remote packet overlays still infer that same prepare-to-hold phase split when packet payloads carry distinct prepare and hold windows (and accept an explicit auto-hold flag bit). The remaining gap is now limited to any still-unmodeled client-only caption edge cases outside these supported branch families plus the lack of a true remote dragon runtime or packet-owned companion entity, so remote dragon ownership still has to be approximated from the remote user body, motion action, and exported dragon animation data instead of a live companion object like the local path does; packet overlays can model a two-phase prepare-to-hold window when the payload provides both windows, but they still cannot reconstruct every client caption or dragon-follow transition when the source packet omits that companion-state detail. | Prepared/keydown skills now surface closer client timing and HUD feedback instead of a single generic bar, longer prepare branches no longer advertise `Ready` while their actual prepare timer is still running, release-armed branches now advertise release ownership instead of a generic ready state, the Dual Blade `4341003` branch no longer renders on the generic `KeyDownBar` skin because it now shares the client-owned `KeyDownBar1` art path with `4341002`, generic keydown branches no longer collapse to a zero-duration fallback because they now inherit the client `2000` ms default gauge from the shared resolver, Monkey Wave no longer drops its charge window onto a zero-duration generic bar or lets the local armed window drift to the longer `prepare` animation sum when the client publishes a real `prepare/time`, including remote packet and command-driven overlays, SG-88 now exposes a branch-specific amplify caption on its `KeyDownBar4` HUD, the client-owned `5221004` branch no longer depends on an explicit keydown flag to enter the keyed HUD flow, sustained keydown branches expose both their live maintain label and remaining hold-bar state more clearly, the Evan/dragon branch no longer falls back to the status-bar path, the older player-top/frame-top approximation, or the late UI layer because it uses the client dragon bar offsets from `CDragon::CreateKeyDownBar` and now renders in the world character pass closer to the client-owned dragon action layer, supported branch families no longer depend on raw local WZ flags or remote packet booleans to enter the keyed HUD flow because the client branch table now normalizes that state before the local release/cancel logic and remote phase splitter run, and the Wild Hunter swallow remote release attack no longer needs an extra follow-up payload to clear the prepared overlay because the remote helper now maps `33101007` back onto the prepared `33101005` family directly. This run removes the largest regression hole around this row by checking in the missing prepared-skill HUD parity test source file and pinning the shared skin table, fallback timings, remote phase splitting, release-owner mapping, and modeled caption states to the current behavior. The row is still partial because unsupported caption edge cases outside these modeled HUD branches remain, and remote dragon ownership is still inferred from actor-plus-WZ animation metadata rather than a true packet-driven companion runtime. | `PreparedSkillHudRules.cs`, `PreparedSkillHudTextResolver.cs`, `StatusBarUI.cs`, `SkillManager.cs`, `MapSimulator.cs`, `MapSimulator.RemoteUsers.cs`, `RemoteUserActorPool.cs`, `PlayerCharacter.cs`, `DragonCompanionRuntime.cs`, `UnitTest_MapSimulator/PreparedSkillHudParityTests.cs` (`CUserLocal::DrawKeyDownBar`, `CDragon::CreateKeyDownBar`, `CDragon::DrawKeyDownBar`, `CUserLocal::Update`, `CUserLocal::TryDoingPreparedSkill`, `is_keydown_skill`, `get_max_gauge_time`, `UI/Basic.img/KeyDownBar*/bar/origin`, `Skill/522.img/skill/5221004`, `Skill/531.img/skill/5311002/prepare/time`, `Skill/3310.img/skill/33101005`, `Skill/3310.img/skill/33101007`, `Skill/Dragon/<job>.img`, `String/Skill.img/35121003`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
