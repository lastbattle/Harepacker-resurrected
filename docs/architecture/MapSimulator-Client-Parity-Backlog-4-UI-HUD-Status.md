# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Physics/CVecCtrl.cs`
- `HaCreator/MapSimulator/Character/CharacterLoader.cs`
- `HaCreator/MapSimulator/Character/CharacterAssembler.cs`
- `HaCreator/MapSimulator/Character/PlayerCharacter.cs`
- `HaCreator/MapSimulator/Character/Skills/SkillManager.cs`
- `HaCreator/MapSimulator/Effects/CombatEffects.cs`
- `HaCreator/MapSimulator/Pools/PortalPool.cs`
- `HaCreator/MapSimulator/UI/StatusBarUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuickSlotUI.cs`

### Client references checked

- `CUserLocal::DoActiveSkill_MeleeAttack` at `0x93b210`
- `CUserLocal::DoActiveSkill_ShootAttack` at `0x93b520`
- `CUserLocal::DoActiveSkill_MagicAttack` at `0x93a010`
- `CUserLocal::DoActiveSkill_Prepare` at `0x941710`
- `CUserLocal::TryDoingFinalAttack` at `0x93aaa0`
- `CUserLocal::TryDoingSerialAttack` at `0x93ac90`
- `CUserLocal::TryDoingSparkAttack` at `0x93abe0`
- `CUserLocal::Update` at `0x937330`
- `CUserLocal::SetMoveAction` at `0x903ce0`
- `CUserLocal::TryDoingPreparedSkill` at `0x944270`
- `CUserLocal::TryDoingRepeatSkill` at `0x93d400`
- `CUserLocal::DrawKeyDownBar` at `0x9153b0`
- `CUserLocal::CheckPortal_Collision` at `0x919a10`
- `CUserLocal::CheckReactor_Collision` at `0x903d20`
- `CUserLocal::TryDoingTeleport` at `0x932c00`
- `CUserLocal::TryDoingRush` at `0x90b8c0`
- `CUserLocal::TryDoingFlyingRush` at `0x90bc10`
- `CUserLocal::TryDoingRocketBooster` at `0x940e50`
- `CUserLocal::TryDoingRocketBoosterEnd` at `0x93d2d0`
- `CUserLocal::TryDoingSmoothingMovingShootAttack` at `0x92de70`
- `CUserLocal::TryDoingSwallowBuff` at `0x944520`
- `CUserLocal::TryDoingSwallowMobWriggle` at `0x93f500`
- `CUserLocal::TryDoingMine` at `0x907d70`
- `CUserLocal::TryDoingCyclone` at `0x932d60`
- `CUserLocal::TryDoingSitdownHealing` at `0x903d60`
- `CUserLocal::UpdateClientTimer` at `0x90e6d0`
- `CUserLocal::TryPassiveTransferField` at `0x91a2e0`
- `CUserLocal::TalkToNpc` at `0x9321f0`
- `CUserLocal::TryAutoRequestFollowCharacter` at `0x904bf0`
- `CUserLocal::TryLeaveDirectionMode` at `0x9054c0`
- `CUser::LoadLayer` at `0x8e96d0`
- `CUser::PrepareActionLayer` at `0x8e3070`
- `CUser::SetMoveAction` at `0x8df540`
- `CUser::Update` at `0x8fb8d0`
- `CUser::UpdateMoreWildEffect` at `0x8fb4c0`
- `CUser::PetAutoSpeaking` at `0x8deb10`
- `CVecCtrl::CollisionDetectFloat` at `0x994740`
- `CMovePath::IsTimeForFlush` at `0x666870`
- `CMovePath::MakeMovePath` at `0x667c90`
- `CMovePath::Flush` at `0x668160`
- `CMovePath::Encode` at `0x666e20`
- `CMovePath::Decode` at `0x667920`
- `CMovePath::OnMovePacket` at `0x6683f0`
- `CPortalList::FindPortal_Hidden` at `0x6ab5d0`
- `CField::IsFearEffectOn` at `0x52a420`
- `CField::OffFearEffect` at `0x52b810`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`
- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`

## Client Function Index By Backlog Area

The list below maps each backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. UI parity

- `CUIStatusBar::Draw` at `0x876cd0`
- `CUIStatusBar::SetStatusValue` at `0x873590`
- `CUIStatusBar::SetNumberValue` at `0x873d50`
- `CUIStatusBar::ChatLogAdd` at `0x87aec0`
- `CUIStatusBar::CQuickSlot::Draw` at `0x875750`
- `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` at `0x871000`
- `CUISkill::Draw` at `0x84ed90`
- `CUISkill::GetSkillRootVisible` at `0x84a6f0`
- `CUISkill::GetRecommendSKill` at `0x84e710`

Notes:
`CUIStatusBar::Draw` confirms that name/job-or-level selection, HP/MP/EXP number updates, and quick-slot redraw are part of a single status-bar draw pass, while `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` shows the client also revalidates slot contents against learned skills and inventory state before redraw. `CUISkill::Draw` confirms visible skill-root selection, recommend-skill highlighting, bonus-SP rendering, skill icon row layout, and skill-level text placement.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.

## Remaining Parity Backlog

### 1. UI parity

This area is more advanced than older parity notes suggested, but still far from complete client parity.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Status bar layout | Positions remain grounded in WZ plus client analysis and the prior parity work stays in place: the Big Bang `StatusBar2.img/mainBar` owner still composes the left cluster, gauge cluster, right-side buttons, and chat strip from authored canvas origins and WZ `z` order; left-cluster name/job/level slots and gauge numbers still follow the client `SetStatusValue` / `SetNumberValue` seams; the chat strip still uses the authored `chatSpace2` lane, `ChatLogDraw` left inset, client-shaped `ChatLogAdd` wrapping, and the owner-local embedded-font-preferred rasterizer path. The whisper-target picker keeps the prior row-skin parity from WZ (`UI/UIWindow2.img/UtilDlgEx/list4/origin = (0,1)` and `list5/origin = (1,-1)`) and now also moves the modal shell width, editable combo lane, and footer button slots onto the recovered client `CUtilDlgEx` combo-box owner. Client re-check confirms `CUIStatusBar::ChangeWhisperTarget` creates a no-NPC `CUtilDlgEx` type-7 dialog, `CUtilDlgEx::Layout_INPUT` / `GetWndWidth` resolve that owner to width `260`, and `CUtilDlgEx::OnCreate_COMBOBOX_EDITABLE` creates the editable combo at `(21, height - 63, 222, 18)` with OK / Close buttons at `x=157` and `x=198`, both at `height - 31`. The simulator now uses those owner constants, clips the wide authored `UtilDlgEx` frame slices to the 260-pixel client window instead of scaling the full 519-pixel WZ shell, tiles the center slice vertically, and clips the wider authored divider to the same client modal width. Remaining gap is now narrower and concentrated in reproducing the exact client `IWzFont` face data and final COM-side shaping/hinting byte-for-byte, plus the last whisper-target picker combo/dropdown behavior and height derivation where the simulator still renders an always-expanded candidate list instead of a true `CCtrlComboBox` popup driven by the client `m_ctHeight` / `m_height = m_scrHeight + 93` path. | The status bar and Big Bang chat strip stay materially closer to the client than before: major overlays and buttons remain anchored from the authored `mainBar` frame, text measurement and clipping remain on the owner-local client-text rasterizer seam, wrapped chat rows still follow the client-shaped line split and retention rules, the picker no longer pins both `UtilDlgEx/list4` and `list5` rows to one simulator origin, and the modal picker no longer stretches the 519-pixel shared util-dialog shell or offsets the combo/footer controls around that overwide simulator-owned frame. The row stays partial because final glyph shape still depends on approximated local or extracted Dotum/Gulim/Batang plus GDI rather than the bundled client font objects, and the modal whisper-target picker still approximates the client combo dropdown/list lifecycle and final height instead of owning a real client-style `CCtrlComboBox` popup. | `StatusBarUI.cs`, `StatusBarLayoutRules.cs`, `StatusBarChatUI.cs`, `StatusBarChatLayoutRules.cs`, `ClientTextRasterizer.cs`, `UILoader.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/StatusBarChatWhisperPickerTests.cs` (`CUIStatusBar::Draw`, `CUIStatusBar::SetStatusValue`, `CUIStatusBar::SetNumberValue`, `CUIStatusBar::ChatLogDraw`, `CUIStatusBar::ChatLogAdd`, `CUIStatusBar::ChangeWhisperTarget`, `CUtilDlgEx::Layout_INPUT`, `CUtilDlgEx::OnCreate_COMBOBOX_EDITABLE`, `get_basic_font`, `UI/StatusBar2.img/mainBar/*`, `UI/UIWindow2.img/UtilDlgEx/list4`, `UI/UIWindow2.img/UtilDlgEx/list5`, `UI/UIWindow2.img/UtilDlgEx/{t,c,s,line,BtOK,BtClose}`) |
| Partial | Skill UI layout | Row height, positions, hit boxes, SP placement, quick-slot assignment wiring, and a client-backed `Basic.img/VScr` scrollbar at the confirmed `153,93,155` placement are now present, with both skill-window variants reusing the client `tip0..tip2` frames for the fixed-size SP and hint balloons, the scrollbar supporting arrow clicks, track paging, thumb dragging, and wheel-over-scrollbar input, the row list drawing the client `Skill/main/recommend/0` banner from `Etc/RecommendSkill.img/<skillRootId>` SP-threshold table at the client `47,nTop-19` overlay offset plus the existing visible-tab levelable-skill fallback, the book header now following the client width-driven two-line wrap path by measuring the authored `110`-pixel text lane and breaking at the first overflowing word boundary instead of the older fixed-index split heuristic, the visible and inactive skill-book tabs drawing and hit-testing from their authored `UIWindow2.img/Skill/main/Tab/*` origins and heights, the `BtSpUp` control using its normal/hover/pressed/disabled states with WZ-backed `req` / `reqLevel` gates, and the local SP recompute seam deriving each visible book's pool from the WZ-backed book-open thresholds plus spent levels instead of fabricating points from every unfilled max level. The shared hover tooltip path already keeps the client `320`-pixel body width, the `x=10/10,32/87` title-icon-text column layout, the live-mouse `+20` Y anchor, and the dedicated prerequisite footer, which now resolves its title and level caption through the client `StringPool` ids used by `CUIToolTip::DrawReqSkill` (`0x801` / `0x800`) instead of staying on simulator-only literals. That same footer now also uses the client requirement-row fill color (`0xA0FFFFFF`) rather than the older simulator-owned red swatch, while preserved `#c...#` formatting markers and WZ-property-order fallback stat text still cover unauthored `h*` rows, including `itemCon` / `itemConNo`, `itemConsume`, `bulletConsume`, damage-over-time fields, `damagebymoneyCon`, `massSpell`, `magicSteal`, `fixdamage`, `selfDestruction`, `subProp`, `subTime`, `reqGuildLevel`, `ar`, `er`, `psdSpeed`, and `psdJump`. This run further narrows that fallback seam by keeping otherwise-unmodeled `level` / `common` properties in their authored WZ order when no authored `h*` text exists, so raw-key lines like `u`, `v`, or `actionSpeed` no longer disappear entirely while metadata-only keys such as `maxLevel` and `priceUnit` stay suppressed. This run also promotes another small class of fallback rows onto client-facing wording by formatting WZ-authored `actionSpeed` values as `Attack Speed` levels, by renaming `padX` / `madX` to the same `Weapon ATT` / `Magic ATT` phrasing that authored `String/Skill.img` `h*` lines already use, and by mining the authored `String/Skill.img` clause text around unresolved `#x/#y/#z/#u/#v/#w` placeholders so fallback rows can now recover client-facing labels such as `Max Movement Speed`, `Enemy Accuracy`, or `Mob Count` instead of dropping back to raw single-letter captions in those common cases. This pass also keeps the dedicated guild-skill owner improvements in the existing seams: the local owner now persists learned levels, remaining local SP, guild fund, and active renewal expirations per guild identity instead of one shared save bucket, so switching between no-guild, guild A, and guild B contexts no longer cross-pollinates local guild-skill state, and the dedicated guild-skill hover detail now uses the authored `UIWindow2.img/UserList/GuildSkill/base` width (`244`) as its bounded text surface, reuses that same `base` canvas as the tooltip shell instead of stretching the broader window frame texture, preserves embedded line breaks and blank spacer rows, wraps title and body text at their actual draw scales, and clamps the tooltip to the viewport instead of letting the older generic box overflow off the window edge. The `sheet1` footer path remains on strip-width-aware wrapping with clipped draw bounds so long current/next/cost summaries stay anchored to the loaded strip geometry. Remaining layout parity is now narrower and concentrated in the original client's exact `IWzFont` glyph raster and palette, the last micro-offset or line-break differences on row or tooltip text outside those string-pool-backed requirement captions, the remaining single-letter fallback cases whose authored WZ text still does not expose enough clause context to recover a client-facing label safely, and the absence of true packet- or login-authored per-book SP, guild-fund, contribution, renewal, and ledger/session ownership where the simulator still has to derive or persist those values locally. | The window is materially closer to the client than the older row implied: recommendation overlays, row and tab geometry, hover-skill requirements, fallback stat coverage, and local SP affordances already live in WZ/client-backed seams, and the remaining gap is now mostly the final font-object and authoritative-session polish rather than structural layout misses. This run further narrows the shared skill-window layout side by replacing the older near-10-character header split guess with a measured word-wrap pass that respects the authored `110`-pixel book-name lane in both skill-window variants, so long job-book names now break where the client-width path would overflow instead of at a fixed string position. The shared hover-skill side still keeps the earlier fallback-stat gains, including the recovered `actionSpeed`, `padX`, and `madX` wording, and now also recovers several previously raw single-letter rows directly from authored `String/Skill.img` placeholder clauses before falling back to generic captions, while the dedicated guild-skill side remains improved from the existing local-owner and bounded-tooltip work. The row stays partial because the simulator still lacks the final client `IWzFont` rendering path, the last micro-offset or line-break differences on row or tooltip text, the remaining single-letter fallback cases whose WZ text still does not describe the placeholder strongly enough to infer a safe client-facing label, and true packet-owned guild contribution, fund, renewal, ledger, and per-book SP ownership. | `SkillUI.cs`, `SkillUIBigBang.cs`, `GuildSkillWindow.cs`, `GuildSkillRuntime.cs`, `SkillDataLoader.cs`, `UIWindowLoader.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/GuildSkillRuntimeTests.cs`, `UnitTest_MapSimulator/SkillTooltipFallbackParityTests.cs` (`CUISkill::Draw`, `CUISkill::OnMouseMove`, `CUIToolTip::SetToolTip_Skill`, `CUIToolTip::DrawReqSkill`, `CUISkill::GetRecommendSKill`, `Etc/RecommendSkill.img`, `Skill/*/skill/*/req`, `UI/UIWindow2.img/Skill/main/Tab`, `UI/UIWindow2.img/Skill/main/recommend`, `UI/UIWindow2.img/Skill/main/BtSpUp`, `UI/UIWindow2.img/Skill/main/tip0`, `UI/UIWindow2.img/Skill/main/tip1`, `UI/UIWindow2.img/Skill/main/tip2`, `UI/UIWindow2.img/UserList/GuildSkill`, `UI/UIWindow2.img/UserList/GuildSkill/base`, `UI/UIWindow2.img/UserList/GuildSkill/sheet1`, `UI/Basic.img/VScr`, `String/Skill.img`, `StringPool 0x800`, `StringPool 0x801`) |
| Implemented | Quick slot cooldown overlay | Cooldown masking now uses the client `UIWindow2.img/Skill/main/CoolTime` frame family, remaining-seconds countdown text renders on assigned quick-slot skills, and the stepped mask holds the initial full-overlay frame until the next visible whole-second boundary instead of advancing immediately after cast | The core client-facing quick-slot cooldown feedback is now present on the simulator HUD | `QuickSlotUI.cs` (`CUIStatusBar::CQuickSlot::Draw`) |
| Implemented | Quick-slot validation and quantity sync | The simulator now rejects passive, hidden, and unlearned skills at hotkey assignment time, restores saved quick-slot bindings through the same validation path after preset load instead of trusting raw slot data, eagerly prunes stale skill slots as soon as learned levels change, cancels an in-flight quick-slot drag if its source binding becomes invalid before the next redraw, and now mirrors the client's `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` inventory-backed recount for owned `USE` / `CASH` quick-slot item entries so stack counts redraw live and empty item slots clear as soon as their backing inventory runs out | Quick-slot contents now stay synchronized with both learned-skill state and live inventory stacks, so the HUD no longer keeps stale consumable or cash-item entries after crafting, pickup, preset restore, or manual reassignment | `QuickSlotUI.cs`, `SkillManager.cs`, `CharacterConfig.cs`, `MapSimulator.cs` (`CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo`) |
| Implemented | HP/MP warning flash | HP/MP gauge warnings now replay the client `aniHPGauge` / `aniMPGauge` and `hpFlash` / `mpFlash` assets for 500 ms when the resource drops further while already below the configured threshold, and the simulator exposes live `/hpwarn` + `/mpwarn` commands to adjust those thresholds in-session | The client-visible low-resource cue is present across both modern and pre-BigBang status-bar loaders, and the trigger point is no longer hardcoded at 20% | `StatusBarUI.cs`, `UILoader.cs`, `MapSimulator.cs` (`CUIStatusBar::Draw`, `CUIStatusBar::SetNumberValue`) |
| Partial | Chat log / whisper flows | The Big Bang status-bar chat strip still keeps the earlier parity work in place: wrapped recent chat lines now follow the client `ChatLogAdd` continuation-indent rules, apply the observed first-line width reduction for `lType 14/16/19/20`, cap visible wrapped history at the client `0x40` entry ceiling, keep manual scroll position until the `_RefreshChatLog` idle snap-back window expires, honor WZ `chatTarget/*/origin` deltas when drawing the target badge, and let both strip clicks and `/w` or `/r` arm whisper mode through the tighter client-shaped `ExtractCharacterName` plus `ChangeWhisperTarget` validation seam confirmed in IDA. The WZ-backed whisper picker still uses `UIWindow2.img/UtilDlgEx/list4` and `list5`, keeps the full remembered candidate history scrollable by wheel, keyboard row navigation, and page movement over the visible six-row window, restores the preserved draft on `Escape`, confirms on row click or `Enter`, and keeps strip-row confirmation on the active picker presentation path instead of transiently reopening the inline picker. The modal-owner gap also keeps the closer recovered `ChangeWhisperTarget` behavior from the prior pass: `/w` and `/r` with no current target still open the client-shaped modal picker even when remembered history is empty, the modal still opens on the editable combo row because the client `CUtilDlgEx::OnCreate_COMBOBOX_EDITABLE` path calls `CWnd::SetFocusChild(this, this->m_pCombobox.p)`, button `keyFocused` canvases still only render after the footer lane is actually armed, `Tab` still toggles between the combo lane and the `BtOK` / `BtClose` footer lane, left/right still only swap OK versus close while that footer lane is active, and text entry, wheel scrolling, row navigation, page movement, or row clicks still route focus back onto the combo lane before continuing the existing selection or edit path. This run narrows the remaining font-routing seam inside that same owner without inventing a new text path: WZ still only contributes the existing `StatusBar2.img/mainBar` and `UIWindow2.img/UtilDlgEx` chat surfaces, while IDA now confirms `CUIStatusBar::OnCreate` creates its chat-log fonts at height `0x0B`, so `StatusBarChatUI` now routes its client text rasterizer through a recovered `11`-pixel chat-font height instead of the older generic `12`-pixel fallback. The stricter editable-combo seam remains in place too: the shared character-name extractor no longer falls through to arbitrary trailing tokens after stripping chat prefixes, and typed modal input still keeps the live edit text while auto-selecting the first remembered candidate whose prefix matches the current combo text, with exact matches still winning over earlier prefix hits. Focused simulator tests are now on disk in `UnitTest_MapSimulator/StatusBarChatWhisperPickerTests.cs` and `UnitTest_MapSimulator/StatusBarChatLayoutRulesTests.cs`, covering the tighter character-name extraction, the no-history modal-open path, prefix-versus-exact combo selection behavior, the modal combo-first focus start, the recovered `11`-pixel chat-font constant, the client wrap rules, and the WZ-origin-aware modal button-slot centering helper seams. Remaining gap is now narrower and concentrated in the exact native `IWzFont` object set and any still-unrecovered per-`lType` font-object routing beyond this recovered `11`-pixel height pass, the rest of the native combo-box edit/history semantics and unrecovered modal key choreography inside `ChangeWhisperTarget`, and broader packet-owned incoming chat ownership outside the already modeled whisper, Messenger, field-feedback, local-utility, summoned, local-overlay, MapleTV, packet-script, and quest-result families. | The status-bar chat surface is materially closer to the client's visible log and whisper flow than before: older lines no longer require button-only review, visible whisperable sender prefixes can arm a new target directly from the strip, the chat-target badge honors the authored WZ origin deltas, modeled client `lType` rows keep the simulator's client palette instead of caller-provided fallback colors, and packet-shaped whisper or messenger lines no longer collapse back to a generic visible-strip treatment. This run narrows the remaining text drift inside the same seam by moving the chat strip and whisper picker off the older generic `12`-pixel client-text fallback and onto a recovered `11`-pixel client chat-font height backed by `CUIStatusBar::OnCreate`, while the closer modal owner, combo-prefix selection, and WZ button-origin behavior from the earlier pass stay in place. The row stays partial because the simulator still approximates the client's final `IWzFont` objects and any richer per-`lType` font-object switching, still does not mirror every internal combo-box text-entry/history behavior or modal key path inside `ChangeWhisperTarget`, and still lacks broader packet-owned incoming chat ownership outside the already modeled feedback families. | `MapSimulatorChat.cs`, `StatusBarChatUI.cs`, `StatusBarChatLayoutRules.cs`, `UILoader.cs`, `MapSimulator.Content.cs`, `MapSimulator.PacketOwnedFieldFeedbackParity.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `MapleTvRuntime.cs`, `MessengerRuntime.cs`, `UnitTest_MapSimulator/StatusBarChatWhisperPickerTests.cs`, `UnitTest_MapSimulator/StatusBarChatLayoutRulesTests.cs` (`CUIStatusBar::ChatLogAdd`, `CUIStatusBar::_RefreshChatLog`, `CUIStatusBar::TryBeginWhisper`, `CUIStatusBar::ChangeWhisperTarget`, `CUIStatusBar::OnCreate`, `is_valid_character_name`, `StringPool 0x31E`, `StringPool 0x31F`, `StringPool 0x320`, `CUtilDlgEx::OnCreate_COMBOBOX_EDITABLE`, `CWnd::SetFocusChild`, `CField::OnWhisper`, `UI/StatusBar2.img/mainBar/chatTarget/*`, `UI/UIWindow2.img/UtilDlgEx/{t,c,s,bar,line,BtOK,BtClose,list4,list5}`) |
| Partial | Full skill UI behavior | The Big Bang skill window now mirrors `CUISkill::GetSkillRootVisible` at the tab layer by only exposing the visible skill-book path for the current job, keeps keyboard-selected entries in view while accepting pane-hover wheel scrolling plus `Up` / `Down` / `PageUp` / `PageDown` / `Home` / `End` navigation, lets `Left` / `Right` cycle across the currently visible skill-book tabs without reaching for the mouse, renders the client recommendation banner from a WZ-loaded skill-root/SP-threshold recommendation table with the existing visible-tab levelable-skill fallback when no applicable entry can level, and now also tracks the displayed skill root per tab so the `CUISkill::GetRecommendSKill` branch for Dual Blade's early `400/430` books follows the client more closely by resolving threshold progress from the combined learned levels across those two visible roots while sourcing the recommendation table from the `430` root instead of treating each tab as a fully isolated recommendation bucket. It routes `BtSpUp` clicks into live skill-level increments with per-tab SP countdown sourced from the remaining unmaxed levels in the loaded book, loads the Aran-only `Skill/main/Tab/AranButton/Bt1..Bt4` guide buttons so the client `3001..3004 -> OpenSkillGuide` path opens the matching `UIWindow2.img/AranSkillGuide/<grade-1>` overlay page, instantiates the WZ-backed bottom-row `BtRide` / `BtGuildSkill` controls, now loads the WZ-authored `Skill/main/Tab/DualTab` strip so Dual Blade jobs walk the client book path across `0, 400, 430, 431, 432, 433, 434` instead of collapsing their extra books onto the standard five-tab branch, and now also opens a dedicated WZ-backed `UIWindow2.img/UserList/GuildSkill` owner from both the skill-window guild shortcut and the guild member-list `BtSkill` action, loading the visible guild-skill catalog from `Skill/9100.img` plus `String/Skill.img` with the row recommendation banner, `BtRenewal`, and `BtUp` actions gated by the parsed `reqGuildLevel` formulas. The dedicated guild-skill runtime now suppresses recommendation cycling, treats `BtRenewal` as a local timed guild-skill renewal action backed by `Skill/9100.img/common/{extendPrice,period,priceUnit}` instead of as a recommendation toggle, persists local learned levels plus active-renewal timers and a simulator-owned guild-fund ledger across placeholder membership loss and restoration, preserves a simulator-owned fallback guild level instead of resetting every in-guild refresh to Guild Lv. 1, refreshes both the skill-window guild shortcut state and the dedicated guild-skill owner from the current build plus social authority context instead of only at initial wire-up, now also persists same-guild packet-owned guild-level and guild-point updates back into the per-guild saved-state bucket so later local-context refreshes keep the last authoritative `CWvsContext::OnGuildResult(75)` point total and guild level instead of falling back to the simulator seam, keeps both the skill-window guild shortcut and the member-list `BtSkill` route disabled until the build exposes a real non-placeholder guild name, preserves the local Master vs. Jr. Master vs. Member role already surfaced in the social list across runtime refreshes so `BtRenewal` / `BtUp` stay view-only for non-manager roles instead of silently re-promoting the local shell back to full authority, and now also gates both actions against the WZ-authored learn and renewal meso costs so the dedicated owner spends from a local guild-fund pool instead of letting every manager action succeed for free. When that guild authority path is packet-owned, the dedicated runtime now stages `BtRenewal` and `BtUp` as pending requests instead of mutating the local shell immediately, feeds the pending learn-vs-renew label into the dedicated summary and hover detail seams, blocks duplicate manager actions while approval is outstanding, and resolves the staged result through the existing `/sociallist packet resolve guild approve|reject` path so packet-authority scenarios can now model an approve-vs-reject round trip instead of always auto-succeeding locally. That resolve seam now also accepts authoritative `level=<n>`, `remain=<minutes>`, and `fund=<mesos>` outcome tokens for pending guild-skill approvals, letting the dedicated runtime mirror packet-owned learn and renewal results with server-shaped skill levels, renewal timers, and post-action guild-fund totals instead of always deriving those values from the local cost-and-duration shell after approval. It also surfaces `Skill/9100.img/common/{price,extendPrice,period,priceUnit}` plus `String/Skill.img/9100000x/h` in the dedicated owner so hover or selection details show the authored current/next effect text, learn cost, renewal cost, duration, local remaining renewal time, current fund state, the pending-action state when packet approval is outstanding, and the packet-owned guild point total carried by `CWvsContext::OnGuildResult(75)`, with packet-owned guild points now staying labeled as guild points instead of being formatted through the meso path and true no-guild detail states no longer leaking the simulator-owned guild-fund or guild-point rows. Unlearned guild skills no longer duplicate the same level-1 WZ effect text as both `Current` and `Next`; the dedicated owner now also loads and draws `UIWindow2.img/UserList/GuildSkill/sheet1/0..2` so the footer uses the client-authored activation-effect strips instead of a simulator rectangle, and the selection summary feeding that footer now follows the effect-focused WZ data rather than reusing a generic guild-name/SP status block. This run also keeps the first resolved guild recommendation banner stable after that entry caps or becomes temporarily unlevelable instead of cycling the highlight to another row on every local state change, makes the footer-summary text flow follow the authored strips more closely by wrapping into the available strip width/height and vertically centering line placement against the loaded `sheet1` row textures instead of relying on a fixed single-line truncation seam, while `Up` / `Down` / `PageUp` / `PageDown` / `Home` / `End` can move the guild-skill selection without reaching back to the mouse and `PageUp` / `PageDown` now step by the visible page size instead of a shorter local jump. The dedicated guild-skill owner now also loads `UI/Basic.img/VScr` and applies the client-style vertical scrollbar seam over the list, so arrow clicks, track paging, and thumb dragging follow the same visible-page scroll state as the existing wheel and keyboard navigation instead of leaving longer guild-skill catalogs mouse-wheel-only. This run further narrows the dedicated-owner detail gap by keeping the generic guild-skill description separate from the authored current-effect slot, explicitly surfacing the simulator-owned guild fund, packet-owned guild points, pending approval, the timed active-vs.-inactive state, and a separate view-only authority label in the hover detail path, honoring embedded line breaks plus overlong-token wrapping in the dedicated owner tooltip instead of flattening everything through a single whitespace-only wrapper, keeping wrapped tooltip title fragments on the same highlighted title palette and scale instead of demoting every fragment after the first to body text, and labeling the true no-guild detail state separately from generic member view-only mode. Focused simulator coverage now exists for the dedicated-owner same-guild packet-context persistence seam, the non-cycling guild recommendation seam, and the split active-state-vs.-view-only-authority tooltip/runtime seam in `UnitTest_MapSimulator/GuildSkillRuntimeParityTests.cs`, alongside the existing runtime coverage. This pass also aligns the shared/classic skill-book visibility helper with the confirmed client `CSkillInfo::IsSkillVisible` hard-hide list by suppressing `10001015` in the same path that already hid `4321001`, `1014`, and the Dual Blade-born `4000001` / `4001344` pair, with WZ confirming `Skill/1000.img/skill/10001015` is an invisible, time-limited, disabled skill entry. This pass also persists the last resolved guild-point total inside the per-guild saved-state bucket so `OnGuildResult(75)` point updates survive same-guild or no-guild context round-trips that momentarily omit a fresh point value, now treats packet-owned guild points as authoritative even when that payload resolves to `0` instead of silently reviving a stale saved total on refresh, keeps hovered guild-skill rows on the authored `iconMouseOver` art branch, and keeps the dedicated tooltip on the explicit no-guild `Current: Join a guild.` / `Next: Requires guild membership.` wording instead of falling back to the generic not-learned branch while the authored WZ description, requirement, duration, and cost rows remain visible. | The simulator now respects the client's visible-root job path, no longer leaves the level-up button dead, follows the client-style recommendation progression more closely, now also avoids undercounting Dual Blade `400/430` recommendation thresholds by mirroring the client-owned combined spent-skill-level branch for those first two books, no longer drops the Aran guide or the WZ-backed ride / guild shortcut controls that the Big Bang skill window exposes, no longer flattens Dual Blade books onto the standard tab strip, no longer treats guild-skill access as a dead generic-tab fallback, no longer leaves the guild shortcut or dedicated guild-skill owner stuck on stale placeholder-membership or stale-role state after the local social/build context refreshes, no longer reuses `BtRenewal` as a recommendation cycler, no longer collapses every in-guild refresh back to a fabricated Guild Lv. 1, now keeps packet-owned no-guild or alternate-guild UI state from snapping back to the local build name or guild level when the dedicated owner refreshes, now also keeps same-guild packet-owned guild-point and guild-level echoes alive across later local-context refreshes, no longer duplicates the same level-1 guild-skill effect string in both detail slots before a skill is learned, no longer reuses the generic guild-skill description as a fake `Current` effect before a skill is learned, now exposes the current guild-fund, packet-owned guild-point total, pending approval, timed activation state, and separate view-only authority directly in the dedicated tooltip path, no longer formats guild points as meso-valued currency in the dedicated owner summary or tooltip seams, no longer shows guild-point or guild-fund rows when the dedicated owner is in a true no-guild detail state, no longer flattens multiline guild-skill detail text through a whitespace-only wrapper, no longer fills the dedicated owner's WZ-titled `Guild Skill Activation Effects` footer with a simulator-owned guild-name/status block because the footer now uses the authored `sheet1` strip art plus selected-skill current/next/cost-duration text plus packet-owned guild points, no longer leaves the dedicated owner without the client-style `Basic.img/VScr` scrollbar when the guild catalog extends beyond the visible seven-row page, no longer reports a true no-guild tooltip as generic view-only state, and no longer drops the highlighted title treatment after the first wrapped guild-skill tooltip fragment. This run also narrows the packet-context gap by feeding `OnGuildResult(75)` guild points through the shared social-list context into the dedicated guild-skill owner summary and tooltip seams without routing them through the meso formatter, now also keeps an authoritative packet-owned guild-point value of exactly `0` alive across runtime refreshes instead of reviving stale saved totals, narrows the economy gap by teaching the dedicated owner to spend and persist a simulator-owned guild fund using the WZ-authored learn and renewal price formulas instead of treating those authored costs as read-only text, narrows the server-outcome gap by staging packet-owned guild-skill learn and renewal requests behind the shared guild approval path instead of mutating locally up front, further narrows that outcome gap by letting the same approval seam carry authoritative resulting level, remaining-time, and guild-fund values, narrows the footer-layout gap by using strip-constrained wrapping and centered line placement on the loaded `sheet1` strips, narrows the dedicated-owner navigation gap by restoring the authored scrollbar click-drag seam on top of the existing wheel and keyboard paths, narrows the detail-tooltip gap by separating base description text from effect rows while keeping authored line breaks intact, preserving title styling across wrapped fragments, distinguishing no-guild from ordinary member view-only state, keeping hovered guild-skill rows on their authored WZ mouse-over icons, keeping the guild recommendation banner stable instead of cycling after every local cap-state change, keeping active-vs.-inactive timed state visible even for non-manager viewers, and keeps the shared/classic skill-book visibility hard-hide set aligned with the client-confirmed `4321001` / `1014` / `10001015` branch. The remaining gap is now concentrated in true packet-owned guild-fund or ledger feeders beyond those command-fed approval outcomes, true wire-level client packet/result handlers for guild-skill learn or renewal outcomes instead of this extended resolve seam, renewal-expiry or level-up echoes that arrive independently of local approval resolution, plus any still-unmodeled client recommendation edge cases outside the now-covered Dual Blade `400/430` recommendation-root exception. | `SkillUI.cs`, `SkillUIBigBang.cs`, `UIWindowLoader.cs`, `SkillDataLoader.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `GuildSkillWindow.cs`, `GuildSkillTooltipContentBuilder.cs`, `GuildSkillRuntime.cs`, `SocialListRuntime.cs`, `SocialListRuntime.PacketModel.cs`, `UserInfoUI.cs`, `SocialListWindow.cs`, `UnitTest_MapSimulator/GuildSkillRuntimeParityTests.cs`, `UnitTest_MapSimulator/SkillBookVisibilityResolverTests.cs` (`CUISkill::Draw`, `CUISkill::GetSkillRootVisible`, `CUISkill::GetRecommendSKill`, `CUISkill::OnButtonClicked`, `CUISkill::OpenSkillGuide`, `CWvsContext::OnGuildResult(75)`, `UI/UIWindow2.img/Skill/main/BtRide`, `UI/UIWindow2.img/Skill/main/BtGuildSkill`, `UI/UIWindow2.img/Skill/main/Tab/DualTab`, `UI/UIWindow2.img/Skill/main/Tab/AranButton`, `UI/UIWindow2.img/AranSkillGuide`, `UI/UIWindow2.img/UserList/GuildSkill`, `UI/UIWindow2.img/UserList/GuildSkill/iconMouseOver`, `UI/Basic.img/VScr`, `Skill/9100.img`, `String/Skill.img`) |
| Partial | Item/skill tooltip behavior | Skill hover tooltips still reuse the client `UIWindow2.img/Skill/main/tip0..tip2` frames in the skill window, quick-slot skill hovers, status-bar buff tray, and status-bar cooldown tray, with the status-bar buff/cooldown owners keeping the icon-led client layout plus live cooldown description and remaining-time text. This run tightens the reused-frame placement seam instead of adding another owner: the quick-slot, main inventory, trunk, status-bar, and quest log/detail item tooltip surfaces now also consume the authored `tip0..tip2/origin` anchors from the same shared WZ tooltip family rather than stretching those frames onto generic cursor-offset rectangles. Equip hovers still stay on the richer `UIWindow2.img/ToolTip/Equip` path across the Big Bang equipment window, main inventory equip tab, trunk equip rows, drag comparisons, and the dragon/mechanic/android/pet equipment tabs, including authored WZ `tradeBlock` / `only`, minute-precision `dateExpire`, live remaining-vs-total upgrade-slot counts, bitmap-backed star / mesos / growth `itemLEV` / `itemEXP` rows, and simulator-owned equip metadata persistence through unequip, storage, reload, merge, and re-equip flows. This run keeps the shared non-equip item seam and extends authored install, consume, cash, and pet coverage without adding new owners: quick-slot item hovers, main inventory non-equip rows, trunk storage/local-inventory non-equip rows, and the quest log/detail item hovers now also surface authored `spec/onlyPickup`, `spec/notPickupByPet`, `spec/deathmark`, `info/pickupAll`, `info/ignorePickup`, `info/pickUpBlock`, pet-owned `info/{life,hungry,longRange,chatBalloon,nameTag}`, richer `spec/morphRandom` previews, and `spec`/`specEx`-owned pickup plus ally-targeted mob-skill rows on top of the earlier chair recovery, wait-interval, buff-chair, cooldown, `consumeItem`, `recoveryRate`, `randEffect`, `runOnPickup`, authored EXP, level-band, map-range, logout-expiry, no-consume, and class-sharing coverage. WZ was rechecked for concrete owning branches in the current extracted data set before wiring those rows into the shared resolver: `Item/Consume/0202/02022359/spec/{consumeOnPickup,onlyPickup}`, `Item/Consume/0200/02002058/spec/{consumeOnPickup,notPickupByPet,deathmark}`, `Item/Cash/0519/05190004/info/pickupAll`, `05190005/info/ignorePickup`, `05190002/info/longRange`, `Item/Pet/5000040/info/{life,hungry,chatBalloon,nameTag,longRange}`, `Item/Etc/0400/04001074/info/pickUpBlock`, `Item/Consume/0221/02211000/spec/morphRandom/0..8`, `Item/Consume/0202/02022268/specEx/consumeOnPickup`, `02022268/specEx/0/{mobSkill,level,target}`, `Item/Consume/0290/02900001/spec/effectedOnAlly`, `02900049/specEx/{0/{mobSkill,level},effectedOnAlly}`, `Item/Consume/0291/02910000/specEx/0/{mobSkill,level}`, and the shared tooltip-anchor canvases the simulator now consumes directly in these non-skill owners: `UI/UIWindow.img/Skill/main/tip0..tip2/origin` and `UI/UIWindow2.img/Skill/main/tip0..tip2/origin`, with the current extracted `UI/UIWindow2.img/Skill/main/tip0..tip2/origin` vectors still resolving to `(195,-25)`, `(-178,-22)`, and `(103,-282)` for the right, left, and lower frame variants the quest owners now consume too. The earlier non-equip expansion remains in place, including authored `dropBlock`, `noMoveToLocker`, `expRate`, `bonusEXPRate`, `cashExpTicketOn`, `partyExpOn`, and `maxExp` install-item rows, authored personality-exp rows resolved from either `info` or `spec` (`charismaEXP`, `insightEXP`, `willEXP`, `craftEXP`, `senseEXP`, `charmEXP`), authored `create` / `createPeriod`, `period`, and `stateChangeItem` rows, authored `info/replace/itemid`, replacement-duration, and `replace/msg` rows, authored reward-preview expiration rows from `reward/*/period`, quest-name-backed `returnMapQR` labels when quest info is loaded, both the structured and simple numeric-list `consumeItem` shapes, scroll / mastery-book rows for success / cursed rates, target skill names, required skill levels, master levels, scroll stat deltas such as `incINT` / `incCraft`, `reqMap` restrictions, monster-book `mob` ownership, associated mob-card labels, scripted-item / pet-food rows from `spec/script`, `spec/npc`, and `spec/inc`, and the earlier `Item.wz/spec` consumable branches for `moveTo`, cure-status, morph, `party`, `consumeOnPickup`, price, unit-price, level, expiration, trade-block, one-of-a-kind, quest/not-sale, HP/MP recovery, attack/defense, movement, duration, `mhpR` / `mmpR`, `pddR` / `mddR` / `accR` / `evaR`, `asrR`, `booster`, `berserk`, `thaw`, `ignoreContinent`, `returnMapQR`, `indieMhp` / `indieMmp` / `indiePad` / `indieMad` / `indiePdd` / `indieMdd` / `indieSpeed` / `indieJump` / `indieAllStat`, `expBuff` / `expR` / `plusExpRate`, `dropRate` / `dropR`, `mesoR`, `indieBooster`, root or `spec/mob` summon rows, and the authored probabilistic pickup-modifier branches from `spec/mesoupbyitem`, `spec/itemupbyitem`, `spec/itemCode`, and `spec/prob`. This run further tightens the same shared non-equip resolver instead of adding another owner: `specEx` mob-skill rows now localize known player-disease and mapped monster-buff ids through the simulator's existing mob-skill/status mappings while preserving the numeric fallback for unresolved ids. WZ re-check on the same owning branches still shows only numeric ids plus levels in those tooltip families, for example `Item/Consume/0202/02022268/specEx/0/{mobSkill=132,level=1,target=1}` and `Item/Consume/0290/02900049/specEx/0/{mobSkill=799,level=3}`, so the shared resolver now emits client-facing rows such as `Reverse Controls` and `Power Up` when that mapping already exists elsewhere in the simulator instead of leaving those cases on raw `Mob Skill #id` text. Full client `ToolTip/Equip` shell framing, the remaining bitmap-value branches beyond the current mixed slash-sensitive rows, richer growth-instance progress beyond the authored default state, broader potential/additional-option coverage outside the local equipped-item path, true server- or packet-authored instance expiration parity for arbitrary inventory/storage equips, and fuller client-localized non-equip item-tooltip parity for still-unmodeled `info` / `spec` branches are still incomplete | The simulator now has a shared client-style tooltip surface across the main skill HUDs, and the quick-slot, status-bar, inventory, trunk, and quest log/detail owners no longer stop at frame reuse alone because they now honor the WZ-authored tooltip-anchor origins from the same `tip0..tip2` canvases the client family uses. The Big Bang equipment window no longer leaves equipped-item hovers blank, metadata-thin, or drag-state-regressed, companion equipment tabs no longer fall back to item-id-only hover stubs when parsed equip data is available, regular inventory and trunk equip rows no longer collapse back to the older generic bubble when the simulator can resolve live equip metadata, and non-equip item hovers in quick-slot, inventory, trunk, and the quest log/detail windows now surface broader authored WZ item metadata and consumable effects instead of flattening to a sparse name/type/count or name-plus-description stub. Equipment hovers consume more of the client `ToolTip/Equip` art set for labels, badges, cash markers, numeric rendering, trade-block / one-of-a-kind metadata, star rows, mesos rows, and growth rows across the equipment window, inventory equip tab, and trunk; authored WZ expiration dates now surface with minute precision; locally upgraded equipped items keep their richer tooltip metadata as they move through inventory/storage; and the broader generic item path now also covers chair recovery rows with authored wait intervals, buff-chair and cooldown metadata, weighted random chair expressions, authored `recoveryRate` percentages, pickup-execution markers, authored EXP-grant rows, authored EXP item level-range and max-level rows, authored effective level-band rows from `info/lvOptimum` / `info/lvRange`, authored map-range restriction rows from `spec/con/*/sMap..eMap`, authored class-sharing restriction text, logout-expiry and non-consumption flags, teleport destinations, cure-item status rows, authored random-morph previews, party / pickup-trigger markers, pet pickup restrictions, pet loot-mode rows, pet lifespan / hunger / long-range loot / chat-balloon / name-tag rows, percent-based HP/MP and defensive/offensive stat rows, independent flat-stat and attack-speed buffs, scroll or mastery-book info rows, scripted NPC-item markers, pet-food fullness rows, continent / return-map or req-map markers, monster-book or summon-mob rows, raw authored pickup-modifier rows for meso or item pickup effects, authored `create` and `createPeriod` outputs, authored self-expiration `period` rows, authored `replace` result / duration / message rows, reward-box preview expiration rows, linked cash-effect `stateChangeItem` rows, quest-name-backed return-map record labels, both structured and plain numeric `consumeItem` requirement lists, authored coupon EXP-rate aliases, authored EXP-ticket rows from `info/expRate`, `info/bonusEXPRate`, `info/cashExpTicketOn`, `info/partyExpOn`, and `info/maxExp`, authored locker/drop restrictions from `info/noMoveToLocker`, `info/dropBlock`, and `info/pickUpBlock`, authored `spec` / `specEx` mob-skill effect, ally-owner, and target rows, and authored personality EXP rows from either `info` or `spec` through the same shared resolver. This run also narrows the last shared wording seam because known `specEx` mob-skill ids now resolve to client-facing disease/buff labels instead of raw numeric ids when the simulator already has that mapping elsewhere, while unresolved ids still keep the stable numeric fallback and shared `target` metadata. Focused regression coverage for this wording seam is now on disk in `UnitTest_MapSimulator/InventoryItemMetadataResolverParityTests.cs` for `specEx` disease/buff label resolution, shared `target` metadata retention, and the unresolved-id numeric fallback. The remaining gap is now narrower and concentrated in the rest of the client's fuller equip-tooltip shell, the remaining bitmap-value branches beyond these mixed rows, richer growth-instance progress beyond the authored default `1` / `0%` state, broader potential or additional-option coverage outside the local equipped-item path, true server-owned expiration or other packet-authored instance data for arbitrary inventory/storage equips, and more client-localized phrasing plus still-unmodeled non-equip `info` / `spec` families beyond the now-covered recovery, random-chair, pickup-trigger, deathmark, pet-loot, pet cosmetic ids, random-morph, authored EXP, level-band, map-range, and `spec` / `specEx` mob-skill ownership branches | `SkillUIBigBang.cs`, `QuickSlotUI.cs`, `StatusBarUI.cs`, `EquipUIBigBang.cs`, `InventoryUI.cs`, `InventoryItemMetadataResolver.cs`, `TrunkUI.cs`, `TrunkUI.Tooltips.cs`, `QuestUI.cs`, `QuestDetailWindow.cs`, `ItemUpgradeUI.cs`, `CharacterData.cs`, `CharacterLoader.cs`, `UIWindowLoader.cs`, `UILoader.cs`, `MapSimulator.cs`, `StorageAccountStore.cs`, `SimulatorStorageRuntime.cs`, `UnitTest_MapSimulator/InventoryItemMetadataResolverParityTests.cs` |
| Partial | Charge bars and richer skill HUD | The simulator keeps the earlier client-timed prepared-skill gauge set for the supported status-bar branches, including the longer prepare-window `Preparing <n> sec` state, dedicated release-owned `Release` caption path, SG-88 `35121003` `Amplifying <n>%` / `Amplified` captions on `KeyDownBar4`, Dual Blade `4341002` / `4341003` routing to `UI/Basic.img/KeyDownBar1`, the shared supported-family gauge-duration resolver, the normalized `5221004` keyed-HUD branch, the Monkey Wave `prepare/time` seam, and the shared remote prepare/release tables plus caption rules. The remote dragon branch remains inside the existing HUD seam instead of adding a new companion system: when remote packet state carries a real client raw-action code, the Evan-dragon `22121000` / `22151001` overlay still consumes that action detail before falling back to the generic hold `move` / `stand` split, and the remote HUD metadata also preserves the authored `Skill/Dragon/<job>.img` per-action, per-frame `origin.x` values instead of collapsing every explicit dragon branch back to the older `stand`/`move` origin pair. That means remote explicit client dragon actions such as `icebreathe_prepare` and `breathe_prepare` no longer just keep the right timeline; their keyed HUD anchor follows the exported explicit-action origin as the local `DragonCompanionRuntime` seam does, while the remote dragon ground anchor still prefers the remote owner live assembled frame bounds for horizontal placement before falling back to the older actor-center/body-origin approximation and ladder/rope anchoring still follows the client-shaped side offsets. This run further narrows the same remote branch by preserving a per-prepared-HUD resolved dragon action timeline: explicit raw-action dragon branches now measure elapsed animation time from the remote owner's packet action start tick, including replay of the same raw action, while generic fallback `stand` / `move` hold overlays still restart from the prepared-skill hold phase. WZ was rechecked before this change: `Skill/Dragon/2215.img/breathe_prepare/0/origin = (103,143)` and `/1/origin = (102,173)` both carry `delay = 120`, confirming that explicit prepare-frame timing and per-frame origins matter beyond the generic `stand/0/origin = (79,143)` and `move/0/origin = (89,139)` pair; the earlier `Skill/Dragon/2212.img/icebreathe_prepare/0/origin = (71,107)` check still matches the Ice Breath stand origin. The remaining gap is now narrower: unsupported caption edge cases outside these modeled keydown families still remain, and remote dragon ownership still lacks a true packet-owned companion runtime, so follow position and any future client-side dragon-only branching beyond the current raw-action-plus-WZ timeline seam are still inferred from the remote owner body frame, raw action code, owner action tick, and exported `Skill/Dragon/<job>.img` timelines rather than a live remote dragon entity with its own packet-driven state. | Prepared/keydown skills keep the closer client timing and HUD feedback from the earlier parity passes, and remote dragon-owned overlays now stay closer to client ownership because packet-carried raw owner actions can drive explicit dragon timelines while the keyed bar anchor follows the authored explicit dragon-frame origin instead of reusing the generic `stand`/`move` offset pair. The row stays partial because only the supported caption families are modeled and remote dragon behavior is still reconstructed from owner-plus-WZ data rather than a full remote companion runtime, but the current HUD overlay now preserves explicit remote dragon action start continuity across owner raw-action changes and replays. | `PreparedSkillHudRules.cs`, `PreparedSkillHudTextResolver.cs`, `StatusBarUI.cs`, `SkillManager.cs`, `MapSimulator.cs`, `MapSimulator.RemoteUsers.cs`, `RemoteUserActorPool.cs`, `PlayerCharacter.cs`, `DragonCompanionRuntime.cs`, `DragonActionLoader.cs`, `CharacterData.cs`, `UnitTest_MapSimulator/PreparedSkillHudParityTests.cs`, `UnitTest_MapSimulator/RemoteDragonPreparedSkillHudParityTests.cs` (`CUserLocal::DrawKeyDownBar`, `CDragon::CreateKeyDownBar`, `CDragon::DrawKeyDownBar`, `CUserLocal::Update`, `CUserLocal::TryDoingPreparedSkill`, `is_keydown_skill`, `get_max_gauge_time`, `UI/Basic.img/KeyDownBar*/bar/origin`, `Skill/522.img/skill/5221004`, `Skill/531.img/skill/5311002/prepare/time`, `Skill/3310.img/skill/33101005`, `Skill/3310.img/skill/33101007`, `Skill/Dragon/<job>.img`, `String/Skill.img/35121003`) |

### 2. Additional quick-slot validation and skill-book recommendation owners discovered by targeted UI decompiles

The latest UI decompiles exposed two owner seams that were still only implied inside the broad status-bar and skill-window rows.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Quick-slot validation parity | WZ re-check still confirms the primary quick-slot owners the simulator is using for this seam: `UI/StatusBar2.img/mainBar/quickSlot/quickSlot` remains the status-bar surface and `UI/UIWindow2.img/KeyConfig/quickslotConfig` remains the settings owner for the eight-slot primary bar. This run moves the primary validation owner onto that same status-bar quick-slot seam instead of leaving all validation implied under the broader hotkey manager: `QuickSlotUI` now keeps an eight-slot compare-and-validate snapshot for the primary bar, revalidates only that primary range before rebuilding the cached presentation, and lets stale learned-skill, macro, and item bindings fall out through the shared hotkey validation path before the bar redraws. The item side is now stricter too. `SkillManager` no longer treats any owned `USE` / `CASH` entry as quick-slot-visible; it now revalidates live counts, clears empty item bindings instead of leaving zero-count ghosts behind, and restricts quick-slot item bindings to the client-observed consumable families from `CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo` that the simulator can currently model directly (`200`, `201`, `202`, `205`, `210`, `212`, `221`, `226`, `227`, `236`, `238`, `245`, plus the currently verified cash-item quick-slot families already present in the simulator inventory flows). The row is still partial because the simulator still approximates the client's two cash-item helper predicates with a narrower family allowlist rather than reproducing `get_cashslot_item_type(...) == 9` and `get_etc_cash_item_type(...) == 6` byte-for-byte, and the simulator's immediate-mode renderer does not currently consume the compare result as a true redraw short-circuit the way the client cache does. | Quick-slot parity is now anchored to the same owner seam the client uses instead of being left as an implicit side effect of broad hotkey cleanup, so the eight primary quick slots now revalidate stale learned skills and live item quantities inside the status-bar quick-slot owner before presentation rebuild. Empty consumable stacks now clear out of the bar instead of surviving as zero-count ghosts, and unsupported non-client quick-slot item families no longer pass through the assignment or restore path just because they live in `USE` or `CASH`. The remaining gap is narrower and limited to the exact client cash-item helper discrimination plus the client's cached redraw/no-redraw decision path. | `QuickSlotUI.cs`, `SkillManager.cs`, `MapSimulator.ChatCommands.cs` (`CUIStatusBar::CQuickSlot::CompareValidateFuncKeyMappedInfo`, `CFuncKeyMappedMan`, `CQuickslotKeyMappedMan`, `CWvsContext::GetCharacterData`, `UI/StatusBar2.img/mainBar/quickSlot/quickSlot`, `UI/UIWindow2.img/KeyConfig/quickslotConfig`) |
| Partial | Skill-root visibility and recommendation parity | This run moves the simulator onto the client-owned skill-root visibility and recommendation seams instead of treating them as static skill-book data. `SkillDataLoader` now preserves invisible and time-limited skill entries in the loaded root data, `SkillUIBigBang` now applies a live client-shaped visibility filter before drawing or counting spent SP so unlearned invisible/time-limited entries stay hidden while learned record-owned entries remain visible, the confirmed client hard hides now suppress beginner `1014` plus the Dual Blade-born `4000001` / `4001344` pair, and the recommendation path now loads and sorts the full `Etc/RecommendSkill.img/<skillRoot>` SP-threshold table without pre-filtering it to the simulator's currently visible skills before the existing Dual Blade `400 + 430` shared-table / combined-SP branch resolves the highlight. The remaining gap is narrower and limited to the full `CSkillInfo::GetSkillRootVisible` root-object refresh path: the simulator still decides which tabs/books exist from its job-path helper instead of rebuilding the visible root list directly from client root metadata plus skill records, and two additional always-hidden skill ids in the client `IsSkillVisible` branch still need firmer IDA/WZ confirmation before they can be mirrored safely. | Skill-window parity is broader than icon layout and hover text. The client owns both the per-root visibility filter and the recommend-skill threshold cache, so mirroring those owners keeps the highlight and visible root contents attached to client data rather than simulator-side heuristics. After this run, hidden skill entries and recommend highlights follow the same live root rules more closely instead of being flattened away during WZ load. | `SkillDataLoader.cs`, `SkillUIBigBang.cs`, `SkillRootVisibilityResolver.cs`, `SkillRootRecommendationResolver.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/SkillRootParityTests.cs` (`CUISkill::GetSkillRootVisible`, `CUISkill::GetRecommendSKill`, `CSkillInfo::GetSkillRootVisible`, `CSkillInfo::IsSkillVisible`, `Etc/RecommendSkill.img`) |

## Priority Order

If the goal is visible parity first, the next work should be sequenced like this:

1. Avatar parity pass:
   Validate default asset selection, client-managed overlay/effect layers, and remaining action coverage against WZ and client behavior.
2. Skill execution pass:
   Implement queued follow-up, repeat-skill, bound-jump, rocket-booster teardown, and movement-coupled shoot families rather than extending the generic cast path indefinitely.
3. UI feedback pass:
   Add quick-slot validation, quest or balloon feedback, chat surfaces, and complete skill-window behavior.
4. Movement refinement pass:
   Remove the remaining ladder/float stubs, add passive transfer-field handoff, and tighten platform/dynamic foothold sync.
5. Interaction pass:
   Add NPC talk/quest flow, follow and direction-mode handling, and richer reactor interactions.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".


