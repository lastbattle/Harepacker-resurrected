# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Effects/SpecialEffectFields.cs`
- `HaCreator/MapSimulator/Fields/MinigameFields.cs`

### Client references checked

- `CField_GuildBoss::CField_GuildBoss` at `0x53e980`
- `CField_Coconut::OnClock` at `0x54a100`
- `CField_Coconut::OnCoconutHit` at `0x54a470`
- `CField_Coconut::BasicActionAttack` at `0x54a5e0`
- `CField_Coconut::DrawBoard` at `0x54a750`
- `CField_Coconut::OnCoconutScore` at `0x54bf70`
- `CField_AriantArena::OnUserScore` at `0x5492b0`
- `CField_AriantArena::OnShowResult` at `0x547630`
- `CField_Battlefield::OnClock` at `0x549ad0`
- `CField_Battlefield::OnScoreUpdate` at `0x5499a0`
- `CField_Battlefield::OnTeamChanged` at `0x5499e0`
- `CField_Battlefield::SetUserTeam` at `0x549870`
- `CField_Dojang::OnClock` at `0x550940`
- `CField_Dojang::Update` at `0x54ef10`
- `CField_CookieHouse::_UpdatePoint` at `0x54daa0`
- `CField_CookieHouse::Init` at `0x54e250`
- `CField_Massacre::OnClock` at `0x556af0`
- `CField_Massacre::UpdateKeyAnimation` at `0x556bf0`
- `CTimerboard_Massacre::Draw` at `0x557100`
- `CField_Massacre::Init` at `0x5579d0`
- `CMemoryGameDlg::OnButtonClicked` at `0x628df0`
- `CMemoryGameDlg::OnCreate` at `0x62f3c0`
- `CMemoryGameDlg::Draw` at `0x631cb0`
- `CField_MonsterCarnival::CreateUIWindow` at `0x55a510`
- `CField_MonsterCarnival::OnEnter` at `0x55a6c0`
- `CField_MonsterCarnival::OnRequestResult` at `0x55a890`
- `CField_MonsterCarnival::OnShowGameResult` at `0x55af80`
- `CField_MonsterCarnivalRevive::OnShowGameResult` at `0x55b110`
- `CField_PartyRaid::_UpdatePoint` at `0x55c120`
- `CField_PartyRaid::Init` at `0x55f110`
- `CField_PartyRaidBoss::OnFieldSetVariable` at `0x55ca40`
- `CField_PartyRaidBoss::OnPartyValue` at `0x55d070`
- `CField_PartyRaidResult::OnSessionValue` at `0x55e5c0`
- `CField_SnowBall::OnSnowBallTouch` at `0x560510`
- `CField_SnowBall::OnSnowBallState` at `0x560ab0`
- `CField_SnowBall::BasicActionAttack` at `0x5617b0`
- `CField_SnowBall::OnSnowBallHit` at `0x5619d0`
- `CField_SnowBall::OnSnowBallMsg` at `0x562040`
- `CField_SpaceGAGA::OnClock` at `0x5625d0`
- `CTimerboard_SpaceGAGA::Draw` at `0x5626c0`
- `CField_Wedding::SetBlessEffect` at `0x563b60`
- `CField_Wedding::OnWeddingProgress` at `0x5640f0`
- `CField_Witchtower::OnScoreUpdate` at `0x564ad0`
- `CScoreboard_Witchtower::Draw` at `0x564bd0`
- `CField_ContiMove::OnStartShipMoveField` at `0x54c970`
- `CField_ContiMove::OnContiState` at `0x54d5a0`
- `CField_KillCount::OnPacket` at `0x554050`
- `CField_LimitedView::Init` at `0x5554a0`
- `CField_HuntingAdballoon::OnFieldSetVariable` at `0x552050`
- `CField_MassacreResult::OnPacket` at `0x55a1d0`
- `CField_EscortResult::OnSessionValue` at `0x551160`
- `CField_Tournament::OnPacket` at `0x563780`
- `CRPSGameDlg::OnPacket` at `0x6d9e00`
- `CField_ShowaBath::DecodeFieldSpecificData` at `0x55ff70`
- `CField_Tutorial::DecodeFieldSpecificData` at `0x563830`
- `CField_MonsterCarnivalWaitingRoom::Init` at `0x55bda0`
- `CField_MonsterCarnivalS2_Game::Init` at `0x55bf10`
- `CField_NoDragon::Init` at `0x55c0e0`
- `CField_WeddingPhoto::GetFieldType` at `0x53eee0`
- `CField_AranTutorial::GetFieldType` at `0x53f1c0`
- `CField_Balrog::GetFieldType` at `0x549810`

## Client Function Index By Backlog Area

The list below maps this backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Special field and minigame parity

- `CField_GuildBoss::CField_GuildBoss` at `0x53e980`
- `CField_Coconut::OnClock` at `0x54a100`
- `CField_Coconut::OnCoconutHit` at `0x54a470`
- `CField_Coconut::BasicActionAttack` at `0x54a5e0`
- `CField_Coconut::DrawBoard` at `0x54a750`
- `CField_AriantArena::OnUserScore` at `0x5492b0`
- `CField_AriantArena::OnShowResult` at `0x547630`
- `CField_Battlefield::OnClock` at `0x549ad0`
- `CField_Battlefield::OnScoreUpdate` at `0x5499a0`
- `CField_Battlefield::OnTeamChanged` at `0x5499e0`
- `CField_Battlefield::SetUserTeam` at `0x549870`
- `CField_Dojang::OnClock` at `0x550940`
- `CField_Dojang::Update` at `0x54ef10`
- `CField_CookieHouse::_UpdatePoint` at `0x54daa0`
- `CField_CookieHouse::Init` at `0x54e250`
- `CField_Wedding::OnWeddingProgress` at `0x5640f0`
- `CField_Wedding::SetBlessEffect` at `0x563b60`
- `CField_Witchtower::OnScoreUpdate` at `0x564ad0`
- `CScoreboard_Witchtower::Draw` at `0x564bd0`
- `CField_Massacre::OnClock` at `0x556af0`
- `CField_Massacre::UpdateKeyAnimation` at `0x556bf0`
- `CTimerboard_Massacre::Draw` at `0x557100`
- `CMemoryGameDlg::OnButtonClicked` at `0x628df0`
- `CMemoryGameDlg::OnCreate` at `0x62f3c0`
- `CMemoryGameDlg::Draw` at `0x631cb0`
- `CField_SnowBall::OnSnowBallState` at `0x560ab0`
- `CField_SnowBall::OnSnowBallHit` at `0x5619d0`
- `CField_SnowBall::OnSnowBallMsg` at `0x562040`
- `CField_SnowBall::OnSnowBallTouch` at `0x560510`
- `CField_MonsterCarnival::CreateUIWindow` at `0x55a510`
- `CField_MonsterCarnival::OnEnter` at `0x55a6c0`
- `CField_PartyRaid::Init` at `0x55f110`
- `CField_PartyRaidBoss::OnFieldSetVariable` at `0x55ca40`
- `CField_PartyRaidResult::OnSessionValue` at `0x55e5c0`
- `CField_SpaceGAGA::OnClock` at `0x5625d0`
- `CTimerboard_SpaceGAGA::Draw` at `0x5626c0`
- `CField_ContiMove::OnStartShipMoveField` at `0x54c970`
- `CField_ContiMove::OnContiState` at `0x54d5a0`
- `CField_KillCount::OnPacket` at `0x554050`
- `CField_LimitedView::Init` at `0x5554a0`
- `CField_HuntingAdballoon::OnFieldSetVariable` at `0x552050`
- `CField_MassacreResult::OnPacket` at `0x55a1d0`
- `CField_EscortResult::OnSessionValue` at `0x551160`
- `CField_Tournament::OnPacket` at `0x563780`
- `CRPSGameDlg::OnPacket` at `0x6d9e00`
- `CField_ShowaBath::DecodeFieldSpecificData` at `0x55ff70`
- `CField_Tutorial::DecodeFieldSpecificData` at `0x563830`
- `CField_MonsterCarnivalWaitingRoom::Init` at `0x55bda0`
- `CField_MonsterCarnivalS2_Game::Init` at `0x55bf10`
- `CField_NoDragon::Init` at `0x55c0e0`
- `CField_WeddingPhoto::GetFieldType` at `0x53eee0`
- `CField_AranTutorial::GetFieldType` at `0x53f1c0`
- `CField_Balrog::GetFieldType` at `0x549810`

Notes:
IDA shows that these field modes are not generic map-rule variants. The client owns separate state machines, packet handlers, timer or scoreboard widgets, and even dedicated minigame dialog owners such as `CMemoryGameDlg` for Guild Boss, Coconut, Wedding, Witchtower, Massacre, SnowBall, Ariant Arena, Battlefield, Dojang, Cookie House, Monster Carnival, Party Raid, SpaceGAGA, and MiniRoom card games, so parity work should be tracked separately from the broader field, combat, or UI backlog areas.
The live `list_functions` scan also surfaced additional field-owner classes with no hits anywhere else in the parity backlog set, which means there is still real untracked special-field work outside the rows already listed below.

### 2. Additional packet-owned microgame dialog discovered by follow-up IDA scan

- `CRPSGameDlg::OnPacket` at `0x6d9e00`
- `CRPSGameDlg::ProcessPacket` at `0x6d72d0`
- `CUtilDlgEx` notice path (`StringPool` `0xE83`) via `CRPSGameDlg::OnPacket`

Notes:
`CRPSGameDlg` is not a generic field-utility callback; it is a dedicated packet-owned dialog owner with create/destroy and round-state branches. Keep this indexed separately from the broader special-field owner list.

### 3. Additional client-owned field wrappers discovered by IDA function scan

- `CField::FieldFactory`
- `CField_DynamicFoothold::GetFieldType`
- `CField_DynamicFoothold::Init` at `0x551050`
- `CField_ContiMove::OnContiState` at `0x54d5a0`
- `CField_Tutorial::DecodeFieldSpecificData` at `0x563830`
- `CField_LimitedView::Init` at `0x5554a0`

Notes:
These wrapper owners are intentionally indexed as a separate scan-discovery surface because they represent field-class ownership seams (transport/tutorial/presentation/dynamic-foothold wrappers) rather than minigame-specific rules.

## Current State Summary

MapSimulator already has a partial special-field baseline instead of a blank area.
The project currently includes:

- A `SpecialEffectFields` surface with wedding, witchtower, guild-boss, and massacre handling.
- A concrete `MinigameFields` surface that now owns the snowball and coconut runtimes behind one backlog-aligned seam.
- A `SpecialFieldRuntimeCoordinator` seam in `MapSimulator` that binds special-field ownership to map load, update, draw, and reset flow so later agents can take one backlog row without redoing the simulator plumbing.
- Several packet-commented simulator seams that already reference the corresponding client field owners.

The parity gap here is not whether the simulator recognizes special maps at all.
The gap is whether those maps reproduce the client's dedicated rules, scoreboards, timers, UI flows, and win or lose transitions.

## Remaining Parity Backlog

### 1. Special field and minigame parity

This area is now tracked in its own backlog document because both the client and simulator already expose dedicated field-specific seams.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Guild boss event fields | The simulator still routes special-field ownership through `SpecialFieldRuntimeCoordinator`, auto-binds guild-boss maps by the actual WZ `healer` plus `pulley` shape instead of only old map-id ranges, and configures `GuildBossField` directly from those map-root nodes. A fresh WZ re-check in this pass kept that evidence pinned to the same guild-boss contract: `Map/Map9/990000900.img` still carries healer values `x=295`, `yMin=-564`, `yMax=-350`, `rise=1`, `fall=10`, `healMin=1000`, `healMax=2000` plus pulley placement `x=-59`, `y=-394`, and those nodes still point at `Map/Obj/guild.img/syarenian/boss/{0,1}` whose current object data still exposes the same 18 healer frames at origin `124,199` with `1800/180/...` delays and the same 3 pulley-hit frames at origin `184,181` with `90` ms delays. IDA re-checks also kept the client transport seam unchanged: `CField_GuildBoss::BasicActionAttack` at `0x5517d0` still hits the pulley locally through `CPulley::Hit` and then sends a bare `COutPacket(259)` with no extra payload. The runtime still mirrors that behavior through `/guildboss packet <344|345> ...`, `/guildboss packetraw <hex>`, loopback transport `packetoutraw 0301`, and the in-process official-session proxy path, and this pass adds the missing automated coverage for what the row now claims: `UnitTest_MapSimulator/GuildBossOfficialSessionBridgeManagerTests.cs` covers occupied-port failure reporting, same-target idempotent start, local-port-filtered discovery for `startauto`, and attached-session preservation across a conflicting restart attempt, while `UnitTest_MapSimulator/GuildBossPacketTransportManagerTests.cs` covers raw guild-boss packet decode and the literal outbound `packetoutraw 0301` plus legacy `pulleyhit <sequence> <tick>` emission. Executing The remaining gap is now narrow and unchanged in kind: the simulator can proxy, decrypt, dispatch, and safely preserve guild-boss traffic inside its own runtime seam, but it still does not attach directly to an already-established Maple client socket pair without a localhost proxy hop first | Guild boss now has row-specific automated verification for both official-session bridge ownership and the loopback transport seam, in addition to the existing WZ-backed map/object wiring and IDA-backed opcode-`259` attack path. The row stays `Partial` only because true in-place live-session attachment is still missing; the remaining work is no longer about guild-boss map detection, art loading, packet decode, outbound opcode recovery, transport emission, discovery filtering, or proxy restart safety. | `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `SpecialFieldRuntimeCoordinator.cs`, `SpecialEffectFields.cs`, `GuildBossPacketTransportManager.cs`, `GuildBossOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/GuildBossOfficialSessionBridgeManagerTests.cs`, `UnitTest_MapSimulator/GuildBossPacketTransportManagerTests.cs`, `GuildBossField` (`CField_GuildBoss`, `CField_GuildBoss::Init`, `CField_GuildBoss::OnPacket`, `CField_GuildBoss::BasicActionAttack`, `CHealer::Init`, `CHealer::Move`, `CPulley::Init`, `CPulley::Hit`) |
| Partial | Coconut minigame runtime | The simulator now follows the client's finish-timestamp clocking, keeps score packet-owned instead of inventing local score on ground contact, applies packet-driven coconut state changes through the runtime's delayed hit queue, mirrors `CField_Coconut::BasicActionAttack` by selecting an intersecting coconut from the player's normal-attack world hitbox, and keeps that request transport-owned when a bridge client is present instead of replaying a simulator-only delayed hit preview. WZ re-checks against the exported IMG filesystem in this pass kept the board art pinned to `Map/Obj/etc.img/coconut/backgrnd` (`258x101`) and the result banners pinned to `Map/Effect.img/event/coconut/{victory,lose}` with the same three-frame timing and recovered delays (`120`, `120`, `3000`) for both banners, while IDA re-checks of `CField_Coconut::OnPacket` at `0x54bfb0`, `CField_Coconut::OnCoconutHit` at `0x54a470`, `CField_Coconut::OnCoconutScore` at `0x54bf70`, and `CField_Coconut::BasicActionAttack` at `0x54a5e0` kept the client packet ownership unchanged: `342` and `343` still dispatch through `OnPacket`, hit packets still queue `target/delay/state` against `get_update_time()`, score packets still just redraw the board, `OnClock` still only refreshes the finish timestamp, and outbound attacks still send `COutPacket(257)` with `Encode2(targetId)` then `Encode2(tDelay)`. The dedicated Coconut line transport still auto-flushes pending requests once a bridge client connects, emits both the compatibility `attack <targetId> <delay> <requestedAtTick>` line and the decrypted Maple outbound frame `packetoutraw <hex>`, accepts opcode-wrapped inbound Coconut packets from both the loopback inbox and `/coconut raw packetraw <hex>`, and ignores echoed opcode-`257` attack frames. The built-in Coconut official-session proxy on top of `MapleLib.PacketLib.Session` remains in place: `/coconut session start <listenPort> <serverHost> <serverPort>` still accepts a real Maple client connection through a local proxy, mirrors the Maple init handshake, decrypts live server-authored Coconut `342/343` packets directly into the existing packet-owned runtime seam, suppresses the simulator-only delayed-hit preview while that live session is attached, and injects opcode `257` back into the proxied live session without any external line bridge process. In this pass the runtime also tightened a round-boundary defect inside the existing seam: a fresh positive `/clock` or packet-owned round start now clears any stale delayed-hit entries before the next update tick, so old queued Coconut hits cannot leak into the next round after the client-owned finish-timestamp reset. The bridge's TCP-owner-table discovery still disambiguates already-running MapleStory sessions by local TCP port, so `/coconut session discover <remotePort> [processName|pid] [localPort]` can narrow a multi-session list to one established connection and `/coconut session startauto <listenPort> <remotePort> [processName|pid] [localPort]` can start the proxy directly from that filtered candidate instead of falling back to a manual host copy when multiple sessions share the same remote port. Focused Coconut parity tests are now on disk in `UnitTest_MapSimulator/CoconutParityTests.cs` for stale-hit-queue clearing on positive clock resets, packet-owned attack-request acknowledgement, raw opcode-`257` echo suppression, opcode-wrapped score payload decode, local-port-scoped discovery candidate resolution, ambiguous discovery diagnostics, and occupied-port startup failure. Executingcs`, `CS1026`). The remaining gap is narrower but still real: Coconut still does not bind in place to an already-established MapleStory socket session, and live opcode `257` injection still depends on the simulator owning a proxy hop rather than attaching directly to the existing client socket pair. | Coconut now covers both transport ownership modes inside the simulator itself, the packet-owned round clock no longer leaves stale delayed hits armed across a fresh round start, and the official-session seam is less manual than before: the older loopback line bridge remains available, the in-process Maple session proxy can decrypt official Coconut hit or score packets and inject opcode `257` on its own, discovery can enumerate matching live MapleStory TCP endpoints, and multi-session cases can be disambiguated inside the simulator by local TCP port before proxy startup. The remaining work is no longer unresolved Coconut packet shape, preview ownership, stale delayed-hit carryover, manual remote-endpoint lookup, or multi-session disambiguation; it is specifically the last step from simulator-owned proxying to true live-session attachment and in-place socket ownership without redirecting the client through localhost first. | `MinigameFields.cs`, `MapSimulator.cs`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.ChatCommands.cs`, `CoconutPacketInboxManager.cs`, `CoconutOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/CoconutParityTests.cs`, `CoconutField` (`CField_Coconut::OnPacket`, `CField_Coconut::OnClock`, `CField_Coconut::OnCoconutHit`, `CField_Coconut::OnCoconutScore`, `CField_Coconut::BasicActionAttack`, `CField_Coconut::DrawBoard`) |
| Partial | Wedding ceremony fields | The simulator now resolves ceremony lines from `String/Npc.img/<npcId>/wedding{step}` with WZ-backed fallbacks, distinguishes groom or bride versus guest state from the local character id, mirrors the chapel-only step-2 guest bless prompt that `CField_Wedding::OnWeddingProgress` special-cases, records the client's ceremony response packet flow through step-advance (`163`) and guest-bless (`164`) responses, exposes a `/wedding` debug seam to drive progress, actor anchors, guest audience overlays, and responses through that same runtime, loads the real bless animation frames from `Effect/BasicEff.img/Wedding`, swaps BGM to the WZ-backed `Sound/BgmEvent.img/wedding` track from the same step-0 seam the client uses, restores the map BGM when the wedding runtime clears, draws the WZ-backed declaration overlay from `UI/UIWindow.img/wedding/text/0` during the opening step, and promotes the later pronouncement and blessing steps into the second recovered wedding scene layer from `UI/UIWindow.img/wedding/card/0` with the existing wedding weather celebration path. A fresh WZ re-check in this pass kept that asset evidence pinned to the same recovered shapes in the current IMG filesystem: `UI/UIWindow.img/wedding` still only exposes `text/0` (`518x659`, origin `233,306`) plus `card/0` (`295x168`, origin `147,84`), `Effect/BasicEff.img/Wedding` still exposes the 16-frame bless animation with the same `120/180/300` timing pattern, and `Map/MapHelper.img/weather/wedding` still exposes the recovered wedding weather particle source. IDA re-checks of `CField_Wedding::OnWeddingProgress` at `0x5640f0` and `CField_Wedding::OnWeddingCeremonyEnd` at `0x5640a0` also tightened the ceremony-dialog owner in this pass: the client closes any existing `CUtilDlgEx` before opening the next wedding dialog, does not own a local timeout-driven auto-dismiss path for those modals, and clears the active dialog again before `SetBlessEffect` when the ceremony-end packet arrives. `WeddingField` now mirrors that client-owned modal lifecycle inside the existing seam by dismissing any live wedding dialog on step changes or ceremony end instead of letting the old dialog linger, and by keeping both participant text dialogs and the chapel guest bless prompt open until an explicit response path closes them rather than auto-closing them on a local five-second timer. The existing wedding-side remote actor seam remains in place too: `/wedding inbox ...` still accepts the adjacent client-shaped remote-user packet aliases for spawn, leave, move, chair, avatar refresh, and temporary-stat set/reset; `WeddingField` still routes those payloads through the shared remote-user packet codec; the recovered `SecondaryStat::DecodeForRemote` presentation slice still survives into groom, bride, and guest actors through the delayed-load and shared-pool handoff; audience actors can still be promoted into groom or bride overlays when ceremony ids later identify them; real remote character ids still survive into the shared wedding renderer path; and packet-backed `PlayerMovementSyncSnapshot` data still lets groom, bride, and guest actors follow `CMovePath` playback instead of teleporting between static overlays. Focused wedding ceremony tests are now on disk in `UnitTest_MapSimulator/WeddingFieldCeremonyParityTests.cs` for groom-dialog modal persistence, chapel guest-bless modal persistence, and ceremony-end dialog dismissal before the bless effect starts, althoughCashServiceStageParity.cs`. The remaining gap is narrower and explicit: the simulator still only models the recovered presentation-relevant `SecondaryStat::DecodeForRemote` slice rather than the full remote temporary-stat surface, it still does not own the full client name-tag refresh lifecycle beyond its existing renderer labels and `OnUserEnterField` guild-tag refresh evidence, and the ceremony scene choreography is still limited to the recovered WZ text/card layers, modal dialog flow, bless effect, and weather particles rather than a fully decoded client wedding timeline | Wedding maps now cover both recovered WZ ceremony overlays and a client-backed modal dialog lifecycle inside the existing wedding seam in addition to the dedicated wedding-side remote actor path for the couple plus guest audience. The remaining work is no longer basic modal ownership, dialog cleanup, or the absence of remote temporary-stat/movement presentation; it is concentrated on the unrecovered temporary-stat surface, the full client name-tag owner flow, and any additional client-only ceremony sequencing beyond the WZ-backed layers and modal behavior already identified. | `SpecialEffectFields.cs`, `WeddingField`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.WeddingRemoteUsers.cs`, `MapSimulator.UpdateLoop.cs`, `RemoteUserPacketCodec.cs`, `WeddingPacketInboxManager.cs`, `UnitTest_MapSimulator/WeddingFieldCeremonyParityTests.cs` (`CField_Wedding::OnWeddingProgress`, `CField_Wedding::OnWeddingCeremonyEnd`, `CField_Wedding::SetBlessEffect`, `CUserPool::OnUserEnterField`, `CUserPool::OnUserLeaveField`, `CUserPool::Update`, `CUserPool::OnUserRemotePacket`, `SecondaryStat::DecodeForRemote`, `CUserRemote::OnMove`, `CUserRemote::OnAvatarModified`, `CUserRemote::Update`) |
| Implemented | Witchtower score UI | The simulator anchors the Witchtower scoreboard to the client's center-top window origin, loads the exact `Map/Obj/etc.img/goldkey` background, key, and bitmap digits that match the `CScoreboard_Witchtower::OnCreate` asset shape, renders the zero-padded score at the verified `(67, 4)` draw origin with the client's negative digit spacing, and keeps the `witchscore` debug seam driving the same update path | This is a visible special-field HUD with client-backed art and placement now, so the remaining special-field work should move to other rows instead of revisiting a placeholder Witchtower overlay | `SpecialEffectFields.cs`, `WitchtowerField`, `MapSimulator.cs` (`CField_Witchtower::OnScoreUpdate`, `CScoreboard_Witchtower::OnCreate`, `CScoreboard_Witchtower::Draw`) |
| Partial | Massacre timerboard and gauge flow | The simulator now mirrors the client's type-2 timerboard ownership at the verified center-top origin, draws a dedicated MM:SS timer surface instead of a generic corner clock, keeps the timer digits alive even without a fallback `SpriteFont`, probes the loaded `UIWindow(.2).img` trees for a real `258x61` timerboard source canvas before falling back to the older `MonsterKilling/Count/backgrd{0,1}` approximation, and keeps the timer-digit source pinned to the same `MonsterKilling/Count/number` image-font subtree that `CTimerboard_Massacre::Draw` uses through `draw_text_by_image`. The runtime also carries the client-owned `CField_Massacre::_SetMassacreInfo` count board: it draws the WZ-backed `UI/UIWindow(.2).img/MonsterKilling/Count/backgrd{0,1}` panel with `number2` bitmap digits at the client draw offsets (`hit 57,11`, `cool 57,33`, `miss 57,55`, `skill 80,85`), exposes the existing `/massacre info <hit> <miss> <cool> [skill]` seam, and keeps `keyBackgrd/{open,ing,close}` on the recovered `nSkill` lifecycle instead of inventing a gauge-owned pulse. In this pass the in-field gauge owner moved closer to the client contracts recovered from `CField_Massacre::_SetDecGauge` at `0x556040` and `_SetMassacreInfo` at `0x555f90`: the draw path still uses the client's `259`-pixel gauge span and the same danger cutoff recovered from `_SetDecGauge`, but it no longer layers simulator-authored kill/combo/current-gauge text, next-threshold labels, or threshold-reached banners on top of the WZ-owned `MonsterKilling/Gauge` art, so the visible HUD now stays on the client-backed `backgrd`, `text`, `pixel`, `backgrdD`, `textD`, `iconD`, and `danger` surfaces instead of mixing in local overlays. The `_SetMassacreInfo` side also tightened around the client branch recovered in IDA: disable-skill maps still suppress the key animation owner, but the runtime now preserves the raw `nSkill` value internally instead of forcing it to zero, matching the client more closely while still hiding the skill board when `mobMassacre/disableSkill` is set. The result owner remains packet-owned and still follows the draw contract recovered from `CField_MassacreResult::OnMassacreResult` at `0x5596c0`: packet `174` stays `<rank:1><score:4>`, the status surface still exposes the recovered result ids `0x151C` and `0x151D`, the board still follows the recovered center-origin layer offset `(-193,-142)` from `Origin_CC`, the simulator still loads both `Result/number` and `Result/number2`, and it still draws the three small KILL/COOL/MISS rate readouts plus their mirrored percentage values at the recovered offsets (`218,63`, `218,87`, `218,111` and `265,63`, `265,87`, `265,111`) before drawing the big EXP score at `258,168`. WZ re-checks against the exported IMG filesystem in this pass kept that evidence pinned to the same client-owned roots: `MonsterKilling/Result/backgrd` is still `386x218`, `backgrd2` still owns the pulse overlay, `Rank/{s,a,b,c,d}` still owns the letter grades, `Result/number` and `Result/number2` still own the small and big digits, `Map/Effect.img/killing/yeti0` through `yeti4` still own the rank effects, and both `UIWindow.img` plus `UIWindow2.img` still expose the same `MonsterKilling` gauge/count/result trees. The dedicated loopback inbox seam remains in place and still auto-starts on Massacre maps, exposes `/massacre inbox [status|start [port]|stop]`, accepts raw opcode-wrapped packet `173` lines, accepts raw client-shaped type-2 clock payloads (`<type:1><seconds:4>`), accepts raw client-shaped `CWvsContext::Massacre` snapshots (`<hit><miss><cool><skill>` as four little-endian ints), and routes those feeds into the existing timerboard, count-board, and gauge seams instead of requiring only manual per-command authoring. The built-in `/massacre session [status|discover|start|startauto|stop]` Maple proxy seam also remains in place: it initializes Maple crypto from the real handshake, forwards live client/server traffic, discovers established MapleStory TCP sessions with the same optional local-port disambiguation helper used by the other special-field bridges, and drains live opcode `173/174` packets back into the existing `MassacreField.TryApplyPacket` seam instead of requiring result and inc-gauge payloads to be retyped by hand. Focused parity tests are now on disk in `UnitTest_MapSimulator/MassacreFieldParityTests.cs` for the recovered key-animation zero-crossing behavior, disable-skill `nSkill` preservation, positive-delta-only packet-`173` kill counting, and packet-`174` rank or score decode into the existing result-owner status seam. The remaining gap is now narrower and explicit: the exact `StringPool` literals behind `0x14EE`, `0x14EC`, the recovered `0x1510`-`0x151D` ids, and the rank-effect string ids reached from `CField_MassacreResult::OnMassacreResult` are still not decoded byte-for-byte, the official-session seam still only proxies the recovered live opcodes `173/174` and still depends on a localhost proxy hop rather than attaching directly to an already-running socket pair, the in-field clear-vs-fail transition is still simulator-authored rather than recovered from the official result owner, and the fuller server-owned Massacre clock/context/result choreography beyond the recovered inc-gauge and result packets is still missing | Hunting-event maps depend on dedicated timer, point-board, skill-key, gauge, and result-owner behavior that the client does not route through the normal status bar, and the simulator now covers the client-backed HUD art, timerboard placement, key lifecycle, map-default gauge rules, stage and bonus presentations, the recovered rank-plus-score result payload shape, the result board's client-backed center-origin placement plus KILL/COOL/MISS rate rows, the WZ-backed rank-specific result effect layer, the removal of simulator-authored gauge text from the visible HUD, and a built-in official-session proxy seam for the recovered live Massacre packet subset in addition to the older loopback ownership path. The remaining work is now concentrated on unresolved `StringPool` text literals, direct in-place live-session attachment plus the unrecovered live clock/context/result choreography, and the last clear/fail result-owner transition rather than on missing timerboard, count-board, gauge, score, rank, or result-rate runtime ownership. | `SpecialEffectFields.cs`, `MassacreField`, `MapSimulator.cs`, `MapSimulator.Content.cs`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.ChatCommands.cs`, `MassacrePacketInboxManager.cs`, `MassacreOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/MassacreFieldParityTests.cs` (`CField_Massacre::Init`, `CField_Massacre::_SetMassacreInfo`, `CField_Massacre::_UpdatePoint`, `CField_Massacre::OnPacket`, `CField_Massacre::OnClock`, `CField_Massacre::Update`, `CField_Massacre::UpdateKeyAnimation`, `CField_Massacre::_SetDecGauge`, `CTimerboard_Massacre::OnCreate`, `CTimerboard_Massacre::Draw`, `CField_MassacreResult::OnPacket`, `CField_MassacreResult::OnMassacreResult`) |
| Partial | Memory Game and MiniRoom card parity | The simulator now syncs the MiniRoom shell occupant roster and status text to the live Match Cards board, accepts mouse-driven sidebar and card hit testing on the `CMemoryGameDlg`-style surface, exposes delayed remote ready/start/flip/tie/give-up/end actions through the same runtime path that the local room uses, adds both a typed `MemoryGamePacketType` dispatch seam behind `/memorygame packet ...`, a raw MiniRoom payload seam behind `/memorygame packetraw ...`, and an opcode-wrapped receive seam behind `/memorygame packetrecv <opcode> <payload>`, decodes the client-backed MiniRoom gameplay subtypes (`6 -> 50/51/58/59/61/62/63/68`) plus the shared MiniRoom base enter, chat, avatar, and leave families (`4/7/8/9/10`) into packet-owned roster, visitor, avatar-summary, chat-log, and leave handling, applies packet-owned board shuffle data from `CMemoryGameDlg::OnUserStart`, upgrades the shared MiniRoom shell with a dedicated Match Cards chat log plus system or speaker message presentation instead of the old generic preview-only pane, preserves visitor leave packets without tearing down the active board while surfacing slot-backed job or face or hair summaries in the room roster, now auto-starts a dedicated loopback MiniRoom packet inbox so live raw payloads or opcode-wrapped receive lines can drain through the same runtime seam with `/memorygame inbox ...` controls, normalizes two-byte Maple opcode wrappers before dispatch so externally captured server receive packets can reuse the same `CMemoryGameDlg` packet path, maps the `CMiniRoomBaseDlg::MakeGameMessage` codes onto the client StringPool ids recovered from IDA while surfacing those ids in system messages, renders decoded `AvatarLook` payloads as actual occupant avatar previews inside the MiniRoom shell by rebuilding `CharacterBuild` previews through the existing avatar loader path, and now also exposes a built-in live Maple proxy bridge behind `/memorygame session [discover|start|startauto|stop]` that initializes Maple crypto from the handshake, forwards live client/server traffic, peels inbound MiniRoom opcode `373` packets back through the same Match Cards dispatch seam, classifies the supported live client `opcode 144` Match Cards request subset (`10/50/52/56/57/58/59/60/61`) into mirrored `packetclientraw ...` bridge messages, and now also routes the local end-button and `/memorygame end` path through the same leave-booking seam that client subtypes `56/57` use so active rounds book or cancel a pending leave instead of tearing the room down immediately, surfaces that pending leave state in the shared MiniRoom shell, and resolves booked departures after the round by closing the local room or removing the booked guest seat. Focused tests are now on disk for the official-client leave-booking subtypes, exit-toggle parity, and post-round leave resolution in addition to the earlier opcode-wrapper normalization, StringPool-id-backed game-message routing, visitor-leave board preservation, mirrored official-session client request classification, and client-request dispatch coverage. The remaining gap is narrower again: the simulator still does not attach to an already-running MapleStory socket session in-process or passively decrypt MiniRoom traffic without the proxy bridge, the special game-message strings are still fallback English text annotated with the recovered client StringPool ids instead of byte-for-byte localized `StringPool::ms_aString` literals, and the exact client confirmation choreography around leave-booking or other outbound request prompts is still not byte-perfect beyond the modeled toggle-and-resolve state machine | MiniRoom parity now covers the room shell, board state machine, click flow, raw gameplay-packet decode, opcode-wrapped receive-packet normalization, packet-owned start-board data, the shared base enter/chat/avatar/leave families, a loopback inbox plus live proxy bridge transport seam, mirrored live-client request playback for the supported outbound Match Cards subset, StringPool-id-backed game-message presentation, real remote-avatar previews inside the shared shell, and a closer `OnClickEndButton`-style leave-booking lifecycle instead of an immediate local teardown, so the remaining work is concentrated on exact localized StringPool extraction, fuller prompt choreography, and true direct live-session ownership rather than missing core Match Cards ownership | `HaCreator/MapSimulator/Fields/MemoryGameField.cs`, `HaCreator/MapSimulator/Interaction/SocialRoomModels.cs`, `HaCreator/MapSimulator/UI/Windows/SocialRoomWindow.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `HaCreator/MapSimulator/MapSimulator.ChatCommands.cs`, `HaCreator/MapSimulator/Managers/MemoryGamePacketInboxManager.cs`, `HaCreator/MapSimulator/Managers/MemoryGameOfficialSessionBridgeManager.cs`, `HaCreator/MapSimulator/Managers/LoginAvatarLookCodec.cs`, `UnitTest_MapSimulator/Fields/MemoryGameFieldLeaveBookingParityTests.cs` (`CMemoryGameDlg::OnCreate`, `CMemoryGameDlg::Draw`, `CMemoryGameDlg::OnPacket`, `CMemoryGameDlg::OnUserStart`, `CMemoryGameDlg::OnTurnUpCard`, `CMemoryGameDlg::OnTimeOver`, `CMemoryGameDlg::OnGameResult`, `CMemoryGameDlg::OnClickReadyButton`, `CMemoryGameDlg::OnClickStartButton`, `CMemoryGameDlg::SendTieRequest`, `CMemoryGameDlg::SendClaimGiveUp`, `CMemoryGameDlg::OnClickEndButton`, `CMiniRoomBaseDlg::OnPacketBase`, `CMiniRoomBaseDlg::OnChat`, `CMiniRoomBaseDlg::OnEnterBase`, `CMiniRoomBaseDlg::OnAvatar`, `CMiniRoomBaseDlg::MakeGameMessage`) |
| Partial | Snowball minigame runtime | The simulator now treats Snowball state `2/3` as the client's packet-owned win states instead of local touch-zone states, keeps snowman HP packet-owned via `OnSnowBallState`, applies WZ-backed snowman stun windows and damage defaults through the runtime seam, smooths active snowball movement from the packet position plus speed data instead of instant teleports, routes the local player's world position through `SpecialFieldRuntimeCoordinator` so `SnowBallField` can surface the client's outbound touch-packet loop as pending team touch requests whenever the local player overlaps a snowball lane, aligns the Story-vs-Maple team labeling used by win banners and the local scoreboard with the client message owner, and routes `OnSnowBallMsg` through the simulator chat log seam with client-shaped team ordering plus packet-owned fallback text instead of showing those packet messages as center-screen overlays. In this pass, the runtime also stopped depending on ad hoc Snowball setup: `SpecialFieldRuntimeCoordinator` now detects Snowball maps through the preserved `snowBall` map property or `FIELDTYPE_SNOWBALL`, `MinigameFields.BindMap` now auto-binds `SnowBallField`, and a new `SnowBallFieldDataLoader` reads the copied WZ `snowBall` node from `MapInfo.additionalNonInfoProps` or the bound `WzImage` so the simulator uses the same client-owned config that `CField_SnowBall::Init` consumes. That bind path now mirrors the client-owned shared-`x`/shared-`x0` plus per-team-`y` model recovered from `CField_SnowBall::Init` at `0x561a10`: the simulator now loads `dx`, `x`, `x0`, `xMin`, `xMax`, `damageSnowBall`, `damageSnowMan0`, `damageSnowMan1`, `snowManHP`, `snowManWait`, `section1`, `section2`, and `section3`, positions both snowmen on the shared client `x` with their own lane `y`, positions both snowballs from the shared client `x0` with their own lane `y`, clamps active movement to the client `xMin/xMax` span, and resolves the WZ `Map/Obj/event.img/snowyRock/snowball/{0,1}` paths directly into `IDXObject` frame sets through the existing minigame seam when a graphics device is available. `SnowBallField.OnSnowBallState` still mirrors the client-owned first-snapshot ownership from `CField_SnowBall::OnSnowBallState` at `0x560ab0` by accepting packet-fed `damageSnowBall`, `damageSnowMan0`, and `damageSnowMan1` values only on that first state snapshot, and repeated `state == 2/3` packets still no longer inflate the simulator-only round scoreboard or replay duplicate win banners. The row was also re-checked against the exported IMG filesystem through `MapleLib.Img.ImgFileSystemDataSource`, and the `String` category still does not expose recoverable localized payload nodes for ids `3445`-`3447` (`0xD75`-`0xD77`). This run closes the transport-attachment gap inside the existing seam instead of leaving `SnowBallOfficialSessionBridgeManager` unreachable on disk: `MapSimulator` now owns that bridge alongside the loopback inbox, stops it on Snowball exit, drains live-session opcode `338`-`341` payloads into the same `SnowBallField.TryApplyPacket` path as loopback traffic, prefers live-session opcode `256` touch injection when a Snowball proxy is attached while still falling back to `SnowBallPacketInboxManager`, and exposes the full runtime through `/snowball` commands for state, hit, message, touch, inbox, session, and pending-touch inspection or control. `SnowBallField.FormatSnowBallMessage` now also routes through `MapleStoryStringPool` before falling back so the exact client literals will take over automatically once ids `0xD75`-`0xD77` are recovered. Focused tests are now on disk for the Snowball official-session bridge discovery and startup shapes used by `/snowball session`. | Snowball remains `Partial`, but the remaining parity gap is now narrow and explicit: the simulator still does not have the literal client `StringPool` text for `0xD75`-`0xD77`, so Snowball chat notices still fall back to simulator-authored English templates even though the owner, team ordering, and string-pool seam now match the client more closely. | `MinigameFields.cs`, `SnowBallField`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `SnowBallPacketInboxManager.cs`, `SnowBallOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/SnowBallOfficialSessionBridgeManagerTests.cs` (`CField_SnowBall::Init`, `CField_SnowBall::OnSnowBallState`, `CField_SnowBall::BasicActionAttack`, `CField_SnowBall::OnSnowBallHit`, `CField_SnowBall::OnSnowBallMsg`, `CField_SnowBall::OnSnowBallTouch`) |
| Partial | Ariant Arena field flow | The simulator now auto-binds `FIELDTYPE_ARIANTARENA` maps into a dedicated `AriantArenaField`, mirrors the client's sorted top-left ranking surface with WZ-backed `AriantMatch/characterIcon` row icons and the client's `(icon=5,y) / (name=21,y+2) / (score=106,y+2)` row offsets, keeps each player's Ariant icon assignment attached to that user across later score-driven re-sorts, plays the WZ-backed `AriantMatch/Result` animation from the client's center-top result origin, resolves the Ariant result UI sound from `Sound/MiniGame.img/Show` with a `Win` fallback instead of a broad heuristic, pushes local player name plus job into the runtime each frame so the client-owned 8xx or 9xx local-job rank suppression is honored, consumes the real Ariant packet types through a runtime packet seam for `354` score batches and `171` show-result packets, auto-starts a dedicated loopback Ariant packet inbox on Ariant maps so live `showresult` or `userscore` lines from the simulator transport can drain onto the main thread into that same runtime seam with `/ariantarena inbox ...` controls, extends that same inbox seam to accept transport-driven remote overlay lines for `actor add`, `actor avatar`, `actor move`, `actor remove`, and `actor clear`, and now routes both those overlay commands and client-owned remote-user packet ids `179` (`UserEnterField`), `180` (`UserLeaveField`), `210` (`UserMove`), `220` (`OnSetActiveEffectItem`), `222` (`SetActivePortableChair`), `223` (`AvatarModified`), `225` (`SetTemporaryStat`), and `226` (`ResetTemporaryStat`) through the shared `RemoteUserActorPool` instead of Ariant's earlier duplicate overlay list. Ariant remotes now render as real world actors through that common pool, keep Ariant-specific ownership via source-tagged actor entries, preserve packet-owned AvatarLook refreshes, chair state, carry-item or relationship overlay state, and raw remote temporary-stat snapshots through the shared actor seam, and apply packet `210` through the simulator's movement-snapshot path so facing plus action playback now follow the decoded move path rather than collapsing to one-off overlay updates. This pass narrows the remaining `SecondaryStat::DecodeForRemote` gap again inside that same shared seam instead of adding new Ariant-local owners: the decoded remote temp-stat state still treats `Ghost` as hidden-like so Ariant remotes can fall onto the client-shaped ghost action family rather than storing the decoded ghost id as dead state, still folds `SpiritJavelin` into the same no-consume presentation family as `SoulArrow`, still decodes the Battle Mage `DarkAura` / `BlueAura` / `YellowAura` bits into an explicit active aura skill choice, and now also keeps `BlessingArmor` as explicit known state from the client-owned `SecondaryStat::DecodeForRemote` branch at `0x72b7b0`, resolves the Paladin-vs-Bishop blessing-armor skill choice from the remote job, and pushes that state through the existing shared remote temporary-stat avatar-effect surface with repeat-branch fallback. WZ re-checks in this pass pinned those blessing-armor visuals to the current export instead of inferred skill names: `Skill/122.img/skill/1220013` still carries `info/blessingArmor = 1` plus a dedicated `repeat` loop, while `Skill/231.img/skill/2311009` still carries `info/blessingArmor = 1` plus live `affected` buff frames. Focused Ariant tests are now on disk in `UnitTest_MapSimulator/AriantArenaFieldTests.cs` for blessing-armor decode, repeat-only remote avatar-effect fallback, and official packet-`220` active-effect-item routing through the Ariant field seam. The remaining gap is now narrower and explicit: Ariant remote spawn decode preserves non-empty remote secondary-stat payloads and the shared actor seam now consumes more of that payload into visible ghost, aura, Spirit Javelin, and Blessing Armor semantics, but the simulator still does not model the full client `SecondaryStat::DecodeForRemote` surface beyond the current hidden or ghost, morph, mechanic, Shadow Partner, Soul Arrow or Spirit Javelin, barrier, Battle Mage aura, Blessing Armor, and raw snapshot or reset-mask slice; it still only covers part of the client `CUserPool::OnUserRemotePacket` family rather than the whole owner; and live official-session socket ownership is still external to the simulator transport seam | Arena scoring and ranking are owned by a dedicated field class in the client and are not covered by generic mob or UI behavior, and this row now also shares the simulator's real remote-user actor owner instead of maintaining an Ariant-only duplicate overlay path. The remaining work is concentrated on the still-unrecovered portions of the remote temporary-stat surface, broader client remote-packet coverage, and native official-session ownership rather than on basic remote actor rendering, packet-owned carry-item overlay refresh, shared avatar-modified ownership, or the newly landed ghost, aura, and Blessing Armor temp-stat presentation slices. | `MinigameFields.cs`, `AriantArenaField`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `AriantArenaPacketInboxManager.cs`, `UnitTest_MapSimulator/AriantArenaFieldTests.cs` (`CField_AriantArena::OnUserScore`, `CField_AriantArena::UpdateScoreAndRank`, `CField_AriantArena::OnShowResult`, `CUserPool::OnUserEnterField`, `CUserPool::OnUserLeaveField`, `CUserPool::OnUserRemotePacket`, `CUserRemote::OnMove`, `CUserRemote::OnSetActiveEffectItem`, `CUserRemote::OnAvatarModified`, `SecondaryStat::DecodeForRemote`, `Skill/122.img/skill/1220013/{info/blessingArmor,repeat}`, `Skill/231.img/skill/2311009/{info/blessingArmor,affected}`, `Skill/3200.img/skill/32001003/{affected,effect}`, `Skill/3210.img/skill/32101002/{affected,effect}`, `Skill/3210.img/skill/32101003/{affected,effect}`) |
| Implemented | Battlefield event flow | The simulator now detects Battlefield maps through the special-field runtime, loads `battleField` WZ defaults such as `timeDefault`, `timeFinish`, reward maps, and effect paths, follows the client's packet-owned `OnClock(type=2, seconds)` and `OnScoreUpdate(wolves, sheep)` seams, exposes a Battlefield team-change seam aligned with `OnTeamChanged(characterId, team)` and `SetUserTeam`, resolves a finish phase that derives win/lose effect plus reward-map routing from the WZ `battleField` node, parses the map's `user/<team>` presets so local Battlefield team changes now reapply both the client-backed outfit pieces and the WZ `stat/speed` move-speed override before restoring the original avatar and speed on clear or map exit, retains each preset's `speedmax` and `noitem` data for inspection or later consumption, resolves packet-owned remote team assignments through the same character-id lookup seam, renders the client Battlefield board from `Map/Obj/etc.img/battleField/backgrnd` with the real sheep or wolf score fonts and the six-digit `fontTime` clock layout, queues the resolved reward map through the existing special-field transfer seam once the WZ `timeFinish` phase expires, mirrors the local `SetUserTeam` minimap side effect by forcing the simulator minimap out of its collapsed state for local teams `0` and `2`, pushes the simulator's actual local character id into `BattlefieldField` so packet-shaped `OnTeamChanged` calls classify the local user correctly, wires the map preset's `noitem` and `speedmax` fields into live simulator systems by blocking local Battlefield item use plus equip drags for blocked item ids and by clamping the live player move-speed path through the preset speed cap, and in this pass drives those same WZ-backed team presets through the shared `RemoteUserActorPool` so remote Battlefield users now hot-swap their live avatar presentation too. The shared actor owner now re-syncs Battlefield looks during map bind and per-frame update, resolves late remote spawns against already-known field-owned team assignments instead of requiring the actor to cache the raw team id first, and restores each remote actor's original appearance and speed when Battlefield ownership clears or the map exits. Focused Battlefield tests were added on disk for that late-spawn remote-team path plus reset restore behavior, but the current workspace could not execute them becausePacketReader` accessibility, `LoginUtilityDialogWindow.Configure` accessibility, and missing `SnowBallField` packet constants | Battlefield maps depend on a team-scoring field runtime rather than only ordinary combat rules, and the client keeps both the scoreboard window creation and `SetUserTeam`-owned avatar presentation inside `CField_Battlefield`; the simulator now mirrors that ownership end-to-end by pushing the same WZ-backed Battlefield team presets through both the local player path and the shared remote-user actor owner instead of leaving remote users visually stale after packet-owned team changes | `SpecialEffectFields.cs`, `BattlefieldField`, `RemoteUserActorPool.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `MapSimulator.Content.cs`, `MapSimulator.UpdateLoop.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `EquipUIBigBang.cs`, `MinimapUI.cs`, `UnitTest_MapSimulator/BattlefieldFieldTests.cs` (`CField_Battlefield::OnClock`, `CField_Battlefield::OnScoreUpdate`, `CField_Battlefield::OnTeamChanged`, `CField_Battlefield::SetUserTeam`, `CScoreboard_Battlefield::OnCreate`) |
| Partial | Mu Lung Dojo field flow | The simulator now binds `FIELDTYPE_DOJANG` and the `MuruengRaid` map block into a dedicated `DojoField`, mirrors the client's type-2 clock ownership into the real `UI/UIWindow(.img)/muruengRaid` timerboard with bitmap digits and colon placement, draws the Dojo player, monster, and energy gauges from the client's WZ canvases at the IDA-verified layer anchors, swaps to the looping `energy/full` animation at max energy, replays the stage-start, clear, and time-over presentations from `Map/Effect.img/dojang` through `/dojostage` and `/dojoresult` debug seams plus the live timer-expiry path, auto-triggers the clear presentation when the live boss HP feed drops to zero, consumes the map's real `returnMap` or `forcedReturn` data so timer expiry waits for the WZ-backed time-over presentation to finish before queuing the Dojo exit transfer through the simulator's normal map-change seam, now resolves the clear-transfer destination from the board's real `dojang_next` portal target map before falling back to map-id inference, preserves that real portal name on the boss-HP-owned auto-clear path as well as the explicit clear-packet path, still delays clear and timeout transfers until the corresponding WZ presentation completes, accepts the official type-`1` raw Dojo clock payload as a decoded client-backed no-op while keeping type-`2` as the only timerboard-owning clock path recovered from `CField_Dojang::OnClock`, stops the live timer immediately when the clear presentation starts so the time-over path cannot race the clear-transfer choreography, clears stale result-transfer state when a new stage or fresh type-`2` clock arrives, extends the raw packet seam beyond clock-only handling by decoding Dojo stage, clear, and time-over payloads into the existing `DojoField` flow, accepts both 1-byte and 4-byte stage payloads plus 4-byte transfer-map payloads with an optional trailing portal-name suffix, surfaces the last decoded non-clock packet in the Dojo status seam for debugging, and already has a built-in `DojoOfficialSessionBridgeManager` wired through `/dojo session ...` plus the main update loop so proxied live Maple sessions can auto-infer or explicitly map inbound Dojo opcodes onto the existing raw-packet runtime seam instead of depending only on the loopback inbox. Focused Dojo tests are now on disk in `UnitTest_MapSimulator/DojoFieldTests.cs` and `UnitTest_MapSimulator/DojoOfficialSessionBridgeManagerTests.cs` for the boss-HP auto-clear portal preservation fix, clear-packet portal decode, type-`1` clock no-op handling, official-session clock inference, next-floor-vs-exit transfer disambiguation, and occupied-port startup failure. The remaining gap is narrower and specific: the simulator still has not recovered the byte-accurate official Dojo packet table beyond the clock path and these best-effort raw payload shapes, still does not reproduce the full server-authored next-floor choreography byte-for-byte, and still does not attach directly to an already-established MapleStory socket pair without the simulator-owned localhost proxy hop first | Dojo behavior is timer-driven and mode-specific, and the simulator now carries the client-backed HUD, WZ-backed stage/clear/time-over presentations, `dojang_next` target-map plus portal-name binding, raw packet decoding for clock plus stage or clear or time-over ownership, and a reachable official-session proxy seam in the runtime instead of stopping at loopback-only input. The remaining work is no longer basic HUD ownership, raw packet handling, clear-vs-timeout race control, or official-session proxy wiring; it is the unrecovered exact Dojo opcode table, the last byte-accurate floor-transition choreography, and true in-place socket attachment without a proxy hop. | `SpecialEffectFields.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `DojoPacketInboxManager.cs`, `DojoOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/DojoFieldTests.cs`, `UnitTest_MapSimulator/DojoOfficialSessionBridgeManagerTests.cs` (`CField_Dojang::Init`, `CField_Dojang::OnClock`, `CField_Dojang::Update`, `CField_Dojang::UpdateTimer`) |
| Partial | Cookie House event flow | The simulator now binds `fieldType == CookieHouse` into a dedicated `CookieHouseField` runtime, mirrors the client-owned HUD geometry from `CField_CookieHouse::Init`, tracks score updates through a score-owned redraw path instead of generic UI, replaces the old solid-color placeholder with WZ-backed HUD panel pieces, mirrors the client-owned redraw ownership more closely by caching the HUD into a dedicated `187x45` render target that only rebuilds when the point value changes before drawing that cached layer each frame, mirrors the client-owned update path by polling a simulator context point source each frame before redrawing instead of mutating the HUD as the source of truth, keeps the background load pinned to the client-shaped `UI/UIWindow.img/raise/backgrnd` root with `UIWindow2.img` only as a fallback variant, and tightens the bitmap-number loader around the actual client constructor shape from `CBitmapNumber::CBitmapNumber`: it now searches for a five-style root whose children expose per-style digits plus sign canvases instead of assuming the old direct `raise` subtree shape, broadens that search across `UIWindow(.2).img` while still preferring `raise`-shaped paths, falls back to unnamed non-digit canvases inside a style when the sign StringPool literals are still unresolved, and in this pass also drops to a direct digit-container compatibility search when the exact five-style root is absent in the loaded data set. WZ re-checks against the exported IMG filesystem made that fallback concrete instead of abstract: `UI/UIWindow(.2).img/raise/{30,31}` are still only five canvases and not the client constructor's per-style digit roots, so the simulator now scans for the closest compatible digit container and fans that one style across all five grade buckets; on the current data set that resolves to the shipped `UI/UIWindow(.2).img/MonsterKilling/Result/number2` subtree with its `0-9` canvases plus `plus` sign canvas rather than falling straight back to `SpriteFont` text. IDA recovery from the earlier pass also still anchors the grade logic to the client data: `FindGrade_` matches the recovered `s_nGradeCount == 4` / `s_anGrade == { 90, 160, 230, 350 }` values from `MapleStory.exe`, and focused runtime tests now cover those recovered grade transitions together with provider-owned polling and the raw-vs-text Cookie House context inbox parse seam. The remaining gap is narrower but still real: the exact literal WZ path behind StringPool id `0x13FC` is still unresolved even though the simulator now has a concrete WZ-backed compatible bitmap fallback when that client root is missing from the current export, and it still lacks the true live server-owned Cookie House packet feed beyond the existing loopback inbox seam | Cookie House is another distinct score-driven field, and this row now carries the client-shaped cached HUD redraw path, a verified client background root, a recovered client grade table, a constructor-shaped bitmap-number search plus a concrete WZ-backed digit-container fallback for exports that do not ship the exact client root, and an external point-stream seam instead of remaining a manual overlay with guessed thresholds and a guessed digit model | `CookieHouseField.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `CharacterData.cs`, `CookieHousePointInboxManager.cs`, `UnitTest_MapSimulator/CookieHouseParityTests.cs` (`CField_CookieHouse::_UpdatePoint`, `CField_CookieHouse::Init`, `CField_CookieHouse::Update`, `CBitmapNumber::CBitmapNumber`, `CBitmapNumber::Draw`, ``anonymous namespace'::FindGrade_``) |
| Partial | Monster Carnival field flow | The simulator now detects Monster Carnival maps through the backlog coordinator, loads the map's WZ-backed Carnival definition into a dedicated runtime, enriches carnival mob entries with WZ-backed `getCP` and revive-chain metadata, pulls the map's `mobGenPos`, `guardianGenPos`, `reactorRed`, and `reactorBlue` data into that same definition, enforces local CP spending plus mob or guardian generation caps when requests succeed, clears prior summon or guardian state on each new `OnEnter` round like the client-owned UI reset path, rejects duplicate guardian requests through the client's dedicated occupied-slot failure reason, records local successful mob summons against the next WZ-backed mob spawn slot while recording guardian placements against their WZ-backed guardian slot plus the team-owned reactor id, reconciles packet-owned summon counts into live spawn-slot state on `Enter` or summoned-count updates, awards WZ-backed mob `getCP` plus revive-chain spawns through an explicit mob-defeat seam, tracks guardian reactor hits and destruction through a dedicated reactor-hit seam that frees occupied slots, switches Carnival team labeling to Red-vs-Blue ownership that matches the WZ `reactorRed` or `reactorBlue` split, mirrors the client-owned StringPool routing for request-failure (`0x101B`-`0x101F`), death (`0x1019`/`0x101A` with team labels `0x1017`/`0x1018`), member-out (`0x1029`/`0x102A`), and result (`0x1020`-`0x1023`) messages through the existing runtime seam, and still reconciles guardian placements into real spawned `ReactorPool` entries so Carnival guardian reactors use the shared live reactor update, render, and attack-hit lanes instead of remaining logical-only placements. WZ re-checks in this pass kept the field HUD anchored to the client-owned `UI/UIWindow.img/MonsterCarnival` asset family instead of simulator-only naming: `backgrnd` is still `162x107`, `backgrnd2` is still `127x107`, `backgrnd3` still splits into `top`, `middle0`, `middle1`, and `bottom`, `BtSide` still exposes `normal`, `mouseOver`, `pressed`, and `disabled`, and `Tab` still exposes distinct `enabled` and `disabled` states. IDA re-checks also kept the packet owner unchanged: `CField_MonsterCarnival::OnPacket` at `0x55bba0` still dispatches only `346` through `353` into `OnEnter`, `OnPersonalCP`, `OnTeamCP`, `OnRequestResult`, `OnProcessForDeath`, `OnShowMemberOutMsg`, and `OnShowGameResult`, while `CField_MonsterCarnival::OnRequestResult` at `0x55a890` still uses the same failure StringPool ids `0x101B`-`0x101F` and resets the local request-timer fields after either success or failure. In this pass the live transport seam moved beyond basic discovery/start wiring: `MonsterCarnivalOfficialSessionBridgeManager` already had configurable opcode mapping and recent-packet history internally, and `/mcarnival session` now exposes that transport ownership directly through `map`, `unmap`, `clearmap`, and `recent` alongside `status`, `discover`, `start`, `startauto`, and `stop`, so live sessions no longer reference unreachable mapping instructions when the recovered `346`-`353` family needs opcode remapping. Focused bridge tests are now on disk in `UnitTest_MapSimulator/MonsterCarnivalOfficialSessionBridgeManagerTests.cs` for native opcode-`346` decode, configured opcode remapping into raw packet type `352`, unmapped-opcode rejection with recent-history capture, and mapping removal/clear behavior. The remaining gap is now narrower and centered on deeper live-session parity rather than bridge operability: the simulator still does not recover the exact localized StringPool bytes, attach in place to an already-established Maple client socket pair without a proxy hop, decode any additional official Monster Carnival packet ownership or guardian-reactor thresholds beyond the recovered `346`-`353` client handlers, or mirror the remaining guardian choreography byte-for-byte once live sessions are attached | Monster Carnival is a self-contained field mode with client-owned rules and UI, and this row now carries the map-owned spawn-slot, reactor metadata, packet-driven status-message selection, shared guardian-reactor integration, and an official-session bridge surface that can both start/discover live sessions and adapt recovered raw packet routing from the field command surface itself. The remaining work is now concentrated on byte-accurate client strings, direct in-place live-session attachment, and deeper official packet or guardian choreography recovery rather than on missing bridge controls, missing remap diagnostics, or logical-only guardian placement. | `MonsterCarnivalField.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.cs`, `MonsterCarnivalPacketInboxManager.cs`, `MonsterCarnivalOfficialSessionBridgeManager.cs`, `ReactorPool.cs`, `UnitTest_MapSimulator/MonsterCarnivalOfficialSessionBridgeManagerTests.cs` (`CField_MonsterCarnival::CreateUIWindow`, `CField_MonsterCarnival::OnEnter`, `CField_MonsterCarnival::OnRequestResult`, `CField_MonsterCarnival::OnShowGameResult`, `CField_MonsterCarnival::OnProcessForDeath`, `CField_MonsterCarnival::OnShowMemberOutMsg`) |
| Partial | Party Raid field flow | The simulator now binds `FIELDTYPE_PARTYRAID`, `FIELDTYPE_PARTYRAID_BOSS`, and `FIELDTYPE_PARTYRAID_RESULT` into a dedicated `PartyRaidField` runtime, renders the field, boss, and result HUDs from the client WZ asset roots (`UI/UIWindow.img/PartyRace/*`, `UI/UIWindow.img/DualMobGauge/*`, `Map/Effect.img/praid/*`), fixes the field and boss point-vs-stage board placement to match the `PartyRace/Stage/backgrd` labels, mirrors the client's team-colored stage-state art, boss red-vs-blue gauge fill, map-owned clear or timeout result effect, and result summary overlay positions, now infers win-vs-fail result mode from the result maps' `info/onUserEnter` scripts (`PRaid_WinEnter`, `PRaid_FailEnter`) and constrains the result panel to the map's `LBSide/LBTop/LBBottom` safe area, carries a type-2 timer lifecycle through the runtime, now also swaps the old plain text countdown for the client `Map/Obj/etc.img/partyRace` timerboard with bitmap digits and separator, broadens the existing raw `field|party|session` key seam with numeric team aliases plus session outcome text handling, adds a WZ-backed `PartyRace/battery` field HUD plus battery charge-capacity aliases through the same raw key seam, adds a dedicated Party Raid loopback inbox that auto-starts on Party Raid maps, drains live `field|party|session|clock` lines from the simulator network path into the existing runtime seam, and exposes `/partyraid inbox [status|start|stop]` controls. IDA re-checks against `CField_PartyRaid::Update` at `0x55c320` still pin the field-state badge positions to the client formulas (`mine x = 23*stage - 71`, `other x = 23*stage - 67`), and that ownership now extends to the recovered client field-specific literals too: the runtime accepts `PRaid_Team` with the client text values `Red Team` / `Blue Team`, routes `Red_Stage` / `Blue_Stage` into mine-vs-other stage ownership relative to the active team color instead of treating them as generic stage aliases, and now feeds `PRaid_ElapssedTimeAtBoss` through the existing type-2 timer seam during structured field-specific payload handoff so packet-owned Party Raid boss time can drive the client timerboard without a parallel debug key. The boss runtime still prefers the client-owned boss template `9700037` when resolving the gauge-capacity fallback that `CField_PartyRaidBoss::OnFieldSetVariable` uses, and the direct result-literal path remains aligned with the client-owned StringPool keys recovered from `CField_PartyRaidResult::OnSessionValue` (`PRaid_Point`, `PRaid_Bonus`, `PRaid_Total`). Focused Party Raid parity tests are now on disk for recovered team-text handling, team-relative `Red_Stage` / `Blue_Stage` ownership, `PRaid_ElapssedTimeAtBoss` timer routing, and direct result-literal acceptance. The row still does not decode the true client Party Raid packet family beyond the current structured string-pair handoff, and some remaining boss-specific icon or layer ownership detail is still not fully recovered. | Party Raid now covers the field battery surface, field board, client-horizontal mine-vs-other stage-state movement, boss gauge, result overlays, client timerboard, script-backed outcome selection, map-safe result placement, the recovered Party Raid field-specific literals for team/stage/boss time, and a live loopback transport seam that feeds the existing runtime handlers. The remaining gap is now concentrated on exact packet-family recovery beyond the current field-specific string-pair route plus the last boss-specific client layer details, rather than on missing literal-key ownership for team, stage, timer, or result values. | `SpecialFieldRuntimeCoordinator.cs`, `PartyRaidField.cs`, `MapSimulator.PacketOwnedFieldStateParity.cs`, `MapSimulator.cs`, `PartyRaidPacketInboxManager.cs`, `UnitTest_MapSimulator/PartyRaidFieldParityTests.cs` (`CField_PartyRaid::_UpdatePoint`, `CField_PartyRaid::Update`, `CField_PartyRaid::Init`, `CField_PartyRaidBoss::Init`, `CField_PartyRaidBoss::OnFieldSetVariable`, `CField_PartyRaidBoss::OnPartyValue`, `CField_PartyRaidResult::Init`, `CField_PartyRaidResult::OnSessionValue`) |
| Implemented | SpaceGAGA timerboard flow | The simulator now binds `FIELDTYPE_SPACEGAGA` maps into a dedicated `SpaceGagaField`, mirrors the client's clock-type-2 timerboard ownership, keeps the board at the verified center-top window origin, removes the invented title banner, pins the timerboard source to `Map/Obj/etc.img/space` so the board comes from `space/backgrnd` and the bitmap timer digits come from `space/fontTime`, and no longer falls back to broad `UIWindow(.2).img` shape scans before dropping to the older procedural board or SpriteFont path. In this pass, WZ re-checks against the exported IMG filesystem kept that `space` subtree pinned to the exact `backgrnd` canvas plus `fontTime` digit and `comma` canvases, IDA re-checks kept the timer-font seam pinned to `CTimerboard_SpaceGAGA::GetFontTime` at `0x5629f0`, and direct `MapleStory.exe` table extraction closed the last StringPool gap: `ms_aString[0x140D]` was recovered as seed `0x44` plus encoded bytes `44 C3 06 CD 1C 7C 6C 18 C2 08 93 3B 4A 0F 27 50 31 FD 17 DC 50 56 21 10 8C 0E 8C 3F 16 08 2E`, which decodes to `Map/Obj/etc.img/space/backgrnd` through `CTimerboard_SpaceGAGA::OnCreate`, and `ms_aString[0x140E]` was recovered as seed `0x45` plus encoded bytes `45 51 AE 0A 49 29 7E 8F F5 BE BA D3 E6 A5 F9 09 12 6F BF 1B 05 03 33 83 B5 B5 BA E4 A1 A1 F1`, which decodes to `Map/Obj/etc.img/space/fontTime` through `GetFontTime`. The `/spacegaga` status seam now reports those byte-accurate client entries instead of inferred-only mappings, and focused `SpaceGagaField` tests on disk cover type-2 clock ownership, ignored non-type-2 clocks, reset behavior, and the recovered StringPool evidence through that runtime seam | This is another example of client-owned special field logic that would otherwise be lost under generic map handling, and the simulator now carries the SpaceGAGA timerboard end-to-end with fixed WZ-backed art, verified ownership for both client StringPool ids, and byte-accurate recovered `ms_aString` evidence rather than inferred-only path mapping | `SpecialEffectFields.cs`, `UnitTest_MapSimulator/SpaceGagaFieldTests.cs`, `SpaceGagaField` (`CField_SpaceGAGA::OnClock`, `CTimerboard_SpaceGAGA::Draw`, `CTimerboard_SpaceGAGA::OnCreate`, `CTimerboard_SpaceGAGA::GetFontTime`, `StringPool::GetString`) |

### 2. Additional packet-owned microgame dialog discovered by follow-up IDA scan

The broader packet-owner pass in backlog 7 already surfaced `CRPSGameDlg::OnPacket`, but it is still missing from the dedicated minigame plan even though it fits this area more closely than the generic field-utility bucket.
A targeted decompile now makes that owner more concrete: `CRPSGameDlg::OnPacket` is not a single opaque callback. Packet subtype `8` first enforces the unique-modeless owner gate, decodes a `uint32` value, opens a `CUtilDlgEx` notice using `StringPool` id `0xE83`, and only then constructs the dialog owner; subtypes `6`, `7`, `9`, `10`, `11`, `12`, and `14` require an already-live `CRPSGameDlg` unique-modeless owner and forward into `ProcessPacket`; and subtype `13` tears the owner down through `CWnd::Destroy`. The same `ProcessPacket` decompile also shows more exact gameplay ownership than the backlog was tracking: `9` and `12` start a fresh round by clearing NPC choice, enabling the three RPS buttons, setting the switch cadence to `120`, and arming a `30000` ms time limit; `11` decodes the NPC pick plus straight-win count and marks the compensation flag when the decoded count goes negative while the current streak is still zero; `10` forces immediate result presentation by clearing the switch cadence, setting the streak to `-1`, and calling `ShowResult`; and `6`/`7`/`14` reset the round state, re-enable the main button, and route the completion notice through `StringPool` ids `3724` or `3723`.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Rock-Paper-Scissors dialog parity | `CRPSGameDlg::OnPacket` at `0x6d9e00` is a dedicated packet-owned microgame dialog owner rather than a generic field notice, and a targeted decompile now shows that it owns a broader lifecycle than the backlog previously captured. Packet subtype `8` is the client-owned entry gate: it requires that no other unique-modeless owner is active, decodes a `uint32` payload, opens a `CUtilDlgEx` notice through `StringPool` id `0xE83`, and only then constructs `CRPSGameDlg`. Subtypes `6`, `7`, `9`, `10`, `11`, `12`, and `14` require that the active unique-modeless owner is already a `CRPSGameDlg` and then dispatch into `ProcessPacket`, while subtype `13` destroys that owner. `ProcessPacket` is also more specific than a generic minigame packet router: `9` and `12` start a live round by clearing the NPC choice, enabling the three RPS buttons, setting `m_tSwitchingTerm = 120`, and arming a `30000` ms round limit; `11` decodes the NPC pick plus straight-win count and derives the compensation flag when the decoded streak goes negative while the previous streak was still zero; `10` forces result presentation immediately by clearing switching, setting the streak to `-1`, and calling `ShowResult`; and `6`/`7`/`14` reset the round state, restore the main button, and route completion notices through `StringPool` ids `3724` or `3723`. A fresh repo search across `HaCreator/MapSimulator` still does not surface any RPS runtime, dialog owner, or packet bridge, so this owner remains genuinely absent rather than merely under-documented. | If RPS stays buried only inside the broad field-utility packet row, minigame parity will look more complete than it is while one of the client-owned dialog games remains totally untracked. The new decompile detail also makes the missing work actionable: this is not only “add an RPS window, Eit is a packet-owned unique-modeless lifecycle with an entry notice, explicit create/destroy packets, a 30-second round timer owner, NPC-choice/result state, and completion-notice routing that belongs beside `CMemoryGameDlg`, not only inside the miscellaneous `CField::OnPacket` bucket. | packet-owned microgame dialog layer (`CRPSGameDlg::OnPacket`, `CRPSGameDlg::ProcessPacket`, `CUtilDlgEx`, `CRPSGameDlg::ShowResult`) |

### 3. Additional client-owned field wrappers discovered by IDA function scan

The live IDA `list_functions` scan surfaced more field-owner classes with no hits anywhere else in the current backlog set.
These started as scan-only seams, but a follow-up pass against the current simulator code shows that some of them are now represented locally and should no longer stay marked as missing just because the older backlog text was never refreshed.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Transit and voyage field wrappers | The simulator still treats `shipObj` as a client-owned map contract instead of depending only on editor `ShipObject` instances: `TransportationFieldDefinitionLoader` reads copied WZ `shipObj` data from `MapInfo.additionalNonInfoProps`, normalizes both `Map/Obj/vehicle.img/...` and `Map/Obj/vehicle/...` object paths, and lets `DetectAndInitializeTransportField` initialize transit and Balrog voyage maps from the same map-owned payload the client uses. WZ re-checks against the exported IMG filesystem in this pass kept those transport contracts anchored to the same client-owned map data instead of simulator defaults: regular transit map `Map/Map1/101000300` still carries `shipObj/x=1545`, `x0=2100`, `y=-195`, `shipKind=0`, `f=0`, `tMove=15`, and ship art path `Map/Obj/vehicle.img/ship/ossyria/99`, while Balrog wrapper maps `Map/Map2/200090000` and `Map/Map2/200110000` still carry `shipKind=1`, `tMove=2`, and ship art path `Map/Obj/vehicle/ship/ossyria/97` with opposite dock positions and flip values (`x=-590`, `f=1` versus `x=485`, `f=0`). IDA re-checks stayed aligned with the existing runtime mapping too: `CField_ContiMove::OnContiMove` still dispatches only subtypes `8`, `10`, and `12`, `CField_ContiMove::OnContiState` still routes state values `0/1/6` to `EnterShipMove`, `2/5` to `LeaveShipMove`, and `3/4` to either `AppearShip` for Balrog ship-kind `1` when the second byte is `1` or `LeaveShipMove` for regular ship-kind `0`, and `CField_Balrog::GetFieldType` still resolves to field type `18`. `TransportationField` still applies a client-owned initial wrapper state so regular `FIELDTYPE_CONTIMOVE` routes start docked at `x` instead of idling offscreen at `x0`, while Balrog-owned wrappers stay hidden until their appear branch runs, matching the dedicated `CField_ContiMove` and `CField_Balrog` ownership more closely. The live packet seam is also more inspectable now: alongside the existing `TransportationPacketInboxManager`, the simulator has a wired `TransportationOfficialSessionBridgeManager` in the active transport row seam, the update loop drains both inbox and proxied live-session packets through the same transport-owned dispatcher, and `/transport` now exposes `session [status|discover|history|clearhistory|start|startauto|stop]` so transport maps can proxy a real Maple client through localhost, decrypt live server-authored `164` (`OnContiMove`) and `165` (`OnContiState`) packets, and inspect or clear recent outbound client traffic from the same in-process bridge without an external helper process. Focused transport bridge tests are now on disk in `UnitTest_MapSimulator/TransportationOfficialSessionBridgeManagerTests.cs` for live-packet decode, non-transport opcode rejection, discovery-candidate local-port filtering, ambiguous multi-session diagnostics, outbound-history ordering/clearing, and occupied-port startup failure, although executing The remaining gap is now narrower and explicit: the simulator still does not attach in place to an already-established Maple client socket pair without a localhost proxy hop first, it still has no recovered client-owned outbound voyage/Balrog request family to inject beyond passive outbound history capture, and the separate voyage-attack/server-timed Balrog event choreography is still limited to the local runtime seam rather than a recovered live packet owner. | Ship travel and Balrog transport are client-owned field experiences with their own packet/state choreography, and this row now covers the WZ-authored route contract, the dedicated loopback inbox, an in-process official-session proxy for the recovered `CField_ContiMove` packet family, and transport-owned diagnostics for both inbound and outbound traffic instead of leaving live session behavior trapped behind ad hoc local calls or an external helper. The remaining work is no longer whether the simulator can own live inbound `164`/`165` traffic or inspect what the client sends during those sessions at all; it is specifically true in-place socket attachment without a proxy hop plus any distinct outbound voyage/Balrog event ownership that the server drives outside the recovered transport packet family. | transit or voyage field runtime (`MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.ClientOwnedFieldWrappers.cs`, `TransportationField.cs`, `TransportationFieldDefinitionLoader.cs`, `TransportationPacketInboxManager.cs`, `TransportationOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/TransportationOfficialSessionBridgeManagerTests.cs`, `CField_ContiMove`, `CField_Balrog`) |
| Partial | Tutorial, camera-mask, and presentation wrappers | The simulator now has a real wrapper seam for most of the client owners that originally surfaced in the scan. `MapSimulator.ClientOwnedFieldWrappers.cs` still mirrors `CField_Tutorial::DecodeFieldSpecificData` by forcing the same tutorial appearance pair recovered in IDA (`1002562` cap and `1052081` longcoat) while preserving the active weapon slot, but this pass also tightened the owner classification around the two remaining client-owned variants that were still too generic. Aran tutorial maps are now carried as an explicit `CField_AranTutorial::GetFieldType = 22` contract with the same WZ markers re-checked from the extracted data (`Map/Map9/914000000` still has `info/fieldType = 22`, `info/onUserEnter = aranTutorAlone`, and `info/mapMark = BlackDragon`), instead of only surfacing as a generic tutorial classification in status text. Limited-view maps still load `Effect/MapEff.img/Viewrange/0` for the mask metadata recovered from WZ, and the update loop now drives the dormant client-owned world-focus path in `LimitedViewField` so the runtime follows the same `CField_LimitedView::Update = CField::Update + DrawViewrange` ownership more closely instead of keeping the mask centered through a simulator-only screen assumption. Wedding-photo ownership also moved closer to the client seam: alongside direct `fieldType = 61` detection from `CField_WeddingPhoto::GetFieldType`, the wrapper now treats the WZ-authored `pledgeEnter` scene maps in the `6800007xx` and `6800008xx` wedding region as scene owners rather than only presentation contracts (`Map/Map6/680000700` and `680000800` still carry `info/onUserEnter = pledgeEnter`, `info/mapMark = Wedding`, and `returnMap = 680000000`). Focused parity tests for the Aran tutorial contract, wedding scene-owner detection, and limited-view focus math are now on disk in `UnitTest_MapSimulator/ClientOwnedFieldWrapperParityTests.cs`. The remaining gap is narrower: Aran tutorial still reuses the same forced appearance runtime as generic tutorial maps instead of having a fuller differentiated owner path beyond classification and status, wedding-photo maps still do not have a bespoke ceremony/photo scene runtime comparable to a dedicated `CField_WeddingPhoto` owner, and `CField_LimitedView::Update` is still only mirrored to the level of recovered focus/mask ownership rather than byte-for-byte client draw sequencing. | These owners are no longer invisible backlog debt because the simulator now treats tutorial appearance forcing, Aran tutorial identification, limited-view focus updates, dragon suppression, and wedding pledge/photo scene ownership as explicit client-owned seams instead of leaving them buried under generic map load logic. The remaining work is now the last owner-specific runtime details, not whether the wrapper layer can identify or drive these clients at all. | tutorial or scripted field layer (`MapSimulator.ClientOwnedFieldWrappers.cs`, `MapSimulator.UpdateLoop.cs`, `LimitedViewField.cs`, `UnitTest_MapSimulator/ClientOwnedFieldWrapperParityTests.cs`, `CField_Tutorial::DecodeFieldSpecificData`, `CField_AranTutorial::GetFieldType`, `CField_LimitedView::Init`, `CField_LimitedView::Update`, `CField_NoDragon::Init`, `CField_WeddingPhoto::GetFieldType`) |
| Partial | Dynamic foothold field-wrapper parity | The latest IDA function scan surfaced `CField_DynamicFoothold` through `CField::FieldFactory`, with a dedicated constructor plus `GetFieldType` and a trivial `Init` stub (`0x551050`) that confirms the client still models dynamic-foothold maps as their own field-owner variant even when most behavior lives elsewhere. The current backlog corpus only tracks the simulator-side moving-platform runtime in backlog 3 (`DynamicFootholdSystem`, `PassengerSyncController`, and transport-deck passenger sync), and a focused source pass across `HaCreator/MapSimulator` still does not show a matching map-owned wrapper classification, field-type bind, or field-specific initialization seam named for `CField_DynamicFoothold`. That means the simulator already has useful dynamic-platform physics, but it is still missing the client-owned wrapper bookkeeping that keeps those maps distinct from general movement or transport-only handling. | Without an explicit row here, the existing dynamic-platform work in backlog 3 can make wrapper parity look more complete than it is. Tracking the field owner separately keeps future work anchored to the actual client field class and leaves room for any map-owned initialization or runtime branching recovered later, instead of treating all dynamic-foothold behavior as fully covered by the generic moving-platform system. | dynamic-foothold field wrapper (`CField::FieldFactory`, `CField_DynamicFoothold::GetFieldType`, `CField_DynamicFoothold::Init`, `HaCreator/MapSimulator/Fields/DynamicFoothold.cs`) |
| Implemented | Result-board and field-variable special wrappers | The missing IDA-owned wrapper surfaces are now covered in the simulator runtime: `CField_KillCount::OnPacket` packet `178` drives a dedicated kill-count overlay, `CField_HuntingAdballoon::OnFieldSetVariable` reuses the existing party-raid boss field-variable HUD with an explicit client-owned bind, `CField_MassacreResult::OnPacket` packet `174` now decodes the client rank/value payload into the existing Massacre result board, `CField_EscortResult::OnSessionValue` fail values arm a timed fail overlay, and `CField_ShowaBath::DecodeFieldSpecificData` now forces the gendered robe appearance recovered from the client and WZ (`1050100` / `1051098`). The field-state command surface also accepts the new packet ids and wrapper value injection for verification. | The result/status wrappers that contextualize these minigame endings are now represented as concrete simulator runtime owners instead of being left as untracked client-only behavior. | event result or field-variable layer (`CField_KillCount`, `CField_HuntingAdballoon`, `CField_MassacreResult`, `CField_EscortResult`, `CField_ShowaBath`) |
| Partial | Tournament and Monster Carnival variant wrappers | WZ re-checks against the exported IMG filesystem kept this row anchored to the same client-owned field seams instead of generic map tags: the MapleStory Minigame Challenge lobby `Map/Map1/109070000` still carries `info/fieldType = 3` and `String/Map.img/victoria/109070000/help0` still describes the tournament bracket, resting period, prize podium, and five-minute post-finals exit flow, while Carnival variant maps still expose the same `monsterCarnival/mapType` seam that the client wrappers read directly (`Map/Map9/980031000` fieldType `16` with `mapType = 0`, `980032000` fieldType `16` with `mapType = 1`, `980033000` fieldType `16` with `mapType = 2`). IDA re-checks kept the client side equally narrow and concrete: `CField_MonsterCarnivalWaitingRoom::Init` and `CField_MonsterCarnivalS2_Game::Init` still do nothing deeper than read `monsterCarnival/mapType`, `CField_MonsterCarnivalRevive::OnShowGameResult` still routes revive results through the same `0x1020`-`0x1023` StringPool ids, and `CField_Tournament::OnPacket` still dispatches only `374` through `378` into notice, match-table dialog, prize, UEW, and no-op branches. In this pass the simulator's Carnival runtime now reports the concrete client owner label alongside its variant and `mapType` so waiting-room, revive, and season-2 maps no longer appear as anonymous shared Carnival states, and the missing Tournament source seam was actually brought on disk: a dedicated `TournamentField` now activates on `FIELDTYPE_TOURNAMENT`, surfaces a tournament status panel in the existing minigame seam, accepts raw and opcode-wrapped packet handling for the exact client packet family (`374`-`378`), preserves the recovered StringPool ids for the notice (`0x3A3`-`0x3A7`), prize (`0x3A8`-`0x3AA`), and UEW (`0x9F6`-`0x9F8`) branches, records match-table payload handoff as the client-owned `CMatchTableDlg` path, exposes `/tournament [status|raw|packetraw|inbox]`, auto-starts a dedicated loopback tournament inbox on tournament maps, and adds focused parity tests for tournament packet decoding and inbox alias parsing in `UnitTest_MapSimulator/TournamentFieldParityTests.cs`. The remaining gap is narrower but still real: Carnival waiting-room/revive/season-2 maps still share one broader Carnival HUD and do not yet reproduce distinct client dialog or session choreography beyond owner labeling, and Tournament still stops at packet-family ownership plus local presentation instead of the full bracket dialog/session transport lifecycle or an official-session proxy. | These variant owners are now explicitly represented in the simulator runtime rather than only implied by stale backlog prose. The remaining work is no longer discovering the wrappers or leaving tournament traffic totally unowned; it is the deeper per-variant UI and session behavior that the client owns beyond the already recovered `fieldType`, `mapType`, StringPool ids, loopback inbox, and packet dispatch seams. | mode-variant field runtime (`MonsterCarnivalField.cs`, `TournamentField.cs`, `TournamentPacketInboxManager.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/TournamentFieldParityTests.cs`, `CField_MonsterCarnivalRevive::OnShowGameResult`, `CField_MonsterCarnivalWaitingRoom::Init`, `CField_MonsterCarnivalS2_Game::Init`, `CField_Tournament::OnPacket`, `CField_Tournament::OnTournament`, `CField_Tournament::OnTournamentMatchTable`, `CField_Tournament::OnTournamentSetPrize`, `CField_Tournament::OnTournamentUEW`) |
| Implemented | Tournament match-table dialog parity | `HaCreator/MapSimulator/Fields/TournamentField.cs` now carries a dedicated `TournamentMatchTableDialogState` owner behind Tournament packet `375`, so `CField_Tournament::OnTournamentMatchTable` is represented as its own dialog surface instead of being buried inside the generic field status panel. The simulator now mirrors the client-owned lifecycle more explicitly: the packet handoff opens a `CMatchTableDlg`-named dialog, the local draw path keeps the IDA-confirmed close/up/down button geometry and layer offsets from `CMatchTableDlg::OnCreate` (`0x784050`) and `CMatchTableDlg::Draw` (`0x797d20`), and the dialog is now interactive through mouse wheel, click-to-close, click-to-scroll, and `/tournament dialog ...` controls. | This row no longer needs to stay open just to track whether Tournament owns a second client dialog surface. Remaining differences are visual-fidelity follow-ups inside the existing dialog seam rather than missing ownership: the simulator still renders the bracket with local primitives and decoded text summaries instead of reproducing every client canvas/layer asset from the WZ/UI stack. | tournament bracket dialog layer (`CField_Tournament::OnTournamentMatchTable`, `CMatchTableDlg::OnCreate`, `CMatchTableDlg::Draw`) |

### 4. Additional Party Raid wrapper-family ownership confirmed by `CField::FieldFactory` re-check

The latest IDA re-check against `CField::FieldFactory` confirms that Party Raid is not a single special-field owner in the client.
The factory still instantiates three distinct wrapper classes:

- `CField_PartyRaid` for field type `25`
- `CField_PartyRaidBoss` for field type `26`
- `CField_PartyRaidResult` for field type `27`

That owner split was only implicit in the existing gameplay row, so it needs its own wrapper-family entry the same way Tournament, Carnival, transport, and dynamic-foothold do.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Explicit Party Raid wrapper-family parity | `CField::FieldFactory` still proves the client owns three Party Raid field wrappers, not one generic mode bucket: `CField_PartyRaid` for the live stage map, `CField_PartyRaidBoss` for the boss variant, and `CField_PartyRaidResult` for the result scene. The simulator already binds `FIELDTYPE_PARTYRAID`, `FIELDTYPE_PARTYRAID_BOSS`, and `FIELDTYPE_PARTYRAID_RESULT` into the existing `PartyRaidField` seam and already mirrors recovered boss/result handlers such as `CField_PartyRaidBoss::OnFieldSetVariable`, `CField_PartyRaidBoss::OnPartyValue`, and `CField_PartyRaidResult::OnSessionValue`, but it still collapses those owners into one broader runtime description instead of treating the wrapper split itself as a first-class parity seam. | Without an explicit wrapper-family row, Party Raid can look fully covered once the HUD and field-state work lands even though the client still distinguishes three map-owner variants with different lifecycle entry points and room for future owner-specific init/update branching. Tracking the split separately keeps future work anchored to the actual client factory contract instead of assuming every Party Raid map is just one shared field mode with cosmetic differences. | Party Raid field-wrapper layer (`CField::FieldFactory`, `CField_PartyRaid::Init`, `CField_PartyRaid::Update`, `CField_PartyRaidBoss::Init`, `CField_PartyRaidBoss::OnFieldSetVariable`, `CField_PartyRaidBoss::OnPartyValue`, `CField_PartyRaidResult::Init`, `CField_PartyRaidResult::OnSessionValue`, `PartyRaidField.cs`) |

## Priority Order

If the goal is visible parity first, the next work in this area should be sequenced like this:

1. Existing-special-field refinement pass:
   Tighten Wedding, Coconut, Guild Boss, Massacre, SnowBall, and Ariant behavior against the client owners that already have simulator counterparts.
2. Missing microgame dialog pass:
   Add the still-untracked `CRPSGameDlg` Rock-Paper-Scissors owner before more packet-owned minigame work gets buried in the generic field-utility bucket.
3. Tournament dialog pass:
   Add the dedicated `CMatchTableDlg` bracket owner before treating Tournament parity as covered by the field wrapper and packet inbox alone.
4. Variant and packet/HUD refinement pass:
   Continue deeper Monster Carnival, Party Raid, Tournament, SpaceGAGA, and MiniRoom packet/UI parity now that those runtime shells already exist.
5. Wrapper and transport pass:
   Triage the transit, tutorial/presentation, result-wrapper, and Monster Carnival variant owners while closing the remaining official-session ownership gaps that still sit behind loopback inboxes or proxy seams.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
