# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Effects/SpecialEffectFields.cs`
- `HaCreator/MapSimulator/Fields/MinigameFields.cs`

### Client references checked

- `CField_GuildBoss::CField_GuildBoss` at `0x53e980`
- `CField_Coconut::OnClock` at `0x54a100`
- `CField_Coconut::OnCoconutHit` at `0x54a470`
- `CField_Coconut::BasicActionAttack` at `0x54a5e0`
- `CField_Coconut::DrawBoard` at `0x54a750`
- `CField_Coconut::OnCoconutScore` at `0x54bf70`
- `CField_AriantArena::OnUserScore` at `0x5492b0`
- `CField_AriantArena::OnShowResult` at `0x547630`
- `CField_Battlefield::OnClock` at `0x549ad0`
- `CField_Battlefield::OnScoreUpdate` at `0x5499a0`
- `CField_Battlefield::OnTeamChanged` at `0x5499e0`
- `CField_Battlefield::SetUserTeam` at `0x549870`
- `CField_Dojang::OnClock` at `0x550940`
- `CField_Dojang::Update` at `0x54ef10`
- `CField_CookieHouse::_UpdatePoint` at `0x54daa0`
- `CField_CookieHouse::Init` at `0x54e250`
- `CField_Massacre::OnClock` at `0x556af0`
- `CField_Massacre::UpdateKeyAnimation` at `0x556bf0`
- `CTimerboard_Massacre::Draw` at `0x557100`
- `CField_Massacre::Init` at `0x5579d0`
- `CMemoryGameDlg::OnButtonClicked` at `0x628df0`
- `CMemoryGameDlg::OnCreate` at `0x62f3c0`
- `CMemoryGameDlg::Draw` at `0x631cb0`
- `CField_MonsterCarnival::CreateUIWindow` at `0x55a510`
- `CField_MonsterCarnival::OnEnter` at `0x55a6c0`
- `CField_MonsterCarnival::OnRequestResult` at `0x55a890`
- `CField_MonsterCarnival::OnShowGameResult` at `0x55af80`
- `CField_MonsterCarnivalRevive::OnShowGameResult` at `0x55b110`
- `CField_PartyRaid::_UpdatePoint` at `0x55c120`
- `CField_PartyRaid::Init` at `0x55f110`
- `CField_PartyRaidBoss::OnFieldSetVariable` at `0x55ca40`
- `CField_PartyRaidBoss::OnPartyValue` at `0x55d070`
- `CField_PartyRaidResult::OnSessionValue` at `0x55e5c0`
- `CField_SnowBall::OnSnowBallTouch` at `0x560510`
- `CField_SnowBall::OnSnowBallState` at `0x560ab0`
- `CField_SnowBall::BasicActionAttack` at `0x5617b0`
- `CField_SnowBall::OnSnowBallHit` at `0x5619d0`
- `CField_SnowBall::OnSnowBallMsg` at `0x562040`
- `CField_SpaceGAGA::OnClock` at `0x5625d0`
- `CTimerboard_SpaceGAGA::Draw` at `0x5626c0`
- `CField_Wedding::SetBlessEffect` at `0x563b60`
- `CField_Wedding::OnWeddingProgress` at `0x5640f0`
- `CField_Witchtower::OnScoreUpdate` at `0x564ad0`
- `CScoreboard_Witchtower::Draw` at `0x564bd0`
- `CField_ContiMove::OnStartShipMoveField` at `0x54c970`
- `CField_ContiMove::OnContiState` at `0x54d5a0`
- `CField_KillCount::OnPacket` at `0x554050`
- `CField_LimitedView::Init` at `0x5554a0`
- `CField_HuntingAdballoon::OnFieldSetVariable` at `0x552050`
- `CField_MassacreResult::OnPacket` at `0x55a1d0`
- `CField_EscortResult::OnSessionValue` at `0x551160`
- `CField_Tournament::OnPacket` at `0x563780`
- `CRPSGameDlg::OnCreate` at `0x6d7480`
- `CRPSGameDlg::OnPacket` at `0x6d9e00`
- `CField_ShowaBath::DecodeFieldSpecificData` at `0x55ff70`
- `CField_Tutorial::DecodeFieldSpecificData` at `0x563830`
- `CField_MonsterCarnivalWaitingRoom::Init` at `0x55bda0`
- `CField_MonsterCarnivalS2_Game::Init` at `0x55bf10`
- `CField_NoDragon::Init` at `0x55c0e0`
- `CField_WeddingPhoto::GetFieldType` at `0x53eee0`
- `CField_AranTutorial::GetFieldType` at `0x53f1c0`
- `CField_Balrog::GetFieldType` at `0x549810`
- `CCakePieEvent::SetEventItemInfo` at `0x47c8e0`
- `CCakePieEvent::OpenUI_ItemInfo` at `0x47c830`
- `CCakePieEvent::CloseUI_ItemInfo` at `0x479040`
- `CUICakePieEventItemInfo::OnCreate` at `0x47a3c0`
- `CUICakePieEventItemInfo::Draw` at `0x47a4e0`
- `CUICakePieEventItemInfo::Update` at `0x4791c0`
- `CUICakePieEventItemInfo::OnButtonClicked` at `0x4791a0`
- `CTimerboard_CakePieEvent::OnCreate` at `0x47abc0`
- `CTimerboard_CakePieEvent::Draw` at `0x47bab0`
- `CTimerboard_CakePieEvent::GetFontTime` at `0x47c670`

## Client Function Index By Backlog Area

The list below maps this backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Special field and minigame parity

- `CField_GuildBoss::CField_GuildBoss` at `0x53e980`
- `CField_Coconut::OnClock` at `0x54a100`
- `CField_Coconut::OnCoconutHit` at `0x54a470`
- `CField_Coconut::BasicActionAttack` at `0x54a5e0`
- `CField_Coconut::DrawBoard` at `0x54a750`
- `CField_AriantArena::OnUserScore` at `0x5492b0`
- `CField_AriantArena::OnShowResult` at `0x547630`
- `CField_Battlefield::OnClock` at `0x549ad0`
- `CField_Battlefield::OnScoreUpdate` at `0x5499a0`
- `CField_Battlefield::OnTeamChanged` at `0x5499e0`
- `CField_Battlefield::SetUserTeam` at `0x549870`
- `CField_Dojang::OnClock` at `0x550940`
- `CField_Dojang::Update` at `0x54ef10`
- `CField_CookieHouse::_UpdatePoint` at `0x54daa0`
- `CField_CookieHouse::Init` at `0x54e250`
- `CField_Wedding::OnWeddingProgress` at `0x5640f0`
- `CField_Wedding::SetBlessEffect` at `0x563b60`
- `CField_Witchtower::OnScoreUpdate` at `0x564ad0`
- `CScoreboard_Witchtower::Draw` at `0x564bd0`
- `CField_Massacre::OnClock` at `0x556af0`
- `CField_Massacre::UpdateKeyAnimation` at `0x556bf0`
- `CTimerboard_Massacre::Draw` at `0x557100`
- `CMemoryGameDlg::OnButtonClicked` at `0x628df0`
- `CMemoryGameDlg::OnCreate` at `0x62f3c0`
- `CMemoryGameDlg::Draw` at `0x631cb0`
- `CField_SnowBall::OnSnowBallState` at `0x560ab0`
- `CField_SnowBall::OnSnowBallHit` at `0x5619d0`
- `CField_SnowBall::OnSnowBallMsg` at `0x562040`
- `CField_SnowBall::OnSnowBallTouch` at `0x560510`
- `CField_MonsterCarnival::CreateUIWindow` at `0x55a510`
- `CField_MonsterCarnival::OnEnter` at `0x55a6c0`
- `CField_PartyRaid::Init` at `0x55f110`
- `CField_PartyRaidBoss::OnFieldSetVariable` at `0x55ca40`
- `CField_PartyRaidResult::OnSessionValue` at `0x55e5c0`
- `CField_SpaceGAGA::OnClock` at `0x5625d0`
- `CTimerboard_SpaceGAGA::Draw` at `0x5626c0`
- `CField_ContiMove::OnStartShipMoveField` at `0x54c970`
- `CField_ContiMove::OnContiState` at `0x54d5a0`
- `CField_KillCount::OnPacket` at `0x554050`
- `CField_LimitedView::Init` at `0x5554a0`
- `CField_HuntingAdballoon::OnFieldSetVariable` at `0x552050`
- `CField_MassacreResult::OnPacket` at `0x55a1d0`
- `CField_EscortResult::OnSessionValue` at `0x551160`
- `CField_Tournament::OnPacket` at `0x563780`
- `CRPSGameDlg::OnCreate` at `0x6d7480`
- `CRPSGameDlg::OnPacket` at `0x6d9e00`
- `CField_ShowaBath::DecodeFieldSpecificData` at `0x55ff70`
- `CField_Tutorial::DecodeFieldSpecificData` at `0x563830`
- `CField_MonsterCarnivalWaitingRoom::Init` at `0x55bda0`
- `CField_MonsterCarnivalS2_Game::Init` at `0x55bf10`
- `CField_NoDragon::Init` at `0x55c0e0`
- `CField_WeddingPhoto::GetFieldType` at `0x53eee0`
- `CField_AranTutorial::GetFieldType` at `0x53f1c0`
- `CField_Balrog::GetFieldType` at `0x549810`

Notes:
IDA shows that these field modes are not generic map-rule variants. The client owns separate state machines, packet handlers, timer or scoreboard widgets, and even dedicated minigame dialog owners such as `CMemoryGameDlg` for Guild Boss, Coconut, Wedding, Witchtower, Massacre, SnowBall, Ariant Arena, Battlefield, Dojang, Cookie House, Monster Carnival, Party Raid, SpaceGAGA, and MiniRoom card games, so parity work should be tracked separately from the broader field, combat, or UI backlog areas.
The latest broad `FieldFactory` / `OnPacket` re-check did not add any new completely untracked special-field families beyond the rows already indexed below; the remaining gaps are now row-depth and ownership-detail gaps rather than missing top-level wrapper discovery.

### 2. Additional packet-owned microgame dialog discovered by follow-up IDA scan

- `CRPSGameDlg::OnCreate` at `0x6d7480`
- `CRPSGameDlg::OnPacket` at `0x6d9e00`
- `CRPSGameDlg::ProcessPacket` at `0x6d72d0`
- `CUtilDlgEx` notice path (`StringPool` `0xE83`) via `CRPSGameDlg::OnPacket`

Notes:
`CRPSGameDlg` is not a generic field-utility callback; it is a dedicated packet-owned dialog owner with an explicit `OnCreate` seam plus packet-owned create/destroy and round-state branches. Keep this indexed separately from the broader special-field owner list.

### 3. Additional client-owned field wrappers discovered by IDA function scan

- `CField::FieldFactory`
- `CField_DynamicFoothold::GetFieldType`
- `CField_DynamicFoothold::Init` at `0x551050`
- `CField_ContiMove::OnContiState` at `0x54d5a0`
- `CField_Tutorial::DecodeFieldSpecificData` at `0x563830`
- `CField_LimitedView::Init` at `0x5554a0`

Notes:
These wrapper owners are intentionally indexed as a separate scan-discovery surface because they represent field-class ownership seams (transport/tutorial/presentation/dynamic-foothold wrappers) rather than minigame-specific rules.

### 4. Additional Party Raid wrapper-family ownership confirmed by `CField::FieldFactory` re-check

- `CField::FieldFactory` at `0x53f220`
- `CField_PartyRaid::GetFieldType` at `0x55c440`
- `CField_PartyRaidBoss::GetFieldType` at `0x55c640`
- `CField_PartyRaidResult::GetFieldType` at `0x55c800`

Notes:
The latest `CField::FieldFactory` re-check confirms that Party Raid is not a single generic owner. The client still instantiates three distinct wrapper classes, so that split should stay indexed separately from the broader gameplay row below.

### 5. Cake Pie event timerboard and item-info owners discovered by follow-up IDA function scan

- `CCakePieEvent::SetEventItemInfo` at `0x47c8e0`
- `CCakePieEvent::OpenUI_ItemInfo` at `0x47c830`
- `CCakePieEvent::CloseUI_ItemInfo` at `0x479040`
- `CUICakePieEventItemInfo::OnCreate` at `0x47a3c0`
- `CUICakePieEventItemInfo::Draw` at `0x47a4e0`
- `CUICakePieEventItemInfo::Update` at `0x4791c0`
- `CUICakePieEventItemInfo::OnButtonClicked` at `0x4791a0`
- `CTimerboard_CakePieEvent::OnCreate` at `0x47abc0`
- `CTimerboard_CakePieEvent::Draw` at `0x47bab0`
- `CTimerboard_CakePieEvent::GetFontTime` at `0x47c670`

Notes:
The follow-up function scan found a Cake Pie event owner family that the field-factory sweep would not expose as a top-level `CField` wrapper. `CTimerboard_CakePieEvent::OnCreate` subclasses the generic `CClock` path, creates its own backing canvas, and switches between `Map/Obj/etc.img/5th_TownUI/{backgrd,bareffect,gage/0,gage/1}` and `Map/Obj/etc.img/5th_Timer/{backgrd,ready,start,cake,pie}` depending on timer type. The paired `CUICakePieEventItemInfo` owner has its own close button and draw/update lifecycle, while `CCakePieEvent` owns item-info setup and open/close routing. That makes Cake Pie a distinct event UI/timerboard owner rather than only a generic clock or reward-popup variant.

## Current State Summary

MapSimulator already has a partial special-field baseline instead of a blank area.
The project currently includes:

- A `SpecialEffectFields` surface with wedding, witchtower, guild-boss, and massacre handling.
- A concrete `MinigameFields` surface that now owns the snowball and coconut runtimes behind one backlog-aligned seam.
- A `SpecialFieldRuntimeCoordinator` seam in `MapSimulator` that binds special-field ownership to map load, update, draw, and reset flow so later agents can take one backlog row without redoing the simulator plumbing.
- Several packet-commented simulator seams that already reference the corresponding client field owners.

The parity gap here is not whether the simulator recognizes special maps at all.
The gap is whether those maps reproduce the client's dedicated rules, scoreboards, timers, UI flows, and win or lose transitions.

## Broad IDA Re-check

The latest broad wrapper-owner re-check against `CField::FieldFactory` (`0x53F220`) and `CField::OnPacket` (`0x546D50`) did not surface any additional special-field families that still sit completely outside this document.
The factory sweep still resolves only to wrapper families already tracked here or intentionally owned by other backlog areas, including transport/Balrog, Tournament, Coconut, Guild Boss, Limited View, Monster Carnival variants, Ariant Arena, Dojo, Wedding/Wedding Photo, tutorial variants, Cookie House, Battlefield, SpaceGAGA, Witchtower, Aran Tutorial, Massacre/Massacre Result, Party Raid variants, No Dragon, Dynamic Foothold, Escort Result, Hunting Ad Balloon, Kill Count, and Showa Bath.
The packet-owner sweep also still funnels the shared active-wrapper handoff through opcode `163`, which is already captured in the shared current-wrapper dispatch row below rather than being a separate missing backlog family.
A later broad function scan did surface the Cake Pie event timerboard and item-info owner family. It is tracked below as an event UI owner because it does not appear to be a new top-level `CField::FieldFactory` wrapper family, but it still owns special-event presentation that would be missed by only following field-factory classes.

## Remaining Parity Backlog

### 1. Special field and minigame parity

This area is now tracked in its own backlog document because both the client and simulator already expose dedicated field-specific seams.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Guild boss event fields | Guild-boss ownership remains in the existing `SpecialEffectFields` seam: `DetectFieldType` still prioritizes the map-root WZ `healer` plus `pulley` contract, preserves legacy field-type and id-range fallbacks, and configures `GuildBossField` directly from those map-root nodes. WZ evidence is unchanged for `Map/Map9/990000900` (`healer`: `x=295`, `yMin=-564`, `yMax=-350`, `rise=1`, `fall=10`, `healMin=1000`, `healMax=2000`; `pulley`: `x=-59`, `y=-394`) and `Map/Obj/guild.img/syarenian/boss/{0,1}` still carries 18 healer frames (origin `124,199`) plus 3 pulley-hit frames (origin `184,181`) with pulley-hit delay `90` ms. Client evidence is also unchanged: `CField_GuildBoss::BasicActionAttack` (`0x5517d0`) still short-circuits pulley flow when `CReactorPool::FindHitReactor(...)` succeeds, still gates outbound `COutPacket(259)` on `m_nState == 0` after `CPulley::Hit`, and `CPulley::Hit` (`0x551390`) still suppresses restart while animation is already active. Runtime ownership still matches that seam: local attacks avoid simulator-only preview while official-session ownership is armed/pending, opcode `259` is sent immediately on initialized proxied sessions, queued for reconnect handoff when the bridge is listening but not initialized, and now also queued instead of rejected when a direct send is attempted while the bridge only has passive established-session ownership; `/guildboss session` still exposes passive `attach <remotePort> [processName|pid] [localPort]`, reconnect-armed `attachproxy <listenPort|0> <remotePort> [processName|pid] [localPort]`, and `start`/`startauto`/`attachproxy` listen-port `0` handling. This pass keeps all work in that seam and tightens live-session listen-port parity further: `GuildBossOfficialSessionBridgeManager.TryStart(...)` now passes `listenPort = 0` through to the underlying localhost proxy and mirrors the actual ephemeral bind back through `ListenPort` and status output, while `EnsureGuildBossOfficialSessionBridgeState(...)` still treats configured auto-listen (`listenPort = 0`) as "keep current ephemeral bind if target host/port still matches" so the bridge no longer churn-restarts while armed, preserving deferred opcode `259` ownership until reconnect attaches. `GuildBossOfficialSessionBridgeManager.TryAttachEstablishedSessionAndStartProxy(...)` now also keeps an already-armed proxy listener when repeated `attachproxy` requests resolve to the same discovered remote endpoint (including `listenPort = 0` auto-listen matching), so idempotent reconnect-arming no longer tears down deferred opcode `259` ownership. `TryAttachEstablishedSessionAndStartProxy(...)` now also preserves passive established-session ownership when proxy listener startup fails (for example, bind conflict on an explicit listen port), so retrying reconnect-arming does not silently drop the observed socket-pair owner seam. Reactor-hit suppression in `HandleSpecialFieldAttackHitbox(...)` now uses a hit-only reactor search (`ReactorPool.FindHitReactorsInBounds(...)`) that mirrors `CReactorPool::FindHitReactor` ownership instead of the broader skill-reactor intersection path, so skill-only reactor overlaps no longer suppress pulley preview/request startup. `GuildBossField.TryHandleLocalPulleyAttack(...)` continues to mirror the client short-circuit ordering by keeping only the minimum local basic-action owner window when reactor-hit flow skips `CPulley::Hit`, while non-reactor pulley hits still use the pulley-hit animation ownership window. Focused source coverage now exists in `UnitTest_MapSimulator/GuildBossParityTests.cs` for reactor-hit suppression vs no-reactor pulley-request arming, reactor-vs-pulley ownership-window duration behavior, hit-reactor candidate filtering rules (`CanParticipateInHitReactorSearch`), ownership helper rules, and auto-listen bridge reuse matching rules plus idempotent `attachproxy` re-arm listener retention and attachproxy bind-failure passive-ownership retention; `UnitTest_MapSimulator/GuildBossOfficialSessionBridgeManagerTests.cs` covers `listenPort = 0` binding to a real ephemeral port, auto-listen target matching, and passive established-session direct-send queueing for reconnect handoff. Remaining gap stays specific to post-handshake live-session ownership: the simulator still cannot decrypt inbound or inject outbound guild-boss traffic into an already-established Maple socket pair unless that session reconnects through the localhost official-session proxy first. | Guild boss now owns WZ-backed map detection, client-owned `CReactorPool::FindHitReactor` attack suppression, `CPulley::Hit` one-shot animation semantics, packet decode, outbound opcode recovery, proxy emission, passive established-session observation, reconnect-armed proxy attach, deferred official-session injection including direct-send queueing while passively attached, real ephemeral auto-listen binding plus auto-listen bridge retention for `listenPort=0`, and row-focused parity coverage in the same special-field seam. The row remains `Partial` only because true in-place post-handshake decrypt/inject ownership on an already-established Maple socket pair is still missing; remaining work is no longer detection, contract extraction, pulley lifecycle, reactor-hit attack gating, basic-action ownership handoff around reactor short-circuit, command-surface exposure, proxy-port `0` handling, actual auto-listen binding/status, auto-listen bridge stability, reactor-short-circuit ownership timing, idempotent `attachproxy` listener reuse, queueing ownership, passive direct-send handoff queueing, or passive-ownership retention when reconnect listener startup fails. | `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `SpecialFieldRuntimeCoordinator.cs`, `SpecialEffectFields.cs`, `GuildBossField.cs`, `ReactorPool.cs`, `GuildBossPacketTransportManager.cs`, `GuildBossOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/GuildBossParityTests.cs`, `GuildBossField` (`CField_GuildBoss`, `CField_GuildBoss::Init`, `CField_GuildBoss::OnPacket`, `CField_GuildBoss::BasicActionAttack`, `CHealer::Init`, `CHealer::Move`, `CPulley::Init`, `CPulley::Hit`) |
| Partial | Coconut minigame runtime | WZ evidence was re-checked first against the active `v95_2` IMG source and still anchors this runtime seam to authored data rather than simulator invention: `Map/Obj/etc/coconut/backgrnd` exposes a `258x101` board, `Map/Map/Map1/109080000/coconut` carries the count/time/avatar contract (`countFalling=401`, `countBombing=80`, `countStopped=20`, `countHit=5`, `timeDefault=300`, `timeExpand=120`, `timeMessage=6`, `timeFinish=12`, `effectWin=event/coconut/victory`, `effectLose=event/coconut/lose`), and the board glyph anchors remain owned by `CField_Coconut::DrawBoard`. Client evidence checked second remains aligned with the same owners: `CField_Coconut::OnCoconutHit` at `0x54a470` decodes target, delay, and state, expands negative targets into one delayed hit entry per coconut, and timestamps them from `get_update_time`; `CField_Coconut::OnCoconutScore` at `0x54bf70` assigns both scores then redraws the board; `CField_Coconut::OnClock` at `0x54a100` keeps finish-timestamp clock semantics with zero coerced to `1`; and `CField_Coconut::DrawBoard` at `0x54a750` still draws scores/time at `25,37`, `150,37`, and `60,83`. This pass keeps work in the existing runtime seam and tightens packet-applied presentation ownership: `CoconutField.ApplyPacketState(...)` now dirties the simulator board layer whenever delayed packet-owned hit state applies and restarts authored state-frame presentation from the delayed apply tick, so negative-target broadcast packets, single-target hit packets, and local preview hits refresh the cached board presentation through the same ownership path as score and clock packets while keeping frame timing aligned to the packet-owned state transition. Focused regression coverage in `UnitTest_MapSimulator/CoconutParityTests.cs` now includes recovered negative-target broadcast expansion, delayed authored-state frame-clock restart, packet-owned score update, zero-clock coercion, no-target basic-action consumption, and first-intersection runtime-order attack target selection. Existing transport ownership rules remain in place: local delayed-hit preview still gates on actual transport ownership state, opcode-`257` queued requests remain queued for attached/passive official ownership until proxied handshake init, and fallback to Coconut inbox transport still only occurs when no official owner exists. Remaining gap is still explicit and narrower: Coconut cannot decrypt/inject against an already-established MapleStory socket pair until reconnect through the localhost proxy restores handshake-owned crypto state, and board rendering still uses simulator-managed primitives/cache rather than the client's full native `IWzCanvas`/`IWzGr2DLayer` lifecycle. | Coconut now keeps packet/runtime ownership, transport/request ownership, local-preview ownership, basic-action-vs-reactor ownership, recovered negative-target delayed-hit broadcast behavior, packet-applied board-cache invalidation, and delayed authored frame-clock restart in the same seam with source coverage for the command/runtime edges touched here. Remaining work is no longer request-targeting, owner-window lifetime, preview-vs-transport gating, queued-request transport ownership precedence, basic-action lane consumption when no coconut target resolves, first-intersection target selection, score/clock packet ownership, negative-target hit expansion, delayed authored-state animation restart, team-selection precedence, or parser/anchor parity; it is the post-handshake decrypt/inject limitation on already-live socket pairs plus deeper native board-layer lifecycle fidelity. | `MinigameFields.cs`, `MapSimulator.cs`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.ClientOwnedFieldWrappers.cs`, `HaCreator/MapSimulator/CoconutSessionCommandParsing.cs`, `CoconutPacketInboxManager.cs`, `CoconutOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/CoconutParityTests.cs`, `CoconutField` (`CField_Coconut::Init`, `CField_Coconut::OnPacket`, `CField_Coconut::OnClock`, `CField_Coconut::OnCoconutHit`, `CField_Coconut::OnCoconutScore`, `CField_Coconut::BasicActionAttack`, `CField_Coconut::DrawBoard`) |
| Partial | Wedding ceremony fields | The simulator still resolves ceremony lines from `String/Npc.img/<npcId>/wedding{step}` with WZ-backed fallbacks, keeps the Saint Maple step-2 guest-bless modal/packet split (`164` only on `YES`), reuses the recovered WZ ceremony presentation assets (`UI/UIWindow.img/wedding/text/0`, `wedding/card/0`, `Effect/BasicEff.img/Wedding`, `Map/MapHelper.img/weather/wedding`, `weather/heartWedding`), and preserves the existing `/wedding` runtime seam for step, dialog, actor, inbox, and response ownership. WZ was rechecked again in the active v95 export for this pass: `UIWindow.img/wedding/text/0` is still a 518x659 canvas, `UIWindow.img/wedding/card/0` is still a 295x168 canvas, `Effect/BasicEff.img/Wedding` still exposes 16 authored frames, `MapHelper.img/weather/wedding` still carries `count=35`, and `MapHelper.img/weather/heartWedding` still carries `count=35` with 8 authored heart frames intact. This pass tightens the same wedding-side remote actor seam around relationship/name-tag ownership one step further: shared remote-user avatar-modified relationship sync still preserves explicit recovered relationship owner ids when the wedding runtime has already normalized couple/friend pair-lookup records, and pair-lookup normalization now also honors the recovered partner record's explicit owner/pair ids before falling back to numeric character-id ordering, so lower-id packet actors no longer steal canonical ownership from an already recovered higher-id groom/bride or friend-pair record. The shared name-tag/relationship overlay table keeps the resolved owner/pair character ids plus owner/pair serials instead of re-collapsing the record onto the packet actor during external renderer sync. The earlier wedding runtime ownership stays intact: couple/friend pair-lookup add packets normalize against the recovered partner record and dispatch-key table before application or deferred replay, relationship-record add handling registers packet dispatch keys and related serial keys, serial-keyed couple/friend/new-year remove packets resolve owner through that dispatch map before broad participant scans, unresolved owner removals queue onto the deferred per-character replay path, and actor teardown/runtime reset clears relationship dispatch ownership to avoid stale remove routing across role swaps and scene resets. Focused coverage in `UnitTest_MapSimulator/WeddingCeremonyParityTests.cs` now covers live pair-lookup normalization including recovered canonical owner preservation when the packet actor has the lower character id, deferred couple pair-lookup add followed by serial remove before actor resolve, and shared remote-user avatar-modified sync preserving the recovered wedding owner id plus serial dispatch removal. The remaining gap stays explicit: the simulator still models only a presentation-focused `SecondaryStat::DecodeForRemote` slice rather than the full temporary-stat surface, still does not reproduce the full native relationship/name-tag owner choreography beyond recovered profile/guild-mark/visibility, pair-lookup normalization, recovered canonical owner preservation, shared-pool owner preservation, and dispatch-key relationship replay seams, and still reconstructs wedding scene ownership from recovered WZ layers/effects rather than the client's complete native `IWzGr2DLayer` wedding timeline. | Wedding maps now keep client-backed dialog lifecycle, WZ-backed overlays/effects/weather, wedding-side remote actor decode/replay, hidden-like-client visibility handoff, deferred profile/guild/temporary-stat/relationship replay, dispatch-key relationship remove ownership for serial-keyed couple/friend/new-year packets, pair-lookup couple/friend relationship normalization with recovered canonical owner preservation, and shared remote-user relationship owner preservation in the same wedding/external-renderer seam. Remaining work is concentrated on unrecovered full temporary-stat decode, deeper native name-tag relationship choreography beyond this pair-lookup/dispatch-key/shared-pool replay slice, and full native wedding layer timeline parity. | `SpecialEffectFields.cs`, `WeddingField`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.WeddingRemoteUsers.cs`, `MapSimulator.UpdateLoop.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `WeddingPacketInboxManager.cs`, `UnitTest_MapSimulator/WeddingCeremonyParityTests.cs` (`CField_Wedding::OnWeddingProgress`, `CField_Wedding::OnWeddingCeremonyEnd`, `CField_Wedding::SetBlessEffect`, `CUserPool::OnUserEnterField`, `CUserPool::OnUserLeaveField`, `CUserPool::Update`, `CUserPool::OnUserRemotePacket`, `SecondaryStat::DecodeForRemote`, `CUserRemote::OnMove`, `CUserRemote::OnAvatarModified`, `CUserRemote::Update`) |
| Implemented | Witchtower score UI | The simulator anchors the Witchtower scoreboard to the client's center-top window origin, loads the exact `Map/Obj/etc.img/goldkey` background, key, and bitmap digits that match the `CScoreboard_Witchtower::OnCreate` asset shape, renders the zero-padded score at the verified `(67, 4)` draw origin with the client's negative digit spacing, and keeps the `witchscore` debug seam driving the same update path | This is a visible special-field HUD with client-backed art and placement now, so the remaining special-field work should move to other rows instead of revisiting a placeholder Witchtower overlay | `SpecialEffectFields.cs`, `WitchtowerField`, `MapSimulator.cs` (`CField_Witchtower::OnScoreUpdate`, `CScoreboard_Witchtower::OnCreate`, `CScoreboard_Witchtower::Draw`) |
| Partial | Massacre timerboard and gauge flow | The simulator keeps the same client-owned Massacre HUD baseline in the existing seam: type-2 timerboard ownership, WZ-backed count board and key animation lifecycle, 259-pixel decay gauge with the same danger cutoff behavior, packet `173` inc-gauge intake, packet `174` result payload shape, mapped clock/context/stage/bonus ingress, and the official-session bridge command surface (`discover`, `attach`, `attachproxy`, `start`, `startauto`, `map`, `unmap`, `recent`, `clearrecent`, `stop`) including `listenPort 0`. WZ and client evidence anchors remain unchanged (`Map/Obj/etc.img/killing/backgrnd`, `UI/UIWindow.img/MonsterKilling/{Count,Gauge,Result}`, `Map/Effect.img/killing/*`, `CField_Massacre::OnClock` `0x556af0`, `CTimerboard_Massacre::Draw` `0x557100`, `CField_MassacreResult::OnMassacreResult` `0x5596c0`, wrapper-flow scripts `killing_BonusSetting` / `Massacre_result`, and `CWvsContext::OnSessionValue` `0x9e41b0`). Ownership remains inside that ingress seam and makes recovered opcode-`93` live traffic concrete instead of generic: default and current-wrapper-relayed `CWvsContext::OnSessionValue(93)` packets decode the native two-`DecodeStr` key/value payload, route `massacre_laststage` (`0x14F3`) to stage presentation, route non-negative `massacre_party` (`0x14F5`) to bonus presentation, accumulate `massacre_hit`/`massacre_miss`/`massacre_cool` plus optional `massacre_skill` into the existing Massacre info message, and reject negative values through the Massacre ownership gates while preserving the client's `atoi` contract (leading sign/whitespace accepted, digit-prefix accepted, non-digit tail ignored). The negative-value owner seam is now tighter: raw packet `173` rejects negative `Decode4` inc-gauge values instead of silently clamping them, raw type-2 clock payloads reject negative durations instead of converting them to an empty timerboard, local/chat command ingress routes clock/inc/info/stage/bonus/result through the same owner gates, inbox bonus messages reject negative values, and simulator-authored result presentation rejects negative score overrides plus unknown rank letters before mutating the result board. Focused coverage now exists in `UnitTest_MapSimulator/MassacreParityTests.cs` for client-style `atoi`, default opcode-`93` stage decode, negative party rejection, hit/miss/cool/skill context accumulation, current-wrapper-relayed opcode-`93` decode, negative raw inc-gauge rejection, negative raw clock-duration rejection, structured negative bonus rejection, structured negative result-score rejection, and structured unknown result-rank rejection. Remaining gap stays narrow and explicit: Massacre still cannot decrypt inbound or inject outbound traffic on an already-established post-handshake Maple socket pair without reconnecting through the localhost proxy first, and byte-accurate recovery of additional server-owned Massacre opcodes beyond the currently recovered `173`/`174`, opcode-`93` Massacre session-value key slice, and mapped clock/context/stage/bonus intake is still missing. | Hunting-event maps depend on dedicated timerboard/count/gauge/result ownership that the client does not route through generic HUD state. This row now keeps that recovered owner surface while also hardening default and relayed opcode-`93` ownership plus raw packet/clock/local command/result ingress against generated StringPool drift, client-style numeric key/value parsing differences, and negative local payload bypasses in the same bridge seam, with test-backed coverage for recovered key ids, value parsing behavior, concrete session-value dispatch, and raw/structured negative-value rejection. Remaining work is concentrated on post-handshake decrypt/inject ownership and unrecovered opcode choreography, not on timerboard/gauge/result rendering ownership, mapped command-surface coverage, recovered session-value key ingestion robustness, or negative-value gating for recovered local Massacre ingress. | `SpecialEffectFields.cs`, `MassacreField`, `MapSimulator.cs`, `MapSimulator.Content.cs`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.ChatCommands.cs`, `MassacreSessionCommandParsing.cs`, `MassacrePacketInboxManager.cs`, `MassacreOfficialSessionBridgeManager.cs`, `MapleStoryStringPool.cs`, `UnitTest_MapSimulator/MassacreParityTests.cs` (`CField_Massacre::Init`, `CField_Massacre::_SetMassacreInfo`, `CField_Massacre::_SetDecGauge`, `CField_Massacre::OnPacket`, `CField_Massacre::OnClock`, `CField_Massacre::Update`, `CField_Massacre::UpdateKeyAnimation`, `CField_Massacre::_SetDecGauge`, `CTimerboard_Massacre::OnCreate`, `CTimerboard_Massacre::Draw`, `CField_MassacreResult::OnPacket`, `CField_MassacreResult::OnMassacreResult`, `CWvsContext::OnSessionValue`) |
| Partial | Memory Game and MiniRoom card parity | The simulator keeps Match Cards prompt choreography in the existing runtime seam and now mirrors more non-tie outbound branch ownership from the client instead of only enforcing payload shape. WZ was re-checked first in `E:\MapleStory\WzExport\v95_2`: `UI/UIWindow2.img/Minigame/Common` still exposes the shared Match Cards button surface (`btDraw`, `btAbsten`, `btExit`) used by this flow. Client evidence was re-checked second in IDA and applied in `MemoryGameField`: `CMemoryGameDlg::OnClickEndButton` (`0x628450`) still sends one-byte subtype `56`, `57`, or `10` and toggles `m_bLeaveBooked`; `CMemoryGameDlg::OnClickReadyButton` (`0x6282b0`) still emits one-byte `58` or `59`; `CMemoryGameDlg::OnClickStartButton` (`0x627a50`) still gates `61` only on seated-user count (`m_nCurUsers >= 1`); and `CMemoryGameDlg::OnClickBanButton` (`0x6283a0`) still sends one-byte `60` after prompt confirm without a late owner/target re-check. This pass also re-checked `CMemoryGameDlg::OnClickEndButton` and mirrors its post-send `Update(2)` call for subtype `10` by preserving the close/update argument after the simulator room reset. This pass keeps the prior typed outbound payload-shape guard for subtypes `10`, `50`, `51`, `52`, `56`, `57`, `58`, `59`, `60`, and `61`, and extends non-tie side-effect parity in the same outbound seam: subtype `10` now accepts non-playing room states (lobby/result) and rejects only active-round use, outbound `56/57` now keep local leave-book toggles without overwriting status text, outbound one-byte `60` no longer revalidates owner/seat after prompt consumption, outbound `61` mirrors the seated-opponent gate without an extra owner gate, and mirrored `58/59` no longer fail solely on simulator stage. This pass also mirrors the client ready-click SFX branch in the same outbound seam by resolving StringPool ids `0x8C4` (`Sound/MiniGame.img/`) and `0x649` (`Ready`) to `Sound/MiniGame.img/Ready` and playing that WZ-backed sound on successful outbound `58/59`. This pass now also keeps the subtype `10` local close/reset branch from erasing the client-opponent ready/score layer release signal: if an opponent seat was materialized before the client leave send, the simulator records ready-layer and score-layer release after the reset while still preserving the post-send `Update(2)` argument. Focused coverage was expanded in `MemoryGamePromptParityTests` for `56/57` prompt-response status preservation, non-owner-seat `61` acceptance with an opponent present, one-byte `60` acceptance without late target re-check, mirrored `58` acceptance during active rounds, outbound `58/59` ready-click SFX callback parity, subtype `10` acceptance from result state, subtype `10` post-send dialog `Update(2)` preservation after local close/reset, and subtype `10` local close ready/score layer release from both lobby and result states, alongside existing malformed prompt-payload and tie decision-byte assertions. Remaining gap is narrower: direct attach to an already-established Maple socket pair still cannot decrypt/inject MiniRoom traffic without reconnecting through the proxy bridge first, and the remaining native per-branch UI choreography is limited to deeper modal/layer timing details beyond the recovered subtype `10` close-update and ready/score layer release signals. | MiniRoom parity now keeps prompt-owned request gating, payload-shape ownership, and a larger share of non-tie send-branch side effects in one runtime path used by local commands, loopback inbox playback, and proxy-mirrored outbound requests, including the ready-click minigame SFX branch for outbound `58/59` through the client StringPool-to-WZ sound seam, the subtype `10` post-send `Update(2)` close argument, and local close ready/score layer release after room reset. Remaining work is concentrated on post-handshake live-session ownership and the last byte-level client-local modal timing details rather than missing Match Cards packet families, missing prompt-bound request-shape validation, or missing subtype `10` close/reset layer invalidation. | `HaCreator/MapSimulator/Fields/MemoryGameField.cs`, `HaCreator/MapSimulator/Interaction/MapleStoryStringPool.cs`, `UnitTest_MapSimulator/MemoryGamePromptParityTests.cs`, `HaCreator/MapSimulator/Managers/MemoryGameOfficialSessionBridgeManager.cs`, `HaCreator/MapSimulator/Managers/MemoryGamePacketInboxManager.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `HaCreator/MapSimulator/MapSimulator.Content.cs`, `HaCreator/MapSimulator/MapSimulator.ChatCommands.cs` (`CMemoryGameDlg::OnButtonClicked`, `CMemoryGameDlg::OnClickEndButton`, `CMemoryGameDlg::OnClickReadyButton`, `CMemoryGameDlg::OnClickStartButton`, `CMemoryGameDlg::OnClickBanButton`, `CMemoryGameDlg::SendClaimGiveUp`, `CMemoryGameDlg::SendTieRequest`, `CMemoryGameDlg::OnTieRequest`) |
| Implemented | Snowball minigame runtime | The simulator now treats Snowball state `2/3` as the client's packet-owned win states instead of local touch-zone states, keeps snowman HP packet-owned via `OnSnowBallState`, applies WZ-backed snowman stun windows and damage defaults through the runtime seam, smooths active snowball movement from the packet position plus speed data instead of instant teleports, routes the local player's world position through `SpecialFieldRuntimeCoordinator` so `SnowBallField` can surface the client's outbound touch-packet loop as pending team touch requests whenever the local player overlaps a snowball lane, aligns the Story-vs-Maple team labeling used by win banners and the local scoreboard with the client message owner, and routes `OnSnowBallMsg` through the simulator chat log seam with client-shaped team ordering plus packet-owned fallback text instead of showing those packet messages as center-screen overlays. The runtime also stopped depending on ad hoc Snowball setup: `SpecialFieldRuntimeCoordinator` now detects Snowball maps through the preserved `snowBall` map property or `FIELDTYPE_SNOWBALL`, `MinigameFields.BindMap` now auto-binds `SnowBallField`, and `SnowBallFieldDataLoader` reads the copied WZ `snowBall` node from `MapInfo.additionalNonInfoProps` or the bound `WzImage` so the simulator uses the same client-owned config that `CField_SnowBall::Init` consumes. That bind path mirrors the client-owned shared-`x`/shared-`x0` plus per-team-`y` model recovered from `CField_SnowBall::Init` at `0x561a10`: the simulator loads `dx`, `x`, `x0`, `xMin`, `xMax`, `damageSnowBall`, `damageSnowMan0`, `damageSnowMan1`, `snowManHP`, `snowManWait`, `section1`, `section2`, and `section3`, positions both snowmen on the shared client `x` with their own lane `y`, positions both snowballs from the shared client `x0` with their own lane `y`, clamps active movement to the client `xMin/xMax` span, and resolves the WZ `Map/Obj/event.img/snowyRock/snowball/{0,1}` paths directly into `IDXObject` frame sets through the existing minigame seam when a graphics device is available. `SnowBallField.OnSnowBallState` still mirrors the client-owned first-snapshot ownership from `CField_SnowBall::OnSnowBallState` at `0x560ab0` by accepting packet-fed `damageSnowBall`, `damageSnowMan0`, and `damageSnowMan1` values only on that first state snapshot, and repeated `state == 2/3` packets no longer inflate the simulator-only round scoreboard or replay duplicate win banners. The row was re-checked against the exported IMG filesystem through `MapleLib.Img.ImgFileSystemDataSource`, which still does not expose localized `String` nodes for ids `3445`-`3447` (`0xD75`-`0xD77`), but the generated client string-pool table in the simulator already carried those three Snowball notice payloads with `%2C`-escaped comma bytes; this pass closes the last runtime gap by normalizing those ids through `MapleStoryStringPool.OverrideEntries` so `SnowBallField.FormatSnowBallMessage` now resolves the actual client literals instead of simulator-authored fallback prose. The transport-attachment gap is also closed inside the existing seam instead of leaving `SnowBallOfficialSessionBridgeManager` unreachable on disk: `MapSimulator` owns that bridge alongside the loopback inbox, stops it on Snowball exit, drains live-session opcode `338`-`341` payloads into the same `SnowBallField.TryApplyPacket` path as loopback traffic, prefers live-session opcode `256` touch injection when a Snowball proxy is attached while still falling back to `SnowBallPacketInboxManager`, and exposes the full runtime through `/snowball` commands for state, hit, message, touch, inbox, session, and pending-touch inspection or control. Focused tests are now on disk for the recovered Snowball string-pool ids and message formatting path in addition to the Snowball official-session bridge discovery and startup shapes used by `/snowball session`. | Snowball chat, state, movement, touch, config loading, and official-session transport now run through the client-owned seams reviewed for this row, including the exact `OnSnowBallMsg` string-pool ids `0xD75`-`0xD77` rather than fallback English templates. | `MinigameFields.cs`, `SnowBallField`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `SnowBallPacketInboxManager.cs`, `SnowBallOfficialSessionBridgeManager.cs`, `MapleStoryStringPool.cs`, `UnitTest_MapSimulator/SnowBallStringPoolParityTests.cs` (`CField_SnowBall::Init`, `CField_SnowBall::OnSnowBallState`, `CField_SnowBall::BasicActionAttack`, `CField_SnowBall::OnSnowBallHit`, `CField_SnowBall::OnSnowBallMsg`, `CField_SnowBall::OnSnowBallTouch`) |
| Partial | Ariant Arena field flow | The simulator still binds `FIELDTYPE_ARIANTARENA` to `AriantArenaField`, routes the official Ariant remote-user packet family (`179`, `180`, `210`, `211`-`217`, `218`, `219`, `220`, `221`, `222`, `223`, `224`, `225`, `226`, `230`) through the shared `RemoteUserActorPool`, and keeps Ariant score/result ownership on `354` and `171` with WZ-backed `UI/UIWindow.img/AriantMatch/{characterIcon,Result}` plus `Sound/MiniGame.img/Show`. WZ evidence was re-checked in `v95_2`: `Result` still has 14 frames with 100 ms delays, `characterIcon` still has six 14x14 canvases with origin `(0,0)`, and `Sound/MiniGame.img/Show` is still the 949 ms result sound. Client handlers remain anchored to the same IDA seams (`CField_AriantArena::OnUserScore` at `0x5492b0`, `CField_AriantArena::OnShowResult` at `0x547630`, `CUser::OnEffect` at `0x8f9a70`, `CUserRemote::OnHit` at `0x954c50`). `RemoteUserPacketCodec` still preserves optional pre-`DecodeStr` branch prefix bytes for both `224` and `218` string payload families (`StringBranchPrefixBytes`, `HitStringBranchPrefixBytes`) while accepting Maple/plain payloads with multi-`Decode4` tails, enforces the client `CUser::OnEffect` case-`25` requirement for at least one trailing `Decode4` after the decoded string, and rejects subtype `24`/`26` payloads that omit required `DecodeStr` bytes. `RemoteUserActorPool` still carries `218` branch-prefix metadata in `RemoteHitState`, `StringEffectRegistered` carries full `224` trailing-int tails plus branch-prefix metadata end-to-end, and `MapSimulator.AnimationDisplayerParity` resolves catch success hints from decoded trailing ints first and preserved branch-prefix bytes as fallback. The score-layer lifecycle still refreshes after every valid score packet, including empty update lists, unchanged scores, and missing-user removals. This pass tightens the same score-layer owner further: rank icons are now recalculated from the sorted display row after every mutating score update so `characterIcon/0..5` behave as rank-slot canvases instead of insertion-order player badges, and hidden local Ariant jobs now remove any pre-existing local rank row when a later score update is suppressed. Focused coverage in `UnitTest_MapSimulator/AriantArenaFieldTests.cs` now covers score-refresh edge cases, sorted rank-icon reassignment, and hidden-local row removal in addition to the existing Ariant codec/pool/presentation coverage described for `224` and `218`. | Ariant Arena remains packet-owned in the client and now preserves richer `224`/`218` metadata end-to-end in the existing shared seam while keeping the score/rank layer lifecycle closer to `CField_AriantArena::OnUserScore`: valid score packets refresh the rank surface even when local score state does not mutate, rank-slot icons follow the sorted scoreboard rows, and hidden local job branches no longer leave stale local rows behind. Remaining work is still narrowed to deeper native runtime fidelity: remote temporary-stat modeling still covers only a visible `SecondaryStat::DecodeForRemote` slice rather than the full client surface, `224` presentation behavior in `RemoteUserActorPool.TryApplyEffect(...)` still does not reproduce every native `CUser::OnEffect` detail byte-for-byte for non-catch branches (notably subtype-specific local side effects and exact layer/z argument nuances in cases `20`, `24`, and `26`), `218` still does not reproduce every native mob-authored choreography branch in `CUserRemote::OnHit`, result/score rendering still approximates GR2 layer identity and exact canvas mutation rather than reproducing every native layer operation, and true direct live-session socket ownership remains outside the simulator seam. | `AriantArenaField.cs`, `RemoteUserPacketCodec.cs`, `RemoteUserActorPool.cs`, `MapSimulator.AnimationDisplayerParity.cs`, `UnitTest_MapSimulator/AriantArenaFieldTests.cs` (`CField_AriantArena::OnUserScore`, `CField_AriantArena::OnShowResult`, `CUser::OnEffect`, `CUserRemote::OnHit`) |
| Implemented | Battlefield event flow | The simulator now detects Battlefield maps through the special-field runtime, loads `battleField` WZ defaults such as `timeDefault`, `timeFinish`, reward maps, and effect paths, follows the client's packet-owned `OnClock(type=2, seconds)` and `OnScoreUpdate(wolves, sheep)` seams, exposes a Battlefield team-change seam aligned with `OnTeamChanged(characterId, team)` and `SetUserTeam`, resolves a finish phase that derives win/lose effect plus reward-map routing from the WZ `battleField` node, parses the map's `user/<team>` presets so local Battlefield team changes now reapply both the client-backed outfit pieces and the WZ `stat/speed` move-speed override before restoring the original avatar and speed on clear or map exit, retains each preset's `speedmax` and `noitem` data for inspection or later consumption, resolves packet-owned remote team assignments through the same character-id lookup seam, renders the client Battlefield board from `Map/Obj/etc.img/battleField/backgrnd` with the real sheep or wolf score fonts and the six-digit `fontTime` clock layout, queues the resolved reward map through the existing special-field transfer seam once the WZ `timeFinish` phase expires, mirrors the local `SetUserTeam` minimap side effect by forcing the simulator minimap out of its collapsed state for local teams `0` and `2`, pushes the simulator's actual local character id into `BattlefieldField` so packet-shaped `OnTeamChanged` calls classify the local user correctly, wires the map preset's `noitem` and `speedmax` fields into live simulator systems by blocking local Battlefield item use plus equip drags for blocked item ids and by clamping the live player move-speed path through the preset speed cap, and in this pass drives those same WZ-backed team presets through the shared `RemoteUserActorPool` so remote Battlefield users now hot-swap their live avatar presentation too. The shared actor owner now re-syncs Battlefield looks during map bind and per-frame update, resolves late remote spawns against already-known field-owned team assignments instead of requiring the actor to cache the raw team id first, and restores each remote actor's original appearance and speed when Battlefield ownership clears or the map exits. Focused Battlefield tests were added on disk for that late-spawn remote-team path plus reset restore behavior, but the current workspace could not execute them becausePacketReader` accessibility, `LoginUtilityDialogWindow.Configure` accessibility, and missing `SnowBallField` packet constants | Battlefield maps depend on a team-scoring field runtime rather than only ordinary combat rules, and the client keeps both the scoreboard window creation and `SetUserTeam`-owned avatar presentation inside `CField_Battlefield`; the simulator now mirrors that ownership end-to-end by pushing the same WZ-backed Battlefield team presets through both the local player path and the shared remote-user actor owner instead of leaving remote users visually stale after packet-owned team changes | `SpecialEffectFields.cs`, `BattlefieldField`, `RemoteUserActorPool.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `MapSimulator.Content.cs`, `MapSimulator.UpdateLoop.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `EquipUIBigBang.cs`, `MinimapUI.cs`, `UnitTest_MapSimulator/BattlefieldFieldTests.cs` (`CField_Battlefield::OnClock`, `CField_Battlefield::OnScoreUpdate`, `CField_Battlefield::OnTeamChanged`, `CField_Battlefield::SetUserTeam`, `CScoreboard_Battlefield::OnCreate`) |
| Partial | Mu Lung Dojo field flow | The simulator still binds `FIELDTYPE_DOJANG` plus `MuruengRaid`/`dojang*` maps into `DojoField`, mirrors the client-owned type-2 Dojo timerboard and HUD layers, replays the WZ-backed stage/clear/time-over presentations with delayed transfer ownership, keeps `dojang_next` map+portal propagation through `SpecialFieldRuntimeCoordinator` and `QueueMapTransfer`, and keeps the existing opcode-`163` relay unwrap plus loopback `packetclientraw` bridge path. This pass keeps ownership in the same runtime seam and closes a detector split: `SpecialEffectFields.DetectFieldType(...)` now uses the same Dojo activation contract as `SpecialFieldRuntimeCoordinator`, so WZ-authored `mapMark=MuruengRaid` and `onUserEnter=dojang*` maps outside the legacy id fallback no longer report the Mu Lung Dojo backlog area while leaving `DojoField` inactive. WZ was re-checked first: `UI/UIWindow.img/muruengRaid/clock/0/origin` remains `(102,26)`, `Map/Map9/925020000` keeps `info/returnMap=925020000`, `info/forcedReturn=999999999`, and `info/mapMark=MuruengRaid`, and the map-owned portal contract still includes `in00 -> 925020001:out00`, `out00` with `script=dojang_exit`, and the same return/forced-return anchors used by the existing transfer path. Existing client evidence remains anchored to `CField_Dojang::Init` `0x54ffa0`, `CField_Dojang::Update` `0x54ef10`, `CField_Dojang::OnClock` `0x550940`, `CField::OnPacket` `0x546d50`, and `CField::OnFieldSpecificData` `0x52a7e0`: type-2 clock ownership, full-energy presentation at `m_nEnergy >= 10000`, and shared wrapper ownership on opcodes `149` and `163` still match the current simulator seams. Prior focused source coverage remains in `UnitTest_MapSimulator/FieldSpecificDojoRelayParityTests.cs`, `UnitTest_MapSimulator/DojoOfficialSessionBridgeNestedRelayParityTests.cs`, `UnitTest_MapSimulator/DojoFieldTransferParityTests.cs`, and `UnitTest_MapSimulator/DojoFieldEnergyPresentationParityTests.cs`; this run adds `UnitTest_MapSimulator/DojoFieldMapDetectionParityTests.cs` for `FIELDTYPE_DOJANG`, `mapMark=MuruengRaid`, and `onUserEnter=dojang*` activation through the coordinator/runtime seam. The remaining gap stays narrow and specific: the simulator still has not recovered the full official Dojo opcode table byte-for-byte beyond the confirmed clock and wrapper-prefix-owned payload contracts, still does not reproduce the complete server-authored next-floor choreography byte-for-byte, and still cannot decrypt/inject an already-established MapleStory socket pair in place without the localhost proxy reconnect hop first. | Dojo parity remains timer-owned, wrapper-owned, and WZ map-contract-owned. This row now also guarantees that the coordinator and concrete Dojo runtime agree on all current activation evidence (`fieldType`, id fallback, `MuruengRaid`, and `dojang*` entry script), avoiding active-area/runtime divergence on authored Dojo maps outside the legacy id range. Remaining work is the unrecovered full opcode table, byte-accurate floor-transition choreography, and true direct post-handshake socket attach/inject ownership. | `SpecialEffectFields.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.PacketOwnedFieldStateParity.cs`, `DojoPacketInboxManager.cs`, `DojoOfficialSessionBridgeManager.cs`, `DojoField.cs`, `UnitTest_MapSimulator/FieldSpecificDojoRelayParityTests.cs`, `UnitTest_MapSimulator/DojoOfficialSessionBridgeNestedRelayParityTests.cs`, `UnitTest_MapSimulator/DojoFieldTransferParityTests.cs`, `UnitTest_MapSimulator/DojoFieldEnergyPresentationParityTests.cs`, `UnitTest_MapSimulator/DojoFieldMapDetectionParityTests.cs` (`CField_Dojang::Init`, `CField_Dojang::OnClock`, `CField_Dojang::Update`, `CField_Dojang::UpdateTimer`, `CField::OnPacket`, `CField::OnFieldSpecificData`) |
| Implemented | Cookie House event flow | Cookie House now binds `fieldType == CookieHouse` into `CookieHouseField`, mirrors the client-owned HUD geometry from `CField_CookieHouse::Init`, keeps the point layer cached in a dedicated `187x45` render target that is redrawn only when the context-owned point changes, and renders the WZ-backed `Map/Obj/etc.img/eventPointCount/backgrnd` plus the five-style digit root from `Map/Obj/etc.img/eventPointCount` without synthesizing sign canvases when the shipped digit-only map root lacks `plus` or `minus`. WZ re-checks against the current exported IMG filesystem still pin that root to a `187x45` background plus five digit containers and no sign children. IDA re-checks close the previous transport uncertainty: `CWvsContext::OnPacket` at `0x9e5830` dispatches packet `93` directly to `CWvsContext::OnSessionValue` at `0x9e41b0`, `OnSessionValue` reads `DecodeStr(key)` / `DecodeStr(value)`, compares the key to StringPool `0x11D9`, writes the parsed value into the same context point slot that `CField_CookieHouse::Update` at `0x54dc50` polls, and then `CField_CookieHouse::_UpdatePoint` at `0x54daa0` redraws the cached layer from `m_pCanvasBg` and `FindGrade_(m_nPoint)`. The simulator now pins StringPool `0x11D9` to the Cookie House session-value key, treats opcode `93` as the version-pinned default session-value point carrier, decodes opcode `93` Maple-string session payloads into the same score-owned update seam as loopback/raw context packets, keeps the recovered raw `opcode + 4-byte CWvsContext+0x4148` candidate inference only as a fallback/manual mapping path, suppresses opcode `93` from ambiguous raw-point inference, preserves invalid negative and over-three-digit rejection, and lets `/cookiepoint session start` plus `startauto` pass `listenPort 0` through to the existing bridge so the OS-selected proxy-port contract is exposed at the command layer. Focused source tests in `UnitTest_MapSimulator/CookieHouseParityTests.cs` now cover the built-in opcode `93` mapping, opcode `93` session-value decode, the pinned StringPool `0x11D9` key, invalid session payload rejection, and auto-selected listen-port startup in addition to the existing score/HUD decode coverage. | This row no longer depends on live-traffic inference for the client/server-owned Cookie House point owner. The simulator carries the recovered WZ asset family, cached HUD redraw ownership, grade table, digit/sign behavior, context-polled update path, StringPool-backed session-value key, and packet `93` session-value ingress in the existing Cookie House seams; raw 4-byte opcode inference remains available only as compatibility tooling for unknown exports or non-v95 traffic rather than a parity blocker. | `CookieHouseField.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `CharacterData.cs`, `MapleStoryStringPool.cs`, `CookieHousePointInboxManager.cs`, `CookieHouseOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/CookieHouseParityTests.cs` (`CWvsContext::OnPacket`, `CWvsContext::OnSessionValue`, `CField_CookieHouse::_UpdatePoint`, `CField_CookieHouse::Init`, `CField_CookieHouse::Update`, `CBitmapNumber::CBitmapNumber`, `CBitmapNumber::Draw`, ``anonymous namespace'::FindGrade_``) |
| Partial | Monster Carnival field flow | The simulator still uses the same client-owned Monster Carnival seam and evidence anchors already captured for this row: map detection and WZ definition loading remain in the coordinator path, mob and guardian request handling still uses WZ-backed getCP, revive-chain, mobGenPos, guardianGenPos, reactorRed, and reactorBlue metadata, request-failure and result routing still mirrors the pinned StringPool ids, and guardian placements still reconcile into live ReactorPool entries with WZ-backed required-hit metadata and hit-progress callback wiring. Official-session transport ownership also remains in the existing bridge path: `/mcarnival session` keeps discover, attach, attachproxy, start, startauto, map, unmap, clearmap, recent, and stop; outbound opcode 262 still injects immediately on initialized proxied sessions, queues while reconnect is armed pre-init, and flushes after handshake init without falling back to simulator-local CP or summon mutation while live transport is active. This pass keeps ownership in the same request/result and reactor-reconcile seams while closing another late-session drift edge: pending-local request tokens still consume exact tab+index first, malformed local-owner opcode-349 success fallback now consumes the oldest pending local token that still resolves to WZ-backed request data instead of losing recovery to a stale/non-resolvable token, non-resolvable pending tokens remain available for opcode-350 reason-only failure ownership, and unmatched non-local result tokens remain queued; live guardian reconciliation still consumes pending guardian placements by exact slot+team first (then slot-only, then team, then oldest), preserves every weaker unconsumed pending guardian token after a stronger match is selected, and still breaks live slot/team scoring ties with pending-placement affinity so ambiguous dual-team reactor candidates prefer the pending slot/team owner signal instead of first-enumerated team bias. Focused source coverage in `UnitTest_MapSimulator/MonsterCarnivalOfficialSessionBridgeParityTests.cs` now includes pending guardian preservation after exact slot+team selection, the local-owner unmatched-result token fallback, malformed local-owner opcode-349 success recovery through the oldest pending WZ-backed local entry when the decoded tab/index cannot resolve even if an older stale token is queued first, non-local malformed-result pending-token preservation, slot-first pending-guardian reconcile ownership, ambiguous-reactor tie ownership using pending slot+team evidence, and the recovered client opcode-owner boundary for 346-353, in addition to out-of-order result matching and unmatched-result token preservation. IDA re-check confirms `CField_MonsterCarnival::OnPacket` dispatches only 346-353 before falling back to `CField::OnPacket`, so the remaining parity gap stays narrowed to deeper live-session recovery that this seam still cannot provide: passive established-session attach cannot decrypt inbound 346-353 or inject outbound opcode 262 after handshake, and fully byte-identical late-session guardian choreography is still limited when neither packet owner/team evidence nor pending-placement ownership evidence is recoverable from live reactor state. | Monster Carnival remains a dedicated client-owned field mode where parity depends on both transport ownership and request-result bookkeeping. With this pass, malformed local-owner success results with unresolved decoded tab/index recover the oldest WZ-backed pending local entry before CP and summon mutation while preserving stale non-resolvable tokens for reason-only failure ownership, local cooldown and CP-reset attribution no longer drifts on unmatched local-owner success/failure ownership, live guardian reconcile no longer defaults to queue-order consumption when a slot-matching pending token exists, stronger pending guardian matches no longer drop weaker still-pending placement evidence, ambiguous live reactor slot/team ties now prefer pending slot/team ownership evidence before falling back to legacy ordering, and the client owner table is confirmed as 346-353 only. Remaining risk is now concentrated in post-handshake decrypt/inject and the residual ambiguous guardian choreography cases that still lack recoverable owner/team evidence. | `MonsterCarnivalField.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.cs`, `MonsterCarnivalPacketInboxManager.cs`, `MonsterCarnivalOfficialSessionBridgeManager.cs`, `ReactorPool.cs`, `MapleStoryStringPool.cs`, `UnitTest_MapSimulator/MonsterCarnivalOfficialSessionBridgeParityTests.cs`, `UnitTest_MapSimulator/TournamentAndMonsterCarnivalVariantParityTests.cs` (`CField_MonsterCarnival::CreateUIWindow`, `CField_MonsterCarnival::OnEnter`, `CField_MonsterCarnival::OnRequestResult`, `CField_MonsterCarnival::OnShowGameResult`, `CField_MonsterCarnival::OnProcessForDeath`, `CField_MonsterCarnival::OnShowMemberOutMsg`, `CField_MonsterCarnival::OnPacket`) |
| Implemented | Party Raid field flow | The simulator now binds `FIELDTYPE_PARTYRAID`, `FIELDTYPE_PARTYRAID_BOSS`, and `FIELDTYPE_PARTYRAID_RESULT` into a dedicated `PartyRaidField` runtime, renders the field, boss, and result HUDs from the client WZ asset roots (`UI/UIWindow.img/PartyRace/*`, `UI/UIWindow.img/DualMobGauge/*`, `Map/Effect.img/praid/*`), fixes the field and boss point-vs-stage board placement to match the `PartyRace/Stage/backgrd` labels, mirrors the client's team-colored stage-state art, boss red-vs-blue gauge fill, map-owned clear or timeout result effect, and result summary overlay positions, infers win-vs-fail result mode from the result maps' `info/onUserEnter` scripts (`PRaid_WinEnter`, `PRaid_FailEnter`), constrains the result panel to the map's `LBSide/LBTop/LBBottom` safe area, carries a type-2 timer lifecycle through the runtime, and swaps the old plain text countdown for the client `Map/Obj/etc.img/partyRace` timerboard with bitmap digits and separator. The ingress seam now matches the recovered client contract instead of stopping at manual `field|party|session|clock` text updates: `PartyRaidField.TryApplyRawPacket` decodes the client key/value packet layout that `CWvsContext::OnSessionValue` at `0x9e41b0`, `CWvsContext::OnPartyValue` at `0x9e4720`, and the Party Raid field-owner path expect (`DecodeStr(key)` + `DecodeStr(value)`), applies raw Party Raid packet families `93`, `94`, and `95` into the existing session/party/field handlers, and accepts packet `149` field-specific Maple-string-pair payloads into the existing owner-hinted `PRaid_Team`, `Red_Stage` / `Blue_Stage`, `PRaid_ElapssedTimeAtBoss`, and result-value seams. The Party Raid loopback inbox parses and dispatches both direct wrapper payload lines (`packet 93|94|95|149|163`) and opcode-framed `packetclientraw` traffic, while `PartyRaidOfficialSessionBridgeManager` proxies a live Maple session, forwards the server stream to the client, decrypts inbound Party Raid traffic in-process, and preserves the explicit current-wrapper relay at `CField::OnPacket` opcode `163` in addition to the recovered `93` / `94` / `95` / `149` payload family. WZ evidence remains anchored to the Party Raid map scripts (`923020100/info/onUserEnter=PRaid_W_Enter`, `923020200/info/link=923020100`, `923020010/info/onUserEnter=PRaid_WinEnter`, `923020020/info/onUserEnter=PRaid_FailEnter`), and IDA re-checks close the charge-owner uncertainty instead of leaving it inferred: `CField_HuntingAdballoon::OnFieldSetVariable` at `0x552050` explicitly consumes string-pool ids `0x174D` / `0x174E` (`red_Charge` / `blue_Charge`) for the two charge strips, while `CField_PartyRaidBoss::OnFieldSetVariable` keeps ownership of `0x1ACE` / `0x1AA3` (`redTeamDamage` / `blueTeamDamage`) and the `9700037` max-HP gauge math. Focused regression coverage is now on disk in `UnitTest_MapSimulator/PartyRaidOfficialSessionBridgeParityTests.cs` for wrapper-relay decode and Party Raid live-bridge/raw-inbox opcode acceptance. | Party Raid now covers the field battery surface, field board, client-horizontal mine-vs-other stage-state movement, boss gauge, result overlays, client timerboard, script-backed outcome selection, map-safe result placement, the recovered Party Raid literals for team/stage/boss time/result values, a client-shaped cached boss gauge layer owner instead of frame-by-frame ad hoc composition, key-based plus raw packet-shaped loopback ingress, the explicit current-wrapper relay at opcode `163`, and the live official-session bridge for the recovered Party Raid packet family. This row is complete within the currently recovered client contract. | `SpecialFieldRuntimeCoordinator.cs`, `PartyRaidField.cs`, `MapSimulator.PacketOwnedFieldStateParity.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `PartyRaidPacketInboxManager.cs`, `PartyRaidOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/PartyRaidOfficialSessionBridgeParityTests.cs` (`CField::OnPacket`, `CWvsContext::OnSessionValue`, `CWvsContext::OnPartyValue`, `CField::OnFieldSpecificData`, `CField_PartyRaid::_UpdatePoint`, `CField_PartyRaid::Update`, `CField_PartyRaid::Init`, `CField_PartyRaidBoss::Init`, `CField_PartyRaidBoss::OnFieldSetVariable`, `CField_PartyRaidBoss::OnPartyValue`, `CField_PartyRaidResult::Init`, `CField_PartyRaidResult::OnSessionValue`, `CField_HuntingAdballoon::OnFieldSetVariable`) |
| Implemented | SpaceGAGA timerboard flow | The simulator now binds `FIELDTYPE_SPACEGAGA` maps into a dedicated `SpaceGagaField`, mirrors the client's clock-type-2 timerboard ownership, keeps the board at the verified center-top window origin, removes the invented title banner, pins the timerboard source to `Map/Obj/etc.img/space` so the board comes from `space/backgrnd` and the bitmap timer digits come from `space/fontTime`, and no longer falls back to broad `UIWindow(.2).img` shape scans before dropping to the older procedural board or SpriteFont path. In this pass, WZ re-checks against the exported IMG filesystem kept that `space` subtree pinned to the exact `backgrnd` canvas plus `fontTime` digit and `comma` canvases, IDA re-checks kept the timer-font seam pinned to `CTimerboard_SpaceGAGA::GetFontTime` at `0x5629f0`, and direct `MapleStory.exe` table extraction closed the last StringPool gap: `ms_aString[0x140D]` was recovered as seed `0x44` plus encoded bytes `44 C3 06 CD 1C 7C 6C 18 C2 08 93 3B 4A 0F 27 50 31 FD 17 DC 50 56 21 10 8C 0E 8C 3F 16 08 2E`, which decodes to `Map/Obj/etc.img/space/backgrnd` through `CTimerboard_SpaceGAGA::OnCreate`, and `ms_aString[0x140E]` was recovered as seed `0x45` plus encoded bytes `45 51 AE 0A 49 29 7E 8F F5 BE BA D3 E6 A5 F9 09 12 6F BF 1B 05 03 33 83 B5 B5 BA E4 A1 A1 F1`, which decodes to `Map/Obj/etc.img/space/fontTime` through `GetFontTime`. The `/spacegaga` status seam now reports those byte-accurate client entries instead of inferred-only mappings, and focused `SpaceGagaField` tests on disk cover type-2 clock ownership, ignored non-type-2 clocks, reset behavior, and the recovered StringPool evidence through that runtime seam | This is another example of client-owned special field logic that would otherwise be lost under generic map handling, and the simulator now carries the SpaceGAGA timerboard end-to-end with fixed WZ-backed art, verified ownership for both client StringPool ids, and byte-accurate recovered `ms_aString` evidence rather than inferred-only path mapping | `SpecialEffectFields.cs`, `UnitTest_MapSimulator/SpaceGagaFieldTests.cs`, `SpaceGagaField` (`CField_SpaceGAGA::OnClock`, `CTimerboard_SpaceGAGA::Draw`, `CTimerboard_SpaceGAGA::OnCreate`, `CTimerboard_SpaceGAGA::GetFontTime`, `StringPool::GetString`) |

### 2. Additional packet-owned microgame dialog discovered by follow-up IDA scan

The broader packet-owner pass in backlog 7 already surfaced `CRPSGameDlg::OnPacket`, but it is still missing from the dedicated minigame plan even though it fits this area more closely than the generic field-utility bucket.
A targeted decompile now makes that owner more concrete: `CRPSGameDlg::OnCreate` is the dialog-create seam, and `CRPSGameDlg::OnPacket` is not a single opaque callback. Packet subtype `8` first enforces the unique-modeless owner gate, decodes a `uint32` value, opens a `CUtilDlgEx` notice using `StringPool` id `0xE83`, and only then constructs the dialog owner; subtypes `6`, `7`, `9`, `10`, `11`, `12`, and `14` require an already-live `CRPSGameDlg` unique-modeless owner and forward into `ProcessPacket`; and subtype `13` tears the owner down through `CWnd::Destroy`. The same `ProcessPacket` decompile also shows more exact gameplay ownership than the backlog was tracking: `9` and `12` start a fresh round by clearing NPC choice, enabling the three RPS buttons, setting the switch cadence to `120`, and arming a `30000` ms time limit; `11` decodes the NPC pick plus straight-win count and marks the compensation flag when the decoded count goes negative while the current streak is still zero; `10` forces immediate result presentation by clearing the switch cadence, setting the streak to `-1`, and calling `ShowResult`; and `6`/`7`/`14` reset the round state, re-enable the main button, and route the completion notice through `StringPool` ids `3724` or `3723`.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Rock-Paper-Scissors dialog parity | `HaCreator/MapSimulator` still carries both sides of the recovered `CRPSGameDlg` ownership instead of only the server-owned packet half, and this row stays in the live-session verification seam rather than changing owner logic again. The local owner/runtime shape remains aligned with IDA (`CRPSGameDlg::OnPacket`/`ProcessPacket`/`Update`/button handlers): subtype `8` enforces the unique-modeless gate, decodes the `uint32` entry payload, queues `StringPool` `0xE83`, and opens the dialog; subtypes `6`,`7`,`9`,`10`,`11`,`12`,`14` require active `CRPSGameDlg`; subtype `13` destroys it; `9`/`12` re-arm round state (`120` cadence, `30000` limit); `11` decodes NPC pick + streak and compensation-on-negative-from-zero; `10` forces result presentation; `6`/`7`/`14` reset and route notices through `3724`/`3723`. This pass keeps ownership unchanged and tightens the existing live verification seam again: `RockPaperScissorsOfficialSessionBridgeManager` still exposes explicit `LiveOwnershipVerificationState` (`Idle`, `ReconnectPending`, waiting-for-both, waiting-for-outbound-`160`, waiting-for-inbound-`371`, `Complete`) for real proxied opcode `160` outbound plus opcode `371` inbound capture, `DescribeStatus()` now reports outbound/inbound trace history separately from live-proxied evidence counts, and only `official-session:*` traces can satisfy the live evidence flags. Simulator-authored injection/queued opcode `160` traces remain visible in recent history for debugging, but they no longer read as captured live client proof or advance verification state; clearing outbound or inbound capture history still clears the matching verification evidence flag so status cannot claim completed live validation after evidence is removed. Focused tests in `UnitTest_MapSimulator/RockPaperScissorsParityTests.cs` cover the verification-state resolver/status text, live inbound/outbound trace capture, local-injection trace exclusion from live evidence, live-vs-trace status wording, history-clear evidence narrowing, and owner packet payload shapes. WZ anchors were re-checked and remain unchanged: `UI/UIWindow2.img/RpsGame` still carries `backgrnd` `310x358`, result/choice/flash-choice/button families, and the existing IDA anchor remains `CRPSGameDlg::OnCreate` (`0x6d7480`) with main/exit/choice ids and placement (`3000` at `108,202`, `3003` at `242,335`, `2000/2001/2002` at `19/60/101,104`). Remaining gap is now only live validation evidence: a full proxied reconnect run against a live Maple session still needs to produce and confirm real `CRPSGameDlg` opcode `160` outbound plus opcode `371` inbound end-to-end through the initialized bridge. | The seam now has owner-aligned runtime behavior plus explicit, evidence-synchronized verification-state telemetry for live bridge capture, and local simulator injection can no longer be mistaken for real proxied client evidence. Remaining risk is real-session evidence collection, not local packet-shape reconstruction, stale status reporting, or trace-history/evidence ambiguity. | packet-owned microgame dialog layer (`HaCreator/MapSimulator/Fields/RockPaperScissorsField.cs`, `HaCreator/MapSimulator/MapSimulator.RockPaperScissorsParity.cs`, `HaCreator/MapSimulator/RockPaperScissorsSessionCommandParsing.cs`, `HaCreator/MapSimulator/Managers/RockPaperScissorsPacketInboxManager.cs`, `HaCreator/MapSimulator/Managers/RockPaperScissorsClientPacketTransportManager.cs`, `HaCreator/MapSimulator/Managers/RockPaperScissorsOfficialSessionBridgeManager.cs`, `HaCreator/MapSimulator/MapSimulator.Content.cs`, `HaCreator/MapSimulator/MapSimulator.UpdateLoop.cs`, `UnitTest_MapSimulator/RockPaperScissorsParityTests.cs`, `CRPSGameDlg::OnPacket`, `CRPSGameDlg::ProcessPacket`, `CRPSGameDlg::OnCreate`, `CRPSGameDlg::ShowResult`, `CRPSGameDlg::Update`, `CRPSGameDlg::OnBtStart`, `CRPSGameDlg::OnBtContinue`, `CRPSGameDlg::OnBtRetry`, `CRPSGameDlg::OnBtExit`, `CRPSGameDlg::SendSelection`, `CUtilDlgEx`) |

### 3. Additional client-owned field wrappers discovered by IDA function scan

The live IDA `list_functions` scan surfaced more field-owner classes with no hits anywhere else in the current backlog set.
These started as scan-only seams, but a follow-up pass against the current simulator code shows that some of them are now represented locally and should no longer stay marked as missing just because the older backlog text was never refreshed.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Transit and voyage field wrappers | This row stays in the same transport-owned seam and closes another runtime drift edge without inventing a packet owner. `TransportationFieldDefinitionLoader` still resolves client-owned `shipObj` in strict contract order from copied `MapInfo.additionalNonInfoProps` first, then current root map-image `shipObj` (`MapInfo.Image`), then linked map-contract `shipObj` through `info/link` when present, and runtime texture loading still honors that resolved map-owned `shipObj` path before falling back to generic `contimove` or broad object-set scans. WZ re-checks in this pass stayed aligned with transport ownership: voyage maps still carry map-owned `shipObj` payloads such as `Map/Map1/101000300/shipObj` (`x=1545`, `x0=2100`, `y=-195`, `tMove=15`, `f=0`, `shipKind=0`, `shipObj=Map/Obj/vehicle.img/ship/ossyria/99`), `Map/Obj/vehicle.img/ship/ossyria/99` still resolves to a `545x746` canvas with origin `(178,313)`, and path normalization still accepts both `Map/Obj/vehicle.img/...` and `Map/Obj/vehicle/...` variants. IDA re-checks also remain aligned: `CField_ContiMove::OnStartShipMoveField` (`0x54c970`) still decodes one byte and routes value `2` to `LeaveShipMove`, `CField_ContiMove::OnEndShipMoveField` (`0x54c9a0`) still decodes one byte and routes value `6` to `EnterShipMove`, `CField_ContiMove::OnMoveField` (`0x54d640`) still routes `4/5` to Balrog ship appear/disappear, `CField_ContiMove::OnContiState` (`0x54d5a0`) still decodes two bytes and routes `0/1/6` to `EnterShipMove`, `2/5` to `LeaveShipMove`, and `3/4` to Balrog appear only for `shipKind == 1 && value == 1`, while `CField_Balrog::GetFieldType` (`0x549810`) still returns `18`. This pass tightens the recovered `OnContiState` branch inside the existing `TransportationField` seam: states `3/4` now record transport packet ownership but leave regular voyage ships unchanged instead of incorrectly reusing the departure path, and Balrog-type maps still only run `AppearShip` when the second decoded byte is `1`. The earlier voyage Balrog attack ownership remains in the same owner gate: manual `/transport voyagebalrog start`, WZ-route auto scheduling for `/ship/ossyria/99`, status owner exposure, reset/reinitialization owner clearing, and the legacy debug hotkey still go through the transport owner gates, and arrival moves still clear the departure-owned voyage attack window so manual and WZ-route auto voyage Balrog attacks cannot fire during `EnterShipMove` arrival movement after a previous departure. Focused coverage in `UnitTest_MapSimulator/PacketInboxManagerTests.cs` now checks command-owned and WZ-route-auto voyage Balrog ownership/status, route-path normalization for `.img` and non-`.img` variants, owner clearing on transport reinitialization, arrival-window rejection for both command-owned and WZ-route-auto voyage Balrog attacks, and the corrected `OnContiState(3/4, value)` regular-ship no-op versus Balrog-only appear gate alongside the existing transport inbox coverage. Remaining gap is still explicit and narrower: transport cannot decrypt/inject in place on an already-established Maple socket pair without a localhost proxy reconnect hop, no additional dedicated outbound voyage/Balrog request family is recovered beyond field-init plus captured raw replay/send/queue, and voyage Balrog attack choreography is owner-attributed, WZ-route anchored, departure-window gated, and now aligned with recovered `OnContiState` branch gates, but still simulator-scheduled/WZ-route-scheduled rather than recovered as a packet-owned server event owner. | Transit and Balrog wrappers are still client-owned map contracts first; keeping `shipObj` resolution in map-owned WZ data across copied, root-image, and linked-map alias paths prevents fallback drift to editor-authored defaults while preserving the recovered packet/state split and wrapper branch gates. The local voyage Balrog path now has explicit owner attribution, no debug-key bypass, exact route texture ownership, a departure-only auto/command gate that prevents arrival-move drift after `EnterShipMove`, and a corrected `OnContiState(3/4)` branch that no longer departs regular ships, so remaining work is concentrated on post-handshake live-session ownership and unrecovered outbound voyage/Balrog packet families, not loader ownership, route-path normalization, local command/auto owner visibility, arrival-window scheduling, or recovered ContiState branch routing. | transit or voyage field runtime (`MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.ClientOwnedFieldWrappers.cs`, `MapSimulator.UpdateLoop.cs`, `TransportationField.cs`, `TransportationFieldDefinitionLoader.cs`, `TransportationPacketInboxManager.cs`, `TransportationOfficialSessionBridgeManager.cs`, `TransportationFieldInitRequestCodec.cs`, `UnitTest_MapSimulator/PacketInboxManagerTests.cs`, `CField_ContiMove`, `CField_Balrog`) |
| Partial | Tutorial, camera-mask, and presentation wrappers | The simulator still carries the tutorial, limited-view, and wedding-photo wrapper seams surfaced by the IDA scan, with this pass tightening the limited-view draw seam again inside the existing owner. WZ evidence was re-checked against `Effect/MapEff.img/Viewrange/0` in `v95_2` (`316x316`, origin `158,179`), and IDA evidence was re-checked for `CField_Tutorial::DecodeFieldSpecificData` (`0x563830`, forced `1002562` cap plus `1052081` longcoat while preserving the active weapon slot), `CField_LimitedView::CField_LimitedView` (`0x53eaf0`, `m_bShareView = 0`), `CField_LimitedView::DrawViewrange` (`0x554ca0`, dark-layer canvas acquisition, previous-mask restore, clear, local user/graphics-center resolution, `Viewrange/0` dimension reads, local `CA_REMOVEALPHA` copy, and share-view-gated remote vector/copy loop), and `CField_WeddingPhoto::GetFieldType` (`0x53eee0`, returns `61`). Generic tutorial maps still mirror the recovered forced appearance pair while preserving weapon, Aran tutorial maps still keep their WZ-backed `user/*/look` path under `CField_AranTutorial::GetFieldType = 22`, and tutorial/wedding-photo contract resolution still follows authored `info/link` aliases before reading wrapper metadata. Limited-view maps still load `Effect/MapEff.img/Viewrange/0`, build the `1024x768` dark layer at `(-512,-468)`, keep local-only masking unless wrapper-owned `shareview`/`m_bShareView` values enable remote copies, reject linked-map-only share-view metadata promotion, accept live packet/session field-specific share-view toggles, reset default share-view on same-id map-info owner rebinding, and reset mask history on owner rebinding. The draw operation plan now also records the recovered COM-level invocation markers around the existing runtime drawing path: dark-layer canvas acquisition, local user vector resolution, graphics-center resolution, viewrange dimension queries, and share-view-gated remote user vector resolution before remote `CA_REMOVEALPHA` copies, while preserving previous-mask restore, clear-then-reappend `m_lpPrev` ordering, and explicit no-target dark-layer fallback continuity. Wedding-photo ownership remains in the scene owner seam: `WeddingField` binds `fieldType = 61` / `pledgeEnter`, displaces stale ceremony state on photo-scene binding, and drives text/card/celebration/bless transitions from `OnWeddingProgress` / `OnWeddingCeremonyEnd`; non-recovered photo-scene packet families remain trail/presentation-owned rather than claimed as recovered local choreography. Focused source coverage in `UnitTest_MapSimulator/ClientOwnedFieldWrapperParityTests.cs` now includes the limited-view COM invocation markers and no-target fallback ordering in addition to the existing init/history/rebind coverage. The remaining gap is narrower and runtime-specific: the simulator still does not reproduce unrecovered photo-specific packet/dialog/layer choreography beyond the shared ceremony path, and `CField_LimitedView` may still have lower-level native COM call details not represented beyond the now-modeled acquisition/vector/center/dimension/copy ordering. | This row now tracks explicit owner behavior rather than wrapper classification: Aran tutorial maps keep their distinct WZ-authored appearance contract, linked map aliases resolve through the same WZ roots the client reads for tutorial and wedding-photo wrappers, limited-view ownership covers recovered clear/reappend history ordering, no-target dark-layer fallback continuity, constructor-matched default share-view gating, runtime share-view toggles, same-id map-owner rebind default reset, owner-rebind history reset, and a more explicit COM-level DrawViewrange operation plan; wedding pledge/photo scene maps keep packet-driven layer transitions inside a dedicated `CField_WeddingPhoto` owner. It remains `Partial` because remaining work is deeper unrecovered photo-scene packet/dialog/layer choreography and any still-unmodeled low-level native limited-view COM details, not wrapper detection, linked-contract resolution, tutorial appearance selection, limited-view share-view ownership, map-owner rebind semantics, mask-history ordering, dark-layer fallback ownership, photo-scene BGM preservation, or scene-owner handoff plus step-driven layer-stage transitions. | tutorial or scripted field layer (`MapSimulator.ClientOwnedFieldWrappers.cs`, `MapSimulator.UpdateLoop.cs`, `LimitedViewField.cs`, `WeddingField.cs`, `SpecialEffectFields.cs`, `UnitTest_MapSimulator/ClientOwnedFieldWrapperParityTests.cs`, `CField_Tutorial::DecodeFieldSpecificData`, `CField_AranTutorial::GetFieldType`, `CField_LimitedView::CField_LimitedView`, `CField_LimitedView::Init`, `CField_LimitedView::DrawViewrange`, `CField_Wedding::OnPacket`, `CField_Wedding::OnWeddingProgress`, `CField_Wedding::OnWeddingCeremonyEnd`, `CField_NoDragon::Init`, `CField_WeddingPhoto::GetFieldType`) |
| Implemented | Dynamic foothold field-wrapper parity | The simulator now carries explicit `CField_DynamicFoothold` wrapper bookkeeping in the same client-owned wrapper seam as transport, tutorial, and limited-view fields instead of leaving dynamic maps implied by the broader moving-platform runtime. `MapSimulator.ClientOwnedFieldWrappers.cs` rebinds the owner on map load through `ConfigureDynamicFootholdFieldWrapper`, `IsDynamicFootholdWrapperMap` binds the native `fieldType = 29` contract, `Fields/DynamicFootholdField.cs` exposes the recovered client owner metadata (`CField::FieldFactory = 0x53F220`, `CField_DynamicFoothold::GetFieldType = 0x551020`, and the `Init` thunk at `0x551050`), follows `info/link` to a linked contract image when present, summarizes foothold-root and dynamic-object map data from WZ, and keeps packet-owned dynamic-object aliases separate from authored canonical names for later lookup. `DynamicFootholdSystem.ResetForClientOwnedWrapper()` also gives the wrapper its own rebinding seam by clearing simulator-owned platform state whenever the client-owned map owner changes. A fresh WZ re-check in this pass kept that contract anchored to current map data instead of simulator-only placeholders: `Map/Map0/003000100` still carries dynamic-tagged object nodes at `0/obj/0/dynamic`, `1/obj/0/dynamic`, `2/obj/0/dynamic`, `4/obj/0/dynamic`, and `5/obj/0/dynamic`, and the same sample object still resolves through `oS=acc9`, `l0=cannonTuto`, `l1=Ani`, `l2=4`, `piece=0`, `x=-701`, `y=302`. IDA re-checks also stayed aligned with the simulator seam: `CField_DynamicFoothold::GetFieldType` still returns `29`, and `CField_DynamicFoothold::Init` at `0x551050` still only forwards to `CField::Init`. Focused regression coverage is now on disk in `UnitTest_MapSimulator/DynamicFootholdFieldWrapperParityTests.cs` for explicit field-type activation, linked-contract resolution and missing-link fallback, wrapper-owned runtime reset, and dynamic-object alias bookkeeping. | This row no longer needs to stay open just to track whether dynamic-foothold maps are represented as their own client-owned field owner. The broader moving-platform behavior, passenger sync, and deeper packet choreography still belong to backlog 3, but the wrapper-family classification, field-type bind, init seam, rebinding, and WZ-backed contract bookkeeping for `CField_DynamicFoothold` are now explicit in the simulator. | dynamic-foothold field wrapper (`MapSimulator.ClientOwnedFieldWrappers.cs`, `HaCreator/MapSimulator/Fields/DynamicFootholdField.cs`, `HaCreator/MapSimulator/Fields/DynamicFoothold.cs`, `UnitTest_MapSimulator/DynamicFootholdFieldWrapperParityTests.cs`, `CField::FieldFactory`, `CField_DynamicFoothold::GetFieldType`, `CField_DynamicFoothold::Init`) |
| Implemented | Result-board and field-variable special wrappers | The missing IDA-owned wrapper surfaces are now covered in the simulator runtime: `CField_KillCount::OnPacket` packet `178` drives a dedicated kill-count overlay, `CField_HuntingAdballoon::OnFieldSetVariable` reuses the existing party-raid boss field-variable HUD with an explicit client-owned bind, `CField_MassacreResult::OnPacket` packet `174` now decodes the client rank/value payload into the existing Massacre result board, `CField_EscortResult::OnSessionValue` fail values arm a timed fail overlay, and `CField_ShowaBath::DecodeFieldSpecificData` now forces the gendered robe appearance recovered from the client and WZ (`1050100` / `1051098`). The field-state command surface also accepts the new packet ids and wrapper value injection for verification. | The result/status wrappers that contextualize these minigame endings are now represented as concrete simulator runtime owners instead of being left as untracked client-only behavior. | event result or field-variable layer (`CField_KillCount`, `CField_HuntingAdballoon`, `CField_MassacreResult`, `CField_EscortResult`, `CField_ShowaBath`) |
| Partial | Tournament and Monster Carnival variant wrappers | WZ re-checks in the current IMG export remain anchored to the same client-owned seams: `Map/Map/Map1/109070000` keeps `info/fieldType=3`; `String/Map.img/victoria/109070000/help0` still describes bracket lobby/rest/prize/five-minute exit; Carnival variants still resolve through `monsterCarnival/mapType` (`Map/Map/Map9/980031000` type `16` + `mapType=0`, `980032000` + `mapType=1`, `980033000` + `mapType=2`); delegated timer values still come from sibling Carnival maps (`980031100` keeps `timeDefault/timeExpand/timeMessage/timeFinish` = `610/120/6/12`); and UI ownership still splits across `UIWindow2.img/MonsterCarnival/{main,summonList,sub}` vs shared `UIWindow.img/MonsterCarnival/{backgrnd,backgrnd2,backgrnd3}`. IDA owner anchors are unchanged (`CField_MonsterCarnivalWaitingRoom::Init` and `CField_MonsterCarnivalS2_Game::Init` both read `monsterCarnival/mapType`; `CField_MonsterCarnivalRevive::OnPacket` keeps direct `346/353` with non-direct forwarding to `CField::OnPacket`; `CField_Tournament::OnPacket` routes `374..378` then falls back). The current implementation keeps the waiting-room side of those same seams narrow: `MonsterCarnivalField` gives the mapType `0` waiting-room wrapper explicit delegated lobby-route accounting, separated into raw vs structured counts plus an ordered route trail for the packets it forwards through `CField_MonsterCarnival` while keeping the wrapper-only surface. This pass deepens the Season 2 sub-dialog side without widening ownership: the `UIWindow2.img/MonsterCarnival/sub` owner now carries selected-entry route text into `BtOK` acknowledgement, tracks selected-entry OK route counts separately from generic OK clicks, preserves stale/no-selection cases in the same route trail, and exposes the selected route plus count in the sub-dialog status. The prior variant visibility remains in place: categorized variant transport counters/trails, Season 2 chat-route trail (`request-failure`, `death`, `member-out`, `result`) alongside the Season 2 timer-event trail (`enter-prime`, `request-arm`, `death-arm`, `result-close`, `timer-expire`), revive raw `346/353` direct ownership with structured `1/4` forwarding and ordered direct/forwarded trails, and Tournament success-only owned-wrapper packet accounting with structured-vs-raw counters plus ordered owner-route entries for `CField_Tournament::OnPacket` handled packets. Focused tests in `UnitTest_MapSimulator/TournamentAndMonsterCarnivalVariantParityTests.cs` now cover waiting-room delegated lobby-route counters/trail separation, Season 2 selected-entry sub-dialog OK route accounting and locked-selection no-selection OK accounting, Season 2 chat-trail lifecycle, revive structured-vs-raw ownership accounting plus ordered direct/forwarded revive-route trails, and Tournament owned-wrapper packet trail with structured-vs-raw ownership splits, ordered owner-route entries, and success-only malformed-packet accounting in addition to prior timer/revive/forwarded-raw assertions. Remaining gap stays narrowed to deeper native runtime fidelity: waiting-room is still explicit wrapper-owned transport and lobby-route accounting, not full native lobby/session network choreography; Season 2 now preserves timer, chat, selection, selected-entry OK, locked-selection, and phase visibility trails but still does not reproduce full native sub-dialog send-packet lifecycle or chat-widget internals; revive still stops short of full native round/session flow beyond direct/forwarded routing plus sequence/counter/ordered-route trails; and Tournament still does not reproduce the complete native bracket/session lifecycle beyond decoded owner packets, notice branches, UEW/prize routing, match-table ctor-shape handling, and owner-vs-forwarded transport visibility with structured-vs-raw ownership trails. | These wrappers now keep stronger per-owner session accounting without inventing new owners: Carnival variants preserve WZ/fieldType-mapType anchors, owned timer phases, waiting-room wrapper-owned delegated lobby-route counters/trails, Season 2 UIWindow2 ownership with explicit timer+chat transport trails plus selected-entry sub-dialog OK route accounting, and revive direct-vs-forwarded ownership aligned to raw `346/353`; Tournament preserves packet-family/session ownership with branch-aware notice summaries, explicit owned-wrapper packet trails, explicit structured-vs-raw owned counters with ordered owner-route trails, and explicit forwarded raw-route/count/trail visibility. Remaining work is concentrated in full native UI/dialog/transport choreography, not wrapper discovery or packet-owner routing visibility. | mode-variant field runtime (`MonsterCarnivalField.cs`, `TournamentField.cs`, `TournamentPacketInboxManager.cs`, `TournamentOfficialSessionBridgeManager.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/TournamentAndMonsterCarnivalVariantParityTests.cs`, `CField_MonsterCarnival::CreateUIWindow`, `CField_MonsterCarnival::OnEnter`, `CField_MonsterCarnival::OnRequestResult`, `CField_MonsterCarnival::OnProcessForDeath`, `CField_MonsterCarnivalRevive::OnPacket`, `CField_MonsterCarnivalRevive::OnShowGameResult`, `CField_MonsterCarnivalWaitingRoom::Init`, `CField_MonsterCarnivalS2_Game::Init`, `CField_MonsterCarnival::OnShowGameResult`, `CField_Tournament::OnPacket`, `CField_Tournament::OnTournament`, `CField_Tournament::OnTournamentMatchTable`, `CField_Tournament::OnTournamentSetPrize`, `CField_Tournament::OnTournamentUEW`, `CMatchTableDlg::CMatchTableDlg`) |
| Implemented | Tournament match-table dialog parity | `HaCreator/MapSimulator/Fields/TournamentField.cs` now carries a dedicated `TournamentMatchTableDialogState` owner behind Tournament packet `375`, so `CField_Tournament::OnTournamentMatchTable` is represented as its own dialog surface instead of being buried inside the generic field status panel. The simulator now mirrors the client-owned lifecycle more explicitly: the packet handoff opens a `CMatchTableDlg`-named dialog, the local draw path keeps the IDA-confirmed close/up/down button geometry and layer offsets from `CMatchTableDlg::OnCreate` (`0x784050`) and `CMatchTableDlg::Draw` (`0x797d20`), and the dialog is now interactive through mouse wheel, click-to-close, click-to-scroll, and `/tournament dialog ...` controls. | This row no longer needs to stay open just to track whether Tournament owns a second client dialog surface. Remaining differences are visual-fidelity follow-ups inside the existing dialog seam rather than missing ownership: the simulator still renders the bracket with local primitives and decoded text summaries instead of reproducing every client canvas/layer asset from the WZ/UI stack. | tournament bracket dialog layer (`CField_Tournament::OnTournamentMatchTable`, `CMatchTableDlg::OnCreate`, `CMatchTableDlg::Draw`) |
| Implemented | Shared current-field wrapper packet handoff parity | The simulator now keeps both packet-owned and non-packet current-wrapper ownership inside the same coordinator seam instead of splitting packet relay traffic from local wrapper shortcuts. `SpecialFieldRuntimeCoordinator.TryDispatchCurrentWrapperPacketRelay(...)` still normalizes direct recovered wrapper packet ids into the same opcode-`163` relay payload shape that the client re-check at `0x546d50` uses, decodes the wrapped payload, and hands it to the active owner with the owner message preserved in the dispatch result. The existing bridge and inbox coverage from the prior pass remains in place across `SpecialEffectFields` and `MinigameFields`: Wedding packet inbox messages, Ariant arena packet inbox messages, Coconut inbox and official-session bridge traffic, Snowball inbox and official-session bridge traffic, Monster Carnival inbox and official-session bridge traffic, Tournament inbox and official-session bridge traffic, Party Raid raw-packet inbox and official-session traffic, Dojo raw packet inbox handling, Massacre raw packet inbox handling, and Guild Boss transport/session traffic all still enter the active-wrapper seam before the concrete runtime consumes them. The field-state bypass from the prior pass also remains closed: direct field-state wrapper packets `174` (`CField_MassacreResult::OnPacket`) and `178` (`CField_KillCount::OnPacket`) route through the same coordinator relay path instead of a separate local shortcut. This pass closes the last ownership split that remained outside that seam: Showa Bath field-specific presentation handoff, Escort Result field-value ownership, Chaos Zakum session-value ownership, and Hunting Ad Balloon field-value ownership now all dispatch through `SpecialFieldRuntimeCoordinator` from `MapSimulator.PacketOwnedFieldStateParity.cs` and `MapSimulator.ClientOwnedFieldWrappers.cs` instead of being consumed by wrapper-specific local branches. Focused source coverage is now on disk in `UnitTest_MapSimulator/SpecialFieldRuntimeCoordinatorRelayTests.cs` for relay payload normalization, relay decode failure on malformed opcode-`163` payloads, the no-active-wrapper fallback message from the literal relay path, and owner-message dispatch for Escort Result, Chaos Zakum, Hunting Ad Balloon, and Showa Bath current-wrapper handoff. | This row now tracks the complete current-wrapper ownership seam in code rather than only a packet-helper abstraction. Packet traffic still reaches the active owner through the explicit opcode-`163` relay path, and the wrappers that do not expose recovered `OnPacket` handlers now enter the same coordinator-owned handoff through dedicated field-value, session-value, and field-specific presentation dispatch methods. That leaves no separate wrapper-consumption shortcut in `MapSimulator` for this row. | current special-field wrapper dispatch layer (`CField::OnPacket` at `0x546d50`, opcode-`163` current-wrapper relay, `HaCreator/MapSimulator/Fields/SpecialFieldRuntimeCoordinator.cs`, `HaCreator/MapSimulator/Effects/SpecialEffectFields.cs`, `HaCreator/MapSimulator/Fields/MinigameFields.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `HaCreator/MapSimulator/MapSimulator.ClientOwnedFieldWrappers.cs`, `HaCreator/MapSimulator/MapSimulator.PacketOwnedFieldStateParity.cs`, `UnitTest_MapSimulator/SpecialFieldRuntimeCoordinatorRelayTests.cs`, field-specific packet runtimes such as `WeddingField`, `TournamentField`, `MonsterCarnivalField`, `PartyRaidField`, `DojoField`, `AriantArenaField`, `SnowBallField`, `CoconutField`, `GuildBossField`, `MassacreField`) |

### 4. Additional Party Raid wrapper-family ownership confirmed by `CField::FieldFactory` re-check

The latest IDA re-check against `CField::FieldFactory` confirms that Party Raid is not a single special-field owner in the client.
The factory still instantiates three distinct wrapper classes, and targeted IDA lookup now pins the concrete `GetFieldType` owners too:

- `CField_PartyRaid::GetFieldType` at `0x55c440` for field type `25`
- `CField_PartyRaidBoss::GetFieldType` at `0x55c640` for field type `26`
- `CField_PartyRaidResult::GetFieldType` at `0x55c800` for field type `27`

That owner split was only implicit in the existing gameplay row, so it needs its own wrapper-family entry the same way Tournament, Carnival, transport, and dynamic-foothold do.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Explicit Party Raid wrapper-family parity | `PartyRaidField` now carries the native Party Raid wrapper-family split as a first-class contract instead of a generic owner bucket. The runtime reports `CField::FieldFactory` (`0x53F220`), the concrete `GetFieldType` owners, and the recovered `Init` entry point for each native client wrapper: `CField_PartyRaid` (`GetFieldType=0x55C440`, `fieldType=25`, `Init=0x55C3D0`), `CField_PartyRaidBoss` (`GetFieldType=0x55C640`, `fieldType=26`, `Init=0x55C5D0`), and `CField_PartyRaidResult` (`GetFieldType=0x55C800`, `fieldType=27`, `Init=0x55C7B0`). Wrapper-status output now names the active owner directly, and the visible contract text also carries the matching WZ evidence for each family branch: the live field owner resolves against `923020100` with `info/onUserEnter=PRaid_W_Enter`, the boss shell resolves through `923020200 -> 923020100`, and the result owner matches `923020010` / `923020020` where `info/fieldType=27` and `info/onUserEnter=PRaid_WinEnter` / `PRaid_FailEnter`. Packet-owned field-specific routing continues to resolve Party Raid keys through the concrete native owner label rather than the broader `PartyRaidField` bucket. | The simulator now exposes the same three-way Party Raid wrapper family that the client factory instantiates, with direct `GetFieldType` evidence plus WZ-backed contract data attached to the visible contract surface. That keeps future Party Raid work anchored to the actual `CField` family split instead of a blended runtime label and closes this wrapper-family ownership row. | Party Raid field-wrapper layer (`CField::FieldFactory`, `CField_PartyRaid::GetFieldType`, `CField_PartyRaid::Init`, `CField_PartyRaid::Update`, `CField_PartyRaidBoss::GetFieldType`, `CField_PartyRaidBoss::Init`, `CField_PartyRaidBoss::OnFieldSetVariable`, `CField_PartyRaidBoss::OnPartyValue`, `CField_PartyRaidResult::GetFieldType`, `CField_PartyRaidResult::Init`, `CField_PartyRaidResult::OnSessionValue`, `PartyRaidField.cs`) |

### 5. Cake Pie event timerboard and item-info owners discovered by follow-up IDA function scan

The follow-up broad function scan surfaced a special-event UI owner family that was not named in this backlog and is not captured by the top-level field-factory wrapper sweep.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Cake Pie event timerboard and item-info parity | The special-field Cake Pie runtime now owns the same presentation family surfaced by the follow-up IDA scan instead of leaving it implicit. `CakePieEventField` keeps the dedicated owner split in the existing seam: the timerboard path loads the Cake Pie-specific `Map/Obj/etc.img/5th_TownUI/{backgrd,bareffect,gage/0,gage/1}` and `Map/Obj/etc.img/5th_Timer/{backgrd,ready,start,cake,pie,fontTime}` assets, preserves the town-vs-typed timer branch with centered typed badges and timer-font digits, and the item-info path keeps the eight fixed `(fieldId,itemId)` rows owned by `CCakePieEvent` plus the dedicated `CUICakePieEventItemInfo` close button contract at `BtClose(1000)` / `(212,14)`. The remaining simulator gap in this row was that the owner button path existed in `CakePieEventField` but was never wired into the main mouse-dismiss flow, so the item-info dialog could block scripted-direction input without honoring the client-owned close interaction. `MapSimulator.UpdateLoop` now routes released left-clicks through `CakePieEventField.HandleMouseClick`, which closes the owner-managed dialog and emits the same owner-specific close message instead of leaving dismissal command-only. Focused source coverage now exists in `UnitTest_MapSimulator/CakePieParityTests.cs` for both the close-button hit path and the non-hit path that must leave the dialog open. | This row no longer depends on inferring the Cake Pie UI from generic clocks or utility windows. The simulator now exposes the dedicated owner family in the same special-field seam that owns the runtime, keeps the timerboard and item-info assets scoped to Cake Pie, and honors the item-info owner button-driven close lifecycle inside the normal input path rather than leaving it as a dead helper. | `CakePieEventField`, `MapSimulator.UpdateLoop.cs`, `UnitTest_MapSimulator/CakePieParityTests.cs`, Cake Pie event UI layer (`CCakePieEvent::SetEventItemInfo`, `CCakePieEvent::OpenUI_ItemInfo`, `CCakePieEvent::CloseUI_ItemInfo`, `CUICakePieEventItemInfo::OnCreate`, `CUICakePieEventItemInfo::Draw`, `CUICakePieEventItemInfo::Update`, `CUICakePieEventItemInfo::OnButtonClicked`, `CTimerboard_CakePieEvent::OnCreate`, `CTimerboard_CakePieEvent::Draw`, `CTimerboard_CakePieEvent::GetFontTime`, `CClock::OnCreate`) |

## Priority Order

If the goal is visible parity first, the next work in this area should be sequenced like this:

1. Existing-special-field refinement pass:
   Tighten Wedding, Coconut, Guild Boss, Massacre, SnowBall, and Ariant behavior against the client owners that already have simulator counterparts.
2. Missing microgame dialog pass:
   Add the still-untracked `CRPSGameDlg` Rock-Paper-Scissors owner and the Cake Pie event timerboard/item-info owner before more packet-owned minigame or event UI work gets buried in the generic field-utility bucket.
3. Shared field-wrapper refinement pass:
   Tighten the existing opcode-`163` current-wrapper dispatch seam before more special-field packet work stays split across per-mode inboxes or bypasses wrappers that still only expose field-value/result owners.
4. Variant and packet/HUD refinement pass:
   Continue deeper Monster Carnival, Party Raid, Tournament, SpaceGAGA, and MiniRoom packet/UI parity now that those runtime shells already exist.
5. Wrapper and transport pass:
   Triage the transit, tutorial/presentation, and Monster Carnival variant owners while closing the remaining official-session ownership gaps that still sit behind loopback inboxes or proxy seams.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
