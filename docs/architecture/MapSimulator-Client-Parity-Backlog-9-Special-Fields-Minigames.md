# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Effects/SpecialEffectFields.cs`
- `HaCreator/MapSimulator/Fields/MinigameFields.cs`

### Client references checked

- `CField_GuildBoss::CField_GuildBoss` at `0x53e980`
- `CField_Coconut::OnClock` at `0x54a100`
- `CField_Coconut::OnCoconutHit` at `0x54a470`
- `CField_Coconut::BasicActionAttack` at `0x54a5e0`
- `CField_Coconut::DrawBoard` at `0x54a750`
- `CField_Coconut::OnCoconutScore` at `0x54bf70`
- `CField_AriantArena::OnUserScore` at `0x5492b0`
- `CField_AriantArena::OnShowResult` at `0x547630`
- `CField_Battlefield::OnClock` at `0x549ad0`
- `CField_Battlefield::OnScoreUpdate` at `0x5499a0`
- `CField_Battlefield::OnTeamChanged` at `0x5499e0`
- `CField_Battlefield::SetUserTeam` at `0x549870`
- `CField_Dojang::OnClock` at `0x550940`
- `CField_Dojang::Update` at `0x54ef10`
- `CField_CookieHouse::_UpdatePoint` at `0x54daa0`
- `CField_CookieHouse::Init` at `0x54e250`
- `CField_Massacre::OnClock` at `0x556af0`
- `CField_Massacre::UpdateKeyAnimation` at `0x556bf0`
- `CTimerboard_Massacre::Draw` at `0x557100`
- `CField_Massacre::Init` at `0x5579d0`
- `CMemoryGameDlg::OnButtonClicked` at `0x628df0`
- `CMemoryGameDlg::OnCreate` at `0x62f3c0`
- `CMemoryGameDlg::Draw` at `0x631cb0`
- `CField_MonsterCarnival::CreateUIWindow` at `0x55a510`
- `CField_MonsterCarnival::OnEnter` at `0x55a6c0`
- `CField_MonsterCarnival::OnRequestResult` at `0x55a890`
- `CField_MonsterCarnival::OnShowGameResult` at `0x55af80`
- `CField_MonsterCarnivalRevive::OnShowGameResult` at `0x55b110`
- `CField_PartyRaid::_UpdatePoint` at `0x55c120`
- `CField_PartyRaid::Init` at `0x55f110`
- `CField_PartyRaidBoss::OnFieldSetVariable` at `0x55ca40`
- `CField_PartyRaidBoss::OnPartyValue` at `0x55d070`
- `CField_PartyRaidResult::OnSessionValue` at `0x55e5c0`
- `CField_SnowBall::OnSnowBallTouch` at `0x560510`
- `CField_SnowBall::OnSnowBallState` at `0x560ab0`
- `CField_SnowBall::BasicActionAttack` at `0x5617b0`
- `CField_SnowBall::OnSnowBallHit` at `0x5619d0`
- `CField_SnowBall::OnSnowBallMsg` at `0x562040`
- `CField_SpaceGAGA::OnClock` at `0x5625d0`
- `CTimerboard_SpaceGAGA::Draw` at `0x5626c0`
- `CField_Wedding::SetBlessEffect` at `0x563b60`
- `CField_Wedding::OnWeddingProgress` at `0x5640f0`
- `CField_Witchtower::OnScoreUpdate` at `0x564ad0`
- `CScoreboard_Witchtower::Draw` at `0x564bd0`
- `CField_ContiMove::OnStartShipMoveField` at `0x54c970`
- `CField_ContiMove::OnContiState` at `0x54d5a0`
- `CField_KillCount::OnPacket` at `0x554050`
- `CField_LimitedView::Init` at `0x5554a0`
- `CField_HuntingAdballoon::OnFieldSetVariable` at `0x552050`
- `CField_MassacreResult::OnPacket` at `0x55a1d0`
- `CField_EscortResult::OnSessionValue` at `0x551160`
- `CField_Tournament::OnPacket` at `0x563780`
- `CRPSGameDlg::OnCreate` at `0x6d7480`
- `CRPSGameDlg::OnPacket` at `0x6d9e00`
- `CField_ShowaBath::DecodeFieldSpecificData` at `0x55ff70`
- `CField_Tutorial::DecodeFieldSpecificData` at `0x563830`
- `CField_MonsterCarnivalWaitingRoom::Init` at `0x55bda0`
- `CField_MonsterCarnivalS2_Game::Init` at `0x55bf10`
- `CField_NoDragon::Init` at `0x55c0e0`
- `CField_WeddingPhoto::GetFieldType` at `0x53eee0`
- `CField_AranTutorial::GetFieldType` at `0x53f1c0`
- `CField_Balrog::GetFieldType` at `0x549810`
- `CCakePieEvent::SetEventItemInfo` at `0x47c8e0`
- `CCakePieEvent::OpenUI_ItemInfo` at `0x47c830`
- `CCakePieEvent::CloseUI_ItemInfo` at `0x479040`
- `CUICakePieEventItemInfo::OnCreate` at `0x47a3c0`
- `CUICakePieEventItemInfo::Draw` at `0x47a4e0`
- `CUICakePieEventItemInfo::Update` at `0x4791c0`
- `CUICakePieEventItemInfo::OnButtonClicked` at `0x4791a0`
- `CTimerboard_CakePieEvent::OnCreate` at `0x47abc0`
- `CTimerboard_CakePieEvent::Draw` at `0x47bab0`
- `CTimerboard_CakePieEvent::GetFontTime` at `0x47c670`

## Client Function Index By Backlog Area

The list below maps this backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Special field and minigame parity

- `CField_GuildBoss::CField_GuildBoss` at `0x53e980`
- `CField_Coconut::OnClock` at `0x54a100`
- `CField_Coconut::OnCoconutHit` at `0x54a470`
- `CField_Coconut::BasicActionAttack` at `0x54a5e0`
- `CField_Coconut::DrawBoard` at `0x54a750`
- `CField_AriantArena::OnUserScore` at `0x5492b0`
- `CField_AriantArena::OnShowResult` at `0x547630`
- `CField_Battlefield::OnClock` at `0x549ad0`
- `CField_Battlefield::OnScoreUpdate` at `0x5499a0`
- `CField_Battlefield::OnTeamChanged` at `0x5499e0`
- `CField_Battlefield::SetUserTeam` at `0x549870`
- `CField_Dojang::OnClock` at `0x550940`
- `CField_Dojang::Update` at `0x54ef10`
- `CField_CookieHouse::_UpdatePoint` at `0x54daa0`
- `CField_CookieHouse::Init` at `0x54e250`
- `CField_Wedding::OnWeddingProgress` at `0x5640f0`
- `CField_Wedding::SetBlessEffect` at `0x563b60`
- `CField_Witchtower::OnScoreUpdate` at `0x564ad0`
- `CScoreboard_Witchtower::Draw` at `0x564bd0`
- `CField_Massacre::OnClock` at `0x556af0`
- `CField_Massacre::UpdateKeyAnimation` at `0x556bf0`
- `CTimerboard_Massacre::Draw` at `0x557100`
- `CMemoryGameDlg::OnButtonClicked` at `0x628df0`
- `CMemoryGameDlg::OnCreate` at `0x62f3c0`
- `CMemoryGameDlg::Draw` at `0x631cb0`
- `CField_SnowBall::OnSnowBallState` at `0x560ab0`
- `CField_SnowBall::OnSnowBallHit` at `0x5619d0`
- `CField_SnowBall::OnSnowBallMsg` at `0x562040`
- `CField_SnowBall::OnSnowBallTouch` at `0x560510`
- `CField_MonsterCarnival::CreateUIWindow` at `0x55a510`
- `CField_MonsterCarnival::OnEnter` at `0x55a6c0`
- `CField_PartyRaid::Init` at `0x55f110`
- `CField_PartyRaidBoss::OnFieldSetVariable` at `0x55ca40`
- `CField_PartyRaidResult::OnSessionValue` at `0x55e5c0`
- `CField_SpaceGAGA::OnClock` at `0x5625d0`
- `CTimerboard_SpaceGAGA::Draw` at `0x5626c0`
- `CField_ContiMove::OnStartShipMoveField` at `0x54c970`
- `CField_ContiMove::OnContiState` at `0x54d5a0`
- `CField_KillCount::OnPacket` at `0x554050`
- `CField_LimitedView::Init` at `0x5554a0`
- `CField_HuntingAdballoon::OnFieldSetVariable` at `0x552050`
- `CField_MassacreResult::OnPacket` at `0x55a1d0`
- `CField_EscortResult::OnSessionValue` at `0x551160`
- `CField_Tournament::OnPacket` at `0x563780`
- `CRPSGameDlg::OnCreate` at `0x6d7480`
- `CRPSGameDlg::OnPacket` at `0x6d9e00`
- `CField_ShowaBath::DecodeFieldSpecificData` at `0x55ff70`
- `CField_Tutorial::DecodeFieldSpecificData` at `0x563830`
- `CField_MonsterCarnivalWaitingRoom::Init` at `0x55bda0`
- `CField_MonsterCarnivalS2_Game::Init` at `0x55bf10`
- `CField_NoDragon::Init` at `0x55c0e0`
- `CField_WeddingPhoto::GetFieldType` at `0x53eee0`
- `CField_AranTutorial::GetFieldType` at `0x53f1c0`
- `CField_Balrog::GetFieldType` at `0x549810`

Notes:
IDA shows that these field modes are not generic map-rule variants. The client owns separate state machines, packet handlers, timer or scoreboard widgets, and even dedicated minigame dialog owners such as `CMemoryGameDlg` for Guild Boss, Coconut, Wedding, Witchtower, Massacre, SnowBall, Ariant Arena, Battlefield, Dojang, Cookie House, Monster Carnival, Party Raid, SpaceGAGA, and MiniRoom card games, so parity work should be tracked separately from the broader field, combat, or UI backlog areas.
The latest broad `FieldFactory` / `OnPacket` re-check did not add any new completely untracked special-field families beyond the rows already indexed below; the remaining gaps are now row-depth and ownership-detail gaps rather than missing top-level wrapper discovery.

### 2. Additional packet-owned microgame dialog discovered by follow-up IDA scan

- `CRPSGameDlg::OnCreate` at `0x6d7480`
- `CRPSGameDlg::OnPacket` at `0x6d9e00`
- `CRPSGameDlg::ProcessPacket` at `0x6d72d0`
- `CUtilDlgEx` notice path (`StringPool` `0xE83`) via `CRPSGameDlg::OnPacket`

Notes:
`CRPSGameDlg` is not a generic field-utility callback; it is a dedicated packet-owned dialog owner with an explicit `OnCreate` seam plus packet-owned create/destroy and round-state branches. Keep this indexed separately from the broader special-field owner list.

### 3. Additional client-owned field wrappers discovered by IDA function scan

- `CField::FieldFactory`
- `CField_DynamicFoothold::GetFieldType`
- `CField_DynamicFoothold::Init` at `0x551050`
- `CField_ContiMove::OnContiState` at `0x54d5a0`
- `CField_Tutorial::DecodeFieldSpecificData` at `0x563830`
- `CField_LimitedView::Init` at `0x5554a0`

Notes:
These wrapper owners are intentionally indexed as a separate scan-discovery surface because they represent field-class ownership seams (transport/tutorial/presentation/dynamic-foothold wrappers) rather than minigame-specific rules.

### 4. Additional Party Raid wrapper-family ownership confirmed by `CField::FieldFactory` re-check

- `CField::FieldFactory` at `0x53f220`
- `CField_PartyRaid::GetFieldType` at `0x55c440`
- `CField_PartyRaidBoss::GetFieldType` at `0x55c640`
- `CField_PartyRaidResult::GetFieldType` at `0x55c800`

Notes:
The latest `CField::FieldFactory` re-check confirms that Party Raid is not a single generic owner. The client still instantiates three distinct wrapper classes, so that split should stay indexed separately from the broader gameplay row below.

### 5. Cake Pie event timerboard and item-info owners discovered by follow-up IDA function scan

- `CCakePieEvent::SetEventItemInfo` at `0x47c8e0`
- `CCakePieEvent::OpenUI_ItemInfo` at `0x47c830`
- `CCakePieEvent::CloseUI_ItemInfo` at `0x479040`
- `CUICakePieEventItemInfo::OnCreate` at `0x47a3c0`
- `CUICakePieEventItemInfo::Draw` at `0x47a4e0`
- `CUICakePieEventItemInfo::Update` at `0x4791c0`
- `CUICakePieEventItemInfo::OnButtonClicked` at `0x4791a0`
- `CTimerboard_CakePieEvent::OnCreate` at `0x47abc0`
- `CTimerboard_CakePieEvent::Draw` at `0x47bab0`
- `CTimerboard_CakePieEvent::GetFontTime` at `0x47c670`

Notes:
The follow-up function scan found a Cake Pie event owner family that the field-factory sweep would not expose as a top-level `CField` wrapper. `CTimerboard_CakePieEvent::OnCreate` subclasses the generic `CClock` path, creates its own backing canvas, and switches between `Map/Obj/etc.img/5th_TownUI/{backgrd,bareffect,gage/0,gage/1}` and `Map/Obj/etc.img/5th_Timer/{backgrd,ready,start,cake,pie}` depending on timer type. The paired `CUICakePieEventItemInfo` owner has its own close button and draw/update lifecycle, while `CCakePieEvent` owns item-info setup and open/close routing. That makes Cake Pie a distinct event UI/timerboard owner rather than only a generic clock or reward-popup variant.

## Current State Summary

MapSimulator already has a partial special-field baseline instead of a blank area.
The project currently includes:

- A `SpecialEffectFields` surface with wedding, witchtower, guild-boss, and massacre handling.
- A concrete `MinigameFields` surface that now owns the snowball and coconut runtimes behind one backlog-aligned seam.
- A `SpecialFieldRuntimeCoordinator` seam in `MapSimulator` that binds special-field ownership to map load, update, draw, and reset flow so later agents can take one backlog row without redoing the simulator plumbing.
- Several packet-commented simulator seams that already reference the corresponding client field owners.

The parity gap here is not whether the simulator recognizes special maps at all.
The gap is whether those maps reproduce the client's dedicated rules, scoreboards, timers, UI flows, and win or lose transitions.

## Broad IDA Re-check

The latest broad wrapper-owner re-check against `CField::FieldFactory` (`0x53F220`) and `CField::OnPacket` (`0x546D50`) did not surface any additional special-field families that still sit completely outside this document.
The factory sweep still resolves only to wrapper families already tracked here or intentionally owned by other backlog areas, including transport/Balrog, Tournament, Coconut, Guild Boss, Limited View, Monster Carnival variants, Ariant Arena, Dojo, Wedding/Wedding Photo, tutorial variants, Cookie House, Battlefield, SpaceGAGA, Witchtower, Aran Tutorial, Massacre/Massacre Result, Party Raid variants, No Dragon, Dynamic Foothold, Escort Result, Hunting Ad Balloon, Kill Count, and Showa Bath.
The packet-owner sweep also still funnels the shared active-wrapper handoff through opcode `163`, which is already captured in the shared current-wrapper dispatch row below rather than being a separate missing backlog family.
A later broad function scan did surface the Cake Pie event timerboard and item-info owner family. It is tracked below as an event UI owner because it does not appear to be a new top-level `CField::FieldFactory` wrapper family, but it still owns special-event presentation that would be missed by only following field-factory classes.

## Remaining Parity Backlog

### 1. Special field and minigame parity

This area is now tracked in its own backlog document because both the client and simulator already expose dedicated field-specific seams.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Guild boss event fields | Guild-boss ownership remains in the existing `SpecialEffectFields` seam: `DetectFieldType` still prioritizes the map-root WZ `healer` plus `pulley` contract, preserves legacy field-type and id-range fallbacks, and configures `GuildBossField` directly from those map-root nodes. WZ evidence is unchanged for `Map/Map9/990000900` (`healer`: `x=295`, `yMin=-564`, `yMax=-350`, `rise=1`, `fall=10`, `healMin=1000`, `healMax=2000`; `pulley`: `x=-59`, `y=-394`) and `Map/Obj/guild.img/syarenian/boss/{0,1}` still carries 18 healer frames (origin `124,199`) plus 3 pulley-hit frames (origin `184,181`) at `90` ms. Client evidence is also unchanged: `CField_GuildBoss::BasicActionAttack` (`0x5517d0`) still gates outbound `COutPacket(259)` on `m_nState == 0` after `CPulley::Hit`, and `CPulley::Hit` (`0x551390`) still suppresses restart while animation is already active. Runtime ownership still matches that seam: local attacks avoid simulator-only preview while official-session ownership is armed/pending, and opcode `259` is sent immediately on initialized proxied sessions or queued for reconnect handoff when the bridge is listening but not initialized. Command-surface parity remains exposed through `/guildboss session` with passive `attach <remotePort> [processName|pid] [localPort]`, reconnect-armed `attachproxy <listenPort|0> <remotePort> [processName|pid] [localPort]`, and `start`/`startauto`/`attachproxy` support for listen port `0`. This pass adds the missing focused regression coverage on disk in `UnitTest_MapSimulator/GuildBossParityTests.cs` for pulley-hit duration fallback, passive established-session queue rejection, reconnect-proxy queue allowance with auto listen-port selection, and parser acceptance of listen port `0`. Remaining gap stays specific to post-handshake live-session ownership: the simulator still cannot decrypt inbound or inject outbound guild-boss traffic into an already-established Maple socket pair unless that session reconnects through the localhost official-session proxy first. | Guild boss now owns WZ-backed map detection, client-owned `CPulley::Hit` one-shot animation semantics, packet decode, outbound opcode recovery, proxy emission, passive established-session observation, reconnect-armed proxy attach, deferred official-session injection, and row-focused parity coverage in the same special-field seam. The row remains `Partial` only because true in-place post-handshake decrypt/inject ownership on an already-established Maple socket pair is still missing; remaining work is no longer detection, contract extraction, pulley lifecycle, command-surface exposure, proxy-port `0` handling, or queueing ownership. | `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `SpecialFieldRuntimeCoordinator.cs`, `SpecialEffectFields.cs`, `GuildBossField.cs`, `GuildBossPacketTransportManager.cs`, `GuildBossOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/GuildBossParityTests.cs`, `GuildBossField` (`CField_GuildBoss`, `CField_GuildBoss::Init`, `CField_GuildBoss::OnPacket`, `CField_GuildBoss::BasicActionAttack`, `CHealer::Init`, `CHealer::Move`, `CPulley::Init`, `CPulley::Hit`) |
| Partial | Coconut minigame runtime | The simulator still follows the client's finish-timestamp clocking, keeps score packet-owned instead of inventing local score on ground contact, applies packet-driven coconut state changes through the runtime's delayed hit queue, mirrors `CField_Coconut::BasicActionAttack` by selecting an intersecting coconut from the player's normal-attack world hitbox, keeps that request transport-owned when a bridge client is present instead of replaying a simulator-only delayed hit preview, and keeps Coconut basic-action ownership alive for the full local normal-attack span even when a hit ack dequeues immediately. A WZ re-check in this pass kept the authored surface unchanged in the current extracted data source: `Map/Obj/etc/coconut/backgrnd` still exposes the `258x101` board, `Map/Map/Map1/109080000/coconut` still exposes the same count/time/avatar contract (`countFalling=401`, `countBombing=80`, `countStopped=20`, `timeDefault=300`, `timeExpand=120`, `timeMessage=6`, `timeFinish=12`), and `Map/Effect.img/event/coconut/{victory,lose}` still exposes the same result-banner frames and delays. Client evidence stayed second and aligned with the same handlers: `CField_Coconut::BasicActionAttack` (`0x54a5e0`) still selects an intersecting coconut rect then sends opcode `257` through the socket owner after `CUserLocal::TryDoingNormalAttack(...)` resolves target id and `tDelay`; `CField_Coconut::OnClock` (`0x54a100`) still owns finish-timestamp semantics (`m_tFinish = now + seconds*1000`, coerced to `1` when zero); and `CField_Coconut::DrawBoard` (`0x54a750`) still uses board-layer glyph anchors that match the runtime seam (`score x/y: 25,37 and 150,37; timer x/y: 60,83`). This pass closes the missing in-repo parity coverage for that command/runtime seam instead of widening ownership: focused regression coverage is now on disk in `UnitTest_MapSimulator/CoconutParityTests.cs` for the basic-action owner window surviving immediate ack dequeue, authored uniform inference from Coconut cap or longcoat ids, preservation of transport-owned team control against later avatar inference, preview gating through `ShouldAllowLocalCoconutAttackPreview(...)`, `listenPort|0` parser behavior, passive established-session attach refusing opcode `257` injection without localhost-proxy reconnect, and board-layer position parity. The remaining gap is now explicit and narrow: Coconut still cannot decrypt or inject against an already-established MapleStory socket pair without reconnecting through the localhost proxy, and the runtime still renders through simulator-owned draw primitives instead of the client's native `IWzCanvas`/`IWzGr2DLayer` board-layer lifecycle. | Coconut now covers proxy-owned transport and passive established-session observation through the existing official-session bridge seam, alongside packet-owned round clocking, delayed hit queue ownership, authored Coconut count/avatar contracts, local Maple-vs-Story uniform ownership, basic-action owner lifetime, board glyph placement parity, and the `listenPort|0` proxy bind contract with focused regression tests now present on disk. Remaining work is no longer unresolved packet shape, preview ownership, stale delayed-hit carryover, local-team source precedence, early local attack-owner release, missing attach-surface exposure, or parser mismatch; it is specifically true post-handshake decrypt/inject ownership on an already-live socket pair plus the still-simplified native board-layer presentation path. | `MinigameFields.cs`, `MapSimulator.cs`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.ClientOwnedFieldWrappers.cs`, `HaCreator/MapSimulator/CoconutSessionCommandParsing.cs`, `CoconutPacketInboxManager.cs`, `CoconutOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/CoconutParityTests.cs`, `CoconutField` (`CField_Coconut::Init`, `CField_Coconut::OnPacket`, `CField_Coconut::OnClock`, `CField_Coconut::OnCoconutHit`, `CField_Coconut::OnCoconutScore`, `CField_Coconut::BasicActionAttack`, `CField_Coconut::DrawBoard`) |
| Partial | Wedding ceremony fields | The simulator now resolves ceremony lines from `String/Npc.img/<npcId>/wedding{step}` with WZ-backed fallbacks, distinguishes groom or bride versus guest state from the local character id, mirrors the Saint Maple Altar-only step-2 guest bless prompt that `CField_Wedding::OnWeddingProgress` special-cases, records the client's ceremony response packet flow through step-advance (`163`) and guest-bless (`164`) responses, exposes a `/wedding` debug seam to drive progress, actor anchors, guest audience overlays, and responses through that same runtime, loads the real bless animation frames from `Effect/BasicEff.img/Wedding`, swaps BGM to the WZ-backed `Sound/BgmEvent.img/wedding` track from the same step-0 seam the client uses, restores the map BGM when the wedding runtime clears, draws the WZ-backed declaration overlay from `UI/UIWindow.img/wedding/text/0` during the opening step, and promotes the later pronouncement and blessing steps into the second recovered wedding scene layer from `UI/UIWindow.img/wedding/card/0` with the existing wedding weather celebration path. A fresh WZ re-check in this pass kept that asset evidence pinned to the same recovered shapes in the current IMG filesystem and tightened the guest-bless response seam inside the existing runtime instead of inventing new assets: `UI/UIWindow.img/wedding` still only exposes `text/0` (`518x659`, origin `233,306`) plus `card/0` (`295x168`, origin `147,84`), `Effect/BasicEff.img/Wedding` still exposes the 16-frame bless animation with the same `120/180/300` timing pattern, `Map/MapHelper.img/weather/wedding` plus `weather/heartWedding` still expose `count=35` wedding-particle contracts in the current export, `weather/heartWedding` still marks `random=1`, `String/Npc.img/9201002` still omits `wedding2` while keeping `wedding0`, `wedding1`, `wedding3`, and `wedding4`, and `String/Map.img/GL/680000110` plus `GL/680000210` still identify the two ceremony maps as `White Wedding Altar` and `Saint Maple Altar`. IDA re-checks of `CField_Wedding::OnWeddingProgress` at `0x5640f0`, `CField_Wedding::OnWeddingCeremonyEnd` at `0x5640a0`, and `CField_Wedding::SetBlessEffect` at `0x563b60` also stayed aligned with the modal and scene-owner rules already recovered here: the client closes any existing `CUtilDlgEx` before opening the next wedding dialog, does not own a local timeout-driven auto-dismiss path for those modals, suppresses the step-2 dialog entirely for the groom and bride on Saint Maple Altar, clears the active dialog again before `SetBlessEffect` when the ceremony-end packet arrives, does not use that ceremony-end handler to rebuild the broader wedding scene, only creates the bless layer after both groom and bride `CUser` entries resolve before anchoring the layer at the couple midpoint with the recovered `-20` Y offset, and only sends opcode `164` when the Saint Maple guest bless modal returns `YES` (`DoModal == 6`) rather than on the `NO` branch. `WeddingField` now mirrors that client-owned lifecycle inside the existing seam by dismissing any live wedding dialog on step changes or ceremony end instead of letting the old dialog linger, by keeping both participant text dialogs and the Saint Maple guest bless prompt open until an explicit response path closes them rather than auto-closing them on a local five-second timer, by explicitly clearing the opening text overlay again when the ceremony-end bless effect starts without forcing the later card or celebration scene owner on from the end packet alone, by leaving an already-active recovered card and celebration scene continuous across the ceremony-end bless packet instead of tearing those particles down and respawning them on every transition, by suppressing the bless effect when either couple actor anchor is still missing instead of synthesizing a local fallback point, by driving the recovered wedding and heart-wedding weather layers from the WZ `count` and `random` metadata instead of the older smaller hardcoded particle counts, by wrapping ceremony lines into a bounded modal box so the long WZ-backed vows and prompts render more like the client utility dialog instead of a single oversized strip, and now by treating the Saint Maple guest bless modal as a yes-only packet seam so the `yes` branch emits the recovered `164` response record while the `no` branch closes locally without inventing a simulator-only packet. The existing wedding-side remote actor seam remains in place too: `/wedding inbox ...` still accepts the adjacent client-shaped remote-user packet aliases for spawn, leave, move, chair, avatar refresh, and temporary-stat set/reset; `WeddingField` still routes those payloads through the shared remote-user packet codec; the recovered `SecondaryStat::DecodeForRemote` presentation slice still survives into groom, bride, and guest actors through the delayed-load and shared-pool handoff; audience actors can still be promoted into groom or bride overlays when ceremony ids later identify them; real remote character ids still survive into the shared wedding renderer path; and packet-backed `PlayerMovementSyncSnapshot` data still lets groom, bride, and guest actors follow `CMovePath` playback instead of teleporting between static overlays. Focused wedding metadata regression coverage is now on disk in `UnitTest_MapSimulator/WeddingRemoteUserMetadataParityTests.cs` for the independent name-tag, profile-metadata, and guild-mark revision gates used by the shared-pool handoff path. The remaining gap is narrower and explicit: the simulator still only models the recovered presentation-relevant `SecondaryStat::DecodeForRemote` slice rather than the full remote temporary-stat surface, it still does not reproduce the full native name-tag owner choreography beyond the now revision-gated profile/guild-mark refresh path in the shared wedding renderer handoff, and the ceremony scene owner is still a simulator reconstruction of the recovered WZ text/card layers, bless effect, and weather particles rather than the client's full native `IWzGr2DLayer` wedding timeline. | Wedding maps now cover both recovered WZ ceremony overlays and a client-backed modal dialog lifecycle inside the existing wedding seam in addition to the dedicated wedding-side remote actor path for the couple plus guest audience, and this pass tightens the shared wedding remote-user handoff by syncing profile and guild-mark metadata on their own revision lifecycles instead of treating name-tag revision as the only refresh gate. The remaining work is no longer basic modal ownership, dialog cleanup, participant step-2 suppression, reset/BGM cleanup, recovered weather-count ownership, promoted-actor continuity, or missing profile/guild-mark metadata refresh triggers; it is concentrated on the unrecovered temporary-stat surface, the unrecovered portions of the client name-tag owner choreography, and the still-undecoded native wedding layer timeline beyond the WZ-backed scene surfaces already identified. | `SpecialEffectFields.cs`, `WeddingField`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.WeddingRemoteUsers.cs`, `MapSimulator.UpdateLoop.cs`, `RemoteUserPacketCodec.cs`, `WeddingPacketInboxManager.cs`, `UnitTest_MapSimulator/WeddingRemoteUserMetadataParityTests.cs` (`CField_Wedding::OnWeddingProgress`, `CField_Wedding::OnWeddingCeremonyEnd`, `CField_Wedding::SetBlessEffect`, `CUserPool::OnUserEnterField`, `CUserPool::OnUserLeaveField`, `CUserPool::Update`, `CUserPool::OnUserRemotePacket`, `SecondaryStat::DecodeForRemote`, `CUserRemote::OnMove`, `CUserRemote::OnAvatarModified`, `CUserRemote::Update`) |
| Implemented | Witchtower score UI | The simulator anchors the Witchtower scoreboard to the client's center-top window origin, loads the exact `Map/Obj/etc.img/goldkey` background, key, and bitmap digits that match the `CScoreboard_Witchtower::OnCreate` asset shape, renders the zero-padded score at the verified `(67, 4)` draw origin with the client's negative digit spacing, and keeps the `witchscore` debug seam driving the same update path | This is a visible special-field HUD with client-backed art and placement now, so the remaining special-field work should move to other rows instead of revisiting a placeholder Witchtower overlay | `SpecialEffectFields.cs`, `WitchtowerField`, `MapSimulator.cs` (`CField_Witchtower::OnScoreUpdate`, `CScoreboard_Witchtower::OnCreate`, `CScoreboard_Witchtower::Draw`) |
| Partial | Massacre timerboard and gauge flow | The simulator now mirrors the client's type-2 Massacre timerboard ownership, WZ-backed count board, key animation lifecycle, 259-pixel decay gauge, danger overlay cutoff, packet `173` inc-gauge intake, packet `174` result payload shape, and the existing loopback plus Maple proxy seams for the recovered live packet subset. This pass keeps the same WZ and client evidence anchors for the board and result owner while closing the stale command-surface gap around the already-shipped established-session seam: `/massacre session` now exposes the passive `attach <remotePort> [processName|pid] [localPort]` path that `MassacreOfficialSessionBridgeManager.TryAttachEstablishedSession(...)` already owned and the reconnect-armed `attachproxy <listenPort|0> <remotePort> [processName|pid] [localPort]` path that calls `TryAttachEstablishedSessionAndStartProxy(...)`, alongside `discover`, `start`, `startauto`, and `stop`; `start`, `startauto`, and `attachproxy` all share the existing listen-port `0` contract for OS-selected localhost proxy ports. The result-board draw contract from the prior pass remains pinned to `CField_MassacreResult::OnMassacreResult` at `0x5596c0`: the left KILL/COOL/MISS rows at (`218,63`), (`218,87`), and (`218,111`) still draw the raw `CWvsContext::Massacre` hit, cool, and miss counts, the mirrored rows at (`265,63`), (`265,87`), and (`265,111`) still draw the corresponding percentages over the same client total, and the big EXP score still lands at (`258,168`). The WZ-backed result roots remain `MonsterKilling/Result/backgrd`, `backgrd2`, `Rank/{s,a,b,c,d}`, `Result/number`, `Result/number2`, and `Map/Effect.img/killing/yeti0` through `yeti4`; the WZ re-check for this pass also kept `Map/Obj/etc.img/killing/backgrnd`, `UIWindow.img/MonsterKilling/Count/backgrd0`, and `UIWindow.img/MonsterKilling/Gauge/{backgrd,pixel}` in the same authored timer/count/gauge contract, plus the wrapper-flow scripts that now drive clear/fail carryover (`Map/Map9/910320010/info/onFirstUserEnter = killing_BonusSetting`, `Map/Map9/910320001/info/onUserEnter = Massacre_result`, `fieldType = 24`). Focused source coverage is now on disk in `UnitTest_MapSimulator/MassacreParityTests.cs` for listen port `0`, passive established-session observation state, auto-selected reconnect-proxy arming, direct main-wrapper to result-wrapper fail carryover, and bonus-wrapper to result-wrapper clear carryover. The remaining gap is narrower again: both passive established-session attach and reconnect-proxy attach are now reachable from the runtime command seam, and the in-field clear/fail transition now follows the recovered WZ-backed wrapper flow under the packet-owned result owner, but the simulator still cannot decrypt or inject Massacre traffic into an already-established post-handshake Maple socket pair unless that session reconnects through the localhost proxy first, and fuller server-owned Massacre clock/context/result choreography beyond the recovered inc-gauge and result packets is still missing. | Hunting-event maps depend on dedicated timer, point-board, skill-key, gauge, and result-owner behavior that the client does not route through the normal status bar. This row now covers the client-backed HUD art, timerboard placement, key lifecycle, map-default gauge rules, stage and bonus presentations, rank-plus-score result payload, center-origin result board placement, raw result count rows, percentage rows, WZ-backed rank effects, WZ-backed result-wrapper clear/fail carryover, the recovered live Massacre packet subset through the existing proxy and inbox seams, passive established-session attach, reconnect-proxy attach, and auto-selected proxy-port handling in the same official-session bridge seam that already owned them internally. Remaining work is concentrated on true in-place post-handshake decrypt/inject ownership and unrecovered live clock/context/result choreography rather than on missing timerboard, count-board, gauge, score, rank, result row, wrapper-flow clear/fail ownership, attach-surface exposure, reconnect-proxy command exposure, proxy-port `0`, or string-pool-path ownership. | `SpecialEffectFields.cs`, `MassacreField`, `MapSimulator.cs`, `MapSimulator.Content.cs`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.ChatCommands.cs`, `MassacreSessionCommandParsing.cs`, `MassacrePacketInboxManager.cs`, `MassacreOfficialSessionBridgeManager.cs`, `MapleStoryStringPool.cs`, `UnitTest_MapSimulator/MassacreParityTests.cs` (`CField_Massacre::Init`, `CField_Massacre::_SetMassacreInfo`, `CField_Massacre::_UpdatePoint`, `CField_Massacre::OnPacket`, `CField_Massacre::OnClock`, `CField_Massacre::Update`, `CField_Massacre::UpdateKeyAnimation`, `CField_Massacre::_SetDecGauge`, `CTimerboard_Massacre::OnCreate`, `CTimerboard_Massacre::Draw`, `CField_MassacreResult::OnPacket`, `CField_MassacreResult::OnMassacreResult`) |
| Partial | Memory Game and MiniRoom card parity | The simulator now syncs the MiniRoom shell occupant roster and status text to the live Match Cards board, accepts mouse-driven sidebar and card hit testing on the `CMemoryGameDlg`-style surface, exposes delayed remote ready/start/flip/tie/give-up/end actions through the same runtime path that the local room uses, adds both a typed `MemoryGamePacketType` dispatch seam behind `/memorygame packet ...`, a raw MiniRoom payload seam behind `/memorygame packetraw ...`, an outbound-client replay seam behind `/memorygame packetclientraw <opcode-wrapped packet>`, and an opcode-wrapped receive seam behind `/memorygame packetrecv <opcode> <payload>`, decodes the client-backed MiniRoom gameplay subtypes (`6 -> 50/51/58/59/61/62/63/68`) plus the shared MiniRoom base enter, chat, avatar, and leave families (`4/7/8/9/10`) into packet-owned roster, visitor, avatar-summary, chat-log, and leave handling, applies packet-owned board shuffle data from `CMemoryGameDlg::OnUserStart`, upgrades the shared MiniRoom shell with a dedicated Match Cards chat log plus system or speaker message presentation instead of the old generic preview-only pane, preserves visitor leave packets without tearing down the active board while surfacing slot-backed job or face or hair summaries in the room roster, now auto-starts a dedicated loopback MiniRoom packet inbox so live raw payloads, opcode-wrapped receive lines, or opcode-`144` client request captures can drain through the same runtime seam with `/memorygame inbox ...` controls, normalizes two-byte Maple opcode wrappers before dispatch so externally captured server receive packets can reuse the same `CMemoryGameDlg` packet path, maps the `CMiniRoomBaseDlg::MakeGameMessage` codes onto the client StringPool ids recovered from IDA while surfacing those ids in system messages, renders decoded `AvatarLook` payloads as actual occupant avatar previews inside the MiniRoom shell by rebuilding `CharacterBuild` previews through the existing avatar loader path, and now also exposes a built-in live Maple proxy bridge behind `/memorygame session [discover|start|startauto|stop]` that initializes Maple crypto from the handshake, forwards live client/server traffic, peels inbound MiniRoom opcode `373` packets back through the same Match Cards dispatch seam, classifies the supported live client `opcode 144` Match Cards request subset (`10/50/52/56/57/58/59/60/61`) into mirrored `packetclientraw ...` bridge messages, routes the local end-button and `/memorygame end` path through the same leave-booking seam that client subtypes `56/57` use so active rounds book or cancel a pending leave instead of tearing the room down immediately, preserves explicit CRLF prompt layout from the client string-pool prompts instead of collapsing those confirmation dialogs into one wrapped paragraph, clears the stale local mismatch timer when packet-owned `63` time-over ownership arrives so `CMemoryGameDlg::OnTimeOver` cannot be followed by a second simulator-only turn advance on the next update tick, pins the recovered Match Cards MiniRoom StringPool ids in the shared `MapleStoryStringPool` override seam, preserves the prior room status when a CUtilDlg-style prompt is cancelled instead of turning prompt cancel into a synthetic room status/system message, and now also mirrors the client-side `m_bDrawRequestSent_Turn` gate recovered from `CMemoryGameDlg::SendTieRequest` and `CMemoryGameDlg::OnTurnUpCard` so a denied local tie request stays blocked for the rest of that turn until a second card-resolution path actually clears it. This pass also mirrors the client `CUtilDlg` modal gate by blocking local ready/start/card-turn actions and opcode-`144` outbound replay requests while a Match Cards confirmation prompt is active, applies inbound ready/cancel-ready gameplay packets to the remote seat resolved from local position instead of assuming seat `1`, and now aligns `CMemoryGameDlg::OnClickStartButton` by allowing local subtype `61` start requests whenever an opponent seat is present (without a remote-ready pre-gate). Focused coverage is now on disk in `UnitTest_MapSimulator/MemoryGamePromptParityTests.cs` for remote-seat ready routing, host start-request gating parity, and prompt-modal outbound-request gating. The remaining gap is narrower again: the simulator still does not attach to an already-running MapleStory socket session in-process or passively decrypt MiniRoom traffic without the proxy bridge, and the exact client confirmation choreography around leave-booking, give-up, ban, and other outbound request prompts is still not byte-perfect beyond the modeled toggle-and-resolve state machine, restored cancel-status handling, the recovered tie-request-per-turn lockout, and the new modal gating around local outbound requests. | MiniRoom parity now covers the room shell, board state machine, click flow, raw gameplay-packet decode, opcode-wrapped receive-packet normalization, manual and bridged outbound-client request playback for the supported opcode-`144` Match Cards subset, client-shaped host start-request gating, packet-owned start-board data, the shared base enter/chat/avatar/leave families, a loopback inbox plus live proxy bridge transport seam, StringPool-id-backed game-message presentation through the shared override table, real remote-avatar previews inside the shared shell, preserved multi-line client prompt text, restored status after cancelled confirmation prompts, a corrected `OnTimeOver`-style packet owner that no longer lets a stale local mismatch timer re-advance the board after the packet already chose the next turn, and the client-backed "one tie request per turn until turn resolution" rule. The remaining work is concentrated on fuller outbound prompt choreography and true direct live-session ownership rather than on missing core Match Cards ownership, missing packet replay coverage, missing localized StringPool coverage, or stale mismatch carryover after time-over packets. | `HaCreator/MapSimulator/Fields/MemoryGameField.cs`, `HaCreator/MapSimulator/Interaction/SocialRoomModels.cs`, `HaCreator/MapSimulator/UI/Windows/SocialRoomWindow.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `HaCreator/MapSimulator/MapSimulator.ChatCommands.cs`, `HaCreator/MapSimulator/Managers/MemoryGamePacketInboxManager.cs`, `HaCreator/MapSimulator/Managers/MemoryGameOfficialSessionBridgeManager.cs`, `HaCreator/MapSimulator/Managers/LoginAvatarLookCodec.cs`, `UnitTest_MapSimulator/MemoryGamePromptParityTests.cs` (`CMemoryGameDlg::OnPacket`, `CMemoryGameDlg::OnUserReady`, `CMemoryGameDlg::OnUserCancelReady`, `CMemoryGameDlg::OnClickReadyButton`, `CMemoryGameDlg::OnClickStartButton`, `CMemoryGameDlg::OnTurnUpCard`, `CMemoryGameDlg::OnClickEndButton`) |
| Implemented | Snowball minigame runtime | The simulator now treats Snowball state `2/3` as the client's packet-owned win states instead of local touch-zone states, keeps snowman HP packet-owned via `OnSnowBallState`, applies WZ-backed snowman stun windows and damage defaults through the runtime seam, smooths active snowball movement from the packet position plus speed data instead of instant teleports, routes the local player's world position through `SpecialFieldRuntimeCoordinator` so `SnowBallField` can surface the client's outbound touch-packet loop as pending team touch requests whenever the local player overlaps a snowball lane, aligns the Story-vs-Maple team labeling used by win banners and the local scoreboard with the client message owner, and routes `OnSnowBallMsg` through the simulator chat log seam with client-shaped team ordering plus packet-owned fallback text instead of showing those packet messages as center-screen overlays. The runtime also stopped depending on ad hoc Snowball setup: `SpecialFieldRuntimeCoordinator` now detects Snowball maps through the preserved `snowBall` map property or `FIELDTYPE_SNOWBALL`, `MinigameFields.BindMap` now auto-binds `SnowBallField`, and `SnowBallFieldDataLoader` reads the copied WZ `snowBall` node from `MapInfo.additionalNonInfoProps` or the bound `WzImage` so the simulator uses the same client-owned config that `CField_SnowBall::Init` consumes. That bind path mirrors the client-owned shared-`x`/shared-`x0` plus per-team-`y` model recovered from `CField_SnowBall::Init` at `0x561a10`: the simulator loads `dx`, `x`, `x0`, `xMin`, `xMax`, `damageSnowBall`, `damageSnowMan0`, `damageSnowMan1`, `snowManHP`, `snowManWait`, `section1`, `section2`, and `section3`, positions both snowmen on the shared client `x` with their own lane `y`, positions both snowballs from the shared client `x0` with their own lane `y`, clamps active movement to the client `xMin/xMax` span, and resolves the WZ `Map/Obj/event.img/snowyRock/snowball/{0,1}` paths directly into `IDXObject` frame sets through the existing minigame seam when a graphics device is available. `SnowBallField.OnSnowBallState` still mirrors the client-owned first-snapshot ownership from `CField_SnowBall::OnSnowBallState` at `0x560ab0` by accepting packet-fed `damageSnowBall`, `damageSnowMan0`, and `damageSnowMan1` values only on that first state snapshot, and repeated `state == 2/3` packets no longer inflate the simulator-only round scoreboard or replay duplicate win banners. The row was re-checked against the exported IMG filesystem through `MapleLib.Img.ImgFileSystemDataSource`, which still does not expose localized `String` nodes for ids `3445`-`3447` (`0xD75`-`0xD77`), but the generated client string-pool table in the simulator already carried those three Snowball notice payloads with `%2C`-escaped comma bytes; this pass closes the last runtime gap by normalizing those ids through `MapleStoryStringPool.OverrideEntries` so `SnowBallField.FormatSnowBallMessage` now resolves the actual client literals instead of simulator-authored fallback prose. The transport-attachment gap is also closed inside the existing seam instead of leaving `SnowBallOfficialSessionBridgeManager` unreachable on disk: `MapSimulator` owns that bridge alongside the loopback inbox, stops it on Snowball exit, drains live-session opcode `338`-`341` payloads into the same `SnowBallField.TryApplyPacket` path as loopback traffic, prefers live-session opcode `256` touch injection when a Snowball proxy is attached while still falling back to `SnowBallPacketInboxManager`, and exposes the full runtime through `/snowball` commands for state, hit, message, touch, inbox, session, and pending-touch inspection or control. Focused tests are now on disk for the recovered Snowball string-pool ids and message formatting path in addition to the Snowball official-session bridge discovery and startup shapes used by `/snowball session`. | Snowball chat, state, movement, touch, config loading, and official-session transport now run through the client-owned seams reviewed for this row, including the exact `OnSnowBallMsg` string-pool ids `0xD75`-`0xD77` rather than fallback English templates. | `MinigameFields.cs`, `SnowBallField`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `SnowBallPacketInboxManager.cs`, `SnowBallOfficialSessionBridgeManager.cs`, `MapleStoryStringPool.cs`, `UnitTest_MapSimulator/SnowBallStringPoolParityTests.cs` (`CField_SnowBall::Init`, `CField_SnowBall::OnSnowBallState`, `CField_SnowBall::BasicActionAttack`, `CField_SnowBall::OnSnowBallHit`, `CField_SnowBall::OnSnowBallMsg`, `CField_SnowBall::OnSnowBallTouch`) |
| Partial | Ariant Arena field flow | The simulator still binds `FIELDTYPE_ARIANTARENA` to `AriantArenaField`, routes the official Ariant remote-user packet family (`179`, `180`, `210`, `211`-`217`, `218`, `219`, `220`, `221`, `222`, `223`, `224`, `225`, `226`, `230`) through the shared `RemoteUserActorPool`, and keeps Ariant score/result ownership on `354` and `171` with WZ-backed `UI/UIWindow.img/AriantMatch/{characterIcon,Result}` plus `Sound/MiniGame.img/Show`. WZ was re-checked first for those UI/sound owners, then client handlers were re-checked in IDA (`CUserRemote::OnHit` and `CUser::OnEffect`): this pass extends the existing codec seam so `RemoteUserPacketCodec.TryParseFlexibleEffectStringPayload(...)` now accepts additional client-shaped `OnEffect` string payload branches used in Ariant-visible `224` flow, including optional one-byte branch markers before `DecodeStr` payloads, Maple-string payloads with optional trailing `Decode4` integers (including zero-padding tails), and plain-text payloads with optional trailing `Decode4` integers. `TryParseHit(...)` keeps the prior optional branch-byte handling and now shares the same branch-marker helper path. Focused Ariant coverage is now on disk in `UnitTest_MapSimulator/AriantArenaFieldTests.cs` for inbox alias parsing of `218`/`221`/`224`/`230`, official remote-user packet-family mapping into Ariant packet ids, `224` prefixed-string and trailing-int decode shapes, and `218` prefixed hit-string tail decode. The remaining gap stays explicit: remote temporary-stat modeling still covers only a visible `SecondaryStat::DecodeForRemote` slice rather than the full client surface, `224` presentation behavior in `RemoteUserActorPool.TryApplyEffect(...)` still does not reproduce every native `CUser::OnEffect` branch detail byte-for-byte, `218` still does not reproduce every native mob-authored choreography branch in `CUserRemote::OnHit`, and true direct live-session socket ownership remains outside the simulator seam. | Ariant Arena remains packet-owned in the client and now keeps more of the packet-authored `224`/`218` string-branch metadata intact inside the existing shared codec/pool seam, with row-local source coverage on Ariant alias/mapping/decode behavior. Remaining work is concentrated on full-surface temporary-stat decode, deeper branch-complete effect/hit presentation choreography, and native live-session ownership rather than packet-family routing or Ariant rank/result ownership. | `MinigameFields.cs`, `AriantArenaField`, `AriantArenaPacketInboxManager.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `UnitTest_MapSimulator/AriantArenaFieldTests.cs` (`CField_AriantArena::OnUserScore`, `CField_AriantArena::UpdateScoreAndRank`, `CField_AriantArena::OnShowResult`, `CUserPool::OnUserRemotePacket`, `CUserRemote::OnHit`, `CUser::OnEffect`, `CUserRemote::OnShowUpgradeTombEffect`, `CUserRemote::OnThrowGrenade`, `SecondaryStat::DecodeForRemote`, `UI/UIWindow.img/AriantMatch/{characterIcon,Result}`, `Sound/MiniGame.img/Show`) |
| Implemented | Battlefield event flow | The simulator now detects Battlefield maps through the special-field runtime, loads `battleField` WZ defaults such as `timeDefault`, `timeFinish`, reward maps, and effect paths, follows the client's packet-owned `OnClock(type=2, seconds)` and `OnScoreUpdate(wolves, sheep)` seams, exposes a Battlefield team-change seam aligned with `OnTeamChanged(characterId, team)` and `SetUserTeam`, resolves a finish phase that derives win/lose effect plus reward-map routing from the WZ `battleField` node, parses the map's `user/<team>` presets so local Battlefield team changes now reapply both the client-backed outfit pieces and the WZ `stat/speed` move-speed override before restoring the original avatar and speed on clear or map exit, retains each preset's `speedmax` and `noitem` data for inspection or later consumption, resolves packet-owned remote team assignments through the same character-id lookup seam, renders the client Battlefield board from `Map/Obj/etc.img/battleField/backgrnd` with the real sheep or wolf score fonts and the six-digit `fontTime` clock layout, queues the resolved reward map through the existing special-field transfer seam once the WZ `timeFinish` phase expires, mirrors the local `SetUserTeam` minimap side effect by forcing the simulator minimap out of its collapsed state for local teams `0` and `2`, pushes the simulator's actual local character id into `BattlefieldField` so packet-shaped `OnTeamChanged` calls classify the local user correctly, wires the map preset's `noitem` and `speedmax` fields into live simulator systems by blocking local Battlefield item use plus equip drags for blocked item ids and by clamping the live player move-speed path through the preset speed cap, and in this pass drives those same WZ-backed team presets through the shared `RemoteUserActorPool` so remote Battlefield users now hot-swap their live avatar presentation too. The shared actor owner now re-syncs Battlefield looks during map bind and per-frame update, resolves late remote spawns against already-known field-owned team assignments instead of requiring the actor to cache the raw team id first, and restores each remote actor's original appearance and speed when Battlefield ownership clears or the map exits. Focused Battlefield tests were added on disk for that late-spawn remote-team path plus reset restore behavior, but the current workspace could not execute them becausePacketReader` accessibility, `LoginUtilityDialogWindow.Configure` accessibility, and missing `SnowBallField` packet constants | Battlefield maps depend on a team-scoring field runtime rather than only ordinary combat rules, and the client keeps both the scoreboard window creation and `SetUserTeam`-owned avatar presentation inside `CField_Battlefield`; the simulator now mirrors that ownership end-to-end by pushing the same WZ-backed Battlefield team presets through both the local player path and the shared remote-user actor owner instead of leaving remote users visually stale after packet-owned team changes | `SpecialEffectFields.cs`, `BattlefieldField`, `RemoteUserActorPool.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `MapSimulator.Content.cs`, `MapSimulator.UpdateLoop.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `EquipUIBigBang.cs`, `MinimapUI.cs`, `UnitTest_MapSimulator/BattlefieldFieldTests.cs` (`CField_Battlefield::OnClock`, `CField_Battlefield::OnScoreUpdate`, `CField_Battlefield::OnTeamChanged`, `CField_Battlefield::SetUserTeam`, `CScoreboard_Battlefield::OnCreate`) |
| Partial | Mu Lung Dojo field flow | The simulator still binds `FIELDTYPE_DOJANG` plus `MuruengRaid` maps into `DojoField`, mirrors the client-owned type-2 Dojo timerboard and HUD layers, replays the WZ-backed stage/clear/time-over presentations with delayed transfer ownership, keeps `dojang_next` map+portal propagation through `SpecialFieldRuntimeCoordinator` and `QueueMapTransfer`, and keeps the existing opcode-`163` relay unwrap and loopback `packetclientraw` bridge path. This pass keeps ownership in the same bridge seam while tightening relay-contract parity and source-backed verification: `DojoOfficialSessionBridgeManager` continues to decode direct field-specific relay-prefix payloads plus both nested relay-prefix shapes (`149 -> Dojo payload` and `163 -> 149 -> Dojo payload`), normalizes all of them into the same `RawPacket` dispatch path, and only persists learned opcode mappings when the opcode is not one of the shared wrappers. WZ was re-checked first in the current export and still pins the runtime anchors used by this seam (`Map/Map/Map9/925020100/info/returnMap=925020002`, `forcedReturn=925020002`, `mapMark=MuruengRaid`, `fieldType=14`, `portal/8/script=dojang_next`, `portal/8/pn=out001`, plus `UI/UIWindow.img/muruengRaid/clock/0/origin=(102,26)`), and IDA was re-checked second against the same client owners (`CField_Dojang::Init` `0x54ffa0`, `CField_Dojang::Update` `0x54ef10`, `CField_Dojang::OnClock` `0x550940`, `CField::OnPacket` `0x546d50`, `CField::OnFieldSpecificData` `0x52a7e0`): `OnClock` still gates timerboard creation/update on clock type `2`, while `OnPacket` still keeps shared wrapper ownership on opcodes `149` and `163`. Focused source coverage is now present on disk in `UnitTest_MapSimulator/FieldSpecificDojoRelayParityTests.cs` and `UnitTest_MapSimulator/DojoOfficialSessionBridgeNestedRelayParityTests.cs`, including explicit checks that shared wrapper opcodes (`149`, `163`) decode Dojo relay contracts without memoized mapping while non-wrapper recovered opcodes are memoized through the same nested-relay contract. The remaining gap stays narrow and specific: the simulator still has not recovered the full official Dojo opcode table byte-for-byte beyond the confirmed clock and wrapper-prefix-owned payload contracts, still does not reproduce the complete server-authored next-floor choreography byte-for-byte, and still cannot decrypt/inject an already-established MapleStory socket pair in place without the localhost proxy reconnect hop first. | Dojo parity remains timer-owned and wrapper-owned, and this row now carries verified source coverage for direct field-specific relay-prefix decode plus both nested relay-prefix shapes in the existing bridge seam, including shared-wrapper non-persistence and non-wrapper opcode memoization behavior. Remaining work is no longer missing HUD ownership, `dojang_next` propagation, delayed transfer ownership, shared-opcode relay unwrapping, dropped opcode-framed loopback dispatch, or missing in-repo relay regression coverage; it is the unrecovered full opcode table, byte-accurate floor-transition choreography, and true direct post-handshake socket attach/inject ownership. | `SpecialEffectFields.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.UpdateLoop.cs`, `DojoPacketInboxManager.cs`, `DojoOfficialSessionBridgeManager.cs`, `DojoField.cs`, `UnitTest_MapSimulator/FieldSpecificDojoRelayParityTests.cs`, `UnitTest_MapSimulator/DojoOfficialSessionBridgeNestedRelayParityTests.cs` (`CField_Dojang::Init`, `CField_Dojang::OnClock`, `CField_Dojang::Update`, `CField_Dojang::UpdateTimer`, `CField::OnPacket`, `CField::OnFieldSpecificData`) |
| Implemented | Cookie House event flow | Cookie House now binds `fieldType == CookieHouse` into `CookieHouseField`, mirrors the client-owned HUD geometry from `CField_CookieHouse::Init`, keeps the point layer cached in a dedicated `187x45` render target that is redrawn only when the context-owned point changes, and renders the WZ-backed `Map/Obj/etc.img/eventPointCount/backgrnd` plus the five-style digit root from `Map/Obj/etc.img/eventPointCount` without synthesizing sign canvases when the shipped digit-only map root lacks `plus` or `minus`. WZ re-checks against the current exported IMG filesystem still pin that root to a `187x45` background plus five digit containers and no sign children. IDA re-checks close the previous transport uncertainty: `CWvsContext::OnPacket` at `0x9e5830` dispatches packet `93` directly to `CWvsContext::OnSessionValue` at `0x9e41b0`, `OnSessionValue` reads `DecodeStr(key)` / `DecodeStr(value)`, compares the key to StringPool `0x11D9`, writes the parsed value into the same context point slot that `CField_CookieHouse::Update` at `0x54dc50` polls, and then `CField_CookieHouse::_UpdatePoint` at `0x54daa0` redraws the cached layer from `m_pCanvasBg` and `FindGrade_(m_nPoint)`. The simulator now pins StringPool `0x11D9` to the Cookie House session-value key, treats opcode `93` as the version-pinned default session-value point carrier, decodes opcode `93` Maple-string session payloads into the same score-owned update seam as loopback/raw context packets, keeps the recovered raw `opcode + 4-byte CWvsContext+0x4148` candidate inference only as a fallback/manual mapping path, suppresses opcode `93` from ambiguous raw-point inference, preserves invalid negative and over-three-digit rejection, and lets `/cookiepoint session start` plus `startauto` pass `listenPort 0` through to the existing bridge so the OS-selected proxy-port contract is exposed at the command layer. Focused source tests in `UnitTest_MapSimulator/CookieHouseParityTests.cs` now cover the built-in opcode `93` mapping, opcode `93` session-value decode, the pinned StringPool `0x11D9` key, invalid session payload rejection, and auto-selected listen-port startup in addition to the existing score/HUD decode coverage. | This row no longer depends on live-traffic inference for the client/server-owned Cookie House point owner. The simulator carries the recovered WZ asset family, cached HUD redraw ownership, grade table, digit/sign behavior, context-polled update path, StringPool-backed session-value key, and packet `93` session-value ingress in the existing Cookie House seams; raw 4-byte opcode inference remains available only as compatibility tooling for unknown exports or non-v95 traffic rather than a parity blocker. | `CookieHouseField.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `CharacterData.cs`, `MapleStoryStringPool.cs`, `CookieHousePointInboxManager.cs`, `CookieHouseOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/CookieHouseParityTests.cs` (`CWvsContext::OnPacket`, `CWvsContext::OnSessionValue`, `CField_CookieHouse::_UpdatePoint`, `CField_CookieHouse::Init`, `CField_CookieHouse::Update`, `CBitmapNumber::CBitmapNumber`, `CBitmapNumber::Draw`, ``anonymous namespace'::FindGrade_``) |
| Partial | Monster Carnival field flow | The simulator now detects Monster Carnival maps through the backlog coordinator, loads the map's WZ-backed Carnival definition into a dedicated runtime, enriches carnival mob entries with WZ-backed `getCP` and revive-chain metadata, pulls the map's `mobGenPos`, `guardianGenPos`, `reactorRed`, and `reactorBlue` data into that same definition, enforces local CP spending plus mob or guardian generation caps when requests succeed, clears prior summon or guardian state on each new `OnEnter` round like the client-owned UI reset path, rejects duplicate guardian requests through the client's dedicated occupied-slot failure reason, records local successful mob summons against the next WZ-backed mob spawn slot while recording guardian placements against their WZ-backed guardian slot plus the team-owned reactor id, reconciles packet-owned summon counts into live spawn-slot state on `Enter` or summoned-count updates, awards WZ-backed mob `getCP` plus revive-chain spawns through an explicit mob-defeat seam, tracks guardian reactor hits and destruction through a dedicated reactor-hit seam that frees occupied slots, switches Carnival team labeling to Red-vs-Blue ownership that matches the WZ `reactorRed` or `reactorBlue` split, mirrors the client-owned StringPool routing for request-failure (`0x101B`-`0x101F`), death (`0x1019`/`0x101A` with team labels `0x1017`/`0x1018`), member-out (`0x1029`/`0x102A`), and result (`0x1020`-`0x1023`) messages through the existing runtime seam, and still reconciles guardian placements into real spawned `ReactorPool` entries so Carnival guardian reactors use the shared live reactor update, render, and attack-hit lanes instead of remaining logical-only placements. This pass keeps the same WZ and client evidence anchors while narrowing the transport gap through the existing bridge seam instead of inventing a new owner: the `/mcarnival session` command surface still exposes the already-established-socket attach path next to `discover`, `start`, `startauto`, `map`, `unmap`, `clearmap`, `recent`, and `stop`, `start` plus `startauto` now accept `listenPort 0` and preserve the OS-selected localhost proxy port in bridge status and target matching, and `/mcarnival request` now also prefers that same official-session seam whenever a live or armed proxy target exists. The bridge now builds the client-owned outbound opcode `262` request shape directly (`tab` byte plus `entryIndex` dword), injects it immediately when Maple crypto is already initialized, queues it when the localhost proxy is armed but the reconnect handshake has not finished yet, flushes those queued requests on post-handshake init, and refuses to fall back to simulator-local CP or summon mutation while that live transport seam is active; passive established-session attach still stays status-only and returns the same explicit cannot-inject-after-handshake guidance instead of pretending the request succeeded locally. The guardian-reactor fidelity work from the prior pass remains in place: when a guardian placement is reconciled into a live `ReactorPool` entry, the spawned runtime data still inherits the WZ-backed required-hit count plus any already-known hit progress from the Carnival placement, and the `SetOnReactorHit` / `SetOnReactorDestroyed` callbacks still feed that authored progress back into `MonsterCarnivalField.TryApplyGuardianReactorHit(...)` instead of overwriting every guardian with `requiredHits = 1` on first contact. The client-owned Monster Carnival StringPool block also remains explicitly pinned through `MapleStoryStringPool` for ids `0x1017`-`0x1033`, so the team labels, failure/result text, and HUD asset UOLs that this field consumes no longer depend on generated-table ordering. WZ re-checks for this row stay anchored to the client-owned `UI/UIWindow.img/MonsterCarnival` asset family instead of simulator-only placeholders, and the runtime continues to consume that evidence directly: `backgrnd` still resolves to `162x107`, `backgrnd2` still resolves to `127x107`, `backgrnd3` still splits into `top`, `middle0`, `middle1`, and `bottom`, `BtSide` still exposes `normal` plus `disabled`, and `Tab` still exposes distinct `enabled` and `disabled` frames sized `21`, `33`, and `45` for the mob, skill, and guardian tabs. IDA re-checks also kept the packet owner unchanged: `CField_MonsterCarnival::OnPacket` at `0x55bba0` still dispatches only `346` through `353` into `OnEnter`, `OnPersonalCP`, `OnTeamCP`, `OnRequestResult`, `OnProcessForDeath`, `OnShowMemberOutMsg`, and `OnShowGameResult`, `CField_MonsterCarnival::OnRequestResult` at `0x55a890` still uses the same failure StringPool ids `0x101B`-`0x101F` and resets the local request-timer fields after either success or failure, and the client-originated request path still leaves outbound ownership on opcode `262`. Focused source coverage now exists in `UnitTest_MapSimulator/MonsterCarnivalOfficialSessionBridgeParityTests.cs` for auto-selected proxy-port queueing while the proxy is armed, passive established-session refusal, native `352` member-out relay through the recovered `346`-`353` family, and auto-select target matching. The remaining gap is narrower and still concentrated on deeper live-session recovery: passive established-session attach still cannot decrypt inbound `346`-`353` traffic or inject outbound opcode `262` after the Maple handshake, the simulator still does not decode any additional official Monster Carnival packet ownership beyond the recovered `346`-`353` client handlers, and the remaining guardian choreography is still not mirrored byte-for-byte once live sessions are attached. | Monster Carnival is a self-contained field mode with client-owned rules and UI, and this row now carries the map-owned spawn-slot and reactor metadata, packet-driven status-message selection, shared guardian-reactor integration, exact pinned client StringPool entries for the field-owned message/UI block, a WZ-backed Monster Carnival HUD shell instead of a simulator-only frame, and an official-session bridge surface that can now discover or attach live targets from `/mcarnival session`, auto-bind a localhost proxy with `listenPort 0`, and own outbound opcode `262` from `/mcarnival request` whenever a live or armed proxy target exists. This pass tightens the backlog around the real remaining seam boundary: request transport no longer falls back to local success while the live bridge is active, but decryptable post-handshake packet ownership and byte-accurate late guardian choreography still require deeper official-session recovery than the current bridge exposes. | `MonsterCarnivalField.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.cs`, `MonsterCarnivalPacketInboxManager.cs`, `MonsterCarnivalOfficialSessionBridgeManager.cs`, `ReactorPool.cs`, `MapleStoryStringPool.cs`, `UnitTest_MapSimulator/MonsterCarnivalGuardianParityTests.cs`, `UnitTest_MapSimulator/MonsterCarnivalOfficialSessionBridgeParityTests.cs` (`CField_MonsterCarnival::CreateUIWindow`, `CField_MonsterCarnival::OnEnter`, `CField_MonsterCarnival::OnRequestResult`, `CField_MonsterCarnival::OnShowGameResult`, `CField_MonsterCarnival::OnProcessForDeath`, `CField_MonsterCarnival::OnShowMemberOutMsg`) |
| Implemented | Party Raid field flow | The simulator now binds `FIELDTYPE_PARTYRAID`, `FIELDTYPE_PARTYRAID_BOSS`, and `FIELDTYPE_PARTYRAID_RESULT` into a dedicated `PartyRaidField` runtime, renders the field, boss, and result HUDs from the client WZ asset roots (`UI/UIWindow.img/PartyRace/*`, `UI/UIWindow.img/DualMobGauge/*`, `Map/Effect.img/praid/*`), fixes the field and boss point-vs-stage board placement to match the `PartyRace/Stage/backgrd` labels, mirrors the client's team-colored stage-state art, boss red-vs-blue gauge fill, map-owned clear or timeout result effect, and result summary overlay positions, infers win-vs-fail result mode from the result maps' `info/onUserEnter` scripts (`PRaid_WinEnter`, `PRaid_FailEnter`), constrains the result panel to the map's `LBSide/LBTop/LBBottom` safe area, carries a type-2 timer lifecycle through the runtime, and swaps the old plain text countdown for the client `Map/Obj/etc.img/partyRace` timerboard with bitmap digits and separator. The ingress seam now matches the recovered client contract instead of stopping at manual `field|party|session|clock` text updates: `PartyRaidField.TryApplyRawPacket` decodes the client key/value packet layout that `CWvsContext::OnSessionValue` at `0x9e41b0`, `CWvsContext::OnPartyValue` at `0x9e4720`, and the Party Raid field-owner path expect (`DecodeStr(key)` + `DecodeStr(value)`), applies raw Party Raid packet families `93`, `94`, and `95` into the existing session/party/field handlers, and accepts packet `149` field-specific Maple-string-pair payloads into the existing owner-hinted `PRaid_Team`, `Red_Stage` / `Blue_Stage`, `PRaid_ElapssedTimeAtBoss`, and result-value seams. The Party Raid loopback inbox parses and dispatches both direct wrapper payload lines (`packet 93|94|95|149|163`) and opcode-framed `packetclientraw` traffic, while `PartyRaidOfficialSessionBridgeManager` proxies a live Maple session, forwards the server stream to the client, decrypts inbound Party Raid traffic in-process, and preserves the explicit current-wrapper relay at `CField::OnPacket` opcode `163` in addition to the recovered `93` / `94` / `95` / `149` payload family. WZ evidence remains anchored to the Party Raid map scripts (`923020100/info/onUserEnter=PRaid_W_Enter`, `923020200/info/link=923020100`, `923020010/info/onUserEnter=PRaid_WinEnter`, `923020020/info/onUserEnter=PRaid_FailEnter`), and IDA re-checks close the charge-owner uncertainty instead of leaving it inferred: `CField_HuntingAdballoon::OnFieldSetVariable` at `0x552050` explicitly consumes string-pool ids `0x174D` / `0x174E` (`red_Charge` / `blue_Charge`) for the two charge strips, while `CField_PartyRaidBoss::OnFieldSetVariable` keeps ownership of `0x1ACE` / `0x1AA3` (`redTeamDamage` / `blueTeamDamage`) and the `9700037` max-HP gauge math. Focused regression coverage is now on disk in `UnitTest_MapSimulator/PartyRaidOfficialSessionBridgeParityTests.cs` for wrapper-relay decode and Party Raid live-bridge/raw-inbox opcode acceptance. | Party Raid now covers the field battery surface, field board, client-horizontal mine-vs-other stage-state movement, boss gauge, result overlays, client timerboard, script-backed outcome selection, map-safe result placement, the recovered Party Raid literals for team/stage/boss time/result values, a client-shaped cached boss gauge layer owner instead of frame-by-frame ad hoc composition, key-based plus raw packet-shaped loopback ingress, the explicit current-wrapper relay at opcode `163`, and the live official-session bridge for the recovered Party Raid packet family. This row is complete within the currently recovered client contract. | `SpecialFieldRuntimeCoordinator.cs`, `PartyRaidField.cs`, `MapSimulator.PacketOwnedFieldStateParity.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `PartyRaidPacketInboxManager.cs`, `PartyRaidOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/PartyRaidOfficialSessionBridgeParityTests.cs` (`CField::OnPacket`, `CWvsContext::OnSessionValue`, `CWvsContext::OnPartyValue`, `CField::OnFieldSpecificData`, `CField_PartyRaid::_UpdatePoint`, `CField_PartyRaid::Update`, `CField_PartyRaid::Init`, `CField_PartyRaidBoss::Init`, `CField_PartyRaidBoss::OnFieldSetVariable`, `CField_PartyRaidBoss::OnPartyValue`, `CField_PartyRaidResult::Init`, `CField_PartyRaidResult::OnSessionValue`, `CField_HuntingAdballoon::OnFieldSetVariable`) |
| Implemented | SpaceGAGA timerboard flow | The simulator now binds `FIELDTYPE_SPACEGAGA` maps into a dedicated `SpaceGagaField`, mirrors the client's clock-type-2 timerboard ownership, keeps the board at the verified center-top window origin, removes the invented title banner, pins the timerboard source to `Map/Obj/etc.img/space` so the board comes from `space/backgrnd` and the bitmap timer digits come from `space/fontTime`, and no longer falls back to broad `UIWindow(.2).img` shape scans before dropping to the older procedural board or SpriteFont path. In this pass, WZ re-checks against the exported IMG filesystem kept that `space` subtree pinned to the exact `backgrnd` canvas plus `fontTime` digit and `comma` canvases, IDA re-checks kept the timer-font seam pinned to `CTimerboard_SpaceGAGA::GetFontTime` at `0x5629f0`, and direct `MapleStory.exe` table extraction closed the last StringPool gap: `ms_aString[0x140D]` was recovered as seed `0x44` plus encoded bytes `44 C3 06 CD 1C 7C 6C 18 C2 08 93 3B 4A 0F 27 50 31 FD 17 DC 50 56 21 10 8C 0E 8C 3F 16 08 2E`, which decodes to `Map/Obj/etc.img/space/backgrnd` through `CTimerboard_SpaceGAGA::OnCreate`, and `ms_aString[0x140E]` was recovered as seed `0x45` plus encoded bytes `45 51 AE 0A 49 29 7E 8F F5 BE BA D3 E6 A5 F9 09 12 6F BF 1B 05 03 33 83 B5 B5 BA E4 A1 A1 F1`, which decodes to `Map/Obj/etc.img/space/fontTime` through `GetFontTime`. The `/spacegaga` status seam now reports those byte-accurate client entries instead of inferred-only mappings, and focused `SpaceGagaField` tests on disk cover type-2 clock ownership, ignored non-type-2 clocks, reset behavior, and the recovered StringPool evidence through that runtime seam | This is another example of client-owned special field logic that would otherwise be lost under generic map handling, and the simulator now carries the SpaceGAGA timerboard end-to-end with fixed WZ-backed art, verified ownership for both client StringPool ids, and byte-accurate recovered `ms_aString` evidence rather than inferred-only path mapping | `SpecialEffectFields.cs`, `UnitTest_MapSimulator/SpaceGagaFieldTests.cs`, `SpaceGagaField` (`CField_SpaceGAGA::OnClock`, `CTimerboard_SpaceGAGA::Draw`, `CTimerboard_SpaceGAGA::OnCreate`, `CTimerboard_SpaceGAGA::GetFontTime`, `StringPool::GetString`) |

### 2. Additional packet-owned microgame dialog discovered by follow-up IDA scan

The broader packet-owner pass in backlog 7 already surfaced `CRPSGameDlg::OnPacket`, but it is still missing from the dedicated minigame plan even though it fits this area more closely than the generic field-utility bucket.
A targeted decompile now makes that owner more concrete: `CRPSGameDlg::OnCreate` is the dialog-create seam, and `CRPSGameDlg::OnPacket` is not a single opaque callback. Packet subtype `8` first enforces the unique-modeless owner gate, decodes a `uint32` value, opens a `CUtilDlgEx` notice using `StringPool` id `0xE83`, and only then constructs the dialog owner; subtypes `6`, `7`, `9`, `10`, `11`, `12`, and `14` require an already-live `CRPSGameDlg` unique-modeless owner and forward into `ProcessPacket`; and subtype `13` tears the owner down through `CWnd::Destroy`. The same `ProcessPacket` decompile also shows more exact gameplay ownership than the backlog was tracking: `9` and `12` start a fresh round by clearing NPC choice, enabling the three RPS buttons, setting the switch cadence to `120`, and arming a `30000` ms time limit; `11` decodes the NPC pick plus straight-win count and marks the compensation flag when the decoded count goes negative while the current streak is still zero; `10` forces immediate result presentation by clearing the switch cadence, setting the streak to `-1`, and calling `ShowResult`; and `6`/`7`/`14` reset the round state, re-enable the main button, and route the completion notice through `StringPool` ids `3724` or `3723`.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Rock-Paper-Scissors dialog parity | `HaCreator/MapSimulator` still carries both sides of the recovered `CRPSGameDlg` ownership instead of only the server-owned packet half, and this pass stays in the live-session verification seam rather than changing owner logic again. `Fields/RockPaperScissorsField.cs`, `MapSimulator.RockPaperScissorsParity.cs`, `Managers/RockPaperScissorsPacketInboxManager.cs`, `Managers/RockPaperScissorsClientPacketTransportManager.cs`, `Managers/RockPaperScissorsOfficialSessionBridgeManager.cs`, `RockPaperScissorsSessionCommandParsing.cs`, and the load/update hooks in `MapSimulator.Content.cs` / `MapSimulator.UpdateLoop.cs` still mirror the targeted `CRPSGameDlg::OnPacket` / `ProcessPacket` / `Update` / button-click shape recovered from IDA: subtype `8` still requires that no other unique-modeless owner is active, decodes the `uint32` entry payload, queues the `StringPool` `0xE83` notice, and opens the local dialog owner; subtypes `6`, `7`, `9`, `10`, `11`, `12`, and `14` still require that the active owner is already `CRPSGameDlg`; subtype `13` still destroys that owner; `9` and `12` still start a live round by clearing the NPC pick, enabling the three RPS buttons, setting the `120` ms switch cadence, and arming the `30000` ms round limit; `11` still decodes the NPC pick plus straight-win count and derives the compensation flag when the decoded streak goes negative while the previous streak was `0`; `10` still forces result presentation; and `6` / `7` / `14` still reset the round state and route completion notices through `3724` / `3723`. The client-owned timeout/button path still runs through both transport seams instead of only the simulator-local outbox: timeout, start, continue, retry, exit, and choice packets still serialize as opcode `160`, `MapSimulator.UpdateLoop.cs` still keeps the official-session bridge alive during play, refreshes discovery, drains bridged opcode-`371` owner packets, and prefers the live bridge whenever an attached Maple client/session is present before falling back to the `packetoutraw` loopback outbox. This pass tightens verification inside that same bridge seam: `/rps session` now surfaces outbound bridge history controls (`recent [count]` and `clearrecent`) that were already tracked internally by `RockPaperScissorsOfficialSessionBridgeManager`, so proxied opcode-`160` observations can be reviewed and reset directly from the command layer without adding a new owner; passive established-session attach and reconnect guidance remain explicit in discovery/status text. Focused source coverage in `UnitTest_MapSimulator/RockPaperScissorsParityTests.cs` now also covers proxy-armed passive attach with auto-selected listen port (`0`) queueing deferred opcode `160`, plus outbound opcode-`160` trace-shape parsing for valid start/select packets and rejection of malformed select/non-select payload shapes, in addition to the existing passive-attach guidance, bridge preference, and bridged opcode-`371` inbound parse/filter path. A fresh WZ re-check against the loaded export kept the dialog art and geometry anchored to current data: `UI/UIWindow2.img/RpsGame` still exposes `backgrnd` `310x358`, the `win` / `lose` / `draw` / `timeover` result canvases, `charWin` / `charLose`, the three choice canvases plus `Frock` / `Fpaper` / `Fscissor`, and the `BtStart` / `BtContinue` / `BtRetry` / `BtExit` plus `BtRock` / `BtPaper` / `BtScissor` button families; `CRPSGameDlg::OnCreate` at `0x6d7480` still pins the same main/exit/choice button ids and placement the simulator uses (`3000` at `108,202`, `3003` at `242,335`, and `2000` / `2001` / `2002` at `19,104`, `60,104`, and `101,104`). The remaining parity gap is now narrowed to live ownership validation, not local seam shape: the simulator still needs a verified full proxied reconnect round trip against a live Maple session that demonstrates real `CRPSGameDlg` opcode `160` outbound packets and opcode `371` inbound packets end-to-end through the initialized bridge. | This owner is still modeled as a full dialog seam instead of a packet-only stub, and the remaining work is now squarely on live-session validation rather than more local owner reconstruction. MapSimulator has the recovered create/destroy gate, packet decoder, result timing, notice path, client timeout/button packet emission, tip scroll/formatting rules, loopback fallback, bridged session preference, passive established-session attach for status-only verification setup, outbound history inspection/reset controls, and focused source tests for both bridge parsing and proxy-armed deferred queueing/trace-shape validation. | packet-owned microgame dialog layer (`HaCreator/MapSimulator/Fields/RockPaperScissorsField.cs`, `HaCreator/MapSimulator/MapSimulator.RockPaperScissorsParity.cs`, `HaCreator/MapSimulator/RockPaperScissorsSessionCommandParsing.cs`, `HaCreator/MapSimulator/Managers/RockPaperScissorsPacketInboxManager.cs`, `HaCreator/MapSimulator/Managers/RockPaperScissorsClientPacketTransportManager.cs`, `HaCreator/MapSimulator/Managers/RockPaperScissorsOfficialSessionBridgeManager.cs`, `HaCreator/MapSimulator/MapSimulator.Content.cs`, `HaCreator/MapSimulator/MapSimulator.UpdateLoop.cs`, `UnitTest_MapSimulator/RockPaperScissorsParityTests.cs`, `CRPSGameDlg::OnPacket`, `CRPSGameDlg::ProcessPacket`, `CRPSGameDlg::OnCreate`, `CRPSGameDlg::ShowResult`, `CRPSGameDlg::Update`, `CRPSGameDlg::OnBtStart`, `CRPSGameDlg::OnBtContinue`, `CRPSGameDlg::OnBtRetry`, `CRPSGameDlg::OnBtExit`, `CRPSGameDlg::SendSelection`, `CUtilDlgEx`) |

### 3. Additional client-owned field wrappers discovered by IDA function scan

The live IDA `list_functions` scan surfaced more field-owner classes with no hits anywhere else in the current backlog set.
These started as scan-only seams, but a follow-up pass against the current simulator code shows that some of them are now represented locally and should no longer stay marked as missing just because the older backlog text was never refreshed.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Transit and voyage field wrappers | The simulator still treats `shipObj` as a client-owned map contract instead of depending only on editor `ShipObject` instances: `TransportationFieldDefinitionLoader` reads copied WZ `shipObj` data from `MapInfo.additionalNonInfoProps`, normalizes both `Map/Obj/vehicle.img/...` and `Map/Obj/vehicle/...` object paths, and lets `DetectAndInitializeTransportField` initialize transit and Balrog voyage maps from the same map-owned payload the client uses. WZ re-checks against the exported IMG filesystem in this pass kept those transport contracts anchored to the same client-owned map data instead of simulator defaults: regular transit map `Map/Map1/101000300` still carries `shipObj/x=1545`, `x0=2100`, `y=-195`, `shipKind=0`, `f=0`, `tMove=15`, and ship art path `Map/Obj/vehicle.img/ship/ossyria/99`, while Balrog wrapper maps `Map/Map2/200090000` and `Map/Map2/200110000` still carry `shipKind=1`, `tMove=2`, and ship art path `Map/Obj/vehicle/ship/ossyria/97` with opposite dock positions and flip values (`x=-590`, `f=1` versus `x=485`, `f=0`). IDA re-checks also stayed aligned with the runtime branches tightened in this pass: `CField_ContiMove::OnContiMove` still dispatches only subtypes `8`, `10`, and `12`; `CField_ContiMove::OnStartShipMoveField` still only reacts to byte `2`; `CField_ContiMove::OnMoveField` still only reacts to bytes `4` and `5`; `CField_ContiMove::OnEndShipMoveField` still only reacts to byte `6`; `CField_ContiMove::OnContiState` still routes state values `0/1/6` to `EnterShipMove`, `2/5` to `LeaveShipMove`, and `3/4` to either `AppearShip` for Balrog ship-kind `1` when the second byte is `1` or `LeaveShipMove` for regular ship-kind `0`; and `CField_Balrog::GetFieldType` still resolves to field type `18`. `TransportationField` still applies a client-owned initial wrapper state so regular `FIELDTYPE_CONTIMOVE` routes start docked at `x` instead of idling offscreen at `x0`, while Balrog-owned wrappers stay hidden until their appear branch runs, and the packet-result seam still filters regular-vs-Balrog branches the way the recovered client ownership split expects. This pass tightened the still-local voyage Balrog event seam inside that same transport-owned runtime instead of leaving it hardcoded to one side: the regular-ship attack choreography now follows the authored transport `f` contract the same way the Balrog wrapper `AppearShip` branch already does, so the local Balrog approach, hover anchor, retreat side, draw flip, and visible status all pivot with the route-owned left-vs-right wrapper contract instead of always spawning from one fixed right-side lane; the existing local command seam is now also reachable from the registered transport surface through `/transport voyagebalrog ...` instead of living as an unreachable helper. The live packet seam remains tighter than before inside the same row seam: alongside `TransportationPacketInboxManager`, the simulator has a wired `TransportationOfficialSessionBridgeManager`, the update loop now actually drains both inbox and proxied live-session packets through the same transport-owned dispatcher, `/transport` root status now surfaces the current field-init request summary next to inbox and bridge state, `/transport session start ...` and `/transport session startauto ...` now automatically mirror the active wrapper map's field-init request into the armed bridge instead of requiring a separate manual `sendinit` or `queueinit`, manual `sendinit`/`queueinit` now preserves the active wrapper map field id and ship-kind when no override is supplied instead of zeroing the field id, the official-session bridge now decodes that client-owned outbound contract in its own status and outbound history with named transit-vs-Balrog ship-kind labels instead of only showing opaque opcode `264` hex, and the same bridge command seam now exposes captured-raw fallback controls for unrecovered outbound families through `/transport session queue <historyIndex>`, `sendraw <hex>`, and `queueraw <hex>` while accepting captured hex with whitespace or hyphen separators plus captured-history snippets such as `raw=...`, `packetraw ...`, or `packetoutraw ...`. Focused transport parity tests are now on disk in `UnitTest_MapSimulator/TransportationParityTests.cs` for route-flip-aware voyage Balrog approach selection, captured outbound raw parsing (including captured-history `raw=` and `packetraw`/`packetoutraw` prefixes), deferred queueing from recent outbound history, deferred queueing from observed outbound-history snapshots, and client-branch-aware transport packet descriptions. The remaining gap is narrower and explicit: the simulator still does not attach in place to an already-established Maple client socket pair without a localhost proxy hop first, it still has no recovered distinct client-owned outbound voyage/Balrog request family beyond the field-init contract plus replaying, sending, or queueing captured raw packets, and the separate voyage-attack/server-timed Balrog event choreography is still a local runtime owner rather than a recovered live packet owner even though that local owner now respects the route-authored flip contract. | Ship travel and Balrog transport are client-owned field experiences with their own packet/state choreography, and this row now covers the WZ-authored route contract, transport-specific branch gating that matches the recovered regular-vs-Balrog ownership split more closely, the dedicated loopback inbox, an in-process official-session proxy for the recovered `CField_ContiMove` packet family, replayable, sendable, and queueable captured outbound traffic inside that same live bridge, an auto-mirrored transport field-init request when the proxy is armed on an already-active wrapper map, decoded field-init visibility in bridge status/history with explicit transit-vs-Balrog labeling, active-map send-init override preservation, a registered voyage-Balrog local control seam, route-flip-aware local Balrog attack staging for regular transit wrappers, and row-specific parity tests for both the local choreography contract and captured/raw outbound packet handling. The remaining work is no longer whether the simulator can read client-owned `shipObj` payloads, identify route art paths, apply the initial wrapper state, filter the recovered packet branches by wrapper kind, expose the field-init request controls, decode the recovered field-init request contract, own live inbound `164`/`165` traffic at all, or keep the local voyage Balrog event aligned with the map-owned left-vs-right route contract; it is specifically true in-place socket attachment without a proxy hop plus any additional outbound voyage/Balrog event ownership that the server drives outside the recovered transport packet family. | transit or voyage field runtime (`MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.ClientOwnedFieldWrappers.cs`, `TransportationField.cs`, `TransportationFieldDefinitionLoader.cs`, `TransportationPacketInboxManager.cs`, `TransportationOfficialSessionBridgeManager.cs`, `TransportationFieldInitRequestCodec.cs`, `UnitTest_MapSimulator/TransportationParityTests.cs`, `CField_ContiMove`, `CField_Balrog`) |
| Partial | Tutorial, camera-mask, and presentation wrappers | The simulator still carries the tutorial, limited-view, and wedding-photo wrapper seams surfaced by the IDA scan, and this pass tightened runtime behavior inside those existing owners again instead of widening scope. Generic tutorial maps still mirror `CField_Tutorial::DecodeFieldSpecificData` by forcing the recovered appearance pair (`1002562` cap and `1052081` longcoat) while preserving the active weapon slot, Aran tutorial maps still keep their differentiated WZ-backed `user/*/look` path under the explicit `CField_AranTutorial::GetFieldType = 22` contract, and tutorial/wedding-photo contract resolution still follows authored `info/link` aliases before reading map-owned wrapper metadata. Limited-view maps still follow the recovered `CField_LimitedView::Init` / `DrawViewrange` contract by loading `Effect/MapEff.img/Viewrange/0`, building the `1024x768` dark layer at `(-512,-468)`, and mutating `m_lpPrev` in the recovered clear-then-reappend order while feeding local plus visible remote-user world positions into the mask plan. Wedding-photo ownership moved further into live packet-driven presentation in this pass: `MapSimulator.ClientOwnedFieldWrappers.cs` still binds direct `fieldType = 61` owners and WZ-authored `pledgeEnter` owners in the `6800007xx` / `6800008xx` region, `WeddingField` still displaces stale packet-owned ceremony state when the photo scene owner binds, and photo-scene packet choreography now advances inside that dedicated owner instead of stopping at owner-bind plus packet trail bookkeeping. `OnWeddingProgress` now transitions ceremony text/card/celebration layers for photo-scene owners even when no altar NPC contract exists on those maps, keeps dialog creation gated to altar-NPC maps only, and preserves the WZ-authored photo-scene BGM contract by not replacing it with the ceremony opening BGM when only the photo-scene owner is active. `OnWeddingCeremonyEnd` now promotes photo-scene card/celebration layers before starting bless effects, and `DescribeWeddingPhotoScenePresentationState()` now reports the active layer state alongside packet-trail ownership. Focused source coverage is now on disk in `UnitTest_MapSimulator/ClientOwnedFieldWrapperParityTests.cs` for photo-scene step-layer transitions without NPC dialogs, photo-scene ceremony-end promotion of card/celebration plus bless-effect activation, photo-scene BGM preservation on step-0 progress, and the limited-view clear-then-reappend operation ordering contract. The remaining gap is narrower and still runtime-specific: the simulator now performs packet-driven photo-scene layer transitions within the dedicated `CField_WeddingPhoto` owner, but it still does not reproduce unrecovered photo-specific packet/dialog/layer choreography beyond this shared ceremony-layer path, and `CField_LimitedView` is still short of the client's full COM canvas/layer invocation details even though operation order and `m_lpPrev` bookkeeping are aligned more closely. | This row now tracks explicit owner behavior rather than wrapper classification: Aran tutorial maps keep their distinct WZ-authored appearance contract, linked map aliases resolve through the same WZ roots the client reads for tutorial and wedding-photo wrappers, limited-view ownership still covers local plus remote focus masks with recovered clear/reappend history ordering, and wedding pledge/photo scene maps now run packet-driven layer transitions through a dedicated `CField_WeddingPhoto` owner that also clears stale packet-owned ceremony state before rebinding. It remains `Partial` because remaining work is the deeper unrecovered photo-scene packet/dialog/layer choreography and the last COM-level limited-view canvas sequencing details, not wrapper detection, linked-contract resolution, tutorial appearance selection, limited-view mask-history ordering, photo-scene BGM preservation, or scene-owner handoff plus step-driven layer transitions. | tutorial or scripted field layer (`MapSimulator.ClientOwnedFieldWrappers.cs`, `MapSimulator.UpdateLoop.cs`, `LimitedViewField.cs`, `WeddingField.cs`, `SpecialEffectFields.cs`, `UnitTest_MapSimulator/ClientOwnedFieldWrapperParityTests.cs`, `CField_Tutorial::DecodeFieldSpecificData`, `CField_AranTutorial::GetFieldType`, `CField_LimitedView::Init`, `CField_LimitedView::DrawViewrange`, `CField_NoDragon::Init`, `CField_WeddingPhoto::GetFieldType`) |
| Implemented | Dynamic foothold field-wrapper parity | The simulator now carries explicit `CField_DynamicFoothold` wrapper bookkeeping in the same client-owned wrapper seam as transport, tutorial, and limited-view fields instead of leaving dynamic maps implied by the broader moving-platform runtime. `MapSimulator.ClientOwnedFieldWrappers.cs` rebinds the owner on map load through `ConfigureDynamicFootholdFieldWrapper`, `IsDynamicFootholdWrapperMap` binds the native `fieldType = 29` contract, `Fields/DynamicFootholdField.cs` exposes the recovered client owner metadata (`CField::FieldFactory = 0x53F220`, `CField_DynamicFoothold::GetFieldType = 0x551020`, and the `Init` thunk at `0x551050`), follows `info/link` to a linked contract image when present, summarizes foothold-root and dynamic-object map data from WZ, and keeps packet-owned dynamic-object aliases separate from authored canonical names for later lookup. `DynamicFootholdSystem.ResetForClientOwnedWrapper()` also gives the wrapper its own rebinding seam by clearing simulator-owned platform state whenever the client-owned map owner changes. A fresh WZ re-check in this pass kept that contract anchored to current map data instead of simulator-only placeholders: `Map/Map0/003000100` still carries dynamic-tagged object nodes at `0/obj/0/dynamic`, `1/obj/0/dynamic`, `2/obj/0/dynamic`, `4/obj/0/dynamic`, and `5/obj/0/dynamic`, and the same sample object still resolves through `oS=acc9`, `l0=cannonTuto`, `l1=Ani`, `l2=4`, `piece=0`, `x=-701`, `y=302`. IDA re-checks also stayed aligned with the simulator seam: `CField_DynamicFoothold::GetFieldType` still returns `29`, and `CField_DynamicFoothold::Init` at `0x551050` still only forwards to `CField::Init`. Focused regression coverage is now on disk in `UnitTest_MapSimulator/DynamicFootholdFieldWrapperParityTests.cs` for explicit field-type activation, linked-contract resolution and missing-link fallback, wrapper-owned runtime reset, and dynamic-object alias bookkeeping. | This row no longer needs to stay open just to track whether dynamic-foothold maps are represented as their own client-owned field owner. The broader moving-platform behavior, passenger sync, and deeper packet choreography still belong to backlog 3, but the wrapper-family classification, field-type bind, init seam, rebinding, and WZ-backed contract bookkeeping for `CField_DynamicFoothold` are now explicit in the simulator. | dynamic-foothold field wrapper (`MapSimulator.ClientOwnedFieldWrappers.cs`, `HaCreator/MapSimulator/Fields/DynamicFootholdField.cs`, `HaCreator/MapSimulator/Fields/DynamicFoothold.cs`, `UnitTest_MapSimulator/DynamicFootholdFieldWrapperParityTests.cs`, `CField::FieldFactory`, `CField_DynamicFoothold::GetFieldType`, `CField_DynamicFoothold::Init`) |
| Implemented | Result-board and field-variable special wrappers | The missing IDA-owned wrapper surfaces are now covered in the simulator runtime: `CField_KillCount::OnPacket` packet `178` drives a dedicated kill-count overlay, `CField_HuntingAdballoon::OnFieldSetVariable` reuses the existing party-raid boss field-variable HUD with an explicit client-owned bind, `CField_MassacreResult::OnPacket` packet `174` now decodes the client rank/value payload into the existing Massacre result board, `CField_EscortResult::OnSessionValue` fail values arm a timed fail overlay, and `CField_ShowaBath::DecodeFieldSpecificData` now forces the gendered robe appearance recovered from the client and WZ (`1050100` / `1051098`). The field-state command surface also accepts the new packet ids and wrapper value injection for verification. | The result/status wrappers that contextualize these minigame endings are now represented as concrete simulator runtime owners instead of being left as untracked client-only behavior. | event result or field-variable layer (`CField_KillCount`, `CField_HuntingAdballoon`, `CField_MassacreResult`, `CField_EscortResult`, `CField_ShowaBath`) |
| Partial | Tournament and Monster Carnival variant wrappers | WZ re-checks against the exported IMG filesystem still anchor this row to the same client-owned seams instead of generic map tags: `Map/Map1/109070000` still carries `info/fieldType = 3`, `String/Map.img/victoria/109070000/help0` still describes the bracket lobby/rest/prize/five-minute exit flow, and the Carnival variant maps still expose the client-read `monsterCarnival/mapType` seam (`Map/Map9/980031000` fieldType `16` + `mapType = 0`, `980032000` + `mapType = 1`, `980033000` + `mapType = 2`). The same WZ pass still confirms the Season 2 HUD family and timer contract used by these wrappers: `UI/UIWindow2.img/MonsterCarnival` keeps `main/summonList/sub`, `UI/UIWindow.img/MonsterCarnival` keeps shared `backgrnd/backgrnd2/backgrnd3`, and Carnival map nodes keep `timeDefault/timeExpand/timeMessage/timeFinish` values that were already loaded but not yet owner-tracked. IDA anchors stay narrow and unchanged (`CField_MonsterCarnival*::Init/CreateUIWindow/OnEnter/OnRequestResult/OnShowGameResult`, `CField_Tournament::OnPacket/OnTournament/OnTournamentUEW`, `CMatchTableDlg::CMatchTableDlg`), and this pass pushes further inside those same owners instead of adding new ones: `MonsterCarnivalField` now keeps a concrete owned clock phase trail (`ActiveRound -> ExtendedRound -> ResultMessage -> ExitGrace -> Closed`) driven from the existing WZ `monsterCarnival/time*` seam, starts that timeline on `OnEnter`, re-roots it on `OnShowGameResult`, and exposes live countdown/phase summaries in status and wrapper headers; it also now surfaces the recovered `OnProcessForDeath -> CUIStatusBar::ChatLogAdd(type=7,item=-1)` route explicitly beside the existing request-failure/member-out/result ownership routes. Season 2 continues to keep exact `UIWindow2.img/MonsterCarnival/main/summonList/sub` ownership with recovered `SetUIData` row counts, `InsertSpelledData` preview, and sub-dialog request/death/result transitions, waiting-room remains wrapper-only with delegated result routing, and revive still keeps direct result-route ownership visible after death/result traffic. `TournamentField` keeps the same owned `374`-`378` packet family/session trail and now also preserves the decoded `OnTournament` branch byte in packet summaries/lifecycle text for both blocked-entry and round-result notices (matching the recovered branch split before notice dispatch), while retaining match-table state-byte ownership, prize-side dialog close, and UEW unknown-code empty-notice ownership. Focused source tests are now on disk in `UnitTest_MapSimulator/TournamentAndMonsterCarnivalVariantParityTests.cs` for Carnival owned-clock transitions, recovered Carnival death chat-route ownership, and Tournament branch-labeled notice summaries in addition to the existing row-local variant checks. Remaining gap is still deeper choreography beyond wrapper discovery: waiting-room still stops at wrapper-only status instead of the full client lobby/session surface, Season 2 still does not reproduce the full native dialog/timer transport and UI choreography beyond current `UIWindow2` ownership + recovered sub-dialog/chat/timer seams, revive still stops short of full round/session behavior beyond recovered death/request/result routes, and Tournament still does not reproduce the full bracket dialog/session transport lifecycle beyond owned packet decoding, branch-labeled notices, UEW empty/argument notice ownership, session trail, prize-side dialog close, exact ctor payload shape, and StringPool `0x752` title seam. | These variant wrappers remain explicit live-session owners in code and now carry more of the recovered per-owner choreography: Carnival variants keep rolling phase/action trails plus owned timer-phase tracking from `monsterCarnival/time*`, explicit request/death/result chat routes, and Season 2 sub-dialog/UIWindow2 ownership, while Tournament keeps concrete packet/session ownership over `374`-`378` with branch-aware notice summaries, match-table state-byte seams, prize-side dialog transitions, and UEW empty-notice behavior. Remaining work is now concentrated in deeper per-variant UI/dialog and transport choreography above these owned seams, not in rediscovering wrapper owners, `fieldType/mapType`, or recovered ctor/stringpool seams. | mode-variant field runtime (`MonsterCarnivalField.cs`, `TournamentField.cs`, `TournamentPacketInboxManager.cs`, `TournamentOfficialSessionBridgeManager.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/TournamentAndMonsterCarnivalVariantParityTests.cs`, `CField_MonsterCarnival::CreateUIWindow`, `CField_MonsterCarnival::OnEnter`, `CField_MonsterCarnival::OnRequestResult`, `CField_MonsterCarnival::OnProcessForDeath`, `CField_MonsterCarnivalRevive::OnShowGameResult`, `CField_MonsterCarnivalWaitingRoom::Init`, `CField_MonsterCarnivalS2_Game::Init`, `CField_MonsterCarnival::OnShowGameResult`, `CField_Tournament::OnPacket`, `CField_Tournament::OnTournament`, `CField_Tournament::OnTournamentMatchTable`, `CField_Tournament::OnTournamentSetPrize`, `CField_Tournament::OnTournamentUEW`, `CMatchTableDlg::CMatchTableDlg`) |
| Implemented | Tournament match-table dialog parity | `HaCreator/MapSimulator/Fields/TournamentField.cs` now carries a dedicated `TournamentMatchTableDialogState` owner behind Tournament packet `375`, so `CField_Tournament::OnTournamentMatchTable` is represented as its own dialog surface instead of being buried inside the generic field status panel. The simulator now mirrors the client-owned lifecycle more explicitly: the packet handoff opens a `CMatchTableDlg`-named dialog, the local draw path keeps the IDA-confirmed close/up/down button geometry and layer offsets from `CMatchTableDlg::OnCreate` (`0x784050`) and `CMatchTableDlg::Draw` (`0x797d20`), and the dialog is now interactive through mouse wheel, click-to-close, click-to-scroll, and `/tournament dialog ...` controls. | This row no longer needs to stay open just to track whether Tournament owns a second client dialog surface. Remaining differences are visual-fidelity follow-ups inside the existing dialog seam rather than missing ownership: the simulator still renders the bracket with local primitives and decoded text summaries instead of reproducing every client canvas/layer asset from the WZ/UI stack. | tournament bracket dialog layer (`CField_Tournament::OnTournamentMatchTable`, `CMatchTableDlg::OnCreate`, `CMatchTableDlg::Draw`) |
| Implemented | Shared current-field wrapper packet handoff parity | The simulator now keeps both packet-owned and non-packet current-wrapper ownership inside the same coordinator seam instead of splitting packet relay traffic from local wrapper shortcuts. `SpecialFieldRuntimeCoordinator.TryDispatchCurrentWrapperPacketRelay(...)` still normalizes direct recovered wrapper packet ids into the same opcode-`163` relay payload shape that the client re-check at `0x546d50` uses, decodes the wrapped payload, and hands it to the active owner with the owner message preserved in the dispatch result. The existing bridge and inbox coverage from the prior pass remains in place across `SpecialEffectFields` and `MinigameFields`: Wedding packet inbox messages, Ariant arena packet inbox messages, Coconut inbox and official-session bridge traffic, Snowball inbox and official-session bridge traffic, Monster Carnival inbox and official-session bridge traffic, Tournament inbox and official-session bridge traffic, Party Raid raw-packet inbox and official-session traffic, Dojo raw packet inbox handling, Massacre raw packet inbox handling, and Guild Boss transport/session traffic all still enter the active-wrapper seam before the concrete runtime consumes them. The field-state bypass from the prior pass also remains closed: direct field-state wrapper packets `174` (`CField_MassacreResult::OnPacket`) and `178` (`CField_KillCount::OnPacket`) route through the same coordinator relay path instead of a separate local shortcut. This pass closes the last ownership split that remained outside that seam: Showa Bath field-specific presentation handoff, Escort Result field-value ownership, Chaos Zakum session-value ownership, and Hunting Ad Balloon field-value ownership now all dispatch through `SpecialFieldRuntimeCoordinator` from `MapSimulator.PacketOwnedFieldStateParity.cs` and `MapSimulator.ClientOwnedFieldWrappers.cs` instead of being consumed by wrapper-specific local branches. Focused source coverage is now on disk in `UnitTest_MapSimulator/SpecialFieldRuntimeCoordinatorRelayTests.cs` for relay payload normalization, relay decode failure on malformed opcode-`163` payloads, the no-active-wrapper fallback message from the literal relay path, and owner-message dispatch for Escort Result, Chaos Zakum, Hunting Ad Balloon, and Showa Bath current-wrapper handoff. | This row now tracks the complete current-wrapper ownership seam in code rather than only a packet-helper abstraction. Packet traffic still reaches the active owner through the explicit opcode-`163` relay path, and the wrappers that do not expose recovered `OnPacket` handlers now enter the same coordinator-owned handoff through dedicated field-value, session-value, and field-specific presentation dispatch methods. That leaves no separate wrapper-consumption shortcut in `MapSimulator` for this row. | current special-field wrapper dispatch layer (`CField::OnPacket` at `0x546d50`, opcode-`163` current-wrapper relay, `HaCreator/MapSimulator/Fields/SpecialFieldRuntimeCoordinator.cs`, `HaCreator/MapSimulator/Effects/SpecialEffectFields.cs`, `HaCreator/MapSimulator/Fields/MinigameFields.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `HaCreator/MapSimulator/MapSimulator.ClientOwnedFieldWrappers.cs`, `HaCreator/MapSimulator/MapSimulator.PacketOwnedFieldStateParity.cs`, `UnitTest_MapSimulator/SpecialFieldRuntimeCoordinatorRelayTests.cs`, field-specific packet runtimes such as `WeddingField`, `TournamentField`, `MonsterCarnivalField`, `PartyRaidField`, `DojoField`, `AriantArenaField`, `SnowBallField`, `CoconutField`, `GuildBossField`, `MassacreField`) |

### 4. Additional Party Raid wrapper-family ownership confirmed by `CField::FieldFactory` re-check

The latest IDA re-check against `CField::FieldFactory` confirms that Party Raid is not a single special-field owner in the client.
The factory still instantiates three distinct wrapper classes, and targeted IDA lookup now pins the concrete `GetFieldType` owners too:

- `CField_PartyRaid::GetFieldType` at `0x55c440` for field type `25`
- `CField_PartyRaidBoss::GetFieldType` at `0x55c640` for field type `26`
- `CField_PartyRaidResult::GetFieldType` at `0x55c800` for field type `27`

That owner split was only implicit in the existing gameplay row, so it needs its own wrapper-family entry the same way Tournament, Carnival, transport, and dynamic-foothold do.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Explicit Party Raid wrapper-family parity | `PartyRaidField` now carries the native Party Raid wrapper-family split as a first-class contract instead of a generic owner bucket. The runtime reports `CField::FieldFactory` (`0x53F220`), the concrete `GetFieldType` owners, and the recovered `Init` entry point for each native client wrapper: `CField_PartyRaid` (`GetFieldType=0x55C440`, `fieldType=25`, `Init=0x55C3D0`), `CField_PartyRaidBoss` (`GetFieldType=0x55C640`, `fieldType=26`, `Init=0x55C5D0`), and `CField_PartyRaidResult` (`GetFieldType=0x55C800`, `fieldType=27`, `Init=0x55C7B0`). Wrapper-status output now names the active owner directly, and the visible contract text also carries the matching WZ evidence for each family branch: the live field owner resolves against `923020100` with `info/onUserEnter=PRaid_W_Enter`, the boss shell resolves through `923020200 -> 923020100`, and the result owner matches `923020010` / `923020020` where `info/fieldType=27` and `info/onUserEnter=PRaid_WinEnter` / `PRaid_FailEnter`. Packet-owned field-specific routing continues to resolve Party Raid keys through the concrete native owner label rather than the broader `PartyRaidField` bucket. | The simulator now exposes the same three-way Party Raid wrapper family that the client factory instantiates, with direct `GetFieldType` evidence plus WZ-backed contract data attached to the visible contract surface. That keeps future Party Raid work anchored to the actual `CField` family split instead of a blended runtime label and closes this wrapper-family ownership row. | Party Raid field-wrapper layer (`CField::FieldFactory`, `CField_PartyRaid::GetFieldType`, `CField_PartyRaid::Init`, `CField_PartyRaid::Update`, `CField_PartyRaidBoss::GetFieldType`, `CField_PartyRaidBoss::Init`, `CField_PartyRaidBoss::OnFieldSetVariable`, `CField_PartyRaidBoss::OnPartyValue`, `CField_PartyRaidResult::GetFieldType`, `CField_PartyRaidResult::Init`, `CField_PartyRaidResult::OnSessionValue`, `PartyRaidField.cs`) |

### 5. Cake Pie event timerboard and item-info owners discovered by follow-up IDA function scan

The follow-up broad function scan surfaced a special-event UI owner family that was not named in this backlog and is not captured by the top-level field-factory wrapper sweep.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Cake Pie event timerboard and item-info parity | The special-field Cake Pie runtime now owns the same presentation family surfaced by the follow-up IDA scan instead of leaving it implicit. `CakePieEventField` keeps the dedicated owner split in the existing seam: the timerboard path loads the Cake Pie-specific `Map/Obj/etc.img/5th_TownUI/{backgrd,bareffect,gage/0,gage/1}` and `Map/Obj/etc.img/5th_Timer/{backgrd,ready,start,cake,pie,fontTime}` assets, preserves the town-vs-typed timer branch with centered typed badges and timer-font digits, and the item-info path keeps the eight fixed `(fieldId,itemId)` rows owned by `CCakePieEvent` plus the dedicated `CUICakePieEventItemInfo` close button contract at `BtClose(1000)` / `(212,14)`. The remaining simulator gap in this row was that the owner button path existed in `CakePieEventField` but was never wired into the main mouse-dismiss flow, so the item-info dialog could block scripted-direction input without honoring the client-owned close interaction. `MapSimulator.UpdateLoop` now routes released left-clicks through `CakePieEventField.HandleMouseClick`, which closes the owner-managed dialog and emits the same owner-specific close message instead of leaving dismissal command-only. Focused source coverage now exists in `UnitTest_MapSimulator/CakePieParityTests.cs` for both the close-button hit path and the non-hit path that must leave the dialog open. | This row no longer depends on inferring the Cake Pie UI from generic clocks or utility windows. The simulator now exposes the dedicated owner family in the same special-field seam that owns the runtime, keeps the timerboard and item-info assets scoped to Cake Pie, and honors the item-info owner’s button-driven close lifecycle inside the normal input path rather than leaving it as a dead helper. | `CakePieEventField`, `MapSimulator.UpdateLoop.cs`, `UnitTest_MapSimulator/CakePieParityTests.cs`, Cake Pie event UI layer (`CCakePieEvent::SetEventItemInfo`, `CCakePieEvent::OpenUI_ItemInfo`, `CCakePieEvent::CloseUI_ItemInfo`, `CUICakePieEventItemInfo::OnCreate`, `CUICakePieEventItemInfo::Draw`, `CUICakePieEventItemInfo::Update`, `CUICakePieEventItemInfo::OnButtonClicked`, `CTimerboard_CakePieEvent::OnCreate`, `CTimerboard_CakePieEvent::Draw`, `CTimerboard_CakePieEvent::GetFontTime`, `CClock::OnCreate`) |

## Priority Order

If the goal is visible parity first, the next work in this area should be sequenced like this:

1. Existing-special-field refinement pass:
   Tighten Wedding, Coconut, Guild Boss, Massacre, SnowBall, and Ariant behavior against the client owners that already have simulator counterparts.
2. Missing microgame dialog pass:
   Add the still-untracked `CRPSGameDlg` Rock-Paper-Scissors owner and the Cake Pie event timerboard/item-info owner before more packet-owned minigame or event UI work gets buried in the generic field-utility bucket.
3. Shared field-wrapper refinement pass:
   Tighten the existing opcode-`163` current-wrapper dispatch seam before more special-field packet work stays split across per-mode inboxes or bypasses wrappers that still only expose field-value/result owners.
4. Variant and packet/HUD refinement pass:
   Continue deeper Monster Carnival, Party Raid, Tournament, SpaceGAGA, and MiniRoom packet/UI parity now that those runtime shells already exist.
5. Wrapper and transport pass:
   Triage the transit, tutorial/presentation, and Monster Carnival variant owners while closing the remaining official-session ownership gaps that still sit behind loopback inboxes or proxy seams.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".





