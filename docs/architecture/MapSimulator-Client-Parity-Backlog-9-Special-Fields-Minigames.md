# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Effects/SpecialEffectFields.cs`
- `HaCreator/MapSimulator/Fields/MinigameFields.cs`

### Client references checked

- `CField_GuildBoss::CField_GuildBoss` at `0x53e980`
- `CField_Coconut::OnClock` at `0x54a100`
- `CField_Coconut::OnCoconutHit` at `0x54a470`
- `CField_Coconut::BasicActionAttack` at `0x54a5e0`
- `CField_Coconut::DrawBoard` at `0x54a750`
- `CField_Coconut::OnCoconutScore` at `0x54bf70`
- `CField_AriantArena::OnUserScore` at `0x5492b0`
- `CField_AriantArena::OnShowResult` at `0x547630`
- `CField_Battlefield::OnClock` at `0x549ad0`
- `CField_Battlefield::OnScoreUpdate` at `0x5499a0`
- `CField_Battlefield::OnTeamChanged` at `0x5499e0`
- `CField_Battlefield::SetUserTeam` at `0x549870`
- `CField_Dojang::OnClock` at `0x550940`
- `CField_Dojang::Update` at `0x54ef10`
- `CField_CookieHouse::_UpdatePoint` at `0x54daa0`
- `CField_CookieHouse::Init` at `0x54e250`
- `CField_Massacre::OnClock` at `0x556af0`
- `CField_Massacre::UpdateKeyAnimation` at `0x556bf0`
- `CTimerboard_Massacre::Draw` at `0x557100`
- `CField_Massacre::Init` at `0x5579d0`
- `CMemoryGameDlg::OnButtonClicked` at `0x628df0`
- `CMemoryGameDlg::OnCreate` at `0x62f3c0`
- `CMemoryGameDlg::Draw` at `0x631cb0`
- `CField_MonsterCarnival::CreateUIWindow` at `0x55a510`
- `CField_MonsterCarnival::OnEnter` at `0x55a6c0`
- `CField_MonsterCarnival::OnRequestResult` at `0x55a890`
- `CField_MonsterCarnival::OnShowGameResult` at `0x55af80`
- `CField_MonsterCarnivalRevive::OnShowGameResult` at `0x55b110`
- `CField_PartyRaid::_UpdatePoint` at `0x55c120`
- `CField_PartyRaid::Init` at `0x55f110`
- `CField_PartyRaidBoss::OnFieldSetVariable` at `0x55ca40`
- `CField_PartyRaidBoss::OnPartyValue` at `0x55d070`
- `CField_PartyRaidResult::OnSessionValue` at `0x55e5c0`
- `CField_SnowBall::OnSnowBallTouch` at `0x560510`
- `CField_SnowBall::OnSnowBallState` at `0x560ab0`
- `CField_SnowBall::BasicActionAttack` at `0x5617b0`
- `CField_SnowBall::OnSnowBallHit` at `0x5619d0`
- `CField_SnowBall::OnSnowBallMsg` at `0x562040`
- `CField_SpaceGAGA::OnClock` at `0x5625d0`
- `CTimerboard_SpaceGAGA::Draw` at `0x5626c0`
- `CField_Wedding::SetBlessEffect` at `0x563b60`
- `CField_Wedding::OnWeddingProgress` at `0x5640f0`
- `CField_Witchtower::OnScoreUpdate` at `0x564ad0`
- `CScoreboard_Witchtower::Draw` at `0x564bd0`
- `CField_ContiMove::OnStartShipMoveField` at `0x54c970`
- `CField_ContiMove::OnContiState` at `0x54d5a0`
- `CField_KillCount::OnPacket` at `0x554050`
- `CField_LimitedView::Init` at `0x5554a0`
- `CField_HuntingAdballoon::OnFieldSetVariable` at `0x552050`
- `CField_MassacreResult::OnPacket` at `0x55a1d0`
- `CField_EscortResult::OnSessionValue` at `0x551160`
- `CField_Tournament::OnPacket` at `0x563780`
- `CRPSGameDlg::OnPacket` at `0x6d9e00`
- `CField_ShowaBath::DecodeFieldSpecificData` at `0x55ff70`
- `CField_Tutorial::DecodeFieldSpecificData` at `0x563830`
- `CField_MonsterCarnivalWaitingRoom::Init` at `0x55bda0`
- `CField_MonsterCarnivalS2_Game::Init` at `0x55bf10`
- `CField_NoDragon::Init` at `0x55c0e0`
- `CField_WeddingPhoto::GetFieldType` at `0x53eee0`
- `CField_AranTutorial::GetFieldType` at `0x53f1c0`
- `CField_Balrog::GetFieldType` at `0x549810`

## Client Function Index By Backlog Area

The list below maps this backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Special field and minigame parity

- `CField_GuildBoss::CField_GuildBoss` at `0x53e980`
- `CField_Coconut::OnClock` at `0x54a100`
- `CField_Coconut::OnCoconutHit` at `0x54a470`
- `CField_Coconut::BasicActionAttack` at `0x54a5e0`
- `CField_Coconut::DrawBoard` at `0x54a750`
- `CField_AriantArena::OnUserScore` at `0x5492b0`
- `CField_AriantArena::OnShowResult` at `0x547630`
- `CField_Battlefield::OnClock` at `0x549ad0`
- `CField_Battlefield::OnScoreUpdate` at `0x5499a0`
- `CField_Battlefield::OnTeamChanged` at `0x5499e0`
- `CField_Battlefield::SetUserTeam` at `0x549870`
- `CField_Dojang::OnClock` at `0x550940`
- `CField_Dojang::Update` at `0x54ef10`
- `CField_CookieHouse::_UpdatePoint` at `0x54daa0`
- `CField_CookieHouse::Init` at `0x54e250`
- `CField_Wedding::OnWeddingProgress` at `0x5640f0`
- `CField_Wedding::SetBlessEffect` at `0x563b60`
- `CField_Witchtower::OnScoreUpdate` at `0x564ad0`
- `CScoreboard_Witchtower::Draw` at `0x564bd0`
- `CField_Massacre::OnClock` at `0x556af0`
- `CField_Massacre::UpdateKeyAnimation` at `0x556bf0`
- `CTimerboard_Massacre::Draw` at `0x557100`
- `CMemoryGameDlg::OnButtonClicked` at `0x628df0`
- `CMemoryGameDlg::OnCreate` at `0x62f3c0`
- `CMemoryGameDlg::Draw` at `0x631cb0`
- `CField_SnowBall::OnSnowBallState` at `0x560ab0`
- `CField_SnowBall::OnSnowBallHit` at `0x5619d0`
- `CField_SnowBall::OnSnowBallMsg` at `0x562040`
- `CField_SnowBall::OnSnowBallTouch` at `0x560510`
- `CField_MonsterCarnival::CreateUIWindow` at `0x55a510`
- `CField_MonsterCarnival::OnEnter` at `0x55a6c0`
- `CField_PartyRaid::Init` at `0x55f110`
- `CField_PartyRaidBoss::OnFieldSetVariable` at `0x55ca40`
- `CField_PartyRaidResult::OnSessionValue` at `0x55e5c0`
- `CField_SpaceGAGA::OnClock` at `0x5625d0`
- `CTimerboard_SpaceGAGA::Draw` at `0x5626c0`
- `CField_ContiMove::OnStartShipMoveField` at `0x54c970`
- `CField_ContiMove::OnContiState` at `0x54d5a0`
- `CField_KillCount::OnPacket` at `0x554050`
- `CField_LimitedView::Init` at `0x5554a0`
- `CField_HuntingAdballoon::OnFieldSetVariable` at `0x552050`
- `CField_MassacreResult::OnPacket` at `0x55a1d0`
- `CField_EscortResult::OnSessionValue` at `0x551160`
- `CField_Tournament::OnPacket` at `0x563780`
- `CRPSGameDlg::OnPacket` at `0x6d9e00`
- `CField_ShowaBath::DecodeFieldSpecificData` at `0x55ff70`
- `CField_Tutorial::DecodeFieldSpecificData` at `0x563830`
- `CField_MonsterCarnivalWaitingRoom::Init` at `0x55bda0`
- `CField_MonsterCarnivalS2_Game::Init` at `0x55bf10`
- `CField_NoDragon::Init` at `0x55c0e0`
- `CField_WeddingPhoto::GetFieldType` at `0x53eee0`
- `CField_AranTutorial::GetFieldType` at `0x53f1c0`
- `CField_Balrog::GetFieldType` at `0x549810`

Notes:
IDA shows that these field modes are not generic map-rule variants. The client owns separate state machines, packet handlers, timer or scoreboard widgets, and even dedicated minigame dialog owners such as `CMemoryGameDlg` for Guild Boss, Coconut, Wedding, Witchtower, Massacre, SnowBall, Ariant Arena, Battlefield, Dojang, Cookie House, Monster Carnival, Party Raid, SpaceGAGA, and MiniRoom card games, so parity work should be tracked separately from the broader field, combat, or UI backlog areas.
The live `list_functions` scan also surfaced additional field-owner classes with no hits anywhere else in the parity backlog set, which means there is still real untracked special-field work outside the rows already listed below.

## Current State Summary

MapSimulator already has a partial special-field baseline instead of a blank area.
The project currently includes:

- A `SpecialEffectFields` surface with wedding, witchtower, guild-boss, and massacre handling.
- A concrete `MinigameFields` surface that now owns the snowball and coconut runtimes behind one backlog-aligned seam.
- A `SpecialFieldRuntimeCoordinator` seam in `MapSimulator` that binds special-field ownership to map load, update, draw, and reset flow so later agents can take one backlog row without redoing the simulator plumbing.
- Several packet-commented simulator seams that already reference the corresponding client field owners.

The parity gap here is not whether the simulator recognizes special maps at all.
The gap is whether those maps reproduce the client's dedicated rules, scoreboards, timers, UI flows, and win or lose transitions.

## Remaining Parity Backlog

### 1. Special field and minigame parity

This area is currently unowned by backlog documents even though the client and simulator both have dedicated field-specific seams.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Guild boss event fields | The simulator now routes special-field ownership through `SpecialFieldRuntimeCoordinator`, auto-binds guild-boss maps into that dedicated runtime seam, and no longer depends only on the older hard-coded map-id ranges: in this pass the coordinator also recognizes the actual guild-boss WZ shape by checking for both map-root `healer` and `pulley` nodes before configuring `GuildBossField`, which matches the exported guild-boss map `Map/Map9/990000900.img` where `healer` and `pulley` point at `Map/Obj/guild.img/syarenian/boss/{0,1}`. The runtime still configures `GuildBossField` directly from those WZ nodes, loads the exact animated art with origin-aligned placement, exposes both `/guildboss packet <344|345> ...` and `/guildboss packetraw <hex>` debug seams, decodes those payloads through a dedicated runtime packet path that mirrors `CField_GuildBoss::OnPacket`, applies `CHealer::Move` with the client's immediate absolute-Y result instead of simulator smoothing, cancels packet-owned preview state whenever an external healer or pulley packet arrives, mirrors `CField_GuildBoss::BasicActionAttack` more closely by driving the local pulley from the player's normal-attack hitbox instead of `Up`, and replays a local `CPulley::Hit`-style animation restart. WZ re-checks against the exported IMG filesystem confirmed the `990000900` healer values (`x=295`, `yMin=-564`, `yMax=-350`, `rise=1`, `fall=10`, `healMin=1000`, `healMax=2000`) plus pulley placement (`x=-59`, `y=-394`) and the `Map/Obj/guild.img/syarenian/boss/{0,1}` frame origins and delays (`0`: 18 frames, origin `124,199`, delays `1800/180/...`; `1`: 3 frames, origin `184,181`, delay `90`), while IDA recovery of `CField_GuildBoss::BasicActionAttack` at `0x5517d0` pinned the outbound pulley request to a bare `COutPacket(259)` with no extra payload. The dedicated guild-boss transport still emits that literal decrypted Maple packet frame as `packetoutraw 0301` alongside the legacy `pulleyhit <sequence> <tick>` bridge line for older tools, but this row now also wires the already-present in-process guild-boss official-session proxy through the live runtime seam: `/guildboss session [status|discover|start|startauto|stop]` now mirrors the Coconut and Memory Game session flow, drains decrypted live-session `344/345` packets into the same `GuildBossField.TryApplyPacket` path, prefers live-session opcode-`259` injection over the older line transport whenever a proxied Maple client is attached, disables simulator-owned pulley preview while either transport owner is active, adds the same local-port filtered session discovery helper used to disambiguate multiple MapleStory TCP sessions before `startauto`, and in this pass also stops reporting false-positive proxy startup when the listener cannot bind. `GuildBossOfficialSessionBridgeManager.TryStart(...)` now returns success or failure, `/guildboss session start ...` surfaces that result directly, and discovery-backed `startauto` now bubbles the same startup failure instead of claiming success after selecting a candidate. Focused guild-boss bridge tests are now on disk for successful listener startup plus occupied-port failure reporting in `UnitTest_MapSimulator/GuildBossOfficialSessionBridgeManagerTests.cs`, although executing `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter GuildBossOfficialSessionBridgeManagerTests` is still blocked in this workspace by unrelated pre-existing `HaCreator` compile failures outside the guild-boss row such as `UserInfoUI`, `LoginCreateCharacterWindow`, `AdminShopWishListUI`, `MinimapUI`, `UIWindowLoader`, `MemoryGameField`, `SkillManager`, and `PacketScriptMessageRuntime`. The remaining gap is now tightly scoped to true in-place session ownership: the simulator can proxy and decrypt an official Maple session for guild-boss traffic inside its own runtime seam, but it still does not attach directly to an already-established client socket pair without a localhost proxy hop first | This item now matches the full client-owned guild-boss seam confirmed in IDA more closely and closes another transport-ownership paper cut inside the built-in proxy path: `CField_GuildBoss` only initializes healer plus pulley, consumes packets `344/345`, and uses `BasicActionAttack` to hit the pulley and send a no-payload opcode-`259` request while idle, and the simulator can now own that packet flow either through the legacy loopback line transport or through an in-process official-session proxy without falsely reporting a listener-start success when the proxy bind fails. The row stays `Partial` only because the last step from simulator-owned proxying to true live-session attachment is still missing, not because the runtime lacks the client-backed WZ wiring, decoded inbound packet seam, recovered outbound request bytes, built-in official-session proxy path, or truthful startup/error surfacing for that proxy seam | `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `SpecialFieldRuntimeCoordinator.cs`, `SpecialEffectFields.cs`, `GuildBossPacketTransportManager.cs`, `GuildBossOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/GuildBossOfficialSessionBridgeManagerTests.cs`, `GuildBossField` (`CField_GuildBoss`, `CField_GuildBoss::Init`, `CField_GuildBoss::OnPacket`, `CField_GuildBoss::BasicActionAttack`, `CHealer::Init`, `CHealer::Move`, `CPulley::Init`, `CPulley::Hit`) |
| Partial | Coconut minigame runtime | The simulator now follows the client's finish-timestamp clocking, keeps score packet-owned instead of inventing local score on ground contact, applies packet-driven coconut state changes through the runtime's delayed hit queue, mirrors `CField_Coconut::BasicActionAttack` by selecting an intersecting coconut from the player's normal-attack world hitbox, and keeps that request transport-owned when a bridge client is present instead of replaying a simulator-only delayed hit preview. WZ re-checks against the exported IMG filesystem in this pass kept the board art pinned to `Map/Obj/etc.img/coconut/backgrnd` (`258x101`) and the result banners pinned to `Map/Effect.img/event/coconut/{victory,lose}` with the same three-frame timing and recovered delays (`120`, `120`, `3000`) for both banners, while IDA re-checks of `CField_Coconut::OnPacket` at `0x54bfb0`, `CField_Coconut::OnCoconutHit` at `0x54a470`, and `CField_Coconut::BasicActionAttack` at `0x54a5e0` kept the client packet ownership unchanged: `342` and `343` dispatch through `OnPacket`, hit packets still queue `target/delay/state` against `get_update_time()`, and outbound attacks still send `COutPacket(257)` with `Encode2(targetId)` then `Encode2(tDelay)`. The dedicated Coconut line transport still auto-flushes pending requests once a bridge client connects, emits both the compatibility `attack <targetId> <delay> <requestedAtTick>` line and the decrypted Maple outbound frame `packetoutraw <hex>`, accepts opcode-wrapped inbound Coconut packets from both the loopback inbox and `/coconut raw packetraw <hex>`, and ignores echoed opcode-`257` attack frames. The built-in Coconut official-session proxy on top of `MapleLib.PacketLib.Session` remains in place: `/coconut session start <listenPort> <serverHost> <serverPort>` still accepts a real Maple client connection through a local proxy, mirrors the Maple init handshake, decrypts live server-authored Coconut `342/343` packets directly into the existing packet-owned runtime seam, suppresses the simulator-only delayed-hit preview while that live session is attached, and injects opcode `257` back into the proxied live session without any external line bridge process. In this pass the bridge's TCP-owner-table discovery also learned how to disambiguate already-running MapleStory sessions by local TCP port, so `/coconut session discover <remotePort> [processName|pid] [localPort]` can narrow a multi-session list to one established connection and `/coconut session startauto <listenPort> <remotePort> [processName|pid] [localPort]` can start the proxy directly from that filtered candidate instead of falling back to a manual host copy when multiple sessions share the same remote port. Focused Coconut bridge tests now cover local-port-scoped candidate selection and the new filtered discovery reporting in addition to the earlier discovery-backed startup path. The remaining gap is narrower but still real: Coconut still does not bind in place to an already-established MapleStory socket session, and live opcode `257` injection still depends on the simulator owning a proxy hop rather than attaching directly to the existing client socket pair. | Coconut now covers both transport ownership modes inside the simulator itself, and the official-session seam is less manual than before: the older loopback line bridge remains available, the in-process Maple session proxy can decrypt official Coconut hit or score packets and inject opcode `257` on its own, discovery can now enumerate matching live MapleStory TCP endpoints, and multi-session cases can be disambiguated inside the simulator by local TCP port before proxy startup. The remaining work is no longer unresolved Coconut packet shape, preview ownership, manual remote-endpoint lookup, or multi-session disambiguation; it is specifically the last step from simulator-owned proxying to true live-session attachment and in-place socket ownership without redirecting the client through localhost first. | `MinigameFields.cs`, `MapSimulator.cs`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.ChatCommands.cs`, `CoconutPacketInboxManager.cs`, `CoconutOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/CoconutOfficialSessionBridgeManagerTests.cs`, `CoconutField` (`CField_Coconut::OnPacket`, `CField_Coconut::OnClock`, `CField_Coconut::OnCoconutHit`, `CField_Coconut::BasicActionAttack`, `CField_Coconut::DrawBoard`, `CField_Coconut::OnCoconutScore`) |
| Partial | Wedding ceremony fields | The simulator now resolves ceremony lines from `String/Npc.img/<npcId>/wedding{step}` with WZ-backed fallbacks, distinguishes groom or bride versus guest state from the local character id, mirrors the chapel-only step-2 guest bless prompt that `CField_Wedding::OnWeddingProgress` special-cases, records the client's modal response packet flow through step-advance (`163`) and guest-bless (`164`) ceremony responses, exposes a `/wedding` debug seam to drive progress, actor anchors, guest audience overlays, and responses through that same runtime, loads the real bless animation frames from `Effect/BasicEff.img/Wedding` while keeping that effect alive until the same client-owned clear path disables it instead of timing it out locally, swaps BGM to the WZ-backed `Sound/BgmEvent.img/wedding` track from the same step-0 ceremony seam the client uses, restores the map BGM when the wedding runtime clears, draws the WZ-backed declaration overlay from `UI/UIWindow.img/wedding/text/0` during the opening step, and also promotes the later pronouncement and blessing steps into a second WZ-backed ceremony scene layer from `UI/UIWindow.img/wedding/card/0` with the existing wedding weather celebration path. The existing wedding-side remote actor seam also moved beyond manual overlay commands: `/wedding inbox ...` accepts client-shaped remote-user packet aliases for `UserEnterField` (`179`), `UserLeaveField` (`180`), `UserMove` (`210`), `SetActivePortableChair` (`222`), and `AvatarModified` (`223`); `WeddingField` routes official remote spawn, leave, move, and chair payloads through the shared remote-user packet codec instead of its older wedding-only subset, which lets the wedding runtime accept client-shaped `UserEnterField` payloads even when the remote `SecondaryStat::DecodeForRemote` blob is non-empty by resolving the AvatarLook boundary after the variable-length temporary-stat segment rather than hard-failing on a non-zero 16-byte mask. The runtime still resolves AvatarLook-backed groom, bride, or guest builds with the existing loader seam, promotes audience actors into groom or bride overlays when ceremony ids later identify them, preserves real remote character ids all the way into the shared wedding renderer path for guests instead of collapsing them back to synthetic name hashes, and retains each decoded `PlayerMovementSyncSnapshot` so groom, bride, and guest actors follow packet-backed `CMovePath` playback instead of teleporting between static overlays. In this pass the remaining `CUserPool` lifecycle gap narrowed further with direct client evidence from `CUserPool::OnUserEnterField` and `CUserPool::Update`: official wedding remote users now queue into a client-shaped delayed-load seam inside `WeddingField`, advance into the shared remote-user pool one actor per update while a 100 ms load window is open, wait through the recovered 1000 ms cooltime before the next load window, and keep drawing inside the wedding-local renderer until that delayed promotion finishes so packet updates do not make actors disappear. That delayed-load ownership is scoped only to official packet-backed remote users, so the older manual `/wedding guest ...` debug overlays continue to mirror immediately instead of being pulled into the client-only queue. Focused parity tests on disk now cover that delayed-load window and cooltime behavior plus the non-delayed manual guest path in `UnitTest_MapSimulator/WeddingRemoteDelayedLoadParityTests.cs`, although this workspace still cannot execute `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter FullyQualifiedName~WeddingRemoteDelayedLoadParityTests` end-to-end because `HaCreator` currently has unrelated pre-existing compile failures outside the wedding row (for example `MapSimulator.FieldRestrictionParity.cs` and `MapSimulator.ClientOwnedFieldWrappers.cs`, alongside the broader existing workspace churn). The remaining gap is now narrower and explicit: the simulator still skips over remote secondary-stat payloads instead of modeling the decoded temporary-stat state itself, it still does not own the full client name-tag refresh lifecycle beyond its existing renderer labels, and the ceremony scene choreography is still limited to the recovered wedding text or card overlays plus particle layers rather than a fully decoded client timeline | Wedding maps now cover both recovered WZ ceremony overlays and a dedicated wedding-side remote actor seam for the couple plus guest audience inside the existing runtime, and that seam can now be driven either by direct `/wedding` overlay commands or by client-shaped remote-user packets for spawn, leave, move, chair, and avatar refresh through the loopback inbox while preserving official `CMovePath` snapshots into both the wedding runtime and the shared renderer bridge. The remaining work is no longer the basic remote delayed-load queue itself; it is concentrated on decoded remote temporary-stat presentation, the full client name-tag owner flow, and any additional client-only ceremony sequencing beyond the WZ-backed layers already identified | `SpecialEffectFields.cs`, `WeddingField`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.WeddingRemoteUsers.cs`, `MapSimulator.UpdateLoop.cs`, `RemoteUserPacketCodec.cs`, `WeddingPacketInboxManager.cs`, `UnitTest_MapSimulator/WeddingRemoteDelayedLoadParityTests.cs` (`CField_Wedding::OnWeddingProgress`, `CField_Wedding::SetBlessEffect`, `CUserPool::OnUserEnterField`, `CUserPool::OnUserLeaveField`, `CUserPool::Update`, `CUserPool::OnUserRemotePacket`, `SecondaryStat::DecodeForRemote`, `CUserRemote::OnMove`, `CUserRemote::OnAvatarModified`) |
| Implemented | Witchtower score UI | The simulator anchors the Witchtower scoreboard to the client's center-top window origin, loads the exact `Map/Obj/etc.img/goldkey` background, key, and bitmap digits that match the `CScoreboard_Witchtower::OnCreate` asset shape, renders the zero-padded score at the verified `(67, 4)` draw origin with the client's negative digit spacing, and keeps the `witchscore` debug seam driving the same update path | This is a visible special-field HUD with client-backed art and placement now, so the remaining special-field work should move to other rows instead of revisiting a placeholder Witchtower overlay | `SpecialEffectFields.cs`, `WitchtowerField`, `MapSimulator.cs` (`CField_Witchtower::OnScoreUpdate`, `CScoreboard_Witchtower::OnCreate`, `CScoreboard_Witchtower::Draw`) |
| Partial | Massacre timerboard and gauge flow | The simulator now mirrors the client's type-2 timerboard ownership at the verified center-top origin, draws a dedicated MM:SS timer surface instead of a generic corner clock, keeps the timer digits alive even without a fallback `SpriteFont`, probes the loaded `UIWindow(.2).img` trees for a real `258x61` timerboard source canvas before falling back to the older `MonsterKilling/Count/backgrd{0,1}` approximation, and keeps the timer-digit source pinned to the same `MonsterKilling/Count/number` image-font subtree that `CTimerboard_Massacre::Draw` uses through `draw_text_by_image`. The runtime also carries the client-owned `CField_Massacre::_SetMassacreInfo` count board: it draws the WZ-backed `UI/UIWindow(.2).img/MonsterKilling/Count/backgrd{0,1}` panel with `number2` bitmap digits at the client draw offsets (`hit 57,11`, `cool 57,33`, `miss 57,55`, `skill 80,85`), exposes the existing `/massacre info <hit> <miss> <cool> [skill]` seam, and keeps `keyBackgrd/{open,ing,close}` on the recovered `nSkill` lifecycle instead of inventing a gauge-owned pulse. In this pass the row also gained a tighter result-owner surface from WZ and IDA evidence instead of staying presentation-only: `CField_MassacreResult::OnMassacreResult` at `0x5596c0` re-confirmed the official packet `174` payload as `<rank:1><score:4>`, the runtime now keeps score and rank packet-owned through that exact shape, the result HUD status surface now exposes the recovered result ids `0x151C` and `0x151D` alongside the earlier timerboard, count, and clear ids, and the draw path now layers the client-backed rank-specific result effect from `Map/Effect.img/killing/yeti{0..4}` on top of the existing `UI/UIWindow2.img/MonsterKilling/Result` board instead of stopping at the generic clear/fail banner plus static rank letter. WZ re-checks against the exported IMG filesystem in this pass kept that result board pinned to `MonsterKilling/Result/backgrd` (`386x218`), the pulse overlay pinned to `backgrd2`, the letter ranks pinned to `Rank/{s,a,b,c,d}`, the big score digits pinned to `number2`, and the five rank-effect variants aligned with the same `killing/yeti0` through `killing/yeti4` roots the client result owner can loop. The dedicated loopback inbox seam remains in place and still auto-starts on Massacre maps, exposes `/massacre inbox [status|start [port]|stop]`, accepts raw opcode-wrapped packet `173` lines, accepts raw client-shaped type-2 clock payloads (`<type:1><seconds:4>`), accepts raw client-shaped `CWvsContext::Massacre` snapshots (`<hit><miss><cool><skill>` as four little-endian ints), and routes those feeds into the existing timerboard, count-board, and gauge seams instead of requiring only manual per-command authoring. This pass also moved live packet ownership one step closer to the client transport seam instead of leaving Massacre fully loopback-only: `/massacre session [status|discover|start|startauto|stop]` now exposes a built-in Maple proxy bridge that mirrors the Coconut/Guild Boss ownership model, initializes Maple crypto from the real handshake, forwards live client/server traffic, discovers established MapleStory TCP sessions with the same optional local-port disambiguation helper, and drains live opcode `173/174` packets straight back into the existing `MassacreField.TryApplyPacket` seam instead of requiring those result and inc-gauge packets to be retyped by hand. Focused bridge startup tests are now on disk for that Massacre proxy manager in `UnitTest_MapSimulator/MassacreOfficialSessionBridgeManagerTests.cs`, although executing `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter MassacreOfficialSessionBridgeManagerTests` is still blocked in this workspace by an unrelated pre-existing `HaCreator` compile failure in `MapSimulator/Combat/MobSkillLevelResolver.cs` (`WzImageProperty` missing). The simulator still renders the Massacre gauge from `MonsterKilling/Gauge` using the client's `259`-pixel fill span and the `_SetDecGauge` danger cutoff at roughly `65%` depletion, applies `mobMassacre` map defaults for `gauge.total`, `gauge.decrease`, `gauge.hitAdd`, and `disableSkill`, surfaces the map's `countEffect` thresholds as next-milestone plus reached-threshold HUD state, replays the WZ-backed milestone presentation from `Map/Effect.img/killing/first/{start,stage}` plus `Map/Effect.img/killing/number/<stage>`, replays the clear screen effect at the client's `timer <= 1` update seam, and still exposes `/massacre bonus` plus `/massacre result ...` for the presentation layer. The remaining gap is now narrower and explicit: the exact `StringPool` literals behind `0x14EE`, `0x14EC`, and the recovered `0x1510`-`0x151D` ids are still not decoded byte-for-byte, the new official-session seam still only proxies the recovered live opcodes `173/174` and still depends on a localhost proxy hop rather than attaching directly to an already-running socket pair, the in-field clear-vs-fail transition is still simulator-authored rather than recovered from the official result owner, and the fuller server-owned Massacre result choreography plus any live clock/context packet ownership beyond the recovered rank/score and inc-gauge packets are still missing | Hunting-event maps depend on dedicated timer, point-board, skill-key, gauge, and result-owner behavior that the client does not route through the normal status bar, and the simulator now covers the client-backed HUD art, timerboard placement, key lifecycle, map-default gauge rules, stage and bonus presentations, the recovered rank-plus-score result payload shape, the WZ-backed rank-specific result effect layer, and a built-in official-session proxy seam for the recovered live Massacre packet subset in addition to the older loopback ownership path. The remaining work is now concentrated on unresolved `StringPool` text literals, direct in-place live-session attachment plus the unrecovered live clock/context/result choreography, and the last clear/fail result-owner transition rather than on missing timerboard, count-board, gauge, score, or rank runtime ownership. | `SpecialEffectFields.cs`, `MassacreField`, `MapSimulator.cs`, `MapSimulator.Content.cs`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.ChatCommands.cs`, `MassacrePacketInboxManager.cs`, `MassacreOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/MassacreOfficialSessionBridgeManagerTests.cs` (`CField_Massacre::Init`, `CField_Massacre::_SetMassacreInfo`, `CField_Massacre::_UpdatePoint`, `CField_Massacre::OnPacket`, `CField_Massacre::OnClock`, `CField_Massacre::Update`, `CField_Massacre::UpdateKeyAnimation`, `CTimerboard_Massacre::OnCreate`, `CTimerboard_Massacre::Draw`, `CField_MassacreResult::OnPacket`, `CField_MassacreResult::OnMassacreResult`) |
| Partial | Memory Game and MiniRoom card parity | The simulator now syncs the MiniRoom shell occupant roster and status text to the live Match Cards board, accepts mouse-driven sidebar and card hit testing on the `CMemoryGameDlg`-style surface, exposes delayed remote ready/start/flip/tie/give-up/end actions through the same runtime path that the local room uses, adds both a typed `MemoryGamePacketType` dispatch seam behind `/memorygame packet ...`, a raw MiniRoom payload seam behind `/memorygame packetraw ...`, and an opcode-wrapped receive seam behind `/memorygame packetrecv <opcode> <payload>`, decodes the client-backed MiniRoom gameplay subtypes (`6 -> 50/51/58/59/61/62/63/68`) plus the shared MiniRoom base enter, chat, avatar, and leave families (`4/7/8/9/10`) into packet-owned roster, visitor, avatar-summary, chat-log, and leave handling, applies packet-owned board shuffle data from `CMemoryGameDlg::OnUserStart`, upgrades the shared MiniRoom shell with a dedicated Match Cards chat log plus system or speaker message presentation instead of the old generic preview-only pane, preserves visitor leave packets without tearing down the active board while surfacing slot-backed job or face or hair summaries in the room roster, now auto-starts a dedicated loopback MiniRoom packet inbox so live raw payloads or opcode-wrapped receive lines can drain through the same runtime seam with `/memorygame inbox ...` controls, normalizes two-byte Maple opcode wrappers before dispatch so externally captured server receive packets can reuse the same `CMemoryGameDlg` packet path, maps the `CMiniRoomBaseDlg::MakeGameMessage` codes onto the client StringPool ids recovered from IDA while surfacing those ids in system messages, renders decoded `AvatarLook` payloads as actual occupant avatar previews inside the MiniRoom shell by rebuilding `CharacterBuild` previews through the existing avatar loader path, and now also exposes a built-in live Maple proxy bridge behind `/memorygame session [discover|start|startauto|stop]` that initializes Maple crypto from the handshake, forwards live client/server traffic, peels inbound MiniRoom opcode `373` packets back through the same Match Cards dispatch seam, classifies the supported live client `opcode 144` Match Cards request subset (`10/50/52/56/57/58/59/60/61`) into mirrored `packetclientraw ...` bridge messages, and now also routes the local end-button and `/memorygame end` path through the same leave-booking seam that client subtypes `56/57` use so active rounds book or cancel a pending leave instead of tearing the room down immediately, surfaces that pending leave state in the shared MiniRoom shell, and resolves booked departures after the round by closing the local room or removing the booked guest seat. Focused tests are now on disk for the official-client leave-booking subtypes, exit-toggle parity, and post-round leave resolution in addition to the earlier opcode-wrapper normalization, StringPool-id-backed game-message routing, visitor-leave board preservation, mirrored official-session client request classification, and client-request dispatch coverage. The remaining gap is narrower again: the simulator still does not attach to an already-running MapleStory socket session in-process or passively decrypt MiniRoom traffic without the proxy bridge, the special game-message strings are still fallback English text annotated with the recovered client StringPool ids instead of byte-for-byte localized `StringPool::ms_aString` literals, and the exact client confirmation choreography around leave-booking or other outbound request prompts is still not byte-perfect beyond the modeled toggle-and-resolve state machine | MiniRoom parity now covers the room shell, board state machine, click flow, raw gameplay-packet decode, opcode-wrapped receive-packet normalization, packet-owned start-board data, the shared base enter/chat/avatar/leave families, a loopback inbox plus live proxy bridge transport seam, mirrored live-client request playback for the supported outbound Match Cards subset, StringPool-id-backed game-message presentation, real remote-avatar previews inside the shared shell, and a closer `OnClickEndButton`-style leave-booking lifecycle instead of an immediate local teardown, so the remaining work is concentrated on exact localized StringPool extraction, fuller prompt choreography, and true direct live-session ownership rather than missing core Match Cards ownership | `HaCreator/MapSimulator/Fields/MemoryGameField.cs`, `HaCreator/MapSimulator/Interaction/SocialRoomModels.cs`, `HaCreator/MapSimulator/UI/Windows/SocialRoomWindow.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `HaCreator/MapSimulator/MapSimulator.ChatCommands.cs`, `HaCreator/MapSimulator/Managers/MemoryGamePacketInboxManager.cs`, `HaCreator/MapSimulator/Managers/MemoryGameOfficialSessionBridgeManager.cs`, `HaCreator/MapSimulator/Managers/LoginAvatarLookCodec.cs`, `UnitTest_MapSimulator/Fields/MemoryGameFieldLeaveBookingParityTests.cs` (`CMemoryGameDlg::OnCreate`, `CMemoryGameDlg::Draw`, `CMemoryGameDlg::OnPacket`, `CMemoryGameDlg::OnUserStart`, `CMemoryGameDlg::OnTurnUpCard`, `CMemoryGameDlg::OnTimeOver`, `CMemoryGameDlg::OnGameResult`, `CMemoryGameDlg::OnClickReadyButton`, `CMemoryGameDlg::OnClickStartButton`, `CMemoryGameDlg::SendTieRequest`, `CMemoryGameDlg::SendClaimGiveUp`, `CMemoryGameDlg::OnClickEndButton`, `CMiniRoomBaseDlg::OnPacketBase`, `CMiniRoomBaseDlg::OnChat`, `CMiniRoomBaseDlg::OnEnterBase`, `CMiniRoomBaseDlg::OnAvatar`, `CMiniRoomBaseDlg::MakeGameMessage`) |
| Partial | Snowball minigame runtime | The simulator now treats Snowball state `2/3` as the client's packet-owned win states instead of local touch-zone states, keeps snowman HP packet-owned via `OnSnowBallState`, applies WZ-backed snowman stun windows and damage defaults through the runtime seam, smooths active snowball movement from the packet position plus speed data instead of instant teleports, routes the local player's world position through `SpecialFieldRuntimeCoordinator` so `SnowBallField` can surface the client's outbound touch-packet loop as pending team touch requests whenever the local player overlaps a snowball lane, aligns the Story-vs-Maple team labeling used by win banners and the local scoreboard with the client message owner, and routes `OnSnowBallMsg` through the simulator chat log seam with client-shaped team ordering plus packet-owned fallback text instead of showing those packet messages as center-screen overlays. In this pass, the runtime also stopped depending on ad hoc Snowball setup: `SpecialFieldRuntimeCoordinator` now detects Snowball maps through the preserved `snowBall` map property or `FIELDTYPE_SNOWBALL`, `MinigameFields.BindMap` now auto-binds `SnowBallField`, and a new `SnowBallFieldDataLoader` reads the copied WZ `snowBall` node from `MapInfo.additionalNonInfoProps` or the bound `WzImage` so the simulator uses the same client-owned config that `CField_SnowBall::Init` consumes. That bind path now mirrors the client-owned shared-`x`/shared-`x0` plus per-team-`y` model recovered from `CField_SnowBall::Init` at `0x561a10`: the simulator now loads `dx`, `x`, `x0`, `xMin`, `xMax`, `damageSnowBall`, `damageSnowMan0`, `damageSnowMan1`, `snowManHP`, `snowManWait`, `section1`, `section2`, and `section3`, positions both snowmen on the shared client `x` with their own lane `y`, positions both snowballs from the shared client `x0` with their own lane `y`, clamps active movement to the client `xMin/xMax` span, and resolves the WZ `Map/Obj/event.img/snowyRock/snowball/{0,1}` paths directly into `IDXObject` frame sets through the existing minigame seam when a graphics device is available. `SnowBallField.OnSnowBallState` still mirrors the client-owned first-snapshot ownership from `CField_SnowBall::OnSnowBallState` at `0x560ab0` by accepting packet-fed `damageSnowBall`, `damageSnowMan0`, and `damageSnowMan1` values only on that first state snapshot, and repeated `state == 2/3` packets still no longer inflate the simulator-only round scoreboard or replay duplicate win banners. Focused Snowball tests are now on disk for WZ definition load, map-backed shared-`x`/per-team-`y` binding, first-snapshot damage ownership, later-snapshot damage preservation, repeated win-state score suppression, touch-request clearing, and client-shaped message team ordering. The row was also re-checked against the exported IMG filesystem through `MapleLib.Img.ImgFileSystemDataSource`, and the `String` category still does not expose recoverable localized payload nodes for ids `3445`-`3447` (`0xD75`-`0xD77`). Attempted execution of `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter SnowBallFieldTests` is still blocked in this workspace by unrelated pre-existing `HaCreator` compile errors outside the Snowball row (`MapTransferUI` soft-keyboard interface breakage). | Snowball is narrower again but still `Partial`: the remaining gap is now specifically recovery of the literal client `StringPool` text for `0xD75`-`0xD77`, plus true server-owned packet transport or decode plumbing beyond the current direct runtime seam. | `MinigameFields.cs`, `SnowBallField`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/SnowBallFieldTests.cs` (`CField_SnowBall::Init`, `CField_SnowBall::OnSnowBallState`, `CField_SnowBall::BasicActionAttack`, `CField_SnowBall::OnSnowBallHit`, `CField_SnowBall::OnSnowBallMsg`, `CField_SnowBall::OnSnowBallTouch`) |
| Partial | Ariant Arena field flow | The simulator now auto-binds `FIELDTYPE_ARIANTARENA` maps into a dedicated `AriantArenaField`, mirrors the client's sorted top-left ranking surface with WZ-backed `AriantMatch/characterIcon` row icons and the client's `(icon=5,y) / (name=21,y+2) / (score=106,y+2)` row offsets, keeps each player's Ariant icon assignment attached to that user across later score-driven re-sorts, plays the WZ-backed `AriantMatch/Result` animation from the client's center-top result origin, resolves the Ariant result UI sound from `Sound/MiniGame.img/Show` with a `Win` fallback instead of a broad heuristic, pushes local player name plus job into the runtime each frame so the client-owned 8xx or 9xx local-job rank suppression is honored, consumes the real Ariant packet types through a runtime packet seam for `354` score batches and `171` show-result packets, auto-starts a dedicated loopback Ariant packet inbox on Ariant maps so live `showresult` or `userscore` lines from the simulator transport can drain onto the main thread into that same runtime seam with `/ariantarena inbox ...` controls, extends that same inbox seam to accept transport-driven remote overlay lines for `actor add`, `actor avatar`, `actor move`, `actor remove`, and `actor clear`, and now routes both those overlay commands and client-owned remote-user packet ids `179` (`UserEnterField`), `180` (`UserLeaveField`), `210` (`UserMove`), `222` (`SetActivePortableChair`), and `223` (`AvatarModified`) through the shared `RemoteUserActorPool` instead of Ariant's earlier duplicate overlay list. Ariant remotes now render as real world actors through that common pool, keep Ariant-specific ownership via source-tagged actor entries, preserve packet-owned AvatarLook refreshes and chair state through the shared actor seam, and apply packet `210` through the simulator's movement-snapshot path so facing plus action playback now follow the decoded move path rather than collapsing to one-off overlay updates. Focused Ariant tests were also added on disk for shared-pool ownership, packet spawn, packet move, and Ariant-only clear behavior. The remaining gap is now narrower and explicit: Ariant remote spawn decode still requires an empty 16-byte remote secondary-stat mask instead of handling the full `SecondaryStat::DecodeForRemote` state space, the simulator still only covers the recovered Ariant remote packet subset rather than the whole client `CUserPool::OnUserRemotePacket` family, and live official-session socket ownership is still external to the simulator transport seam | Arena scoring and ranking are owned by a dedicated field class in the client and are not covered by generic mob or UI behavior, and this row now also shares the simulator's real remote-user actor owner instead of maintaining an Ariant-only duplicate overlay path, leaving the remaining work concentrated on non-empty remote secondary-stat decode, broader client remote-packet coverage, and native official-session ownership rather than basic remote actor rendering or snapshot-driven motion | `MinigameFields.cs`, `AriantArenaField`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `AriantArenaPacketInboxManager.cs`, `UnitTest_MapSimulator/AriantArenaFieldTests.cs` (`CField_AriantArena::OnUserScore`, `CField_AriantArena::UpdateScoreAndRank`, `CField_AriantArena::OnShowResult`, `CUserPool::OnUserEnterField`, `CUserPool::OnUserLeaveField`, `CUserPool::OnUserRemotePacket`, `CUserRemote::Init`, `CUserRemote::OnMove`, `CUserRemote::OnAvatarModified`) |
| Implemented | Battlefield event flow | The simulator now detects Battlefield maps through the special-field runtime, loads `battleField` WZ defaults such as `timeDefault`, `timeFinish`, reward maps, and effect paths, follows the client's packet-owned `OnClock(type=2, seconds)` and `OnScoreUpdate(wolves, sheep)` seams, exposes a Battlefield team-change seam aligned with `OnTeamChanged(characterId, team)` and `SetUserTeam`, resolves a finish phase that derives win/lose effect plus reward-map routing from the WZ `battleField` node, parses the map's `user/<team>` presets so local Battlefield team changes now reapply both the client-backed outfit pieces and the WZ `stat/speed` move-speed override before restoring the original avatar and speed on clear or map exit, retains each preset's `speedmax` and `noitem` data for inspection or later consumption, resolves packet-owned remote team assignments through the same character-id lookup seam, renders the client Battlefield board from `Map/Obj/etc.img/battleField/backgrnd` with the real sheep or wolf score fonts and the six-digit `fontTime` clock layout, queues the resolved reward map through the existing special-field transfer seam once the WZ `timeFinish` phase expires, mirrors the local `SetUserTeam` minimap side effect by forcing the simulator minimap out of its collapsed state for local teams `0` and `2`, pushes the simulator's actual local character id into `BattlefieldField` so packet-shaped `OnTeamChanged` calls classify the local user correctly, wires the map preset's `noitem` and `speedmax` fields into live simulator systems by blocking local Battlefield item use plus equip drags for blocked item ids and by clamping the live player move-speed path through the preset speed cap, and in this pass drives those same WZ-backed team presets through the shared `RemoteUserActorPool` so remote Battlefield users now hot-swap their live avatar presentation too. The shared actor owner now re-syncs Battlefield looks during map bind and per-frame update, resolves late remote spawns against already-known field-owned team assignments instead of requiring the actor to cache the raw team id first, and restores each remote actor's original appearance and speed when Battlefield ownership clears or the map exits. Focused Battlefield tests were added on disk for that late-spawn remote-team path plus reset restore behavior, but the current workspace could not execute them because `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter BattlefieldFieldTests` is still blocked by unrelated pre-existing `HaCreator` compile errors outside the Battlefield row, including `RemoteUserPacketCodec.PacketReader` accessibility, `LoginUtilityDialogWindow.Configure` accessibility, and missing `SnowBallField` packet constants | Battlefield maps depend on a team-scoring field runtime rather than only ordinary combat rules, and the client keeps both the scoreboard window creation and `SetUserTeam`-owned avatar presentation inside `CField_Battlefield`; the simulator now mirrors that ownership end-to-end by pushing the same WZ-backed Battlefield team presets through both the local player path and the shared remote-user actor owner instead of leaving remote users visually stale after packet-owned team changes | `SpecialEffectFields.cs`, `BattlefieldField`, `RemoteUserActorPool.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `MapSimulator.Content.cs`, `MapSimulator.UpdateLoop.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `EquipUIBigBang.cs`, `MinimapUI.cs`, `UnitTest_MapSimulator/BattlefieldFieldTests.cs` (`CField_Battlefield::OnClock`, `CField_Battlefield::OnScoreUpdate`, `CField_Battlefield::OnTeamChanged`, `CField_Battlefield::SetUserTeam`, `CScoreboard_Battlefield::OnCreate`) |
| Partial | Mu Lung Dojo field flow | The simulator now binds `FIELDTYPE_DOJANG` and the `MuruengRaid` map block into a dedicated `DojoField`, mirrors the client's type-2 clock ownership into the real `UI/UIWindow(.img)/muruengRaid` timerboard with bitmap digits and colon placement, draws the Dojo player, monster, and energy gauges from the client's WZ canvases at the IDA-verified layer anchors, swaps to the looping `energy/full` animation at max energy, replays the stage-start, clear, and time-over presentations from `Map/Effect.img/dojang` through `/dojostage` and `/dojoresult` debug seams plus the live timer-expiry path, auto-triggers the clear presentation when the live boss HP feed drops to zero, consumes the map's real `returnMap` or `forcedReturn` data so timer expiry waits for the WZ-backed time-over presentation to finish before queuing the Dojo exit transfer through the simulator's normal map-change seam, now resolves the clear-transfer destination from the board's real `dojang_next` portal target map before falling back to map-id inference, still delays that transfer until the clear presentation completes, accepts the official type-`1` raw Dojo clock payload as a decoded client-backed no-op while keeping type-`2` as the only timerboard-owning clock path recovered from `CField_Dojang::OnClock`, stops the live timer immediately when the clear presentation starts so the time-over path cannot race the clear-transfer choreography, clears stale result-transfer state when a new stage or fresh type-`2` clock arrives, and in this pass also extends the raw packet seam beyond clock-only handling by decoding Dojo stage, clear, and time-over payloads into the existing `DojoField` flow, accepting both 1-byte and 4-byte stage payloads plus 4-byte transfer-map payloads with an optional trailing portal-name suffix, then surfacing the last decoded non-clock packet in the Dojo status seam for debugging. Focused `DojoField` tests are now present on disk in this workspace for the new raw stage, clear, and time-over decode paths together with the earlier transition coverage, but `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter DojoFieldTests` still cannot run end-to-end here because unrelated pre-existing `HaCreator` compile errors stop the build first (currently `LoginUtilityDialogWindow` and `MapTransferUI` missing new `ISoftKeyboardHost` members). The remaining gap is now narrower and specific: the simulator still has not recovered the byte-accurate official Dojo packet table beyond the clock path and these best-effort raw payload shapes, still does not reproduce the server-authored next-floor choreography byte-for-byte, and still depends on loopback-fed runtime events instead of a true official-session transport attachment | Dojo behavior is timer-driven and mode-specific, and the simulator now has a client-backed HUD, WZ-backed clear/time-over transfers, a real `dojang_next` target-map binding, raw packet decoding that covers clock plus stage / clear / time-over ownership through the existing seam, and tighter clear-vs-timeout transition handling, leaving the remaining parity gap concentrated on the unrecovered official packet table, full server-owned floor-transition choreography, and official-session attachment | `SpecialEffectFields.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `DojoPacketInboxManager.cs`, `UnitTest_MapSimulator/DojoFieldTests.cs` (`CField_Dojang::Init`, `CField_Dojang::OnClock`, `CField_Dojang::Update`, `CField_Dojang::UpdateTimer`) |
| Partial | Cookie House event flow | The simulator now binds `fieldType == CookieHouse` into a dedicated `CookieHouseField` runtime, mirrors the client-owned HUD geometry from `CField_CookieHouse::Init`, tracks score updates through a score-owned redraw path instead of generic UI, replaces the old solid-color placeholder with WZ-backed HUD panel pieces, mirrors the client-owned redraw ownership more closely by caching the HUD into a dedicated `187x45` render target that only rebuilds when the point value changes before drawing that cached layer each frame, mirrors the client-owned update path by polling a simulator context point source each frame before redrawing instead of mutating the HUD as the source of truth, keeps the background load pinned to the client-shaped `UI/UIWindow.img/raise/backgrnd` root with `UIWindow2.img` only as a fallback variant, and tightens the bitmap-number loader around the actual client constructor shape from `CBitmapNumber::CBitmapNumber`: it now searches for a five-style root whose children expose per-style digits plus sign canvases instead of assuming the old direct `raise` subtree shape, broadens that search across `UIWindow(.2).img` while still preferring `raise`-shaped paths, and falls back to unnamed non-digit canvases inside a style when the sign StringPool literals are still unresolved. In this pass, IDA recovery also closed the client grade-table gap: `FindGrade_` now matches the recovered `s_nGradeCount == 4` / `s_anGrade == { 90, 160, 230, 350 }` data from `MapleStory.exe`, the status seam reports those exact client thresholds instead of a placeholder, and focused runtime tests now cover the recovered grade transitions plus provider-owned polling. The remaining gap is narrower: the exact literal WZ path behind StringPool id `0x13FC` is still unresolved even though the simulator now mirrors the client-owned root and per-style lookup shape more closely, and it still lacks the true live server-owned Cookie House packet feed beyond the existing loopback inbox seam | Cookie House is another distinct score-driven field, and this row now has a client-shaped cached HUD redraw path, a verified client background root, a recovered client grade table, a constructor-shaped bitmap-number search and sign fallback path, and an external point-stream seam instead of remaining a manual overlay with guessed thresholds and a guessed digit model | `CookieHouseField.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `CharacterData.cs`, `CookieHousePointInboxManager.cs`, `UnitTest_MapSimulator/CookieHouseFieldTests.cs` (`CField_CookieHouse::_UpdatePoint`, `CField_CookieHouse::Init`, `CField_CookieHouse::Update`, `CBitmapNumber::CBitmapNumber`, `CBitmapNumber::Draw`, ``anonymous namespace'::FindGrade_``) |
| Partial | Monster Carnival field flow | The simulator now detects Monster Carnival maps through the backlog coordinator, loads the map's WZ-backed Carnival definition into a dedicated runtime, enriches carnival mob entries with WZ-backed `getCP` and revive-chain metadata, pulls the map's `mobGenPos`, `guardianGenPos`, `reactorRed`, and `reactorBlue` data into that same definition, enforces local CP spending plus mob or guardian generation caps when requests succeed, clears prior summon or guardian state on each new `OnEnter` round like the client-owned UI reset path, rejects duplicate guardian requests through the client's dedicated occupied-slot failure reason, records local successful mob summons against the next WZ-backed mob spawn slot while recording guardian placements against their WZ-backed guardian slot plus the team-owned reactor id, reconciles packet-owned summon counts into live spawn-slot state on `Enter` or summoned-count updates, awards WZ-backed mob `getCP` plus revive-chain spawns through an explicit mob-defeat seam, tracks guardian reactor hits and destruction through a dedicated reactor-hit seam that frees occupied slots, switches Carnival team labeling to Red-vs-Blue ownership that matches the WZ `reactorRed` or `reactorBlue` split, mirrors the client-owned StringPool routing for request-failure (`0x101B`-`0x101F`), death (`0x1019`/`0x101A` with team labels `0x1017`/`0x1018`), member-out (`0x1029`/`0x102A`), and result (`0x1020`-`0x1023`) messages through the existing runtime seam, and in this pass now also reconciles guardian placements into real spawned ReactorPool entries so Carnival guardian reactors use the shared live reactor update, render, and attack-hit lanes instead of remaining logical-only placements. The shared reactor runtime now treats dynamically spawned reactors as first-class entries for lookup, rendering, and hit queries, and it also infers hit activation from authored reactor `hit` branches when Carnival reactors such as `9980000.img` / `9980001.img` omit an explicit `reactorType`, so live local attacks can advance or destroy the WZ-backed guardian reactor states and feed those callbacks back into the existing Monster Carnival guardian-hit seam. Focused runtime tests remain on disk for summon-count reconciliation, mob defeat or revive handling, guardian reactor teardown, StringPool-id-backed message selection, and the new dynamic reactor hit-query path. The runtime still exposes packet-shaped enter, request-result, request-failure, game-result, revive-death, CP-update, CP-delta, and summoned-mob-count handlers behind the same seam, and still auto-starts a dedicated loopback Monster Carnival packet inbox on Carnival maps so live raw packet types `346`-`353` can drain into that path with `/mcarnival raw ...` and `/mcarnival inbox ...` controls. The remaining gap is now narrower and centered on exact server ownership: the simulator still does not yet recover the exact localized StringPool text bytes themselves, integrate with true server-owned Monster Carnival transport beyond the loopback inbox, or decode the official server packet ownership and thresholds for the remaining guardian reactor choreography, so the guardian-hit flow now runs through true shared local reactor runtime ownership but not yet true live server packet decode | Monster Carnival is a self-contained field mode with client-owned rules and UI, and this row now carries the map-owned spawn-slot, reactor metadata, and packet-driven status-message selection deeper into live runtime ownership instead of stopping at CP counters and list entries: packet-owned summon counts now hydrate actual tracked spawn slots, WZ-authored revive chains and `getCP` rewards now feed live mob-defeat handling, guardian placements can now materialize as spawned Carnival reactors in the shared reactor runtime and transition through live hit/destroy callbacks, and the request/death/member-out/result status surface follows the client's recovered StringPool id map and argument ordering even though the exact localized literals and broader live transport are still outstanding | `MonsterCarnivalField.cs`, `MinigameFields.cs`, `MapSimulator.cs`, `MonsterCarnivalPacketInboxManager.cs`, `ReactorPool.cs`, `UnitTest_MapSimulator/MonsterCarnivalFieldTests.cs`, `UnitTest_MapSimulator/ReactorPoolDynamicBoundsTests.cs` (`CField_MonsterCarnival::CreateUIWindow`, `CField_MonsterCarnival::OnEnter`, `CField_MonsterCarnival::OnRequestResult`, `CField_MonsterCarnival::OnShowGameResult`, `CField_MonsterCarnival::OnProcessForDeath`, `CField_MonsterCarnival::OnShowMemberOutMsg`) |
| Partial | Party Raid field flow | The simulator now binds `FIELDTYPE_PARTYRAID`, `FIELDTYPE_PARTYRAID_BOSS`, and `FIELDTYPE_PARTYRAID_RESULT` into a dedicated `PartyRaidField` runtime, renders the field, boss, and result HUDs from the client WZ asset roots (`UI/UIWindow.img/PartyRace/*`, `UI/UIWindow.img/DualMobGauge/*`, `Map/Effect.img/praid/*`), fixes the field and boss point-vs-stage board placement to match the `PartyRace/Stage/backgrd` labels, mirrors the client's team-colored stage-state art, boss red-vs-blue gauge fill, map-owned clear or timeout result effect, and result summary overlay positions, now infers win-vs-fail result mode from the result maps' `info/onUserEnter` scripts (`PRaid_WinEnter`, `PRaid_FailEnter`) and constrains the result panel to the map's `LBSide/LBTop/LBBottom` safe area, carries a type-2 timer lifecycle through the runtime, now also swaps the old plain text countdown for the client `Map/Obj/etc.img/partyRace` timerboard with bitmap digits and separator, broadens the existing raw `field|party|session` key seam with numeric team aliases plus session outcome text handling, adds a WZ-backed `PartyRace/battery` field HUD plus battery charge-capacity aliases through the same raw key seam, adds a dedicated Party Raid loopback inbox that auto-starts on Party Raid maps, drains live `field|party|session|clock` lines from the simulator network path into the existing runtime seam, and exposes `/partyraid inbox [status|start|stop]` controls. In this pass, IDA re-checks against `CField_PartyRaid::Update` at `0x55c320` corrected a remaining field-stage ownership mismatch: the mine and other state badges now step horizontally on the client formulas (`mine x = 23*stage - 71`, `other x = 23*stage - 67`) instead of drifting vertically, while their lane rows stay fixed at the client `y` positions and the board continues to show the mine stage. The boss runtime also now prefers the client-owned boss template `9700037` when resolving the gauge-capacity fallback that `CField_PartyRaidBoss::OnFieldSetVariable` uses, instead of depending only on the map `life` list. Focused Party Raid parity tests were added on disk for horizontal mine/other stage stepping, clamped stage bounds, and script-owned result binding, but executing `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter PartyRaidFieldParityTests` is still blocked in this workspace by an unrelated pre-existing `HaCreator` compile error in `TemporaryPortalField.cs` (`RemoteOpenGateRuntime` missing). The row still does not decode the true client packet shape or exact server-owned Party Raid key names beyond the aliases recovered so far, and exact client literals or remaining boss-specific icon or layout ownership details are still not fully recovered. | Party Raid now covers the field battery surface, field board, client-horizontal mine-vs-other stage-state movement, boss gauge, result overlays, client timerboard, script-backed outcome selection, map-safe result placement, and a live loopback transport seam that feeds the existing runtime handlers. The remaining gap is now concentrated on exact packet-format and literal-key recovery plus the last boss-specific client ownership details rather than on missing field-stage motion or boss gauge-capacity ownership. | `SpecialFieldRuntimeCoordinator.cs`, `PartyRaidField.cs`, `MapSimulator.cs`, `PartyRaidPacketInboxManager.cs`, `UnitTest_MapSimulator/PartyRaidFieldParityTests.cs` (`CField_PartyRaid::_UpdatePoint`, `CField_PartyRaid::Update`, `CField_PartyRaid::Init`, `CField_PartyRaidBoss::Init`, `CField_PartyRaidBoss::OnFieldSetVariable`, `CField_PartyRaidBoss::OnPartyValue`, `CField_PartyRaidResult::Init`, `CField_PartyRaidResult::OnSessionValue`) |
| Implemented | SpaceGAGA timerboard flow | The simulator now binds `FIELDTYPE_SPACEGAGA` maps into a dedicated `SpaceGagaField`, mirrors the client's clock-type-2 timerboard ownership, keeps the board at the verified center-top window origin, removes the invented title banner, pins the timerboard source to `Map/Obj/etc.img/space` so the board comes from `space/backgrnd` and the bitmap timer digits come from `space/fontTime`, and no longer falls back to broad `UIWindow(.2).img` shape scans before dropping to the older procedural board or SpriteFont path. In this pass, WZ re-checks against the exported IMG filesystem kept that `space` subtree pinned to the exact `backgrnd` canvas plus `fontTime` digit and `comma` canvases, IDA re-checks kept the timer-font seam pinned to `CTimerboard_SpaceGAGA::GetFontTime` at `0x5629f0`, and direct `MapleStory.exe` table extraction closed the last StringPool gap: `ms_aString[0x140D]` was recovered as seed `0x44` plus encoded bytes `44 C3 06 CD 1C 7C 6C 18 C2 08 93 3B 4A 0F 27 50 31 FD 17 DC 50 56 21 10 8C 0E 8C 3F 16 08 2E`, which decodes to `Map/Obj/etc.img/space/backgrnd` through `CTimerboard_SpaceGAGA::OnCreate`, and `ms_aString[0x140E]` was recovered as seed `0x45` plus encoded bytes `45 51 AE 0A 49 29 7E 8F F5 BE BA D3 E6 A5 F9 09 12 6F BF 1B 05 03 33 83 B5 B5 BA E4 A1 A1 F1`, which decodes to `Map/Obj/etc.img/space/fontTime` through `GetFontTime`. The `/spacegaga` status seam now reports those byte-accurate client entries instead of inferred-only mappings, and focused `SpaceGagaField` tests on disk cover type-2 clock ownership, ignored non-type-2 clocks, reset behavior, and the recovered StringPool evidence through that runtime seam | This is another example of client-owned special field logic that would otherwise be lost under generic map handling, and the simulator now carries the SpaceGAGA timerboard end-to-end with fixed WZ-backed art, verified ownership for both client StringPool ids, and byte-accurate recovered `ms_aString` evidence rather than inferred-only path mapping | `SpecialEffectFields.cs`, `UnitTest_MapSimulator/SpaceGagaFieldTests.cs`, `SpaceGagaField` (`CField_SpaceGAGA::OnClock`, `CTimerboard_SpaceGAGA::Draw`, `CTimerboard_SpaceGAGA::OnCreate`, `CTimerboard_SpaceGAGA::GetFontTime`, `StringPool::GetString`) |

### 2. Additional packet-owned microgame dialog discovered by follow-up IDA scan

The broader packet-owner pass in backlog 7 already surfaced `CRPSGameDlg::OnPacket`, but it is still missing from the dedicated minigame plan even though it fits this area more closely than the generic field-utility bucket.
A targeted decompile now makes that owner more concrete: `CRPSGameDlg::OnPacket` is not a single opaque callback. Packet subtype `8` first enforces the unique-modeless owner gate, decodes a `uint32` value, opens a `CUtilDlgEx` notice using `StringPool` id `0xE83`, and only then constructs the dialog owner; subtypes `6`, `7`, `9`, `10`, `11`, `12`, and `14` require an already-live `CRPSGameDlg` unique-modeless owner and forward into `ProcessPacket`; and subtype `13` tears the owner down through `CWnd::Destroy`. The same `ProcessPacket` decompile also shows more exact gameplay ownership than the backlog was tracking: `9` and `12` start a fresh round by clearing NPC choice, enabling the three RPS buttons, setting the switch cadence to `120`, and arming a `30000` ms time limit; `11` decodes the NPC pick plus straight-win count and marks the compensation flag when the decoded count goes negative while the current streak is still zero; `10` forces immediate result presentation by clearing the switch cadence, setting the streak to `-1`, and calling `ShowResult`; and `6`/`7`/`14` reset the round state, re-enable the main button, and route the completion notice through `StringPool` ids `3724` or `3723`.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Rock-Paper-Scissors dialog parity | `CRPSGameDlg::OnPacket` at `0x6d9e00` is a dedicated packet-owned microgame dialog owner rather than a generic field notice, and a targeted decompile now shows that it owns a broader lifecycle than the backlog previously captured. Packet subtype `8` is the client-owned entry gate: it requires that no other unique-modeless owner is active, decodes a `uint32` payload, opens a `CUtilDlgEx` notice through `StringPool` id `0xE83`, and only then constructs `CRPSGameDlg`. Subtypes `6`, `7`, `9`, `10`, `11`, `12`, and `14` require that the active unique-modeless owner is already a `CRPSGameDlg` and then dispatch into `ProcessPacket`, while subtype `13` destroys that owner. `ProcessPacket` is also more specific than a generic minigame packet router: `9` and `12` start a live round by clearing the NPC choice, enabling the three RPS buttons, setting `m_tSwitchingTerm = 120`, and arming a `30000` ms round limit; `11` decodes the NPC pick plus straight-win count and derives the compensation flag when the decoded streak goes negative while the previous streak was still zero; `10` forces result presentation immediately by clearing switching, setting the streak to `-1`, and calling `ShowResult`; and `6`/`7`/`14` reset the round state, restore the main button, and route completion notices through `StringPool` ids `3724` or `3723`. A fresh repo search across `HaCreator/MapSimulator` still does not surface any RPS runtime, dialog owner, or packet bridge, so this owner remains genuinely absent rather than merely under-documented. | If RPS stays buried only inside the broad field-utility packet row, minigame parity will look more complete than it is while one of the client-owned dialog games remains totally untracked. The new decompile detail also makes the missing work actionable: this is not only “add an RPS window,” it is a packet-owned unique-modeless lifecycle with an entry notice, explicit create/destroy packets, a 30-second round timer owner, NPC-choice/result state, and completion-notice routing that belongs beside `CMemoryGameDlg`, not only inside the miscellaneous `CField::OnPacket` bucket. | packet-owned microgame dialog layer (`CRPSGameDlg::OnPacket`, `CRPSGameDlg::ProcessPacket`, `CUtilDlgEx`, `CRPSGameDlg::ShowResult`) |

### 3. Additional client-owned field wrappers discovered by IDA function scan

The live IDA `list_functions` scan surfaced more field-owner classes with no hits anywhere else in the current backlog set.
These started as scan-only seams, but a follow-up pass against the current simulator code shows that some of them are now represented locally and should no longer stay marked as missing just because the older backlog text was never refreshed.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Transit and voyage field wrappers | The simulator now treats `shipObj` as a client-owned map contract instead of depending only on editor `ShipObject` instances: a dedicated `TransportationFieldDefinitionLoader` reads copied WZ `shipObj` data from `MapInfo.additionalNonInfoProps`, normalizes both `Map/Obj/vehicle.img/...` and `Map/Obj/vehicle/...` object paths, and lets `DetectAndInitializeTransportField` initialize transit and Balrog voyage maps from the same map-owned payload the client uses. `TransportationField` also now applies a client-owned initial wrapper state so regular `FIELDTYPE_CONTIMOVE` routes start docked at `x` instead of idling offscreen at `x0`, while Balrog-owned wrappers stay hidden until their appear branch runs, matching the dedicated `CField_ContiMove` and `CField_Balrog` ownership more closely. In this pass the live packet seam moved out of the local-only runtime hooks and into a dedicated transport inbox: a new `TransportationPacketInboxManager` auto-starts on transit and voyage wrapper maps, accepts client-shaped alias lines for `OnStartShipMoveField`, `OnMoveField`, `OnEndShipMoveField`, and `OnContiState`, and also accepts decrypted Maple raw packets for the exact recovered `CField_ContiMove::OnPacket` opcodes `164` (`OnContiMove`) and `165` (`OnContiState`). The simulator update loop now drains that inbox through one transport-owned dispatcher that mirrors the client switch shape recovered in IDA: opcode `164` only reacts to subtypes `8`, `10`, and `12`, while opcode `165` applies the two-byte state/value pair through the existing `TransportationField` client-branch helpers. A focused `/transport` debug seam now reports runtime plus inbox status and can inject those same packet-shaped payloads without bypassing the transport dispatcher, and focused transport tests were expanded to cover the new alias/raw decode plus the Balrog-appear and dock-leave packet branches. The remaining gap is now narrower and explicit: the simulator still does not own an official-session proxy or in-place socket attachment for transport packets, and the separate voyage-attack/server-timed Balrog event choreography is still limited to the local runtime seam rather than a recovered live packet owner. | Ship travel and Balrog transport are client-owned field experiences with their own packet/state choreography, and this row now covers both the WZ-authored route contract and a dedicated packet inbox for the recovered `CField_ContiMove` handlers instead of leaving transport state changes trapped behind ad hoc local calls. The remaining work is no longer the basic `OnStartShipMoveField`/`OnEndShipMoveField`/`OnContiState` feed; it is specifically true official-session ownership for transport packets plus whatever separate voyage-attack or Balrog-timing events the server drives outside the recovered `164`/`165` path. | transit or voyage field runtime (`CField_ContiMove`, `CField_Balrog`) |
| Partial | Tutorial, camera-mask, and presentation wrappers | The simulator now has a real wrapper seam for most of the client owners that originally surfaced in the scan. `MapSimulator.ClientOwnedFieldWrappers.cs` mirrors `CField_Tutorial::DecodeFieldSpecificData` by forcing the same tutorial appearance pair recovered in IDA (`1002562` cap and `1052081` longcoat) while preserving the active weapon slot, enables a dedicated limited-view mask on `FIELDTYPE_LIMITEDVIEW` maps using `Effect/MapEff.img/Viewrange/0` metadata instead of a simulator-only debug radius, suppresses dragon presentation when either `fieldType = FIELDTYPE_NODRAGON` or map `info/vanishDragon` says the client should hide it, and now exposes wedding-photo wrapper activation plus safe-area reporting through the same map-owned wrapper status path. The remaining gap is narrower: `CField_AranTutorial::GetFieldType` is still only represented as part of the broader tutorial wrapper classification rather than its own differentiated runtime, `CField_WeddingPhoto` is currently tracked as a presentation wrapper rather than a bespoke ceremony/photo scene owner, and the exact client-owned `CField_LimitedView::Update` behavior beyond the recovered init/mask metadata still is not mirrored byte for byte. | These owners are no longer invisible backlog debt because the simulator already treats tutorial appearance forcing, limited-view masking, dragon suppression, and wedding-photo wrapper detection as explicit client-owned presentation seams instead of leaving them buried under generic map load logic. The remaining work is now about finer-grained owner behavior and variant differentiation, not total absence. | tutorial or scripted field layer (`MapSimulator.ClientOwnedFieldWrappers.cs`, `LimitedViewField.cs`, `CField_Tutorial::DecodeFieldSpecificData`, `CField_AranTutorial::GetFieldType`, `CField_LimitedView::Init`, `CField_LimitedView::Update`, `CField_NoDragon::Init`, `CField_WeddingPhoto::GetFieldType`) |
| Implemented | Result-board and field-variable special wrappers | The missing IDA-owned wrapper surfaces are now covered in the simulator runtime: `CField_KillCount::OnPacket` packet `178` drives a dedicated kill-count overlay, `CField_HuntingAdballoon::OnFieldSetVariable` reuses the existing party-raid boss field-variable HUD with an explicit client-owned bind, `CField_MassacreResult::OnPacket` packet `174` now decodes the client rank/value payload into the existing Massacre result board, `CField_EscortResult::OnSessionValue` fail values arm a timed fail overlay, and `CField_ShowaBath::DecodeFieldSpecificData` now forces the gendered robe appearance recovered from the client and WZ (`1050100` / `1051098`). The field-state command surface also accepts the new packet ids and wrapper value injection for verification. | The result/status wrappers that contextualize these minigame endings are now represented as concrete simulator runtime owners instead of being left as untracked client-only behavior. | event result or field-variable layer (`CField_KillCount`, `CField_HuntingAdballoon`, `CField_MassacreResult`, `CField_EscortResult`, `CField_ShowaBath`) |
| Partial | Tournament and Monster Carnival variant wrappers | WZ re-checks against the exported IMG filesystem kept this row anchored to the same client-owned field seams instead of generic map tags: the MapleStory Minigame Challenge lobby `Map/Map1/109070000` still carries `info/fieldType = 3` and `String/Map.img/victoria/109070000/help0` still describes the tournament bracket, resting period, prize podium, and five-minute post-finals exit flow, while Carnival variant maps still expose the same `monsterCarnival/mapType` seam that the client wrappers read directly (`Map/Map9/980031000` fieldType `16` with `mapType = 0`, `980032000` fieldType `16` with `mapType = 1`, `980033000` fieldType `16` with `mapType = 2`). IDA re-checks kept the client side equally narrow and concrete: `CField_MonsterCarnivalWaitingRoom::Init` and `CField_MonsterCarnivalS2_Game::Init` still do nothing deeper than read `monsterCarnival/mapType`, `CField_MonsterCarnivalRevive::OnShowGameResult` still routes revive results through the same `0x1020`-`0x1023` StringPool ids, and `CField_Tournament::OnPacket` still dispatches only `374` through `378` into notice, match-table dialog, prize, UEW, and no-op branches. In this pass the simulator's Carnival runtime now reports the concrete client owner label alongside its variant and `mapType` so waiting-room, revive, and season-2 maps no longer appear as anonymous shared Carnival states, and the missing Tournament source seam was actually brought on disk: a dedicated `TournamentField` now activates on `FIELDTYPE_TOURNAMENT`, surfaces a tournament status panel in the existing minigame seam, accepts raw and opcode-wrapped packet handling for the exact client packet family (`374`-`378`), preserves the recovered StringPool ids for the notice (`0x3A3`-`0x3A7`), prize (`0x3A8`-`0x3AA`), and UEW (`0x9F6`-`0x9F8`) branches, records match-table payload handoff as the client-owned `CMatchTableDlg` path, exposes `/tournament [status|raw|packetraw|inbox]`, auto-starts a dedicated loopback tournament inbox on tournament maps, and adds focused parity tests for tournament packet decoding and inbox alias parsing in `UnitTest_MapSimulator/TournamentFieldParityTests.cs`. The remaining gap is narrower but still real: Carnival waiting-room/revive/season-2 maps still share one broader Carnival HUD and do not yet reproduce distinct client dialog or session choreography beyond owner labeling, and Tournament still stops at packet-family ownership plus local presentation instead of the full bracket dialog/session transport lifecycle or an official-session proxy. | These variant owners are now explicitly represented in the simulator runtime rather than only implied by stale backlog prose. The remaining work is no longer discovering the wrappers or leaving tournament traffic totally unowned; it is the deeper per-variant UI and session behavior that the client owns beyond the already recovered `fieldType`, `mapType`, StringPool ids, loopback inbox, and packet dispatch seams. | mode-variant field runtime (`MonsterCarnivalField.cs`, `TournamentField.cs`, `TournamentPacketInboxManager.cs`, `MapSimulator.ChatCommands.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/TournamentFieldParityTests.cs`, `CField_MonsterCarnivalRevive::OnShowGameResult`, `CField_MonsterCarnivalWaitingRoom::Init`, `CField_MonsterCarnivalS2_Game::Init`, `CField_Tournament::OnPacket`, `CField_Tournament::OnTournament`, `CField_Tournament::OnTournamentMatchTable`, `CField_Tournament::OnTournamentSetPrize`, `CField_Tournament::OnTournamentUEW`) |

## Priority Order

If the goal is visible parity first, the next work in this area should be sequenced like this:

1. Existing-special-field refinement pass:
   Tighten Wedding, Coconut, Guild Boss, Massacre, SnowBall, and Ariant behavior against the client owners that already have simulator counterparts.
2. Missing microgame dialog pass:
   Add the still-untracked `CRPSGameDlg` Rock-Paper-Scissors owner before more packet-owned minigame work gets buried in the generic field-utility bucket.
3. Variant and packet/HUD refinement pass:
   Continue deeper Monster Carnival, Party Raid, Tournament, SpaceGAGA, and MiniRoom packet/UI parity now that those runtime shells already exist.
4. Wrapper and transport pass:
   Triage the transit, tutorial/presentation, result-wrapper, and Monster Carnival variant owners while closing the remaining official-session ownership gaps that still sit behind loopback inboxes or proxy seams.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
