# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/MapSimulator.cs`
- `HaCreator/MapSimulator/Effects/SpecialEffectFields.cs`
- `HaCreator/MapSimulator/Fields/MinigameFields.cs`

### Client references checked

- `CField_GuildBoss::CField_GuildBoss` at `0x53e980`
- `CField_Coconut::OnClock` at `0x54a100`
- `CField_Coconut::OnCoconutHit` at `0x54a470`
- `CField_Coconut::BasicActionAttack` at `0x54a5e0`
- `CField_Coconut::DrawBoard` at `0x54a750`
- `CField_Coconut::OnCoconutScore` at `0x54bf70`
- `CField_AriantArena::OnUserScore` at `0x5492b0`
- `CField_AriantArena::OnShowResult` at `0x547630`
- `CField_Battlefield::OnClock` at `0x549ad0`
- `CField_Battlefield::OnScoreUpdate` at `0x5499a0`
- `CField_Battlefield::OnTeamChanged` at `0x5499e0`
- `CField_Battlefield::SetUserTeam` at `0x549870`
- `CField_Dojang::OnClock` at `0x550940`
- `CField_Dojang::Update` at `0x54ef10`
- `CField_CookieHouse::_UpdatePoint` at `0x54daa0`
- `CField_CookieHouse::Init` at `0x54e250`
- `CField_Massacre::OnClock` at `0x556af0`
- `CField_Massacre::UpdateKeyAnimation` at `0x556bf0`
- `CTimerboard_Massacre::Draw` at `0x557100`
- `CField_Massacre::Init` at `0x5579d0`
- `CMemoryGameDlg::OnButtonClicked` at `0x628df0`
- `CMemoryGameDlg::OnCreate` at `0x62f3c0`
- `CMemoryGameDlg::Draw` at `0x631cb0`
- `CField_MonsterCarnival::CreateUIWindow` at `0x55a510`
- `CField_MonsterCarnival::OnEnter` at `0x55a6c0`
- `CField_MonsterCarnival::OnRequestResult` at `0x55a890`
- `CField_MonsterCarnival::OnShowGameResult` at `0x55af80`
- `CField_MonsterCarnivalRevive::OnShowGameResult` at `0x55b110`
- `CField_PartyRaid::_UpdatePoint` at `0x55c120`
- `CField_PartyRaid::Init` at `0x55f110`
- `CField_PartyRaidBoss::OnFieldSetVariable` at `0x55ca40`
- `CField_PartyRaidBoss::OnPartyValue` at `0x55d070`
- `CField_PartyRaidResult::OnSessionValue` at `0x55e5c0`
- `CField_SnowBall::OnSnowBallTouch` at `0x560510`
- `CField_SnowBall::OnSnowBallState` at `0x560ab0`
- `CField_SnowBall::BasicActionAttack` at `0x5617b0`
- `CField_SnowBall::OnSnowBallHit` at `0x5619d0`
- `CField_SnowBall::OnSnowBallMsg` at `0x562040`
- `CField_SpaceGAGA::OnClock` at `0x5625d0`
- `CTimerboard_SpaceGAGA::Draw` at `0x5626c0`
- `CField_Wedding::SetBlessEffect` at `0x563b60`
- `CField_Wedding::OnWeddingProgress` at `0x5640f0`
- `CField_Witchtower::OnScoreUpdate` at `0x564ad0`
- `CScoreboard_Witchtower::Draw` at `0x564bd0`

## Client Function Index By Backlog Area

The list below maps this backlog area to concrete client seams confirmed in IDA.
These are the first functions to inspect before changing simulator behavior.

### 1. Special field and minigame parity

- `CField_GuildBoss::CField_GuildBoss` at `0x53e980`
- `CField_Coconut::OnClock` at `0x54a100`
- `CField_Coconut::OnCoconutHit` at `0x54a470`
- `CField_Coconut::BasicActionAttack` at `0x54a5e0`
- `CField_Coconut::DrawBoard` at `0x54a750`
- `CField_AriantArena::OnUserScore` at `0x5492b0`
- `CField_AriantArena::OnShowResult` at `0x547630`
- `CField_Battlefield::OnClock` at `0x549ad0`
- `CField_Battlefield::OnScoreUpdate` at `0x5499a0`
- `CField_Battlefield::OnTeamChanged` at `0x5499e0`
- `CField_Battlefield::SetUserTeam` at `0x549870`
- `CField_Dojang::OnClock` at `0x550940`
- `CField_Dojang::Update` at `0x54ef10`
- `CField_CookieHouse::_UpdatePoint` at `0x54daa0`
- `CField_CookieHouse::Init` at `0x54e250`
- `CField_Wedding::OnWeddingProgress` at `0x5640f0`
- `CField_Wedding::SetBlessEffect` at `0x563b60`
- `CField_Witchtower::OnScoreUpdate` at `0x564ad0`
- `CScoreboard_Witchtower::Draw` at `0x564bd0`
- `CField_Massacre::OnClock` at `0x556af0`
- `CField_Massacre::UpdateKeyAnimation` at `0x556bf0`
- `CTimerboard_Massacre::Draw` at `0x557100`
- `CMemoryGameDlg::OnButtonClicked` at `0x628df0`
- `CMemoryGameDlg::OnCreate` at `0x62f3c0`
- `CMemoryGameDlg::Draw` at `0x631cb0`
- `CField_SnowBall::OnSnowBallState` at `0x560ab0`
- `CField_SnowBall::OnSnowBallHit` at `0x5619d0`
- `CField_SnowBall::OnSnowBallMsg` at `0x562040`
- `CField_SnowBall::OnSnowBallTouch` at `0x560510`
- `CField_MonsterCarnival::CreateUIWindow` at `0x55a510`
- `CField_MonsterCarnival::OnEnter` at `0x55a6c0`
- `CField_PartyRaid::Init` at `0x55f110`
- `CField_PartyRaidBoss::OnFieldSetVariable` at `0x55ca40`
- `CField_PartyRaidResult::OnSessionValue` at `0x55e5c0`
- `CField_SpaceGAGA::OnClock` at `0x5625d0`
- `CTimerboard_SpaceGAGA::Draw` at `0x5626c0`

Notes:
IDA shows that these field modes are not generic map-rule variants. The client owns separate state machines, packet handlers, timer or scoreboard widgets, and even dedicated minigame dialog owners such as `CMemoryGameDlg` for Guild Boss, Coconut, Wedding, Witchtower, Massacre, SnowBall, Ariant Arena, Battlefield, Dojang, Cookie House, Monster Carnival, Party Raid, SpaceGAGA, and MiniRoom card games, so parity work should be tracked separately from the broader field, combat, or UI backlog areas.

## Current State Summary

MapSimulator already has a partial special-field baseline instead of a blank area.
The project currently includes:

- A `SpecialEffectFields` surface with wedding, witchtower, guild-boss, and massacre handling.
- A concrete `MinigameFields` surface that now owns the snowball and coconut runtimes behind one backlog-aligned seam.
- A `SpecialFieldRuntimeCoordinator` seam in `MapSimulator` that binds special-field ownership to map load, update, draw, and reset flow so later agents can take one backlog row without redoing the simulator plumbing.
- Several packet-commented simulator seams that already reference the corresponding client field owners.

The parity gap here is not whether the simulator recognizes special maps at all.
The gap is whether those maps reproduce the client's dedicated rules, scoreboards, timers, UI flows, and win or lose transitions.

## Remaining Parity Backlog

### 1. Special field and minigame parity

This area is currently unowned by backlog documents even though the client and simulator both have dedicated field-specific seams.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Guild boss event fields | The simulator now routes special-field ownership through `SpecialFieldRuntimeCoordinator`, auto-binds guild-boss maps into that dedicated runtime seam, configures `GuildBossField` directly from the map's `healer` and `pulley` WZ nodes, loads the exact `Map/Obj/guild.img/syarenian/boss/{0,1}` animated art with origin-aligned placement, exposes both `/guildboss packet <344|345> ...` and `/guildboss packetraw <hex>` debug seams, decodes those payloads through a dedicated runtime packet path that mirrors `CField_GuildBoss::OnPacket`, applies `CHealer::Move` with the client's immediate absolute-Y result instead of simulator smoothing, cancels packet-owned preview state whenever an external healer or pulley packet arrives, mirrors `CField_GuildBoss::BasicActionAttack` more closely by driving the local pulley from the player's normal-attack hitbox instead of `Up`, replays a local `CPulley::Hit`-style animation restart, and now auto-runs a dedicated guild-boss transport seam on guild-boss maps so live socket peers can still exchange the old text commands while also feeding decrypted Maple packet frames through `packetraw <hex>` into the same runtime path for packet types `344/345`. In this pass, WZ re-checks against the exported `v115` IMG filesystem confirmed the guild-boss art still lives at `Map/Obj/guild.img/syarenian/boss/{0,1}` with the expected origin-backed animation frames, and IDA recovery of `CField_GuildBoss::BasicActionAttack` at `0x5517d0` pinned the outbound pulley request to a bare `COutPacket(259)` with no extra payload, so the transport now emits that literal decrypted Maple packet frame (`0301`) alongside the legacy `pulleyhit` bridge line for older tools; when that transport has a live peer, the local pulley attack path stops inventing the old simulator-owned preview state and waits on transport-authored replies instead. The remaining gap is now almost entirely transport ownership: the simulator still does not hook into the real MapleStory socket session or decrypt live guild-boss traffic on its own, so even though the outbound request shape is now client-accurate, it is still only emitted through the simulator bridge rather than a true official session | This item now matches the full client-owned guild-boss seam confirmed in IDA more closely: `CField_GuildBoss` only initializes healer plus pulley, consumes packets `344/345`, and uses `BasicActionAttack` to hit the pulley and send a no-payload opcode-`259` request while idle. This pass removes the last packet-shape guess in the guild-boss row and leaves the remaining work concentrated on true official-session ownership rather than unknown outbound packet recovery | `MapSimulator.cs`, `SpecialFieldRuntimeCoordinator.cs`, `SpecialEffectFields.cs`, `GuildBossPacketTransportManager.cs`, `UnitTest_MapSimulator/GuildBossPacketTransportManagerTests.cs`, `GuildBossField` (`CField_GuildBoss`, `CField_GuildBoss::Init`, `CField_GuildBoss::OnPacket`, `CField_GuildBoss::BasicActionAttack`, `CHealer::Init`, `CHealer::Move`, `CPulley::Init`, `CPulley::Hit`) |
| Partial | Coconut minigame runtime | The simulator now follows the client's finish-timestamp clocking, keeps score packet-owned instead of inventing local score on ground contact, applies packet-driven coconut state changes through the runtime's delayed hit queue, mirrors `CField_Coconut::BasicActionAttack` by selecting an intersecting coconut from the player's normal-attack world hitbox before queuing the delayed state change, and no longer leaves that request as simulator-only pending state: the runtime now tracks whether each pending Coconut attack has been transport-dispatched, auto-flushes unsent requests through the dedicated Coconut transport once a bridge client is connected, and emits both a compatibility `attack ...` line and the IDA-recovered decrypted Maple outbound packet shape `packetoutraw <hex>` for opcode `257` with `<targetId><delay>` payload fields. It still draws the client board from `Map/Obj/etc.img/coconut/backgrnd`, renders the client bitmap score and time fonts at the IDA-verified `DrawBoard` positions, keeps the round in an awaiting-result state after the clock expires until a score packet lands instead of locally inventing a final result from a grace timeout, shows the WZ-backed victory or lose banner from `Map/Effect.img/event/coconut` once that packet-owned result resolves, and now accepts opcode-wrapped decrypted Maple inbound Coconut packets through both the auto-started Coconut inbox transport and `/coconut raw packetraw <hex>` in addition to the older typed `hit` or `score` feeds. The remaining gap is the last transport hop: the simulator still does not hook directly into the live MapleStory socket session, decrypt traffic on its own, or inject opcode `257` onto a real client socket without an external bridge, so Coconut result and hit ownership still depend on a transport peer forwarding real server-authored packets into this seam rather than the simulator attaching to the official session itself | Coconut is another field mode with its own client-owned action loop, not a variant of ordinary combat or map-object handling, and this row now narrows the remaining work to the final official-session bridge instead of leaving the outbound request opcode and inbound raw-packet ownership unresolved | `MinigameFields.cs`, `MapSimulator.cs`, `MapSimulator.UpdateLoop.cs`, `MapSimulator.ChatCommands.cs`, `CoconutPacketInboxManager.cs`, `UnitTest_MapSimulator/CoconutTransportTests.cs`, `CoconutField` (`CField_Coconut::OnClock`, `CField_Coconut::OnCoconutHit`, `CField_Coconut::BasicActionAttack`, `CField_Coconut::DrawBoard`, `CField_Coconut::OnCoconutScore`) |
| Partial | Wedding ceremony fields | The simulator now resolves ceremony lines from `String/Npc.img/<npcId>/wedding{step}` with WZ-backed fallbacks, distinguishes groom or bride versus guest state from the local character id, mirrors the chapel-only step-2 guest bless prompt that `CField_Wedding::OnWeddingProgress` special-cases, records the client's modal response packet flow through step-advance (`163`) and guest-bless (`164`) ceremony responses, exposes a `/wedding` debug seam to drive progress, actor anchors, and responses through that same runtime, loads the real bless animation frames from `Effect/BasicEff.img/Wedding` while keeping that effect alive until the same client-owned clear path disables it instead of timing it out locally, swaps BGM to the WZ-backed `Sound/BgmEvent.img/wedding` track from the same step-0 ceremony seam the client uses, restores the map BGM when the wedding runtime clears, draws the WZ-backed declaration overlay from `UI/UIWindow.img/wedding/text/0` during the opening step, now also promotes the later pronouncement and blessing steps into a second WZ-backed ceremony scene layer from `UI/UIWindow.img/wedding/card/0` with the existing wedding weather celebration path, mirrors a first cut of the client-owned remote couple presentation by materializing groom and bride actor overlays from the wedding runtime itself, anchored to the same participant positions the step flow already tracks while suppressing the local participant so that only remote ceremony actors are drawn, and in this pass lets that same remote couple overlay accept per-participant AvatarLook-backed presentation plus explicit action and facing updates through `/wedding actor avatar ...` and `/wedding actor ...` without losing those overrides when later ceremony progress packets resync the step runtime. The remaining gap is narrower but still real: the simulator still does not have the full client `CUserPool` wedding audience, does not decode or ingest the official live remote wedding movement or look packets on its own, and the ceremony scene choreography is still limited to the recovered wedding text or card overlays plus particle layers rather than a fully decoded client timeline | Wedding maps now cover both recovered WZ ceremony overlays and a dedicated wedding-side remote couple overlay inside the existing runtime seam, and that overlay can now be driven by AvatarLook-backed groom or bride presentation rather than only cloned-local placeholders, so the remaining work is concentrated on true user-pool ownership, official remote packet feeds, and any additional client-only ceremony sequencing beyond the WZ-backed layers already identified | `SpecialEffectFields.cs`, `WeddingField`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `UnitTest_MapSimulator/WeddingFieldTests.cs` (`CField_Wedding::OnWeddingProgress`, `CField_Wedding::SetBlessEffect`) |
| Implemented | Witchtower score UI | The simulator anchors the Witchtower scoreboard to the client's center-top window origin, loads the exact `Map/Obj/etc.img/goldkey` background, key, and bitmap digits that match the `CScoreboard_Witchtower::OnCreate` asset shape, renders the zero-padded score at the verified `(67, 4)` draw origin with the client's negative digit spacing, and keeps the `witchscore` debug seam driving the same update path | This is a visible special-field HUD with client-backed art and placement now, so the remaining special-field work should move to other rows instead of revisiting a placeholder Witchtower overlay | `SpecialEffectFields.cs`, `WitchtowerField`, `MapSimulator.cs` (`CField_Witchtower::OnScoreUpdate`, `CScoreboard_Witchtower::OnCreate`, `CScoreboard_Witchtower::Draw`) |
| Partial | Massacre timerboard and gauge flow | The simulator now mirrors the client's type-2 timerboard ownership at the verified center-top origin, draws a dedicated MM:SS timer surface instead of a generic corner clock, keeps the timer digits alive even without a fallback `SpriteFont`, probes the loaded `UIWindow(.2).img` trees for a real `258x61` timerboard source canvas before falling back to the older `MonsterKilling/Count/backgrd{0,1}` approximation, advances the real three-stage `UI/UIWindow(.2).img/MonsterKilling/Count/keyBackgrd/{open,ing,close}` pulse whenever the gauge increases, renders the Massacre gauge from `MonsterKilling/Gauge` using the client's `259`-pixel fill span and the `_SetDecGauge` danger cutoff at roughly `65%` depletion instead of the older hardcoded low-gauge heuristic, applies `mobMassacre` map defaults for `gauge.total`, `gauge.decrease`, `gauge.hitAdd`, and `disableSkill` instead of hardcoded HUD values, surfaces the map's `countEffect` thresholds as next-milestone plus reached-threshold HUD state, and now replays a dedicated WZ-backed milestone presentation from `Map/Effect.img/killing/first/{start,stage}` plus `Map/Effect.img/killing/number/<stage>` when those thresholds are reached instead of leaving them as banner text only. It also still replays the clear screen effect at the client's `timer <= 1` update seam without auto-inventing a local clear or fail result board, and still exposes `/massacre bonus` plus `/massacre result ...` seams to replay the WZ-backed presentation layer. The remaining gap is narrower but still real: the exact `CTimerboard_Massacre::OnCreate` string-pool source canvas and timer-font paths are still not pinned to one literal WZ node, the stage-presentation trigger is still local threshold inference rather than decoded packet ownership, and the true packet-fed result choreography is still not decoded, so the result board's score, rank, and clear or fail outcome remain simulator or debug authored rather than server authored | Hunting-event maps depend on dedicated timer and gauge behavior that the client does not route through the normal status bar, and the simulator now follows the client more closely on threshold-stage presentation as well as timer-expiry presentation, danger-threshold math, and timerboard asset sourcing while leaving the remaining work concentrated on exact literal asset-path confirmation plus packet-owned count-effect and result choreography | `SpecialEffectFields.cs`, `MassacreField`, `MapSimulator.cs`, `UnitTest_MapSimulator/MassacreFieldTests.cs` (`CField_Massacre::Init`, `CField_Massacre::OnClock`, `CField_Massacre::UpdateKeyAnimation`, `CTimerboard_Massacre::Draw`) |
| Partial | Memory Game and MiniRoom card parity | The simulator now syncs the MiniRoom shell occupant roster and status text to the live Match Cards board, accepts mouse-driven sidebar and card hit testing on the `CMemoryGameDlg`-style surface, exposes delayed remote ready/start/flip/tie/give-up/end actions through the same runtime path that the local room uses, adds both a typed `MemoryGamePacketType` dispatch seam behind `/memorygame packet ...`, a raw MiniRoom payload seam behind `/memorygame packetraw ...`, and an opcode-wrapped receive seam behind `/memorygame packetrecv <opcode> <payload>`, decodes the client-backed MiniRoom gameplay subtypes (`6 -> 50/51/58/59/61/62/63/68`) plus the shared MiniRoom base enter, chat, avatar, and leave families (`4/7/8/9/10`) into packet-owned roster, visitor, avatar-summary, chat-log, and leave handling, applies packet-owned board shuffle data from `CMemoryGameDlg::OnUserStart`, upgrades the shared MiniRoom shell with a dedicated Match Cards chat log plus system or speaker message presentation instead of the old generic preview-only pane, preserves visitor leave packets without tearing down the active board while surfacing slot-backed job or face or hair summaries in the room roster, now auto-starts a dedicated loopback MiniRoom packet inbox so live raw payloads or opcode-wrapped receive lines can drain through the same runtime seam with `/memorygame inbox ...` controls, normalizes two-byte Maple opcode wrappers before dispatch so externally captured server receive packets can reuse the same `CMemoryGameDlg` packet path, maps the `CMiniRoomBaseDlg::MakeGameMessage` codes onto the client StringPool ids recovered from IDA while surfacing those ids in system messages, and renders decoded `AvatarLook` payloads as actual occupant avatar previews inside the MiniRoom shell by rebuilding `CharacterBuild` previews through the existing avatar loader path. The remaining gap is narrower: the simulator still does not hook directly into the real MapleStory socket session or decrypt and peel MiniRoom traffic from the live game connection on its own, and the special game-message strings are still fallback English text annotated with the recovered client StringPool ids instead of byte-for-byte localized StringPool literals | MiniRoom parity now covers the room shell, board state machine, click flow, raw gameplay-packet decode, opcode-wrapped receive-packet normalization, packet-owned start-board data, the shared base enter/chat/avatar/leave families, a live loopback MiniRoom transport seam, StringPool-id-backed game-message presentation, and real remote-avatar previews inside the shared shell, so the remaining work is concentrated on exact localized StringPool extraction plus true live-session transport ownership instead of missing core Match Cards ownership | `MinigameFields.cs`, `SocialRoomModels.cs`, `SocialRoomWindow.cs`, `MapSimulator.cs`, `MemoryGamePacketInboxManager.cs`, `LoginAvatarLookCodec.cs`, `UnitTest_MapSimulator/MemoryGameFieldPacketTests.cs` (`CMemoryGameDlg::OnCreate`, `CMemoryGameDlg::Draw`, `CMemoryGameDlg::OnPacket`, `CMemoryGameDlg::OnUserStart`, `CMemoryGameDlg::OnTurnUpCard`, `CMemoryGameDlg::OnTimeOver`, `CMemoryGameDlg::OnGameResult`, `CMiniRoomBaseDlg::OnPacketBase`, `CMiniRoomBaseDlg::OnChat`, `CMiniRoomBaseDlg::OnEnterBase`, `CMiniRoomBaseDlg::OnAvatar`, `CMiniRoomBaseDlg::MakeGameMessage`) |
| Partial | Snowball minigame runtime | The simulator now treats Snowball state `2/3` as the client's packet-owned win states instead of local touch-zone states, keeps snowman HP packet-owned via `OnSnowBallState`, applies WZ-backed snowman stun windows and damage defaults through the runtime seam, smooths active snowball movement from the packet position plus speed data instead of instant teleports, routes the local player's world position through `SpecialFieldRuntimeCoordinator` so `SnowBallField` can surface the client's outbound touch-packet loop as pending team touch requests whenever the local player overlaps a snowball lane, aligns the Story-vs-Maple team labeling used by win banners and the local scoreboard with the client message owner, and now routes `OnSnowBallMsg` through the simulator chat log seam with client-shaped team ordering plus packet-owned fallback text instead of showing those packet messages as center-screen overlays. In this pass, `SnowBallField.OnSnowBallState` was tightened to mirror the client-owned first-snapshot ownership from `CField_SnowBall::OnSnowBallState` at `0x560ab0` by accepting packet-fed `damageSnowBall`, `damageSnowMan0`, and `damageSnowMan1` values on that first state snapshot while leaving later snapshots from overwriting them, and repeated `state == 2/3` packets no longer keep inflating the simulator-only round scoreboard or replaying duplicate win banners when the same packet-owned win state is resent. The runtime still anchors its fallback chat templates to the client's `OnSnowBallMsg` StringPool ids `0xD75`-`0xD77`, still uses the client-owned `CSnowBall::ms_anDelay` cadence table from MapleStory.exe RVA `0x8568E4` (`150, 200, 250, 300, 350, 400, 450, 500, 0, -500`), and now has focused Snowball tests on disk for first-snapshot damage ownership, later-snapshot damage preservation, repeated win-state score suppression, and touch-request clearing. The row was also re-checked against the exported `C:\\Users\\twili\\Desktop\\Export\\v115` IMG filesystem through `MapleLib.Img.ImgFileSystemDataSource`, and the `String` category still does not expose recoverable localized payload nodes for ids `3445`-`3447` (`0xD75`-`0xD77`) | Snowball is narrower again but still `Partial`: the remaining gap is now specifically recovery of the literal client `StringPool` text for `0xD75`-`0xD77`, plus true packet-decode plumbing if this row later grows beyond the current direct runtime seam. The new Snowball tests could not be executed end-to-end in this run because `dotnet test UnitTest_MapSimulator\\UnitTest_MapSimulator.csproj --filter SnowBallFieldTests --no-restore` was blocked by an external file lock on `HaSharedLibrary\\obj\\Debug\\net10.0-windows\\HaSharedLibrary.dll` in the current workspace rather than a Snowball compile failure | `MinigameFields.cs`, `SnowBallField`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/SnowBallFieldTests.cs` (`CField_SnowBall::OnSnowBallState`, `CField_SnowBall::BasicActionAttack`, `CField_SnowBall::OnSnowBallHit`, `CField_SnowBall::OnSnowBallMsg`, `CField_SnowBall::OnSnowBallTouch`) |
| Partial | Ariant Arena field flow | The simulator now auto-binds `FIELDTYPE_ARIANTARENA` maps into a dedicated `AriantArenaField`, mirrors the client's sorted top-left ranking surface with WZ-backed `AriantMatch/characterIcon` row icons and the client's `(icon=5,y) / (name=21,y+2) / (score=106,y+2)` row offsets, keeps each player's Ariant icon assignment attached to that user across later score-driven re-sorts, plays the WZ-backed `AriantMatch/Result` animation from the client's center-top result origin, resolves the Ariant result UI sound from `Sound/MiniGame.img/Show` with a `Win` fallback instead of a broad heuristic, pushes local player name plus job into the runtime each frame so the client-owned 8xx or 9xx local-job rank suppression is honored, consumes the real Ariant packet types through a runtime packet seam for `354` score batches and `171` show-result packets, auto-starts a dedicated loopback Ariant packet inbox on Ariant maps so live `showresult` or `userscore` lines from the simulator transport can drain onto the main thread into that same runtime seam with `/ariantarena inbox ...` controls, and now extends that same Ariant inbox seam to accept transport-driven remote overlay lines for `actor add`, `actor avatar`, `actor move`, `actor remove`, and `actor clear`, so cloned-local or `AvatarLook`-backed remotes can spawn, update facing or action, and move live without requiring only manual `/ariantarena actor ...` commands. The remaining gap is narrower but still client-owned: the simulator still does not have a true `CUserPool`-style remote actor layer, does not decode the official Ariant remote spawn or movement or look packet shapes, and therefore still treats those remotes as Ariant-runtime overlays fed by simulator transport lines rather than literal server-authored remote-user packets | Arena scoring and ranking are owned by a dedicated field class in the client and are not covered by generic mob or UI behavior, and this row now also has a dedicated Ariant-side remote actor/name-tag seam plus a live transport path for remote overlay updates, leaving the remaining work concentrated on real user-pool ownership and exact remote packet decode instead of an entirely simulator-command-owned presentation layer | `MinigameFields.cs`, `AriantArenaField`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `AriantArenaPacketInboxManager.cs`, `UnitTest_MapSimulator/AriantArenaFieldTests.cs` (`CField_AriantArena::OnUserScore`, `CField_AriantArena::UpdateScoreAndRank`, `CField_AriantArena::OnShowResult`) |
| Partial | Battlefield event flow | The simulator now detects Battlefield maps through the special-field runtime, loads `battleField` WZ defaults such as `timeDefault`, `timeFinish`, reward maps, and effect paths, follows the client's packet-owned `OnClock(type=2, seconds)` and `OnScoreUpdate(wolves, sheep)` seams, exposes a Battlefield team-change seam aligned with `OnTeamChanged(characterId, team)` and `SetUserTeam`, resolves a finish phase that derives win/lose effect plus reward-map routing from the WZ `battleField` node, parses the map's `user/<team>` presets so local Battlefield team changes now reapply both the client-backed outfit pieces and the WZ `stat/speed` move-speed override before restoring the original avatar and speed on clear or map exit, retains each preset's `speedmax` and `noitem` data for inspection or later consumption, now resolves packet-owned remote team assignments all the way to the same WZ-backed team preset through a character-id lookup seam for future actor-layer consumers instead of storing only raw remote team ids, renders the client Battlefield board from `Map/Obj/etc.img/battleField/backgrnd` with the real sheep or wolf score fonts and the six-digit `fontTime` clock layout, queues the resolved reward map through the existing special-field transfer seam once the WZ `timeFinish` phase expires, mirrors the local `SetUserTeam` minimap side effect by forcing the simulator minimap out of its collapsed state for local teams `0` and `2`, now pushes the simulator's actual local character id into `BattlefieldField` so packet-shaped `OnTeamChanged` calls classify the local user correctly, and in this pass wires the map preset's `noitem` and `speedmax` fields into live simulator systems by blocking local Battlefield item use plus equip drags for blocked item ids and by clamping the live player move-speed path through the preset speed cap. The remaining gap is remote actor presentation: the simulator still does not render or hot-swap remote user looks because it has no client-style user-pool actor layer | Battlefield maps depend on a team-scoring field runtime rather than only ordinary combat rules, and the client keeps both the scoreboard window creation and team totals inside `CField_Battlefield` rather than generic field HUD code, so the remaining work is now concentrated on the missing remote actor-presentation path instead of leaving the WZ-backed Battlefield preset restrictions disconnected from live item and movement flow | `SpecialEffectFields.cs`, `BattlefieldField`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `PlayerCharacter.cs`, `PlayerManager.cs`, `EquipUIBigBang.cs`, `MinimapUI.cs`, `UnitTest_MapSimulator/BattlefieldFieldTests.cs` (`CField_Battlefield::OnClock`, `CField_Battlefield::OnScoreUpdate`, `CField_Battlefield::OnTeamChanged`, `CField_Battlefield::SetUserTeam`, `CScoreboard_Battlefield::OnCreate`) |
| Partial | Mu Lung Dojo field flow | The simulator now binds `FIELDTYPE_DOJANG` and the `MuruengRaid` map block into a dedicated `DojoField`, mirrors the client's type-2 clock ownership into the real `UI/UIWindow(.img)/muruengRaid` timerboard with bitmap digits and colon placement, draws the Dojo player, monster, and energy gauges from the client's WZ canvases at the IDA-verified layer anchors, swaps to the looping `energy/full` animation at max energy, replays the stage-start, clear, and time-over presentations from `Map/Effect.img/dojang` through `/dojostage` and `/dojoresult` debug seams plus the live timer-expiry path, auto-triggers the clear presentation when the live boss HP feed drops to zero, consumes the map's real `returnMap` or `forcedReturn` data so timer expiry waits for the WZ-backed time-over presentation to finish before queuing the Dojo exit transfer through the simulator's normal map-change seam, now derives the availability of a next-floor clear transfer from the map's real `dojang_next` portal script and releases that transfer only after the clear presentation completes, and exposes a dedicated Dojo loopback inbox seam that can drive energy, clock, stage, clear, and time-over updates through the live runtime; it still does not decode the official Dojo packet payloads or reproduce the full server-owned next-floor choreography beyond the loopback-fed runtime events and boss-death clear detection | Dojo behavior is timer-driven and mode-specific, and the simulator now has a client-backed HUD, WZ-backed clear/time-over transfers, and a dedicated packet-driving seam inside the existing runtime, leaving the remaining parity gap focused on byte-accurate server packet decoding and the broader official floor-transition choreography | `SpecialEffectFields.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `DojoPacketInboxManager.cs` (`CField_Dojang::Init`, `CField_Dojang::OnClock`, `CField_Dojang::Update`, `CField_Dojang::UpdateTimer`) |
| Partial | Cookie House event flow | The simulator now binds `fieldType == CookieHouse` into a dedicated `CookieHouseField` runtime, mirrors the client-owned HUD geometry from `CField_CookieHouse::Init`, tracks score updates through a score-owned redraw path instead of generic UI, replaces the old solid-color placeholder with WZ-backed HUD panel pieces, mirrors the client-owned redraw ownership more closely by caching the HUD into a dedicated `187x45` render target that only rebuilds when the point value changes before drawing that cached layer each frame, mirrors the client-owned update path by polling a simulator context point source each frame before redrawing instead of mutating the HUD as the source of truth, now pins the background load path to the client-shaped `UI/UIWindow.img/raise/backgrnd` root with `UIWindow2.img` kept only as a fallback variant, narrows bitmap-number probing to the same `raise` subtree instead of scanning unrelated UI images, and surfaces both resolved asset roots through the Cookie House status seam while keeping the existing dedicated loopback point inbox behind `/cookiepoint inbox ...`. The remaining gap is narrower but still real: the simulator still falls back to placeholder grade thresholds because the client's `s_anGrade` table values are not yet recovered, the exact literal bitmap-number WZ node behind StringPool id `0x13FC` is still not recovered even though probing is now constrained to the client-backed `raise` subtree, and it still lacks the true live server-owned Cookie House packet feed beyond that loopback inbox seam | Cookie House is another distinct score-driven field, and this row now has a client-shaped cached HUD redraw path, a fixed WZ-backed background root, narrower client-backed bitmap probing, and an external point-stream seam instead of remaining a manual overlay with a guessed digit model | `CookieHouseField.cs`, `SpecialFieldRuntimeCoordinator.cs`, `MapSimulator.cs`, `CharacterData.cs`, `CookieHousePointInboxManager.cs` (`CField_CookieHouse::_UpdatePoint`, `CField_CookieHouse::Init`, `CField_CookieHouse::Update`, `CBitmapNumber::CBitmapNumber`, `CBitmapNumber::Draw`) |
| Partial | Monster Carnival field flow | The simulator now detects Monster Carnival maps through the backlog coordinator, loads the map's WZ-backed Carnival definition into a dedicated runtime, enriches carnival mob entries with WZ-backed `getCP` and revive-chain metadata, pulls the map's `mobGenPos`, `guardianGenPos`, `reactorRed`, and `reactorBlue` data into that same definition, enforces local CP spending plus mob or guardian generation caps when requests succeed, clears prior summon or guardian state on each new `OnEnter` round like the client-owned UI reset path, rejects duplicate guardian requests through the client's dedicated occupied-slot failure reason, and now also records local successful mob summons against the next WZ-backed mob spawn slot while recording guardian placements against their WZ-backed guardian slot plus the team-owned reactor id. The runtime still exposes packet-shaped enter, request-result, request-failure, game-result, revive-death, CP-update, CP-delta, and summoned-mob-count handlers behind the same seam, and still auto-starts a dedicated loopback Monster Carnival packet inbox on Carnival maps so live raw packet types `346`-`353` can drain into that path with `/mcarnival raw ...` and `/mcarnival inbox ...` controls plus focused runtime tests. The remaining gap is narrower but still real: the simulator still does not yet mirror the client's exact localized StringPool request or result text, integrate with true server-owned Monster Carnival transport beyond the loopback inbox, or reproduce the full server-owned summon, guardian reactor-hit, and broader reactor-side gameplay rules beyond WZ-backed placement ownership | Monster Carnival is a self-contained field mode with client-owned rules and UI, and this row now carries the map-owned spawn-slot and reactor metadata all the way into live runtime placement state instead of stopping at CP counters and list entries, which makes the remaining parity gap explicitly about missing client text and server-driven gameplay ownership rather than missing WZ-backed field structure | `MonsterCarnivalField.cs`, `MinigameFields.cs`, `MapSimulator.cs`, `MonsterCarnivalPacketInboxManager.cs`, `UnitTest_MapSimulator/MonsterCarnivalFieldTests.cs` (`CField_MonsterCarnival::CreateUIWindow`, `CField_MonsterCarnival::OnEnter`, `CField_MonsterCarnival::OnRequestResult`, `CField_MonsterCarnival::OnShowGameResult`, `CField_MonsterCarnival::OnProcessForDeath`) |
| Partial | Party Raid field flow | The simulator now binds `FIELDTYPE_PARTYRAID`, `FIELDTYPE_PARTYRAID_BOSS`, and `FIELDTYPE_PARTYRAID_RESULT` into a dedicated `PartyRaidField` runtime, renders the field, boss, and result HUDs from the client WZ asset roots (`UI/UIWindow.img/PartyRace/*`, `UI/UIWindow.img/DualMobGauge/*`, `Map/Effect.img/praid/*`), fixes the field and boss point-vs-stage board placement to match the `PartyRace/Stage/backgrd` labels, mirrors the client's team-colored stage-state art, boss red-vs-blue gauge fill, map-owned clear or timeout result effect, and result summary overlay positions, now infers win-vs-fail result mode from the result maps' `info/onUserEnter` scripts (`PRaid_WinEnter`, `PRaid_FailEnter`) and constrains the result panel to the map's `LBSide/LBTop/LBBottom` safe area, carries a type-2 timer lifecycle through the runtime, now also swaps the old plain text countdown for the client `Map/Obj/etc.img/partyRace` timerboard with bitmap digits and separator, broadens the existing raw `field|party|session` key seam with numeric team aliases plus session outcome text handling, adds a WZ-backed `PartyRace/battery` field HUD plus battery charge-capacity aliases through the same raw key seam, and in this pass adds a dedicated Party Raid loopback inbox that auto-starts on Party Raid maps, drains live `field|party|session|clock` lines from the simulator network path into the existing runtime seam, and exposes `/partyraid inbox [status|start|stop]` controls with focused Party Raid tests for inbox parsing, script-owned outcome binding, battery decode, and session totals, but it still does not decode the true client packet shape or exact server-owned Party Raid key names beyond the aliases recovered so far, and the remaining per-map boss layout details that are only implicit in the client are still unconfirmed | Party Raid now covers the field battery surface, the field board, boss gauge, result overlays, the client timerboard, script-backed outcome selection, map-safe result placement, and a live loopback transport seam that feeds the existing runtime handlers, so the remaining gap is narrowed to exact packet-format and literal-key recovery plus any last client-only boss-layout details rather than missing runtime ownership | `SpecialFieldRuntimeCoordinator.cs`, `PartyRaidField.cs`, `MapSimulator.cs`, `PartyRaidPacketInboxManager.cs`, `UnitTest_MapSimulator/PartyRaidFieldTests.cs` (`CField_PartyRaid::_UpdatePoint`, `CField_PartyRaid::Init`, `CField_PartyRaidBoss::Init`, `CField_PartyRaidBoss::OnFieldSetVariable`, `CField_PartyRaidBoss::OnPartyValue`, `CField_PartyRaidResult::Init`, `CField_PartyRaidResult::OnSessionValue`) |
| Partial | SpaceGAGA timerboard flow | The simulator now binds `FIELDTYPE_SPACEGAGA` maps into a dedicated `SpaceGagaField`, mirrors the client's clock-type-2 timerboard ownership, keeps the board at the verified center-top window origin, removes the invented title banner, pins the timerboard source to `Map/Obj/etc.img/space` so the board comes from `space/backgrnd` and the bitmap timer digits come from `space/fontTime`, and no longer falls back to broad `UIWindow(.2).img` shape scans before dropping to the older procedural board or SpriteFont path. In this pass, the `/spacegaga` status seam now reports that fixed client-backed WZ root together with the unresolved `StringPool` id, and focused `SpaceGagaField` tests were added on disk to cover type-2 clock ownership, ignored non-type-2 clocks, and reset behavior through that runtime seam. The remaining gap is now narrower and specific: IDA confirms `CTimerboard_SpaceGAGA::OnCreate` resolves the source through `StringPool::GetBSTR(0x140D)`, and `StringPool::GetString` pulls that text from the client `ms_aString` table rather than the exported WZ `string` category, but the decoded `ms_aString[0x140D]` literal itself still has not been recovered, so the simulator still uses the verified WZ root directly instead of the byte-accurate client StringPool literal | This is another example of client-owned special field logic that would otherwise be lost under generic map handling, and the simulator now has a backlog-owned runtime seam with a fixed WZ-backed timerboard source, a status surface that exposes the client `StringPool` id still in play, and targeted runtime coverage, leaving only the unresolved `ms_aString[0x140D]` literal recovery work | `SpecialEffectFields.cs`, `UnitTest_MapSimulator/SpaceGagaFieldTests.cs`, `SpaceGagaField` (`CField_SpaceGAGA::OnClock`, `CTimerboard_SpaceGAGA::Draw`, `CTimerboard_SpaceGAGA::OnCreate`) |

## Priority Order

If the goal is visible parity first, the next work in this area should be sequenced like this:

1. Existing-special-field refinement pass:
   Tighten Wedding, Witchtower, Massacre, and SnowBall behavior against the client owners that already have simulator counterparts.
2. Missing-event-field bring-up pass:
   Add Monster Carnival, Party Raid, SpaceGAGA, and MiniRoom card-game runtime shells so the major field-specific owners present in the client are at least represented in the simulator.
3. Packet and HUD parity pass:
   Match timerboard, scoreboard, and result-surface behavior before extending generic field systems again.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
