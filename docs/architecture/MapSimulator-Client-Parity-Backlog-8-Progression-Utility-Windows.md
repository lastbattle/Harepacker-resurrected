# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/UI/MinimapUI.cs`
- `HaCreator/MapSimulator/UI/MouseCursorItem.cs`
- `HaCreator/MapSimulator/UI/PickupNoticeUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AbilityUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AdminShopDialogUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AbilityUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/BookCollectionWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/EquipUI.cs`
- `HaCreator/MapSimulator/UI/Windows/EquipUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/InventoryUI.cs`
- `HaCreator/MapSimulator/UI/Windows/InventoryUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/MemoMailboxWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/MemoSendWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/MemoGetWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/QuestUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuestUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillMacroUI.cs`
- `HaCreator/MapSimulator/UI/Windows/WorldMapUI.cs`
- `HaCreator/MapSimulator/Interaction/MemoMailboxManager.cs`

### Client references checked

- `CUIStat::Draw` (captured in simulator layout comments and offsets)
- `CUIStat::DrawTextA` (captured in simulator layout comments and offsets)
- `CUISkillMacro` (captured in simulator window comments)
- `CWvsContext::OnDropPickUpMessage` (captured in `PickupNoticeUI` comments)
- `CUIScreenMsg::ScrMsg_Add` (captured in `PickupNoticeUI` comments)
- `CUIItem::Draw` at `0x7ccf50`
- `CAdminShopDlg::OnCreate` at `0x42ecd0`
- `CAdminShopDlg::Draw` at `0x42fdf0`
- `CAdminShopDlg::OnButtonClicked` at `0x430f90`
- `CCashShop::Init` at `0x484920`
- `CCashShop::OnPacket` at `0x4997e0`
- `CCashShop::OnCashItemResult` at `0x499370`
- `CCSWnd_Char::OnCreate` at `0x4c4590`
- `CCSWnd_Char::OnDefaultAvatar` at `0x4bec80`
- `CCSWnd_Char::OnWear` at `0x4c5a60`
- `CCSWnd_Char::SetPet` at `0x4bc400`
- `CCSWnd_Char::ShowMessageBox` at `0x4bbb00`
- `CCSWnd_Char::ShowPersonalShop` at `0x4bc660`
- `CCSWnd_Char::ShowEntrustedShop` at `0x4bc6b0`
- `CCSWnd_Char::BlowWeather` at `0x4c5d30`
- `CCSWnd_Locker::OnCreate` at `0x4c31c0`
- `CCSWnd_Inventory::OnCreate` at `0x4c34f0`
- `CCSWnd_List::OnCreate` at `0x4d0500`
- `CCSWnd_Status::OnCreate` at `0x4cb840`
- `CCSWnd_OneADay::OnCreate` at `0x4d2fd0`
- `CCashTradingRoomDlg::OnCreate` at `0x49e8c0`
- `CUIChannelSelect::OnCreate` at `0x606a10`
- `CUIChannelSelect::Draw` at `0x6056d0`
- `CUIChannelSelect::OnButtonClicked` at `0x6035f0`
- `CUIWorldSelect::OnCreate` at `0x60b660`
- `CUIWorldSelect::OnButtonClicked` at `0x6025e0`
- `CUIChannelShift::OnCreate` at `0x96c160`
- `CUIChannelShift::Draw` at `0x96ccb0`
- `CUIItemMaker::Draw` at `0x7d4980`
- `CUIItemMaker::OnButtonClicked` at `0x7d5dc0`
- `CUIItemUpgrade::Draw` at `0x7c0100`
- `CUIItemUpgrade::OnButtonClicked` at `0x7c0ca0`
- `CUIMapTransfer::Draw` at `0x7df5c0`
- `CUIMapTransfer::OnButtonClicked` at `0x7e0ed0`
- `CUIEquip::Draw` at `0x7aa560`
- `CUIQuestInfo::Draw` at `0x82a780`
- `CUIQuestInfoDetail::Draw` at `0x8248c0`
- `CUIQuestInfoDetail::OnButtonClicked` at `0x834c90`
- `CUIQuestAlarm::Draw` at `0x82d850`
- `CUIRanking::OnCreate` at `0x83ca40`
- `CUIRanking::OnButtonClicked` at `0x83c6a0`
- `CUIRanking::Update` at `0x83c690`
- `CUIStatusBar::OnButtonClicked` at `0x880540`
- `CUIStat::Draw` at `0x864bd0`
- `CUIMacroSysEx::OnCreate` at `0x85c420`
- `CUISoftKeyboard::GetKeyIndexFromPoint` at `0x85faa0`
- `CUISoftKeyboard::OnMouseMove` at `0x85fba0`
- `CUISoftKeyboard::HitTest` at `0x85fc50`
- `CUISoftKeyboard::UpdateKeyConstraint` at `0x85fd70`
- `CUIUserInfo::Draw` at `0x8b3a50`
- `CUIUserInfo::OnButtonClicked` at `0x8b6d80`
- `CUIEventAlarm::OnCreate` at `0x7ac4f0`
- `CUIEventAlarm::Draw` at `0x7aad90`
- `CUIEventAlarm::Update` at `0x7aac00`
- `CBookDlg::OnCreate` at `0x471f00`
- `CBookDlg::Draw` at `0x473360`
- `CBookDlg::OnButtonClicked` at `0x474300`
- `CMonsterBookMan::LoadBook` at `0x664c10`
- `CParcelDlg::OnCreate` at `0x6914c0`
- `CParcelDlg::Draw` at `0x694db0`
- `CParcelDlg::OnButtonClicked` at `0x690c30`
- `CITC::Init` at `0x573ee0`
- `CITC::OnPacket` at `0x577470`
- `CITC::OnNormalItemResult` at `0x5771d0`
- `CUserLocal::OnPacket` at `0x9340c0`
- `CUserLocal::OnOpenUI` at `0x9055f0`
- `CUserLocal::OnOpenUIWithOption` at `0x932300`
- `CUserLocal::OnGoToCommoditySN` at `0x905760`
- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`
- `CUserLocal::OnRadioSchedule` at `0x918120`
- `CRadioManager::Play` at `0x6cb940`
- `CRadioManager::Stop` at `0x6cbd20`
- `CRadioManager::Update` at `0x6cbf20`

Notes:
Direct IDA seam capture is now confirmed for inventory, cash-service, channel-shift, item-maker, item-upgrade, map-transfer, equipment, quest-log, quest-detail, quest-alarm, the dedicated ranking/event owners (`CUIRanking`, `CUIEventAlarm`), status-bar utility clicks, stat-window, user-info owners, the book/collection owner family (`CBookDlg` plus `CMonsterBookMan::LoadBook`), the parcel/mail owner family (`CParcelDlg`), the separate service-stage owners for Cash Shop and ITC (`CCashShop`, `CITC`), the stage-internal Cash Shop owners (`CCSWnd_Char`, `CCSWnd_Locker`, `CCSWnd_Inventory`, `CCSWnd_List`, `CCSWnd_Status`, `CCSWnd_OneADay`, `CCashTradingRoomDlg`), the soft-keyboard helper that sits beside `CUIMacroSysEx`, and a broader local-user packet dispatcher for utility/event surfaces. `CUserLocal::OnPacket` is important because it shows the client does not only open these windows from direct button clicks: packet-owned handlers also drive generic `UI_Open`, optioned UI launch, commodity-shop migration, field fade overlays, balloon messages, and damage-meter timer resets. The Cash Shop and ITC decompiles also show that those services are not just alternate button skins on top of `CAdminShopDlg`: `CCashShop::Init` and `CITC::Init` each clear field UI, build their own staged child-window layout, select service BGM, and then wait on service-specific packet dispatch through `OnPacket`. Follow-up decompiles now also show that the stage owners do not directly absorb all of the in-service interaction either: `CCSWnd_Char::OnCreate` builds a dedicated avatar-preview surface with default-avatar, buy-avatar, and takeoff-avatar buttons plus a `CUserPreview`/physics-space pair, while `CCashTradingRoomDlg::OnCreate` builds its own chat edit, buttons, scrollbar, fonts, and cash-balance state inside the service stage. The remaining seam-collection gap in this backlog area is now narrower and centered on the minimap window, the exact `CUISkillMacro` draw owner, and any deeper packet/data ownership behind the newly confirmed ranking/event, book, parcel, and cash-service subwindow families rather than on an untracked progression-window family.

## Client Function Index By Backlog Area

The list below maps this backlog area to the concrete client seams that are already named in code or prior reverse-engineering notes.
These are the first functions or classes to inspect before changing simulator behavior.

### 1. Progression and utility window parity

- `CUIStat::Draw`
- `CUIStat::DrawTextA`
- `CUIItem::Draw` at `0x7ccf50`
- `CAdminShopDlg::OnCreate` at `0x42ecd0`
- `CAdminShopDlg::Draw` at `0x42fdf0`
- `CAdminShopDlg::OnButtonClicked` at `0x430f90`
- `CCashShop::Init` at `0x484920`
- `CCashShop::OnPacket` at `0x4997e0`
- `CCashShop::OnCashItemResult` at `0x499370`
- `CUIChannelSelect::OnCreate` at `0x606a10`
- `CUIChannelSelect::Draw` at `0x6056d0`
- `CUIChannelSelect::OnButtonClicked` at `0x6035f0`
- `CUIWorldSelect::OnCreate` at `0x60b660`
- `CUIWorldSelect::OnButtonClicked` at `0x6025e0`
- `CUIChannelShift::OnCreate` at `0x96c160`
- `CUIChannelShift::Draw` at `0x96ccb0`
- `CUIItemMaker::Draw` at `0x7d4980`
- `CUIItemMaker::OnButtonClicked` at `0x7d5dc0`
- `CUIItemUpgrade::Draw` at `0x7c0100`
- `CUIItemUpgrade::OnButtonClicked` at `0x7c0ca0`
- `CUIMapTransfer::Draw` at `0x7df5c0`
- `CUIMapTransfer::OnButtonClicked` at `0x7e0ed0`
- `CUIEquip::Draw` at `0x7aa560`
- `CUIQuestInfo::Draw` at `0x82a780`
- `CUIQuestInfoDetail::Draw` at `0x8248c0`
- `CUIQuestInfoDetail::OnButtonClicked` at `0x834c90`
- `CUIQuestAlarm::Draw` at `0x82d850`
- `CUIRanking::OnCreate` at `0x83ca40`
- `CUIRanking::OnButtonClicked` at `0x83c6a0`
- `CUIRanking::Update` at `0x83c690`
- `CUIStatusBar::OnButtonClicked` at `0x880540`
- `CUIMacroSysEx::OnCreate` at `0x85c420`
- `CUISoftKeyboard::GetKeyIndexFromPoint` at `0x85faa0`
- `CUISoftKeyboard::OnMouseMove` at `0x85fba0`
- `CUISoftKeyboard::HitTest` at `0x85fc50`
- `CUISoftKeyboard::UpdateKeyConstraint` at `0x85fd70`
- `CUIUserInfo::Draw` at `0x8b3a50`
- `CUIUserInfo::OnButtonClicked` at `0x8b6d80`
- `CUIEventAlarm::OnCreate` at `0x7ac4f0`
- `CUIEventAlarm::Draw` at `0x7aad90`
- `CUIEventAlarm::Update` at `0x7aac00`
- `CBookDlg::OnCreate` at `0x471f00`
- `CBookDlg::Draw` at `0x473360`
- `CBookDlg::OnButtonClicked` at `0x474300`
- `CMonsterBookMan::LoadBook` at `0x664c10`
- `CParcelDlg::OnCreate` at `0x6914c0`
- `CParcelDlg::Draw` at `0x694db0`
- `CParcelDlg::OnButtonClicked` at `0x690c30`
- `CITC::Init` at `0x573ee0`
- `CITC::OnPacket` at `0x577470`
- `CITC::OnNormalItemResult` at `0x5771d0`
- `CRepairDurabilityDlg` (constructed from `CUserLocal::OnOpenUIWithOption` at `0x932300` when `uiId == 33`)
- `CUISkillMacro`
- `CWvsContext::OnDropPickUpMessage`
- `CUIScreenMsg::ScrMsg_Add`

Notes:
`CUIItem::Draw` confirms that the client inventory window owns meso text, per-slot icon and quantity drawing, item-grade frames, disabled-slot overlays, and special overlays such as the active bullet marker. `CAdminShopDlg::OnCreate`, `CAdminShopDlg::Draw`, and `CAdminShopDlg::OnButtonClicked` confirm a dedicated cash-service shop dialog rather than treating cash-shop and MTS access as simple status-bar button stubs. `CUIChannelSelect::OnCreate`, `CUIChannelSelect::Draw`, `CUIChannelSelect::OnButtonClicked`, `CUIWorldSelect::OnCreate`, and `CUIWorldSelect::OnButtonClicked` confirm that the status-bar channel path fans out into explicit world and channel selector owners rather than a generic toggle, while `CUIChannelShift::OnCreate` and `CUIChannelShift::Draw` keep a separate shift or transition surface in play. `CUIItemMaker::Draw` and `CUIItemMaker::OnButtonClicked` confirm a dedicated crafting or maker window rather than burying production flow inside generic inventory handling. `CUIItemUpgrade::Draw` and `CUIItemUpgrade::OnButtonClicked` confirm a separate item-enhancement surface rather than treating scroll or upgrade flow as a generic inventory tooltip action. `CUIMapTransfer::Draw` and `CUIMapTransfer::OnButtonClicked` confirm a dedicated map-transfer window distinct from the minimap shell. `CUIEquip::Draw` confirms that equip-window parity is tied to slot-disable rules, expiration and durability overlays, and job or subjob gating rather than just slot placement. `CUIQuestInfo::Draw`, `CUIQuestInfoDetail::Draw`, and `CUIQuestAlarm::Draw` confirm that quest progression parity includes a separate detail pane with dedicated button handling, category-button rebuilds, scroll-driven list rebuilding, quest-delivery requirement rendering, condition-state coloring, and a standalone quest-alert window rather than only the quest log shell. Follow-up direct lookups now also confirm that the `BtRank` and `BtEvent` status-bar launches land on dedicated client owners rather than unnamed utility shells: `CUIRanking::{OnCreate,OnButtonClicked,Update}` and `CUIEventAlarm::{OnCreate,Draw,Update}`. `CBookDlg::{OnCreate,Draw,OnButtonClicked}` together with `CMonsterBookMan::LoadBook` confirm that the client keeps a real book-style collection owner tied to Monster Book data rather than treating book presentation as a generic stats subpage. `CParcelDlg::{OnCreate,Draw,OnButtonClicked}` confirm a dedicated parcel/mail owner with receive, send, and quick-send surfaces rather than burying package delivery under memo/social text alone. `CUserLocal::OnOpenUIWithOption` now also gives direct client evidence that repair-durability is its own utility owner rather than a generic option toggle: the decompile at `0x932300` shows `uiId == 33` allocating `CRepairDurabilityDlg(v5)` when no `CUniqueModeless` owner is already active. `CUIStatusBar::OnButtonClicked` confirms the utility-button cluster is a real interaction owner instead of only decorative art. `CUIMacroSysEx::OnCreate` together with `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::OnMouseMove`, `CUISoftKeyboard::HitTest`, and `CUISoftKeyboard::UpdateKeyConstraint` confirms that client text-entry parity is not just committed character handling: the macro family also owns an explicit soft-keyboard helper with hover, hit-test, and per-mode key-constraint behavior. `CUIUserInfo::Draw` and `CUIUserInfo::OnButtonClicked` confirm a separate character-information or profile surface beyond the stat window itself.

### 2. Local utility and event packet parity

- `CUserLocal::OnPacket` at `0x9340c0`
- `CUserLocal::OnOpenUI` at `0x9055f0`
- `CUserLocal::OnOpenUIWithOption` at `0x932300`
- `CUserLocal::OnGoToCommoditySN` at `0x905760`
- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`

Notes:
`CUserLocal::OnPacket` is a distinct client seam rather than a random collection of callbacks. It dispatches packet-owned local utility behavior including `OnOpenUI`, `OnOpenUIWithOption`, `OnGoToCommoditySN`, field fade in/out, balloon messages, damage-meter timing, chat/notice surfaces, quest-guide and delivery-quest results, buff-zone effects, field HP-dec notices, tutor flows, and several event-sound/minigame-sound owners. The narrower decompiles already show concrete responsibilities that are not represented well by the existing window rows: `OnOpenUI` forwards a raw UI id into `CWvsContext::UI_Open`, `OnOpenUIWithOption` adds option-driven branches such as party-search requests and the repair-durability owner, `OnGoToCommoditySN` stores a commodity serial number before calling `SendMigrateToShopRequest`, `OnFieldFadeInOut` registers a fade animation relative to the avatar layer Z, `OnBalloonMsg` can bind a balloon to the avatar origin or an explicit world position, and `OnDamageMeter` resets a dedicated timer surface. That means the client has a packet-driven utility/event layer that sits between local gameplay and the specific window owners already tracked above.

### 6. Packet-authored radio-schedule parity discovered by targeted IDA scan

- `CUserLocal::OnRadioSchedule` at `0x918120`
- `CRadioManager::Play` at `0x6cb940`
- `CRadioManager::Stop` at `0x6cbd20`
- `CRadioManager::Update` at `0x6cbf20`

Notes:
The broader local-utility row already mentioned tutor/radio handlers as a residual gap, but targeted IDA lookup shows radio scheduling is its own client-owned lifecycle rather than one more generic sound callback. `CUserLocal::OnRadioSchedule` decodes a track name plus time value and forwards that payload into a singleton `CRadioManager` owner when no radio session is already active. `CRadioManager::Play` then resolves a WZ/UOL-backed track through `StringPool`, starts multimedia playback, stores the active track name, mutes field BGM through the sound manager, shows a dedicated radio UI, and pushes a chat-log notice for the start event; `CRadioManager::Stop` reverses that ownership by stopping playback, hiding the UI, restoring BGM state, and pushing the completion notice; and `CRadioManager::Update` confirms a timer-driven runtime instead of a fire-and-forget sound effect. None of that owner-specific audio/UI/chat lifecycle is tracked anywhere else in the backlog today.

### 3. Packet-authored local overlays and guidance parity

- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`
- `CUserLocal::OnNotifyHPDecByField` at `0x90fed0`
- `CUserLocal::OnQuestGuideResult` at `0x90f1e0`
- `CUserLocal::OnDeliveryQuest` at `0x90ef60`
- `CUserLocal::OnOpenClassCompetitionPage` at `0x905580`

Notes:
These handlers form a narrower client-owned feedback layer under `CUserLocal::OnPacket`, and the current backlog only references them inside the broad dispatcher row. IDA now confirms concrete responsibilities that deserve separate tracking: `OnFieldFadeInOut` decodes fade-in, fade-out, and duration fields then registers the animation relative to the avatar layer Z; `OnBalloonMsg` decodes text, width, lifetime, and either avatar-relative or explicit world-position placement before calling `ShowBallonMsg`; `OnDamageMeter` resets the dedicated damage-meter timer owner and updates shared timing state in `CWvsContext`; `OnQuestGuideResult` rebuilds quest-demand targets and opens the world-map quest-guide owner when the packet contains viable quest-mob targets; `OnDeliveryQuest` either opens the dedicated delivery-quest owner or routes the result back through the status-bar chat seam when another unique modeless owner is active; and `OnOpenClassCompetitionPage` is a packet-driven utility launch rather than a normal button path.

### 4. External cash-service stage parity

- `CCashShop::Init` at `0x484920`
- `CCashShop::OnPacket` at `0x4997e0`
- `CCashShop::OnCashItemResult` at `0x499370`
- `CITC::Init` at `0x573ee0`
- `CITC::OnPacket` at `0x577470`
- `CITC::OnNormalItemResult` at `0x5771d0`

Notes:
This service-stage surface is distinct from the in-map `CAdminShopDlg` approximation already tracked above. IDA shows `CCashShop::Init` and `CITC::Init` clearing field UI, constructing their own staged child-window layouts, selecting service BGM, and entering standalone stage owners rather than opening one more utility dialog over the live HUD. The packet routers are distinct too: `CCashShop::OnPacket` dispatches cash-service result families such as charge-param, query-cash, cash-item, purchase-exp, gift-mate, duplicate-id, name-change, transfer-world, gachapon, one-a-day, and free-item notices, while `CITC::OnPacket` owns its own smaller result family around charge-param, query-cash, and normal-item results. That means Cash Shop and ITC parity should be tracked as their own service-stage owner family rather than being treated as a subset of status-bar launches or the current admin-shop fallback.

### 5. Cash-service child-window and preview parity discovered by targeted IDA scan

- `CCSWnd_Char::OnCreate` at `0x4c4590`
- `CCSWnd_Char::OnDefaultAvatar` at `0x4bec80`
- `CCSWnd_Char::OnWear` at `0x4c5a60`
- `CCSWnd_Char::SetPet` at `0x4bc400`
- `CCSWnd_Char::ShowMessageBox` at `0x4bbb00`
- `CCSWnd_Char::ShowPersonalShop` at `0x4bc660`
- `CCSWnd_Char::ShowEntrustedShop` at `0x4bc6b0`
- `CCSWnd_Char::BlowWeather` at `0x4c5d30`
- `CCSWnd_Locker::OnCreate` at `0x4c31c0`
- `CCSWnd_Inventory::OnCreate` at `0x4c34f0`
- `CCSWnd_List::OnCreate` at `0x4d0500`
- `CCSWnd_Status::OnCreate` at `0x4cb840`
- `CCSWnd_OneADay::OnCreate` at `0x4d2fd0`
- `CCashTradingRoomDlg::OnCreate` at `0x49e8c0`

Notes:
The stage-owner row above is still too coarse on its own. IDA shows that Cash Shop and related cash-service flows split into dedicated child-window owners after `CCashShop::Init` creates the stage: `CCSWnd_Char::OnCreate` allocates its own avatar preview layer, user-preview actor, physical-space helpers, and `BtBuyAvatar`/`BtDefaultAvatar`/`BtTakeoffAvatar` controls before calling `OnDefaultAvatar`; the adjacent `CCSWnd_Char` methods also expose stage-local preview actions for wear/takeoff, pet preview, weather, message box, personal shop, and entrusted shop effects rather than routing those previews through generic field windows. Separate owners then build the rest of the service UI: `CCSWnd_Locker`, `CCSWnd_Inventory`, `CCSWnd_List`, and `CCSWnd_Status` each have their own `OnCreate` routines, `CCSWnd_OneADay::OnCreate` owns the One-a-Day panel inside the service, and `CCashTradingRoomDlg::OnCreate` builds a standalone cash trading room with its own chat edit, trade/coin buttons, scrollbar, fonts, and initial cash balance. None of these client-owned child-window seams are named anywhere else in the backlog today even though they sit directly on the parity path for cash-avatar preview and service-stage usability.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.
- Progression or utility window groundwork now has a shared seam: named owners for world map, map transfer, cash-service, channel-selection, crafting, enhancement, character-info, and quest-side utility windows are registered with `UIWindowManager`, while minimap and status-bar utility buttons now route into those owners instead of staying entirely dead-ended. The world/channel path now opens dedicated simulator-side `WorldSelect`, `ChannelSelect`, and timed `ChannelShift` surfaces backed by `UIWindow2.img/Channel` art, with the channel grid, centered placement, per-channel load feedback, login-step request gating, delayed `CheckUserLimitResult` / `SelectWorldResult` style request flow, and simulator-side `WorldInformation` / `RecommendWorldMessage` / `LatestConnectedWorld` metadata overlays now aligned to the client-owned selector layout instead of remaining a placeholder-only launch seam. The map-transfer owner now also keeps saved teleport destinations in a character-scoped destination book keyed to the active player or selected login-roster character instead of sharing one global saved list across every simulator avatar, persists that destination book under the simulator AppData profile across restarts, and hands the currently selected transfer target into the world-map owner when `BtMap` is used so the two navigation surfaces stay linked.

## Remaining Parity Backlog

### 1. Progression and utility window parity

This area is currently absent from the backlog set even though the simulator already has concrete window and feedback implementations.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Minimap window parity | The minimap now loads WZ-backed helper markers for generic NPCs, quest-start NPCs, quest-end NPCs, portals, out-of-bounds direction arrows, and the broader `DefaultHelper` remote-user families from `UIWindow2.img/MiniMapSimpleMode/DefaultHelper`; it upgrades the post-Big-Bang generic NPC notice marker plus off-map direction overlays to the animated `UIWindow2.img/MiniMap/iconNpc` and `MiniMap/iconDirection/*` frame families when those client assets are present, feeds visible portal markers plus quest-aware NPC marker families into `MinimapUI`, keeps the post-Big-Bang `BtNpc` list panel from `ListNpc`, and now also tolerates the client-shaped fallback where only quest-start or quest-end marker art is present instead of assuming the generic NPC helper family always exists before the NPC toggle can work. The minimap now also feeds same-field simulator social/family entries into the minimap as `another` / `friend` / `party` / `partymaster` / `guild` / `guildmaster` helper markers, reusing family-chart positions when the simulator has them and otherwise assigning deterministic local field positions so those helper families can render with matching off-map direction overlays instead of staying dead data. It also resolves a local helper marker through the same seam, so active Match Cards rooms can switch the player dot to the WZ `match` family and personal- or entrusted-shop field actors can switch it to `usertrader`, while visible Match Cards participants plus shop occupants now seed `match` / `anothertrader` helper markers even when they only have runtime occupant state and no packet-authored world position. `BtBig` still opens a dedicated `WorldMap` owner whose `WorldMapSearch` side panel is no longer just background art: `BtSearch` switches into a dedicated WZ-backed search surface with `BtAllsearch` return flow, optional `BtLevelMob` mob-only filtering, live field/NPC/mob result rows sourced from the current simulator field, and a working text-entry search path that filters those result families plus global field-name or map-id matches, keeps search-row selection distinct even when multiple rows resolve to the same field, supports keyboard navigation or activation, accepts clipboard paste into the search box, now uses the client `UIWindow2.img/WorldMap/title` canvas instead of a text-only heading, keeps the search caret and visible query text clipped inside the edit box when the query overruns the field, lets mouse clicks reposition the caret inside that visible query span, adds word-wise `Ctrl+Backspace` / `Ctrl+Delete` editing on top of the existing keyboard path, now routes committed text through the shared `GameWindow.TextInput` seam instead of relying only on raw key translation, previews Win32 IME preedit text inline with a live underline before commit, and now uses the WZ `WorldMapSearch/resultField|resultNpc|resultMob/mouseOverBase` canvases for actual pointer-hover chrome while keeping a separate selected-row tint and still honoring the WZ icon/origin offsets instead of pinning every result family to zero-origin placement, while `BtAll` / `BtAnother` browsing, current-map focus, paged field selection, and direct map-transfer queuing still run through the existing pending-map seam. The remaining gap is that the simulator still does not mirror true packet-authored remote-user field state, the client's exact trader or match helper semantics beyond the current local runtime synthesis, the still-missing `DefaultHelper` families that depend on richer live packet state, or the client's exact IME candidate-window/edit-control behavior plus the remaining presentation polish inside that search owner | Navigation feedback now has animated minimap-owned notice and direction art, quest- and portal-aware markers, a broader WZ-backed remote-user helper surface, runtime-driven local and remote `match` / trader helper-family usage instead of leaving those `DefaultHelper` families reachable only through manual remote-user injection, and true WZ-backed hover chrome inside the world-map search rows instead of treating the `mouseOverBase` art like a selection-only fill; the remaining gap is narrowed to true packet-authored field state, exact social-helper semantics, and the client's exact IME candidate/polish fidelity rather than missing helper-family wiring or a static search panel | `MinimapUI.cs`, `WorldMapUI.cs`, minimap/window loader path |
| Partial | Map transfer / navigation window parity | The simulator now routes minimap `BtMap` into a dedicated WZ-backed `MapTransfer` owner built from the `Teleport*` window art, constrains the destination surface to the client's five visible rows, caps saved teleport destinations to a client-shaped five-vs-ten slot budget based on the loaded `Teleport` vs `Teleport3` art variant, respects teleport-item field limits separately from generic migration checks, layers return-map plus portal-derived routes on top of the saved list while driving actual map changes through the existing pending-map seam instead of stopping at the minimap shell, keeps saved teleport destinations in a character-scoped destination book keyed to the active player or selected login-roster character instead of one simulator-global list, persists that per-character destination book under the simulator AppData profile across restarts, opens the world-map owner focused on the currently selected transfer target rather than always resetting to the current field, preserves fixed per-character slot indices instead of compacting into a generic list, renders empty saved slots inside the transfer window, keeps the selected saved slot alive through the world-map handoff so a `[Target]` selection can be written back into that exact slot through `BtRegister`, now draws the client-shaped saved-count header instead of treating the list like an unbounded generic scroller, resolves saved-slot labels from map IDs rather than persisting simulator-only custom names, exposes the client-owned edit-target lane as a local numeric map-id entry path with 12-digit input, focus-driven selection clearing, paste support, and direct `BtMove` routing when no list row is selected, now loads the client `FadeYesNo/backgrnd7` plus `BtOK` / `BtCancel` confirmation chrome so register, delete, saved-route, portal-route, and manual map-id requests all pass through a local yes/no confirmation step instead of mutating or moving immediately, and now applies the client-shaped saved-destination category gate from `CUIMapTransfer::OnRegister` so sub-`100000000` ids and `xx9xxxxx` map families cannot be written into saved teleport slots while duplicate-save attempts refocus the already-owned slot instead of silently pretending to create a second entry, but it still does not mirror the client's full `CUIMapTransfer` behavior surface such as live packet-backed `CharacterData::adwMapTransfer` / `adwMapTransferEx` sourcing, the exact official request/response path behind `CWvsContext::SendMapTransferRequest`, a visible client-owned scrollbar path for the 10-slot continent variant, or any deeper unsaveable rules beyond the category gate now mirrored locally | Navigation testing can now exercise a more client-shaped transfer and destination-selection surface above raw portal travel, saved destinations no longer bleed across unrelated simulator characters, the saved-slot header and raw map-id presentation are closer to the official owner, the edit target no longer behaves like dead art, world-map retargeting now lands in the intended saved slot instead of collapsing back into a generic append path, saved-slot registration now follows the same broad category gate the client applies before writing `adwMapTransfer` / `adwMapTransferEx`, and register/delete/move requests now all pass through the same WZ-backed yes/no chrome instead of firing immediately; the remaining gap is narrowed to live character-data population, packet-authored updates, the 10-slot variant's client scrollbar treatment, and the deeper destination-validation rules before it fully matches the client flow | navigation/UI layer, minimap integration, destination-selection flow (`CUIMapTransfer::Draw`, `CUIMapTransfer::OnButtonClicked`, `CUIMapTransfer::OnCreate`) |
| Partial | Cash shop / admin shop dialog parity | The simulator now replaces the generic Cash Shop and MTS placeholders with a dedicated `AdminShopDialogUI` owner backed by `UIWindow2.img/Shop`, reuses the shop dialog frame plus `BtBuy` / `BtSell` / `BtExit` / `BtRecharge`, keeps Cash Shop and MTS inside the same button-driven surface, routes the status-bar launchers so only the requested service window stays open, exposes separate scrollable NPC-offer and user-listing panes with per-row selection and request submission messaging, consumes the client `UIWindow2.img/Shop/TabShop` category strip as a real WZ-backed catalog filter, now also consumes the owner `TabBuy` and `TabSell` tab canvases with their client WZ offsets instead of leaving the service and pane strips as text-only chrome, now anchors the `TabShop` category strip to the client-owned bottom control position from `CAdminShopDlg::OnCreate` instead of treating the zero-origin canvases like a stack at window origin, mirrors the client-owned twin scrollbar layout called out by `CAdminShopDlg::OnCreate`, replaces the wish-list footer prompt with a `FadeYesNo`-style confirmation modal before saving a wish-list entry, syncs the displayed balance to the live inventory meso runtime like `CAdminShopDlg::Draw`, now renders WZ-backed item icons plus stack counts for requestable rows in both panes and the detail area so the dialog no longer behaves like a text-only catalog despite `CAdminShopDlg::DrawNPCItem` / `DrawUserItem`, resolves simulator-side shop requests through a broader local session matrix with meso consumption, inventory-capacity checks, actual item delivery for requestable rows, post-purchase sold-out locking on one-shot listings, rejected outcomes such as seller-offline, expired, inventory-full, and refreshed-sold-out responses, plus real icon carry-through on delivered items instead of treating every non-expansion row as the same generic acknowledgment, now interprets the left browse strip as the real WZ-backed `ALL` / `MOST` / `SELL` / `BUY` / `RE-BUY` family for Cash Shop while MTS swaps to its own WZ-backed `ALL` / `MOST` / `SELL` / `BUY` subset from `UIWindow2.img/Shop/TabSell`, and keeps the right `TabSell` strip acting as WZ-backed quick category tabs (`Equip` / `Use` / `Etc` / `Set-up` / `Cash`) instead of fake Cash Shop or MTS toggles, with featured-row curation for `MOST`, pane focus changes for `SELL` and `BUY`, and in-session wish-list plus delivered-row state restoration so `RE-BUY` can surface previously granted rows after page flips. The Cash Shop NPC pane now also resolves sample commodity metadata against live `Etc/Commodity.img` rows so matching entries inherit WZ-backed price, stack-count, and commodity-SN data instead of only hard-coded sample values, and the packet-owned `OnGoToCommoditySN` launch seam now focuses the matching Cash Shop sample row plus category inside the existing owner after migration instead of only storing the SN in a side channel. The dialog also supports owner-local keyboard parity for the current browse surface: `Up` / `Down` / `PageUp` / `PageDown` / `Home` / `End` walk the active pane, `Tab` / `Left` / `Right` swap between the NPC-offer and user-listing panes, `Enter` submits the highlighted request, `Space` opens the wish-list confirmation when supported, and `Enter` / `Escape` resolve the `FadeYesNo` modal without forcing mouse-only interaction. It still does not mirror the full `CAdminShopDlg` behavior surface such as true packet-backed shop-session state beyond commodity-SN targeting, live server-populated catalogs, the exact official semantics behind each client tab state, or the server-authored request and trade matrix behind those controls | The simulator can now exercise the client cash-service launch path through a dedicated dialog owner with concrete per-item browsing, mode-specific WZ-backed browse chrome for Cash Shop versus MTS, client-shaped scrollbar and category-tab affordances whose bottom `TabShop` strip now lands on the client-owned control geometry instead of a zero-origin overlap, visible item art in the browse and detail surfaces, a real wish-list confirmation step, live balance readout, a keyboard-drivable selection and request flow, and a more varied local request-result loop instead of a dead-end placeholder; using `Etc/Commodity.img` for matching NPC rows and honoring packet-owned commodity-SN focus also gives the owner a first real packet-shaped catalog-selection seam instead of treating `OnGoToCommoditySN` as message-only bookkeeping. The remaining gap is narrowed to true live-catalog/session sourcing, the deeper official meaning of each tab state, and the wider server-authored trade/session rules inside that owner rather than missing commodity targeting, category-strip placement, shared browse art, unlabeled placeholder tabs, or a mouse-only dialog | cash-service UI layer, status-bar integration, shop-session flow (`CAdminShopDlg::OnCreate`, `CAdminShopDlg::Draw`, `CAdminShopDlg::OnButtonClicked`) |
| Partial | World and channel selector parity | The simulator now routes `BtChannel` into dedicated `WorldSelect`, `ChannelSelect`, and timed `ChannelShift` windows, uses `UIWindow2.img/Channel` world badges and channel art for the local selector stack, preserves a visible current world/channel choice for the simulator session, expands the world list to the full badge set, aligns the channel grid and window centering to the client `GetRect`/owner layout, simulates per-world channel availability plus busy/full load bars so invalid targets no longer behave like always-open buttons, mirrors the client-owned request shape more closely by disabling selector input outside the login `WorldSelect` step and resolving world/channel picks through delayed `CheckUserLimitResult` / `SelectWorldResult` style simulator responses instead of committing immediately, lets login-scene `WorldInformation`, `RecommendWorldMessage`, and `LatestConnectedWorld` packets drive selector-side world load data, recommended-world markers, latest-world focus, and immediate UI refresh instead of leaving those packet owners as message-only stubs, carries simulator-side adult-channel gating, denied `CheckUserLimitResult` / `SelectWorldResult` style result messaging, plus low-frequency live population drift across login-scene world metadata so the selector surface no longer behaves like a fixed always-successful snapshot, now also accepts client-shaped `WorldInformation` payload bytes through the existing login-packet seam, decodes the `CLogin::OnWorldInformation` packet layout into per-world channel user counts and adult-channel flags, supports incremental worldinfo accumulation plus the client terminator packet instead of only one-shot synthetic profile overrides, preserves those packet-authored per-channel populations against local drift so repeated server-authored selector updates can refresh the UI without being immediately replaced by simulator heuristics, and now exposes the existing login loopback transport as an explicit `/loginpacket inbox` control surface with configurable listen port plus parser support for raw `<packet> <args>`, `<packet>:<args>`, and `<packet>=<args>` feed shapes so selector-side world updates no longer depend on chat-only injection formatting. The same selector seam now also accepts packet-authored `RecommendWorldMessage`, `LatestConnectedWorld`, and `CheckUserLimitResult` payload bytes directly, the inbox/chat path can collapse relay-style raw hex payload fragments for `WorldInformation`, `RecommendWorldMessage`, and `SelectWorldResult` into the existing decoder flow instead of requiring every external source to pre-wrap them as `payloadhex=...`, and the loopback parser now also accepts relay-tagged `recv`/`incoming` feeds with `packet=` / `type=` / `handler=` metadata, `payload` / `hex` / `data` / `bytes` / `base64` aliases, plus `CLogin::OnWorldInformation` / `OnRecommendWorldMessage` / `OnLatestConnectedWorld` / `OnSelectWorldResult` style handler names so external decrypted login relays no longer need to rewrite every line into the simulator's chat command syntax first. The selector now mirrors the client-observed `SelectWorldResult` rejection families from `CLogin::OnSelectWorldResult` plus the `CheckUserLimitResult` code-2 over-user-limit path from `CUIWorldSelect::UserLimitResult`, including the grouped login-error branches and the code-7 title return instead of collapsing every non-success packet into one generic rejection, but it still does not capture or decrypt the client's authenticated upstream login traffic on its own and still relies on an external relay/sniffer to source true live server population packets; true network-opcode-level login frame ingestion is also still external rather than owned by the simulator | Channel switching is no longer a dead-end HUD affordance, selector population can now follow repeated client-shaped `WorldInformation` updates through a built-in configurable loopback transport, direct payload-byte decoding for the other selector packets, and a more relay-friendly metadata-tagged inbox parser instead of only manual command injection, and the remaining gap is narrowed to sourcing, decrypting, and opcode-level mapping of the real upstream login feed rather than missing selector surfaces, packet decoding, or lacking a practical relay-facing transport seam inside the simulator | world-selection and channel-selection UI layer, status-bar integration (`CUIWorldSelect::OnCreate`, `CUIWorldSelect::OnButtonClicked`, `CUIChannelSelect::OnCreate`, `CUIChannelSelect::Draw`, `CUIChannelSelect::OnButtonClicked`, `CUIChannelShift::OnCreate`, `CUIChannelShift::Draw`, `CLogin::OnCheckUserLimitResult`, `CLogin::OnSelectWorldResult`, `CUIWorldSelect::UserLimitResult`, `CLogin::OnWorldInformation`) |
| Partial | Inventory window parity | Legacy and Big Bang inventory windows now render right-aligned meso text, per-slot item icons and stack counts, WZ-backed grade and quality markers, disabled-slot overlays, active-slot highlighting, and Big Bang tab or expanded layouts, while the simulator also feeds meso and picked-up items into the inventory surface at runtime, resolves equip pickup icons through `Character/*/*.img` like the client asset families, respects `info/slotMax`-style stack splitting, merge limits, and pickup-capacity checks for stackable items, wires `BtGather` and `BtSort` into simulator-side stack compaction and ordering, shows shared client-style hover tooltips with cached item names, type labels, stack data, and descriptions across both legacy and Big Bang inventory layouts, and now keeps the shared inventory metadata resolver aligned with that same cache ordering so inventory-adjacent pickup and utility feedback surfaces read the display item name instead of the item-category label, supports client-shaped intra-inventory drag/drop for occupied-slot swapping, stack merging, trailing-empty-slot moves, plus a held-item cursor overlay in both layouts, and now carries client-shaped slot-unlock progression through per-tab slot limits that grow in 4-slot rows up to a capped 96-slot budget while `BtCashshop` routes directly into the simulator cash-service dialog and its `Extending Equip/Use/Set-Up/Etc Inventory` offers consume live meso balance to unlock additional rows instead of leaving capacity as a fixed 24-slot shell; right-click setup-chair items now route into the existing portable-chair runtime so inventory use can activate or clear owned chairs without falling back to chat commands, WZ-backed chair `info/recoveryHP` / `recoveryMP` values now tick against the seated player on a client-shaped 10-second cadence, pet-food items now honor `Consume/*/spec/*` supported-pet families plus the pet runtime's live fullness gate when selecting the target runtime, only consume when a compatible summoned pet can actually eat the food, and surface simulator-side failure feedback when no valid target is available instead of silently burning or dead-ending the click, `Consume/*/spec/moveTo` scrolls now consume through the inventory surface to queue either fixed-map warps or `999999999` nearest-town returns through the existing map-transfer seam instead of remaining unsupported right-clicks, and the same WZ-backed consume path now also applies client-shaped abnormal-status cures, timed morph consumables from `Consume/0221` via `spec/morph` and weighted `spec/morphRandom`, temporary stat buffs from `pad` / `mad` / `pdd` / `mdd` / `acc` / `eva` / `speed` / `jump`, plus the `Consume/0202` max-vital percentage buff family via `spec/mhpR|mhpRRate` and `spec/mmpR|mmpRRate` entries such as `2022221`, all through the shared player-status, avatar-transform, and buff runtimes instead of treating those right-clicks as dead inventory actions; that shared WZ-backed consume seam is now also reusable from the cash tab when an item exposes the same recovery/moveTo/morph/cure/buff `spec` schema instead of being hard-wired to `USE` only, and the corresponding `MobSkill.img/134` pain-mark disease now lands in the same player-status controller with WZ `hp` / `interval` / `count` timing so those cure items clear a live runtime effect instead of a dead flag | The remaining gap is narrowed to broader client-authenticated item-use rules for non-chair setup items, pet-feeding fidelity beyond the current supported-family plus fullness/runtime routing (notably command or closeness progression and exact official success or failure messaging), and the still-wider long-tail consume-item matrix beyond the simulator's current HP/MP, max-HP/max-MP percentage buff, moveTo, cure, morph, timed disease, and temporary-stat coverage, plus stricter server-side validation and packet-authored rejection semantics rather than missing inventory rendering, name-cached tooltip or feedback text, drag/drop, pickup-capacity, slot-progression, or the common local item-use loop itself | `InventoryUI.cs`, `InventoryUIBigBang.cs`, `UIWindowLoader.cs`, `AdminShopDialogUI.cs`, inventory/runtime integration (`CUIItem::Draw`, `CUIItem::OnMouseButton`) |
| Partial | Item maker / crafting window parity | The simulator now replaces the `ItemMaker` placeholder with a dedicated WZ-backed `Maker` window owner, exposes `BtStart` / `BtCancel` crafting flow, animates the maker gauge during crafting, seeds a starter ore inventory plus meso and catalyst stock, resolves weighted `randomReward` branches, blocks crafts whose fixed or random reward branch cannot fit in inventory, routes NPC talk overlays whose loaded `String/Npc.img` function text advertises item-maker families such as `Item Maker`, `Glove Maker`, `Shoemaker`, and `Toy Maker` into the dedicated crafting owner, and now rebuilds the selector surface around client `CUIItemMaker` rules by deriving the visible `Etc/ItemMake.img` bucket set from the live job (`0` plus the matching `1` / `2` / `4` / `8` / `16` job bucket, or all five for admin-family jobs), regrouping those recipes into client-like category and item selectors instead of the old raw bucket picker, adding a leading `All` selector plus client-shaped aggregate pages for `Gem`, `Monster Crystal`, `Use`, `Setup`, and `Etc`, hiding selector entries that fail the client-side precondition pass for `reqLevel`, `reqSkillLevel`, `reqItem`, `reqEquip`, and `reqQuest` before the detailed craft validation runs, applying a local hidden-maker-list pass so maker-family NPC launches such as glove, shoe, toy, and generic item-maker flows narrow the visible recipe set instead of only focusing the nearest matching row, and now also backs the owner with a simulator-side per-character maker progression store that tracks family-specific mastery levels for generic/glove/shoe/toy crafting, advances that state on successful crafts, persists it under the simulator AppData profile, uses that family mastery rather than the raw craft trait alone when evaluating `reqSkillLevel`, correctly reloads the per-character discovered-recipe list from that AppData profile across simulator restarts, keeps recipes that passed the selector precondition sweep visible on later visits even after transient `reqItem` / `reqEquip` / `reqQuest` conditions drop back out, aligns recipe hidden-vs-normal classification more closely with the client `CItemMakerInfo::RegisterTargetItem` split by honoring the WZ `hide` flag instead of treating every `reqItem` recipe as implicitly hidden, and surfaces the client-observed `DrawTargetItem` / `DrawRecipe` / `DrawGem` / `DrawCatalyst` / `DrawMeso` split more faithfully by drawing item-result and material icon rows through the live inventory/runtime icon seams. The owner now also keeps the client-shaped `???` page alive whenever the active job or maker-family launch context has hidden recipes, treats those rows as a separate hidden list instead of dropping them out with the normal precondition sweep, obscures locked hidden entries in the row list, selector, detail header, and craft path until a local WZ-backed unlock pass (`reqItem`, `reqEquip`, or `reqQuest`) reveals them once, persists that reveal through the existing AppData-backed recipe-discovery store across later visits or restarts, and now also locally reveals and persists hide-only hidden rows once their WZ-authored persistent level/mastery gate passes so they do not remain permanently locked without a server profession session. It still lacks the official server-authored hidden-list unlock decisions and packet/session model authored by the client `CItemMakerInfo` runtime, so the remaining hide-only cases still run on a simulator-side heuristic rather than real profession-state packets | Production and crafting progression now has a real client-backed launch path, client-shaped category and item selection, job-aware recipe exposure, family-focused NPC launch selection, AppData-backed family mastery plus recipe discovery that now survives reload, tighter precondition plus result-capacity validation, a client-backed hidden-vs-normal recipe split, a dedicated hidden `???` page whose locked rows stay obscured until locally revealed, and a local fallback that keeps hide-only hidden rows from staying permanently locked once their WZ mastery or level gate is satisfied, while the remaining gap is narrowed to the official server-authored hidden unlock matrix plus the broader profession session/runtime the simulator still does not model | inventory/runtime integration, crafting UI layer (`CUIItemMaker::Draw`, `CUIItemMaker::OnCreate`, `CUIItemMaker::SetComboBox_Category`, `CUIItemMaker::SetComboBox_Item`, `CUIItemMaker::OnButtonClicked`) |
| Partial | Item upgrade / enhancement window parity | The simulator now registers a dedicated `ItemUpgrade` owner instead of a placeholder, loads Gold Hammer or Vicious Hammer style client art from `UIWindow2.img` or `UIWindow.img`, binds the window to the live `CharacterBuild` plus inventory runtime, seeds starter enhancement scroll stock so the loop is immediately exercisable, resolves eligible items from WZ-backed `info/tuc` upgrade-slot data instead of a pure slot-type guess, consumes enhancement scrolls from the inventory surface, tracks per-item remaining upgrade slots and success or fail counts, applies lightweight stat growth back into `CharacterBuild`, routes right-click activation from supported enhancement consumables in the `USE` and `CASH` inventory tabs plus eligible equips in both legacy and Big Bang equipment windows into the dedicated owner while preselecting the clicked consumable or equipped target, now lets NPC interaction overlays whose loaded `String/Npc.img` function text advertises item-upgrade, enhancement, hammer, potential, or cube families open the dedicated owner, broadens the local enhancement loop beyond the baseline normal or advanced scroll pair by seeding and resolving the client `2049300/1/3/4/5/6/7/8/9` enhancement-scroll family including 2-star through 5-star result variants with multi-star success gains and destroy-on-fail handling, now adds local potential-scroll plus Miracle Cube result handling through `2049400/1/6`, `2049407/8`, `2049700/1/2/3`, and the broader WZ-backed cash family `5062000/1/2/3/4/5/2100`, seeds and resolves `2049500/1`, `5534000`, and `5610000/1` so carved seals, Urete's Time Lab, and Vega's Spell consumables are immediately exercisable from inventory, now also seeds a Maple 8th Anniversary Crimson equip so the WZ-gated `5062100` Maple Miracle Cube flow can be exercised locally, reads `Item/Cash/0506.img/05062100/req` to enforce the official Maple Miracle Cube equip whitelist instead of letting that cash item roll on arbitrary gear, preserves existing revealed potential lines when a carved seal adds an extra line instead of rerolling the whole set, now mirrors the WZ-described Epic Potential Scroll 80% failure behavior by treating destruction as a 20% on-fail roll instead of unconditional breakage, keeps Golden and Revolutionary Miracle Cube fragment grants attached to the specific cash items that advertise them, now also grants the matching Enlightening Miracle Cube shard locally, enforces the WZ-described Golden Miracle Cube Rare-through-Unique limit, now routes `5610000/1` into a dedicated `VegaSpell` owner loaded from `UIWindow.img/VegaSpell` instead of keeping Vega inside the shared item-upgrade surface, reuses the client `backgrnd10` / `backgrnd60`, count glyphs, success/fail panes, and `EffectTwinkling` / `EffectSpelling` / `EffectArrow` / `EffectSuccess` / `EffectFail` families for a separate local cast-and-result presentation, and tightens modifier-aware scroll resolution so Vega 10% and 60% consistently pair with the compatible advanced-vs-normal enhancement-scroll families before the backend attempt runs, but it still lacks the broader official server-authored scroll and cash-item result matrix outside this supported family, any stronger client-evidenced presentation swap into the dedicated MiracleCube-family owner art paths such as `UI/UIWindow2.img/MiracleCube_8th`, and the deeper exact client-side Vega session rules beyond this local owner-backed presentation | Progression-sensitive item workflows now have a visible dedicated enhancement surface, live inventory consumption, client-shaped inventory, equip, and NPC launch shortcuts, a broader enhancement-scroll and potential-scroll result matrix, WZ-backed Maple-cube whitelist enforcement for the `5062100` family, additional local shard-result coverage, preserved seal behavior, and a separate WZ-backed Vega cast/result owner instead of treating Vega as only another modifier inside `ItemUpgrade`, while the remaining gap is narrowed to unsupported server-authored item-result families, dedicated MiracleCube presentation ownership, and exact official Vega session rules rather than the already-wired local scroll, seal, cube-fragment, Maple-cube whitelist, and basic Vega presentation path | inventory/runtime integration, enhancement UI layer (`CUIItemUpgrade::Draw`, `CUIItemUpgrade::OnButtonClicked`) |
| Partial | Equipment window parity | Legacy and Big Bang equip windows now share `EquipSlotStateResolver`-driven disable handling for overall-vs-pants conflicts, beginner subjob and two-handed shield gating, Monster Riding slot gating, expired-item overlays, and zero-durability overlays, stay bound to the live `CharacterBuild` used by the avatar preview, and support simulator-side drag or drop between compatible character slots with held-item cursor feedback plus compare-style hover messaging while an equipped item is being moved; the Big Bang owner now also accepts inventory-to-character equips on the live character tab, returns displaced equips back to inventory with their item icons, stops mutating cached loader templates when slot moves retarget cloned `CharacterPart` instances, maps the heart slot onto the live android-heart family, and covers the broader client-facing slot families already exposed by the window art for pendant1 vs pendant2, pocket, badge, shoulder, android, android heart, medal, and belt-backed character panes. `CharacterBuild` now preserves concealed base equips under visible cash equips through a dedicated hidden-equipment layer, `CharacterLoader` no longer drops `LoginAvatarLook` hidden entries when a visible cash item already owns that slot, `LoginAvatarLookCodec` round-trips that visible-versus-hidden split, build stat totals plus weapon-sensitive shield gating can still see the concealed stat-bearing item, and both equip owners now surface the hidden base item in tooltip and drag messaging while restoring it when the visible cash item is moved off the slot. The Big Bang owner now also loads the full authored dynamic character-pane chrome set from `UIWindow2.img/Equip/character` through one WZ-backed seam, which in this client resolves to `cashPendant` plus `charmPocket`, and keeps those authored overlays tied to the live build-state rules for pendant2 and pocket availability even when those states are preserved only through the hidden-equipment layer. Both local slot-to-slot moves plus inventory-to-character equips still route through `CharacterBuild.PlaceEquipment` so concealed base items stay hidden under cash equips and only the correct displaced base layer returns to inventory, and the Big Bang character tab now rejects invalid local equip attempts with explicit blocked messaging for disabled slots, wrong slot families, item-load failures, unmet level or stat or fame or job requirements, and invalid swap targets that cannot return to the source slot. The remaining gap is the true packet-authored and server-owned equip-change workflow: official accept or reject sequencing, authoritative post-move validation against server state, and the surrounding session or packet ownership that still wraps real client equip changes rather than additional shipped chrome variants in this client build | Equipment-heavy testing now has visible disable and break-state feedback on the actual character equipment surface, a client-shaped held-item interaction path, live inventory-to-equip routing on the Big Bang character page, broader slot-family coverage for the non-rendered accessory families already present in the client window art, the full shipped authored pendant/pocket chrome family loaded through the same WZ-backed seam that drives its placement, a real client-shaped visible-versus-hidden equip data path instead of dropping concealed base gear from avatar-look and equipment-window resolution or returning the wrong concealed base item during local swaps, and a simulator-side rejection path that now surfaces the main local slot, requirement, and swap failures instead of silently ignoring them; the remaining gap is narrowed to authoritative packet or server ownership and exact official validation sequencing rather than missing authored chrome or the main local cash-layer swap semantics | `EquipUI.cs`, `EquipUIBigBang.cs`, `CharacterBuild`, `CharacterLoader`, `CharacterAssembler`, `EquipSlotStateResolver.cs`, `UIWindowLoader.EquipmentAndStats.cs` (`CUIEquip::Draw`, `UIWindow2.img/Equip/character`) |
| Partial | Pet, dragon, and android equipment tabs | The Big Bang equip window now loads the WZ-backed companion pane art for `pet`, `dragon`, `mechanic`, and `Android`, keeps the character equipment surface visible while those panes are open, highlights the active tab buttons, populates the Multi Pet pane from the live `PetController` with per-pet icons plus auto-loot markers and tooltips, seeds dedicated dragon and android companion equipment runtimes so those pages render live equipped-item icons and tooltips on the client slot geometry, now also seeds a dedicated mechanic equipment runtime from the client `Character/Mechanic` asset family so the mechanic page draws live `ENGINE`, `FRAME`, `TRANS`, `ARM`, and `LEG` icons with slot-specific tooltips instead of stopping at an empty-state stub, and now accepts drag/drop from the live equip inventory into the pet, dragon, mechanic, and android panes with slot-family validation, client-shaped android overall-vs-pants gating, occupied-slot replacement back into inventory, a WZ-backed `Character/PetEquip` loader for live pet accessory icons and tooltips, seeded starter pet accessory inventory entries, and drag-back-out handoff from every companion pane into the live inventory window; pet slots can now accept simulator-side pet accessories, render the equipped accessory in-slot with pet-aware tooltip context, and swap that accessory across active pets instead of remaining a pet-icon-only stub, and the shared companion validation seam now also reads WZ `reqLevel`, `reqJob`, `reqSTR`, `reqDEX`, `reqINT`, `reqLUK`, and `reqPOP` metadata for pet/dragon/mechanic items, surfaces reject messages through the same blocked-equip notice path the main equip window already uses, preserves pet-specific compatibility checks during pet-to-pet accessory swaps, requires both an equipped android and equipped heart before the Android page accepts gear instead of treating the android slot alone as sufficient ownership, and now resolves android heart-family compatibility against the WZ `String/Eqp.img/Eqp/Android` catalog so duplicate heart item ids that share a client family name still satisfy the same android-family requirement instead of relying on a name-only heuristic. The remaining gap is that the simulator still does not mirror the client's deeper cash-state and account/character ownership rules or the packet-authored companion-inventory validation / server rejection flow that surrounds official companion equipment changes | Companion tab clicks are no longer dead-end toggles, the pet page now participates in the same inventory-backed equip loop as the other companion panes, invalid companion drops now explain why they were rejected instead of failing silently, and android ownership gating now includes WZ-backed heart-family matching rather than a freeform name comparison; the remaining gap is narrowed to deeper cash/ownership semantics and packet-authored validation rather than missing pet equip handling, an empty mechanic page, one-way drag/drop, or the android heart matrix itself | `EquipUIBigBang.cs`, `UIWindowLoader.cs`, `PetLoader.cs`, `PetController.cs`, `CompanionEquipmentController.cs`, inventory/window manager integration (`CUIEquip::Draw`) |
| Partial | Ability and stat window parity | The simulator already draws both legacy and Big Bang stat windows, supports AP assignment, job-biased auto-assign, detail-panel toggles, build-backed guild/fame/EXP text, hands/critical detail values, per-stat AP cap enablement, and Big Bang open-vs-close detail button visibility; the stat rows now also resolve equipped stat bonuses through `CharacterBuild`, the detail pane now derives attack/defense, accuracy/avoidability, hands, critical, speed, and jump from live build totals instead of static placeholder fields, `SkillManager` now seeds non-hidden passive books alongside active skills for the current job path, and `CharacterBuild` now folds passive plus live buff attack/defense, magic attack/defense, accuracy, avoidability, critical-rate, speed, jump, and skill-driven max HP/MP bonuses into those totals so the stat windows react to learned skills rather than gear alone, while Big Bang HP/MP AP clicks still use class-aware gain ranges with a 30,000 cap instead of a flat `+20`; the displayed physical and magical damage rows now also use a client-shaped min/max average where mastery scales the primary term instead of naively scaling the full max value, dagger formulas now split thief-vs-non-thief primary stats instead of treating every dagger as a thief weapon, extended Cygnus/hero/Resistance job branches now map back onto their base warrior or mage or archer or thief or pirate stat archetypes for accuracy, avoidability, defense, and Big Bang HP/MP AP growth, `CharacterBuild` now recognizes cane, dual bowgun, and cannon weapon families in the displayed damage surface, and `SkillLoader` plus `SkillManager` now use `String/Skill.img` mastery names together with broader passive alias-field parsing (`ar`, `accX`, `padX`, `madX`, `pddX`, `mddX`, `er`, `evaX`) plus enhanced-stat aliases (`epad`, `emad`, `epdd`, `emdd`, `emhp`, `emmp`) so newer mastery, expert, blessing, shield-style, and avoidability or accuracy books can feed the stat windows instead of being dropped when WZ does not use the older base property names; `SkillManager` now also falls back to `String/Skill.img` description text for generic and specialized mastery books whose weapon family is not encoded in the display name alone, so WZ-authored books such as Fighter/Page/Spearman `Weapon Mastery`, Aran `High Mastery`, and Battle Mage `Barricade Mastery` once again drive the displayed mastery and damage floor for the correct equipped weapon families instead of being stranded behind name-only matching; the same mastery path now also respects WZ-authored conditionals such as Paladin `1220010 Advanced Charge` only while a live charge buff is active and covers stringless fallback cases such as `skill/5110.img/skill/51100001/common/mastery` and `skill/570.img/skill/5700000/common/mastery` by mapping those books back onto their observed knuckle and gun families instead of dropping them when `String/Skill.img` has no matching entry; `SkillLoader` now also evaluates WZ-authored max-HP/max-MP rate fields such as `skill/100.img/skill/1000006/common/mhpR = 2*x`, `skill/200.img/skill/2000006/common/mmpR = 2*x`, `skill/510.img/skill/5100009/common/mhpR = 2*x`, and buff-side `indieMhpR` / `indieMmpR` branches such as `skill/2411.img/skill/24111003/common/indieMhpR` so the stat windows include percentage-based HP/MP books instead of only flat `emhp` / `emmp` bonuses, and `CharacterBuild.TotalHands` now also includes equipped `incCraft` bonuses instead of dropping glove or gear hands from the detail panel. Mechanic's `Extreme Mech` bonuses now only surface while the live mech mount is active instead of behaving like an unconditional passive, weapon-specific mastery no longer resolves while no weapon is equipped, and non-Mechanic alias-backed buffs such as Onyx or Divine Shield style `e*` stat lines now also contribute to the live totals, but the simulator still does not mirror every exact post-Big-Bang coefficient or the remaining long-tail conditional mastery or weapon-formula branches for the unresolved newer-family and per-job edge cases beyond the newly covered charge-state and stringless-mastery cases | Visible stat windows now expose runtime-backed totals from equipped gear, learned passive skills, active buff bonuses, class-sensitive HP/MP AP growth across both classic and extended job branches, WZ-backed percentage HP/MP skill boosts, the missing `incCraft` hands contribution, a broader client-shaped mastery-aware damage surface for cane, dual-bowgun, and cannon users, WZ-backed `ar`/`er` alias support for accuracy and avoidability books, and charge-aware plus stringless fallback handling for mastery books. The remaining divergence is narrowed to the exact coefficient matrix and the still-unimplemented long-tail mastery or formula rules beyond these rate-backed stat surfaces, alias-backed passive totals, description-backed mastery family mapping, charge-state mastery gating, and Mechanic mech-state gating | `AbilityUI.cs`, `AbilityUIBigBang.cs`, `CharacterBuild`, `SkillManager`, `SkillLoader`, stat/runtime layer (`CUIStat::Draw`, `CUIStat::DrawTextA`) |
| Partial | Character info / profile window parity | The simulator now loads a dedicated WZ-backed `UserInfo` window from `UIWindow.img/UserInfo` or `UIWindow2.img/UserInfo/character`, routes the status-bar `BtCharacter` button into that owner, keeps the primary profile page build-backed with name/job/level plus world-rank and job-rank plus guild/alliance fields, and the Big Bang owner now switches into WZ-backed ride, pet, collect, and personality subpages that render simulator-side mount state, active pet data, collection summaries, and optional trait values instead of leaving those buttons decorative. The owner now also loads the page-specific `BtException`, `BtArrayName`, and `BtArrayGet` controls from `UIWindow2.img/UserInfo`, hides primary action buttons outside the character page so the per-page button surface matches the selected subpage more closely, forwards `BtParty` into the existing social-list owner instead of leaving it as a pure message stub, opens the WZ-backed `exception` popup with local register/delete plus meso-filter toggles instead of leaving that client-owned control path dead, consumes the character-page `BtPopUp` and `BtPopDown` buttons as real selector controls for the `item` and `wish` popups, renders the `productSkill` icon family from `UIWindow2.img/UserInfo/character/productSkill` against the live simulator-side maker progression snapshot, and uses that same progression snapshot to replace the old equip-count collection summary with maker, glove, shoe, toy, craft-count, and discovered-recipe metadata. The collection page now also treats `BtArrayGet` as a local claim action instead of another decorative control while the collect icon still opens the dedicated book owner, the primary rank rows now consume packet/account-backed previous-rank state through the existing `RankDeltaSnapshot` seam so stored world/job rank movement surfaces inside the profile window instead of being stranded in login-only data, and the personality page now consumes the fuller WZ-backed `Tooltip` family from `UIWindow2.img/UserInfo/personality/Tooltip`, including the charm `collection/0` body and numeric glyph set, so hover state can render the client art for pocket-slot charm collection messaging instead of falling back to plain text only. The remaining gap is the deeper `CUIUserInfo` interaction surface such as true remote-target trade or party invite submission, popularity/family-chart request semantics for inspected remote characters, server-sourced collection/medal/charm payloads, and the exact server workflows behind those buttons | Character-facing information now has its own launch path, a broader button-driven surface distinct from the stat window, WZ-backed popup navigation and personality-hover art, packet/account-backed rank-delta presentation, and richer simulator-backed maker-collection metadata instead of a mostly read-only page shell; the remaining gap is narrowed to remote/server-authored `CUIUserInfo` request and data workflows rather than dead `BtPopUp`/`BtPopDown`, missing tooltip art, an inert collect action, or rank deltas that never reach the owner | character/profile UI layer, player metadata presentation (`CUIUserInfo::Draw`, `CUIUserInfo::OnButtonClicked`) |
| Partial | Book / collection window parity | MapSimulator now registers a dedicated `BookCollection` owner backed by `UIWindow.img/UIWindow2.img/Book`, uses the client-owned `BtPrev`, `BtNext`, and `BtClose` controls at the IDA-recovered `CBookDlg::OnCreate` button positions, reuses separate dot-marker canvases for the spread markers, and keeps the same two-page paging cadence that `CBookDlg::OnButtonClicked` applies by moving in two-page increments and closing through the owner-local close path instead of behaving like another generic placeholder. The runtime now also feeds the owner from the live character build plus the existing item-maker progression snapshot, opens it from the `UserInfo` collect surface, and renders a real two-page spread with centered page-index text plus per-row collection entries for overview, crafting-family progression, traits, equipment ledger rows, and discovered recipe pages. The remaining gap is that the simulator still approximates the client's `CT_INFO` layout, font matrix, and data source: it does not yet mirror the exact twelve-font draw treatment recovered from `CBookDlg::OnCreate`, the exact page-marker resource path used by the client, or the true server-/packet-authored collection dataset and `CWvsContext::CloseBook` workflow that drive the official owner. | This is now a real stand-alone utility surface rather than dead backlog theory: the simulator has the separate paged owner, explicit book-button flow, and a concrete collection-data presentation seam distinct from `UserInfo`. The remaining divergence is narrowed to exact draw fidelity and live client-owned collection data instead of total owner absence. | collection/book UI layer (`CBookDlg::OnCreate`, `CBookDlg::Draw`, `CBookDlg::OnButtonClicked`) |
| Partial | Quest log window parity | The simulator quest windows now render live runtime-backed lists for available, in-progress, completed, and recommended quests, instantiate the client `Tab/enabled` / `Tab/disabled` category buttons plus `BtMyLevel` / `BtAllLevel` filters from `UIWindow.img` or `UIWindow2.img`, keep selection synchronized with quest state mutations, color rows by accept/complete readiness, expand the list owner to use the dedicated quest-log shell instead of a simulator-only inline detail pane, preserve item-backed requirement/reward hover popups by moving them onto the dedicated detail owner, now load and honor the legacy `UIWindow.img/Quest/BtDetail` list-shell button so pre-Big-Bang quest browsing uses a client-shaped select-then-button cadence instead of forcing the Big Bang-style double-click open path, now also mirror the client `CUIQuestInfo::OnTabChanged` reselection cadence more closely by preferring suitable-level quests on the available tab, recently updated or recently viewed quests on the in-progress tab, and top entries when the prior selection no longer survives a tab or filter rebuild, now expose the client quest-detail location button families through the existing detail owner by routing NPC, mob-demand, and item-demand fallback cases into the world-map search owner instead of treating every specialized case as a pure NPC locate, now also mirror the client demand-item query split more closely by parsing WZ `Check.img/.../item/*/secret` flags, suppressing hidden demand items from local world-map results, and seeding a packet-shaped current-field item-query result set when the detail owner asks for demand-item guidance, now also split the Big Bang quest-detail shell back onto the client button families by reserving `UIWindow2.img/Quest/quest_info/BtQuestDeliveryAccept` and `BtQuestDeliveryComplete` for delivery-specific guidance while primary accept/complete return to the shared `Quest/BtOK` art, surfacing item-backed delivery hints through those dedicated buttons and routing their cash-item fallback into the existing quest-delivery or Cash Shop owners with the client hardcoded cash-item ids `5660000` / `5660001` and shop migration SNs `50200218` / `50200219`, and now parse WZ quest `area` ids into readable quest categories so the Big Bang list shell can rebuild each tab through persistent per-tab category option filters driven by the client `BtMax` / `BtMin` controls instead of treating category changes as a no-op. They still do not mirror the client's native per-tab category-button matrix and icon-info sheet compositor exactly, server-authored item-query map results beyond the simulator's local current-field fallback, per-quest delivery-type metadata, or the remaining demand-rect-specific delivery branches behind the most specialized quest-detail buttons | Quest interaction now has a usable standalone log that reflects the same runtime state as NPC quest overlays, uses WZ-backed quest categories instead of a custom tab strip, follows the legacy detail-button cadence where that WZ shell exists, automatically reselects client-shaped quest entries when tabs or category filters rebuild, preserves the client split between generic quest actions and the dedicated Big Bang delivery-button family, and exposes broader world-map or delivery-owner quest target routing instead of leaving specialized locate or delivery cases as decorative art; the remaining divergence is narrowed to the last client-native category matrix presentation, true server-authored demand-item query resolution, and deeper delivery or demand-rect metadata rather than tab reselection, secret-item filtering, or every locate button collapsing to the same NPC-only path | `QuestUI.cs`, `QuestUIBigBang.cs`, `QuestDetailWindow.cs`, `QuestRuntimeManager.cs`, `UIWindowLoader.SkillsAndQuests.cs`, NPC/UI bridge (`CUIQuestInfo::Draw`, `CUIQuestInfo::OnButtonClicked`, `CUIQuestInfo::OnTabChanged`, `CUIQuestInfoDetail::OnButtonClicked`) |
| Partial | Quest detail window parity | The simulator now exposes a dedicated `QuestDetail` owner backed by the quest runtime, reuses the client `Quest/quest_info` frame family when present, opens from quest-list selection, renders separate summary/requirements/rewards sections with WZ-backed summary headers plus gauge art, shows color-coded requirement rows, now also switches incomplete requirement rows onto the client `prob` demand-strip family while completed/reward rows keep the `select` strip family when those canvases are present, draws reward item icon rows through the live item-icon loader, switches accept/complete/track/give-up controls across the client quest-detail button families when that art is present, supports previous/next quest navigation within the active tab, wires standalone accept or give-up actions plus completion guidance distinct from the list shell, keeps the runtime-backed tertiary locate-NPC action visible through the quest-detail owner, reuses legacy `BtGotoNpc` and `BtMarkNpc` families when present while falling back to the Big Bang `BtNPC` family, now adds a dedicated mob-locate action for incomplete mob demands and routes it into the world-map search owner so quest-detail delivery guidance can mark the current-field mob target without displacing the NPC-locate button, lays out four-button delivery or completion cases without collapsing them back into the older two-button shell, draws quest notice banners plus animated notice icons from the client quest notice families when that art is present, and now pushes demand/reward count text into separate runtime fields so the detail owner can draw native-width `select` / `prob` strips with split body-vs-count composition, right-aligned counters, live item icons, and tighter client-shaped `253px` content bounds instead of stretching every row across a generic full-width strip. The remaining gap is that the simulator still does not mirror the client遯�E�蜀�E� full CT-driven row compositor and scrolling math from `CUIQuestInfoDetail::Draw`, broader quest-demand world-map overlays outside the current search-result seam, or the remaining packet- and cash-item-driven delivery workflows behind the most specialized detail buttons | The simulator now exercises the client's split between quest browsing and per-quest inspection with client-shaped notice chrome, WZ-backed incomplete-vs-complete demand row art, distinct NPC and mob world-map guidance from the detail owner, broader delivery/completion button layouts, and more client-shaped demand row composition instead of a mostly text-only detail pane; the remaining gap is now concentrated in the last CT-layout fidelity and deeper delivery/world-map branches rather than owner absence, missing locate-button paths, or stretched placeholder demand strips | `QuestUI.cs`, `QuestUIBigBang.cs`, `QuestDetailWindow.cs`, `QuestRuntimeManager.cs`, quest-detail interaction layer (`CUIQuestInfoDetail::Draw`, `CUIQuestInfoDetail::OnButtonClicked`) |
| Partial | Quest alarm / notifier window parity | The simulator now replaces the `QuestAlarm` placeholder with a dedicated WZ-backed tracker built from `UIWindow.img/UIWindow2.img/QuestAlarm`, supports minimize/maximize toggles, title counts, live in-progress quest rows with completion-state coloring, row selection, per-row dismiss buttons shaped from the client `BtDelete` art, double-click routing into the quest detail flow, and now reuses shared `UIWindow2.img/Quest/quest_info` chrome for selection, incomplete `prob` strips, and progress-gauge rendering while surfacing negative quest `act/money` costs as live meso progress rows and rewarding or consuming mesos against the bound inventory runtime during quest start or completion. The quest-alarm runtime also now tracks recent quest-state, mob-kill, and tracked-item updates so the client `BtQ/ani` alert frames and sky-blue recent-update emphasis can light up before `BtQ` falls back to the normal quest-log toggle path, and the owner now follows the client-recovered five delete-slot layout more closely by exposing five visible quest titles per page, resizing the maximized frame to the active selected-demand stack, paging larger quest sets with mouse-wheel plus keyboard navigation, and adding a local delete-all header action with `Delete`/`Shift+Delete` shortcuts on top of the existing per-row dismiss path. The `BtAuto` toggle now drives a local auto-register mode instead of acting as selection-only chrome: auto-registered active quests can be locally deleted into a persisted hidden set, manual quest-detail tracking pins quests into a persisted tracked set even when auto-register is off, and the owner restores per-character auto mode plus minimized state from AppData across simulator restarts. The selected quest row now composes more like the recovered client `CUIQuestAlarm::Draw` path by keeping non-selected quests collapsed to 18px title bands, expanding only the selected quest into a 150px demand rect, and drawing all requirement or meso rows with right-aligned counters and `select` versus `prob` strip families instead of rendering every visible quest as a full multi-line simulator card. The owner now also follows `CUIQuestAlarm::OnButtonClicked` more closely when the local tracker is empty or cleared: `BtMax` refuses to expand and surfaces a chat notice when there are no registered quests, `Delete all` clears the local registration set and closes the owner, title rows clamp against the client-shaped pixel width instead of a fixed character count, and demand-rect body text now carries wrapped issue lines plus oversized-token splitting so long simulator text stays inside the 150px client band more reliably. The remaining gap is that this is still a simulator-side approximation rather than the client's packet-driven `CUIQuestAlarm` registration path, so exact server-authored pin/delete semantics, the full official auto-register rule matrix behind `BtAuto`, the exact StringPool-driven label text and font choices, and the last CT-authored line compositor or wrap micro-rules inside the demand rect are still not mirrored exactly | Active questing now has a persistent standalone progress tracker with animated quest-alert feedback, row-local and owner-wide dismiss affordances, a client-shaped five-slot paging surface, richer WZ-backed progress chrome, recent-update emphasis, a closer `BtMax`/`Delete all` lifecycle, and a per-character persisted auto-register/pinned-tracking model instead of a mostly session-local list shell. The remaining gap is now narrowed further to packet-authored registration semantics, the official `BtAuto` rule matrix, and the final client-only text/font/compositor behavior rather than to missing persistence, an over-permissive empty maximize path, absent delete-all close behavior, or fixed-character title and body wrapping. | progression/UI layer, quest runtime, demand rendering (`CUIQuestAlarm::Draw`, `CUIQuestAlarm::OnButtonClicked`) |
| Partial | Book collection / Monster Book parity | `BookCollectionWindow` now swaps the old generic `UIWindow(.2).img/Book` shell for the dedicated `UIWindow(.2).img/MonsterBook` owner, loads the client-backed `arrowLeft` / `arrowRight` / `BtClose` controls at the IDA-confirmed `CBookDlg::OnCreate` positions, renders the WZ `cardSlot`, `infoPage`, `cover`, `select`, and `fullMark` chrome, and binds both the book window and the `UserInfo` collect page to a real simulator-side Monster Book runtime instead of the local item-maker snapshot. The new `MonsterBookManager` builds its catalog from `item/Consume/0238.img` plus mob metadata, persists per-character card ownership under AppData, tracks card counts up to the client-like five-card completion mark, records local ownership from matching mob kills plus picked-up Monster Card items, and feeds paged 25-slot card spreads with selected-card detail plus owned/completed boss-vs-normal summary counts into the owner. IDA still anchors the split correctly: `CBookDlg::{OnCreate,Draw,OnButtonClicked}` own the UI shell while `CMonsterBookMan::LoadBook` confirms the separate loader path, and the simulator now mirrors that relationship with a dedicated manager feeding the owner rather than a generic progression provider. The remaining gap is the exact client tab/category routing across `LeftTab` / `RightTab`, the search/register/release context-menu flows, official card-drop or packet/save ownership instead of the simulator's local kill-backed approximation, and the deeper `CWvsContext::CloseBook` / packet-authored lifecycle around the manager-owner pair. | Collection/book parity is now materially closer to the client because the simulator no longer treats this area as a maker-flavored collection page: it has a dedicated Monster Book owner, a distinct Monster Book manager, save-backed card state, WZ-backed card catalog metadata, and live card-page rendering. The remaining divergence is narrowed to exact client category semantics and network/authored ownership flow rather than absence of a Monster-Book-specific data/runtime seam. | book-style collection owner and data bridge (`BookCollectionWindow.cs`, `UserInfoUI.cs`, `MonsterBookManager.cs`, `CBookDlg::OnCreate`, `CBookDlg::Draw`, `CBookDlg::OnButtonClicked`, `CMonsterBookMan::LoadBook`) |
| Partial | Parcel / package-delivery window parity | The simulator already has a usable mailbox package seam through `MemoMailboxWindow`, `MemoSendWindow`, `MemoGetWindow`, and `MemoMailboxManager`: the status-bar launcher can open an inbox, drafts carry recipient/subject/body plus item or meso attachments, delivered entries preserve read/unread and kept/deleted state, and attachment claims exercise a dedicated package-view flow instead of collapsing package delivery into plain chat text. The remaining gap is that this still models the older memo/mailbox flow through `UIWindow2.img/Memo` rather than the client-confirmed parcel owner: IDA shows `CParcelDlg::{OnCreate,Draw,OnButtonClicked}` constructing a distinct receive/send/quick-send dialog with its own tabs, edit controls, scroll bar, and button routing. Packet-owned parcel session population, exact tab behavior, quick-send constraints, and the true relation between memo/social messaging and the parcel/package owner are still not mirrored. | Package delivery is a real client-owned utility window family, not just a note inside messenger or memo systems. Making it explicit keeps the simulator's existing mailbox/package work visible while also separating that progress from the still-missing `CParcelDlg` tabbed parcel/session behavior recovered in IDA. | mailbox/package owner versus parcel dialog owner (`MemoMailboxWindow.cs`, `MemoSendWindow.cs`, `MemoGetWindow.cs`, `MemoMailboxManager.cs`, `CParcelDlg::OnCreate`, `CParcelDlg::Draw`, `CParcelDlg::OnButtonClicked`) |
| Partial | Skill macro parity | The macro window now enforces trimmed macro names against a client-shaped 12-byte budget instead of a flat 12-character ASCII subset, validates names inline against the current Windows ANSI locale, shows a live byte counter in the editor, routes committed character input through the simulator `GameWindow.TextInput` path so locale and IME text commits no longer depend on the old synthetic ASCII key mapping, accepts clipboard paste as a best-effort locale text insert that fills the remaining byte budget instead of rejecting an otherwise valid prefix wholesale, owns a real cursor-aware name edit field with mouse caret placement plus `Backspace`/`Delete`/arrow/`Home`/`End` behavior instead of the old append-only buffer, now advances that caret and applies delete or backspace on grapheme-text-element boundaries so composed IME text and surrogate-pair glyphs are edited as a single unit instead of being torn apart mid-sequence, mirrors `CUIMacroSysEx`/`CCtrlEdit` ownership more closely by routing Win32 IME preedit composition into inline macro-name preview text with a blinking caret and composition underline before committed `TextInput` lands, and now clamps that preedit preview to the same locale or byte-valid insertable prefix the committed path would accept so the visible preview and live byte counter stay aligned with the eventual insert result. It also loads the client `UIWindow.img/SoftKeyboard` chrome as an attached helper with client-shaped key-grid hit-testing from `CUISoftKeyboard::GetKeyIndexFromPoint`, hover feedback, min/max/close controls, caps or shift toggles, backspace or enter actions, and now applies the client-derived `CUISoftKeyboard::UpdateKeyConstraint` mode split before the existing byte-budget or locale-aware insertability gate decides whether a key can advertise as clickable, still supports drag-binding selected macros into QuickSlot entries, routes macro hotkeys back through `SkillManager` with party-chat announce semantics, paces queued casts so the next macro skill waits for the current cast to finish, and persists the five-slot macro definition list per active player or selected login-roster character under the simulator AppData profile across restarts instead of losing macro definitions with the session. The remaining gap is the deeper locale-specific character blacklist behavior beyond the active code-page check, plus the exact client candidate-window placement and IME candidate UI. | Macro support now behaves like a real runtime surface instead of an editor-only stub, and the remaining gap is narrowed to deeper locale blacklist rules plus exact candidate-window ownership rather than missing interaction ownership, committed locale input, byte-count enforcement, preview-clamped preedit rendering, text-element-safe editing, or the soft-keyboard constraint-mode split. | `SkillMacroUI.cs`, `SkillMacroSoftKeyboardLayout.cs`, `SkillMacroNameRules.cs`, `SkillManager.cs`, `QuickSlotUI.cs`, `MapSimulator.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `WindowsImeCompositionMonitor.cs` (`CUIMacroSysEx`, `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::UpdateKeyConstraint`) |
| Partial | Soft keyboard and constrained text-entry parity | The simulator now has a dedicated WZ-backed soft-keyboard owner loaded from `UIWindow.img/SoftKeyboard`, uses the client-derived `CUISoftKeyboard::GetKeyIndexFromPoint` grid geometry for hover and click hit-testing, routes hover state through a top-level UI manager seam so the key surface owns mouse focus ahead of underlying windows, and applies explicit per-mode enable gating that now matches the recovered four-way `CUISoftKeyboard::UpdateKeyConstraint` matrix (`0` alphanumeric-until-full, `1` alpha-edge/numeric-middle, `2` numeric-until-full, `3` numeric-until-full-alt) instead of only a loose alpha-vs-numeric split. The constrained map-transfer target field still attaches as a numeric-only edit surface, the shared helper now also owns the login title ID/PW fields through the existing login-title owner seam, and `LoginUtilityDialogWindow` now carries an explicit keyboard type per prompt so client-authored PIC flows use the same `type 3` numeric-only-alt constraint recovered from `CPinCodeDlg::OnKey` while physical-key entry is filtered through the same shared mode gate instead of bypassing the helper's restrictions. The remaining gap is that the simulator still does not model every client utility edit owner that would exercise the remaining non-PIC utility types such as the `CLoginUtilDlg::OnKey` `type 2` fixed-length numeric path, and it still approximates the client's exact modal soft-keyboard ownership plus IME candidate-window behavior rather than mirroring every dialog-specific launch/close path. | This is now a separate parity target from generic macro name validation because it affects how locale input, constrained text fields, and client-owned edit controls behave before characters are committed. The simulator now has the on-screen key surface, hover ownership, a client-shaped four-type constraint matrix, and shared-host attachment across map transfer plus the login-owned utility fields that are currently modeled; the remaining divergence is narrowed to still-missing utility owners and the last modal/IME ownership details rather than helper absence or unconstrained raw keyboard entry. | utility-window text-entry layer, login-owned edit bridges, map-transfer edit bridge, shared soft-keyboard owner (`CUIMacroSysEx::OnCreate`, `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::OnMouseMove`, `CUISoftKeyboard::HitTest`, `CUISoftKeyboard::UpdateKeyConstraint`, `CLoginUtilDlg::OnKey`, `CPinCodeDlg::OnKey`) |
| Partial | Repair-durability utility parity | MapSimulator now registers a dedicated `RepairDurability` owner backed by `UIWindow2.img/Repair`, routes packet `OnOpenUIWithOption` launches for `uiId == 33` into that owner instead of stopping at the dispatcher seam, preserves the client-derived option value as the repair NPC template id, blocks the launch when another unique-modeless utility owner is already visible, and lists currently damaged equipped or hidden-equipped parts with per-item durability, selection, scroll, WZ-backed row chrome, and dedicated `Repair` / `Repair All` buttons. The owner still uses the client-derived repair-cost formula shape recovered from `CItemInfo::EQUIPITEM::CalcRepairDurabilityPay`, but it now also replaces the text-only NPC header with a live animated NPC preview built from the WZ NPC loader, orders the damaged entries in client-shaped avatar/body-part order before hidden overlays, and routes repair clicks through a simulator-side send/result queue so the owner shows a pending request state before meso consumption and durability restoration are applied on the response path instead of immediately on button press. The remaining gap is narrowed to the exact client wear-source enumeration and wire semantics behind `SetNPC`, `SendRepairDurability`, and `SendRepairDurabilityAll`, especially the unresolved slot-pos encoding and any server-driven success/failure details that are still locally simulated. | Repair-durability is now a packet-owned utility surface with dedicated art, owner lifetime, option routing, repair math, an animated NPC layer, and a request/response-shaped repair flow, so the remaining divergence is concentrated in the final client slot-source and packet-result fidelity rather than missing owner behavior. | utility-window owner launched from local packet dispatch (`CUserLocal::OnOpenUIWithOption`, `CRepairDurabilityDlg`, `CItemInfo::EQUIPITEM::CalcRepairDurabilityPay`) |
| Partial | Pickup notice parity | The simulator pickup-notice stack now renders item icons, resolves cached item names and type labels for success messages, records player pickup history, reconnects manual pickup failures through `PlayerCombat` so ownership and inventory-capacity checks consistently raise notices before the drop is consumed, mirrors the client `-3` style split through a shared screen/chat formatter, restores the client `-2` split by leaving inventory-full as a screen-only notice, routes meso success notices through the same formatter so pet meso pickups surface as a chat-log side effect instead of polluting the screen string, and aligns regular plus quest-item success wording more closely with `CWvsContext::OnDropPickUpMessage` by using the same item-type label path rather than pet-prefixed or generic quest text. It still applies a pet-specific validator backed by WZ `item/0200.img/02002058/spec/notPickupByPet` so local auto-loot can surface pet-blocked items with item-aware pet messaging without spamming repeated failures, resolves `Unavailable` failures against `DropPool` recent-pickup history so pet-, player-, mob-, and generic remote-preemption cases surface actor-aware pickup lines instead of collapsing to one fallback string, and wires `DropPool` remote player and remote pet pickup callbacks into the same paired screen/chat notice seam instead of limiting live remote-loot surfacing to the mob callback alone. The remaining gap is narrowed to exact official wording for simulator-owned failure or remote-preemption messages whose backing StringPool text is still not captured in-source, plus the fact that live remote-loot surfacing still depends on explicit `DropPool` producers rather than every packet/runtime source that could feed those outcomes | Pickup feedback now matches the verified client branch shape for `0`, `1`, `2`, `-2`, `-3`, and the default fallback more closely, with success-path screen/chat splits and inventory-full behavior aligned to the decompiled handler; the remaining divergence is concentrated in exact uncaptured strings for simulator-owned remote or failure variants and in still-unwired remote-loot producers outside the current `DropPool` callbacks | `PickupNoticeUI.cs`, drop/item runtime (`CWvsContext::OnDropPickUpMessage`, `CUIScreenMsg::ScrMsg_Add`) |
| Partial | Cursor and utility-button parity | The simulator now builds dedicated WZ-backed `Menu` and `System` popup owners from `StatusBar2.img/mainBar/Menu` and `StatusBar2.img/mainBar/System`, routes the status-bar `BtMenu` and `BtSystem` buttons through those owners, fans their core entries back into inventory/equip/stat/skill/quest/character-info and channel-selection flows instead of leaving them as dead launchers, switches the cursor to the client hold art during active skill, quick-slot, and window drag flows, loads the client forbidden-hand and busy-search cursor families from `Basic.img/Cursor/13` and `Basic.img/Cursor/16`, dismisses the popup owners on outside clicks, now registers WZ-backed `KeyConfig` and `OptionMenu` owners from `UIWindow2.img/KeyConfig` and `UIWindow2.img/OptionMenu`, routes `BtKeySetting`, `BtGameOption`, `BtSystemOption`, `BtOption`, and `BtJoyPad` into those owners instead of forbidden stubs, binds key settings back to live `PlayerInput`, now shows the attached pad binding beside each keyboard row in `KeyConfig`, cycles the shared joypad page through active controller slot plus core jump/attack/pickup/interact/skill/inventory/escape gamepad bindings instead of leaving `BtJoyPad` as placeholder copy, and still exposes simulator-backed smooth-camera plus BGM/SFX/focus-loss audio toggles through the option pages; `BtRank` and `BtEvent` also now land on WZ-backed ranking/event owners that render live simulator-backed ranking cards plus filtered event rows from login bootstrap, packet-owned field or overlay state, quest-guide or delivery routing, tracked quest alerts, and active special-field runtimes instead of placeholder shells. The remaining gap is that full client joypad calibration and constraint behavior, true packet-fed ranking ladders, attendance or calendar event payloads, and the deeper remote ranking/event dispatch behind `CUIStatusBar::OnButtonClicked` are still not mirrored exactly. | This is part of the client feel layer and affects almost every window or interaction surface once the rest of the UI becomes more complete. The row now covers client-shaped utility owners with live simulator-backed payloads for joypad, ranking, and event flows, while the remaining gap is narrowed to official network-fed data, exact joypad semantics, and the last client dispatch fidelity rather than dead owners or placeholder content. | `MouseCursorItem.cs`, `StatusBarUI.cs`, `UILoader.cs`, `StatusBarPopupMenuWindow.cs`, broader UI manager layer (`CUIStatusBar::OnButtonClicked`) |
| Partial | Explicit ranking/event owner parity | A follow-up IDA lookup after the backlog sweep confirms that the ranking and event launches are not just anonymous status-bar utility shells. The client has a dedicated ranking owner through `CUIRanking::OnCreate` at `0x83ca40`, `CUIRanking::OnButtonClicked` at `0x83c6a0`, and `CUIRanking::Update` at `0x83c690`, plus a separate event owner through `CUIEventAlarm::OnCreate` at `0x7ac4f0`, `CUIEventAlarm::Draw` at `0x7aad90`, and `CUIEventAlarm::Update` at `0x7aac00`. The simulator already has WZ-backed `RankingWindow` and `EventWindow` surfaces behind `BtRank`/`BtEvent`, but the backlog previously only tracked them as part of the broad cursor/utility-button row instead of naming the concrete client owners and their separate lifecycle. The remaining gap is the exact client page/filter/button semantics, attendance/calendar payload ownership, and true packet-fed ranking/event content behind those owners rather than the existence of dedicated windows at all. | Status-bar parity here is not only about buttons launching something. The client treats ranking and event views as separate utility owners with their own create/update/draw lifecycle, so tracking them explicitly prevents this area from looking "done" once the generic popup routing exists while the owner-specific ranking/event behavior is still only approximated. | status-bar launched utility-window owners (`CUIRanking`, `CUIEventAlarm`, `CUIStatusBar::OnButtonClicked`) |

### 2. Local utility and event packet parity

This area is still underrepresented by the existing window rows because the client keeps a packet-owned dispatch seam in front of several of those owners.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned local utility/event dispatch parity | `MapSimulator.PacketOwnedUtilityParity.cs` now mirrors more of the concrete `CUserLocal::OnPacket` seam instead of stopping at synthetic aliases. The simulator can dispatch raw/pseudo packet payloads for `OnOpenUI`, `OnOpenUIWithOption`, `OnGoToCommoditySN`, `OnOpenClassCompetitionPage`, `OnQuestGuideResult`, `OnDeliveryQuest`, `OnNoticeMsg`, `OnChatMsg`, `OnBuffzoneEffect`, `OnPlayEventSound`, `OnPlayMinigameSound`, `OnAskAPSPEvent`, `OnFollowCharacterFailed`, and `OnSkillCooltimeSet`, while still delegating `OnFieldFadeInOut`, `OnBalloonMsg`, `OnDamageMeter`, and `OnNotifyHPDecByField` payloads back into the existing packet-owned overlay seam. Client-backed `CWvsContext::UI_Open` ids that already have simulator owners still route through this bridge for inventory/equip/stat/skill/key-config/quest/character-info/item-maker/ranking/family/guild/party-search windows, and the exact `OnOpenUIWithOption` `uiId == 7` toggle path is no longer a gap: the bridge now follows the client’s `UI_Close(7)` plus `UI_Toggle(7, option)` behavior by routing into the simulator `SocialList` owner and using the option as the requested `Friend` / `Party` / `Guild` / `Alliance` / `Blacklist` tab. `OnNoticeMsg` and `OnChatMsg` also decode their client packet shape more closely now, with `OnNoticeMsg` accepting the raw Maple string payload and `OnChatMsg` accepting the raw `UInt16` chat-log type plus Maple string payload before feeding the existing status-bar chat seam, while the loopback `LocalUtilityPacketInboxManager` and `/localutility` plus `/localutilitypacket` commands now accept the real packet ids recovered from `CUserLocal::OnPacket` for these bridged handlers (`246`, `247`, `251`, `252`, `263`, `264`, `265`, `266`, `270`, `273`, `276` alongside the already-modeled ids). The remaining gap is narrower: tutor/radio handlers still do not have real simulator owners, the skill-cooltime path does not yet mirror the client’s special sentinel branch or every downstream UI side effect exactly, and several notice/chat or event-sound presentations still use simulator approximations rather than the client’s full payload-dependent rules. | This row now has a more faithful `CUserLocal::OnPacket`-style utility bridge instead of mostly synthetic packet aliases. The simulator can consume real local-user packet ids for the bridged handlers, route `uiId == 7` into the same social/user-list tab seam the client uses, decode the raw notice/chat packet shapes that actually hit `CUtilDlg::Notice` and `CUIStatusBar::ChatLogAdd`, and apply packet-authored skill cooldown state through the existing cooldown runtime. The remaining risk is concentrated in still-missing tutor/radio owners plus the last payload/presentation details, not in a missing dispatcher, unknown `uiId == 7` target, or synthetic-only packet ingress. | local-user packet/runtime bridge (`CUserLocal::OnPacket`, `CUserLocal::OnOpenUI`, `CUserLocal::OnOpenUIWithOption`, `CUserLocal::OnGoToCommoditySN`, `CUserLocal::OnFieldFadeInOut`, `CUserLocal::OnBalloonMsg`, `CUserLocal::OnDamageMeter`) |

### 3. Packet-authored local overlays and guidance parity

This slice is narrower than the full dispatcher row above and is still missing as its own planning surface even though the handlers have concrete client-owned behavior.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field fade and world/actor balloon parity | MapSimulator now carries this row through a packet-owned overlay dispatcher seam instead of direct-command-only state: `MapSimulator.PacketOwnedLocalOverlayParity.cs` and `LocalOverlayPacketInboxManager` can drain loopback `fade` and `balloon` packets through a dedicated local inbox, route them back into the same owner used by `/localoverlay`, and report dispatch status separately from the broader utility inbox. The field-fade path also now matches the recovered client packet semantics more closely: the fourth `OnFieldFadeInOut` `Int32` is treated as the animation alpha input that `CAnimationDisplayer::RegisterFadeInOutAnimation` uses for a black full-screen fade, and `PacketFieldFadeOverlay` now tracks the fade-in span, scheduled fade-out start tick, and expiry separately instead of flattening the sequence into one implicit timer. Balloon rendering likewise moved closer to `CUserLocal::ShowBallonMsg` and `CBalloonMsg::Init`: avatar-attached packets replace the single avatar balloon while field/world packets append into a separate field list, avatar anchors still derive from the active player plus current assembled frame height to mirror `CAvatar::GetHeight`, the shared `UI/Login.img/WorldNotice/Balloon` skin now loads and uses the recovered short, corner, and long-arrow variants (`arrow`, `slArrow`, `se/sw/nw/neArrow`, `sel/swl/nwl/nelArrow`), wrapped text now breaks long tokens instead of overflowing the body, and each line is centered inside a client-shaped packet-width text box with the balloon body sized as `nWidth + 20` to better match the decompiled `CBalloonMsg::Init` canvas layout. The remaining gap is now narrower: the simulator still approximates the client’s exact COM-layer fade ownership and layer-object choreography inside `RegisterFadeInOutAnimation`, and balloon composition still lacks the client’s full `CTextAnalyzer` font-selection, per-token color/emphasis handling, and exact `make_balloon` canvas assembly pipeline. | These are visible event-owned overlays that sit between local gameplay and many later scripted or utility flows, and the simulator now has both a packet-shaped launch bridge and a substantially closer render owner for the two surfaces instead of collapsing them into chat lines or ad hoc notices. The remaining risk is concentrated in the final low-level animation-layer and text-layout details rather than in missing dispatch, missing multiple field balloons, or missing arrow-variant support. | packet-owned overlay layer (`CUserLocal::OnFieldFadeInOut`, `CAnimationDisplayer::RegisterFadeInOutAnimation`, `CUserLocal::OnBalloonMsg`, `CUserLocal::ShowBallonMsg`) |
| Partial | Damage-meter and field-hazard notice parity | MapSimulator now carries this row through the same packet-owned utility dispatcher seam the client uses instead of leaving it on overlay-only commands: the local utility inbox and direct packet injection path now accept the real `CUserLocal::OnPacket` handler ids for this row (`243` for `OnNotifyHPDecByField`, `267` for `OnDamageMeter`) alongside the existing aliases, and both route back into the dedicated WZ-backed HUD/notice owner built from `Basic.img/Notice2`, `Basic.img/Notice3`, and `StatusBar2.img/mainBar/notice`. `OnNotifyHPDecByField` no longer stops at a banner either: the simulator now performs a client-shaped `TryConsumePetHP` approximation on top of the hazard notice by checking the predicted post-damage HP threshold, requiring an active pet, preferring hotkey-bound HP consumables before falling back to USE/CASH inventory scans, surfacing the pet's no-HP-potion speech/status-bar failure path when nothing usable is found, and recording the follow-up detail in the local overlay runtime. When the current simulator HP state already has missing HP, the same follow-up can also consume a real inventory potion through the existing consumable seam; otherwise it preserves the client's request-style intent by reporting which potion the pet would have asked to use. The remaining gap is now narrower and centered on the deeper client-only rules inside `TryConsumePetHP`: the exact per-pet consume toggle and configured potion item, the real `SendStatChangeItemUseRequestByPetQ` request/ack path and force/buff-skill parameters, and the precise status-bar presentation rules on success or failure are still approximated rather than mirrored exactly. | This row now has both halves of the client seam represented in the simulator: the damage-meter timer and field-hazard notice ride a `CUserLocal::OnPacket`-shaped dispatch path using the real recovered handler ids, and the HP-dec branch now owns a concrete pet auto-consume follow-up instead of only painting a banner. The remaining risk is concentrated in the still-missing client configuration/request semantics for `TryConsumePetHP`, not in a missing dispatcher or totally absent follow-up layer. | packet-owned HUD/notice layer (`CUserLocal::OnDamageMeter`, `CDamageMeter::SetTimer`, `CUserLocal::OnNotifyHPDecByField`, `CUserLocal::TryConsumePetHP`) |
| Partial | Quest-guide, delivery-quest, and class-competition launch parity | MapSimulator now carries this row through a raw packet-owned local utility inbox and decoder path instead of command-only stubs: the simulator can drain `CUserLocal::OnPacket`-shaped `274` quest-guide, `275` delivery-quest, and `250` class-competition launches from a dedicated loopback inbox, decode the client packet payloads for quest-guide mode or reset, quest-id or mob-map target rebuilds, delivery quest or item ids plus the disallowed-delivery list, and open the existing owners through the same packet-owned seam the manual launch helpers use. Packet-authored quest-guide demand state still rebuilds world-map mob targets into `WorldMapUI` and clears itself when no usable mob survives, while `QuestDelivery` keeps the `UIWindow2.img/UtilDlgEx` `notice` frame and `QDeliveryIcon` art, owns a client-shaped four-row worthy-quest viewport with scrollbar state and row selection, and now routes its OK path through a forced quest-delivery NPC interaction seam instead of completing directly from the list: selecting a worthy row opens a quest-specific NPC dialogue overlay bound to the packet target NPC and lets the existing quest primary-action runtime accept or complete from that owner context, which is materially closer to `CUIQuestDelivery::OnButtonClicked` than the earlier quest-log/detail shortcut. The packet-driven `ClassCompetition` owner is also a dedicated live page rather than a static placeholder, with packet-launch timing plus active character and local ranking seed context bound into the singleton owner content so the launch stays modeled as a packet-owned surface rather than a menu alias. The remaining gap is that `QuestDelivery` still approximates `CUIQuestDelivery::LoadData` and `OnButtonClicked` with simulator quest definitions plus a synthetic dialogue overlay instead of the client's full worthy-series filtering, created NPC instance, item-slot handoff, and `CUserLocal::TalkToNpc` runtime, and the class-competition page still lacks the real `CClassCompetition` data model and any live server-fed ladder payloads. | These are not generic variants of quest log or world map browsing. They are packet-authored launch surfaces for guided quest routing and delivery/class-competition utility flows, and the simulator now has the real packet-owned bridge, client packet ids, WZ-backed delivery chrome, worthy-quest selection state, a packet-shaped delivery-to-NPC handoff seam, and a dedicated class-competition owner needed to exercise those launches end-to-end. The remaining risk is now narrowed to the deeper client-owned `CUIQuestDelivery` worthy filtering or created-NPC handoff details and the still-missing live `CClassCompetition` payload model rather than to missing raw dispatch, owner absence, or direct-list action shortcuts. | packet-owned guidance/utility launch layer (`CUserLocal::OnQuestGuideResult`, `CUserLocal::OnDeliveryQuest`, `CUIQuestDelivery`, `CUserLocal::OnOpenClassCompetitionPage`) |

### 4. External cash-service stage parity

This area is currently unowned by the backlog set even though the client keeps Cash Shop and ITC as dedicated stage owners rather than utility-window variants.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Cash Shop and ITC stage ownership parity | The backlog currently tracks an in-map `AdminShopDialogUI` approximation plus the `OnGoToCommoditySN` migration hook, but IDA shows a separate service-stage family that is still unrepresented: `CCashShop::Init` clears field UI, resets wishlist and cash balances, selects job-specific preview art, builds dedicated child owners for character, locker, inventory, tabs, catalog list, best-items, status, and item-search panes, and resumes at a pending commodity serial through `GoToCommoditySN`; `CCashShop::OnPacket` then dispatches charge-param (`382`), query-cash (`383`), cash-item (`384`), purchase-exp (`385`), gift-mate (`386`), duplicate-id (`387`), name-change (`388`), transfer-world (`390`), gachapon stamp/result (`391`-`393`), one-a-day (`395`), and free-item notice (`396`) handlers instead of using the local utility bridge or `CAdminShopDlg` button flow. The client also keeps the Item Trading Center in a separate stage owner: `CITC::Init` clears field UI, resets its own category/search/sort state, loads NPT exception items, creates separate character, sale, purchase, inventory, tab, subtab, list, and status panes, and `CITC::OnPacket` owns its own charge-param (`410`), query-cash (`411`), and normal-item (`412`) result routing. None of that service-stage lifecycle, packet ownership, or child-window structure is tracked anywhere in the current backlog set. | Treating cash services as only a status-bar launch plus an in-map admin-shop shell hides a large client-owned stage transition before the player returns to field gameplay. Tracking the service-stage owners explicitly keeps future parity work anchored to the real client lifecycle for Cash Shop and ITC instead of overloading `CAdminShopDlg`, `OnGoToCommoditySN`, or generic utility-window rows with stage-owned behavior they do not actually own. | external cash-service stage layer (`CCashShop::Init`, `CCashShop::OnPacket`, `CCashShop::OnCashItemResult`, `CITC::Init`, `CITC::OnPacket`, `CITC::OnNormalItemResult`) |

### 5. Cash-service child-window and preview parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Cash avatar-preview and service-subwindow parity | The current backlog stops at stage ownership, but the client immediately hands real behavior to dedicated child-window owners once that stage is alive. `CCSWnd_Char::OnCreate` builds a separate avatar-preview surface with its own background layer, `CUserPreview` actor, physical-space data, and client buttons for buy-avatar, default-avatar, and takeoff-avatar actions, then hands preview state changes to adjacent methods such as `OnWear`, `SetPet`, `ShowMessageBox`, `ShowPersonalShop`, `ShowEntrustedShop`, and `BlowWeather`; those are client-owned in-service preview actions, not generic field windows. The rest of the service stage is similarly split across dedicated owners that are not tracked today: `CCSWnd_Locker::OnCreate`, `CCSWnd_Inventory::OnCreate`, `CCSWnd_List::OnCreate`, and `CCSWnd_Status::OnCreate` build the main panes inside Cash Shop, `CCSWnd_OneADay::OnCreate` owns the One-a-Day panel, and `CCashTradingRoomDlg::OnCreate` creates a separate cash trading room with its own chat entry, buttons, scrollbar, fonts, and cash-balance initialization. The backlog currently has no row for that stage-internal owner family, so cash-service parity work would still blur avatar preview, subwindow behavior, and trading-room behavior into the broader stage row. | Cash-service parity is not only about entering the right stage. The client also depends on a second layer of named owners to make the stage usable and to preview avatar-affecting cash items correctly. Tracking those owners separately keeps future work anchored to the real preview, locker, inventory, list, status, One-a-Day, and cash-trading seams instead of treating them as anonymous controls inside `CCashShop::Init` or folding them back into field-side UI rows. | cash-service child-window layer (`CCSWnd_Char::OnCreate`, `CCSWnd_Locker::OnCreate`, `CCSWnd_Inventory::OnCreate`, `CCSWnd_List::OnCreate`, `CCSWnd_Status::OnCreate`, `CCSWnd_OneADay::OnCreate`, `CCashTradingRoomDlg::OnCreate`) |

### 6. Packet-authored radio-schedule parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Radio schedule and `CRadioManager` lifecycle parity | The current backlog only mentions tutor/radio as a residual hole inside the broad local-utility dispatcher row, but targeted IDA lookup shows radio playback is a distinct client-owned manager surface that the simulator does not represent today. `CUserLocal::OnRadioSchedule` at `0x918120` decodes a packet-authored track string plus time value and forwards it into `CRadioManager::Play` when no radio session is already running. `CRadioManager::Play` at `0x6cb940` resolves the authored track through a `StringPool`-formatted WZ/UOL path, starts multimedia playback, stores the active track name, shows a dedicated radio UI, mutes the field BGM through the sound manager, and emits a chat-log start notice; `CRadioManager::Stop` at `0x6cbd20` tears that session down, hides the UI, restores BGM state, clears the active track, and emits the completion notice; and `CRadioManager::Update` at `0x6cbf20` confirms the radio path has its own timer-driven runtime instead of behaving like one-shot `OnPlayEventSound` or `OnPlayMinigameSound`. A focused code search across `HaCreator/MapSimulator` also found no existing radio owner or schedule runtime, so this surface is still effectively untracked on the simulator side. | Packet-owned radio scheduling is more than a sound trigger. It is a client-managed audio/UI/chat lifecycle that temporarily changes BGM ownership and presents user-facing state, so leaving it inside the generic dispatcher row makes the remaining gap look smaller and less coherent than it is. Tracking it explicitly keeps future work anchored to the right packet handler and manager lifecycle instead of folding radio playback into unrelated sound-effect or notice work. | packet-owned radio manager layer (`CUserLocal::OnRadioSchedule`, `CRadioManager::Play`, `CRadioManager::Update`, `CRadioManager::Stop`) |

## Priority Order

If the goal is visible parity first, the next work in this area should be sequenced like this:

1. Window usefulness pass:
   Finish minimap, inventory, equip, and quest-log interactions so the existing windows become usable client-facing tools rather than frame shells.
2. Progression correctness pass:
   Tighten ability/stat formulas, equip presentation, and inventory-backed validations so the windows reflect the runtime accurately.
3. Packet-dispatch pass:
   Add the `CUserLocal::OnPacket`-style bridge for packet-owned local utility/event handlers before layering more one-off launches onto chat commands or button callbacks.
4. Packet-overlay pass:
   Add the packet-owned fade, balloon, damage-meter, field-hazard, quest-guide, and delivery/class-competition surfaces after the base dispatcher exists so those handlers do not stay hidden behind one catch-all row.
5. External-service-stage pass:
   Add dedicated Cash Shop and ITC stage owners before treating cash-service parity as an extension of `CAdminShopDlg`, `OnGoToCommoditySN`, or the local utility bridge.
6. Cash-service subwindow pass:
   Add the avatar-preview, locker, inventory, list, status, One-a-Day, and cash-trading child owners after the base service stages exist so cash-item preview and service-pane behavior do not stay folded into one coarse stage row.
7. Radio-manager pass:
   Add the packet-owned `OnRadioSchedule` bridge plus a real `CRadioManager`-style audio/UI/chat owner before treating radio playback as another event-sound alias inside the local utility dispatcher.
8. Utility feedback pass:
   Complete pickup notices, soft-keyboard or constrained text-entry behavior, macro behavior, cursor states, and status-bar utility-button behavior.
9. Client seam pass:
   Capture direct IDA seams for minimap, inventory, equip, and quest windows so future parity fixes are anchored to named client owners instead of simulator-only structure.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".

