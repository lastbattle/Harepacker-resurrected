# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/UI/MinimapUI.cs`
- `HaCreator/MapSimulator/UI/MouseCursorItem.cs`
- `HaCreator/MapSimulator/UI/PickupNoticeUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AbilityUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AdminShopDialogUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AbilityUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/BookCollectionWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/EquipUI.cs`
- `HaCreator/MapSimulator/UI/Windows/EquipUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/InventoryUI.cs`
- `HaCreator/MapSimulator/UI/Windows/InventoryUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/KeyConfigWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/MemoMailboxWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/MemoSendWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/MemoGetWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/OptionMenuWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/QuestUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuestUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillMacroUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SoftKeyboardUI.cs`
- `HaCreator/MapSimulator/UI/Windows/VegaSpellUI.cs`
- `HaCreator/MapSimulator/UI/Windows/WorldMapUI.cs`
- `HaCreator/MapSimulator/Interaction/MemoMailboxManager.cs`
- `HaCreator/MapSimulator/Fields/FieldInteractionRestrictionEvaluator.cs`

### Client references checked

- `CUIStat::Draw` (captured in simulator layout comments and offsets)
- `CUIStat::DrawTextA` (captured in simulator layout comments and offsets)
- `CUISkillMacro` (captured in simulator window comments)
- `CWvsContext::OnDropPickUpMessage` (captured in `PickupNoticeUI` comments)
- `CUIScreenMsg::ScrMsg_Add` (captured in `PickupNoticeUI` comments)
- `CUIItem::Draw` at `0x7ccf50`
- `CAdminShopDlg::OnCreate` at `0x42ecd0`
- `CAdminShopDlg::OnPacket` at `0x4310f0`
- `CAdminShopDlg::SetAdminShopDlg` at `0x4307c0`
- `CAdminShopDlg::Draw` at `0x42fdf0`
- `CAdminShopDlg::OnButtonClicked` at `0x430f90`
- `CUIAdminShopWishList::HitTest` at `0x76cf50`
- `CUIAdminShopWishList::OnCreate` at `0x772640`
- `CUIAdminShopWishList::OnButtonClicked` at `0x772670`
- `CUIAdminShopWishList::OnKey` at `0x772770`
- `CUIAdminShopWishListSearchResult::OnCreate` at `0x772840`
- `CUIAdminShopWishListSearchResult::OnButtonClicked` at `0x771d30`
- `CUIAdminShopWishListCategory::OnCreate` at `0x76ec90`
- `CUIAdminShopWishListCategory::OnButtonClicked` at `0x772620`
- `CCashShop::Init` at `0x484920`
- `CCashShop::OnPacket` at `0x4997e0`
- `CCashShop::OnCashItemResult` at `0x499370`
- `CCSWnd_Char::OnCreate` at `0x4c4590`
- `CCSWnd_Char::OnDefaultAvatar` at `0x4bec80`
- `CCSWnd_Char::OnWear` at `0x4c5a60`
- `CCSWnd_Char::SetPet` at `0x4bc400`
- `CCSWnd_Char::ShowMessageBox` at `0x4bbb00`
- `CCSWnd_Char::ShowPersonalShop` at `0x4bc660`
- `CCSWnd_Char::ShowEntrustedShop` at `0x4bc6b0`
- `CCSWnd_Char::BlowWeather` at `0x4c5d30`
- `CCSWnd_Locker::OnCreate` at `0x4c31c0`
- `CCSWnd_Inventory::OnCreate` at `0x4c34f0`
- `CCSWnd_List::OnCreate` at `0x4d0500`
- `CCSWnd_Status::OnCreate` at `0x4cb840`
- `CCSWnd_OneADay::OnCreate` at `0x4d2fd0`
- `CCashTradingRoomDlg::OnCreate` at `0x49e8c0`
- `CCouponUseSelectDlg::OnCreate` at `0x48c810`
- `CConfirmPurchaseDlg::OnCreate` at `0x48acc0`
- `CCashShop::OnCheckNameChangePossibleResult` at `0x495470`
- `CCashShop::OnCashItemResLoadGiftDone` at `0x496520`
- `CCashShop::OnCheckTransferWorldPossibleResult` at `0x4980b0`
- `CUIItemMaker::Draw` at `0x7d4980`
- `CUIItemMaker::OnButtonClicked` at `0x7d5dc0`
- `CUIItemUpgrade::Draw` at `0x7c0100`
- `CUIItemUpgrade::OnButtonClicked` at `0x7c0ca0`
- `CUIVega::OnCreate` at `0x7c1320`
- `CUIVega::Draw` at `0x7c1dd0`
- `CUIVega::OnButtonClicked` at `0x7bf4a0`
- `CAnimationDisplayer::Effect_ItemUpgrade` at `0x4567f0`
- `CAnimationDisplayer::Effect_ItemMake` at `0x456f80`
- `CAnimationDisplayer::Effect_ViciousHammer` at `0x457210`
- `CAnimationDisplayer::Effect_Vega` at `0x457600`
- `CAnimationDisplayer::Effect_SkillBookUsed` at `0x4581b0`
- `CUIMapTransfer::Draw` at `0x7df5c0`
- `CUIMapTransfer::OnButtonClicked` at `0x7e0ed0`
- `CUIEquip::Draw` at `0x7aa560`
- `CUIQuestInfo::Draw` at `0x82a780`
- `CUIQuestInfoDetail::Draw` at `0x8248c0`
- `CUIQuestInfoDetail::OnButtonClicked` at `0x834c90`
- `CUIQuestAlarm::Draw` at `0x82d850`
- `CUIRanking::OnCreate` at `0x83ca40`
- `CUIRanking::OnButtonClicked` at `0x83c6a0`
- `CUIRanking::Update` at `0x83c690`
- `CUIStatusBar::OnButtonClicked` at `0x880540`
- `CUIStat::Draw` at `0x864bd0`
- `CUIKeyConfig::OnCreate` at `0x7dc5d0`
- `CUIKeyConfig::Draw` at `0x7dd7c0`
- `CUIGameOpt::OnCreate` at `0x9777d0`
- `CUIMacroSysEx::OnCreate` at `0x85c420`
- `CUISoftKeyboard::GetKeyIndexFromPoint` at `0x85faa0`
- `CUISoftKeyboard::OnMouseMove` at `0x85fba0`
- `CUISoftKeyboard::HitTest` at `0x85fc50`
- `CUISoftKeyboard::UpdateKeyConstraint` at `0x85fd70`
- `CUIMiniMap::OnButtonClicked` at `0x803e40`
- `CUIMiniMap::OnMouseMove` at `0x8040a0`
- `CWvsContext::OnMiniMapOnOff` at `0x9cfff0`
- `CSoftKeyboardDlg::OnCreate` at `0x7162c0`
- `CSoftKeyboardDlg::OnButtonClicked` at `0x716c20`
- `CUIUserInfo::Draw` at `0x8b3a50`
- `CUIUserInfo::OnButtonClicked` at `0x8b6d80`
- `CUIEventAlarm::OnCreate` at `0x7ac4f0`
- `CUIEventAlarm::Draw` at `0x7aad90`
- `CUIEventAlarm::Update` at `0x7aac00`
- `CBookDlg::OnCreate` at `0x471f00`
- `CBookDlg::Draw` at `0x473360`
- `CBookDlg::OnButtonClicked` at `0x474300`
- `CMonsterBookMan::LoadBook` at `0x664c10`
- `CParcelDlg::OnCreate` at `0x6914c0`
- `CParcelDlg::Draw` at `0x694db0`
- `CParcelDlg::OnButtonClicked` at `0x690c30`
- `CTrunkDlg::OnCreate` at `0x768c00`
- `CITC::Init` at `0x573ee0`
- `CITC::OnPacket` at `0x577470`
- `CITC::OnNormalItemResult` at `0x5771d0`
- `CUserLocal::OnPacket` at `0x9340c0`
- `CUserLocal::OnOpenUI` at `0x9055f0`
- `CUserLocal::OnOpenUIWithOption` at `0x932300`
- `CUserLocal::OnGoToCommoditySN` at `0x905760`
- `CUserLocal::OnMesoGive_Succeeded` at `0x90f950`
- `CUserLocal::OnMesoGive_Failed` at `0x90d530`
- `CUserLocal::OnRandomMesobag_Succeeded` at `0x90fa30`
- `CUserLocal::OnRandomMesobag_Failed` at `0x90d560`
- `CUserLocal::OnResignQuestReturn` at `0x905720`
- `CUserLocal::OnPassMateName` at `0x918260`
- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`
- `CUserLocal::OnHireTutor` at `0x90e560`
- `CUserLocal::OnTutorMsg` at `0x916f60`
- `CUserLocal::OnOpenSkillGuide` at `0x90e6a0`
- `CUserLocal::OnAskAPSPEvent` at `0x90f0e0`
- `CUserLocal::OnRadioSchedule` at `0x918120`
- `CUIRandomMorphDlg::ShowDlg` at `0xa1ec00`
- `CUIRandomMorphDlg::CUIRandomMorphDlg` at `0xa1eb20`
- `CUIRandomMorphDlg::OnCreate` at `0xa1ee20`
- `CUIRandomMorphDlg::_SendMorphRequest` at `0xa1ec70`
- `CUIRandomMorphDlg::Draw` at `0xa1e910`
- `CUIAntiMacro::OnCreate` at `0x78b980`
- `CUIAntiMacro::Draw` at `0x78bb30`
- `CUIAntiMacro::Update` at `0x77f1f0`
- `CUIAdminAntiMacro::OnCreate` at `0x78cd90`
- `CUIAdminAntiMacro::Draw` at `0x78cf40`
- `CUIAdminAntiMacro::Update` at `0x77f250`
- `CWvsContext::OnAccountMoreInfo` at `0x9dbe50`
- `CUIAccountMoreInfo::OnCreate` at `0x76c4c0`
- `CUIAccountMoreInfo::OnButtonClicked` at `0x76b5a0`
- `CUIAccountMoreInfo::OnLoadAccountMoreInfoResult` at `0x76c2f0`
- `CUIAccountMoreInfo::OnSaveAccountMoreInfoResult` at `0x76b600`
- `CWvsContext::OnDragonBallBox` at `0x9db630`
- `CUIDragonBox::OnCreate` at `0x7a01a0`
- `CUIDragonBox::Draw` at `0x79f750`
- `CUIDragonBox::OnButtonClicked` at `0x79f9d0`
- `CWvsContext::OnAntiMacroResult` at `0x9ff580`
- `CWvsContext::OnLogoutGift` at `0x9cccb0`
- `CWvsContext::SaveAntiMacroScreenShot` at `0x9fa330`
- `CWvsContext::ShowAntiMacroNotice` at `0x9f3c30`
- `CUILogoutGift::OnCreate` at `0x7dec80`
- `CUILogoutGift::Draw` at `0x7de9b0`
- `CRadioManager::Play` at `0x6cb940`
- `CRadioManager::Stop` at `0x6cbd20`
- `CRadioManager::Update` at `0x6cbf20`

Notes:
Direct IDA seam capture is now confirmed for inventory, cash-service, item-maker, item-upgrade, map-transfer, equipment, quest-log, quest-detail, quest-alarm, the dedicated ranking/event owners (`CUIRanking`, `CUIEventAlarm`), status-bar utility clicks, stat-window, the explicit key-config/game-option/soft-keyboard owner family (`CUIKeyConfig`, `CUIGameOpt`, `CSoftKeyboardDlg`, `CUIMacroSysEx`, `CUISoftKeyboard`), user-info owners, the book/collection owner family (`CBookDlg` plus `CMonsterBookMan::LoadBook`), the parcel/mail owner family (`CParcelDlg`), the storage/trunk dialog owner (`CTrunkDlg::OnCreate`), the separate service-stage owners for Cash Shop and ITC (`CCashShop`, `CITC`), the stage-internal Cash Shop owners (`CCSWnd_Char`, `CCSWnd_Locker`, `CCSWnd_Inventory`, `CCSWnd_List`, `CCSWnd_Status`, `CCSWnd_OneADay`, `CCashTradingRoomDlg`), the separate cash-service wishlist owner (`CUIAdminShopWishList`), its follow-up search-result/category child owners (`CUIAdminShopWishListSearchResult`, `CUIAdminShopWishListCategory`), the anti-macro challenge owners (`CUIAntiMacro`, `CUIAdminAntiMacro`), the packet-owned anti-macro result bridge in `CWvsContext`, and a broader local-user packet dispatcher for utility/event surfaces. `CUserLocal::OnPacket` is important because it shows the client does not only open these windows from direct button clicks: packet-owned handlers also drive generic `UI_Open`, optioned UI launch, commodity-shop migration, field fade overlays, balloon messages, and damage-meter timer resets. The Cash Shop and ITC decompiles also show that those services are not just alternate button skins on top of `CAdminShopDlg`: `CCashShop::Init` and `CITC::Init` each clear field UI, build their own staged child-window layout, select service BGM, and then wait on service-specific packet dispatch through `OnPacket`. Follow-up decompiles now also show that the stage owners do not directly absorb all of the in-service interaction either: `CCSWnd_Char::OnCreate` builds a dedicated avatar-preview surface with default-avatar, buy-avatar, and takeoff-avatar buttons plus a `CUserPreview`/physics-space pair, `CUIAdminShopWishList::OnCreate` builds a distinct wishlist owner with its own layer, controls, and focused search edit instead of leaving wish-list behavior inside the base shop dialog, the follow-up wishlist scan confirms that search results and category selection also live in dedicated child owners instead of staying anonymous popups, `CCashTradingRoomDlg::OnCreate` builds its own chat edit, buttons, scrollbar, fonts, and cash-balance state inside the service stage, `CUIAntiMacro::{OnCreate,Draw,Update}` confirm a separate timed text-entry owner rather than treating lie-detector checks as item-use restriction text only, `CTrunkDlg::OnCreate` likewise confirms the storage surface as a dedicated dialog owner with its own tabs, buttons, dual-scrollbar layout, and font/title setup before packet-fed state arrives, and `CWvsContext::OnAntiMacroResult` shows that the owner is itself packet-driven rather than purely item- or dialog-launched. The remaining seam-collection gap in this backlog area is now narrower and centered on deeper packet/data ownership inside the newly confirmed utility-owner families plus the still-missing quest reward/revive and adjacent utility-owner follow-through, rather than on the already-captured minimap or explicit skill-macro owner seams.

## Cross-Document Scope Notes

- `CStage::OnSetCashShop` and `CStage::OnSetITC` are login-to-service handoff references that may appear in backlog 11, but the actual Cash Shop / ITC stage ownership belongs here under the external cash-service family.
- `CParcelDlg` and `CTrunkDlg` stay owned by this document, including packet-driven state, draw ownership, and dialog lifecycle. Backlog 10 should only keep the remaining social-system packet owners that are not parcel/trunk utility windows.

## Client Function Index By Backlog Area

The list below maps this backlog area to the concrete client seams that are already named in code or prior reverse-engineering notes.
These are the first functions or classes to inspect before changing simulator behavior.
Packet/controller rows and window/dialog-owner rows are intentionally kept separate when the client splits those responsibilities across different seams, and the index keeps follow-up scan additions close to their first discovery notes.

### 1. Progression and utility window parity

- `CUIStat::Draw`
- `CUIStat::DrawTextA`
- `CUIItem::Draw` at `0x7ccf50`
- `CAdminShopDlg::OnCreate` at `0x42ecd0`
- `CAdminShopDlg::OnPacket` at `0x4310f0`
- `CAdminShopDlg::SetAdminShopDlg` at `0x4307c0`
- `CAdminShopDlg::Draw` at `0x42fdf0`
- `CAdminShopDlg::OnButtonClicked` at `0x430f90`
- `CCashShop::Init` at `0x484920`
- `CCashShop::OnPacket` at `0x4997e0`
- `CCashShop::OnCashItemResult` at `0x499370`
- `CUIItemMaker::Draw` at `0x7d4980`
- `CUIItemMaker::OnButtonClicked` at `0x7d5dc0`
- `CUIItemUpgrade::Draw` at `0x7c0100`
- `CUIItemUpgrade::OnButtonClicked` at `0x7c0ca0`
- `CUIVega::OnCreate` at `0x7c1320`
- `CUIVega::Draw` at `0x7c1dd0`
- `CUIVega::OnButtonClicked` at `0x7bf4a0`
- `CUIMapTransfer::Draw` at `0x7df5c0`
- `CUIMapTransfer::OnButtonClicked` at `0x7e0ed0`
- `CUIEquip::Draw` at `0x7aa560`
- `CUIQuestInfo::Draw` at `0x82a780`
- `CUIQuestInfoDetail::Draw` at `0x8248c0`
- `CUIQuestInfoDetail::OnButtonClicked` at `0x834c90`
- `CUIQuestAlarm::Draw` at `0x82d850`
- `CUIRanking::OnCreate` at `0x83ca40`
- `CUIRanking::OnButtonClicked` at `0x83c6a0`
- `CUIRanking::Update` at `0x83c690`
- `CUIStatusBar::OnButtonClicked` at `0x880540`
- `CUIMacroSysEx::OnCreate` at `0x85c420`
- `CUISoftKeyboard::GetKeyIndexFromPoint` at `0x85faa0`
- `CUISoftKeyboard::OnMouseMove` at `0x85fba0`
- `CUISoftKeyboard::HitTest` at `0x85fc50`
- `CUISoftKeyboard::UpdateKeyConstraint` at `0x85fd70`
- `CUIUserInfo::Draw` at `0x8b3a50`
- `CUIUserInfo::OnButtonClicked` at `0x8b6d80`
- `CUIEventAlarm::OnCreate` at `0x7ac4f0`
- `CUIEventAlarm::Draw` at `0x7aad90`
- `CUIEventAlarm::Update` at `0x7aac00`
- `CBookDlg::OnCreate` at `0x471f00`
- `CBookDlg::Draw` at `0x473360`
- `CBookDlg::OnButtonClicked` at `0x474300`
- `CMonsterBookMan::LoadBook` at `0x664c10`
- `CParcelDlg::OnCreate` at `0x6914c0`
- `CParcelDlg::Draw` at `0x694db0`
- `CParcelDlg::OnButtonClicked` at `0x690c30`
- `CTrunkDlg::OnCreate` at `0x768c00`
- `CITC::Init` at `0x573ee0`
- `CITC::OnPacket` at `0x577470`
- `CITC::OnNormalItemResult` at `0x5771d0`
- `CRepairDurabilityDlg` (constructed from `CUserLocal::OnOpenUIWithOption` at `0x932300` when `uiId == 33`)
- `CUISkillMacro`
- `CWvsContext::OnDropPickUpMessage`
- `CUIScreenMsg::ScrMsg_Add`

Notes:
`CUIItem::Draw` confirms that the client inventory window owns meso text, per-slot icon and quantity drawing, item-grade frames, disabled-slot overlays, and special overlays such as the active bullet marker. `CAdminShopDlg::{OnCreate,OnPacket,SetAdminShopDlg,Draw,OnButtonClicked}` now make the cash-service dialog boundary much clearer than the older notes did: the client keeps a dedicated in-map admin-shop owner, and `CField::OnPacket` routes packet types `366` and `367` into that dialog's open/result/reopen lifecycle instead of leaving it as only a status-bar launch plus local button handling. That packet-owned dialog seam is intentionally distinct from the separate `CCashShop` / `CITC` stage owners tracked below. `CUIItemMaker::Draw` and `CUIItemMaker::OnButtonClicked` confirm a dedicated crafting or maker window rather than burying production flow inside generic inventory handling. `CUIItemUpgrade::Draw` and `CUIItemUpgrade::OnButtonClicked` confirm a separate item-enhancement surface rather than treating scroll or upgrade flow as a generic inventory tooltip action. Follow-up IDA lookup now shows Vega enhancement is also a distinct owner rather than a cosmetic branch inside ordinary upgrade flow: `CUIVega::{OnCreate,Draw,OnButtonClicked}` build their own dialog, validate equip/scroll state, gate the cast behind the same exclusive-request checks as the client, and trigger the dedicated Vega effect path after sending the upgrade packet. `CUIMapTransfer::Draw` and `CUIMapTransfer::OnButtonClicked` confirm a dedicated map-transfer window distinct from the minimap shell. `CUIEquip::Draw` confirms that equip-window parity is tied to slot-disable rules, expiration and durability overlays, and job or subjob gating rather than just slot placement. `CUIQuestInfo::Draw`, `CUIQuestInfoDetail::Draw`, and `CUIQuestAlarm::Draw` confirm that quest progression parity includes a separate detail pane with dedicated button handling, category-button rebuilds, scroll-driven list rebuilding, quest-delivery requirement rendering, condition-state coloring, and a standalone quest-alert window rather than only the quest log shell. Follow-up direct lookups now also confirm that the `BtRank` and `BtEvent` status-bar launches land on dedicated client owners rather than unnamed utility shells: `CUIRanking::{OnCreate,OnButtonClicked,Update}` and `CUIEventAlarm::{OnCreate,Draw,Update}`. `CBookDlg::{OnCreate,Draw,OnButtonClicked}` together with `CMonsterBookMan::LoadBook` confirm that the client keeps a real book-style collection owner tied to Monster Book data rather than treating book presentation as a generic stats subpage. `CParcelDlg::{OnCreate,Draw,OnButtonClicked}` confirm a dedicated parcel/mail owner with receive, send, and quick-send surfaces rather than burying package delivery under memo/social text alone. `CUserLocal::OnOpenUIWithOption` now also gives direct client evidence that repair-durability is its own utility owner rather than a generic option toggle: the decompile at `0x932300` shows `uiId == 33` allocating `CRepairDurabilityDlg(v5)` when no `CUniqueModeless` owner is already active. `CUIStatusBar::OnButtonClicked` confirms the utility-button cluster is a real interaction owner instead of only decorative art. `CUIMacroSysEx::OnCreate` together with `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::OnMouseMove`, `CUISoftKeyboard::HitTest`, and `CUISoftKeyboard::UpdateKeyConstraint` confirms that client text-entry parity is not just committed character handling: the macro family also owns an explicit soft-keyboard helper with hover, hit-test, and per-mode key-constraint behavior. `CUIUserInfo::Draw` and `CUIUserInfo::OnButtonClicked` confirm a separate character-information or profile surface beyond the stat window itself.

### 2. Local utility and event packet parity

- `CUserLocal::OnPacket` at `0x9340c0`
- `CUserLocal::OnOpenUI` at `0x9055f0`
- `CUserLocal::OnOpenUIWithOption` at `0x932300`
- `CUserLocal::OnGoToCommoditySN` at `0x905760`
- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`

Notes:
`CUserLocal::OnPacket` is a distinct client seam rather than a random collection of callbacks. It dispatches packet-owned local utility behavior including `OnOpenUI`, `OnOpenUIWithOption`, `OnGoToCommoditySN`, field fade in/out, balloon messages, damage-meter timing, chat/notice surfaces, quest-guide and delivery-quest results, buff-zone effects, field HP-dec notices, tutor flows, and several event-sound/minigame-sound owners. The narrower decompiles already show concrete responsibilities that are not represented well by the existing window rows: `OnOpenUI` forwards a raw UI id into `CWvsContext::UI_Open`, `OnOpenUIWithOption` adds option-driven branches such as party-search requests and the repair-durability owner, `OnGoToCommoditySN` stores a commodity serial number before calling `SendMigrateToShopRequest`, `OnFieldFadeInOut` registers a fade animation relative to the avatar layer Z, `OnBalloonMsg` can bind a balloon to the avatar origin or an explicit world position, and `OnDamageMeter` resets a dedicated timer surface. That means the client has a packet-driven utility/event layer that sits between local gameplay and the specific window owners already tracked above.

### 3. Packet-authored local overlays and guidance parity

- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`
- `CUserLocal::OnNotifyHPDecByField` at `0x90fed0`
- `CUserLocal::OnQuestGuideResult` at `0x90f1e0`
- `CUserLocal::OnDeliveryQuest` at `0x90ef60`
- `CUserLocal::OnOpenClassCompetitionPage` at `0x905580`

Notes:
These handlers form a narrower client-owned feedback layer under `CUserLocal::OnPacket`, and the current backlog only references them inside the broad dispatcher row. IDA now confirms concrete responsibilities that deserve separate tracking: `OnFieldFadeInOut` decodes fade-in, fade-out, and duration fields then registers the animation relative to the avatar layer Z; `OnBalloonMsg` decodes text, width, lifetime, and either avatar-relative or explicit world-position placement before calling `ShowBallonMsg`; `OnDamageMeter` resets the dedicated damage-meter timer owner and updates shared timing state in `CWvsContext`; `OnQuestGuideResult` rebuilds quest-demand targets and opens the world-map quest-guide owner when the packet contains viable quest-mob targets; `OnDeliveryQuest` either opens the dedicated delivery-quest owner or routes the result back through the status-bar chat seam when another unique modeless owner is active; and `OnOpenClassCompetitionPage` is a packet-driven utility launch rather than a normal button path.

### 4. External cash-service stage parity

- `CCashShop::Init` at `0x484920`
- `CCashShop::OnPacket` at `0x4997e0`
- `CCashShop::OnCashItemResult` at `0x499370`
- `CITC::Init` at `0x573ee0`
- `CITC::OnPacket` at `0x577470`
- `CITC::OnNormalItemResult` at `0x5771d0`

Notes:
This service-stage surface is distinct from the in-map `CAdminShopDlg` approximation already tracked above. IDA shows `CCashShop::Init` and `CITC::Init` clearing field UI, constructing their own staged child-window layouts, selecting service BGM, and entering standalone stage owners rather than opening one more utility dialog over the live HUD. The packet routers are distinct too: `CCashShop::OnPacket` dispatches cash-service result families such as charge-param, query-cash, cash-item, purchase-exp, gift-mate, duplicate-id, name-change, transfer-world, gachapon, one-a-day, and free-item notices, while `CITC::OnPacket` owns its own smaller result family around charge-param, query-cash, and normal-item results. That means Cash Shop and ITC parity should be tracked as their own service-stage owner family rather than being treated as a subset of status-bar launches or the current admin-shop fallback.

### 5. Cash-service child-window and preview parity discovered by targeted IDA scan

- `CCSWnd_Char::OnCreate` at `0x4c4590`
- `CCSWnd_Char::OnDefaultAvatar` at `0x4bec80`
- `CCSWnd_Char::OnWear` at `0x4c5a60`
- `CCSWnd_Char::SetPet` at `0x4bc400`
- `CCSWnd_Char::ShowMessageBox` at `0x4bbb00`
- `CCSWnd_Char::ShowPersonalShop` at `0x4bc660`
- `CCSWnd_Char::ShowEntrustedShop` at `0x4bc6b0`
- `CCSWnd_Char::BlowWeather` at `0x4c5d30`
- `CCSWnd_Locker::OnCreate` at `0x4c31c0`
- `CCSWnd_Inventory::OnCreate` at `0x4c34f0`
- `CCSWnd_List::OnCreate` at `0x4d0500`
- `CCSWnd_Status::OnCreate` at `0x4cb840`
- `CCSWnd_OneADay::OnCreate` at `0x4d2fd0`
- `CCashTradingRoomDlg::OnCreate` at `0x49e8c0`

Notes:
The stage-owner row above is still too coarse on its own. IDA shows that Cash Shop and related cash-service flows split into dedicated child-window owners after `CCashShop::Init` creates the stage: `CCSWnd_Char::OnCreate` allocates its own avatar preview layer, user-preview actor, physical-space helpers, and `BtBuyAvatar`/`BtDefaultAvatar`/`BtTakeoffAvatar` controls before calling `OnDefaultAvatar`; the adjacent `CCSWnd_Char` methods also expose stage-local preview actions for wear/takeoff, pet preview, weather, message box, personal shop, and entrusted shop effects rather than routing those previews through generic field windows. Separate owners then build the rest of the service UI: `CCSWnd_Locker`, `CCSWnd_Inventory`, `CCSWnd_List`, and `CCSWnd_Status` each have their own `OnCreate` routines, `CCSWnd_OneADay::OnCreate` owns the One-a-Day panel inside the service, and `CCashTradingRoomDlg::OnCreate` builds a standalone cash trading room with its own chat edit, trade/coin buttons, scrollbar, fonts, and initial cash balance. None of these client-owned child-window seams are named anywhere else in the backlog today even though they sit directly on the parity path for cash-avatar preview and service-stage usability.

### 6. Cash-service wishlist parity discovered by targeted IDA scan

- `CUIAdminShopWishList::HitTest` at `0x76cf50`
- `CUIAdminShopWishList::OnCreate` at `0x772640`
- `CUIAdminShopWishList::OnButtonClicked` at `0x772670`
- `CUIAdminShopWishList::OnKey` at `0x772770`
- `CUIAdminShopWishListSearchResult::OnCreate` at `0x772840`
- `CUIAdminShopWishListSearchResult::OnButtonClicked` at `0x771d30`
- `CUIAdminShopWishListCategory::OnCreate` at `0x76ec90`
- `CUIAdminShopWishListCategory::OnButtonClicked` at `0x772620`

Notes:
The cash-service stage and child-window rows still need this owner called out explicitly: `CUIAdminShopWishList` is its own modeless wishlist/search controller, not just a button branch inside `CAdminShopDlg` or `CCSWnd_List`.

The follow-up lookup also makes the internal split of that owner more concrete than the earlier backlog text did. `CUIAdminShopWishList::OnKey` confirms owner-local keyboard routing on the focused search edit, while `CUIAdminShopWishListSearchResult::{OnCreate,OnButtonClicked}` and `CUIAdminShopWishListCategory::{OnCreate,OnButtonClicked}` show that search results and category selection are real child owners rather than anonymous in-window popups. That means the current wishlist row should continue to be treated as its own owner family rather than a note under the broader cash-shop or admin-shop rows.

### 7. Packet-authored radio-schedule parity discovered by targeted IDA scan

- `CUserLocal::OnRadioSchedule` at `0x918120`
- `CRadioManager::Play` at `0x6cb940`
- `CRadioManager::Stop` at `0x6cbd20`
- `CRadioManager::Update` at `0x6cbf20`

Notes:
The broader local-utility row already mentioned tutor/radio handlers as a residual gap, but targeted IDA lookup shows radio scheduling is its own client-owned lifecycle rather than one more generic sound callback. `CUserLocal::OnRadioSchedule` decodes a track name plus time value and forwards that payload into a singleton `CRadioManager` owner when no radio session is already active. `CRadioManager::Play` then resolves a WZ/UOL-backed track through `StringPool`, starts multimedia playback, stores the active track name, mutes field BGM through the sound manager, shows a dedicated radio UI, and pushes a chat-log notice for the start event; `CRadioManager::Stop` reverses that ownership by stopping playback, hiding the UI, restoring BGM state, and pushing the completion notice; and `CRadioManager::Update` confirms a timer-driven runtime instead of a fire-and-forget sound effect.

- Area: `Radio schedule and CRadioManager lifecycle parity`
- Current status: `Partial`
- Gap summary: `MapSimulator.PacketOwnedUtilityParity.cs` still keeps the client-shaped `CRadioManager` owner behavior inside the existing radio seam, still mirrors the recovered `StringPool` lookup order by resolving `StringPool[0x1501] = "Sound/Radio.img/%s"` before `StringPool[0x1502] = "Sound/Radio.img/%s/track"` so track-metadata lookup stays distinct from playable sound lookup, still blocks re-entry while a session is live, still clears the live track/audio descriptors together with the playback state on stop, and still seeds its expected stop from remaining runtime (`ms_length - ms_position`) before polling completion on the client-shaped `~2000 ms` cadence. This pass tightens the owner-authored `CWvsContext[3562]` seam further without widening it: `PacketOwnedLocalUtilityContextState` now records every explicit radio-slot mutation instead of only value changes, keeps a bounded recent-mutation history beside the existing bound-character/runtime-character diagnostics, and surfaces that history through the existing radio owner details so repeated packet-authored `bLeft` writes and later clears no longer collapse into one observational state. `MapleStoryStringPool` still carries the recovered radio-owner literals (`0x14CF`, `0x14D0`, `0x1501`, `0x1502`), and `UnitTest_MapSimulator/PacketOwnedRadioParityTests.cs` now locks the playback-window math, expected-stop timing, offset rejection, `CreateLayer` left-resolution order, frozen-session refresh guard, runtime-character reset rule, repeated `CWvsContext[3562]` mutation sequencing, and the recovered radio string-pool entries so the radio timing and owner-state seam do not drift again. WZ was rechecked again in the active extracted data source: `UI/UIWindow.img/Radio/Off/0` is still `54x32`, `UI/UIWindow.img/Radio/On/0-4` are still `54x32`, and each animated `On` frame still carries `delay = 150`. The remaining gap is narrower and stays concentrated in true client-owned playback and packet authorship: playback still terminates through the MonoGame/WZ backend instead of the client's `CRadioManager::MMS_Play` `IWzSound -> raw buffer -> AIL_quick_load_mem / AIL_quick_set_ms_position / AIL_quick_play` handle path, and `CWvsContext[3562]` is still simulator-owned context state rather than an official packet-fed client slot persisted end-to-end outside the simulator runtime.`

### 8. Explicit key-config, game-option, and soft-keyboard owner parity discovered by targeted IDA scan

- `CUIKeyConfig::OnCreate` at `0x7dc5d0`
- `CUIKeyConfig::Draw` at `0x7dd7c0`
- `CUIGameOpt::OnCreate` at `0x9777d0`
- `CUIMacroSysEx::OnCreate` at `0x85c420`
- `CUISoftKeyboard::GetKeyIndexFromPoint` at `0x85faa0`
- `CUISoftKeyboard::OnMouseMove` at `0x85fba0`
- `CUISoftKeyboard::HitTest` at `0x85fc50`
- `CUISoftKeyboard::UpdateKeyConstraint` at `0x85fd70`
- `CSoftKeyboardDlg::OnCreate` at `0x7162c0`
- `CSoftKeyboardDlg::OnButtonClicked` at `0x716c20`

Notes:
The broader cursor/utility-button row already mentions that the simulator can launch KeyConfig and OptionMenu owners, but targeted IDA lookups show the client keeps a more explicit owner family here than that row names. `CUIKeyConfig::{OnCreate,Draw}` confirm a dedicated key-setting owner with its own layout and draw lifecycle instead of a generic submenu under the status-bar system popup. `CUIGameOpt::OnCreate` confirms that game/system options are owned by a dedicated option dialog rather than only button callbacks hanging off `CUIStatusBar::OnButtonClicked`. `CUIMacroSysEx::OnCreate`, `CUISoftKeyboard::*`, and `CSoftKeyboardDlg::{OnCreate,OnButtonClicked}` show that the soft-keyboard path also has its own dialog owner and button lifecycle, not just helper hit-testing functions. Keeping that owner family explicit matters because the simulator already has `KeyConfigWindow`, `OptionMenuWindow`, and `SoftKeyboardUI`, so the remaining parity work is now about owner-specific lifecycle, constraints, and button semantics rather than whether these surfaces exist at all.

### 9. Tutor, skill-guide, and AP/SP helper parity discovered by `CUserLocal::OnPacket` scan

- `CUserLocal::OnHireTutor` at `0x90e560`
- `CUserLocal::OnTutorMsg` at `0x916f60`
- `CUserLocal::OnOpenSkillGuide` at `0x90e6a0`
- `CUserLocal::OnAskAPSPEvent` at `0x90f0e0`

Notes:
The broader local-utility row currently mentions tutor flows only as residual dispatch coverage, but the targeted `CUserLocal::OnPacket` decompiles show a distinct client-owned helper surface. `OnHireTutor` tears down the prior tutor owner, allocates a `CTutor`, initializes it through the same summoned-style world seam the client uses, and tracks active tutor variants in `CUserLocal::ms_anTutors`; `OnTutorMsg` then forwards either numeric or string payloads plus two packet-owned ints into `CTutor::OnMessage` instead of using ordinary chat. The same scan also shows that guided helper flows are not only local button opens: `OnOpenSkillGuide` packet-opens the skill UI and immediately calls `CUISkill::OpenCurSkillGuide`, while `OnAskAPSPEvent` opens a Yes/No prompt and can emit a follow-up outpacket (`195`) when the player accepts. None of those owner-specific tutor/helper semantics are tracked explicitly in the backlog today.

### 10. Minimap owner parity discovered by targeted IDA scan

- `CUIMiniMap::OnButtonClicked` at `0x803e40`
- `CUIMiniMap::OnMouseMove` at `0x8040a0`

Notes:
The minimap row already tracks a large amount of behavior, but the owner itself was still unnamed in the seam index. Targeted IDA decompiles now confirm a dedicated `CUIMiniMap` owner rather than a loose collection of helper draws. `CUIMiniMap::OnButtonClicked` cycles the minimap option state, remembers the previous option, and opens `CWorldMapDlg` directly from minimap button ids `1000` to `1003`, including the fallback notice path when the world-map dialog cannot be created. `CUIMiniMap::OnMouseMove` is equally important because it shows the client-owned tooltip and helper-marker surface that sits behind the existing backlog row: the owner computes tooltip hit rectangles for off-map remote direction markers, live NPC markers, employee markers from `CEmployeePool`, and portal markers from `CPortalList`, then resolves tooltip strings from `CNpcTemplate`, `CEmployee::GetNameTag`, portal script-property text, and map-string fallbacks before routing them through `CUIToolTip`. A targeted `CWvsContext::OnPacket` re-check also clarifies the cross-document split with backlog 7: persistent minimap visibility belongs here on the explicit owner row because `CWvsContext::OnMiniMapOnOff` writes `m_bMiniMapOnOff` and immediately calls `CUIMiniMap::ReloadMiniMap`, while backlog 7 keeps only the field-owned `CField::OnAdminResult` subtype-`41` hide pulse. That is concrete client evidence for minimap-specific owner logic rather than only simulator-side UI structure.

### 11. Explicit skill-macro owner parity discovered by follow-up IDA lookup

- `CUIMacroSysEx::OnCreate` at `0x85c420`

Notes:
The broader progression/utility owner list already confirmed the macro dialog family, but the explicit follow-up row should stay visible in the seam index too. `CUIMacroSysEx::OnCreate` confirms that skill macros are owned by a dedicated dialog with its own control layout and embedded text-entry surface rather than only by generic text input or utility-button launches.

### 12. Packet-owned keymap and pet auto-consume parity discovered by targeted IDA scan

- `CFuncKeyMappedMan::OnInit` at `0x568c30`
- `CFuncKeyMappedMan::OnPetConsumeItemInit` at `0x5688c0`
- `CFuncKeyMappedMan::OnPetConsumeMPItemInit` at `0x5688f0`

Notes:
This packet-owned bootstrap seam is distinct from both the `CQuickslotKeyMappedMan` owner tracked in backlog 7 and the explicit key-config window owners tracked above. The targeted IDA pass already pinned `CFuncKeyMappedMan::OnInit` plus the pet-consume init pair as their own owner family, so keymap hydration and pet auto-consume defaults should stay indexed separately from the later UI-owner rows.

### 13. Packet-owned anti-macro result parity discovered by targeted IDA scan

- `CWvsContext::OnAntiMacroResult` at `0x9ff580`
- `CWvsContext::SaveAntiMacroScreenShot` at `0x9fa330`
- `CWvsContext::ShowAntiMacroNotice` at `0x9f3c30`

Notes:
The anti-macro dialog owner is only one half of the client surface. These `CWvsContext` handlers confirm a separate packet/result controller that owns launch, screenshot, notice, and result-routing behavior before the later `CUIAntiMacro` or `CUIAdminAntiMacro` owner finishes the challenge flow.

### 14. Anti-macro owner parity discovered by targeted IDA scan

- `CUIAntiMacro::OnCreate` at `0x78b980`
- `CUIAntiMacro::Draw` at `0x78bb30`
- `CUIAntiMacro::Update` at `0x77f1f0`

Notes:
This owner row remains intentionally separate from row 13. `CUIAntiMacro::{OnCreate,Draw,Update}` confirm a timed text-entry dialog owner with its own layout, countdown, and edit-control lifecycle instead of only a packet/result bridge or generic notice path.

| Area | Status | Gap summary |
| --- | --- | --- |
| Anti-macro challenge dialog parity | Partial | `HaCreator/MapSimulator` now keeps the packet-owned anti-macro owner on the client-observed `CUIAntiMacro::{OnCreate,Draw,Update}` seam instead of only a packet bridge. `MapSimulator.PacketOwnedAntiMacroParity.cs` still registers dedicated normal/admin owners, both surfaces still draw from `UI/UIWindow2.img/Macro/step1*`, stay centered from the active WZ frame, keep the recovered countdown digit coordinates (`95,39`, `115,39`, `137,39`, `147,39`, `167,39`) and attempt-text origin (`222,206`), and still route the attempt line through `MapleStoryStringPool` string id `0x1A15` before falling back. `UI/Windows/AntiMacroChallengeWindow.cs` now copies the challenge canvas at the recovered `CUIAntiMacro::Draw` owner origin instead of fit-scaling it into the slot, draws the attempt line through the client-text path in the recovered small-red color instead of the older shadowed simulator text, and gates submit availability on remaining milliseconds so the answer button stays live until the same final-second boundary the client uses. The anti-macro answer runtime still prefers a true hosted Win32 child edit box when the simulator window handle is available, and both the hosted control and the managed fallback now resolve `StringPool 0x1A25` through the client-text private-font pipeline before falling back to installed faces so the edit font stays closer to the client family selection. The remaining gap is that this is still a hosted desktop `EDIT`/`TextBox` child rather than MapleStory's exact in-client `CCtrlEdit` implementation and message loop, so the runtime is much closer to native ownership than before but not byte-for-byte identical to the client widget stack. |

### 15. Additional quest-timer UI, quest-reward, and revive owners discovered by follow-up IDA scan

- `CUIQuestTimer::OnCreate`
- `CUIRaiseWndBase::OnButtonClicked` at `0x8391d0`
- `CUIRevive::OnCreate` at `0x83cea0`
- `CUIRevive::OnButtonClicked` at `0x83ce40`
- `CUIRevive::Update` at `0x83ce70`
- `CUIRevive::Revive` at `0x83cde0`

Notes:
The latest IDA pass exposed another utility-owner family that should stay visible in the seam index: dedicated quest-timer UI, quest reward/raise windows, and revive confirmation are all separate modeless owners rather than generic extensions of the quest log, field-state packet handling, or raw respawn state.

### 16. Packet-owned NPC shop, store-bank, and battle-record owners discovered by follow-up `CField::OnPacket` decompile

- `CShopDlg::OnPacket` at `0x6eb7d0`
- `CStoreBankDlg::OnPacket` at `0x745c60`
- `CBattleRecordMan::OnPacket` at `0x470ba0`

Notes:
The latest `CField::OnPacket` decompile exposed another utility-owner family that is still unnamed in this backlog. Packet types `364`/`365` dispatch directly into `CShopDlg::OnPacket`, `369`/`370` dispatch into `CStoreBankDlg::OnPacket`, and `420` through `423` dispatch into `CBattleRecordMan::OnPacket` instead of flowing through the broader `CUserLocal::OnPacket` utility row or the existing social/storage packet rows. Those owners do not appear anywhere else in the current backlog set, so NPC shop parity, store-bank parity, and battle-record parity are currently easy to overstate once adjacent window shells or NPC interactions exist locally.

### 18. Additional packet-owned local reward and helper-result parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnMesoGive_Succeeded` at `0x90f950`
- `CUserLocal::OnMesoGive_Failed` at `0x90d530`
- `CUserLocal::OnRandomMesobag_Succeeded` at `0x90fa30`
- `CUserLocal::OnRandomMesobag_Failed` at `0x90d560`
- `CUserLocal::OnResignQuestReturn` at `0x905720`
- `CUserLocal::OnPassMateName` at `0x918260`

Notes:
The direct `CUserLocal::OnPacket` re-check exposed one more local-owner family that is still missing from this backlog. These handlers do not fit the existing UI-launch, overlay, tutor, or radio rows: `OnMesoGive_*` and `OnRandomMesobag_*` are separate money/reward result owners rather than generic pickup notices, and `OnResignQuestReturn` plus `OnPassMateName` are packet-authored helper responses that sit beside quest/social utility flow instead of ordinary field feedback. The dedicated maker-result seam stays merged into the existing item-maker row rather than being duplicated here.

### 19. Additional packet-owned logout-gift owner parity discovered by direct `CField::OnPacket` decompile

- `CField::OnPacket` at `0x546d50`
- `CWvsContext::OnLogoutGift` at `0x9cccb0`
- `CUILogoutGift::OnCreate` at `0x7dec80`
- `CUILogoutGift::Draw` at `0x7de9b0`

Notes:
Direct `CField::OnPacket` decompile now confirms packet `432` is a dedicated logout-gift branch that calls `CWvsContext::OnLogoutGift` instead of flowing through generic local utility handlers. MapSimulator now mirrors that seam with a dedicated `LogoutGiftWindow` owner registered beside the other packet-owned utility owners, a packet `432` dispatcher branch that refreshes and surfaces that owner through the `CWvsContext::OnLogoutGift` path, and a `SetField` decode hook that peels the client-confirmed trailing 12-byte logout-gift cache off mixed character-data tails while preserving any preceding adjacent tail bytes as exact raw bytes plus aligned `int32` views instead of collapsing them to a count. That preserved aligned lead-in is now also mapped to the exact contiguous `CWvsContext` precursor dword slots that sit immediately before the `CUILogoutGift::OnCreate` commodity cache reads at `dword[4138..4140]`, so the simulator surfaces ownership as `CWvsContext dword[...] @ 0x...` instead of leaving those bytes entirely anonymous. `CUILogoutGift::OnCreate` and `CUILogoutGift::Draw` now inform both the simulator window geometry and click-slot behavior, so selecting a slot mirrors `CUILogoutGift::OnButtonClicked` by recording the client opcode `313` slot request rather than treating the click as pure local commodity navigation. The owner still attempts to use authored `UI/UIWindow.img/LogoutGift/backgrnd` art when a dataset exposes it and falls back otherwise. The remaining parity gap is narrower: the preserved lead-in bytes are now mapped to contiguous client-owned slots, but their exact semantic field names and any other client consumers are still unknown, and the active extracted UI dataset still does not expose a recoverable `LogoutGift` node even though the generated string-pool references that asset path.

### 20. Additional cash-service modal-dialog owners discovered by broad function scan and targeted `CCashShop` decompiles

- `CCouponUseSelectDlg::OnCreate` at `0x48c810`
- `CConfirmPurchaseDlg::OnCreate` at `0x48acc0`
- `CCashShop::OnCheckNameChangePossibleResult` at `0x495470`
- `CCashShop::OnCashItemResLoadGiftDone` at `0x496520`
- `CCashShop::OnCheckTransferWorldPossibleResult` at `0x4980b0`

Notes:
The broad `list_functions` sweep did not justify another split document, but it did surface one more cash-service owner family that is not named anywhere else in the backlog: stage-internal modal dialogs and notices. Targeted `CCashShop` decompiles confirm that these are not just generic notices hanging off the existing stage rows. `CCouponUseSelectDlg::OnCreate` and `CConfirmPurchaseDlg::OnCreate` show dedicated modal dialog owners inside the cash-service stage, while `CCashShop::OnCashItemResLoadGiftDone`, `CCashShop::OnCheckNameChangePossibleResult`, and `CCashShop::OnCheckTransferWorldPossibleResult` explicitly allocate `CUIReceiveGift`, `CUIChangingLicenseNotice`, and `CUITransferWorldLicenseNotice` and then drive them through `CDialog::DoModal`. That makes this a missing owner-family row inside the existing cash-service scope, not a duplicate of backlog 10 social systems or backlog 11 login flows.

### 21. Random Morph item-use dialog owner discovered by broad IDA function scan

- `CUIRandomMorphDlg::ShowDlg` at `0xa1ec00`
- `CUIRandomMorphDlg::CUIRandomMorphDlg` at `0xa1eb20`
- `CUIRandomMorphDlg::OnCreate` at `0xa1ee20`
- `CUIRandomMorphDlg::_SendMorphRequest` at `0xa1ec70`
- `CUIRandomMorphDlg::Draw` at `0xa1e910`

Notes:
The broad IDA function scan also surfaced a modeless item-use dialog owner that was not named elsewhere in the backlog set. This row belongs in progression/utility windows rather than avatar morph rendering: `CUIRandomMorphDlg` owns the dialog shown when a random-morph item is used on a target player, while backlog 1 owns morph image/action loading and avatar runtime presentation. WZ re-check confirms the constructor's background art path at `UI/UIWindow.img/Transform/backgrnd`, and IDA shows this owner carries the inventory position, item id, edit-control constraints, 500 ms request throttle, StringPool `0x136` blocked-send notice, and outbound opcode `184` payload shape.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.
- Progression or utility window groundwork now has a shared seam: named owners for world map, map transfer, cash-service, crafting, enhancement, character-info, and quest-side utility windows are registered with `UIWindowManager`, while minimap and status-bar utility buttons now route into those owners instead of staying entirely dead-ended. The dedicated pre-field `WorldSelect`, `ChannelSelect`, and `ChannelShift` selector family is tracked in backlog 11; this utility-window document only keeps the gameplay-side launch and gating hooks that hand control over to those login-owned selectors. The map-transfer owner now also keeps saved teleport destinations in a character-scoped destination book keyed to the active player or selected login-roster character instead of sharing one global saved list across every simulator avatar, persists that destination book under the simulator AppData profile across restarts, and hands the currently selected transfer target into the world-map owner when `BtMap` is used so the two navigation surfaces stay linked.

## Broad IDA Cross-Check

A broad follow-up `list_functions` sweep against the v95 client, compared back against the full backlog corpus, still did not surface another owner family that needs a new split document, but it did expose one smaller cash-service modal-dialog family that belongs in this document. A subsequent targeted `CWvsContext::OnPacket` pass also exposed two smaller context-owned utility owners that belong here.

The candidates that initially looked like possible omissions are already represented by existing rows instead of needing a new split document or duplicate utility section:

- `CWorldMapDlg` / `CreateWorldMapDlg` remain covered by the world-map and minimap-owner rows in this backlog.
- `CSlideMenuDlg` remains covered by the packet-script/message row in backlog 7, where the client owns it through `OnAskSlideMenu`.
- `CConnectionNoticeDlg`, `CPinCodeDlg`, and `CConfirmRaceDlg` remain covered by the login and character-entry backlog in document 11.
- `CWeddingWishListDlg` plus `CWishListRecvDlg`, `CWishListGiveDlg`, and `CWishListInputDlg` remain covered by the companion/social backlog in document 10.
- `CAdminShopDlg`, `CBookDlg`, `CStoreBankDlg`, `CGuildRankDlg`, and the other high-signal utility/social families surfaced by the scan are already named inside this backlog or the dedicated social backlog instead of being truly absent.

That means the right follow-up from this scan is to keep extending the existing rows above, not to add another umbrella utility backlog section that would duplicate ownership across documents. The one new utility-area split that did survive the cross-check is the cash-service modal-dialog family added below, because those owners were still absent from every backlog document rather than merely hidden under an existing row title.

## Remaining Parity Backlog

### 1. Progression and utility window parity

This area is currently absent from the backlog set even though the simulator already has concrete window and feedback implementations.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Minimap window parity | The minimap still loads the WZ-backed helper families from `UIWindow2.img/MiniMapSimpleMode/DefaultHelper`, still upgrades the post-Big-Bang NPC and direction art to the animated `UIWindow2.img/MiniMap/iconNpc` and `MiniMap/iconDirection/*` frame families when those assets exist, still feeds quest-aware NPC, portal, employee, same-field social/family, and packet-authored remote helper markers into `MinimapUI`, and still keeps the WZ-backed `WorldMap` search owner. WZ was rechecked again in the active data source for this pass: `UIWindow2.img/MiniMapSimpleMode/DefaultHelper` still carries the expected `user` / `another` / `friend` / `guild` / `guildmaster` / `match` / `party` / `partymaster` / `usertrader` / `anothertrader` helper canvases together with the NPC, portal, and arrow families, `UIWindow2.img/MiniMap/iconNpc` still exposes two animated NPC frames, and each `UIWindow2.img/MiniMap/iconDirection/*` family still exposes four animated direction frames. This pass keeps the earlier WZ load path and social-room anchoring work intact, keeps the earlier named `user` helper-family packet path in place, and closes one more packet-authored helper-semantics gap inside the existing minimap seams: `RemoteUserActorPool` now resolves packet-owned remote users with a Battlefield-team assignment but no explicit helper packet or relationship overlay to the WZ `DefaultHelper/match` marker instead of falling back to generic `another`, `MapSimulator` mirrors that same Battlefield-team-to-`match` fallback for synthetic tracked-user records before they reach `MinimapUI`, and focused `UnitTest_MapSimulator/MinimapHelperMarkerParityTests.cs` coverage now locks both the existing named `user` / legacy numeric helper payload paths and the new Battlefield-team `match` fallback precedence against relationship overlays. The earlier owner-local world-map candidate chrome work also remains in place: `WorldMapUI` measures candidate rows with the same client-text raster path already used by other client-owned IME surfaces in this codebase, trims vertical and horizontal candidate labels to the resolved candidate-cell width instead of letting raw `SpriteFont` draws spill past the frame, and tightens the candidate popup width/height calculation to the actual visible entry count plus the client viewport cap already used by the shared candidate hit-test layout. The remaining gap is now narrower and still limited to helper semantics rather than owner chrome or the missing WZ `user` helper family: the client's exact rule for which Match Cards or trader occupants should receive helper markers at all versus only owner-window occupancy state is still inferred rather than packet-decoded, richer `DefaultHelper` cases that depend on unknown helper packet state beyond the now-supported named `user` / occupant helper names are still not recovered, and packet-authored remote-user helper semantics beyond the explicit helper / relationship / Battlefield-team fallback now in place remain open. | Minimap helper feedback now stays closer to the client not only for live social-room occupancy and the owner-local world-map chrome work from earlier passes, but also when packet-owned Battlefield metadata is the only remote-user helper signal available: those users now land on the WZ `match` family instead of a generic fallback marker. The broader minimap and world-map parity wins from the earlier passes remain intact: animated minimap-owned notice and direction art, quest- and portal-aware markers, WZ-backed helper families including the addressable `user` helper canvas, packet-aware tooltip reuse, employee hover routing, duplicate suppression for same-name packet helpers, shared native IME placement updates, and the room-shaped synthetic helper clustering for Match Cards and trader occupants. | `MapSimulator.cs`, `MinimapUI.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `WorldMapUI.cs`, minimap/window loader path, `UnitTest_MapSimulator/MinimapHelperMarkerParityTests.cs` |
| Partial | Map transfer / navigation window parity | The simulator still routes minimap `BtMap` into a dedicated WZ-backed `MapTransfer` owner built from the `Teleport*` window art, constrains the surface to the client five-row viewport, keeps the client five-vs-ten slot split from `Teleport` versus `Teleport2` / `Teleport3`, persists regular and continent books per character, preserves empty slots plus exact slot indices through world-map retargeting, resolves saved labels from map ids, exposes the 12-digit manual map-id lane, and runs register/delete/move through the WZ-backed `FadeYesNo/backgrnd7` confirmation owner. This pass tightened the remaining register/session seam instead of widening it: the client StringPool prompt block (`0xBB8` / `0xBB9` / `0xBBA`) still resolves labels from the underlying map id rather than simulator category chrome, and the live-session register path now also keeps the owned manual retarget lane in place until an official opcode `69` result actually arrives instead of clearing it immediately on outbound injection. The broader row behavior remains on the recovered client-shaped path too: `CUIMapTransfer::OnRegister` parity still keeps the coarse category gate plus `0xBB4` / `0xBB5` preflight, local and proxied `CWvsContext::OnMapTransferResult` failures still resolve through the recovered `0xBB0` / `0xBB3` / `0xBB6` / `0xBB7` / `0xBD3` StringPool ids, and `CStage::OnSetField` bootstrap handling still decodes the `dbcharFlag & 0x8000` lead-in as the structured `ushort count` plus `(int key, ushort value)` group before `adwMapTransfer` / `adwMapTransferEx`, including the logout-gift-aware tail split already in place. Focused regression coverage now actually lives on disk in `UnitTest_MapSimulator/MapTransferPromptParityTests.cs` and `UnitTest_MapSimulator/MapTransferSetFieldBootstrapParityTests.cs`, covering client-owned prompt wording, coarse register gating, result notices, the structured `0x8000` lead-in, opaque pre-map-transfer bytes, and the logout-gift-aware bootstrap boundary. The remaining gap stays narrower and specific: deeper post-transfer tail ownership beyond the mirrored logout-gift-aware split still remains approximate, the full automatic network/session ownership around `CWvsContext::SendMapTransferRequest` / `OnMapTransferResult` is still broader than the optional live-session bridge, and the server's exact remaining unsaveable-map rules beyond the current category plus WZ-metadata plus migrate-forbidden `fieldLimit` gate plus recovered `0xBB4` / `0xBB5` preflight are still not fully mirrored. | Navigation parity now stays closer to the client not only in prompt text and destination storage but also in the live-session confirmation flow: a world-map retarget selected for registration remains visible until the authoritative result resolves it, and the row's focused prompt/bootstrap coverage is now present in the test project instead of only documented as intended coverage. The broader row is still intentionally partial because the unresolved work is concentrated in the post-map-transfer tail and the fuller automatic session/server ownership, not in the five-row owner surface, saved-slot persistence, prompt wording, current register preflight, `0x8000` CharacterData lead-in, or world-map retarget slot preservation seams already implemented here. | navigation/UI layer, minimap integration, destination-selection flow (`CUIMapTransfer::Draw`, `CUIMapTransfer::OnButtonClicked`, `CUIMapTransfer::OnCreate`, `CWvsContext::OnMapTransferResult`) |
| Partial | Cash shop / admin shop dialog parity | The simulator now replaces the generic Cash Shop and MTS placeholders with a dedicated `AdminShopDialogUI` owner backed by `UIWindow2.img/Shop`, reuses the shop dialog frame plus `BtBuy` / `BtSell` / `BtExit` / `BtRecharge`, keeps Cash Shop and MTS inside the same button-driven surface, routes the status-bar launchers so only the requested service window stays open, exposes separate scrollable NPC-offer and user-item panes with per-row selection and request submission messaging, consumes the client `UIWindow2.img/Shop/TabShop` category strip as a real WZ-backed catalog filter, now also consumes the owner `TabBuy` and `TabSell` tab canvases with their client WZ offsets instead of leaving the service and pane strips as text-only chrome, anchors the `TabShop` category strip to the client-owned bottom control position from `CAdminShopDlg::OnCreate`, mirrors the client-owned twin scrollbar layout called out by `CAdminShopDlg::OnCreate`, replaces the wish-list footer prompt with a `FadeYesNo`-style confirmation modal and routes `BtRecharge` through that same owner-local yes/no step before the dedicated `CUIAdminShopWishList`-style window opens, mirrors `CUIAdminShopWishList::SendRegisterPacket` by sending opcode `74` mode `3` with the selected item id through the existing admin-shop outbound bridge before focusing the saved row, syncs the displayed balance to the live inventory meso runtime like `CAdminShopDlg::Draw`, renders WZ-backed item icons plus stack counts for requestable rows in both panes and the detail area, interprets the left browse strip as the real WZ-backed `ALL` / `MOST` / `SELL` / `BUY` / `RE-BUY` family for Cash Shop while MTS swaps to its own WZ-backed `ALL` / `MOST` / `SELL` / `BUY` subset from `UIWindow2.img/Shop/TabSell`, and keeps the right `TabSell` strip acting as WZ-backed quick category tabs in the client inventory-tab order (`Equip` / `Use` / `Set-up` / `Etc` / `Cash`) instead of fake Cash Shop or MTS toggles. The Cash Shop NPC pane also resolves seeded rows against live `Etc/Commodity.img` metadata so matching entries inherit WZ-backed price, stack-count, commodity-SN, on-sale state, stack-limit data, and commodity-period text while off-sale commodities stay preview-only, and the packet-owned `OnGoToCommoditySN` launch seam now focuses the matching existing row plus category or synthesizes a WZ-backed commodity row when the SN was not part of the local sample list. The owner mirrors the client `AskItemCount` and singly treated `SendTradeRequest` branches for stackable and one-shot rows by opening the existing quantity or yes/no modals before the request is staged, clamps stackable delivery against live reward `info/slotMax`, keeps keyboard parity (`Up` / `Down` / `PageUp` / `PageDown` / `Home` / `End`, pane swaps, `Enter`, `Space`, modal confirm/cancel), and keeps row double-click on the selected entry bound to the same request path the client uses in `CAdminShopDlg::OnMouseButton`. Local zero-price and packet-owned non-positive rows now stay on the client-shaped sell-template path: the right pane rebuilds from live inventory slots that match the current source-item rows, filters those rows through the same WZ `info/cash`, `info/notSale`, and `info/quest` restrictions used at submit time, binds the request to the exact selected slot, re-applies the recovered `SetUserItems` / `CmpSellItem` mutation seam after successful source-item exchanges so consumed slots rebuild and reselect instead of falling back to whichever matching stack appears first, and now also mirrors the send-side `CAdminShopDlg::SetUserItems` branch from `CAdminShopDlg::SendTradeRequest` by retargeting the active owner surface to the matching right-pane inventory family and reselecting the exact slot-backed row as soon as opcode `74` mode `1` is built rather than leaving the pre-result sell surface stale until packet `366` arrives. Packet-owned packet `366` handling also stays closer to `CAdminShopDlg::OnPacket`: once a request is staged the simulator only treats subtype `4` as actionable, restores the row's pre-request state when any other subtype arrives, reports that ignored subtype without converting it into a local rejection, and now also mirrors the recovered `CAdminShopDlg::SendTradeRequest` outbound itself by sending opcode `74` mode `1` with the selected row serial number, requested count, and slot-backed `nPOS` for user-pane/source-item rows before waiting on packet `366`; if a packet-owned row cannot build that client-shaped trade payload, the owner now stays on the local simulator request seam instead of falsely entering the packet-wait state with no outbound. Focused regression coverage for the exact slot-match helper and opcode `74` mode `1` payload build now also lives in `UnitTest_MapSimulator/AdminShopUserSellMutationParityTests.cs`. The remaining gap is now concentrated in true live server-populated catalogs and packet-authored session state beyond commodity-SN targeting plus local synthetic commodity insertion, the broader authoritative sell/result refresh matrix beyond the current `SetAdminShopDlg` buy-vs-sell split plus send-side `SetUserItems` retargeting and success-side `CmpSellItem`-style right-pane rebuild, the client's wider sell-or-relay request matrix beyond the current local and decoded source-item lanes, and the wider server-authored trade/session rules behind those controls rather than missing request outbounds, quantity prompts, selected-slot source handling, quick-tab ordering, commodity targeting, off-sale commodity gating, commodity-row creation for packet-owned serials, recharge-gated wishlist launching, browse-position restoration, row double-click activation, immediate send-side user-pane retargeting, or ignored `366` subtypes being treated like simulator-authored local failures. | The simulator can now exercise the client cash-service launch path through a dedicated dialog owner with concrete per-item browsing, mode-specific WZ-backed browse chrome for Cash Shop versus MTS, client-shaped scrollbar and category-tab affordances whose bottom `TabShop` strip lands on the client-owned control geometry instead of a zero-origin overlap, visible item art in the browse and detail surfaces, a real wish-list confirmation step, a keyboard-drivable selection and request flow, client-shaped row double-click activation, remembered per-surface browse position instead of always dropping back to the first row after each tab/filter hop, an owner-local count prompt for stackable rows, a matching owner-local yes/no confirm step for singly treated requests, a zero-price trade-in seam that binds the right pane to live slot-backed user items, immediate send-side `SetUserItems` retargeting onto the exact slot-backed user row once opcode `74` mode `1` is staged, and packet-owned request dispatch that now mirrors all three observed admin-shop opcode `74` owner outbounds in this surface (`mode 0` reopen, `mode 1` trade request, `mode 3` wish-list register) instead of leaving the actual trade send path simulator-local. The remaining parity work is therefore centered on true server-authored catalog/session ownership and the broader result matrix, not on placeholder browse chrome, missing request dispatch, fake seller panes, stale pre-result sell-pane state, or mouse-only interaction. | cash-service UI layer, status-bar integration, shop-session flow (`CAdminShopDlg::OnCreate`, `CAdminShopDlg::Draw`, `CAdminShopDlg::OnButtonClicked`, `CAdminShopDlg::OnMouseButton`, `CAdminShopDlg::SendTradeRequest`, `CAdminShopDlg::OnChildNotify`) |
| Implemented | Status-bar login selector launch integration | The simulator now routes `BtChannel` into the dedicated login-scene `WorldSelect`, `ChannelSelect`, and `ChannelShift` owners instead of leaving the status-bar button as a dead-end in-field affordance. That launch path preserves the current world/channel context and hands control over to the pre-field selector runtime rather than duplicating a second world/channel browser inside the general utility-window layer. On the in-map utility side, the status-bar and System-popup `BtChannel` launch seam now mirrors the client-owned `CWvsContext::ChannelShift` guard order the simulator models in `MapSimulator.cs`: selector access is suppressed while a selector request is pending, while the packet-owned anti-macro challenge owns the field, while the packet-owned initial-quiz timer is active, and while the current field forbids migration/channel change; when those field-side guards trip, any open selector windows are collapsed back out of the gameplay scene instead of lingering as valid utility owners. The selector owners, packet-fed world metadata, `BtViewAll` roster handoff, and official-session bridge work remain tracked in backlog 11, but those are separate login-runtime parity tasks rather than remaining work in this status-bar launch row. | This row is now complete as a status-bar integration seam: the in-field launcher opens the dedicated selector owners, respects the same anti-macro, initial-quiz, pending-request, and no-migrate suppression branches that gate `CWvsContext::ChannelShift`, and tears down gameplay-side selector owners when those guards become active. Remaining login-scene selector metadata and packet/session ownership stay with backlog 11 by design rather than as unfinished work here. | status-bar utility-button integration (`CUIStatusBar::OnButtonClicked`, `CWvsContext::ChannelShift`, simulator seam: `MapSimulator.cs`) |
| Partial | Packet-owned admin-shop dialog parity | `HaCreator/MapSimulator` still no longer routes packet types `366` and `367` through the generic packet-owned NPC utility dispatch gate at the outer local-utility ingress; the admin-shop packets branch directly into the live `CashShop` / `AdminShopDialogUI` owner path instead of remaining just another case inside the broader NPC utility dispatcher, while the dialog itself still stays on the existing local-session and stage-migration seams. The simulator still mirrors the recovered `CField::OnPacket -> CAdminShopDlg::OnPacket` gate more closely: packet `367` with a non-zero decoded item count stages a packet-owned admin-shop session on the unique-modeless owner, records the NPC template id plus decoded row count, reuses the existing owner surface, and resets the visible row selection state back to the recovered `SetAdminShopDlg` defaults instead of auto-selecting the first row after the pane rebuild; packet `367` with a zero count still follows the recovered refuse path by showing a `CUtilDlgEx`-style notice through `StringPool` id `0x1238`, hiding the owner family, and mirroring the client close-ack `74` mode `2` branch through the existing outbound bridge/outbox seam. The recovered `CAdminShopDlg::SetAdminShopDlg` payload still remains modeled as an explicit packet codec rather than an implicit inline decode: direct client evidence confirms that this client version consumes exactly the commodity body (`SN`, `itemId`, signed `price`, `saleState`, `maxPerSlot`) plus the trailing wishlist byte before calling `SetUserItems`, `SetScrollBar`, `SetAvatar`, and `SetNPC`, so the simulator decodes that exact structure authoritatively, records any unexpected opaque tail length instead of assuming hidden fields, uses the decoded wishlist flag in owner-state reporting, and still runs that authoritative decode before the unique-modeless block gate so blocked packet `367` opens preserve the same NPC, row-count, wishlist, and opaque-tail evidence instead of falling back to a header-only record. Positive-price rows still populate the NPC pane directly, and non-positive rows still stay packet-authored as NPC sell templates that rebuild the user pane through the existing `SetUserItems`-style inventory seam instead of falling back to the older local browse catalog. The recovered sell-side mutation seam also remains packet-owned with direct client evidence from `CAdminShopDlg::SetUserItems` and `CAdminShopDlg::CmpSellItem`: packet-owned right-pane rebuild still walks live inventory slots in slot order, skips rows whose authoritative `saleState` is non-zero just like the client `nSaleState` gate, snapshots the affected user sell list when a packet-owned request is sent, and on successful packet `366` subtype `4` result `0` re-applies the recovered `CmpSellItem` diff plus user-scrollbar reposition rules against the rebuilt pane rather than only reselecting the pre-result row object. Packet `366` still owns the already-live follow-up result seam instead of falling back to the old timer-owned local resolution path: `AdminShopDialogUI` keeps non-storage requests pending on the recovered packet owner, only accepts subtype `4`, clears the packet-owned wait state when the result arrives, pays mesos on successful packet-owned sell rows, maps rejection notices through the recovered `StringPool` ids `0x1239` through `0x1240` plus `0x16ED`, mirrors the client reopen branch for result codes `1`, `2`, `3`, `6`, `7`, `8`, and `11` through opcode `74` mode `0`, and records the recovered disconnect / `CDisconnectException` hazard when packet `366` arrives without a live request instead of silently treating that mismatch as a local no-op. This pass narrows the transport gap inside the same owner/session seam instead of widening the row: the live official-session bridge now mirrors inbound admin-shop packets too, so opcode-framed `366` / `367` traffic that arrives on the existing local-utility bridge no longer dies at the bridge admission gate before the dedicated admin-shop handler can reuse the `CAdminShopDlg` owner path, and the bridge status surface now exposes the mirrored admin-shop inbound plus outbound opcode `74` traffic explicitly rather than leaving that transport leg invisible in admin-shop parity reporting. `AdminShopPacketOwnedSessionContract` still keeps dedicated admin-shop transport state (`opcode 74` payload/opcode/summary) and owner-family visibility state (`visible`, `staged-but-hidden`, `hidden by Cash Shop family`) so the packet-owned summary reflects deeper owner-family state outside the immediate request/result branch, and focused regression coverage for the recovered open-payload decode plus the dedicated ownership/session record and packet parser now actually lives on disk in `UnitTest_MapSimulator/AdminShopPacketOwnedOpenCodecParityTests.cs`, `UnitTest_MapSimulator/AdminShopPacketOwnedSessionContractParityTests.cs`, `UnitTest_MapSimulator/AdminShopPacketInboxParityTests.cs`, and `UnitTest_MapSimulator/AdminShopOfficialSessionBridgeParityTests.cs`. The remaining gap is narrower again and is now mostly outside the decoded row/session/inbox/bridge-admission surface: deeper native unique-modeless lifecycle or ownership transitions beyond the recovered `OnPacket` / `SetAdminShopDlg` / `SetUserItems` / `CmpSellItem` surface plus the visible-vs-hidden owner-family tracking remain unrecovered, and the official-session shape for admin-shop packets is still only the shared local-utility bridge transport plus the dedicated admin-shop inbox/command seam rather than a fully recovered retail-only admin-shop bridge/session contract end to end. | Without this row, cash-service parity can look much further along once `AdminShopDialogUI` draws and local request handling works, even though the client still drives open/refuse/result/reopen behavior through a dedicated packet owner on the in-map dialog itself. Keeping this row separate also prevents the in-map dialog lifecycle from being blurred into the distinct `CCashShop` / `CITC` stage owners or the `CUserLocal::OnGoToCommoditySN` migration seam. | packet-owned admin-shop dialog layer (`CField::OnPacket`, `CAdminShopDlg::OnPacket`, `CAdminShopDlg::SetAdminShopDlg`, `CAdminShopDlg::SetUserItems`, `CAdminShopDlg::CmpSellItem`, `HaCreator/MapSimulator/UI/Windows/AdminShopDialogUI.cs`, `HaCreator/MapSimulator/UI/Windows/AdminShopPacketOwnedOpenCodec.cs`, `HaCreator/MapSimulator/UI/Windows/AdminShopPacketOwnedSessionContract.cs`, `HaCreator/MapSimulator/UI/Windows/AdminShopUserSellMutationParity.cs`, `HaCreator/MapSimulator/Managers/AdminShopPacketInboxManager.cs`, `HaCreator/MapSimulator/Managers/LocalUtilityOfficialSessionBridgeManager.cs`, `UnitTest_MapSimulator/AdminShopPacketOwnedOpenCodecParityTests.cs`, `UnitTest_MapSimulator/AdminShopPacketOwnedSessionContractParityTests.cs`, `UnitTest_MapSimulator/AdminShopPacketInboxParityTests.cs`, `UnitTest_MapSimulator/AdminShopOfficialSessionBridgeParityTests.cs`) |
| Partial | Inventory window parity | Legacy and Big Bang inventory windows now render right-aligned meso text, per-slot item icons and stack counts, WZ-backed grade and quality markers, disabled-slot overlays, active-slot highlighting, and Big Bang tab or expanded layouts, while the simulator also feeds meso and picked-up items into the inventory surface at runtime, resolves equip pickup icons through `Character/*/*.img` like the client asset families, respects `info/slotMax`-style stack splitting, merge limits, and pickup-capacity checks for stackable items, wires `BtGather` and `BtSort` into simulator-side stack compaction and ordering, shows shared client-style hover tooltips with cached item names, type labels, stack data, descriptions, broader cure coverage, nearest-town `moveTo` text for `999999999`, post-Big-Bang stat aliases such as `indieSTR` / `indieDEX` / `indieINT` / `indieLUK` / `indieAllStat`, and the wider percentage consumable rows the data authors through `padRate`, `madRate`, `pddRate`, `mddRate`, `accRate`, `evaRate`, `speedRate`, `mesoR`, and `plusExpRate`. The inventory runtime still supports client-shaped intra-inventory drag/drop for occupied-slot swapping, stack merging, trailing-empty-slot moves, a held-item cursor overlay in both layouts, per-tab slot limits that grow in 4-slot rows up to a capped 96-slot budget, `BtCashshop` routing into the simulator cash-service dialog with live-meso inventory expansion, setup-chair activation plus `info/recoveryHP` / `recoveryMP` ticking on a client-shaped 10-second cadence, and now also respects the authored buff-chair metadata that the extracted install data still carries on rows such as `item/Install/0301/03010195`, `03010213`, `03010214`, and `03010327`: the same setup-chair seam now enforces the WZ `info/consumeitem` alternatives before the sit starts, uses the authored `rate` weights when multiple eligible alternatives exist instead of always burning the first owned row, and stamps the authored `info/cooltime = 1800` item cooldown instead of leaving those rows tooltip-only. WZ was rechecked again for this pass in the active extracted data source: `item/Install/0301/03010195/info/consumeitem` still exposes the ordered `2022124 (50)`, `2022070 (30)`, `2022280 (20)` table, while `03010213/info/consumeitem` still exposes `2022461 (50)`, `2022239 (20)`, and `2022280 (30)`, so the runtime now consumes from the same authored weighted alternative set that the tooltip and metadata resolver already surface. The runtime still returns explicit reject feedback for other non-chair setup items, keeps pet-food family/fullness gating with split no-pet/no-compatible-pet/no-hungry-pet feedback, preserves `Consume/*/spec/moveTo` failure-before-burn handling, timed morphs from `item/Consume/0221` `spec/morph` and weighted `spec/morphRandom`, abnormal-status cures, direct-stat and max-vital buffs, attack-speed (`booster` / `indieBooster`), abnormal-status resistance (`asrR`), EXP/drop/meso-rate (`expBuff`, `expR`, `plusExpRate`, `dropRate`, `mesoR`), and attack/defense/accuracy/avoidability/speed percentage buffs through the shared temporary-buff runtime so live totals reflect them instead of only the tooltip text. The same consume loop now also applies WZ-authored direct progression gains instead of leaving them tooltip-only: the active extracted data still exposes `item/Consume/0202/02022442/spec/expinc = 10000`, `item/Consume/0237/02370000/spec/exp = 100000`, and `item/Consume/0202/02022740/spec/charismaEXP = 5` alongside the sibling `insightEXP` / `willEXP` / `craftEXP` / `senseEXP` / `charmEXP` rows in that family, and inventory use now pushes those authored EXP and personality gains into the live `CharacterBuild` totals on burn. This pass also keeps the WZ-backed `spec/thaw` handling in the same consume loop: timed consumables such as `item/Consume/0202/02022001/spec/thaw = 10` and `02022040/spec/thaw = -6` still apply a live environmental-damage reduction against `MapInfo.decHP` field damage for their authored duration instead of remaining tooltip-only, and the shared tooltip path still surfaces that row as environmental-damage protection rather than the raw `Thaw +/-N` placeholder. WZ-authored item-launched NPC families now stay inside the inventory owner for `SETUP`, `USE`, and `CASH` instead of only the setup path: items with `spec/npc` / `spec/script` reuse the existing NPC interaction seam directly from inventory use, preserve slot-aware consumption when they do burn, honor both legacy `info/noExpend` and the newer `info/notConsume` no-burn flags before opening the authored NPC preview, and now also accept the numeric-string `npc` rows that still appear in the active extracted data source. The evidence rechecked cleanly in the active extracted data source: `item/Cash/0568/05680000` still carries `spec/npc = 9010000`, `spec/script = cash_5680000`, and `info/notConsume = 1`, while `item/Consume/0243/02430251/spec` still carries `script = consume_2430251` with `npc = "9000021"` and `02430288/spec` still carries `script = consume_2430288` with `npc = "9000018"`, so the simulator now keeps those string-backed authored coupon/service launches on the same consume-vs-persist branch instead of dropping them behind the generic reject path. The inventory owner still keeps that slot-aware right-click seam pinned even when item-reactor fallback consumes the item, so acknowledged inventory-click uses no longer drift to another identical stack when the local use loop hands off to reactor-triggered burns, and the same locked-slot helper still covers pet-food burns in addition to ordinary consume items. This pass also adds focused regression coverage on disk in `UnitTest_MapSimulator/InventoryItemMetadataResolverParityTests.cs` for the shared no-burn metadata seam, authored `consumeitem` ordering and weighted selection, authored `cooltime` rows, trimmed script values, and numeric-string `npc` resolution, so the metadata and inventory-use hooks stay aligned as the remaining runtime parity work continues. | The remaining gap is now centered on the client-authenticated accept/use families for specific non-chair setup items beyond the simulator's generic reject path, the exact client RNG ownership and any packet-authored branch nuances for multi-entry buff-chair `consumeitem` tables beyond the current WZ-rate-weighted eligible selection, deeper pet-feeding fidelity beyond the current WZ family/fullness planner plus local tameness or command-level gain path (especially exact official chat/UI wording and packet-authored success/failure ownership), the remaining long-tail consume-item matrix beyond the current recovery/direct-EXP/personality/transfer/cure/morph/stat/buff/environmental-protection plus the broader item-authored NPC launch coverage, and stricter packet-authored accept/reject semantics for unsupported item families rather than missing inventory rendering, drag/drop, tooltip metadata, slot progression, slot-stable local inventory-use dispatch, or the shared authored metadata seams now covered by the parity regression tests. | `InventoryUI.cs`, `InventoryUIBigBang.cs`, `InventoryItemMetadataResolver.cs`, `MapSimulator.cs`, `FieldRuleRuntime.cs`, `FieldInteractionRestrictionEvaluator.cs`, `SkillManager.cs`, `CharacterData.cs`, `PetController.cs`, `UIWindowLoader.cs`, `AdminShopDialogUI.cs`, `UnitTest_MapSimulator/InventoryItemMetadataResolverParityTests.cs`, inventory/runtime integration (`CUIItem::Draw`, `CUIItem::OnMouseButton`) |
| Partial | Item maker / crafting window parity | The simulator now replaces the `ItemMaker` placeholder with a dedicated WZ-backed `Maker` owner, routes maker-family NPC launches from `String/Npc.img` function text into that owner, derives the visible `Etc/ItemMake.img` bucket set from the live job the same way the client does, rebuilds the selector surface into client-like category/item combos with `All` plus aggregate `Strengthening Gem` / `Monster Crystal` / `Use` / `Setup` / `Etc` pages, keeps WZ/client insertion order instead of locally sorting rows, applies the client-side selector precondition sweep for `reqLevel`, `reqSkillLevel`, `reqItem`, `reqEquip`, and `reqQuest`, preserves the generic `Item Maker` family split instead of dropping all equip outputs, and narrows visible recipes for glove/shoe/toy/generic maker launches. This pass also carries one more client selector/runtime branch through the same owner: IDA-backed `CUIItemMaker::SetComboBox_Category` / `SetComboBox_Item` evidence is now reflected in the simulator with the missing dedicated `Disassemble Equipment` category, the corrected client aggregate-label mapping (`425 = Strengthening Gem`, `426 = Monster Crystal`), a packet-owned disassembly target list sourced from the live EQUIP inventory, and a matching detail/state surface that stages disassembly through the existing gauge/result seam instead of misclassifying packet-owned disassembly results as ordinary maker crafts. The owner exposes `BtStart` / `BtCancel`, animates the maker gauge, seeds starter ore, catalyst, and meso stock, resolves weighted `randomReward` branches, blocks crafts whose fixed or random result cannot fit inventory, carries `tuc` into the fixed-result summary, and now draws the client-shaped target/recipe/gem/catalyst/meso detail sections through the live inventory/runtime icon seams. Direct IDA evidence from `CUIItemMaker::StartItemMake` now also tightens the packet-owned session branch at that same seam: even when a maker session marks craft execution as server-owned, the simulator still enforces the client-owned empty-slot or result-capacity gate before it stages the request instead of bypassing local inventory-fit validation just because `OnMakerResult` will reconcile the final output later. Progression is now per-character and AppData-backed: family-specific mastery levels advance on success, discovered and hidden recipe ledgers persist on stable WZ row keys (`bucket/outputId`) with raw output ids kept as legacy-load fallback, duplicate-output rows stay distinct, hidden rows are classified from WZ `hide` first with a narrow unlock-hint fallback, hide-only rows can reveal locally once their persistent level/mastery gate passes, and the dedicated `???` page keeps locked hidden entries obscured until a local WZ-backed unlock pass or a packet-owned hidden-unlock payload reveals them. This row now also carries the packet-owned result side farther through the existing seams: local opcode `248` maker-result payloads decode the client-owned `CUserLocal::OnMakerResult` shapes, feed status updates and slot-reset behavior back into the window, reuse the existing craft-completion seam so successful packet-driven crafts advance the same simulator-side discovery/hidden/mastery progression store as local crafts instead of stopping at UI text only, keep the newly added disassembly selector path packet-owned instead of letting subtype `3/4` results leak into local craft progression, and now also reconcile the pending craft or disassembly request back through the live inventory seam by consuming the staged source equip/material/catalyst/meso costs and granting the packet-authored outputs the same way the client-owned result path implies. Direct client evidence from `CUserLocal::OnMakerResult` now also drives the disassembly status surface more closely: subtype `4` still clears and reseeds the disassembly slot, subtype `3` is treated as a two-output disassembly result rather than being flattened into a generic craft message, and the shared packet-result classification now explicitly keeps those subtype `3/4` disassembly payloads out of the craft-progression path even when their reward ids overlap ordinary maker outputs. Dedicated hidden-unlock payload support also persists packet-resolved hidden rows into the separate hidden bucket. This run closes more of the remaining session-side owner gap through the same seams: a dedicated maker-session payload can now drive `ServerOwnsCraftExecution`, authoritative disassembly target slots, and authoritative hidden-list membership back into the live `Maker` owner, packet-owned profession sessions can stage ordinary crafts through the same gauge/result seam as disassembly instead of forcing local reward application, the `Disassemble Equipment` page narrows to the packet-authored slot list rather than every live EQUIP row, the `???` page tightens to the packet-owned hidden roster while still letting explicit hidden-unlock payloads reveal rows immediately, and the authoritative maker-session state now persists above the window seam so it survives Item Maker hides or relaunches and can still apply on the next launch when the session packet arrived while the window was closed. Focused `UnitTest_MapSimulator` coverage now includes packet-owned craft reconciliation, disassembly slot clearing, subtype `3` multi-output grant decoding, explicit disassembly-vs-craft result classification guards, maker-session decode, maker-session clear-all reduction, authoritative disassembly-target filtering, packet-owned hidden-list gating, owner-lifetime session persistence, and the client-backed aggregate category labels alongside the earlier maker-result decode, hidden-unlock decode, keyed progression persistence, family-mastery advancement, and legacy fallback loading coverage. The remaining gap is now narrowed to the true server-authored hidden unlock matrix and the still-unrecovered official `CItemMakerInfo` profession/session packet contract itself: the simulator can now retain and replay an inbox-fed authoritative session across window lifetime changes, but it still does not know the real server opcode cadence, the complete payload shapes that grant or revoke those profession-session lists, or the fuller server-owned inventory/result lifecycle that surrounds the official owner outside the already-modeled `OnMakerResult` reconciliation seam | Crafting parity now covers the dedicated owner, client-shaped job/category filtering, corrected client aggregate labels, the missing disassembly selector branch, WZ-first hidden-row handling, row-keyed progression persistence, packet-aware hidden unlocks, packet-aware authoritative session filtering for disassembly and hidden-list membership, client-backed local inventory-fit gating before server-owned craft requests are staged, packet-driven craft completions feeding the same local mastery/discovery ledger as ordinary crafts without letting packet disassembly mutate that progression store, a client-backed packet-result reconciliation pass that now mutates the live inventory surface for staged packet-owned crafts and disassemblies instead of stopping at UI text, and a simulator-owned authoritative maker-session cache that no longer drops packet session state when the window is hidden or when the session packet lands before the next launch. The remaining divergence is concentrated in the unrecovered official profession-session packet contract, its true server-authored grant/revoke cadence, and the wider inventory/session lifecycle that may surround those packets rather than missing selector flow, gauge/craft controls, aggregate category wording, result rendering, hidden-page persistence, packet-session lifetime inside the simulator seams, or the already-threaded `OnMakerResult` decode/application inside the existing owner seams | inventory/runtime integration, crafting UI layer (`CUIItemMaker::Draw`, `CUIItemMaker::OnCreate`, `CUIItemMaker::SetComboBox_Category`, `CUIItemMaker::SetComboBox_Item`, `CUIItemMaker::OnButtonClicked`, `CUIItemMaker::StartItemMake`, `CUserLocal::OnMakerResult`) |
| Partial | Item upgrade / enhancement window parity | The simulator keeps the dedicated `ItemUpgrade` and `VegaSpell` owners, live inventory consumption, WZ-backed `info/tuc` slot recovery, local enhancement/potential/cube/hammer/reset flows, cash-owner theme swaps from `info/path`, staged cube and Vega presentation, WZ-backed Clean Slate / Innocence / Golden Hammer / Vicious' Hammer handling, and the broader string-backed scroll parser for Vega-marked scrolls, tablets, grouped stat text, number-first success rates, accessory-family aliases, and destroy-on-fail wording. This pass keeps the Maple Miracle Cube whitelist on one authored seam instead of drifting between duplicated lists and also tightens the parser against real extracted wording that was still easy to miss inside that same seam: `Item/Cash/0506/05062100/info/path` still resolves to `UI/UIWindow2.img/MiracleCube_8th`, `Item/Cash/0506/05062100/req` still exposes the full 22-item Maple 8th Anniversary Crimson family in the active extracted data, the starter enhancement seed still reuses that same `ItemUpgradeUI` req resolver instead of carrying a second hard-coded roster in `UIWindowLoader`, and `5062100` compatibility failures still surface as the family-level Crimson requirement instead of collapsing back to a single-equip message. WZ was rechecked again for the parser edge cases now covered directly in code and tests: `String/Consume.img/2040102/desc` and `2040202/desc` still use plural `face accessories` / `eye accessories`, `String/Consume.img/2046318/desc` and related accessory scrolls still use the plural `accessories` family label, `String/Consume.img/2046328` and `2046329/desc` still expose `MHP` / `MMP` shorthand inside grouped stat text, `String/Cash.img/5610000/desc` still carries the Vega rate sentence `enables a 30% success rate on a 10% scroll`, and the narrower accessory-scroll family authored in `String/Consume.img/2049200..2049211/desc` still uses `accessories (pendant, belt, ring)` / `accessories (pendants, belts, rings)` wording instead of the broader all-accessories alias. The item-upgrade target-slot parser now keeps that narrower authored family on the existing seam by resolving those parenthesized accessory subsets before falling back to the broader accessory alias, so pendant/belt/ring scrolls no longer silently widen back out to face, eye, earring, shoulder, pocket, badge, or medal slots when the extracted strings are already more specific. Focused regression coverage is now actually on disk in `UnitTest_MapSimulator/ItemUpgradeParityTests.cs` for the full req whitelist, the shared starter-seed seam, the `info/path` owner seam, the family-label resolution, the family-level block reason, plural accessory targeting, grouped `MHP` / `MMP` parsing, `Magic ATT` classification, destroy-on-fail wording, hammer chance parsing, the Vega rate text seam, and the newly narrowed pendant/belt/ring accessory-subfamily target and failure-text seam. | Item-upgrade parity now covers the dedicated owner family, client-shaped launch hooks from inventory, equips, and NPC overlays, the broadened local enhancement and potential loop, WZ-backed cube theme ownership, WZ-backed Vega modifiers and preview rates, WZ-backed tablet targeting and diligence bonuses, WZ-backed Maple Miracle Cube req gating with the full authored Crimson whitelist family, a shared starter-enhancement seed wired to that same req data, and on-disk regression tests for the whitelist, owner-path, and parser seams. The remaining gap is now concentrated in unrecovered server-authored enhancement and cube session ownership, official accept or reject and cooldown sequencing, and the long-tail scroll families whose payloads still sit outside the current WZ `info/inc*` plus authored `String/Consume.img` parsing seam rather than in the core local item-upgrade owner flow, Maple whitelist gating, Vega presentation, the now-covered accessory-family alias and pendant/belt/ring subfamily wording, the grouped `MHP` / `MMP` shorthand, or the currently covered destroy and rate text variants. | inventory/runtime integration, enhancement UI layer (`CUIItemUpgrade::Draw`, `CUIItemUpgrade::OnButtonClicked`, `CUIVega::OnCreate`, `CUIVega::Draw`, `CUIVega::OnButtonClicked`), `UnitTest_MapSimulator/ItemUpgradeParityTests.cs` |
| Partial | Equipment window parity | Legacy and Big Bang equip windows still share the same live-`CharacterBuild` equipment surface, disable-state resolver, hidden-base-under-cash layer, drag/drop routing, broader slot-family coverage, shared pending-request drain, and WZ-backed `UIWindow2.img/Equip/character` chrome seam already in place here, including the refreshed `cashPendant` and `charmPocket` chrome nodes from the active WZ export. This run narrows the remaining request/result gap inside that same seam instead of widening into another owner: the character equipment packet bridge no longer rejects `CharacterEquipmentAuthorityResultKind.AuthoritativeStateAccept` as unsupported, and now scope-validates packet-authored slot-state payloads against the active inventory-to-character, character-to-character, or character-to-inventory request before reconciling the live `CharacterBuild` visible and hidden equipment layers from the returned slot states. That authoritative branch now accepts equip-under-cash results where the requested base item lands in the hidden layer beneath a visible cash item, rejects packet payloads that mutate slots outside the active request, returns packet-displaced target-slot equips back to inventory without losing concealed-base semantics, and preserves authoritative unequip behavior by requiring the source slot to clear before the removed item returns to inventory. Focused regression coverage is now explicit in `UnitTest_MapSimulator/EquipmentWindowParityTests.cs` for authoritative-state payload round-trip, hidden-layer authoritative apply, unrelated-slot mutation rejection, hidden-under-cash acceptance, and source-slot clear validation for packet-owned unequip results. The remaining gap is narrower again and stays concentrated in true packet-authored async accept/reject ordering, opcode-specific session ownership rather than the simulator-owned owner-session stamp, server-fed multi-step reconciliation beyond the returned slot-state subset, and the exact retail packet/result cadence rather than unsupported authoritative-state apply, missing hidden-layer reconciliation, or packet payloads that were still unable to describe a final slot state at all | Equipment-heavy testing now covers not only the local drag, hidden-equipment, and shared pending-completion seam already in place for both equip owners, but also the packet-authored authoritative-state branch that can now reconcile visible versus concealed equipment layers on the live build instead of falling back to local-only acceptance whenever a result payload carried explicit slot state. That keeps the row anchored to the real client-facing equipment owner seam without overstating the remaining transport and server-cadence gaps. | `EquipUI.cs`, `EquipUIBigBang.cs`, `InventoryUI.cs`, `CharacterBuild`, `CharacterLoader`, `CharacterAssembler`, `EquipSlotStateResolver.cs`, `UIWindowLoader.EquipmentAndStats.cs`, `UIWindowManager.cs`, `MapSimulator.EquipmentWindowParity.cs`, `UnitTest_MapSimulator/EquipmentWindowParityTests.cs`, `CharacterEquipmentPacketParity.cs` (`CUIEquip::Draw`, `UIWindow2.img/Equip/character`, `CUIEquip::OnMouseButton`, `CUIItem::OnMouseButton`) |
| Partial | Mechanic equipment tab parity | The Big Bang equip window still keeps a genuinely separate utility-window concern that should not stay merged into the broader companion backlog: the mechanic pane still loads the client `Character/Mechanic` asset family, highlights the active tab button, seeds a dedicated mechanic equipment runtime so the page can render live `ENGINE`, `FRAME`, `TRANS`, `ARM`, and `LEG` icons with slot-specific tooltips, accepts drag/drop from the live equip inventory with slot-family validation, reuses the shared delayed submit/result seam from the main equip owner for equip-in and drag-back-out, mirrors the client `CUIEquip::ToggleMechanicEquip` gate by hiding the tab until the build is on a Mechanic job path and at least level `50`, snapshots plus resolves a dedicated mechanic runtime state token through that delayed owner path so stale mechanic-tab completions or interleaved pane changes reject instead of silently applying against the wrong machine-part state, and still exposes packet-owned external mutation through opcode/alias `1023` (`mechanicequip`) for five-slot snapshot, single-slot replace-or-clear, full clear, and reset-to-client-defaults updates. This run keeps the mechanic work inside that same transport seam instead of widening into the other companion panes: the dedicated `1023` mechanic authority-request planner still follows the intended bridge-first fallback order by trying the live local-utility official-session bridge first, only falling back to the generic local-utility outbox when the live bridge send path is unavailable, and only then queueing deferred official-session injection or deferred generic outbox delivery when both live send paths reject the payload. The same live bridge still admits inbound `1023` traffic into the existing packet inbox seam, so packet-authored mechanic authority requests, accept/reject results, and direct mechanic snapshot or slot-mutation updates keep flowing back through the same delayed equip-result owner that already protects against stale completions; the active-slot scope guard still rejects multi-slot authority completions such as reset-to-defaults for single pending mechanic drag/drop requests instead of silently mutating unrelated machine-part state. Focused source-side regression coverage now lives on disk in `UnitTest_MapSimulator/MechanicEquipmentTransportParityTests.cs`: it locks the authority-request payload round-trip, direct snapshot/slot-mutation/reset-default payload decoding, the live-bridge vs generic-outbox routing order, deferred official-bridge queue fallback, the single-slot reset-defaults rejection guard, the `mechanicequip` alias mapping, and the live bridge `1023` admission rule so this mechanic-only seam does not drift back into the generic local loopback behavior. The overlapping pet, dragon, and android companion equipment surfaces still live in backlog 10, which keeps this row focused on the one utility-window tab family that is not otherwise tracked there. The remaining gap is narrower again: the mechanic tab is no longer confined to the generic local loopback when a live local-utility bridge is present and the dedicated planner plus checked-in source-side regression coverage now preserve the intended transport priority, but the request/result byte layout is still simulator-authored and `1023` is still not the exact recovered retail server packet body/opcode family that the client uses for official mechanic-equip authority. | WZ still shows that this page is its own `UIWindow2.img/Equip/mechanic` surface backed by the separate `Character/Mechanic` asset family; the current extracted data still exposes the mechanic owner art under `UIWindow2.img/Equip/mechanic`, the separate mechanic button family under `UIWindow2.img/Equip/character/BtMechanic`, and the default part families under `character/Mechanic/01612000`, `01622000`, `01632000`, `01642000`, and `01652000`, which keeps the slot order, default runtime assumptions, and owner-local button surface grounded in the same asset family the pane already uses. The current export still shows the first default engine part at `character/Mechanic/01612000/info/{reqJob = 16, reqLevel = 50}` and higher-tier frame parts such as `01642000` continuing to carry their own job/level gates, which matches the existing WZ-backed validation/controller seam. Direct client lookup still confirms `CUIEquip::ToggleMechanicEquip` (`0x7a7af0`) spawns and destroys a dedicated `CUIMechanicEquip` owner at level `50+` instead of folding the page into ordinary character equipment state, and `CUIMechanicEquip::CUIMechanicEquip` (`0x7a3da0`) still creates that owner as a separate `151x172` mechanic window. The simulator now mirrors that split more closely not only by carrying mechanic-only state through request submission/completion and external mutation, but also by separating authority request payloads from plain mechanic-state payloads, by preserving the same live-bridge then outbox then deferred-queue transport order already used by nearby local-utility seams, and by pinning that transport contract with source-side coverage instead of leaving it as an untested local-loopback detail. The remaining divergence is concentrated in the unrecovered official packet contract rather than in missing mechanic-only owner/runtime separation, transport through the live local-utility bridge, or missing authority-path regression coverage. | `EquipUIBigBang.cs`, `MapSimulator.EquipmentWindowParity.cs`, `EquipmentChangeRequest.cs`, `CharacterLoader.cs`, `UIWindowLoader.cs`, `CompanionEquipmentController.cs`, `MechanicEquipmentPacketParity.cs`, `LocalUtilityPacketInboxManager.cs`, `LocalUtilityOfficialSessionBridgeManager.cs`, `MechanicAuthorityTransportPlanner.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, `UnitTest_MapSimulator/MechanicEquipmentTransportParityTests.cs`, inventory/window manager integration (`CUIEquip::Draw`, `CUIEquip::ToggleMechanicEquip`, `CUIMechanicEquip::CUIMechanicEquip`, `CUIItem::OnMouseButton`, `UIWindow2.img/Equip/mechanic`, `UIWindow2.img/Equip/character/BtMechanic`, `character/Mechanic/01612000/info/{reqJob,reqLevel}`, `character/Mechanic/01612000`, `character/Mechanic/01622000`, `character/Mechanic/01632000`, `character/Mechanic/01642000`, `character/Mechanic/01652000`) |
| Partial | Ability and stat window parity | The simulator already draws both legacy and Big Bang stat windows, supports AP assignment, detail-panel toggles, build-backed guild/fame/EXP text, hands/critical detail values, per-stat AP cap enablement, Big Bang open-vs-close detail button visibility, and the recovered v95 `CUIStat::Draw` beginner-cover branch that composites WZ-backed `UI/UIWindow2.img/Stat/main/cover0` plus `cover1` at `(0,0)` for beginner-root jobs and Evan beginner (`2001`) through level 10 before the stat text is drawn; the stat rows now also resolve equipped stat bonuses through `CharacterBuild` and mirror the recovered v95 `CUIStat::Draw` StringPool `1979` (`0x07BB`) bonus-decomposition format `%d (%d%+d)` when total STR/DEX/INT/LUK differ from the base stat instead of flattening all gear or buff gains into one opaque number, the detail pane now derives attack/defense, accuracy/avoidability, hands, critical, speed, and jump from live build totals instead of static placeholder fields, `SkillManager` now seeds non-hidden passive books alongside active skills for the current job path, and `SkillLoader` now follows the authored special-beginner and full advancement chains instead of the older `0/first/second/third/current` shortcut so WZ-backed starter books such as `skill/1000.img`, `skill/2000.img`, `skill/2001.img`, `skill/3000.img`, `skill/3001.img`, and `skill/5000.img` plus full multi-step lines such as Evan and Dual Blade can actually feed the stat totals before the current book alone, while `CharacterBuild` now folds passive plus live buff attack/defense, magic attack/defense, accuracy, avoidability, critical-rate, speed, jump, and skill-driven max HP/MP bonuses into those totals so the stat windows react to learned skills rather than gear alone, and Big Bang HP/MP AP clicks still use class-aware gain ranges with a 30,000 cap instead of a flat `+20`; the displayed physical and magical damage rows now also use a client-shaped min/max average where mastery scales the primary term instead of naively scaling the full max value, dagger formulas now split thief-vs-non-thief primary stats instead of treating every dagger as a thief weapon, thief-family dagger/katara/cane/claw damage now also restores the live `STR + DEX` secondary term instead of dropping the thief weapon STR contribution from the displayed range, extended Cygnus/hero/Resistance plus later post-Big-Bang class lines now map back onto their base warrior or mage or archer or thief or pirate stat archetypes for accuracy, avoidability, defense, Big Bang HP/MP AP growth, and baseline stat-window auto-assign so Aran/Evan/Mercedes/Phantom/Demon roots, Luminous (`2004` / `27xx`), Mihile (`5000` / `51xx`), Kaiser (`6000` / `61xx`), and Angelic Buster (`6001` / `65xx`) no longer fall back to the generic beginner spread, and the auto-assign path itself now mirrors the client `GetIdealStatUp` target-stat formulas for the supported legacy families instead of using the earlier generic heuristics: beginner and Evan-beginner builds now follow the client warrior-like `DEX = level`, `STR = 4*level + 12` target, warrior lines now hold `DEX` at the recovered v95 client `min(2*level, level + 30)` threshold while brawler-style lines stay on `min(2*level, level + 20)`, magicians now hold `LUK = level + 3`, bow users now distinguish bow `STR = level + 5` from crossbow or Wild Hunter style `STR = level`, thieves now hold `DEX = min(2*level, level + 40)`, gunslinger plus Mechanic families now use the client gun-style `STR = level`, `DEX = 4*level + 12` target, and beginner-pirate `500` auto-assign now chooses between the client infighter-vs-gunslinger split by live equipped weapon family with a stat-bias fallback when the window cannot ask the original client question. `CharacterBuild` now also recognizes cane, dual bowgun, and cannon weapon families in the displayed damage surface, and the same stat-window damage path now keeps closer family coefficients even while no weapon is equipped instead of collapsing those jobs onto generic bowman or warrior defaults, so crossbow or Wild Hunter or Mercedes, gunslinger or Mechanic, Cannoneer, Angelic Buster, and polearm-style warrior or Aran lines still follow their observed family split before gear is present. `SkillLoader` plus `SkillManager` now use `String/Skill.img` mastery names together with broader passive alias-field parsing (`ar`, `accX`, `padX`, `madX`, `pddX`, `mddX`, `er`, `evaX`) plus enhanced-stat aliases (`epad`, `emad`, `epdd`, `emdd`, `emhp`, `emmp`) so newer mastery, expert, blessing, shield-style, and avoidability or accuracy books can feed the stat windows instead of being dropped when WZ does not use the older base property names; `SkillManager` now also falls back to `String/Skill.img` description text for generic and specialized mastery books whose weapon family is not encoded in the display name alone, so WZ-authored books such as Fighter/Page/Spearman `Weapon Mastery`, Aran `High Mastery`, Battle Mage `Barricade Mastery`, Mercedes `23100005 Dual Bowguns Mastery`, Phantom `24100004 Cane Mastery`, Cannoneer `5300005 Cannon Mastery`, and Gunslinger `5200000 Gun Mastery` once again drive the displayed mastery and damage floor for the correct equipped weapon families even when the usable family token only survives in generic `weapon mastery ... of <family>` description text instead of the title. The mastery resolver now also normalizes punctuation and broader family wording from `String/Skill.img`, so description-authored variants such as `Cross Bow`, `Throwing Stars`, `Dual Bowgun(s)`, `Hand Cannon`, and `Pistol` text can still bind to the correct weapon family instead of depending on one exact title token; the mastery resolver now also respects narrower WZ-authored family text instead of broadening those books past their stated scope, so one-handed-only blunt/axe books such as Battle Mage `Barricade Mastery` no longer leak onto two-handed axes or blunts, and explicit staff-only mastery text no longer resolve against wands while generic `Spell Mastery` books still cover both magic-weapon families; the same mastery path now also respects WZ-authored conditionals such as Paladin `1220010 Advanced Charge` only while a live charge buff is active, covers stringless fallback cases such as `skill/5110.img/skill/51100001/common/mastery` and `skill/570.img/skill/5700000/common/mastery` by mapping those books back onto their observed knuckle and gun families instead of dropping them when `String/Skill.img` has no matching entry, and now also treats active mastery-bearing runtime owners such as Dark Knight `skill/132.img/skill/1321007/common/mastery = 2*x` `Beholder` as live mastery sources only while the summon is actually present instead of ignoring non-passive WZ mastery entirely. The same description-backed alias seam now also restores the missing `Magic ATT: +#x` branch that WZ still uses for multiple magic-mastery books, so authored hints such as `String/Skill.img/12100007/h` `Magic Mastery: #mastery%, Magic ATT: +#x`, `2100006/h`, `2200006/h`, `22120002/h`, `2300006/h`, `32100006/h` `Staff Mastery: +#mastery%, Magic ATT: +#x, Critical Hit Rate: +#cr%`, and `27120007/h` now once again raise the displayed magic-attack stat instead of losing their `x` payload because the older seam only recognized `Magic ATT` on `#y`. `SkillLoader` now also evaluates WZ-authored max-HP/max-MP rate fields such as `skill/100.img/skill/1000006/common/mhpR = 2*x`, `skill/200.img/skill/2000006/common/mmpR = 2*x`, `skill/510.img/skill/5100009/common/mhpR = 2*x`, and buff-side `indieMhpR` / `indieMmpR` branches such as `skill/2411.img/skill/24111003/common/indieMhpR` so the stat windows include percentage-based HP/MP books instead of only flat `emhp` / `emmp` bonuses, while the same authored-description seam now also resolves classic generic placeholder buffs that still publish stat totals through `#x/#y/#z/#u/#v/#w` hint text instead of explicit stat property names: `SkillLoader` now maps `String/Skill.img` hint surfaces such as `2301004` `Bless` `Weapon ATT: +#x, Magic ATT: +#y, Weapon DEF: +#z, Magic DEF: +#u, Accuracy: +#v, Avoidability +#w`, `1301007` `Hyper Body` `Max HP and Max MP: +#x%`, `5001005` `Dash` `Movement Speed: +#x, Jump: +#y`, and `3121002` `Sharp Eyes` `Critical Rate: +#x%` back onto the live attack/defense, HP/MP-percent, speed/jump, accuracy/avoidability, and critical-rate lanes instead of letting the older broad `x` fallback misclassify or drop those authored multi-stat buffs. The same description-backed placeholder seam now also covers the shared accuracy-plus-avoidability `#z` lane that `String/Skill.img` still publishes for beginner or support buffs such as `0000012`, `10000012`, `20000012`, `20010012`, `30000012`, `0000073`, `10000073`, `20000073`, `20010073`, and `30000073` `Weapon ATT: +#x, Magic ATT: +#y, Accuracy, Avoidability: +#z`, so those books once again feed both displayed accuracy and avoidability instead of dropping both stats when WZ does not split them into separate `#v/#w` placeholders. `CharacterBuild.TotalHands` now also includes equipped `incCraft` bonuses instead of dropping glove or gear hands from the detail panel, WZ-authored Maple Warrior books such as `skill/112.img/skill/1121000/common/x = u(x/2)` together with `String/Skill.img/1121000/h` `All Stats: +#x%` now raise live STR/DEX/INT/LUK through the stat-window totals instead of surfacing only as tray metadata, and Echo of Hero books such as `skill/000.img/skill/0001005/level/1/x = 4` together with `String/Skill.img/0001005/h1` `Weapon attack +4%, Magic attack +4%` now resolve into the existing attack-percent and magic-attack-percent lanes instead of being treated as a generic `x` payload. Mechanic's `Extreme Mech` bonuses now only surface while the live mech mount is active instead of behaving like an unconditional passive, weapon-specific mastery no longer resolves while no weapon is equipped, and non-Mechanic alias-backed buffs such as Onyx or Divine Shield style `e*` stat lines now also contribute to the live totals, but the simulator still does not mirror every exact post-Big-Bang coefficient or the remaining long-tail conditional mastery and weapon-formula branches for unresolved per-job edge cases beyond the newly covered charge-state, active-summon mastery, stringless-mastery, family-only generic-mastery descriptions, punctuation-normalized family-text, one-handed-only, staff-vs-wand, thief-secondary-stat, the recovered warrior-versus-brawler auto-assign split, the new weaponless family fallbacks for bow-vs-crossbow or Wild Hunter or Mercedes, brawler-vs-gunslinger or Mechanic-vs-cannon, soul-shooter, polearm-style warrior or Aran lines, the description-backed generic support-buff placeholder lanes including the shared `Accuracy, Avoidability: +#z` variant, the recovered magic-mastery `Magic ATT: +#x` alias branch, and the newly covered special-beginner plus full-advancement passive-skill lineage loading for Cygnus, Aran, Evan, Resistance, Dual Blade, Mihile, Kaiser, and Angelic Buster. | Visible stat windows now expose the recovered beginner-cover compositor for beginner-root and Evan-beginner characters through level 10, runtime-backed totals from equipped gear, learned passive skills, active buff bonuses, class-sensitive HP/MP AP growth across both classic and extended job branches, client-shaped classic auto-assign targets for warrior, magician, bow, crossbow or Wild Hunter, thief, brawler, gunslinger, Mechanic, beginner, Evan-beginner, and beginner-pirate weapon-choice cases, including the recovered v95 warrior-versus-brawler DEX threshold split from `GetIdealStatUp`, WZ-backed percentage HP/MP skill boosts, authored generic `#x/#y/...` support-buff placeholders for Bless, Hyper Body, Dash, Sharp Eyes, and the shared beginner/support `Accuracy, Avoidability: +#z` lane, the missing `incCraft` hands contribution, WZ-backed Maple Warrior `x%` basic-stat buffs, WZ-backed Echo of Hero attack/magic-attack percent buffs, WZ-backed magic-mastery `Magic ATT: +#x` hints for classic magician, Evan, Battle Mage, and Luminous mastery books, a broader client-shaped mastery-aware damage surface for cane, dual-bowgun, cannon, and thief-family secondary-stat users, closer weaponless family fallback for bow-vs-crossbow or Wild Hunter or Mercedes, brawler-vs-gunslinger or Mechanic-vs-cannon, soul-shooter, and polearm-style warrior or Aran lines, WZ-backed `ar`/`er` alias support for accuracy and avoidability books, tighter WZ-text mastery family matching that now also honors generic `weapon mastery ... of dual bowguns/canes/cannons/guns` description wording alongside the earlier punctuation, pluralized-family, charge-aware, active-summon, stringless, one-handed-only, and staff-versus-wand handling, that same family-aware seam now also gates the passive stat payloads published by those mastery/expert books so sword-or-axe or dual-bowgun accuracy bonuses do not survive onto mismatched weapons, special-job passive seeding that now walks authored beginner and intermediate skill books instead of skipping those books for lines such as Evan, Dual Blade, Resistance, Cygnus, and Mihile, and the recovered client primary-stat decomposition text so bonus-bearing STR/DEX/INT/LUK rows show `total (base+delta)` instead of a flattened total. The remaining divergence is narrowed to the exact coefficient matrix, the still-unimplemented long-tail mastery or weapon-formula rules for newer special cases such as Xenon and later unsupported weapon families, the unsupported client question flow for beginner-pirate auto-assign beyond the simulator's weapon-or-stat inference, and residual client-only conditional branches beyond these rate-backed stat surfaces, description-backed generic support-buff placeholder lanes, special-job passive lineage loading, alias-backed passive totals, description-backed mastery family mapping, charge-state and summon-state mastery gating, family narrowing, mech-state gating, weaponless family fallback, weapon-scoped passive stat gating, recovered magic-mastery `Magic ATT: +#x` placeholder support, and now client-shaped classic AP distribution rules including the recovered warrior-versus-brawler split plus the client `1979` stat-row decomposition format. | `AbilityUI.cs`, `AbilityUIBigBang.cs`, `CharacterBuild`, `SkillManager`, `SkillLoader`, `HaCreator/MapSimulator/Interaction/AbilityStatWindowClientText.cs`, `UnitTest_MapSimulator/AbilityStatWindowClientTextTests.cs`, `UnitTest_MapSimulator/AbilityStatSkillAliasParityTests.cs`, stat/runtime layer (`CUIStat::Draw`, `CUIStat::DrawTextA`, `GetIdealStatUp`) |
| Partial | Character info / profile window parity | The simulator now loads a dedicated WZ-backed `UserInfo` window from `UIWindow.img/UserInfo` or `UIWindow2.img/UserInfo/character`, routes the status-bar `BtCharacter` button into that owner, keeps the primary profile page build-backed with name/job/level plus world-rank and job-rank plus guild/alliance fields, and the Big Bang owner now switches into WZ-backed ride, pet, collect, and personality subpages that render simulator-side mount state, active pet data, collection summaries, and optional trait values instead of leaving those buttons decorative. The owner also loads the page-specific `BtException`, `BtArrayName`, and `BtArrayGet` controls from `UIWindow2.img/UserInfo`, hides primary action buttons outside the character page so the per-page button surface matches the selected subpage more closely, forwards `BtParty` into the existing social-list owner, now also consumes the legacy `UIWindow.img/UserInfo/BtSearch` surface and routes that pre-Big Bang button into the existing `UserList/Search` owner, opens the WZ-backed `exception` popup with local register/delete plus meso-filter toggles, and uses the character-page `BtPopUp` / `BtPopDown` controls in the two modes this row needs: local-owner popup selection for the `item` / `wish` auxiliary lists, and the recovered inspected-target popularity up/down path from `CUIUserInfo::OnButtonClicked` when the owner is showing a remote target. The `productSkill` loader consumes the full `UIWindow2.img/UserInfo/character/productSkill` family, including `9204`, so the character page can light the maker, glove, shoe, toy, and recipe ledger icons from the simulator-side maker progression snapshot; that same progression snapshot replaces the old equip-count collection summary with maker, glove, shoe, toy, craft-count, and discovered-recipe metadata, while the collection page treats `BtArrayGet` as a local claim action and keeps the collect icon routed into the dedicated book owner. The primary rank rows now consume packet/account-backed previous-rank state through a target-aware `RankDeltaSnapshot` seam, the personality page still uses the fuller WZ-backed `Tooltip` family from `UIWindow2.img/UserInfo/personality/Tooltip` including the charm `collection/0` body and numeric glyph set, and `/remoteuser inspect <characterId|name>` still reopens the same owner against a shared-pool remote actor build with target-aware banner, party/trade/family/popularity routing, remote pet-tab count, and active inspected-build draw/gate behavior for character, ride, medal, pocket, personality, and fame-check rows. The marriage seam also stays WZ- and client-backed: the Big Bang loader still consumes `UIWindow2.img/UserInfo/character/married`, the draw path still mirrors the client `CUIUserInfo::Draw` placement, and the badge gate still resolves authored wedding-ring ids first from the WZ string families before falling back to a narrow custom-name check so inspected remote builds can light the same badge through equipped ring data even without a marriage overlay packet. This pass narrows the remote inspection seam again with packet-backed and authority-aware profile handling instead of leaving remote zero/default state to masquerade as real profile data: remote enter-field packets now reapply their already-decoded `Level`, `JobId`, and `GuildName` values onto the inspected actor build on later refreshes instead of only on first spawn, while the `UserInfo` draw path still distinguishes authoritative-versus-unavailable remote rows for fame, guild/alliance, world/job rank, ride state, medal, pocket availability, personality values, and maker/collection summaries but now keeps build-backed remote subpage data visible when the inspected AvatarLook or stored snapshot already carries real evidence. Concretely, unauthored inspected ride pages now retain mount and saddle names from the remote build with explicit build-backed copy, medal and pocket summaries now surface equipped medal or pocket items plus inferred pocket unlock/charm state from the inspected build instead of collapsing every non-authoritative row to `-`, personality pages now keep non-zero build-backed trait rows visible with warning text instead of suppressing the whole page, collection pages now keep non-empty maker or Monster Book snapshots visible as build-backed summaries while still marking the live remote payload as unavailable, and the inspection resolver plus action-presence gate now also consume existing Messenger participant cards and Family-chart tracked-member strings so remote banner/action routing can keep authored location and channel context outside the social-list seam when those other runtimes already know the target. The same inspection resolver now also consumes active `WeddingField` participant snapshots as another target-presence seam, so wedding-room participants can keep their current-map banner/build context even when the shared remote actor pool is no longer the only live source, while the marriage badge path now treats active remote marriage relationship records plus active wedding groom-or-bride participant roles as authoritative evidence before falling back to the build-backed wedding-ring resolver. The remaining gap is narrower again: truly server-authored remote pet progression, collection/book payloads, live ride state, authoritative fame/rank/alliance plus medal/pocket/trait payloads beyond these build-backed AvatarLook or snapshot fallbacks, remote field/channel ownership outside the currently known shared-field, social-roster, Messenger-room, Family-chart, or active wedding seams, the exact server accept/reject or result workflows behind party, trade, family, and popularity requests, and the exact client launch-selection or deeper search-result handoff rules behind the legacy `BtSearch` path beyond this owner-level reopen are still approximated rather than mirrored exactly | Character-facing information now has its own launch path, a broader button-driven surface distinct from the stat window, WZ-backed popup navigation and personality-hover art, packet/account-backed rank-delta presentation that follows the active inspected build, the full WZ-backed maker `productSkill` icon family including the recipe slot, the recovered pre-Big Bang `BtSearch` reopen path into the existing social search owner, and an inspected-remote branch that reuses the same `CUIUserInfo` owner for target-aware party, trade, family, popularity, and per-character collection snapshot presentation instead of keeping those buttons or subpages local-only. That remote branch now carries tracked social-roster location/channel metadata into the inspection context when available, rejects the inspected-target party invite path when the local player is not the party leader, exposes packet-authored remote level/job/guild data when `CUserPool::OnUserEnterField` already provided it, keeps unauthored remote fame/rank/ride/medal/pocket/trait/collection lanes visibly unavailable instead of silently collapsing them to local-looking zero/none states, exposes the client-authored married badge art on the Big Bang character page when the simulator can resolve married state, resolves the married badge through the WZ-backed wedding-ring id family for both local and inspected remote builds when build data is present, no longer strands remote pet-slot tabs or local collection-owner metadata behind local-only defaults, no longer lets repeated remote enter-field refreshes leave inspected level/job/guild rows stale after the actor already exists, now also reuses Messenger-room, Family-chart, and active wedding participant snapshots before falling back to the shared-field/current-map owner for inspected banner and action context, and now also stops template-cloned local guild/rank/trait/pocket/ride/chair plus level/job state from masquerading as packet-owned remote profile data while still keeping build-backed medal, pocket, ride, personality, and maker/book rows visible when the inspected build actually carries them. The remaining gap is concentrated in truly server-authored remote profile payloads and result handling plus the last authoritative marriage-state and legacy-search-launch sources rather than dead `BtPopUp`/`BtPopDown`, a missing recipe icon, rank deltas that only followed the local player, inspected-target subpage rows that still read local build state, an inspected-target banner that always claimed channel 1/current map, remote pet tabs that only followed local summoned pets, a local collection-book context that dropped field/channel metadata, a local marriage badge that only looked for the raw `"wedding ring"` string, an inspected remote badge path that only trusted overlay packets, a remote `BtParty` path that ignored the local party-leader gate, remote metadata that only refreshed on first spawn, template-cloned local profile metadata leaking into remote inspect pages after AvatarLook decode, or the unwired legacy `BtSearch` surface itself | character/profile UI layer, player metadata presentation (`UIWindow.img/UserInfo/BtSearch`, `UIWindow2.img/UserInfo/character`, `UIWindow2.img/UserInfo/personality/Tooltip`, `UIWindow2.img/UserList/Search`, `String/Eqp.img/Eqp/Ring/1112315..1112320`, `String/Eqp.img/Eqp/Ring/1112803`, `String/Eqp.img/Eqp/Ring/1112806`, `String/Eqp.img/Eqp/Ring/1112807`, `String/Eqp.img/Eqp/Ring/1112809`, `CUIUserInfo::Draw`, `CUIUserInfo::OnButtonClicked`, `CUserPool::OnUserEnterField`, `CharacterData.cs`, `MapSimulator.cs`, `MapSimulator.RemoteUsers.cs`, `RemoteUserActorPool.cs`, `RemoteUserPacketCodec.cs`, `WeddingField.cs`, `MessengerRuntime.cs`, `FamilyChartRuntime.cs`, `UserInfoUI.cs`, `UnitTest_MapSimulator/CharacterInfoInspectionPresenceParityTests.cs`) |
| Partial | Book / collection window parity | MapSimulator still keeps `BookCollection` as a dedicated owner on the real `UIWindow.img/UIWindow2.img/Book` shell, keeps the IDA-recovered `BtPrev`, `BtNext`, and `BtClose` positions from `CBookDlg::OnCreate`, keeps the shared `UIWindow(.2).img/UtilDlgEx/dot0` and `dot1` spread markers that the client binds there, and keeps the same two-page paging cadence that `CBookDlg::OnButtonClicked` applies by advancing in two-page increments. This pass keeps that owner seam intact and narrows the remaining `CT_INFO` drift again inside the same snapshot path. WZ was rechecked again for the shared spread chrome and still confirms `UIWindow(.2).img/Book/backgrnd` at `478x353`, `UIWindow(.2).img/UtilDlgEx/line` as a native `2px` separator, and `dot0`/`dot1` as the shared page-marker pair; IDA re-checks still pin the button slots from `CBookDlg::OnCreate` at `(34,287)`, `(403,287)`, and `(430,10)`, the body-text draw base from `CBookDlg::Draw` at per-page `+35,+30`, the separator-rule base at `+3,+30`, the page counter lane at width `190` on fixed `y=293`, and `CBookDlg::SetPage` still drives the snapshot builder more literally by confirming a `CTextAnalyzer` width of `190`, margin `1`, and per-block `+10` vertical carry between analyzed `CT_INFO` groups. The simulator still prefers the Book shell and Book button family over the Monster Book frame art for this owner, still binds the live window to `BuildActiveCollectionBookSnapshot()`, still keeps the open-time payload flowing from the active `UserInfo` action context so inspected targets remain bound through the dedicated spread, and still carries the recovered `CBookDlg::OnCreate` font-table evidence through the same snapshot seam by tagging the twelve styles with the client `IWzFont` object family (`StringPool 0x5AF`), face (`0x1A25`), and odd-slot style variant (`0x5B0`) while resolving those odd styles through a dedicated bold `ClientTextRasterizer` path instead of one flat regular rasterizer. The snapshot seam still passes the live Book owner's style-aware text measurement callback into `BuildActiveCollectionBookSnapshot()`, still stops treating the label/value headline lane inside those synthetic collection-entry groups as one-line-only simulator copy, and now also drops the extra simulator-owned bottom status caption from the live collection spread because `CBookDlg::Draw` only paints the analyzed `CT_INFO` pages plus the centered page counters. Focused unit coverage for this row now pins the client rule-vs-text anchor split, the wrapped-headline carry path, the resulting page spill, the recovered twelve-style font matrix, and the now-empty collection-status lane so later snapshot edits do not reintroduce one-line-only headline budgeting or the removed simulator footer text. The remaining gap is narrower but still present: the simulator still approximates the client's exact native `CT_INFO` payload strings and the last unresolved per-line x/y micro-placements inside those corrected block lanes, still uses simulator-owned rasterizers instead of true COM-owned `IWzFont` objects, still synthesizes the left/right ledger split instead of replaying the exact client-authored page payloads, and still lacks the true server-/packet-authored collection dataset plus the final packet-owned `CWvsContext::CloseBook` ownership chain that would drive the official owner end to end. | This is now a real stand-alone utility surface rather than dead backlog theory: the simulator has the separate paged owner, the exact client dot-marker path, explicit spread-button flow, the proper Book shell and Book button family, an ordered collection-page compositor that tracks the client's text/rule draw sequencing more closely, a live collection-snapshot bridge from the `UserInfo` action context, an active client color-style matrix for the page body, and a closer font/measurement path because the Book styles now carry the recovered `OnCreate` font-table metadata and use regular-vs-bold client rasterizers in the same odd/even cadence as the native owner. The collection spread also now places its left and right page counters on the same fixed `y=293` per-page draw lane recovered from `CBookDlg::Draw`, keeps the `190px` left/right text content bands for `CT_INFO` body rows, reuses the authored `UtilDlgEx/line` separator art at its native `2px` height, keeps those separators on the client's wider page-local `+3,+30` rule origin, lets wrapped headline blocks as well as wrapped detail blocks carry later rules and rows down the page through the same `SetPage`-shaped `190px` analyzer seam, and no longer overlays the simulator-owned status caption that the native Book draw path never paints. This run narrows the remaining drift again by letting the live Book owner feed those same record builders a style-aware measurement callback for headline text too, so equipment and recipe spreads now spill later entries once wrapped name/value lanes consume the recovered page budget instead of only reacting to wrapped detail text, while the added coverage also keeps the recovered client font-table matrix explicit instead of letting later edits collapse it back into one undifferentiated text style. It still avoids simulator-authored inner page frame chrome on top of the Book shell, keeps the compact ledger copy across the overview/crafting/trait/equipment/recipe spreads, keeps the overview Pocket payload visible, remains genuinely bound to the `UserInfo` inspection context instead of silently snapping back to the local player, keeps the client draw families explicit in the snapshot seam instead of flattening every page line into one generic text type, and still closes through the dedicated owner-local `CWvsContext::CloseBook` analogue rather than only loose window callbacks. Its remaining divergence is now concentrated in unrecovered native `CT_INFO` payload micro-layout and payload wording, the exact client-authored mixed left/right block composition, true `IWzFont` COM ownership, live client-owned collection data, and packet-authored close ownership rather than shell preference, collection-provider wiring, page-counter lane placement, detail-only wrap carry, overview-row survival, remote-target binding, collapsed record roles, or the removed bottom status text continuing to drift from the native owner. | collection/book UI layer (`BookCollectionWindow.cs`, `ProgressionUtilityWindowModels.cs`, `MapSimulator.Content.cs`, `UIWindowLoader.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/CollectionBookClientParityTests.cs`, `CBookDlg::OnCreate`, `CBookDlg::Draw`, `CBookDlg::SetPage`, `CBookDlg::OnButtonClicked`, `CWvsContext::CloseBook`) |
| Partial | Quest log window parity | The simulator quest windows now render live runtime-backed lists for available, in-progress, completed, and recommended quests, instantiate the client `Tab/enabled` / `Tab/disabled` category buttons plus `BtMyLevel` / `BtAllLevel` filters from `UIWindow.img` or `UIWindow2.img`, keep selection synchronized with quest state mutations, color rows by accept/complete readiness, expand the list owner to use the dedicated quest-log shell instead of a simulator-only inline detail pane, preserve item-backed requirement/reward hover popups by moving them onto the dedicated detail owner, now load and honor the legacy `UIWindow.img/Quest/BtDetail` list-shell button so pre-Big-Bang quest browsing uses a client-shaped select-then-button cadence instead of forcing the Big Bang-style double-click open path, now also mirror the client `CUIQuestInfo::OnTabChanged` reselection cadence more closely by preferring suitable-level quests on the available tab, recently updated or recently viewed quests on the in-progress tab, and top entries when the prior selection no longer survives a tab or filter rebuild, now expose the client quest-detail location button families through the existing detail owner by routing NPC, mob-demand, and item-demand fallback cases into the world-map search owner instead of treating every specialized case as a pure NPC locate, now also mirror the client demand-item query split more closely by parsing WZ `Check.img/.../item/*/secret` flags, suppressing hidden demand items from local world-map results, preferring visible outstanding demand items when choosing the detail-owner/world-map item target, seeding packet-shaped item-query result sets when the detail owner asks for demand-item guidance, and when packet-owned item-query rows do not carry usable map results merging those rows back through the same WZ-backed per-item mob/NPC map candidate seam before the last current-field fallback instead of collapsing sparse packet queries to the active field. Active quest-detail item overlays now also reuse that per-item query seam so the highlighted delivery-item target follows the selected outstanding item’s resolved candidate maps rather than a single quest-wide fallback set. It now also splits the Big Bang quest-detail shell back onto the client button families by reserving `UIWindow2.img/Quest/quest_info/BtQuestDeliveryAccept` and `BtQuestDeliveryComplete` for delivery-specific guidance while primary accept/complete return to the shared `Quest/BtOK` art, surfacing item-backed delivery hints through those dedicated buttons and routing their cash-item fallback into the existing quest-delivery or Cash Shop owners with the client hardcoded cash-item ids `5660000` / `5660001` and shop migration SNs `50200218` / `50200219`, now keep a dedicated simulator-side delivery phase on the quest-detail state so the Big Bang delivery buttons, packet-owned delivery snapshots, and packet fallback rows no longer disappear as soon as the first relevant item count is satisfied locally, now preserve the relevant start/end delivery item metadata on the detail state even after counts are met so item-backed accept/complete handoff flows stay visible through the dedicated quest-delivery seam instead of being inferred only from the generic primary action, and now also gate those delivery branches on the presence of the relevant start/end NPC handoff so item-only quests without a client-shaped delivery NPC no longer light the delivery-button family purely from local item requirements. The delivery seam now also mirrors the client `CQuestMan::IsDeliveryAcceptQuest` / `CQuestMan::IsDeliveryCompleteQuest` heuristics more closely by parsing WZ `Check.img` `normalAutoStart`, `fieldEnter`, `equipAllNeed` / `equipSelectNeed`, and `interval` keys, excluding dragon/script/timed/auto-start accept cases from the dedicated delivery-button family, excluding the client hardcoded accept exception quest `10394`, and requiring the client-shaped completion cadence (`interval >= 1440` or end-demand mob/item data plus the relevant NPC handoff) before exposing the dedicated completion button. It also still parses WZ quest `area` ids into readable quest categories so the Big Bang list shell can rebuild each tab through persistent per-tab category option filters driven by the client `BtMax` / `BtMin` controls instead of treating category changes as a no-op, loads the full `UIWindow2.img/Quest/icon_info` family so the legend owner uses the client outer frame, inner sheet backing, and `Sheet/*` compositor, replaces the simulator-only ON/OFF checklist hitboxes with generated category-button slots that rebuild per tab/page, preserve per-tab hidden-category state, sort by the client category index instead of snapshot encounter order, suppress the client-skipped category bucket recovered from `CUIQuestInfo::LoadData` (`area == 51`), now anchors the Big Bang category-toggle family into the bottom quest shell by reusing the live `BtIconInfo` position as the client-shaped seed for `BtMax` / `BtMin`, docks the generated category panel above the footer controls instead of leaving it in the earlier top-left placeholder lane, centers each generated category row to the client `icon_info/Sheet/*` width (`192px`) so the divided count gutter sits closer to the real shell instead of spanning the full inner frame width, now composites those same WZ `icon_info/Sheet/*` row shells behind the generated category entries while keeping the inline `BtMax` / `BtMin` button art in the client `31px` label and `188px` count-gutter lane recovered from `CUIQuestInfo::Draw`, and now also fills that last label-to-count lane with a client-leaning dot-leader micro-compositor instead of leaving the row text and count visually disconnected. The generated category shells now also keep their top/middle/bottom strip selection and top-anchored sheet placement coherent across scrolled pages instead of re-capping each visible slice independently, while the packet-owned delivery seam now accepts an optional packet-shaped delivery-type override so the detail owner and delivery list can prefer packet-authored accept/complete button-family ownership when that hint is present instead of always falling back to the local heuristic, and now decodes the client-observed `QuestInfo::nDeliveryType` raw values from `CUIQuestInfo::LoadData` (`0 = accept`, `1 = complete`, `2 = none`) instead of treating packet hints as a simulator-local numbering. This pass also follows `CUIQuestInfoDetail::SetButton` and `CUIQuestInfoDetail::OnButtonClicked` more closely by collapsing started-quest guidance back onto the shared quest-guide button path instead of keeping separate simulator-only started-state NPC and mob locate buttons: pre-start detail browsing still uses the dedicated NPC handoff family, but active quests now resolve one guide action through the existing `TryGetQuestWorldMapTarget` seam so the same started-state button can drive packet-guided mob focus, visible-item query launch, hidden-item NPC fallback, or completion-NPC world-map guidance according to the current WZ/runtime state. The remaining gap is narrower: the simulator still does not mirror the client's exact generated-category text raster/compositor rules beyond the new leader fill, true server-authored item-query map results beyond the simulator's packet-plus-WZ per-item mob/NPC candidate merge when neither source can prove the final maps, the broader downstream server-result ownership behind those delivery buttons beyond the now client-aligned raw `nDeliveryType` override hook, or the last demand-rect-specific inline delivery branches behind the most specialized quest-detail buttons | Quest interaction now has a usable standalone log that reflects the same runtime state as NPC quest overlays, uses WZ-backed quest categories instead of a custom tab strip, follows the legacy detail-button cadence where that WZ shell exists, automatically reselects client-shaped quest entries when tabs or category filters rebuild, preserves the client split between generic quest actions and the dedicated Big Bang delivery-button family, keeps delivery-specific buttons and packet-owned delivery rows alive on a dedicated quest-detail delivery phase instead of dropping them once the local item-count heuristic changes, now suppresses delivery-button activation for quests that lack the corresponding NPC handoff the client expects, now also suppresses the delivery-button family for client-shaped auto-start, dragon, script-linked, equip-on-start, field-enter, timed, and the client hardcoded `10394` accept-exception cases while only surfacing delivery completion when the WZ demand cadence matches the client heuristic, now orders category toggles by the client category index while omitting the client-suppressed area-51 bucket, keeps the detail-owner item target aligned with the visible demand-item query path before falling back to hidden-item or NPC guidance, now feeds the world-map item-query and active-detail item-overlay seams with packet-plus-WZ per-item map candidates instead of either a quest-wide fallback set or the older current-field-only query collapse, and now places the Big Bang category controls and generated rows materially closer to the client bottom-shell geometry instead of the earlier placeholder top-left layout. The generated category rows now also use the WZ `icon_info/Sheet/*` shells with scroll-aware cap selection plus a dot-leader text/count lane instead of sitting on a bare simulator fill with disconnected labels, and the packet-owned delivery seam can now override the visible accept/complete delivery family when a packet or local utility test supplies that hint while also respecting the client raw `0/1/2` delivery-type numbering recovered from `CUIQuestInfo::LoadData`. Active quest-detail guidance now also uses one client-shaped started-state guide branch that can land on mob, item, or NPC guidance instead of keeping separate simulator-only started-state locate buttons, which narrows the remaining divergence further to exact client text raster polish, true server-authored demand-item query resolution when both packet and WZ evidence still run out, the broader delivery result lifecycle behind those packet-authored button families, and the last specialized demand-rect inline delivery branches rather than tab reselection, client-skipped category suppression, secret-item filtering, the Big Bang category-shell docking math, the older current-field-only item query fallback, or every started-quest guide button collapsing to a pure NPC-only path | `QuestUI.cs`, `QuestUIBigBang.cs`, `QuestDetailWindow.cs`, `QuestRuntimeManager.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `MapSimulator.cs`, packet/local-utility delivery seam, NPC/UI bridge (`CUIQuestInfo::Draw`, `CQuestMan::IsDeliveryAcceptQuest`, `CQuestMan::IsDeliveryCompleteQuest`, `CUIQuestInfo::OnButtonClicked`, `CUIQuestInfo::OnTabChanged`, `CUIQuestInfoDetail::SetButton`, `CUIQuestInfoDetail::OnButtonClicked`) |
| Partial | Quest detail window parity | The simulator already covered the client quest-detail shell, delivery inset, timer inset, action-button families, world-map guidance, tracked-state button pinning, close-button art, low-level note, `23,75` eligibility lane, `IsWorthlessQuest` header note rule, stored-or-synthesized `sLevelLimit` fallback, the shared client-text rasterizer seam, preserved `#W...#` summary-icon surfaces, `#i...#` / `#v...#` item markers, inline color tags (`#b`, `#r`, `#g`, `#c`, `#m`, `#d`, `#k`), inline emphasis tags (`#e`, `#n`), joined freeform detail-body marker preservation for `summary`, `StartDescription`, `ProgressDescription`, and `CompletionDescription`, and separate reset semantics where `#k` resets only color and `#n` resets only emphasis. This pass tightens the same formatter/owner seam again by narrowing the `#m` and `#c` preservation rule to actual client token shapes instead of any following `#` or digit. WZ evidence from `quest/QuestInfo.img/10001/1` and similar route headers still anchors map-name payloads such as `#m100020000#`, WZ evidence from `quest/QuestInfo.img/10002/1` still anchors item-count payloads such as `#c4031988#`, and the quest-detail path now also keeps the client current-map token shape `#m#` distinct from authored magenta style. Parameterized map-name, current-map, and item-count tokens therefore survive long enough to resolve through the normal formatter path, while bare authored `#m` / `#c` color tags still become quest-detail style markers and invalid `#c#`-style fragments no longer leak a trailing literal `#` into the body lane. The remaining gap is narrower: the simulator still does not reproduce the full typed CT-array compositor from `CUIQuestInfoDetail::Draw` with its per-entry fonts, exact bold font selection, authored x/y offsets, and exact canvas-vs-text ordering beyond the current inline marker path plus mixed body/row stacking and inline color/emphasis preservation, the exact official in-map marker placement and per-overlay art selection beyond the current list/search plus grouped-surface overlay seam, the exact authored `sLevelLimit` payload for quests whose client-localized text is generated outside the loaded quest WZ fields and carries custom wording or extra restriction shaping beyond the simulator's current stored-string-or-level/job fallback, or the full server-driven delivery-button result path behind the rarest quest-detail buttons such as the true `SendConsumeCashItemUseRequest` packet ownership, authoritative commodity migration/result sequencing, and server-authored delivery outcomes | Visible parity is closer for quest-detail freeform body text because authored route and demand rows that combine style runs with parameterized references now keep the intended payloads through the same preserved-marker path as icons, summary surfaces, colors, and emphasis. Loaded quest descriptions and `demandSummary` / `rewardSummary` strings can now render map-name lanes like `#m100020000#`, current-map lanes like `#m#`, and live item-count lanes like `#c4031988#` without literal numeric or stray marker tails leaking into the body or the formatter flattening those tokens into magenta/cyan style state. Focused `UnitTest_MapSimulator/QuestDetailTextFormatterParityTests.cs` coverage now pins the route-header, current-map, item-count, bare cyan/magenta style, and invalid bare `#c#` cases so later quest-detail formatter edits do not collapse those token families again. The remaining work is still concentrated in final CT-layout fidelity, exact overlay art and placement rules, client-synthesized eligibility wording outside loaded quest WZ text, and authoritative server delivery sequencing rather than in unloaded inline quest-detail surfaces, flattened color/emphasis styling, corrupted `#m...#` or `#m#` map references, corrupted `#c...#` item-count references, mixed body-versus-row suppression, or literal marker text leaking into the body lanes. | `QuestRuntimeManager.cs`, `NpcDialogueTextFormatter.cs`, `QuestDetailWindow.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `MapSimulator.cs`, `UnitTest_MapSimulator/QuestDetailTextFormatterParityTests.cs`, quest-detail interaction layer (`CUIQuestInfoDetail::Draw`, `CUIQuestInfoDetail::OnButtonClicked`, `CWvsContext::IsWorthlessQuest`) |
| Partial | Quest alarm / notifier window parity | The simulator now replaces the `QuestAlarm` placeholder with a dedicated WZ-backed tracker built from `UIWindow.img/UIWindow2.img/QuestAlarm`, supports minimize/maximize toggles, title counts, live in-progress quest rows with completion-state coloring, row selection, per-row dismiss buttons shaped from the client `BtDelete` art, double-click routing into the quest detail flow, and now reuses shared `UIWindow2.img/Quest/quest_info` chrome for selection, incomplete `prob` strips, and progress-gauge rendering while surfacing negative quest `act/money` costs as live meso progress rows and rewarding or consuming mesos against the bound inventory runtime during quest start or completion. The quest-alarm runtime also now tracks recent quest-state, mob-kill, and tracked-item updates so the client `BtQ/ani` alert frames and sky-blue recent-update emphasis can light up before `BtQ` falls back to the normal quest-log toggle path; when that alert route is active, the owner now prefers the first recent-update quest instead of blindly reopening whichever row happened to stay selected, simply clicking a recent-update title row now acknowledges that quest's alert through the shared quest-runtime seam just like the recovered `CUIQuestAlarm::OnMouseButton` path so the `BtQ` alert route and sky-blue emphasis do not stay latched until a double-click opens quest detail, opening a quest detail still acknowledges that quest's recent-update alert as well, and the loader now preserves the authored `UIWindow2.img/QuestAlarm/BtQ/ani` frame origins when composing that alert overlay instead of stamping the animation at an unanchored local button origin. The owner now follows the client-recovered five delete-slot layout more closely by exposing five visible quest titles, keeping each visible `BtDelete` slot live instead of dimming non-selected rows behind simulator-only disabled chrome, resizing the maximized frame to the active selected-demand stack, adding a local delete-all header action with `Delete`/`Shift+Delete` shortcuts on top of the existing per-row dismiss path, and now also following the recovered `DeleteQuestByIndex` recreate seam more closely by clearing stale selection after any delete, resetting the local page offset, and clearing the active quest-detail payload instead of leaving pre-delete detail content pinned in the simulator after the tracker mutates. The `BtAuto` toggle now drives a local auto-register mode instead of acting as selection-only chrome: per-row deletes now always add the quest to the persisted hidden-auto tombstone set just like the recovered client `m_aDeletedQuestID` path even when auto-register is currently off, manual quest-detail tracking pins quests into a persisted tracked set even when auto-register is off, those manual registrations now preserve their local pin order instead of being re-sorted by the snapshot title ordering, the effective registered list is still clamped to the client-backed five-quest limit from `string/ToolTipHelp.img/Game/Button/QuestAlarm`, the loader now actually resolves that authored tooltip `Desc` into the owner so sixth manual registrations surface the WZ help-text cap sentence instead of a simulator-only warning, `BtAuto` now surfaces the recovered `Auto Alarm on/off` wording through the shared quest-alarm StringPool seam, auto-registered quests now follow the recovered `StringPool 0x107C` ten-minute inactivity expiry lane instead of staying simulator-visible indefinitely once qualifying progress has been seen, and `Delete all` now clears the full local registration set plus the persisted hidden-auto tombstones under that cap instead of only the visible subset. The owner restores per-character auto mode plus minimized state from AppData across simulator restarts. The selected quest row now composes more like the recovered client `CUIQuestAlarm::Draw` path by keeping non-selected quests collapsed to 18px title bands, expanding only the selected quest into a 150px demand rect, drawing requirement counters through client-like `CUIQuestAlarm::GetProgressFont` and `GetProgressFontMeso` threshold colors instead of one flat incomplete/complete tint, drawing all requirement or meso rows with right-aligned counters and `select` versus `prob` strip families instead of rendering every visible quest as a full multi-line simulator card, vertically centering the selected-band strips, item icons, and count text inside each 18px demand lane, and now also routing the quest-helper title, page labels, row titles, row counters, requirement labels, requirement body text, requirement counters, header action text, and wrapped demand text through the shared client-text rasterizer/measurement seam instead of the older raw `SpriteFont` pass so truncation, wrapping, and inline alignment sit closer to the client text lane. The owner now also follows `CUIQuestAlarm::OnButtonClicked` more closely when the local tracker is empty or cleared: `BtMax` refuses to expand and surfaces the dedicated client-owned empty notice route (`StringPool 0x18EC`) when there are no registered quests, per-row delete feedback is now routed through the same recovered client notice seam (`StringPool 0x106F`) for every local delete instead of only when the simulator happened to be in auto mode, title text now flows through the recovered client title-format seam (`StringPool 0xE4C`) before falling back locally, those recovered quest-alarm StringPool payloads are now pinned explicitly in the shared string-pool override table so the owner keeps the exact client wording even if generated table order drifts, the header action anchor now measures that resolved `Quest Helper` title text instead of the older hardcoded local title width, `Delete all` clears the local registration set and closes the owner without the extra simulator-authored clear notice, title rows and inline requirement text now clamp through the shared delimiter-aware width helper instead of a blunt local character splitter, and the demand-rect compositor now prefers delimiter-aware wraps from the selected 150px band before falling back to raw character splits so slash-, hyphen-, and colon-heavy simulator text stays inside the client band more like the recovered owner instead of being chopped only by blunt token width. The owner now also follows the recovered `CUIQuestAlarm::OnMouseMove` title-hover path more closely: title/delete/header local action hitboxes advertise the existing clickable cursor seam, title/delete/header local actions no longer accidentally start simulator window drags, and recently updated title rows draw the recovered StringPool `0x18A8` tooltip near the cursor using the same client-text draw path as the quest-alarm labels, while ordinary non-recent title rows no longer synthesize generic simulator demand/issue hover text that could mask the recovered recent-update tooltip route or future packet-authored row tooltip payloads. The remaining gap is that this is still a simulator-side approximation rather than the client's packet-driven `CUIQuestAlarm` registration path, so exact server-authored registration sync and recreate semantics, the remaining unrecovered branches of the official `BtAuto` rule matrix beyond the recovered ten-minute inactivity lane plus auto-toggle wording, packet-authored per-row title-tooltip text beyond the recent-update `0x18A8` branch, any remaining unrecovered quest-alarm notice ids beyond the stabilized title/delete/empty/recent-update hover plus auto-toggle ids, and the last CT-authored spacing, font-object selection, or line-break micro-rules inside the demand rect are still not mirrored exactly | Active questing now has a persistent standalone progress tracker with animated quest-alert feedback, row-local and owner-wide dismiss affordances, a client-shaped five-slot registration cap instead of simulator-only paging, richer WZ-backed progress chrome, recent-update emphasis that now clears on row selection instead of only after opening quest detail, a cap notice that is now actually sourced from the authored `ToolTipHelp.img/Game/Button/QuestAlarm/Desc` text instead of an unwired local fallback, and a per-character persisted auto-register/pinned-tracking model whose manual registrations keep a stable local pin order instead of being re-sorted by the quest snapshot. `BtQ` alert routing now also prefers an actually recent quest instead of whichever row happened to remain selected, the alert overlay itself now honors the authored Big Bang `ani` origins instead of drawing a locally offset approximation, every visible delete slot now stays actionable like the client slot layout instead of only the selected row presenting an enabled-looking delete affordance, the selected demand stack now uses recovered client-style progress-threshold coloring plus tighter centered strip/icon/count placement for requirement and meso rows, per-row delete now also records the client-shaped hidden-auto tombstone and surfaces the recovered delete notice even when `BtAuto` is off, `BtAuto` itself now surfaces the recovered `Auto Alarm on/off` wording and follows the recovered ten-minute inactivity expiry lane instead of leaving simulator auto registrations pinned indefinitely after qualifying progress, the visible quest-alarm text lanes now sit on the same shared client-text measurement/draw seam already used by newer parity owners instead of the older raw simulator font path, and the row title hover path now has the recovered clickable cursor plus recent-update tooltip behavior instead of being a static simulator text band that could also begin a window drag. The remaining gap is now narrowed further to packet-authored registration semantics, the unrecovered remainder of the official `BtAuto` rule matrix beyond that ten-minute inactivity branch, per-row packet tooltip text beyond the recovered recent-update branch, remaining quest-alarm notice-id recovery beyond the stabilized title/delete/empty/recent-update hover plus auto-toggle text set, and the last spacing/font-selection/line-break micro-rules rather than to missing persistence, sticky viewed-alert routing, unstable manual pin order, an unanchored alert animation, loose demand-band row alignment, an over-permissive empty maximize path, stale hidden-auto delete-all state, an unwired authored cap sentence, drifting quest-helper wording, extra auto-off delete drift against the client tombstone seam, an extra delete-all clear notice, blunt local title/body truncation, static/no-cursor recent title hovers, generic local demand/issue title-hover text on ordinary rows, accidental local-action drag starts, or stale detail payload retention after delete. | progression/UI layer, quest runtime, demand rendering (`CUIQuestAlarm::Draw`, `CUIQuestAlarm::OnButtonClicked`, `CUIQuestAlarm::OnMouseButton`, `CUIQuestAlarm::OnMouseMove`, `CUIQuestAlarm::DeleteQuestByIndex`) |
| Partial | Book collection / Monster Book parity | `BookCollectionWindow` is now actually wired through the dedicated Monster Book runtime instead of still defaulting back to the local collection-ledger snapshot path. The loader now prefers `UIWindow(.2).img/MonsterBook/backgrnd` for the owner shell and loads the client-backed `arrowLeft`, `arrowRight`, and `BtClose` controls from the Monster Book node itself while still placing them at the IDA-confirmed `CBookDlg::OnCreate` coordinates (`34,287`, `403,287`, `430,10`). The existing WZ-backed `cardSlot`, `infoPage`, `cover`, `select`, `fullMark`, `BtSearch`, `LeftTabInfo`, `LeftTab`, `RightTab`, and `ContextMenu/BtRegister` / `BtRelease` assets remain active, and the dedicated owner path now stays live from open through close: `MapSimulator.Content` clears the old collection-ledger provider for the book window, routes the live window snapshot through `ResolveBookCollectionActionContext`, feeds register/release clicks back through `_monsterBookManager.SetRegisteredCard` for the active inspected character build, and now also routes IME candidate clicks through the same Win32 IMM selection bridge already used by the simulator's other recovered `CCtrlEdit`-style owners instead of leaving the popup as a draw-only overlay. The `UserInfo` collect page still consumes the same Monster Book snapshot for summary text, while the opened book owner itself now consistently comes up on the dedicated Monster Book overview plus chapter-tab path, keeps the close/reset lifecycle aligned to `CWvsContext::CloseBook`, and no longer silently falls back to the synthetic collection-book spread when reopened from character info. This pass also keeps tightening the pickup side with direct WZ evidence from `item/Consume/0238/*/spec/consumeOnPickup = 1`: `MonsterBookManager` retains that authored flag from the card catalog, pickup-capacity checks no longer reject Monster Card drops on ordinary inventory-space limits, and resolved card pickups feed the Monster Book runtime without also leaving the `0238xxxx` card behind in inventory like a normal use item. The existing `MonsterBookManager` work remains in place: it still builds the catalog from `item/Consume/0238.img` plus mob metadata, persists per-character ownership under AppData, tracks up to the client-like five-card completion mark, resolves each mob back to its authored `0238xxxx` card item, groups by the client-facing `0238xxxx` chapter buckets, resolves reward and habitat naming through `String/MonsterBook.img` plus the item/map name caches, normalizes legacy multi-register saves down to one registered slot, and keeps the local search lane on the owner-local caret/IME/exact-match-first seam instead of the older dead append-only query path. This run narrows the remaining owner-local search polish further: the IME candidate popup already measured and drew through dedicated client-text rasterizers, sized itself from the visible candidate page rather than the full candidate list, numbered each visible candidate page through the client StringPool `0x1A15` `%d` format (`1..n`), supported owner-local selection highlight, select-all/copy/cut/paste/insert clipboard paths, shift-extended caret movement, IME-first `Enter`/`Up`/`Down` handling while a candidate page is open, caret hit-testing plus visible-text windowing on text-element caret stops, and a fixed client-shaped `360ms` / `42ms` held-key repeat cadence; it now also honors the active IME candidate-window placement form before falling back to the local search-box anchor, uses the recovered vertical/horizontal font-height/page-size metrics including the `266px` overflow branch and post-size client viewport placement, applies the clause-anchor X shift even for non-vertical candidate owners, explicitly clears the native IMM composition/candidate placement again when search focus exits or the book closes instead of leaving stale native candidate-window state behind across close or reopen, and now feeds the same recovered point/rect/exclude candidate-window form through `WindowsImePresentationBridge` instead of collapsing native IMM placement back to an always-exclude shape even when `CCtrlEdit::CreateIMECandWnd` recovered a different owner form. The owner-local search box still supports closer `CCtrlEdit`-style mouse editing through click-to-anchor selection drags plus shift-click extension instead of only single-click caret placement. The remaining gap is narrower but still real: ownership and save flow are still simulator-local rather than packet-authored, Monster Card drops still use a simulator-spawned matched card item instead of recovered server/drop-table authorship and odds, and the last native client search-owner polish is now mainly the unrecovered locale-specific edit or candidate-owner quirks beyond this closer `CCtrlEdit`-style click, drag-selection, placement-form carry-through, edit, and native-placement reset behavior. | This row is now closer to the client in one more important way: the dedicated Monster Book shell is no longer only loaded cosmetically while the live window keeps running a collection-ledger mode underneath, the card pickup path respects the authored `consumeOnPickup` contract carried by the WZ card items instead of treating Monster Cards like normal inventory loot, and the local search owner now behaves more like the recovered `CCtrlEdit` family rather than a simplified append-only text box. The simulator now opens the real Monster Book owner/runtime path for the book window, keeps the collect-page summary and book-owner detail views split the way the client seams imply, uses the Monster Book node itself for the shell plus page-turn/close buttons, preserves the already-implemented tab, search, registration, pickup, and close/reset behavior, tightens search editing with owner-local selection, click-drag and shift-click mouse selection, clipboard, IME-priority navigation, text-element caret stepping, candidate-click ownership, StringPool-numbered candidate ordinals, client-shaped candidate-owner metrics, IME window-form placement, native IMM placement-style carry-through for point/rect/exclude candidate owners, and an explicit native IMM reset when the active search owner closes or yields focus, and no longer lets Monster Book search repeat cadence drift with the host machine's keyboard settings. The remaining divergence is concentrated in packet-authored ownership persistence, real server-authored drop odds/save flow, and the last locale-specific owner quirks rather than missing owner wiring, dead register callbacks, fallback collection-ledger reopen paths, stale full-list candidate sizing, global candidate numbering, draw-only candidate-popup clicks, forced fallback candidate anchoring, locally measured or over-clamped candidate popup bounds, mouse selection that only moved the caret, simplified text selection behavior, stale native candidate placement surviving close or reopen, native point/rect/exclude forms being flattened back to exclude-only placement, OS-shaped repeat timing, or card items lingering in inventory after pickup. | book-style collection owner and data bridge (`BookCollectionWindow.cs`, `UserInfoUI.cs`, `MonsterBookManager.cs`, `UIWindowLoader.cs`, `MapSimulator.Content.cs`, `MapSimulator.cs`, `WindowsImePresentationBridge.cs`, `SkillMacroImeWindowPlacementLayout.cs`, `CBookDlg::OnCreate`, `CBookDlg::Draw`, `CBookDlg::OnButtonClicked`, `CWvsContext::CloseBook`, `CMonsterBookMan::LoadBook`, `CCtrlEdit::OnKey`, `CCtrlEdit::CreateIMECandWnd`, `CCtrlEdit::OnSetFocus`) |
| Partial | Parcel / package-delivery window parity | The simulator no longer presents package delivery only through the older memo owner. The main owner still loads `UIWindow2.img/Delivery` into `MemoMailboxWindow`, mirrors the client-confirmed `CParcelDlg` three-tab split with explicit receive/send/quick-send tab state, uses the authored `Delivery/backgrnd2`, `Delivery/Tab/*`, `Keep/base`, `Keep/base2`, `Keep_Info/backgrnd*`, `Send/base`, `Send_Info/backgrnd2`, `Send_Info/backgrnd3`, `Quick/base`, `Quick_Info/backgrnd2`, and `Quick_Info/backgrnd3` surfaces, keeps receive rows in a dedicated parcel backlog with row selection plus wheel/scrollbar paging, routes the receive button family through the dedicated package-claim flow, and drives send/quick-send button behavior through the client-shaped split that IDA showed in `CParcelDlg::OnButtonClicked` (`Send`, `SetMoney`, and fee-info open/close routing). This pass tightens the owner interaction seam further instead of leaving receive-state handling mouse-only or command-shaped: the receive backlog now follows the dedicated owner with keyboard row navigation and paging (`Up`/`Down`, `PageUp`/`PageDown`, `Home`/`End`, `Enter`) that keeps the selected row opened and visible inside the client-shaped six-row lane, the empty receive owner no longer tells the player to fall back to `/memo packet deliver ...`, the `BtMeso` feedback path now reports that the parcel-owner meso field was focused instead of pushing the user back toward `/memo draft ...` instructions, and compose focus no longer lingers on hidden send/quick fields after the active owner tab shifts back to receive or across the client-shaped send-vs-quick split. The packet/data seam stays tighter too: `PacketOwnedParcelPacketCodec` still treats the post-body byte as the current parcel-state bitfield seam (`read`, `keep`, `claimed`, `has-item`, `has-meso`) instead of a plain boolean, applies those decoded flags into receive-session rows and single-entry packet deliveries, only decodes optional `GW_ItemSlotBase` bodies when the item bit is present, only surfaces mesos when the meso bit is present, and still extracts memo text from MapleString-like candidates in the `PARCEL::Decode` body before falling back to printable-ASCII heuristics. The simulator also no longer reuses `dateExpire_Parcel` as a fake receive timestamp: packet-owned rows keep their simulator-side receive ordering timestamp while the decoded expiration is surfaced explicitly as the client-backed claim deadline in the parcel body/status seam, disables expired package rows from the receive `ClaimableCount` / `BtGet` path, surfaces that deadline in both the receive-detail and package-claim owners, and blocks late claims instead of letting expired attachments remain claimable behind a decoded `Expired` label only. Packet 8 arrival notices still preserve more of the client ownership split by reporting the leading sender plus decoded meso or resolved item-name-and-quantity attachment summary instead of collapsing back to a sender-only note or an item-id-only note. Owner-side compose parity is still closer on the attach flow as well: send-tab package staging no longer depends on `/memo draft item` commands, still accepts real live-inventory drag-drop into the package lane, now opens `/memo compose` without immediately redirecting the user back to `/memo draft ...` for basic edits, and also arms an explicit owner-driven inventory pick mode when the package lane is clicked without an active drag, brings the existing inventory owner forward, and routes the next live inventory slot click back into the parcel send lane while lane re-click, tab change, or successful staging clears that armed state. Quick-send still blocks item packages on both click and inventory drop. `UnitTest_MapSimulator/PacketOwnedParcelParityTests.cs` now exists and locks the state-bit decode, attachment gating, dialog-mode tab split, expired-row `ClaimableCount`, packet 26 quick-owner open, packet-result 18 quick-owner close, and quick-send item-block seam so parcel receive-state drift is less likely to regress. The remaining gap is narrower and now centered on the parts still not fully recovered from WZ plus client seams: the simulator still lacks the fully proven official `PARCEL::Decode` field map beyond current flags/text/attachment/expiry essentials (for example the exact ownership of the still-unproven fixed-body bytes around quick-delivery and any true sent-time or internal server fields), still does not have a recovered true packet launch mapping that ties parcel owner startup into `UI_Open`-family dispatch (IDA reconfirms `CUserLocal::OnOpenUI` only forwards to `CWvsContext::UI_Open`, and the recovered `UI_Open` switch still has no explicit parcel-owner case), still does not implement the exact native client attach picker/modal inventory browse widget beyond this closer inventory-owner handoff, and the exact memo/social ownership boundary beyond this explicit parcel-dialog split is still not fully recovered. | Package delivery is a real client-owned utility window family, not just a note inside messenger or memo systems. This row now tracks the dedicated Delivery owner, the recovered receive/send/quick-send split, flag-aware packet-owned receive row state from `CParcelDlg::OnPacket`, a keyboard-backed receive backlog in the same owner seam instead of mouse-only paging, an owner-local item-staging lane over the `Delivery/Send` layout, WZ-backed receive/detail chrome from the authored `Delivery`, `Keep`, `Keep_Info`, `Send_Info`, and `Quick_Info` layers, a client-backed claim-deadline seam from `dateExpire_Parcel` instead of reusing that field as a fake receive time, a less command-shaped owner feedback path for empty receive and meso-focus states, a safer owner-focus handoff when the active parcel tab changes, and a closer arrival-notice summary path that preserves decoded parcel attachment state all the way through resolved item names and quantities. The remaining divergence is narrowed to the unrecovered `PARCEL` fixed-body field map beyond the now-modeled state bits or expiry, final launch ownership around `UI_Open`, the last exact native attach-picker/modal widget behavior beyond this inventory-owner pick seam, and the remaining memo-vs-parcel ownership details rather than the older summary-only compose surface, stale hidden-field compose focus, unflagged packet rows, meso or item-state collapse in the post-body byte, expiry masquerading as delivery time, expired rows staying claimable, command-only attachment staging, first-item fallback click staging, a missing parcel regression file, or the simulator-authored fee-info panel chrome that now yields to the authored `Delivery/*_Info` layers. | parcel dialog owner and packet-shaped parcel session seam (`MemoMailboxWindow.cs`, `MemoMailboxManager.cs`, `MemoGetWindow.cs`, `UIWindowLoader.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `UIWindowManager.cs`, `PacketOwnedParcelPacketCodec.cs`, `PacketOwnedSocialUtilityDialogDispatcher.cs`, `UnitTest_MapSimulator/PacketOwnedParcelParityTests.cs`, `CParcelDlg::OnCreate`, `CParcelDlg::Draw`, `CParcelDlg::OnButtonClicked`, `CParcelDlg::OnPacket`, `PARCEL::Decode`, `CTabReceive::SetParcel`, `CUserLocal::OnOpenUI`, `CWvsContext::UI_Open`) |
| Partial | Skill macro parity | The macro window now enforces trimmed macro names against a client-shaped 12-byte budget instead of a flat 12-character ASCII subset, validates names inline against the current Windows ANSI locale, now actually renders a live byte counter in the editor, routes committed character input through the simulator `GameWindow.TextInput` path so locale and IME text commits no longer depend on the old synthetic ASCII key mapping, accepts clipboard paste as a best-effort locale text insert that fills the remaining byte budget instead of rejecting an otherwise valid prefix wholesale, owns a real cursor-aware name edit field with mouse caret placement plus `Backspace`/`Delete`/arrow/`Home`/`End` behavior instead of the old append-only buffer, now advances that caret and applies delete or backspace on grapheme-text-element boundaries so composed IME text and surrogate-pair glyphs are edited as a single unit instead of being torn apart mid-sequence, mirrors `CUIMacroSysEx`/`CCtrlEdit` ownership more closely by routing Win32 IME preedit composition into inline macro-name preview text with a blinking caret and composition underline before committed `TextInput` lands, and clamps that preedit preview to the same locale- and byte-valid insertable prefix the committed path would accept so the visible preview and live byte counter stay aligned with the eventual insert result. This pass tightens the remaining physical-key and paste path closer to the client's constrained macro surface by enforcing a best-effort blacklist seam on top of the code-page gate: macro names now reject dash and connector punctuation beyond the client-visible soft-keyboard `-`/`_` surface instead of still admitting the wider Unicode punctuation categories. It also loads the client `UIWindow.img/SoftKeyboard` chrome as an attached helper with client-shaped key-grid hit-testing from `CUISoftKeyboard::GetKeyIndexFromPoint`, hover feedback, min/max/close controls, caps or shift toggles, backspace or enter actions, and now feeds the client-derived `CUISoftKeyboard::UpdateKeyConstraint` mode split from the same locale-shaped byte count the edit field enforces before the existing byte-budget or locale-aware insertability gate decides whether a key can advertise as clickable. The recovered WZ `SoftKeyboard/key/36` dash or underscore slot is now surfaced through that same macro-owner seam too: the attached helper no longer drops the final `-`/`_` key from its visible key walk or mouse hit-testing, so the client-visible punctuation key can actually render and insert through the existing locale-aware gate instead of remaining defined in layout data but unreachable. The owner still supports drag-binding selected macros into QuickSlot entries, routes macro hotkeys back through `SkillManager` with party-chat announce semantics, paces queued casts so the next macro skill waits for the current cast to finish, and persists the five-slot macro definition list per active player or selected login-roster character under the simulator AppData profile across restarts instead of losing macro definitions with the session. This pass also extends the Win32 IME bridge past raw preedit text: the simulator now captures composition clause and cursor metadata plus the active candidate list, then renders a client-shaped candidate popup beside the macro-name edit field with page-sized rows, page-local numbered entries, selection highlight, below-or-above caret placement, and clause-aware horizontal anchoring derived from `CCtrlEdit::CreateIMECandWnd` instead of leaving candidate UI entirely to the host OS; the popup sizing now measures the visible candidate page rather than the nominal page size, so later pages stay aligned with the recovered `CreateIMECandWnd` numbering and sizing walk. The candidate popup is now also owned by the macro window's mouse-interaction seam instead of being a draw-only overlay: it participates in hit-testing, keeps edit focus when clicked, routes page-local candidate clicks back through Win32 IMM `NI_SELECTCANDIDATESTR` against the active candidate-list index rather than letting those clicks blur or fall through into the underlying window stack, and now draws candidate text through the simulator's existing client-text rasterizer seam with a distinct selected-row font so the owner no longer falls back to the generic XNA font family while the recovered `CCtrlEdit` path clearly carries dedicated candidate fonts. This run narrows more of the `CreateIMECandWnd` metric gap with direct client evidence from `0x4e2100`: vertical candidate pages that cross the recovered `266px` width threshold now keep the client overflow branch's extra `(fontHeight + 7)` width padding instead of collapsing back to a flat viewport-width popup, the owner now preserves the native above-owner fallback Y without re-clamping it back on-screen after the post-size placement branch runs, page-local candidate ordinals are now measured and drawn through the same client `StringPool 0x1A15` `%d` format that `CCtrlEdit::CreateIMECandWnd` uses instead of an ad hoc dotted label, and the clause-anchor X shift now follows the client branch for clause-owned candidate placement even when the active IME reports a non-vertical candidate owner instead of only applying that adjustment to vertical popup layouts. Focused `UnitTest_MapSimulator/SkillMacroImeCandidateWindowLayoutTests.cs` coverage now locks the recovered wide-page width padding, the no-reclamp above-owner fallback placement, the reachable `SoftKeyboard/key/36` dash or underscore hit-test lane, and the best-effort punctuation blacklist seam so those client-backed helper branches stop drifting. The remaining gap is the unrecovered per-locale or per-IME blacklist matrix beyond this best-effort filter, plus the last locale-specific `CCtrlEdit` candidate-owner metrics and ownership quirks across IMEs beyond this closer numbering and placement match. | Macro support now behaves like a real runtime surface instead of an editor-only stub, and the remaining gap is narrowed to the unrecovered locale or IME-specific filter matrix plus final candidate-owner metric polish rather than missing interaction ownership, committed locale input, byte-count enforcement, the rendered live counter, preview-clamped preedit rendering, text-element-safe editing, the soft-keyboard constraint-mode split, visible-page candidate sizing, client-string-pool candidate numbering, candidate-popup ownership, candidate-font selection, or the recovered wide-page overflow, no-reclamp above-owner fallback, and clause-anchor placement branches themselves. The simulator-drawn candidate owner now also keeps mouse ownership, can forward page-local clicks back into the active IME candidate list instead of acting like a passive overlay that lets clicks fall through or blur the edit, uses client-leaning candidate text rasterization rather than the old generic sprite-font draw path, keeps the soft-keyboard constraint split aligned with the same multibyte byte budget that governs actual macro-name insertion instead of only the raw character count, now actually exposes the visible `-`/`_` soft-key slot that the WZ layout and macro-name blacklist seam already imply, preserves the native above-owner candidate fallback instead of forcing that branch back on-screen, and now follows the recovered `CreateIMECandWnd` numbering and clause-anchor branches more closely instead of drifting to a local dotted-label or vertical-only placement interpretation. | `SkillMacroUI.cs`, `SkillMacroSoftKeyboardLayout.cs`, `SkillMacroNameRules.cs`, `SkillMacroImeCandidateWindowLayout.cs`, `SkillManager.cs`, `QuickSlotUI.cs`, `MapSimulator.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `WindowsImeCompositionMonitor.cs`, `WindowsImeCandidateSelectionBridge.cs`, `ImeInputState.cs`, `UnitTest_MapSimulator/SkillMacroImeCandidateWindowLayoutTests.cs` (`CUIMacroSysEx`, `CCtrlEdit::OnIMEComp`, `CCtrlEdit::CreateIMECandWnd`, `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::UpdateKeyConstraint`, `UIWindow.img/SoftKeyboard/key/36`) |
| Partial | Soft keyboard and constrained text-entry parity | The simulator now has a dedicated WZ-backed soft-keyboard owner loaded from `UIWindow.img/SoftKeyboard`, uses the client-derived `CUISoftKeyboard::GetKeyIndexFromPoint` grid geometry for hover and click hit-testing, routes hover state through a top-level UI manager seam so the key surface owns mouse focus ahead of underlying windows, and keeps mouse ownership modal while the keyboard is open so clicks no longer fall through into the underlying dialog. Its per-mode enable gating still matches the recovered four-way `CUISoftKeyboard::UpdateKeyConstraint` matrix (`0` alphanumeric-until-full, `1` alpha-edge/numeric-middle, `2` numeric-until-full, `3` numeric-until-full-alt`) instead of only a loose alpha-vs-numeric split. The constrained map-transfer target field still attaches as a numeric-only edit surface, but it now also routes physical-key text through the same committed-text plus IME composition seam the simulator already uses elsewhere instead of relying on raw digit polling, keeps an owner-local composition preview while focused, and no longer drops edit focus when the modal soft keyboard closes so the underlying client-shaped edit lane keeps ownership for follow-up physical-key input. The shared helper still owns the login title ID/PW fields through the existing login-title owner seam, and `LoginUtilityDialogWindow` still carries explicit per-prompt keyboard typing across both PIC and non-PIC utility flows: PIC prompts still use the `CPinCodeDlg::OnKey` `type 3` numeric-only-alt gate and honor the client `CCtrlEdit` 8-character cap, while account-migration acceptance still mirrors the recovered `CLoginUtilDlg::OnKey` `type 2` path with a masked 8-digit birth-date edit surface and the same numeric-only mode gate for both soft-keyboard and physical-key input. Physical-key routing for the modeled login-owned, anti-macro, trunk-owned, and map-transfer-owned utility edit surfaces therefore still goes through the same committed-text and IME composition seam, so those owners render in-place composition previews, own candidate-list presentation while focused, and clear that transient state when the active owner hides or the edit focus changes instead of leaving locale input to raw per-key polling. Earlier passes already closed `SkillMacroUI`, `AdminShopWishListUI`, `WorldMapUI`, `BookCollectionWindow`, and `CashTradingRoomWindow` through the shared host seam; this pass keeps the remaining modeled compose owner on that same path instead of leaving it on physical-key-only edit logic: `MemoMailboxWindow` advertises the `UIWindow2.img/Delivery` send and quick-send recipient, memo-body, and meso lanes through `ISoftKeyboardHost`, anchors the shared modal keyboard to the active parcel edit bounds, keeps modal close from dropping the focused parcel edit lane, preserves numeric-only gating for meso entry, sends soft-keyboard submit through the same draft-dispatch seam as the owner send button, and still falls back to an owner-local IME candidate overlay when the active IME does not expose usable native candidate-window placement data. The mailbox owner also still wires its IME placement bridge to the native simulator window handle so monitored `ImeCandidateWindowForm` updates actually feed the parcel compose surface, continues to fall back to the active parcel caret or multiline compose row instead of pinning candidate pages to the left edge of the entire field when native candidate-window data is missing, and continues to honor the recovered `CParcelDlg::OnCreate` edit caps in the shared seam by constraining the target field to the client `CCtrlEdit` `nHorzMax = 12` lane and the memo body to the client `CCtrlMLEdit` `nHorzMax = 100` lane instead of leaving those compose surfaces effectively unbounded. The modeled login-title, login-utility, anti-macro, map-transfer, trunk, skill-macro, admin-shop wish-list, world-map, Monster Book, cash-trade, and parcel-delivery edit owners now also prefer the native Windows IME candidate window whenever the active IME exposes usable `ImeCandidateWindowForm` placement data, so those owners stop drawing or mouse-owning the simulator candidate overlay in the native case and fall back to the existing client-shaped owner-local overlay only when the IME does not expose usable native placement hints. The remaining gap is now narrowed to the last post-close native candidate-window placement quirks after modal dialog close rather than native candidate-window chrome or ownership during the active edit session. | This is now a separate parity target from generic macro-name validation because it affects how locale input, constrained text fields, and client-owned edit controls behave before characters are committed. The simulator now has the on-screen key surface, hover ownership, modal mouse capture, a client-shaped four-type constraint matrix, shared-host attachment across the modeled login, anti-macro, trunk, map-transfer, skill-macro, admin-shop wish-list, world-map, Monster Book, cash-trade, and parcel-delivery edit owners, IME-aware committed-text ownership in those edit surfaces instead of only raw key polling, preserved focus return for the modeled map-transfer and parcel-delivery owners when the modal key surface closes, native candidate-window preference when Windows exposes usable candidate-form placement data, fallback owner-local candidate overlays when an IME does not, and a closer `CDialog::DoModal`-style request lifecycle for the modeled utility owners instead of keeping the soft keyboard permanently active whenever those fields exist. The remaining divergence is now narrowed to unrecovered post-close native candidate-window placement details rather than helper absence, click-through behavior, unconstrained PIC length, raw map-transfer polling, simulator-owned candidate chrome during active input, the old macro-local soft keyboard, or missing compose/message owners living outside the shared host seam. | utility-window text-entry layer, login-owned edit bridges, anti-macro edit bridge, map-transfer edit bridge, trunk prompt edit bridge, skill-macro name edit bridge, cash-service wish-list search bridge, world-map search bridge, Monster Book search bridge, cash-trade chat bridge, parcel-delivery compose bridge, shared soft-keyboard owner (`CUIMacroSysEx::OnCreate`, `CUIAdminShopWishList::OnCreate`, `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::OnMouseMove`, `CUISoftKeyboard::HitTest`, `CUISoftKeyboard::UpdateKeyConstraint`, `CLoginUtilDlg::OnKey`, `CPinCodeDlg::OnKey`, `CUIAntiMacro::OnCreate`, `CTrunkDlg::OnCreate`, `CCashTradingRoomDlg` chat-entry owner, `CParcelDlg::OnCreate`, `CParcelDlg::OnKey`, `UIWindow2.img/WorldMap/WorldMapSearch`, `UIWindow2.img/Delivery`, simulator seams: `MemoMailboxWindow.ResolveImeWindowHandle`, `WindowsImeCompositionMonitor`, native candidate-window preference with owner-local fallback placement) |
| Partial | Repair-durability utility parity | MapSimulator now registers a dedicated `RepairDurability` owner backed by `UIWindow2.img/Repair`, routes packet `OnOpenUIWithOption` launches for `uiId == 33` into that owner instead of stopping at the dispatcher seam, preserves the client-derived option value as the repair NPC template id, blocks the launch when another unique-modeless utility owner is already visible, and lists damaged repair targets with per-item durability, selection, scroll, WZ-backed row chrome, and dedicated `Repair` / `Repair All` buttons. The owner still uses the client-derived repair-cost formula shape recovered from `CItemInfo::EQUIPITEM::CalcRepairDurabilityPay`, but it now also replaces the text-only NPC header with a live animated NPC preview built from the WZ NPC loader, routes repair clicks through a simulator-side send/result queue so the owner shows a pending request state before meso consumption and durability restoration are applied on the response path instead of immediately on button press, mirrors the recovered `SendRepairDurability` / `SendRepairDurabilityAll` wire shape more closely by carrying the client opcodes (`131` single, `130` all), and now also pushes those exact outbound opcode/payload shapes through the existing local-utility official-session bridge or generic packet outbox before falling back to the simulator-owned delayed response when no injected result arrives. The repair result seam remains narrower than before as well: the owner can consume an explicit local-utility `RepairDurabilityResult(1025)` override payload while a request is pending, so a bridged or manually injected repair result can accept or reject the queued single/all request before the delayed local fallback path runs, with optional rejection reason codes preserved on the simulator status message rather than every failure collapsing to the same local branch. It now also follows the recovered `SetNPC` seam more closely by pinning the preview to the client-observed layer anchor (`57`, `92`), disabling generic idle-action cycling for the preview, and preferring the NPC template's WZ shop-action family (`info/shop` id, then explicit `shop` / `say` / `speak`, then other loaded action roots that actually expose a WZ `speak` child, then `stand*`) before falling back to `stand`. This pass closes more of the `SetItems` gap from IDA as well: the simulator no longer has to approximate equipped `nPOS` values only from coarse visible slots. It still appends damaged equip-inventory entries first and then damaged currently equipped entries the way the client decompile shows, keeps the proven `-1..-59` shape for those equipped rows, and still resolves delayed single-item repair responses back against either the live inventory slot or the live equipped part instead of only equipped-part object references. The category/body-part-backed encode path remains primary for equipped rows, but the shared repair/Vega encoder now really does fall back to the older legacy slot table for unresolved repairable equip slots instead of silently dropping them when avatar-look body-part recovery cannot prove a value. The owner still exposes damaged hidden-under-cash equipped items by resolving the live underlying repairable part for each equipped slot before generating the row list, and still applies delayed `Repair All` responses against the queued request set instead of re-sweeping every damaged item currently visible when the response lands. It also keeps the recovered scrollbar lane at (`212`, `109`, `203`) live through click, wheel, and thumb-drag interaction, and aligns the fee-text draw anchor more closely to the client-observed `DrawMoney(..., 214)` / y `81` seam instead of keeping a looser simulator-only right edge. The tooltip path remains WZ-backed and closer to the client as well: the repair owner uses the same five-row hit geometry and icon-strip hover rule the client decompile shows (`GetItemIndexFromPoint`, `rx < 46`), anchors the hover-owned tooltip from the recovered `ShowItemToolTip(x, y + 20, ...)` seam instead of an extra simulator-side X offset, loads the full `UIWindow2.img/ToolTip/Equip/*` label set so the hovered repair target can render WZ-backed equip stat, requirement, category, mesos, and star chrome instead of falling back to text-only labels, resolves the hovered sheet through a client-style hover candidate order (below-right, below-left, above-right, above-left with the recovered cursor gap), consumes the already loaded `Can` / `Cannot` and `GrowthEnabled` / `GrowthDisabled` digit strips for requirement and growth values instead of falling back to plain text digits, surfaces item-growth `itemLEV` / `itemEXP` rows when the hovered equip actually has WZ growth metadata, and renders the required-job badge strip (`beginner`, `warrior`, `magician`, `bowman`, `thief`, `pirate`) from the same tooltip art family the shared equip owners use. Focused `UnitTest_MapSimulator` coverage now actually lives beside this seam in `RepairDurabilityClientParityTests.cs`, covering repair request payload encoding, synthetic result-payload decode, NPC repair-action fallback order, hover-tooltip placement, the five-row `GetItemIndexFromPoint` geometry helper, and the newly restored legacy equipped-position fallback so those recovered helpers stop living only as doc claims. The remaining gap is narrower and now concentrated in the seams the simulator still cannot prove exactly: any truly rare equipped-array categories outside the item/body-part mappings plus the restored legacy fallback, the last `CActionMan::LoadNpcAction(..., -2)` action-set-index details for repair NPCs whose shop presentation is not recoverable from the loaded WZ action roots alone, the last native `CUIToolTip::ShowItemToolTip` sheet-composition and frame-origin details beyond the now-closer hover anchor plus hover-autoposition lane, and the true official inbound repair-result opcode/body plus its exact server-authored success/failure payload layout and localized rejection text rather than an explicit simulator-side result override. | Repair-durability is now a packet-owned utility surface with dedicated art, owner lifetime, option routing, repair math, a `SetNPC`-shaped animated NPC layer that stays pinned to a shop-facing action instead of random idle cycling, a client-shaped icon-hover tooltip lane for repair targets, and a request/response-shaped repair flow whose send opcode and single-slot encoding are much closer to the client. This row now also matches its own claimed encode seam better: damaged equip-inventory entries still lead damaged equipped entries, hidden-under-cash repairable equips are still surfaced through the underlying live part, queued `Repair All` results still apply back onto the original requested set, and unresolved equipped-slot encodes no longer vanish when avatar-look body-part recovery misses because the shared repair encoder now falls back to the older legacy slot table instead of dropping that request path entirely. The repair helper behavior is narrower and less speculative now too because the targeted regression file really guards the request payload, synthetic result decode, NPC action fallback, hover-placement lane, row-hit helper, and legacy-slot fallback. The remaining divergence is concentrated in the last unrecovered equipped-array edge cases, the native action-manager `-2` action-set details, the remaining unproven parts of `CUIToolTip` frame-origin or sheet-composition behavior, and the still-unrecovered official repair-result packet body rather than missing owner behavior, missing inventory-source repair paths, a dead icon-hover seam, a draw-only scrollbar lane, a doc-only fallback slot path, or a fully simulator-isolated request transport. | utility-window owner launched from local packet dispatch (`CUserLocal::OnOpenUIWithOption`, `CRepairDurabilityDlg`, `CItemInfo::EQUIPITEM::CalcRepairDurabilityPay`, `CRepairDurabilityDlg::SetNPC`, `CRepairDurabilityDlg::SetItems`, `CRepairDurabilityDlg::OnCreate`, `CRepairDurabilityDlg::OnMouseMove`, `CRepairDurabilityDlg::GetItemIndexFromPoint`, `CRepairDurabilityDlg::DrawMoney`, `CRepairDurabilityDlg::SendRepairDurability`, `CRepairDurabilityDlg::SendRepairDurabilityAll`, `AvatarLook::Decode`, `CActionMan::LoadNpcAction`, `HaCreator/MapSimulator/RepairDurabilityClientParity.cs`, `UnitTest_MapSimulator/RepairDurabilityClientParityTests.cs`) |
| Partial | Pickup notice parity | The simulator pickup-notice stack now renders item icons, resolves cached item names and type labels for success messages, records player pickup history, reconnects manual pickup failures through `PlayerCombat` so ownership and inventory-capacity checks consistently raise notices before the drop is consumed, mirrors the client `-3` style split through a shared screen/chat formatter, restores the client `-2` split by leaving inventory-full as a screen-only notice, routes meso success notices through the same formatter so pet meso pickups surface as a chat-log side effect instead of polluting the screen string, and aligns regular plus quest-item success wording more closely with `CWvsContext::OnDropPickUpMessage` by using the same item-type label path rather than pet-prefixed or generic quest text. It still applies a pet-specific validator backed by WZ `item/Consume/0200.img/02002058/spec/notPickupByPet` so local auto-loot can surface pet-blocked items with item-aware pet messaging without spamming repeated failures, resolves `Unavailable` failures against `DropPool` recent-pickup history so pet-, player-, mob-, and generic remote-preemption cases surface actor-aware pickup lines instead of collapsing to one fallback string, wires `DropPool` remote player and remote pet pickup callbacks into the same paired screen/chat notice seam instead of limiting live remote-loot surfacing to the mob callback alone, now preserves recorded remote-pet actor names when those recent-pickup records are replayed through `Unavailable` notices, lets live `RemoteUserActorPool` field actors feed that same seam through bounded remote-player plus avatar-look pet pickup producers instead of requiring surfaced remote loot to be manually injected, and now also exposes `DropPool`'s existing `DropPickupActorKind.Other` resolve path through a live callback so packet-authored or other non-avatar remote pickups can reuse the same notice formatter instead of only landing in recent-pickup history. This run also pins the client `format_string` continuation marker through the shared `MapleStoryStringPool` seam (`StringPool[0x08B8] = ".."`) and keeps the long-name shaping path on the same recovered `FONT_BASIC_WHITE`/`120` or `96` width budget that `CWvsContext::OnDropPickUpMessage` uses before formatting `5442`/`5443`, rather than leaving that continuation marker to whatever the generated table currently resolves. The existing synthetic remote-user packet pickup producer on the `RemoteUserPacketCodec` / `MapSimulator.RemoteUsers` seam, the corrected true drop-leave packet producer on the `RemoteDropPacketRuntime` / `DropPool.ApplyPacketLeave` seam for remote pet actor attribution, the bounded fallback label for unnamed `DropPickupActorKind.Other` history, the client-consistent white screen text for quest/failure branches, the unresolved-item success suppression, and the decompiled name-only success gate when the item-type label is blank all remain in place. Focused `UnitTest_MapSimulator/PickupNoticeParityTests.cs` coverage now guards the client `-2`/`-3` screen-chat split, the unresolved-item name gate, the decompiled name-only success gate when item-type text is missing, and the client long-name continuation-marker plus width-budget shaping path. The remaining gap is narrowed to exact official wording for simulator-owned failure or remote-preemption messages whose backing `StringPool` text is still not captured in-source, any still-missing pickup producers outside the currently wired synthetic remote-user and packet drop-leave paths, and deeper client ownership gates that may still suppress or reshape those notices | Pickup feedback now matches the verified client branch shape for `0`, `1`, `2`, `-2`, `-3`, and the default fallback more closely, with success-path screen/chat splits, inventory-full behavior, WZ-backed pet restrictions, recent remote-pet actor replay, live remote-player plus avatar-look remote-pet pickup surfacing, a generalized live `Other`-actor remote pickup seam aligned to the decompiled handler and existing runtime seams, a synthetic remote-user pickup replay hook, a corrected true drop-leave packet path for remote pet actor attribution, a bounded fallback label for unnamed `Other`-actor pickup history, client-consistent white screen text for quest/failure branches, client-consistent suppression of unresolved success notices when the item name is missing, the decompiled name-only success gate when the item-type label is blank, and the recovered long-name `format_string` continuation-marker budget that shapes item names before `5442`/`5443`. The remaining divergence is concentrated in uncaptured official strings, any pickup producers beyond the currently wired packet seams, and client ownership gates that should still suppress or reshape those notices end to end | `PickupNoticeUI.cs`, pickup formatting/runtime (`PickupNoticeTextFormatter.cs`, `MapSimulator.cs`, `MapSimulator.RemoteUsers.cs`, `MapSimulator.RemoteDropPackets.cs`, `RemoteUserPacketCodec.cs`, `RemoteDropPacketCodec.cs`, `DropPool.cs`, `MapleStoryStringPool.cs`, `UnitTest_MapSimulator/PickupNoticeParityTests.cs`) |
| Partial | Cursor and utility-button parity | The simulator now builds dedicated WZ-backed `Menu` and `System` popup owners from `StatusBar2.img/mainBar/Menu` and `StatusBar2.img/mainBar/System`, routes the status-bar `BtMenu` and `BtSystem` buttons through those owners, fans their core entries back into inventory/equip/stat/skill/quest/character-info and channel-selection flows instead of leaving them as dead launchers, switches the cursor to the client hold art during active skill, quick-slot, and window drag flows, loads the client forbidden-hand and busy-search cursor families from `Basic.img/Cursor/13` and `Basic.img/Cursor/16`, and now dismisses those popup owners on any click outside their modeless bounds through the shared window-manager input path instead of only inside the popup shell. It also now registers WZ-backed `KeyConfig` and `OptionMenu` owners from `UIWindow2.img/KeyConfig` and `UIWindow2.img/OptionMenu`, routes `BtKeySetting`, `BtGameOption`, `BtSystemOption`, `BtOption`, and `BtJoyPad` into those owners instead of forbidden stubs, binds key settings back to live `PlayerInput`, shows the attached pad binding beside each keyboard row in `KeyConfig`, constrains captured joypad remaps for those utility owners to the shared non-directional button family instead of letting `BtJoyPad` or `KeyConfig` steal left-stick or D-pad movement inputs, preserves the exact WZ launcher path (`StatusBar2.img/mainBar/System/BtKeySetting`, `BtGameOption`, `BtSystemOption`, `BtOption`, `BtJoyPad`) on the owning `KeyConfig` / `OptionMenu` status surface instead of flattening those launches into one generic popup source, and now lets the `KeyConfig` owner seed focus to the first client row on open, move that focus across the WZ-backed main and quick-slot pages through keyboard or joypad navigation, arm capture from the focused row, clear or restore staged bindings in-owner, and commit or discard through the owner instead of forcing mouse-only row selection. The `BtJoyPad` owner also now prefers connected controller slots when stepping ports in either direction, renders live stick and trigger calibration meters inside the `UIWindow2.img/OptionMenu` owner header instead of only text readouts, supports the existing staged dead-zone, per-axis dead-zone, trigger-threshold, curve, inversion, reset, and shared utility-binding rows through mouse, keyboard, and joypad navigation, lets left/right or D-pad adjustments drive the currently selected calibration row, lets the selected binding row cycle staged shared-button candidates or arm live capture through `A`/space without stealing directional movement inputs, seeds selection to the first client-style row when the owner opens, and now tracks staged-dirty calibration or binding rows directly inside that owner instead of leaving the whole page visually flat until commit. The same `BtJoyPad` owner also now mirrors more of the client branch choreography: reset-defaults, restore-live-profile, and close-time discard all move through explicit in-owner confirm states, block further row edits while pending, and let `BtOK` / `BtCancle` plus `Start` / `Enter` or `Back` / `Escape` confirm or cancel the pending branch instead of bypassing it. `BtRank` and `BtEvent` also now land on WZ-backed ranking/event owners that render live simulator-backed ranking cards plus filtered event rows from login bootstrap, packet-owned field or overlay state, quest-guide or delivery routing, tracked quest alerts, and active special-field runtimes instead of placeholder shells, and those launches now pass through the recorded progression-owner seam with the exact `StatusBar2.img/mainBar/Menu/BtRank` / `BtEvent` source preserved through the loading, `NavigateUrl`, and event auto-dismiss lifecycle text instead of a generic status-bar popup label. The `CUIEventAlarm` owner path now also accepts keyboard navigation on top of the existing WZ-backed filter and calendar buttons: filter tabs can be switched from the keyboard, list selection can page without the mouse, the authored calendar can toggle and move its selected day from the keyboard while still respecting the WZ month shells, and `Escape` can dismiss the visible owner without waiting on the close button. Packet/runtime event branches now also reuse that same recorded `CUIEventAlarm` owner seam instead of waiting on `BtEvent`: notice, chat, buff-zone, AP/SP helper, tutor, quest-guide, delivery, class-competition, radio, event/minigame sound, and logout-gift updates can auto-surface the existing owner with a packet-authored launch source while their text remains on the shared event snapshot instead of dying in chat-only feedback. `BtGameQuit` also no longer dies in a chat notice; it now routes into a dedicated in-field confirm owner registered separately from the login utility dialog, built from the same WZ `FadeYesNo/backgrnd7` plus `BtOK` / `BtCancel` seam, keeps the recovered quit prompt text inside that compact owner, and preserves the exact `StatusBar2.img/mainBar/System/BtGameQuit` launcher context on the owner footer instead of dropping back through the login-only utility surface. The remaining gap is narrower: the client's full joypad calibration workflow is still not mirrored exactly because the simulator still uses a compact staged row model instead of the client's full dedicated joypad control set and still does not reproduce every native joypad branch beyond the recovered reset, restore, and discard confirm flow, while true packet-fed ranking ladders, attendance or calendar event payloads, broader event-owner hotkey or pad semantics beyond the recovered in-owner navigation, and the remaining remote ranking/event data ownership behind the client utility dispatch are still not mirrored exactly. | This is part of the client feel layer and affects almost every window or interaction surface once the rest of the UI becomes more complete. The row now covers client-shaped utility owners with live simulator-backed payloads for joypad, ranking, and event flows, keeps the recovered `Bt*` launcher identity attached to those owners instead of flattening them into a generic popup source, keeps staged joypad edits visible and branch-confirmed inside the shared owner, lets `KeyConfig` and `CUIEventAlarm` stay operable from the owner itself instead of only through mouse clicks, auto-surfaces the same event owner from packet/runtime activity instead of only status-bar clicks, and moves the quit branch off the login utility dialog seam onto its own in-field confirm owner. The remaining gap is narrowed to the unrecovered full client joypad owner controls and remaining branch logic plus official network-fed ranking/event data and final dispatch fidelity rather than dead owners, placeholder content, read-only utility surfaces, anonymous owner launches, event traffic that only updates chat, or a login-scene quit dialog reuse. | `MouseCursorItem.cs`, `StatusBarUI.cs`, `UILoader.cs`, `StatusBarPopupMenuWindow.cs`, `PlayerInput.cs`, `OptionMenuWindow.cs`, `KeyConfigWindow.cs`, `EventWindow.cs`, `UIWindowManager.cs`, broader UI manager layer (`CUIStatusBar::OnButtonClicked`) |
| Partial | Explicit ranking/event owner parity | `BtRank` and `BtEvent` now route into explicit simulator owners instead of generic utility shells. The ranking surface still stays simulator-local, but it now reflects the client-observed `CWebWnd` landing shape more directly: `UIWindow2.img/Ranking` remains the owner art seam, the snapshot still seeds a web-style landing request around the active world and character id before showing local `World Rank`, `Job Rank`, `Popularity`, and combat context, the IDA follow-up is wired through the actual `CUIRanking::OnCreate` request format by pairing `get_server_string_0` with the extracted `StringPool[0xAA2]` format string `http://%s.nexon.com/maplestory/page/Gnxgame.aspx?URL=webclient/totpersonrank&worldid=%d&characterid=%d`, and the owner now reports that request in a more client-shaped split between the resolved `NavigateUrl`, the recovered host seed, and the explicit `worldid` / `characterid` request payload instead of only flattening everything into one simulator note. The dedicated ranking owner also no longer freezes on its first snapshot once opened: it refreshes the loading-to-navigated lifecycle live while visible and starts the `UIWindow.img/Ranking/Loading/*` animation from the owner launch tick instead of an arbitrary global phase, honoring authored per-frame `delay` values when present and otherwise staying on the existing simulator fallback cadence. The event surface also moved closer to the real `CUIEventAlarm` owner instead of only using the main-row shell: `UIWindow2.img/EventList` now loads the WZ-backed row `slot` and `icon` family plus the authored `calendar/bg/{0,1}/backgrnd*`, `today`, and `number/{normal,select}` assets, the calendar initializes around the nearest scheduled entry instead of always resetting to the current month, month changes now keep the selected day anchored to the nearest authored entry inside that month instead of dropping to day 1 whenever possible, the day render keeps the authored `today` overlay separate from the authored selected-number family instead of tinting both states the same way, the owner now renders a dedicated packet-authored alarm strip near the top of the window so recent notice, chat, buff-zone, AP/SP, tutor, radio, follow-failure, and logout-gift text can live in a tighter client-style timed feed instead of only as list rows, the top strip stays blank when no packet/runtime text is active instead of filling that region with a simulator-only placeholder sentence, the fallback close control now uses the recovered `CUIEventAlarm::OnCreate` anchor `(m_width - 22, 10)` instead of the older generic utility-button inset, and the 8-second owner timeout only disarms on the recovered WZ-backed filter/calendar interactions so list-row selection no longer pins the owner open indefinitely. Same-day event ordering now explicitly prefers active packet/runtime rows over idle bootstrap or placeholder summaries before falling back to packet/source recency, and the calendar summary still surfaces the selected day's leading entries plus overflow count. The IDA follow-up still defines the client owners through `CUIRanking::OnButtonClicked` at `0x83c6a0`, `CUIRanking::Update` at `0x83c690`, plus `CUIEventAlarm::OnCreate` at `0x7ac4f0`, `CUIEventAlarm::Draw` at `0x7aad90`, and `CUIEventAlarm::Update` at `0x7aac00`. The remaining gap is narrower: the simulator still does not have the remote page payloads or live server-fed ladder content behind `CUIRanking`, the true packet-owned attendance/calendar text construction that populates `CUIEventAlarm::m_aCT`, or the attendance/calendar network event payloads that should eventually replace the remaining simulator-authored event-list fallback rows once those packets are modeled. | Status-bar parity here is not only about buttons launching something. The client treats ranking and event views as separate utility owners with their own create/update/draw lifecycle, so tracking them explicitly prevents this area from looking "done" once the generic popup routing exists while the owner-specific ranking/event behavior is still only approximated. | status-bar launched utility-window owners (`CUIRanking`, `CUIEventAlarm`, `CUIStatusBar::OnButtonClicked`) |

### 2. Local utility and event packet parity

This area is still underrepresented by the existing window rows because the client keeps a packet-owned dispatch seam in front of several of those owners.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned local utility/event dispatch parity | `MapSimulator.PacketOwnedUtilityParity.cs` remains the concrete local-utility bridge for the `CUserLocal::OnPacket`-owned utility/event shapes instead of synthetic aliases. It still accepts raw/pseudo packet payloads for `OnOpenUI`, `OnOpenUIWithOption`, `OnGoToCommoditySN`, `OnOpenClassCompetitionPage`, `OnQuestGuideResult`, `OnDeliveryQuest`, `OnNoticeMsg`, `OnChatMsg`, `OnBuffzoneEffect`, `OnPlayEventSound`, `OnPlayMinigameSound`, `OnHireTutor`, `OnTutorMsg`, `OnAskAPSPEvent`, `OnFollowCharacter`, `OnFollowCharacterFailed`, `OnRadioSchedule`, and `OnSkillCooltimeSet`, while overlay-owned `OnFieldFadeInOut`, `OnBalloonMsg`, `OnDamageMeter`, and `OnNotifyHPDecByField` remain routed through their packet overlay owner. Client-backed `CWvsContext::UI_Open` ids with simulator owners still route through this bridge, and `OnOpenUIWithOption` `uiId == 7` still follows the recovered `UI_Close(7)` plus `UI_Toggle(7, option)` path by targeting the simulator `SocialList` tabs. `OnNoticeMsg` and `OnChatMsg` still decode client-shaped packet payloads, with `OnNoticeMsg` using the simulator modal utility-dialog owner and mirroring the WZ-backed `Sound/UI.img/DlgNotice` cue. `OnPlayEventSound` / `OnPlayMinigameSound` still prefer `Field.img` / `MiniGame.img`. `OnSkillCooltimeSet` still maps the recovered `0x004FAE6F` vehicle sentinel to `5221006` Battleship, now with the cooldown HUD/ready notice path plus the narrowed Battleship durability presentation side effect pinned through the recovered `get_max_durability_of_vehicle(5221006, slv, charLevel)` formula (`300 * level + 500 * (slv - 72)`). The radio path keeps the WZ-confirmed `ui/UIWindow.img/Radio/On/0..4` and `ui/UIWindow.img/Radio/Off/0` art family (`54x32`, `delay=150`) and the IDA-confirmed `CUIRadio::CreateLayer(int bLeft)` placement rule (`nMargin = bLeft ? 40 : 0`, `Origin_RT`, `RelMove(-3-width-nMargin, 3)`, resident `Off/0`, alpha-origin reset `(0,0)`, stopped resident animation). The existing `RadioStatusWindow` owner keeps `Off/0` resident, cross-fades the animated `On/*` overlay complementarily through the recovered 100 ms fade seam, starts animation from `On/0` relative to the packet-owned session start, honors the packet-authored `timeValue` playback offset and overrun rejection, latches the packet/minimap `bLeft` branch, and now has source regression coverage for the client `DecodeStr + Decode4 timeValue` radio schedule body, placement, alpha handoff, session-relative animation, authored playback window, and the recovered Battleship formula. The tutor path still creates and maintains a real packet-owned local summon through the summoned-pool seam for `OnHireTutor`, drives visible indexed and text messages from `OnTutorMsg`, sources summon height from WZ-backed tutor skill data with client fallback ids, uses `UI/ChatBalloon.img/tutorial`, centers indexed cues above the tutor actor by summon height, and uses the recovered `nWidth / 2 + 10` text-bubble arrow rule with the WZ-confirmed tutorial extents (`19/19/21/19`, arrow `21x27`). The remaining gap is narrower: the Battleship sentinel branch still stops at the simulator durability/cooldown presentation instead of every downstream `CTemporaryStatView::UpdatePassively` side effect, tutor presentation still uses a simulator-owned summon and tutorial balloon instead of a byte-for-byte `CTutor` plus exact `CSummoned::Init` choreography, notice/event-sound presentation still relies on simulator modal/UI and WZ sound playback seams instead of the exact `CUtilDlg` lifecycle or native sound-engine behavior, and radio still lacks byte-for-byte COM layer allocation/parenting plus raw `AIL_quick_*` handle ownership beyond the now-correct string ids, art family, placement math, mute fade, session-relative animation, playback offset, minimap branch, update cadence, and complementary alpha handoff. | This row now covers the actual packet entry points, owned window routing, chat/notice/audio packet decoding, modal notice ownership, cooldown HUD/ready-notice surfacing, Battleship sentinel durability presentation formula, tutor summon/text/indexed-cue owner, WZ-backed tutorial balloon extents, radio schedule client-payload decoding, radio `CreateLayer` placement helper, radio session-relative frame phase, packet-authored radio playback window, and helper-level regression coverage in `UnitTest_MapSimulator/PacketOwnedRadioTutorUtilityParityTests.cs`. Remaining risk is concentrated in low-level native/lifecycle fidelity: full temporary-stat side effects, exact `CTutor`/`CSummoned` construction, exact `CUtilDlg` object lifetime, native sound-engine handles, and COM radio-layer allocation/parenting. | local-user packet/runtime bridge (`CUserLocal::OnPacket`, `CUserLocal::OnOpenUI`, `CUserLocal::OnOpenUIWithOption`, `CUserLocal::OnGoToCommoditySN`, `CUserLocal::OnFieldFadeInOut`, `CUserLocal::OnBalloonMsg`, `CUserLocal::OnDamageMeter`) |

### 3. Packet-authored local overlays and guidance parity

This slice is narrower than the full dispatcher row above and is still missing as its own planning surface even though the handlers have concrete client-owned behavior.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field fade and world/actor balloon parity | MapSimulator now keeps this row on direct overlay commands plus the broader local-utility packet dispatcher instead of a row-specific packet loopback: `MapSimulator.PacketOwnedLocalOverlayParity.cs` no longer owns a dedicated `LocalOverlayPacketInboxManager`, `/localoverlay` has been trimmed back to direct fade or balloon control, and packet-shaped `fade` or `balloon` payloads continue to enter only through the existing `MapSimulator.PacketOwnedUtilityParity.cs` utility-owner seam that already mirrors `CUserLocal::OnPacket`. WZ was rechecked again in the active extracted data source: `UI/Login.img/WorldNotice/Balloon` still carries the shared nine-slice body (`nw/ne/sw/se/n/s/w/e/c`), the recovered short and long tail set (`arrow`, `slArrow`, `se/sw/nw/neArrow`, `sel/swl/nwl/nelArrow`), and `clr = -16777216`, so the balloon owner still stays anchored to real client art instead of simulator-authored chrome. The field-fade path now matches the recovered `CUserLocal::OnFieldFadeInOut` and `CAnimationDisplayer::RegisterFadeInOutAnimation` shape more closely: the fourth decoded `Int32` is still treated as the start alpha that drives the black full-screen fade, but the local runtime now resolves the requested fade layer from the active avatar's composed `GetLayerZ()` equivalent before applying the same `- 2` offset the client uses, and `PacketFieldFadeOverlay` now runs on one implicit elapsed-time timeline instead of carrying a separate armed fade-out flag or cached total-duration state. Balloon rendering still follows the tighter `CUserLocal::OnBalloonMsg`, `CUserLocal::ShowBallonMsg`, `CBalloonMsg::Init`, `make_balloon`, and `UI/Login.img/WorldNotice/Balloon` evidence from the previous pass: avatar-attached packets still replace the single avatar balloon while field/world packets append into a separate field list, long tokens still wrap instead of overflowing, leading and trailing authored blank lines are still preserved, and this pass closes a narrower `CTextAnalyzer` mismatch inside that same seam by no longer collapsing whitespace-only authored payloads before wrapping and by resolving `#j` through the active build/job fallback path instead of leaving the existing job-name formatting context unused. Each authored message still caches a client-shaped body canvas up front while final canvases stay cached per arrow variant, avatar-attached state still snapshots the same `0, -15 - CAvatar::GetHeight()` arrival offset, the text layout still centers inside the packet width `nWidth` while keeping the body at `nWidth + 20`, and field/world balloons still switch to the recovered top-mounted `nw/ne/nwl/nelArrow` variants when clamp pressure forces the body below the authored anchor. `LoadUiArrowSprite` still samples opaque rows from the WZ arrow canvases to recover top and bottom mount points, and the draw path still uses those recovered mount points to place centered tails and derive body separation without hardcoded overlap ratios. Client evidence from `CUserLocal::CBalloonMsg::Init` still shows the deeper text owner going through `CTextAnalyzer`, `CT_INFO` line-change records, and the client font table built around `StringPool[0x1A25]`, so this row now follows more of that path without claiming the full analyzer stack. `UnitTest_MapSimulator/PacketOwnedLocalOverlayParityTests.cs` now locks the flattened fade timeline plus the avatar-replace and field-append balloon ownership split so the row does not drift back toward the older dispatcher seam. The remaining gap is narrower again: the simulator still approximates the client's exact COM-layer fade owner and `m_Fade.m_lList` lifecycle inside `RegisterFadeInOutAnimation`, still only supports a focused subset of `CTextAnalyzer` controls instead of the full token/font/icon pipeline and native font-table selection, and still does not reproduce the full COM-owned balloon attachment and layer ownership path end to end even though packet ingress ownership, fade layer selection, spawn-time avatar offset capture, packet-width placement, whitespace-preserving text normalization, job-token resolution, and clamped field-arrow selection now follow the recovered client flow more closely. | These are visible event-owned overlays that sit between local gameplay and many later scripted or utility flows, and the simulator now keeps the row on the same split the client implies: direct local owner controls for manual exercising and the broader packet-owned utility dispatcher for actual packet ingress. The remaining risk is concentrated in the final COM-backed animation lifecycle, the deeper native `CTextAnalyzer` font/icon stack, and COM-owned balloon attachment ownership rather than in a duplicate local inbox, missing multiple field balloons, missing the avatar-derived fade layer source, missing whitespace-only packet preservation, the `#j` token falling back to simulator text, or the centered-tail placement being driven by hardcoded offsets instead of WZ-backed mount points. | packet-owned overlay layer (`CUserLocal::OnFieldFadeInOut`, `CAnimationDisplayer::RegisterFadeInOutAnimation`, `CUserLocal::OnBalloonMsg`, `CUserLocal::ShowBallonMsg`, `CUserLocal::CBalloonMsg::Init`, `make_balloon`) |
| Partial | Damage-meter and field-hazard notice parity | MapSimulator still carries this row through the same packet-owned utility dispatcher seam the client uses: the local utility inbox and direct packet injection path accept the real `CUserLocal::OnPacket` handler ids for this row (`243` for `OnNotifyHPDecByField`, `267` for `OnDamageMeter`) alongside the existing aliases, and both route back into the dedicated WZ-backed HUD/notice owner built from `Basic.img/Notice2`, `Basic.img/Notice3`, and `StatusBar2.img/mainBar/notice`. WZ was rechecked again in the active extracted data source during this pass: `Basic.img/Notice2` still carries `t/c/s` canvases at `258x40/258x20/258x47`, `Basic.img/Notice3` still carries `t/c/s` canvases at `266x21/266x20/266x55`, and `StatusBar2.img/mainBar/notice` still measures `27x21`, so the hazard and damage-meter owners continue sizing themselves from the real canvases instead of simulator-authored panel guesses. IDA was also rechecked for this row: `CUserLocal::OnPacket` still dispatches `243` to `CUserLocal::OnNotifyHPDecByField` and `267` to `CUserLocal::OnDamageMeter`, `OnDamageMeter` still decodes one `Int32`, calls `CDamageMeter::SetTimer`, and resets the shared `CWvsContext` timing fields, `OnNotifyHPDecByField` still decodes one `Int32` and calls `TryConsumePetHP(damage, 0, 0)`, `TryConsumePetHP` still gates non-forced use on the configured low-HP percentage, the first HP-enabled pet branch, a configured pet consume item, a concrete USE-slot lookup, the `StringPool[0x0D89]` no-potion status-bar chat line, and `CWvsContext::SendStatChangeItemUseRequestByPetQ`, and that send path still emits opcode `203` with `petSerial[8]`, buff-skill byte, `update_time`, slot, and item id after the exclusive-request and `200ms` request-index gate. This pass keeps the earlier first-enabled-pet configuration rule intact and closes one more 1026 ownership mismatch around the recovered result seam: when a field-hazard auto-consume request is pending, decoded opcode `1026` payloads now stay on the dedicated pet-consume result owner even if their slot, item id, or request-index targeting later fails validation, so the simulator rejects mismatched targeting inside the pet-consume seam instead of falling through into the unrelated quest-reward `1026` owner. The earlier packet-owned lifecycle split also remains intact: hazard notice badges keep explicit deferred (`QUEUE`) and dispatched (`SENT`) follow-up states in addition to pending, throttled, acknowledged, consumed, and failed badges; external-observed follow-up text preserves the actual packet-owned stage; deferred requests that later leave the queue promote into dispatched; remote-observed requests that expire without a terminal result keep deferred or dispatched ownership unless an explicit acknowledged result already arrived; and the dedicated `1026` pet-consume result path still revalidates slot/item/request-index targeting before applying deferred, dispatched, acknowledged, consumed, failed, and no-HP-potion outcomes. On the pet side, the existing per-pet runtime seam still owns enablement and optional preferred-potion overrides, keeps a simulated shared auto-HP item selection alive across notices, records the concrete slot that would back `SendStatChangeItemUseRequestByPetQ`, validates that same slot again when the synthetic or packet-owned follow-up window closes, and still uses the recovered no-potion chat string rather than a simulator-authored paraphrase. `UnitTest_MapSimulator/PacketOwnedLocalOverlayParityTests.cs` now exists in-source and locks the row’s real packet ids and aliases (`243`, `267`, `1026`), the `1026` decode path, the dedicated-vs-targeted `1026` routing split, Maple-string detail decoding, malformed detail-length rejection, and the strict slot/item/request-index targeting gate so this row does not drift back into quest-reward fallback ownership or a flattened result parser. The remaining gap is now narrower and concentrated in lifecycle that still belongs to the live client/server pair: the simulator still does not know the official server packet body for pet-use results, still does not mirror the true live `SendStatChangeItemUseRequestByPetQ` result ordering end to end, still cannot mirror the exact status-bar/chat timing beyond the recovered no-potion chat string and the improved simulator-owned dispatch/result windows, and still cannot confirm the live server-side semantics of the threaded forced/buff-skill variants beyond simulator-owned request shaping, queue ownership, result shaping, and status ownership. | This row now keeps more of the real packet-owned lifecycle and the recovered first-pet gate visible instead of flattening everything after send time, letting later pet configurations leak into the hazard path, or misclassifying decoded `1026` pet-consume results as quest-reward owner sync when their targeting does not match the pending hazard request. The simulator now has row-specific regression coverage for the places most likely to drift in this seam: real packet-id ingress, `1026` decode ownership, strict slot/item/request-index revalidation, and malformed Maple-string rejection. The remaining risk is concentrated in the still-unrecovered official result packet body and live server timing or ordering around `TryConsumePetHP`, not in missing dispatch-state visibility, missing zero-threshold disablement, missing deferred/live HUD differentiation, missing outbound packet shaping, missing slot attribution, missing first-enabled pet gating, missing variant threading, or the `1026` seam flattening every decoded pending-result payload back into quest-reward ownership. | packet-owned HUD/notice layer (`CUserLocal::OnDamageMeter`, `CDamageMeter::SetTimer`, `CUserLocal::OnNotifyHPDecByField`, `CUserLocal::TryConsumePetHP`, `CWvsContext::SendStatChangeItemUseRequestByPetQ`) |
| Partial | Quest-guide, delivery-quest, and class-competition launch parity | MapSimulator still carries this row through the raw packet-owned local utility inbox and decoder seam instead of command-only stubs: the simulator drains `CUserLocal::OnPacket`-shaped `274` quest-guide, `275` delivery-quest, `250` class-competition launch, and the follow-up `291` class-competition auth-cache packet through the same packet-owned dispatcher that the manual launch helpers and live session bridge use. WZ was rechecked again in the active extracted data source for this pass: `UI/UIWindow2.img/UtilDlgEx/notice` still measures `312x132`, `UI/UIWindow2.img/UtilDlgEx/QDeliveryIcon` still exposes seven frames with `delay = 150`, `UI/UIWindow.img/classMatch/backgrnd` still measures `312x389`, and `UI/UIWindow.img/classMatch/Loading/0-4` still exposes five `69x34` frames, so the packet-owned delivery and class-competition owners continue to sit on the same authored chrome the client uses. Packet-authored quest-guide demand state still rebuilds world-map mob targets into `WorldMapUI`, still falls back to demand-item query routing when the payload is item-authored instead of mob-authored, and this pass fixes the item-authored map-resolution seam so those demand-item rows now derive map coverage from the current packet's mob targets through the simulator quest/world-map mob index instead of mistakenly consulting the already-cleared packet mob cache. `QuestDelivery` still uses that authored `UtilDlgEx` `notice` frame plus `QDeliveryIcon` art, and its list still mirrors the client `CUIQuestDelivery::LoadData` series/show-quest model more closely than the earlier quest-log shortcut. This pass keeps the tighter packet decoder and launch gating in the same owner: `275` delivery-quest packets still accept the earlier count-prefixed `Int32` blacklist support plus the count-prefixed `Int32 count + UInt16 quest ids` blacklist shape that `CQuestMan::DecodeDisallowedDeliveryList` can plausibly surface, still reject leftover widths outside `0`/`1`/`2`/`4` for the optional accept/complete discriminator, and now have direct parity coverage in `UnitTest_MapSimulator/PacketOwnedLocalOverlayParityTests.cs`; `250` class-competition launch packets still stay on the same empty-body seam the client uses instead of ignoring stray payload bytes; and opcode `291` auth caches still accept Maple-string payloads in addition to the earlier raw or length-prefixed ASCII/Unicode fallbacks, also with direct parity coverage in that test file. IDA was rechecked for `CUserLocal::OnDeliveryQuest`: after decoding `questId`, `itemId`, and the disallowed-delivery list, the client still gates both the status-bar fallback and `CUIQuestDelivery` construction on `CItemInfo::GetItemInfo(itemId)` succeeding first, so the simulator still keeps the delivery owner closed when the packet-authored item cannot be resolved through the client item-info path instead of opening an impossible unknown-item overlay. The client-side worthy gating from `CUIQuestDelivery::IsWorthlessQuest` still suppresses overleveled open-ended accept rows with the recovered `minLevel + 10` and open-end-date rule, still preserves the explicitly selected packet quest, and still keeps continuation rows visible when a series already has a completed prefix. The selected delivery entry still resolves the live delivery cash-item inventory slot, exposes the client-facing slot index in the owner, and routes the quest-detail/local-delivery handoff through slot-aware inventory consumption before opening a single-quest created-NPC-style interaction seam. The packet-owned delivery OK path still prefers a synthetic preview NPC for the target template and opens only the requested quest against that NPC, which remains closer to `CUIQuestDelivery::OnButtonClicked` creating a `CNpc`, setting accept/complete-only quest state, and calling `CUserLocal::TalkToNpc`. The packet-driven `ClassCompetition` owner still uses the authored `UIWindow2.img/classMatch` `backgrnd`, `Loading`, and `BtOK` assets, still follows `get_server_string_0`, `StringPool[0x11DC]`, and `CClassCompetition::Update` by recovering `gamerank.maplestory`, formatting the same `NavigateUrl` template for `Event/classbattle/gameview&key=%s`, waiting for an initial auth cache before first navigation, and preserving the already navigated page on later relaunches or 180-second refreshes while real packet-authored `291` updates refresh the cached auth token without forcing a reload. The owner still surfaces summarized cached auth-token state and a seeded nearby-rank ladder preview so the singleton reads more like a cached `CWebWnd` page while auth remains local-only. The remaining gap is that `QuestDelivery` still approximates the client server-created `CNpc` quest list and real `TalkToNpc` packet/runtime with simulator quest definitions plus an overlay, while the class-competition page still lacks the true server auth key, remote `CWebWnd` page payloads, and live ladder content. | These are not generic variants of quest log or world map browsing. They are packet-authored launch surfaces for guided quest routing and delivery/class-competition utility flows, and the simulator now has the real packet-owned bridge, client packet ids, WZ-backed delivery chrome, current-packet item-authored quest-guide map resolution through the quest/world-map mob index, client-shaped worthy-quest suppression for open-ended overleveled accepts, representative-series delivery rows, slot-aware delivery item handoff, a created-NPC-style quest-only interaction seam, the client item-info gate ahead of delivery-owner launch, an exact `250` empty-payload class-competition launch seam, a broader but width-checked `275` decoder that stays in the same ownership path across counted `Int32` and counted `UInt16` blacklist variants, and a closer auth-pending/auth-issued/navigated class-competition singleton whose cached-page lifecycle can ingest opcode `291` auth caches from Maple-string as well as raw/length-prefixed payloads without resetting an already navigated page. The remaining risk is now narrowed to the deeper client-owned `CUserLocal::TalkToNpc` or server-created `CNpc` delivery runtime and the still-missing remote `CClassCompetition` web/auth payload model rather than to missing raw dispatch, stale item-authored map resolution, missing packet-authored auth-cache ingress, missing counted blacklist coverage, missing item-info gating, missing exact launch-body validation, owner absence, or direct-list action shortcuts. | packet-owned guidance/utility launch layer (`CUserLocal::OnQuestGuideResult`, `CUserLocal::OnDeliveryQuest`, `CUIQuestDelivery`, `CUserLocal::OnOpenClassCompetitionPage`) |

Follow-up update for this row: the status remains `Partial`. The active WZ source still confirms the delivery `notice` frame at `312x132`, seven `QDeliveryIcon` frames at `delay = 150`, the class-match `backgrnd` at `312x389`, and five `69x34` `Loading` frames with no authored delay in this data source. The local utility inbox now explicitly accepts numeric `291`, `classcompetitionauth`-style aliases, and framed raw opcode `291` payloads, so packet-authored class-competition auth-cache ingress reaches the same Maple-string/raw/length-prefixed auth decoder as direct commands and the live bridge. New parity coverage in `UnitTest_MapSimulator/PacketOwnedLocalOverlayParityTests.cs` locks the `291` inbox/framed ingress, Maple-string auth token decode, exact empty-body `250` launch validation, and count-prefixed `UInt16` delivery blacklist plus discriminator decode. The remaining gap is still the deeper delivery runtime and remote web content: `QuestDelivery` approximates the client server-created `CNpc` quest list and `CUserLocal::TalkToNpc` packet/runtime with simulator quest definitions plus an overlay, while class competition still lacks the true server auth key, remote `CWebWnd` page payloads, and live ladder content.

### 4. External cash-service stage parity

This area is currently unowned by the backlog set even though the client keeps Cash Shop and ITC as dedicated stage owners rather than utility-window variants.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Cash Shop and ITC stage ownership parity | `HaCreator/MapSimulator` still keeps this row inside the dedicated service-stage seams instead of collapsing it back into `AdminShopDialogUI`, and the stage-open/close BGM override remains tied to WZ evidence from `sound/BgmUI.img/ShopBgm` so entering either `CCashShop` or `CITC` overrides field BGM and leaving both families restores the map track. `CashServiceStageWindow` now covers more of the client-owned `384` / `412` runtime from `CCashShop::OnCashItemResult`, `CITC::OnNormalItemResult`, and `CITC::OnLoadWishSaleListDone`: Cash Shop purchase-record packets now persist per-SN packet-owned state back onto packet-authored wishlist rows; the stage now also keeps packet-owned pane labels plus packet-row snapshots for gift inbox count ownership, buy or locker mutations, coupon registration, package purchase/gift, name-change, transfer-world, and gachapon result families instead of leaving those paths as status text only; and ITC no longer stops at coarse list summaries because the stage owns decoded wish-sale rows plus client-shaped list mutations for cancel-sale, move-purchase-to-inventory, set/delete/register wish, buy-item/buy-wish, and cancel-wish notification flows. `MapSimulator.CashServiceStageParity.cs` now feeds those packet-owned stage rows back into the child owners, so `CashShopList` can fall back to packet-owned gift/package/coupon/name-change/transfer/gachapon or inventory-result rows when the shared Cash Shop shell has no richer catalog snapshot, `CCSWnd_Status/BtCoupon` now reports the stage-owned coupon summary instead of an unconditional unimplemented placeholder, the status owner surfaces the newer packet-owned coupon/name-change/transfer/gachapon summaries directly, `ItcList` can still fall back to decoded wish-sale rows when the shared `Mts` shell has neither a live catalog snapshot nor packet-owned main-list rows, and the existing `ItcSale` / `ItcPurchase` surfaces still reflect the packet-owned wish runtime instead of only a generic status string. The remaining gap is narrower but still real: the simulator still relies on `AdminShopDialogUI` for the richer non-wishlist Cash Shop catalog/runtime path, `GW_GiftList` is now surfaced as packet-owned row count ownership but still not decoded field-for-field, the simulator still does not decode full `GW_CashItemInfo` bodies end-to-end for every cash result family, and it still does not run a wholly separate client-faithful render/runtime path for every Cash Shop and ITC child owner. | Treating cash services as only a status-bar launch plus an in-map admin-shop shell hides a large client-owned stage transition before the player returns to field gameplay. Tracking the service-stage owners explicitly keeps future parity work anchored to the real client lifecycle for Cash Shop and ITC instead of overloading `CAdminShopDlg`, `OnGoToCommoditySN`, or generic utility-window rows with stage-owned behavior they do not actually own. | external cash-service stage layer (`CCashShop::Init`, `CCashShop::OnPacket`, `CCashShop::OnCashItemResult`, `CITC::Init`, `CITC::OnPacket`, `CITC::OnNormalItemResult`) |

### 5. Cash-service child-window and preview parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Cash avatar-preview and service-subwindow parity | The simulator now keeps the client-owned cash-service surface split into the same named seams the targeted IDA pass recovered instead of collapsing them back into one coarse shop owner. `CashShopStage` is registered as the top-level stage owner, `CashAvatarPreviewWindow` remains the dedicated `CCSWnd_Char`-style preview owner from `CashShop.img/Base/Preview` and `CashShop.img/CSChar`, and the cash shop now hides the whole owner family when the parent stage closes, including the dedicated `CashTradingRoomWindow` child owner instead of leaving it orphaned after `CCSWnd_Status/BtExit` or parent-stage close. The narrower stage child owners from the scan also now exist as first-class owners: `CCSWnd_Locker`, `CCSWnd_Inventory`, `CCSWnd_List`, `CCSWnd_Status`, and `CCSWnd_OneADay` are each registered separately, shown with the cash-shop family, and seeded from the recovered WZ control seams (`CashShop.img/CSLocker`, `CashShop.img/CSInventory`, `CashShop.img/CSList/Base`, `CashShop.img/CSStatus`, and `CashShop.img/PicturePlate/*`) instead of staying implicit inside the parent cash-shop summary. This pass pushed those owners closer to the client-specific `OnCreate` state too: the locker owner now surfaces the recovered selector/scrollbar and number-font seam (`wheelRange 208`, number-font present) and owns keyboard plus mouse selector stepping against the shared-character list instead of staying a passive text snapshot; the inventory owner now follows the narrower client seam from WZ plus IDA more closely because `CashShop.img/CSInventory` still only exposes the four recovered tab buttons plus `BtExTrunk`, the child owner now cycles only that four-tab strip instead of inventing a fifth tab, still routes `BtExTrunk` back into the dedicated locker owner, and now publishes live per-tab inventory rows from the shared runtime so the owner-local row focus/scrollbar state is no longer only a coarse count summary; and the list owner no longer only mirrors parent text because the dedicated `CCSWnd_List` surface now routes `BtBuy`, `BtGift`, `BtReserve`, and `BtRemove` back through `AdminShopDialogUI` while also driving selected-row focus, mouse-wheel row movement, pane toggling, and `PageUp` / `PageDown` / `Home` / `End` keyboard movement directly against the parent cash list state. `CCSWnd_Char` also moved closer to the recovered owner lifecycle from the targeted IDA read: it now keeps an explicit avatar-look snapshot, tracks a client-shaped `WEAR_INFO`-style item array, surfaces the recovered `CUserPreview` plus `CWvsPhysicalSpace2D` setup as owner-local state (`24,40 212x165` preview region, field/ladder-rope physical-space setup), refreshes that state through default-avatar, wear, takeoff, pet, and weapon-sticker mutations instead of only swapping the simulator render result, lets the dedicated owner trigger the recovered buy/default/takeoff button family through keyboard paths in addition to the WZ button seams, restores the preview to the base avatar when the selected cash row is a non-avatar-affecting service action instead of leaving the last avatar mutation stuck on screen, and now lets `OnTakeoffAvatar` unwind staged pet previews back to the avatar-look pet snapshot instead of only removing equip or weapon-sticker mutations. `CCashTradingRoomDlg` is likewise no longer routed through the anonymous social-room shell: the simulator instantiates a dedicated `CashTradingRoomWindow` from `UIWindow2.img/CashTradingRoom`, binds the client button family (`BtTrade`, `BtReset`, `BtCoin`, `BtClame`, `BtEnter`), resets its local session when shown so the init-money snapshot is refreshed from the current wallet, keeps a separate session-wallet snapshot instead of only rereading the outside wallet, and now exposes more of the recovered dedicated chat-entry / scrollbar / font-owner seam directly in the owner state, including wheel, page, home/end, track-click, and drag-thumb chat scrolling, staged draft cycling on the dedicated edit-control runtime, and a narrower staged remote-owner trade lifecycle where remote lock and final acceptance advance after the local owner instead of mirroring instantly. `CCSWnd_OneADay` also moved closer to the recovered `OnCreate` object graph from the targeted IDA read: the owner now publishes the recovered selector seed (`SetSelectorStart(2,2,1,1)` with two StringPool-backed selector labels), keeps the recovered key-focus / plate / plate-big / number-font seams explicit in owner state, and no longer draws the whole picture-plate family at once because the WZ-backed surface now switches between `NoItem`, `NoItem0`, `NoItem1`, and `ShortcutHelp` according to the dedicated owner-local selector and help state instead of leaving every plate implicitly stacked. The remaining gap is now narrower and concentrated in the still-deeper client-specific runtime that the simulator does not yet own: `CCSWnd_Char` still renders through the simulator assembler instead of the client's actual `CUserPreview` object and physical-space runtime; the locker, inventory, and One-a-Day owners still approximate the client selector, scrollbar, number-font, and reward/session machines instead of reproducing the full packet-fed object state, tab/control plumbing, and packet-authored countdown or reward history byte for byte; and `CCashTradingRoomDlg` still does not run on the client's real `CCtrlEdit`, scrollbar, font-object, and packet-fed trade/session implementation even though the dedicated owner shell, scroll ownership, and staged local-versus-remote lifecycle are now explicit. | Cash-service parity is not only about entering the right stage. The client also depends on a second layer of named owners to make the stage usable and to preview avatar-affecting cash items correctly. Tracking those owners separately keeps future work anchored to the real preview, locker, inventory, list, status, One-a-Day, and cash-trading seams instead of treating them as anonymous controls inside `CCashShop::Init` or folding them back into field-side UI rows. | cash-service child-window layer (`CCSWnd_Char::OnCreate`, `CCSWnd_Locker::OnCreate`, `CCSWnd_Inventory::OnCreate`, `CCSWnd_List::OnCreate`, `CCSWnd_Status::OnCreate`, `CCSWnd_OneADay::OnCreate`, `CCashTradingRoomDlg::OnCreate`) |

### 6. Cash-service wishlist parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Cash-service wishlist dialog parity | The simulator now registers a dedicated `AdminShopWishListUI` owner instead of keeping wish-list handling inside the base `AdminShopDialogUI` modal path, using the WZ-backed `CashShop.img/CSItemSearch/PopUp` frame plus the `BtAllItem`, `BtSearch`, `BtCancel`, `BtBuy`, `PopUp/BtComboBox`, `CSItemSearch/Price/*`, `PopUp1/backgrnd`, and `PopUp1/Scroll/normal/*` control family as the current asset seam for the split owner. `BtRecharge` on the admin-shop dialog still opens that separate owner behind the existing yes/no confirmation, `AdminShopWishListUI` still focuses its local search lane on show, mirrors the client-observed `CUIAdminShopWishList::{OnButtonClicked,OnKey}` split by routing `0x7D0` into a dedicated add-on toggle, trimming and submitting the search string through a separate search path, preserving owner-local keyboard handling on the focused edit, and closing through its own owner-local close button, and it now also drives the WZ-backed combo seam into the `CSItemSearch/Price/*` ranges instead of hard-collapsing search to one implicit band. `SearchItemName` no longer stays folded into the parent owner either: the loader now registers a dedicated `AdminShopWishListSearchResultUI` child owner from the client-owned `UIWindow.img/itemSearch/resultback`, `UIWindow.img/itemSearch/BtLeft`, `UIWindow.img/itemSearch/BtRight`, `UIWindow.img/itemSearch/BtRegist`, `UIWindow.img/itemSearch/icon0`, and `UI/Basic.img/BtHide` seam recovered through `CUIAdminShopWishListSearchResult::{OnCreate,OnButtonClicked}`, `AdminShopWishListUI` now hands staged local results over to that separate owner when the seam is available, and the main owner suppresses its local keyboard lane while the child is visible instead of treating search results as an in-owner popup only. This pass tightened that child-owner seam again by moving the staged paging and selection state back into the main wishlist owner instead of leaving the result window to own a detached raw list copy: a fresh `SearchItemName` run now resets an owner-local result session, the dedicated result owner pages through that session via `BtLeft` and `BtRight`, row clicks and wheel/arrow navigation update the owner-held staged selection, and `BtRegist` now commits the owner-held staged row back into `AdminShopDialogUI` rather than acting on a child-local copy of the data. The child owner still stays pinned to the wishlist owner instead of behaving like an independent draggable surface, still shows the selected row's icon, category, seller, detail, and price summary, and still routes the chosen NPC offer back into the in-session wish-list state instead of always forcing the first result. This pass tightened the local session seam again by keying that staged search session to live NPC catalog entry keys instead of a detached result payload: the result owner now re-resolves each staged page against the current admin-shop rows before drawing, selection stays anchored to the live staged entry key, dropped catalog rows are pruned out of the local session, and editing the search string or changing the staged category / price band now invalidates the old local session instead of leaving stale results alive across filter changes. `SearchItemName` is also narrower and closer to the shipped client seam than before: the search path still loads the WZ-backed alias rows from `String/CashItemSearch.img`, normalizes those per-item search terms into the existing wishlist scorer, and lets exact, prefix, substring, and multi-token matches land through that shipped alias vocabulary instead of only the local title/detail/seller text, and it now keeps already-saved wish-list rows in the staged result owner with their live `AlreadyWishlisted` state instead of silently trimming them out before the `BtRegist`-disabled path can surface. `ToggleAddOn` no longer stays inside the parent owner either: the loader now registers a dedicated `AdminShopWishListCategoryUI` child owner from the same `PopUp1/backgrnd`, `PopUp1/BtCancel`, and `PopUp1/Scroll/normal/*` seam, `AdminShopWishListUI` now hands `0x7D0` over to that separate owner when the seam is available, the main owner suppresses its in-owner category popup and keyboard handling while the child is visible, and the child closes back through its own owner-local close or toggle path while preserving the staged category selection; follow-up IDA now keeps that split anchored to `CUIAdminShopWishListCategory::{OnCreate,OnButtonClicked}` instead of only inferring it from art/layout. This pass also moved the category text closer to the recovered client constructor: `AdminShopDialogUI` now publishes the category table through a client-shaped row matrix keyed to the exact `CUIAdminShopWishListCategory::OnCreate` StringPool id order (`634`, `637`, `639`, `640`, `649`, `642`, `641`, `644`, `643`, `651`, `652` plus child groups `653-656`, `6755`, `657-660`, `618-623`, and `624-633`) instead of a free-form simulator taxonomy, resolves those row labels through `MapleStoryStringPool` when the generated/overridden table carries the recovered client text, and only falls back to authored English labels when an id is still unresolved in the local string-pool seam. `AdminShopWishListCategoryUI` now behaves like a parent-owned add-on layer by staying pinned to the wishlist owner instead of acting like an independently draggable popup, and the add-on now treats row-button clicks like owner-local actions instead of keyboard-only staging by opening parent rows into their child stage, preserving the previously staged descendant when that branch is reopened, and committing leaf rows back to the wishlist owner on click or `Enter`. This pass also tightens the remaining packet-facing session seam without inventing server search payloads: the staged wishlist result session is now keyed to both the live NPC catalog signature and the packet-owned admin-shop service-state signature recovered through the existing `CAdminShopDlg::OnPacket` owner seam, so packet-owned open/result/reopen-wait transitions now invalidate stale local `SearchItemName` sessions even when the visible catalog rows do not change, and the dedicated result owner now surfaces that packet-owned service state summary in-window instead of leaving those transitions invisible behind only local row revalidation. The remaining gap is now narrower and still concentrated in truly packet-authored behavior: `SearchItemName` still does not receive true packet- or server-authored result rows, remote result metadata, or live service-side search/session flow, and the dedicated search-result owner still lacks reproduced packet-fed search result payloads, true service session ids, and the full server-driven state machine beyond the simulator's local live-entry plus packet-owner-state invalidation seam. | Cash-service parity is not only about catalog browsing and stage ownership. The client exposes a separate wish-list/search owner with its own text-entry, toggle, close lifecycle, price band, staged result selection surface, and category add-on tree, and the follow-up IDA pass confirms those search-result/category panes are distinct child owners rather than anonymous subpanes, so leaving that work folded into the base admin-shop row would hide a distinct client seam and make wish-list parity look more complete than it is. | cash-service wishlist owner (`CUIAdminShopWishList::OnCreate`, `CUIAdminShopWishList::OnButtonClicked`, `CUIAdminShopWishList::OnKey`, `CUIAdminShopWishList::HitTest`, `CUIAdminShopWishListSearchResult::OnCreate`, `CUIAdminShopWishListSearchResult::OnButtonClicked`, `CUIAdminShopWishListCategory::OnCreate`, `CUIAdminShopWishListCategory::OnButtonClicked`) |

### 7. Packet-authored radio-schedule parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Radio schedule and `CRadioManager` lifecycle parity | `MapSimulator.PacketOwnedUtilityParity.cs` now keeps more of the recovered `CRadioManager` owner behavior inside the existing radio seam instead of leaving `CreateLayer` on a simulator-only shortcut. `OnRadioSchedule` remains a distinct packet-authored runtime; re-entry is still blocked while a session is live; the simulator still resolves `StringPool[0x1501] = "Sound/Radio.img/%s"` before `StringPool[0x1502] = "Sound/Radio.img/%s/track"` so track-metadata lookup stays separate from playable sound lookup; stop still clears the live track/audio descriptors together with the playback state; and the playback clock now mirrors the recovered `CRadioManager::MMS_Play` / `CRadioManager::Update` timing more closely by treating `timeValue` as the initial playhead, deriving the expected stop from `ms_length - ms_position`, and only polling completion after the client-shaped `~2000 ms` post-end cadence. The exact radio string-pool bodies are now implemented instead of being left as unresolved ids: `MapleStoryStringPool` carries the recovered `StringPool[0x14CF] = "[%s]'s broadcasting will begin. Please turn up the volume."`, `StringPool[0x14D0] = "[%s]'s broadcasting has ended."`, `StringPool[0x1501] = "Sound/Radio.img/%s"`, and `StringPool[0x1502] = "Sound/Radio.img/%s/track"`, and `UnitTest_MapSimulator/PacketOwnedRadioParityTests.cs` now locks the playback-window math, expected-stop timing, offset rejection, the formatted start/path diagnostics, the `bLeft` resolution order, and the packet-owned `CWvsContext[3562]` mutation history so the radio owner strings, timing, and placement lifecycle do not drift again. WZ was rechecked again for the radio HUD seam in the active extracted data source: `UI/UIWindow.img/Radio/Off/0` still carries the `54x32` anchor canvas, while `UI/UIWindow.img/Radio/On/0-4` still carry the same `54x32` animated frames with `delay = 150` on each child. The dedicated `RadioStatusWindow` owner and tooltip diagnostics now expose the recovered `CUIRadio::CreateLayer` seam more faithfully by preferring the packet-owned `CWvsContext[3562]` override when present but otherwise falling back to the live minimap expanded-state branch for `bLeft`, still applying `nMargin = 40`, `Origin_RT`, and the concrete `UI/UIWindow.img/Radio/{On,Off}` art, and now freezing the active HUD to the session's captured `CreateLayer` side instead of re-evaluating minimap expansion every draw. `PacketOwnedLocalUtilityContextState` now also keeps a dedicated per-character radio slot lifecycle for `CWvsContext[3562]`, including runtime binding, character-transition reset, mutation sequence tracking, and last-mutation diagnostics so the active session can follow the recovered owner semantics more closely without scattering radio state outside the existing context seam. The remaining gap is now concentrated in the client-owned playback stack and true packet authorship: playback still terminates through the MonoGame/WZ backend instead of the client's `CRadioManager::MMS_Play` `IWzSound -> raw buffer -> AIL_quick_load_mem / AIL_quick_set_ms_position / AIL_quick_play` handle path, and `CWvsContext[3562]` is still simulator-owned context state rather than a real client slot fed by official packet mutation history or persisted end-to-end outside the simulator runtime. | Packet-owned radio scheduling is more than a sound trigger. It is a client-managed audio/UI/chat lifecycle that temporarily changes BGM ownership and presents user-facing state, so leaving it inside the generic dispatcher row makes the remaining gap look smaller and less coherent than it is. Tracking it explicitly keeps future work anchored to the right packet handler and manager lifecycle instead of folding radio playback into unrelated sound-effect or notice work. | packet-owned radio manager layer (`CUserLocal::OnRadioSchedule`, `CRadioManager::Play`, `CRadioManager::Update`, `CRadioManager::Stop`) |

### 8. Explicit key-config, game-option, and soft-keyboard owner parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Explicit key-config, game-option, and soft-keyboard owner parity | The simulator now carries this owner family more explicitly instead of only relying on generic utility launches. `KeyConfigWindow` still keeps the client-owned main vs quick-slot page split from `UIWindow2.img/KeyConfig`, stages edits until `OK`, preserves `Cancel`, and keeps the quick-slot settings page as a dedicated owner branch rather than a status-bar stub; this pass closes the previously unwired footer seam by actually connecting the window’s existing packet-owned shortcut visual provider to the live `FuncKeyMapped` translation path, so the selected-row owner footer and non-palette bindable rows can now surface live packet-owned `skill`, `item`, `cash item`, and `macro` ownership with the same live hotkey slot state already staged into the simulator input/runtime instead of stopping at text-only owner status. The WZ `KeyConfig/icon/*` footer order stays deterministic against the authored slot families (`0`-`32`, `50`-`54`, `100`-`106`), and the simulator rows that already have clear client-owned action icons (`Equip`, `Inventory`, `Char Stats`, `Mini Map`, `Quest Log`, `Pick Up`, `Attack`, `Jump`) still resolve to those recovered slot ids while packet-owned cast rows now fall through to their live shortcut visuals when no recovered action-palette slot exists. `OptionMenuWindow` remains aligned with the recovered `CUIGameOpt::OnCreate` roster and staging lifecycle: the simulator-side checkbox list already matches the recovered `1001`-`1014` client ids, y positions, and ordering, still snapshots current game/system/extra/joypad values on show, and still commits only on `OK` while `Cancel` drops the staged owner state. The soft-keyboard path also remains close to the explicit `CSoftKeyboardDlg` family instead of stopping at helper hit-testing: `SoftKeyboardUI` still loads the full per-key `SoftKeyboard/key/*` families plus `funckey/{capslock,lshift,rshift,enter,backspace}`, randomizes the numeric row per owner session and again when the numeric tab is reselected, keeps the recovered three-tab owner header and compact-vs-expanded layout, follows the switching-character branch by latching active T9 groups, cycling same-group presses through the client-style `1010`-`1018` / `1020`-`1028` letter families, clearing that state on tab or case changes, and expiring stale switching state when the owner text diverges or idles out. `UIWindowManager` still reasserts modal owner focus after handled soft-keyboard button clicks instead of only restoring that ownership when the keyboard closes or its host disappears, which keeps the simulator close to the `CSoftKeyboardDlg::OnButtonClicked` branch that repeatedly pushes focus back onto the active edit owner. The remaining gap is now tighter and specific: the simulator still does not reproduce the client’s full `CUIKeyConfig::DrawFuncKeyMapped` item/skill/action layering or the unrecovered palette-map table behind every shortcut cell, so palette ownership outside the already-recovered action-icon rows still falls back to explicit staged-owner presentation instead of byte-for-byte client palette ownership, and the soft-keyboard owner still approximates the deeper native `CCtrlEditEx` switching-timeout and edit-control stack rather than reproducing the complete modal edit-control sequence end to end. | This row now tracks real owner behavior rather than unnamed popup existence. Keeping it separate prevents the broader utility-button and constrained-text rows from overstating parity now that key-config, option, and soft-keyboard surfaces have their own staged owner state, page/layout splits, checkbox roster, WZ-backed palette/footer content keyed to recovered slot ids, live packet-owned shortcut visuals wired into the selected-action footer, randomized keypad state, explicit dialog buttons, per-click modal owner re-focus, and explicit close-time owner restoration. The remaining work is concentrated in the last client-specific palette-map linkage, full shortcut-cell draw layering, and full native edit-control sequencing rather than in missing owners or missing owner-local button lifecycle. | utility-owner layer (`CUIKeyConfig::OnCreate`, `CUIKeyConfig::Draw`, `CUIGameOpt::OnCreate`, `CUIMacroSysEx::OnCreate`, `CUISoftKeyboard::{GetKeyIndexFromPoint,OnMouseMove,HitTest,UpdateKeyConstraint}`, `CSoftKeyboardDlg::OnCreate`, `CSoftKeyboardDlg::OnButtonClicked`) |

### 9. Tutor, skill-guide, and AP/SP helper parity discovered by `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Tutor summon and message parity | The simulator now keeps tutor traffic on a dedicated packet-owned runtime and summoned-owner seam instead of burying it under generic notice/chat behavior: client packet `255`/`256` toggles a tutor actor through the existing summoned seam, resolves the Cygnus (`10001013`) vs Aran (`20001013`) tutor variant from the local job family, mirrors the WZ-backed summoned heights from `Skill/1000.img/skill/10001013/summon/height = 80` and `Skill/2000.img/skill/20001013/summon/height = 70`, renders string tutor messages with the WZ-authored `UI/ChatBalloon.img/tutorial` skin (`nw/ne/sw/se/n/s/w/e/c/arrow`), and explicitly tears down any prior tutor summon before re-hiring to match `CUserLocal::OnHireTutor`'s remove-then-init lifecycle. `CUserLocal::OnTutorMsg` parity is split by payload shape now: string payloads keep the tutor-text balloon path, indexed payloads render their `UI/tutorial.img/<index>` asset layer with the client-style half-width and `(layerHeight + summonHeight)` placement, and both payload shapes now trigger the tutor summon's `say` playback through the summoned seam, including the client `CTutor::OnMessage(long,long)` Aran exception where numeric cues skip `SetAttackAction(17)`. The packet-owned tutor runtime now mirrors the recovered static `CUserLocal::ms_anTutors` bookkeeping more closely in both slot order and richer state: hires append the active tutor skill id only if it is not already present, removals delete that skill id from the ordered slot list, local-character rebinds now clear only the runtime-local visible actor while preserving the shared active tutor snapshot instead of incorrectly downgrading the registry to inactive, and the simulator-side Cygnus/Aran variant snapshots now live on shared static tutor state alongside the client-style slot list so separate tutor runtimes can restore their visible actor from the shared active variant when they rebind to the same character. The shared tutor surface now carries message ownership through that same seam instead of stopping at actor visibility: active text/indexed `CTutor::OnMessage` state is snapshotted alongside the shared tutor variant, rebind restores pull the live message back into the runtime when they re-materialize the actor, and the summoned display/status/overlay path can consume that shared message snapshot directly so a tutor that stays active through shared-variant display also keeps its balloon or indexed cue plus `say` playback. This pass narrows the session-to-runtime gap further without leaving the existing seam: the local-utility official-session bridge now mirrors live inbound tutor opcodes `255` and `256` into the packet-owned tutor lane instead of silently skipping them, its status/packet labels expose those tutor opcodes explicitly, and `UnitTest_MapSimulator/TutorRuntimeParityTests.cs` now locks that live-session bridge coverage alongside the owner-aware tutor runtime behavior so captured local tutor packets can reach the same summon/message path end to end. The remaining gap is narrower: tutor traffic is still sourced through the single local utility bridge/session and runtime lane, so distinct non-local tutor packet streams are still not decoded end to end, and the simulator still models shared tutor state through variant/message snapshots rather than a fully separate client-faithful `CTutor` object graph per owner. | Leaving tutor flows buried inside the generic local-utility row hides a separate visible helper actor plus packet-authored message lifecycle that can break tutorial or guided-play fields in ways ordinary notice/chat parity will not catch. Tracking the tutor owner explicitly keeps future parity work anchored to the right packet handlers and actor lifecycle instead of treating tutoring as another dialog string. | packet-owned tutor/helper actor layer (`CUserLocal::OnHireTutor`, `CUserLocal::OnTutorMsg`, `CTutor::OnMessage`, summoned-style tutor initialization inside `CUserLocal::OnHireTutor`, local-utility official-session bridge for live opcode `255`/`256` mirroring) |
| Implemented | Packet-authored skill-guide and AP/SP helper prompt parity | The simulator now tracks the dedicated `CUserLocal::OnOpenSkillGuide` dispatch as client packet `262`, exposes a `skillguide` alias on the local utility inbox/command seam, packet-opens the skill owner through the existing `UI_Open(3, 1)` path that matches the IDA seam at `0x90e6a0`, and explicitly opens the current Aran guide page when the active job has one. `CUserLocal::OnAskAPSPEvent` is also modeled beyond plain text now: packet `273`/`1008` decodes the client-shaped two-Int32 payload, validates it against the recovered `CWvsContext+0x20B4` receive token from the IDA seam at `0x90f0e0`, opens the helper prompt with the exact recovered `StringPool::ms_aString[0x17BA]` copy from `MapleStory.exe`, uses `OK` and `Cancel` labels to match that copy, and simulates the accepted follow-up outpacket `195` with response `6` through a separate send-context token path mirroring the client `CWvsContext+0x2030` field split. The AP/SP receive/send tokens now persist under a broader simulator-side packet-owned local utility `CWvsContext` owner instead of an AP/SP-only seam: that owner initializes once for the active character, preserves detached receive/send divergence while that character remains active, and resets only when the simulator actually transitions to another local character so stale prompts and follow-up state do not leak across sessions. | These helper prompts sit between tutorial/script flow and ordinary utility windows. Keeping them explicit prevents the broader local-utility dispatcher row from looking complete once the simulator can only show text or open the guide manually, and it anchors future work to the packet-driven launch/confirm path the client actually uses. | packet-owned helper prompt layer (`CUserLocal::OnOpenSkillGuide`, `CUISkill::OpenCurSkillGuide`, `CUserLocal::OnAskAPSPEvent`) |

### 10. Explicit minimap owner parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Explicit minimap owner parity | `MinimapUI` now mirrors the concrete `CUIMiniMap` owner path more closely instead of only carrying broad simulator-side behavior: the loader consumes the client WZ `UIWindow2.img/MiniMap/MinMap`, `MaxMap`, `Min`, and `BtSmall` assets that were previously ignored, the minimap owner now switches between compact and expanded minimap options through its own button family, preserves the previous expanded option when `BtMin` collapses the window, restores that remembered option on `BtMax`, keeps the world-map launch routed directly into the dedicated `WorldMapUI` owner instead of the separate map-transfer window, and still performs a `CWorldMapDlg::CreateWorldMapDlg`-style preflight before showing the world map with the dedicated failure-notice branch tied to `StringPool[0x118]` when the current field cannot be presented by the simulator world-map owner. A targeted `CUIMiniMap::OnButtonClicked` and `CWvsContext::OnMiniMapOnOff` pass now tightens the explicit owner-state split further too: packet `89` (`CWvsContext::OnMiniMapOnOff`) now stays on this explicit minimap owner row instead of the backlog 7 field-utility row because the client stores `m_bMiniMapOnOff` on `CWvsContext` and immediately calls `CUIMiniMap::ReloadMiniMap`, and the state-button family now mirrors the recovered client ids instead of collapsing them into one simulator path. `BtMin` now follows the client `1000` route and collapses directly from both compact and expanded views while remembering whichever expanded option was active, `BtMax` now follows the distinct client `1001` restore path instead of reusing the `1000` handler, and `BtBig`/`BtSmall` stay on the shared `1003` option-button path that only flips between compact and expanded minimap layouts. This pass also tightens the collapsed owner chrome seam further instead of leaving it on the broader approximation: the collapsed strip's title reserve now explicitly budgets the single visible collapsed state-button lane (`BtMax`) plus `BtMap` rather than treating both state-button families as interchangeable, which matches the recovered owner-local lane model even if future WZ variants stop keeping `BtMin` and `BtMax` at the same width. The keyboard/runtime minimap toggle still routes through the currently visible client state-button family instead of keeping the older three-state shortcut, so expanded or compact views use the recovered `1000` collapse path and collapsed views use the recovered `1001` restore path with the remembered expanded option intact. The collapsed frame still uses the dedicated WZ `MiniMap/Min` chrome instead of reusing the larger map-frame assets, the explicit ensure-collapse / ensure-expand helpers still honor the owner-local remembered-option state machine instead of falling back to the older binary approximation, the reserve inset is still derived from the collapsed `MiniMap/Min` chrome, the collapsed title lane still measures and clips through a single-line client-leaning `TextRenderer` path instead of the looser `Graphics.DrawString` approximation, the title draw rect stays clamped to the WZ-derived lane and centered against that lane, and the `BtNpc` family no longer stays visible while the owner is collapsed so the minimized strip only carries the controls its WZ lane actually budgets for. Focused `UnitTest_MapSimulator` coverage now also exists for the corrected `1000`/`1001`/`1003` transition split, the keyboard/runtime toggle route, the collapsed hidden-button visibility rule, and the collapsed title reserve-width math so this owner row stops drifting away from the recovered client paths. The existing owner-local hover path for off-map helper arrows, NPC markers, employee markers, and portal markers remains in place and is still anchored to this explicit owner row rather than only the broader minimap feature row. WZ evidence was rechecked in the active extracted data source for `UIWindow2.img/MiniMap`: `MinMap`, `MaxMap`, `Min`, `BtMin`, `BtMax`, `BtMap`, `BtBig`, `BtSmall`, `BtNpc`, and the direction/marker families remain present with the collapsed `Min` strip authored as `w/c/e` canvases and `BtMax`/`BtMap` still authored at `13x12` and `40x12` respectively in the active export. IDA evidence from `CUIMiniMap::OnButtonClicked` at `0x803e40` still confirms the owner button ids used by the simulator seam: `1000` mutates `m_nOption` through the collapse/state route, `1001` restores from `m_nPrevOption`, `1002` owns the world-map preflight/notice branch, and `1003` flips only compact-vs-expanded option state. The remaining gap is now limited to live-client screenshot comparison: the simulator still has not been matched pixel-for-pixel against MapleStory for the collapsed strip's final glyph raster, icon-to-title spacing, and any last one-pixel offsets inside `MiniMap/Min`, so this row should stay `Partial` until that side-by-side comparison is finished. | Naming `CUIMiniMap` explicitly closes the seam-collection gap that backlog 8 itself called out. It gives future minimap fixes a client owner for option cycling, world-map launch ownership, persistent minimap on/off reload state, remote-direction helper tooltips, NPC/employee/portal hover routing, and other minimap-only behavior that should not keep being inferred indirectly from `MinimapUI` or `WorldMapUI` alone. | minimap owner layer (`CUIMiniMap::OnButtonClicked`, `CUIMiniMap::OnMouseMove`, `CWvsContext::OnMiniMapOnOff`, `CWorldMapDlg::CreateWorldMapDlg`, `CEmployeePool`, `CPortalList`) |

### 11. Explicit skill-macro owner parity discovered by follow-up IDA lookup

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Explicit skill-macro owner parity | `SkillMacroUI` now follows the explicit owner seam end to end instead of only borrowing the old `OnCreate` layout: the loader binds the WZ `UIWindow2.img/Skill/macro` owner chrome (`backgrnd2`, `backgrnd3`, `check`, `select`, and all three `Macroicon` owner states: `icon`, `iconDisabled`, `iconMouseOver`), the row hit-testing and draw layout follow the IDA-recovered `CUIMacroSysEx::Draw` geometry (`select` at `(11, 27 + 41*i)`, skill icons at `(14 + 34*j, 30 + 41*i)`, selected macro icon at `(14, 241)`), the embedded name field follows the `CCtrlEdit` owner bounds from `CUIMacroSysEx::OnCreate` (`53, 260, 114, 14`), the checkbox hit area is owner-gated to an actively selected macro, and `BtOK` mirrors `CUIMacroSysEx::OnButtonClicked` by committing the selected macro in place and surfacing the dedicated owner tooltip and notice path. The owner-local `CCtrlEdit` focus seam is narrower too: hardware IME/keyboard capture and the soft keyboard now only stay active while the embedded name field is actually focused, and row, drag, and checkbox interactions explicitly transfer focus away from the edit control instead of leaving the whole dialog in a pseudo-focused text-entry state. Follow-up verification for this row also confirmed the exact `StringPool::GetString` payloads tied to the owner button path, so the simulator now resolves and uses the localized owner texts for `0x1108` (`Saving the skill name.`) and `0x0D01` (`It is saved.`) instead of leaving them as unresolved fallbacks. | Naming the concrete macro owner keeps this area from looking complete once text entry and macro execution mostly work. It anchors future parity fixes to the actual client dialog that owns macro presentation and button handling instead of leaving macro polish split across generic text-entry and utility-window rows. | skill-macro owner layer (`CUIMacroSysEx::OnCreate`, `CUIMacroSysEx::Draw`, `CUIMacroSysEx::OnButtonClicked`) |

### 12. Packet-owned keymap and pet auto-consume parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned keymap and pet auto-consume initialization parity | `HaCreator/MapSimulator` now keeps `CFuncKeyMappedMan` bootstrap ownership in its own seam instead of folding it into later local UI edits. The local utility inbox accepts `398/399/400`, dispatches them through the packet-owned utility controller, persists the hydrated state in a dedicated simulator config store, and restores that config during player initialization so packet-owned fallback data exists before the settings and field-hazard layers use it. Packet `398` decodes and stores the full 89-entry `FUNCKEY_MAPPED` table, clears decoded entries whose id is `22`, snapshots the previous table into an `Old` mirror, and falls back to the persisted simulator config/default path when the payload is flagged as local/default ownership, matching `CFuncKeyMappedMan::OnInit` at `0x568c30`. Local `KeyConfigWindow` commits save the simulator binding fallback into that same store, and packet-owned `398/399/400` hydration preserves that local fallback snapshot instead of overwriting it with the transient live packet remap. The live type-`4` function remap now covers both the recovered menu ids and the already-confirmed non-menu control ids from this owner family: the simulator translates packet-owned ids `0/1/2/3/7/8/9/15/16/50/52/53/54` into equip, item inventory, stat, skill, minimap, quest, key-config, quick-slot, chat, pickup, attack, jump, and interact `PlayerInput` bindings. `CFuncKeyMappedMan::AdaptVirtualKey` at `0x5687e0` is now mirrored too: the packet-owned seam drives the status-bar shortcut tooltip owner for equip, inventory, stat, skill, and quest, and the simulator now refreshes those tooltip bindings again when the status-bar utility UI is created so the recovered `CUIStatusBar::OnCreate -> AdaptVirtualKey` path is not lost when packet-owned config hydrates before the HUD exists. Packet-owned cast entries also translate into every bindable simulator hotkey surface that currently exists when the raw key does not collide with protected movement/chat ownership: the packet-owned seam repopulates the primary, function, and Ctrl bars, rebinds `InputAction.Skill1-8`, `FunctionSlot1-12`, and `CtrlSlot1-8` to the packet-authored keys, and marks those assignments so the raw runtime shim only fires entries that the ordinary input layer still cannot absorb. This pass closes another ownership gap inside that bindable remap and the remaining protected-key seam: when a packet-owned cast key already belonged to a persisted local primary/function/Ctrl slot before packet hydration, the packet-owned seam now reuses that same simulator hotkey owner first instead of always falling back to the default hotkey-layout preference, so non-default local fallback keys stay attached to the same visible bindable slot family after packet-owned rehydration, and physical `Escape` is now hard-reserved even when the simulator's local `InputAction.Escape` binding is moved elsewhere so packet-owned cast hydration and the raw runtime shim still treat it as client-owned protected input. That keeps packet-owned skill/item/macro placements visible in the existing quick-slot and key-config seams without needlessly shuffling locally-authored bindable slot ownership or stealing the client's out-of-band `Escape` owner. Packets `399` and `400` still decode packet-owned auto-HP / auto-MP consume item ids, fall back to the persisted config value when the payload sends `0`, and persist the resolved ids; the HP item is also pushed into the existing field-hazard shared-potion seam when an inventory type can be resolved, while the stored packet-owned MP item now runs through the simulator's consumable-use path once packet-owned config is loaded, MP is below the warning threshold, and at least one active pet owner exists, matching the client fallback shape recovered from `CFuncKeyMappedMan::OnPetConsumeItemInit` / `OnPetConsumeMPItemInit`. Focused coverage now also exists in `UnitTest_MapSimulator/PacketOwnedFuncKeyParityTests.cs` for the recovered function-id map, the `AdaptVirtualKey` tooltip-owner table, Ctrl-bar bindable slot coverage, local bindable-slot reuse, and the new exact-`Escape` protected-key/runtime-shim rule. The remaining gap is now narrower and centered on client-owned protected ids that still have not been recovered plus packet-owned raw placements that still fall outside the simulator's current 28 bindable primary/function/Ctrl hotkey actions and therefore still rely on the packet-owned runtime shim instead of a fully client-faithful input owner. | This row now tracks a real packet-owned initialization owner instead of a missing note. The simulator no longer loses the distinction between packet-owned keymap/bootstrap state and later local UI edits: packet dispatch, fallback persistence, startup hydration, `id == 22` clearing, config-backed restore, recovered menu-plus-control function-id translation, `AdaptVirtualKey` tooltip ownership, bindable primary/function/Ctrl hotkey remap, local bindable-slot reuse for packet-authored non-default keys, exact physical `Escape` protection, and both HP/MP consume owner hydration all live under the correct owner seam. Keeping the row `Partial` makes the remaining unrecovered protected ids and out-of-range raw placements explicit instead of hiding them under bootstrap behavior that is now already implemented. | packet-owned keymap/runtime init layer (`CFuncKeyMappedMan::OnPacket`, `CFuncKeyMappedMan::OnInit`, `CFuncKeyMappedMan::AdaptVirtualKey`, `CFuncKeyMappedMan::OnPetConsumeItemInit`, `CFuncKeyMappedMan::OnPetConsumeMPItemInit`, `UnitTest_MapSimulator/PacketOwnedFuncKeyParityTests.cs`) |

### 13. Packet-owned anti-macro result parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned anti-macro result and launch parity | `HaCreator/MapSimulator` now has a dedicated packet-owned anti-macro bridge in `MapSimulator.PacketOwnedAntiMacroParity.cs`: packet type `1011` is dispatched from the existing packet-owned utility seam, launch packets split normal-vs-admin owners, carry the decoded answer-count into the anti-macro owner seam, decode the remaining payload bytes as the JPEG challenge canvas, hold and release local Ctrl-combo input while the challenge is active, route `4/5/8/10` report branches through status-bar chat plus screenshot persistence, and route `7/9/11` close or result families through a dedicated `AntiMacroNoticeWindow` modal owner that now follows the recovered `CUIAntiMacroNotice` seam instead of the generic utility-feedback path. That notice owner uses the WZ-backed `UI/UIWindow2.img/Macro/popup`, `btOK`, `btCancle`, and the recovered `Macro/admin/*` avatar canvases, carries the recovered `StringPool` ids for the notice/chat families (`0xC84`-`0xC9A`, `0x1A65`, `0xC8D`-`0xC91`) through the existing `AntiMacroOwnerStringPoolText` seam, and now also draws the notice copy through the client-text path instead of the older simulator shadowed-text surface so the popup stays closer to `CUIAntiMacroNotice::Draw`. This pass tightened the launch timer behavior against `CWvsContext::OnAntiMacroResult`: mode `6` now reseeds the owner countdown to `60000` ms only when the decoded answer-count byte is non-zero, while zero-count relaunches preserve the current anti-macro remaining time the way the client reuses `m_tRemainAntiMacroQuestion`. The bridge still keeps the prior owner-state fixes too: `mode 9` unknown subtypes follow the client's passed-thanks branch from `CUIAntiMacroNotice::OnCreate`, closed notice modals no longer report as active, `7/9` teardown clears the preserved submitted-answer snapshot the way the client zeroes its anti-macro question state, the countdown uses floor-divided remaining seconds instead of rounding up, and `SetRet`-shaped submission stays enabled until the real timeout expires instead of cutting off during the last sub-second window. Follow-up IDA recovery for `CWvsContext::SaveAntiMacroScreenShot`, `CScreenShot::GetBaseFolder`, `CUIAntiMacro::SetRet`, and `CUIAdminAntiMacro::SetRet` remains wired through the same seam too: anti-macro screenshots keep the client-shaped `%s_%04d%02d%02d_%02d%02d%02d.jpg` naming, screenshot folder modes mirror the recovered client branches for parent-of-exe, desktop-without-trailing-slash, and `C:\\`, and answer submission mirrors the client owner teardown more closely by closing the active challenge locally, preserving the held-combo state while awaiting resolution, attempting a real outbound packet `117` send through the local-utility official-session bridge, and using the bridge's deferred outbound queue when a Maple client is already attached but session init has not completed yet so the client-shaped submit can flush as soon as crypto init finishes instead of being dropped immediately. The remaining gap is now almost entirely authoritative network ownership and real packet round-trip validation: full submit parity still depends on a bridged Maple session plus server-issued response timing, and without a live Maple session the simulator still cannot complete or validate the anti-macro round-trip against authoritative result packets. | This row now covers the actual packet-owned anti-macro controller more faithfully: the simulator models owner selection, combo hold lifecycle, JPEG challenge decode, report/result branch routing, screenshot capture, the dedicated modal notice owner, the confirmed screenshot-root behavior from `CScreenShot::GetBaseFolder`, the generated client `StringPool` text path for the notice/chat families, the client-shaped launch timer reseed-vs-preserve rule from `CWvsContext::OnAntiMacroResult`, the client-shaped submit dispatch attempt, the client-timed final-second countdown/submit window, and the attached-but-not-yet-initialized live-session queue path for outbound opcode `117` instead of leaving those pieces buried behind generic chat or feedback surfaces. Keeping the row `Partial` makes the remaining requirement for a real server-backed anti-macro response explicit rather than hiding it under work that is now already owned by the simulator seams. | packet-owned anti-macro controller (`CWvsContext::OnAntiMacroResult`, `CWvsContext::SaveAntiMacroScreenShot`, `CWvsContext::ShowAntiMacroNotice`, `CUIAntiMacroNotice::{CUIAntiMacroNotice,OnCreate,Draw}`, `CUIAntiMacro::SetRet`, `CUIAdminAntiMacro::SetRet`) |

### 14. Anti-macro owner parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Anti-macro challenge dialog parity | `HaCreator/MapSimulator` now has a dedicated anti-macro owner instead of only field-limit item gating. `MapSimulator.PacketOwnedAntiMacroParity.cs` registers a real `AntiMacroChallengeWindow`, and `UI/Windows/AntiMacroChallengeWindow.cs` now mirrors the client owners more closely: the normal anti-macro surface uses the WZ-backed `UI/UIWindow2.img/Macro/step1` frame instead of a generic box, the admin surface also uses the shipped `UI/UIWindow2.img/Macro/step1_admin` layered frame and `btOK` chrome instead of the older simulator shell, both owners are re-centered from their active WZ frame size, the challenge image now copies into the owner from the recovered `CUIAntiMacro::Draw` origin instead of being fit-scaled, the typed-answer runtime stays inside the owner with the client-observed 12-character cap, the countdown draws from `UI/UIWindow2.img/Macro/num1/*` at the recovered `CUIAntiMacro::Draw` coordinates, the attempt line resolves through the live `MapleStoryStringPool` override for recovered `StringPool 0x1A15` (`%d`) and now renders through the client-text path in the recovered small-red owner color, and submit availability now follows remaining milliseconds so the button stays active through the same final-second boundary as the client. The anti-macro edit seam still keeps the recovered `CUIAntiMacro::OnCreate` contract (`id=1000`, `42,189,194,16`, font `StringPool 0x1A25`, `nBackColor=0`, `nFontColor=0xFF000000`, `nHorzMax=12`), and the hosted Win32 edit plus managed fallback now resolve that font through the client-text private-font pipeline before falling back to installed families. The remaining gap is now specifically exact control implementation: the simulator still does not instantiate MapleStory's own in-client `CCtrlEdit` object and message loop, so the current seam is close to native ownership but not the exact widget stack the client runs. | Lie-detector or anti-macro checks are more than an item ban. The client presents them as a distinct timed text-entry owner with its own layout, countdown, and attempt messaging, so leaving this surface untracked makes progression/utility parity look more complete than it is and hides a tutorial/event-facing failure mode that will not be covered by field-limit notices alone. | timed anti-macro utility owner (`CUIAntiMacro::OnCreate`, `CUIAntiMacro::Update`, `CUIAntiMacro::Draw`, existing simulator seam contrast: `FieldInteractionRestrictionEvaluator.cs`) |

### 15. Additional quest-timer UI, quest-reward, and revive owners discovered by follow-up IDA scan

The latest IDA pass surfaced another utility-owner family that is still underrepresented in this backlog: the client keeps dedicated modeless owners for quest-timer UI, quest reward/raise windows, and revive confirmation instead of folding those flows into generic quest log, field-state packet handling, or respawn handling. A follow-up lookup also pinned the adjacent action hooks more concretely: `CUIRaiseWndBase::OnButtonClicked` at `0x8391d0`, `CUIRevive::OnButtonClicked` at `0x83ce40`, `CUIRevive::Update` at `0x83ce70`, and `CUIRevive::Revive` at `0x83cde0`.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Dedicated quest-timer UI owner parity | `UIWindowManager` now allocates a concrete quest-timer owner pair for each active packet timer instead of routing the whole family through one shared host pair: active quests get distinct `QuestTimer:<questId>` and `QuestTimerAction:<questId>` modeless windows, matching the client evidence that `CUIQuestTimer::OnCreate` allocates a fresh `CUIQuestTimerAction` alongside each timer owner. Those windows stay resident utility owners, delegate draw/input to the packet-field timer runtime per quest, and are retired when the timer expires or is removed. WZ-backed timing art follows `Quest/TimeQuest/TimeBar/*`, the client-shaped `SetScreenMode` horizontal shift (`224` px) and `500 ms` tooltip refresh remain intact, and the tooltip composition follows the client structure of `quest name + newline + remain-time line`. The remain-time formatter seam now also carries the exact client `StringPool::ms_aString` payloads recovered from MapleStory.exe v95 through the `StringPool::GetString` decode path: `0x1014 = "Time Left %d:%d:%d"` and `0x18BD = "Time Remaining %d:%d:%d"`. `QuestTimerOwnerStringPoolText` formats both owner strings directly from explicit hour/minute/second components, matching `CUIQuestTimer::GetRemainTimeString`, and focused `UnitTest_MapSimulator` coverage now locks the recovered literals plus the client-shaped formatting behavior in place. | Without an explicit row for the dedicated timer UI owners, the backlog overstates quest utility parity by counting packet timers while skipping the actual client-owned modeless layer that exposes tooltip, screen-mode, and per-timer action behavior. Tracking it separately keeps timer UI work anchored to the real owner family instead of leaving it buried inside the broader `OnQuestResult` and field-state packet rows. | timed quest utility owner (`CUIQuestTimer::OnCreate`, `CUIQuestTimer::Draw`, `CUIQuestTimer::Update`, `CUIQuestTimer::SetVisible`, `CUIQuestTimer::SetScreenMode`, `CUIQuestTimerAction`) |
| Partial | Quest reward / raise-window parity | `HaCreator/MapSimulator` now keeps more of the recovered raise-owner lifecycle inside the dedicated seam instead of stopping at local piece-slot simulation. The existing `UI/UIWindow*.img/raise` loader path remains WZ-backed from the dedicated `raise/backgrnd` frame family plus `raise/29/0` preview art and `raise/30` / `raise/31` button skins, and the client-side cancel evidence from `CUIRaiseWndBase::OnButtonClicked` at `0x8391d0` is now mirrored more closely in the simulator-owned owner flow: `QuestRewardRaiseManagerRuntime` still assigns a distinct manager session id and owner request id when the raise owner opens, `MapSimulator.QuestRewardRaiseParity.cs` still mirrors the recovered outbound `PutItem` family through the existing local-utility bridge/outbox seam, and the packet-owned inbound reconciliation path now reaches both the live owner and the retained closed-owner state instead of stopping at active-window refresh. `LocalUtilityPacketInboxManager` still accepts dedicated raise aliases for owner sync, `PutItem` add result, `PutItem` release result, confirm result, and owner destroy result; `QuestRewardRaiseInboundPacketCodec` still decodes those payloads from the same owner/request payload family; and the owner runtime now carries that inbound echo through the rest of the close/ack lifecycle as well: cancel, local resolve, and local teardown paths retain the closed raise owner inside the dedicated seam while packet-owned confirm or destroy acks are still outstanding, inactive retained raises now reconcile owner session/request ids, QR value, window mode, and per-piece lifecycle state from inbound packet echoes just like the live window does, and owner-destroy acks now clear that retained seam explicitly instead of leaving the closed-owner observation path dormant. Queued rows therefore no longer disappear simply because the simulator-local window closed first: release-pending rows stay visible as `pending release` until the packet-owned result resolves them, rejected add results still clear the reserved inventory slot and remove the row, accepted add results still promote rows to active ownership, accepted release results still clear the row, confirm results now also settle retained closed rows into their final lifecycle state after local resolve, and the owner summary/footer path still prefers the last inbound owner summary when packet-owned reconciliation arrives. The quest/runtime side is also still less synthetic than before even without a true native owner source: when `OwnerContext` is still absent, the raise open path still falls back to the current numeric quest-record value as QR data instead of always showing `0`, and focused `UnitTest_MapSimulator/QuestRewardRaiseManagerRuntimeTests.cs` now locks the retained-owner snapshot/update/destroy lifecycle so the manager/session seam does not drift back to active-window-only behavior. Client evidence still keeps the row `Partial`: the simulator still does not have the true client packet ids and full `CUIRaiseManager` / `CUIRaiseWnd` / `CUIRaisePieceWnd` object graph behind those inbound results, the quest/runtime side still does not author authoritative owner item id or max-drop state the way the client owner does, and authoritative owner persistence beyond the current synthetic retained-owner seam is still not the same as a native client owner kept alive end to end by real packet/session ownership. | Quest progression parity is broader than quest lists, alarms, and result text. Tracking this row as `Partial` keeps the dedicated raise shell, preview, button lifecycle, local piece-placement/drop-lock flow, mirrored `PutItem` outbound path, and now the synthetic-but-packet-owned inbound reconciliation plus retained closed-owner lifecycle attached to the correct owner seam while narrowing the remaining gap to the true client packet ids, native owner graph, and authoritative quest-owned owner context instead of to missing standalone raise-window behavior. | quest reward/selection dialog layer (`CUIRaiseManager`, `CUIRaiseWndBase`, `CUIRaiseWnd`, `CUIRaisePieceWnd`, `CUIRaiseWndBase::OnButtonClicked`) |
| Partial | Revive confirmation owner parity | `HaCreator/MapSimulator` keeps revive flow behind the dedicated `Revive` owner seam and now tracks more of the follow-up `CUIRevive` evidence in the existing files instead of leaving the row at an inventory-only approximation: `MapSimulator.ReviveOwnerParity.cs`, `Interaction/ReviveOwnerRuntime.cs`, `UI/Windows/ReviveConfirmationWindow.cs`, and `Loaders/UIWindowLoader.ReviveOwner.cs` still open from `Player.OnDeath`, preserve the recovered branch order (`Soul Stone` buff first, then `Wheel of Fortune`, then premium/default safety-charm branches), honor the recovered timeout boundary (`Update` only auto-resolves after `0x927C0` ms), and mirror `CUIRevive::Revive` by routing the synthetic `CField::SendTransferFieldRequest(..., bPremium, ...)` payload through outbound opcode `41` before the local respawn. This pass tightens the field-owned revive metadata seam further instead of leaving it dependent on one exact property shape: the premium safety-charm gate and current-field revive-point lookup now read map-authored revive markers case-insensitively, accept bool-backed string flags in addition to integer flags, and accept `ReviveCurFieldOfNoTransferPoint` both as a preserved WZ vector and as an `x/y` subproperty point before falling back to the conservative `forcedReturn` or death-point approximations. Focused `UnitTest_MapSimulator` coverage now locks the recovered branch priority, the `0x927C0` timeout boundary, the mirrored opcode `41` payload shape, and the revive-map-info parsing helpers in place so the simulator-owned owner logic stays aligned with the current evidence instead of drifting back toward a looser inventory-only approximation. The fallback shell still reuses the WZ-backed `UIWindow2.img/UtilDlgEx` art that is actually present in the extracted dataset (`notice`, `line`, `bar`, `dot0`, `dot1`), the revive-detail text still pulls WZ-backed cash-item metadata for `5130000`, `5131000`, and `5510000`, and the Soul Stone branch remains wired to the active `Soul Stone` buff runtime (`22181003`) so premium in-field recovery clears the buff after use. The remaining gap is narrower again and still centered on native ownership that is not recoverable from the current assets alone: the active extracted UI data still does not surface the exact revive-specific UOL backgrounds referenced by native `aUi_138` through `aUi_143`, so the simulator cannot yet draw the real client-owned per-branch backdrops, and the `CWvsContext` slot `2073` lifecycle that arms the premium safety-charm branch is still inferred from simulator-side state rather than decoded from the original client/session ownership. | Respawn parity is not only about whether the player can come back. The client puts recovery choice behind a named owner with its own create/update/button lifecycle, so tracking that seam explicitly prevents death/revive parity from being overstated once the movement and respawn runtime works. | revive-choice utility owner (`CUIRevive::OnCreate`, `CUIRevive::OnButtonClicked`, `CUIRevive::Update`, `CUIRevive::Revive`) |

### 16. Packet-owned NPC shop, store-bank, and battle-record owners discovered by follow-up `CField::OnPacket` decompile

The latest `CField::OnPacket` decompile surfaced another utility-owner family that is still missing from this backlog: the client dispatches dedicated NPC shop, store-bank, and battle-record packet families through their own managers instead of folding them into the broader utility rows.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned NPC shop, store-bank, and battle-record parity | `HaCreator/MapSimulator` now has explicit packet-owned `NpcShop`, `StoreBank`, and `BattleRecord` windows backed by the WZ-owned `UIWindow2.img/Shop`, `UIWindow2.img/StoreBank`, and `UIWindow2.img/BattleRecord` seams, plus dedicated runtimes and a focused `/npcutility` command surface for `364`/`365`, `369`/`370`, and `420` through `423`. This row now also subsumes the old trunk/storage tracking that had drifted into the companions/social backlog, because `CTrunkDlg` and `CStoreBankDlg` are the same client utility-owner family and splitting them across documents was duplicating one storage surface under two scopes. The simulator continues to decode the concrete `CShopDlg::SetShopDlg` header and item rows that IDA shows on packet `364`, and the row-owned buy/sell/recharge commands now do both parts of the client path together: they still apply the local inventory/meso mutation, and they now also mirror the recovered `CShopDlg::{SendBuyRequest,SendSellRequest,SendRechargeRequest}` outbound payloads (`opcode 66`, modes `0/1/2`) through the existing local-utility live bridge / outbox / deferred queue seam while preserving the last mirrored payload in owner status/footer output. `CStoreBankDlg` still honors the client-confirmed subtype `36` `(passingDay, fee)` get-all modal, and the packet-owned store-bank runtime now carries a fuller staged row snapshot through the existing owner seam instead of stopping at shallow markers: accepted get-all requests continue to mirror `CStoreBankDlg::SendGetAllRequest` as outbound `opcode 69`, mode `27`, while the decoded `GW_ItemSlotBase` rows now preserve the staged item serial/base-expiration state and surface richer owner selection/footer detail for equip, bundle, and pet bodies (serials, slot-kind, decode size, equip IUC-or-EXP tails, recharge serials, shared expiration strings). The same owner family still covers the simulator's trunk window/runtime, storage-keeper access, account/PIC/SPW/passcode gating, NX-backed `BtExTrunk` expansion flow, and the persisted storage/account state that had previously been described in backlog 10, so future storage work should stay anchored here with the rest of the client utility-owner surface instead of re-opening a parallel social-system row. Battle-record handling remains corrected to the direct v95 `CBattleRecordMan::OnPacket` decompile: only `421` and `422` mutate manager state, while `420` and `423` are routed to the owner family but ignored by `OnPacket` itself, and the simulator's `421` gate now matches the decompile more closely by requiring `m_bOnCalc`, `m_bServerOnCalc`, and `m_bDot` together instead of accepting DOT damage as soon as only the first two flags are armed. The remaining gap is narrower again: `CStoreBankDlg` still does not hold every native `GW_ItemSlotBase` tail field and exact slot-behavior branch that `GW_ItemSlotBase::Decode` and the dialog's `nStock` virtuals own, and the manager paths that arm/open battle-record state outside `421`/`422` are still not fully decompiled. | This row now covers the right owner family end-to-end enough that future work can stay in the client seams that actually own it: WZ-backed window ownership is explicit, direct packet application is in place, shop/store-bank commands now mirror the recovered outbound packet writes instead of stopping at local simulation, store-bank rows preserve more of the native staged item object before the remaining deeper decode/stock-behavior work, battle-record packet gating no longer overstates what `421` can do without the native `m_bDot` arm path, and storage/trunk parity stays consolidated under the same utility-owner scope instead of being duplicated in the social backlog. Keeping it `Partial` makes the unresolved native row-tail fields, stock behavior, and battle-record arming paths explicit instead of hiding them behind the now-correct owner discovery and packet gating. | packet-owned NPC utility layer (`CField::OnPacket`, `CShopDlg::OnPacket`, `CStoreBankDlg::OnPacket`, `CBattleRecordMan::OnPacket`, `CTrunkDlg::OnCreate`, `CTrunkDlg::Draw`, `CTrunkDlg::OnButtonClicked`) |

### 17. Explicit trunk/store-bank dialog owner parity clarified by follow-up `CTrunkDlg` and `CStoreBankDlg` lookups

The follow-up IDA pass exposed another storage seam that the utility backlog was still only implying through packet ownership and soft-keyboard edits: storage is not just a packet-fed `CStoreBankDlg::OnPacket` sink. `CTrunkDlg::OnCreate` proves the storage/trunk surface is a dedicated dialog owner with its own tab, button, scrollbar, and font setup before packet-fed state is applied, and a fresh IDA re-check now shows `CStoreBankDlg` has that same explicit owner shape too through `CStoreBankDlg::OnCreate` at `0x744c00` and `CStoreBankDlg::Draw` at `0x745330`.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Explicit trunk/store-bank dialog owner parity | The simulator now models more of the native owner layer instead of leaving this row at discovery only. On the trunk side, the `TrunkUI` owner still keeps the client-faithful split between storage-owned and inventory-owned choreography tighter than before: storage row interaction and the storage scrollbar are gated by actual trunk access state, tab changes re-enter the owner status path instead of always resetting to a generic prompt, the WZ-backed `BtSort` path no longer re-sorts live inventory rows that `CTrunkDlg` does not own, and trunk close now mirrors `CTrunkDlg::SetRet` more directly by routing the owner exit through a packet-backed `67 [08]` return request before the local surface disappears. On the store-bank side, the packet-owned owner is now a dedicated `StoreBank` dialog surface instead of a generic text panel: it still wires the explicit `UIWindow2.img/StoreBank/BtExit` and `BtGet` controls from the same `CStoreBankDlg::OnCreate` branch, repeats the authored `StoreBank/en` row field inside the owner, keeps a dedicated `Basic.img/VScr9` scrollbar at the native `190,93` seam with owner-local page/scroll state, allows row selection before `BtGet`, clears staged packet rows on the client-matched subtype-30 clear branch, opens an in-field Yes/No confirmation before accepting packet `370` subtype `36`, now preserves the selected row more like the native owner across `SetStoreBankDlg` row refreshes by re-anchoring through the same packet row and serial/title evidence instead of dropping selection on every owner revision, and now routes `BtExit` through the same `CStoreBankDlg::SetRet` close/return path (`69 [28]`) rather than closing as a UI-only dismiss. The loader also continues to resolve the store-bank number and cash art through the same client-confirmed StringPool ids that `CStoreBankDlg::OnCreate` uses (`0x050E -> UI/Basic.img/ItemNo`, `0x095F -> UI/CashShop.img/CashItem/0`) instead of leaving those assets as unproven guesses. The remaining gap is narrower and now focused on exact native detail: the simulator still does not model the byte-accurate per-row retrieval packet/body that `BtGet` ultimately drives after selection, the full `CStoreBankDlg::DrawGetItem` row rendering branch and final `DrawMoney` presentation byte for byte, or the last trunk-side tab/button transitions, prompt flow, and expansion behavior byte for byte. | The row now tracks concrete owner behavior rather than only naming the decompiles. `CTrunkDlg` and `CStoreBankDlg` both have local control state that sits above packet decode, and the simulator now reflects that by tightening trunk owner gating, by mirroring both dialogs' `SetRet` close/return packet path instead of treating exit as local-only state, and by giving the packet-owned store-bank owner row selection persistence across packet refreshes alongside its row field, scrollbar/page state, explicit StringPool-backed art resolution, and native get-all confirmation step. Keeping the row `Partial` makes the remaining exact retrieval/body, final draw branch, and final trunk choreography gaps explicit without losing the newly implemented owner surface. | storage/trunk dialog owner (`CTrunkDlg::OnCreate`, `CTrunkDlg::Draw`, `CTrunkDlg::OnButtonClicked`, `CStoreBankDlg::OnCreate`, `CStoreBankDlg::Draw`, `CStoreBankDlg::OnPacket`) |

### 18. Additional packet-owned local reward and helper-result parity discovered by direct `CUserLocal::OnPacket` scan

The latest `CUserLocal::OnPacket` re-check exposed another local-owner family that is still missing from this backlog: the client has dedicated result handlers for mesos-give/mesobag reward responses and a pair of quest/social helper responses instead of collapsing all of that feedback into generic chat, pickup notices, or nearby utility windows. The dedicated maker-result seam is already tracked in the earlier item-maker row and does not need to be duplicated here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned mesos-give and random-mesobag result parity | `LocalUtilityPacketInboxManager` now routes the dedicated local packet ids and aliases for `CUserLocal::OnMesoGive_Succeeded` (`236` / `0x90f950`), `CUserLocal::OnMesoGive_Failed` (`237` / `0x90d530`), `CUserLocal::OnRandomMesobag_Succeeded` (`238` / `0x90fa30`), and `CUserLocal::OnRandomMesobag_Failed` (`239` / `0x90d560`) into `MapSimulator.PacketOwnedUtilityParity`. `PacketOwnedRewardResultRuntime` decodes the local reward-result payloads, resolves the client string-pool text with simulator fallbacks, and now also resolves the default `CUtilDlg::SetUtilDlg` notice sound slot (`1272` / `0x04F8`) through `MapleStoryStringPool` so the meso-give success/failure and random-mesobag failure notice owner plays the same `Sound/UI.img/DlgNotice` cue the client fires when `CUtilDlg::Notice` is called without an explicit sound name. The random-mesobag path still routes the dialog/button/sound resource ids through the same client constructor entries (`0x17A9`-`0x17AD`, `0x17AF`-`0x17B4`, `0x17B6`-`0x17B9`). The dedicated packet-owned reward notice owner for the meso-give success/failure and random-mesobag failure paths now drops the extra simulator title bar text, centers the notice body closer to `CUtilDlg::Notice`, keeps the client-observed `SetUtilDlg` wrap widths (`200` / `234`) and centered-line math from `CUtilDlg::Draw`, re-centers the `BtOK` button against the active frame width, places that button using the same bottom-margin rule visible in `CUtilDlg::OnCreate` (`height - 31` for the stock 40x16 `BtOK`), and anchors the close button to the same top-right chrome slot instead of leaving the `UtilDlgEx/BtClose` branch at its unplaced default. The dedicated `RandomMesoBag` owner still follows the recovered `CUIRandomMesoBag` control and text coordinates (`BtOk` at `204,77`, receive text at `78,16`, right-aligned meso amount at `195,34`), mirrors the client-observed status-bar chat line plus rank-owned sound cue, and the loader now resolves the recovered `UI/UIWindow.img/RandomMesoBag/*` string-pool resource paths against either loaded `UIWindow` image before falling back to `UIWindow2.img/UtilDlgEx/notice`; when that fallback notice shell is used because the extracted dataset lacks the authored `RandomMesoBag` canvases, the simulator now keeps the same recovered client control/text coordinates instead of dropping back to centered placeholder layout. The remaining gap is now narrower and almost entirely about authored frame assembly and art fidelity: the active WZ export still does not contain the authored `RandomMesoBag` canvases, so the simulator cannot yet match the exact per-rank art branches byte-for-byte, and the `CUtilDlg::Notice` owner still approximates the full dynamically assembled `CUtilDlg::OnCreate` / `CUtilDlg::SetUtilDlg` frame-building path rather than reproducing every stitched top/center/bottom piece and separator branch. | These handlers sit on the visible reward-feedback path between local gameplay and the utility/HUD owners already documented here. Tracking the dedicated owner/runtime path prevents money-result coverage from being overstated by generic pickup or inventory feedback, while the remaining note now stays anchored to the still-missing authored art and deeper dynamic `SetUtilDlg` frame assembly instead of to packet routing, text ids, fallback-layout loss, owner-local sound dispatch, or owner-local control placement. | packet-owned local reward-result layer (`CUserLocal::OnPacket`, `CUserLocal::OnMesoGive_Succeeded`, `CUserLocal::OnMesoGive_Failed`, `CUserLocal::OnRandomMesobag_Succeeded`, `CUserLocal::OnRandomMesobag_Failed`, `PacketOwnedRewardResultRuntime`, packet-owned reward notice owner, `RandomMesoBag` owner) |
| Implemented | Packet-owned quest-resign and mate-name helper parity | `LocalUtilityPacketInboxManager` now maps the local helper-result aliases and packet ids (`259`/`260`), while `MapSimulator.PacketOwnedUtilityParity` applies the same client-observed payloads as `CUserLocal::OnResignQuestReturn` (`0x905720`) and `CUserLocal::OnPassMateName` (`0x918260`): resign-quest re-arms the auto-start registration and removes any matching Quest Alarm entry, and pass-mate-name stores or clears the quest mate-name string used by the quest runtime/detail UI. `UnitTest_MapSimulator/PacketOwnedQuestHelperParityTests.cs` now covers the alias routing and both packet payload handlers so this helper family is no longer only implied by nearby quest/social rows. | Tracking these helper-result owners prevents the utility backlog from silently dropping smaller packet-fed client flows that sit between quest/social UI and ordinary field feedback. Keeping them explicit also avoids misfiling them under generic chat/notice rows later. | packet-owned local helper-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnResignQuestReturn`, `CUserLocal::OnPassMateName`) |

### 19. Additional packet-owned logout-gift owner parity discovered by direct `CField::OnPacket` decompile

The latest direct `CField::OnPacket` decompile surfaced one more utility owner that is still untracked in this backlog: packet type `432` routes to `CWvsContext::OnLogoutGift`, and that context handler updates a dedicated `CUILogoutGift` owner when present.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned logout-gift owner parity | MapSimulator now registers a dedicated `LogoutGiftWindow` owner, caches the three logout-gift commodity serial numbers from the client-confirmed trailing 12-byte logout-gift block on character-data `SetField` even when larger mixed tails are present, preserves any preceding adjacent bytes as exact raw tail state plus aligned `int32` views, and routes packet `432` through a dedicated `CWvsContext::OnLogoutGift` branch that only refreshes an already-instantiated logout-gift owner just like the direct decompile instead of treating packet `432` as a fresh show-window trigger. The simulator owner still mirrors the client-observed three-slot/button ids and opcode `313` selection send, maps any aligned leading tail `int32` values onto the exact contiguous `CWvsContext` precursor dword slots immediately before the client-read commodity cache at `dword[4138..4140]`, and now also routes the post-selection / post-close StringPool `0x16AB` follow-up through the existing shared `LoginUtilityDialog` owner so the quit continuation stays behind a real client-style completion dialog instead of firing immediately from the logout-gift click path. It still attempts to consume authored `UI/UIWindow.img/LogoutGift/backgrnd` art when a dataset actually exposes that node, but the active extracted UI export still does not surface any recoverable `LogoutGift` branch. The remaining gap is now narrower and mostly about unresolved native field semantics plus missing art: the preserved precursor dwords are no longer anonymous, but everything before `m_bPredictQuit` still lacks exact client field names and non-logout-gift consumers, and without the authored `LogoutGift` canvases/buttons the simulator still falls back to a custom visual shell instead of byte-matching the native UI art. | Without an explicit row, utility parity can look complete once the broad `CUserLocal` dispatcher and nearby reward windows are covered even though the client still has a separate packet-owned logout-gift owner path. The row now stays anchored to the actual client dispatch and owner lifecycle while clearly isolating the remaining work to precursor-context semantics and missing authored UI assets instead of to packet routing, packet-432 visibility semantics, or the completion-dialog owner chain. | packet-owned logout-gift utility owner (`CField::OnPacket`, `CWvsContext::OnLogoutGift`, `CUILogoutGift::OnCreate`, `CUILogoutGift::Draw`) |

### 20. Additional Vega enhancement owner discovered by targeted IDA scan

The targeted IDA sweep surfaced one more utility-window owner that was not represented anywhere in this backlog set: the dedicated Vega enhancement dialog. This owner sits next to the existing item-upgrade flow, but it is not just a simulator-only convenience wrapper around scrolling.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Vega enhancement dialog parity | WZ was rechecked first against the extracted owner assets and consumable strings before touching the runtime seam: `UI/UIWindow.img/VegaSpell/{backgrnd10,backgrnd60,SuccessWnd,FailWnd,Count,EffectTwinkling,EffectSpelling,EffectArrow,EffectSuccess,EffectFail}` still carries the dedicated dialog chrome, rates, and effect frames, while `String/Cash.img/{5610000,5610001}/desc` still author the `30% on 10% scroll` and `90% on 60% scroll` Vega modifier wording the simulator already parses for preview and compatibility. IDA then tightened the client owner evidence further: `CUIVega::OnCreate` (`0x7c1320`) still builds the dedicated dialog and count strip, `CUIVega::Draw` (`0x7c1dd0`) still owns the specialized frame/effect presentation, `CUIVega::OnButtonClicked` (`0x7bf4a0`) still runs the missing-selection (`5422`), exclusive-request (`278`), blocked-state (`66`), incompatible-pair (`5423`), and white-scroll prompt (`0x1160`) notice path before disabling the owner buttons, stamping `CWvsContext::SetExclRequestSent`, and sending the Vega request, `CWvsContext::SetExclRequestSent` (`0x49c580`) still refreshes the same exclusive-request tick on both set and clear, `CWvsContext::SendConsumeCashItemUseRequest` still shows the owning consume-cash opcode `0x55`, and `CUIVega::OnVegaResult` (`0x7bf7b0`) still drives the recovered `68/69` success and `73/71` failure code pairs around the shared `Effect_Vega(..., 0/4)` result prelude. The simulator now follows more of that owner choreography in the existing seam instead of leaving the request lifecycle implicit: `MapSimulator.VegaSpellParity.cs` keeps Vega on its dedicated window path, mirrors the recovered `500 ms` exclusive-request cooldown even after the owner clears the pending flag, keeps local consumable launch on the recovered packet-owned `SendConsumeCashItemUseRequest` seam instead of opening Vega directly, builds a client-shaped consume-cash launch payload with an explicit owner tail, mirrors opcode `0x55` through the same live bridge / outbox / deferred transport seam used by other local-utility owners, routes dedicated packet-owned Vega launch and `OnVegaResult` result payloads through the local-utility inbox / `packetclientraw` ingress seam instead of leaving those helpers disconnected, and now resolves the owner request payload's equip or scroll item-TI fields from stable synthetic per-instance tokens derived from the live equipped slot and live inventory-slot metadata instead of just writing raw item ids back out. The seam still falls back from the mispointed Vega string-pool UI asset path to the WZ-backed `Sound/UI.img/EnchantDelay` loop descriptor when the owner wants a result loop, lengthens fallback result timing when an external transport path takes the request, and now keeps on-disk regression coverage under `UnitTest_MapSimulator/VegaSpellParityTests.cs` for the consume-cash prefix plus explicit owner tail, synthetic item-token layout, launch decode fallback, recovered result-code pairs, cooldown preservation, packet alias / opcode ingress, and loop-sound descriptor normalization. The remaining gap is narrower but still real: the outbound owner packet still uses simulator-authored synthetic item-instance tokens instead of the client's byte-for-byte inventory-instance fields, the resolved success/failure branch still depends on the local upgrade backend to mutate equipment state before the packet-owned result prelude is shown instead of after the owner finishes its recovered prelude-to-popup choreography, and the exact client loop-sound ownership beyond the current `EnchantDelay` fallback still is not modeled one-for-one. | Without an explicit row, the backlog overstates item-enhancement parity by treating Vega as if it were already covered once scrolling and item-upgrade windows exist. The client gives Vega its own dialog, validation, notice flow, send path, and effect owner, and the simulator already has a corresponding window/runtime seam, so tracking it explicitly keeps future work anchored to the real client class instead of leaving the feature buried inside generic upgrade notes. | dedicated enhancement utility owner (`CUIVega::OnCreate`, `CUIVega::Draw`, `CUIVega::OnButtonClicked`, `CWvsContext::SendConsumeCashItemUseRequest`, `CWvsContext::SetExclRequestSent`, `CUIVega::OnVegaResult`, `CAnimationDisplayer::Effect_Vega`, simulator seams: `HaCreator/MapSimulator/UI/Windows/VegaSpellUI.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`, `HaCreator/MapSimulator/MapSimulator.VegaSpellParity.cs`, `HaCreator/MapSimulator/UI/Windows/ItemUpgradeUI.cs`, `UnitTest_MapSimulator/VegaSpellParityTests.cs`) |

### 21. Shared production and enhancement animation-owner parity discovered by targeted `CAnimationDisplayer` scan

The latest IDA scan exposed another owner family that the backlog was still flattening into the maker, upgrade, and Vega window rows: the client plays several production and enhancement result effects through shared `CAnimationDisplayer` helpers rather than leaving all result presentation inside `CUIItemMaker`, `CUIItemUpgrade`, or `CUIVega`.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Shared item-production and enhancement effect parity | The shared owner seam now stays intact across the recovered production and enhancement families. `InventoryItemMetadataResolver` still resolves mastery-book metadata straight from the WZ-backed item info path (`info/masterLevel`, `info/reqSkillLevel`, `info/skill`, plus success-rate parsing from the authored description), and `MapSimulator.TryUseConsumableInventoryItem` still recognizes mastery books before the generic consumable path so local skill-book use validates learned skills, enforces the client-authored skill-level gate, consumes the book, applies the target master level on success, and registers `ProductionEnhancementAnimationDisplayer.PlaySkillBookResult(...)` through the shared Skills-window owner instead of leaving `Effect_SkillBookUsed` uninvoked. The packet-owned side still decodes opcode `50` with the client-confirmed `CWvsContext::OnSkillLearnItemResult` byte layout, mirrors local packet-authored mastery-book consumption through the same shared Skills-window owner, re-applies the WZ-backed target master level on success, plays the recovered success/failure enchant sounds for used results, and formats packet-owned chat through the recovered client notice ids `StringPool[0x0F2F..0x0F34]`. Remote packet-authored skill-book results now stay on that same recovered `Effect_SkillBookUsed` owner path even when the remote actor is not yet materialized: the simulator queues the front/back BasicEff branches (`StringPool[0x0FF1..0x0FF4]`, `Effect/BasicEff.img/SkillBook/{Success,Failure}/{0,1}`) through the existing transient avatar-effect queue in `RemoteUserActorPool` and applies them when the actor enters the pool instead of dropping to packet-owned/chat-only feedback. The shared displayer remains seeded from `UI/UIWindow.img/VegaSpell/{EffectTwinkling,EffectSpelling,EffectArrow,EffectSuccess,EffectFail}`, and `VegaSpellUI` still follows the recovered `Effect_Vega` branch split: `CUIVega::OnButtonClicked` triggers `Effect_Vega(..., 3)` for cast start, `CUIVega::OnVegaResult` triggers the repeat-owned result prelude through `Effect_Vega(..., 0)` and `Effect_Vega(..., 4)`, and `CUIVegaResultPopup::OnCreate` triggers `Effect_Vega(..., 1/2)` for the success/fail popup effects. The earlier shared item-make, enchant, and hammer work also remains in place, including the `3120` ms enchant-skill delay from `Effect_ItemUpgrade` and the `2700` ms hammer repeat/finish ownership at `91,81` and `89,105` from `Effect_ViciousHammer`. WZ was rechecked again in the active extracted data source for the remaining cube seam: `UI/UIWindow2.img/{MiracleCube,HyperMiracleCube,MiracleCube_8th}` still expose the authored `Effect` frame stacks under the cube owners, and the simulator now feeds those frame stacks into `ProductionEnhancementAnimationDisplayer.ConfigureCube(...)` so cube casting duration comes from the same shared overlay manager instead of staying local-only inside `ItemUpgradeUI`. No parity gap remains in this row after the queue-backed remote skill-book follow-through and the shared cube-owner routing were both in place. | If this owner stays implicit, progression parity can look more complete than it is once the maker, upgrade, and Vega windows animate locally. The client still keeps a separate shared animation manager for result effects, delay insertion, one-time versus repeat registration, and several packet-owned follow-up sounds, so tracking that owner explicitly keeps future work anchored to the real effect-composition seam instead of burying it inside individual utility-window rows. | shared production/enhancement animation layer (`CAnimationDisplayer::Effect_ItemMake`, `CAnimationDisplayer::Effect_ItemUpgrade`, `CAnimationDisplayer::Effect_ViciousHammer`, `CAnimationDisplayer::Effect_Vega`, `CAnimationDisplayer::Effect_SkillBookUsed`, simulator seams: `HaCreator/MapSimulator/Animation/ProductionEnhancementAnimationDisplayer.cs`, `HaCreator/MapSimulator/Animation/AnimationDisplayerWindowOverlayOwner.cs`, `HaCreator/MapSimulator/UI/Windows/UIWindowBase.cs`, `HaCreator/MapSimulator/UI/Windows/UIWindowManager.cs`, `HaCreator/MapSimulator/UI/Windows/ItemMakerUI.cs`, `HaCreator/MapSimulator/UI/Windows/ItemUpgradeUI.cs`, `HaCreator/MapSimulator/UI/Windows/VegaSpellUI.cs`) |

### 22. Additional context-owned utility owners discovered by targeted `CWvsContext::OnPacket` scan

The targeted `CWvsContext::OnPacket` pass exposed two small but real utility-window owners that were still missing from the backlog corpus. These do not justify a new split document, but they do belong in the progression/utility backlog because the client opens and drives them through dedicated context-owned UI ids instead of treating them as generic notices or side effects. The same pass also confirmed that `CWvsContext::OnClaimResult`, `CWvsContext::OnSetGender`, and `CWvsContext::OnHourChanged` are adjacent notice/state handlers rather than standalone utility-window owners, so they are noted here as evidence but do not need their own row yet.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Account-more-info utility owner parity | This pass tightens the dedicated simulator owner/runtime seam instead of expanding account-more-info into login prompts or character-info windows. `MapSimulatorWindowNames.AccountMoreInfo`, `AccountMoreInfoWindow`, `Managers.AccountMoreInfoRuntime`, and `MapSimulator.AccountMoreInfoParity` still register a dedicated context-owned utility window for client UI id `40`, open it from `CWvsContext::OnAccountMoreInfo` packet type `133` subtype `0`, issue the client outbound load/save requests on opcode `193` with request subtypes `1` and `3`, apply packet-owned server subtype `2`/`4` load/save bodies to the owner, close the owner on successful save, leave it open and re-enable saving on failed save, and route adjacent `CWvsContext::OnSetGender` packet type `58` into the same context state without treating it as a separate dialog owner. The WZ check against the active `E:\MapleStory\WzExport\v95_2` data source still found no `AccountMoreInfo`-named `UIWindow.img`/`UIWindow2.img` node and no `Etc/CountryName.img`, but it did confirm the generic `UI/Basic.img/ComboBox` segment family and `UI/Basic.img/CheckBox/0..3` canvases that the current shell can keep using. The latest IDA pass also added smaller client-owned details now reflected in the seam: `CUIAccountMoreInfo::OnCreate` (`0x76c4c0`) creates the five comboboxes at `(89,32,155,18)`, `(248,32,135,18)`, `(89,57,60,18)`, `(152,57,40,18)`, and `(194,57,40,18)`, creates five play-style checkboxes and nineteen activity checkboxes with empty control text at the recovered native coordinates, and calls `SendLoadAccountMoreInfoRequest` after `SetAregComboBox(0)` and `SetBrithDayComboBox`; `SetBrithDayComboBox` (`0x76b670`) seeds birthday years from local current year minus `100` through local current year and defaults to current year minus `15`; `LoadCountryName` (`0x76b950`) resolves the default region item from StringPool id `0x16B8`; `OnSaveAccountMoreInfoResult` (`0x76b600`) re-enables OK first and shows StringPool id `0x16B7` on failure. `MapleStoryStringPool` now pins the drifted `0x16B7` failed-save notice and `0x16B8` default region item, `AccountMoreInfoRuntime` now uses the local-year birthday range/default and server subtype wording, `AccountMoreInfoWindow` no longer draws invented numbered labels over the client-empty checkbox text, and `UnitTest_MapSimulator/AccountMoreInfoParityTests.cs` covers those recovered runtime/string-pool details. The remaining gap is now narrower: the simulator still uses a dedicated owner shell rather than the exact `CUIWnd::SetBackgrnd` string-pool-backed skin from id `0x16AE`, and exact country/region combo data plus any authored label text baked into the missing background still need recovery from a data source that exposes `Etc/CountryName.img` and the account-more-info background. | Without this row, parity can overstate account/profile coverage once login prompts and user-info windows exist, even though the client still owns a separate utility dialog for the extra account-more-info form and its packet-backed save/load lifecycle. Keeping it here anchors future work to the right context/UI owner instead of misfiling it under login or general character-info polish. | context-owned utility owner (`CWvsContext::OnAccountMoreInfo`, `CWvsContext::OnSetGender`, `CUIAccountMoreInfo::OnCreate`, `CUIAccountMoreInfo::OnButtonClicked`, `CUIAccountMoreInfo::SendLoadAccountMoreInfoRequest`, `CUIAccountMoreInfo::SendSaveAccountMoreInfoRequest`, `CUIAccountMoreInfo::OnLoadAccountMoreInfoResult`, `CUIAccountMoreInfo::OnSaveAccountMoreInfoResult`, `CUIAccountMoreInfo::SetAregComboBox`, `CUIAccountMoreInfo::SetBrithDayComboBox`, `CUIAccountMoreInfo::LoadCountryName`; simulator seams: `HaCreator/MapSimulator/MapSimulator.AccountMoreInfoParity.cs`, `HaCreator/MapSimulator/Managers/AccountMoreInfoRuntime.cs`, `HaCreator/MapSimulator/UI/Windows/AccountMoreInfoWindow.cs`, `HaCreator/MapSimulator/Interaction/MapleStoryStringPool.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`, `UnitTest_MapSimulator/AccountMoreInfoParityTests.cs`) |
| Implemented | Dragon Box utility owner parity | The simulator now tracks Dragon Box as an explicit packet-owned utility owner instead of flattening it into generic event feedback. WZ-backed loader work uses `UI/UIWindow.img/DragonBall_A` and `DragonBall_B` for the dedicated owner shell, close button, summon button, and background offsets, matching the recovered client backgrounds and `BtSummon` branch. The packet/runtime seam is now named and wired through `MapSimulator.DragonBoxParity`: packet type `164` decodes the client `remainMs`, `bShowUI`, close flag, `bAbleToSummon`, and orb mask shape from `CWvsContext::OnDragonBallBox` (`0x9db630`); open and close ownership are applied through the dedicated `MapSimulatorWindowNames.DragonBox` owner and direction-mode registry entry; live state is exposed through `DragonBoxWindowSnapshot`; and the owner updates orb/timer presentation using the same nine-slot orb mask layout that `CUIDragonBox::SetOrb` (`0x79fab0`) draws. The summon branch is also anchored in the right place: `DragonBoxWindow` routes button `2000` into the dedicated handler, and the simulator mirrors `CUIDragonBox::OnButtonClicked` (`0x79f9d0`) by blocking when `remainMs > 0` or `bAbleToSummon == 0`, otherwise emitting opcode `197` as the Dragon Ball summon request. This row no longer has an open parity gap at the backlog level. | Event/progression parity can look more complete than it is once field scoreboards and generic notices exist, but the client still opens a dedicated Dragon Box utility owner with its own timer/orb state and summon-enable branch. Naming that owner keeps future work anchored to the actual UI and packet lifecycle instead of flattening it into general event feedback. | context-owned event utility owner (`CWvsContext::OnDragonBallBox`, `CUIDragonBox::OnCreate`, `CUIDragonBox::Draw`, `CUIDragonBox::OnButtonClicked`; simulator seams: `HaCreator/MapSimulator/MapSimulator.DragonBoxParity.cs`, `HaCreator/MapSimulator/UI/Windows/DragonBoxWindow.cs`, `HaCreator/MapSimulator/UI/Windows/ProgressionUtilityWindowModels.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`, `HaCreator/MapSimulator/Managers/LocalUtilityPacketInboxManager.cs`, `HaCreator/MapSimulator/Managers/DirectionModeWindowOwnerRegistry.cs`) |

### 23. Additional cash-service modal-dialog owners discovered by broad function scan and targeted `CCashShop` decompiles

The broad function scan and follow-up `CCashShop` decompiles exposed one more cash-service owner family that was still missing from the backlog corpus. These dialogs do not belong in backlog 10 or 11: the client allocates them from the in-stage cash-service result flow, not from the social-room or login selectors.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Cash-service modal-dialog owner parity | The cash-service rows currently stop at stages, subwindows, wish-list panes, and packet result summaries, but the client still opens a separate family of modal owners inside those flows. IDA now shows five concrete seams that are not named anywhere else in the backlog: `CCouponUseSelectDlg::OnCreate` (`0x48c810`) builds the coupon-selection dialog that cash-item coupon result paths rely on; `CConfirmPurchaseDlg::OnCreate` (`0x48acc0`) is the dedicated purchase-confirmation owner rather than a generic `CUtilDlg` notice; `CCashShop::OnCashItemResLoadGiftDone` (`0x496520`) allocates `CUIReceiveGift`, sets per-entry values, and runs each gift row through `CDialog::DoModal`; `CCashShop::OnCheckNameChangePossibleResult` (`0x495470`) allocates `CUIChangingLicenseNotice`, seeds the recovered birth-date payload, and uses it as the success branch instead of falling straight to a notice; and `CCashShop::OnCheckTransferWorldPossibleResult` (`0x4980b0`) does the same for `CUITransferWorldLicenseNotice` after decoding world-list and birth-date state. None of those owners are tracked as first-class simulator seams today, so cash-service parity can look more complete than it is even while coupon selection, purchase confirmation, gift acceptance, name-change confirmation, and transfer-world confirmation are still flattened into generic dialogs or omitted outright. | Cash-service parity is not only about stage entry, preview panes, and list mutation. The client still routes several user-facing purchase and entitlement branches through dedicated modal owners, and those branches sit directly on the path for coupon redemption, buy confirmation, gift intake, name-change eligibility, and transfer-world eligibility. Keeping this as its own row prevents those gaps from disappearing under the broader stage and child-window rows. | cash-service modal-dialog layer (`CCouponUseSelectDlg::OnCreate`, `CConfirmPurchaseDlg::OnCreate`, `CCashShop::OnCashItemResLoadGiftDone`, `CCashShop::OnCheckNameChangePossibleResult`, `CCashShop::OnCheckTransferWorldPossibleResult`, client-owned dialog classes `CUIReceiveGift`, `CUIChangingLicenseNotice`, `CUITransferWorldLicenseNotice`) |

### 24. Additional cash-item gachapon animation-owner discovered by targeted `CAnimationDisplayer` scan

The targeted animation-owner pass exposed one more cash-service presentation seam that is still not tracked anywhere in this backlog set. This owner does not belong under backlog 7's field-feedback animation rows, because it is driven from the cash-service result family rather than field packets, and it is also distinct from the existing production/enhancement animation row because it is a cash-stage reward presentation, not an item-maker, item-upgrade, hammer, Vega, or skill-book effect.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Cash-item gachapon animation-owner parity | `HaCreator/MapSimulator` already keeps the broader cash-service result flow on the dedicated `CashServiceStageWindow` seam: packet-owned cash-gachapon result summaries are visible in stage status, decoded result packets are cataloged through the existing `CCashShop` result path, and the stage can distinguish open/copy/stamp result families instead of flattening them into a generic cash-item notice. What is still missing from the backlog ownership map is the separate client presentation seam that turns those results into a visible animation. IDA shows `CAnimationDisplayer::Effect_CashItemGachapon` at `0x457f30` is a dedicated owner rather than a small helper: it chooses one of two StringPool-backed UOL branches depending on whether the caller passes a jackpot animation layer, calls `CAnimationDisplayer::LoadLayer` with the caller-supplied origin/overlay relationship, explicitly animates the jackpot branch with `GA_STOP`, and then registers that layer through `RegisterOneTimeAnimation` instead of leaving gachapon presentation on the `CCashShop` stage window or packet summary path. The current backlog covers cash-stage packet/session ownership, child windows, modal dialogs, and packet-owned result summaries, but it does not yet give this shared animation-manager seam a dedicated row, so cash gachapon parity is easy to overstate once packet results and window text exist. The remaining gap is therefore narrower and clearer than a generic cash-stage miss: the simulator still needs an explicit owner for the normal-versus-jackpot visual branch, `LoadLayer` registration, and one-time animation lifecycle that the client runs after the packet result reaches the cash-service stage. | Cash-service parity is not only about opening the stage, decoding result packets, and surfacing result text. The client also routes cash-gachapon reveal presentation through its shared animation manager, so keeping this owner explicit prevents a visible reward effect from staying buried inside the cash-stage packet row and makes the remaining animation work separable from the broader cash-stage or modal-dialog backlog. | cash-service reward animation layer (`CAnimationDisplayer::Effect_CashItemGachapon`, `CAnimationDisplayer::LoadLayer`, `CAnimationDisplayer::RegisterOneTimeAnimation`, `CCashShop::OnCashItemResult`, simulator seams: `HaCreator/MapSimulator/UI/Windows/CashServiceStageWindow.cs`, `HaCreator/MapSimulator/MapSimulator.CashServiceStageParity.cs`) |

### 25. Random Morph item-use dialog owner discovered by broad IDA function scan

The broad function scan surfaced another item-use dialog owner that was not represented in the utility-window backlog. This belongs here rather than backlog 1's morph action-loader row because it is the UI and outbound request surface for a random-morph item, not the avatar morph frame loader or runtime transform owner.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Random Morph dialog and outbound request parity | WZ re-check confirms the UI owner art that the client constructor uses: `UI/UIWindow.img/Transform/backgrnd` exists in the mounted export as a canvas under the `Transform` branch. IDA shows `CUIRandomMorphDlg::ShowDlg` only opens when the unique-modeless slot is free, `CUIRandomMorphDlg::CUIRandomMorphDlg` stores the inventory position and item id, sets `UI/UIWindow.img/Transform/backgrnd`, and creates the dialog at `(250,234)` with size `300x131`. `OnCreate` then builds OK and Cancel buttons plus a target-name edit control at `(84,66,200x14)` with a `12`-character horizontal max, `_SendMorphRequest` rejects empty targets, blocks while the context-side request flag or 500 ms throttle is active, emits the StringPool `0x136` notice for blocked sends, and otherwise sends opcode `184` with update time, inventory position, item id, and target name before latching the request flag and timestamp. `Draw` clears the full `300x131` dialog surface before delegating to the ordinary window draw path. The simulator backlog already mentions random-morph item metadata/tooltips elsewhere, but it still does not name this dedicated dialog, throttle, and outbound packet owner. | Random-morph parity can otherwise look covered once the item metadata and avatar morph action loaders exist. The client has a separate item-use dialog with concrete WZ art, edit constraints, request throttling, and a packet-shaped send path, so future work should not hide that UI/request lifecycle inside generic item-use, tooltip, or morph-rendering rows. | random-morph item-use dialog layer (`CUIRandomMorphDlg::ShowDlg`, `CUIRandomMorphDlg::CUIRandomMorphDlg`, `CUIRandomMorphDlg::OnCreate`, `CUIRandomMorphDlg::_SendMorphRequest`, `CUIRandomMorphDlg::Draw`, `UI/UIWindow.img/Transform/backgrnd`) |

## Priority Order

If the goal is visible parity first, the next work in this area should be sequenced like this:

1. Window usefulness pass:
   Finish minimap, inventory, equip, and quest-log interactions so the existing windows become usable client-facing tools rather than frame shells.
2. Progression correctness pass:
   Tighten ability/stat formulas, equip presentation, and inventory-backed validations so the windows reflect the runtime accurately.
3. External cash-service ownership pass:
   Add dedicated Cash Shop and ITC stage owners, and route the in-map `CAdminShopDlg::OnPacket` `366`/`367` lifecycle before treating cash-service parity as an extension of `CAdminShopDlg` shell drawing, `OnGoToCommoditySN`, or the local utility bridge.
4. Cash-service subwindow pass:
   Add the avatar-preview, locker, inventory, list, status, One-a-Day, and cash-trading child owners after the base service stages exist so cash-item preview and service-pane behavior do not stay folded into one coarse stage row.
5. Cash-service modal-dialog pass:
   Add the coupon-selection, purchase-confirmation, receive-gift, name-change-license, and transfer-world-license owners after the base service stages and child panes exist so cash-item result and entitlement flows do not stay flattened into generic notices.
6. Cash-service wishlist pass:
   Add the dedicated `CUIAdminShopWishList`-style owner after the base service stages and child panes exist so wish-list search/toggle behavior does not stay buried inside the coarse admin-shop row.
7. Anti-macro packet/result pass:
   Add the `CWvsContext::OnAntiMacroResult` bridge, screenshot/notice side effects, combo-hold lifecycle, and admin-vs-normal owner split before treating lie-detector parity as covered by an owner shell alone.
8. Anti-macro owner pass:
   Add the dedicated `CUIAntiMacro`-style timed text-entry owner after the packet/result bridge exists so the launch and timeout flow does not stay inferred from item bans or generic notice text.
9. Utility-owner refinement pass:
   Tighten `CUIKeyConfig`, `CUIGameOpt`, and `CSoftKeyboardDlg` lifecycle parity after the broad utility buttons work so key-binding, option, and soft-keyboard behavior does not stay hidden inside the generic cursor/utility row.
10. Macro-owner refinement pass:
   Tighten `CUIMacroSysEx` draw/button-owner parity after the macro surface and shared text-entry seams exist so macro behavior does not stay documented only as input polish.
11. Utility feedback pass:
   Complete pickup notices, soft-keyboard or constrained text-entry behavior, macro behavior, cursor states, and status-bar utility-button behavior.
12. Production/effect animation pass:
   Split maker, upgrade, hammer, skill-book, Vega, and cash-gachapon result-effect ownership out from the window and cash-stage rows before more reward presentation drifts into packet summaries or window-local implementations only.
13. Owner-consolidation pass:
   Refresh older umbrella rows so minimap, macro, inventory, equip, and quest utility work stays anchored to the explicit client owners already captured here instead of drifting back into stale generic-window buckets.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".







