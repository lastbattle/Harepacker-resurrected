# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/UI/MinimapUI.cs`
- `HaCreator/MapSimulator/UI/MouseCursorItem.cs`
- `HaCreator/MapSimulator/UI/PickupNoticeUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AbilityUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AdminShopDialogUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AbilityUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/EquipUI.cs`
- `HaCreator/MapSimulator/UI/Windows/EquipUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/InventoryUI.cs`
- `HaCreator/MapSimulator/UI/Windows/InventoryUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/QuestUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuestUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillMacroUI.cs`
- `HaCreator/MapSimulator/UI/Windows/WorldMapUI.cs`

### Client references checked

- `CUIStat::Draw` (captured in simulator layout comments and offsets)
- `CUIStat::DrawTextA` (captured in simulator layout comments and offsets)
- `CUISkillMacro` (captured in simulator window comments)
- `CWvsContext::OnDropPickUpMessage` (captured in `PickupNoticeUI` comments)
- `CUIScreenMsg::ScrMsg_Add` (captured in `PickupNoticeUI` comments)
- `CUIItem::Draw` at `0x7ccf50`
- `CAdminShopDlg::OnCreate` at `0x42ecd0`
- `CAdminShopDlg::Draw` at `0x42fdf0`
- `CAdminShopDlg::OnButtonClicked` at `0x430f90`
- `CUIChannelSelect::OnCreate` at `0x606a10`
- `CUIChannelSelect::Draw` at `0x6056d0`
- `CUIChannelSelect::OnButtonClicked` at `0x6035f0`
- `CUIWorldSelect::OnCreate` at `0x60b660`
- `CUIWorldSelect::OnButtonClicked` at `0x6025e0`
- `CUIChannelShift::OnCreate` at `0x96c160`
- `CUIChannelShift::Draw` at `0x96ccb0`
- `CUIItemMaker::Draw` at `0x7d4980`
- `CUIItemMaker::OnButtonClicked` at `0x7d5dc0`
- `CUIItemUpgrade::Draw` at `0x7c0100`
- `CUIItemUpgrade::OnButtonClicked` at `0x7c0ca0`
- `CUIMapTransfer::Draw` at `0x7df5c0`
- `CUIMapTransfer::OnButtonClicked` at `0x7e0ed0`
- `CUIEquip::Draw` at `0x7aa560`
- `CUIQuestInfo::Draw` at `0x82a780`
- `CUIQuestInfoDetail::Draw` at `0x8248c0`
- `CUIQuestInfoDetail::OnButtonClicked` at `0x834c90`
- `CUIQuestAlarm::Draw` at `0x82d850`
- `CUIStatusBar::OnButtonClicked` at `0x880540`
- `CUIStat::Draw` at `0x864bd0`
- `CUIMacroSysEx::OnCreate` at `0x85c420`
- `CUISoftKeyboard::GetKeyIndexFromPoint` at `0x85faa0`
- `CUISoftKeyboard::OnMouseMove` at `0x85fba0`
- `CUISoftKeyboard::HitTest` at `0x85fc50`
- `CUISoftKeyboard::UpdateKeyConstraint` at `0x85fd70`
- `CUIUserInfo::Draw` at `0x8b3a50`
- `CUIUserInfo::OnButtonClicked` at `0x8b6d80`
- `CBookDlg::OnCreate` at `0x471f00`
- `CBookDlg::Draw` at `0x473360`
- `CBookDlg::OnButtonClicked` at `0x474300`
- `CUserLocal::OnPacket` at `0x9340c0`
- `CUserLocal::OnOpenUI` at `0x9055f0`
- `CUserLocal::OnOpenUIWithOption` at `0x932300`
- `CUserLocal::OnGoToCommoditySN` at `0x905760`
- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`

Notes:
Direct IDA seam capture is now confirmed for inventory, cash-service, channel-shift, item-maker, item-upgrade, map-transfer, equipment, quest-log, quest-detail, quest-alarm, status-bar utility clicks, stat-window, user-info owners, the untracked `CBookDlg` book/collection owner, the soft-keyboard helper that sits beside `CUIMacroSysEx`, and a broader local-user packet dispatcher for utility/event surfaces. `CUserLocal::OnPacket` is important because it shows the client does not only open these windows from direct button clicks: packet-owned handlers also drive generic `UI_Open`, optioned UI launch, commodity-shop migration, field fade overlays, balloon messages, and damage-meter timer resets. The remaining seam-collection gap in this backlog area is now narrower and centered on the minimap window plus any exact `CUISkillMacro` draw owner rather than on an untracked progression-window family.

## Client Function Index By Backlog Area

The list below maps this backlog area to the concrete client seams that are already named in code or prior reverse-engineering notes.
These are the first functions or classes to inspect before changing simulator behavior.

### 1. Progression and utility window parity

- `CUIStat::Draw`
- `CUIStat::DrawTextA`
- `CUIItem::Draw` at `0x7ccf50`
- `CAdminShopDlg::OnCreate` at `0x42ecd0`
- `CAdminShopDlg::Draw` at `0x42fdf0`
- `CAdminShopDlg::OnButtonClicked` at `0x430f90`
- `CUIChannelSelect::OnCreate` at `0x606a10`
- `CUIChannelSelect::Draw` at `0x6056d0`
- `CUIChannelSelect::OnButtonClicked` at `0x6035f0`
- `CUIWorldSelect::OnCreate` at `0x60b660`
- `CUIWorldSelect::OnButtonClicked` at `0x6025e0`
- `CUIChannelShift::OnCreate` at `0x96c160`
- `CUIChannelShift::Draw` at `0x96ccb0`
- `CUIItemMaker::Draw` at `0x7d4980`
- `CUIItemMaker::OnButtonClicked` at `0x7d5dc0`
- `CUIItemUpgrade::Draw` at `0x7c0100`
- `CUIItemUpgrade::OnButtonClicked` at `0x7c0ca0`
- `CUIMapTransfer::Draw` at `0x7df5c0`
- `CUIMapTransfer::OnButtonClicked` at `0x7e0ed0`
- `CUIEquip::Draw` at `0x7aa560`
- `CUIQuestInfo::Draw` at `0x82a780`
- `CUIQuestInfoDetail::Draw` at `0x8248c0`
- `CUIQuestInfoDetail::OnButtonClicked` at `0x834c90`
- `CUIQuestAlarm::Draw` at `0x82d850`
- `CUIStatusBar::OnButtonClicked` at `0x880540`
- `CUIMacroSysEx::OnCreate` at `0x85c420`
- `CUISoftKeyboard::GetKeyIndexFromPoint` at `0x85faa0`
- `CUISoftKeyboard::OnMouseMove` at `0x85fba0`
- `CUISoftKeyboard::HitTest` at `0x85fc50`
- `CUISoftKeyboard::UpdateKeyConstraint` at `0x85fd70`
- `CUIUserInfo::Draw` at `0x8b3a50`
- `CUIUserInfo::OnButtonClicked` at `0x8b6d80`
- `CBookDlg::OnCreate` at `0x471f00`
- `CBookDlg::Draw` at `0x473360`
- `CBookDlg::OnButtonClicked` at `0x474300`
- `CUISkillMacro`
- `CWvsContext::OnDropPickUpMessage`
- `CUIScreenMsg::ScrMsg_Add`

Notes:
`CUIItem::Draw` confirms that the client inventory window owns meso text, per-slot icon and quantity drawing, item-grade frames, disabled-slot overlays, and special overlays such as the active bullet marker. `CAdminShopDlg::OnCreate`, `CAdminShopDlg::Draw`, and `CAdminShopDlg::OnButtonClicked` confirm a dedicated cash-service shop dialog rather than treating cash-shop and MTS access as simple status-bar button stubs. `CUIChannelSelect::OnCreate`, `CUIChannelSelect::Draw`, `CUIChannelSelect::OnButtonClicked`, `CUIWorldSelect::OnCreate`, and `CUIWorldSelect::OnButtonClicked` confirm that the status-bar channel path fans out into explicit world and channel selector owners rather than a generic toggle, while `CUIChannelShift::OnCreate` and `CUIChannelShift::Draw` keep a separate shift or transition surface in play. `CUIItemMaker::Draw` and `CUIItemMaker::OnButtonClicked` confirm a dedicated crafting or maker window rather than burying production flow inside generic inventory handling. `CUIItemUpgrade::Draw` and `CUIItemUpgrade::OnButtonClicked` confirm a separate item-enhancement surface rather than treating scroll or upgrade flow as a generic inventory tooltip action. `CUIMapTransfer::Draw` and `CUIMapTransfer::OnButtonClicked` confirm a dedicated map-transfer window distinct from the minimap shell. `CUIEquip::Draw` confirms that equip-window parity is tied to slot-disable rules, expiration and durability overlays, and job or subjob gating rather than just slot placement. `CUIQuestInfo::Draw`, `CUIQuestInfoDetail::Draw`, and `CUIQuestAlarm::Draw` confirm that quest progression parity includes a separate detail pane with dedicated button handling, category-button rebuilds, scroll-driven list rebuilding, quest-delivery requirement rendering, condition-state coloring, and a standalone quest-alert window rather than only the quest log shell. `CUIStatusBar::OnButtonClicked` confirms the utility-button cluster is a real interaction owner instead of only decorative art. `CUIMacroSysEx::OnCreate` together with `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::OnMouseMove`, `CUISoftKeyboard::HitTest`, and `CUISoftKeyboard::UpdateKeyConstraint` confirms that client text-entry parity is not just committed character handling: the macro family also owns an explicit soft-keyboard helper with hover, hit-test, and per-mode key-constraint behavior. `CUIUserInfo::Draw` and `CUIUserInfo::OnButtonClicked` confirm a separate character-information or profile surface beyond the stat window itself.

### 2. Local utility and event packet parity

- `CUserLocal::OnPacket` at `0x9340c0`
- `CUserLocal::OnOpenUI` at `0x9055f0`
- `CUserLocal::OnOpenUIWithOption` at `0x932300`
- `CUserLocal::OnGoToCommoditySN` at `0x905760`
- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`

Notes:
`CUserLocal::OnPacket` is a distinct client seam rather than a random collection of callbacks. It dispatches packet-owned local utility behavior including `OnOpenUI`, `OnOpenUIWithOption`, `OnGoToCommoditySN`, field fade in/out, balloon messages, damage-meter timing, chat/notice surfaces, quest-guide and delivery-quest results, buff-zone effects, field HP-dec notices, tutor flows, and several event-sound/minigame-sound owners. The narrower decompiles already show concrete responsibilities that are not represented well by the existing window rows: `OnOpenUI` forwards a raw UI id into `CWvsContext::UI_Open`, `OnOpenUIWithOption` adds option-driven branches such as party-search requests and the repair-durability owner, `OnGoToCommoditySN` stores a commodity serial number before calling `SendMigrateToShopRequest`, `OnFieldFadeInOut` registers a fade animation relative to the avatar layer Z, `OnBalloonMsg` can bind a balloon to the avatar origin or an explicit world position, and `OnDamageMeter` resets a dedicated timer surface. That means the client has a packet-driven utility/event layer that sits between local gameplay and the specific window owners already tracked above.

### 3. Packet-authored local overlays and guidance parity

- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`
- `CUserLocal::OnNotifyHPDecByField` at `0x90fed0`
- `CUserLocal::OnQuestGuideResult` at `0x90f1e0`
- `CUserLocal::OnDeliveryQuest` at `0x90ef60`
- `CUserLocal::OnOpenClassCompetitionPage` at `0x905580`

Notes:
These handlers form a narrower client-owned feedback layer under `CUserLocal::OnPacket`, and the current backlog only references them inside the broad dispatcher row. IDA now confirms concrete responsibilities that deserve separate tracking: `OnFieldFadeInOut` decodes fade-in, fade-out, and duration fields then registers the animation relative to the avatar layer Z; `OnBalloonMsg` decodes text, width, lifetime, and either avatar-relative or explicit world-position placement before calling `ShowBallonMsg`; `OnDamageMeter` resets the dedicated damage-meter timer owner and updates shared timing state in `CWvsContext`; `OnQuestGuideResult` rebuilds quest-demand targets and opens the world-map quest-guide owner when the packet contains viable quest-mob targets; `OnDeliveryQuest` either opens the dedicated delivery-quest owner or routes the result back through the status-bar chat seam when another unique modeless owner is active; and `OnOpenClassCompetitionPage` is a packet-driven utility launch rather than a normal button path.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.
- Progression or utility window groundwork now has a shared seam: named owners for world map, map transfer, cash-service, channel-selection, crafting, enhancement, character-info, and quest-side utility windows are registered with `UIWindowManager`, while minimap and status-bar utility buttons now route into those owners instead of staying entirely dead-ended. The world/channel path now opens dedicated simulator-side `WorldSelect`, `ChannelSelect`, and timed `ChannelShift` surfaces backed by `UIWindow2.img/Channel` art, with the channel grid, centered placement, per-channel load feedback, login-step request gating, delayed `CheckUserLimitResult` / `SelectWorldResult` style request flow, and simulator-side `WorldInformation` / `RecommendWorldMessage` / `LatestConnectedWorld` metadata overlays now aligned to the client-owned selector layout instead of remaining a placeholder-only launch seam. The map-transfer owner now also keeps saved teleport destinations in a character-scoped destination book keyed to the active player or selected login-roster character instead of sharing one global saved list across every simulator avatar, persists that destination book under the simulator AppData profile across restarts, and hands the currently selected transfer target into the world-map owner when `BtMap` is used so the two navigation surfaces stay linked.

## Remaining Parity Backlog

### 1. Progression and utility window parity

This area is currently absent from the backlog set even though the simulator already has concrete window and feedback implementations.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Minimap window parity | The minimap now loads WZ-backed helper markers for generic NPCs, quest-start NPCs, quest-end NPCs, portals, out-of-bounds direction arrows, and the broader `DefaultHelper` remote-user families from `UIWindow2.img/MiniMapSimpleMode/DefaultHelper`; it upgrades the post-Big-Bang generic NPC notice marker plus off-map direction overlays to the animated `UIWindow2.img/MiniMap/iconNpc` and `MiniMap/iconDirection/*` frame families when those client assets are present, feeds visible portal markers plus quest-aware NPC marker families into `MinimapUI`, keeps the post-Big-Bang `BtNpc` list panel from `ListNpc`, and now also feeds same-field simulator social/family entries into the minimap as `another` / `friend` / `party` / `partymaster` / `guild` / `guildmaster` helper markers, reusing family-chart positions when the simulator has them and otherwise assigning deterministic local field positions so those helper families can render with matching off-map direction overlays instead of staying dead data. `BtBig` still opens a dedicated `WorldMap` owner whose `WorldMapSearch` side panel is no longer just background art: `BtSearch` switches into a dedicated WZ-backed search surface with `BtAllsearch` return flow, optional `BtLevelMob` mob-only filtering, live field/NPC/mob result rows sourced from the current simulator field, and a working text-entry search path that filters those result families plus global field-name or map-id matches, keeps search-row selection distinct even when multiple rows resolve to the same field, supports keyboard navigation or activation, accepts clipboard paste into the search box, now uses the client `UIWindow2.img/WorldMap/title` canvas instead of a text-only heading, keeps the search caret and visible query text clipped inside the edit box when the query overruns the field, lets mouse clicks reposition the caret inside that visible query span, and adds word-wise `Ctrl+Backspace` / `Ctrl+Delete` editing on top of the existing keyboard path, while `BtAll` / `BtAnother` browsing, current-map focus, paged field selection, and direct map-transfer queuing still run through the existing pending-map seam. The remaining gap is that the simulator still does not mirror the client's packet-authored remote-user field state, exact social/trader/match helper semantics, or the still-missing `DefaultHelper` families that depend on those richer runtimes, and it still falls short of the client's exact IME/edit-control behavior and the last presentation polish inside that search owner | Navigation feedback now has animated minimap-owned notice and direction art, quest- and portal-aware markers, plus a broader WZ-backed remote-user helper surface instead of limiting `DefaultHelper` to NPC/quest/portal art, while the world-map search owner continues to behave more like a real client-owned edit control with WZ title art, clipped query rendering, mouse caret placement, and word-wise deletion; the remaining gap is narrowed to true packet-authored remote-user state and the client's exact IME or polish fidelity rather than missing helper-family wiring or a static search panel | `MinimapUI.cs`, `WorldMapUI.cs`, minimap/window loader path |
| Partial | Map transfer / navigation window parity | The simulator now routes minimap `BtMap` into a dedicated WZ-backed `MapTransfer` owner built from the `Teleport*` window art, constrains the destination surface to the client's five visible rows, caps saved teleport destinations to a client-shaped five-vs-ten slot budget based on the loaded `Teleport` vs `Teleport3` art variant, respects teleport-item field limits separately from generic migration checks, layers return-map plus portal-derived routes on top of the saved list while driving actual map changes through the existing pending-map seam instead of stopping at the minimap shell, keeps saved teleport destinations in a character-scoped destination book keyed to the active player or selected login-roster character instead of one simulator-global list, persists that per-character destination book under the simulator AppData profile across restarts, opens the world-map owner focused on the currently selected transfer target rather than always resetting to the current field, preserves fixed per-character slot indices instead of compacting into a generic list, renders empty saved slots inside the transfer window, keeps the selected saved slot alive through the world-map handoff so a `[Target]` selection can be written back into that exact slot through `BtRegister`, now draws the client-shaped saved-count header instead of treating the list like an unbounded generic scroller, resolves saved-slot labels from map IDs rather than persisting simulator-only custom names, exposes the client-owned edit-target lane as a local numeric map-id entry path with 12-digit input, focus-driven selection clearing, paste support, and direct `BtMove` routing when no list row is selected, now loads the client `FadeYesNo/backgrnd7` plus `BtOK` / `BtCancel` confirmation chrome so register, delete, saved-route, portal-route, and manual map-id requests all pass through a local yes/no confirmation step instead of mutating or moving immediately, and now applies the client-shaped saved-destination category gate from `CUIMapTransfer::OnRegister` so sub-`100000000` ids and `xx9xxxxx` map families cannot be written into saved teleport slots while duplicate-save attempts refocus the already-owned slot instead of silently pretending to create a second entry, but it still does not mirror the client's full `CUIMapTransfer` behavior surface such as live packet-backed `CharacterData::adwMapTransfer` / `adwMapTransferEx` sourcing, the exact official request/response path behind `CWvsContext::SendMapTransferRequest`, a visible client-owned scrollbar path for the 10-slot continent variant, or any deeper unsaveable rules beyond the category gate now mirrored locally | Navigation testing can now exercise a more client-shaped transfer and destination-selection surface above raw portal travel, saved destinations no longer bleed across unrelated simulator characters, the saved-slot header and raw map-id presentation are closer to the official owner, the edit target no longer behaves like dead art, world-map retargeting now lands in the intended saved slot instead of collapsing back into a generic append path, saved-slot registration now follows the same broad category gate the client applies before writing `adwMapTransfer` / `adwMapTransferEx`, and register/delete/move requests now all pass through the same WZ-backed yes/no chrome instead of firing immediately; the remaining gap is narrowed to live character-data population, packet-authored updates, the 10-slot variant's client scrollbar treatment, and the deeper destination-validation rules before it fully matches the client flow | navigation/UI layer, minimap integration, destination-selection flow (`CUIMapTransfer::Draw`, `CUIMapTransfer::OnButtonClicked`, `CUIMapTransfer::OnCreate`) |
| Partial | Cash shop / admin shop dialog parity | The simulator now replaces the generic Cash Shop and MTS placeholders with a dedicated `AdminShopDialogUI` owner backed by `UIWindow2.img/Shop`, reuses the shop dialog frame plus `BtBuy` / `BtSell` / `BtExit` / `BtRecharge`, keeps Cash Shop and MTS inside the same button-driven surface, routes the status-bar launchers so only the requested service window stays open, exposes separate scrollable NPC-offer and user-listing panes with per-row selection and request submission messaging, consumes the client `UIWindow2.img/Shop/TabShop` category strip as a real WZ-backed catalog filter, now also consumes the owner `TabBuy` and `TabSell` tab canvases with their client WZ offsets instead of leaving the service and pane strips as text-only chrome, mirrors the client-owned twin scrollbar layout called out by `CAdminShopDlg::OnCreate`, replaces the wish-list footer prompt with a `FadeYesNo`-style confirmation modal before saving a wish-list entry, syncs the displayed balance to the live inventory meso runtime like `CAdminShopDlg::Draw`, now renders WZ-backed item icons plus stack counts for requestable rows in both panes and the detail area so the dialog no longer behaves like a text-only catalog despite `CAdminShopDlg::DrawNPCItem` / `DrawUserItem`, resolves simulator-side shop requests through a broader local session matrix with meso consumption, inventory-capacity checks, actual item delivery for requestable rows, post-purchase sold-out locking on one-shot listings, rejected outcomes such as seller-offline, expired, inventory-full, and refreshed-sold-out responses, plus real icon carry-through on delivered items instead of treating every non-expansion row as the same generic acknowledgment, now interprets the left browse strip as the real WZ-backed `ALL` / `MOST` / `SELL` / `BUY` / `RE-BUY` family for Cash Shop while MTS swaps to its own WZ-backed `ALL` / `MOST` / `SELL` / `BUY` subset from `UIWindow2.img/Shop/TabSell`, and keeps the right `TabSell` strip acting as WZ-backed quick category tabs (`Equip` / `Use` / `Etc` / `Set-up` / `Cash`) instead of fake Cash Shop or MTS toggles, with featured-row curation for `MOST`, pane focus changes for `SELL` and `BUY`, and in-session wish-list plus delivered-row state restoration so `RE-BUY` can surface previously granted rows after page flips, and now also supports owner-local keyboard parity for the current browse surface: `Up` / `Down` / `PageUp` / `PageDown` / `Home` / `End` walk the active pane, `Tab` / `Left` / `Right` swap between the NPC-offer and user-listing panes, `Enter` submits the highlighted request, `Space` opens the wish-list confirmation when supported, and `Enter` / `Escape` resolve the `FadeYesNo` modal without forcing mouse-only interaction. It still does not mirror the full `CAdminShopDlg` behavior surface such as packet-backed shop sessions, live server-populated catalogs, the exact official semantics behind each client tab state, or the server-authored request and trade matrix behind those controls | The simulator can now exercise the client cash-service launch path through a dedicated dialog owner with concrete per-item browsing, mode-specific WZ-backed browse chrome for Cash Shop versus MTS, client-shaped scrollbar and category-tab affordances, visible item art in the browse and detail surfaces, a real wish-list confirmation step, live balance readout, a keyboard-drivable selection and request flow, and a more varied local request-result loop instead of a dead-end placeholder; the remaining gap is narrowed to true packet/live-catalog sourcing, the deeper official meaning of each tab state, and the server-authored session rules inside that owner rather than shared browse art, unlabeled placeholder tabs, or a mouse-only dialog | cash-service UI layer, status-bar integration, shop-session flow (`CAdminShopDlg::OnCreate`, `CAdminShopDlg::Draw`, `CAdminShopDlg::OnButtonClicked`) |
| Partial | World and channel selector parity | The simulator now routes `BtChannel` into dedicated `WorldSelect`, `ChannelSelect`, and timed `ChannelShift` windows, uses `UIWindow2.img/Channel` world badges and channel art for the local selector stack, preserves a visible current world/channel choice for the simulator session, expands the world list to the full badge set, aligns the channel grid and window centering to the client `GetRect`/owner layout, simulates per-world channel availability plus busy/full load bars so invalid targets no longer behave like always-open buttons, mirrors the client-owned request shape more closely by disabling selector input outside the login `WorldSelect` step and resolving world/channel picks through delayed `CheckUserLimitResult` / `SelectWorldResult` style simulator responses instead of committing immediately, lets login-scene `WorldInformation`, `RecommendWorldMessage`, and `LatestConnectedWorld` packets drive selector-side world load data, recommended-world markers, latest-world focus, and immediate UI refresh instead of leaving those packet owners as message-only stubs, carries simulator-side adult-channel gating, denied `CheckUserLimitResult` / `SelectWorldResult` style result messaging, plus low-frequency live population drift across login-scene world metadata so the selector surface no longer behaves like a fixed always-successful snapshot, now also accepts client-shaped `WorldInformation` payload bytes through the existing login-packet seam, decodes the `CLogin::OnWorldInformation` packet layout into per-world channel user counts and adult-channel flags, supports incremental worldinfo accumulation plus the client terminator packet instead of only one-shot synthetic profile overrides, preserves those packet-authored per-channel populations against local drift so repeated server-authored selector updates can refresh the UI without being immediately replaced by simulator heuristics, and now exposes the existing login loopback transport as an explicit `/loginpacket inbox` control surface with configurable listen port plus parser support for raw `<packet> <args>`, `<packet>:<args>`, and `<packet>=<args>` feed shapes so selector-side world updates no longer depend on chat-only injection formatting. The same selector seam now also accepts packet-authored `RecommendWorldMessage`, `LatestConnectedWorld`, and `CheckUserLimitResult` payload bytes directly, and the inbox/chat path can collapse relay-style raw hex payload fragments for `WorldInformation`, `RecommendWorldMessage`, and `SelectWorldResult` into the existing decoder flow instead of requiring every external source to pre-wrap them as `payloadhex=...`. The selector now mirrors the client-observed `SelectWorldResult` rejection families from `CLogin::OnSelectWorldResult` plus the `CheckUserLimitResult` code-2 over-user-limit path from `CUIWorldSelect::UserLimitResult`, including the grouped login-error branches and the code-7 title return instead of collapsing every non-success packet into one generic rejection, but it still does not capture or decrypt the client's authenticated upstream login traffic on its own and still relies on an external relay/sniffer to source true live server population packets | Channel switching is no longer a dead-end HUD affordance, selector population can now follow repeated client-shaped `WorldInformation` updates through a built-in configurable loopback transport and direct payload-byte decoding for the other selector packets instead of only manual command injection, and the remaining gap is narrowed to sourcing and decrypting the real upstream login feed rather than missing selector surfaces, packet decoding, or lacking a practical relay-facing transport seam inside the simulator | world-selection and channel-selection UI layer, status-bar integration (`CUIWorldSelect::OnCreate`, `CUIWorldSelect::OnButtonClicked`, `CUIChannelSelect::OnCreate`, `CUIChannelSelect::Draw`, `CUIChannelSelect::OnButtonClicked`, `CUIChannelShift::OnCreate`, `CUIChannelShift::Draw`, `CLogin::OnCheckUserLimitResult`, `CLogin::OnSelectWorldResult`, `CUIWorldSelect::UserLimitResult`, `CLogin::OnWorldInformation`) |
| Partial | Inventory window parity | Legacy and Big Bang inventory windows now render right-aligned meso text, per-slot item icons and stack counts, WZ-backed grade and quality markers, disabled-slot overlays, active-slot highlighting, and Big Bang tab or expanded layouts, while the simulator also feeds meso and picked-up items into the inventory surface at runtime, resolves equip pickup icons through `Character/*/*.img` like the client asset families, respects `info/slotMax`-style stack splitting, merge limits, and pickup-capacity checks for stackable items, wires `BtGather` and `BtSort` into simulator-side stack compaction and ordering, shows shared client-style hover tooltips with cached item names, type labels, stack data, and descriptions across both legacy and Big Bang inventory layouts, supports client-shaped intra-inventory drag/drop for occupied-slot swapping, stack merging, trailing-empty-slot moves, plus a held-item cursor overlay in both layouts, and now carries client-shaped slot-unlock progression through per-tab slot limits that grow in 4-slot rows up to a capped 96-slot budget while `BtCashshop` routes directly into the simulator cash-service dialog and its `Extending Equip/Use/Set-Up/Etc Inventory` offers consume live meso balance to unlock additional rows instead of leaving capacity as a fixed 24-slot shell; right-click setup-chair items now route into the existing portable-chair runtime so inventory use can activate or clear owned chairs without falling back to chat commands, WZ-backed chair `info/recoveryHP` / `recoveryMP` values now tick against the seated player on a client-shaped 10-second cadence, pet-food items now honor `Consume/*/spec/*` supported-pet families plus the pet runtime's live fullness gate when selecting the target runtime, only consume when a compatible summoned pet can actually eat the food, and surface simulator-side failure feedback when no valid target is available instead of silently burning or dead-ending the click, `Consume/*/spec/moveTo` scrolls now consume through the inventory surface to queue either fixed-map warps or `999999999` nearest-town returns through the existing map-transfer seam instead of remaining unsupported right-clicks, and the same WZ-backed consume path now also applies client-shaped abnormal-status cures, timed morph consumables from `Consume/0221` via `spec/morph` and weighted `spec/morphRandom`, plus temporary stat buffs by honoring `spec` flags such as `seal` / `darkness` / `weakness` / `poison` / `curse` / `painmark` and timed `pad` / `mad` / `pdd` / `mdd` / `acc` / `eva` / `speed` / `jump` entries like `2050004` and `2022179` through the shared player-status, avatar-transform, and buff runtimes instead of treating those right-clicks as dead inventory actions; that shared WZ-backed consume seam is now also reusable from the cash tab when an item exposes the same recovery/moveTo/morph/cure/buff `spec` schema instead of being hard-wired to `USE` only, and the corresponding `MobSkill.img/134` pain-mark disease now lands in the same player-status controller with WZ `hp` / `interval` / `count` timing so those cure items clear a live runtime effect instead of a dead flag | The remaining gap is narrowed to broader client-authenticated item-use rules for non-chair setup items, pet-feeding fidelity beyond the current supported-family plus fullness/runtime routing (notably command or closeness progression and exact official success or failure messaging), and the wider consume-item matrix beyond the simulator's current HP/MP, moveTo, cure, morph, timed disease, and temporary-stat coverage, plus stricter server-side validation rather than missing inventory rendering, drag/drop, tooltip, pickup-capacity, slot-progression, or the common local item-use loop itself | `InventoryUI.cs`, `InventoryUIBigBang.cs`, `UIWindowLoader.cs`, `AdminShopDialogUI.cs`, inventory/runtime integration (`CUIItem::Draw`, `CUIItem::OnMouseButton`) |
| Partial | Item maker / crafting window parity | The simulator now replaces the `ItemMaker` placeholder with a dedicated WZ-backed `Maker` window owner, exposes `BtStart` / `BtCancel` crafting flow, animates the maker gauge during crafting, seeds a starter ore inventory plus meso and catalyst stock, resolves weighted `randomReward` branches, blocks crafts whose fixed or random reward branch cannot fit in inventory, routes NPC talk overlays whose loaded `String/Npc.img` function text advertises item-maker families such as `Item Maker`, `Glove Maker`, `Shoemaker`, and `Toy Maker` into the dedicated crafting owner, and now rebuilds the selector surface around client `CUIItemMaker` rules by deriving the visible `Etc/ItemMake.img` bucket set from the live job (`0` plus the matching `1` / `2` / `4` / `8` / `16` job bucket, or all five for admin-family jobs), regrouping those recipes into client-like category and item selectors instead of the old raw bucket picker, adding a leading `All` selector plus client-shaped aggregate pages for `Gem`, `Monster Crystal`, `Use`, `Setup`, and `Etc`, hiding selector entries that fail the client-side precondition pass for `reqLevel`, `reqSkillLevel`, `reqItem`, `reqEquip`, and `reqQuest` before the detailed craft validation runs, applying a local hidden-maker-list pass so maker-family NPC launches such as glove, shoe, toy, and generic item-maker flows narrow the visible recipe set instead of only focusing the nearest matching row, and now also backs the owner with a simulator-side per-character maker progression store that tracks family-specific mastery levels for generic/glove/shoe/toy crafting, advances that state on successful crafts, persists it under the simulator AppData profile, uses that family mastery rather than the raw craft trait alone when evaluating `reqSkillLevel`, correctly reloads the per-character discovered-recipe list from that AppData profile across simulator restarts, keeps recipes that passed the selector precondition sweep visible on later visits even after transient `reqItem` / `reqEquip` / `reqQuest` conditions drop back out, aligns recipe hidden-vs-normal classification more closely with the client `CItemMakerInfo::RegisterTargetItem` split by honoring the WZ `hide` flag instead of treating every `reqItem` recipe as implicitly hidden, and surfaces the client-observed `DrawTargetItem` / `DrawRecipe` / `DrawGem` / `DrawCatalyst` / `DrawMeso` split more faithfully by drawing item-result and material icon rows through the live inventory/runtime icon seams, but it still lacks the official hidden-list unlock decisions and broader profession packet/session model authored by the client `CItemMakerInfo` runtime | Production and crafting progression now has a real client-backed launch path, client-shaped category and item selection, job-aware recipe exposure, family-focused NPC launch selection, AppData-backed family mastery plus recipe discovery that now survives reload, tighter precondition plus result-capacity validation, a client-backed hidden-vs-normal recipe split, and a more client-shaped recipe/result presentation instead of a dead-end placeholder, while the remaining gap is narrowed to the official `CItemMakerInfo` hidden-list unlock/session rules and the broader profession runtime the simulator still does not model | inventory/runtime integration, crafting UI layer (`CUIItemMaker::Draw`, `CUIItemMaker::OnCreate`, `CUIItemMaker::SetComboBox_Category`, `CUIItemMaker::SetComboBox_Item`, `CUIItemMaker::OnButtonClicked`) |
| Partial | Item upgrade / enhancement window parity | The simulator now registers a dedicated `ItemUpgrade` owner instead of a placeholder, loads Gold Hammer or Vicious Hammer style client art from `UIWindow2.img` or `UIWindow.img`, binds the window to the live `CharacterBuild` plus inventory runtime, seeds starter enhancement scroll stock so the loop is immediately exercisable, resolves eligible items from WZ-backed `info/tuc` upgrade-slot data instead of a pure slot-type guess, consumes enhancement scrolls from the inventory surface, tracks per-item remaining upgrade slots and success or fail counts, applies lightweight stat growth back into `CharacterBuild`, routes right-click activation from supported enhancement consumables in the `USE` and `CASH` inventory tabs plus eligible equips in both legacy and Big Bang equipment windows into the dedicated owner while preselecting the clicked consumable or equipped target, now lets NPC interaction overlays whose loaded `String/Npc.img` function text advertises item-upgrade, enhancement, hammer, potential, or cube families open the dedicated owner, broadens the local enhancement loop beyond the baseline normal or advanced scroll pair by seeding and resolving the client `2049300/1/3/4/5/6/7/8/9` enhancement-scroll family including 2-star through 5-star result variants with multi-star success gains and destroy-on-fail handling, now adds local potential-scroll plus Miracle Cube result handling through `2049400/1/6`, `2049407/8`, `2049700/1/2/3`, and the broader WZ-backed cash family `5062000/1/2/3/4/5`, seeds and resolves `2049500/1`, `5534000`, and `5610000/1` so carved seals, Urete's Time Lab, and Vega's Spell consumables are immediately exercisable from inventory, preserves existing revealed potential lines when a carved seal adds an extra line instead of rerolling the whole set, now mirrors the WZ-described Epic Potential Scroll 80% failure behavior by treating destruction as a 20% on-fail roll instead of unconditional breakage, keeps Golden and Revolutionary Miracle Cube fragment grants attached to the specific cash items that advertise them, enforces the WZ-described Golden Miracle Cube Rare-through-Unique limit, now routes `5610000/1` into a dedicated `VegaSpell` owner loaded from `UIWindow.img/VegaSpell` instead of keeping Vega inside the shared item-upgrade surface, reuses the client `backgrnd10` / `backgrnd60`, count glyphs, success/fail panes, and `EffectTwinkling` / `EffectSpelling` / `EffectArrow` / `EffectSuccess` / `EffectFail` families for a separate local cast-and-result presentation, and tightens modifier-aware scroll resolution so Vega 10% and 60% consistently pair with the compatible advanced-vs-normal enhancement-scroll families before the backend attempt runs, but it still lacks the broader official server-authored scroll and cash-item result matrix outside this supported family, the remaining fragment or reward variants beyond the Golden/Revolutionary pair already covered here, and the deeper exact client-side Vega session rules beyond this local owner-backed presentation | Progression-sensitive item workflows now have a visible dedicated enhancement surface, live inventory consumption, client-shaped inventory, equip, and NPC launch shortcuts, a broader enhancement-scroll and potential-scroll result matrix, WZ-backed partial-destruction and fragment-result handling for the currently supported items, preserved seal behavior, and a separate WZ-backed Vega cast/result owner instead of treating Vega as only another modifier inside `ItemUpgrade`, while the remaining gap is narrowed to unsupported server-authored item-result families and exact official Vega session rules rather than the already-wired local scroll, seal, cube-fragment, and basic Vega presentation path | inventory/runtime integration, enhancement UI layer (`CUIItemUpgrade::Draw`, `CUIItemUpgrade::OnButtonClicked`) |
| Partial | Equipment window parity | Legacy and Big Bang equip windows now share `EquipSlotStateResolver`-driven disable handling for overall-vs-pants conflicts, beginner subjob and two-handed shield gating, Monster Riding slot gating, expired-item overlays, and zero-durability overlays, stay bound to the live `CharacterBuild` used by the avatar preview, and support simulator-side drag or drop between compatible character slots with held-item cursor feedback plus compare-style hover messaging while an equipped item is being moved; the Big Bang owner now also accepts inventory-to-character equips on the live character tab, returns displaced equips back to inventory with their item icons, stops mutating cached loader templates when slot moves retarget cloned `CharacterPart` instances, maps the heart slot onto the live android-heart family, and covers the broader client-facing slot families already exposed by the window art for pendant1 vs pendant2, pocket, badge, shoulder, android, android heart, medal, and belt-backed character panes. `CharacterBuild` now preserves concealed base equips under visible cash equips through a dedicated hidden-equipment layer, `CharacterLoader` no longer drops `LoginAvatarLook` hidden entries when a visible cash item already owns that slot, `LoginAvatarLookCodec` round-trips that visible-versus-hidden split, build stat totals plus weapon-sensitive shield gating can still see the concealed stat-bearing item, and both equip owners now surface the hidden base item in tooltip and drag messaging while restoring it when the visible cash item is moved off the slot. The Big Bang owner now also loads and draws the client-authored `UIWindow2.img/Equip/character/cashPendant` and `charmPocket` chrome when the live build activates pendant2 or pocket availability, and both local slot-to-slot moves plus inventory-to-character equips now route through `CharacterBuild.PlaceEquipment` so concealed base items stay hidden under cash equips and only the correct displaced base layer returns to inventory. The remaining gap is the exact client toggle and placement rules for every long-tail equip-window chrome variant beyond these authored pendant or pocket states, plus the deeper packet-authored validation that still surrounds official equip changes | Equipment-heavy testing now has visible disable and break-state feedback on the actual character equipment surface, a client-shaped held-item interaction path, live inventory-to-equip routing on the Big Bang character page, broader slot-family coverage for the non-rendered accessory families already present in the client window art, the client-authored pendant and pocket chrome families when those build states are active, and a real client-shaped visible-versus-hidden equip data path instead of dropping concealed base gear from avatar-look and equipment-window resolution or returning the wrong concealed base item during local swaps; the remaining gap is narrowed to long-tail chrome toggles and server-authored equip validation rather than missing authored slot chrome or the main local cash-layer swap semantics | `EquipUI.cs`, `EquipUIBigBang.cs`, `CharacterBuild`, `CharacterLoader`, `CharacterAssembler`, `EquipSlotStateResolver.cs` (`CUIEquip::Draw`) |
| Partial | Pet, dragon, and android equipment tabs | The Big Bang equip window now loads the WZ-backed companion pane art for `pet`, `dragon`, `mechanic`, and `Android`, keeps the character equipment surface visible while those panes are open, highlights the active tab buttons, populates the Multi Pet pane from the live `PetController` with per-pet icons plus auto-loot markers and tooltips, seeds dedicated dragon and android companion equipment runtimes so those pages render live equipped-item icons and tooltips on the client slot geometry, now also seeds a dedicated mechanic equipment runtime from the client `Character/Mechanic` asset family so the mechanic page draws live `ENGINE`, `FRAME`, `TRANS`, `ARM`, and `LEG` icons with slot-specific tooltips instead of stopping at an empty-state stub, and now accepts drag/drop from the live equip inventory into the pet, dragon, mechanic, and android panes with slot-family validation, client-shaped android overall-vs-pants gating, occupied-slot replacement back into inventory, a WZ-backed `Character/PetEquip` loader for live pet accessory icons and tooltips, seeded starter pet accessory inventory entries, and drag-back-out handoff from every companion pane into the live inventory window; pet slots can now accept simulator-side pet accessories, render the equipped accessory in-slot with pet-aware tooltip context, and swap that accessory across active pets instead of remaining a pet-icon-only stub, and the shared companion validation seam now also reads WZ `reqLevel`, `reqJob`, `reqSTR`, `reqDEX`, `reqINT`, `reqLUK`, and `reqPOP` metadata for pet/dragon/mechanic items, surfaces reject messages through the same blocked-equip notice path the main equip window already uses, preserves pet-specific compatibility checks during pet-to-pet accessory swaps, and requires both an equipped android and equipped heart before the Android page accepts gear instead of treating the android slot alone as sufficient ownership. The remaining gap is that the simulator still does not mirror the client's deeper cash-state and account/character ownership rules, android-to-heart family compatibility matrix, or packet-authored companion-inventory validation / server rejection flow that surrounds official companion equipment changes | Companion tab clicks are no longer dead-end toggles, the pet page now participates in the same inventory-backed equip loop as the other companion panes, invalid companion drops now explain why they were rejected instead of failing silently, and android ownership gating is closer to WZ-backed client expectations; the remaining gap is narrowed to deeper cash/ownership semantics, exact android heart-family matching, and packet-authored validation rather than missing pet equip handling, a blank mechanic page, or one-way drag/drop | `EquipUIBigBang.cs`, `UIWindowLoader.cs`, `PetLoader.cs`, `PetController.cs`, `CompanionEquipmentController.cs`, inventory/window manager integration (`CUIEquip::Draw`) |
| Partial | Ability and stat window parity | The simulator already draws both legacy and Big Bang stat windows, supports AP assignment, job-biased auto-assign, detail-panel toggles, build-backed guild/fame/EXP text, hands/critical detail values, per-stat AP cap enablement, and Big Bang open-vs-close detail button visibility; the stat rows now also resolve equipped stat bonuses through `CharacterBuild`, the detail pane now derives attack/defense, accuracy/avoidability, hands, critical, speed, and jump from live build totals instead of static placeholder fields, `SkillManager` now seeds non-hidden passive books alongside active skills for the current job path, and `CharacterBuild` now folds passive plus live buff attack/defense, magic attack/defense, accuracy, avoidability, critical-rate, speed, jump, and skill-driven max HP/MP bonuses into those totals so the stat windows react to learned skills rather than gear alone, while Big Bang HP/MP AP clicks still use class-aware gain ranges with a 30,000 cap instead of a flat `+20`; the displayed physical and magical damage rows now also use a client-shaped min/max average where mastery scales the primary term instead of naively scaling the full max value, dagger formulas now split thief-vs-non-thief primary stats instead of treating every dagger as a thief weapon, extended Cygnus/hero/Resistance job branches now map back onto their base warrior or mage or archer or thief or pirate stat archetypes for accuracy, avoidability, defense, and Big Bang HP/MP AP growth, `CharacterBuild` now recognizes cane, dual bowgun, and cannon weapon families in the displayed damage surface, and `SkillLoader` plus `SkillManager` now use `String/Skill.img` mastery names together with broader passive alias-field parsing (`accX`, `padX`, `madX`, `pddX`, `mddX`, `evaX`) plus enhanced-stat aliases (`epad`, `emad`, `epdd`, `emdd`, `emhp`, `emmp`) so newer mastery, expert, blessing, and shield-style books can feed the stat windows instead of being dropped when WZ does not use the older base property names; `SkillLoader` now also evaluates WZ-authored max-HP/max-MP rate fields such as `skill/100.img/skill/1000006/common/mhpR = 2*x`, `skill/200.img/skill/2000006/common/mmpR = 2*x`, `skill/510.img/skill/5100009/common/mhpR = 2*x`, and buff-side `indieMhpR` / `indieMmpR` branches such as `skill/2411.img/skill/24111003/common/indieMhpR` so the stat windows include percentage-based HP/MP books instead of only flat `emhp` / `emmp` bonuses, and `CharacterBuild.TotalHands` now also includes equipped `incCraft` bonuses instead of dropping glove or gear hands from the detail panel. Mechanic's `Extreme Mech` bonuses now only surface while the live mech mount is active instead of behaving like an unconditional passive, weapon-specific mastery no longer resolves while no weapon is equipped, and non-Mechanic alias-backed buffs such as Onyx or Divine Shield style `e*` stat lines now also contribute to the live totals, but the simulator still does not mirror every exact post-Big-Bang coefficient or long-tail conditional mastery or formula branch for the remaining newer-family and per-job edge cases | Visible stat windows now expose runtime-backed totals from equipped gear, learned passive skills, active buff bonuses, class-sensitive HP/MP AP growth across both classic and extended job branches, WZ-backed percentage HP/MP skill boosts, the missing `incCraft` hands contribution, a broader client-shaped mastery-aware damage surface for cane, dual-bowgun, and cannon users, and a wider WZ-backed alias-field pass for newer-family passive and active buff books, while the remaining divergence is narrowed to the exact coefficient matrix and the remaining long-tail conditional mastery or formula rules beyond these rate-backed stat surfaces and Mechanic mech-state gating | `AbilityUI.cs`, `AbilityUIBigBang.cs`, `CharacterBuild`, `SkillManager`, `SkillLoader`, stat/runtime layer (`CUIStat::Draw`, `CUIStat::DrawTextA`) |
| Partial | Character info / profile window parity | The simulator now loads a dedicated WZ-backed `UserInfo` window from `UIWindow.img/UserInfo` or `UIWindow2.img/UserInfo/character`, routes the status-bar `BtCharacter` button into that owner, keeps the primary profile page build-backed with name/job/level plus world-rank and job-rank plus guild/alliance fields, and the Big Bang owner now switches into WZ-backed ride, pet, collect, and personality subpages that render simulator-side mount state, active pet data, collection summaries, and optional trait values instead of leaving those buttons decorative. The owner now also loads the page-specific `BtException`, `BtArrayName`, and `BtArrayGet` controls from `UIWindow2.img/UserInfo`, hides primary action buttons outside the character page so the per-page button surface matches the selected subpage more closely, forwards `BtParty` into the existing social-list owner instead of leaving it as a pure message stub, opens the WZ-backed `exception` popup with local register/delete plus meso-filter toggles instead of leaving that client-owned control path dead, consumes the character-page `BtPopUp` and `BtPopDown` buttons as real selector controls for the `item` and `wish` popups, renders the `productSkill` icon family from `UIWindow2.img/UserInfo/character/productSkill` against the live simulator-side maker progression snapshot, and uses that same progression snapshot to replace the old equip-count collection summary with maker, glove, shoe, toy, craft-count, and discovered-recipe metadata. The personality page now also consumes the WZ-backed `Tooltip` canvases from `UIWindow2.img/UserInfo/personality/Tooltip` so hovering trait rows opens the client art instead of leaving that hover path dead. The remaining gap is the deeper `CUIUserInfo` interaction surface such as packet-authored ranking deltas, server-sourced collection or charm-family data, real trade-request submission, and the exact server workflows behind those buttons | Character-facing information now has its own launch path, a broader button-driven surface distinct from the stat window, WZ-backed popup navigation and personality-hover art, plus richer simulator-backed ranking and maker-collection metadata instead of a mostly read-only page shell; the remaining gap is narrowed to server-authored trade, ranking, and collection workflows rather than dead `BtPopUp`/`BtPopDown`, missing tooltip art, or a flat local-only collection readout | character/profile UI layer, player metadata presentation (`CUIUserInfo::Draw`, `CUIUserInfo::OnButtonClicked`) |
| Partial | Book / collection window parity | MapSimulator now registers a dedicated `BookCollection` owner backed by `UIWindow.img/UIWindow2.img/Book`, uses the client-owned `BtPrev`, `BtNext`, and `BtClose` controls at the IDA-recovered `CBookDlg::OnCreate` button positions, reuses separate dot-marker canvases for the spread markers, and keeps the same two-page paging cadence that `CBookDlg::OnButtonClicked` applies by moving in two-page increments and closing through the owner-local close path instead of behaving like another generic placeholder. The runtime now also feeds the owner from the live character build plus the existing item-maker progression snapshot, opens it from the `UserInfo` collect surface, and renders a real two-page spread with centered page-index text plus per-row collection entries for overview, crafting-family progression, traits, equipment ledger rows, and discovered recipe pages. The remaining gap is that the simulator still approximates the client's `CT_INFO` layout, font matrix, and data source: it does not yet mirror the exact twelve-font draw treatment recovered from `CBookDlg::OnCreate`, the exact page-marker resource path used by the client, or the true server-/packet-authored collection dataset and `CWvsContext::CloseBook` workflow that drive the official owner. | This is now a real stand-alone utility surface rather than dead backlog theory: the simulator has the separate paged owner, explicit book-button flow, and a concrete collection-data presentation seam distinct from `UserInfo`. The remaining divergence is narrowed to exact draw fidelity and live client-owned collection data instead of total owner absence. | collection/book UI layer (`CBookDlg::OnCreate`, `CBookDlg::Draw`, `CBookDlg::OnButtonClicked`) |
| Partial | Quest log window parity | The simulator quest windows now render live runtime-backed lists for available, in-progress, completed, and recommended quests, instantiate the client `Tab/enabled` / `Tab/disabled` category buttons plus `BtMyLevel` / `BtAllLevel` filters from `UIWindow.img` or `UIWindow2.img`, keep selection synchronized with quest state mutations, color rows by accept/complete readiness, expand the list owner to use the dedicated quest-log shell instead of a simulator-only inline detail pane, preserve item-backed requirement/reward hover popups by moving them onto the dedicated detail owner, now load and honor the legacy `UIWindow.img/Quest/BtDetail` list-shell button so pre-Big-Bang quest browsing uses a client-shaped select-then-button cadence instead of forcing the Big Bang-style double-click open path, now also mirror the client `CUIQuestInfo::OnTabChanged` reselection cadence more closely by preferring suitable-level quests on the available tab, recently updated or recently viewed quests on the in-progress tab, and top entries when the prior selection no longer survives a tab or filter rebuild, now expose the client quest-detail location button families through the existing detail owner by routing NPC, mob-demand, and item-delivery fallback cases into the world-map search owner instead of treating every specialized case as a pure NPC locate, and now also split the Big Bang quest-detail shell back onto the client button families by reserving `UIWindow2.img/Quest/quest_info/BtQuestDeliveryAccept` and `BtQuestDeliveryComplete` for delivery-specific guidance while primary accept/complete return to the shared `Quest/BtOK` art, surfacing item-backed delivery hints through those dedicated buttons and routing their cash-item fallback into the existing quest-delivery or Cash Shop owners with the client hardcoded cash-item ids `5660000` / `5660001` and shop migration SNs `50200218` / `50200219`; they still do not mirror the client's actual category-option bitfield rebuild controls, packet-backed demand-item query flow, per-quest delivery-type metadata, or the remaining demand-rect-specific delivery branches behind the most specialized quest-detail buttons | Quest interaction now has a usable standalone log that reflects the same runtime state as NPC quest overlays, uses WZ-backed quest categories instead of a custom tab strip, follows the legacy detail-button cadence where that WZ shell exists, automatically reselects client-shaped quest entries when tabs rebuild, preserves the client split between generic quest actions and the dedicated Big Bang delivery-button family, and exposes broader world-map or delivery-owner quest target routing instead of leaving specialized locate or delivery cases as decorative art, while the remaining divergence is narrowed to the hidden category-option control matrix plus the deeper packet-authored delivery metadata and demand-rect handling rather than tab reselection or every locate button collapsing to the same NPC-only path | `QuestUI.cs`, `QuestUIBigBang.cs`, `QuestDetailWindow.cs`, `QuestRuntimeManager.cs`, `UIWindowLoader.cs`, NPC/UI bridge (`CUIQuestInfo::Draw`, `CUIQuestInfoDetail::OnButtonClicked`) |
| Partial | Quest detail window parity | The simulator now exposes a dedicated `QuestDetail` owner backed by the quest runtime, reuses the client `Quest/quest_info` frame family when present, opens from quest-list selection, renders separate summary/requirements/rewards sections with WZ-backed summary headers plus gauge art, shows color-coded requirement rows, now also switches incomplete requirement rows onto the client `prob` demand-strip family while completed/reward rows keep the `select` strip family when those canvases are present, draws reward item icon rows through the live item-icon loader, switches accept/complete/track/give-up controls across the client quest-detail button families when that art is present, supports previous/next quest navigation within the active tab, wires standalone accept or give-up actions plus completion guidance distinct from the list shell, keeps the runtime-backed tertiary locate-NPC action visible through the quest-detail owner, reuses legacy `BtGotoNpc` and `BtMarkNpc` families when present while falling back to the Big Bang `BtNPC` family, now adds a dedicated mob-locate action for incomplete mob demands and routes it into the world-map search owner so quest-detail delivery guidance can mark the current-field mob target without displacing the NPC-locate button, lays out four-button delivery or completion cases without collapsing them back into the older two-button shell, draws quest notice banners plus animated notice icons from the client quest notice families when that art is present, and now pushes demand/reward count text into separate runtime fields so the detail owner can draw native-width `select` / `prob` strips with split body-vs-count composition, right-aligned counters, live item icons, and tighter client-shaped `253px` content bounds instead of stretching every row across a generic full-width strip. The remaining gap is that the simulator still does not mirror the client遯ｶ蜀ｱ full CT-driven row compositor and scrolling math from `CUIQuestInfoDetail::Draw`, broader quest-demand world-map overlays outside the current search-result seam, or the remaining packet- and cash-item-driven delivery workflows behind the most specialized detail buttons | The simulator now exercises the client's split between quest browsing and per-quest inspection with client-shaped notice chrome, WZ-backed incomplete-vs-complete demand row art, distinct NPC and mob world-map guidance from the detail owner, broader delivery/completion button layouts, and more client-shaped demand row composition instead of a mostly text-only detail pane; the remaining gap is now concentrated in the last CT-layout fidelity and deeper delivery/world-map branches rather than owner absence, missing locate-button paths, or stretched placeholder demand strips | `QuestUI.cs`, `QuestUIBigBang.cs`, `QuestDetailWindow.cs`, `QuestRuntimeManager.cs`, quest-detail interaction layer (`CUIQuestInfoDetail::Draw`, `CUIQuestInfoDetail::OnButtonClicked`) |
| Partial | Quest alarm / notifier window parity | The simulator now replaces the `QuestAlarm` placeholder with a dedicated WZ-backed tracker built from `UIWindow.img/UIWindow2.img/QuestAlarm`, supports minimize/maximize toggles, title counts, live in-progress quest rows with mob and item requirement text plus demand-summary lines, completion-state coloring, row selection, per-row dismiss buttons shaped from the client `BtDelete` art, double-click routing into the quest detail flow, and now reuses shared `UIWindow2.img/Quest/quest_info` chrome for selection and progress-gauge rendering while surfacing negative quest `act/money` costs as live meso progress rows and rewarding or consuming mesos against the bound inventory runtime during quest start or completion; the quest-alarm runtime also now tracks recent quest-state, mob-kill, and tracked-item updates so the client `BtQ/ani` alert frames and sky-blue recent-update emphasis can light up before `BtQ` falls back to the normal quest-log toggle path, and the owner now follows the client-recovered five delete-slot layout more closely by exposing five visible rows per page, resizing the maximized frame to the active demand stack, paging larger quest sets with mouse-wheel plus keyboard navigation, and adding a local delete-all header action with `Delete`/`Shift+Delete` shortcuts on top of the existing per-row dismiss path. The `BtAuto` toggle now drives a local auto-register mode instead of acting as selection-only chrome: auto-registered active quests can be locally deleted into a persisted hidden set, manual quest-detail tracking pins quests into a persisted tracked set even when auto-register is off, and the owner restores per-character auto mode plus minimized state from AppData across simulator restarts. The remaining gap is that this is still a simulator-side approximation rather than the client's packet-driven `CUIQuestAlarm` registration path, so exact server-authored pin/delete semantics, the full official auto-register rule matrix behind `BtAuto`, and the precise client demand-rect composition and text-layout rules are still not mirrored | Active questing now has a persistent standalone progress tracker with animated quest-alert feedback, row-local and owner-wide dismiss affordances, a client-shaped five-slot paging surface, richer WZ-backed progress chrome, recent-update emphasis, and a per-character persisted auto-register/pinned-tracking model instead of a mostly session-local list shell; the remaining gap is narrowed to packet-authored registration semantics and deeper demand-layout fidelity rather than missing persistence, a dead `BtAuto`, or absent delete-all behavior | progression/UI layer, quest runtime, demand rendering (`CUIQuestAlarm::Draw`, `CUIQuestAlarm::OnButtonClicked`) |
| Partial | Skill macro parity | The macro window now enforces trimmed macro names against a client-shaped 12-byte budget instead of a flat 12-character ASCII subset, validates names inline against the current Windows ANSI locale, shows a live byte counter in the editor, routes committed character input through the simulator `GameWindow.TextInput` path so locale and IME text commits no longer depend on the old synthetic ASCII key mapping, accepts clipboard paste as a best-effort locale text insert that fills the remaining byte budget instead of rejecting an otherwise valid prefix wholesale, owns a real cursor-aware name edit field with mouse caret placement plus `Backspace`/`Delete`/arrow/`Home`/`End` behavior instead of the old append-only buffer, mirrors `CUIMacroSysEx`/`CCtrlEdit` ownership more closely by routing Win32 IME preedit composition into inline macro-name preview text with a blinking caret and composition underline before committed `TextInput` lands, now also loads the client `UIWindow.img/SoftKeyboard` chrome as an attached helper with client-shaped key-grid hit-testing from `CUISoftKeyboard::GetKeyIndexFromPoint`, hover feedback, min/max/close controls, caps or shift toggles, backspace or enter actions, and byte-budget or locale-aware key disabling before a key advertises as clickable, still supports drag-binding selected macros into QuickSlot entries, routes macro hotkeys back through `SkillManager` with party-chat announce semantics, paces queued casts so the next macro skill waits for the current cast to finish, and persists the five-slot macro definition list per active player or selected login-roster character under the simulator AppData profile across restarts instead of losing macro definitions with the session. The remaining gap is that locale-specific character blacklists beyond the active code-page check, the exact client candidate-window placement and composition UI, and the fuller `CUISoftKeyboard::UpdateKeyConstraint` mode matrix are still only approximated rather than mirrored exactly. | Macro support now behaves like a real runtime surface instead of an editor-only stub, and the remaining gap is narrowed to deeper locale blacklist rules plus the last candidate-window and soft-keyboard mode details rather than missing interaction ownership, committed locale input, byte-count enforcement, preedit rendering, or a client-owned on-screen helper. | `SkillMacroUI.cs`, `SkillMacroSoftKeyboardLayout.cs`, `SkillMacroNameRules.cs`, `SkillManager.cs`, `QuickSlotUI.cs`, `MapSimulator.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `WindowsImeCompositionMonitor.cs` (`CUIMacroSysEx`, `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::UpdateKeyConstraint`) |
| Partial | Soft keyboard and constrained text-entry parity | The simulator now has a dedicated WZ-backed soft-keyboard owner loaded from `UIWindow.img/SoftKeyboard`, uses the client-derived `CUISoftKeyboard::GetKeyIndexFromPoint` grid geometry for hover and click hit-testing, routes hover state through a top-level UI manager seam so the key surface owns mouse focus ahead of underlying windows, and applies explicit per-mode enable gating that mirrors the client遯ｶ蜀ｱ high-level `SetKeyMode` split between alphabetic, numeric, and fully-disabled key families rather than letting every visible key advertise as clickable. The constrained map-transfer target field now attaches to that helper as a numeric-only edit surface, so the utility path no longer relies only on raw physical-key digit translation or paste when a client-owned helper is expected. The remaining gap is that other utility edit owners are not yet attached to the shared helper, and the exact client `UpdateKeyConstraint` type matrix beside `CUIMacroSysEx` is still only approximated through the implemented alpha/numeric gating rather than a full reproduction of every client keyboard type. | This is now a separate parity target from generic macro name validation because it affects how locale input, constrained text fields, and client-owned edit controls behave before characters are committed. The simulator now has the on-screen key surface, hover ownership, and explicit key-enable routing for at least one constrained utility owner; the remaining divergence is narrowed to broader utility-host attachment and the deeper client mode matrix rather than total helper absence. | utility-window text-entry layer, map-transfer edit bridge, shared soft-keyboard owner (`CUIMacroSysEx::OnCreate`, `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::OnMouseMove`, `CUISoftKeyboard::HitTest`, `CUISoftKeyboard::UpdateKeyConstraint`) |
| Partial | Pickup notice parity | The simulator pickup-notice stack now renders item icons, resolves cached item names and type labels for success messages, records player pickup history, reconnects manual pickup failures through `PlayerCombat` so ownership and inventory-capacity checks consistently raise notices before the drop is consumed, mirrors the client `-3` style split more closely by pairing pickup-failure screen notices with a chat-log line through a shared formatter, now routes inventory-full through that same paired screen/chat failure seam instead of leaving `-2` as a screen-only special case, applies a pet-specific validator backed by WZ `item/0200.img/02002058/spec/notPickupByPet` so local auto-loot can surface pet-blocked items with item-aware pet messaging without spamming repeated failures, resolves `Unavailable` failures against `DropPool` recent-pickup history so pet-, player-, mob-, and generic remote-preemption cases surface actor-aware pickup lines instead of collapsing to one fallback string, and now also wires `DropPool` remote player and remote pet pickup callbacks into the same paired screen/chat notice seam instead of limiting live remote-loot surfacing to the mob callback alone; recent-pickup fallback naming also preserves recorded player/other actor names instead of dropping them during formatter resolution. The remaining gap is that the simulator still lacks the client's fuller pickup-result string matrix and exact official wording around `CWvsContext::OnDropPickUpMessage` beyond the implemented `0`, `1`, `2`, `-2`, `-3`, and generic-default style branches, and it still depends on explicit `DropPool` remote-pickup producers rather than covering every real multiplayer loot source/runtime that could feed those branches | Pickup feedback now reflects real item metadata, shared inventory-capacity validation, item-aware pet-blocked failures, paired screen/chat failures, actor-aware recent-preemption messaging, and live remote player/pet/mob pickup notices when the drop runtime resolves those actors through `DropPool`; the remaining divergence is narrowed to the deeper pickup-result string matrix, exact official strings, and still-unwired remote-loot producers outside the current `DropPool` callbacks | `PickupNoticeUI.cs`, drop/item runtime (`CWvsContext::OnDropPickUpMessage`, `CUIScreenMsg::ScrMsg_Add`) |
| Partial | Cursor and utility-button parity | The simulator now builds dedicated WZ-backed `Menu` and `System` popup owners from `StatusBar2.img/mainBar/Menu` and `StatusBar2.img/mainBar/System`, routes the status-bar `BtMenu` and `BtSystem` buttons through those owners, fans their core entries back into inventory/equip/stat/skill/quest/character-info and channel-selection flows instead of leaving them as dead launchers, switches the cursor to the client hold art during active skill, quick-slot, and window drag flows, loads the client forbidden-hand and busy-search cursor families from `Basic.img/Cursor/13` and `Basic.img/Cursor/16`, dismisses the popup owners on outside clicks, now registers WZ-backed `KeyConfig` and `OptionMenu` owners from `UIWindow2.img/KeyConfig` and `UIWindow2.img/OptionMenu`, routes `BtKeySetting`, `BtGameOption`, `BtSystemOption`, `BtOption`, and `BtJoyPad` into those owners instead of forbidden stubs, binds key settings back to live `PlayerInput`, and exposes simulator-backed smooth-camera plus BGM/SFX/focus-loss audio toggles through the option pages; `BtRank` and `BtEvent` also now land on WZ-backed ranking/event owners instead of dead feedback toasts, but their contents are still placeholder shells and joypad remapping plus the deeper ranking/event packet flows still fall short of full `CUIStatusBar::OnButtonClicked` parity | This is part of the client feel layer and affects almost every window or interaction surface once the rest of the UI becomes more complete, and the remaining gap is now the fidelity of the deeper submenu payloads rather than whether the utility buttons open client-shaped owners at all | `MouseCursorItem.cs`, `StatusBarUI.cs`, `UILoader.cs`, `StatusBarPopupMenuWindow.cs`, broader UI manager layer (`CUIStatusBar::OnButtonClicked`) |

### 2. Local utility and event packet parity

This area is still underrepresented by the existing window rows because the client keeps a packet-owned dispatch seam in front of several of those owners.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Packet-owned local utility/event dispatch parity | The current simulator tracks many individual utility windows or overlays, but it still does not mirror the broader `CUserLocal::OnPacket` dispatch surface that opens or drives them from packet-owned local events. IDA shows that this one seam routes `OnOpenUI`, `OnOpenUIWithOption`, `OnGoToCommoditySN`, `OnFieldFadeInOut`, `OnBalloonMsg`, `OnDamageMeter`, `OnNotifyHPDecByField`, `OnNoticeMsg`, `OnChatMsg`, `OnBuffzoneEffect`, `OnPlayEventSound`, `OnPlayMinigameSound`, `OnOpenClassCompetitionPage`, `OnQuestGuideResult`, `OnDeliveryQuest`, `OnAskAPSPEvent`, `OnFollowCharacterFailed`, and related tutor/radio/cooltime handlers. The narrower handler decompiles confirm concrete client behavior that is not just 遯ｶ蠕冪en a window later遯ｶ繝ｻ `OnOpenUI` forwards raw ids to `CWvsContext::UI_Open`, `OnOpenUIWithOption` adds option-driven follow-up such as party-search requests, durability-repair owner creation, and toggle-by-option behavior, `OnGoToCommoditySN` stores a commodity serial number then requests shop migration, `OnFieldFadeInOut` registers a fade overlay relative to the avatar render layer, `OnBalloonMsg` can bind timed balloons either to the avatar origin or to explicit world coordinates, and `OnDamageMeter` resets a dedicated meter timer. The simulator still lacks that packet-authored bridge, so many of these behaviors remain command-owned, button-owned, stubbed, or missing altogether instead of flowing through the same local dispatch seam the client uses. | Without this bridge, parity work keeps implementing isolated windows while missing the local-user owner that actually launches, parameterizes, or times them in the client. This blocks accurate event-driven utility behavior for repair/party-search launches, commodity-shop jumps, field fades, balloon prompts, damage-meter resets, buff-zone or field-hazard feedback, event/minigame sound cues, and the broader quest-guide/delivery or notice/chat utility surfaces that are not reducible to static window ownership alone. | local-user packet/runtime bridge (`CUserLocal::OnPacket`, `CUserLocal::OnOpenUI`, `CUserLocal::OnOpenUIWithOption`, `CUserLocal::OnGoToCommoditySN`, `CUserLocal::OnFieldFadeInOut`, `CUserLocal::OnBalloonMsg`, `CUserLocal::OnDamageMeter`) |

### 3. Packet-authored local overlays and guidance parity

This slice is narrower than the full dispatcher row above and is still missing as its own planning surface even though the handlers have concrete client-owned behavior.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field fade and world/actor balloon parity | MapSimulator now has a dedicated packet-owned overlay seam for this row: `MapSimulator.PacketOwnedLocalOverlayParity.cs` loads the shared balloon chrome from `UI/Login.img/WorldNotice/Balloon`, updates and draws a packet-authored field-fade overlay between world rendering and the HUD, decodes the client-shaped `OnFieldFadeInOut` payload (`fadeIn`, `hold`, `fadeOut`, `style`) from raw bytes for simulator-side packet injection, and tracks `OnBalloonMsg` state with both avatar-relative and explicit world-coordinate anchors plus packet-shaped payload decoding (`Maple string`, width, lifetime, avatar-vs-world flag, optional `x/y`). Avatar balloons now resolve their anchor from the active player and current assembled frame height to mirror the client遯ｶ蜀ｱ `CAvatar::GetHeight` usage instead of collapsing everything into chat or static UI, and the new `/localoverlay` command provides an existing simulator seam for status, clear, direct parameter injection, and raw payload verification for this owner. The remaining gap is that these overlays still are not launched by a true `CUserLocal::OnPacket` / inbox dispatcher, and the simulator still approximates the client遯ｶ蜀ｱ exact `RegisterFadeInOutAnimation` layer choreography plus the fuller `ShowBallonMsg` layout or arrow-variant behavior beyond the shared `WorldNotice/Balloon` skin. | These are visible event-owned overlays that sit between local gameplay and many later scripted or utility flows, and the simulator now has a concrete owner for both surfaces instead of collapsing them into chat lines or ad hoc notices. The remaining risk is now concentrated in the missing live packet bridge and the last client-only micro-layout details rather than in complete absence of field fades or avatar/world guidance balloons. | packet-owned overlay layer (`CUserLocal::OnFieldFadeInOut`, `CAnimationDisplayer::RegisterFadeInOutAnimation`, `CUserLocal::OnBalloonMsg`, `CUserLocal::ShowBallonMsg`) |
| Partial | Damage-meter and field-hazard notice parity | MapSimulator now gives this row its own packet-owned HUD/notice owner instead of leaving it buried under the broader dispatcher item: `LocalOverlayRuntime` is wired into the main update loop, a dedicated top-center HUD renderer now surfaces `OnDamageMeter` state with a countdown, progress bar, and the shared timing-age stamp that the client also updates in `CWvsContext`, and a separate field-hazard banner now renders independently of ordinary combat damage or generic chat logging using the client notice families already present in `Basic.img/Notice2`, `Basic.img/Notice3`, and `StatusBar2.img/mainBar/notice`. The existing `localoverlay` parity command now also accepts packet-shaped `damagemeter` and `hpdec` payloads so raw `Int32` timer/damage payloads can drive the same owner the future dispatcher will call. The remaining gap is that this row is still fed through simulator-side command hooks rather than the real raw `CUserLocal::OnPacket` dispatcher, and the `OnNotifyHPDecByField` path is still a simulator-side hazard banner approximation rather than the client's fuller `TryConsumePetHP` follow-up and exact presentation rules. | This is a distinct feedback owner for event scoring and map-hazard presentation, and the simulator now carries its own HUD/notice layer with WZ-backed chrome plus packet-shaped decode helpers instead of collapsing these callbacks into generic chat text. The remaining risk is narrower: final raw-dispatch hookup and the deeper pet auto-consume/presentation semantics still need to sit on top of this owner, but the timer and hazard surfaces themselves no longer depend on unrelated HUD or window work. | packet-owned HUD/notice layer (`CUserLocal::OnDamageMeter`, `CDamageMeter::SetTimer`, `CUserLocal::OnNotifyHPDecByField`) |
| Partial | Quest-guide, delivery-quest, and class-competition launch parity | MapSimulator now has a dedicated packet-owned utility seam for this row: packet-authored quest-guide demand state can rebuild world-map mob targets into the existing `WorldMapUI` search surface and only keep the guide state alive when a usable mob target survives, the broader utility runtime now tracks the shared request-timing stamp these handlers update in the client, a dedicated `QuestDelivery` owner is now registered from `UIWindow2.img/UtilDlgEx` plus `QDeliveryIcon` art with unique-modeless fallback routing back into the chat/notice path when another tracked owner is already visible, and a named `ClassCompetition` owner now exists as a packet-driven utility surface instead of treating that launch as a static menu button. The remaining gap is that these handlers still are not wired into a raw `CUserLocal::OnPacket` / inbox decoder path, `QuestDelivery` is still a lightweight simulator approximation rather than the full `CUIQuestDelivery` worthy-quest list / scrollbar / button-state runtime, and the class-competition page is still a placeholder owner rather than a modeled live page. | These are not generic variants of quest log or world map browsing. They are packet-authored launch surfaces for guided quest routing and delivery/class-competition utility flows, and the simulator now has the owning seams, WZ-backed delivery owner registration, world-map target injection, and unique-modeless gating needed to hang the real packet dispatcher on top. The remaining risk is now concentrated in the missing raw packet decode hookup plus the deeper client-owned `CUIQuestDelivery` and class-competition page behaviors rather than in complete owner absence. | packet-owned guidance/utility launch layer (`CUserLocal::OnQuestGuideResult`, `CUserLocal::OnDeliveryQuest`, `CUIQuestDelivery`, `CUserLocal::OnOpenClassCompetitionPage`) |

## Priority Order

If the goal is visible parity first, the next work in this area should be sequenced like this:

1. Window usefulness pass:
   Finish minimap, inventory, equip, and quest-log interactions so the existing windows become usable client-facing tools rather than frame shells.
2. Progression correctness pass:
   Tighten ability/stat formulas, equip presentation, and inventory-backed validations so the windows reflect the runtime accurately.
3. Packet-dispatch pass:
   Add the `CUserLocal::OnPacket`-style bridge for packet-owned local utility/event handlers before layering more one-off launches onto chat commands or button callbacks.
4. Packet-overlay pass:
   Add the packet-owned fade, balloon, damage-meter, field-hazard, quest-guide, and delivery/class-competition surfaces after the base dispatcher exists so those handlers do not stay hidden behind one catch-all row.
5. Utility feedback pass:
   Complete pickup notices, soft-keyboard or constrained text-entry behavior, macro behavior, cursor states, and status-bar utility-button behavior.
6. Client seam pass:
   Capture direct IDA seams for minimap, inventory, equip, and quest windows so future parity fixes are anchored to named client owners instead of simulator-only structure.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".

