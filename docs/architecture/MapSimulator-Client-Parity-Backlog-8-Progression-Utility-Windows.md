# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/UI/MinimapUI.cs`
- `HaCreator/MapSimulator/UI/MouseCursorItem.cs`
- `HaCreator/MapSimulator/UI/PickupNoticeUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AbilityUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AdminShopDialogUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AbilityUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/BookCollectionWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/EquipUI.cs`
- `HaCreator/MapSimulator/UI/Windows/EquipUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/InventoryUI.cs`
- `HaCreator/MapSimulator/UI/Windows/InventoryUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/KeyConfigWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/MemoMailboxWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/MemoSendWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/MemoGetWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/OptionMenuWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/QuestUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuestUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillMacroUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SoftKeyboardUI.cs`
- `HaCreator/MapSimulator/UI/Windows/VegaSpellUI.cs`
- `HaCreator/MapSimulator/UI/Windows/WorldMapUI.cs`
- `HaCreator/MapSimulator/Interaction/MemoMailboxManager.cs`
- `HaCreator/MapSimulator/Fields/FieldInteractionRestrictionEvaluator.cs`

### Client references checked

- `CUIStat::Draw` (captured in simulator layout comments and offsets)
- `CUIStat::DrawTextA` (captured in simulator layout comments and offsets)
- `CUISkillMacro` (captured in simulator window comments)
- `CWvsContext::OnDropPickUpMessage` (captured in `PickupNoticeUI` comments)
- `CUIScreenMsg::ScrMsg_Add` (captured in `PickupNoticeUI` comments)
- `CUIItem::Draw` at `0x7ccf50`
- `CAdminShopDlg::OnCreate` at `0x42ecd0`
- `CAdminShopDlg::OnPacket` at `0x4310f0`
- `CAdminShopDlg::SetAdminShopDlg` at `0x4307c0`
- `CAdminShopDlg::Draw` at `0x42fdf0`
- `CAdminShopDlg::OnButtonClicked` at `0x430f90`
- `CUIAdminShopWishList::HitTest` at `0x76cf50`
- `CUIAdminShopWishList::OnCreate` at `0x772640`
- `CUIAdminShopWishList::OnButtonClicked` at `0x772670`
- `CCashShop::Init` at `0x484920`
- `CCashShop::OnPacket` at `0x4997e0`
- `CCashShop::OnCashItemResult` at `0x499370`
- `CCSWnd_Char::OnCreate` at `0x4c4590`
- `CCSWnd_Char::OnDefaultAvatar` at `0x4bec80`
- `CCSWnd_Char::OnWear` at `0x4c5a60`
- `CCSWnd_Char::SetPet` at `0x4bc400`
- `CCSWnd_Char::ShowMessageBox` at `0x4bbb00`
- `CCSWnd_Char::ShowPersonalShop` at `0x4bc660`
- `CCSWnd_Char::ShowEntrustedShop` at `0x4bc6b0`
- `CCSWnd_Char::BlowWeather` at `0x4c5d30`
- `CCSWnd_Locker::OnCreate` at `0x4c31c0`
- `CCSWnd_Inventory::OnCreate` at `0x4c34f0`
- `CCSWnd_List::OnCreate` at `0x4d0500`
- `CCSWnd_Status::OnCreate` at `0x4cb840`
- `CCSWnd_OneADay::OnCreate` at `0x4d2fd0`
- `CCashTradingRoomDlg::OnCreate` at `0x49e8c0`
- `CUIItemMaker::Draw` at `0x7d4980`
- `CUIItemMaker::OnButtonClicked` at `0x7d5dc0`
- `CUIItemUpgrade::Draw` at `0x7c0100`
- `CUIItemUpgrade::OnButtonClicked` at `0x7c0ca0`
- `CUIVega::OnCreate` at `0x7c1320`
- `CUIVega::Draw` at `0x7c1dd0`
- `CUIVega::OnButtonClicked` at `0x7bf4a0`
- `CAnimationDisplayer::Effect_ItemUpgrade` at `0x4567f0`
- `CAnimationDisplayer::Effect_ItemMake` at `0x456f80`
- `CAnimationDisplayer::Effect_ViciousHammer` at `0x457210`
- `CAnimationDisplayer::Effect_Vega` at `0x457600`
- `CAnimationDisplayer::Effect_SkillBookUsed` at `0x4581b0`
- `CUIMapTransfer::Draw` at `0x7df5c0`
- `CUIMapTransfer::OnButtonClicked` at `0x7e0ed0`
- `CUIEquip::Draw` at `0x7aa560`
- `CUIQuestInfo::Draw` at `0x82a780`
- `CUIQuestInfoDetail::Draw` at `0x8248c0`
- `CUIQuestInfoDetail::OnButtonClicked` at `0x834c90`
- `CUIQuestAlarm::Draw` at `0x82d850`
- `CUIRanking::OnCreate` at `0x83ca40`
- `CUIRanking::OnButtonClicked` at `0x83c6a0`
- `CUIRanking::Update` at `0x83c690`
- `CUIStatusBar::OnButtonClicked` at `0x880540`
- `CUIStat::Draw` at `0x864bd0`
- `CUIKeyConfig::OnCreate` at `0x7dc5d0`
- `CUIKeyConfig::Draw` at `0x7dd7c0`
- `CUIGameOpt::OnCreate` at `0x9777d0`
- `CUIMacroSysEx::OnCreate` at `0x85c420`
- `CUISoftKeyboard::GetKeyIndexFromPoint` at `0x85faa0`
- `CUISoftKeyboard::OnMouseMove` at `0x85fba0`
- `CUISoftKeyboard::HitTest` at `0x85fc50`
- `CUISoftKeyboard::UpdateKeyConstraint` at `0x85fd70`
- `CUIMiniMap::OnButtonClicked` at `0x803e40`
- `CUIMiniMap::OnMouseMove` at `0x8040a0`
- `CSoftKeyboardDlg::OnCreate` at `0x7162c0`
- `CSoftKeyboardDlg::OnButtonClicked` at `0x716c20`
- `CUIUserInfo::Draw` at `0x8b3a50`
- `CUIUserInfo::OnButtonClicked` at `0x8b6d80`
- `CUIEventAlarm::OnCreate` at `0x7ac4f0`
- `CUIEventAlarm::Draw` at `0x7aad90`
- `CUIEventAlarm::Update` at `0x7aac00`
- `CBookDlg::OnCreate` at `0x471f00`
- `CBookDlg::Draw` at `0x473360`
- `CBookDlg::OnButtonClicked` at `0x474300`
- `CMonsterBookMan::LoadBook` at `0x664c10`
- `CParcelDlg::OnCreate` at `0x6914c0`
- `CParcelDlg::Draw` at `0x694db0`
- `CParcelDlg::OnButtonClicked` at `0x690c30`
- `CITC::Init` at `0x573ee0`
- `CITC::OnPacket` at `0x577470`
- `CITC::OnNormalItemResult` at `0x5771d0`
- `CUserLocal::OnPacket` at `0x9340c0`
- `CUserLocal::OnOpenUI` at `0x9055f0`
- `CUserLocal::OnOpenUIWithOption` at `0x932300`
- `CUserLocal::OnGoToCommoditySN` at `0x905760`
- `CUserLocal::OnMesoGive_Succeeded` at `0x90f950`
- `CUserLocal::OnMesoGive_Failed` at `0x90d530`
- `CUserLocal::OnRandomMesobag_Succeeded` at `0x90fa30`
- `CUserLocal::OnRandomMesobag_Failed` at `0x90d560`
- `CUserLocal::OnResignQuestReturn` at `0x905720`
- `CUserLocal::OnPassMateName` at `0x918260`
- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`
- `CUserLocal::OnHireTutor` at `0x90e560`
- `CUserLocal::OnTutorMsg` at `0x916f60`
- `CUserLocal::OnOpenSkillGuide` at `0x90e6a0`
- `CUserLocal::OnAskAPSPEvent` at `0x90f0e0`
- `CUserLocal::OnRadioSchedule` at `0x918120`
- `CUIAntiMacro::OnCreate` at `0x78b980`
- `CUIAntiMacro::Draw` at `0x78bb30`
- `CUIAntiMacro::Update` at `0x77f1f0`
- `CUIAdminAntiMacro::OnCreate` at `0x78cd90`
- `CUIAdminAntiMacro::Draw` at `0x78cf40`
- `CUIAdminAntiMacro::Update` at `0x77f250`
- `CWvsContext::OnAntiMacroResult` at `0x9ff580`
- `CWvsContext::OnLogoutGift` at `0x9cccb0`
- `CWvsContext::SaveAntiMacroScreenShot` at `0x9fa330`
- `CWvsContext::ShowAntiMacroNotice` at `0x9f3c30`
- `CUILogoutGift::OnCreate` at `0x7dec80`
- `CUILogoutGift::Draw` at `0x7de9b0`
- `CRadioManager::Play` at `0x6cb940`
- `CRadioManager::Stop` at `0x6cbd20`
- `CRadioManager::Update` at `0x6cbf20`

Notes:
Direct IDA seam capture is now confirmed for inventory, cash-service, item-maker, item-upgrade, map-transfer, equipment, quest-log, quest-detail, quest-alarm, the dedicated ranking/event owners (`CUIRanking`, `CUIEventAlarm`), status-bar utility clicks, stat-window, the explicit key-config/game-option/soft-keyboard owner family (`CUIKeyConfig`, `CUIGameOpt`, `CSoftKeyboardDlg`, `CUIMacroSysEx`, `CUISoftKeyboard`), user-info owners, the book/collection owner family (`CBookDlg` plus `CMonsterBookMan::LoadBook`), the parcel/mail owner family (`CParcelDlg`), the separate service-stage owners for Cash Shop and ITC (`CCashShop`, `CITC`), the stage-internal Cash Shop owners (`CCSWnd_Char`, `CCSWnd_Locker`, `CCSWnd_Inventory`, `CCSWnd_List`, `CCSWnd_Status`, `CCSWnd_OneADay`, `CCashTradingRoomDlg`), the separate cash-service wishlist owner (`CUIAdminShopWishList`), the anti-macro challenge owners (`CUIAntiMacro`, `CUIAdminAntiMacro`), the packet-owned anti-macro result bridge in `CWvsContext`, and a broader local-user packet dispatcher for utility/event surfaces. `CUserLocal::OnPacket` is important because it shows the client does not only open these windows from direct button clicks: packet-owned handlers also drive generic `UI_Open`, optioned UI launch, commodity-shop migration, field fade overlays, balloon messages, and damage-meter timer resets. The Cash Shop and ITC decompiles also show that those services are not just alternate button skins on top of `CAdminShopDlg`: `CCashShop::Init` and `CITC::Init` each clear field UI, build their own staged child-window layout, select service BGM, and then wait on service-specific packet dispatch through `OnPacket`. Follow-up decompiles now also show that the stage owners do not directly absorb all of the in-service interaction either: `CCSWnd_Char::OnCreate` builds a dedicated avatar-preview surface with default-avatar, buy-avatar, and takeoff-avatar buttons plus a `CUserPreview`/physics-space pair, `CUIAdminShopWishList::OnCreate` builds a distinct wishlist owner with its own layer, controls, and focused search edit instead of leaving wish-list behavior inside the base shop dialog, `CCashTradingRoomDlg::OnCreate` builds its own chat edit, buttons, scrollbar, fonts, and cash-balance state inside the service stage, `CUIAntiMacro::{OnCreate,Draw,Update}` confirm a separate timed text-entry owner rather than treating lie-detector checks as item-use restriction text only, and `CWvsContext::OnAntiMacroResult` shows that the owner is itself packet-driven rather than purely item- or dialog-launched. The remaining seam-collection gap in this backlog area is now narrower and centered on deeper packet/data ownership inside the newly confirmed utility-owner families plus the still-missing quest reward/revive and adjacent utility-owner follow-through, rather than on the already-captured minimap or explicit skill-macro owner seams.

## Client Function Index By Backlog Area

The list below maps this backlog area to the concrete client seams that are already named in code or prior reverse-engineering notes.
These are the first functions or classes to inspect before changing simulator behavior.
Packet/controller rows and window/dialog-owner rows are intentionally kept separate when the client splits those responsibilities across different seams, and the index keeps follow-up scan additions close to their first discovery notes.

### 1. Progression and utility window parity

- `CUIStat::Draw`
- `CUIStat::DrawTextA`
- `CUIItem::Draw` at `0x7ccf50`
- `CAdminShopDlg::OnCreate` at `0x42ecd0`
- `CAdminShopDlg::OnPacket` at `0x4310f0`
- `CAdminShopDlg::SetAdminShopDlg` at `0x4307c0`
- `CAdminShopDlg::Draw` at `0x42fdf0`
- `CAdminShopDlg::OnButtonClicked` at `0x430f90`
- `CCashShop::Init` at `0x484920`
- `CCashShop::OnPacket` at `0x4997e0`
- `CCashShop::OnCashItemResult` at `0x499370`
- `CUIItemMaker::Draw` at `0x7d4980`
- `CUIItemMaker::OnButtonClicked` at `0x7d5dc0`
- `CUIItemUpgrade::Draw` at `0x7c0100`
- `CUIItemUpgrade::OnButtonClicked` at `0x7c0ca0`
- `CUIVega::OnCreate` at `0x7c1320`
- `CUIVega::Draw` at `0x7c1dd0`
- `CUIVega::OnButtonClicked` at `0x7bf4a0`
- `CUIMapTransfer::Draw` at `0x7df5c0`
- `CUIMapTransfer::OnButtonClicked` at `0x7e0ed0`
- `CUIEquip::Draw` at `0x7aa560`
- `CUIQuestInfo::Draw` at `0x82a780`
- `CUIQuestInfoDetail::Draw` at `0x8248c0`
- `CUIQuestInfoDetail::OnButtonClicked` at `0x834c90`
- `CUIQuestAlarm::Draw` at `0x82d850`
- `CUIRanking::OnCreate` at `0x83ca40`
- `CUIRanking::OnButtonClicked` at `0x83c6a0`
- `CUIRanking::Update` at `0x83c690`
- `CUIStatusBar::OnButtonClicked` at `0x880540`
- `CUIMacroSysEx::OnCreate` at `0x85c420`
- `CUISoftKeyboard::GetKeyIndexFromPoint` at `0x85faa0`
- `CUISoftKeyboard::OnMouseMove` at `0x85fba0`
- `CUISoftKeyboard::HitTest` at `0x85fc50`
- `CUISoftKeyboard::UpdateKeyConstraint` at `0x85fd70`
- `CUIUserInfo::Draw` at `0x8b3a50`
- `CUIUserInfo::OnButtonClicked` at `0x8b6d80`
- `CUIEventAlarm::OnCreate` at `0x7ac4f0`
- `CUIEventAlarm::Draw` at `0x7aad90`
- `CUIEventAlarm::Update` at `0x7aac00`
- `CBookDlg::OnCreate` at `0x471f00`
- `CBookDlg::Draw` at `0x473360`
- `CBookDlg::OnButtonClicked` at `0x474300`
- `CMonsterBookMan::LoadBook` at `0x664c10`
- `CParcelDlg::OnCreate` at `0x6914c0`
- `CParcelDlg::Draw` at `0x694db0`
- `CParcelDlg::OnButtonClicked` at `0x690c30`
- `CITC::Init` at `0x573ee0`
- `CITC::OnPacket` at `0x577470`
- `CITC::OnNormalItemResult` at `0x5771d0`
- `CRepairDurabilityDlg` (constructed from `CUserLocal::OnOpenUIWithOption` at `0x932300` when `uiId == 33`)
- `CUISkillMacro`
- `CWvsContext::OnDropPickUpMessage`
- `CUIScreenMsg::ScrMsg_Add`

Notes:
`CUIItem::Draw` confirms that the client inventory window owns meso text, per-slot icon and quantity drawing, item-grade frames, disabled-slot overlays, and special overlays such as the active bullet marker. `CAdminShopDlg::{OnCreate,OnPacket,SetAdminShopDlg,Draw,OnButtonClicked}` now make the cash-service dialog boundary much clearer than the older notes did: the client keeps a dedicated in-map admin-shop owner, and `CField::OnPacket` routes packet types `366` and `367` into that dialog's open/result/reopen lifecycle instead of leaving it as only a status-bar launch plus local button handling. That packet-owned dialog seam is intentionally distinct from the separate `CCashShop` / `CITC` stage owners tracked below. `CUIItemMaker::Draw` and `CUIItemMaker::OnButtonClicked` confirm a dedicated crafting or maker window rather than burying production flow inside generic inventory handling. `CUIItemUpgrade::Draw` and `CUIItemUpgrade::OnButtonClicked` confirm a separate item-enhancement surface rather than treating scroll or upgrade flow as a generic inventory tooltip action. Follow-up IDA lookup now shows Vega enhancement is also a distinct owner rather than a cosmetic branch inside ordinary upgrade flow: `CUIVega::{OnCreate,Draw,OnButtonClicked}` build their own dialog, validate equip/scroll state, gate the cast behind the same exclusive-request checks as the client, and trigger the dedicated Vega effect path after sending the upgrade packet. `CUIMapTransfer::Draw` and `CUIMapTransfer::OnButtonClicked` confirm a dedicated map-transfer window distinct from the minimap shell. `CUIEquip::Draw` confirms that equip-window parity is tied to slot-disable rules, expiration and durability overlays, and job or subjob gating rather than just slot placement. `CUIQuestInfo::Draw`, `CUIQuestInfoDetail::Draw`, and `CUIQuestAlarm::Draw` confirm that quest progression parity includes a separate detail pane with dedicated button handling, category-button rebuilds, scroll-driven list rebuilding, quest-delivery requirement rendering, condition-state coloring, and a standalone quest-alert window rather than only the quest log shell. Follow-up direct lookups now also confirm that the `BtRank` and `BtEvent` status-bar launches land on dedicated client owners rather than unnamed utility shells: `CUIRanking::{OnCreate,OnButtonClicked,Update}` and `CUIEventAlarm::{OnCreate,Draw,Update}`. `CBookDlg::{OnCreate,Draw,OnButtonClicked}` together with `CMonsterBookMan::LoadBook` confirm that the client keeps a real book-style collection owner tied to Monster Book data rather than treating book presentation as a generic stats subpage. `CParcelDlg::{OnCreate,Draw,OnButtonClicked}` confirm a dedicated parcel/mail owner with receive, send, and quick-send surfaces rather than burying package delivery under memo/social text alone. `CUserLocal::OnOpenUIWithOption` now also gives direct client evidence that repair-durability is its own utility owner rather than a generic option toggle: the decompile at `0x932300` shows `uiId == 33` allocating `CRepairDurabilityDlg(v5)` when no `CUniqueModeless` owner is already active. `CUIStatusBar::OnButtonClicked` confirms the utility-button cluster is a real interaction owner instead of only decorative art. `CUIMacroSysEx::OnCreate` together with `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::OnMouseMove`, `CUISoftKeyboard::HitTest`, and `CUISoftKeyboard::UpdateKeyConstraint` confirms that client text-entry parity is not just committed character handling: the macro family also owns an explicit soft-keyboard helper with hover, hit-test, and per-mode key-constraint behavior. `CUIUserInfo::Draw` and `CUIUserInfo::OnButtonClicked` confirm a separate character-information or profile surface beyond the stat window itself.

### 2. Local utility and event packet parity

- `CUserLocal::OnPacket` at `0x9340c0`
- `CUserLocal::OnOpenUI` at `0x9055f0`
- `CUserLocal::OnOpenUIWithOption` at `0x932300`
- `CUserLocal::OnGoToCommoditySN` at `0x905760`
- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`

Notes:
`CUserLocal::OnPacket` is a distinct client seam rather than a random collection of callbacks. It dispatches packet-owned local utility behavior including `OnOpenUI`, `OnOpenUIWithOption`, `OnGoToCommoditySN`, field fade in/out, balloon messages, damage-meter timing, chat/notice surfaces, quest-guide and delivery-quest results, buff-zone effects, field HP-dec notices, tutor flows, and several event-sound/minigame-sound owners. The narrower decompiles already show concrete responsibilities that are not represented well by the existing window rows: `OnOpenUI` forwards a raw UI id into `CWvsContext::UI_Open`, `OnOpenUIWithOption` adds option-driven branches such as party-search requests and the repair-durability owner, `OnGoToCommoditySN` stores a commodity serial number before calling `SendMigrateToShopRequest`, `OnFieldFadeInOut` registers a fade animation relative to the avatar layer Z, `OnBalloonMsg` can bind a balloon to the avatar origin or an explicit world position, and `OnDamageMeter` resets a dedicated timer surface. That means the client has a packet-driven utility/event layer that sits between local gameplay and the specific window owners already tracked above.

### 6. Cash-service wishlist parity discovered by targeted IDA scan

- `CUIAdminShopWishList::HitTest` at `0x76cf50`
- `CUIAdminShopWishList::OnCreate` at `0x772640`
- `CUIAdminShopWishList::OnButtonClicked` at `0x772670`

Notes:
The cash-service stage and child-window rows still need this owner called out explicitly: `CUIAdminShopWishList` is its own modeless wishlist/search controller, not just a button branch inside `CAdminShopDlg` or `CCSWnd_List`.

### 7. Packet-authored radio-schedule parity discovered by targeted IDA scan

- `CUserLocal::OnRadioSchedule` at `0x918120`
- `CRadioManager::Play` at `0x6cb940`
- `CRadioManager::Stop` at `0x6cbd20`
- `CRadioManager::Update` at `0x6cbf20`

Notes:
The broader local-utility row already mentioned tutor/radio handlers as a residual gap, but targeted IDA lookup shows radio scheduling is its own client-owned lifecycle rather than one more generic sound callback. `CUserLocal::OnRadioSchedule` decodes a track name plus time value and forwards that payload into a singleton `CRadioManager` owner when no radio session is already active. `CRadioManager::Play` then resolves a WZ/UOL-backed track through `StringPool`, starts multimedia playback, stores the active track name, mutes field BGM through the sound manager, shows a dedicated radio UI, and pushes a chat-log notice for the start event; `CRadioManager::Stop` reverses that ownership by stopping playback, hiding the UI, restoring BGM state, and pushing the completion notice; and `CRadioManager::Update` confirms a timer-driven runtime instead of a fire-and-forget sound effect. None of that owner-specific audio/UI/chat lifecycle is tracked anywhere else in the backlog today.

### 8. Explicit key-config, game-option, and soft-keyboard owner parity discovered by targeted IDA scan

- `CUIKeyConfig::OnCreate` at `0x7dc5d0`
- `CUIKeyConfig::Draw` at `0x7dd7c0`
- `CUIGameOpt::OnCreate` at `0x9777d0`
- `CUIMacroSysEx::OnCreate` at `0x85c420`
- `CUISoftKeyboard::GetKeyIndexFromPoint` at `0x85faa0`
- `CUISoftKeyboard::OnMouseMove` at `0x85fba0`
- `CUISoftKeyboard::HitTest` at `0x85fc50`
- `CUISoftKeyboard::UpdateKeyConstraint` at `0x85fd70`
- `CSoftKeyboardDlg::OnCreate` at `0x7162c0`
- `CSoftKeyboardDlg::OnButtonClicked` at `0x716c20`

Notes:
The broader cursor/utility-button row already mentions that the simulator can launch KeyConfig and OptionMenu owners, but targeted IDA lookups show the client keeps a more explicit owner family here than that row names. `CUIKeyConfig::{OnCreate,Draw}` confirm a dedicated key-setting owner with its own layout and draw lifecycle instead of a generic submenu under the status-bar system popup. `CUIGameOpt::OnCreate` confirms that game/system options are owned by a dedicated option dialog rather than only button callbacks hanging off `CUIStatusBar::OnButtonClicked`. `CUIMacroSysEx::OnCreate`, `CUISoftKeyboard::*`, and `CSoftKeyboardDlg::{OnCreate,OnButtonClicked}` show that the soft-keyboard path also has its own dialog owner and button lifecycle, not just helper hit-testing functions. Keeping that owner family explicit matters because the simulator already has `KeyConfigWindow`, `OptionMenuWindow`, and `SoftKeyboardUI`, so the remaining parity work is now about owner-specific lifecycle, constraints, and button semantics rather than whether these surfaces exist at all.

### 9. Tutor, skill-guide, and AP/SP helper parity discovered by `CUserLocal::OnPacket` scan

- `CUserLocal::OnHireTutor` at `0x90e560`
- `CUserLocal::OnTutorMsg` at `0x916f60`
- `CUserLocal::OnOpenSkillGuide` at `0x90e6a0`
- `CUserLocal::OnAskAPSPEvent` at `0x90f0e0`

Notes:
The broader local-utility row currently mentions tutor flows only as residual dispatch coverage, but the targeted `CUserLocal::OnPacket` decompiles show a distinct client-owned helper surface. `OnHireTutor` tears down the prior tutor owner, allocates a `CTutor`, initializes it through the same summoned-style world seam the client uses, and tracks active tutor variants in `CUserLocal::ms_anTutors`; `OnTutorMsg` then forwards either numeric or string payloads plus two packet-owned ints into `CTutor::OnMessage` instead of using ordinary chat. The same scan also shows that guided helper flows are not only local button opens: `OnOpenSkillGuide` packet-opens the skill UI and immediately calls `CUISkill::OpenCurSkillGuide`, while `OnAskAPSPEvent` opens a Yes/No prompt and can emit a follow-up outpacket (`195`) when the player accepts. None of those owner-specific tutor/helper semantics are tracked explicitly in the backlog today.

### 10. Minimap owner parity discovered by targeted IDA scan

- `CUIMiniMap::OnButtonClicked` at `0x803e40`
- `CUIMiniMap::OnMouseMove` at `0x8040a0`

Notes:
The minimap row already tracks a large amount of behavior, but the owner itself was still unnamed in the seam index. Targeted IDA decompiles now confirm a dedicated `CUIMiniMap` owner rather than a loose collection of helper draws. `CUIMiniMap::OnButtonClicked` cycles the minimap option state, remembers the previous option, and opens `CWorldMapDlg` directly from minimap button ids `1000` to `1003`, including the fallback notice path when the world-map dialog cannot be created. `CUIMiniMap::OnMouseMove` is equally important because it shows the client-owned tooltip and helper-marker surface that sits behind the existing backlog row: the owner computes tooltip hit rectangles for off-map remote direction markers, live NPC markers, employee markers from `CEmployeePool`, and portal markers from `CPortalList`, then resolves tooltip strings from `CNpcTemplate`, `CEmployee::GetNameTag`, portal script-property text, and map-string fallbacks before routing them through `CUIToolTip`. That is concrete client evidence for minimap-specific owner logic rather than only simulator-side UI structure.

### 11. Explicit skill-macro owner parity discovered by follow-up IDA lookup

- `CUIMacroSysEx::OnCreate` at `0x85c420`

Notes:
The broader progression/utility owner list already confirmed the macro dialog family, but the explicit follow-up row should stay visible in the seam index too. `CUIMacroSysEx::OnCreate` confirms that skill macros are owned by a dedicated dialog with its own control layout and embedded text-entry surface rather than only by generic text input or utility-button launches.

### 12. Packet-owned keymap and pet auto-consume parity discovered by targeted IDA scan

- `CFuncKeyMappedMan::OnInit` at `0x568c30`
- `CFuncKeyMappedMan::OnPetConsumeItemInit` at `0x5688c0`
- `CFuncKeyMappedMan::OnPetConsumeMPItemInit` at `0x5688f0`

Notes:
This packet-owned bootstrap seam is distinct from both the `CQuickslotKeyMappedMan` owner tracked in backlog 7 and the explicit key-config window owners tracked above. The targeted IDA pass already pinned `CFuncKeyMappedMan::OnInit` plus the pet-consume init pair as their own owner family, so keymap hydration and pet auto-consume defaults should stay indexed separately from the later UI-owner rows.

### 13. Packet-owned anti-macro result parity discovered by targeted IDA scan

- `CWvsContext::OnAntiMacroResult` at `0x9ff580`
- `CWvsContext::SaveAntiMacroScreenShot` at `0x9fa330`
- `CWvsContext::ShowAntiMacroNotice` at `0x9f3c30`

Notes:
The anti-macro dialog owner is only one half of the client surface. These `CWvsContext` handlers confirm a separate packet/result controller that owns launch, screenshot, notice, and result-routing behavior before the later `CUIAntiMacro` or `CUIAdminAntiMacro` owner finishes the challenge flow.

### 14. Anti-macro owner parity discovered by targeted IDA scan

- `CUIAntiMacro::OnCreate` at `0x78b980`
- `CUIAntiMacro::Draw` at `0x78bb30`
- `CUIAntiMacro::Update` at `0x77f1f0`

Notes:
This owner row remains intentionally separate from row 13. `CUIAntiMacro::{OnCreate,Draw,Update}` confirm a timed text-entry dialog owner with its own layout, countdown, and edit-control lifecycle instead of only a packet/result bridge or generic notice path.

| Area | Status | Gap summary |
| --- | --- | --- |
| Anti-macro challenge dialog parity | Partial | `HaCreator/MapSimulator` now keeps the packet-owned anti-macro owner on the client-observed `CUIAntiMacro::{OnCreate,Draw,Update}` seam instead of only a packet bridge. `MapSimulator.PacketOwnedAntiMacroParity.cs` still registers dedicated normal/admin owners, both surfaces still draw from `UI/UIWindow2.img/Macro/step1*`, stay centered from the active WZ frame, keep the recovered countdown digit coordinates (`95,39`, `115,39`, `137,39`, `147,39`, `167,39`) and attempt-text origin (`222,206`), and still route the attempt line through `MapleStoryStringPool` string id `0x1A15` before falling back. The anti-macro answer runtime now prefers a true hosted Win32 child edit box over the older fully managed approximation when the simulator window handle is available, preserving the client `OnCreate` contract (`id=1000`, `42,189,194,16`, `StringPool 0x1A25`, `nBackColor=0`, `nFontColor=0xFF000000`, `nHorzMax=12`) while delegating caret blink, selection rendering, clipboard, and IME candidate behavior to the OS control pipeline. The remaining gap is that this is still a hosted desktop `EDIT`/`TextBox` child rather than MapleStory's exact in-client `CCtrlEdit` implementation and message loop, so the runtime is much closer to native ownership than before but not byte-for-byte identical to the client widget stack. |

### 15. Additional quest-timer UI, quest-reward, and revive owners discovered by follow-up IDA scan

- `CUIQuestTimer::OnCreate`
- `CUIRaiseWndBase::OnButtonClicked` at `0x8391d0`
- `CUIRevive::OnCreate` at `0x83cea0`
- `CUIRevive::OnButtonClicked` at `0x83ce40`
- `CUIRevive::Update` at `0x83ce70`
- `CUIRevive::Revive` at `0x83cde0`

Notes:
The latest IDA pass exposed another utility-owner family that should stay visible in the seam index: dedicated quest-timer UI, quest reward/raise windows, and revive confirmation are all separate modeless owners rather than generic extensions of the quest log, field-state packet handling, or raw respawn state.

### 3. Packet-authored local overlays and guidance parity

- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`
- `CUserLocal::OnNotifyHPDecByField` at `0x90fed0`
- `CUserLocal::OnQuestGuideResult` at `0x90f1e0`
- `CUserLocal::OnDeliveryQuest` at `0x90ef60`
- `CUserLocal::OnOpenClassCompetitionPage` at `0x905580`

Notes:
These handlers form a narrower client-owned feedback layer under `CUserLocal::OnPacket`, and the current backlog only references them inside the broad dispatcher row. IDA now confirms concrete responsibilities that deserve separate tracking: `OnFieldFadeInOut` decodes fade-in, fade-out, and duration fields then registers the animation relative to the avatar layer Z; `OnBalloonMsg` decodes text, width, lifetime, and either avatar-relative or explicit world-position placement before calling `ShowBallonMsg`; `OnDamageMeter` resets the dedicated damage-meter timer owner and updates shared timing state in `CWvsContext`; `OnQuestGuideResult` rebuilds quest-demand targets and opens the world-map quest-guide owner when the packet contains viable quest-mob targets; `OnDeliveryQuest` either opens the dedicated delivery-quest owner or routes the result back through the status-bar chat seam when another unique modeless owner is active; and `OnOpenClassCompetitionPage` is a packet-driven utility launch rather than a normal button path.

### 4. External cash-service stage parity

- `CCashShop::Init` at `0x484920`
- `CCashShop::OnPacket` at `0x4997e0`
- `CCashShop::OnCashItemResult` at `0x499370`
- `CITC::Init` at `0x573ee0`
- `CITC::OnPacket` at `0x577470`
- `CITC::OnNormalItemResult` at `0x5771d0`

Notes:
This service-stage surface is distinct from the in-map `CAdminShopDlg` approximation already tracked above. IDA shows `CCashShop::Init` and `CITC::Init` clearing field UI, constructing their own staged child-window layouts, selecting service BGM, and entering standalone stage owners rather than opening one more utility dialog over the live HUD. The packet routers are distinct too: `CCashShop::OnPacket` dispatches cash-service result families such as charge-param, query-cash, cash-item, purchase-exp, gift-mate, duplicate-id, name-change, transfer-world, gachapon, one-a-day, and free-item notices, while `CITC::OnPacket` owns its own smaller result family around charge-param, query-cash, and normal-item results. That means Cash Shop and ITC parity should be tracked as their own service-stage owner family rather than being treated as a subset of status-bar launches or the current admin-shop fallback.

### 5. Cash-service child-window and preview parity discovered by targeted IDA scan

- `CCSWnd_Char::OnCreate` at `0x4c4590`
- `CCSWnd_Char::OnDefaultAvatar` at `0x4bec80`
- `CCSWnd_Char::OnWear` at `0x4c5a60`
- `CCSWnd_Char::SetPet` at `0x4bc400`
- `CCSWnd_Char::ShowMessageBox` at `0x4bbb00`
- `CCSWnd_Char::ShowPersonalShop` at `0x4bc660`
- `CCSWnd_Char::ShowEntrustedShop` at `0x4bc6b0`
- `CCSWnd_Char::BlowWeather` at `0x4c5d30`
- `CCSWnd_Locker::OnCreate` at `0x4c31c0`
- `CCSWnd_Inventory::OnCreate` at `0x4c34f0`
- `CCSWnd_List::OnCreate` at `0x4d0500`
- `CCSWnd_Status::OnCreate` at `0x4cb840`
- `CCSWnd_OneADay::OnCreate` at `0x4d2fd0`
- `CCashTradingRoomDlg::OnCreate` at `0x49e8c0`

Notes:
The stage-owner row above is still too coarse on its own. IDA shows that Cash Shop and related cash-service flows split into dedicated child-window owners after `CCashShop::Init` creates the stage: `CCSWnd_Char::OnCreate` allocates its own avatar preview layer, user-preview actor, physical-space helpers, and `BtBuyAvatar`/`BtDefaultAvatar`/`BtTakeoffAvatar` controls before calling `OnDefaultAvatar`; the adjacent `CCSWnd_Char` methods also expose stage-local preview actions for wear/takeoff, pet preview, weather, message box, personal shop, and entrusted shop effects rather than routing those previews through generic field windows. Separate owners then build the rest of the service UI: `CCSWnd_Locker`, `CCSWnd_Inventory`, `CCSWnd_List`, and `CCSWnd_Status` each have their own `OnCreate` routines, `CCSWnd_OneADay::OnCreate` owns the One-a-Day panel inside the service, and `CCashTradingRoomDlg::OnCreate` builds a standalone cash trading room with its own chat edit, trade/coin buttons, scrollbar, fonts, and initial cash balance. None of these client-owned child-window seams are named anywhere else in the backlog today even though they sit directly on the parity path for cash-avatar preview and service-stage usability.

### 16. Packet-owned NPC shop, store-bank, and battle-record owners discovered by follow-up `CField::OnPacket` decompile

- `CShopDlg::OnPacket` at `0x6eb7d0`
- `CStoreBankDlg::OnPacket` at `0x745c60`
- `CBattleRecordMan::OnPacket` at `0x470ba0`

Notes:
The latest `CField::OnPacket` decompile exposed another utility-owner family that is still unnamed in this backlog. Packet types `364`/`365` dispatch directly into `CShopDlg::OnPacket`, `369`/`370` dispatch into `CStoreBankDlg::OnPacket`, and `420` through `423` dispatch into `CBattleRecordMan::OnPacket` instead of flowing through the broader `CUserLocal::OnPacket` utility row or the existing social/storage packet rows. Those owners do not appear anywhere else in the current backlog set, so NPC shop parity, store-bank parity, and battle-record parity are currently easy to overstate once adjacent window shells or NPC interactions exist locally.

### 17. Additional packet-owned local production, reward, and helper-result parity discovered by direct `CUserLocal::OnPacket` scan

- `CUserLocal::OnMakerResult` at `0x9102f0`
- `CUserLocal::OnMesoGive_Succeeded` at `0x90f950`
- `CUserLocal::OnMesoGive_Failed` at `0x90d530`
- `CUserLocal::OnRandomMesobag_Succeeded` at `0x90fa30`
- `CUserLocal::OnRandomMesobag_Failed` at `0x90d560`
- `CUserLocal::OnResignQuestReturn` at `0x905720`
- `CUserLocal::OnPassMateName` at `0x918260`

Notes:
The direct `CUserLocal::OnPacket` re-check exposed one more local-owner family that is still missing from this backlog. These handlers do not fit the existing UI-launch, overlay, tutor, or radio rows: `OnMakerResult` is a dedicated packet-owned production-result path adjacent to `CUIItemMaker`, `OnMesoGive_*` and `OnRandomMesobag_*` are separate money/reward result owners rather than generic pickup notices, and `OnResignQuestReturn` plus `OnPassMateName` are packet-authored helper responses that sit beside quest/social utility flow instead of ordinary field feedback. A repo search across `HaCreator/MapSimulator` still does not surface corresponding runtime or packet-bridge seams for any of these handlers, so this family remains genuinely untracked rather than merely under-described.

### 18. Additional packet-owned logout-gift owner parity discovered by direct `CField::OnPacket` decompile

- `CField::OnPacket` at `0x546d50`
- `CWvsContext::OnLogoutGift` at `0x9cccb0`
- `CUILogoutGift::OnCreate` at `0x7dec80`
- `CUILogoutGift::Draw` at `0x7de9b0`

Notes:
Direct `CField::OnPacket` decompile now confirms packet `432` is a dedicated logout-gift branch that calls `CWvsContext::OnLogoutGift` instead of flowing through generic local utility handlers. MapSimulator now mirrors that seam with a dedicated `LogoutGiftWindow` owner registered beside the other packet-owned utility owners, a packet `432` dispatcher branch that refreshes and surfaces that owner through the `CWvsContext::OnLogoutGift` path, and a `SetField` decode hook that peels the client-confirmed trailing 12-byte logout-gift cache off mixed character-data tails while preserving any preceding adjacent tail bytes as exact raw bytes plus aligned `int32` views instead of collapsing them to a count. That preserved aligned lead-in is now also mapped to the exact contiguous `CWvsContext` precursor dword slots that sit immediately before the `CUILogoutGift::OnCreate` commodity cache reads at `dword[4138..4140]`, so the simulator surfaces ownership as `CWvsContext dword[...] @ 0x...` instead of leaving those bytes entirely anonymous. `CUILogoutGift::OnCreate` and `CUILogoutGift::Draw` now inform both the simulator window geometry and click-slot behavior, so selecting a slot mirrors `CUILogoutGift::OnButtonClicked` by recording the client opcode `313` slot request rather than treating the click as pure local commodity navigation. The owner still attempts to use authored `UI/UIWindow.img/LogoutGift/backgrnd` art when a dataset exposes it and falls back otherwise. The remaining parity gap is narrower: the preserved lead-in bytes are now mapped to contiguous client-owned slots, but their exact semantic field names and any other client consumers are still unknown, and the active extracted UI dataset still does not expose a recoverable `LogoutGift` node even though the generated string-pool references that asset path.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.
- Progression or utility window groundwork now has a shared seam: named owners for world map, map transfer, cash-service, crafting, enhancement, character-info, and quest-side utility windows are registered with `UIWindowManager`, while minimap and status-bar utility buttons now route into those owners instead of staying entirely dead-ended. The dedicated pre-field `WorldSelect`, `ChannelSelect`, and `ChannelShift` selector family is tracked in backlog 11; this utility-window document only keeps the gameplay-side launch and gating hooks that hand control over to those login-owned selectors. The map-transfer owner now also keeps saved teleport destinations in a character-scoped destination book keyed to the active player or selected login-roster character instead of sharing one global saved list across every simulator avatar, persists that destination book under the simulator AppData profile across restarts, and hands the currently selected transfer target into the world-map owner when `BtMap` is used so the two navigation surfaces stay linked.

## Broad IDA Cross-Check

A broad follow-up `list_functions` sweep against the v95 client, compared back against the full backlog corpus, did not surface another major owner family that still lacks a backlog home.

The candidates that initially looked like possible omissions are already represented by existing rows instead of needing a new split document or duplicate utility section:

- `CWorldMapDlg` / `CreateWorldMapDlg` remain covered by the world-map and minimap-owner rows in this backlog.
- `CSlideMenuDlg` remains covered by the packet-script/message row in backlog 7, where the client owns it through `OnAskSlideMenu`.
- `CConnectionNoticeDlg`, `CPinCodeDlg`, and `CConfirmRaceDlg` remain covered by the login and character-entry backlog in document 11.
- `CWeddingWishListDlg` plus `CWishListRecvDlg`, `CWishListGiveDlg`, and `CWishListInputDlg` remain covered by the companion/social backlog in document 10.
- `CAdminShopDlg`, `CBookDlg`, `CStoreBankDlg`, `CGuildRankDlg`, and the other high-signal utility/social families surfaced by the scan are already named inside this backlog or the dedicated social backlog instead of being truly absent.

That means the right follow-up from this scan is to keep extending the existing rows above, not to add another umbrella utility backlog section that would duplicate ownership across documents.

## Remaining Parity Backlog

### 1. Progression and utility window parity

This area is currently absent from the backlog set even though the simulator already has concrete window and feedback implementations.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Minimap window parity | The minimap now loads WZ-backed helper markers for generic NPCs, quest-start NPCs, quest-end NPCs, portals, out-of-bounds direction arrows, and the broader `DefaultHelper` remote-user families from `UIWindow2.img/MiniMapSimpleMode/DefaultHelper`; it upgrades the post-Big-Bang generic NPC notice marker plus off-map direction overlays to the animated `UIWindow2.img/MiniMap/iconNpc` and `MiniMap/iconDirection/*` frame families when those client assets are present, feeds visible portal markers plus quest-aware NPC marker families into `MinimapUI`, keeps the post-Big-Bang `BtNpc` list panel from `ListNpc`, and now also tolerates the client-shaped fallback where only quest-start or quest-end marker art is present instead of assuming the generic NPC helper family always exists before the NPC toggle can work. The minimap now also feeds same-field simulator social/family entries into the minimap as `another` / `friend` / `party` / `partymaster` / `guild` / `guildmaster` helper markers, reusing family-chart positions when the simulator has them and otherwise assigning deterministic local field positions so those helper families can render with matching off-map direction overlays instead of staying dead data. It also resolves a local helper marker through the same seam, so active Match Cards rooms can switch the player dot to the WZ `match` family and personal- or entrusted-shop field actors can switch it to `usertrader`; that helper path now suppresses the legacy fallback yellow pixel dot while those local helper families are active instead of double-drawing both markers, and it now routes those local helper-family markers through the same off-map direction-overlay seam as the rest of the helper surface instead of letting them bypass the minimap-owned clamp path. Visible Match Cards participants plus shop occupants still seed `match` / `anothertrader` helper markers even when they only have runtime occupant state and no packet-authored world position, and packet-authored remote helper markers now win over same-name synthetic social/trader/match fallback markers so the minimap stops double-drawing the same user when live helper packets are present while still reusing the richer tracked tooltip text for that packet-owned marker. This pass also extends the packet-owned field seam itself: visible remote users without an explicit helper packet now still seed tracked minimap entries from live field presence, so same-field packet actors can fall back to the WZ `DefaultHelper/another` family instead of remaining absent until a separate helper packet arrives, while packet-authored Battlefield team assignments now flow into the same tooltip seam as explicit helper markers rather than leaving those match-shaped markers as name-only hover targets. The minimap owner now also actually wires its hover seam instead of leaving it dormant: NPC markers expose quest-state-aware name tooltips, portal markers expose script or map-target fallback text, helper markers now surface tracked remote or synthetic user names, and the active personal-shop or entrusted-shop employee snapshot now seeds a dedicated minimap marker plus tooltip path using the same owner-local hover chrome and off-map direction overlay rules while preferring the WZ `DefaultHelper/usertrader` trader-family marker with `anothertrader` or NPC fallback when those helper canvases are absent. `BtBig` still opens a dedicated `WorldMap` owner whose `WorldMapSearch` side panel is no longer just background art: `BtSearch` switches into a dedicated WZ-backed search surface with `BtAllsearch` return flow, optional `BtLevelMob` mob-only filtering, live field/NPC/mob result rows sourced from the current simulator field, and a working text-entry search path that filters those result families plus global field-name or map-id matches, now keeps same-label search rows distinct by keying selection on the full result identity instead of collapsing them to kind-plus-map-plus-label alone, supports keyboard navigation or activation, accepts clipboard paste into the search box, now uses the client `UIWindow2.img/WorldMap/title` canvas instead of a text-only heading, keeps the search caret and visible query text clipped inside the edit box when the query overruns the field, lets mouse clicks reposition the caret inside that visible query span, adds word-wise `Ctrl+Backspace` / `Ctrl+Delete` editing on top of the existing keyboard path, now also adds word-wise `Ctrl+Left` / `Ctrl+Right` caret travel inside that same edit seam, routes committed text through the shared `GameWindow.TextInput` seam instead of relying only on raw key translation, previews Win32 IME preedit text inline with a live underline before commit, now also renders an owner-local IME candidate list anchored to that same search box via the shared candidate seam instead of dropping candidate updates on the floor, measures the candidate surface against the visible page entries so both horizontal width and page-local numbering track the live candidate page instead of stale off-page entries, allows mouse clicks on that candidate surface to commit the selected IME entry through the same Win32 candidate-selection bridge already used by the skill-macro owner, clamps that candidate owner to the actual `WorldMap` frame bounds instead of letting placement logic drift outside the live owner, now also honors the captured Win32 candidate-window form styles (`CANDIDATEFORM` rect/point/exclude variants) when placing that owner-local candidate list so IME-provided anchors can override the caret fallback before the result is clamped back into the `WorldMap` owner, and now also treats the candidate surface plus search edit/results chrome as part of the same non-draggable owner interaction seam so clicks over live search UI stop falling through into window-drag ownership while `BtAll` / `BtAnother` browsing, current-map focus, paged field selection, and direct map-transfer queuing still run through the existing pending-map seam. The remaining gap is the client's exact trader or match helper semantics beyond the simulator's current local runtime synthesis, the richer `DefaultHelper` cases that depend on packet state the simulator still does not decode, the final packet-authored remote-user helper semantics beyond the broader same-field `another` / relationship / Battlefield-team fallback now in place, and the last exact IME candidate-window chrome polish inside that search owner rather than missing placement ownership or candidate-window anchor data | Navigation feedback now has animated minimap-owned notice and direction art, quest- and portal-aware markers, a broader WZ-backed remote-user helper surface, runtime-driven local and remote `match` / trader helper-family usage whose local helper swap now actually replaces the old pixel dot and shares the same off-map direction-overlay seam, active employee markers that now prefer the trader helper family before falling back to NPC art, live minimap hover feedback for NPC, portal, helper, and active employee markers, true WZ-backed hover chrome inside the world-map search rows instead of treating the `mouseOverBase` art like a selection-only fill, and an owner-local IME candidate list for search input whose live page now sizes and numbers itself from the visible candidates, stays constrained to the world-map owner, can be committed by direct mouse selection through the shared Windows IME bridge instead of behaving like dead overdraw, and now follows captured Win32 candidate-window rect/point/exclude placement before falling back to the local caret anchor. Packet-owned remote helper markers now also stay authoritative over same-name synthetic social or trader fallback markers, a live helper packet no longer yields a second duplicate minimap marker just because the simulator can also infer that user from party/guild/friend, family, match, or trader context, and packet-authored same-field users without helper packets now still surface through the generic `another` helper family with shared tooltip routing instead of remaining absent from the minimap until a dedicated helper update arrives. The remaining gap is narrowed to exact trader-or-match semantics, richer packet-decoded helper states the simulator still does not recover, the client's last packet-authored remote-helper edge cases, and the final candidate-window chrome details rather than dormant minimap hover data, missing employee hover routing, a double-drawn local helper marker, duplicate same-user helper overlays, helper silence for generic packet-owned field actors, name-only Battlefield-team hover text, candidate-text clipping, off-page candidate sizing, dead candidate clicks, candidate-owner escape, drag-through candidate chrome, same-label search-row collapse, missing candidate feedback, a static search panel, or ignored IME candidate-window placement data | `MinimapUI.cs`, `WorldMapUI.cs`, minimap/window loader path |
| Partial | Map transfer / navigation window parity | The simulator now routes minimap `BtMap` into a dedicated WZ-backed `MapTransfer` owner built from the `Teleport*` window art, constrains the destination surface to the client's five visible rows, caps saved teleport destinations to a client-shaped five-vs-ten slot budget based on the loaded `Teleport` versus continent `Teleport2`/`Teleport3` art variant, respects teleport-item field limits separately from generic migration checks, layers return-map plus portal-derived routes on top of the saved list while driving actual map changes through the existing pending-map seam instead of stopping at the minimap shell, keeps saved teleport destinations in a character-scoped destination book keyed to the active player or selected login-roster character instead of one simulator-global list, now splits that per-character book into client-shaped regular-versus-continent slot banks so the active 5-slot `Teleport` owner no longer shares saved rows with the 10-slot `Teleport2`/`Teleport3` owner the way the client keeps `CharacterData::adwMapTransfer` and `adwMapTransferEx` separate, persists those per-character books under the simulator AppData profile across restarts with legacy single-book saves migrated into the continent-capable bank, opens the world-map owner focused on the currently selected transfer target rather than always resetting to the current field, preserves fixed per-character slot indices instead of compacting into a generic list, renders empty saved slots inside the transfer window, keeps the selected saved slot alive through the world-map handoff so a `[Target]` selection can be written back into that exact slot through `BtRegister`, now draws the client-shaped saved-count header instead of treating the list like an unbounded generic scroller, resolves saved-slot labels from map IDs rather than persisting simulator-only custom names, exposes the client-owned edit-target lane as a local numeric map-id entry path with 12-digit input, focus-driven selection clearing, paste support, and direct `BtMove` routing when no list row is selected, now loads the client `FadeYesNo/backgrnd7` plus `BtOK` / `BtCancel` confirmation chrome so register, delete, saved-route, portal-route, and manual map-id requests all pass through a local yes/no confirmation step instead of mutating or moving immediately, applies the client-shaped saved-destination category gate from `CUIMapTransfer::OnRegister` so sub-`100000000` ids and `xx9xxxxx` map families cannot be written into saved teleport slots while duplicate-save attempts refocus the already-owned slot instead of silently pretending to create a second entry, now also feeds the save gate through loaded WZ `MapInfo` metadata so command-locked (`noMapCmd`), move-limited, and special non-default `fieldType` destinations fail locally with the same unsaveable result path instead of only surfacing after a server-shaped rejection, mirrors the continent variant's visible client-owned scrollbar path by wiring `UI/Basic.img/VScr9` into the 10-slot `Teleport2`/`Teleport3` owner instead of leaving the scrollbar logic disconnected, now pushes simulator-local register/delete replies through a synthesized `CWvsContext::OnMapTransferResult`-shaped payload whose leading result code plus continent flag mirror the client-owned `2`/`3` success and `5`/`9`/`10` failure split before the fixed 5-slot or 10-slot arrays are rehydrated and persisted back to the per-character AppData book, now also keeps those same request/result seams swappable with a live Maple session by encoding client-shaped opcode `114` register/delete requests, filtering inbound opcode `69` result packets through a dedicated map-transfer official-session bridge, and applying the returned field arrays back through the existing runtime instead of always stopping at local synthetic replies, now also mirrors the broader client `CWvsContext::OnMapTransferResult` failure surface for proxied opcode `69` replies so result codes `6` / `7` / `8` / `11` resolve through the client-owned StringPool path instead of degrading into unlabeled failures, and now also decodes the official `CStage::OnSetField` character-data prelude closely enough to recover the real `CharacterData::Decode` `dbcharFlag` so `0x1000` map-transfer ownership can be detected before the field fully comes up. When that flag is present, the simulator conservatively recovers the client-shaped `5 + 10` `adwMapTransfer` / `adwMapTransferEx` arrays from the authoritative CharacterData tail and hydrates the active player or selected login-roster character's regular and continent destination books on field entry instead of waiting for a later local save or opcode `69` reply, and now peels the client-owned trailing 12-byte logout-gift cache back off the SetField tail before probing those authoritative arrays so bootstrap recovery follows the same `CharacterData::Decode` then `CWvsContext::OnSetLogoutGiftConfig` boundary the client uses instead of scanning straight through the adjacent logout-gift bytes. It still does not mirror the exact full remaining `CharacterData::Decode` payload boundary and surrounding session tail ownership beyond that logout-gift-aware authoritative-array recovery, the full automatic network/session ownership around `CWvsContext::SendMapTransferRequest` and `OnMapTransferResult` beyond the new optional live-session bridge, or the exact full server-side destination-validation matrix beyond the newly mirrored WZ metadata checks | Navigation testing can now exercise a more client-shaped transfer and destination-selection surface above raw portal travel, saved destinations no longer bleed across unrelated simulator characters or across the regular-vs-continent transfer books, the saved-slot header, scrollbar, raw map-id presentation, and fixed-slot semantics are closer to the official owner, the edit target no longer behaves like dead art, world-map retargeting now lands in the intended saved slot instead of collapsing back into a generic append path, saved-slot registration now follows the same broad category gate the client applies before writing `adwMapTransfer` / `adwMapTransferEx`, local save attempts also now reject obvious WZ-authored special or command-locked destinations before a synthetic or proxied request is sent, register/delete/move requests now all pass through the same WZ-backed yes/no chrome, the simulator can optionally proxy the client-shaped `114`/`69` packet pair through a live Maple session and rehydrate the fixed slot arrays from the returned result payload instead of only local loopback state, official `69` failures now surface through the broader client result-code notice path instead of degrading into raw unlabeled codes, and official `SetField` CharacterData can now seed the per-character regular and continent transfer books on field entry before the player touches the transfer UI while respecting the adjacent logout-gift cache boundary the client keeps outside `CharacterData::Decode`. The remaining gap is narrowed to the still-unmirrored remainder of the full `CharacterData::Decode` tail boundary beyond this logout-gift-aware split, automatic session ownership instead of command-driven bridge attachment, and the server's exact remaining unsaveable-map rules beyond the current category plus WZ-metadata gate | navigation/UI layer, minimap integration, destination-selection flow (`CUIMapTransfer::Draw`, `CUIMapTransfer::OnButtonClicked`, `CUIMapTransfer::OnCreate`) |
| Partial | Cash shop / admin shop dialog parity | The simulator now replaces the generic Cash Shop and MTS placeholders with a dedicated `AdminShopDialogUI` owner backed by `UIWindow2.img/Shop`, reuses the shop dialog frame plus `BtBuy` / `BtSell` / `BtExit` / `BtRecharge`, keeps Cash Shop and MTS inside the same button-driven surface, routes the status-bar launchers so only the requested service window stays open, exposes separate scrollable NPC-offer and user-listing panes with per-row selection and request submission messaging, consumes the client `UIWindow2.img/Shop/TabShop` category strip as a real WZ-backed catalog filter, now also consumes the owner `TabBuy` and `TabSell` tab canvases with their client WZ offsets instead of leaving the service and pane strips as text-only chrome, now anchors the `TabShop` category strip to the client-owned bottom control position from `CAdminShopDlg::OnCreate` instead of treating the zero-origin canvases like a stack at window origin, mirrors the client-owned twin scrollbar layout called out by `CAdminShopDlg::OnCreate`, replaces the wish-list footer prompt with a `FadeYesNo`-style confirmation modal and now routes `BtRecharge` through that same owner-local yes/no step before the dedicated `CUIAdminShopWishList`-style window opens, matching the `CAdminShopDlg::OnButtonClicked` launch shape instead of jumping straight into the search owner, syncs the displayed balance to the live inventory meso runtime like `CAdminShopDlg::Draw`, now renders WZ-backed item icons plus stack counts for requestable rows in both panes and the detail area so the dialog no longer behaves like a text-only catalog despite `CAdminShopDlg::DrawNPCItem` / `DrawUserItem`, resolves simulator-side shop requests through a broader local session matrix with meso consumption, inventory-capacity checks, actual item delivery for requestable rows, post-purchase sold-out locking on one-shot listings, rejected outcomes such as seller-offline, expired, inventory-full, and refreshed-sold-out responses, plus real icon carry-through on delivered items instead of treating every non-expansion row as the same generic acknowledgment, now interprets the left browse strip as the real WZ-backed `ALL` / `MOST` / `SELL` / `BUY` / `RE-BUY` family for Cash Shop while MTS swaps to its own WZ-backed `ALL` / `MOST` / `SELL` / `BUY` subset from `UIWindow2.img/Shop/TabSell`, and keeps the right `TabSell` strip acting as WZ-backed quick category tabs (`Equip` / `Use` / `Etc` / `Set-up` / `Cash`) instead of fake Cash Shop or MTS toggles, with featured-row curation for `MOST`, pane focus changes for `SELL` and `BUY`, in-session wish-list plus delivered-row state restoration so `RE-BUY` can surface previously granted rows after page flips, and browse-surface state memory keyed to the active pane plus browse/category strip so tab changes restore the prior row focus and scrollbar position instead of snapping back to the top of every filtered surface. The Cash Shop NPC pane now also resolves sample commodity metadata against live `Etc/Commodity.img` rows so matching entries inherit WZ-backed price, stack-count, commodity-SN, on-sale state, stack-limit data, and commodity-period text, while seeded rows whose best live commodity is off sale now stay preview-only instead of remaining requestable on hard-coded sample defaults, the packet-owned `OnGoToCommoditySN` launch seam now focuses the matching Cash Shop row plus category inside the existing owner after migration and can synthesize a WZ-backed NPC offer on demand when the serial number resolves to a commodity that was not part of the simulator窶冱 seeded sample set, and the owner now mirrors the `AskItemCount` branch inside `CAdminShopDlg::SendTradeRequest` for stackable sample rows by opening a quantity-selection modal that reuses WZ-backed `UIWindow2.img/UtilDlgEx/BtPrev` and `BtNext`, supports keyboard plus mouse-wheel count changes, and now clamps against the live reward item `info/slotMax` data during inventory-capacity checks and delivered stack totals instead of falling back to generic per-tab defaults. It now also mirrors the singly treated `SendTradeRequest` branch by routing one-shot purchases and zero-price source-item exchanges through the same owner-local yes/no modal seam before the request is staged, instead of jumping straight from row selection into the pending state. This pass also mirrors the zero-price inventory-source validation branch inside `CAdminShopDlg::SendTradeRequest` for dedicated trade-in rows by adding local zero-meso exchange entries, clamping the quantity modal against live source-item stock, validating WZ `info/cash`, `info/notSale`, and `info/quest` trade restrictions before submit, surfacing owned-count messaging in the detail pane, and only consuming the matching source item when delivery succeeds instead of treating every request as mesos-only. The dialog also supports owner-local keyboard parity for the current browse surface: `Up` / `Down` / `PageUp` / `PageDown` / `Home` / `End` walk the active pane, `Tab` / `Left` / `Right` swap between the NPC-offer and user-listing panes, `Enter` submits the highlighted request, `Space` opens the wish-list confirmation when supported, `Enter` / `Escape` resolve the `FadeYesNo` modal without forcing mouse-only interaction, the quantity modal also maps `Left` / `Right` / `Up` / `Down` plus `PageUp` / `PageDown` / `Home` / `End` to count changes, and mouse double-click on the already-selected row now fires the same request path the client uses in `CAdminShopDlg::OnMouseButton` instead of requiring the explicit buttons or keyboard only. It still does not mirror the full `CAdminShopDlg` behavior surface such as true packet-backed shop-session state beyond commodity-SN targeting and local synthetic commodity insertion, live server-populated catalogs, the exact official semantics behind each client tab state, the client's full sell-or-relay request matrix beyond these local source-item trade-ins, or the wider server-authored request and trade matrix behind those controls | The simulator can now exercise the client cash-service launch path through a dedicated dialog owner with concrete per-item browsing, mode-specific WZ-backed browse chrome for Cash Shop versus MTS, client-shaped scrollbar and category-tab affordances whose bottom `TabShop` strip now lands on the client-owned control geometry instead of a zero-origin overlap, visible item art in the browse and detail surfaces, a real wish-list confirmation step whose `BtRecharge` path now gates the wishlist owner behind the same local yes/no prompt the client uses, live balance readout, a keyboard-drivable selection and request flow, client-shaped row double-click activation, remembered per-surface browse position instead of always dropping back to the first row after each tab/filter hop, an owner-local count prompt for stackable rows that no longer collapses every request into quantity `1`, a matching owner-local yes/no confirm step for singly treated requests, and now a first local zero-price trade-in seam that validates WZ-backed source-item restrictions and live owned counts before consuming the required inventory item on success. Using `Etc/Commodity.img` for matching NPC rows, honoring packet-owned commodity-SN focus, synthesizing WZ-backed fallback commodity rows when the requested SN is outside the seeded sample list, applying live `slotMax` limits to the quantity modal, and now pushing `Commodity.img` off-sale plus period metadata back onto seeded rows further moves the owner onto data the client actually owns instead of simulator-only placeholders. The remaining gap is narrowed to true live-catalog/session sourcing, deeper tab semantics, the broader sell-side and relay request matrix beyond the current local source-item exchanges, and the wider server-authored trade/session rules rather than missing quantity prompts, single-item confirm prompts, commodity targeting, off-sale commodity gating on seeded rows, commodity-row creation for packet-owned serials, recharge-gated wishlist launching, zero-price source-item validation, category-strip placement, browse-position restoration, row double-click activation, shared browse art, unlabeled placeholder tabs, or a mouse-only dialog | cash-service UI layer, status-bar integration, shop-session flow (`CAdminShopDlg::OnCreate`, `CAdminShopDlg::Draw`, `CAdminShopDlg::OnButtonClicked`, `CAdminShopDlg::OnMouseButton`, `CAdminShopDlg::SendTradeRequest`, `CAdminShopDlg::OnChildNotify`) |
| Implemented | Status-bar login selector launch integration | The simulator now routes `BtChannel` into the dedicated login-scene `WorldSelect`, `ChannelSelect`, and `ChannelShift` owners instead of leaving the status-bar button as a dead-end in-field affordance. That launch path preserves the current world/channel context and hands control over to the pre-field selector runtime rather than duplicating a second world/channel browser inside the general utility-window layer. On the in-map utility side, the status-bar and System-popup `BtChannel` launch seam now mirrors the client-owned `CWvsContext::ChannelShift` guard order the simulator models in `MapSimulator.cs`: selector access is suppressed while a selector request is pending, while the packet-owned anti-macro challenge owns the field, while the packet-owned initial-quiz timer is active, and while the current field forbids migration/channel change; when those field-side guards trip, any open selector windows are collapsed back out of the gameplay scene instead of lingering as valid utility owners. The selector owners, packet-fed world metadata, `BtViewAll` roster handoff, and official-session bridge work remain tracked in backlog 11, but those are separate login-runtime parity tasks rather than remaining work in this status-bar launch row. | This row is now complete as a status-bar integration seam: the in-field launcher opens the dedicated selector owners, respects the same anti-macro, initial-quiz, pending-request, and no-migrate suppression branches that gate `CWvsContext::ChannelShift`, and tears down gameplay-side selector owners when those guards become active. Remaining login-scene selector metadata and packet/session ownership stay with backlog 11 by design rather than as unfinished work here. | status-bar utility-button integration (`CUIStatusBar::OnButtonClicked`, `CWvsContext::ChannelShift`, simulator seam: `MapSimulator.cs`) |
| Partial | Packet-owned admin-shop dialog parity | `CField::OnPacket` routes packet types `366` and `367` into `CAdminShopDlg::OnPacket`, and the decompile shows that the in-map admin-shop dialog is not only a local browse surface. Packet `367` is the open/result gate: when the decoded item-count short is non-zero it either reuses the live `CAdminShopDlg` unique-modeless owner or allocates one and hands the packet to `CAdminShopDlg::SetAdminShopDlg`; when that count is zero it instead opens a `CUtilDlgEx` notice using `StringPool` id `0x1238` and then sends opcode `74` mode `2`. Packet `366` is the follow-up result owner for an already-live dialog: it clears `m_bShopRequestSent`, only handles subtype `4`, reselects the sold row on success, optionally reopens the dialog for result codes `1`, `2`, `3`, `6`, `7`, `8`, and `11`, maps the other notice branches through `StringPool` ids `0x1239` through `0x1240` plus `0x16ED`, and sends opcode `74` mode `0` with the NPC template id when the owner reopens. A focused source pass still does not show a matching packet-owned `366`/`367` dispatcher or reopen/result bridge in `HaCreator/MapSimulator`; the simulator's `AdminShopDialogUI` remains primarily status-bar, local-session, and stage-migration driven even though the owner surface itself is now substantial. | Without this row, cash-service parity can look much further along once `AdminShopDialogUI` draws and local request handling works, even though the client still drives open/refuse/result/reopen behavior through a dedicated packet owner on the in-map dialog itself. Keeping this row separate also prevents the in-map dialog lifecycle from being blurred into the distinct `CCashShop` / `CITC` stage owners or the `CUserLocal::OnGoToCommoditySN` migration seam. | packet-owned admin-shop dialog layer (`CField::OnPacket`, `CAdminShopDlg::OnPacket`, `CAdminShopDlg::SetAdminShopDlg`) |
| Partial | Inventory window parity | Legacy and Big Bang inventory windows now render right-aligned meso text, per-slot item icons and stack counts, WZ-backed grade and quality markers, disabled-slot overlays, active-slot highlighting, and Big Bang tab or expanded layouts, while the simulator also feeds meso and picked-up items into the inventory surface at runtime, resolves equip pickup icons through `Character/*/*.img` like the client asset families, respects `info/slotMax`-style stack splitting, merge limits, and pickup-capacity checks for stackable items, wires `BtGather` and `BtSort` into simulator-side stack compaction and ordering, shows shared client-style hover tooltips with cached item names, type labels, stack data, descriptions, broader cure coverage, nearest-town `moveTo` text for `999999999`, post-Big-Bang stat aliases such as `indieSTR` / `indieDEX` / `indieINT` / `indieLUK` / `indieAllStat`, and the wider percentage consumable rows the data authors through `padRate`, `madRate`, `pddRate`, `mddRate`, `accRate`, `evaRate`, `speedRate`, `mesoR`, and `plusExpRate`. The inventory runtime still supports client-shaped intra-inventory drag/drop for occupied-slot swapping, stack merging, trailing-empty-slot moves, a held-item cursor overlay in both layouts, per-tab slot limits that grow in 4-slot rows up to a capped 96-slot budget, `BtCashshop` routing into the simulator cash-service dialog with live-meso inventory expansion, setup-chair activation plus `info/recoveryHP` / `recoveryMP` ticking on a client-shaped 10-second cadence, explicit reject feedback for non-chair setup items, pet-food family/fullness gating with split no-pet/no-compatible-pet/no-hungry-pet feedback, `Consume/*/spec/moveTo` failure-before-burn handling, timed morphs from `item/Consume/0221` `spec/morph` and weighted `spec/morphRandom`, abnormal-status cures, direct-stat and max-vital buffs, attack-speed (`booster` / `indieBooster`), abnormal-status resistance (`asrR`), EXP/drop/meso-rate (`expBuff`, `expR`, `plusExpRate`, `dropRate`, `mesoR`), and attack/defense/accuracy/avoidability/speed percentage buffs through the shared temporary-buff runtime so live totals reflect them instead of only the tooltip text. This pass also adds WZ-backed `spec/thaw` handling to the same consume loop: timed consumables such as `item/Consume/0202/02022001/spec/thaw = 10` and `02022040/spec/thaw = -6` now apply a live environmental-damage reduction against `MapInfo.decHP` field damage for their authored duration instead of remaining tooltip-only, and the shared tooltip path now surfaces that row as environmental-damage protection rather than the raw `Thaw +/-N` placeholder. Slot-authenticated consume flows still burn the acknowledged stack instead of drifting to an identical stack elsewhere in the bag. Focused regression coverage now lives in `UnitTest_MapSimulator/FieldRuleRuntimeConsumableParityTests.cs` for thaw tooltip formatting plus field-rule damage reduction and nullification. | The remaining gap is now centered on the client-authenticated accept/use families for specific non-chair setup items beyond the simulator's generic reject path, deeper pet-feeding fidelity beyond the current WZ family/fullness planner plus local tameness or command-level gain path (especially exact official chat/UI wording and packet-authored success/failure ownership), the broader long-tail consume-item matrix beyond the current recovery/transfer/cure/morph/stat/buff/environmental-protection coverage, and stricter packet-authored accept/reject semantics for unsupported item families rather than missing inventory rendering, drag/drop, tooltip metadata, slot progression, or the common local item-use loop itself | `InventoryUI.cs`, `InventoryUIBigBang.cs`, `InventoryItemMetadataResolver.cs`, `MapSimulator.cs`, `FieldRuleRuntime.cs`, `FieldInteractionRestrictionEvaluator.cs`, `SkillManager.cs`, `CharacterData.cs`, `PetController.cs`, `UIWindowLoader.cs`, `AdminShopDialogUI.cs`, `UnitTest_MapSimulator/FieldRuleRuntimeConsumableParityTests.cs`, inventory/runtime integration (`CUIItem::Draw`, `CUIItem::OnMouseButton`) |
| Partial | Item maker / crafting window parity | The simulator now replaces the `ItemMaker` placeholder with a dedicated WZ-backed `Maker` owner, routes maker-family NPC launches from `String/Npc.img` function text into that owner, derives the visible `Etc/ItemMake.img` bucket set from the live job the same way the client does, rebuilds the selector surface into client-like category/item combos with `All` plus aggregate `Strengthening Gem` / `Monster Crystal` / `Use` / `Setup` / `Etc` pages, keeps WZ/client insertion order instead of locally sorting rows, applies the client-side selector precondition sweep for `reqLevel`, `reqSkillLevel`, `reqItem`, `reqEquip`, and `reqQuest`, preserves the generic `Item Maker` family split instead of dropping all equip outputs, and narrows visible recipes for glove/shoe/toy/generic maker launches. This pass also carries one more client selector/runtime branch through the same owner: IDA-backed `CUIItemMaker::SetComboBox_Category` / `SetComboBox_Item` evidence is now reflected in the simulator with the missing dedicated `Disassemble Equipment` category, the corrected client aggregate-label mapping (`425 = Strengthening Gem`, `426 = Monster Crystal`), a packet-owned disassembly target list sourced from the live EQUIP inventory, and a matching detail/state surface that stages disassembly through the existing gauge/result seam instead of misclassifying packet-owned disassembly results as ordinary maker crafts. The owner exposes `BtStart` / `BtCancel`, animates the maker gauge, seeds starter ore, catalyst, and meso stock, resolves weighted `randomReward` branches, blocks crafts whose fixed or random result cannot fit inventory, carries `tuc` into the fixed-result summary, and now draws the client-shaped target/recipe/gem/catalyst/meso detail sections through the live inventory/runtime icon seams. Progression is now per-character and AppData-backed: family-specific mastery levels advance on success, discovered and hidden recipe ledgers persist on stable WZ row keys (`bucket/outputId`) with raw output ids kept as legacy-load fallback, duplicate-output rows stay distinct, hidden rows are classified from WZ `hide` first with a narrow unlock-hint fallback, hide-only rows can reveal locally once their persistent level/mastery gate passes, and the dedicated `???` page keeps locked hidden entries obscured until a local WZ-backed unlock pass or a packet-owned hidden-unlock payload reveals them. This row now also carries the packet-owned result side farther through the existing seams: local opcode `248` maker-result payloads decode the client-owned `CUserLocal::OnMakerResult` shapes, feed status updates and slot-reset behavior back into the window, reuse the existing craft-completion seam so successful packet-driven crafts advance the same simulator-side discovery/hidden/mastery progression store as local crafts instead of stopping at UI text only, and keep the newly added disassembly selector path packet-owned instead of letting subtype `3/4` results leak into local craft progression. Dedicated hidden-unlock payload support also persists packet-resolved hidden rows into the separate hidden bucket. This run closes most of the remaining session-side owner gap through the same seams: a dedicated maker-session payload can now drive `ServerOwnsCraftExecution`, authoritative disassembly target slots, and authoritative hidden-list membership back into the live `Maker` owner, so packet-owned profession sessions can stage ordinary crafts through the same gauge/result seam as disassembly instead of forcing local reward application, narrow the `Disassemble Equipment` page to the packet-authored slot list rather than every live EQUIP row, and tighten the `???` page to the packet-owned hidden roster while still letting explicit hidden-unlock payloads reveal rows immediately. Focused `UnitTest_MapSimulator` coverage now includes maker-session decode, authoritative disassembly-target filtering, packet-owned hidden-list gating, and the client-backed aggregate category labels alongside the earlier maker-result decode, hidden-unlock decode, keyed progression persistence, family-mastery advancement, and legacy fallback loading coverage. The remaining gap is now narrowed to the true server-authored hidden unlock matrix and the still-unrecovered official `CItemMakerInfo` profession/session packet contract itself: the simulator can honor an inbox-fed authoritative session when present, but it still does not know the real server opcode cadence, the complete payload shapes that grant or revoke those profession-session lists, or any deeper packet-owned post-request inventory/result reconciliation that surrounds the official owner in the client | Crafting parity now covers the dedicated owner, client-shaped job/category filtering, corrected client aggregate labels, the missing disassembly selector branch, WZ-first hidden-row handling, row-keyed progression persistence, packet-aware hidden unlocks, packet-aware authoritative session filtering for disassembly and hidden-list membership, and packet-driven craft completions feeding the same local mastery/discovery ledger as ordinary crafts without letting packet disassembly mutate that progression store. The remaining divergence is concentrated in the unrecovered official profession-session packet contract and its true server-authored grant/revoke cadence rather than missing selector flow, gauge/craft controls, aggregate category wording, result rendering, hidden-page persistence, or packet-result/session decode and application inside the existing owner seams | inventory/runtime integration, crafting UI layer (`CUIItemMaker::Draw`, `CUIItemMaker::OnCreate`, `CUIItemMaker::SetComboBox_Category`, `CUIItemMaker::SetComboBox_Item`, `CUIItemMaker::OnButtonClicked`, `CUserLocal::OnMakerResult`) |
| Partial | Item upgrade / enhancement window parity | The simulator now registers a dedicated `ItemUpgrade` owner instead of a placeholder, loads Gold Hammer or Vicious Hammer style client art from `UIWindow2.img` or `UIWindow.img`, binds the window to the live `CharacterBuild` plus inventory runtime, seeds starter enhancement scroll stock so the loop is immediately exercisable, resolves eligible items from WZ-backed `info/tuc` upgrade-slot data instead of a pure slot-type guess, consumes enhancement scrolls from the inventory surface, tracks per-item remaining upgrade slots and success or fail counts, applies lightweight stat growth back into `CharacterBuild`, routes right-click activation from supported enhancement consumables in the `USE` and `CASH` inventory tabs plus eligible equips in both legacy and Big Bang equipment windows into the dedicated owner while preselecting the clicked consumable or equipped target, now lets NPC interaction overlays whose loaded `String/Npc.img` function text advertises item-upgrade, enhancement, hammer, potential, or cube families open the dedicated owner, broadens the local enhancement loop beyond the baseline normal or advanced scroll pair by seeding and resolving the client `2049300/1/3/4/5/6/7/8/9` enhancement-scroll family including 2-star through 5-star result variants with multi-star success gains and destroy-on-fail handling, now adds local potential-scroll plus Miracle Cube result handling through `2049400/1/6`, `2049407/8`, `2049700/1/2/3`, and the broader WZ-backed cash family `5062000/1/2/3/4/5/2100`, seeds and resolves `2049500/1`, `5534000`, and `5610000/1` so carved seals, Urete's Time Lab, and Vega's Spell consumables are immediately exercisable from inventory, now also seeds a Maple 8th Anniversary Crimson equip so the WZ-gated `5062100` Maple Miracle Cube flow can be exercised locally, reads `Item/Cash/0506.img/05062100/req` to enforce the official Maple Miracle Cube equip whitelist instead of letting that cash item roll on arbitrary gear, preserves existing revealed potential lines when a carved seal adds an extra line instead of rerolling the whole set, now mirrors the WZ-described Epic Potential Scroll 80% failure behavior by treating destruction as a 20% on-fail roll instead of unconditional breakage, keeps Golden and Revolutionary Miracle Cube fragment grants attached to the specific cash items that advertise them, now also grants the matching Enlightening Miracle Cube shard locally, enforces the WZ-described Golden Miracle Cube Rare-through-Unique limit, now routes `5610000/1` into a dedicated `VegaSpell` owner loaded from `UIWindow.img/VegaSpell` instead of keeping Vega inside the shared item-upgrade surface, reuses the client `backgrnd10` / `backgrnd60`, count glyphs, success/fail panes, and `EffectTwinkling` / `EffectSpelling` / `EffectArrow` / `EffectSuccess` / `EffectFail` families for a separate local cast-and-result presentation, and now also resolves WZ cash-item `info/path` owner art into dedicated `MiracleCube`, `HyperMiracleCube`, and `MiracleCube_8th` frame themes so Maple, Master/Amazing-family, and base Miracle Cube flows no longer stay on the Gold Hammer enhancement shell. This run also activates the WZ-authored Clean Slate / Reverse Scroll family (`2049000+`) through `info/recover`, `info/success`, and `info/cursed`, adds Innocence Scroll handling for `2049600/1/4` so locally applied upgrade bonuses and consumed slots reset while revealed potential is preserved, and brings the Golden Hammer and Vicious' Hammer families (`2470000/1/2`, `5570000`) onto the same owner with WZ-backed success rates, including `String/Consume.img` fallback parsing for the authored 50%-chance Golden Hammer variants, plus the client-described usage rules that Golden Hammer and Vicious' Hammer are mutually exclusive, Vicious' Hammer is capped at two uses, and the Horntail Necklace family is excluded from Vicious' Hammer. It now also reads the authored `String/Cash.img/5610000/1` Vega descriptions instead of hardwiring the modifier pair to the enhancement-scroll family, seeds the matching `String/Consume.img` Vega-enabled `2040759` and `2040760` scrolls into starter stock, parses their WZ-authored `10%` versus `60%` base-rate text into local enhancement consumables, derives Vega's `30%` and `90%` outcomes from the same cash-item descriptions, surfaces those same WZ-authored modifier names and fallback rate captions inside the dedicated `VegaSpell` owner instead of keeping `5610000/1` labels hardcoded in the window itself, filters the dedicated `VegaSpell` owner down to equipment that actually has a compatible Vega-marked scroll in inventory, blocks mismatched target equipment by parsing the scroll name's authored target family instead of letting Vega browse or cast against arbitrary upgradeable gear, and now also parses the authored `String/Consume.img` stat text on those Vega-marked scrolls so local success applies the same ATT/DEF/speed/jump deltas the description advertises instead of falling back to a slot-only enhancement guess; those authored deltas also unwind correctly when Innocence or destruction removes the local upgrade state. The dedicated item-upgrade owner now also consumes the WZ-authored `MiracleCube`, `HyperMiracleCube`, and `MiracleCube_8th` `Effect` and `BtOk` / `BtCancel` families so cube themes no longer only swap frame art: local cube attempts now lock the active cube theme through a staged cast/result presentation before returning to the reusable owner flow. This pass also widens the non-Vega enhancement family with WZ-authored tablet support: string-backed enhancement resolution now accepts `String/Consume.img` names such as `2047204` `Tablet for Armor for Diligence` and `2047304` `Tablet for Accessory for Diligence`, the target-family parser now honors the authored `Armor`, `Accessory`, and generic `Weapon` buckets instead of treating those tablets as universally compatible, the stat parser now consumes `info/incCraft` plus `Diligence +n` text into local Hands/Diligence bonuses, and starter stock now seeds the armor and accessory diligence tablets so that WZ-backed path is immediately exercisable. This run also extends the non-Vega description fallback beyond tablets by parsing grouped `String/Consume.img` stat text such as `All stats +n`, `Weapon/Magic ATT+ n`, `Weapon/Magic DEF+ n`, `HP/MP +n`, `Accuracy/Aviodability+n`, and `Speed/Jump +n` back into local enhancement deltas when an authored scroll family does not expose every stat through `info/inc*`, recognizes authored `Face Eqp.` / `Eye Eqp.` aliases plus newer `Shoulder`, `Pocket`, `Badge`, and `Medal` slot text in the target-family parser, and now also accepts additional client-authored stat wording such as `STR/INT/DEX/LUK +n`, `PDD/MDD+n`, `Attack Power +n`, `Magical Power +n`, and cursed-scroll destroy text including 50%-chance and complete-destruction description variants when WZ `info/*` fields omit those payloads. Focused regression coverage now pins those grouped-stat, legacy-target-label, and destroy-text parsing seams in `UnitTest_MapSimulator/ItemUpgradeParityTests.cs`. | Progression-sensitive item workflows now have a visible dedicated enhancement surface, live inventory consumption, client-shaped inventory, equip, and NPC launch shortcuts, a broader enhancement-scroll and potential-scroll result matrix, WZ-backed Maple-cube whitelist enforcement for the `5062100` family, additional local shard-result coverage, preserved seal behavior, WZ-backed MiracleCube-family frame swaps keyed from cash-item owner paths, WZ-backed MiracleCube-family effect/button choreography for staged local cube casts, a separate WZ-backed Vega cast/result owner instead of treating Vega as only another modifier inside `ItemUpgrade`, WZ-backed Vega 10%-vs-60% modifier routing that now resolves against authored Vega-enabled scroll descriptions plus live preview rates and owner-local modifier captions, and now also WZ-driven Clean Slate, Innocence Scroll, Golden Hammer, and Vicious' Hammer support with locally tracked recovered-slot and hammer-use state plus authored 50%-chance Golden Hammer fallback parsing when `info/success` is absent. Vega-marked scroll successes now also apply and later remove the authored ATT/DEF/speed/jump bonuses described in `String/Consume.img` instead of reusing the generic slot-based enhancement bonus, non-Vega tablet families now also respect authored Armor/Accessory/Weapon target buckets while applying WZ-backed `incCraft` / Diligence bonuses instead of falling back to slot-only guesses or universal compatibility, and the wider non-Vega description fallback now also covers grouped All-Stat, four-stat, Weapon/Magic, HP/MP, Accuracy/Avoidability, PDD/MDD, Attack Power, Magical Power, and cursed-scroll destroy phrases while honoring client-authored `Face Eqp.` / `Eye Eqp.` plus newer Shoulder/Pocket/Badge/Medal slot labels instead of leaving those families partially ungated or under-described when `req`, `success`, or `cursed` fields are absent. The remaining gap is narrowed to unsupported server-authored item-result families outside this broader local scroll/cube/recovery/reset/hammer plus Vega-and-tablet set, remaining non-Vega enhancement consumables whose authored results still live outside the current WZ `info/inc*` plus expanded `String/Consume.img` stat-text and destroy-text parser, packet-authored Vega or item-result session ownership and cooldown constraints, and the exact server-fed accept/reject or post-result reconciliation that still surrounds official enhancement and cube workflows | inventory/runtime integration, enhancement UI layer (`CUIItemUpgrade::Draw`, `CUIItemUpgrade::OnButtonClicked`), `UnitTest_MapSimulator/ItemUpgradeParityTests.cs` |
| Partial | Equipment window parity | Legacy and Big Bang equip windows now share `EquipSlotStateResolver`-driven disable handling for overall-vs-pants conflicts, beginner subjob and two-handed shield gating, Monster Riding slot gating, expired-item overlays, and zero-durability overlays, stay bound to the live `CharacterBuild` used by the avatar preview, and support simulator-side drag or drop between compatible character slots with held-item cursor feedback plus compare-style hover messaging while an equipped item is being moved; the Big Bang owner now also accepts inventory-to-character equips on the live character tab, returns displaced equips back to inventory with their item icons, stops mutating cached loader templates when slot moves retarget cloned `CharacterPart` instances, maps the heart slot onto the live android-heart family, and covers the broader client-facing slot families already exposed by the window art for pendant1 vs pendant2, pocket, badge, shoulder, android, android heart, medal, and belt-backed character panes. `CharacterBuild` now preserves concealed base equips under visible cash equips through a dedicated hidden-equipment layer, `CharacterLoader` no longer drops `LoginAvatarLook` hidden entries when a visible cash item already owns that slot, `LoginAvatarLookCodec` round-trips that visible-versus-hidden split, build stat totals plus weapon-sensitive shield gating can still see the concealed stat-bearing item, and both equip owners now surface the hidden base item in tooltip and drag messaging while restoring it when the visible cash item is moved off the slot. `CharacterBuild.Clone` now also deep-clones both visible and hidden equipment layers, so preview, remote, or other cloned-build consumers cannot retarget the live/shared `CharacterPart` instances when a local slot move or cash-item removal mutates `part.Slot` or restores the concealed base layer. The Big Bang owner still loads the full authored dynamic character-pane chrome set from `UIWindow2.img/Equip/character` through one WZ-backed seam, and WZ was refreshed again here against `ui/UIWindow2.img/Equip/character` to confirm that this client-facing branch still resolves to `cashPendant` plus `charmPocket`; the same live build-state seam also disables `Pendant2` until pendant-slot expansion is active and disables `Pocket` until the charm/unlock rule is satisfied instead of only toggling the authored chrome while still allowing local equips into those slots. Both local slot-to-slot moves plus inventory-to-character equips still route through `CharacterBuild.PlaceEquipment` so concealed base items stay hidden under cash equips and only the correct displaced base layer returns to inventory, and the Big Bang character tab now rejects invalid local equip attempts with explicit blocked messaging for disabled slots, wrong slot families, item-load failures, unmet level or stat or fame or job requirements, invalid swap targets that cannot return to the source slot, and battlefield-blocked items. The owner-mediated `EquipmentChangeRequest` seam also still covers completion polling consistently across both equip owners: `UIWindowManager` no longer hard-wires delayed resolution to `EquipUIBigBang`, and instead drains pending equipment results through a shared equip-window seam so the legacy owner can finish delayed character-to-character and character-to-inventory changes under the same session-aware request lifecycle instead of leaving classic-window requests stuck pending. This run also keeps the client `CUIItem::OnMouseButton` and `CUIEquip::OnMouseButton` 500 ms exclusive-request gate in the simulator's drag-start path through one shared simulator-owned stamp, so inventory slots plus both equip owners now silently refuse drag begin while an equip request is pending or still inside the client-shaped cooldown window instead of letting the drag start and only rejecting on drop. The focused `UnitTest_MapSimulator/EquipmentWindowParityTests.cs` regression file is now actually on disk for this row's request, drag-gate, hidden-equip, and shared completion-polling seam, covering the client-shaped 500 ms drag-start boundary, hidden-base restore under visible cash equips, concealed-base displacement when a new base item equips under cash, concealed two-handed weapon shield gating through the hidden layer, deep-clone isolation for visible and hidden equipment layers, and the shared `UIWindowManager` pending-equip drain path used by both equip owners. The remaining gap is narrower again: true packet-authored async accept or reject ordering, opcode-specific session ownership rather than the simulator-owned owner-session stamp, real server-fed post-apply state reconciliation, and the exact packet/result cadence around those stamped requests still are not mirrored, so this remains a simulator-owned authoritative shim rather than the real client packet workflow | Equipment-heavy testing now has visible disable and break-state feedback on the actual character equipment surface, a client-shaped held-item interaction path, live inventory-to-equip routing on the Big Bang character page, broader slot-family coverage for the non-rendered accessory families already present in the client window art, the full shipped authored pendant/pocket chrome family loaded through the same WZ-backed seam that drives its placement, real local lockout for still-unavailable `Pendant2` and `Pocket` slots instead of chrome-only hints, a client-shaped visible-versus-hidden equip data path instead of dropping concealed base gear from avatar-look and equipment-window resolution or returning the wrong concealed base item during local swaps, cloned-build isolation for slot-retarget and cash-layer restore paths, a queued request-id or tick seam that delays local equip completion until the next simulator-owned acceptance pass, a locked pending-source inventory slot while that local equip request is outstanding without self-rejecting its own delayed apply, a client-shaped drag-start no-op while the exclusive equip request window is still active instead of a late drop-time rejection, and focused on-disk regression coverage for the simulator-owned request, hidden-equipment, hidden-weapon shield-gate, and shared pending-window completion seam. The remaining gap is narrowed to true packet-authored session ownership, server-authored async ordering, server-fed post-apply reconciliation, and the last client-exact packet/result sequencing details rather than missing authored chrome, a missing mouse-down exclusive-request gate, immediate local acceptance sequencing, the cloned-build mutation hazard, hidden-base displacement or restore coverage, hidden-layer-aware shield gating, shared pending result draining, or stale local delayed results being keyed only by request id and item id | `EquipUI.cs`, `EquipUIBigBang.cs`, `InventoryUI.cs`, `CharacterBuild`, `CharacterLoader`, `CharacterAssembler`, `EquipSlotStateResolver.cs`, `UIWindowLoader.EquipmentAndStats.cs`, `UIWindowManager.cs`, `MapSimulator.EquipmentWindowParity.cs`, `UnitTest_MapSimulator/EquipmentWindowParityTests.cs` (`CUIEquip::Draw`, `UIWindow2.img/Equip/character`, `CUIEquip::OnMouseButton`, `CUIItem::OnMouseButton`) |
| Partial | Mechanic equipment tab parity | The Big Bang equip window still keeps a genuinely separate utility-window concern that should not stay merged into the broader companion backlog: the mechanic pane now loads the client `Character/Mechanic` asset family, highlights the active tab button, seeds a dedicated mechanic equipment runtime so the page can render live `ENGINE`, `FRAME`, `TRANS`, `ARM`, and `LEG` icons with slot-specific tooltips, accepts drag/drop from the live equip inventory with slot-family validation, reuses the shared delayed submit/result seam from the main equip owner for equip-in and drag-back-out, mirrors the client `CUIEquip::ToggleMechanicEquip` gate by hiding the tab until the build is on a Mechanic job path and at least level `50`, snapshots plus resolves a dedicated mechanic runtime state token through that delayed owner path so stale mechanic-tab completions or interleaved pane changes reject instead of silently applying against the wrong machine-part state, and now also gives the pane its first packet-owned external mutation seam through the local utility inbox: opcode/alias `1023` (`mechanicequip`) can apply a packet-authored five-slot snapshot, a single-slot replace-or-clear, a full clear, or a reset-to-client-defaults update directly onto the dedicated mechanic runtime, reusing the same controller plus WZ-backed `Character/Mechanic` validation so externally authored pane changes invalidate stale delayed completions instead of waiting on the purely local delayed resolver path. The overlapping pet, dragon, and android companion equipment surfaces now live in backlog 10, which keeps this row focused on the one utility-window tab family that is not otherwise tracked there. The remaining gap is narrower: the simulator still approximates the client's true packet-authored server authority for mechanic equip accept/reject and result payloads, and the new `1023` owner is still a simulator-side local-utility packet seam rather than the real official session packet flow or the exact server-authored mechanic result payload format. | WZ still shows that this page is its own `UIWindow2.img/Equip/mechanic` surface backed by the separate `Character/Mechanic` asset family, while direct client lookup confirms `CUIEquip::ToggleMechanicEquip` (`0x7a7af0`) spawns and destroys a dedicated `CUIMechanicEquip` owner at level `50+` instead of folding the page into ordinary character equipment state. The simulator now mirrors that split more closely by carrying mechanic-only state through request submission and completion and by exposing an external packet-owned mutation seam that can rewrite machine-part state outside the equip window itself, but true server packet ownership and result opcodes are still approximated locally rather than mirrored exactly. | `EquipUIBigBang.cs`, `MapSimulator.EquipmentWindowParity.cs`, `EquipmentChangeRequest.cs`, `CharacterLoader.cs`, `UIWindowLoader.cs`, `CompanionEquipmentController.cs`, `LocalUtilityPacketInboxManager.cs`, `MapSimulator.PacketOwnedUtilityParity.cs`, inventory/window manager integration (`CUIEquip::Draw`, `CUIEquip::ToggleMechanicEquip`, `CUIItem::OnMouseButton`) |
| Partial | Ability and stat window parity | The simulator already draws both legacy and Big Bang stat windows, supports AP assignment, detail-panel toggles, build-backed guild/fame/EXP text, hands/critical detail values, per-stat AP cap enablement, and Big Bang open-vs-close detail button visibility; the stat rows now also resolve equipped stat bonuses through `CharacterBuild`, the detail pane now derives attack/defense, accuracy/avoidability, hands, critical, speed, and jump from live build totals instead of static placeholder fields, `SkillManager` now seeds non-hidden passive books alongside active skills for the current job path, and `CharacterBuild` now folds passive plus live buff attack/defense, magic attack/defense, accuracy, avoidability, critical-rate, speed, jump, and skill-driven max HP/MP bonuses into those totals so the stat windows react to learned skills rather than gear alone, while Big Bang HP/MP AP clicks still use class-aware gain ranges with a 30,000 cap instead of a flat `+20`; the displayed physical and magical damage rows now also use a client-shaped min/max average where mastery scales the primary term instead of naively scaling the full max value, dagger formulas now split thief-vs-non-thief primary stats instead of treating every dagger as a thief weapon, thief-family dagger/katara/cane/claw damage now also restores the live `STR + DEX` secondary term instead of dropping the thief weapon STR contribution from the displayed range, extended Cygnus/hero/Resistance plus later post-Big-Bang class lines now map back onto their base warrior or mage or archer or thief or pirate stat archetypes for accuracy, avoidability, defense, Big Bang HP/MP AP growth, and baseline stat-window auto-assign so Aran/Evan/Mercedes/Phantom/Demon roots, Luminous (`2004` / `27xx`), Mihile (`5000` / `51xx`), Kaiser (`6000` / `61xx`), and Angelic Buster (`6001` / `65xx`) no longer fall back to the generic beginner spread, and the auto-assign path itself now mirrors the client `GetIdealStatUp` target-stat formulas for the supported legacy families instead of using the earlier generic heuristics: beginner and Evan-beginner builds now follow the client warrior-like `DEX = level`, `STR = 4*level + 12` target, warrior lines now hold `DEX` at the recovered v95 client `min(2*level, level + 30)` threshold while brawler-style lines stay on `min(2*level, level + 20)`, magicians now hold `LUK = level + 3`, bow users now distinguish bow `STR = level + 5` from crossbow or Wild Hunter style `STR = level`, thieves now hold `DEX = min(2*level, level + 40)`, gunslinger plus Mechanic families now use the client gun-style `STR = level`, `DEX = 4*level + 12` target, and beginner-pirate `500` auto-assign now chooses between the client infighter-vs-gunslinger split by live equipped weapon family with a stat-bias fallback when the window cannot ask the original client question. `CharacterBuild` now also recognizes cane, dual bowgun, and cannon weapon families in the displayed damage surface, and `SkillLoader` plus `SkillManager` now use `String/Skill.img` mastery names together with broader passive alias-field parsing (`ar`, `accX`, `padX`, `madX`, `pddX`, `mddX`, `er`, `evaX`) plus enhanced-stat aliases (`epad`, `emad`, `epdd`, `emdd`, `emhp`, `emmp`) so newer mastery, expert, blessing, shield-style, and avoidability or accuracy books can feed the stat windows instead of being dropped when WZ does not use the older base property names; `SkillManager` now also falls back to `String/Skill.img` description text for generic and specialized mastery books whose weapon family is not encoded in the display name alone, so WZ-authored books such as Fighter/Page/Spearman `Weapon Mastery`, Aran `High Mastery`, Battle Mage `Barricade Mastery`, Mercedes `23100005 Dual Bowguns Mastery`, Phantom `24100004 Cane Mastery`, Cannoneer `5300005 Cannon Mastery`, and Gunslinger `5200000 Gun Mastery` once again drive the displayed mastery and damage floor for the correct equipped weapon families even when the usable family token only survives in generic `weapon mastery ... of <family>` description text instead of the title. The mastery resolver now also normalizes punctuation and broader family wording from `String/Skill.img`, so description-authored variants such as `Cross Bow`, `Throwing Stars`, `Dual Bowgun(s)`, `Hand Cannon`, and `Pistol` text can still bind to the correct weapon family instead of depending on one exact title token; the mastery resolver now also respects narrower WZ-authored family text instead of broadening those books past their stated scope, so one-handed-only blunt/axe books such as Battle Mage `Barricade Mastery` no longer leak onto two-handed axes or blunts, and explicit staff-only mastery text no longer resolve against wands while generic `Spell Mastery` books still cover both magic-weapon families; the same mastery path now also respects WZ-authored conditionals such as Paladin `1220010 Advanced Charge` only while a live charge buff is active, covers stringless fallback cases such as `skill/5110.img/skill/51100001/common/mastery` and `skill/570.img/skill/5700000/common/mastery` by mapping those books back onto their observed knuckle and gun families instead of dropping them when `String/Skill.img` has no matching entry, and now also treats active mastery-bearing runtime owners such as Dark Knight `skill/132.img/skill/1321007/common/mastery = 2*x` `Beholder` as live mastery sources only while the summon is actually present instead of ignoring non-passive WZ mastery entirely; `SkillLoader` now also evaluates WZ-authored max-HP/max-MP rate fields such as `skill/100.img/skill/1000006/common/mhpR = 2*x`, `skill/200.img/skill/2000006/common/mmpR = 2*x`, `skill/510.img/skill/5100009/common/mhpR = 2*x`, and buff-side `indieMhpR` / `indieMmpR` branches such as `skill/2411.img/skill/24111003/common/indieMhpR` so the stat windows include percentage-based HP/MP books instead of only flat `emhp` / `emmp` bonuses, `CharacterBuild.TotalHands` now also includes equipped `incCraft` bonuses instead of dropping glove or gear hands from the detail panel, WZ-authored Maple Warrior books such as `skill/112.img/skill/1121000/common/x = u(x/2)` together with `String/Skill.img/1121000/h` `All Stats: +#x%` now raise live STR/DEX/INT/LUK through the stat-window totals instead of surfacing only as tray metadata, and Echo of Hero books such as `skill/000.img/skill/0001005/level/1/x = 4` together with `String/Skill.img/0001005/h1` `Weapon attack +4%, Magic attack +4%` now resolve into the existing attack-percent and magic-attack-percent lanes instead of being treated as a generic `x` payload. Mechanic's `Extreme Mech` bonuses now only surface while the live mech mount is active instead of behaving like an unconditional passive, weapon-specific mastery no longer resolves while no weapon is equipped, and non-Mechanic alias-backed buffs such as Onyx or Divine Shield style `e*` stat lines now also contribute to the live totals, but the simulator still does not mirror every exact post-Big-Bang coefficient or the remaining long-tail conditional mastery and weapon-formula branches for the unresolved per-job edge cases beyond the newly covered charge-state, active-summon mastery, stringless-mastery, family-only generic-mastery descriptions, punctuation-normalized family-text, one-handed-only, staff-vs-wand, thief-secondary-stat, the recovered warrior-versus-brawler auto-assign split, the newly covered Maple-Warrior and Echo description-backed `x` stat branches, and the earlier Luminous/Mihile/Kaiser/Angelic-Buster branch mapping | Visible stat windows now expose runtime-backed totals from equipped gear, learned passive skills, active buff bonuses, class-sensitive HP/MP AP growth across both classic and extended job branches, client-shaped classic auto-assign targets for warrior, magician, bow, crossbow or Wild Hunter, thief, brawler, gunslinger, Mechanic, beginner, Evan-beginner, and beginner-pirate weapon-choice cases, including the recovered v95 warrior-versus-brawler DEX threshold split from `GetIdealStatUp`, WZ-backed percentage HP/MP skill boosts, the missing `incCraft` hands contribution, WZ-backed Maple Warrior `x%` basic-stat buffs, WZ-backed Echo of Hero attack/magic-attack percent buffs, a broader client-shaped mastery-aware damage surface for cane, dual-bowgun, cannon, and thief-family secondary-stat users, WZ-backed `ar`/`er` alias support for accuracy and avoidability books, and tighter WZ-text mastery family matching that now also honors generic `weapon mastery ... of dual bowguns/canes/cannons/guns` description wording alongside the earlier punctuation, pluralized-family, charge-aware, active-summon, stringless, one-handed-only, and staff-versus-wand handling. The remaining divergence is narrowed to the exact coefficient matrix, the still-unimplemented long-tail mastery or weapon-formula rules for newer special cases such as Xenon and later unsupported weapon families, the unsupported client question flow for beginner-pirate auto-assign beyond the simulator's weapon-or-stat inference, and any residual client-only conditional branches beyond these rate-backed stat surfaces, alias-backed passive totals, description-backed mastery family mapping, charge-state and summon-state mastery gating, family narrowing, mech-state gating, and now client-shaped classic AP distribution rules including the recovered warrior-versus-brawler split | `AbilityUI.cs`, `AbilityUIBigBang.cs`, `CharacterBuild`, `SkillManager`, `SkillLoader`, `UnitTest_MapSimulator/AbilityStatMasteryParityTests.cs`, stat/runtime layer (`CUIStat::Draw`, `CUIStat::DrawTextA`, `GetIdealStatUp`) |
| Partial | Character info / profile window parity | The simulator now loads a dedicated WZ-backed `UserInfo` window from `UIWindow.img/UserInfo` or `UIWindow2.img/UserInfo/character`, routes the status-bar `BtCharacter` button into that owner, keeps the primary profile page build-backed with name/job/level plus world-rank and job-rank plus guild/alliance fields, and the Big Bang owner now switches into WZ-backed ride, pet, collect, and personality subpages that render simulator-side mount state, active pet data, collection summaries, and optional trait values instead of leaving those buttons decorative. The owner now also loads the page-specific `BtException`, `BtArrayName`, and `BtArrayGet` controls from `UIWindow2.img/UserInfo`, hides primary action buttons outside the character page so the per-page button surface matches the selected subpage more closely, forwards `BtParty` into the existing social-list owner instead of leaving it as a pure message stub, opens the WZ-backed `exception` popup with local register/delete plus meso-filter toggles instead of leaving that client-owned control path dead, and now uses the character-page `BtPopUp` / `BtPopDown` controls in the two modes the row needs: local-owner popup selection for the `item` / `wish` auxiliary lists, and the recovered inspected-target popularity up/down path from `CUIUserInfo::OnButtonClicked` when the owner is showing a remote target. The `productSkill` loader now consumes the full `UIWindow2.img/UserInfo/character/productSkill` family, including the previously omitted `9204` recipe icon, so the character page can light the maker, glove, shoe, toy, and recipe ledger icons from the live simulator-side maker progression snapshot instead of leaving the recipe slot dead. That same progression snapshot still replaces the old equip-count collection summary with maker, glove, shoe, toy, craft-count, and discovered-recipe metadata, while the collection page treats `BtArrayGet` as a local claim action and keeps the collect icon routed into the dedicated book owner. The primary rank rows now consume packet/account-backed previous-rank state through a target-aware `RankDeltaSnapshot` seam, so stored world/job rank movement can follow whichever build the owner is currently showing instead of always falling back to the active local player. The personality page still consumes the fuller WZ-backed `Tooltip` family from `UIWindow2.img/UserInfo/personality/Tooltip`, including the charm `collection/0` body and numeric glyph set, so hover state can render the client art for pocket-slot charm collection messaging instead of falling back to plain text only. This row also keeps the real inspected-target seam inside the same owner: `/remoteuser inspect <characterId|name>` can reopen `CharacterInfo` against a shared-pool remote actor build, the character page draws an inspected-target banner, local-only `item` / `wish` popups are suppressed for that remote context, `BtParty` can now also mirror the client boss-gate more closely by refusing the remote invite path unless the local player is the current party leader, tracked social-roster location/channel metadata now flows into the inspected-target banner when the target already exists in friend/party/guild data instead of always falling back to the current field and channel 1, `BtTrad` can retarget the trading-room shell to the inspected actor instead of always opening an anonymous local preview, and `BtFamily` now drives a family-chart request preview for the inspected name through the existing family-chart runtime rather than always falling back to the local chart. The owner-local draw and gate paths now also consistently read the active inspected build instead of leaking back to the local player: character-page name/job/level/fame/guild/rank rows, ride-page mount/saddle/job text, medal and pocket summaries, personality trait values plus the charm tooltip gate, and the inspected-target `BtPopDown` fame check all now follow the remote build that `/remoteuser inspect` selected. This run closes one more marriage-state gap inside the same seam: the Big Bang loader still consumes the WZ-authored `UIWindow2.img/UserInfo/character/married` badge and the draw path still mirrors the client `CUIUserInfo::Draw` placement, while the badge gate now resolves authored wedding-ring ids first from the WZ string families (`String/Eqp.img/Eqp/Ring/1112315..1112320` and `1112803/1112806/1112807/1112809`) before falling back to a narrow custom-name check, and inspected remote builds can now light that same badge through their equipped ring data even when no marriage overlay packet was observed. The pet-tab button surface still follows the active inspected build's authored remote pet-slot count instead of only the local summoned-pet count so remote pet pages can actually switch across packet-owned slots, and the local collect launch still carries the current field plus simulator channel into the stored `UserInfo` action context so the dedicated book owner subtitle/overview/status text no longer falls back to generic current-field/channel-1 wording after a local open. The remaining gap is now narrower: truly server-authored remote pet, collection, medal, and charm payloads, remote field/channel ownership outside the currently known shared-field or social-roster seams, the exact server accept/reject or result workflows behind party, trade, family, and popularity requests, and a fully authoritative married-state source beyond the simulator's current marriage overlay packets plus build-backed wedding-ring fallback are still approximated rather than mirrored exactly | Character-facing information now has its own launch path, a broader button-driven surface distinct from the stat window, WZ-backed popup navigation and personality-hover art, packet/account-backed rank-delta presentation that now follows the active inspected build, the full WZ-backed maker `productSkill` icon family including the recipe slot, and an inspected-remote branch that reuses the same `CUIUserInfo` owner for target-aware party, trade, family, popularity, and per-character collection snapshot presentation instead of keeping those buttons or subpages local-only. That remote branch now also carries tracked social-roster location/channel metadata into the inspection context when available, rejects the inspected-target party invite path when the local player is not the party leader, keeps the visible character, ride, medal, pocket, personality, and fame-gate state aligned to the currently inspected build instead of silently falling back to the local player, now exposes the client-authored married badge art on the Big Bang character page when the simulator can resolve married state, resolves the married badge through the WZ-backed wedding-ring id family for both local and inspected remote builds when build data is present, and no longer strands remote pet-slot tabs or local collection-owner metadata behind local-only defaults. The remaining gap is concentrated in truly server-authored remote payload/result handling and the last authoritative marriage-state source rather than dead `BtPopUp`/`BtPopDown`, a missing recipe icon, rank deltas that only followed the local player, inspected-target subpage rows that still read local build state, an inspected-target banner that always claimed channel 1/current map, remote pet tabs that only followed local summoned pets, a local collection-book context that dropped field/channel metadata, a local marriage badge that only looked for the raw `"wedding ring"` string, an inspected remote badge path that only trusted overlay packets, or a remote `BtParty` path that ignored the local party-leader gate entirely | character/profile UI layer, player metadata presentation (`CUIUserInfo::Draw`, `CUIUserInfo::OnButtonClicked`) |
| Partial | Book / collection window parity | MapSimulator still keeps `BookCollection` as a dedicated owner on the real `UIWindow.img/UIWindow2.img/Book` shell, keeps the IDA-recovered `BtPrev`, `BtNext`, and `BtClose` positions from `CBookDlg::OnCreate`, keeps the shared `UIWindow(.2).img/UtilDlgEx/dot0` and `dot1` spread markers that the client binds there, and keeps the same two-page paging cadence that `CBookDlg::OnButtonClicked` applies by advancing in two-page increments. This pass keeps that owner seam intact and trims the remaining payload drift again inside the existing snapshot path: the loader still prefers the Book shell and Book button family over the Monster Book frame art for this owner, `MapSimulator.Content` still binds the live window to `BuildActiveCollectionBookSnapshot()`, the open-time payload still flows from the active `UserInfo` action context so inspected targets remain bound through the dedicated spread, and the draw path now carries more of the recovered `CBookDlg::OnCreate` font-table evidence through the same snapshot seam by tagging the twelve styles with the client `IWzFont` object family (`StringPool 0x5AF`), face (`0x1A25`), and odd-slot style variant (`0x5B0`) while resolving those odd styles through a dedicated bold `ClientTextRasterizer` path instead of one flat regular rasterizer. The collection page counters now also sit on the client `CBookDlg::Draw` lane again by centering each `n/n` string inside the recovered `190px` per-page index band at `y=293` rather than the older generic bottom bounds, the collection separators now draw through the same WZ-authored `UIWindow(.2).img/UtilDlgEx/line` asset family instead of a simulator-only pixel rule fallback, and the overview page now uses a dedicated record layout that keeps all seven snapshot rows visible, including the previously dropped Pocket lane, while crafting, trait, equipment, and recipe pages keep the tighter compact field/ledger subtitles, footers, and row-detail strings from the existing snapshot payload seam. The remaining gap is narrower but still present: the simulator still approximates the client's exact native `CT_INFO` row contents, spacing, and true COM-owned `IWzFont` objects, and it still lacks the true server-/packet-authored collection dataset plus the final packet-owned `CWvsContext::CloseBook` ownership chain that would drive the official owner end to end. | This is now a real stand-alone utility surface rather than dead backlog theory: the simulator has the separate paged owner, the exact client dot-marker path, explicit spread-button flow, the proper Book shell and Book button family, an ordered collection-page compositor that tracks the client's text/rule draw sequencing more closely, a live collection-snapshot bridge from the `UserInfo` action context, an active client color-style matrix for the page body, and a closer font/measurement path because the Book styles now carry the recovered `OnCreate` font-table metadata and use regular-vs-bold client rasterizers in the same odd/even cadence as the native owner. The collection spread also now places its left and right page counters on the same per-page draw lane recovered from `CBookDlg::Draw`, reuses the authored `UtilDlgEx/line` separator art rather than a synthetic rule, keeps the compact ledger copy across the overview/crafting/trait/equipment/recipe spreads, restores the overview Pocket payload instead of silently truncating the seventh row, remains genuinely bound to the `UserInfo` inspection context instead of silently snapping back to the local player, and still closes through the dedicated owner-local `CWvsContext::CloseBook` analogue rather than only loose window callbacks. Its remaining divergence is now concentrated in unrecovered native `CT_INFO` payload micro-layout, true `IWzFont` COM ownership, live client-owned collection data, and packet-authored close ownership rather than shell preference, collection-provider wiring, page-counter lane placement, overview-row truncation, remote-target binding, or flat regular-font text rendering. | collection/book UI layer (`BookCollectionWindow.cs`, `ProgressionUtilityWindowModels.cs`, `MapSimulator.Content.cs`, `UIWindowLoader.cs`, `MapSimulator.cs`, `CBookDlg::OnCreate`, `CBookDlg::Draw`, `CBookDlg::OnButtonClicked`, `CWvsContext::CloseBook`) |
| Partial | Quest log window parity | The simulator quest windows now render live runtime-backed lists for available, in-progress, completed, and recommended quests, instantiate the client `Tab/enabled` / `Tab/disabled` category buttons plus `BtMyLevel` / `BtAllLevel` filters from `UIWindow.img` or `UIWindow2.img`, keep selection synchronized with quest state mutations, color rows by accept/complete readiness, expand the list owner to use the dedicated quest-log shell instead of a simulator-only inline detail pane, preserve item-backed requirement/reward hover popups by moving them onto the dedicated detail owner, now load and honor the legacy `UIWindow.img/Quest/BtDetail` list-shell button so pre-Big-Bang quest browsing uses a client-shaped select-then-button cadence instead of forcing the Big Bang-style double-click open path, now also mirror the client `CUIQuestInfo::OnTabChanged` reselection cadence more closely by preferring suitable-level quests on the available tab, recently updated or recently viewed quests on the in-progress tab, and top entries when the prior selection no longer survives a tab or filter rebuild, now expose the client quest-detail location button families through the existing detail owner by routing NPC, mob-demand, and item-demand fallback cases into the world-map search owner instead of treating every specialized case as a pure NPC locate, now also mirror the client demand-item query split more closely by parsing WZ `Check.img/.../item/*/secret` flags, suppressing hidden demand items from local world-map results, preferring visible outstanding demand items when choosing the detail-owner/world-map item target, and seeding a packet-shaped current-field item-query result set when the detail owner asks for demand-item guidance, now also split the Big Bang quest-detail shell back onto the client button families by reserving `UIWindow2.img/Quest/quest_info/BtQuestDeliveryAccept` and `BtQuestDeliveryComplete` for delivery-specific guidance while primary accept/complete return to the shared `Quest/BtOK` art, surfacing item-backed delivery hints through those dedicated buttons and routing their cash-item fallback into the existing quest-delivery or Cash Shop owners with the client hardcoded cash-item ids `5660000` / `5660001` and shop migration SNs `50200218` / `50200219`, now keep a dedicated simulator-side delivery phase on the quest-detail state so the Big Bang delivery buttons, packet-owned delivery snapshots, and packet fallback rows no longer disappear as soon as the first relevant item count is satisfied locally, now preserve the relevant start/end delivery item metadata on the detail state even after counts are met so item-backed accept/complete handoff flows stay visible through the dedicated quest-delivery seam instead of being inferred only from the generic primary action, and now also gate those delivery branches on the presence of the relevant start/end NPC handoff so item-only quests without a client-shaped delivery NPC no longer light the delivery-button family purely from local item requirements. The delivery seam now also mirrors the client `CQuestMan::IsDeliveryAcceptQuest` / `CQuestMan::IsDeliveryCompleteQuest` heuristics more closely by parsing WZ `Check.img` `normalAutoStart`, `fieldEnter`, `equipAllNeed` / `equipSelectNeed`, and `interval` keys, excluding dragon/script/timed/auto-start accept cases from the dedicated delivery-button family, excluding the client hardcoded accept exception quest `10394`, and requiring the client-shaped completion cadence (`interval >= 1440` or end-demand mob/item data plus the relevant NPC handoff) before exposing the dedicated completion button. It also still parses WZ quest `area` ids into readable quest categories so the Big Bang list shell can rebuild each tab through persistent per-tab category option filters driven by the client `BtMax` / `BtMin` controls instead of treating category changes as a no-op, loads the full `UIWindow2.img/Quest/icon_info` family so the legend owner uses the client outer frame, inner sheet backing, and `Sheet/*` compositor, replaces the simulator-only ON/OFF checklist hitboxes with generated category-button slots that rebuild per tab/page, preserve per-tab hidden-category state, sort by the client category index instead of snapshot encounter order, suppress the client-skipped category bucket recovered from `CUIQuestInfo::LoadData` (`area == 51`), now anchors the Big Bang category-toggle family into the bottom quest shell by reusing the live `BtIconInfo` position as the client-shaped seed for `BtMax` / `BtMin`, docks the generated category panel above the footer controls instead of leaving it in the earlier top-left placeholder lane, centers each generated category row to the client `icon_info/Sheet/*` width (`192px`) so the divided count gutter sits closer to the real shell instead of spanning the full inner frame width, now composites those same WZ `icon_info/Sheet/*` row shells behind the generated category entries while keeping the inline `BtMax` / `BtMin` button art in the client `31px` label and `188px` count-gutter lane recovered from `CUIQuestInfo::Draw`, and now also fills that last label-to-count lane with a client-leaning dot-leader micro-compositor instead of leaving the row text and count visually disconnected. The generated category shells now also keep their top/middle/bottom strip selection and top-anchored sheet placement coherent across scrolled pages instead of re-capping each visible slice independently, while the packet-owned delivery seam now accepts an optional packet-shaped delivery-type override so the detail owner and delivery list can prefer packet-authored accept/complete button-family ownership when that hint is present instead of always falling back to the local heuristic. The remaining gap is narrower: the simulator still does not mirror the client's exact generated-category text raster/compositor rules beyond the new leader fill, server-authored item-query map results beyond the simulator's local current-field fallback when no packet-owned results were supplied, the full authoritative `QuestInfo::nDeliveryType` and downstream server-result ownership behind those delivery buttons beyond the new packet-shaped override hook, or the last demand-rect-specific delivery branches behind the most specialized quest-detail buttons | Quest interaction now has a usable standalone log that reflects the same runtime state as NPC quest overlays, uses WZ-backed quest categories instead of a custom tab strip, follows the legacy detail-button cadence where that WZ shell exists, automatically reselects client-shaped quest entries when tabs or category filters rebuild, preserves the client split between generic quest actions and the dedicated Big Bang delivery-button family, keeps delivery-specific buttons and packet-owned delivery rows alive on a dedicated quest-detail delivery phase instead of dropping them once the local item-count heuristic changes, now suppresses delivery-button activation for quests that lack the corresponding NPC handoff the client expects, now also suppresses the delivery-button family for client-shaped auto-start, dragon, script-linked, equip-on-start, field-enter, timed, and the client hardcoded `10394` accept-exception cases while only surfacing delivery completion when the WZ demand cadence matches the client heuristic, now orders category toggles by the client category index while omitting the client-suppressed area-51 bucket, keeps the detail-owner item target aligned with the visible demand-item query path before falling back to hidden-item or NPC guidance, and now places the Big Bang category controls and generated rows materially closer to the client bottom-shell geometry instead of the earlier placeholder top-left layout. The generated category rows now also use the WZ `icon_info/Sheet/*` shells with scroll-aware cap selection plus a dot-leader text/count lane instead of sitting on a bare simulator fill with disconnected labels, and the packet-owned delivery seam can now override the visible accept/complete delivery family when a packet or local utility test supplies that hint, which narrows the remaining divergence further to exact client text raster polish, true server-authored demand-item query resolution when packets do not already supply it, the full authoritative delivery-type/result lifecycle, and the last specialized demand-rect delivery branches rather than tab reselection, client-skipped category suppression, secret-item filtering, the Big Bang category-shell docking math, or every locate button collapsing to the same NPC-only path | `QuestUI.cs`, `QuestUIBigBang.cs`, `QuestDetailWindow.cs`, `QuestRuntimeManager.cs`, `UIWindowLoader.SkillsAndQuests.cs`, packet/local-utility delivery seam, NPC/UI bridge (`CUIQuestInfo::Draw`, `CQuestMan::IsDeliveryAcceptQuest`, `CQuestMan::IsDeliveryCompleteQuest`, `CUIQuestInfo::OnButtonClicked`, `CUIQuestInfo::OnTabChanged`, `CUIQuestInfoDetail::OnButtonClicked`) |
| Partial | Quest detail window parity | The simulator now exposes a dedicated `QuestDetail` owner backed by the quest runtime, reuses the client `Quest/quest_info` frame family when present, opens from quest-list selection, renders separate summary/requirements/rewards sections with WZ-backed summary headers plus gauge art, now also draws the Big Bang `UIWindow2.img/Quest/quest_info/summary` lower-panel canvas and the matching `tip` canvas when the delivery inset is active instead of leaving those client surfaces unloaded, shows color-coded requirement rows, now also switches incomplete requirement rows onto the client `prob` demand-strip family while completed/reward rows keep the `select` strip family when those canvases are present, draws reward item icon rows through the live item-icon loader, switches accept/complete/track/give-up controls across the client quest-detail button families when that art is present, supports previous/next quest navigation within the active tab, wires standalone accept or give-up actions plus completion guidance distinct from the list shell, keeps the runtime-backed tertiary locate-NPC action visible through the quest-detail owner, reuses legacy `BtGotoNpc` and `BtMarkNpc` families when present while falling back to the Big Bang `BtNPC` family, now adds a dedicated mob-locate action for incomplete mob demands and routes it into the world-map search owner so quest-detail delivery guidance can use packet-authored quest-guide maps when present instead of staying pinned to the current field, lays out four-button delivery or completion cases without collapsing them back into the older two-button shell, draws quest notice banners plus animated notice icons from the client quest notice families when that art is present, pushes demand/reward count text into separate runtime fields so the detail owner can draw native-width `select` / `prob` strips with split body-vs-count composition, right-aligned counters, live item icons, and tighter client-shaped `253px` content bounds, now matches the client `GetScrLogLen` and delivery-inset clip math for the detail and summary panes, now also carries quest-start time into the runtime so started time-limit quests can render an authored inset payload with the client `Quest/TimeQuest/TimeBar` gauge plus `AlarmClock/{timerUI}` indicator styles instead of only appending a plain-text time-limit hint, and feeds the world-map owner with explicit quest-target overlay summaries outside the older search-result-only seam while reusing `UIWindow2.img/QuestGuide/QuestMark` so overlaid maps light up with the client quest-guide marker animation in both the world-map list and search panes. The world-map owner now also wires the authored `QuestGuide/Button/Location` and `WorldMapQuestToggle` controls into the existing seam with client-leaning pressed-state toggles, the detail owner now reflects quest-alarm registration state by pinning already tracked quests onto a selected `Track` button instead of leaving that client button family visually indistinguishable from an untracked quest, and the quest-detail delivery path now also consumes a locally owned delivery cash item when present before routing into the dedicated delivery owner while missing delivery cash items resolve through `Etc/Commodity.img` and the existing packet-shaped `GoToCommoditySN` Cash Shop seam instead of dropping to a generic shop-open stub. The world-map list-side quest-guide marker now also uses the WZ `QuestMark` origin data when placing the animated marker inside each row instead of treating the frame like a fixed unanchored stamp, the detail owner now prefers the authored `Quest/quest_info/BtClose` family instead of the generic utility close chrome when that client button art is present, the quest-detail header/body header lane now follows `CUIQuestInfoDetail::Draw` more closely by using the recovered low-level header note plus the level-limit line at the client `23,75` slot instead of a simulator-authored start/end NPC summary, the packet-owned mate-name quest now also uses the client-special `y=45` note position, grouped surface overlays now keep multiple quest-guide targets visible on the same map spot by fanning non-primary markers around the WZ-authored anchor while leaving the active detail target on the center client marker seam, the detail header note now follows the recovered `CWvsContext::IsWorthlessQuest` rule instead of a simple max-level check, and the detail header now also folds authored quest-check job and subjob restrictions into that `23,75` eligibility lane so the stored client `sLevelLimit` seam is no longer reduced to level-only min/max text when job gating is present. The runtime now also prefers any stored quest-side eligibility string if the loaded quest data exposes one and otherwise falls back to the client StringPool `No limit` text instead of leaving unrestricted quests blank. The remaining gap is narrower: the simulator still does not reproduce the client's full CT-array compositor from `CUIQuestInfoDetail::Draw`, the exact official in-map marker placement and per-overlay art selection beyond the current list/search plus grouped-surface overlay seam, the exact authored `sLevelLimit` payload for quests whose client-localized text is generated outside the loaded quest WZ fields and carries custom wording or extra restriction shaping beyond the simulator's current stored-string-or-level/job fallback, or the full server-driven delivery-button result path behind the rarest quest-detail buttons such as the true `SendConsumeCashItemUseRequest` packet ownership, authoritative commodity migration/result sequencing, and server-authored delivery outcomes | The simulator now exercises the client's split between quest browsing and per-quest inspection with client-shaped notice chrome, the actual Big Bang summary-panel and delivery-tip canvases, a real authored time-limit inset payload instead of plain hint text, WZ-backed incomplete-vs-complete demand row art, client-shaped scroll clips, separate NPC and mob world-map guidance, explicit world-map quest overlays plus animated quest-guide markers, authored location/toggle controls on the world-map owner, track-button state that now follows local quest-alarm registration, WZ-origin-backed row-marker placement, grouped in-map overlay presentation, broader delivery/completion button layouts, a closer delivery cash-item fallback that now either consumes the local cash item or packet-routes the matching Cash Shop commodity from `Commodity.img`, and a header lane that is materially closer to the recovered client draw path because it now uses the quest-info close-button art, the low-level header note seam, the level-limit text slot, the client worthlessness threshold, the StringPool-backed min/max level phrasing, the client `No limit` fallback text, any stored quest-side eligibility string when present, WZ-backed quest job or branch restrictions synthesized into the stored eligibility lane when no authored override is available, and the mate-name special-case offset. The remaining gap is concentrated in the final CT-layout fidelity, the last exact in-map quest-marker art and placement rules, the exact authored `sLevelLimit` payload text when the client shapes a custom localized string outside the loaded quest-data fields, and the true server-owned delivery/cash-item request lifecycle rather than in missing owners, missing locate-button paths, stale track-button state, fixed-origin row markers, missing timer inset behavior, unloaded `quest_info`/`QuestGuide` chrome, a generic close button, a generic quest-detail Cash Shop fallback stub, or the earlier simulator-authored level-limit phrasing, blank unrestricted eligibility lane, and level-only `sLevelLimit` approximation. | `QuestUI.cs`, `QuestUIBigBang.cs`, `QuestDetailWindow.cs`, `QuestAlarmWindow.cs`, `QuestRuntimeManager.cs`, `WorldMapUI.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `AdminShopDialogUI.cs`, `MapSimulator.cs`, quest-detail interaction layer (`CUIQuestInfoDetail::Draw`, `CUIQuestInfoDetail::OnButtonClicked`, `CWvsContext::IsWorthlessQuest`) |
| Partial | Quest alarm / notifier window parity | The simulator now replaces the `QuestAlarm` placeholder with a dedicated WZ-backed tracker built from `UIWindow.img/UIWindow2.img/QuestAlarm`, supports minimize/maximize toggles, title counts, live in-progress quest rows with completion-state coloring, row selection, per-row dismiss buttons shaped from the client `BtDelete` art, double-click routing into the quest detail flow, and now reuses shared `UIWindow2.img/Quest/quest_info` chrome for selection, incomplete `prob` strips, and progress-gauge rendering while surfacing negative quest `act/money` costs as live meso progress rows and rewarding or consuming mesos against the bound inventory runtime during quest start or completion. The quest-alarm runtime also now tracks recent quest-state, mob-kill, and tracked-item updates so the client `BtQ/ani` alert frames and sky-blue recent-update emphasis can light up before `BtQ` falls back to the normal quest-log toggle path; when that alert route is active, the owner now prefers the first recent-update quest instead of blindly reopening whichever row happened to stay selected, opening a quest detail still acknowledges that quest's recent-update alert so viewed quests stop holding the alert route open for the rest of the timer window, and the loader now preserves the authored `UIWindow2.img/QuestAlarm/BtQ/ani` frame origins when composing that alert overlay instead of stamping the animation at an unanchored local button origin. The owner now follows the client-recovered five delete-slot layout more closely by exposing five visible quest titles, keeping each visible `BtDelete` slot live instead of dimming non-selected rows behind simulator-only disabled chrome, resizing the maximized frame to the active selected-demand stack, and adding a local delete-all header action with `Delete`/`Shift+Delete` shortcuts on top of the existing per-row dismiss path. The `BtAuto` toggle now drives a local auto-register mode instead of acting as selection-only chrome: auto-registered active quests can be locally deleted into a persisted hidden set, manual quest-detail tracking pins quests into a persisted tracked set even when auto-register is off, those manual registrations now preserve their local pin order instead of being re-sorted by the snapshot title ordering, the effective registered list is still clamped to the client-backed five-quest limit from `string/ToolTipHelp.img/Game/Button/QuestAlarm`, the loader now actually resolves that authored tooltip `Desc` into the owner so sixth manual registrations surface the WZ help-text cap sentence instead of a simulator-only warning, and `Delete all` now clears the full local registration set plus the persisted hidden-auto tombstones under that cap instead of only the visible subset. The owner restores per-character auto mode plus minimized state from AppData across simulator restarts. The selected quest row now composes more like the recovered client `CUIQuestAlarm::Draw` path by keeping non-selected quests collapsed to 18px title bands, expanding only the selected quest into a 150px demand rect, drawing requirement counters through client-like `CUIQuestAlarm::GetProgressFont` and `GetProgressFontMeso` threshold colors instead of one flat incomplete/complete tint, drawing all requirement or meso rows with right-aligned counters and `select` versus `prob` strip families instead of rendering every visible quest as a full multi-line simulator card, and now vertically centering the selected-band strips, item icons, and count text inside each 18px demand lane instead of leaving those elements on older simulator-fixed top offsets. The owner now also follows `CUIQuestAlarm::OnButtonClicked` more closely when the local tracker is empty or cleared: `BtMax` refuses to expand and surfaces the dedicated client-owned empty notice route (`StringPool 0x18EC`) when there are no registered quests, per-row delete feedback is now routed through the same recovered client notice seam (`StringPool 0x106F`) instead of a simulator-only sentence, title text now flows through the recovered client title-format seam (`StringPool 0xE4C`) before falling back locally, those recovered quest-alarm StringPool payloads are now pinned explicitly in the shared string-pool override table so the owner keeps the exact client wording even if generated table order drifts, the header action anchor now measures that resolved `Quest Helper` title text instead of the older hardcoded local title width, `Delete all` clears the local registration set and closes the owner without the extra simulator-authored clear notice, title rows and inline requirement text now clamp through the shared delimiter-aware width helper instead of a blunt local character splitter, and the demand-rect compositor now prefers delimiter-aware wraps from the selected 150px band before falling back to raw character splits so slash-, hyphen-, and colon-heavy simulator text stays inside the client band more like the recovered owner instead of being chopped only by blunt token width. The remaining gap is that this is still a simulator-side approximation rather than the client's packet-driven `CUIQuestAlarm` registration path, so exact server-authored pin/delete semantics, the full official auto-register rule matrix behind `BtAuto`, the exact font tokens and any remaining unrecovered quest-alarm notice ids beyond the stabilized title/delete/empty trio, and the last CT-authored spacing or line-break micro-rules inside the demand rect are still not mirrored exactly | Active questing now has a persistent standalone progress tracker with animated quest-alert feedback, row-local and owner-wide dismiss affordances, a client-shaped five-slot registration cap instead of simulator-only paging, richer WZ-backed progress chrome, recent-update emphasis that now clears when the quest is actually viewed, a cap notice that is now actually sourced from the authored `ToolTipHelp.img/Game/Button/QuestAlarm/Desc` text instead of an unwired local fallback, and a per-character persisted auto-register/pinned-tracking model whose manual registrations keep a stable local pin order instead of being re-sorted by the quest snapshot. `BtQ` alert routing now also prefers an actually recent quest instead of whichever row happened to remain selected, the alert overlay itself now honors the authored Big Bang `ani` origins instead of drawing a locally offset approximation, every visible delete slot now stays actionable like the client slot layout instead of only the selected row presenting an enabled-looking delete affordance, and the selected demand stack now uses recovered client-style progress-threshold coloring plus tighter centered strip/icon/count placement for requirement and meso rows. The remaining gap is now narrowed further to packet-authored registration semantics, the official `BtAuto` rule matrix, remaining quest-alarm font or notice-id recovery beyond the stabilized text trio, and the last spacing/line-break micro-rules rather than to missing persistence, sticky viewed-alert routing, unstable manual pin order, an unanchored alert animation, loose demand-band row alignment, an over-permissive empty maximize path, stale hidden-auto delete-all state, an unwired authored cap sentence, drifting quest-helper wording, an extra delete-all clear notice, or blunt local title/body truncation. | progression/UI layer, quest runtime, demand rendering (`CUIQuestAlarm::Draw`, `CUIQuestAlarm::OnButtonClicked`) |
| Partial | Book collection / Monster Book parity | `BookCollectionWindow` is now actually wired through the dedicated Monster Book runtime instead of still defaulting back to the local collection-ledger snapshot path. The loader now prefers `UIWindow(.2).img/MonsterBook/backgrnd` for the owner shell and loads the client-backed `arrowLeft`, `arrowRight`, and `BtClose` controls from the Monster Book node itself while still placing them at the IDA-confirmed `CBookDlg::OnCreate` coordinates (`34,287`, `403,287`, `430,10`). The existing WZ-backed `cardSlot`, `infoPage`, `cover`, `select`, `fullMark`, `BtSearch`, `LeftTabInfo`, `LeftTab`, `RightTab`, and `ContextMenu/BtRegister` / `BtRelease` assets remain active, and the dedicated owner path now stays live from open through close: `MapSimulator.Content` clears the old collection-ledger provider for the book window, routes the live window snapshot through `ResolveBookCollectionActionContext`, feeds register/release clicks back through `_monsterBookManager.SetRegisteredCard` for the active inspected character build, and now also routes IME candidate clicks through the same Win32 IMM selection bridge already used by the simulator's other recovered `CCtrlEdit`-style owners instead of leaving the popup as a draw-only overlay. The `UserInfo` collect page still consumes the same Monster Book snapshot for summary text, while the opened book owner itself now consistently comes up on the dedicated Monster Book overview plus chapter-tab path, keeps the close/reset lifecycle aligned to `CWvsContext::CloseBook`, and no longer silently falls back to the synthetic collection-book spread when reopened from character info. This pass also keeps tightening the pickup side with direct WZ evidence from `item/Consume/0238/*/spec/consumeOnPickup = 1`: `MonsterBookManager` retains that authored flag from the card catalog, pickup-capacity checks no longer reject Monster Card drops on ordinary inventory-space limits, and resolved card pickups feed the Monster Book runtime without also leaving the `0238xxxx` card behind in inventory like a normal use item. The existing `MonsterBookManager` work remains in place: it still builds the catalog from `item/Consume/0238.img` plus mob metadata, persists per-character ownership under AppData, tracks up to the client-like five-card completion mark, resolves each mob back to its authored `0238xxxx` card item, groups by the client-facing `0238xxxx` chapter buckets, resolves reward and habitat naming through `String/MonsterBook.img` plus the item/map name caches, normalizes legacy multi-register saves down to one registered slot, and keeps the local search lane on the owner-local caret/IME/exact-match-first seam instead of the older dead append-only query path. This run narrows the remaining owner-local search polish further: the IME candidate popup already measured and drew through dedicated client-text rasterizers, sized itself from the visible candidate page rather than the full candidate list, numbered each visible candidate page locally (`1..n`), supported owner-local selection highlight, select-all/copy/cut/paste/insert clipboard paths, shift-extended caret movement, IME-first `Enter`/`Up`/`Down` handling while a candidate page is open, caret hit-testing plus visible-text windowing on text-element caret stops, and a fixed client-shaped `360ms` / `42ms` held-key repeat cadence; it now also honors the active IME candidate-window placement form before falling back to the local search-box anchor, so `CCtrlEdit::CreateIMECandWnd`-style point/rect/exclude placement can shape the simulator popup instead of every locale being forced onto the same local fallback origin. The remaining gap is narrower but still real: ownership and save flow are still simulator-local rather than packet-authored, Monster Card drops still use a simulator-spawned matched card item instead of recovered server/drop-table authorship and odds, and the last native client search-owner polish is now mainly the unrecovered locale-specific edit or candidate-owner quirks beyond this closer `CCtrlEdit`-style click, placement, and edit behavior. | This row is now closer to the client in one more important way: the dedicated Monster Book shell is no longer only loaded cosmetically while the live window keeps running a collection-ledger mode underneath, the card pickup path respects the authored `consumeOnPickup` contract carried by the WZ card items instead of treating Monster Cards like normal inventory loot, and the local search owner now behaves more like the recovered `CCtrlEdit` family rather than a simplified append-only text box. The simulator now opens the real Monster Book owner/runtime path for the book window, keeps the collect-page summary and book-owner detail views split the way the client seams imply, uses the Monster Book node itself for the shell plus page-turn/close buttons, preserves the already-implemented tab, search, registration, pickup, and close/reset behavior, tightens search editing with owner-local selection, clipboard, IME-priority navigation, text-element caret stepping, candidate-click ownership, and IME window-form placement, and no longer lets Monster Book search repeat cadence drift with the host machine's keyboard settings. The remaining divergence is concentrated in packet-authored ownership persistence, real server-authored drop odds/save flow, and the last locale-specific owner quirks rather than missing owner wiring, dead register callbacks, fallback collection-ledger reopen paths, stale full-list candidate sizing, global candidate numbering, draw-only candidate-popup clicks, forced fallback candidate anchoring, simplified text selection behavior, OS-shaped repeat timing, or card items lingering in inventory after pickup. | book-style collection owner and data bridge (`BookCollectionWindow.cs`, `UserInfoUI.cs`, `MonsterBookManager.cs`, `UIWindowLoader.cs`, `MapSimulator.Content.cs`, `MapSimulator.cs`, `CBookDlg::OnCreate`, `CBookDlg::Draw`, `CBookDlg::OnButtonClicked`, `CWvsContext::CloseBook`, `CMonsterBookMan::LoadBook`, `CCtrlEdit::OnKey`, `CCtrlEdit::CreateIMECandWnd`) |
| Partial | Parcel / package-delivery window parity | The simulator no longer presents package delivery only through the older memo owner. The main owner now loads `UIWindow2.img/Delivery` into `MemoMailboxWindow`, mirrors the client-confirmed `CParcelDlg` three-tab split with explicit receive/send/quick-send tab state, uses the authored `Delivery/Tab/*`, `Keep/base`, `Send/base`, and `Quick/base` surfaces, keeps receive rows in a dedicated parcel backlog with row selection plus wheel/scrollbar paging, routes the receive button family through the dedicated package-claim flow, and drives send/quick-send button behavior through the client-shaped split that IDA showed in `CParcelDlg::OnButtonClicked` (`Send`, `SetMoney`, and fee-info open/close routing). This pass also closes more of the packet/data seam: the parcel packet codec now treats the post-body byte as the client-shaped parcel state bitfield (`read`, `keep`, `claimed`, `has-item`, `has-meso`) instead of a plain boolean, applies those decoded flags into receive-session rows and single-entry packet deliveries, and extracts memo text from MapleString-like candidates in the `PARCEL::Decode` body before falling back to printable-ASCII heuristics. Owner-side compose parity is also tighter: send-tab package staging no longer has to start from `/memo draft item` commands because the in-owner package lane now accepts both click staging and real live-inventory drag-drop, preferring the exact dragged slot when one is active and otherwise falling back to the first live inventory item, while quick-send keeps item packages blocked both on click and on inventory drop. The remaining gap is narrower and now centered on the parts still not fully recovered from WZ plus client seams: the simulator still lacks the fully proven official `PARCEL::Decode` field map beyond current flags/text/attachment essentials (for example full timestamp, quick-delivery, and internal-field ownership), still does not have a recovered true packet launch mapping that ties parcel owner startup into `UI_Open`-family dispatch (IDA reconfirms `CUserLocal::OnOpenUI` only forwards to `CWvsContext::UI_Open`, and the recovered `UI_Open` switch still has no explicit parcel-owner case), still does not implement the true client attach picker/modal inventory browse flow beyond the now-modeled package-lane drag-drop handoff, and the exact memo/social ownership boundary beyond this explicit parcel-dialog split is still not fully recovered. | Package delivery is a real client-owned utility window family, not just a note inside messenger or memo systems. This row now tracks the dedicated Delivery owner, the recovered receive/send/quick-send split, flag-aware packet-owned receive row state from `CParcelDlg::OnPacket`, an owner-local item-staging click path over the `Delivery/Send` layout, and a closer attach handoff because the package lane now consumes real inventory drags from the existing inventory owner instead of only staging the first available item through a simulator fallback. The remaining divergence is narrowed to full official parcel payload mapping, final launch ownership around `UI_Open`, the unrecovered attach picker/modal browse flow beyond this lane-drop seam, and the last memo-vs-parcel ownership details rather than the older summary-only compose surface, unflagged packet rows, command-only attachment staging, or placeholder receive scrollbar behavior. | parcel dialog owner and packet-shaped parcel session seam (`MemoMailboxWindow.cs`, `MemoMailboxManager.cs`, `MemoGetWindow.cs`, `UIWindowLoader.cs`, `MapSimulator.cs`, `MapSimulator.ChatCommands.cs`, `UIWindowManager.cs`, `PacketOwnedParcelPacketCodec.cs`, `CParcelDlg::OnCreate`, `CParcelDlg::Draw`, `CParcelDlg::OnButtonClicked`, `CParcelDlg::OnPacket`, `PARCEL::Decode`, `CTabReceive::SetParcel`, `CUserLocal::OnOpenUI`, `CWvsContext::UI_Open`) |
| Partial | Skill macro parity | The macro window now enforces trimmed macro names against a client-shaped 12-byte budget instead of a flat 12-character ASCII subset, validates names inline against the current Windows ANSI locale, now actually renders a live byte counter in the editor, routes committed character input through the simulator `GameWindow.TextInput` path so locale and IME text commits no longer depend on the old synthetic ASCII key mapping, accepts clipboard paste as a best-effort locale text insert that fills the remaining byte budget instead of rejecting an otherwise valid prefix wholesale, owns a real cursor-aware name edit field with mouse caret placement plus `Backspace`/`Delete`/arrow/`Home`/`End` behavior instead of the old append-only buffer, now advances that caret and applies delete or backspace on grapheme-text-element boundaries so composed IME text and surrogate-pair glyphs are edited as a single unit instead of being torn apart mid-sequence, mirrors `CUIMacroSysEx`/`CCtrlEdit` ownership more closely by routing Win32 IME preedit composition into inline macro-name preview text with a blinking caret and composition underline before committed `TextInput` lands, and clamps that preedit preview to the same locale- and byte-valid insertable prefix the committed path would accept so the visible preview and live byte counter stay aligned with the eventual insert result. This pass tightens the remaining physical-key and paste path closer to the client's constrained macro surface by enforcing a best-effort blacklist seam on top of the code-page gate: macro names now reject dash and connector punctuation beyond the client-visible soft-keyboard `-`/`_` surface instead of still admitting the wider Unicode punctuation categories. It also loads the client `UIWindow.img/SoftKeyboard` chrome as an attached helper with client-shaped key-grid hit-testing from `CUISoftKeyboard::GetKeyIndexFromPoint`, hover feedback, min/max/close controls, caps or shift toggles, backspace or enter actions, and now feeds the client-derived `CUISoftKeyboard::UpdateKeyConstraint` mode split from the same locale-shaped byte count the edit field enforces before the existing byte-budget or locale-aware insertability gate decides whether a key can advertise as clickable, still supports drag-binding selected macros into QuickSlot entries, routes macro hotkeys back through `SkillManager` with party-chat announce semantics, paces queued casts so the next macro skill waits for the current cast to finish, and persists the five-slot macro definition list per active player or selected login-roster character under the simulator AppData profile across restarts instead of losing macro definitions with the session. This pass also extends the Win32 IME bridge past raw preedit text: the simulator now captures composition clause and cursor metadata plus the active candidate list, then renders a client-shaped candidate popup beside the macro-name edit field with page-sized rows, page-local numbered entries, selection highlight, below-or-above caret placement, and clause-aware horizontal anchoring derived from `CCtrlEdit::CreateIMECandWnd` instead of leaving candidate UI entirely to the host OS; the popup sizing now measures the visible candidate page rather than the nominal page size, so later pages stay aligned with the recovered `CreateIMECandWnd` numbering and sizing walk. The candidate popup is now also owned by the macro window's mouse-interaction seam instead of being a draw-only overlay: it participates in hit-testing, keeps edit focus when clicked, routes page-local candidate clicks back through Win32 IMM `NI_SELECTCANDIDATESTR` against the active candidate-list index rather than letting those clicks blur or fall through into the underlying window stack, and now draws candidate text through the simulator's existing client-text rasterizer seam with a distinct selected-row font so the owner no longer falls back to the generic XNA font family while the recovered `CCtrlEdit` path clearly carries dedicated candidate fonts. The remaining gap is the unrecovered per-locale or per-IME blacklist matrix beyond this best-effort filter, plus the last exact `CCtrlEdit` candidate-owner details such as locale-specific popup metrics and ownership quirks across IMEs. | Macro support now behaves like a real runtime surface instead of an editor-only stub, and the remaining gap is narrowed to the unrecovered locale or IME-specific filter matrix plus final candidate-owner metric polish rather than missing interaction ownership, committed locale input, byte-count enforcement, the rendered live counter, preview-clamped preedit rendering, text-element-safe editing, the soft-keyboard constraint-mode split, visible-page candidate sizing, page-local candidate numbering, candidate-popup ownership, or candidate-font selection itself. The simulator-drawn candidate owner now also keeps mouse ownership, can forward page-local clicks back into the active IME candidate list instead of acting like a passive overlay that lets clicks fall through or blur the edit, uses client-leaning candidate text rasterization rather than the old generic sprite-font draw path, and now keeps the soft-keyboard constraint split aligned with the same multibyte byte budget that governs actual macro-name insertion instead of only the raw character count. | `SkillMacroUI.cs`, `SkillMacroSoftKeyboardLayout.cs`, `SkillMacroNameRules.cs`, `SkillMacroImeCandidateWindowLayout.cs`, `SkillManager.cs`, `QuickSlotUI.cs`, `MapSimulator.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `WindowsImeCompositionMonitor.cs`, `WindowsImeCandidateSelectionBridge.cs`, `ImeInputState.cs`, `UnitTest_MapSimulator/UI/SkillMacroImeCandidateWindowLayoutTests.cs`, `UnitTest_MapSimulator/UI/SkillMacroSoftKeyboardConstraintTests.cs` (`CUIMacroSysEx`, `CCtrlEdit::OnIMEComp`, `CCtrlEdit::CreateIMECandWnd`, `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::UpdateKeyConstraint`) |
| Partial | Soft keyboard and constrained text-entry parity | The simulator now has a dedicated WZ-backed soft-keyboard owner loaded from `UIWindow.img/SoftKeyboard`, uses the client-derived `CUISoftKeyboard::GetKeyIndexFromPoint` grid geometry for hover and click hit-testing, routes hover state through a top-level UI manager seam so the key surface owns mouse focus ahead of underlying windows, and keeps mouse ownership modal while the keyboard is open so clicks no longer fall through into the underlying dialog. Its per-mode enable gating still matches the recovered four-way `CUISoftKeyboard::UpdateKeyConstraint` matrix (`0` alphanumeric-until-full, `1` alpha-edge/numeric-middle, `2` numeric-until-full, `3` numeric-until-full-alt`) instead of only a loose alpha-vs-numeric split. The constrained map-transfer target field still attaches as a numeric-only edit surface, but it now also routes physical-key text through the same committed-text plus IME composition seam the simulator already uses elsewhere instead of relying on raw digit polling, keeps an owner-local composition preview plus candidate-list overlay while focused, and no longer drops edit focus when the modal soft keyboard closes so the underlying client-shaped edit lane keeps ownership for follow-up physical-key input. The shared helper still owns the login title ID/PW fields through the existing login-title owner seam, and `LoginUtilityDialogWindow` still carries explicit per-prompt keyboard typing across both PIC and non-PIC utility flows: PIC prompts still use the `CPinCodeDlg::OnKey` `type 3` numeric-only-alt gate and honor the client `CCtrlEdit` 8-character cap, while account-migration acceptance still mirrors the recovered `CLoginUtilDlg::OnKey` `type 2` path with a masked 8-digit birth-date edit surface and the same numeric-only mode gate for both soft-keyboard and physical-key input. Physical-key routing for the modeled login-owned, anti-macro, trunk-owned, and map-transfer-owned utility edit surfaces therefore still goes through the same committed-text and IME composition seam, so those owners render in-place composition previews, own candidate-list presentation while focused, and clear that transient state when the active owner hides or the edit focus changes instead of leaving locale input to raw per-key polling. This pass closes two more owner gaps inside the same seam: `SkillMacroUI` now follows the recovered `CUIMacroSysEx::OnCreate` edit-owner shape by advertising its macro-name lane through `ISoftKeyboardHost`, anchoring the shared modal keyboard to the client `53,260,114,14` edit bounds, preserving the existing byte-budgeted 12-byte name gate, and dropping the older owner-local soft-keyboard surface in favor of the shared helper; and `AdminShopWishListUI` now follows `CUIAdminShopWishList::OnCreate` more closely by keeping the search edit focused on open, advertising that search lane through the same shared host seam, routing soft-keyboard input and physical IME composition through the focused edit instead of raw append-only polling, and keeping an owner-local candidate overlay attached to the search lane while focused. The modeled login-utility, anti-macro, map-transfer, trunk, skill-macro, and admin-shop wish-list edit owners therefore now mirror the client modal launch shape more closely, while the remaining gap is narrowed to still-missing utility search or edit owners plus the simulator-drawn IME candidate overlay that still stands in for the client's native candidate-window placement and post-close placement quirks after modal dialog close. | This is now a separate parity target from generic macro-name validation because it affects how locale input, constrained text fields, and client-owned edit controls behave before characters are committed. The simulator now has the on-screen key surface, hover ownership, modal mouse capture, a client-shaped four-type constraint matrix, shared-host attachment across the modeled login, anti-macro, trunk, map-transfer, skill-macro, and admin-shop wish-list edit owners, IME-aware committed-text ownership in those edit surfaces instead of only raw key polling, preserved focus return for the modeled map-transfer owner when the modal key surface closes, and a closer `CDialog::DoModal`-style request lifecycle for the modeled utility owners instead of keeping the soft keyboard permanently active whenever those fields exist. The remaining divergence is narrowed to still-missing utility owners such as the remaining search-oriented owner family plus the final native candidate-window and unrecovered post-close placement details rather than helper absence, click-through behavior, unconstrained PIC length, raw map-transfer polling, the old macro-local soft keyboard, or the cash-service wish-list search edit living outside the shared host seam. | utility-window text-entry layer, login-owned edit bridges, anti-macro edit bridge, map-transfer edit bridge, trunk prompt edit bridge, skill-macro name edit bridge, cash-service wish-list search bridge, shared soft-keyboard owner (`CUIMacroSysEx::OnCreate`, `CUIAdminShopWishList::OnCreate`, `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::OnMouseMove`, `CUISoftKeyboard::HitTest`, `CUISoftKeyboard::UpdateKeyConstraint`, `CLoginUtilDlg::OnKey`, `CPinCodeDlg::OnKey`, `CUIAntiMacro::OnCreate`, `CTrunkDlg::OnCreate`) |
| Partial | Repair-durability utility parity | MapSimulator now registers a dedicated `RepairDurability` owner backed by `UIWindow2.img/Repair`, routes packet `OnOpenUIWithOption` launches for `uiId == 33` into that owner instead of stopping at the dispatcher seam, preserves the client-derived option value as the repair NPC template id, blocks the launch when another unique-modeless utility owner is already visible, and lists damaged repair targets with per-item durability, selection, scroll, WZ-backed row chrome, and dedicated `Repair` / `Repair All` buttons. The owner still uses the client-derived repair-cost formula shape recovered from `CItemInfo::EQUIPITEM::CalcRepairDurabilityPay`, but it now also replaces the text-only NPC header with a live animated NPC preview built from the WZ NPC loader, routes repair clicks through a simulator-side send/result queue so the owner shows a pending request state before meso consumption and durability restoration are applied on the response path instead of immediately on button press, now mirrors the recovered `SendRepairDurability` / `SendRepairDurabilityAll` wire shape more closely by carrying the client opcodes (`131` single, `130` all), and now also follows the recovered `SetNPC` seam more closely by pinning the preview to the client-observed layer anchor (`57`, `92`), disabling generic idle-action cycling for the preview, and preferring the NPC template's WZ shop-action family (`info/shop` id, then explicit `shop` / `say` / `speak`, then other loaded action roots that actually expose a WZ `speak` child, then `stand*`) before falling back to `stand`. This pass closes more of the `SetItems` gap from IDA as well: the simulator no longer has to approximate equipped `nPOS` values only from coarse visible slots. It still appends damaged equip-inventory entries first and then damaged currently equipped entries the way the client decompile shows, but equipped entries now encode their single-item `nPOS` from the same client-shaped body-part categories the simulator already uses for avatar-look decoding (`100..118`, weapon buckets, pendant/ring variants, medal/belt/shoulder/pocket/badge) before falling back to the older slot table, keeps the proven `-1..-59` shape for those equipped rows, resolves delayed single-item repair responses back against either the live inventory slot or the live equipped part instead of only equipped-part object references, still exposes damaged hidden-under-cash equipped items by resolving the live underlying repairable part for each equipped slot before generating the row list, and still applies delayed `Repair All` responses against the queued request set instead of re-sweeping every damaged item currently visible when the response lands. It now also closes more of the owner-control gap from `CRepairDurabilityDlg::OnCreate` and `DrawMoney`: the repair window no longer just paints a passive scrollbar, but now exposes the recovered scrollbar lane at (`212`, `109`, `203`) through click, wheel, and thumb-drag interaction, and it aligns the fee-text draw anchor more closely to the client-observed `DrawMoney(..., 214)` / y `81` seam instead of keeping a looser simulator-only right edge. It now also closes more of the tooltip gap from `CRepairDurabilityDlg::OnMouseMove` and the WZ owner art: the repair owner uses the same five-row hit geometry and icon-strip hover rule the client decompile shows (`GetItemIndexFromPoint`, `rx < 46`), anchors the hover-owned tooltip from the recovered `ShowItemToolTip(x, y + 20, ...)` seam instead of an extra simulator-side X offset, and now loads the full `UIWindow2.img/ToolTip/Equip/*` label set so the hovered repair target can render WZ-backed equip stat, requirement, category, mesos, and star chrome instead of falling back to text-only labels. This pass closes more of that sheet-composition gap as well: the repair owner now consumes the already loaded `Can` / `Cannot` and `GrowthEnabled` / `GrowthDisabled` digit strips for requirement and growth values instead of falling back to plain text digits, surfaces item-growth `itemLEV` / `itemEXP` rows when the hovered equip actually has WZ growth metadata, and renders the required-job badge strip (`beginner`, `warrior`, `magician`, `bowman`, `thief`, `pirate`) from the same tooltip art family the shared equip owners use. Focused `UnitTest_MapSimulator` coverage now also guards the client-shaped equipped-position encoding and NPC repair-action fallback order so those recovered seams stop living as unverified helper behavior. The remaining gap is narrower and now concentrated in the seams the simulator still cannot prove exactly: any truly rare equipped-array categories outside the item/body-part mappings the client decode path already proves, the last `CActionMan::LoadNpcAction(..., -2)` action-set-index details for repair NPCs whose shop presentation is not recoverable from the loaded WZ action roots alone, the full native `CUIToolTip::ShowItemToolTip` sheet composition and autoposition behavior beyond the recovered hover anchor plus WZ label assets, and the true server-authored success/failure result payloads or rejection reasons that are still locally simulated after the delayed response. | Repair-durability is now a packet-owned utility surface with dedicated art, owner lifetime, option routing, repair math, a `SetNPC`-shaped animated NPC layer that stays pinned to a shop-facing action instead of random idle cycling, a client-shaped icon-hover tooltip lane for repair targets, and a request/response-shaped repair flow whose send opcode and single-slot encoding are much closer to the client. The row now also follows the recovered client `SetItems` source shape materially better by including damaged equip-inventory entries ahead of damaged equipped entries, surfacing underlying repairable equips hidden by cash presentation instead of dropping those slots, applying category/body-part-backed equipped `nPOS` encoding instead of only a coarse slot map, exposing the recovered scrollbar owner interaction instead of a draw-only proxy, applying `Repair All` results back onto the original queued candidate set rather than a later simulator-wide rescan, and using the shared WZ equip-tooltip chrome instead of repair-owner-specific text fallbacks for the hovered item sheet. The hovered repair tooltip is now closer to the simulator's other client-shaped equip owners too: it carries WZ-backed growth rows, required-job badges, and digit-strip value rendering rather than stopping at plain-text value draws once stat and requirement labels are present. The remaining divergence is narrowed to the last unrecovered equipped-array edge cases, the native action-manager `-2` action-set details, the remaining unproven parts of `CUIToolTip` layout/autoposition, and server-result fidelity rather than missing owner behavior, missing inventory-source repair paths, a dead icon-hover seam, a draw-only scrollbar lane, or an incorrect NPC preview anchor. | utility-window owner launched from local packet dispatch (`CUserLocal::OnOpenUIWithOption`, `CRepairDurabilityDlg`, `CItemInfo::EQUIPITEM::CalcRepairDurabilityPay`, `CRepairDurabilityDlg::SetNPC`, `CRepairDurabilityDlg::SetItems`, `CRepairDurabilityDlg::OnCreate`, `CRepairDurabilityDlg::OnMouseMove`, `CRepairDurabilityDlg::GetItemIndexFromPoint`, `CRepairDurabilityDlg::DrawMoney`, `CRepairDurabilityDlg::SendRepairDurability`, `CRepairDurabilityDlg::SendRepairDurabilityAll`, `AvatarLook::Decode`, `CActionMan::LoadNpcAction`) |
| Partial | Pickup notice parity | The simulator pickup-notice stack now renders item icons, resolves cached item names and type labels for success messages, records player pickup history, reconnects manual pickup failures through `PlayerCombat` so ownership and inventory-capacity checks consistently raise notices before the drop is consumed, mirrors the client `-3` style split through a shared screen/chat formatter, restores the client `-2` split by leaving inventory-full as a screen-only notice, routes meso success notices through the same formatter so pet meso pickups surface as a chat-log side effect instead of polluting the screen string, and aligns regular plus quest-item success wording more closely with `CWvsContext::OnDropPickUpMessage` by using the same item-type label path rather than pet-prefixed or generic quest text. It still applies a pet-specific validator backed by WZ `item/Consume/0200.img/02002058/spec/notPickupByPet` so local auto-loot can surface pet-blocked items with item-aware pet messaging without spamming repeated failures, resolves `Unavailable` failures against `DropPool` recent-pickup history so pet-, player-, mob-, and generic remote-preemption cases surface actor-aware pickup lines instead of collapsing to one fallback string, wires `DropPool` remote player and remote pet pickup callbacks into the same paired screen/chat notice seam instead of limiting live remote-loot surfacing to the mob callback alone, now preserves recorded remote-pet actor names when those recent-pickup records are replayed through `Unavailable` notices, lets live `RemoteUserActorPool` field actors feed that same seam through bounded remote-player plus avatar-look pet pickup producers instead of requiring surfaced remote loot to be manually injected, and now also exposes `DropPool`'s existing `DropPickupActorKind.Other` resolve path through a live callback so packet-authored or other non-avatar remote pickups can reuse the same notice formatter instead of only landing in recent-pickup history. This run also adds a synthetic remote-user packet pickup producer on the existing `RemoteUserPacketCodec` / `MapSimulator.RemoteUsers` seam, fixes the existing true drop-leave packet producer on the `RemoteDropPacketRuntime` / `DropPool.ApplyPacketLeave` seam so packet-authored remote pet pickups resolve the actual pet actor from `SecondaryActorId` with owner fallback instead of misattributing those notices to the owning player, and now keeps unnamed `DropPickupActorKind.Other` pickups actor-addressable once they pass through history by falling back to bounded `Actor <id>` labels during recent-pickup capture plus `Unavailable` replay instead of dropping back to the generic other-character wording. Focused `UnitTest_MapSimulator` coverage now guards both that `Other`-actor fallback seam and the earlier drop-leave pet decode / packet-driven remote-pet actor-name resolution path. The remaining gap is narrowed to exact official wording for simulator-owned failure or remote-preemption messages whose backing `StringPool` text is still not captured in-source, any still-missing pickup producers outside the currently wired synthetic remote-user and packet drop-leave paths, and deeper client ownership gates that may still suppress or reshape those notices | Pickup feedback now matches the verified client branch shape for `0`, `1`, `2`, `-2`, `-3`, and the default fallback more closely, with success-path screen/chat splits, inventory-full behavior, WZ-backed pet restrictions, recent remote-pet actor replay, live remote-player plus avatar-look remote-pet pickup surfacing, a generalized live `Other`-actor remote pickup seam aligned to the decompiled handler and existing runtime seams, a synthetic remote-user pickup replay hook, a corrected true drop-leave packet path for remote pet actor attribution, and a bounded fallback label for unnamed `Other`-actor pickup history so those replayed remote-preemption notices stay actor-addressable instead of collapsing to the generic branch after the live callback passes. The remaining divergence is concentrated in uncaptured official strings, any pickup producers beyond the currently wired packet seams, and client ownership gates that should still suppress or reshape those notices end to end | `PickupNoticeUI.cs`, pickup formatting/runtime (`PickupNoticeTextFormatter.cs`, `MapSimulator.cs`, `MapSimulator.RemoteUsers.cs`, `MapSimulator.RemoteDropPackets.cs`, `RemoteUserPacketCodec.cs`, `RemoteDropPacketCodec.cs`, `DropPool.cs`, `UnitTest_MapSimulator/PickupNoticeParityTests.cs`) |
| Partial | Cursor and utility-button parity | The simulator now builds dedicated WZ-backed `Menu` and `System` popup owners from `StatusBar2.img/mainBar/Menu` and `StatusBar2.img/mainBar/System`, routes the status-bar `BtMenu` and `BtSystem` buttons through those owners, fans their core entries back into inventory/equip/stat/skill/quest/character-info and channel-selection flows instead of leaving them as dead launchers, switches the cursor to the client hold art during active skill, quick-slot, and window drag flows, loads the client forbidden-hand and busy-search cursor families from `Basic.img/Cursor/13` and `Basic.img/Cursor/16`, and now dismisses those popup owners on any click outside their modeless bounds through the shared window-manager input path instead of only inside the popup shell. It also now registers WZ-backed `KeyConfig` and `OptionMenu` owners from `UIWindow2.img/KeyConfig` and `UIWindow2.img/OptionMenu`, routes `BtKeySetting`, `BtGameOption`, `BtSystemOption`, `BtOption`, and `BtJoyPad` into those owners instead of forbidden stubs, binds key settings back to live `PlayerInput`, shows the attached pad binding beside each keyboard row in `KeyConfig`, and constrains captured joypad remaps for those utility owners to the shared non-directional button family instead of letting `BtJoyPad` or `KeyConfig` steal left-stick or D-pad movement inputs. The `BtJoyPad` owner also now prefers connected controller slots when stepping ports in either direction, renders live stick and trigger calibration meters inside the `UIWindow2.img/OptionMenu` owner header instead of only text readouts, supports the existing staged dead-zone, per-axis dead-zone, trigger-threshold, curve, inversion, reset, and shared utility-binding rows through mouse, keyboard, and joypad navigation, lets left/right or D-pad adjustments drive the currently selected calibration row, lets the selected binding row cycle staged shared-button candidates or arm live capture through `A`/space without stealing directional movement inputs, seeds selection to the first client-style row when the owner opens, and now treats `Back`/`Escape` during an armed binding capture as a capture cancel rather than immediately discarding the whole owner before `Start`/`Enter` or `BtOK`/`BtCancle` commit or discard the staged state back into `PlayerInput`. `BtRank` and `BtEvent` also now land on WZ-backed ranking/event owners that render live simulator-backed ranking cards plus filtered event rows from login bootstrap, packet-owned field or overlay state, quest-guide or delivery routing, tracked quest alerts, and active special-field runtimes instead of placeholder shells, and those launches now finally pass through the recorded progression-owner seam so the ranking owner actually enters its client-shaped loading then NavigateUrl phase and the event owner records the same explicit launch source or open tick that its auto-dismiss lifecycle reports. `BtGameQuit` also no longer dies in a chat notice; it now opens a shared WZ-backed yes/no confirmation owner using the recovered quit prompt text instead of leaving the quit branch as a dead launcher. The remaining gap is narrower: the client's full joypad calibration workflow is still not mirrored exactly because the simulator still uses a compact staged row model instead of the client's full dedicated joypad control set and exact confirm choreography for every calibration branch, true packet-fed ranking ladders, attendance or calendar event payloads, and the deeper remote ranking/event dispatch behind `CUIStatusBar::OnButtonClicked` are still not mirrored exactly, and the quit branch still reuses the simulator's shared utility confirm owner instead of a fully recovered in-field quit owner path. | This is part of the client feel layer and affects almost every window or interaction surface once the rest of the UI becomes more complete. The row now covers client-shaped utility owners with live simulator-backed payloads for joypad, ranking, and event flows, the ranking or event launch path no longer bypasses the owner-lifecycle tracking that those windows already draw, and the system popup no longer leaves its quit branch as a dead launcher. The remaining gap is narrowed to the unrecovered full client joypad owner controls plus official network-fed ranking/event data, final dispatch fidelity, and the exact quit-confirm owner branch rather than dead owners, placeholder content, or read-only utility surfaces. | `MouseCursorItem.cs`, `StatusBarUI.cs`, `UILoader.cs`, `StatusBarPopupMenuWindow.cs`, `PlayerInput.cs`, `OptionMenuWindow.cs`, `UIWindowManager.cs`, broader UI manager layer (`CUIStatusBar::OnButtonClicked`) |
| Partial | Explicit ranking/event owner parity | `BtRank` and `BtEvent` now route into explicit simulator owners instead of generic utility shells. The ranking surface still stays simulator-local, but it now reflects the client-observed `CWebWnd` landing shape more directly: `UIWindow2.img/Ranking` remains the owner art seam, the snapshot still seeds a web-style landing request around the active world and character id before showing local `World Rank`, `Job Rank`, `Popularity`, and combat context, the IDA follow-up is wired through the actual `CUIRanking::OnCreate` request format by pairing `get_server_string_0` with the extracted `StringPool[0xAA2]` format string `http://%s.nexon.com/maplestory/page/Gnxgame.aspx?URL=webclient/totpersonrank&worldid=%d&characterid=%d`, and the owner now reports that request in a more client-shaped split between the resolved `NavigateUrl`, the recovered host seed, and the explicit `worldid` / `characterid` request payload instead of only flattening everything into one simulator note. The event surface also moved closer to the real `CUIEventAlarm` owner instead of only using the main-row shell: `UIWindow2.img/EventList` now loads the WZ-backed row `slot` and `icon` family plus the authored `calendar/bg/{0,1}/backgrnd*`, `today`, and `number/{normal,select}` assets, the calendar initializes around the nearest scheduled entry instead of always resetting to the current month, the day render now uses the authored day canvases instead of treating them as synthetic digit glyphs, the owner now renders a dedicated packet-authored alarm strip near the top of the window so recent notice, chat, buff-zone, AP/SP, tutor, radio, and follow-failure text can live in a tighter client-style timed feed instead of only as list rows, and the event list now prefers active packet/runtime rows over idle bootstrap summaries while still ordering same-day rows by packet/source recency and letting the calendar summary surface the nearest day's leading authored entries plus overflow count. The event owner still auto-dismisses after the client-observed 8 second window unless the user interacts with its WZ-backed filter or calendar controls, and the IDA follow-up still defines the client owners through `CUIRanking::OnButtonClicked` at `0x83c6a0`, `CUIRanking::Update` at `0x83c690`, plus `CUIEventAlarm::OnCreate` at `0x7ac4f0`, `CUIEventAlarm::Draw` at `0x7aad90`, and `CUIEventAlarm::Update` at `0x7aac00`. The remaining gap is narrower: the simulator still does not have the remote page payloads or live server-fed ladder content behind `CUIRanking`, the true packet-owned attendance/calendar text construction that populates `CUIEventAlarm::m_aCT`, or the live network event content that should replace the last simulator-authored fallback rows once real attendance/calendar packets are modeled. | Status-bar parity here is not only about buttons launching something. The client treats ranking and event views as separate utility owners with their own create/update/draw lifecycle, so tracking them explicitly prevents this area from looking "done" once the generic popup routing exists while the owner-specific ranking/event behavior is still only approximated. | status-bar launched utility-window owners (`CUIRanking`, `CUIEventAlarm`, `CUIStatusBar::OnButtonClicked`) |

### 2. Local utility and event packet parity

This area is still underrepresented by the existing window rows because the client keeps a packet-owned dispatch seam in front of several of those owners.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned local utility/event dispatch parity | `MapSimulator.PacketOwnedUtilityParity.cs` still carries the packet-owned local-utility bridge much closer to the concrete `CUserLocal::OnPacket` seam instead of synthetic aliases. The simulator can still dispatch raw/pseudo packet payloads for `OnOpenUI`, `OnOpenUIWithOption`, `OnGoToCommoditySN`, `OnOpenClassCompetitionPage`, `OnQuestGuideResult`, `OnDeliveryQuest`, `OnNoticeMsg`, `OnChatMsg`, `OnBuffzoneEffect`, `OnPlayEventSound`, `OnPlayMinigameSound`, `OnHireTutor`, `OnTutorMsg`, `OnAskAPSPEvent`, `OnFollowCharacter`, `OnFollowCharacterFailed`, `OnRadioSchedule`, and `OnSkillCooltimeSet`, while the overlay-owned `OnFieldFadeInOut`, `OnBalloonMsg`, `OnDamageMeter`, and `OnNotifyHPDecByField` shapes stay routed through their existing packet overlay owner. Client-backed `CWvsContext::UI_Open` ids that already have simulator owners still route through this bridge for inventory/equip/stat/skill/key-config/quest/character-info/item-maker/ranking/family/guild/party-search windows, and the exact `OnOpenUIWithOption` `uiId == 7` path still follows the recovered `UI_Close(7)` plus `UI_Toggle(7, option)` behavior by routing into the simulator `SocialList` owner with the requested `Friend` / `Party` / `Guild` / `Alliance` / `Blacklist` tab. `OnNoticeMsg` and `OnChatMsg` still decode the client-shaped packet payloads everywhere this seam accepts packets, with `OnNoticeMsg` still routing through the simulator's existing modal utility-dialog owner while mirroring the WZ-backed `Sound/UI.img/DlgNotice` cue. `OnPlayEventSound` / `OnPlayMinigameSound` still prefer `Field.img` / `MiniGame.img`, and `OnSkillCooltimeSet` still maps the recovered `0x004FAE6F` vehicle sentinel into the existing `5221006` Battleship cooldown runtime. The radio and tutor portions narrow further in this pass instead of introducing new owners: WZ recheck still shows `ui/UIWindow.img/Radio/On/0..4` and `ui/UIWindow.img/Radio/Off/0` as the client radio art family (`54x32`, `delay=150` on the animated `On` frames), and IDA recheck at `?CreateLayer@CUIRadio@@IAEXH@Z` still recovers the exact `CUIRadio::CreateLayer(int bLeft)` seam with `nMargin = bLeft ? 40 : 0`, `Origin_RT`, `RelMove(-3-width-nMargin, 3)`, resident `Off/0`, and the ctor-owned `bMute` alpha choreography between `Off` and `On`. The simulator now mirrors more of that concrete owner in the existing `RadioStatusWindow` seam instead of collapsing the HUD into a single texture: `Off/0` stays resident, the animated `On/*` overlay now fades against `_utilityBgmMuted` over the same recovered `100 ms` seam, and the already-landed packet/minimap `bLeft` latch, authored `timeValue` playback offset, overrun rejection, active-session silent no-op, and recovered `StringPool` ids `0x14CF` / `0x14D0` / `0x1501` / `0x1502` all stay intact. Focused regression coverage is now checked in under `UnitTest_MapSimulator/PacketOwnedLocalUtilityParityTests.cs` for the recovered radio `bLeft` resolution helper and the new client-style radio fade target/step helpers. The cooldown and tutor portions remain substantially narrowed from the earlier status-only state: packet-authored cooldown changes still feed the existing WZ-backed cooldown HUD/notice owner and ready-state chat path instead of only mutating cooldown bookkeeping, while `255/256` no longer stop at `TutorRuntime` status text because `OnHireTutor` still creates and maintains a real local packet-owned summon owner through the existing summoned-pool seam using the recovered tutor skill/object ids, `OnTutorMsg` still drives that owner into a visible message state, the rendered text message still uses the dedicated `UI/ChatBalloon.img/tutorial` frame, indexed tutor cue playback still follows the recovered `CTutor::OnMessage(long,long)` placement rule by centering the cue over the tutor actor and offsetting it upward by the summon height, and the tutor actor height still resolves from the same WZ `skill/<id>/summon/height` data the client reads through `CActionMan::GetSummonedProp` before falling back to the known Cygnus/Aran defaults. The remaining gap is narrower: the battleship sentinel path still approximates the client's `CTemporaryStatView::UpdatePassively` ownership rather than reproducing every downstream temporary-stat side effect end to end, tutor presentation still stops at a simulator-owned summon plus tutorial balloon rather than a byte-for-byte `CTutor` implementation and exact `CSummoned::Init` choreography, notice/event-sound presentation still relies on simulator-owned modal/UI and WZ sound playback seams rather than the client's exact `CUtilDlg` object lifecycle or sound-engine behavior, and the radio path still lacks the rest of the recovered `CUIRadio::CreateLayer` placement matrix plus the client's raw `AIL_quick_*` audio-handle ownership beyond the now-correct string ids, art family, `bMute` fade owner, playback offset, and minimap-offset branch. | This row still covers the actual packet entry points, owned window routing, chat/notice/audio packet decoding, modal notice ownership for `OnNoticeMsg`, the cooldown HUD/ready-notice seam for packet-authored cooldown updates, and the tutor owner that now drives both tutorial-balloon text and client-backed indexed cue placement while sourcing summon height from WZ. The radio owner is closer again in this pass: it still honors the packet-authored playback offset instead of replaying from zero, still uses the recovered radio `StringPool` bodies and path templates directly, still applies the client minimap-offset branch, and now also keeps `Off/0` resident while fading the animated `On` overlay through `_utilityBgmMuted` instead of treating the HUD as a single binary texture. Remaining risk is concentrated in the last low-level temporary-stat, modal-dialog lifecycle, summon-construction, sound-engine, and remaining radio-widget placement branches, not in a missing dispatcher, missing tutor owner, unknown `uiId == 7` target, missing notice/chat packet decoding, missing cooldown UI surfacing, absent radio/tutor packet entry points, the tutor indexed cue anchor rule, the tutor summon-height source, the radio mute-state art seam, or an ignored `OnRadioSchedule` time payload. | local-user packet/runtime bridge (`CUserLocal::OnPacket`, `CUserLocal::OnOpenUI`, `CUserLocal::OnOpenUIWithOption`, `CUserLocal::OnGoToCommoditySN`, `CUserLocal::OnFieldFadeInOut`, `CUserLocal::OnBalloonMsg`, `CUserLocal::OnDamageMeter`) |

### 3. Packet-authored local overlays and guidance parity

This slice is narrower than the full dispatcher row above and is still missing as its own planning surface even though the handlers have concrete client-owned behavior.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field fade and world/actor balloon parity | MapSimulator still carries this row through the packet-owned overlay dispatcher seam instead of direct-command-only state: `MapSimulator.PacketOwnedLocalOverlayParity.cs` and `LocalOverlayPacketInboxManager` can drain loopback `fade` and `balloon` packets through a dedicated local inbox, route them back into the same owner used by `/localoverlay`, and report dispatch status separately from the broader utility inbox. The field-fade path still matches more of `CUserLocal::OnFieldFadeInOut`: the fourth `OnFieldFadeInOut` `Int32` is still treated as the animation alpha input that `CAnimationDisplayer::RegisterFadeInOutAnimation` uses for a black full-screen fade, `PacketFieldFadeOverlay` still tracks the fade-in span, scheduled fade-out start tick, and expiry separately instead of flattening the sequence into one implicit timer, and the simulator now resolves the requested fade layer from the active avatar's current under-face composed layer and then applies the same `- 2` offset the client takes from `CAvatar::GetLayerZ()` instead of hardcoding a fixed local layer. Balloon rendering also moved another step closer to `CUserLocal::OnBalloonMsg`, `CUserLocal::ShowBallonMsg`, `CBalloonMsg::Init`, `make_balloon`, and the `UI/Login.img/WorldNotice/Balloon` assets: avatar-attached packets still replace the single avatar balloon while field/world packets append into a separate field list, the shared skin still loads the recovered short, corner, and long-arrow variants (`arrow`, `slArrow`, `se/sw/nw/neArrow`, `sel/swl/nwl/nelArrow`), wrapped text still breaks long tokens instead of overflowing the body, and each line still centers inside a client-shaped packet-width text box with the balloon body sized as `nWidth + 20`. This pass narrows the remaining `ShowBallonMsg` / `CBalloonMsg::Init` placement gap further: avatar-attached balloon state now snapshots the same `0, -15 - CAvatar::GetHeight()` origin-relative offset that `CUserLocal::OnBalloonMsg` computes when the packet arrives instead of re-deriving the offset from the live assembled frame every draw, avatar-attached layouts now stay on a fixed above-origin placement path that matches the client's `SetPosition(x - nWidth / 2, y - nHeight)` flow more closely, and only the field/world list still participates in the simulator's screen-clamp/overlap pass while the avatar message keeps its own attachment-local offset. Packet balloon state still preserves leading and trailing authored blank lines, each authored message still caches a client-shaped balloon body canvas up front, the simulator still caches the final attached balloon canvases per arrow variant so the nine-slice body, centered text, and tail are assembled once per needed variant, and the centered tail mount itself now comes from the loaded WZ arrow texture instead of a hardcoded horizontal anchor ratio and fixed `9px` overlap. `LoadUiArrowSprite` still samples the opaque rows of each `WorldNotice/Balloon` arrow canvas to recover top and bottom mount points, and the layout/draw path uses those recovered mount points to place centered tails and derive how far the balloon body should sit above the authored anchor. The remaining gap is narrower again: the simulator still approximates the client's exact COM-layer fade owner and fade-list lifecycle inside `RegisterFadeInOutAnimation`, only supports a focused subset of `CTextAnalyzer` controls instead of the full token/font/icon pipeline, and still does not reproduce the full COM-owned balloon attachment/layer ownership path end to end even though the spawn-time avatar offset and above-origin placement now follow the recovered `OnBalloonMsg` / `ShowBallonMsg` flow more closely. | These are visible event-owned overlays that sit between local gameplay and many later scripted or utility flows, and the simulator now has both a packet-shaped launch bridge and a closer owner for the two surfaces instead of collapsing them into chat lines or ad hoc notices. The remaining risk is concentrated in the final COM-backed animation lifecycle, full text-analysis pipeline, and COM-owned balloon attachment ownership rather than in missing dispatch, missing multiple field balloons, missing the avatar-derived fade layer source, missing packet-authored blank-line preservation, the avatar balloon still being tied to live frame-height recompute, or the centered-tail placement still being driven by hardcoded offsets instead of the balloon art. | packet-owned overlay layer (`CUserLocal::OnFieldFadeInOut`, `CAnimationDisplayer::RegisterFadeInOutAnimation`, `CUserLocal::OnBalloonMsg`, `CUserLocal::ShowBallonMsg`, `CUserLocal::CBalloonMsg::Init`, `make_balloon`) |
| Partial | Damage-meter and field-hazard notice parity | MapSimulator still carries this row through the same packet-owned utility dispatcher seam the client uses: the local utility inbox and direct packet injection path accept the real `CUserLocal::OnPacket` handler ids for this row (`243` for `OnNotifyHPDecByField`, `267` for `OnDamageMeter`) alongside the existing aliases, and both route back into the dedicated WZ-backed HUD/notice owner built from `Basic.img/Notice2`, `Basic.img/Notice3`, and `StatusBar2.img/mainBar/notice`. The HUD side still sizes the hazard and damage-meter frames from the actual WZ canvases (`Notice2` at `258x40/20/47`, `Notice3` at `266x21/20/55`), and the hazard panel still uses the real `StatusBar2.img/mainBar/notice` icon size while now drawing explicit colored follow-up badges for pending, throttled, acknowledged, consumed, and failed request states instead of only tinting flat text. On the pet side, the existing per-pet runtime seam still owns enablement and optional preferred-potion overrides, and the hazard follow-up now mirrors more of recovered `CUserLocal::TryConsumePetHP` and `CUserLocal::OnNotifyHPDecByField`: the request owner still chooses the first enabled pet as the requester, keeps a simulated shared auto-HP item selection alive across notices, resolves and records the concrete USE/CASH slot that would back `SendStatChangeItemUseRequestByPetQ`, and still validates that same slot again when the synthetic ack window closes before deciding whether the request only acknowledged, acknowledged and consumed a real potion through the existing inventory seam, or failed back into the no-HP-potion speech/chat path because the queued slot disappeared. The failure chat path itself now uses the same client string-pool line that `TryConsumePetHP` pushes through `CUIStatusBar::ChatLogAdd` (`StringPool[0x0D89]`, "You are lacking the HP Potion that your pet is supposed to use.") instead of a simulator-authored paraphrase. This pass moved the request seam itself closer to the client: the hazard path now emits the same packet-owned pet item-use request shape the simulator already uses elsewhere instead of only minting a local pending record, routing the synthetic `SendStatChangeItemUseRequestByPetQ` follow-up through the packet-owned CWvsContext gate, opcode `203` outpacket payload, live official-session bridge, generic outbox transport, and deferred queue fallbacks before the result stays simulator-owned. The row now also carries the deeper `TryConsumePetHP` variant flags through that same seam instead of hardcoding only the recovered field-hazard `0,0` branch: the simulator hazard command/runtime can mark forced and buff-skill requests, route them through the existing request model and context owner, emit the buff-skill payload bit, select the alternate packet-owned request indices (`1` for forced, `2` for buff-skill), and expose those variants in the follow-up text and packet-owned context status while keeping ordinary field-hazard traffic on request index `0`. Local-overlay status therefore now carries the packet-owned request index, synthetic pet serial, emitted payload hex, bridge/outbox dispatch disposition, buff-skill state, and the packet-owned exclusivity/outpacket context alongside the existing shared-item and pending-slot state, while the persisted packet-owned HP inventory type still stays coupled to `m_nPetConsumeItemID` instead of being overwritten by transient hazard-time guesses. The remaining gap is now narrower and concentrated on lifecycle that still belongs to the live client/server pair: the simulator still does not model the true networked `SendStatChangeItemUseRequestByPetQ` result packet ordering and server-owned success/failure resolution end to end, still cannot mirror the exact status-bar/chat timing beyond the recovered no-potion chat string and current synthetic ack window, and still cannot confirm the live server-side semantics of the newly threaded forced/buff-skill variants beyond simulator-owned request shaping and status ownership. | This row now has both halves of the client seam represented with more of the real lifecycle shape: the damage-meter timer still rides a `CUserLocal::OnPacket`-style launch into a WZ-backed HUD owner, and the HP-dec branch now has a pet-aware request/ack follow-up state machine, shared-item transport approximation, concrete inventory-slot attribution, persisted packet-owned HP item-type ownership, WZ-sized notice presentation, explicit badge-state rendering, the client-owned no-HP-potion chat literal, a real packet-owned outbound request/bridge seam, and variant-aware request ownership for forced/buff-skill branches instead of a single local "would request" approximation. The remaining risk is now concentrated in the still-missing live request-result ownership around `TryConsumePetHP`, not in missing dispatch, missing HUD art, missing request-state visibility, missing outbound packet shaping, missing slot attribution, the hazard failure path still using simulator-authored chat text, missing variant threading, or packet-config persistence being overwritten by transient simulator fallback state. | packet-owned HUD/notice layer (`CUserLocal::OnDamageMeter`, `CDamageMeter::SetTimer`, `CUserLocal::OnNotifyHPDecByField`, `CUserLocal::TryConsumePetHP`) |
| Partial | Quest-guide, delivery-quest, and class-competition launch parity | MapSimulator now carries this row through a raw packet-owned local utility inbox and decoder path instead of command-only stubs: the simulator can drain `CUserLocal::OnPacket`-shaped `274` quest-guide, `275` delivery-quest, and `250` class-competition launches from a dedicated loopback inbox, decode the client packet payloads for quest-guide mode or reset, quest-id or mob-map target rebuilds, delivery quest or item ids plus the disallowed-delivery list, and open the existing owners through the same packet-owned seam the manual launch helpers use. Packet-authored quest-guide demand state still rebuilds world-map mob targets into `WorldMapUI` and clears itself when no usable mob survives. `QuestDelivery` still uses the `UIWindow2.img/UtilDlgEx` `notice` frame and `QDeliveryIcon` art, and its list still mirrors the client’s `LoadData` series/show-quest model more closely than the earlier quest-log shortcut. This pass tightened the client-side worthy gating further with `CUIQuestDelivery::IsWorthlessQuest` evidence: packet-authored accept rows now suppress overleveled open-ended quests using the client’s `minLevel + 10` / open-end-date rule, preserve the explicitly selected packet quest, and still keep continuation rows visible when the series already has a completed prefix. The selected entry still resolves the live delivery cash-item inventory slot, exposes the client-facing slot index in the owner, and routes the quest-detail/local-delivery handoff through slot-aware inventory consumption before opening a single-quest created-NPC-style interaction seam. The packet-owned delivery OK path still prefers a synthetic preview NPC for the target template and opens only the requested quest against that NPC instead of the earlier broader delivery-only stub, which remains closer to `CUIQuestDelivery::OnButtonClicked` creating a `CNpc`, setting accept/complete-only quest state, and calling `CUserLocal::TalkToNpc`. The packet-driven `ClassCompetition` owner has been tightened again with direct client evidence: it still uses the authored `UIWindow2.img/classMatch` `backgrnd`, `Loading`, and `BtOK` assets, but its local `CWebWnd` lifecycle now also follows `get_server_string_0`, `StringPool[0x11DC]`, and `CClassCompetition::Update` more closely by recovering the literal `gamerank.maplestory` host, formatting the same `NavigateUrl` template the client uses for `Event/classbattle/gameview&key=%s`, waiting for an initial opcode `291` auth cache before the first synthetic navigation, and preserving the already navigated page on later packet relaunches or 180-second auth refreshes instead of blanking back to a fresh load state. The owner still surfaces summarized cached auth-token state and a seeded nearby-rank ladder preview so the singleton reads more like a cached `CWebWnd` page while auth remains local-only. The remaining gap is that `QuestDelivery` still approximates the client’s server-created `CNpc` quest list and real `TalkToNpc` packet/runtime with simulator quest definitions plus an overlay, while the class-competition page still lacks the true server auth key, remote `CWebWnd` page payloads, and live ladder content. | These are not generic variants of quest log or world map browsing. They are packet-authored launch surfaces for guided quest routing and delivery/class-competition utility flows, and the simulator now has the real packet-owned bridge, client packet ids, WZ-backed delivery chrome, client-shaped worthy-quest suppression for open-ended overleveled accepts, representative-series delivery rows, slot-aware delivery item handoff, a created-NPC-style quest-only interaction seam, and a closer auth-pending/auth-issued/navigated class-competition singleton with WZ-backed owner art, client `NavigateUrl` formatting, cached-page/background-refresh behavior, and seeded standings needed to exercise those launches end-to-end. The remaining risk is now narrowed to the deeper client-owned `CUserLocal::TalkToNpc` or server-created `CNpc` delivery runtime and the still-missing remote `CClassCompetition` web/auth payload model rather than to missing raw dispatch, owner absence, or direct-list action shortcuts. | packet-owned guidance/utility launch layer (`CUserLocal::OnQuestGuideResult`, `CUserLocal::OnDeliveryQuest`, `CUIQuestDelivery`, `CUserLocal::OnOpenClassCompetitionPage`) |

### 4. External cash-service stage parity

This area is currently unowned by the backlog set even though the client keeps Cash Shop and ITC as dedicated stage owners rather than utility-window variants.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Cash Shop and ITC stage ownership parity | `HaCreator/MapSimulator` now keeps this row deeper inside the existing service-stage seams instead of collapsing it back into `AdminShopDialogUI`. `MapSimulator.CashServiceStageParity.cs` still opens dedicated `CashShopStage` / `MtsStatus` owners and routes packet families through `TryApplyCashServiceStagePacket`, and the service-stage BGM override remains tied to WZ evidence from `sound/BgmUI.img/ShopBgm` across the full stage-open/close lifecycle so entering either `CCashShop` or `CITC` overrides map BGM and leaving both families restores the field track. `CashServiceStageWindow` now moves further from the earlier fixed-offset approximation for `384` / `412`: client evidence from `CCashShop::OnCashItemResult` and `CITC::OnNormalItemResult` now drives subtype-owned summaries, counter updates, and child-owner state, including decoded Cash Shop locker / gift / wishlist / buy / slot-count branches (`88`, `90`, `92`, `98`, `100`, `111`, `113`, `115` plus failure/result families) and packet-authored CITC list, search-list, sale-list, and purchase-list ownership through `ITCITEM::Decode`-shaped row parsing instead of only a single coarse listing summary. The stage now exposes packet-owned catalog rows back to the child owners, so `ItcList` can fall back to decoded packet rows when the shared `Mts` shell snapshot is absent, and `CashShopList` can likewise fall back to packet-owned wishlist rows when the parent shell has no live list snapshot. The remaining gap is narrower and explicit: the simulator still relies on `AdminShopDialogUI` for the richer non-wishlist Cash Shop catalog/runtime path, still only decodes the subset of `384` bodies that the current client evidence made concrete (not the full `GW_CashItemInfo` / gift / other cash-result families end-to-end), and still does not run a wholly separate client-faithful render/runtime path for every Cash Shop and ITC child owner. | Treating cash services as only a status-bar launch plus an in-map admin-shop shell hides a large client-owned stage transition before the player returns to field gameplay. Tracking the service-stage owners explicitly keeps future parity work anchored to the real client lifecycle for Cash Shop and ITC instead of overloading `CAdminShopDlg`, `OnGoToCommoditySN`, or generic utility-window rows with stage-owned behavior they do not actually own. | external cash-service stage layer (`CCashShop::Init`, `CCashShop::OnPacket`, `CCashShop::OnCashItemResult`, `CITC::Init`, `CITC::OnPacket`, `CITC::OnNormalItemResult`) |

### 5. Cash-service child-window and preview parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Cash avatar-preview and service-subwindow parity | The simulator now keeps the client-owned cash-service surface split into the same named seams the targeted IDA pass recovered instead of collapsing them back into one coarse shop owner. `CashShopStage` is registered as the top-level stage owner, `CashAvatarPreviewWindow` remains the dedicated `CCSWnd_Char`-style preview owner from `CashShop.img/Base/Preview` and `CashShop.img/CSChar`, and the cash shop now hides the whole owner family when the parent stage closes, including the dedicated `CashTradingRoomWindow` child owner instead of leaving it orphaned after `CCSWnd_Status/BtExit` or parent-stage close. The narrower stage child owners from the scan also now exist as first-class owners: `CCSWnd_Locker`, `CCSWnd_Inventory`, `CCSWnd_List`, `CCSWnd_Status`, and `CCSWnd_OneADay` are each registered separately, shown with the cash-shop family, and seeded from the recovered WZ control seams (`CashShop.img/CSLocker`, `CashShop.img/CSInventory`, `CashShop.img/CSList/Base`, `CashShop.img/CSStatus`, and `CashShop.img/PicturePlate/NoItem*`) instead of staying implicit inside the parent cash-shop summary. This pass pushed those owners closer to the client-specific `OnCreate` state too: the locker owner now surfaces the recovered selector/scrollbar and number-font seam (`wheelRange 208`, number-font present) and owns keyboard plus mouse selector stepping against the shared-character list instead of staying a passive text snapshot; the inventory owner now surfaces its tab/scrollbar/number-font seam (`wheelRange 140`), tracks owner-local row focus and wheel or keyboard scrollbar movement, supports keyboard tab cycling across the recovered inventory tabs, and still routes `BtExTrunk` back into the dedicated locker owner instead of staying a local text stub; and the list owner no longer only mirrors parent text because the dedicated `CCSWnd_List` surface now routes `BtBuy`, `BtGift`, `BtReserve`, and `BtRemove` back through `AdminShopDialogUI` while also driving selected-row focus, mouse-wheel row movement, pane toggling, and `PageUp` / `PageDown` / `Home` / `End` keyboard movement directly against the parent cash list state. `CCSWnd_Char` also moved closer to the recovered owner lifecycle from the targeted IDA read: it now keeps an explicit avatar-look snapshot, tracks a client-shaped `WEAR_INFO`-style item array, surfaces the recovered `CUserPreview` plus `CWvsPhysicalSpace2D` setup as owner-local state (`24,40 212x165` preview region, field/ladder-rope physical-space setup), refreshes that state through default-avatar, wear, takeoff, pet, and weapon-sticker mutations instead of only swapping the simulator render result, and now lets the dedicated owner trigger the recovered buy/default/takeoff button family through keyboard paths in addition to the WZ button seams. `CCashTradingRoomDlg` is likewise no longer routed through the anonymous social-room shell: the simulator instantiates a dedicated `CashTradingRoomWindow` from `UIWindow2.img/CashTradingRoom`, binds the client button family (`BtTrade`, `BtReset`, `BtCoin`, `BtClame`, `BtEnter`), resets its local session when shown so the init-money snapshot is refreshed from the current wallet, keeps a separate session-wallet snapshot instead of only rereading the outside wallet, and now exposes more of the recovered dedicated chat-entry / scrollbar / font-owner seam directly in the owner state, including wheel, page, home/end, track-click, and drag-thumb chat scrolling, staged draft cycling on the dedicated edit-control runtime, and a narrower staged remote-owner trade lifecycle where remote lock and final acceptance advance after the local owner instead of mirroring instantly. The remaining gap is now concentrated in the still-deeper client-specific runtime that the simulator does not yet own: `CCSWnd_Char` still renders through the simulator assembler instead of the client’s actual `CUserPreview` object and physical-space runtime; the locker, inventory, and One-a-Day owners still approximate the client selector, scrollbar, number-font, and reward/session machines instead of reproducing the full packet-fed object state and control plumbing; and `CCashTradingRoomDlg` still does not run on the client’s real `CCtrlEdit`, scrollbar, font-object, and packet-fed trade/session implementation even though the dedicated owner shell, scroll ownership, and staged local-versus-remote lifecycle are now explicit. | Cash-service parity is not only about entering the right stage. The client also depends on a second layer of named owners to make the stage usable and to preview avatar-affecting cash items correctly. Tracking those owners separately keeps future work anchored to the real preview, locker, inventory, list, status, One-a-Day, and cash-trading seams instead of treating them as anonymous controls inside `CCashShop::Init` or folding them back into field-side UI rows. | cash-service child-window layer (`CCSWnd_Char::OnCreate`, `CCSWnd_Locker::OnCreate`, `CCSWnd_Inventory::OnCreate`, `CCSWnd_List::OnCreate`, `CCSWnd_Status::OnCreate`, `CCSWnd_OneADay::OnCreate`, `CCashTradingRoomDlg::OnCreate`) |

### 6. Cash-service wishlist parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Cash-service wishlist dialog parity | The simulator now registers a dedicated `AdminShopWishListUI` owner instead of keeping wish-list handling inside the base `AdminShopDialogUI` modal path, using the WZ-backed `CashShop.img/CSItemSearch/PopUp` frame plus the `BtAllItem`, `BtSearch`, `BtCancel`, `BtBuy`, `PopUp/BtComboBox`, `CSItemSearch/Price/*`, `PopUp1/backgrnd`, and `PopUp1/Scroll/normal/*` control family as the current asset seam for the split owner. `BtRecharge` on the admin-shop dialog still opens that separate owner behind the existing yes/no confirmation, `AdminShopWishListUI` still focuses its local search lane on show, mirrors the client-observed `CUIAdminShopWishList::OnButtonClicked` split by routing `0x7D0` into a dedicated add-on toggle, trimming and submitting the search string through a separate search path, and closing through its own owner-local close button, and it now also drives the WZ-backed combo seam into the `CSItemSearch/Price/*` ranges instead of hard-collapsing search to one implicit band. `SearchItemName` no longer stays folded into the parent owner either: the loader now registers a dedicated `AdminShopWishListSearchResultUI` child owner from the client-owned `UIWindow.img/itemSearch/resultback`, `UIWindow.img/itemSearch/BtLeft`, `UIWindow.img/itemSearch/BtRight`, `UIWindow.img/itemSearch/BtRegist`, `UIWindow.img/itemSearch/icon0`, and `UI/Basic.img/BtHide` seam recovered through `CUIAdminShopWishList::ToggleAddOn` / `CUIAdminShopWishListSearchResult::OnCreate`, `AdminShopWishListUI` now hands staged local results over to that separate owner when the seam is available, and the main owner suppresses its local keyboard lane while the child is visible instead of treating search results as an in-owner popup only. That child owner now resets its local page/selection on show like the client's add-on path, stays pinned to the wishlist owner instead of behaving like an independent draggable surface, pages through the staged results via the dedicated left/right controls, shows the selected row's icon, category, seller, detail, and price summary, and routes `BtRegist` back into `AdminShopDialogUI` so the chosen NPC offer is focused and saved into the in-session wish-list state instead of always forcing the first result. `SearchItemName` is also narrower and closer to the shipped client seam than before: the search path still loads the WZ-backed alias rows from `String/CashItemSearch.img`, normalizes those per-item search terms into the existing wishlist scorer, and lets exact, prefix, substring, and multi-token matches land through that shipped alias vocabulary instead of only the local title/detail/seller text. `ToggleAddOn` no longer stays inside the parent owner either: the loader now registers a dedicated `AdminShopWishListCategoryUI` child owner from the same `PopUp1/backgrnd`, `PopUp1/BtCancel`, and `PopUp1/Scroll/normal/*` seam, `AdminShopWishListUI` now hands `0x7D0` over to that separate owner when the seam is available, the main owner suppresses its in-owner category popup and keyboard handling while the child is visible, and the child closes back through its own owner-local close or toggle path while preserving the staged category selection. This pass also moved the category text closer to the recovered client constructor: `AdminShopDialogUI` now publishes the category table through a client-shaped row matrix keyed to the exact `CUIAdminShopWishListCategory::OnCreate` StringPool id order (`634`, `637`, `639`, `640`, `649`, `642`, `641`, `644`, `643`, `651`, `652` plus child groups `653-656`, `6755`, `657-660`, `618-623`, and `624-633`) instead of a free-form simulator taxonomy, resolves those row labels through `MapleStoryStringPool` when the generated/overridden table carries the recovered client text, and only falls back to authored English labels when an id is still unresolved in the local string-pool seam. `AdminShopWishListCategoryUI` now behaves like a parent-owned add-on layer by staying pinned to the wishlist owner instead of acting like an independently draggable popup, and the add-on now treats row-button clicks like owner-local actions instead of keyboard-only staging by opening parent rows into their child stage, preserving the previously staged descendant when that branch is reopened, and committing leaf rows back to the wishlist owner on click or `Enter`. The remaining gap is now concentrated in what the simulator still cannot prove exactly: `SearchItemName` still does not receive true packet- or server-authored result rows, remote result metadata, or live service-side search/session flow, and the dedicated search-result owner still approximates the client's full packet-fed paging/result-session machine with local staged rows instead of reproducing every server-authored transition and payload. | Cash-service parity is not only about catalog browsing and stage ownership. The client exposes a separate wish-list/search owner with its own text-entry, toggle, close lifecycle, price band, staged result selection surface, and category add-on tree, so leaving that work folded into the base admin-shop row would hide a distinct client seam and make wish-list parity look more complete than it is. | cash-service wishlist owner (`CUIAdminShopWishList::OnCreate`, `CUIAdminShopWishList::OnButtonClicked`, `CUIAdminShopWishList::HitTest`, `CUIAdminShopWishListSearchResult::OnCreate`, `CUIAdminShopWishListCategory::OnCreate`, `CUIAdminShopWishListCategory::OnButtonClicked`) |

### 7. Packet-authored radio-schedule parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Radio schedule and `CRadioManager` lifecycle parity | `MapSimulator.PacketOwnedUtilityParity.cs` now keeps more of the recovered `CRadioManager` owner behavior inside the existing radio seam instead of leaving `CreateLayer` on a simulator-only shortcut. `OnRadioSchedule` remains a distinct packet-authored runtime; re-entry is still blocked while a session is live; the simulator still resolves `StringPool[0x1501] = "Sound/Radio.img/%s"` before `StringPool[0x1502] = "Sound/Radio.img/%s/track"` so track-metadata lookup stays separate from playable sound lookup; stop still clears the live track/audio descriptors together with the playback state; and completion is still polled on the client-shaped `~2000 ms` cadence recovered from `CRadioManager::Update`. The exact radio string-pool bodies are now implemented instead of being left as unresolved ids: `MapleStoryStringPool` carries the recovered `StringPool[0x14CF] = "[%s]'s broadcasting will begin. Please turn up the volume."`, `StringPool[0x14D0] = "[%s]'s broadcasting has ended."`, `StringPool[0x1501] = "Sound/Radio.img/%s"`, and `StringPool[0x1502] = "Sound/Radio.img/%s/track"`, and `UnitTest_MapSimulator/PacketOwnedRadioParityTests.cs` now locks those ids, the formatted start/path diagnostics, the `bLeft` resolution order, and the packet-owned `CWvsContext[3562]` mutation history so the radio owner strings and placement lifecycle do not drift again. WZ was rechecked again for the radio HUD seam in the active extracted data source: `UI/UIWindow.img/Radio/Off/0` still carries the `54x32` anchor canvas, while `UI/UIWindow.img/Radio/On/0-4` still carry the same `54x32` animated frames with `delay = 150` on each child. The dedicated `RadioStatusWindow` owner and tooltip diagnostics now expose the recovered `CUIRadio::CreateLayer` seam more faithfully by preferring the packet-owned `CWvsContext[3562]` override when present but otherwise falling back to the live minimap expanded-state branch for `bLeft`, still applying `nMargin = 40`, `Origin_RT`, and the concrete `UI/UIWindow.img/Radio/{On,Off}` art, and now freezing the active HUD to the session's captured `CreateLayer` side instead of re-evaluating minimap expansion every draw. `PacketOwnedLocalUtilityContextState` now also keeps a dedicated per-character radio slot lifecycle for `CWvsContext[3562]`, including runtime binding, character-transition reset, mutation sequence tracking, and last-mutation diagnostics so the active session can follow the recovered owner semantics more closely without scattering radio state outside the existing context seam. The remaining gap is now concentrated in the client-owned playback stack and true packet authorship: playback still terminates through the MonoGame/WZ backend instead of the client's `CRadioManager::MMS_Play` `IWzSound -> raw buffer -> AIL_quick_load_mem / AIL_quick_set_ms_position / AIL_quick_play` handle path, and `CWvsContext[3562]` is still simulator-owned context state rather than a real client slot fed by official packet mutation history or persisted end-to-end outside the simulator runtime. | Packet-owned radio scheduling is more than a sound trigger. It is a client-managed audio/UI/chat lifecycle that temporarily changes BGM ownership and presents user-facing state, so leaving it inside the generic dispatcher row makes the remaining gap look smaller and less coherent than it is. Tracking it explicitly keeps future work anchored to the right packet handler and manager lifecycle instead of folding radio playback into unrelated sound-effect or notice work. | packet-owned radio manager layer (`CUserLocal::OnRadioSchedule`, `CRadioManager::Play`, `CRadioManager::Update`, `CRadioManager::Stop`) |

### 8. Explicit key-config, game-option, and soft-keyboard owner parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Explicit key-config, game-option, and soft-keyboard owner parity | The simulator now carries this owner family more explicitly instead of only relying on generic utility launches. `KeyConfigWindow` still keeps the client-owned main vs quick-slot page split from `UIWindow2.img/KeyConfig`, stages edits until `OK`, preserves `Cancel`, and keeps the quick-slot settings page as a dedicated owner branch rather than a status-bar stub; this pass moves a bit further into the recovered `CUIKeyConfig::Draw` seam by keeping the WZ-backed `KeyConfig/icon/*` canvases inside a dedicated footer owner strip that now follows the currently selected action, previews the mapped palette slot for that action, and shows its staged binding state instead of drawing an unowned icon grid. `OptionMenuWindow` remains aligned with the recovered `CUIGameOpt::OnCreate` roster and staging lifecycle: the simulator-side checkbox list already matches the recovered `1001`-`1014` client ids, y positions, and ordering, still snapshots current game/system/extra/joypad values on show, and still commits only on `OK` while `Cancel` drops the staged owner state. The soft-keyboard path also moved closer again to the explicit `CSoftKeyboardDlg` family instead of stopping at helper hit-testing: `SoftKeyboardUI` now loads the full per-key `SoftKeyboard/key/*` families rather than reusing only key `0`, carries the client `capslock`, `lshift`, `rshift`, `enter`, and `backspace` function-key art, randomizes the numeric row per owner session and again when the numeric tab is reselected, adds the recovered three-tab owner header, keeps the compact vs expanded dialog layout, follows the recovered switching-character branch more tightly by keeping active T9 groups latched, cycling same-group presses through the client-style `1010`-`1018` / `1020`-`1028` letter families, clearing that state on tab or case changes, and expiring stale switching state when the owner text diverges or idles out instead of leaving the last switching group armed indefinitely, and now restores modal owner focus more explicitly by snapshotting the active soft-keyboard owner window in `UIWindowManager` and bringing that owner back to the front when the keyboard closes or its host disappears instead of leaving restoration entirely inferred. The remaining gap is narrower and now specific: the simulator still does not reproduce the client’s full `CUIKeyConfig::Draw` action-icon-to-palette/map layering, the selected key-config palette mapping is still a staged owner approximation rather than a recovered client palette/map table, and the soft-keyboard owner still approximates parts of `CSoftKeyboardDlg`’s native `CCtrlEditEx` switching-timeout and per-button focus-return stack rather than reproducing the complete modal edit-control sequence end to end. | This row now tracks real owner behavior rather than unnamed popup existence. Keeping it separate prevents the broader utility-button and constrained-text rows from overstating parity now that key-config, option, and soft-keyboard surfaces have their own staged owner state, page/layout splits, checkbox roster, WZ-backed palette/footer content, selected-action footer ownership, randomized keypad state, explicit dialog buttons, and explicit modal-owner restoration. The remaining work is concentrated in the last client-specific palette/map linkage plus the final native edit-control sequencing details rather than in missing owners or missing owner-local button lifecycle. | utility-owner layer (`CUIKeyConfig::OnCreate`, `CUIKeyConfig::Draw`, `CUIGameOpt::OnCreate`, `CUIMacroSysEx::OnCreate`, `CUISoftKeyboard::{GetKeyIndexFromPoint,OnMouseMove,HitTest,UpdateKeyConstraint}`, `CSoftKeyboardDlg::OnCreate`, `CSoftKeyboardDlg::OnButtonClicked`) |

### 9. Tutor, skill-guide, and AP/SP helper parity discovered by `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Tutor summon and message parity | The simulator now keeps tutor traffic on a dedicated packet-owned runtime and summoned-owner seam instead of burying it under generic notice/chat behavior: client packet `255`/`1012` toggles a tutor actor through the existing summoned seam, resolves the Cygnus (`10001013`) vs Aran (`20001013`) tutor variant from the local job family, mirrors the WZ-backed summoned heights from `Skill/1000.img/skill/10001013/summon/height = 80` and `Skill/2000.img/skill/20001013/summon/height = 70`, renders string tutor messages with the `UI/ChatBalloon.img/tutorial` skin, and explicitly tears down any prior tutor summon before re-hiring to match `CUserLocal::OnHireTutor`'s remove-then-init lifecycle. `CUserLocal::OnTutorMsg` parity is split by payload shape now: string payloads keep the tutor-text balloon path, indexed payloads render their `UI/tutorial.img/<index>` asset layer with the client-style half-width and `(layerHeight + summonHeight)` placement, and both payload shapes now trigger the tutor summon's `say` playback through the summoned seam, including the client `CTutor::OnMessage(long,long)` Aran exception where numeric cues skip `SetAttackAction(17)`. The packet-owned tutor runtime now mirrors the recovered static `CUserLocal::ms_anTutors` bookkeeping more closely as well: hires append the active tutor skill id only if it is not already present, removals delete that skill id from the ordered slot list, local-character rebinds clear only the visible actor while preserving the static slot list, and the existing local-utility/Event-owner status paths now surface both the client-style slot order and the richer simulator-side Cygnus/Aran snapshot metadata (active-vs-listed state plus bound character / last hire-or-removal tick). The remaining gap is that ownership outside the local visible tutor actor is still inferred through simulator-side metadata rather than the full broader client semantics that may reuse `ms_anTutors` across non-local or future tutor-owner paths, so exact non-local slot semantics remain unresolved. | Leaving tutor flows buried inside the generic local-utility row hides a separate visible helper actor plus packet-authored message lifecycle that can break tutorial or guided-play fields in ways ordinary notice/chat parity will not catch. Tracking the tutor owner explicitly keeps future parity work anchored to the right packet handlers and actor lifecycle instead of treating tutoring as another dialog string. | packet-owned tutor/helper actor layer (`CUserLocal::OnHireTutor`, `CUserLocal::OnTutorMsg`, `CTutor::OnMessage`, summoned-style tutor initialization inside `CUserLocal::OnHireTutor`) |
| Implemented | Packet-authored skill-guide and AP/SP helper prompt parity | The simulator now tracks the dedicated `CUserLocal::OnOpenSkillGuide` dispatch as client packet `262`, exposes a `skillguide` alias on the local utility inbox/command seam, packet-opens the skill owner through the existing `UI_Open(3, 1)` path that matches the IDA seam at `0x90e6a0`, and explicitly opens the current Aran guide page when the active job has one. `CUserLocal::OnAskAPSPEvent` is also modeled beyond plain text now: packet `273`/`1008` decodes the client-shaped two-Int32 payload, validates it against the recovered `CWvsContext+0x20B4` receive token from the IDA seam at `0x90f0e0`, opens the helper prompt with the exact recovered `StringPool::ms_aString[0x17BA]` copy from `MapleStory.exe`, uses `OK` and `Cancel` labels to match that copy, and simulates the accepted follow-up outpacket `195` with response `6` through a separate send-context token path mirroring the client `CWvsContext+0x2030` field split. The AP/SP receive/send tokens now persist under a broader simulator-side packet-owned local utility `CWvsContext` owner instead of an AP/SP-only seam: that owner initializes once for the active character, preserves detached receive/send divergence while that character remains active, and resets only when the simulator actually transitions to another local character so stale prompts and follow-up state do not leak across sessions. | These helper prompts sit between tutorial/script flow and ordinary utility windows. Keeping them explicit prevents the broader local-utility dispatcher row from looking complete once the simulator can only show text or open the guide manually, and it anchors future work to the packet-driven launch/confirm path the client actually uses. | packet-owned helper prompt layer (`CUserLocal::OnOpenSkillGuide`, `CUISkill::OpenCurSkillGuide`, `CUserLocal::OnAskAPSPEvent`) |

### 10. Explicit minimap owner parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Explicit minimap owner parity | `MinimapUI` now mirrors the concrete `CUIMiniMap` owner path more closely instead of only carrying broad simulator-side behavior: the loader consumes the client WZ `UIWindow2.img/MiniMap/MinMap`, `MaxMap`, `Min`, and `BtSmall` assets that were previously ignored, the minimap owner now switches between compact and expanded minimap options through its own button family, preserves the previous expanded option when `BtMin` collapses the window, restores that remembered option on `BtMax`, keeps the world-map launch routed directly into the dedicated `WorldMapUI` owner instead of the separate map-transfer window, and still performs a `CWorldMapDlg::CreateWorldMapDlg`-style preflight before showing the world map with the dedicated failure-notice branch tied to `StringPool[0x118]` when the current field cannot be presented by the simulator world-map owner. This pass tightens the owner-local option surface further too: the collapsed frame uses the dedicated WZ `MiniMap/Min` chrome instead of reusing the larger map-frame assets, the owner toggle path cycles compact -> expanded -> collapsed -> remembered expanded, `BtMin` and `BtMax` both route through the shared `1000` state-button path while `BtBig` and `BtSmall` stay on the shared `1003` option-button path, the explicit ensure-collapse / ensure-expand helpers now honor that owner-local state machine instead of falling back to the old binary expand/collapse approximation, and the visible state-button family now follows the owner state more closely by exposing `BtMin` only while expanded and `BtMax` only while collapsed. The collapsed strip now also follows the owner-local slot geometry more closely: the title reserve width uses the single shared `1000` state-button lane plus `BtMap` instead of double-counting both `BtMin` and `BtMax`, the reserve inset now matches the live top-row padding used for the collapsed `MiniMap/Min` chrome, and the `BtNpc` family no longer stays visible while the owner is collapsed so the minimized strip only carries the controls its WZ lane actually budgets for. Focused `UnitTest_MapSimulator` coverage now locks that shared collapsed reserve-slot math and the collapsed-vs-expanded button-family visibility split alongside the earlier owner-local transition coverage, so this seam stops regressing silently. The existing owner-local hover path for off-map helper arrows, NPC markers, employee markers, and portal markers remains in place and is still anchored to this explicit owner row rather than only the broader minimap feature row. The remaining gap is now narrowed to final visual polish against the live client: the collapsed-bar title text positioning, clipping, and exact per-pixel spacing inside the `MiniMap/Min` strip still need direct client comparison before this row can be marked complete. | Naming `CUIMiniMap` explicitly closes the seam-collection gap that backlog 8 itself called out. It gives future minimap fixes a client owner for option cycling, world-map launch ownership, remote-direction helper tooltips, NPC/employee/portal hover routing, and other minimap-only behavior that should not keep being inferred indirectly from `MinimapUI` or `WorldMapUI` alone. | minimap owner layer (`CUIMiniMap::OnButtonClicked`, `CUIMiniMap::OnMouseMove`, `CWorldMapDlg::CreateWorldMapDlg`, `CEmployeePool`, `CPortalList`) |

### 11. Explicit skill-macro owner parity discovered by follow-up IDA lookup

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Explicit skill-macro owner parity | `SkillMacroUI` now follows the explicit owner seam end to end instead of only borrowing the old `OnCreate` layout: the loader binds the WZ `UIWindow2.img/Skill/macro` owner chrome (`backgrnd2`, `backgrnd3`, `check`, `select`, and all three `Macroicon` owner states: `icon`, `iconDisabled`, `iconMouseOver`), the row hit-testing and draw layout follow the IDA-recovered `CUIMacroSysEx::Draw` geometry (`select` at `(11, 27 + 41*i)`, skill icons at `(14 + 34*j, 30 + 41*i)`, selected macro icon at `(14, 241)`), the embedded name field follows the `CCtrlEdit` owner bounds from `CUIMacroSysEx::OnCreate` (`53, 260, 114, 14`), the checkbox hit area is owner-gated to an actively selected macro, and `BtOK` mirrors `CUIMacroSysEx::OnButtonClicked` by committing the selected macro in place and surfacing the dedicated owner tooltip and notice path. The owner-local `CCtrlEdit` focus seam is narrower too: hardware IME/keyboard capture and the soft keyboard now only stay active while the embedded name field is actually focused, and row, drag, and checkbox interactions explicitly transfer focus away from the edit control instead of leaving the whole dialog in a pseudo-focused text-entry state. Follow-up verification for this row also confirmed the exact `StringPool::GetString` payloads tied to the owner button path, so the simulator now resolves and uses the localized owner texts for `0x1108` (`Saving the skill name.`) and `0x0D01` (`It is saved.`) instead of leaving them as unresolved fallbacks. | Naming the concrete macro owner keeps this area from looking complete once text entry and macro execution mostly work. It anchors future parity fixes to the actual client dialog that owns macro presentation and button handling instead of leaving macro polish split across generic text-entry and utility-window rows. | skill-macro owner layer (`CUIMacroSysEx::OnCreate`, `CUIMacroSysEx::Draw`, `CUIMacroSysEx::OnButtonClicked`) |

### 12. Packet-owned keymap and pet auto-consume parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned keymap and pet auto-consume initialization parity | `HaCreator/MapSimulator` now has a dedicated packet-owned `CFuncKeyMappedMan` owner seam. The local utility inbox accepts `398/399/400`, dispatches them through the packet-owned utility controller, persists the hydrated state in a dedicated simulator config store, and restores that config during player initialization so packet-owned fallback data exists before the settings and field-hazard layers use it. Packet `398` now decodes and stores the full 89-entry `FUNCKEY_MAPPED` table, clears decoded entries whose id is `22`, snapshots the previous table into an `Old` mirror, and falls back to the persisted simulator config/default path when the packet payload is flagged as local/default ownership. Local `KeyConfigWindow` commits now save the simulator binding fallback into that same store so the packet-owned fallback path is backed by real local config instead of transient runtime state, and packet-owned `398/399/400` hydration now preserves that local fallback snapshot instead of overwriting it with the transient live packet remap. The live menu/window remap still follows the client’s menu ids instead of the previous guessed mapping: the simulator translates packet-owned type-`4` ids `0/1/2/3/7/8/9/15` into equip, item inventory, stat, skill, minimap, quest, key-config, and quick-slot `PlayerInput` toggles, matching the v95 `MENU_*` owner ids used by `CDraggableMenu`. Follow-up owner work now also translates arbitrary packet-owned skill/item/macro placements into the simulator’s existing bindable hotkey seams when the raw key does not collide with protected movement/chat/escape ownership: packet-owned cast entries repopulate the primary plus function hotkey bars, rebind `InputAction.Skill1-8` and `FunctionSlot1-12` to the packet-authored keys, and mark those assignments so the packet-owned raw runtime shim only fires the entries that the ordinary input layer still cannot absorb. That keeps packet-owned skill/item/macro placements visible in the existing quick-slot and key-config seams instead of leaving them as invisible raw-key dispatch only. Packets `399` and `400` still decode packet-owned auto-HP / auto-MP consume item ids, fall back to the persisted config value when the payload sends `0`, and persist the resolved ids; the HP item is also pushed into the existing field-hazard shared-potion seam when an inventory type can be resolved, while the stored packet-owned MP item now runs through the simulator’s consumable-use path once packet-owned config is loaded, MP is below the warning threshold, and at least one active pet owner exists. The remaining gap is narrower and now centered on exact client-owned keys that the simulator still protects or does not model as bindable packet-owned actions: `AdaptVirtualKey`’s status-bar shortcut-tooltip ownership is still missing, the simulator still does not translate the rest of the client’s non-menu control semantics for jump/attack/pickup/interact/chat/escape and adjacent protected keys, and packet-owned raw placements that fall outside the simulator’s current 20 bindable primary/function hotkey actions still rely on the packet-owned runtime shim instead of a fully client-faithful input owner. | This row now tracks a real packet-owned initialization owner instead of a missing note. The simulator no longer loses the distinction between packet-owned keymap/bootstrap state and later local UI edits: packet dispatch, fallback persistence, startup hydration, `id == 22` clearing, config-backed restore, corrected client menu-id translation, preserved local fallback bindings, bindable primary/function hotkey remap, and both HP/MP consume owner hydration all live under the correct owner seam. Keeping the row `Partial` prevents the remaining exact `AdaptVirtualKey` tooltip ownership and full non-menu key-owner translation work from being lost under the now-implemented bootstrap, live bindable remap, and pet consume runtime path. | packet-owned keymap/runtime init layer (`CFuncKeyMappedMan::OnPacket`, `CFuncKeyMappedMan::OnInit`, `CFuncKeyMappedMan::OnPetConsumeItemInit`, `CFuncKeyMappedMan::OnPetConsumeMPItemInit`) |

### 13. Packet-owned anti-macro result parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned anti-macro result and launch parity | `HaCreator/MapSimulator` now has a dedicated packet-owned anti-macro bridge in `MapSimulator.PacketOwnedAntiMacroParity.cs`: packet type `1011` is dispatched from the existing packet-owned utility seam, launch packets split normal-vs-admin owners, carry the decoded answer-count into the anti-macro owner seam, treat the client timer as a fixed `60000` ms timeout, decode the remaining payload bytes as the JPEG challenge canvas, hold and release local Ctrl-combo input while the challenge is active, route `4/5/8/10` report branches through status-bar chat plus screenshot persistence, and route `7/9/11` close or result families through a dedicated `AntiMacroNoticeWindow` modal owner that now follows the recovered `CUIAntiMacroNotice` seam instead of the generic utility-feedback path. That notice owner uses the WZ-backed `UI/UIWindow2.img/Macro/popup`, `btOK`, `btCancle`, and the recovered `Macro/admin/*` avatar canvases, and the bridge now carries the recovered `StringPool` ids for the notice/chat families (`0xC84`-`0xC9A`, `0x1A65`, `0xC8D`-`0xC91`) through the simulator feedback state via the existing `AntiMacroOwnerStringPoolText` seam instead of leaving those ids embedded only in the packet bridge. This run also moved those notice/chat surfaces off bridge-local English literals and back onto the generated `MapleStoryStringPool` client table as the primary text source, leaving the recovered raw hex/seed evidence only as provenance and fallback metadata, tightened the `mode 9` notice default so unknown anti-macro subtypes now follow the same passed-thanks branch the client uses in `CUIAntiMacroNotice::OnCreate`, and corrected owner-local status tracking so closed notice modals no longer report as still active and completed result teardown now clears the preserved submitted-answer snapshot the same way the client zeroes its remaining anti-macro question state after `7/9`. Follow-up IDA recovery for `CWvsContext::SaveAntiMacroScreenShot`, `CScreenShot::GetBaseFolder`, `CWvsContext::OnAntiMacroResult`, `CUIAntiMacro::SetRet`, and `CUIAdminAntiMacro::SetRet` is now wired through the same seam too: anti-macro screenshots keep the client-shaped `%s_%04d%02d%02d_%02d%02d%02d.jpg` naming, screenshot folder modes mirror the recovered client branches for parent-of-exe, desktop-without-trailing-slash, and `C:\\`, and answer submission now mirrors the client owner teardown more closely by closing the active challenge locally, preserving the held-combo state while awaiting resolution, attempting a real outbound packet `117` send through the local-utility official-session bridge, and now also using the bridge's deferred outbound queue when a Maple client is already attached but session init has not completed yet so the client-shaped submit can flush as soon as crypto init finishes instead of being dropped immediately. The remaining gap is now almost entirely tied to authoritative network ownership rather than simulator UI/text behavior: full submit parity still depends on a real bridged Maple session and server-issued response timing, and the simulator still cannot complete or validate the anti-macro round-trip against authoritative result packets when no live Maple session is attached at all. | This row now covers the actual packet-owned anti-macro controller more faithfully: the simulator models owner selection, combo hold lifecycle, JPEG challenge decode, report/result branch routing, screenshot capture, the dedicated modal notice owner, the confirmed screenshot-root behavior from `CScreenShot::GetBaseFolder`, the generated client `StringPool` text path for the notice/chat families, the client-shaped submit dispatch attempt, and now the attached-but-not-yet-initialized live-session queue path for outbound opcode `117` instead of leaving those pieces buried behind generic chat or feedback surfaces. Keeping the row `Partial` makes the remaining requirement for a real server-backed anti-macro response explicit rather than hiding it under work that is now already owned by the simulator seams. | packet-owned anti-macro controller (`CWvsContext::OnAntiMacroResult`, `CWvsContext::SaveAntiMacroScreenShot`, `CWvsContext::ShowAntiMacroNotice`, `CUIAntiMacroNotice::{CUIAntiMacroNotice,OnCreate,Draw}`, `CUIAntiMacro::SetRet`, `CUIAdminAntiMacro::SetRet`) |

### 14. Anti-macro owner parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Anti-macro challenge dialog parity | `HaCreator/MapSimulator` now has a dedicated anti-macro owner instead of only field-limit item gating. `MapSimulator.PacketOwnedAntiMacroParity.cs` registers a real `AntiMacroChallengeWindow`, and `UI/Windows/AntiMacroChallengeWindow.cs` now mirrors the client owners more closely: the normal anti-macro surface uses the WZ-backed `UI/UIWindow2.img/Macro/step1` frame instead of a generic box, the admin surface also uses the shipped `UI/UIWindow2.img/Macro/step1_admin` layered frame and `btOK` chrome instead of the older simulator shell, both owners are re-centered from their active WZ frame size, the typed-answer runtime stays inside the owner with the client-observed 12-character cap, the countdown draws from `UI/UIWindow2.img/Macro/num1/*` at the recovered `CUIAntiMacro::Draw` coordinates, the anti-macro edit seam keeps the recovered `CUIAntiMacro::OnCreate` bounds/colors/font-cap contract (`id=1000`, `42,189,194,16`, font `StringPool 0x1A25`, `nBackColor=0`, `nFontColor=0xFF000000`, `nHorzMax=12`), and the edit approximation now preserves the selected substring and active-edge caret instead of collapsing selected text out of the visible buffer. The attempt line also now resolves through the live `MapleStoryStringPool` override for recovered `StringPool 0x1A15` (`%d`) before falling back to simulator wording, so this row no longer depends on an unimported attempt-string literal. The remaining gap is now specifically native control ownership: the simulator still does not instantiate a true child `CCtrlEdit` with the client's own Win32 font, caret blink, IME candidate, and selection-rendering pipeline, so the current seam remains a close managed approximation rather than the exact client text-control runtime. | Lie-detector or anti-macro checks are more than an item ban. The client presents them as a distinct timed text-entry owner with its own layout, countdown, and attempt messaging, so leaving this surface untracked makes progression/utility parity look more complete than it is and hides a tutorial/event-facing failure mode that will not be covered by field-limit notices alone. | timed anti-macro utility owner (`CUIAntiMacro::OnCreate`, `CUIAntiMacro::Update`, `CUIAntiMacro::Draw`, existing simulator seam contrast: `FieldInteractionRestrictionEvaluator.cs`) |

### 15. Additional quest-timer UI, quest-reward, and revive owners discovered by follow-up IDA scan

The latest IDA pass surfaced another utility-owner family that is still underrepresented in this backlog: the client keeps dedicated modeless owners for quest-timer UI, quest reward/raise windows, and revive confirmation instead of folding those flows into generic quest log, field-state packet handling, or respawn handling. A follow-up lookup also pinned the adjacent action hooks more concretely: `CUIRaiseWndBase::OnButtonClicked` at `0x8391d0`, `CUIRevive::OnButtonClicked` at `0x83ce40`, `CUIRevive::Update` at `0x83ce70`, and `CUIRevive::Revive` at `0x83cde0`.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Implemented | Dedicated quest-timer UI owner parity | `UIWindowManager` now allocates a concrete quest-timer owner pair for each active packet timer instead of routing the whole family through one shared host pair: active quests get distinct `QuestTimer:<questId>` and `QuestTimerAction:<questId>` modeless windows, matching the client evidence that `CUIQuestTimer::OnCreate` allocates a fresh `CUIQuestTimerAction` alongside each timer owner. Those windows stay resident utility owners, delegate draw/input to the packet-field timer runtime per quest, and are retired when the timer expires or is removed. WZ-backed timing art follows `Quest/TimeQuest/TimeBar/*`, the client-shaped `SetScreenMode` horizontal shift (`224` px) and `500 ms` tooltip refresh remain intact, and the tooltip composition follows the client structure of `quest name + newline + remain-time line`. The remain-time formatter seam now also carries the exact client `StringPool::ms_aString` payloads recovered from MapleStory.exe v95 through the `StringPool::GetString` decode path: `0x1014 = "Time Left %d:%d:%d"` and `0x18BD = "Time Remaining %d:%d:%d"`. `QuestTimerOwnerStringPoolText` formats both owner strings directly from explicit hour/minute/second components, matching `CUIQuestTimer::GetRemainTimeString`, and focused `UnitTest_MapSimulator` coverage now locks the recovered literals plus the client-shaped formatting behavior in place. | Without an explicit row for the dedicated timer UI owners, the backlog overstates quest utility parity by counting packet timers while skipping the actual client-owned modeless layer that exposes tooltip, screen-mode, and per-timer action behavior. Tracking it separately keeps timer UI work anchored to the real owner family instead of leaving it buried inside the broader `OnQuestResult` and field-state packet rows. | timed quest utility owner (`CUIQuestTimer::OnCreate`, `CUIQuestTimer::Draw`, `CUIQuestTimer::Update`, `CUIQuestTimer::SetVisible`, `CUIQuestTimer::SetScreenMode`, `CUIQuestTimerAction`) |
| Partial | Quest reward / raise-window parity | `HaCreator/MapSimulator` now has a dedicated raise-window seam instead of only folding quest reward choice into the quest log panes: `MapSimulator.QuestRewardRaiseParity.cs` owns the active raise prompt lifecycle, the `QuestRewardRaise` manager window is registered through `UIWindowLoader`, and the window now uses the client `UI/UIWindow*.img/raise` shell more directly rather than a generic utility dialog. The loader pulls the dedicated `raise/backgrnd` frame family, the `raise/29/0` piece-preview art, and the `raise/30` / `raise/31` button skins into `QuestRewardRaiseWindow`; the window keeps its own confirm/cancel routing, overlays owner-local button labels, restricts dragging to the title band in line with the client `HitTest` split around the top `20` pixels, and updates selection/preview state inside the standalone raise surface instead of treating it as plain list text. This run extended that seam into the recovered piece-window path: the active raise state now carries owner-item, max-drop, window-mode, and QR snapshot data from `QuestRewardRaiseOwnerContext`; `QuestRewardRaiseWindow` has a dedicated piece-placement mode that shows local owner metadata inside the raise shell, accepts inventory drops over the raise preview/list surface, tracks placed-piece rows with per-request ids, and lets the user release queued rows without cancelling the whole owner; and `UIWindowManager` now routes inventory drags into the raise owner before equipment handling so the raise window can lock source slots through the existing pending-request inventory seam and commit or release them on confirm/cancel instead of ignoring `CUIRaiseWnd` / `CUIRaisePieceWnd`-style local placement entirely. Client evidence still keeps the row `Partial`: `CUIRaiseWndBase::OnButtonClicked` at `0x8391d0` confirms destroy-on-cancel through `CUIRaiseManager`, but the simulator still does not model a real packet-owned `CUIRaiseManager`, does not emit or reconcile the true outbound `PutItem` packet family (`283/285/286`) beyond local request-id simulation, and does not yet have an upstream quest/runtime producer that fills authoritative QR-data and piece-owner lifecycle state the way the client owner does. | Quest progression parity is broader than quest lists, alarms, and result text. Tracking this row as `Partial` keeps the dedicated raise shell, preview, button lifecycle, and now the local piece-placement/drop-lock flow attached to the correct owner seam while making the remaining gap explicitly about the true packet-owned manager/network lifecycle and authoritative QR/piece-owner state instead of about missing standalone raise-window behavior. | quest reward/selection dialog layer (`CUIRaiseManager`, `CUIRaiseWndBase`, `CUIRaiseWnd`, `CUIRaisePieceWnd`, `CUIRaiseWndBase::OnButtonClicked`) |
| Missing | Revive confirmation owner parity | Follow-up IDA lookups now pin the full revive-owner lifecycle rather than only the create seam: `CUIRevive::OnCreate` at `0x83cea0`, `CUIRevive::OnButtonClicked` at `0x83ce40`, `CUIRevive::Update` at `0x83ce70`, and `CUIRevive::Revive` at `0x83cde0` confirm that death recovery is mediated through a dedicated utility owner instead of only raw respawn state. The simulator already supports death and respawn at the gameplay layer, but a repo search still does not surface a dedicated revive window, modal owner, or packet/UI seam matching this client owner. | Respawn parity is not only about whether the player can come back. The client puts recovery choice behind a named owner with its own create/update/button lifecycle, so tracking that seam explicitly prevents death/revive parity from being overstated once the movement and respawn runtime works. | revive-choice utility owner (`CUIRevive::OnCreate`, `CUIRevive::OnButtonClicked`, `CUIRevive::Update`, `CUIRevive::Revive`) |

### 16. Packet-owned NPC shop, store-bank, and battle-record owners discovered by follow-up `CField::OnPacket` decompile

The latest `CField::OnPacket` decompile surfaced another utility-owner family that is still missing from this backlog: the client dispatches dedicated NPC shop, store-bank, and battle-record packet families through their own managers instead of folding them into the broader utility rows.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned NPC shop, store-bank, and battle-record parity | `HaCreator/MapSimulator` now has explicit packet-owned `NpcShop`, `StoreBank`, and `BattleRecord` windows backed by the WZ-owned `UIWindow2.img/Shop`, `UIWindow2.img/StoreBank`, and `UIWindow2.img/BattleRecord` seams, plus dedicated runtimes and a focused `/npcutility` command surface for `364`/`365`, `369`/`370`, and `420` through `423`. The simulator continues to decode the concrete `CShopDlg::SetShopDlg` header and item rows that IDA shows on packet `364`, and the row-owned buy/sell/recharge commands now do both parts of the client path together: they still apply the local inventory/meso mutation, and they now also mirror the recovered `CShopDlg::{SendBuyRequest,SendSellRequest,SendRechargeRequest}` outbound payloads (`opcode 66`, modes `0/1/2`) through the existing local-utility live bridge / outbox / deferred queue seam while preserving the last mirrored payload in owner status/footer output. `CStoreBankDlg` still honors the client-confirmed subtype `36` `(passingDay, fee)` get-all modal, but the accept path now also mirrors `CStoreBankDlg::SendGetAllRequest` as outbound `opcode 69`, mode `27`, and the packet-owned store-bank snapshot no longer stops at item and cash serial markers: the staged per-row previews now surface more of the decoded `GW_ItemSlotBase` body for equip, bundle, and pet rows (equip stat/scroll/options/socket summaries, recharge serial markers, pet level/fullness/life/fatigue metadata) instead of treating the row body as an opaque partial blob. Battle-record handling remains corrected to the direct v95 `CBattleRecordMan::OnPacket` decompile: only `421` and `422` mutate manager state, while `420` and `423` are routed to the owner family but ignored by `OnPacket` itself. The remaining gap is narrower again: `CStoreBankDlg` still does not hold a fully client-faithful `GW_ItemSlotBase` row object with every field and slot-behavior branch that the native dialog owns, and the manager paths that arm/open battle-record state outside `421`/`422` are still not fully decompiled. | This row now covers the right owner family end-to-end enough that future work can stay in the client seams that actually own it: WZ-backed window ownership is explicit, direct packet application is in place, shop/store-bank commands now mirror the recovered outbound packet writes instead of stopping at local simulation, and store-bank rows expose much more of the native item-body metadata before the remaining deeper row-model and battle-record-manager gaps. Keeping it `Partial` makes the unresolved native row object and battle-record arming paths explicit instead of hiding them behind the now-correct owner discovery and outbound request coverage. | packet-owned NPC utility layer (`CField::OnPacket`, `CShopDlg::OnPacket`, `CStoreBankDlg::OnPacket`, `CBattleRecordMan::OnPacket`) |

### 17. Additional packet-owned local reward and helper-result parity discovered by direct `CUserLocal::OnPacket` scan

The latest `CUserLocal::OnPacket` re-check exposed another local-owner family that is still missing from this backlog: the client has dedicated result handlers for mesos-give/mesobag reward responses and a pair of quest/social helper responses instead of collapsing all of that feedback into generic chat, pickup notices, or nearby utility windows. The dedicated maker-result seam is already tracked in the earlier item-maker row and does not need to be duplicated here.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned mesos-give and random-mesobag result parity | `LocalUtilityPacketInboxManager` now routes the dedicated local packet ids and aliases for `CUserLocal::OnMesoGive_Succeeded` (`236` / `0x90f950`), `CUserLocal::OnMesoGive_Failed` (`237` / `0x90d530`), `CUserLocal::OnRandomMesobag_Succeeded` (`238` / `0x90fa30`), and `CUserLocal::OnRandomMesobag_Failed` (`239` / `0x90d560`) into `MapSimulator.PacketOwnedUtilityParity`. `PacketOwnedRewardResultRuntime` decodes the local reward-result payloads, resolves the client string-pool text with simulator fallbacks, and routes the random-mesobag dialog/button/sound resource ids through the same string-pool entries the client constructor uses (`0x17A9`-`0x17AD`, `0x17AF`-`0x17B4`, `0x17B6`-`0x17B9`). The dedicated packet-owned reward notice owner for the meso-give success/failure and random-mesobag failure paths now drops the extra simulator title bar text, centers the notice body closer to `CUtilDlg::Notice`, recenters the `BtOK` button against the actual `UtilDlgEx/notice` frame width, and anchors the close button to the same top-right chrome slot instead of leaving the `UtilDlgEx/BtClose` branch at its unplaced default. The dedicated `RandomMesoBag` owner still follows the recovered `CUIRandomMesoBag` control and text coordinates (`BtOk` at `204,77`, receive text at `78,16`, right-aligned meso amount at `195,34`), mirrors the client-observed status-bar chat line plus rank-owned sound cue, and the loader now resolves the recovered `UI/UIWindow.img/RandomMesoBag/*` string-pool resource paths against either loaded `UIWindow` image before falling back to `UIWindow2.img/UtilDlgEx/notice`, so datasets that surface the authored branch under `UIWindow2.img` no longer get skipped. The remaining gap is now mostly authored-asset and deep util-dialog behavior fidelity: the active WZ export still does not contain the authored `RandomMesoBag` canvases, so the simulator cannot yet match the exact per-rank art branches byte-for-byte, and the `CUtilDlg::Notice` owner still approximates the full `CUtilDlg::SetUtilDlg` auto-separation and tight-line branches rather than reproducing every separator and layout path. | These handlers sit on the visible reward-feedback path between local gameplay and the utility/HUD owners already documented here. Tracking the dedicated owner/runtime path prevents money-result coverage from being overstated by generic pickup or inventory feedback, while the remaining note now stays anchored to the still-missing authored art and deeper `SetUtilDlg` line-separation behavior instead of to packet routing, text ids, or owner-local control placement. | packet-owned local reward-result layer (`CUserLocal::OnPacket`, `CUserLocal::OnMesoGive_Succeeded`, `CUserLocal::OnMesoGive_Failed`, `CUserLocal::OnRandomMesobag_Succeeded`, `CUserLocal::OnRandomMesobag_Failed`, `PacketOwnedRewardResultRuntime`, packet-owned reward notice owner, `RandomMesoBag` owner) |
| Implemented | Packet-owned quest-resign and mate-name helper parity | `LocalUtilityPacketInboxManager` now maps the local helper-result aliases and packet ids (`259`/`260`), while `MapSimulator.PacketOwnedUtilityParity` applies the same client-observed payloads as `CUserLocal::OnResignQuestReturn` (`0x905720`) and `CUserLocal::OnPassMateName` (`0x918260`): resign-quest re-arms the auto-start registration and removes any matching Quest Alarm entry, and pass-mate-name stores or clears the quest mate-name string used by the quest runtime/detail UI. `UnitTest_MapSimulator/PacketOwnedQuestHelperParityTests.cs` now covers the alias routing and both packet payload handlers so this helper family is no longer only implied by nearby quest/social rows. | Tracking these helper-result owners prevents the utility backlog from silently dropping smaller packet-fed client flows that sit between quest/social UI and ordinary field feedback. Keeping them explicit also avoids misfiling them under generic chat/notice rows later. | packet-owned local helper-response layer (`CUserLocal::OnPacket`, `CUserLocal::OnResignQuestReturn`, `CUserLocal::OnPassMateName`) |

### 18. Additional packet-owned logout-gift owner parity discovered by direct `CField::OnPacket` decompile

The latest direct `CField::OnPacket` decompile surfaced one more utility owner that is still untracked in this backlog: packet type `432` routes to `CWvsContext::OnLogoutGift`, and that context handler updates a dedicated `CUILogoutGift` owner when present.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned logout-gift owner parity | MapSimulator now registers a dedicated `LogoutGiftWindow` owner, caches the three logout-gift commodity serial numbers from the client-confirmed trailing 12-byte logout-gift block on character-data `SetField` even when larger mixed tails are present, preserves any preceding adjacent bytes as exact raw tail state plus aligned `int32` views, and routes packet `432` through a dedicated `CWvsContext::OnLogoutGift`-style dispatcher branch that refreshes and surfaces that owner instead of leaving logout-gift state buried in stage-transition helper bytes. The simulator owner also now follows the client-observed three-slot geometry from `CUILogoutGift::{OnCreate,Draw}`, mirrors `CUILogoutGift::OnButtonClicked` by recording the client opcode `313` slot request when a gift slot is selected, and maps any aligned leading tail `int32` values onto the exact contiguous `CWvsContext` precursor dword slots immediately before the client-read commodity cache at `dword[4138..4140]`, exposing them as `CWvsContext dword[...] @ 0x...` while still preserving the raw bytes. It still attempts to consume authored `UI/UIWindow.img/LogoutGift/backgrnd` art when a dataset actually exposes that node. The remaining gap is now mostly semantic ownership plus missing extracted art: the lead-in bytes are no longer anonymous, but their exact client field names and any non-logout-gift behavior tied to those precursor `CWvsContext` slots are still unknown, and the active extracted UI dataset still does not expose a recoverable `LogoutGift` node even though the generated string-pool references `UI/UIWindow.img/LogoutGift/backgrnd`. | Without an explicit row, utility parity can look complete once the broad `CUserLocal` dispatcher and nearby reward windows are covered even though the client still has a separate packet-owned logout-gift owner path. The row now stays anchored to the actual client dispatch and owner lifecycle while clearly isolating the remaining work to semantic ownership of the precursor context slots and missing authored UI assets instead of to packet routing, mixed-tail splitting, or owner absence. | packet-owned logout-gift utility owner (`CField::OnPacket`, `CWvsContext::OnLogoutGift`, `CUILogoutGift::OnCreate`, `CUILogoutGift::Draw`) |

### 19. Additional Vega enhancement owner discovered by targeted IDA scan

The targeted IDA sweep surfaced one more utility-window owner that was not represented anywhere in this backlog set: the dedicated Vega enhancement dialog. This owner sits next to the existing item-upgrade flow, but it is not just a simulator-only convenience wrapper around scrolling.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Vega enhancement dialog parity | IDA now confirms the client keeps Vega on its own owner path instead of folding it entirely into `CUIItemUpgrade`: `CUIVega::OnCreate` (`0x7c1320`) builds a dedicated dialog, `CUIVega::Draw` (`0x7c1dd0`) owns the specialized frame/effect presentation, and `CUIVega::OnButtonClicked` (`0x7bf4a0`) validates the selected equip/scroll pair, checks the same exclusive-request gate the client stores on `CUserLocal`, prompts through `CUtilDlg::Notice`/`YesNo` for the same missing-selection and white-scroll cases, encodes the Vega request packet fields, and triggers `CAnimationDisplayer::Effect_Vega` after send. A focused source pass shows the simulator already has real owner coverage on disk even though the backlog never named it: `VegaSpellUI` is registered through `UIWindowLoader` from `UIWindow.img/VegaSpell`, loads the dedicated `backgrnd10`, `backgrnd60`, `SuccessWnd`, `FailWnd`, `Count`, and effect-frame assets, routes item-consumable launch through `MapSimulator.OpenVegaSpellWindowForConsumable`, and drives a dedicated cast/result animation path against the existing `ItemUpgradeUI` backend instead of leaving Vega as a tooltip-only modifier. The remaining gap is narrower but still real: the simulator still enters Vega from local consumable use rather than a recovered packet-owned launch path, does not mirror the client's exact notice-string ids or exclusive-request bookkeeping byte-for-byte, and still approximates the final send/result choreography through the local upgrade backend rather than a dedicated client-faithful Vega packet/result lifecycle. | Without an explicit row, the backlog overstates item-enhancement parity by treating Vega as if it were already covered once scrolling and item-upgrade windows exist. The client gives Vega its own dialog, validation, notice flow, send path, and effect owner, and the simulator already has a corresponding window/runtime seam, so tracking it explicitly keeps future work anchored to the real client class instead of leaving the feature buried inside generic upgrade notes. | dedicated enhancement utility owner (`CUIVega::OnCreate`, `CUIVega::Draw`, `CUIVega::OnButtonClicked`, `CAnimationDisplayer::Effect_Vega`, simulator seams: `HaCreator/MapSimulator/UI/Windows/VegaSpellUI.cs`, `HaCreator/MapSimulator/Loaders/UIWindowLoader.cs`, `HaCreator/MapSimulator/MapSimulator.cs`, `HaCreator/MapSimulator/UI/Windows/ItemUpgradeUI.cs`) |

### 20. Shared production and enhancement animation-owner parity discovered by targeted `CAnimationDisplayer` scan

The latest IDA scan exposed another owner family that the backlog was still flattening into the maker, upgrade, and Vega window rows: the client plays several production and enhancement result effects through shared `CAnimationDisplayer` helpers rather than leaving all result presentation inside `CUIItemMaker`, `CUIItemUpgrade`, or `CUIVega`.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Shared item-production and enhancement effect parity | IDA now confirms that multiple progression result surfaces are owned by `CAnimationDisplayer` instead of by the window classes alone. `Effect_ItemMake` (`0x456f80`) chooses one of two `Effect/BasicEff` UOLs from the result flag, loads that layer at the item-maker origin/overlay seam, animates it once, and registers it through the shared one-time animation owner. `Effect_ItemUpgrade` (`0x4567f0`) is broader: it resolves success/failure effect names through StringPool ids `0xD91` and `0xD92`, formats layer UOLs through the same shared StringPool template id `986`, inserts `Effect/BasicEff.img/EmptyCanvas/0` for `3120` ms when the client is in the enchant-skill branch, and then registers the finished layer as a one-time animation rather than keeping all timing in `CUIItemUpgrade`. `Effect_ViciousHammer` (`0x457210`) splits the hammer flow into a repeat-owned in-progress branch and a one-time finished branch with distinct layer origins, and `Effect_SkillBookUsed` (`0x4581b0`) composes separate front/back success or failure layers via StringPool ids `0xFF1` through `0xFF4` before registering those one-shot overlays. A focused source pass shows the simulator already has meaningful local coverage on disk, but it is still window-local: `ItemMakerUI` owns its gauge/result presentation, `ItemUpgradeUI` owns theme-specific cast/result playback including cube and hammer themes, and `VegaSpellUI` owns its dedicated cast/result animation sequence. The remaining gap is that these local window presentations are still not tracked or factored as one shared `CAnimationDisplayer`-shaped owner, and packet-owned or non-window callers such as hammer or skill-book result effects still do not have an explicit backlog row or simulator seam tied back to the client animation manager. | If this owner stays implicit, progression parity can look more complete than it is once the maker, upgrade, and Vega windows animate locally. The client still keeps a separate shared animation manager for result effects, delay insertion, and one-time versus repeat registration, so tracking that owner explicitly keeps future work anchored to the real effect-composition seam instead of burying it inside individual utility-window rows. | shared production/enhancement animation layer (`CAnimationDisplayer::Effect_ItemMake`, `CAnimationDisplayer::Effect_ItemUpgrade`, `CAnimationDisplayer::Effect_ViciousHammer`, `CAnimationDisplayer::Effect_Vega`, `CAnimationDisplayer::Effect_SkillBookUsed`, simulator seams: `HaCreator/MapSimulator/UI/Windows/ItemMakerUI.cs`, `HaCreator/MapSimulator/UI/Windows/ItemUpgradeUI.cs`, `HaCreator/MapSimulator/UI/Windows/VegaSpellUI.cs`) |

## Priority Order

If the goal is visible parity first, the next work in this area should be sequenced like this:

1. Window usefulness pass:
   Finish minimap, inventory, equip, and quest-log interactions so the existing windows become usable client-facing tools rather than frame shells.
2. Progression correctness pass:
   Tighten ability/stat formulas, equip presentation, and inventory-backed validations so the windows reflect the runtime accurately.
3. Packet-dispatch pass:
   Add the `CUserLocal::OnPacket`-style bridge for packet-owned local utility/event handlers before layering more one-off launches onto chat commands or button callbacks.
4. Local result-owner pass:
   Add the still-missing mesos-give/mesobag result and adjacent quest/helper response owners so production/reward follow-through does not stay hidden behind generic notices once the broad dispatcher exists.
5. Packet-overlay pass:
   Add the packet-owned fade, balloon, damage-meter, field-hazard, quest-guide, and delivery/class-competition surfaces after the base dispatcher exists so those handlers do not stay hidden behind one catch-all row.
6. External cash-service ownership pass:
   Add dedicated Cash Shop and ITC stage owners, and route the in-map `CAdminShopDlg::OnPacket` `366`/`367` lifecycle before treating cash-service parity as an extension of `CAdminShopDlg` shell drawing, `OnGoToCommoditySN`, or the local utility bridge.
7. Cash-service subwindow pass:
   Add the avatar-preview, locker, inventory, list, status, One-a-Day, and cash-trading child owners after the base service stages exist so cash-item preview and service-pane behavior do not stay folded into one coarse stage row.
8. Cash-service wishlist pass:
   Add the dedicated `CUIAdminShopWishList`-style owner after the base service stages and child panes exist so wish-list search/toggle behavior does not stay buried inside the coarse admin-shop row.
9. Radio-manager pass:
   Add the packet-owned `OnRadioSchedule` bridge plus a real `CRadioManager`-style audio/UI/chat owner before treating radio playback as another event-sound alias inside the local utility dispatcher.
10. Tutor/helper pass:
   Add the packet-owned tutor actor plus the `OnOpenSkillGuide` / `OnAskAPSPEvent` helper-owner behavior before broader tutorial/guidance flows continue to rely on generic notices or manual window opens.
11. Keymap-init pass:
   Add the packet-owned `CFuncKeyMappedMan` bridge for function-key hydration and pet auto-consume defaults before treating key config, quick-slot state, or auto-potion behavior as fully client-owned.
12. Anti-macro packet/result pass:
   Add the `CWvsContext::OnAntiMacroResult` bridge, screenshot/notice side effects, combo-hold lifecycle, and admin-vs-normal owner split before treating lie-detector parity as covered by an owner shell alone.
13. Anti-macro owner pass:
   Add the dedicated `CUIAntiMacro`-style timed text-entry owner after the packet/result bridge exists so the launch and timeout flow does not stay inferred from item bans or generic notice text.
14. Utility-owner refinement pass:
   Tighten `CUIKeyConfig`, `CUIGameOpt`, and `CSoftKeyboardDlg` lifecycle parity after the broad utility buttons work so key-binding, option, and soft-keyboard behavior does not stay hidden inside the generic cursor/utility row.
15. Macro-owner refinement pass:
   Tighten `CUIMacroSysEx` draw/button-owner parity after the macro surface and shared text-entry seams exist so macro behavior does not stay documented only as input polish.
16. Utility feedback pass:
   Complete pickup notices, soft-keyboard or constrained text-entry behavior, macro behavior, cursor states, and status-bar utility-button behavior.
17. Packet-owned NPC utility pass:
   Add `CShopDlg`, `CStoreBankDlg`, and `CBattleRecordMan` packet bridges before treating NPC shop/storage-adjacent utility parity as covered by nearby trunk, NPC, or generic utility rows.
18. Logout-gift owner pass:
   Add packet `432` routing through `CWvsContext::OnLogoutGift` and a dedicated `CUILogoutGift`-style owner/runtime instead of leaving logout-gift handling as hidden stage-payload bytes.
19. Production/effect animation pass:
   Split maker, upgrade, hammer, skill-book, and Vega result-effect ownership out from the window rows before more cast-result behavior drifts into window-local implementations only.
20. Owner-consolidation pass:
   Refresh older umbrella rows so minimap, macro, inventory, equip, and quest utility work stays anchored to the explicit client owners already captured here instead of drifting back into stale generic-window buckets.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".



