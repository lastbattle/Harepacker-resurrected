# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/UI/MinimapUI.cs`
- `HaCreator/MapSimulator/UI/MouseCursorItem.cs`
- `HaCreator/MapSimulator/UI/PickupNoticeUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AbilityUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AdminShopDialogUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AbilityUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/EquipUI.cs`
- `HaCreator/MapSimulator/UI/Windows/EquipUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/InventoryUI.cs`
- `HaCreator/MapSimulator/UI/Windows/InventoryUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/QuestUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuestUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillMacroUI.cs`
- `HaCreator/MapSimulator/UI/Windows/WorldMapUI.cs`

### Client references checked

- `CUIStat::Draw` (captured in simulator layout comments and offsets)
- `CUIStat::DrawTextA` (captured in simulator layout comments and offsets)
- `CUISkillMacro` (captured in simulator window comments)
- `CWvsContext::OnDropPickUpMessage` (captured in `PickupNoticeUI` comments)
- `CUIScreenMsg::ScrMsg_Add` (captured in `PickupNoticeUI` comments)
- `CUIItem::Draw` at `0x7ccf50`
- `CAdminShopDlg::OnCreate` at `0x42ecd0`
- `CAdminShopDlg::Draw` at `0x42fdf0`
- `CAdminShopDlg::OnButtonClicked` at `0x430f90`
- `CUIChannelSelect::OnCreate` at `0x606a10`
- `CUIChannelSelect::Draw` at `0x6056d0`
- `CUIChannelSelect::OnButtonClicked` at `0x6035f0`
- `CUIWorldSelect::OnCreate` at `0x60b660`
- `CUIWorldSelect::OnButtonClicked` at `0x6025e0`
- `CUIChannelShift::OnCreate` at `0x96c160`
- `CUIChannelShift::Draw` at `0x96ccb0`
- `CUIItemMaker::Draw` at `0x7d4980`
- `CUIItemMaker::OnButtonClicked` at `0x7d5dc0`
- `CUIItemUpgrade::Draw` at `0x7c0100`
- `CUIItemUpgrade::OnButtonClicked` at `0x7c0ca0`
- `CUIMapTransfer::Draw` at `0x7df5c0`
- `CUIMapTransfer::OnButtonClicked` at `0x7e0ed0`
- `CUIEquip::Draw` at `0x7aa560`
- `CUIQuestInfo::Draw` at `0x82a780`
- `CUIQuestInfoDetail::Draw` at `0x8248c0`
- `CUIQuestInfoDetail::OnButtonClicked` at `0x834c90`
- `CUIQuestAlarm::Draw` at `0x82d850`
- `CUIStatusBar::OnButtonClicked` at `0x880540`
- `CUIStat::Draw` at `0x864bd0`
- `CUIUserInfo::Draw` at `0x8b3a50`
- `CUIUserInfo::OnButtonClicked` at `0x8b6d80`

Notes:
Direct IDA seam capture is now confirmed for inventory, cash-service, channel-shift, item-maker, item-upgrade, map-transfer, equipment, quest-log, quest-detail, quest-alarm, status-bar utility clicks, stat-window, and user-info owners. The remaining seam-collection gap in this backlog area is primarily the minimap window and any exact `CUISkillMacro` draw or interaction owners, not the broader progression-window family as a whole.

## Client Function Index By Backlog Area

The list below maps this backlog area to the concrete client seams that are already named in code or prior reverse-engineering notes.
These are the first functions or classes to inspect before changing simulator behavior.

### 1. Progression and utility window parity

- `CUIStat::Draw`
- `CUIStat::DrawTextA`
- `CUIItem::Draw` at `0x7ccf50`
- `CAdminShopDlg::OnCreate` at `0x42ecd0`
- `CAdminShopDlg::Draw` at `0x42fdf0`
- `CAdminShopDlg::OnButtonClicked` at `0x430f90`
- `CUIChannelSelect::OnCreate` at `0x606a10`
- `CUIChannelSelect::Draw` at `0x6056d0`
- `CUIChannelSelect::OnButtonClicked` at `0x6035f0`
- `CUIWorldSelect::OnCreate` at `0x60b660`
- `CUIWorldSelect::OnButtonClicked` at `0x6025e0`
- `CUIChannelShift::OnCreate` at `0x96c160`
- `CUIChannelShift::Draw` at `0x96ccb0`
- `CUIItemMaker::Draw` at `0x7d4980`
- `CUIItemMaker::OnButtonClicked` at `0x7d5dc0`
- `CUIItemUpgrade::Draw` at `0x7c0100`
- `CUIItemUpgrade::OnButtonClicked` at `0x7c0ca0`
- `CUIMapTransfer::Draw` at `0x7df5c0`
- `CUIMapTransfer::OnButtonClicked` at `0x7e0ed0`
- `CUIEquip::Draw` at `0x7aa560`
- `CUIQuestInfo::Draw` at `0x82a780`
- `CUIQuestInfoDetail::Draw` at `0x8248c0`
- `CUIQuestInfoDetail::OnButtonClicked` at `0x834c90`
- `CUIQuestAlarm::Draw` at `0x82d850`
- `CUIStatusBar::OnButtonClicked` at `0x880540`
- `CUIUserInfo::Draw` at `0x8b3a50`
- `CUIUserInfo::OnButtonClicked` at `0x8b6d80`
- `CUISkillMacro`
- `CWvsContext::OnDropPickUpMessage`
- `CUIScreenMsg::ScrMsg_Add`

Notes:
`CUIItem::Draw` confirms that the client inventory window owns meso text, per-slot icon and quantity drawing, item-grade frames, disabled-slot overlays, and special overlays such as the active bullet marker. `CAdminShopDlg::OnCreate`, `CAdminShopDlg::Draw`, and `CAdminShopDlg::OnButtonClicked` confirm a dedicated cash-service shop dialog rather than treating cash-shop and MTS access as simple status-bar button stubs. `CUIChannelSelect::OnCreate`, `CUIChannelSelect::Draw`, `CUIChannelSelect::OnButtonClicked`, `CUIWorldSelect::OnCreate`, and `CUIWorldSelect::OnButtonClicked` confirm that the status-bar channel path fans out into explicit world and channel selector owners rather than a generic toggle, while `CUIChannelShift::OnCreate` and `CUIChannelShift::Draw` keep a separate shift or transition surface in play. `CUIItemMaker::Draw` and `CUIItemMaker::OnButtonClicked` confirm a dedicated crafting or maker window rather than burying production flow inside generic inventory handling. `CUIItemUpgrade::Draw` and `CUIItemUpgrade::OnButtonClicked` confirm a separate item-enhancement surface rather than treating scroll or upgrade flow as a generic inventory tooltip action. `CUIMapTransfer::Draw` and `CUIMapTransfer::OnButtonClicked` confirm a dedicated map-transfer window distinct from the minimap shell. `CUIEquip::Draw` confirms that equip-window parity is tied to slot-disable rules, expiration and durability overlays, and job or subjob gating rather than just slot placement. `CUIQuestInfo::Draw`, `CUIQuestInfoDetail::Draw`, and `CUIQuestAlarm::Draw` confirm that quest progression parity includes a separate detail pane with dedicated button handling, category-button rebuilds, scroll-driven list rebuilding, quest-delivery requirement rendering, condition-state coloring, and a standalone quest-alert window rather than only the quest log shell. `CUIStatusBar::OnButtonClicked` confirms the utility-button cluster is a real interaction owner instead of only decorative art. `CUIUserInfo::Draw` and `CUIUserInfo::OnButtonClicked` confirm a separate character-information or profile surface beyond the stat window itself.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.
- Progression or utility window groundwork now has a shared seam: named owners for world map, map transfer, cash-service, channel-selection, crafting, enhancement, character-info, and quest-side utility windows are registered with `UIWindowManager`, while minimap and status-bar utility buttons now route into those owners instead of staying entirely dead-ended. The world/channel path now opens dedicated simulator-side `WorldSelect`, `ChannelSelect`, and timed `ChannelShift` surfaces backed by `UIWindow2.img/Channel` art, with the channel grid, centered placement, per-channel load feedback, login-step request gating, delayed `CheckUserLimitResult` / `SelectWorldResult` style request flow, and simulator-side `WorldInformation` / `RecommendWorldMessage` / `LatestConnectedWorld` metadata overlays now aligned to the client-owned selector layout instead of remaining a placeholder-only launch seam. The map-transfer owner now also keeps saved teleport destinations in a character-scoped destination book keyed to the active player or selected login-roster character instead of sharing one global saved list across every simulator avatar, persists that destination book under the simulator AppData profile across restarts, and hands the currently selected transfer target into the world-map owner when `BtMap` is used so the two navigation surfaces stay linked.

## Remaining Parity Backlog

### 1. Progression and utility window parity

This area is currently absent from the backlog set even though the simulator already has concrete window and feedback implementations.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Minimap window parity | The minimap now loads WZ-backed helper markers for generic NPCs, quest-start NPCs, quest-end NPCs, portals, and out-of-bounds direction arrows from `UIWindow2.img/MiniMapSimpleMode/DefaultHelper`, upgrades the post-Big-Bang generic NPC notice marker plus off-map direction overlays to the animated `UIWindow2.img/MiniMap/iconNpc` and `MiniMap/iconDirection/*` frame families when those client assets are present, feeds visible portal markers plus quest-aware NPC marker families into `MinimapUI`, keeps the post-Big-Bang `BtNpc` list panel from `ListNpc`, and `BtBig` now opens a dedicated `WorldMap` owner whose `WorldMapSearch` side panel is no longer just background art: `BtSearch` switches into a dedicated WZ-backed search surface with `BtAllsearch` return flow, optional `BtLevelMob` mob-only filtering, live field/NPC/mob result rows sourced from the current simulator field, and a working text-entry search path that filters those result families plus global field-name or map-id matches, keeps search-row selection distinct even when multiple rows resolve to the same field, supports keyboard navigation or activation, accepts clipboard paste into the search box, now uses the client `UIWindow2.img/WorldMap/title` canvas instead of a text-only heading, keeps the search caret and visible query text clipped inside the edit box when the query overruns the field, lets mouse clicks reposition the caret inside that visible query span, and adds word-wise `Ctrl+Backspace` / `Ctrl+Delete` editing on top of the existing keyboard path, while `BtAll` / `BtAnother` browsing, current-map focus, paged field selection, and direct map-transfer queuing still run through the existing pending-map seam, but the simulator still does not mirror the broader social/tracking helper families exposed by `DefaultHelper` beyond NPC/quest/portal markers because it lacks client-shaped tracked remote-user field state and positions, and it still falls short of the client's exact IME/edit-control behavior and the remaining presentation polish inside that search owner | Navigation feedback now has animated minimap-owned notice and direction art plus a world-map search/result surface that can actually resolve typed map/NPC/mob queries instead of only showing static seeded rows, and the search shell now behaves more like a real client-owned edit control with WZ title art, clipped query rendering, mouse caret placement, and word-wise deletion, while the remaining gap is narrowed to tracked-user helper families and exact IME or polish fidelity rather than a static search panel | `MinimapUI.cs`, `WorldMapUI.cs`, minimap/window loader path |
| Partial | Map transfer / navigation window parity | The simulator now routes minimap `BtMap` into a dedicated WZ-backed `MapTransfer` owner built from the `Teleport*` window art, constrains the destination surface to the client's five visible rows, caps saved teleport destinations to a client-shaped five-vs-ten slot budget based on the loaded `Teleport` vs `Teleport3` art variant, respects teleport-item field limits separately from generic migration checks, layers return-map plus portal-derived routes on top of the saved list while driving actual map changes through the existing pending-map seam instead of stopping at the minimap shell, keeps saved teleport destinations in a character-scoped destination book keyed to the active player or selected login-roster character instead of one simulator-global list, persists that per-character destination book under the simulator AppData profile across restarts, opens the world-map owner focused on the currently selected transfer target rather than always resetting to the current field, preserves fixed per-character slot indices instead of compacting into a generic list, renders empty saved slots inside the transfer window, keeps the selected saved slot alive through the world-map handoff so a `[Target]` selection can be written back into that exact slot through `BtRegister`, now draws the client-shaped saved-count header instead of treating the list like an unbounded generic scroller, resolves saved-slot labels from map IDs rather than persisting simulator-only custom names, and exposes the client-owned edit-target lane as a local numeric map-id entry path with 12-digit input, focus-driven selection clearing, paste support, and direct `BtMove` routing when no list row is selected, but it still does not mirror the client's full `CUIMapTransfer` behavior surface such as live packet-backed `CharacterData::adwMapTransfer` / `adwMapTransferEx` sourcing, the exact official request/response path behind `CWvsContext::SendMapTransferRequest`, or the stricter unsaveable-destination matrix the client applies beyond the simulator's synthesized return, portal, and manual-id routes | Navigation testing can now exercise a more client-shaped transfer and destination-selection surface above raw portal travel, saved destinations no longer bleed across unrelated simulator characters, the saved-slot header and raw map-id presentation are closer to the official owner, the edit target no longer behaves like dead art, and world-map retargeting now lands in the intended saved slot instead of collapsing back into a generic append path, but the owner still needs live character-data population, packet-authored updates, and the rest of the official destination-validation rules before it fully matches the client flow | navigation/UI layer, minimap integration, destination-selection flow (`CUIMapTransfer::Draw`, `CUIMapTransfer::OnButtonClicked`, `CUIMapTransfer::OnCreate`) |
| Partial | Cash shop / admin shop dialog parity | The simulator now replaces the generic Cash Shop and MTS placeholders with a dedicated `AdminShopDialogUI` owner backed by `UIWindow2.img/Shop`, reuses the shop dialog frame plus `BtBuy` / `BtSell` / `BtExit` / `BtRecharge`, keeps Cash Shop and MTS inside the same button-driven surface, routes the status-bar launchers so only the requested service window stays open, exposes separate scrollable NPC-offer and user-listing panes with per-row selection and request submission messaging, consumes the client `UIWindow2.img/Shop/TabShop` category strip as a real WZ-backed catalog filter, now also consumes the owner `TabBuy` and `TabSell` tab canvases with their client WZ offsets instead of leaving the service and pane strips as text-only chrome, mirrors the client-owned twin scrollbar layout called out by `CAdminShopDlg::OnCreate`, replaces the wish-list footer prompt with a `FadeYesNo`-style confirmation modal before saving a wish-list entry, syncs the displayed balance to the live inventory meso runtime like `CAdminShopDlg::Draw`, now renders WZ-backed item icons plus stack counts for requestable rows in both panes and the detail area so the dialog no longer behaves like a text-only catalog despite `CAdminShopDlg::DrawNPCItem` / `DrawUserItem`, and resolves simulator-side shop requests through a broader local session matrix with meso consumption, inventory-capacity checks, actual item delivery for requestable rows, post-purchase sold-out locking on one-shot listings, rejected outcomes such as seller-offline, expired, inventory-full, and refreshed-sold-out responses, plus real icon carry-through on delivered items instead of treating every non-expansion row as the same generic acknowledgment, but it still does not mirror the full `CAdminShopDlg` behavior surface such as packet-backed shop sessions, live server-populated catalogs, the remaining client tab-control semantics behind the five-entry `TabBuy` / nine-entry `TabSell` owners, or the official server-authored request and trade matrix behind those controls | The simulator can now exercise the client cash-service launch path through a dedicated dialog owner with concrete per-item browsing, WZ-backed shop tab chrome, client-shaped scrollbar and category-tab affordances, visible item art in the browse and detail surfaces, a real wish-list confirmation step, live balance readout, and a more varied local request-result loop instead of a dead-end placeholder, while the remaining gap is narrowed to true packet/live-catalog sourcing, fuller client tab semantics, and the deeper server-authored session rules inside that owner | cash-service UI layer, status-bar integration, shop-session flow (`CAdminShopDlg::OnCreate`, `CAdminShopDlg::Draw`, `CAdminShopDlg::OnButtonClicked`) |
| Partial | World and channel selector parity | The simulator now routes `BtChannel` into dedicated `WorldSelect`, `ChannelSelect`, and timed `ChannelShift` windows, uses `UIWindow2.img/Channel` world badges and channel art for the local selector stack, preserves a visible current world/channel choice for the simulator session, expands the world list to the full badge set, aligns the channel grid and window centering to the client `GetRect`/owner layout, simulates per-world channel availability plus busy/full load bars so invalid targets no longer behave like always-open buttons, mirrors the client-owned request shape more closely by disabling selector input outside the login `WorldSelect` step and resolving world/channel picks through delayed `CheckUserLimitResult` / `SelectWorldResult` style simulator responses instead of committing immediately, lets login-scene `WorldInformation`, `RecommendWorldMessage`, and `LatestConnectedWorld` packets drive selector-side world load data, recommended-world markers, latest-world focus, and immediate UI refresh instead of leaving those packet owners as message-only stubs, carries simulator-side adult-channel gating, denied `CheckUserLimitResult` / `SelectWorldResult` style result messaging, plus low-frequency live population drift across login-scene world metadata so the selector surface no longer behaves like a fixed always-successful snapshot, now also accepts packet-authored worldinfo payloads, recommended-world ordering, latest-world overrides, `CheckUserLimitResult` result or population bytes, and raw `SelectWorldResult` codes through the existing login-packet seam so selector-side world load, busy-warning, and accepted-or-rejected world/channel transitions no longer have to come only from the simulator’s local heuristic defaults, and now mirrors the client-observed `SelectWorldResult` rejection families from `CLogin::OnSelectWorldResult` plus the `CheckUserLimitResult` code-2 over-user-limit path from `CUIWorldSelect::UserLimitResult`, including the grouped login-error branches and the code-7 title return instead of collapsing every non-success packet into one generic rejection, but it still lacks the client’s true live server population stream and any server-authored population updates beyond the simulator’s local drift or manually injected packet overrides | Channel switching is no longer a dead-end HUD affordance, and the remaining gap is now primarily true live server population sourcing rather than missing selector surfaces, stale login-packet stubs, or a generic one-size-fits-all packet rejection path | world-selection and channel-selection UI layer, status-bar integration (`CUIWorldSelect::OnCreate`, `CUIWorldSelect::OnButtonClicked`, `CUIChannelSelect::OnCreate`, `CUIChannelSelect::Draw`, `CUIChannelSelect::OnButtonClicked`, `CUIChannelShift::OnCreate`, `CUIChannelShift::Draw`, `CLogin::OnCheckUserLimitResult`, `CLogin::OnSelectWorldResult`, `CUIWorldSelect::UserLimitResult`) |
| Partial | Inventory window parity | Legacy and Big Bang inventory windows now render right-aligned meso text, per-slot item icons and stack counts, WZ-backed grade and quality markers, disabled-slot overlays, active-slot highlighting, and Big Bang tab or expanded layouts, while the simulator also feeds meso and picked-up items into the inventory surface at runtime, resolves equip pickup icons through `Character/*/*.img` like the client asset families, respects `info/slotMax`-style stack splitting, merge limits, and pickup-capacity checks for stackable items, wires `BtGather` and `BtSort` into simulator-side stack compaction and ordering, shows shared client-style hover tooltips with cached item names, type labels, stack data, and descriptions across both legacy and Big Bang inventory layouts, supports client-shaped intra-inventory drag/drop for occupied-slot swapping, stack merging, trailing-empty-slot moves, plus a held-item cursor overlay in both layouts, and now carries client-shaped slot-unlock progression through per-tab slot limits that grow in 4-slot rows up to a capped 96-slot budget while `BtCashshop` routes directly into the simulator cash-service dialog and its `Extending Equip/Use/Set-Up/Etc Inventory` offers consume live meso balance to unlock additional rows instead of leaving capacity as a fixed 24-slot shell; right-click setup-chair items now route into the existing portable-chair runtime so inventory use can activate or clear owned chairs without falling back to chat commands, WZ-backed chair `info/recoveryHP` / `recoveryMP` values now tick against the seated player on a client-shaped 10-second cadence, and `Consume/*/spec/moveTo` scrolls now consume through the inventory surface to queue either fixed-map warps or `999999999` nearest-town returns through the existing map-transfer seam instead of remaining unsupported right-clicks | The remaining gap is narrowed to broader client-authenticated item-use rules for non-chair setup items, cure or buff or pet-food style consumables, and stricter server-side validation or result messaging beyond the simulator's current HP/MP, town-scroll, and chair-use coverage rather than missing inventory rendering, drag/drop, tooltip, pickup-capacity, or slot-progression behavior | `InventoryUI.cs`, `InventoryUIBigBang.cs`, `UIWindowLoader.cs`, `AdminShopDialogUI.cs`, inventory/runtime integration (`CUIItem::Draw`, `CUIItem::OnMouseButton`) |
| Partial | Item maker / crafting window parity | The simulator now replaces the `ItemMaker` placeholder with a dedicated WZ-backed `Maker` window owner, exposes `BtStart` / `BtCancel` crafting flow, animates the maker gauge during crafting, seeds a starter ore inventory plus meso and catalyst stock, resolves weighted `randomReward` branches, blocks crafts whose fixed or random reward branch cannot fit in inventory, routes NPC talk overlays whose loaded `String/Npc.img` function text advertises item-maker families such as `Item Maker`, `Glove Maker`, `Shoemaker`, and `Toy Maker` into the dedicated crafting owner, and now rebuilds the selector surface around client `CUIItemMaker` rules by deriving the visible `Etc/ItemMake.img` bucket set from the live job (`0` plus the matching `1` / `2` / `4` / `8` / `16` job bucket, or all five for admin-family jobs), regrouping those recipes into client-like category and item selectors instead of the old raw bucket picker, adding a leading `All` selector plus client-shaped aggregate pages for `Gem`, `Monster Crystal`, `Use`, `Setup`, and `Etc`, hiding selector entries that fail the client-side precondition pass for `reqLevel`, `reqSkillLevel`, `reqItem`, `reqEquip`, and `reqQuest` before the detailed craft validation runs, and applying a local hidden-maker-list pass so maker-family NPC launches such as glove, shoe, toy, and generic item-maker flows narrow the visible recipe set instead of only focusing the nearest matching row, but it still lacks the broader profession runtime and deeper `CItemMakerInfo` progression state beyond the simulator's current local craft trait, family heuristics, and shared inventory / quest state | Production and crafting progression now has a real client-backed launch path, client-shaped category and item selection, job-aware recipe exposure, family-focused NPC launch selection, local hidden-list filtering, and tighter precondition plus result-capacity validation instead of a dead-end placeholder, while the remaining gap is the deeper profession-state runtime and official hidden-list progression logic around `CItemMakerInfo` that is not yet simulated | inventory/runtime integration, crafting UI layer (`CUIItemMaker::OnCreate`, `CUIItemMaker::SetComboBox_Category`, `CUIItemMaker::SetComboBox_Item`, `CUIItemMaker::OnButtonClicked`) |
| Partial | Item upgrade / enhancement window parity | The simulator now registers a dedicated `ItemUpgrade` owner instead of a placeholder, loads Gold Hammer or Vicious Hammer style client art from `UIWindow2.img` or `UIWindow.img`, binds the window to the live `CharacterBuild` plus inventory runtime, seeds starter enhancement scroll stock so the loop is immediately exercisable, resolves eligible items from WZ-backed `info/tuc` upgrade-slot data instead of a pure slot-type guess, consumes enhancement scrolls from the inventory surface, tracks per-item remaining upgrade slots and success or fail counts, applies lightweight stat growth back into `CharacterBuild`, routes right-click activation from supported enhancement consumables in the `USE` and `CASH` inventory tabs plus eligible equips in both legacy and Big Bang equipment windows into the dedicated owner while preselecting the clicked consumable or equipped target, now lets NPC interaction overlays whose loaded `String/Npc.img` function text advertises item-upgrade, enhancement, hammer, potential, or cube families open the dedicated owner, broadens the local enhancement loop beyond the baseline normal or advanced scroll pair by seeding and resolving the client `2049300/1/3/4/5/6/7/8/9` enhancement-scroll family including 2-star through 5-star result variants with multi-star success gains and destroy-on-fail handling, now adds local potential-scroll plus Miracle Cube result handling through `2049400/1/6` and the broader WZ-backed cash family `5062000/1/2/3/4/5`, seeds and resolves `2049500/1`, `5534000`, and `5610000/1` so carved seals, Urete's Time Lab, and Vega's Spell consumables are immediately exercisable from inventory, preserves existing revealed potential lines when a carved seal adds an extra line instead of rerolling the whole set, enforces the WZ-described Golden Miracle Cube Rare-through-Unique limit, now routes `5610000/1` into a dedicated `VegaSpell` owner loaded from `UIWindow.img/VegaSpell` instead of keeping Vega inside the shared item-upgrade surface, reuses the client `backgrnd10` / `backgrnd60`, count glyphs, success/fail panes, and `EffectTwinkling` / `EffectSpelling` / `EffectArrow` / `EffectSuccess` / `EffectFail` families for a separate local cast-and-result presentation, and tightens modifier-aware scroll resolution so Vega 10% and 60% consistently pair with the compatible advanced-vs-normal enhancement-scroll families before the backend attempt runs, but it still lacks the broader official server-authored scroll, fragment, and cash-item result matrix that surrounds these items and the deeper exact client-side Vega session rules beyond this local owner-backed presentation | Progression-sensitive item workflows now have a visible dedicated enhancement surface, live inventory consumption, client-shaped inventory, equip, and NPC launch shortcuts, a broader enhancement-scroll result matrix, richer cash-item coverage, preserved seal behavior, and a separate WZ-backed Vega cast/result owner instead of treating Vega as only another modifier inside `ItemUpgrade`, while the remaining gap is narrowed to the fuller server-authored result matrix and exact official Vega session rules that are still not simulated | inventory/runtime integration, enhancement UI layer (`CUIItemUpgrade::Draw`, `CUIItemUpgrade::OnButtonClicked`) |
| Partial | Equipment window parity | Legacy and Big Bang equip windows now share `EquipSlotStateResolver`-driven disable handling for overall-vs-pants conflicts, beginner subjob and two-handed shield gating, Monster Riding slot gating, expired-item overlays, and zero-durability overlays, stay bound to the live `CharacterBuild` used by the avatar preview, and support simulator-side drag or drop between compatible character slots with held-item cursor feedback plus compare-style hover messaging while an equipped item is being moved; the Big Bang owner now also accepts inventory-to-character equips on the live character tab, returns displaced equips back to inventory with their item icons, stops mutating cached loader templates when slot moves retarget cloned `CharacterPart` instances, and covers the broader client-facing slot families already exposed by the window art for pendant1 vs pendant2, pocket, badge, shoulder, android, android heart, medal, and belt-backed character panes, but it still does not mirror the client `CUIEquip::Draw` cash-equipped-versus-base-equipped resolution path or the exact official cash-layer presentation rules behind those slot families | Equipment-heavy testing now has visible disable and break-state feedback on the actual character equipment surface, a client-shaped held-item interaction path, live inventory-to-equip routing on the Big Bang character page, and much broader slot-family coverage for the non-rendered accessory families already present in the client window art, while the remaining gap is narrowed mainly to true cash-slot resolution and the client’s exact cash-layer presentation rules | `EquipUI.cs`, `EquipUIBigBang.cs`, `CharacterBuild`, `CharacterLoader`, `CharacterAssembler`, `EquipSlotStateResolver.cs` (`CUIEquip::Draw`) |
| Partial | Pet, dragon, and android equipment tabs | The Big Bang equip window now loads the WZ-backed companion pane art for `pet`, `dragon`, `mechanic`, and `Android`, keeps the character equipment surface visible while those panes are open, highlights the active tab buttons, populates the Multi Pet pane from the live `PetController` with per-pet icons plus auto-loot markers and tooltips, seeds dedicated dragon and android companion equipment runtimes so those pages render live equipped-item icons and tooltips on the client slot geometry, now also seeds a dedicated mechanic equipment runtime from the client `Character/Mechanic` asset family so the mechanic page draws live `ENGINE`, `FRAME`, `TRANS`, `ARM`, and `LEG` icons with slot-specific tooltips instead of stopping at an empty-state stub, and now accepts drag/drop from the live equip inventory into the pet, dragon, mechanic, and android panes with slot-family validation, client-shaped android overall-vs-pants gating, occupied-slot replacement back into inventory, a WZ-backed `Character/PetEquip` loader for live pet accessory icons and tooltips, seeded starter pet accessory inventory entries, and drag-back-out handoff from every companion pane into the live inventory window; pet slots can now accept simulator-side pet accessories, render the equipped accessory in-slot with pet-aware tooltip context, and swap that accessory across active pets instead of remaining a pet-icon-only stub, but the simulator still does not enforce the deeper client-side ownership, pet-compatibility, cash/equip-state, or packet-authored validation rules that surround official companion inventories | Companion tab clicks are no longer dead-end toggles, the pet page now participates in the same inventory-backed equip loop as the other companion panes, and equipped companion items can be dragged back into inventory instead of getting stranded in-place, while the remaining gap is narrowed to the client's deeper validation and ownership semantics rather than missing pet equip handling, a blank mechanic page, or one-way drag/drop | `EquipUIBigBang.cs`, `UIWindowLoader.cs`, `PetLoader.cs`, `PetController.cs`, `CompanionEquipmentController.cs`, inventory/window manager integration (`CUIEquip::Draw`) |
| Partial | Ability and stat window parity | The simulator already draws both legacy and Big Bang stat windows, supports AP assignment, job-biased auto-assign, detail-panel toggles, build-backed guild/fame/EXP text, hands/critical detail values, per-stat AP cap enablement, and Big Bang open-vs-close detail button visibility; the stat rows now also resolve equipped stat bonuses through `CharacterBuild`, the detail pane now derives attack/defense, accuracy/avoidability, hands, critical, speed, and jump from live build totals instead of static placeholder fields, `SkillManager` now seeds non-hidden passive books alongside active skills for the current job path, and `CharacterBuild` now folds passive plus live buff attack/defense, magic attack/defense, accuracy, avoidability, critical-rate, speed, and jump bonuses into those totals so the stat windows react to learned skills rather than gear alone, while Big Bang HP/MP AP clicks still use class-aware gain ranges with a 30,000 cap instead of a flat `+20`; the displayed physical and magical damage rows now also use a client-shaped min/max average where mastery scales the primary term instead of naively scaling the full max value, dagger formulas now split thief-vs-non-thief primary stats instead of treating every dagger as a thief weapon, extended Cygnus/hero/Resistance job branches now map back onto their base warrior or mage or archer or thief or pirate stat archetypes for accuracy, avoidability, defense, and Big Bang HP/MP AP growth, `CharacterBuild` now recognizes cane, dual bowgun, and cannon weapon families in the displayed damage surface, and `SkillLoader` plus `SkillManager` now use `String/Skill.img` mastery names to match passive mastery against the equipped weapon family instead of blindly taking the highest passive mastery on the job path, but the simulator still does not mirror every exact post-Big-Bang coefficient or conditional mastery state for the newer families and other per-job edge cases | Visible stat windows now expose runtime-backed totals from equipped gear, learned passive skills, active buff bonuses, class-sensitive HP/MP AP growth across both classic and extended job branches, and a broader client-shaped mastery-aware damage surface for cane, dual-bowgun, and cannon users instead of only the older formula set, but progression-sensitive calculations still diverge for some exact post-Big-Bang coefficients, conditional mastery states, and the remaining long-tail per-job formula matrix | `AbilityUI.cs`, `AbilityUIBigBang.cs`, `CharacterBuild`, `SkillManager`, `SkillLoader`, stat/runtime layer (`CUIStat::Draw`, `CUIStat::DrawTextA`) |
| Partial | Character info / profile window parity | The simulator now loads a dedicated WZ-backed `UserInfo` window from `UIWindow.img/UserInfo` or `UIWindow2.img/UserInfo/character`, routes the status-bar `BtCharacter` button into that owner, keeps the primary profile page build-backed with name/job/level plus rank or guild or alliance fields, and the Big Bang owner now switches into WZ-backed ride, pet, collect, and personality subpages that render simulator-side mount state, active pet data, collection summaries, and optional trait values instead of leaving those buttons decorative. The owner now also loads the page-specific `BtException`, `BtArrayName`, and `BtArrayGet` controls from `UIWindow2.img/UserInfo`, hides primary action buttons outside the character page so the per-page button surface matches the selected subpage more closely, forwards `BtParty` into the existing social-list owner instead of leaving it as a pure message stub, and opens the WZ-backed `exception` popup with local register/delete plus meso-filter toggles instead of leaving that client-owned control path dead, but it still lacks the deeper `CUIUserInfo` interaction surface such as real trade submission, family-chart flow, richer packet-backed ranking or collection metadata, and the exact server-authored workflows behind those buttons | Character-facing information now has its own launch path, a broader button-driven surface distinct from the stat window, and a first pass at the client-owned pet-exception and collection-side controls instead of a mostly read-only page shell, while the remaining gap is the deeper networked workflow and packet-authored metadata that make the official profile window more than a local readout | character/profile UI layer, player metadata presentation (`CUIUserInfo::Draw`, `CUIUserInfo::OnButtonClicked`) |
| Partial | Quest log window parity | The simulator quest windows now render live runtime-backed lists for available, in-progress, completed, and recommended quests, instantiate the client `Tab/enabled` / `Tab/disabled` category buttons plus `BtMyLevel` / `BtAllLevel` filters from `UIWindow.img` or `UIWindow2.img`, keep selection synchronized with quest state mutations, color rows by accept/complete readiness, expand the list owner to use the dedicated quest-log shell instead of a simulator-only inline detail pane, preserve item-backed requirement/reward hover popups by moving them onto the dedicated detail owner, now load and honor the legacy `UIWindow.img/Quest/BtDetail` list-shell button so pre-Big-Bang quest browsing uses a client-shaped select-then-button cadence instead of forcing the Big Bang-style double-click open path, and now also expose the client quest-detail NPC-location button families (`BtMarkNpc` / `BtGotoNpc` pre-BB and `BtNPC` in `UIWindow2.img/Quest/quest_info`) through the existing detail owner by deriving quest-state target NPCs from the runtime and routing the action into the world-map search owner, but they still do not mirror the client's exact dynamic category-option rebuild cadence or the fuller delivery/goto/world-map result matrix behind specialized quest-detail button cases | Quest interaction now has a usable standalone log that reflects the same runtime state as NPC quest overlays, uses WZ-backed quest categories instead of a custom tab strip, follows the legacy detail-button cadence where that WZ shell exists, and exposes client-shaped NPC-locate buttons instead of leaving that detail-owner path decorative, while the remaining divergence is narrowed to the client's dynamic category-option rebuild behavior and the deeper delivery-specific quest-detail actions beyond the simulator's world-map-backed NPC search handoff | `QuestUI.cs`, `QuestUIBigBang.cs`, `QuestDetailWindow.cs`, `QuestRuntimeManager.cs`, `UIWindowLoader.cs`, NPC/UI bridge (`CUIQuestInfo::Draw`) |
| Partial | Quest detail window parity | The simulator now exposes a dedicated `QuestDetail` owner backed by the quest runtime, reuses the client `Quest/quest_info` frame family when present, opens from quest-list selection, renders separate summary/requirements/rewards sections with WZ-backed summary headers plus gauge art, shows color-coded requirement rows, draws reward item icon rows through the live item-icon loader, switches accept/complete/track/give-up controls across the client quest-detail button families when that art is present, supports previous/next quest navigation within the active tab, wires standalone accept or give-up actions plus completion guidance distinct from the list shell, now keeps the runtime-backed tertiary locate-NPC action visible through the quest-detail owner, reuses legacy `BtGotoNpc` and `BtMarkNpc` families when present while falling back to the Big Bang `BtNPC` family, lays out three-button delivery or completion cases without collapsing them back into the older two-button shell, and draws quest notice banners plus animated notice icons from the client quest notice families when that art is present, but it still does not mirror the client's exact demand-rect row art, fuller world-map demand or mob-mark behavior, or the remaining packet or cash-item-driven delivery workflows behind the most specialized detail buttons | The simulator now exercises the client's split between quest browsing and per-quest inspection with client-shaped notice chrome, NPC-locator button variants, and broader delivery/completion button layouts instead of a mostly text-only detail pane, while the remaining gap is narrowed to exact row rendering and deeper world-map or packet-authored delivery behavior rather than owner absence or missing locate-button paths | `QuestUI.cs`, `QuestUIBigBang.cs`, `QuestDetailWindow.cs`, `QuestRuntimeManager.cs`, quest-detail interaction layer (`CUIQuestInfoDetail::Draw`, `CUIQuestInfoDetail::OnButtonClicked`) |
| Partial | Quest alarm / notifier window parity | The simulator now replaces the `QuestAlarm` placeholder with a dedicated WZ-backed tracker built from `UIWindow.img/UIWindow2.img/QuestAlarm`, supports minimize/maximize toggles, title counts, live in-progress quest rows with mob and item requirement text plus demand-summary lines, completion-state coloring, row selection, per-row dismiss buttons shaped from the client `BtDelete` art, double-click routing into the quest detail flow, and now reuses shared `UIWindow2.img/Quest/quest_info` chrome for selection and progress-gauge rendering while surfacing negative quest `act/money` costs as live meso progress rows and rewarding or consuming mesos against the bound inventory runtime during quest start or completion; the quest-alarm runtime also now tracks recent quest-state, mob-kill, and tracked-item updates so the client `BtQ/ani` alert frames and sky-blue recent-update emphasis can light up before `BtQ` falls back to the normal quest-log toggle path, but it still does not mirror the client's full `CUIQuestAlarm` behavior such as packet-driven quest pinning, the full five-row owner layout and delete-all/config semantics, or the exact client demand-rect composition rules beyond this WZ-backed approximation | Active questing now has a persistent standalone progress tracker with animated quest-alert feedback, row-local dismiss affordances, richer WZ-backed progress chrome, and recent-update emphasis instead of a mostly custom list shell, while the remaining gap is narrowed to packet/config-driven tracking rules and the deeper owner-layout fidelity rather than the window being absent or the alert button behaving like a generic per-row shortcut | progression/UI layer, quest runtime, demand rendering (`CUIQuestAlarm::Draw`, `CUIQuestAlarm::OnButtonClicked`) |
| Partial | Skill macro parity | The macro window now enforces trimmed macro names against a client-shaped 12-byte budget instead of a flat 12-character ASCII subset, validates names inline against the current Windows ANSI locale, shows a live byte counter in the editor, routes committed character input through the simulator `GameWindow.TextInput` path so locale and IME text commits no longer depend on the old synthetic ASCII key mapping, accepts clipboard paste as a best-effort locale text insert that fills the remaining byte budget instead of rejecting an otherwise valid prefix wholesale, still supports drag-binding selected macros into QuickSlot entries, routes macro hotkeys back through `SkillManager` with party-chat announce semantics, paces queued casts so the next macro skill waits for the current cast to finish, and persists the five-slot macro definition list per active player or selected login-roster character under the simulator AppData profile across restarts instead of losing macro definitions with the session, but exact `CUISkillMacro` preedit composition visuals/ownership and any locale-specific character blacklists beyond the active code-page check are still not mirrored | Macro support now behaves like a real runtime surface instead of an editor-only stub, and the remaining gap is narrowed to the client's pre-commit IME composition behavior and deeper locale-specific blacklist rules rather than missing interaction ownership, committed locale input, byte-count enforcement, or session-only macro definitions | `SkillMacroUI.cs`, `SkillMacroNameRules.cs`, `SkillManager.cs`, `QuickSlotUI.cs`, `MapSimulator.cs` (`CUISkillMacro`) |
| Partial | Pickup notice parity | The simulator pickup-notice stack now renders item icons, resolves cached item names and type labels for success messages, records player pickup history, reconnects manual pickup failures through `PlayerCombat` so ownership and inventory-capacity checks consistently raise notices before the drop is consumed, mirrors the client `-3` style split more closely by pairing pickup-failure screen notices with a chat-log line through a shared formatter, applies a pet-specific validator backed by WZ `item/*/spec/notPickupByPet` so local auto-loot can surface pet-blocked items with item-aware pet messaging without spamming repeated failures, and now also routes `DropPool` mob-loot callbacks into the same notice stack so remote monster pickup events surface as a generic screen warning plus a specific chat line instead of silently consuming the drop, but it still lacks the client's full pickup-result surface such as the wider string-pool reason matrix beyond the simulator's current ownership, inventory-full, generic-unavailable, pet-blocked, and mob-pickup cases, other remote-loot actors beyond the wired mob path, and exact official screen-message wording and formatting across every pickup path | Pickup feedback now reflects real item metadata, shared inventory-capacity validation, item-aware pet-blocked failures, and a first remote-loot notice path for mob pickups rather than only local-player success text, so the remaining gap is narrowed to the deeper client string matrix, broader remote actor coverage, and exact official wording or formatting | `PickupNoticeUI.cs`, drop/item runtime (`CWvsContext::OnDropPickUpMessage`, `CUIScreenMsg::ScrMsg_Add`) |
| Partial | Cursor and utility-button parity | The simulator now builds dedicated WZ-backed `Menu` and `System` popup owners from `StatusBar2.img/mainBar/Menu` and `StatusBar2.img/mainBar/System`, routes the status-bar `BtMenu` and `BtSystem` buttons through those owners, fans their core entries back into inventory/equip/stat/skill/quest/character-info and channel-selection flows instead of leaving them as dead launchers, switches the cursor to the client hold art during active skill, quick-slot, and window drag flows, now loads the client forbidden-hand and busy-search cursor families from `Basic.img/Cursor/13` and `Basic.img/Cursor/16` so blocked or pending utility entries stop advertising as normal clicks, and dismisses the popup owners on outside clicks instead of leaving them latched open, but unsupported submenu families such as ranking, event, key-settings, joypad, and deeper option flows still remain simulator-local stubs rather than full `CUIStatusBar::OnButtonClicked` parity | This is part of the client feel layer and affects almost every window or interaction surface once the rest of the UI becomes more complete, and the remaining gap is now the deeper cursor-state matrix and submenu fidelity rather than dead utility-button seams | `MouseCursorItem.cs`, `StatusBarUI.cs`, `UILoader.cs`, `StatusBarPopupMenuWindow.cs`, broader UI manager layer (`CUIStatusBar::OnButtonClicked`) |

## Priority Order

If the goal is visible parity first, the next work in this area should be sequenced like this:

1. Window usefulness pass:
   Finish minimap, inventory, equip, and quest-log interactions so the existing windows become usable client-facing tools rather than frame shells.
2. Progression correctness pass:
   Tighten ability/stat formulas, equip presentation, and inventory-backed validations so the windows reflect the runtime accurately.
3. Utility feedback pass:
   Complete pickup notices, macro behavior, cursor states, and status-bar utility-button behavior.
4. Client seam pass:
   Capture direct IDA seams for minimap, inventory, equip, and quest windows so future parity fixes are anchored to named client owners instead of simulator-only structure.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".
