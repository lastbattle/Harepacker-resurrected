# MapSimulator Client Parity Backlog

## Purpose

This document is the single source of truth for MapSimulator parity work against the MapleStory client.

It does three things that the old notes did not do well:

1. Separates shipped work from missing work.
2. Distinguishes broad simulator features from player/avatar parity.
3. Anchors claims to actual code seams and, where relevant, client-side reverse engineering.

## Evidence Used

### Code seams reviewed

- `HaCreator/MapSimulator/UI/MinimapUI.cs`
- `HaCreator/MapSimulator/UI/MouseCursorItem.cs`
- `HaCreator/MapSimulator/UI/PickupNoticeUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AbilityUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AdminShopDialogUI.cs`
- `HaCreator/MapSimulator/UI/Windows/AbilityUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/BookCollectionWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/EquipUI.cs`
- `HaCreator/MapSimulator/UI/Windows/EquipUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/InventoryUI.cs`
- `HaCreator/MapSimulator/UI/Windows/InventoryUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/KeyConfigWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/MemoMailboxWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/MemoSendWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/MemoGetWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/OptionMenuWindow.cs`
- `HaCreator/MapSimulator/UI/Windows/QuestUI.cs`
- `HaCreator/MapSimulator/UI/Windows/QuestUIBigBang.cs`
- `HaCreator/MapSimulator/UI/Windows/SkillMacroUI.cs`
- `HaCreator/MapSimulator/UI/Windows/SoftKeyboardUI.cs`
- `HaCreator/MapSimulator/UI/Windows/WorldMapUI.cs`
- `HaCreator/MapSimulator/Interaction/MemoMailboxManager.cs`
- `HaCreator/MapSimulator/Fields/FieldInteractionRestrictionEvaluator.cs`

### Client references checked

- `CUIStat::Draw` (captured in simulator layout comments and offsets)
- `CUIStat::DrawTextA` (captured in simulator layout comments and offsets)
- `CUISkillMacro` (captured in simulator window comments)
- `CWvsContext::OnDropPickUpMessage` (captured in `PickupNoticeUI` comments)
- `CUIScreenMsg::ScrMsg_Add` (captured in `PickupNoticeUI` comments)
- `CUIItem::Draw` at `0x7ccf50`
- `CAdminShopDlg::OnCreate` at `0x42ecd0`
- `CAdminShopDlg::Draw` at `0x42fdf0`
- `CAdminShopDlg::OnButtonClicked` at `0x430f90`
- `CUIAdminShopWishList::HitTest` at `0x76cf50`
- `CUIAdminShopWishList::OnCreate` at `0x772640`
- `CUIAdminShopWishList::OnButtonClicked` at `0x772670`
- `CCashShop::Init` at `0x484920`
- `CCashShop::OnPacket` at `0x4997e0`
- `CCashShop::OnCashItemResult` at `0x499370`
- `CCSWnd_Char::OnCreate` at `0x4c4590`
- `CCSWnd_Char::OnDefaultAvatar` at `0x4bec80`
- `CCSWnd_Char::OnWear` at `0x4c5a60`
- `CCSWnd_Char::SetPet` at `0x4bc400`
- `CCSWnd_Char::ShowMessageBox` at `0x4bbb00`
- `CCSWnd_Char::ShowPersonalShop` at `0x4bc660`
- `CCSWnd_Char::ShowEntrustedShop` at `0x4bc6b0`
- `CCSWnd_Char::BlowWeather` at `0x4c5d30`
- `CCSWnd_Locker::OnCreate` at `0x4c31c0`
- `CCSWnd_Inventory::OnCreate` at `0x4c34f0`
- `CCSWnd_List::OnCreate` at `0x4d0500`
- `CCSWnd_Status::OnCreate` at `0x4cb840`
- `CCSWnd_OneADay::OnCreate` at `0x4d2fd0`
- `CCashTradingRoomDlg::OnCreate` at `0x49e8c0`
- `CUIChannelSelect::OnCreate` at `0x606a10`
- `CUIChannelSelect::Draw` at `0x6056d0`
- `CUIChannelSelect::OnButtonClicked` at `0x6035f0`
- `CUIWorldSelect::OnCreate` at `0x60b660`
- `CUIWorldSelect::OnButtonClicked` at `0x6025e0`
- `CUIChannelShift::OnCreate` at `0x96c160`
- `CUIChannelShift::Draw` at `0x96ccb0`
- `CUIItemMaker::Draw` at `0x7d4980`
- `CUIItemMaker::OnButtonClicked` at `0x7d5dc0`
- `CUIItemUpgrade::Draw` at `0x7c0100`
- `CUIItemUpgrade::OnButtonClicked` at `0x7c0ca0`
- `CUIMapTransfer::Draw` at `0x7df5c0`
- `CUIMapTransfer::OnButtonClicked` at `0x7e0ed0`
- `CUIEquip::Draw` at `0x7aa560`
- `CUIQuestInfo::Draw` at `0x82a780`
- `CUIQuestInfoDetail::Draw` at `0x8248c0`
- `CUIQuestInfoDetail::OnButtonClicked` at `0x834c90`
- `CUIQuestAlarm::Draw` at `0x82d850`
- `CUIRanking::OnCreate` at `0x83ca40`
- `CUIRanking::OnButtonClicked` at `0x83c6a0`
- `CUIRanking::Update` at `0x83c690`
- `CUIStatusBar::OnButtonClicked` at `0x880540`
- `CUIStat::Draw` at `0x864bd0`
- `CUIKeyConfig::OnCreate` at `0x7dc5d0`
- `CUIKeyConfig::Draw` at `0x7dd7c0`
- `CUIGameOpt::OnCreate` at `0x9777d0`
- `CUIMacroSysEx::OnCreate` at `0x85c420`
- `CUISoftKeyboard::GetKeyIndexFromPoint` at `0x85faa0`
- `CUISoftKeyboard::OnMouseMove` at `0x85fba0`
- `CUISoftKeyboard::HitTest` at `0x85fc50`
- `CUISoftKeyboard::UpdateKeyConstraint` at `0x85fd70`
- `CUIMiniMap::OnButtonClicked` at `0x803e40`
- `CUIMiniMap::OnMouseMove` at `0x8040a0`
- `CSoftKeyboardDlg::OnCreate` at `0x7162c0`
- `CSoftKeyboardDlg::OnButtonClicked` at `0x716c20`
- `CUIUserInfo::Draw` at `0x8b3a50`
- `CUIUserInfo::OnButtonClicked` at `0x8b6d80`
- `CUIEventAlarm::OnCreate` at `0x7ac4f0`
- `CUIEventAlarm::Draw` at `0x7aad90`
- `CUIEventAlarm::Update` at `0x7aac00`
- `CBookDlg::OnCreate` at `0x471f00`
- `CBookDlg::Draw` at `0x473360`
- `CBookDlg::OnButtonClicked` at `0x474300`
- `CMonsterBookMan::LoadBook` at `0x664c10`
- `CParcelDlg::OnCreate` at `0x6914c0`
- `CParcelDlg::Draw` at `0x694db0`
- `CParcelDlg::OnButtonClicked` at `0x690c30`
- `CITC::Init` at `0x573ee0`
- `CITC::OnPacket` at `0x577470`
- `CITC::OnNormalItemResult` at `0x5771d0`
- `CUserLocal::OnPacket` at `0x9340c0`
- `CUserLocal::OnOpenUI` at `0x9055f0`
- `CUserLocal::OnOpenUIWithOption` at `0x932300`
- `CUserLocal::OnGoToCommoditySN` at `0x905760`
- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`
- `CUserLocal::OnHireTutor` at `0x90e560`
- `CUserLocal::OnTutorMsg` at `0x916f60`
- `CUserLocal::OnOpenSkillGuide` at `0x90e6a0`
- `CUserLocal::OnAskAPSPEvent` at `0x90f0e0`
- `CUserLocal::OnRadioSchedule` at `0x918120`
- `CUIAntiMacro::OnCreate` at `0x78b980`
- `CUIAntiMacro::Draw` at `0x78bb30`
- `CUIAntiMacro::Update` at `0x77f1f0`
- `CUIAdminAntiMacro::OnCreate` at `0x78cd90`
- `CUIAdminAntiMacro::Draw` at `0x78cf40`
- `CUIAdminAntiMacro::Update` at `0x77f250`
- `CWvsContext::OnAntiMacroResult` at `0x9ff580`
- `CWvsContext::SaveAntiMacroScreenShot` at `0x9fa330`
- `CWvsContext::ShowAntiMacroNotice` at `0x9f3c30`
- `CRadioManager::Play` at `0x6cb940`
- `CRadioManager::Stop` at `0x6cbd20`
- `CRadioManager::Update` at `0x6cbf20`

Notes:
Direct IDA seam capture is now confirmed for inventory, cash-service, channel-shift, item-maker, item-upgrade, map-transfer, equipment, quest-log, quest-detail, quest-alarm, the dedicated ranking/event owners (`CUIRanking`, `CUIEventAlarm`), status-bar utility clicks, stat-window, the explicit key-config/game-option/soft-keyboard owner family (`CUIKeyConfig`, `CUIGameOpt`, `CSoftKeyboardDlg`, `CUIMacroSysEx`, `CUISoftKeyboard`), user-info owners, the book/collection owner family (`CBookDlg` plus `CMonsterBookMan::LoadBook`), the parcel/mail owner family (`CParcelDlg`), the separate service-stage owners for Cash Shop and ITC (`CCashShop`, `CITC`), the stage-internal Cash Shop owners (`CCSWnd_Char`, `CCSWnd_Locker`, `CCSWnd_Inventory`, `CCSWnd_List`, `CCSWnd_Status`, `CCSWnd_OneADay`, `CCashTradingRoomDlg`), the separate cash-service wishlist owner (`CUIAdminShopWishList`), the anti-macro challenge owners (`CUIAntiMacro`, `CUIAdminAntiMacro`), the packet-owned anti-macro result bridge in `CWvsContext`, and a broader local-user packet dispatcher for utility/event surfaces. `CUserLocal::OnPacket` is important because it shows the client does not only open these windows from direct button clicks: packet-owned handlers also drive generic `UI_Open`, optioned UI launch, commodity-shop migration, field fade overlays, balloon messages, and damage-meter timer resets. The Cash Shop and ITC decompiles also show that those services are not just alternate button skins on top of `CAdminShopDlg`: `CCashShop::Init` and `CITC::Init` each clear field UI, build their own staged child-window layout, select service BGM, and then wait on service-specific packet dispatch through `OnPacket`. Follow-up decompiles now also show that the stage owners do not directly absorb all of the in-service interaction either: `CCSWnd_Char::OnCreate` builds a dedicated avatar-preview surface with default-avatar, buy-avatar, and takeoff-avatar buttons plus a `CUserPreview`/physics-space pair, `CUIAdminShopWishList::OnCreate` builds a distinct wishlist owner with its own layer, controls, and focused search edit instead of leaving wish-list behavior inside the base shop dialog, `CCashTradingRoomDlg::OnCreate` builds its own chat edit, buttons, scrollbar, fonts, and cash-balance state inside the service stage, `CUIAntiMacro::{OnCreate,Draw,Update}` confirm a separate timed text-entry owner rather than treating lie-detector checks as item-use restriction text only, and `CWvsContext::OnAntiMacroResult` shows that the owner is itself packet-driven rather than purely item- or dialog-launched. The remaining seam-collection gap in this backlog area is now narrower and centered on the minimap window, the exact `CUISkillMacro` draw owner, and deeper packet/data ownership inside the newly confirmed utility-owner families rather than on an untracked anti-macro packet bridge or key-config/option/soft-keyboard surface.

## Client Function Index By Backlog Area

The list below maps this backlog area to the concrete client seams that are already named in code or prior reverse-engineering notes.
These are the first functions or classes to inspect before changing simulator behavior.

### 1. Progression and utility window parity

- `CUIStat::Draw`
- `CUIStat::DrawTextA`
- `CUIItem::Draw` at `0x7ccf50`
- `CAdminShopDlg::OnCreate` at `0x42ecd0`
- `CAdminShopDlg::Draw` at `0x42fdf0`
- `CAdminShopDlg::OnButtonClicked` at `0x430f90`
- `CCashShop::Init` at `0x484920`
- `CCashShop::OnPacket` at `0x4997e0`
- `CCashShop::OnCashItemResult` at `0x499370`
- `CUIChannelSelect::OnCreate` at `0x606a10`
- `CUIChannelSelect::Draw` at `0x6056d0`
- `CUIChannelSelect::OnButtonClicked` at `0x6035f0`
- `CUIWorldSelect::OnCreate` at `0x60b660`
- `CUIWorldSelect::OnButtonClicked` at `0x6025e0`
- `CUIChannelShift::OnCreate` at `0x96c160`
- `CUIChannelShift::Draw` at `0x96ccb0`
- `CUIItemMaker::Draw` at `0x7d4980`
- `CUIItemMaker::OnButtonClicked` at `0x7d5dc0`
- `CUIItemUpgrade::Draw` at `0x7c0100`
- `CUIItemUpgrade::OnButtonClicked` at `0x7c0ca0`
- `CUIMapTransfer::Draw` at `0x7df5c0`
- `CUIMapTransfer::OnButtonClicked` at `0x7e0ed0`
- `CUIEquip::Draw` at `0x7aa560`
- `CUIQuestInfo::Draw` at `0x82a780`
- `CUIQuestInfoDetail::Draw` at `0x8248c0`
- `CUIQuestInfoDetail::OnButtonClicked` at `0x834c90`
- `CUIQuestAlarm::Draw` at `0x82d850`
- `CUIRanking::OnCreate` at `0x83ca40`
- `CUIRanking::OnButtonClicked` at `0x83c6a0`
- `CUIRanking::Update` at `0x83c690`
- `CUIStatusBar::OnButtonClicked` at `0x880540`
- `CUIMacroSysEx::OnCreate` at `0x85c420`
- `CUISoftKeyboard::GetKeyIndexFromPoint` at `0x85faa0`
- `CUISoftKeyboard::OnMouseMove` at `0x85fba0`
- `CUISoftKeyboard::HitTest` at `0x85fc50`
- `CUISoftKeyboard::UpdateKeyConstraint` at `0x85fd70`
- `CUIUserInfo::Draw` at `0x8b3a50`
- `CUIUserInfo::OnButtonClicked` at `0x8b6d80`
- `CUIEventAlarm::OnCreate` at `0x7ac4f0`
- `CUIEventAlarm::Draw` at `0x7aad90`
- `CUIEventAlarm::Update` at `0x7aac00`
- `CBookDlg::OnCreate` at `0x471f00`
- `CBookDlg::Draw` at `0x473360`
- `CBookDlg::OnButtonClicked` at `0x474300`
- `CMonsterBookMan::LoadBook` at `0x664c10`
- `CParcelDlg::OnCreate` at `0x6914c0`
- `CParcelDlg::Draw` at `0x694db0`
- `CParcelDlg::OnButtonClicked` at `0x690c30`
- `CITC::Init` at `0x573ee0`
- `CITC::OnPacket` at `0x577470`
- `CITC::OnNormalItemResult` at `0x5771d0`
- `CRepairDurabilityDlg` (constructed from `CUserLocal::OnOpenUIWithOption` at `0x932300` when `uiId == 33`)
- `CUISkillMacro`
- `CWvsContext::OnDropPickUpMessage`
- `CUIScreenMsg::ScrMsg_Add`

Notes:
`CUIItem::Draw` confirms that the client inventory window owns meso text, per-slot icon and quantity drawing, item-grade frames, disabled-slot overlays, and special overlays such as the active bullet marker. `CAdminShopDlg::OnCreate`, `CAdminShopDlg::Draw`, and `CAdminShopDlg::OnButtonClicked` confirm a dedicated cash-service shop dialog rather than treating cash-shop and MTS access as simple status-bar button stubs. `CUIChannelSelect::OnCreate`, `CUIChannelSelect::Draw`, `CUIChannelSelect::OnButtonClicked`, `CUIWorldSelect::OnCreate`, and `CUIWorldSelect::OnButtonClicked` confirm that the status-bar channel path fans out into explicit world and channel selector owners rather than a generic toggle, while `CUIChannelShift::OnCreate` and `CUIChannelShift::Draw` keep a separate shift or transition surface in play. `CUIItemMaker::Draw` and `CUIItemMaker::OnButtonClicked` confirm a dedicated crafting or maker window rather than burying production flow inside generic inventory handling. `CUIItemUpgrade::Draw` and `CUIItemUpgrade::OnButtonClicked` confirm a separate item-enhancement surface rather than treating scroll or upgrade flow as a generic inventory tooltip action. `CUIMapTransfer::Draw` and `CUIMapTransfer::OnButtonClicked` confirm a dedicated map-transfer window distinct from the minimap shell. `CUIEquip::Draw` confirms that equip-window parity is tied to slot-disable rules, expiration and durability overlays, and job or subjob gating rather than just slot placement. `CUIQuestInfo::Draw`, `CUIQuestInfoDetail::Draw`, and `CUIQuestAlarm::Draw` confirm that quest progression parity includes a separate detail pane with dedicated button handling, category-button rebuilds, scroll-driven list rebuilding, quest-delivery requirement rendering, condition-state coloring, and a standalone quest-alert window rather than only the quest log shell. Follow-up direct lookups now also confirm that the `BtRank` and `BtEvent` status-bar launches land on dedicated client owners rather than unnamed utility shells: `CUIRanking::{OnCreate,OnButtonClicked,Update}` and `CUIEventAlarm::{OnCreate,Draw,Update}`. `CBookDlg::{OnCreate,Draw,OnButtonClicked}` together with `CMonsterBookMan::LoadBook` confirm that the client keeps a real book-style collection owner tied to Monster Book data rather than treating book presentation as a generic stats subpage. `CParcelDlg::{OnCreate,Draw,OnButtonClicked}` confirm a dedicated parcel/mail owner with receive, send, and quick-send surfaces rather than burying package delivery under memo/social text alone. `CUserLocal::OnOpenUIWithOption` now also gives direct client evidence that repair-durability is its own utility owner rather than a generic option toggle: the decompile at `0x932300` shows `uiId == 33` allocating `CRepairDurabilityDlg(v5)` when no `CUniqueModeless` owner is already active. `CUIStatusBar::OnButtonClicked` confirms the utility-button cluster is a real interaction owner instead of only decorative art. `CUIMacroSysEx::OnCreate` together with `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::OnMouseMove`, `CUISoftKeyboard::HitTest`, and `CUISoftKeyboard::UpdateKeyConstraint` confirms that client text-entry parity is not just committed character handling: the macro family also owns an explicit soft-keyboard helper with hover, hit-test, and per-mode key-constraint behavior. `CUIUserInfo::Draw` and `CUIUserInfo::OnButtonClicked` confirm a separate character-information or profile surface beyond the stat window itself.

### 2. Local utility and event packet parity

- `CUserLocal::OnPacket` at `0x9340c0`
- `CUserLocal::OnOpenUI` at `0x9055f0`
- `CUserLocal::OnOpenUIWithOption` at `0x932300`
- `CUserLocal::OnGoToCommoditySN` at `0x905760`
- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`

Notes:
`CUserLocal::OnPacket` is a distinct client seam rather than a random collection of callbacks. It dispatches packet-owned local utility behavior including `OnOpenUI`, `OnOpenUIWithOption`, `OnGoToCommoditySN`, field fade in/out, balloon messages, damage-meter timing, chat/notice surfaces, quest-guide and delivery-quest results, buff-zone effects, field HP-dec notices, tutor flows, and several event-sound/minigame-sound owners. The narrower decompiles already show concrete responsibilities that are not represented well by the existing window rows: `OnOpenUI` forwards a raw UI id into `CWvsContext::UI_Open`, `OnOpenUIWithOption` adds option-driven branches such as party-search requests and the repair-durability owner, `OnGoToCommoditySN` stores a commodity serial number before calling `SendMigrateToShopRequest`, `OnFieldFadeInOut` registers a fade animation relative to the avatar layer Z, `OnBalloonMsg` can bind a balloon to the avatar origin or an explicit world position, and `OnDamageMeter` resets a dedicated timer surface. That means the client has a packet-driven utility/event layer that sits between local gameplay and the specific window owners already tracked above.

### 6. Packet-authored radio-schedule parity discovered by targeted IDA scan

- `CUserLocal::OnRadioSchedule` at `0x918120`
- `CRadioManager::Play` at `0x6cb940`
- `CRadioManager::Stop` at `0x6cbd20`
- `CRadioManager::Update` at `0x6cbf20`

Notes:
The broader local-utility row already mentioned tutor/radio handlers as a residual gap, but targeted IDA lookup shows radio scheduling is its own client-owned lifecycle rather than one more generic sound callback. `CUserLocal::OnRadioSchedule` decodes a track name plus time value and forwards that payload into a singleton `CRadioManager` owner when no radio session is already active. `CRadioManager::Play` then resolves a WZ/UOL-backed track through `StringPool`, starts multimedia playback, stores the active track name, mutes field BGM through the sound manager, shows a dedicated radio UI, and pushes a chat-log notice for the start event; `CRadioManager::Stop` reverses that ownership by stopping playback, hiding the UI, restoring BGM state, and pushing the completion notice; and `CRadioManager::Update` confirms a timer-driven runtime instead of a fire-and-forget sound effect. None of that owner-specific audio/UI/chat lifecycle is tracked anywhere else in the backlog today.

### 7. Anti-macro owner parity discovered by targeted IDA scan

- `CUIAntiMacro::OnCreate` at `0x78b980`
- `CUIAntiMacro::Draw` at `0x78bb30`
- `CUIAntiMacro::Update` at `0x77f1f0`

Notes:
The current backlog only captures anti-macro behavior indirectly through field-limit item bans, but targeted IDA lookup shows the client also owns a dedicated challenge dialog. `CUIAntiMacro::OnCreate` initializes a standalone layout, creates a focused `CCtrlEdit` child with a 12-character horizontal cap, and adds the owner button chrome instead of routing lie-detector checks through a generic notice or inventory-use prompt. `CUIAntiMacro::Update` confirms this is a timed runtime that recomputes the remaining seconds against `m_tTimeOver` and invalidates the owner as the countdown changes, while `CUIAntiMacro::Draw` renders countdown digits from the dedicated WZ canvas family and formats the remaining-attempt message from `StringPool` instead of relying on generic tooltip or chat presentation. That is a separate utility-owner family, not just another field restriction.

### 8. Explicit key-config, game-option, and soft-keyboard owner parity discovered by targeted IDA scan

- `CUIKeyConfig::OnCreate` at `0x7dc5d0`
- `CUIKeyConfig::Draw` at `0x7dd7c0`
- `CUIGameOpt::OnCreate` at `0x9777d0`
- `CUIMacroSysEx::OnCreate` at `0x85c420`
- `CUISoftKeyboard::GetKeyIndexFromPoint` at `0x85faa0`
- `CUISoftKeyboard::OnMouseMove` at `0x85fba0`
- `CUISoftKeyboard::HitTest` at `0x85fc50`
- `CUISoftKeyboard::UpdateKeyConstraint` at `0x85fd70`
- `CSoftKeyboardDlg::OnCreate` at `0x7162c0`
- `CSoftKeyboardDlg::OnButtonClicked` at `0x716c20`

Notes:
The broader cursor/utility-button row already mentions that the simulator can launch KeyConfig and OptionMenu owners, but targeted IDA lookups show the client keeps a more explicit owner family here than that row names. `CUIKeyConfig::{OnCreate,Draw}` confirm a dedicated key-setting owner with its own layout and draw lifecycle instead of a generic submenu under the status-bar system popup. `CUIGameOpt::OnCreate` confirms that game/system options are owned by a dedicated option dialog rather than only button callbacks hanging off `CUIStatusBar::OnButtonClicked`. `CUIMacroSysEx::OnCreate`, `CUISoftKeyboard::*`, and `CSoftKeyboardDlg::{OnCreate,OnButtonClicked}` show that the soft-keyboard path also has its own dialog owner and button lifecycle, not just helper hit-testing functions. Keeping that owner family explicit matters because the simulator already has `KeyConfigWindow`, `OptionMenuWindow`, and `SoftKeyboardUI`, so the remaining parity work is now about owner-specific lifecycle, constraints, and button semantics rather than whether these surfaces exist at all.

### 9. Tutor, skill-guide, and AP/SP helper parity discovered by `CUserLocal::OnPacket` scan

- `CUserLocal::OnHireTutor` at `0x90e560`
- `CUserLocal::OnTutorMsg` at `0x916f60`
- `CUserLocal::OnOpenSkillGuide` at `0x90e6a0`
- `CUserLocal::OnAskAPSPEvent` at `0x90f0e0`

Notes:
The broader local-utility row currently mentions tutor flows only as residual dispatch coverage, but the targeted `CUserLocal::OnPacket` decompiles show a distinct client-owned helper surface. `OnHireTutor` tears down the prior tutor owner, allocates a `CTutor`, initializes it through the same summoned-style world seam the client uses, and tracks active tutor variants in `CUserLocal::ms_anTutors`; `OnTutorMsg` then forwards either numeric or string payloads plus two packet-owned ints into `CTutor::OnMessage` instead of using ordinary chat. The same scan also shows that guided helper flows are not only local button opens: `OnOpenSkillGuide` packet-opens the skill UI and immediately calls `CUISkill::OpenCurSkillGuide`, while `OnAskAPSPEvent` opens a Yes/No prompt and can emit a follow-up outpacket (`195`) when the player accepts. None of those owner-specific tutor/helper semantics are tracked explicitly in the backlog today.

### 10. Minimap owner parity discovered by targeted IDA scan

- `CUIMiniMap::OnButtonClicked` at `0x803e40`
- `CUIMiniMap::OnMouseMove` at `0x8040a0`

Notes:
The minimap row already tracks a large amount of behavior, but the owner itself was still unnamed in the seam index. Targeted IDA decompiles now confirm a dedicated `CUIMiniMap` owner rather than a loose collection of helper draws. `CUIMiniMap::OnButtonClicked` cycles the minimap option state, remembers the previous option, and opens `CWorldMapDlg` directly from minimap button ids `1000` to `1003`, including the fallback notice path when the world-map dialog cannot be created. `CUIMiniMap::OnMouseMove` is equally important because it shows the client-owned tooltip and helper-marker surface that sits behind the existing backlog row: the owner computes tooltip hit rectangles for off-map remote direction markers, live NPC markers, employee markers from `CEmployeePool`, and portal markers from `CPortalList`, then resolves tooltip strings from `CNpcTemplate`, `CEmployee::GetNameTag`, portal script-property text, and map-string fallbacks before routing them through `CUIToolTip`. That is concrete client evidence for minimap-specific owner logic rather than only simulator-side UI structure.

### 3. Packet-authored local overlays and guidance parity

- `CUserLocal::OnFieldFadeInOut` at `0x905790`
- `CUserLocal::OnBalloonMsg` at `0x91d780`
- `CUserLocal::OnDamageMeter` at `0x905620`
- `CUserLocal::OnNotifyHPDecByField` at `0x90fed0`
- `CUserLocal::OnQuestGuideResult` at `0x90f1e0`
- `CUserLocal::OnDeliveryQuest` at `0x90ef60`
- `CUserLocal::OnOpenClassCompetitionPage` at `0x905580`

Notes:
These handlers form a narrower client-owned feedback layer under `CUserLocal::OnPacket`, and the current backlog only references them inside the broad dispatcher row. IDA now confirms concrete responsibilities that deserve separate tracking: `OnFieldFadeInOut` decodes fade-in, fade-out, and duration fields then registers the animation relative to the avatar layer Z; `OnBalloonMsg` decodes text, width, lifetime, and either avatar-relative or explicit world-position placement before calling `ShowBallonMsg`; `OnDamageMeter` resets the dedicated damage-meter timer owner and updates shared timing state in `CWvsContext`; `OnQuestGuideResult` rebuilds quest-demand targets and opens the world-map quest-guide owner when the packet contains viable quest-mob targets; `OnDeliveryQuest` either opens the dedicated delivery-quest owner or routes the result back through the status-bar chat seam when another unique modeless owner is active; and `OnOpenClassCompetitionPage` is a packet-driven utility launch rather than a normal button path.

### 4. External cash-service stage parity

- `CCashShop::Init` at `0x484920`
- `CCashShop::OnPacket` at `0x4997e0`
- `CCashShop::OnCashItemResult` at `0x499370`
- `CITC::Init` at `0x573ee0`
- `CITC::OnPacket` at `0x577470`
- `CITC::OnNormalItemResult` at `0x5771d0`

Notes:
This service-stage surface is distinct from the in-map `CAdminShopDlg` approximation already tracked above. IDA shows `CCashShop::Init` and `CITC::Init` clearing field UI, constructing their own staged child-window layouts, selecting service BGM, and entering standalone stage owners rather than opening one more utility dialog over the live HUD. The packet routers are distinct too: `CCashShop::OnPacket` dispatches cash-service result families such as charge-param, query-cash, cash-item, purchase-exp, gift-mate, duplicate-id, name-change, transfer-world, gachapon, one-a-day, and free-item notices, while `CITC::OnPacket` owns its own smaller result family around charge-param, query-cash, and normal-item results. That means Cash Shop and ITC parity should be tracked as their own service-stage owner family rather than being treated as a subset of status-bar launches or the current admin-shop fallback.

### 5. Cash-service child-window and preview parity discovered by targeted IDA scan

- `CCSWnd_Char::OnCreate` at `0x4c4590`
- `CCSWnd_Char::OnDefaultAvatar` at `0x4bec80`
- `CCSWnd_Char::OnWear` at `0x4c5a60`
- `CCSWnd_Char::SetPet` at `0x4bc400`
- `CCSWnd_Char::ShowMessageBox` at `0x4bbb00`
- `CCSWnd_Char::ShowPersonalShop` at `0x4bc660`
- `CCSWnd_Char::ShowEntrustedShop` at `0x4bc6b0`
- `CCSWnd_Char::BlowWeather` at `0x4c5d30`
- `CCSWnd_Locker::OnCreate` at `0x4c31c0`
- `CCSWnd_Inventory::OnCreate` at `0x4c34f0`
- `CCSWnd_List::OnCreate` at `0x4d0500`
- `CCSWnd_Status::OnCreate` at `0x4cb840`
- `CCSWnd_OneADay::OnCreate` at `0x4d2fd0`
- `CCashTradingRoomDlg::OnCreate` at `0x49e8c0`

Notes:
The stage-owner row above is still too coarse on its own. IDA shows that Cash Shop and related cash-service flows split into dedicated child-window owners after `CCashShop::Init` creates the stage: `CCSWnd_Char::OnCreate` allocates its own avatar preview layer, user-preview actor, physical-space helpers, and `BtBuyAvatar`/`BtDefaultAvatar`/`BtTakeoffAvatar` controls before calling `OnDefaultAvatar`; the adjacent `CCSWnd_Char` methods also expose stage-local preview actions for wear/takeoff, pet preview, weather, message box, personal shop, and entrusted shop effects rather than routing those previews through generic field windows. Separate owners then build the rest of the service UI: `CCSWnd_Locker`, `CCSWnd_Inventory`, `CCSWnd_List`, and `CCSWnd_Status` each have their own `OnCreate` routines, `CCSWnd_OneADay::OnCreate` owns the One-a-Day panel inside the service, and `CCashTradingRoomDlg::OnCreate` builds a standalone cash trading room with its own chat edit, trade/coin buttons, scrollbar, fonts, and initial cash balance. None of these client-owned child-window seams are named anywhere else in the backlog today even though they sit directly on the parity path for cash-avatar preview and service-stage usability.

## Current State Summary

MapSimulator is no longer "missing player simulation".
The project already has:

- A playable local character with foothold, ladder, rope, swim, fly, jump, prone, hit, death, and respawn states.
- Character WZ loading for body, head, face, hair, equipment, weapon overlays, z-map ordering, and slot-visibility conflicts.
- A working generic skill system with job skill loading, buffs, cooldowns, projectiles, hit effects, and quick-slot assignment.
- Mob and drop pools, hidden portal support, damage number rendering from WZ assets, mob HP bars, and boss HP bars.
- Several field-effect systems and special field implementations that are already present in the current simulator.

The real parity gap is no longer "does the simulator have a feature at all?".
The gap is now "how closely does the current implementation match client behavior, data selection, timing, and fallback rules?".

## Recent Progress Summary

Recent implementation progress has been folded into the backlog tables below instead of being tracked as a separate changelog.
The main baseline updates worth keeping visible are:

- Avatar parity moved out of the stub phase: action coverage, rare-action rendering, facial expression behavior, and anchor fallback rules are implemented, while mount and transform handling is partially in place.
- Skill parity is broader than the old notes implied: the simulator now loads and casts the full player skill catalog, supports runtime job swaps, shows buff and cooldown feedback, and covers more movement-family and summon behavior.
- Physics and movement now have real runtime seams for ladder lookup, float collision, movement-path snapshots, and moving-platform foothold sync.
- Combat and UI both shifted from "missing feature" to "refinement": HP indicators, damage rendering, status-bar layout, warning flashes, chat strip behavior, skill UI layout, and tooltip reuse all exist but still need client-accurate polish.
- NPC and quest interaction now have a usable simulator baseline through dialogue overlays, quest lists, and common quest-state mutations, but full scripting and inventory-backed requirements remain incomplete.
- Progression or utility window groundwork now has a shared seam: named owners for world map, map transfer, cash-service, channel-selection, crafting, enhancement, character-info, and quest-side utility windows are registered with `UIWindowManager`, while minimap and status-bar utility buttons now route into those owners instead of staying entirely dead-ended. The world/channel path now opens dedicated simulator-side `WorldSelect`, `ChannelSelect`, and timed `ChannelShift` surfaces backed by `UIWindow2.img/Channel` art, with the channel grid, centered placement, per-channel load feedback, login-step request gating, delayed `CheckUserLimitResult` / `SelectWorldResult` style request flow, and simulator-side `WorldInformation` / `RecommendWorldMessage` / `LatestConnectedWorld` metadata overlays now aligned to the client-owned selector layout instead of remaining a placeholder-only launch seam. The map-transfer owner now also keeps saved teleport destinations in a character-scoped destination book keyed to the active player or selected login-roster character instead of sharing one global saved list across every simulator avatar, persists that destination book under the simulator AppData profile across restarts, and hands the currently selected transfer target into the world-map owner when `BtMap` is used so the two navigation surfaces stay linked.

## Remaining Parity Backlog

### 1. Progression and utility window parity

This area is currently absent from the backlog set even though the simulator already has concrete window and feedback implementations.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Minimap window parity | The minimap now loads WZ-backed helper markers for generic NPCs, quest-start NPCs, quest-end NPCs, portals, out-of-bounds direction arrows, and the broader `DefaultHelper` remote-user families from `UIWindow2.img/MiniMapSimpleMode/DefaultHelper`; it upgrades the post-Big-Bang generic NPC notice marker plus off-map direction overlays to the animated `UIWindow2.img/MiniMap/iconNpc` and `MiniMap/iconDirection/*` frame families when those client assets are present, feeds visible portal markers plus quest-aware NPC marker families into `MinimapUI`, keeps the post-Big-Bang `BtNpc` list panel from `ListNpc`, and now also tolerates the client-shaped fallback where only quest-start or quest-end marker art is present instead of assuming the generic NPC helper family always exists before the NPC toggle can work. The minimap now also feeds same-field simulator social/family entries into the minimap as `another` / `friend` / `party` / `partymaster` / `guild` / `guildmaster` helper markers, reusing family-chart positions when the simulator has them and otherwise assigning deterministic local field positions so those helper families can render with matching off-map direction overlays instead of staying dead data. It also resolves a local helper marker through the same seam, so active Match Cards rooms can switch the player dot to the WZ `match` family and personal- or entrusted-shop field actors can switch it to `usertrader`; that helper path now suppresses the legacy fallback yellow pixel dot while those local helper families are active instead of double-drawing both markers. Visible Match Cards participants plus shop occupants still seed `match` / `anothertrader` helper markers even when they only have runtime occupant state and no packet-authored world position. The minimap owner now also actually wires its hover seam instead of leaving it dormant: NPC markers expose quest-state-aware name tooltips, portal markers expose script or map-target fallback text, helper markers now surface tracked remote or synthetic user names, and the active personal-shop or entrusted-shop employee snapshot now seeds a dedicated minimap marker plus tooltip path using the same owner-local hover chrome and off-map direction overlay rules while preferring the WZ `DefaultHelper/usertrader` trader-family marker with `anothertrader` or NPC fallback when those helper canvases are absent. `BtBig` still opens a dedicated `WorldMap` owner whose `WorldMapSearch` side panel is no longer just background art: `BtSearch` switches into a dedicated WZ-backed search surface with `BtAllsearch` return flow, optional `BtLevelMob` mob-only filtering, live field/NPC/mob result rows sourced from the current simulator field, and a working text-entry search path that filters those result families plus global field-name or map-id matches, keeps search-row selection distinct even when multiple rows resolve to the same field, supports keyboard navigation or activation, accepts clipboard paste into the search box, now uses the client `UIWindow2.img/WorldMap/title` canvas instead of a text-only heading, keeps the search caret and visible query text clipped inside the edit box when the query overruns the field, lets mouse clicks reposition the caret inside that visible query span, adds word-wise `Ctrl+Backspace` / `Ctrl+Delete` editing on top of the existing keyboard path, now also adds word-wise `Ctrl+Left` / `Ctrl+Right` caret travel inside that same edit seam, routes committed text through the shared `GameWindow.TextInput` seam instead of relying only on raw key translation, previews Win32 IME preedit text inline with a live underline before commit, now also renders an owner-local IME candidate list anchored to that same search box via the shared candidate seam instead of dropping candidate updates on the floor, measures the horizontal candidate window against the visible candidate strings so wider IME candidates stop clipping inside the search owner, and now also clamps that candidate owner to the actual `WorldMap` frame bounds instead of letting placement logic drift outside the live owner while `BtAll` / `BtAnother` browsing, current-map focus, paged field selection, and direct map-transfer queuing still run through the existing pending-map seam. The remaining gap is that the simulator still does not mirror true packet-authored remote-user field state, the client's exact trader or match helper semantics beyond the current local runtime synthesis, the still-missing `DefaultHelper` families that depend on richer live packet state, or the client's exact IME candidate-window chrome polish inside that search owner | Navigation feedback now has animated minimap-owned notice and direction art, quest- and portal-aware markers, a broader WZ-backed remote-user helper surface, runtime-driven local and remote `match` / trader helper-family usage whose local helper swap now actually replaces the old pixel dot, active employee markers that now prefer the trader helper family before falling back to NPC art, live minimap hover feedback for NPC, portal, helper, and active employee markers, true WZ-backed hover chrome inside the world-map search rows instead of treating the `mouseOverBase` art like a selection-only fill, and an owner-local IME candidate list for search input whose horizontal layout now sizes to the active candidate text and stays constrained to the live world-map owner. The remaining gap is narrowed to true packet-authored field state, exact social-helper semantics, missing richer-helper families, and the client's exact IME chrome polish rather than dormant minimap hover data, missing employee hover routing, a double-drawn local helper marker, candidate-text clipping, candidate-owner escape, missing candidate feedback, or a static search panel | `MinimapUI.cs`, `WorldMapUI.cs`, minimap/window loader path |
| Partial | Map transfer / navigation window parity | The simulator now routes minimap `BtMap` into a dedicated WZ-backed `MapTransfer` owner built from the `Teleport*` window art, constrains the destination surface to the client's five visible rows, caps saved teleport destinations to a client-shaped five-vs-ten slot budget based on the loaded `Teleport` versus continent `Teleport2`/`Teleport3` art variant, respects teleport-item field limits separately from generic migration checks, layers return-map plus portal-derived routes on top of the saved list while driving actual map changes through the existing pending-map seam instead of stopping at the minimap shell, keeps saved teleport destinations in a character-scoped destination book keyed to the active player or selected login-roster character instead of one simulator-global list, now splits that per-character book into client-shaped regular-versus-continent slot banks so the active 5-slot `Teleport` owner no longer shares saved rows with the 10-slot `Teleport2`/`Teleport3` owner the way the client keeps `CharacterData::adwMapTransfer` and `adwMapTransferEx` separate, persists those per-character books under the simulator AppData profile across restarts with legacy single-book saves migrated into the continent-capable bank, opens the world-map owner focused on the currently selected transfer target rather than always resetting to the current field, preserves fixed per-character slot indices instead of compacting into a generic list, renders empty saved slots inside the transfer window, keeps the selected saved slot alive through the world-map handoff so a `[Target]` selection can be written back into that exact slot through `BtRegister`, now draws the client-shaped saved-count header instead of treating the list like an unbounded generic scroller, resolves saved-slot labels from map IDs rather than persisting simulator-only custom names, exposes the client-owned edit-target lane as a local numeric map-id entry path with 12-digit input, focus-driven selection clearing, paste support, and direct `BtMove` routing when no list row is selected, now loads the client `FadeYesNo/backgrnd7` plus `BtOK` / `BtCancel` confirmation chrome so register, delete, saved-route, portal-route, and manual map-id requests all pass through a local yes/no confirmation step instead of mutating or moving immediately, applies the client-shaped saved-destination category gate from `CUIMapTransfer::OnRegister` so sub-`100000000` ids and `xx9xxxxx` map families cannot be written into saved teleport slots while duplicate-save attempts refocus the already-owned slot instead of silently pretending to create a second entry, now mirrors the continent variant's visible client-owned scrollbar path by wiring `UI/Basic.img/VScr9` into the 10-slot `Teleport2`/`Teleport3` owner instead of leaving the scrollbar logic disconnected, and in this pass fully closes the simulator-local request seam by hydrating per-character fixed 5-slot and 10-slot runtime arrays from the AppData store, routing local register and delete actions through a `CWvsContext::SendMapTransferRequest`-shaped request/response cycle, then applying the returned field list back into those fixed arrays before persisting them to disk. It still does not mirror the client's true packet-backed `CharacterData::adwMapTransfer` / `adwMapTransferEx` population from the server, the actual outbound packet transport and inbound response lifecycle behind `CWvsContext::SendMapTransferRequest`, or deeper unsaveable rules beyond the category gate now mirrored locally | Navigation testing can now exercise a more client-shaped transfer and destination-selection surface above raw portal travel, saved destinations no longer bleed across unrelated simulator characters or across the regular-vs-continent transfer books, the saved-slot header, scrollbar, raw map-id presentation, and fixed-slot semantics are closer to the official owner, the edit target no longer behaves like dead art, world-map retargeting now lands in the intended saved slot instead of collapsing back into a generic append path, saved-slot registration now follows the same broad category gate the client applies before writing `adwMapTransfer` / `adwMapTransferEx`, and register/delete/move requests now all pass through the same WZ-backed yes/no chrome, while successful local save/delete requests also complete the client-shaped loopback by updating the fixed slot arrays and persisted character book from the synthesized response payload instead of stopping at a transient in-memory reply. The remaining gap is narrowed to true network-authored character-data population, real outbound packet transport plus inbound response ownership, and the deeper destination-validation rules before it fully matches the client flow | navigation/UI layer, minimap integration, destination-selection flow (`CUIMapTransfer::Draw`, `CUIMapTransfer::OnButtonClicked`, `CUIMapTransfer::OnCreate`) |
| Partial | Cash shop / admin shop dialog parity | The simulator now replaces the generic Cash Shop and MTS placeholders with a dedicated `AdminShopDialogUI` owner backed by `UIWindow2.img/Shop`, reuses the shop dialog frame plus `BtBuy` / `BtSell` / `BtExit` / `BtRecharge`, keeps Cash Shop and MTS inside the same button-driven surface, routes the status-bar launchers so only the requested service window stays open, exposes separate scrollable NPC-offer and user-listing panes with per-row selection and request submission messaging, consumes the client `UIWindow2.img/Shop/TabShop` category strip as a real WZ-backed catalog filter, now also consumes the owner `TabBuy` and `TabSell` tab canvases with their client WZ offsets instead of leaving the service and pane strips as text-only chrome, now anchors the `TabShop` category strip to the client-owned bottom control position from `CAdminShopDlg::OnCreate` instead of treating the zero-origin canvases like a stack at window origin, mirrors the client-owned twin scrollbar layout called out by `CAdminShopDlg::OnCreate`, replaces the wish-list footer prompt with a `FadeYesNo`-style confirmation modal and now routes `BtRecharge` through that same owner-local yes/no step before the dedicated `CUIAdminShopWishList`-style window opens, matching the `CAdminShopDlg::OnButtonClicked` launch shape instead of jumping straight into the search owner, syncs the displayed balance to the live inventory meso runtime like `CAdminShopDlg::Draw`, now renders WZ-backed item icons plus stack counts for requestable rows in both panes and the detail area so the dialog no longer behaves like a text-only catalog despite `CAdminShopDlg::DrawNPCItem` / `DrawUserItem`, resolves simulator-side shop requests through a broader local session matrix with meso consumption, inventory-capacity checks, actual item delivery for requestable rows, post-purchase sold-out locking on one-shot listings, rejected outcomes such as seller-offline, expired, inventory-full, and refreshed-sold-out responses, plus real icon carry-through on delivered items instead of treating every non-expansion row as the same generic acknowledgment, now interprets the left browse strip as the real WZ-backed `ALL` / `MOST` / `SELL` / `BUY` / `RE-BUY` family for Cash Shop while MTS swaps to its own WZ-backed `ALL` / `MOST` / `SELL` / `BUY` subset from `UIWindow2.img/Shop/TabSell`, and keeps the right `TabSell` strip acting as WZ-backed quick category tabs (`Equip` / `Use` / `Etc` / `Set-up` / `Cash`) instead of fake Cash Shop or MTS toggles, with featured-row curation for `MOST`, pane focus changes for `SELL` and `BUY`, in-session wish-list plus delivered-row state restoration so `RE-BUY` can surface previously granted rows after page flips, and browse-surface state memory keyed to the active pane plus browse/category strip so tab changes restore the prior row focus and scrollbar position instead of snapping back to the top of every filtered surface. The Cash Shop NPC pane now also resolves sample commodity metadata against live `Etc/Commodity.img` rows so matching entries inherit WZ-backed price, stack-count, and commodity-SN data instead of only hard-coded sample values, the packet-owned `OnGoToCommoditySN` launch seam now focuses the matching Cash Shop sample row plus category inside the existing owner after migration instead of only storing the SN in a side channel, and the owner now mirrors the `AskItemCount` branch inside `CAdminShopDlg::SendTradeRequest` for stackable sample rows by opening a quantity-selection modal that reuses WZ-backed `UIWindow2.img/UtilDlgEx/BtPrev` and `BtNext`, supports keyboard plus mouse-wheel count changes, and carries the chosen count through meso validation, inventory-capacity checks, refunds, and delivered stack totals instead of always hard-submitting quantity `1`. This pass also mirrors the zero-price inventory-source validation branch inside `CAdminShopDlg::SendTradeRequest` for dedicated trade-in rows by adding local zero-meso exchange entries, clamping the quantity modal against live source-item stock, validating WZ `info/cash`, `info/notSale`, and `info/quest` trade restrictions before submit, surfacing owned-count messaging in the detail pane, and only consuming the matching source item when delivery succeeds instead of treating every request as mesos-only. The dialog also supports owner-local keyboard parity for the current browse surface: `Up` / `Down` / `PageUp` / `PageDown` / `Home` / `End` walk the active pane, `Tab` / `Left` / `Right` swap between the NPC-offer and user-listing panes, `Enter` submits the highlighted request, `Space` opens the wish-list confirmation when supported, `Enter` / `Escape` resolve the `FadeYesNo` modal without forcing mouse-only interaction, the quantity modal also maps `Left` / `Right` / `Up` / `Down` plus `PageUp` / `PageDown` / `Home` / `End` to count changes, and mouse double-click on the already-selected row now fires the same request path the client uses in `CAdminShopDlg::OnMouseButton` instead of requiring the explicit buttons or keyboard only. It still does not mirror the full `CAdminShopDlg` behavior surface such as true packet-backed shop-session state beyond commodity-SN targeting, live server-populated catalogs, the exact official semantics behind each client tab state, the client's full sell-or-relay request matrix beyond these local source-item trade-ins, or the wider server-authored request and trade matrix behind those controls | The simulator can now exercise the client cash-service launch path through a dedicated dialog owner with concrete per-item browsing, mode-specific WZ-backed browse chrome for Cash Shop versus MTS, client-shaped scrollbar and category-tab affordances whose bottom `TabShop` strip now lands on the client-owned control geometry instead of a zero-origin overlap, visible item art in the browse and detail surfaces, a real wish-list confirmation step whose `BtRecharge` path now gates the wishlist owner behind the same local yes/no prompt the client uses, live balance readout, a keyboard-drivable selection and request flow, client-shaped row double-click activation, remembered per-surface browse position instead of always dropping back to the first row after each tab/filter hop, an owner-local count prompt for stackable rows that no longer collapses every request into quantity `1`, and now a first local zero-price trade-in seam that validates WZ-backed source-item restrictions and live owned counts before consuming the required inventory item on success. Using `Etc/Commodity.img` for matching NPC rows and honoring packet-owned commodity-SN focus also gives the owner a first real packet-shaped catalog-selection seam instead of treating `OnGoToCommoditySN` as message-only bookkeeping, while the remaining gap is narrowed to true live-catalog/session sourcing, deeper tab semantics, the broader sell-side and relay request matrix beyond the current local source-item exchanges, and the wider server-authored trade/session rules rather than missing quantity prompts, commodity targeting, recharge-gated wishlist launching, zero-price source-item validation, category-strip placement, browse-position restoration, row double-click activation, shared browse art, unlabeled placeholder tabs, or a mouse-only dialog | cash-service UI layer, status-bar integration, shop-session flow (`CAdminShopDlg::OnCreate`, `CAdminShopDlg::Draw`, `CAdminShopDlg::OnButtonClicked`, `CAdminShopDlg::OnMouseButton`, `CAdminShopDlg::SendTradeRequest`, `CAdminShopDlg::OnChildNotify`) |
| Partial | World and channel selector parity | The simulator now routes `BtChannel` into dedicated `WorldSelect`, `ChannelSelect`, and timed `ChannelShift` windows, uses `UIWindow2.img/Channel` world badges and channel art for the local selector stack, preserves a visible current world/channel choice for the simulator session, expands the world list to the full badge set, aligns the channel grid and window centering to the client `GetRect`/owner layout, simulates per-world channel availability plus busy/full load bars so invalid targets no longer behave like always-open buttons, mirrors the client-owned request shape more closely by disabling selector input outside the login `WorldSelect` step and resolving world/channel picks through delayed `CheckUserLimitResult` / `SelectWorldResult` style simulator responses instead of committing immediately, lets login-scene `WorldInformation`, `RecommendWorldMessage`, and `LatestConnectedWorld` packets drive selector-side world load data, recommended-world markers, latest-world focus, and immediate UI refresh instead of leaving those packet owners as message-only stubs, carries simulator-side adult-channel gating, denied `CheckUserLimitResult` / `SelectWorldResult` style result messaging, plus low-frequency live population drift across login-scene world metadata so the selector surface no longer behaves like a fixed always-successful snapshot, now also accepts client-shaped `WorldInformation` payload bytes through the existing login-packet seam, decodes the `CLogin::OnWorldInformation` packet layout into per-world channel user counts and adult-channel flags, supports incremental worldinfo accumulation plus the client terminator packet instead of only one-shot synthetic profile overrides, preserves those packet-authored per-channel populations against local drift so repeated server-authored selector updates can refresh the UI without being immediately replaced by simulator heuristics, and now exposes the existing login loopback transport as an explicit `/loginpacket inbox` control surface with configurable listen port plus parser support for raw `<packet> <args>`, `<packet>:<args>`, and `<packet>=<args>` feed shapes so selector-side world updates no longer depend on chat-only injection formatting. The same selector seam now also accepts packet-authored `RecommendWorldMessage`, `LatestConnectedWorld`, and `CheckUserLimitResult` payload bytes directly, the inbox/chat path can collapse relay-style raw hex payload fragments for `WorldInformation`, `RecommendWorldMessage`, and `SelectWorldResult` into the existing decoder flow instead of requiring every external source to pre-wrap them as `payloadhex=...`, and the loopback parser now also accepts relay-tagged `recv`/`incoming` feeds with `packet=` / `type=` / `handler=` metadata, `payload` / `hex` / `data` / `bytes` / `base64` aliases, plus `CLogin::OnWorldInformation` / `OnRecommendWorldMessage` / `OnLatestConnectedWorld` / `OnSelectWorldResult` style handler names so external decrypted login relays no longer need to rewrite every line into the simulator's chat command syntax first. `Login.img/WorldSelect/BtViewAll` is now also live on the actual selector surface instead of a dead loaded asset: the world selector wires the button through the existing delayed selector-request seam, blocks it outside the login `WorldSelect` step the same way the client request path is gated, and resolves it through a timed `ViewAllCharResult`-style runtime handoff into the existing packet-backed expanded-roster flow instead of jumping steps immediately or leaving the button inert. The selector now mirrors the client-observed `SelectWorldResult` rejection families from `CLogin::OnSelectWorldResult` plus the `CheckUserLimitResult` code-2 over-user-limit path from `CUIWorldSelect::UserLimitResult`, including the grouped login-error branches and the code-7 title return instead of collapsing every non-success packet into one generic rejection, and the same transport seam now also exposes the built-in `LoginOfficialSessionBridgeManager` through `/loginpacket session [status|discover|start|startauto|stop]`, including direct proxy and discovery-backed proxy startup, optional local-port filtering for multi-session discovery, Maple init-handshake or crypto setup, automatic mirroring of decrypted live login packets back into the existing selector packet inbox instead of requiring an external relay process for every real session replay, and low-frequency auto-refresh of discovery-backed bridge targets while the login scene is active so the built-in proxy can re-arm itself against newly discovered official login sessions after disconnects instead of staying pinned to the first resolved endpoint until the user restarts the command. The remaining gap is that the simulator still does not attach in place to an already-established Maple login socket, still depends on either loopback relay input or a simulator-owned proxy hop rather than owning authenticated upstream traffic transparently inside the running client, still approximates the client `ViewAllChar` request or response choreography with a simulator-owned delayed handoff rather than a true upstream request packet and server-fed chunk sequence when no packet-authored VAC roster is present, and still does not model the broader backend request or response session beyond the selector packet families it can currently decode | Channel switching is no longer a dead-end HUD affordance, selector population can now follow repeated client-shaped `WorldInformation` updates through either the explicit loopback inbox or a built-in official-session proxy bridge with discovery and local-port disambiguation, plus direct payload-byte decoding for the other selector packets, a more relay-friendly metadata-tagged parser, automatic discovery refresh that keeps the bridge pointed at the latest eligible official login endpoint during login-scene replay instead of requiring a manual restart after each disconnect, and a live `BtViewAll` path that now enters the existing expanded-roster seam through the same delayed request model the simulator already uses for world and channel picks. The remaining gap is narrowed to in-place live-session attachment, true upstream `ViewAllChar` packet choreography when no packet-authored roster is being replayed, and fuller authenticated backend ownership rather than missing selector surfaces, packet decoding, or a practical simulator-owned transport seam for replaying real selector traffic | world-selection and channel-selection UI layer, status-bar integration (`CUIWorldSelect::OnCreate`, `CUIWorldSelect::OnButtonClicked`, `CUIChannelSelect::OnCreate`, `CUIChannelSelect::Draw`, `CUIChannelSelect::OnButtonClicked`, `CUIChannelShift::OnCreate`, `CUIChannelShift::Draw`, `CLogin::OnCheckUserLimitResult`, `CLogin::OnSelectWorldResult`, `CLogin::OnViewAllCharResult`, `CUIWorldSelect::UserLimitResult`, `CLogin::OnWorldInformation`) |
| Partial | Inventory window parity | Legacy and Big Bang inventory windows now render right-aligned meso text, per-slot item icons and stack counts, WZ-backed grade and quality markers, disabled-slot overlays, active-slot highlighting, and Big Bang tab or expanded layouts, while the simulator also feeds meso and picked-up items into the inventory surface at runtime, resolves equip pickup icons through `Character/*/*.img` like the client asset families, respects `info/slotMax`-style stack splitting, merge limits, and pickup-capacity checks for stackable items, wires `BtGather` and `BtSort` into simulator-side stack compaction and ordering, shows shared client-style hover tooltips with cached item names, type labels, stack data, and descriptions across both legacy and Big Bang inventory layouts, and now keeps the shared inventory metadata resolver aligned with that same cache ordering so inventory-adjacent pickup and utility feedback surfaces read the display item name instead of the item-category label, supports client-shaped intra-inventory drag/drop for occupied-slot swapping, stack merging, trailing-empty-slot moves, plus a held-item cursor overlay in both layouts, and now carries client-shaped slot-unlock progression through per-tab slot limits that grow in 4-slot rows up to a capped 96-slot budget while `BtCashshop` routes directly into the simulator cash-service dialog and its `Extending Equip/Use/Set-Up/Etc Inventory` offers consume live meso balance to unlock additional rows instead of leaving capacity as a fixed 24-slot shell; right-click setup-chair items now route into the existing portable-chair runtime so inventory use can activate or clear owned chairs without falling back to chat commands, WZ-backed chair `info/recoveryHP` / `recoveryMP` values now tick against the seated player on a client-shaped 10-second cadence, pet-food items now honor `Consume/*/spec/*` supported-pet families plus the pet runtime's live fullness gate when selecting the target runtime, only consume when a compatible summoned pet can actually eat the food, and surface simulator-side failure feedback when no valid target is available instead of silently burning or dead-ending the click, `Consume/*/spec/moveTo` scrolls now consume through the inventory surface to queue either fixed-map warps or `999999999` nearest-town returns through the existing map-transfer seam instead of remaining unsupported right-clicks, and now also fail before inventory consumption when the current map cannot resolve a valid transfer target or the simulator has no transfer seam available instead of burning then locally refunding the scroll after a dead-end queue attempt. The same WZ-backed consume path now also applies client-shaped abnormal-status cures, timed morph consumables from `item/Consume/0221` `spec/morph` and weighted `spec/morphRandom`, temporary stat buffs from `pad` / `mad` / `pdd` / `mdd` / `acc` / `eva` / `speed` / `jump` plus the post-Big-Bang `indiePad` / `indieMad` / `indiePdd` / `indieMdd` / `indieSpeed` / `indieJump` aliases the client item data uses in `item/Consume/0202`, the same `Consume/0202` family’s flat max-vital aliases via `spec/indieMhp` and `spec/indieMmp` entries such as `2022789` and `2022814`, and the percentage max-vital buff family via `spec/mhpR|mhpRRate` and `spec/mmpR|mmpRRate` entries such as `2022221`, all through the shared player-status, avatar-transform, and buff runtimes instead of treating those right-clicks as dead inventory actions; that morph seam now also validates the WZ-backed transform before consumption without mutating the live avatar until the consume succeeds, so failed right-click paths do not leave a stale morph applied. That shared WZ-backed consume seam is now also reusable from the cash tab when an item exposes the same recovery/moveTo/morph/cure/buff `spec` schema instead of being hard-wired to `USE` only, and the corresponding `MobSkill.img/134` pain-mark disease now lands in the same player-status controller with WZ `hp` / `interval` / `count` timing so those cure items clear a live runtime effect instead of a dead flag | The remaining gap is narrowed to broader client-authenticated item-use rules for non-chair setup items, pet-feeding fidelity beyond the current supported-family plus fullness/runtime routing (notably command or closeness progression and exact official success or failure messaging), the still-wider long-tail consume-item matrix beyond the simulator's current HP/MP, flat/percent max-vital buffs, moveTo, cure, morph, timed disease, and direct-stat consume coverage, and stricter server-side validation plus packet-authored rejection semantics rather than missing inventory rendering, name-cached tooltip or feedback text, drag/drop, pickup-capacity, slot-progression, or the common local item-use loop itself | `InventoryUI.cs`, `InventoryUIBigBang.cs`, `UIWindowLoader.cs`, `AdminShopDialogUI.cs`, inventory/runtime integration (`CUIItem::Draw`, `CUIItem::OnMouseButton`) |
| Partial | Item maker / crafting window parity | The simulator now replaces the `ItemMaker` placeholder with a dedicated WZ-backed `Maker` window owner, exposes `BtStart` / `BtCancel` crafting flow, animates the maker gauge during crafting, seeds a starter ore inventory plus meso and catalyst stock, resolves weighted `randomReward` branches, blocks crafts whose fixed or random reward branch cannot fit in inventory, routes NPC talk overlays whose loaded `String/Npc.img` function text advertises item-maker families such as `Item Maker`, `Glove Maker`, `Shoemaker`, and `Toy Maker` into the dedicated crafting owner, and rebuilds the selector surface around client `CUIItemMaker` rules by deriving the visible `Etc/ItemMake.img` bucket set from the live job (`0` plus the matching `1` / `2` / `4` / `8` / `16` job bucket, or all five for admin-family jobs), regrouping those recipes into client-like category and item selectors instead of the old raw bucket picker, adding a leading `All` selector plus client-shaped aggregate pages for `Gem`, `Monster Crystal`, `Use`, `Setup`, and `Etc`, keeping the visible recipe and dynamic-category ordering in the same client/WZ insertion order that `GetMakableItem_ForJob`, `SetComboBox_Category`, and `SetComboBox_Item` build instead of locally resorting rows by level or title, hiding selector entries that fail the client-side precondition pass for `reqLevel`, `reqSkillLevel`, `reqItem`, `reqEquip`, and `reqQuest` before the detailed craft validation runs, applying a local hidden-maker-list pass so maker-family NPC launches such as glove, shoe, toy, and generic item-maker flows narrow the visible recipe set instead of only focusing the nearest matching row, now keeping the generic `Item Maker` launch aligned with the WZ/client family split by preserving generic maker equips and other non-glove, non-shoe, non-toy recipes instead of dropping every equip output from that launch context, and backing the owner with a simulator-side per-character maker progression store that tracks family-specific mastery levels for generic/glove/shoe/toy crafting, advances that state on successful crafts, persists it under the simulator AppData profile, uses that family mastery rather than the raw craft trait alone when evaluating `reqSkillLevel`, correctly reloads the per-character discovered-recipe list from that AppData profile across simulator restarts, keeps recipes that passed the selector precondition sweep visible on later visits even after transient `reqItem` / `reqEquip` / `reqQuest` conditions drop back out, aligns recipe hidden-vs-normal classification more closely with the client `CItemMakerInfo::RegisterTargetItem` split by honoring the WZ `hide` flag instead of treating every `reqItem` recipe as implicitly hidden, and surfaces the client-observed `DrawTargetItem` / `DrawRecipe` / `DrawGem` / `DrawCatalyst` / `DrawMeso` split more faithfully by drawing item-result and material icon rows through the live inventory/runtime icon seams, including a dedicated target-item header plus separate recipe, gem, catalyst, meso, and reward-pool sections instead of one collapsed material list. The owner also keeps the client-shaped `???` page alive whenever the active job or maker-family launch context has hidden recipes, treats those rows as a separate hidden list instead of dropping them out with the normal precondition sweep, obscures locked hidden entries in the row list, selector, detail header, and craft path until a local WZ-backed unlock pass (`reqItem`, `reqEquip`, or `reqQuest`) reveals them once, persists hidden unlocks in their own AppData-backed hidden-recipe bucket instead of merging them into the normal discovered-recipe set, restores that hidden unlock state across later visits or restarts, records hidden craft completions back into the hidden bucket, locally reveals and persists hide-only hidden rows once their WZ-authored persistent level/mastery gate passes so they do not remain permanently locked without a server profession session, and now carries discovery, hidden-unlock, and craft-completion persistence on the stable WZ recipe-row key (`bucket/outputId`) with legacy output-id fallback so `randomReward` recipes and later row-specific state round-trip correctly across visits or restarts. Focused simulator tests in `UnitTest_MapSimulator` now also cover the per-character progression-store persistence path for family mastery, recipe-key discovery, hidden unlock buckets, legacy fallback loading, and the maker-family launch-filter split. It still lacks the official server-authored hidden-list unlock decisions and packet/session model authored by the client `CItemMakerInfo` runtime, so the remaining hide-only cases still run on a simulator-side heuristic rather than real profession-state packets | Production and crafting progression now has a real client-backed launch path, client-shaped category and item selection, client-order recipe sequencing that matches the makable-item lists instead of a local level/title sort, job-aware recipe exposure, family-focused NPC launch selection whose generic `Item Maker` context now keeps generic equip recipes instead of collapsing to non-equip rows, AppData-backed family mastery plus recipe discovery that survives reload on stable WZ recipe-row keys, tighter precondition plus result-capacity validation, a client-backed hidden-vs-normal recipe split, a dedicated hidden `???` page whose locked rows stay obscured until locally revealed, client-shaped target/recipe/gem/catalyst/meso detail sections instead of one flattened requirement block, hidden unlock persistence that round-trips separately from the normal recipe-discovery list, and focused simulator tests that now exercise both recipe-key persistence/legacy fallback and the launch-family filter behavior. The remaining gap is narrowed to the official server-authored hidden unlock matrix and broader profession session/runtime the simulator still does not model | inventory/runtime integration, crafting UI layer (`CUIItemMaker::Draw`, `CUIItemMaker::OnCreate`, `CUIItemMaker::SetComboBox_Category`, `CUIItemMaker::SetComboBox_Item`, `CUIItemMaker::OnButtonClicked`) |
| Partial | Item upgrade / enhancement window parity | The simulator now registers a dedicated `ItemUpgrade` owner instead of a placeholder, loads Gold Hammer or Vicious Hammer style client art from `UIWindow2.img` or `UIWindow.img`, binds the window to the live `CharacterBuild` plus inventory runtime, seeds starter enhancement scroll stock so the loop is immediately exercisable, resolves eligible items from WZ-backed `info/tuc` upgrade-slot data instead of a pure slot-type guess, consumes enhancement scrolls from the inventory surface, tracks per-item remaining upgrade slots and success or fail counts, applies lightweight stat growth back into `CharacterBuild`, routes right-click activation from supported enhancement consumables in the `USE` and `CASH` inventory tabs plus eligible equips in both legacy and Big Bang equipment windows into the dedicated owner while preselecting the clicked consumable or equipped target, now lets NPC interaction overlays whose loaded `String/Npc.img` function text advertises item-upgrade, enhancement, hammer, potential, or cube families open the dedicated owner, broadens the local enhancement loop beyond the baseline normal or advanced scroll pair by seeding and resolving the client `2049300/1/3/4/5/6/7/8/9` enhancement-scroll family including 2-star through 5-star result variants with multi-star success gains and destroy-on-fail handling, now adds local potential-scroll plus Miracle Cube result handling through `2049400/1/6`, `2049407/8`, `2049700/1/2/3`, and the broader WZ-backed cash family `5062000/1/2/3/4/5/2100`, seeds and resolves `2049500/1`, `5534000`, and `5610000/1` so carved seals, Urete's Time Lab, and Vega's Spell consumables are immediately exercisable from inventory, now also seeds a Maple 8th Anniversary Crimson equip so the WZ-gated `5062100` Maple Miracle Cube flow can be exercised locally, reads `Item/Cash/0506.img/05062100/req` to enforce the official Maple Miracle Cube equip whitelist instead of letting that cash item roll on arbitrary gear, preserves existing revealed potential lines when a carved seal adds an extra line instead of rerolling the whole set, now mirrors the WZ-described Epic Potential Scroll 80% failure behavior by treating destruction as a 20% on-fail roll instead of unconditional breakage, keeps Golden and Revolutionary Miracle Cube fragment grants attached to the specific cash items that advertise them, now also grants the matching Enlightening Miracle Cube shard locally, enforces the WZ-described Golden Miracle Cube Rare-through-Unique limit, now routes `5610000/1` into a dedicated `VegaSpell` owner loaded from `UIWindow.img/VegaSpell` instead of keeping Vega inside the shared item-upgrade surface, reuses the client `backgrnd10` / `backgrnd60`, count glyphs, success/fail panes, and `EffectTwinkling` / `EffectSpelling` / `EffectArrow` / `EffectSuccess` / `EffectFail` families for a separate local cast-and-result presentation, and now also resolves WZ cash-item `info/path` owner art into dedicated `MiracleCube`, `HyperMiracleCube`, and `MiracleCube_8th` frame themes so Maple, Master/Amazing-family, and base Miracle Cube flows no longer stay on the Gold Hammer enhancement shell. This run also activates the WZ-authored Clean Slate / Reverse Scroll family (`2049000+`) through `info/recover`, `info/success`, and `info/cursed`, adds Innocence Scroll handling for `2049600/1/4` so locally applied upgrade bonuses and consumed slots reset while revealed potential is preserved, and brings the Golden Hammer and Vicious' Hammer families (`2470000/1/2`, `5570000`) onto the same owner with WZ-backed success rates plus the client-described usage rules that Golden Hammer and Vicious' Hammer are mutually exclusive, Vicious' Hammer is capped at two uses, and the Horntail Necklace family is excluded from Vicious' Hammer. It now also reads the authored `String/Cash.img/5610000/1` Vega descriptions instead of hardwiring the modifier pair to the enhancement-scroll family, seeds the matching `String/Consume.img` Vega-enabled `2040759` and `2040760` scrolls into starter stock, parses their WZ-authored `10%` versus `60%` base-rate text into local enhancement consumables, derives Vega’s `30%` and `90%` outcomes from the same cash-item descriptions, filters the dedicated `VegaSpell` owner down to equipment that actually has a compatible Vega-marked scroll in inventory, and blocks mismatched target equipment by parsing the scroll name’s authored target family instead of letting Vega browse or cast against arbitrary upgradeable gear. The dedicated item-upgrade owner now also consumes the WZ-authored `MiracleCube`, `HyperMiracleCube`, and `MiracleCube_8th` `Effect` and `BtOk` / `BtCancel` families so cube themes no longer only swap frame art: local cube attempts now lock the active cube theme through a staged cast/result presentation before returning to the reusable owner flow. | Progression-sensitive item workflows now have a visible dedicated enhancement surface, live inventory consumption, client-shaped inventory, equip, and NPC launch shortcuts, a broader enhancement-scroll and potential-scroll result matrix, WZ-backed Maple-cube whitelist enforcement for the `5062100` family, additional local shard-result coverage, preserved seal behavior, WZ-backed MiracleCube-family frame swaps keyed from cash-item owner paths, WZ-backed MiracleCube-family effect/button choreography for staged local cube casts, a separate WZ-backed Vega cast/result owner instead of treating Vega as only another modifier inside `ItemUpgrade`, WZ-backed Vega 10%-vs-60% modifier routing that now resolves against authored Vega-enabled scroll descriptions plus live preview rates, and now also WZ-driven Clean Slate, Innocence Scroll, Golden Hammer, and Vicious' Hammer support with locally tracked recovered-slot and hammer-use state. The remaining gap is narrowed to unsupported server-authored item-result families outside this now broader local scroll/cube/recovery/reset/hammer plus Vega-marked-scroll set, packet-authored Vega or item-result session ownership and cooldown constraints, and the exact server-fed accept/reject or post-result reconciliation that still surrounds official enhancement and cube workflows | inventory/runtime integration, enhancement UI layer (`CUIItemUpgrade::Draw`, `CUIItemUpgrade::OnButtonClicked`) |
| Partial | Equipment window parity | Legacy and Big Bang equip windows now share `EquipSlotStateResolver`-driven disable handling for overall-vs-pants conflicts, beginner subjob and two-handed shield gating, Monster Riding slot gating, expired-item overlays, and zero-durability overlays, stay bound to the live `CharacterBuild` used by the avatar preview, and support simulator-side drag or drop between compatible character slots with held-item cursor feedback plus compare-style hover messaging while an equipped item is being moved; the Big Bang owner now also accepts inventory-to-character equips on the live character tab, returns displaced equips back to inventory with their item icons, stops mutating cached loader templates when slot moves retarget cloned `CharacterPart` instances, maps the heart slot onto the live android-heart family, and covers the broader client-facing slot families already exposed by the window art for pendant1 vs pendant2, pocket, badge, shoulder, android, android heart, medal, and belt-backed character panes. `CharacterBuild` now preserves concealed base equips under visible cash equips through a dedicated hidden-equipment layer, `CharacterLoader` no longer drops `LoginAvatarLook` hidden entries when a visible cash item already owns that slot, `LoginAvatarLookCodec` round-trips that visible-versus-hidden split, build stat totals plus weapon-sensitive shield gating can still see the concealed stat-bearing item, and both equip owners now surface the hidden base item in tooltip and drag messaging while restoring it when the visible cash item is moved off the slot. `CharacterBuild.Clone` now also deep-clones both visible and hidden equipment layers, so preview, remote, or other cloned-build consumers cannot retarget the live/shared `CharacterPart` instances when a local slot move or cash-item removal mutates `part.Slot` or restores the concealed base layer. The Big Bang owner now also loads the full authored dynamic character-pane chrome set from `UIWindow2.img/Equip/character` through one WZ-backed seam, which in this client resolves to `cashPendant` plus `charmPocket`, and the same live build-state seam now also disables `Pendant2` until pendant-slot expansion is active and disables `Pocket` until the charm/unlock rule is satisfied instead of only toggling the authored chrome while still allowing local equips into those slots. Both local slot-to-slot moves plus inventory-to-character equips still route through `CharacterBuild.PlaceEquipment` so concealed base items stay hidden under cash equips and only the correct displaced base layer returns to inventory, and the Big Bang character tab now rejects invalid local equip attempts with explicit blocked messaging for disabled slots, wrong slot families, item-load failures, unmet level or stat or fame or job requirements, invalid swap targets that cannot return to the source slot, and battlefield-blocked items. The Big Bang owner now also routes character-to-character, inventory-to-character, and character-to-inventory moves through an owner-mediated `EquipmentChangeRequest` seam that stamps request ids and ticks, queues a simulator-owned delayed completion before the build mutates, keeps at most one character-pane equip request pending at a time, locks the live source inventory slot under that request id until the queued result resolves, revalidates the live inventory slot or equipped source item before applying the move, rechecks source and target slot disable state plus move-time equip requirements and battlefield restrictions against the current `CharacterBuild`, and only then commits the build mutation that the window manager finishes by removing or returning inventory icons on completion. This pass narrows the request-side gap further again: the same seam now also stamps a Big Bang owner session id plus a deterministic live `CharacterBuild` equipment-state token onto each local request, rejects stale result polling when the owner session has changed, rejects delayed apply when the active character or live visible/hidden equipment state changed before completion, and returns the resolved build-state token with the delayed result so the simulator-owned request path is no longer keyed only by item id plus slot position. Focused `UnitTest_MapSimulator` coverage now also guards the request-state token seam in addition to the earlier clone-retarget and hidden-base-restore regressions. The remaining gap is narrower again: true packet-authored async accept or reject ordering, opcode-specific session ownership rather than the simulator-owned owner-session stamp, real server-fed post-apply state reconciliation, and client-exact network sequencing around those stamped requests still are not mirrored, so this remains a simulator-owned authoritative shim rather than the real client packet workflow | Equipment-heavy testing now has visible disable and break-state feedback on the actual character equipment surface, a client-shaped held-item interaction path, live inventory-to-equip routing on the Big Bang character page, broader slot-family coverage for the non-rendered accessory families already present in the client window art, the full shipped authored pendant/pocket chrome family loaded through the same WZ-backed seam that drives its placement, real local lockout for still-unavailable `Pendant2` and `Pocket` slots instead of chrome-only hints, a client-shaped visible-versus-hidden equip data path instead of dropping concealed base gear from avatar-look and equipment-window resolution or returning the wrong concealed base item during local swaps, cloned-build isolation for slot-retarget and cash-layer restore paths, a queued request-id or tick seam that delays local equip completion until the next simulator-owned acceptance pass, a locked pending-source inventory slot while that local equip request is outstanding, and a simulator-owned owner-session plus build-state stamp that rejects stale delayed completions when the live character or equipment layers changed mid-flight. The remaining gap is narrowed to true packet-authored session ownership, server-authored async ordering, and post-apply reconciliation rather than missing authored chrome, immediate local acceptance sequencing, the cloned-build mutation hazard, or stale local delayed results being keyed only by request id and item id | `EquipUI.cs`, `EquipUIBigBang.cs`, `CharacterBuild`, `CharacterLoader`, `CharacterAssembler`, `EquipSlotStateResolver.cs`, `UIWindowLoader.EquipmentAndStats.cs`, `MapSimulator.EquipmentWindowParity.cs` (`CUIEquip::Draw`, `UIWindow2.img/Equip/character`) |
| Partial | Pet, dragon, and android equipment tabs | The Big Bang equip window now loads the WZ-backed companion pane art for `pet`, `dragon`, `mechanic`, and `Android`, keeps the character equipment surface visible while those panes are open, highlights the active tab buttons, populates the Multi Pet pane from the live `PetController` with per-pet icons plus auto-loot markers and tooltips, seeds dedicated dragon and android companion equipment runtimes so those pages render live equipped-item icons and tooltips on the client slot geometry, now also seeds a dedicated mechanic equipment runtime from the client `Character/Mechanic` asset family so the mechanic page draws live `ENGINE`, `FRAME`, `TRANS`, `ARM`, and `LEG` icons with slot-specific tooltips instead of stopping at an empty-state stub, and now accepts drag/drop from the live equip inventory into the pet, dragon, mechanic, and android panes with slot-family validation, client-shaped android overall-vs-pants gating, occupied-slot replacement back into inventory, a WZ-backed `Character/PetEquip` loader for live pet accessory icons and tooltips, seeded starter pet accessory inventory entries, and drag-back-out handoff from every companion pane into the live inventory window; pet slots can now accept simulator-side pet accessories, render the equipped accessory in-slot with pet-aware tooltip context, and swap that accessory across active pets instead of remaining a pet-icon-only stub, and the shared companion validation seam now also reads WZ `reqLevel`, `reqJob`, `reqSTR`, `reqDEX`, `reqINT`, `reqLUK`, and `reqPOP` metadata for pet/dragon/mechanic items, surfaces reject messages through the same blocked-equip notice path the main equip window already uses, preserves pet-specific compatibility checks during pet-to-pet accessory swaps, requires both an equipped android and equipped heart before the Android page accepts gear instead of treating the android slot alone as sufficient ownership, resolves android heart-family compatibility against the WZ `String/Eqp.img/Eqp/Android` catalog so duplicate heart item ids that share a client family name still satisfy the same android-family requirement instead of relying on a name-only heuristic, now also mirrors the client `CUIEquip::ToggleMechanicEquip` gate by hiding the mechanic tab and rejecting mechanic ownership until the build is both on a Mechanic job path and at least level `50`, and now revalidates the live source equip-inventory slot before delayed companion applies while rejecting companion equips that would overflow the equip inventory with displaced returns such as android overall swaps that evict both the current clothes and pants. The remaining gap is now narrower: the simulator still does not mirror the client's deeper cash-state and account/character ownership rules or the true packet-authored accept/reject/result flow that surrounds official companion equipment changes beyond this local pending-request revalidation and displaced-slot-capacity guard | Companion tab clicks are no longer dead-end toggles, the pet page now participates in the same inventory-backed equip loop as the other companion panes, invalid companion drops now explain why they were rejected instead of failing silently, android ownership gating now includes WZ-backed heart-family matching rather than a freeform name comparison, the mechanic page no longer appears before the client-owned level-50 unlock, and delayed companion equips now fail cleanly when the source inventory slot changed or the displaced returns would overfill equip inventory. The remaining gap is narrowed to deeper cash/ownership semantics and true packet-authored companion result handling rather than missing pet equip handling, an empty mechanic page, one-way drag/drop, stale delayed source-slot commits, android-overall overflow cases, the mechanic unlock gate, or the android heart matrix itself | `EquipUIBigBang.cs`, `UIWindowLoader.cs`, `PetLoader.cs`, `PetController.cs`, `CompanionEquipmentController.cs`, inventory/window manager integration (`CUIEquip::Draw`, `CUIEquip::ToggleMechanicEquip`) |
| Partial | Ability and stat window parity | The simulator already draws both legacy and Big Bang stat windows, supports AP assignment, job-biased auto-assign, detail-panel toggles, build-backed guild/fame/EXP text, hands/critical detail values, per-stat AP cap enablement, and Big Bang open-vs-close detail button visibility; the stat rows now also resolve equipped stat bonuses through `CharacterBuild`, the detail pane now derives attack/defense, accuracy/avoidability, hands, critical, speed, and jump from live build totals instead of static placeholder fields, `SkillManager` now seeds non-hidden passive books alongside active skills for the current job path, and `CharacterBuild` now folds passive plus live buff attack/defense, magic attack/defense, accuracy, avoidability, critical-rate, speed, jump, and skill-driven max HP/MP bonuses into those totals so the stat windows react to learned skills rather than gear alone, while Big Bang HP/MP AP clicks still use class-aware gain ranges with a 30,000 cap instead of a flat `+20`; the displayed physical and magical damage rows now also use a client-shaped min/max average where mastery scales the primary term instead of naively scaling the full max value, dagger formulas now split thief-vs-non-thief primary stats instead of treating every dagger as a thief weapon, thief-family dagger/katara/cane/claw damage now also restores the live `STR + DEX` secondary term instead of dropping the thief weapon STR contribution from the displayed range, extended Cygnus/hero/Resistance job branches now map back onto their base warrior or mage or archer or thief or pirate stat archetypes for accuracy, avoidability, defense, and Big Bang HP/MP AP growth, `CharacterBuild` now recognizes cane, dual bowgun, and cannon weapon families in the displayed damage surface, and `SkillLoader` plus `SkillManager` now use `String/Skill.img` mastery names together with broader passive alias-field parsing (`ar`, `accX`, `padX`, `madX`, `pddX`, `mddX`, `er`, `evaX`) plus enhanced-stat aliases (`epad`, `emad`, `epdd`, `emdd`, `emhp`, `emmp`) so newer mastery, expert, blessing, shield-style, and avoidability or accuracy books can feed the stat windows instead of being dropped when WZ does not use the older base property names; `SkillManager` now also falls back to `String/Skill.img` description text for generic and specialized mastery books whose weapon family is not encoded in the display name alone, so WZ-authored books such as Fighter/Page/Spearman `Weapon Mastery`, Aran `High Mastery`, and Battle Mage `Barricade Mastery` once again drive the displayed mastery and damage floor for the correct equipped weapon families instead of being stranded behind name-only matching; the mastery resolver now also normalizes punctuation and broader family wording from `String/Skill.img`, so description-authored variants such as `Cross Bow`, `Throwing Stars`, `Dual Bowgun(s)`, `Hand Cannon`, and `Pistol` text can still bind to the correct weapon family instead of depending on one exact title token; the mastery resolver now also respects narrower WZ-authored family text instead of broadening those books past their stated scope, so one-handed-only blunt/axe books such as Battle Mage `Barricade Mastery` no longer leak onto two-handed axes or blunts, and explicit staff-only mastery text no longer resolves against wands while generic `Spell Mastery` books still cover both magic-weapon families; the same mastery path now also respects WZ-authored conditionals such as Paladin `1220010 Advanced Charge` only while a live charge buff is active, covers stringless fallback cases such as `skill/5110.img/skill/51100001/common/mastery` and `skill/570.img/skill/5700000/common/mastery` by mapping those books back onto their observed knuckle and gun families instead of dropping them when `String/Skill.img` has no matching entry, and now also treats active mastery-bearing runtime owners such as Dark Knight `skill/132.img/skill/1321007/common/mastery = 2*x` `Beholder` as live mastery sources only while the summon is actually present instead of ignoring non-passive WZ mastery entirely; `SkillLoader` now also evaluates WZ-authored max-HP/max-MP rate fields such as `skill/100.img/skill/1000006/common/mhpR = 2*x`, `skill/200.img/skill/2000006/common/mmpR = 2*x`, `skill/510.img/skill/5100009/common/mhpR = 2*x`, and buff-side `indieMhpR` / `indieMmpR` branches such as `skill/2411.img/skill/24111003/common/indieMhpR` so the stat windows include percentage-based HP/MP books instead of only flat `emhp` / `emmp` bonuses, and `CharacterBuild.TotalHands` now also includes equipped `incCraft` bonuses instead of dropping glove or gear hands from the detail panel. Mechanic's `Extreme Mech` bonuses now only surface while the live mech mount is active instead of behaving like an unconditional passive, weapon-specific mastery no longer resolves while no weapon is equipped, and non-Mechanic alias-backed buffs such as Onyx or Divine Shield style `e*` stat lines now also contribute to the live totals, but the simulator still does not mirror every exact post-Big-Bang coefficient or the remaining long-tail conditional mastery and weapon-formula branches for the unresolved per-job edge cases beyond the newly covered charge-state, active-summon mastery, stringless-mastery, punctuation-normalized family-text, one-handed-only, staff-vs-wand, and thief-secondary-stat cases | Visible stat windows now expose runtime-backed totals from equipped gear, learned passive skills, active buff bonuses, class-sensitive HP/MP AP growth across both classic and extended job branches, WZ-backed percentage HP/MP skill boosts, the missing `incCraft` hands contribution, a broader client-shaped mastery-aware damage surface for cane, dual-bowgun, cannon, and thief-family secondary-stat users, WZ-backed `ar`/`er` alias support for accuracy and avoidability books, and tighter WZ-text mastery family matching for punctuation or pluralized family text alongside the earlier charge-aware, active-summon, stringless, one-handed-only, and staff-versus-wand handling. The remaining divergence is narrowed to the exact coefficient matrix and the still-unimplemented long-tail mastery or formula rules beyond these rate-backed stat surfaces, alias-backed passive totals, deeper normalized description-backed mastery family mapping, charge-state and summon-state mastery gating, one-handed-only or staff-only family narrowing, and Mechanic mech-state gating | `AbilityUI.cs`, `AbilityUIBigBang.cs`, `CharacterBuild`, `SkillManager`, `SkillLoader`, `UnitTest_MapSimulator/AbilityStatWindowParityTests.cs`, stat/runtime layer (`CUIStat::Draw`, `CUIStat::DrawTextA`) |
| Partial | Character info / profile window parity | The simulator now loads a dedicated WZ-backed `UserInfo` window from `UIWindow.img/UserInfo` or `UIWindow2.img/UserInfo/character`, routes the status-bar `BtCharacter` button into that owner, keeps the primary profile page build-backed with name/job/level plus world-rank and job-rank plus guild/alliance fields, and the Big Bang owner now switches into WZ-backed ride, pet, collect, and personality subpages that render simulator-side mount state, active pet data, collection summaries, and optional trait values instead of leaving those buttons decorative. The owner now also loads the page-specific `BtException`, `BtArrayName`, and `BtArrayGet` controls from `UIWindow2.img/UserInfo`, hides primary action buttons outside the character page so the per-page button surface matches the selected subpage more closely, forwards `BtParty` into the existing social-list owner instead of leaving it as a pure message stub, opens the WZ-backed `exception` popup with local register/delete plus meso-filter toggles instead of leaving that client-owned control path dead, and now uses the character-page `BtPopUp` / `BtPopDown` controls in the two modes the row needs: local-owner popup selection for the `item` / `wish` auxiliary lists, and the recovered inspected-target popularity up/down path from `CUIUserInfo::OnButtonClicked` when the owner is showing a remote target. The `productSkill` loader now consumes the full `UIWindow2.img/UserInfo/character/productSkill` family, including the previously omitted `9204` recipe icon, so the character page can light the maker, glove, shoe, toy, and recipe ledger icons from the live simulator-side maker progression snapshot instead of leaving the recipe slot dead. That same progression snapshot still replaces the old equip-count collection summary with maker, glove, shoe, toy, craft-count, and discovered-recipe metadata, while the collection page treats `BtArrayGet` as a local claim action and keeps the collect icon routed into the dedicated book owner. The primary rank rows now consume packet/account-backed previous-rank state through a target-aware `RankDeltaSnapshot` seam, so stored world/job rank movement can follow whichever build the owner is currently showing instead of always falling back to the active local player. The personality page still consumes the fuller WZ-backed `Tooltip` family from `UIWindow2.img/UserInfo/personality/Tooltip`, including the charm `collection/0` body and numeric glyph set, so hover state can render the client art for pocket-slot charm collection messaging instead of falling back to plain text only. This row also keeps the real inspected-target seam inside the same owner: `/remoteuser inspect <characterId|name>` can reopen `CharacterInfo` against a shared-pool remote actor build, the character page draws an inspected-target banner, local-only `item` / `wish` popups are suppressed for that remote context, `BtParty` can queue a target-specific party invite into the existing social-list runtime instead of only opening the local list, `BtTrad` can retarget the trading-room shell to the inspected actor instead of always opening an anonymous local preview, and `BtFamily` now drives a family-chart request preview for the inspected name through the existing family-chart runtime rather than always falling back to the local chart. The remaining gap is now narrower: truly server-authored remote collection/medal/charm payloads, remote channel or field ownership beyond the current shared-field inspect seam, and the exact server accept/reject or result workflows behind party, trade, family, and popularity requests are still approximated rather than mirrored exactly | Character-facing information now has its own launch path, a broader button-driven surface distinct from the stat window, WZ-backed popup navigation and personality-hover art, packet/account-backed rank-delta presentation that now follows the active inspected build, the full WZ-backed maker `productSkill` icon family including the recipe slot, and an inspected-remote branch that reuses the same `CUIUserInfo` owner for target-aware party, trade, family, popularity, and per-character collection snapshot presentation instead of keeping those buttons or subpages local-only. The remaining gap is concentrated in truly server-authored remote payload/result handling rather than dead `BtPopUp`/`BtPopDown`, a missing recipe icon, rank deltas that only followed the local player, or remote request buttons and collect rows that still ignore the inspected target entirely | character/profile UI layer, player metadata presentation (`CUIUserInfo::Draw`, `CUIUserInfo::OnButtonClicked`) |
| Partial | Book / collection window parity | MapSimulator now registers a dedicated `BookCollection` owner backed by `UIWindow.img/UIWindow2.img/Book`, uses the client-owned `BtPrev`, `BtNext`, and `BtClose` controls at the IDA-recovered `CBookDlg::OnCreate` button positions, loads the exact shared `UIWindow(.2).img/UtilDlgEx/dot0` and `dot1` canvases that `CBookDlg::OnCreate` binds for its spread markers, and keeps the same two-page paging cadence that `CBookDlg::OnButtonClicked` applies by moving in two-page increments and closing through the owner-local close path instead of behaving like another generic placeholder. The runtime now feeds the owner from the live character build plus the existing item-maker progression snapshot and Monster Book snapshot, opens it from the `UserInfo` collect surface, and renders a dedicated two-page collection spread on the real `Book` shell instead of reusing the Monster Book chrome: each page now composes title/subtitle/footer text, row separators, and six client-shaped ledger rows for overview, crafting-family progression, traits, equipment ledger entries, and discovered recipe pages, while the draw path also splits the left/right page-number text the way `CBookDlg::Draw` does and applies a dedicated twelve-style text matrix so collection labels, values, accent rows, warning rows, success rows, and footer/status text no longer collapse to one generic font treatment. The remaining gap is narrower but still present: the simulator still approximates the client's exact `CT_INFO` record ordering, glyph metrics, and native `IWzFont` objects, and it still lacks the true server-/packet-authored collection dataset plus the full `CWvsContext::CloseBook` lifecycle that would drive the official owner end to end. | This is now a real stand-alone utility surface rather than dead backlog theory: the simulator has the separate paged owner, the exact client dot-marker path, explicit spread-button flow, client-shaped left/right page counters, a dedicated collection-page compositor on the proper `Book` shell, and a broader style matrix for the page body instead of a placeholder counter-only spread. The remaining divergence is narrowed to exact native text/layout fidelity and live client-owned collection data rather than total owner absence, a mismatched shell, or a one-font generic page body. | collection/book UI layer (`CBookDlg::OnCreate`, `CBookDlg::Draw`, `CBookDlg::OnButtonClicked`) |
| Partial | Quest log window parity | The simulator quest windows now render live runtime-backed lists for available, in-progress, completed, and recommended quests, instantiate the client `Tab/enabled` / `Tab/disabled` category buttons plus `BtMyLevel` / `BtAllLevel` filters from `UIWindow.img` or `UIWindow2.img`, keep selection synchronized with quest state mutations, color rows by accept/complete readiness, expand the list owner to use the dedicated quest-log shell instead of a simulator-only inline detail pane, preserve item-backed requirement/reward hover popups by moving them onto the dedicated detail owner, now load and honor the legacy `UIWindow.img/Quest/BtDetail` list-shell button so pre-Big-Bang quest browsing uses a client-shaped select-then-button cadence instead of forcing the Big Bang-style double-click open path, now also mirror the client `CUIQuestInfo::OnTabChanged` reselection cadence more closely by preferring suitable-level quests on the available tab, recently updated or recently viewed quests on the in-progress tab, and top entries when the prior selection no longer survives a tab or filter rebuild, now expose the client quest-detail location button families through the existing detail owner by routing NPC, mob-demand, and item-demand fallback cases into the world-map search owner instead of treating every specialized case as a pure NPC locate, now also mirror the client demand-item query split more closely by parsing WZ `Check.img/.../item/*/secret` flags, suppressing hidden demand items from local world-map results, preferring visible outstanding demand items when choosing the detail-owner/world-map item target, and seeding a packet-shaped current-field item-query result set when the detail owner asks for demand-item guidance, now also split the Big Bang quest-detail shell back onto the client button families by reserving `UIWindow2.img/Quest/quest_info/BtQuestDeliveryAccept` and `BtQuestDeliveryComplete` for delivery-specific guidance while primary accept/complete return to the shared `Quest/BtOK` art, surfacing item-backed delivery hints through those dedicated buttons and routing their cash-item fallback into the existing quest-delivery or Cash Shop owners with the client hardcoded cash-item ids `5660000` / `5660001` and shop migration SNs `50200218` / `50200219`, now keep a dedicated simulator-side delivery phase on the quest-detail state so the Big Bang delivery buttons, packet-owned delivery snapshots, and packet fallback rows no longer disappear as soon as the first relevant item count is satisfied locally, and now preserve the relevant start/end delivery item metadata on the detail state even after counts are met so item-backed accept/complete handoff flows stay visible through the dedicated quest-delivery seam instead of being inferred only from the generic primary action. It also still parses WZ quest `area` ids into readable quest categories so the Big Bang list shell can rebuild each tab through persistent per-tab category option filters driven by the client `BtMax` / `BtMin` controls instead of treating category changes as a no-op, loads the full `UIWindow2.img/Quest/icon_info` family so the legend owner uses the client outer frame, inner sheet backing, and `Sheet/*` compositor, replaces the simulator-only ON/OFF checklist hitboxes with generated category-button slots that rebuild per tab/page, preserve per-tab hidden-category state, sort by the client category index instead of snapshot encounter order, suppress the client-skipped category bucket recovered from `CUIQuestInfo::LoadData` (`area == 51`), sit inside the WZ `BtMax` / `BtMin` shell more like the client-created area-button lifecycle, and use a closer Maple-style beveled generated skin plus panel anchoring off the Big Bang list pane and button geometry instead of flat placeholder rectangles. The remaining gap is narrower: the simulator still does not mirror the client's exact pixel art or final placement math for those generated category buttons, server-authored item-query map results beyond the simulator's local current-field fallback, the true server-supplied `QuestInfo::nDeliveryType` / packet ownership behind those delivery buttons, or the last demand-rect-specific delivery branches behind the most specialized quest-detail buttons | Quest interaction now has a usable standalone log that reflects the same runtime state as NPC quest overlays, uses WZ-backed quest categories instead of a custom tab strip, follows the legacy detail-button cadence where that WZ shell exists, automatically reselects client-shaped quest entries when tabs or category filters rebuild, preserves the client split between generic quest actions and the dedicated Big Bang delivery-button family, keeps delivery-specific buttons and packet-owned delivery rows alive on a dedicated quest-detail delivery phase instead of dropping them once the local item-count heuristic changes, now orders category toggles by the client category index while omitting the client-suppressed area-51 bucket, and keeps the detail-owner item target aligned with the visible demand-item query path before falling back to hidden-item or NPC guidance. The remaining divergence is narrowed further to pixel-exact generated category-button art/placement, true server-authored demand-item query resolution, exact packet-owned delivery-type ownership, and the last specialized demand-rect delivery branches rather than tab reselection, client-skipped category suppression, secret-item filtering, or every locate button collapsing to the same NPC-only path | `QuestUI.cs`, `QuestUIBigBang.cs`, `QuestDetailWindow.cs`, `QuestRuntimeManager.cs`, `UIWindowLoader.SkillsAndQuests.cs`, NPC/UI bridge (`CUIQuestInfo::Draw`, `CUIQuestInfo::OnButtonClicked`, `CUIQuestInfo::OnTabChanged`, `CUIQuestInfoDetail::OnButtonClicked`) |
| Partial | Quest detail window parity | The simulator now exposes a dedicated `QuestDetail` owner backed by the quest runtime, reuses the client `Quest/quest_info` frame family when present, opens from quest-list selection, renders separate summary/requirements/rewards sections with WZ-backed summary headers plus gauge art, now also draws the Big Bang `UIWindow2.img/Quest/quest_info/summary` lower-panel canvas and the matching `tip` canvas when the delivery inset is active instead of leaving those client surfaces unloaded, shows color-coded requirement rows, now also switches incomplete requirement rows onto the client `prob` demand-strip family while completed/reward rows keep the `select` strip family when those canvases are present, draws reward item icon rows through the live item-icon loader, switches accept/complete/track/give-up controls across the client quest-detail button families when that art is present, supports previous/next quest navigation within the active tab, wires standalone accept or give-up actions plus completion guidance distinct from the list shell, keeps the runtime-backed tertiary locate-NPC action visible through the quest-detail owner, reuses legacy `BtGotoNpc` and `BtMarkNpc` families when present while falling back to the Big Bang `BtNPC` family, now adds a dedicated mob-locate action for incomplete mob demands and routes it into the world-map search owner so quest-detail delivery guidance can use packet-authored quest-guide maps when present instead of staying pinned to the current field, lays out four-button delivery or completion cases without collapsing them back into the older two-button shell, draws quest notice banners plus animated notice icons from the client quest notice families when that art is present, pushes demand/reward count text into separate runtime fields so the detail owner can draw native-width `select` / `prob` strips with split body-vs-count composition, right-aligned counters, live item icons, and tighter client-shaped `253px` content bounds, now matches the client `GetScrLogLen` and delivery-inset clip math for the detail and summary panes, and feeds the world-map owner with explicit quest-target overlay summaries outside the older search-result-only seam while reusing `UIWindow2.img/QuestGuide/QuestMark` so overlaid maps light up with the client quest-guide marker animation in both the world-map list and search panes. The remaining gap is narrower: the simulator still does not reproduce the client's full CT-array compositor from `CUIQuestInfoDetail::Draw`, the exact client in-map marker placement/toggle behavior beyond the current row-marker seam, the time-limit branch that also drives the client's delivery-height inset, or the deepest specialized delivery-button packet and cash-item request flows (`SendConsumeCashItemUseRequest`, commodity migration, and server-driven delivery results) behind the rarest quest-detail buttons | The simulator now exercises the client's split between quest browsing and per-quest inspection with client-shaped notice chrome, the actual Big Bang summary-panel and delivery-tip canvases, WZ-backed incomplete-vs-complete demand row art, client-shaped scroll clips, separate NPC and mob world-map guidance, explicit world-map quest overlays plus animated quest-guide markers, and broader delivery/completion button layouts; the remaining gap is concentrated in the final CT-layout fidelity, the exact in-map quest-marker/toggle behavior, the client time-limit inset branch, and the deepest delivery/world-map packet branches rather than in missing owners, missing locate-button paths, or unloaded `quest_info` panel chrome | `QuestUI.cs`, `QuestUIBigBang.cs`, `QuestDetailWindow.cs`, `QuestRuntimeManager.cs`, `WorldMapUI.cs`, quest-detail interaction layer (`CUIQuestInfoDetail::Draw`, `CUIQuestInfoDetail::OnButtonClicked`) |
| Partial | Quest alarm / notifier window parity | The simulator now replaces the `QuestAlarm` placeholder with a dedicated WZ-backed tracker built from `UIWindow.img/UIWindow2.img/QuestAlarm`, supports minimize/maximize toggles, title counts, live in-progress quest rows with completion-state coloring, row selection, per-row dismiss buttons shaped from the client `BtDelete` art, double-click routing into the quest detail flow, and now reuses shared `UIWindow2.img/Quest/quest_info` chrome for selection, incomplete `prob` strips, and progress-gauge rendering while surfacing negative quest `act/money` costs as live meso progress rows and rewarding or consuming mesos against the bound inventory runtime during quest start or completion. The quest-alarm runtime also now tracks recent quest-state, mob-kill, and tracked-item updates so the client `BtQ/ani` alert frames and sky-blue recent-update emphasis can light up before `BtQ` falls back to the normal quest-log toggle path, and opening a quest detail now acknowledges that quest’s recent-update alert so viewed quests stop holding the alert route open for the rest of the timer window. The owner now follows the client-recovered five delete-slot layout more closely by exposing five visible quest titles, resizing the maximized frame to the active selected-demand stack, and adding a local delete-all header action with `Delete`/`Shift+Delete` shortcuts on top of the existing per-row dismiss path. The `BtAuto` toggle now drives a local auto-register mode instead of acting as selection-only chrome: auto-registered active quests can be locally deleted into a persisted hidden set, manual quest-detail tracking pins quests into a persisted tracked set even when auto-register is off, those manual registrations now preserve their local pin order instead of being re-sorted by the snapshot title ordering, the effective registered list is still clamped to the client-backed five-quest limit from `string/ToolTipHelp.img/Game/Button/QuestAlarm`, sixth manual registrations now surface the authored tooltip-backed cap sentence instead of a simulator-only warning, and `Delete all` now clears the full local registration set under that cap instead of only the visible subset. The owner restores per-character auto mode plus minimized state from AppData across simulator restarts. The selected quest row now composes more like the recovered client `CUIQuestAlarm::Draw` path by keeping non-selected quests collapsed to 18px title bands, expanding only the selected quest into a 150px demand rect, and drawing all requirement or meso rows with right-aligned counters and `select` versus `prob` strip families instead of rendering every visible quest as a full multi-line simulator card. The owner now also follows `CUIQuestAlarm::OnButtonClicked` more closely when the local tracker is empty or cleared: `BtMax` refuses to expand and surfaces a chat notice when there are no registered quests, `Delete all` clears the local registration set and closes the owner, title rows clamp against the client-shaped pixel width instead of a fixed character count, and the demand-rect compositor now prefers delimiter-aware wraps from the selected 150px band before falling back to raw character splits so slash-, hyphen-, and colon-heavy simulator text stays inside the client band more like the recovered owner instead of being chopped only by blunt token width. The remaining gap is that this is still a simulator-side approximation rather than the client's packet-driven `CUIQuestAlarm` registration path, so exact server-authored pin/delete semantics, the full official auto-register rule matrix behind `BtAuto`, the exact StringPool-driven label text and font choices beyond the tooltip-backed cap copy, and the last CT-authored spacing or font micro-rules inside the demand rect are still not mirrored exactly | Active questing now has a persistent standalone progress tracker with animated quest-alert feedback, row-local and owner-wide dismiss affordances, a client-shaped five-slot registration cap instead of simulator-only paging, richer WZ-backed progress chrome, recent-update emphasis that now clears when the quest is actually viewed, a tooltip-backed cap notice sourced from the authored quest-alarm help text, and a per-character persisted auto-register/pinned-tracking model whose manual registrations keep a stable local pin order instead of being re-sorted by the quest snapshot. The demand rect also now wraps delimiter-heavy issue and summary text more like the client owner before falling back to hard character splitting. The remaining gap is now narrowed further to packet-authored registration semantics, the official `BtAuto` rule matrix, and the final client-only StringPool/font/compositor polish rather than to missing persistence, sticky viewed-alert routing, unstable manual pin order, an over-permissive empty maximize path, absent delete-all close behavior, fixed-character title and body wrapping, or over-cap local paging beyond the client tooltip limit. | progression/UI layer, quest runtime, demand rendering (`CUIQuestAlarm::Draw`, `CUIQuestAlarm::OnButtonClicked`) |
| Partial | Book collection / Monster Book parity | `BookCollectionWindow` now swaps the old generic `UIWindow(.2).img/Book` shell for the dedicated `UIWindow(.2).img/MonsterBook` owner, loads the client-backed `arrowLeft` / `arrowRight` / `BtClose` controls at the IDA-confirmed `CBookDlg::OnCreate` positions, renders the WZ `cardSlot`, `infoPage`, `cover`, `select`, and `fullMark` chrome, and binds both the book window and the `UserInfo` collect page to a real simulator-side Monster Book runtime instead of the local item-maker snapshot. The new `MonsterBookManager` builds its catalog from `item/Consume/0238.img` plus mob metadata, persists per-character card ownership under AppData, tracks card counts up to the client-like five-card completion mark, resolves each mob back to its authored `0238xxxx` card item, and now groups the book by the client-facing `0238xxxx` card buckets instead of flat arbitrary chunks so the owner can expose the dedicated overview tab plus the nine WZ `LeftTab` chapter routes. The mob-death reward seam now uses that WZ-backed mapping to spawn the matching Monster Card item into `DropPool`, so ownership advances through the normal pickup path rather than direct kill credit, while the loader still wires the Monster Book `BtSearch`, `LeftTabInfo`, `LeftTab`, `RightTab`, and `ContextMenu/BtRegister` / `BtRelease` assets into the owner, preserves the WZ right-tab order when mapping to the detail panes, and feeds the runtime back through `_monsterBookManager.SetRegisteredCard` so local register or release actions and chapter-scoped spread paging live on the dedicated manager-owner seam rather than dead UI shells. This pass also replaces the remaining synthetic detail payload with WZ-backed catalog naming: the reward pane now resolves `String/MonsterBook.img/reward` item ids through the item-name cache instead of showing `Reward item #...`, the habitat pane resolves `String/MonsterBook.img/map` ids through the map-name cache instead of `Map #...`, and the local search index now includes the authored card item id/name plus those resolved reward and habitat names rather than only mob-name/episode text. The local register path is also tighter now: the manager normalizes legacy multi-register saves down to a single registered card slot, overview rendering exposes that registered card back on the summary page, the search affordance can focus from the search box itself, and `BtSearch` now cycles existing local matches instead of acting like a pure simulator-only mode toggle. IDA still anchors the split correctly: `CBookDlg::{OnCreate,Draw,OnButtonClicked}` own the UI shell while `CMonsterBookMan::LoadBook` confirms the separate loader path, and `CWvsContext::CloseBook` destroys the book dialog singleton; the simulator now mirrors that more closely by resetting the owner back to its default overview/search state when the window closes instead of carrying an old session forward across reopen. The remaining gap is the exact packet-authored ownership and save lifecycle, the real server/drop-table authorship and odds behind card drops instead of the simulator spawning the matched Monster Card item locally, and the last client-only search edit-control ordering, caret, and result semantics beyond this broader local WZ-backed query path. | Collection/book parity is now materially closer to the client because the simulator no longer treats this area as a maker-flavored collection page: it has a dedicated Monster Book owner, a distinct Monster Book manager, WZ-backed overview or chapter routing across `LeftTabInfo` plus `LeftTab`, wired right-tab detail panes, a singular local registered-card slot, a search focus/cycle path that behaves more like a real edit-control owner, save-backed card state, pickup-backed Monster Card ownership sourced through the normal drop flow, and a closer close or reopen lifecycle that resets the owner the way `CWvsContext::CloseBook` destroys `CBookDlg`. The reward, habitat, and search surfaces also now consume authored WZ names instead of numeric placeholders, so the remaining divergence is narrowed to packet-authored ownership persistence, real server-authored drop odds/save flow, and the final client-only search-control semantics rather than missing Monster-Book-specific tab routing, dead search/context controls, placeholder detail text, multi-register drift, or the close lifecycle itself. | book-style collection owner and data bridge (`BookCollectionWindow.cs`, `UserInfoUI.cs`, `MonsterBookManager.cs`, `UIWindowLoader.cs`, `MapSimulator.Content.cs`, `MapSimulator.cs`, `CBookDlg::OnCreate`, `CBookDlg::Draw`, `CBookDlg::OnButtonClicked`, `CWvsContext::CloseBook`, `CMonsterBookMan::LoadBook`) |
| Partial | Parcel / package-delivery window parity | The simulator already has a usable mailbox package seam through `MemoMailboxWindow`, `MemoSendWindow`, `MemoGetWindow`, and `MemoMailboxManager`: the status-bar launcher can open an inbox, drafts carry recipient/subject/body plus item or meso attachments, delivered entries preserve read/unread and kept/deleted state, and attachment claims exercise a dedicated package-view flow instead of collapsing package delivery into plain chat text. The remaining gap is that this still models the older memo/mailbox flow through `UIWindow2.img/Memo` rather than the client-confirmed parcel owner: IDA shows `CParcelDlg::{OnCreate,Draw,OnButtonClicked}` constructing a distinct receive/send/quick-send dialog with its own tabs, edit controls, scroll bar, and button routing. Packet-owned parcel session population, exact tab behavior, quick-send constraints, and the true relation between memo/social messaging and the parcel/package owner are still not mirrored. | Package delivery is a real client-owned utility window family, not just a note inside messenger or memo systems. Making it explicit keeps the simulator's existing mailbox/package work visible while also separating that progress from the still-missing `CParcelDlg` tabbed parcel/session behavior recovered in IDA. | mailbox/package owner versus parcel dialog owner (`MemoMailboxWindow.cs`, `MemoSendWindow.cs`, `MemoGetWindow.cs`, `MemoMailboxManager.cs`, `CParcelDlg::OnCreate`, `CParcelDlg::Draw`, `CParcelDlg::OnButtonClicked`) |
| Partial | Skill macro parity | The macro window now enforces trimmed macro names against a client-shaped 12-byte budget instead of a flat 12-character ASCII subset, validates names inline against the current Windows ANSI locale, shows a live byte counter in the editor, routes committed character input through the simulator `GameWindow.TextInput` path so locale and IME text commits no longer depend on the old synthetic ASCII key mapping, accepts clipboard paste as a best-effort locale text insert that fills the remaining byte budget instead of rejecting an otherwise valid prefix wholesale, owns a real cursor-aware name edit field with mouse caret placement plus `Backspace`/`Delete`/arrow/`Home`/`End` behavior instead of the old append-only buffer, now advances that caret and applies delete or backspace on grapheme-text-element boundaries so composed IME text and surrogate-pair glyphs are edited as a single unit instead of being torn apart mid-sequence, mirrors `CUIMacroSysEx`/`CCtrlEdit` ownership more closely by routing Win32 IME preedit composition into inline macro-name preview text with a blinking caret and composition underline before committed `TextInput` lands, and now clamps that preedit preview to the same locale or byte-valid insertable prefix the committed path would accept so the visible preview and live byte counter stay aligned with the eventual insert result. This pass tightens the remaining physical-key and paste path closer to the client’s constrained macro surface by enforcing a best-effort blacklist seam on top of the code-page gate: macro names now reject dash and connector punctuation beyond the client-visible soft-keyboard `-`/`_` surface instead of still admitting the wider Unicode punctuation categories. It also loads the client `UIWindow.img/SoftKeyboard` chrome as an attached helper with client-shaped key-grid hit-testing from `CUISoftKeyboard::GetKeyIndexFromPoint`, hover feedback, min/max/close controls, caps or shift toggles, backspace or enter actions, and now applies the client-derived `CUISoftKeyboard::UpdateKeyConstraint` mode split before the existing byte-budget or locale-aware insertability gate decides whether a key can advertise as clickable, still supports drag-binding selected macros into QuickSlot entries, routes macro hotkeys back through `SkillManager` with party-chat announce semantics, paces queued casts so the next macro skill waits for the current cast to finish, and persists the five-slot macro definition list per active player or selected login-roster character under the simulator AppData profile across restarts instead of losing macro definitions with the session. This pass also extends the Win32 IME bridge past raw preedit text: the simulator now captures composition clause and cursor metadata plus the active candidate list, then renders a client-shaped candidate popup beside the macro-name edit field with page-sized rows, page-local numbered entries, selection highlight, below-or-above caret placement, and clause-aware horizontal anchoring derived from `CCtrlEdit::CreateIMECandWnd` instead of leaving candidate UI entirely to the host OS; the popup sizing now measures the visible candidate page instead of a flat horizontal width so later pages stay aligned with the recovered `CreateIMECandWnd` numbering and sizing walk. The remaining gap is the unrecovered per-locale or per-IME blacklist matrix beyond this best-effort filter, plus the last exact `CCtrlEdit` candidate-owner details such as locale-specific popup metrics and ownership quirks across IMEs. | Macro support now behaves like a real runtime surface instead of an editor-only stub, and the remaining gap is narrowed to the unrecovered locale or IME-specific filter matrix plus final candidate-owner metric polish rather than missing interaction ownership, committed locale input, byte-count enforcement, preview-clamped preedit rendering, text-element-safe editing, the soft-keyboard constraint-mode split, the visible-page candidate sizing walk, page-local candidate numbering, or candidate-popup ownership itself. | `SkillMacroUI.cs`, `SkillMacroSoftKeyboardLayout.cs`, `SkillMacroNameRules.cs`, `SkillManager.cs`, `QuickSlotUI.cs`, `MapSimulator.cs`, `UIWindowLoader.SkillsAndQuests.cs`, `WindowsImeCompositionMonitor.cs`, `ImeInputState.cs`, `UnitTest_MapSimulator/SkillMacroNameRulesTests.cs` (`CUIMacroSysEx`, `CCtrlEdit::OnIMEComp`, `CCtrlEdit::CreateIMECandWnd`, `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::UpdateKeyConstraint`) |
| Partial | Soft keyboard and constrained text-entry parity | The simulator now has a dedicated WZ-backed soft-keyboard owner loaded from `UIWindow.img/SoftKeyboard`, uses the client-derived `CUISoftKeyboard::GetKeyIndexFromPoint` grid geometry for hover and click hit-testing, routes hover state through a top-level UI manager seam so the key surface owns mouse focus ahead of underlying windows, and now also keeps mouse ownership modal while the keyboard is open so clicks no longer fall through into the underlying dialog the way they previously did. Its per-mode enable gating still matches the recovered four-way `CUISoftKeyboard::UpdateKeyConstraint` matrix (`0` alphanumeric-until-full, `1` alpha-edge/numeric-middle, `2` numeric-until-full, `3` numeric-until-full-alt`) instead of only a loose alpha-vs-numeric split. The constrained map-transfer target field still attaches as a numeric-only edit surface, the shared helper now also owns the login title ID/PW fields through the existing login-title owner seam, and `LoginUtilityDialogWindow` now carries explicit per-prompt keyboard typing across both PIC and non-PIC utility flows: PIC prompts still use the `CPinCodeDlg::OnKey` `type 3` numeric-only-alt gate and now also honor the client `CCtrlEdit` 8-character cap instead of the simulator's older 16-character allowance, while account-migration acceptance now opens a dedicated fixed-length birth-date prompt that mirrors the recovered `CLoginUtilDlg::OnKey` `type 2` path with a masked 8-digit edit surface, persisted account birth-date state, the same numeric-only mode gate for both soft-keyboard and physical-key input, and the same existing utility-owner seam instead of a one-off field. The shared login-prompt override seam also now preserves and accepts explicit soft-keyboard types so prompt merges no longer silently collapse constrained utility inputs back to generic alphanumeric behavior. Physical-key routing for the modeled login-owned and anti-macro utility edit owners now goes through the same committed-text and IME composition seam the simulator already used elsewhere, so those owners render in-place composition previews, own candidate-list presentation while focused, and clear that transient state when the active owner hides or the edit focus changes instead of leaving locale input to raw per-key polling. The modeled login-utility and map-transfer edit owners also now mirror the client modal launch shape more closely: their soft keyboard no longer advertises itself continuously just because an edit exists, and instead opens from the owner interaction path, then dismisses when the owner hides, the field blurs, the move confirmation path commits, or the modal keyboard closes. The remaining gap is that the simulator still does not model every client utility edit owner or every exact launcher trigger and focus-return path around those owners, and its IME presentation still uses simulator-drawn candidate overlays instead of the client's native candidate-window placement after each modal dialog closes. | This is now a separate parity target from generic macro name validation because it affects how locale input, constrained text fields, and client-owned edit controls behave before characters are committed. The simulator now has the on-screen key surface, hover ownership, modal mouse capture, a client-shaped four-type constraint matrix, shared-host attachment across map transfer plus the login-owned utility fields that are currently modeled, IME-aware committed-text ownership in the login and anti-macro edit surfaces instead of only raw key polling, and a closer `CDialog::DoModal`-style request lifecycle for the modeled login-utility and map-transfer edit owners instead of keeping the soft keyboard permanently active whenever those fields exist. The remaining divergence is narrowed to still-missing utility owners plus the final client-owned trigger, focus-return, and native candidate-window details rather than helper absence, click-through behavior, unconstrained PIC length, or the lack of any `type 2` utility path at all. | utility-window text-entry layer, login-owned edit bridges, anti-macro edit bridge, map-transfer edit bridge, shared soft-keyboard owner (`CUIMacroSysEx::OnCreate`, `CUISoftKeyboard::GetKeyIndexFromPoint`, `CUISoftKeyboard::OnMouseMove`, `CUISoftKeyboard::HitTest`, `CUISoftKeyboard::UpdateKeyConstraint`, `CLoginUtilDlg::OnKey`, `CPinCodeDlg::OnKey`, `CUIAntiMacro::OnCreate`) |
| Partial | Repair-durability utility parity | MapSimulator now registers a dedicated `RepairDurability` owner backed by `UIWindow2.img/Repair`, routes packet `OnOpenUIWithOption` launches for `uiId == 33` into that owner instead of stopping at the dispatcher seam, preserves the client-derived option value as the repair NPC template id, blocks the launch when another unique-modeless utility owner is already visible, and lists damaged repair targets with per-item durability, selection, scroll, WZ-backed row chrome, and dedicated `Repair` / `Repair All` buttons. The owner still uses the client-derived repair-cost formula shape recovered from `CItemInfo::EQUIPITEM::CalcRepairDurabilityPay`, but it now also replaces the text-only NPC header with a live animated NPC preview built from the WZ NPC loader, routes repair clicks through a simulator-side send/result queue so the owner shows a pending request state before meso consumption and durability restoration are applied on the response path instead of immediately on button press, now mirrors the recovered `SendRepairDurability` / `SendRepairDurabilityAll` wire shape more closely by carrying the client opcodes (`131` single, `130` all), and now also follows the recovered `SetNPC` seam more closely by disabling generic idle-action cycling for the preview and preferring the NPC template's WZ shop-action family (`info/shop` id, then shop/say/speak fallbacks) before falling back to `stand`. This pass closes the biggest remaining send-side gap from IDA: `CRepairDurabilityDlg::SetItems` no longer has to be approximated as visible-plus-hidden avatar slots. The simulator now builds the repair list in the same broad source order the client decompile shows, by appending damaged equip-inventory entries first and then damaged currently equipped entries, uses raw client-shaped single-item `nPOS` values for those sources (equip bag rows as `1..N`, equipped rows as `-1..-59` where the simulator can prove the mapping), and resolves delayed repair responses back against either the live inventory slot or the live equipped part instead of only equipped-part object references. The remaining gap is narrower and now concentrated in the seams the simulator still cannot prove exactly: the exact client `aEquipped[0..59]` to simulator-slot mapping for every rare equip category, whether hidden/cash presentation cases ever surface different repair candidates than the simulator can currently expose, the final `CActionMan::LoadNpcAction` mapping details for repair NPCs whose shop presentation is not expressible through the raw WZ action names alone, and the true server-authored success/failure result payloads or rejection reasons that are still locally simulated after the delayed response. | Repair-durability is now a packet-owned utility surface with dedicated art, owner lifetime, option routing, repair math, a `SetNPC`-shaped animated NPC layer that stays pinned to a shop-facing action instead of random idle cycling, and a request/response-shaped repair flow whose send opcode and single-slot encoding are much closer to the client. The row now also follows the recovered client `SetItems` source shape materially better by including damaged equip-inventory entries ahead of damaged equipped entries and by sending raw client-style `nPOS` values instead of a simulator-only hidden/cash encoding. The remaining divergence is narrowed to the last unresolved equipped-array mapping edge cases, unrecovered action-manager mapping details, and server-result fidelity rather than missing owner behavior or a missing inventory-source repair path. | utility-window owner launched from local packet dispatch (`CUserLocal::OnOpenUIWithOption`, `CRepairDurabilityDlg`, `CItemInfo::EQUIPITEM::CalcRepairDurabilityPay`, `CRepairDurabilityDlg::SetNPC`, `CRepairDurabilityDlg::SetItems`, `CRepairDurabilityDlg::SendRepairDurability`, `CRepairDurabilityDlg::SendRepairDurabilityAll`) |
| Partial | Pickup notice parity | The simulator pickup-notice stack now renders item icons, resolves cached item names and type labels for success messages, records player pickup history, reconnects manual pickup failures through `PlayerCombat` so ownership and inventory-capacity checks consistently raise notices before the drop is consumed, mirrors the client `-3` style split through a shared screen/chat formatter, restores the client `-2` split by leaving inventory-full as a screen-only notice, routes meso success notices through the same formatter so pet meso pickups surface as a chat-log side effect instead of polluting the screen string, and aligns regular plus quest-item success wording more closely with `CWvsContext::OnDropPickUpMessage` by using the same item-type label path rather than pet-prefixed or generic quest text. It still applies a pet-specific validator backed by WZ `item/Consume/0200.img/02002058/spec/notPickupByPet` so local auto-loot can surface pet-blocked items with item-aware pet messaging without spamming repeated failures, resolves `Unavailable` failures against `DropPool` recent-pickup history so pet-, player-, mob-, and generic remote-preemption cases surface actor-aware pickup lines instead of collapsing to one fallback string, wires `DropPool` remote player and remote pet pickup callbacks into the same paired screen/chat notice seam instead of limiting live remote-loot surfacing to the mob callback alone, now preserves recorded remote-pet actor names when those recent-pickup records are replayed through `Unavailable` notices, lets live `RemoteUserActorPool` field actors feed that same seam through bounded remote-player plus avatar-look pet pickup producers instead of requiring surfaced remote loot to be manually injected, and now also exposes `DropPool`'s existing `DropPickupActorKind.Other` resolve path through a live callback so packet-authored or other non-avatar remote pickups can reuse the same notice formatter instead of only landing in recent-pickup history. The remaining gap is narrowed to exact official wording for simulator-owned failure or remote-preemption messages whose backing `StringPool` text is still not captured in-source, the still-missing packet/runtime producers that should call that generic remote seam, and any deeper packet ownership rules the client applies before surfacing those events | Pickup feedback now matches the verified client branch shape for `0`, `1`, `2`, `-2`, `-3`, and the default fallback more closely, with success-path screen/chat splits, inventory-full behavior, WZ-backed pet restrictions, recent remote-pet actor replay, live remote-player plus avatar-look remote-pet pickup surfacing, and a generalized live `Other`-actor remote pickup seam aligned to the decompiled handler and existing runtime seams. The remaining divergence is concentrated in uncaptured official strings plus the packet-authored producers and ownership checks that still need to feed or constrain that seam end to end | `PickupNoticeUI.cs`, drop/item runtime (`CWvsContext::OnDropPickUpMessage`, `CUIScreenMsg::ScrMsg_Add`) |
| Partial | Cursor and utility-button parity | The simulator now builds dedicated WZ-backed `Menu` and `System` popup owners from `StatusBar2.img/mainBar/Menu` and `StatusBar2.img/mainBar/System`, routes the status-bar `BtMenu` and `BtSystem` buttons through those owners, fans their core entries back into inventory/equip/stat/skill/quest/character-info and channel-selection flows instead of leaving them as dead launchers, switches the cursor to the client hold art during active skill, quick-slot, and window drag flows, loads the client forbidden-hand and busy-search cursor families from `Basic.img/Cursor/13` and `Basic.img/Cursor/16`, and now dismisses those popup owners on any click outside their modeless bounds through the shared window-manager input path instead of only inside the popup shell. It also now registers WZ-backed `KeyConfig` and `OptionMenu` owners from `UIWindow2.img/KeyConfig` and `UIWindow2.img/OptionMenu`, routes `BtKeySetting`, `BtGameOption`, `BtSystemOption`, `BtOption`, and `BtJoyPad` into those owners instead of forbidden stubs, binds key settings back to live `PlayerInput`, shows the attached pad binding beside each keyboard row in `KeyConfig`, and constrains captured joypad remaps for those utility owners to the shared non-directional button family instead of letting `BtJoyPad` or `KeyConfig` steal left-stick or D-pad movement inputs. The `BtJoyPad` owner also now prefers connected controller slots when cycling ports, renders a live connection plus stick/trigger state strip above the shared jump/attack/pickup/interact/skill/inventory/escape bindings, and now lets that same owner cycle paired left-stick dead-zone and trigger-threshold presets that feed back into the runtime `PlayerInput` axis/button rules instead of leaving joypad calibration as a read-only strip. `BtRank` and `BtEvent` also now land on WZ-backed ranking/event owners that render live simulator-backed ranking cards plus filtered event rows from login bootstrap, packet-owned field or overlay state, quest-guide or delivery routing, tracked quest alerts, and active special-field runtimes instead of placeholder shells. The remaining gap is now narrower: the client’s full joypad calibration workflow still is not mirrored exactly because its complete axis-shaping, dead-zone, inversion, and confirm/reset semantics are only approximated through shared preset cycling plus live runtime readout, and true packet-fed ranking ladders, attendance or calendar event payloads, and the deeper remote ranking/event dispatch behind `CUIStatusBar::OnButtonClicked` are still not mirrored exactly. | This is part of the client feel layer and affects almost every window or interaction surface once the rest of the UI becomes more complete. The row now covers client-shaped utility owners with live simulator-backed payloads for joypad, ranking, and event flows, and the joypad branch no longer collapses movement directions into generic remap targets or leaves calibration detached from the runtime axis thresholds that actually drive movement and trigger-owned skills. The remaining gap is narrowed to the unrecovered full calibration semantics plus official network-fed data and final dispatch fidelity rather than dead owners, placeholder content, or static joypad readouts. | `MouseCursorItem.cs`, `StatusBarUI.cs`, `UILoader.cs`, `StatusBarPopupMenuWindow.cs`, `PlayerInput.cs`, `OptionMenuWindow.cs`, `UIWindowManager.cs`, broader UI manager layer (`CUIStatusBar::OnButtonClicked`) |
| Partial | Explicit ranking/event owner parity | `BtRank` and `BtEvent` now route into explicit simulator owners instead of generic utility shells. The ranking surface still stays simulator-local, but it now reflects the client-observed `CWebWnd` landing shape more directly: `UIWindow2.img/Ranking` remains the owner art seam, and the snapshot now seeds a web-style landing request around the active world and character id before showing local `World Rank`, `Job Rank`, `Popularity`, and combat context. The event surface also moved closer to the real `CUIEventAlarm` owner instead of only using the main-row shell: `UIWindow2.img/EventList` now loads the WZ-backed row `slot` and `icon` family plus the `calendar/bg/1`, `today`, and `number/{normal,select}` assets, the calendar initializes around the nearest scheduled entry instead of always resetting to the current month, and the day render now uses the authored day canvases instead of treating them as synthetic digit glyphs. The event owner still auto-dismisses after the client-observed 8 second window unless the user interacts with its WZ-backed filter or calendar controls, and the IDA follow-up still defines the client owners through `CUIRanking::OnCreate` at `0x83ca40`, `CUIRanking::OnButtonClicked` at `0x83c6a0`, `CUIRanking::Update` at `0x83c690`, plus `CUIEventAlarm::OnCreate` at `0x7ac4f0`, `CUIEventAlarm::Draw` at `0x7aad90`, and `CUIEventAlarm::Update` at `0x7aac00`. The remaining gap is narrower: the simulator still does not know the client’s real ranking URL template or remote page payloads behind `CUIRanking`, the exact packet-authored event-alarm text/attendance ownership, or the live server-fed ranking and event content that should replace the simulator-authored snapshots. | Status-bar parity here is not only about buttons launching something. The client treats ranking and event views as separate utility owners with their own create/update/draw lifecycle, so tracking them explicitly prevents this area from looking "done" once the generic popup routing exists while the owner-specific ranking/event behavior is still only approximated. | status-bar launched utility-window owners (`CUIRanking`, `CUIEventAlarm`, `CUIStatusBar::OnButtonClicked`) |

### 2. Local utility and event packet parity

This area is still underrepresented by the existing window rows because the client keeps a packet-owned dispatch seam in front of several of those owners.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned local utility/event dispatch parity | `MapSimulator.PacketOwnedUtilityParity.cs` now carries the full packet-owned utility bridge much closer to the concrete `CUserLocal::OnPacket` seam instead of synthetic aliases. The simulator can dispatch raw/pseudo packet payloads for `OnOpenUI`, `OnOpenUIWithOption`, `OnGoToCommoditySN`, `OnOpenClassCompetitionPage`, `OnQuestGuideResult`, `OnDeliveryQuest`, `OnNoticeMsg`, `OnChatMsg`, `OnBuffzoneEffect`, `OnPlayEventSound`, `OnPlayMinigameSound`, `OnHireTutor`, `OnTutorMsg`, `OnAskAPSPEvent`, `OnFollowCharacter`, `OnFollowCharacterFailed`, `OnRadioSchedule`, and `OnSkillCooltimeSet`, while the overlay-owned `OnFieldFadeInOut`, `OnBalloonMsg`, `OnDamageMeter`, and `OnNotifyHPDecByField` shapes stay routed through their existing packet overlay owner. Client-backed `CWvsContext::UI_Open` ids that already have simulator owners still route through this bridge for inventory/equip/stat/skill/key-config/quest/character-info/item-maker/ranking/family/guild/party-search windows, and the exact `OnOpenUIWithOption` `uiId == 7` path still follows the recovered `UI_Close(7)` plus `UI_Toggle(7, option)` behavior by routing into the simulator `SocialList` owner with the requested `Friend` / `Party` / `Guild` / `Alliance` / `Blacklist` tab. `OnNoticeMsg` and `OnChatMsg` still decode the client-shaped packet payloads everywhere this seam accepts packets, `OnNoticeMsg` still mirrors the WZ-backed `Sound/UI.img/DlgNotice` cue into the utility feedback path, `OnPlayEventSound` / `OnPlayMinigameSound` still prefer `Field.img` / `MiniGame.img`, `OnRadioSchedule` still owns per-frame playback state, and `OnSkillCooltimeSet` still maps the recovered `0x004FAE6F` vehicle sentinel into the existing `5221006` Battleship cooldown runtime. This pass closes the biggest remaining tutor gap: WZ-backed tutor assets now participate directly in the utility seam, so `255/256` no longer stop at `TutorRuntime` status text. `OnHireTutor` now creates and maintains a real local packet-owned summon owner through the existing summoned-pool seam using the recovered tutor skill/object ids, `OnTutorMsg` now drives that owner into a visible message state, and the rendered message now uses the dedicated `UI/ChatBalloon.img/tutorial` frame instead of piggybacking on generic world-notice feedback. The remaining gap is narrower: the battleship sentinel path still approximates the client's temporary-stat durability view update instead of reproducing every downstream UI side effect, tutor presentation still stops at a simulator-owned summon plus tutorial balloon rather than a byte-for-byte `CTutor` implementation and exact `CSummoned::Init` choreography, and notice/event-sound presentation still stops at simulator feedback plus WZ sound playback rather than a dedicated `CUtilDlg::Notice` dialog or the client's exact sound-engine behavior. | This row now covers the actual packet entry points, the owned window routing, the chat/notice/audio packet decoding, the radio owner tick, and a real tutor summon/message presentation instead of only a status mirror. The remaining risk is concentrated in the last low-level temporary-stat, dialog, summon-construction, and sound-engine details, not in a missing dispatcher, missing tutor owner, unknown `uiId == 7` target, missing notice/chat packet decoding, or an absent radio/tutor packet entry point. | local-user packet/runtime bridge (`CUserLocal::OnPacket`, `CUserLocal::OnOpenUI`, `CUserLocal::OnOpenUIWithOption`, `CUserLocal::OnGoToCommoditySN`, `CUserLocal::OnFieldFadeInOut`, `CUserLocal::OnBalloonMsg`, `CUserLocal::OnDamageMeter`) |

### 3. Packet-authored local overlays and guidance parity

This slice is narrower than the full dispatcher row above and is still missing as its own planning surface even though the handlers have concrete client-owned behavior.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Field fade and world/actor balloon parity | MapSimulator now carries this row through a packet-owned overlay dispatcher seam instead of direct-command-only state: `MapSimulator.PacketOwnedLocalOverlayParity.cs` and `LocalOverlayPacketInboxManager` can drain loopback `fade` and `balloon` packets through a dedicated local inbox, route them back into the same owner used by `/localoverlay`, and report dispatch status separately from the broader utility inbox. The field-fade path also matches the recovered client packet semantics more closely: the fourth `OnFieldFadeInOut` `Int32` is treated as the animation alpha input that `CAnimationDisplayer::RegisterFadeInOutAnimation` uses for a black full-screen fade, `PacketFieldFadeOverlay` tracks the fade-in span, scheduled fade-out start tick, and expiry separately instead of flattening the sequence into one implicit timer, and overlay status now reports the active fade phase plus the pending fade-out start. Balloon rendering likewise moved closer to `CUserLocal::ShowBallonMsg`, `CBalloonMsg::Init`, and the `UI/Login.img/WorldNotice/Balloon` assets: avatar-attached packets still replace the single avatar balloon while field/world packets append into a separate field list, avatar anchors still derive from the active player plus current assembled frame height to mirror `CAvatar::GetHeight`, the shared skin still loads the recovered short, corner, and long-arrow variants (`arrow`, `slArrow`, `se/sw/nw/neArrow`, `sel/swl/nwl/nelArrow`), wrapped text still breaks long tokens instead of overflowing the body, and each line is still centered inside a client-shaped packet-width text box with the balloon body sized as `nWidth + 20`. This pass narrows the text/layout gap further by preserving common `CTextAnalyzer`-style inline controls across wrapping and drawing (`#k/#r/#g/#b/#d/#e/#n` reset, color, and emphasis state) instead of flattening styled packet text to one color, by preserving explicit blank packet line breaks instead of collapsing empty rows during wrapping, by separating the balloon canvas bounds from the inner body so placement and overlap handling follow the recovered `CBalloonMsg::Init` / `make_balloon` layering shape more closely, by moving the center-arrow placement to the client's `(nWidth + 20) * 0.7` tail region with the recovered 9-pixel overlap inset instead of generic midpoint placement, and by deriving the assembled balloon canvas from the actual WZ arrow sprite extents and origins so off-screen clamping and collision stacking operate on the full body-plus-tail footprint instead of a body-only rectangle. Collision resolution also now reruns against that full canvas after each reclamp pass, which keeps stacked world balloons from falling back into overlaps after screen-edge adjustment. The remaining gap is now narrower: the simulator still approximates the client's exact COM-layer fade ownership and layer-object choreography inside `RegisterFadeInOutAnimation`, only supports a focused subset of `CTextAnalyzer` controls instead of the full token/font/icon pipeline, and still draws the balloon procedurally rather than reproducing the client's exact COM-backed `make_balloon` canvas assembly and layer lifecycle end to end. | These are visible event-owned overlays that sit between local gameplay and many later scripted or utility flows, and the simulator now has both a packet-shaped launch bridge and a substantially closer render owner for the two surfaces instead of collapsing them into chat lines or ad hoc notices. The remaining risk is concentrated in the final low-level animation-layer and text-layout details rather than in missing dispatch, missing multiple field balloons, missing inline styled text, missing explicit blank-line handling, missing client-shaped canvas placement or center-tail positioning, or missing full-canvas overlap restacking for the WZ arrow variants. | packet-owned overlay layer (`CUserLocal::OnFieldFadeInOut`, `CAnimationDisplayer::RegisterFadeInOutAnimation`, `CUserLocal::OnBalloonMsg`, `CUserLocal::ShowBallonMsg`) |
| Partial | Damage-meter and field-hazard notice parity | MapSimulator still carries this row through the same packet-owned utility dispatcher seam the client uses: the local utility inbox and direct packet injection path accept the real `CUserLocal::OnPacket` handler ids for this row (`243` for `OnNotifyHPDecByField`, `267` for `OnDamageMeter`) alongside the existing aliases, and both route back into the dedicated WZ-backed HUD/notice owner built from `Basic.img/Notice2`, `Basic.img/Notice3`, and `StatusBar2.img/mainBar/notice`. This pass moved both presentation and the `OnNotifyHPDecByField` follow-up closer to recovered client behavior again. On the HUD side, the top-center owner still sizes the hazard and damage-meter frames from the actual WZ canvases (`Notice2` at `258x40/20/47`, `Notice3` at `266x21/20/55`), and the hazard panel now also uses the real `StatusBar2.img/mainBar/notice` icon size instead of a smaller fixed square while surfacing distinct pending, throttled, acknowledged, consumed, and failed follow-up badges. On the pet side, the existing per-pet runtime seam still owns enablement and optional preferred-potion overrides, but the hazard follow-up now mirrors more of recovered `CUserLocal::TryConsumePetHP`: the request owner chooses the first enabled pet as the requester, keeps a simulated shared auto-HP item selection alive across notices, resolves and records the concrete USE/CASH slot that would back `SendStatChangeItemUseRequestByPetQ`, respects the `200ms` resend cooldown, and then validates that same slot again when the synthetic ack window closes before deciding whether the request only acknowledged, acknowledged and consumed a real potion through the existing inventory seam, or failed back into the no-HP-potion speech/chat path because the queued slot disappeared. Local-overlay status now reports that shared-item and pending-slot transport state directly, so the row exposes more of the same request ownership the client keeps around `m_nPetConsumeItemID` and the outgoing pet item-use request path instead of only a one-shot “would request Eapproximation. The remaining gap is now narrower and mostly concentrated on client-only ownership the simulator still cannot source exactly: it still approximates how the client persists and sources the shared pet auto-HP selection outside the live runtime, still does not model the true networked `SendStatChangeItemUseRequestByPetQ` request/result ordering and server ack lifecycle end to end, and still cannot mirror the exact status-bar/chat timing or the deeper `bForce` / `bBuffSkill` variants beyond the field-hazard `0,0` path. | This row now has both halves of the client seam represented with more of the real lifecycle shape: the damage-meter timer still rides a `CUserLocal::OnPacket`-style launch into a WZ-backed HUD owner, and the HP-dec branch now has a pet-aware request/ack follow-up state machine, shared-item transport approximation, concrete inventory-slot attribution, and WZ-sized notice presentation instead of a single global fallback and static notice shell. The remaining risk is concentrated in the still-missing persistence and true network/result ownership around `TryConsumePetHP`, not in missing dispatch, missing HUD art, missing request-state visibility, missing slot attribution, or missing pet-specific follow-up attribution. | packet-owned HUD/notice layer (`CUserLocal::OnDamageMeter`, `CDamageMeter::SetTimer`, `CUserLocal::OnNotifyHPDecByField`, `CUserLocal::TryConsumePetHP`) |
| Partial | Quest-guide, delivery-quest, and class-competition launch parity | MapSimulator now carries this row through a raw packet-owned local utility inbox and decoder path instead of command-only stubs: the simulator can drain `CUserLocal::OnPacket`-shaped `274` quest-guide, `275` delivery-quest, and `250` class-competition launches from a dedicated loopback inbox, decode the client packet payloads for quest-guide mode or reset, quest-id or mob-map target rebuilds, delivery quest or item ids plus the disallowed-delivery list, and open the existing owners through the same packet-owned seam the manual launch helpers use. Packet-authored quest-guide demand state still rebuilds world-map mob targets into `WorldMapUI` and clears itself when no usable mob survives. `QuestDelivery` still uses the `UIWindow2.img/UtilDlgEx` `notice` frame and `QDeliveryIcon` art, but its list now seeds from the quest-runtime delivery snapshot that mirrors the client’s `LoadData` series/show-quest model more closely: series representatives keep their display quest id, blocked packet rows are overlaid separately, and the selected entry now exposes both the representative quest and delivery cash-item context before routing OK through the packet-shaped NPC interaction seam. That keeps the simulator closer to `CUIQuestDelivery::LoadData`/`OnButtonClicked` than the earlier quest-log/detail shortcut even though the final handoff still uses simulator quest state. The packet-driven `ClassCompetition` owner has also been tightened from a small static placeholder into a dedicated 312x389 packet-owned surface with the client-observed close-only layout, a seeded auth-request/auth-key cadence, loading-versus-navigated web-owner state, and a local class-competition URL seed bound to the active character plus ranking context so the launch behaves more like a `CClassCompetition` singleton than a menu alias. The remaining gap is that `QuestDelivery` still approximates the client’s worthy filtering, created NPC instance, inventory slot handoff, and `CUserLocal::TalkToNpc` runtime with simulator quest definitions plus a synthetic dialogue overlay, while the class-competition page still lacks the real remote `CWebWnd` payloads, server auth key, and live ladder page content. | These are not generic variants of quest log or world map browsing. They are packet-authored launch surfaces for guided quest routing and delivery/class-competition utility flows, and the simulator now has the real packet-owned bridge, client packet ids, WZ-backed delivery chrome, representative-series delivery rows, a packet-shaped delivery-to-NPC handoff seam, and a closer web-owner-style class-competition singleton needed to exercise those launches end-to-end. The remaining risk is now narrowed to the deeper client-owned `CUIQuestDelivery` worthy filtering or created-NPC handoff details and the still-missing remote `CClassCompetition` web/auth payload model rather than to missing raw dispatch, owner absence, or direct-list action shortcuts. | packet-owned guidance/utility launch layer (`CUserLocal::OnQuestGuideResult`, `CUserLocal::OnDeliveryQuest`, `CUIQuestDelivery`, `CUserLocal::OnOpenClassCompetitionPage`) |

### 4. External cash-service stage parity

This area is currently unowned by the backlog set even though the client keeps Cash Shop and ITC as dedicated stage owners rather than utility-window variants.

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Cash Shop and ITC stage ownership parity | The backlog currently tracks an in-map `AdminShopDialogUI` approximation plus the `OnGoToCommoditySN` migration hook, but IDA shows a separate service-stage family that is still unrepresented: `CCashShop::Init` clears field UI, resets wishlist and cash balances, selects job-specific preview art, builds dedicated child owners for character, locker, inventory, tabs, catalog list, best-items, status, and item-search panes, and resumes at a pending commodity serial through `GoToCommoditySN`; `CCashShop::OnPacket` then dispatches charge-param (`382`), query-cash (`383`), cash-item (`384`), purchase-exp (`385`), gift-mate (`386`), duplicate-id (`387`), name-change (`388`), transfer-world (`390`), gachapon stamp/result (`391`-`393`), one-a-day (`395`), and free-item notice (`396`) handlers instead of using the local utility bridge or `CAdminShopDlg` button flow. The client also keeps the Item Trading Center in a separate stage owner: `CITC::Init` clears field UI, resets its own category/search/sort state, loads NPT exception items, creates separate character, sale, purchase, inventory, tab, subtab, list, and status panes, and `CITC::OnPacket` owns its own charge-param (`410`), query-cash (`411`), and normal-item (`412`) result routing. None of that service-stage lifecycle, packet ownership, or child-window structure is tracked anywhere in the current backlog set. | Treating cash services as only a status-bar launch plus an in-map admin-shop shell hides a large client-owned stage transition before the player returns to field gameplay. Tracking the service-stage owners explicitly keeps future parity work anchored to the real client lifecycle for Cash Shop and ITC instead of overloading `CAdminShopDlg`, `OnGoToCommoditySN`, or generic utility-window rows with stage-owned behavior they do not actually own. | external cash-service stage layer (`CCashShop::Init`, `CCashShop::OnPacket`, `CCashShop::OnCashItemResult`, `CITC::Init`, `CITC::OnPacket`, `CITC::OnNormalItemResult`) |

### 5. Cash-service child-window and preview parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Cash avatar-preview and service-subwindow parity | The simulator now instantiates a dedicated `CCSWnd_Char`-style owner instead of leaving cash-avatar preview behavior folded into the main shop surface: `CashAvatarPreviewWindow` is registered from `CashShop.img/Base/Preview` and `CashShop.img/CSChar`, wired to the live cash-shop selection, exposes the client-facing buy-avatar/default-avatar/takeoff-avatar buttons, and routes preview-owned service actions to dedicated adjacent owners or handlers for personal shop, entrusted shop, weather, and a separate `CashTradingRoom` window. The cash trading seam is also no longer anonymous: the simulator now registers a distinct `CCashTradingRoomDlg`-style owner from `UIWindow2.img/CashTradingRoom` with the client button family and direction-mode ownership, and the cash shop now hides its child preview when the parent window closes. Remaining parity for this row is narrower and still client-owned: `CCSWnd_Locker::OnCreate`, `CCSWnd_Inventory::OnCreate`, `CCSWnd_List::OnCreate`, `CCSWnd_Status::OnCreate`, and `CCSWnd_OneADay::OnCreate` are still represented by the broader cash-shop surface rather than first-class stage child owners; `CCSWnd_Char` still uses the simulator's existing avatar assembly instead of the client's dedicated `CUserPreview` plus physical-space setup; and `CCashTradingRoomDlg` still reuses the generic social-room runtime instead of the exact chat-entry, scrollbar, font, and cash-balance initialization path seen in the client. | Cash-service parity is not only about entering the right stage. The client also depends on a second layer of named owners to make the stage usable and to preview avatar-affecting cash items correctly. Tracking those owners separately keeps future work anchored to the real preview, locker, inventory, list, status, One-a-Day, and cash-trading seams instead of treating them as anonymous controls inside `CCashShop::Init` or folding them back into field-side UI rows. | cash-service child-window layer (`CCSWnd_Char::OnCreate`, `CCSWnd_Locker::OnCreate`, `CCSWnd_Inventory::OnCreate`, `CCSWnd_List::OnCreate`, `CCSWnd_Status::OnCreate`, `CCSWnd_OneADay::OnCreate`, `CCashTradingRoomDlg::OnCreate`) |

### 6. Cash-service wishlist parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Cash-service wishlist dialog parity | The simulator now registers a dedicated `AdminShopWishListUI` owner instead of keeping wish-list handling inside the base `AdminShopDialogUI` modal path, using the WZ-backed `CashShop.img/CSItemSearch/PopUp` frame plus the `BtAllItem`, `BtSearch`, `BtCancel`, `BtBuy`, `PopUp/BtComboBox`, `CSItemSearch/Price/*`, `PopUp1/backgrnd`, and `PopUp1/Scroll/normal/*` control family as the current asset seam for the split owner. `BtRecharge` on the admin-shop dialog still opens that separate owner behind the existing yes/no confirmation, `AdminShopWishListUI` still focuses its local search lane on show, mirrors the client-observed `CUIAdminShopWishList::OnButtonClicked` split by routing `0x7D0` into a dedicated add-on toggle, trimming and submitting the search string through a separate search path, and closing through its own owner-local close button, and it now also drives the WZ-backed combo seam into the `CSItemSearch/Price/*` ranges instead of hard-collapsing search to one implicit band. Search no longer auto-applies only the best local match: the owner now stages a scrollable result list on the larger `PopUp1` surface, keeps an owner-local highlighted result with keyboard navigation, shows the selected row's category, seller, detail, and price summary, and routes `BtBuy` back into `AdminShopDialogUI` so the chosen NPC offer is focused and saved into the in-session wish-list state instead of always forcing the first result. The remaining gap is that the simulator still approximates `ToggleAddOn` rather than mirroring the client's exact `CUIAdminShopWishListCategory` child-owner construction and category tree, and `SearchItemName` still resolves through local catalog scoring instead of true packet- or server-authored result rows, remote result metadata, or live service-side search/session flow. | Cash-service parity is not only about catalog browsing and stage ownership. The client exposes a separate wish-list/search owner with its own text-entry, toggle, close lifecycle, price band, and staged result selection surface, so leaving that work folded into the base admin-shop row would hide a distinct client seam and make wish-list parity look more complete than it is. | cash-service wishlist owner (`CUIAdminShopWishList::OnCreate`, `CUIAdminShopWishList::OnButtonClicked`, `CUIAdminShopWishList::HitTest`, `CUIAdminShopWishListCategory::OnCreate`, `CUIAdminShopWishListCategory::OnButtonClicked`) |

### 7. Packet-authored radio-schedule parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Radio schedule and `CRadioManager` lifecycle parity | `MapSimulator.PacketOwnedUtilityParity.cs` now owns `OnRadioSchedule` as a distinct packet-authored runtime instead of leaving radio playback buried inside the broad utility row. The simulator already decoded the Maple string plus time value, guarded against re-entry while a session is active, started non-looped radio playback, muted field BGM ownership while the radio session is live, polled the radio owner on a `~2000 ms` timer inside the update loop, hid the window and restored BGM on stop, and emitted start/finish chat notices. This pass pushed the remaining owner seams closer to the client: radio resolution now models the client's split between track metadata lookup and playable sound lookup by trying a dedicated radio-track node first, pulling the display title from the resolved track node, and only then deriving the sound property that feeds the MonoGame audio handle; stop now clears both the live track-node and audio-source descriptors alongside the active playback state; and the radio UI no longer stays in a draggable utility summary panel. The simulator now uses a dedicated `RadioStatusWindow` owner that renders the `ui/UIWindow2.img/Radio` `On` / `Off` indicator art as a top-right HUD widget and exposes the current track title as hover chrome instead of a generic placeholder surface, while the manual `/localutility` packet bridge still advertises the `radio` / `261` aliases that dispatch into this path. The remaining gap is narrower and now mostly StringPool-specific: the simulator still does not reproduce the exact client `StringPool(0x1501)` / `StringPool(0x1502)` format strings and true `MMS_Play` resource-handle path, it still mirrors start/stop chat with simulator-authored text instead of the exact `StringPool(0x14CF/0x14D0)` templates, and the HUD anchor is only a client-shaped right-top approximation rather than a byte-for-byte recreation of `CUIRadio::CreateLayer`'s final `Origin_RT` placement and margin logic. | Packet-owned radio scheduling is more than a sound trigger. It is a client-managed audio/UI/chat lifecycle that temporarily changes BGM ownership and presents user-facing state, so leaving it inside the generic dispatcher row makes the remaining gap look smaller and less coherent than it is. Tracking it explicitly keeps future work anchored to the right packet handler and manager lifecycle instead of folding radio playback into unrelated sound-effect or notice work. | packet-owned radio manager layer (`CUserLocal::OnRadioSchedule`, `CRadioManager::Play`, `CRadioManager::Update`, `CRadioManager::Stop`) |

### 8. Explicit key-config, game-option, and soft-keyboard owner parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Explicit key-config, game-option, and soft-keyboard owner parity | The simulator now carries this owner family more explicitly instead of only relying on generic utility launches. `KeyConfigWindow` already keeps the client-owned main vs quick-slot page split from `UIWindow2.img/KeyConfig`, stages edits until `OK`, preserves `Cancel`, and keeps the quick-slot settings page as a dedicated owner branch rather than a status-bar stub. `OptionMenuWindow` now mirrors the `CUIGameOpt::OnCreate` owner lifecycle more closely by snapshotting current game/system/extra/joypad values on show, keeping staged checkbox and joypad edits local to the dialog session, and only committing them on `OK` while `Cancel` drops the staged owner state. The soft-keyboard path also moved closer to the explicit `CSoftKeyboardDlg` family instead of stopping at helper hit-testing: `SoftKeyboardUI` now loads the client `SoftKeyboard/backgrnd/{0,1}` and `Bt` / `funckey` art, owns dedicated `min` / `max` / `close` / `enter` buttons, keeps a compact vs expanded dialog layout, and routes explicit submit behavior back through the existing login-title, login-utility, and map-transfer owners rather than treating the keyboard as insert/backspace-only chrome. The remaining gap is narrower and now specific: the simulator still does not reproduce the client’s full `CUIKeyConfig::Draw` palette or map layers, `CUIGameOpt::OnCreate` still approximates the exact checkbox roster and config ids that the client copies from `CConfig`, and the soft-keyboard owner still stops short of the true `CSoftKeyboardDlg` tabbed/randomized keypad, switching-character/T9-style button behavior, and exact modal focus-restoration stack after each dialog closes. | This row now tracks real owner behavior rather than unnamed popup existence. Keeping it separate prevents the broader utility-button and constrained-text rows from overstating parity now that key-config, option, and soft-keyboard surfaces have their own staged owner state, page/layout splits, and explicit dialog buttons. The remaining work is concentrated in the last client-specific palette, checkbox-id, randomized-keypad, and modal-stack details rather than in missing owners or missing owner-local button lifecycle. | utility-owner layer (`CUIKeyConfig::OnCreate`, `CUIKeyConfig::Draw`, `CUIGameOpt::OnCreate`, `CUIMacroSysEx::OnCreate`, `CUISoftKeyboard::{GetKeyIndexFromPoint,OnMouseMove,HitTest,UpdateKeyConstraint}`, `CSoftKeyboardDlg::OnCreate`, `CSoftKeyboardDlg::OnButtonClicked`) |

### 9. Tutor, skill-guide, and AP/SP helper parity discovered by `CUserLocal::OnPacket` scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Missing | Tutor summon and message parity | The current backlog only mentions tutor flows as residual packet-dispatch fallout, but targeted IDA decompiles show a distinct client-owned tutor owner that is still absent from the simulator plan. `CUserLocal::OnHireTutor` tears down the prior tutor variant, allocates a `CTutor`, initializes it through a summoned-style world seam at the local avatar position, and tracks the active tutor variant in `CUserLocal::ms_anTutors` rather than treating tutoring as plain UI text. `CUserLocal::OnTutorMsg` then routes either numeric or string payloads plus two packet-owned ints through `CTutor::OnMessage`, which implies timed/tutorial-scripted speech or state changes on that tutor actor rather than ordinary chat-log lines. A focused code search across `HaCreator/MapSimulator` still shows no tutor runtime, actor, or packet owner today. | Leaving tutor flows buried inside the generic local-utility row hides a separate visible helper actor plus packet-authored message lifecycle that can break tutorial or guided-play fields in ways ordinary notice/chat parity will not catch. Tracking the tutor owner explicitly keeps future parity work anchored to the right packet handlers and actor lifecycle instead of treating tutoring as another dialog string. | packet-owned tutor/helper actor layer (`CUserLocal::OnHireTutor`, `CUserLocal::OnTutorMsg`, `CTutor::OnMessage`, summoned-style tutor initialization inside `CUserLocal::OnHireTutor`) |
| Partial | Packet-authored skill-guide and AP/SP helper prompt parity | The simulator now tracks the dedicated `CUserLocal::OnOpenSkillGuide` dispatch as client packet `262`, exposes a `skillguide` alias on the local utility inbox/command seam, packet-opens the skill owner through the existing `UI_Open(3, 1)` path, and explicitly opens the current Aran guide page when the active job has one. `CUserLocal::OnAskAPSPEvent` is also modeled beyond plain text now: packet `273`/`1008` decodes the client-shaped two-Int32 payload, validates it against a dedicated AP/SP receive-context token seam, opens the helper prompt with the exact recovered `StringPool::ms_aString[0x17BA]` copy from `MapleStory.exe`, uses `OK` and `Cancel` labels to match that copy, and simulates the accepted follow-up outpacket `195` with response `6` through a separate send-context token path mirroring the client's `CWvsContext+0x20B4` receive check vs `+0x2030` send field split. The remaining gap is that those two context tokens are still synchronized from the simulator's local player/runtime seam instead of a fuller persisted `CWvsContext` state model that can diverge the receive and send tokens exactly as the client does. | These helper prompts sit between tutorial/script flow and ordinary utility windows. Keeping them explicit prevents the broader local-utility dispatcher row from looking complete once the simulator can only show text or open the guide manually, and it anchors future work to the packet-driven launch/confirm path the client actually uses. | packet-owned helper prompt layer (`CUserLocal::OnOpenSkillGuide`, `CUISkill::OpenCurSkillGuide`, `CUserLocal::OnAskAPSPEvent`) |

### 10. Explicit minimap owner parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Explicit minimap owner parity | The simulator already has a broad `MinimapUI` row, but the client owner was still absent from the backlog tables. Targeted IDA decompiles now show that the minimap has its own concrete owner lifecycle in `CUIMiniMap`: `OnButtonClicked` cycles compact-vs-expanded minimap options, preserves a previous-option restore path, and opens `CWorldMapDlg` directly from the minimap button family rather than treating world-map launch as a generic status-bar or utility-window action. `OnMouseMove` then owns the denser minimap-specific feedback surface by computing tooltip hit rectangles for off-map remote direction markers, live NPC minimap markers, employee markers sourced from `CEmployeePool`, and visible portal markers from `CPortalList`, resolving tooltip strings through `CNpcTemplate`, `CEmployee::GetNameTag`, portal script-property text, and `CItemInfo::GetMapString` fallback data before routing those strings through `CUIToolTip`. The current simulator row already covers marker families, helper toggles, and world-map handoff at a behavior level, so the remaining gap is no longer "make minimap features exist" but "mirror the client-owned `CUIMiniMap` control flow, tooltip ownership, and button semantics explicitly instead of leaving minimap parity anchored only to simulator-side structure." | Naming `CUIMiniMap` explicitly closes the seam-collection gap that backlog 8 itself called out. It gives future minimap fixes a client owner for option cycling, world-map launch ownership, remote-direction helper tooltips, NPC/employee/portal hover routing, and other minimap-only behavior that should not keep being inferred indirectly from `MinimapUI` or `WorldMapUI` alone. | minimap owner layer (`CUIMiniMap::OnButtonClicked`, `CUIMiniMap::OnMouseMove`, `CWorldMapDlg::CreateWorldMapDlg`, `CEmployeePool`, `CPortalList`) |

### 11. Explicit skill-macro owner parity discovered by follow-up IDA lookup

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Explicit skill-macro owner parity | `SkillMacroUI` now uses the explicit owner evidence instead of only borrowing the old `OnCreate` seam: the loader binds the WZ `UIWindow2.img/Skill/macro` owner chrome (`backgrnd2`, `backgrnd3`, `check`, `select`, and `Macroicon/*/icon`), the row hit-testing and draw layout now follow the IDA-recovered `CUIMacroSysEx::Draw` geometry (`select` at `(11, 27 + 41*i)`, skill icons at `(14 + 34*j, 30 + 41*i)`, selected macro icon at `(14, 241)`), the embedded name field now follows the `CCtrlEdit` owner bounds from `CUIMacroSysEx::OnCreate` (`53, 260, 114, 14`), and the owner lifecycle now resets selection on show, keeps the save button disabled until a row is selected, and treats `BtOK` like `CUIMacroSysEx::OnButtonClicked` by committing the selected macro in-place instead of saving and closing the whole window. The remaining gap is narrower and now mostly client-detail work: the simulator still approximates the exact `StringPool` tooltip/notice text that the owner loads for `BtOK`, the final `CCtrlCheckBox` and `CCtrlEdit` focus/ownership quirks beyond this layout and button-state pass, and the last row-local icon state details such as the client's disabled/mouse-over `Macroicon` variants rather than only the normal icon path. | Naming the concrete macro owner keeps this area from looking complete once text entry and macro execution mostly work. It anchors future parity fixes to the actual client dialog that owns macro presentation and button handling instead of leaving macro polish split across generic text-entry and utility-window rows. | skill-macro owner layer (`CUIMacroSysEx::OnCreate`, `CUIMacroSysEx::Draw`, `CUIMacroSysEx::OnButtonClicked`) |

### 12. Packet-owned keymap and pet auto-consume parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned keymap and pet auto-consume initialization parity | `HaCreator/MapSimulator` now has a dedicated packet-owned `CFuncKeyMappedMan` owner seam. The local utility inbox accepts `398/399/400`, dispatches them through the packet-owned utility controller, persists the hydrated state in a dedicated simulator config store, and restores that config during player initialization so packet-owned fallback data exists before the settings and field-hazard layers use it. Packet `398` now decodes and stores the full 89-entry `FUNCKEY_MAPPED` table, clears decoded entries whose id is `22`, snapshots the previous table into an `Old` mirror, and falls back to the persisted simulator config/default path when the packet payload is flagged as local/default ownership. Local `KeyConfigWindow` commits now save the simulator binding fallback into that same store so the packet-owned fallback path is backed by real local config instead of transient runtime state. Packets `399` and `400` now decode packet-owned auto-HP / auto-MP consume item ids, fall back to the persisted config value when the payload sends `0`, and persist the resolved ids; the HP item is also pushed into the existing field-hazard shared-potion seam when an inventory type can be resolved. The remaining gap is narrower: the simulator still does not translate the raw 89-entry client `FUNCKEY_MAPPED` action ids into a full client-faithful live `PlayerInput` remap the way `AdaptVirtualKey` and the rest of the client key owner do, and the stored packet-owned auto-MP item id is not yet consumed by a matching simulator MP auto-potion runtime path. | This row now tracks a real packet-owned initialization owner instead of a missing note. The simulator no longer loses the distinction between packet-owned keymap/bootstrap state and later local UI edits: packet dispatch, fallback persistence, startup hydration, `id == 22` clearing, HP item seeding, and config-backed restore all live under the correct owner seam. Keeping the row `Partial` prevents the remaining full key-action translation work and MP consumption runtime parity from being lost under the already-implemented owner bootstrap. | packet-owned keymap/runtime init layer (`CFuncKeyMappedMan::OnPacket`, `CFuncKeyMappedMan::OnInit`, `CFuncKeyMappedMan::OnPetConsumeItemInit`, `CFuncKeyMappedMan::OnPetConsumeMPItemInit`) |

### 13. Packet-owned anti-macro result parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Packet-owned anti-macro result and launch parity | `HaCreator/MapSimulator` now has a dedicated packet-owned anti-macro bridge in `MapSimulator.PacketOwnedAntiMacroParity.cs`: packet type `1011` is dispatched from the existing packet-owned utility seam, launch packets split normal-vs-admin owners, carry the decoded answer-count into the anti-macro owner seam, treat the client timer as a fixed `60000` ms timeout, decode the remaining payload bytes as the JPEG challenge canvas, hold and release local Ctrl-combo input while the challenge is active, route `4/5/8/10` report branches through status-bar chat plus screenshot persistence, and route `7/9/11` close or result families through a dedicated `AntiMacroNoticeWindow` modal owner that now follows the recovered `CUIAntiMacroNotice` seam instead of the generic utility-feedback path. That notice owner uses `UI/UIWindow2.img/Macro/popup`, `btOK`, `btCancle`, and the recovered `Macro/admin/*` avatar canvases, and the bridge now carries the recovered `StringPool` ids for the notice/chat families (`0xC84`-`0xC9A`, `0x1A65`, `0xC8D`-`0xC91`) through the simulator feedback state. Answer submission also now mirrors the client owner teardown more closely by closing the active challenge locally, preserving the held-combo state while awaiting resolution, and recording a simulated outpacket `117` submission with the remaining timeout snapshot instead of stopping at plain local text entry. The remaining gap is narrower and now mostly about exact client-owned data sources rather than missing seam ownership: the simulator still uses approximate fallback English text instead of the exact localized `StringPool` payloads, its screenshot base-folder path is only an inferred `My Pictures\\MapleStory` stand-in rather than a confirmed `CScreenShot::GetBaseFolder` implementation, and answer submission still ends at a simulated packet state instead of a real outbound/network round-trip. | This row now covers the actual packet-owned anti-macro controller more faithfully: the simulator models owner selection, combo hold lifecycle, JPEG challenge decode, report/result branch routing, screenshot capture, the dedicated modal notice owner, and the client-shaped local submit teardown state instead of leaving those pieces buried behind generic chat or feedback surfaces. Keeping the row `Partial` makes the remaining exact `StringPool`, screenshot-root, and real packet-send fidelity explicit rather than losing them under the now-implemented bridge and modal-owner work. | packet-owned anti-macro controller (`CWvsContext::OnAntiMacroResult`, `CWvsContext::SaveAntiMacroScreenShot`, `CWvsContext::ShowAntiMacroNotice`, `CUIAntiMacroNotice::{CUIAntiMacroNotice,OnCreate,Draw}`, `CUIAntiMacro::SetRet`, `CUIAdminAntiMacro::SetRet`) |

### 14. Anti-macro owner parity discovered by targeted IDA scan

| Status | Area | Gap | Why it matters | Primary seam |
|--------|------|-----|----------------|--------------|
| Partial | Anti-macro challenge dialog parity | `HaCreator/MapSimulator` now has a dedicated anti-macro owner instead of only field-limit item gating. `MapSimulator.PacketOwnedAntiMacroParity.cs` registers a real `AntiMacroChallengeWindow`, and `UI/Windows/AntiMacroChallengeWindow.cs` now mirrors the client owner more closely: the normal anti-macro surface uses the WZ-backed `UI/UIWindow2.img/Macro/step1` frame instead of a generic box, keeps the typed-answer runtime inside the owner with the client-observed 12-character cap, adds the WZ `btOK` chrome as an owner-local submit button, draws the countdown from `UI/UIWindow2.img/Macro/num1/*` at the `CUIAntiMacro::Draw` coordinates recovered from IDA, and keeps the timed challenge image and attempt message inside that owner rather than routing them through notice/chat UI. The remaining gap is narrower and owner-specific: the simulator still approximates the `CCtrlEdit` child with its existing IME/text-input seam instead of a true `CCtrlEdit` control, the attempt text is simulator-authored rather than the exact `StringPool` copy used by `CUIAntiMacro::Draw`, and only the normal `CUIAntiMacro` owner has been moved onto the recovered WZ chrome while the admin variant still falls back to the simulator shell. | Lie-detector or anti-macro checks are more than an item ban. The client presents them as a distinct timed text-entry owner with its own layout, countdown, and attempt messaging, so leaving this surface untracked makes progression/utility parity look more complete than it is and hides a tutorial/event-facing failure mode that will not be covered by field-limit notices alone. | timed anti-macro utility owner (`CUIAntiMacro::OnCreate`, `CUIAntiMacro::Update`, `CUIAntiMacro::Draw`, existing simulator seam contrast: `FieldInteractionRestrictionEvaluator.cs`) |

## Priority Order

If the goal is visible parity first, the next work in this area should be sequenced like this:

1. Window usefulness pass:
   Finish minimap, inventory, equip, and quest-log interactions so the existing windows become usable client-facing tools rather than frame shells.
2. Progression correctness pass:
   Tighten ability/stat formulas, equip presentation, and inventory-backed validations so the windows reflect the runtime accurately.
3. Packet-dispatch pass:
   Add the `CUserLocal::OnPacket`-style bridge for packet-owned local utility/event handlers before layering more one-off launches onto chat commands or button callbacks.
4. Packet-overlay pass:
   Add the packet-owned fade, balloon, damage-meter, field-hazard, quest-guide, and delivery/class-competition surfaces after the base dispatcher exists so those handlers do not stay hidden behind one catch-all row.
5. External-service-stage pass:
   Add dedicated Cash Shop and ITC stage owners before treating cash-service parity as an extension of `CAdminShopDlg`, `OnGoToCommoditySN`, or the local utility bridge.
6. Cash-service subwindow pass:
   Add the avatar-preview, locker, inventory, list, status, One-a-Day, and cash-trading child owners after the base service stages exist so cash-item preview and service-pane behavior do not stay folded into one coarse stage row.
7. Cash-service wishlist pass:
   Add the dedicated `CUIAdminShopWishList`-style owner after the base service stages and child panes exist so wish-list search/toggle behavior does not stay buried inside the coarse admin-shop row.
8. Radio-manager pass:
   Add the packet-owned `OnRadioSchedule` bridge plus a real `CRadioManager`-style audio/UI/chat owner before treating radio playback as another event-sound alias inside the local utility dispatcher.
9. Tutor/helper pass:
   Add the packet-owned tutor actor plus the `OnOpenSkillGuide` / `OnAskAPSPEvent` helper-owner behavior before broader tutorial/guidance flows continue to rely on generic notices or manual window opens.
10. Keymap-init pass:
   Add the packet-owned `CFuncKeyMappedMan` bridge for function-key hydration and pet auto-consume defaults before treating key config, quick-slot state, or auto-potion behavior as fully client-owned.
11. Anti-macro packet/result pass:
   Add the `CWvsContext::OnAntiMacroResult` bridge, screenshot/notice side effects, combo-hold lifecycle, and admin-vs-normal owner split before treating lie-detector parity as covered by an owner shell alone.
12. Anti-macro owner pass:
   Add the dedicated `CUIAntiMacro`-style timed text-entry owner after the packet/result bridge exists so the launch and timeout flow does not stay inferred from item bans or generic notice text.
13. Utility-owner refinement pass:
   Tighten `CUIKeyConfig`, `CUIGameOpt`, and `CSoftKeyboardDlg` lifecycle parity after the broad utility buttons work so key-binding, option, and soft-keyboard behavior does not stay hidden inside the generic cursor/utility row.
14. Macro-owner refinement pass:
   Tighten `CUIMacroSysEx` draw/button-owner parity after the macro surface and shared text-entry seams exist so macro behavior does not stay documented only as input polish.
15. Utility feedback pass:
   Complete pickup notices, soft-keyboard or constrained text-entry behavior, macro behavior, cursor states, and status-bar utility-button behavior.
16. Client seam pass:
   Capture direct IDA seams for minimap, inventory, equip, and quest windows so future parity fixes are anchored to named client owners instead of simulator-only structure.

## Working Rule For Future Updates

Update this file directly, and always classify each item as one of:

- `Implemented`
- `Partial`
- `Missing`

That keeps the backlog honest and prevents already-shipped work from reappearing as "missing".

