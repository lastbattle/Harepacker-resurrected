<Project>
  <PropertyGroup>
    <!-- Prevent redirected build/intermediate outputs under project-local artifacts/ from being compiled as source. -->
    <DefaultItemExcludes>$(DefaultItemExcludes);**\artifacts\**;**\obj_codex*\**</DefaultItemExcludes>
  </PropertyGroup>
</Project>
