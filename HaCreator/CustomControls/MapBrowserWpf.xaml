<UserControl x:Class="HaCreator.CustomControls.MapBrowserWpf"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dialogLoc="clr-namespace:HaCreator.GUI.Localization"
             MinWidth="1"
             MinHeight="1"
             Background="{DynamicResource HareCanvasBrush}">
    <UserControl.Resources>
        <ResourceDictionary>
        <ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="/HaSharedLibrary;component/GUI/Themes/HarepackerTheme.xaml"/>
        </ResourceDictionary.MergedDictionaries>
        <SolidColorBrush x:Key="AccentBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="DividerBrush" Color="#DCE3EC"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#64748B"/>

        <Style x:Key="MapListItemStyle" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Card" Padding="10,8" CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Width="20" Height="20" CornerRadius="4" Background="#EFF6FF">
                                    <Path Width="11" Height="13" Stretch="Fill" Fill="#2563EB"
                                          Data="M2,1 L9,1 L12,4 L12,13 L2,13 Z M9,1 L9,4 L12,4 M4,7 L10,7 M4,10 L9,10"/>
                                </Border>
                                <TextBlock Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Center"
                                           Foreground="#334155" Text="{Binding}" TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Card" Property="Background" Value="#F1F5F9"/>
                                <Setter TargetName="Card" Property="BorderBrush" Value="#DCE3EC"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Card" Property="Background" Value="#EFF6FF"/>
                                <Setter TargetName="Card" Property="BorderBrush" Value="#93C5FD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="layoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition x:Name="previewRow" Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border x:Name="previewPanel"
                Grid.Row="0"
                Height="168"
                Margin="0,0,0,10"
                Background="White"
                BorderBrush="{StaticResource DividerBrush}"
                BorderThickness="1"
                CornerRadius="7"
                ClipToBounds="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="32"/>
                </Grid.RowDefinitions>

        <Border Margin="8" Background="{StaticResource HareSurfaceAltBrush}" CornerRadius="5">
                    <Image x:Name="minimapBox"
                           Margin="6"
                           Stretch="Uniform"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"/>
                </Border>

        <Border Grid.Row="1" Background="{StaticResource HareSurfaceAltBrush}" BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,1,0,0">
                    <TextBlock Margin="10,0" VerticalAlignment="Center" FontSize="11"
                               Foreground="{StaticResource MutedTextBrush}"
                               Text="{Binding SelectedItem, ElementName=mapNamesBox, FallbackValue=Select a map to preview}"
                               TextTrimming="CharacterEllipsis"/>
                </Border>

                <Border x:Name="panel_linkWarning"
                        Grid.RowSpan="2"
                        Visibility="Collapsed"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="12"
                        Padding="8,5"
                        Background="#FFFFF7ED"
                        BorderBrush="#FDBA74"
                        BorderThickness="1"
                        CornerRadius="4">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Foreground="#C2410C" Text="{dialogLoc:DialogText Dialog_Linked_to_B4B7FE}"/>
                        <TextBlock x:Name="label_linkMapId" FontWeight="SemiBold" Foreground="#C2410C"/>
                    </StackPanel>
                </Border>

                <Border x:Name="panel_mapExistWarning"
                        Grid.RowSpan="2"
                        Visibility="Collapsed"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Padding="10,7"
                        Background="#FFFEF2F2"
                        BorderBrush="#FCA5A5"
                        BorderThickness="1"
                        CornerRadius="4">
                    <TextBlock Foreground="#B91C1C" FontWeight="SemiBold" Text="{dialogLoc:DialogText Dialog_Map_data_is_unavailable_537DF8}"/>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource HareSurfaceBrush}" BorderBrush="{StaticResource DividerBrush}" BorderThickness="1" CornerRadius="7">
            <ListBox x:Name="mapNamesBox"
                     Margin="5"
                     Padding="0"
                     Background="Transparent"
                     BorderThickness="0"
                     ItemContainerStyle="{StaticResource MapListItemStyle}"
                     HorizontalAlignment="Stretch"
                     VerticalAlignment="Stretch"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     SelectionChanged="MapNamesBox_SelectionChanged"/>
        </Border>
    </Grid>
</UserControl>
