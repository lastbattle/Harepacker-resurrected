<Window x:Class="HaCreator.GUI.UserSettingsForm" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dialogLoc="clr-namespace:HaCreator.GUI.Localization" Title="{dialogLoc:DialogText Dialog_HaCreator_settings_762561}" Width="720" Height="720" MinHeight="520" WindowStartupLocation="CenterOwner" Background="{DynamicResource HareCanvasBrush}" FontFamily="Segoe UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/HaSharedLibrary;component/GUI/Themes/HarepackerTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource HareToolbarStyle}" Padding="20,16">
        </Border>
        <TabControl Grid.Row="1" Margin="20">
            <TabItem Header="{dialogLoc:DialogText Dialog_General_C910D4}">
                <ScrollViewer>
                    <StackPanel Margin="12">
                        <CheckBox x:Name="errors" Content="{dialogLoc:DialogText Dialog_Show_error_summaries_7C99AC}"/>
                        <CheckBox x:Name="clip" Content="{dialogLoc:DialogText Dialog_Clip_text_to_bounds_C0904C}"/>
                        <CheckBox x:Name="fixFootholds" Content="{dialogLoc:DialogText Dialog_Fix_foothold_mispositions_D4B68F}"/>
                        <CheckBox x:Name="invertLayers" Content="{dialogLoc:DialogText Dialog_Invert_layer_up_down_8D7820}"/>
                        <CheckBox x:Name="backups" Content="{dialogLoc:DialogText Dialog_Enable_automatic_backups_217ACD}"/>
                        <TextBlock Text="{dialogLoc:DialogText Dialog_Font_name_2CD8F5}" Margin="0,12,0,4"/>
                        <TextBox x:Name="fontName"/>
                        <StackPanel x:Name="generalValues" Margin="0,12,0,0"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="{dialogLoc:DialogText Dialog_Behavior_EDF8D3}">
                <ScrollViewer>
                    <StackPanel x:Name="behaviorValues" Margin="12"/>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="{dialogLoc:DialogText Dialog_Colors_88C45D}">
                <ScrollViewer>
                    <WrapPanel x:Name="colorsPanel" Margin="12"/>
                </ScrollViewer>
            </TabItem>
        </TabControl>
        <Border Grid.Row="2" Style="{StaticResource HareToolbarStyle}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button MinWidth="88" IsCancel="True" Content="{dialogLoc:DialogText Dialog_Cancel_19766E}"/>
                <Button Margin="8,0,0,0" MinWidth="88" IsDefault="True" Style="{StaticResource HarePrimaryButtonStyle}" Click="Save_Click" Content="{dialogLoc:DialogText Dialog_Save_1509F5}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
