<UserControl x:Class="HaCreator.GUI.EditorPanels.TilePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:HaCreator.GUI.EditorPanels">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/HaSharedLibrary;component/GUI/Themes/HarepackerTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="{StaticResource HareCanvasBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Style="{StaticResource HareToolbarStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Style="{StaticResource HareSectionTitleStyle}" Text="Tile set"/>
                    <ComboBox x:Name="tileSetList" Margin="0,5,8,0"
                              SelectionChanged="TileSetList_SelectionChanged"/>
                </StackPanel>
                <Button x:Name="tileBrowse" Grid.Column="1" Margin="0,21,0,0"
                        Style="{StaticResource HarePrimaryButtonStyle}"
                        Click="TileBrowse_Click" Content="Manage…"/>
            </Grid>
        </Border>
        <local:AssetGallery x:Name="tileImagesContainer" Grid.Row="1"
                            ItemActivated="TileImagesContainer_ItemActivated"/>
    </Grid>
</UserControl>
